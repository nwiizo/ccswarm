Analyzing Rust code similarity...

=== Function Similarity ===
Checking 154 files for duplicates...

Duplicates in ./crates/ccswarm/src/cli/command_registry.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/cli/command_registry.rs:26-34 method new <-> ./crates/ccswarm/src/cli/command_registry.rs:382-390 method execute
  Similarity: 78.33%
  Classes: CommandRegistry <-> CommandRegistry

[36m--- ./crates/ccswarm/src/cli/command_registry.rs:new (lines 26-34) ---[0m
    pub fn new() -> Self {
        let mut registry = Self {
            handlers: HashMap::new(),
        };

        // Register all commands using a macro to eliminate duplication
        registry.register_commands();
        registry
    }

[36m--- ./crates/ccswarm/src/cli/command_registry.rs:execute (lines 382-390) ---[0m
    pub async fn execute(&self, runner: &CliRunner, command: &Commands) -> Result<()> {
        let command_name = Self::get_command_name(command);

        if let Some(handler) = self.handlers.get(command_name) {
            handler(runner, command).await
        } else {
            anyhow::bail!("Unknown command: {}", command_name)
        }
    }


Duplicates in ./crates/ccswarm/src/tests/auto_accept_tests.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/tests/auto_accept_tests.rs:204-250 function test_should_auto_accept_restricted_files <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:363-395 function test_validation_execution_time
  Similarity: 92.97%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_restricted_files (lines 204-250) ---[0m
fn test_should_auto_accept_restricted_files() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::EditFile],
        restricted_files: vec!["Cargo.toml".to_string(), "*.sql".to_string()],
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    // Test exact match restriction
    let operation1 = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit Cargo.toml".to_string(),
        affected_files: vec![PathBuf::from("Cargo.toml")],
        commands: vec!["sed -i 's/old/new/g' Cargo.toml".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision1 = engine.should_auto_accept(&operation1).unwrap();
    match decision1 {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("restricted pattern"));
        }
        _ => panic!("Should reject restricted file Cargo.toml"),
    }
    
    // Test pattern match restriction
    let operation2 = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit SQL file".to_string(),
        affected_files: vec![PathBuf::from("migration.sql")],
        commands: vec!["sed -i 's/old/new/g' migration.sql".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision2 = engine.should_auto_accept(&operation2).unwrap();
    match decision2 {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("restricted pattern"));
        }
        _ => panic!("Should reject restricted pattern *.sql"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_validation_execution_time (lines 363-395) ---[0m
fn test_validation_execution_time() {
    let config = AutoAcceptConfig {
        max_execution_time: 60, // 1 minute limit
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Test operation".to_string(),
        affected_files: vec![],
        commands: vec!["ls".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    // Test within time limit
    let result1 = engine.validate_changes(&operation, 30).unwrap();
    match result1 {
        ValidationResult::Valid => {} // Expected
        ValidationResult::Invalid(_) => panic!("Should be valid within time limit"),
    }
    
    // Test exceeding time limit
    let result2 = engine.validate_changes(&operation, 120).unwrap(); // 2 minutes > 1 minute limit
    match result2 {
        ValidationResult::Valid => panic!("Should be invalid when exceeding time limit"),
        ValidationResult::Invalid(issues) => {
            assert!(issues.iter().any(|issue| issue.contains("Execution time exceeded")));
        }
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:204-250 function test_should_auto_accept_restricted_files <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:332-360 function test_operation_history
  Similarity: 85.42%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_restricted_files (lines 204-250) ---[0m
fn test_should_auto_accept_restricted_files() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::EditFile],
        restricted_files: vec!["Cargo.toml".to_string(), "*.sql".to_string()],
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    // Test exact match restriction
    let operation1 = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit Cargo.toml".to_string(),
        affected_files: vec![PathBuf::from("Cargo.toml")],
        commands: vec!["sed -i 's/old/new/g' Cargo.toml".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision1 = engine.should_auto_accept(&operation1).unwrap();
    match decision1 {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("restricted pattern"));
        }
        _ => panic!("Should reject restricted file Cargo.toml"),
    }
    
    // Test pattern match restriction
    let operation2 = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit SQL file".to_string(),
        affected_files: vec![PathBuf::from("migration.sql")],
        commands: vec!["sed -i 's/old/new/g' migration.sql".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision2 = engine.should_auto_accept(&operation2).unwrap();
    match decision2 {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("restricted pattern"));
        }
        _ => panic!("Should reject restricted pattern *.sql"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_operation_history (lines 332-360) ---[0m
fn test_operation_history() {
    let mut engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
    let session_id = "test-session".to_string();
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Test operation".to_string(),
        affected_files: vec![],
        commands: vec!["ls".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    // Initially no history
    assert!(engine.get_operation_history(&session_id).is_none());
    
    // Record operation
    engine.record_operation(session_id.clone(), operation.clone());
    
    // Should have history now
    let history = engine.get_operation_history(&session_id).unwrap();
    assert_eq!(history.len(), 1);
    assert_eq!(history[0].operation_type, OperationType::ReadFile);
    
    // Clear history
    engine.clear_history(&session_id);
    assert!(engine.get_operation_history(&session_id).is_none());
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:204-250 function test_should_auto_accept_restricted_files <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:398-419 function test_task_integration
  Similarity: 88.01%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_restricted_files (lines 204-250) ---[0m
fn test_should_auto_accept_restricted_files() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::EditFile],
        restricted_files: vec!["Cargo.toml".to_string(), "*.sql".to_string()],
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    // Test exact match restriction
    let operation1 = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit Cargo.toml".to_string(),
        affected_files: vec![PathBuf::from("Cargo.toml")],
        commands: vec!["sed -i 's/old/new/g' Cargo.toml".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision1 = engine.should_auto_accept(&operation1).unwrap();
    match decision1 {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("restricted pattern"));
        }
        _ => panic!("Should reject restricted file Cargo.toml"),
    }
    
    // Test pattern match restriction
    let operation2 = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit SQL file".to_string(),
        affected_files: vec![PathBuf::from("migration.sql")],
        commands: vec!["sed -i 's/old/new/g' migration.sql".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision2 = engine.should_auto_accept(&operation2).unwrap();
    match decision2 {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("restricted pattern"));
        }
        _ => panic!("Should reject restricted pattern *.sql"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_task_integration (lines 398-419) ---[0m
fn test_task_integration() {
    let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
    
    let task = Task {
        id: "test-task".to_string(),
        description: "Run tests".to_string(),
        details: Some("Run all unit tests".to_string()),
        priority: Priority::Medium,
        task_type: TaskType::Testing,
        estimated_duration: Some(300),
    };
    
    let commands = vec!["cargo test".to_string()];
    let operation = engine.analyze_operation(&commands, Some(&task)).unwrap();
    
    assert_eq!(operation.operation_type, OperationType::RunTests);
    assert!(operation.task.is_some());
    
    let op_task = operation.task.unwrap();
    assert_eq!(op_task.id, "test-task");
    assert_eq!(op_task.task_type, TaskType::Testing);
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:171-201 function test_should_auto_accept_too_many_files <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:253-283 function test_should_auto_accept_success
  Similarity: 93.98%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_too_many_files (lines 171-201) ---[0m
fn test_should_auto_accept_too_many_files() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::EditFile],
        max_file_changes: 2, // Low limit for testing
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit multiple files".to_string(),
        affected_files: vec![
            PathBuf::from("file1.rs"),
            PathBuf::from("file2.rs"),
            PathBuf::from("file3.rs"), // 3 files > 2 limit
        ],
        commands: vec!["sed -i 's/old/new/g' *.rs".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Too many file changes"));
        }
        _ => panic!("Should reject when too many files affected"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_success (lines 253-283) ---[0m
fn test_should_auto_accept_success() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::FormatCode, OperationType::LintCode],
        max_file_changes: 10,
        require_tests_pass: true,
        require_clean_git: false, // Disable for testing
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::FormatCode,
        description: "Format code".to_string(),
        affected_files: vec![PathBuf::from("src/main.rs")],
        commands: vec!["cargo fmt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Accept(conditions) => {
            assert!(conditions.contains(&"Tests must pass".to_string()));
        }
        AutoAcceptDecision::Reject(reason) => {
            panic!("Should accept safe operation: {}", reason);
        }
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:171-201 function test_should_auto_accept_too_many_files <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:204-250 function test_should_auto_accept_restricted_files
  Similarity: 74.48%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_too_many_files (lines 171-201) ---[0m
fn test_should_auto_accept_too_many_files() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::EditFile],
        max_file_changes: 2, // Low limit for testing
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit multiple files".to_string(),
        affected_files: vec![
            PathBuf::from("file1.rs"),
            PathBuf::from("file2.rs"),
            PathBuf::from("file3.rs"), // 3 files > 2 limit
        ],
        commands: vec!["sed -i 's/old/new/g' *.rs".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Too many file changes"));
        }
        _ => panic!("Should reject when too many files affected"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_restricted_files (lines 204-250) ---[0m
fn test_should_auto_accept_restricted_files() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::EditFile],
        restricted_files: vec!["Cargo.toml".to_string(), "*.sql".to_string()],
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    // Test exact match restriction
    let operation1 = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit Cargo.toml".to_string(),
        affected_files: vec![PathBuf::from("Cargo.toml")],
        commands: vec!["sed -i 's/old/new/g' Cargo.toml".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision1 = engine.should_auto_accept(&operation1).unwrap();
    match decision1 {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("restricted pattern"));
        }
        _ => panic!("Should reject restricted file Cargo.toml"),
    }
    
    // Test pattern match restriction
    let operation2 = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit SQL file".to_string(),
        affected_files: vec![PathBuf::from("migration.sql")],
        commands: vec!["sed -i 's/old/new/g' migration.sql".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision2 = engine.should_auto_accept(&operation2).unwrap();
    match decision2 {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("restricted pattern"));
        }
        _ => panic!("Should reject restricted pattern *.sql"),
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:204-250 function test_should_auto_accept_restricted_files <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:253-283 function test_should_auto_accept_success
  Similarity: 72.69%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_restricted_files (lines 204-250) ---[0m
fn test_should_auto_accept_restricted_files() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::EditFile],
        restricted_files: vec!["Cargo.toml".to_string(), "*.sql".to_string()],
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    // Test exact match restriction
    let operation1 = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit Cargo.toml".to_string(),
        affected_files: vec![PathBuf::from("Cargo.toml")],
        commands: vec!["sed -i 's/old/new/g' Cargo.toml".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision1 = engine.should_auto_accept(&operation1).unwrap();
    match decision1 {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("restricted pattern"));
        }
        _ => panic!("Should reject restricted file Cargo.toml"),
    }
    
    // Test pattern match restriction
    let operation2 = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit SQL file".to_string(),
        affected_files: vec![PathBuf::from("migration.sql")],
        commands: vec!["sed -i 's/old/new/g' migration.sql".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision2 = engine.should_auto_accept(&operation2).unwrap();
    match decision2 {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("restricted pattern"));
        }
        _ => panic!("Should reject restricted pattern *.sql"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_success (lines 253-283) ---[0m
fn test_should_auto_accept_success() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::FormatCode, OperationType::LintCode],
        max_file_changes: 10,
        require_tests_pass: true,
        require_clean_git: false, // Disable for testing
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::FormatCode,
        description: "Format code".to_string(),
        affected_files: vec![PathBuf::from("src/main.rs")],
        commands: vec!["cargo fmt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Accept(conditions) => {
            assert!(conditions.contains(&"Tests must pass".to_string()));
        }
        AutoAcceptDecision::Reject(reason) => {
            panic!("Should accept safe operation: {}", reason);
        }
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:115-140 function test_should_auto_accept_untrusted_operation <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:171-201 function test_should_auto_accept_too_many_files
  Similarity: 96.80%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_untrusted_operation (lines 115-140) ---[0m
fn test_should_auto_accept_untrusted_operation() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::ReadFile], // Only read files trusted
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::WriteFile, // Not in trusted list
        description: "Write a file".to_string(),
        affected_files: vec![],
        commands: vec!["echo 'test' > file.txt".to_string()],
        risk_level: 4,
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("not in trusted operations"));
        }
        _ => panic!("Should reject untrusted operations"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_too_many_files (lines 171-201) ---[0m
fn test_should_auto_accept_too_many_files() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::EditFile],
        max_file_changes: 2, // Low limit for testing
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit multiple files".to_string(),
        affected_files: vec![
            PathBuf::from("file1.rs"),
            PathBuf::from("file2.rs"),
            PathBuf::from("file3.rs"), // 3 files > 2 limit
        ],
        commands: vec!["sed -i 's/old/new/g' *.rs".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Too many file changes"));
        }
        _ => panic!("Should reject when too many files affected"),
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:143-168 function test_should_auto_accept_high_risk <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:171-201 function test_should_auto_accept_too_many_files
  Similarity: 96.80%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_high_risk (lines 143-168) ---[0m
fn test_should_auto_accept_high_risk() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::DeleteFile], // Allow delete for this test
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::DeleteFile,
        description: "Delete files".to_string(),
        affected_files: vec![],
        commands: vec!["rm important_file.txt".to_string()],
        risk_level: 8, // High risk
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Risk level too high"));
        }
        _ => panic!("Should reject high-risk operations"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_too_many_files (lines 171-201) ---[0m
fn test_should_auto_accept_too_many_files() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::EditFile],
        max_file_changes: 2, // Low limit for testing
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit multiple files".to_string(),
        affected_files: vec![
            PathBuf::from("file1.rs"),
            PathBuf::from("file2.rs"),
            PathBuf::from("file3.rs"), // 3 files > 2 limit
        ],
        commands: vec!["sed -i 's/old/new/g' *.rs".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Too many file changes"));
        }
        _ => panic!("Should reject when too many files affected"),
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:87-112 function test_should_auto_accept_emergency_stop <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:171-201 function test_should_auto_accept_too_many_files
  Similarity: 96.52%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_emergency_stop (lines 87-112) ---[0m
fn test_should_auto_accept_emergency_stop() {
    let config = AutoAcceptConfig {
        enabled: true,
        emergency_stop: true,
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Read a file".to_string(),
        affected_files: vec![],
        commands: vec!["cat test.txt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Emergency stop"));
        }
        _ => panic!("Should reject when emergency stop is active"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_too_many_files (lines 171-201) ---[0m
fn test_should_auto_accept_too_many_files() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::EditFile],
        max_file_changes: 2, // Low limit for testing
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit multiple files".to_string(),
        affected_files: vec![
            PathBuf::from("file1.rs"),
            PathBuf::from("file2.rs"),
            PathBuf::from("file3.rs"), // 3 files > 2 limit
        ],
        commands: vec!["sed -i 's/old/new/g' *.rs".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Too many file changes"));
        }
        _ => panic!("Should reject when too many files affected"),
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:204-250 function test_should_auto_accept_restricted_files <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:286-306 function test_pattern_matching
  Similarity: 80.62%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_restricted_files (lines 204-250) ---[0m
fn test_should_auto_accept_restricted_files() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::EditFile],
        restricted_files: vec!["Cargo.toml".to_string(), "*.sql".to_string()],
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    // Test exact match restriction
    let operation1 = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit Cargo.toml".to_string(),
        affected_files: vec![PathBuf::from("Cargo.toml")],
        commands: vec!["sed -i 's/old/new/g' Cargo.toml".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision1 = engine.should_auto_accept(&operation1).unwrap();
    match decision1 {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("restricted pattern"));
        }
        _ => panic!("Should reject restricted file Cargo.toml"),
    }
    
    // Test pattern match restriction
    let operation2 = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit SQL file".to_string(),
        affected_files: vec![PathBuf::from("migration.sql")],
        commands: vec!["sed -i 's/old/new/g' migration.sql".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision2 = engine.should_auto_accept(&operation2).unwrap();
    match decision2 {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("restricted pattern"));
        }
        _ => panic!("Should reject restricted pattern *.sql"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_pattern_matching (lines 286-306) ---[0m
fn test_pattern_matching() {
    let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
    
    // Test exact match
    assert!(engine.matches_pattern("Cargo.toml", "Cargo.toml"));
    assert!(!engine.matches_pattern("Cargo.lock", "Cargo.toml"));
    
    // Test suffix wildcard
    assert!(engine.matches_pattern("test.sql", "*.sql"));
    assert!(engine.matches_pattern("migration.sql", "*.sql"));
    assert!(!engine.matches_pattern("test.rs", "*.sql"));
    
    // Test prefix wildcard
    assert!(engine.matches_pattern("config.toml", "*.toml"));
    assert!(!engine.matches_pattern("config.json", "*.toml"));
    
    // Test directory patterns
    assert!(engine.matches_pattern("src/migrations/001.sql", "**/migrations/*"));
    assert!(engine.matches_pattern("db/migrations/init.sql", "**/migrations/*"));
    assert!(!engine.matches_pattern("src/models/user.rs", "**/migrations/*"));
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:60-84 function test_should_auto_accept_disabled <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:171-201 function test_should_auto_accept_too_many_files
  Similarity: 96.52%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_disabled (lines 60-84) ---[0m
fn test_should_auto_accept_disabled() {
    let config = AutoAcceptConfig {
        enabled: false,
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Read a file".to_string(),
        affected_files: vec![],
        commands: vec!["cat test.txt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Auto-accept is disabled"));
        }
        _ => panic!("Should reject when disabled"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_too_many_files (lines 171-201) ---[0m
fn test_should_auto_accept_too_many_files() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::EditFile],
        max_file_changes: 2, // Low limit for testing
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit multiple files".to_string(),
        affected_files: vec![
            PathBuf::from("file1.rs"),
            PathBuf::from("file2.rs"),
            PathBuf::from("file3.rs"), // 3 files > 2 limit
        ],
        commands: vec!["sed -i 's/old/new/g' *.rs".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Too many file changes"));
        }
        _ => panic!("Should reject when too many files affected"),
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:115-140 function test_should_auto_accept_untrusted_operation <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:204-250 function test_should_auto_accept_restricted_files
  Similarity: 74.00%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_untrusted_operation (lines 115-140) ---[0m
fn test_should_auto_accept_untrusted_operation() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::ReadFile], // Only read files trusted
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::WriteFile, // Not in trusted list
        description: "Write a file".to_string(),
        affected_files: vec![],
        commands: vec!["echo 'test' > file.txt".to_string()],
        risk_level: 4,
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("not in trusted operations"));
        }
        _ => panic!("Should reject untrusted operations"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_restricted_files (lines 204-250) ---[0m
fn test_should_auto_accept_restricted_files() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::EditFile],
        restricted_files: vec!["Cargo.toml".to_string(), "*.sql".to_string()],
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    // Test exact match restriction
    let operation1 = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit Cargo.toml".to_string(),
        affected_files: vec![PathBuf::from("Cargo.toml")],
        commands: vec!["sed -i 's/old/new/g' Cargo.toml".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision1 = engine.should_auto_accept(&operation1).unwrap();
    match decision1 {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("restricted pattern"));
        }
        _ => panic!("Should reject restricted file Cargo.toml"),
    }
    
    // Test pattern match restriction
    let operation2 = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit SQL file".to_string(),
        affected_files: vec![PathBuf::from("migration.sql")],
        commands: vec!["sed -i 's/old/new/g' migration.sql".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision2 = engine.should_auto_accept(&operation2).unwrap();
    match decision2 {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("restricted pattern"));
        }
        _ => panic!("Should reject restricted pattern *.sql"),
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:143-168 function test_should_auto_accept_high_risk <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:204-250 function test_should_auto_accept_restricted_files
  Similarity: 74.00%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_high_risk (lines 143-168) ---[0m
fn test_should_auto_accept_high_risk() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::DeleteFile], // Allow delete for this test
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::DeleteFile,
        description: "Delete files".to_string(),
        affected_files: vec![],
        commands: vec!["rm important_file.txt".to_string()],
        risk_level: 8, // High risk
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Risk level too high"));
        }
        _ => panic!("Should reject high-risk operations"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_restricted_files (lines 204-250) ---[0m
fn test_should_auto_accept_restricted_files() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::EditFile],
        restricted_files: vec!["Cargo.toml".to_string(), "*.sql".to_string()],
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    // Test exact match restriction
    let operation1 = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit Cargo.toml".to_string(),
        affected_files: vec![PathBuf::from("Cargo.toml")],
        commands: vec!["sed -i 's/old/new/g' Cargo.toml".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision1 = engine.should_auto_accept(&operation1).unwrap();
    match decision1 {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("restricted pattern"));
        }
        _ => panic!("Should reject restricted file Cargo.toml"),
    }
    
    // Test pattern match restriction
    let operation2 = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit SQL file".to_string(),
        affected_files: vec![PathBuf::from("migration.sql")],
        commands: vec!["sed -i 's/old/new/g' migration.sql".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision2 = engine.should_auto_accept(&operation2).unwrap();
    match decision2 {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("restricted pattern"));
        }
        _ => panic!("Should reject restricted pattern *.sql"),
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:87-112 function test_should_auto_accept_emergency_stop <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:253-283 function test_should_auto_accept_success
  Similarity: 94.02%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_emergency_stop (lines 87-112) ---[0m
fn test_should_auto_accept_emergency_stop() {
    let config = AutoAcceptConfig {
        enabled: true,
        emergency_stop: true,
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Read a file".to_string(),
        affected_files: vec![],
        commands: vec!["cat test.txt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Emergency stop"));
        }
        _ => panic!("Should reject when emergency stop is active"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_success (lines 253-283) ---[0m
fn test_should_auto_accept_success() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::FormatCode, OperationType::LintCode],
        max_file_changes: 10,
        require_tests_pass: true,
        require_clean_git: false, // Disable for testing
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::FormatCode,
        description: "Format code".to_string(),
        affected_files: vec![PathBuf::from("src/main.rs")],
        commands: vec!["cargo fmt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Accept(conditions) => {
            assert!(conditions.contains(&"Tests must pass".to_string()));
        }
        AutoAcceptDecision::Reject(reason) => {
            panic!("Should accept safe operation: {}", reason);
        }
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:115-140 function test_should_auto_accept_untrusted_operation <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:253-283 function test_should_auto_accept_success
  Similarity: 93.98%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_untrusted_operation (lines 115-140) ---[0m
fn test_should_auto_accept_untrusted_operation() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::ReadFile], // Only read files trusted
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::WriteFile, // Not in trusted list
        description: "Write a file".to_string(),
        affected_files: vec![],
        commands: vec!["echo 'test' > file.txt".to_string()],
        risk_level: 4,
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("not in trusted operations"));
        }
        _ => panic!("Should reject untrusted operations"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_success (lines 253-283) ---[0m
fn test_should_auto_accept_success() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::FormatCode, OperationType::LintCode],
        max_file_changes: 10,
        require_tests_pass: true,
        require_clean_git: false, // Disable for testing
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::FormatCode,
        description: "Format code".to_string(),
        affected_files: vec![PathBuf::from("src/main.rs")],
        commands: vec!["cargo fmt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Accept(conditions) => {
            assert!(conditions.contains(&"Tests must pass".to_string()));
        }
        AutoAcceptDecision::Reject(reason) => {
            panic!("Should accept safe operation: {}", reason);
        }
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:143-168 function test_should_auto_accept_high_risk <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:253-283 function test_should_auto_accept_success
  Similarity: 93.98%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_high_risk (lines 143-168) ---[0m
fn test_should_auto_accept_high_risk() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::DeleteFile], // Allow delete for this test
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::DeleteFile,
        description: "Delete files".to_string(),
        affected_files: vec![],
        commands: vec!["rm important_file.txt".to_string()],
        risk_level: 8, // High risk
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Risk level too high"));
        }
        _ => panic!("Should reject high-risk operations"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_success (lines 253-283) ---[0m
fn test_should_auto_accept_success() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::FormatCode, OperationType::LintCode],
        max_file_changes: 10,
        require_tests_pass: true,
        require_clean_git: false, // Disable for testing
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::FormatCode,
        description: "Format code".to_string(),
        affected_files: vec![PathBuf::from("src/main.rs")],
        commands: vec!["cargo fmt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Accept(conditions) => {
            assert!(conditions.contains(&"Tests must pass".to_string()));
        }
        AutoAcceptDecision::Reject(reason) => {
            panic!("Should accept safe operation: {}", reason);
        }
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:6-26 function test_auto_accept_config_default <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:204-250 function test_should_auto_accept_restricted_files
  Similarity: 77.58%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_auto_accept_config_default (lines 6-26) ---[0m
fn test_auto_accept_config_default() {
    let config = AutoAcceptConfig::default();
    
    // Should be disabled by default for safety
    assert!(!config.enabled);
    assert!(!config.emergency_stop);
    assert!(config.require_tests_pass);
    assert!(config.require_clean_git);
    assert_eq!(config.max_file_changes, 5);
    assert_eq!(config.max_execution_time, 300);
    
    // Should have safe default trusted operations
    assert!(config.trusted_operations.contains(&OperationType::ReadFile));
    assert!(config.trusted_operations.contains(&OperationType::FormatCode));
    assert!(config.trusted_operations.contains(&OperationType::RunTests));
    assert!(config.trusted_operations.contains(&OperationType::LintCode));
    
    // Should have restricted file patterns
    assert!(config.restricted_files.contains(&"Cargo.toml".to_string()));
    assert!(config.restricted_files.contains(&"*.sql".to_string()));
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_restricted_files (lines 204-250) ---[0m
fn test_should_auto_accept_restricted_files() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::EditFile],
        restricted_files: vec!["Cargo.toml".to_string(), "*.sql".to_string()],
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    // Test exact match restriction
    let operation1 = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit Cargo.toml".to_string(),
        affected_files: vec![PathBuf::from("Cargo.toml")],
        commands: vec!["sed -i 's/old/new/g' Cargo.toml".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision1 = engine.should_auto_accept(&operation1).unwrap();
    match decision1 {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("restricted pattern"));
        }
        _ => panic!("Should reject restricted file Cargo.toml"),
    }
    
    // Test pattern match restriction
    let operation2 = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit SQL file".to_string(),
        affected_files: vec![PathBuf::from("migration.sql")],
        commands: vec!["sed -i 's/old/new/g' migration.sql".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision2 = engine.should_auto_accept(&operation2).unwrap();
    match decision2 {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("restricted pattern"));
        }
        _ => panic!("Should reject restricted pattern *.sql"),
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:60-84 function test_should_auto_accept_disabled <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:253-283 function test_should_auto_accept_success
  Similarity: 94.02%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_disabled (lines 60-84) ---[0m
fn test_should_auto_accept_disabled() {
    let config = AutoAcceptConfig {
        enabled: false,
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Read a file".to_string(),
        affected_files: vec![],
        commands: vec!["cat test.txt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Auto-accept is disabled"));
        }
        _ => panic!("Should reject when disabled"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_success (lines 253-283) ---[0m
fn test_should_auto_accept_success() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::FormatCode, OperationType::LintCode],
        max_file_changes: 10,
        require_tests_pass: true,
        require_clean_git: false, // Disable for testing
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::FormatCode,
        description: "Format code".to_string(),
        affected_files: vec![PathBuf::from("src/main.rs")],
        commands: vec!["cargo fmt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Accept(conditions) => {
            assert!(conditions.contains(&"Tests must pass".to_string()));
        }
        AutoAcceptDecision::Reject(reason) => {
            panic!("Should accept safe operation: {}", reason);
        }
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:115-140 function test_should_auto_accept_untrusted_operation <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:143-168 function test_should_auto_accept_high_risk
  Similarity: 99.00%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_untrusted_operation (lines 115-140) ---[0m
fn test_should_auto_accept_untrusted_operation() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::ReadFile], // Only read files trusted
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::WriteFile, // Not in trusted list
        description: "Write a file".to_string(),
        affected_files: vec![],
        commands: vec!["echo 'test' > file.txt".to_string()],
        risk_level: 4,
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("not in trusted operations"));
        }
        _ => panic!("Should reject untrusted operations"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_high_risk (lines 143-168) ---[0m
fn test_should_auto_accept_high_risk() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::DeleteFile], // Allow delete for this test
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::DeleteFile,
        description: "Delete files".to_string(),
        affected_files: vec![],
        commands: vec!["rm important_file.txt".to_string()],
        risk_level: 8, // High risk
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Risk level too high"));
        }
        _ => panic!("Should reject high-risk operations"),
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:87-112 function test_should_auto_accept_emergency_stop <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:115-140 function test_should_auto_accept_untrusted_operation
  Similarity: 97.89%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_emergency_stop (lines 87-112) ---[0m
fn test_should_auto_accept_emergency_stop() {
    let config = AutoAcceptConfig {
        enabled: true,
        emergency_stop: true,
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Read a file".to_string(),
        affected_files: vec![],
        commands: vec!["cat test.txt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Emergency stop"));
        }
        _ => panic!("Should reject when emergency stop is active"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_untrusted_operation (lines 115-140) ---[0m
fn test_should_auto_accept_untrusted_operation() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::ReadFile], // Only read files trusted
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::WriteFile, // Not in trusted list
        description: "Write a file".to_string(),
        affected_files: vec![],
        commands: vec!["echo 'test' > file.txt".to_string()],
        risk_level: 4,
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("not in trusted operations"));
        }
        _ => panic!("Should reject untrusted operations"),
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:87-112 function test_should_auto_accept_emergency_stop <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:143-168 function test_should_auto_accept_high_risk
  Similarity: 97.89%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_emergency_stop (lines 87-112) ---[0m
fn test_should_auto_accept_emergency_stop() {
    let config = AutoAcceptConfig {
        enabled: true,
        emergency_stop: true,
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Read a file".to_string(),
        affected_files: vec![],
        commands: vec!["cat test.txt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Emergency stop"));
        }
        _ => panic!("Should reject when emergency stop is active"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_high_risk (lines 143-168) ---[0m
fn test_should_auto_accept_high_risk() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::DeleteFile], // Allow delete for this test
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::DeleteFile,
        description: "Delete files".to_string(),
        affected_files: vec![],
        commands: vec!["rm important_file.txt".to_string()],
        risk_level: 8, // High risk
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Risk level too high"));
        }
        _ => panic!("Should reject high-risk operations"),
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:332-360 function test_operation_history <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:363-395 function test_validation_execution_time
  Similarity: 81.62%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_operation_history (lines 332-360) ---[0m
fn test_operation_history() {
    let mut engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
    let session_id = "test-session".to_string();
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Test operation".to_string(),
        affected_files: vec![],
        commands: vec!["ls".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    // Initially no history
    assert!(engine.get_operation_history(&session_id).is_none());
    
    // Record operation
    engine.record_operation(session_id.clone(), operation.clone());
    
    // Should have history now
    let history = engine.get_operation_history(&session_id).unwrap();
    assert_eq!(history.len(), 1);
    assert_eq!(history[0].operation_type, OperationType::ReadFile);
    
    // Clear history
    engine.clear_history(&session_id);
    assert!(engine.get_operation_history(&session_id).is_none());
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_validation_execution_time (lines 363-395) ---[0m
fn test_validation_execution_time() {
    let config = AutoAcceptConfig {
        max_execution_time: 60, // 1 minute limit
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Test operation".to_string(),
        affected_files: vec![],
        commands: vec!["ls".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    // Test within time limit
    let result1 = engine.validate_changes(&operation, 30).unwrap();
    match result1 {
        ValidationResult::Valid => {} // Expected
        ValidationResult::Invalid(_) => panic!("Should be valid within time limit"),
    }
    
    // Test exceeding time limit
    let result2 = engine.validate_changes(&operation, 120).unwrap(); // 2 minutes > 1 minute limit
    match result2 {
        ValidationResult::Valid => panic!("Should be invalid when exceeding time limit"),
        ValidationResult::Invalid(issues) => {
            assert!(issues.iter().any(|issue| issue.contains("Execution time exceeded")));
        }
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:60-84 function test_should_auto_accept_disabled <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:87-112 function test_should_auto_accept_emergency_stop
  Similarity: 98.07%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_disabled (lines 60-84) ---[0m
fn test_should_auto_accept_disabled() {
    let config = AutoAcceptConfig {
        enabled: false,
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Read a file".to_string(),
        affected_files: vec![],
        commands: vec!["cat test.txt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Auto-accept is disabled"));
        }
        _ => panic!("Should reject when disabled"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_emergency_stop (lines 87-112) ---[0m
fn test_should_auto_accept_emergency_stop() {
    let config = AutoAcceptConfig {
        enabled: true,
        emergency_stop: true,
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Read a file".to_string(),
        affected_files: vec![],
        commands: vec!["cat test.txt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Emergency stop"));
        }
        _ => panic!("Should reject when emergency stop is active"),
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:60-84 function test_should_auto_accept_disabled <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:115-140 function test_should_auto_accept_untrusted_operation
  Similarity: 97.42%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_disabled (lines 60-84) ---[0m
fn test_should_auto_accept_disabled() {
    let config = AutoAcceptConfig {
        enabled: false,
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Read a file".to_string(),
        affected_files: vec![],
        commands: vec!["cat test.txt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Auto-accept is disabled"));
        }
        _ => panic!("Should reject when disabled"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_untrusted_operation (lines 115-140) ---[0m
fn test_should_auto_accept_untrusted_operation() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::ReadFile], // Only read files trusted
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::WriteFile, // Not in trusted list
        description: "Write a file".to_string(),
        affected_files: vec![],
        commands: vec!["echo 'test' > file.txt".to_string()],
        risk_level: 4,
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("not in trusted operations"));
        }
        _ => panic!("Should reject untrusted operations"),
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:60-84 function test_should_auto_accept_disabled <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:143-168 function test_should_auto_accept_high_risk
  Similarity: 97.42%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_disabled (lines 60-84) ---[0m
fn test_should_auto_accept_disabled() {
    let config = AutoAcceptConfig {
        enabled: false,
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Read a file".to_string(),
        affected_files: vec![],
        commands: vec!["cat test.txt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Auto-accept is disabled"));
        }
        _ => panic!("Should reject when disabled"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_high_risk (lines 143-168) ---[0m
fn test_should_auto_accept_high_risk() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::DeleteFile], // Allow delete for this test
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::DeleteFile,
        description: "Delete files".to_string(),
        affected_files: vec![],
        commands: vec!["rm important_file.txt".to_string()],
        risk_level: 8, // High risk
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Risk level too high"));
        }
        _ => panic!("Should reject high-risk operations"),
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:171-201 function test_should_auto_accept_too_many_files <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:363-395 function test_validation_execution_time
  Similarity: 74.37%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_too_many_files (lines 171-201) ---[0m
fn test_should_auto_accept_too_many_files() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::EditFile],
        max_file_changes: 2, // Low limit for testing
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit multiple files".to_string(),
        affected_files: vec![
            PathBuf::from("file1.rs"),
            PathBuf::from("file2.rs"),
            PathBuf::from("file3.rs"), // 3 files > 2 limit
        ],
        commands: vec!["sed -i 's/old/new/g' *.rs".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Too many file changes"));
        }
        _ => panic!("Should reject when too many files affected"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_validation_execution_time (lines 363-395) ---[0m
fn test_validation_execution_time() {
    let config = AutoAcceptConfig {
        max_execution_time: 60, // 1 minute limit
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Test operation".to_string(),
        affected_files: vec![],
        commands: vec!["ls".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    // Test within time limit
    let result1 = engine.validate_changes(&operation, 30).unwrap();
    match result1 {
        ValidationResult::Valid => {} // Expected
        ValidationResult::Invalid(_) => panic!("Should be valid within time limit"),
    }
    
    // Test exceeding time limit
    let result2 = engine.validate_changes(&operation, 120).unwrap(); // 2 minutes > 1 minute limit
    match result2 {
        ValidationResult::Valid => panic!("Should be invalid when exceeding time limit"),
        ValidationResult::Invalid(issues) => {
            assert!(issues.iter().any(|issue| issue.contains("Execution time exceeded")));
        }
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:253-283 function test_should_auto_accept_success <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:363-395 function test_validation_execution_time
  Similarity: 73.10%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_success (lines 253-283) ---[0m
fn test_should_auto_accept_success() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::FormatCode, OperationType::LintCode],
        max_file_changes: 10,
        require_tests_pass: true,
        require_clean_git: false, // Disable for testing
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::FormatCode,
        description: "Format code".to_string(),
        affected_files: vec![PathBuf::from("src/main.rs")],
        commands: vec!["cargo fmt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Accept(conditions) => {
            assert!(conditions.contains(&"Tests must pass".to_string()));
        }
        AutoAcceptDecision::Reject(reason) => {
            panic!("Should accept safe operation: {}", reason);
        }
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_validation_execution_time (lines 363-395) ---[0m
fn test_validation_execution_time() {
    let config = AutoAcceptConfig {
        max_execution_time: 60, // 1 minute limit
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Test operation".to_string(),
        affected_files: vec![],
        commands: vec!["ls".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    // Test within time limit
    let result1 = engine.validate_changes(&operation, 30).unwrap();
    match result1 {
        ValidationResult::Valid => {} // Expected
        ValidationResult::Invalid(_) => panic!("Should be valid within time limit"),
    }
    
    // Test exceeding time limit
    let result2 = engine.validate_changes(&operation, 120).unwrap(); // 2 minutes > 1 minute limit
    match result2 {
        ValidationResult::Valid => panic!("Should be invalid when exceeding time limit"),
        ValidationResult::Invalid(issues) => {
            assert!(issues.iter().any(|issue| issue.contains("Execution time exceeded")));
        }
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:363-395 function test_validation_execution_time <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:422-443 function test_config_update
  Similarity: 84.93%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_validation_execution_time (lines 363-395) ---[0m
fn test_validation_execution_time() {
    let config = AutoAcceptConfig {
        max_execution_time: 60, // 1 minute limit
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Test operation".to_string(),
        affected_files: vec![],
        commands: vec!["ls".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    // Test within time limit
    let result1 = engine.validate_changes(&operation, 30).unwrap();
    match result1 {
        ValidationResult::Valid => {} // Expected
        ValidationResult::Invalid(_) => panic!("Should be valid within time limit"),
    }
    
    // Test exceeding time limit
    let result2 = engine.validate_changes(&operation, 120).unwrap(); // 2 minutes > 1 minute limit
    match result2 {
        ValidationResult::Valid => panic!("Should be invalid when exceeding time limit"),
        ValidationResult::Invalid(issues) => {
            assert!(issues.iter().any(|issue| issue.contains("Execution time exceeded")));
        }
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_config_update (lines 422-443) ---[0m
fn test_config_update() {
    let initial_config = AutoAcceptConfig {
        enabled: false,
        max_file_changes: 5,
        ..AutoAcceptConfig::default()
    };
    let mut engine = AutoAcceptEngine::new(initial_config);
    
    assert!(!engine.config.enabled);
    assert_eq!(engine.config.max_file_changes, 5);
    
    let new_config = AutoAcceptConfig {
        enabled: true,
        max_file_changes: 10,
        ..AutoAcceptConfig::default()
    };
    
    engine.update_config(new_config);
    
    assert!(engine.config.enabled);
    assert_eq!(engine.config.max_file_changes, 10);
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:363-395 function test_validation_execution_time <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:398-419 function test_task_integration
  Similarity: 84.51%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_validation_execution_time (lines 363-395) ---[0m
fn test_validation_execution_time() {
    let config = AutoAcceptConfig {
        max_execution_time: 60, // 1 minute limit
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Test operation".to_string(),
        affected_files: vec![],
        commands: vec!["ls".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    // Test within time limit
    let result1 = engine.validate_changes(&operation, 30).unwrap();
    match result1 {
        ValidationResult::Valid => {} // Expected
        ValidationResult::Invalid(_) => panic!("Should be valid within time limit"),
    }
    
    // Test exceeding time limit
    let result2 = engine.validate_changes(&operation, 120).unwrap(); // 2 minutes > 1 minute limit
    match result2 {
        ValidationResult::Valid => panic!("Should be invalid when exceeding time limit"),
        ValidationResult::Invalid(issues) => {
            assert!(issues.iter().any(|issue| issue.contains("Execution time exceeded")));
        }
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_task_integration (lines 398-419) ---[0m
fn test_task_integration() {
    let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
    
    let task = Task {
        id: "test-task".to_string(),
        description: "Run tests".to_string(),
        details: Some("Run all unit tests".to_string()),
        priority: Priority::Medium,
        task_type: TaskType::Testing,
        estimated_duration: Some(300),
    };
    
    let commands = vec!["cargo test".to_string()];
    let operation = engine.analyze_operation(&commands, Some(&task)).unwrap();
    
    assert_eq!(operation.operation_type, OperationType::RunTests);
    assert!(operation.task.is_some());
    
    let op_task = operation.task.unwrap();
    assert_eq!(op_task.id, "test-task");
    assert_eq!(op_task.task_type, TaskType::Testing);
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:87-112 function test_should_auto_accept_emergency_stop <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:363-395 function test_validation_execution_time
  Similarity: 74.37%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_emergency_stop (lines 87-112) ---[0m
fn test_should_auto_accept_emergency_stop() {
    let config = AutoAcceptConfig {
        enabled: true,
        emergency_stop: true,
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Read a file".to_string(),
        affected_files: vec![],
        commands: vec!["cat test.txt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Emergency stop"));
        }
        _ => panic!("Should reject when emergency stop is active"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_validation_execution_time (lines 363-395) ---[0m
fn test_validation_execution_time() {
    let config = AutoAcceptConfig {
        max_execution_time: 60, // 1 minute limit
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Test operation".to_string(),
        affected_files: vec![],
        commands: vec!["ls".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    // Test within time limit
    let result1 = engine.validate_changes(&operation, 30).unwrap();
    match result1 {
        ValidationResult::Valid => {} // Expected
        ValidationResult::Invalid(_) => panic!("Should be valid within time limit"),
    }
    
    // Test exceeding time limit
    let result2 = engine.validate_changes(&operation, 120).unwrap(); // 2 minutes > 1 minute limit
    match result2 {
        ValidationResult::Valid => panic!("Should be invalid when exceeding time limit"),
        ValidationResult::Invalid(issues) => {
            assert!(issues.iter().any(|issue| issue.contains("Execution time exceeded")));
        }
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:115-140 function test_should_auto_accept_untrusted_operation <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:363-395 function test_validation_execution_time
  Similarity: 74.37%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_untrusted_operation (lines 115-140) ---[0m
fn test_should_auto_accept_untrusted_operation() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::ReadFile], // Only read files trusted
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::WriteFile, // Not in trusted list
        description: "Write a file".to_string(),
        affected_files: vec![],
        commands: vec!["echo 'test' > file.txt".to_string()],
        risk_level: 4,
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("not in trusted operations"));
        }
        _ => panic!("Should reject untrusted operations"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_validation_execution_time (lines 363-395) ---[0m
fn test_validation_execution_time() {
    let config = AutoAcceptConfig {
        max_execution_time: 60, // 1 minute limit
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Test operation".to_string(),
        affected_files: vec![],
        commands: vec!["ls".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    // Test within time limit
    let result1 = engine.validate_changes(&operation, 30).unwrap();
    match result1 {
        ValidationResult::Valid => {} // Expected
        ValidationResult::Invalid(_) => panic!("Should be valid within time limit"),
    }
    
    // Test exceeding time limit
    let result2 = engine.validate_changes(&operation, 120).unwrap(); // 2 minutes > 1 minute limit
    match result2 {
        ValidationResult::Valid => panic!("Should be invalid when exceeding time limit"),
        ValidationResult::Invalid(issues) => {
            assert!(issues.iter().any(|issue| issue.contains("Execution time exceeded")));
        }
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:143-168 function test_should_auto_accept_high_risk <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:363-395 function test_validation_execution_time
  Similarity: 74.37%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_high_risk (lines 143-168) ---[0m
fn test_should_auto_accept_high_risk() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::DeleteFile], // Allow delete for this test
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::DeleteFile,
        description: "Delete files".to_string(),
        affected_files: vec![],
        commands: vec!["rm important_file.txt".to_string()],
        risk_level: 8, // High risk
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Risk level too high"));
        }
        _ => panic!("Should reject high-risk operations"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_validation_execution_time (lines 363-395) ---[0m
fn test_validation_execution_time() {
    let config = AutoAcceptConfig {
        max_execution_time: 60, // 1 minute limit
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Test operation".to_string(),
        affected_files: vec![],
        commands: vec!["ls".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    // Test within time limit
    let result1 = engine.validate_changes(&operation, 30).unwrap();
    match result1 {
        ValidationResult::Valid => {} // Expected
        ValidationResult::Invalid(_) => panic!("Should be valid within time limit"),
    }
    
    // Test exceeding time limit
    let result2 = engine.validate_changes(&operation, 120).unwrap(); // 2 minutes > 1 minute limit
    match result2 {
        ValidationResult::Valid => panic!("Should be invalid when exceeding time limit"),
        ValidationResult::Invalid(issues) => {
            assert!(issues.iter().any(|issue| issue.contains("Execution time exceeded")));
        }
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:60-84 function test_should_auto_accept_disabled <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:363-395 function test_validation_execution_time
  Similarity: 75.32%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_disabled (lines 60-84) ---[0m
fn test_should_auto_accept_disabled() {
    let config = AutoAcceptConfig {
        enabled: false,
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Read a file".to_string(),
        affected_files: vec![],
        commands: vec!["cat test.txt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Auto-accept is disabled"));
        }
        _ => panic!("Should reject when disabled"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_validation_execution_time (lines 363-395) ---[0m
fn test_validation_execution_time() {
    let config = AutoAcceptConfig {
        max_execution_time: 60, // 1 minute limit
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Test operation".to_string(),
        affected_files: vec![],
        commands: vec!["ls".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    // Test within time limit
    let result1 = engine.validate_changes(&operation, 30).unwrap();
    match result1 {
        ValidationResult::Valid => {} // Expected
        ValidationResult::Invalid(_) => panic!("Should be valid within time limit"),
    }
    
    // Test exceeding time limit
    let result2 = engine.validate_changes(&operation, 120).unwrap(); // 2 minutes > 1 minute limit
    match result2 {
        ValidationResult::Valid => panic!("Should be invalid when exceeding time limit"),
        ValidationResult::Invalid(issues) => {
            assert!(issues.iter().any(|issue| issue.contains("Execution time exceeded")));
        }
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:309-329 function test_emergency_stop_functionality <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:332-360 function test_operation_history
  Similarity: 84.19%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_emergency_stop_functionality (lines 309-329) ---[0m
fn test_emergency_stop_functionality() {
    let config = AutoAcceptConfig {
        enabled: true,
        ..AutoAcceptConfig::default()
    };
    let mut engine = AutoAcceptEngine::new(config);
    
    // Initially enabled
    assert!(engine.config.enabled);
    assert!(!engine.config.emergency_stop);
    
    // Trigger emergency stop
    engine.emergency_stop();
    assert!(!engine.config.enabled);
    assert!(engine.config.emergency_stop);
    
    // Reset emergency stop
    engine.reset_emergency_stop();
    assert!(!engine.config.emergency_stop);
    // Note: enabled remains false after emergency stop - must be manually re-enabled
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_operation_history (lines 332-360) ---[0m
fn test_operation_history() {
    let mut engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
    let session_id = "test-session".to_string();
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Test operation".to_string(),
        affected_files: vec![],
        commands: vec!["ls".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    // Initially no history
    assert!(engine.get_operation_history(&session_id).is_none());
    
    // Record operation
    engine.record_operation(session_id.clone(), operation.clone());
    
    // Should have history now
    let history = engine.get_operation_history(&session_id).unwrap();
    assert_eq!(history.len(), 1);
    assert_eq!(history[0].operation_type, OperationType::ReadFile);
    
    // Clear history
    engine.clear_history(&session_id);
    assert!(engine.get_operation_history(&session_id).is_none());
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:286-306 function test_pattern_matching <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:332-360 function test_operation_history
  Similarity: 80.30%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_pattern_matching (lines 286-306) ---[0m
fn test_pattern_matching() {
    let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
    
    // Test exact match
    assert!(engine.matches_pattern("Cargo.toml", "Cargo.toml"));
    assert!(!engine.matches_pattern("Cargo.lock", "Cargo.toml"));
    
    // Test suffix wildcard
    assert!(engine.matches_pattern("test.sql", "*.sql"));
    assert!(engine.matches_pattern("migration.sql", "*.sql"));
    assert!(!engine.matches_pattern("test.rs", "*.sql"));
    
    // Test prefix wildcard
    assert!(engine.matches_pattern("config.toml", "*.toml"));
    assert!(!engine.matches_pattern("config.json", "*.toml"));
    
    // Test directory patterns
    assert!(engine.matches_pattern("src/migrations/001.sql", "**/migrations/*"));
    assert!(engine.matches_pattern("db/migrations/init.sql", "**/migrations/*"));
    assert!(!engine.matches_pattern("src/models/user.rs", "**/migrations/*"));
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_operation_history (lines 332-360) ---[0m
fn test_operation_history() {
    let mut engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
    let session_id = "test-session".to_string();
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Test operation".to_string(),
        affected_files: vec![],
        commands: vec!["ls".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    // Initially no history
    assert!(engine.get_operation_history(&session_id).is_none());
    
    // Record operation
    engine.record_operation(session_id.clone(), operation.clone());
    
    // Should have history now
    let history = engine.get_operation_history(&session_id).unwrap();
    assert_eq!(history.len(), 1);
    assert_eq!(history[0].operation_type, OperationType::ReadFile);
    
    // Clear history
    engine.clear_history(&session_id);
    assert!(engine.get_operation_history(&session_id).is_none());
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:253-283 function test_should_auto_accept_success <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:422-443 function test_config_update
  Similarity: 75.68%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_success (lines 253-283) ---[0m
fn test_should_auto_accept_success() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::FormatCode, OperationType::LintCode],
        max_file_changes: 10,
        require_tests_pass: true,
        require_clean_git: false, // Disable for testing
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::FormatCode,
        description: "Format code".to_string(),
        affected_files: vec![PathBuf::from("src/main.rs")],
        commands: vec!["cargo fmt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Accept(conditions) => {
            assert!(conditions.contains(&"Tests must pass".to_string()));
        }
        AutoAcceptDecision::Reject(reason) => {
            panic!("Should accept safe operation: {}", reason);
        }
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_config_update (lines 422-443) ---[0m
fn test_config_update() {
    let initial_config = AutoAcceptConfig {
        enabled: false,
        max_file_changes: 5,
        ..AutoAcceptConfig::default()
    };
    let mut engine = AutoAcceptEngine::new(initial_config);
    
    assert!(!engine.config.enabled);
    assert_eq!(engine.config.max_file_changes, 5);
    
    let new_config = AutoAcceptConfig {
        enabled: true,
        max_file_changes: 10,
        ..AutoAcceptConfig::default()
    };
    
    engine.update_config(new_config);
    
    assert!(engine.config.enabled);
    assert_eq!(engine.config.max_file_changes, 10);
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:171-201 function test_should_auto_accept_too_many_files <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:422-443 function test_config_update
  Similarity: 74.66%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_too_many_files (lines 171-201) ---[0m
fn test_should_auto_accept_too_many_files() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::EditFile],
        max_file_changes: 2, // Low limit for testing
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::EditFile,
        description: "Edit multiple files".to_string(),
        affected_files: vec![
            PathBuf::from("file1.rs"),
            PathBuf::from("file2.rs"),
            PathBuf::from("file3.rs"), // 3 files > 2 limit
        ],
        commands: vec!["sed -i 's/old/new/g' *.rs".to_string()],
        risk_level: 3,
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Too many file changes"));
        }
        _ => panic!("Should reject when too many files affected"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_config_update (lines 422-443) ---[0m
fn test_config_update() {
    let initial_config = AutoAcceptConfig {
        enabled: false,
        max_file_changes: 5,
        ..AutoAcceptConfig::default()
    };
    let mut engine = AutoAcceptEngine::new(initial_config);
    
    assert!(!engine.config.enabled);
    assert_eq!(engine.config.max_file_changes, 5);
    
    let new_config = AutoAcceptConfig {
        enabled: true,
        max_file_changes: 10,
        ..AutoAcceptConfig::default()
    };
    
    engine.update_config(new_config);
    
    assert!(engine.config.enabled);
    assert_eq!(engine.config.max_file_changes, 10);
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:286-306 function test_pattern_matching <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:363-395 function test_validation_execution_time
  Similarity: 72.71%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_pattern_matching (lines 286-306) ---[0m
fn test_pattern_matching() {
    let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
    
    // Test exact match
    assert!(engine.matches_pattern("Cargo.toml", "Cargo.toml"));
    assert!(!engine.matches_pattern("Cargo.lock", "Cargo.toml"));
    
    // Test suffix wildcard
    assert!(engine.matches_pattern("test.sql", "*.sql"));
    assert!(engine.matches_pattern("migration.sql", "*.sql"));
    assert!(!engine.matches_pattern("test.rs", "*.sql"));
    
    // Test prefix wildcard
    assert!(engine.matches_pattern("config.toml", "*.toml"));
    assert!(!engine.matches_pattern("config.json", "*.toml"));
    
    // Test directory patterns
    assert!(engine.matches_pattern("src/migrations/001.sql", "**/migrations/*"));
    assert!(engine.matches_pattern("db/migrations/init.sql", "**/migrations/*"));
    assert!(!engine.matches_pattern("src/models/user.rs", "**/migrations/*"));
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_validation_execution_time (lines 363-395) ---[0m
fn test_validation_execution_time() {
    let config = AutoAcceptConfig {
        max_execution_time: 60, // 1 minute limit
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Test operation".to_string(),
        affected_files: vec![],
        commands: vec!["ls".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    // Test within time limit
    let result1 = engine.validate_changes(&operation, 30).unwrap();
    match result1 {
        ValidationResult::Valid => {} // Expected
        ValidationResult::Invalid(_) => panic!("Should be valid within time limit"),
    }
    
    // Test exceeding time limit
    let result2 = engine.validate_changes(&operation, 120).unwrap(); // 2 minutes > 1 minute limit
    match result2 {
        ValidationResult::Valid => panic!("Should be invalid when exceeding time limit"),
        ValidationResult::Invalid(issues) => {
            assert!(issues.iter().any(|issue| issue.contains("Execution time exceeded")));
        }
    }
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:332-360 function test_operation_history <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:398-419 function test_task_integration
  Similarity: 75.89%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_operation_history (lines 332-360) ---[0m
fn test_operation_history() {
    let mut engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
    let session_id = "test-session".to_string();
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Test operation".to_string(),
        affected_files: vec![],
        commands: vec!["ls".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    // Initially no history
    assert!(engine.get_operation_history(&session_id).is_none());
    
    // Record operation
    engine.record_operation(session_id.clone(), operation.clone());
    
    // Should have history now
    let history = engine.get_operation_history(&session_id).unwrap();
    assert_eq!(history.len(), 1);
    assert_eq!(history[0].operation_type, OperationType::ReadFile);
    
    // Clear history
    engine.clear_history(&session_id);
    assert!(engine.get_operation_history(&session_id).is_none());
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_task_integration (lines 398-419) ---[0m
fn test_task_integration() {
    let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
    
    let task = Task {
        id: "test-task".to_string(),
        description: "Run tests".to_string(),
        details: Some("Run all unit tests".to_string()),
        priority: Priority::Medium,
        task_type: TaskType::Testing,
        estimated_duration: Some(300),
    };
    
    let commands = vec!["cargo test".to_string()];
    let operation = engine.analyze_operation(&commands, Some(&task)).unwrap();
    
    assert_eq!(operation.operation_type, OperationType::RunTests);
    assert!(operation.task.is_some());
    
    let op_task = operation.task.unwrap();
    assert_eq!(op_task.id, "test-task");
    assert_eq!(op_task.task_type, TaskType::Testing);
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:6-26 function test_auto_accept_config_default <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:332-360 function test_operation_history
  Similarity: 76.45%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_auto_accept_config_default (lines 6-26) ---[0m
fn test_auto_accept_config_default() {
    let config = AutoAcceptConfig::default();
    
    // Should be disabled by default for safety
    assert!(!config.enabled);
    assert!(!config.emergency_stop);
    assert!(config.require_tests_pass);
    assert!(config.require_clean_git);
    assert_eq!(config.max_file_changes, 5);
    assert_eq!(config.max_execution_time, 300);
    
    // Should have safe default trusted operations
    assert!(config.trusted_operations.contains(&OperationType::ReadFile));
    assert!(config.trusted_operations.contains(&OperationType::FormatCode));
    assert!(config.trusted_operations.contains(&OperationType::RunTests));
    assert!(config.trusted_operations.contains(&OperationType::LintCode));
    
    // Should have restricted file patterns
    assert!(config.restricted_files.contains(&"Cargo.toml".to_string()));
    assert!(config.restricted_files.contains(&"*.sql".to_string()));
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_operation_history (lines 332-360) ---[0m
fn test_operation_history() {
    let mut engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
    let session_id = "test-session".to_string();
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Test operation".to_string(),
        affected_files: vec![],
        commands: vec!["ls".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    // Initially no history
    assert!(engine.get_operation_history(&session_id).is_none());
    
    // Record operation
    engine.record_operation(session_id.clone(), operation.clone());
    
    // Should have history now
    let history = engine.get_operation_history(&session_id).unwrap();
    assert_eq!(history.len(), 1);
    assert_eq!(history[0].operation_type, OperationType::ReadFile);
    
    // Clear history
    engine.clear_history(&session_id);
    assert!(engine.get_operation_history(&session_id).is_none());
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:332-360 function test_operation_history <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:422-443 function test_config_update
  Similarity: 72.94%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_operation_history (lines 332-360) ---[0m
fn test_operation_history() {
    let mut engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
    let session_id = "test-session".to_string();
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Test operation".to_string(),
        affected_files: vec![],
        commands: vec!["ls".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    // Initially no history
    assert!(engine.get_operation_history(&session_id).is_none());
    
    // Record operation
    engine.record_operation(session_id.clone(), operation.clone());
    
    // Should have history now
    let history = engine.get_operation_history(&session_id).unwrap();
    assert_eq!(history.len(), 1);
    assert_eq!(history[0].operation_type, OperationType::ReadFile);
    
    // Clear history
    engine.clear_history(&session_id);
    assert!(engine.get_operation_history(&session_id).is_none());
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_config_update (lines 422-443) ---[0m
fn test_config_update() {
    let initial_config = AutoAcceptConfig {
        enabled: false,
        max_file_changes: 5,
        ..AutoAcceptConfig::default()
    };
    let mut engine = AutoAcceptEngine::new(initial_config);
    
    assert!(!engine.config.enabled);
    assert_eq!(engine.config.max_file_changes, 5);
    
    let new_config = AutoAcceptConfig {
        enabled: true,
        max_file_changes: 10,
        ..AutoAcceptConfig::default()
    };
    
    engine.update_config(new_config);
    
    assert!(engine.config.enabled);
    assert_eq!(engine.config.max_file_changes, 10);
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:253-283 function test_should_auto_accept_success <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:309-329 function test_emergency_stop_functionality
  Similarity: 70.39%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_success (lines 253-283) ---[0m
fn test_should_auto_accept_success() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::FormatCode, OperationType::LintCode],
        max_file_changes: 10,
        require_tests_pass: true,
        require_clean_git: false, // Disable for testing
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::FormatCode,
        description: "Format code".to_string(),
        affected_files: vec![PathBuf::from("src/main.rs")],
        commands: vec!["cargo fmt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Accept(conditions) => {
            assert!(conditions.contains(&"Tests must pass".to_string()));
        }
        AutoAcceptDecision::Reject(reason) => {
            panic!("Should accept safe operation: {}", reason);
        }
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_emergency_stop_functionality (lines 309-329) ---[0m
fn test_emergency_stop_functionality() {
    let config = AutoAcceptConfig {
        enabled: true,
        ..AutoAcceptConfig::default()
    };
    let mut engine = AutoAcceptEngine::new(config);
    
    // Initially enabled
    assert!(engine.config.enabled);
    assert!(!engine.config.emergency_stop);
    
    // Trigger emergency stop
    engine.emergency_stop();
    assert!(!engine.config.enabled);
    assert!(engine.config.emergency_stop);
    
    // Reset emergency stop
    engine.reset_emergency_stop();
    assert!(!engine.config.emergency_stop);
    // Note: enabled remains false after emergency stop - must be manually re-enabled
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:6-26 function test_auto_accept_config_default <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:29-57 function test_operation_type_from_commands
  Similarity: 71.64%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_auto_accept_config_default (lines 6-26) ---[0m
fn test_auto_accept_config_default() {
    let config = AutoAcceptConfig::default();
    
    // Should be disabled by default for safety
    assert!(!config.enabled);
    assert!(!config.emergency_stop);
    assert!(config.require_tests_pass);
    assert!(config.require_clean_git);
    assert_eq!(config.max_file_changes, 5);
    assert_eq!(config.max_execution_time, 300);
    
    // Should have safe default trusted operations
    assert!(config.trusted_operations.contains(&OperationType::ReadFile));
    assert!(config.trusted_operations.contains(&OperationType::FormatCode));
    assert!(config.trusted_operations.contains(&OperationType::RunTests));
    assert!(config.trusted_operations.contains(&OperationType::LintCode));
    
    // Should have restricted file patterns
    assert!(config.restricted_files.contains(&"Cargo.toml".to_string()));
    assert!(config.restricted_files.contains(&"*.sql".to_string()));
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_operation_type_from_commands (lines 29-57) ---[0m
fn test_operation_type_from_commands() {
    let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
    
    // Test read operations
    let read_commands = vec!["cat src/main.rs".to_string(), "ls -la".to_string()];
    let operation = engine.analyze_operation(&read_commands, None).unwrap();
    assert_eq!(operation.operation_type, OperationType::ReadFile);
    assert_eq!(operation.risk_level, 1);
    assert!(operation.reversible);
    
    // Test write operations
    let write_commands = vec!["echo 'test' > file.txt".to_string()];
    let operation = engine.analyze_operation(&write_commands, None).unwrap();
    assert_eq!(operation.operation_type, OperationType::WriteFile);
    assert!(operation.risk_level >= 4);
    
    // Test dangerous operations
    let dangerous_commands = vec!["rm -rf /".to_string()];
    let operation = engine.analyze_operation(&dangerous_commands, None).unwrap();
    assert_eq!(operation.operation_type, OperationType::DeleteFile);
    assert!(operation.risk_level >= 8);
    
    // Test test operations
    let test_commands = vec!["cargo test".to_string()];
    let operation = engine.analyze_operation(&test_commands, None).unwrap();
    assert_eq!(operation.operation_type, OperationType::RunTests);
    assert_eq!(operation.risk_level, 2);
    assert!(operation.reversible);
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:286-306 function test_pattern_matching <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:309-329 function test_emergency_stop_functionality
  Similarity: 85.11%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_pattern_matching (lines 286-306) ---[0m
fn test_pattern_matching() {
    let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
    
    // Test exact match
    assert!(engine.matches_pattern("Cargo.toml", "Cargo.toml"));
    assert!(!engine.matches_pattern("Cargo.lock", "Cargo.toml"));
    
    // Test suffix wildcard
    assert!(engine.matches_pattern("test.sql", "*.sql"));
    assert!(engine.matches_pattern("migration.sql", "*.sql"));
    assert!(!engine.matches_pattern("test.rs", "*.sql"));
    
    // Test prefix wildcard
    assert!(engine.matches_pattern("config.toml", "*.toml"));
    assert!(!engine.matches_pattern("config.json", "*.toml"));
    
    // Test directory patterns
    assert!(engine.matches_pattern("src/migrations/001.sql", "**/migrations/*"));
    assert!(engine.matches_pattern("db/migrations/init.sql", "**/migrations/*"));
    assert!(!engine.matches_pattern("src/models/user.rs", "**/migrations/*"));
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_emergency_stop_functionality (lines 309-329) ---[0m
fn test_emergency_stop_functionality() {
    let config = AutoAcceptConfig {
        enabled: true,
        ..AutoAcceptConfig::default()
    };
    let mut engine = AutoAcceptEngine::new(config);
    
    // Initially enabled
    assert!(engine.config.enabled);
    assert!(!engine.config.emergency_stop);
    
    // Trigger emergency stop
    engine.emergency_stop();
    assert!(!engine.config.enabled);
    assert!(engine.config.emergency_stop);
    
    // Reset emergency stop
    engine.reset_emergency_stop();
    assert!(!engine.config.emergency_stop);
    // Note: enabled remains false after emergency stop - must be manually re-enabled
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:6-26 function test_auto_accept_config_default <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:286-306 function test_pattern_matching
  Similarity: 83.94%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_auto_accept_config_default (lines 6-26) ---[0m
fn test_auto_accept_config_default() {
    let config = AutoAcceptConfig::default();
    
    // Should be disabled by default for safety
    assert!(!config.enabled);
    assert!(!config.emergency_stop);
    assert!(config.require_tests_pass);
    assert!(config.require_clean_git);
    assert_eq!(config.max_file_changes, 5);
    assert_eq!(config.max_execution_time, 300);
    
    // Should have safe default trusted operations
    assert!(config.trusted_operations.contains(&OperationType::ReadFile));
    assert!(config.trusted_operations.contains(&OperationType::FormatCode));
    assert!(config.trusted_operations.contains(&OperationType::RunTests));
    assert!(config.trusted_operations.contains(&OperationType::LintCode));
    
    // Should have restricted file patterns
    assert!(config.restricted_files.contains(&"Cargo.toml".to_string()));
    assert!(config.restricted_files.contains(&"*.sql".to_string()));
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_pattern_matching (lines 286-306) ---[0m
fn test_pattern_matching() {
    let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
    
    // Test exact match
    assert!(engine.matches_pattern("Cargo.toml", "Cargo.toml"));
    assert!(!engine.matches_pattern("Cargo.lock", "Cargo.toml"));
    
    // Test suffix wildcard
    assert!(engine.matches_pattern("test.sql", "*.sql"));
    assert!(engine.matches_pattern("migration.sql", "*.sql"));
    assert!(!engine.matches_pattern("test.rs", "*.sql"));
    
    // Test prefix wildcard
    assert!(engine.matches_pattern("config.toml", "*.toml"));
    assert!(!engine.matches_pattern("config.json", "*.toml"));
    
    // Test directory patterns
    assert!(engine.matches_pattern("src/migrations/001.sql", "**/migrations/*"));
    assert!(engine.matches_pattern("db/migrations/init.sql", "**/migrations/*"));
    assert!(!engine.matches_pattern("src/models/user.rs", "**/migrations/*"));
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:398-419 function test_task_integration <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:422-443 function test_config_update
  Similarity: 79.79%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_task_integration (lines 398-419) ---[0m
fn test_task_integration() {
    let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
    
    let task = Task {
        id: "test-task".to_string(),
        description: "Run tests".to_string(),
        details: Some("Run all unit tests".to_string()),
        priority: Priority::Medium,
        task_type: TaskType::Testing,
        estimated_duration: Some(300),
    };
    
    let commands = vec!["cargo test".to_string()];
    let operation = engine.analyze_operation(&commands, Some(&task)).unwrap();
    
    assert_eq!(operation.operation_type, OperationType::RunTests);
    assert!(operation.task.is_some());
    
    let op_task = operation.task.unwrap();
    assert_eq!(op_task.id, "test-task");
    assert_eq!(op_task.task_type, TaskType::Testing);
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_config_update (lines 422-443) ---[0m
fn test_config_update() {
    let initial_config = AutoAcceptConfig {
        enabled: false,
        max_file_changes: 5,
        ..AutoAcceptConfig::default()
    };
    let mut engine = AutoAcceptEngine::new(initial_config);
    
    assert!(!engine.config.enabled);
    assert_eq!(engine.config.max_file_changes, 5);
    
    let new_config = AutoAcceptConfig {
        enabled: true,
        max_file_changes: 10,
        ..AutoAcceptConfig::default()
    };
    
    engine.update_config(new_config);
    
    assert!(engine.config.enabled);
    assert_eq!(engine.config.max_file_changes, 10);
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:309-329 function test_emergency_stop_functionality <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:398-419 function test_task_integration
  Similarity: 80.55%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_emergency_stop_functionality (lines 309-329) ---[0m
fn test_emergency_stop_functionality() {
    let config = AutoAcceptConfig {
        enabled: true,
        ..AutoAcceptConfig::default()
    };
    let mut engine = AutoAcceptEngine::new(config);
    
    // Initially enabled
    assert!(engine.config.enabled);
    assert!(!engine.config.emergency_stop);
    
    // Trigger emergency stop
    engine.emergency_stop();
    assert!(!engine.config.enabled);
    assert!(engine.config.emergency_stop);
    
    // Reset emergency stop
    engine.reset_emergency_stop();
    assert!(!engine.config.emergency_stop);
    // Note: enabled remains false after emergency stop - must be manually re-enabled
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_task_integration (lines 398-419) ---[0m
fn test_task_integration() {
    let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
    
    let task = Task {
        id: "test-task".to_string(),
        description: "Run tests".to_string(),
        details: Some("Run all unit tests".to_string()),
        priority: Priority::Medium,
        task_type: TaskType::Testing,
        estimated_duration: Some(300),
    };
    
    let commands = vec!["cargo test".to_string()];
    let operation = engine.analyze_operation(&commands, Some(&task)).unwrap();
    
    assert_eq!(operation.operation_type, OperationType::RunTests);
    assert!(operation.task.is_some());
    
    let op_task = operation.task.unwrap();
    assert_eq!(op_task.id, "test-task");
    assert_eq!(op_task.task_type, TaskType::Testing);
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:115-140 function test_should_auto_accept_untrusted_operation <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:422-443 function test_config_update
  Similarity: 71.00%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_untrusted_operation (lines 115-140) ---[0m
fn test_should_auto_accept_untrusted_operation() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::ReadFile], // Only read files trusted
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::WriteFile, // Not in trusted list
        description: "Write a file".to_string(),
        affected_files: vec![],
        commands: vec!["echo 'test' > file.txt".to_string()],
        risk_level: 4,
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("not in trusted operations"));
        }
        _ => panic!("Should reject untrusted operations"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_config_update (lines 422-443) ---[0m
fn test_config_update() {
    let initial_config = AutoAcceptConfig {
        enabled: false,
        max_file_changes: 5,
        ..AutoAcceptConfig::default()
    };
    let mut engine = AutoAcceptEngine::new(initial_config);
    
    assert!(!engine.config.enabled);
    assert_eq!(engine.config.max_file_changes, 5);
    
    let new_config = AutoAcceptConfig {
        enabled: true,
        max_file_changes: 10,
        ..AutoAcceptConfig::default()
    };
    
    engine.update_config(new_config);
    
    assert!(engine.config.enabled);
    assert_eq!(engine.config.max_file_changes, 10);
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:143-168 function test_should_auto_accept_high_risk <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:422-443 function test_config_update
  Similarity: 71.00%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_high_risk (lines 143-168) ---[0m
fn test_should_auto_accept_high_risk() {
    let config = AutoAcceptConfig {
        enabled: true,
        trusted_operations: vec![OperationType::DeleteFile], // Allow delete for this test
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::DeleteFile,
        description: "Delete files".to_string(),
        affected_files: vec![],
        commands: vec!["rm important_file.txt".to_string()],
        risk_level: 8, // High risk
        reversible: false,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Risk level too high"));
        }
        _ => panic!("Should reject high-risk operations"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_config_update (lines 422-443) ---[0m
fn test_config_update() {
    let initial_config = AutoAcceptConfig {
        enabled: false,
        max_file_changes: 5,
        ..AutoAcceptConfig::default()
    };
    let mut engine = AutoAcceptEngine::new(initial_config);
    
    assert!(!engine.config.enabled);
    assert_eq!(engine.config.max_file_changes, 5);
    
    let new_config = AutoAcceptConfig {
        enabled: true,
        max_file_changes: 10,
        ..AutoAcceptConfig::default()
    };
    
    engine.update_config(new_config);
    
    assert!(engine.config.enabled);
    assert_eq!(engine.config.max_file_changes, 10);
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:6-26 function test_auto_accept_config_default <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:309-329 function test_emergency_stop_functionality
  Similarity: 80.04%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_auto_accept_config_default (lines 6-26) ---[0m
fn test_auto_accept_config_default() {
    let config = AutoAcceptConfig::default();
    
    // Should be disabled by default for safety
    assert!(!config.enabled);
    assert!(!config.emergency_stop);
    assert!(config.require_tests_pass);
    assert!(config.require_clean_git);
    assert_eq!(config.max_file_changes, 5);
    assert_eq!(config.max_execution_time, 300);
    
    // Should have safe default trusted operations
    assert!(config.trusted_operations.contains(&OperationType::ReadFile));
    assert!(config.trusted_operations.contains(&OperationType::FormatCode));
    assert!(config.trusted_operations.contains(&OperationType::RunTests));
    assert!(config.trusted_operations.contains(&OperationType::LintCode));
    
    // Should have restricted file patterns
    assert!(config.restricted_files.contains(&"Cargo.toml".to_string()));
    assert!(config.restricted_files.contains(&"*.sql".to_string()));
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_emergency_stop_functionality (lines 309-329) ---[0m
fn test_emergency_stop_functionality() {
    let config = AutoAcceptConfig {
        enabled: true,
        ..AutoAcceptConfig::default()
    };
    let mut engine = AutoAcceptEngine::new(config);
    
    // Initially enabled
    assert!(engine.config.enabled);
    assert!(!engine.config.emergency_stop);
    
    // Trigger emergency stop
    engine.emergency_stop();
    assert!(!engine.config.enabled);
    assert!(engine.config.emergency_stop);
    
    // Reset emergency stop
    engine.reset_emergency_stop();
    assert!(!engine.config.emergency_stop);
    // Note: enabled remains false after emergency stop - must be manually re-enabled
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:87-112 function test_should_auto_accept_emergency_stop <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:422-443 function test_config_update
  Similarity: 70.00%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_should_auto_accept_emergency_stop (lines 87-112) ---[0m
fn test_should_auto_accept_emergency_stop() {
    let config = AutoAcceptConfig {
        enabled: true,
        emergency_stop: true,
        ..AutoAcceptConfig::default()
    };
    let engine = AutoAcceptEngine::new(config);
    
    let operation = Operation {
        operation_type: OperationType::ReadFile,
        description: "Read a file".to_string(),
        affected_files: vec![],
        commands: vec!["cat test.txt".to_string()],
        risk_level: 1,
        reversible: true,
        task: None,
    };
    
    let decision = engine.should_auto_accept(&operation).unwrap();
    match decision {
        AutoAcceptDecision::Reject(reason) => {
            assert!(reason.contains("Emergency stop"));
        }
        _ => panic!("Should reject when emergency stop is active"),
    }
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_config_update (lines 422-443) ---[0m
fn test_config_update() {
    let initial_config = AutoAcceptConfig {
        enabled: false,
        max_file_changes: 5,
        ..AutoAcceptConfig::default()
    };
    let mut engine = AutoAcceptEngine::new(initial_config);
    
    assert!(!engine.config.enabled);
    assert_eq!(engine.config.max_file_changes, 5);
    
    let new_config = AutoAcceptConfig {
        enabled: true,
        max_file_changes: 10,
        ..AutoAcceptConfig::default()
    };
    
    engine.update_config(new_config);
    
    assert!(engine.config.enabled);
    assert_eq!(engine.config.max_file_changes, 10);
}

  ./crates/ccswarm/src/tests/auto_accept_tests.rs:309-329 function test_emergency_stop_functionality <-> ./crates/ccswarm/src/tests/auto_accept_tests.rs:422-443 function test_config_update
  Similarity: 73.49%

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_emergency_stop_functionality (lines 309-329) ---[0m
fn test_emergency_stop_functionality() {
    let config = AutoAcceptConfig {
        enabled: true,
        ..AutoAcceptConfig::default()
    };
    let mut engine = AutoAcceptEngine::new(config);
    
    // Initially enabled
    assert!(engine.config.enabled);
    assert!(!engine.config.emergency_stop);
    
    // Trigger emergency stop
    engine.emergency_stop();
    assert!(!engine.config.enabled);
    assert!(engine.config.emergency_stop);
    
    // Reset emergency stop
    engine.reset_emergency_stop();
    assert!(!engine.config.emergency_stop);
    // Note: enabled remains false after emergency stop - must be manually re-enabled
}

[36m--- ./crates/ccswarm/src/tests/auto_accept_tests.rs:test_config_update (lines 422-443) ---[0m
fn test_config_update() {
    let initial_config = AutoAcceptConfig {
        enabled: false,
        max_file_changes: 5,
        ..AutoAcceptConfig::default()
    };
    let mut engine = AutoAcceptEngine::new(initial_config);
    
    assert!(!engine.config.enabled);
    assert_eq!(engine.config.max_file_changes, 5);
    
    let new_config = AutoAcceptConfig {
        enabled: true,
        max_file_changes: 10,
        ..AutoAcceptConfig::default()
    };
    
    engine.update_config(new_config);
    
    assert!(engine.config.enabled);
    assert_eq!(engine.config.max_file_changes, 10);
}


Duplicates in ./crates/ccswarm/src/session/memory.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/session/memory.rs:544-589 method retrieve_relevant_memories <-> ./crates/ccswarm/src/session/memory.rs:693-715 method add_episode
  Similarity: 87.39%
  Classes: SessionMemory <-> EpisodicMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:retrieve_relevant_memories (lines 544-589) ---[0m
    pub fn retrieve_relevant_memories(&self, query: &str) -> RetrievalResult {
        let mut result = RetrievalResult::new();

        // Search working memory
        for item in &self.working_memory.current_items {
            if item.content.to_lowercase().contains(&query.to_lowercase()) {
                result.working_memory_items.push(item.clone());
            }
        }

        // Search episodic memory
        for episode in &self.episodic_memory.episodes {
            if episode
                .description
                .to_lowercase()
                .contains(&query.to_lowercase())
            {
                result.relevant_episodes.push(episode.clone());
            }
        }

        // Search semantic memory
        for concept in self.semantic_memory.concepts.values() {
            if concept.name.to_lowercase().contains(&query.to_lowercase())
                || concept
                    .definition
                    .to_lowercase()
                    .contains(&query.to_lowercase())
            {
                result.relevant_concepts.push(concept.clone());
            }
        }

        // Search procedural memory
        for procedure in self.procedural_memory.procedures.values() {
            if procedure
                .name
                .to_lowercase()
                .contains(&query.to_lowercase())
            {
                result.relevant_procedures.push(procedure.clone());
            }
        }

        result
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_episode (lines 693-715) ---[0m
    fn add_episode(&mut self, episode: Episode) {
        // Remove oldest episode if at capacity
        if self.episodes.len() >= self.max_episodes {
            if let Some(old_episode) = self.episodes.pop_front() {
                self.emotional_markers.remove(&old_episode.id);
            }
        }

        // Add emotional marker if significant
        if episode.emotional_valence.abs() > 0.5 {
            self.emotional_markers.insert(
                episode.id.clone(),
                EmotionalMarker {
                    event_id: episode.id.clone(),
                    emotion_type: EmotionType::Satisfaction, // Simplified
                    intensity: episode.emotional_valence.abs(),
                    influence_on_learning: episode.learning_value,
                },
            );
        }

        self.episodes.push_back(episode);
    }

  ./crates/ccswarm/src/session/memory.rs:380-400 method add_episode <-> ./crates/ccswarm/src/session/memory.rs:403-421 method add_concept
  Similarity: 94.41%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_episode (lines 380-400) ---[0m
    pub fn add_episode(
        &mut self,
        event_type: EpisodeType,
        description: String,
        context: HashMap<String, String>,
        outcome: EpisodeOutcome,
    ) {
        let episode = Episode {
            id: uuid::Uuid::new_v4().to_string(),
            timestamp: Utc::now(),
            event_type,
            description,
            context,
            outcome,
            emotional_valence: 0.0, // Will be set based on outcome
            learning_value: 0.5,    // Default learning value
            related_episodes: Vec::new(),
        };

        self.episodic_memory.add_episode(episode);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_concept (lines 403-421) ---[0m
    pub fn add_concept(
        &mut self,
        name: String,
        definition: String,
        properties: HashMap<String, String>,
    ) {
        let concept = Concept {
            name: name.clone(),
            definition,
            properties,
            examples: Vec::new(),
            confidence: 0.7,
            source_episodes: Vec::new(),
            last_updated: Utc::now(),
            usage_frequency: 1,
        };

        self.semantic_memory.add_concept(concept);
    }

  ./crates/ccswarm/src/session/memory.rs:440-460 method consolidate_memories <-> ./crates/ccswarm/src/session/memory.rs:693-715 method add_episode
  Similarity: 84.86%
  Classes: SessionMemory <-> EpisodicMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:consolidate_memories (lines 440-460) ---[0m
    pub fn consolidate_memories(&mut self) {
        // Process working memory items for consolidation
        let items_to_consolidate: Vec<_> = self
            .working_memory
            .current_items
            .iter()
            .filter(|item| self.should_consolidate_item(item))
            .cloned()
            .collect();

        for item in items_to_consolidate {
            self.consolidate_working_memory_item(&item);
        }

        // Clean up old working memory items
        self.working_memory.cleanup_expired_items();

        // Update consolidation timestamp
        self.last_consolidation = Utc::now();
        self.memory_stats.consolidation_events += 1;
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_episode (lines 693-715) ---[0m
    fn add_episode(&mut self, episode: Episode) {
        // Remove oldest episode if at capacity
        if self.episodes.len() >= self.max_episodes {
            if let Some(old_episode) = self.episodes.pop_front() {
                self.emotional_markers.remove(&old_episode.id);
            }
        }

        // Add emotional marker if significant
        if episode.emotional_valence.abs() > 0.5 {
            self.emotional_markers.insert(
                episode.id.clone(),
                EmotionalMarker {
                    event_id: episode.id.clone(),
                    emotion_type: EmotionType::Satisfaction, // Simplified
                    intensity: episode.emotional_valence.abs(),
                    influence_on_learning: episode.learning_value,
                },
            );
        }

        self.episodes.push_back(episode);
    }

  ./crates/ccswarm/src/session/memory.rs:380-400 method add_episode <-> ./crates/ccswarm/src/session/memory.rs:597-616 method generate_memory_summary
  Similarity: 89.45%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_episode (lines 380-400) ---[0m
    pub fn add_episode(
        &mut self,
        event_type: EpisodeType,
        description: String,
        context: HashMap<String, String>,
        outcome: EpisodeOutcome,
    ) {
        let episode = Episode {
            id: uuid::Uuid::new_v4().to_string(),
            timestamp: Utc::now(),
            event_type,
            description,
            context,
            outcome,
            emotional_valence: 0.0, // Will be set based on outcome
            learning_value: 0.5,    // Default learning value
            related_episodes: Vec::new(),
        };

        self.episodic_memory.add_episode(episode);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:generate_memory_summary (lines 597-616) ---[0m
    pub fn generate_memory_summary(&self) -> MemorySummary {
        MemorySummary {
            session_id: self.session_id.clone(),
            agent_id: self.agent_id.clone(),
            working_memory_load: self.working_memory.cognitive_load,
            episodic_memory_size: self.episodic_memory.episodes.len(),
            semantic_concepts: self.semantic_memory.concepts.len(),
            procedural_skills: self.procedural_memory.procedures.len(),
            recent_episodes: self
                .episodic_memory
                .episodes
                .iter()
                .rev()
                .take(5)
                .map(|e| e.description.clone())
                .collect(),
            last_consolidation: self.last_consolidation,
            memory_efficiency: self.memory_stats.memory_efficiency,
        }
    }

  ./crates/ccswarm/src/session/memory.rs:345-363 method add_to_working_memory <-> ./crates/ccswarm/src/session/memory.rs:693-715 method add_episode
  Similarity: 81.48%
  Classes: SessionMemory <-> EpisodicMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_to_working_memory (lines 345-363) ---[0m
    pub fn add_to_working_memory(
        &mut self,
        content: String,
        item_type: WorkingMemoryType,
        priority: f32,
    ) {
        let item = WorkingMemoryItem {
            id: uuid::Uuid::new_v4().to_string(),
            content,
            item_type,
            priority,
            decay_rate: 0.1,
            created_at: Utc::now(),
            last_accessed: Utc::now(),
        };

        self.working_memory.add_item(item);
        self.update_cognitive_load();
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_episode (lines 693-715) ---[0m
    fn add_episode(&mut self, episode: Episode) {
        // Remove oldest episode if at capacity
        if self.episodes.len() >= self.max_episodes {
            if let Some(old_episode) = self.episodes.pop_front() {
                self.emotional_markers.remove(&old_episode.id);
            }
        }

        // Add emotional marker if significant
        if episode.emotional_valence.abs() > 0.5 {
            self.emotional_markers.insert(
                episode.id.clone(),
                EmotionalMarker {
                    event_id: episode.id.clone(),
                    emotion_type: EmotionType::Satisfaction, // Simplified
                    intensity: episode.emotional_valence.abs(),
                    influence_on_learning: episode.learning_value,
                },
            );
        }

        self.episodes.push_back(episode);
    }

  ./crates/ccswarm/src/session/memory.rs:345-363 method add_to_working_memory <-> ./crates/ccswarm/src/session/memory.rs:380-400 method add_episode
  Similarity: 84.00%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_to_working_memory (lines 345-363) ---[0m
    pub fn add_to_working_memory(
        &mut self,
        content: String,
        item_type: WorkingMemoryType,
        priority: f32,
    ) {
        let item = WorkingMemoryItem {
            id: uuid::Uuid::new_v4().to_string(),
            content,
            item_type,
            priority,
            decay_rate: 0.1,
            created_at: Utc::now(),
            last_accessed: Utc::now(),
        };

        self.working_memory.add_item(item);
        self.update_cognitive_load();
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_episode (lines 380-400) ---[0m
    pub fn add_episode(
        &mut self,
        event_type: EpisodeType,
        description: String,
        context: HashMap<String, String>,
        outcome: EpisodeOutcome,
    ) {
        let episode = Episode {
            id: uuid::Uuid::new_v4().to_string(),
            timestamp: Utc::now(),
            event_type,
            description,
            context,
            outcome,
            emotional_valence: 0.0, // Will be set based on outcome
            learning_value: 0.5,    // Default learning value
            related_episodes: Vec::new(),
        };

        self.episodic_memory.add_episode(episode);
    }

  ./crates/ccswarm/src/session/memory.rs:380-400 method add_episode <-> ./crates/ccswarm/src/session/memory.rs:424-437 method add_procedure
  Similarity: 94.85%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_episode (lines 380-400) ---[0m
    pub fn add_episode(
        &mut self,
        event_type: EpisodeType,
        description: String,
        context: HashMap<String, String>,
        outcome: EpisodeOutcome,
    ) {
        let episode = Episode {
            id: uuid::Uuid::new_v4().to_string(),
            timestamp: Utc::now(),
            event_type,
            description,
            context,
            outcome,
            emotional_valence: 0.0, // Will be set based on outcome
            learning_value: 0.5,    // Default learning value
            related_episodes: Vec::new(),
        };

        self.episodic_memory.add_episode(episode);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_procedure (lines 424-437) ---[0m
    pub fn add_procedure(&mut self, name: String, steps: Vec<ProcedureStep>) {
        let procedure = Procedure {
            name: name.clone(),
            steps,
            prerequisites: Vec::new(),
            success_rate: 0.8,
            average_execution_time: std::time::Duration::from_secs(60),
            complexity_level: ComplexityLevel::Moderate,
            last_executed: None,
            optimization_history: Vec::new(),
        };

        self.procedural_memory.add_procedure(procedure);
    }

  ./crates/ccswarm/src/session/memory.rs:345-363 method add_to_working_memory <-> ./crates/ccswarm/src/session/memory.rs:597-616 method generate_memory_summary
  Similarity: 83.99%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_to_working_memory (lines 345-363) ---[0m
    pub fn add_to_working_memory(
        &mut self,
        content: String,
        item_type: WorkingMemoryType,
        priority: f32,
    ) {
        let item = WorkingMemoryItem {
            id: uuid::Uuid::new_v4().to_string(),
            content,
            item_type,
            priority,
            decay_rate: 0.1,
            created_at: Utc::now(),
            last_accessed: Utc::now(),
        };

        self.working_memory.add_item(item);
        self.update_cognitive_load();
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:generate_memory_summary (lines 597-616) ---[0m
    pub fn generate_memory_summary(&self) -> MemorySummary {
        MemorySummary {
            session_id: self.session_id.clone(),
            agent_id: self.agent_id.clone(),
            working_memory_load: self.working_memory.cognitive_load,
            episodic_memory_size: self.episodic_memory.episodes.len(),
            semantic_concepts: self.semantic_memory.concepts.len(),
            procedural_skills: self.procedural_memory.procedures.len(),
            recent_episodes: self
                .episodic_memory
                .episodes
                .iter()
                .rev()
                .take(5)
                .map(|e| e.description.clone())
                .collect(),
            last_consolidation: self.last_consolidation,
            memory_efficiency: self.memory_stats.memory_efficiency,
        }
    }

  ./crates/ccswarm/src/session/memory.rs:345-363 method add_to_working_memory <-> ./crates/ccswarm/src/session/memory.rs:403-421 method add_concept
  Similarity: 84.00%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_to_working_memory (lines 345-363) ---[0m
    pub fn add_to_working_memory(
        &mut self,
        content: String,
        item_type: WorkingMemoryType,
        priority: f32,
    ) {
        let item = WorkingMemoryItem {
            id: uuid::Uuid::new_v4().to_string(),
            content,
            item_type,
            priority,
            decay_rate: 0.1,
            created_at: Utc::now(),
            last_accessed: Utc::now(),
        };

        self.working_memory.add_item(item);
        self.update_cognitive_load();
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_concept (lines 403-421) ---[0m
    pub fn add_concept(
        &mut self,
        name: String,
        definition: String,
        properties: HashMap<String, String>,
    ) {
        let concept = Concept {
            name: name.clone(),
            definition,
            properties,
            examples: Vec::new(),
            confidence: 0.7,
            source_episodes: Vec::new(),
            last_updated: Utc::now(),
            usage_frequency: 1,
        };

        self.semantic_memory.add_concept(concept);
    }

  ./crates/ccswarm/src/session/memory.rs:366-377 method set_task_context <-> ./crates/ccswarm/src/session/memory.rs:597-616 method generate_memory_summary
  Similarity: 98.74%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:set_task_context (lines 366-377) ---[0m
    pub fn set_task_context(&mut self, task_id: String, description: String) {
        self.working_memory.active_task_context = Some(TaskContext {
            task_id,
            task_description: description,
            current_step: "initialization".to_string(),
            progress: 0.0,
            obstacles: Vec::new(),
            solutions_attempted: Vec::new(),
            time_spent: std::time::Duration::from_secs(0),
            expected_completion: None,
        });
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:generate_memory_summary (lines 597-616) ---[0m
    pub fn generate_memory_summary(&self) -> MemorySummary {
        MemorySummary {
            session_id: self.session_id.clone(),
            agent_id: self.agent_id.clone(),
            working_memory_load: self.working_memory.cognitive_load,
            episodic_memory_size: self.episodic_memory.episodes.len(),
            semantic_concepts: self.semantic_memory.concepts.len(),
            procedural_skills: self.procedural_memory.procedures.len(),
            recent_episodes: self
                .episodic_memory
                .episodes
                .iter()
                .rev()
                .take(5)
                .map(|e| e.description.clone())
                .collect(),
            last_consolidation: self.last_consolidation,
            memory_efficiency: self.memory_stats.memory_efficiency,
        }
    }

  ./crates/ccswarm/src/session/memory.rs:403-421 method add_concept <-> ./crates/ccswarm/src/session/memory.rs:424-437 method add_procedure
  Similarity: 94.85%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_concept (lines 403-421) ---[0m
    pub fn add_concept(
        &mut self,
        name: String,
        definition: String,
        properties: HashMap<String, String>,
    ) {
        let concept = Concept {
            name: name.clone(),
            definition,
            properties,
            examples: Vec::new(),
            confidence: 0.7,
            source_episodes: Vec::new(),
            last_updated: Utc::now(),
            usage_frequency: 1,
        };

        self.semantic_memory.add_concept(concept);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_procedure (lines 424-437) ---[0m
    pub fn add_procedure(&mut self, name: String, steps: Vec<ProcedureStep>) {
        let procedure = Procedure {
            name: name.clone(),
            steps,
            prerequisites: Vec::new(),
            success_rate: 0.8,
            average_execution_time: std::time::Duration::from_secs(60),
            complexity_level: ComplexityLevel::Moderate,
            last_executed: None,
            optimization_history: Vec::new(),
        };

        self.procedural_memory.add_procedure(procedure);
    }

  ./crates/ccswarm/src/session/memory.rs:424-437 method add_procedure <-> ./crates/ccswarm/src/session/memory.rs:597-616 method generate_memory_summary
  Similarity: 89.45%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_procedure (lines 424-437) ---[0m
    pub fn add_procedure(&mut self, name: String, steps: Vec<ProcedureStep>) {
        let procedure = Procedure {
            name: name.clone(),
            steps,
            prerequisites: Vec::new(),
            success_rate: 0.8,
            average_execution_time: std::time::Duration::from_secs(60),
            complexity_level: ComplexityLevel::Moderate,
            last_executed: None,
            optimization_history: Vec::new(),
        };

        self.procedural_memory.add_procedure(procedure);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:generate_memory_summary (lines 597-616) ---[0m
    pub fn generate_memory_summary(&self) -> MemorySummary {
        MemorySummary {
            session_id: self.session_id.clone(),
            agent_id: self.agent_id.clone(),
            working_memory_load: self.working_memory.cognitive_load,
            episodic_memory_size: self.episodic_memory.episodes.len(),
            semantic_concepts: self.semantic_memory.concepts.len(),
            procedural_skills: self.procedural_memory.procedures.len(),
            recent_episodes: self
                .episodic_memory
                .episodes
                .iter()
                .rev()
                .take(5)
                .map(|e| e.description.clone())
                .collect(),
            last_consolidation: self.last_consolidation,
            memory_efficiency: self.memory_stats.memory_efficiency,
        }
    }

  ./crates/ccswarm/src/session/memory.rs:345-363 method add_to_working_memory <-> ./crates/ccswarm/src/session/memory.rs:424-437 method add_procedure
  Similarity: 84.95%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_to_working_memory (lines 345-363) ---[0m
    pub fn add_to_working_memory(
        &mut self,
        content: String,
        item_type: WorkingMemoryType,
        priority: f32,
    ) {
        let item = WorkingMemoryItem {
            id: uuid::Uuid::new_v4().to_string(),
            content,
            item_type,
            priority,
            decay_rate: 0.1,
            created_at: Utc::now(),
            last_accessed: Utc::now(),
        };

        self.working_memory.add_item(item);
        self.update_cognitive_load();
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_procedure (lines 424-437) ---[0m
    pub fn add_procedure(&mut self, name: String, steps: Vec<ProcedureStep>) {
        let procedure = Procedure {
            name: name.clone(),
            steps,
            prerequisites: Vec::new(),
            success_rate: 0.8,
            average_execution_time: std::time::Duration::from_secs(60),
            complexity_level: ComplexityLevel::Moderate,
            last_executed: None,
            optimization_history: Vec::new(),
        };

        self.procedural_memory.add_procedure(procedure);
    }

  ./crates/ccswarm/src/session/memory.rs:380-400 method add_episode <-> ./crates/ccswarm/src/session/memory.rs:673-680 method cleanup_expired_items
  Similarity: 93.66%
  Classes: SessionMemory <-> WorkingMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_episode (lines 380-400) ---[0m
    pub fn add_episode(
        &mut self,
        event_type: EpisodeType,
        description: String,
        context: HashMap<String, String>,
        outcome: EpisodeOutcome,
    ) {
        let episode = Episode {
            id: uuid::Uuid::new_v4().to_string(),
            timestamp: Utc::now(),
            event_type,
            description,
            context,
            outcome,
            emotional_valence: 0.0, // Will be set based on outcome
            learning_value: 0.5,    // Default learning value
            related_episodes: Vec::new(),
        };

        self.episodic_memory.add_episode(episode);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:cleanup_expired_items (lines 673-680) ---[0m
    fn cleanup_expired_items(&mut self) {
        let now = Utc::now();
        self.current_items.retain(|item| {
            let age = now - item.created_at;
            let decay_threshold = item.decay_rate * age.num_minutes() as f32;
            decay_threshold < 1.0 // Keep if not fully decayed
        });
    }

  ./crates/ccswarm/src/session/memory.rs:366-377 method set_task_context <-> ./crates/ccswarm/src/session/memory.rs:380-400 method add_episode
  Similarity: 80.31%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:set_task_context (lines 366-377) ---[0m
    pub fn set_task_context(&mut self, task_id: String, description: String) {
        self.working_memory.active_task_context = Some(TaskContext {
            task_id,
            task_description: description,
            current_step: "initialization".to_string(),
            progress: 0.0,
            obstacles: Vec::new(),
            solutions_attempted: Vec::new(),
            time_spent: std::time::Duration::from_secs(0),
            expected_completion: None,
        });
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_episode (lines 380-400) ---[0m
    pub fn add_episode(
        &mut self,
        event_type: EpisodeType,
        description: String,
        context: HashMap<String, String>,
        outcome: EpisodeOutcome,
    ) {
        let episode = Episode {
            id: uuid::Uuid::new_v4().to_string(),
            timestamp: Utc::now(),
            event_type,
            description,
            context,
            outcome,
            emotional_valence: 0.0, // Will be set based on outcome
            learning_value: 0.5,    // Default learning value
            related_episodes: Vec::new(),
        };

        self.episodic_memory.add_episode(episode);
    }

  ./crates/ccswarm/src/session/memory.rs:331-342 method new <-> ./crates/ccswarm/src/session/memory.rs:380-400 method add_episode
  Similarity: 79.25%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 331-342) ---[0m
    pub fn new(session_id: String, agent_id: String) -> Self {
        Self {
            session_id,
            agent_id,
            working_memory: WorkingMemory::new(),
            episodic_memory: EpisodicMemory::new(),
            semantic_memory: SemanticMemory::new(),
            procedural_memory: ProceduralMemory::new(),
            memory_stats: MemoryStats::new(),
            last_consolidation: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_episode (lines 380-400) ---[0m
    pub fn add_episode(
        &mut self,
        event_type: EpisodeType,
        description: String,
        context: HashMap<String, String>,
        outcome: EpisodeOutcome,
    ) {
        let episode = Episode {
            id: uuid::Uuid::new_v4().to_string(),
            timestamp: Utc::now(),
            event_type,
            description,
            context,
            outcome,
            emotional_valence: 0.0, // Will be set based on outcome
            learning_value: 0.5,    // Default learning value
            related_episodes: Vec::new(),
        };

        self.episodic_memory.add_episode(episode);
    }

  ./crates/ccswarm/src/session/memory.rs:380-400 method add_episode <-> ./crates/ccswarm/src/session/memory.rs:749-759 method new
  Similarity: 79.25%
  Classes: SessionMemory <-> MemoryStats

[36m--- ./crates/ccswarm/src/session/memory.rs:add_episode (lines 380-400) ---[0m
    pub fn add_episode(
        &mut self,
        event_type: EpisodeType,
        description: String,
        context: HashMap<String, String>,
        outcome: EpisodeOutcome,
    ) {
        let episode = Episode {
            id: uuid::Uuid::new_v4().to_string(),
            timestamp: Utc::now(),
            event_type,
            description,
            context,
            outcome,
            emotional_valence: 0.0, // Will be set based on outcome
            learning_value: 0.5,    // Default learning value
            related_episodes: Vec::new(),
        };

        self.episodic_memory.add_episode(episode);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 749-759) ---[0m
    fn new() -> Self {
        Self {
            working_memory_utilization: 0.0,
            episodic_memory_size: 0,
            semantic_concepts_count: 0,
            procedural_skills_count: 0,
            consolidation_events: 0,
            memory_efficiency: 1.0,
            last_updated: Utc::now(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:403-421 method add_concept <-> ./crates/ccswarm/src/session/memory.rs:673-680 method cleanup_expired_items
  Similarity: 93.37%
  Classes: SessionMemory <-> WorkingMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_concept (lines 403-421) ---[0m
    pub fn add_concept(
        &mut self,
        name: String,
        definition: String,
        properties: HashMap<String, String>,
    ) {
        let concept = Concept {
            name: name.clone(),
            definition,
            properties,
            examples: Vec::new(),
            confidence: 0.7,
            source_episodes: Vec::new(),
            last_updated: Utc::now(),
            usage_frequency: 1,
        };

        self.semantic_memory.add_concept(concept);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:cleanup_expired_items (lines 673-680) ---[0m
    fn cleanup_expired_items(&mut self) {
        let now = Utc::now();
        self.current_items.retain(|item| {
            let age = now - item.created_at;
            let decay_threshold = item.decay_rate * age.num_minutes() as f32;
            decay_threshold < 1.0 // Keep if not fully decayed
        });
    }

  ./crates/ccswarm/src/session/memory.rs:366-377 method set_task_context <-> ./crates/ccswarm/src/session/memory.rs:403-421 method add_concept
  Similarity: 80.31%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:set_task_context (lines 366-377) ---[0m
    pub fn set_task_context(&mut self, task_id: String, description: String) {
        self.working_memory.active_task_context = Some(TaskContext {
            task_id,
            task_description: description,
            current_step: "initialization".to_string(),
            progress: 0.0,
            obstacles: Vec::new(),
            solutions_attempted: Vec::new(),
            time_spent: std::time::Duration::from_secs(0),
            expected_completion: None,
        });
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_concept (lines 403-421) ---[0m
    pub fn add_concept(
        &mut self,
        name: String,
        definition: String,
        properties: HashMap<String, String>,
    ) {
        let concept = Concept {
            name: name.clone(),
            definition,
            properties,
            examples: Vec::new(),
            confidence: 0.7,
            source_episodes: Vec::new(),
            last_updated: Utc::now(),
            usage_frequency: 1,
        };

        self.semantic_memory.add_concept(concept);
    }

  ./crates/ccswarm/src/session/memory.rs:331-342 method new <-> ./crates/ccswarm/src/session/memory.rs:403-421 method add_concept
  Similarity: 78.31%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 331-342) ---[0m
    pub fn new(session_id: String, agent_id: String) -> Self {
        Self {
            session_id,
            agent_id,
            working_memory: WorkingMemory::new(),
            episodic_memory: EpisodicMemory::new(),
            semantic_memory: SemanticMemory::new(),
            procedural_memory: ProceduralMemory::new(),
            memory_stats: MemoryStats::new(),
            last_consolidation: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_concept (lines 403-421) ---[0m
    pub fn add_concept(
        &mut self,
        name: String,
        definition: String,
        properties: HashMap<String, String>,
    ) {
        let concept = Concept {
            name: name.clone(),
            definition,
            properties,
            examples: Vec::new(),
            confidence: 0.7,
            source_episodes: Vec::new(),
            last_updated: Utc::now(),
            usage_frequency: 1,
        };

        self.semantic_memory.add_concept(concept);
    }

  ./crates/ccswarm/src/session/memory.rs:440-460 method consolidate_memories <-> ./crates/ccswarm/src/session/memory.rs:533-541 method update_cognitive_load
  Similarity: 79.39%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:consolidate_memories (lines 440-460) ---[0m
    pub fn consolidate_memories(&mut self) {
        // Process working memory items for consolidation
        let items_to_consolidate: Vec<_> = self
            .working_memory
            .current_items
            .iter()
            .filter(|item| self.should_consolidate_item(item))
            .cloned()
            .collect();

        for item in items_to_consolidate {
            self.consolidate_working_memory_item(&item);
        }

        // Clean up old working memory items
        self.working_memory.cleanup_expired_items();

        // Update consolidation timestamp
        self.last_consolidation = Utc::now();
        self.memory_stats.consolidation_events += 1;
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:update_cognitive_load (lines 533-541) ---[0m
    fn update_cognitive_load(&mut self) {
        let utilization =
            self.working_memory.current_items.len() as f32 / WORKING_MEMORY_CAPACITY as f32;
        self.working_memory.cognitive_load = utilization.min(1.0);

        // Update memory stats
        self.memory_stats.working_memory_utilization = utilization;
        self.memory_stats.last_updated = Utc::now();
    }

  ./crates/ccswarm/src/session/memory.rs:403-421 method add_concept <-> ./crates/ccswarm/src/session/memory.rs:749-759 method new
  Similarity: 78.31%
  Classes: SessionMemory <-> MemoryStats

[36m--- ./crates/ccswarm/src/session/memory.rs:add_concept (lines 403-421) ---[0m
    pub fn add_concept(
        &mut self,
        name: String,
        definition: String,
        properties: HashMap<String, String>,
    ) {
        let concept = Concept {
            name: name.clone(),
            definition,
            properties,
            examples: Vec::new(),
            confidence: 0.7,
            source_episodes: Vec::new(),
            last_updated: Utc::now(),
            usage_frequency: 1,
        };

        self.semantic_memory.add_concept(concept);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 749-759) ---[0m
    fn new() -> Self {
        Self {
            working_memory_utilization: 0.0,
            episodic_memory_size: 0,
            semantic_concepts_count: 0,
            procedural_skills_count: 0,
            consolidation_events: 0,
            memory_efficiency: 1.0,
            last_updated: Utc::now(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:331-342 method new <-> ./crates/ccswarm/src/session/memory.rs:366-377 method set_task_context
  Similarity: 97.65%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 331-342) ---[0m
    pub fn new(session_id: String, agent_id: String) -> Self {
        Self {
            session_id,
            agent_id,
            working_memory: WorkingMemory::new(),
            episodic_memory: EpisodicMemory::new(),
            semantic_memory: SemanticMemory::new(),
            procedural_memory: ProceduralMemory::new(),
            memory_stats: MemoryStats::new(),
            last_consolidation: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:set_task_context (lines 366-377) ---[0m
    pub fn set_task_context(&mut self, task_id: String, description: String) {
        self.working_memory.active_task_context = Some(TaskContext {
            task_id,
            task_description: description,
            current_step: "initialization".to_string(),
            progress: 0.0,
            obstacles: Vec::new(),
            solutions_attempted: Vec::new(),
            time_spent: std::time::Duration::from_secs(0),
            expected_completion: None,
        });
    }

  ./crates/ccswarm/src/session/memory.rs:380-400 method add_episode <-> ./crates/ccswarm/src/session/memory.rs:629-636 method new
  Similarity: 79.25%
  Classes: SessionMemory <-> RetrievalResult

[36m--- ./crates/ccswarm/src/session/memory.rs:add_episode (lines 380-400) ---[0m
    pub fn add_episode(
        &mut self,
        event_type: EpisodeType,
        description: String,
        context: HashMap<String, String>,
        outcome: EpisodeOutcome,
    ) {
        let episode = Episode {
            id: uuid::Uuid::new_v4().to_string(),
            timestamp: Utc::now(),
            event_type,
            description,
            context,
            outcome,
            emotional_valence: 0.0, // Will be set based on outcome
            learning_value: 0.5,    // Default learning value
            related_episodes: Vec::new(),
        };

        self.episodic_memory.add_episode(episode);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 629-636) ---[0m
    fn new() -> Self {
        Self {
            working_memory_items: Vec::new(),
            relevant_episodes: Vec::new(),
            relevant_concepts: Vec::new(),
            relevant_procedures: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:380-400 method add_episode <-> ./crates/ccswarm/src/session/memory.rs:684-691 method new
  Similarity: 79.25%
  Classes: SessionMemory <-> EpisodicMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_episode (lines 380-400) ---[0m
    pub fn add_episode(
        &mut self,
        event_type: EpisodeType,
        description: String,
        context: HashMap<String, String>,
        outcome: EpisodeOutcome,
    ) {
        let episode = Episode {
            id: uuid::Uuid::new_v4().to_string(),
            timestamp: Utc::now(),
            event_type,
            description,
            context,
            outcome,
            emotional_valence: 0.0, // Will be set based on outcome
            learning_value: 0.5,    // Default learning value
            related_episodes: Vec::new(),
        };

        self.episodic_memory.add_episode(episode);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 684-691) ---[0m
    fn new() -> Self {
        Self {
            episodes: VecDeque::with_capacity(1000),
            max_episodes: 1000,
            recent_experiences: Vec::new(),
            emotional_markers: HashMap::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:380-400 method add_episode <-> ./crates/ccswarm/src/session/memory.rs:719-726 method new
  Similarity: 79.25%
  Classes: SessionMemory <-> SemanticMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_episode (lines 380-400) ---[0m
    pub fn add_episode(
        &mut self,
        event_type: EpisodeType,
        description: String,
        context: HashMap<String, String>,
        outcome: EpisodeOutcome,
    ) {
        let episode = Episode {
            id: uuid::Uuid::new_v4().to_string(),
            timestamp: Utc::now(),
            event_type,
            description,
            context,
            outcome,
            emotional_valence: 0.0, // Will be set based on outcome
            learning_value: 0.5,    // Default learning value
            related_episodes: Vec::new(),
        };

        self.episodic_memory.add_episode(episode);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 719-726) ---[0m
    fn new() -> Self {
        Self {
            concepts: HashMap::new(),
            relationships: Vec::new(),
            knowledge_domains: HashMap::new(),
            fact_base: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:380-400 method add_episode <-> ./crates/ccswarm/src/session/memory.rs:734-741 method new
  Similarity: 79.25%
  Classes: SessionMemory <-> ProceduralMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_episode (lines 380-400) ---[0m
    pub fn add_episode(
        &mut self,
        event_type: EpisodeType,
        description: String,
        context: HashMap<String, String>,
        outcome: EpisodeOutcome,
    ) {
        let episode = Episode {
            id: uuid::Uuid::new_v4().to_string(),
            timestamp: Utc::now(),
            event_type,
            description,
            context,
            outcome,
            emotional_valence: 0.0, // Will be set based on outcome
            learning_value: 0.5,    // Default learning value
            related_episodes: Vec::new(),
        };

        self.episodic_memory.add_episode(episode);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 734-741) ---[0m
    fn new() -> Self {
        Self {
            procedures: HashMap::new(),
            skill_patterns: Vec::new(),
            automation_levels: HashMap::new(),
            execution_templates: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:345-363 method add_to_working_memory <-> ./crates/ccswarm/src/session/memory.rs:673-680 method cleanup_expired_items
  Similarity: 83.26%
  Classes: SessionMemory <-> WorkingMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_to_working_memory (lines 345-363) ---[0m
    pub fn add_to_working_memory(
        &mut self,
        content: String,
        item_type: WorkingMemoryType,
        priority: f32,
    ) {
        let item = WorkingMemoryItem {
            id: uuid::Uuid::new_v4().to_string(),
            content,
            item_type,
            priority,
            decay_rate: 0.1,
            created_at: Utc::now(),
            last_accessed: Utc::now(),
        };

        self.working_memory.add_item(item);
        self.update_cognitive_load();
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:cleanup_expired_items (lines 673-680) ---[0m
    fn cleanup_expired_items(&mut self) {
        let now = Utc::now();
        self.current_items.retain(|item| {
            let age = now - item.created_at;
            let decay_threshold = item.decay_rate * age.num_minutes() as f32;
            decay_threshold < 1.0 // Keep if not fully decayed
        });
    }

  ./crates/ccswarm/src/session/memory.rs:345-363 method add_to_working_memory <-> ./crates/ccswarm/src/session/memory.rs:665-671 method add_item
  Similarity: 85.89%
  Classes: SessionMemory <-> WorkingMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_to_working_memory (lines 345-363) ---[0m
    pub fn add_to_working_memory(
        &mut self,
        content: String,
        item_type: WorkingMemoryType,
        priority: f32,
    ) {
        let item = WorkingMemoryItem {
            id: uuid::Uuid::new_v4().to_string(),
            content,
            item_type,
            priority,
            decay_rate: 0.1,
            created_at: Utc::now(),
            last_accessed: Utc::now(),
        };

        self.working_memory.add_item(item);
        self.update_cognitive_load();
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_item (lines 665-671) ---[0m
    fn add_item(&mut self, item: WorkingMemoryItem) {
        // Remove oldest item if at capacity
        if self.current_items.len() >= self.capacity {
            self.current_items.pop_front();
        }
        self.current_items.push_back(item);
    }

  ./crates/ccswarm/src/session/memory.rs:403-421 method add_concept <-> ./crates/ccswarm/src/session/memory.rs:655-663 method new
  Similarity: 78.31%
  Classes: SessionMemory <-> WorkingMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_concept (lines 403-421) ---[0m
    pub fn add_concept(
        &mut self,
        name: String,
        definition: String,
        properties: HashMap<String, String>,
    ) {
        let concept = Concept {
            name: name.clone(),
            definition,
            properties,
            examples: Vec::new(),
            confidence: 0.7,
            source_episodes: Vec::new(),
            last_updated: Utc::now(),
            usage_frequency: 1,
        };

        self.semantic_memory.add_concept(concept);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 655-663) ---[0m
    fn new() -> Self {
        Self {
            current_items: VecDeque::with_capacity(WORKING_MEMORY_CAPACITY),
            capacity: WORKING_MEMORY_CAPACITY,
            active_task_context: None,
            attention_focus: Vec::new(),
            cognitive_load: 0.0,
        }
    }

  ./crates/ccswarm/src/session/memory.rs:345-363 method add_to_working_memory <-> ./crates/ccswarm/src/session/memory.rs:366-377 method set_task_context
  Similarity: 70.10%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_to_working_memory (lines 345-363) ---[0m
    pub fn add_to_working_memory(
        &mut self,
        content: String,
        item_type: WorkingMemoryType,
        priority: f32,
    ) {
        let item = WorkingMemoryItem {
            id: uuid::Uuid::new_v4().to_string(),
            content,
            item_type,
            priority,
            decay_rate: 0.1,
            created_at: Utc::now(),
            last_accessed: Utc::now(),
        };

        self.working_memory.add_item(item);
        self.update_cognitive_load();
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:set_task_context (lines 366-377) ---[0m
    pub fn set_task_context(&mut self, task_id: String, description: String) {
        self.working_memory.active_task_context = Some(TaskContext {
            task_id,
            task_description: description,
            current_step: "initialization".to_string(),
            progress: 0.0,
            obstacles: Vec::new(),
            solutions_attempted: Vec::new(),
            time_spent: std::time::Duration::from_secs(0),
            expected_completion: None,
        });
    }

  ./crates/ccswarm/src/session/memory.rs:403-421 method add_concept <-> ./crates/ccswarm/src/session/memory.rs:629-636 method new
  Similarity: 78.31%
  Classes: SessionMemory <-> RetrievalResult

[36m--- ./crates/ccswarm/src/session/memory.rs:add_concept (lines 403-421) ---[0m
    pub fn add_concept(
        &mut self,
        name: String,
        definition: String,
        properties: HashMap<String, String>,
    ) {
        let concept = Concept {
            name: name.clone(),
            definition,
            properties,
            examples: Vec::new(),
            confidence: 0.7,
            source_episodes: Vec::new(),
            last_updated: Utc::now(),
            usage_frequency: 1,
        };

        self.semantic_memory.add_concept(concept);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 629-636) ---[0m
    fn new() -> Self {
        Self {
            working_memory_items: Vec::new(),
            relevant_episodes: Vec::new(),
            relevant_concepts: Vec::new(),
            relevant_procedures: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:403-421 method add_concept <-> ./crates/ccswarm/src/session/memory.rs:684-691 method new
  Similarity: 78.31%
  Classes: SessionMemory <-> EpisodicMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_concept (lines 403-421) ---[0m
    pub fn add_concept(
        &mut self,
        name: String,
        definition: String,
        properties: HashMap<String, String>,
    ) {
        let concept = Concept {
            name: name.clone(),
            definition,
            properties,
            examples: Vec::new(),
            confidence: 0.7,
            source_episodes: Vec::new(),
            last_updated: Utc::now(),
            usage_frequency: 1,
        };

        self.semantic_memory.add_concept(concept);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 684-691) ---[0m
    fn new() -> Self {
        Self {
            episodes: VecDeque::with_capacity(1000),
            max_episodes: 1000,
            recent_experiences: Vec::new(),
            emotional_markers: HashMap::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:403-421 method add_concept <-> ./crates/ccswarm/src/session/memory.rs:719-726 method new
  Similarity: 78.31%
  Classes: SessionMemory <-> SemanticMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_concept (lines 403-421) ---[0m
    pub fn add_concept(
        &mut self,
        name: String,
        definition: String,
        properties: HashMap<String, String>,
    ) {
        let concept = Concept {
            name: name.clone(),
            definition,
            properties,
            examples: Vec::new(),
            confidence: 0.7,
            source_episodes: Vec::new(),
            last_updated: Utc::now(),
            usage_frequency: 1,
        };

        self.semantic_memory.add_concept(concept);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 719-726) ---[0m
    fn new() -> Self {
        Self {
            concepts: HashMap::new(),
            relationships: Vec::new(),
            knowledge_domains: HashMap::new(),
            fact_base: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:403-421 method add_concept <-> ./crates/ccswarm/src/session/memory.rs:734-741 method new
  Similarity: 78.31%
  Classes: SessionMemory <-> ProceduralMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_concept (lines 403-421) ---[0m
    pub fn add_concept(
        &mut self,
        name: String,
        definition: String,
        properties: HashMap<String, String>,
    ) {
        let concept = Concept {
            name: name.clone(),
            definition,
            properties,
            examples: Vec::new(),
            confidence: 0.7,
            source_episodes: Vec::new(),
            last_updated: Utc::now(),
            usage_frequency: 1,
        };

        self.semantic_memory.add_concept(concept);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 734-741) ---[0m
    fn new() -> Self {
        Self {
            procedures: HashMap::new(),
            skill_patterns: Vec::new(),
            automation_levels: HashMap::new(),
            execution_templates: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:380-400 method add_episode <-> ./crates/ccswarm/src/session/memory.rs:463-469 method should_consolidate_item
  Similarity: 75.48%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_episode (lines 380-400) ---[0m
    pub fn add_episode(
        &mut self,
        event_type: EpisodeType,
        description: String,
        context: HashMap<String, String>,
        outcome: EpisodeOutcome,
    ) {
        let episode = Episode {
            id: uuid::Uuid::new_v4().to_string(),
            timestamp: Utc::now(),
            event_type,
            description,
            context,
            outcome,
            emotional_valence: 0.0, // Will be set based on outcome
            learning_value: 0.5,    // Default learning value
            related_episodes: Vec::new(),
        };

        self.episodic_memory.add_episode(episode);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:should_consolidate_item (lines 463-469) ---[0m
    fn should_consolidate_item(&self, item: &WorkingMemoryItem) -> bool {
        let age = Utc::now() - item.created_at;
        let significance = item.priority;

        // Consolidate if item is old enough and significant
        age.num_minutes() > 30 && significance > 0.7
    }

  ./crates/ccswarm/src/session/memory.rs:331-342 method new <-> ./crates/ccswarm/src/session/memory.rs:424-437 method add_procedure
  Similarity: 80.89%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 331-342) ---[0m
    pub fn new(session_id: String, agent_id: String) -> Self {
        Self {
            session_id,
            agent_id,
            working_memory: WorkingMemory::new(),
            episodic_memory: EpisodicMemory::new(),
            semantic_memory: SemanticMemory::new(),
            procedural_memory: ProceduralMemory::new(),
            memory_stats: MemoryStats::new(),
            last_consolidation: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_procedure (lines 424-437) ---[0m
    pub fn add_procedure(&mut self, name: String, steps: Vec<ProcedureStep>) {
        let procedure = Procedure {
            name: name.clone(),
            steps,
            prerequisites: Vec::new(),
            success_rate: 0.8,
            average_execution_time: std::time::Duration::from_secs(60),
            complexity_level: ComplexityLevel::Moderate,
            last_executed: None,
            optimization_history: Vec::new(),
        };

        self.procedural_memory.add_procedure(procedure);
    }

  ./crates/ccswarm/src/session/memory.rs:366-377 method set_task_context <-> ./crates/ccswarm/src/session/memory.rs:424-437 method add_procedure
  Similarity: 80.89%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:set_task_context (lines 366-377) ---[0m
    pub fn set_task_context(&mut self, task_id: String, description: String) {
        self.working_memory.active_task_context = Some(TaskContext {
            task_id,
            task_description: description,
            current_step: "initialization".to_string(),
            progress: 0.0,
            obstacles: Vec::new(),
            solutions_attempted: Vec::new(),
            time_spent: std::time::Duration::from_secs(0),
            expected_completion: None,
        });
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_procedure (lines 424-437) ---[0m
    pub fn add_procedure(&mut self, name: String, steps: Vec<ProcedureStep>) {
        let procedure = Procedure {
            name: name.clone(),
            steps,
            prerequisites: Vec::new(),
            success_rate: 0.8,
            average_execution_time: std::time::Duration::from_secs(60),
            complexity_level: ComplexityLevel::Moderate,
            last_executed: None,
            optimization_history: Vec::new(),
        };

        self.procedural_memory.add_procedure(procedure);
    }

  ./crates/ccswarm/src/session/memory.rs:424-437 method add_procedure <-> ./crates/ccswarm/src/session/memory.rs:673-680 method cleanup_expired_items
  Similarity: 94.16%
  Classes: SessionMemory <-> WorkingMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_procedure (lines 424-437) ---[0m
    pub fn add_procedure(&mut self, name: String, steps: Vec<ProcedureStep>) {
        let procedure = Procedure {
            name: name.clone(),
            steps,
            prerequisites: Vec::new(),
            success_rate: 0.8,
            average_execution_time: std::time::Duration::from_secs(60),
            complexity_level: ComplexityLevel::Moderate,
            last_executed: None,
            optimization_history: Vec::new(),
        };

        self.procedural_memory.add_procedure(procedure);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:cleanup_expired_items (lines 673-680) ---[0m
    fn cleanup_expired_items(&mut self) {
        let now = Utc::now();
        self.current_items.retain(|item| {
            let age = now - item.created_at;
            let decay_threshold = item.decay_rate * age.num_minutes() as f32;
            decay_threshold < 1.0 // Keep if not fully decayed
        });
    }

  ./crates/ccswarm/src/session/memory.rs:345-363 method add_to_working_memory <-> ./crates/ccswarm/src/session/memory.rs:463-469 method should_consolidate_item
  Similarity: 79.47%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_to_working_memory (lines 345-363) ---[0m
    pub fn add_to_working_memory(
        &mut self,
        content: String,
        item_type: WorkingMemoryType,
        priority: f32,
    ) {
        let item = WorkingMemoryItem {
            id: uuid::Uuid::new_v4().to_string(),
            content,
            item_type,
            priority,
            decay_rate: 0.1,
            created_at: Utc::now(),
            last_accessed: Utc::now(),
        };

        self.working_memory.add_item(item);
        self.update_cognitive_load();
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:should_consolidate_item (lines 463-469) ---[0m
    fn should_consolidate_item(&self, item: &WorkingMemoryItem) -> bool {
        let age = Utc::now() - item.created_at;
        let significance = item.priority;

        // Consolidate if item is old enough and significant
        age.num_minutes() > 30 && significance > 0.7
    }

  ./crates/ccswarm/src/session/memory.rs:380-400 method add_episode <-> ./crates/ccswarm/src/session/memory.rs:665-671 method add_item
  Similarity: 73.01%
  Classes: SessionMemory <-> WorkingMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_episode (lines 380-400) ---[0m
    pub fn add_episode(
        &mut self,
        event_type: EpisodeType,
        description: String,
        context: HashMap<String, String>,
        outcome: EpisodeOutcome,
    ) {
        let episode = Episode {
            id: uuid::Uuid::new_v4().to_string(),
            timestamp: Utc::now(),
            event_type,
            description,
            context,
            outcome,
            emotional_valence: 0.0, // Will be set based on outcome
            learning_value: 0.5,    // Default learning value
            related_episodes: Vec::new(),
        };

        self.episodic_memory.add_episode(episode);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_item (lines 665-671) ---[0m
    fn add_item(&mut self, item: WorkingMemoryItem) {
        // Remove oldest item if at capacity
        if self.current_items.len() >= self.capacity {
            self.current_items.pop_front();
        }
        self.current_items.push_back(item);
    }

  ./crates/ccswarm/src/session/memory.rs:331-342 method new <-> ./crates/ccswarm/src/session/memory.rs:749-759 method new
  Similarity: 85.95%
  Classes: SessionMemory <-> MemoryStats

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 331-342) ---[0m
    pub fn new(session_id: String, agent_id: String) -> Self {
        Self {
            session_id,
            agent_id,
            working_memory: WorkingMemory::new(),
            episodic_memory: EpisodicMemory::new(),
            semantic_memory: SemanticMemory::new(),
            procedural_memory: ProceduralMemory::new(),
            memory_stats: MemoryStats::new(),
            last_consolidation: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 749-759) ---[0m
    fn new() -> Self {
        Self {
            working_memory_utilization: 0.0,
            episodic_memory_size: 0,
            semantic_concepts_count: 0,
            procedural_skills_count: 0,
            consolidation_events: 0,
            memory_efficiency: 1.0,
            last_updated: Utc::now(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:345-363 method add_to_working_memory <-> ./crates/ccswarm/src/session/memory.rs:533-541 method update_cognitive_load
  Similarity: 70.42%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_to_working_memory (lines 345-363) ---[0m
    pub fn add_to_working_memory(
        &mut self,
        content: String,
        item_type: WorkingMemoryType,
        priority: f32,
    ) {
        let item = WorkingMemoryItem {
            id: uuid::Uuid::new_v4().to_string(),
            content,
            item_type,
            priority,
            decay_rate: 0.1,
            created_at: Utc::now(),
            last_accessed: Utc::now(),
        };

        self.working_memory.add_item(item);
        self.update_cognitive_load();
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:update_cognitive_load (lines 533-541) ---[0m
    fn update_cognitive_load(&mut self) {
        let utilization =
            self.working_memory.current_items.len() as f32 / WORKING_MEMORY_CAPACITY as f32;
        self.working_memory.cognitive_load = utilization.min(1.0);

        // Update memory stats
        self.memory_stats.working_memory_utilization = utilization;
        self.memory_stats.last_updated = Utc::now();
    }

  ./crates/ccswarm/src/session/memory.rs:366-377 method set_task_context <-> ./crates/ccswarm/src/session/memory.rs:629-636 method new
  Similarity: 97.65%
  Classes: SessionMemory <-> RetrievalResult

[36m--- ./crates/ccswarm/src/session/memory.rs:set_task_context (lines 366-377) ---[0m
    pub fn set_task_context(&mut self, task_id: String, description: String) {
        self.working_memory.active_task_context = Some(TaskContext {
            task_id,
            task_description: description,
            current_step: "initialization".to_string(),
            progress: 0.0,
            obstacles: Vec::new(),
            solutions_attempted: Vec::new(),
            time_spent: std::time::Duration::from_secs(0),
            expected_completion: None,
        });
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 629-636) ---[0m
    fn new() -> Self {
        Self {
            working_memory_items: Vec::new(),
            relevant_episodes: Vec::new(),
            relevant_concepts: Vec::new(),
            relevant_procedures: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:366-377 method set_task_context <-> ./crates/ccswarm/src/session/memory.rs:719-726 method new
  Similarity: 97.65%
  Classes: SessionMemory <-> SemanticMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:set_task_context (lines 366-377) ---[0m
    pub fn set_task_context(&mut self, task_id: String, description: String) {
        self.working_memory.active_task_context = Some(TaskContext {
            task_id,
            task_description: description,
            current_step: "initialization".to_string(),
            progress: 0.0,
            obstacles: Vec::new(),
            solutions_attempted: Vec::new(),
            time_spent: std::time::Duration::from_secs(0),
            expected_completion: None,
        });
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 719-726) ---[0m
    fn new() -> Self {
        Self {
            concepts: HashMap::new(),
            relationships: Vec::new(),
            knowledge_domains: HashMap::new(),
            fact_base: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:366-377 method set_task_context <-> ./crates/ccswarm/src/session/memory.rs:734-741 method new
  Similarity: 97.65%
  Classes: SessionMemory <-> ProceduralMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:set_task_context (lines 366-377) ---[0m
    pub fn set_task_context(&mut self, task_id: String, description: String) {
        self.working_memory.active_task_context = Some(TaskContext {
            task_id,
            task_description: description,
            current_step: "initialization".to_string(),
            progress: 0.0,
            obstacles: Vec::new(),
            solutions_attempted: Vec::new(),
            time_spent: std::time::Duration::from_secs(0),
            expected_completion: None,
        });
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 734-741) ---[0m
    fn new() -> Self {
        Self {
            procedures: HashMap::new(),
            skill_patterns: Vec::new(),
            automation_levels: HashMap::new(),
            execution_templates: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:403-421 method add_concept <-> ./crates/ccswarm/src/session/memory.rs:463-469 method should_consolidate_item
  Similarity: 74.38%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_concept (lines 403-421) ---[0m
    pub fn add_concept(
        &mut self,
        name: String,
        definition: String,
        properties: HashMap<String, String>,
    ) {
        let concept = Concept {
            name: name.clone(),
            definition,
            properties,
            examples: Vec::new(),
            confidence: 0.7,
            source_episodes: Vec::new(),
            last_updated: Utc::now(),
            usage_frequency: 1,
        };

        self.semantic_memory.add_concept(concept);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:should_consolidate_item (lines 463-469) ---[0m
    fn should_consolidate_item(&self, item: &WorkingMemoryItem) -> bool {
        let age = Utc::now() - item.created_at;
        let significance = item.priority;

        // Consolidate if item is old enough and significant
        age.num_minutes() > 30 && significance > 0.7
    }

  ./crates/ccswarm/src/session/memory.rs:403-421 method add_concept <-> ./crates/ccswarm/src/session/memory.rs:665-671 method add_item
  Similarity: 71.80%
  Classes: SessionMemory <-> WorkingMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_concept (lines 403-421) ---[0m
    pub fn add_concept(
        &mut self,
        name: String,
        definition: String,
        properties: HashMap<String, String>,
    ) {
        let concept = Concept {
            name: name.clone(),
            definition,
            properties,
            examples: Vec::new(),
            confidence: 0.7,
            source_episodes: Vec::new(),
            last_updated: Utc::now(),
            usage_frequency: 1,
        };

        self.semantic_memory.add_concept(concept);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_item (lines 665-671) ---[0m
    fn add_item(&mut self, item: WorkingMemoryItem) {
        // Remove oldest item if at capacity
        if self.current_items.len() >= self.capacity {
            self.current_items.pop_front();
        }
        self.current_items.push_back(item);
    }

  ./crates/ccswarm/src/session/memory.rs:424-437 method add_procedure <-> ./crates/ccswarm/src/session/memory.rs:629-636 method new
  Similarity: 80.89%
  Classes: SessionMemory <-> RetrievalResult

[36m--- ./crates/ccswarm/src/session/memory.rs:add_procedure (lines 424-437) ---[0m
    pub fn add_procedure(&mut self, name: String, steps: Vec<ProcedureStep>) {
        let procedure = Procedure {
            name: name.clone(),
            steps,
            prerequisites: Vec::new(),
            success_rate: 0.8,
            average_execution_time: std::time::Duration::from_secs(60),
            complexity_level: ComplexityLevel::Moderate,
            last_executed: None,
            optimization_history: Vec::new(),
        };

        self.procedural_memory.add_procedure(procedure);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 629-636) ---[0m
    fn new() -> Self {
        Self {
            working_memory_items: Vec::new(),
            relevant_episodes: Vec::new(),
            relevant_concepts: Vec::new(),
            relevant_procedures: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:424-437 method add_procedure <-> ./crates/ccswarm/src/session/memory.rs:719-726 method new
  Similarity: 80.89%
  Classes: SessionMemory <-> SemanticMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_procedure (lines 424-437) ---[0m
    pub fn add_procedure(&mut self, name: String, steps: Vec<ProcedureStep>) {
        let procedure = Procedure {
            name: name.clone(),
            steps,
            prerequisites: Vec::new(),
            success_rate: 0.8,
            average_execution_time: std::time::Duration::from_secs(60),
            complexity_level: ComplexityLevel::Moderate,
            last_executed: None,
            optimization_history: Vec::new(),
        };

        self.procedural_memory.add_procedure(procedure);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 719-726) ---[0m
    fn new() -> Self {
        Self {
            concepts: HashMap::new(),
            relationships: Vec::new(),
            knowledge_domains: HashMap::new(),
            fact_base: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:424-437 method add_procedure <-> ./crates/ccswarm/src/session/memory.rs:734-741 method new
  Similarity: 80.89%
  Classes: SessionMemory <-> ProceduralMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_procedure (lines 424-437) ---[0m
    pub fn add_procedure(&mut self, name: String, steps: Vec<ProcedureStep>) {
        let procedure = Procedure {
            name: name.clone(),
            steps,
            prerequisites: Vec::new(),
            success_rate: 0.8,
            average_execution_time: std::time::Duration::from_secs(60),
            complexity_level: ComplexityLevel::Moderate,
            last_executed: None,
            optimization_history: Vec::new(),
        };

        self.procedural_memory.add_procedure(procedure);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 734-741) ---[0m
    fn new() -> Self {
        Self {
            procedures: HashMap::new(),
            skill_patterns: Vec::new(),
            automation_levels: HashMap::new(),
            execution_templates: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:331-342 method new <-> ./crates/ccswarm/src/session/memory.rs:655-663 method new
  Similarity: 81.67%
  Classes: SessionMemory <-> WorkingMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 331-342) ---[0m
    pub fn new(session_id: String, agent_id: String) -> Self {
        Self {
            session_id,
            agent_id,
            working_memory: WorkingMemory::new(),
            episodic_memory: EpisodicMemory::new(),
            semantic_memory: SemanticMemory::new(),
            procedural_memory: ProceduralMemory::new(),
            memory_stats: MemoryStats::new(),
            last_consolidation: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 655-663) ---[0m
    fn new() -> Self {
        Self {
            current_items: VecDeque::with_capacity(WORKING_MEMORY_CAPACITY),
            capacity: WORKING_MEMORY_CAPACITY,
            active_task_context: None,
            attention_focus: Vec::new(),
            cognitive_load: 0.0,
        }
    }

  ./crates/ccswarm/src/session/memory.rs:331-342 method new <-> ./crates/ccswarm/src/session/memory.rs:629-636 method new
  Similarity: 82.86%
  Classes: SessionMemory <-> RetrievalResult

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 331-342) ---[0m
    pub fn new(session_id: String, agent_id: String) -> Self {
        Self {
            session_id,
            agent_id,
            working_memory: WorkingMemory::new(),
            episodic_memory: EpisodicMemory::new(),
            semantic_memory: SemanticMemory::new(),
            procedural_memory: ProceduralMemory::new(),
            memory_stats: MemoryStats::new(),
            last_consolidation: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 629-636) ---[0m
    fn new() -> Self {
        Self {
            working_memory_items: Vec::new(),
            relevant_episodes: Vec::new(),
            relevant_concepts: Vec::new(),
            relevant_procedures: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:331-342 method new <-> ./crates/ccswarm/src/session/memory.rs:719-726 method new
  Similarity: 82.86%
  Classes: SessionMemory <-> SemanticMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 331-342) ---[0m
    pub fn new(session_id: String, agent_id: String) -> Self {
        Self {
            session_id,
            agent_id,
            working_memory: WorkingMemory::new(),
            episodic_memory: EpisodicMemory::new(),
            semantic_memory: SemanticMemory::new(),
            procedural_memory: ProceduralMemory::new(),
            memory_stats: MemoryStats::new(),
            last_consolidation: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 719-726) ---[0m
    fn new() -> Self {
        Self {
            concepts: HashMap::new(),
            relationships: Vec::new(),
            knowledge_domains: HashMap::new(),
            fact_base: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:331-342 method new <-> ./crates/ccswarm/src/session/memory.rs:734-741 method new
  Similarity: 82.86%
  Classes: SessionMemory <-> ProceduralMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 331-342) ---[0m
    pub fn new(session_id: String, agent_id: String) -> Self {
        Self {
            session_id,
            agent_id,
            working_memory: WorkingMemory::new(),
            episodic_memory: EpisodicMemory::new(),
            semantic_memory: SemanticMemory::new(),
            procedural_memory: ProceduralMemory::new(),
            memory_stats: MemoryStats::new(),
            last_consolidation: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 734-741) ---[0m
    fn new() -> Self {
        Self {
            procedures: HashMap::new(),
            skill_patterns: Vec::new(),
            automation_levels: HashMap::new(),
            execution_templates: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:331-342 method new <-> ./crates/ccswarm/src/session/memory.rs:673-680 method cleanup_expired_items
  Similarity: 81.79%
  Classes: SessionMemory <-> WorkingMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 331-342) ---[0m
    pub fn new(session_id: String, agent_id: String) -> Self {
        Self {
            session_id,
            agent_id,
            working_memory: WorkingMemory::new(),
            episodic_memory: EpisodicMemory::new(),
            semantic_memory: SemanticMemory::new(),
            procedural_memory: ProceduralMemory::new(),
            memory_stats: MemoryStats::new(),
            last_consolidation: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:cleanup_expired_items (lines 673-680) ---[0m
    fn cleanup_expired_items(&mut self) {
        let now = Utc::now();
        self.current_items.retain(|item| {
            let age = now - item.created_at;
            let decay_threshold = item.decay_rate * age.num_minutes() as f32;
            decay_threshold < 1.0 // Keep if not fully decayed
        });
    }

  ./crates/ccswarm/src/session/memory.rs:366-377 method set_task_context <-> ./crates/ccswarm/src/session/memory.rs:673-680 method cleanup_expired_items
  Similarity: 81.43%
  Classes: SessionMemory <-> WorkingMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:set_task_context (lines 366-377) ---[0m
    pub fn set_task_context(&mut self, task_id: String, description: String) {
        self.working_memory.active_task_context = Some(TaskContext {
            task_id,
            task_description: description,
            current_step: "initialization".to_string(),
            progress: 0.0,
            obstacles: Vec::new(),
            solutions_attempted: Vec::new(),
            time_spent: std::time::Duration::from_secs(0),
            expected_completion: None,
        });
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:cleanup_expired_items (lines 673-680) ---[0m
    fn cleanup_expired_items(&mut self) {
        let now = Utc::now();
        self.current_items.retain(|item| {
            let age = now - item.created_at;
            let decay_threshold = item.decay_rate * age.num_minutes() as f32;
            decay_threshold < 1.0 // Keep if not fully decayed
        });
    }

  ./crates/ccswarm/src/session/memory.rs:331-342 method new <-> ./crates/ccswarm/src/session/memory.rs:684-691 method new
  Similarity: 80.48%
  Classes: SessionMemory <-> EpisodicMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 331-342) ---[0m
    pub fn new(session_id: String, agent_id: String) -> Self {
        Self {
            session_id,
            agent_id,
            working_memory: WorkingMemory::new(),
            episodic_memory: EpisodicMemory::new(),
            semantic_memory: SemanticMemory::new(),
            procedural_memory: ProceduralMemory::new(),
            memory_stats: MemoryStats::new(),
            last_consolidation: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 684-691) ---[0m
    fn new() -> Self {
        Self {
            episodes: VecDeque::with_capacity(1000),
            max_episodes: 1000,
            recent_experiences: Vec::new(),
            emotional_markers: HashMap::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:424-437 method add_procedure <-> ./crates/ccswarm/src/session/memory.rs:665-671 method add_item
  Similarity: 75.15%
  Classes: SessionMemory <-> WorkingMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_procedure (lines 424-437) ---[0m
    pub fn add_procedure(&mut self, name: String, steps: Vec<ProcedureStep>) {
        let procedure = Procedure {
            name: name.clone(),
            steps,
            prerequisites: Vec::new(),
            success_rate: 0.8,
            average_execution_time: std::time::Duration::from_secs(60),
            complexity_level: ComplexityLevel::Moderate,
            last_executed: None,
            optimization_history: Vec::new(),
        };

        self.procedural_memory.add_procedure(procedure);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_item (lines 665-671) ---[0m
    fn add_item(&mut self, item: WorkingMemoryItem) {
        // Remove oldest item if at capacity
        if self.current_items.len() >= self.capacity {
            self.current_items.pop_front();
        }
        self.current_items.push_back(item);
    }

  ./crates/ccswarm/src/session/memory.rs:655-663 method new <-> ./crates/ccswarm/src/session/memory.rs:749-759 method new
  Similarity: 78.33%
  Classes: WorkingMemory <-> MemoryStats

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 655-663) ---[0m
    fn new() -> Self {
        Self {
            current_items: VecDeque::with_capacity(WORKING_MEMORY_CAPACITY),
            capacity: WORKING_MEMORY_CAPACITY,
            active_task_context: None,
            attention_focus: Vec::new(),
            cognitive_load: 0.0,
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 749-759) ---[0m
    fn new() -> Self {
        Self {
            working_memory_utilization: 0.0,
            episodic_memory_size: 0,
            semantic_concepts_count: 0,
            procedural_skills_count: 0,
            consolidation_events: 0,
            memory_efficiency: 1.0,
            last_updated: Utc::now(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:719-726 method new <-> ./crates/ccswarm/src/session/memory.rs:734-741 method new
  Similarity: 97.78%
  Classes: SemanticMemory <-> ProceduralMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 719-726) ---[0m
    fn new() -> Self {
        Self {
            concepts: HashMap::new(),
            relationships: Vec::new(),
            knowledge_domains: HashMap::new(),
            fact_base: Vec::new(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 734-741) ---[0m
    fn new() -> Self {
        Self {
            procedures: HashMap::new(),
            skill_patterns: Vec::new(),
            automation_levels: HashMap::new(),
            execution_templates: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:629-636 method new <-> ./crates/ccswarm/src/session/memory.rs:719-726 method new
  Similarity: 96.67%
  Classes: RetrievalResult <-> SemanticMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 629-636) ---[0m
    fn new() -> Self {
        Self {
            working_memory_items: Vec::new(),
            relevant_episodes: Vec::new(),
            relevant_concepts: Vec::new(),
            relevant_procedures: Vec::new(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 719-726) ---[0m
    fn new() -> Self {
        Self {
            concepts: HashMap::new(),
            relationships: Vec::new(),
            knowledge_domains: HashMap::new(),
            fact_base: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:629-636 method new <-> ./crates/ccswarm/src/session/memory.rs:734-741 method new
  Similarity: 96.67%
  Classes: RetrievalResult <-> ProceduralMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 629-636) ---[0m
    fn new() -> Self {
        Self {
            working_memory_items: Vec::new(),
            relevant_episodes: Vec::new(),
            relevant_concepts: Vec::new(),
            relevant_procedures: Vec::new(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 734-741) ---[0m
    fn new() -> Self {
        Self {
            procedures: HashMap::new(),
            skill_patterns: Vec::new(),
            automation_levels: HashMap::new(),
            execution_templates: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:673-680 method cleanup_expired_items <-> ./crates/ccswarm/src/session/memory.rs:749-759 method new
  Similarity: 79.04%
  Classes: WorkingMemory <-> MemoryStats

[36m--- ./crates/ccswarm/src/session/memory.rs:cleanup_expired_items (lines 673-680) ---[0m
    fn cleanup_expired_items(&mut self) {
        let now = Utc::now();
        self.current_items.retain(|item| {
            let age = now - item.created_at;
            let decay_threshold = item.decay_rate * age.num_minutes() as f32;
            decay_threshold < 1.0 // Keep if not fully decayed
        });
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 749-759) ---[0m
    fn new() -> Self {
        Self {
            working_memory_utilization: 0.0,
            episodic_memory_size: 0,
            semantic_concepts_count: 0,
            procedural_skills_count: 0,
            consolidation_events: 0,
            memory_efficiency: 1.0,
            last_updated: Utc::now(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:655-663 method new <-> ./crates/ccswarm/src/session/memory.rs:684-691 method new
  Similarity: 87.92%
  Classes: WorkingMemory <-> EpisodicMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 655-663) ---[0m
    fn new() -> Self {
        Self {
            current_items: VecDeque::with_capacity(WORKING_MEMORY_CAPACITY),
            capacity: WORKING_MEMORY_CAPACITY,
            active_task_context: None,
            attention_focus: Vec::new(),
            cognitive_load: 0.0,
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 684-691) ---[0m
    fn new() -> Self {
        Self {
            episodes: VecDeque::with_capacity(1000),
            max_episodes: 1000,
            recent_experiences: Vec::new(),
            emotional_markers: HashMap::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:629-636 method new <-> ./crates/ccswarm/src/session/memory.rs:684-691 method new
  Similarity: 92.41%
  Classes: RetrievalResult <-> EpisodicMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 629-636) ---[0m
    fn new() -> Self {
        Self {
            working_memory_items: Vec::new(),
            relevant_episodes: Vec::new(),
            relevant_concepts: Vec::new(),
            relevant_procedures: Vec::new(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 684-691) ---[0m
    fn new() -> Self {
        Self {
            episodes: VecDeque::with_capacity(1000),
            max_episodes: 1000,
            recent_experiences: Vec::new(),
            emotional_markers: HashMap::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:629-636 method new <-> ./crates/ccswarm/src/session/memory.rs:655-663 method new
  Similarity: 86.67%
  Classes: RetrievalResult <-> WorkingMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 629-636) ---[0m
    fn new() -> Self {
        Self {
            working_memory_items: Vec::new(),
            relevant_episodes: Vec::new(),
            relevant_concepts: Vec::new(),
            relevant_procedures: Vec::new(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 655-663) ---[0m
    fn new() -> Self {
        Self {
            current_items: VecDeque::with_capacity(WORKING_MEMORY_CAPACITY),
            capacity: WORKING_MEMORY_CAPACITY,
            active_task_context: None,
            attention_focus: Vec::new(),
            cognitive_load: 0.0,
        }
    }

  ./crates/ccswarm/src/session/memory.rs:684-691 method new <-> ./crates/ccswarm/src/session/memory.rs:719-726 method new
  Similarity: 91.85%
  Classes: EpisodicMemory <-> SemanticMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 684-691) ---[0m
    fn new() -> Self {
        Self {
            episodes: VecDeque::with_capacity(1000),
            max_episodes: 1000,
            recent_experiences: Vec::new(),
            emotional_markers: HashMap::new(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 719-726) ---[0m
    fn new() -> Self {
        Self {
            concepts: HashMap::new(),
            relationships: Vec::new(),
            knowledge_domains: HashMap::new(),
            fact_base: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:684-691 method new <-> ./crates/ccswarm/src/session/memory.rs:734-741 method new
  Similarity: 91.85%
  Classes: EpisodicMemory <-> ProceduralMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 684-691) ---[0m
    fn new() -> Self {
        Self {
            episodes: VecDeque::with_capacity(1000),
            max_episodes: 1000,
            recent_experiences: Vec::new(),
            emotional_markers: HashMap::new(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 734-741) ---[0m
    fn new() -> Self {
        Self {
            procedures: HashMap::new(),
            skill_patterns: Vec::new(),
            automation_levels: HashMap::new(),
            execution_templates: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:655-663 method new <-> ./crates/ccswarm/src/session/memory.rs:719-726 method new
  Similarity: 86.11%
  Classes: WorkingMemory <-> SemanticMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 655-663) ---[0m
    fn new() -> Self {
        Self {
            current_items: VecDeque::with_capacity(WORKING_MEMORY_CAPACITY),
            capacity: WORKING_MEMORY_CAPACITY,
            active_task_context: None,
            attention_focus: Vec::new(),
            cognitive_load: 0.0,
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 719-726) ---[0m
    fn new() -> Self {
        Self {
            concepts: HashMap::new(),
            relationships: Vec::new(),
            knowledge_domains: HashMap::new(),
            fact_base: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:655-663 method new <-> ./crates/ccswarm/src/session/memory.rs:734-741 method new
  Similarity: 86.11%
  Classes: WorkingMemory <-> ProceduralMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 655-663) ---[0m
    fn new() -> Self {
        Self {
            current_items: VecDeque::with_capacity(WORKING_MEMORY_CAPACITY),
            capacity: WORKING_MEMORY_CAPACITY,
            active_task_context: None,
            attention_focus: Vec::new(),
            cognitive_load: 0.0,
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 734-741) ---[0m
    fn new() -> Self {
        Self {
            procedures: HashMap::new(),
            skill_patterns: Vec::new(),
            automation_levels: HashMap::new(),
            execution_templates: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:366-377 method set_task_context <-> ./crates/ccswarm/src/session/memory.rs:665-671 method add_item
  Similarity: 76.94%
  Classes: SessionMemory <-> WorkingMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:set_task_context (lines 366-377) ---[0m
    pub fn set_task_context(&mut self, task_id: String, description: String) {
        self.working_memory.active_task_context = Some(TaskContext {
            task_id,
            task_description: description,
            current_step: "initialization".to_string(),
            progress: 0.0,
            obstacles: Vec::new(),
            solutions_attempted: Vec::new(),
            time_spent: std::time::Duration::from_secs(0),
            expected_completion: None,
        });
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:add_item (lines 665-671) ---[0m
    fn add_item(&mut self, item: WorkingMemoryItem) {
        // Remove oldest item if at capacity
        if self.current_items.len() >= self.capacity {
            self.current_items.pop_front();
        }
        self.current_items.push_back(item);
    }

  ./crates/ccswarm/src/session/memory.rs:629-636 method new <-> ./crates/ccswarm/src/session/memory.rs:749-759 method new
  Similarity: 74.44%
  Classes: RetrievalResult <-> MemoryStats

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 629-636) ---[0m
    fn new() -> Self {
        Self {
            working_memory_items: Vec::new(),
            relevant_episodes: Vec::new(),
            relevant_concepts: Vec::new(),
            relevant_procedures: Vec::new(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 749-759) ---[0m
    fn new() -> Self {
        Self {
            working_memory_utilization: 0.0,
            episodic_memory_size: 0,
            semantic_concepts_count: 0,
            procedural_skills_count: 0,
            consolidation_events: 0,
            memory_efficiency: 1.0,
            last_updated: Utc::now(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:719-726 method new <-> ./crates/ccswarm/src/session/memory.rs:749-759 method new
  Similarity: 74.44%
  Classes: SemanticMemory <-> MemoryStats

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 719-726) ---[0m
    fn new() -> Self {
        Self {
            concepts: HashMap::new(),
            relationships: Vec::new(),
            knowledge_domains: HashMap::new(),
            fact_base: Vec::new(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 749-759) ---[0m
    fn new() -> Self {
        Self {
            working_memory_utilization: 0.0,
            episodic_memory_size: 0,
            semantic_concepts_count: 0,
            procedural_skills_count: 0,
            consolidation_events: 0,
            memory_efficiency: 1.0,
            last_updated: Utc::now(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:734-741 method new <-> ./crates/ccswarm/src/session/memory.rs:749-759 method new
  Similarity: 74.44%
  Classes: ProceduralMemory <-> MemoryStats

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 734-741) ---[0m
    fn new() -> Self {
        Self {
            procedures: HashMap::new(),
            skill_patterns: Vec::new(),
            automation_levels: HashMap::new(),
            execution_templates: Vec::new(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 749-759) ---[0m
    fn new() -> Self {
        Self {
            working_memory_utilization: 0.0,
            episodic_memory_size: 0,
            semantic_concepts_count: 0,
            procedural_skills_count: 0,
            consolidation_events: 0,
            memory_efficiency: 1.0,
            last_updated: Utc::now(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:684-691 method new <-> ./crates/ccswarm/src/session/memory.rs:749-759 method new
  Similarity: 72.71%
  Classes: EpisodicMemory <-> MemoryStats

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 684-691) ---[0m
    fn new() -> Self {
        Self {
            episodes: VecDeque::with_capacity(1000),
            max_episodes: 1000,
            recent_experiences: Vec::new(),
            emotional_markers: HashMap::new(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 749-759) ---[0m
    fn new() -> Self {
        Self {
            working_memory_utilization: 0.0,
            episodic_memory_size: 0,
            semantic_concepts_count: 0,
            procedural_skills_count: 0,
            consolidation_events: 0,
            memory_efficiency: 1.0,
            last_updated: Utc::now(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:655-663 method new <-> ./crates/ccswarm/src/session/memory.rs:673-680 method cleanup_expired_items
  Similarity: 79.04%
  Classes: WorkingMemory <-> WorkingMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 655-663) ---[0m
    fn new() -> Self {
        Self {
            current_items: VecDeque::with_capacity(WORKING_MEMORY_CAPACITY),
            capacity: WORKING_MEMORY_CAPACITY,
            active_task_context: None,
            attention_focus: Vec::new(),
            cognitive_load: 0.0,
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:cleanup_expired_items (lines 673-680) ---[0m
    fn cleanup_expired_items(&mut self) {
        let now = Utc::now();
        self.current_items.retain(|item| {
            let age = now - item.created_at;
            let decay_threshold = item.decay_rate * age.num_minutes() as f32;
            decay_threshold < 1.0 // Keep if not fully decayed
        });
    }

  ./crates/ccswarm/src/session/memory.rs:629-636 method new <-> ./crates/ccswarm/src/session/memory.rs:673-680 method cleanup_expired_items
  Similarity: 79.04%
  Classes: RetrievalResult <-> WorkingMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 629-636) ---[0m
    fn new() -> Self {
        Self {
            working_memory_items: Vec::new(),
            relevant_episodes: Vec::new(),
            relevant_concepts: Vec::new(),
            relevant_procedures: Vec::new(),
        }
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:cleanup_expired_items (lines 673-680) ---[0m
    fn cleanup_expired_items(&mut self) {
        let now = Utc::now();
        self.current_items.retain(|item| {
            let age = now - item.created_at;
            let decay_threshold = item.decay_rate * age.num_minutes() as f32;
            decay_threshold < 1.0 // Keep if not fully decayed
        });
    }

  ./crates/ccswarm/src/session/memory.rs:673-680 method cleanup_expired_items <-> ./crates/ccswarm/src/session/memory.rs:684-691 method new
  Similarity: 79.04%
  Classes: WorkingMemory <-> EpisodicMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:cleanup_expired_items (lines 673-680) ---[0m
    fn cleanup_expired_items(&mut self) {
        let now = Utc::now();
        self.current_items.retain(|item| {
            let age = now - item.created_at;
            let decay_threshold = item.decay_rate * age.num_minutes() as f32;
            decay_threshold < 1.0 // Keep if not fully decayed
        });
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 684-691) ---[0m
    fn new() -> Self {
        Self {
            episodes: VecDeque::with_capacity(1000),
            max_episodes: 1000,
            recent_experiences: Vec::new(),
            emotional_markers: HashMap::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:673-680 method cleanup_expired_items <-> ./crates/ccswarm/src/session/memory.rs:719-726 method new
  Similarity: 79.04%
  Classes: WorkingMemory <-> SemanticMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:cleanup_expired_items (lines 673-680) ---[0m
    fn cleanup_expired_items(&mut self) {
        let now = Utc::now();
        self.current_items.retain(|item| {
            let age = now - item.created_at;
            let decay_threshold = item.decay_rate * age.num_minutes() as f32;
            decay_threshold < 1.0 // Keep if not fully decayed
        });
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 719-726) ---[0m
    fn new() -> Self {
        Self {
            concepts: HashMap::new(),
            relationships: Vec::new(),
            knowledge_domains: HashMap::new(),
            fact_base: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:673-680 method cleanup_expired_items <-> ./crates/ccswarm/src/session/memory.rs:734-741 method new
  Similarity: 79.04%
  Classes: WorkingMemory <-> ProceduralMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:cleanup_expired_items (lines 673-680) ---[0m
    fn cleanup_expired_items(&mut self) {
        let now = Utc::now();
        self.current_items.retain(|item| {
            let age = now - item.created_at;
            let decay_threshold = item.decay_rate * age.num_minutes() as f32;
            decay_threshold < 1.0 // Keep if not fully decayed
        });
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:new (lines 734-741) ---[0m
    fn new() -> Self {
        Self {
            procedures: HashMap::new(),
            skill_patterns: Vec::new(),
            automation_levels: HashMap::new(),
            execution_templates: Vec::new(),
        }
    }

  ./crates/ccswarm/src/session/memory.rs:665-671 method add_item <-> ./crates/ccswarm/src/session/memory.rs:673-680 method cleanup_expired_items
  Similarity: 80.68%
  Classes: WorkingMemory <-> WorkingMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:add_item (lines 665-671) ---[0m
    fn add_item(&mut self, item: WorkingMemoryItem) {
        // Remove oldest item if at capacity
        if self.current_items.len() >= self.capacity {
            self.current_items.pop_front();
        }
        self.current_items.push_back(item);
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:cleanup_expired_items (lines 673-680) ---[0m
    fn cleanup_expired_items(&mut self) {
        let now = Utc::now();
        self.current_items.retain(|item| {
            let age = now - item.created_at;
            let decay_threshold = item.decay_rate * age.num_minutes() as f32;
            decay_threshold < 1.0 // Keep if not fully decayed
        });
    }

  ./crates/ccswarm/src/session/memory.rs:463-469 method should_consolidate_item <-> ./crates/ccswarm/src/session/memory.rs:533-541 method update_cognitive_load
  Similarity: 75.12%
  Classes: SessionMemory <-> SessionMemory

[36m--- ./crates/ccswarm/src/session/memory.rs:should_consolidate_item (lines 463-469) ---[0m
    fn should_consolidate_item(&self, item: &WorkingMemoryItem) -> bool {
        let age = Utc::now() - item.created_at;
        let significance = item.priority;

        // Consolidate if item is old enough and significant
        age.num_minutes() > 30 && significance > 0.7
    }

[36m--- ./crates/ccswarm/src/session/memory.rs:update_cognitive_load (lines 533-541) ---[0m
    fn update_cognitive_load(&mut self) {
        let utilization =
            self.working_memory.current_items.len() as f32 / WORKING_MEMORY_CAPACITY as f32;
        self.working_memory.cognitive_load = utilization.min(1.0);

        // Update memory stats
        self.memory_stats.working_memory_utilization = utilization;
        self.memory_stats.last_updated = Utc::now();
    }


Duplicates in ./crates/ccswarm/src/utils/async_error_boundary.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/utils/async_error_boundary.rs:43-58 function with_retry <-> ./crates/ccswarm/src/utils/async_error_boundary.rs:76-100 method call
  Similarity: 89.71%

[36m--- ./crates/ccswarm/src/utils/async_error_boundary.rs:with_retry (lines 43-58) ---[0m
pub async fn with_retry<F, T>(f: F, max_retries: u32) -> Result<T, CCSwarmError>
where
    F: Fn() -> Pin<Box<dyn Future<Output = Result<T, CCSwarmError>> + Send>>,
{
    for attempt in 0..max_retries {
        match f().await {
            Ok(result) => return Ok(result),
            Err(e) if attempt == max_retries - 1 => return Err(e),
            _ => continue,
        }
    }
    Err(CCSwarmError::Other {
        message: "Max retries exceeded".to_string(),
        source: None,
    })
}

[36m--- ./crates/ccswarm/src/utils/async_error_boundary.rs:call (lines 76-100) ---[0m
    pub async fn call<F, T>(&mut self, f: F) -> Result<T, CCSwarmError>
    where
        F: Future<Output = Result<T, CCSwarmError>>,
    {
        if self.is_open {
            return Err(CCSwarmError::Other {
                message: "Circuit breaker is open".to_string(),
                source: None,
            });
        }

        match f.await {
            Ok(result) => {
                self.current_failures = 0;
                Ok(result)
            }
            Err(e) => {
                self.current_failures += 1;
                if self.current_failures >= self.failure_threshold {
                    self.is_open = true;
                }
                Err(e)
            }
        }
    }


Duplicates in ./crates/ccswarm/src/extension_stub/meta_learning.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/extension_stub/meta_learning.rs:52-58 method new <-> ./crates/ccswarm/src/extension_stub/meta_learning.rs:70-75 method get_recommendations
  Similarity: 89.76%
  Classes: MetaLearningSystem <-> MetaLearningSystem

[36m--- ./crates/ccswarm/src/extension_stub/meta_learning.rs:new (lines 52-58) ---[0m
    pub fn new() -> Self {
        Self {
            learning_history: Vec::new(),
            patterns: HashMap::new(),
            insights: Vec::new(),
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/meta_learning.rs:get_recommendations (lines 70-75) ---[0m
    pub fn get_recommendations(&self) -> Vec<String> {
        self.patterns
            .values()
            .flat_map(|p| p.recommendations.clone())
            .collect()
    }


Duplicates in ./crates/ccswarm/src/agent/async_agent_example.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/agent/async_agent_example.rs:59-70 method execute_task_internal <-> ./crates/ccswarm/src/agent/async_agent_example.rs:74-91 function execute_multi_agent_task
  Similarity: 84.34%

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:execute_task_internal (lines 59-70) ---[0m
    async fn execute_task_internal(&self, task: &str) -> Result<String> {
        // Simulate task execution that might fail
        tracing::info!("Agent {} executing task: {}", self.name, task);
        
        // Simulate potential failure scenarios
        if task.contains("fail") {
            return Err(anyhow::anyhow!("Task execution failed"));
        }
        
        tokio::time::sleep(Duration::from_millis(100)).await;
        Ok(format!("Task '{}' completed by {}", task, self.name))
    }

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:execute_multi_agent_task (lines 74-91) ---[0m
pub async fn execute_multi_agent_task(agents: Vec<ResilientAgent>, task: &str) -> Result<Vec<Result<String>>> {
    let task = task.to_string();
    
    // Create concurrent boundary for all agent operations
    let mut boundary = ConcurrentBoundary::new("multi_agent_execution")
        .max_failures(2);  // Allow up to 2 failures
    
    // Add each agent's operation to the boundary
    for agent in agents {
        let task_clone = task.clone();
        boundary = boundary.add_operation(async move {
            agent.execute_task_safely(&task_clone).await.map(|_| ())
        });
    }
    
    // Execute all operations with boundary protection
    boundary.execute().await
}

  ./crates/ccswarm/src/agent/async_agent_example.rs:74-91 function execute_multi_agent_task <-> ./crates/ccswarm/src/agent/async_agent_example.rs:162-174 function test_circuit_breaker_protection
  Similarity: 79.38%

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:execute_multi_agent_task (lines 74-91) ---[0m
pub async fn execute_multi_agent_task(agents: Vec<ResilientAgent>, task: &str) -> Result<Vec<Result<String>>> {
    let task = task.to_string();
    
    // Create concurrent boundary for all agent operations
    let mut boundary = ConcurrentBoundary::new("multi_agent_execution")
        .max_failures(2);  // Allow up to 2 failures
    
    // Add each agent's operation to the boundary
    for agent in agents {
        let task_clone = task.clone();
        boundary = boundary.add_operation(async move {
            agent.execute_task_safely(&task_clone).await.map(|_| ())
        });
    }
    
    // Execute all operations with boundary protection
    boundary.execute().await
}

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:test_circuit_breaker_protection (lines 162-174) ---[0m
    async fn test_circuit_breaker_protection() {
        let agent = ResilientAgent::new("test_agent");
        
        // Cause multiple failures
        for _ in 0..3 {
            let _ = agent.execute_with_circuit_breaker("fail task").await;
        }
        
        // Circuit should be open now
        let result = agent.execute_with_circuit_breaker("good task").await;
        assert!(result.is_err());
        assert!(result.unwrap_err().to_string().contains("Circuit breaker"));
    }

  ./crates/ccswarm/src/agent/async_agent_example.rs:74-91 function execute_multi_agent_task <-> ./crates/ccswarm/src/agent/async_agent_example.rs:177-190 function test_multi_agent_boundary
  Similarity: 74.67%

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:execute_multi_agent_task (lines 74-91) ---[0m
pub async fn execute_multi_agent_task(agents: Vec<ResilientAgent>, task: &str) -> Result<Vec<Result<String>>> {
    let task = task.to_string();
    
    // Create concurrent boundary for all agent operations
    let mut boundary = ConcurrentBoundary::new("multi_agent_execution")
        .max_failures(2);  // Allow up to 2 failures
    
    // Add each agent's operation to the boundary
    for agent in agents {
        let task_clone = task.clone();
        boundary = boundary.add_operation(async move {
            agent.execute_task_safely(&task_clone).await.map(|_| ())
        });
    }
    
    // Execute all operations with boundary protection
    boundary.execute().await
}

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:test_multi_agent_boundary (lines 177-190) ---[0m
    async fn test_multi_agent_boundary() {
        let agents = vec![
            ResilientAgent::new("agent1"),
            ResilientAgent::new("agent2"),
            ResilientAgent::new("agent3"),
        ];
        
        let results = execute_multi_agent_task(agents, "test task").await;
        assert!(results.is_ok());
        
        let results = results.unwrap();
        assert_eq!(results.len(), 3);
        assert!(results.iter().all(|r| r.is_ok()));
    }

  ./crates/ccswarm/src/agent/async_agent_example.rs:59-70 method execute_task_internal <-> ./crates/ccswarm/src/agent/async_agent_example.rs:177-190 function test_multi_agent_boundary
  Similarity: 80.96%

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:execute_task_internal (lines 59-70) ---[0m
    async fn execute_task_internal(&self, task: &str) -> Result<String> {
        // Simulate task execution that might fail
        tracing::info!("Agent {} executing task: {}", self.name, task);
        
        // Simulate potential failure scenarios
        if task.contains("fail") {
            return Err(anyhow::anyhow!("Task execution failed"));
        }
        
        tokio::time::sleep(Duration::from_millis(100)).await;
        Ok(format!("Task '{}' completed by {}", task, self.name))
    }

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:test_multi_agent_boundary (lines 177-190) ---[0m
    async fn test_multi_agent_boundary() {
        let agents = vec![
            ResilientAgent::new("agent1"),
            ResilientAgent::new("agent2"),
            ResilientAgent::new("agent3"),
        ];
        
        let results = execute_multi_agent_task(agents, "test task").await;
        assert!(results.is_ok());
        
        let results = results.unwrap();
        assert_eq!(results.len(), 3);
        assert!(results.iter().all(|r| r.is_ok()));
    }

  ./crates/ccswarm/src/agent/async_agent_example.rs:59-70 method execute_task_internal <-> ./crates/ccswarm/src/agent/async_agent_example.rs:162-174 function test_circuit_breaker_protection
  Similarity: 82.93%

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:execute_task_internal (lines 59-70) ---[0m
    async fn execute_task_internal(&self, task: &str) -> Result<String> {
        // Simulate task execution that might fail
        tracing::info!("Agent {} executing task: {}", self.name, task);
        
        // Simulate potential failure scenarios
        if task.contains("fail") {
            return Err(anyhow::anyhow!("Task execution failed"));
        }
        
        tokio::time::sleep(Duration::from_millis(100)).await;
        Ok(format!("Task '{}' completed by {}", task, self.name))
    }

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:test_circuit_breaker_protection (lines 162-174) ---[0m
    async fn test_circuit_breaker_protection() {
        let agent = ResilientAgent::new("test_agent");
        
        // Cause multiple failures
        for _ in 0..3 {
            let _ = agent.execute_with_circuit_breaker("fail task").await;
        }
        
        // Circuit should be open now
        let result = agent.execute_with_circuit_breaker("good task").await;
        assert!(result.is_err());
        assert!(result.unwrap_err().to_string().contains("Circuit breaker"));
    }

  ./crates/ccswarm/src/agent/async_agent_example.rs:162-174 function test_circuit_breaker_protection <-> ./crates/ccswarm/src/agent/async_agent_example.rs:177-190 function test_multi_agent_boundary
  Similarity: 76.52%

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:test_circuit_breaker_protection (lines 162-174) ---[0m
    async fn test_circuit_breaker_protection() {
        let agent = ResilientAgent::new("test_agent");
        
        // Cause multiple failures
        for _ in 0..3 {
            let _ = agent.execute_with_circuit_breaker("fail task").await;
        }
        
        // Circuit should be open now
        let result = agent.execute_with_circuit_breaker("good task").await;
        assert!(result.is_err());
        assert!(result.unwrap_err().to_string().contains("Circuit breaker"));
    }

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:test_multi_agent_boundary (lines 177-190) ---[0m
    async fn test_multi_agent_boundary() {
        let agents = vec![
            ResilientAgent::new("agent1"),
            ResilientAgent::new("agent2"),
            ResilientAgent::new("agent3"),
        ];
        
        let results = execute_multi_agent_task(agents, "test task").await;
        assert!(results.is_ok());
        
        let results = results.unwrap();
        assert_eq!(results.len(), 3);
        assert!(results.iter().all(|r| r.is_ok()));
    }

  ./crates/ccswarm/src/agent/async_agent_example.rs:17-27 method new <-> ./crates/ccswarm/src/agent/async_agent_example.rs:30-39 method execute_task_safely
  Similarity: 93.54%
  Classes: ResilientAgent <-> ResilientAgent

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:new (lines 17-27) ---[0m
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            circuit_breaker: Arc::new(AsyncCircuitBreaker::new(
                "agent_operations",
                3,  // failure threshold
                2,  // success threshold  
                Duration::from_secs(30),  // timeout
            )),
        }
    }

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:execute_task_safely (lines 30-39) ---[0m
    pub async fn execute_task_safely(&self, task: &str) -> Result<String> {
        // Use error boundary with fallback
        boundary(self.execute_task_internal(task), format!("{}_task", self.name))
            .with_fallback(|| async move {
                tracing::warn!("Executing fallback for task");
                Ok(())
            })
            .execute()
            .await
    }

  ./crates/ccswarm/src/agent/async_agent_example.rs:74-91 function execute_multi_agent_task <-> ./crates/ccswarm/src/agent/async_agent_example.rs:147-152 function test_resilient_agent_success
  Similarity: 75.66%

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:execute_multi_agent_task (lines 74-91) ---[0m
pub async fn execute_multi_agent_task(agents: Vec<ResilientAgent>, task: &str) -> Result<Vec<Result<String>>> {
    let task = task.to_string();
    
    // Create concurrent boundary for all agent operations
    let mut boundary = ConcurrentBoundary::new("multi_agent_execution")
        .max_failures(2);  // Allow up to 2 failures
    
    // Add each agent's operation to the boundary
    for agent in agents {
        let task_clone = task.clone();
        boundary = boundary.add_operation(async move {
            agent.execute_task_safely(&task_clone).await.map(|_| ())
        });
    }
    
    // Execute all operations with boundary protection
    boundary.execute().await
}

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:test_resilient_agent_success (lines 147-152) ---[0m
    async fn test_resilient_agent_success() {
        let agent = ResilientAgent::new("test_agent");
        let result = agent.execute_task_safely("test task").await;
        assert!(result.is_ok());
        assert!(result.unwrap().contains("completed"));
    }

  ./crates/ccswarm/src/agent/async_agent_example.rs:17-27 method new <-> ./crates/ccswarm/src/agent/async_agent_example.rs:49-56 method execute_with_retry
  Similarity: 94.21%
  Classes: ResilientAgent <-> ResilientAgent

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:new (lines 17-27) ---[0m
    pub fn new(name: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            circuit_breaker: Arc::new(AsyncCircuitBreaker::new(
                "agent_operations",
                3,  // failure threshold
                2,  // success threshold  
                Duration::from_secs(30),  // timeout
            )),
        }
    }

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:execute_with_retry (lines 49-56) ---[0m
    pub async fn execute_with_retry(&self, task: &str) -> Result<String> {
        with_retry(
            || Box::pin(self.execute_task_internal(task)),
            3,  // max retries
            format!("{}_retry", self.name),
        )
        .await
    }

  ./crates/ccswarm/src/agent/async_agent_example.rs:30-39 method execute_task_safely <-> ./crates/ccswarm/src/agent/async_agent_example.rs:49-56 method execute_with_retry
  Similarity: 92.68%
  Classes: ResilientAgent <-> ResilientAgent

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:execute_task_safely (lines 30-39) ---[0m
    pub async fn execute_task_safely(&self, task: &str) -> Result<String> {
        // Use error boundary with fallback
        boundary(self.execute_task_internal(task), format!("{}_task", self.name))
            .with_fallback(|| async move {
                tracing::warn!("Executing fallback for task");
                Ok(())
            })
            .execute()
            .await
    }

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:execute_with_retry (lines 49-56) ---[0m
    pub async fn execute_with_retry(&self, task: &str) -> Result<String> {
        with_retry(
            || Box::pin(self.execute_task_internal(task)),
            3,  // max retries
            format!("{}_retry", self.name),
        )
        .await
    }

  ./crates/ccswarm/src/agent/async_agent_example.rs:59-70 method execute_task_internal <-> ./crates/ccswarm/src/agent/async_agent_example.rs:147-152 function test_resilient_agent_success
  Similarity: 80.54%

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:execute_task_internal (lines 59-70) ---[0m
    async fn execute_task_internal(&self, task: &str) -> Result<String> {
        // Simulate task execution that might fail
        tracing::info!("Agent {} executing task: {}", self.name, task);
        
        // Simulate potential failure scenarios
        if task.contains("fail") {
            return Err(anyhow::anyhow!("Task execution failed"));
        }
        
        tokio::time::sleep(Duration::from_millis(100)).await;
        Ok(format!("Task '{}' completed by {}", task, self.name))
    }

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:test_resilient_agent_success (lines 147-152) ---[0m
    async fn test_resilient_agent_success() {
        let agent = ResilientAgent::new("test_agent");
        let result = agent.execute_task_safely("test task").await;
        assert!(result.is_ok());
        assert!(result.unwrap().contains("completed"));
    }

  ./crates/ccswarm/src/agent/async_agent_example.rs:30-39 method execute_task_safely <-> ./crates/ccswarm/src/agent/async_agent_example.rs:155-159 function test_resilient_agent_fallback
  Similarity: 74.88%

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:execute_task_safely (lines 30-39) ---[0m
    pub async fn execute_task_safely(&self, task: &str) -> Result<String> {
        // Use error boundary with fallback
        boundary(self.execute_task_internal(task), format!("{}_task", self.name))
            .with_fallback(|| async move {
                tracing::warn!("Executing fallback for task");
                Ok(())
            })
            .execute()
            .await
    }

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:test_resilient_agent_fallback (lines 155-159) ---[0m
    async fn test_resilient_agent_fallback() {
        let agent = ResilientAgent::new("test_agent");
        let result = agent.execute_task_safely("fail task").await;
        assert!(result.is_err());
    }

  ./crates/ccswarm/src/agent/async_agent_example.rs:147-152 function test_resilient_agent_success <-> ./crates/ccswarm/src/agent/async_agent_example.rs:155-159 function test_resilient_agent_fallback
  Similarity: 75.20%

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:test_resilient_agent_success (lines 147-152) ---[0m
    async fn test_resilient_agent_success() {
        let agent = ResilientAgent::new("test_agent");
        let result = agent.execute_task_safely("test task").await;
        assert!(result.is_ok());
        assert!(result.unwrap().contains("completed"));
    }

[36m--- ./crates/ccswarm/src/agent/async_agent_example.rs:test_resilient_agent_fallback (lines 155-159) ---[0m
    async fn test_resilient_agent_fallback() {
        let agent = ResilientAgent::new("test_agent");
        let result = agent.execute_task_safely("fail task").await;
        assert!(result.is_err());
    }


Duplicates in ./crates/ccswarm/src/agent/mod.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/agent/mod.rs:753-936 method execute_task_with_monitoring <-> ./crates/ccswarm/src/agent/mod.rs:996-1221 method execute_task_with_orchestration
  Similarity: 87.32%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_task_with_monitoring (lines 753-936) ---[0m
    async fn execute_task_with_monitoring(&mut self, task: Task) -> Result<TaskResult> {
        let start_time = std::time::Instant::now();
        let mut monitor = IdentityMonitor::new(&self.identity.agent_id);
        let mut thinking_engine = InterleavedThinkingEngine::new().with_config(crate::agent::interleaved_thinking::ThinkingConfig {
            max_depth: 15,
            timeout_ms: 5000,
            parallel_thoughts: false,
        }); // Max 15 steps thinking configuration

        // Create whiteboard section for this task
        let task_section_id = self
            .whiteboard
            .create_section(&format!("Task: {}", task.description));

        // Record initial task analysis on whiteboard
        let initial_note = self.whiteboard.add_note(
            &format!(
                "タスク開始: {}. タイプ: {:?}, 優先度: {:?}",
                task.description, task.task_type, task.priority
            ),
            vec!["task_start".to_string()],
        );
        self.whiteboard
            .add_to_section(&task_section_id, &initial_note);

        // Initial thinking step - analyze the task
        let initial_observation = format!(
            "Starting task: {}. Type: {:?}, Priority: {:?}",
            task.description, task.task_type, task.priority
        );
        thinking_engine
            .process_observation(format!("{} ({})", initial_observation, self.identity.specialization.name()));

        // Prepare task prompt with identity header
        let mut prompt = claude::generate_task_prompt(&self.identity, &task);

        // Execute Claude Code with progressive refinement
        let mut final_output = String::new();
        let mut execution_count = 0;
        let max_executions = 3;

        // Start a thought trace on whiteboard
        let thought_trace_id = self.whiteboard.start_thought_trace();
        self.whiteboard
            .add_to_section(&task_section_id, &thought_trace_id);

        loop {
            execution_count += 1;
            let output = self.execute_claude_command(&prompt).await?;

            // Record execution attempt on whiteboard
            let exec_note = self.whiteboard.add_note(
                &format!(
                    "実行試行 #{}: 出力長 {} 文字",
                    execution_count,
                    output.len()
                ),
                vec!["execution".to_string()],
            );
            self.whiteboard.add_to_section(&task_section_id, &exec_note);

            // Process the output through thinking engine
            let observation = self.extract_execution_observation(&output);
            thinking_engine
                .process_observation(format!("{} ({})", observation, self.identity.specialization.name()));

            // Record thinking on whiteboard
            self.whiteboard
                .add_thought(&thought_trace_id, &format!("観察: {}", observation));

            // Monitor the response for identity
            let identity_status = monitor.monitor_response(&output).await?;
            self.handle_identity_status(identity_status, &mut monitor)
                .await?;

            // Handle thinking decision
            let thinking_step = ThinkingStep::new(
                observation.clone(),
                "Analysis".to_string(),
                DecisionType::Continue { reason: "Processing".to_string() }
            );

            match thinking_step.decision {
                DecisionType::Continue { reason } => {
                    tracing::debug!("Continuing execution: {}", reason);
                    self.whiteboard
                        .add_thought(&thought_trace_id, &format!("継続: {}", reason));
                    final_output = output;
                    if execution_count >= max_executions {
                        break;
                    }
                }
                DecisionType::Refine { refinement, reason } => {
                    tracing::info!("Refining approach: {} - {}", reason, refinement);
                    self.whiteboard.add_thought(
                        &thought_trace_id,
                        &format!("改善: {} - {}", reason, refinement),
                    );

                    // Record refinement as hypothesis
                    let hypothesis_id = self.whiteboard.add_hypothesis(&refinement, 0.7);
                    self.whiteboard
                        .add_to_section(&task_section_id, &hypothesis_id);

                    prompt = self.refine_prompt(&prompt, &refinement, &task);
                    final_output = output; // Keep last output
                }
                DecisionType::Complete { summary } => {
                    tracing::info!("Task completed: {}", summary);
                    self.whiteboard
                        .add_thought(&thought_trace_id, &format!("完了: {}", summary));
                    self.whiteboard.set_conclusion(&thought_trace_id, &summary);
                    final_output = output;
                    break;
                }
                DecisionType::Pivot {
                    new_direction,
                    reason,
                } => {
                    tracing::warn!("Pivoting approach: {} - {}", reason, new_direction);
                    self.whiteboard.add_thought(
                        &thought_trace_id,
                        &format!("方針転換: {} - {}", reason, new_direction),
                    );
                    self.whiteboard.annotate(
                        &thought_trace_id,
                        "大幅な方針変更",
                        AnnotationMarker::Important,
                    );
                    prompt = self.generate_pivot_prompt(&task, &new_direction);
                }
                DecisionType::RequestContext { questions } => {
                    tracing::info!("Additional context needed: {:?}", questions);
                    self.whiteboard
                        .add_thought(&thought_trace_id, &format!("追加情報必要: {:?}", questions));
                    // In a real implementation, this would request from orchestrator
                    // For now, we'll add questions to prompt and continue
                    prompt.push_str(&format!("\n\nPlease address: {}", questions.join(", ")));
                }
                DecisionType::Abort { reason } => {
                    self.whiteboard
                        .add_thought(&thought_trace_id, &format!("中断: {}", reason));
                    self.whiteboard.annotate(
                        &thought_trace_id,
                        "タスク中断",
                        AnnotationMarker::Important,
                    );
                    return Err(anyhow::anyhow!("Task aborted: {}", reason));
                }
            }

            if execution_count >= max_executions {
                break;
            }
        }

        // Generate thinking summary
        let thinking_summary = thinking_engine.get_thinking_summary();

        // Update agent's experience based on task completion
        self.update_agent_experience(&task);

        // Record success in phronesis system
        let _lesson = format!(
            "Task completed with thinking summary: {}",
            thinking_summary
        );
        self.phronesis.record_success(
            format!("Task {} completed successfully with interleaved thinking", task.id)
        );

        Ok(TaskResult {
            success: true,
            output: serde_json::json!({
                "response": final_output,
                "agent": self.identity.agent_id,
                "task_id": task.id,
                "thinking_summary": thinking_summary,
                "execution_iterations": execution_count,
            }),
            error: None,
            duration: start_time.elapsed(),
        })
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_task_with_orchestration (lines 996-1221) ---[0m
    async fn execute_task_with_orchestration(&mut self, task: Task) -> Result<TaskResult> {
        use self::orchestrator::{AgentOrchestrator, OrchestrationBuilder, ParallelTask, TaskPlan};
        use async_trait::async_trait;

        tracing::info!(
            "Agent {} orchestrating complex task: {}",
            self.identity.agent_id,
            task.description
        );

        // Create an orchestrator instance for this agent
        struct AgentTaskOrchestrator<'a> {
            agent: &'a mut ClaudeCodeAgent,
        }

        #[async_trait]
        impl<'a> AgentOrchestrator for AgentTaskOrchestrator<'a> {
            async fn analyze_task(&self, task: &Task) -> Result<TaskPlan> {
                let mut plan = TaskPlan::new(task.id.clone());

                // Add context from master orchestration
                if let Some(parent_task_id) = &task.parent_task_id {
                    plan.update_context("parent_task".to_string(), parent_task_id.clone());
                }
                plan.update_context(
                    "agent_role".to_string(),
                    self.agent.identity.specialization.name().to_string(),
                );
                plan.update_context("task_type".to_string(), format!("{:?}", task.task_type));

                // Create analysis step based on agent specialization
                let analysis_tasks = match &self.agent.identity.specialization {
                    crate::identity::AgentRole::Frontend { .. } => vec![
                        OrchestrationBuilder::parallel_task(
                            "analyze_ui_requirements",
                            "Analyze UI Requirements",
                            "Identify UI components and user interactions needed",
                            true,
                        ),
                        OrchestrationBuilder::parallel_task(
                            "check_design_system",
                            "Check Design System",
                            "Review existing design patterns and components",
                            false,
                        ),
                    ],
                    crate::identity::AgentRole::Backend { .. } => vec![
                        OrchestrationBuilder::parallel_task(
                            "analyze_api_requirements",
                            "Analyze API Requirements",
                            "Identify API endpoints and data models needed",
                            true,
                        ),
                        OrchestrationBuilder::parallel_task(
                            "check_dependencies",
                            "Check Dependencies",
                            "Review existing services and dependencies",
                            true,
                        ),
                    ],
                    crate::identity::AgentRole::DevOps { .. } => vec![
                        OrchestrationBuilder::parallel_task(
                            "analyze_infrastructure",
                            "Analyze Infrastructure",
                            "Identify infrastructure requirements",
                            true,
                        ),
                        OrchestrationBuilder::parallel_task(
                            "check_deployment",
                            "Check Deployment",
                            "Review deployment pipeline and configurations",
                            true,
                        ),
                    ],
                    crate::identity::AgentRole::QA { .. } => vec![
                        OrchestrationBuilder::parallel_task(
                            "analyze_test_requirements",
                            "Analyze Test Requirements",
                            "Identify test scenarios and coverage needs",
                            true,
                        ),
                        OrchestrationBuilder::parallel_task(
                            "check_test_framework",
                            "Check Test Framework",
                            "Review existing test infrastructure",
                            false,
                        ),
                    ],
                    crate::identity::AgentRole::Master { .. } => {
                        vec![OrchestrationBuilder::parallel_task(
                            "analyze_overall_impact",
                            "Analyze Overall Impact",
                            "Assess cross-team dependencies and coordination needs",
                            true,
                        )]
                    }
                    crate::identity::AgentRole::Search { .. } => vec![
                        OrchestrationBuilder::parallel_task(
                            "analyze_search_query",
                            "Analyze Search Query",
                            "Parse and optimize the search query",
                            true,
                        ),
                        OrchestrationBuilder::parallel_task(
                            "identify_search_sources",
                            "Identify Search Sources",
                            "Determine best sources for information",
                            false,
                        ),
                    ],
                };

                let analysis_step = OrchestrationBuilder::analysis_step(
                    "step1_analysis",
                    "Initial Analysis",
                    analysis_tasks,
                );
                plan.add_step(analysis_step);

                // Add execution step
                let mut execution_step = OrchestrationBuilder::execution_step(
                    "step2_execution",
                    "Main Implementation",
                    vec!["step1_analysis"],
                );
                execution_step.add_parallel_task(OrchestrationBuilder::parallel_task(
                    "implement_solution",
                    "Implement Solution",
                    &task.description,
                    true,
                ));
                plan.add_step(execution_step);

                // Add validation step
                let mut validation_step = OrchestrationBuilder::validation_step(
                    "step3_validation",
                    "Validate Implementation",
                )
                .depends_on("step2_execution".to_string());
                validation_step.add_parallel_task(OrchestrationBuilder::parallel_task(
                    "validate_solution",
                    "Validate Solution",
                    "Ensure implementation meets requirements",
                    true,
                ));
                plan.add_step(validation_step);

                Ok(plan)
            }

            async fn execute_parallel_task(
                &self,
                task: &ParallelTask,
                context: &std::collections::HashMap<String, String>,
            ) -> Result<crate::agent::orchestrator::task_plan::ParallelTaskResult> {
                // Execute the task using agent's capabilities
                let prompt = format!(
                    "## Parallel Task: {}\n{}\n\nContext:\n{:?}",
                    task.name, task.command, context
                );

                let response = self.agent.execute_claude_command(&prompt).await?;

                Ok(crate::agent::orchestrator::task_plan::ParallelTaskResult {
                    task_id: task.id.clone(),
                    success: !response.contains("error") && !response.contains("failed"),
                    output: response,
                    error: None,
                })
            }

            async fn synthesize_results(
                &self,
                task: &Task,
                results: Vec<crate::agent::orchestrator::task_plan::StepResult>,
            ) -> Result<TaskResult> {
                let all_success = results.iter().all(|r| r.is_success());
                let total_duration: u64 = results.iter().map(|r| r.duration_ms).sum();

                // Create comprehensive output
                let mut output_sections = vec![];

                for (i, result) in results.iter().enumerate() {
                    output_sections.push(format!(
                        "Step {}: {} - {}",
                        i + 1,
                        if result.is_success() { "✓" } else { "✗" },
                        result.summary
                    ));
                }

                let final_output = format!(
                    "Orchestrated task execution complete:\n\n{}\n\nTotal steps: {}\nSuccessful: {}\nTotal duration: {}ms",
                    output_sections.join("\n"),
                    results.len(),
                    results.iter().filter(|r| r.is_success()).count(),
                    total_duration
                );

                if all_success {
                    Ok(TaskResult::success(
                        serde_json::json!({
                            "task_id": task.id,
                            "agent": self.agent.identity.agent_id,
                            "orchestrated": true,
                            "steps_completed": results.len(),
                            "output": final_output,
                        }),
                        std::time::Duration::from_millis(total_duration),
                    ))
                } else {
                    let errors: Vec<String> =
                        results.iter().flat_map(|r| &r.errors).cloned().collect();

                    Ok(TaskResult::failure(
                        format!("Orchestration failed: {}", errors.join(", ")),
                        std::time::Duration::from_millis(total_duration),
                    ))
                }
            }
        }

        // Create orchestrator and execute
        let orchestrator = AgentTaskOrchestrator { agent: self };
        orchestrator.orchestrate_task(&task).await
    }

  ./crates/ccswarm/src/agent/mod.rs:662-750 method execute_task <-> ./crates/ccswarm/src/agent/mod.rs:939-993 method is_complex_task
  Similarity: 92.39%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_task (lines 662-750) ---[0m
    pub async fn execute_task(&mut self, task: Task) -> Result<TaskResult> {
        self.status = AgentStatus::Working;
        self.current_task = Some(task.clone());
        self.last_activity = Utc::now();

        // Evaluate task boundaries
        let checker = TaskBoundaryChecker::new(self.identity.specialization.clone());
        let evaluation = checker.evaluate_task(&task).await;

        let result = match evaluation {
            TaskEvaluation::Accept { reason } => {
                tracing::info!("Task accepted by {}: {}", self.identity.agent_id, reason);

                // Check if this is a complex task that needs orchestration
                if self.is_complex_task(&task) {
                    tracing::info!("Task identified as complex, using agent orchestrator");
                    self.execute_task_with_orchestration(task.clone()).await?
                } else {
                    self.execute_task_with_monitoring(task.clone()).await?
                }
            }
            TaskEvaluation::Delegate {
                target_agent,
                suggestion,
                ..
            } => {
                tracing::info!(
                    "Task delegated by {} to {}",
                    self.identity.agent_id,
                    target_agent
                );
                TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "action": "delegated",
                        "target": target_agent,
                        "suggestion": suggestion,
                    }),
                    error: None,
                    duration: std::time::Duration::from_secs(0),
                }
            }
            TaskEvaluation::Clarify { questions, .. } => {
                tracing::info!(
                    "Task requires clarification from {}",
                    self.identity.agent_id
                );
                TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "action": "clarification_needed",
                        "questions": questions,
                    }),
                    error: None,
                    duration: std::time::Duration::from_secs(0),
                }
            }
            TaskEvaluation::Reject { reason } => {
                tracing::warn!("Task rejected by {}: {}", self.identity.agent_id, reason);

                // Record rejection as a learning event
                self.phronesis.record_learning_event(
                    format!("Task rejected: {}", reason),
                    false
                );

                TaskResult {
                    success: false,
                    output: serde_json::json!({}),
                    error: Some(format!("Task rejected: {}", reason)),
                    duration: std::time::Duration::from_secs(0),
                }
            }
        };

        // Update status and history
        self.status = if result.success {
            AgentStatus::WaitingForReview
        } else {
            AgentStatus::Available
        };
        self.current_task = None;
        self.task_history.push((task, result.clone()));

        // Report status
        self.report_status(&result).await?;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:is_complex_task (lines 939-993) ---[0m
    fn is_complex_task(&self, task: &Task) -> bool {
        // A task is complex if it:
        // 1. Has multiple explicit steps in the description
        // 2. Contains keywords indicating multi-step work
        // 3. Has high priority and involves multiple components
        // 4. Is a feature or infrastructure task (typically multi-step)

        let description_lower = task.description.to_lowercase();
        let details_lower = task.details.as_deref().unwrap_or("").to_lowercase();
        let combined_text = format!("{} {}", description_lower, details_lower);

        // Check for multi-step indicators
        let multi_step_keywords = [
            "implement",
            "create",
            "build",
            "setup",
            "configure",
            "integrate",
            "migrate",
            "refactor",
            "design",
            "develop",
            "and then",
            "first",
            "next",
            "finally",
            "step",
        ];

        let has_multi_step_keywords = multi_step_keywords
            .iter()
            .any(|keyword| combined_text.contains(keyword));

        // Check for complexity indicators
        let complexity_indicators = combined_text.contains("multiple")
            || combined_text.contains("several")
            || combined_text.contains("comprehensive")
            || combined_text.contains("full")
            || combined_text.contains("complete");

        // Task type based complexity
        let complex_task_types = matches!(
            task.task_type,
            TaskType::Feature | TaskType::Infrastructure | TaskType::Development
        );

        // High priority tasks often need careful orchestration
        let high_priority = matches!(task.priority, Priority::High | Priority::Critical);

        // Decision logic
        (has_multi_step_keywords && complexity_indicators)
            || (complex_task_types && high_priority)
            || (complexity_indicators && complex_task_types)
    }

  ./crates/ccswarm/src/agent/mod.rs:406-453 method setup_worktree <-> ./crates/ccswarm/src/agent/mod.rs:662-750 method execute_task
  Similarity: 91.17%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:setup_worktree (lines 406-453) ---[0m
    async fn setup_worktree(&self) -> Result<()> {
        // Create parent directory if needed
        if let Some(parent) = self.worktree_path.parent() {
            tokio::fs::create_dir_all(parent)
                .await
                .context("Failed to create parent directory")?;
        }

        // Create branch
        let output = Command::new("git")
            .args(["checkout", "-b", &self.branch_name])
            .output()
            .await
            .context("Failed to create branch")?;

        if !output.status.success()
            && !String::from_utf8_lossy(&output.stderr).contains("already exists")
        {
            return Err(anyhow::anyhow!(
                "Failed to create branch: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        // Create worktree
        let output = Command::new("git")
            .args([
                "worktree",
                "add",
                self.worktree_path
                    .to_str()
                    .ok_or_else(|| anyhow::anyhow!("Invalid UTF-8 in worktree path"))?,
                &self.branch_name,
            ])
            .output()
            .await
            .context("Failed to create worktree")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to create worktree: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        tracing::info!("Git worktree created at: {}", self.worktree_path.display());
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_task (lines 662-750) ---[0m
    pub async fn execute_task(&mut self, task: Task) -> Result<TaskResult> {
        self.status = AgentStatus::Working;
        self.current_task = Some(task.clone());
        self.last_activity = Utc::now();

        // Evaluate task boundaries
        let checker = TaskBoundaryChecker::new(self.identity.specialization.clone());
        let evaluation = checker.evaluate_task(&task).await;

        let result = match evaluation {
            TaskEvaluation::Accept { reason } => {
                tracing::info!("Task accepted by {}: {}", self.identity.agent_id, reason);

                // Check if this is a complex task that needs orchestration
                if self.is_complex_task(&task) {
                    tracing::info!("Task identified as complex, using agent orchestrator");
                    self.execute_task_with_orchestration(task.clone()).await?
                } else {
                    self.execute_task_with_monitoring(task.clone()).await?
                }
            }
            TaskEvaluation::Delegate {
                target_agent,
                suggestion,
                ..
            } => {
                tracing::info!(
                    "Task delegated by {} to {}",
                    self.identity.agent_id,
                    target_agent
                );
                TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "action": "delegated",
                        "target": target_agent,
                        "suggestion": suggestion,
                    }),
                    error: None,
                    duration: std::time::Duration::from_secs(0),
                }
            }
            TaskEvaluation::Clarify { questions, .. } => {
                tracing::info!(
                    "Task requires clarification from {}",
                    self.identity.agent_id
                );
                TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "action": "clarification_needed",
                        "questions": questions,
                    }),
                    error: None,
                    duration: std::time::Duration::from_secs(0),
                }
            }
            TaskEvaluation::Reject { reason } => {
                tracing::warn!("Task rejected by {}: {}", self.identity.agent_id, reason);

                // Record rejection as a learning event
                self.phronesis.record_learning_event(
                    format!("Task rejected: {}", reason),
                    false
                );

                TaskResult {
                    success: false,
                    output: serde_json::json!({}),
                    error: Some(format!("Task rejected: {}", reason)),
                    duration: std::time::Duration::from_secs(0),
                }
            }
        };

        // Update status and history
        self.status = if result.success {
            AgentStatus::WaitingForReview
        } else {
            AgentStatus::Available
        };
        self.current_task = None;
        self.task_history.push((task, result.clone()));

        // Report status
        self.report_status(&result).await?;

        Ok(result)
    }

  ./crates/ccswarm/src/agent/mod.rs:662-750 method execute_task <-> ./crates/ccswarm/src/agent/mod.rs:1324-1370 method execute_claude_in_worktree
  Similarity: 90.63%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_task (lines 662-750) ---[0m
    pub async fn execute_task(&mut self, task: Task) -> Result<TaskResult> {
        self.status = AgentStatus::Working;
        self.current_task = Some(task.clone());
        self.last_activity = Utc::now();

        // Evaluate task boundaries
        let checker = TaskBoundaryChecker::new(self.identity.specialization.clone());
        let evaluation = checker.evaluate_task(&task).await;

        let result = match evaluation {
            TaskEvaluation::Accept { reason } => {
                tracing::info!("Task accepted by {}: {}", self.identity.agent_id, reason);

                // Check if this is a complex task that needs orchestration
                if self.is_complex_task(&task) {
                    tracing::info!("Task identified as complex, using agent orchestrator");
                    self.execute_task_with_orchestration(task.clone()).await?
                } else {
                    self.execute_task_with_monitoring(task.clone()).await?
                }
            }
            TaskEvaluation::Delegate {
                target_agent,
                suggestion,
                ..
            } => {
                tracing::info!(
                    "Task delegated by {} to {}",
                    self.identity.agent_id,
                    target_agent
                );
                TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "action": "delegated",
                        "target": target_agent,
                        "suggestion": suggestion,
                    }),
                    error: None,
                    duration: std::time::Duration::from_secs(0),
                }
            }
            TaskEvaluation::Clarify { questions, .. } => {
                tracing::info!(
                    "Task requires clarification from {}",
                    self.identity.agent_id
                );
                TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "action": "clarification_needed",
                        "questions": questions,
                    }),
                    error: None,
                    duration: std::time::Duration::from_secs(0),
                }
            }
            TaskEvaluation::Reject { reason } => {
                tracing::warn!("Task rejected by {}: {}", self.identity.agent_id, reason);

                // Record rejection as a learning event
                self.phronesis.record_learning_event(
                    format!("Task rejected: {}", reason),
                    false
                );

                TaskResult {
                    success: false,
                    output: serde_json::json!({}),
                    error: Some(format!("Task rejected: {}", reason)),
                    duration: std::time::Duration::from_secs(0),
                }
            }
        };

        // Update status and history
        self.status = if result.success {
            AgentStatus::WaitingForReview
        } else {
            AgentStatus::Available
        };
        self.current_task = None;
        self.task_history.push((task, result.clone()));

        // Report status
        self.report_status(&result).await?;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_in_worktree (lines 1324-1370) ---[0m
    async fn execute_claude_in_worktree(&self, prompt: &str) -> Result<String> {
        // Check if we should use real API instead of simulation
        if self.claude_config.use_real_api {
            return self.execute_claude_real_api(prompt).await;
        }

        let mut cmd = Command::new("claude");

        // Set working directory
        cmd.current_dir(&self.worktree_path);

        // Add environment variables
        for (key, value) in &self.identity.env_vars {
            cmd.env(key, value);
        }

        // Add Claude arguments
        cmd.arg("-p").arg(prompt);

        if self.claude_config.json_output {
            cmd.arg("--json");
        }

        if self.claude_config.dangerous_skip {
            cmd.arg("--dangerously-skip-permissions");
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.claude_config.think_mode {
            cmd.arg("--think").arg(think_mode.to_string());
        }

        // Execute command
        let output = cmd
            .output()
            .await
            .context("Failed to execute Claude Code")?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).to_string())
        } else {
            Err(anyhow::anyhow!(
                "Claude Code execution failed: {}",
                String::from_utf8_lossy(&output.stderr)
            ))
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:280-324 method new_with_isolation <-> ./crates/ccswarm/src/agent/mod.rs:662-750 method execute_task
  Similarity: 91.40%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:new_with_isolation (lines 280-324) ---[0m
    pub async fn new_with_isolation(
        role: AgentRole,
        workspace_root: &std::path::Path,
        branch_prefix: &str,
        claude_config: ClaudeConfig,
        isolation_mode: IsolationMode,
    ) -> Result<Self> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());
        let session_id = Uuid::new_v4().to_string();
        let worktree_path = workspace_root.join(format!("agents/{}", &agent_id));
        let branch_name = format!("{}/{}", branch_prefix, &agent_id);

        let identity = AgentIdentity {
            agent_id: agent_id.clone(),
            specialization: role.clone(),
            workspace_path: worktree_path.clone(),
            env_vars: Self::create_env_vars(&agent_id, &session_id),
            session_id,
            parent_process_id: std::process::id().to_string(),
            initialized_at: Utc::now(),
        };

        let personality = AgentPersonality::new(agent_id.clone());

        let whiteboard = Whiteboard::new(agent_id.clone());
        let phronesis = PhronesisManager::new();

        let agent = Self {
            identity,
            worktree_path,
            branch_name,
            claude_config,
            status: AgentStatus::Initializing,
            current_task: None,
            task_history: Vec::new(),
            last_activity: Utc::now(),
            isolation_mode,
            container_id: None,
            personality,
            whiteboard,
            phronesis,
        };

        Ok(agent)
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_task (lines 662-750) ---[0m
    pub async fn execute_task(&mut self, task: Task) -> Result<TaskResult> {
        self.status = AgentStatus::Working;
        self.current_task = Some(task.clone());
        self.last_activity = Utc::now();

        // Evaluate task boundaries
        let checker = TaskBoundaryChecker::new(self.identity.specialization.clone());
        let evaluation = checker.evaluate_task(&task).await;

        let result = match evaluation {
            TaskEvaluation::Accept { reason } => {
                tracing::info!("Task accepted by {}: {}", self.identity.agent_id, reason);

                // Check if this is a complex task that needs orchestration
                if self.is_complex_task(&task) {
                    tracing::info!("Task identified as complex, using agent orchestrator");
                    self.execute_task_with_orchestration(task.clone()).await?
                } else {
                    self.execute_task_with_monitoring(task.clone()).await?
                }
            }
            TaskEvaluation::Delegate {
                target_agent,
                suggestion,
                ..
            } => {
                tracing::info!(
                    "Task delegated by {} to {}",
                    self.identity.agent_id,
                    target_agent
                );
                TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "action": "delegated",
                        "target": target_agent,
                        "suggestion": suggestion,
                    }),
                    error: None,
                    duration: std::time::Duration::from_secs(0),
                }
            }
            TaskEvaluation::Clarify { questions, .. } => {
                tracing::info!(
                    "Task requires clarification from {}",
                    self.identity.agent_id
                );
                TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "action": "clarification_needed",
                        "questions": questions,
                    }),
                    error: None,
                    duration: std::time::Duration::from_secs(0),
                }
            }
            TaskEvaluation::Reject { reason } => {
                tracing::warn!("Task rejected by {}: {}", self.identity.agent_id, reason);

                // Record rejection as a learning event
                self.phronesis.record_learning_event(
                    format!("Task rejected: {}", reason),
                    false
                );

                TaskResult {
                    success: false,
                    output: serde_json::json!({}),
                    error: Some(format!("Task rejected: {}", reason)),
                    duration: std::time::Duration::from_secs(0),
                }
            }
        };

        // Update status and history
        self.status = if result.success {
            AgentStatus::WaitingForReview
        } else {
            AgentStatus::Available
        };
        self.current_task = None;
        self.task_history.push((task, result.clone()));

        // Report status
        self.report_status(&result).await?;

        Ok(result)
    }

  ./crates/ccswarm/src/agent/mod.rs:662-750 method execute_task <-> ./crates/ccswarm/src/agent/mod.rs:1437-1476 method report_status
  Similarity: 85.69%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_task (lines 662-750) ---[0m
    pub async fn execute_task(&mut self, task: Task) -> Result<TaskResult> {
        self.status = AgentStatus::Working;
        self.current_task = Some(task.clone());
        self.last_activity = Utc::now();

        // Evaluate task boundaries
        let checker = TaskBoundaryChecker::new(self.identity.specialization.clone());
        let evaluation = checker.evaluate_task(&task).await;

        let result = match evaluation {
            TaskEvaluation::Accept { reason } => {
                tracing::info!("Task accepted by {}: {}", self.identity.agent_id, reason);

                // Check if this is a complex task that needs orchestration
                if self.is_complex_task(&task) {
                    tracing::info!("Task identified as complex, using agent orchestrator");
                    self.execute_task_with_orchestration(task.clone()).await?
                } else {
                    self.execute_task_with_monitoring(task.clone()).await?
                }
            }
            TaskEvaluation::Delegate {
                target_agent,
                suggestion,
                ..
            } => {
                tracing::info!(
                    "Task delegated by {} to {}",
                    self.identity.agent_id,
                    target_agent
                );
                TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "action": "delegated",
                        "target": target_agent,
                        "suggestion": suggestion,
                    }),
                    error: None,
                    duration: std::time::Duration::from_secs(0),
                }
            }
            TaskEvaluation::Clarify { questions, .. } => {
                tracing::info!(
                    "Task requires clarification from {}",
                    self.identity.agent_id
                );
                TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "action": "clarification_needed",
                        "questions": questions,
                    }),
                    error: None,
                    duration: std::time::Duration::from_secs(0),
                }
            }
            TaskEvaluation::Reject { reason } => {
                tracing::warn!("Task rejected by {}: {}", self.identity.agent_id, reason);

                // Record rejection as a learning event
                self.phronesis.record_learning_event(
                    format!("Task rejected: {}", reason),
                    false
                );

                TaskResult {
                    success: false,
                    output: serde_json::json!({}),
                    error: Some(format!("Task rejected: {}", reason)),
                    duration: std::time::Duration::from_secs(0),
                }
            }
        };

        // Update status and history
        self.status = if result.success {
            AgentStatus::WaitingForReview
        } else {
            AgentStatus::Available
        };
        self.current_task = None;
        self.task_history.push((task, result.clone()));

        // Report status
        self.report_status(&result).await?;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:report_status (lines 1437-1476) ---[0m
    async fn report_status(&self, result: &TaskResult) -> Result<()> {
        let status_report = serde_json::json!({
            "agent_id": self.identity.agent_id,
            "specialization": self.identity.specialization.name(),
            "status": self.status,
            "current_task": self.current_task,
            "last_result": result,
            "timestamp": Utc::now(),
            "worktree": self.worktree_path.to_string_lossy(),
            "branch": self.branch_name,
            "personality": {
                "description": self.personality.describe_personality(),
                "working_style": self.personality.working_style,
                "traits": self.personality.traits,
                "composability_score": self.personality.composability_score(),
                "skills": self.personality.skills.iter().map(|(name, skill)| {
                    serde_json::json!({
                        "name": name,
                        "category": "general",
                        "level": skill.level,
                        "experience": skill.experience_points,
                    })
                }).collect::<Vec<_>>(),
            },
            "whiteboard_summary": self.whiteboard.summarize(),
            "phronesis_summary": self.phronesis.summarize(),
        });

        // Write to coordination directory
        let status_file = PathBuf::from("coordination/agent-status")
            .join(format!("{}.json", self.identity.agent_id));

        if let Some(parent) = status_file.parent() {
            tokio::fs::create_dir_all(parent).await?;
        }

        tokio::fs::write(&status_file, serde_json::to_string_pretty(&status_report)?).await?;

        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:406-453 method setup_worktree <-> ./crates/ccswarm/src/agent/mod.rs:939-993 method is_complex_task
  Similarity: 82.26%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:setup_worktree (lines 406-453) ---[0m
    async fn setup_worktree(&self) -> Result<()> {
        // Create parent directory if needed
        if let Some(parent) = self.worktree_path.parent() {
            tokio::fs::create_dir_all(parent)
                .await
                .context("Failed to create parent directory")?;
        }

        // Create branch
        let output = Command::new("git")
            .args(["checkout", "-b", &self.branch_name])
            .output()
            .await
            .context("Failed to create branch")?;

        if !output.status.success()
            && !String::from_utf8_lossy(&output.stderr).contains("already exists")
        {
            return Err(anyhow::anyhow!(
                "Failed to create branch: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        // Create worktree
        let output = Command::new("git")
            .args([
                "worktree",
                "add",
                self.worktree_path
                    .to_str()
                    .ok_or_else(|| anyhow::anyhow!("Invalid UTF-8 in worktree path"))?,
                &self.branch_name,
            ])
            .output()
            .await
            .context("Failed to create worktree")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to create worktree: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        tracing::info!("Git worktree created at: {}", self.worktree_path.display());
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:is_complex_task (lines 939-993) ---[0m
    fn is_complex_task(&self, task: &Task) -> bool {
        // A task is complex if it:
        // 1. Has multiple explicit steps in the description
        // 2. Contains keywords indicating multi-step work
        // 3. Has high priority and involves multiple components
        // 4. Is a feature or infrastructure task (typically multi-step)

        let description_lower = task.description.to_lowercase();
        let details_lower = task.details.as_deref().unwrap_or("").to_lowercase();
        let combined_text = format!("{} {}", description_lower, details_lower);

        // Check for multi-step indicators
        let multi_step_keywords = [
            "implement",
            "create",
            "build",
            "setup",
            "configure",
            "integrate",
            "migrate",
            "refactor",
            "design",
            "develop",
            "and then",
            "first",
            "next",
            "finally",
            "step",
        ];

        let has_multi_step_keywords = multi_step_keywords
            .iter()
            .any(|keyword| combined_text.contains(keyword));

        // Check for complexity indicators
        let complexity_indicators = combined_text.contains("multiple")
            || combined_text.contains("several")
            || combined_text.contains("comprehensive")
            || combined_text.contains("full")
            || combined_text.contains("complete");

        // Task type based complexity
        let complex_task_types = matches!(
            task.task_type,
            TaskType::Feature | TaskType::Infrastructure | TaskType::Development
        );

        // High priority tasks often need careful orchestration
        let high_priority = matches!(task.priority, Priority::High | Priority::Critical);

        // Decision logic
        (has_multi_step_keywords && complexity_indicators)
            || (complex_task_types && high_priority)
            || (complexity_indicators && complex_task_types)
    }

  ./crates/ccswarm/src/agent/mod.rs:280-324 method new_with_isolation <-> ./crates/ccswarm/src/agent/mod.rs:939-993 method is_complex_task
  Similarity: 84.43%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:new_with_isolation (lines 280-324) ---[0m
    pub async fn new_with_isolation(
        role: AgentRole,
        workspace_root: &std::path::Path,
        branch_prefix: &str,
        claude_config: ClaudeConfig,
        isolation_mode: IsolationMode,
    ) -> Result<Self> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());
        let session_id = Uuid::new_v4().to_string();
        let worktree_path = workspace_root.join(format!("agents/{}", &agent_id));
        let branch_name = format!("{}/{}", branch_prefix, &agent_id);

        let identity = AgentIdentity {
            agent_id: agent_id.clone(),
            specialization: role.clone(),
            workspace_path: worktree_path.clone(),
            env_vars: Self::create_env_vars(&agent_id, &session_id),
            session_id,
            parent_process_id: std::process::id().to_string(),
            initialized_at: Utc::now(),
        };

        let personality = AgentPersonality::new(agent_id.clone());

        let whiteboard = Whiteboard::new(agent_id.clone());
        let phronesis = PhronesisManager::new();

        let agent = Self {
            identity,
            worktree_path,
            branch_name,
            claude_config,
            status: AgentStatus::Initializing,
            current_task: None,
            task_history: Vec::new(),
            last_activity: Utc::now(),
            isolation_mode,
            container_id: None,
            personality,
            whiteboard,
            phronesis,
        };

        Ok(agent)
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:is_complex_task (lines 939-993) ---[0m
    fn is_complex_task(&self, task: &Task) -> bool {
        // A task is complex if it:
        // 1. Has multiple explicit steps in the description
        // 2. Contains keywords indicating multi-step work
        // 3. Has high priority and involves multiple components
        // 4. Is a feature or infrastructure task (typically multi-step)

        let description_lower = task.description.to_lowercase();
        let details_lower = task.details.as_deref().unwrap_or("").to_lowercase();
        let combined_text = format!("{} {}", description_lower, details_lower);

        // Check for multi-step indicators
        let multi_step_keywords = [
            "implement",
            "create",
            "build",
            "setup",
            "configure",
            "integrate",
            "migrate",
            "refactor",
            "design",
            "develop",
            "and then",
            "first",
            "next",
            "finally",
            "step",
        ];

        let has_multi_step_keywords = multi_step_keywords
            .iter()
            .any(|keyword| combined_text.contains(keyword));

        // Check for complexity indicators
        let complexity_indicators = combined_text.contains("multiple")
            || combined_text.contains("several")
            || combined_text.contains("comprehensive")
            || combined_text.contains("full")
            || combined_text.contains("complete");

        // Task type based complexity
        let complex_task_types = matches!(
            task.task_type,
            TaskType::Feature | TaskType::Infrastructure | TaskType::Development
        );

        // High priority tasks often need careful orchestration
        let high_priority = matches!(task.priority, Priority::High | Priority::Critical);

        // Decision logic
        (has_multi_step_keywords && complexity_indicators)
            || (complex_task_types && high_priority)
            || (complexity_indicators && complex_task_types)
    }

  ./crates/ccswarm/src/agent/mod.rs:939-993 method is_complex_task <-> ./crates/ccswarm/src/agent/mod.rs:1324-1370 method execute_claude_in_worktree
  Similarity: 81.42%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:is_complex_task (lines 939-993) ---[0m
    fn is_complex_task(&self, task: &Task) -> bool {
        // A task is complex if it:
        // 1. Has multiple explicit steps in the description
        // 2. Contains keywords indicating multi-step work
        // 3. Has high priority and involves multiple components
        // 4. Is a feature or infrastructure task (typically multi-step)

        let description_lower = task.description.to_lowercase();
        let details_lower = task.details.as_deref().unwrap_or("").to_lowercase();
        let combined_text = format!("{} {}", description_lower, details_lower);

        // Check for multi-step indicators
        let multi_step_keywords = [
            "implement",
            "create",
            "build",
            "setup",
            "configure",
            "integrate",
            "migrate",
            "refactor",
            "design",
            "develop",
            "and then",
            "first",
            "next",
            "finally",
            "step",
        ];

        let has_multi_step_keywords = multi_step_keywords
            .iter()
            .any(|keyword| combined_text.contains(keyword));

        // Check for complexity indicators
        let complexity_indicators = combined_text.contains("multiple")
            || combined_text.contains("several")
            || combined_text.contains("comprehensive")
            || combined_text.contains("full")
            || combined_text.contains("complete");

        // Task type based complexity
        let complex_task_types = matches!(
            task.task_type,
            TaskType::Feature | TaskType::Infrastructure | TaskType::Development
        );

        // High priority tasks often need careful orchestration
        let high_priority = matches!(task.priority, Priority::High | Priority::Critical);

        // Decision logic
        (has_multi_step_keywords && complexity_indicators)
            || (complex_task_types && high_priority)
            || (complexity_indicators && complex_task_types)
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_in_worktree (lines 1324-1370) ---[0m
    async fn execute_claude_in_worktree(&self, prompt: &str) -> Result<String> {
        // Check if we should use real API instead of simulation
        if self.claude_config.use_real_api {
            return self.execute_claude_real_api(prompt).await;
        }

        let mut cmd = Command::new("claude");

        // Set working directory
        cmd.current_dir(&self.worktree_path);

        // Add environment variables
        for (key, value) in &self.identity.env_vars {
            cmd.env(key, value);
        }

        // Add Claude arguments
        cmd.arg("-p").arg(prompt);

        if self.claude_config.json_output {
            cmd.arg("--json");
        }

        if self.claude_config.dangerous_skip {
            cmd.arg("--dangerously-skip-permissions");
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.claude_config.think_mode {
            cmd.arg("--think").arg(think_mode.to_string());
        }

        // Execute command
        let output = cmd
            .output()
            .await
            .context("Failed to execute Claude Code")?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).to_string())
        } else {
            Err(anyhow::anyhow!(
                "Claude Code execution failed: {}",
                String::from_utf8_lossy(&output.stderr)
            ))
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:406-453 method setup_worktree <-> ./crates/ccswarm/src/agent/mod.rs:1529-1568 method update_agent_experience
  Similarity: 91.96%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:setup_worktree (lines 406-453) ---[0m
    async fn setup_worktree(&self) -> Result<()> {
        // Create parent directory if needed
        if let Some(parent) = self.worktree_path.parent() {
            tokio::fs::create_dir_all(parent)
                .await
                .context("Failed to create parent directory")?;
        }

        // Create branch
        let output = Command::new("git")
            .args(["checkout", "-b", &self.branch_name])
            .output()
            .await
            .context("Failed to create branch")?;

        if !output.status.success()
            && !String::from_utf8_lossy(&output.stderr).contains("already exists")
        {
            return Err(anyhow::anyhow!(
                "Failed to create branch: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        // Create worktree
        let output = Command::new("git")
            .args([
                "worktree",
                "add",
                self.worktree_path
                    .to_str()
                    .ok_or_else(|| anyhow::anyhow!("Invalid UTF-8 in worktree path"))?,
                &self.branch_name,
            ])
            .output()
            .await
            .context("Failed to create worktree")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to create worktree: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        tracing::info!("Git worktree created at: {}", self.worktree_path.display());
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:update_agent_experience (lines 1529-1568) ---[0m
    pub fn update_agent_experience(&mut self, task: &Task) {
        // タスクタイプに基づいて経験値を付与
        let experience_points = match task.priority {
            Priority::Critical => 100,
            Priority::High => 50,
            Priority::Medium => 30,
            Priority::Low => 10,
        };

        // タスクタイプに関連するスキルを特定して経験値を追加
        // Simplified: give experience to all skills, with some variation by task type
        let experience_multiplier = match task.task_type {
            TaskType::Development | TaskType::Feature | TaskType::Infrastructure => 1.0,
            TaskType::Testing | TaskType::Review => 0.8,
            TaskType::Documentation => 0.6,
            TaskType::Coordination => 0.7,
            TaskType::Bugfix | TaskType::Bug => 1.2,
            TaskType::Remediation => 1.5,
            TaskType::Assistance => 0.9,
            TaskType::Research => 0.7,
        };

        let adjusted_experience = (experience_points as f32 * experience_multiplier) as u32;

        for skill in self.personality.skills.values_mut() {
            skill.add_experience(adjusted_experience as f32);
            tracing::debug!(
                "Added {} experience to skill: {}",
                adjusted_experience,
                skill.name
            );
        }

        // 個性の説明を更新してログ出力
        tracing::info!(
            "Agent {} personality update: {}",
            self.identity.agent_id,
            self.personality.describe_personality()
        );
    }

  ./crates/ccswarm/src/agent/mod.rs:406-453 method setup_worktree <-> ./crates/ccswarm/src/agent/mod.rs:1373-1412 method execute_claude_real_api
  Similarity: 90.55%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:setup_worktree (lines 406-453) ---[0m
    async fn setup_worktree(&self) -> Result<()> {
        // Create parent directory if needed
        if let Some(parent) = self.worktree_path.parent() {
            tokio::fs::create_dir_all(parent)
                .await
                .context("Failed to create parent directory")?;
        }

        // Create branch
        let output = Command::new("git")
            .args(["checkout", "-b", &self.branch_name])
            .output()
            .await
            .context("Failed to create branch")?;

        if !output.status.success()
            && !String::from_utf8_lossy(&output.stderr).contains("already exists")
        {
            return Err(anyhow::anyhow!(
                "Failed to create branch: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        // Create worktree
        let output = Command::new("git")
            .args([
                "worktree",
                "add",
                self.worktree_path
                    .to_str()
                    .ok_or_else(|| anyhow::anyhow!("Invalid UTF-8 in worktree path"))?,
                &self.branch_name,
            ])
            .output()
            .await
            .context("Failed to create worktree")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to create worktree: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        tracing::info!("Git worktree created at: {}", self.worktree_path.display());
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_real_api (lines 1373-1412) ---[0m
    async fn execute_claude_real_api(&self, prompt: &str) -> Result<String> {
        use crate::providers::claude_api::ClaudeApiClient;

        tracing::info!(
            "Using real Claude API for agent: {}",
            self.identity.agent_id
        );

        // Create API client
        let api_client = ClaudeApiClient::new(None)?;

        // Format the full prompt with agent identity
        let full_prompt = format!(
            "{}\n\n{}",
            claude::generate_identity_header(&self.identity),
            prompt
        );

        // Make API call
        let response = api_client
            .simple_completion(
                &self.claude_config.model,
                &full_prompt,
                4096, // Max tokens
            )
            .await?;

        // Format response to match expected output format
        if self.claude_config.json_output {
            // Wrap response in JSON format similar to CLI output
            let json_response = serde_json::json!({
                "response": response,
                "agent": self.identity.agent_id,
                "timestamp": chrono::Utc::now().to_rfc3339(),
            });
            Ok(serde_json::to_string(&json_response)?)
        } else {
            Ok(response)
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:939-993 method is_complex_task <-> ./crates/ccswarm/src/agent/mod.rs:1373-1412 method execute_claude_real_api
  Similarity: 82.59%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:is_complex_task (lines 939-993) ---[0m
    fn is_complex_task(&self, task: &Task) -> bool {
        // A task is complex if it:
        // 1. Has multiple explicit steps in the description
        // 2. Contains keywords indicating multi-step work
        // 3. Has high priority and involves multiple components
        // 4. Is a feature or infrastructure task (typically multi-step)

        let description_lower = task.description.to_lowercase();
        let details_lower = task.details.as_deref().unwrap_or("").to_lowercase();
        let combined_text = format!("{} {}", description_lower, details_lower);

        // Check for multi-step indicators
        let multi_step_keywords = [
            "implement",
            "create",
            "build",
            "setup",
            "configure",
            "integrate",
            "migrate",
            "refactor",
            "design",
            "develop",
            "and then",
            "first",
            "next",
            "finally",
            "step",
        ];

        let has_multi_step_keywords = multi_step_keywords
            .iter()
            .any(|keyword| combined_text.contains(keyword));

        // Check for complexity indicators
        let complexity_indicators = combined_text.contains("multiple")
            || combined_text.contains("several")
            || combined_text.contains("comprehensive")
            || combined_text.contains("full")
            || combined_text.contains("complete");

        // Task type based complexity
        let complex_task_types = matches!(
            task.task_type,
            TaskType::Feature | TaskType::Infrastructure | TaskType::Development
        );

        // High priority tasks often need careful orchestration
        let high_priority = matches!(task.priority, Priority::High | Priority::Critical);

        // Decision logic
        (has_multi_step_keywords && complexity_indicators)
            || (complex_task_types && high_priority)
            || (complexity_indicators && complex_task_types)
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_real_api (lines 1373-1412) ---[0m
    async fn execute_claude_real_api(&self, prompt: &str) -> Result<String> {
        use crate::providers::claude_api::ClaudeApiClient;

        tracing::info!(
            "Using real Claude API for agent: {}",
            self.identity.agent_id
        );

        // Create API client
        let api_client = ClaudeApiClient::new(None)?;

        // Format the full prompt with agent identity
        let full_prompt = format!(
            "{}\n\n{}",
            claude::generate_identity_header(&self.identity),
            prompt
        );

        // Make API call
        let response = api_client
            .simple_completion(
                &self.claude_config.model,
                &full_prompt,
                4096, // Max tokens
            )
            .await?;

        // Format response to match expected output format
        if self.claude_config.json_output {
            // Wrap response in JSON format similar to CLI output
            let json_response = serde_json::json!({
                "response": response,
                "agent": self.identity.agent_id,
                "timestamp": chrono::Utc::now().to_rfc3339(),
            });
            Ok(serde_json::to_string(&json_response)?)
        } else {
            Ok(response)
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:939-993 method is_complex_task <-> ./crates/ccswarm/src/agent/mod.rs:1529-1568 method update_agent_experience
  Similarity: 81.08%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:is_complex_task (lines 939-993) ---[0m
    fn is_complex_task(&self, task: &Task) -> bool {
        // A task is complex if it:
        // 1. Has multiple explicit steps in the description
        // 2. Contains keywords indicating multi-step work
        // 3. Has high priority and involves multiple components
        // 4. Is a feature or infrastructure task (typically multi-step)

        let description_lower = task.description.to_lowercase();
        let details_lower = task.details.as_deref().unwrap_or("").to_lowercase();
        let combined_text = format!("{} {}", description_lower, details_lower);

        // Check for multi-step indicators
        let multi_step_keywords = [
            "implement",
            "create",
            "build",
            "setup",
            "configure",
            "integrate",
            "migrate",
            "refactor",
            "design",
            "develop",
            "and then",
            "first",
            "next",
            "finally",
            "step",
        ];

        let has_multi_step_keywords = multi_step_keywords
            .iter()
            .any(|keyword| combined_text.contains(keyword));

        // Check for complexity indicators
        let complexity_indicators = combined_text.contains("multiple")
            || combined_text.contains("several")
            || combined_text.contains("comprehensive")
            || combined_text.contains("full")
            || combined_text.contains("complete");

        // Task type based complexity
        let complex_task_types = matches!(
            task.task_type,
            TaskType::Feature | TaskType::Infrastructure | TaskType::Development
        );

        // High priority tasks often need careful orchestration
        let high_priority = matches!(task.priority, Priority::High | Priority::Critical);

        // Decision logic
        (has_multi_step_keywords && complexity_indicators)
            || (complex_task_types && high_priority)
            || (complexity_indicators && complex_task_types)
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:update_agent_experience (lines 1529-1568) ---[0m
    pub fn update_agent_experience(&mut self, task: &Task) {
        // タスクタイプに基づいて経験値を付与
        let experience_points = match task.priority {
            Priority::Critical => 100,
            Priority::High => 50,
            Priority::Medium => 30,
            Priority::Low => 10,
        };

        // タスクタイプに関連するスキルを特定して経験値を追加
        // Simplified: give experience to all skills, with some variation by task type
        let experience_multiplier = match task.task_type {
            TaskType::Development | TaskType::Feature | TaskType::Infrastructure => 1.0,
            TaskType::Testing | TaskType::Review => 0.8,
            TaskType::Documentation => 0.6,
            TaskType::Coordination => 0.7,
            TaskType::Bugfix | TaskType::Bug => 1.2,
            TaskType::Remediation => 1.5,
            TaskType::Assistance => 0.9,
            TaskType::Research => 0.7,
        };

        let adjusted_experience = (experience_points as f32 * experience_multiplier) as u32;

        for skill in self.personality.skills.values_mut() {
            skill.add_experience(adjusted_experience as f32);
            tracing::debug!(
                "Added {} experience to skill: {}",
                adjusted_experience,
                skill.name
            );
        }

        // 個性の説明を更新してログ出力
        tracing::info!(
            "Agent {} personality update: {}",
            self.identity.agent_id,
            self.personality.describe_personality()
        );
    }

  ./crates/ccswarm/src/agent/mod.rs:280-324 method new_with_isolation <-> ./crates/ccswarm/src/agent/mod.rs:406-453 method setup_worktree
  Similarity: 82.49%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:new_with_isolation (lines 280-324) ---[0m
    pub async fn new_with_isolation(
        role: AgentRole,
        workspace_root: &std::path::Path,
        branch_prefix: &str,
        claude_config: ClaudeConfig,
        isolation_mode: IsolationMode,
    ) -> Result<Self> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());
        let session_id = Uuid::new_v4().to_string();
        let worktree_path = workspace_root.join(format!("agents/{}", &agent_id));
        let branch_name = format!("{}/{}", branch_prefix, &agent_id);

        let identity = AgentIdentity {
            agent_id: agent_id.clone(),
            specialization: role.clone(),
            workspace_path: worktree_path.clone(),
            env_vars: Self::create_env_vars(&agent_id, &session_id),
            session_id,
            parent_process_id: std::process::id().to_string(),
            initialized_at: Utc::now(),
        };

        let personality = AgentPersonality::new(agent_id.clone());

        let whiteboard = Whiteboard::new(agent_id.clone());
        let phronesis = PhronesisManager::new();

        let agent = Self {
            identity,
            worktree_path,
            branch_name,
            claude_config,
            status: AgentStatus::Initializing,
            current_task: None,
            task_history: Vec::new(),
            last_activity: Utc::now(),
            isolation_mode,
            container_id: None,
            personality,
            whiteboard,
            phronesis,
        };

        Ok(agent)
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:setup_worktree (lines 406-453) ---[0m
    async fn setup_worktree(&self) -> Result<()> {
        // Create parent directory if needed
        if let Some(parent) = self.worktree_path.parent() {
            tokio::fs::create_dir_all(parent)
                .await
                .context("Failed to create parent directory")?;
        }

        // Create branch
        let output = Command::new("git")
            .args(["checkout", "-b", &self.branch_name])
            .output()
            .await
            .context("Failed to create branch")?;

        if !output.status.success()
            && !String::from_utf8_lossy(&output.stderr).contains("already exists")
        {
            return Err(anyhow::anyhow!(
                "Failed to create branch: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        // Create worktree
        let output = Command::new("git")
            .args([
                "worktree",
                "add",
                self.worktree_path
                    .to_str()
                    .ok_or_else(|| anyhow::anyhow!("Invalid UTF-8 in worktree path"))?,
                &self.branch_name,
            ])
            .output()
            .await
            .context("Failed to create worktree")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to create worktree: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        tracing::info!("Git worktree created at: {}", self.worktree_path.display());
        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:280-324 method new_with_isolation <-> ./crates/ccswarm/src/agent/mod.rs:1373-1412 method execute_claude_real_api
  Similarity: 89.82%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:new_with_isolation (lines 280-324) ---[0m
    pub async fn new_with_isolation(
        role: AgentRole,
        workspace_root: &std::path::Path,
        branch_prefix: &str,
        claude_config: ClaudeConfig,
        isolation_mode: IsolationMode,
    ) -> Result<Self> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());
        let session_id = Uuid::new_v4().to_string();
        let worktree_path = workspace_root.join(format!("agents/{}", &agent_id));
        let branch_name = format!("{}/{}", branch_prefix, &agent_id);

        let identity = AgentIdentity {
            agent_id: agent_id.clone(),
            specialization: role.clone(),
            workspace_path: worktree_path.clone(),
            env_vars: Self::create_env_vars(&agent_id, &session_id),
            session_id,
            parent_process_id: std::process::id().to_string(),
            initialized_at: Utc::now(),
        };

        let personality = AgentPersonality::new(agent_id.clone());

        let whiteboard = Whiteboard::new(agent_id.clone());
        let phronesis = PhronesisManager::new();

        let agent = Self {
            identity,
            worktree_path,
            branch_name,
            claude_config,
            status: AgentStatus::Initializing,
            current_task: None,
            task_history: Vec::new(),
            last_activity: Utc::now(),
            isolation_mode,
            container_id: None,
            personality,
            whiteboard,
            phronesis,
        };

        Ok(agent)
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_real_api (lines 1373-1412) ---[0m
    async fn execute_claude_real_api(&self, prompt: &str) -> Result<String> {
        use crate::providers::claude_api::ClaudeApiClient;

        tracing::info!(
            "Using real Claude API for agent: {}",
            self.identity.agent_id
        );

        // Create API client
        let api_client = ClaudeApiClient::new(None)?;

        // Format the full prompt with agent identity
        let full_prompt = format!(
            "{}\n\n{}",
            claude::generate_identity_header(&self.identity),
            prompt
        );

        // Make API call
        let response = api_client
            .simple_completion(
                &self.claude_config.model,
                &full_prompt,
                4096, // Max tokens
            )
            .await?;

        // Format response to match expected output format
        if self.claude_config.json_output {
            // Wrap response in JSON format similar to CLI output
            let json_response = serde_json::json!({
                "response": response,
                "agent": self.identity.agent_id,
                "timestamp": chrono::Utc::now().to_rfc3339(),
            });
            Ok(serde_json::to_string(&json_response)?)
        } else {
            Ok(response)
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:406-453 method setup_worktree <-> ./crates/ccswarm/src/agent/mod.rs:1324-1370 method execute_claude_in_worktree
  Similarity: 79.85%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:setup_worktree (lines 406-453) ---[0m
    async fn setup_worktree(&self) -> Result<()> {
        // Create parent directory if needed
        if let Some(parent) = self.worktree_path.parent() {
            tokio::fs::create_dir_all(parent)
                .await
                .context("Failed to create parent directory")?;
        }

        // Create branch
        let output = Command::new("git")
            .args(["checkout", "-b", &self.branch_name])
            .output()
            .await
            .context("Failed to create branch")?;

        if !output.status.success()
            && !String::from_utf8_lossy(&output.stderr).contains("already exists")
        {
            return Err(anyhow::anyhow!(
                "Failed to create branch: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        // Create worktree
        let output = Command::new("git")
            .args([
                "worktree",
                "add",
                self.worktree_path
                    .to_str()
                    .ok_or_else(|| anyhow::anyhow!("Invalid UTF-8 in worktree path"))?,
                &self.branch_name,
            ])
            .output()
            .await
            .context("Failed to create worktree")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to create worktree: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        tracing::info!("Git worktree created at: {}", self.worktree_path.display());
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_in_worktree (lines 1324-1370) ---[0m
    async fn execute_claude_in_worktree(&self, prompt: &str) -> Result<String> {
        // Check if we should use real API instead of simulation
        if self.claude_config.use_real_api {
            return self.execute_claude_real_api(prompt).await;
        }

        let mut cmd = Command::new("claude");

        // Set working directory
        cmd.current_dir(&self.worktree_path);

        // Add environment variables
        for (key, value) in &self.identity.env_vars {
            cmd.env(key, value);
        }

        // Add Claude arguments
        cmd.arg("-p").arg(prompt);

        if self.claude_config.json_output {
            cmd.arg("--json");
        }

        if self.claude_config.dangerous_skip {
            cmd.arg("--dangerously-skip-permissions");
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.claude_config.think_mode {
            cmd.arg("--think").arg(think_mode.to_string());
        }

        // Execute command
        let output = cmd
            .output()
            .await
            .context("Failed to execute Claude Code")?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).to_string())
        } else {
            Err(anyhow::anyhow!(
                "Claude Code execution failed: {}",
                String::from_utf8_lossy(&output.stderr)
            ))
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:371-403 method initialize <-> ./crates/ccswarm/src/agent/mod.rs:939-993 method is_complex_task
  Similarity: 86.08%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:initialize (lines 371-403) ---[0m
    pub async fn initialize(&mut self) -> Result<()> {
        tracing::info!(
            "Initializing agent: {} with isolation mode: {:?}",
            self.identity.agent_id,
            self.isolation_mode
        );

        // Setup Git worktree (always needed for code synchronization)
        self.setup_worktree().await?;

        // If using container isolation, create and configure container
        if self.isolation_mode.requires_docker() {
            self.setup_container().await?;
        }

        // Generate and write CLAUDE.md
        self.generate_claude_md().await?;

        // Setup Claude configuration
        self.setup_claude_config().await?;

        // Establish identity
        self.establish_identity().await?;

        // Run boundary verification
        self.verify_boundaries().await?;

        self.status = AgentStatus::Available;
        self.last_activity = Utc::now();

        tracing::info!("Agent {} initialized successfully", self.identity.agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:is_complex_task (lines 939-993) ---[0m
    fn is_complex_task(&self, task: &Task) -> bool {
        // A task is complex if it:
        // 1. Has multiple explicit steps in the description
        // 2. Contains keywords indicating multi-step work
        // 3. Has high priority and involves multiple components
        // 4. Is a feature or infrastructure task (typically multi-step)

        let description_lower = task.description.to_lowercase();
        let details_lower = task.details.as_deref().unwrap_or("").to_lowercase();
        let combined_text = format!("{} {}", description_lower, details_lower);

        // Check for multi-step indicators
        let multi_step_keywords = [
            "implement",
            "create",
            "build",
            "setup",
            "configure",
            "integrate",
            "migrate",
            "refactor",
            "design",
            "develop",
            "and then",
            "first",
            "next",
            "finally",
            "step",
        ];

        let has_multi_step_keywords = multi_step_keywords
            .iter()
            .any(|keyword| combined_text.contains(keyword));

        // Check for complexity indicators
        let complexity_indicators = combined_text.contains("multiple")
            || combined_text.contains("several")
            || combined_text.contains("comprehensive")
            || combined_text.contains("full")
            || combined_text.contains("complete");

        // Task type based complexity
        let complex_task_types = matches!(
            task.task_type,
            TaskType::Feature | TaskType::Infrastructure | TaskType::Development
        );

        // High priority tasks often need careful orchestration
        let high_priority = matches!(task.priority, Priority::High | Priority::Critical);

        // Decision logic
        (has_multi_step_keywords && complexity_indicators)
            || (complex_task_types && high_priority)
            || (complexity_indicators && complex_task_types)
    }

  ./crates/ccswarm/src/agent/mod.rs:406-453 method setup_worktree <-> ./crates/ccswarm/src/agent/mod.rs:1437-1476 method report_status
  Similarity: 84.58%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:setup_worktree (lines 406-453) ---[0m
    async fn setup_worktree(&self) -> Result<()> {
        // Create parent directory if needed
        if let Some(parent) = self.worktree_path.parent() {
            tokio::fs::create_dir_all(parent)
                .await
                .context("Failed to create parent directory")?;
        }

        // Create branch
        let output = Command::new("git")
            .args(["checkout", "-b", &self.branch_name])
            .output()
            .await
            .context("Failed to create branch")?;

        if !output.status.success()
            && !String::from_utf8_lossy(&output.stderr).contains("already exists")
        {
            return Err(anyhow::anyhow!(
                "Failed to create branch: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        // Create worktree
        let output = Command::new("git")
            .args([
                "worktree",
                "add",
                self.worktree_path
                    .to_str()
                    .ok_or_else(|| anyhow::anyhow!("Invalid UTF-8 in worktree path"))?,
                &self.branch_name,
            ])
            .output()
            .await
            .context("Failed to create worktree")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to create worktree: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        tracing::info!("Git worktree created at: {}", self.worktree_path.display());
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:report_status (lines 1437-1476) ---[0m
    async fn report_status(&self, result: &TaskResult) -> Result<()> {
        let status_report = serde_json::json!({
            "agent_id": self.identity.agent_id,
            "specialization": self.identity.specialization.name(),
            "status": self.status,
            "current_task": self.current_task,
            "last_result": result,
            "timestamp": Utc::now(),
            "worktree": self.worktree_path.to_string_lossy(),
            "branch": self.branch_name,
            "personality": {
                "description": self.personality.describe_personality(),
                "working_style": self.personality.working_style,
                "traits": self.personality.traits,
                "composability_score": self.personality.composability_score(),
                "skills": self.personality.skills.iter().map(|(name, skill)| {
                    serde_json::json!({
                        "name": name,
                        "category": "general",
                        "level": skill.level,
                        "experience": skill.experience_points,
                    })
                }).collect::<Vec<_>>(),
            },
            "whiteboard_summary": self.whiteboard.summarize(),
            "phronesis_summary": self.phronesis.summarize(),
        });

        // Write to coordination directory
        let status_file = PathBuf::from("coordination/agent-status")
            .join(format!("{}.json", self.identity.agent_id));

        if let Some(parent) = status_file.parent() {
            tokio::fs::create_dir_all(parent).await?;
        }

        tokio::fs::write(&status_file, serde_json::to_string_pretty(&status_report)?).await?;

        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:1437-1476 method report_status <-> ./crates/ccswarm/src/agent/mod.rs:1529-1568 method update_agent_experience
  Similarity: 91.16%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:report_status (lines 1437-1476) ---[0m
    async fn report_status(&self, result: &TaskResult) -> Result<()> {
        let status_report = serde_json::json!({
            "agent_id": self.identity.agent_id,
            "specialization": self.identity.specialization.name(),
            "status": self.status,
            "current_task": self.current_task,
            "last_result": result,
            "timestamp": Utc::now(),
            "worktree": self.worktree_path.to_string_lossy(),
            "branch": self.branch_name,
            "personality": {
                "description": self.personality.describe_personality(),
                "working_style": self.personality.working_style,
                "traits": self.personality.traits,
                "composability_score": self.personality.composability_score(),
                "skills": self.personality.skills.iter().map(|(name, skill)| {
                    serde_json::json!({
                        "name": name,
                        "category": "general",
                        "level": skill.level,
                        "experience": skill.experience_points,
                    })
                }).collect::<Vec<_>>(),
            },
            "whiteboard_summary": self.whiteboard.summarize(),
            "phronesis_summary": self.phronesis.summarize(),
        });

        // Write to coordination directory
        let status_file = PathBuf::from("coordination/agent-status")
            .join(format!("{}.json", self.identity.agent_id));

        if let Some(parent) = status_file.parent() {
            tokio::fs::create_dir_all(parent).await?;
        }

        tokio::fs::write(&status_file, serde_json::to_string_pretty(&status_report)?).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:update_agent_experience (lines 1529-1568) ---[0m
    pub fn update_agent_experience(&mut self, task: &Task) {
        // タスクタイプに基づいて経験値を付与
        let experience_points = match task.priority {
            Priority::Critical => 100,
            Priority::High => 50,
            Priority::Medium => 30,
            Priority::Low => 10,
        };

        // タスクタイプに関連するスキルを特定して経験値を追加
        // Simplified: give experience to all skills, with some variation by task type
        let experience_multiplier = match task.task_type {
            TaskType::Development | TaskType::Feature | TaskType::Infrastructure => 1.0,
            TaskType::Testing | TaskType::Review => 0.8,
            TaskType::Documentation => 0.6,
            TaskType::Coordination => 0.7,
            TaskType::Bugfix | TaskType::Bug => 1.2,
            TaskType::Remediation => 1.5,
            TaskType::Assistance => 0.9,
            TaskType::Research => 0.7,
        };

        let adjusted_experience = (experience_points as f32 * experience_multiplier) as u32;

        for skill in self.personality.skills.values_mut() {
            skill.add_experience(adjusted_experience as f32);
            tracing::debug!(
                "Added {} experience to skill: {}",
                adjusted_experience,
                skill.name
            );
        }

        // 個性の説明を更新してログ出力
        tracing::info!(
            "Agent {} personality update: {}",
            self.identity.agent_id,
            self.personality.describe_personality()
        );
    }

  ./crates/ccswarm/src/agent/mod.rs:280-324 method new_with_isolation <-> ./crates/ccswarm/src/agent/mod.rs:1529-1568 method update_agent_experience
  Similarity: 85.41%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:new_with_isolation (lines 280-324) ---[0m
    pub async fn new_with_isolation(
        role: AgentRole,
        workspace_root: &std::path::Path,
        branch_prefix: &str,
        claude_config: ClaudeConfig,
        isolation_mode: IsolationMode,
    ) -> Result<Self> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());
        let session_id = Uuid::new_v4().to_string();
        let worktree_path = workspace_root.join(format!("agents/{}", &agent_id));
        let branch_name = format!("{}/{}", branch_prefix, &agent_id);

        let identity = AgentIdentity {
            agent_id: agent_id.clone(),
            specialization: role.clone(),
            workspace_path: worktree_path.clone(),
            env_vars: Self::create_env_vars(&agent_id, &session_id),
            session_id,
            parent_process_id: std::process::id().to_string(),
            initialized_at: Utc::now(),
        };

        let personality = AgentPersonality::new(agent_id.clone());

        let whiteboard = Whiteboard::new(agent_id.clone());
        let phronesis = PhronesisManager::new();

        let agent = Self {
            identity,
            worktree_path,
            branch_name,
            claude_config,
            status: AgentStatus::Initializing,
            current_task: None,
            task_history: Vec::new(),
            last_activity: Utc::now(),
            isolation_mode,
            container_id: None,
            personality,
            whiteboard,
            phronesis,
        };

        Ok(agent)
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:update_agent_experience (lines 1529-1568) ---[0m
    pub fn update_agent_experience(&mut self, task: &Task) {
        // タスクタイプに基づいて経験値を付与
        let experience_points = match task.priority {
            Priority::Critical => 100,
            Priority::High => 50,
            Priority::Medium => 30,
            Priority::Low => 10,
        };

        // タスクタイプに関連するスキルを特定して経験値を追加
        // Simplified: give experience to all skills, with some variation by task type
        let experience_multiplier = match task.task_type {
            TaskType::Development | TaskType::Feature | TaskType::Infrastructure => 1.0,
            TaskType::Testing | TaskType::Review => 0.8,
            TaskType::Documentation => 0.6,
            TaskType::Coordination => 0.7,
            TaskType::Bugfix | TaskType::Bug => 1.2,
            TaskType::Remediation => 1.5,
            TaskType::Assistance => 0.9,
            TaskType::Research => 0.7,
        };

        let adjusted_experience = (experience_points as f32 * experience_multiplier) as u32;

        for skill in self.personality.skills.values_mut() {
            skill.add_experience(adjusted_experience as f32);
            tracing::debug!(
                "Added {} experience to skill: {}",
                adjusted_experience,
                skill.name
            );
        }

        // 個性の説明を更新してログ出力
        tracing::info!(
            "Agent {} personality update: {}",
            self.identity.agent_id,
            self.personality.describe_personality()
        );
    }

  ./crates/ccswarm/src/agent/mod.rs:1373-1412 method execute_claude_real_api <-> ./crates/ccswarm/src/agent/mod.rs:1437-1476 method report_status
  Similarity: 89.04%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_real_api (lines 1373-1412) ---[0m
    async fn execute_claude_real_api(&self, prompt: &str) -> Result<String> {
        use crate::providers::claude_api::ClaudeApiClient;

        tracing::info!(
            "Using real Claude API for agent: {}",
            self.identity.agent_id
        );

        // Create API client
        let api_client = ClaudeApiClient::new(None)?;

        // Format the full prompt with agent identity
        let full_prompt = format!(
            "{}\n\n{}",
            claude::generate_identity_header(&self.identity),
            prompt
        );

        // Make API call
        let response = api_client
            .simple_completion(
                &self.claude_config.model,
                &full_prompt,
                4096, // Max tokens
            )
            .await?;

        // Format response to match expected output format
        if self.claude_config.json_output {
            // Wrap response in JSON format similar to CLI output
            let json_response = serde_json::json!({
                "response": response,
                "agent": self.identity.agent_id,
                "timestamp": chrono::Utc::now().to_rfc3339(),
            });
            Ok(serde_json::to_string(&json_response)?)
        } else {
            Ok(response)
        }
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:report_status (lines 1437-1476) ---[0m
    async fn report_status(&self, result: &TaskResult) -> Result<()> {
        let status_report = serde_json::json!({
            "agent_id": self.identity.agent_id,
            "specialization": self.identity.specialization.name(),
            "status": self.status,
            "current_task": self.current_task,
            "last_result": result,
            "timestamp": Utc::now(),
            "worktree": self.worktree_path.to_string_lossy(),
            "branch": self.branch_name,
            "personality": {
                "description": self.personality.describe_personality(),
                "working_style": self.personality.working_style,
                "traits": self.personality.traits,
                "composability_score": self.personality.composability_score(),
                "skills": self.personality.skills.iter().map(|(name, skill)| {
                    serde_json::json!({
                        "name": name,
                        "category": "general",
                        "level": skill.level,
                        "experience": skill.experience_points,
                    })
                }).collect::<Vec<_>>(),
            },
            "whiteboard_summary": self.whiteboard.summarize(),
            "phronesis_summary": self.phronesis.summarize(),
        });

        // Write to coordination directory
        let status_file = PathBuf::from("coordination/agent-status")
            .join(format!("{}.json", self.identity.agent_id));

        if let Some(parent) = status_file.parent() {
            tokio::fs::create_dir_all(parent).await?;
        }

        tokio::fs::write(&status_file, serde_json::to_string_pretty(&status_report)?).await?;

        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:1373-1412 method execute_claude_real_api <-> ./crates/ccswarm/src/agent/mod.rs:1529-1568 method update_agent_experience
  Similarity: 86.97%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_real_api (lines 1373-1412) ---[0m
    async fn execute_claude_real_api(&self, prompt: &str) -> Result<String> {
        use crate::providers::claude_api::ClaudeApiClient;

        tracing::info!(
            "Using real Claude API for agent: {}",
            self.identity.agent_id
        );

        // Create API client
        let api_client = ClaudeApiClient::new(None)?;

        // Format the full prompt with agent identity
        let full_prompt = format!(
            "{}\n\n{}",
            claude::generate_identity_header(&self.identity),
            prompt
        );

        // Make API call
        let response = api_client
            .simple_completion(
                &self.claude_config.model,
                &full_prompt,
                4096, // Max tokens
            )
            .await?;

        // Format response to match expected output format
        if self.claude_config.json_output {
            // Wrap response in JSON format similar to CLI output
            let json_response = serde_json::json!({
                "response": response,
                "agent": self.identity.agent_id,
                "timestamp": chrono::Utc::now().to_rfc3339(),
            });
            Ok(serde_json::to_string(&json_response)?)
        } else {
            Ok(response)
        }
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:update_agent_experience (lines 1529-1568) ---[0m
    pub fn update_agent_experience(&mut self, task: &Task) {
        // タスクタイプに基づいて経験値を付与
        let experience_points = match task.priority {
            Priority::Critical => 100,
            Priority::High => 50,
            Priority::Medium => 30,
            Priority::Low => 10,
        };

        // タスクタイプに関連するスキルを特定して経験値を追加
        // Simplified: give experience to all skills, with some variation by task type
        let experience_multiplier = match task.task_type {
            TaskType::Development | TaskType::Feature | TaskType::Infrastructure => 1.0,
            TaskType::Testing | TaskType::Review => 0.8,
            TaskType::Documentation => 0.6,
            TaskType::Coordination => 0.7,
            TaskType::Bugfix | TaskType::Bug => 1.2,
            TaskType::Remediation => 1.5,
            TaskType::Assistance => 0.9,
            TaskType::Research => 0.7,
        };

        let adjusted_experience = (experience_points as f32 * experience_multiplier) as u32;

        for skill in self.personality.skills.values_mut() {
            skill.add_experience(adjusted_experience as f32);
            tracing::debug!(
                "Added {} experience to skill: {}",
                adjusted_experience,
                skill.name
            );
        }

        // 個性の説明を更新してログ出力
        tracing::info!(
            "Agent {} personality update: {}",
            self.identity.agent_id,
            self.personality.describe_personality()
        );
    }

  ./crates/ccswarm/src/agent/mod.rs:939-993 method is_complex_task <-> ./crates/ccswarm/src/agent/mod.rs:1437-1476 method report_status
  Similarity: 73.18%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:is_complex_task (lines 939-993) ---[0m
    fn is_complex_task(&self, task: &Task) -> bool {
        // A task is complex if it:
        // 1. Has multiple explicit steps in the description
        // 2. Contains keywords indicating multi-step work
        // 3. Has high priority and involves multiple components
        // 4. Is a feature or infrastructure task (typically multi-step)

        let description_lower = task.description.to_lowercase();
        let details_lower = task.details.as_deref().unwrap_or("").to_lowercase();
        let combined_text = format!("{} {}", description_lower, details_lower);

        // Check for multi-step indicators
        let multi_step_keywords = [
            "implement",
            "create",
            "build",
            "setup",
            "configure",
            "integrate",
            "migrate",
            "refactor",
            "design",
            "develop",
            "and then",
            "first",
            "next",
            "finally",
            "step",
        ];

        let has_multi_step_keywords = multi_step_keywords
            .iter()
            .any(|keyword| combined_text.contains(keyword));

        // Check for complexity indicators
        let complexity_indicators = combined_text.contains("multiple")
            || combined_text.contains("several")
            || combined_text.contains("comprehensive")
            || combined_text.contains("full")
            || combined_text.contains("complete");

        // Task type based complexity
        let complex_task_types = matches!(
            task.task_type,
            TaskType::Feature | TaskType::Infrastructure | TaskType::Development
        );

        // High priority tasks often need careful orchestration
        let high_priority = matches!(task.priority, Priority::High | Priority::Critical);

        // Decision logic
        (has_multi_step_keywords && complexity_indicators)
            || (complex_task_types && high_priority)
            || (complexity_indicators && complex_task_types)
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:report_status (lines 1437-1476) ---[0m
    async fn report_status(&self, result: &TaskResult) -> Result<()> {
        let status_report = serde_json::json!({
            "agent_id": self.identity.agent_id,
            "specialization": self.identity.specialization.name(),
            "status": self.status,
            "current_task": self.current_task,
            "last_result": result,
            "timestamp": Utc::now(),
            "worktree": self.worktree_path.to_string_lossy(),
            "branch": self.branch_name,
            "personality": {
                "description": self.personality.describe_personality(),
                "working_style": self.personality.working_style,
                "traits": self.personality.traits,
                "composability_score": self.personality.composability_score(),
                "skills": self.personality.skills.iter().map(|(name, skill)| {
                    serde_json::json!({
                        "name": name,
                        "category": "general",
                        "level": skill.level,
                        "experience": skill.experience_points,
                    })
                }).collect::<Vec<_>>(),
            },
            "whiteboard_summary": self.whiteboard.summarize(),
            "phronesis_summary": self.phronesis.summarize(),
        });

        // Write to coordination directory
        let status_file = PathBuf::from("coordination/agent-status")
            .join(format!("{}.json", self.identity.agent_id));

        if let Some(parent) = status_file.parent() {
            tokio::fs::create_dir_all(parent).await?;
        }

        tokio::fs::write(&status_file, serde_json::to_string_pretty(&status_report)?).await?;

        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:371-403 method initialize <-> ./crates/ccswarm/src/agent/mod.rs:1324-1370 method execute_claude_in_worktree
  Similarity: 84.81%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:initialize (lines 371-403) ---[0m
    pub async fn initialize(&mut self) -> Result<()> {
        tracing::info!(
            "Initializing agent: {} with isolation mode: {:?}",
            self.identity.agent_id,
            self.isolation_mode
        );

        // Setup Git worktree (always needed for code synchronization)
        self.setup_worktree().await?;

        // If using container isolation, create and configure container
        if self.isolation_mode.requires_docker() {
            self.setup_container().await?;
        }

        // Generate and write CLAUDE.md
        self.generate_claude_md().await?;

        // Setup Claude configuration
        self.setup_claude_config().await?;

        // Establish identity
        self.establish_identity().await?;

        // Run boundary verification
        self.verify_boundaries().await?;

        self.status = AgentStatus::Available;
        self.last_activity = Utc::now();

        tracing::info!("Agent {} initialized successfully", self.identity.agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_in_worktree (lines 1324-1370) ---[0m
    async fn execute_claude_in_worktree(&self, prompt: &str) -> Result<String> {
        // Check if we should use real API instead of simulation
        if self.claude_config.use_real_api {
            return self.execute_claude_real_api(prompt).await;
        }

        let mut cmd = Command::new("claude");

        // Set working directory
        cmd.current_dir(&self.worktree_path);

        // Add environment variables
        for (key, value) in &self.identity.env_vars {
            cmd.env(key, value);
        }

        // Add Claude arguments
        cmd.arg("-p").arg(prompt);

        if self.claude_config.json_output {
            cmd.arg("--json");
        }

        if self.claude_config.dangerous_skip {
            cmd.arg("--dangerously-skip-permissions");
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.claude_config.think_mode {
            cmd.arg("--think").arg(think_mode.to_string());
        }

        // Execute command
        let output = cmd
            .output()
            .await
            .context("Failed to execute Claude Code")?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).to_string())
        } else {
            Err(anyhow::anyhow!(
                "Claude Code execution failed: {}",
                String::from_utf8_lossy(&output.stderr)
            ))
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:371-403 method initialize <-> ./crates/ccswarm/src/agent/mod.rs:406-453 method setup_worktree
  Similarity: 83.47%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:initialize (lines 371-403) ---[0m
    pub async fn initialize(&mut self) -> Result<()> {
        tracing::info!(
            "Initializing agent: {} with isolation mode: {:?}",
            self.identity.agent_id,
            self.isolation_mode
        );

        // Setup Git worktree (always needed for code synchronization)
        self.setup_worktree().await?;

        // If using container isolation, create and configure container
        if self.isolation_mode.requires_docker() {
            self.setup_container().await?;
        }

        // Generate and write CLAUDE.md
        self.generate_claude_md().await?;

        // Setup Claude configuration
        self.setup_claude_config().await?;

        // Establish identity
        self.establish_identity().await?;

        // Run boundary verification
        self.verify_boundaries().await?;

        self.status = AgentStatus::Available;
        self.last_activity = Utc::now();

        tracing::info!("Agent {} initialized successfully", self.identity.agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:setup_worktree (lines 406-453) ---[0m
    async fn setup_worktree(&self) -> Result<()> {
        // Create parent directory if needed
        if let Some(parent) = self.worktree_path.parent() {
            tokio::fs::create_dir_all(parent)
                .await
                .context("Failed to create parent directory")?;
        }

        // Create branch
        let output = Command::new("git")
            .args(["checkout", "-b", &self.branch_name])
            .output()
            .await
            .context("Failed to create branch")?;

        if !output.status.success()
            && !String::from_utf8_lossy(&output.stderr).contains("already exists")
        {
            return Err(anyhow::anyhow!(
                "Failed to create branch: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        // Create worktree
        let output = Command::new("git")
            .args([
                "worktree",
                "add",
                self.worktree_path
                    .to_str()
                    .ok_or_else(|| anyhow::anyhow!("Invalid UTF-8 in worktree path"))?,
                &self.branch_name,
            ])
            .output()
            .await
            .context("Failed to create worktree")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to create worktree: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        tracing::info!("Git worktree created at: {}", self.worktree_path.display());
        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:1324-1370 method execute_claude_in_worktree <-> ./crates/ccswarm/src/agent/mod.rs:1373-1412 method execute_claude_real_api
  Similarity: 77.33%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_in_worktree (lines 1324-1370) ---[0m
    async fn execute_claude_in_worktree(&self, prompt: &str) -> Result<String> {
        // Check if we should use real API instead of simulation
        if self.claude_config.use_real_api {
            return self.execute_claude_real_api(prompt).await;
        }

        let mut cmd = Command::new("claude");

        // Set working directory
        cmd.current_dir(&self.worktree_path);

        // Add environment variables
        for (key, value) in &self.identity.env_vars {
            cmd.env(key, value);
        }

        // Add Claude arguments
        cmd.arg("-p").arg(prompt);

        if self.claude_config.json_output {
            cmd.arg("--json");
        }

        if self.claude_config.dangerous_skip {
            cmd.arg("--dangerously-skip-permissions");
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.claude_config.think_mode {
            cmd.arg("--think").arg(think_mode.to_string());
        }

        // Execute command
        let output = cmd
            .output()
            .await
            .context("Failed to execute Claude Code")?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).to_string())
        } else {
            Err(anyhow::anyhow!(
                "Claude Code execution failed: {}",
                String::from_utf8_lossy(&output.stderr)
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_real_api (lines 1373-1412) ---[0m
    async fn execute_claude_real_api(&self, prompt: &str) -> Result<String> {
        use crate::providers::claude_api::ClaudeApiClient;

        tracing::info!(
            "Using real Claude API for agent: {}",
            self.identity.agent_id
        );

        // Create API client
        let api_client = ClaudeApiClient::new(None)?;

        // Format the full prompt with agent identity
        let full_prompt = format!(
            "{}\n\n{}",
            claude::generate_identity_header(&self.identity),
            prompt
        );

        // Make API call
        let response = api_client
            .simple_completion(
                &self.claude_config.model,
                &full_prompt,
                4096, // Max tokens
            )
            .await?;

        // Format response to match expected output format
        if self.claude_config.json_output {
            // Wrap response in JSON format similar to CLI output
            let json_response = serde_json::json!({
                "response": response,
                "agent": self.identity.agent_id,
                "timestamp": chrono::Utc::now().to_rfc3339(),
            });
            Ok(serde_json::to_string(&json_response)?)
        } else {
            Ok(response)
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:280-324 method new_with_isolation <-> ./crates/ccswarm/src/agent/mod.rs:1437-1476 method report_status
  Similarity: 76.92%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:new_with_isolation (lines 280-324) ---[0m
    pub async fn new_with_isolation(
        role: AgentRole,
        workspace_root: &std::path::Path,
        branch_prefix: &str,
        claude_config: ClaudeConfig,
        isolation_mode: IsolationMode,
    ) -> Result<Self> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());
        let session_id = Uuid::new_v4().to_string();
        let worktree_path = workspace_root.join(format!("agents/{}", &agent_id));
        let branch_name = format!("{}/{}", branch_prefix, &agent_id);

        let identity = AgentIdentity {
            agent_id: agent_id.clone(),
            specialization: role.clone(),
            workspace_path: worktree_path.clone(),
            env_vars: Self::create_env_vars(&agent_id, &session_id),
            session_id,
            parent_process_id: std::process::id().to_string(),
            initialized_at: Utc::now(),
        };

        let personality = AgentPersonality::new(agent_id.clone());

        let whiteboard = Whiteboard::new(agent_id.clone());
        let phronesis = PhronesisManager::new();

        let agent = Self {
            identity,
            worktree_path,
            branch_name,
            claude_config,
            status: AgentStatus::Initializing,
            current_task: None,
            task_history: Vec::new(),
            last_activity: Utc::now(),
            isolation_mode,
            container_id: None,
            personality,
            whiteboard,
            phronesis,
        };

        Ok(agent)
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:report_status (lines 1437-1476) ---[0m
    async fn report_status(&self, result: &TaskResult) -> Result<()> {
        let status_report = serde_json::json!({
            "agent_id": self.identity.agent_id,
            "specialization": self.identity.specialization.name(),
            "status": self.status,
            "current_task": self.current_task,
            "last_result": result,
            "timestamp": Utc::now(),
            "worktree": self.worktree_path.to_string_lossy(),
            "branch": self.branch_name,
            "personality": {
                "description": self.personality.describe_personality(),
                "working_style": self.personality.working_style,
                "traits": self.personality.traits,
                "composability_score": self.personality.composability_score(),
                "skills": self.personality.skills.iter().map(|(name, skill)| {
                    serde_json::json!({
                        "name": name,
                        "category": "general",
                        "level": skill.level,
                        "experience": skill.experience_points,
                    })
                }).collect::<Vec<_>>(),
            },
            "whiteboard_summary": self.whiteboard.summarize(),
            "phronesis_summary": self.phronesis.summarize(),
        });

        // Write to coordination directory
        let status_file = PathBuf::from("coordination/agent-status")
            .join(format!("{}.json", self.identity.agent_id));

        if let Some(parent) = status_file.parent() {
            tokio::fs::create_dir_all(parent).await?;
        }

        tokio::fs::write(&status_file, serde_json::to_string_pretty(&status_report)?).await?;

        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:406-453 method setup_worktree <-> ./crates/ccswarm/src/agent/mod.rs:1597-1620 method shutdown
  Similarity: 88.77%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:setup_worktree (lines 406-453) ---[0m
    async fn setup_worktree(&self) -> Result<()> {
        // Create parent directory if needed
        if let Some(parent) = self.worktree_path.parent() {
            tokio::fs::create_dir_all(parent)
                .await
                .context("Failed to create parent directory")?;
        }

        // Create branch
        let output = Command::new("git")
            .args(["checkout", "-b", &self.branch_name])
            .output()
            .await
            .context("Failed to create branch")?;

        if !output.status.success()
            && !String::from_utf8_lossy(&output.stderr).contains("already exists")
        {
            return Err(anyhow::anyhow!(
                "Failed to create branch: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        // Create worktree
        let output = Command::new("git")
            .args([
                "worktree",
                "add",
                self.worktree_path
                    .to_str()
                    .ok_or_else(|| anyhow::anyhow!("Invalid UTF-8 in worktree path"))?,
                &self.branch_name,
            ])
            .output()
            .await
            .context("Failed to create worktree")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to create worktree: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        tracing::info!("Git worktree created at: {}", self.worktree_path.display());
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:shutdown (lines 1597-1620) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down agent: {}", self.identity.agent_id);
        self.status = AgentStatus::ShuttingDown;

        // Final status report
        self.report_status(&TaskResult {
            success: true,
            output: serde_json::json!({"action": "shutdown"}),
            error: None,
            duration: std::time::Duration::from_secs(0),
        })
        .await?;

        // Clean up container if using container isolation
        if let Some(container_id) = &self.container_id {
            tracing::info!("Cleaning up container: {}", container_id);
            match self.cleanup_container(container_id).await {
                Ok(_) => tracing::info!("Container cleanup complete"),
                Err(e) => tracing::warn!("Container cleanup failed: {}", e),
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:1324-1370 method execute_claude_in_worktree <-> ./crates/ccswarm/src/agent/mod.rs:1529-1568 method update_agent_experience
  Similarity: 71.62%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_in_worktree (lines 1324-1370) ---[0m
    async fn execute_claude_in_worktree(&self, prompt: &str) -> Result<String> {
        // Check if we should use real API instead of simulation
        if self.claude_config.use_real_api {
            return self.execute_claude_real_api(prompt).await;
        }

        let mut cmd = Command::new("claude");

        // Set working directory
        cmd.current_dir(&self.worktree_path);

        // Add environment variables
        for (key, value) in &self.identity.env_vars {
            cmd.env(key, value);
        }

        // Add Claude arguments
        cmd.arg("-p").arg(prompt);

        if self.claude_config.json_output {
            cmd.arg("--json");
        }

        if self.claude_config.dangerous_skip {
            cmd.arg("--dangerously-skip-permissions");
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.claude_config.think_mode {
            cmd.arg("--think").arg(think_mode.to_string());
        }

        // Execute command
        let output = cmd
            .output()
            .await
            .context("Failed to execute Claude Code")?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).to_string())
        } else {
            Err(anyhow::anyhow!(
                "Claude Code execution failed: {}",
                String::from_utf8_lossy(&output.stderr)
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:update_agent_experience (lines 1529-1568) ---[0m
    pub fn update_agent_experience(&mut self, task: &Task) {
        // タスクタイプに基づいて経験値を付与
        let experience_points = match task.priority {
            Priority::Critical => 100,
            Priority::High => 50,
            Priority::Medium => 30,
            Priority::Low => 10,
        };

        // タスクタイプに関連するスキルを特定して経験値を追加
        // Simplified: give experience to all skills, with some variation by task type
        let experience_multiplier = match task.task_type {
            TaskType::Development | TaskType::Feature | TaskType::Infrastructure => 1.0,
            TaskType::Testing | TaskType::Review => 0.8,
            TaskType::Documentation => 0.6,
            TaskType::Coordination => 0.7,
            TaskType::Bugfix | TaskType::Bug => 1.2,
            TaskType::Remediation => 1.5,
            TaskType::Assistance => 0.9,
            TaskType::Research => 0.7,
        };

        let adjusted_experience = (experience_points as f32 * experience_multiplier) as u32;

        for skill in self.personality.skills.values_mut() {
            skill.add_experience(adjusted_experience as f32);
            tracing::debug!(
                "Added {} experience to skill: {}",
                adjusted_experience,
                skill.name
            );
        }

        // 個性の説明を更新してログ出力
        tracing::info!(
            "Agent {} personality update: {}",
            self.identity.agent_id,
            self.personality.describe_personality()
        );
    }

  ./crates/ccswarm/src/agent/mod.rs:1437-1476 method report_status <-> ./crates/ccswarm/src/agent/mod.rs:1597-1620 method shutdown
  Similarity: 94.21%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:report_status (lines 1437-1476) ---[0m
    async fn report_status(&self, result: &TaskResult) -> Result<()> {
        let status_report = serde_json::json!({
            "agent_id": self.identity.agent_id,
            "specialization": self.identity.specialization.name(),
            "status": self.status,
            "current_task": self.current_task,
            "last_result": result,
            "timestamp": Utc::now(),
            "worktree": self.worktree_path.to_string_lossy(),
            "branch": self.branch_name,
            "personality": {
                "description": self.personality.describe_personality(),
                "working_style": self.personality.working_style,
                "traits": self.personality.traits,
                "composability_score": self.personality.composability_score(),
                "skills": self.personality.skills.iter().map(|(name, skill)| {
                    serde_json::json!({
                        "name": name,
                        "category": "general",
                        "level": skill.level,
                        "experience": skill.experience_points,
                    })
                }).collect::<Vec<_>>(),
            },
            "whiteboard_summary": self.whiteboard.summarize(),
            "phronesis_summary": self.phronesis.summarize(),
        });

        // Write to coordination directory
        let status_file = PathBuf::from("coordination/agent-status")
            .join(format!("{}.json", self.identity.agent_id));

        if let Some(parent) = status_file.parent() {
            tokio::fs::create_dir_all(parent).await?;
        }

        tokio::fs::write(&status_file, serde_json::to_string_pretty(&status_report)?).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:shutdown (lines 1597-1620) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down agent: {}", self.identity.agent_id);
        self.status = AgentStatus::ShuttingDown;

        // Final status report
        self.report_status(&TaskResult {
            success: true,
            output: serde_json::json!({"action": "shutdown"}),
            error: None,
            duration: std::time::Duration::from_secs(0),
        })
        .await?;

        // Clean up container if using container isolation
        if let Some(container_id) = &self.container_id {
            tracing::info!("Cleaning up container: {}", container_id);
            match self.cleanup_container(container_id).await {
                Ok(_) => tracing::info!("Container cleanup complete"),
                Err(e) => tracing::warn!("Container cleanup failed: {}", e),
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:280-324 method new_with_isolation <-> ./crates/ccswarm/src/agent/mod.rs:371-403 method initialize
  Similarity: 77.12%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:new_with_isolation (lines 280-324) ---[0m
    pub async fn new_with_isolation(
        role: AgentRole,
        workspace_root: &std::path::Path,
        branch_prefix: &str,
        claude_config: ClaudeConfig,
        isolation_mode: IsolationMode,
    ) -> Result<Self> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());
        let session_id = Uuid::new_v4().to_string();
        let worktree_path = workspace_root.join(format!("agents/{}", &agent_id));
        let branch_name = format!("{}/{}", branch_prefix, &agent_id);

        let identity = AgentIdentity {
            agent_id: agent_id.clone(),
            specialization: role.clone(),
            workspace_path: worktree_path.clone(),
            env_vars: Self::create_env_vars(&agent_id, &session_id),
            session_id,
            parent_process_id: std::process::id().to_string(),
            initialized_at: Utc::now(),
        };

        let personality = AgentPersonality::new(agent_id.clone());

        let whiteboard = Whiteboard::new(agent_id.clone());
        let phronesis = PhronesisManager::new();

        let agent = Self {
            identity,
            worktree_path,
            branch_name,
            claude_config,
            status: AgentStatus::Initializing,
            current_task: None,
            task_history: Vec::new(),
            last_activity: Utc::now(),
            isolation_mode,
            container_id: None,
            personality,
            whiteboard,
            phronesis,
        };

        Ok(agent)
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:initialize (lines 371-403) ---[0m
    pub async fn initialize(&mut self) -> Result<()> {
        tracing::info!(
            "Initializing agent: {} with isolation mode: {:?}",
            self.identity.agent_id,
            self.isolation_mode
        );

        // Setup Git worktree (always needed for code synchronization)
        self.setup_worktree().await?;

        // If using container isolation, create and configure container
        if self.isolation_mode.requires_docker() {
            self.setup_container().await?;
        }

        // Generate and write CLAUDE.md
        self.generate_claude_md().await?;

        // Setup Claude configuration
        self.setup_claude_config().await?;

        // Establish identity
        self.establish_identity().await?;

        // Run boundary verification
        self.verify_boundaries().await?;

        self.status = AgentStatus::Available;
        self.last_activity = Utc::now();

        tracing::info!("Agent {} initialized successfully", self.identity.agent_id);
        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:1529-1568 method update_agent_experience <-> ./crates/ccswarm/src/agent/mod.rs:1597-1620 method shutdown
  Similarity: 91.42%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:update_agent_experience (lines 1529-1568) ---[0m
    pub fn update_agent_experience(&mut self, task: &Task) {
        // タスクタイプに基づいて経験値を付与
        let experience_points = match task.priority {
            Priority::Critical => 100,
            Priority::High => 50,
            Priority::Medium => 30,
            Priority::Low => 10,
        };

        // タスクタイプに関連するスキルを特定して経験値を追加
        // Simplified: give experience to all skills, with some variation by task type
        let experience_multiplier = match task.task_type {
            TaskType::Development | TaskType::Feature | TaskType::Infrastructure => 1.0,
            TaskType::Testing | TaskType::Review => 0.8,
            TaskType::Documentation => 0.6,
            TaskType::Coordination => 0.7,
            TaskType::Bugfix | TaskType::Bug => 1.2,
            TaskType::Remediation => 1.5,
            TaskType::Assistance => 0.9,
            TaskType::Research => 0.7,
        };

        let adjusted_experience = (experience_points as f32 * experience_multiplier) as u32;

        for skill in self.personality.skills.values_mut() {
            skill.add_experience(adjusted_experience as f32);
            tracing::debug!(
                "Added {} experience to skill: {}",
                adjusted_experience,
                skill.name
            );
        }

        // 個性の説明を更新してログ出力
        tracing::info!(
            "Agent {} personality update: {}",
            self.identity.agent_id,
            self.personality.describe_personality()
        );
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:shutdown (lines 1597-1620) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down agent: {}", self.identity.agent_id);
        self.status = AgentStatus::ShuttingDown;

        // Final status report
        self.report_status(&TaskResult {
            success: true,
            output: serde_json::json!({"action": "shutdown"}),
            error: None,
            duration: std::time::Duration::from_secs(0),
        })
        .await?;

        // Clean up container if using container isolation
        if let Some(container_id) = &self.container_id {
            tracing::info!("Cleaning up container: {}", container_id);
            match self.cleanup_container(container_id).await {
                Ok(_) => tracing::info!("Container cleanup complete"),
                Err(e) => tracing::warn!("Container cleanup failed: {}", e),
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:939-993 method is_complex_task <-> ./crates/ccswarm/src/agent/mod.rs:1597-1620 method shutdown
  Similarity: 72.39%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:is_complex_task (lines 939-993) ---[0m
    fn is_complex_task(&self, task: &Task) -> bool {
        // A task is complex if it:
        // 1. Has multiple explicit steps in the description
        // 2. Contains keywords indicating multi-step work
        // 3. Has high priority and involves multiple components
        // 4. Is a feature or infrastructure task (typically multi-step)

        let description_lower = task.description.to_lowercase();
        let details_lower = task.details.as_deref().unwrap_or("").to_lowercase();
        let combined_text = format!("{} {}", description_lower, details_lower);

        // Check for multi-step indicators
        let multi_step_keywords = [
            "implement",
            "create",
            "build",
            "setup",
            "configure",
            "integrate",
            "migrate",
            "refactor",
            "design",
            "develop",
            "and then",
            "first",
            "next",
            "finally",
            "step",
        ];

        let has_multi_step_keywords = multi_step_keywords
            .iter()
            .any(|keyword| combined_text.contains(keyword));

        // Check for complexity indicators
        let complexity_indicators = combined_text.contains("multiple")
            || combined_text.contains("several")
            || combined_text.contains("comprehensive")
            || combined_text.contains("full")
            || combined_text.contains("complete");

        // Task type based complexity
        let complex_task_types = matches!(
            task.task_type,
            TaskType::Feature | TaskType::Infrastructure | TaskType::Development
        );

        // High priority tasks often need careful orchestration
        let high_priority = matches!(task.priority, Priority::High | Priority::Critical);

        // Decision logic
        (has_multi_step_keywords && complexity_indicators)
            || (complex_task_types && high_priority)
            || (complexity_indicators && complex_task_types)
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:shutdown (lines 1597-1620) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down agent: {}", self.identity.agent_id);
        self.status = AgentStatus::ShuttingDown;

        // Final status report
        self.report_status(&TaskResult {
            success: true,
            output: serde_json::json!({"action": "shutdown"}),
            error: None,
            duration: std::time::Duration::from_secs(0),
        })
        .await?;

        // Clean up container if using container isolation
        if let Some(container_id) = &self.container_id {
            tracing::info!("Cleaning up container: {}", container_id);
            match self.cleanup_container(container_id).await {
                Ok(_) => tracing::info!("Container cleanup complete"),
                Err(e) => tracing::warn!("Container cleanup failed: {}", e),
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:1373-1412 method execute_claude_real_api <-> ./crates/ccswarm/src/agent/mod.rs:1597-1620 method shutdown
  Similarity: 85.27%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_real_api (lines 1373-1412) ---[0m
    async fn execute_claude_real_api(&self, prompt: &str) -> Result<String> {
        use crate::providers::claude_api::ClaudeApiClient;

        tracing::info!(
            "Using real Claude API for agent: {}",
            self.identity.agent_id
        );

        // Create API client
        let api_client = ClaudeApiClient::new(None)?;

        // Format the full prompt with agent identity
        let full_prompt = format!(
            "{}\n\n{}",
            claude::generate_identity_header(&self.identity),
            prompt
        );

        // Make API call
        let response = api_client
            .simple_completion(
                &self.claude_config.model,
                &full_prompt,
                4096, // Max tokens
            )
            .await?;

        // Format response to match expected output format
        if self.claude_config.json_output {
            // Wrap response in JSON format similar to CLI output
            let json_response = serde_json::json!({
                "response": response,
                "agent": self.identity.agent_id,
                "timestamp": chrono::Utc::now().to_rfc3339(),
            });
            Ok(serde_json::to_string(&json_response)?)
        } else {
            Ok(response)
        }
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:shutdown (lines 1597-1620) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down agent: {}", self.identity.agent_id);
        self.status = AgentStatus::ShuttingDown;

        // Final status report
        self.report_status(&TaskResult {
            success: true,
            output: serde_json::json!({"action": "shutdown"}),
            error: None,
            duration: std::time::Duration::from_secs(0),
        })
        .await?;

        // Clean up container if using container isolation
        if let Some(container_id) = &self.container_id {
            tracing::info!("Cleaning up container: {}", container_id);
            match self.cleanup_container(container_id).await {
                Ok(_) => tracing::info!("Container cleanup complete"),
                Err(e) => tracing::warn!("Container cleanup failed: {}", e),
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:516-548 method create_test_task_for_role <-> ./crates/ccswarm/src/agent/mod.rs:1437-1476 method report_status
  Similarity: 74.75%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:create_test_task_for_role (lines 516-548) ---[0m
    fn create_test_task_for_role(&self) -> Task {
        let (description, task_type) = match &self.identity.specialization {
            AgentRole::Frontend { .. } => (
                "Verify React component rendering".to_string(),
                TaskType::Development,
            ),
            AgentRole::Backend { .. } => (
                "Verify API endpoint functionality".to_string(),
                TaskType::Development,
            ),
            AgentRole::DevOps { .. } => (
                "Verify Docker container build".to_string(),
                TaskType::Infrastructure,
            ),
            AgentRole::QA { .. } => ("Verify test suite execution".to_string(), TaskType::Testing),
            AgentRole::Master { .. } => (
                "Verify coordination capabilities".to_string(),
                TaskType::Coordination,
            ),
            AgentRole::Search { .. } => (
                "Verify search capabilities".to_string(),
                TaskType::Assistance,
            ),
        };

        Task::new(
            Uuid::new_v4().to_string(),
            description,
            Priority::Low,
            task_type,
        )
        .with_details("Boundary verification test".to_string())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:report_status (lines 1437-1476) ---[0m
    async fn report_status(&self, result: &TaskResult) -> Result<()> {
        let status_report = serde_json::json!({
            "agent_id": self.identity.agent_id,
            "specialization": self.identity.specialization.name(),
            "status": self.status,
            "current_task": self.current_task,
            "last_result": result,
            "timestamp": Utc::now(),
            "worktree": self.worktree_path.to_string_lossy(),
            "branch": self.branch_name,
            "personality": {
                "description": self.personality.describe_personality(),
                "working_style": self.personality.working_style,
                "traits": self.personality.traits,
                "composability_score": self.personality.composability_score(),
                "skills": self.personality.skills.iter().map(|(name, skill)| {
                    serde_json::json!({
                        "name": name,
                        "category": "general",
                        "level": skill.level,
                        "experience": skill.experience_points,
                    })
                }).collect::<Vec<_>>(),
            },
            "whiteboard_summary": self.whiteboard.summarize(),
            "phronesis_summary": self.phronesis.summarize(),
        });

        // Write to coordination directory
        let status_file = PathBuf::from("coordination/agent-status")
            .join(format!("{}.json", self.identity.agent_id));

        if let Some(parent) = status_file.parent() {
            tokio::fs::create_dir_all(parent).await?;
        }

        tokio::fs::write(&status_file, serde_json::to_string_pretty(&status_report)?).await?;

        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:371-403 method initialize <-> ./crates/ccswarm/src/agent/mod.rs:1529-1568 method update_agent_experience
  Similarity: 72.74%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:initialize (lines 371-403) ---[0m
    pub async fn initialize(&mut self) -> Result<()> {
        tracing::info!(
            "Initializing agent: {} with isolation mode: {:?}",
            self.identity.agent_id,
            self.isolation_mode
        );

        // Setup Git worktree (always needed for code synchronization)
        self.setup_worktree().await?;

        // If using container isolation, create and configure container
        if self.isolation_mode.requires_docker() {
            self.setup_container().await?;
        }

        // Generate and write CLAUDE.md
        self.generate_claude_md().await?;

        // Setup Claude configuration
        self.setup_claude_config().await?;

        // Establish identity
        self.establish_identity().await?;

        // Run boundary verification
        self.verify_boundaries().await?;

        self.status = AgentStatus::Available;
        self.last_activity = Utc::now();

        tracing::info!("Agent {} initialized successfully", self.identity.agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:update_agent_experience (lines 1529-1568) ---[0m
    pub fn update_agent_experience(&mut self, task: &Task) {
        // タスクタイプに基づいて経験値を付与
        let experience_points = match task.priority {
            Priority::Critical => 100,
            Priority::High => 50,
            Priority::Medium => 30,
            Priority::Low => 10,
        };

        // タスクタイプに関連するスキルを特定して経験値を追加
        // Simplified: give experience to all skills, with some variation by task type
        let experience_multiplier = match task.task_type {
            TaskType::Development | TaskType::Feature | TaskType::Infrastructure => 1.0,
            TaskType::Testing | TaskType::Review => 0.8,
            TaskType::Documentation => 0.6,
            TaskType::Coordination => 0.7,
            TaskType::Bugfix | TaskType::Bug => 1.2,
            TaskType::Remediation => 1.5,
            TaskType::Assistance => 0.9,
            TaskType::Research => 0.7,
        };

        let adjusted_experience = (experience_points as f32 * experience_multiplier) as u32;

        for skill in self.personality.skills.values_mut() {
            skill.add_experience(adjusted_experience as f32);
            tracing::debug!(
                "Added {} experience to skill: {}",
                adjusted_experience,
                skill.name
            );
        }

        // 個性の説明を更新してログ出力
        tracing::info!(
            "Agent {} personality update: {}",
            self.identity.agent_id,
            self.personality.describe_personality()
        );
    }

  ./crates/ccswarm/src/agent/mod.rs:280-324 method new_with_isolation <-> ./crates/ccswarm/src/agent/mod.rs:1597-1620 method shutdown
  Similarity: 76.50%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:new_with_isolation (lines 280-324) ---[0m
    pub async fn new_with_isolation(
        role: AgentRole,
        workspace_root: &std::path::Path,
        branch_prefix: &str,
        claude_config: ClaudeConfig,
        isolation_mode: IsolationMode,
    ) -> Result<Self> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());
        let session_id = Uuid::new_v4().to_string();
        let worktree_path = workspace_root.join(format!("agents/{}", &agent_id));
        let branch_name = format!("{}/{}", branch_prefix, &agent_id);

        let identity = AgentIdentity {
            agent_id: agent_id.clone(),
            specialization: role.clone(),
            workspace_path: worktree_path.clone(),
            env_vars: Self::create_env_vars(&agent_id, &session_id),
            session_id,
            parent_process_id: std::process::id().to_string(),
            initialized_at: Utc::now(),
        };

        let personality = AgentPersonality::new(agent_id.clone());

        let whiteboard = Whiteboard::new(agent_id.clone());
        let phronesis = PhronesisManager::new();

        let agent = Self {
            identity,
            worktree_path,
            branch_name,
            claude_config,
            status: AgentStatus::Initializing,
            current_task: None,
            task_history: Vec::new(),
            last_activity: Utc::now(),
            isolation_mode,
            container_id: None,
            personality,
            whiteboard,
            phronesis,
        };

        Ok(agent)
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:shutdown (lines 1597-1620) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down agent: {}", self.identity.agent_id);
        self.status = AgentStatus::ShuttingDown;

        // Final status report
        self.report_status(&TaskResult {
            success: true,
            output: serde_json::json!({"action": "shutdown"}),
            error: None,
            duration: std::time::Duration::from_secs(0),
        })
        .await?;

        // Clean up container if using container isolation
        if let Some(container_id) = &self.container_id {
            tracing::info!("Cleaning up container: {}", container_id);
            match self.cleanup_container(container_id).await {
                Ok(_) => tracing::info!("Container cleanup complete"),
                Err(e) => tracing::warn!("Container cleanup failed: {}", e),
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:371-403 method initialize <-> ./crates/ccswarm/src/agent/mod.rs:1373-1412 method execute_claude_real_api
  Similarity: 71.82%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:initialize (lines 371-403) ---[0m
    pub async fn initialize(&mut self) -> Result<()> {
        tracing::info!(
            "Initializing agent: {} with isolation mode: {:?}",
            self.identity.agent_id,
            self.isolation_mode
        );

        // Setup Git worktree (always needed for code synchronization)
        self.setup_worktree().await?;

        // If using container isolation, create and configure container
        if self.isolation_mode.requires_docker() {
            self.setup_container().await?;
        }

        // Generate and write CLAUDE.md
        self.generate_claude_md().await?;

        // Setup Claude configuration
        self.setup_claude_config().await?;

        // Establish identity
        self.establish_identity().await?;

        // Run boundary verification
        self.verify_boundaries().await?;

        self.status = AgentStatus::Available;
        self.last_activity = Utc::now();

        tracing::info!("Agent {} initialized successfully", self.identity.agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_real_api (lines 1373-1412) ---[0m
    async fn execute_claude_real_api(&self, prompt: &str) -> Result<String> {
        use crate::providers::claude_api::ClaudeApiClient;

        tracing::info!(
            "Using real Claude API for agent: {}",
            self.identity.agent_id
        );

        // Create API client
        let api_client = ClaudeApiClient::new(None)?;

        // Format the full prompt with agent identity
        let full_prompt = format!(
            "{}\n\n{}",
            claude::generate_identity_header(&self.identity),
            prompt
        );

        // Make API call
        let response = api_client
            .simple_completion(
                &self.claude_config.model,
                &full_prompt,
                4096, // Max tokens
            )
            .await?;

        // Format response to match expected output format
        if self.claude_config.json_output {
            // Wrap response in JSON format similar to CLI output
            let json_response = serde_json::json!({
                "response": response,
                "agent": self.identity.agent_id,
                "timestamp": chrono::Utc::now().to_rfc3339(),
            });
            Ok(serde_json::to_string(&json_response)?)
        } else {
            Ok(response)
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:1244-1280 method handle_identity_status <-> ./crates/ccswarm/src/agent/mod.rs:1303-1321 method execute_claude_command
  Similarity: 89.04%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:handle_identity_status (lines 1244-1280) ---[0m
    async fn handle_identity_status(
        &mut self,
        status: IdentityStatus,
        monitor: &mut IdentityMonitor,
    ) -> Result<()> {
        match status {
            IdentityStatus::Healthy => {
                tracing::debug!("Identity maintained during task execution");
                Ok(())
            }
            IdentityStatus::DriftDetected(msg) => {
                tracing::warn!("Identity drift detected: {}", msg);

                // Record drift as learning event
                self.phronesis.record_learning_event(
                    format!("Identity drift detected: {}", msg),
                    false
                );

                self.correct_identity_drift(monitor).await
            }
            IdentityStatus::BoundaryViolation(msg) => {
                // Record boundary violation
                self.phronesis.record_failure(
                    format!("Boundary violation: {}", msg)
                );
                Err(anyhow::anyhow!("Boundary violation detected: {}", msg))
            }
            IdentityStatus::CriticalFailure(msg) => {
                // Record critical failure
                self.phronesis.record_failure(
                    format!("Critical identity failure: {}", msg)
                );
                Err(anyhow::anyhow!("Critical identity failure: {}", msg))
            }
        }
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_command (lines 1303-1321) ---[0m
    async fn execute_claude_command(&self, prompt: &str) -> Result<String> {
        match self.isolation_mode {
            IsolationMode::Container => self.execute_claude_in_container(prompt).await,
            IsolationMode::GitWorktree => self.execute_claude_in_worktree(prompt).await,
            IsolationMode::Hybrid => {
                // Try container first, fall back to worktree
                match self.execute_claude_in_container(prompt).await {
                    Ok(result) => Ok(result),
                    Err(e) => {
                        tracing::warn!(
                            "Container execution failed, falling back to worktree: {}",
                            e
                        );
                        self.execute_claude_in_worktree(prompt).await
                    }
                }
            }
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:482-495 method establish_identity <-> ./crates/ccswarm/src/agent/mod.rs:1373-1412 method execute_claude_real_api
  Similarity: 85.09%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:establish_identity (lines 482-495) ---[0m
    pub async fn establish_identity(&mut self) -> Result<()> {
        let prompt = claude::generate_identity_establishment_prompt(&self.identity);

        // Execute identity establishment
        let response = self.execute_claude_command(&prompt).await?;

        // Verify response contains required identity markers
        if !claude::verify_identity_response(&response, &self.identity) {
            return Err(anyhow::anyhow!("Failed to establish agent identity"));
        }

        tracing::info!("Identity established for agent: {}", self.identity.agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_real_api (lines 1373-1412) ---[0m
    async fn execute_claude_real_api(&self, prompt: &str) -> Result<String> {
        use crate::providers::claude_api::ClaudeApiClient;

        tracing::info!(
            "Using real Claude API for agent: {}",
            self.identity.agent_id
        );

        // Create API client
        let api_client = ClaudeApiClient::new(None)?;

        // Format the full prompt with agent identity
        let full_prompt = format!(
            "{}\n\n{}",
            claude::generate_identity_header(&self.identity),
            prompt
        );

        // Make API call
        let response = api_client
            .simple_completion(
                &self.claude_config.model,
                &full_prompt,
                4096, // Max tokens
            )
            .await?;

        // Format response to match expected output format
        if self.claude_config.json_output {
            // Wrap response in JSON format similar to CLI output
            let json_response = serde_json::json!({
                "response": response,
                "agent": self.identity.agent_id,
                "timestamp": chrono::Utc::now().to_rfc3339(),
            });
            Ok(serde_json::to_string(&json_response)?)
        } else {
            Ok(response)
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:327-339 method create_env_vars <-> ./crates/ccswarm/src/agent/mod.rs:1529-1568 method update_agent_experience
  Similarity: 85.33%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:create_env_vars (lines 327-339) ---[0m
    fn create_env_vars(
        agent_id: &str,
        session_id: &str,
    ) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:update_agent_experience (lines 1529-1568) ---[0m
    pub fn update_agent_experience(&mut self, task: &Task) {
        // タスクタイプに基づいて経験値を付与
        let experience_points = match task.priority {
            Priority::Critical => 100,
            Priority::High => 50,
            Priority::Medium => 30,
            Priority::Low => 10,
        };

        // タスクタイプに関連するスキルを特定して経験値を追加
        // Simplified: give experience to all skills, with some variation by task type
        let experience_multiplier = match task.task_type {
            TaskType::Development | TaskType::Feature | TaskType::Infrastructure => 1.0,
            TaskType::Testing | TaskType::Review => 0.8,
            TaskType::Documentation => 0.6,
            TaskType::Coordination => 0.7,
            TaskType::Bugfix | TaskType::Bug => 1.2,
            TaskType::Remediation => 1.5,
            TaskType::Assistance => 0.9,
            TaskType::Research => 0.7,
        };

        let adjusted_experience = (experience_points as f32 * experience_multiplier) as u32;

        for skill in self.personality.skills.values_mut() {
            skill.add_experience(adjusted_experience as f32);
            tracing::debug!(
                "Added {} experience to skill: {}",
                adjusted_experience,
                skill.name
            );
        }

        // 個性の説明を更新してログ出力
        tracing::info!(
            "Agent {} personality update: {}",
            self.identity.agent_id,
            self.personality.describe_personality()
        );
    }

  ./crates/ccswarm/src/agent/mod.rs:516-548 method create_test_task_for_role <-> ./crates/ccswarm/src/agent/mod.rs:1224-1241 method extract_execution_observation
  Similarity: 86.74%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:create_test_task_for_role (lines 516-548) ---[0m
    fn create_test_task_for_role(&self) -> Task {
        let (description, task_type) = match &self.identity.specialization {
            AgentRole::Frontend { .. } => (
                "Verify React component rendering".to_string(),
                TaskType::Development,
            ),
            AgentRole::Backend { .. } => (
                "Verify API endpoint functionality".to_string(),
                TaskType::Development,
            ),
            AgentRole::DevOps { .. } => (
                "Verify Docker container build".to_string(),
                TaskType::Infrastructure,
            ),
            AgentRole::QA { .. } => ("Verify test suite execution".to_string(), TaskType::Testing),
            AgentRole::Master { .. } => (
                "Verify coordination capabilities".to_string(),
                TaskType::Coordination,
            ),
            AgentRole::Search { .. } => (
                "Verify search capabilities".to_string(),
                TaskType::Assistance,
            ),
        };

        Task::new(
            Uuid::new_v4().to_string(),
            description,
            Priority::Low,
            task_type,
        )
        .with_details("Boundary verification test".to_string())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:extract_execution_observation (lines 1224-1241) ---[0m
    fn extract_execution_observation(&self, output: &str) -> String {
        // Look for key indicators in output
        if output.contains("error") || output.contains("Error") {
            format!(
                "Execution encountered errors: {}",
                output
                    .lines()
                    .find(|l| l.contains("error"))
                    .unwrap_or("unknown error")
            )
        } else if output.contains("success") || output.contains("completed") {
            "Execution completed successfully".to_string()
        } else if output.contains("created") || output.contains("generated") {
            "New artifacts generated".to_string()
        } else {
            format!("Execution output: {} characters", output.len())
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:327-339 method create_env_vars <-> ./crates/ccswarm/src/agent/mod.rs:1373-1412 method execute_claude_real_api
  Similarity: 79.91%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:create_env_vars (lines 327-339) ---[0m
    fn create_env_vars(
        agent_id: &str,
        session_id: &str,
    ) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_real_api (lines 1373-1412) ---[0m
    async fn execute_claude_real_api(&self, prompt: &str) -> Result<String> {
        use crate::providers::claude_api::ClaudeApiClient;

        tracing::info!(
            "Using real Claude API for agent: {}",
            self.identity.agent_id
        );

        // Create API client
        let api_client = ClaudeApiClient::new(None)?;

        // Format the full prompt with agent identity
        let full_prompt = format!(
            "{}\n\n{}",
            claude::generate_identity_header(&self.identity),
            prompt
        );

        // Make API call
        let response = api_client
            .simple_completion(
                &self.claude_config.model,
                &full_prompt,
                4096, // Max tokens
            )
            .await?;

        // Format response to match expected output format
        if self.claude_config.json_output {
            // Wrap response in JSON format similar to CLI output
            let json_response = serde_json::json!({
                "response": response,
                "agent": self.identity.agent_id,
                "timestamp": chrono::Utc::now().to_rfc3339(),
            });
            Ok(serde_json::to_string(&json_response)?)
        } else {
            Ok(response)
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:516-548 method create_test_task_for_role <-> ./crates/ccswarm/src/agent/mod.rs:1597-1620 method shutdown
  Similarity: 73.81%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:create_test_task_for_role (lines 516-548) ---[0m
    fn create_test_task_for_role(&self) -> Task {
        let (description, task_type) = match &self.identity.specialization {
            AgentRole::Frontend { .. } => (
                "Verify React component rendering".to_string(),
                TaskType::Development,
            ),
            AgentRole::Backend { .. } => (
                "Verify API endpoint functionality".to_string(),
                TaskType::Development,
            ),
            AgentRole::DevOps { .. } => (
                "Verify Docker container build".to_string(),
                TaskType::Infrastructure,
            ),
            AgentRole::QA { .. } => ("Verify test suite execution".to_string(), TaskType::Testing),
            AgentRole::Master { .. } => (
                "Verify coordination capabilities".to_string(),
                TaskType::Coordination,
            ),
            AgentRole::Search { .. } => (
                "Verify search capabilities".to_string(),
                TaskType::Assistance,
            ),
        };

        Task::new(
            Uuid::new_v4().to_string(),
            description,
            Priority::Low,
            task_type,
        )
        .with_details("Boundary verification test".to_string())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:shutdown (lines 1597-1620) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down agent: {}", self.identity.agent_id);
        self.status = AgentStatus::ShuttingDown;

        // Final status report
        self.report_status(&TaskResult {
            success: true,
            output: serde_json::json!({"action": "shutdown"}),
            error: None,
            duration: std::time::Duration::from_secs(0),
        })
        .await?;

        // Clean up container if using container isolation
        if let Some(container_id) = &self.container_id {
            tracing::info!("Cleaning up container: {}", container_id);
            match self.cleanup_container(container_id).await {
                Ok(_) => tracing::info!("Container cleanup complete"),
                Err(e) => tracing::warn!("Container cleanup failed: {}", e),
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:1373-1412 method execute_claude_real_api <-> ./crates/ccswarm/src/agent/mod.rs:1637-1648 function test_agent_creation
  Similarity: 79.68%

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_real_api (lines 1373-1412) ---[0m
    async fn execute_claude_real_api(&self, prompt: &str) -> Result<String> {
        use crate::providers::claude_api::ClaudeApiClient;

        tracing::info!(
            "Using real Claude API for agent: {}",
            self.identity.agent_id
        );

        // Create API client
        let api_client = ClaudeApiClient::new(None)?;

        // Format the full prompt with agent identity
        let full_prompt = format!(
            "{}\n\n{}",
            claude::generate_identity_header(&self.identity),
            prompt
        );

        // Make API call
        let response = api_client
            .simple_completion(
                &self.claude_config.model,
                &full_prompt,
                4096, // Max tokens
            )
            .await?;

        // Format response to match expected output format
        if self.claude_config.json_output {
            // Wrap response in JSON format similar to CLI output
            let json_response = serde_json::json!({
                "response": response,
                "agent": self.identity.agent_id,
                "timestamp": chrono::Utc::now().to_rfc3339(),
            });
            Ok(serde_json::to_string(&json_response)?)
        } else {
            Ok(response)
        }
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:test_agent_creation (lines 1637-1648) ---[0m
    async fn test_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace = temp_dir.path().to_path_buf();

        let config = ClaudeConfig::default();
        let agent = ClaudeCodeAgent::new(default_frontend_role(), &workspace, "feature", config)
            .await
            .unwrap();

        assert!(agent.identity.agent_id.starts_with("frontend-agent-"));
        assert_eq!(agent.status, AgentStatus::Initializing);
    }

  ./crates/ccswarm/src/agent/mod.rs:498-513 method verify_boundaries <-> ./crates/ccswarm/src/agent/mod.rs:1373-1412 method execute_claude_real_api
  Similarity: 72.45%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:verify_boundaries (lines 498-513) ---[0m
    async fn verify_boundaries(&self) -> Result<()> {
        // Test with a task that should be accepted
        let test_task = self.create_test_task_for_role();
        let checker = TaskBoundaryChecker::new(self.identity.specialization.clone());

        match checker.evaluate_task(&test_task).await {
            TaskEvaluation::Accept { .. } => {
                tracing::info!(
                    "Boundary verification passed for agent: {}",
                    self.identity.agent_id
                );
                Ok(())
            }
            _ => Err(anyhow::anyhow!("Boundary verification failed")),
        }
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_real_api (lines 1373-1412) ---[0m
    async fn execute_claude_real_api(&self, prompt: &str) -> Result<String> {
        use crate::providers::claude_api::ClaudeApiClient;

        tracing::info!(
            "Using real Claude API for agent: {}",
            self.identity.agent_id
        );

        // Create API client
        let api_client = ClaudeApiClient::new(None)?;

        // Format the full prompt with agent identity
        let full_prompt = format!(
            "{}\n\n{}",
            claude::generate_identity_header(&self.identity),
            prompt
        );

        // Make API call
        let response = api_client
            .simple_completion(
                &self.claude_config.model,
                &full_prompt,
                4096, // Max tokens
            )
            .await?;

        // Format response to match expected output format
        if self.claude_config.json_output {
            // Wrap response in JSON format similar to CLI output
            let json_response = serde_json::json!({
                "response": response,
                "agent": self.identity.agent_id,
                "timestamp": chrono::Utc::now().to_rfc3339(),
            });
            Ok(serde_json::to_string(&json_response)?)
        } else {
            Ok(response)
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:498-513 method verify_boundaries <-> ./crates/ccswarm/src/agent/mod.rs:1244-1280 method handle_identity_status
  Similarity: 72.61%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:verify_boundaries (lines 498-513) ---[0m
    async fn verify_boundaries(&self) -> Result<()> {
        // Test with a task that should be accepted
        let test_task = self.create_test_task_for_role();
        let checker = TaskBoundaryChecker::new(self.identity.specialization.clone());

        match checker.evaluate_task(&test_task).await {
            TaskEvaluation::Accept { .. } => {
                tracing::info!(
                    "Boundary verification passed for agent: {}",
                    self.identity.agent_id
                );
                Ok(())
            }
            _ => Err(anyhow::anyhow!("Boundary verification failed")),
        }
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:handle_identity_status (lines 1244-1280) ---[0m
    async fn handle_identity_status(
        &mut self,
        status: IdentityStatus,
        monitor: &mut IdentityMonitor,
    ) -> Result<()> {
        match status {
            IdentityStatus::Healthy => {
                tracing::debug!("Identity maintained during task execution");
                Ok(())
            }
            IdentityStatus::DriftDetected(msg) => {
                tracing::warn!("Identity drift detected: {}", msg);

                // Record drift as learning event
                self.phronesis.record_learning_event(
                    format!("Identity drift detected: {}", msg),
                    false
                );

                self.correct_identity_drift(monitor).await
            }
            IdentityStatus::BoundaryViolation(msg) => {
                // Record boundary violation
                self.phronesis.record_failure(
                    format!("Boundary violation: {}", msg)
                );
                Err(anyhow::anyhow!("Boundary violation detected: {}", msg))
            }
            IdentityStatus::CriticalFailure(msg) => {
                // Record critical failure
                self.phronesis.record_failure(
                    format!("Critical identity failure: {}", msg)
                );
                Err(anyhow::anyhow!("Critical identity failure: {}", msg))
            }
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:327-339 method create_env_vars <-> ./crates/ccswarm/src/agent/mod.rs:516-548 method create_test_task_for_role
  Similarity: 75.27%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:create_env_vars (lines 327-339) ---[0m
    fn create_env_vars(
        agent_id: &str,
        session_id: &str,
    ) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:create_test_task_for_role (lines 516-548) ---[0m
    fn create_test_task_for_role(&self) -> Task {
        let (description, task_type) = match &self.identity.specialization {
            AgentRole::Frontend { .. } => (
                "Verify React component rendering".to_string(),
                TaskType::Development,
            ),
            AgentRole::Backend { .. } => (
                "Verify API endpoint functionality".to_string(),
                TaskType::Development,
            ),
            AgentRole::DevOps { .. } => (
                "Verify Docker container build".to_string(),
                TaskType::Infrastructure,
            ),
            AgentRole::QA { .. } => ("Verify test suite execution".to_string(), TaskType::Testing),
            AgentRole::Master { .. } => (
                "Verify coordination capabilities".to_string(),
                TaskType::Coordination,
            ),
            AgentRole::Search { .. } => (
                "Verify search capabilities".to_string(),
                TaskType::Assistance,
            ),
        };

        Task::new(
            Uuid::new_v4().to_string(),
            description,
            Priority::Low,
            task_type,
        )
        .with_details("Boundary verification test".to_string())
    }

  ./crates/ccswarm/src/agent/mod.rs:482-495 method establish_identity <-> ./crates/ccswarm/src/agent/mod.rs:1597-1620 method shutdown
  Similarity: 89.06%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:establish_identity (lines 482-495) ---[0m
    pub async fn establish_identity(&mut self) -> Result<()> {
        let prompt = claude::generate_identity_establishment_prompt(&self.identity);

        // Execute identity establishment
        let response = self.execute_claude_command(&prompt).await?;

        // Verify response contains required identity markers
        if !claude::verify_identity_response(&response, &self.identity) {
            return Err(anyhow::anyhow!("Failed to establish agent identity"));
        }

        tracing::info!("Identity established for agent: {}", self.identity.agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:shutdown (lines 1597-1620) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down agent: {}", self.identity.agent_id);
        self.status = AgentStatus::ShuttingDown;

        // Final status report
        self.report_status(&TaskResult {
            success: true,
            output: serde_json::json!({"action": "shutdown"}),
            error: None,
            duration: std::time::Duration::from_secs(0),
        })
        .await?;

        // Clean up container if using container isolation
        if let Some(container_id) = &self.container_id {
            tracing::info!("Cleaning up container: {}", container_id);
            match self.cleanup_container(container_id).await {
                Ok(_) => tracing::info!("Container cleanup complete"),
                Err(e) => tracing::warn!("Container cleanup failed: {}", e),
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:498-513 method verify_boundaries <-> ./crates/ccswarm/src/agent/mod.rs:1597-1620 method shutdown
  Similarity: 82.54%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:verify_boundaries (lines 498-513) ---[0m
    async fn verify_boundaries(&self) -> Result<()> {
        // Test with a task that should be accepted
        let test_task = self.create_test_task_for_role();
        let checker = TaskBoundaryChecker::new(self.identity.specialization.clone());

        match checker.evaluate_task(&test_task).await {
            TaskEvaluation::Accept { .. } => {
                tracing::info!(
                    "Boundary verification passed for agent: {}",
                    self.identity.agent_id
                );
                Ok(())
            }
            _ => Err(anyhow::anyhow!("Boundary verification failed")),
        }
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:shutdown (lines 1597-1620) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down agent: {}", self.identity.agent_id);
        self.status = AgentStatus::ShuttingDown;

        // Final status report
        self.report_status(&TaskResult {
            success: true,
            output: serde_json::json!({"action": "shutdown"}),
            error: None,
            duration: std::time::Duration::from_secs(0),
        })
        .await?;

        // Clean up container if using container isolation
        if let Some(container_id) = &self.container_id {
            tracing::info!("Cleaning up container: {}", container_id);
            match self.cleanup_container(container_id).await {
                Ok(_) => tracing::info!("Container cleanup complete"),
                Err(e) => tracing::warn!("Container cleanup failed: {}", e),
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:327-339 method create_env_vars <-> ./crates/ccswarm/src/agent/mod.rs:1597-1620 method shutdown
  Similarity: 87.46%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:create_env_vars (lines 327-339) ---[0m
    fn create_env_vars(
        agent_id: &str,
        session_id: &str,
    ) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:shutdown (lines 1597-1620) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down agent: {}", self.identity.agent_id);
        self.status = AgentStatus::ShuttingDown;

        // Final status report
        self.report_status(&TaskResult {
            success: true,
            output: serde_json::json!({"action": "shutdown"}),
            error: None,
            duration: std::time::Duration::from_secs(0),
        })
        .await?;

        // Clean up container if using container isolation
        if let Some(container_id) = &self.container_id {
            tracing::info!("Cleaning up container: {}", container_id);
            match self.cleanup_container(container_id).await {
                Ok(_) => tracing::info!("Container cleanup complete"),
                Err(e) => tracing::warn!("Container cleanup failed: {}", e),
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:1597-1620 method shutdown <-> ./crates/ccswarm/src/agent/mod.rs:1637-1648 function test_agent_creation
  Similarity: 84.64%

[36m--- ./crates/ccswarm/src/agent/mod.rs:shutdown (lines 1597-1620) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down agent: {}", self.identity.agent_id);
        self.status = AgentStatus::ShuttingDown;

        // Final status report
        self.report_status(&TaskResult {
            success: true,
            output: serde_json::json!({"action": "shutdown"}),
            error: None,
            duration: std::time::Duration::from_secs(0),
        })
        .await?;

        // Clean up container if using container isolation
        if let Some(container_id) = &self.container_id {
            tracing::info!("Cleaning up container: {}", container_id);
            match self.cleanup_container(container_id).await {
                Ok(_) => tracing::info!("Container cleanup complete"),
                Err(e) => tracing::warn!("Container cleanup failed: {}", e),
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:test_agent_creation (lines 1637-1648) ---[0m
    async fn test_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace = temp_dir.path().to_path_buf();

        let config = ClaudeConfig::default();
        let agent = ClaudeCodeAgent::new(default_frontend_role(), &workspace, "feature", config)
            .await
            .unwrap();

        assert!(agent.identity.agent_id.starts_with("frontend-agent-"));
        assert_eq!(agent.status, AgentStatus::Initializing);
    }

  ./crates/ccswarm/src/agent/mod.rs:1224-1241 method extract_execution_observation <-> ./crates/ccswarm/src/agent/mod.rs:1303-1321 method execute_claude_command
  Similarity: 78.78%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:extract_execution_observation (lines 1224-1241) ---[0m
    fn extract_execution_observation(&self, output: &str) -> String {
        // Look for key indicators in output
        if output.contains("error") || output.contains("Error") {
            format!(
                "Execution encountered errors: {}",
                output
                    .lines()
                    .find(|l| l.contains("error"))
                    .unwrap_or("unknown error")
            )
        } else if output.contains("success") || output.contains("completed") {
            "Execution completed successfully".to_string()
        } else if output.contains("created") || output.contains("generated") {
            "New artifacts generated".to_string()
        } else {
            format!("Execution output: {} characters", output.len())
        }
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:execute_claude_command (lines 1303-1321) ---[0m
    async fn execute_claude_command(&self, prompt: &str) -> Result<String> {
        match self.isolation_mode {
            IsolationMode::Container => self.execute_claude_in_container(prompt).await,
            IsolationMode::GitWorktree => self.execute_claude_in_worktree(prompt).await,
            IsolationMode::Hybrid => {
                // Try container first, fall back to worktree
                match self.execute_claude_in_container(prompt).await {
                    Ok(result) => Ok(result),
                    Err(e) => {
                        tracing::warn!(
                            "Container execution failed, falling back to worktree: {}",
                            e
                        );
                        self.execute_claude_in_worktree(prompt).await
                    }
                }
            }
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:498-513 method verify_boundaries <-> ./crates/ccswarm/src/agent/mod.rs:1224-1241 method extract_execution_observation
  Similarity: 83.17%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:verify_boundaries (lines 498-513) ---[0m
    async fn verify_boundaries(&self) -> Result<()> {
        // Test with a task that should be accepted
        let test_task = self.create_test_task_for_role();
        let checker = TaskBoundaryChecker::new(self.identity.specialization.clone());

        match checker.evaluate_task(&test_task).await {
            TaskEvaluation::Accept { .. } => {
                tracing::info!(
                    "Boundary verification passed for agent: {}",
                    self.identity.agent_id
                );
                Ok(())
            }
            _ => Err(anyhow::anyhow!("Boundary verification failed")),
        }
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:extract_execution_observation (lines 1224-1241) ---[0m
    fn extract_execution_observation(&self, output: &str) -> String {
        // Look for key indicators in output
        if output.contains("error") || output.contains("Error") {
            format!(
                "Execution encountered errors: {}",
                output
                    .lines()
                    .find(|l| l.contains("error"))
                    .unwrap_or("unknown error")
            )
        } else if output.contains("success") || output.contains("completed") {
            "Execution completed successfully".to_string()
        } else if output.contains("created") || output.contains("generated") {
            "New artifacts generated".to_string()
        } else {
            format!("Execution output: {} characters", output.len())
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:498-513 method verify_boundaries <-> ./crates/ccswarm/src/agent/mod.rs:1422-1434 method correct_identity_drift
  Similarity: 87.69%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:verify_boundaries (lines 498-513) ---[0m
    async fn verify_boundaries(&self) -> Result<()> {
        // Test with a task that should be accepted
        let test_task = self.create_test_task_for_role();
        let checker = TaskBoundaryChecker::new(self.identity.specialization.clone());

        match checker.evaluate_task(&test_task).await {
            TaskEvaluation::Accept { .. } => {
                tracing::info!(
                    "Boundary verification passed for agent: {}",
                    self.identity.agent_id
                );
                Ok(())
            }
            _ => Err(anyhow::anyhow!("Boundary verification failed")),
        }
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:correct_identity_drift (lines 1422-1434) ---[0m
    async fn correct_identity_drift(&self, monitor: &mut IdentityMonitor) -> Result<()> {
        let correction_prompt = monitor.generate_correction_prompt(
            &self.worktree_path.to_string_lossy(),
            self.identity.specialization.name(),
        );

        let _ = self.execute_claude_command(&correction_prompt).await?;
        tracing::info!(
            "Identity drift correction applied for agent: {}",
            self.identity.agent_id
        );
        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:327-339 method create_env_vars <-> ./crates/ccswarm/src/agent/mod.rs:498-513 method verify_boundaries
  Similarity: 79.86%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:create_env_vars (lines 327-339) ---[0m
    fn create_env_vars(
        agent_id: &str,
        session_id: &str,
    ) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:verify_boundaries (lines 498-513) ---[0m
    async fn verify_boundaries(&self) -> Result<()> {
        // Test with a task that should be accepted
        let test_task = self.create_test_task_for_role();
        let checker = TaskBoundaryChecker::new(self.identity.specialization.clone());

        match checker.evaluate_task(&test_task).await {
            TaskEvaluation::Accept { .. } => {
                tracing::info!(
                    "Boundary verification passed for agent: {}",
                    self.identity.agent_id
                );
                Ok(())
            }
            _ => Err(anyhow::anyhow!("Boundary verification failed")),
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:327-339 method create_env_vars <-> ./crates/ccswarm/src/agent/mod.rs:1422-1434 method correct_identity_drift
  Similarity: 83.96%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:create_env_vars (lines 327-339) ---[0m
    fn create_env_vars(
        agent_id: &str,
        session_id: &str,
    ) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:correct_identity_drift (lines 1422-1434) ---[0m
    async fn correct_identity_drift(&self, monitor: &mut IdentityMonitor) -> Result<()> {
        let correction_prompt = monitor.generate_correction_prompt(
            &self.worktree_path.to_string_lossy(),
            self.identity.specialization.name(),
        );

        let _ = self.execute_claude_command(&correction_prompt).await?;
        tracing::info!(
            "Identity drift correction applied for agent: {}",
            self.identity.agent_id
        );
        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:456-466 method generate_claude_md <-> ./crates/ccswarm/src/agent/mod.rs:482-495 method establish_identity
  Similarity: 85.82%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:generate_claude_md (lines 456-466) ---[0m
    async fn generate_claude_md(&self) -> Result<()> {
        let content = claude::generate_claude_md_content(&self.identity);
        let claude_md_path = self.worktree_path.join("CLAUDE.md");

        tokio::fs::write(&claude_md_path, content)
            .await
            .context("Failed to write CLAUDE.md")?;

        tracing::info!("CLAUDE.md generated for agent: {}", self.identity.agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:establish_identity (lines 482-495) ---[0m
    pub async fn establish_identity(&mut self) -> Result<()> {
        let prompt = claude::generate_identity_establishment_prompt(&self.identity);

        // Execute identity establishment
        let response = self.execute_claude_command(&prompt).await?;

        // Verify response contains required identity markers
        if !claude::verify_identity_response(&response, &self.identity) {
            return Err(anyhow::anyhow!("Failed to establish agent identity"));
        }

        tracing::info!("Identity established for agent: {}", self.identity.agent_id);
        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:469-479 method setup_claude_config <-> ./crates/ccswarm/src/agent/mod.rs:482-495 method establish_identity
  Similarity: 82.79%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:setup_claude_config (lines 469-479) ---[0m
    async fn setup_claude_config(&self) -> Result<()> {
        let config_path = self.worktree_path.join(".claude.json");
        let config_json = serde_json::to_string_pretty(&self.claude_config)?;

        tokio::fs::write(&config_path, config_json)
            .await
            .context("Failed to write .claude.json")?;

        tracing::info!(".claude.json created for agent: {}", self.identity.agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:establish_identity (lines 482-495) ---[0m
    pub async fn establish_identity(&mut self) -> Result<()> {
        let prompt = claude::generate_identity_establishment_prompt(&self.identity);

        // Execute identity establishment
        let response = self.execute_claude_command(&prompt).await?;

        // Verify response contains required identity markers
        if !claude::verify_identity_response(&response, &self.identity) {
            return Err(anyhow::anyhow!("Failed to establish agent identity"));
        }

        tracing::info!("Identity established for agent: {}", self.identity.agent_id);
        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:456-466 method generate_claude_md <-> ./crates/ccswarm/src/agent/mod.rs:498-513 method verify_boundaries
  Similarity: 76.31%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:generate_claude_md (lines 456-466) ---[0m
    async fn generate_claude_md(&self) -> Result<()> {
        let content = claude::generate_claude_md_content(&self.identity);
        let claude_md_path = self.worktree_path.join("CLAUDE.md");

        tokio::fs::write(&claude_md_path, content)
            .await
            .context("Failed to write CLAUDE.md")?;

        tracing::info!("CLAUDE.md generated for agent: {}", self.identity.agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:verify_boundaries (lines 498-513) ---[0m
    async fn verify_boundaries(&self) -> Result<()> {
        // Test with a task that should be accepted
        let test_task = self.create_test_task_for_role();
        let checker = TaskBoundaryChecker::new(self.identity.specialization.clone());

        match checker.evaluate_task(&test_task).await {
            TaskEvaluation::Accept { .. } => {
                tracing::info!(
                    "Boundary verification passed for agent: {}",
                    self.identity.agent_id
                );
                Ok(())
            }
            _ => Err(anyhow::anyhow!("Boundary verification failed")),
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:327-339 method create_env_vars <-> ./crates/ccswarm/src/agent/mod.rs:482-495 method establish_identity
  Similarity: 76.19%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:create_env_vars (lines 327-339) ---[0m
    fn create_env_vars(
        agent_id: &str,
        session_id: &str,
    ) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:establish_identity (lines 482-495) ---[0m
    pub async fn establish_identity(&mut self) -> Result<()> {
        let prompt = claude::generate_identity_establishment_prompt(&self.identity);

        // Execute identity establishment
        let response = self.execute_claude_command(&prompt).await?;

        // Verify response contains required identity markers
        if !claude::verify_identity_response(&response, &self.identity) {
            return Err(anyhow::anyhow!("Failed to establish agent identity"));
        }

        tracing::info!("Identity established for agent: {}", self.identity.agent_id);
        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:469-479 method setup_claude_config <-> ./crates/ccswarm/src/agent/mod.rs:498-513 method verify_boundaries
  Similarity: 75.77%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:setup_claude_config (lines 469-479) ---[0m
    async fn setup_claude_config(&self) -> Result<()> {
        let config_path = self.worktree_path.join(".claude.json");
        let config_json = serde_json::to_string_pretty(&self.claude_config)?;

        tokio::fs::write(&config_path, config_json)
            .await
            .context("Failed to write .claude.json")?;

        tracing::info!(".claude.json created for agent: {}", self.identity.agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:verify_boundaries (lines 498-513) ---[0m
    async fn verify_boundaries(&self) -> Result<()> {
        // Test with a task that should be accepted
        let test_task = self.create_test_task_for_role();
        let checker = TaskBoundaryChecker::new(self.identity.specialization.clone());

        match checker.evaluate_task(&test_task).await {
            TaskEvaluation::Accept { .. } => {
                tracing::info!(
                    "Boundary verification passed for agent: {}",
                    self.identity.agent_id
                );
                Ok(())
            }
            _ => Err(anyhow::anyhow!("Boundary verification failed")),
        }
    }

  ./crates/ccswarm/src/agent/mod.rs:482-495 method establish_identity <-> ./crates/ccswarm/src/agent/mod.rs:1422-1434 method correct_identity_drift
  Similarity: 74.51%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:establish_identity (lines 482-495) ---[0m
    pub async fn establish_identity(&mut self) -> Result<()> {
        let prompt = claude::generate_identity_establishment_prompt(&self.identity);

        // Execute identity establishment
        let response = self.execute_claude_command(&prompt).await?;

        // Verify response contains required identity markers
        if !claude::verify_identity_response(&response, &self.identity) {
            return Err(anyhow::anyhow!("Failed to establish agent identity"));
        }

        tracing::info!("Identity established for agent: {}", self.identity.agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:correct_identity_drift (lines 1422-1434) ---[0m
    async fn correct_identity_drift(&self, monitor: &mut IdentityMonitor) -> Result<()> {
        let correction_prompt = monitor.generate_correction_prompt(
            &self.worktree_path.to_string_lossy(),
            self.identity.specialization.name(),
        );

        let _ = self.execute_claude_command(&correction_prompt).await?;
        tracing::info!(
            "Identity drift correction applied for agent: {}",
            self.identity.agent_id
        );
        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:456-466 method generate_claude_md <-> ./crates/ccswarm/src/agent/mod.rs:469-479 method setup_claude_config
  Similarity: 90.67%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:generate_claude_md (lines 456-466) ---[0m
    async fn generate_claude_md(&self) -> Result<()> {
        let content = claude::generate_claude_md_content(&self.identity);
        let claude_md_path = self.worktree_path.join("CLAUDE.md");

        tokio::fs::write(&claude_md_path, content)
            .await
            .context("Failed to write CLAUDE.md")?;

        tracing::info!("CLAUDE.md generated for agent: {}", self.identity.agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:setup_claude_config (lines 469-479) ---[0m
    async fn setup_claude_config(&self) -> Result<()> {
        let config_path = self.worktree_path.join(".claude.json");
        let config_json = serde_json::to_string_pretty(&self.claude_config)?;

        tokio::fs::write(&config_path, config_json)
            .await
            .context("Failed to write .claude.json")?;

        tracing::info!(".claude.json created for agent: {}", self.identity.agent_id);
        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:482-495 method establish_identity <-> ./crates/ccswarm/src/agent/mod.rs:1637-1648 function test_agent_creation
  Similarity: 76.62%

[36m--- ./crates/ccswarm/src/agent/mod.rs:establish_identity (lines 482-495) ---[0m
    pub async fn establish_identity(&mut self) -> Result<()> {
        let prompt = claude::generate_identity_establishment_prompt(&self.identity);

        // Execute identity establishment
        let response = self.execute_claude_command(&prompt).await?;

        // Verify response contains required identity markers
        if !claude::verify_identity_response(&response, &self.identity) {
            return Err(anyhow::anyhow!("Failed to establish agent identity"));
        }

        tracing::info!("Identity established for agent: {}", self.identity.agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:test_agent_creation (lines 1637-1648) ---[0m
    async fn test_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace = temp_dir.path().to_path_buf();

        let config = ClaudeConfig::default();
        let agent = ClaudeCodeAgent::new(default_frontend_role(), &workspace, "feature", config)
            .await
            .unwrap();

        assert!(agent.identity.agent_id.starts_with("frontend-agent-"));
        assert_eq!(agent.status, AgentStatus::Initializing);
    }

  ./crates/ccswarm/src/agent/mod.rs:327-339 method create_env_vars <-> ./crates/ccswarm/src/agent/mod.rs:456-466 method generate_claude_md
  Similarity: 82.81%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:create_env_vars (lines 327-339) ---[0m
    fn create_env_vars(
        agent_id: &str,
        session_id: &str,
    ) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:generate_claude_md (lines 456-466) ---[0m
    async fn generate_claude_md(&self) -> Result<()> {
        let content = claude::generate_claude_md_content(&self.identity);
        let claude_md_path = self.worktree_path.join("CLAUDE.md");

        tokio::fs::write(&claude_md_path, content)
            .await
            .context("Failed to write CLAUDE.md")?;

        tracing::info!("CLAUDE.md generated for agent: {}", self.identity.agent_id);
        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:327-339 method create_env_vars <-> ./crates/ccswarm/src/agent/mod.rs:469-479 method setup_claude_config
  Similarity: 82.81%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:create_env_vars (lines 327-339) ---[0m
    fn create_env_vars(
        agent_id: &str,
        session_id: &str,
    ) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:setup_claude_config (lines 469-479) ---[0m
    async fn setup_claude_config(&self) -> Result<()> {
        let config_path = self.worktree_path.join(".claude.json");
        let config_json = serde_json::to_string_pretty(&self.claude_config)?;

        tokio::fs::write(&config_path, config_json)
            .await
            .context("Failed to write .claude.json")?;

        tracing::info!(".claude.json created for agent: {}", self.identity.agent_id);
        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:327-339 method create_env_vars <-> ./crates/ccswarm/src/agent/mod.rs:1637-1648 function test_agent_creation
  Similarity: 74.46%

[36m--- ./crates/ccswarm/src/agent/mod.rs:create_env_vars (lines 327-339) ---[0m
    fn create_env_vars(
        agent_id: &str,
        session_id: &str,
    ) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:test_agent_creation (lines 1637-1648) ---[0m
    async fn test_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace = temp_dir.path().to_path_buf();

        let config = ClaudeConfig::default();
        let agent = ClaudeCodeAgent::new(default_frontend_role(), &workspace, "feature", config)
            .await
            .unwrap();

        assert!(agent.identity.agent_id.starts_with("frontend-agent-"));
        assert_eq!(agent.status, AgentStatus::Initializing);
    }

  ./crates/ccswarm/src/agent/mod.rs:469-479 method setup_claude_config <-> ./crates/ccswarm/src/agent/mod.rs:1422-1434 method correct_identity_drift
  Similarity: 76.67%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:setup_claude_config (lines 469-479) ---[0m
    async fn setup_claude_config(&self) -> Result<()> {
        let config_path = self.worktree_path.join(".claude.json");
        let config_json = serde_json::to_string_pretty(&self.claude_config)?;

        tokio::fs::write(&config_path, config_json)
            .await
            .context("Failed to write .claude.json")?;

        tracing::info!(".claude.json created for agent: {}", self.identity.agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:correct_identity_drift (lines 1422-1434) ---[0m
    async fn correct_identity_drift(&self, monitor: &mut IdentityMonitor) -> Result<()> {
        let correction_prompt = monitor.generate_correction_prompt(
            &self.worktree_path.to_string_lossy(),
            self.identity.specialization.name(),
        );

        let _ = self.execute_claude_command(&correction_prompt).await?;
        tracing::info!(
            "Identity drift correction applied for agent: {}",
            self.identity.agent_id
        );
        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:456-466 method generate_claude_md <-> ./crates/ccswarm/src/agent/mod.rs:1422-1434 method correct_identity_drift
  Similarity: 76.21%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/mod.rs:generate_claude_md (lines 456-466) ---[0m
    async fn generate_claude_md(&self) -> Result<()> {
        let content = claude::generate_claude_md_content(&self.identity);
        let claude_md_path = self.worktree_path.join("CLAUDE.md");

        tokio::fs::write(&claude_md_path, content)
            .await
            .context("Failed to write CLAUDE.md")?;

        tracing::info!("CLAUDE.md generated for agent: {}", self.identity.agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:correct_identity_drift (lines 1422-1434) ---[0m
    async fn correct_identity_drift(&self, monitor: &mut IdentityMonitor) -> Result<()> {
        let correction_prompt = monitor.generate_correction_prompt(
            &self.worktree_path.to_string_lossy(),
            self.identity.specialization.name(),
        );

        let _ = self.execute_claude_command(&correction_prompt).await?;
        tracing::info!(
            "Identity drift correction applied for agent: {}",
            self.identity.agent_id
        );
        Ok(())
    }

  ./crates/ccswarm/src/agent/mod.rs:456-466 method generate_claude_md <-> ./crates/ccswarm/src/agent/mod.rs:1637-1648 function test_agent_creation
  Similarity: 73.85%

[36m--- ./crates/ccswarm/src/agent/mod.rs:generate_claude_md (lines 456-466) ---[0m
    async fn generate_claude_md(&self) -> Result<()> {
        let content = claude::generate_claude_md_content(&self.identity);
        let claude_md_path = self.worktree_path.join("CLAUDE.md");

        tokio::fs::write(&claude_md_path, content)
            .await
            .context("Failed to write CLAUDE.md")?;

        tracing::info!("CLAUDE.md generated for agent: {}", self.identity.agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:test_agent_creation (lines 1637-1648) ---[0m
    async fn test_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace = temp_dir.path().to_path_buf();

        let config = ClaudeConfig::default();
        let agent = ClaudeCodeAgent::new(default_frontend_role(), &workspace, "feature", config)
            .await
            .unwrap();

        assert!(agent.identity.agent_id.starts_with("frontend-agent-"));
        assert_eq!(agent.status, AgentStatus::Initializing);
    }

  ./crates/ccswarm/src/agent/mod.rs:469-479 method setup_claude_config <-> ./crates/ccswarm/src/agent/mod.rs:1637-1648 function test_agent_creation
  Similarity: 73.77%

[36m--- ./crates/ccswarm/src/agent/mod.rs:setup_claude_config (lines 469-479) ---[0m
    async fn setup_claude_config(&self) -> Result<()> {
        let config_path = self.worktree_path.join(".claude.json");
        let config_json = serde_json::to_string_pretty(&self.claude_config)?;

        tokio::fs::write(&config_path, config_json)
            .await
            .context("Failed to write .claude.json")?;

        tracing::info!(".claude.json created for agent: {}", self.identity.agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/mod.rs:test_agent_creation (lines 1637-1648) ---[0m
    async fn test_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace = temp_dir.path().to_path_buf();

        let config = ClaudeConfig::default();
        let agent = ClaudeCodeAgent::new(default_frontend_role(), &workspace, "feature", config)
            .await
            .unwrap();

        assert!(agent.identity.agent_id.starts_with("frontend-agent-"));
        assert_eq!(agent.status, AgentStatus::Initializing);
    }


Duplicates in ./crates/ccswarm/src/providers/claude_api.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/providers/claude_api.rs:76-119 method create_completion <-> ./crates/ccswarm/src/providers/claude_api.rs:122-152 method simple_completion
  Similarity: 94.63%
  Classes: ClaudeApiClient <-> ClaudeApiClient

[36m--- ./crates/ccswarm/src/providers/claude_api.rs:create_completion (lines 76-119) ---[0m
    pub async fn create_completion(
        &self,
        model: &str,
        messages: Vec<Message>,
        max_tokens: i32,
        temperature: Option<f32>,
        system: Option<String>,
    ) -> Result<ClaudeApiResponse> {
        let request = ClaudeApiRequest {
            model: model.to_string(),
            messages,
            max_tokens,
            temperature,
            system,
        };

        let response = self
            .client
            .post(format!("{}/v1/messages", self.base_url))
            .header("Content-Type", "application/json")
            .header("x-api-key", &self.api_key)
            .header("anthropic-version", "2023-06-01")
            .json(&request)
            .send()
            .await
            .context("Failed to send request to Claude API")?;

        if !response.status().is_success() {
            let status = response.status();
            let error_text = response.text().await.unwrap_or_default();
            return Err(anyhow::anyhow!(
                "Claude API error ({}): {}",
                status,
                error_text
            ));
        }

        let api_response: ClaudeApiResponse = response
            .json()
            .await
            .context("Failed to parse Claude API response")?;

        Ok(api_response)
    }

[36m--- ./crates/ccswarm/src/providers/claude_api.rs:simple_completion (lines 122-152) ---[0m
    pub async fn simple_completion(
        &self,
        model: &str,
        prompt: &str,
        max_tokens: i32,
    ) -> Result<String> {
        let messages = vec![Message {
            role: "user".to_string(),
            content: prompt.to_string(),
        }];

        let response = self
            .create_completion(
                model,
                messages,
                max_tokens,
                Some(0.0), // Temperature 0 for deterministic responses
                None,
            )
            .await?;

        // Extract text from response
        let text = response
            .content
            .into_iter()
            .map(|block| block.text)
            .collect::<Vec<_>>()
            .join("");

        Ok(text)
    }

  ./crates/ccswarm/src/providers/claude_api.rs:76-119 method create_completion <-> ./crates/ccswarm/src/providers/claude_api.rs:171-186 function test_request_serialization
  Similarity: 88.96%

[36m--- ./crates/ccswarm/src/providers/claude_api.rs:create_completion (lines 76-119) ---[0m
    pub async fn create_completion(
        &self,
        model: &str,
        messages: Vec<Message>,
        max_tokens: i32,
        temperature: Option<f32>,
        system: Option<String>,
    ) -> Result<ClaudeApiResponse> {
        let request = ClaudeApiRequest {
            model: model.to_string(),
            messages,
            max_tokens,
            temperature,
            system,
        };

        let response = self
            .client
            .post(format!("{}/v1/messages", self.base_url))
            .header("Content-Type", "application/json")
            .header("x-api-key", &self.api_key)
            .header("anthropic-version", "2023-06-01")
            .json(&request)
            .send()
            .await
            .context("Failed to send request to Claude API")?;

        if !response.status().is_success() {
            let status = response.status();
            let error_text = response.text().await.unwrap_or_default();
            return Err(anyhow::anyhow!(
                "Claude API error ({}): {}",
                status,
                error_text
            ));
        }

        let api_response: ClaudeApiResponse = response
            .json()
            .await
            .context("Failed to parse Claude API response")?;

        Ok(api_response)
    }

[36m--- ./crates/ccswarm/src/providers/claude_api.rs:test_request_serialization (lines 171-186) ---[0m
    fn test_request_serialization() {
        let request = ClaudeApiRequest {
            model: "claude-3.5-sonnet".to_string(),
            messages: vec![Message {
                role: "user".to_string(),
                content: "Test".to_string(),
            }],
            max_tokens: 1000,
            temperature: Some(0.7),
            system: None,
        };

        let json = serde_json::to_string(&request).unwrap();
        assert!(json.contains("claude-3.5-sonnet"));
        assert!(json.contains("Test"));
    }

  ./crates/ccswarm/src/providers/claude_api.rs:122-152 method simple_completion <-> ./crates/ccswarm/src/providers/claude_api.rs:171-186 function test_request_serialization
  Similarity: 87.27%

[36m--- ./crates/ccswarm/src/providers/claude_api.rs:simple_completion (lines 122-152) ---[0m
    pub async fn simple_completion(
        &self,
        model: &str,
        prompt: &str,
        max_tokens: i32,
    ) -> Result<String> {
        let messages = vec![Message {
            role: "user".to_string(),
            content: prompt.to_string(),
        }];

        let response = self
            .create_completion(
                model,
                messages,
                max_tokens,
                Some(0.0), // Temperature 0 for deterministic responses
                None,
            )
            .await?;

        // Extract text from response
        let text = response
            .content
            .into_iter()
            .map(|block| block.text)
            .collect::<Vec<_>>()
            .join("");

        Ok(text)
    }

[36m--- ./crates/ccswarm/src/providers/claude_api.rs:test_request_serialization (lines 171-186) ---[0m
    fn test_request_serialization() {
        let request = ClaudeApiRequest {
            model: "claude-3.5-sonnet".to_string(),
            messages: vec![Message {
                role: "user".to_string(),
                content: "Test".to_string(),
            }],
            max_tokens: 1000,
            temperature: Some(0.7),
            system: None,
        };

        let json = serde_json::to_string(&request).unwrap();
        assert!(json.contains("claude-3.5-sonnet"));
        assert!(json.contains("Test"));
    }

  ./crates/ccswarm/src/providers/claude_api.rs:57-73 method new <-> ./crates/ccswarm/src/providers/claude_api.rs:122-152 method simple_completion
  Similarity: 84.64%
  Classes: ClaudeApiClient <-> ClaudeApiClient

[36m--- ./crates/ccswarm/src/providers/claude_api.rs:new (lines 57-73) ---[0m
    pub fn new(api_key: Option<String>) -> Result<Self> {
        let api_key = api_key
            .or_else(|| env::var("ANTHROPIC_API_KEY").ok())
            .context(
                "ANTHROPIC_API_KEY not found. Please set it in environment or provide in config.",
            )?;

        let client = Client::builder()
            .timeout(std::time::Duration::from_secs(300)) // 5 minute timeout
            .build()?;

        Ok(Self {
            client,
            api_key,
            base_url: "https://api.anthropic.com".to_string(),
        })
    }

[36m--- ./crates/ccswarm/src/providers/claude_api.rs:simple_completion (lines 122-152) ---[0m
    pub async fn simple_completion(
        &self,
        model: &str,
        prompt: &str,
        max_tokens: i32,
    ) -> Result<String> {
        let messages = vec![Message {
            role: "user".to_string(),
            content: prompt.to_string(),
        }];

        let response = self
            .create_completion(
                model,
                messages,
                max_tokens,
                Some(0.0), // Temperature 0 for deterministic responses
                None,
            )
            .await?;

        // Extract text from response
        let text = response
            .content
            .into_iter()
            .map(|block| block.text)
            .collect::<Vec<_>>()
            .join("");

        Ok(text)
    }

  ./crates/ccswarm/src/providers/claude_api.rs:122-152 method simple_completion <-> ./crates/ccswarm/src/providers/claude_api.rs:160-168 function test_message_creation
  Similarity: 83.41%

[36m--- ./crates/ccswarm/src/providers/claude_api.rs:simple_completion (lines 122-152) ---[0m
    pub async fn simple_completion(
        &self,
        model: &str,
        prompt: &str,
        max_tokens: i32,
    ) -> Result<String> {
        let messages = vec![Message {
            role: "user".to_string(),
            content: prompt.to_string(),
        }];

        let response = self
            .create_completion(
                model,
                messages,
                max_tokens,
                Some(0.0), // Temperature 0 for deterministic responses
                None,
            )
            .await?;

        // Extract text from response
        let text = response
            .content
            .into_iter()
            .map(|block| block.text)
            .collect::<Vec<_>>()
            .join("");

        Ok(text)
    }

[36m--- ./crates/ccswarm/src/providers/claude_api.rs:test_message_creation (lines 160-168) ---[0m
    fn test_message_creation() {
        let message = Message {
            role: "user".to_string(),
            content: "Hello, Claude!".to_string(),
        };

        assert_eq!(message.role, "user");
        assert_eq!(message.content, "Hello, Claude!");
    }

  ./crates/ccswarm/src/providers/claude_api.rs:57-73 method new <-> ./crates/ccswarm/src/providers/claude_api.rs:171-186 function test_request_serialization
  Similarity: 80.98%

[36m--- ./crates/ccswarm/src/providers/claude_api.rs:new (lines 57-73) ---[0m
    pub fn new(api_key: Option<String>) -> Result<Self> {
        let api_key = api_key
            .or_else(|| env::var("ANTHROPIC_API_KEY").ok())
            .context(
                "ANTHROPIC_API_KEY not found. Please set it in environment or provide in config.",
            )?;

        let client = Client::builder()
            .timeout(std::time::Duration::from_secs(300)) // 5 minute timeout
            .build()?;

        Ok(Self {
            client,
            api_key,
            base_url: "https://api.anthropic.com".to_string(),
        })
    }

[36m--- ./crates/ccswarm/src/providers/claude_api.rs:test_request_serialization (lines 171-186) ---[0m
    fn test_request_serialization() {
        let request = ClaudeApiRequest {
            model: "claude-3.5-sonnet".to_string(),
            messages: vec![Message {
                role: "user".to_string(),
                content: "Test".to_string(),
            }],
            max_tokens: 1000,
            temperature: Some(0.7),
            system: None,
        };

        let json = serde_json::to_string(&request).unwrap();
        assert!(json.contains("claude-3.5-sonnet"));
        assert!(json.contains("Test"));
    }

  ./crates/ccswarm/src/providers/claude_api.rs:57-73 method new <-> ./crates/ccswarm/src/providers/claude_api.rs:160-168 function test_message_creation
  Similarity: 91.90%

[36m--- ./crates/ccswarm/src/providers/claude_api.rs:new (lines 57-73) ---[0m
    pub fn new(api_key: Option<String>) -> Result<Self> {
        let api_key = api_key
            .or_else(|| env::var("ANTHROPIC_API_KEY").ok())
            .context(
                "ANTHROPIC_API_KEY not found. Please set it in environment or provide in config.",
            )?;

        let client = Client::builder()
            .timeout(std::time::Duration::from_secs(300)) // 5 minute timeout
            .build()?;

        Ok(Self {
            client,
            api_key,
            base_url: "https://api.anthropic.com".to_string(),
        })
    }

[36m--- ./crates/ccswarm/src/providers/claude_api.rs:test_message_creation (lines 160-168) ---[0m
    fn test_message_creation() {
        let message = Message {
            role: "user".to_string(),
            content: "Hello, Claude!".to_string(),
        };

        assert_eq!(message.role, "user");
        assert_eq!(message.content, "Hello, Claude!");
    }

  ./crates/ccswarm/src/providers/claude_api.rs:160-168 function test_message_creation <-> ./crates/ccswarm/src/providers/claude_api.rs:171-186 function test_request_serialization
  Similarity: 78.04%

[36m--- ./crates/ccswarm/src/providers/claude_api.rs:test_message_creation (lines 160-168) ---[0m
    fn test_message_creation() {
        let message = Message {
            role: "user".to_string(),
            content: "Hello, Claude!".to_string(),
        };

        assert_eq!(message.role, "user");
        assert_eq!(message.content, "Hello, Claude!");
    }

[36m--- ./crates/ccswarm/src/providers/claude_api.rs:test_request_serialization (lines 171-186) ---[0m
    fn test_request_serialization() {
        let request = ClaudeApiRequest {
            model: "claude-3.5-sonnet".to_string(),
            messages: vec![Message {
                role: "user".to_string(),
                content: "Test".to_string(),
            }],
            max_tokens: 1000,
            temperature: Some(0.7),
            system: None,
        };

        let json = serde_json::to_string(&request).unwrap();
        assert!(json.contains("claude-3.5-sonnet"));
        assert!(json.contains("Test"));
    }


Duplicates in ./crates/ccswarm/src/extension.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/extension.rs:483-512 method validate <-> ./crates/ccswarm/src/extension.rs:514-546 method auto_fix
  Similarity: 92.89%
  Classes: Validatable <-> Validatable

[36m--- ./crates/ccswarm/src/extension.rs:validate (lines 483-512) ---[0m
    fn validate(&self) -> Result<Self::ValidationResult> {
        let mut issues = Vec::new();

        // Validate name is not empty
        if self.name.trim().is_empty() {
            issues.push("Extension name cannot be empty".to_string());
        }

        // Validate description is not empty
        if self.description.trim().is_empty() {
            issues.push("Extension description cannot be empty".to_string());
        }

        // Validate author is not empty
        if self.author.trim().is_empty() {
            issues.push("Extension author cannot be empty".to_string());
        }

        // Validate capabilities are specified
        if self.capabilities.is_empty() {
            issues.push("Extension must specify at least one capability".to_string());
        }

        // Validate configuration
        if let Err(e) = Self::validate_config(&self.config) {
            issues.push(format!("Configuration validation failed: {}", e));
        }

        Ok(issues)
    }

[36m--- ./crates/ccswarm/src/extension.rs:auto_fix (lines 514-546) ---[0m
    fn auto_fix(&mut self) -> Result<Vec<String>> {
        let mut fixes = Vec::new();

        // Auto-fix empty name
        if self.name.trim().is_empty() {
            self.name = format!("Extension-{}", Uuid::new_v4());
            fixes.push("Generated name for extension".to_string());
        }

        // Auto-fix empty description
        if self.description.trim().is_empty() {
            self.description = "No description provided".to_string();
            fixes.push("Added default description".to_string());
        }

        // Auto-fix empty author
        if self.author.trim().is_empty() {
            self.author = "Unknown".to_string();
            fixes.push("Set default author".to_string());
        }

        // Auto-fix missing capabilities
        if self.capabilities.is_empty() {
            self.capabilities.push(ExtensionCapability::Integration);
            fixes.push("Added default capability".to_string());
        }

        if !fixes.is_empty() {
            self.touch();
        }

        Ok(fixes)
    }

  ./crates/ccswarm/src/extension.rs:514-546 method auto_fix <-> ./crates/ccswarm/src/extension.rs:674-705 function test_dependency_checking
  Similarity: 87.21%

[36m--- ./crates/ccswarm/src/extension.rs:auto_fix (lines 514-546) ---[0m
    fn auto_fix(&mut self) -> Result<Vec<String>> {
        let mut fixes = Vec::new();

        // Auto-fix empty name
        if self.name.trim().is_empty() {
            self.name = format!("Extension-{}", Uuid::new_v4());
            fixes.push("Generated name for extension".to_string());
        }

        // Auto-fix empty description
        if self.description.trim().is_empty() {
            self.description = "No description provided".to_string();
            fixes.push("Added default description".to_string());
        }

        // Auto-fix empty author
        if self.author.trim().is_empty() {
            self.author = "Unknown".to_string();
            fixes.push("Set default author".to_string());
        }

        // Auto-fix missing capabilities
        if self.capabilities.is_empty() {
            self.capabilities.push(ExtensionCapability::Integration);
            fixes.push("Added default capability".to_string());
        }

        if !fixes.is_empty() {
            self.touch();
        }

        Ok(fixes)
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_dependency_checking (lines 674-705) ---[0m
    fn test_dependency_checking() {
        let mut extension = Extension::new(
            "Dependent Extension",
            Version::new(1, 0, 0),
            "Extension with dependencies",
            "Test Author",
            vec![ExtensionCapability::Integration],
        );

        extension.add_dependency(
            "Required Extension".to_string(),
            VersionRequirement::GreaterThanOrEqual(Version::new(1, 0, 0)),
            false,
        );

        // No available extensions - should have missing dependencies
        let missing = extension.check_dependencies(&[]).unwrap();
        assert_eq!(missing.len(), 1);

        // Create the required extension
        let required_ext = Extension::new(
            "Required Extension",
            Version::new(1, 1, 0),
            "A required extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        // Now dependencies should be satisfied
        let missing = extension.check_dependencies(&[required_ext]).unwrap();
        assert!(missing.is_empty());
    }

  ./crates/ccswarm/src/extension.rs:71-101 method from_str <-> ./crates/ccswarm/src/extension.rs:674-705 function test_dependency_checking
  Similarity: 88.58%

[36m--- ./crates/ccswarm/src/extension.rs:from_str (lines 71-101) ---[0m
    fn from_str(s: &str) -> Result<Self> {
        let parts: Vec<&str> = s.split('-').collect();
        let version_part = parts[0];
        let pre_release = if parts.len() > 1 {
            Some(parts[1].to_string())
        } else {
            None
        };

        let version_nums: Vec<&str> = version_part.split('.').collect();
        if version_nums.len() != 3 {
            return Err(CCSwarmError::config(format!("Invalid version format: {}", s)));
        }

        let major = version_nums[0]
            .parse::<u32>()
            .map_err(|_| CCSwarmError::config(format!("Invalid major version: {}", version_nums[0])))?;
        let minor = version_nums[1]
            .parse::<u32>()
            .map_err(|_| CCSwarmError::config(format!("Invalid minor version: {}", version_nums[1])))?;
        let patch = version_nums[2]
            .parse::<u32>()
            .map_err(|_| CCSwarmError::config(format!("Invalid patch version: {}", version_nums[2])))?;

        Ok(Version {
            major,
            minor,
            patch,
            pre_release,
        })
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_dependency_checking (lines 674-705) ---[0m
    fn test_dependency_checking() {
        let mut extension = Extension::new(
            "Dependent Extension",
            Version::new(1, 0, 0),
            "Extension with dependencies",
            "Test Author",
            vec![ExtensionCapability::Integration],
        );

        extension.add_dependency(
            "Required Extension".to_string(),
            VersionRequirement::GreaterThanOrEqual(Version::new(1, 0, 0)),
            false,
        );

        // No available extensions - should have missing dependencies
        let missing = extension.check_dependencies(&[]).unwrap();
        assert_eq!(missing.len(), 1);

        // Create the required extension
        let required_ext = Extension::new(
            "Required Extension",
            Version::new(1, 1, 0),
            "A required extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        // Now dependencies should be satisfied
        let missing = extension.check_dependencies(&[required_ext]).unwrap();
        assert!(missing.is_empty());
    }

  ./crates/ccswarm/src/extension.rs:514-546 method auto_fix <-> ./crates/ccswarm/src/extension.rs:737-767 function test_config_validation
  Similarity: 84.38%

[36m--- ./crates/ccswarm/src/extension.rs:auto_fix (lines 514-546) ---[0m
    fn auto_fix(&mut self) -> Result<Vec<String>> {
        let mut fixes = Vec::new();

        // Auto-fix empty name
        if self.name.trim().is_empty() {
            self.name = format!("Extension-{}", Uuid::new_v4());
            fixes.push("Generated name for extension".to_string());
        }

        // Auto-fix empty description
        if self.description.trim().is_empty() {
            self.description = "No description provided".to_string();
            fixes.push("Added default description".to_string());
        }

        // Auto-fix empty author
        if self.author.trim().is_empty() {
            self.author = "Unknown".to_string();
            fixes.push("Set default author".to_string());
        }

        // Auto-fix missing capabilities
        if self.capabilities.is_empty() {
            self.capabilities.push(ExtensionCapability::Integration);
            fixes.push("Added default capability".to_string());
        }

        if !fixes.is_empty() {
            self.touch();
        }

        Ok(fixes)
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_config_validation (lines 737-767) ---[0m
    fn test_config_validation() {
        let mut config = ExtensionConfig::default();

        // Valid config should pass
        assert!(Extension::validate_config(&config).is_ok());

        // Too many permissions should fail
        config.permissions = (0..15).map(|i| format!("permission_{}", i)).collect();
        assert!(Extension::validate_config(&config).is_err());

        // Reset permissions
        config.permissions.clear();

        // Excessive memory limit should fail
        config.resource_limits = Some(ResourceLimits {
            max_memory: Some(2 * 1024 * 1024 * 1024), // 2GB
            max_cpu_percent: None,
            max_threads: None,
            max_disk_space: None,
        });
        assert!(Extension::validate_config(&config).is_err());

        // Excessive CPU limit should fail
        config.resource_limits = Some(ResourceLimits {
            max_memory: None,
            max_cpu_percent: Some(150.0), // 150%
            max_threads: None,
            max_disk_space: None,
        });
        assert!(Extension::validate_config(&config).is_err());
    }

  ./crates/ccswarm/src/extension.rs:674-705 function test_dependency_checking <-> ./crates/ccswarm/src/extension.rs:737-767 function test_config_validation
  Similarity: 82.78%

[36m--- ./crates/ccswarm/src/extension.rs:test_dependency_checking (lines 674-705) ---[0m
    fn test_dependency_checking() {
        let mut extension = Extension::new(
            "Dependent Extension",
            Version::new(1, 0, 0),
            "Extension with dependencies",
            "Test Author",
            vec![ExtensionCapability::Integration],
        );

        extension.add_dependency(
            "Required Extension".to_string(),
            VersionRequirement::GreaterThanOrEqual(Version::new(1, 0, 0)),
            false,
        );

        // No available extensions - should have missing dependencies
        let missing = extension.check_dependencies(&[]).unwrap();
        assert_eq!(missing.len(), 1);

        // Create the required extension
        let required_ext = Extension::new(
            "Required Extension",
            Version::new(1, 1, 0),
            "A required extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        // Now dependencies should be satisfied
        let missing = extension.check_dependencies(&[required_ext]).unwrap();
        assert!(missing.is_empty());
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_config_validation (lines 737-767) ---[0m
    fn test_config_validation() {
        let mut config = ExtensionConfig::default();

        // Valid config should pass
        assert!(Extension::validate_config(&config).is_ok());

        // Too many permissions should fail
        config.permissions = (0..15).map(|i| format!("permission_{}", i)).collect();
        assert!(Extension::validate_config(&config).is_err());

        // Reset permissions
        config.permissions.clear();

        // Excessive memory limit should fail
        config.resource_limits = Some(ResourceLimits {
            max_memory: Some(2 * 1024 * 1024 * 1024), // 2GB
            max_cpu_percent: None,
            max_threads: None,
            max_disk_space: None,
        });
        assert!(Extension::validate_config(&config).is_err());

        // Excessive CPU limit should fail
        config.resource_limits = Some(ResourceLimits {
            max_memory: None,
            max_cpu_percent: Some(150.0), // 150%
            max_threads: None,
            max_disk_space: None,
        });
        assert!(Extension::validate_config(&config).is_err());
    }

  ./crates/ccswarm/src/extension.rs:483-512 method validate <-> ./crates/ccswarm/src/extension.rs:674-705 function test_dependency_checking
  Similarity: 83.58%

[36m--- ./crates/ccswarm/src/extension.rs:validate (lines 483-512) ---[0m
    fn validate(&self) -> Result<Self::ValidationResult> {
        let mut issues = Vec::new();

        // Validate name is not empty
        if self.name.trim().is_empty() {
            issues.push("Extension name cannot be empty".to_string());
        }

        // Validate description is not empty
        if self.description.trim().is_empty() {
            issues.push("Extension description cannot be empty".to_string());
        }

        // Validate author is not empty
        if self.author.trim().is_empty() {
            issues.push("Extension author cannot be empty".to_string());
        }

        // Validate capabilities are specified
        if self.capabilities.is_empty() {
            issues.push("Extension must specify at least one capability".to_string());
        }

        // Validate configuration
        if let Err(e) = Self::validate_config(&self.config) {
            issues.push(format!("Configuration validation failed: {}", e));
        }

        Ok(issues)
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_dependency_checking (lines 674-705) ---[0m
    fn test_dependency_checking() {
        let mut extension = Extension::new(
            "Dependent Extension",
            Version::new(1, 0, 0),
            "Extension with dependencies",
            "Test Author",
            vec![ExtensionCapability::Integration],
        );

        extension.add_dependency(
            "Required Extension".to_string(),
            VersionRequirement::GreaterThanOrEqual(Version::new(1, 0, 0)),
            false,
        );

        // No available extensions - should have missing dependencies
        let missing = extension.check_dependencies(&[]).unwrap();
        assert_eq!(missing.len(), 1);

        // Create the required extension
        let required_ext = Extension::new(
            "Required Extension",
            Version::new(1, 1, 0),
            "A required extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        // Now dependencies should be satisfied
        let missing = extension.check_dependencies(&[required_ext]).unwrap();
        assert!(missing.is_empty());
    }

  ./crates/ccswarm/src/extension.rs:483-512 method validate <-> ./crates/ccswarm/src/extension.rs:737-767 function test_config_validation
  Similarity: 83.95%

[36m--- ./crates/ccswarm/src/extension.rs:validate (lines 483-512) ---[0m
    fn validate(&self) -> Result<Self::ValidationResult> {
        let mut issues = Vec::new();

        // Validate name is not empty
        if self.name.trim().is_empty() {
            issues.push("Extension name cannot be empty".to_string());
        }

        // Validate description is not empty
        if self.description.trim().is_empty() {
            issues.push("Extension description cannot be empty".to_string());
        }

        // Validate author is not empty
        if self.author.trim().is_empty() {
            issues.push("Extension author cannot be empty".to_string());
        }

        // Validate capabilities are specified
        if self.capabilities.is_empty() {
            issues.push("Extension must specify at least one capability".to_string());
        }

        // Validate configuration
        if let Err(e) = Self::validate_config(&self.config) {
            issues.push(format!("Configuration validation failed: {}", e));
        }

        Ok(issues)
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_config_validation (lines 737-767) ---[0m
    fn test_config_validation() {
        let mut config = ExtensionConfig::default();

        // Valid config should pass
        assert!(Extension::validate_config(&config).is_ok());

        // Too many permissions should fail
        config.permissions = (0..15).map(|i| format!("permission_{}", i)).collect();
        assert!(Extension::validate_config(&config).is_err());

        // Reset permissions
        config.permissions.clear();

        // Excessive memory limit should fail
        config.resource_limits = Some(ResourceLimits {
            max_memory: Some(2 * 1024 * 1024 * 1024), // 2GB
            max_cpu_percent: None,
            max_threads: None,
            max_disk_space: None,
        });
        assert!(Extension::validate_config(&config).is_err());

        // Excessive CPU limit should fail
        config.resource_limits = Some(ResourceLimits {
            max_memory: None,
            max_cpu_percent: Some(150.0), // 150%
            max_threads: None,
            max_disk_space: None,
        });
        assert!(Extension::validate_config(&config).is_err());
    }

  ./crates/ccswarm/src/extension.rs:71-101 method from_str <-> ./crates/ccswarm/src/extension.rs:737-767 function test_config_validation
  Similarity: 81.97%

[36m--- ./crates/ccswarm/src/extension.rs:from_str (lines 71-101) ---[0m
    fn from_str(s: &str) -> Result<Self> {
        let parts: Vec<&str> = s.split('-').collect();
        let version_part = parts[0];
        let pre_release = if parts.len() > 1 {
            Some(parts[1].to_string())
        } else {
            None
        };

        let version_nums: Vec<&str> = version_part.split('.').collect();
        if version_nums.len() != 3 {
            return Err(CCSwarmError::config(format!("Invalid version format: {}", s)));
        }

        let major = version_nums[0]
            .parse::<u32>()
            .map_err(|_| CCSwarmError::config(format!("Invalid major version: {}", version_nums[0])))?;
        let minor = version_nums[1]
            .parse::<u32>()
            .map_err(|_| CCSwarmError::config(format!("Invalid minor version: {}", version_nums[1])))?;
        let patch = version_nums[2]
            .parse::<u32>()
            .map_err(|_| CCSwarmError::config(format!("Invalid patch version: {}", version_nums[2])))?;

        Ok(Version {
            major,
            minor,
            patch,
            pre_release,
        })
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_config_validation (lines 737-767) ---[0m
    fn test_config_validation() {
        let mut config = ExtensionConfig::default();

        // Valid config should pass
        assert!(Extension::validate_config(&config).is_ok());

        // Too many permissions should fail
        config.permissions = (0..15).map(|i| format!("permission_{}", i)).collect();
        assert!(Extension::validate_config(&config).is_err());

        // Reset permissions
        config.permissions.clear();

        // Excessive memory limit should fail
        config.resource_limits = Some(ResourceLimits {
            max_memory: Some(2 * 1024 * 1024 * 1024), // 2GB
            max_cpu_percent: None,
            max_threads: None,
            max_disk_space: None,
        });
        assert!(Extension::validate_config(&config).is_err());

        // Excessive CPU limit should fail
        config.resource_limits = Some(ResourceLimits {
            max_memory: None,
            max_cpu_percent: Some(150.0), // 150%
            max_threads: None,
            max_disk_space: None,
        });
        assert!(Extension::validate_config(&config).is_err());
    }

  ./crates/ccswarm/src/extension.rs:71-101 method from_str <-> ./crates/ccswarm/src/extension.rs:514-546 method auto_fix
  Similarity: 79.13%
  Classes: FromStr <-> Validatable

[36m--- ./crates/ccswarm/src/extension.rs:from_str (lines 71-101) ---[0m
    fn from_str(s: &str) -> Result<Self> {
        let parts: Vec<&str> = s.split('-').collect();
        let version_part = parts[0];
        let pre_release = if parts.len() > 1 {
            Some(parts[1].to_string())
        } else {
            None
        };

        let version_nums: Vec<&str> = version_part.split('.').collect();
        if version_nums.len() != 3 {
            return Err(CCSwarmError::config(format!("Invalid version format: {}", s)));
        }

        let major = version_nums[0]
            .parse::<u32>()
            .map_err(|_| CCSwarmError::config(format!("Invalid major version: {}", version_nums[0])))?;
        let minor = version_nums[1]
            .parse::<u32>()
            .map_err(|_| CCSwarmError::config(format!("Invalid minor version: {}", version_nums[1])))?;
        let patch = version_nums[2]
            .parse::<u32>()
            .map_err(|_| CCSwarmError::config(format!("Invalid patch version: {}", version_nums[2])))?;

        Ok(Version {
            major,
            minor,
            patch,
            pre_release,
        })
    }

[36m--- ./crates/ccswarm/src/extension.rs:auto_fix (lines 514-546) ---[0m
    fn auto_fix(&mut self) -> Result<Vec<String>> {
        let mut fixes = Vec::new();

        // Auto-fix empty name
        if self.name.trim().is_empty() {
            self.name = format!("Extension-{}", Uuid::new_v4());
            fixes.push("Generated name for extension".to_string());
        }

        // Auto-fix empty description
        if self.description.trim().is_empty() {
            self.description = "No description provided".to_string();
            fixes.push("Added default description".to_string());
        }

        // Auto-fix empty author
        if self.author.trim().is_empty() {
            self.author = "Unknown".to_string();
            fixes.push("Set default author".to_string());
        }

        // Auto-fix missing capabilities
        if self.capabilities.is_empty() {
            self.capabilities.push(ExtensionCapability::Integration);
            fixes.push("Added default capability".to_string());
        }

        if !fixes.is_empty() {
            self.touch();
        }

        Ok(fixes)
    }

  ./crates/ccswarm/src/extension.rs:71-101 method from_str <-> ./crates/ccswarm/src/extension.rs:483-512 method validate
  Similarity: 79.89%
  Classes: FromStr <-> Validatable

[36m--- ./crates/ccswarm/src/extension.rs:from_str (lines 71-101) ---[0m
    fn from_str(s: &str) -> Result<Self> {
        let parts: Vec<&str> = s.split('-').collect();
        let version_part = parts[0];
        let pre_release = if parts.len() > 1 {
            Some(parts[1].to_string())
        } else {
            None
        };

        let version_nums: Vec<&str> = version_part.split('.').collect();
        if version_nums.len() != 3 {
            return Err(CCSwarmError::config(format!("Invalid version format: {}", s)));
        }

        let major = version_nums[0]
            .parse::<u32>()
            .map_err(|_| CCSwarmError::config(format!("Invalid major version: {}", version_nums[0])))?;
        let minor = version_nums[1]
            .parse::<u32>()
            .map_err(|_| CCSwarmError::config(format!("Invalid minor version: {}", version_nums[1])))?;
        let patch = version_nums[2]
            .parse::<u32>()
            .map_err(|_| CCSwarmError::config(format!("Invalid patch version: {}", version_nums[2])))?;

        Ok(Version {
            major,
            minor,
            patch,
            pre_release,
        })
    }

[36m--- ./crates/ccswarm/src/extension.rs:validate (lines 483-512) ---[0m
    fn validate(&self) -> Result<Self::ValidationResult> {
        let mut issues = Vec::new();

        // Validate name is not empty
        if self.name.trim().is_empty() {
            issues.push("Extension name cannot be empty".to_string());
        }

        // Validate description is not empty
        if self.description.trim().is_empty() {
            issues.push("Extension description cannot be empty".to_string());
        }

        // Validate author is not empty
        if self.author.trim().is_empty() {
            issues.push("Extension author cannot be empty".to_string());
        }

        // Validate capabilities are specified
        if self.capabilities.is_empty() {
            issues.push("Extension must specify at least one capability".to_string());
        }

        // Validate configuration
        if let Err(e) = Self::validate_config(&self.config) {
            issues.push(format!("Configuration validation failed: {}", e));
        }

        Ok(issues)
    }

  ./crates/ccswarm/src/extension.rs:654-671 function test_extension_validation <-> ./crates/ccswarm/src/extension.rs:674-705 function test_dependency_checking
  Similarity: 85.79%

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_validation (lines 654-671) ---[0m
    fn test_extension_validation() {
        let mut extension = Extension::new(
            "",  // Empty name should be invalid
            Version::new(1, 0, 0),
            "",  // Empty description should be invalid
            "",  // Empty author should be invalid
            vec![], // No capabilities should be invalid
        );

        let issues = extension.validate().unwrap();
        assert!(!issues.is_empty());

        let fixes = extension.auto_fix().unwrap();
        assert!(!fixes.is_empty());

        let issues_after_fix = extension.validate().unwrap();
        assert!(issues_after_fix.is_empty());
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_dependency_checking (lines 674-705) ---[0m
    fn test_dependency_checking() {
        let mut extension = Extension::new(
            "Dependent Extension",
            Version::new(1, 0, 0),
            "Extension with dependencies",
            "Test Author",
            vec![ExtensionCapability::Integration],
        );

        extension.add_dependency(
            "Required Extension".to_string(),
            VersionRequirement::GreaterThanOrEqual(Version::new(1, 0, 0)),
            false,
        );

        // No available extensions - should have missing dependencies
        let missing = extension.check_dependencies(&[]).unwrap();
        assert_eq!(missing.len(), 1);

        // Create the required extension
        let required_ext = Extension::new(
            "Required Extension",
            Version::new(1, 1, 0),
            "A required extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        // Now dependencies should be satisfied
        let missing = extension.check_dependencies(&[required_ext]).unwrap();
        assert!(missing.is_empty());
    }

  ./crates/ccswarm/src/extension.rs:416-437 method validate_config <-> ./crates/ccswarm/src/extension.rs:514-546 method auto_fix
  Similarity: 76.66%
  Classes: Configurable <-> Validatable

[36m--- ./crates/ccswarm/src/extension.rs:validate_config (lines 416-437) ---[0m
    fn validate_config(config: &Self::Config) -> Result<()> {
        // Validate permissions are not excessive
        if config.permissions.len() > 10 {
            return Err(CCSwarmError::config("Too many permissions requested"));
        }

        // Validate resource limits are reasonable
        if let Some(ref limits) = config.resource_limits {
            if let Some(max_memory) = limits.max_memory {
                if max_memory > 1024 * 1024 * 1024 { // 1GB
                    return Err(CCSwarmError::config("Memory limit too high"));
                }
            }
            if let Some(max_cpu) = limits.max_cpu_percent {
                if max_cpu > 100.0 {
                    return Err(CCSwarmError::config("CPU limit cannot exceed 100%"));
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension.rs:auto_fix (lines 514-546) ---[0m
    fn auto_fix(&mut self) -> Result<Vec<String>> {
        let mut fixes = Vec::new();

        // Auto-fix empty name
        if self.name.trim().is_empty() {
            self.name = format!("Extension-{}", Uuid::new_v4());
            fixes.push("Generated name for extension".to_string());
        }

        // Auto-fix empty description
        if self.description.trim().is_empty() {
            self.description = "No description provided".to_string();
            fixes.push("Added default description".to_string());
        }

        // Auto-fix empty author
        if self.author.trim().is_empty() {
            self.author = "Unknown".to_string();
            fixes.push("Set default author".to_string());
        }

        // Auto-fix missing capabilities
        if self.capabilities.is_empty() {
            self.capabilities.push(ExtensionCapability::Integration);
            fixes.push("Added default capability".to_string());
        }

        if !fixes.is_empty() {
            self.touch();
        }

        Ok(fixes)
    }

  ./crates/ccswarm/src/extension.rs:279-305 method new <-> ./crates/ccswarm/src/extension.rs:416-437 method validate_config
  Similarity: 81.12%
  Classes: Extension <-> Configurable

[36m--- ./crates/ccswarm/src/extension.rs:new (lines 279-305) ---[0m
    pub fn new<S: Into<String>>(
        name: S,
        version: Version,
        description: S,
        author: S,
        capabilities: Vec<ExtensionCapability>,
    ) -> Self {
        let name = name.into();
        let now = chrono::Utc::now();

        Self {
            id: format!("{}-{}", name.to_lowercase().replace(' ', "-"), Uuid::new_v4()),
            name,
            version,
            description: description.into(),
            author: author.into(),
            capabilities,
            dependencies: Vec::new(),
            config: ExtensionConfig::default(),
            state: ExtensionState::Installed,
            created_at: now,
            updated_at: now,
            metadata: HashMap::new(),
            entry_point: None,
            manifest_path: None,
        }
    }

[36m--- ./crates/ccswarm/src/extension.rs:validate_config (lines 416-437) ---[0m
    fn validate_config(config: &Self::Config) -> Result<()> {
        // Validate permissions are not excessive
        if config.permissions.len() > 10 {
            return Err(CCSwarmError::config("Too many permissions requested"));
        }

        // Validate resource limits are reasonable
        if let Some(ref limits) = config.resource_limits {
            if let Some(max_memory) = limits.max_memory {
                if max_memory > 1024 * 1024 * 1024 { // 1GB
                    return Err(CCSwarmError::config("Memory limit too high"));
                }
            }
            if let Some(max_cpu) = limits.max_cpu_percent {
                if max_cpu > 100.0 {
                    return Err(CCSwarmError::config("CPU limit cannot exceed 100%"));
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/extension.rs:416-437 method validate_config <-> ./crates/ccswarm/src/extension.rs:483-512 method validate
  Similarity: 72.17%
  Classes: Configurable <-> Validatable

[36m--- ./crates/ccswarm/src/extension.rs:validate_config (lines 416-437) ---[0m
    fn validate_config(config: &Self::Config) -> Result<()> {
        // Validate permissions are not excessive
        if config.permissions.len() > 10 {
            return Err(CCSwarmError::config("Too many permissions requested"));
        }

        // Validate resource limits are reasonable
        if let Some(ref limits) = config.resource_limits {
            if let Some(max_memory) = limits.max_memory {
                if max_memory > 1024 * 1024 * 1024 { // 1GB
                    return Err(CCSwarmError::config("Memory limit too high"));
                }
            }
            if let Some(max_cpu) = limits.max_cpu_percent {
                if max_cpu > 100.0 {
                    return Err(CCSwarmError::config("CPU limit cannot exceed 100%"));
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension.rs:validate (lines 483-512) ---[0m
    fn validate(&self) -> Result<Self::ValidationResult> {
        let mut issues = Vec::new();

        // Validate name is not empty
        if self.name.trim().is_empty() {
            issues.push("Extension name cannot be empty".to_string());
        }

        // Validate description is not empty
        if self.description.trim().is_empty() {
            issues.push("Extension description cannot be empty".to_string());
        }

        // Validate author is not empty
        if self.author.trim().is_empty() {
            issues.push("Extension author cannot be empty".to_string());
        }

        // Validate capabilities are specified
        if self.capabilities.is_empty() {
            issues.push("Extension must specify at least one capability".to_string());
        }

        // Validate configuration
        if let Err(e) = Self::validate_config(&self.config) {
            issues.push(format!("Configuration validation failed: {}", e));
        }

        Ok(issues)
    }

  ./crates/ccswarm/src/extension.rs:624-635 function test_version_requirements <-> ./crates/ccswarm/src/extension.rs:674-705 function test_dependency_checking
  Similarity: 85.04%

[36m--- ./crates/ccswarm/src/extension.rs:test_version_requirements (lines 624-635) ---[0m
    fn test_version_requirements() {
        let v1_0_0 = Version::new(1, 0, 0);
        let v1_1_0 = Version::new(1, 1, 0);

        let req = VersionRequirement::GreaterThanOrEqual(v1_0_0.clone());
        assert!(req.satisfies(&v1_1_0));
        assert!(req.satisfies(&v1_0_0));

        let req = VersionRequirement::Compatible(v1_0_0.clone());
        assert!(req.satisfies(&v1_1_0));
        assert!(req.satisfies(&v1_0_0));
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_dependency_checking (lines 674-705) ---[0m
    fn test_dependency_checking() {
        let mut extension = Extension::new(
            "Dependent Extension",
            Version::new(1, 0, 0),
            "Extension with dependencies",
            "Test Author",
            vec![ExtensionCapability::Integration],
        );

        extension.add_dependency(
            "Required Extension".to_string(),
            VersionRequirement::GreaterThanOrEqual(Version::new(1, 0, 0)),
            false,
        );

        // No available extensions - should have missing dependencies
        let missing = extension.check_dependencies(&[]).unwrap();
        assert_eq!(missing.len(), 1);

        // Create the required extension
        let required_ext = Extension::new(
            "Required Extension",
            Version::new(1, 1, 0),
            "A required extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        // Now dependencies should be satisfied
        let missing = extension.check_dependencies(&[required_ext]).unwrap();
        assert!(missing.is_empty());
    }

  ./crates/ccswarm/src/extension.rs:279-305 method new <-> ./crates/ccswarm/src/extension.rs:708-719 function test_extension_health_check
  Similarity: 94.33%

[36m--- ./crates/ccswarm/src/extension.rs:new (lines 279-305) ---[0m
    pub fn new<S: Into<String>>(
        name: S,
        version: Version,
        description: S,
        author: S,
        capabilities: Vec<ExtensionCapability>,
    ) -> Self {
        let name = name.into();
        let now = chrono::Utc::now();

        Self {
            id: format!("{}-{}", name.to_lowercase().replace(' ', "-"), Uuid::new_v4()),
            name,
            version,
            description: description.into(),
            author: author.into(),
            capabilities,
            dependencies: Vec::new(),
            config: ExtensionConfig::default(),
            state: ExtensionState::Installed,
            created_at: now,
            updated_at: now,
            metadata: HashMap::new(),
            entry_point: None,
            manifest_path: None,
        }
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_health_check (lines 708-719) ---[0m
    async fn test_extension_health_check() {
        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Monitoring],
        );

        let health = extension.health_check().await.unwrap();
        assert!(matches!(health.status, HealthStatus::Unknown));
    }

  ./crates/ccswarm/src/extension.rs:600-610 function test_version_parsing <-> ./crates/ccswarm/src/extension.rs:674-705 function test_dependency_checking
  Similarity: 85.17%

[36m--- ./crates/ccswarm/src/extension.rs:test_version_parsing (lines 600-610) ---[0m
    fn test_version_parsing() {
        let version = "1.2.3".parse::<Version>().unwrap();
        assert_eq!(version.major, 1);
        assert_eq!(version.minor, 2);
        assert_eq!(version.patch, 3);
        assert!(version.is_stable());

        let pre_version = "1.2.3-beta".parse::<Version>().unwrap();
        assert_eq!(pre_version.pre_release, Some("beta".to_string()));
        assert!(!pre_version.is_stable());
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_dependency_checking (lines 674-705) ---[0m
    fn test_dependency_checking() {
        let mut extension = Extension::new(
            "Dependent Extension",
            Version::new(1, 0, 0),
            "Extension with dependencies",
            "Test Author",
            vec![ExtensionCapability::Integration],
        );

        extension.add_dependency(
            "Required Extension".to_string(),
            VersionRequirement::GreaterThanOrEqual(Version::new(1, 0, 0)),
            false,
        );

        // No available extensions - should have missing dependencies
        let missing = extension.check_dependencies(&[]).unwrap();
        assert_eq!(missing.len(), 1);

        // Create the required extension
        let required_ext = Extension::new(
            "Required Extension",
            Version::new(1, 1, 0),
            "A required extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        // Now dependencies should be satisfied
        let missing = extension.check_dependencies(&[required_ext]).unwrap();
        assert!(missing.is_empty());
    }

  ./crates/ccswarm/src/extension.rs:654-671 function test_extension_validation <-> ./crates/ccswarm/src/extension.rs:737-767 function test_config_validation
  Similarity: 73.12%

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_validation (lines 654-671) ---[0m
    fn test_extension_validation() {
        let mut extension = Extension::new(
            "",  // Empty name should be invalid
            Version::new(1, 0, 0),
            "",  // Empty description should be invalid
            "",  // Empty author should be invalid
            vec![], // No capabilities should be invalid
        );

        let issues = extension.validate().unwrap();
        assert!(!issues.is_empty());

        let fixes = extension.auto_fix().unwrap();
        assert!(!fixes.is_empty());

        let issues_after_fix = extension.validate().unwrap();
        assert!(issues_after_fix.is_empty());
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_config_validation (lines 737-767) ---[0m
    fn test_config_validation() {
        let mut config = ExtensionConfig::default();

        // Valid config should pass
        assert!(Extension::validate_config(&config).is_ok());

        // Too many permissions should fail
        config.permissions = (0..15).map(|i| format!("permission_{}", i)).collect();
        assert!(Extension::validate_config(&config).is_err());

        // Reset permissions
        config.permissions.clear();

        // Excessive memory limit should fail
        config.resource_limits = Some(ResourceLimits {
            max_memory: Some(2 * 1024 * 1024 * 1024), // 2GB
            max_cpu_percent: None,
            max_threads: None,
            max_disk_space: None,
        });
        assert!(Extension::validate_config(&config).is_err());

        // Excessive CPU limit should fail
        config.resource_limits = Some(ResourceLimits {
            max_memory: None,
            max_cpu_percent: Some(150.0), // 150%
            max_threads: None,
            max_disk_space: None,
        });
        assert!(Extension::validate_config(&config).is_err());
    }

  ./crates/ccswarm/src/extension.rs:330-352 method check_dependencies <-> ./crates/ccswarm/src/extension.rs:416-437 method validate_config
  Similarity: 78.37%
  Classes: Extension <-> Configurable

[36m--- ./crates/ccswarm/src/extension.rs:check_dependencies (lines 330-352) ---[0m
    pub fn check_dependencies(&self, available_extensions: &[Extension]) -> Result<Vec<String>> {
        let mut missing_deps = Vec::new();

        for dep in &self.dependencies {
            if dep.optional {
                continue;
            }

            let satisfied = available_extensions.iter().any(|ext| {
                ext.name == dep.name && dep.version_requirement.satisfies(&ext.version)
            });

            if !satisfied {
                missing_deps.push(dep.name.clone());
            }
        }

        if missing_deps.is_empty() {
            Ok(Vec::new())
        } else {
            Ok(missing_deps)
        }
    }

[36m--- ./crates/ccswarm/src/extension.rs:validate_config (lines 416-437) ---[0m
    fn validate_config(config: &Self::Config) -> Result<()> {
        // Validate permissions are not excessive
        if config.permissions.len() > 10 {
            return Err(CCSwarmError::config("Too many permissions requested"));
        }

        // Validate resource limits are reasonable
        if let Some(ref limits) = config.resource_limits {
            if let Some(max_memory) = limits.max_memory {
                if max_memory > 1024 * 1024 * 1024 { // 1GB
                    return Err(CCSwarmError::config("Memory limit too high"));
                }
            }
            if let Some(max_cpu) = limits.max_cpu_percent {
                if max_cpu > 100.0 {
                    return Err(CCSwarmError::config("CPU limit cannot exceed 100%"));
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/extension.rs:279-305 method new <-> ./crates/ccswarm/src/extension.rs:722-734 function test_extension_metrics
  Similarity: 86.73%

[36m--- ./crates/ccswarm/src/extension.rs:new (lines 279-305) ---[0m
    pub fn new<S: Into<String>>(
        name: S,
        version: Version,
        description: S,
        author: S,
        capabilities: Vec<ExtensionCapability>,
    ) -> Self {
        let name = name.into();
        let now = chrono::Utc::now();

        Self {
            id: format!("{}-{}", name.to_lowercase().replace(' ', "-"), Uuid::new_v4()),
            name,
            version,
            description: description.into(),
            author: author.into(),
            capabilities,
            dependencies: Vec::new(),
            config: ExtensionConfig::default(),
            state: ExtensionState::Installed,
            created_at: now,
            updated_at: now,
            metadata: HashMap::new(),
            entry_point: None,
            manifest_path: None,
        }
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_metrics (lines 722-734) ---[0m
    async fn test_extension_metrics() {
        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Performance],
        );

        let metrics = extension.metrics().await.unwrap();
        assert_eq!(metrics.memory_usage, 0);
        assert_eq!(metrics.cpu_usage, 0.0);
    }

  ./crates/ccswarm/src/extension.rs:638-651 function test_extension_creation <-> ./crates/ccswarm/src/extension.rs:674-705 function test_dependency_checking
  Similarity: 74.29%

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_creation (lines 638-651) ---[0m
    fn test_extension_creation() {
        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        assert_eq!(extension.name, "Test Extension");
        assert_eq!(extension.version, Version::new(1, 0, 0));
        assert!(extension.has_capability(&ExtensionCapability::Command));
        assert!(!extension.has_capability(&ExtensionCapability::Security));
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_dependency_checking (lines 674-705) ---[0m
    fn test_dependency_checking() {
        let mut extension = Extension::new(
            "Dependent Extension",
            Version::new(1, 0, 0),
            "Extension with dependencies",
            "Test Author",
            vec![ExtensionCapability::Integration],
        );

        extension.add_dependency(
            "Required Extension".to_string(),
            VersionRequirement::GreaterThanOrEqual(Version::new(1, 0, 0)),
            false,
        );

        // No available extensions - should have missing dependencies
        let missing = extension.check_dependencies(&[]).unwrap();
        assert_eq!(missing.len(), 1);

        // Create the required extension
        let required_ext = Extension::new(
            "Required Extension",
            Version::new(1, 1, 0),
            "A required extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        // Now dependencies should be satisfied
        let missing = extension.check_dependencies(&[required_ext]).unwrap();
        assert!(missing.is_empty());
    }

  ./crates/ccswarm/src/extension.rs:483-512 method validate <-> ./crates/ccswarm/src/extension.rs:654-671 function test_extension_validation
  Similarity: 70.31%

[36m--- ./crates/ccswarm/src/extension.rs:validate (lines 483-512) ---[0m
    fn validate(&self) -> Result<Self::ValidationResult> {
        let mut issues = Vec::new();

        // Validate name is not empty
        if self.name.trim().is_empty() {
            issues.push("Extension name cannot be empty".to_string());
        }

        // Validate description is not empty
        if self.description.trim().is_empty() {
            issues.push("Extension description cannot be empty".to_string());
        }

        // Validate author is not empty
        if self.author.trim().is_empty() {
            issues.push("Extension author cannot be empty".to_string());
        }

        // Validate capabilities are specified
        if self.capabilities.is_empty() {
            issues.push("Extension must specify at least one capability".to_string());
        }

        // Validate configuration
        if let Err(e) = Self::validate_config(&self.config) {
            issues.push(format!("Configuration validation failed: {}", e));
        }

        Ok(issues)
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_validation (lines 654-671) ---[0m
    fn test_extension_validation() {
        let mut extension = Extension::new(
            "",  // Empty name should be invalid
            Version::new(1, 0, 0),
            "",  // Empty description should be invalid
            "",  // Empty author should be invalid
            vec![], // No capabilities should be invalid
        );

        let issues = extension.validate().unwrap();
        assert!(!issues.is_empty());

        let fixes = extension.auto_fix().unwrap();
        assert!(!fixes.is_empty());

        let issues_after_fix = extension.validate().unwrap();
        assert!(issues_after_fix.is_empty());
    }

  ./crates/ccswarm/src/extension.rs:624-635 function test_version_requirements <-> ./crates/ccswarm/src/extension.rs:737-767 function test_config_validation
  Similarity: 73.88%

[36m--- ./crates/ccswarm/src/extension.rs:test_version_requirements (lines 624-635) ---[0m
    fn test_version_requirements() {
        let v1_0_0 = Version::new(1, 0, 0);
        let v1_1_0 = Version::new(1, 1, 0);

        let req = VersionRequirement::GreaterThanOrEqual(v1_0_0.clone());
        assert!(req.satisfies(&v1_1_0));
        assert!(req.satisfies(&v1_0_0));

        let req = VersionRequirement::Compatible(v1_0_0.clone());
        assert!(req.satisfies(&v1_1_0));
        assert!(req.satisfies(&v1_0_0));
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_config_validation (lines 737-767) ---[0m
    fn test_config_validation() {
        let mut config = ExtensionConfig::default();

        // Valid config should pass
        assert!(Extension::validate_config(&config).is_ok());

        // Too many permissions should fail
        config.permissions = (0..15).map(|i| format!("permission_{}", i)).collect();
        assert!(Extension::validate_config(&config).is_err());

        // Reset permissions
        config.permissions.clear();

        // Excessive memory limit should fail
        config.resource_limits = Some(ResourceLimits {
            max_memory: Some(2 * 1024 * 1024 * 1024), // 2GB
            max_cpu_percent: None,
            max_threads: None,
            max_disk_space: None,
        });
        assert!(Extension::validate_config(&config).is_err());

        // Excessive CPU limit should fail
        config.resource_limits = Some(ResourceLimits {
            max_memory: None,
            max_cpu_percent: Some(150.0), // 150%
            max_threads: None,
            max_disk_space: None,
        });
        assert!(Extension::validate_config(&config).is_err());
    }

  ./crates/ccswarm/src/extension.rs:483-512 method validate <-> ./crates/ccswarm/src/extension.rs:600-610 function test_version_parsing
  Similarity: 76.65%

[36m--- ./crates/ccswarm/src/extension.rs:validate (lines 483-512) ---[0m
    fn validate(&self) -> Result<Self::ValidationResult> {
        let mut issues = Vec::new();

        // Validate name is not empty
        if self.name.trim().is_empty() {
            issues.push("Extension name cannot be empty".to_string());
        }

        // Validate description is not empty
        if self.description.trim().is_empty() {
            issues.push("Extension description cannot be empty".to_string());
        }

        // Validate author is not empty
        if self.author.trim().is_empty() {
            issues.push("Extension author cannot be empty".to_string());
        }

        // Validate capabilities are specified
        if self.capabilities.is_empty() {
            issues.push("Extension must specify at least one capability".to_string());
        }

        // Validate configuration
        if let Err(e) = Self::validate_config(&self.config) {
            issues.push(format!("Configuration validation failed: {}", e));
        }

        Ok(issues)
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_version_parsing (lines 600-610) ---[0m
    fn test_version_parsing() {
        let version = "1.2.3".parse::<Version>().unwrap();
        assert_eq!(version.major, 1);
        assert_eq!(version.minor, 2);
        assert_eq!(version.patch, 3);
        assert!(version.is_stable());

        let pre_version = "1.2.3-beta".parse::<Version>().unwrap();
        assert_eq!(pre_version.pre_release, Some("beta".to_string()));
        assert!(!pre_version.is_stable());
    }

  ./crates/ccswarm/src/extension.rs:416-437 method validate_config <-> ./crates/ccswarm/src/extension.rs:638-651 function test_extension_creation
  Similarity: 86.69%

[36m--- ./crates/ccswarm/src/extension.rs:validate_config (lines 416-437) ---[0m
    fn validate_config(config: &Self::Config) -> Result<()> {
        // Validate permissions are not excessive
        if config.permissions.len() > 10 {
            return Err(CCSwarmError::config("Too many permissions requested"));
        }

        // Validate resource limits are reasonable
        if let Some(ref limits) = config.resource_limits {
            if let Some(max_memory) = limits.max_memory {
                if max_memory > 1024 * 1024 * 1024 { // 1GB
                    return Err(CCSwarmError::config("Memory limit too high"));
                }
            }
            if let Some(max_cpu) = limits.max_cpu_percent {
                if max_cpu > 100.0 {
                    return Err(CCSwarmError::config("CPU limit cannot exceed 100%"));
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_creation (lines 638-651) ---[0m
    fn test_extension_creation() {
        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        assert_eq!(extension.name, "Test Extension");
        assert_eq!(extension.version, Version::new(1, 0, 0));
        assert!(extension.has_capability(&ExtensionCapability::Command));
        assert!(!extension.has_capability(&ExtensionCapability::Security));
    }

  ./crates/ccswarm/src/extension.rs:483-512 method validate <-> ./crates/ccswarm/src/extension.rs:624-635 function test_version_requirements
  Similarity: 74.17%

[36m--- ./crates/ccswarm/src/extension.rs:validate (lines 483-512) ---[0m
    fn validate(&self) -> Result<Self::ValidationResult> {
        let mut issues = Vec::new();

        // Validate name is not empty
        if self.name.trim().is_empty() {
            issues.push("Extension name cannot be empty".to_string());
        }

        // Validate description is not empty
        if self.description.trim().is_empty() {
            issues.push("Extension description cannot be empty".to_string());
        }

        // Validate author is not empty
        if self.author.trim().is_empty() {
            issues.push("Extension author cannot be empty".to_string());
        }

        // Validate capabilities are specified
        if self.capabilities.is_empty() {
            issues.push("Extension must specify at least one capability".to_string());
        }

        // Validate configuration
        if let Err(e) = Self::validate_config(&self.config) {
            issues.push(format!("Configuration validation failed: {}", e));
        }

        Ok(issues)
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_version_requirements (lines 624-635) ---[0m
    fn test_version_requirements() {
        let v1_0_0 = Version::new(1, 0, 0);
        let v1_1_0 = Version::new(1, 1, 0);

        let req = VersionRequirement::GreaterThanOrEqual(v1_0_0.clone());
        assert!(req.satisfies(&v1_1_0));
        assert!(req.satisfies(&v1_0_0));

        let req = VersionRequirement::Compatible(v1_0_0.clone());
        assert!(req.satisfies(&v1_1_0));
        assert!(req.satisfies(&v1_0_0));
    }

  ./crates/ccswarm/src/extension.rs:600-610 function test_version_parsing <-> ./crates/ccswarm/src/extension.rs:737-767 function test_config_validation
  Similarity: 72.55%

[36m--- ./crates/ccswarm/src/extension.rs:test_version_parsing (lines 600-610) ---[0m
    fn test_version_parsing() {
        let version = "1.2.3".parse::<Version>().unwrap();
        assert_eq!(version.major, 1);
        assert_eq!(version.minor, 2);
        assert_eq!(version.patch, 3);
        assert!(version.is_stable());

        let pre_version = "1.2.3-beta".parse::<Version>().unwrap();
        assert_eq!(pre_version.pre_release, Some("beta".to_string()));
        assert!(!pre_version.is_stable());
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_config_validation (lines 737-767) ---[0m
    fn test_config_validation() {
        let mut config = ExtensionConfig::default();

        // Valid config should pass
        assert!(Extension::validate_config(&config).is_ok());

        // Too many permissions should fail
        config.permissions = (0..15).map(|i| format!("permission_{}", i)).collect();
        assert!(Extension::validate_config(&config).is_err());

        // Reset permissions
        config.permissions.clear();

        // Excessive memory limit should fail
        config.resource_limits = Some(ResourceLimits {
            max_memory: Some(2 * 1024 * 1024 * 1024), // 2GB
            max_cpu_percent: None,
            max_threads: None,
            max_disk_space: None,
        });
        assert!(Extension::validate_config(&config).is_err());

        // Excessive CPU limit should fail
        config.resource_limits = Some(ResourceLimits {
            max_memory: None,
            max_cpu_percent: Some(150.0), // 150%
            max_threads: None,
            max_disk_space: None,
        });
        assert!(Extension::validate_config(&config).is_err());
    }

  ./crates/ccswarm/src/extension.rs:279-305 method new <-> ./crates/ccswarm/src/extension.rs:638-651 function test_extension_creation
  Similarity: 73.16%

[36m--- ./crates/ccswarm/src/extension.rs:new (lines 279-305) ---[0m
    pub fn new<S: Into<String>>(
        name: S,
        version: Version,
        description: S,
        author: S,
        capabilities: Vec<ExtensionCapability>,
    ) -> Self {
        let name = name.into();
        let now = chrono::Utc::now();

        Self {
            id: format!("{}-{}", name.to_lowercase().replace(' ', "-"), Uuid::new_v4()),
            name,
            version,
            description: description.into(),
            author: author.into(),
            capabilities,
            dependencies: Vec::new(),
            config: ExtensionConfig::default(),
            state: ExtensionState::Installed,
            created_at: now,
            updated_at: now,
            metadata: HashMap::new(),
            entry_point: None,
            manifest_path: None,
        }
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_creation (lines 638-651) ---[0m
    fn test_extension_creation() {
        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        assert_eq!(extension.name, "Test Extension");
        assert_eq!(extension.version, Version::new(1, 0, 0));
        assert!(extension.has_capability(&ExtensionCapability::Command));
        assert!(!extension.has_capability(&ExtensionCapability::Security));
    }

  ./crates/ccswarm/src/extension.rs:330-352 method check_dependencies <-> ./crates/ccswarm/src/extension.rs:708-719 function test_extension_health_check
  Similarity: 83.87%

[36m--- ./crates/ccswarm/src/extension.rs:check_dependencies (lines 330-352) ---[0m
    pub fn check_dependencies(&self, available_extensions: &[Extension]) -> Result<Vec<String>> {
        let mut missing_deps = Vec::new();

        for dep in &self.dependencies {
            if dep.optional {
                continue;
            }

            let satisfied = available_extensions.iter().any(|ext| {
                ext.name == dep.name && dep.version_requirement.satisfies(&ext.version)
            });

            if !satisfied {
                missing_deps.push(dep.name.clone());
            }
        }

        if missing_deps.is_empty() {
            Ok(Vec::new())
        } else {
            Ok(missing_deps)
        }
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_health_check (lines 708-719) ---[0m
    async fn test_extension_health_check() {
        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Monitoring],
        );

        let health = extension.health_check().await.unwrap();
        assert!(matches!(health.status, HealthStatus::Unknown));
    }

  ./crates/ccswarm/src/extension.rs:330-352 method check_dependencies <-> ./crates/ccswarm/src/extension.rs:467-477 method metrics
  Similarity: 86.19%
  Classes: Extension <-> Monitorable

[36m--- ./crates/ccswarm/src/extension.rs:check_dependencies (lines 330-352) ---[0m
    pub fn check_dependencies(&self, available_extensions: &[Extension]) -> Result<Vec<String>> {
        let mut missing_deps = Vec::new();

        for dep in &self.dependencies {
            if dep.optional {
                continue;
            }

            let satisfied = available_extensions.iter().any(|ext| {
                ext.name == dep.name && dep.version_requirement.satisfies(&ext.version)
            });

            if !satisfied {
                missing_deps.push(dep.name.clone());
            }
        }

        if missing_deps.is_empty() {
            Ok(Vec::new())
        } else {
            Ok(missing_deps)
        }
    }

[36m--- ./crates/ccswarm/src/extension.rs:metrics (lines 467-477) ---[0m
    async fn metrics(&self) -> Result<Self::Metrics> {
        // In a real implementation, these would be collected from the running extension
        Ok(ExtensionMetrics {
            memory_usage: 0,
            cpu_usage: 0.0,
            requests_handled: 0,
            errors_count: 0,
            average_response_time: std::time::Duration::from_millis(0),
            uptime: chrono::Utc::now().signed_duration_since(self.created_at).to_std().unwrap_or_default(),
        })
    }

  ./crates/ccswarm/src/extension.rs:416-437 method validate_config <-> ./crates/ccswarm/src/extension.rs:722-734 function test_extension_metrics
  Similarity: 83.65%

[36m--- ./crates/ccswarm/src/extension.rs:validate_config (lines 416-437) ---[0m
    fn validate_config(config: &Self::Config) -> Result<()> {
        // Validate permissions are not excessive
        if config.permissions.len() > 10 {
            return Err(CCSwarmError::config("Too many permissions requested"));
        }

        // Validate resource limits are reasonable
        if let Some(ref limits) = config.resource_limits {
            if let Some(max_memory) = limits.max_memory {
                if max_memory > 1024 * 1024 * 1024 { // 1GB
                    return Err(CCSwarmError::config("Memory limit too high"));
                }
            }
            if let Some(max_cpu) = limits.max_cpu_percent {
                if max_cpu > 100.0 {
                    return Err(CCSwarmError::config("CPU limit cannot exceed 100%"));
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_metrics (lines 722-734) ---[0m
    async fn test_extension_metrics() {
        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Performance],
        );

        let metrics = extension.metrics().await.unwrap();
        assert_eq!(metrics.memory_usage, 0);
        assert_eq!(metrics.cpu_usage, 0.0);
    }

  ./crates/ccswarm/src/extension.rs:330-352 method check_dependencies <-> ./crates/ccswarm/src/extension.rs:449-465 method health_check
  Similarity: 72.08%
  Classes: Extension <-> Monitorable

[36m--- ./crates/ccswarm/src/extension.rs:check_dependencies (lines 330-352) ---[0m
    pub fn check_dependencies(&self, available_extensions: &[Extension]) -> Result<Vec<String>> {
        let mut missing_deps = Vec::new();

        for dep in &self.dependencies {
            if dep.optional {
                continue;
            }

            let satisfied = available_extensions.iter().any(|ext| {
                ext.name == dep.name && dep.version_requirement.satisfies(&ext.version)
            });

            if !satisfied {
                missing_deps.push(dep.name.clone());
            }
        }

        if missing_deps.is_empty() {
            Ok(Vec::new())
        } else {
            Ok(missing_deps)
        }
    }

[36m--- ./crates/ccswarm/src/extension.rs:health_check (lines 449-465) ---[0m
    async fn health_check(&self) -> Result<Self::HealthStatus> {
        let status = match &self.state {
            ExtensionState::Active => HealthStatus::Healthy,
            ExtensionState::Loaded => HealthStatus::Healthy,
            ExtensionState::Paused => HealthStatus::Degraded("Extension is paused".to_string()),
            ExtensionState::Error(e) => HealthStatus::Unhealthy(e.clone()),
            _ => HealthStatus::Unknown,
        };

        Ok(ExtensionHealth {
            status,
            last_check: chrono::Utc::now(),
            uptime: chrono::Utc::now().signed_duration_since(self.created_at).to_std().unwrap_or_default(),
            error_count: 0, // Would be tracked by extension runtime
            performance_score: 1.0, // Would be calculated based on metrics
        })
    }

  ./crates/ccswarm/src/extension.rs:416-437 method validate_config <-> ./crates/ccswarm/src/extension.rs:467-477 method metrics
  Similarity: 86.80%
  Classes: Configurable <-> Monitorable

[36m--- ./crates/ccswarm/src/extension.rs:validate_config (lines 416-437) ---[0m
    fn validate_config(config: &Self::Config) -> Result<()> {
        // Validate permissions are not excessive
        if config.permissions.len() > 10 {
            return Err(CCSwarmError::config("Too many permissions requested"));
        }

        // Validate resource limits are reasonable
        if let Some(ref limits) = config.resource_limits {
            if let Some(max_memory) = limits.max_memory {
                if max_memory > 1024 * 1024 * 1024 { // 1GB
                    return Err(CCSwarmError::config("Memory limit too high"));
                }
            }
            if let Some(max_cpu) = limits.max_cpu_percent {
                if max_cpu > 100.0 {
                    return Err(CCSwarmError::config("CPU limit cannot exceed 100%"));
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension.rs:metrics (lines 467-477) ---[0m
    async fn metrics(&self) -> Result<Self::Metrics> {
        // In a real implementation, these would be collected from the running extension
        Ok(ExtensionMetrics {
            memory_usage: 0,
            cpu_usage: 0.0,
            requests_handled: 0,
            errors_count: 0,
            average_response_time: std::time::Duration::from_millis(0),
            uptime: chrono::Utc::now().signed_duration_since(self.created_at).to_std().unwrap_or_default(),
        })
    }

  ./crates/ccswarm/src/extension.rs:416-437 method validate_config <-> ./crates/ccswarm/src/extension.rs:708-719 function test_extension_health_check
  Similarity: 81.18%

[36m--- ./crates/ccswarm/src/extension.rs:validate_config (lines 416-437) ---[0m
    fn validate_config(config: &Self::Config) -> Result<()> {
        // Validate permissions are not excessive
        if config.permissions.len() > 10 {
            return Err(CCSwarmError::config("Too many permissions requested"));
        }

        // Validate resource limits are reasonable
        if let Some(ref limits) = config.resource_limits {
            if let Some(max_memory) = limits.max_memory {
                if max_memory > 1024 * 1024 * 1024 { // 1GB
                    return Err(CCSwarmError::config("Memory limit too high"));
                }
            }
            if let Some(max_cpu) = limits.max_cpu_percent {
                if max_cpu > 100.0 {
                    return Err(CCSwarmError::config("CPU limit cannot exceed 100%"));
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_health_check (lines 708-719) ---[0m
    async fn test_extension_health_check() {
        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Monitoring],
        );

        let health = extension.health_check().await.unwrap();
        assert!(matches!(health.status, HealthStatus::Unknown));
    }

  ./crates/ccswarm/src/extension.rs:416-437 method validate_config <-> ./crates/ccswarm/src/extension.rs:449-465 method health_check
  Similarity: 70.27%
  Classes: Configurable <-> Monitorable

[36m--- ./crates/ccswarm/src/extension.rs:validate_config (lines 416-437) ---[0m
    fn validate_config(config: &Self::Config) -> Result<()> {
        // Validate permissions are not excessive
        if config.permissions.len() > 10 {
            return Err(CCSwarmError::config("Too many permissions requested"));
        }

        // Validate resource limits are reasonable
        if let Some(ref limits) = config.resource_limits {
            if let Some(max_memory) = limits.max_memory {
                if max_memory > 1024 * 1024 * 1024 { // 1GB
                    return Err(CCSwarmError::config("Memory limit too high"));
                }
            }
            if let Some(max_cpu) = limits.max_cpu_percent {
                if max_cpu > 100.0 {
                    return Err(CCSwarmError::config("CPU limit cannot exceed 100%"));
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension.rs:health_check (lines 449-465) ---[0m
    async fn health_check(&self) -> Result<Self::HealthStatus> {
        let status = match &self.state {
            ExtensionState::Active => HealthStatus::Healthy,
            ExtensionState::Loaded => HealthStatus::Healthy,
            ExtensionState::Paused => HealthStatus::Degraded("Extension is paused".to_string()),
            ExtensionState::Error(e) => HealthStatus::Unhealthy(e.clone()),
            _ => HealthStatus::Unknown,
        };

        Ok(ExtensionHealth {
            status,
            last_check: chrono::Utc::now(),
            uptime: chrono::Utc::now().signed_duration_since(self.created_at).to_std().unwrap_or_default(),
            error_count: 0, // Would be tracked by extension runtime
            performance_score: 1.0, // Would be calculated based on metrics
        })
    }

  ./crates/ccswarm/src/extension.rs:330-352 method check_dependencies <-> ./crates/ccswarm/src/extension.rs:722-734 function test_extension_metrics
  Similarity: 75.36%

[36m--- ./crates/ccswarm/src/extension.rs:check_dependencies (lines 330-352) ---[0m
    pub fn check_dependencies(&self, available_extensions: &[Extension]) -> Result<Vec<String>> {
        let mut missing_deps = Vec::new();

        for dep in &self.dependencies {
            if dep.optional {
                continue;
            }

            let satisfied = available_extensions.iter().any(|ext| {
                ext.name == dep.name && dep.version_requirement.satisfies(&ext.version)
            });

            if !satisfied {
                missing_deps.push(dep.name.clone());
            }
        }

        if missing_deps.is_empty() {
            Ok(Vec::new())
        } else {
            Ok(missing_deps)
        }
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_metrics (lines 722-734) ---[0m
    async fn test_extension_metrics() {
        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Performance],
        );

        let metrics = extension.metrics().await.unwrap();
        assert_eq!(metrics.memory_usage, 0);
        assert_eq!(metrics.cpu_usage, 0.0);
    }

  ./crates/ccswarm/src/extension.rs:449-465 method health_check <-> ./crates/ccswarm/src/extension.rs:467-477 method metrics
  Similarity: 96.72%
  Classes: Monitorable <-> Monitorable

[36m--- ./crates/ccswarm/src/extension.rs:health_check (lines 449-465) ---[0m
    async fn health_check(&self) -> Result<Self::HealthStatus> {
        let status = match &self.state {
            ExtensionState::Active => HealthStatus::Healthy,
            ExtensionState::Loaded => HealthStatus::Healthy,
            ExtensionState::Paused => HealthStatus::Degraded("Extension is paused".to_string()),
            ExtensionState::Error(e) => HealthStatus::Unhealthy(e.clone()),
            _ => HealthStatus::Unknown,
        };

        Ok(ExtensionHealth {
            status,
            last_check: chrono::Utc::now(),
            uptime: chrono::Utc::now().signed_duration_since(self.created_at).to_std().unwrap_or_default(),
            error_count: 0, // Would be tracked by extension runtime
            performance_score: 1.0, // Would be calculated based on metrics
        })
    }

[36m--- ./crates/ccswarm/src/extension.rs:metrics (lines 467-477) ---[0m
    async fn metrics(&self) -> Result<Self::Metrics> {
        // In a real implementation, these would be collected from the running extension
        Ok(ExtensionMetrics {
            memory_usage: 0,
            cpu_usage: 0.0,
            requests_handled: 0,
            errors_count: 0,
            average_response_time: std::time::Duration::from_millis(0),
            uptime: chrono::Utc::now().signed_duration_since(self.created_at).to_std().unwrap_or_default(),
        })
    }

  ./crates/ccswarm/src/extension.rs:449-465 method health_check <-> ./crates/ccswarm/src/extension.rs:708-719 function test_extension_health_check
  Similarity: 83.83%

[36m--- ./crates/ccswarm/src/extension.rs:health_check (lines 449-465) ---[0m
    async fn health_check(&self) -> Result<Self::HealthStatus> {
        let status = match &self.state {
            ExtensionState::Active => HealthStatus::Healthy,
            ExtensionState::Loaded => HealthStatus::Healthy,
            ExtensionState::Paused => HealthStatus::Degraded("Extension is paused".to_string()),
            ExtensionState::Error(e) => HealthStatus::Unhealthy(e.clone()),
            _ => HealthStatus::Unknown,
        };

        Ok(ExtensionHealth {
            status,
            last_check: chrono::Utc::now(),
            uptime: chrono::Utc::now().signed_duration_since(self.created_at).to_std().unwrap_or_default(),
            error_count: 0, // Would be tracked by extension runtime
            performance_score: 1.0, // Would be calculated based on metrics
        })
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_health_check (lines 708-719) ---[0m
    async fn test_extension_health_check() {
        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Monitoring],
        );

        let health = extension.health_check().await.unwrap();
        assert!(matches!(health.status, HealthStatus::Unknown));
    }

  ./crates/ccswarm/src/extension.rs:449-465 method health_check <-> ./crates/ccswarm/src/extension.rs:722-734 function test_extension_metrics
  Similarity: 80.55%

[36m--- ./crates/ccswarm/src/extension.rs:health_check (lines 449-465) ---[0m
    async fn health_check(&self) -> Result<Self::HealthStatus> {
        let status = match &self.state {
            ExtensionState::Active => HealthStatus::Healthy,
            ExtensionState::Loaded => HealthStatus::Healthy,
            ExtensionState::Paused => HealthStatus::Degraded("Extension is paused".to_string()),
            ExtensionState::Error(e) => HealthStatus::Unhealthy(e.clone()),
            _ => HealthStatus::Unknown,
        };

        Ok(ExtensionHealth {
            status,
            last_check: chrono::Utc::now(),
            uptime: chrono::Utc::now().signed_duration_since(self.created_at).to_std().unwrap_or_default(),
            error_count: 0, // Would be tracked by extension runtime
            performance_score: 1.0, // Would be calculated based on metrics
        })
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_metrics (lines 722-734) ---[0m
    async fn test_extension_metrics() {
        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Performance],
        );

        let metrics = extension.metrics().await.unwrap();
        assert_eq!(metrics.memory_usage, 0);
        assert_eq!(metrics.cpu_usage, 0.0);
    }

  ./crates/ccswarm/src/extension.rs:416-437 method validate_config <-> ./crates/ccswarm/src/extension.rs:613-621 function test_version_compatibility
  Similarity: 74.78%

[36m--- ./crates/ccswarm/src/extension.rs:validate_config (lines 416-437) ---[0m
    fn validate_config(config: &Self::Config) -> Result<()> {
        // Validate permissions are not excessive
        if config.permissions.len() > 10 {
            return Err(CCSwarmError::config("Too many permissions requested"));
        }

        // Validate resource limits are reasonable
        if let Some(ref limits) = config.resource_limits {
            if let Some(max_memory) = limits.max_memory {
                if max_memory > 1024 * 1024 * 1024 { // 1GB
                    return Err(CCSwarmError::config("Memory limit too high"));
                }
            }
            if let Some(max_cpu) = limits.max_cpu_percent {
                if max_cpu > 100.0 {
                    return Err(CCSwarmError::config("CPU limit cannot exceed 100%"));
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_version_compatibility (lines 613-621) ---[0m
    fn test_version_compatibility() {
        let v1_0_0 = Version::new(1, 0, 0);
        let v1_1_0 = Version::new(1, 1, 0);
        let v2_0_0 = Version::new(2, 0, 0);

        assert!(v1_1_0.is_compatible_with(&v1_0_0));
        assert!(!v1_0_0.is_compatible_with(&v1_1_0));
        assert!(!v2_0_0.is_compatible_with(&v1_0_0));
    }

  ./crates/ccswarm/src/extension.rs:638-651 function test_extension_creation <-> ./crates/ccswarm/src/extension.rs:722-734 function test_extension_metrics
  Similarity: 79.07%

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_creation (lines 638-651) ---[0m
    fn test_extension_creation() {
        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        assert_eq!(extension.name, "Test Extension");
        assert_eq!(extension.version, Version::new(1, 0, 0));
        assert!(extension.has_capability(&ExtensionCapability::Command));
        assert!(!extension.has_capability(&ExtensionCapability::Security));
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_metrics (lines 722-734) ---[0m
    async fn test_extension_metrics() {
        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Performance],
        );

        let metrics = extension.metrics().await.unwrap();
        assert_eq!(metrics.memory_usage, 0);
        assert_eq!(metrics.cpu_usage, 0.0);
    }

  ./crates/ccswarm/src/extension.rs:600-610 function test_version_parsing <-> ./crates/ccswarm/src/extension.rs:654-671 function test_extension_validation
  Similarity: 72.96%

[36m--- ./crates/ccswarm/src/extension.rs:test_version_parsing (lines 600-610) ---[0m
    fn test_version_parsing() {
        let version = "1.2.3".parse::<Version>().unwrap();
        assert_eq!(version.major, 1);
        assert_eq!(version.minor, 2);
        assert_eq!(version.patch, 3);
        assert!(version.is_stable());

        let pre_version = "1.2.3-beta".parse::<Version>().unwrap();
        assert_eq!(pre_version.pre_release, Some("beta".to_string()));
        assert!(!pre_version.is_stable());
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_validation (lines 654-671) ---[0m
    fn test_extension_validation() {
        let mut extension = Extension::new(
            "",  // Empty name should be invalid
            Version::new(1, 0, 0),
            "",  // Empty description should be invalid
            "",  // Empty author should be invalid
            vec![], // No capabilities should be invalid
        );

        let issues = extension.validate().unwrap();
        assert!(!issues.is_empty());

        let fixes = extension.auto_fix().unwrap();
        assert!(!fixes.is_empty());

        let issues_after_fix = extension.validate().unwrap();
        assert!(issues_after_fix.is_empty());
    }

  ./crates/ccswarm/src/extension.rs:624-635 function test_version_requirements <-> ./crates/ccswarm/src/extension.rs:654-671 function test_extension_validation
  Similarity: 70.46%

[36m--- ./crates/ccswarm/src/extension.rs:test_version_requirements (lines 624-635) ---[0m
    fn test_version_requirements() {
        let v1_0_0 = Version::new(1, 0, 0);
        let v1_1_0 = Version::new(1, 1, 0);

        let req = VersionRequirement::GreaterThanOrEqual(v1_0_0.clone());
        assert!(req.satisfies(&v1_1_0));
        assert!(req.satisfies(&v1_0_0));

        let req = VersionRequirement::Compatible(v1_0_0.clone());
        assert!(req.satisfies(&v1_1_0));
        assert!(req.satisfies(&v1_0_0));
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_validation (lines 654-671) ---[0m
    fn test_extension_validation() {
        let mut extension = Extension::new(
            "",  // Empty name should be invalid
            Version::new(1, 0, 0),
            "",  // Empty description should be invalid
            "",  // Empty author should be invalid
            vec![], // No capabilities should be invalid
        );

        let issues = extension.validate().unwrap();
        assert!(!issues.is_empty());

        let fixes = extension.auto_fix().unwrap();
        assert!(!fixes.is_empty());

        let issues_after_fix = extension.validate().unwrap();
        assert!(issues_after_fix.is_empty());
    }

  ./crates/ccswarm/src/extension.rs:264-274 method satisfies <-> ./crates/ccswarm/src/extension.rs:467-477 method metrics
  Similarity: 95.63%
  Classes: VersionRequirement <-> Monitorable

[36m--- ./crates/ccswarm/src/extension.rs:satisfies (lines 264-274) ---[0m
    pub fn satisfies(&self, version: &Version) -> bool {
        match self {
            Self::Exact(v) => version == v,
            Self::GreaterThan(v) => version > v,
            Self::GreaterThanOrEqual(v) => version >= v,
            Self::LessThan(v) => version < v,
            Self::LessThanOrEqual(v) => version <= v,
            Self::Compatible(v) => version.is_compatible_with(v),
            Self::Range { min, max } => version >= min && version <= max,
        }
    }

[36m--- ./crates/ccswarm/src/extension.rs:metrics (lines 467-477) ---[0m
    async fn metrics(&self) -> Result<Self::Metrics> {
        // In a real implementation, these would be collected from the running extension
        Ok(ExtensionMetrics {
            memory_usage: 0,
            cpu_usage: 0.0,
            requests_handled: 0,
            errors_count: 0,
            average_response_time: std::time::Duration::from_millis(0),
            uptime: chrono::Utc::now().signed_duration_since(self.created_at).to_std().unwrap_or_default(),
        })
    }

  ./crates/ccswarm/src/extension.rs:708-719 function test_extension_health_check <-> ./crates/ccswarm/src/extension.rs:722-734 function test_extension_metrics
  Similarity: 82.55%

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_health_check (lines 708-719) ---[0m
    async fn test_extension_health_check() {
        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Monitoring],
        );

        let health = extension.health_check().await.unwrap();
        assert!(matches!(health.status, HealthStatus::Unknown));
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_metrics (lines 722-734) ---[0m
    async fn test_extension_metrics() {
        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Performance],
        );

        let metrics = extension.metrics().await.unwrap();
        assert_eq!(metrics.memory_usage, 0);
        assert_eq!(metrics.cpu_usage, 0.0);
    }

  ./crates/ccswarm/src/extension.rs:613-621 function test_version_compatibility <-> ./crates/ccswarm/src/extension.rs:654-671 function test_extension_validation
  Similarity: 73.88%

[36m--- ./crates/ccswarm/src/extension.rs:test_version_compatibility (lines 613-621) ---[0m
    fn test_version_compatibility() {
        let v1_0_0 = Version::new(1, 0, 0);
        let v1_1_0 = Version::new(1, 1, 0);
        let v2_0_0 = Version::new(2, 0, 0);

        assert!(v1_1_0.is_compatible_with(&v1_0_0));
        assert!(!v1_0_0.is_compatible_with(&v1_1_0));
        assert!(!v2_0_0.is_compatible_with(&v1_0_0));
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_validation (lines 654-671) ---[0m
    fn test_extension_validation() {
        let mut extension = Extension::new(
            "",  // Empty name should be invalid
            Version::new(1, 0, 0),
            "",  // Empty description should be invalid
            "",  // Empty author should be invalid
            vec![], // No capabilities should be invalid
        );

        let issues = extension.validate().unwrap();
        assert!(!issues.is_empty());

        let fixes = extension.auto_fix().unwrap();
        assert!(!fixes.is_empty());

        let issues_after_fix = extension.validate().unwrap();
        assert!(issues_after_fix.is_empty());
    }

  ./crates/ccswarm/src/extension.rs:600-610 function test_version_parsing <-> ./crates/ccswarm/src/extension.rs:624-635 function test_version_requirements
  Similarity: 78.22%

[36m--- ./crates/ccswarm/src/extension.rs:test_version_parsing (lines 600-610) ---[0m
    fn test_version_parsing() {
        let version = "1.2.3".parse::<Version>().unwrap();
        assert_eq!(version.major, 1);
        assert_eq!(version.minor, 2);
        assert_eq!(version.patch, 3);
        assert!(version.is_stable());

        let pre_version = "1.2.3-beta".parse::<Version>().unwrap();
        assert_eq!(pre_version.pre_release, Some("beta".to_string()));
        assert!(!pre_version.is_stable());
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_version_requirements (lines 624-635) ---[0m
    fn test_version_requirements() {
        let v1_0_0 = Version::new(1, 0, 0);
        let v1_1_0 = Version::new(1, 1, 0);

        let req = VersionRequirement::GreaterThanOrEqual(v1_0_0.clone());
        assert!(req.satisfies(&v1_1_0));
        assert!(req.satisfies(&v1_0_0));

        let req = VersionRequirement::Compatible(v1_0_0.clone());
        assert!(req.satisfies(&v1_1_0));
        assert!(req.satisfies(&v1_0_0));
    }

  ./crates/ccswarm/src/extension.rs:467-477 method metrics <-> ./crates/ccswarm/src/extension.rs:708-719 function test_extension_health_check
  Similarity: 71.56%

[36m--- ./crates/ccswarm/src/extension.rs:metrics (lines 467-477) ---[0m
    async fn metrics(&self) -> Result<Self::Metrics> {
        // In a real implementation, these would be collected from the running extension
        Ok(ExtensionMetrics {
            memory_usage: 0,
            cpu_usage: 0.0,
            requests_handled: 0,
            errors_count: 0,
            average_response_time: std::time::Duration::from_millis(0),
            uptime: chrono::Utc::now().signed_duration_since(self.created_at).to_std().unwrap_or_default(),
        })
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_health_check (lines 708-719) ---[0m
    async fn test_extension_health_check() {
        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Monitoring],
        );

        let health = extension.health_check().await.unwrap();
        assert!(matches!(health.status, HealthStatus::Unknown));
    }

  ./crates/ccswarm/src/extension.rs:613-621 function test_version_compatibility <-> ./crates/ccswarm/src/extension.rs:638-651 function test_extension_creation
  Similarity: 70.78%

[36m--- ./crates/ccswarm/src/extension.rs:test_version_compatibility (lines 613-621) ---[0m
    fn test_version_compatibility() {
        let v1_0_0 = Version::new(1, 0, 0);
        let v1_1_0 = Version::new(1, 1, 0);
        let v2_0_0 = Version::new(2, 0, 0);

        assert!(v1_1_0.is_compatible_with(&v1_0_0));
        assert!(!v1_0_0.is_compatible_with(&v1_1_0));
        assert!(!v2_0_0.is_compatible_with(&v1_0_0));
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_creation (lines 638-651) ---[0m
    fn test_extension_creation() {
        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        assert_eq!(extension.name, "Test Extension");
        assert_eq!(extension.version, Version::new(1, 0, 0));
        assert!(extension.has_capability(&ExtensionCapability::Command));
        assert!(!extension.has_capability(&ExtensionCapability::Security));
    }

  ./crates/ccswarm/src/extension.rs:59-65 method fmt <-> ./crates/ccswarm/src/extension.rs:467-477 method metrics
  Similarity: 89.24%
  Classes: Version <-> Monitorable

[36m--- ./crates/ccswarm/src/extension.rs:fmt (lines 59-65) ---[0m
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        if let Some(ref pre) = self.pre_release {
            write!(f, "{}.{}.{}-{}", self.major, self.minor, self.patch, pre)
        } else {
            write!(f, "{}.{}.{}", self.major, self.minor, self.patch)
        }
    }

[36m--- ./crates/ccswarm/src/extension.rs:metrics (lines 467-477) ---[0m
    async fn metrics(&self) -> Result<Self::Metrics> {
        // In a real implementation, these would be collected from the running extension
        Ok(ExtensionMetrics {
            memory_usage: 0,
            cpu_usage: 0.0,
            requests_handled: 0,
            errors_count: 0,
            average_response_time: std::time::Duration::from_millis(0),
            uptime: chrono::Utc::now().signed_duration_since(self.created_at).to_std().unwrap_or_default(),
        })
    }

  ./crates/ccswarm/src/extension.rs:360-367 method can_activate <-> ./crates/ccswarm/src/extension.rs:467-477 method metrics
  Similarity: 78.70%
  Classes: Extension <-> Monitorable

[36m--- ./crates/ccswarm/src/extension.rs:can_activate (lines 360-367) ---[0m
    pub fn can_activate(&self, available_extensions: &[Extension]) -> Result<bool> {
        if !matches!(self.state, ExtensionState::Loaded) {
            return Ok(false);
        }

        let missing_deps = self.check_dependencies(available_extensions)?;
        Ok(missing_deps.is_empty())
    }

[36m--- ./crates/ccswarm/src/extension.rs:metrics (lines 467-477) ---[0m
    async fn metrics(&self) -> Result<Self::Metrics> {
        // In a real implementation, these would be collected from the running extension
        Ok(ExtensionMetrics {
            memory_usage: 0,
            cpu_usage: 0.0,
            requests_handled: 0,
            errors_count: 0,
            average_response_time: std::time::Duration::from_millis(0),
            uptime: chrono::Utc::now().signed_duration_since(self.created_at).to_std().unwrap_or_default(),
        })
    }

  ./crates/ccswarm/src/extension.rs:360-367 method can_activate <-> ./crates/ccswarm/src/extension.rs:708-719 function test_extension_health_check
  Similarity: 70.00%

[36m--- ./crates/ccswarm/src/extension.rs:can_activate (lines 360-367) ---[0m
    pub fn can_activate(&self, available_extensions: &[Extension]) -> Result<bool> {
        if !matches!(self.state, ExtensionState::Loaded) {
            return Ok(false);
        }

        let missing_deps = self.check_dependencies(available_extensions)?;
        Ok(missing_deps.is_empty())
    }

[36m--- ./crates/ccswarm/src/extension.rs:test_extension_health_check (lines 708-719) ---[0m
    async fn test_extension_health_check() {
        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Monitoring],
        );

        let health = extension.health_check().await.unwrap();
        assert!(matches!(health.status, HealthStatus::Unknown));
    }

  ./crates/ccswarm/src/extension.rs:59-65 method fmt <-> ./crates/ccswarm/src/extension.rs:264-274 method satisfies
  Similarity: 76.07%
  Classes: Version <-> VersionRequirement

[36m--- ./crates/ccswarm/src/extension.rs:fmt (lines 59-65) ---[0m
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        if let Some(ref pre) = self.pre_release {
            write!(f, "{}.{}.{}-{}", self.major, self.minor, self.patch, pre)
        } else {
            write!(f, "{}.{}.{}", self.major, self.minor, self.patch)
        }
    }

[36m--- ./crates/ccswarm/src/extension.rs:satisfies (lines 264-274) ---[0m
    pub fn satisfies(&self, version: &Version) -> bool {
        match self {
            Self::Exact(v) => version == v,
            Self::GreaterThan(v) => version > v,
            Self::GreaterThanOrEqual(v) => version >= v,
            Self::LessThan(v) => version < v,
            Self::LessThanOrEqual(v) => version <= v,
            Self::Compatible(v) => version.is_compatible_with(v),
            Self::Range { min, max } => version >= min && version <= max,
        }
    }

  ./crates/ccswarm/src/extension.rs:59-65 method fmt <-> ./crates/ccswarm/src/extension.rs:156-163 method default
  Similarity: 75.26%
  Classes: Version <-> Default

[36m--- ./crates/ccswarm/src/extension.rs:fmt (lines 59-65) ---[0m
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        if let Some(ref pre) = self.pre_release {
            write!(f, "{}.{}.{}-{}", self.major, self.minor, self.patch, pre)
        } else {
            write!(f, "{}.{}.{}", self.major, self.minor, self.patch)
        }
    }

[36m--- ./crates/ccswarm/src/extension.rs:default (lines 156-163) ---[0m
    fn default() -> Self {
        Self {
            auto_start: false,
            settings: HashMap::new(),
            permissions: Vec::new(),
            resource_limits: None,
        }
    }

  ./crates/ccswarm/src/extension.rs:156-163 method default <-> ./crates/ccswarm/src/extension.rs:308-315 method add_dependency
  Similarity: 70.00%
  Classes: Default <-> Extension

[36m--- ./crates/ccswarm/src/extension.rs:default (lines 156-163) ---[0m
    fn default() -> Self {
        Self {
            auto_start: false,
            settings: HashMap::new(),
            permissions: Vec::new(),
            resource_limits: None,
        }
    }

[36m--- ./crates/ccswarm/src/extension.rs:add_dependency (lines 308-315) ---[0m
    pub fn add_dependency(&mut self, name: String, version_requirement: VersionRequirement, optional: bool) {
        self.dependencies.push(ExtensionDependency {
            name,
            version_requirement,
            optional,
        });
        self.touch();
    }

  ./crates/ccswarm/src/extension.rs:308-315 method add_dependency <-> ./crates/ccswarm/src/extension.rs:318-321 method add_metadata
  Similarity: 87.89%
  Classes: Extension <-> Extension

[36m--- ./crates/ccswarm/src/extension.rs:add_dependency (lines 308-315) ---[0m
    pub fn add_dependency(&mut self, name: String, version_requirement: VersionRequirement, optional: bool) {
        self.dependencies.push(ExtensionDependency {
            name,
            version_requirement,
            optional,
        });
        self.touch();
    }

[36m--- ./crates/ccswarm/src/extension.rs:add_metadata (lines 318-321) ---[0m
    pub fn add_metadata<K: Into<String>>(&mut self, key: K, value: serde_json::Value) {
        self.metadata.insert(key.into(), value);
        self.touch();
    }

  ./crates/ccswarm/src/extension.rs:156-163 method default <-> ./crates/ccswarm/src/extension.rs:318-321 method add_metadata
  Similarity: 70.00%
  Classes: Default <-> Extension

[36m--- ./crates/ccswarm/src/extension.rs:default (lines 156-163) ---[0m
    fn default() -> Self {
        Self {
            auto_start: false,
            settings: HashMap::new(),
            permissions: Vec::new(),
            resource_limits: None,
        }
    }

[36m--- ./crates/ccswarm/src/extension.rs:add_metadata (lines 318-321) ---[0m
    pub fn add_metadata<K: Into<String>>(&mut self, key: K, value: serde_json::Value) {
        self.metadata.insert(key.into(), value);
        self.touch();
    }


Duplicates in ./crates/ccswarm/src/session/context_bridge.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/session/context_bridge.rs:61-71 method add_to_working_memory <-> ./crates/ccswarm/src/session/context_bridge.rs:74-87 method get_enhanced_memory_summary
  Similarity: 86.40%
  Classes: ContextBridge <-> ContextBridge

[36m--- ./crates/ccswarm/src/session/context_bridge.rs:add_to_working_memory (lines 61-71) ---[0m
    pub async fn add_to_working_memory(
        &self,
        content: String,
        memory_type: WorkingMemoryType,
        priority: f32,
    ) -> Result<()> {
        // Add to ccswarm's memory system
        let mut memory = self.session_memory.write().await;
        memory.add_to_working_memory(content, memory_type, priority);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/context_bridge.rs:get_enhanced_memory_summary (lines 74-87) ---[0m
    pub async fn get_enhanced_memory_summary(&self) -> Result<EnhancedMemorySummary> {
        let memory = self.session_memory.read().await;
        let base_summary = memory.get_summary();

        Ok(EnhancedMemorySummary {
            base_summary,
            context_stats: ContextStats {
                total_tokens: 0,
                message_count: 0,
                compression_ratio: 1.0,
                token_savings_percent: 0.0,
            },
        })
    }


Duplicates in ./crates/ccswarm/src/agent/claude.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/agent/claude.rs:155-171 function verify_identity_response <-> ./crates/ccswarm/src/agent/claude.rs:326-334 function test_claude_md_generation
  Similarity: 74.14%

[36m--- ./crates/ccswarm/src/agent/claude.rs:verify_identity_response (lines 155-171) ---[0m
pub fn verify_identity_response(response: &str, identity: &AgentIdentity) -> bool {
    let role_name = identity.specialization.name();
    let agent_phrase = format!("I am the {} Agent", role_name);
    let workspace_phrase = format!("My workspace is {}", identity.workspace_path.display());

    let required_phrases = [
        agent_phrase.as_str(),
        workspace_phrase.as_str(),
        "I specialize in:",
        "I will NOT work on:",
        "I will coordinate with other agents",
    ];

    required_phrases
        .iter()
        .all(|phrase| response.contains(phrase))
}

[36m--- ./crates/ccswarm/src/agent/claude.rs:test_claude_md_generation (lines 326-334) ---[0m
    fn test_claude_md_generation() {
        let identity = create_test_identity();
        let content = generate_claude_md_content(&identity);

        assert!(content.contains("Frontend Agent CRITICAL IDENTITY"));
        assert!(content.contains(&identity.agent_id));
        assert!(content.contains("React"));
        assert!(content.contains("TypeScript"));
    }


Duplicates in ./crates/ccswarm/src/orchestrator/mod.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/orchestrator/mod.rs:252-365 method start_coordination <-> ./crates/ccswarm/src/orchestrator/mod.rs:410-531 method assign_task
  Similarity: 85.75%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:start_coordination (lines 252-365) ---[0m
    pub async fn start_coordination(&self) -> Result<()> {
        info!("Starting Master Claude coordination");

        // Start message handling
        let agents = self.agents.clone();
        let bus = self.coordination_bus.clone();
        let state = self.state.clone();
        let task_queue_tx = self.task_queue_tx.clone();
        let judge = self.quality_judge.clone();

        tokio::spawn(async move {
            loop {
                match bus.receive_message().await {
                    Ok(message) => {
                        if let Err(e) = Self::handle_agent_message(
                            message,
                            &agents,
                            &state,
                            &task_queue_tx,
                            &judge,
                        )
                        .await
                        {
                            error!("Error handling agent message: {}", e);
                        }
                    }
                    Err(e) => {
                        error!("Error receiving message: {}", e);
                        tokio::time::sleep(std::time::Duration::from_secs(1)).await;
                    }
                }
            }
        });

        // Start quality review loop with LLM judge
        let agents = self.agents.clone();
        let standards = self.quality_standards.clone();
        let bus = self.coordination_bus.clone();
        let judge = self.quality_judge.clone();
        let worktree_manager = self.worktree_manager.clone();

        tokio::spawn(async move {
            loop {
                tokio::time::sleep(std::time::Duration::from_secs(30)).await;

                if let Err(e) = Self::perform_llm_quality_review(
                    &agents,
                    &standards,
                    &bus,
                    &judge,
                    &worktree_manager,
                )
                .await
                {
                    error!("Error performing quality review: {}", e);
                }
            }
        });

        // Start proactive Master Claude intelligence loop (DEFAULT ENABLED)
        if self.config.project.master_claude.enable_proactive_mode {
            let agents_proactive = self.agents.clone();
            let bus_proactive = self.coordination_bus.clone();
            let proactive_master = self.proactive_master.clone();
            let frequency = self.config.project.master_claude.proactive_frequency;

            info!(
                "🧠 Proactive Mode ENABLED by default - Standard analysis every {}s",
                frequency
            );
            tokio::spawn(async move {
                loop {
                    tokio::time::sleep(std::time::Duration::from_secs(frequency)).await;

                    let proactive_guard = proactive_master.read().await;
                    if let Err(e) = proactive_guard
                        .analyze_and_decide(&agents_proactive, &bus_proactive)
                        .await
                    {
                        error!("Error in proactive analysis: {}", e);
                    }
                }
            });

            // Enable high-frequency proactive mode
            let agents_hf = self.agents.clone();
            let bus_hf = self.coordination_bus.clone();
            let proactive_master_hf = self.proactive_master.clone();
            let high_frequency = self.config.project.master_claude.high_frequency;

            info!(
                "⚡ High-frequency proactive analysis active - every {}s",
                high_frequency
            );
            tokio::spawn(async move {
                loop {
                    tokio::time::sleep(std::time::Duration::from_secs(high_frequency)).await;

                    let proactive_guard = proactive_master_hf.read().await;
                    if let Err(e) = proactive_guard
                        .analyze_and_decide(&agents_hf, &bus_hf)
                        .await
                    {
                        error!("Error in high-frequency proactive analysis: {}", e);
                    }
                }
            });
        } else {
            info!("Proactive mode is disabled in configuration");
        }

        // Main coordination loop
        self.coordinate_development().await
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:assign_task (lines 410-531) ---[0m
    async fn assign_task(&self, task: Task) -> Result<()> {
        info!("Assigning task: {} - {}", task.id, task.description);

        // Check if task needs search assistance
        let search_keywords = vec![
            "research",
            "find information",
            "look up",
            "best practices",
            "documentation",
            "examples",
            "how to",
            "comparison",
            "compare",
            "alternatives",
            "investigate",
            "unclear",
            "unknown",
        ];

        let task_desc_lower = task.description.to_lowercase();
        let needs_search = search_keywords
            .iter()
            .any(|&keyword| task_desc_lower.contains(keyword));

        if needs_search && task.task_type != TaskType::Research {
            // Create a search request for this task
            info!(
                "Task '{}' appears to need search assistance",
                task.description
            );

            let search_request = crate::agent::search_agent::SearchRequest {
                requesting_agent: "master-claude".to_string(),
                query: task.description.clone(),
                scope: crate::agent::search_agent::SearchScope::All,
                max_results: Some(10),
                filters: None,
                context: Some(format!("Supporting task: {}", task.id)),
            };

            let message = AgentMessage::Coordination {
                from_agent: "master-claude".to_string(),
                to_agent: "search".to_string(),
                message_type: CoordinationType::Custom("search_request".to_string()),
                payload: serde_json::to_value(search_request)?,
            };

            self.coordination_bus.send_message(message).await?;
        }

        // Select optimal agent
        let agent_id = self.select_optimal_agent(&task).await?;

        // Get agent
        let mut agent = self
            .agents
            .get_mut(&agent_id)
            .ok_or_else(|| anyhow::anyhow!("Agent not found: {}", agent_id))?
            .clone();

        // Add orchestration context to the task if it comes from master
        let mut enhanced_task = task.clone();
        if enhanced_task.metadata.is_none() {
            enhanced_task.metadata = Some(serde_json::Map::new());
        }
        if let Some(metadata) = enhanced_task.metadata.as_mut() {
            metadata.insert(
                "orchestration_context".to_string(),
                serde_json::json!({
                    "master_id": self.id,
                    "agent_role": agent.identity.specialization.name(),
                    "coordination_enabled": true,
                    "quality_standards": {
                        "min_test_coverage": self.quality_standards.min_test_coverage,
                        "max_complexity": self.quality_standards.max_complexity,
                    },
                    "proactive_insights": self.get_proactive_insights_for_task(&task).await,
                }),
            );
        }

        // Execute task with enhanced context
        let result = agent.execute_task(enhanced_task).await?;

        // Update agent in map
        self.agents.insert(agent_id.clone(), agent);

        // Update statistics
        let mut state = self.state.write().await;
        state.total_tasks_processed += 1;
        if result.success {
            state.successful_tasks += 1;
        } else {
            state.failed_tasks += 1;
        }
        state.pending_tasks.retain(|t| t.id != task.id);

        // Send result to coordination bus
        let task_id = task.id.clone();
        self.coordination_bus
            .send_message(AgentMessage::TaskCompleted {
                agent_id: agent_id.clone(),
                task_id: task_id.clone(),
                result: result.clone(),
            })
            .await?;

        // Update proactive master with completion data
        if result.success {
            let proactive_master = self.proactive_master.read().await;
            if let Err(e) = proactive_master
                .update_context_from_completion(&task, &result)
                .await
            {
                warn!("Failed to update proactive master context: {}", e);
            }
        }

        info!("Task completed: {} - Success: {}", task_id, result.success);
        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:252-365 method start_coordination <-> ./crates/ccswarm/src/orchestrator/mod.rs:534-604 method select_optimal_agent
  Similarity: 85.76%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:start_coordination (lines 252-365) ---[0m
    pub async fn start_coordination(&self) -> Result<()> {
        info!("Starting Master Claude coordination");

        // Start message handling
        let agents = self.agents.clone();
        let bus = self.coordination_bus.clone();
        let state = self.state.clone();
        let task_queue_tx = self.task_queue_tx.clone();
        let judge = self.quality_judge.clone();

        tokio::spawn(async move {
            loop {
                match bus.receive_message().await {
                    Ok(message) => {
                        if let Err(e) = Self::handle_agent_message(
                            message,
                            &agents,
                            &state,
                            &task_queue_tx,
                            &judge,
                        )
                        .await
                        {
                            error!("Error handling agent message: {}", e);
                        }
                    }
                    Err(e) => {
                        error!("Error receiving message: {}", e);
                        tokio::time::sleep(std::time::Duration::from_secs(1)).await;
                    }
                }
            }
        });

        // Start quality review loop with LLM judge
        let agents = self.agents.clone();
        let standards = self.quality_standards.clone();
        let bus = self.coordination_bus.clone();
        let judge = self.quality_judge.clone();
        let worktree_manager = self.worktree_manager.clone();

        tokio::spawn(async move {
            loop {
                tokio::time::sleep(std::time::Duration::from_secs(30)).await;

                if let Err(e) = Self::perform_llm_quality_review(
                    &agents,
                    &standards,
                    &bus,
                    &judge,
                    &worktree_manager,
                )
                .await
                {
                    error!("Error performing quality review: {}", e);
                }
            }
        });

        // Start proactive Master Claude intelligence loop (DEFAULT ENABLED)
        if self.config.project.master_claude.enable_proactive_mode {
            let agents_proactive = self.agents.clone();
            let bus_proactive = self.coordination_bus.clone();
            let proactive_master = self.proactive_master.clone();
            let frequency = self.config.project.master_claude.proactive_frequency;

            info!(
                "🧠 Proactive Mode ENABLED by default - Standard analysis every {}s",
                frequency
            );
            tokio::spawn(async move {
                loop {
                    tokio::time::sleep(std::time::Duration::from_secs(frequency)).await;

                    let proactive_guard = proactive_master.read().await;
                    if let Err(e) = proactive_guard
                        .analyze_and_decide(&agents_proactive, &bus_proactive)
                        .await
                    {
                        error!("Error in proactive analysis: {}", e);
                    }
                }
            });

            // Enable high-frequency proactive mode
            let agents_hf = self.agents.clone();
            let bus_hf = self.coordination_bus.clone();
            let proactive_master_hf = self.proactive_master.clone();
            let high_frequency = self.config.project.master_claude.high_frequency;

            info!(
                "⚡ High-frequency proactive analysis active - every {}s",
                high_frequency
            );
            tokio::spawn(async move {
                loop {
                    tokio::time::sleep(std::time::Duration::from_secs(high_frequency)).await;

                    let proactive_guard = proactive_master_hf.read().await;
                    if let Err(e) = proactive_guard
                        .analyze_and_decide(&agents_hf, &bus_hf)
                        .await
                    {
                        error!("Error in high-frequency proactive analysis: {}", e);
                    }
                }
            });
        } else {
            info!("Proactive mode is disabled in configuration");
        }

        // Main coordination loop
        self.coordinate_development().await
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:select_optimal_agent (lines 534-604) ---[0m
    async fn select_optimal_agent(&self, task: &Task) -> Result<String> {
        // For remediation tasks, use the assigned agent if specified
        if task.task_type == TaskType::Remediation && task.assigned_to.is_some() {
            return Ok(task
                .assigned_to
                .as_ref()
                .ok_or_else(|| anyhow::anyhow!("Assigned agent not found for remediation task"))?
                .clone());
        }

        // Determine required specialization
        let required_role = match task.task_type {
            TaskType::Development => {
                if task.description.to_lowercase().contains("ui")
                    || task.description.to_lowercase().contains("component")
                    || task.description.to_lowercase().contains("frontend")
                {
                    "Frontend"
                } else {
                    "Backend" // Default to backend for general development
                }
            }
            TaskType::Infrastructure => "DevOps",
            TaskType::Testing => "QA",
            TaskType::Remediation => "Backend", // Fallback for unassigned remediation
            TaskType::Research => "Search",     // Research tasks go to search agent
            _ => "Backend",                     // Default
        };

        // Find available agents with matching specialization
        let mut suitable_agents = Vec::new();

        for entry in self.agents.iter() {
            let agent = entry.value();
            if agent.identity.specialization.name() == required_role
                && matches!(agent.status, AgentStatus::Available)
            {
                suitable_agents.push(agent.identity.agent_id.clone());
            }
        }

        if suitable_agents.is_empty() {
            // No available agent with exact match, find any available agent
            for entry in self.agents.iter() {
                let agent = entry.value();
                if matches!(agent.status, AgentStatus::Available) {
                    warn!(
                        "No {} agent available, using {}",
                        required_role,
                        agent.identity.specialization.name()
                    );
                    return Ok(agent.identity.agent_id.clone());
                }
            }

            return Err(anyhow::anyhow!("No available agents for task"));
        }

        // Select agent with least recent activity (load balancing)
        let selected = suitable_agents
            .into_iter()
            .min_by_key(|id| {
                self.agents
                    .get(id)
                    .map(|a| a.last_activity)
                    .unwrap_or_else(Utc::now)
            })
            .unwrap();

        Ok(selected)
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:252-365 method start_coordination <-> ./crates/ccswarm/src/orchestrator/mod.rs:1186-1273 method perform_llm_quality_review
  Similarity: 74.82%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:start_coordination (lines 252-365) ---[0m
    pub async fn start_coordination(&self) -> Result<()> {
        info!("Starting Master Claude coordination");

        // Start message handling
        let agents = self.agents.clone();
        let bus = self.coordination_bus.clone();
        let state = self.state.clone();
        let task_queue_tx = self.task_queue_tx.clone();
        let judge = self.quality_judge.clone();

        tokio::spawn(async move {
            loop {
                match bus.receive_message().await {
                    Ok(message) => {
                        if let Err(e) = Self::handle_agent_message(
                            message,
                            &agents,
                            &state,
                            &task_queue_tx,
                            &judge,
                        )
                        .await
                        {
                            error!("Error handling agent message: {}", e);
                        }
                    }
                    Err(e) => {
                        error!("Error receiving message: {}", e);
                        tokio::time::sleep(std::time::Duration::from_secs(1)).await;
                    }
                }
            }
        });

        // Start quality review loop with LLM judge
        let agents = self.agents.clone();
        let standards = self.quality_standards.clone();
        let bus = self.coordination_bus.clone();
        let judge = self.quality_judge.clone();
        let worktree_manager = self.worktree_manager.clone();

        tokio::spawn(async move {
            loop {
                tokio::time::sleep(std::time::Duration::from_secs(30)).await;

                if let Err(e) = Self::perform_llm_quality_review(
                    &agents,
                    &standards,
                    &bus,
                    &judge,
                    &worktree_manager,
                )
                .await
                {
                    error!("Error performing quality review: {}", e);
                }
            }
        });

        // Start proactive Master Claude intelligence loop (DEFAULT ENABLED)
        if self.config.project.master_claude.enable_proactive_mode {
            let agents_proactive = self.agents.clone();
            let bus_proactive = self.coordination_bus.clone();
            let proactive_master = self.proactive_master.clone();
            let frequency = self.config.project.master_claude.proactive_frequency;

            info!(
                "🧠 Proactive Mode ENABLED by default - Standard analysis every {}s",
                frequency
            );
            tokio::spawn(async move {
                loop {
                    tokio::time::sleep(std::time::Duration::from_secs(frequency)).await;

                    let proactive_guard = proactive_master.read().await;
                    if let Err(e) = proactive_guard
                        .analyze_and_decide(&agents_proactive, &bus_proactive)
                        .await
                    {
                        error!("Error in proactive analysis: {}", e);
                    }
                }
            });

            // Enable high-frequency proactive mode
            let agents_hf = self.agents.clone();
            let bus_hf = self.coordination_bus.clone();
            let proactive_master_hf = self.proactive_master.clone();
            let high_frequency = self.config.project.master_claude.high_frequency;

            info!(
                "⚡ High-frequency proactive analysis active - every {}s",
                high_frequency
            );
            tokio::spawn(async move {
                loop {
                    tokio::time::sleep(std::time::Duration::from_secs(high_frequency)).await;

                    let proactive_guard = proactive_master_hf.read().await;
                    if let Err(e) = proactive_guard
                        .analyze_and_decide(&agents_hf, &bus_hf)
                        .await
                    {
                        error!("Error in high-frequency proactive analysis: {}", e);
                    }
                }
            });
        } else {
            info!("Proactive mode is disabled in configuration");
        }

        // Main coordination loop
        self.coordinate_development().await
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_llm_quality_review (lines 1186-1273) ---[0m
    async fn perform_llm_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
        judge: &Arc<RwLock<LLMQualityJudge>>,
        _worktree_manager: &Arc<WorktreeManager>,
    ) -> Result<()> {
        info!("Performing LLM-based quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Get agent workspace path
                    let workspace_path = agent.identity.workspace_path.clone();

                    // Perform LLM-based quality evaluation
                    let mut judge_guard = judge.write().await;
                    match judge_guard
                        .evaluate_task(
                            task,
                            result,
                            &agent.identity.specialization,
                            &workspace_path.to_string_lossy(),
                        )
                        .await
                    {
                        Ok(evaluation) => {
                            info!(
                                "Quality evaluation for task {}: score={:.2}, passes={}",
                                task.id, evaluation.overall_score, evaluation.passes_standards
                            );

                            // Check if quality standards are met
                            if !evaluation.passes_standards
                                || evaluation.overall_score < (standards.min_test_coverage / 100.0)
                            {
                                // Convert evaluation to issues
                                let issues = judge_guard.evaluation_to_issues(&evaluation);

                                if !issues.is_empty() {
                                    warn!(
                                        "Task {} from agent {} failed quality standards: score={:.2}, confidence={:.2}",
                                        task.id, agent.identity.agent_id, evaluation.overall_score, evaluation.confidence
                                    );

                                    // Log detailed feedback
                                    info!("Quality feedback: {}", evaluation.feedback);

                                    // Send quality issue message with detailed information
                                    bus.send_message(AgentMessage::QualityIssue {
                                        agent_id: agent.identity.agent_id.clone(),
                                        task_id: task.id.clone(),
                                        issues,
                                    })
                                    .await?;

                                    // Store the detailed evaluation for remediation task creation
                                    // This could be enhanced to pass the full evaluation through the message
                                }
                            } else {
                                info!(
                                    "Task {} from agent {} passed quality review with score {:.2}",
                                    task.id, agent.identity.agent_id, evaluation.overall_score
                                );
                            }
                        }
                        Err(e) => {
                            error!(
                                "Failed to evaluate task {} for agent {}: {}",
                                task.id, agent.identity.agent_id, e
                            );
                            // Continue with next task
                        }
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:410-531 method assign_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:534-604 method select_optimal_agent
  Similarity: 74.54%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:assign_task (lines 410-531) ---[0m
    async fn assign_task(&self, task: Task) -> Result<()> {
        info!("Assigning task: {} - {}", task.id, task.description);

        // Check if task needs search assistance
        let search_keywords = vec![
            "research",
            "find information",
            "look up",
            "best practices",
            "documentation",
            "examples",
            "how to",
            "comparison",
            "compare",
            "alternatives",
            "investigate",
            "unclear",
            "unknown",
        ];

        let task_desc_lower = task.description.to_lowercase();
        let needs_search = search_keywords
            .iter()
            .any(|&keyword| task_desc_lower.contains(keyword));

        if needs_search && task.task_type != TaskType::Research {
            // Create a search request for this task
            info!(
                "Task '{}' appears to need search assistance",
                task.description
            );

            let search_request = crate::agent::search_agent::SearchRequest {
                requesting_agent: "master-claude".to_string(),
                query: task.description.clone(),
                scope: crate::agent::search_agent::SearchScope::All,
                max_results: Some(10),
                filters: None,
                context: Some(format!("Supporting task: {}", task.id)),
            };

            let message = AgentMessage::Coordination {
                from_agent: "master-claude".to_string(),
                to_agent: "search".to_string(),
                message_type: CoordinationType::Custom("search_request".to_string()),
                payload: serde_json::to_value(search_request)?,
            };

            self.coordination_bus.send_message(message).await?;
        }

        // Select optimal agent
        let agent_id = self.select_optimal_agent(&task).await?;

        // Get agent
        let mut agent = self
            .agents
            .get_mut(&agent_id)
            .ok_or_else(|| anyhow::anyhow!("Agent not found: {}", agent_id))?
            .clone();

        // Add orchestration context to the task if it comes from master
        let mut enhanced_task = task.clone();
        if enhanced_task.metadata.is_none() {
            enhanced_task.metadata = Some(serde_json::Map::new());
        }
        if let Some(metadata) = enhanced_task.metadata.as_mut() {
            metadata.insert(
                "orchestration_context".to_string(),
                serde_json::json!({
                    "master_id": self.id,
                    "agent_role": agent.identity.specialization.name(),
                    "coordination_enabled": true,
                    "quality_standards": {
                        "min_test_coverage": self.quality_standards.min_test_coverage,
                        "max_complexity": self.quality_standards.max_complexity,
                    },
                    "proactive_insights": self.get_proactive_insights_for_task(&task).await,
                }),
            );
        }

        // Execute task with enhanced context
        let result = agent.execute_task(enhanced_task).await?;

        // Update agent in map
        self.agents.insert(agent_id.clone(), agent);

        // Update statistics
        let mut state = self.state.write().await;
        state.total_tasks_processed += 1;
        if result.success {
            state.successful_tasks += 1;
        } else {
            state.failed_tasks += 1;
        }
        state.pending_tasks.retain(|t| t.id != task.id);

        // Send result to coordination bus
        let task_id = task.id.clone();
        self.coordination_bus
            .send_message(AgentMessage::TaskCompleted {
                agent_id: agent_id.clone(),
                task_id: task_id.clone(),
                result: result.clone(),
            })
            .await?;

        // Update proactive master with completion data
        if result.success {
            let proactive_master = self.proactive_master.read().await;
            if let Err(e) = proactive_master
                .update_context_from_completion(&task, &result)
                .await
            {
                warn!("Failed to update proactive master context: {}", e);
            }
        }

        info!("Task completed: {} - Success: {}", task_id, result.success);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:select_optimal_agent (lines 534-604) ---[0m
    async fn select_optimal_agent(&self, task: &Task) -> Result<String> {
        // For remediation tasks, use the assigned agent if specified
        if task.task_type == TaskType::Remediation && task.assigned_to.is_some() {
            return Ok(task
                .assigned_to
                .as_ref()
                .ok_or_else(|| anyhow::anyhow!("Assigned agent not found for remediation task"))?
                .clone());
        }

        // Determine required specialization
        let required_role = match task.task_type {
            TaskType::Development => {
                if task.description.to_lowercase().contains("ui")
                    || task.description.to_lowercase().contains("component")
                    || task.description.to_lowercase().contains("frontend")
                {
                    "Frontend"
                } else {
                    "Backend" // Default to backend for general development
                }
            }
            TaskType::Infrastructure => "DevOps",
            TaskType::Testing => "QA",
            TaskType::Remediation => "Backend", // Fallback for unassigned remediation
            TaskType::Research => "Search",     // Research tasks go to search agent
            _ => "Backend",                     // Default
        };

        // Find available agents with matching specialization
        let mut suitable_agents = Vec::new();

        for entry in self.agents.iter() {
            let agent = entry.value();
            if agent.identity.specialization.name() == required_role
                && matches!(agent.status, AgentStatus::Available)
            {
                suitable_agents.push(agent.identity.agent_id.clone());
            }
        }

        if suitable_agents.is_empty() {
            // No available agent with exact match, find any available agent
            for entry in self.agents.iter() {
                let agent = entry.value();
                if matches!(agent.status, AgentStatus::Available) {
                    warn!(
                        "No {} agent available, using {}",
                        required_role,
                        agent.identity.specialization.name()
                    );
                    return Ok(agent.identity.agent_id.clone());
                }
            }

            return Err(anyhow::anyhow!("No available agents for task"));
        }

        // Select agent with least recent activity (load balancing)
        let selected = suitable_agents
            .into_iter()
            .min_by_key(|id| {
                self.agents
                    .get(id)
                    .map(|a| a.last_activity)
                    .unwrap_or_else(Utc::now)
            })
            .unwrap();

        Ok(selected)
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1186-1273 method perform_llm_quality_review <-> ./crates/ccswarm/src/orchestrator/mod.rs:1277-1316 method perform_quality_review
  Similarity: 99.08%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_llm_quality_review (lines 1186-1273) ---[0m
    async fn perform_llm_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
        judge: &Arc<RwLock<LLMQualityJudge>>,
        _worktree_manager: &Arc<WorktreeManager>,
    ) -> Result<()> {
        info!("Performing LLM-based quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Get agent workspace path
                    let workspace_path = agent.identity.workspace_path.clone();

                    // Perform LLM-based quality evaluation
                    let mut judge_guard = judge.write().await;
                    match judge_guard
                        .evaluate_task(
                            task,
                            result,
                            &agent.identity.specialization,
                            &workspace_path.to_string_lossy(),
                        )
                        .await
                    {
                        Ok(evaluation) => {
                            info!(
                                "Quality evaluation for task {}: score={:.2}, passes={}",
                                task.id, evaluation.overall_score, evaluation.passes_standards
                            );

                            // Check if quality standards are met
                            if !evaluation.passes_standards
                                || evaluation.overall_score < (standards.min_test_coverage / 100.0)
                            {
                                // Convert evaluation to issues
                                let issues = judge_guard.evaluation_to_issues(&evaluation);

                                if !issues.is_empty() {
                                    warn!(
                                        "Task {} from agent {} failed quality standards: score={:.2}, confidence={:.2}",
                                        task.id, agent.identity.agent_id, evaluation.overall_score, evaluation.confidence
                                    );

                                    // Log detailed feedback
                                    info!("Quality feedback: {}", evaluation.feedback);

                                    // Send quality issue message with detailed information
                                    bus.send_message(AgentMessage::QualityIssue {
                                        agent_id: agent.identity.agent_id.clone(),
                                        task_id: task.id.clone(),
                                        issues,
                                    })
                                    .await?;

                                    // Store the detailed evaluation for remediation task creation
                                    // This could be enhanced to pass the full evaluation through the message
                                }
                            } else {
                                info!(
                                    "Task {} from agent {} passed quality review with score {:.2}",
                                    task.id, agent.identity.agent_id, evaluation.overall_score
                                );
                            }
                        }
                        Err(e) => {
                            error!(
                                "Failed to evaluate task {} for agent {}: {}",
                                task.id, agent.identity.agent_id, e
                            );
                            // Continue with next task
                        }
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_quality_review (lines 1277-1316) ---[0m
    async fn perform_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Performing basic quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Basic placeholder logic
                    let quality_score = 0.92; // Placeholder

                    if quality_score < (standards.min_test_coverage / 100.0) {
                        warn!(
                            "Task {} from agent {} failed quality standards: score={}",
                            task.id, agent.identity.agent_id, quality_score
                        );

                        bus.send_message(AgentMessage::QualityIssue {
                            agent_id: agent.identity.agent_id.clone(),
                            task_id: task.id.clone(),
                            issues: vec!["Low test coverage".to_string()],
                        })
                        .await?;
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1186-1273 method perform_llm_quality_review <-> ./crates/ccswarm/src/orchestrator/mod.rs:1566-1605 function create_test_config
  Similarity: 92.83%

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_llm_quality_review (lines 1186-1273) ---[0m
    async fn perform_llm_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
        judge: &Arc<RwLock<LLMQualityJudge>>,
        _worktree_manager: &Arc<WorktreeManager>,
    ) -> Result<()> {
        info!("Performing LLM-based quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Get agent workspace path
                    let workspace_path = agent.identity.workspace_path.clone();

                    // Perform LLM-based quality evaluation
                    let mut judge_guard = judge.write().await;
                    match judge_guard
                        .evaluate_task(
                            task,
                            result,
                            &agent.identity.specialization,
                            &workspace_path.to_string_lossy(),
                        )
                        .await
                    {
                        Ok(evaluation) => {
                            info!(
                                "Quality evaluation for task {}: score={:.2}, passes={}",
                                task.id, evaluation.overall_score, evaluation.passes_standards
                            );

                            // Check if quality standards are met
                            if !evaluation.passes_standards
                                || evaluation.overall_score < (standards.min_test_coverage / 100.0)
                            {
                                // Convert evaluation to issues
                                let issues = judge_guard.evaluation_to_issues(&evaluation);

                                if !issues.is_empty() {
                                    warn!(
                                        "Task {} from agent {} failed quality standards: score={:.2}, confidence={:.2}",
                                        task.id, agent.identity.agent_id, evaluation.overall_score, evaluation.confidence
                                    );

                                    // Log detailed feedback
                                    info!("Quality feedback: {}", evaluation.feedback);

                                    // Send quality issue message with detailed information
                                    bus.send_message(AgentMessage::QualityIssue {
                                        agent_id: agent.identity.agent_id.clone(),
                                        task_id: task.id.clone(),
                                        issues,
                                    })
                                    .await?;

                                    // Store the detailed evaluation for remediation task creation
                                    // This could be enhanced to pass the full evaluation through the message
                                }
                            } else {
                                info!(
                                    "Task {} from agent {} passed quality review with score {:.2}",
                                    task.id, agent.identity.agent_id, evaluation.overall_score
                                );
                            }
                        }
                        Err(e) => {
                            error!(
                                "Failed to evaluate task {} for agent {}: {}",
                                task.id, agent.identity.agent_id, e
                            );
                            // Continue with next task
                        }
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_test_config (lines 1566-1605) ---[0m
    fn create_test_config() -> CcswarmConfig {
        let mut agents = HashMap::new();
        agents.insert(
            "frontend".to_string(),
            crate::config::AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend".to_string(),
                branch: "feature/frontend".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        CcswarmConfig {
            project: crate::config::ProjectConfig {
                name: "Test Project".to_string(),
                repository: crate::config::RepositoryConfig {
                    url: "https://github.com/test/repo".to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: crate::config::MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.9,
                    think_mode: crate::config::ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: crate::config::CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        }
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:194-249 method initialize <-> ./crates/ccswarm/src/orchestrator/mod.rs:1186-1273 method perform_llm_quality_review
  Similarity: 80.92%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:initialize (lines 194-249) ---[0m
    pub async fn initialize(&self) -> Result<()> {
        info!(
            "Initializing Master Claude orchestrator: {} with isolation mode: {:?}",
            self.id, self.isolation_mode
        );

        // Initialize agents based on configuration
        for (agent_name, agent_config) in &self.config.agents {
            info!("Initializing agent: {}", agent_name);

            let role = match agent_config.specialization.as_str() {
                "react_typescript" | "frontend" => default_frontend_role(),
                "node_microservices" | "backend" => default_backend_role(),
                "aws_kubernetes" | "devops" => default_devops_role(),
                "qa" | "testing" => default_qa_role(),
                _ => {
                    warn!(
                        "Unknown specialization: {}, using frontend role",
                        agent_config.specialization
                    );
                    default_frontend_role()
                }
            };

            // Create agent with isolation mode
            let mut agent = ClaudeCodeAgent::new_with_isolation(
                role,
                &PathBuf::from(&self.config.project.name),
                &agent_config.branch,
                agent_config.claude_config.clone(),
                self.isolation_mode,
            )
            .await?;

            // Initialize agent (worktree, identity, etc.)
            agent.initialize().await?;

            // Add to managed agents
            self.agents.insert(agent.identity.agent_id.clone(), agent);
        }

        // Update state
        let mut state = self.state.write().await;
        state.status = OrchestratorStatus::Running;
        state.active_agents = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        info!(
            "Master Claude initialized with {} agents",
            self.agents.len()
        );
        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_llm_quality_review (lines 1186-1273) ---[0m
    async fn perform_llm_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
        judge: &Arc<RwLock<LLMQualityJudge>>,
        _worktree_manager: &Arc<WorktreeManager>,
    ) -> Result<()> {
        info!("Performing LLM-based quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Get agent workspace path
                    let workspace_path = agent.identity.workspace_path.clone();

                    // Perform LLM-based quality evaluation
                    let mut judge_guard = judge.write().await;
                    match judge_guard
                        .evaluate_task(
                            task,
                            result,
                            &agent.identity.specialization,
                            &workspace_path.to_string_lossy(),
                        )
                        .await
                    {
                        Ok(evaluation) => {
                            info!(
                                "Quality evaluation for task {}: score={:.2}, passes={}",
                                task.id, evaluation.overall_score, evaluation.passes_standards
                            );

                            // Check if quality standards are met
                            if !evaluation.passes_standards
                                || evaluation.overall_score < (standards.min_test_coverage / 100.0)
                            {
                                // Convert evaluation to issues
                                let issues = judge_guard.evaluation_to_issues(&evaluation);

                                if !issues.is_empty() {
                                    warn!(
                                        "Task {} from agent {} failed quality standards: score={:.2}, confidence={:.2}",
                                        task.id, agent.identity.agent_id, evaluation.overall_score, evaluation.confidence
                                    );

                                    // Log detailed feedback
                                    info!("Quality feedback: {}", evaluation.feedback);

                                    // Send quality issue message with detailed information
                                    bus.send_message(AgentMessage::QualityIssue {
                                        agent_id: agent.identity.agent_id.clone(),
                                        task_id: task.id.clone(),
                                        issues,
                                    })
                                    .await?;

                                    // Store the detailed evaluation for remediation task creation
                                    // This could be enhanced to pass the full evaluation through the message
                                }
                            } else {
                                info!(
                                    "Task {} from agent {} passed quality review with score {:.2}",
                                    task.id, agent.identity.agent_id, evaluation.overall_score
                                );
                            }
                        }
                        Err(e) => {
                            error!(
                                "Failed to evaluate task {} for agent {}: {}",
                                task.id, agent.identity.agent_id, e
                            );
                            // Continue with next task
                        }
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:194-249 method initialize <-> ./crates/ccswarm/src/orchestrator/mod.rs:534-604 method select_optimal_agent
  Similarity: 91.73%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:initialize (lines 194-249) ---[0m
    pub async fn initialize(&self) -> Result<()> {
        info!(
            "Initializing Master Claude orchestrator: {} with isolation mode: {:?}",
            self.id, self.isolation_mode
        );

        // Initialize agents based on configuration
        for (agent_name, agent_config) in &self.config.agents {
            info!("Initializing agent: {}", agent_name);

            let role = match agent_config.specialization.as_str() {
                "react_typescript" | "frontend" => default_frontend_role(),
                "node_microservices" | "backend" => default_backend_role(),
                "aws_kubernetes" | "devops" => default_devops_role(),
                "qa" | "testing" => default_qa_role(),
                _ => {
                    warn!(
                        "Unknown specialization: {}, using frontend role",
                        agent_config.specialization
                    );
                    default_frontend_role()
                }
            };

            // Create agent with isolation mode
            let mut agent = ClaudeCodeAgent::new_with_isolation(
                role,
                &PathBuf::from(&self.config.project.name),
                &agent_config.branch,
                agent_config.claude_config.clone(),
                self.isolation_mode,
            )
            .await?;

            // Initialize agent (worktree, identity, etc.)
            agent.initialize().await?;

            // Add to managed agents
            self.agents.insert(agent.identity.agent_id.clone(), agent);
        }

        // Update state
        let mut state = self.state.write().await;
        state.status = OrchestratorStatus::Running;
        state.active_agents = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        info!(
            "Master Claude initialized with {} agents",
            self.agents.len()
        );
        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:select_optimal_agent (lines 534-604) ---[0m
    async fn select_optimal_agent(&self, task: &Task) -> Result<String> {
        // For remediation tasks, use the assigned agent if specified
        if task.task_type == TaskType::Remediation && task.assigned_to.is_some() {
            return Ok(task
                .assigned_to
                .as_ref()
                .ok_or_else(|| anyhow::anyhow!("Assigned agent not found for remediation task"))?
                .clone());
        }

        // Determine required specialization
        let required_role = match task.task_type {
            TaskType::Development => {
                if task.description.to_lowercase().contains("ui")
                    || task.description.to_lowercase().contains("component")
                    || task.description.to_lowercase().contains("frontend")
                {
                    "Frontend"
                } else {
                    "Backend" // Default to backend for general development
                }
            }
            TaskType::Infrastructure => "DevOps",
            TaskType::Testing => "QA",
            TaskType::Remediation => "Backend", // Fallback for unassigned remediation
            TaskType::Research => "Search",     // Research tasks go to search agent
            _ => "Backend",                     // Default
        };

        // Find available agents with matching specialization
        let mut suitable_agents = Vec::new();

        for entry in self.agents.iter() {
            let agent = entry.value();
            if agent.identity.specialization.name() == required_role
                && matches!(agent.status, AgentStatus::Available)
            {
                suitable_agents.push(agent.identity.agent_id.clone());
            }
        }

        if suitable_agents.is_empty() {
            // No available agent with exact match, find any available agent
            for entry in self.agents.iter() {
                let agent = entry.value();
                if matches!(agent.status, AgentStatus::Available) {
                    warn!(
                        "No {} agent available, using {}",
                        required_role,
                        agent.identity.specialization.name()
                    );
                    return Ok(agent.identity.agent_id.clone());
                }
            }

            return Err(anyhow::anyhow!("No available agents for task"));
        }

        // Select agent with least recent activity (load balancing)
        let selected = suitable_agents
            .into_iter()
            .min_by_key(|id| {
                self.agents
                    .get(id)
                    .map(|a| a.last_activity)
                    .unwrap_or_else(Utc::now)
            })
            .unwrap();

        Ok(selected)
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:695-726 method check_agent_health <-> ./crates/ccswarm/src/orchestrator/mod.rs:1186-1273 method perform_llm_quality_review
  Similarity: 91.37%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:check_agent_health (lines 695-726) ---[0m
    async fn check_agent_health(&self) -> Result<()> {
        let mut unhealthy_agents = Vec::new();

        for entry in self.agents.iter() {
            let agent = entry.value();

            // Check if agent has been inactive for too long
            let inactive_duration = Utc::now() - agent.last_activity;
            if inactive_duration.num_seconds() > 300 {
                // 5 minutes
                warn!(
                    "Agent {} has been inactive for {} seconds",
                    agent.identity.agent_id,
                    inactive_duration.num_seconds()
                );
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }

            // Check for error status
            if matches!(agent.status, AgentStatus::Error(_)) {
                error!("Agent {} is in error state", agent.identity.agent_id);
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }
        }

        // Handle unhealthy agents
        for agent_id in unhealthy_agents {
            self.restart_agent(&agent_id).await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_llm_quality_review (lines 1186-1273) ---[0m
    async fn perform_llm_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
        judge: &Arc<RwLock<LLMQualityJudge>>,
        _worktree_manager: &Arc<WorktreeManager>,
    ) -> Result<()> {
        info!("Performing LLM-based quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Get agent workspace path
                    let workspace_path = agent.identity.workspace_path.clone();

                    // Perform LLM-based quality evaluation
                    let mut judge_guard = judge.write().await;
                    match judge_guard
                        .evaluate_task(
                            task,
                            result,
                            &agent.identity.specialization,
                            &workspace_path.to_string_lossy(),
                        )
                        .await
                    {
                        Ok(evaluation) => {
                            info!(
                                "Quality evaluation for task {}: score={:.2}, passes={}",
                                task.id, evaluation.overall_score, evaluation.passes_standards
                            );

                            // Check if quality standards are met
                            if !evaluation.passes_standards
                                || evaluation.overall_score < (standards.min_test_coverage / 100.0)
                            {
                                // Convert evaluation to issues
                                let issues = judge_guard.evaluation_to_issues(&evaluation);

                                if !issues.is_empty() {
                                    warn!(
                                        "Task {} from agent {} failed quality standards: score={:.2}, confidence={:.2}",
                                        task.id, agent.identity.agent_id, evaluation.overall_score, evaluation.confidence
                                    );

                                    // Log detailed feedback
                                    info!("Quality feedback: {}", evaluation.feedback);

                                    // Send quality issue message with detailed information
                                    bus.send_message(AgentMessage::QualityIssue {
                                        agent_id: agent.identity.agent_id.clone(),
                                        task_id: task.id.clone(),
                                        issues,
                                    })
                                    .await?;

                                    // Store the detailed evaluation for remediation task creation
                                    // This could be enhanced to pass the full evaluation through the message
                                }
                            } else {
                                info!(
                                    "Task {} from agent {} passed quality review with score {:.2}",
                                    task.id, agent.identity.agent_id, evaluation.overall_score
                                );
                            }
                        }
                        Err(e) => {
                            error!(
                                "Failed to evaluate task {} for agent {}: {}",
                                task.id, agent.identity.agent_id, e
                            );
                            // Continue with next task
                        }
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:145-186 method new <-> ./crates/ccswarm/src/orchestrator/mod.rs:534-604 method select_optimal_agent
  Similarity: 93.36%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:new (lines 145-186) ---[0m
    pub async fn new(config: CcswarmConfig, repo_path: PathBuf) -> Result<Self> {
        let id = format!("master-claude-{}", Uuid::new_v4());

        // Create channels
        let (task_queue_tx, task_queue_rx) = async_channel::bounded(1000);

        // Initialize worktree manager
        let worktree_manager = Arc::new(WorktreeManager::new(repo_path).unwrap());

        // Initialize coordination bus
        let coordination_bus = Arc::new(CoordinationBus::new().await?);

        // Create initial state
        let state = Arc::new(RwLock::new(OrchestratorState {
            status: OrchestratorStatus::Initializing,
            start_time: Utc::now(),
            total_tasks_processed: 0,
            successful_tasks: 0,
            failed_tasks: 0,
            active_agents: Vec::new(),
            pending_tasks: Vec::new(),
            review_history: HashMap::new(),
        }));

        let quality_judge = Arc::new(RwLock::new(LLMQualityJudge::new()));
        let proactive_master = Arc::new(RwLock::new(ProactiveMaster::new().await?));

        Ok(Self {
            id,
            config: config.clone(),
            agents: Arc::new(DashMap::new()),
            task_queue_tx,
            task_queue_rx,
            quality_standards: config.project.master_claude.quality_threshold.into(),
            coordination_bus,
            worktree_manager,
            state,
            quality_judge,
            proactive_master,
            isolation_mode: crate::agent::IsolationMode::default(),
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:select_optimal_agent (lines 534-604) ---[0m
    async fn select_optimal_agent(&self, task: &Task) -> Result<String> {
        // For remediation tasks, use the assigned agent if specified
        if task.task_type == TaskType::Remediation && task.assigned_to.is_some() {
            return Ok(task
                .assigned_to
                .as_ref()
                .ok_or_else(|| anyhow::anyhow!("Assigned agent not found for remediation task"))?
                .clone());
        }

        // Determine required specialization
        let required_role = match task.task_type {
            TaskType::Development => {
                if task.description.to_lowercase().contains("ui")
                    || task.description.to_lowercase().contains("component")
                    || task.description.to_lowercase().contains("frontend")
                {
                    "Frontend"
                } else {
                    "Backend" // Default to backend for general development
                }
            }
            TaskType::Infrastructure => "DevOps",
            TaskType::Testing => "QA",
            TaskType::Remediation => "Backend", // Fallback for unassigned remediation
            TaskType::Research => "Search",     // Research tasks go to search agent
            _ => "Backend",                     // Default
        };

        // Find available agents with matching specialization
        let mut suitable_agents = Vec::new();

        for entry in self.agents.iter() {
            let agent = entry.value();
            if agent.identity.specialization.name() == required_role
                && matches!(agent.status, AgentStatus::Available)
            {
                suitable_agents.push(agent.identity.agent_id.clone());
            }
        }

        if suitable_agents.is_empty() {
            // No available agent with exact match, find any available agent
            for entry in self.agents.iter() {
                let agent = entry.value();
                if matches!(agent.status, AgentStatus::Available) {
                    warn!(
                        "No {} agent available, using {}",
                        required_role,
                        agent.identity.specialization.name()
                    );
                    return Ok(agent.identity.agent_id.clone());
                }
            }

            return Err(anyhow::anyhow!("No available agents for task"));
        }

        // Select agent with least recent activity (load balancing)
        let selected = suitable_agents
            .into_iter()
            .min_by_key(|id| {
                self.agents
                    .get(id)
                    .map(|a| a.last_activity)
                    .unwrap_or_else(Utc::now)
            })
            .unwrap();

        Ok(selected)
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1186-1273 method perform_llm_quality_review <-> ./crates/ccswarm/src/orchestrator/mod.rs:1319-1346 method generate_status_report
  Similarity: 90.34%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_llm_quality_review (lines 1186-1273) ---[0m
    async fn perform_llm_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
        judge: &Arc<RwLock<LLMQualityJudge>>,
        _worktree_manager: &Arc<WorktreeManager>,
    ) -> Result<()> {
        info!("Performing LLM-based quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Get agent workspace path
                    let workspace_path = agent.identity.workspace_path.clone();

                    // Perform LLM-based quality evaluation
                    let mut judge_guard = judge.write().await;
                    match judge_guard
                        .evaluate_task(
                            task,
                            result,
                            &agent.identity.specialization,
                            &workspace_path.to_string_lossy(),
                        )
                        .await
                    {
                        Ok(evaluation) => {
                            info!(
                                "Quality evaluation for task {}: score={:.2}, passes={}",
                                task.id, evaluation.overall_score, evaluation.passes_standards
                            );

                            // Check if quality standards are met
                            if !evaluation.passes_standards
                                || evaluation.overall_score < (standards.min_test_coverage / 100.0)
                            {
                                // Convert evaluation to issues
                                let issues = judge_guard.evaluation_to_issues(&evaluation);

                                if !issues.is_empty() {
                                    warn!(
                                        "Task {} from agent {} failed quality standards: score={:.2}, confidence={:.2}",
                                        task.id, agent.identity.agent_id, evaluation.overall_score, evaluation.confidence
                                    );

                                    // Log detailed feedback
                                    info!("Quality feedback: {}", evaluation.feedback);

                                    // Send quality issue message with detailed information
                                    bus.send_message(AgentMessage::QualityIssue {
                                        agent_id: agent.identity.agent_id.clone(),
                                        task_id: task.id.clone(),
                                        issues,
                                    })
                                    .await?;

                                    // Store the detailed evaluation for remediation task creation
                                    // This could be enhanced to pass the full evaluation through the message
                                }
                            } else {
                                info!(
                                    "Task {} from agent {} passed quality review with score {:.2}",
                                    task.id, agent.identity.agent_id, evaluation.overall_score
                                );
                            }
                        }
                        Err(e) => {
                            error!(
                                "Failed to evaluate task {} for agent {}: {}",
                                task.id, agent.identity.agent_id, e
                            );
                            // Continue with next task
                        }
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_status_report (lines 1319-1346) ---[0m
    pub async fn generate_status_report(&self) -> Result<StatusReport> {
        let state = self.state.read().await;

        let agent_statuses: HashMap<String, AgentStatus> = self
            .agents
            .iter()
            .map(|entry| (entry.key().clone(), entry.value().status.clone()))
            .collect();

        let uptime = Utc::now() - state.start_time;

        Ok(StatusReport {
            orchestrator_id: self.id.clone(),
            status: state.status.clone(),
            uptime_seconds: uptime.num_seconds() as u64,
            total_agents: self.agents.len(),
            active_agents: agent_statuses
                .values()
                .filter(|s| matches!(s, AgentStatus::Available | AgentStatus::Working))
                .count(),
            total_tasks_processed: state.total_tasks_processed,
            successful_tasks: state.successful_tasks,
            failed_tasks: state.failed_tasks,
            pending_tasks: state.pending_tasks.len(),
            agent_statuses,
            timestamp: Utc::now(),
        })
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:145-186 method new <-> ./crates/ccswarm/src/orchestrator/mod.rs:194-249 method initialize
  Similarity: 90.85%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:new (lines 145-186) ---[0m
    pub async fn new(config: CcswarmConfig, repo_path: PathBuf) -> Result<Self> {
        let id = format!("master-claude-{}", Uuid::new_v4());

        // Create channels
        let (task_queue_tx, task_queue_rx) = async_channel::bounded(1000);

        // Initialize worktree manager
        let worktree_manager = Arc::new(WorktreeManager::new(repo_path).unwrap());

        // Initialize coordination bus
        let coordination_bus = Arc::new(CoordinationBus::new().await?);

        // Create initial state
        let state = Arc::new(RwLock::new(OrchestratorState {
            status: OrchestratorStatus::Initializing,
            start_time: Utc::now(),
            total_tasks_processed: 0,
            successful_tasks: 0,
            failed_tasks: 0,
            active_agents: Vec::new(),
            pending_tasks: Vec::new(),
            review_history: HashMap::new(),
        }));

        let quality_judge = Arc::new(RwLock::new(LLMQualityJudge::new()));
        let proactive_master = Arc::new(RwLock::new(ProactiveMaster::new().await?));

        Ok(Self {
            id,
            config: config.clone(),
            agents: Arc::new(DashMap::new()),
            task_queue_tx,
            task_queue_rx,
            quality_standards: config.project.master_claude.quality_threshold.into(),
            coordination_bus,
            worktree_manager,
            state,
            quality_judge,
            proactive_master,
            isolation_mode: crate::agent::IsolationMode::default(),
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:initialize (lines 194-249) ---[0m
    pub async fn initialize(&self) -> Result<()> {
        info!(
            "Initializing Master Claude orchestrator: {} with isolation mode: {:?}",
            self.id, self.isolation_mode
        );

        // Initialize agents based on configuration
        for (agent_name, agent_config) in &self.config.agents {
            info!("Initializing agent: {}", agent_name);

            let role = match agent_config.specialization.as_str() {
                "react_typescript" | "frontend" => default_frontend_role(),
                "node_microservices" | "backend" => default_backend_role(),
                "aws_kubernetes" | "devops" => default_devops_role(),
                "qa" | "testing" => default_qa_role(),
                _ => {
                    warn!(
                        "Unknown specialization: {}, using frontend role",
                        agent_config.specialization
                    );
                    default_frontend_role()
                }
            };

            // Create agent with isolation mode
            let mut agent = ClaudeCodeAgent::new_with_isolation(
                role,
                &PathBuf::from(&self.config.project.name),
                &agent_config.branch,
                agent_config.claude_config.clone(),
                self.isolation_mode,
            )
            .await?;

            // Initialize agent (worktree, identity, etc.)
            agent.initialize().await?;

            // Add to managed agents
            self.agents.insert(agent.identity.agent_id.clone(), agent);
        }

        // Update state
        let mut state = self.state.write().await;
        state.status = OrchestratorStatus::Running;
        state.active_agents = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        info!(
            "Master Claude initialized with {} agents",
            self.agents.len()
        );
        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1186-1273 method perform_llm_quality_review <-> ./crates/ccswarm/src/orchestrator/mod.rs:1443-1463 method identify_task_dependencies
  Similarity: 73.97%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_llm_quality_review (lines 1186-1273) ---[0m
    async fn perform_llm_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
        judge: &Arc<RwLock<LLMQualityJudge>>,
        _worktree_manager: &Arc<WorktreeManager>,
    ) -> Result<()> {
        info!("Performing LLM-based quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Get agent workspace path
                    let workspace_path = agent.identity.workspace_path.clone();

                    // Perform LLM-based quality evaluation
                    let mut judge_guard = judge.write().await;
                    match judge_guard
                        .evaluate_task(
                            task,
                            result,
                            &agent.identity.specialization,
                            &workspace_path.to_string_lossy(),
                        )
                        .await
                    {
                        Ok(evaluation) => {
                            info!(
                                "Quality evaluation for task {}: score={:.2}, passes={}",
                                task.id, evaluation.overall_score, evaluation.passes_standards
                            );

                            // Check if quality standards are met
                            if !evaluation.passes_standards
                                || evaluation.overall_score < (standards.min_test_coverage / 100.0)
                            {
                                // Convert evaluation to issues
                                let issues = judge_guard.evaluation_to_issues(&evaluation);

                                if !issues.is_empty() {
                                    warn!(
                                        "Task {} from agent {} failed quality standards: score={:.2}, confidence={:.2}",
                                        task.id, agent.identity.agent_id, evaluation.overall_score, evaluation.confidence
                                    );

                                    // Log detailed feedback
                                    info!("Quality feedback: {}", evaluation.feedback);

                                    // Send quality issue message with detailed information
                                    bus.send_message(AgentMessage::QualityIssue {
                                        agent_id: agent.identity.agent_id.clone(),
                                        task_id: task.id.clone(),
                                        issues,
                                    })
                                    .await?;

                                    // Store the detailed evaluation for remediation task creation
                                    // This could be enhanced to pass the full evaluation through the message
                                }
                            } else {
                                info!(
                                    "Task {} from agent {} passed quality review with score {:.2}",
                                    task.id, agent.identity.agent_id, evaluation.overall_score
                                );
                            }
                        }
                        Err(e) => {
                            error!(
                                "Failed to evaluate task {} for agent {}: {}",
                                task.id, agent.identity.agent_id, e
                            );
                            // Continue with next task
                        }
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:identify_task_dependencies (lines 1443-1463) ---[0m
    async fn identify_task_dependencies(&self, task: &Task) -> Vec<String> {
        let mut dependencies = Vec::new();

        // Check task description for dependency indicators
        let description_lower = task.description.to_lowercase();

        if description_lower.contains("api") || description_lower.contains("endpoint") {
            dependencies.push("backend_api".to_string());
        }
        if description_lower.contains("ui") || description_lower.contains("component") {
            dependencies.push("frontend_components".to_string());
        }
        if description_lower.contains("database") || description_lower.contains("migration") {
            dependencies.push("database_schema".to_string());
        }
        if description_lower.contains("deploy") || description_lower.contains("infrastructure") {
            dependencies.push("deployment_pipeline".to_string());
        }

        dependencies
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:648-692 method generate_fix_instructions <-> ./crates/ccswarm/src/orchestrator/mod.rs:1566-1605 function create_test_config
  Similarity: 92.00%

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_fix_instructions (lines 648-692) ---[0m
    async fn generate_fix_instructions(&self, issues: &[String]) -> Result<String> {
        // For now, generate instructions based on the issue types
        // In a full implementation, this would use Claude API to generate intelligent instructions

        let mut instructions = Vec::new();

        for issue in issues {
            let instruction = match issue.as_str() {
                "Low test coverage" => {
                    "1. Add unit tests to achieve at least 85% coverage\n\
                     2. Focus on edge cases and error paths\n\
                     3. Use test-driven development for any new code\n\
                     4. Run coverage report and ensure all critical paths are tested"
                }
                "High complexity" => {
                    "1. Break down complex functions into smaller, focused functions\n\
                     2. Extract repeated logic into helper functions\n\
                     3. Simplify conditional logic using early returns\n\
                     4. Consider using design patterns to reduce complexity"
                }
                "Security vulnerability" => {
                    "1. Review and fix all security warnings\n\
                     2. Validate all user inputs\n\
                     3. Use parameterized queries for database operations\n\
                     4. Update dependencies to latest secure versions"
                }
                "Missing documentation" => {
                    "1. Add comprehensive docstrings to all public functions\n\
                     2. Include parameter descriptions and return value documentation\n\
                     3. Add usage examples where appropriate\n\
                     4. Update README with any new functionality"
                }
                _ => {
                    "1. Review the specific issue and determine root cause\n\
                     2. Implement the most appropriate fix\n\
                     3. Add tests to prevent regression\n\
                     4. Document the changes made"
                }
            };

            instructions.push(format!("For '{}': \n{}", issue, instruction));
        }

        Ok(instructions.join("\n\n"))
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_test_config (lines 1566-1605) ---[0m
    fn create_test_config() -> CcswarmConfig {
        let mut agents = HashMap::new();
        agents.insert(
            "frontend".to_string(),
            crate::config::AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend".to_string(),
                branch: "feature/frontend".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        CcswarmConfig {
            project: crate::config::ProjectConfig {
                name: "Test Project".to_string(),
                repository: crate::config::RepositoryConfig {
                    url: "https://github.com/test/repo".to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: crate::config::MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.9,
                    think_mode: crate::config::ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: crate::config::CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        }
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:194-249 method initialize <-> ./crates/ccswarm/src/orchestrator/mod.rs:1566-1605 function create_test_config
  Similarity: 78.91%

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:initialize (lines 194-249) ---[0m
    pub async fn initialize(&self) -> Result<()> {
        info!(
            "Initializing Master Claude orchestrator: {} with isolation mode: {:?}",
            self.id, self.isolation_mode
        );

        // Initialize agents based on configuration
        for (agent_name, agent_config) in &self.config.agents {
            info!("Initializing agent: {}", agent_name);

            let role = match agent_config.specialization.as_str() {
                "react_typescript" | "frontend" => default_frontend_role(),
                "node_microservices" | "backend" => default_backend_role(),
                "aws_kubernetes" | "devops" => default_devops_role(),
                "qa" | "testing" => default_qa_role(),
                _ => {
                    warn!(
                        "Unknown specialization: {}, using frontend role",
                        agent_config.specialization
                    );
                    default_frontend_role()
                }
            };

            // Create agent with isolation mode
            let mut agent = ClaudeCodeAgent::new_with_isolation(
                role,
                &PathBuf::from(&self.config.project.name),
                &agent_config.branch,
                agent_config.claude_config.clone(),
                self.isolation_mode,
            )
            .await?;

            // Initialize agent (worktree, identity, etc.)
            agent.initialize().await?;

            // Add to managed agents
            self.agents.insert(agent.identity.agent_id.clone(), agent);
        }

        // Update state
        let mut state = self.state.write().await;
        state.status = OrchestratorStatus::Running;
        state.active_agents = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        info!(
            "Master Claude initialized with {} agents",
            self.agents.len()
        );
        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_test_config (lines 1566-1605) ---[0m
    fn create_test_config() -> CcswarmConfig {
        let mut agents = HashMap::new();
        agents.insert(
            "frontend".to_string(),
            crate::config::AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend".to_string(),
                branch: "feature/frontend".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        CcswarmConfig {
            project: crate::config::ProjectConfig {
                name: "Test Project".to_string(),
                repository: crate::config::RepositoryConfig {
                    url: "https://github.com/test/repo".to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: crate::config::MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.9,
                    think_mode: crate::config::ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: crate::config::CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        }
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:194-249 method initialize <-> ./crates/ccswarm/src/orchestrator/mod.rs:1277-1316 method perform_quality_review
  Similarity: 78.42%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:initialize (lines 194-249) ---[0m
    pub async fn initialize(&self) -> Result<()> {
        info!(
            "Initializing Master Claude orchestrator: {} with isolation mode: {:?}",
            self.id, self.isolation_mode
        );

        // Initialize agents based on configuration
        for (agent_name, agent_config) in &self.config.agents {
            info!("Initializing agent: {}", agent_name);

            let role = match agent_config.specialization.as_str() {
                "react_typescript" | "frontend" => default_frontend_role(),
                "node_microservices" | "backend" => default_backend_role(),
                "aws_kubernetes" | "devops" => default_devops_role(),
                "qa" | "testing" => default_qa_role(),
                _ => {
                    warn!(
                        "Unknown specialization: {}, using frontend role",
                        agent_config.specialization
                    );
                    default_frontend_role()
                }
            };

            // Create agent with isolation mode
            let mut agent = ClaudeCodeAgent::new_with_isolation(
                role,
                &PathBuf::from(&self.config.project.name),
                &agent_config.branch,
                agent_config.claude_config.clone(),
                self.isolation_mode,
            )
            .await?;

            // Initialize agent (worktree, identity, etc.)
            agent.initialize().await?;

            // Add to managed agents
            self.agents.insert(agent.identity.agent_id.clone(), agent);
        }

        // Update state
        let mut state = self.state.write().await;
        state.status = OrchestratorStatus::Running;
        state.active_agents = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        info!(
            "Master Claude initialized with {} agents",
            self.agents.len()
        );
        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_quality_review (lines 1277-1316) ---[0m
    async fn perform_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Performing basic quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Basic placeholder logic
                    let quality_score = 0.92; // Placeholder

                    if quality_score < (standards.min_test_coverage / 100.0) {
                        warn!(
                            "Task {} from agent {} failed quality standards: score={}",
                            task.id, agent.identity.agent_id, quality_score
                        );

                        bus.send_message(AgentMessage::QualityIssue {
                            agent_id: agent.identity.agent_id.clone(),
                            task_id: task.id.clone(),
                            issues: vec!["Low test coverage".to_string()],
                        })
                        .await?;
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:194-249 method initialize <-> ./crates/ccswarm/src/orchestrator/mod.rs:695-726 method check_agent_health
  Similarity: 85.02%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:initialize (lines 194-249) ---[0m
    pub async fn initialize(&self) -> Result<()> {
        info!(
            "Initializing Master Claude orchestrator: {} with isolation mode: {:?}",
            self.id, self.isolation_mode
        );

        // Initialize agents based on configuration
        for (agent_name, agent_config) in &self.config.agents {
            info!("Initializing agent: {}", agent_name);

            let role = match agent_config.specialization.as_str() {
                "react_typescript" | "frontend" => default_frontend_role(),
                "node_microservices" | "backend" => default_backend_role(),
                "aws_kubernetes" | "devops" => default_devops_role(),
                "qa" | "testing" => default_qa_role(),
                _ => {
                    warn!(
                        "Unknown specialization: {}, using frontend role",
                        agent_config.specialization
                    );
                    default_frontend_role()
                }
            };

            // Create agent with isolation mode
            let mut agent = ClaudeCodeAgent::new_with_isolation(
                role,
                &PathBuf::from(&self.config.project.name),
                &agent_config.branch,
                agent_config.claude_config.clone(),
                self.isolation_mode,
            )
            .await?;

            // Initialize agent (worktree, identity, etc.)
            agent.initialize().await?;

            // Add to managed agents
            self.agents.insert(agent.identity.agent_id.clone(), agent);
        }

        // Update state
        let mut state = self.state.write().await;
        state.status = OrchestratorStatus::Running;
        state.active_agents = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        info!(
            "Master Claude initialized with {} agents",
            self.agents.len()
        );
        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:check_agent_health (lines 695-726) ---[0m
    async fn check_agent_health(&self) -> Result<()> {
        let mut unhealthy_agents = Vec::new();

        for entry in self.agents.iter() {
            let agent = entry.value();

            // Check if agent has been inactive for too long
            let inactive_duration = Utc::now() - agent.last_activity;
            if inactive_duration.num_seconds() > 300 {
                // 5 minutes
                warn!(
                    "Agent {} has been inactive for {} seconds",
                    agent.identity.agent_id,
                    inactive_duration.num_seconds()
                );
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }

            // Check for error status
            if matches!(agent.status, AgentStatus::Error(_)) {
                error!("Agent {} is in error state", agent.identity.agent_id);
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }
        }

        // Handle unhealthy agents
        for agent_id in unhealthy_agents {
            self.restart_agent(&agent_id).await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:648-692 method generate_fix_instructions <-> ./crates/ccswarm/src/orchestrator/mod.rs:1277-1316 method perform_quality_review
  Similarity: 87.70%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_fix_instructions (lines 648-692) ---[0m
    async fn generate_fix_instructions(&self, issues: &[String]) -> Result<String> {
        // For now, generate instructions based on the issue types
        // In a full implementation, this would use Claude API to generate intelligent instructions

        let mut instructions = Vec::new();

        for issue in issues {
            let instruction = match issue.as_str() {
                "Low test coverage" => {
                    "1. Add unit tests to achieve at least 85% coverage\n\
                     2. Focus on edge cases and error paths\n\
                     3. Use test-driven development for any new code\n\
                     4. Run coverage report and ensure all critical paths are tested"
                }
                "High complexity" => {
                    "1. Break down complex functions into smaller, focused functions\n\
                     2. Extract repeated logic into helper functions\n\
                     3. Simplify conditional logic using early returns\n\
                     4. Consider using design patterns to reduce complexity"
                }
                "Security vulnerability" => {
                    "1. Review and fix all security warnings\n\
                     2. Validate all user inputs\n\
                     3. Use parameterized queries for database operations\n\
                     4. Update dependencies to latest secure versions"
                }
                "Missing documentation" => {
                    "1. Add comprehensive docstrings to all public functions\n\
                     2. Include parameter descriptions and return value documentation\n\
                     3. Add usage examples where appropriate\n\
                     4. Update README with any new functionality"
                }
                _ => {
                    "1. Review the specific issue and determine root cause\n\
                     2. Implement the most appropriate fix\n\
                     3. Add tests to prevent regression\n\
                     4. Document the changes made"
                }
            };

            instructions.push(format!("For '{}': \n{}", issue, instruction));
        }

        Ok(instructions.join("\n\n"))
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_quality_review (lines 1277-1316) ---[0m
    async fn perform_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Performing basic quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Basic placeholder logic
                    let quality_score = 0.92; // Placeholder

                    if quality_score < (standards.min_test_coverage / 100.0) {
                        warn!(
                            "Task {} from agent {} failed quality standards: score={}",
                            task.id, agent.identity.agent_id, quality_score
                        );

                        bus.send_message(AgentMessage::QualityIssue {
                            agent_id: agent.identity.agent_id.clone(),
                            task_id: task.id.clone(),
                            issues: vec!["Low test coverage".to_string()],
                        })
                        .await?;
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:648-692 method generate_fix_instructions <-> ./crates/ccswarm/src/orchestrator/mod.rs:695-726 method check_agent_health
  Similarity: 94.40%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_fix_instructions (lines 648-692) ---[0m
    async fn generate_fix_instructions(&self, issues: &[String]) -> Result<String> {
        // For now, generate instructions based on the issue types
        // In a full implementation, this would use Claude API to generate intelligent instructions

        let mut instructions = Vec::new();

        for issue in issues {
            let instruction = match issue.as_str() {
                "Low test coverage" => {
                    "1. Add unit tests to achieve at least 85% coverage\n\
                     2. Focus on edge cases and error paths\n\
                     3. Use test-driven development for any new code\n\
                     4. Run coverage report and ensure all critical paths are tested"
                }
                "High complexity" => {
                    "1. Break down complex functions into smaller, focused functions\n\
                     2. Extract repeated logic into helper functions\n\
                     3. Simplify conditional logic using early returns\n\
                     4. Consider using design patterns to reduce complexity"
                }
                "Security vulnerability" => {
                    "1. Review and fix all security warnings\n\
                     2. Validate all user inputs\n\
                     3. Use parameterized queries for database operations\n\
                     4. Update dependencies to latest secure versions"
                }
                "Missing documentation" => {
                    "1. Add comprehensive docstrings to all public functions\n\
                     2. Include parameter descriptions and return value documentation\n\
                     3. Add usage examples where appropriate\n\
                     4. Update README with any new functionality"
                }
                _ => {
                    "1. Review the specific issue and determine root cause\n\
                     2. Implement the most appropriate fix\n\
                     3. Add tests to prevent regression\n\
                     4. Document the changes made"
                }
            };

            instructions.push(format!("For '{}': \n{}", issue, instruction));
        }

        Ok(instructions.join("\n\n"))
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:check_agent_health (lines 695-726) ---[0m
    async fn check_agent_health(&self) -> Result<()> {
        let mut unhealthy_agents = Vec::new();

        for entry in self.agents.iter() {
            let agent = entry.value();

            // Check if agent has been inactive for too long
            let inactive_duration = Utc::now() - agent.last_activity;
            if inactive_duration.num_seconds() > 300 {
                // 5 minutes
                warn!(
                    "Agent {} has been inactive for {} seconds",
                    agent.identity.agent_id,
                    inactive_duration.num_seconds()
                );
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }

            // Check for error status
            if matches!(agent.status, AgentStatus::Error(_)) {
                error!("Agent {} is in error state", agent.identity.agent_id);
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }
        }

        // Handle unhealthy agents
        for agent_id in unhealthy_agents {
            self.restart_agent(&agent_id).await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1277-1316 method perform_quality_review <-> ./crates/ccswarm/src/orchestrator/mod.rs:1566-1605 function create_test_config
  Similarity: 90.85%

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_quality_review (lines 1277-1316) ---[0m
    async fn perform_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Performing basic quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Basic placeholder logic
                    let quality_score = 0.92; // Placeholder

                    if quality_score < (standards.min_test_coverage / 100.0) {
                        warn!(
                            "Task {} from agent {} failed quality standards: score={}",
                            task.id, agent.identity.agent_id, quality_score
                        );

                        bus.send_message(AgentMessage::QualityIssue {
                            agent_id: agent.identity.agent_id.clone(),
                            task_id: task.id.clone(),
                            issues: vec!["Low test coverage".to_string()],
                        })
                        .await?;
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_test_config (lines 1566-1605) ---[0m
    fn create_test_config() -> CcswarmConfig {
        let mut agents = HashMap::new();
        agents.insert(
            "frontend".to_string(),
            crate::config::AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend".to_string(),
                branch: "feature/frontend".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        CcswarmConfig {
            project: crate::config::ProjectConfig {
                name: "Test Project".to_string(),
                repository: crate::config::RepositoryConfig {
                    url: "https://github.com/test/repo".to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: crate::config::MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.9,
                    think_mode: crate::config::ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: crate::config::CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        }
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:145-186 method new <-> ./crates/ccswarm/src/orchestrator/mod.rs:608-644 method create_remediation_task
  Similarity: 91.62%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:new (lines 145-186) ---[0m
    pub async fn new(config: CcswarmConfig, repo_path: PathBuf) -> Result<Self> {
        let id = format!("master-claude-{}", Uuid::new_v4());

        // Create channels
        let (task_queue_tx, task_queue_rx) = async_channel::bounded(1000);

        // Initialize worktree manager
        let worktree_manager = Arc::new(WorktreeManager::new(repo_path).unwrap());

        // Initialize coordination bus
        let coordination_bus = Arc::new(CoordinationBus::new().await?);

        // Create initial state
        let state = Arc::new(RwLock::new(OrchestratorState {
            status: OrchestratorStatus::Initializing,
            start_time: Utc::now(),
            total_tasks_processed: 0,
            successful_tasks: 0,
            failed_tasks: 0,
            active_agents: Vec::new(),
            pending_tasks: Vec::new(),
            review_history: HashMap::new(),
        }));

        let quality_judge = Arc::new(RwLock::new(LLMQualityJudge::new()));
        let proactive_master = Arc::new(RwLock::new(ProactiveMaster::new().await?));

        Ok(Self {
            id,
            config: config.clone(),
            agents: Arc::new(DashMap::new()),
            task_queue_tx,
            task_queue_rx,
            quality_standards: config.project.master_claude.quality_threshold.into(),
            coordination_bus,
            worktree_manager,
            state,
            quality_judge,
            proactive_master,
            isolation_mode: crate::agent::IsolationMode::default(),
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_remediation_task (lines 608-644) ---[0m
    async fn create_remediation_task(
        &self,
        original_task_id: &str,
        agent_id: &str,
        issues: Vec<String>,
    ) -> Result<Task> {
        // Generate specific fix instructions based on the issues
        let instructions = self.generate_fix_instructions(&issues).await?;

        // Create remediation task
        let task_id = format!("remediate-{}-{}", original_task_id, Uuid::new_v4());

        let description = format!(
            "Fix quality issues in task {}: {}",
            original_task_id, instructions
        );

        let details = format!(
            "Quality issues found:\n{}\n\nSpecific instructions:\n{}",
            issues.join("\n- "),
            instructions
        );

        let remediation_task = Task::new(
            task_id,
            description,
            crate::agent::Priority::High, // High priority for quality fixes
            TaskType::Remediation,
        )
        .with_details(details)
        .assign_to(agent_id.to_string())
        .with_parent_task(original_task_id.to_string())
        .with_quality_issues(issues)
        .with_duration(1800); // 30 minutes estimate

        Ok(remediation_task)
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:194-249 method initialize <-> ./crates/ccswarm/src/orchestrator/mod.rs:1319-1346 method generate_status_report
  Similarity: 82.99%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:initialize (lines 194-249) ---[0m
    pub async fn initialize(&self) -> Result<()> {
        info!(
            "Initializing Master Claude orchestrator: {} with isolation mode: {:?}",
            self.id, self.isolation_mode
        );

        // Initialize agents based on configuration
        for (agent_name, agent_config) in &self.config.agents {
            info!("Initializing agent: {}", agent_name);

            let role = match agent_config.specialization.as_str() {
                "react_typescript" | "frontend" => default_frontend_role(),
                "node_microservices" | "backend" => default_backend_role(),
                "aws_kubernetes" | "devops" => default_devops_role(),
                "qa" | "testing" => default_qa_role(),
                _ => {
                    warn!(
                        "Unknown specialization: {}, using frontend role",
                        agent_config.specialization
                    );
                    default_frontend_role()
                }
            };

            // Create agent with isolation mode
            let mut agent = ClaudeCodeAgent::new_with_isolation(
                role,
                &PathBuf::from(&self.config.project.name),
                &agent_config.branch,
                agent_config.claude_config.clone(),
                self.isolation_mode,
            )
            .await?;

            // Initialize agent (worktree, identity, etc.)
            agent.initialize().await?;

            // Add to managed agents
            self.agents.insert(agent.identity.agent_id.clone(), agent);
        }

        // Update state
        let mut state = self.state.write().await;
        state.status = OrchestratorStatus::Running;
        state.active_agents = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        info!(
            "Master Claude initialized with {} agents",
            self.agents.len()
        );
        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_status_report (lines 1319-1346) ---[0m
    pub async fn generate_status_report(&self) -> Result<StatusReport> {
        let state = self.state.read().await;

        let agent_statuses: HashMap<String, AgentStatus> = self
            .agents
            .iter()
            .map(|entry| (entry.key().clone(), entry.value().status.clone()))
            .collect();

        let uptime = Utc::now() - state.start_time;

        Ok(StatusReport {
            orchestrator_id: self.id.clone(),
            status: state.status.clone(),
            uptime_seconds: uptime.num_seconds() as u64,
            total_agents: self.agents.len(),
            active_agents: agent_statuses
                .values()
                .filter(|s| matches!(s, AgentStatus::Available | AgentStatus::Working))
                .count(),
            total_tasks_processed: state.total_tasks_processed,
            successful_tasks: state.successful_tasks,
            failed_tasks: state.failed_tasks,
            pending_tasks: state.pending_tasks.len(),
            agent_statuses,
            timestamp: Utc::now(),
        })
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:194-249 method initialize <-> ./crates/ccswarm/src/orchestrator/mod.rs:1443-1463 method identify_task_dependencies
  Similarity: 88.74%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:initialize (lines 194-249) ---[0m
    pub async fn initialize(&self) -> Result<()> {
        info!(
            "Initializing Master Claude orchestrator: {} with isolation mode: {:?}",
            self.id, self.isolation_mode
        );

        // Initialize agents based on configuration
        for (agent_name, agent_config) in &self.config.agents {
            info!("Initializing agent: {}", agent_name);

            let role = match agent_config.specialization.as_str() {
                "react_typescript" | "frontend" => default_frontend_role(),
                "node_microservices" | "backend" => default_backend_role(),
                "aws_kubernetes" | "devops" => default_devops_role(),
                "qa" | "testing" => default_qa_role(),
                _ => {
                    warn!(
                        "Unknown specialization: {}, using frontend role",
                        agent_config.specialization
                    );
                    default_frontend_role()
                }
            };

            // Create agent with isolation mode
            let mut agent = ClaudeCodeAgent::new_with_isolation(
                role,
                &PathBuf::from(&self.config.project.name),
                &agent_config.branch,
                agent_config.claude_config.clone(),
                self.isolation_mode,
            )
            .await?;

            // Initialize agent (worktree, identity, etc.)
            agent.initialize().await?;

            // Add to managed agents
            self.agents.insert(agent.identity.agent_id.clone(), agent);
        }

        // Update state
        let mut state = self.state.write().await;
        state.status = OrchestratorStatus::Running;
        state.active_agents = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        info!(
            "Master Claude initialized with {} agents",
            self.agents.len()
        );
        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:identify_task_dependencies (lines 1443-1463) ---[0m
    async fn identify_task_dependencies(&self, task: &Task) -> Vec<String> {
        let mut dependencies = Vec::new();

        // Check task description for dependency indicators
        let description_lower = task.description.to_lowercase();

        if description_lower.contains("api") || description_lower.contains("endpoint") {
            dependencies.push("backend_api".to_string());
        }
        if description_lower.contains("ui") || description_lower.contains("component") {
            dependencies.push("frontend_components".to_string());
        }
        if description_lower.contains("database") || description_lower.contains("migration") {
            dependencies.push("database_schema".to_string());
        }
        if description_lower.contains("deploy") || description_lower.contains("infrastructure") {
            dependencies.push("deployment_pipeline".to_string());
        }

        dependencies
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:648-692 method generate_fix_instructions <-> ./crates/ccswarm/src/orchestrator/mod.rs:1319-1346 method generate_status_report
  Similarity: 91.83%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_fix_instructions (lines 648-692) ---[0m
    async fn generate_fix_instructions(&self, issues: &[String]) -> Result<String> {
        // For now, generate instructions based on the issue types
        // In a full implementation, this would use Claude API to generate intelligent instructions

        let mut instructions = Vec::new();

        for issue in issues {
            let instruction = match issue.as_str() {
                "Low test coverage" => {
                    "1. Add unit tests to achieve at least 85% coverage\n\
                     2. Focus on edge cases and error paths\n\
                     3. Use test-driven development for any new code\n\
                     4. Run coverage report and ensure all critical paths are tested"
                }
                "High complexity" => {
                    "1. Break down complex functions into smaller, focused functions\n\
                     2. Extract repeated logic into helper functions\n\
                     3. Simplify conditional logic using early returns\n\
                     4. Consider using design patterns to reduce complexity"
                }
                "Security vulnerability" => {
                    "1. Review and fix all security warnings\n\
                     2. Validate all user inputs\n\
                     3. Use parameterized queries for database operations\n\
                     4. Update dependencies to latest secure versions"
                }
                "Missing documentation" => {
                    "1. Add comprehensive docstrings to all public functions\n\
                     2. Include parameter descriptions and return value documentation\n\
                     3. Add usage examples where appropriate\n\
                     4. Update README with any new functionality"
                }
                _ => {
                    "1. Review the specific issue and determine root cause\n\
                     2. Implement the most appropriate fix\n\
                     3. Add tests to prevent regression\n\
                     4. Document the changes made"
                }
            };

            instructions.push(format!("For '{}': \n{}", issue, instruction));
        }

        Ok(instructions.join("\n\n"))
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_status_report (lines 1319-1346) ---[0m
    pub async fn generate_status_report(&self) -> Result<StatusReport> {
        let state = self.state.read().await;

        let agent_statuses: HashMap<String, AgentStatus> = self
            .agents
            .iter()
            .map(|entry| (entry.key().clone(), entry.value().status.clone()))
            .collect();

        let uptime = Utc::now() - state.start_time;

        Ok(StatusReport {
            orchestrator_id: self.id.clone(),
            status: state.status.clone(),
            uptime_seconds: uptime.num_seconds() as u64,
            total_agents: self.agents.len(),
            active_agents: agent_statuses
                .values()
                .filter(|s| matches!(s, AgentStatus::Available | AgentStatus::Working))
                .count(),
            total_tasks_processed: state.total_tasks_processed,
            successful_tasks: state.successful_tasks,
            failed_tasks: state.failed_tasks,
            pending_tasks: state.pending_tasks.len(),
            agent_statuses,
            timestamp: Utc::now(),
        })
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:608-644 method create_remediation_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:648-692 method generate_fix_instructions
  Similarity: 79.01%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_remediation_task (lines 608-644) ---[0m
    async fn create_remediation_task(
        &self,
        original_task_id: &str,
        agent_id: &str,
        issues: Vec<String>,
    ) -> Result<Task> {
        // Generate specific fix instructions based on the issues
        let instructions = self.generate_fix_instructions(&issues).await?;

        // Create remediation task
        let task_id = format!("remediate-{}-{}", original_task_id, Uuid::new_v4());

        let description = format!(
            "Fix quality issues in task {}: {}",
            original_task_id, instructions
        );

        let details = format!(
            "Quality issues found:\n{}\n\nSpecific instructions:\n{}",
            issues.join("\n- "),
            instructions
        );

        let remediation_task = Task::new(
            task_id,
            description,
            crate::agent::Priority::High, // High priority for quality fixes
            TaskType::Remediation,
        )
        .with_details(details)
        .assign_to(agent_id.to_string())
        .with_parent_task(original_task_id.to_string())
        .with_quality_issues(issues)
        .with_duration(1800); // 30 minutes estimate

        Ok(remediation_task)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_fix_instructions (lines 648-692) ---[0m
    async fn generate_fix_instructions(&self, issues: &[String]) -> Result<String> {
        // For now, generate instructions based on the issue types
        // In a full implementation, this would use Claude API to generate intelligent instructions

        let mut instructions = Vec::new();

        for issue in issues {
            let instruction = match issue.as_str() {
                "Low test coverage" => {
                    "1. Add unit tests to achieve at least 85% coverage\n\
                     2. Focus on edge cases and error paths\n\
                     3. Use test-driven development for any new code\n\
                     4. Run coverage report and ensure all critical paths are tested"
                }
                "High complexity" => {
                    "1. Break down complex functions into smaller, focused functions\n\
                     2. Extract repeated logic into helper functions\n\
                     3. Simplify conditional logic using early returns\n\
                     4. Consider using design patterns to reduce complexity"
                }
                "Security vulnerability" => {
                    "1. Review and fix all security warnings\n\
                     2. Validate all user inputs\n\
                     3. Use parameterized queries for database operations\n\
                     4. Update dependencies to latest secure versions"
                }
                "Missing documentation" => {
                    "1. Add comprehensive docstrings to all public functions\n\
                     2. Include parameter descriptions and return value documentation\n\
                     3. Add usage examples where appropriate\n\
                     4. Update README with any new functionality"
                }
                _ => {
                    "1. Review the specific issue and determine root cause\n\
                     2. Implement the most appropriate fix\n\
                     3. Add tests to prevent regression\n\
                     4. Document the changes made"
                }
            };

            instructions.push(format!("For '{}': \n{}", issue, instruction));
        }

        Ok(instructions.join("\n\n"))
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:648-692 method generate_fix_instructions <-> ./crates/ccswarm/src/orchestrator/mod.rs:1417-1440 method get_proactive_insights_for_task
  Similarity: 92.48%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_fix_instructions (lines 648-692) ---[0m
    async fn generate_fix_instructions(&self, issues: &[String]) -> Result<String> {
        // For now, generate instructions based on the issue types
        // In a full implementation, this would use Claude API to generate intelligent instructions

        let mut instructions = Vec::new();

        for issue in issues {
            let instruction = match issue.as_str() {
                "Low test coverage" => {
                    "1. Add unit tests to achieve at least 85% coverage\n\
                     2. Focus on edge cases and error paths\n\
                     3. Use test-driven development for any new code\n\
                     4. Run coverage report and ensure all critical paths are tested"
                }
                "High complexity" => {
                    "1. Break down complex functions into smaller, focused functions\n\
                     2. Extract repeated logic into helper functions\n\
                     3. Simplify conditional logic using early returns\n\
                     4. Consider using design patterns to reduce complexity"
                }
                "Security vulnerability" => {
                    "1. Review and fix all security warnings\n\
                     2. Validate all user inputs\n\
                     3. Use parameterized queries for database operations\n\
                     4. Update dependencies to latest secure versions"
                }
                "Missing documentation" => {
                    "1. Add comprehensive docstrings to all public functions\n\
                     2. Include parameter descriptions and return value documentation\n\
                     3. Add usage examples where appropriate\n\
                     4. Update README with any new functionality"
                }
                _ => {
                    "1. Review the specific issue and determine root cause\n\
                     2. Implement the most appropriate fix\n\
                     3. Add tests to prevent regression\n\
                     4. Document the changes made"
                }
            };

            instructions.push(format!("For '{}': \n{}", issue, instruction));
        }

        Ok(instructions.join("\n\n"))
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:get_proactive_insights_for_task (lines 1417-1440) ---[0m
    async fn get_proactive_insights_for_task(&self, task: &Task) -> serde_json::Value {
        let _proactive_master = self.proactive_master.read().await;

        // Get relevant insights from proactive master
        let insights = serde_json::json!({
            "task_complexity": match task.task_type {
                TaskType::Feature => "high",
                TaskType::Infrastructure => "high",
                TaskType::Development => "medium",
                TaskType::Testing => "medium",
                TaskType::Bug | TaskType::Bugfix => "low",
                _ => "medium",
            },
            "recommended_approach": match task.priority {
                crate::agent::Priority::Critical => "orchestrated_execution",
                crate::agent::Priority::High => "careful_planning",
                _ => "standard_execution",
            },
            "potential_dependencies": self.identify_task_dependencies(task).await,
            "similar_tasks_completed": self.find_similar_completed_tasks(task).await,
        });

        insights
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:695-726 method check_agent_health <-> ./crates/ccswarm/src/orchestrator/mod.rs:1566-1605 function create_test_config
  Similarity: 88.33%

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:check_agent_health (lines 695-726) ---[0m
    async fn check_agent_health(&self) -> Result<()> {
        let mut unhealthy_agents = Vec::new();

        for entry in self.agents.iter() {
            let agent = entry.value();

            // Check if agent has been inactive for too long
            let inactive_duration = Utc::now() - agent.last_activity;
            if inactive_duration.num_seconds() > 300 {
                // 5 minutes
                warn!(
                    "Agent {} has been inactive for {} seconds",
                    agent.identity.agent_id,
                    inactive_duration.num_seconds()
                );
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }

            // Check for error status
            if matches!(agent.status, AgentStatus::Error(_)) {
                error!("Agent {} is in error state", agent.identity.agent_id);
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }
        }

        // Handle unhealthy agents
        for agent_id in unhealthy_agents {
            self.restart_agent(&agent_id).await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_test_config (lines 1566-1605) ---[0m
    fn create_test_config() -> CcswarmConfig {
        let mut agents = HashMap::new();
        agents.insert(
            "frontend".to_string(),
            crate::config::AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend".to_string(),
                branch: "feature/frontend".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        CcswarmConfig {
            project: crate::config::ProjectConfig {
                name: "Test Project".to_string(),
                repository: crate::config::RepositoryConfig {
                    url: "https://github.com/test/repo".to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: crate::config::MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.9,
                    think_mode: crate::config::ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: crate::config::CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        }
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:608-644 method create_remediation_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:1566-1605 function create_test_config
  Similarity: 81.86%

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_remediation_task (lines 608-644) ---[0m
    async fn create_remediation_task(
        &self,
        original_task_id: &str,
        agent_id: &str,
        issues: Vec<String>,
    ) -> Result<Task> {
        // Generate specific fix instructions based on the issues
        let instructions = self.generate_fix_instructions(&issues).await?;

        // Create remediation task
        let task_id = format!("remediate-{}-{}", original_task_id, Uuid::new_v4());

        let description = format!(
            "Fix quality issues in task {}: {}",
            original_task_id, instructions
        );

        let details = format!(
            "Quality issues found:\n{}\n\nSpecific instructions:\n{}",
            issues.join("\n- "),
            instructions
        );

        let remediation_task = Task::new(
            task_id,
            description,
            crate::agent::Priority::High, // High priority for quality fixes
            TaskType::Remediation,
        )
        .with_details(details)
        .assign_to(agent_id.to_string())
        .with_parent_task(original_task_id.to_string())
        .with_quality_issues(issues)
        .with_duration(1800); // 30 minutes estimate

        Ok(remediation_task)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_test_config (lines 1566-1605) ---[0m
    fn create_test_config() -> CcswarmConfig {
        let mut agents = HashMap::new();
        agents.insert(
            "frontend".to_string(),
            crate::config::AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend".to_string(),
                branch: "feature/frontend".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        CcswarmConfig {
            project: crate::config::ProjectConfig {
                name: "Test Project".to_string(),
                repository: crate::config::RepositoryConfig {
                    url: "https://github.com/test/repo".to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: crate::config::MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.9,
                    think_mode: crate::config::ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: crate::config::CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        }
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:145-186 method new <-> ./crates/ccswarm/src/orchestrator/mod.rs:648-692 method generate_fix_instructions
  Similarity: 72.32%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:new (lines 145-186) ---[0m
    pub async fn new(config: CcswarmConfig, repo_path: PathBuf) -> Result<Self> {
        let id = format!("master-claude-{}", Uuid::new_v4());

        // Create channels
        let (task_queue_tx, task_queue_rx) = async_channel::bounded(1000);

        // Initialize worktree manager
        let worktree_manager = Arc::new(WorktreeManager::new(repo_path).unwrap());

        // Initialize coordination bus
        let coordination_bus = Arc::new(CoordinationBus::new().await?);

        // Create initial state
        let state = Arc::new(RwLock::new(OrchestratorState {
            status: OrchestratorStatus::Initializing,
            start_time: Utc::now(),
            total_tasks_processed: 0,
            successful_tasks: 0,
            failed_tasks: 0,
            active_agents: Vec::new(),
            pending_tasks: Vec::new(),
            review_history: HashMap::new(),
        }));

        let quality_judge = Arc::new(RwLock::new(LLMQualityJudge::new()));
        let proactive_master = Arc::new(RwLock::new(ProactiveMaster::new().await?));

        Ok(Self {
            id,
            config: config.clone(),
            agents: Arc::new(DashMap::new()),
            task_queue_tx,
            task_queue_rx,
            quality_standards: config.project.master_claude.quality_threshold.into(),
            coordination_bus,
            worktree_manager,
            state,
            quality_judge,
            proactive_master,
            isolation_mode: crate::agent::IsolationMode::default(),
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_fix_instructions (lines 648-692) ---[0m
    async fn generate_fix_instructions(&self, issues: &[String]) -> Result<String> {
        // For now, generate instructions based on the issue types
        // In a full implementation, this would use Claude API to generate intelligent instructions

        let mut instructions = Vec::new();

        for issue in issues {
            let instruction = match issue.as_str() {
                "Low test coverage" => {
                    "1. Add unit tests to achieve at least 85% coverage\n\
                     2. Focus on edge cases and error paths\n\
                     3. Use test-driven development for any new code\n\
                     4. Run coverage report and ensure all critical paths are tested"
                }
                "High complexity" => {
                    "1. Break down complex functions into smaller, focused functions\n\
                     2. Extract repeated logic into helper functions\n\
                     3. Simplify conditional logic using early returns\n\
                     4. Consider using design patterns to reduce complexity"
                }
                "Security vulnerability" => {
                    "1. Review and fix all security warnings\n\
                     2. Validate all user inputs\n\
                     3. Use parameterized queries for database operations\n\
                     4. Update dependencies to latest secure versions"
                }
                "Missing documentation" => {
                    "1. Add comprehensive docstrings to all public functions\n\
                     2. Include parameter descriptions and return value documentation\n\
                     3. Add usage examples where appropriate\n\
                     4. Update README with any new functionality"
                }
                _ => {
                    "1. Review the specific issue and determine root cause\n\
                     2. Implement the most appropriate fix\n\
                     3. Add tests to prevent regression\n\
                     4. Document the changes made"
                }
            };

            instructions.push(format!("For '{}': \n{}", issue, instruction));
        }

        Ok(instructions.join("\n\n"))
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:368-394 method coordinate_development <-> ./crates/ccswarm/src/orchestrator/mod.rs:1277-1316 method perform_quality_review
  Similarity: 92.62%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:coordinate_development (lines 368-394) ---[0m
    async fn coordinate_development(&self) -> Result<()> {
        info!("Master Claude coordination loop started");

        loop {
            tokio::select! {
                // Handle incoming tasks
                Ok(task) = self.task_queue_rx.recv() => {
                    if let Err(e) = self.assign_task(task).await {
                        error!("Error assigning task: {}", e);
                    }
                }

                // Periodic status check
                _ = tokio::time::sleep(std::time::Duration::from_secs(10)) => {
                    self.check_agent_health().await?;
                }
            }

            // Check if we should shutdown
            let state = self.state.read().await;
            if state.status == OrchestratorStatus::ShuttingDown {
                break;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_quality_review (lines 1277-1316) ---[0m
    async fn perform_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Performing basic quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Basic placeholder logic
                    let quality_score = 0.92; // Placeholder

                    if quality_score < (standards.min_test_coverage / 100.0) {
                        warn!(
                            "Task {} from agent {} failed quality standards: score={}",
                            task.id, agent.identity.agent_id, quality_score
                        );

                        bus.send_message(AgentMessage::QualityIssue {
                            agent_id: agent.identity.agent_id.clone(),
                            task_id: task.id.clone(),
                            issues: vec!["Low test coverage".to_string()],
                        })
                        .await?;
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1417-1440 method get_proactive_insights_for_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:1566-1605 function create_test_config
  Similarity: 96.74%

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:get_proactive_insights_for_task (lines 1417-1440) ---[0m
    async fn get_proactive_insights_for_task(&self, task: &Task) -> serde_json::Value {
        let _proactive_master = self.proactive_master.read().await;

        // Get relevant insights from proactive master
        let insights = serde_json::json!({
            "task_complexity": match task.task_type {
                TaskType::Feature => "high",
                TaskType::Infrastructure => "high",
                TaskType::Development => "medium",
                TaskType::Testing => "medium",
                TaskType::Bug | TaskType::Bugfix => "low",
                _ => "medium",
            },
            "recommended_approach": match task.priority {
                crate::agent::Priority::Critical => "orchestrated_execution",
                crate::agent::Priority::High => "careful_planning",
                _ => "standard_execution",
            },
            "potential_dependencies": self.identify_task_dependencies(task).await,
            "similar_tasks_completed": self.find_similar_completed_tasks(task).await,
        });

        insights
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_test_config (lines 1566-1605) ---[0m
    fn create_test_config() -> CcswarmConfig {
        let mut agents = HashMap::new();
        agents.insert(
            "frontend".to_string(),
            crate::config::AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend".to_string(),
                branch: "feature/frontend".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        CcswarmConfig {
            project: crate::config::ProjectConfig {
                name: "Test Project".to_string(),
                repository: crate::config::RepositoryConfig {
                    url: "https://github.com/test/repo".to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: crate::config::MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.9,
                    think_mode: crate::config::ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: crate::config::CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        }
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:368-394 method coordinate_development <-> ./crates/ccswarm/src/orchestrator/mod.rs:648-692 method generate_fix_instructions
  Similarity: 85.74%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:coordinate_development (lines 368-394) ---[0m
    async fn coordinate_development(&self) -> Result<()> {
        info!("Master Claude coordination loop started");

        loop {
            tokio::select! {
                // Handle incoming tasks
                Ok(task) = self.task_queue_rx.recv() => {
                    if let Err(e) = self.assign_task(task).await {
                        error!("Error assigning task: {}", e);
                    }
                }

                // Periodic status check
                _ = tokio::time::sleep(std::time::Duration::from_secs(10)) => {
                    self.check_agent_health().await?;
                }
            }

            // Check if we should shutdown
            let state = self.state.read().await;
            if state.status == OrchestratorStatus::ShuttingDown {
                break;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_fix_instructions (lines 648-692) ---[0m
    async fn generate_fix_instructions(&self, issues: &[String]) -> Result<String> {
        // For now, generate instructions based on the issue types
        // In a full implementation, this would use Claude API to generate intelligent instructions

        let mut instructions = Vec::new();

        for issue in issues {
            let instruction = match issue.as_str() {
                "Low test coverage" => {
                    "1. Add unit tests to achieve at least 85% coverage\n\
                     2. Focus on edge cases and error paths\n\
                     3. Use test-driven development for any new code\n\
                     4. Run coverage report and ensure all critical paths are tested"
                }
                "High complexity" => {
                    "1. Break down complex functions into smaller, focused functions\n\
                     2. Extract repeated logic into helper functions\n\
                     3. Simplify conditional logic using early returns\n\
                     4. Consider using design patterns to reduce complexity"
                }
                "Security vulnerability" => {
                    "1. Review and fix all security warnings\n\
                     2. Validate all user inputs\n\
                     3. Use parameterized queries for database operations\n\
                     4. Update dependencies to latest secure versions"
                }
                "Missing documentation" => {
                    "1. Add comprehensive docstrings to all public functions\n\
                     2. Include parameter descriptions and return value documentation\n\
                     3. Add usage examples where appropriate\n\
                     4. Update README with any new functionality"
                }
                _ => {
                    "1. Review the specific issue and determine root cause\n\
                     2. Implement the most appropriate fix\n\
                     3. Add tests to prevent regression\n\
                     4. Document the changes made"
                }
            };

            instructions.push(format!("For '{}': \n{}", issue, instruction));
        }

        Ok(instructions.join("\n\n"))
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1319-1346 method generate_status_report <-> ./crates/ccswarm/src/orchestrator/mod.rs:1566-1605 function create_test_config
  Similarity: 89.48%

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_status_report (lines 1319-1346) ---[0m
    pub async fn generate_status_report(&self) -> Result<StatusReport> {
        let state = self.state.read().await;

        let agent_statuses: HashMap<String, AgentStatus> = self
            .agents
            .iter()
            .map(|entry| (entry.key().clone(), entry.value().status.clone()))
            .collect();

        let uptime = Utc::now() - state.start_time;

        Ok(StatusReport {
            orchestrator_id: self.id.clone(),
            status: state.status.clone(),
            uptime_seconds: uptime.num_seconds() as u64,
            total_agents: self.agents.len(),
            active_agents: agent_statuses
                .values()
                .filter(|s| matches!(s, AgentStatus::Available | AgentStatus::Working))
                .count(),
            total_tasks_processed: state.total_tasks_processed,
            successful_tasks: state.successful_tasks,
            failed_tasks: state.failed_tasks,
            pending_tasks: state.pending_tasks.len(),
            agent_statuses,
            timestamp: Utc::now(),
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_test_config (lines 1566-1605) ---[0m
    fn create_test_config() -> CcswarmConfig {
        let mut agents = HashMap::new();
        agents.insert(
            "frontend".to_string(),
            crate::config::AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend".to_string(),
                branch: "feature/frontend".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        CcswarmConfig {
            project: crate::config::ProjectConfig {
                name: "Test Project".to_string(),
                repository: crate::config::RepositoryConfig {
                    url: "https://github.com/test/repo".to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: crate::config::MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.9,
                    think_mode: crate::config::ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: crate::config::CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        }
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1277-1316 method perform_quality_review <-> ./crates/ccswarm/src/orchestrator/mod.rs:1417-1440 method get_proactive_insights_for_task
  Similarity: 94.71%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_quality_review (lines 1277-1316) ---[0m
    async fn perform_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Performing basic quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Basic placeholder logic
                    let quality_score = 0.92; // Placeholder

                    if quality_score < (standards.min_test_coverage / 100.0) {
                        warn!(
                            "Task {} from agent {} failed quality standards: score={}",
                            task.id, agent.identity.agent_id, quality_score
                        );

                        bus.send_message(AgentMessage::QualityIssue {
                            agent_id: agent.identity.agent_id.clone(),
                            task_id: task.id.clone(),
                            issues: vec!["Low test coverage".to_string()],
                        })
                        .await?;
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:get_proactive_insights_for_task (lines 1417-1440) ---[0m
    async fn get_proactive_insights_for_task(&self, task: &Task) -> serde_json::Value {
        let _proactive_master = self.proactive_master.read().await;

        // Get relevant insights from proactive master
        let insights = serde_json::json!({
            "task_complexity": match task.task_type {
                TaskType::Feature => "high",
                TaskType::Infrastructure => "high",
                TaskType::Development => "medium",
                TaskType::Testing => "medium",
                TaskType::Bug | TaskType::Bugfix => "low",
                _ => "medium",
            },
            "recommended_approach": match task.priority {
                crate::agent::Priority::Critical => "orchestrated_execution",
                crate::agent::Priority::High => "careful_planning",
                _ => "standard_execution",
            },
            "potential_dependencies": self.identify_task_dependencies(task).await,
            "similar_tasks_completed": self.find_similar_completed_tasks(task).await,
        });

        insights
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:695-726 method check_agent_health <-> ./crates/ccswarm/src/orchestrator/mod.rs:1277-1316 method perform_quality_review
  Similarity: 83.52%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:check_agent_health (lines 695-726) ---[0m
    async fn check_agent_health(&self) -> Result<()> {
        let mut unhealthy_agents = Vec::new();

        for entry in self.agents.iter() {
            let agent = entry.value();

            // Check if agent has been inactive for too long
            let inactive_duration = Utc::now() - agent.last_activity;
            if inactive_duration.num_seconds() > 300 {
                // 5 minutes
                warn!(
                    "Agent {} has been inactive for {} seconds",
                    agent.identity.agent_id,
                    inactive_duration.num_seconds()
                );
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }

            // Check for error status
            if matches!(agent.status, AgentStatus::Error(_)) {
                error!("Agent {} is in error state", agent.identity.agent_id);
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }
        }

        // Handle unhealthy agents
        for agent_id in unhealthy_agents {
            self.restart_agent(&agent_id).await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_quality_review (lines 1277-1316) ---[0m
    async fn perform_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Performing basic quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Basic placeholder logic
                    let quality_score = 0.92; // Placeholder

                    if quality_score < (standards.min_test_coverage / 100.0) {
                        warn!(
                            "Task {} from agent {} failed quality standards: score={}",
                            task.id, agent.identity.agent_id, quality_score
                        );

                        bus.send_message(AgentMessage::QualityIssue {
                            agent_id: agent.identity.agent_id.clone(),
                            task_id: task.id.clone(),
                            issues: vec!["Low test coverage".to_string()],
                        })
                        .await?;
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:648-692 method generate_fix_instructions <-> ./crates/ccswarm/src/orchestrator/mod.rs:1375-1399 method add_milestone
  Similarity: 84.06%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_fix_instructions (lines 648-692) ---[0m
    async fn generate_fix_instructions(&self, issues: &[String]) -> Result<String> {
        // For now, generate instructions based on the issue types
        // In a full implementation, this would use Claude API to generate intelligent instructions

        let mut instructions = Vec::new();

        for issue in issues {
            let instruction = match issue.as_str() {
                "Low test coverage" => {
                    "1. Add unit tests to achieve at least 85% coverage\n\
                     2. Focus on edge cases and error paths\n\
                     3. Use test-driven development for any new code\n\
                     4. Run coverage report and ensure all critical paths are tested"
                }
                "High complexity" => {
                    "1. Break down complex functions into smaller, focused functions\n\
                     2. Extract repeated logic into helper functions\n\
                     3. Simplify conditional logic using early returns\n\
                     4. Consider using design patterns to reduce complexity"
                }
                "Security vulnerability" => {
                    "1. Review and fix all security warnings\n\
                     2. Validate all user inputs\n\
                     3. Use parameterized queries for database operations\n\
                     4. Update dependencies to latest secure versions"
                }
                "Missing documentation" => {
                    "1. Add comprehensive docstrings to all public functions\n\
                     2. Include parameter descriptions and return value documentation\n\
                     3. Add usage examples where appropriate\n\
                     4. Update README with any new functionality"
                }
                _ => {
                    "1. Review the specific issue and determine root cause\n\
                     2. Implement the most appropriate fix\n\
                     3. Add tests to prevent regression\n\
                     4. Document the changes made"
                }
            };

            instructions.push(format!("For '{}': \n{}", issue, instruction));
        }

        Ok(instructions.join("\n\n"))
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:add_milestone (lines 1375-1399) ---[0m
    pub async fn add_milestone(
        &self,
        name: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Milestone;

        let milestone_id = format!("milestone-{}", uuid::Uuid::new_v4());
        let milestone = Milestone {
            id: milestone_id.clone(),
            name,
            description,
            deadline,
            completion_percentage: 0.0,
            dependencies: vec![],
            critical_path: false,
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.add_milestone(milestone).await?;

        info!("Added milestone: {}", milestone_id);
        Ok(milestone_id)
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:648-692 method generate_fix_instructions <-> ./crates/ccswarm/src/orchestrator/mod.rs:1349-1372 method set_objective
  Similarity: 84.06%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_fix_instructions (lines 648-692) ---[0m
    async fn generate_fix_instructions(&self, issues: &[String]) -> Result<String> {
        // For now, generate instructions based on the issue types
        // In a full implementation, this would use Claude API to generate intelligent instructions

        let mut instructions = Vec::new();

        for issue in issues {
            let instruction = match issue.as_str() {
                "Low test coverage" => {
                    "1. Add unit tests to achieve at least 85% coverage\n\
                     2. Focus on edge cases and error paths\n\
                     3. Use test-driven development for any new code\n\
                     4. Run coverage report and ensure all critical paths are tested"
                }
                "High complexity" => {
                    "1. Break down complex functions into smaller, focused functions\n\
                     2. Extract repeated logic into helper functions\n\
                     3. Simplify conditional logic using early returns\n\
                     4. Consider using design patterns to reduce complexity"
                }
                "Security vulnerability" => {
                    "1. Review and fix all security warnings\n\
                     2. Validate all user inputs\n\
                     3. Use parameterized queries for database operations\n\
                     4. Update dependencies to latest secure versions"
                }
                "Missing documentation" => {
                    "1. Add comprehensive docstrings to all public functions\n\
                     2. Include parameter descriptions and return value documentation\n\
                     3. Add usage examples where appropriate\n\
                     4. Update README with any new functionality"
                }
                _ => {
                    "1. Review the specific issue and determine root cause\n\
                     2. Implement the most appropriate fix\n\
                     3. Add tests to prevent regression\n\
                     4. Document the changes made"
                }
            };

            instructions.push(format!("For '{}': \n{}", issue, instruction));
        }

        Ok(instructions.join("\n\n"))
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:set_objective (lines 1349-1372) ---[0m
    pub async fn set_objective(
        &self,
        title: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Objective;

        let objective_id = format!("obj-{}", uuid::Uuid::new_v4());
        let objective = Objective {
            id: objective_id.clone(),
            title,
            description,
            deadline,
            progress: 0.0,
            key_results: vec![],
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.set_objective(objective).await?;

        info!("Set project objective: {}", objective_id);
        Ok(objective_id)
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:648-692 method generate_fix_instructions <-> ./crates/ccswarm/src/orchestrator/mod.rs:1506-1531 method shutdown
  Similarity: 81.58%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_fix_instructions (lines 648-692) ---[0m
    async fn generate_fix_instructions(&self, issues: &[String]) -> Result<String> {
        // For now, generate instructions based on the issue types
        // In a full implementation, this would use Claude API to generate intelligent instructions

        let mut instructions = Vec::new();

        for issue in issues {
            let instruction = match issue.as_str() {
                "Low test coverage" => {
                    "1. Add unit tests to achieve at least 85% coverage\n\
                     2. Focus on edge cases and error paths\n\
                     3. Use test-driven development for any new code\n\
                     4. Run coverage report and ensure all critical paths are tested"
                }
                "High complexity" => {
                    "1. Break down complex functions into smaller, focused functions\n\
                     2. Extract repeated logic into helper functions\n\
                     3. Simplify conditional logic using early returns\n\
                     4. Consider using design patterns to reduce complexity"
                }
                "Security vulnerability" => {
                    "1. Review and fix all security warnings\n\
                     2. Validate all user inputs\n\
                     3. Use parameterized queries for database operations\n\
                     4. Update dependencies to latest secure versions"
                }
                "Missing documentation" => {
                    "1. Add comprehensive docstrings to all public functions\n\
                     2. Include parameter descriptions and return value documentation\n\
                     3. Add usage examples where appropriate\n\
                     4. Update README with any new functionality"
                }
                _ => {
                    "1. Review the specific issue and determine root cause\n\
                     2. Implement the most appropriate fix\n\
                     3. Add tests to prevent regression\n\
                     4. Document the changes made"
                }
            };

            instructions.push(format!("For '{}': \n{}", issue, instruction));
        }

        Ok(instructions.join("\n\n"))
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:shutdown (lines 1506-1531) ---[0m
    pub async fn shutdown(&self) -> Result<()> {
        info!("Shutting down Master Claude orchestrator");

        // Update state
        {
            let mut state = self.state.write().await;
            state.status = OrchestratorStatus::ShuttingDown;
        }

        // Shutdown all agents
        for entry in self.agents.iter() {
            let mut agent = entry.value().clone();
            if let Err(e) = agent.shutdown().await {
                error!(
                    "Error shutting down agent {}: {}",
                    agent.identity.agent_id, e
                );
            }
        }

        // Close coordination bus
        self.coordination_bus.close().await?;

        info!("Master Claude shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1277-1316 method perform_quality_review <-> ./crates/ccswarm/src/orchestrator/mod.rs:1319-1346 method generate_status_report
  Similarity: 84.48%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_quality_review (lines 1277-1316) ---[0m
    async fn perform_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Performing basic quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Basic placeholder logic
                    let quality_score = 0.92; // Placeholder

                    if quality_score < (standards.min_test_coverage / 100.0) {
                        warn!(
                            "Task {} from agent {} failed quality standards: score={}",
                            task.id, agent.identity.agent_id, quality_score
                        );

                        bus.send_message(AgentMessage::QualityIssue {
                            agent_id: agent.identity.agent_id.clone(),
                            task_id: task.id.clone(),
                            issues: vec!["Low test coverage".to_string()],
                        })
                        .await?;
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_status_report (lines 1319-1346) ---[0m
    pub async fn generate_status_report(&self) -> Result<StatusReport> {
        let state = self.state.read().await;

        let agent_statuses: HashMap<String, AgentStatus> = self
            .agents
            .iter()
            .map(|entry| (entry.key().clone(), entry.value().status.clone()))
            .collect();

        let uptime = Utc::now() - state.start_time;

        Ok(StatusReport {
            orchestrator_id: self.id.clone(),
            status: state.status.clone(),
            uptime_seconds: uptime.num_seconds() as u64,
            total_agents: self.agents.len(),
            active_agents: agent_statuses
                .values()
                .filter(|s| matches!(s, AgentStatus::Available | AgentStatus::Working))
                .count(),
            total_tasks_processed: state.total_tasks_processed,
            successful_tasks: state.successful_tasks,
            failed_tasks: state.failed_tasks,
            pending_tasks: state.pending_tasks.len(),
            agent_statuses,
            timestamp: Utc::now(),
        })
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:648-692 method generate_fix_instructions <-> ./crates/ccswarm/src/orchestrator/mod.rs:1483-1503 method enable_proactive_mode
  Similarity: 84.60%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_fix_instructions (lines 648-692) ---[0m
    async fn generate_fix_instructions(&self, issues: &[String]) -> Result<String> {
        // For now, generate instructions based on the issue types
        // In a full implementation, this would use Claude API to generate intelligent instructions

        let mut instructions = Vec::new();

        for issue in issues {
            let instruction = match issue.as_str() {
                "Low test coverage" => {
                    "1. Add unit tests to achieve at least 85% coverage\n\
                     2. Focus on edge cases and error paths\n\
                     3. Use test-driven development for any new code\n\
                     4. Run coverage report and ensure all critical paths are tested"
                }
                "High complexity" => {
                    "1. Break down complex functions into smaller, focused functions\n\
                     2. Extract repeated logic into helper functions\n\
                     3. Simplify conditional logic using early returns\n\
                     4. Consider using design patterns to reduce complexity"
                }
                "Security vulnerability" => {
                    "1. Review and fix all security warnings\n\
                     2. Validate all user inputs\n\
                     3. Use parameterized queries for database operations\n\
                     4. Update dependencies to latest secure versions"
                }
                "Missing documentation" => {
                    "1. Add comprehensive docstrings to all public functions\n\
                     2. Include parameter descriptions and return value documentation\n\
                     3. Add usage examples where appropriate\n\
                     4. Update README with any new functionality"
                }
                _ => {
                    "1. Review the specific issue and determine root cause\n\
                     2. Implement the most appropriate fix\n\
                     3. Add tests to prevent regression\n\
                     4. Document the changes made"
                }
            };

            instructions.push(format!("For '{}': \n{}", issue, instruction));
        }

        Ok(instructions.join("\n\n"))
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:enable_proactive_mode (lines 1483-1503) ---[0m
    pub async fn enable_proactive_mode(&self) -> Result<()> {
        info!("Enabling proactive mode - increasing analysis frequency");

        // Start additional proactive analysis loop with higher frequency
        let agents = self.agents.clone();
        let bus = self.coordination_bus.clone();
        let proactive_master = self.proactive_master.clone();

        tokio::spawn(async move {
            loop {
                tokio::time::sleep(std::time::Duration::from_secs(15)).await; // Every 15 seconds

                let proactive_guard = proactive_master.read().await;
                if let Err(e) = proactive_guard.analyze_and_decide(&agents, &bus).await {
                    error!("Error in high-frequency proactive analysis: {}", e);
                }
            }
        });

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:648-692 method generate_fix_instructions <-> ./crates/ccswarm/src/orchestrator/mod.rs:1466-1480 method find_similar_completed_tasks
  Similarity: 92.82%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_fix_instructions (lines 648-692) ---[0m
    async fn generate_fix_instructions(&self, issues: &[String]) -> Result<String> {
        // For now, generate instructions based on the issue types
        // In a full implementation, this would use Claude API to generate intelligent instructions

        let mut instructions = Vec::new();

        for issue in issues {
            let instruction = match issue.as_str() {
                "Low test coverage" => {
                    "1. Add unit tests to achieve at least 85% coverage\n\
                     2. Focus on edge cases and error paths\n\
                     3. Use test-driven development for any new code\n\
                     4. Run coverage report and ensure all critical paths are tested"
                }
                "High complexity" => {
                    "1. Break down complex functions into smaller, focused functions\n\
                     2. Extract repeated logic into helper functions\n\
                     3. Simplify conditional logic using early returns\n\
                     4. Consider using design patterns to reduce complexity"
                }
                "Security vulnerability" => {
                    "1. Review and fix all security warnings\n\
                     2. Validate all user inputs\n\
                     3. Use parameterized queries for database operations\n\
                     4. Update dependencies to latest secure versions"
                }
                "Missing documentation" => {
                    "1. Add comprehensive docstrings to all public functions\n\
                     2. Include parameter descriptions and return value documentation\n\
                     3. Add usage examples where appropriate\n\
                     4. Update README with any new functionality"
                }
                _ => {
                    "1. Review the specific issue and determine root cause\n\
                     2. Implement the most appropriate fix\n\
                     3. Add tests to prevent regression\n\
                     4. Document the changes made"
                }
            };

            instructions.push(format!("For '{}': \n{}", issue, instruction));
        }

        Ok(instructions.join("\n\n"))
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:find_similar_completed_tasks (lines 1466-1480) ---[0m
    async fn find_similar_completed_tasks(&self, task: &Task) -> Vec<String> {
        let state = self.state.read().await;
        let mut similar_tasks = Vec::new();

        // Look through review history for similar task types
        for (task_id, _) in state.review_history.iter().take(10) {
            // Use debug format for TaskType since it doesn't implement Display
            let task_type_str = format!("{:?}", task.task_type).to_lowercase();
            if task_id.contains(&task_type_str) {
                similar_tasks.push(task_id.clone());
            }
        }

        similar_tasks
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:608-644 method create_remediation_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:1375-1399 method add_milestone
  Similarity: 87.70%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_remediation_task (lines 608-644) ---[0m
    async fn create_remediation_task(
        &self,
        original_task_id: &str,
        agent_id: &str,
        issues: Vec<String>,
    ) -> Result<Task> {
        // Generate specific fix instructions based on the issues
        let instructions = self.generate_fix_instructions(&issues).await?;

        // Create remediation task
        let task_id = format!("remediate-{}-{}", original_task_id, Uuid::new_v4());

        let description = format!(
            "Fix quality issues in task {}: {}",
            original_task_id, instructions
        );

        let details = format!(
            "Quality issues found:\n{}\n\nSpecific instructions:\n{}",
            issues.join("\n- "),
            instructions
        );

        let remediation_task = Task::new(
            task_id,
            description,
            crate::agent::Priority::High, // High priority for quality fixes
            TaskType::Remediation,
        )
        .with_details(details)
        .assign_to(agent_id.to_string())
        .with_parent_task(original_task_id.to_string())
        .with_quality_issues(issues)
        .with_duration(1800); // 30 minutes estimate

        Ok(remediation_task)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:add_milestone (lines 1375-1399) ---[0m
    pub async fn add_milestone(
        &self,
        name: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Milestone;

        let milestone_id = format!("milestone-{}", uuid::Uuid::new_v4());
        let milestone = Milestone {
            id: milestone_id.clone(),
            name,
            description,
            deadline,
            completion_percentage: 0.0,
            dependencies: vec![],
            critical_path: false,
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.add_milestone(milestone).await?;

        info!("Added milestone: {}", milestone_id);
        Ok(milestone_id)
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:145-186 method new <-> ./crates/ccswarm/src/orchestrator/mod.rs:1483-1503 method enable_proactive_mode
  Similarity: 85.46%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:new (lines 145-186) ---[0m
    pub async fn new(config: CcswarmConfig, repo_path: PathBuf) -> Result<Self> {
        let id = format!("master-claude-{}", Uuid::new_v4());

        // Create channels
        let (task_queue_tx, task_queue_rx) = async_channel::bounded(1000);

        // Initialize worktree manager
        let worktree_manager = Arc::new(WorktreeManager::new(repo_path).unwrap());

        // Initialize coordination bus
        let coordination_bus = Arc::new(CoordinationBus::new().await?);

        // Create initial state
        let state = Arc::new(RwLock::new(OrchestratorState {
            status: OrchestratorStatus::Initializing,
            start_time: Utc::now(),
            total_tasks_processed: 0,
            successful_tasks: 0,
            failed_tasks: 0,
            active_agents: Vec::new(),
            pending_tasks: Vec::new(),
            review_history: HashMap::new(),
        }));

        let quality_judge = Arc::new(RwLock::new(LLMQualityJudge::new()));
        let proactive_master = Arc::new(RwLock::new(ProactiveMaster::new().await?));

        Ok(Self {
            id,
            config: config.clone(),
            agents: Arc::new(DashMap::new()),
            task_queue_tx,
            task_queue_rx,
            quality_standards: config.project.master_claude.quality_threshold.into(),
            coordination_bus,
            worktree_manager,
            state,
            quality_judge,
            proactive_master,
            isolation_mode: crate::agent::IsolationMode::default(),
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:enable_proactive_mode (lines 1483-1503) ---[0m
    pub async fn enable_proactive_mode(&self) -> Result<()> {
        info!("Enabling proactive mode - increasing analysis frequency");

        // Start additional proactive analysis loop with higher frequency
        let agents = self.agents.clone();
        let bus = self.coordination_bus.clone();
        let proactive_master = self.proactive_master.clone();

        tokio::spawn(async move {
            loop {
                tokio::time::sleep(std::time::Duration::from_secs(15)).await; // Every 15 seconds

                let proactive_guard = proactive_master.read().await;
                if let Err(e) = proactive_guard.analyze_and_decide(&agents, &bus).await {
                    error!("Error in high-frequency proactive analysis: {}", e);
                }
            }
        });

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:608-644 method create_remediation_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:1319-1346 method generate_status_report
  Similarity: 82.69%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_remediation_task (lines 608-644) ---[0m
    async fn create_remediation_task(
        &self,
        original_task_id: &str,
        agent_id: &str,
        issues: Vec<String>,
    ) -> Result<Task> {
        // Generate specific fix instructions based on the issues
        let instructions = self.generate_fix_instructions(&issues).await?;

        // Create remediation task
        let task_id = format!("remediate-{}-{}", original_task_id, Uuid::new_v4());

        let description = format!(
            "Fix quality issues in task {}: {}",
            original_task_id, instructions
        );

        let details = format!(
            "Quality issues found:\n{}\n\nSpecific instructions:\n{}",
            issues.join("\n- "),
            instructions
        );

        let remediation_task = Task::new(
            task_id,
            description,
            crate::agent::Priority::High, // High priority for quality fixes
            TaskType::Remediation,
        )
        .with_details(details)
        .assign_to(agent_id.to_string())
        .with_parent_task(original_task_id.to_string())
        .with_quality_issues(issues)
        .with_duration(1800); // 30 minutes estimate

        Ok(remediation_task)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_status_report (lines 1319-1346) ---[0m
    pub async fn generate_status_report(&self) -> Result<StatusReport> {
        let state = self.state.read().await;

        let agent_statuses: HashMap<String, AgentStatus> = self
            .agents
            .iter()
            .map(|entry| (entry.key().clone(), entry.value().status.clone()))
            .collect();

        let uptime = Utc::now() - state.start_time;

        Ok(StatusReport {
            orchestrator_id: self.id.clone(),
            status: state.status.clone(),
            uptime_seconds: uptime.num_seconds() as u64,
            total_agents: self.agents.len(),
            active_agents: agent_statuses
                .values()
                .filter(|s| matches!(s, AgentStatus::Available | AgentStatus::Working))
                .count(),
            total_tasks_processed: state.total_tasks_processed,
            successful_tasks: state.successful_tasks,
            failed_tasks: state.failed_tasks,
            pending_tasks: state.pending_tasks.len(),
            agent_statuses,
            timestamp: Utc::now(),
        })
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:608-644 method create_remediation_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:1349-1372 method set_objective
  Similarity: 87.70%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_remediation_task (lines 608-644) ---[0m
    async fn create_remediation_task(
        &self,
        original_task_id: &str,
        agent_id: &str,
        issues: Vec<String>,
    ) -> Result<Task> {
        // Generate specific fix instructions based on the issues
        let instructions = self.generate_fix_instructions(&issues).await?;

        // Create remediation task
        let task_id = format!("remediate-{}-{}", original_task_id, Uuid::new_v4());

        let description = format!(
            "Fix quality issues in task {}: {}",
            original_task_id, instructions
        );

        let details = format!(
            "Quality issues found:\n{}\n\nSpecific instructions:\n{}",
            issues.join("\n- "),
            instructions
        );

        let remediation_task = Task::new(
            task_id,
            description,
            crate::agent::Priority::High, // High priority for quality fixes
            TaskType::Remediation,
        )
        .with_details(details)
        .assign_to(agent_id.to_string())
        .with_parent_task(original_task_id.to_string())
        .with_quality_issues(issues)
        .with_duration(1800); // 30 minutes estimate

        Ok(remediation_task)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:set_objective (lines 1349-1372) ---[0m
    pub async fn set_objective(
        &self,
        title: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Objective;

        let objective_id = format!("obj-{}", uuid::Uuid::new_v4());
        let objective = Objective {
            id: objective_id.clone(),
            title,
            description,
            deadline,
            progress: 0.0,
            key_results: vec![],
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.set_objective(objective).await?;

        info!("Set project objective: {}", objective_id);
        Ok(objective_id)
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1483-1503 method enable_proactive_mode <-> ./crates/ccswarm/src/orchestrator/mod.rs:1566-1605 function create_test_config
  Similarity: 86.71%

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:enable_proactive_mode (lines 1483-1503) ---[0m
    pub async fn enable_proactive_mode(&self) -> Result<()> {
        info!("Enabling proactive mode - increasing analysis frequency");

        // Start additional proactive analysis loop with higher frequency
        let agents = self.agents.clone();
        let bus = self.coordination_bus.clone();
        let proactive_master = self.proactive_master.clone();

        tokio::spawn(async move {
            loop {
                tokio::time::sleep(std::time::Duration::from_secs(15)).await; // Every 15 seconds

                let proactive_guard = proactive_master.read().await;
                if let Err(e) = proactive_guard.analyze_and_decide(&agents, &bus).await {
                    error!("Error in high-frequency proactive analysis: {}", e);
                }
            }
        });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_test_config (lines 1566-1605) ---[0m
    fn create_test_config() -> CcswarmConfig {
        let mut agents = HashMap::new();
        agents.insert(
            "frontend".to_string(),
            crate::config::AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend".to_string(),
                branch: "feature/frontend".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        CcswarmConfig {
            project: crate::config::ProjectConfig {
                name: "Test Project".to_string(),
                repository: crate::config::RepositoryConfig {
                    url: "https://github.com/test/repo".to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: crate::config::MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.9,
                    think_mode: crate::config::ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: crate::config::CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        }
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:608-644 method create_remediation_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:1506-1531 method shutdown
  Similarity: 83.95%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_remediation_task (lines 608-644) ---[0m
    async fn create_remediation_task(
        &self,
        original_task_id: &str,
        agent_id: &str,
        issues: Vec<String>,
    ) -> Result<Task> {
        // Generate specific fix instructions based on the issues
        let instructions = self.generate_fix_instructions(&issues).await?;

        // Create remediation task
        let task_id = format!("remediate-{}-{}", original_task_id, Uuid::new_v4());

        let description = format!(
            "Fix quality issues in task {}: {}",
            original_task_id, instructions
        );

        let details = format!(
            "Quality issues found:\n{}\n\nSpecific instructions:\n{}",
            issues.join("\n- "),
            instructions
        );

        let remediation_task = Task::new(
            task_id,
            description,
            crate::agent::Priority::High, // High priority for quality fixes
            TaskType::Remediation,
        )
        .with_details(details)
        .assign_to(agent_id.to_string())
        .with_parent_task(original_task_id.to_string())
        .with_quality_issues(issues)
        .with_duration(1800); // 30 minutes estimate

        Ok(remediation_task)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:shutdown (lines 1506-1531) ---[0m
    pub async fn shutdown(&self) -> Result<()> {
        info!("Shutting down Master Claude orchestrator");

        // Update state
        {
            let mut state = self.state.write().await;
            state.status = OrchestratorStatus::ShuttingDown;
        }

        // Shutdown all agents
        for entry in self.agents.iter() {
            let mut agent = entry.value().clone();
            if let Err(e) = agent.shutdown().await {
                error!(
                    "Error shutting down agent {}: {}",
                    agent.identity.agent_id, e
                );
            }
        }

        // Close coordination bus
        self.coordination_bus.close().await?;

        info!("Master Claude shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:695-726 method check_agent_health <-> ./crates/ccswarm/src/orchestrator/mod.rs:1319-1346 method generate_status_report
  Similarity: 87.24%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:check_agent_health (lines 695-726) ---[0m
    async fn check_agent_health(&self) -> Result<()> {
        let mut unhealthy_agents = Vec::new();

        for entry in self.agents.iter() {
            let agent = entry.value();

            // Check if agent has been inactive for too long
            let inactive_duration = Utc::now() - agent.last_activity;
            if inactive_duration.num_seconds() > 300 {
                // 5 minutes
                warn!(
                    "Agent {} has been inactive for {} seconds",
                    agent.identity.agent_id,
                    inactive_duration.num_seconds()
                );
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }

            // Check for error status
            if matches!(agent.status, AgentStatus::Error(_)) {
                error!("Agent {} is in error state", agent.identity.agent_id);
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }
        }

        // Handle unhealthy agents
        for agent_id in unhealthy_agents {
            self.restart_agent(&agent_id).await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_status_report (lines 1319-1346) ---[0m
    pub async fn generate_status_report(&self) -> Result<StatusReport> {
        let state = self.state.read().await;

        let agent_statuses: HashMap<String, AgentStatus> = self
            .agents
            .iter()
            .map(|entry| (entry.key().clone(), entry.value().status.clone()))
            .collect();

        let uptime = Utc::now() - state.start_time;

        Ok(StatusReport {
            orchestrator_id: self.id.clone(),
            status: state.status.clone(),
            uptime_seconds: uptime.num_seconds() as u64,
            total_agents: self.agents.len(),
            active_agents: agent_statuses
                .values()
                .filter(|s| matches!(s, AgentStatus::Available | AgentStatus::Working))
                .count(),
            total_tasks_processed: state.total_tasks_processed,
            successful_tasks: state.successful_tasks,
            failed_tasks: state.failed_tasks,
            pending_tasks: state.pending_tasks.len(),
            agent_statuses,
            timestamp: Utc::now(),
        })
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:695-726 method check_agent_health <-> ./crates/ccswarm/src/orchestrator/mod.rs:1417-1440 method get_proactive_insights_for_task
  Similarity: 93.21%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:check_agent_health (lines 695-726) ---[0m
    async fn check_agent_health(&self) -> Result<()> {
        let mut unhealthy_agents = Vec::new();

        for entry in self.agents.iter() {
            let agent = entry.value();

            // Check if agent has been inactive for too long
            let inactive_duration = Utc::now() - agent.last_activity;
            if inactive_duration.num_seconds() > 300 {
                // 5 minutes
                warn!(
                    "Agent {} has been inactive for {} seconds",
                    agent.identity.agent_id,
                    inactive_duration.num_seconds()
                );
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }

            // Check for error status
            if matches!(agent.status, AgentStatus::Error(_)) {
                error!("Agent {} is in error state", agent.identity.agent_id);
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }
        }

        // Handle unhealthy agents
        for agent_id in unhealthy_agents {
            self.restart_agent(&agent_id).await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:get_proactive_insights_for_task (lines 1417-1440) ---[0m
    async fn get_proactive_insights_for_task(&self, task: &Task) -> serde_json::Value {
        let _proactive_master = self.proactive_master.read().await;

        // Get relevant insights from proactive master
        let insights = serde_json::json!({
            "task_complexity": match task.task_type {
                TaskType::Feature => "high",
                TaskType::Infrastructure => "high",
                TaskType::Development => "medium",
                TaskType::Testing => "medium",
                TaskType::Bug | TaskType::Bugfix => "low",
                _ => "medium",
            },
            "recommended_approach": match task.priority {
                crate::agent::Priority::Critical => "orchestrated_execution",
                crate::agent::Priority::High => "careful_planning",
                _ => "standard_execution",
            },
            "potential_dependencies": self.identify_task_dependencies(task).await,
            "similar_tasks_completed": self.find_similar_completed_tasks(task).await,
        });

        insights
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:648-692 method generate_fix_instructions <-> ./crates/ccswarm/src/orchestrator/mod.rs:1443-1463 method identify_task_dependencies
  Similarity: 79.07%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_fix_instructions (lines 648-692) ---[0m
    async fn generate_fix_instructions(&self, issues: &[String]) -> Result<String> {
        // For now, generate instructions based on the issue types
        // In a full implementation, this would use Claude API to generate intelligent instructions

        let mut instructions = Vec::new();

        for issue in issues {
            let instruction = match issue.as_str() {
                "Low test coverage" => {
                    "1. Add unit tests to achieve at least 85% coverage\n\
                     2. Focus on edge cases and error paths\n\
                     3. Use test-driven development for any new code\n\
                     4. Run coverage report and ensure all critical paths are tested"
                }
                "High complexity" => {
                    "1. Break down complex functions into smaller, focused functions\n\
                     2. Extract repeated logic into helper functions\n\
                     3. Simplify conditional logic using early returns\n\
                     4. Consider using design patterns to reduce complexity"
                }
                "Security vulnerability" => {
                    "1. Review and fix all security warnings\n\
                     2. Validate all user inputs\n\
                     3. Use parameterized queries for database operations\n\
                     4. Update dependencies to latest secure versions"
                }
                "Missing documentation" => {
                    "1. Add comprehensive docstrings to all public functions\n\
                     2. Include parameter descriptions and return value documentation\n\
                     3. Add usage examples where appropriate\n\
                     4. Update README with any new functionality"
                }
                _ => {
                    "1. Review the specific issue and determine root cause\n\
                     2. Implement the most appropriate fix\n\
                     3. Add tests to prevent regression\n\
                     4. Document the changes made"
                }
            };

            instructions.push(format!("For '{}': \n{}", issue, instruction));
        }

        Ok(instructions.join("\n\n"))
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:identify_task_dependencies (lines 1443-1463) ---[0m
    async fn identify_task_dependencies(&self, task: &Task) -> Vec<String> {
        let mut dependencies = Vec::new();

        // Check task description for dependency indicators
        let description_lower = task.description.to_lowercase();

        if description_lower.contains("api") || description_lower.contains("endpoint") {
            dependencies.push("backend_api".to_string());
        }
        if description_lower.contains("ui") || description_lower.contains("component") {
            dependencies.push("frontend_components".to_string());
        }
        if description_lower.contains("database") || description_lower.contains("migration") {
            dependencies.push("database_schema".to_string());
        }
        if description_lower.contains("deploy") || description_lower.contains("infrastructure") {
            dependencies.push("deployment_pipeline".to_string());
        }

        dependencies
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:608-644 method create_remediation_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:695-726 method check_agent_health
  Similarity: 74.96%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_remediation_task (lines 608-644) ---[0m
    async fn create_remediation_task(
        &self,
        original_task_id: &str,
        agent_id: &str,
        issues: Vec<String>,
    ) -> Result<Task> {
        // Generate specific fix instructions based on the issues
        let instructions = self.generate_fix_instructions(&issues).await?;

        // Create remediation task
        let task_id = format!("remediate-{}-{}", original_task_id, Uuid::new_v4());

        let description = format!(
            "Fix quality issues in task {}: {}",
            original_task_id, instructions
        );

        let details = format!(
            "Quality issues found:\n{}\n\nSpecific instructions:\n{}",
            issues.join("\n- "),
            instructions
        );

        let remediation_task = Task::new(
            task_id,
            description,
            crate::agent::Priority::High, // High priority for quality fixes
            TaskType::Remediation,
        )
        .with_details(details)
        .assign_to(agent_id.to_string())
        .with_parent_task(original_task_id.to_string())
        .with_quality_issues(issues)
        .with_duration(1800); // 30 minutes estimate

        Ok(remediation_task)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:check_agent_health (lines 695-726) ---[0m
    async fn check_agent_health(&self) -> Result<()> {
        let mut unhealthy_agents = Vec::new();

        for entry in self.agents.iter() {
            let agent = entry.value();

            // Check if agent has been inactive for too long
            let inactive_duration = Utc::now() - agent.last_activity;
            if inactive_duration.num_seconds() > 300 {
                // 5 minutes
                warn!(
                    "Agent {} has been inactive for {} seconds",
                    agent.identity.agent_id,
                    inactive_duration.num_seconds()
                );
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }

            // Check for error status
            if matches!(agent.status, AgentStatus::Error(_)) {
                error!("Agent {} is in error state", agent.identity.agent_id);
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }
        }

        // Handle unhealthy agents
        for agent_id in unhealthy_agents {
            self.restart_agent(&agent_id).await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:145-186 method new <-> ./crates/ccswarm/src/orchestrator/mod.rs:1443-1463 method identify_task_dependencies
  Similarity: 81.41%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:new (lines 145-186) ---[0m
    pub async fn new(config: CcswarmConfig, repo_path: PathBuf) -> Result<Self> {
        let id = format!("master-claude-{}", Uuid::new_v4());

        // Create channels
        let (task_queue_tx, task_queue_rx) = async_channel::bounded(1000);

        // Initialize worktree manager
        let worktree_manager = Arc::new(WorktreeManager::new(repo_path).unwrap());

        // Initialize coordination bus
        let coordination_bus = Arc::new(CoordinationBus::new().await?);

        // Create initial state
        let state = Arc::new(RwLock::new(OrchestratorState {
            status: OrchestratorStatus::Initializing,
            start_time: Utc::now(),
            total_tasks_processed: 0,
            successful_tasks: 0,
            failed_tasks: 0,
            active_agents: Vec::new(),
            pending_tasks: Vec::new(),
            review_history: HashMap::new(),
        }));

        let quality_judge = Arc::new(RwLock::new(LLMQualityJudge::new()));
        let proactive_master = Arc::new(RwLock::new(ProactiveMaster::new().await?));

        Ok(Self {
            id,
            config: config.clone(),
            agents: Arc::new(DashMap::new()),
            task_queue_tx,
            task_queue_rx,
            quality_standards: config.project.master_claude.quality_threshold.into(),
            coordination_bus,
            worktree_manager,
            state,
            quality_judge,
            proactive_master,
            isolation_mode: crate::agent::IsolationMode::default(),
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:identify_task_dependencies (lines 1443-1463) ---[0m
    async fn identify_task_dependencies(&self, task: &Task) -> Vec<String> {
        let mut dependencies = Vec::new();

        // Check task description for dependency indicators
        let description_lower = task.description.to_lowercase();

        if description_lower.contains("api") || description_lower.contains("endpoint") {
            dependencies.push("backend_api".to_string());
        }
        if description_lower.contains("ui") || description_lower.contains("component") {
            dependencies.push("frontend_components".to_string());
        }
        if description_lower.contains("database") || description_lower.contains("migration") {
            dependencies.push("database_schema".to_string());
        }
        if description_lower.contains("deploy") || description_lower.contains("infrastructure") {
            dependencies.push("deployment_pipeline".to_string());
        }

        dependencies
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1319-1346 method generate_status_report <-> ./crates/ccswarm/src/orchestrator/mod.rs:1417-1440 method get_proactive_insights_for_task
  Similarity: 97.28%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_status_report (lines 1319-1346) ---[0m
    pub async fn generate_status_report(&self) -> Result<StatusReport> {
        let state = self.state.read().await;

        let agent_statuses: HashMap<String, AgentStatus> = self
            .agents
            .iter()
            .map(|entry| (entry.key().clone(), entry.value().status.clone()))
            .collect();

        let uptime = Utc::now() - state.start_time;

        Ok(StatusReport {
            orchestrator_id: self.id.clone(),
            status: state.status.clone(),
            uptime_seconds: uptime.num_seconds() as u64,
            total_agents: self.agents.len(),
            active_agents: agent_statuses
                .values()
                .filter(|s| matches!(s, AgentStatus::Available | AgentStatus::Working))
                .count(),
            total_tasks_processed: state.total_tasks_processed,
            successful_tasks: state.successful_tasks,
            failed_tasks: state.failed_tasks,
            pending_tasks: state.pending_tasks.len(),
            agent_statuses,
            timestamp: Utc::now(),
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:get_proactive_insights_for_task (lines 1417-1440) ---[0m
    async fn get_proactive_insights_for_task(&self, task: &Task) -> serde_json::Value {
        let _proactive_master = self.proactive_master.read().await;

        // Get relevant insights from proactive master
        let insights = serde_json::json!({
            "task_complexity": match task.task_type {
                TaskType::Feature => "high",
                TaskType::Infrastructure => "high",
                TaskType::Development => "medium",
                TaskType::Testing => "medium",
                TaskType::Bug | TaskType::Bugfix => "low",
                _ => "medium",
            },
            "recommended_approach": match task.priority {
                crate::agent::Priority::Critical => "orchestrated_execution",
                crate::agent::Priority::High => "careful_planning",
                _ => "standard_execution",
            },
            "potential_dependencies": self.identify_task_dependencies(task).await,
            "similar_tasks_completed": self.find_similar_completed_tasks(task).await,
        });

        insights
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:608-644 method create_remediation_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:1483-1503 method enable_proactive_mode
  Similarity: 86.40%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_remediation_task (lines 608-644) ---[0m
    async fn create_remediation_task(
        &self,
        original_task_id: &str,
        agent_id: &str,
        issues: Vec<String>,
    ) -> Result<Task> {
        // Generate specific fix instructions based on the issues
        let instructions = self.generate_fix_instructions(&issues).await?;

        // Create remediation task
        let task_id = format!("remediate-{}-{}", original_task_id, Uuid::new_v4());

        let description = format!(
            "Fix quality issues in task {}: {}",
            original_task_id, instructions
        );

        let details = format!(
            "Quality issues found:\n{}\n\nSpecific instructions:\n{}",
            issues.join("\n- "),
            instructions
        );

        let remediation_task = Task::new(
            task_id,
            description,
            crate::agent::Priority::High, // High priority for quality fixes
            TaskType::Remediation,
        )
        .with_details(details)
        .assign_to(agent_id.to_string())
        .with_parent_task(original_task_id.to_string())
        .with_quality_issues(issues)
        .with_duration(1800); // 30 minutes estimate

        Ok(remediation_task)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:enable_proactive_mode (lines 1483-1503) ---[0m
    pub async fn enable_proactive_mode(&self) -> Result<()> {
        info!("Enabling proactive mode - increasing analysis frequency");

        // Start additional proactive analysis loop with higher frequency
        let agents = self.agents.clone();
        let bus = self.coordination_bus.clone();
        let proactive_master = self.proactive_master.clone();

        tokio::spawn(async move {
            loop {
                tokio::time::sleep(std::time::Duration::from_secs(15)).await; // Every 15 seconds

                let proactive_guard = proactive_master.read().await;
                if let Err(e) = proactive_guard.analyze_and_decide(&agents, &bus).await {
                    error!("Error in high-frequency proactive analysis: {}", e);
                }
            }
        });

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1277-1316 method perform_quality_review <-> ./crates/ccswarm/src/orchestrator/mod.rs:1506-1531 method shutdown
  Similarity: 75.82%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_quality_review (lines 1277-1316) ---[0m
    async fn perform_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Performing basic quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Basic placeholder logic
                    let quality_score = 0.92; // Placeholder

                    if quality_score < (standards.min_test_coverage / 100.0) {
                        warn!(
                            "Task {} from agent {} failed quality standards: score={}",
                            task.id, agent.identity.agent_id, quality_score
                        );

                        bus.send_message(AgentMessage::QualityIssue {
                            agent_id: agent.identity.agent_id.clone(),
                            task_id: task.id.clone(),
                            issues: vec!["Low test coverage".to_string()],
                        })
                        .await?;
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:shutdown (lines 1506-1531) ---[0m
    pub async fn shutdown(&self) -> Result<()> {
        info!("Shutting down Master Claude orchestrator");

        // Update state
        {
            let mut state = self.state.write().await;
            state.status = OrchestratorStatus::ShuttingDown;
        }

        // Shutdown all agents
        for entry in self.agents.iter() {
            let mut agent = entry.value().clone();
            if let Err(e) = agent.shutdown().await {
                error!(
                    "Error shutting down agent {}: {}",
                    agent.identity.agent_id, e
                );
            }
        }

        // Close coordination bus
        self.coordination_bus.close().await?;

        info!("Master Claude shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:368-394 method coordinate_development <-> ./crates/ccswarm/src/orchestrator/mod.rs:695-726 method check_agent_health
  Similarity: 82.95%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:coordinate_development (lines 368-394) ---[0m
    async fn coordinate_development(&self) -> Result<()> {
        info!("Master Claude coordination loop started");

        loop {
            tokio::select! {
                // Handle incoming tasks
                Ok(task) = self.task_queue_rx.recv() => {
                    if let Err(e) = self.assign_task(task).await {
                        error!("Error assigning task: {}", e);
                    }
                }

                // Periodic status check
                _ = tokio::time::sleep(std::time::Duration::from_secs(10)) => {
                    self.check_agent_health().await?;
                }
            }

            // Check if we should shutdown
            let state = self.state.read().await;
            if state.status == OrchestratorStatus::ShuttingDown {
                break;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:check_agent_health (lines 695-726) ---[0m
    async fn check_agent_health(&self) -> Result<()> {
        let mut unhealthy_agents = Vec::new();

        for entry in self.agents.iter() {
            let agent = entry.value();

            // Check if agent has been inactive for too long
            let inactive_duration = Utc::now() - agent.last_activity;
            if inactive_duration.num_seconds() > 300 {
                // 5 minutes
                warn!(
                    "Agent {} has been inactive for {} seconds",
                    agent.identity.agent_id,
                    inactive_duration.num_seconds()
                );
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }

            // Check for error status
            if matches!(agent.status, AgentStatus::Error(_)) {
                error!("Agent {} is in error state", agent.identity.agent_id);
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }
        }

        // Handle unhealthy agents
        for agent_id in unhealthy_agents {
            self.restart_agent(&agent_id).await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:695-726 method check_agent_health <-> ./crates/ccswarm/src/orchestrator/mod.rs:1506-1531 method shutdown
  Similarity: 84.06%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:check_agent_health (lines 695-726) ---[0m
    async fn check_agent_health(&self) -> Result<()> {
        let mut unhealthy_agents = Vec::new();

        for entry in self.agents.iter() {
            let agent = entry.value();

            // Check if agent has been inactive for too long
            let inactive_duration = Utc::now() - agent.last_activity;
            if inactive_duration.num_seconds() > 300 {
                // 5 minutes
                warn!(
                    "Agent {} has been inactive for {} seconds",
                    agent.identity.agent_id,
                    inactive_duration.num_seconds()
                );
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }

            // Check for error status
            if matches!(agent.status, AgentStatus::Error(_)) {
                error!("Agent {} is in error state", agent.identity.agent_id);
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }
        }

        // Handle unhealthy agents
        for agent_id in unhealthy_agents {
            self.restart_agent(&agent_id).await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:shutdown (lines 1506-1531) ---[0m
    pub async fn shutdown(&self) -> Result<()> {
        info!("Shutting down Master Claude orchestrator");

        // Update state
        {
            let mut state = self.state.write().await;
            state.status = OrchestratorStatus::ShuttingDown;
        }

        // Shutdown all agents
        for entry in self.agents.iter() {
            let mut agent = entry.value().clone();
            if let Err(e) = agent.shutdown().await {
                error!(
                    "Error shutting down agent {}: {}",
                    agent.identity.agent_id, e
                );
            }
        }

        // Close coordination bus
        self.coordination_bus.close().await?;

        info!("Master Claude shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1277-1316 method perform_quality_review <-> ./crates/ccswarm/src/orchestrator/mod.rs:1466-1480 method find_similar_completed_tasks
  Similarity: 88.16%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_quality_review (lines 1277-1316) ---[0m
    async fn perform_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Performing basic quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Basic placeholder logic
                    let quality_score = 0.92; // Placeholder

                    if quality_score < (standards.min_test_coverage / 100.0) {
                        warn!(
                            "Task {} from agent {} failed quality standards: score={}",
                            task.id, agent.identity.agent_id, quality_score
                        );

                        bus.send_message(AgentMessage::QualityIssue {
                            agent_id: agent.identity.agent_id.clone(),
                            task_id: task.id.clone(),
                            issues: vec!["Low test coverage".to_string()],
                        })
                        .await?;
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:find_similar_completed_tasks (lines 1466-1480) ---[0m
    async fn find_similar_completed_tasks(&self, task: &Task) -> Vec<String> {
        let state = self.state.read().await;
        let mut similar_tasks = Vec::new();

        // Look through review history for similar task types
        for (task_id, _) in state.review_history.iter().take(10) {
            // Use debug format for TaskType since it doesn't implement Display
            let task_type_str = format!("{:?}", task.task_type).to_lowercase();
            if task_id.contains(&task_type_str) {
                similar_tasks.push(task_id.clone());
            }
        }

        similar_tasks
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:608-644 method create_remediation_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:1417-1440 method get_proactive_insights_for_task
  Similarity: 78.70%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_remediation_task (lines 608-644) ---[0m
    async fn create_remediation_task(
        &self,
        original_task_id: &str,
        agent_id: &str,
        issues: Vec<String>,
    ) -> Result<Task> {
        // Generate specific fix instructions based on the issues
        let instructions = self.generate_fix_instructions(&issues).await?;

        // Create remediation task
        let task_id = format!("remediate-{}-{}", original_task_id, Uuid::new_v4());

        let description = format!(
            "Fix quality issues in task {}: {}",
            original_task_id, instructions
        );

        let details = format!(
            "Quality issues found:\n{}\n\nSpecific instructions:\n{}",
            issues.join("\n- "),
            instructions
        );

        let remediation_task = Task::new(
            task_id,
            description,
            crate::agent::Priority::High, // High priority for quality fixes
            TaskType::Remediation,
        )
        .with_details(details)
        .assign_to(agent_id.to_string())
        .with_parent_task(original_task_id.to_string())
        .with_quality_issues(issues)
        .with_duration(1800); // 30 minutes estimate

        Ok(remediation_task)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:get_proactive_insights_for_task (lines 1417-1440) ---[0m
    async fn get_proactive_insights_for_task(&self, task: &Task) -> serde_json::Value {
        let _proactive_master = self.proactive_master.read().await;

        // Get relevant insights from proactive master
        let insights = serde_json::json!({
            "task_complexity": match task.task_type {
                TaskType::Feature => "high",
                TaskType::Infrastructure => "high",
                TaskType::Development => "medium",
                TaskType::Testing => "medium",
                TaskType::Bug | TaskType::Bugfix => "low",
                _ => "medium",
            },
            "recommended_approach": match task.priority {
                crate::agent::Priority::Critical => "orchestrated_execution",
                crate::agent::Priority::High => "careful_planning",
                _ => "standard_execution",
            },
            "potential_dependencies": self.identify_task_dependencies(task).await,
            "similar_tasks_completed": self.find_similar_completed_tasks(task).await,
        });

        insights
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:368-394 method coordinate_development <-> ./crates/ccswarm/src/orchestrator/mod.rs:1417-1440 method get_proactive_insights_for_task
  Similarity: 93.03%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:coordinate_development (lines 368-394) ---[0m
    async fn coordinate_development(&self) -> Result<()> {
        info!("Master Claude coordination loop started");

        loop {
            tokio::select! {
                // Handle incoming tasks
                Ok(task) = self.task_queue_rx.recv() => {
                    if let Err(e) = self.assign_task(task).await {
                        error!("Error assigning task: {}", e);
                    }
                }

                // Periodic status check
                _ = tokio::time::sleep(std::time::Duration::from_secs(10)) => {
                    self.check_agent_health().await?;
                }
            }

            // Check if we should shutdown
            let state = self.state.read().await;
            if state.status == OrchestratorStatus::ShuttingDown {
                break;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:get_proactive_insights_for_task (lines 1417-1440) ---[0m
    async fn get_proactive_insights_for_task(&self, task: &Task) -> serde_json::Value {
        let _proactive_master = self.proactive_master.read().await;

        // Get relevant insights from proactive master
        let insights = serde_json::json!({
            "task_complexity": match task.task_type {
                TaskType::Feature => "high",
                TaskType::Infrastructure => "high",
                TaskType::Development => "medium",
                TaskType::Testing => "medium",
                TaskType::Bug | TaskType::Bugfix => "low",
                _ => "medium",
            },
            "recommended_approach": match task.priority {
                crate::agent::Priority::Critical => "orchestrated_execution",
                crate::agent::Priority::High => "careful_planning",
                _ => "standard_execution",
            },
            "potential_dependencies": self.identify_task_dependencies(task).await,
            "similar_tasks_completed": self.find_similar_completed_tasks(task).await,
        });

        insights
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:368-394 method coordinate_development <-> ./crates/ccswarm/src/orchestrator/mod.rs:1319-1346 method generate_status_report
  Similarity: 85.17%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:coordinate_development (lines 368-394) ---[0m
    async fn coordinate_development(&self) -> Result<()> {
        info!("Master Claude coordination loop started");

        loop {
            tokio::select! {
                // Handle incoming tasks
                Ok(task) = self.task_queue_rx.recv() => {
                    if let Err(e) = self.assign_task(task).await {
                        error!("Error assigning task: {}", e);
                    }
                }

                // Periodic status check
                _ = tokio::time::sleep(std::time::Duration::from_secs(10)) => {
                    self.check_agent_health().await?;
                }
            }

            // Check if we should shutdown
            let state = self.state.read().await;
            if state.status == OrchestratorStatus::ShuttingDown {
                break;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_status_report (lines 1319-1346) ---[0m
    pub async fn generate_status_report(&self) -> Result<StatusReport> {
        let state = self.state.read().await;

        let agent_statuses: HashMap<String, AgentStatus> = self
            .agents
            .iter()
            .map(|entry| (entry.key().clone(), entry.value().status.clone()))
            .collect();

        let uptime = Utc::now() - state.start_time;

        Ok(StatusReport {
            orchestrator_id: self.id.clone(),
            status: state.status.clone(),
            uptime_seconds: uptime.num_seconds() as u64,
            total_agents: self.agents.len(),
            active_agents: agent_statuses
                .values()
                .filter(|s| matches!(s, AgentStatus::Available | AgentStatus::Working))
                .count(),
            total_tasks_processed: state.total_tasks_processed,
            successful_tasks: state.successful_tasks,
            failed_tasks: state.failed_tasks,
            pending_tasks: state.pending_tasks.len(),
            agent_statuses,
            timestamp: Utc::now(),
        })
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:608-644 method create_remediation_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:1443-1463 method identify_task_dependencies
  Similarity: 80.61%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_remediation_task (lines 608-644) ---[0m
    async fn create_remediation_task(
        &self,
        original_task_id: &str,
        agent_id: &str,
        issues: Vec<String>,
    ) -> Result<Task> {
        // Generate specific fix instructions based on the issues
        let instructions = self.generate_fix_instructions(&issues).await?;

        // Create remediation task
        let task_id = format!("remediate-{}-{}", original_task_id, Uuid::new_v4());

        let description = format!(
            "Fix quality issues in task {}: {}",
            original_task_id, instructions
        );

        let details = format!(
            "Quality issues found:\n{}\n\nSpecific instructions:\n{}",
            issues.join("\n- "),
            instructions
        );

        let remediation_task = Task::new(
            task_id,
            description,
            crate::agent::Priority::High, // High priority for quality fixes
            TaskType::Remediation,
        )
        .with_details(details)
        .assign_to(agent_id.to_string())
        .with_parent_task(original_task_id.to_string())
        .with_quality_issues(issues)
        .with_duration(1800); // 30 minutes estimate

        Ok(remediation_task)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:identify_task_dependencies (lines 1443-1463) ---[0m
    async fn identify_task_dependencies(&self, task: &Task) -> Vec<String> {
        let mut dependencies = Vec::new();

        // Check task description for dependency indicators
        let description_lower = task.description.to_lowercase();

        if description_lower.contains("api") || description_lower.contains("endpoint") {
            dependencies.push("backend_api".to_string());
        }
        if description_lower.contains("ui") || description_lower.contains("component") {
            dependencies.push("frontend_components".to_string());
        }
        if description_lower.contains("database") || description_lower.contains("migration") {
            dependencies.push("database_schema".to_string());
        }
        if description_lower.contains("deploy") || description_lower.contains("infrastructure") {
            dependencies.push("deployment_pipeline".to_string());
        }

        dependencies
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1349-1372 method set_objective <-> ./crates/ccswarm/src/orchestrator/mod.rs:1375-1399 method add_milestone
  Similarity: 95.28%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:set_objective (lines 1349-1372) ---[0m
    pub async fn set_objective(
        &self,
        title: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Objective;

        let objective_id = format!("obj-{}", uuid::Uuid::new_v4());
        let objective = Objective {
            id: objective_id.clone(),
            title,
            description,
            deadline,
            progress: 0.0,
            key_results: vec![],
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.set_objective(objective).await?;

        info!("Set project objective: {}", objective_id);
        Ok(objective_id)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:add_milestone (lines 1375-1399) ---[0m
    pub async fn add_milestone(
        &self,
        name: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Milestone;

        let milestone_id = format!("milestone-{}", uuid::Uuid::new_v4());
        let milestone = Milestone {
            id: milestone_id.clone(),
            name,
            description,
            deadline,
            completion_percentage: 0.0,
            dependencies: vec![],
            critical_path: false,
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.add_milestone(milestone).await?;

        info!("Added milestone: {}", milestone_id);
        Ok(milestone_id)
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1277-1316 method perform_quality_review <-> ./crates/ccswarm/src/orchestrator/mod.rs:1483-1503 method enable_proactive_mode
  Similarity: 76.19%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_quality_review (lines 1277-1316) ---[0m
    async fn perform_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Performing basic quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Basic placeholder logic
                    let quality_score = 0.92; // Placeholder

                    if quality_score < (standards.min_test_coverage / 100.0) {
                        warn!(
                            "Task {} from agent {} failed quality standards: score={}",
                            task.id, agent.identity.agent_id, quality_score
                        );

                        bus.send_message(AgentMessage::QualityIssue {
                            agent_id: agent.identity.agent_id.clone(),
                            task_id: task.id.clone(),
                            issues: vec!["Low test coverage".to_string()],
                        })
                        .await?;
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:enable_proactive_mode (lines 1483-1503) ---[0m
    pub async fn enable_proactive_mode(&self) -> Result<()> {
        info!("Enabling proactive mode - increasing analysis frequency");

        // Start additional proactive analysis loop with higher frequency
        let agents = self.agents.clone();
        let bus = self.coordination_bus.clone();
        let proactive_master = self.proactive_master.clone();

        tokio::spawn(async move {
            loop {
                tokio::time::sleep(std::time::Duration::from_secs(15)).await; // Every 15 seconds

                let proactive_guard = proactive_master.read().await;
                if let Err(e) = proactive_guard.analyze_and_decide(&agents, &bus).await {
                    error!("Error in high-frequency proactive analysis: {}", e);
                }
            }
        });

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1277-1316 method perform_quality_review <-> ./crates/ccswarm/src/orchestrator/mod.rs:1375-1399 method add_milestone
  Similarity: 70.82%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_quality_review (lines 1277-1316) ---[0m
    async fn perform_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Performing basic quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Basic placeholder logic
                    let quality_score = 0.92; // Placeholder

                    if quality_score < (standards.min_test_coverage / 100.0) {
                        warn!(
                            "Task {} from agent {} failed quality standards: score={}",
                            task.id, agent.identity.agent_id, quality_score
                        );

                        bus.send_message(AgentMessage::QualityIssue {
                            agent_id: agent.identity.agent_id.clone(),
                            task_id: task.id.clone(),
                            issues: vec!["Low test coverage".to_string()],
                        })
                        .await?;
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:add_milestone (lines 1375-1399) ---[0m
    pub async fn add_milestone(
        &self,
        name: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Milestone;

        let milestone_id = format!("milestone-{}", uuid::Uuid::new_v4());
        let milestone = Milestone {
            id: milestone_id.clone(),
            name,
            description,
            deadline,
            completion_percentage: 0.0,
            dependencies: vec![],
            critical_path: false,
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.add_milestone(milestone).await?;

        info!("Added milestone: {}", milestone_id);
        Ok(milestone_id)
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:608-644 method create_remediation_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:1608-1624 function test_master_claude_creation
  Similarity: 85.10%

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_remediation_task (lines 608-644) ---[0m
    async fn create_remediation_task(
        &self,
        original_task_id: &str,
        agent_id: &str,
        issues: Vec<String>,
    ) -> Result<Task> {
        // Generate specific fix instructions based on the issues
        let instructions = self.generate_fix_instructions(&issues).await?;

        // Create remediation task
        let task_id = format!("remediate-{}-{}", original_task_id, Uuid::new_v4());

        let description = format!(
            "Fix quality issues in task {}: {}",
            original_task_id, instructions
        );

        let details = format!(
            "Quality issues found:\n{}\n\nSpecific instructions:\n{}",
            issues.join("\n- "),
            instructions
        );

        let remediation_task = Task::new(
            task_id,
            description,
            crate::agent::Priority::High, // High priority for quality fixes
            TaskType::Remediation,
        )
        .with_details(details)
        .assign_to(agent_id.to_string())
        .with_parent_task(original_task_id.to_string())
        .with_quality_issues(issues)
        .with_duration(1800); // 30 minutes estimate

        Ok(remediation_task)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:test_master_claude_creation (lines 1608-1624) ---[0m
    async fn test_master_claude_creation() {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Initialize git repo
        crate::git::WorktreeManager::init_if_needed(&repo_path)
            .await
            .unwrap();

        let config = create_test_config();
        let master = MasterClaude::new(config, repo_path).await.unwrap();

        assert!(master.id.starts_with("master-claude-"));

        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Initializing);
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1277-1316 method perform_quality_review <-> ./crates/ccswarm/src/orchestrator/mod.rs:1349-1372 method set_objective
  Similarity: 70.82%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:perform_quality_review (lines 1277-1316) ---[0m
    async fn perform_quality_review(
        agents: &DashMap<String, ClaudeCodeAgent>,
        standards: &QualityStandards,
        bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Performing basic quality review");

        for entry in agents.iter() {
            let agent = entry.value();

            // Skip if agent hasn't completed any tasks
            if agent.task_history.is_empty() {
                continue;
            }

            // Review recent completed tasks
            for (task, result) in agent.task_history.iter().rev().take(5) {
                if result.success {
                    // Basic placeholder logic
                    let quality_score = 0.92; // Placeholder

                    if quality_score < (standards.min_test_coverage / 100.0) {
                        warn!(
                            "Task {} from agent {} failed quality standards: score={}",
                            task.id, agent.identity.agent_id, quality_score
                        );

                        bus.send_message(AgentMessage::QualityIssue {
                            agent_id: agent.identity.agent_id.clone(),
                            task_id: task.id.clone(),
                            issues: vec!["Low test coverage".to_string()],
                        })
                        .await?;
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:set_objective (lines 1349-1372) ---[0m
    pub async fn set_objective(
        &self,
        title: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Objective;

        let objective_id = format!("obj-{}", uuid::Uuid::new_v4());
        let objective = Objective {
            id: objective_id.clone(),
            title,
            description,
            deadline,
            progress: 0.0,
            key_results: vec![],
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.set_objective(objective).await?;

        info!("Set project objective: {}", objective_id);
        Ok(objective_id)
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:695-726 method check_agent_health <-> ./crates/ccswarm/src/orchestrator/mod.rs:1483-1503 method enable_proactive_mode
  Similarity: 81.67%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:check_agent_health (lines 695-726) ---[0m
    async fn check_agent_health(&self) -> Result<()> {
        let mut unhealthy_agents = Vec::new();

        for entry in self.agents.iter() {
            let agent = entry.value();

            // Check if agent has been inactive for too long
            let inactive_duration = Utc::now() - agent.last_activity;
            if inactive_duration.num_seconds() > 300 {
                // 5 minutes
                warn!(
                    "Agent {} has been inactive for {} seconds",
                    agent.identity.agent_id,
                    inactive_duration.num_seconds()
                );
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }

            // Check for error status
            if matches!(agent.status, AgentStatus::Error(_)) {
                error!("Agent {} is in error state", agent.identity.agent_id);
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }
        }

        // Handle unhealthy agents
        for agent_id in unhealthy_agents {
            self.restart_agent(&agent_id).await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:enable_proactive_mode (lines 1483-1503) ---[0m
    pub async fn enable_proactive_mode(&self) -> Result<()> {
        info!("Enabling proactive mode - increasing analysis frequency");

        // Start additional proactive analysis loop with higher frequency
        let agents = self.agents.clone();
        let bus = self.coordination_bus.clone();
        let proactive_master = self.proactive_master.clone();

        tokio::spawn(async move {
            loop {
                tokio::time::sleep(std::time::Duration::from_secs(15)).await; // Every 15 seconds

                let proactive_guard = proactive_master.read().await;
                if let Err(e) = proactive_guard.analyze_and_decide(&agents, &bus).await {
                    error!("Error in high-frequency proactive analysis: {}", e);
                }
            }
        });

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:695-726 method check_agent_health <-> ./crates/ccswarm/src/orchestrator/mod.rs:1375-1399 method add_milestone
  Similarity: 75.64%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:check_agent_health (lines 695-726) ---[0m
    async fn check_agent_health(&self) -> Result<()> {
        let mut unhealthy_agents = Vec::new();

        for entry in self.agents.iter() {
            let agent = entry.value();

            // Check if agent has been inactive for too long
            let inactive_duration = Utc::now() - agent.last_activity;
            if inactive_duration.num_seconds() > 300 {
                // 5 minutes
                warn!(
                    "Agent {} has been inactive for {} seconds",
                    agent.identity.agent_id,
                    inactive_duration.num_seconds()
                );
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }

            // Check for error status
            if matches!(agent.status, AgentStatus::Error(_)) {
                error!("Agent {} is in error state", agent.identity.agent_id);
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }
        }

        // Handle unhealthy agents
        for agent_id in unhealthy_agents {
            self.restart_agent(&agent_id).await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:add_milestone (lines 1375-1399) ---[0m
    pub async fn add_milestone(
        &self,
        name: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Milestone;

        let milestone_id = format!("milestone-{}", uuid::Uuid::new_v4());
        let milestone = Milestone {
            id: milestone_id.clone(),
            name,
            description,
            deadline,
            completion_percentage: 0.0,
            dependencies: vec![],
            critical_path: false,
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.add_milestone(milestone).await?;

        info!("Added milestone: {}", milestone_id);
        Ok(milestone_id)
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1319-1346 method generate_status_report <-> ./crates/ccswarm/src/orchestrator/mod.rs:1506-1531 method shutdown
  Similarity: 78.93%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_status_report (lines 1319-1346) ---[0m
    pub async fn generate_status_report(&self) -> Result<StatusReport> {
        let state = self.state.read().await;

        let agent_statuses: HashMap<String, AgentStatus> = self
            .agents
            .iter()
            .map(|entry| (entry.key().clone(), entry.value().status.clone()))
            .collect();

        let uptime = Utc::now() - state.start_time;

        Ok(StatusReport {
            orchestrator_id: self.id.clone(),
            status: state.status.clone(),
            uptime_seconds: uptime.num_seconds() as u64,
            total_agents: self.agents.len(),
            active_agents: agent_statuses
                .values()
                .filter(|s| matches!(s, AgentStatus::Available | AgentStatus::Working))
                .count(),
            total_tasks_processed: state.total_tasks_processed,
            successful_tasks: state.successful_tasks,
            failed_tasks: state.failed_tasks,
            pending_tasks: state.pending_tasks.len(),
            agent_statuses,
            timestamp: Utc::now(),
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:shutdown (lines 1506-1531) ---[0m
    pub async fn shutdown(&self) -> Result<()> {
        info!("Shutting down Master Claude orchestrator");

        // Update state
        {
            let mut state = self.state.write().await;
            state.status = OrchestratorStatus::ShuttingDown;
        }

        // Shutdown all agents
        for entry in self.agents.iter() {
            let mut agent = entry.value().clone();
            if let Err(e) = agent.shutdown().await {
                error!(
                    "Error shutting down agent {}: {}",
                    agent.identity.agent_id, e
                );
            }
        }

        // Close coordination bus
        self.coordination_bus.close().await?;

        info!("Master Claude shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1319-1346 method generate_status_report <-> ./crates/ccswarm/src/orchestrator/mod.rs:1483-1503 method enable_proactive_mode
  Similarity: 86.83%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_status_report (lines 1319-1346) ---[0m
    pub async fn generate_status_report(&self) -> Result<StatusReport> {
        let state = self.state.read().await;

        let agent_statuses: HashMap<String, AgentStatus> = self
            .agents
            .iter()
            .map(|entry| (entry.key().clone(), entry.value().status.clone()))
            .collect();

        let uptime = Utc::now() - state.start_time;

        Ok(StatusReport {
            orchestrator_id: self.id.clone(),
            status: state.status.clone(),
            uptime_seconds: uptime.num_seconds() as u64,
            total_agents: self.agents.len(),
            active_agents: agent_statuses
                .values()
                .filter(|s| matches!(s, AgentStatus::Available | AgentStatus::Working))
                .count(),
            total_tasks_processed: state.total_tasks_processed,
            successful_tasks: state.successful_tasks,
            failed_tasks: state.failed_tasks,
            pending_tasks: state.pending_tasks.len(),
            agent_statuses,
            timestamp: Utc::now(),
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:enable_proactive_mode (lines 1483-1503) ---[0m
    pub async fn enable_proactive_mode(&self) -> Result<()> {
        info!("Enabling proactive mode - increasing analysis frequency");

        // Start additional proactive analysis loop with higher frequency
        let agents = self.agents.clone();
        let bus = self.coordination_bus.clone();
        let proactive_master = self.proactive_master.clone();

        tokio::spawn(async move {
            loop {
                tokio::time::sleep(std::time::Duration::from_secs(15)).await; // Every 15 seconds

                let proactive_guard = proactive_master.read().await;
                if let Err(e) = proactive_guard.analyze_and_decide(&agents, &bus).await {
                    error!("Error in high-frequency proactive analysis: {}", e);
                }
            }
        });

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:695-726 method check_agent_health <-> ./crates/ccswarm/src/orchestrator/mod.rs:1349-1372 method set_objective
  Similarity: 75.64%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:check_agent_health (lines 695-726) ---[0m
    async fn check_agent_health(&self) -> Result<()> {
        let mut unhealthy_agents = Vec::new();

        for entry in self.agents.iter() {
            let agent = entry.value();

            // Check if agent has been inactive for too long
            let inactive_duration = Utc::now() - agent.last_activity;
            if inactive_duration.num_seconds() > 300 {
                // 5 minutes
                warn!(
                    "Agent {} has been inactive for {} seconds",
                    agent.identity.agent_id,
                    inactive_duration.num_seconds()
                );
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }

            // Check for error status
            if matches!(agent.status, AgentStatus::Error(_)) {
                error!("Agent {} is in error state", agent.identity.agent_id);
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }
        }

        // Handle unhealthy agents
        for agent_id in unhealthy_agents {
            self.restart_agent(&agent_id).await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:set_objective (lines 1349-1372) ---[0m
    pub async fn set_objective(
        &self,
        title: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Objective;

        let objective_id = format!("obj-{}", uuid::Uuid::new_v4());
        let objective = Objective {
            id: objective_id.clone(),
            title,
            description,
            deadline,
            progress: 0.0,
            key_results: vec![],
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.set_objective(objective).await?;

        info!("Set project objective: {}", objective_id);
        Ok(objective_id)
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:695-726 method check_agent_health <-> ./crates/ccswarm/src/orchestrator/mod.rs:1466-1480 method find_similar_completed_tasks
  Similarity: 88.38%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:check_agent_health (lines 695-726) ---[0m
    async fn check_agent_health(&self) -> Result<()> {
        let mut unhealthy_agents = Vec::new();

        for entry in self.agents.iter() {
            let agent = entry.value();

            // Check if agent has been inactive for too long
            let inactive_duration = Utc::now() - agent.last_activity;
            if inactive_duration.num_seconds() > 300 {
                // 5 minutes
                warn!(
                    "Agent {} has been inactive for {} seconds",
                    agent.identity.agent_id,
                    inactive_duration.num_seconds()
                );
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }

            // Check for error status
            if matches!(agent.status, AgentStatus::Error(_)) {
                error!("Agent {} is in error state", agent.identity.agent_id);
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }
        }

        // Handle unhealthy agents
        for agent_id in unhealthy_agents {
            self.restart_agent(&agent_id).await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:find_similar_completed_tasks (lines 1466-1480) ---[0m
    async fn find_similar_completed_tasks(&self, task: &Task) -> Vec<String> {
        let state = self.state.read().await;
        let mut similar_tasks = Vec::new();

        // Look through review history for similar task types
        for (task_id, _) in state.review_history.iter().take(10) {
            // Use debug format for TaskType since it doesn't implement Display
            let task_type_str = format!("{:?}", task.task_type).to_lowercase();
            if task_id.contains(&task_type_str) {
                similar_tasks.push(task_id.clone());
            }
        }

        similar_tasks
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1375-1399 method add_milestone <-> ./crates/ccswarm/src/orchestrator/mod.rs:1506-1531 method shutdown
  Similarity: 81.40%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:add_milestone (lines 1375-1399) ---[0m
    pub async fn add_milestone(
        &self,
        name: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Milestone;

        let milestone_id = format!("milestone-{}", uuid::Uuid::new_v4());
        let milestone = Milestone {
            id: milestone_id.clone(),
            name,
            description,
            deadline,
            completion_percentage: 0.0,
            dependencies: vec![],
            critical_path: false,
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.add_milestone(milestone).await?;

        info!("Added milestone: {}", milestone_id);
        Ok(milestone_id)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:shutdown (lines 1506-1531) ---[0m
    pub async fn shutdown(&self) -> Result<()> {
        info!("Shutting down Master Claude orchestrator");

        // Update state
        {
            let mut state = self.state.write().await;
            state.status = OrchestratorStatus::ShuttingDown;
        }

        // Shutdown all agents
        for entry in self.agents.iter() {
            let mut agent = entry.value().clone();
            if let Err(e) = agent.shutdown().await {
                error!(
                    "Error shutting down agent {}: {}",
                    agent.identity.agent_id, e
                );
            }
        }

        // Close coordination bus
        self.coordination_bus.close().await?;

        info!("Master Claude shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:608-644 method create_remediation_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:1466-1480 method find_similar_completed_tasks
  Similarity: 79.10%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:create_remediation_task (lines 608-644) ---[0m
    async fn create_remediation_task(
        &self,
        original_task_id: &str,
        agent_id: &str,
        issues: Vec<String>,
    ) -> Result<Task> {
        // Generate specific fix instructions based on the issues
        let instructions = self.generate_fix_instructions(&issues).await?;

        // Create remediation task
        let task_id = format!("remediate-{}-{}", original_task_id, Uuid::new_v4());

        let description = format!(
            "Fix quality issues in task {}: {}",
            original_task_id, instructions
        );

        let details = format!(
            "Quality issues found:\n{}\n\nSpecific instructions:\n{}",
            issues.join("\n- "),
            instructions
        );

        let remediation_task = Task::new(
            task_id,
            description,
            crate::agent::Priority::High, // High priority for quality fixes
            TaskType::Remediation,
        )
        .with_details(details)
        .assign_to(agent_id.to_string())
        .with_parent_task(original_task_id.to_string())
        .with_quality_issues(issues)
        .with_duration(1800); // 30 minutes estimate

        Ok(remediation_task)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:find_similar_completed_tasks (lines 1466-1480) ---[0m
    async fn find_similar_completed_tasks(&self, task: &Task) -> Vec<String> {
        let state = self.state.read().await;
        let mut similar_tasks = Vec::new();

        // Look through review history for similar task types
        for (task_id, _) in state.review_history.iter().take(10) {
            // Use debug format for TaskType since it doesn't implement Display
            let task_type_str = format!("{:?}", task.task_type).to_lowercase();
            if task_id.contains(&task_type_str) {
                similar_tasks.push(task_id.clone());
            }
        }

        similar_tasks
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1349-1372 method set_objective <-> ./crates/ccswarm/src/orchestrator/mod.rs:1506-1531 method shutdown
  Similarity: 81.40%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:set_objective (lines 1349-1372) ---[0m
    pub async fn set_objective(
        &self,
        title: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Objective;

        let objective_id = format!("obj-{}", uuid::Uuid::new_v4());
        let objective = Objective {
            id: objective_id.clone(),
            title,
            description,
            deadline,
            progress: 0.0,
            key_results: vec![],
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.set_objective(objective).await?;

        info!("Set project objective: {}", objective_id);
        Ok(objective_id)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:shutdown (lines 1506-1531) ---[0m
    pub async fn shutdown(&self) -> Result<()> {
        info!("Shutting down Master Claude orchestrator");

        // Update state
        {
            let mut state = self.state.write().await;
            state.status = OrchestratorStatus::ShuttingDown;
        }

        // Shutdown all agents
        for entry in self.agents.iter() {
            let mut agent = entry.value().clone();
            if let Err(e) = agent.shutdown().await {
                error!(
                    "Error shutting down agent {}: {}",
                    agent.identity.agent_id, e
                );
            }
        }

        // Close coordination bus
        self.coordination_bus.close().await?;

        info!("Master Claude shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1375-1399 method add_milestone <-> ./crates/ccswarm/src/orchestrator/mod.rs:1483-1503 method enable_proactive_mode
  Similarity: 85.65%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:add_milestone (lines 1375-1399) ---[0m
    pub async fn add_milestone(
        &self,
        name: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Milestone;

        let milestone_id = format!("milestone-{}", uuid::Uuid::new_v4());
        let milestone = Milestone {
            id: milestone_id.clone(),
            name,
            description,
            deadline,
            completion_percentage: 0.0,
            dependencies: vec![],
            critical_path: false,
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.add_milestone(milestone).await?;

        info!("Added milestone: {}", milestone_id);
        Ok(milestone_id)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:enable_proactive_mode (lines 1483-1503) ---[0m
    pub async fn enable_proactive_mode(&self) -> Result<()> {
        info!("Enabling proactive mode - increasing analysis frequency");

        // Start additional proactive analysis loop with higher frequency
        let agents = self.agents.clone();
        let bus = self.coordination_bus.clone();
        let proactive_master = self.proactive_master.clone();

        tokio::spawn(async move {
            loop {
                tokio::time::sleep(std::time::Duration::from_secs(15)).await; // Every 15 seconds

                let proactive_guard = proactive_master.read().await;
                if let Err(e) = proactive_guard.analyze_and_decide(&agents, &bus).await {
                    error!("Error in high-frequency proactive analysis: {}", e);
                }
            }
        });

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1483-1503 method enable_proactive_mode <-> ./crates/ccswarm/src/orchestrator/mod.rs:1506-1531 method shutdown
  Similarity: 83.01%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:enable_proactive_mode (lines 1483-1503) ---[0m
    pub async fn enable_proactive_mode(&self) -> Result<()> {
        info!("Enabling proactive mode - increasing analysis frequency");

        // Start additional proactive analysis loop with higher frequency
        let agents = self.agents.clone();
        let bus = self.coordination_bus.clone();
        let proactive_master = self.proactive_master.clone();

        tokio::spawn(async move {
            loop {
                tokio::time::sleep(std::time::Duration::from_secs(15)).await; // Every 15 seconds

                let proactive_guard = proactive_master.read().await;
                if let Err(e) = proactive_guard.analyze_and_decide(&agents, &bus).await {
                    error!("Error in high-frequency proactive analysis: {}", e);
                }
            }
        });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:shutdown (lines 1506-1531) ---[0m
    pub async fn shutdown(&self) -> Result<()> {
        info!("Shutting down Master Claude orchestrator");

        // Update state
        {
            let mut state = self.state.write().await;
            state.status = OrchestratorStatus::ShuttingDown;
        }

        // Shutdown all agents
        for entry in self.agents.iter() {
            let mut agent = entry.value().clone();
            if let Err(e) = agent.shutdown().await {
                error!(
                    "Error shutting down agent {}: {}",
                    agent.identity.agent_id, e
                );
            }
        }

        // Close coordination bus
        self.coordination_bus.close().await?;

        info!("Master Claude shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1375-1399 method add_milestone <-> ./crates/ccswarm/src/orchestrator/mod.rs:1443-1463 method identify_task_dependencies
  Similarity: 84.51%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:add_milestone (lines 1375-1399) ---[0m
    pub async fn add_milestone(
        &self,
        name: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Milestone;

        let milestone_id = format!("milestone-{}", uuid::Uuid::new_v4());
        let milestone = Milestone {
            id: milestone_id.clone(),
            name,
            description,
            deadline,
            completion_percentage: 0.0,
            dependencies: vec![],
            critical_path: false,
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.add_milestone(milestone).await?;

        info!("Added milestone: {}", milestone_id);
        Ok(milestone_id)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:identify_task_dependencies (lines 1443-1463) ---[0m
    async fn identify_task_dependencies(&self, task: &Task) -> Vec<String> {
        let mut dependencies = Vec::new();

        // Check task description for dependency indicators
        let description_lower = task.description.to_lowercase();

        if description_lower.contains("api") || description_lower.contains("endpoint") {
            dependencies.push("backend_api".to_string());
        }
        if description_lower.contains("ui") || description_lower.contains("component") {
            dependencies.push("frontend_components".to_string());
        }
        if description_lower.contains("database") || description_lower.contains("migration") {
            dependencies.push("database_schema".to_string());
        }
        if description_lower.contains("deploy") || description_lower.contains("infrastructure") {
            dependencies.push("deployment_pipeline".to_string());
        }

        dependencies
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:695-726 method check_agent_health <-> ./crates/ccswarm/src/orchestrator/mod.rs:1443-1463 method identify_task_dependencies
  Similarity: 73.13%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:check_agent_health (lines 695-726) ---[0m
    async fn check_agent_health(&self) -> Result<()> {
        let mut unhealthy_agents = Vec::new();

        for entry in self.agents.iter() {
            let agent = entry.value();

            // Check if agent has been inactive for too long
            let inactive_duration = Utc::now() - agent.last_activity;
            if inactive_duration.num_seconds() > 300 {
                // 5 minutes
                warn!(
                    "Agent {} has been inactive for {} seconds",
                    agent.identity.agent_id,
                    inactive_duration.num_seconds()
                );
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }

            // Check for error status
            if matches!(agent.status, AgentStatus::Error(_)) {
                error!("Agent {} is in error state", agent.identity.agent_id);
                unhealthy_agents.push(agent.identity.agent_id.clone());
            }
        }

        // Handle unhealthy agents
        for agent_id in unhealthy_agents {
            self.restart_agent(&agent_id).await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:identify_task_dependencies (lines 1443-1463) ---[0m
    async fn identify_task_dependencies(&self, task: &Task) -> Vec<String> {
        let mut dependencies = Vec::new();

        // Check task description for dependency indicators
        let description_lower = task.description.to_lowercase();

        if description_lower.contains("api") || description_lower.contains("endpoint") {
            dependencies.push("backend_api".to_string());
        }
        if description_lower.contains("ui") || description_lower.contains("component") {
            dependencies.push("frontend_components".to_string());
        }
        if description_lower.contains("database") || description_lower.contains("migration") {
            dependencies.push("database_schema".to_string());
        }
        if description_lower.contains("deploy") || description_lower.contains("infrastructure") {
            dependencies.push("deployment_pipeline".to_string());
        }

        dependencies
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1417-1440 method get_proactive_insights_for_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:1506-1531 method shutdown
  Similarity: 77.19%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:get_proactive_insights_for_task (lines 1417-1440) ---[0m
    async fn get_proactive_insights_for_task(&self, task: &Task) -> serde_json::Value {
        let _proactive_master = self.proactive_master.read().await;

        // Get relevant insights from proactive master
        let insights = serde_json::json!({
            "task_complexity": match task.task_type {
                TaskType::Feature => "high",
                TaskType::Infrastructure => "high",
                TaskType::Development => "medium",
                TaskType::Testing => "medium",
                TaskType::Bug | TaskType::Bugfix => "low",
                _ => "medium",
            },
            "recommended_approach": match task.priority {
                crate::agent::Priority::Critical => "orchestrated_execution",
                crate::agent::Priority::High => "careful_planning",
                _ => "standard_execution",
            },
            "potential_dependencies": self.identify_task_dependencies(task).await,
            "similar_tasks_completed": self.find_similar_completed_tasks(task).await,
        });

        insights
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:shutdown (lines 1506-1531) ---[0m
    pub async fn shutdown(&self) -> Result<()> {
        info!("Shutting down Master Claude orchestrator");

        // Update state
        {
            let mut state = self.state.write().await;
            state.status = OrchestratorStatus::ShuttingDown;
        }

        // Shutdown all agents
        for entry in self.agents.iter() {
            let mut agent = entry.value().clone();
            if let Err(e) = agent.shutdown().await {
                error!(
                    "Error shutting down agent {}: {}",
                    agent.identity.agent_id, e
                );
            }
        }

        // Close coordination bus
        self.coordination_bus.close().await?;

        info!("Master Claude shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1349-1372 method set_objective <-> ./crates/ccswarm/src/orchestrator/mod.rs:1483-1503 method enable_proactive_mode
  Similarity: 85.65%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:set_objective (lines 1349-1372) ---[0m
    pub async fn set_objective(
        &self,
        title: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Objective;

        let objective_id = format!("obj-{}", uuid::Uuid::new_v4());
        let objective = Objective {
            id: objective_id.clone(),
            title,
            description,
            deadline,
            progress: 0.0,
            key_results: vec![],
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.set_objective(objective).await?;

        info!("Set project objective: {}", objective_id);
        Ok(objective_id)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:enable_proactive_mode (lines 1483-1503) ---[0m
    pub async fn enable_proactive_mode(&self) -> Result<()> {
        info!("Enabling proactive mode - increasing analysis frequency");

        // Start additional proactive analysis loop with higher frequency
        let agents = self.agents.clone();
        let bus = self.coordination_bus.clone();
        let proactive_master = self.proactive_master.clone();

        tokio::spawn(async move {
            loop {
                tokio::time::sleep(std::time::Duration::from_secs(15)).await; // Every 15 seconds

                let proactive_guard = proactive_master.read().await;
                if let Err(e) = proactive_guard.analyze_and_decide(&agents, &bus).await {
                    error!("Error in high-frequency proactive analysis: {}", e);
                }
            }
        });

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1375-1399 method add_milestone <-> ./crates/ccswarm/src/orchestrator/mod.rs:1417-1440 method get_proactive_insights_for_task
  Similarity: 78.27%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:add_milestone (lines 1375-1399) ---[0m
    pub async fn add_milestone(
        &self,
        name: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Milestone;

        let milestone_id = format!("milestone-{}", uuid::Uuid::new_v4());
        let milestone = Milestone {
            id: milestone_id.clone(),
            name,
            description,
            deadline,
            completion_percentage: 0.0,
            dependencies: vec![],
            critical_path: false,
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.add_milestone(milestone).await?;

        info!("Added milestone: {}", milestone_id);
        Ok(milestone_id)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:get_proactive_insights_for_task (lines 1417-1440) ---[0m
    async fn get_proactive_insights_for_task(&self, task: &Task) -> serde_json::Value {
        let _proactive_master = self.proactive_master.read().await;

        // Get relevant insights from proactive master
        let insights = serde_json::json!({
            "task_complexity": match task.task_type {
                TaskType::Feature => "high",
                TaskType::Infrastructure => "high",
                TaskType::Development => "medium",
                TaskType::Testing => "medium",
                TaskType::Bug | TaskType::Bugfix => "low",
                _ => "medium",
            },
            "recommended_approach": match task.priority {
                crate::agent::Priority::Critical => "orchestrated_execution",
                crate::agent::Priority::High => "careful_planning",
                _ => "standard_execution",
            },
            "potential_dependencies": self.identify_task_dependencies(task).await,
            "similar_tasks_completed": self.find_similar_completed_tasks(task).await,
        });

        insights
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1443-1463 method identify_task_dependencies <-> ./crates/ccswarm/src/orchestrator/mod.rs:1506-1531 method shutdown
  Similarity: 81.46%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:identify_task_dependencies (lines 1443-1463) ---[0m
    async fn identify_task_dependencies(&self, task: &Task) -> Vec<String> {
        let mut dependencies = Vec::new();

        // Check task description for dependency indicators
        let description_lower = task.description.to_lowercase();

        if description_lower.contains("api") || description_lower.contains("endpoint") {
            dependencies.push("backend_api".to_string());
        }
        if description_lower.contains("ui") || description_lower.contains("component") {
            dependencies.push("frontend_components".to_string());
        }
        if description_lower.contains("database") || description_lower.contains("migration") {
            dependencies.push("database_schema".to_string());
        }
        if description_lower.contains("deploy") || description_lower.contains("infrastructure") {
            dependencies.push("deployment_pipeline".to_string());
        }

        dependencies
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:shutdown (lines 1506-1531) ---[0m
    pub async fn shutdown(&self) -> Result<()> {
        info!("Shutting down Master Claude orchestrator");

        // Update state
        {
            let mut state = self.state.write().await;
            state.status = OrchestratorStatus::ShuttingDown;
        }

        // Shutdown all agents
        for entry in self.agents.iter() {
            let mut agent = entry.value().clone();
            if let Err(e) = agent.shutdown().await {
                error!(
                    "Error shutting down agent {}: {}",
                    agent.identity.agent_id, e
                );
            }
        }

        // Close coordination bus
        self.coordination_bus.close().await?;

        info!("Master Claude shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1349-1372 method set_objective <-> ./crates/ccswarm/src/orchestrator/mod.rs:1443-1463 method identify_task_dependencies
  Similarity: 84.51%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:set_objective (lines 1349-1372) ---[0m
    pub async fn set_objective(
        &self,
        title: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Objective;

        let objective_id = format!("obj-{}", uuid::Uuid::new_v4());
        let objective = Objective {
            id: objective_id.clone(),
            title,
            description,
            deadline,
            progress: 0.0,
            key_results: vec![],
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.set_objective(objective).await?;

        info!("Set project objective: {}", objective_id);
        Ok(objective_id)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:identify_task_dependencies (lines 1443-1463) ---[0m
    async fn identify_task_dependencies(&self, task: &Task) -> Vec<String> {
        let mut dependencies = Vec::new();

        // Check task description for dependency indicators
        let description_lower = task.description.to_lowercase();

        if description_lower.contains("api") || description_lower.contains("endpoint") {
            dependencies.push("backend_api".to_string());
        }
        if description_lower.contains("ui") || description_lower.contains("component") {
            dependencies.push("frontend_components".to_string());
        }
        if description_lower.contains("database") || description_lower.contains("migration") {
            dependencies.push("database_schema".to_string());
        }
        if description_lower.contains("deploy") || description_lower.contains("infrastructure") {
            dependencies.push("deployment_pipeline".to_string());
        }

        dependencies
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1417-1440 method get_proactive_insights_for_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:1466-1480 method find_similar_completed_tasks
  Similarity: 96.43%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:get_proactive_insights_for_task (lines 1417-1440) ---[0m
    async fn get_proactive_insights_for_task(&self, task: &Task) -> serde_json::Value {
        let _proactive_master = self.proactive_master.read().await;

        // Get relevant insights from proactive master
        let insights = serde_json::json!({
            "task_complexity": match task.task_type {
                TaskType::Feature => "high",
                TaskType::Infrastructure => "high",
                TaskType::Development => "medium",
                TaskType::Testing => "medium",
                TaskType::Bug | TaskType::Bugfix => "low",
                _ => "medium",
            },
            "recommended_approach": match task.priority {
                crate::agent::Priority::Critical => "orchestrated_execution",
                crate::agent::Priority::High => "careful_planning",
                _ => "standard_execution",
            },
            "potential_dependencies": self.identify_task_dependencies(task).await,
            "similar_tasks_completed": self.find_similar_completed_tasks(task).await,
        });

        insights
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:find_similar_completed_tasks (lines 1466-1480) ---[0m
    async fn find_similar_completed_tasks(&self, task: &Task) -> Vec<String> {
        let state = self.state.read().await;
        let mut similar_tasks = Vec::new();

        // Look through review history for similar task types
        for (task_id, _) in state.review_history.iter().take(10) {
            // Use debug format for TaskType since it doesn't implement Display
            let task_type_str = format!("{:?}", task.task_type).to_lowercase();
            if task_id.contains(&task_type_str) {
                similar_tasks.push(task_id.clone());
            }
        }

        similar_tasks
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1349-1372 method set_objective <-> ./crates/ccswarm/src/orchestrator/mod.rs:1417-1440 method get_proactive_insights_for_task
  Similarity: 78.27%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:set_objective (lines 1349-1372) ---[0m
    pub async fn set_objective(
        &self,
        title: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Objective;

        let objective_id = format!("obj-{}", uuid::Uuid::new_v4());
        let objective = Objective {
            id: objective_id.clone(),
            title,
            description,
            deadline,
            progress: 0.0,
            key_results: vec![],
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.set_objective(objective).await?;

        info!("Set project objective: {}", objective_id);
        Ok(objective_id)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:get_proactive_insights_for_task (lines 1417-1440) ---[0m
    async fn get_proactive_insights_for_task(&self, task: &Task) -> serde_json::Value {
        let _proactive_master = self.proactive_master.read().await;

        // Get relevant insights from proactive master
        let insights = serde_json::json!({
            "task_complexity": match task.task_type {
                TaskType::Feature => "high",
                TaskType::Infrastructure => "high",
                TaskType::Development => "medium",
                TaskType::Testing => "medium",
                TaskType::Bug | TaskType::Bugfix => "low",
                _ => "medium",
            },
            "recommended_approach": match task.priority {
                crate::agent::Priority::Critical => "orchestrated_execution",
                crate::agent::Priority::High => "careful_planning",
                _ => "standard_execution",
            },
            "potential_dependencies": self.identify_task_dependencies(task).await,
            "similar_tasks_completed": self.find_similar_completed_tasks(task).await,
        });

        insights
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1417-1440 method get_proactive_insights_for_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:1483-1503 method enable_proactive_mode
  Similarity: 81.94%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:get_proactive_insights_for_task (lines 1417-1440) ---[0m
    async fn get_proactive_insights_for_task(&self, task: &Task) -> serde_json::Value {
        let _proactive_master = self.proactive_master.read().await;

        // Get relevant insights from proactive master
        let insights = serde_json::json!({
            "task_complexity": match task.task_type {
                TaskType::Feature => "high",
                TaskType::Infrastructure => "high",
                TaskType::Development => "medium",
                TaskType::Testing => "medium",
                TaskType::Bug | TaskType::Bugfix => "low",
                _ => "medium",
            },
            "recommended_approach": match task.priority {
                crate::agent::Priority::Critical => "orchestrated_execution",
                crate::agent::Priority::High => "careful_planning",
                _ => "standard_execution",
            },
            "potential_dependencies": self.identify_task_dependencies(task).await,
            "similar_tasks_completed": self.find_similar_completed_tasks(task).await,
        });

        insights
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:enable_proactive_mode (lines 1483-1503) ---[0m
    pub async fn enable_proactive_mode(&self) -> Result<()> {
        info!("Enabling proactive mode - increasing analysis frequency");

        // Start additional proactive analysis loop with higher frequency
        let agents = self.agents.clone();
        let bus = self.coordination_bus.clone();
        let proactive_master = self.proactive_master.clone();

        tokio::spawn(async move {
            loop {
                tokio::time::sleep(std::time::Duration::from_secs(15)).await; // Every 15 seconds

                let proactive_guard = proactive_master.read().await;
                if let Err(e) = proactive_guard.analyze_and_decide(&agents, &bus).await {
                    error!("Error in high-frequency proactive analysis: {}", e);
                }
            }
        });

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:368-394 method coordinate_development <-> ./crates/ccswarm/src/orchestrator/mod.rs:729-742 method restart_agent
  Similarity: 88.06%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:coordinate_development (lines 368-394) ---[0m
    async fn coordinate_development(&self) -> Result<()> {
        info!("Master Claude coordination loop started");

        loop {
            tokio::select! {
                // Handle incoming tasks
                Ok(task) = self.task_queue_rx.recv() => {
                    if let Err(e) = self.assign_task(task).await {
                        error!("Error assigning task: {}", e);
                    }
                }

                // Periodic status check
                _ = tokio::time::sleep(std::time::Duration::from_secs(10)) => {
                    self.check_agent_health().await?;
                }
            }

            // Check if we should shutdown
            let state = self.state.read().await;
            if state.status == OrchestratorStatus::ShuttingDown {
                break;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:restart_agent (lines 729-742) ---[0m
    async fn restart_agent(&self, agent_id: &str) -> Result<()> {
        warn!("Restarting agent: {}", agent_id);

        if let Some(mut agent) = self.agents.get_mut(agent_id) {
            // Reset agent status
            agent.status = AgentStatus::Available;
            agent.last_activity = Utc::now();

            // Re-establish identity
            agent.establish_identity().await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1319-1346 method generate_status_report <-> ./crates/ccswarm/src/orchestrator/mod.rs:1443-1463 method identify_task_dependencies
  Similarity: 72.28%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_status_report (lines 1319-1346) ---[0m
    pub async fn generate_status_report(&self) -> Result<StatusReport> {
        let state = self.state.read().await;

        let agent_statuses: HashMap<String, AgentStatus> = self
            .agents
            .iter()
            .map(|entry| (entry.key().clone(), entry.value().status.clone()))
            .collect();

        let uptime = Utc::now() - state.start_time;

        Ok(StatusReport {
            orchestrator_id: self.id.clone(),
            status: state.status.clone(),
            uptime_seconds: uptime.num_seconds() as u64,
            total_agents: self.agents.len(),
            active_agents: agent_statuses
                .values()
                .filter(|s| matches!(s, AgentStatus::Available | AgentStatus::Working))
                .count(),
            total_tasks_processed: state.total_tasks_processed,
            successful_tasks: state.successful_tasks,
            failed_tasks: state.failed_tasks,
            pending_tasks: state.pending_tasks.len(),
            agent_statuses,
            timestamp: Utc::now(),
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:identify_task_dependencies (lines 1443-1463) ---[0m
    async fn identify_task_dependencies(&self, task: &Task) -> Vec<String> {
        let mut dependencies = Vec::new();

        // Check task description for dependency indicators
        let description_lower = task.description.to_lowercase();

        if description_lower.contains("api") || description_lower.contains("endpoint") {
            dependencies.push("backend_api".to_string());
        }
        if description_lower.contains("ui") || description_lower.contains("component") {
            dependencies.push("frontend_components".to_string());
        }
        if description_lower.contains("database") || description_lower.contains("migration") {
            dependencies.push("database_schema".to_string());
        }
        if description_lower.contains("deploy") || description_lower.contains("infrastructure") {
            dependencies.push("deployment_pipeline".to_string());
        }

        dependencies
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:368-394 method coordinate_development <-> ./crates/ccswarm/src/orchestrator/mod.rs:1466-1480 method find_similar_completed_tasks
  Similarity: 84.25%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:coordinate_development (lines 368-394) ---[0m
    async fn coordinate_development(&self) -> Result<()> {
        info!("Master Claude coordination loop started");

        loop {
            tokio::select! {
                // Handle incoming tasks
                Ok(task) = self.task_queue_rx.recv() => {
                    if let Err(e) = self.assign_task(task).await {
                        error!("Error assigning task: {}", e);
                    }
                }

                // Periodic status check
                _ = tokio::time::sleep(std::time::Duration::from_secs(10)) => {
                    self.check_agent_health().await?;
                }
            }

            // Check if we should shutdown
            let state = self.state.read().await;
            if state.status == OrchestratorStatus::ShuttingDown {
                break;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:find_similar_completed_tasks (lines 1466-1480) ---[0m
    async fn find_similar_completed_tasks(&self, task: &Task) -> Vec<String> {
        let state = self.state.read().await;
        let mut similar_tasks = Vec::new();

        // Look through review history for similar task types
        for (task_id, _) in state.review_history.iter().take(10) {
            // Use debug format for TaskType since it doesn't implement Display
            let task_type_str = format!("{:?}", task.task_type).to_lowercase();
            if task_id.contains(&task_type_str) {
                similar_tasks.push(task_id.clone());
            }
        }

        similar_tasks
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:729-742 method restart_agent <-> ./crates/ccswarm/src/orchestrator/mod.rs:1417-1440 method get_proactive_insights_for_task
  Similarity: 93.03%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:restart_agent (lines 729-742) ---[0m
    async fn restart_agent(&self, agent_id: &str) -> Result<()> {
        warn!("Restarting agent: {}", agent_id);

        if let Some(mut agent) = self.agents.get_mut(agent_id) {
            // Reset agent status
            agent.status = AgentStatus::Available;
            agent.last_activity = Utc::now();

            // Re-establish identity
            agent.establish_identity().await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:get_proactive_insights_for_task (lines 1417-1440) ---[0m
    async fn get_proactive_insights_for_task(&self, task: &Task) -> serde_json::Value {
        let _proactive_master = self.proactive_master.read().await;

        // Get relevant insights from proactive master
        let insights = serde_json::json!({
            "task_complexity": match task.task_type {
                TaskType::Feature => "high",
                TaskType::Infrastructure => "high",
                TaskType::Development => "medium",
                TaskType::Testing => "medium",
                TaskType::Bug | TaskType::Bugfix => "low",
                _ => "medium",
            },
            "recommended_approach": match task.priority {
                crate::agent::Priority::Critical => "orchestrated_execution",
                crate::agent::Priority::High => "careful_planning",
                _ => "standard_execution",
            },
            "potential_dependencies": self.identify_task_dependencies(task).await,
            "similar_tasks_completed": self.find_similar_completed_tasks(task).await,
        });

        insights
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1417-1440 method get_proactive_insights_for_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:1443-1463 method identify_task_dependencies
  Similarity: 78.13%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:get_proactive_insights_for_task (lines 1417-1440) ---[0m
    async fn get_proactive_insights_for_task(&self, task: &Task) -> serde_json::Value {
        let _proactive_master = self.proactive_master.read().await;

        // Get relevant insights from proactive master
        let insights = serde_json::json!({
            "task_complexity": match task.task_type {
                TaskType::Feature => "high",
                TaskType::Infrastructure => "high",
                TaskType::Development => "medium",
                TaskType::Testing => "medium",
                TaskType::Bug | TaskType::Bugfix => "low",
                _ => "medium",
            },
            "recommended_approach": match task.priority {
                crate::agent::Priority::Critical => "orchestrated_execution",
                crate::agent::Priority::High => "careful_planning",
                _ => "standard_execution",
            },
            "potential_dependencies": self.identify_task_dependencies(task).await,
            "similar_tasks_completed": self.find_similar_completed_tasks(task).await,
        });

        insights
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:identify_task_dependencies (lines 1443-1463) ---[0m
    async fn identify_task_dependencies(&self, task: &Task) -> Vec<String> {
        let mut dependencies = Vec::new();

        // Check task description for dependency indicators
        let description_lower = task.description.to_lowercase();

        if description_lower.contains("api") || description_lower.contains("endpoint") {
            dependencies.push("backend_api".to_string());
        }
        if description_lower.contains("ui") || description_lower.contains("component") {
            dependencies.push("frontend_components".to_string());
        }
        if description_lower.contains("database") || description_lower.contains("migration") {
            dependencies.push("database_schema".to_string());
        }
        if description_lower.contains("deploy") || description_lower.contains("infrastructure") {
            dependencies.push("deployment_pipeline".to_string());
        }

        dependencies
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1506-1531 method shutdown <-> ./crates/ccswarm/src/orchestrator/mod.rs:1608-1624 function test_master_claude_creation
  Similarity: 81.57%

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:shutdown (lines 1506-1531) ---[0m
    pub async fn shutdown(&self) -> Result<()> {
        info!("Shutting down Master Claude orchestrator");

        // Update state
        {
            let mut state = self.state.write().await;
            state.status = OrchestratorStatus::ShuttingDown;
        }

        // Shutdown all agents
        for entry in self.agents.iter() {
            let mut agent = entry.value().clone();
            if let Err(e) = agent.shutdown().await {
                error!(
                    "Error shutting down agent {}: {}",
                    agent.identity.agent_id, e
                );
            }
        }

        // Close coordination bus
        self.coordination_bus.close().await?;

        info!("Master Claude shutdown complete");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:test_master_claude_creation (lines 1608-1624) ---[0m
    async fn test_master_claude_creation() {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Initialize git repo
        crate::git::WorktreeManager::init_if_needed(&repo_path)
            .await
            .unwrap();

        let config = create_test_config();
        let master = MasterClaude::new(config, repo_path).await.unwrap();

        assert!(master.id.starts_with("master-claude-"));

        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Initializing);
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:368-394 method coordinate_development <-> ./crates/ccswarm/src/orchestrator/mod.rs:1483-1503 method enable_proactive_mode
  Similarity: 71.40%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:coordinate_development (lines 368-394) ---[0m
    async fn coordinate_development(&self) -> Result<()> {
        info!("Master Claude coordination loop started");

        loop {
            tokio::select! {
                // Handle incoming tasks
                Ok(task) = self.task_queue_rx.recv() => {
                    if let Err(e) = self.assign_task(task).await {
                        error!("Error assigning task: {}", e);
                    }
                }

                // Periodic status check
                _ = tokio::time::sleep(std::time::Duration::from_secs(10)) => {
                    self.check_agent_health().await?;
                }
            }

            // Check if we should shutdown
            let state = self.state.read().await;
            if state.status == OrchestratorStatus::ShuttingDown {
                break;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:enable_proactive_mode (lines 1483-1503) ---[0m
    pub async fn enable_proactive_mode(&self) -> Result<()> {
        info!("Enabling proactive mode - increasing analysis frequency");

        // Start additional proactive analysis loop with higher frequency
        let agents = self.agents.clone();
        let bus = self.coordination_bus.clone();
        let proactive_master = self.proactive_master.clone();

        tokio::spawn(async move {
            loop {
                tokio::time::sleep(std::time::Duration::from_secs(15)).await; // Every 15 seconds

                let proactive_guard = proactive_master.read().await;
                if let Err(e) = proactive_guard.analyze_and_decide(&agents, &bus).await {
                    error!("Error in high-frequency proactive analysis: {}", e);
                }
            }
        });

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1443-1463 method identify_task_dependencies <-> ./crates/ccswarm/src/orchestrator/mod.rs:1483-1503 method enable_proactive_mode
  Similarity: 79.80%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:identify_task_dependencies (lines 1443-1463) ---[0m
    async fn identify_task_dependencies(&self, task: &Task) -> Vec<String> {
        let mut dependencies = Vec::new();

        // Check task description for dependency indicators
        let description_lower = task.description.to_lowercase();

        if description_lower.contains("api") || description_lower.contains("endpoint") {
            dependencies.push("backend_api".to_string());
        }
        if description_lower.contains("ui") || description_lower.contains("component") {
            dependencies.push("frontend_components".to_string());
        }
        if description_lower.contains("database") || description_lower.contains("migration") {
            dependencies.push("database_schema".to_string());
        }
        if description_lower.contains("deploy") || description_lower.contains("infrastructure") {
            dependencies.push("deployment_pipeline".to_string());
        }

        dependencies
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:enable_proactive_mode (lines 1483-1503) ---[0m
    pub async fn enable_proactive_mode(&self) -> Result<()> {
        info!("Enabling proactive mode - increasing analysis frequency");

        // Start additional proactive analysis loop with higher frequency
        let agents = self.agents.clone();
        let bus = self.coordination_bus.clone();
        let proactive_master = self.proactive_master.clone();

        tokio::spawn(async move {
            loop {
                tokio::time::sleep(std::time::Duration::from_secs(15)).await; // Every 15 seconds

                let proactive_guard = proactive_master.read().await;
                if let Err(e) = proactive_guard.analyze_and_decide(&agents, &bus).await {
                    error!("Error in high-frequency proactive analysis: {}", e);
                }
            }
        });

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:368-394 method coordinate_development <-> ./crates/ccswarm/src/orchestrator/mod.rs:397-407 method add_task
  Similarity: 86.12%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:coordinate_development (lines 368-394) ---[0m
    async fn coordinate_development(&self) -> Result<()> {
        info!("Master Claude coordination loop started");

        loop {
            tokio::select! {
                // Handle incoming tasks
                Ok(task) = self.task_queue_rx.recv() => {
                    if let Err(e) = self.assign_task(task).await {
                        error!("Error assigning task: {}", e);
                    }
                }

                // Periodic status check
                _ = tokio::time::sleep(std::time::Duration::from_secs(10)) => {
                    self.check_agent_health().await?;
                }
            }

            // Check if we should shutdown
            let state = self.state.read().await;
            if state.status == OrchestratorStatus::ShuttingDown {
                break;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:add_task (lines 397-407) ---[0m
    pub async fn add_task(&self, task: Task) -> Result<()> {
        self.task_queue_tx
            .send(task.clone())
            .await
            .context("Failed to add task to queue")?;

        let mut state = self.state.write().await;
        state.pending_tasks.push(task);

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:368-394 method coordinate_development <-> ./crates/ccswarm/src/orchestrator/mod.rs:1402-1414 method trigger_proactive_analysis
  Similarity: 79.88%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:coordinate_development (lines 368-394) ---[0m
    async fn coordinate_development(&self) -> Result<()> {
        info!("Master Claude coordination loop started");

        loop {
            tokio::select! {
                // Handle incoming tasks
                Ok(task) = self.task_queue_rx.recv() => {
                    if let Err(e) = self.assign_task(task).await {
                        error!("Error assigning task: {}", e);
                    }
                }

                // Periodic status check
                _ = tokio::time::sleep(std::time::Duration::from_secs(10)) => {
                    self.check_agent_health().await?;
                }
            }

            // Check if we should shutdown
            let state = self.state.read().await;
            if state.status == OrchestratorStatus::ShuttingDown {
                break;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:trigger_proactive_analysis (lines 1402-1414) ---[0m
    pub async fn trigger_proactive_analysis(
        &self,
    ) -> Result<Vec<self::proactive_master::ProactiveDecision>> {
        info!("Triggering immediate proactive analysis");

        let proactive_master = self.proactive_master.read().await;
        let decisions = proactive_master
            .analyze_and_decide(&self.agents, &self.coordination_bus)
            .await?;

        info!("Proactive analysis generated {} decisions", decisions.len());
        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1319-1346 method generate_status_report <-> ./crates/ccswarm/src/orchestrator/mod.rs:1608-1624 function test_master_claude_creation
  Similarity: 70.28%

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:generate_status_report (lines 1319-1346) ---[0m
    pub async fn generate_status_report(&self) -> Result<StatusReport> {
        let state = self.state.read().await;

        let agent_statuses: HashMap<String, AgentStatus> = self
            .agents
            .iter()
            .map(|entry| (entry.key().clone(), entry.value().status.clone()))
            .collect();

        let uptime = Utc::now() - state.start_time;

        Ok(StatusReport {
            orchestrator_id: self.id.clone(),
            status: state.status.clone(),
            uptime_seconds: uptime.num_seconds() as u64,
            total_agents: self.agents.len(),
            active_agents: agent_statuses
                .values()
                .filter(|s| matches!(s, AgentStatus::Available | AgentStatus::Working))
                .count(),
            total_tasks_processed: state.total_tasks_processed,
            successful_tasks: state.successful_tasks,
            failed_tasks: state.failed_tasks,
            pending_tasks: state.pending_tasks.len(),
            agent_statuses,
            timestamp: Utc::now(),
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:test_master_claude_creation (lines 1608-1624) ---[0m
    async fn test_master_claude_creation() {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Initialize git repo
        crate::git::WorktreeManager::init_if_needed(&repo_path)
            .await
            .unwrap();

        let config = create_test_config();
        let master = MasterClaude::new(config, repo_path).await.unwrap();

        assert!(master.id.starts_with("master-claude-"));

        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Initializing);
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1375-1399 method add_milestone <-> ./crates/ccswarm/src/orchestrator/mod.rs:1608-1624 function test_master_claude_creation
  Similarity: 74.44%

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:add_milestone (lines 1375-1399) ---[0m
    pub async fn add_milestone(
        &self,
        name: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Milestone;

        let milestone_id = format!("milestone-{}", uuid::Uuid::new_v4());
        let milestone = Milestone {
            id: milestone_id.clone(),
            name,
            description,
            deadline,
            completion_percentage: 0.0,
            dependencies: vec![],
            critical_path: false,
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.add_milestone(milestone).await?;

        info!("Added milestone: {}", milestone_id);
        Ok(milestone_id)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:test_master_claude_creation (lines 1608-1624) ---[0m
    async fn test_master_claude_creation() {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Initialize git repo
        crate::git::WorktreeManager::init_if_needed(&repo_path)
            .await
            .unwrap();

        let config = create_test_config();
        let master = MasterClaude::new(config, repo_path).await.unwrap();

        assert!(master.id.starts_with("master-claude-"));

        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Initializing);
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1349-1372 method set_objective <-> ./crates/ccswarm/src/orchestrator/mod.rs:1608-1624 function test_master_claude_creation
  Similarity: 74.44%

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:set_objective (lines 1349-1372) ---[0m
    pub async fn set_objective(
        &self,
        title: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Objective;

        let objective_id = format!("obj-{}", uuid::Uuid::new_v4());
        let objective = Objective {
            id: objective_id.clone(),
            title,
            description,
            deadline,
            progress: 0.0,
            key_results: vec![],
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.set_objective(objective).await?;

        info!("Set project objective: {}", objective_id);
        Ok(objective_id)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:test_master_claude_creation (lines 1608-1624) ---[0m
    async fn test_master_claude_creation() {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Initialize git repo
        crate::git::WorktreeManager::init_if_needed(&repo_path)
            .await
            .unwrap();

        let config = create_test_config();
        let master = MasterClaude::new(config, repo_path).await.unwrap();

        assert!(master.id.starts_with("master-claude-"));

        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Initializing);
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1466-1480 method find_similar_completed_tasks <-> ./crates/ccswarm/src/orchestrator/mod.rs:1506-1531 method shutdown
  Similarity: 74.10%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:find_similar_completed_tasks (lines 1466-1480) ---[0m
    async fn find_similar_completed_tasks(&self, task: &Task) -> Vec<String> {
        let state = self.state.read().await;
        let mut similar_tasks = Vec::new();

        // Look through review history for similar task types
        for (task_id, _) in state.review_history.iter().take(10) {
            // Use debug format for TaskType since it doesn't implement Display
            let task_type_str = format!("{:?}", task.task_type).to_lowercase();
            if task_id.contains(&task_type_str) {
                similar_tasks.push(task_id.clone());
            }
        }

        similar_tasks
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:shutdown (lines 1506-1531) ---[0m
    pub async fn shutdown(&self) -> Result<()> {
        info!("Shutting down Master Claude orchestrator");

        // Update state
        {
            let mut state = self.state.write().await;
            state.status = OrchestratorStatus::ShuttingDown;
        }

        // Shutdown all agents
        for entry in self.agents.iter() {
            let mut agent = entry.value().clone();
            if let Err(e) = agent.shutdown().await {
                error!(
                    "Error shutting down agent {}: {}",
                    agent.identity.agent_id, e
                );
            }
        }

        // Close coordination bus
        self.coordination_bus.close().await?;

        info!("Master Claude shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1483-1503 method enable_proactive_mode <-> ./crates/ccswarm/src/orchestrator/mod.rs:1608-1624 function test_master_claude_creation
  Similarity: 78.71%

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:enable_proactive_mode (lines 1483-1503) ---[0m
    pub async fn enable_proactive_mode(&self) -> Result<()> {
        info!("Enabling proactive mode - increasing analysis frequency");

        // Start additional proactive analysis loop with higher frequency
        let agents = self.agents.clone();
        let bus = self.coordination_bus.clone();
        let proactive_master = self.proactive_master.clone();

        tokio::spawn(async move {
            loop {
                tokio::time::sleep(std::time::Duration::from_secs(15)).await; // Every 15 seconds

                let proactive_guard = proactive_master.read().await;
                if let Err(e) = proactive_guard.analyze_and_decide(&agents, &bus).await {
                    error!("Error in high-frequency proactive analysis: {}", e);
                }
            }
        });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:test_master_claude_creation (lines 1608-1624) ---[0m
    async fn test_master_claude_creation() {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Initialize git repo
        crate::git::WorktreeManager::init_if_needed(&repo_path)
            .await
            .unwrap();

        let config = create_test_config();
        let master = MasterClaude::new(config, repo_path).await.unwrap();

        assert!(master.id.starts_with("master-claude-"));

        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Initializing);
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1443-1463 method identify_task_dependencies <-> ./crates/ccswarm/src/orchestrator/mod.rs:1608-1624 function test_master_claude_creation
  Similarity: 77.32%

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:identify_task_dependencies (lines 1443-1463) ---[0m
    async fn identify_task_dependencies(&self, task: &Task) -> Vec<String> {
        let mut dependencies = Vec::new();

        // Check task description for dependency indicators
        let description_lower = task.description.to_lowercase();

        if description_lower.contains("api") || description_lower.contains("endpoint") {
            dependencies.push("backend_api".to_string());
        }
        if description_lower.contains("ui") || description_lower.contains("component") {
            dependencies.push("frontend_components".to_string());
        }
        if description_lower.contains("database") || description_lower.contains("migration") {
            dependencies.push("database_schema".to_string());
        }
        if description_lower.contains("deploy") || description_lower.contains("infrastructure") {
            dependencies.push("deployment_pipeline".to_string());
        }

        dependencies
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:test_master_claude_creation (lines 1608-1624) ---[0m
    async fn test_master_claude_creation() {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Initialize git repo
        crate::git::WorktreeManager::init_if_needed(&repo_path)
            .await
            .unwrap();

        let config = create_test_config();
        let master = MasterClaude::new(config, repo_path).await.unwrap();

        assert!(master.id.starts_with("master-claude-"));

        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Initializing);
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1466-1480 method find_similar_completed_tasks <-> ./crates/ccswarm/src/orchestrator/mod.rs:1483-1503 method enable_proactive_mode
  Similarity: 80.22%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:find_similar_completed_tasks (lines 1466-1480) ---[0m
    async fn find_similar_completed_tasks(&self, task: &Task) -> Vec<String> {
        let state = self.state.read().await;
        let mut similar_tasks = Vec::new();

        // Look through review history for similar task types
        for (task_id, _) in state.review_history.iter().take(10) {
            // Use debug format for TaskType since it doesn't implement Display
            let task_type_str = format!("{:?}", task.task_type).to_lowercase();
            if task_id.contains(&task_type_str) {
                similar_tasks.push(task_id.clone());
            }
        }

        similar_tasks
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:enable_proactive_mode (lines 1483-1503) ---[0m
    pub async fn enable_proactive_mode(&self) -> Result<()> {
        info!("Enabling proactive mode - increasing analysis frequency");

        // Start additional proactive analysis loop with higher frequency
        let agents = self.agents.clone();
        let bus = self.coordination_bus.clone();
        let proactive_master = self.proactive_master.clone();

        tokio::spawn(async move {
            loop {
                tokio::time::sleep(std::time::Duration::from_secs(15)).await; // Every 15 seconds

                let proactive_guard = proactive_master.read().await;
                if let Err(e) = proactive_guard.analyze_and_decide(&agents, &bus).await {
                    error!("Error in high-frequency proactive analysis: {}", e);
                }
            }
        });

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1375-1399 method add_milestone <-> ./crates/ccswarm/src/orchestrator/mod.rs:1402-1414 method trigger_proactive_analysis
  Similarity: 75.49%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:add_milestone (lines 1375-1399) ---[0m
    pub async fn add_milestone(
        &self,
        name: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Milestone;

        let milestone_id = format!("milestone-{}", uuid::Uuid::new_v4());
        let milestone = Milestone {
            id: milestone_id.clone(),
            name,
            description,
            deadline,
            completion_percentage: 0.0,
            dependencies: vec![],
            critical_path: false,
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.add_milestone(milestone).await?;

        info!("Added milestone: {}", milestone_id);
        Ok(milestone_id)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:trigger_proactive_analysis (lines 1402-1414) ---[0m
    pub async fn trigger_proactive_analysis(
        &self,
    ) -> Result<Vec<self::proactive_master::ProactiveDecision>> {
        info!("Triggering immediate proactive analysis");

        let proactive_master = self.proactive_master.read().await;
        let decisions = proactive_master
            .analyze_and_decide(&self.agents, &self.coordination_bus)
            .await?;

        info!("Proactive analysis generated {} decisions", decisions.len());
        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1375-1399 method add_milestone <-> ./crates/ccswarm/src/orchestrator/mod.rs:1466-1480 method find_similar_completed_tasks
  Similarity: 71.48%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:add_milestone (lines 1375-1399) ---[0m
    pub async fn add_milestone(
        &self,
        name: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Milestone;

        let milestone_id = format!("milestone-{}", uuid::Uuid::new_v4());
        let milestone = Milestone {
            id: milestone_id.clone(),
            name,
            description,
            deadline,
            completion_percentage: 0.0,
            dependencies: vec![],
            critical_path: false,
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.add_milestone(milestone).await?;

        info!("Added milestone: {}", milestone_id);
        Ok(milestone_id)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:find_similar_completed_tasks (lines 1466-1480) ---[0m
    async fn find_similar_completed_tasks(&self, task: &Task) -> Vec<String> {
        let state = self.state.read().await;
        let mut similar_tasks = Vec::new();

        // Look through review history for similar task types
        for (task_id, _) in state.review_history.iter().take(10) {
            // Use debug format for TaskType since it doesn't implement Display
            let task_type_str = format!("{:?}", task.task_type).to_lowercase();
            if task_id.contains(&task_type_str) {
                similar_tasks.push(task_id.clone());
            }
        }

        similar_tasks
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1443-1463 method identify_task_dependencies <-> ./crates/ccswarm/src/orchestrator/mod.rs:1466-1480 method find_similar_completed_tasks
  Similarity: 77.15%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:identify_task_dependencies (lines 1443-1463) ---[0m
    async fn identify_task_dependencies(&self, task: &Task) -> Vec<String> {
        let mut dependencies = Vec::new();

        // Check task description for dependency indicators
        let description_lower = task.description.to_lowercase();

        if description_lower.contains("api") || description_lower.contains("endpoint") {
            dependencies.push("backend_api".to_string());
        }
        if description_lower.contains("ui") || description_lower.contains("component") {
            dependencies.push("frontend_components".to_string());
        }
        if description_lower.contains("database") || description_lower.contains("migration") {
            dependencies.push("database_schema".to_string());
        }
        if description_lower.contains("deploy") || description_lower.contains("infrastructure") {
            dependencies.push("deployment_pipeline".to_string());
        }

        dependencies
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:find_similar_completed_tasks (lines 1466-1480) ---[0m
    async fn find_similar_completed_tasks(&self, task: &Task) -> Vec<String> {
        let state = self.state.read().await;
        let mut similar_tasks = Vec::new();

        // Look through review history for similar task types
        for (task_id, _) in state.review_history.iter().take(10) {
            // Use debug format for TaskType since it doesn't implement Display
            let task_type_str = format!("{:?}", task.task_type).to_lowercase();
            if task_id.contains(&task_type_str) {
                similar_tasks.push(task_id.clone());
            }
        }

        similar_tasks
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1349-1372 method set_objective <-> ./crates/ccswarm/src/orchestrator/mod.rs:1402-1414 method trigger_proactive_analysis
  Similarity: 74.41%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:set_objective (lines 1349-1372) ---[0m
    pub async fn set_objective(
        &self,
        title: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Objective;

        let objective_id = format!("obj-{}", uuid::Uuid::new_v4());
        let objective = Objective {
            id: objective_id.clone(),
            title,
            description,
            deadline,
            progress: 0.0,
            key_results: vec![],
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.set_objective(objective).await?;

        info!("Set project objective: {}", objective_id);
        Ok(objective_id)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:trigger_proactive_analysis (lines 1402-1414) ---[0m
    pub async fn trigger_proactive_analysis(
        &self,
    ) -> Result<Vec<self::proactive_master::ProactiveDecision>> {
        info!("Triggering immediate proactive analysis");

        let proactive_master = self.proactive_master.read().await;
        let decisions = proactive_master
            .analyze_and_decide(&self.agents, &self.coordination_bus)
            .await?;

        info!("Proactive analysis generated {} decisions", decisions.len());
        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1349-1372 method set_objective <-> ./crates/ccswarm/src/orchestrator/mod.rs:1466-1480 method find_similar_completed_tasks
  Similarity: 70.22%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:set_objective (lines 1349-1372) ---[0m
    pub async fn set_objective(
        &self,
        title: String,
        description: String,
        deadline: Option<DateTime<Utc>>,
    ) -> Result<String> {
        use self::proactive_master::Objective;

        let objective_id = format!("obj-{}", uuid::Uuid::new_v4());
        let objective = Objective {
            id: objective_id.clone(),
            title,
            description,
            deadline,
            progress: 0.0,
            key_results: vec![],
        };

        let proactive_master = self.proactive_master.read().await;
        proactive_master.set_objective(objective).await?;

        info!("Set project objective: {}", objective_id);
        Ok(objective_id)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:find_similar_completed_tasks (lines 1466-1480) ---[0m
    async fn find_similar_completed_tasks(&self, task: &Task) -> Vec<String> {
        let state = self.state.read().await;
        let mut similar_tasks = Vec::new();

        // Look through review history for similar task types
        for (task_id, _) in state.review_history.iter().take(10) {
            // Use debug format for TaskType since it doesn't implement Display
            let task_type_str = format!("{:?}", task.task_type).to_lowercase();
            if task_id.contains(&task_type_str) {
                similar_tasks.push(task_id.clone());
            }
        }

        similar_tasks
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1402-1414 method trigger_proactive_analysis <-> ./crates/ccswarm/src/orchestrator/mod.rs:1466-1480 method find_similar_completed_tasks
  Similarity: 81.15%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:trigger_proactive_analysis (lines 1402-1414) ---[0m
    pub async fn trigger_proactive_analysis(
        &self,
    ) -> Result<Vec<self::proactive_master::ProactiveDecision>> {
        info!("Triggering immediate proactive analysis");

        let proactive_master = self.proactive_master.read().await;
        let decisions = proactive_master
            .analyze_and_decide(&self.agents, &self.coordination_bus)
            .await?;

        info!("Proactive analysis generated {} decisions", decisions.len());
        Ok(decisions)
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:find_similar_completed_tasks (lines 1466-1480) ---[0m
    async fn find_similar_completed_tasks(&self, task: &Task) -> Vec<String> {
        let state = self.state.read().await;
        let mut similar_tasks = Vec::new();

        // Look through review history for similar task types
        for (task_id, _) in state.review_history.iter().take(10) {
            // Use debug format for TaskType since it doesn't implement Display
            let task_type_str = format!("{:?}", task.task_type).to_lowercase();
            if task_id.contains(&task_type_str) {
                similar_tasks.push(task_id.clone());
            }
        }

        similar_tasks
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:729-742 method restart_agent <-> ./crates/ccswarm/src/orchestrator/mod.rs:1627-1637 function test_task_assignment
  Similarity: 90.53%

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:restart_agent (lines 729-742) ---[0m
    async fn restart_agent(&self, agent_id: &str) -> Result<()> {
        warn!("Restarting agent: {}", agent_id);

        if let Some(mut agent) = self.agents.get_mut(agent_id) {
            // Reset agent status
            agent.status = AgentStatus::Available;
            agent.last_activity = Utc::now();

            // Re-establish identity
            agent.establish_identity().await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:test_task_assignment (lines 1627-1637) ---[0m
    async fn test_task_assignment() {
        let task = Task::new(
            "test-1".to_string(),
            "Create React component".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        // Test that frontend tasks are recognized
        assert!(task.description.to_lowercase().contains("component"));
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:729-742 method restart_agent <-> ./crates/ccswarm/src/orchestrator/mod.rs:1466-1480 method find_similar_completed_tasks
  Similarity: 77.85%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:restart_agent (lines 729-742) ---[0m
    async fn restart_agent(&self, agent_id: &str) -> Result<()> {
        warn!("Restarting agent: {}", agent_id);

        if let Some(mut agent) = self.agents.get_mut(agent_id) {
            // Reset agent status
            agent.status = AgentStatus::Available;
            agent.last_activity = Utc::now();

            // Re-establish identity
            agent.establish_identity().await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:find_similar_completed_tasks (lines 1466-1480) ---[0m
    async fn find_similar_completed_tasks(&self, task: &Task) -> Vec<String> {
        let state = self.state.read().await;
        let mut similar_tasks = Vec::new();

        // Look through review history for similar task types
        for (task_id, _) in state.review_history.iter().take(10) {
            // Use debug format for TaskType since it doesn't implement Display
            let task_type_str = format!("{:?}", task.task_type).to_lowercase();
            if task_id.contains(&task_type_str) {
                similar_tasks.push(task_id.clone());
            }
        }

        similar_tasks
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:397-407 method add_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:1466-1480 method find_similar_completed_tasks
  Similarity: 84.69%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:add_task (lines 397-407) ---[0m
    pub async fn add_task(&self, task: Task) -> Result<()> {
        self.task_queue_tx
            .send(task.clone())
            .await
            .context("Failed to add task to queue")?;

        let mut state = self.state.write().await;
        state.pending_tasks.push(task);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:find_similar_completed_tasks (lines 1466-1480) ---[0m
    async fn find_similar_completed_tasks(&self, task: &Task) -> Vec<String> {
        let state = self.state.read().await;
        let mut similar_tasks = Vec::new();

        // Look through review history for similar task types
        for (task_id, _) in state.review_history.iter().take(10) {
            // Use debug format for TaskType since it doesn't implement Display
            let task_type_str = format!("{:?}", task.task_type).to_lowercase();
            if task_id.contains(&task_type_str) {
                similar_tasks.push(task_id.clone());
            }
        }

        similar_tasks
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:1466-1480 method find_similar_completed_tasks <-> ./crates/ccswarm/src/orchestrator/mod.rs:1627-1637 function test_task_assignment
  Similarity: 82.62%

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:find_similar_completed_tasks (lines 1466-1480) ---[0m
    async fn find_similar_completed_tasks(&self, task: &Task) -> Vec<String> {
        let state = self.state.read().await;
        let mut similar_tasks = Vec::new();

        // Look through review history for similar task types
        for (task_id, _) in state.review_history.iter().take(10) {
            // Use debug format for TaskType since it doesn't implement Display
            let task_type_str = format!("{:?}", task.task_type).to_lowercase();
            if task_id.contains(&task_type_str) {
                similar_tasks.push(task_id.clone());
            }
        }

        similar_tasks
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:test_task_assignment (lines 1627-1637) ---[0m
    async fn test_task_assignment() {
        let task = Task::new(
            "test-1".to_string(),
            "Create React component".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        // Test that frontend tasks are recognized
        assert!(task.description.to_lowercase().contains("component"));
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:397-407 method add_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:729-742 method restart_agent
  Similarity: 76.38%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:add_task (lines 397-407) ---[0m
    pub async fn add_task(&self, task: Task) -> Result<()> {
        self.task_queue_tx
            .send(task.clone())
            .await
            .context("Failed to add task to queue")?;

        let mut state = self.state.write().await;
        state.pending_tasks.push(task);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:restart_agent (lines 729-742) ---[0m
    async fn restart_agent(&self, agent_id: &str) -> Result<()> {
        warn!("Restarting agent: {}", agent_id);

        if let Some(mut agent) = self.agents.get_mut(agent_id) {
            // Reset agent status
            agent.status = AgentStatus::Available;
            agent.last_activity = Utc::now();

            // Re-establish identity
            agent.establish_identity().await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:397-407 method add_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:1402-1414 method trigger_proactive_analysis
  Similarity: 78.64%
  Classes: MasterClaude <-> MasterClaude

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:add_task (lines 397-407) ---[0m
    pub async fn add_task(&self, task: Task) -> Result<()> {
        self.task_queue_tx
            .send(task.clone())
            .await
            .context("Failed to add task to queue")?;

        let mut state = self.state.write().await;
        state.pending_tasks.push(task);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:trigger_proactive_analysis (lines 1402-1414) ---[0m
    pub async fn trigger_proactive_analysis(
        &self,
    ) -> Result<Vec<self::proactive_master::ProactiveDecision>> {
        info!("Triggering immediate proactive analysis");

        let proactive_master = self.proactive_master.read().await;
        let decisions = proactive_master
            .analyze_and_decide(&self.agents, &self.coordination_bus)
            .await?;

        info!("Proactive analysis generated {} decisions", decisions.len());
        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/mod.rs:397-407 method add_task <-> ./crates/ccswarm/src/orchestrator/mod.rs:1627-1637 function test_task_assignment
  Similarity: 73.00%

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:add_task (lines 397-407) ---[0m
    pub async fn add_task(&self, task: Task) -> Result<()> {
        self.task_queue_tx
            .send(task.clone())
            .await
            .context("Failed to add task to queue")?;

        let mut state = self.state.write().await;
        state.pending_tasks.push(task);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/mod.rs:test_task_assignment (lines 1627-1637) ---[0m
    async fn test_task_assignment() {
        let task = Task::new(
            "test-1".to_string(),
            "Create React component".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        // Test that frontend tasks are recognized
        assert!(task.description.to_lowercase().contains("component"));
    }


Duplicates in ./crates/ccswarm/src/cli/interactive_help.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/cli/interactive_help.rs:294-327 method show_topic_list <-> ./crates/ccswarm/src/cli/interactive_help.rs:329-374 method display_topic
  Similarity: 88.17%
  Classes: InteractiveHelp <-> InteractiveHelp

[36m--- ./crates/ccswarm/src/cli/interactive_help.rs:show_topic_list (lines 294-327) ---[0m
    pub fn show_topic_list(&self) {
        println!();
        println!("{}", "📚 Available Help Topics".bright_cyan().bold());
        println!();

        let topics = vec![
            (
                "getting-started",
                "Learn the basics and set up your first project",
            ),
            ("tasks", "Create and manage tasks for AI agents"),
            ("agents", "Work with specialized AI agents"),
            ("sessions", "Manage AI-powered terminal sessions"),
            ("quality", "Code quality and automated reviews"),
            ("troubleshooting", "Fix common problems"),
            ("errors", "Error codes and recovery procedures"),
        ];

        for (key, desc) in topics {
            if self.topics.contains_key(key) {
                println!(
                    "  {} {}",
                    format!("{:>16}", key).bright_white(),
                    format!("- {}", desc).dimmed()
                );
            }
        }

        println!();
        println!("{}", "💡 Usage:".bright_yellow());
        println!("  ccswarm help <topic>     View detailed help");
        println!("  ccswarm help search <q>  Search help topics");
        println!();
    }

[36m--- ./crates/ccswarm/src/cli/interactive_help.rs:display_topic (lines 329-374) ---[0m
    fn display_topic(&self, topic: &HelpTopic) {
        println!();
        println!("{}", topic.title.bright_cyan().bold());
        println!("{}", "─".repeat(topic.title.len()).bright_cyan());
        println!();
        println!("{}", topic.description.white());

        if !topic.examples.is_empty() {
            println!();
            println!("{}", "📝 Examples:".bright_yellow());
            println!();

            for example in &topic.examples {
                println!("  {}", example.command.bright_white());
                println!("  {}", example.description.dimmed());

                if let Some(output) = &example.output {
                    println!();
                    for line in output.lines() {
                        println!("  {}", line.bright_green());
                    }
                }
                println!();
            }
        }

        if !topic.tips.is_empty() {
            println!("{}", "💡 Tips:".bright_yellow());
            for tip in &topic.tips {
                println!("  • {}", tip.white());
            }
            println!();
        }

        if !topic.related.is_empty() {
            println!("{}", "🔗 Related topics:".bright_cyan());
            print!("  ");
            for (i, related) in topic.related.iter().enumerate() {
                if i > 0 {
                    print!(", ");
                }
                print!("{}", related.bright_white());
            }
            println!("\n");
        }
    }

  ./crates/ccswarm/src/cli/interactive_help.rs:294-327 method show_topic_list <-> ./crates/ccswarm/src/cli/interactive_help.rs:391-422 method calculate_relevance
  Similarity: 82.16%
  Classes: InteractiveHelp <-> InteractiveHelp

[36m--- ./crates/ccswarm/src/cli/interactive_help.rs:show_topic_list (lines 294-327) ---[0m
    pub fn show_topic_list(&self) {
        println!();
        println!("{}", "📚 Available Help Topics".bright_cyan().bold());
        println!();

        let topics = vec![
            (
                "getting-started",
                "Learn the basics and set up your first project",
            ),
            ("tasks", "Create and manage tasks for AI agents"),
            ("agents", "Work with specialized AI agents"),
            ("sessions", "Manage AI-powered terminal sessions"),
            ("quality", "Code quality and automated reviews"),
            ("troubleshooting", "Fix common problems"),
            ("errors", "Error codes and recovery procedures"),
        ];

        for (key, desc) in topics {
            if self.topics.contains_key(key) {
                println!(
                    "  {} {}",
                    format!("{:>16}", key).bright_white(),
                    format!("- {}", desc).dimmed()
                );
            }
        }

        println!();
        println!("{}", "💡 Usage:".bright_yellow());
        println!("  ccswarm help <topic>     View detailed help");
        println!("  ccswarm help search <q>  Search help topics");
        println!();
    }

[36m--- ./crates/ccswarm/src/cli/interactive_help.rs:calculate_relevance (lines 391-422) ---[0m
    fn calculate_relevance(&self, topic: &HelpTopic, query: &str) -> u32 {
        let mut score = 0;

        // Title match (highest weight)
        if topic.title.to_lowercase().contains(query) {
            score += 100;
        }

        // Description match
        if topic.description.to_lowercase().contains(query) {
            score += 50;
        }

        // Example matches
        for example in &topic.examples {
            if example.command.to_lowercase().contains(query) {
                score += 30;
            }
            if example.description.to_lowercase().contains(query) {
                score += 20;
            }
        }

        // Tips matches
        for tip in &topic.tips {
            if tip.to_lowercase().contains(query) {
                score += 10;
            }
        }

        score
    }

  ./crates/ccswarm/src/cli/interactive_help.rs:391-422 method calculate_relevance <-> ./crates/ccswarm/src/cli/interactive_help.rs:426-460 function show_quick_help
  Similarity: 73.44%

[36m--- ./crates/ccswarm/src/cli/interactive_help.rs:calculate_relevance (lines 391-422) ---[0m
    fn calculate_relevance(&self, topic: &HelpTopic, query: &str) -> u32 {
        let mut score = 0;

        // Title match (highest weight)
        if topic.title.to_lowercase().contains(query) {
            score += 100;
        }

        // Description match
        if topic.description.to_lowercase().contains(query) {
            score += 50;
        }

        // Example matches
        for example in &topic.examples {
            if example.command.to_lowercase().contains(query) {
                score += 30;
            }
            if example.description.to_lowercase().contains(query) {
                score += 20;
            }
        }

        // Tips matches
        for tip in &topic.tips {
            if tip.to_lowercase().contains(query) {
                score += 10;
            }
        }

        score
    }

[36m--- ./crates/ccswarm/src/cli/interactive_help.rs:show_quick_help (lines 426-460) ---[0m
pub fn show_quick_help(context: &str) {
    let tips = match context {
        "task-created" => vec![
            "View task progress: ccswarm task status <id>",
            "List all tasks: ccswarm task list",
            "Tasks are automatically delegated to the best agent",
        ],
        "session-created" => vec![
            "Attach to session: ccswarm session attach <id>",
            "Sessions provide efficient context management",
            "Sessions persist across ccswarm restarts",
        ],
        "agent-busy" => vec![
            "Check agent status: ccswarm agent status",
            "Queue tasks will be processed in order",
            "Use --force to interrupt (not recommended)",
        ],
        "error-occurred" => vec![
            "Enable debug logs: RUST_LOG=debug ccswarm <command>",
            "Check system health: ccswarm doctor",
            "View detailed help: ccswarm help troubleshooting",
        ],
        _ => vec![
            "Get help: ccswarm help",
            "View examples: ccswarm help <topic>",
            "Join our Discord for support",
        ],
    };

    println!();
    println!("{}", "💡 Quick tips:".bright_yellow());
    for tip in tips {
        println!("  • {}", tip.dimmed());
    }
}

  ./crates/ccswarm/src/cli/interactive_help.rs:294-327 method show_topic_list <-> ./crates/ccswarm/src/cli/interactive_help.rs:426-460 function show_quick_help
  Similarity: 70.07%

[36m--- ./crates/ccswarm/src/cli/interactive_help.rs:show_topic_list (lines 294-327) ---[0m
    pub fn show_topic_list(&self) {
        println!();
        println!("{}", "📚 Available Help Topics".bright_cyan().bold());
        println!();

        let topics = vec![
            (
                "getting-started",
                "Learn the basics and set up your first project",
            ),
            ("tasks", "Create and manage tasks for AI agents"),
            ("agents", "Work with specialized AI agents"),
            ("sessions", "Manage AI-powered terminal sessions"),
            ("quality", "Code quality and automated reviews"),
            ("troubleshooting", "Fix common problems"),
            ("errors", "Error codes and recovery procedures"),
        ];

        for (key, desc) in topics {
            if self.topics.contains_key(key) {
                println!(
                    "  {} {}",
                    format!("{:>16}", key).bright_white(),
                    format!("- {}", desc).dimmed()
                );
            }
        }

        println!();
        println!("{}", "💡 Usage:".bright_yellow());
        println!("  ccswarm help <topic>     View detailed help");
        println!("  ccswarm help search <q>  Search help topics");
        println!();
    }

[36m--- ./crates/ccswarm/src/cli/interactive_help.rs:show_quick_help (lines 426-460) ---[0m
pub fn show_quick_help(context: &str) {
    let tips = match context {
        "task-created" => vec![
            "View task progress: ccswarm task status <id>",
            "List all tasks: ccswarm task list",
            "Tasks are automatically delegated to the best agent",
        ],
        "session-created" => vec![
            "Attach to session: ccswarm session attach <id>",
            "Sessions provide efficient context management",
            "Sessions persist across ccswarm restarts",
        ],
        "agent-busy" => vec![
            "Check agent status: ccswarm agent status",
            "Queue tasks will be processed in order",
            "Use --force to interrupt (not recommended)",
        ],
        "error-occurred" => vec![
            "Enable debug logs: RUST_LOG=debug ccswarm <command>",
            "Check system health: ccswarm doctor",
            "View detailed help: ccswarm help troubleshooting",
        ],
        _ => vec![
            "Get help: ccswarm help",
            "View examples: ccswarm help <topic>",
            "Join our Discord for support",
        ],
    };

    println!();
    println!("{}", "💡 Quick tips:".bright_yellow());
    for tip in tips {
        println!("  • {}", tip.dimmed());
    }
}

  ./crates/ccswarm/src/cli/interactive_help.rs:376-389 method search <-> ./crates/ccswarm/src/cli/interactive_help.rs:426-460 function show_quick_help
  Similarity: 79.20%

[36m--- ./crates/ccswarm/src/cli/interactive_help.rs:search (lines 376-389) ---[0m
    pub fn search(&self, query: &str) -> Vec<(&str, &HelpTopic)> {
        let query_lower = query.to_lowercase();
        let mut results = Vec::new();

        for (key, topic) in &self.topics {
            let score = self.calculate_relevance(topic, &query_lower);
            if score > 0 {
                results.push((key.as_str(), topic, score));
            }
        }

        results.sort_by(|a, b| b.2.cmp(&a.2));
        results.into_iter().map(|(k, t, _)| (k, t)).collect()
    }

[36m--- ./crates/ccswarm/src/cli/interactive_help.rs:show_quick_help (lines 426-460) ---[0m
pub fn show_quick_help(context: &str) {
    let tips = match context {
        "task-created" => vec![
            "View task progress: ccswarm task status <id>",
            "List all tasks: ccswarm task list",
            "Tasks are automatically delegated to the best agent",
        ],
        "session-created" => vec![
            "Attach to session: ccswarm session attach <id>",
            "Sessions provide efficient context management",
            "Sessions persist across ccswarm restarts",
        ],
        "agent-busy" => vec![
            "Check agent status: ccswarm agent status",
            "Queue tasks will be processed in order",
            "Use --force to interrupt (not recommended)",
        ],
        "error-occurred" => vec![
            "Enable debug logs: RUST_LOG=debug ccswarm <command>",
            "Check system health: ccswarm doctor",
            "View detailed help: ccswarm help troubleshooting",
        ],
        _ => vec![
            "Get help: ccswarm help",
            "View examples: ccswarm help <topic>",
            "Join our Discord for support",
        ],
    };

    println!();
    println!("{}", "💡 Quick tips:".bright_yellow());
    for tip in tips {
        println!("  • {}", tip.dimmed());
    }
}

  ./crates/ccswarm/src/cli/interactive_help.rs:294-327 method show_topic_list <-> ./crates/ccswarm/src/cli/interactive_help.rs:376-389 method search
  Similarity: 73.52%
  Classes: InteractiveHelp <-> InteractiveHelp

[36m--- ./crates/ccswarm/src/cli/interactive_help.rs:show_topic_list (lines 294-327) ---[0m
    pub fn show_topic_list(&self) {
        println!();
        println!("{}", "📚 Available Help Topics".bright_cyan().bold());
        println!();

        let topics = vec![
            (
                "getting-started",
                "Learn the basics and set up your first project",
            ),
            ("tasks", "Create and manage tasks for AI agents"),
            ("agents", "Work with specialized AI agents"),
            ("sessions", "Manage AI-powered terminal sessions"),
            ("quality", "Code quality and automated reviews"),
            ("troubleshooting", "Fix common problems"),
            ("errors", "Error codes and recovery procedures"),
        ];

        for (key, desc) in topics {
            if self.topics.contains_key(key) {
                println!(
                    "  {} {}",
                    format!("{:>16}", key).bright_white(),
                    format!("- {}", desc).dimmed()
                );
            }
        }

        println!();
        println!("{}", "💡 Usage:".bright_yellow());
        println!("  ccswarm help <topic>     View detailed help");
        println!("  ccswarm help search <q>  Search help topics");
        println!();
    }

[36m--- ./crates/ccswarm/src/cli/interactive_help.rs:search (lines 376-389) ---[0m
    pub fn search(&self, query: &str) -> Vec<(&str, &HelpTopic)> {
        let query_lower = query.to_lowercase();
        let mut results = Vec::new();

        for (key, topic) in &self.topics {
            let score = self.calculate_relevance(topic, &query_lower);
            if score > 0 {
                results.push((key.as_str(), topic, score));
            }
        }

        results.sort_by(|a, b| b.2.cmp(&a.2));
        results.into_iter().map(|(k, t, _)| (k, t)).collect()
    }


Duplicates in ./crates/ccswarm/src/resource/monitoring_trait.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/resource/monitoring_trait.rs:258-272 function test_unified_monitor <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:275-292 function test_exceeding_limits_detection
  Similarity: 84.09%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:test_unified_monitor (lines 258-272) ---[0m
    async fn test_unified_monitor() {
        let monitor = MonitorFactory::create_agent_monitor();
        
        let agent = AgentResource {
            name: "test-agent".to_string(),
            pid: Some(1234),
            limits: ResourceLimits::default(),
        };
        
        monitor.register(agent).await.unwrap();
        monitor.update_all().await.unwrap();
        
        let metrics = monitor.get_metrics("test-agent").await;
        assert!(metrics.is_some());
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:test_exceeding_limits_detection (lines 275-292) ---[0m
    async fn test_exceeding_limits_detection() {
        let monitor = Arc::new(UnifiedMonitor::<AgentResource>::new(
            std::time::Duration::from_secs(1)
        ));
        
        let mut agent = AgentResource {
            name: "high-usage-agent".to_string(),
            pid: Some(5678),
            limits: ResourceLimits::default(),
        };
        agent.limits.cpu_limit = Some(50.0);
        
        monitor.register(agent).await.unwrap();
        monitor.update_all().await.unwrap();
        
        let exceeding = monitor.find_exceeding_limits().await;
        // Would check actual exceeding based on real metrics
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:76-92 method update_all <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:100-114 method find_exceeding_limits
  Similarity: 86.19%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:update_all (lines 76-92) ---[0m
    pub async fn update_all(&self) -> Result<()> {
        let resources = self.resources.read().await;
        let mut metrics = self.metrics_cache.write().await;
        
        for (id, resource) in resources.iter() {
            match resource.collect_metrics().await {
                Ok(m) => {
                    metrics.insert(id.clone(), m);
                }
                Err(e) => {
                    log::warn!("Failed to collect metrics for {}: {}", id, e);
                }
            }
        }
        
        Ok(())
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:find_exceeding_limits (lines 100-114) ---[0m
    pub async fn find_exceeding_limits(&self) -> Vec<(String, R::Metrics)> {
        let resources = self.resources.read().await;
        let metrics = self.metrics_cache.read().await;
        
        let mut exceeding = Vec::new();
        for (id, resource) in resources.iter() {
            if let Some(m) = metrics.get(id) {
                if !resource.is_healthy(m) {
                    exceeding.push((id.clone(), m.clone()));
                }
            }
        }
        
        exceeding
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:76-92 method update_all <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:172-184 method is_healthy
  Similarity: 82.65%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:update_all (lines 76-92) ---[0m
    pub async fn update_all(&self) -> Result<()> {
        let resources = self.resources.read().await;
        let mut metrics = self.metrics_cache.write().await;
        
        for (id, resource) in resources.iter() {
            match resource.collect_metrics().await {
                Ok(m) => {
                    metrics.insert(id.clone(), m);
                }
                Err(e) => {
                    log::warn!("Failed to collect metrics for {}: {}", id, e);
                }
            }
        }
        
        Ok(())
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:is_healthy (lines 172-184) ---[0m
    fn is_healthy(&self, metrics: &Self::Metrics) -> bool {
        if let Some(cpu_limit) = self.limits.cpu_limit {
            if metrics.cpu_usage > cpu_limit {
                return false;
            }
        }
        if let Some(mem_limit) = self.limits.memory_limit {
            if metrics.memory_usage > mem_limit {
                return false;
            }
        }
        true
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:160-170 method collect_metrics <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:172-184 method is_healthy
  Similarity: 94.46%
  Classes: MonitorableResource <-> MonitorableResource

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:collect_metrics (lines 160-170) ---[0m
    async fn collect_metrics(&self) -> Result<Self::Metrics> {
        // Implementation to collect actual metrics
        // This replaces the duplicated collection logic
        Ok(AgentMetrics {
            cpu_usage: 0.0, // Would be actual CPU usage
            memory_usage: 0, // Would be actual memory
            disk_usage: 0,   // Would be actual disk
            active_tasks: 0,
            last_updated: std::time::SystemTime::now(),
        })
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:is_healthy (lines 172-184) ---[0m
    fn is_healthy(&self, metrics: &Self::Metrics) -> bool {
        if let Some(cpu_limit) = self.limits.cpu_limit {
            if metrics.cpu_usage > cpu_limit {
                return false;
            }
        }
        if let Some(mem_limit) = self.limits.memory_limit {
            if metrics.memory_usage > mem_limit {
                return false;
            }
        }
        true
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:100-114 method find_exceeding_limits <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:172-184 method is_healthy
  Similarity: 78.28%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:find_exceeding_limits (lines 100-114) ---[0m
    pub async fn find_exceeding_limits(&self) -> Vec<(String, R::Metrics)> {
        let resources = self.resources.read().await;
        let metrics = self.metrics_cache.read().await;
        
        let mut exceeding = Vec::new();
        for (id, resource) in resources.iter() {
            if let Some(m) = metrics.get(id) {
                if !resource.is_healthy(m) {
                    exceeding.push((id.clone(), m.clone()));
                }
            }
        }
        
        exceeding
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:is_healthy (lines 172-184) ---[0m
    fn is_healthy(&self, metrics: &Self::Metrics) -> bool {
        if let Some(cpu_limit) = self.limits.cpu_limit {
            if metrics.cpu_usage > cpu_limit {
                return false;
            }
        }
        if let Some(mem_limit) = self.limits.memory_limit {
            if metrics.memory_usage > mem_limit {
                return false;
            }
        }
        true
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:100-114 method find_exceeding_limits <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:258-272 function test_unified_monitor
  Similarity: 70.34%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:find_exceeding_limits (lines 100-114) ---[0m
    pub async fn find_exceeding_limits(&self) -> Vec<(String, R::Metrics)> {
        let resources = self.resources.read().await;
        let metrics = self.metrics_cache.read().await;
        
        let mut exceeding = Vec::new();
        for (id, resource) in resources.iter() {
            if let Some(m) = metrics.get(id) {
                if !resource.is_healthy(m) {
                    exceeding.push((id.clone(), m.clone()));
                }
            }
        }
        
        exceeding
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:test_unified_monitor (lines 258-272) ---[0m
    async fn test_unified_monitor() {
        let monitor = MonitorFactory::create_agent_monitor();
        
        let agent = AgentResource {
            name: "test-agent".to_string(),
            pid: Some(1234),
            limits: ResourceLimits::default(),
        };
        
        monitor.register(agent).await.unwrap();
        monitor.update_all().await.unwrap();
        
        let metrics = monitor.get_metrics("test-agent").await;
        assert!(metrics.is_some());
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:172-184 method is_healthy <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:223-231 method is_healthy
  Similarity: 90.24%
  Classes: MonitorableResource <-> MonitorableResource

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:is_healthy (lines 172-184) ---[0m
    fn is_healthy(&self, metrics: &Self::Metrics) -> bool {
        if let Some(cpu_limit) = self.limits.cpu_limit {
            if metrics.cpu_usage > cpu_limit {
                return false;
            }
        }
        if let Some(mem_limit) = self.limits.memory_limit {
            if metrics.memory_usage > mem_limit {
                return false;
            }
        }
        true
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:is_healthy (lines 223-231) ---[0m
    fn is_healthy(&self, metrics: &Self::Metrics) -> bool {
        // Check token limits, context size, etc.
        if let Some(token_limit) = self.limits.custom_limits.get("max_tokens") {
            if metrics.token_usage as f64 > *token_limit {
                return false;
            }
        }
        true
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:122-133 method start_monitoring <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:172-184 method is_healthy
  Similarity: 78.57%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:start_monitoring (lines 122-133) ---[0m
    pub async fn start_monitoring(self: Arc<Self>) {
        let monitor = self.clone();
        tokio::spawn(async move {
            let mut interval = tokio::time::interval(monitor.update_interval);
            loop {
                interval.tick().await;
                if let Err(e) = monitor.update_all().await {
                    log::error!("Monitoring update failed: {}", e);
                }
            }
        });
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:is_healthy (lines 172-184) ---[0m
    fn is_healthy(&self, metrics: &Self::Metrics) -> bool {
        if let Some(cpu_limit) = self.limits.cpu_limit {
            if metrics.cpu_usage > cpu_limit {
                return false;
            }
        }
        if let Some(mem_limit) = self.limits.memory_limit {
            if metrics.memory_usage > mem_limit {
                return false;
            }
        }
        true
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:160-170 method collect_metrics <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:223-231 method is_healthy
  Similarity: 94.59%
  Classes: MonitorableResource <-> MonitorableResource

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:collect_metrics (lines 160-170) ---[0m
    async fn collect_metrics(&self) -> Result<Self::Metrics> {
        // Implementation to collect actual metrics
        // This replaces the duplicated collection logic
        Ok(AgentMetrics {
            cpu_usage: 0.0, // Would be actual CPU usage
            memory_usage: 0, // Would be actual memory
            disk_usage: 0,   // Would be actual disk
            active_tasks: 0,
            last_updated: std::time::SystemTime::now(),
        })
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:is_healthy (lines 223-231) ---[0m
    fn is_healthy(&self, metrics: &Self::Metrics) -> bool {
        // Check token limits, context size, etc.
        if let Some(token_limit) = self.limits.custom_limits.get("max_tokens") {
            if metrics.token_usage as f64 > *token_limit {
                return false;
            }
        }
        true
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:122-133 method start_monitoring <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:223-231 method is_healthy
  Similarity: 87.32%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:start_monitoring (lines 122-133) ---[0m
    pub async fn start_monitoring(self: Arc<Self>) {
        let monitor = self.clone();
        tokio::spawn(async move {
            let mut interval = tokio::time::interval(monitor.update_interval);
            loop {
                interval.tick().await;
                if let Err(e) = monitor.update_all().await {
                    log::error!("Monitoring update failed: {}", e);
                }
            }
        });
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:is_healthy (lines 223-231) ---[0m
    fn is_healthy(&self, metrics: &Self::Metrics) -> bool {
        // Check token limits, context size, etc.
        if let Some(token_limit) = self.limits.custom_limits.get("max_tokens") {
            if metrics.token_usage as f64 > *token_limit {
                return false;
            }
        }
        true
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:41-48 method default <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:122-133 method start_monitoring
  Similarity: 86.34%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:default (lines 41-48) ---[0m
    fn default() -> Self {
        Self {
            cpu_limit: Some(80.0),
            memory_limit: Some(1024 * 1024 * 1024), // 1GB
            disk_limit: Some(10 * 1024 * 1024 * 1024), // 10GB
            custom_limits: HashMap::new(),
        }
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:start_monitoring (lines 122-133) ---[0m
    pub async fn start_monitoring(self: Arc<Self>) {
        let monitor = self.clone();
        tokio::spawn(async move {
            let mut interval = tokio::time::interval(monitor.update_interval);
            loop {
                interval.tick().await;
                if let Err(e) = monitor.update_all().await {
                    log::error!("Monitoring update failed: {}", e);
                }
            }
        });
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:122-133 method start_monitoring <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:214-221 method collect_metrics
  Similarity: 86.34%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:start_monitoring (lines 122-133) ---[0m
    pub async fn start_monitoring(self: Arc<Self>) {
        let monitor = self.clone();
        tokio::spawn(async move {
            let mut interval = tokio::time::interval(monitor.update_interval);
            loop {
                interval.tick().await;
                if let Err(e) = monitor.update_all().await {
                    log::error!("Monitoring update failed: {}", e);
                }
            }
        });
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:collect_metrics (lines 214-221) ---[0m
    async fn collect_metrics(&self) -> Result<Self::Metrics> {
        Ok(SessionMetrics {
            token_usage: 0,
            context_size: 0,
            duration: std::time::Duration::from_secs(0),
            last_activity: std::time::SystemTime::now(),
        })
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:60-66 method new <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:122-133 method start_monitoring
  Similarity: 86.34%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:new (lines 60-66) ---[0m
    pub fn new(update_interval: std::time::Duration) -> Self {
        Self {
            resources: Arc::new(RwLock::new(HashMap::new())),
            metrics_cache: Arc::new(RwLock::new(HashMap::new())),
            update_interval,
        }
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:start_monitoring (lines 122-133) ---[0m
    pub async fn start_monitoring(self: Arc<Self>) {
        let monitor = self.clone();
        tokio::spawn(async move {
            let mut interval = tokio::time::interval(monitor.update_interval);
            loop {
                interval.tick().await;
                if let Err(e) = monitor.update_all().await {
                    log::error!("Monitoring update failed: {}", e);
                }
            }
        });
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:160-170 method collect_metrics <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:214-221 method collect_metrics
  Similarity: 86.21%
  Classes: MonitorableResource <-> MonitorableResource

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:collect_metrics (lines 160-170) ---[0m
    async fn collect_metrics(&self) -> Result<Self::Metrics> {
        // Implementation to collect actual metrics
        // This replaces the duplicated collection logic
        Ok(AgentMetrics {
            cpu_usage: 0.0, // Would be actual CPU usage
            memory_usage: 0, // Would be actual memory
            disk_usage: 0,   // Would be actual disk
            active_tasks: 0,
            last_updated: std::time::SystemTime::now(),
        })
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:collect_metrics (lines 214-221) ---[0m
    async fn collect_metrics(&self) -> Result<Self::Metrics> {
        Ok(SessionMetrics {
            token_usage: 0,
            context_size: 0,
            duration: std::time::Duration::from_secs(0),
            last_activity: std::time::SystemTime::now(),
        })
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:69-73 method register <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:160-170 method collect_metrics
  Similarity: 89.82%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:register (lines 69-73) ---[0m
    pub async fn register(&self, resource: R) -> Result<()> {
        let id = resource.identifier();
        self.resources.write().await.insert(id.clone(), resource);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:collect_metrics (lines 160-170) ---[0m
    async fn collect_metrics(&self) -> Result<Self::Metrics> {
        // Implementation to collect actual metrics
        // This replaces the duplicated collection logic
        Ok(AgentMetrics {
            cpu_usage: 0.0, // Would be actual CPU usage
            memory_usage: 0, // Would be actual memory
            disk_usage: 0,   // Would be actual disk
            active_tasks: 0,
            last_updated: std::time::SystemTime::now(),
        })
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:60-66 method new <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:160-170 method collect_metrics
  Similarity: 78.62%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:new (lines 60-66) ---[0m
    pub fn new(update_interval: std::time::Duration) -> Self {
        Self {
            resources: Arc::new(RwLock::new(HashMap::new())),
            metrics_cache: Arc::new(RwLock::new(HashMap::new())),
            update_interval,
        }
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:collect_metrics (lines 160-170) ---[0m
    async fn collect_metrics(&self) -> Result<Self::Metrics> {
        // Implementation to collect actual metrics
        // This replaces the duplicated collection logic
        Ok(AgentMetrics {
            cpu_usage: 0.0, // Would be actual CPU usage
            memory_usage: 0, // Would be actual memory
            disk_usage: 0,   // Would be actual disk
            active_tasks: 0,
            last_updated: std::time::SystemTime::now(),
        })
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:214-221 method collect_metrics <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:223-231 method is_healthy
  Similarity: 80.49%
  Classes: MonitorableResource <-> MonitorableResource

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:collect_metrics (lines 214-221) ---[0m
    async fn collect_metrics(&self) -> Result<Self::Metrics> {
        Ok(SessionMetrics {
            token_usage: 0,
            context_size: 0,
            duration: std::time::Duration::from_secs(0),
            last_activity: std::time::SystemTime::now(),
        })
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:is_healthy (lines 223-231) ---[0m
    fn is_healthy(&self, metrics: &Self::Metrics) -> bool {
        // Check token limits, context size, etc.
        if let Some(token_limit) = self.limits.custom_limits.get("max_tokens") {
            if metrics.token_usage as f64 > *token_limit {
                return false;
            }
        }
        true
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:41-48 method default <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:60-66 method new
  Similarity: 86.15%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:default (lines 41-48) ---[0m
    fn default() -> Self {
        Self {
            cpu_limit: Some(80.0),
            memory_limit: Some(1024 * 1024 * 1024), // 1GB
            disk_limit: Some(10 * 1024 * 1024 * 1024), // 10GB
            custom_limits: HashMap::new(),
        }
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:new (lines 60-66) ---[0m
    pub fn new(update_interval: std::time::Duration) -> Self {
        Self {
            resources: Arc::new(RwLock::new(HashMap::new())),
            metrics_cache: Arc::new(RwLock::new(HashMap::new())),
            update_interval,
        }
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:60-66 method new <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:214-221 method collect_metrics
  Similarity: 84.77%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:new (lines 60-66) ---[0m
    pub fn new(update_interval: std::time::Duration) -> Self {
        Self {
            resources: Arc::new(RwLock::new(HashMap::new())),
            metrics_cache: Arc::new(RwLock::new(HashMap::new())),
            update_interval,
        }
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:collect_metrics (lines 214-221) ---[0m
    async fn collect_metrics(&self) -> Result<Self::Metrics> {
        Ok(SessionMetrics {
            token_usage: 0,
            context_size: 0,
            duration: std::time::Duration::from_secs(0),
            last_activity: std::time::SystemTime::now(),
        })
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:95-97 method get_metrics <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:160-170 method collect_metrics
  Similarity: 86.79%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:get_metrics (lines 95-97) ---[0m
    pub async fn get_metrics(&self, id: &str) -> Option<R::Metrics> {
        self.metrics_cache.read().await.get(id).cloned()
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:collect_metrics (lines 160-170) ---[0m
    async fn collect_metrics(&self) -> Result<Self::Metrics> {
        // Implementation to collect actual metrics
        // This replaces the duplicated collection logic
        Ok(AgentMetrics {
            cpu_usage: 0.0, // Would be actual CPU usage
            memory_usage: 0, // Would be actual memory
            disk_usage: 0,   // Would be actual disk
            active_tasks: 0,
            last_updated: std::time::SystemTime::now(),
        })
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:60-66 method new <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:223-231 method is_healthy
  Similarity: 75.08%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:new (lines 60-66) ---[0m
    pub fn new(update_interval: std::time::Duration) -> Self {
        Self {
            resources: Arc::new(RwLock::new(HashMap::new())),
            metrics_cache: Arc::new(RwLock::new(HashMap::new())),
            update_interval,
        }
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:is_healthy (lines 223-231) ---[0m
    fn is_healthy(&self, metrics: &Self::Metrics) -> bool {
        // Check token limits, context size, etc.
        if let Some(token_limit) = self.limits.custom_limits.get("max_tokens") {
            if metrics.token_usage as f64 > *token_limit {
                return false;
            }
        }
        true
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:160-170 method collect_metrics <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:243-245 method create_agent_monitor
  Similarity: 83.02%
  Classes: MonitorableResource <-> MonitorFactory

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:collect_metrics (lines 160-170) ---[0m
    async fn collect_metrics(&self) -> Result<Self::Metrics> {
        // Implementation to collect actual metrics
        // This replaces the duplicated collection logic
        Ok(AgentMetrics {
            cpu_usage: 0.0, // Would be actual CPU usage
            memory_usage: 0, // Would be actual memory
            disk_usage: 0,   // Would be actual disk
            active_tasks: 0,
            last_updated: std::time::SystemTime::now(),
        })
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:create_agent_monitor (lines 243-245) ---[0m
    pub fn create_agent_monitor() -> Arc<UnifiedMonitor<AgentResource>> {
        Arc::new(UnifiedMonitor::new(std::time::Duration::from_secs(5)))
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:160-170 method collect_metrics <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:248-250 method create_session_monitor
  Similarity: 83.02%
  Classes: MonitorableResource <-> MonitorFactory

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:collect_metrics (lines 160-170) ---[0m
    async fn collect_metrics(&self) -> Result<Self::Metrics> {
        // Implementation to collect actual metrics
        // This replaces the duplicated collection logic
        Ok(AgentMetrics {
            cpu_usage: 0.0, // Would be actual CPU usage
            memory_usage: 0, // Would be actual memory
            disk_usage: 0,   // Would be actual disk
            active_tasks: 0,
            last_updated: std::time::SystemTime::now(),
        })
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:create_session_monitor (lines 248-250) ---[0m
    pub fn create_session_monitor() -> Arc<UnifiedMonitor<SessionResource>> {
        Arc::new(UnifiedMonitor::new(std::time::Duration::from_secs(10)))
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:69-73 method register <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:223-231 method is_healthy
  Similarity: 75.08%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:register (lines 69-73) ---[0m
    pub async fn register(&self, resource: R) -> Result<()> {
        let id = resource.identifier();
        self.resources.write().await.insert(id.clone(), resource);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:is_healthy (lines 223-231) ---[0m
    fn is_healthy(&self, metrics: &Self::Metrics) -> bool {
        // Check token limits, context size, etc.
        if let Some(token_limit) = self.limits.custom_limits.get("max_tokens") {
            if metrics.token_usage as f64 > *token_limit {
                return false;
            }
        }
        true
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:95-97 method get_metrics <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:214-221 method collect_metrics
  Similarity: 94.83%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:get_metrics (lines 95-97) ---[0m
    pub async fn get_metrics(&self, id: &str) -> Option<R::Metrics> {
        self.metrics_cache.read().await.get(id).cloned()
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:collect_metrics (lines 214-221) ---[0m
    async fn collect_metrics(&self) -> Result<Self::Metrics> {
        Ok(SessionMetrics {
            token_usage: 0,
            context_size: 0,
            duration: std::time::Duration::from_secs(0),
            last_activity: std::time::SystemTime::now(),
        })
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:214-221 method collect_metrics <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:243-245 method create_agent_monitor
  Similarity: 91.38%
  Classes: MonitorableResource <-> MonitorFactory

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:collect_metrics (lines 214-221) ---[0m
    async fn collect_metrics(&self) -> Result<Self::Metrics> {
        Ok(SessionMetrics {
            token_usage: 0,
            context_size: 0,
            duration: std::time::Duration::from_secs(0),
            last_activity: std::time::SystemTime::now(),
        })
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:create_agent_monitor (lines 243-245) ---[0m
    pub fn create_agent_monitor() -> Arc<UnifiedMonitor<AgentResource>> {
        Arc::new(UnifiedMonitor::new(std::time::Duration::from_secs(5)))
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:214-221 method collect_metrics <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:248-250 method create_session_monitor
  Similarity: 91.38%
  Classes: MonitorableResource <-> MonitorFactory

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:collect_metrics (lines 214-221) ---[0m
    async fn collect_metrics(&self) -> Result<Self::Metrics> {
        Ok(SessionMetrics {
            token_usage: 0,
            context_size: 0,
            duration: std::time::Duration::from_secs(0),
            last_activity: std::time::SystemTime::now(),
        })
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:create_session_monitor (lines 248-250) ---[0m
    pub fn create_session_monitor() -> Arc<UnifiedMonitor<SessionResource>> {
        Arc::new(UnifiedMonitor::new(std::time::Duration::from_secs(10)))
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:223-231 method is_healthy <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:243-245 method create_agent_monitor
  Similarity: 71.80%
  Classes: MonitorableResource <-> MonitorFactory

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:is_healthy (lines 223-231) ---[0m
    fn is_healthy(&self, metrics: &Self::Metrics) -> bool {
        // Check token limits, context size, etc.
        if let Some(token_limit) = self.limits.custom_limits.get("max_tokens") {
            if metrics.token_usage as f64 > *token_limit {
                return false;
            }
        }
        true
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:create_agent_monitor (lines 243-245) ---[0m
    pub fn create_agent_monitor() -> Arc<UnifiedMonitor<AgentResource>> {
        Arc::new(UnifiedMonitor::new(std::time::Duration::from_secs(5)))
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:223-231 method is_healthy <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:248-250 method create_session_monitor
  Similarity: 71.80%
  Classes: MonitorableResource <-> MonitorFactory

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:is_healthy (lines 223-231) ---[0m
    fn is_healthy(&self, metrics: &Self::Metrics) -> bool {
        // Check token limits, context size, etc.
        if let Some(token_limit) = self.limits.custom_limits.get("max_tokens") {
            if metrics.token_usage as f64 > *token_limit {
                return false;
            }
        }
        true
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:create_session_monitor (lines 248-250) ---[0m
    pub fn create_session_monitor() -> Arc<UnifiedMonitor<SessionResource>> {
        Arc::new(UnifiedMonitor::new(std::time::Duration::from_secs(10)))
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:243-245 method create_agent_monitor <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:248-250 method create_session_monitor
  Similarity: 99.06%
  Classes: MonitorFactory <-> MonitorFactory

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:create_agent_monitor (lines 243-245) ---[0m
    pub fn create_agent_monitor() -> Arc<UnifiedMonitor<AgentResource>> {
        Arc::new(UnifiedMonitor::new(std::time::Duration::from_secs(5)))
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:create_session_monitor (lines 248-250) ---[0m
    pub fn create_session_monitor() -> Arc<UnifiedMonitor<SessionResource>> {
        Arc::new(UnifiedMonitor::new(std::time::Duration::from_secs(10)))
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:95-97 method get_metrics <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:243-245 method create_agent_monitor
  Similarity: 87.81%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:get_metrics (lines 95-97) ---[0m
    pub async fn get_metrics(&self, id: &str) -> Option<R::Metrics> {
        self.metrics_cache.read().await.get(id).cloned()
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:create_agent_monitor (lines 243-245) ---[0m
    pub fn create_agent_monitor() -> Arc<UnifiedMonitor<AgentResource>> {
        Arc::new(UnifiedMonitor::new(std::time::Duration::from_secs(5)))
    }

  ./crates/ccswarm/src/resource/monitoring_trait.rs:95-97 method get_metrics <-> ./crates/ccswarm/src/resource/monitoring_trait.rs:248-250 method create_session_monitor
  Similarity: 87.81%

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:get_metrics (lines 95-97) ---[0m
    pub async fn get_metrics(&self, id: &str) -> Option<R::Metrics> {
        self.metrics_cache.read().await.get(id).cloned()
    }

[36m--- ./crates/ccswarm/src/resource/monitoring_trait.rs:create_session_monitor (lines 248-250) ---[0m
    pub fn create_session_monitor() -> Arc<UnifiedMonitor<SessionResource>> {
        Arc::new(UnifiedMonitor::new(std::time::Duration::from_secs(10)))
    }


Duplicates in ./crates/ccswarm/src/monitoring/mod.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/monitoring/mod.rs:60-94 method matches_filter <-> ./crates/ccswarm/src/monitoring/mod.rs:273-313 method add_output
  Similarity: 91.93%
  Classes: OutputEntry <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:matches_filter (lines 60-94) ---[0m
    pub fn matches_filter(&self, filter: &OutputFilter) -> bool {
        // Check agent filter
        if let Some(ref agent_ids) = filter.agent_ids {
            if !agent_ids.contains(&self.agent_id) {
                return false;
            }
        }

        // Check output type filter
        if let Some(ref output_types) = filter.output_types {
            if !output_types.contains(&self.output_type) {
                return false;
            }
        }

        // Check content filter
        if let Some(ref pattern) = filter.content_pattern {
            if !self
                .content
                .to_lowercase()
                .contains(&pattern.to_lowercase())
            {
                return false;
            }
        }

        // Check task filter
        if let Some(ref task_id) = filter.task_id {
            if self.task_id.as_ref() != Some(task_id) {
                return false;
            }
        }

        true
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:add_output (lines 273-313) ---[0m
    pub fn add_output(
        &self,
        agent_id: String,
        agent_type: String,
        output_type: OutputType,
        content: String,
        task_id: Option<String>,
        session_id: String,
    ) -> Result<(), String> {
        let entry = OutputEntry::new(
            agent_id.clone(),
            agent_type,
            output_type,
            content,
            task_id,
            session_id,
        );

        // Send to global broadcast
        let _ = self.global_broadcast.send(entry.clone());

        // Send to agent-specific stream
        if let Some(stream) = self.get_agent_stream(&agent_id) {
            stream.add_output(entry.clone())?;
        } else {
            // Auto-register agent if not exists
            let stream = self.register_agent(agent_id)?;
            stream.add_output(entry.clone())?;
        }

        // Notify subscribers
        if let Ok(subscribers) = self.subscribers.read() {
            for subscriber in subscribers.iter() {
                if subscriber.accepts(&entry) {
                    subscriber.on_output(&entry);
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:273-313 method add_output <-> ./crates/ccswarm/src/monitoring/mod.rs:382-422 method get_stats
  Similarity: 81.96%
  Classes: MonitoringSystem <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:add_output (lines 273-313) ---[0m
    pub fn add_output(
        &self,
        agent_id: String,
        agent_type: String,
        output_type: OutputType,
        content: String,
        task_id: Option<String>,
        session_id: String,
    ) -> Result<(), String> {
        let entry = OutputEntry::new(
            agent_id.clone(),
            agent_type,
            output_type,
            content,
            task_id,
            session_id,
        );

        // Send to global broadcast
        let _ = self.global_broadcast.send(entry.clone());

        // Send to agent-specific stream
        if let Some(stream) = self.get_agent_stream(&agent_id) {
            stream.add_output(entry.clone())?;
        } else {
            // Auto-register agent if not exists
            let stream = self.register_agent(agent_id)?;
            stream.add_output(entry.clone())?;
        }

        // Notify subscribers
        if let Ok(subscribers) = self.subscribers.read() {
            for subscriber in subscribers.iter() {
                if subscriber.accepts(&entry) {
                    subscriber.on_output(&entry);
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:get_stats (lines 382-422) ---[0m
    pub fn get_stats(&self) -> MonitoringStats {
        let streams = match self.streams.read() {
            Ok(s) => s,
            Err(_) => {
                return MonitoringStats {
                    total_entries: 0,
                    entries_per_agent: std::collections::HashMap::new(),
                    entries_per_type: std::collections::HashMap::new(),
                    active_streams: 0,
                    total_subscribers: 0,
                }
            }
        };

        let mut total_entries = 0;
        let mut entries_per_agent = std::collections::HashMap::new();
        let mut entries_per_type: std::collections::HashMap<String, usize> =
            std::collections::HashMap::new();

        for (agent_id, stream) in streams.iter() {
            let count = stream.len();
            total_entries += count;
            entries_per_agent.insert(agent_id.clone(), count);

            // Count by type (sampling last 100 entries)
            for entry in stream.get_recent(100, None) {
                let type_name = format!("{:?}", entry.output_type);
                *entries_per_type.entry(type_name).or_insert(0) += 1;
            }
        }

        let total_subscribers = self.subscribers.read().map(|s| s.len()).unwrap_or(0);

        MonitoringStats {
            total_entries,
            entries_per_agent,
            entries_per_type,
            active_streams: streams.len(),
            total_subscribers,
        }
    }

  ./crates/ccswarm/src/monitoring/mod.rs:60-94 method matches_filter <-> ./crates/ccswarm/src/monitoring/mod.rs:382-422 method get_stats
  Similarity: 80.61%
  Classes: OutputEntry <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:matches_filter (lines 60-94) ---[0m
    pub fn matches_filter(&self, filter: &OutputFilter) -> bool {
        // Check agent filter
        if let Some(ref agent_ids) = filter.agent_ids {
            if !agent_ids.contains(&self.agent_id) {
                return false;
            }
        }

        // Check output type filter
        if let Some(ref output_types) = filter.output_types {
            if !output_types.contains(&self.output_type) {
                return false;
            }
        }

        // Check content filter
        if let Some(ref pattern) = filter.content_pattern {
            if !self
                .content
                .to_lowercase()
                .contains(&pattern.to_lowercase())
            {
                return false;
            }
        }

        // Check task filter
        if let Some(ref task_id) = filter.task_id {
            if self.task_id.as_ref() != Some(task_id) {
                return false;
            }
        }

        true
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:get_stats (lines 382-422) ---[0m
    pub fn get_stats(&self) -> MonitoringStats {
        let streams = match self.streams.read() {
            Ok(s) => s,
            Err(_) => {
                return MonitoringStats {
                    total_entries: 0,
                    entries_per_agent: std::collections::HashMap::new(),
                    entries_per_type: std::collections::HashMap::new(),
                    active_streams: 0,
                    total_subscribers: 0,
                }
            }
        };

        let mut total_entries = 0;
        let mut entries_per_agent = std::collections::HashMap::new();
        let mut entries_per_type: std::collections::HashMap<String, usize> =
            std::collections::HashMap::new();

        for (agent_id, stream) in streams.iter() {
            let count = stream.len();
            total_entries += count;
            entries_per_agent.insert(agent_id.clone(), count);

            // Count by type (sampling last 100 entries)
            for entry in stream.get_recent(100, None) {
                let type_name = format!("{:?}", entry.output_type);
                *entries_per_type.entry(type_name).or_insert(0) += 1;
            }
        }

        let total_subscribers = self.subscribers.read().map(|s| s.len()).unwrap_or(0);

        MonitoringStats {
            total_entries,
            entries_per_agent,
            entries_per_type,
            active_streams: streams.len(),
            total_subscribers,
        }
    }

  ./crates/ccswarm/src/monitoring/mod.rs:128-158 method add_output <-> ./crates/ccswarm/src/monitoring/mod.rs:273-313 method add_output
  Similarity: 83.03%
  Classes: AgentOutputStream <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:add_output (lines 128-158) ---[0m
    pub fn add_output(&self, entry: OutputEntry) -> Result<(), String> {
        // Broadcast to live subscribers
        let _ = self.broadcast_tx.send(entry.clone());

        let mut buffer = self
            .buffer
            .lock()
            .map_err(|e| format!("Failed to lock buffer: {}", e))?;

        // Add new entry
        buffer.push_back(entry);

        // Enforce buffer size limit
        while buffer.len() > MAX_BUFFER_SIZE {
            buffer.pop_front();
        }

        // Periodic cleanup of old entries
        let mut last_cleanup = self
            .last_cleanup
            .lock()
            .map_err(|e| format!("Failed to lock cleanup time: {}", e))?;
        if last_cleanup.elapsed() > Duration::from_secs(300) {
            // Check every 5 minutes
            let cutoff_time = Utc::now() - chrono::Duration::from_std(MAX_OUTPUT_AGE).unwrap();
            buffer.retain(|entry| entry.timestamp > cutoff_time);
            *last_cleanup = Instant::now();
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:add_output (lines 273-313) ---[0m
    pub fn add_output(
        &self,
        agent_id: String,
        agent_type: String,
        output_type: OutputType,
        content: String,
        task_id: Option<String>,
        session_id: String,
    ) -> Result<(), String> {
        let entry = OutputEntry::new(
            agent_id.clone(),
            agent_type,
            output_type,
            content,
            task_id,
            session_id,
        );

        // Send to global broadcast
        let _ = self.global_broadcast.send(entry.clone());

        // Send to agent-specific stream
        if let Some(stream) = self.get_agent_stream(&agent_id) {
            stream.add_output(entry.clone())?;
        } else {
            // Auto-register agent if not exists
            let stream = self.register_agent(agent_id)?;
            stream.add_output(entry.clone())?;
        }

        // Notify subscribers
        if let Ok(subscribers) = self.subscribers.read() {
            for subscriber in subscribers.iter() {
                if subscriber.accepts(&entry) {
                    subscriber.on_output(&entry);
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:128-158 method add_output <-> ./crates/ccswarm/src/monitoring/mod.rs:382-422 method get_stats
  Similarity: 80.17%
  Classes: AgentOutputStream <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:add_output (lines 128-158) ---[0m
    pub fn add_output(&self, entry: OutputEntry) -> Result<(), String> {
        // Broadcast to live subscribers
        let _ = self.broadcast_tx.send(entry.clone());

        let mut buffer = self
            .buffer
            .lock()
            .map_err(|e| format!("Failed to lock buffer: {}", e))?;

        // Add new entry
        buffer.push_back(entry);

        // Enforce buffer size limit
        while buffer.len() > MAX_BUFFER_SIZE {
            buffer.pop_front();
        }

        // Periodic cleanup of old entries
        let mut last_cleanup = self
            .last_cleanup
            .lock()
            .map_err(|e| format!("Failed to lock cleanup time: {}", e))?;
        if last_cleanup.elapsed() > Duration::from_secs(300) {
            // Check every 5 minutes
            let cutoff_time = Utc::now() - chrono::Duration::from_std(MAX_OUTPUT_AGE).unwrap();
            buffer.retain(|entry| entry.timestamp > cutoff_time);
            *last_cleanup = Instant::now();
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:get_stats (lines 382-422) ---[0m
    pub fn get_stats(&self) -> MonitoringStats {
        let streams = match self.streams.read() {
            Ok(s) => s,
            Err(_) => {
                return MonitoringStats {
                    total_entries: 0,
                    entries_per_agent: std::collections::HashMap::new(),
                    entries_per_type: std::collections::HashMap::new(),
                    active_streams: 0,
                    total_subscribers: 0,
                }
            }
        };

        let mut total_entries = 0;
        let mut entries_per_agent = std::collections::HashMap::new();
        let mut entries_per_type: std::collections::HashMap<String, usize> =
            std::collections::HashMap::new();

        for (agent_id, stream) in streams.iter() {
            let count = stream.len();
            total_entries += count;
            entries_per_agent.insert(agent_id.clone(), count);

            // Count by type (sampling last 100 entries)
            for entry in stream.get_recent(100, None) {
                let type_name = format!("{:?}", entry.output_type);
                *entries_per_type.entry(type_name).or_insert(0) += 1;
            }
        }

        let total_subscribers = self.subscribers.read().map(|s| s.len()).unwrap_or(0);

        MonitoringStats {
            total_entries,
            entries_per_agent,
            entries_per_type,
            active_streams: streams.len(),
            total_subscribers,
        }
    }

  ./crates/ccswarm/src/monitoring/mod.rs:60-94 method matches_filter <-> ./crates/ccswarm/src/monitoring/mod.rs:128-158 method add_output
  Similarity: 81.30%
  Classes: OutputEntry <-> AgentOutputStream

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:matches_filter (lines 60-94) ---[0m
    pub fn matches_filter(&self, filter: &OutputFilter) -> bool {
        // Check agent filter
        if let Some(ref agent_ids) = filter.agent_ids {
            if !agent_ids.contains(&self.agent_id) {
                return false;
            }
        }

        // Check output type filter
        if let Some(ref output_types) = filter.output_types {
            if !output_types.contains(&self.output_type) {
                return false;
            }
        }

        // Check content filter
        if let Some(ref pattern) = filter.content_pattern {
            if !self
                .content
                .to_lowercase()
                .contains(&pattern.to_lowercase())
            {
                return false;
            }
        }

        // Check task filter
        if let Some(ref task_id) = filter.task_id {
            if self.task_id.as_ref() != Some(task_id) {
                return false;
            }
        }

        true
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:add_output (lines 128-158) ---[0m
    pub fn add_output(&self, entry: OutputEntry) -> Result<(), String> {
        // Broadcast to live subscribers
        let _ = self.broadcast_tx.send(entry.clone());

        let mut buffer = self
            .buffer
            .lock()
            .map_err(|e| format!("Failed to lock buffer: {}", e))?;

        // Add new entry
        buffer.push_back(entry);

        // Enforce buffer size limit
        while buffer.len() > MAX_BUFFER_SIZE {
            buffer.pop_front();
        }

        // Periodic cleanup of old entries
        let mut last_cleanup = self
            .last_cleanup
            .lock()
            .map_err(|e| format!("Failed to lock cleanup time: {}", e))?;
        if last_cleanup.elapsed() > Duration::from_secs(300) {
            // Check every 5 minutes
            let cutoff_time = Utc::now() - chrono::Duration::from_std(MAX_OUTPUT_AGE).unwrap();
            buffer.retain(|entry| entry.timestamp > cutoff_time);
            *last_cleanup = Instant::now();
        }

        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:273-313 method add_output <-> ./crates/ccswarm/src/monitoring/mod.rs:341-357 method get_all_recent
  Similarity: 90.26%
  Classes: MonitoringSystem <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:add_output (lines 273-313) ---[0m
    pub fn add_output(
        &self,
        agent_id: String,
        agent_type: String,
        output_type: OutputType,
        content: String,
        task_id: Option<String>,
        session_id: String,
    ) -> Result<(), String> {
        let entry = OutputEntry::new(
            agent_id.clone(),
            agent_type,
            output_type,
            content,
            task_id,
            session_id,
        );

        // Send to global broadcast
        let _ = self.global_broadcast.send(entry.clone());

        // Send to agent-specific stream
        if let Some(stream) = self.get_agent_stream(&agent_id) {
            stream.add_output(entry.clone())?;
        } else {
            // Auto-register agent if not exists
            let stream = self.register_agent(agent_id)?;
            stream.add_output(entry.clone())?;
        }

        // Notify subscribers
        if let Ok(subscribers) = self.subscribers.read() {
            for subscriber in subscribers.iter() {
                if subscriber.accepts(&entry) {
                    subscriber.on_output(&entry);
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:get_all_recent (lines 341-357) ---[0m
    pub fn get_all_recent(&self, count: usize, filter: Option<&OutputFilter>) -> Vec<OutputEntry> {
        let streams = match self.streams.read() {
            Ok(s) => s,
            Err(_) => return vec![],
        };

        let mut all_entries: Vec<OutputEntry> = streams
            .values()
            .flat_map(|stream| stream.get_recent(count, filter))
            .collect();

        // Sort by timestamp
        all_entries.sort_by(|a, b| a.timestamp.cmp(&b.timestamp));

        // Take the most recent entries
        all_entries.into_iter().rev().take(count).rev().collect()
    }

  ./crates/ccswarm/src/monitoring/mod.rs:60-94 method matches_filter <-> ./crates/ccswarm/src/monitoring/mod.rs:341-357 method get_all_recent
  Similarity: 81.95%
  Classes: OutputEntry <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:matches_filter (lines 60-94) ---[0m
    pub fn matches_filter(&self, filter: &OutputFilter) -> bool {
        // Check agent filter
        if let Some(ref agent_ids) = filter.agent_ids {
            if !agent_ids.contains(&self.agent_id) {
                return false;
            }
        }

        // Check output type filter
        if let Some(ref output_types) = filter.output_types {
            if !output_types.contains(&self.output_type) {
                return false;
            }
        }

        // Check content filter
        if let Some(ref pattern) = filter.content_pattern {
            if !self
                .content
                .to_lowercase()
                .contains(&pattern.to_lowercase())
            {
                return false;
            }
        }

        // Check task filter
        if let Some(ref task_id) = filter.task_id {
            if self.task_id.as_ref() != Some(task_id) {
                return false;
            }
        }

        true
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:get_all_recent (lines 341-357) ---[0m
    pub fn get_all_recent(&self, count: usize, filter: Option<&OutputFilter>) -> Vec<OutputEntry> {
        let streams = match self.streams.read() {
            Ok(s) => s,
            Err(_) => return vec![],
        };

        let mut all_entries: Vec<OutputEntry> = streams
            .values()
            .flat_map(|stream| stream.get_recent(count, filter))
            .collect();

        // Sort by timestamp
        all_entries.sort_by(|a, b| a.timestamp.cmp(&b.timestamp));

        // Take the most recent entries
        all_entries.into_iter().rev().take(count).rev().collect()
    }

  ./crates/ccswarm/src/monitoring/mod.rs:128-158 method add_output <-> ./crates/ccswarm/src/monitoring/mod.rs:341-357 method get_all_recent
  Similarity: 84.48%
  Classes: AgentOutputStream <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:add_output (lines 128-158) ---[0m
    pub fn add_output(&self, entry: OutputEntry) -> Result<(), String> {
        // Broadcast to live subscribers
        let _ = self.broadcast_tx.send(entry.clone());

        let mut buffer = self
            .buffer
            .lock()
            .map_err(|e| format!("Failed to lock buffer: {}", e))?;

        // Add new entry
        buffer.push_back(entry);

        // Enforce buffer size limit
        while buffer.len() > MAX_BUFFER_SIZE {
            buffer.pop_front();
        }

        // Periodic cleanup of old entries
        let mut last_cleanup = self
            .last_cleanup
            .lock()
            .map_err(|e| format!("Failed to lock cleanup time: {}", e))?;
        if last_cleanup.elapsed() > Duration::from_secs(300) {
            // Check every 5 minutes
            let cutoff_time = Utc::now() - chrono::Duration::from_std(MAX_OUTPUT_AGE).unwrap();
            buffer.retain(|entry| entry.timestamp > cutoff_time);
            *last_cleanup = Instant::now();
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:get_all_recent (lines 341-357) ---[0m
    pub fn get_all_recent(&self, count: usize, filter: Option<&OutputFilter>) -> Vec<OutputEntry> {
        let streams = match self.streams.read() {
            Ok(s) => s,
            Err(_) => return vec![],
        };

        let mut all_entries: Vec<OutputEntry> = streams
            .values()
            .flat_map(|stream| stream.get_recent(count, filter))
            .collect();

        // Sort by timestamp
        all_entries.sort_by(|a, b| a.timestamp.cmp(&b.timestamp));

        // Take the most recent entries
        all_entries.into_iter().rev().take(count).rev().collect()
    }

  ./crates/ccswarm/src/monitoring/mod.rs:161-177 method get_recent <-> ./crates/ccswarm/src/monitoring/mod.rs:452-470 method on_output
  Similarity: 94.61%
  Classes: AgentOutputStream <-> OutputSubscriber

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:get_recent (lines 161-177) ---[0m
    pub fn get_recent(&self, count: usize, filter: Option<&OutputFilter>) -> Vec<OutputEntry> {
        let buffer = match self.buffer.lock() {
            Ok(b) => b,
            Err(_) => return vec![],
        };

        buffer
            .iter()
            .rev()
            .filter(|entry| filter.is_none_or(|f| entry.matches_filter(f)))
            .take(count)
            .cloned()
            .collect::<Vec<_>>()
            .into_iter()
            .rev()
            .collect()
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:on_output (lines 452-470) ---[0m
    fn on_output(&self, entry: &OutputEntry) {
        let type_indicator = match entry.output_type {
            OutputType::Error => "❌",
            OutputType::Warning => "⚠️",
            OutputType::Info => "ℹ️",
            OutputType::Debug => "🔍",
            OutputType::System => "⚙️",
            _ => "📝",
        };

        println!(
            "[{}] {} {} [{}]: {}",
            entry.timestamp.format("%H:%M:%S"),
            type_indicator,
            entry.agent_type,
            entry.agent_id,
            entry.content
        );
    }

  ./crates/ccswarm/src/monitoring/mod.rs:117-125 method new <-> ./crates/ccswarm/src/monitoring/mod.rs:452-470 method on_output
  Similarity: 94.07%
  Classes: AgentOutputStream <-> OutputSubscriber

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:new (lines 117-125) ---[0m
    pub fn new(agent_id: String) -> Self {
        let (broadcast_tx, _) = broadcast::channel(1024);
        Self {
            buffer: Arc::new(Mutex::new(VecDeque::with_capacity(MAX_BUFFER_SIZE))),
            last_cleanup: Arc::new(Mutex::new(Instant::now())),
            agent_id,
            broadcast_tx,
        }
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:on_output (lines 452-470) ---[0m
    fn on_output(&self, entry: &OutputEntry) {
        let type_indicator = match entry.output_type {
            OutputType::Error => "❌",
            OutputType::Warning => "⚠️",
            OutputType::Info => "ℹ️",
            OutputType::Debug => "🔍",
            OutputType::System => "⚙️",
            _ => "📝",
        };

        println!(
            "[{}] {} {} [{}]: {}",
            entry.timestamp.format("%H:%M:%S"),
            type_indicator,
            entry.agent_type,
            entry.agent_id,
            entry.content
        );
    }

  ./crates/ccswarm/src/monitoring/mod.rs:235-242 method new <-> ./crates/ccswarm/src/monitoring/mod.rs:452-470 method on_output
  Similarity: 94.07%
  Classes: MonitoringSystem <-> OutputSubscriber

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:new (lines 235-242) ---[0m
    pub fn new() -> Self {
        let (global_broadcast, _) = broadcast::channel(2048);
        Self {
            streams: Arc::new(RwLock::new(std::collections::HashMap::new())),
            subscribers: Arc::new(RwLock::new(Vec::new())),
            global_broadcast,
        }
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:on_output (lines 452-470) ---[0m
    fn on_output(&self, entry: &OutputEntry) {
        let type_indicator = match entry.output_type {
            OutputType::Error => "❌",
            OutputType::Warning => "⚠️",
            OutputType::Info => "ℹ️",
            OutputType::Debug => "🔍",
            OutputType::System => "⚙️",
            _ => "📝",
        };

        println!(
            "[{}] {} {} [{}]: {}",
            entry.timestamp.format("%H:%M:%S"),
            type_indicator,
            entry.agent_type,
            entry.agent_id,
            entry.content
        );
    }

  ./crates/ccswarm/src/monitoring/mod.rs:245-255 method register_agent <-> ./crates/ccswarm/src/monitoring/mod.rs:341-357 method get_all_recent
  Similarity: 88.29%
  Classes: MonitoringSystem <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:register_agent (lines 245-255) ---[0m
    pub fn register_agent(&self, agent_id: String) -> Result<Arc<AgentOutputStream>, String> {
        let mut streams = self
            .streams
            .write()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;

        let stream = Arc::new(AgentOutputStream::new(agent_id.clone()));
        streams.insert(agent_id, stream.clone());

        Ok(stream)
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:get_all_recent (lines 341-357) ---[0m
    pub fn get_all_recent(&self, count: usize, filter: Option<&OutputFilter>) -> Vec<OutputEntry> {
        let streams = match self.streams.read() {
            Ok(s) => s,
            Err(_) => return vec![],
        };

        let mut all_entries: Vec<OutputEntry> = streams
            .values()
            .flat_map(|stream| stream.get_recent(count, filter))
            .collect();

        // Sort by timestamp
        all_entries.sort_by(|a, b| a.timestamp.cmp(&b.timestamp));

        // Take the most recent entries
        all_entries.into_iter().rev().take(count).rev().collect()
    }

  ./crates/ccswarm/src/monitoring/mod.rs:117-125 method new <-> ./crates/ccswarm/src/monitoring/mod.rs:161-177 method get_recent
  Similarity: 93.88%
  Classes: AgentOutputStream <-> AgentOutputStream

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:new (lines 117-125) ---[0m
    pub fn new(agent_id: String) -> Self {
        let (broadcast_tx, _) = broadcast::channel(1024);
        Self {
            buffer: Arc::new(Mutex::new(VecDeque::with_capacity(MAX_BUFFER_SIZE))),
            last_cleanup: Arc::new(Mutex::new(Instant::now())),
            agent_id,
            broadcast_tx,
        }
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:get_recent (lines 161-177) ---[0m
    pub fn get_recent(&self, count: usize, filter: Option<&OutputFilter>) -> Vec<OutputEntry> {
        let buffer = match self.buffer.lock() {
            Ok(b) => b,
            Err(_) => return vec![],
        };

        buffer
            .iter()
            .rev()
            .filter(|entry| filter.is_none_or(|f| entry.matches_filter(f)))
            .take(count)
            .cloned()
            .collect::<Vec<_>>()
            .into_iter()
            .rev()
            .collect()
    }

  ./crates/ccswarm/src/monitoring/mod.rs:161-177 method get_recent <-> ./crates/ccswarm/src/monitoring/mod.rs:235-242 method new
  Similarity: 93.88%
  Classes: AgentOutputStream <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:get_recent (lines 161-177) ---[0m
    pub fn get_recent(&self, count: usize, filter: Option<&OutputFilter>) -> Vec<OutputEntry> {
        let buffer = match self.buffer.lock() {
            Ok(b) => b,
            Err(_) => return vec![],
        };

        buffer
            .iter()
            .rev()
            .filter(|entry| filter.is_none_or(|f| entry.matches_filter(f)))
            .take(count)
            .cloned()
            .collect::<Vec<_>>()
            .into_iter()
            .rev()
            .collect()
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:new (lines 235-242) ---[0m
    pub fn new() -> Self {
        let (global_broadcast, _) = broadcast::channel(2048);
        Self {
            streams: Arc::new(RwLock::new(std::collections::HashMap::new())),
            subscribers: Arc::new(RwLock::new(Vec::new())),
            global_broadcast,
        }
    }

  ./crates/ccswarm/src/monitoring/mod.rs:258-265 method unregister_agent <-> ./crates/ccswarm/src/monitoring/mod.rs:452-470 method on_output
  Similarity: 86.26%
  Classes: MonitoringSystem <-> OutputSubscriber

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:unregister_agent (lines 258-265) ---[0m
    pub fn unregister_agent(&self, agent_id: &str) -> Result<(), String> {
        let mut streams = self
            .streams
            .write()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;
        streams.remove(agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:on_output (lines 452-470) ---[0m
    fn on_output(&self, entry: &OutputEntry) {
        let type_indicator = match entry.output_type {
            OutputType::Error => "❌",
            OutputType::Warning => "⚠️",
            OutputType::Info => "ℹ️",
            OutputType::Debug => "🔍",
            OutputType::System => "⚙️",
            _ => "📝",
        };

        println!(
            "[{}] {} {} [{}]: {}",
            entry.timestamp.format("%H:%M:%S"),
            type_indicator,
            entry.agent_type,
            entry.agent_id,
            entry.content
        );
    }

  ./crates/ccswarm/src/monitoring/mod.rs:321-328 method add_subscriber <-> ./crates/ccswarm/src/monitoring/mod.rs:452-470 method on_output
  Similarity: 86.26%
  Classes: MonitoringSystem <-> OutputSubscriber

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:add_subscriber (lines 321-328) ---[0m
    pub fn add_subscriber(&self, subscriber: Arc<dyn OutputSubscriber>) -> Result<(), String> {
        let mut subscribers = self
            .subscribers
            .write()
            .map_err(|e| format!("Failed to lock subscribers: {}", e))?;
        subscribers.push(subscriber);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:on_output (lines 452-470) ---[0m
    fn on_output(&self, entry: &OutputEntry) {
        let type_indicator = match entry.output_type {
            OutputType::Error => "❌",
            OutputType::Warning => "⚠️",
            OutputType::Info => "ℹ️",
            OutputType::Debug => "🔍",
            OutputType::System => "⚙️",
            _ => "📝",
        };

        println!(
            "[{}] {} {} [{}]: {}",
            entry.timestamp.format("%H:%M:%S"),
            type_indicator,
            entry.agent_type,
            entry.agent_id,
            entry.content
        );
    }

  ./crates/ccswarm/src/monitoring/mod.rs:161-177 method get_recent <-> ./crates/ccswarm/src/monitoring/mod.rs:370-379 method clear_all_output
  Similarity: 86.25%
  Classes: AgentOutputStream <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:get_recent (lines 161-177) ---[0m
    pub fn get_recent(&self, count: usize, filter: Option<&OutputFilter>) -> Vec<OutputEntry> {
        let buffer = match self.buffer.lock() {
            Ok(b) => b,
            Err(_) => return vec![],
        };

        buffer
            .iter()
            .rev()
            .filter(|entry| filter.is_none_or(|f| entry.matches_filter(f)))
            .take(count)
            .cloned()
            .collect::<Vec<_>>()
            .into_iter()
            .rev()
            .collect()
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:clear_all_output (lines 370-379) ---[0m
    pub fn clear_all_output(&self) -> Result<(), String> {
        let streams = self
            .streams
            .read()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;
        for stream in streams.values() {
            stream.clear();
        }
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:161-177 method get_recent <-> ./crates/ccswarm/src/monitoring/mod.rs:331-338 method remove_subscriber
  Similarity: 87.83%
  Classes: AgentOutputStream <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:get_recent (lines 161-177) ---[0m
    pub fn get_recent(&self, count: usize, filter: Option<&OutputFilter>) -> Vec<OutputEntry> {
        let buffer = match self.buffer.lock() {
            Ok(b) => b,
            Err(_) => return vec![],
        };

        buffer
            .iter()
            .rev()
            .filter(|entry| filter.is_none_or(|f| entry.matches_filter(f)))
            .take(count)
            .cloned()
            .collect::<Vec<_>>()
            .into_iter()
            .rev()
            .collect()
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:remove_subscriber (lines 331-338) ---[0m
    pub fn remove_subscriber(&self, subscriber_id: &str) -> Result<(), String> {
        let mut subscribers = self
            .subscribers
            .write()
            .map_err(|e| format!("Failed to lock subscribers: {}", e))?;
        subscribers.retain(|s| s.id() != subscriber_id);
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:370-379 method clear_all_output <-> ./crates/ccswarm/src/monitoring/mod.rs:452-470 method on_output
  Similarity: 75.69%
  Classes: MonitoringSystem <-> OutputSubscriber

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:clear_all_output (lines 370-379) ---[0m
    pub fn clear_all_output(&self) -> Result<(), String> {
        let streams = self
            .streams
            .read()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;
        for stream in streams.values() {
            stream.clear();
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:on_output (lines 452-470) ---[0m
    fn on_output(&self, entry: &OutputEntry) {
        let type_indicator = match entry.output_type {
            OutputType::Error => "❌",
            OutputType::Warning => "⚠️",
            OutputType::Info => "ℹ️",
            OutputType::Debug => "🔍",
            OutputType::System => "⚙️",
            _ => "📝",
        };

        println!(
            "[{}] {} {} [{}]: {}",
            entry.timestamp.format("%H:%M:%S"),
            type_indicator,
            entry.agent_type,
            entry.agent_id,
            entry.content
        );
    }

  ./crates/ccswarm/src/monitoring/mod.rs:245-255 method register_agent <-> ./crates/ccswarm/src/monitoring/mod.rs:452-470 method on_output
  Similarity: 71.63%
  Classes: MonitoringSystem <-> OutputSubscriber

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:register_agent (lines 245-255) ---[0m
    pub fn register_agent(&self, agent_id: String) -> Result<Arc<AgentOutputStream>, String> {
        let mut streams = self
            .streams
            .write()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;

        let stream = Arc::new(AgentOutputStream::new(agent_id.clone()));
        streams.insert(agent_id, stream.clone());

        Ok(stream)
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:on_output (lines 452-470) ---[0m
    fn on_output(&self, entry: &OutputEntry) {
        let type_indicator = match entry.output_type {
            OutputType::Error => "❌",
            OutputType::Warning => "⚠️",
            OutputType::Info => "ℹ️",
            OutputType::Debug => "🔍",
            OutputType::System => "⚙️",
            _ => "📝",
        };

        println!(
            "[{}] {} {} [{}]: {}",
            entry.timestamp.format("%H:%M:%S"),
            type_indicator,
            entry.agent_type,
            entry.agent_id,
            entry.content
        );
    }

  ./crates/ccswarm/src/monitoring/mod.rs:161-177 method get_recent <-> ./crates/ccswarm/src/monitoring/mod.rs:245-255 method register_agent
  Similarity: 76.18%
  Classes: AgentOutputStream <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:get_recent (lines 161-177) ---[0m
    pub fn get_recent(&self, count: usize, filter: Option<&OutputFilter>) -> Vec<OutputEntry> {
        let buffer = match self.buffer.lock() {
            Ok(b) => b,
            Err(_) => return vec![],
        };

        buffer
            .iter()
            .rev()
            .filter(|entry| filter.is_none_or(|f| entry.matches_filter(f)))
            .take(count)
            .cloned()
            .collect::<Vec<_>>()
            .into_iter()
            .rev()
            .collect()
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:register_agent (lines 245-255) ---[0m
    pub fn register_agent(&self, agent_id: String) -> Result<Arc<AgentOutputStream>, String> {
        let mut streams = self
            .streams
            .write()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;

        let stream = Arc::new(AgentOutputStream::new(agent_id.clone()));
        streams.insert(agent_id, stream.clone());

        Ok(stream)
    }

  ./crates/ccswarm/src/monitoring/mod.rs:331-338 method remove_subscriber <-> ./crates/ccswarm/src/monitoring/mod.rs:452-470 method on_output
  Similarity: 74.07%
  Classes: MonitoringSystem <-> OutputSubscriber

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:remove_subscriber (lines 331-338) ---[0m
    pub fn remove_subscriber(&self, subscriber_id: &str) -> Result<(), String> {
        let mut subscribers = self
            .subscribers
            .write()
            .map_err(|e| format!("Failed to lock subscribers: {}", e))?;
        subscribers.retain(|s| s.id() != subscriber_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:on_output (lines 452-470) ---[0m
    fn on_output(&self, entry: &OutputEntry) {
        let type_indicator = match entry.output_type {
            OutputType::Error => "❌",
            OutputType::Warning => "⚠️",
            OutputType::Info => "ℹ️",
            OutputType::Debug => "🔍",
            OutputType::System => "⚙️",
            _ => "📝",
        };

        println!(
            "[{}] {} {} [{}]: {}",
            entry.timestamp.format("%H:%M:%S"),
            type_indicator,
            entry.agent_type,
            entry.agent_id,
            entry.content
        );
    }

  ./crates/ccswarm/src/monitoring/mod.rs:40-57 method new <-> ./crates/ccswarm/src/monitoring/mod.rs:268-270 method get_agent_stream
  Similarity: 88.06%
  Classes: OutputEntry <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:new (lines 40-57) ---[0m
    pub fn new(
        agent_id: String,
        agent_type: String,
        output_type: OutputType,
        content: String,
        task_id: Option<String>,
        session_id: String,
    ) -> Self {
        Self {
            timestamp: Utc::now(),
            agent_id,
            agent_type,
            output_type,
            content,
            task_id,
            session_id,
        }
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:get_agent_stream (lines 268-270) ---[0m
    pub fn get_agent_stream(&self, agent_id: &str) -> Option<Arc<AgentOutputStream>> {
        self.streams.read().ok()?.get(agent_id).cloned()
    }

  ./crates/ccswarm/src/monitoring/mod.rs:40-57 method new <-> ./crates/ccswarm/src/monitoring/mod.rs:192-194 method len
  Similarity: 86.75%
  Classes: OutputEntry <-> AgentOutputStream

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:new (lines 40-57) ---[0m
    pub fn new(
        agent_id: String,
        agent_type: String,
        output_type: OutputType,
        content: String,
        task_id: Option<String>,
        session_id: String,
    ) -> Self {
        Self {
            timestamp: Utc::now(),
            agent_id,
            agent_type,
            output_type,
            content,
            task_id,
            session_id,
        }
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:len (lines 192-194) ---[0m
    pub fn len(&self) -> usize {
        self.buffer.lock().map(|b| b.len()).unwrap_or(0)
    }

  ./crates/ccswarm/src/monitoring/mod.rs:245-255 method register_agent <-> ./crates/ccswarm/src/monitoring/mod.rs:258-265 method unregister_agent
  Similarity: 86.26%
  Classes: MonitoringSystem <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:register_agent (lines 245-255) ---[0m
    pub fn register_agent(&self, agent_id: String) -> Result<Arc<AgentOutputStream>, String> {
        let mut streams = self
            .streams
            .write()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;

        let stream = Arc::new(AgentOutputStream::new(agent_id.clone()));
        streams.insert(agent_id, stream.clone());

        Ok(stream)
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:unregister_agent (lines 258-265) ---[0m
    pub fn unregister_agent(&self, agent_id: &str) -> Result<(), String> {
        let mut streams = self
            .streams
            .write()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;
        streams.remove(agent_id);
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:245-255 method register_agent <-> ./crates/ccswarm/src/monitoring/mod.rs:321-328 method add_subscriber
  Similarity: 85.35%
  Classes: MonitoringSystem <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:register_agent (lines 245-255) ---[0m
    pub fn register_agent(&self, agent_id: String) -> Result<Arc<AgentOutputStream>, String> {
        let mut streams = self
            .streams
            .write()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;

        let stream = Arc::new(AgentOutputStream::new(agent_id.clone()));
        streams.insert(agent_id, stream.clone());

        Ok(stream)
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:add_subscriber (lines 321-328) ---[0m
    pub fn add_subscriber(&self, subscriber: Arc<dyn OutputSubscriber>) -> Result<(), String> {
        let mut subscribers = self
            .subscribers
            .write()
            .map_err(|e| format!("Failed to lock subscribers: {}", e))?;
        subscribers.push(subscriber);
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:245-255 method register_agent <-> ./crates/ccswarm/src/monitoring/mod.rs:331-338 method remove_subscriber
  Similarity: 85.35%
  Classes: MonitoringSystem <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:register_agent (lines 245-255) ---[0m
    pub fn register_agent(&self, agent_id: String) -> Result<Arc<AgentOutputStream>, String> {
        let mut streams = self
            .streams
            .write()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;

        let stream = Arc::new(AgentOutputStream::new(agent_id.clone()));
        streams.insert(agent_id, stream.clone());

        Ok(stream)
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:remove_subscriber (lines 331-338) ---[0m
    pub fn remove_subscriber(&self, subscriber_id: &str) -> Result<(), String> {
        let mut subscribers = self
            .subscribers
            .write()
            .map_err(|e| format!("Failed to lock subscribers: {}", e))?;
        subscribers.retain(|s| s.id() != subscriber_id);
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:245-255 method register_agent <-> ./crates/ccswarm/src/monitoring/mod.rs:370-379 method clear_all_output
  Similarity: 74.95%
  Classes: MonitoringSystem <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:register_agent (lines 245-255) ---[0m
    pub fn register_agent(&self, agent_id: String) -> Result<Arc<AgentOutputStream>, String> {
        let mut streams = self
            .streams
            .write()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;

        let stream = Arc::new(AgentOutputStream::new(agent_id.clone()));
        streams.insert(agent_id, stream.clone());

        Ok(stream)
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:clear_all_output (lines 370-379) ---[0m
    pub fn clear_all_output(&self) -> Result<(), String> {
        let streams = self
            .streams
            .read()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;
        for stream in streams.values() {
            stream.clear();
        }
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:321-328 method add_subscriber <-> ./crates/ccswarm/src/monitoring/mod.rs:331-338 method remove_subscriber
  Similarity: 98.33%
  Classes: MonitoringSystem <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:add_subscriber (lines 321-328) ---[0m
    pub fn add_subscriber(&self, subscriber: Arc<dyn OutputSubscriber>) -> Result<(), String> {
        let mut subscribers = self
            .subscribers
            .write()
            .map_err(|e| format!("Failed to lock subscribers: {}", e))?;
        subscribers.push(subscriber);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:remove_subscriber (lines 331-338) ---[0m
    pub fn remove_subscriber(&self, subscriber_id: &str) -> Result<(), String> {
        let mut subscribers = self
            .subscribers
            .write()
            .map_err(|e| format!("Failed to lock subscribers: {}", e))?;
        subscribers.retain(|s| s.id() != subscriber_id);
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:258-265 method unregister_agent <-> ./crates/ccswarm/src/monitoring/mod.rs:321-328 method add_subscriber
  Similarity: 97.14%
  Classes: MonitoringSystem <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:unregister_agent (lines 258-265) ---[0m
    pub fn unregister_agent(&self, agent_id: &str) -> Result<(), String> {
        let mut streams = self
            .streams
            .write()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;
        streams.remove(agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:add_subscriber (lines 321-328) ---[0m
    pub fn add_subscriber(&self, subscriber: Arc<dyn OutputSubscriber>) -> Result<(), String> {
        let mut subscribers = self
            .subscribers
            .write()
            .map_err(|e| format!("Failed to lock subscribers: {}", e))?;
        subscribers.push(subscriber);
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:117-125 method new <-> ./crates/ccswarm/src/monitoring/mod.rs:235-242 method new
  Similarity: 91.33%
  Classes: AgentOutputStream <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:new (lines 117-125) ---[0m
    pub fn new(agent_id: String) -> Self {
        let (broadcast_tx, _) = broadcast::channel(1024);
        Self {
            buffer: Arc::new(Mutex::new(VecDeque::with_capacity(MAX_BUFFER_SIZE))),
            last_cleanup: Arc::new(Mutex::new(Instant::now())),
            agent_id,
            broadcast_tx,
        }
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:new (lines 235-242) ---[0m
    pub fn new() -> Self {
        let (global_broadcast, _) = broadcast::channel(2048);
        Self {
            streams: Arc::new(RwLock::new(std::collections::HashMap::new())),
            subscribers: Arc::new(RwLock::new(Vec::new())),
            global_broadcast,
        }
    }

  ./crates/ccswarm/src/monitoring/mod.rs:258-265 method unregister_agent <-> ./crates/ccswarm/src/monitoring/mod.rs:331-338 method remove_subscriber
  Similarity: 96.79%
  Classes: MonitoringSystem <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:unregister_agent (lines 258-265) ---[0m
    pub fn unregister_agent(&self, agent_id: &str) -> Result<(), String> {
        let mut streams = self
            .streams
            .write()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;
        streams.remove(agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:remove_subscriber (lines 331-338) ---[0m
    pub fn remove_subscriber(&self, subscriber_id: &str) -> Result<(), String> {
        let mut subscribers = self
            .subscribers
            .write()
            .map_err(|e| format!("Failed to lock subscribers: {}", e))?;
        subscribers.retain(|s| s.id() != subscriber_id);
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:258-265 method unregister_agent <-> ./crates/ccswarm/src/monitoring/mod.rs:370-379 method clear_all_output
  Similarity: 85.97%
  Classes: MonitoringSystem <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:unregister_agent (lines 258-265) ---[0m
    pub fn unregister_agent(&self, agent_id: &str) -> Result<(), String> {
        let mut streams = self
            .streams
            .write()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;
        streams.remove(agent_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:clear_all_output (lines 370-379) ---[0m
    pub fn clear_all_output(&self) -> Result<(), String> {
        let streams = self
            .streams
            .read()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;
        for stream in streams.values() {
            stream.clear();
        }
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:321-328 method add_subscriber <-> ./crates/ccswarm/src/monitoring/mod.rs:370-379 method clear_all_output
  Similarity: 84.81%
  Classes: MonitoringSystem <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:add_subscriber (lines 321-328) ---[0m
    pub fn add_subscriber(&self, subscriber: Arc<dyn OutputSubscriber>) -> Result<(), String> {
        let mut subscribers = self
            .subscribers
            .write()
            .map_err(|e| format!("Failed to lock subscribers: {}", e))?;
        subscribers.push(subscriber);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:clear_all_output (lines 370-379) ---[0m
    pub fn clear_all_output(&self) -> Result<(), String> {
        let streams = self
            .streams
            .read()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;
        for stream in streams.values() {
            stream.clear();
        }
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:331-338 method remove_subscriber <-> ./crates/ccswarm/src/monitoring/mod.rs:370-379 method clear_all_output
  Similarity: 81.92%
  Classes: MonitoringSystem <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:remove_subscriber (lines 331-338) ---[0m
    pub fn remove_subscriber(&self, subscriber_id: &str) -> Result<(), String> {
        let mut subscribers = self
            .subscribers
            .write()
            .map_err(|e| format!("Failed to lock subscribers: {}", e))?;
        subscribers.retain(|s| s.id() != subscriber_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:clear_all_output (lines 370-379) ---[0m
    pub fn clear_all_output(&self) -> Result<(), String> {
        let streams = self
            .streams
            .read()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;
        for stream in streams.values() {
            stream.clear();
        }
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:117-125 method new <-> ./crates/ccswarm/src/monitoring/mod.rs:258-265 method unregister_agent
  Similarity: 83.41%
  Classes: AgentOutputStream <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:new (lines 117-125) ---[0m
    pub fn new(agent_id: String) -> Self {
        let (broadcast_tx, _) = broadcast::channel(1024);
        Self {
            buffer: Arc::new(Mutex::new(VecDeque::with_capacity(MAX_BUFFER_SIZE))),
            last_cleanup: Arc::new(Mutex::new(Instant::now())),
            agent_id,
            broadcast_tx,
        }
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:unregister_agent (lines 258-265) ---[0m
    pub fn unregister_agent(&self, agent_id: &str) -> Result<(), String> {
        let mut streams = self
            .streams
            .write()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;
        streams.remove(agent_id);
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:117-125 method new <-> ./crates/ccswarm/src/monitoring/mod.rs:321-328 method add_subscriber
  Similarity: 83.41%
  Classes: AgentOutputStream <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:new (lines 117-125) ---[0m
    pub fn new(agent_id: String) -> Self {
        let (broadcast_tx, _) = broadcast::channel(1024);
        Self {
            buffer: Arc::new(Mutex::new(VecDeque::with_capacity(MAX_BUFFER_SIZE))),
            last_cleanup: Arc::new(Mutex::new(Instant::now())),
            agent_id,
            broadcast_tx,
        }
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:add_subscriber (lines 321-328) ---[0m
    pub fn add_subscriber(&self, subscriber: Arc<dyn OutputSubscriber>) -> Result<(), String> {
        let mut subscribers = self
            .subscribers
            .write()
            .map_err(|e| format!("Failed to lock subscribers: {}", e))?;
        subscribers.push(subscriber);
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:117-125 method new <-> ./crates/ccswarm/src/monitoring/mod.rs:331-338 method remove_subscriber
  Similarity: 83.41%
  Classes: AgentOutputStream <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:new (lines 117-125) ---[0m
    pub fn new(agent_id: String) -> Self {
        let (broadcast_tx, _) = broadcast::channel(1024);
        Self {
            buffer: Arc::new(Mutex::new(VecDeque::with_capacity(MAX_BUFFER_SIZE))),
            last_cleanup: Arc::new(Mutex::new(Instant::now())),
            agent_id,
            broadcast_tx,
        }
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:remove_subscriber (lines 331-338) ---[0m
    pub fn remove_subscriber(&self, subscriber_id: &str) -> Result<(), String> {
        let mut subscribers = self
            .subscribers
            .write()
            .map_err(|e| format!("Failed to lock subscribers: {}", e))?;
        subscribers.retain(|s| s.id() != subscriber_id);
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:235-242 method new <-> ./crates/ccswarm/src/monitoring/mod.rs:370-379 method clear_all_output
  Similarity: 74.89%
  Classes: MonitoringSystem <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:new (lines 235-242) ---[0m
    pub fn new() -> Self {
        let (global_broadcast, _) = broadcast::channel(2048);
        Self {
            streams: Arc::new(RwLock::new(std::collections::HashMap::new())),
            subscribers: Arc::new(RwLock::new(Vec::new())),
            global_broadcast,
        }
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:clear_all_output (lines 370-379) ---[0m
    pub fn clear_all_output(&self) -> Result<(), String> {
        let streams = self
            .streams
            .read()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;
        for stream in streams.values() {
            stream.clear();
        }
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:117-125 method new <-> ./crates/ccswarm/src/monitoring/mod.rs:370-379 method clear_all_output
  Similarity: 70.91%
  Classes: AgentOutputStream <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:new (lines 117-125) ---[0m
    pub fn new(agent_id: String) -> Self {
        let (broadcast_tx, _) = broadcast::channel(1024);
        Self {
            buffer: Arc::new(Mutex::new(VecDeque::with_capacity(MAX_BUFFER_SIZE))),
            last_cleanup: Arc::new(Mutex::new(Instant::now())),
            agent_id,
            broadcast_tx,
        }
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:clear_all_output (lines 370-379) ---[0m
    pub fn clear_all_output(&self) -> Result<(), String> {
        let streams = self
            .streams
            .read()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;
        for stream in streams.values() {
            stream.clear();
        }
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:235-242 method new <-> ./crates/ccswarm/src/monitoring/mod.rs:258-265 method unregister_agent
  Similarity: 83.78%
  Classes: MonitoringSystem <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:new (lines 235-242) ---[0m
    pub fn new() -> Self {
        let (global_broadcast, _) = broadcast::channel(2048);
        Self {
            streams: Arc::new(RwLock::new(std::collections::HashMap::new())),
            subscribers: Arc::new(RwLock::new(Vec::new())),
            global_broadcast,
        }
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:unregister_agent (lines 258-265) ---[0m
    pub fn unregister_agent(&self, agent_id: &str) -> Result<(), String> {
        let mut streams = self
            .streams
            .write()
            .map_err(|e| format!("Failed to lock streams: {}", e))?;
        streams.remove(agent_id);
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:235-242 method new <-> ./crates/ccswarm/src/monitoring/mod.rs:321-328 method add_subscriber
  Similarity: 83.78%
  Classes: MonitoringSystem <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:new (lines 235-242) ---[0m
    pub fn new() -> Self {
        let (global_broadcast, _) = broadcast::channel(2048);
        Self {
            streams: Arc::new(RwLock::new(std::collections::HashMap::new())),
            subscribers: Arc::new(RwLock::new(Vec::new())),
            global_broadcast,
        }
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:add_subscriber (lines 321-328) ---[0m
    pub fn add_subscriber(&self, subscriber: Arc<dyn OutputSubscriber>) -> Result<(), String> {
        let mut subscribers = self
            .subscribers
            .write()
            .map_err(|e| format!("Failed to lock subscribers: {}", e))?;
        subscribers.push(subscriber);
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:235-242 method new <-> ./crates/ccswarm/src/monitoring/mod.rs:331-338 method remove_subscriber
  Similarity: 83.78%
  Classes: MonitoringSystem <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:new (lines 235-242) ---[0m
    pub fn new() -> Self {
        let (global_broadcast, _) = broadcast::channel(2048);
        Self {
            streams: Arc::new(RwLock::new(std::collections::HashMap::new())),
            subscribers: Arc::new(RwLock::new(Vec::new())),
            global_broadcast,
        }
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:remove_subscriber (lines 331-338) ---[0m
    pub fn remove_subscriber(&self, subscriber_id: &str) -> Result<(), String> {
        let mut subscribers = self
            .subscribers
            .write()
            .map_err(|e| format!("Failed to lock subscribers: {}", e))?;
        subscribers.retain(|s| s.id() != subscriber_id);
        Ok(())
    }

  ./crates/ccswarm/src/monitoring/mod.rs:185-189 method clear <-> ./crates/ccswarm/src/monitoring/mod.rs:360-367 method clear_agent_output
  Similarity: 80.94%
  Classes: AgentOutputStream <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:clear (lines 185-189) ---[0m
    pub fn clear(&self) {
        if let Ok(mut buffer) = self.buffer.lock() {
            buffer.clear();
        }
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:clear_agent_output (lines 360-367) ---[0m
    pub fn clear_agent_output(&self, agent_id: &str) -> Result<(), String> {
        if let Some(stream) = self.get_agent_stream(agent_id) {
            stream.clear();
            Ok(())
        } else {
            Err(format!("Agent stream not found: {}", agent_id))
        }
    }

  ./crates/ccswarm/src/monitoring/mod.rs:268-270 method get_agent_stream <-> ./crates/ccswarm/src/monitoring/mod.rs:360-367 method clear_agent_output
  Similarity: 76.41%
  Classes: MonitoringSystem <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:get_agent_stream (lines 268-270) ---[0m
    pub fn get_agent_stream(&self, agent_id: &str) -> Option<Arc<AgentOutputStream>> {
        self.streams.read().ok()?.get(agent_id).cloned()
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:clear_agent_output (lines 360-367) ---[0m
    pub fn clear_agent_output(&self, agent_id: &str) -> Result<(), String> {
        if let Some(stream) = self.get_agent_stream(agent_id) {
            stream.clear();
            Ok(())
        } else {
            Err(format!("Agent stream not found: {}", agent_id))
        }
    }

  ./crates/ccswarm/src/monitoring/mod.rs:192-194 method len <-> ./crates/ccswarm/src/monitoring/mod.rs:360-367 method clear_agent_output
  Similarity: 71.25%
  Classes: AgentOutputStream <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:len (lines 192-194) ---[0m
    pub fn len(&self) -> usize {
        self.buffer.lock().map(|b| b.len()).unwrap_or(0)
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:clear_agent_output (lines 360-367) ---[0m
    pub fn clear_agent_output(&self, agent_id: &str) -> Result<(), String> {
        if let Some(stream) = self.get_agent_stream(agent_id) {
            stream.clear();
            Ok(())
        } else {
            Err(format!("Agent stream not found: {}", agent_id))
        }
    }

  ./crates/ccswarm/src/monitoring/mod.rs:185-189 method clear <-> ./crates/ccswarm/src/monitoring/mod.rs:268-270 method get_agent_stream
  Similarity: 82.63%
  Classes: AgentOutputStream <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:clear (lines 185-189) ---[0m
    pub fn clear(&self) {
        if let Ok(mut buffer) = self.buffer.lock() {
            buffer.clear();
        }
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:get_agent_stream (lines 268-270) ---[0m
    pub fn get_agent_stream(&self, agent_id: &str) -> Option<Arc<AgentOutputStream>> {
        self.streams.read().ok()?.get(agent_id).cloned()
    }

  ./crates/ccswarm/src/monitoring/mod.rs:185-189 method clear <-> ./crates/ccswarm/src/monitoring/mod.rs:192-194 method len
  Similarity: 81.00%
  Classes: AgentOutputStream <-> AgentOutputStream

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:clear (lines 185-189) ---[0m
    pub fn clear(&self) {
        if let Ok(mut buffer) = self.buffer.lock() {
            buffer.clear();
        }
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:len (lines 192-194) ---[0m
    pub fn len(&self) -> usize {
        self.buffer.lock().map(|b| b.len()).unwrap_or(0)
    }

  ./crates/ccswarm/src/monitoring/mod.rs:268-270 method get_agent_stream <-> ./crates/ccswarm/src/monitoring/mod.rs:476-478 method accepts
  Similarity: 82.78%
  Classes: MonitoringSystem <-> OutputSubscriber

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:get_agent_stream (lines 268-270) ---[0m
    pub fn get_agent_stream(&self, agent_id: &str) -> Option<Arc<AgentOutputStream>> {
        self.streams.read().ok()?.get(agent_id).cloned()
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:accepts (lines 476-478) ---[0m
    fn accepts(&self, entry: &OutputEntry) -> bool {
        self.filter.as_ref().is_none_or(|f| entry.matches_filter(f))
    }

  ./crates/ccswarm/src/monitoring/mod.rs:192-194 method len <-> ./crates/ccswarm/src/monitoring/mod.rs:476-478 method accepts
  Similarity: 82.75%
  Classes: AgentOutputStream <-> OutputSubscriber

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:len (lines 192-194) ---[0m
    pub fn len(&self) -> usize {
        self.buffer.lock().map(|b| b.len()).unwrap_or(0)
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:accepts (lines 476-478) ---[0m
    fn accepts(&self, entry: &OutputEntry) -> bool {
        self.filter.as_ref().is_none_or(|f| entry.matches_filter(f))
    }

  ./crates/ccswarm/src/monitoring/mod.rs:192-194 method len <-> ./crates/ccswarm/src/monitoring/mod.rs:268-270 method get_agent_stream
  Similarity: 81.25%
  Classes: AgentOutputStream <-> MonitoringSystem

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:len (lines 192-194) ---[0m
    pub fn len(&self) -> usize {
        self.buffer.lock().map(|b| b.len()).unwrap_or(0)
    }

[36m--- ./crates/ccswarm/src/monitoring/mod.rs:get_agent_stream (lines 268-270) ---[0m
    pub fn get_agent_stream(&self, agent_id: &str) -> Option<Arc<AgentOutputStream>> {
        self.streams.read().ok()?.get(agent_id).cloned()
    }


Duplicates in ./crates/ccswarm/src/utils/telemetry.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/utils/telemetry.rs:41-54 method record_counter <-> ./crates/ccswarm/src/utils/telemetry.rs:56-69 method record_gauge
  Similarity: 99.71%
  Classes: Telemetry <-> Telemetry

[36m--- ./crates/ccswarm/src/utils/telemetry.rs:record_counter (lines 41-54) ---[0m
    pub async fn record_counter(&self, name: impl Into<String>, value: i64) {
        let mut metrics = self.metrics.write().await;
        let name = name.into();

        metrics.insert(
            name.clone(),
            Metric {
                name,
                value: MetricValue::Counter(value),
                tags: HashMap::new(),
                timestamp: chrono::Utc::now(),
            },
        );
    }

[36m--- ./crates/ccswarm/src/utils/telemetry.rs:record_gauge (lines 56-69) ---[0m
    pub async fn record_gauge(&self, name: impl Into<String>, value: f64) {
        let mut metrics = self.metrics.write().await;
        let name = name.into();

        metrics.insert(
            name.clone(),
            Metric {
                name,
                value: MetricValue::Gauge(value),
                tags: HashMap::new(),
                timestamp: chrono::Utc::now(),
            },
        );
    }


Duplicates in ./crates/ccswarm/src/git/mod.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/git/mod.rs:15-27 method get_current_branch <-> ./crates/ccswarm/src/git/mod.rs:30-42 method get_head_commit
  Similarity: 99.34%
  Classes: GitUtils <-> GitUtils

[36m--- ./crates/ccswarm/src/git/mod.rs:get_current_branch (lines 15-27) ---[0m
    pub async fn get_current_branch(repo_path: &std::path::Path) -> anyhow::Result<String> {
        let output = tokio::process::Command::new("git")
            .args(["branch", "--show-current"])
            .current_dir(repo_path)
            .output()
            .await?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).trim().to_string())
        } else {
            Err(anyhow::anyhow!("Failed to get current branch"))
        }
    }

[36m--- ./crates/ccswarm/src/git/mod.rs:get_head_commit (lines 30-42) ---[0m
    pub async fn get_head_commit(repo_path: &std::path::Path) -> anyhow::Result<String> {
        let output = tokio::process::Command::new("git")
            .args(["rev-parse", "HEAD"])
            .current_dir(repo_path)
            .output()
            .await?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).trim().to_string())
        } else {
            Err(anyhow::anyhow!("Failed to get HEAD commit"))
        }
    }


Duplicates in ./crates/ccswarm/src/security.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/security.rs:38-44 method new <-> ./crates/ccswarm/src/security.rs:60-67 method log_audit
  Similarity: 96.23%
  Classes: SecurityContext <-> SecurityContext

[36m--- ./crates/ccswarm/src/security.rs:new (lines 38-44) ---[0m
    pub fn new(isolation_level: IsolationLevel) -> Self {
        Self {
            permissions: HashSet::new(),
            isolation_level,
            audit_log: Vec::new(),
        }
    }

[36m--- ./crates/ccswarm/src/security.rs:log_audit (lines 60-67) ---[0m
    fn log_audit(&mut self, action: String, result: bool) {
        self.audit_log.push(AuditEntry {
            timestamp: chrono::Utc::now(),
            action: action.clone(),
            result,
            details: String::new(),
        });
    }

  ./crates/ccswarm/src/security.rs:46-49 method grant_permission <-> ./crates/ccswarm/src/security.rs:51-54 method revoke_permission
  Similarity: 96.19%
  Classes: SecurityContext <-> SecurityContext

[36m--- ./crates/ccswarm/src/security.rs:grant_permission (lines 46-49) ---[0m
    pub fn grant_permission(&mut self, permission: Permission) {
        self.permissions.insert(permission);
        self.log_audit(format!("Granted permission: {:?}", permission), true);
    }

[36m--- ./crates/ccswarm/src/security.rs:revoke_permission (lines 51-54) ---[0m
    pub fn revoke_permission(&mut self, permission: Permission) {
        self.permissions.remove(&permission);
        self.log_audit(format!("Revoked permission: {:?}", permission), true);
    }


Duplicates in ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:169-214 function test_malformed_quality_issues <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:321-365 function test_rapid_agent_status_changes
  Similarity: 90.98%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_malformed_quality_issues (lines 169-214) ---[0m
    async fn test_malformed_quality_issues() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Start the orchestrator
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        // Send quality issue with empty issues list
        let empty_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "empty-issues-task".to_string(),
            issues: vec![],
        };

        // Should handle gracefully
        master
            .coordination_bus
            .send_message(empty_issues)
            .await
            .unwrap();

        // Send quality issue with very long issue descriptions
        let long_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "long-issues-task".to_string(),
            issues: vec!["X".repeat(10000)], // 10k character issue
        };

        master
            .coordination_bus
            .send_message(long_issues)
            .await
            .unwrap();

        // Allow processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Should not crash
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_rapid_agent_status_changes (lines 321-365) ---[0m
    async fn test_rapid_agent_status_changes() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/rapid",
            ClaudeConfig::for_agent("rapid-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("rapid-agent".to_string(), agent);

        // Rapidly change status
        let statuses = vec![
            AgentStatus::Available,
            AgentStatus::Working,
            AgentStatus::Error("Test error".to_string()),
            AgentStatus::Available,
            AgentStatus::Working,
        ];

        for status in statuses {
            let message = AgentMessage::StatusUpdate {
                agent_id: "rapid-agent".to_string(),
                status: status.clone(),
                metrics: serde_json::Value::Object(serde_json::Map::new()),
            };
            master.coordination_bus.send_message(message).await.unwrap();
        }

        // Should handle all changes without crashing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // The messages were sent successfully, which is what we're testing
        // The actual status update would require the orchestrator to be running
        assert!(master.agents.contains_key("rapid-agent"));
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:258-295 function test_agent_disconnection_during_task <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:321-365 function test_rapid_agent_status_changes
  Similarity: 94.75%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_agent_disconnection_during_task (lines 258-295) ---[0m
    async fn test_agent_disconnection_during_task() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/disconnect",
            ClaudeConfig::for_agent("disconnect-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("disconnect-agent".to_string(), agent);

        // Simulate agent disconnection (remove from pool)
        master.agents.remove("disconnect-agent");

        // Try to send message for this agent
        let message = AgentMessage::TaskCompleted {
            agent_id: "disconnect-agent".to_string(),
            task_id: "orphan-task".to_string(),
            result: TaskResult {
                success: false,
                output: serde_json::json!({"error": "disconnected"}),
                error: Some("Agent disconnected".to_string()),
                duration: std::time::Duration::from_secs(0),
            },
        };

        // Should handle gracefully
        master.coordination_bus.send_message(message).await.unwrap();
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_rapid_agent_status_changes (lines 321-365) ---[0m
    async fn test_rapid_agent_status_changes() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/rapid",
            ClaudeConfig::for_agent("rapid-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("rapid-agent".to_string(), agent);

        // Rapidly change status
        let statuses = vec![
            AgentStatus::Available,
            AgentStatus::Working,
            AgentStatus::Error("Test error".to_string()),
            AgentStatus::Available,
            AgentStatus::Working,
        ];

        for status in statuses {
            let message = AgentMessage::StatusUpdate {
                agent_id: "rapid-agent".to_string(),
                status: status.clone(),
                metrics: serde_json::Value::Object(serde_json::Map::new()),
            };
            master.coordination_bus.send_message(message).await.unwrap();
        }

        // Should handle all changes without crashing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // The messages were sent successfully, which is what we're testing
        // The actual status update would require the orchestrator to be running
        assert!(master.agents.contains_key("rapid-agent"));
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:169-214 function test_malformed_quality_issues <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:217-255 function test_cyclic_task_dependencies
  Similarity: 89.42%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_malformed_quality_issues (lines 169-214) ---[0m
    async fn test_malformed_quality_issues() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Start the orchestrator
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        // Send quality issue with empty issues list
        let empty_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "empty-issues-task".to_string(),
            issues: vec![],
        };

        // Should handle gracefully
        master
            .coordination_bus
            .send_message(empty_issues)
            .await
            .unwrap();

        // Send quality issue with very long issue descriptions
        let long_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "long-issues-task".to_string(),
            issues: vec!["X".repeat(10000)], // 10k character issue
        };

        master
            .coordination_bus
            .send_message(long_issues)
            .await
            .unwrap();

        // Allow processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Should not crash
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_cyclic_task_dependencies (lines 217-255) ---[0m
    async fn test_cyclic_task_dependencies() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with cyclic dependencies
        let task1 = Task::new(
            "cycle-1".to_string(),
            "Task 1".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-3".to_string());

        let task2 = Task::new(
            "cycle-2".to_string(),
            "Task 2".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-1".to_string());

        let task3 = Task::new(
            "cycle-3".to_string(),
            "Task 3".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-2".to_string());

        // Should handle without stack overflow
        master.add_task(task1).await.unwrap();
        master.add_task(task2).await.unwrap();
        master.add_task(task3).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:217-255 function test_cyclic_task_dependencies <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:321-365 function test_rapid_agent_status_changes
  Similarity: 89.17%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_cyclic_task_dependencies (lines 217-255) ---[0m
    async fn test_cyclic_task_dependencies() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with cyclic dependencies
        let task1 = Task::new(
            "cycle-1".to_string(),
            "Task 1".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-3".to_string());

        let task2 = Task::new(
            "cycle-2".to_string(),
            "Task 2".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-1".to_string());

        let task3 = Task::new(
            "cycle-3".to_string(),
            "Task 3".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-2".to_string());

        // Should handle without stack overflow
        master.add_task(task1).await.unwrap();
        master.add_task(task2).await.unwrap();
        master.add_task(task3).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_rapid_agent_status_changes (lines 321-365) ---[0m
    async fn test_rapid_agent_status_changes() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/rapid",
            ClaudeConfig::for_agent("rapid-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("rapid-agent".to_string(), agent);

        // Rapidly change status
        let statuses = vec![
            AgentStatus::Available,
            AgentStatus::Working,
            AgentStatus::Error("Test error".to_string()),
            AgentStatus::Available,
            AgentStatus::Working,
        ];

        for status in statuses {
            let message = AgentMessage::StatusUpdate {
                agent_id: "rapid-agent".to_string(),
                status: status.clone(),
                metrics: serde_json::Value::Object(serde_json::Map::new()),
            };
            master.coordination_bus.send_message(message).await.unwrap();
        }

        // Should handle all changes without crashing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // The messages were sent successfully, which is what we're testing
        // The actual status update would require the orchestrator to be running
        assert!(master.agents.contains_key("rapid-agent"));
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:169-214 function test_malformed_quality_issues <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:258-295 function test_agent_disconnection_during_task
  Similarity: 89.09%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_malformed_quality_issues (lines 169-214) ---[0m
    async fn test_malformed_quality_issues() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Start the orchestrator
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        // Send quality issue with empty issues list
        let empty_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "empty-issues-task".to_string(),
            issues: vec![],
        };

        // Should handle gracefully
        master
            .coordination_bus
            .send_message(empty_issues)
            .await
            .unwrap();

        // Send quality issue with very long issue descriptions
        let long_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "long-issues-task".to_string(),
            issues: vec!["X".repeat(10000)], // 10k character issue
        };

        master
            .coordination_bus
            .send_message(long_issues)
            .await
            .unwrap();

        // Allow processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Should not crash
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_agent_disconnection_during_task (lines 258-295) ---[0m
    async fn test_agent_disconnection_during_task() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/disconnect",
            ClaudeConfig::for_agent("disconnect-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("disconnect-agent".to_string(), agent);

        // Simulate agent disconnection (remove from pool)
        master.agents.remove("disconnect-agent");

        // Try to send message for this agent
        let message = AgentMessage::TaskCompleted {
            agent_id: "disconnect-agent".to_string(),
            task_id: "orphan-task".to_string(),
            result: TaskResult {
                success: false,
                output: serde_json::json!({"error": "disconnected"}),
                error: Some("Agent disconnected".to_string()),
                duration: std::time::Duration::from_secs(0),
            },
        };

        // Should handle gracefully
        master.coordination_bus.send_message(message).await.unwrap();
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:169-214 function test_malformed_quality_issues <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:451-487 function test_message_queue_overflow
  Similarity: 89.01%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_malformed_quality_issues (lines 169-214) ---[0m
    async fn test_malformed_quality_issues() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Start the orchestrator
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        // Send quality issue with empty issues list
        let empty_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "empty-issues-task".to_string(),
            issues: vec![],
        };

        // Should handle gracefully
        master
            .coordination_bus
            .send_message(empty_issues)
            .await
            .unwrap();

        // Send quality issue with very long issue descriptions
        let long_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "long-issues-task".to_string(),
            issues: vec!["X".repeat(10000)], // 10k character issue
        };

        master
            .coordination_bus
            .send_message(long_issues)
            .await
            .unwrap();

        // Allow processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Should not crash
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_message_queue_overflow (lines 451-487) ---[0m
    async fn test_message_queue_overflow() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to overflow task queue (bounded at 1000)
        let mut handles = vec![];
        for i in 0..1100 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("overflow-{}", i),
                    "Overflow task".to_string(),
                    Priority::Low,
                    TaskType::Development,
                );
                master_clone.add_task(task).await
            });
            handles.push(handle);
        }

        // Some should fail due to channel being full
        let mut success_count = 0;
        let mut fail_count = 0;

        for handle in handles {
            match handle.await.unwrap() {
                Ok(_) => success_count += 1,
                Err(_) => fail_count += 1,
            }
        }

        // Should have some failures
        assert!(fail_count > 0);
        assert!(success_count <= 1000);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:321-365 function test_rapid_agent_status_changes <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:414-447 function test_invalid_agent_specialization
  Similarity: 92.83%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_rapid_agent_status_changes (lines 321-365) ---[0m
    async fn test_rapid_agent_status_changes() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/rapid",
            ClaudeConfig::for_agent("rapid-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("rapid-agent".to_string(), agent);

        // Rapidly change status
        let statuses = vec![
            AgentStatus::Available,
            AgentStatus::Working,
            AgentStatus::Error("Test error".to_string()),
            AgentStatus::Available,
            AgentStatus::Working,
        ];

        for status in statuses {
            let message = AgentMessage::StatusUpdate {
                agent_id: "rapid-agent".to_string(),
                status: status.clone(),
                metrics: serde_json::Value::Object(serde_json::Map::new()),
            };
            master.coordination_bus.send_message(message).await.unwrap();
        }

        // Should handle all changes without crashing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // The messages were sent successfully, which is what we're testing
        // The actual status update would require the orchestrator to be running
        assert!(master.agents.contains_key("rapid-agent"));
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_agent_specialization (lines 414-447) ---[0m
    async fn test_invalid_agent_specialization() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent with frontend role
        let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/unknown",
            ClaudeConfig::for_agent("unknown-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Agent has frontend role, but we'll test task assignment for a role that doesn't match
        agent.status = AgentStatus::Available;

        master.agents.insert("unknown-agent".to_string(), agent);

        // Task assignment should still work (fall back to any available)
        let task = Task::new(
            "unknown-task".to_string(),
            "Task for unknown specialization".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_ok());
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:321-365 function test_rapid_agent_status_changes <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:451-487 function test_message_queue_overflow
  Similarity: 89.24%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_rapid_agent_status_changes (lines 321-365) ---[0m
    async fn test_rapid_agent_status_changes() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/rapid",
            ClaudeConfig::for_agent("rapid-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("rapid-agent".to_string(), agent);

        // Rapidly change status
        let statuses = vec![
            AgentStatus::Available,
            AgentStatus::Working,
            AgentStatus::Error("Test error".to_string()),
            AgentStatus::Available,
            AgentStatus::Working,
        ];

        for status in statuses {
            let message = AgentMessage::StatusUpdate {
                agent_id: "rapid-agent".to_string(),
                status: status.clone(),
                metrics: serde_json::Value::Object(serde_json::Map::new()),
            };
            master.coordination_bus.send_message(message).await.unwrap();
        }

        // Should handle all changes without crashing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // The messages were sent successfully, which is what we're testing
        // The actual status update would require the orchestrator to be running
        assert!(master.agents.contains_key("rapid-agent"));
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_message_queue_overflow (lines 451-487) ---[0m
    async fn test_message_queue_overflow() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to overflow task queue (bounded at 1000)
        let mut handles = vec![];
        for i in 0..1100 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("overflow-{}", i),
                    "Overflow task".to_string(),
                    Priority::Low,
                    TaskType::Development,
                );
                master_clone.add_task(task).await
            });
            handles.push(handle);
        }

        // Some should fail due to channel being full
        let mut success_count = 0;
        let mut fail_count = 0;

        for handle in handles {
            match handle.await.unwrap() {
                Ok(_) => success_count += 1,
                Err(_) => fail_count += 1,
            }
        }

        // Should have some failures
        assert!(fail_count > 0);
        assert!(success_count <= 1000);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:103-138 function test_invalid_task_metadata <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:321-365 function test_rapid_agent_status_changes
  Similarity: 88.38%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_task_metadata (lines 103-138) ---[0m
    async fn test_invalid_task_metadata() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with invalid metadata
        let mut task = Task::new(
            "invalid-meta".to_string(),
            "Task with bad metadata".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        task.metadata = Some(
            serde_json::json!({
                "invalid": null,
                "nested": {
                    "deeply": {
                        "nested": {
                            "value": "too deep"
                        }
                    }
                }
            })
            .as_object()
            .unwrap()
            .clone(),
        );

        // Should still handle the task
        master.add_task(task.clone()).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_rapid_agent_status_changes (lines 321-365) ---[0m
    async fn test_rapid_agent_status_changes() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/rapid",
            ClaudeConfig::for_agent("rapid-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("rapid-agent".to_string(), agent);

        // Rapidly change status
        let statuses = vec![
            AgentStatus::Available,
            AgentStatus::Working,
            AgentStatus::Error("Test error".to_string()),
            AgentStatus::Available,
            AgentStatus::Working,
        ];

        for status in statuses {
            let message = AgentMessage::StatusUpdate {
                agent_id: "rapid-agent".to_string(),
                status: status.clone(),
                metrics: serde_json::Value::Object(serde_json::Map::new()),
            };
            master.coordination_bus.send_message(message).await.unwrap();
        }

        // Should handle all changes without crashing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // The messages were sent successfully, which is what we're testing
        // The actual status update would require the orchestrator to be running
        assert!(master.agents.contains_key("rapid-agent"));
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:169-214 function test_malformed_quality_issues <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:414-447 function test_invalid_agent_specialization
  Similarity: 87.84%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_malformed_quality_issues (lines 169-214) ---[0m
    async fn test_malformed_quality_issues() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Start the orchestrator
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        // Send quality issue with empty issues list
        let empty_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "empty-issues-task".to_string(),
            issues: vec![],
        };

        // Should handle gracefully
        master
            .coordination_bus
            .send_message(empty_issues)
            .await
            .unwrap();

        // Send quality issue with very long issue descriptions
        let long_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "long-issues-task".to_string(),
            issues: vec!["X".repeat(10000)], // 10k character issue
        };

        master
            .coordination_bus
            .send_message(long_issues)
            .await
            .unwrap();

        // Allow processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Should not crash
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_agent_specialization (lines 414-447) ---[0m
    async fn test_invalid_agent_specialization() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent with frontend role
        let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/unknown",
            ClaudeConfig::for_agent("unknown-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Agent has frontend role, but we'll test task assignment for a role that doesn't match
        agent.status = AgentStatus::Available;

        master.agents.insert("unknown-agent".to_string(), agent);

        // Task assignment should still work (fall back to any available)
        let task = Task::new(
            "unknown-task".to_string(),
            "Task for unknown specialization".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_ok());
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:217-255 function test_cyclic_task_dependencies <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:258-295 function test_agent_disconnection_during_task
  Similarity: 90.63%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_cyclic_task_dependencies (lines 217-255) ---[0m
    async fn test_cyclic_task_dependencies() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with cyclic dependencies
        let task1 = Task::new(
            "cycle-1".to_string(),
            "Task 1".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-3".to_string());

        let task2 = Task::new(
            "cycle-2".to_string(),
            "Task 2".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-1".to_string());

        let task3 = Task::new(
            "cycle-3".to_string(),
            "Task 3".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-2".to_string());

        // Should handle without stack overflow
        master.add_task(task1).await.unwrap();
        master.add_task(task2).await.unwrap();
        master.add_task(task3).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_agent_disconnection_during_task (lines 258-295) ---[0m
    async fn test_agent_disconnection_during_task() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/disconnect",
            ClaudeConfig::for_agent("disconnect-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("disconnect-agent".to_string(), agent);

        // Simulate agent disconnection (remove from pool)
        master.agents.remove("disconnect-agent");

        // Try to send message for this agent
        let message = AgentMessage::TaskCompleted {
            agent_id: "disconnect-agent".to_string(),
            task_id: "orphan-task".to_string(),
            result: TaskResult {
                success: false,
                output: serde_json::json!({"error": "disconnected"}),
                error: Some("Agent disconnected".to_string()),
                duration: std::time::Duration::from_secs(0),
            },
        };

        // Should handle gracefully
        master.coordination_bus.send_message(message).await.unwrap();
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:103-138 function test_invalid_task_metadata <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:169-214 function test_malformed_quality_issues
  Similarity: 85.09%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_task_metadata (lines 103-138) ---[0m
    async fn test_invalid_task_metadata() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with invalid metadata
        let mut task = Task::new(
            "invalid-meta".to_string(),
            "Task with bad metadata".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        task.metadata = Some(
            serde_json::json!({
                "invalid": null,
                "nested": {
                    "deeply": {
                        "nested": {
                            "value": "too deep"
                        }
                    }
                }
            })
            .as_object()
            .unwrap()
            .clone(),
        );

        // Should still handle the task
        master.add_task(task.clone()).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_malformed_quality_issues (lines 169-214) ---[0m
    async fn test_malformed_quality_issues() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Start the orchestrator
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        // Send quality issue with empty issues list
        let empty_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "empty-issues-task".to_string(),
            issues: vec![],
        };

        // Should handle gracefully
        master
            .coordination_bus
            .send_message(empty_issues)
            .await
            .unwrap();

        // Send quality issue with very long issue descriptions
        let long_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "long-issues-task".to_string(),
            issues: vec!["X".repeat(10000)], // 10k character issue
        };

        master
            .coordination_bus
            .send_message(long_issues)
            .await
            .unwrap();

        // Allow processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Should not crash
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:258-295 function test_agent_disconnection_during_task <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:414-447 function test_invalid_agent_specialization
  Similarity: 94.01%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_agent_disconnection_during_task (lines 258-295) ---[0m
    async fn test_agent_disconnection_during_task() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/disconnect",
            ClaudeConfig::for_agent("disconnect-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("disconnect-agent".to_string(), agent);

        // Simulate agent disconnection (remove from pool)
        master.agents.remove("disconnect-agent");

        // Try to send message for this agent
        let message = AgentMessage::TaskCompleted {
            agent_id: "disconnect-agent".to_string(),
            task_id: "orphan-task".to_string(),
            result: TaskResult {
                success: false,
                output: serde_json::json!({"error": "disconnected"}),
                error: Some("Agent disconnected".to_string()),
                duration: std::time::Duration::from_secs(0),
            },
        };

        // Should handle gracefully
        master.coordination_bus.send_message(message).await.unwrap();
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_agent_specialization (lines 414-447) ---[0m
    async fn test_invalid_agent_specialization() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent with frontend role
        let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/unknown",
            ClaudeConfig::for_agent("unknown-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Agent has frontend role, but we'll test task assignment for a role that doesn't match
        agent.status = AgentStatus::Available;

        master.agents.insert("unknown-agent".to_string(), agent);

        // Task assignment should still work (fall back to any available)
        let task = Task::new(
            "unknown-task".to_string(),
            "Task for unknown specialization".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_ok());
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:258-295 function test_agent_disconnection_during_task <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:451-487 function test_message_queue_overflow
  Similarity: 89.89%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_agent_disconnection_during_task (lines 258-295) ---[0m
    async fn test_agent_disconnection_during_task() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/disconnect",
            ClaudeConfig::for_agent("disconnect-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("disconnect-agent".to_string(), agent);

        // Simulate agent disconnection (remove from pool)
        master.agents.remove("disconnect-agent");

        // Try to send message for this agent
        let message = AgentMessage::TaskCompleted {
            agent_id: "disconnect-agent".to_string(),
            task_id: "orphan-task".to_string(),
            result: TaskResult {
                success: false,
                output: serde_json::json!({"error": "disconnected"}),
                error: Some("Agent disconnected".to_string()),
                duration: std::time::Duration::from_secs(0),
            },
        };

        // Should handle gracefully
        master.coordination_bus.send_message(message).await.unwrap();
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_message_queue_overflow (lines 451-487) ---[0m
    async fn test_message_queue_overflow() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to overflow task queue (bounded at 1000)
        let mut handles = vec![];
        for i in 0..1100 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("overflow-{}", i),
                    "Overflow task".to_string(),
                    Priority::Low,
                    TaskType::Development,
                );
                master_clone.add_task(task).await
            });
            handles.push(handle);
        }

        // Some should fail due to channel being full
        let mut success_count = 0;
        let mut fail_count = 0;

        for handle in handles {
            match handle.await.unwrap() {
                Ok(_) => success_count += 1,
                Err(_) => fail_count += 1,
            }
        }

        // Should have some failures
        assert!(fail_count > 0);
        assert!(success_count <= 1000);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:67-100 function test_all_agents_busy <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:321-365 function test_rapid_agent_status_changes
  Similarity: 85.28%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_all_agents_busy (lines 67-100) ---[0m
    async fn test_all_agents_busy() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create busy agents
        for i in 0..3 {
            let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/busy-{}", i),
                ClaudeConfig::for_agent(&format!("busy-{}", i)),
                crate::agent::IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            agent.status = AgentStatus::Working;
            master.agents.insert(format!("busy-agent-{}", i), agent);
        }

        // Try to assign task
        let task = Task::new(
            "busy-task".to_string(),
            "Task for busy agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_rapid_agent_status_changes (lines 321-365) ---[0m
    async fn test_rapid_agent_status_changes() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/rapid",
            ClaudeConfig::for_agent("rapid-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("rapid-agent".to_string(), agent);

        // Rapidly change status
        let statuses = vec![
            AgentStatus::Available,
            AgentStatus::Working,
            AgentStatus::Error("Test error".to_string()),
            AgentStatus::Available,
            AgentStatus::Working,
        ];

        for status in statuses {
            let message = AgentMessage::StatusUpdate {
                agent_id: "rapid-agent".to_string(),
                status: status.clone(),
                metrics: serde_json::Value::Object(serde_json::Map::new()),
            };
            master.coordination_bus.send_message(message).await.unwrap();
        }

        // Should handle all changes without crashing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // The messages were sent successfully, which is what we're testing
        // The actual status update would require the orchestrator to be running
        assert!(master.agents.contains_key("rapid-agent"));
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:103-138 function test_invalid_task_metadata <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:258-295 function test_agent_disconnection_during_task
  Similarity: 90.99%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_task_metadata (lines 103-138) ---[0m
    async fn test_invalid_task_metadata() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with invalid metadata
        let mut task = Task::new(
            "invalid-meta".to_string(),
            "Task with bad metadata".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        task.metadata = Some(
            serde_json::json!({
                "invalid": null,
                "nested": {
                    "deeply": {
                        "nested": {
                            "value": "too deep"
                        }
                    }
                }
            })
            .as_object()
            .unwrap()
            .clone(),
        );

        // Should still handle the task
        master.add_task(task.clone()).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_agent_disconnection_during_task (lines 258-295) ---[0m
    async fn test_agent_disconnection_during_task() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/disconnect",
            ClaudeConfig::for_agent("disconnect-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("disconnect-agent".to_string(), agent);

        // Simulate agent disconnection (remove from pool)
        master.agents.remove("disconnect-agent");

        // Try to send message for this agent
        let message = AgentMessage::TaskCompleted {
            agent_id: "disconnect-agent".to_string(),
            task_id: "orphan-task".to_string(),
            result: TaskResult {
                success: false,
                output: serde_json::json!({"error": "disconnected"}),
                error: Some("Agent disconnected".to_string()),
                duration: std::time::Duration::from_secs(0),
            },
        };

        // Should handle gracefully
        master.coordination_bus.send_message(message).await.unwrap();
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:103-138 function test_invalid_task_metadata <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:217-255 function test_cyclic_task_dependencies
  Similarity: 89.23%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_task_metadata (lines 103-138) ---[0m
    async fn test_invalid_task_metadata() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with invalid metadata
        let mut task = Task::new(
            "invalid-meta".to_string(),
            "Task with bad metadata".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        task.metadata = Some(
            serde_json::json!({
                "invalid": null,
                "nested": {
                    "deeply": {
                        "nested": {
                            "value": "too deep"
                        }
                    }
                }
            })
            .as_object()
            .unwrap()
            .clone(),
        );

        // Should still handle the task
        master.add_task(task.clone()).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_cyclic_task_dependencies (lines 217-255) ---[0m
    async fn test_cyclic_task_dependencies() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with cyclic dependencies
        let task1 = Task::new(
            "cycle-1".to_string(),
            "Task 1".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-3".to_string());

        let task2 = Task::new(
            "cycle-2".to_string(),
            "Task 2".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-1".to_string());

        let task3 = Task::new(
            "cycle-3".to_string(),
            "Task 3".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-2".to_string());

        // Should handle without stack overflow
        master.add_task(task1).await.unwrap();
        master.add_task(task2).await.unwrap();
        master.add_task(task3).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:217-255 function test_cyclic_task_dependencies <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:414-447 function test_invalid_agent_specialization
  Similarity: 90.58%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_cyclic_task_dependencies (lines 217-255) ---[0m
    async fn test_cyclic_task_dependencies() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with cyclic dependencies
        let task1 = Task::new(
            "cycle-1".to_string(),
            "Task 1".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-3".to_string());

        let task2 = Task::new(
            "cycle-2".to_string(),
            "Task 2".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-1".to_string());

        let task3 = Task::new(
            "cycle-3".to_string(),
            "Task 3".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-2".to_string());

        // Should handle without stack overflow
        master.add_task(task1).await.unwrap();
        master.add_task(task2).await.unwrap();
        master.add_task(task3).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_agent_specialization (lines 414-447) ---[0m
    async fn test_invalid_agent_specialization() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent with frontend role
        let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/unknown",
            ClaudeConfig::for_agent("unknown-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Agent has frontend role, but we'll test task assignment for a role that doesn't match
        agent.status = AgentStatus::Available;

        master.agents.insert("unknown-agent".to_string(), agent);

        // Task assignment should still work (fall back to any available)
        let task = Task::new(
            "unknown-task".to_string(),
            "Task for unknown specialization".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_ok());
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:67-100 function test_all_agents_busy <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:258-295 function test_agent_disconnection_during_task
  Similarity: 90.63%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_all_agents_busy (lines 67-100) ---[0m
    async fn test_all_agents_busy() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create busy agents
        for i in 0..3 {
            let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/busy-{}", i),
                ClaudeConfig::for_agent(&format!("busy-{}", i)),
                crate::agent::IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            agent.status = AgentStatus::Working;
            master.agents.insert(format!("busy-agent-{}", i), agent);
        }

        // Try to assign task
        let task = Task::new(
            "busy-task".to_string(),
            "Task for busy agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_agent_disconnection_during_task (lines 258-295) ---[0m
    async fn test_agent_disconnection_during_task() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/disconnect",
            ClaudeConfig::for_agent("disconnect-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("disconnect-agent".to_string(), agent);

        // Simulate agent disconnection (remove from pool)
        master.agents.remove("disconnect-agent");

        // Try to send message for this agent
        let message = AgentMessage::TaskCompleted {
            agent_id: "disconnect-agent".to_string(),
            task_id: "orphan-task".to_string(),
            result: TaskResult {
                success: false,
                output: serde_json::json!({"error": "disconnected"}),
                error: Some("Agent disconnected".to_string()),
                duration: std::time::Duration::from_secs(0),
            },
        };

        // Should handle gracefully
        master.coordination_bus.send_message(message).await.unwrap();
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:217-255 function test_cyclic_task_dependencies <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:451-487 function test_message_queue_overflow
  Similarity: 84.95%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_cyclic_task_dependencies (lines 217-255) ---[0m
    async fn test_cyclic_task_dependencies() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with cyclic dependencies
        let task1 = Task::new(
            "cycle-1".to_string(),
            "Task 1".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-3".to_string());

        let task2 = Task::new(
            "cycle-2".to_string(),
            "Task 2".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-1".to_string());

        let task3 = Task::new(
            "cycle-3".to_string(),
            "Task 3".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-2".to_string());

        // Should handle without stack overflow
        master.add_task(task1).await.unwrap();
        master.add_task(task2).await.unwrap();
        master.add_task(task3).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_message_queue_overflow (lines 451-487) ---[0m
    async fn test_message_queue_overflow() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to overflow task queue (bounded at 1000)
        let mut handles = vec![];
        for i in 0..1100 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("overflow-{}", i),
                    "Overflow task".to_string(),
                    Priority::Low,
                    TaskType::Development,
                );
                master_clone.add_task(task).await
            });
            handles.push(handle);
        }

        // Some should fail due to channel being full
        let mut success_count = 0;
        let mut fail_count = 0;

        for handle in handles {
            match handle.await.unwrap() {
                Ok(_) => success_count += 1,
                Err(_) => fail_count += 1,
            }
        }

        // Should have some failures
        assert!(fail_count > 0);
        assert!(success_count <= 1000);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:67-100 function test_all_agents_busy <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:169-214 function test_malformed_quality_issues
  Similarity: 79.91%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_all_agents_busy (lines 67-100) ---[0m
    async fn test_all_agents_busy() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create busy agents
        for i in 0..3 {
            let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/busy-{}", i),
                ClaudeConfig::for_agent(&format!("busy-{}", i)),
                crate::agent::IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            agent.status = AgentStatus::Working;
            master.agents.insert(format!("busy-agent-{}", i), agent);
        }

        // Try to assign task
        let task = Task::new(
            "busy-task".to_string(),
            "Task for busy agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_malformed_quality_issues (lines 169-214) ---[0m
    async fn test_malformed_quality_issues() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Start the orchestrator
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        // Send quality issue with empty issues list
        let empty_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "empty-issues-task".to_string(),
            issues: vec![],
        };

        // Should handle gracefully
        master
            .coordination_bus
            .send_message(empty_issues)
            .await
            .unwrap();

        // Send quality issue with very long issue descriptions
        let long_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "long-issues-task".to_string(),
            issues: vec!["X".repeat(10000)], // 10k character issue
        };

        master
            .coordination_bus
            .send_message(long_issues)
            .await
            .unwrap();

        // Allow processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Should not crash
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:67-100 function test_all_agents_busy <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:414-447 function test_invalid_agent_specialization
  Similarity: 93.65%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_all_agents_busy (lines 67-100) ---[0m
    async fn test_all_agents_busy() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create busy agents
        for i in 0..3 {
            let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/busy-{}", i),
                ClaudeConfig::for_agent(&format!("busy-{}", i)),
                crate::agent::IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            agent.status = AgentStatus::Working;
            master.agents.insert(format!("busy-agent-{}", i), agent);
        }

        // Try to assign task
        let task = Task::new(
            "busy-task".to_string(),
            "Task for busy agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_agent_specialization (lines 414-447) ---[0m
    async fn test_invalid_agent_specialization() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent with frontend role
        let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/unknown",
            ClaudeConfig::for_agent("unknown-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Agent has frontend role, but we'll test task assignment for a role that doesn't match
        agent.status = AgentStatus::Available;

        master.agents.insert("unknown-agent".to_string(), agent);

        // Task assignment should still work (fall back to any available)
        let task = Task::new(
            "unknown-task".to_string(),
            "Task for unknown specialization".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_ok());
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:67-100 function test_all_agents_busy <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:103-138 function test_invalid_task_metadata
  Similarity: 90.90%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_all_agents_busy (lines 67-100) ---[0m
    async fn test_all_agents_busy() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create busy agents
        for i in 0..3 {
            let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/busy-{}", i),
                ClaudeConfig::for_agent(&format!("busy-{}", i)),
                crate::agent::IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            agent.status = AgentStatus::Working;
            master.agents.insert(format!("busy-agent-{}", i), agent);
        }

        // Try to assign task
        let task = Task::new(
            "busy-task".to_string(),
            "Task for busy agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_task_metadata (lines 103-138) ---[0m
    async fn test_invalid_task_metadata() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with invalid metadata
        let mut task = Task::new(
            "invalid-meta".to_string(),
            "Task with bad metadata".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        task.metadata = Some(
            serde_json::json!({
                "invalid": null,
                "nested": {
                    "deeply": {
                        "nested": {
                            "value": "too deep"
                        }
                    }
                }
            })
            .as_object()
            .unwrap()
            .clone(),
        );

        // Should still handle the task
        master.add_task(task.clone()).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:103-138 function test_invalid_task_metadata <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:451-487 function test_message_queue_overflow
  Similarity: 86.43%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_task_metadata (lines 103-138) ---[0m
    async fn test_invalid_task_metadata() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with invalid metadata
        let mut task = Task::new(
            "invalid-meta".to_string(),
            "Task with bad metadata".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        task.metadata = Some(
            serde_json::json!({
                "invalid": null,
                "nested": {
                    "deeply": {
                        "nested": {
                            "value": "too deep"
                        }
                    }
                }
            })
            .as_object()
            .unwrap()
            .clone(),
        );

        // Should still handle the task
        master.add_task(task.clone()).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_message_queue_overflow (lines 451-487) ---[0m
    async fn test_message_queue_overflow() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to overflow task queue (bounded at 1000)
        let mut handles = vec![];
        for i in 0..1100 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("overflow-{}", i),
                    "Overflow task".to_string(),
                    Priority::Low,
                    TaskType::Development,
                );
                master_clone.add_task(task).await
            });
            handles.push(handle);
        }

        // Some should fail due to channel being full
        let mut success_count = 0;
        let mut fail_count = 0;

        for handle in handles {
            match handle.await.unwrap() {
                Ok(_) => success_count += 1,
                Err(_) => fail_count += 1,
            }
        }

        // Should have some failures
        assert!(fail_count > 0);
        assert!(success_count <= 1000);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:67-100 function test_all_agents_busy <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:451-487 function test_message_queue_overflow
  Similarity: 88.20%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_all_agents_busy (lines 67-100) ---[0m
    async fn test_all_agents_busy() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create busy agents
        for i in 0..3 {
            let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/busy-{}", i),
                ClaudeConfig::for_agent(&format!("busy-{}", i)),
                crate::agent::IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            agent.status = AgentStatus::Working;
            master.agents.insert(format!("busy-agent-{}", i), agent);
        }

        // Try to assign task
        let task = Task::new(
            "busy-task".to_string(),
            "Task for busy agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_message_queue_overflow (lines 451-487) ---[0m
    async fn test_message_queue_overflow() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to overflow task queue (bounded at 1000)
        let mut handles = vec![];
        for i in 0..1100 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("overflow-{}", i),
                    "Overflow task".to_string(),
                    Priority::Low,
                    TaskType::Development,
                );
                master_clone.add_task(task).await
            });
            handles.push(handle);
        }

        // Some should fail due to channel being full
        let mut success_count = 0;
        let mut fail_count = 0;

        for handle in handles {
            match handle.await.unwrap() {
                Ok(_) => success_count += 1,
                Err(_) => fail_count += 1,
            }
        }

        // Should have some failures
        assert!(fail_count > 0);
        assert!(success_count <= 1000);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:414-447 function test_invalid_agent_specialization <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:451-487 function test_message_queue_overflow
  Similarity: 88.15%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_agent_specialization (lines 414-447) ---[0m
    async fn test_invalid_agent_specialization() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent with frontend role
        let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/unknown",
            ClaudeConfig::for_agent("unknown-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Agent has frontend role, but we'll test task assignment for a role that doesn't match
        agent.status = AgentStatus::Available;

        master.agents.insert("unknown-agent".to_string(), agent);

        // Task assignment should still work (fall back to any available)
        let task = Task::new(
            "unknown-task".to_string(),
            "Task for unknown specialization".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_ok());
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_message_queue_overflow (lines 451-487) ---[0m
    async fn test_message_queue_overflow() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to overflow task queue (bounded at 1000)
        let mut handles = vec![];
        for i in 0..1100 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("overflow-{}", i),
                    "Overflow task".to_string(),
                    Priority::Low,
                    TaskType::Development,
                );
                master_clone.add_task(task).await
            });
            handles.push(handle);
        }

        // Some should fail due to channel being full
        let mut success_count = 0;
        let mut fail_count = 0;

        for handle in handles {
            match handle.await.unwrap() {
                Ok(_) => success_count += 1,
                Err(_) => fail_count += 1,
            }
        }

        // Should have some failures
        assert!(fail_count > 0);
        assert!(success_count <= 1000);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:103-138 function test_invalid_task_metadata <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:414-447 function test_invalid_agent_specialization
  Similarity: 88.60%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_task_metadata (lines 103-138) ---[0m
    async fn test_invalid_task_metadata() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with invalid metadata
        let mut task = Task::new(
            "invalid-meta".to_string(),
            "Task with bad metadata".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        task.metadata = Some(
            serde_json::json!({
                "invalid": null,
                "nested": {
                    "deeply": {
                        "nested": {
                            "value": "too deep"
                        }
                    }
                }
            })
            .as_object()
            .unwrap()
            .clone(),
        );

        // Should still handle the task
        master.add_task(task.clone()).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_agent_specialization (lines 414-447) ---[0m
    async fn test_invalid_agent_specialization() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent with frontend role
        let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/unknown",
            ClaudeConfig::for_agent("unknown-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Agent has frontend role, but we'll test task assignment for a role that doesn't match
        agent.status = AgentStatus::Available;

        master.agents.insert("unknown-agent".to_string(), agent);

        // Task assignment should still work (fall back to any available)
        let task = Task::new(
            "unknown-task".to_string(),
            "Task for unknown specialization".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_ok());
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:67-100 function test_all_agents_busy <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:217-255 function test_cyclic_task_dependencies
  Similarity: 82.36%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_all_agents_busy (lines 67-100) ---[0m
    async fn test_all_agents_busy() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create busy agents
        for i in 0..3 {
            let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/busy-{}", i),
                ClaudeConfig::for_agent(&format!("busy-{}", i)),
                crate::agent::IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            agent.status = AgentStatus::Working;
            master.agents.insert(format!("busy-agent-{}", i), agent);
        }

        // Try to assign task
        let task = Task::new(
            "busy-task".to_string(),
            "Task for busy agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_cyclic_task_dependencies (lines 217-255) ---[0m
    async fn test_cyclic_task_dependencies() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with cyclic dependencies
        let task1 = Task::new(
            "cycle-1".to_string(),
            "Task 1".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-3".to_string());

        let task2 = Task::new(
            "cycle-2".to_string(),
            "Task 2".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-1".to_string());

        let task3 = Task::new(
            "cycle-3".to_string(),
            "Task 3".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-2".to_string());

        // Should handle without stack overflow
        master.add_task(task1).await.unwrap();
        master.add_task(task2).await.unwrap();
        master.add_task(task3).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:141-166 function test_concurrent_message_overload <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:321-365 function test_rapid_agent_status_changes
  Similarity: 84.04%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_concurrent_message_overload (lines 141-166) ---[0m
    async fn test_concurrent_message_overload() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Send many messages concurrently
        let mut handles = vec![];
        for i in 0..100 {
            let bus_clone = bus.clone();
            let handle = tokio::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id: format!("agent-{}", i),
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await
            });
            handles.push(handle);
        }

        // All should complete without panic
        for handle in handles {
            assert!(handle.await.is_ok());
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_rapid_agent_status_changes (lines 321-365) ---[0m
    async fn test_rapid_agent_status_changes() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/rapid",
            ClaudeConfig::for_agent("rapid-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("rapid-agent".to_string(), agent);

        // Rapidly change status
        let statuses = vec![
            AgentStatus::Available,
            AgentStatus::Working,
            AgentStatus::Error("Test error".to_string()),
            AgentStatus::Available,
            AgentStatus::Working,
        ];

        for status in statuses {
            let message = AgentMessage::StatusUpdate {
                agent_id: "rapid-agent".to_string(),
                status: status.clone(),
                metrics: serde_json::Value::Object(serde_json::Map::new()),
            };
            master.coordination_bus.send_message(message).await.unwrap();
        }

        // Should handle all changes without crashing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // The messages were sent successfully, which is what we're testing
        // The actual status update would require the orchestrator to be running
        assert!(master.agents.contains_key("rapid-agent"));
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:169-214 function test_malformed_quality_issues <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:298-318 function test_extremely_long_task_descriptions
  Similarity: 88.92%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_malformed_quality_issues (lines 169-214) ---[0m
    async fn test_malformed_quality_issues() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Start the orchestrator
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        // Send quality issue with empty issues list
        let empty_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "empty-issues-task".to_string(),
            issues: vec![],
        };

        // Should handle gracefully
        master
            .coordination_bus
            .send_message(empty_issues)
            .await
            .unwrap();

        // Send quality issue with very long issue descriptions
        let long_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "long-issues-task".to_string(),
            issues: vec!["X".repeat(10000)], // 10k character issue
        };

        master
            .coordination_bus
            .send_message(long_issues)
            .await
            .unwrap();

        // Allow processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Should not crash
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_extremely_long_task_descriptions (lines 298-318) ---[0m
    async fn test_extremely_long_task_descriptions() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with extremely long description
        let long_description = "A".repeat(100_000); // 100k characters
        let task = Task::new(
            "long-desc".to_string(),
            long_description,
            Priority::Low,
            TaskType::Documentation,
        );

        // Should handle without memory issues
        master.add_task(task).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:217-255 function test_cyclic_task_dependencies <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:298-318 function test_extremely_long_task_descriptions
  Similarity: 94.45%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_cyclic_task_dependencies (lines 217-255) ---[0m
    async fn test_cyclic_task_dependencies() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with cyclic dependencies
        let task1 = Task::new(
            "cycle-1".to_string(),
            "Task 1".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-3".to_string());

        let task2 = Task::new(
            "cycle-2".to_string(),
            "Task 2".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-1".to_string());

        let task3 = Task::new(
            "cycle-3".to_string(),
            "Task 3".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-2".to_string());

        // Should handle without stack overflow
        master.add_task(task1).await.unwrap();
        master.add_task(task2).await.unwrap();
        master.add_task(task3).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_extremely_long_task_descriptions (lines 298-318) ---[0m
    async fn test_extremely_long_task_descriptions() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with extremely long description
        let long_description = "A".repeat(100_000); // 100k characters
        let task = Task::new(
            "long-desc".to_string(),
            long_description,
            Priority::Low,
            TaskType::Documentation,
        );

        // Should handle without memory issues
        master.add_task(task).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:141-166 function test_concurrent_message_overload <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:258-295 function test_agent_disconnection_during_task
  Similarity: 87.17%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_concurrent_message_overload (lines 141-166) ---[0m
    async fn test_concurrent_message_overload() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Send many messages concurrently
        let mut handles = vec![];
        for i in 0..100 {
            let bus_clone = bus.clone();
            let handle = tokio::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id: format!("agent-{}", i),
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await
            });
            handles.push(handle);
        }

        // All should complete without panic
        for handle in handles {
            assert!(handle.await.is_ok());
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_agent_disconnection_during_task (lines 258-295) ---[0m
    async fn test_agent_disconnection_during_task() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/disconnect",
            ClaudeConfig::for_agent("disconnect-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("disconnect-agent".to_string(), agent);

        // Simulate agent disconnection (remove from pool)
        master.agents.remove("disconnect-agent");

        // Try to send message for this agent
        let message = AgentMessage::TaskCompleted {
            agent_id: "disconnect-agent".to_string(),
            task_id: "orphan-task".to_string(),
            result: TaskResult {
                success: false,
                output: serde_json::json!({"error": "disconnected"}),
                error: Some("Agent disconnected".to_string()),
                duration: std::time::Duration::from_secs(0),
            },
        };

        // Should handle gracefully
        master.coordination_bus.send_message(message).await.unwrap();
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:103-138 function test_invalid_task_metadata <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:298-318 function test_extremely_long_task_descriptions
  Similarity: 96.06%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_task_metadata (lines 103-138) ---[0m
    async fn test_invalid_task_metadata() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with invalid metadata
        let mut task = Task::new(
            "invalid-meta".to_string(),
            "Task with bad metadata".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        task.metadata = Some(
            serde_json::json!({
                "invalid": null,
                "nested": {
                    "deeply": {
                        "nested": {
                            "value": "too deep"
                        }
                    }
                }
            })
            .as_object()
            .unwrap()
            .clone(),
        );

        // Should still handle the task
        master.add_task(task.clone()).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_extremely_long_task_descriptions (lines 298-318) ---[0m
    async fn test_extremely_long_task_descriptions() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with extremely long description
        let long_description = "A".repeat(100_000); // 100k characters
        let task = Task::new(
            "long-desc".to_string(),
            long_description,
            Priority::Low,
            TaskType::Documentation,
        );

        // Should handle without memory issues
        master.add_task(task).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:141-166 function test_concurrent_message_overload <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:451-487 function test_message_queue_overflow
  Similarity: 86.50%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_concurrent_message_overload (lines 141-166) ---[0m
    async fn test_concurrent_message_overload() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Send many messages concurrently
        let mut handles = vec![];
        for i in 0..100 {
            let bus_clone = bus.clone();
            let handle = tokio::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id: format!("agent-{}", i),
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await
            });
            handles.push(handle);
        }

        // All should complete without panic
        for handle in handles {
            assert!(handle.await.is_ok());
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_message_queue_overflow (lines 451-487) ---[0m
    async fn test_message_queue_overflow() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to overflow task queue (bounded at 1000)
        let mut handles = vec![];
        for i in 0..1100 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("overflow-{}", i),
                    "Overflow task".to_string(),
                    Priority::Low,
                    TaskType::Development,
                );
                master_clone.add_task(task).await
            });
            handles.push(handle);
        }

        // Some should fail due to channel being full
        let mut success_count = 0;
        let mut fail_count = 0;

        for handle in handles {
            match handle.await.unwrap() {
                Ok(_) => success_count += 1,
                Err(_) => fail_count += 1,
            }
        }

        // Should have some failures
        assert!(fail_count > 0);
        assert!(success_count <= 1000);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:258-295 function test_agent_disconnection_during_task <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:298-318 function test_extremely_long_task_descriptions
  Similarity: 92.01%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_agent_disconnection_during_task (lines 258-295) ---[0m
    async fn test_agent_disconnection_during_task() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/disconnect",
            ClaudeConfig::for_agent("disconnect-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("disconnect-agent".to_string(), agent);

        // Simulate agent disconnection (remove from pool)
        master.agents.remove("disconnect-agent");

        // Try to send message for this agent
        let message = AgentMessage::TaskCompleted {
            agent_id: "disconnect-agent".to_string(),
            task_id: "orphan-task".to_string(),
            result: TaskResult {
                success: false,
                output: serde_json::json!({"error": "disconnected"}),
                error: Some("Agent disconnected".to_string()),
                duration: std::time::Duration::from_secs(0),
            },
        };

        // Should handle gracefully
        master.coordination_bus.send_message(message).await.unwrap();
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_extremely_long_task_descriptions (lines 298-318) ---[0m
    async fn test_extremely_long_task_descriptions() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with extremely long description
        let long_description = "A".repeat(100_000); // 100k characters
        let task = Task::new(
            "long-desc".to_string(),
            long_description,
            Priority::Low,
            TaskType::Documentation,
        );

        // Should handle without memory issues
        master.add_task(task).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:103-138 function test_invalid_task_metadata <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:141-166 function test_concurrent_message_overload
  Similarity: 87.29%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_task_metadata (lines 103-138) ---[0m
    async fn test_invalid_task_metadata() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with invalid metadata
        let mut task = Task::new(
            "invalid-meta".to_string(),
            "Task with bad metadata".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        task.metadata = Some(
            serde_json::json!({
                "invalid": null,
                "nested": {
                    "deeply": {
                        "nested": {
                            "value": "too deep"
                        }
                    }
                }
            })
            .as_object()
            .unwrap()
            .clone(),
        );

        // Should still handle the task
        master.add_task(task.clone()).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_concurrent_message_overload (lines 141-166) ---[0m
    async fn test_concurrent_message_overload() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Send many messages concurrently
        let mut handles = vec![];
        for i in 0..100 {
            let bus_clone = bus.clone();
            let handle = tokio::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id: format!("agent-{}", i),
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await
            });
            handles.push(handle);
        }

        // All should complete without panic
        for handle in handles {
            assert!(handle.await.is_ok());
        }
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:67-100 function test_all_agents_busy <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:141-166 function test_concurrent_message_overload
  Similarity: 88.48%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_all_agents_busy (lines 67-100) ---[0m
    async fn test_all_agents_busy() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create busy agents
        for i in 0..3 {
            let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/busy-{}", i),
                ClaudeConfig::for_agent(&format!("busy-{}", i)),
                crate::agent::IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            agent.status = AgentStatus::Working;
            master.agents.insert(format!("busy-agent-{}", i), agent);
        }

        // Try to assign task
        let task = Task::new(
            "busy-task".to_string(),
            "Task for busy agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_concurrent_message_overload (lines 141-166) ---[0m
    async fn test_concurrent_message_overload() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Send many messages concurrently
        let mut handles = vec![];
        for i in 0..100 {
            let bus_clone = bus.clone();
            let handle = tokio::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id: format!("agent-{}", i),
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await
            });
            handles.push(handle);
        }

        // All should complete without panic
        for handle in handles {
            assert!(handle.await.is_ok());
        }
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:141-166 function test_concurrent_message_overload <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:169-214 function test_malformed_quality_issues
  Similarity: 73.54%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_concurrent_message_overload (lines 141-166) ---[0m
    async fn test_concurrent_message_overload() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Send many messages concurrently
        let mut handles = vec![];
        for i in 0..100 {
            let bus_clone = bus.clone();
            let handle = tokio::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id: format!("agent-{}", i),
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await
            });
            handles.push(handle);
        }

        // All should complete without panic
        for handle in handles {
            assert!(handle.await.is_ok());
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_malformed_quality_issues (lines 169-214) ---[0m
    async fn test_malformed_quality_issues() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Start the orchestrator
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        // Send quality issue with empty issues list
        let empty_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "empty-issues-task".to_string(),
            issues: vec![],
        };

        // Should handle gracefully
        master
            .coordination_bus
            .send_message(empty_issues)
            .await
            .unwrap();

        // Send quality issue with very long issue descriptions
        let long_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "long-issues-task".to_string(),
            issues: vec!["X".repeat(10000)], // 10k character issue
        };

        master
            .coordination_bus
            .send_message(long_issues)
            .await
            .unwrap();

        // Allow processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Should not crash
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:9-41 function create_minimal_config <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:67-100 function test_all_agents_busy
  Similarity: 78.48%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:create_minimal_config (lines 9-41) ---[0m
    async fn create_minimal_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Create minimal config manually
        let config = CcswarmConfig {
            project: crate::config::ProjectConfig {
                name: "Test Project".to_string(),
                repository: crate::config::RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: crate::config::MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: crate::config::ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: false,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents: std::collections::HashMap::new(),
            coordination: crate::config::CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };
        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_all_agents_busy (lines 67-100) ---[0m
    async fn test_all_agents_busy() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create busy agents
        for i in 0..3 {
            let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/busy-{}", i),
                ClaudeConfig::for_agent(&format!("busy-{}", i)),
                crate::agent::IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            agent.status = AgentStatus::Working;
            master.agents.insert(format!("busy-agent-{}", i), agent);
        }

        // Try to assign task
        let task = Task::new(
            "busy-task".to_string(),
            "Task for busy agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:103-138 function test_invalid_task_metadata <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:368-388 function test_nil_task_priority
  Similarity: 92.12%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_task_metadata (lines 103-138) ---[0m
    async fn test_invalid_task_metadata() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with invalid metadata
        let mut task = Task::new(
            "invalid-meta".to_string(),
            "Task with bad metadata".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        task.metadata = Some(
            serde_json::json!({
                "invalid": null,
                "nested": {
                    "deeply": {
                        "nested": {
                            "value": "too deep"
                        }
                    }
                }
            })
            .as_object()
            .unwrap()
            .clone(),
        );

        // Should still handle the task
        master.add_task(task.clone()).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_nil_task_priority (lines 368-388) ---[0m
    async fn test_nil_task_priority() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with same priority
        for i in 0..5 {
            let task = Task::new(
                format!("same-priority-{}", i),
                "Same priority task".to_string(),
                Priority::Medium,
                TaskType::Development,
            );
            master.add_task(task).await.unwrap();
        }

        // All should be added successfully
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 5);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:141-166 function test_concurrent_message_overload <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:217-255 function test_cyclic_task_dependencies
  Similarity: 80.77%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_concurrent_message_overload (lines 141-166) ---[0m
    async fn test_concurrent_message_overload() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Send many messages concurrently
        let mut handles = vec![];
        for i in 0..100 {
            let bus_clone = bus.clone();
            let handle = tokio::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id: format!("agent-{}", i),
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await
            });
            handles.push(handle);
        }

        // All should complete without panic
        for handle in handles {
            assert!(handle.await.is_ok());
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_cyclic_task_dependencies (lines 217-255) ---[0m
    async fn test_cyclic_task_dependencies() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with cyclic dependencies
        let task1 = Task::new(
            "cycle-1".to_string(),
            "Task 1".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-3".to_string());

        let task2 = Task::new(
            "cycle-2".to_string(),
            "Task 2".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-1".to_string());

        let task3 = Task::new(
            "cycle-3".to_string(),
            "Task 3".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-2".to_string());

        // Should handle without stack overflow
        master.add_task(task1).await.unwrap();
        master.add_task(task2).await.unwrap();
        master.add_task(task3).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:9-41 function create_minimal_config <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:451-487 function test_message_queue_overflow
  Similarity: 74.44%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:create_minimal_config (lines 9-41) ---[0m
    async fn create_minimal_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Create minimal config manually
        let config = CcswarmConfig {
            project: crate::config::ProjectConfig {
                name: "Test Project".to_string(),
                repository: crate::config::RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: crate::config::MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: crate::config::ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: false,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents: std::collections::HashMap::new(),
            coordination: crate::config::CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };
        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_message_queue_overflow (lines 451-487) ---[0m
    async fn test_message_queue_overflow() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to overflow task queue (bounded at 1000)
        let mut handles = vec![];
        for i in 0..1100 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("overflow-{}", i),
                    "Overflow task".to_string(),
                    Priority::Low,
                    TaskType::Development,
                );
                master_clone.add_task(task).await
            });
            handles.push(handle);
        }

        // Some should fail due to channel being full
        let mut success_count = 0;
        let mut fail_count = 0;

        for handle in handles {
            match handle.await.unwrap() {
                Ok(_) => success_count += 1,
                Err(_) => fail_count += 1,
            }
        }

        // Should have some failures
        assert!(fail_count > 0);
        assert!(success_count <= 1000);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:9-41 function create_minimal_config <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:103-138 function test_invalid_task_metadata
  Similarity: 75.40%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:create_minimal_config (lines 9-41) ---[0m
    async fn create_minimal_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Create minimal config manually
        let config = CcswarmConfig {
            project: crate::config::ProjectConfig {
                name: "Test Project".to_string(),
                repository: crate::config::RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: crate::config::MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: crate::config::ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: false,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents: std::collections::HashMap::new(),
            coordination: crate::config::CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };
        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_task_metadata (lines 103-138) ---[0m
    async fn test_invalid_task_metadata() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with invalid metadata
        let mut task = Task::new(
            "invalid-meta".to_string(),
            "Task with bad metadata".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        task.metadata = Some(
            serde_json::json!({
                "invalid": null,
                "nested": {
                    "deeply": {
                        "nested": {
                            "value": "too deep"
                        }
                    }
                }
            })
            .as_object()
            .unwrap()
            .clone(),
        );

        // Should still handle the task
        master.add_task(task.clone()).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:103-138 function test_invalid_task_metadata <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:391-411 function test_orchestrator_state_recovery
  Similarity: 90.97%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_task_metadata (lines 103-138) ---[0m
    async fn test_invalid_task_metadata() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with invalid metadata
        let mut task = Task::new(
            "invalid-meta".to_string(),
            "Task with bad metadata".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        task.metadata = Some(
            serde_json::json!({
                "invalid": null,
                "nested": {
                    "deeply": {
                        "nested": {
                            "value": "too deep"
                        }
                    }
                }
            })
            .as_object()
            .unwrap()
            .clone(),
        );

        // Should still handle the task
        master.add_task(task.clone()).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_orchestrator_state_recovery (lines 391-411) ---[0m
    async fn test_orchestrator_state_recovery() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Simulate error state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Error("Test error".to_string());
        }

        // Should be able to recover
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:298-318 function test_extremely_long_task_descriptions <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:451-487 function test_message_queue_overflow
  Similarity: 89.13%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_extremely_long_task_descriptions (lines 298-318) ---[0m
    async fn test_extremely_long_task_descriptions() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with extremely long description
        let long_description = "A".repeat(100_000); // 100k characters
        let task = Task::new(
            "long-desc".to_string(),
            long_description,
            Priority::Low,
            TaskType::Documentation,
        );

        // Should handle without memory issues
        master.add_task(task).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_message_queue_overflow (lines 451-487) ---[0m
    async fn test_message_queue_overflow() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to overflow task queue (bounded at 1000)
        let mut handles = vec![];
        for i in 0..1100 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("overflow-{}", i),
                    "Overflow task".to_string(),
                    Priority::Low,
                    TaskType::Development,
                );
                master_clone.add_task(task).await
            });
            handles.push(handle);
        }

        // Some should fail due to channel being full
        let mut success_count = 0;
        let mut fail_count = 0;

        for handle in handles {
            match handle.await.unwrap() {
                Ok(_) => success_count += 1,
                Err(_) => fail_count += 1,
            }
        }

        // Should have some failures
        assert!(fail_count > 0);
        assert!(success_count <= 1000);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:9-41 function create_minimal_config <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:258-295 function test_agent_disconnection_during_task
  Similarity: 72.03%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:create_minimal_config (lines 9-41) ---[0m
    async fn create_minimal_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Create minimal config manually
        let config = CcswarmConfig {
            project: crate::config::ProjectConfig {
                name: "Test Project".to_string(),
                repository: crate::config::RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: crate::config::MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: crate::config::ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: false,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents: std::collections::HashMap::new(),
            coordination: crate::config::CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };
        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_agent_disconnection_during_task (lines 258-295) ---[0m
    async fn test_agent_disconnection_during_task() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/disconnect",
            ClaudeConfig::for_agent("disconnect-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("disconnect-agent".to_string(), agent);

        // Simulate agent disconnection (remove from pool)
        master.agents.remove("disconnect-agent");

        // Try to send message for this agent
        let message = AgentMessage::TaskCompleted {
            agent_id: "disconnect-agent".to_string(),
            task_id: "orphan-task".to_string(),
            result: TaskResult {
                success: false,
                output: serde_json::json!({"error": "disconnected"}),
                error: Some("Agent disconnected".to_string()),
                duration: std::time::Duration::from_secs(0),
            },
        };

        // Should handle gracefully
        master.coordination_bus.send_message(message).await.unwrap();
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:258-295 function test_agent_disconnection_during_task <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:391-411 function test_orchestrator_state_recovery
  Similarity: 86.37%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_agent_disconnection_during_task (lines 258-295) ---[0m
    async fn test_agent_disconnection_during_task() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/disconnect",
            ClaudeConfig::for_agent("disconnect-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("disconnect-agent".to_string(), agent);

        // Simulate agent disconnection (remove from pool)
        master.agents.remove("disconnect-agent");

        // Try to send message for this agent
        let message = AgentMessage::TaskCompleted {
            agent_id: "disconnect-agent".to_string(),
            task_id: "orphan-task".to_string(),
            result: TaskResult {
                success: false,
                output: serde_json::json!({"error": "disconnected"}),
                error: Some("Agent disconnected".to_string()),
                duration: std::time::Duration::from_secs(0),
            },
        };

        // Should handle gracefully
        master.coordination_bus.send_message(message).await.unwrap();
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_orchestrator_state_recovery (lines 391-411) ---[0m
    async fn test_orchestrator_state_recovery() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Simulate error state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Error("Test error".to_string());
        }

        // Should be able to recover
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:67-100 function test_all_agents_busy <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:298-318 function test_extremely_long_task_descriptions
  Similarity: 92.23%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_all_agents_busy (lines 67-100) ---[0m
    async fn test_all_agents_busy() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create busy agents
        for i in 0..3 {
            let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/busy-{}", i),
                ClaudeConfig::for_agent(&format!("busy-{}", i)),
                crate::agent::IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            agent.status = AgentStatus::Working;
            master.agents.insert(format!("busy-agent-{}", i), agent);
        }

        // Try to assign task
        let task = Task::new(
            "busy-task".to_string(),
            "Task for busy agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_extremely_long_task_descriptions (lines 298-318) ---[0m
    async fn test_extremely_long_task_descriptions() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with extremely long description
        let long_description = "A".repeat(100_000); // 100k characters
        let task = Task::new(
            "long-desc".to_string(),
            long_description,
            Priority::Low,
            TaskType::Documentation,
        );

        // Should handle without memory issues
        master.add_task(task).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:258-295 function test_agent_disconnection_during_task <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:368-388 function test_nil_task_priority
  Similarity: 85.41%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_agent_disconnection_during_task (lines 258-295) ---[0m
    async fn test_agent_disconnection_during_task() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent
        let agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/disconnect",
            ClaudeConfig::for_agent("disconnect-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master.agents.insert("disconnect-agent".to_string(), agent);

        // Simulate agent disconnection (remove from pool)
        master.agents.remove("disconnect-agent");

        // Try to send message for this agent
        let message = AgentMessage::TaskCompleted {
            agent_id: "disconnect-agent".to_string(),
            task_id: "orphan-task".to_string(),
            result: TaskResult {
                success: false,
                output: serde_json::json!({"error": "disconnected"}),
                error: Some("Agent disconnected".to_string()),
                duration: std::time::Duration::from_secs(0),
            },
        };

        // Should handle gracefully
        master.coordination_bus.send_message(message).await.unwrap();
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_nil_task_priority (lines 368-388) ---[0m
    async fn test_nil_task_priority() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with same priority
        for i in 0..5 {
            let task = Task::new(
                format!("same-priority-{}", i),
                "Same priority task".to_string(),
                Priority::Medium,
                TaskType::Development,
            );
            master.add_task(task).await.unwrap();
        }

        // All should be added successfully
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 5);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:44-64 function test_empty_agent_pool <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:103-138 function test_invalid_task_metadata
  Similarity: 88.33%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_empty_agent_pool (lines 44-64) ---[0m
    async fn test_empty_agent_pool() {
        let (config, _temp_dir) = create_minimal_config().await;
        let repo_path = std::path::PathBuf::from("/tmp/test");

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to assign task with no agents
        let task = Task::new(
            "no-agents-task".to_string(),
            "Task with no agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("No available agents"));
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_task_metadata (lines 103-138) ---[0m
    async fn test_invalid_task_metadata() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with invalid metadata
        let mut task = Task::new(
            "invalid-meta".to_string(),
            "Task with bad metadata".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        task.metadata = Some(
            serde_json::json!({
                "invalid": null,
                "nested": {
                    "deeply": {
                        "nested": {
                            "value": "too deep"
                        }
                    }
                }
            })
            .as_object()
            .unwrap()
            .clone(),
        );

        // Should still handle the task
        master.add_task(task.clone()).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:391-411 function test_orchestrator_state_recovery <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:451-487 function test_message_queue_overflow
  Similarity: 86.61%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_orchestrator_state_recovery (lines 391-411) ---[0m
    async fn test_orchestrator_state_recovery() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Simulate error state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Error("Test error".to_string());
        }

        // Should be able to recover
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_message_queue_overflow (lines 451-487) ---[0m
    async fn test_message_queue_overflow() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to overflow task queue (bounded at 1000)
        let mut handles = vec![];
        for i in 0..1100 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("overflow-{}", i),
                    "Overflow task".to_string(),
                    Priority::Low,
                    TaskType::Development,
                );
                master_clone.add_task(task).await
            });
            handles.push(handle);
        }

        // Some should fail due to channel being full
        let mut success_count = 0;
        let mut fail_count = 0;

        for handle in handles {
            match handle.await.unwrap() {
                Ok(_) => success_count += 1,
                Err(_) => fail_count += 1,
            }
        }

        // Should have some failures
        assert!(fail_count > 0);
        assert!(success_count <= 1000);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:67-100 function test_all_agents_busy <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:368-388 function test_nil_task_priority
  Similarity: 91.05%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_all_agents_busy (lines 67-100) ---[0m
    async fn test_all_agents_busy() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create busy agents
        for i in 0..3 {
            let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/busy-{}", i),
                ClaudeConfig::for_agent(&format!("busy-{}", i)),
                crate::agent::IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            agent.status = AgentStatus::Working;
            master.agents.insert(format!("busy-agent-{}", i), agent);
        }

        // Try to assign task
        let task = Task::new(
            "busy-task".to_string(),
            "Task for busy agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_nil_task_priority (lines 368-388) ---[0m
    async fn test_nil_task_priority() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with same priority
        for i in 0..5 {
            let task = Task::new(
                format!("same-priority-{}", i),
                "Same priority task".to_string(),
                Priority::Medium,
                TaskType::Development,
            );
            master.add_task(task).await.unwrap();
        }

        // All should be added successfully
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 5);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:67-100 function test_all_agents_busy <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:391-411 function test_orchestrator_state_recovery
  Similarity: 90.25%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_all_agents_busy (lines 67-100) ---[0m
    async fn test_all_agents_busy() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create busy agents
        for i in 0..3 {
            let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/busy-{}", i),
                ClaudeConfig::for_agent(&format!("busy-{}", i)),
                crate::agent::IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            agent.status = AgentStatus::Working;
            master.agents.insert(format!("busy-agent-{}", i), agent);
        }

        // Try to assign task
        let task = Task::new(
            "busy-task".to_string(),
            "Task for busy agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_orchestrator_state_recovery (lines 391-411) ---[0m
    async fn test_orchestrator_state_recovery() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Simulate error state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Error("Test error".to_string());
        }

        // Should be able to recover
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:169-214 function test_malformed_quality_issues <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:391-411 function test_orchestrator_state_recovery
  Similarity: 73.68%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_malformed_quality_issues (lines 169-214) ---[0m
    async fn test_malformed_quality_issues() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Start the orchestrator
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        // Send quality issue with empty issues list
        let empty_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "empty-issues-task".to_string(),
            issues: vec![],
        };

        // Should handle gracefully
        master
            .coordination_bus
            .send_message(empty_issues)
            .await
            .unwrap();

        // Send quality issue with very long issue descriptions
        let long_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "long-issues-task".to_string(),
            issues: vec!["X".repeat(10000)], // 10k character issue
        };

        master
            .coordination_bus
            .send_message(long_issues)
            .await
            .unwrap();

        // Allow processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Should not crash
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_orchestrator_state_recovery (lines 391-411) ---[0m
    async fn test_orchestrator_state_recovery() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Simulate error state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Error("Test error".to_string());
        }

        // Should be able to recover
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:368-388 function test_nil_task_priority <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:451-487 function test_message_queue_overflow
  Similarity: 84.69%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_nil_task_priority (lines 368-388) ---[0m
    async fn test_nil_task_priority() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with same priority
        for i in 0..5 {
            let task = Task::new(
                format!("same-priority-{}", i),
                "Same priority task".to_string(),
                Priority::Medium,
                TaskType::Development,
            );
            master.add_task(task).await.unwrap();
        }

        // All should be added successfully
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 5);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_message_queue_overflow (lines 451-487) ---[0m
    async fn test_message_queue_overflow() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to overflow task queue (bounded at 1000)
        let mut handles = vec![];
        for i in 0..1100 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("overflow-{}", i),
                    "Overflow task".to_string(),
                    Priority::Low,
                    TaskType::Development,
                );
                master_clone.add_task(task).await
            });
            handles.push(handle);
        }

        // Some should fail due to channel being full
        let mut success_count = 0;
        let mut fail_count = 0;

        for handle in handles {
            match handle.await.unwrap() {
                Ok(_) => success_count += 1,
                Err(_) => fail_count += 1,
            }
        }

        // Should have some failures
        assert!(fail_count > 0);
        assert!(success_count <= 1000);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:44-64 function test_empty_agent_pool <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:169-214 function test_malformed_quality_issues
  Similarity: 73.07%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_empty_agent_pool (lines 44-64) ---[0m
    async fn test_empty_agent_pool() {
        let (config, _temp_dir) = create_minimal_config().await;
        let repo_path = std::path::PathBuf::from("/tmp/test");

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to assign task with no agents
        let task = Task::new(
            "no-agents-task".to_string(),
            "Task with no agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("No available agents"));
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_malformed_quality_issues (lines 169-214) ---[0m
    async fn test_malformed_quality_issues() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Start the orchestrator
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        // Send quality issue with empty issues list
        let empty_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "empty-issues-task".to_string(),
            issues: vec![],
        };

        // Should handle gracefully
        master
            .coordination_bus
            .send_message(empty_issues)
            .await
            .unwrap();

        // Send quality issue with very long issue descriptions
        let long_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "long-issues-task".to_string(),
            issues: vec!["X".repeat(10000)], // 10k character issue
        };

        master
            .coordination_bus
            .send_message(long_issues)
            .await
            .unwrap();

        // Allow processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Should not crash
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:298-318 function test_extremely_long_task_descriptions <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:414-447 function test_invalid_agent_specialization
  Similarity: 88.80%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_extremely_long_task_descriptions (lines 298-318) ---[0m
    async fn test_extremely_long_task_descriptions() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with extremely long description
        let long_description = "A".repeat(100_000); // 100k characters
        let task = Task::new(
            "long-desc".to_string(),
            long_description,
            Priority::Low,
            TaskType::Documentation,
        );

        // Should handle without memory issues
        master.add_task(task).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_agent_specialization (lines 414-447) ---[0m
    async fn test_invalid_agent_specialization() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent with frontend role
        let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/unknown",
            ClaudeConfig::for_agent("unknown-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Agent has frontend role, but we'll test task assignment for a role that doesn't match
        agent.status = AgentStatus::Available;

        master.agents.insert("unknown-agent".to_string(), agent);

        // Task assignment should still work (fall back to any available)
        let task = Task::new(
            "unknown-task".to_string(),
            "Task for unknown specialization".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_ok());
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:9-41 function create_minimal_config <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:141-166 function test_concurrent_message_overload
  Similarity: 81.60%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:create_minimal_config (lines 9-41) ---[0m
    async fn create_minimal_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Create minimal config manually
        let config = CcswarmConfig {
            project: crate::config::ProjectConfig {
                name: "Test Project".to_string(),
                repository: crate::config::RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: crate::config::MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: crate::config::ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: false,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents: std::collections::HashMap::new(),
            coordination: crate::config::CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };
        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_concurrent_message_overload (lines 141-166) ---[0m
    async fn test_concurrent_message_overload() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Send many messages concurrently
        let mut handles = vec![];
        for i in 0..100 {
            let bus_clone = bus.clone();
            let handle = tokio::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id: format!("agent-{}", i),
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await
            });
            handles.push(handle);
        }

        // All should complete without panic
        for handle in handles {
            assert!(handle.await.is_ok());
        }
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:141-166 function test_concurrent_message_overload <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:414-447 function test_invalid_agent_specialization
  Similarity: 80.21%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_concurrent_message_overload (lines 141-166) ---[0m
    async fn test_concurrent_message_overload() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Send many messages concurrently
        let mut handles = vec![];
        for i in 0..100 {
            let bus_clone = bus.clone();
            let handle = tokio::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id: format!("agent-{}", i),
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await
            });
            handles.push(handle);
        }

        // All should complete without panic
        for handle in handles {
            assert!(handle.await.is_ok());
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_agent_specialization (lines 414-447) ---[0m
    async fn test_invalid_agent_specialization() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent with frontend role
        let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/unknown",
            ClaudeConfig::for_agent("unknown-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Agent has frontend role, but we'll test task assignment for a role that doesn't match
        agent.status = AgentStatus::Available;

        master.agents.insert("unknown-agent".to_string(), agent);

        // Task assignment should still work (fall back to any available)
        let task = Task::new(
            "unknown-task".to_string(),
            "Task for unknown specialization".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_ok());
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:44-64 function test_empty_agent_pool <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:451-487 function test_message_queue_overflow
  Similarity: 82.38%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_empty_agent_pool (lines 44-64) ---[0m
    async fn test_empty_agent_pool() {
        let (config, _temp_dir) = create_minimal_config().await;
        let repo_path = std::path::PathBuf::from("/tmp/test");

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to assign task with no agents
        let task = Task::new(
            "no-agents-task".to_string(),
            "Task with no agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("No available agents"));
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_message_queue_overflow (lines 451-487) ---[0m
    async fn test_message_queue_overflow() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to overflow task queue (bounded at 1000)
        let mut handles = vec![];
        for i in 0..1100 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("overflow-{}", i),
                    "Overflow task".to_string(),
                    Priority::Low,
                    TaskType::Development,
                );
                master_clone.add_task(task).await
            });
            handles.push(handle);
        }

        // Some should fail due to channel being full
        let mut success_count = 0;
        let mut fail_count = 0;

        for handle in handles {
            match handle.await.unwrap() {
                Ok(_) => success_count += 1,
                Err(_) => fail_count += 1,
            }
        }

        // Should have some failures
        assert!(fail_count > 0);
        assert!(success_count <= 1000);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:9-41 function create_minimal_config <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:368-388 function test_nil_task_priority
  Similarity: 87.99%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:create_minimal_config (lines 9-41) ---[0m
    async fn create_minimal_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Create minimal config manually
        let config = CcswarmConfig {
            project: crate::config::ProjectConfig {
                name: "Test Project".to_string(),
                repository: crate::config::RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: crate::config::MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: crate::config::ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: false,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents: std::collections::HashMap::new(),
            coordination: crate::config::CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };
        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_nil_task_priority (lines 368-388) ---[0m
    async fn test_nil_task_priority() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with same priority
        for i in 0..5 {
            let task = Task::new(
                format!("same-priority-{}", i),
                "Same priority task".to_string(),
                Priority::Medium,
                TaskType::Development,
            );
            master.add_task(task).await.unwrap();
        }

        // All should be added successfully
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 5);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:169-214 function test_malformed_quality_issues <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:368-388 function test_nil_task_priority
  Similarity: 70.88%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_malformed_quality_issues (lines 169-214) ---[0m
    async fn test_malformed_quality_issues() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Start the orchestrator
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        // Send quality issue with empty issues list
        let empty_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "empty-issues-task".to_string(),
            issues: vec![],
        };

        // Should handle gracefully
        master
            .coordination_bus
            .send_message(empty_issues)
            .await
            .unwrap();

        // Send quality issue with very long issue descriptions
        let long_issues = AgentMessage::QualityIssue {
            agent_id: "test-agent".to_string(),
            task_id: "long-issues-task".to_string(),
            issues: vec!["X".repeat(10000)], // 10k character issue
        };

        master
            .coordination_bus
            .send_message(long_issues)
            .await
            .unwrap();

        // Allow processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Should not crash
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_nil_task_priority (lines 368-388) ---[0m
    async fn test_nil_task_priority() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with same priority
        for i in 0..5 {
            let task = Task::new(
                format!("same-priority-{}", i),
                "Same priority task".to_string(),
                Priority::Medium,
                TaskType::Development,
            );
            master.add_task(task).await.unwrap();
        }

        // All should be added successfully
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 5);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:44-64 function test_empty_agent_pool <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:67-100 function test_all_agents_busy
  Similarity: 85.60%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_empty_agent_pool (lines 44-64) ---[0m
    async fn test_empty_agent_pool() {
        let (config, _temp_dir) = create_minimal_config().await;
        let repo_path = std::path::PathBuf::from("/tmp/test");

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to assign task with no agents
        let task = Task::new(
            "no-agents-task".to_string(),
            "Task with no agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("No available agents"));
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_all_agents_busy (lines 67-100) ---[0m
    async fn test_all_agents_busy() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create busy agents
        for i in 0..3 {
            let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/busy-{}", i),
                ClaudeConfig::for_agent(&format!("busy-{}", i)),
                crate::agent::IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            agent.status = AgentStatus::Working;
            master.agents.insert(format!("busy-agent-{}", i), agent);
        }

        // Try to assign task
        let task = Task::new(
            "busy-task".to_string(),
            "Task for busy agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:217-255 function test_cyclic_task_dependencies <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:391-411 function test_orchestrator_state_recovery
  Similarity: 78.24%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_cyclic_task_dependencies (lines 217-255) ---[0m
    async fn test_cyclic_task_dependencies() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with cyclic dependencies
        let task1 = Task::new(
            "cycle-1".to_string(),
            "Task 1".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-3".to_string());

        let task2 = Task::new(
            "cycle-2".to_string(),
            "Task 2".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-1".to_string());

        let task3 = Task::new(
            "cycle-3".to_string(),
            "Task 3".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-2".to_string());

        // Should handle without stack overflow
        master.add_task(task1).await.unwrap();
        master.add_task(task2).await.unwrap();
        master.add_task(task3).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_orchestrator_state_recovery (lines 391-411) ---[0m
    async fn test_orchestrator_state_recovery() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Simulate error state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Error("Test error".to_string());
        }

        // Should be able to recover
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:217-255 function test_cyclic_task_dependencies <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:368-388 function test_nil_task_priority
  Similarity: 75.58%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_cyclic_task_dependencies (lines 217-255) ---[0m
    async fn test_cyclic_task_dependencies() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with cyclic dependencies
        let task1 = Task::new(
            "cycle-1".to_string(),
            "Task 1".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-3".to_string());

        let task2 = Task::new(
            "cycle-2".to_string(),
            "Task 2".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-1".to_string());

        let task3 = Task::new(
            "cycle-3".to_string(),
            "Task 3".to_string(),
            Priority::High,
            TaskType::Development,
        )
        .with_parent_task("cycle-2".to_string());

        // Should handle without stack overflow
        master.add_task(task1).await.unwrap();
        master.add_task(task2).await.unwrap();
        master.add_task(task3).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_nil_task_priority (lines 368-388) ---[0m
    async fn test_nil_task_priority() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with same priority
        for i in 0..5 {
            let task = Task::new(
                format!("same-priority-{}", i),
                "Same priority task".to_string(),
                Priority::Medium,
                TaskType::Development,
            );
            master.add_task(task).await.unwrap();
        }

        // All should be added successfully
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 5);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:9-41 function create_minimal_config <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:44-64 function test_empty_agent_pool
  Similarity: 83.13%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:create_minimal_config (lines 9-41) ---[0m
    async fn create_minimal_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Create minimal config manually
        let config = CcswarmConfig {
            project: crate::config::ProjectConfig {
                name: "Test Project".to_string(),
                repository: crate::config::RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: crate::config::MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: crate::config::ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: false,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents: std::collections::HashMap::new(),
            coordination: crate::config::CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };
        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_empty_agent_pool (lines 44-64) ---[0m
    async fn test_empty_agent_pool() {
        let (config, _temp_dir) = create_minimal_config().await;
        let repo_path = std::path::PathBuf::from("/tmp/test");

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to assign task with no agents
        let task = Task::new(
            "no-agents-task".to_string(),
            "Task with no agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("No available agents"));
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:44-64 function test_empty_agent_pool <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:414-447 function test_invalid_agent_specialization
  Similarity: 79.52%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_empty_agent_pool (lines 44-64) ---[0m
    async fn test_empty_agent_pool() {
        let (config, _temp_dir) = create_minimal_config().await;
        let repo_path = std::path::PathBuf::from("/tmp/test");

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to assign task with no agents
        let task = Task::new(
            "no-agents-task".to_string(),
            "Task with no agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("No available agents"));
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_agent_specialization (lines 414-447) ---[0m
    async fn test_invalid_agent_specialization() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent with frontend role
        let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/unknown",
            ClaudeConfig::for_agent("unknown-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Agent has frontend role, but we'll test task assignment for a role that doesn't match
        agent.status = AgentStatus::Available;

        master.agents.insert("unknown-agent".to_string(), agent);

        // Task assignment should still work (fall back to any available)
        let task = Task::new(
            "unknown-task".to_string(),
            "Task for unknown specialization".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_ok());
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:368-388 function test_nil_task_priority <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:414-447 function test_invalid_agent_specialization
  Similarity: 79.08%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_nil_task_priority (lines 368-388) ---[0m
    async fn test_nil_task_priority() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with same priority
        for i in 0..5 {
            let task = Task::new(
                format!("same-priority-{}", i),
                "Same priority task".to_string(),
                Priority::Medium,
                TaskType::Development,
            );
            master.add_task(task).await.unwrap();
        }

        // All should be added successfully
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 5);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_agent_specialization (lines 414-447) ---[0m
    async fn test_invalid_agent_specialization() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent with frontend role
        let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/unknown",
            ClaudeConfig::for_agent("unknown-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Agent has frontend role, but we'll test task assignment for a role that doesn't match
        agent.status = AgentStatus::Available;

        master.agents.insert("unknown-agent".to_string(), agent);

        // Task assignment should still work (fall back to any available)
        let task = Task::new(
            "unknown-task".to_string(),
            "Task for unknown specialization".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_ok());
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:391-411 function test_orchestrator_state_recovery <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:414-447 function test_invalid_agent_specialization
  Similarity: 79.08%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_orchestrator_state_recovery (lines 391-411) ---[0m
    async fn test_orchestrator_state_recovery() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Simulate error state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Error("Test error".to_string());
        }

        // Should be able to recover
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_invalid_agent_specialization (lines 414-447) ---[0m
    async fn test_invalid_agent_specialization() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(crate::agent::IsolationMode::GitWorktree);

        // Create agent with frontend role
        let mut agent = crate::agent::ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/unknown",
            ClaudeConfig::for_agent("unknown-agent"),
            crate::agent::IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Agent has frontend role, but we'll test task assignment for a role that doesn't match
        agent.status = AgentStatus::Available;

        master.agents.insert("unknown-agent".to_string(), agent);

        // Task assignment should still work (fall back to any available)
        let task = Task::new(
            "unknown-task".to_string(),
            "Task for unknown specialization".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_ok());
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:9-41 function create_minimal_config <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:391-411 function test_orchestrator_state_recovery
  Similarity: 78.75%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:create_minimal_config (lines 9-41) ---[0m
    async fn create_minimal_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Create minimal config manually
        let config = CcswarmConfig {
            project: crate::config::ProjectConfig {
                name: "Test Project".to_string(),
                repository: crate::config::RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: crate::config::MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: crate::config::ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: false,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents: std::collections::HashMap::new(),
            coordination: crate::config::CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };
        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_orchestrator_state_recovery (lines 391-411) ---[0m
    async fn test_orchestrator_state_recovery() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Simulate error state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Error("Test error".to_string());
        }

        // Should be able to recover
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:141-166 function test_concurrent_message_overload <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:391-411 function test_orchestrator_state_recovery
  Similarity: 89.14%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_concurrent_message_overload (lines 141-166) ---[0m
    async fn test_concurrent_message_overload() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Send many messages concurrently
        let mut handles = vec![];
        for i in 0..100 {
            let bus_clone = bus.clone();
            let handle = tokio::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id: format!("agent-{}", i),
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await
            });
            handles.push(handle);
        }

        // All should complete without panic
        for handle in handles {
            assert!(handle.await.is_ok());
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_orchestrator_state_recovery (lines 391-411) ---[0m
    async fn test_orchestrator_state_recovery() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Simulate error state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Error("Test error".to_string());
        }

        // Should be able to recover
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:9-41 function create_minimal_config <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:298-318 function test_extremely_long_task_descriptions
  Similarity: 76.07%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:create_minimal_config (lines 9-41) ---[0m
    async fn create_minimal_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Create minimal config manually
        let config = CcswarmConfig {
            project: crate::config::ProjectConfig {
                name: "Test Project".to_string(),
                repository: crate::config::RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: crate::config::MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: crate::config::ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: false,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents: std::collections::HashMap::new(),
            coordination: crate::config::CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };
        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_extremely_long_task_descriptions (lines 298-318) ---[0m
    async fn test_extremely_long_task_descriptions() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with extremely long description
        let long_description = "A".repeat(100_000); // 100k characters
        let task = Task::new(
            "long-desc".to_string(),
            long_description,
            Priority::Low,
            TaskType::Documentation,
        );

        // Should handle without memory issues
        master.add_task(task).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:141-166 function test_concurrent_message_overload <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:368-388 function test_nil_task_priority
  Similarity: 87.00%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_concurrent_message_overload (lines 141-166) ---[0m
    async fn test_concurrent_message_overload() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Send many messages concurrently
        let mut handles = vec![];
        for i in 0..100 {
            let bus_clone = bus.clone();
            let handle = tokio::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id: format!("agent-{}", i),
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await
            });
            handles.push(handle);
        }

        // All should complete without panic
        for handle in handles {
            assert!(handle.await.is_ok());
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_nil_task_priority (lines 368-388) ---[0m
    async fn test_nil_task_priority() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with same priority
        for i in 0..5 {
            let task = Task::new(
                format!("same-priority-{}", i),
                "Same priority task".to_string(),
                Priority::Medium,
                TaskType::Development,
            );
            master.add_task(task).await.unwrap();
        }

        // All should be added successfully
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 5);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:368-388 function test_nil_task_priority <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:391-411 function test_orchestrator_state_recovery
  Similarity: 95.21%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_nil_task_priority (lines 368-388) ---[0m
    async fn test_nil_task_priority() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with same priority
        for i in 0..5 {
            let task = Task::new(
                format!("same-priority-{}", i),
                "Same priority task".to_string(),
                Priority::Medium,
                TaskType::Development,
            );
            master.add_task(task).await.unwrap();
        }

        // All should be added successfully
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 5);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_orchestrator_state_recovery (lines 391-411) ---[0m
    async fn test_orchestrator_state_recovery() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Simulate error state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Error("Test error".to_string());
        }

        // Should be able to recover
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:141-166 function test_concurrent_message_overload <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:298-318 function test_extremely_long_task_descriptions
  Similarity: 84.98%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_concurrent_message_overload (lines 141-166) ---[0m
    async fn test_concurrent_message_overload() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Send many messages concurrently
        let mut handles = vec![];
        for i in 0..100 {
            let bus_clone = bus.clone();
            let handle = tokio::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id: format!("agent-{}", i),
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await
            });
            handles.push(handle);
        }

        // All should complete without panic
        for handle in handles {
            assert!(handle.await.is_ok());
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_extremely_long_task_descriptions (lines 298-318) ---[0m
    async fn test_extremely_long_task_descriptions() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with extremely long description
        let long_description = "A".repeat(100_000); // 100k characters
        let task = Task::new(
            "long-desc".to_string(),
            long_description,
            Priority::Low,
            TaskType::Documentation,
        );

        // Should handle without memory issues
        master.add_task(task).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:44-64 function test_empty_agent_pool <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:141-166 function test_concurrent_message_overload
  Similarity: 82.70%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_empty_agent_pool (lines 44-64) ---[0m
    async fn test_empty_agent_pool() {
        let (config, _temp_dir) = create_minimal_config().await;
        let repo_path = std::path::PathBuf::from("/tmp/test");

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to assign task with no agents
        let task = Task::new(
            "no-agents-task".to_string(),
            "Task with no agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("No available agents"));
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_concurrent_message_overload (lines 141-166) ---[0m
    async fn test_concurrent_message_overload() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Send many messages concurrently
        let mut handles = vec![];
        for i in 0..100 {
            let bus_clone = bus.clone();
            let handle = tokio::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id: format!("agent-{}", i),
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await
            });
            handles.push(handle);
        }

        // All should complete without panic
        for handle in handles {
            assert!(handle.await.is_ok());
        }
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:298-318 function test_extremely_long_task_descriptions <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:391-411 function test_orchestrator_state_recovery
  Similarity: 87.89%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_extremely_long_task_descriptions (lines 298-318) ---[0m
    async fn test_extremely_long_task_descriptions() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with extremely long description
        let long_description = "A".repeat(100_000); // 100k characters
        let task = Task::new(
            "long-desc".to_string(),
            long_description,
            Priority::Low,
            TaskType::Documentation,
        );

        // Should handle without memory issues
        master.add_task(task).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_orchestrator_state_recovery (lines 391-411) ---[0m
    async fn test_orchestrator_state_recovery() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Simulate error state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Error("Test error".to_string());
        }

        // Should be able to recover
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:298-318 function test_extremely_long_task_descriptions <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:368-388 function test_nil_task_priority
  Similarity: 85.78%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_extremely_long_task_descriptions (lines 298-318) ---[0m
    async fn test_extremely_long_task_descriptions() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with extremely long description
        let long_description = "A".repeat(100_000); // 100k characters
        let task = Task::new(
            "long-desc".to_string(),
            long_description,
            Priority::Low,
            TaskType::Documentation,
        );

        // Should handle without memory issues
        master.add_task(task).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_nil_task_priority (lines 368-388) ---[0m
    async fn test_nil_task_priority() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with same priority
        for i in 0..5 {
            let task = Task::new(
                format!("same-priority-{}", i),
                "Same priority task".to_string(),
                Priority::Medium,
                TaskType::Development,
            );
            master.add_task(task).await.unwrap();
        }

        // All should be added successfully
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 5);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:44-64 function test_empty_agent_pool <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:391-411 function test_orchestrator_state_recovery
  Similarity: 83.84%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_empty_agent_pool (lines 44-64) ---[0m
    async fn test_empty_agent_pool() {
        let (config, _temp_dir) = create_minimal_config().await;
        let repo_path = std::path::PathBuf::from("/tmp/test");

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to assign task with no agents
        let task = Task::new(
            "no-agents-task".to_string(),
            "Task with no agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("No available agents"));
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_orchestrator_state_recovery (lines 391-411) ---[0m
    async fn test_orchestrator_state_recovery() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Simulate error state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Error("Test error".to_string());
        }

        // Should be able to recover
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
        }

        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::Running);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:44-64 function test_empty_agent_pool <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:368-388 function test_nil_task_priority
  Similarity: 83.74%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_empty_agent_pool (lines 44-64) ---[0m
    async fn test_empty_agent_pool() {
        let (config, _temp_dir) = create_minimal_config().await;
        let repo_path = std::path::PathBuf::from("/tmp/test");

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to assign task with no agents
        let task = Task::new(
            "no-agents-task".to_string(),
            "Task with no agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("No available agents"));
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_nil_task_priority (lines 368-388) ---[0m
    async fn test_nil_task_priority() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create tasks with same priority
        for i in 0..5 {
            let task = Task::new(
                format!("same-priority-{}", i),
                "Same priority task".to_string(),
                Priority::Medium,
                TaskType::Development,
            );
            master.add_task(task).await.unwrap();
        }

        // All should be added successfully
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 5);
    }

  ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:44-64 function test_empty_agent_pool <-> ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:298-318 function test_extremely_long_task_descriptions
  Similarity: 83.61%

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_empty_agent_pool (lines 44-64) ---[0m
    async fn test_empty_agent_pool() {
        let (config, _temp_dir) = create_minimal_config().await;
        let repo_path = std::path::PathBuf::from("/tmp/test");

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Try to assign task with no agents
        let task = Task::new(
            "no-agents-task".to_string(),
            "Task with no agents".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = master.select_optimal_agent(&task).await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("No available agents"));
    }

[36m--- ./crates/ccswarm/src/orchestrator/edge_case_tests.rs:test_extremely_long_task_descriptions (lines 298-318) ---[0m
    async fn test_extremely_long_task_descriptions() {
        let (config, temp_dir) = create_minimal_config().await;
        let repo_path = temp_dir.path().to_path_buf();

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create task with extremely long description
        let long_description = "A".repeat(100_000); // 100k characters
        let task = Task::new(
            "long-desc".to_string(),
            long_description,
            Priority::Low,
            TaskType::Documentation,
        );

        // Should handle without memory issues
        master.add_task(task).await.unwrap();

        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
    }


Duplicates in ./crates/ccswarm/src/config/mod.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/config/mod.rs:96-137 method for_agent <-> ./crates/ccswarm/src/config/mod.rs:332-364 function test_config_serialization
  Similarity: 89.91%

[36m--- ./crates/ccswarm/src/config/mod.rs:for_agent (lines 96-137) ---[0m
    pub fn for_agent(role: &str) -> Self {
        let think_mode = match role {
            "frontend" | "backend" => Some(ThinkMode::ThinkHard),
            "devops" => Some(ThinkMode::Think),
            "qa" => Some(ThinkMode::ThinkHard),
            _ => Some(ThinkMode::Think),
        };

        let custom_commands = match role {
            "frontend" => vec![
                "npm test".to_string(),
                "npm run lint".to_string(),
                "npm run build".to_string(),
            ],
            "backend" => vec![
                "npm test".to_string(),
                "npm run migrate".to_string(),
                "npm run api-test".to_string(),
            ],
            "devops" => vec![
                "terraform plan".to_string(),
                "kubectl get pods".to_string(),
                "docker build".to_string(),
            ],
            "qa" => vec![
                "npm test".to_string(),
                "npm run e2e".to_string(),
                "npm run coverage".to_string(),
            ],
            _ => Vec::new(),
        };

        Self {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: true,
            think_mode,
            json_output: true,
            custom_commands,
            mcp_servers: HashMap::new(),
            use_real_api: false,
        }
    }

[36m--- ./crates/ccswarm/src/config/mod.rs:test_config_serialization (lines 332-364) ---[0m
    fn test_config_serialization() {
        let config = CcswarmConfig {
            project: ProjectConfig {
                name: "Test Project".to_string(),
                repository: RepositoryConfig {
                    url: "https://github.com/test/repo".to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.9,
                    think_mode: ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents: HashMap::new(),
            coordination: CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };

        let serialized = serde_json::to_string(&config).unwrap();
        let deserialized: CcswarmConfig = serde_json::from_str(&serialized).unwrap();

        assert_eq!(config.project.name, deserialized.project.name);
    }

  ./crates/ccswarm/src/config/mod.rs:63-73 method default <-> ./crates/ccswarm/src/config/mod.rs:78-93 method for_master
  Similarity: 96.23%
  Classes: Default <-> ClaudeConfig

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 63-73) ---[0m
    fn default() -> Self {
        Self {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: true,
            think_mode: Some(ThinkMode::Think),
            json_output: true,
            custom_commands: Vec::new(),
            mcp_servers: HashMap::new(),
            use_real_api: false,
        }
    }

[36m--- ./crates/ccswarm/src/config/mod.rs:for_master (lines 78-93) ---[0m
    pub fn for_master() -> Self {
        Self {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: true,
            think_mode: Some(ThinkMode::UltraThink),
            json_output: true,
            custom_commands: vec![
                "ccswarm status".to_string(),
                "ccswarm review".to_string(),
                "ccswarm deploy".to_string(),
                "ccswarm quality-gate".to_string(),
            ],
            mcp_servers: HashMap::new(),
            use_real_api: false,
        }
    }

  ./crates/ccswarm/src/config/mod.rs:78-93 method for_master <-> ./crates/ccswarm/src/config/mod.rs:228-239 method default
  Similarity: 87.28%
  Classes: ClaudeConfig <-> Default

[36m--- ./crates/ccswarm/src/config/mod.rs:for_master (lines 78-93) ---[0m
    pub fn for_master() -> Self {
        Self {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: true,
            think_mode: Some(ThinkMode::UltraThink),
            json_output: true,
            custom_commands: vec![
                "ccswarm status".to_string(),
                "ccswarm review".to_string(),
                "ccswarm deploy".to_string(),
                "ccswarm quality-gate".to_string(),
            ],
            mcp_servers: HashMap::new(),
            use_real_api: false,
        }
    }

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 228-239) ---[0m
    fn default() -> Self {
        Self {
            role: "master".to_string(),
            quality_threshold: 0.85,
            think_mode: ThinkMode::Think,
            permission_level: "standard".to_string(),
            claude_config: ClaudeConfig::default(),
            enable_proactive_mode: default_proactive_mode(),
            proactive_frequency: default_proactive_frequency(),
            high_frequency: default_high_frequency(),
        }
    }

  ./crates/ccswarm/src/config/mod.rs:63-73 method default <-> ./crates/ccswarm/src/config/mod.rs:228-239 method default
  Similarity: 84.32%
  Classes: Default <-> Default

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 63-73) ---[0m
    fn default() -> Self {
        Self {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: true,
            think_mode: Some(ThinkMode::Think),
            json_output: true,
            custom_commands: Vec::new(),
            mcp_servers: HashMap::new(),
            use_real_api: false,
        }
    }

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 228-239) ---[0m
    fn default() -> Self {
        Self {
            role: "master".to_string(),
            quality_threshold: 0.85,
            think_mode: ThinkMode::Think,
            permission_level: "standard".to_string(),
            claude_config: ClaudeConfig::default(),
            enable_proactive_mode: default_proactive_mode(),
            proactive_frequency: default_proactive_frequency(),
            high_frequency: default_high_frequency(),
        }
    }

  ./crates/ccswarm/src/config/mod.rs:228-239 method default <-> ./crates/ccswarm/src/config/mod.rs:252-259 method default
  Similarity: 82.84%
  Classes: Default <-> Default

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 228-239) ---[0m
    fn default() -> Self {
        Self {
            role: "master".to_string(),
            quality_threshold: 0.85,
            think_mode: ThinkMode::Think,
            permission_level: "standard".to_string(),
            claude_config: ClaudeConfig::default(),
            enable_proactive_mode: default_proactive_mode(),
            proactive_frequency: default_proactive_frequency(),
            high_frequency: default_high_frequency(),
        }
    }

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 252-259) ---[0m
    fn default() -> Self {
        Self {
            communication_method: "json".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_task_completion".to_string(),
            master_review_trigger: "auto".to_string(),
        }
    }

  ./crates/ccswarm/src/config/mod.rs:63-73 method default <-> ./crates/ccswarm/src/config/mod.rs:252-259 method default
  Similarity: 80.39%
  Classes: Default <-> Default

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 63-73) ---[0m
    fn default() -> Self {
        Self {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: true,
            think_mode: Some(ThinkMode::Think),
            json_output: true,
            custom_commands: Vec::new(),
            mcp_servers: HashMap::new(),
            use_real_api: false,
        }
    }

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 252-259) ---[0m
    fn default() -> Self {
        Self {
            communication_method: "json".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_task_completion".to_string(),
            master_review_trigger: "auto".to_string(),
        }
    }

  ./crates/ccswarm/src/config/mod.rs:168-174 method default <-> ./crates/ccswarm/src/config/mod.rs:228-239 method default
  Similarity: 77.50%
  Classes: Default <-> Default

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 168-174) ---[0m
    fn default() -> Self {
        Self {
            name: "default-project".to_string(),
            repository: RepositoryConfig::default(),
            master_claude: MasterClaudeConfig::default(),
        }
    }

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 228-239) ---[0m
    fn default() -> Self {
        Self {
            role: "master".to_string(),
            quality_threshold: 0.85,
            think_mode: ThinkMode::Think,
            permission_level: "standard".to_string(),
            claude_config: ClaudeConfig::default(),
            enable_proactive_mode: default_proactive_mode(),
            proactive_frequency: default_proactive_frequency(),
            high_frequency: default_high_frequency(),
        }
    }

  ./crates/ccswarm/src/config/mod.rs:63-73 method default <-> ./crates/ccswarm/src/config/mod.rs:168-174 method default
  Similarity: 77.89%
  Classes: Default <-> Default

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 63-73) ---[0m
    fn default() -> Self {
        Self {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: true,
            think_mode: Some(ThinkMode::Think),
            json_output: true,
            custom_commands: Vec::new(),
            mcp_servers: HashMap::new(),
            use_real_api: false,
        }
    }

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 168-174) ---[0m
    fn default() -> Self {
        Self {
            name: "default-project".to_string(),
            repository: RepositoryConfig::default(),
            master_claude: MasterClaudeConfig::default(),
        }
    }

  ./crates/ccswarm/src/config/mod.rs:17-25 method to_prompt_suffix <-> ./crates/ccswarm/src/config/mod.rs:185-190 method default
  Similarity: 88.82%
  Classes: ThinkMode <-> Default

[36m--- ./crates/ccswarm/src/config/mod.rs:to_prompt_suffix (lines 17-25) ---[0m
    pub fn to_prompt_suffix(&self) -> &str {
        match self {
            ThinkMode::Think => "think",
            ThinkMode::ThinkHard => "think hard",
            ThinkMode::ThinkHarder => "think harder",
            ThinkMode::UltraThink => "ultrathink",
            ThinkMode::MegaThink => "megathink",
        }
    }

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 185-190) ---[0m
    fn default() -> Self {
        Self {
            url: "https://github.com/example/repo.git".to_string(),
            main_branch: "main".to_string(),
        }
    }

  ./crates/ccswarm/src/config/mod.rs:168-174 method default <-> ./crates/ccswarm/src/config/mod.rs:252-259 method default
  Similarity: 87.68%
  Classes: Default <-> Default

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 168-174) ---[0m
    fn default() -> Self {
        Self {
            name: "default-project".to_string(),
            repository: RepositoryConfig::default(),
            master_claude: MasterClaudeConfig::default(),
        }
    }

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 252-259) ---[0m
    fn default() -> Self {
        Self {
            communication_method: "json".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_task_completion".to_string(),
            master_review_trigger: "auto".to_string(),
        }
    }

  ./crates/ccswarm/src/config/mod.rs:298-303 function test_claude_config_defaults <-> ./crates/ccswarm/src/config/mod.rs:306-313 function test_master_config
  Similarity: 89.14%

[36m--- ./crates/ccswarm/src/config/mod.rs:test_claude_config_defaults (lines 298-303) ---[0m
    fn test_claude_config_defaults() {
        let config = ClaudeConfig::default();
        assert_eq!(config.model, "claude-3.5-sonnet");
        assert!(config.dangerous_skip);
        assert!(config.json_output);
    }

[36m--- ./crates/ccswarm/src/config/mod.rs:test_master_config (lines 306-313) ---[0m
    fn test_master_config() {
        let config = ClaudeConfig::for_master();
        assert_eq!(config.think_mode, Some(ThinkMode::UltraThink));
        assert!(config.dangerous_skip);
        assert!(config
            .custom_commands
            .contains(&"ccswarm status".to_string()));
    }

  ./crates/ccswarm/src/config/mod.rs:185-190 method default <-> ./crates/ccswarm/src/config/mod.rs:252-259 method default
  Similarity: 87.14%
  Classes: Default <-> Default

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 185-190) ---[0m
    fn default() -> Self {
        Self {
            url: "https://github.com/example/repo.git".to_string(),
            main_branch: "main".to_string(),
        }
    }

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 252-259) ---[0m
    fn default() -> Self {
        Self {
            communication_method: "json".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_task_completion".to_string(),
            master_review_trigger: "auto".to_string(),
        }
    }

  ./crates/ccswarm/src/config/mod.rs:228-239 method default <-> ./crates/ccswarm/src/config/mod.rs:280-284 method to_file
  Similarity: 70.11%
  Classes: Default <-> CcswarmConfig

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 228-239) ---[0m
    fn default() -> Self {
        Self {
            role: "master".to_string(),
            quality_threshold: 0.85,
            think_mode: ThinkMode::Think,
            permission_level: "standard".to_string(),
            claude_config: ClaudeConfig::default(),
            enable_proactive_mode: default_proactive_mode(),
            proactive_frequency: default_proactive_frequency(),
            high_frequency: default_high_frequency(),
        }
    }

[36m--- ./crates/ccswarm/src/config/mod.rs:to_file (lines 280-284) ---[0m
    pub async fn to_file(&self, path: PathBuf) -> anyhow::Result<()> {
        let contents = serde_json::to_string_pretty(self)?;
        tokio::fs::write(path, contents).await?;
        Ok(())
    }

  ./crates/ccswarm/src/config/mod.rs:17-25 method to_prompt_suffix <-> ./crates/ccswarm/src/config/mod.rs:168-174 method default
  Similarity: 71.18%
  Classes: ThinkMode <-> Default

[36m--- ./crates/ccswarm/src/config/mod.rs:to_prompt_suffix (lines 17-25) ---[0m
    pub fn to_prompt_suffix(&self) -> &str {
        match self {
            ThinkMode::Think => "think",
            ThinkMode::ThinkHard => "think hard",
            ThinkMode::ThinkHarder => "think harder",
            ThinkMode::UltraThink => "ultrathink",
            ThinkMode::MegaThink => "megathink",
        }
    }

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 168-174) ---[0m
    fn default() -> Self {
        Self {
            name: "default-project".to_string(),
            repository: RepositoryConfig::default(),
            master_claude: MasterClaudeConfig::default(),
        }
    }

  ./crates/ccswarm/src/config/mod.rs:168-174 method default <-> ./crates/ccswarm/src/config/mod.rs:185-190 method default
  Similarity: 87.56%
  Classes: Default <-> Default

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 168-174) ---[0m
    fn default() -> Self {
        Self {
            name: "default-project".to_string(),
            repository: RepositoryConfig::default(),
            master_claude: MasterClaudeConfig::default(),
        }
    }

[36m--- ./crates/ccswarm/src/config/mod.rs:default (lines 185-190) ---[0m
    fn default() -> Self {
        Self {
            url: "https://github.com/example/repo.git".to_string(),
            main_branch: "main".to_string(),
        }
    }

  ./crates/ccswarm/src/config/mod.rs:280-284 method to_file <-> ./crates/ccswarm/src/config/mod.rs:306-313 function test_master_config
  Similarity: 70.14%

[36m--- ./crates/ccswarm/src/config/mod.rs:to_file (lines 280-284) ---[0m
    pub async fn to_file(&self, path: PathBuf) -> anyhow::Result<()> {
        let contents = serde_json::to_string_pretty(self)?;
        tokio::fs::write(path, contents).await?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/config/mod.rs:test_master_config (lines 306-313) ---[0m
    fn test_master_config() {
        let config = ClaudeConfig::for_master();
        assert_eq!(config.think_mode, Some(ThinkMode::UltraThink));
        assert!(config.dangerous_skip);
        assert!(config
            .custom_commands
            .contains(&"ccswarm status".to_string()));
    }

  ./crates/ccswarm/src/config/mod.rs:273-277 method from_file <-> ./crates/ccswarm/src/config/mod.rs:280-284 method to_file
  Similarity: 74.90%
  Classes: CcswarmConfig <-> CcswarmConfig

[36m--- ./crates/ccswarm/src/config/mod.rs:from_file (lines 273-277) ---[0m
    pub async fn from_file(path: PathBuf) -> anyhow::Result<Self> {
        let contents = tokio::fs::read_to_string(path).await?;
        let config: Self = serde_json::from_str(&contents)?;
        Ok(config)
    }

[36m--- ./crates/ccswarm/src/config/mod.rs:to_file (lines 280-284) ---[0m
    pub async fn to_file(&self, path: PathBuf) -> anyhow::Result<()> {
        let contents = serde_json::to_string_pretty(self)?;
        tokio::fs::write(path, contents).await?;
        Ok(())
    }


Duplicates in ./crates/ccswarm/src/tui/app.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/tui/app.rs:445-546 method load_agents <-> ./crates/ccswarm/src/tui/app.rs:1188-1262 method analyze_task_content
  Similarity: 95.83%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:load_agents (lines 445-546) ---[0m
    async fn load_agents(&mut self) -> Result<()> {
        let statuses = self.status_tracker.get_all_statuses().await?;

        self.agents.clear();

        // Always add Master Claude Code agent
        let master_agent = AgentInfo {
            id: "master-claude-code".to_string(),
            name: "master".to_string(),
            specialization: "Master Claude Code".to_string(),
            provider_type: "claude_code".to_string(),
            provider_icon: "👑".to_string(),
            provider_color: "gold".to_string(),
            status: AgentStatus::Available,
            current_task: None,
            tasks_completed: 0,
            last_activity: Utc::now(),
            workspace: "/workspace".to_string(),
        };
        self.agents.push(master_agent);

        // Add other default agents
        let default_agents = vec![
            ("qa-agent", "qa", "QA Specialist"),
            ("devops-agent", "devops", "DevOps Specialist"),
            ("test-agent", "test", "Test Specialist"),
            ("error-agent", "error", "Error Handler"),
            ("backend-agent", "backend", "Backend Specialist"),
            ("frontend-agent", "frontend", "Frontend Specialist"),
        ];

        for (id, name, spec) in default_agents {
            let agent = AgentInfo {
                id: id.to_string(),
                name: name.to_string(),
                specialization: spec.to_string(),
                provider_type: "claude_code".to_string(),
                provider_icon: "🤖".to_string(),
                provider_color: "blue".to_string(),
                status: AgentStatus::Available,
                current_task: None,
                tasks_completed: 0,
                last_activity: Utc::now(),
                workspace: "Unknown".to_string(),
            };
            self.agents.push(agent);
        }

        // Load dynamic agents from coordination system
        for status in statuses.iter() {
            if let (Some(agent_id), Some(status_val)) = (
                status.get("agent_id").and_then(|v| v.as_str()),
                status.get("status").and_then(|v| v.as_str()),
            ) {
                // Skip if already exists in default agents
                if self.agents.iter().any(|a| a.id == agent_id) {
                    continue;
                }

                let agent_info = AgentInfo {
                    id: agent_id.to_string(),
                    name: agent_id.split('-').next().unwrap_or("Unknown").to_string(),
                    specialization: status
                        .get("specialization")
                        .and_then(|v| v.as_str())
                        .unwrap_or("Unknown")
                        .to_string(),
                    provider_type: "claude_code".to_string(),
                    provider_icon: "🤖".to_string(),
                    provider_color: "blue".to_string(),
                    status: parse_agent_status(status_val),
                    current_task: status
                        .get("current_task")
                        .and_then(|v| v.as_str())
                        .map(|s| s.to_string()),
                    tasks_completed: status
                        .get("tasks_completed")
                        .and_then(|v| v.as_u64())
                        .unwrap_or(0) as u32,
                    last_activity: status
                        .get("timestamp")
                        .and_then(|v| v.as_str())
                        .and_then(|s| DateTime::parse_from_rfc3339(s).ok())
                        .map(|dt| dt.with_timezone(&Utc))
                        .unwrap_or_else(Utc::now),
                    workspace: status
                        .get("workspace")
                        .and_then(|v| v.as_str())
                        .unwrap_or("Unknown")
                        .to_string(),
                };
                self.agents.push(agent_info);
            }
        }

        // Ensure selection is valid
        if self.selected_agent >= self.agents.len() {
            self.selected_agent = 0;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:analyze_task_content (lines 1188-1262) ---[0m
    fn analyze_task_content(&self, task_description: &str) -> (String, f64, String) {
        let desc_lower = task_description.to_lowercase();

        // Frontend keywords
        if desc_lower.contains("ui")
            || desc_lower.contains("html")
            || desc_lower.contains("css")
            || desc_lower.contains("javascript")
            || desc_lower.contains("component")
            || desc_lower.contains("react")
            || desc_lower.contains("vue")
            || desc_lower.contains("frontend")
        {
            return (
                "Frontend".to_string(),
                0.9,
                "Contains UI/frontend keywords".to_string(),
            );
        }

        // Backend keywords
        if desc_lower.contains("api")
            || desc_lower.contains("server")
            || desc_lower.contains("database")
            || desc_lower.contains("backend")
            || desc_lower.contains("endpoint")
            || desc_lower.contains("node")
            || desc_lower.contains("express")
            || desc_lower.contains("rest")
        {
            return (
                "Backend".to_string(),
                0.9,
                "Contains API/backend keywords".to_string(),
            );
        }

        // Testing keywords
        if desc_lower.contains("test")
            || desc_lower.contains("testing")
            || desc_lower.contains("qa")
            || desc_lower.contains("quality")
            || desc_lower.contains("validation")
            || desc_lower.contains("unit")
        {
            return (
                "QA".to_string(),
                0.9,
                "Contains testing/QA keywords".to_string(),
            );
        }

        // Infrastructure keywords
        if desc_lower.contains("deploy")
            || desc_lower.contains("ci/cd")
            || desc_lower.contains("docker")
            || desc_lower.contains("infrastructure")
            || desc_lower.contains("pipeline")
            || desc_lower.contains("build")
            || desc_lower.contains("devops")
        {
            return (
                "DevOps".to_string(),
                0.9,
                "Contains infrastructure/DevOps keywords".to_string(),
            );
        }

        // Default to backend for general development
        (
            "Backend".to_string(),
            0.6,
            "General development task, defaulting to backend".to_string(),
        )
    }

  ./crates/ccswarm/src/tui/app.rs:445-546 method load_agents <-> ./crates/ccswarm/src/tui/app.rs:783-854 method execute_command
  Similarity: 89.46%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:load_agents (lines 445-546) ---[0m
    async fn load_agents(&mut self) -> Result<()> {
        let statuses = self.status_tracker.get_all_statuses().await?;

        self.agents.clear();

        // Always add Master Claude Code agent
        let master_agent = AgentInfo {
            id: "master-claude-code".to_string(),
            name: "master".to_string(),
            specialization: "Master Claude Code".to_string(),
            provider_type: "claude_code".to_string(),
            provider_icon: "👑".to_string(),
            provider_color: "gold".to_string(),
            status: AgentStatus::Available,
            current_task: None,
            tasks_completed: 0,
            last_activity: Utc::now(),
            workspace: "/workspace".to_string(),
        };
        self.agents.push(master_agent);

        // Add other default agents
        let default_agents = vec![
            ("qa-agent", "qa", "QA Specialist"),
            ("devops-agent", "devops", "DevOps Specialist"),
            ("test-agent", "test", "Test Specialist"),
            ("error-agent", "error", "Error Handler"),
            ("backend-agent", "backend", "Backend Specialist"),
            ("frontend-agent", "frontend", "Frontend Specialist"),
        ];

        for (id, name, spec) in default_agents {
            let agent = AgentInfo {
                id: id.to_string(),
                name: name.to_string(),
                specialization: spec.to_string(),
                provider_type: "claude_code".to_string(),
                provider_icon: "🤖".to_string(),
                provider_color: "blue".to_string(),
                status: AgentStatus::Available,
                current_task: None,
                tasks_completed: 0,
                last_activity: Utc::now(),
                workspace: "Unknown".to_string(),
            };
            self.agents.push(agent);
        }

        // Load dynamic agents from coordination system
        for status in statuses.iter() {
            if let (Some(agent_id), Some(status_val)) = (
                status.get("agent_id").and_then(|v| v.as_str()),
                status.get("status").and_then(|v| v.as_str()),
            ) {
                // Skip if already exists in default agents
                if self.agents.iter().any(|a| a.id == agent_id) {
                    continue;
                }

                let agent_info = AgentInfo {
                    id: agent_id.to_string(),
                    name: agent_id.split('-').next().unwrap_or("Unknown").to_string(),
                    specialization: status
                        .get("specialization")
                        .and_then(|v| v.as_str())
                        .unwrap_or("Unknown")
                        .to_string(),
                    provider_type: "claude_code".to_string(),
                    provider_icon: "🤖".to_string(),
                    provider_color: "blue".to_string(),
                    status: parse_agent_status(status_val),
                    current_task: status
                        .get("current_task")
                        .and_then(|v| v.as_str())
                        .map(|s| s.to_string()),
                    tasks_completed: status
                        .get("tasks_completed")
                        .and_then(|v| v.as_u64())
                        .unwrap_or(0) as u32,
                    last_activity: status
                        .get("timestamp")
                        .and_then(|v| v.as_str())
                        .and_then(|s| DateTime::parse_from_rfc3339(s).ok())
                        .map(|dt| dt.with_timezone(&Utc))
                        .unwrap_or_else(Utc::now),
                    workspace: status
                        .get("workspace")
                        .and_then(|v| v.as_str())
                        .unwrap_or("Unknown")
                        .to_string(),
                };
                self.agents.push(agent_info);
            }
        }

        // Ensure selection is valid
        if self.selected_agent >= self.agents.len() {
            self.selected_agent = 0;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_command (lines 783-854) ---[0m
    async fn execute_command(&mut self, command: &str) -> Result<()> {
        let parts: Vec<&str> = command.split_whitespace().collect();
        if parts.is_empty() {
            return Ok(());
        }

        let cmd = parts[0].to_lowercase();
        let args = &parts[1..];

        match cmd.as_str() {
            "help" => self.show_help().await,
            "status" => self.show_detailed_status().await?,
            "agents" => self.list_agents_command().await?,
            "tasks" => self.list_tasks_command().await?,
            "task" => {
                if !args.is_empty() {
                    let full_desc = args.join(" ");
                    self.execute_add_task(&full_desc).await?;
                } else {
                    self.add_log("System", "Usage: task <description>").await;
                }
            }
            "agent" => {
                if let Some(agent_type) = args.first() {
                    self.execute_create_agent(agent_type).await?;
                } else {
                    self.add_log("System", "Usage: agent <type>").await;
                }
            }
            "start_agent" | "activate" => {
                if let Some(agent_id) = args.first() {
                    self.start_agent_by_id(agent_id).await?;
                } else {
                    self.add_log("System", "Usage: start_agent <agent_id|agent_name>")
                        .await;
                }
            }
            "start" => self.start_orchestrator().await?,
            "stop" => self.stop_orchestrator().await?,
            "refresh" => {
                self.refresh_data().await?;
                self.add_log("System", "Data refreshed").await;
            }
            "clear" => {
                self.logs.clear();
                self.add_log("System", "Logs cleared").await;
            }
            "worktree" => {
                if args.is_empty() {
                    self.list_worktrees().await?;
                } else {
                    match args[0] {
                        "list" => self.list_worktrees().await?,
                        "prune" => self.prune_worktrees().await?,
                        _ => self.add_log("System", "Usage: worktree [list|prune]").await,
                    }
                }
            }
            _ => {
                self.add_log(
                    "System",
                    &format!(
                        "Unknown command: {}. Type 'help' for available commands.",
                        cmd
                    ),
                )
                .await;
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:445-546 method load_agents <-> ./crates/ccswarm/src/tui/app.rs:881-922 method parse_task_description
  Similarity: 94.69%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:load_agents (lines 445-546) ---[0m
    async fn load_agents(&mut self) -> Result<()> {
        let statuses = self.status_tracker.get_all_statuses().await?;

        self.agents.clear();

        // Always add Master Claude Code agent
        let master_agent = AgentInfo {
            id: "master-claude-code".to_string(),
            name: "master".to_string(),
            specialization: "Master Claude Code".to_string(),
            provider_type: "claude_code".to_string(),
            provider_icon: "👑".to_string(),
            provider_color: "gold".to_string(),
            status: AgentStatus::Available,
            current_task: None,
            tasks_completed: 0,
            last_activity: Utc::now(),
            workspace: "/workspace".to_string(),
        };
        self.agents.push(master_agent);

        // Add other default agents
        let default_agents = vec![
            ("qa-agent", "qa", "QA Specialist"),
            ("devops-agent", "devops", "DevOps Specialist"),
            ("test-agent", "test", "Test Specialist"),
            ("error-agent", "error", "Error Handler"),
            ("backend-agent", "backend", "Backend Specialist"),
            ("frontend-agent", "frontend", "Frontend Specialist"),
        ];

        for (id, name, spec) in default_agents {
            let agent = AgentInfo {
                id: id.to_string(),
                name: name.to_string(),
                specialization: spec.to_string(),
                provider_type: "claude_code".to_string(),
                provider_icon: "🤖".to_string(),
                provider_color: "blue".to_string(),
                status: AgentStatus::Available,
                current_task: None,
                tasks_completed: 0,
                last_activity: Utc::now(),
                workspace: "Unknown".to_string(),
            };
            self.agents.push(agent);
        }

        // Load dynamic agents from coordination system
        for status in statuses.iter() {
            if let (Some(agent_id), Some(status_val)) = (
                status.get("agent_id").and_then(|v| v.as_str()),
                status.get("status").and_then(|v| v.as_str()),
            ) {
                // Skip if already exists in default agents
                if self.agents.iter().any(|a| a.id == agent_id) {
                    continue;
                }

                let agent_info = AgentInfo {
                    id: agent_id.to_string(),
                    name: agent_id.split('-').next().unwrap_or("Unknown").to_string(),
                    specialization: status
                        .get("specialization")
                        .and_then(|v| v.as_str())
                        .unwrap_or("Unknown")
                        .to_string(),
                    provider_type: "claude_code".to_string(),
                    provider_icon: "🤖".to_string(),
                    provider_color: "blue".to_string(),
                    status: parse_agent_status(status_val),
                    current_task: status
                        .get("current_task")
                        .and_then(|v| v.as_str())
                        .map(|s| s.to_string()),
                    tasks_completed: status
                        .get("tasks_completed")
                        .and_then(|v| v.as_u64())
                        .unwrap_or(0) as u32,
                    last_activity: status
                        .get("timestamp")
                        .and_then(|v| v.as_str())
                        .and_then(|s| DateTime::parse_from_rfc3339(s).ok())
                        .map(|dt| dt.with_timezone(&Utc))
                        .unwrap_or_else(Utc::now),
                    workspace: status
                        .get("workspace")
                        .and_then(|v| v.as_str())
                        .unwrap_or("Unknown")
                        .to_string(),
                };
                self.agents.push(agent_info);
            }
        }

        // Ensure selection is valid
        if self.selected_agent >= self.agents.len() {
            self.selected_agent = 0;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:parse_task_description (lines 881-922) ---[0m
    fn parse_task_description(
        &self,
        description: &str,
    ) -> (String, crate::agent::Priority, crate::agent::TaskType) {
        use crate::agent::{Priority, TaskType};

        let mut desc = description.to_string();
        let mut priority = Priority::Medium;
        let mut task_type = TaskType::Development;

        // Extract priority
        if desc.contains("[high]") || desc.contains("[urgent]") {
            priority = Priority::High;
            desc = desc.replace("[high]", "").replace("[urgent]", "");
        } else if desc.contains("[low]") {
            priority = Priority::Low;
            desc = desc.replace("[low]", "");
        } else if desc.contains("[critical]") {
            priority = Priority::Critical;
            desc = desc.replace("[critical]", "");
        }

        // Extract task type
        if desc.contains("[test]") || desc.contains("[testing]") {
            task_type = TaskType::Testing;
            desc = desc.replace("[test]", "").replace("[testing]", "");
        } else if desc.contains("[docs]") || desc.contains("[documentation]") {
            task_type = TaskType::Documentation;
            desc = desc.replace("[docs]", "").replace("[documentation]", "");
        } else if desc.contains("[infra]") || desc.contains("[infrastructure]") {
            task_type = TaskType::Infrastructure;
            desc = desc.replace("[infra]", "").replace("[infrastructure]", "");
        } else if desc.contains("[bug]") || desc.contains("[bugfix]") {
            task_type = TaskType::Bugfix;
            desc = desc.replace("[bug]", "").replace("[bugfix]", "");
        } else if desc.contains("[feature]") {
            task_type = TaskType::Feature;
            desc = desc.replace("[feature]", "");
        }

        (desc.trim().to_string(), priority, task_type)
    }

  ./crates/ccswarm/src/tui/app.rs:783-854 method execute_command <-> ./crates/ccswarm/src/tui/app.rs:1188-1262 method analyze_task_content
  Similarity: 88.76%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_command (lines 783-854) ---[0m
    async fn execute_command(&mut self, command: &str) -> Result<()> {
        let parts: Vec<&str> = command.split_whitespace().collect();
        if parts.is_empty() {
            return Ok(());
        }

        let cmd = parts[0].to_lowercase();
        let args = &parts[1..];

        match cmd.as_str() {
            "help" => self.show_help().await,
            "status" => self.show_detailed_status().await?,
            "agents" => self.list_agents_command().await?,
            "tasks" => self.list_tasks_command().await?,
            "task" => {
                if !args.is_empty() {
                    let full_desc = args.join(" ");
                    self.execute_add_task(&full_desc).await?;
                } else {
                    self.add_log("System", "Usage: task <description>").await;
                }
            }
            "agent" => {
                if let Some(agent_type) = args.first() {
                    self.execute_create_agent(agent_type).await?;
                } else {
                    self.add_log("System", "Usage: agent <type>").await;
                }
            }
            "start_agent" | "activate" => {
                if let Some(agent_id) = args.first() {
                    self.start_agent_by_id(agent_id).await?;
                } else {
                    self.add_log("System", "Usage: start_agent <agent_id|agent_name>")
                        .await;
                }
            }
            "start" => self.start_orchestrator().await?,
            "stop" => self.stop_orchestrator().await?,
            "refresh" => {
                self.refresh_data().await?;
                self.add_log("System", "Data refreshed").await;
            }
            "clear" => {
                self.logs.clear();
                self.add_log("System", "Logs cleared").await;
            }
            "worktree" => {
                if args.is_empty() {
                    self.list_worktrees().await?;
                } else {
                    match args[0] {
                        "list" => self.list_worktrees().await?,
                        "prune" => self.prune_worktrees().await?,
                        _ => self.add_log("System", "Usage: worktree [list|prune]").await,
                    }
                }
            }
            _ => {
                self.add_log(
                    "System",
                    &format!(
                        "Unknown command: {}. Type 'help' for available commands.",
                        cmd
                    ),
                )
                .await;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:analyze_task_content (lines 1188-1262) ---[0m
    fn analyze_task_content(&self, task_description: &str) -> (String, f64, String) {
        let desc_lower = task_description.to_lowercase();

        // Frontend keywords
        if desc_lower.contains("ui")
            || desc_lower.contains("html")
            || desc_lower.contains("css")
            || desc_lower.contains("javascript")
            || desc_lower.contains("component")
            || desc_lower.contains("react")
            || desc_lower.contains("vue")
            || desc_lower.contains("frontend")
        {
            return (
                "Frontend".to_string(),
                0.9,
                "Contains UI/frontend keywords".to_string(),
            );
        }

        // Backend keywords
        if desc_lower.contains("api")
            || desc_lower.contains("server")
            || desc_lower.contains("database")
            || desc_lower.contains("backend")
            || desc_lower.contains("endpoint")
            || desc_lower.contains("node")
            || desc_lower.contains("express")
            || desc_lower.contains("rest")
        {
            return (
                "Backend".to_string(),
                0.9,
                "Contains API/backend keywords".to_string(),
            );
        }

        // Testing keywords
        if desc_lower.contains("test")
            || desc_lower.contains("testing")
            || desc_lower.contains("qa")
            || desc_lower.contains("quality")
            || desc_lower.contains("validation")
            || desc_lower.contains("unit")
        {
            return (
                "QA".to_string(),
                0.9,
                "Contains testing/QA keywords".to_string(),
            );
        }

        // Infrastructure keywords
        if desc_lower.contains("deploy")
            || desc_lower.contains("ci/cd")
            || desc_lower.contains("docker")
            || desc_lower.contains("infrastructure")
            || desc_lower.contains("pipeline")
            || desc_lower.contains("build")
            || desc_lower.contains("devops")
        {
            return (
                "DevOps".to_string(),
                0.9,
                "Contains infrastructure/DevOps keywords".to_string(),
            );
        }

        // Default to backend for general development
        (
            "Backend".to_string(),
            0.6,
            "General development task, defaulting to backend".to_string(),
        )
    }

  ./crates/ccswarm/src/tui/app.rs:549-603 method load_tasks <-> ./crates/ccswarm/src/tui/app.rs:1188-1262 method analyze_task_content
  Similarity: 91.52%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:load_tasks (lines 549-603) ---[0m
    async fn load_tasks(&mut self) -> Result<()> {
        self.tasks.clear();

        // If execution engine is available, use it for real-time task data
        if let Some(ref execution_engine) = self.execution_engine {
            let executor = execution_engine.get_executor();
            let task_queue = executor.get_task_queue();
            let all_tasks = task_queue.list_tasks(None, None).await;

            for queued_task in all_tasks {
                let status_str = match &queued_task.status {
                    TaskStatus::Pending => "⏳ Pending",
                    TaskStatus::Assigned { .. } => "📋 Assigned",
                    TaskStatus::InProgress { .. } => "🏃 In Progress",
                    TaskStatus::Completed { .. } => "✅ Completed",
                    TaskStatus::Failed { .. } => "❌ Failed",
                    TaskStatus::Cancelled { .. } => "🚫 Cancelled",
                };

                let task_info = TaskInfo {
                    id: queued_task.task.id.clone(),
                    description: queued_task.task.description.clone(),
                    priority: format!("{:?}", queued_task.task.priority),
                    task_type: format!("{}", queued_task.task.task_type), // Use Display trait
                    status: status_str.to_string(),
                    assigned_agent: queued_task.assigned_agent,
                    created_at: queued_task.created_at,
                };
                self.tasks.push(task_info);
            }
        } else {
            // Fall back to original task queue
            let pending_tasks = self.task_queue.get_pending_tasks().await?;

            for task in pending_tasks {
                let task_info = TaskInfo {
                    id: task.id.clone(),
                    description: task.description.clone(),
                    priority: format!("{:?}", task.priority),
                    task_type: format!("{}", task.task_type),
                    status: "Pending".to_string(),
                    assigned_agent: None,
                    created_at: Utc::now(), // TODO: Get actual creation time
                };
                self.tasks.push(task_info);
            }
        }

        // Ensure selection is valid
        if self.selected_task >= self.tasks.len() {
            self.selected_task = 0;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:analyze_task_content (lines 1188-1262) ---[0m
    fn analyze_task_content(&self, task_description: &str) -> (String, f64, String) {
        let desc_lower = task_description.to_lowercase();

        // Frontend keywords
        if desc_lower.contains("ui")
            || desc_lower.contains("html")
            || desc_lower.contains("css")
            || desc_lower.contains("javascript")
            || desc_lower.contains("component")
            || desc_lower.contains("react")
            || desc_lower.contains("vue")
            || desc_lower.contains("frontend")
        {
            return (
                "Frontend".to_string(),
                0.9,
                "Contains UI/frontend keywords".to_string(),
            );
        }

        // Backend keywords
        if desc_lower.contains("api")
            || desc_lower.contains("server")
            || desc_lower.contains("database")
            || desc_lower.contains("backend")
            || desc_lower.contains("endpoint")
            || desc_lower.contains("node")
            || desc_lower.contains("express")
            || desc_lower.contains("rest")
        {
            return (
                "Backend".to_string(),
                0.9,
                "Contains API/backend keywords".to_string(),
            );
        }

        // Testing keywords
        if desc_lower.contains("test")
            || desc_lower.contains("testing")
            || desc_lower.contains("qa")
            || desc_lower.contains("quality")
            || desc_lower.contains("validation")
            || desc_lower.contains("unit")
        {
            return (
                "QA".to_string(),
                0.9,
                "Contains testing/QA keywords".to_string(),
            );
        }

        // Infrastructure keywords
        if desc_lower.contains("deploy")
            || desc_lower.contains("ci/cd")
            || desc_lower.contains("docker")
            || desc_lower.contains("infrastructure")
            || desc_lower.contains("pipeline")
            || desc_lower.contains("build")
            || desc_lower.contains("devops")
        {
            return (
                "DevOps".to_string(),
                0.9,
                "Contains infrastructure/DevOps keywords".to_string(),
            );
        }

        // Default to backend for general development
        (
            "Backend".to_string(),
            0.6,
            "General development task, defaulting to backend".to_string(),
        )
    }

  ./crates/ccswarm/src/tui/app.rs:549-603 method load_tasks <-> ./crates/ccswarm/src/tui/app.rs:783-854 method execute_command
  Similarity: 93.63%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:load_tasks (lines 549-603) ---[0m
    async fn load_tasks(&mut self) -> Result<()> {
        self.tasks.clear();

        // If execution engine is available, use it for real-time task data
        if let Some(ref execution_engine) = self.execution_engine {
            let executor = execution_engine.get_executor();
            let task_queue = executor.get_task_queue();
            let all_tasks = task_queue.list_tasks(None, None).await;

            for queued_task in all_tasks {
                let status_str = match &queued_task.status {
                    TaskStatus::Pending => "⏳ Pending",
                    TaskStatus::Assigned { .. } => "📋 Assigned",
                    TaskStatus::InProgress { .. } => "🏃 In Progress",
                    TaskStatus::Completed { .. } => "✅ Completed",
                    TaskStatus::Failed { .. } => "❌ Failed",
                    TaskStatus::Cancelled { .. } => "🚫 Cancelled",
                };

                let task_info = TaskInfo {
                    id: queued_task.task.id.clone(),
                    description: queued_task.task.description.clone(),
                    priority: format!("{:?}", queued_task.task.priority),
                    task_type: format!("{}", queued_task.task.task_type), // Use Display trait
                    status: status_str.to_string(),
                    assigned_agent: queued_task.assigned_agent,
                    created_at: queued_task.created_at,
                };
                self.tasks.push(task_info);
            }
        } else {
            // Fall back to original task queue
            let pending_tasks = self.task_queue.get_pending_tasks().await?;

            for task in pending_tasks {
                let task_info = TaskInfo {
                    id: task.id.clone(),
                    description: task.description.clone(),
                    priority: format!("{:?}", task.priority),
                    task_type: format!("{}", task.task_type),
                    status: "Pending".to_string(),
                    assigned_agent: None,
                    created_at: Utc::now(), // TODO: Get actual creation time
                };
                self.tasks.push(task_info);
            }
        }

        // Ensure selection is valid
        if self.selected_task >= self.tasks.len() {
            self.selected_task = 0;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_command (lines 783-854) ---[0m
    async fn execute_command(&mut self, command: &str) -> Result<()> {
        let parts: Vec<&str> = command.split_whitespace().collect();
        if parts.is_empty() {
            return Ok(());
        }

        let cmd = parts[0].to_lowercase();
        let args = &parts[1..];

        match cmd.as_str() {
            "help" => self.show_help().await,
            "status" => self.show_detailed_status().await?,
            "agents" => self.list_agents_command().await?,
            "tasks" => self.list_tasks_command().await?,
            "task" => {
                if !args.is_empty() {
                    let full_desc = args.join(" ");
                    self.execute_add_task(&full_desc).await?;
                } else {
                    self.add_log("System", "Usage: task <description>").await;
                }
            }
            "agent" => {
                if let Some(agent_type) = args.first() {
                    self.execute_create_agent(agent_type).await?;
                } else {
                    self.add_log("System", "Usage: agent <type>").await;
                }
            }
            "start_agent" | "activate" => {
                if let Some(agent_id) = args.first() {
                    self.start_agent_by_id(agent_id).await?;
                } else {
                    self.add_log("System", "Usage: start_agent <agent_id|agent_name>")
                        .await;
                }
            }
            "start" => self.start_orchestrator().await?,
            "stop" => self.stop_orchestrator().await?,
            "refresh" => {
                self.refresh_data().await?;
                self.add_log("System", "Data refreshed").await;
            }
            "clear" => {
                self.logs.clear();
                self.add_log("System", "Logs cleared").await;
            }
            "worktree" => {
                if args.is_empty() {
                    self.list_worktrees().await?;
                } else {
                    match args[0] {
                        "list" => self.list_worktrees().await?,
                        "prune" => self.prune_worktrees().await?,
                        _ => self.add_log("System", "Usage: worktree [list|prune]").await,
                    }
                }
            }
            _ => {
                self.add_log(
                    "System",
                    &format!(
                        "Unknown command: {}. Type 'help' for available commands.",
                        cmd
                    ),
                )
                .await;
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:1120-1185 method delegate_task_to_agent <-> ./crates/ccswarm/src/tui/app.rs:1188-1262 method analyze_task_content
  Similarity: 82.67%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:delegate_task_to_agent (lines 1120-1185) ---[0m
    async fn delegate_task_to_agent(&mut self, input: &str) -> Result<()> {
        // Parse input as "agent_type task_description"
        let parts: Vec<&str> = input.splitn(2, ' ').collect();
        if parts.len() < 2 {
            self.add_log("Master", "Usage: <agent_type> <task_description>")
                .await;
            return Ok(());
        }

        let agent_type = parts[0];
        let task_description = parts[1];

        // Validate agent type
        let valid_agents = ["frontend", "backend", "devops", "qa"];
        if !valid_agents.contains(&agent_type) {
            self.add_log(
                "Master",
                &format!(
                    "Invalid agent type: {}. Valid agents: {}",
                    agent_type,
                    valid_agents.join(", ")
                ),
            )
            .await;
            return Ok(());
        }

        self.add_log(
            "Master",
            &format!(
                "🎯 Delegating task to {} agent: '{}'",
                agent_type, task_description
            ),
        )
        .await;

        // Create and add task to queue
        let task = crate::agent::Task::new(
            uuid::Uuid::new_v4().to_string(),
            task_description.to_string(),
            crate::agent::Priority::Medium,
            crate::agent::TaskType::Development,
        );

        self.task_queue.add_task(&task).await?;

        // Add delegation decision for tracking
        let delegation_info = DelegationInfo {
            task_description: task_description.to_string(),
            recommended_agent: agent_type.to_string(),
            confidence: 1.0, // Manual delegation is 100% confident
            reasoning: "Manual delegation by Master".to_string(),
            created_at: chrono::Utc::now(),
        };

        self.delegation_decisions.push(delegation_info);

        self.add_log(
            "Master",
            &format!("✅ Task delegated to {} agent successfully", agent_type),
        )
        .await;
        self.refresh_data().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:analyze_task_content (lines 1188-1262) ---[0m
    fn analyze_task_content(&self, task_description: &str) -> (String, f64, String) {
        let desc_lower = task_description.to_lowercase();

        // Frontend keywords
        if desc_lower.contains("ui")
            || desc_lower.contains("html")
            || desc_lower.contains("css")
            || desc_lower.contains("javascript")
            || desc_lower.contains("component")
            || desc_lower.contains("react")
            || desc_lower.contains("vue")
            || desc_lower.contains("frontend")
        {
            return (
                "Frontend".to_string(),
                0.9,
                "Contains UI/frontend keywords".to_string(),
            );
        }

        // Backend keywords
        if desc_lower.contains("api")
            || desc_lower.contains("server")
            || desc_lower.contains("database")
            || desc_lower.contains("backend")
            || desc_lower.contains("endpoint")
            || desc_lower.contains("node")
            || desc_lower.contains("express")
            || desc_lower.contains("rest")
        {
            return (
                "Backend".to_string(),
                0.9,
                "Contains API/backend keywords".to_string(),
            );
        }

        // Testing keywords
        if desc_lower.contains("test")
            || desc_lower.contains("testing")
            || desc_lower.contains("qa")
            || desc_lower.contains("quality")
            || desc_lower.contains("validation")
            || desc_lower.contains("unit")
        {
            return (
                "QA".to_string(),
                0.9,
                "Contains testing/QA keywords".to_string(),
            );
        }

        // Infrastructure keywords
        if desc_lower.contains("deploy")
            || desc_lower.contains("ci/cd")
            || desc_lower.contains("docker")
            || desc_lower.contains("infrastructure")
            || desc_lower.contains("pipeline")
            || desc_lower.contains("build")
            || desc_lower.contains("devops")
        {
            return (
                "DevOps".to_string(),
                0.9,
                "Contains infrastructure/DevOps keywords".to_string(),
            );
        }

        // Default to backend for general development
        (
            "Backend".to_string(),
            0.6,
            "General development task, defaulting to backend".to_string(),
        )
    }

  ./crates/ccswarm/src/tui/app.rs:606-653 method update_system_stats <-> ./crates/ccswarm/src/tui/app.rs:783-854 method execute_command
  Similarity: 93.50%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:update_system_stats (lines 606-653) ---[0m
    async fn update_system_stats(&mut self) -> Result<()> {
        self.total_agents = self.agents.len();
        self.active_agents = self
            .agents
            .iter()
            .filter(|a| matches!(a.status, AgentStatus::Available | AgentStatus::Working))
            .count();

        // Update task counts from execution engine if available
        if let Some(ref execution_engine) = self.execution_engine {
            let executor = execution_engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            self.pending_tasks = queue_stats.pending_count + queue_stats.active_count;
            self.completed_tasks = execution_stats.tasks_succeeded;
            self.tasks_executed = execution_stats.tasks_executed;
            self.tasks_failed = execution_stats.tasks_failed;
            self.orchestration_usage = execution_stats.orchestration_usage;

            // Calculate success rate
            self.success_rate = if execution_stats.tasks_executed > 0 {
                (execution_stats.tasks_succeeded as f64 / execution_stats.tasks_executed as f64)
                    * 100.0
            } else {
                100.0
            };

            self.system_status = if self.active_agents > 0 {
                format!("Running ({:.1}% success)", self.success_rate)
            } else {
                "Stopped".to_string()
            };
        } else {
            // Fall back to original calculation
            self.pending_tasks = self.tasks.len();
            self.completed_tasks =
                self.agents.iter().map(|a| a.tasks_completed).sum::<u32>() as usize;

            self.system_status = if self.active_agents > 0 {
                "Running".to_string()
            } else {
                "Stopped".to_string()
            };
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_command (lines 783-854) ---[0m
    async fn execute_command(&mut self, command: &str) -> Result<()> {
        let parts: Vec<&str> = command.split_whitespace().collect();
        if parts.is_empty() {
            return Ok(());
        }

        let cmd = parts[0].to_lowercase();
        let args = &parts[1..];

        match cmd.as_str() {
            "help" => self.show_help().await,
            "status" => self.show_detailed_status().await?,
            "agents" => self.list_agents_command().await?,
            "tasks" => self.list_tasks_command().await?,
            "task" => {
                if !args.is_empty() {
                    let full_desc = args.join(" ");
                    self.execute_add_task(&full_desc).await?;
                } else {
                    self.add_log("System", "Usage: task <description>").await;
                }
            }
            "agent" => {
                if let Some(agent_type) = args.first() {
                    self.execute_create_agent(agent_type).await?;
                } else {
                    self.add_log("System", "Usage: agent <type>").await;
                }
            }
            "start_agent" | "activate" => {
                if let Some(agent_id) = args.first() {
                    self.start_agent_by_id(agent_id).await?;
                } else {
                    self.add_log("System", "Usage: start_agent <agent_id|agent_name>")
                        .await;
                }
            }
            "start" => self.start_orchestrator().await?,
            "stop" => self.stop_orchestrator().await?,
            "refresh" => {
                self.refresh_data().await?;
                self.add_log("System", "Data refreshed").await;
            }
            "clear" => {
                self.logs.clear();
                self.add_log("System", "Logs cleared").await;
            }
            "worktree" => {
                if args.is_empty() {
                    self.list_worktrees().await?;
                } else {
                    match args[0] {
                        "list" => self.list_worktrees().await?,
                        "prune" => self.prune_worktrees().await?,
                        _ => self.add_log("System", "Usage: worktree [list|prune]").await,
                    }
                }
            }
            _ => {
                self.add_log(
                    "System",
                    &format!(
                        "Unknown command: {}. Type 'help' for available commands.",
                        cmd
                    ),
                )
                .await;
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:881-922 method parse_task_description <-> ./crates/ccswarm/src/tui/app.rs:1188-1262 method analyze_task_content
  Similarity: 94.03%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:parse_task_description (lines 881-922) ---[0m
    fn parse_task_description(
        &self,
        description: &str,
    ) -> (String, crate::agent::Priority, crate::agent::TaskType) {
        use crate::agent::{Priority, TaskType};

        let mut desc = description.to_string();
        let mut priority = Priority::Medium;
        let mut task_type = TaskType::Development;

        // Extract priority
        if desc.contains("[high]") || desc.contains("[urgent]") {
            priority = Priority::High;
            desc = desc.replace("[high]", "").replace("[urgent]", "");
        } else if desc.contains("[low]") {
            priority = Priority::Low;
            desc = desc.replace("[low]", "");
        } else if desc.contains("[critical]") {
            priority = Priority::Critical;
            desc = desc.replace("[critical]", "");
        }

        // Extract task type
        if desc.contains("[test]") || desc.contains("[testing]") {
            task_type = TaskType::Testing;
            desc = desc.replace("[test]", "").replace("[testing]", "");
        } else if desc.contains("[docs]") || desc.contains("[documentation]") {
            task_type = TaskType::Documentation;
            desc = desc.replace("[docs]", "").replace("[documentation]", "");
        } else if desc.contains("[infra]") || desc.contains("[infrastructure]") {
            task_type = TaskType::Infrastructure;
            desc = desc.replace("[infra]", "").replace("[infrastructure]", "");
        } else if desc.contains("[bug]") || desc.contains("[bugfix]") {
            task_type = TaskType::Bugfix;
            desc = desc.replace("[bug]", "").replace("[bugfix]", "");
        } else if desc.contains("[feature]") {
            task_type = TaskType::Feature;
            desc = desc.replace("[feature]", "");
        }

        (desc.trim().to_string(), priority, task_type)
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:analyze_task_content (lines 1188-1262) ---[0m
    fn analyze_task_content(&self, task_description: &str) -> (String, f64, String) {
        let desc_lower = task_description.to_lowercase();

        // Frontend keywords
        if desc_lower.contains("ui")
            || desc_lower.contains("html")
            || desc_lower.contains("css")
            || desc_lower.contains("javascript")
            || desc_lower.contains("component")
            || desc_lower.contains("react")
            || desc_lower.contains("vue")
            || desc_lower.contains("frontend")
        {
            return (
                "Frontend".to_string(),
                0.9,
                "Contains UI/frontend keywords".to_string(),
            );
        }

        // Backend keywords
        if desc_lower.contains("api")
            || desc_lower.contains("server")
            || desc_lower.contains("database")
            || desc_lower.contains("backend")
            || desc_lower.contains("endpoint")
            || desc_lower.contains("node")
            || desc_lower.contains("express")
            || desc_lower.contains("rest")
        {
            return (
                "Backend".to_string(),
                0.9,
                "Contains API/backend keywords".to_string(),
            );
        }

        // Testing keywords
        if desc_lower.contains("test")
            || desc_lower.contains("testing")
            || desc_lower.contains("qa")
            || desc_lower.contains("quality")
            || desc_lower.contains("validation")
            || desc_lower.contains("unit")
        {
            return (
                "QA".to_string(),
                0.9,
                "Contains testing/QA keywords".to_string(),
            );
        }

        // Infrastructure keywords
        if desc_lower.contains("deploy")
            || desc_lower.contains("ci/cd")
            || desc_lower.contains("docker")
            || desc_lower.contains("infrastructure")
            || desc_lower.contains("pipeline")
            || desc_lower.contains("build")
            || desc_lower.contains("devops")
        {
            return (
                "DevOps".to_string(),
                0.9,
                "Contains infrastructure/DevOps keywords".to_string(),
            );
        }

        // Default to backend for general development
        (
            "Backend".to_string(),
            0.6,
            "General development task, defaulting to backend".to_string(),
        )
    }

  ./crates/ccswarm/src/tui/app.rs:606-653 method update_system_stats <-> ./crates/ccswarm/src/tui/app.rs:1188-1262 method analyze_task_content
  Similarity: 87.99%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:update_system_stats (lines 606-653) ---[0m
    async fn update_system_stats(&mut self) -> Result<()> {
        self.total_agents = self.agents.len();
        self.active_agents = self
            .agents
            .iter()
            .filter(|a| matches!(a.status, AgentStatus::Available | AgentStatus::Working))
            .count();

        // Update task counts from execution engine if available
        if let Some(ref execution_engine) = self.execution_engine {
            let executor = execution_engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            self.pending_tasks = queue_stats.pending_count + queue_stats.active_count;
            self.completed_tasks = execution_stats.tasks_succeeded;
            self.tasks_executed = execution_stats.tasks_executed;
            self.tasks_failed = execution_stats.tasks_failed;
            self.orchestration_usage = execution_stats.orchestration_usage;

            // Calculate success rate
            self.success_rate = if execution_stats.tasks_executed > 0 {
                (execution_stats.tasks_succeeded as f64 / execution_stats.tasks_executed as f64)
                    * 100.0
            } else {
                100.0
            };

            self.system_status = if self.active_agents > 0 {
                format!("Running ({:.1}% success)", self.success_rate)
            } else {
                "Stopped".to_string()
            };
        } else {
            // Fall back to original calculation
            self.pending_tasks = self.tasks.len();
            self.completed_tasks =
                self.agents.iter().map(|a| a.tasks_completed).sum::<u32>() as usize;

            self.system_status = if self.active_agents > 0 {
                "Running".to_string()
            } else {
                "Stopped".to_string()
            };
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:analyze_task_content (lines 1188-1262) ---[0m
    fn analyze_task_content(&self, task_description: &str) -> (String, f64, String) {
        let desc_lower = task_description.to_lowercase();

        // Frontend keywords
        if desc_lower.contains("ui")
            || desc_lower.contains("html")
            || desc_lower.contains("css")
            || desc_lower.contains("javascript")
            || desc_lower.contains("component")
            || desc_lower.contains("react")
            || desc_lower.contains("vue")
            || desc_lower.contains("frontend")
        {
            return (
                "Frontend".to_string(),
                0.9,
                "Contains UI/frontend keywords".to_string(),
            );
        }

        // Backend keywords
        if desc_lower.contains("api")
            || desc_lower.contains("server")
            || desc_lower.contains("database")
            || desc_lower.contains("backend")
            || desc_lower.contains("endpoint")
            || desc_lower.contains("node")
            || desc_lower.contains("express")
            || desc_lower.contains("rest")
        {
            return (
                "Backend".to_string(),
                0.9,
                "Contains API/backend keywords".to_string(),
            );
        }

        // Testing keywords
        if desc_lower.contains("test")
            || desc_lower.contains("testing")
            || desc_lower.contains("qa")
            || desc_lower.contains("quality")
            || desc_lower.contains("validation")
            || desc_lower.contains("unit")
        {
            return (
                "QA".to_string(),
                0.9,
                "Contains testing/QA keywords".to_string(),
            );
        }

        // Infrastructure keywords
        if desc_lower.contains("deploy")
            || desc_lower.contains("ci/cd")
            || desc_lower.contains("docker")
            || desc_lower.contains("infrastructure")
            || desc_lower.contains("pipeline")
            || desc_lower.contains("build")
            || desc_lower.contains("devops")
        {
            return (
                "DevOps".to_string(),
                0.9,
                "Contains infrastructure/DevOps keywords".to_string(),
            );
        }

        // Default to backend for general development
        (
            "Backend".to_string(),
            0.6,
            "General development task, defaulting to backend".to_string(),
        )
    }

  ./crates/ccswarm/src/tui/app.rs:783-854 method execute_command <-> ./crates/ccswarm/src/tui/app.rs:881-922 method parse_task_description
  Similarity: 89.60%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_command (lines 783-854) ---[0m
    async fn execute_command(&mut self, command: &str) -> Result<()> {
        let parts: Vec<&str> = command.split_whitespace().collect();
        if parts.is_empty() {
            return Ok(());
        }

        let cmd = parts[0].to_lowercase();
        let args = &parts[1..];

        match cmd.as_str() {
            "help" => self.show_help().await,
            "status" => self.show_detailed_status().await?,
            "agents" => self.list_agents_command().await?,
            "tasks" => self.list_tasks_command().await?,
            "task" => {
                if !args.is_empty() {
                    let full_desc = args.join(" ");
                    self.execute_add_task(&full_desc).await?;
                } else {
                    self.add_log("System", "Usage: task <description>").await;
                }
            }
            "agent" => {
                if let Some(agent_type) = args.first() {
                    self.execute_create_agent(agent_type).await?;
                } else {
                    self.add_log("System", "Usage: agent <type>").await;
                }
            }
            "start_agent" | "activate" => {
                if let Some(agent_id) = args.first() {
                    self.start_agent_by_id(agent_id).await?;
                } else {
                    self.add_log("System", "Usage: start_agent <agent_id|agent_name>")
                        .await;
                }
            }
            "start" => self.start_orchestrator().await?,
            "stop" => self.stop_orchestrator().await?,
            "refresh" => {
                self.refresh_data().await?;
                self.add_log("System", "Data refreshed").await;
            }
            "clear" => {
                self.logs.clear();
                self.add_log("System", "Logs cleared").await;
            }
            "worktree" => {
                if args.is_empty() {
                    self.list_worktrees().await?;
                } else {
                    match args[0] {
                        "list" => self.list_worktrees().await?,
                        "prune" => self.prune_worktrees().await?,
                        _ => self.add_log("System", "Usage: worktree [list|prune]").await,
                    }
                }
            }
            _ => {
                self.add_log(
                    "System",
                    &format!(
                        "Unknown command: {}. Type 'help' for available commands.",
                        cmd
                    ),
                )
                .await;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:parse_task_description (lines 881-922) ---[0m
    fn parse_task_description(
        &self,
        description: &str,
    ) -> (String, crate::agent::Priority, crate::agent::TaskType) {
        use crate::agent::{Priority, TaskType};

        let mut desc = description.to_string();
        let mut priority = Priority::Medium;
        let mut task_type = TaskType::Development;

        // Extract priority
        if desc.contains("[high]") || desc.contains("[urgent]") {
            priority = Priority::High;
            desc = desc.replace("[high]", "").replace("[urgent]", "");
        } else if desc.contains("[low]") {
            priority = Priority::Low;
            desc = desc.replace("[low]", "");
        } else if desc.contains("[critical]") {
            priority = Priority::Critical;
            desc = desc.replace("[critical]", "");
        }

        // Extract task type
        if desc.contains("[test]") || desc.contains("[testing]") {
            task_type = TaskType::Testing;
            desc = desc.replace("[test]", "").replace("[testing]", "");
        } else if desc.contains("[docs]") || desc.contains("[documentation]") {
            task_type = TaskType::Documentation;
            desc = desc.replace("[docs]", "").replace("[documentation]", "");
        } else if desc.contains("[infra]") || desc.contains("[infrastructure]") {
            task_type = TaskType::Infrastructure;
            desc = desc.replace("[infra]", "").replace("[infrastructure]", "");
        } else if desc.contains("[bug]") || desc.contains("[bugfix]") {
            task_type = TaskType::Bugfix;
            desc = desc.replace("[bug]", "").replace("[bugfix]", "");
        } else if desc.contains("[feature]") {
            task_type = TaskType::Feature;
            desc = desc.replace("[feature]", "");
        }

        (desc.trim().to_string(), priority, task_type)
    }

  ./crates/ccswarm/src/tui/app.rs:783-854 method execute_command <-> ./crates/ccswarm/src/tui/app.rs:1120-1185 method delegate_task_to_agent
  Similarity: 72.42%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_command (lines 783-854) ---[0m
    async fn execute_command(&mut self, command: &str) -> Result<()> {
        let parts: Vec<&str> = command.split_whitespace().collect();
        if parts.is_empty() {
            return Ok(());
        }

        let cmd = parts[0].to_lowercase();
        let args = &parts[1..];

        match cmd.as_str() {
            "help" => self.show_help().await,
            "status" => self.show_detailed_status().await?,
            "agents" => self.list_agents_command().await?,
            "tasks" => self.list_tasks_command().await?,
            "task" => {
                if !args.is_empty() {
                    let full_desc = args.join(" ");
                    self.execute_add_task(&full_desc).await?;
                } else {
                    self.add_log("System", "Usage: task <description>").await;
                }
            }
            "agent" => {
                if let Some(agent_type) = args.first() {
                    self.execute_create_agent(agent_type).await?;
                } else {
                    self.add_log("System", "Usage: agent <type>").await;
                }
            }
            "start_agent" | "activate" => {
                if let Some(agent_id) = args.first() {
                    self.start_agent_by_id(agent_id).await?;
                } else {
                    self.add_log("System", "Usage: start_agent <agent_id|agent_name>")
                        .await;
                }
            }
            "start" => self.start_orchestrator().await?,
            "stop" => self.stop_orchestrator().await?,
            "refresh" => {
                self.refresh_data().await?;
                self.add_log("System", "Data refreshed").await;
            }
            "clear" => {
                self.logs.clear();
                self.add_log("System", "Logs cleared").await;
            }
            "worktree" => {
                if args.is_empty() {
                    self.list_worktrees().await?;
                } else {
                    match args[0] {
                        "list" => self.list_worktrees().await?,
                        "prune" => self.prune_worktrees().await?,
                        _ => self.add_log("System", "Usage: worktree [list|prune]").await,
                    }
                }
            }
            _ => {
                self.add_log(
                    "System",
                    &format!(
                        "Unknown command: {}. Type 'help' for available commands.",
                        cmd
                    ),
                )
                .await;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:delegate_task_to_agent (lines 1120-1185) ---[0m
    async fn delegate_task_to_agent(&mut self, input: &str) -> Result<()> {
        // Parse input as "agent_type task_description"
        let parts: Vec<&str> = input.splitn(2, ' ').collect();
        if parts.len() < 2 {
            self.add_log("Master", "Usage: <agent_type> <task_description>")
                .await;
            return Ok(());
        }

        let agent_type = parts[0];
        let task_description = parts[1];

        // Validate agent type
        let valid_agents = ["frontend", "backend", "devops", "qa"];
        if !valid_agents.contains(&agent_type) {
            self.add_log(
                "Master",
                &format!(
                    "Invalid agent type: {}. Valid agents: {}",
                    agent_type,
                    valid_agents.join(", ")
                ),
            )
            .await;
            return Ok(());
        }

        self.add_log(
            "Master",
            &format!(
                "🎯 Delegating task to {} agent: '{}'",
                agent_type, task_description
            ),
        )
        .await;

        // Create and add task to queue
        let task = crate::agent::Task::new(
            uuid::Uuid::new_v4().to_string(),
            task_description.to_string(),
            crate::agent::Priority::Medium,
            crate::agent::TaskType::Development,
        );

        self.task_queue.add_task(&task).await?;

        // Add delegation decision for tracking
        let delegation_info = DelegationInfo {
            task_description: task_description.to_string(),
            recommended_agent: agent_type.to_string(),
            confidence: 1.0, // Manual delegation is 100% confident
            reasoning: "Manual delegation by Master".to_string(),
            created_at: chrono::Utc::now(),
        };

        self.delegation_decisions.push(delegation_info);

        self.add_log(
            "Master",
            &format!("✅ Task delegated to {} agent successfully", agent_type),
        )
        .await;
        self.refresh_data().await?;

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:549-603 method load_tasks <-> ./crates/ccswarm/src/tui/app.rs:606-653 method update_system_stats
  Similarity: 95.58%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:load_tasks (lines 549-603) ---[0m
    async fn load_tasks(&mut self) -> Result<()> {
        self.tasks.clear();

        // If execution engine is available, use it for real-time task data
        if let Some(ref execution_engine) = self.execution_engine {
            let executor = execution_engine.get_executor();
            let task_queue = executor.get_task_queue();
            let all_tasks = task_queue.list_tasks(None, None).await;

            for queued_task in all_tasks {
                let status_str = match &queued_task.status {
                    TaskStatus::Pending => "⏳ Pending",
                    TaskStatus::Assigned { .. } => "📋 Assigned",
                    TaskStatus::InProgress { .. } => "🏃 In Progress",
                    TaskStatus::Completed { .. } => "✅ Completed",
                    TaskStatus::Failed { .. } => "❌ Failed",
                    TaskStatus::Cancelled { .. } => "🚫 Cancelled",
                };

                let task_info = TaskInfo {
                    id: queued_task.task.id.clone(),
                    description: queued_task.task.description.clone(),
                    priority: format!("{:?}", queued_task.task.priority),
                    task_type: format!("{}", queued_task.task.task_type), // Use Display trait
                    status: status_str.to_string(),
                    assigned_agent: queued_task.assigned_agent,
                    created_at: queued_task.created_at,
                };
                self.tasks.push(task_info);
            }
        } else {
            // Fall back to original task queue
            let pending_tasks = self.task_queue.get_pending_tasks().await?;

            for task in pending_tasks {
                let task_info = TaskInfo {
                    id: task.id.clone(),
                    description: task.description.clone(),
                    priority: format!("{:?}", task.priority),
                    task_type: format!("{}", task.task_type),
                    status: "Pending".to_string(),
                    assigned_agent: None,
                    created_at: Utc::now(), // TODO: Get actual creation time
                };
                self.tasks.push(task_info);
            }
        }

        // Ensure selection is valid
        if self.selected_task >= self.tasks.len() {
            self.selected_task = 0;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:update_system_stats (lines 606-653) ---[0m
    async fn update_system_stats(&mut self) -> Result<()> {
        self.total_agents = self.agents.len();
        self.active_agents = self
            .agents
            .iter()
            .filter(|a| matches!(a.status, AgentStatus::Available | AgentStatus::Working))
            .count();

        // Update task counts from execution engine if available
        if let Some(ref execution_engine) = self.execution_engine {
            let executor = execution_engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            self.pending_tasks = queue_stats.pending_count + queue_stats.active_count;
            self.completed_tasks = execution_stats.tasks_succeeded;
            self.tasks_executed = execution_stats.tasks_executed;
            self.tasks_failed = execution_stats.tasks_failed;
            self.orchestration_usage = execution_stats.orchestration_usage;

            // Calculate success rate
            self.success_rate = if execution_stats.tasks_executed > 0 {
                (execution_stats.tasks_succeeded as f64 / execution_stats.tasks_executed as f64)
                    * 100.0
            } else {
                100.0
            };

            self.system_status = if self.active_agents > 0 {
                format!("Running ({:.1}% success)", self.success_rate)
            } else {
                "Stopped".to_string()
            };
        } else {
            // Fall back to original calculation
            self.pending_tasks = self.tasks.len();
            self.completed_tasks =
                self.agents.iter().map(|a| a.tasks_completed).sum::<u32>() as usize;

            self.system_status = if self.active_agents > 0 {
                "Running".to_string()
            } else {
                "Stopped".to_string()
            };
        }

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:549-603 method load_tasks <-> ./crates/ccswarm/src/tui/app.rs:881-922 method parse_task_description
  Similarity: 92.08%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:load_tasks (lines 549-603) ---[0m
    async fn load_tasks(&mut self) -> Result<()> {
        self.tasks.clear();

        // If execution engine is available, use it for real-time task data
        if let Some(ref execution_engine) = self.execution_engine {
            let executor = execution_engine.get_executor();
            let task_queue = executor.get_task_queue();
            let all_tasks = task_queue.list_tasks(None, None).await;

            for queued_task in all_tasks {
                let status_str = match &queued_task.status {
                    TaskStatus::Pending => "⏳ Pending",
                    TaskStatus::Assigned { .. } => "📋 Assigned",
                    TaskStatus::InProgress { .. } => "🏃 In Progress",
                    TaskStatus::Completed { .. } => "✅ Completed",
                    TaskStatus::Failed { .. } => "❌ Failed",
                    TaskStatus::Cancelled { .. } => "🚫 Cancelled",
                };

                let task_info = TaskInfo {
                    id: queued_task.task.id.clone(),
                    description: queued_task.task.description.clone(),
                    priority: format!("{:?}", queued_task.task.priority),
                    task_type: format!("{}", queued_task.task.task_type), // Use Display trait
                    status: status_str.to_string(),
                    assigned_agent: queued_task.assigned_agent,
                    created_at: queued_task.created_at,
                };
                self.tasks.push(task_info);
            }
        } else {
            // Fall back to original task queue
            let pending_tasks = self.task_queue.get_pending_tasks().await?;

            for task in pending_tasks {
                let task_info = TaskInfo {
                    id: task.id.clone(),
                    description: task.description.clone(),
                    priority: format!("{:?}", task.priority),
                    task_type: format!("{}", task.task_type),
                    status: "Pending".to_string(),
                    assigned_agent: None,
                    created_at: Utc::now(), // TODO: Get actual creation time
                };
                self.tasks.push(task_info);
            }
        }

        // Ensure selection is valid
        if self.selected_task >= self.tasks.len() {
            self.selected_task = 0;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:parse_task_description (lines 881-922) ---[0m
    fn parse_task_description(
        &self,
        description: &str,
    ) -> (String, crate::agent::Priority, crate::agent::TaskType) {
        use crate::agent::{Priority, TaskType};

        let mut desc = description.to_string();
        let mut priority = Priority::Medium;
        let mut task_type = TaskType::Development;

        // Extract priority
        if desc.contains("[high]") || desc.contains("[urgent]") {
            priority = Priority::High;
            desc = desc.replace("[high]", "").replace("[urgent]", "");
        } else if desc.contains("[low]") {
            priority = Priority::Low;
            desc = desc.replace("[low]", "");
        } else if desc.contains("[critical]") {
            priority = Priority::Critical;
            desc = desc.replace("[critical]", "");
        }

        // Extract task type
        if desc.contains("[test]") || desc.contains("[testing]") {
            task_type = TaskType::Testing;
            desc = desc.replace("[test]", "").replace("[testing]", "");
        } else if desc.contains("[docs]") || desc.contains("[documentation]") {
            task_type = TaskType::Documentation;
            desc = desc.replace("[docs]", "").replace("[documentation]", "");
        } else if desc.contains("[infra]") || desc.contains("[infrastructure]") {
            task_type = TaskType::Infrastructure;
            desc = desc.replace("[infra]", "").replace("[infrastructure]", "");
        } else if desc.contains("[bug]") || desc.contains("[bugfix]") {
            task_type = TaskType::Bugfix;
            desc = desc.replace("[bug]", "").replace("[bugfix]", "");
        } else if desc.contains("[feature]") {
            task_type = TaskType::Feature;
            desc = desc.replace("[feature]", "");
        }

        (desc.trim().to_string(), priority, task_type)
    }

  ./crates/ccswarm/src/tui/app.rs:152-189 method new <-> ./crates/ccswarm/src/tui/app.rs:549-603 method load_tasks
  Similarity: 92.51%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:new (lines 152-189) ---[0m
    pub async fn new() -> Result<Self> {
        let coordination_bus = CoordinationBus::new().await?;
        let status_tracker = StatusTracker::new().await?;
        let task_queue = TaskQueue::new().await?;

        Ok(Self {
            current_tab: Tab::Overview,
            selected_agent: 0,
            selected_task: 0,
            selected_log: 0,
            selected_delegation: 0,
            agents: Vec::new(),
            tasks: Vec::new(),
            logs: Vec::new(),
            delegation_decisions: Vec::new(),
            system_status: "Starting...".to_string(),
            total_agents: 0,
            active_agents: 0,
            pending_tasks: 0,
            completed_tasks: 0,
            tasks_executed: 0,
            tasks_failed: 0,
            success_rate: 100.0,
            orchestration_usage: 0.0,
            input_mode: InputMode::Normal,
            input_buffer: String::new(),
            delegation_mode: DelegationMode::Analyze,
            delegation_input: String::new(),
            coordination_bus,
            status_tracker,
            task_queue,
            execution_engine: None, // Will be set by CLI runner
            terminal_width: 80,
            terminal_height: 24,
            last_update: Instant::now(),
            update_interval: Duration::from_millis(500), // More frequent updates for real-time feel
        })
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:load_tasks (lines 549-603) ---[0m
    async fn load_tasks(&mut self) -> Result<()> {
        self.tasks.clear();

        // If execution engine is available, use it for real-time task data
        if let Some(ref execution_engine) = self.execution_engine {
            let executor = execution_engine.get_executor();
            let task_queue = executor.get_task_queue();
            let all_tasks = task_queue.list_tasks(None, None).await;

            for queued_task in all_tasks {
                let status_str = match &queued_task.status {
                    TaskStatus::Pending => "⏳ Pending",
                    TaskStatus::Assigned { .. } => "📋 Assigned",
                    TaskStatus::InProgress { .. } => "🏃 In Progress",
                    TaskStatus::Completed { .. } => "✅ Completed",
                    TaskStatus::Failed { .. } => "❌ Failed",
                    TaskStatus::Cancelled { .. } => "🚫 Cancelled",
                };

                let task_info = TaskInfo {
                    id: queued_task.task.id.clone(),
                    description: queued_task.task.description.clone(),
                    priority: format!("{:?}", queued_task.task.priority),
                    task_type: format!("{}", queued_task.task.task_type), // Use Display trait
                    status: status_str.to_string(),
                    assigned_agent: queued_task.assigned_agent,
                    created_at: queued_task.created_at,
                };
                self.tasks.push(task_info);
            }
        } else {
            // Fall back to original task queue
            let pending_tasks = self.task_queue.get_pending_tasks().await?;

            for task in pending_tasks {
                let task_info = TaskInfo {
                    id: task.id.clone(),
                    description: task.description.clone(),
                    priority: format!("{:?}", task.priority),
                    task_type: format!("{}", task.task_type),
                    status: "Pending".to_string(),
                    assigned_agent: None,
                    created_at: Utc::now(), // TODO: Get actual creation time
                };
                self.tasks.push(task_info);
            }
        }

        // Ensure selection is valid
        if self.selected_task >= self.tasks.len() {
            self.selected_task = 0;
        }

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:881-922 method parse_task_description <-> ./crates/ccswarm/src/tui/app.rs:1120-1185 method delegate_task_to_agent
  Similarity: 74.98%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:parse_task_description (lines 881-922) ---[0m
    fn parse_task_description(
        &self,
        description: &str,
    ) -> (String, crate::agent::Priority, crate::agent::TaskType) {
        use crate::agent::{Priority, TaskType};

        let mut desc = description.to_string();
        let mut priority = Priority::Medium;
        let mut task_type = TaskType::Development;

        // Extract priority
        if desc.contains("[high]") || desc.contains("[urgent]") {
            priority = Priority::High;
            desc = desc.replace("[high]", "").replace("[urgent]", "");
        } else if desc.contains("[low]") {
            priority = Priority::Low;
            desc = desc.replace("[low]", "");
        } else if desc.contains("[critical]") {
            priority = Priority::Critical;
            desc = desc.replace("[critical]", "");
        }

        // Extract task type
        if desc.contains("[test]") || desc.contains("[testing]") {
            task_type = TaskType::Testing;
            desc = desc.replace("[test]", "").replace("[testing]", "");
        } else if desc.contains("[docs]") || desc.contains("[documentation]") {
            task_type = TaskType::Documentation;
            desc = desc.replace("[docs]", "").replace("[documentation]", "");
        } else if desc.contains("[infra]") || desc.contains("[infrastructure]") {
            task_type = TaskType::Infrastructure;
            desc = desc.replace("[infra]", "").replace("[infrastructure]", "");
        } else if desc.contains("[bug]") || desc.contains("[bugfix]") {
            task_type = TaskType::Bugfix;
            desc = desc.replace("[bug]", "").replace("[bugfix]", "");
        } else if desc.contains("[feature]") {
            task_type = TaskType::Feature;
            desc = desc.replace("[feature]", "");
        }

        (desc.trim().to_string(), priority, task_type)
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:delegate_task_to_agent (lines 1120-1185) ---[0m
    async fn delegate_task_to_agent(&mut self, input: &str) -> Result<()> {
        // Parse input as "agent_type task_description"
        let parts: Vec<&str> = input.splitn(2, ' ').collect();
        if parts.len() < 2 {
            self.add_log("Master", "Usage: <agent_type> <task_description>")
                .await;
            return Ok(());
        }

        let agent_type = parts[0];
        let task_description = parts[1];

        // Validate agent type
        let valid_agents = ["frontend", "backend", "devops", "qa"];
        if !valid_agents.contains(&agent_type) {
            self.add_log(
                "Master",
                &format!(
                    "Invalid agent type: {}. Valid agents: {}",
                    agent_type,
                    valid_agents.join(", ")
                ),
            )
            .await;
            return Ok(());
        }

        self.add_log(
            "Master",
            &format!(
                "🎯 Delegating task to {} agent: '{}'",
                agent_type, task_description
            ),
        )
        .await;

        // Create and add task to queue
        let task = crate::agent::Task::new(
            uuid::Uuid::new_v4().to_string(),
            task_description.to_string(),
            crate::agent::Priority::Medium,
            crate::agent::TaskType::Development,
        );

        self.task_queue.add_task(&task).await?;

        // Add delegation decision for tracking
        let delegation_info = DelegationInfo {
            task_description: task_description.to_string(),
            recommended_agent: agent_type.to_string(),
            confidence: 1.0, // Manual delegation is 100% confident
            reasoning: "Manual delegation by Master".to_string(),
            created_at: chrono::Utc::now(),
        };

        self.delegation_decisions.push(delegation_info);

        self.add_log(
            "Master",
            &format!("✅ Task delegated to {} agent successfully", agent_type),
        )
        .await;
        self.refresh_data().await?;

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:606-653 method update_system_stats <-> ./crates/ccswarm/src/tui/app.rs:881-922 method parse_task_description
  Similarity: 89.61%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:update_system_stats (lines 606-653) ---[0m
    async fn update_system_stats(&mut self) -> Result<()> {
        self.total_agents = self.agents.len();
        self.active_agents = self
            .agents
            .iter()
            .filter(|a| matches!(a.status, AgentStatus::Available | AgentStatus::Working))
            .count();

        // Update task counts from execution engine if available
        if let Some(ref execution_engine) = self.execution_engine {
            let executor = execution_engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            self.pending_tasks = queue_stats.pending_count + queue_stats.active_count;
            self.completed_tasks = execution_stats.tasks_succeeded;
            self.tasks_executed = execution_stats.tasks_executed;
            self.tasks_failed = execution_stats.tasks_failed;
            self.orchestration_usage = execution_stats.orchestration_usage;

            // Calculate success rate
            self.success_rate = if execution_stats.tasks_executed > 0 {
                (execution_stats.tasks_succeeded as f64 / execution_stats.tasks_executed as f64)
                    * 100.0
            } else {
                100.0
            };

            self.system_status = if self.active_agents > 0 {
                format!("Running ({:.1}% success)", self.success_rate)
            } else {
                "Stopped".to_string()
            };
        } else {
            // Fall back to original calculation
            self.pending_tasks = self.tasks.len();
            self.completed_tasks =
                self.agents.iter().map(|a| a.tasks_completed).sum::<u32>() as usize;

            self.system_status = if self.active_agents > 0 {
                "Running".to_string()
            } else {
                "Stopped".to_string()
            };
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:parse_task_description (lines 881-922) ---[0m
    fn parse_task_description(
        &self,
        description: &str,
    ) -> (String, crate::agent::Priority, crate::agent::TaskType) {
        use crate::agent::{Priority, TaskType};

        let mut desc = description.to_string();
        let mut priority = Priority::Medium;
        let mut task_type = TaskType::Development;

        // Extract priority
        if desc.contains("[high]") || desc.contains("[urgent]") {
            priority = Priority::High;
            desc = desc.replace("[high]", "").replace("[urgent]", "");
        } else if desc.contains("[low]") {
            priority = Priority::Low;
            desc = desc.replace("[low]", "");
        } else if desc.contains("[critical]") {
            priority = Priority::Critical;
            desc = desc.replace("[critical]", "");
        }

        // Extract task type
        if desc.contains("[test]") || desc.contains("[testing]") {
            task_type = TaskType::Testing;
            desc = desc.replace("[test]", "").replace("[testing]", "");
        } else if desc.contains("[docs]") || desc.contains("[documentation]") {
            task_type = TaskType::Documentation;
            desc = desc.replace("[docs]", "").replace("[documentation]", "");
        } else if desc.contains("[infra]") || desc.contains("[infrastructure]") {
            task_type = TaskType::Infrastructure;
            desc = desc.replace("[infra]", "").replace("[infrastructure]", "");
        } else if desc.contains("[bug]") || desc.contains("[bugfix]") {
            task_type = TaskType::Bugfix;
            desc = desc.replace("[bug]", "").replace("[bugfix]", "");
        } else if desc.contains("[feature]") {
            task_type = TaskType::Feature;
            desc = desc.replace("[feature]", "");
        }

        (desc.trim().to_string(), priority, task_type)
    }

  ./crates/ccswarm/src/tui/app.rs:152-189 method new <-> ./crates/ccswarm/src/tui/app.rs:606-653 method update_system_stats
  Similarity: 92.89%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:new (lines 152-189) ---[0m
    pub async fn new() -> Result<Self> {
        let coordination_bus = CoordinationBus::new().await?;
        let status_tracker = StatusTracker::new().await?;
        let task_queue = TaskQueue::new().await?;

        Ok(Self {
            current_tab: Tab::Overview,
            selected_agent: 0,
            selected_task: 0,
            selected_log: 0,
            selected_delegation: 0,
            agents: Vec::new(),
            tasks: Vec::new(),
            logs: Vec::new(),
            delegation_decisions: Vec::new(),
            system_status: "Starting...".to_string(),
            total_agents: 0,
            active_agents: 0,
            pending_tasks: 0,
            completed_tasks: 0,
            tasks_executed: 0,
            tasks_failed: 0,
            success_rate: 100.0,
            orchestration_usage: 0.0,
            input_mode: InputMode::Normal,
            input_buffer: String::new(),
            delegation_mode: DelegationMode::Analyze,
            delegation_input: String::new(),
            coordination_bus,
            status_tracker,
            task_queue,
            execution_engine: None, // Will be set by CLI runner
            terminal_width: 80,
            terminal_height: 24,
            last_update: Instant::now(),
            update_interval: Duration::from_millis(500), // More frequent updates for real-time feel
        })
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:update_system_stats (lines 606-653) ---[0m
    async fn update_system_stats(&mut self) -> Result<()> {
        self.total_agents = self.agents.len();
        self.active_agents = self
            .agents
            .iter()
            .filter(|a| matches!(a.status, AgentStatus::Available | AgentStatus::Working))
            .count();

        // Update task counts from execution engine if available
        if let Some(ref execution_engine) = self.execution_engine {
            let executor = execution_engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            self.pending_tasks = queue_stats.pending_count + queue_stats.active_count;
            self.completed_tasks = execution_stats.tasks_succeeded;
            self.tasks_executed = execution_stats.tasks_executed;
            self.tasks_failed = execution_stats.tasks_failed;
            self.orchestration_usage = execution_stats.orchestration_usage;

            // Calculate success rate
            self.success_rate = if execution_stats.tasks_executed > 0 {
                (execution_stats.tasks_succeeded as f64 / execution_stats.tasks_executed as f64)
                    * 100.0
            } else {
                100.0
            };

            self.system_status = if self.active_agents > 0 {
                format!("Running ({:.1}% success)", self.success_rate)
            } else {
                "Stopped".to_string()
            };
        } else {
            // Fall back to original calculation
            self.pending_tasks = self.tasks.len();
            self.completed_tasks =
                self.agents.iter().map(|a| a.tasks_completed).sum::<u32>() as usize;

            self.system_status = if self.active_agents > 0 {
                "Running".to_string()
            } else {
                "Stopped".to_string()
            };
        }

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:1120-1185 method delegate_task_to_agent <-> ./crates/ccswarm/src/tui/app.rs:1265-1297 method get_delegation_stats
  Similarity: 79.77%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:delegate_task_to_agent (lines 1120-1185) ---[0m
    async fn delegate_task_to_agent(&mut self, input: &str) -> Result<()> {
        // Parse input as "agent_type task_description"
        let parts: Vec<&str> = input.splitn(2, ' ').collect();
        if parts.len() < 2 {
            self.add_log("Master", "Usage: <agent_type> <task_description>")
                .await;
            return Ok(());
        }

        let agent_type = parts[0];
        let task_description = parts[1];

        // Validate agent type
        let valid_agents = ["frontend", "backend", "devops", "qa"];
        if !valid_agents.contains(&agent_type) {
            self.add_log(
                "Master",
                &format!(
                    "Invalid agent type: {}. Valid agents: {}",
                    agent_type,
                    valid_agents.join(", ")
                ),
            )
            .await;
            return Ok(());
        }

        self.add_log(
            "Master",
            &format!(
                "🎯 Delegating task to {} agent: '{}'",
                agent_type, task_description
            ),
        )
        .await;

        // Create and add task to queue
        let task = crate::agent::Task::new(
            uuid::Uuid::new_v4().to_string(),
            task_description.to_string(),
            crate::agent::Priority::Medium,
            crate::agent::TaskType::Development,
        );

        self.task_queue.add_task(&task).await?;

        // Add delegation decision for tracking
        let delegation_info = DelegationInfo {
            task_description: task_description.to_string(),
            recommended_agent: agent_type.to_string(),
            confidence: 1.0, // Manual delegation is 100% confident
            reasoning: "Manual delegation by Master".to_string(),
            created_at: chrono::Utc::now(),
        };

        self.delegation_decisions.push(delegation_info);

        self.add_log(
            "Master",
            &format!("✅ Task delegated to {} agent successfully", agent_type),
        )
        .await;
        self.refresh_data().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:get_delegation_stats (lines 1265-1297) ---[0m
    pub fn get_delegation_stats(&self) -> String {
        if self.delegation_decisions.is_empty() {
            return "No delegation decisions yet".to_string();
        }

        let total = self.delegation_decisions.len();
        let mut agent_counts = std::collections::HashMap::new();
        let mut total_confidence = 0.0;

        for decision in &self.delegation_decisions {
            *agent_counts
                .entry(decision.recommended_agent.clone())
                .or_insert(0) += 1;
            total_confidence += decision.confidence;
        }

        let avg_confidence = total_confidence / total as f64;

        let mut stats = "📊 Delegation Statistics:\n".to_string();
        stats.push_str(&format!("Total delegations: {}\n", total));
        stats.push_str(&format!(
            "Average confidence: {:.1}%\n",
            avg_confidence * 100.0
        ));
        stats.push_str("Agent distribution:\n");

        for (agent, count) in agent_counts {
            let percentage = (count as f64 / total as f64) * 100.0;
            stats.push_str(&format!("  {}: {} ({:.1}%)\n", agent, count, percentage));
        }

        stats
    }

  ./crates/ccswarm/src/tui/app.rs:313-347 method start_agent <-> ./crates/ccswarm/src/tui/app.rs:606-653 method update_system_stats
  Similarity: 94.63%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent (lines 313-347) ---[0m
    pub async fn start_agent(&mut self, agent_id: &str) -> Result<()> {
        let mut agent_info = None;

        // Find the agent and collect info
        if let Some(agent) = self.agents.iter_mut().find(|a| a.id == agent_id) {
            // Change status to Working
            agent.status = AgentStatus::Working;
            agent.last_activity = Utc::now();

            // Collect info for logging
            agent_info = Some((agent.name.clone(), agent.specialization.clone()));

            // Update system stats
            self.active_agents += 1;
        }

        // Log after borrowing ends
        if let Some((name, specialization)) = agent_info {
            self.add_log(
                "System",
                &format!("🚀 Starting agent: {} ({})", name, specialization),
            )
            .await;

            // If this is a Master agent, provide special logging
            if specialization.contains("Master") {
                self.add_log("Master", "🎯 Master Claude Code orchestrator activated")
                    .await;
                self.add_log("Master", "📋 Ready to coordinate multi-agent tasks")
                    .await;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:update_system_stats (lines 606-653) ---[0m
    async fn update_system_stats(&mut self) -> Result<()> {
        self.total_agents = self.agents.len();
        self.active_agents = self
            .agents
            .iter()
            .filter(|a| matches!(a.status, AgentStatus::Available | AgentStatus::Working))
            .count();

        // Update task counts from execution engine if available
        if let Some(ref execution_engine) = self.execution_engine {
            let executor = execution_engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            self.pending_tasks = queue_stats.pending_count + queue_stats.active_count;
            self.completed_tasks = execution_stats.tasks_succeeded;
            self.tasks_executed = execution_stats.tasks_executed;
            self.tasks_failed = execution_stats.tasks_failed;
            self.orchestration_usage = execution_stats.orchestration_usage;

            // Calculate success rate
            self.success_rate = if execution_stats.tasks_executed > 0 {
                (execution_stats.tasks_succeeded as f64 / execution_stats.tasks_executed as f64)
                    * 100.0
            } else {
                100.0
            };

            self.system_status = if self.active_agents > 0 {
                format!("Running ({:.1}% success)", self.success_rate)
            } else {
                "Stopped".to_string()
            };
        } else {
            // Fall back to original calculation
            self.pending_tasks = self.tasks.len();
            self.completed_tasks =
                self.agents.iter().map(|a| a.tasks_completed).sum::<u32>() as usize;

            self.system_status = if self.active_agents > 0 {
                "Running".to_string()
            } else {
                "Stopped".to_string()
            };
        }

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:549-603 method load_tasks <-> ./crates/ccswarm/src/tui/app.rs:925-955 method show_detailed_status
  Similarity: 87.81%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:load_tasks (lines 549-603) ---[0m
    async fn load_tasks(&mut self) -> Result<()> {
        self.tasks.clear();

        // If execution engine is available, use it for real-time task data
        if let Some(ref execution_engine) = self.execution_engine {
            let executor = execution_engine.get_executor();
            let task_queue = executor.get_task_queue();
            let all_tasks = task_queue.list_tasks(None, None).await;

            for queued_task in all_tasks {
                let status_str = match &queued_task.status {
                    TaskStatus::Pending => "⏳ Pending",
                    TaskStatus::Assigned { .. } => "📋 Assigned",
                    TaskStatus::InProgress { .. } => "🏃 In Progress",
                    TaskStatus::Completed { .. } => "✅ Completed",
                    TaskStatus::Failed { .. } => "❌ Failed",
                    TaskStatus::Cancelled { .. } => "🚫 Cancelled",
                };

                let task_info = TaskInfo {
                    id: queued_task.task.id.clone(),
                    description: queued_task.task.description.clone(),
                    priority: format!("{:?}", queued_task.task.priority),
                    task_type: format!("{}", queued_task.task.task_type), // Use Display trait
                    status: status_str.to_string(),
                    assigned_agent: queued_task.assigned_agent,
                    created_at: queued_task.created_at,
                };
                self.tasks.push(task_info);
            }
        } else {
            // Fall back to original task queue
            let pending_tasks = self.task_queue.get_pending_tasks().await?;

            for task in pending_tasks {
                let task_info = TaskInfo {
                    id: task.id.clone(),
                    description: task.description.clone(),
                    priority: format!("{:?}", task.priority),
                    task_type: format!("{}", task.task_type),
                    status: "Pending".to_string(),
                    assigned_agent: None,
                    created_at: Utc::now(), // TODO: Get actual creation time
                };
                self.tasks.push(task_info);
            }
        }

        // Ensure selection is valid
        if self.selected_task >= self.tasks.len() {
            self.selected_task = 0;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_detailed_status (lines 925-955) ---[0m
    async fn show_detailed_status(&mut self) -> Result<()> {
        self.add_log("System", "=== Detailed System Status ===")
            .await;
        self.add_log("System", &format!("System Status: {}", self.system_status))
            .await;
        self.add_log("System", &format!("Total Agents: {}", self.total_agents))
            .await;
        self.add_log("System", &format!("Active Agents: {}", self.active_agents))
            .await;
        self.add_log("System", &format!("Pending Tasks: {}", self.pending_tasks))
            .await;
        self.add_log(
            "System",
            &format!("Completed Tasks: {}", self.completed_tasks),
        )
        .await;

        // Show Master Claude Code status specifically
        if let Some(master) = self
            .agents
            .iter()
            .find(|a| a.specialization.contains("Master"))
        {
            self.add_log(
                "System",
                &format!("👑 Master Claude Code: {:?}", master.status),
            )
            .await;
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:925-955 method show_detailed_status <-> ./crates/ccswarm/src/tui/app.rs:1120-1185 method delegate_task_to_agent
  Similarity: 76.80%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_detailed_status (lines 925-955) ---[0m
    async fn show_detailed_status(&mut self) -> Result<()> {
        self.add_log("System", "=== Detailed System Status ===")
            .await;
        self.add_log("System", &format!("System Status: {}", self.system_status))
            .await;
        self.add_log("System", &format!("Total Agents: {}", self.total_agents))
            .await;
        self.add_log("System", &format!("Active Agents: {}", self.active_agents))
            .await;
        self.add_log("System", &format!("Pending Tasks: {}", self.pending_tasks))
            .await;
        self.add_log(
            "System",
            &format!("Completed Tasks: {}", self.completed_tasks),
        )
        .await;

        // Show Master Claude Code status specifically
        if let Some(master) = self
            .agents
            .iter()
            .find(|a| a.specialization.contains("Master"))
        {
            self.add_log(
                "System",
                &format!("👑 Master Claude Code: {:?}", master.status),
            )
            .await;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:delegate_task_to_agent (lines 1120-1185) ---[0m
    async fn delegate_task_to_agent(&mut self, input: &str) -> Result<()> {
        // Parse input as "agent_type task_description"
        let parts: Vec<&str> = input.splitn(2, ' ').collect();
        if parts.len() < 2 {
            self.add_log("Master", "Usage: <agent_type> <task_description>")
                .await;
            return Ok(());
        }

        let agent_type = parts[0];
        let task_description = parts[1];

        // Validate agent type
        let valid_agents = ["frontend", "backend", "devops", "qa"];
        if !valid_agents.contains(&agent_type) {
            self.add_log(
                "Master",
                &format!(
                    "Invalid agent type: {}. Valid agents: {}",
                    agent_type,
                    valid_agents.join(", ")
                ),
            )
            .await;
            return Ok(());
        }

        self.add_log(
            "Master",
            &format!(
                "🎯 Delegating task to {} agent: '{}'",
                agent_type, task_description
            ),
        )
        .await;

        // Create and add task to queue
        let task = crate::agent::Task::new(
            uuid::Uuid::new_v4().to_string(),
            task_description.to_string(),
            crate::agent::Priority::Medium,
            crate::agent::TaskType::Development,
        );

        self.task_queue.add_task(&task).await?;

        // Add delegation decision for tracking
        let delegation_info = DelegationInfo {
            task_description: task_description.to_string(),
            recommended_agent: agent_type.to_string(),
            confidence: 1.0, // Manual delegation is 100% confident
            reasoning: "Manual delegation by Master".to_string(),
            created_at: chrono::Utc::now(),
        };

        self.delegation_decisions.push(delegation_info);

        self.add_log(
            "Master",
            &format!("✅ Task delegated to {} agent successfully", agent_type),
        )
        .await;
        self.refresh_data().await?;

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:606-653 method update_system_stats <-> ./crates/ccswarm/src/tui/app.rs:710-740 method process_input
  Similarity: 89.75%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:update_system_stats (lines 606-653) ---[0m
    async fn update_system_stats(&mut self) -> Result<()> {
        self.total_agents = self.agents.len();
        self.active_agents = self
            .agents
            .iter()
            .filter(|a| matches!(a.status, AgentStatus::Available | AgentStatus::Working))
            .count();

        // Update task counts from execution engine if available
        if let Some(ref execution_engine) = self.execution_engine {
            let executor = execution_engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            self.pending_tasks = queue_stats.pending_count + queue_stats.active_count;
            self.completed_tasks = execution_stats.tasks_succeeded;
            self.tasks_executed = execution_stats.tasks_executed;
            self.tasks_failed = execution_stats.tasks_failed;
            self.orchestration_usage = execution_stats.orchestration_usage;

            // Calculate success rate
            self.success_rate = if execution_stats.tasks_executed > 0 {
                (execution_stats.tasks_succeeded as f64 / execution_stats.tasks_executed as f64)
                    * 100.0
            } else {
                100.0
            };

            self.system_status = if self.active_agents > 0 {
                format!("Running ({:.1}% success)", self.success_rate)
            } else {
                "Stopped".to_string()
            };
        } else {
            // Fall back to original calculation
            self.pending_tasks = self.tasks.len();
            self.completed_tasks =
                self.agents.iter().map(|a| a.tasks_completed).sum::<u32>() as usize;

            self.system_status = if self.active_agents > 0 {
                "Running".to_string()
            } else {
                "Stopped".to_string()
            };
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:process_input (lines 710-740) ---[0m
    pub async fn process_input(&mut self) -> Result<()> {
        let input = self.input_buffer.trim().to_string();

        match self.input_mode {
            InputMode::AddingTask => {
                if !input.is_empty() {
                    self.execute_add_task(&input).await?;
                }
            }
            InputMode::CreatingAgent => {
                if !input.is_empty() {
                    self.execute_create_agent(&input).await?;
                }
            }
            InputMode::Command => {
                if !input.is_empty() {
                    self.execute_command(&input).await?;
                }
            }
            InputMode::DelegationInput => {
                if !input.is_empty() {
                    self.execute_delegation_action(&input).await?;
                }
            }
            _ => {}
        }

        self.input_mode = InputMode::Normal;
        self.input_buffer.clear();
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:549-603 method load_tasks <-> ./crates/ccswarm/src/tui/app.rs:1265-1297 method get_delegation_stats
  Similarity: 78.55%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:load_tasks (lines 549-603) ---[0m
    async fn load_tasks(&mut self) -> Result<()> {
        self.tasks.clear();

        // If execution engine is available, use it for real-time task data
        if let Some(ref execution_engine) = self.execution_engine {
            let executor = execution_engine.get_executor();
            let task_queue = executor.get_task_queue();
            let all_tasks = task_queue.list_tasks(None, None).await;

            for queued_task in all_tasks {
                let status_str = match &queued_task.status {
                    TaskStatus::Pending => "⏳ Pending",
                    TaskStatus::Assigned { .. } => "📋 Assigned",
                    TaskStatus::InProgress { .. } => "🏃 In Progress",
                    TaskStatus::Completed { .. } => "✅ Completed",
                    TaskStatus::Failed { .. } => "❌ Failed",
                    TaskStatus::Cancelled { .. } => "🚫 Cancelled",
                };

                let task_info = TaskInfo {
                    id: queued_task.task.id.clone(),
                    description: queued_task.task.description.clone(),
                    priority: format!("{:?}", queued_task.task.priority),
                    task_type: format!("{}", queued_task.task.task_type), // Use Display trait
                    status: status_str.to_string(),
                    assigned_agent: queued_task.assigned_agent,
                    created_at: queued_task.created_at,
                };
                self.tasks.push(task_info);
            }
        } else {
            // Fall back to original task queue
            let pending_tasks = self.task_queue.get_pending_tasks().await?;

            for task in pending_tasks {
                let task_info = TaskInfo {
                    id: task.id.clone(),
                    description: task.description.clone(),
                    priority: format!("{:?}", task.priority),
                    task_type: format!("{}", task.task_type),
                    status: "Pending".to_string(),
                    assigned_agent: None,
                    created_at: Utc::now(), // TODO: Get actual creation time
                };
                self.tasks.push(task_info);
            }
        }

        // Ensure selection is valid
        if self.selected_task >= self.tasks.len() {
            self.selected_task = 0;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:get_delegation_stats (lines 1265-1297) ---[0m
    pub fn get_delegation_stats(&self) -> String {
        if self.delegation_decisions.is_empty() {
            return "No delegation decisions yet".to_string();
        }

        let total = self.delegation_decisions.len();
        let mut agent_counts = std::collections::HashMap::new();
        let mut total_confidence = 0.0;

        for decision in &self.delegation_decisions {
            *agent_counts
                .entry(decision.recommended_agent.clone())
                .or_insert(0) += 1;
            total_confidence += decision.confidence;
        }

        let avg_confidence = total_confidence / total as f64;

        let mut stats = "📊 Delegation Statistics:\n".to_string();
        stats.push_str(&format!("Total delegations: {}\n", total));
        stats.push_str(&format!(
            "Average confidence: {:.1}%\n",
            avg_confidence * 100.0
        ));
        stats.push_str("Agent distribution:\n");

        for (agent, count) in agent_counts {
            let percentage = (count as f64 / total as f64) * 100.0;
            stats.push_str(&format!("  {}: {} ({:.1}%)\n", agent, count, percentage));
        }

        stats
    }

  ./crates/ccswarm/src/tui/app.rs:281-310 method activate_selected <-> ./crates/ccswarm/src/tui/app.rs:606-653 method update_system_stats
  Similarity: 86.79%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:activate_selected (lines 281-310) ---[0m
    pub async fn activate_selected(&mut self) -> Result<()> {
        match self.current_tab {
            Tab::Agents => {
                if let Some(agent) = self.agents.get(self.selected_agent).cloned() {
                    match agent.status {
                        AgentStatus::Available => {
                            // Start the agent
                            self.start_agent(&agent.id).await?;
                        }
                        AgentStatus::Working => {
                            // Show agent details
                            self.show_agent_details(&agent.id).await?;
                        }
                        _ => {
                            // Show agent details for other statuses
                            self.show_agent_details(&agent.id).await?;
                        }
                    }
                }
            }
            Tab::Tasks => {
                if let Some(task) = self.tasks.get(self.selected_task) {
                    let task_id = task.id.clone();
                    self.show_task_details(&task_id).await?;
                }
            }
            _ => {}
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:update_system_stats (lines 606-653) ---[0m
    async fn update_system_stats(&mut self) -> Result<()> {
        self.total_agents = self.agents.len();
        self.active_agents = self
            .agents
            .iter()
            .filter(|a| matches!(a.status, AgentStatus::Available | AgentStatus::Working))
            .count();

        // Update task counts from execution engine if available
        if let Some(ref execution_engine) = self.execution_engine {
            let executor = execution_engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            self.pending_tasks = queue_stats.pending_count + queue_stats.active_count;
            self.completed_tasks = execution_stats.tasks_succeeded;
            self.tasks_executed = execution_stats.tasks_executed;
            self.tasks_failed = execution_stats.tasks_failed;
            self.orchestration_usage = execution_stats.orchestration_usage;

            // Calculate success rate
            self.success_rate = if execution_stats.tasks_executed > 0 {
                (execution_stats.tasks_succeeded as f64 / execution_stats.tasks_executed as f64)
                    * 100.0
            } else {
                100.0
            };

            self.system_status = if self.active_agents > 0 {
                format!("Running ({:.1}% success)", self.success_rate)
            } else {
                "Stopped".to_string()
            };
        } else {
            // Fall back to original calculation
            self.pending_tasks = self.tasks.len();
            self.completed_tasks =
                self.agents.iter().map(|a| a.tasks_completed).sum::<u32>() as usize;

            self.system_status = if self.active_agents > 0 {
                "Running".to_string()
            } else {
                "Stopped".to_string()
            };
        }

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:152-189 method new <-> ./crates/ccswarm/src/tui/app.rs:313-347 method start_agent
  Similarity: 89.82%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:new (lines 152-189) ---[0m
    pub async fn new() -> Result<Self> {
        let coordination_bus = CoordinationBus::new().await?;
        let status_tracker = StatusTracker::new().await?;
        let task_queue = TaskQueue::new().await?;

        Ok(Self {
            current_tab: Tab::Overview,
            selected_agent: 0,
            selected_task: 0,
            selected_log: 0,
            selected_delegation: 0,
            agents: Vec::new(),
            tasks: Vec::new(),
            logs: Vec::new(),
            delegation_decisions: Vec::new(),
            system_status: "Starting...".to_string(),
            total_agents: 0,
            active_agents: 0,
            pending_tasks: 0,
            completed_tasks: 0,
            tasks_executed: 0,
            tasks_failed: 0,
            success_rate: 100.0,
            orchestration_usage: 0.0,
            input_mode: InputMode::Normal,
            input_buffer: String::new(),
            delegation_mode: DelegationMode::Analyze,
            delegation_input: String::new(),
            coordination_bus,
            status_tracker,
            task_queue,
            execution_engine: None, // Will be set by CLI runner
            terminal_width: 80,
            terminal_height: 24,
            last_update: Instant::now(),
            update_interval: Duration::from_millis(500), // More frequent updates for real-time feel
        })
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent (lines 313-347) ---[0m
    pub async fn start_agent(&mut self, agent_id: &str) -> Result<()> {
        let mut agent_info = None;

        // Find the agent and collect info
        if let Some(agent) = self.agents.iter_mut().find(|a| a.id == agent_id) {
            // Change status to Working
            agent.status = AgentStatus::Working;
            agent.last_activity = Utc::now();

            // Collect info for logging
            agent_info = Some((agent.name.clone(), agent.specialization.clone()));

            // Update system stats
            self.active_agents += 1;
        }

        // Log after borrowing ends
        if let Some((name, specialization)) = agent_info {
            self.add_log(
                "System",
                &format!("🚀 Starting agent: {} ({})", name, specialization),
            )
            .await;

            // If this is a Master agent, provide special logging
            if specialization.contains("Master") {
                self.add_log("Master", "🎯 Master Claude Code orchestrator activated")
                    .await;
                self.add_log("Master", "📋 Ready to coordinate multi-agent tasks")
                    .await;
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:152-189 method new <-> ./crates/ccswarm/src/tui/app.rs:710-740 method process_input
  Similarity: 90.21%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:new (lines 152-189) ---[0m
    pub async fn new() -> Result<Self> {
        let coordination_bus = CoordinationBus::new().await?;
        let status_tracker = StatusTracker::new().await?;
        let task_queue = TaskQueue::new().await?;

        Ok(Self {
            current_tab: Tab::Overview,
            selected_agent: 0,
            selected_task: 0,
            selected_log: 0,
            selected_delegation: 0,
            agents: Vec::new(),
            tasks: Vec::new(),
            logs: Vec::new(),
            delegation_decisions: Vec::new(),
            system_status: "Starting...".to_string(),
            total_agents: 0,
            active_agents: 0,
            pending_tasks: 0,
            completed_tasks: 0,
            tasks_executed: 0,
            tasks_failed: 0,
            success_rate: 100.0,
            orchestration_usage: 0.0,
            input_mode: InputMode::Normal,
            input_buffer: String::new(),
            delegation_mode: DelegationMode::Analyze,
            delegation_input: String::new(),
            coordination_bus,
            status_tracker,
            task_queue,
            execution_engine: None, // Will be set by CLI runner
            terminal_width: 80,
            terminal_height: 24,
            last_update: Instant::now(),
            update_interval: Duration::from_millis(500), // More frequent updates for real-time feel
        })
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:process_input (lines 710-740) ---[0m
    pub async fn process_input(&mut self) -> Result<()> {
        let input = self.input_buffer.trim().to_string();

        match self.input_mode {
            InputMode::AddingTask => {
                if !input.is_empty() {
                    self.execute_add_task(&input).await?;
                }
            }
            InputMode::CreatingAgent => {
                if !input.is_empty() {
                    self.execute_create_agent(&input).await?;
                }
            }
            InputMode::Command => {
                if !input.is_empty() {
                    self.execute_command(&input).await?;
                }
            }
            InputMode::DelegationInput => {
                if !input.is_empty() {
                    self.execute_delegation_action(&input).await?;
                }
            }
            _ => {}
        }

        self.input_mode = InputMode::Normal;
        self.input_buffer.clear();
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:606-653 method update_system_stats <-> ./crates/ccswarm/src/tui/app.rs:925-955 method show_detailed_status
  Similarity: 78.06%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:update_system_stats (lines 606-653) ---[0m
    async fn update_system_stats(&mut self) -> Result<()> {
        self.total_agents = self.agents.len();
        self.active_agents = self
            .agents
            .iter()
            .filter(|a| matches!(a.status, AgentStatus::Available | AgentStatus::Working))
            .count();

        // Update task counts from execution engine if available
        if let Some(ref execution_engine) = self.execution_engine {
            let executor = execution_engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            self.pending_tasks = queue_stats.pending_count + queue_stats.active_count;
            self.completed_tasks = execution_stats.tasks_succeeded;
            self.tasks_executed = execution_stats.tasks_executed;
            self.tasks_failed = execution_stats.tasks_failed;
            self.orchestration_usage = execution_stats.orchestration_usage;

            // Calculate success rate
            self.success_rate = if execution_stats.tasks_executed > 0 {
                (execution_stats.tasks_succeeded as f64 / execution_stats.tasks_executed as f64)
                    * 100.0
            } else {
                100.0
            };

            self.system_status = if self.active_agents > 0 {
                format!("Running ({:.1}% success)", self.success_rate)
            } else {
                "Stopped".to_string()
            };
        } else {
            // Fall back to original calculation
            self.pending_tasks = self.tasks.len();
            self.completed_tasks =
                self.agents.iter().map(|a| a.tasks_completed).sum::<u32>() as usize;

            self.system_status = if self.active_agents > 0 {
                "Running".to_string()
            } else {
                "Stopped".to_string()
            };
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_detailed_status (lines 925-955) ---[0m
    async fn show_detailed_status(&mut self) -> Result<()> {
        self.add_log("System", "=== Detailed System Status ===")
            .await;
        self.add_log("System", &format!("System Status: {}", self.system_status))
            .await;
        self.add_log("System", &format!("Total Agents: {}", self.total_agents))
            .await;
        self.add_log("System", &format!("Active Agents: {}", self.active_agents))
            .await;
        self.add_log("System", &format!("Pending Tasks: {}", self.pending_tasks))
            .await;
        self.add_log(
            "System",
            &format!("Completed Tasks: {}", self.completed_tasks),
        )
        .await;

        // Show Master Claude Code status specifically
        if let Some(master) = self
            .agents
            .iter()
            .find(|a| a.specialization.contains("Master"))
        {
            self.add_log(
                "System",
                &format!("👑 Master Claude Code: {:?}", master.status),
            )
            .await;
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:313-347 method start_agent <-> ./crates/ccswarm/src/tui/app.rs:710-740 method process_input
  Similarity: 91.42%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent (lines 313-347) ---[0m
    pub async fn start_agent(&mut self, agent_id: &str) -> Result<()> {
        let mut agent_info = None;

        // Find the agent and collect info
        if let Some(agent) = self.agents.iter_mut().find(|a| a.id == agent_id) {
            // Change status to Working
            agent.status = AgentStatus::Working;
            agent.last_activity = Utc::now();

            // Collect info for logging
            agent_info = Some((agent.name.clone(), agent.specialization.clone()));

            // Update system stats
            self.active_agents += 1;
        }

        // Log after borrowing ends
        if let Some((name, specialization)) = agent_info {
            self.add_log(
                "System",
                &format!("🚀 Starting agent: {} ({})", name, specialization),
            )
            .await;

            // If this is a Master agent, provide special logging
            if specialization.contains("Master") {
                self.add_log("Master", "🎯 Master Claude Code orchestrator activated")
                    .await;
                self.add_log("Master", "📋 Ready to coordinate multi-agent tasks")
                    .await;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:process_input (lines 710-740) ---[0m
    pub async fn process_input(&mut self) -> Result<()> {
        let input = self.input_buffer.trim().to_string();

        match self.input_mode {
            InputMode::AddingTask => {
                if !input.is_empty() {
                    self.execute_add_task(&input).await?;
                }
            }
            InputMode::CreatingAgent => {
                if !input.is_empty() {
                    self.execute_create_agent(&input).await?;
                }
            }
            InputMode::Command => {
                if !input.is_empty() {
                    self.execute_command(&input).await?;
                }
            }
            InputMode::DelegationInput => {
                if !input.is_empty() {
                    self.execute_delegation_action(&input).await?;
                }
            }
            _ => {}
        }

        self.input_mode = InputMode::Normal;
        self.input_buffer.clear();
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:925-955 method show_detailed_status <-> ./crates/ccswarm/src/tui/app.rs:1083-1117 method analyze_task_for_delegation
  Similarity: 87.80%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_detailed_status (lines 925-955) ---[0m
    async fn show_detailed_status(&mut self) -> Result<()> {
        self.add_log("System", "=== Detailed System Status ===")
            .await;
        self.add_log("System", &format!("System Status: {}", self.system_status))
            .await;
        self.add_log("System", &format!("Total Agents: {}", self.total_agents))
            .await;
        self.add_log("System", &format!("Active Agents: {}", self.active_agents))
            .await;
        self.add_log("System", &format!("Pending Tasks: {}", self.pending_tasks))
            .await;
        self.add_log(
            "System",
            &format!("Completed Tasks: {}", self.completed_tasks),
        )
        .await;

        // Show Master Claude Code status specifically
        if let Some(master) = self
            .agents
            .iter()
            .find(|a| a.specialization.contains("Master"))
        {
            self.add_log(
                "System",
                &format!("👑 Master Claude Code: {:?}", master.status),
            )
            .await;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:analyze_task_for_delegation (lines 1083-1117) ---[0m
    async fn analyze_task_for_delegation(&mut self, task_description: &str) -> Result<()> {
        self.add_log(
            "Master",
            &format!("🔍 Analyzing task: '{}'", task_description),
        )
        .await;

        // Use basic rule-based analysis for demo
        let (recommended_agent, confidence, reasoning) =
            self.analyze_task_content(task_description);

        let delegation_info = DelegationInfo {
            task_description: task_description.to_string(),
            recommended_agent: recommended_agent.clone(),
            confidence,
            reasoning: reasoning.clone(),
            created_at: chrono::Utc::now(),
        };

        self.delegation_decisions.push(delegation_info);

        self.add_log(
            "Master",
            &format!(
                "✅ Analysis complete: {} agent recommended ({:.1}% confidence)",
                recommended_agent,
                confidence * 100.0
            ),
        )
        .await;
        self.add_log("Master", &format!("📝 Reasoning: {}", reasoning))
            .await;

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:152-189 method new <-> ./crates/ccswarm/src/tui/app.rs:1083-1117 method analyze_task_for_delegation
  Similarity: 78.77%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:new (lines 152-189) ---[0m
    pub async fn new() -> Result<Self> {
        let coordination_bus = CoordinationBus::new().await?;
        let status_tracker = StatusTracker::new().await?;
        let task_queue = TaskQueue::new().await?;

        Ok(Self {
            current_tab: Tab::Overview,
            selected_agent: 0,
            selected_task: 0,
            selected_log: 0,
            selected_delegation: 0,
            agents: Vec::new(),
            tasks: Vec::new(),
            logs: Vec::new(),
            delegation_decisions: Vec::new(),
            system_status: "Starting...".to_string(),
            total_agents: 0,
            active_agents: 0,
            pending_tasks: 0,
            completed_tasks: 0,
            tasks_executed: 0,
            tasks_failed: 0,
            success_rate: 100.0,
            orchestration_usage: 0.0,
            input_mode: InputMode::Normal,
            input_buffer: String::new(),
            delegation_mode: DelegationMode::Analyze,
            delegation_input: String::new(),
            coordination_bus,
            status_tracker,
            task_queue,
            execution_engine: None, // Will be set by CLI runner
            terminal_width: 80,
            terminal_height: 24,
            last_update: Instant::now(),
            update_interval: Duration::from_millis(500), // More frequent updates for real-time feel
        })
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:analyze_task_for_delegation (lines 1083-1117) ---[0m
    async fn analyze_task_for_delegation(&mut self, task_description: &str) -> Result<()> {
        self.add_log(
            "Master",
            &format!("🔍 Analyzing task: '{}'", task_description),
        )
        .await;

        // Use basic rule-based analysis for demo
        let (recommended_agent, confidence, reasoning) =
            self.analyze_task_content(task_description);

        let delegation_info = DelegationInfo {
            task_description: task_description.to_string(),
            recommended_agent: recommended_agent.clone(),
            confidence,
            reasoning: reasoning.clone(),
            created_at: chrono::Utc::now(),
        };

        self.delegation_decisions.push(delegation_info);

        self.add_log(
            "Master",
            &format!(
                "✅ Analysis complete: {} agent recommended ({:.1}% confidence)",
                recommended_agent,
                confidence * 100.0
            ),
        )
        .await;
        self.add_log("Master", &format!("📝 Reasoning: {}", reasoning))
            .await;

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:152-189 method new <-> ./crates/ccswarm/src/tui/app.rs:281-310 method activate_selected
  Similarity: 83.93%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:new (lines 152-189) ---[0m
    pub async fn new() -> Result<Self> {
        let coordination_bus = CoordinationBus::new().await?;
        let status_tracker = StatusTracker::new().await?;
        let task_queue = TaskQueue::new().await?;

        Ok(Self {
            current_tab: Tab::Overview,
            selected_agent: 0,
            selected_task: 0,
            selected_log: 0,
            selected_delegation: 0,
            agents: Vec::new(),
            tasks: Vec::new(),
            logs: Vec::new(),
            delegation_decisions: Vec::new(),
            system_status: "Starting...".to_string(),
            total_agents: 0,
            active_agents: 0,
            pending_tasks: 0,
            completed_tasks: 0,
            tasks_executed: 0,
            tasks_failed: 0,
            success_rate: 100.0,
            orchestration_usage: 0.0,
            input_mode: InputMode::Normal,
            input_buffer: String::new(),
            delegation_mode: DelegationMode::Analyze,
            delegation_input: String::new(),
            coordination_bus,
            status_tracker,
            task_queue,
            execution_engine: None, // Will be set by CLI runner
            terminal_width: 80,
            terminal_height: 24,
            last_update: Instant::now(),
            update_interval: Duration::from_millis(500), // More frequent updates for real-time feel
        })
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:activate_selected (lines 281-310) ---[0m
    pub async fn activate_selected(&mut self) -> Result<()> {
        match self.current_tab {
            Tab::Agents => {
                if let Some(agent) = self.agents.get(self.selected_agent).cloned() {
                    match agent.status {
                        AgentStatus::Available => {
                            // Start the agent
                            self.start_agent(&agent.id).await?;
                        }
                        AgentStatus::Working => {
                            // Show agent details
                            self.show_agent_details(&agent.id).await?;
                        }
                        _ => {
                            // Show agent details for other statuses
                            self.show_agent_details(&agent.id).await?;
                        }
                    }
                }
            }
            Tab::Tasks => {
                if let Some(task) = self.tasks.get(self.selected_task) {
                    let task_id = task.id.clone();
                    self.show_task_details(&task_id).await?;
                }
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:313-347 method start_agent <-> ./crates/ccswarm/src/tui/app.rs:1083-1117 method analyze_task_for_delegation
  Similarity: 81.42%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent (lines 313-347) ---[0m
    pub async fn start_agent(&mut self, agent_id: &str) -> Result<()> {
        let mut agent_info = None;

        // Find the agent and collect info
        if let Some(agent) = self.agents.iter_mut().find(|a| a.id == agent_id) {
            // Change status to Working
            agent.status = AgentStatus::Working;
            agent.last_activity = Utc::now();

            // Collect info for logging
            agent_info = Some((agent.name.clone(), agent.specialization.clone()));

            // Update system stats
            self.active_agents += 1;
        }

        // Log after borrowing ends
        if let Some((name, specialization)) = agent_info {
            self.add_log(
                "System",
                &format!("🚀 Starting agent: {} ({})", name, specialization),
            )
            .await;

            // If this is a Master agent, provide special logging
            if specialization.contains("Master") {
                self.add_log("Master", "🎯 Master Claude Code orchestrator activated")
                    .await;
                self.add_log("Master", "📋 Ready to coordinate multi-agent tasks")
                    .await;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:analyze_task_for_delegation (lines 1083-1117) ---[0m
    async fn analyze_task_for_delegation(&mut self, task_description: &str) -> Result<()> {
        self.add_log(
            "Master",
            &format!("🔍 Analyzing task: '{}'", task_description),
        )
        .await;

        // Use basic rule-based analysis for demo
        let (recommended_agent, confidence, reasoning) =
            self.analyze_task_content(task_description);

        let delegation_info = DelegationInfo {
            task_description: task_description.to_string(),
            recommended_agent: recommended_agent.clone(),
            confidence,
            reasoning: reasoning.clone(),
            created_at: chrono::Utc::now(),
        };

        self.delegation_decisions.push(delegation_info);

        self.add_log(
            "Master",
            &format!(
                "✅ Analysis complete: {} agent recommended ({:.1}% confidence)",
                recommended_agent,
                confidence * 100.0
            ),
        )
        .await;
        self.add_log("Master", &format!("📝 Reasoning: {}", reasoning))
            .await;

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:152-189 method new <-> ./crates/ccswarm/src/tui/app.rs:673-693 method add_log
  Similarity: 91.72%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:new (lines 152-189) ---[0m
    pub async fn new() -> Result<Self> {
        let coordination_bus = CoordinationBus::new().await?;
        let status_tracker = StatusTracker::new().await?;
        let task_queue = TaskQueue::new().await?;

        Ok(Self {
            current_tab: Tab::Overview,
            selected_agent: 0,
            selected_task: 0,
            selected_log: 0,
            selected_delegation: 0,
            agents: Vec::new(),
            tasks: Vec::new(),
            logs: Vec::new(),
            delegation_decisions: Vec::new(),
            system_status: "Starting...".to_string(),
            total_agents: 0,
            active_agents: 0,
            pending_tasks: 0,
            completed_tasks: 0,
            tasks_executed: 0,
            tasks_failed: 0,
            success_rate: 100.0,
            orchestration_usage: 0.0,
            input_mode: InputMode::Normal,
            input_buffer: String::new(),
            delegation_mode: DelegationMode::Analyze,
            delegation_input: String::new(),
            coordination_bus,
            status_tracker,
            task_queue,
            execution_engine: None, // Will be set by CLI runner
            terminal_width: 80,
            terminal_height: 24,
            last_update: Instant::now(),
            update_interval: Duration::from_millis(500), // More frequent updates for real-time feel
        })
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:add_log (lines 673-693) ---[0m
    async fn add_log(&mut self, source: &str, message: &str) {
        let log_entry = LogEntry {
            timestamp: Utc::now(),
            level: "INFO".to_string(),
            agent: if source == "System" {
                None
            } else {
                Some(source.to_string())
            },
            message: message.to_string(),
        };
        self.logs.push(log_entry);

        // Keep only last 1000 log entries
        if self.logs.len() > 1000 {
            self.logs.remove(0);
        }

        // Auto-scroll to bottom
        self.selected_log = self.logs.len().saturating_sub(1);
    }

  ./crates/ccswarm/src/tui/app.rs:313-347 method start_agent <-> ./crates/ccswarm/src/tui/app.rs:673-693 method add_log
  Similarity: 95.87%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent (lines 313-347) ---[0m
    pub async fn start_agent(&mut self, agent_id: &str) -> Result<()> {
        let mut agent_info = None;

        // Find the agent and collect info
        if let Some(agent) = self.agents.iter_mut().find(|a| a.id == agent_id) {
            // Change status to Working
            agent.status = AgentStatus::Working;
            agent.last_activity = Utc::now();

            // Collect info for logging
            agent_info = Some((agent.name.clone(), agent.specialization.clone()));

            // Update system stats
            self.active_agents += 1;
        }

        // Log after borrowing ends
        if let Some((name, specialization)) = agent_info {
            self.add_log(
                "System",
                &format!("🚀 Starting agent: {} ({})", name, specialization),
            )
            .await;

            // If this is a Master agent, provide special logging
            if specialization.contains("Master") {
                self.add_log("Master", "🎯 Master Claude Code orchestrator activated")
                    .await;
                self.add_log("Master", "📋 Ready to coordinate multi-agent tasks")
                    .await;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:add_log (lines 673-693) ---[0m
    async fn add_log(&mut self, source: &str, message: &str) {
        let log_entry = LogEntry {
            timestamp: Utc::now(),
            level: "INFO".to_string(),
            agent: if source == "System" {
                None
            } else {
                Some(source.to_string())
            },
            message: message.to_string(),
        };
        self.logs.push(log_entry);

        // Keep only last 1000 log entries
        if self.logs.len() > 1000 {
            self.logs.remove(0);
        }

        // Auto-scroll to bottom
        self.selected_log = self.logs.len().saturating_sub(1);
    }

  ./crates/ccswarm/src/tui/app.rs:152-189 method new <-> ./crates/ccswarm/src/tui/app.rs:758-780 method execute_create_agent
  Similarity: 87.72%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:new (lines 152-189) ---[0m
    pub async fn new() -> Result<Self> {
        let coordination_bus = CoordinationBus::new().await?;
        let status_tracker = StatusTracker::new().await?;
        let task_queue = TaskQueue::new().await?;

        Ok(Self {
            current_tab: Tab::Overview,
            selected_agent: 0,
            selected_task: 0,
            selected_log: 0,
            selected_delegation: 0,
            agents: Vec::new(),
            tasks: Vec::new(),
            logs: Vec::new(),
            delegation_decisions: Vec::new(),
            system_status: "Starting...".to_string(),
            total_agents: 0,
            active_agents: 0,
            pending_tasks: 0,
            completed_tasks: 0,
            tasks_executed: 0,
            tasks_failed: 0,
            success_rate: 100.0,
            orchestration_usage: 0.0,
            input_mode: InputMode::Normal,
            input_buffer: String::new(),
            delegation_mode: DelegationMode::Analyze,
            delegation_input: String::new(),
            coordination_bus,
            status_tracker,
            task_queue,
            execution_engine: None, // Will be set by CLI runner
            terminal_width: 80,
            terminal_height: 24,
            last_update: Instant::now(),
            update_interval: Duration::from_millis(500), // More frequent updates for real-time feel
        })
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_create_agent (lines 758-780) ---[0m
    async fn execute_create_agent(&mut self, agent_type: &str) -> Result<()> {
        let agent_type = agent_type.to_lowercase();
        match agent_type.as_str() {
            "frontend" | "backend" | "devops" | "qa" => {
                self.add_log("System", &format!("Creating {} agent...", agent_type))
                    .await;
                // TODO: Implement actual agent creation
                self.add_log(
                    "System",
                    &format!("{} agent created successfully", agent_type),
                )
                .await;
            }
            _ => {
                self.add_log(
                    "System",
                    "Invalid agent type. Use: frontend, backend, devops, qa",
                )
                .await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:313-347 method start_agent <-> ./crates/ccswarm/src/tui/app.rs:925-955 method show_detailed_status
  Similarity: 80.82%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent (lines 313-347) ---[0m
    pub async fn start_agent(&mut self, agent_id: &str) -> Result<()> {
        let mut agent_info = None;

        // Find the agent and collect info
        if let Some(agent) = self.agents.iter_mut().find(|a| a.id == agent_id) {
            // Change status to Working
            agent.status = AgentStatus::Working;
            agent.last_activity = Utc::now();

            // Collect info for logging
            agent_info = Some((agent.name.clone(), agent.specialization.clone()));

            // Update system stats
            self.active_agents += 1;
        }

        // Log after borrowing ends
        if let Some((name, specialization)) = agent_info {
            self.add_log(
                "System",
                &format!("🚀 Starting agent: {} ({})", name, specialization),
            )
            .await;

            // If this is a Master agent, provide special logging
            if specialization.contains("Master") {
                self.add_log("Master", "🎯 Master Claude Code orchestrator activated")
                    .await;
                self.add_log("Master", "📋 Ready to coordinate multi-agent tasks")
                    .await;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_detailed_status (lines 925-955) ---[0m
    async fn show_detailed_status(&mut self) -> Result<()> {
        self.add_log("System", "=== Detailed System Status ===")
            .await;
        self.add_log("System", &format!("System Status: {}", self.system_status))
            .await;
        self.add_log("System", &format!("Total Agents: {}", self.total_agents))
            .await;
        self.add_log("System", &format!("Active Agents: {}", self.active_agents))
            .await;
        self.add_log("System", &format!("Pending Tasks: {}", self.pending_tasks))
            .await;
        self.add_log(
            "System",
            &format!("Completed Tasks: {}", self.completed_tasks),
        )
        .await;

        // Show Master Claude Code status specifically
        if let Some(master) = self
            .agents
            .iter()
            .find(|a| a.specialization.contains("Master"))
        {
            self.add_log(
                "System",
                &format!("👑 Master Claude Code: {:?}", master.status),
            )
            .await;
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:281-310 method activate_selected <-> ./crates/ccswarm/src/tui/app.rs:313-347 method start_agent
  Similarity: 80.24%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:activate_selected (lines 281-310) ---[0m
    pub async fn activate_selected(&mut self) -> Result<()> {
        match self.current_tab {
            Tab::Agents => {
                if let Some(agent) = self.agents.get(self.selected_agent).cloned() {
                    match agent.status {
                        AgentStatus::Available => {
                            // Start the agent
                            self.start_agent(&agent.id).await?;
                        }
                        AgentStatus::Working => {
                            // Show agent details
                            self.show_agent_details(&agent.id).await?;
                        }
                        _ => {
                            // Show agent details for other statuses
                            self.show_agent_details(&agent.id).await?;
                        }
                    }
                }
            }
            Tab::Tasks => {
                if let Some(task) = self.tasks.get(self.selected_task) {
                    let task_id = task.id.clone();
                    self.show_task_details(&task_id).await?;
                }
            }
            _ => {}
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent (lines 313-347) ---[0m
    pub async fn start_agent(&mut self, agent_id: &str) -> Result<()> {
        let mut agent_info = None;

        // Find the agent and collect info
        if let Some(agent) = self.agents.iter_mut().find(|a| a.id == agent_id) {
            // Change status to Working
            agent.status = AgentStatus::Working;
            agent.last_activity = Utc::now();

            // Collect info for logging
            agent_info = Some((agent.name.clone(), agent.specialization.clone()));

            // Update system stats
            self.active_agents += 1;
        }

        // Log after borrowing ends
        if let Some((name, specialization)) = agent_info {
            self.add_log(
                "System",
                &format!("🚀 Starting agent: {} ({})", name, specialization),
            )
            .await;

            // If this is a Master agent, provide special logging
            if specialization.contains("Master") {
                self.add_log("Master", "🎯 Master Claude Code orchestrator activated")
                    .await;
                self.add_log("Master", "📋 Ready to coordinate multi-agent tasks")
                    .await;
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:710-740 method process_input <-> ./crates/ccswarm/src/tui/app.rs:1083-1117 method analyze_task_for_delegation
  Similarity: 76.52%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:process_input (lines 710-740) ---[0m
    pub async fn process_input(&mut self) -> Result<()> {
        let input = self.input_buffer.trim().to_string();

        match self.input_mode {
            InputMode::AddingTask => {
                if !input.is_empty() {
                    self.execute_add_task(&input).await?;
                }
            }
            InputMode::CreatingAgent => {
                if !input.is_empty() {
                    self.execute_create_agent(&input).await?;
                }
            }
            InputMode::Command => {
                if !input.is_empty() {
                    self.execute_command(&input).await?;
                }
            }
            InputMode::DelegationInput => {
                if !input.is_empty() {
                    self.execute_delegation_action(&input).await?;
                }
            }
            _ => {}
        }

        self.input_mode = InputMode::Normal;
        self.input_buffer.clear();
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:analyze_task_for_delegation (lines 1083-1117) ---[0m
    async fn analyze_task_for_delegation(&mut self, task_description: &str) -> Result<()> {
        self.add_log(
            "Master",
            &format!("🔍 Analyzing task: '{}'", task_description),
        )
        .await;

        // Use basic rule-based analysis for demo
        let (recommended_agent, confidence, reasoning) =
            self.analyze_task_content(task_description);

        let delegation_info = DelegationInfo {
            task_description: task_description.to_string(),
            recommended_agent: recommended_agent.clone(),
            confidence,
            reasoning: reasoning.clone(),
            created_at: chrono::Utc::now(),
        };

        self.delegation_decisions.push(delegation_info);

        self.add_log(
            "Master",
            &format!(
                "✅ Analysis complete: {} agent recommended ({:.1}% confidence)",
                recommended_agent,
                confidence * 100.0
            ),
        )
        .await;
        self.add_log("Master", &format!("📝 Reasoning: {}", reasoning))
            .await;

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:1083-1117 method analyze_task_for_delegation <-> ./crates/ccswarm/src/tui/app.rs:1265-1297 method get_delegation_stats
  Similarity: 73.63%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:analyze_task_for_delegation (lines 1083-1117) ---[0m
    async fn analyze_task_for_delegation(&mut self, task_description: &str) -> Result<()> {
        self.add_log(
            "Master",
            &format!("🔍 Analyzing task: '{}'", task_description),
        )
        .await;

        // Use basic rule-based analysis for demo
        let (recommended_agent, confidence, reasoning) =
            self.analyze_task_content(task_description);

        let delegation_info = DelegationInfo {
            task_description: task_description.to_string(),
            recommended_agent: recommended_agent.clone(),
            confidence,
            reasoning: reasoning.clone(),
            created_at: chrono::Utc::now(),
        };

        self.delegation_decisions.push(delegation_info);

        self.add_log(
            "Master",
            &format!(
                "✅ Analysis complete: {} agent recommended ({:.1}% confidence)",
                recommended_agent,
                confidence * 100.0
            ),
        )
        .await;
        self.add_log("Master", &format!("📝 Reasoning: {}", reasoning))
            .await;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:get_delegation_stats (lines 1265-1297) ---[0m
    pub fn get_delegation_stats(&self) -> String {
        if self.delegation_decisions.is_empty() {
            return "No delegation decisions yet".to_string();
        }

        let total = self.delegation_decisions.len();
        let mut agent_counts = std::collections::HashMap::new();
        let mut total_confidence = 0.0;

        for decision in &self.delegation_decisions {
            *agent_counts
                .entry(decision.recommended_agent.clone())
                .or_insert(0) += 1;
            total_confidence += decision.confidence;
        }

        let avg_confidence = total_confidence / total as f64;

        let mut stats = "📊 Delegation Statistics:\n".to_string();
        stats.push_str(&format!("Total delegations: {}\n", total));
        stats.push_str(&format!(
            "Average confidence: {:.1}%\n",
            avg_confidence * 100.0
        ));
        stats.push_str("Agent distribution:\n");

        for (agent, count) in agent_counts {
            let percentage = (count as f64 / total as f64) * 100.0;
            stats.push_str(&format!("  {}: {} ({:.1}%)\n", agent, count, percentage));
        }

        stats
    }

  ./crates/ccswarm/src/tui/app.rs:152-189 method new <-> ./crates/ccswarm/src/tui/app.rs:216-240 method previous_item
  Similarity: 79.12%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:new (lines 152-189) ---[0m
    pub async fn new() -> Result<Self> {
        let coordination_bus = CoordinationBus::new().await?;
        let status_tracker = StatusTracker::new().await?;
        let task_queue = TaskQueue::new().await?;

        Ok(Self {
            current_tab: Tab::Overview,
            selected_agent: 0,
            selected_task: 0,
            selected_log: 0,
            selected_delegation: 0,
            agents: Vec::new(),
            tasks: Vec::new(),
            logs: Vec::new(),
            delegation_decisions: Vec::new(),
            system_status: "Starting...".to_string(),
            total_agents: 0,
            active_agents: 0,
            pending_tasks: 0,
            completed_tasks: 0,
            tasks_executed: 0,
            tasks_failed: 0,
            success_rate: 100.0,
            orchestration_usage: 0.0,
            input_mode: InputMode::Normal,
            input_buffer: String::new(),
            delegation_mode: DelegationMode::Analyze,
            delegation_input: String::new(),
            coordination_bus,
            status_tracker,
            task_queue,
            execution_engine: None, // Will be set by CLI runner
            terminal_width: 80,
            terminal_height: 24,
            last_update: Instant::now(),
            update_interval: Duration::from_millis(500), // More frequent updates for real-time feel
        })
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_item (lines 216-240) ---[0m
    pub fn previous_item(&mut self) {
        match self.current_tab {
            Tab::Agents => {
                if self.selected_agent > 0 {
                    self.selected_agent -= 1;
                }
            }
            Tab::Tasks => {
                if self.selected_task > 0 {
                    self.selected_task -= 1;
                }
            }
            Tab::Logs => {
                if self.selected_log > 0 {
                    self.selected_log -= 1;
                }
            }
            Tab::Delegation => {
                if self.selected_delegation > 0 {
                    self.selected_delegation -= 1;
                }
            }
            _ => {}
        }
    }

  ./crates/ccswarm/src/tui/app.rs:152-189 method new <-> ./crates/ccswarm/src/tui/app.rs:243-267 method next_item
  Similarity: 79.12%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:new (lines 152-189) ---[0m
    pub async fn new() -> Result<Self> {
        let coordination_bus = CoordinationBus::new().await?;
        let status_tracker = StatusTracker::new().await?;
        let task_queue = TaskQueue::new().await?;

        Ok(Self {
            current_tab: Tab::Overview,
            selected_agent: 0,
            selected_task: 0,
            selected_log: 0,
            selected_delegation: 0,
            agents: Vec::new(),
            tasks: Vec::new(),
            logs: Vec::new(),
            delegation_decisions: Vec::new(),
            system_status: "Starting...".to_string(),
            total_agents: 0,
            active_agents: 0,
            pending_tasks: 0,
            completed_tasks: 0,
            tasks_executed: 0,
            tasks_failed: 0,
            success_rate: 100.0,
            orchestration_usage: 0.0,
            input_mode: InputMode::Normal,
            input_buffer: String::new(),
            delegation_mode: DelegationMode::Analyze,
            delegation_input: String::new(),
            coordination_bus,
            status_tracker,
            task_queue,
            execution_engine: None, // Will be set by CLI runner
            terminal_width: 80,
            terminal_height: 24,
            last_update: Instant::now(),
            update_interval: Duration::from_millis(500), // More frequent updates for real-time feel
        })
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:next_item (lines 243-267) ---[0m
    pub fn next_item(&mut self) {
        match self.current_tab {
            Tab::Agents => {
                if self.selected_agent < self.agents.len().saturating_sub(1) {
                    self.selected_agent += 1;
                }
            }
            Tab::Tasks => {
                if self.selected_task < self.tasks.len().saturating_sub(1) {
                    self.selected_task += 1;
                }
            }
            Tab::Logs => {
                if self.selected_log < self.logs.len().saturating_sub(1) {
                    self.selected_log += 1;
                }
            }
            Tab::Delegation => {
                if self.selected_delegation < self.delegation_decisions.len().saturating_sub(1) {
                    self.selected_delegation += 1;
                }
            }
            _ => {}
        }
    }

  ./crates/ccswarm/src/tui/app.rs:216-240 method previous_item <-> ./crates/ccswarm/src/tui/app.rs:281-310 method activate_selected
  Similarity: 90.37%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_item (lines 216-240) ---[0m
    pub fn previous_item(&mut self) {
        match self.current_tab {
            Tab::Agents => {
                if self.selected_agent > 0 {
                    self.selected_agent -= 1;
                }
            }
            Tab::Tasks => {
                if self.selected_task > 0 {
                    self.selected_task -= 1;
                }
            }
            Tab::Logs => {
                if self.selected_log > 0 {
                    self.selected_log -= 1;
                }
            }
            Tab::Delegation => {
                if self.selected_delegation > 0 {
                    self.selected_delegation -= 1;
                }
            }
            _ => {}
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:activate_selected (lines 281-310) ---[0m
    pub async fn activate_selected(&mut self) -> Result<()> {
        match self.current_tab {
            Tab::Agents => {
                if let Some(agent) = self.agents.get(self.selected_agent).cloned() {
                    match agent.status {
                        AgentStatus::Available => {
                            // Start the agent
                            self.start_agent(&agent.id).await?;
                        }
                        AgentStatus::Working => {
                            // Show agent details
                            self.show_agent_details(&agent.id).await?;
                        }
                        _ => {
                            // Show agent details for other statuses
                            self.show_agent_details(&agent.id).await?;
                        }
                    }
                }
            }
            Tab::Tasks => {
                if let Some(task) = self.tasks.get(self.selected_task) {
                    let task_id = task.id.clone();
                    self.show_task_details(&task_id).await?;
                }
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:243-267 method next_item <-> ./crates/ccswarm/src/tui/app.rs:281-310 method activate_selected
  Similarity: 90.37%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:next_item (lines 243-267) ---[0m
    pub fn next_item(&mut self) {
        match self.current_tab {
            Tab::Agents => {
                if self.selected_agent < self.agents.len().saturating_sub(1) {
                    self.selected_agent += 1;
                }
            }
            Tab::Tasks => {
                if self.selected_task < self.tasks.len().saturating_sub(1) {
                    self.selected_task += 1;
                }
            }
            Tab::Logs => {
                if self.selected_log < self.logs.len().saturating_sub(1) {
                    self.selected_log += 1;
                }
            }
            Tab::Delegation => {
                if self.selected_delegation < self.delegation_decisions.len().saturating_sub(1) {
                    self.selected_delegation += 1;
                }
            }
            _ => {}
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:activate_selected (lines 281-310) ---[0m
    pub async fn activate_selected(&mut self) -> Result<()> {
        match self.current_tab {
            Tab::Agents => {
                if let Some(agent) = self.agents.get(self.selected_agent).cloned() {
                    match agent.status {
                        AgentStatus::Available => {
                            // Start the agent
                            self.start_agent(&agent.id).await?;
                        }
                        AgentStatus::Working => {
                            // Show agent details
                            self.show_agent_details(&agent.id).await?;
                        }
                        _ => {
                            // Show agent details for other statuses
                            self.show_agent_details(&agent.id).await?;
                        }
                    }
                }
            }
            Tab::Tasks => {
                if let Some(task) = self.tasks.get(self.selected_task) {
                    let task_id = task.id.clone();
                    self.show_task_details(&task_id).await?;
                }
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:313-347 method start_agent <-> ./crates/ccswarm/src/tui/app.rs:758-780 method execute_create_agent
  Similarity: 85.67%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent (lines 313-347) ---[0m
    pub async fn start_agent(&mut self, agent_id: &str) -> Result<()> {
        let mut agent_info = None;

        // Find the agent and collect info
        if let Some(agent) = self.agents.iter_mut().find(|a| a.id == agent_id) {
            // Change status to Working
            agent.status = AgentStatus::Working;
            agent.last_activity = Utc::now();

            // Collect info for logging
            agent_info = Some((agent.name.clone(), agent.specialization.clone()));

            // Update system stats
            self.active_agents += 1;
        }

        // Log after borrowing ends
        if let Some((name, specialization)) = agent_info {
            self.add_log(
                "System",
                &format!("🚀 Starting agent: {} ({})", name, specialization),
            )
            .await;

            // If this is a Master agent, provide special logging
            if specialization.contains("Master") {
                self.add_log("Master", "🎯 Master Claude Code orchestrator activated")
                    .await;
                self.add_log("Master", "📋 Ready to coordinate multi-agent tasks")
                    .await;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_create_agent (lines 758-780) ---[0m
    async fn execute_create_agent(&mut self, agent_type: &str) -> Result<()> {
        let agent_type = agent_type.to_lowercase();
        match agent_type.as_str() {
            "frontend" | "backend" | "devops" | "qa" => {
                self.add_log("System", &format!("Creating {} agent...", agent_type))
                    .await;
                // TODO: Implement actual agent creation
                self.add_log(
                    "System",
                    &format!("{} agent created successfully", agent_type),
                )
                .await;
            }
            _ => {
                self.add_log(
                    "System",
                    "Invalid agent type. Use: frontend, backend, devops, qa",
                )
                .await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:281-310 method activate_selected <-> ./crates/ccswarm/src/tui/app.rs:710-740 method process_input
  Similarity: 80.92%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:activate_selected (lines 281-310) ---[0m
    pub async fn activate_selected(&mut self) -> Result<()> {
        match self.current_tab {
            Tab::Agents => {
                if let Some(agent) = self.agents.get(self.selected_agent).cloned() {
                    match agent.status {
                        AgentStatus::Available => {
                            // Start the agent
                            self.start_agent(&agent.id).await?;
                        }
                        AgentStatus::Working => {
                            // Show agent details
                            self.show_agent_details(&agent.id).await?;
                        }
                        _ => {
                            // Show agent details for other statuses
                            self.show_agent_details(&agent.id).await?;
                        }
                    }
                }
            }
            Tab::Tasks => {
                if let Some(task) = self.tasks.get(self.selected_task) {
                    let task_id = task.id.clone();
                    self.show_task_details(&task_id).await?;
                }
            }
            _ => {}
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:process_input (lines 710-740) ---[0m
    pub async fn process_input(&mut self) -> Result<()> {
        let input = self.input_buffer.trim().to_string();

        match self.input_mode {
            InputMode::AddingTask => {
                if !input.is_empty() {
                    self.execute_add_task(&input).await?;
                }
            }
            InputMode::CreatingAgent => {
                if !input.is_empty() {
                    self.execute_create_agent(&input).await?;
                }
            }
            InputMode::Command => {
                if !input.is_empty() {
                    self.execute_command(&input).await?;
                }
            }
            InputMode::DelegationInput => {
                if !input.is_empty() {
                    self.execute_delegation_action(&input).await?;
                }
            }
            _ => {}
        }

        self.input_mode = InputMode::Normal;
        self.input_buffer.clear();
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:152-189 method new <-> ./crates/ccswarm/src/tui/app.rs:350-365 method start_agent_by_id
  Similarity: 91.16%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:new (lines 152-189) ---[0m
    pub async fn new() -> Result<Self> {
        let coordination_bus = CoordinationBus::new().await?;
        let status_tracker = StatusTracker::new().await?;
        let task_queue = TaskQueue::new().await?;

        Ok(Self {
            current_tab: Tab::Overview,
            selected_agent: 0,
            selected_task: 0,
            selected_log: 0,
            selected_delegation: 0,
            agents: Vec::new(),
            tasks: Vec::new(),
            logs: Vec::new(),
            delegation_decisions: Vec::new(),
            system_status: "Starting...".to_string(),
            total_agents: 0,
            active_agents: 0,
            pending_tasks: 0,
            completed_tasks: 0,
            tasks_executed: 0,
            tasks_failed: 0,
            success_rate: 100.0,
            orchestration_usage: 0.0,
            input_mode: InputMode::Normal,
            input_buffer: String::new(),
            delegation_mode: DelegationMode::Analyze,
            delegation_input: String::new(),
            coordination_bus,
            status_tracker,
            task_queue,
            execution_engine: None, // Will be set by CLI runner
            terminal_width: 80,
            terminal_height: 24,
            last_update: Instant::now(),
            update_interval: Duration::from_millis(500), // More frequent updates for real-time feel
        })
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent_by_id (lines 350-365) ---[0m
    pub async fn start_agent_by_id(&mut self, identifier: &str) -> Result<()> {
        // Find agent by ID or name
        let agent_to_start = self
            .agents
            .iter()
            .find(|a| a.id == identifier || a.name == identifier)
            .map(|a| a.id.clone());

        if let Some(agent_id) = agent_to_start {
            self.start_agent(&agent_id).await?;
        } else {
            self.add_log("System", &format!("Agent not found: {}", identifier))
                .await;
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:216-240 method previous_item <-> ./crates/ccswarm/src/tui/app.rs:243-267 method next_item
  Similarity: 97.06%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_item (lines 216-240) ---[0m
    pub fn previous_item(&mut self) {
        match self.current_tab {
            Tab::Agents => {
                if self.selected_agent > 0 {
                    self.selected_agent -= 1;
                }
            }
            Tab::Tasks => {
                if self.selected_task > 0 {
                    self.selected_task -= 1;
                }
            }
            Tab::Logs => {
                if self.selected_log > 0 {
                    self.selected_log -= 1;
                }
            }
            Tab::Delegation => {
                if self.selected_delegation > 0 {
                    self.selected_delegation -= 1;
                }
            }
            _ => {}
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:next_item (lines 243-267) ---[0m
    pub fn next_item(&mut self) {
        match self.current_tab {
            Tab::Agents => {
                if self.selected_agent < self.agents.len().saturating_sub(1) {
                    self.selected_agent += 1;
                }
            }
            Tab::Tasks => {
                if self.selected_task < self.tasks.len().saturating_sub(1) {
                    self.selected_task += 1;
                }
            }
            Tab::Logs => {
                if self.selected_log < self.logs.len().saturating_sub(1) {
                    self.selected_log += 1;
                }
            }
            Tab::Delegation => {
                if self.selected_delegation < self.delegation_decisions.len().saturating_sub(1) {
                    self.selected_delegation += 1;
                }
            }
            _ => {}
        }
    }

  ./crates/ccswarm/src/tui/app.rs:152-189 method new <-> ./crates/ccswarm/src/tui/app.rs:958-979 method list_agents_command
  Similarity: 80.74%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:new (lines 152-189) ---[0m
    pub async fn new() -> Result<Self> {
        let coordination_bus = CoordinationBus::new().await?;
        let status_tracker = StatusTracker::new().await?;
        let task_queue = TaskQueue::new().await?;

        Ok(Self {
            current_tab: Tab::Overview,
            selected_agent: 0,
            selected_task: 0,
            selected_log: 0,
            selected_delegation: 0,
            agents: Vec::new(),
            tasks: Vec::new(),
            logs: Vec::new(),
            delegation_decisions: Vec::new(),
            system_status: "Starting...".to_string(),
            total_agents: 0,
            active_agents: 0,
            pending_tasks: 0,
            completed_tasks: 0,
            tasks_executed: 0,
            tasks_failed: 0,
            success_rate: 100.0,
            orchestration_usage: 0.0,
            input_mode: InputMode::Normal,
            input_buffer: String::new(),
            delegation_mode: DelegationMode::Analyze,
            delegation_input: String::new(),
            coordination_bus,
            status_tracker,
            task_queue,
            execution_engine: None, // Will be set by CLI runner
            terminal_width: 80,
            terminal_height: 24,
            last_update: Instant::now(),
            update_interval: Duration::from_millis(500), // More frequent updates for real-time feel
        })
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_agents_command (lines 958-979) ---[0m
    async fn list_agents_command(&mut self) -> Result<()> {
        if self.agents.is_empty() {
            self.add_log("System", "No agents found").await;
        } else {
            self.add_log("System", "Active agents:").await;
            let agent_info: Vec<String> = self
                .agents
                .iter()
                .map(|agent| {
                    format!(
                        "  {} ({}) - {:?}",
                        agent.name, agent.specialization, agent.status
                    )
                })
                .collect();

            for info in agent_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:152-189 method new <-> ./crates/ccswarm/src/tui/app.rs:982-1003 method list_tasks_command
  Similarity: 80.74%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:new (lines 152-189) ---[0m
    pub async fn new() -> Result<Self> {
        let coordination_bus = CoordinationBus::new().await?;
        let status_tracker = StatusTracker::new().await?;
        let task_queue = TaskQueue::new().await?;

        Ok(Self {
            current_tab: Tab::Overview,
            selected_agent: 0,
            selected_task: 0,
            selected_log: 0,
            selected_delegation: 0,
            agents: Vec::new(),
            tasks: Vec::new(),
            logs: Vec::new(),
            delegation_decisions: Vec::new(),
            system_status: "Starting...".to_string(),
            total_agents: 0,
            active_agents: 0,
            pending_tasks: 0,
            completed_tasks: 0,
            tasks_executed: 0,
            tasks_failed: 0,
            success_rate: 100.0,
            orchestration_usage: 0.0,
            input_mode: InputMode::Normal,
            input_buffer: String::new(),
            delegation_mode: DelegationMode::Analyze,
            delegation_input: String::new(),
            coordination_bus,
            status_tracker,
            task_queue,
            execution_engine: None, // Will be set by CLI runner
            terminal_width: 80,
            terminal_height: 24,
            last_update: Instant::now(),
            update_interval: Duration::from_millis(500), // More frequent updates for real-time feel
        })
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_tasks_command (lines 982-1003) ---[0m
    async fn list_tasks_command(&mut self) -> Result<()> {
        if self.tasks.is_empty() {
            self.add_log("System", "No pending tasks").await;
        } else {
            self.add_log("System", "Pending tasks:").await;
            let task_info: Vec<String> = self
                .tasks
                .iter()
                .map(|task| {
                    format!(
                        "  {} - {} ({})",
                        task.description, task.priority, task.task_type
                    )
                })
                .collect();

            for info in task_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:925-955 method show_detailed_status <-> ./crates/ccswarm/src/tui/app.rs:1265-1297 method get_delegation_stats
  Similarity: 75.29%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_detailed_status (lines 925-955) ---[0m
    async fn show_detailed_status(&mut self) -> Result<()> {
        self.add_log("System", "=== Detailed System Status ===")
            .await;
        self.add_log("System", &format!("System Status: {}", self.system_status))
            .await;
        self.add_log("System", &format!("Total Agents: {}", self.total_agents))
            .await;
        self.add_log("System", &format!("Active Agents: {}", self.active_agents))
            .await;
        self.add_log("System", &format!("Pending Tasks: {}", self.pending_tasks))
            .await;
        self.add_log(
            "System",
            &format!("Completed Tasks: {}", self.completed_tasks),
        )
        .await;

        // Show Master Claude Code status specifically
        if let Some(master) = self
            .agents
            .iter()
            .find(|a| a.specialization.contains("Master"))
        {
            self.add_log(
                "System",
                &format!("👑 Master Claude Code: {:?}", master.status),
            )
            .await;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:get_delegation_stats (lines 1265-1297) ---[0m
    pub fn get_delegation_stats(&self) -> String {
        if self.delegation_decisions.is_empty() {
            return "No delegation decisions yet".to_string();
        }

        let total = self.delegation_decisions.len();
        let mut agent_counts = std::collections::HashMap::new();
        let mut total_confidence = 0.0;

        for decision in &self.delegation_decisions {
            *agent_counts
                .entry(decision.recommended_agent.clone())
                .or_insert(0) += 1;
            total_confidence += decision.confidence;
        }

        let avg_confidence = total_confidence / total as f64;

        let mut stats = "📊 Delegation Statistics:\n".to_string();
        stats.push_str(&format!("Total delegations: {}\n", total));
        stats.push_str(&format!(
            "Average confidence: {:.1}%\n",
            avg_confidence * 100.0
        ));
        stats.push_str("Agent distribution:\n");

        for (agent, count) in agent_counts {
            let percentage = (count as f64 / total as f64) * 100.0;
            stats.push_str(&format!("  {}: {} ({:.1}%)\n", agent, count, percentage));
        }

        stats
    }

  ./crates/ccswarm/src/tui/app.rs:281-310 method activate_selected <-> ./crates/ccswarm/src/tui/app.rs:758-780 method execute_create_agent
  Similarity: 90.86%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:activate_selected (lines 281-310) ---[0m
    pub async fn activate_selected(&mut self) -> Result<()> {
        match self.current_tab {
            Tab::Agents => {
                if let Some(agent) = self.agents.get(self.selected_agent).cloned() {
                    match agent.status {
                        AgentStatus::Available => {
                            // Start the agent
                            self.start_agent(&agent.id).await?;
                        }
                        AgentStatus::Working => {
                            // Show agent details
                            self.show_agent_details(&agent.id).await?;
                        }
                        _ => {
                            // Show agent details for other statuses
                            self.show_agent_details(&agent.id).await?;
                        }
                    }
                }
            }
            Tab::Tasks => {
                if let Some(task) = self.tasks.get(self.selected_task) {
                    let task_id = task.id.clone();
                    self.show_task_details(&task_id).await?;
                }
            }
            _ => {}
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_create_agent (lines 758-780) ---[0m
    async fn execute_create_agent(&mut self, agent_type: &str) -> Result<()> {
        let agent_type = agent_type.to_lowercase();
        match agent_type.as_str() {
            "frontend" | "backend" | "devops" | "qa" => {
                self.add_log("System", &format!("Creating {} agent...", agent_type))
                    .await;
                // TODO: Implement actual agent creation
                self.add_log(
                    "System",
                    &format!("{} agent created successfully", agent_type),
                )
                .await;
            }
            _ => {
                self.add_log(
                    "System",
                    "Invalid agent type. Use: frontend, backend, devops, qa",
                )
                .await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:673-693 method add_log <-> ./crates/ccswarm/src/tui/app.rs:710-740 method process_input
  Similarity: 90.64%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:add_log (lines 673-693) ---[0m
    async fn add_log(&mut self, source: &str, message: &str) {
        let log_entry = LogEntry {
            timestamp: Utc::now(),
            level: "INFO".to_string(),
            agent: if source == "System" {
                None
            } else {
                Some(source.to_string())
            },
            message: message.to_string(),
        };
        self.logs.push(log_entry);

        // Keep only last 1000 log entries
        if self.logs.len() > 1000 {
            self.logs.remove(0);
        }

        // Auto-scroll to bottom
        self.selected_log = self.logs.len().saturating_sub(1);
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:process_input (lines 710-740) ---[0m
    pub async fn process_input(&mut self) -> Result<()> {
        let input = self.input_buffer.trim().to_string();

        match self.input_mode {
            InputMode::AddingTask => {
                if !input.is_empty() {
                    self.execute_add_task(&input).await?;
                }
            }
            InputMode::CreatingAgent => {
                if !input.is_empty() {
                    self.execute_create_agent(&input).await?;
                }
            }
            InputMode::Command => {
                if !input.is_empty() {
                    self.execute_command(&input).await?;
                }
            }
            InputMode::DelegationInput => {
                if !input.is_empty() {
                    self.execute_delegation_action(&input).await?;
                }
            }
            _ => {}
        }

        self.input_mode = InputMode::Normal;
        self.input_buffer.clear();
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:313-347 method start_agent <-> ./crates/ccswarm/src/tui/app.rs:350-365 method start_agent_by_id
  Similarity: 87.64%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent (lines 313-347) ---[0m
    pub async fn start_agent(&mut self, agent_id: &str) -> Result<()> {
        let mut agent_info = None;

        // Find the agent and collect info
        if let Some(agent) = self.agents.iter_mut().find(|a| a.id == agent_id) {
            // Change status to Working
            agent.status = AgentStatus::Working;
            agent.last_activity = Utc::now();

            // Collect info for logging
            agent_info = Some((agent.name.clone(), agent.specialization.clone()));

            // Update system stats
            self.active_agents += 1;
        }

        // Log after borrowing ends
        if let Some((name, specialization)) = agent_info {
            self.add_log(
                "System",
                &format!("🚀 Starting agent: {} ({})", name, specialization),
            )
            .await;

            // If this is a Master agent, provide special logging
            if specialization.contains("Master") {
                self.add_log("Master", "🎯 Master Claude Code orchestrator activated")
                    .await;
                self.add_log("Master", "📋 Ready to coordinate multi-agent tasks")
                    .await;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent_by_id (lines 350-365) ---[0m
    pub async fn start_agent_by_id(&mut self, identifier: &str) -> Result<()> {
        // Find agent by ID or name
        let agent_to_start = self
            .agents
            .iter()
            .find(|a| a.id == identifier || a.name == identifier)
            .map(|a| a.id.clone());

        if let Some(agent_id) = agent_to_start {
            self.start_agent(&agent_id).await?;
        } else {
            self.add_log("System", &format!("Agent not found: {}", identifier))
                .await;
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:673-693 method add_log <-> ./crates/ccswarm/src/tui/app.rs:1083-1117 method analyze_task_for_delegation
  Similarity: 79.48%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:add_log (lines 673-693) ---[0m
    async fn add_log(&mut self, source: &str, message: &str) {
        let log_entry = LogEntry {
            timestamp: Utc::now(),
            level: "INFO".to_string(),
            agent: if source == "System" {
                None
            } else {
                Some(source.to_string())
            },
            message: message.to_string(),
        };
        self.logs.push(log_entry);

        // Keep only last 1000 log entries
        if self.logs.len() > 1000 {
            self.logs.remove(0);
        }

        // Auto-scroll to bottom
        self.selected_log = self.logs.len().saturating_sub(1);
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:analyze_task_for_delegation (lines 1083-1117) ---[0m
    async fn analyze_task_for_delegation(&mut self, task_description: &str) -> Result<()> {
        self.add_log(
            "Master",
            &format!("🔍 Analyzing task: '{}'", task_description),
        )
        .await;

        // Use basic rule-based analysis for demo
        let (recommended_agent, confidence, reasoning) =
            self.analyze_task_content(task_description);

        let delegation_info = DelegationInfo {
            task_description: task_description.to_string(),
            recommended_agent: recommended_agent.clone(),
            confidence,
            reasoning: reasoning.clone(),
            created_at: chrono::Utc::now(),
        };

        self.delegation_decisions.push(delegation_info);

        self.add_log(
            "Master",
            &format!(
                "✅ Analysis complete: {} agent recommended ({:.1}% confidence)",
                recommended_agent,
                confidence * 100.0
            ),
        )
        .await;
        self.add_log("Master", &format!("📝 Reasoning: {}", reasoning))
            .await;

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:281-310 method activate_selected <-> ./crates/ccswarm/src/tui/app.rs:958-979 method list_agents_command
  Similarity: 85.55%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:activate_selected (lines 281-310) ---[0m
    pub async fn activate_selected(&mut self) -> Result<()> {
        match self.current_tab {
            Tab::Agents => {
                if let Some(agent) = self.agents.get(self.selected_agent).cloned() {
                    match agent.status {
                        AgentStatus::Available => {
                            // Start the agent
                            self.start_agent(&agent.id).await?;
                        }
                        AgentStatus::Working => {
                            // Show agent details
                            self.show_agent_details(&agent.id).await?;
                        }
                        _ => {
                            // Show agent details for other statuses
                            self.show_agent_details(&agent.id).await?;
                        }
                    }
                }
            }
            Tab::Tasks => {
                if let Some(task) = self.tasks.get(self.selected_task) {
                    let task_id = task.id.clone();
                    self.show_task_details(&task_id).await?;
                }
            }
            _ => {}
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_agents_command (lines 958-979) ---[0m
    async fn list_agents_command(&mut self) -> Result<()> {
        if self.agents.is_empty() {
            self.add_log("System", "No agents found").await;
        } else {
            self.add_log("System", "Active agents:").await;
            let agent_info: Vec<String> = self
                .agents
                .iter()
                .map(|agent| {
                    format!(
                        "  {} ({}) - {:?}",
                        agent.name, agent.specialization, agent.status
                    )
                })
                .collect();

            for info in agent_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:281-310 method activate_selected <-> ./crates/ccswarm/src/tui/app.rs:982-1003 method list_tasks_command
  Similarity: 85.55%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:activate_selected (lines 281-310) ---[0m
    pub async fn activate_selected(&mut self) -> Result<()> {
        match self.current_tab {
            Tab::Agents => {
                if let Some(agent) = self.agents.get(self.selected_agent).cloned() {
                    match agent.status {
                        AgentStatus::Available => {
                            // Start the agent
                            self.start_agent(&agent.id).await?;
                        }
                        AgentStatus::Working => {
                            // Show agent details
                            self.show_agent_details(&agent.id).await?;
                        }
                        _ => {
                            // Show agent details for other statuses
                            self.show_agent_details(&agent.id).await?;
                        }
                    }
                }
            }
            Tab::Tasks => {
                if let Some(task) = self.tasks.get(self.selected_task) {
                    let task_id = task.id.clone();
                    self.show_task_details(&task_id).await?;
                }
            }
            _ => {}
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_tasks_command (lines 982-1003) ---[0m
    async fn list_tasks_command(&mut self) -> Result<()> {
        if self.tasks.is_empty() {
            self.add_log("System", "No pending tasks").await;
        } else {
            self.add_log("System", "Pending tasks:").await;
            let task_info: Vec<String> = self
                .tasks
                .iter()
                .map(|task| {
                    format!(
                        "  {} - {} ({})",
                        task.description, task.priority, task.task_type
                    )
                })
                .collect();

            for info in task_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:152-189 method new <-> ./crates/ccswarm/src/tui/app.rs:743-755 method execute_add_task
  Similarity: 85.68%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:new (lines 152-189) ---[0m
    pub async fn new() -> Result<Self> {
        let coordination_bus = CoordinationBus::new().await?;
        let status_tracker = StatusTracker::new().await?;
        let task_queue = TaskQueue::new().await?;

        Ok(Self {
            current_tab: Tab::Overview,
            selected_agent: 0,
            selected_task: 0,
            selected_log: 0,
            selected_delegation: 0,
            agents: Vec::new(),
            tasks: Vec::new(),
            logs: Vec::new(),
            delegation_decisions: Vec::new(),
            system_status: "Starting...".to_string(),
            total_agents: 0,
            active_agents: 0,
            pending_tasks: 0,
            completed_tasks: 0,
            tasks_executed: 0,
            tasks_failed: 0,
            success_rate: 100.0,
            orchestration_usage: 0.0,
            input_mode: InputMode::Normal,
            input_buffer: String::new(),
            delegation_mode: DelegationMode::Analyze,
            delegation_input: String::new(),
            coordination_bus,
            status_tracker,
            task_queue,
            execution_engine: None, // Will be set by CLI runner
            terminal_width: 80,
            terminal_height: 24,
            last_update: Instant::now(),
            update_interval: Duration::from_millis(500), // More frequent updates for real-time feel
        })
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_add_task (lines 743-755) ---[0m
    async fn execute_add_task(&mut self, description: &str) -> Result<()> {
        // Parse priority and type from description if included
        let (desc, priority, task_type) = self.parse_task_description(description);

        let task =
            crate::agent::Task::new(uuid::Uuid::new_v4().to_string(), desc, priority, task_type);

        self.task_queue.add_task(&task).await?;
        self.add_log("System", &format!("Task added: {}", task.description))
            .await;
        self.refresh_data().await?;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:313-347 method start_agent <-> ./crates/ccswarm/src/tui/app.rs:743-755 method execute_add_task
  Similarity: 90.75%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent (lines 313-347) ---[0m
    pub async fn start_agent(&mut self, agent_id: &str) -> Result<()> {
        let mut agent_info = None;

        // Find the agent and collect info
        if let Some(agent) = self.agents.iter_mut().find(|a| a.id == agent_id) {
            // Change status to Working
            agent.status = AgentStatus::Working;
            agent.last_activity = Utc::now();

            // Collect info for logging
            agent_info = Some((agent.name.clone(), agent.specialization.clone()));

            // Update system stats
            self.active_agents += 1;
        }

        // Log after borrowing ends
        if let Some((name, specialization)) = agent_info {
            self.add_log(
                "System",
                &format!("🚀 Starting agent: {} ({})", name, specialization),
            )
            .await;

            // If this is a Master agent, provide special logging
            if specialization.contains("Master") {
                self.add_log("Master", "🎯 Master Claude Code orchestrator activated")
                    .await;
                self.add_log("Master", "📋 Ready to coordinate multi-agent tasks")
                    .await;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_add_task (lines 743-755) ---[0m
    async fn execute_add_task(&mut self, description: &str) -> Result<()> {
        // Parse priority and type from description if included
        let (desc, priority, task_type) = self.parse_task_description(description);

        let task =
            crate::agent::Task::new(uuid::Uuid::new_v4().to_string(), desc, priority, task_type);

        self.task_queue.add_task(&task).await?;
        self.add_log("System", &format!("Task added: {}", task.description))
            .await;
        self.refresh_data().await?;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:710-740 method process_input <-> ./crates/ccswarm/src/tui/app.rs:758-780 method execute_create_agent
  Similarity: 80.24%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:process_input (lines 710-740) ---[0m
    pub async fn process_input(&mut self) -> Result<()> {
        let input = self.input_buffer.trim().to_string();

        match self.input_mode {
            InputMode::AddingTask => {
                if !input.is_empty() {
                    self.execute_add_task(&input).await?;
                }
            }
            InputMode::CreatingAgent => {
                if !input.is_empty() {
                    self.execute_create_agent(&input).await?;
                }
            }
            InputMode::Command => {
                if !input.is_empty() {
                    self.execute_command(&input).await?;
                }
            }
            InputMode::DelegationInput => {
                if !input.is_empty() {
                    self.execute_delegation_action(&input).await?;
                }
            }
            _ => {}
        }

        self.input_mode = InputMode::Normal;
        self.input_buffer.clear();
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_create_agent (lines 758-780) ---[0m
    async fn execute_create_agent(&mut self, agent_type: &str) -> Result<()> {
        let agent_type = agent_type.to_lowercase();
        match agent_type.as_str() {
            "frontend" | "backend" | "devops" | "qa" => {
                self.add_log("System", &format!("Creating {} agent...", agent_type))
                    .await;
                // TODO: Implement actual agent creation
                self.add_log(
                    "System",
                    &format!("{} agent created successfully", agent_type),
                )
                .await;
            }
            _ => {
                self.add_log(
                    "System",
                    "Invalid agent type. Use: frontend, backend, devops, qa",
                )
                .await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:958-979 method list_agents_command <-> ./crates/ccswarm/src/tui/app.rs:982-1003 method list_tasks_command
  Similarity: 97.56%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:list_agents_command (lines 958-979) ---[0m
    async fn list_agents_command(&mut self) -> Result<()> {
        if self.agents.is_empty() {
            self.add_log("System", "No agents found").await;
        } else {
            self.add_log("System", "Active agents:").await;
            let agent_info: Vec<String> = self
                .agents
                .iter()
                .map(|agent| {
                    format!(
                        "  {} ({}) - {:?}",
                        agent.name, agent.specialization, agent.status
                    )
                })
                .collect();

            for info in agent_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_tasks_command (lines 982-1003) ---[0m
    async fn list_tasks_command(&mut self) -> Result<()> {
        if self.tasks.is_empty() {
            self.add_log("System", "No pending tasks").await;
        } else {
            self.add_log("System", "Pending tasks:").await;
            let task_info: Vec<String> = self
                .tasks
                .iter()
                .map(|task| {
                    format!(
                        "  {} - {} ({})",
                        task.description, task.priority, task.task_type
                    )
                })
                .collect();

            for info in task_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:743-755 method execute_add_task <-> ./crates/ccswarm/src/tui/app.rs:1083-1117 method analyze_task_for_delegation
  Similarity: 87.97%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_add_task (lines 743-755) ---[0m
    async fn execute_add_task(&mut self, description: &str) -> Result<()> {
        // Parse priority and type from description if included
        let (desc, priority, task_type) = self.parse_task_description(description);

        let task =
            crate::agent::Task::new(uuid::Uuid::new_v4().to_string(), desc, priority, task_type);

        self.task_queue.add_task(&task).await?;
        self.add_log("System", &format!("Task added: {}", task.description))
            .await;
        self.refresh_data().await?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:analyze_task_for_delegation (lines 1083-1117) ---[0m
    async fn analyze_task_for_delegation(&mut self, task_description: &str) -> Result<()> {
        self.add_log(
            "Master",
            &format!("🔍 Analyzing task: '{}'", task_description),
        )
        .await;

        // Use basic rule-based analysis for demo
        let (recommended_agent, confidence, reasoning) =
            self.analyze_task_content(task_description);

        let delegation_info = DelegationInfo {
            task_description: task_description.to_string(),
            recommended_agent: recommended_agent.clone(),
            confidence,
            reasoning: reasoning.clone(),
            created_at: chrono::Utc::now(),
        };

        self.delegation_decisions.push(delegation_info);

        self.add_log(
            "Master",
            &format!(
                "✅ Analysis complete: {} agent recommended ({:.1}% confidence)",
                recommended_agent,
                confidence * 100.0
            ),
        )
        .await;
        self.add_log("Master", &format!("📝 Reasoning: {}", reasoning))
            .await;

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:673-693 method add_log <-> ./crates/ccswarm/src/tui/app.rs:925-955 method show_detailed_status
  Similarity: 79.83%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:add_log (lines 673-693) ---[0m
    async fn add_log(&mut self, source: &str, message: &str) {
        let log_entry = LogEntry {
            timestamp: Utc::now(),
            level: "INFO".to_string(),
            agent: if source == "System" {
                None
            } else {
                Some(source.to_string())
            },
            message: message.to_string(),
        };
        self.logs.push(log_entry);

        // Keep only last 1000 log entries
        if self.logs.len() > 1000 {
            self.logs.remove(0);
        }

        // Auto-scroll to bottom
        self.selected_log = self.logs.len().saturating_sub(1);
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_detailed_status (lines 925-955) ---[0m
    async fn show_detailed_status(&mut self) -> Result<()> {
        self.add_log("System", "=== Detailed System Status ===")
            .await;
        self.add_log("System", &format!("System Status: {}", self.system_status))
            .await;
        self.add_log("System", &format!("Total Agents: {}", self.total_agents))
            .await;
        self.add_log("System", &format!("Active Agents: {}", self.active_agents))
            .await;
        self.add_log("System", &format!("Pending Tasks: {}", self.pending_tasks))
            .await;
        self.add_log(
            "System",
            &format!("Completed Tasks: {}", self.completed_tasks),
        )
        .await;

        // Show Master Claude Code status specifically
        if let Some(master) = self
            .agents
            .iter()
            .find(|a| a.specialization.contains("Master"))
        {
            self.add_log(
                "System",
                &format!("👑 Master Claude Code: {:?}", master.status),
            )
            .await;
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:313-347 method start_agent <-> ./crates/ccswarm/src/tui/app.rs:958-979 method list_agents_command
  Similarity: 71.85%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent (lines 313-347) ---[0m
    pub async fn start_agent(&mut self, agent_id: &str) -> Result<()> {
        let mut agent_info = None;

        // Find the agent and collect info
        if let Some(agent) = self.agents.iter_mut().find(|a| a.id == agent_id) {
            // Change status to Working
            agent.status = AgentStatus::Working;
            agent.last_activity = Utc::now();

            // Collect info for logging
            agent_info = Some((agent.name.clone(), agent.specialization.clone()));

            // Update system stats
            self.active_agents += 1;
        }

        // Log after borrowing ends
        if let Some((name, specialization)) = agent_info {
            self.add_log(
                "System",
                &format!("🚀 Starting agent: {} ({})", name, specialization),
            )
            .await;

            // If this is a Master agent, provide special logging
            if specialization.contains("Master") {
                self.add_log("Master", "🎯 Master Claude Code orchestrator activated")
                    .await;
                self.add_log("Master", "📋 Ready to coordinate multi-agent tasks")
                    .await;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_agents_command (lines 958-979) ---[0m
    async fn list_agents_command(&mut self) -> Result<()> {
        if self.agents.is_empty() {
            self.add_log("System", "No agents found").await;
        } else {
            self.add_log("System", "Active agents:").await;
            let agent_info: Vec<String> = self
                .agents
                .iter()
                .map(|agent| {
                    format!(
                        "  {} ({}) - {:?}",
                        agent.name, agent.specialization, agent.status
                    )
                })
                .collect();

            for info in agent_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:313-347 method start_agent <-> ./crates/ccswarm/src/tui/app.rs:982-1003 method list_tasks_command
  Similarity: 71.85%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent (lines 313-347) ---[0m
    pub async fn start_agent(&mut self, agent_id: &str) -> Result<()> {
        let mut agent_info = None;

        // Find the agent and collect info
        if let Some(agent) = self.agents.iter_mut().find(|a| a.id == agent_id) {
            // Change status to Working
            agent.status = AgentStatus::Working;
            agent.last_activity = Utc::now();

            // Collect info for logging
            agent_info = Some((agent.name.clone(), agent.specialization.clone()));

            // Update system stats
            self.active_agents += 1;
        }

        // Log after borrowing ends
        if let Some((name, specialization)) = agent_info {
            self.add_log(
                "System",
                &format!("🚀 Starting agent: {} ({})", name, specialization),
            )
            .await;

            // If this is a Master agent, provide special logging
            if specialization.contains("Master") {
                self.add_log("Master", "🎯 Master Claude Code orchestrator activated")
                    .await;
                self.add_log("Master", "📋 Ready to coordinate multi-agent tasks")
                    .await;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_tasks_command (lines 982-1003) ---[0m
    async fn list_tasks_command(&mut self) -> Result<()> {
        if self.tasks.is_empty() {
            self.add_log("System", "No pending tasks").await;
        } else {
            self.add_log("System", "Pending tasks:").await;
            let task_info: Vec<String> = self
                .tasks
                .iter()
                .map(|task| {
                    format!(
                        "  {} - {} ({})",
                        task.description, task.priority, task.task_type
                    )
                })
                .collect();

            for info in task_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:710-740 method process_input <-> ./crates/ccswarm/src/tui/app.rs:958-979 method list_agents_command
  Similarity: 76.56%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:process_input (lines 710-740) ---[0m
    pub async fn process_input(&mut self) -> Result<()> {
        let input = self.input_buffer.trim().to_string();

        match self.input_mode {
            InputMode::AddingTask => {
                if !input.is_empty() {
                    self.execute_add_task(&input).await?;
                }
            }
            InputMode::CreatingAgent => {
                if !input.is_empty() {
                    self.execute_create_agent(&input).await?;
                }
            }
            InputMode::Command => {
                if !input.is_empty() {
                    self.execute_command(&input).await?;
                }
            }
            InputMode::DelegationInput => {
                if !input.is_empty() {
                    self.execute_delegation_action(&input).await?;
                }
            }
            _ => {}
        }

        self.input_mode = InputMode::Normal;
        self.input_buffer.clear();
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_agents_command (lines 958-979) ---[0m
    async fn list_agents_command(&mut self) -> Result<()> {
        if self.agents.is_empty() {
            self.add_log("System", "No agents found").await;
        } else {
            self.add_log("System", "Active agents:").await;
            let agent_info: Vec<String> = self
                .agents
                .iter()
                .map(|agent| {
                    format!(
                        "  {} ({}) - {:?}",
                        agent.name, agent.specialization, agent.status
                    )
                })
                .collect();

            for info in agent_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:710-740 method process_input <-> ./crates/ccswarm/src/tui/app.rs:982-1003 method list_tasks_command
  Similarity: 76.56%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:process_input (lines 710-740) ---[0m
    pub async fn process_input(&mut self) -> Result<()> {
        let input = self.input_buffer.trim().to_string();

        match self.input_mode {
            InputMode::AddingTask => {
                if !input.is_empty() {
                    self.execute_add_task(&input).await?;
                }
            }
            InputMode::CreatingAgent => {
                if !input.is_empty() {
                    self.execute_create_agent(&input).await?;
                }
            }
            InputMode::Command => {
                if !input.is_empty() {
                    self.execute_command(&input).await?;
                }
            }
            InputMode::DelegationInput => {
                if !input.is_empty() {
                    self.execute_delegation_action(&input).await?;
                }
            }
            _ => {}
        }

        self.input_mode = InputMode::Normal;
        self.input_buffer.clear();
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_tasks_command (lines 982-1003) ---[0m
    async fn list_tasks_command(&mut self) -> Result<()> {
        if self.tasks.is_empty() {
            self.add_log("System", "No pending tasks").await;
        } else {
            self.add_log("System", "Pending tasks:").await;
            let task_info: Vec<String> = self
                .tasks
                .iter()
                .map(|task| {
                    format!(
                        "  {} - {} ({})",
                        task.description, task.priority, task.task_type
                    )
                })
                .collect();

            for info in task_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:216-240 method previous_item <-> ./crates/ccswarm/src/tui/app.rs:710-740 method process_input
  Similarity: 70.12%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_item (lines 216-240) ---[0m
    pub fn previous_item(&mut self) {
        match self.current_tab {
            Tab::Agents => {
                if self.selected_agent > 0 {
                    self.selected_agent -= 1;
                }
            }
            Tab::Tasks => {
                if self.selected_task > 0 {
                    self.selected_task -= 1;
                }
            }
            Tab::Logs => {
                if self.selected_log > 0 {
                    self.selected_log -= 1;
                }
            }
            Tab::Delegation => {
                if self.selected_delegation > 0 {
                    self.selected_delegation -= 1;
                }
            }
            _ => {}
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:process_input (lines 710-740) ---[0m
    pub async fn process_input(&mut self) -> Result<()> {
        let input = self.input_buffer.trim().to_string();

        match self.input_mode {
            InputMode::AddingTask => {
                if !input.is_empty() {
                    self.execute_add_task(&input).await?;
                }
            }
            InputMode::CreatingAgent => {
                if !input.is_empty() {
                    self.execute_create_agent(&input).await?;
                }
            }
            InputMode::Command => {
                if !input.is_empty() {
                    self.execute_command(&input).await?;
                }
            }
            InputMode::DelegationInput => {
                if !input.is_empty() {
                    self.execute_delegation_action(&input).await?;
                }
            }
            _ => {}
        }

        self.input_mode = InputMode::Normal;
        self.input_buffer.clear();
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:243-267 method next_item <-> ./crates/ccswarm/src/tui/app.rs:710-740 method process_input
  Similarity: 70.12%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:next_item (lines 243-267) ---[0m
    pub fn next_item(&mut self) {
        match self.current_tab {
            Tab::Agents => {
                if self.selected_agent < self.agents.len().saturating_sub(1) {
                    self.selected_agent += 1;
                }
            }
            Tab::Tasks => {
                if self.selected_task < self.tasks.len().saturating_sub(1) {
                    self.selected_task += 1;
                }
            }
            Tab::Logs => {
                if self.selected_log < self.logs.len().saturating_sub(1) {
                    self.selected_log += 1;
                }
            }
            Tab::Delegation => {
                if self.selected_delegation < self.delegation_decisions.len().saturating_sub(1) {
                    self.selected_delegation += 1;
                }
            }
            _ => {}
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:process_input (lines 710-740) ---[0m
    pub async fn process_input(&mut self) -> Result<()> {
        let input = self.input_buffer.trim().to_string();

        match self.input_mode {
            InputMode::AddingTask => {
                if !input.is_empty() {
                    self.execute_add_task(&input).await?;
                }
            }
            InputMode::CreatingAgent => {
                if !input.is_empty() {
                    self.execute_create_agent(&input).await?;
                }
            }
            InputMode::Command => {
                if !input.is_empty() {
                    self.execute_command(&input).await?;
                }
            }
            InputMode::DelegationInput => {
                if !input.is_empty() {
                    self.execute_delegation_action(&input).await?;
                }
            }
            _ => {}
        }

        self.input_mode = InputMode::Normal;
        self.input_buffer.clear();
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:350-365 method start_agent_by_id <-> ./crates/ccswarm/src/tui/app.rs:710-740 method process_input
  Similarity: 83.36%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent_by_id (lines 350-365) ---[0m
    pub async fn start_agent_by_id(&mut self, identifier: &str) -> Result<()> {
        // Find agent by ID or name
        let agent_to_start = self
            .agents
            .iter()
            .find(|a| a.id == identifier || a.name == identifier)
            .map(|a| a.id.clone());

        if let Some(agent_id) = agent_to_start {
            self.start_agent(&agent_id).await?;
        } else {
            self.add_log("System", &format!("Agent not found: {}", identifier))
                .await;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:process_input (lines 710-740) ---[0m
    pub async fn process_input(&mut self) -> Result<()> {
        let input = self.input_buffer.trim().to_string();

        match self.input_mode {
            InputMode::AddingTask => {
                if !input.is_empty() {
                    self.execute_add_task(&input).await?;
                }
            }
            InputMode::CreatingAgent => {
                if !input.is_empty() {
                    self.execute_create_agent(&input).await?;
                }
            }
            InputMode::Command => {
                if !input.is_empty() {
                    self.execute_command(&input).await?;
                }
            }
            InputMode::DelegationInput => {
                if !input.is_empty() {
                    self.execute_delegation_action(&input).await?;
                }
            }
            _ => {}
        }

        self.input_mode = InputMode::Normal;
        self.input_buffer.clear();
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:281-310 method activate_selected <-> ./crates/ccswarm/src/tui/app.rs:350-365 method start_agent_by_id
  Similarity: 84.65%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:activate_selected (lines 281-310) ---[0m
    pub async fn activate_selected(&mut self) -> Result<()> {
        match self.current_tab {
            Tab::Agents => {
                if let Some(agent) = self.agents.get(self.selected_agent).cloned() {
                    match agent.status {
                        AgentStatus::Available => {
                            // Start the agent
                            self.start_agent(&agent.id).await?;
                        }
                        AgentStatus::Working => {
                            // Show agent details
                            self.show_agent_details(&agent.id).await?;
                        }
                        _ => {
                            // Show agent details for other statuses
                            self.show_agent_details(&agent.id).await?;
                        }
                    }
                }
            }
            Tab::Tasks => {
                if let Some(task) = self.tasks.get(self.selected_task) {
                    let task_id = task.id.clone();
                    self.show_task_details(&task_id).await?;
                }
            }
            _ => {}
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent_by_id (lines 350-365) ---[0m
    pub async fn start_agent_by_id(&mut self, identifier: &str) -> Result<()> {
        // Find agent by ID or name
        let agent_to_start = self
            .agents
            .iter()
            .find(|a| a.id == identifier || a.name == identifier)
            .map(|a| a.id.clone());

        if let Some(agent_id) = agent_to_start {
            self.start_agent(&agent_id).await?;
        } else {
            self.add_log("System", &format!("Agent not found: {}", identifier))
                .await;
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:758-780 method execute_create_agent <-> ./crates/ccswarm/src/tui/app.rs:857-878 method show_help
  Similarity: 85.85%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_create_agent (lines 758-780) ---[0m
    async fn execute_create_agent(&mut self, agent_type: &str) -> Result<()> {
        let agent_type = agent_type.to_lowercase();
        match agent_type.as_str() {
            "frontend" | "backend" | "devops" | "qa" => {
                self.add_log("System", &format!("Creating {} agent...", agent_type))
                    .await;
                // TODO: Implement actual agent creation
                self.add_log(
                    "System",
                    &format!("{} agent created successfully", agent_type),
                )
                .await;
            }
            _ => {
                self.add_log(
                    "System",
                    "Invalid agent type. Use: frontend, backend, devops, qa",
                )
                .await;
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_help (lines 857-878) ---[0m
    async fn show_help(&mut self) {
        let help_text = vec![
            "Available commands:",
            "  help                    - Show this help",
            "  status                  - Show system status",
            "  agents                  - List all agents",
            "  tasks                   - List all tasks",
            "  task <description>      - Add new task",
            "  agent <type>            - Create new agent (frontend/backend/devops/qa)",
            "  start_agent <id|name>   - Start/activate an agent (e.g., 'start_agent master')",
            "  activate <id|name>      - Alias for start_agent",
            "  start                   - Start orchestrator",
            "  stop                    - Stop orchestrator",
            "  refresh                 - Refresh all data",
            "  clear                   - Clear logs",
            "  worktree [list|prune] - Manage worktrees",
        ];

        for line in help_text {
            self.add_log("System", line).await;
        }
    }

  ./crates/ccswarm/src/tui/app.rs:758-780 method execute_create_agent <-> ./crates/ccswarm/src/tui/app.rs:958-979 method list_agents_command
  Similarity: 85.17%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_create_agent (lines 758-780) ---[0m
    async fn execute_create_agent(&mut self, agent_type: &str) -> Result<()> {
        let agent_type = agent_type.to_lowercase();
        match agent_type.as_str() {
            "frontend" | "backend" | "devops" | "qa" => {
                self.add_log("System", &format!("Creating {} agent...", agent_type))
                    .await;
                // TODO: Implement actual agent creation
                self.add_log(
                    "System",
                    &format!("{} agent created successfully", agent_type),
                )
                .await;
            }
            _ => {
                self.add_log(
                    "System",
                    "Invalid agent type. Use: frontend, backend, devops, qa",
                )
                .await;
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_agents_command (lines 958-979) ---[0m
    async fn list_agents_command(&mut self) -> Result<()> {
        if self.agents.is_empty() {
            self.add_log("System", "No agents found").await;
        } else {
            self.add_log("System", "Active agents:").await;
            let agent_info: Vec<String> = self
                .agents
                .iter()
                .map(|agent| {
                    format!(
                        "  {} ({}) - {:?}",
                        agent.name, agent.specialization, agent.status
                    )
                })
                .collect();

            for info in agent_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:758-780 method execute_create_agent <-> ./crates/ccswarm/src/tui/app.rs:982-1003 method list_tasks_command
  Similarity: 85.17%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_create_agent (lines 758-780) ---[0m
    async fn execute_create_agent(&mut self, agent_type: &str) -> Result<()> {
        let agent_type = agent_type.to_lowercase();
        match agent_type.as_str() {
            "frontend" | "backend" | "devops" | "qa" => {
                self.add_log("System", &format!("Creating {} agent...", agent_type))
                    .await;
                // TODO: Implement actual agent creation
                self.add_log(
                    "System",
                    &format!("{} agent created successfully", agent_type),
                )
                .await;
            }
            _ => {
                self.add_log(
                    "System",
                    "Invalid agent type. Use: frontend, backend, devops, qa",
                )
                .await;
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_tasks_command (lines 982-1003) ---[0m
    async fn list_tasks_command(&mut self) -> Result<()> {
        if self.tasks.is_empty() {
            self.add_log("System", "No pending tasks").await;
        } else {
            self.add_log("System", "Pending tasks:").await;
            let task_info: Vec<String> = self
                .tasks
                .iter()
                .map(|task| {
                    format!(
                        "  {} - {} ({})",
                        task.description, task.priority, task.task_type
                    )
                })
                .collect();

            for info in task_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:243-267 method next_item <-> ./crates/ccswarm/src/tui/app.rs:758-780 method execute_create_agent
  Similarity: 77.43%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:next_item (lines 243-267) ---[0m
    pub fn next_item(&mut self) {
        match self.current_tab {
            Tab::Agents => {
                if self.selected_agent < self.agents.len().saturating_sub(1) {
                    self.selected_agent += 1;
                }
            }
            Tab::Tasks => {
                if self.selected_task < self.tasks.len().saturating_sub(1) {
                    self.selected_task += 1;
                }
            }
            Tab::Logs => {
                if self.selected_log < self.logs.len().saturating_sub(1) {
                    self.selected_log += 1;
                }
            }
            Tab::Delegation => {
                if self.selected_delegation < self.delegation_decisions.len().saturating_sub(1) {
                    self.selected_delegation += 1;
                }
            }
            _ => {}
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_create_agent (lines 758-780) ---[0m
    async fn execute_create_agent(&mut self, agent_type: &str) -> Result<()> {
        let agent_type = agent_type.to_lowercase();
        match agent_type.as_str() {
            "frontend" | "backend" | "devops" | "qa" => {
                self.add_log("System", &format!("Creating {} agent...", agent_type))
                    .await;
                // TODO: Implement actual agent creation
                self.add_log(
                    "System",
                    &format!("{} agent created successfully", agent_type),
                )
                .await;
            }
            _ => {
                self.add_log(
                    "System",
                    "Invalid agent type. Use: frontend, backend, devops, qa",
                )
                .await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:710-740 method process_input <-> ./crates/ccswarm/src/tui/app.rs:743-755 method execute_add_task
  Similarity: 83.80%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:process_input (lines 710-740) ---[0m
    pub async fn process_input(&mut self) -> Result<()> {
        let input = self.input_buffer.trim().to_string();

        match self.input_mode {
            InputMode::AddingTask => {
                if !input.is_empty() {
                    self.execute_add_task(&input).await?;
                }
            }
            InputMode::CreatingAgent => {
                if !input.is_empty() {
                    self.execute_create_agent(&input).await?;
                }
            }
            InputMode::Command => {
                if !input.is_empty() {
                    self.execute_command(&input).await?;
                }
            }
            InputMode::DelegationInput => {
                if !input.is_empty() {
                    self.execute_delegation_action(&input).await?;
                }
            }
            _ => {}
        }

        self.input_mode = InputMode::Normal;
        self.input_buffer.clear();
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_add_task (lines 743-755) ---[0m
    async fn execute_add_task(&mut self, description: &str) -> Result<()> {
        // Parse priority and type from description if included
        let (desc, priority, task_type) = self.parse_task_description(description);

        let task =
            crate::agent::Task::new(uuid::Uuid::new_v4().to_string(), desc, priority, task_type);

        self.task_queue.add_task(&task).await?;
        self.add_log("System", &format!("Task added: {}", task.description))
            .await;
        self.refresh_data().await?;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:1050-1066 method start_delegation_input <-> ./crates/ccswarm/src/tui/app.rs:1083-1117 method analyze_task_for_delegation
  Similarity: 70.85%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_delegation_input (lines 1050-1066) ---[0m
    pub async fn start_delegation_input(&mut self) -> Result<()> {
        self.input_mode = InputMode::DelegationInput;
        self.delegation_input.clear();

        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.add_log("Master", "Enter task description to analyze:")
                    .await;
            }
            DelegationMode::Delegate => {
                self.add_log("Master", "Enter task description to delegate:")
                    .await;
            }
            _ => {}
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:analyze_task_for_delegation (lines 1083-1117) ---[0m
    async fn analyze_task_for_delegation(&mut self, task_description: &str) -> Result<()> {
        self.add_log(
            "Master",
            &format!("🔍 Analyzing task: '{}'", task_description),
        )
        .await;

        // Use basic rule-based analysis for demo
        let (recommended_agent, confidence, reasoning) =
            self.analyze_task_content(task_description);

        let delegation_info = DelegationInfo {
            task_description: task_description.to_string(),
            recommended_agent: recommended_agent.clone(),
            confidence,
            reasoning: reasoning.clone(),
            created_at: chrono::Utc::now(),
        };

        self.delegation_decisions.push(delegation_info);

        self.add_log(
            "Master",
            &format!(
                "✅ Analysis complete: {} agent recommended ({:.1}% confidence)",
                recommended_agent,
                confidence * 100.0
            ),
        )
        .await;
        self.add_log("Master", &format!("📝 Reasoning: {}", reasoning))
            .await;

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:350-365 method start_agent_by_id <-> ./crates/ccswarm/src/tui/app.rs:1083-1117 method analyze_task_for_delegation
  Similarity: 70.09%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent_by_id (lines 350-365) ---[0m
    pub async fn start_agent_by_id(&mut self, identifier: &str) -> Result<()> {
        // Find agent by ID or name
        let agent_to_start = self
            .agents
            .iter()
            .find(|a| a.id == identifier || a.name == identifier)
            .map(|a| a.id.clone());

        if let Some(agent_id) = agent_to_start {
            self.start_agent(&agent_id).await?;
        } else {
            self.add_log("System", &format!("Agent not found: {}", identifier))
                .await;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:analyze_task_for_delegation (lines 1083-1117) ---[0m
    async fn analyze_task_for_delegation(&mut self, task_description: &str) -> Result<()> {
        self.add_log(
            "Master",
            &format!("🔍 Analyzing task: '{}'", task_description),
        )
        .await;

        // Use basic rule-based analysis for demo
        let (recommended_agent, confidence, reasoning) =
            self.analyze_task_content(task_description);

        let delegation_info = DelegationInfo {
            task_description: task_description.to_string(),
            recommended_agent: recommended_agent.clone(),
            confidence,
            reasoning: reasoning.clone(),
            created_at: chrono::Utc::now(),
        };

        self.delegation_decisions.push(delegation_info);

        self.add_log(
            "Master",
            &format!(
                "✅ Analysis complete: {} agent recommended ({:.1}% confidence)",
                recommended_agent,
                confidence * 100.0
            ),
        )
        .await;
        self.add_log("Master", &format!("📝 Reasoning: {}", reasoning))
            .await;

        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:350-365 method start_agent_by_id <-> ./crates/ccswarm/src/tui/app.rs:758-780 method execute_create_agent
  Similarity: 88.43%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent_by_id (lines 350-365) ---[0m
    pub async fn start_agent_by_id(&mut self, identifier: &str) -> Result<()> {
        // Find agent by ID or name
        let agent_to_start = self
            .agents
            .iter()
            .find(|a| a.id == identifier || a.name == identifier)
            .map(|a| a.id.clone());

        if let Some(agent_id) = agent_to_start {
            self.start_agent(&agent_id).await?;
        } else {
            self.add_log("System", &format!("Agent not found: {}", identifier))
                .await;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_create_agent (lines 758-780) ---[0m
    async fn execute_create_agent(&mut self, agent_type: &str) -> Result<()> {
        let agent_type = agent_type.to_lowercase();
        match agent_type.as_str() {
            "frontend" | "backend" | "devops" | "qa" => {
                self.add_log("System", &format!("Creating {} agent...", agent_type))
                    .await;
                // TODO: Implement actual agent creation
                self.add_log(
                    "System",
                    &format!("{} agent created successfully", agent_type),
                )
                .await;
            }
            _ => {
                self.add_log(
                    "System",
                    "Invalid agent type. Use: frontend, backend, devops, qa",
                )
                .await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:673-693 method add_log <-> ./crates/ccswarm/src/tui/app.rs:758-780 method execute_create_agent
  Similarity: 77.20%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:add_log (lines 673-693) ---[0m
    async fn add_log(&mut self, source: &str, message: &str) {
        let log_entry = LogEntry {
            timestamp: Utc::now(),
            level: "INFO".to_string(),
            agent: if source == "System" {
                None
            } else {
                Some(source.to_string())
            },
            message: message.to_string(),
        };
        self.logs.push(log_entry);

        // Keep only last 1000 log entries
        if self.logs.len() > 1000 {
            self.logs.remove(0);
        }

        // Auto-scroll to bottom
        self.selected_log = self.logs.len().saturating_sub(1);
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_create_agent (lines 758-780) ---[0m
    async fn execute_create_agent(&mut self, agent_type: &str) -> Result<()> {
        let agent_type = agent_type.to_lowercase();
        match agent_type.as_str() {
            "frontend" | "backend" | "devops" | "qa" => {
                self.add_log("System", &format!("Creating {} agent...", agent_type))
                    .await;
                // TODO: Implement actual agent creation
                self.add_log(
                    "System",
                    &format!("{} agent created successfully", agent_type),
                )
                .await;
            }
            _ => {
                self.add_log(
                    "System",
                    "Invalid agent type. Use: frontend, backend, devops, qa",
                )
                .await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:758-780 method execute_create_agent <-> ./crates/ccswarm/src/tui/app.rs:1050-1066 method start_delegation_input
  Similarity: 83.46%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_create_agent (lines 758-780) ---[0m
    async fn execute_create_agent(&mut self, agent_type: &str) -> Result<()> {
        let agent_type = agent_type.to_lowercase();
        match agent_type.as_str() {
            "frontend" | "backend" | "devops" | "qa" => {
                self.add_log("System", &format!("Creating {} agent...", agent_type))
                    .await;
                // TODO: Implement actual agent creation
                self.add_log(
                    "System",
                    &format!("{} agent created successfully", agent_type),
                )
                .await;
            }
            _ => {
                self.add_log(
                    "System",
                    "Invalid agent type. Use: frontend, backend, devops, qa",
                )
                .await;
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_delegation_input (lines 1050-1066) ---[0m
    pub async fn start_delegation_input(&mut self) -> Result<()> {
        self.input_mode = InputMode::DelegationInput;
        self.delegation_input.clear();

        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.add_log("Master", "Enter task description to analyze:")
                    .await;
            }
            DelegationMode::Delegate => {
                self.add_log("Master", "Enter task description to delegate:")
                    .await;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:758-780 method execute_create_agent <-> ./crates/ccswarm/src/tui/app.rs:1300-1314 method handle_delegation_enter
  Similarity: 85.91%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_create_agent (lines 758-780) ---[0m
    async fn execute_create_agent(&mut self, agent_type: &str) -> Result<()> {
        let agent_type = agent_type.to_lowercase();
        match agent_type.as_str() {
            "frontend" | "backend" | "devops" | "qa" => {
                self.add_log("System", &format!("Creating {} agent...", agent_type))
                    .await;
                // TODO: Implement actual agent creation
                self.add_log(
                    "System",
                    &format!("{} agent created successfully", agent_type),
                )
                .await;
            }
            _ => {
                self.add_log(
                    "System",
                    "Invalid agent type. Use: frontend, backend, devops, qa",
                )
                .await;
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_delegation_enter (lines 1300-1314) ---[0m
    pub async fn handle_delegation_enter(&mut self) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze | DelegationMode::Delegate => {
                self.start_delegation_input().await?;
            }
            DelegationMode::ViewStats => {
                // Show delegation statistics
                let stats = self.get_delegation_stats();
                for line in stats.lines() {
                    self.add_log("Master", line).await;
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:350-365 method start_agent_by_id <-> ./crates/ccswarm/src/tui/app.rs:857-878 method show_help
  Similarity: 85.68%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent_by_id (lines 350-365) ---[0m
    pub async fn start_agent_by_id(&mut self, identifier: &str) -> Result<()> {
        // Find agent by ID or name
        let agent_to_start = self
            .agents
            .iter()
            .find(|a| a.id == identifier || a.name == identifier)
            .map(|a| a.id.clone());

        if let Some(agent_id) = agent_to_start {
            self.start_agent(&agent_id).await?;
        } else {
            self.add_log("System", &format!("Agent not found: {}", identifier))
                .await;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_help (lines 857-878) ---[0m
    async fn show_help(&mut self) {
        let help_text = vec![
            "Available commands:",
            "  help                    - Show this help",
            "  status                  - Show system status",
            "  agents                  - List all agents",
            "  tasks                   - List all tasks",
            "  task <description>      - Add new task",
            "  agent <type>            - Create new agent (frontend/backend/devops/qa)",
            "  start_agent <id|name>   - Start/activate an agent (e.g., 'start_agent master')",
            "  activate <id|name>      - Alias for start_agent",
            "  start                   - Start orchestrator",
            "  stop                    - Stop orchestrator",
            "  refresh                 - Refresh all data",
            "  clear                   - Clear logs",
            "  worktree [list|prune] - Manage worktrees",
        ];

        for line in help_text {
            self.add_log("System", line).await;
        }
    }

  ./crates/ccswarm/src/tui/app.rs:243-267 method next_item <-> ./crates/ccswarm/src/tui/app.rs:1050-1066 method start_delegation_input
  Similarity: 77.29%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:next_item (lines 243-267) ---[0m
    pub fn next_item(&mut self) {
        match self.current_tab {
            Tab::Agents => {
                if self.selected_agent < self.agents.len().saturating_sub(1) {
                    self.selected_agent += 1;
                }
            }
            Tab::Tasks => {
                if self.selected_task < self.tasks.len().saturating_sub(1) {
                    self.selected_task += 1;
                }
            }
            Tab::Logs => {
                if self.selected_log < self.logs.len().saturating_sub(1) {
                    self.selected_log += 1;
                }
            }
            Tab::Delegation => {
                if self.selected_delegation < self.delegation_decisions.len().saturating_sub(1) {
                    self.selected_delegation += 1;
                }
            }
            _ => {}
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_delegation_input (lines 1050-1066) ---[0m
    pub async fn start_delegation_input(&mut self) -> Result<()> {
        self.input_mode = InputMode::DelegationInput;
        self.delegation_input.clear();

        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.add_log("Master", "Enter task description to analyze:")
                    .await;
            }
            DelegationMode::Delegate => {
                self.add_log("Master", "Enter task description to delegate:")
                    .await;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:350-365 method start_agent_by_id <-> ./crates/ccswarm/src/tui/app.rs:673-693 method add_log
  Similarity: 86.89%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent_by_id (lines 350-365) ---[0m
    pub async fn start_agent_by_id(&mut self, identifier: &str) -> Result<()> {
        // Find agent by ID or name
        let agent_to_start = self
            .agents
            .iter()
            .find(|a| a.id == identifier || a.name == identifier)
            .map(|a| a.id.clone());

        if let Some(agent_id) = agent_to_start {
            self.start_agent(&agent_id).await?;
        } else {
            self.add_log("System", &format!("Agent not found: {}", identifier))
                .await;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:add_log (lines 673-693) ---[0m
    async fn add_log(&mut self, source: &str, message: &str) {
        let log_entry = LogEntry {
            timestamp: Utc::now(),
            level: "INFO".to_string(),
            agent: if source == "System" {
                None
            } else {
                Some(source.to_string())
            },
            message: message.to_string(),
        };
        self.logs.push(log_entry);

        // Keep only last 1000 log entries
        if self.logs.len() > 1000 {
            self.logs.remove(0);
        }

        // Auto-scroll to bottom
        self.selected_log = self.logs.len().saturating_sub(1);
    }

  ./crates/ccswarm/src/tui/app.rs:673-693 method add_log <-> ./crates/ccswarm/src/tui/app.rs:1050-1066 method start_delegation_input
  Similarity: 83.35%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:add_log (lines 673-693) ---[0m
    async fn add_log(&mut self, source: &str, message: &str) {
        let log_entry = LogEntry {
            timestamp: Utc::now(),
            level: "INFO".to_string(),
            agent: if source == "System" {
                None
            } else {
                Some(source.to_string())
            },
            message: message.to_string(),
        };
        self.logs.push(log_entry);

        // Keep only last 1000 log entries
        if self.logs.len() > 1000 {
            self.logs.remove(0);
        }

        // Auto-scroll to bottom
        self.selected_log = self.logs.len().saturating_sub(1);
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_delegation_input (lines 1050-1066) ---[0m
    pub async fn start_delegation_input(&mut self) -> Result<()> {
        self.input_mode = InputMode::DelegationInput;
        self.delegation_input.clear();

        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.add_log("Master", "Enter task description to analyze:")
                    .await;
            }
            DelegationMode::Delegate => {
                self.add_log("Master", "Enter task description to delegate:")
                    .await;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:216-240 method previous_item <-> ./crates/ccswarm/src/tui/app.rs:270-278 method reset_selection
  Similarity: 91.78%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_item (lines 216-240) ---[0m
    pub fn previous_item(&mut self) {
        match self.current_tab {
            Tab::Agents => {
                if self.selected_agent > 0 {
                    self.selected_agent -= 1;
                }
            }
            Tab::Tasks => {
                if self.selected_task > 0 {
                    self.selected_task -= 1;
                }
            }
            Tab::Logs => {
                if self.selected_log > 0 {
                    self.selected_log -= 1;
                }
            }
            Tab::Delegation => {
                if self.selected_delegation > 0 {
                    self.selected_delegation -= 1;
                }
            }
            _ => {}
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:reset_selection (lines 270-278) ---[0m
    fn reset_selection(&mut self) {
        match self.current_tab {
            Tab::Agents => self.selected_agent = 0,
            Tab::Tasks => self.selected_task = 0,
            Tab::Logs => self.selected_log = 0,
            Tab::Delegation => self.selected_delegation = 0,
            _ => {}
        }
    }

  ./crates/ccswarm/src/tui/app.rs:958-979 method list_agents_command <-> ./crates/ccswarm/src/tui/app.rs:1050-1066 method start_delegation_input
  Similarity: 79.65%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:list_agents_command (lines 958-979) ---[0m
    async fn list_agents_command(&mut self) -> Result<()> {
        if self.agents.is_empty() {
            self.add_log("System", "No agents found").await;
        } else {
            self.add_log("System", "Active agents:").await;
            let agent_info: Vec<String> = self
                .agents
                .iter()
                .map(|agent| {
                    format!(
                        "  {} ({}) - {:?}",
                        agent.name, agent.specialization, agent.status
                    )
                })
                .collect();

            for info in agent_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_delegation_input (lines 1050-1066) ---[0m
    pub async fn start_delegation_input(&mut self) -> Result<()> {
        self.input_mode = InputMode::DelegationInput;
        self.delegation_input.clear();

        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.add_log("Master", "Enter task description to analyze:")
                    .await;
            }
            DelegationMode::Delegate => {
                self.add_log("Master", "Enter task description to delegate:")
                    .await;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:982-1003 method list_tasks_command <-> ./crates/ccswarm/src/tui/app.rs:1050-1066 method start_delegation_input
  Similarity: 79.65%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:list_tasks_command (lines 982-1003) ---[0m
    async fn list_tasks_command(&mut self) -> Result<()> {
        if self.tasks.is_empty() {
            self.add_log("System", "No pending tasks").await;
        } else {
            self.add_log("System", "Pending tasks:").await;
            let task_info: Vec<String> = self
                .tasks
                .iter()
                .map(|task| {
                    format!(
                        "  {} - {} ({})",
                        task.description, task.priority, task.task_type
                    )
                })
                .collect();

            for info in task_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_delegation_input (lines 1050-1066) ---[0m
    pub async fn start_delegation_input(&mut self) -> Result<()> {
        self.input_mode = InputMode::DelegationInput;
        self.delegation_input.clear();

        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.add_log("Master", "Enter task description to analyze:")
                    .await;
            }
            DelegationMode::Delegate => {
                self.add_log("Master", "Enter task description to delegate:")
                    .await;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:216-240 method previous_item <-> ./crates/ccswarm/src/tui/app.rs:1069-1080 method execute_delegation_action
  Similarity: 83.22%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_item (lines 216-240) ---[0m
    pub fn previous_item(&mut self) {
        match self.current_tab {
            Tab::Agents => {
                if self.selected_agent > 0 {
                    self.selected_agent -= 1;
                }
            }
            Tab::Tasks => {
                if self.selected_task > 0 {
                    self.selected_task -= 1;
                }
            }
            Tab::Logs => {
                if self.selected_log > 0 {
                    self.selected_log -= 1;
                }
            }
            Tab::Delegation => {
                if self.selected_delegation > 0 {
                    self.selected_delegation -= 1;
                }
            }
            _ => {}
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_delegation_action (lines 1069-1080) ---[0m
    async fn execute_delegation_action(&mut self, input: &str) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.analyze_task_for_delegation(input).await?;
            }
            DelegationMode::Delegate => {
                self.delegate_task_to_agent(input).await?;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:216-240 method previous_item <-> ./crates/ccswarm/src/tui/app.rs:1318-1327 function parse_agent_status
  Similarity: 84.66%

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_item (lines 216-240) ---[0m
    pub fn previous_item(&mut self) {
        match self.current_tab {
            Tab::Agents => {
                if self.selected_agent > 0 {
                    self.selected_agent -= 1;
                }
            }
            Tab::Tasks => {
                if self.selected_task > 0 {
                    self.selected_task -= 1;
                }
            }
            Tab::Logs => {
                if self.selected_log > 0 {
                    self.selected_log -= 1;
                }
            }
            Tab::Delegation => {
                if self.selected_delegation > 0 {
                    self.selected_delegation -= 1;
                }
            }
            _ => {}
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:parse_agent_status (lines 1318-1327) ---[0m
fn parse_agent_status(status: &str) -> AgentStatus {
    match status {
        "Initializing" => AgentStatus::Initializing,
        "Available" => AgentStatus::Available,
        "Working" => AgentStatus::Working,
        "WaitingForReview" => AgentStatus::WaitingForReview,
        "ShuttingDown" => AgentStatus::ShuttingDown,
        _ => AgentStatus::Error(format!("Unknown status: {}", status)),
    }
}

  ./crates/ccswarm/src/tui/app.rs:216-240 method previous_item <-> ./crates/ccswarm/src/tui/app.rs:1300-1314 method handle_delegation_enter
  Similarity: 73.97%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_item (lines 216-240) ---[0m
    pub fn previous_item(&mut self) {
        match self.current_tab {
            Tab::Agents => {
                if self.selected_agent > 0 {
                    self.selected_agent -= 1;
                }
            }
            Tab::Tasks => {
                if self.selected_task > 0 {
                    self.selected_task -= 1;
                }
            }
            Tab::Logs => {
                if self.selected_log > 0 {
                    self.selected_log -= 1;
                }
            }
            Tab::Delegation => {
                if self.selected_delegation > 0 {
                    self.selected_delegation -= 1;
                }
            }
            _ => {}
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_delegation_enter (lines 1300-1314) ---[0m
    pub async fn handle_delegation_enter(&mut self) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze | DelegationMode::Delegate => {
                self.start_delegation_input().await?;
            }
            DelegationMode::ViewStats => {
                // Show delegation statistics
                let stats = self.get_delegation_stats();
                for line in stats.lines() {
                    self.add_log("Master", line).await;
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:673-693 method add_log <-> ./crates/ccswarm/src/tui/app.rs:743-755 method execute_add_task
  Similarity: 86.04%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:add_log (lines 673-693) ---[0m
    async fn add_log(&mut self, source: &str, message: &str) {
        let log_entry = LogEntry {
            timestamp: Utc::now(),
            level: "INFO".to_string(),
            agent: if source == "System" {
                None
            } else {
                Some(source.to_string())
            },
            message: message.to_string(),
        };
        self.logs.push(log_entry);

        // Keep only last 1000 log entries
        if self.logs.len() > 1000 {
            self.logs.remove(0);
        }

        // Auto-scroll to bottom
        self.selected_log = self.logs.len().saturating_sub(1);
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_add_task (lines 743-755) ---[0m
    async fn execute_add_task(&mut self, description: &str) -> Result<()> {
        // Parse priority and type from description if included
        let (desc, priority, task_type) = self.parse_task_description(description);

        let task =
            crate::agent::Task::new(uuid::Uuid::new_v4().to_string(), desc, priority, task_type);

        self.task_queue.add_task(&task).await?;
        self.add_log("System", &format!("Task added: {}", task.description))
            .await;
        self.refresh_data().await?;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:350-365 method start_agent_by_id <-> ./crates/ccswarm/src/tui/app.rs:958-979 method list_agents_command
  Similarity: 76.92%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent_by_id (lines 350-365) ---[0m
    pub async fn start_agent_by_id(&mut self, identifier: &str) -> Result<()> {
        // Find agent by ID or name
        let agent_to_start = self
            .agents
            .iter()
            .find(|a| a.id == identifier || a.name == identifier)
            .map(|a| a.id.clone());

        if let Some(agent_id) = agent_to_start {
            self.start_agent(&agent_id).await?;
        } else {
            self.add_log("System", &format!("Agent not found: {}", identifier))
                .await;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_agents_command (lines 958-979) ---[0m
    async fn list_agents_command(&mut self) -> Result<()> {
        if self.agents.is_empty() {
            self.add_log("System", "No agents found").await;
        } else {
            self.add_log("System", "Active agents:").await;
            let agent_info: Vec<String> = self
                .agents
                .iter()
                .map(|agent| {
                    format!(
                        "  {} ({}) - {:?}",
                        agent.name, agent.specialization, agent.status
                    )
                })
                .collect();

            for info in agent_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:350-365 method start_agent_by_id <-> ./crates/ccswarm/src/tui/app.rs:982-1003 method list_tasks_command
  Similarity: 76.92%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent_by_id (lines 350-365) ---[0m
    pub async fn start_agent_by_id(&mut self, identifier: &str) -> Result<()> {
        // Find agent by ID or name
        let agent_to_start = self
            .agents
            .iter()
            .find(|a| a.id == identifier || a.name == identifier)
            .map(|a| a.id.clone());

        if let Some(agent_id) = agent_to_start {
            self.start_agent(&agent_id).await?;
        } else {
            self.add_log("System", &format!("Agent not found: {}", identifier))
                .await;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_tasks_command (lines 982-1003) ---[0m
    async fn list_tasks_command(&mut self) -> Result<()> {
        if self.tasks.is_empty() {
            self.add_log("System", "No pending tasks").await;
        } else {
            self.add_log("System", "Pending tasks:").await;
            let task_info: Vec<String> = self
                .tasks
                .iter()
                .map(|task| {
                    format!(
                        "  {} - {} ({})",
                        task.description, task.priority, task.task_type
                    )
                })
                .collect();

            for info in task_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:958-979 method list_agents_command <-> ./crates/ccswarm/src/tui/app.rs:1300-1314 method handle_delegation_enter
  Similarity: 78.02%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:list_agents_command (lines 958-979) ---[0m
    async fn list_agents_command(&mut self) -> Result<()> {
        if self.agents.is_empty() {
            self.add_log("System", "No agents found").await;
        } else {
            self.add_log("System", "Active agents:").await;
            let agent_info: Vec<String> = self
                .agents
                .iter()
                .map(|agent| {
                    format!(
                        "  {} ({}) - {:?}",
                        agent.name, agent.specialization, agent.status
                    )
                })
                .collect();

            for info in agent_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_delegation_enter (lines 1300-1314) ---[0m
    pub async fn handle_delegation_enter(&mut self) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze | DelegationMode::Delegate => {
                self.start_delegation_input().await?;
            }
            DelegationMode::ViewStats => {
                // Show delegation statistics
                let stats = self.get_delegation_stats();
                for line in stats.lines() {
                    self.add_log("Master", line).await;
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:982-1003 method list_tasks_command <-> ./crates/ccswarm/src/tui/app.rs:1300-1314 method handle_delegation_enter
  Similarity: 78.02%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:list_tasks_command (lines 982-1003) ---[0m
    async fn list_tasks_command(&mut self) -> Result<()> {
        if self.tasks.is_empty() {
            self.add_log("System", "No pending tasks").await;
        } else {
            self.add_log("System", "Pending tasks:").await;
            let task_info: Vec<String> = self
                .tasks
                .iter()
                .map(|task| {
                    format!(
                        "  {} - {} ({})",
                        task.description, task.priority, task.task_type
                    )
                })
                .collect();

            for info in task_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_delegation_enter (lines 1300-1314) ---[0m
    pub async fn handle_delegation_enter(&mut self) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze | DelegationMode::Delegate => {
                self.start_delegation_input().await?;
            }
            DelegationMode::ViewStats => {
                // Show delegation statistics
                let stats = self.get_delegation_stats();
                for line in stats.lines() {
                    self.add_log("Master", line).await;
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:192-201 method next_tab <-> ./crates/ccswarm/src/tui/app.rs:758-780 method execute_create_agent
  Similarity: 86.23%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:next_tab (lines 192-201) ---[0m
    pub fn next_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Agents,
            Tab::Agents => Tab::Tasks,
            Tab::Tasks => Tab::Logs,
            Tab::Logs => Tab::Delegation,
            Tab::Delegation => Tab::Overview,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_create_agent (lines 758-780) ---[0m
    async fn execute_create_agent(&mut self, agent_type: &str) -> Result<()> {
        let agent_type = agent_type.to_lowercase();
        match agent_type.as_str() {
            "frontend" | "backend" | "devops" | "qa" => {
                self.add_log("System", &format!("Creating {} agent...", agent_type))
                    .await;
                // TODO: Implement actual agent creation
                self.add_log(
                    "System",
                    &format!("{} agent created successfully", agent_type),
                )
                .await;
            }
            _ => {
                self.add_log(
                    "System",
                    "Invalid agent type. Use: frontend, backend, devops, qa",
                )
                .await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:204-213 method previous_tab <-> ./crates/ccswarm/src/tui/app.rs:758-780 method execute_create_agent
  Similarity: 86.23%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_tab (lines 204-213) ---[0m
    pub fn previous_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Delegation,
            Tab::Agents => Tab::Overview,
            Tab::Tasks => Tab::Agents,
            Tab::Logs => Tab::Tasks,
            Tab::Delegation => Tab::Logs,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_create_agent (lines 758-780) ---[0m
    async fn execute_create_agent(&mut self, agent_type: &str) -> Result<()> {
        let agent_type = agent_type.to_lowercase();
        match agent_type.as_str() {
            "frontend" | "backend" | "devops" | "qa" => {
                self.add_log("System", &format!("Creating {} agent...", agent_type))
                    .await;
                // TODO: Implement actual agent creation
                self.add_log(
                    "System",
                    &format!("{} agent created successfully", agent_type),
                )
                .await;
            }
            _ => {
                self.add_log(
                    "System",
                    "Invalid agent type. Use: frontend, backend, devops, qa",
                )
                .await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:192-201 method next_tab <-> ./crates/ccswarm/src/tui/app.rs:857-878 method show_help
  Similarity: 88.00%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:next_tab (lines 192-201) ---[0m
    pub fn next_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Agents,
            Tab::Agents => Tab::Tasks,
            Tab::Tasks => Tab::Logs,
            Tab::Logs => Tab::Delegation,
            Tab::Delegation => Tab::Overview,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_help (lines 857-878) ---[0m
    async fn show_help(&mut self) {
        let help_text = vec![
            "Available commands:",
            "  help                    - Show this help",
            "  status                  - Show system status",
            "  agents                  - List all agents",
            "  tasks                   - List all tasks",
            "  task <description>      - Add new task",
            "  agent <type>            - Create new agent (frontend/backend/devops/qa)",
            "  start_agent <id|name>   - Start/activate an agent (e.g., 'start_agent master')",
            "  activate <id|name>      - Alias for start_agent",
            "  start                   - Start orchestrator",
            "  stop                    - Stop orchestrator",
            "  refresh                 - Refresh all data",
            "  clear                   - Clear logs",
            "  worktree [list|prune] - Manage worktrees",
        ];

        for line in help_text {
            self.add_log("System", line).await;
        }
    }

  ./crates/ccswarm/src/tui/app.rs:204-213 method previous_tab <-> ./crates/ccswarm/src/tui/app.rs:857-878 method show_help
  Similarity: 88.00%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_tab (lines 204-213) ---[0m
    pub fn previous_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Delegation,
            Tab::Agents => Tab::Overview,
            Tab::Tasks => Tab::Agents,
            Tab::Logs => Tab::Tasks,
            Tab::Delegation => Tab::Logs,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_help (lines 857-878) ---[0m
    async fn show_help(&mut self) {
        let help_text = vec![
            "Available commands:",
            "  help                    - Show this help",
            "  status                  - Show system status",
            "  agents                  - List all agents",
            "  tasks                   - List all tasks",
            "  task <description>      - Add new task",
            "  agent <type>            - Create new agent (frontend/backend/devops/qa)",
            "  start_agent <id|name>   - Start/activate an agent (e.g., 'start_agent master')",
            "  activate <id|name>      - Alias for start_agent",
            "  start                   - Start orchestrator",
            "  stop                    - Stop orchestrator",
            "  refresh                 - Refresh all data",
            "  clear                   - Clear logs",
            "  worktree [list|prune] - Manage worktrees",
        ];

        for line in help_text {
            self.add_log("System", line).await;
        }
    }

  ./crates/ccswarm/src/tui/app.rs:350-365 method start_agent_by_id <-> ./crates/ccswarm/src/tui/app.rs:1050-1066 method start_delegation_input
  Similarity: 83.04%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent_by_id (lines 350-365) ---[0m
    pub async fn start_agent_by_id(&mut self, identifier: &str) -> Result<()> {
        // Find agent by ID or name
        let agent_to_start = self
            .agents
            .iter()
            .find(|a| a.id == identifier || a.name == identifier)
            .map(|a| a.id.clone());

        if let Some(agent_id) = agent_to_start {
            self.start_agent(&agent_id).await?;
        } else {
            self.add_log("System", &format!("Agent not found: {}", identifier))
                .await;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_delegation_input (lines 1050-1066) ---[0m
    pub async fn start_delegation_input(&mut self) -> Result<()> {
        self.input_mode = InputMode::DelegationInput;
        self.delegation_input.clear();

        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.add_log("Master", "Enter task description to analyze:")
                    .await;
            }
            DelegationMode::Delegate => {
                self.add_log("Master", "Enter task description to delegate:")
                    .await;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:1069-1080 method execute_delegation_action <-> ./crates/ccswarm/src/tui/app.rs:1300-1314 method handle_delegation_enter
  Similarity: 90.83%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_delegation_action (lines 1069-1080) ---[0m
    async fn execute_delegation_action(&mut self, input: &str) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.analyze_task_for_delegation(input).await?;
            }
            DelegationMode::Delegate => {
                self.delegate_task_to_agent(input).await?;
            }
            _ => {}
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_delegation_enter (lines 1300-1314) ---[0m
    pub async fn handle_delegation_enter(&mut self) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze | DelegationMode::Delegate => {
                self.start_delegation_input().await?;
            }
            DelegationMode::ViewStats => {
                // Show delegation statistics
                let stats = self.get_delegation_stats();
                for line in stats.lines() {
                    self.add_log("Master", line).await;
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:192-201 method next_tab <-> ./crates/ccswarm/src/tui/app.rs:958-979 method list_agents_command
  Similarity: 75.41%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:next_tab (lines 192-201) ---[0m
    pub fn next_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Agents,
            Tab::Agents => Tab::Tasks,
            Tab::Tasks => Tab::Logs,
            Tab::Logs => Tab::Delegation,
            Tab::Delegation => Tab::Overview,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_agents_command (lines 958-979) ---[0m
    async fn list_agents_command(&mut self) -> Result<()> {
        if self.agents.is_empty() {
            self.add_log("System", "No agents found").await;
        } else {
            self.add_log("System", "Active agents:").await;
            let agent_info: Vec<String> = self
                .agents
                .iter()
                .map(|agent| {
                    format!(
                        "  {} ({}) - {:?}",
                        agent.name, agent.specialization, agent.status
                    )
                })
                .collect();

            for info in agent_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:192-201 method next_tab <-> ./crates/ccswarm/src/tui/app.rs:982-1003 method list_tasks_command
  Similarity: 75.41%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:next_tab (lines 192-201) ---[0m
    pub fn next_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Agents,
            Tab::Agents => Tab::Tasks,
            Tab::Tasks => Tab::Logs,
            Tab::Logs => Tab::Delegation,
            Tab::Delegation => Tab::Overview,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_tasks_command (lines 982-1003) ---[0m
    async fn list_tasks_command(&mut self) -> Result<()> {
        if self.tasks.is_empty() {
            self.add_log("System", "No pending tasks").await;
        } else {
            self.add_log("System", "Pending tasks:").await;
            let task_info: Vec<String> = self
                .tasks
                .iter()
                .map(|task| {
                    format!(
                        "  {} - {} ({})",
                        task.description, task.priority, task.task_type
                    )
                })
                .collect();

            for info in task_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:204-213 method previous_tab <-> ./crates/ccswarm/src/tui/app.rs:958-979 method list_agents_command
  Similarity: 75.41%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_tab (lines 204-213) ---[0m
    pub fn previous_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Delegation,
            Tab::Agents => Tab::Overview,
            Tab::Tasks => Tab::Agents,
            Tab::Logs => Tab::Tasks,
            Tab::Delegation => Tab::Logs,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_agents_command (lines 958-979) ---[0m
    async fn list_agents_command(&mut self) -> Result<()> {
        if self.agents.is_empty() {
            self.add_log("System", "No agents found").await;
        } else {
            self.add_log("System", "Active agents:").await;
            let agent_info: Vec<String> = self
                .agents
                .iter()
                .map(|agent| {
                    format!(
                        "  {} ({}) - {:?}",
                        agent.name, agent.specialization, agent.status
                    )
                })
                .collect();

            for info in agent_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:204-213 method previous_tab <-> ./crates/ccswarm/src/tui/app.rs:982-1003 method list_tasks_command
  Similarity: 75.41%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_tab (lines 204-213) ---[0m
    pub fn previous_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Delegation,
            Tab::Agents => Tab::Overview,
            Tab::Tasks => Tab::Agents,
            Tab::Logs => Tab::Tasks,
            Tab::Delegation => Tab::Logs,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_tasks_command (lines 982-1003) ---[0m
    async fn list_tasks_command(&mut self) -> Result<()> {
        if self.tasks.is_empty() {
            self.add_log("System", "No pending tasks").await;
        } else {
            self.add_log("System", "Pending tasks:").await;
            let task_info: Vec<String> = self
                .tasks
                .iter()
                .map(|task| {
                    format!(
                        "  {} - {} ({})",
                        task.description, task.priority, task.task_type
                    )
                })
                .collect();

            for info in task_info {
                self.add_log("System", &info).await;
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:350-365 method start_agent_by_id <-> ./crates/ccswarm/src/tui/app.rs:1300-1314 method handle_delegation_enter
  Similarity: 76.22%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent_by_id (lines 350-365) ---[0m
    pub async fn start_agent_by_id(&mut self, identifier: &str) -> Result<()> {
        // Find agent by ID or name
        let agent_to_start = self
            .agents
            .iter()
            .find(|a| a.id == identifier || a.name == identifier)
            .map(|a| a.id.clone());

        if let Some(agent_id) = agent_to_start {
            self.start_agent(&agent_id).await?;
        } else {
            self.add_log("System", &format!("Agent not found: {}", identifier))
                .await;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_delegation_enter (lines 1300-1314) ---[0m
    pub async fn handle_delegation_enter(&mut self) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze | DelegationMode::Delegate => {
                self.start_delegation_input().await?;
            }
            DelegationMode::ViewStats => {
                // Show delegation statistics
                let stats = self.get_delegation_stats();
                for line in stats.lines() {
                    self.add_log("Master", line).await;
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:1050-1066 method start_delegation_input <-> ./crates/ccswarm/src/tui/app.rs:1300-1314 method handle_delegation_enter
  Similarity: 72.70%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_delegation_input (lines 1050-1066) ---[0m
    pub async fn start_delegation_input(&mut self) -> Result<()> {
        self.input_mode = InputMode::DelegationInput;
        self.delegation_input.clear();

        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.add_log("Master", "Enter task description to analyze:")
                    .await;
            }
            DelegationMode::Delegate => {
                self.add_log("Master", "Enter task description to delegate:")
                    .await;
            }
            _ => {}
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_delegation_enter (lines 1300-1314) ---[0m
    pub async fn handle_delegation_enter(&mut self) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze | DelegationMode::Delegate => {
                self.start_delegation_input().await?;
            }
            DelegationMode::ViewStats => {
                // Show delegation statistics
                let stats = self.get_delegation_stats();
                for line in stats.lines() {
                    self.add_log("Master", line).await;
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:857-878 method show_help <-> ./crates/ccswarm/src/tui/app.rs:1025-1030 method list_worktrees
  Similarity: 81.00%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_help (lines 857-878) ---[0m
    async fn show_help(&mut self) {
        let help_text = vec![
            "Available commands:",
            "  help                    - Show this help",
            "  status                  - Show system status",
            "  agents                  - List all agents",
            "  tasks                   - List all tasks",
            "  task <description>      - Add new task",
            "  agent <type>            - Create new agent (frontend/backend/devops/qa)",
            "  start_agent <id|name>   - Start/activate an agent (e.g., 'start_agent master')",
            "  activate <id|name>      - Alias for start_agent",
            "  start                   - Start orchestrator",
            "  stop                    - Stop orchestrator",
            "  refresh                 - Refresh all data",
            "  clear                   - Clear logs",
            "  worktree [list|prune] - Manage worktrees",
        ];

        for line in help_text {
            self.add_log("System", line).await;
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_worktrees (lines 1025-1030) ---[0m
    async fn list_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Git worktrees:").await;
        // TODO: Implement actual worktree listing
        self.add_log("System", "  No worktrees found").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:857-878 method show_help <-> ./crates/ccswarm/src/tui/app.rs:1033-1038 method prune_worktrees
  Similarity: 81.00%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_help (lines 857-878) ---[0m
    async fn show_help(&mut self) {
        let help_text = vec![
            "Available commands:",
            "  help                    - Show this help",
            "  status                  - Show system status",
            "  agents                  - List all agents",
            "  tasks                   - List all tasks",
            "  task <description>      - Add new task",
            "  agent <type>            - Create new agent (frontend/backend/devops/qa)",
            "  start_agent <id|name>   - Start/activate an agent (e.g., 'start_agent master')",
            "  activate <id|name>      - Alias for start_agent",
            "  start                   - Start orchestrator",
            "  stop                    - Stop orchestrator",
            "  refresh                 - Refresh all data",
            "  clear                   - Clear logs",
            "  worktree [list|prune] - Manage worktrees",
        ];

        for line in help_text {
            self.add_log("System", line).await;
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:prune_worktrees (lines 1033-1038) ---[0m
    async fn prune_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Pruning stale worktrees...").await;
        // TODO: Implement actual worktree pruning
        self.add_log("System", "Worktree pruning completed").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:192-201 method next_tab <-> ./crates/ccswarm/src/tui/app.rs:1300-1314 method handle_delegation_enter
  Similarity: 89.35%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:next_tab (lines 192-201) ---[0m
    pub fn next_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Agents,
            Tab::Agents => Tab::Tasks,
            Tab::Tasks => Tab::Logs,
            Tab::Logs => Tab::Delegation,
            Tab::Delegation => Tab::Overview,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_delegation_enter (lines 1300-1314) ---[0m
    pub async fn handle_delegation_enter(&mut self) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze | DelegationMode::Delegate => {
                self.start_delegation_input().await?;
            }
            DelegationMode::ViewStats => {
                // Show delegation statistics
                let stats = self.get_delegation_stats();
                for line in stats.lines() {
                    self.add_log("Master", line).await;
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:204-213 method previous_tab <-> ./crates/ccswarm/src/tui/app.rs:1300-1314 method handle_delegation_enter
  Similarity: 89.35%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_tab (lines 204-213) ---[0m
    pub fn previous_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Delegation,
            Tab::Agents => Tab::Overview,
            Tab::Tasks => Tab::Agents,
            Tab::Logs => Tab::Tasks,
            Tab::Delegation => Tab::Logs,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_delegation_enter (lines 1300-1314) ---[0m
    pub async fn handle_delegation_enter(&mut self) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze | DelegationMode::Delegate => {
                self.start_delegation_input().await?;
            }
            DelegationMode::ViewStats => {
                // Show delegation statistics
                let stats = self.get_delegation_stats();
                for line in stats.lines() {
                    self.add_log("Master", line).await;
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:192-201 method next_tab <-> ./crates/ccswarm/src/tui/app.rs:350-365 method start_agent_by_id
  Similarity: 83.58%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:next_tab (lines 192-201) ---[0m
    pub fn next_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Agents,
            Tab::Agents => Tab::Tasks,
            Tab::Tasks => Tab::Logs,
            Tab::Logs => Tab::Delegation,
            Tab::Delegation => Tab::Overview,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent_by_id (lines 350-365) ---[0m
    pub async fn start_agent_by_id(&mut self, identifier: &str) -> Result<()> {
        // Find agent by ID or name
        let agent_to_start = self
            .agents
            .iter()
            .find(|a| a.id == identifier || a.name == identifier)
            .map(|a| a.id.clone());

        if let Some(agent_id) = agent_to_start {
            self.start_agent(&agent_id).await?;
        } else {
            self.add_log("System", &format!("Agent not found: {}", identifier))
                .await;
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:204-213 method previous_tab <-> ./crates/ccswarm/src/tui/app.rs:350-365 method start_agent_by_id
  Similarity: 83.58%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_tab (lines 204-213) ---[0m
    pub fn previous_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Delegation,
            Tab::Agents => Tab::Overview,
            Tab::Tasks => Tab::Agents,
            Tab::Logs => Tab::Tasks,
            Tab::Delegation => Tab::Logs,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent_by_id (lines 350-365) ---[0m
    pub async fn start_agent_by_id(&mut self, identifier: &str) -> Result<()> {
        // Find agent by ID or name
        let agent_to_start = self
            .agents
            .iter()
            .find(|a| a.id == identifier || a.name == identifier)
            .map(|a| a.id.clone());

        if let Some(agent_id) = agent_to_start {
            self.start_agent(&agent_id).await?;
        } else {
            self.add_log("System", &format!("Agent not found: {}", identifier))
                .await;
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:1006-1013 method start_orchestrator <-> ./crates/ccswarm/src/tui/app.rs:1050-1066 method start_delegation_input
  Similarity: 86.00%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_orchestrator (lines 1006-1013) ---[0m
    async fn start_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Starting orchestrator...").await;
        // TODO: Implement actual orchestrator start
        self.system_status = "Running".to_string();
        self.add_log("System", "Orchestrator started successfully")
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_delegation_input (lines 1050-1066) ---[0m
    pub async fn start_delegation_input(&mut self) -> Result<()> {
        self.input_mode = InputMode::DelegationInput;
        self.delegation_input.clear();

        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.add_log("Master", "Enter task description to analyze:")
                    .await;
            }
            DelegationMode::Delegate => {
                self.add_log("Master", "Enter task description to delegate:")
                    .await;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:1050-1066 method start_delegation_input <-> ./crates/ccswarm/src/tui/app.rs:1069-1080 method execute_delegation_action
  Similarity: 72.70%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_delegation_input (lines 1050-1066) ---[0m
    pub async fn start_delegation_input(&mut self) -> Result<()> {
        self.input_mode = InputMode::DelegationInput;
        self.delegation_input.clear();

        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.add_log("Master", "Enter task description to analyze:")
                    .await;
            }
            DelegationMode::Delegate => {
                self.add_log("Master", "Enter task description to delegate:")
                    .await;
            }
            _ => {}
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_delegation_action (lines 1069-1080) ---[0m
    async fn execute_delegation_action(&mut self, input: &str) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.analyze_task_for_delegation(input).await?;
            }
            DelegationMode::Delegate => {
                self.delegate_task_to_agent(input).await?;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:350-365 method start_agent_by_id <-> ./crates/ccswarm/src/tui/app.rs:1069-1080 method execute_delegation_action
  Similarity: 75.07%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent_by_id (lines 350-365) ---[0m
    pub async fn start_agent_by_id(&mut self, identifier: &str) -> Result<()> {
        // Find agent by ID or name
        let agent_to_start = self
            .agents
            .iter()
            .find(|a| a.id == identifier || a.name == identifier)
            .map(|a| a.id.clone());

        if let Some(agent_id) = agent_to_start {
            self.start_agent(&agent_id).await?;
        } else {
            self.add_log("System", &format!("Agent not found: {}", identifier))
                .await;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_delegation_action (lines 1069-1080) ---[0m
    async fn execute_delegation_action(&mut self, input: &str) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.analyze_task_for_delegation(input).await?;
            }
            DelegationMode::Delegate => {
                self.delegate_task_to_agent(input).await?;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:350-365 method start_agent_by_id <-> ./crates/ccswarm/src/tui/app.rs:1006-1013 method start_orchestrator
  Similarity: 87.09%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent_by_id (lines 350-365) ---[0m
    pub async fn start_agent_by_id(&mut self, identifier: &str) -> Result<()> {
        // Find agent by ID or name
        let agent_to_start = self
            .agents
            .iter()
            .find(|a| a.id == identifier || a.name == identifier)
            .map(|a| a.id.clone());

        if let Some(agent_id) = agent_to_start {
            self.start_agent(&agent_id).await?;
        } else {
            self.add_log("System", &format!("Agent not found: {}", identifier))
                .await;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_orchestrator (lines 1006-1013) ---[0m
    async fn start_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Starting orchestrator...").await;
        // TODO: Implement actual orchestrator start
        self.system_status = "Running".to_string();
        self.add_log("System", "Orchestrator started successfully")
            .await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:368-374 method create_new_session <-> ./crates/ccswarm/src/tui/app.rs:857-878 method show_help
  Similarity: 71.91%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:create_new_session (lines 368-374) ---[0m
    pub async fn create_new_session(&mut self) -> Result<()> {
        self.input_mode = InputMode::CreatingAgent;
        self.input_buffer.clear();
        self.add_log("System", "Enter agent type (frontend/backend/devops/qa):")
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_help (lines 857-878) ---[0m
    async fn show_help(&mut self) {
        let help_text = vec![
            "Available commands:",
            "  help                    - Show this help",
            "  status                  - Show system status",
            "  agents                  - List all agents",
            "  tasks                   - List all tasks",
            "  task <description>      - Add new task",
            "  agent <type>            - Create new agent (frontend/backend/devops/qa)",
            "  start_agent <id|name>   - Start/activate an agent (e.g., 'start_agent master')",
            "  activate <id|name>      - Alias for start_agent",
            "  start                   - Start orchestrator",
            "  stop                    - Stop orchestrator",
            "  refresh                 - Refresh all data",
            "  clear                   - Clear logs",
            "  worktree [list|prune] - Manage worktrees",
        ];

        for line in help_text {
            self.add_log("System", line).await;
        }
    }

  ./crates/ccswarm/src/tui/app.rs:402-408 method open_command_prompt <-> ./crates/ccswarm/src/tui/app.rs:857-878 method show_help
  Similarity: 71.91%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:open_command_prompt (lines 402-408) ---[0m
    pub async fn open_command_prompt(&mut self) -> Result<()> {
        self.input_mode = InputMode::Command;
        self.input_buffer.clear();
        self.add_log("System", "Enter command (help for available commands):")
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_help (lines 857-878) ---[0m
    async fn show_help(&mut self) {
        let help_text = vec![
            "Available commands:",
            "  help                    - Show this help",
            "  status                  - Show system status",
            "  agents                  - List all agents",
            "  tasks                   - List all tasks",
            "  task <description>      - Add new task",
            "  agent <type>            - Create new agent (frontend/backend/devops/qa)",
            "  start_agent <id|name>   - Start/activate an agent (e.g., 'start_agent master')",
            "  activate <id|name>      - Alias for start_agent",
            "  start                   - Start orchestrator",
            "  stop                    - Stop orchestrator",
            "  refresh                 - Refresh all data",
            "  clear                   - Clear logs",
            "  worktree [list|prune] - Manage worktrees",
        ];

        for line in help_text {
            self.add_log("System", line).await;
        }
    }

  ./crates/ccswarm/src/tui/app.rs:1016-1022 method stop_orchestrator <-> ./crates/ccswarm/src/tui/app.rs:1050-1066 method start_delegation_input
  Similarity: 86.00%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:stop_orchestrator (lines 1016-1022) ---[0m
    async fn stop_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Stopping orchestrator...").await;
        // TODO: Implement actual orchestrator stop
        self.system_status = "Stopped".to_string();
        self.add_log("System", "Orchestrator stopped").await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_delegation_input (lines 1050-1066) ---[0m
    pub async fn start_delegation_input(&mut self) -> Result<()> {
        self.input_mode = InputMode::DelegationInput;
        self.delegation_input.clear();

        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.add_log("Master", "Enter task description to analyze:")
                    .await;
            }
            DelegationMode::Delegate => {
                self.add_log("Master", "Enter task description to delegate:")
                    .await;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:394-399 method add_task_prompt <-> ./crates/ccswarm/src/tui/app.rs:857-878 method show_help
  Similarity: 71.91%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:add_task_prompt (lines 394-399) ---[0m
    pub async fn add_task_prompt(&mut self) -> Result<()> {
        self.input_mode = InputMode::AddingTask;
        self.input_buffer.clear();
        self.add_log("System", "Enter task description:").await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_help (lines 857-878) ---[0m
    async fn show_help(&mut self) {
        let help_text = vec![
            "Available commands:",
            "  help                    - Show this help",
            "  status                  - Show system status",
            "  agents                  - List all agents",
            "  tasks                   - List all tasks",
            "  task <description>      - Add new task",
            "  agent <type>            - Create new agent (frontend/backend/devops/qa)",
            "  start_agent <id|name>   - Start/activate an agent (e.g., 'start_agent master')",
            "  activate <id|name>      - Alias for start_agent",
            "  start                   - Start orchestrator",
            "  stop                    - Stop orchestrator",
            "  refresh                 - Refresh all data",
            "  clear                   - Clear logs",
            "  worktree [list|prune] - Manage worktrees",
        ];

        for line in help_text {
            self.add_log("System", line).await;
        }
    }

  ./crates/ccswarm/src/tui/app.rs:192-201 method next_tab <-> ./crates/ccswarm/src/tui/app.rs:1050-1066 method start_delegation_input
  Similarity: 74.35%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:next_tab (lines 192-201) ---[0m
    pub fn next_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Agents,
            Tab::Agents => Tab::Tasks,
            Tab::Tasks => Tab::Logs,
            Tab::Logs => Tab::Delegation,
            Tab::Delegation => Tab::Overview,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_delegation_input (lines 1050-1066) ---[0m
    pub async fn start_delegation_input(&mut self) -> Result<()> {
        self.input_mode = InputMode::DelegationInput;
        self.delegation_input.clear();

        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.add_log("Master", "Enter task description to analyze:")
                    .await;
            }
            DelegationMode::Delegate => {
                self.add_log("Master", "Enter task description to delegate:")
                    .await;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:204-213 method previous_tab <-> ./crates/ccswarm/src/tui/app.rs:1050-1066 method start_delegation_input
  Similarity: 74.35%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_tab (lines 204-213) ---[0m
    pub fn previous_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Delegation,
            Tab::Agents => Tab::Overview,
            Tab::Tasks => Tab::Agents,
            Tab::Logs => Tab::Tasks,
            Tab::Delegation => Tab::Logs,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_delegation_input (lines 1050-1066) ---[0m
    pub async fn start_delegation_input(&mut self) -> Result<()> {
        self.input_mode = InputMode::DelegationInput;
        self.delegation_input.clear();

        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.add_log("Master", "Enter task description to analyze:")
                    .await;
            }
            DelegationMode::Delegate => {
                self.add_log("Master", "Enter task description to delegate:")
                    .await;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:377-384 method delete_current_session <-> ./crates/ccswarm/src/tui/app.rs:1300-1314 method handle_delegation_enter
  Similarity: 87.22%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:delete_current_session (lines 377-384) ---[0m
    pub async fn delete_current_session(&mut self) -> Result<()> {
        if let Some(agent) = self.agents.get(self.selected_agent) {
            self.add_log("System", &format!("Deleting agent: {}", agent.name))
                .await;
            // TODO: Implement actual agent deletion
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_delegation_enter (lines 1300-1314) ---[0m
    pub async fn handle_delegation_enter(&mut self) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze | DelegationMode::Delegate => {
                self.start_delegation_input().await?;
            }
            DelegationMode::ViewStats => {
                // Show delegation statistics
                let stats = self.get_delegation_stats();
                for line in stats.lines() {
                    self.add_log("Master", line).await;
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:350-365 method start_agent_by_id <-> ./crates/ccswarm/src/tui/app.rs:1016-1022 method stop_orchestrator
  Similarity: 87.09%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_agent_by_id (lines 350-365) ---[0m
    pub async fn start_agent_by_id(&mut self, identifier: &str) -> Result<()> {
        // Find agent by ID or name
        let agent_to_start = self
            .agents
            .iter()
            .find(|a| a.id == identifier || a.name == identifier)
            .map(|a| a.id.clone());

        if let Some(agent_id) = agent_to_start {
            self.start_agent(&agent_id).await?;
        } else {
            self.add_log("System", &format!("Agent not found: {}", identifier))
                .await;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:stop_orchestrator (lines 1016-1022) ---[0m
    async fn stop_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Stopping orchestrator...").await;
        // TODO: Implement actual orchestrator stop
        self.system_status = "Stopped".to_string();
        self.add_log("System", "Orchestrator stopped").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:192-201 method next_tab <-> ./crates/ccswarm/src/tui/app.rs:204-213 method previous_tab
  Similarity: 98.30%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:next_tab (lines 192-201) ---[0m
    pub fn next_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Agents,
            Tab::Agents => Tab::Tasks,
            Tab::Tasks => Tab::Logs,
            Tab::Logs => Tab::Delegation,
            Tab::Delegation => Tab::Overview,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_tab (lines 204-213) ---[0m
    pub fn previous_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Delegation,
            Tab::Agents => Tab::Overview,
            Tab::Tasks => Tab::Agents,
            Tab::Logs => Tab::Tasks,
            Tab::Delegation => Tab::Logs,
        };
        self.reset_selection();
    }

  ./crates/ccswarm/src/tui/app.rs:411-417 method refresh_data <-> ./crates/ccswarm/src/tui/app.rs:1050-1066 method start_delegation_input
  Similarity: 78.87%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:refresh_data (lines 411-417) ---[0m
    pub async fn refresh_data(&mut self) -> Result<()> {
        self.load_agents().await?;
        self.load_tasks().await?;
        self.update_system_stats().await?;
        self.last_update = Instant::now();
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_delegation_input (lines 1050-1066) ---[0m
    pub async fn start_delegation_input(&mut self) -> Result<()> {
        self.input_mode = InputMode::DelegationInput;
        self.delegation_input.clear();

        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.add_log("Master", "Enter task description to analyze:")
                    .await;
            }
            DelegationMode::Delegate => {
                self.add_log("Master", "Enter task description to delegate:")
                    .await;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:656-663 method show_agent_details <-> ./crates/ccswarm/src/tui/app.rs:1069-1080 method execute_delegation_action
  Similarity: 94.49%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_agent_details (lines 656-663) ---[0m
    async fn show_agent_details(&mut self, agent_id: &str) -> Result<()> {
        self.add_log(
            "System",
            &format!("Showing details for agent: {}", agent_id),
        )
        .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_delegation_action (lines 1069-1080) ---[0m
    async fn execute_delegation_action(&mut self, input: &str) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.analyze_task_for_delegation(input).await?;
            }
            DelegationMode::Delegate => {
                self.delegate_task_to_agent(input).await?;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:1041-1047 method switch_delegation_mode <-> ./crates/ccswarm/src/tui/app.rs:1300-1314 method handle_delegation_enter
  Similarity: 84.07%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:switch_delegation_mode (lines 1041-1047) ---[0m
    pub fn switch_delegation_mode(&mut self) {
        self.delegation_mode = match self.delegation_mode {
            DelegationMode::Analyze => DelegationMode::Delegate,
            DelegationMode::Delegate => DelegationMode::ViewStats,
            DelegationMode::ViewStats => DelegationMode::Analyze,
        };
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_delegation_enter (lines 1300-1314) ---[0m
    pub async fn handle_delegation_enter(&mut self) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze | DelegationMode::Delegate => {
                self.start_delegation_input().await?;
            }
            DelegationMode::ViewStats => {
                // Show delegation statistics
                let stats = self.get_delegation_stats();
                for line in stats.lines() {
                    self.add_log("Master", line).await;
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:377-384 method delete_current_session <-> ./crates/ccswarm/src/tui/app.rs:1050-1066 method start_delegation_input
  Similarity: 73.04%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:delete_current_session (lines 377-384) ---[0m
    pub async fn delete_current_session(&mut self) -> Result<()> {
        if let Some(agent) = self.agents.get(self.selected_agent) {
            self.add_log("System", &format!("Deleting agent: {}", agent.name))
                .await;
            // TODO: Implement actual agent deletion
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_delegation_input (lines 1050-1066) ---[0m
    pub async fn start_delegation_input(&mut self) -> Result<()> {
        self.input_mode = InputMode::DelegationInput;
        self.delegation_input.clear();

        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.add_log("Master", "Enter task description to analyze:")
                    .await;
            }
            DelegationMode::Delegate => {
                self.add_log("Master", "Enter task description to delegate:")
                    .await;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:1025-1030 method list_worktrees <-> ./crates/ccswarm/src/tui/app.rs:1300-1314 method handle_delegation_enter
  Similarity: 84.63%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:list_worktrees (lines 1025-1030) ---[0m
    async fn list_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Git worktrees:").await;
        // TODO: Implement actual worktree listing
        self.add_log("System", "  No worktrees found").await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_delegation_enter (lines 1300-1314) ---[0m
    pub async fn handle_delegation_enter(&mut self) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze | DelegationMode::Delegate => {
                self.start_delegation_input().await?;
            }
            DelegationMode::ViewStats => {
                // Show delegation statistics
                let stats = self.get_delegation_stats();
                for line in stats.lines() {
                    self.add_log("Master", line).await;
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:1033-1038 method prune_worktrees <-> ./crates/ccswarm/src/tui/app.rs:1300-1314 method handle_delegation_enter
  Similarity: 84.63%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:prune_worktrees (lines 1033-1038) ---[0m
    async fn prune_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Pruning stale worktrees...").await;
        // TODO: Implement actual worktree pruning
        self.add_log("System", "Worktree pruning completed").await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_delegation_enter (lines 1300-1314) ---[0m
    pub async fn handle_delegation_enter(&mut self) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze | DelegationMode::Delegate => {
                self.start_delegation_input().await?;
            }
            DelegationMode::ViewStats => {
                // Show delegation statistics
                let stats = self.get_delegation_stats();
                for line in stats.lines() {
                    self.add_log("Master", line).await;
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:270-278 method reset_selection <-> ./crates/ccswarm/src/tui/app.rs:1300-1314 method handle_delegation_enter
  Similarity: 73.61%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:reset_selection (lines 270-278) ---[0m
    fn reset_selection(&mut self) {
        match self.current_tab {
            Tab::Agents => self.selected_agent = 0,
            Tab::Tasks => self.selected_task = 0,
            Tab::Logs => self.selected_log = 0,
            Tab::Delegation => self.selected_delegation = 0,
            _ => {}
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_delegation_enter (lines 1300-1314) ---[0m
    pub async fn handle_delegation_enter(&mut self) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze | DelegationMode::Delegate => {
                self.start_delegation_input().await?;
            }
            DelegationMode::ViewStats => {
                // Show delegation statistics
                let stats = self.get_delegation_stats();
                for line in stats.lines() {
                    self.add_log("Master", line).await;
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:192-201 method next_tab <-> ./crates/ccswarm/src/tui/app.rs:1069-1080 method execute_delegation_action
  Similarity: 78.98%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:next_tab (lines 192-201) ---[0m
    pub fn next_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Agents,
            Tab::Agents => Tab::Tasks,
            Tab::Tasks => Tab::Logs,
            Tab::Logs => Tab::Delegation,
            Tab::Delegation => Tab::Overview,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_delegation_action (lines 1069-1080) ---[0m
    async fn execute_delegation_action(&mut self, input: &str) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.analyze_task_for_delegation(input).await?;
            }
            DelegationMode::Delegate => {
                self.delegate_task_to_agent(input).await?;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:204-213 method previous_tab <-> ./crates/ccswarm/src/tui/app.rs:1069-1080 method execute_delegation_action
  Similarity: 78.98%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_tab (lines 204-213) ---[0m
    pub fn previous_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Delegation,
            Tab::Agents => Tab::Overview,
            Tab::Tasks => Tab::Agents,
            Tab::Logs => Tab::Tasks,
            Tab::Delegation => Tab::Logs,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_delegation_action (lines 1069-1080) ---[0m
    async fn execute_delegation_action(&mut self, input: &str) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.analyze_task_for_delegation(input).await?;
            }
            DelegationMode::Delegate => {
                self.delegate_task_to_agent(input).await?;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:743-755 method execute_add_task <-> ./crates/ccswarm/src/tui/app.rs:1006-1013 method start_orchestrator
  Similarity: 78.62%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_add_task (lines 743-755) ---[0m
    async fn execute_add_task(&mut self, description: &str) -> Result<()> {
        // Parse priority and type from description if included
        let (desc, priority, task_type) = self.parse_task_description(description);

        let task =
            crate::agent::Task::new(uuid::Uuid::new_v4().to_string(), desc, priority, task_type);

        self.task_queue.add_task(&task).await?;
        self.add_log("System", &format!("Task added: {}", task.description))
            .await;
        self.refresh_data().await?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_orchestrator (lines 1006-1013) ---[0m
    async fn start_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Starting orchestrator...").await;
        // TODO: Implement actual orchestrator start
        self.system_status = "Running".to_string();
        self.add_log("System", "Orchestrator started successfully")
            .await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:368-374 method create_new_session <-> ./crates/ccswarm/src/tui/app.rs:1300-1314 method handle_delegation_enter
  Similarity: 73.24%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:create_new_session (lines 368-374) ---[0m
    pub async fn create_new_session(&mut self) -> Result<()> {
        self.input_mode = InputMode::CreatingAgent;
        self.input_buffer.clear();
        self.add_log("System", "Enter agent type (frontend/backend/devops/qa):")
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_delegation_enter (lines 1300-1314) ---[0m
    pub async fn handle_delegation_enter(&mut self) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze | DelegationMode::Delegate => {
                self.start_delegation_input().await?;
            }
            DelegationMode::ViewStats => {
                // Show delegation statistics
                let stats = self.get_delegation_stats();
                for line in stats.lines() {
                    self.add_log("Master", line).await;
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:402-408 method open_command_prompt <-> ./crates/ccswarm/src/tui/app.rs:1300-1314 method handle_delegation_enter
  Similarity: 73.24%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:open_command_prompt (lines 402-408) ---[0m
    pub async fn open_command_prompt(&mut self) -> Result<()> {
        self.input_mode = InputMode::Command;
        self.input_buffer.clear();
        self.add_log("System", "Enter command (help for available commands):")
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_delegation_enter (lines 1300-1314) ---[0m
    pub async fn handle_delegation_enter(&mut self) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze | DelegationMode::Delegate => {
                self.start_delegation_input().await?;
            }
            DelegationMode::ViewStats => {
                // Show delegation statistics
                let stats = self.get_delegation_stats();
                for line in stats.lines() {
                    self.add_log("Master", line).await;
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:20-28 method title <-> ./crates/ccswarm/src/tui/app.rs:1318-1327 function parse_agent_status
  Similarity: 84.62%

[36m--- ./crates/ccswarm/src/tui/app.rs:title (lines 20-28) ---[0m
    pub fn title(&self) -> &str {
        match self {
            Tab::Overview => "Overview",
            Tab::Agents => "Agents",
            Tab::Tasks => "Tasks",
            Tab::Logs => "Logs",
            Tab::Delegation => "Delegation",
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:parse_agent_status (lines 1318-1327) ---[0m
fn parse_agent_status(status: &str) -> AgentStatus {
    match status {
        "Initializing" => AgentStatus::Initializing,
        "Available" => AgentStatus::Available,
        "Working" => AgentStatus::Working,
        "WaitingForReview" => AgentStatus::WaitingForReview,
        "ShuttingDown" => AgentStatus::ShuttingDown,
        _ => AgentStatus::Error(format!("Unknown status: {}", status)),
    }
}

  ./crates/ccswarm/src/tui/app.rs:666-670 method show_task_details <-> ./crates/ccswarm/src/tui/app.rs:1069-1080 method execute_delegation_action
  Similarity: 94.49%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_task_details (lines 666-670) ---[0m
    async fn show_task_details(&mut self, task_id: &str) -> Result<()> {
        self.add_log("System", &format!("Showing details for task: {}", task_id))
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_delegation_action (lines 1069-1080) ---[0m
    async fn execute_delegation_action(&mut self, input: &str) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.analyze_task_for_delegation(input).await?;
            }
            DelegationMode::Delegate => {
                self.delegate_task_to_agent(input).await?;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:192-201 method next_tab <-> ./crates/ccswarm/src/tui/app.rs:377-384 method delete_current_session
  Similarity: 88.86%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:next_tab (lines 192-201) ---[0m
    pub fn next_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Agents,
            Tab::Agents => Tab::Tasks,
            Tab::Tasks => Tab::Logs,
            Tab::Logs => Tab::Delegation,
            Tab::Delegation => Tab::Overview,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:delete_current_session (lines 377-384) ---[0m
    pub async fn delete_current_session(&mut self) -> Result<()> {
        if let Some(agent) = self.agents.get(self.selected_agent) {
            self.add_log("System", &format!("Deleting agent: {}", agent.name))
                .await;
            // TODO: Implement actual agent deletion
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:204-213 method previous_tab <-> ./crates/ccswarm/src/tui/app.rs:377-384 method delete_current_session
  Similarity: 88.86%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_tab (lines 204-213) ---[0m
    pub fn previous_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Delegation,
            Tab::Agents => Tab::Overview,
            Tab::Tasks => Tab::Agents,
            Tab::Logs => Tab::Tasks,
            Tab::Delegation => Tab::Logs,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:delete_current_session (lines 377-384) ---[0m
    pub async fn delete_current_session(&mut self) -> Result<()> {
        if let Some(agent) = self.agents.get(self.selected_agent) {
            self.add_log("System", &format!("Deleting agent: {}", agent.name))
                .await;
            // TODO: Implement actual agent deletion
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:377-384 method delete_current_session <-> ./crates/ccswarm/src/tui/app.rs:1069-1080 method execute_delegation_action
  Similarity: 79.74%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:delete_current_session (lines 377-384) ---[0m
    pub async fn delete_current_session(&mut self) -> Result<()> {
        if let Some(agent) = self.agents.get(self.selected_agent) {
            self.add_log("System", &format!("Deleting agent: {}", agent.name))
                .await;
            // TODO: Implement actual agent deletion
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_delegation_action (lines 1069-1080) ---[0m
    async fn execute_delegation_action(&mut self, input: &str) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.analyze_task_for_delegation(input).await?;
            }
            DelegationMode::Delegate => {
                self.delegate_task_to_agent(input).await?;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:743-755 method execute_add_task <-> ./crates/ccswarm/src/tui/app.rs:1016-1022 method stop_orchestrator
  Similarity: 78.62%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_add_task (lines 743-755) ---[0m
    async fn execute_add_task(&mut self, description: &str) -> Result<()> {
        // Parse priority and type from description if included
        let (desc, priority, task_type) = self.parse_task_description(description);

        let task =
            crate::agent::Task::new(uuid::Uuid::new_v4().to_string(), desc, priority, task_type);

        self.task_queue.add_task(&task).await?;
        self.add_log("System", &format!("Task added: {}", task.description))
            .await;
        self.refresh_data().await?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:stop_orchestrator (lines 1016-1022) ---[0m
    async fn stop_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Stopping orchestrator...").await;
        // TODO: Implement actual orchestrator stop
        self.system_status = "Stopped".to_string();
        self.add_log("System", "Orchestrator stopped").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:394-399 method add_task_prompt <-> ./crates/ccswarm/src/tui/app.rs:1300-1314 method handle_delegation_enter
  Similarity: 73.24%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:add_task_prompt (lines 394-399) ---[0m
    pub async fn add_task_prompt(&mut self) -> Result<()> {
        self.input_mode = InputMode::AddingTask;
        self.input_buffer.clear();
        self.add_log("System", "Enter task description:").await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_delegation_enter (lines 1300-1314) ---[0m
    pub async fn handle_delegation_enter(&mut self) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze | DelegationMode::Delegate => {
                self.start_delegation_input().await?;
            }
            DelegationMode::ViewStats => {
                // Show delegation statistics
                let stats = self.get_delegation_stats();
                for line in stats.lines() {
                    self.add_log("Master", line).await;
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:270-278 method reset_selection <-> ./crates/ccswarm/src/tui/app.rs:1318-1327 function parse_agent_status
  Similarity: 80.22%

[36m--- ./crates/ccswarm/src/tui/app.rs:reset_selection (lines 270-278) ---[0m
    fn reset_selection(&mut self) {
        match self.current_tab {
            Tab::Agents => self.selected_agent = 0,
            Tab::Tasks => self.selected_task = 0,
            Tab::Logs => self.selected_log = 0,
            Tab::Delegation => self.selected_delegation = 0,
            _ => {}
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:parse_agent_status (lines 1318-1327) ---[0m
fn parse_agent_status(status: &str) -> AgentStatus {
    match status {
        "Initializing" => AgentStatus::Initializing,
        "Available" => AgentStatus::Available,
        "Working" => AgentStatus::Working,
        "WaitingForReview" => AgentStatus::WaitingForReview,
        "ShuttingDown" => AgentStatus::ShuttingDown,
        _ => AgentStatus::Error(format!("Unknown status: {}", status)),
    }
}

  ./crates/ccswarm/src/tui/app.rs:20-28 method title <-> ./crates/ccswarm/src/tui/app.rs:270-278 method reset_selection
  Similarity: 83.85%
  Classes: Tab <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:title (lines 20-28) ---[0m
    pub fn title(&self) -> &str {
        match self {
            Tab::Overview => "Overview",
            Tab::Agents => "Agents",
            Tab::Tasks => "Tasks",
            Tab::Logs => "Logs",
            Tab::Delegation => "Delegation",
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:reset_selection (lines 270-278) ---[0m
    fn reset_selection(&mut self) {
        match self.current_tab {
            Tab::Agents => self.selected_agent = 0,
            Tab::Tasks => self.selected_task = 0,
            Tab::Logs => self.selected_log = 0,
            Tab::Delegation => self.selected_delegation = 0,
            _ => {}
        }
    }

  ./crates/ccswarm/src/tui/app.rs:377-384 method delete_current_session <-> ./crates/ccswarm/src/tui/app.rs:656-663 method show_agent_details
  Similarity: 94.03%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:delete_current_session (lines 377-384) ---[0m
    pub async fn delete_current_session(&mut self) -> Result<()> {
        if let Some(agent) = self.agents.get(self.selected_agent) {
            self.add_log("System", &format!("Deleting agent: {}", agent.name))
                .await;
            // TODO: Implement actual agent deletion
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_agent_details (lines 656-663) ---[0m
    async fn show_agent_details(&mut self, agent_id: &str) -> Result<()> {
        self.add_log(
            "System",
            &format!("Showing details for agent: {}", agent_id),
        )
        .await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:437-442 method update <-> ./crates/ccswarm/src/tui/app.rs:1069-1080 method execute_delegation_action
  Similarity: 83.33%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:update (lines 437-442) ---[0m
    pub async fn update(&mut self) -> Result<()> {
        if self.last_update.elapsed() >= self.update_interval {
            self.refresh_data().await?;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_delegation_action (lines 1069-1080) ---[0m
    async fn execute_delegation_action(&mut self, input: &str) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.analyze_task_for_delegation(input).await?;
            }
            DelegationMode::Delegate => {
                self.delegate_task_to_agent(input).await?;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:1006-1013 method start_orchestrator <-> ./crates/ccswarm/src/tui/app.rs:1016-1022 method stop_orchestrator
  Similarity: 98.78%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_orchestrator (lines 1006-1013) ---[0m
    async fn start_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Starting orchestrator...").await;
        // TODO: Implement actual orchestrator start
        self.system_status = "Running".to_string();
        self.add_log("System", "Orchestrator started successfully")
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:stop_orchestrator (lines 1016-1022) ---[0m
    async fn stop_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Stopping orchestrator...").await;
        // TODO: Implement actual orchestrator stop
        self.system_status = "Stopped".to_string();
        self.add_log("System", "Orchestrator stopped").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:270-278 method reset_selection <-> ./crates/ccswarm/src/tui/app.rs:656-663 method show_agent_details
  Similarity: 84.23%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:reset_selection (lines 270-278) ---[0m
    fn reset_selection(&mut self) {
        match self.current_tab {
            Tab::Agents => self.selected_agent = 0,
            Tab::Tasks => self.selected_task = 0,
            Tab::Logs => self.selected_log = 0,
            Tab::Delegation => self.selected_delegation = 0,
            _ => {}
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_agent_details (lines 656-663) ---[0m
    async fn show_agent_details(&mut self, agent_id: &str) -> Result<()> {
        self.add_log(
            "System",
            &format!("Showing details for agent: {}", agent_id),
        )
        .await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:192-201 method next_tab <-> ./crates/ccswarm/src/tui/app.rs:437-442 method update
  Similarity: 88.86%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:next_tab (lines 192-201) ---[0m
    pub fn next_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Agents,
            Tab::Agents => Tab::Tasks,
            Tab::Tasks => Tab::Logs,
            Tab::Logs => Tab::Delegation,
            Tab::Delegation => Tab::Overview,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:update (lines 437-442) ---[0m
    pub async fn update(&mut self) -> Result<()> {
        if self.last_update.elapsed() >= self.update_interval {
            self.refresh_data().await?;
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:204-213 method previous_tab <-> ./crates/ccswarm/src/tui/app.rs:437-442 method update
  Similarity: 88.86%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_tab (lines 204-213) ---[0m
    pub fn previous_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Delegation,
            Tab::Agents => Tab::Overview,
            Tab::Tasks => Tab::Agents,
            Tab::Logs => Tab::Tasks,
            Tab::Delegation => Tab::Logs,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:update (lines 437-442) ---[0m
    pub async fn update(&mut self) -> Result<()> {
        if self.last_update.elapsed() >= self.update_interval {
            self.refresh_data().await?;
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:1025-1030 method list_worktrees <-> ./crates/ccswarm/src/tui/app.rs:1069-1080 method execute_delegation_action
  Similarity: 78.72%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:list_worktrees (lines 1025-1030) ---[0m
    async fn list_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Git worktrees:").await;
        // TODO: Implement actual worktree listing
        self.add_log("System", "  No worktrees found").await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_delegation_action (lines 1069-1080) ---[0m
    async fn execute_delegation_action(&mut self, input: &str) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.analyze_task_for_delegation(input).await?;
            }
            DelegationMode::Delegate => {
                self.delegate_task_to_agent(input).await?;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:1033-1038 method prune_worktrees <-> ./crates/ccswarm/src/tui/app.rs:1069-1080 method execute_delegation_action
  Similarity: 78.72%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:prune_worktrees (lines 1033-1038) ---[0m
    async fn prune_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Pruning stale worktrees...").await;
        // TODO: Implement actual worktree pruning
        self.add_log("System", "Worktree pruning completed").await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_delegation_action (lines 1069-1080) ---[0m
    async fn execute_delegation_action(&mut self, input: &str) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.analyze_task_for_delegation(input).await?;
            }
            DelegationMode::Delegate => {
                self.delegate_task_to_agent(input).await?;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:20-28 method title <-> ./crates/ccswarm/src/tui/app.rs:656-663 method show_agent_details
  Similarity: 82.94%
  Classes: Tab <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:title (lines 20-28) ---[0m
    pub fn title(&self) -> &str {
        match self {
            Tab::Overview => "Overview",
            Tab::Agents => "Agents",
            Tab::Tasks => "Tasks",
            Tab::Logs => "Logs",
            Tab::Delegation => "Delegation",
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_agent_details (lines 656-663) ---[0m
    async fn show_agent_details(&mut self, agent_id: &str) -> Result<()> {
        self.add_log(
            "System",
            &format!("Showing details for agent: {}", agent_id),
        )
        .await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:368-374 method create_new_session <-> ./crates/ccswarm/src/tui/app.rs:402-408 method open_command_prompt
  Similarity: 98.93%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:create_new_session (lines 368-374) ---[0m
    pub async fn create_new_session(&mut self) -> Result<()> {
        self.input_mode = InputMode::CreatingAgent;
        self.input_buffer.clear();
        self.add_log("System", "Enter agent type (frontend/backend/devops/qa):")
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:open_command_prompt (lines 402-408) ---[0m
    pub async fn open_command_prompt(&mut self) -> Result<()> {
        self.input_mode = InputMode::Command;
        self.input_buffer.clear();
        self.add_log("System", "Enter command (help for available commands):")
            .await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:192-201 method next_tab <-> ./crates/ccswarm/src/tui/app.rs:1041-1047 method switch_delegation_mode
  Similarity: 79.32%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:next_tab (lines 192-201) ---[0m
    pub fn next_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Agents,
            Tab::Agents => Tab::Tasks,
            Tab::Tasks => Tab::Logs,
            Tab::Logs => Tab::Delegation,
            Tab::Delegation => Tab::Overview,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:switch_delegation_mode (lines 1041-1047) ---[0m
    pub fn switch_delegation_mode(&mut self) {
        self.delegation_mode = match self.delegation_mode {
            DelegationMode::Analyze => DelegationMode::Delegate,
            DelegationMode::Delegate => DelegationMode::ViewStats,
            DelegationMode::ViewStats => DelegationMode::Analyze,
        };
    }

  ./crates/ccswarm/src/tui/app.rs:204-213 method previous_tab <-> ./crates/ccswarm/src/tui/app.rs:1041-1047 method switch_delegation_mode
  Similarity: 79.32%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_tab (lines 204-213) ---[0m
    pub fn previous_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Delegation,
            Tab::Agents => Tab::Overview,
            Tab::Tasks => Tab::Agents,
            Tab::Logs => Tab::Tasks,
            Tab::Delegation => Tab::Logs,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:switch_delegation_mode (lines 1041-1047) ---[0m
    pub fn switch_delegation_mode(&mut self) {
        self.delegation_mode = match self.delegation_mode {
            DelegationMode::Analyze => DelegationMode::Delegate,
            DelegationMode::Delegate => DelegationMode::ViewStats,
            DelegationMode::ViewStats => DelegationMode::Analyze,
        };
    }

  ./crates/ccswarm/src/tui/app.rs:1006-1013 method start_orchestrator <-> ./crates/ccswarm/src/tui/app.rs:1025-1030 method list_worktrees
  Similarity: 93.78%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_orchestrator (lines 1006-1013) ---[0m
    async fn start_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Starting orchestrator...").await;
        // TODO: Implement actual orchestrator start
        self.system_status = "Running".to_string();
        self.add_log("System", "Orchestrator started successfully")
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_worktrees (lines 1025-1030) ---[0m
    async fn list_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Git worktrees:").await;
        // TODO: Implement actual worktree listing
        self.add_log("System", "  No worktrees found").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:1006-1013 method start_orchestrator <-> ./crates/ccswarm/src/tui/app.rs:1033-1038 method prune_worktrees
  Similarity: 93.78%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:start_orchestrator (lines 1006-1013) ---[0m
    async fn start_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Starting orchestrator...").await;
        // TODO: Implement actual orchestrator start
        self.system_status = "Running".to_string();
        self.add_log("System", "Orchestrator started successfully")
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:prune_worktrees (lines 1033-1038) ---[0m
    async fn prune_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Pruning stale worktrees...").await;
        // TODO: Implement actual worktree pruning
        self.add_log("System", "Worktree pruning completed").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:368-374 method create_new_session <-> ./crates/ccswarm/src/tui/app.rs:394-399 method add_task_prompt
  Similarity: 98.93%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:create_new_session (lines 368-374) ---[0m
    pub async fn create_new_session(&mut self) -> Result<()> {
        self.input_mode = InputMode::CreatingAgent;
        self.input_buffer.clear();
        self.add_log("System", "Enter agent type (frontend/backend/devops/qa):")
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:add_task_prompt (lines 394-399) ---[0m
    pub async fn add_task_prompt(&mut self) -> Result<()> {
        self.input_mode = InputMode::AddingTask;
        self.input_buffer.clear();
        self.add_log("System", "Enter task description:").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:394-399 method add_task_prompt <-> ./crates/ccswarm/src/tui/app.rs:402-408 method open_command_prompt
  Similarity: 98.93%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:add_task_prompt (lines 394-399) ---[0m
    pub async fn add_task_prompt(&mut self) -> Result<()> {
        self.input_mode = InputMode::AddingTask;
        self.input_buffer.clear();
        self.add_log("System", "Enter task description:").await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:open_command_prompt (lines 402-408) ---[0m
    pub async fn open_command_prompt(&mut self) -> Result<()> {
        self.input_mode = InputMode::Command;
        self.input_buffer.clear();
        self.add_log("System", "Enter command (help for available commands):")
            .await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:437-442 method update <-> ./crates/ccswarm/src/tui/app.rs:656-663 method show_agent_details
  Similarity: 90.85%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:update (lines 437-442) ---[0m
    pub async fn update(&mut self) -> Result<()> {
        if self.last_update.elapsed() >= self.update_interval {
            self.refresh_data().await?;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_agent_details (lines 656-663) ---[0m
    async fn show_agent_details(&mut self, agent_id: &str) -> Result<()> {
        self.add_log(
            "System",
            &format!("Showing details for agent: {}", agent_id),
        )
        .await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:368-374 method create_new_session <-> ./crates/ccswarm/src/tui/app.rs:1006-1013 method start_orchestrator
  Similarity: 83.65%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:create_new_session (lines 368-374) ---[0m
    pub async fn create_new_session(&mut self) -> Result<()> {
        self.input_mode = InputMode::CreatingAgent;
        self.input_buffer.clear();
        self.add_log("System", "Enter agent type (frontend/backend/devops/qa):")
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_orchestrator (lines 1006-1013) ---[0m
    async fn start_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Starting orchestrator...").await;
        // TODO: Implement actual orchestrator start
        self.system_status = "Running".to_string();
        self.add_log("System", "Orchestrator started successfully")
            .await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:402-408 method open_command_prompt <-> ./crates/ccswarm/src/tui/app.rs:1006-1013 method start_orchestrator
  Similarity: 83.65%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:open_command_prompt (lines 402-408) ---[0m
    pub async fn open_command_prompt(&mut self) -> Result<()> {
        self.input_mode = InputMode::Command;
        self.input_buffer.clear();
        self.add_log("System", "Enter command (help for available commands):")
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_orchestrator (lines 1006-1013) ---[0m
    async fn start_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Starting orchestrator...").await;
        // TODO: Implement actual orchestrator start
        self.system_status = "Running".to_string();
        self.add_log("System", "Orchestrator started successfully")
            .await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:656-663 method show_agent_details <-> ./crates/ccswarm/src/tui/app.rs:666-670 method show_task_details
  Similarity: 96.19%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_agent_details (lines 656-663) ---[0m
    async fn show_agent_details(&mut self, agent_id: &str) -> Result<()> {
        self.add_log(
            "System",
            &format!("Showing details for agent: {}", agent_id),
        )
        .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_task_details (lines 666-670) ---[0m
    async fn show_task_details(&mut self, task_id: &str) -> Result<()> {
        self.add_log("System", &format!("Showing details for task: {}", task_id))
            .await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:192-201 method next_tab <-> ./crates/ccswarm/src/tui/app.rs:1025-1030 method list_worktrees
  Similarity: 76.70%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:next_tab (lines 192-201) ---[0m
    pub fn next_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Agents,
            Tab::Agents => Tab::Tasks,
            Tab::Tasks => Tab::Logs,
            Tab::Logs => Tab::Delegation,
            Tab::Delegation => Tab::Overview,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_worktrees (lines 1025-1030) ---[0m
    async fn list_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Git worktrees:").await;
        // TODO: Implement actual worktree listing
        self.add_log("System", "  No worktrees found").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:192-201 method next_tab <-> ./crates/ccswarm/src/tui/app.rs:1033-1038 method prune_worktrees
  Similarity: 76.70%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:next_tab (lines 192-201) ---[0m
    pub fn next_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Agents,
            Tab::Agents => Tab::Tasks,
            Tab::Tasks => Tab::Logs,
            Tab::Logs => Tab::Delegation,
            Tab::Delegation => Tab::Overview,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:prune_worktrees (lines 1033-1038) ---[0m
    async fn prune_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Pruning stale worktrees...").await;
        // TODO: Implement actual worktree pruning
        self.add_log("System", "Worktree pruning completed").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:204-213 method previous_tab <-> ./crates/ccswarm/src/tui/app.rs:1025-1030 method list_worktrees
  Similarity: 76.70%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_tab (lines 204-213) ---[0m
    pub fn previous_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Delegation,
            Tab::Agents => Tab::Overview,
            Tab::Tasks => Tab::Agents,
            Tab::Logs => Tab::Tasks,
            Tab::Delegation => Tab::Logs,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_worktrees (lines 1025-1030) ---[0m
    async fn list_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Git worktrees:").await;
        // TODO: Implement actual worktree listing
        self.add_log("System", "  No worktrees found").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:204-213 method previous_tab <-> ./crates/ccswarm/src/tui/app.rs:1033-1038 method prune_worktrees
  Similarity: 76.70%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_tab (lines 204-213) ---[0m
    pub fn previous_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Delegation,
            Tab::Agents => Tab::Overview,
            Tab::Tasks => Tab::Agents,
            Tab::Logs => Tab::Tasks,
            Tab::Delegation => Tab::Logs,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:prune_worktrees (lines 1033-1038) ---[0m
    async fn prune_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Pruning stale worktrees...").await;
        // TODO: Implement actual worktree pruning
        self.add_log("System", "Worktree pruning completed").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:377-384 method delete_current_session <-> ./crates/ccswarm/src/tui/app.rs:666-670 method show_task_details
  Similarity: 94.03%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:delete_current_session (lines 377-384) ---[0m
    pub async fn delete_current_session(&mut self) -> Result<()> {
        if let Some(agent) = self.agents.get(self.selected_agent) {
            self.add_log("System", &format!("Deleting agent: {}", agent.name))
                .await;
            // TODO: Implement actual agent deletion
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_task_details (lines 666-670) ---[0m
    async fn show_task_details(&mut self, task_id: &str) -> Result<()> {
        self.add_log("System", &format!("Showing details for task: {}", task_id))
            .await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:1016-1022 method stop_orchestrator <-> ./crates/ccswarm/src/tui/app.rs:1025-1030 method list_worktrees
  Similarity: 93.78%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:stop_orchestrator (lines 1016-1022) ---[0m
    async fn stop_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Stopping orchestrator...").await;
        // TODO: Implement actual orchestrator stop
        self.system_status = "Stopped".to_string();
        self.add_log("System", "Orchestrator stopped").await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_worktrees (lines 1025-1030) ---[0m
    async fn list_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Git worktrees:").await;
        // TODO: Implement actual worktree listing
        self.add_log("System", "  No worktrees found").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:1016-1022 method stop_orchestrator <-> ./crates/ccswarm/src/tui/app.rs:1033-1038 method prune_worktrees
  Similarity: 93.78%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:stop_orchestrator (lines 1016-1022) ---[0m
    async fn stop_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Stopping orchestrator...").await;
        // TODO: Implement actual orchestrator stop
        self.system_status = "Stopped".to_string();
        self.add_log("System", "Orchestrator stopped").await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:prune_worktrees (lines 1033-1038) ---[0m
    async fn prune_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Pruning stale worktrees...").await;
        // TODO: Implement actual worktree pruning
        self.add_log("System", "Worktree pruning completed").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:703-707 method handle_backspace <-> ./crates/ccswarm/src/tui/app.rs:1069-1080 method execute_delegation_action
  Similarity: 70.77%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_backspace (lines 703-707) ---[0m
    pub fn handle_backspace(&mut self) {
        if self.input_mode != InputMode::Normal && !self.input_buffer.is_empty() {
            self.input_buffer.pop();
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:execute_delegation_action (lines 1069-1080) ---[0m
    async fn execute_delegation_action(&mut self, input: &str) -> Result<()> {
        match self.delegation_mode {
            DelegationMode::Analyze => {
                self.analyze_task_for_delegation(input).await?;
            }
            DelegationMode::Delegate => {
                self.delegate_task_to_agent(input).await?;
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:192-201 method next_tab <-> ./crates/ccswarm/src/tui/app.rs:703-707 method handle_backspace
  Similarity: 80.11%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:next_tab (lines 192-201) ---[0m
    pub fn next_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Agents,
            Tab::Agents => Tab::Tasks,
            Tab::Tasks => Tab::Logs,
            Tab::Logs => Tab::Delegation,
            Tab::Delegation => Tab::Overview,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_backspace (lines 703-707) ---[0m
    pub fn handle_backspace(&mut self) {
        if self.input_mode != InputMode::Normal && !self.input_buffer.is_empty() {
            self.input_buffer.pop();
        }
    }

  ./crates/ccswarm/src/tui/app.rs:204-213 method previous_tab <-> ./crates/ccswarm/src/tui/app.rs:703-707 method handle_backspace
  Similarity: 80.11%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:previous_tab (lines 204-213) ---[0m
    pub fn previous_tab(&mut self) {
        self.current_tab = match self.current_tab {
            Tab::Overview => Tab::Delegation,
            Tab::Agents => Tab::Overview,
            Tab::Tasks => Tab::Agents,
            Tab::Logs => Tab::Tasks,
            Tab::Delegation => Tab::Logs,
        };
        self.reset_selection();
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_backspace (lines 703-707) ---[0m
    pub fn handle_backspace(&mut self) {
        if self.input_mode != InputMode::Normal && !self.input_buffer.is_empty() {
            self.input_buffer.pop();
        }
    }

  ./crates/ccswarm/src/tui/app.rs:377-384 method delete_current_session <-> ./crates/ccswarm/src/tui/app.rs:437-442 method update
  Similarity: 85.28%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:delete_current_session (lines 377-384) ---[0m
    pub async fn delete_current_session(&mut self) -> Result<()> {
        if let Some(agent) = self.agents.get(self.selected_agent) {
            self.add_log("System", &format!("Deleting agent: {}", agent.name))
                .await;
            // TODO: Implement actual agent deletion
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:update (lines 437-442) ---[0m
    pub async fn update(&mut self) -> Result<()> {
        if self.last_update.elapsed() >= self.update_interval {
            self.refresh_data().await?;
        }
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:1025-1030 method list_worktrees <-> ./crates/ccswarm/src/tui/app.rs:1033-1038 method prune_worktrees
  Similarity: 98.91%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:list_worktrees (lines 1025-1030) ---[0m
    async fn list_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Git worktrees:").await;
        // TODO: Implement actual worktree listing
        self.add_log("System", "  No worktrees found").await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:prune_worktrees (lines 1033-1038) ---[0m
    async fn prune_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Pruning stale worktrees...").await;
        // TODO: Implement actual worktree pruning
        self.add_log("System", "Worktree pruning completed").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:270-278 method reset_selection <-> ./crates/ccswarm/src/tui/app.rs:666-670 method show_task_details
  Similarity: 84.23%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:reset_selection (lines 270-278) ---[0m
    fn reset_selection(&mut self) {
        match self.current_tab {
            Tab::Agents => self.selected_agent = 0,
            Tab::Tasks => self.selected_task = 0,
            Tab::Logs => self.selected_log = 0,
            Tab::Delegation => self.selected_delegation = 0,
            _ => {}
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_task_details (lines 666-670) ---[0m
    async fn show_task_details(&mut self, task_id: &str) -> Result<()> {
        self.add_log("System", &format!("Showing details for task: {}", task_id))
            .await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:377-384 method delete_current_session <-> ./crates/ccswarm/src/tui/app.rs:1041-1047 method switch_delegation_mode
  Similarity: 78.47%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:delete_current_session (lines 377-384) ---[0m
    pub async fn delete_current_session(&mut self) -> Result<()> {
        if let Some(agent) = self.agents.get(self.selected_agent) {
            self.add_log("System", &format!("Deleting agent: {}", agent.name))
                .await;
            // TODO: Implement actual agent deletion
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:switch_delegation_mode (lines 1041-1047) ---[0m
    pub fn switch_delegation_mode(&mut self) {
        self.delegation_mode = match self.delegation_mode {
            DelegationMode::Analyze => DelegationMode::Delegate,
            DelegationMode::Delegate => DelegationMode::ViewStats,
            DelegationMode::ViewStats => DelegationMode::Analyze,
        };
    }

  ./crates/ccswarm/src/tui/app.rs:368-374 method create_new_session <-> ./crates/ccswarm/src/tui/app.rs:1016-1022 method stop_orchestrator
  Similarity: 83.65%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:create_new_session (lines 368-374) ---[0m
    pub async fn create_new_session(&mut self) -> Result<()> {
        self.input_mode = InputMode::CreatingAgent;
        self.input_buffer.clear();
        self.add_log("System", "Enter agent type (frontend/backend/devops/qa):")
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:stop_orchestrator (lines 1016-1022) ---[0m
    async fn stop_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Stopping orchestrator...").await;
        // TODO: Implement actual orchestrator stop
        self.system_status = "Stopped".to_string();
        self.add_log("System", "Orchestrator stopped").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:394-399 method add_task_prompt <-> ./crates/ccswarm/src/tui/app.rs:1006-1013 method start_orchestrator
  Similarity: 83.65%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:add_task_prompt (lines 394-399) ---[0m
    pub async fn add_task_prompt(&mut self) -> Result<()> {
        self.input_mode = InputMode::AddingTask;
        self.input_buffer.clear();
        self.add_log("System", "Enter task description:").await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_orchestrator (lines 1006-1013) ---[0m
    async fn start_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Starting orchestrator...").await;
        // TODO: Implement actual orchestrator start
        self.system_status = "Running".to_string();
        self.add_log("System", "Orchestrator started successfully")
            .await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:402-408 method open_command_prompt <-> ./crates/ccswarm/src/tui/app.rs:1016-1022 method stop_orchestrator
  Similarity: 83.65%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:open_command_prompt (lines 402-408) ---[0m
    pub async fn open_command_prompt(&mut self) -> Result<()> {
        self.input_mode = InputMode::Command;
        self.input_buffer.clear();
        self.add_log("System", "Enter command (help for available commands):")
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:stop_orchestrator (lines 1016-1022) ---[0m
    async fn stop_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Stopping orchestrator...").await;
        // TODO: Implement actual orchestrator stop
        self.system_status = "Stopped".to_string();
        self.add_log("System", "Orchestrator stopped").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:20-28 method title <-> ./crates/ccswarm/src/tui/app.rs:666-670 method show_task_details
  Similarity: 82.94%
  Classes: Tab <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:title (lines 20-28) ---[0m
    pub fn title(&self) -> &str {
        match self {
            Tab::Overview => "Overview",
            Tab::Agents => "Agents",
            Tab::Tasks => "Tasks",
            Tab::Logs => "Logs",
            Tab::Delegation => "Delegation",
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_task_details (lines 666-670) ---[0m
    async fn show_task_details(&mut self, task_id: &str) -> Result<()> {
        self.add_log("System", &format!("Showing details for task: {}", task_id))
            .await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:656-663 method show_agent_details <-> ./crates/ccswarm/src/tui/app.rs:1041-1047 method switch_delegation_mode
  Similarity: 76.11%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_agent_details (lines 656-663) ---[0m
    async fn show_agent_details(&mut self, agent_id: &str) -> Result<()> {
        self.add_log(
            "System",
            &format!("Showing details for agent: {}", agent_id),
        )
        .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:switch_delegation_mode (lines 1041-1047) ---[0m
    pub fn switch_delegation_mode(&mut self) {
        self.delegation_mode = match self.delegation_mode {
            DelegationMode::Analyze => DelegationMode::Delegate,
            DelegationMode::Delegate => DelegationMode::ViewStats,
            DelegationMode::ViewStats => DelegationMode::Analyze,
        };
    }

  ./crates/ccswarm/src/tui/app.rs:411-417 method refresh_data <-> ./crates/ccswarm/src/tui/app.rs:1006-1013 method start_orchestrator
  Similarity: 75.68%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:refresh_data (lines 411-417) ---[0m
    pub async fn refresh_data(&mut self) -> Result<()> {
        self.load_agents().await?;
        self.load_tasks().await?;
        self.update_system_stats().await?;
        self.last_update = Instant::now();
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:start_orchestrator (lines 1006-1013) ---[0m
    async fn start_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Starting orchestrator...").await;
        // TODO: Implement actual orchestrator start
        self.system_status = "Running".to_string();
        self.add_log("System", "Orchestrator started successfully")
            .await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:368-374 method create_new_session <-> ./crates/ccswarm/src/tui/app.rs:1025-1030 method list_worktrees
  Similarity: 87.14%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:create_new_session (lines 368-374) ---[0m
    pub async fn create_new_session(&mut self) -> Result<()> {
        self.input_mode = InputMode::CreatingAgent;
        self.input_buffer.clear();
        self.add_log("System", "Enter agent type (frontend/backend/devops/qa):")
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_worktrees (lines 1025-1030) ---[0m
    async fn list_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Git worktrees:").await;
        // TODO: Implement actual worktree listing
        self.add_log("System", "  No worktrees found").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:368-374 method create_new_session <-> ./crates/ccswarm/src/tui/app.rs:1033-1038 method prune_worktrees
  Similarity: 87.14%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:create_new_session (lines 368-374) ---[0m
    pub async fn create_new_session(&mut self) -> Result<()> {
        self.input_mode = InputMode::CreatingAgent;
        self.input_buffer.clear();
        self.add_log("System", "Enter agent type (frontend/backend/devops/qa):")
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:prune_worktrees (lines 1033-1038) ---[0m
    async fn prune_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Pruning stale worktrees...").await;
        // TODO: Implement actual worktree pruning
        self.add_log("System", "Worktree pruning completed").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:402-408 method open_command_prompt <-> ./crates/ccswarm/src/tui/app.rs:1025-1030 method list_worktrees
  Similarity: 87.14%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:open_command_prompt (lines 402-408) ---[0m
    pub async fn open_command_prompt(&mut self) -> Result<()> {
        self.input_mode = InputMode::Command;
        self.input_buffer.clear();
        self.add_log("System", "Enter command (help for available commands):")
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_worktrees (lines 1025-1030) ---[0m
    async fn list_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Git worktrees:").await;
        // TODO: Implement actual worktree listing
        self.add_log("System", "  No worktrees found").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:402-408 method open_command_prompt <-> ./crates/ccswarm/src/tui/app.rs:1033-1038 method prune_worktrees
  Similarity: 87.14%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:open_command_prompt (lines 402-408) ---[0m
    pub async fn open_command_prompt(&mut self) -> Result<()> {
        self.input_mode = InputMode::Command;
        self.input_buffer.clear();
        self.add_log("System", "Enter command (help for available commands):")
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:prune_worktrees (lines 1033-1038) ---[0m
    async fn prune_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Pruning stale worktrees...").await;
        // TODO: Implement actual worktree pruning
        self.add_log("System", "Worktree pruning completed").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:437-442 method update <-> ./crates/ccswarm/src/tui/app.rs:1318-1327 function parse_agent_status
  Similarity: 70.32%

[36m--- ./crates/ccswarm/src/tui/app.rs:update (lines 437-442) ---[0m
    pub async fn update(&mut self) -> Result<()> {
        if self.last_update.elapsed() >= self.update_interval {
            self.refresh_data().await?;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:parse_agent_status (lines 1318-1327) ---[0m
fn parse_agent_status(status: &str) -> AgentStatus {
    match status {
        "Initializing" => AgentStatus::Initializing,
        "Available" => AgentStatus::Available,
        "Working" => AgentStatus::Working,
        "WaitingForReview" => AgentStatus::WaitingForReview,
        "ShuttingDown" => AgentStatus::ShuttingDown,
        _ => AgentStatus::Error(format!("Unknown status: {}", status)),
    }
}

  ./crates/ccswarm/src/tui/app.rs:394-399 method add_task_prompt <-> ./crates/ccswarm/src/tui/app.rs:1016-1022 method stop_orchestrator
  Similarity: 83.65%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:add_task_prompt (lines 394-399) ---[0m
    pub async fn add_task_prompt(&mut self) -> Result<()> {
        self.input_mode = InputMode::AddingTask;
        self.input_buffer.clear();
        self.add_log("System", "Enter task description:").await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:stop_orchestrator (lines 1016-1022) ---[0m
    async fn stop_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Stopping orchestrator...").await;
        // TODO: Implement actual orchestrator stop
        self.system_status = "Stopped".to_string();
        self.add_log("System", "Orchestrator stopped").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:377-384 method delete_current_session <-> ./crates/ccswarm/src/tui/app.rs:1025-1030 method list_worktrees
  Similarity: 76.94%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:delete_current_session (lines 377-384) ---[0m
    pub async fn delete_current_session(&mut self) -> Result<()> {
        if let Some(agent) = self.agents.get(self.selected_agent) {
            self.add_log("System", &format!("Deleting agent: {}", agent.name))
                .await;
            // TODO: Implement actual agent deletion
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_worktrees (lines 1025-1030) ---[0m
    async fn list_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Git worktrees:").await;
        // TODO: Implement actual worktree listing
        self.add_log("System", "  No worktrees found").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:377-384 method delete_current_session <-> ./crates/ccswarm/src/tui/app.rs:1033-1038 method prune_worktrees
  Similarity: 76.94%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:delete_current_session (lines 377-384) ---[0m
    pub async fn delete_current_session(&mut self) -> Result<()> {
        if let Some(agent) = self.agents.get(self.selected_agent) {
            self.add_log("System", &format!("Deleting agent: {}", agent.name))
                .await;
            // TODO: Implement actual agent deletion
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:prune_worktrees (lines 1033-1038) ---[0m
    async fn prune_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Pruning stale worktrees...").await;
        // TODO: Implement actual worktree pruning
        self.add_log("System", "Worktree pruning completed").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:411-417 method refresh_data <-> ./crates/ccswarm/src/tui/app.rs:1016-1022 method stop_orchestrator
  Similarity: 75.68%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:refresh_data (lines 411-417) ---[0m
    pub async fn refresh_data(&mut self) -> Result<()> {
        self.load_agents().await?;
        self.load_tasks().await?;
        self.update_system_stats().await?;
        self.last_update = Instant::now();
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:stop_orchestrator (lines 1016-1022) ---[0m
    async fn stop_orchestrator(&mut self) -> Result<()> {
        self.add_log("System", "Stopping orchestrator...").await;
        // TODO: Implement actual orchestrator stop
        self.system_status = "Stopped".to_string();
        self.add_log("System", "Orchestrator stopped").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:270-278 method reset_selection <-> ./crates/ccswarm/src/tui/app.rs:703-707 method handle_backspace
  Similarity: 75.38%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:reset_selection (lines 270-278) ---[0m
    fn reset_selection(&mut self) {
        match self.current_tab {
            Tab::Agents => self.selected_agent = 0,
            Tab::Tasks => self.selected_task = 0,
            Tab::Logs => self.selected_log = 0,
            Tab::Delegation => self.selected_delegation = 0,
            _ => {}
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_backspace (lines 703-707) ---[0m
    pub fn handle_backspace(&mut self) {
        if self.input_mode != InputMode::Normal && !self.input_buffer.is_empty() {
            self.input_buffer.pop();
        }
    }

  ./crates/ccswarm/src/tui/app.rs:394-399 method add_task_prompt <-> ./crates/ccswarm/src/tui/app.rs:1025-1030 method list_worktrees
  Similarity: 87.14%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:add_task_prompt (lines 394-399) ---[0m
    pub async fn add_task_prompt(&mut self) -> Result<()> {
        self.input_mode = InputMode::AddingTask;
        self.input_buffer.clear();
        self.add_log("System", "Enter task description:").await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_worktrees (lines 1025-1030) ---[0m
    async fn list_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Git worktrees:").await;
        // TODO: Implement actual worktree listing
        self.add_log("System", "  No worktrees found").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:394-399 method add_task_prompt <-> ./crates/ccswarm/src/tui/app.rs:1033-1038 method prune_worktrees
  Similarity: 87.14%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:add_task_prompt (lines 394-399) ---[0m
    pub async fn add_task_prompt(&mut self) -> Result<()> {
        self.input_mode = InputMode::AddingTask;
        self.input_buffer.clear();
        self.add_log("System", "Enter task description:").await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:prune_worktrees (lines 1033-1038) ---[0m
    async fn prune_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Pruning stale worktrees...").await;
        // TODO: Implement actual worktree pruning
        self.add_log("System", "Worktree pruning completed").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:20-28 method title <-> ./crates/ccswarm/src/tui/app.rs:703-707 method handle_backspace
  Similarity: 74.12%
  Classes: Tab <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:title (lines 20-28) ---[0m
    pub fn title(&self) -> &str {
        match self {
            Tab::Overview => "Overview",
            Tab::Agents => "Agents",
            Tab::Tasks => "Tasks",
            Tab::Logs => "Logs",
            Tab::Delegation => "Delegation",
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_backspace (lines 703-707) ---[0m
    pub fn handle_backspace(&mut self) {
        if self.input_mode != InputMode::Normal && !self.input_buffer.is_empty() {
            self.input_buffer.pop();
        }
    }

  ./crates/ccswarm/src/tui/app.rs:696-700 method handle_char_input <-> ./crates/ccswarm/src/tui/app.rs:1041-1047 method switch_delegation_mode
  Similarity: 86.11%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_char_input (lines 696-700) ---[0m
    pub fn handle_char_input(&mut self, c: char) {
        if self.input_mode != InputMode::Normal {
            self.input_buffer.push(c);
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:switch_delegation_mode (lines 1041-1047) ---[0m
    pub fn switch_delegation_mode(&mut self) {
        self.delegation_mode = match self.delegation_mode {
            DelegationMode::Analyze => DelegationMode::Delegate,
            DelegationMode::Delegate => DelegationMode::ViewStats,
            DelegationMode::ViewStats => DelegationMode::Analyze,
        };
    }

  ./crates/ccswarm/src/tui/app.rs:703-707 method handle_backspace <-> ./crates/ccswarm/src/tui/app.rs:1041-1047 method switch_delegation_mode
  Similarity: 86.11%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_backspace (lines 703-707) ---[0m
    pub fn handle_backspace(&mut self) {
        if self.input_mode != InputMode::Normal && !self.input_buffer.is_empty() {
            self.input_buffer.pop();
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:switch_delegation_mode (lines 1041-1047) ---[0m
    pub fn switch_delegation_mode(&mut self) {
        self.delegation_mode = match self.delegation_mode {
            DelegationMode::Analyze => DelegationMode::Delegate,
            DelegationMode::Delegate => DelegationMode::ViewStats,
            DelegationMode::ViewStats => DelegationMode::Analyze,
        };
    }

  ./crates/ccswarm/src/tui/app.rs:377-384 method delete_current_session <-> ./crates/ccswarm/src/tui/app.rs:387-391 method show_status
  Similarity: 78.47%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:delete_current_session (lines 377-384) ---[0m
    pub async fn delete_current_session(&mut self) -> Result<()> {
        if let Some(agent) = self.agents.get(self.selected_agent) {
            self.add_log("System", &format!("Deleting agent: {}", agent.name))
                .await;
            // TODO: Implement actual agent deletion
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_status (lines 387-391) ---[0m
    pub async fn show_status(&mut self) -> Result<()> {
        self.current_tab = Tab::Overview;
        self.refresh_data().await?;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:656-663 method show_agent_details <-> ./crates/ccswarm/src/tui/app.rs:1025-1030 method list_worktrees
  Similarity: 72.18%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_agent_details (lines 656-663) ---[0m
    async fn show_agent_details(&mut self, agent_id: &str) -> Result<()> {
        self.add_log(
            "System",
            &format!("Showing details for agent: {}", agent_id),
        )
        .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_worktrees (lines 1025-1030) ---[0m
    async fn list_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Git worktrees:").await;
        // TODO: Implement actual worktree listing
        self.add_log("System", "  No worktrees found").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:656-663 method show_agent_details <-> ./crates/ccswarm/src/tui/app.rs:1033-1038 method prune_worktrees
  Similarity: 72.18%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_agent_details (lines 656-663) ---[0m
    async fn show_agent_details(&mut self, agent_id: &str) -> Result<()> {
        self.add_log(
            "System",
            &format!("Showing details for agent: {}", agent_id),
        )
        .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:prune_worktrees (lines 1033-1038) ---[0m
    async fn prune_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Pruning stale worktrees...").await;
        // TODO: Implement actual worktree pruning
        self.add_log("System", "Worktree pruning completed").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:437-442 method update <-> ./crates/ccswarm/src/tui/app.rs:666-670 method show_task_details
  Similarity: 90.85%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:update (lines 437-442) ---[0m
    pub async fn update(&mut self) -> Result<()> {
        if self.last_update.elapsed() >= self.update_interval {
            self.refresh_data().await?;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_task_details (lines 666-670) ---[0m
    async fn show_task_details(&mut self, task_id: &str) -> Result<()> {
        self.add_log("System", &format!("Showing details for task: {}", task_id))
            .await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:656-663 method show_agent_details <-> ./crates/ccswarm/src/tui/app.rs:703-707 method handle_backspace
  Similarity: 72.67%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_agent_details (lines 656-663) ---[0m
    async fn show_agent_details(&mut self, agent_id: &str) -> Result<()> {
        self.add_log(
            "System",
            &format!("Showing details for agent: {}", agent_id),
        )
        .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_backspace (lines 703-707) ---[0m
    pub fn handle_backspace(&mut self) {
        if self.input_mode != InputMode::Normal && !self.input_buffer.is_empty() {
            self.input_buffer.pop();
        }
    }

  ./crates/ccswarm/src/tui/app.rs:377-384 method delete_current_session <-> ./crates/ccswarm/src/tui/app.rs:703-707 method handle_backspace
  Similarity: 71.94%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:delete_current_session (lines 377-384) ---[0m
    pub async fn delete_current_session(&mut self) -> Result<()> {
        if let Some(agent) = self.agents.get(self.selected_agent) {
            self.add_log("System", &format!("Deleting agent: {}", agent.name))
                .await;
            // TODO: Implement actual agent deletion
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_backspace (lines 703-707) ---[0m
    pub fn handle_backspace(&mut self) {
        if self.input_mode != InputMode::Normal && !self.input_buffer.is_empty() {
            self.input_buffer.pop();
        }
    }

  ./crates/ccswarm/src/tui/app.rs:437-442 method update <-> ./crates/ccswarm/src/tui/app.rs:1041-1047 method switch_delegation_mode
  Similarity: 71.30%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:update (lines 437-442) ---[0m
    pub async fn update(&mut self) -> Result<()> {
        if self.last_update.elapsed() >= self.update_interval {
            self.refresh_data().await?;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:switch_delegation_mode (lines 1041-1047) ---[0m
    pub fn switch_delegation_mode(&mut self) {
        self.delegation_mode = match self.delegation_mode {
            DelegationMode::Analyze => DelegationMode::Delegate,
            DelegationMode::Delegate => DelegationMode::ViewStats,
            DelegationMode::ViewStats => DelegationMode::Analyze,
        };
    }

  ./crates/ccswarm/src/tui/app.rs:656-663 method show_agent_details <-> ./crates/ccswarm/src/tui/app.rs:696-700 method handle_char_input
  Similarity: 70.71%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_agent_details (lines 656-663) ---[0m
    async fn show_agent_details(&mut self, agent_id: &str) -> Result<()> {
        self.add_log(
            "System",
            &format!("Showing details for agent: {}", agent_id),
        )
        .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_char_input (lines 696-700) ---[0m
    pub fn handle_char_input(&mut self, c: char) {
        if self.input_mode != InputMode::Normal {
            self.input_buffer.push(c);
        }
    }

  ./crates/ccswarm/src/tui/app.rs:666-670 method show_task_details <-> ./crates/ccswarm/src/tui/app.rs:1041-1047 method switch_delegation_mode
  Similarity: 76.11%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_task_details (lines 666-670) ---[0m
    async fn show_task_details(&mut self, task_id: &str) -> Result<()> {
        self.add_log("System", &format!("Showing details for task: {}", task_id))
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:switch_delegation_mode (lines 1041-1047) ---[0m
    pub fn switch_delegation_mode(&mut self) {
        self.delegation_mode = match self.delegation_mode {
            DelegationMode::Analyze => DelegationMode::Delegate,
            DelegationMode::Delegate => DelegationMode::ViewStats,
            DelegationMode::ViewStats => DelegationMode::Analyze,
        };
    }

  ./crates/ccswarm/src/tui/app.rs:696-700 method handle_char_input <-> ./crates/ccswarm/src/tui/app.rs:703-707 method handle_backspace
  Similarity: 84.89%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_char_input (lines 696-700) ---[0m
    pub fn handle_char_input(&mut self, c: char) {
        if self.input_mode != InputMode::Normal {
            self.input_buffer.push(c);
        }
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_backspace (lines 703-707) ---[0m
    pub fn handle_backspace(&mut self) {
        if self.input_mode != InputMode::Normal && !self.input_buffer.is_empty() {
            self.input_buffer.pop();
        }
    }

  ./crates/ccswarm/src/tui/app.rs:368-374 method create_new_session <-> ./crates/ccswarm/src/tui/app.rs:387-391 method show_status
  Similarity: 70.71%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:create_new_session (lines 368-374) ---[0m
    pub async fn create_new_session(&mut self) -> Result<()> {
        self.input_mode = InputMode::CreatingAgent;
        self.input_buffer.clear();
        self.add_log("System", "Enter agent type (frontend/backend/devops/qa):")
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:show_status (lines 387-391) ---[0m
    pub async fn show_status(&mut self) -> Result<()> {
        self.current_tab = Tab::Overview;
        self.refresh_data().await?;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:387-391 method show_status <-> ./crates/ccswarm/src/tui/app.rs:402-408 method open_command_prompt
  Similarity: 70.71%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_status (lines 387-391) ---[0m
    pub async fn show_status(&mut self) -> Result<()> {
        self.current_tab = Tab::Overview;
        self.refresh_data().await?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:open_command_prompt (lines 402-408) ---[0m
    pub async fn open_command_prompt(&mut self) -> Result<()> {
        self.input_mode = InputMode::Command;
        self.input_buffer.clear();
        self.add_log("System", "Enter command (help for available commands):")
            .await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:387-391 method show_status <-> ./crates/ccswarm/src/tui/app.rs:1025-1030 method list_worktrees
  Similarity: 74.36%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_status (lines 387-391) ---[0m
    pub async fn show_status(&mut self) -> Result<()> {
        self.current_tab = Tab::Overview;
        self.refresh_data().await?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_worktrees (lines 1025-1030) ---[0m
    async fn list_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Git worktrees:").await;
        // TODO: Implement actual worktree listing
        self.add_log("System", "  No worktrees found").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:387-391 method show_status <-> ./crates/ccswarm/src/tui/app.rs:1033-1038 method prune_worktrees
  Similarity: 74.36%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_status (lines 387-391) ---[0m
    pub async fn show_status(&mut self) -> Result<()> {
        self.current_tab = Tab::Overview;
        self.refresh_data().await?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:prune_worktrees (lines 1033-1038) ---[0m
    async fn prune_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Pruning stale worktrees...").await;
        // TODO: Implement actual worktree pruning
        self.add_log("System", "Worktree pruning completed").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:666-670 method show_task_details <-> ./crates/ccswarm/src/tui/app.rs:1025-1030 method list_worktrees
  Similarity: 72.91%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_task_details (lines 666-670) ---[0m
    async fn show_task_details(&mut self, task_id: &str) -> Result<()> {
        self.add_log("System", &format!("Showing details for task: {}", task_id))
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:list_worktrees (lines 1025-1030) ---[0m
    async fn list_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Git worktrees:").await;
        // TODO: Implement actual worktree listing
        self.add_log("System", "  No worktrees found").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:666-670 method show_task_details <-> ./crates/ccswarm/src/tui/app.rs:1033-1038 method prune_worktrees
  Similarity: 72.91%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_task_details (lines 666-670) ---[0m
    async fn show_task_details(&mut self, task_id: &str) -> Result<()> {
        self.add_log("System", &format!("Showing details for task: {}", task_id))
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:prune_worktrees (lines 1033-1038) ---[0m
    async fn prune_worktrees(&mut self) -> Result<()> {
        self.add_log("System", "Pruning stale worktrees...").await;
        // TODO: Implement actual worktree pruning
        self.add_log("System", "Worktree pruning completed").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:387-391 method show_status <-> ./crates/ccswarm/src/tui/app.rs:394-399 method add_task_prompt
  Similarity: 70.71%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_status (lines 387-391) ---[0m
    pub async fn show_status(&mut self) -> Result<()> {
        self.current_tab = Tab::Overview;
        self.refresh_data().await?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:add_task_prompt (lines 394-399) ---[0m
    pub async fn add_task_prompt(&mut self) -> Result<()> {
        self.input_mode = InputMode::AddingTask;
        self.input_buffer.clear();
        self.add_log("System", "Enter task description:").await;
        Ok(())
    }

  ./crates/ccswarm/src/tui/app.rs:666-670 method show_task_details <-> ./crates/ccswarm/src/tui/app.rs:703-707 method handle_backspace
  Similarity: 72.67%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_task_details (lines 666-670) ---[0m
    async fn show_task_details(&mut self, task_id: &str) -> Result<()> {
        self.add_log("System", &format!("Showing details for task: {}", task_id))
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_backspace (lines 703-707) ---[0m
    pub fn handle_backspace(&mut self) {
        if self.input_mode != InputMode::Normal && !self.input_buffer.is_empty() {
            self.input_buffer.pop();
        }
    }

  ./crates/ccswarm/src/tui/app.rs:666-670 method show_task_details <-> ./crates/ccswarm/src/tui/app.rs:696-700 method handle_char_input
  Similarity: 70.00%
  Classes: App <-> App

[36m--- ./crates/ccswarm/src/tui/app.rs:show_task_details (lines 666-670) ---[0m
    async fn show_task_details(&mut self, task_id: &str) -> Result<()> {
        self.add_log("System", &format!("Showing details for task: {}", task_id))
            .await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/tui/app.rs:handle_char_input (lines 696-700) ---[0m
    pub fn handle_char_input(&mut self, c: char) {
        if self.input_mode != InputMode::Normal {
            self.input_buffer.push(c);
        }
    }


Duplicates in ./crates/ccswarm/src/identity/boundary.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/identity/boundary.rs:416-433 function test_frontend_delegates_backend_task <-> ./crates/ccswarm/src/identity/boundary.rs:436-453 function test_unclear_task_triggers_clarification
  Similarity: 95.86%

[36m--- ./crates/ccswarm/src/identity/boundary.rs:test_frontend_delegates_backend_task (lines 416-433) ---[0m
    async fn test_frontend_delegates_backend_task() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());
        let task = Task::new(
            "2".to_string(),
            "Create REST API endpoint for authentication".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = checker.evaluate_task(&task).await;

        match result {
            TaskEvaluation::Delegate { target_agent, .. } => {
                assert_eq!(target_agent, "backend-agent");
            }
            _ => panic!("Frontend should delegate backend tasks"),
        }
    }

[36m--- ./crates/ccswarm/src/identity/boundary.rs:test_unclear_task_triggers_clarification (lines 436-453) ---[0m
    async fn test_unclear_task_triggers_clarification() {
        let checker = TaskBoundaryChecker::new(default_backend_role());
        let task = Task::new(
            "3".to_string(),
            "Update the user system".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let result = checker.evaluate_task(&task).await;

        match result {
            TaskEvaluation::Clarify { questions, .. } => {
                assert!(!questions.is_empty());
            }
            _ => panic!("Unclear tasks should trigger clarification"),
        }
    }

  ./crates/ccswarm/src/identity/boundary.rs:52-75 method evaluate_task <-> ./crates/ccswarm/src/identity/boundary.rs:416-433 function test_frontend_delegates_backend_task
  Similarity: 77.65%

[36m--- ./crates/ccswarm/src/identity/boundary.rs:evaluate_task (lines 52-75) ---[0m
    pub async fn evaluate_task(&self, task: &Task) -> TaskEvaluation {
        // Check if explicitly forbidden first to ensure proper delegation
        if self.is_explicitly_forbidden(task) {
            let target_agent = self.determine_correct_agent(task);
            return TaskEvaluation::Delegate {
                reason: "Task is outside my specialization".to_string(),
                target_agent: target_agent.clone(),
                suggestion: self.generate_delegation_message(task, &target_agent),
            };
        }

        // Check if explicitly allowed
        if self.is_explicitly_allowed(task) {
            return TaskEvaluation::Accept {
                reason: "Task is within my specialization".to_string(),
            };
        }

        // Ambiguous case - request clarification
        TaskEvaluation::Clarify {
            reason: "Task scope is unclear".to_string(),
            questions: self.generate_clarification_questions(task),
        }
    }

[36m--- ./crates/ccswarm/src/identity/boundary.rs:test_frontend_delegates_backend_task (lines 416-433) ---[0m
    async fn test_frontend_delegates_backend_task() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());
        let task = Task::new(
            "2".to_string(),
            "Create REST API endpoint for authentication".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = checker.evaluate_task(&task).await;

        match result {
            TaskEvaluation::Delegate { target_agent, .. } => {
                assert_eq!(target_agent, "backend-agent");
            }
            _ => panic!("Frontend should delegate backend tasks"),
        }
    }

  ./crates/ccswarm/src/identity/boundary.rs:52-75 method evaluate_task <-> ./crates/ccswarm/src/identity/boundary.rs:436-453 function test_unclear_task_triggers_clarification
  Similarity: 77.65%

[36m--- ./crates/ccswarm/src/identity/boundary.rs:evaluate_task (lines 52-75) ---[0m
    pub async fn evaluate_task(&self, task: &Task) -> TaskEvaluation {
        // Check if explicitly forbidden first to ensure proper delegation
        if self.is_explicitly_forbidden(task) {
            let target_agent = self.determine_correct_agent(task);
            return TaskEvaluation::Delegate {
                reason: "Task is outside my specialization".to_string(),
                target_agent: target_agent.clone(),
                suggestion: self.generate_delegation_message(task, &target_agent),
            };
        }

        // Check if explicitly allowed
        if self.is_explicitly_allowed(task) {
            return TaskEvaluation::Accept {
                reason: "Task is within my specialization".to_string(),
            };
        }

        // Ambiguous case - request clarification
        TaskEvaluation::Clarify {
            reason: "Task scope is unclear".to_string(),
            questions: self.generate_clarification_questions(task),
        }
    }

[36m--- ./crates/ccswarm/src/identity/boundary.rs:test_unclear_task_triggers_clarification (lines 436-453) ---[0m
    async fn test_unclear_task_triggers_clarification() {
        let checker = TaskBoundaryChecker::new(default_backend_role());
        let task = Task::new(
            "3".to_string(),
            "Update the user system".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let result = checker.evaluate_task(&task).await;

        match result {
            TaskEvaluation::Clarify { questions, .. } => {
                assert!(!questions.is_empty());
            }
            _ => panic!("Unclear tasks should trigger clarification"),
        }
    }

  ./crates/ccswarm/src/identity/boundary.rs:397-413 function test_frontend_accepts_ui_task <-> ./crates/ccswarm/src/identity/boundary.rs:416-433 function test_frontend_delegates_backend_task
  Similarity: 93.14%

[36m--- ./crates/ccswarm/src/identity/boundary.rs:test_frontend_accepts_ui_task (lines 397-413) ---[0m
    async fn test_frontend_accepts_ui_task() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());
        let task = Task::new(
            "1".to_string(),
            "Create a React component for user profile".to_string(),
            Priority::Medium,
            TaskType::Development,
        )
        .with_details("Using TypeScript and Tailwind CSS".to_string());

        let result = checker.evaluate_task(&task).await;

        match result {
            TaskEvaluation::Accept { .. } => {}
            _ => panic!("Frontend should accept UI tasks"),
        }
    }

[36m--- ./crates/ccswarm/src/identity/boundary.rs:test_frontend_delegates_backend_task (lines 416-433) ---[0m
    async fn test_frontend_delegates_backend_task() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());
        let task = Task::new(
            "2".to_string(),
            "Create REST API endpoint for authentication".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let result = checker.evaluate_task(&task).await;

        match result {
            TaskEvaluation::Delegate { target_agent, .. } => {
                assert_eq!(target_agent, "backend-agent");
            }
            _ => panic!("Frontend should delegate backend tasks"),
        }
    }

  ./crates/ccswarm/src/identity/boundary.rs:397-413 function test_frontend_accepts_ui_task <-> ./crates/ccswarm/src/identity/boundary.rs:436-453 function test_unclear_task_triggers_clarification
  Similarity: 92.93%

[36m--- ./crates/ccswarm/src/identity/boundary.rs:test_frontend_accepts_ui_task (lines 397-413) ---[0m
    async fn test_frontend_accepts_ui_task() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());
        let task = Task::new(
            "1".to_string(),
            "Create a React component for user profile".to_string(),
            Priority::Medium,
            TaskType::Development,
        )
        .with_details("Using TypeScript and Tailwind CSS".to_string());

        let result = checker.evaluate_task(&task).await;

        match result {
            TaskEvaluation::Accept { .. } => {}
            _ => panic!("Frontend should accept UI tasks"),
        }
    }

[36m--- ./crates/ccswarm/src/identity/boundary.rs:test_unclear_task_triggers_clarification (lines 436-453) ---[0m
    async fn test_unclear_task_triggers_clarification() {
        let checker = TaskBoundaryChecker::new(default_backend_role());
        let task = Task::new(
            "3".to_string(),
            "Update the user system".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let result = checker.evaluate_task(&task).await;

        match result {
            TaskEvaluation::Clarify { questions, .. } => {
                assert!(!questions.is_empty());
            }
            _ => panic!("Unclear tasks should trigger clarification"),
        }
    }

  ./crates/ccswarm/src/identity/boundary.rs:52-75 method evaluate_task <-> ./crates/ccswarm/src/identity/boundary.rs:397-413 function test_frontend_accepts_ui_task
  Similarity: 77.65%

[36m--- ./crates/ccswarm/src/identity/boundary.rs:evaluate_task (lines 52-75) ---[0m
    pub async fn evaluate_task(&self, task: &Task) -> TaskEvaluation {
        // Check if explicitly forbidden first to ensure proper delegation
        if self.is_explicitly_forbidden(task) {
            let target_agent = self.determine_correct_agent(task);
            return TaskEvaluation::Delegate {
                reason: "Task is outside my specialization".to_string(),
                target_agent: target_agent.clone(),
                suggestion: self.generate_delegation_message(task, &target_agent),
            };
        }

        // Check if explicitly allowed
        if self.is_explicitly_allowed(task) {
            return TaskEvaluation::Accept {
                reason: "Task is within my specialization".to_string(),
            };
        }

        // Ambiguous case - request clarification
        TaskEvaluation::Clarify {
            reason: "Task scope is unclear".to_string(),
            questions: self.generate_clarification_questions(task),
        }
    }

[36m--- ./crates/ccswarm/src/identity/boundary.rs:test_frontend_accepts_ui_task (lines 397-413) ---[0m
    async fn test_frontend_accepts_ui_task() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());
        let task = Task::new(
            "1".to_string(),
            "Create a React component for user profile".to_string(),
            Priority::Medium,
            TaskType::Development,
        )
        .with_details("Using TypeScript and Tailwind CSS".to_string());

        let result = checker.evaluate_task(&task).await;

        match result {
            TaskEvaluation::Accept { .. } => {}
            _ => panic!("Frontend should accept UI tasks"),
        }
    }

  ./crates/ccswarm/src/identity/boundary.rs:52-75 method evaluate_task <-> ./crates/ccswarm/src/identity/boundary.rs:380-387 method create_delegation_map
  Similarity: 81.35%
  Classes: TaskBoundaryChecker <-> TaskBoundaryChecker

[36m--- ./crates/ccswarm/src/identity/boundary.rs:evaluate_task (lines 52-75) ---[0m
    pub async fn evaluate_task(&self, task: &Task) -> TaskEvaluation {
        // Check if explicitly forbidden first to ensure proper delegation
        if self.is_explicitly_forbidden(task) {
            let target_agent = self.determine_correct_agent(task);
            return TaskEvaluation::Delegate {
                reason: "Task is outside my specialization".to_string(),
                target_agent: target_agent.clone(),
                suggestion: self.generate_delegation_message(task, &target_agent),
            };
        }

        // Check if explicitly allowed
        if self.is_explicitly_allowed(task) {
            return TaskEvaluation::Accept {
                reason: "Task is within my specialization".to_string(),
            };
        }

        // Ambiguous case - request clarification
        TaskEvaluation::Clarify {
            reason: "Task scope is unclear".to_string(),
            questions: self.generate_clarification_questions(task),
        }
    }

[36m--- ./crates/ccswarm/src/identity/boundary.rs:create_delegation_map (lines 380-387) ---[0m
    fn create_delegation_map() -> HashMap<String, String> {
        let mut map = HashMap::new();
        map.insert("frontend".to_string(), "frontend-agent".to_string());
        map.insert("backend".to_string(), "backend-agent".to_string());
        map.insert("devops".to_string(), "devops-agent".to_string());
        map.insert("qa".to_string(), "qa-agent".to_string());
        map
    }

  ./crates/ccswarm/src/identity/boundary.rs:78-88 method is_explicitly_allowed <-> ./crates/ccswarm/src/identity/boundary.rs:91-101 method is_explicitly_forbidden
  Similarity: 99.58%
  Classes: TaskBoundaryChecker <-> TaskBoundaryChecker

[36m--- ./crates/ccswarm/src/identity/boundary.rs:is_explicitly_allowed (lines 78-88) ---[0m
    fn is_explicitly_allowed(&self, task: &Task) -> bool {
        let task_text = format!(
            "{} {}",
            task.description,
            task.details.as_deref().unwrap_or("")
        );

        self.allowed_patterns
            .iter()
            .any(|pattern| pattern.is_match(&task_text))
    }

[36m--- ./crates/ccswarm/src/identity/boundary.rs:is_explicitly_forbidden (lines 91-101) ---[0m
    fn is_explicitly_forbidden(&self, task: &Task) -> bool {
        let task_text = format!(
            "{} {}",
            task.description,
            task.details.as_deref().unwrap_or("")
        );

        self.forbidden_patterns
            .iter()
            .any(|pattern| pattern.is_match(&task_text))
    }

  ./crates/ccswarm/src/identity/boundary.rs:215-225 method generate_clarification_questions <-> ./crates/ccswarm/src/identity/boundary.rs:372-377 method compile_patterns
  Similarity: 91.85%
  Classes: TaskBoundaryChecker <-> TaskBoundaryChecker

[36m--- ./crates/ccswarm/src/identity/boundary.rs:generate_clarification_questions (lines 215-225) ---[0m
    fn generate_clarification_questions(&self, _task: &Task) -> Vec<String> {
        vec![
            format!("Is this task specifically related to {}?", self.role.name()),
            "What components or systems will this task modify?".to_string(),
            "Are there any API, database, or infrastructure changes involved?".to_string(),
            format!(
                "Should this be handled by a {} specialist?",
                self.role.name()
            ),
        ]
    }

[36m--- ./crates/ccswarm/src/identity/boundary.rs:compile_patterns (lines 372-377) ---[0m
    fn compile_patterns(patterns: Vec<&str>) -> Vec<Regex> {
        patterns
            .into_iter()
            .filter_map(|p| Regex::new(p).ok())
            .collect()
    }

  ./crates/ccswarm/src/identity/boundary.rs:178-184 method check_patterns <-> ./crates/ccswarm/src/identity/boundary.rs:372-377 method compile_patterns
  Similarity: 91.11%
  Classes: TaskBoundaryChecker <-> TaskBoundaryChecker

[36m--- ./crates/ccswarm/src/identity/boundary.rs:check_patterns (lines 178-184) ---[0m
    fn check_patterns(&self, text: &str, patterns: &[&str]) -> bool {
        patterns.iter().any(|pattern| {
            Regex::new(pattern)
                .map(|re| re.is_match(text))
                .unwrap_or(false)
        })
    }

[36m--- ./crates/ccswarm/src/identity/boundary.rs:compile_patterns (lines 372-377) ---[0m
    fn compile_patterns(patterns: Vec<&str>) -> Vec<Regex> {
        patterns
            .into_iter()
            .filter_map(|p| Regex::new(p).ok())
            .collect()
    }


Duplicates in ./crates/ccswarm/src/cli/health.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/cli/health.rs:209-261 method check_all <-> ./crates/ccswarm/src/cli/health.rs:534-620 function run_diagnostics
  Similarity: 90.29%

[36m--- ./crates/ccswarm/src/cli/health.rs:check_all (lines 209-261) ---[0m
    pub async fn check_all(&self) -> Result<SystemHealthReport> {
        let mut checks = Vec::new();
        let _start_time = Instant::now();

        // Check orchestrator status
        checks.push(self.check_orchestrator().await);

        // Check all agents
        let agent_checks = self.check_agents().await?;
        let healthy_agents = agent_checks
            .iter()
            .filter(|c| c.status == HealthStatus::Healthy)
            .count();
        checks.extend(agent_checks);

        // Check AI sessions
        let session_checks = self.check_sessions().await?;
        let session_count = session_checks.len();
        checks.extend(session_checks);

        // Check monitoring system
        if self.monitoring_system.is_some() {
            checks.push(self.check_monitoring_system().await);
        }

        // Check coordination bus
        checks.push(self.check_coordination_bus().await);

        // Determine overall status
        let overall_status = if checks
            .iter()
            .any(|c| matches!(c.status, HealthStatus::Down | HealthStatus::Critical))
        {
            HealthStatus::Critical
        } else if checks.iter().any(|c| c.status == HealthStatus::Warning) {
            HealthStatus::Warning
        } else {
            HealthStatus::Healthy
        };

        // Get active task count
        let active_tasks = self.get_active_task_count().await;

        Ok(SystemHealthReport {
            timestamp: chrono::Utc::now(),
            overall_status,
            checks,
            total_agents: self.get_total_agent_count().await,
            healthy_agents,
            active_tasks,
            session_count,
        })
    }

[36m--- ./crates/ccswarm/src/cli/health.rs:run_diagnostics (lines 534-620) ---[0m
pub async fn run_diagnostics(repo_path: &std::path::Path) -> Result<()> {
    println!("{}", "Running ccswarm diagnostics...".bold().cyan());

    // Check basic requirements
    println!("\n{}", "System Requirements:".underline());

    // Check Rust version
    let rust_version = std::process::Command::new("rustc")
        .arg("--version")
        .output()
        .map(|o| String::from_utf8_lossy(&o.stdout).trim().to_string())
        .unwrap_or_else(|_| "Not found".to_string());
    println!("  Rust: {}", rust_version);

    // Check Git version
    let git_version = std::process::Command::new("git")
        .arg("--version")
        .output()
        .map(|o| String::from_utf8_lossy(&o.stdout).trim().to_string())
        .unwrap_or_else(|_| "Not found".to_string());
    println!("  Git: {}", git_version);

    // Check environment variables
    println!("\n{}", "Environment Variables:".underline());
    let env_vars = vec![
        (
            "ANTHROPIC_API_KEY",
            std::env::var("ANTHROPIC_API_KEY").is_ok(),
        ),
        ("OPENAI_API_KEY", std::env::var("OPENAI_API_KEY").is_ok()),
        ("RUST_LOG", std::env::var("RUST_LOG").is_ok()),
    ];

    for (var, exists) in env_vars {
        println!(
            "  {}: {}",
            var,
            if exists {
                "Set ✓".green().to_string()
            } else {
                "Not set".yellow().to_string()
            }
        );
    }

    // Check ccswarm directories
    println!("\n{}", "ccswarm Directories:".underline());
    let dirs = vec![
        ".ccswarm",
        ".ccswarm/coordination",
        ".ccswarm/sessions",
        ".ccswarm/agents",
        ".ccswarm/worktrees",
    ];

    for dir in dirs {
        let path = repo_path.join(dir);
        println!(
            "  {}: {}",
            dir,
            if path.exists() {
                "Exists ✓".green().to_string()
            } else {
                "Missing".red().to_string()
            }
        );
    }

    // Check for running processes
    println!("\n{}", "Process Check:".underline());

    // Try to connect to orchestrator
    let orchestrator_running = tokio::fs::read_to_string(".ccswarm/coordination/status.json")
        .await
        .is_ok();

    println!(
        "  Orchestrator: {}",
        if orchestrator_running {
            "Running ✓".green().to_string()
        } else {
            "Not running".yellow().to_string()
        }
    );

    Ok(())
}

  ./crates/ccswarm/src/cli/health.rs:275-329 method check_orchestrator <-> ./crates/ccswarm/src/cli/health.rs:331-389 method check_agents
  Similarity: 95.60%
  Classes: HealthChecker <-> HealthChecker

[36m--- ./crates/ccswarm/src/cli/health.rs:check_orchestrator (lines 275-329) ---[0m
    async fn check_orchestrator(&self) -> HealthCheckResult {
        let start = Instant::now();

        // Check if orchestrator is running by checking coordination files
        match tokio::fs::read_to_string(".ccswarm/coordination/status.json").await {
            Ok(content) => {
                if let Ok(status) = serde_json::from_str::<serde_json::Value>(&content) {
                    let last_update = status
                        .get("last_update")
                        .and_then(|v| v.as_str())
                        .and_then(|s| chrono::DateTime::parse_from_rfc3339(s).ok());

                    let status = if let Some(last_update) = last_update {
                        let age = chrono::Utc::now() - last_update.with_timezone(&chrono::Utc);
                        if age.num_seconds() < 60 {
                            HealthStatus::Healthy
                        } else if age.num_seconds() < 300 {
                            HealthStatus::Warning
                        } else {
                            HealthStatus::Critical
                        }
                    } else {
                        HealthStatus::Warning
                    };

                    HealthCheckResult {
                        component: "orchestrator".to_string(),
                        status,
                        message: match status {
                            HealthStatus::Healthy => "Orchestrator is running normally".to_string(),
                            HealthStatus::Warning => "Orchestrator status is stale".to_string(),
                            _ => "Orchestrator may be unresponsive".to_string(),
                        },
                        details: Some(serde_json::json!({"status": status.as_colored_text()})),
                        response_time_ms: start.elapsed().as_millis() as u64,
                    }
                } else {
                    HealthCheckResult {
                        component: "orchestrator".to_string(),
                        status: HealthStatus::Down,
                        message: "Invalid status file".to_string(),
                        details: None,
                        response_time_ms: start.elapsed().as_millis() as u64,
                    }
                }
            }
            Err(_) => HealthCheckResult {
                component: "orchestrator".to_string(),
                status: HealthStatus::Down,
                message: "Orchestrator not running".to_string(),
                details: None,
                response_time_ms: start.elapsed().as_millis() as u64,
            },
        }
    }

[36m--- ./crates/ccswarm/src/cli/health.rs:check_agents (lines 331-389) ---[0m
    async fn check_agents(&self) -> Result<Vec<HealthCheckResult>> {
        let mut results = Vec::new();

        // Get all agent statuses
        let statuses = self.status_tracker.get_all_statuses().await?;

        for status in statuses.into_iter() {
            let agent_name = status
                .get("agent_id")
                .and_then(|v| v.as_str())
                .unwrap_or("unknown")
                .to_string();
            let start = Instant::now();

            // Check agent responsiveness
            let timestamp_str = status
                .get("timestamp")
                .and_then(|v| v.as_str())
                .unwrap_or("");

            let last_update = chrono::DateTime::parse_from_rfc3339(timestamp_str)
                .map(|dt| dt.with_timezone(&chrono::Utc))
                .unwrap_or_else(|_| chrono::Utc::now());

            let age = chrono::Utc::now() - last_update;
            let health_status = if age.num_seconds() < 30 {
                HealthStatus::Healthy
            } else if age.num_seconds() < 120 {
                HealthStatus::Warning
            } else {
                HealthStatus::Critical
            };

            let mut details = serde_json::json!({
                "status": status.get("status").unwrap_or(&serde_json::Value::Null),
                "last_update": timestamp_str,
                "age_seconds": age.num_seconds(),
            });

            // Add agent-specific details
            if let Some(agent_details) = status.get("additional_info") {
                details["agent_details"] = agent_details.clone();
            }

            results.push(HealthCheckResult {
                component: format!("agent:{}", agent_name),
                status: health_status,
                message: match health_status {
                    HealthStatus::Healthy => format!("{} agent is responsive", agent_name),
                    HealthStatus::Warning => format!("{} agent response is delayed", agent_name),
                    _ => format!("{} agent is unresponsive", agent_name),
                },
                details: Some(details),
                response_time_ms: start.elapsed().as_millis() as u64,
            });
        }

        Ok(results)
    }

  ./crates/ccswarm/src/cli/health.rs:331-389 method check_agents <-> ./crates/ccswarm/src/cli/health.rs:391-440 method check_sessions
  Similarity: 95.10%
  Classes: HealthChecker <-> HealthChecker

[36m--- ./crates/ccswarm/src/cli/health.rs:check_agents (lines 331-389) ---[0m
    async fn check_agents(&self) -> Result<Vec<HealthCheckResult>> {
        let mut results = Vec::new();

        // Get all agent statuses
        let statuses = self.status_tracker.get_all_statuses().await?;

        for status in statuses.into_iter() {
            let agent_name = status
                .get("agent_id")
                .and_then(|v| v.as_str())
                .unwrap_or("unknown")
                .to_string();
            let start = Instant::now();

            // Check agent responsiveness
            let timestamp_str = status
                .get("timestamp")
                .and_then(|v| v.as_str())
                .unwrap_or("");

            let last_update = chrono::DateTime::parse_from_rfc3339(timestamp_str)
                .map(|dt| dt.with_timezone(&chrono::Utc))
                .unwrap_or_else(|_| chrono::Utc::now());

            let age = chrono::Utc::now() - last_update;
            let health_status = if age.num_seconds() < 30 {
                HealthStatus::Healthy
            } else if age.num_seconds() < 120 {
                HealthStatus::Warning
            } else {
                HealthStatus::Critical
            };

            let mut details = serde_json::json!({
                "status": status.get("status").unwrap_or(&serde_json::Value::Null),
                "last_update": timestamp_str,
                "age_seconds": age.num_seconds(),
            });

            // Add agent-specific details
            if let Some(agent_details) = status.get("additional_info") {
                details["agent_details"] = agent_details.clone();
            }

            results.push(HealthCheckResult {
                component: format!("agent:{}", agent_name),
                status: health_status,
                message: match health_status {
                    HealthStatus::Healthy => format!("{} agent is responsive", agent_name),
                    HealthStatus::Warning => format!("{} agent response is delayed", agent_name),
                    _ => format!("{} agent is unresponsive", agent_name),
                },
                details: Some(details),
                response_time_ms: start.elapsed().as_millis() as u64,
            });
        }

        Ok(results)
    }

[36m--- ./crates/ccswarm/src/cli/health.rs:check_sessions (lines 391-440) ---[0m
    async fn check_sessions(&self) -> Result<Vec<HealthCheckResult>> {
        let mut results = Vec::new();

        // Check for active sessions in .ccswarm/sessions directory
        let sessions_dir = std::path::Path::new(".ccswarm/sessions");
        if sessions_dir.exists() {
            let mut entries = tokio::fs::read_dir(sessions_dir).await?;

            while let Some(entry) = entries.next_entry().await? {
                if entry.file_type().await?.is_dir() {
                    let session_name = entry.file_name().to_string_lossy().to_string();
                    let start = Instant::now();

                    // Check session state file
                    let state_file = entry.path().join("state.json");
                    let health_status = if state_file.exists() {
                        match tokio::fs::read_to_string(&state_file).await {
                            Ok(content) => {
                                if serde_json::from_str::<serde_json::Value>(&content).is_ok() {
                                    HealthStatus::Healthy
                                } else {
                                    HealthStatus::Warning
                                }
                            }
                            Err(_) => HealthStatus::Warning,
                        }
                    } else {
                        HealthStatus::Critical
                    };

                    results.push(HealthCheckResult {
                        component: format!("session:{}", session_name),
                        status: health_status,
                        message: match health_status {
                            HealthStatus::Healthy => "Session is active and healthy".to_string(),
                            HealthStatus::Warning => "Session state is unclear".to_string(),
                            _ => "Session may be corrupted".to_string(),
                        },
                        details: Some(serde_json::json!({
                            "session_name": session_name,
                            "state_file_exists": state_file.exists(),
                        })),
                        response_time_ms: start.elapsed().as_millis() as u64,
                    });
                }
            }
        }

        Ok(results)
    }

  ./crates/ccswarm/src/cli/health.rs:275-329 method check_orchestrator <-> ./crates/ccswarm/src/cli/health.rs:391-440 method check_sessions
  Similarity: 95.57%
  Classes: HealthChecker <-> HealthChecker

[36m--- ./crates/ccswarm/src/cli/health.rs:check_orchestrator (lines 275-329) ---[0m
    async fn check_orchestrator(&self) -> HealthCheckResult {
        let start = Instant::now();

        // Check if orchestrator is running by checking coordination files
        match tokio::fs::read_to_string(".ccswarm/coordination/status.json").await {
            Ok(content) => {
                if let Ok(status) = serde_json::from_str::<serde_json::Value>(&content) {
                    let last_update = status
                        .get("last_update")
                        .and_then(|v| v.as_str())
                        .and_then(|s| chrono::DateTime::parse_from_rfc3339(s).ok());

                    let status = if let Some(last_update) = last_update {
                        let age = chrono::Utc::now() - last_update.with_timezone(&chrono::Utc);
                        if age.num_seconds() < 60 {
                            HealthStatus::Healthy
                        } else if age.num_seconds() < 300 {
                            HealthStatus::Warning
                        } else {
                            HealthStatus::Critical
                        }
                    } else {
                        HealthStatus::Warning
                    };

                    HealthCheckResult {
                        component: "orchestrator".to_string(),
                        status,
                        message: match status {
                            HealthStatus::Healthy => "Orchestrator is running normally".to_string(),
                            HealthStatus::Warning => "Orchestrator status is stale".to_string(),
                            _ => "Orchestrator may be unresponsive".to_string(),
                        },
                        details: Some(serde_json::json!({"status": status.as_colored_text()})),
                        response_time_ms: start.elapsed().as_millis() as u64,
                    }
                } else {
                    HealthCheckResult {
                        component: "orchestrator".to_string(),
                        status: HealthStatus::Down,
                        message: "Invalid status file".to_string(),
                        details: None,
                        response_time_ms: start.elapsed().as_millis() as u64,
                    }
                }
            }
            Err(_) => HealthCheckResult {
                component: "orchestrator".to_string(),
                status: HealthStatus::Down,
                message: "Orchestrator not running".to_string(),
                details: None,
                response_time_ms: start.elapsed().as_millis() as u64,
            },
        }
    }

[36m--- ./crates/ccswarm/src/cli/health.rs:check_sessions (lines 391-440) ---[0m
    async fn check_sessions(&self) -> Result<Vec<HealthCheckResult>> {
        let mut results = Vec::new();

        // Check for active sessions in .ccswarm/sessions directory
        let sessions_dir = std::path::Path::new(".ccswarm/sessions");
        if sessions_dir.exists() {
            let mut entries = tokio::fs::read_dir(sessions_dir).await?;

            while let Some(entry) = entries.next_entry().await? {
                if entry.file_type().await?.is_dir() {
                    let session_name = entry.file_name().to_string_lossy().to_string();
                    let start = Instant::now();

                    // Check session state file
                    let state_file = entry.path().join("state.json");
                    let health_status = if state_file.exists() {
                        match tokio::fs::read_to_string(&state_file).await {
                            Ok(content) => {
                                if serde_json::from_str::<serde_json::Value>(&content).is_ok() {
                                    HealthStatus::Healthy
                                } else {
                                    HealthStatus::Warning
                                }
                            }
                            Err(_) => HealthStatus::Warning,
                        }
                    } else {
                        HealthStatus::Critical
                    };

                    results.push(HealthCheckResult {
                        component: format!("session:{}", session_name),
                        status: health_status,
                        message: match health_status {
                            HealthStatus::Healthy => "Session is active and healthy".to_string(),
                            HealthStatus::Warning => "Session state is unclear".to_string(),
                            _ => "Session may be corrupted".to_string(),
                        },
                        details: Some(serde_json::json!({
                            "session_name": session_name,
                            "state_file_exists": state_file.exists(),
                        })),
                        response_time_ms: start.elapsed().as_millis() as u64,
                    });
                }
            }
        }

        Ok(results)
    }

  ./crates/ccswarm/src/cli/health.rs:122-191 method print_detailed <-> ./crates/ccswarm/src/cli/health.rs:391-440 method check_sessions
  Similarity: 81.71%
  Classes: SystemHealthReport <-> HealthChecker

[36m--- ./crates/ccswarm/src/cli/health.rs:print_detailed (lines 122-191) ---[0m
    pub fn print_detailed(&self) {
        self.print_summary();

        println!("\n{}", "Detailed Component Information:".bold());

        // Group checks by component type
        let agent_checks: Vec<_> = self
            .checks
            .iter()
            .filter(|c| c.component.starts_with("agent:"))
            .collect();
        let session_checks: Vec<_> = self
            .checks
            .iter()
            .filter(|c| c.component.starts_with("session:"))
            .collect();
        let system_checks: Vec<_> = self
            .checks
            .iter()
            .filter(|c| !c.component.starts_with("agent:") && !c.component.starts_with("session:"))
            .collect();

        if !agent_checks.is_empty() {
            println!("\n{}:", "Agent Health".underline());
            for check in agent_checks {
                println!("  {} {}", check.status.as_emoji(), check.component);
                println!("    Status: {}", check.status.as_colored_text());
                println!("    Message: {}", check.message);
                if let Some(details) = &check.details {
                    println!(
                        "    Details: {}",
                        serde_json::to_string_pretty(details)
                            .unwrap_or_else(|e| format!("Failed to serialize details: {}", e))
                    );
                }
            }
        }

        if !session_checks.is_empty() {
            println!("\n{}:", "AI-Session Health".underline());
            for check in session_checks {
                println!("  {} {}", check.status.as_emoji(), check.component);
                println!("    Status: {}", check.status.as_colored_text());
                println!("    Message: {}", check.message);
                if let Some(details) = &check.details {
                    println!(
                        "    Details: {}",
                        serde_json::to_string_pretty(details)
                            .unwrap_or_else(|e| format!("Failed to serialize details: {}", e))
                    );
                }
            }
        }

        if !system_checks.is_empty() {
            println!("\n{}:", "System Health".underline());
            for check in system_checks {
                println!("  {} {}", check.status.as_emoji(), check.component);
                println!("    Status: {}", check.status.as_colored_text());
                println!("    Message: {}", check.message);
                if let Some(details) = &check.details {
                    println!(
                        "    Details: {}",
                        serde_json::to_string_pretty(details)
                            .unwrap_or_else(|e| format!("Failed to serialize details: {}", e))
                    );
                }
            }
        }
    }

[36m--- ./crates/ccswarm/src/cli/health.rs:check_sessions (lines 391-440) ---[0m
    async fn check_sessions(&self) -> Result<Vec<HealthCheckResult>> {
        let mut results = Vec::new();

        // Check for active sessions in .ccswarm/sessions directory
        let sessions_dir = std::path::Path::new(".ccswarm/sessions");
        if sessions_dir.exists() {
            let mut entries = tokio::fs::read_dir(sessions_dir).await?;

            while let Some(entry) = entries.next_entry().await? {
                if entry.file_type().await?.is_dir() {
                    let session_name = entry.file_name().to_string_lossy().to_string();
                    let start = Instant::now();

                    // Check session state file
                    let state_file = entry.path().join("state.json");
                    let health_status = if state_file.exists() {
                        match tokio::fs::read_to_string(&state_file).await {
                            Ok(content) => {
                                if serde_json::from_str::<serde_json::Value>(&content).is_ok() {
                                    HealthStatus::Healthy
                                } else {
                                    HealthStatus::Warning
                                }
                            }
                            Err(_) => HealthStatus::Warning,
                        }
                    } else {
                        HealthStatus::Critical
                    };

                    results.push(HealthCheckResult {
                        component: format!("session:{}", session_name),
                        status: health_status,
                        message: match health_status {
                            HealthStatus::Healthy => "Session is active and healthy".to_string(),
                            HealthStatus::Warning => "Session state is unclear".to_string(),
                            _ => "Session may be corrupted".to_string(),
                        },
                        details: Some(serde_json::json!({
                            "session_name": session_name,
                            "state_file_exists": state_file.exists(),
                        })),
                        response_time_ms: start.elapsed().as_millis() as u64,
                    });
                }
            }
        }

        Ok(results)
    }

  ./crates/ccswarm/src/cli/health.rs:122-191 method print_detailed <-> ./crates/ccswarm/src/cli/health.rs:209-261 method check_all
  Similarity: 78.99%
  Classes: SystemHealthReport <-> HealthChecker

[36m--- ./crates/ccswarm/src/cli/health.rs:print_detailed (lines 122-191) ---[0m
    pub fn print_detailed(&self) {
        self.print_summary();

        println!("\n{}", "Detailed Component Information:".bold());

        // Group checks by component type
        let agent_checks: Vec<_> = self
            .checks
            .iter()
            .filter(|c| c.component.starts_with("agent:"))
            .collect();
        let session_checks: Vec<_> = self
            .checks
            .iter()
            .filter(|c| c.component.starts_with("session:"))
            .collect();
        let system_checks: Vec<_> = self
            .checks
            .iter()
            .filter(|c| !c.component.starts_with("agent:") && !c.component.starts_with("session:"))
            .collect();

        if !agent_checks.is_empty() {
            println!("\n{}:", "Agent Health".underline());
            for check in agent_checks {
                println!("  {} {}", check.status.as_emoji(), check.component);
                println!("    Status: {}", check.status.as_colored_text());
                println!("    Message: {}", check.message);
                if let Some(details) = &check.details {
                    println!(
                        "    Details: {}",
                        serde_json::to_string_pretty(details)
                            .unwrap_or_else(|e| format!("Failed to serialize details: {}", e))
                    );
                }
            }
        }

        if !session_checks.is_empty() {
            println!("\n{}:", "AI-Session Health".underline());
            for check in session_checks {
                println!("  {} {}", check.status.as_emoji(), check.component);
                println!("    Status: {}", check.status.as_colored_text());
                println!("    Message: {}", check.message);
                if let Some(details) = &check.details {
                    println!(
                        "    Details: {}",
                        serde_json::to_string_pretty(details)
                            .unwrap_or_else(|e| format!("Failed to serialize details: {}", e))
                    );
                }
            }
        }

        if !system_checks.is_empty() {
            println!("\n{}:", "System Health".underline());
            for check in system_checks {
                println!("  {} {}", check.status.as_emoji(), check.component);
                println!("    Status: {}", check.status.as_colored_text());
                println!("    Message: {}", check.message);
                if let Some(details) = &check.details {
                    println!(
                        "    Details: {}",
                        serde_json::to_string_pretty(details)
                            .unwrap_or_else(|e| format!("Failed to serialize details: {}", e))
                    );
                }
            }
        }
    }

[36m--- ./crates/ccswarm/src/cli/health.rs:check_all (lines 209-261) ---[0m
    pub async fn check_all(&self) -> Result<SystemHealthReport> {
        let mut checks = Vec::new();
        let _start_time = Instant::now();

        // Check orchestrator status
        checks.push(self.check_orchestrator().await);

        // Check all agents
        let agent_checks = self.check_agents().await?;
        let healthy_agents = agent_checks
            .iter()
            .filter(|c| c.status == HealthStatus::Healthy)
            .count();
        checks.extend(agent_checks);

        // Check AI sessions
        let session_checks = self.check_sessions().await?;
        let session_count = session_checks.len();
        checks.extend(session_checks);

        // Check monitoring system
        if self.monitoring_system.is_some() {
            checks.push(self.check_monitoring_system().await);
        }

        // Check coordination bus
        checks.push(self.check_coordination_bus().await);

        // Determine overall status
        let overall_status = if checks
            .iter()
            .any(|c| matches!(c.status, HealthStatus::Down | HealthStatus::Critical))
        {
            HealthStatus::Critical
        } else if checks.iter().any(|c| c.status == HealthStatus::Warning) {
            HealthStatus::Warning
        } else {
            HealthStatus::Healthy
        };

        // Get active task count
        let active_tasks = self.get_active_task_count().await;

        Ok(SystemHealthReport {
            timestamp: chrono::Utc::now(),
            overall_status,
            checks,
            total_agents: self.get_total_agent_count().await,
            healthy_agents,
            active_tasks,
            session_count,
        })
    }

  ./crates/ccswarm/src/cli/health.rs:331-389 method check_agents <-> ./crates/ccswarm/src/cli/health.rs:470-508 method check_coordination_bus
  Similarity: 94.65%
  Classes: HealthChecker <-> HealthChecker

[36m--- ./crates/ccswarm/src/cli/health.rs:check_agents (lines 331-389) ---[0m
    async fn check_agents(&self) -> Result<Vec<HealthCheckResult>> {
        let mut results = Vec::new();

        // Get all agent statuses
        let statuses = self.status_tracker.get_all_statuses().await?;

        for status in statuses.into_iter() {
            let agent_name = status
                .get("agent_id")
                .and_then(|v| v.as_str())
                .unwrap_or("unknown")
                .to_string();
            let start = Instant::now();

            // Check agent responsiveness
            let timestamp_str = status
                .get("timestamp")
                .and_then(|v| v.as_str())
                .unwrap_or("");

            let last_update = chrono::DateTime::parse_from_rfc3339(timestamp_str)
                .map(|dt| dt.with_timezone(&chrono::Utc))
                .unwrap_or_else(|_| chrono::Utc::now());

            let age = chrono::Utc::now() - last_update;
            let health_status = if age.num_seconds() < 30 {
                HealthStatus::Healthy
            } else if age.num_seconds() < 120 {
                HealthStatus::Warning
            } else {
                HealthStatus::Critical
            };

            let mut details = serde_json::json!({
                "status": status.get("status").unwrap_or(&serde_json::Value::Null),
                "last_update": timestamp_str,
                "age_seconds": age.num_seconds(),
            });

            // Add agent-specific details
            if let Some(agent_details) = status.get("additional_info") {
                details["agent_details"] = agent_details.clone();
            }

            results.push(HealthCheckResult {
                component: format!("agent:{}", agent_name),
                status: health_status,
                message: match health_status {
                    HealthStatus::Healthy => format!("{} agent is responsive", agent_name),
                    HealthStatus::Warning => format!("{} agent response is delayed", agent_name),
                    _ => format!("{} agent is unresponsive", agent_name),
                },
                details: Some(details),
                response_time_ms: start.elapsed().as_millis() as u64,
            });
        }

        Ok(results)
    }

[36m--- ./crates/ccswarm/src/cli/health.rs:check_coordination_bus (lines 470-508) ---[0m
    async fn check_coordination_bus(&self) -> HealthCheckResult {
        let start = Instant::now();

        // Check if coordination directory exists and is writable
        let coord_dir = std::path::Path::new(".ccswarm/coordination");
        if coord_dir.exists() && coord_dir.is_dir() {
            // Try to write a test file
            let test_file = coord_dir.join(".health_check");
            match tokio::fs::write(&test_file, b"health_check").await {
                Ok(_) => {
                    // Clean up test file
                    let _ = tokio::fs::remove_file(&test_file).await;

                    HealthCheckResult {
                        component: "coordination_bus".to_string(),
                        status: HealthStatus::Healthy,
                        message: "Coordination bus is operational".to_string(),
                        details: None,
                        response_time_ms: start.elapsed().as_millis() as u64,
                    }
                }
                Err(e) => HealthCheckResult {
                    component: "coordination_bus".to_string(),
                    status: HealthStatus::Critical,
                    message: format!("Coordination bus write failed: {}", e),
                    details: None,
                    response_time_ms: start.elapsed().as_millis() as u64,
                },
            }
        } else {
            HealthCheckResult {
                component: "coordination_bus".to_string(),
                status: HealthStatus::Down,
                message: "Coordination directory not found".to_string(),
                details: None,
                response_time_ms: start.elapsed().as_millis() as u64,
            }
        }
    }

  ./crates/ccswarm/src/cli/health.rs:275-329 method check_orchestrator <-> ./crates/ccswarm/src/cli/health.rs:470-508 method check_coordination_bus
  Similarity: 96.95%
  Classes: HealthChecker <-> HealthChecker

[36m--- ./crates/ccswarm/src/cli/health.rs:check_orchestrator (lines 275-329) ---[0m
    async fn check_orchestrator(&self) -> HealthCheckResult {
        let start = Instant::now();

        // Check if orchestrator is running by checking coordination files
        match tokio::fs::read_to_string(".ccswarm/coordination/status.json").await {
            Ok(content) => {
                if let Ok(status) = serde_json::from_str::<serde_json::Value>(&content) {
                    let last_update = status
                        .get("last_update")
                        .and_then(|v| v.as_str())
                        .and_then(|s| chrono::DateTime::parse_from_rfc3339(s).ok());

                    let status = if let Some(last_update) = last_update {
                        let age = chrono::Utc::now() - last_update.with_timezone(&chrono::Utc);
                        if age.num_seconds() < 60 {
                            HealthStatus::Healthy
                        } else if age.num_seconds() < 300 {
                            HealthStatus::Warning
                        } else {
                            HealthStatus::Critical
                        }
                    } else {
                        HealthStatus::Warning
                    };

                    HealthCheckResult {
                        component: "orchestrator".to_string(),
                        status,
                        message: match status {
                            HealthStatus::Healthy => "Orchestrator is running normally".to_string(),
                            HealthStatus::Warning => "Orchestrator status is stale".to_string(),
                            _ => "Orchestrator may be unresponsive".to_string(),
                        },
                        details: Some(serde_json::json!({"status": status.as_colored_text()})),
                        response_time_ms: start.elapsed().as_millis() as u64,
                    }
                } else {
                    HealthCheckResult {
                        component: "orchestrator".to_string(),
                        status: HealthStatus::Down,
                        message: "Invalid status file".to_string(),
                        details: None,
                        response_time_ms: start.elapsed().as_millis() as u64,
                    }
                }
            }
            Err(_) => HealthCheckResult {
                component: "orchestrator".to_string(),
                status: HealthStatus::Down,
                message: "Orchestrator not running".to_string(),
                details: None,
                response_time_ms: start.elapsed().as_millis() as u64,
            },
        }
    }

[36m--- ./crates/ccswarm/src/cli/health.rs:check_coordination_bus (lines 470-508) ---[0m
    async fn check_coordination_bus(&self) -> HealthCheckResult {
        let start = Instant::now();

        // Check if coordination directory exists and is writable
        let coord_dir = std::path::Path::new(".ccswarm/coordination");
        if coord_dir.exists() && coord_dir.is_dir() {
            // Try to write a test file
            let test_file = coord_dir.join(".health_check");
            match tokio::fs::write(&test_file, b"health_check").await {
                Ok(_) => {
                    // Clean up test file
                    let _ = tokio::fs::remove_file(&test_file).await;

                    HealthCheckResult {
                        component: "coordination_bus".to_string(),
                        status: HealthStatus::Healthy,
                        message: "Coordination bus is operational".to_string(),
                        details: None,
                        response_time_ms: start.elapsed().as_millis() as u64,
                    }
                }
                Err(e) => HealthCheckResult {
                    component: "coordination_bus".to_string(),
                    status: HealthStatus::Critical,
                    message: format!("Coordination bus write failed: {}", e),
                    details: None,
                    response_time_ms: start.elapsed().as_millis() as u64,
                },
            }
        } else {
            HealthCheckResult {
                component: "coordination_bus".to_string(),
                status: HealthStatus::Down,
                message: "Coordination directory not found".to_string(),
                details: None,
                response_time_ms: start.elapsed().as_millis() as u64,
            }
        }
    }

  ./crates/ccswarm/src/cli/health.rs:391-440 method check_sessions <-> ./crates/ccswarm/src/cli/health.rs:470-508 method check_coordination_bus
  Similarity: 94.42%
  Classes: HealthChecker <-> HealthChecker

[36m--- ./crates/ccswarm/src/cli/health.rs:check_sessions (lines 391-440) ---[0m
    async fn check_sessions(&self) -> Result<Vec<HealthCheckResult>> {
        let mut results = Vec::new();

        // Check for active sessions in .ccswarm/sessions directory
        let sessions_dir = std::path::Path::new(".ccswarm/sessions");
        if sessions_dir.exists() {
            let mut entries = tokio::fs::read_dir(sessions_dir).await?;

            while let Some(entry) = entries.next_entry().await? {
                if entry.file_type().await?.is_dir() {
                    let session_name = entry.file_name().to_string_lossy().to_string();
                    let start = Instant::now();

                    // Check session state file
                    let state_file = entry.path().join("state.json");
                    let health_status = if state_file.exists() {
                        match tokio::fs::read_to_string(&state_file).await {
                            Ok(content) => {
                                if serde_json::from_str::<serde_json::Value>(&content).is_ok() {
                                    HealthStatus::Healthy
                                } else {
                                    HealthStatus::Warning
                                }
                            }
                            Err(_) => HealthStatus::Warning,
                        }
                    } else {
                        HealthStatus::Critical
                    };

                    results.push(HealthCheckResult {
                        component: format!("session:{}", session_name),
                        status: health_status,
                        message: match health_status {
                            HealthStatus::Healthy => "Session is active and healthy".to_string(),
                            HealthStatus::Warning => "Session state is unclear".to_string(),
                            _ => "Session may be corrupted".to_string(),
                        },
                        details: Some(serde_json::json!({
                            "session_name": session_name,
                            "state_file_exists": state_file.exists(),
                        })),
                        response_time_ms: start.elapsed().as_millis() as u64,
                    });
                }
            }
        }

        Ok(results)
    }

[36m--- ./crates/ccswarm/src/cli/health.rs:check_coordination_bus (lines 470-508) ---[0m
    async fn check_coordination_bus(&self) -> HealthCheckResult {
        let start = Instant::now();

        // Check if coordination directory exists and is writable
        let coord_dir = std::path::Path::new(".ccswarm/coordination");
        if coord_dir.exists() && coord_dir.is_dir() {
            // Try to write a test file
            let test_file = coord_dir.join(".health_check");
            match tokio::fs::write(&test_file, b"health_check").await {
                Ok(_) => {
                    // Clean up test file
                    let _ = tokio::fs::remove_file(&test_file).await;

                    HealthCheckResult {
                        component: "coordination_bus".to_string(),
                        status: HealthStatus::Healthy,
                        message: "Coordination bus is operational".to_string(),
                        details: None,
                        response_time_ms: start.elapsed().as_millis() as u64,
                    }
                }
                Err(e) => HealthCheckResult {
                    component: "coordination_bus".to_string(),
                    status: HealthStatus::Critical,
                    message: format!("Coordination bus write failed: {}", e),
                    details: None,
                    response_time_ms: start.elapsed().as_millis() as u64,
                },
            }
        } else {
            HealthCheckResult {
                component: "coordination_bus".to_string(),
                status: HealthStatus::Down,
                message: "Coordination directory not found".to_string(),
                details: None,
                response_time_ms: start.elapsed().as_millis() as u64,
            }
        }
    }

  ./crates/ccswarm/src/cli/health.rs:81-120 method print_summary <-> ./crates/ccswarm/src/cli/health.rs:331-389 method check_agents
  Similarity: 76.47%
  Classes: SystemHealthReport <-> HealthChecker

[36m--- ./crates/ccswarm/src/cli/health.rs:print_summary (lines 81-120) ---[0m
    pub fn print_summary(&self) {
        println!("\n{}", "═══ ccswarm Health Report ═══".bold());
        println!(
            "Timestamp: {}",
            self.timestamp.format("%Y-%m-%d %H:%M:%S UTC")
        );
        println!(
            "Overall Status: {} {}",
            self.overall_status.as_emoji(),
            self.overall_status.as_colored_text()
        );
        println!();

        // Agent summary
        let agent_health_pct = if self.total_agents > 0 {
            (self.healthy_agents as f64 / self.total_agents as f64 * 100.0) as u32
        } else {
            0
        };
        println!(
            "Agents: {}/{} healthy ({}%)",
            self.healthy_agents, self.total_agents, agent_health_pct
        );
        println!("Active Tasks: {}", self.active_tasks);
        println!("AI Sessions: {}", self.session_count);
        println!();

        // Component checks
        println!("{}", "Component Health Checks:".bold());
        for check in &self.checks {
            println!("  {}", check);
            if let Some(details) = &check.details {
                if let Some(obj) = details.as_object() {
                    for (key, value) in obj {
                        println!("    - {}: {}", key.dimmed(), value);
                    }
                }
            }
        }
    }

[36m--- ./crates/ccswarm/src/cli/health.rs:check_agents (lines 331-389) ---[0m
    async fn check_agents(&self) -> Result<Vec<HealthCheckResult>> {
        let mut results = Vec::new();

        // Get all agent statuses
        let statuses = self.status_tracker.get_all_statuses().await?;

        for status in statuses.into_iter() {
            let agent_name = status
                .get("agent_id")
                .and_then(|v| v.as_str())
                .unwrap_or("unknown")
                .to_string();
            let start = Instant::now();

            // Check agent responsiveness
            let timestamp_str = status
                .get("timestamp")
                .and_then(|v| v.as_str())
                .unwrap_or("");

            let last_update = chrono::DateTime::parse_from_rfc3339(timestamp_str)
                .map(|dt| dt.with_timezone(&chrono::Utc))
                .unwrap_or_else(|_| chrono::Utc::now());

            let age = chrono::Utc::now() - last_update;
            let health_status = if age.num_seconds() < 30 {
                HealthStatus::Healthy
            } else if age.num_seconds() < 120 {
                HealthStatus::Warning
            } else {
                HealthStatus::Critical
            };

            let mut details = serde_json::json!({
                "status": status.get("status").unwrap_or(&serde_json::Value::Null),
                "last_update": timestamp_str,
                "age_seconds": age.num_seconds(),
            });

            // Add agent-specific details
            if let Some(agent_details) = status.get("additional_info") {
                details["agent_details"] = agent_details.clone();
            }

            results.push(HealthCheckResult {
                component: format!("agent:{}", agent_name),
                status: health_status,
                message: match health_status {
                    HealthStatus::Healthy => format!("{} agent is responsive", agent_name),
                    HealthStatus::Warning => format!("{} agent response is delayed", agent_name),
                    _ => format!("{} agent is unresponsive", agent_name),
                },
                details: Some(details),
                response_time_ms: start.elapsed().as_millis() as u64,
            });
        }

        Ok(results)
    }

  ./crates/ccswarm/src/cli/health.rs:81-120 method print_summary <-> ./crates/ccswarm/src/cli/health.rs:209-261 method check_all
  Similarity: 75.50%
  Classes: SystemHealthReport <-> HealthChecker

[36m--- ./crates/ccswarm/src/cli/health.rs:print_summary (lines 81-120) ---[0m
    pub fn print_summary(&self) {
        println!("\n{}", "═══ ccswarm Health Report ═══".bold());
        println!(
            "Timestamp: {}",
            self.timestamp.format("%Y-%m-%d %H:%M:%S UTC")
        );
        println!(
            "Overall Status: {} {}",
            self.overall_status.as_emoji(),
            self.overall_status.as_colored_text()
        );
        println!();

        // Agent summary
        let agent_health_pct = if self.total_agents > 0 {
            (self.healthy_agents as f64 / self.total_agents as f64 * 100.0) as u32
        } else {
            0
        };
        println!(
            "Agents: {}/{} healthy ({}%)",
            self.healthy_agents, self.total_agents, agent_health_pct
        );
        println!("Active Tasks: {}", self.active_tasks);
        println!("AI Sessions: {}", self.session_count);
        println!();

        // Component checks
        println!("{}", "Component Health Checks:".bold());
        for check in &self.checks {
            println!("  {}", check);
            if let Some(details) = &check.details {
                if let Some(obj) = details.as_object() {
                    for (key, value) in obj {
                        println!("    - {}: {}", key.dimmed(), value);
                    }
                }
            }
        }
    }

[36m--- ./crates/ccswarm/src/cli/health.rs:check_all (lines 209-261) ---[0m
    pub async fn check_all(&self) -> Result<SystemHealthReport> {
        let mut checks = Vec::new();
        let _start_time = Instant::now();

        // Check orchestrator status
        checks.push(self.check_orchestrator().await);

        // Check all agents
        let agent_checks = self.check_agents().await?;
        let healthy_agents = agent_checks
            .iter()
            .filter(|c| c.status == HealthStatus::Healthy)
            .count();
        checks.extend(agent_checks);

        // Check AI sessions
        let session_checks = self.check_sessions().await?;
        let session_count = session_checks.len();
        checks.extend(session_checks);

        // Check monitoring system
        if self.monitoring_system.is_some() {
            checks.push(self.check_monitoring_system().await);
        }

        // Check coordination bus
        checks.push(self.check_coordination_bus().await);

        // Determine overall status
        let overall_status = if checks
            .iter()
            .any(|c| matches!(c.status, HealthStatus::Down | HealthStatus::Critical))
        {
            HealthStatus::Critical
        } else if checks.iter().any(|c| c.status == HealthStatus::Warning) {
            HealthStatus::Warning
        } else {
            HealthStatus::Healthy
        };

        // Get active task count
        let active_tasks = self.get_active_task_count().await;

        Ok(SystemHealthReport {
            timestamp: chrono::Utc::now(),
            overall_status,
            checks,
            total_agents: self.get_total_agent_count().await,
            healthy_agents,
            active_tasks,
            session_count,
        })
    }

  ./crates/ccswarm/src/cli/health.rs:81-120 method print_summary <-> ./crates/ccswarm/src/cli/health.rs:391-440 method check_sessions
  Similarity: 73.42%
  Classes: SystemHealthReport <-> HealthChecker

[36m--- ./crates/ccswarm/src/cli/health.rs:print_summary (lines 81-120) ---[0m
    pub fn print_summary(&self) {
        println!("\n{}", "═══ ccswarm Health Report ═══".bold());
        println!(
            "Timestamp: {}",
            self.timestamp.format("%Y-%m-%d %H:%M:%S UTC")
        );
        println!(
            "Overall Status: {} {}",
            self.overall_status.as_emoji(),
            self.overall_status.as_colored_text()
        );
        println!();

        // Agent summary
        let agent_health_pct = if self.total_agents > 0 {
            (self.healthy_agents as f64 / self.total_agents as f64 * 100.0) as u32
        } else {
            0
        };
        println!(
            "Agents: {}/{} healthy ({}%)",
            self.healthy_agents, self.total_agents, agent_health_pct
        );
        println!("Active Tasks: {}", self.active_tasks);
        println!("AI Sessions: {}", self.session_count);
        println!();

        // Component checks
        println!("{}", "Component Health Checks:".bold());
        for check in &self.checks {
            println!("  {}", check);
            if let Some(details) = &check.details {
                if let Some(obj) = details.as_object() {
                    for (key, value) in obj {
                        println!("    - {}: {}", key.dimmed(), value);
                    }
                }
            }
        }
    }

[36m--- ./crates/ccswarm/src/cli/health.rs:check_sessions (lines 391-440) ---[0m
    async fn check_sessions(&self) -> Result<Vec<HealthCheckResult>> {
        let mut results = Vec::new();

        // Check for active sessions in .ccswarm/sessions directory
        let sessions_dir = std::path::Path::new(".ccswarm/sessions");
        if sessions_dir.exists() {
            let mut entries = tokio::fs::read_dir(sessions_dir).await?;

            while let Some(entry) = entries.next_entry().await? {
                if entry.file_type().await?.is_dir() {
                    let session_name = entry.file_name().to_string_lossy().to_string();
                    let start = Instant::now();

                    // Check session state file
                    let state_file = entry.path().join("state.json");
                    let health_status = if state_file.exists() {
                        match tokio::fs::read_to_string(&state_file).await {
                            Ok(content) => {
                                if serde_json::from_str::<serde_json::Value>(&content).is_ok() {
                                    HealthStatus::Healthy
                                } else {
                                    HealthStatus::Warning
                                }
                            }
                            Err(_) => HealthStatus::Warning,
                        }
                    } else {
                        HealthStatus::Critical
                    };

                    results.push(HealthCheckResult {
                        component: format!("session:{}", session_name),
                        status: health_status,
                        message: match health_status {
                            HealthStatus::Healthy => "Session is active and healthy".to_string(),
                            HealthStatus::Warning => "Session state is unclear".to_string(),
                            _ => "Session may be corrupted".to_string(),
                        },
                        details: Some(serde_json::json!({
                            "session_name": session_name,
                            "state_file_exists": state_file.exists(),
                        })),
                        response_time_ms: start.elapsed().as_millis() as u64,
                    });
                }
            }
        }

        Ok(results)
    }

  ./crates/ccswarm/src/cli/health.rs:442-468 method check_monitoring_system <-> ./crates/ccswarm/src/cli/health.rs:470-508 method check_coordination_bus
  Similarity: 90.44%
  Classes: HealthChecker <-> HealthChecker

[36m--- ./crates/ccswarm/src/cli/health.rs:check_monitoring_system (lines 442-468) ---[0m
    async fn check_monitoring_system(&self) -> HealthCheckResult {
        let start = Instant::now();

        if let Some(monitoring) = &self.monitoring_system {
            let stats = monitoring.get_stats();

            HealthCheckResult {
                component: "monitoring_system".to_string(),
                status: HealthStatus::Healthy,
                message: "Monitoring system is active".to_string(),
                details: Some(serde_json::json!({
                    "total_entries": stats.total_entries,
                    "entries_per_agent": stats.entries_per_agent,
                    "entries_per_type": stats.entries_per_type,
                })),
                response_time_ms: start.elapsed().as_millis() as u64,
            }
        } else {
            HealthCheckResult {
                component: "monitoring_system".to_string(),
                status: HealthStatus::Down,
                message: "Monitoring system not initialized".to_string(),
                details: None,
                response_time_ms: start.elapsed().as_millis() as u64,
            }
        }
    }

[36m--- ./crates/ccswarm/src/cli/health.rs:check_coordination_bus (lines 470-508) ---[0m
    async fn check_coordination_bus(&self) -> HealthCheckResult {
        let start = Instant::now();

        // Check if coordination directory exists and is writable
        let coord_dir = std::path::Path::new(".ccswarm/coordination");
        if coord_dir.exists() && coord_dir.is_dir() {
            // Try to write a test file
            let test_file = coord_dir.join(".health_check");
            match tokio::fs::write(&test_file, b"health_check").await {
                Ok(_) => {
                    // Clean up test file
                    let _ = tokio::fs::remove_file(&test_file).await;

                    HealthCheckResult {
                        component: "coordination_bus".to_string(),
                        status: HealthStatus::Healthy,
                        message: "Coordination bus is operational".to_string(),
                        details: None,
                        response_time_ms: start.elapsed().as_millis() as u64,
                    }
                }
                Err(e) => HealthCheckResult {
                    component: "coordination_bus".to_string(),
                    status: HealthStatus::Critical,
                    message: format!("Coordination bus write failed: {}", e),
                    details: None,
                    response_time_ms: start.elapsed().as_millis() as u64,
                },
            }
        } else {
            HealthCheckResult {
                component: "coordination_bus".to_string(),
                status: HealthStatus::Down,
                message: "Coordination directory not found".to_string(),
                details: None,
                response_time_ms: start.elapsed().as_millis() as u64,
            }
        }
    }

  ./crates/ccswarm/src/cli/health.rs:442-468 method check_monitoring_system <-> ./crates/ccswarm/src/cli/health.rs:510-522 method get_active_task_count
  Similarity: 94.00%
  Classes: HealthChecker <-> HealthChecker

[36m--- ./crates/ccswarm/src/cli/health.rs:check_monitoring_system (lines 442-468) ---[0m
    async fn check_monitoring_system(&self) -> HealthCheckResult {
        let start = Instant::now();

        if let Some(monitoring) = &self.monitoring_system {
            let stats = monitoring.get_stats();

            HealthCheckResult {
                component: "monitoring_system".to_string(),
                status: HealthStatus::Healthy,
                message: "Monitoring system is active".to_string(),
                details: Some(serde_json::json!({
                    "total_entries": stats.total_entries,
                    "entries_per_agent": stats.entries_per_agent,
                    "entries_per_type": stats.entries_per_type,
                })),
                response_time_ms: start.elapsed().as_millis() as u64,
            }
        } else {
            HealthCheckResult {
                component: "monitoring_system".to_string(),
                status: HealthStatus::Down,
                message: "Monitoring system not initialized".to_string(),
                details: None,
                response_time_ms: start.elapsed().as_millis() as u64,
            }
        }
    }

[36m--- ./crates/ccswarm/src/cli/health.rs:get_active_task_count (lines 510-522) ---[0m
    async fn get_active_task_count(&self) -> usize {
        // Read from task queue file if exists
        if let Ok(content) =
            tokio::fs::read_to_string(".ccswarm/coordination/task_queue.json").await
        {
            if let Ok(queue) = serde_json::from_str::<serde_json::Value>(&content) {
                if let Some(tasks) = queue.get("pending_tasks").and_then(|v| v.as_array()) {
                    return tasks.len();
                }
            }
        }
        0
    }

  ./crates/ccswarm/src/cli/health.rs:56-65 method fmt <-> ./crates/ccswarm/src/cli/health.rs:524-530 method get_total_agent_count
  Similarity: 85.35%
  Classes: HealthCheckResult <-> HealthChecker

[36m--- ./crates/ccswarm/src/cli/health.rs:fmt (lines 56-65) ---[0m
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        write!(
            f,
            "{} {} - {} ({}ms)",
            self.status.as_emoji(),
            self.component,
            self.message,
            self.response_time_ms
        )
    }

[36m--- ./crates/ccswarm/src/cli/health.rs:get_total_agent_count (lines 524-530) ---[0m
    async fn get_total_agent_count(&self) -> usize {
        self.status_tracker
            .get_all_statuses()
            .await
            .map(|statuses| statuses.len())
            .unwrap_or(0)
    }

  ./crates/ccswarm/src/cli/health.rs:26-33 method as_emoji <-> ./crates/ccswarm/src/cli/health.rs:524-530 method get_total_agent_count
  Similarity: 87.68%
  Classes: HealthStatus <-> HealthChecker

[36m--- ./crates/ccswarm/src/cli/health.rs:as_emoji (lines 26-33) ---[0m
    pub fn as_emoji(&self) -> &'static str {
        match self {
            HealthStatus::Healthy => "✅",
            HealthStatus::Warning => "⚠️ ",
            HealthStatus::Critical => "🔴",
            HealthStatus::Down => "❌",
        }
    }

[36m--- ./crates/ccswarm/src/cli/health.rs:get_total_agent_count (lines 524-530) ---[0m
    async fn get_total_agent_count(&self) -> usize {
        self.status_tracker
            .get_all_statuses()
            .await
            .map(|statuses| statuses.len())
            .unwrap_or(0)
    }


Duplicates in ./crates/ccswarm/tests/resource_monitoring_test.rs:
------------------------------------------------------------
  ./crates/ccswarm/tests/resource_monitoring_test.rs:10-47 function test_resource_monitoring_integration <-> ./crates/ccswarm/tests/resource_monitoring_test.rs:50-92 function test_idle_agent_suspension
  Similarity: 88.29%

[36m--- ./crates/ccswarm/tests/resource_monitoring_test.rs:test_resource_monitoring_integration (lines 10-47) ---[0m
async fn test_resource_monitoring_integration() -> Result<()> {
    // Create resource limits with short idle timeout for testing
    let mut limits = ResourceLimits::default();
    limits.idle_timeout = Duration::seconds(5);
    limits.auto_suspend_enabled = true;

    // Create session manager with resource monitoring
    let session_manager = SessionManager::with_resource_monitoring(limits).await?;

    // Create a test session
    let session = session_manager
        .create_session(
            "test-agent".to_string(),
            default_frontend_role(),
            "/tmp/test".to_string(),
            Some("Test session".to_string()),
            false,
        )
        .await?;

    // Check that resource monitoring was started
    let usage = session_manager.get_session_resource_usage(&session.id);
    assert!(usage.is_some(), "Resource usage should be available");

    // Get efficiency stats
    let stats = session_manager.get_resource_efficiency_stats();
    assert!(stats.is_some(), "Resource stats should be available");

    let stats = stats.unwrap();
    assert_eq!(stats.total_agents, 1);
    assert_eq!(stats.active_agents, 1);
    assert_eq!(stats.suspended_agents, 0);

    // Clean up
    session_manager.terminate_session(&session.id).await?;

    Ok(())
}

[36m--- ./crates/ccswarm/tests/resource_monitoring_test.rs:test_idle_agent_suspension (lines 50-92) ---[0m
async fn test_idle_agent_suspension() -> Result<()> {
    // Create resource limits with very short idle timeout
    let mut limits = ResourceLimits::default();
    limits.idle_timeout = Duration::milliseconds(100); // Very short for testing
    limits.auto_suspend_enabled = true;

    // Create session manager
    let session_manager = Arc::new(SessionManager::with_resource_monitoring(limits).await?);

    // Create a test session
    let session = session_manager
        .create_session(
            "idle-test-agent".to_string(),
            default_frontend_role(),
            "/tmp/test".to_string(),
            Some("Idle test session".to_string()),
            false,
        )
        .await?;

    // Wait for idle timeout
    tokio::time::sleep(tokio::time::Duration::from_millis(200)).await;

    // Check for idle agents
    let suspended = session_manager.check_and_suspend_idle_agents().await?;

    // Should have suspended our agent
    assert!(!suspended.is_empty(), "Should have suspended idle agent");
    assert!(suspended.contains(&"idle-test-agent".to_string()));

    // Verify session is paused
    let current_session = session_manager.get_session(&session.id);
    assert!(current_session.is_some());
    assert_eq!(
        current_session.unwrap().status,
        ccswarm::session::SessionStatus::Paused
    );

    // Clean up
    session_manager.terminate_session(&session.id).await?;

    Ok(())
}


Duplicates in ./crates/ccswarm/src/cli/output.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/cli/output.rs:53-64 method format_success <-> ./crates/ccswarm/src/cli/output.rs:121-131 method format_list
  Similarity: 83.89%
  Classes: JsonFormatter <-> HumanFormatter

[36m--- ./crates/ccswarm/src/cli/output.rs:format_success (lines 53-64) ---[0m
    pub fn format_success(&self, message: &str, data: Option<Value>) -> String {
        let mut result = serde_json::json!({
            "status": "success",
            "message": message
        });

        if let Some(data) = data {
            result["data"] = data;
        }

        serde_json::to_string_pretty(&result).unwrap_or_else(|_| message.to_string())
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:format_list (lines 121-131) ---[0m
    pub fn format_list<T: Serialize>(&self, items: &[T], title: &str) -> String {
        let mut output = format!("📋 {} ({} items):\n", title, items.len());

        for (i, item) in items.iter().enumerate() {
            if let Ok(json) = serde_json::to_value(item) {
                output.push_str(&format!("  {}. {}\n", i + 1, format_item(&json)));
            }
        }

        output
    }

  ./crates/ccswarm/src/cli/output.rs:67-74 method format_error <-> ./crates/ccswarm/src/cli/output.rs:77-86 method format_list
  Similarity: 90.59%
  Classes: JsonFormatter <-> JsonFormatter

[36m--- ./crates/ccswarm/src/cli/output.rs:format_error (lines 67-74) ---[0m
    pub fn format_error(&self, error: &str) -> String {
        let result = serde_json::json!({
            "status": "error",
            "error": error
        });

        serde_json::to_string_pretty(&result).unwrap_or_else(|_| error.to_string())
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:format_list (lines 77-86) ---[0m
    pub fn format_list<T: Serialize>(&self, items: &[T], title: &str) -> String {
        let result = serde_json::json!({
            "title": title,
            "count": items.len(),
            "items": items
        });

        serde_json::to_string_pretty(&result)
            .unwrap_or_else(|_| format!("{}: {} items", title, items.len()))
    }

  ./crates/ccswarm/src/cli/output.rs:77-86 method format_list <-> ./crates/ccswarm/src/cli/output.rs:89-96 method format_status
  Similarity: 90.59%
  Classes: JsonFormatter <-> JsonFormatter

[36m--- ./crates/ccswarm/src/cli/output.rs:format_list (lines 77-86) ---[0m
    pub fn format_list<T: Serialize>(&self, items: &[T], title: &str) -> String {
        let result = serde_json::json!({
            "title": title,
            "count": items.len(),
            "items": items
        });

        serde_json::to_string_pretty(&result)
            .unwrap_or_else(|_| format!("{}: {} items", title, items.len()))
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:format_status (lines 89-96) ---[0m
    pub fn format_status<T: Serialize>(&self, status: &T, title: &str) -> String {
        let result = serde_json::json!({
            "title": title,
            "status": status
        });

        serde_json::to_string_pretty(&result).unwrap_or_else(|_| title.to_string())
    }

  ./crates/ccswarm/src/cli/output.rs:77-86 method format_list <-> ./crates/ccswarm/src/cli/output.rs:121-131 method format_list
  Similarity: 75.22%
  Classes: JsonFormatter <-> HumanFormatter

[36m--- ./crates/ccswarm/src/cli/output.rs:format_list (lines 77-86) ---[0m
    pub fn format_list<T: Serialize>(&self, items: &[T], title: &str) -> String {
        let result = serde_json::json!({
            "title": title,
            "count": items.len(),
            "items": items
        });

        serde_json::to_string_pretty(&result)
            .unwrap_or_else(|_| format!("{}: {} items", title, items.len()))
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:format_list (lines 121-131) ---[0m
    pub fn format_list<T: Serialize>(&self, items: &[T], title: &str) -> String {
        let mut output = format!("📋 {} ({} items):\n", title, items.len());

        for (i, item) in items.iter().enumerate() {
            if let Ok(json) = serde_json::to_value(item) {
                output.push_str(&format!("  {}. {}\n", i + 1, format_item(&json)));
            }
        }

        output
    }

  ./crates/ccswarm/src/cli/output.rs:67-74 method format_error <-> ./crates/ccswarm/src/cli/output.rs:89-96 method format_status
  Similarity: 96.62%
  Classes: JsonFormatter <-> JsonFormatter

[36m--- ./crates/ccswarm/src/cli/output.rs:format_error (lines 67-74) ---[0m
    pub fn format_error(&self, error: &str) -> String {
        let result = serde_json::json!({
            "status": "error",
            "error": error
        });

        serde_json::to_string_pretty(&result).unwrap_or_else(|_| error.to_string())
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:format_status (lines 89-96) ---[0m
    pub fn format_status<T: Serialize>(&self, status: &T, title: &str) -> String {
        let result = serde_json::json!({
            "title": title,
            "status": status
        });

        serde_json::to_string_pretty(&result).unwrap_or_else(|_| title.to_string())
    }

  ./crates/ccswarm/src/cli/output.rs:103-113 method format_success <-> ./crates/ccswarm/src/cli/output.rs:166-172 function create_formatter
  Similarity: 85.48%

[36m--- ./crates/ccswarm/src/cli/output.rs:format_success (lines 103-113) ---[0m
    pub fn format_success(&self, message: &str, data: Option<Value>) -> String {
        if let Some(data) = data {
            format!(
                "✅ {}\n\n{}",
                message,
                serde_json::to_string_pretty(&data).unwrap_or_default()
            )
        } else {
            format!("✅ {}", message)
        }
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:create_formatter (lines 166-172) ---[0m
pub fn create_formatter(json: bool) -> OutputFormatter {
    if json {
        OutputFormatter::Json(JsonFormatter)
    } else {
        OutputFormatter::Human(HumanFormatter)
    }
}

  ./crates/ccswarm/src/cli/output.rs:67-74 method format_error <-> ./crates/ccswarm/src/cli/output.rs:121-131 method format_list
  Similarity: 75.49%
  Classes: JsonFormatter <-> HumanFormatter

[36m--- ./crates/ccswarm/src/cli/output.rs:format_error (lines 67-74) ---[0m
    pub fn format_error(&self, error: &str) -> String {
        let result = serde_json::json!({
            "status": "error",
            "error": error
        });

        serde_json::to_string_pretty(&result).unwrap_or_else(|_| error.to_string())
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:format_list (lines 121-131) ---[0m
    pub fn format_list<T: Serialize>(&self, items: &[T], title: &str) -> String {
        let mut output = format!("📋 {} ({} items):\n", title, items.len());

        for (i, item) in items.iter().enumerate() {
            if let Ok(json) = serde_json::to_value(item) {
                output.push_str(&format!("  {}. {}\n", i + 1, format_item(&json)));
            }
        }

        output
    }

  ./crates/ccswarm/src/cli/output.rs:89-96 method format_status <-> ./crates/ccswarm/src/cli/output.rs:121-131 method format_list
  Similarity: 75.49%
  Classes: JsonFormatter <-> HumanFormatter

[36m--- ./crates/ccswarm/src/cli/output.rs:format_status (lines 89-96) ---[0m
    pub fn format_status<T: Serialize>(&self, status: &T, title: &str) -> String {
        let result = serde_json::json!({
            "title": title,
            "status": status
        });

        serde_json::to_string_pretty(&result).unwrap_or_else(|_| title.to_string())
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:format_list (lines 121-131) ---[0m
    pub fn format_list<T: Serialize>(&self, items: &[T], title: &str) -> String {
        let mut output = format!("📋 {} ({} items):\n", title, items.len());

        for (i, item) in items.iter().enumerate() {
            if let Ok(json) = serde_json::to_value(item) {
                output.push_str(&format!("  {}. {}\n", i + 1, format_item(&json)));
            }
        }

        output
    }

  ./crates/ccswarm/src/cli/output.rs:14-19 method format_success <-> ./crates/ccswarm/src/cli/output.rs:103-113 method format_success
  Similarity: 82.42%
  Classes: OutputFormatter <-> HumanFormatter

[36m--- ./crates/ccswarm/src/cli/output.rs:format_success (lines 14-19) ---[0m
    pub fn format_success(&self, message: &str, data: Option<Value>) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_success(message, data),
            OutputFormatter::Human(f) => f.format_success(message, data),
        }
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:format_success (lines 103-113) ---[0m
    pub fn format_success(&self, message: &str, data: Option<Value>) -> String {
        if let Some(data) = data {
            format!(
                "✅ {}\n\n{}",
                message,
                serde_json::to_string_pretty(&data).unwrap_or_default()
            )
        } else {
            format!("✅ {}", message)
        }
    }

  ./crates/ccswarm/src/cli/output.rs:23-28 method format_error <-> ./crates/ccswarm/src/cli/output.rs:103-113 method format_success
  Similarity: 82.42%
  Classes: OutputFormatter <-> HumanFormatter

[36m--- ./crates/ccswarm/src/cli/output.rs:format_error (lines 23-28) ---[0m
    pub fn format_error(&self, error: &str) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_error(error),
            OutputFormatter::Human(f) => f.format_error(error),
        }
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:format_success (lines 103-113) ---[0m
    pub fn format_success(&self, message: &str, data: Option<Value>) -> String {
        if let Some(data) = data {
            format!(
                "✅ {}\n\n{}",
                message,
                serde_json::to_string_pretty(&data).unwrap_or_default()
            )
        } else {
            format!("✅ {}", message)
        }
    }

  ./crates/ccswarm/src/cli/output.rs:32-37 method format_list <-> ./crates/ccswarm/src/cli/output.rs:103-113 method format_success
  Similarity: 82.42%
  Classes: OutputFormatter <-> HumanFormatter

[36m--- ./crates/ccswarm/src/cli/output.rs:format_list (lines 32-37) ---[0m
    pub fn format_list<T: Serialize>(&self, items: &[T], title: &str) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_list(items, title),
            OutputFormatter::Human(f) => f.format_list(items, title),
        }
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:format_success (lines 103-113) ---[0m
    pub fn format_success(&self, message: &str, data: Option<Value>) -> String {
        if let Some(data) = data {
            format!(
                "✅ {}\n\n{}",
                message,
                serde_json::to_string_pretty(&data).unwrap_or_default()
            )
        } else {
            format!("✅ {}", message)
        }
    }

  ./crates/ccswarm/src/cli/output.rs:41-46 method format_status <-> ./crates/ccswarm/src/cli/output.rs:103-113 method format_success
  Similarity: 82.42%
  Classes: OutputFormatter <-> HumanFormatter

[36m--- ./crates/ccswarm/src/cli/output.rs:format_status (lines 41-46) ---[0m
    pub fn format_status<T: Serialize>(&self, status: &T, title: &str) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_status(status, title),
            OutputFormatter::Human(f) => f.format_status(status, title),
        }
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:format_success (lines 103-113) ---[0m
    pub fn format_success(&self, message: &str, data: Option<Value>) -> String {
        if let Some(data) = data {
            format!(
                "✅ {}\n\n{}",
                message,
                serde_json::to_string_pretty(&data).unwrap_or_default()
            )
        } else {
            format!("✅ {}", message)
        }
    }

  ./crates/ccswarm/src/cli/output.rs:32-37 method format_list <-> ./crates/ccswarm/src/cli/output.rs:41-46 method format_status
  Similarity: 97.78%
  Classes: OutputFormatter <-> OutputFormatter

[36m--- ./crates/ccswarm/src/cli/output.rs:format_list (lines 32-37) ---[0m
    pub fn format_list<T: Serialize>(&self, items: &[T], title: &str) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_list(items, title),
            OutputFormatter::Human(f) => f.format_list(items, title),
        }
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:format_status (lines 41-46) ---[0m
    pub fn format_status<T: Serialize>(&self, status: &T, title: &str) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_status(status, title),
            OutputFormatter::Human(f) => f.format_status(status, title),
        }
    }

  ./crates/ccswarm/src/cli/output.rs:14-19 method format_success <-> ./crates/ccswarm/src/cli/output.rs:32-37 method format_list
  Similarity: 96.67%
  Classes: OutputFormatter <-> OutputFormatter

[36m--- ./crates/ccswarm/src/cli/output.rs:format_success (lines 14-19) ---[0m
    pub fn format_success(&self, message: &str, data: Option<Value>) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_success(message, data),
            OutputFormatter::Human(f) => f.format_success(message, data),
        }
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:format_list (lines 32-37) ---[0m
    pub fn format_list<T: Serialize>(&self, items: &[T], title: &str) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_list(items, title),
            OutputFormatter::Human(f) => f.format_list(items, title),
        }
    }

  ./crates/ccswarm/src/cli/output.rs:14-19 method format_success <-> ./crates/ccswarm/src/cli/output.rs:41-46 method format_status
  Similarity: 96.67%
  Classes: OutputFormatter <-> OutputFormatter

[36m--- ./crates/ccswarm/src/cli/output.rs:format_success (lines 14-19) ---[0m
    pub fn format_success(&self, message: &str, data: Option<Value>) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_success(message, data),
            OutputFormatter::Human(f) => f.format_success(message, data),
        }
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:format_status (lines 41-46) ---[0m
    pub fn format_status<T: Serialize>(&self, status: &T, title: &str) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_status(status, title),
            OutputFormatter::Human(f) => f.format_status(status, title),
        }
    }

  ./crates/ccswarm/src/cli/output.rs:14-19 method format_success <-> ./crates/ccswarm/src/cli/output.rs:23-28 method format_error
  Similarity: 90.37%
  Classes: OutputFormatter <-> OutputFormatter

[36m--- ./crates/ccswarm/src/cli/output.rs:format_success (lines 14-19) ---[0m
    pub fn format_success(&self, message: &str, data: Option<Value>) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_success(message, data),
            OutputFormatter::Human(f) => f.format_success(message, data),
        }
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:format_error (lines 23-28) ---[0m
    pub fn format_error(&self, error: &str) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_error(error),
            OutputFormatter::Human(f) => f.format_error(error),
        }
    }

  ./crates/ccswarm/src/cli/output.rs:23-28 method format_error <-> ./crates/ccswarm/src/cli/output.rs:32-37 method format_list
  Similarity: 90.37%
  Classes: OutputFormatter <-> OutputFormatter

[36m--- ./crates/ccswarm/src/cli/output.rs:format_error (lines 23-28) ---[0m
    pub fn format_error(&self, error: &str) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_error(error),
            OutputFormatter::Human(f) => f.format_error(error),
        }
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:format_list (lines 32-37) ---[0m
    pub fn format_list<T: Serialize>(&self, items: &[T], title: &str) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_list(items, title),
            OutputFormatter::Human(f) => f.format_list(items, title),
        }
    }

  ./crates/ccswarm/src/cli/output.rs:23-28 method format_error <-> ./crates/ccswarm/src/cli/output.rs:41-46 method format_status
  Similarity: 90.37%
  Classes: OutputFormatter <-> OutputFormatter

[36m--- ./crates/ccswarm/src/cli/output.rs:format_error (lines 23-28) ---[0m
    pub fn format_error(&self, error: &str) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_error(error),
            OutputFormatter::Human(f) => f.format_error(error),
        }
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:format_status (lines 41-46) ---[0m
    pub fn format_status<T: Serialize>(&self, status: &T, title: &str) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_status(status, title),
            OutputFormatter::Human(f) => f.format_status(status, title),
        }
    }

  ./crates/ccswarm/src/cli/output.rs:14-19 method format_success <-> ./crates/ccswarm/src/cli/output.rs:166-172 function create_formatter
  Similarity: 81.11%

[36m--- ./crates/ccswarm/src/cli/output.rs:format_success (lines 14-19) ---[0m
    pub fn format_success(&self, message: &str, data: Option<Value>) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_success(message, data),
            OutputFormatter::Human(f) => f.format_success(message, data),
        }
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:create_formatter (lines 166-172) ---[0m
pub fn create_formatter(json: bool) -> OutputFormatter {
    if json {
        OutputFormatter::Json(JsonFormatter)
    } else {
        OutputFormatter::Human(HumanFormatter)
    }
}

  ./crates/ccswarm/src/cli/output.rs:32-37 method format_list <-> ./crates/ccswarm/src/cli/output.rs:166-172 function create_formatter
  Similarity: 81.11%

[36m--- ./crates/ccswarm/src/cli/output.rs:format_list (lines 32-37) ---[0m
    pub fn format_list<T: Serialize>(&self, items: &[T], title: &str) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_list(items, title),
            OutputFormatter::Human(f) => f.format_list(items, title),
        }
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:create_formatter (lines 166-172) ---[0m
pub fn create_formatter(json: bool) -> OutputFormatter {
    if json {
        OutputFormatter::Json(JsonFormatter)
    } else {
        OutputFormatter::Human(HumanFormatter)
    }
}

  ./crates/ccswarm/src/cli/output.rs:41-46 method format_status <-> ./crates/ccswarm/src/cli/output.rs:166-172 function create_formatter
  Similarity: 81.11%

[36m--- ./crates/ccswarm/src/cli/output.rs:format_status (lines 41-46) ---[0m
    pub fn format_status<T: Serialize>(&self, status: &T, title: &str) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_status(status, title),
            OutputFormatter::Human(f) => f.format_status(status, title),
        }
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:create_formatter (lines 166-172) ---[0m
pub fn create_formatter(json: bool) -> OutputFormatter {
    if json {
        OutputFormatter::Json(JsonFormatter)
    } else {
        OutputFormatter::Human(HumanFormatter)
    }
}

  ./crates/ccswarm/src/cli/output.rs:23-28 method format_error <-> ./crates/ccswarm/src/cli/output.rs:166-172 function create_formatter
  Similarity: 80.00%

[36m--- ./crates/ccswarm/src/cli/output.rs:format_error (lines 23-28) ---[0m
    pub fn format_error(&self, error: &str) -> String {
        match self {
            OutputFormatter::Json(f) => f.format_error(error),
            OutputFormatter::Human(f) => f.format_error(error),
        }
    }

[36m--- ./crates/ccswarm/src/cli/output.rs:create_formatter (lines 166-172) ---[0m
pub fn create_formatter(json: bool) -> OutputFormatter {
    if json {
        OutputFormatter::Json(JsonFormatter)
    } else {
        OutputFormatter::Human(HumanFormatter)
    }
}


Duplicates in ./crates/ccswarm/src/subagent/delegation.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/subagent/delegation.rs:152-173 method detect_frontend_work <-> ./crates/ccswarm/src/subagent/delegation.rs:198-218 method detect_devops_work
  Similarity: 96.03%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_frontend_work (lines 152-173) ---[0m
    fn detect_frontend_work(&self, request: &str) -> bool {
        let frontend_keywords = [
            "ui",
            "frontend",
            "react",
            "vue",
            "component",
            "style",
            "css",
            "layout",
            "responsive",
            "design",
            "interface",
            "form",
            "button",
        ];

        let request_lower = request.to_lowercase();
        frontend_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_devops_work (lines 198-218) ---[0m
    fn detect_devops_work(&self, request: &str) -> bool {
        let devops_keywords = [
            "docker",
            "kubernetes",
            "deploy",
            "ci/cd",
            "pipeline",
            "container",
            "infrastructure",
            "aws",
            "azure",
            "gcp",
            "terraform",
            "ansible",
        ];

        let request_lower = request.to_lowercase();
        devops_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

  ./crates/ccswarm/src/subagent/delegation.rs:152-173 method detect_frontend_work <-> ./crates/ccswarm/src/subagent/delegation.rs:176-195 method detect_backend_work
  Similarity: 96.03%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_frontend_work (lines 152-173) ---[0m
    fn detect_frontend_work(&self, request: &str) -> bool {
        let frontend_keywords = [
            "ui",
            "frontend",
            "react",
            "vue",
            "component",
            "style",
            "css",
            "layout",
            "responsive",
            "design",
            "interface",
            "form",
            "button",
        ];

        let request_lower = request.to_lowercase();
        frontend_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_backend_work (lines 176-195) ---[0m
    fn detect_backend_work(&self, request: &str) -> bool {
        let backend_keywords = [
            "api",
            "backend",
            "database",
            "server",
            "endpoint",
            "rest",
            "graphql",
            "authentication",
            "authorization",
            "model",
            "schema",
        ];

        let request_lower = request.to_lowercase();
        backend_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

  ./crates/ccswarm/src/subagent/delegation.rs:152-173 method detect_frontend_work <-> ./crates/ccswarm/src/subagent/delegation.rs:221-240 method detect_qa_work
  Similarity: 96.03%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_frontend_work (lines 152-173) ---[0m
    fn detect_frontend_work(&self, request: &str) -> bool {
        let frontend_keywords = [
            "ui",
            "frontend",
            "react",
            "vue",
            "component",
            "style",
            "css",
            "layout",
            "responsive",
            "design",
            "interface",
            "form",
            "button",
        ];

        let request_lower = request.to_lowercase();
        frontend_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_qa_work (lines 221-240) ---[0m
    fn detect_qa_work(&self, request: &str) -> bool {
        let qa_keywords = [
            "test",
            "testing",
            "qa",
            "quality",
            "coverage",
            "unit test",
            "integration test",
            "e2e",
            "bug",
            "validation",
            "verification",
        ];

        let request_lower = request.to_lowercase();
        qa_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

  ./crates/ccswarm/src/subagent/delegation.rs:152-173 method detect_frontend_work <-> ./crates/ccswarm/src/subagent/delegation.rs:243-261 method detect_semantic_analysis
  Similarity: 96.03%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_frontend_work (lines 152-173) ---[0m
    fn detect_frontend_work(&self, request: &str) -> bool {
        let frontend_keywords = [
            "ui",
            "frontend",
            "react",
            "vue",
            "component",
            "style",
            "css",
            "layout",
            "responsive",
            "design",
            "interface",
            "form",
            "button",
        ];

        let request_lower = request.to_lowercase();
        frontend_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_semantic_analysis (lines 243-261) ---[0m
    fn detect_semantic_analysis(&self, request: &str) -> bool {
        let semantic_keywords = [
            "analyze",
            "understand",
            "refactor",
            "optimize",
            "pattern",
            "architecture",
            "structure",
            "dependency",
            "complexity",
            "metrics",
        ];

        let request_lower = request.to_lowercase();
        semantic_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

  ./crates/ccswarm/src/subagent/delegation.rs:176-195 method detect_backend_work <-> ./crates/ccswarm/src/subagent/delegation.rs:198-218 method detect_devops_work
  Similarity: 95.86%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_backend_work (lines 176-195) ---[0m
    fn detect_backend_work(&self, request: &str) -> bool {
        let backend_keywords = [
            "api",
            "backend",
            "database",
            "server",
            "endpoint",
            "rest",
            "graphql",
            "authentication",
            "authorization",
            "model",
            "schema",
        ];

        let request_lower = request.to_lowercase();
        backend_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_devops_work (lines 198-218) ---[0m
    fn detect_devops_work(&self, request: &str) -> bool {
        let devops_keywords = [
            "docker",
            "kubernetes",
            "deploy",
            "ci/cd",
            "pipeline",
            "container",
            "infrastructure",
            "aws",
            "azure",
            "gcp",
            "terraform",
            "ansible",
        ];

        let request_lower = request.to_lowercase();
        devops_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

  ./crates/ccswarm/src/subagent/delegation.rs:198-218 method detect_devops_work <-> ./crates/ccswarm/src/subagent/delegation.rs:221-240 method detect_qa_work
  Similarity: 95.86%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_devops_work (lines 198-218) ---[0m
    fn detect_devops_work(&self, request: &str) -> bool {
        let devops_keywords = [
            "docker",
            "kubernetes",
            "deploy",
            "ci/cd",
            "pipeline",
            "container",
            "infrastructure",
            "aws",
            "azure",
            "gcp",
            "terraform",
            "ansible",
        ];

        let request_lower = request.to_lowercase();
        devops_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_qa_work (lines 221-240) ---[0m
    fn detect_qa_work(&self, request: &str) -> bool {
        let qa_keywords = [
            "test",
            "testing",
            "qa",
            "quality",
            "coverage",
            "unit test",
            "integration test",
            "e2e",
            "bug",
            "validation",
            "verification",
        ];

        let request_lower = request.to_lowercase();
        qa_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

  ./crates/ccswarm/src/subagent/delegation.rs:176-195 method detect_backend_work <-> ./crates/ccswarm/src/subagent/delegation.rs:221-240 method detect_qa_work
  Similarity: 96.32%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_backend_work (lines 176-195) ---[0m
    fn detect_backend_work(&self, request: &str) -> bool {
        let backend_keywords = [
            "api",
            "backend",
            "database",
            "server",
            "endpoint",
            "rest",
            "graphql",
            "authentication",
            "authorization",
            "model",
            "schema",
        ];

        let request_lower = request.to_lowercase();
        backend_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_qa_work (lines 221-240) ---[0m
    fn detect_qa_work(&self, request: &str) -> bool {
        let qa_keywords = [
            "test",
            "testing",
            "qa",
            "quality",
            "coverage",
            "unit test",
            "integration test",
            "e2e",
            "bug",
            "validation",
            "verification",
        ];

        let request_lower = request.to_lowercase();
        qa_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

  ./crates/ccswarm/src/subagent/delegation.rs:198-218 method detect_devops_work <-> ./crates/ccswarm/src/subagent/delegation.rs:243-261 method detect_semantic_analysis
  Similarity: 95.86%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_devops_work (lines 198-218) ---[0m
    fn detect_devops_work(&self, request: &str) -> bool {
        let devops_keywords = [
            "docker",
            "kubernetes",
            "deploy",
            "ci/cd",
            "pipeline",
            "container",
            "infrastructure",
            "aws",
            "azure",
            "gcp",
            "terraform",
            "ansible",
        ];

        let request_lower = request.to_lowercase();
        devops_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_semantic_analysis (lines 243-261) ---[0m
    fn detect_semantic_analysis(&self, request: &str) -> bool {
        let semantic_keywords = [
            "analyze",
            "understand",
            "refactor",
            "optimize",
            "pattern",
            "architecture",
            "structure",
            "dependency",
            "complexity",
            "metrics",
        ];

        let request_lower = request.to_lowercase();
        semantic_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

  ./crates/ccswarm/src/subagent/delegation.rs:88-109 method create_task <-> ./crates/ccswarm/src/subagent/delegation.rs:152-173 method detect_frontend_work
  Similarity: 86.12%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_task (lines 88-109) ---[0m
    pub fn create_task(
        &mut self,
        description: String,
        prompt: String,
        subagent_type: String,
        priority: TaskPriority,
    ) -> DelegatedTask {
        let task_id = format!("task-{}", uuid::Uuid::new_v4());

        let task = DelegatedTask {
            id: task_id.clone(),
            description,
            prompt,
            subagent_type,
            context: HashMap::new(),
            priority,
            expected_outputs: Vec::new(),
        };

        self.active_tasks.insert(task_id, task.clone());
        task
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_frontend_work (lines 152-173) ---[0m
    fn detect_frontend_work(&self, request: &str) -> bool {
        let frontend_keywords = [
            "ui",
            "frontend",
            "react",
            "vue",
            "component",
            "style",
            "css",
            "layout",
            "responsive",
            "design",
            "interface",
            "form",
            "button",
        ];

        let request_lower = request.to_lowercase();
        frontend_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

  ./crates/ccswarm/src/subagent/delegation.rs:264-282 method create_frontend_task <-> ./crates/ccswarm/src/subagent/delegation.rs:285-303 method create_backend_task
  Similarity: 98.52%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_frontend_task (lines 264-282) ---[0m
    fn create_frontend_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-frontend-{}", uuid::Uuid::new_v4()),
            description: "Frontend implementation".to_string(),
            prompt: format!(
                "As a frontend specialist, implement the following:\n\n{}\n\n\
                Focus on UI/UX, component architecture, and responsive design.",
                request
            ),
            subagent_type: "frontend-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::High,
            expected_outputs: vec![
                "Component implementation".to_string(),
                "Style definitions".to_string(),
                "State management".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_backend_task (lines 285-303) ---[0m
    fn create_backend_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-backend-{}", uuid::Uuid::new_v4()),
            description: "Backend development".to_string(),
            prompt: format!(
                "As a backend specialist, implement the following:\n\n{}\n\n\
                Focus on API design, database optimization, and business logic.",
                request
            ),
            subagent_type: "backend-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::High,
            expected_outputs: vec![
                "API endpoints".to_string(),
                "Database schemas".to_string(),
                "Business logic".to_string(),
            ],
        }
    }

  ./crates/ccswarm/src/subagent/delegation.rs:264-282 method create_frontend_task <-> ./crates/ccswarm/src/subagent/delegation.rs:348-366 method create_semantic_task
  Similarity: 98.52%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_frontend_task (lines 264-282) ---[0m
    fn create_frontend_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-frontend-{}", uuid::Uuid::new_v4()),
            description: "Frontend implementation".to_string(),
            prompt: format!(
                "As a frontend specialist, implement the following:\n\n{}\n\n\
                Focus on UI/UX, component architecture, and responsive design.",
                request
            ),
            subagent_type: "frontend-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::High,
            expected_outputs: vec![
                "Component implementation".to_string(),
                "Style definitions".to_string(),
                "State management".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_semantic_task (lines 348-366) ---[0m
    fn create_semantic_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-semantic-{}", uuid::Uuid::new_v4()),
            description: "Semantic analysis".to_string(),
            prompt: format!(
                "Perform semantic analysis:\n\n{}\n\n\
                Use semantic tools to understand code structure and patterns.",
                request
            ),
            subagent_type: "semantic-analyst".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::High,
            expected_outputs: vec![
                "Code analysis".to_string(),
                "Pattern identification".to_string(),
                "Improvement suggestions".to_string(),
            ],
        }
    }

  ./crates/ccswarm/src/subagent/delegation.rs:285-303 method create_backend_task <-> ./crates/ccswarm/src/subagent/delegation.rs:348-366 method create_semantic_task
  Similarity: 98.52%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_backend_task (lines 285-303) ---[0m
    fn create_backend_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-backend-{}", uuid::Uuid::new_v4()),
            description: "Backend development".to_string(),
            prompt: format!(
                "As a backend specialist, implement the following:\n\n{}\n\n\
                Focus on API design, database optimization, and business logic.",
                request
            ),
            subagent_type: "backend-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::High,
            expected_outputs: vec![
                "API endpoints".to_string(),
                "Database schemas".to_string(),
                "Business logic".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_semantic_task (lines 348-366) ---[0m
    fn create_semantic_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-semantic-{}", uuid::Uuid::new_v4()),
            description: "Semantic analysis".to_string(),
            prompt: format!(
                "Perform semantic analysis:\n\n{}\n\n\
                Use semantic tools to understand code structure and patterns.",
                request
            ),
            subagent_type: "semantic-analyst".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::High,
            expected_outputs: vec![
                "Code analysis".to_string(),
                "Pattern identification".to_string(),
                "Improvement suggestions".to_string(),
            ],
        }
    }

  ./crates/ccswarm/src/subagent/delegation.rs:306-324 method create_devops_task <-> ./crates/ccswarm/src/subagent/delegation.rs:327-345 method create_qa_task
  Similarity: 98.52%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_devops_task (lines 306-324) ---[0m
    fn create_devops_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-devops-{}", uuid::Uuid::new_v4()),
            description: "DevOps configuration".to_string(),
            prompt: format!(
                "As a DevOps specialist, implement the following:\n\n{}\n\n\
                Focus on containerization, CI/CD, and infrastructure.",
                request
            ),
            subagent_type: "devops-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::Medium,
            expected_outputs: vec![
                "Docker configuration".to_string(),
                "CI/CD pipeline".to_string(),
                "Deployment scripts".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_qa_task (lines 327-345) ---[0m
    fn create_qa_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-qa-{}", uuid::Uuid::new_v4()),
            description: "Quality assurance".to_string(),
            prompt: format!(
                "As a QA specialist, implement the following:\n\n{}\n\n\
                Focus on comprehensive testing and quality metrics.",
                request
            ),
            subagent_type: "qa-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::Medium,
            expected_outputs: vec![
                "Test suites".to_string(),
                "Coverage reports".to_string(),
                "Quality metrics".to_string(),
            ],
        }
    }

  ./crates/ccswarm/src/subagent/delegation.rs:264-282 method create_frontend_task <-> ./crates/ccswarm/src/subagent/delegation.rs:306-324 method create_devops_task
  Similarity: 98.31%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_frontend_task (lines 264-282) ---[0m
    fn create_frontend_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-frontend-{}", uuid::Uuid::new_v4()),
            description: "Frontend implementation".to_string(),
            prompt: format!(
                "As a frontend specialist, implement the following:\n\n{}\n\n\
                Focus on UI/UX, component architecture, and responsive design.",
                request
            ),
            subagent_type: "frontend-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::High,
            expected_outputs: vec![
                "Component implementation".to_string(),
                "Style definitions".to_string(),
                "State management".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_devops_task (lines 306-324) ---[0m
    fn create_devops_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-devops-{}", uuid::Uuid::new_v4()),
            description: "DevOps configuration".to_string(),
            prompt: format!(
                "As a DevOps specialist, implement the following:\n\n{}\n\n\
                Focus on containerization, CI/CD, and infrastructure.",
                request
            ),
            subagent_type: "devops-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::Medium,
            expected_outputs: vec![
                "Docker configuration".to_string(),
                "CI/CD pipeline".to_string(),
                "Deployment scripts".to_string(),
            ],
        }
    }

  ./crates/ccswarm/src/subagent/delegation.rs:264-282 method create_frontend_task <-> ./crates/ccswarm/src/subagent/delegation.rs:327-345 method create_qa_task
  Similarity: 98.31%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_frontend_task (lines 264-282) ---[0m
    fn create_frontend_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-frontend-{}", uuid::Uuid::new_v4()),
            description: "Frontend implementation".to_string(),
            prompt: format!(
                "As a frontend specialist, implement the following:\n\n{}\n\n\
                Focus on UI/UX, component architecture, and responsive design.",
                request
            ),
            subagent_type: "frontend-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::High,
            expected_outputs: vec![
                "Component implementation".to_string(),
                "Style definitions".to_string(),
                "State management".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_qa_task (lines 327-345) ---[0m
    fn create_qa_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-qa-{}", uuid::Uuid::new_v4()),
            description: "Quality assurance".to_string(),
            prompt: format!(
                "As a QA specialist, implement the following:\n\n{}\n\n\
                Focus on comprehensive testing and quality metrics.",
                request
            ),
            subagent_type: "qa-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::Medium,
            expected_outputs: vec![
                "Test suites".to_string(),
                "Coverage reports".to_string(),
                "Quality metrics".to_string(),
            ],
        }
    }

  ./crates/ccswarm/src/subagent/delegation.rs:285-303 method create_backend_task <-> ./crates/ccswarm/src/subagent/delegation.rs:306-324 method create_devops_task
  Similarity: 98.31%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_backend_task (lines 285-303) ---[0m
    fn create_backend_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-backend-{}", uuid::Uuid::new_v4()),
            description: "Backend development".to_string(),
            prompt: format!(
                "As a backend specialist, implement the following:\n\n{}\n\n\
                Focus on API design, database optimization, and business logic.",
                request
            ),
            subagent_type: "backend-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::High,
            expected_outputs: vec![
                "API endpoints".to_string(),
                "Database schemas".to_string(),
                "Business logic".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_devops_task (lines 306-324) ---[0m
    fn create_devops_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-devops-{}", uuid::Uuid::new_v4()),
            description: "DevOps configuration".to_string(),
            prompt: format!(
                "As a DevOps specialist, implement the following:\n\n{}\n\n\
                Focus on containerization, CI/CD, and infrastructure.",
                request
            ),
            subagent_type: "devops-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::Medium,
            expected_outputs: vec![
                "Docker configuration".to_string(),
                "CI/CD pipeline".to_string(),
                "Deployment scripts".to_string(),
            ],
        }
    }

  ./crates/ccswarm/src/subagent/delegation.rs:285-303 method create_backend_task <-> ./crates/ccswarm/src/subagent/delegation.rs:327-345 method create_qa_task
  Similarity: 98.31%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_backend_task (lines 285-303) ---[0m
    fn create_backend_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-backend-{}", uuid::Uuid::new_v4()),
            description: "Backend development".to_string(),
            prompt: format!(
                "As a backend specialist, implement the following:\n\n{}\n\n\
                Focus on API design, database optimization, and business logic.",
                request
            ),
            subagent_type: "backend-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::High,
            expected_outputs: vec![
                "API endpoints".to_string(),
                "Database schemas".to_string(),
                "Business logic".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_qa_task (lines 327-345) ---[0m
    fn create_qa_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-qa-{}", uuid::Uuid::new_v4()),
            description: "Quality assurance".to_string(),
            prompt: format!(
                "As a QA specialist, implement the following:\n\n{}\n\n\
                Focus on comprehensive testing and quality metrics.",
                request
            ),
            subagent_type: "qa-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::Medium,
            expected_outputs: vec![
                "Test suites".to_string(),
                "Coverage reports".to_string(),
                "Quality metrics".to_string(),
            ],
        }
    }

  ./crates/ccswarm/src/subagent/delegation.rs:306-324 method create_devops_task <-> ./crates/ccswarm/src/subagent/delegation.rs:348-366 method create_semantic_task
  Similarity: 98.31%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_devops_task (lines 306-324) ---[0m
    fn create_devops_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-devops-{}", uuid::Uuid::new_v4()),
            description: "DevOps configuration".to_string(),
            prompt: format!(
                "As a DevOps specialist, implement the following:\n\n{}\n\n\
                Focus on containerization, CI/CD, and infrastructure.",
                request
            ),
            subagent_type: "devops-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::Medium,
            expected_outputs: vec![
                "Docker configuration".to_string(),
                "CI/CD pipeline".to_string(),
                "Deployment scripts".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_semantic_task (lines 348-366) ---[0m
    fn create_semantic_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-semantic-{}", uuid::Uuid::new_v4()),
            description: "Semantic analysis".to_string(),
            prompt: format!(
                "Perform semantic analysis:\n\n{}\n\n\
                Use semantic tools to understand code structure and patterns.",
                request
            ),
            subagent_type: "semantic-analyst".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::High,
            expected_outputs: vec![
                "Code analysis".to_string(),
                "Pattern identification".to_string(),
                "Improvement suggestions".to_string(),
            ],
        }
    }

  ./crates/ccswarm/src/subagent/delegation.rs:327-345 method create_qa_task <-> ./crates/ccswarm/src/subagent/delegation.rs:348-366 method create_semantic_task
  Similarity: 98.31%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_qa_task (lines 327-345) ---[0m
    fn create_qa_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-qa-{}", uuid::Uuid::new_v4()),
            description: "Quality assurance".to_string(),
            prompt: format!(
                "As a QA specialist, implement the following:\n\n{}\n\n\
                Focus on comprehensive testing and quality metrics.",
                request
            ),
            subagent_type: "qa-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::Medium,
            expected_outputs: vec![
                "Test suites".to_string(),
                "Coverage reports".to_string(),
                "Quality metrics".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_semantic_task (lines 348-366) ---[0m
    fn create_semantic_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-semantic-{}", uuid::Uuid::new_v4()),
            description: "Semantic analysis".to_string(),
            prompt: format!(
                "Perform semantic analysis:\n\n{}\n\n\
                Use semantic tools to understand code structure and patterns.",
                request
            ),
            subagent_type: "semantic-analyst".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::High,
            expected_outputs: vec![
                "Code analysis".to_string(),
                "Pattern identification".to_string(),
                "Improvement suggestions".to_string(),
            ],
        }
    }

  ./crates/ccswarm/src/subagent/delegation.rs:176-195 method detect_backend_work <-> ./crates/ccswarm/src/subagent/delegation.rs:243-261 method detect_semantic_analysis
  Similarity: 95.66%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_backend_work (lines 176-195) ---[0m
    fn detect_backend_work(&self, request: &str) -> bool {
        let backend_keywords = [
            "api",
            "backend",
            "database",
            "server",
            "endpoint",
            "rest",
            "graphql",
            "authentication",
            "authorization",
            "model",
            "schema",
        ];

        let request_lower = request.to_lowercase();
        backend_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_semantic_analysis (lines 243-261) ---[0m
    fn detect_semantic_analysis(&self, request: &str) -> bool {
        let semantic_keywords = [
            "analyze",
            "understand",
            "refactor",
            "optimize",
            "pattern",
            "architecture",
            "structure",
            "dependency",
            "complexity",
            "metrics",
        ];

        let request_lower = request.to_lowercase();
        semantic_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

  ./crates/ccswarm/src/subagent/delegation.rs:221-240 method detect_qa_work <-> ./crates/ccswarm/src/subagent/delegation.rs:243-261 method detect_semantic_analysis
  Similarity: 95.66%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_qa_work (lines 221-240) ---[0m
    fn detect_qa_work(&self, request: &str) -> bool {
        let qa_keywords = [
            "test",
            "testing",
            "qa",
            "quality",
            "coverage",
            "unit test",
            "integration test",
            "e2e",
            "bug",
            "validation",
            "verification",
        ];

        let request_lower = request.to_lowercase();
        qa_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_semantic_analysis (lines 243-261) ---[0m
    fn detect_semantic_analysis(&self, request: &str) -> bool {
        let semantic_keywords = [
            "analyze",
            "understand",
            "refactor",
            "optimize",
            "pattern",
            "architecture",
            "structure",
            "dependency",
            "complexity",
            "metrics",
        ];

        let request_lower = request.to_lowercase();
        semantic_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

  ./crates/ccswarm/src/subagent/delegation.rs:88-109 method create_task <-> ./crates/ccswarm/src/subagent/delegation.rs:198-218 method detect_devops_work
  Similarity: 85.50%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_task (lines 88-109) ---[0m
    pub fn create_task(
        &mut self,
        description: String,
        prompt: String,
        subagent_type: String,
        priority: TaskPriority,
    ) -> DelegatedTask {
        let task_id = format!("task-{}", uuid::Uuid::new_v4());

        let task = DelegatedTask {
            id: task_id.clone(),
            description,
            prompt,
            subagent_type,
            context: HashMap::new(),
            priority,
            expected_outputs: Vec::new(),
        };

        self.active_tasks.insert(task_id, task.clone());
        task
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_devops_work (lines 198-218) ---[0m
    fn detect_devops_work(&self, request: &str) -> bool {
        let devops_keywords = [
            "docker",
            "kubernetes",
            "deploy",
            "ci/cd",
            "pipeline",
            "container",
            "infrastructure",
            "aws",
            "azure",
            "gcp",
            "terraform",
            "ansible",
        ];

        let request_lower = request.to_lowercase();
        devops_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

  ./crates/ccswarm/src/subagent/delegation.rs:88-109 method create_task <-> ./crates/ccswarm/src/subagent/delegation.rs:176-195 method detect_backend_work
  Similarity: 84.95%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_task (lines 88-109) ---[0m
    pub fn create_task(
        &mut self,
        description: String,
        prompt: String,
        subagent_type: String,
        priority: TaskPriority,
    ) -> DelegatedTask {
        let task_id = format!("task-{}", uuid::Uuid::new_v4());

        let task = DelegatedTask {
            id: task_id.clone(),
            description,
            prompt,
            subagent_type,
            context: HashMap::new(),
            priority,
            expected_outputs: Vec::new(),
        };

        self.active_tasks.insert(task_id, task.clone());
        task
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_backend_work (lines 176-195) ---[0m
    fn detect_backend_work(&self, request: &str) -> bool {
        let backend_keywords = [
            "api",
            "backend",
            "database",
            "server",
            "endpoint",
            "rest",
            "graphql",
            "authentication",
            "authorization",
            "model",
            "schema",
        ];

        let request_lower = request.to_lowercase();
        backend_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

  ./crates/ccswarm/src/subagent/delegation.rs:88-109 method create_task <-> ./crates/ccswarm/src/subagent/delegation.rs:221-240 method detect_qa_work
  Similarity: 84.95%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_task (lines 88-109) ---[0m
    pub fn create_task(
        &mut self,
        description: String,
        prompt: String,
        subagent_type: String,
        priority: TaskPriority,
    ) -> DelegatedTask {
        let task_id = format!("task-{}", uuid::Uuid::new_v4());

        let task = DelegatedTask {
            id: task_id.clone(),
            description,
            prompt,
            subagent_type,
            context: HashMap::new(),
            priority,
            expected_outputs: Vec::new(),
        };

        self.active_tasks.insert(task_id, task.clone());
        task
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_qa_work (lines 221-240) ---[0m
    fn detect_qa_work(&self, request: &str) -> bool {
        let qa_keywords = [
            "test",
            "testing",
            "qa",
            "quality",
            "coverage",
            "unit test",
            "integration test",
            "e2e",
            "bug",
            "validation",
            "verification",
        ];

        let request_lower = request.to_lowercase();
        qa_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

  ./crates/ccswarm/src/subagent/delegation.rs:88-109 method create_task <-> ./crates/ccswarm/src/subagent/delegation.rs:243-261 method detect_semantic_analysis
  Similarity: 84.95%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_task (lines 88-109) ---[0m
    pub fn create_task(
        &mut self,
        description: String,
        prompt: String,
        subagent_type: String,
        priority: TaskPriority,
    ) -> DelegatedTask {
        let task_id = format!("task-{}", uuid::Uuid::new_v4());

        let task = DelegatedTask {
            id: task_id.clone(),
            description,
            prompt,
            subagent_type,
            context: HashMap::new(),
            priority,
            expected_outputs: Vec::new(),
        };

        self.active_tasks.insert(task_id, task.clone());
        task
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_semantic_analysis (lines 243-261) ---[0m
    fn detect_semantic_analysis(&self, request: &str) -> bool {
        let semantic_keywords = [
            "analyze",
            "understand",
            "refactor",
            "optimize",
            "pattern",
            "architecture",
            "structure",
            "dependency",
            "complexity",
            "metrics",
        ];

        let request_lower = request.to_lowercase();
        semantic_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

  ./crates/ccswarm/src/subagent/delegation.rs:306-324 method create_devops_task <-> ./crates/ccswarm/src/subagent/delegation.rs:369-379 method create_general_task
  Similarity: 93.73%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_devops_task (lines 306-324) ---[0m
    fn create_devops_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-devops-{}", uuid::Uuid::new_v4()),
            description: "DevOps configuration".to_string(),
            prompt: format!(
                "As a DevOps specialist, implement the following:\n\n{}\n\n\
                Focus on containerization, CI/CD, and infrastructure.",
                request
            ),
            subagent_type: "devops-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::Medium,
            expected_outputs: vec![
                "Docker configuration".to_string(),
                "CI/CD pipeline".to_string(),
                "Deployment scripts".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_general_task (lines 369-379) ---[0m
    fn create_general_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-general-{}", uuid::Uuid::new_v4()),
            description: "General task".to_string(),
            prompt: request.to_string(),
            subagent_type: "general-purpose".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::Medium,
            expected_outputs: vec!["Task completion".to_string()],
        }
    }

  ./crates/ccswarm/src/subagent/delegation.rs:327-345 method create_qa_task <-> ./crates/ccswarm/src/subagent/delegation.rs:369-379 method create_general_task
  Similarity: 93.73%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_qa_task (lines 327-345) ---[0m
    fn create_qa_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-qa-{}", uuid::Uuid::new_v4()),
            description: "Quality assurance".to_string(),
            prompt: format!(
                "As a QA specialist, implement the following:\n\n{}\n\n\
                Focus on comprehensive testing and quality metrics.",
                request
            ),
            subagent_type: "qa-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::Medium,
            expected_outputs: vec![
                "Test suites".to_string(),
                "Coverage reports".to_string(),
                "Quality metrics".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_general_task (lines 369-379) ---[0m
    fn create_general_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-general-{}", uuid::Uuid::new_v4()),
            description: "General task".to_string(),
            prompt: request.to_string(),
            subagent_type: "general-purpose".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::Medium,
            expected_outputs: vec!["Task completion".to_string()],
        }
    }

  ./crates/ccswarm/src/subagent/delegation.rs:264-282 method create_frontend_task <-> ./crates/ccswarm/src/subagent/delegation.rs:369-379 method create_general_task
  Similarity: 93.52%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_frontend_task (lines 264-282) ---[0m
    fn create_frontend_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-frontend-{}", uuid::Uuid::new_v4()),
            description: "Frontend implementation".to_string(),
            prompt: format!(
                "As a frontend specialist, implement the following:\n\n{}\n\n\
                Focus on UI/UX, component architecture, and responsive design.",
                request
            ),
            subagent_type: "frontend-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::High,
            expected_outputs: vec![
                "Component implementation".to_string(),
                "Style definitions".to_string(),
                "State management".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_general_task (lines 369-379) ---[0m
    fn create_general_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-general-{}", uuid::Uuid::new_v4()),
            description: "General task".to_string(),
            prompt: request.to_string(),
            subagent_type: "general-purpose".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::Medium,
            expected_outputs: vec!["Task completion".to_string()],
        }
    }

  ./crates/ccswarm/src/subagent/delegation.rs:285-303 method create_backend_task <-> ./crates/ccswarm/src/subagent/delegation.rs:369-379 method create_general_task
  Similarity: 93.52%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_backend_task (lines 285-303) ---[0m
    fn create_backend_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-backend-{}", uuid::Uuid::new_v4()),
            description: "Backend development".to_string(),
            prompt: format!(
                "As a backend specialist, implement the following:\n\n{}\n\n\
                Focus on API design, database optimization, and business logic.",
                request
            ),
            subagent_type: "backend-specialist".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::High,
            expected_outputs: vec![
                "API endpoints".to_string(),
                "Database schemas".to_string(),
                "Business logic".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_general_task (lines 369-379) ---[0m
    fn create_general_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-general-{}", uuid::Uuid::new_v4()),
            description: "General task".to_string(),
            prompt: request.to_string(),
            subagent_type: "general-purpose".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::Medium,
            expected_outputs: vec!["Task completion".to_string()],
        }
    }

  ./crates/ccswarm/src/subagent/delegation.rs:348-366 method create_semantic_task <-> ./crates/ccswarm/src/subagent/delegation.rs:369-379 method create_general_task
  Similarity: 93.52%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_semantic_task (lines 348-366) ---[0m
    fn create_semantic_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-semantic-{}", uuid::Uuid::new_v4()),
            description: "Semantic analysis".to_string(),
            prompt: format!(
                "Perform semantic analysis:\n\n{}\n\n\
                Use semantic tools to understand code structure and patterns.",
                request
            ),
            subagent_type: "semantic-analyst".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::High,
            expected_outputs: vec![
                "Code analysis".to_string(),
                "Pattern identification".to_string(),
                "Improvement suggestions".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_general_task (lines 369-379) ---[0m
    fn create_general_task(&self, request: &str) -> DelegatedTask {
        DelegatedTask {
            id: format!("task-general-{}", uuid::Uuid::new_v4()),
            description: "General task".to_string(),
            prompt: request.to_string(),
            subagent_type: "general-purpose".to_string(),
            context: HashMap::new(),
            priority: TaskPriority::Medium,
            expected_outputs: vec!["Task completion".to_string()],
        }
    }

  ./crates/ccswarm/src/subagent/delegation.rs:88-109 method create_task <-> ./crates/ccswarm/src/subagent/delegation.rs:428-444 function test_analyze_for_delegation
  Similarity: 70.87%

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_task (lines 88-109) ---[0m
    pub fn create_task(
        &mut self,
        description: String,
        prompt: String,
        subagent_type: String,
        priority: TaskPriority,
    ) -> DelegatedTask {
        let task_id = format!("task-{}", uuid::Uuid::new_v4());

        let task = DelegatedTask {
            id: task_id.clone(),
            description,
            prompt,
            subagent_type,
            context: HashMap::new(),
            priority,
            expected_outputs: Vec::new(),
        };

        self.active_tasks.insert(task_id, task.clone());
        task
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:test_analyze_for_delegation (lines 428-444) ---[0m
    fn test_analyze_for_delegation() {
        let manager = DelegationManager::new();

        let tasks = manager
            .analyze_for_delegation(
                "Create a full-stack application with React frontend and REST API backend",
            )
            .unwrap();

        assert!(tasks.len() >= 2);
        assert!(tasks
            .iter()
            .any(|t| t.subagent_type == "frontend-specialist"));
        assert!(tasks
            .iter()
            .any(|t| t.subagent_type == "backend-specialist"));
    }

  ./crates/ccswarm/src/subagent/delegation.rs:88-109 method create_task <-> ./crates/ccswarm/src/subagent/delegation.rs:410-416 function test_detect_frontend_work
  Similarity: 84.30%

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_task (lines 88-109) ---[0m
    pub fn create_task(
        &mut self,
        description: String,
        prompt: String,
        subagent_type: String,
        priority: TaskPriority,
    ) -> DelegatedTask {
        let task_id = format!("task-{}", uuid::Uuid::new_v4());

        let task = DelegatedTask {
            id: task_id.clone(),
            description,
            prompt,
            subagent_type,
            context: HashMap::new(),
            priority,
            expected_outputs: Vec::new(),
        };

        self.active_tasks.insert(task_id, task.clone());
        task
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:test_detect_frontend_work (lines 410-416) ---[0m
    fn test_detect_frontend_work() {
        let manager = DelegationManager::new();

        assert!(manager.detect_frontend_work("Create a React component"));
        assert!(manager.detect_frontend_work("Style the dashboard UI"));
        assert!(!manager.detect_frontend_work("Create database schema"));
    }

  ./crates/ccswarm/src/subagent/delegation.rs:88-109 method create_task <-> ./crates/ccswarm/src/subagent/delegation.rs:419-425 function test_detect_backend_work
  Similarity: 84.30%

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_task (lines 88-109) ---[0m
    pub fn create_task(
        &mut self,
        description: String,
        prompt: String,
        subagent_type: String,
        priority: TaskPriority,
    ) -> DelegatedTask {
        let task_id = format!("task-{}", uuid::Uuid::new_v4());

        let task = DelegatedTask {
            id: task_id.clone(),
            description,
            prompt,
            subagent_type,
            context: HashMap::new(),
            priority,
            expected_outputs: Vec::new(),
        };

        self.active_tasks.insert(task_id, task.clone());
        task
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:test_detect_backend_work (lines 419-425) ---[0m
    fn test_detect_backend_work() {
        let manager = DelegationManager::new();

        assert!(manager.detect_backend_work("Create REST API endpoints"));
        assert!(manager.detect_backend_work("Implement database models"));
        assert!(!manager.detect_backend_work("Style the button"));
    }

  ./crates/ccswarm/src/subagent/delegation.rs:152-173 method detect_frontend_work <-> ./crates/ccswarm/src/subagent/delegation.rs:392-396 method get_task_status
  Similarity: 87.16%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_frontend_work (lines 152-173) ---[0m
    fn detect_frontend_work(&self, request: &str) -> bool {
        let frontend_keywords = [
            "ui",
            "frontend",
            "react",
            "vue",
            "component",
            "style",
            "css",
            "layout",
            "responsive",
            "design",
            "interface",
            "form",
            "button",
        ];

        let request_lower = request.to_lowercase();
        frontend_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:get_task_status (lines 392-396) ---[0m
    pub fn get_task_status(&self) -> (Vec<&DelegatedTask>, Vec<&TaskResult>) {
        let active: Vec<&DelegatedTask> = self.active_tasks.values().collect();
        let completed: Vec<&TaskResult> = self.completed_tasks.values().collect();
        (active, completed)
    }

  ./crates/ccswarm/src/subagent/delegation.rs:88-109 method create_task <-> ./crates/ccswarm/src/subagent/delegation.rs:382-389 method complete_task
  Similarity: 76.92%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_task (lines 88-109) ---[0m
    pub fn create_task(
        &mut self,
        description: String,
        prompt: String,
        subagent_type: String,
        priority: TaskPriority,
    ) -> DelegatedTask {
        let task_id = format!("task-{}", uuid::Uuid::new_v4());

        let task = DelegatedTask {
            id: task_id.clone(),
            description,
            prompt,
            subagent_type,
            context: HashMap::new(),
            priority,
            expected_outputs: Vec::new(),
        };

        self.active_tasks.insert(task_id, task.clone());
        task
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:complete_task (lines 382-389) ---[0m
    pub fn complete_task(&mut self, task_id: &str, result: TaskResult) -> SubagentResult<()> {
        if self.active_tasks.remove(task_id).is_none() {
            return Err(SubagentError::NotFound(task_id.to_string()));
        }

        self.completed_tasks.insert(task_id.to_string(), result);
        Ok(())
    }

  ./crates/ccswarm/src/subagent/delegation.rs:88-109 method create_task <-> ./crates/ccswarm/src/subagent/delegation.rs:392-396 method get_task_status
  Similarity: 84.86%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:create_task (lines 88-109) ---[0m
    pub fn create_task(
        &mut self,
        description: String,
        prompt: String,
        subagent_type: String,
        priority: TaskPriority,
    ) -> DelegatedTask {
        let task_id = format!("task-{}", uuid::Uuid::new_v4());

        let task = DelegatedTask {
            id: task_id.clone(),
            description,
            prompt,
            subagent_type,
            context: HashMap::new(),
            priority,
            expected_outputs: Vec::new(),
        };

        self.active_tasks.insert(task_id, task.clone());
        task
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:get_task_status (lines 392-396) ---[0m
    pub fn get_task_status(&self) -> (Vec<&DelegatedTask>, Vec<&TaskResult>) {
        let active: Vec<&DelegatedTask> = self.active_tasks.values().collect();
        let completed: Vec<&TaskResult> = self.completed_tasks.values().collect();
        (active, completed)
    }

  ./crates/ccswarm/src/subagent/delegation.rs:198-218 method detect_devops_work <-> ./crates/ccswarm/src/subagent/delegation.rs:392-396 method get_task_status
  Similarity: 86.58%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_devops_work (lines 198-218) ---[0m
    fn detect_devops_work(&self, request: &str) -> bool {
        let devops_keywords = [
            "docker",
            "kubernetes",
            "deploy",
            "ci/cd",
            "pipeline",
            "container",
            "infrastructure",
            "aws",
            "azure",
            "gcp",
            "terraform",
            "ansible",
        ];

        let request_lower = request.to_lowercase();
        devops_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:get_task_status (lines 392-396) ---[0m
    pub fn get_task_status(&self) -> (Vec<&DelegatedTask>, Vec<&TaskResult>) {
        let active: Vec<&DelegatedTask> = self.active_tasks.values().collect();
        let completed: Vec<&TaskResult> = self.completed_tasks.values().collect();
        (active, completed)
    }

  ./crates/ccswarm/src/subagent/delegation.rs:152-173 method detect_frontend_work <-> ./crates/ccswarm/src/subagent/delegation.rs:410-416 function test_detect_frontend_work
  Similarity: 76.29%

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_frontend_work (lines 152-173) ---[0m
    fn detect_frontend_work(&self, request: &str) -> bool {
        let frontend_keywords = [
            "ui",
            "frontend",
            "react",
            "vue",
            "component",
            "style",
            "css",
            "layout",
            "responsive",
            "design",
            "interface",
            "form",
            "button",
        ];

        let request_lower = request.to_lowercase();
        frontend_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:test_detect_frontend_work (lines 410-416) ---[0m
    fn test_detect_frontend_work() {
        let manager = DelegationManager::new();

        assert!(manager.detect_frontend_work("Create a React component"));
        assert!(manager.detect_frontend_work("Style the dashboard UI"));
        assert!(!manager.detect_frontend_work("Create database schema"));
    }

  ./crates/ccswarm/src/subagent/delegation.rs:152-173 method detect_frontend_work <-> ./crates/ccswarm/src/subagent/delegation.rs:419-425 function test_detect_backend_work
  Similarity: 76.29%

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_frontend_work (lines 152-173) ---[0m
    fn detect_frontend_work(&self, request: &str) -> bool {
        let frontend_keywords = [
            "ui",
            "frontend",
            "react",
            "vue",
            "component",
            "style",
            "css",
            "layout",
            "responsive",
            "design",
            "interface",
            "form",
            "button",
        ];

        let request_lower = request.to_lowercase();
        frontend_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:test_detect_backend_work (lines 419-425) ---[0m
    fn test_detect_backend_work() {
        let manager = DelegationManager::new();

        assert!(manager.detect_backend_work("Create REST API endpoints"));
        assert!(manager.detect_backend_work("Implement database models"));
        assert!(!manager.detect_backend_work("Style the button"));
    }

  ./crates/ccswarm/src/subagent/delegation.rs:176-195 method detect_backend_work <-> ./crates/ccswarm/src/subagent/delegation.rs:392-396 method get_task_status
  Similarity: 85.94%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_backend_work (lines 176-195) ---[0m
    fn detect_backend_work(&self, request: &str) -> bool {
        let backend_keywords = [
            "api",
            "backend",
            "database",
            "server",
            "endpoint",
            "rest",
            "graphql",
            "authentication",
            "authorization",
            "model",
            "schema",
        ];

        let request_lower = request.to_lowercase();
        backend_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:get_task_status (lines 392-396) ---[0m
    pub fn get_task_status(&self) -> (Vec<&DelegatedTask>, Vec<&TaskResult>) {
        let active: Vec<&DelegatedTask> = self.active_tasks.values().collect();
        let completed: Vec<&TaskResult> = self.completed_tasks.values().collect();
        (active, completed)
    }

  ./crates/ccswarm/src/subagent/delegation.rs:221-240 method detect_qa_work <-> ./crates/ccswarm/src/subagent/delegation.rs:392-396 method get_task_status
  Similarity: 85.94%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_qa_work (lines 221-240) ---[0m
    fn detect_qa_work(&self, request: &str) -> bool {
        let qa_keywords = [
            "test",
            "testing",
            "qa",
            "quality",
            "coverage",
            "unit test",
            "integration test",
            "e2e",
            "bug",
            "validation",
            "verification",
        ];

        let request_lower = request.to_lowercase();
        qa_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:get_task_status (lines 392-396) ---[0m
    pub fn get_task_status(&self) -> (Vec<&DelegatedTask>, Vec<&TaskResult>) {
        let active: Vec<&DelegatedTask> = self.active_tasks.values().collect();
        let completed: Vec<&TaskResult> = self.completed_tasks.values().collect();
        (active, completed)
    }

  ./crates/ccswarm/src/subagent/delegation.rs:198-218 method detect_devops_work <-> ./crates/ccswarm/src/subagent/delegation.rs:410-416 function test_detect_frontend_work
  Similarity: 75.23%

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_devops_work (lines 198-218) ---[0m
    fn detect_devops_work(&self, request: &str) -> bool {
        let devops_keywords = [
            "docker",
            "kubernetes",
            "deploy",
            "ci/cd",
            "pipeline",
            "container",
            "infrastructure",
            "aws",
            "azure",
            "gcp",
            "terraform",
            "ansible",
        ];

        let request_lower = request.to_lowercase();
        devops_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:test_detect_frontend_work (lines 410-416) ---[0m
    fn test_detect_frontend_work() {
        let manager = DelegationManager::new();

        assert!(manager.detect_frontend_work("Create a React component"));
        assert!(manager.detect_frontend_work("Style the dashboard UI"));
        assert!(!manager.detect_frontend_work("Create database schema"));
    }

  ./crates/ccswarm/src/subagent/delegation.rs:198-218 method detect_devops_work <-> ./crates/ccswarm/src/subagent/delegation.rs:419-425 function test_detect_backend_work
  Similarity: 75.23%

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_devops_work (lines 198-218) ---[0m
    fn detect_devops_work(&self, request: &str) -> bool {
        let devops_keywords = [
            "docker",
            "kubernetes",
            "deploy",
            "ci/cd",
            "pipeline",
            "container",
            "infrastructure",
            "aws",
            "azure",
            "gcp",
            "terraform",
            "ansible",
        ];

        let request_lower = request.to_lowercase();
        devops_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:test_detect_backend_work (lines 419-425) ---[0m
    fn test_detect_backend_work() {
        let manager = DelegationManager::new();

        assert!(manager.detect_backend_work("Create REST API endpoints"));
        assert!(manager.detect_backend_work("Implement database models"));
        assert!(!manager.detect_backend_work("Style the button"));
    }

  ./crates/ccswarm/src/subagent/delegation.rs:243-261 method detect_semantic_analysis <-> ./crates/ccswarm/src/subagent/delegation.rs:392-396 method get_task_status
  Similarity: 85.25%
  Classes: DelegationManager <-> DelegationManager

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_semantic_analysis (lines 243-261) ---[0m
    fn detect_semantic_analysis(&self, request: &str) -> bool {
        let semantic_keywords = [
            "analyze",
            "understand",
            "refactor",
            "optimize",
            "pattern",
            "architecture",
            "structure",
            "dependency",
            "complexity",
            "metrics",
        ];

        let request_lower = request.to_lowercase();
        semantic_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:get_task_status (lines 392-396) ---[0m
    pub fn get_task_status(&self) -> (Vec<&DelegatedTask>, Vec<&TaskResult>) {
        let active: Vec<&DelegatedTask> = self.active_tasks.values().collect();
        let completed: Vec<&TaskResult> = self.completed_tasks.values().collect();
        (active, completed)
    }

  ./crates/ccswarm/src/subagent/delegation.rs:176-195 method detect_backend_work <-> ./crates/ccswarm/src/subagent/delegation.rs:410-416 function test_detect_frontend_work
  Similarity: 74.06%

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_backend_work (lines 176-195) ---[0m
    fn detect_backend_work(&self, request: &str) -> bool {
        let backend_keywords = [
            "api",
            "backend",
            "database",
            "server",
            "endpoint",
            "rest",
            "graphql",
            "authentication",
            "authorization",
            "model",
            "schema",
        ];

        let request_lower = request.to_lowercase();
        backend_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:test_detect_frontend_work (lines 410-416) ---[0m
    fn test_detect_frontend_work() {
        let manager = DelegationManager::new();

        assert!(manager.detect_frontend_work("Create a React component"));
        assert!(manager.detect_frontend_work("Style the dashboard UI"));
        assert!(!manager.detect_frontend_work("Create database schema"));
    }

  ./crates/ccswarm/src/subagent/delegation.rs:176-195 method detect_backend_work <-> ./crates/ccswarm/src/subagent/delegation.rs:419-425 function test_detect_backend_work
  Similarity: 74.06%

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_backend_work (lines 176-195) ---[0m
    fn detect_backend_work(&self, request: &str) -> bool {
        let backend_keywords = [
            "api",
            "backend",
            "database",
            "server",
            "endpoint",
            "rest",
            "graphql",
            "authentication",
            "authorization",
            "model",
            "schema",
        ];

        let request_lower = request.to_lowercase();
        backend_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:test_detect_backend_work (lines 419-425) ---[0m
    fn test_detect_backend_work() {
        let manager = DelegationManager::new();

        assert!(manager.detect_backend_work("Create REST API endpoints"));
        assert!(manager.detect_backend_work("Implement database models"));
        assert!(!manager.detect_backend_work("Style the button"));
    }

  ./crates/ccswarm/src/subagent/delegation.rs:221-240 method detect_qa_work <-> ./crates/ccswarm/src/subagent/delegation.rs:410-416 function test_detect_frontend_work
  Similarity: 74.06%

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_qa_work (lines 221-240) ---[0m
    fn detect_qa_work(&self, request: &str) -> bool {
        let qa_keywords = [
            "test",
            "testing",
            "qa",
            "quality",
            "coverage",
            "unit test",
            "integration test",
            "e2e",
            "bug",
            "validation",
            "verification",
        ];

        let request_lower = request.to_lowercase();
        qa_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:test_detect_frontend_work (lines 410-416) ---[0m
    fn test_detect_frontend_work() {
        let manager = DelegationManager::new();

        assert!(manager.detect_frontend_work("Create a React component"));
        assert!(manager.detect_frontend_work("Style the dashboard UI"));
        assert!(!manager.detect_frontend_work("Create database schema"));
    }

  ./crates/ccswarm/src/subagent/delegation.rs:221-240 method detect_qa_work <-> ./crates/ccswarm/src/subagent/delegation.rs:419-425 function test_detect_backend_work
  Similarity: 74.06%

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_qa_work (lines 221-240) ---[0m
    fn detect_qa_work(&self, request: &str) -> bool {
        let qa_keywords = [
            "test",
            "testing",
            "qa",
            "quality",
            "coverage",
            "unit test",
            "integration test",
            "e2e",
            "bug",
            "validation",
            "verification",
        ];

        let request_lower = request.to_lowercase();
        qa_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:test_detect_backend_work (lines 419-425) ---[0m
    fn test_detect_backend_work() {
        let manager = DelegationManager::new();

        assert!(manager.detect_backend_work("Create REST API endpoints"));
        assert!(manager.detect_backend_work("Implement database models"));
        assert!(!manager.detect_backend_work("Style the button"));
    }

  ./crates/ccswarm/src/subagent/delegation.rs:243-261 method detect_semantic_analysis <-> ./crates/ccswarm/src/subagent/delegation.rs:410-416 function test_detect_frontend_work
  Similarity: 72.77%

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_semantic_analysis (lines 243-261) ---[0m
    fn detect_semantic_analysis(&self, request: &str) -> bool {
        let semantic_keywords = [
            "analyze",
            "understand",
            "refactor",
            "optimize",
            "pattern",
            "architecture",
            "structure",
            "dependency",
            "complexity",
            "metrics",
        ];

        let request_lower = request.to_lowercase();
        semantic_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:test_detect_frontend_work (lines 410-416) ---[0m
    fn test_detect_frontend_work() {
        let manager = DelegationManager::new();

        assert!(manager.detect_frontend_work("Create a React component"));
        assert!(manager.detect_frontend_work("Style the dashboard UI"));
        assert!(!manager.detect_frontend_work("Create database schema"));
    }

  ./crates/ccswarm/src/subagent/delegation.rs:243-261 method detect_semantic_analysis <-> ./crates/ccswarm/src/subagent/delegation.rs:419-425 function test_detect_backend_work
  Similarity: 72.77%

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:detect_semantic_analysis (lines 243-261) ---[0m
    fn detect_semantic_analysis(&self, request: &str) -> bool {
        let semantic_keywords = [
            "analyze",
            "understand",
            "refactor",
            "optimize",
            "pattern",
            "architecture",
            "structure",
            "dependency",
            "complexity",
            "metrics",
        ];

        let request_lower = request.to_lowercase();
        semantic_keywords
            .iter()
            .any(|keyword| request_lower.contains(keyword))
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:test_detect_backend_work (lines 419-425) ---[0m
    fn test_detect_backend_work() {
        let manager = DelegationManager::new();

        assert!(manager.detect_backend_work("Create REST API endpoints"));
        assert!(manager.detect_backend_work("Implement database models"));
        assert!(!manager.detect_backend_work("Style the button"));
    }

  ./crates/ccswarm/src/subagent/delegation.rs:410-416 function test_detect_frontend_work <-> ./crates/ccswarm/src/subagent/delegation.rs:428-444 function test_analyze_for_delegation
  Similarity: 72.43%

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:test_detect_frontend_work (lines 410-416) ---[0m
    fn test_detect_frontend_work() {
        let manager = DelegationManager::new();

        assert!(manager.detect_frontend_work("Create a React component"));
        assert!(manager.detect_frontend_work("Style the dashboard UI"));
        assert!(!manager.detect_frontend_work("Create database schema"));
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:test_analyze_for_delegation (lines 428-444) ---[0m
    fn test_analyze_for_delegation() {
        let manager = DelegationManager::new();

        let tasks = manager
            .analyze_for_delegation(
                "Create a full-stack application with React frontend and REST API backend",
            )
            .unwrap();

        assert!(tasks.len() >= 2);
        assert!(tasks
            .iter()
            .any(|t| t.subagent_type == "frontend-specialist"));
        assert!(tasks
            .iter()
            .any(|t| t.subagent_type == "backend-specialist"));
    }

  ./crates/ccswarm/src/subagent/delegation.rs:419-425 function test_detect_backend_work <-> ./crates/ccswarm/src/subagent/delegation.rs:428-444 function test_analyze_for_delegation
  Similarity: 72.43%

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:test_detect_backend_work (lines 419-425) ---[0m
    fn test_detect_backend_work() {
        let manager = DelegationManager::new();

        assert!(manager.detect_backend_work("Create REST API endpoints"));
        assert!(manager.detect_backend_work("Implement database models"));
        assert!(!manager.detect_backend_work("Style the button"));
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:test_analyze_for_delegation (lines 428-444) ---[0m
    fn test_analyze_for_delegation() {
        let manager = DelegationManager::new();

        let tasks = manager
            .analyze_for_delegation(
                "Create a full-stack application with React frontend and REST API backend",
            )
            .unwrap();

        assert!(tasks.len() >= 2);
        assert!(tasks
            .iter()
            .any(|t| t.subagent_type == "frontend-specialist"));
        assert!(tasks
            .iter()
            .any(|t| t.subagent_type == "backend-specialist"));
    }

  ./crates/ccswarm/src/subagent/delegation.rs:410-416 function test_detect_frontend_work <-> ./crates/ccswarm/src/subagent/delegation.rs:419-425 function test_detect_backend_work
  Similarity: 97.39%

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:test_detect_frontend_work (lines 410-416) ---[0m
    fn test_detect_frontend_work() {
        let manager = DelegationManager::new();

        assert!(manager.detect_frontend_work("Create a React component"));
        assert!(manager.detect_frontend_work("Style the dashboard UI"));
        assert!(!manager.detect_frontend_work("Create database schema"));
    }

[36m--- ./crates/ccswarm/src/subagent/delegation.rs:test_detect_backend_work (lines 419-425) ---[0m
    fn test_detect_backend_work() {
        let manager = DelegationManager::new();

        assert!(manager.detect_backend_work("Create REST API endpoints"));
        assert!(manager.detect_backend_work("Implement database models"));
        assert!(!manager.detect_backend_work("Style the button"));
    }


Duplicates in ./crates/ccswarm/src/session/session_pool.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/session/session_pool.rs:345-422 method find_available_session <-> ./crates/ccswarm/src/session/session_pool.rs:483-539 method create_new_pooled_session
  Similarity: 83.52%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:find_available_session (lines 345-422) ---[0m
    async fn find_available_session(&self, role_key: &str) -> Option<Arc<PooledSession>> {
        let pools = self.pools.read().await;
        let sessions = pools.get(role_key)?;

        if sessions.is_empty() {
            return None;
        }

        match self.config.load_balancing {
            LoadBalancingStrategy::LeastLoaded => {
                // Find session with lowest current load
                sessions
                    .iter()
                    .filter(|s| s.stats.current_load < 1.0) // Not at max capacity
                    .min_by(|a, b| {
                        a.stats
                            .current_load
                            .partial_cmp(&b.stats.current_load)
                            .unwrap_or(std::cmp::Ordering::Equal)
                    })
                    .map(|s| Arc::new((*s).clone()))
            }
            LoadBalancingStrategy::RoundRobin => {
                // Simple round-robin selection
                let mut lb_state = self.load_balancer_state.write().await;
                let state = lb_state
                    .entry(role_key.to_string())
                    .or_insert(LoadBalancerState {
                        last_selected: 0,
                        selection_weights: vec![1.0; sessions.len()],
                        performance_history: VecDeque::new(),
                    });

                let available_sessions: Vec<_> = sessions
                    .iter()
                    .filter(|s| s.stats.current_load < 1.0)
                    .collect();

                if available_sessions.is_empty() {
                    return None;
                }

                let selected_idx = state.last_selected % available_sessions.len();
                state.last_selected = (state.last_selected + 1) % available_sessions.len();

                Some(Arc::new((*available_sessions[selected_idx]).clone()))
            }
            LoadBalancingStrategy::Adaptive => {
                // Choose based on performance metrics
                sessions
                    .iter()
                    .filter(|s| s.stats.current_load < 1.0)
                    .max_by(|a, b| {
                        let score_a = self.calculate_session_score(a);
                        let score_b = self.calculate_session_score(b);
                        score_a
                            .partial_cmp(&score_b)
                            .unwrap_or(std::cmp::Ordering::Equal)
                    })
                    .map(|s| Arc::new((*s).clone()))
            }
            LoadBalancingStrategy::WeightedRandom => {
                // Weighted random selection based on performance
                let available_sessions: Vec<_> = sessions
                    .iter()
                    .filter(|s| s.stats.current_load < 1.0)
                    .collect();

                if available_sessions.is_empty() {
                    return None;
                }

                // For simplicity, use round-robin for now
                // In real implementation, would use weighted random
                available_sessions.first().map(|s| Arc::new((**s).clone()))
            }
        }
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:create_new_pooled_session (lines 483-539) ---[0m
    async fn create_new_pooled_session(
        &self,
        role_key: &str,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        // Acquire semaphore to limit concurrent creation
        let _permit = self.creation_semaphore.acquire().await?;

        tracing::info!("Creating new pooled session for role: {}", role_key);

        // Create session through worktree manager
        let agent = {
            let worktree_manager = self.worktree_manager.lock().await;
            worktree_manager
                .get_or_create_worktree_session(role.clone(), claude_config.clone())
                .await?
        };

        // Create pool metadata
        let metadata = SessionPoolMetadata {
            pool_id: Uuid::new_v4().to_string(),
            agent_id: {
                let agent_guard = agent.lock().await;
                agent_guard.identity.agent_id.clone()
            },
            role: role.clone(),
            created_at: Utc::now(),
            pool_generation: 1,
            priority_score: 1.0,
            max_concurrent_tasks: self.config.max_concurrent_tasks_per_session,
        };

        let pooled_session = PooledSession {
            agent,
            metadata,
            stats: SessionUsageStats::default(),
            last_health_check: Instant::now(),
        };

        let pooled_session = Arc::new(pooled_session);

        // Add to pool
        {
            let mut pools = self.pools.write().await;
            pools
                .entry(role_key.to_string())
                .or_insert_with(Vec::new)
                .push(pooled_session.as_ref().clone());
        }

        tracing::info!(
            "Created pooled session successfully: {}",
            pooled_session.metadata.agent_id
        );
        Ok(pooled_session)
    }

  ./crates/ccswarm/src/session/session_pool.rs:483-539 method create_new_pooled_session <-> ./crates/ccswarm/src/session/session_pool.rs:706-748 method get_pool_statistics
  Similarity: 84.64%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:create_new_pooled_session (lines 483-539) ---[0m
    async fn create_new_pooled_session(
        &self,
        role_key: &str,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        // Acquire semaphore to limit concurrent creation
        let _permit = self.creation_semaphore.acquire().await?;

        tracing::info!("Creating new pooled session for role: {}", role_key);

        // Create session through worktree manager
        let agent = {
            let worktree_manager = self.worktree_manager.lock().await;
            worktree_manager
                .get_or_create_worktree_session(role.clone(), claude_config.clone())
                .await?
        };

        // Create pool metadata
        let metadata = SessionPoolMetadata {
            pool_id: Uuid::new_v4().to_string(),
            agent_id: {
                let agent_guard = agent.lock().await;
                agent_guard.identity.agent_id.clone()
            },
            role: role.clone(),
            created_at: Utc::now(),
            pool_generation: 1,
            priority_score: 1.0,
            max_concurrent_tasks: self.config.max_concurrent_tasks_per_session,
        };

        let pooled_session = PooledSession {
            agent,
            metadata,
            stats: SessionUsageStats::default(),
            last_health_check: Instant::now(),
        };

        let pooled_session = Arc::new(pooled_session);

        // Add to pool
        {
            let mut pools = self.pools.write().await;
            pools
                .entry(role_key.to_string())
                .or_insert_with(Vec::new)
                .push(pooled_session.as_ref().clone());
        }

        tracing::info!(
            "Created pooled session successfully: {}",
            pooled_session.metadata.agent_id
        );
        Ok(pooled_session)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:get_pool_statistics (lines 706-748) ---[0m
    pub async fn get_pool_statistics(&self) -> PoolStatistics {
        let pools = self.pools.read().await;
        let performance_metrics = self.performance_metrics.read().await;

        let mut role_stats = HashMap::new();
        let mut total_sessions = 0;
        let mut active_sessions = 0;

        for (role_key, sessions) in pools.iter() {
            total_sessions += sessions.len();
            let role_active = sessions
                .iter()
                .filter(|s| s.stats.current_load > 0.0)
                .count();
            active_sessions += role_active;

            role_stats.insert(
                role_key.clone(),
                RoleStatistics {
                    total_sessions: sessions.len(),
                    active_sessions: role_active,
                    average_load: Self::calculate_role_load(sessions),
                    total_tasks: sessions.iter().map(|s| s.stats.total_tasks_executed).sum(),
                },
            );
        }

        PoolStatistics {
            total_sessions,
            active_sessions,
            role_statistics: role_stats,
            global_performance: GlobalPerformanceStats {
                total_tasks_processed: performance_metrics.total_tasks_processed,
                total_execution_time: performance_metrics.total_execution_time,
                average_execution_time: if performance_metrics.total_tasks_processed > 0 {
                    performance_metrics.total_execution_time
                        / performance_metrics.total_tasks_processed as u32
                } else {
                    Duration::ZERO
                },
            },
        }
    }

  ./crates/ccswarm/src/session/session_pool.rs:483-539 method create_new_pooled_session <-> ./crates/ccswarm/src/session/session_pool.rs:826-866 function test_session_score_calculation
  Similarity: 86.06%

[36m--- ./crates/ccswarm/src/session/session_pool.rs:create_new_pooled_session (lines 483-539) ---[0m
    async fn create_new_pooled_session(
        &self,
        role_key: &str,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        // Acquire semaphore to limit concurrent creation
        let _permit = self.creation_semaphore.acquire().await?;

        tracing::info!("Creating new pooled session for role: {}", role_key);

        // Create session through worktree manager
        let agent = {
            let worktree_manager = self.worktree_manager.lock().await;
            worktree_manager
                .get_or_create_worktree_session(role.clone(), claude_config.clone())
                .await?
        };

        // Create pool metadata
        let metadata = SessionPoolMetadata {
            pool_id: Uuid::new_v4().to_string(),
            agent_id: {
                let agent_guard = agent.lock().await;
                agent_guard.identity.agent_id.clone()
            },
            role: role.clone(),
            created_at: Utc::now(),
            pool_generation: 1,
            priority_score: 1.0,
            max_concurrent_tasks: self.config.max_concurrent_tasks_per_session,
        };

        let pooled_session = PooledSession {
            agent,
            metadata,
            stats: SessionUsageStats::default(),
            last_health_check: Instant::now(),
        };

        let pooled_session = Arc::new(pooled_session);

        // Add to pool
        {
            let mut pools = self.pools.write().await;
            pools
                .entry(role_key.to_string())
                .or_insert_with(Vec::new)
                .push(pooled_session.as_ref().clone());
        }

        tracing::info!(
            "Created pooled session successfully: {}",
            pooled_session.metadata.agent_id
        );
        Ok(pooled_session)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:test_session_score_calculation (lines 826-866) ---[0m
    async fn test_session_score_calculation() {
        let temp_dir = TempDir::new().unwrap();

        let worktree_config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let pool_config = SessionPoolConfig::default();
        let _pool = SessionPool::new(worktree_config, pool_config)
            .await
            .unwrap();

        // Create a test session with mock data
        let session_stats = SessionUsageStats {
            current_load: 0.5,
            successful_tasks: 8,
            total_tasks_executed: 10,
            average_execution_time: Duration::from_secs(1),
            ..Default::default()
        };

        // Test the scoring calculation directly without creating a full PooledSession
        let load_factor = 1.0 - session_stats.current_load;
        let success_rate = if session_stats.total_tasks_executed > 0 {
            session_stats.successful_tasks as f64 / session_stats.total_tasks_executed as f64
        } else {
            1.0
        };
        let speed_factor = if session_stats.average_execution_time.as_millis() > 0 {
            1000.0 / session_stats.average_execution_time.as_millis() as f64
        } else {
            1.0
        };

        let score = (load_factor * 0.4) + (success_rate * 0.4) + (speed_factor.min(2.0) * 0.2);

        assert!(score > 0.0);
        assert!(score <= 1.0);
        assert_eq!(success_rate, 0.8); // 8 successful out of 10
    }

  ./crates/ccswarm/src/session/session_pool.rs:289-322 method execute_task_batch <-> ./crates/ccswarm/src/session/session_pool.rs:483-539 method create_new_pooled_session
  Similarity: 90.89%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task_batch (lines 289-322) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        let start_time = Instant::now();

        // For batch operations, prefer a single session for maximum efficiency
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute batch
        let results = {
            let mut agent = session.agent.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Update statistics for batch
        for result in &results {
            self.update_session_stats(
                &session,
                result,
                start_time.elapsed() / results.len() as u32,
            )
            .await;
        }

        tracing::info!("Batch of {} tasks completed in session pool", results.len());
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:create_new_pooled_session (lines 483-539) ---[0m
    async fn create_new_pooled_session(
        &self,
        role_key: &str,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        // Acquire semaphore to limit concurrent creation
        let _permit = self.creation_semaphore.acquire().await?;

        tracing::info!("Creating new pooled session for role: {}", role_key);

        // Create session through worktree manager
        let agent = {
            let worktree_manager = self.worktree_manager.lock().await;
            worktree_manager
                .get_or_create_worktree_session(role.clone(), claude_config.clone())
                .await?
        };

        // Create pool metadata
        let metadata = SessionPoolMetadata {
            pool_id: Uuid::new_v4().to_string(),
            agent_id: {
                let agent_guard = agent.lock().await;
                agent_guard.identity.agent_id.clone()
            },
            role: role.clone(),
            created_at: Utc::now(),
            pool_generation: 1,
            priority_score: 1.0,
            max_concurrent_tasks: self.config.max_concurrent_tasks_per_session,
        };

        let pooled_session = PooledSession {
            agent,
            metadata,
            stats: SessionUsageStats::default(),
            last_health_check: Instant::now(),
        };

        let pooled_session = Arc::new(pooled_session);

        // Add to pool
        {
            let mut pools = self.pools.write().await;
            pools
                .entry(role_key.to_string())
                .or_insert_with(Vec::new)
                .push(pooled_session.as_ref().clone());
        }

        tracing::info!(
            "Created pooled session successfully: {}",
            pooled_session.metadata.agent_id
        );
        Ok(pooled_session)
    }

  ./crates/ccswarm/src/session/session_pool.rs:235-261 method start <-> ./crates/ccswarm/src/session/session_pool.rs:483-539 method create_new_pooled_session
  Similarity: 85.26%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start (lines 235-261) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting session pool");

        // Start health check task
        let health_check_task = self.start_health_check_task().await;
        self.background_tasks.push(health_check_task);

        // Start auto-scaling task if enabled
        if self.config.auto_scaling.enabled {
            let scaling_task = self.start_auto_scaling_task().await;
            self.background_tasks.push(scaling_task);
        }

        // Start performance monitoring task
        if self.config.enable_performance_monitoring {
            let monitoring_task = self.start_performance_monitoring_task().await;
            self.background_tasks.push(monitoring_task);
        }

        // Pre-warm sessions if using eager strategy
        if matches!(self.config.warmup_strategy, WarmupStrategy::Eager) {
            self.warmup_essential_sessions().await?;
        }

        tracing::info!("Session pool started successfully");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:create_new_pooled_session (lines 483-539) ---[0m
    async fn create_new_pooled_session(
        &self,
        role_key: &str,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        // Acquire semaphore to limit concurrent creation
        let _permit = self.creation_semaphore.acquire().await?;

        tracing::info!("Creating new pooled session for role: {}", role_key);

        // Create session through worktree manager
        let agent = {
            let worktree_manager = self.worktree_manager.lock().await;
            worktree_manager
                .get_or_create_worktree_session(role.clone(), claude_config.clone())
                .await?
        };

        // Create pool metadata
        let metadata = SessionPoolMetadata {
            pool_id: Uuid::new_v4().to_string(),
            agent_id: {
                let agent_guard = agent.lock().await;
                agent_guard.identity.agent_id.clone()
            },
            role: role.clone(),
            created_at: Utc::now(),
            pool_generation: 1,
            priority_score: 1.0,
            max_concurrent_tasks: self.config.max_concurrent_tasks_per_session,
        };

        let pooled_session = PooledSession {
            agent,
            metadata,
            stats: SessionUsageStats::default(),
            last_health_check: Instant::now(),
        };

        let pooled_session = Arc::new(pooled_session);

        // Add to pool
        {
            let mut pools = self.pools.write().await;
            pools
                .entry(role_key.to_string())
                .or_insert_with(Vec::new)
                .push(pooled_session.as_ref().clone());
        }

        tracing::info!(
            "Created pooled session successfully: {}",
            pooled_session.metadata.agent_id
        );
        Ok(pooled_session)
    }

  ./crates/ccswarm/src/session/session_pool.rs:483-539 method create_new_pooled_session <-> ./crates/ccswarm/src/session/session_pool.rs:607-642 method start_auto_scaling_task
  Similarity: 70.32%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:create_new_pooled_session (lines 483-539) ---[0m
    async fn create_new_pooled_session(
        &self,
        role_key: &str,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        // Acquire semaphore to limit concurrent creation
        let _permit = self.creation_semaphore.acquire().await?;

        tracing::info!("Creating new pooled session for role: {}", role_key);

        // Create session through worktree manager
        let agent = {
            let worktree_manager = self.worktree_manager.lock().await;
            worktree_manager
                .get_or_create_worktree_session(role.clone(), claude_config.clone())
                .await?
        };

        // Create pool metadata
        let metadata = SessionPoolMetadata {
            pool_id: Uuid::new_v4().to_string(),
            agent_id: {
                let agent_guard = agent.lock().await;
                agent_guard.identity.agent_id.clone()
            },
            role: role.clone(),
            created_at: Utc::now(),
            pool_generation: 1,
            priority_score: 1.0,
            max_concurrent_tasks: self.config.max_concurrent_tasks_per_session,
        };

        let pooled_session = PooledSession {
            agent,
            metadata,
            stats: SessionUsageStats::default(),
            last_health_check: Instant::now(),
        };

        let pooled_session = Arc::new(pooled_session);

        // Add to pool
        {
            let mut pools = self.pools.write().await;
            pools
                .entry(role_key.to_string())
                .or_insert_with(Vec::new)
                .push(pooled_session.as_ref().clone());
        }

        tracing::info!(
            "Created pooled session successfully: {}",
            pooled_session.metadata.agent_id
        );
        Ok(pooled_session)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_auto_scaling_task (lines 607-642) ---[0m
    async fn start_auto_scaling_task(&self) -> tokio::task::JoinHandle<()> {
        let pools = Arc::clone(&self.pools);
        let _scaling_state = Arc::clone(&self.scaling_state);
        let config = self.config.clone();

        tokio::spawn(async move {
            let mut interval = interval(Duration::from_secs(30));

            loop {
                interval.tick().await;

                // Check each role for scaling needs
                let pools_guard = pools.read().await;
                for (role_key, sessions) in pools_guard.iter() {
                    let current_load = Self::calculate_role_load(sessions);

                    // Implement scaling logic based on load
                    if current_load > config.auto_scaling.scale_up_threshold {
                        tracing::info!(
                            "High load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale up logic would go here
                    } else if current_load < config.auto_scaling.scale_down_threshold {
                        tracing::debug!(
                            "Low load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale down logic would go here
                    }
                }
            }
        })
    }

  ./crates/ccswarm/src/session/session_pool.rs:289-322 method execute_task_batch <-> ./crates/ccswarm/src/session/session_pool.rs:706-748 method get_pool_statistics
  Similarity: 84.61%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task_batch (lines 289-322) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        let start_time = Instant::now();

        // For batch operations, prefer a single session for maximum efficiency
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute batch
        let results = {
            let mut agent = session.agent.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Update statistics for batch
        for result in &results {
            self.update_session_stats(
                &session,
                result,
                start_time.elapsed() / results.len() as u32,
            )
            .await;
        }

        tracing::info!("Batch of {} tasks completed in session pool", results.len());
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:get_pool_statistics (lines 706-748) ---[0m
    pub async fn get_pool_statistics(&self) -> PoolStatistics {
        let pools = self.pools.read().await;
        let performance_metrics = self.performance_metrics.read().await;

        let mut role_stats = HashMap::new();
        let mut total_sessions = 0;
        let mut active_sessions = 0;

        for (role_key, sessions) in pools.iter() {
            total_sessions += sessions.len();
            let role_active = sessions
                .iter()
                .filter(|s| s.stats.current_load > 0.0)
                .count();
            active_sessions += role_active;

            role_stats.insert(
                role_key.clone(),
                RoleStatistics {
                    total_sessions: sessions.len(),
                    active_sessions: role_active,
                    average_load: Self::calculate_role_load(sessions),
                    total_tasks: sessions.iter().map(|s| s.stats.total_tasks_executed).sum(),
                },
            );
        }

        PoolStatistics {
            total_sessions,
            active_sessions,
            role_statistics: role_stats,
            global_performance: GlobalPerformanceStats {
                total_tasks_processed: performance_metrics.total_tasks_processed,
                total_execution_time: performance_metrics.total_execution_time,
                average_execution_time: if performance_metrics.total_tasks_processed > 0 {
                    performance_metrics.total_execution_time
                        / performance_metrics.total_tasks_processed as u32
                } else {
                    Duration::ZERO
                },
            },
        }
    }

  ./crates/ccswarm/src/session/session_pool.rs:607-642 method start_auto_scaling_task <-> ./crates/ccswarm/src/session/session_pool.rs:706-748 method get_pool_statistics
  Similarity: 79.72%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_auto_scaling_task (lines 607-642) ---[0m
    async fn start_auto_scaling_task(&self) -> tokio::task::JoinHandle<()> {
        let pools = Arc::clone(&self.pools);
        let _scaling_state = Arc::clone(&self.scaling_state);
        let config = self.config.clone();

        tokio::spawn(async move {
            let mut interval = interval(Duration::from_secs(30));

            loop {
                interval.tick().await;

                // Check each role for scaling needs
                let pools_guard = pools.read().await;
                for (role_key, sessions) in pools_guard.iter() {
                    let current_load = Self::calculate_role_load(sessions);

                    // Implement scaling logic based on load
                    if current_load > config.auto_scaling.scale_up_threshold {
                        tracing::info!(
                            "High load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale up logic would go here
                    } else if current_load < config.auto_scaling.scale_down_threshold {
                        tracing::debug!(
                            "Low load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale down logic would go here
                    }
                }
            }
        })
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:get_pool_statistics (lines 706-748) ---[0m
    pub async fn get_pool_statistics(&self) -> PoolStatistics {
        let pools = self.pools.read().await;
        let performance_metrics = self.performance_metrics.read().await;

        let mut role_stats = HashMap::new();
        let mut total_sessions = 0;
        let mut active_sessions = 0;

        for (role_key, sessions) in pools.iter() {
            total_sessions += sessions.len();
            let role_active = sessions
                .iter()
                .filter(|s| s.stats.current_load > 0.0)
                .count();
            active_sessions += role_active;

            role_stats.insert(
                role_key.clone(),
                RoleStatistics {
                    total_sessions: sessions.len(),
                    active_sessions: role_active,
                    average_load: Self::calculate_role_load(sessions),
                    total_tasks: sessions.iter().map(|s| s.stats.total_tasks_executed).sum(),
                },
            );
        }

        PoolStatistics {
            total_sessions,
            active_sessions,
            role_statistics: role_stats,
            global_performance: GlobalPerformanceStats {
                total_tasks_processed: performance_metrics.total_tasks_processed,
                total_execution_time: performance_metrics.total_execution_time,
                average_execution_time: if performance_metrics.total_tasks_processed > 0 {
                    performance_metrics.total_execution_time
                        / performance_metrics.total_tasks_processed as u32
                } else {
                    Duration::ZERO
                },
            },
        }
    }

  ./crates/ccswarm/src/session/session_pool.rs:564-591 method start_health_check_task <-> ./crates/ccswarm/src/session/session_pool.rs:607-642 method start_auto_scaling_task
  Similarity: 89.65%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_health_check_task (lines 564-591) ---[0m
    async fn start_health_check_task(&self) -> tokio::task::JoinHandle<()> {
        let pools = Arc::clone(&self.pools);
        let interval_duration = self.config.health_check_interval;

        tokio::spawn(async move {
            let mut interval = interval(interval_duration);

            loop {
                interval.tick().await;

                // Perform health checks on all sessions
                let pools_guard = pools.read().await;
                for (role_key, sessions) in pools_guard.iter() {
                    for session in sessions {
                        // Check session health
                        if let Err(e) = Self::health_check_session(session).await {
                            tracing::warn!(
                                "Health check failed for session {} in role {}: {}",
                                session.metadata.agent_id,
                                role_key,
                                e
                            );
                        }
                    }
                }
            }
        })
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_auto_scaling_task (lines 607-642) ---[0m
    async fn start_auto_scaling_task(&self) -> tokio::task::JoinHandle<()> {
        let pools = Arc::clone(&self.pools);
        let _scaling_state = Arc::clone(&self.scaling_state);
        let config = self.config.clone();

        tokio::spawn(async move {
            let mut interval = interval(Duration::from_secs(30));

            loop {
                interval.tick().await;

                // Check each role for scaling needs
                let pools_guard = pools.read().await;
                for (role_key, sessions) in pools_guard.iter() {
                    let current_load = Self::calculate_role_load(sessions);

                    // Implement scaling logic based on load
                    if current_load > config.auto_scaling.scale_up_threshold {
                        tracing::info!(
                            "High load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale up logic would go here
                    } else if current_load < config.auto_scaling.scale_down_threshold {
                        tracing::debug!(
                            "Low load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale down logic would go here
                    }
                }
            }
        })
    }

  ./crates/ccswarm/src/session/session_pool.rs:289-322 method execute_task_batch <-> ./crates/ccswarm/src/session/session_pool.rs:826-866 function test_session_score_calculation
  Similarity: 76.40%

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task_batch (lines 289-322) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        let start_time = Instant::now();

        // For batch operations, prefer a single session for maximum efficiency
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute batch
        let results = {
            let mut agent = session.agent.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Update statistics for batch
        for result in &results {
            self.update_session_stats(
                &session,
                result,
                start_time.elapsed() / results.len() as u32,
            )
            .await;
        }

        tracing::info!("Batch of {} tasks completed in session pool", results.len());
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:test_session_score_calculation (lines 826-866) ---[0m
    async fn test_session_score_calculation() {
        let temp_dir = TempDir::new().unwrap();

        let worktree_config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let pool_config = SessionPoolConfig::default();
        let _pool = SessionPool::new(worktree_config, pool_config)
            .await
            .unwrap();

        // Create a test session with mock data
        let session_stats = SessionUsageStats {
            current_load: 0.5,
            successful_tasks: 8,
            total_tasks_executed: 10,
            average_execution_time: Duration::from_secs(1),
            ..Default::default()
        };

        // Test the scoring calculation directly without creating a full PooledSession
        let load_factor = 1.0 - session_stats.current_load;
        let success_rate = if session_stats.total_tasks_executed > 0 {
            session_stats.successful_tasks as f64 / session_stats.total_tasks_executed as f64
        } else {
            1.0
        };
        let speed_factor = if session_stats.average_execution_time.as_millis() > 0 {
            1000.0 / session_stats.average_execution_time.as_millis() as f64
        } else {
            1.0
        };

        let score = (load_factor * 0.4) + (success_rate * 0.4) + (speed_factor.min(2.0) * 0.2);

        assert!(score > 0.0);
        assert!(score <= 1.0);
        assert_eq!(success_rate, 0.8); // 8 successful out of 10
    }

  ./crates/ccswarm/src/session/session_pool.rs:679-703 method warmup_essential_sessions <-> ./crates/ccswarm/src/session/session_pool.rs:706-748 method get_pool_statistics
  Similarity: 81.90%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:warmup_essential_sessions (lines 679-703) ---[0m
    async fn warmup_essential_sessions(&self) -> Result<()> {
        use crate::identity::{default_backend_role, default_frontend_role};

        tracing::info!("Warming up essential sessions");

        let essential_roles = vec![default_frontend_role(), default_backend_role()];

        for role in essential_roles {
            for _ in 0..self.config.min_sessions_per_role {
                let claude_config = ClaudeConfig::default();
                if let Err(e) = self
                    .create_new_pooled_session(
                        &role.name().to_lowercase(),
                        role.clone(),
                        claude_config,
                    )
                    .await
                {
                    tracing::warn!("Failed to warmup session for role {}: {}", role.name(), e);
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:get_pool_statistics (lines 706-748) ---[0m
    pub async fn get_pool_statistics(&self) -> PoolStatistics {
        let pools = self.pools.read().await;
        let performance_metrics = self.performance_metrics.read().await;

        let mut role_stats = HashMap::new();
        let mut total_sessions = 0;
        let mut active_sessions = 0;

        for (role_key, sessions) in pools.iter() {
            total_sessions += sessions.len();
            let role_active = sessions
                .iter()
                .filter(|s| s.stats.current_load > 0.0)
                .count();
            active_sessions += role_active;

            role_stats.insert(
                role_key.clone(),
                RoleStatistics {
                    total_sessions: sessions.len(),
                    active_sessions: role_active,
                    average_load: Self::calculate_role_load(sessions),
                    total_tasks: sessions.iter().map(|s| s.stats.total_tasks_executed).sum(),
                },
            );
        }

        PoolStatistics {
            total_sessions,
            active_sessions,
            role_statistics: role_stats,
            global_performance: GlobalPerformanceStats {
                total_tasks_processed: performance_metrics.total_tasks_processed,
                total_execution_time: performance_metrics.total_execution_time,
                average_execution_time: if performance_metrics.total_tasks_processed > 0 {
                    performance_metrics.total_execution_time
                        / performance_metrics.total_tasks_processed as u32
                } else {
                    Duration::ZERO
                },
            },
        }
    }

  ./crates/ccswarm/src/session/session_pool.rs:213-232 method new <-> ./crates/ccswarm/src/session/session_pool.rs:483-539 method create_new_pooled_session
  Similarity: 71.67%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:new (lines 213-232) ---[0m
    pub async fn new(
        worktree_config: WorktreeSessionConfig,
        pool_config: SessionPoolConfig,
    ) -> Result<Self> {
        let mut worktree_manager = WorktreeSessionManager::new(worktree_config)?;
        worktree_manager.start().await?;

        let pool = Self {
            pools: Arc::new(RwLock::new(HashMap::new())),
            worktree_manager: Arc::new(Mutex::new(worktree_manager)),
            config: pool_config.clone(),
            load_balancer_state: Arc::new(RwLock::new(HashMap::new())),
            performance_metrics: Arc::new(RwLock::new(PerformanceMetrics::default())),
            background_tasks: Vec::new(),
            creation_semaphore: Arc::new(Semaphore::new(pool_config.max_sessions_per_role * 4)),
            scaling_state: Arc::new(RwLock::new(HashMap::new())),
        };

        Ok(pool)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:create_new_pooled_session (lines 483-539) ---[0m
    async fn create_new_pooled_session(
        &self,
        role_key: &str,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        // Acquire semaphore to limit concurrent creation
        let _permit = self.creation_semaphore.acquire().await?;

        tracing::info!("Creating new pooled session for role: {}", role_key);

        // Create session through worktree manager
        let agent = {
            let worktree_manager = self.worktree_manager.lock().await;
            worktree_manager
                .get_or_create_worktree_session(role.clone(), claude_config.clone())
                .await?
        };

        // Create pool metadata
        let metadata = SessionPoolMetadata {
            pool_id: Uuid::new_v4().to_string(),
            agent_id: {
                let agent_guard = agent.lock().await;
                agent_guard.identity.agent_id.clone()
            },
            role: role.clone(),
            created_at: Utc::now(),
            pool_generation: 1,
            priority_score: 1.0,
            max_concurrent_tasks: self.config.max_concurrent_tasks_per_session,
        };

        let pooled_session = PooledSession {
            agent,
            metadata,
            stats: SessionUsageStats::default(),
            last_health_check: Instant::now(),
        };

        let pooled_session = Arc::new(pooled_session);

        // Add to pool
        {
            let mut pools = self.pools.write().await;
            pools
                .entry(role_key.to_string())
                .or_insert_with(Vec::new)
                .push(pooled_session.as_ref().clone());
        }

        tracing::info!(
            "Created pooled session successfully: {}",
            pooled_session.metadata.agent_id
        );
        Ok(pooled_session)
    }

  ./crates/ccswarm/src/session/session_pool.rs:235-261 method start <-> ./crates/ccswarm/src/session/session_pool.rs:706-748 method get_pool_statistics
  Similarity: 78.82%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start (lines 235-261) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting session pool");

        // Start health check task
        let health_check_task = self.start_health_check_task().await;
        self.background_tasks.push(health_check_task);

        // Start auto-scaling task if enabled
        if self.config.auto_scaling.enabled {
            let scaling_task = self.start_auto_scaling_task().await;
            self.background_tasks.push(scaling_task);
        }

        // Start performance monitoring task
        if self.config.enable_performance_monitoring {
            let monitoring_task = self.start_performance_monitoring_task().await;
            self.background_tasks.push(monitoring_task);
        }

        // Pre-warm sessions if using eager strategy
        if matches!(self.config.warmup_strategy, WarmupStrategy::Eager) {
            self.warmup_essential_sessions().await?;
        }

        tracing::info!("Session pool started successfully");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:get_pool_statistics (lines 706-748) ---[0m
    pub async fn get_pool_statistics(&self) -> PoolStatistics {
        let pools = self.pools.read().await;
        let performance_metrics = self.performance_metrics.read().await;

        let mut role_stats = HashMap::new();
        let mut total_sessions = 0;
        let mut active_sessions = 0;

        for (role_key, sessions) in pools.iter() {
            total_sessions += sessions.len();
            let role_active = sessions
                .iter()
                .filter(|s| s.stats.current_load > 0.0)
                .count();
            active_sessions += role_active;

            role_stats.insert(
                role_key.clone(),
                RoleStatistics {
                    total_sessions: sessions.len(),
                    active_sessions: role_active,
                    average_load: Self::calculate_role_load(sessions),
                    total_tasks: sessions.iter().map(|s| s.stats.total_tasks_executed).sum(),
                },
            );
        }

        PoolStatistics {
            total_sessions,
            active_sessions,
            role_statistics: role_stats,
            global_performance: GlobalPerformanceStats {
                total_tasks_processed: performance_metrics.total_tasks_processed,
                total_execution_time: performance_metrics.total_execution_time,
                average_execution_time: if performance_metrics.total_tasks_processed > 0 {
                    performance_metrics.total_execution_time
                        / performance_metrics.total_tasks_processed as u32
                } else {
                    Duration::ZERO
                },
            },
        }
    }

  ./crates/ccswarm/src/session/session_pool.rs:564-591 method start_health_check_task <-> ./crates/ccswarm/src/session/session_pool.rs:706-748 method get_pool_statistics
  Similarity: 76.39%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_health_check_task (lines 564-591) ---[0m
    async fn start_health_check_task(&self) -> tokio::task::JoinHandle<()> {
        let pools = Arc::clone(&self.pools);
        let interval_duration = self.config.health_check_interval;

        tokio::spawn(async move {
            let mut interval = interval(interval_duration);

            loop {
                interval.tick().await;

                // Perform health checks on all sessions
                let pools_guard = pools.read().await;
                for (role_key, sessions) in pools_guard.iter() {
                    for session in sessions {
                        // Check session health
                        if let Err(e) = Self::health_check_session(session).await {
                            tracing::warn!(
                                "Health check failed for session {} in role {}: {}",
                                session.metadata.agent_id,
                                role_key,
                                e
                            );
                        }
                    }
                }
            }
        })
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:get_pool_statistics (lines 706-748) ---[0m
    pub async fn get_pool_statistics(&self) -> PoolStatistics {
        let pools = self.pools.read().await;
        let performance_metrics = self.performance_metrics.read().await;

        let mut role_stats = HashMap::new();
        let mut total_sessions = 0;
        let mut active_sessions = 0;

        for (role_key, sessions) in pools.iter() {
            total_sessions += sessions.len();
            let role_active = sessions
                .iter()
                .filter(|s| s.stats.current_load > 0.0)
                .count();
            active_sessions += role_active;

            role_stats.insert(
                role_key.clone(),
                RoleStatistics {
                    total_sessions: sessions.len(),
                    active_sessions: role_active,
                    average_load: Self::calculate_role_load(sessions),
                    total_tasks: sessions.iter().map(|s| s.stats.total_tasks_executed).sum(),
                },
            );
        }

        PoolStatistics {
            total_sessions,
            active_sessions,
            role_statistics: role_stats,
            global_performance: GlobalPerformanceStats {
                total_tasks_processed: performance_metrics.total_tasks_processed,
                total_execution_time: performance_metrics.total_execution_time,
                average_execution_time: if performance_metrics.total_tasks_processed > 0 {
                    performance_metrics.total_execution_time
                        / performance_metrics.total_tasks_processed as u32
                } else {
                    Duration::ZERO
                },
            },
        }
    }

  ./crates/ccswarm/src/session/session_pool.rs:235-261 method start <-> ./crates/ccswarm/src/session/session_pool.rs:826-866 function test_session_score_calculation
  Similarity: 79.41%

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start (lines 235-261) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting session pool");

        // Start health check task
        let health_check_task = self.start_health_check_task().await;
        self.background_tasks.push(health_check_task);

        // Start auto-scaling task if enabled
        if self.config.auto_scaling.enabled {
            let scaling_task = self.start_auto_scaling_task().await;
            self.background_tasks.push(scaling_task);
        }

        // Start performance monitoring task
        if self.config.enable_performance_monitoring {
            let monitoring_task = self.start_performance_monitoring_task().await;
            self.background_tasks.push(monitoring_task);
        }

        // Pre-warm sessions if using eager strategy
        if matches!(self.config.warmup_strategy, WarmupStrategy::Eager) {
            self.warmup_essential_sessions().await?;
        }

        tracing::info!("Session pool started successfully");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:test_session_score_calculation (lines 826-866) ---[0m
    async fn test_session_score_calculation() {
        let temp_dir = TempDir::new().unwrap();

        let worktree_config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let pool_config = SessionPoolConfig::default();
        let _pool = SessionPool::new(worktree_config, pool_config)
            .await
            .unwrap();

        // Create a test session with mock data
        let session_stats = SessionUsageStats {
            current_load: 0.5,
            successful_tasks: 8,
            total_tasks_executed: 10,
            average_execution_time: Duration::from_secs(1),
            ..Default::default()
        };

        // Test the scoring calculation directly without creating a full PooledSession
        let load_factor = 1.0 - session_stats.current_load;
        let success_rate = if session_stats.total_tasks_executed > 0 {
            session_stats.successful_tasks as f64 / session_stats.total_tasks_executed as f64
        } else {
            1.0
        };
        let speed_factor = if session_stats.average_execution_time.as_millis() > 0 {
            1000.0 / session_stats.average_execution_time.as_millis() as f64
        } else {
            1.0
        };

        let score = (load_factor * 0.4) + (success_rate * 0.4) + (speed_factor.min(2.0) * 0.2);

        assert!(score > 0.0);
        assert!(score <= 1.0);
        assert_eq!(success_rate, 0.8); // 8 successful out of 10
    }

  ./crates/ccswarm/src/session/session_pool.rs:607-642 method start_auto_scaling_task <-> ./crates/ccswarm/src/session/session_pool.rs:679-703 method warmup_essential_sessions
  Similarity: 86.48%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_auto_scaling_task (lines 607-642) ---[0m
    async fn start_auto_scaling_task(&self) -> tokio::task::JoinHandle<()> {
        let pools = Arc::clone(&self.pools);
        let _scaling_state = Arc::clone(&self.scaling_state);
        let config = self.config.clone();

        tokio::spawn(async move {
            let mut interval = interval(Duration::from_secs(30));

            loop {
                interval.tick().await;

                // Check each role for scaling needs
                let pools_guard = pools.read().await;
                for (role_key, sessions) in pools_guard.iter() {
                    let current_load = Self::calculate_role_load(sessions);

                    // Implement scaling logic based on load
                    if current_load > config.auto_scaling.scale_up_threshold {
                        tracing::info!(
                            "High load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale up logic would go here
                    } else if current_load < config.auto_scaling.scale_down_threshold {
                        tracing::debug!(
                            "Low load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale down logic would go here
                    }
                }
            }
        })
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:warmup_essential_sessions (lines 679-703) ---[0m
    async fn warmup_essential_sessions(&self) -> Result<()> {
        use crate::identity::{default_backend_role, default_frontend_role};

        tracing::info!("Warming up essential sessions");

        let essential_roles = vec![default_frontend_role(), default_backend_role()];

        for role in essential_roles {
            for _ in 0..self.config.min_sessions_per_role {
                let claude_config = ClaudeConfig::default();
                if let Err(e) = self
                    .create_new_pooled_session(
                        &role.name().to_lowercase(),
                        role.clone(),
                        claude_config,
                    )
                    .await
                {
                    tracing::warn!("Failed to warmup session for role {}: {}", role.name(), e);
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:443-467 method ensure_session_available <-> ./crates/ccswarm/src/session/session_pool.rs:607-642 method start_auto_scaling_task
  Similarity: 85.87%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:ensure_session_available (lines 443-467) ---[0m
    async fn ensure_session_available(
        &self,
        role_key: &str,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        // Check if we're at max capacity
        let current_count = {
            let pools = self.pools.read().await;
            pools.get(role_key).map(|p| p.len()).unwrap_or(0)
        };

        if current_count >= self.config.max_sessions_per_role {
            // Wait for an available session or fail
            tokio::time::timeout(
                Duration::from_secs(30),
                self.wait_for_available_session(role_key),
            )
            .await?
        } else {
            // Create a new session
            self.create_new_pooled_session(role_key, role, claude_config)
                .await
        }
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_auto_scaling_task (lines 607-642) ---[0m
    async fn start_auto_scaling_task(&self) -> tokio::task::JoinHandle<()> {
        let pools = Arc::clone(&self.pools);
        let _scaling_state = Arc::clone(&self.scaling_state);
        let config = self.config.clone();

        tokio::spawn(async move {
            let mut interval = interval(Duration::from_secs(30));

            loop {
                interval.tick().await;

                // Check each role for scaling needs
                let pools_guard = pools.read().await;
                for (role_key, sessions) in pools_guard.iter() {
                    let current_load = Self::calculate_role_load(sessions);

                    // Implement scaling logic based on load
                    if current_load > config.auto_scaling.scale_up_threshold {
                        tracing::info!(
                            "High load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale up logic would go here
                    } else if current_load < config.auto_scaling.scale_down_threshold {
                        tracing::debug!(
                            "Low load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale down logic would go here
                    }
                }
            }
        })
    }

  ./crates/ccswarm/src/session/session_pool.rs:264-286 method execute_task <-> ./crates/ccswarm/src/session/session_pool.rs:607-642 method start_auto_scaling_task
  Similarity: 88.74%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task (lines 264-286) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let start_time = Instant::now();

        // Get optimal session for the task
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute task
        let result = {
            let mut agent = session.agent.lock().await;
            agent.execute_task(task.clone()).await?
        };

        // Update statistics
        self.update_session_stats(&session, &result, start_time.elapsed())
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_auto_scaling_task (lines 607-642) ---[0m
    async fn start_auto_scaling_task(&self) -> tokio::task::JoinHandle<()> {
        let pools = Arc::clone(&self.pools);
        let _scaling_state = Arc::clone(&self.scaling_state);
        let config = self.config.clone();

        tokio::spawn(async move {
            let mut interval = interval(Duration::from_secs(30));

            loop {
                interval.tick().await;

                // Check each role for scaling needs
                let pools_guard = pools.read().await;
                for (role_key, sessions) in pools_guard.iter() {
                    let current_load = Self::calculate_role_load(sessions);

                    // Implement scaling logic based on load
                    if current_load > config.auto_scaling.scale_up_threshold {
                        tracing::info!(
                            "High load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale up logic would go here
                    } else if current_load < config.auto_scaling.scale_down_threshold {
                        tracing::debug!(
                            "Low load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale down logic would go here
                    }
                }
            }
        })
    }

  ./crates/ccswarm/src/session/session_pool.rs:213-232 method new <-> ./crates/ccswarm/src/session/session_pool.rs:607-642 method start_auto_scaling_task
  Similarity: 93.48%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:new (lines 213-232) ---[0m
    pub async fn new(
        worktree_config: WorktreeSessionConfig,
        pool_config: SessionPoolConfig,
    ) -> Result<Self> {
        let mut worktree_manager = WorktreeSessionManager::new(worktree_config)?;
        worktree_manager.start().await?;

        let pool = Self {
            pools: Arc::new(RwLock::new(HashMap::new())),
            worktree_manager: Arc::new(Mutex::new(worktree_manager)),
            config: pool_config.clone(),
            load_balancer_state: Arc::new(RwLock::new(HashMap::new())),
            performance_metrics: Arc::new(RwLock::new(PerformanceMetrics::default())),
            background_tasks: Vec::new(),
            creation_semaphore: Arc::new(Semaphore::new(pool_config.max_sessions_per_role * 4)),
            scaling_state: Arc::new(RwLock::new(HashMap::new())),
        };

        Ok(pool)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_auto_scaling_task (lines 607-642) ---[0m
    async fn start_auto_scaling_task(&self) -> tokio::task::JoinHandle<()> {
        let pools = Arc::clone(&self.pools);
        let _scaling_state = Arc::clone(&self.scaling_state);
        let config = self.config.clone();

        tokio::spawn(async move {
            let mut interval = interval(Duration::from_secs(30));

            loop {
                interval.tick().await;

                // Check each role for scaling needs
                let pools_guard = pools.read().await;
                for (role_key, sessions) in pools_guard.iter() {
                    let current_load = Self::calculate_role_load(sessions);

                    // Implement scaling logic based on load
                    if current_load > config.auto_scaling.scale_up_threshold {
                        tracing::info!(
                            "High load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale up logic would go here
                    } else if current_load < config.auto_scaling.scale_down_threshold {
                        tracing::debug!(
                            "Low load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale down logic would go here
                    }
                }
            }
        })
    }

  ./crates/ccswarm/src/session/session_pool.rs:264-286 method execute_task <-> ./crates/ccswarm/src/session/session_pool.rs:289-322 method execute_task_batch
  Similarity: 87.07%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task (lines 264-286) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let start_time = Instant::now();

        // Get optimal session for the task
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute task
        let result = {
            let mut agent = session.agent.lock().await;
            agent.execute_task(task.clone()).await?
        };

        // Update statistics
        self.update_session_stats(&session, &result, start_time.elapsed())
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task_batch (lines 289-322) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        let start_time = Instant::now();

        // For batch operations, prefer a single session for maximum efficiency
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute batch
        let results = {
            let mut agent = session.agent.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Update statistics for batch
        for result in &results {
            self.update_session_stats(
                &session,
                result,
                start_time.elapsed() / results.len() as u32,
            )
            .await;
        }

        tracing::info!("Batch of {} tasks completed in session pool", results.len());
        Ok(results)
    }

  ./crates/ccswarm/src/session/session_pool.rs:235-261 method start <-> ./crates/ccswarm/src/session/session_pool.rs:289-322 method execute_task_batch
  Similarity: 80.10%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start (lines 235-261) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting session pool");

        // Start health check task
        let health_check_task = self.start_health_check_task().await;
        self.background_tasks.push(health_check_task);

        // Start auto-scaling task if enabled
        if self.config.auto_scaling.enabled {
            let scaling_task = self.start_auto_scaling_task().await;
            self.background_tasks.push(scaling_task);
        }

        // Start performance monitoring task
        if self.config.enable_performance_monitoring {
            let monitoring_task = self.start_performance_monitoring_task().await;
            self.background_tasks.push(monitoring_task);
        }

        // Pre-warm sessions if using eager strategy
        if matches!(self.config.warmup_strategy, WarmupStrategy::Eager) {
            self.warmup_essential_sessions().await?;
        }

        tracing::info!("Session pool started successfully");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task_batch (lines 289-322) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        let start_time = Instant::now();

        // For batch operations, prefer a single session for maximum efficiency
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute batch
        let results = {
            let mut agent = session.agent.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Update statistics for batch
        for result in &results {
            self.update_session_stats(
                &session,
                result,
                start_time.elapsed() / results.len() as u32,
            )
            .await;
        }

        tracing::info!("Batch of {} tasks completed in session pool", results.len());
        Ok(results)
    }

  ./crates/ccswarm/src/session/session_pool.rs:289-322 method execute_task_batch <-> ./crates/ccswarm/src/session/session_pool.rs:751-773 method shutdown
  Similarity: 84.90%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task_batch (lines 289-322) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        let start_time = Instant::now();

        // For batch operations, prefer a single session for maximum efficiency
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute batch
        let results = {
            let mut agent = session.agent.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Update statistics for batch
        for result in &results {
            self.update_session_stats(
                &session,
                result,
                start_time.elapsed() / results.len() as u32,
            )
            .await;
        }

        tracing::info!("Batch of {} tasks completed in session pool", results.len());
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:shutdown (lines 751-773) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down session pool");

        // Cancel background tasks
        for handle in self.background_tasks.drain(..) {
            handle.abort();
        }

        // Shutdown worktree manager
        {
            let mut worktree_manager = self.worktree_manager.lock().await;
            worktree_manager.shutdown().await?;
        }

        // Clear pools
        {
            let mut pools = self.pools.write().await;
            pools.clear();
        }

        tracing::info!("Session pool shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:213-232 method new <-> ./crates/ccswarm/src/session/session_pool.rs:706-748 method get_pool_statistics
  Similarity: 76.36%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:new (lines 213-232) ---[0m
    pub async fn new(
        worktree_config: WorktreeSessionConfig,
        pool_config: SessionPoolConfig,
    ) -> Result<Self> {
        let mut worktree_manager = WorktreeSessionManager::new(worktree_config)?;
        worktree_manager.start().await?;

        let pool = Self {
            pools: Arc::new(RwLock::new(HashMap::new())),
            worktree_manager: Arc::new(Mutex::new(worktree_manager)),
            config: pool_config.clone(),
            load_balancer_state: Arc::new(RwLock::new(HashMap::new())),
            performance_metrics: Arc::new(RwLock::new(PerformanceMetrics::default())),
            background_tasks: Vec::new(),
            creation_semaphore: Arc::new(Semaphore::new(pool_config.max_sessions_per_role * 4)),
            scaling_state: Arc::new(RwLock::new(HashMap::new())),
        };

        Ok(pool)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:get_pool_statistics (lines 706-748) ---[0m
    pub async fn get_pool_statistics(&self) -> PoolStatistics {
        let pools = self.pools.read().await;
        let performance_metrics = self.performance_metrics.read().await;

        let mut role_stats = HashMap::new();
        let mut total_sessions = 0;
        let mut active_sessions = 0;

        for (role_key, sessions) in pools.iter() {
            total_sessions += sessions.len();
            let role_active = sessions
                .iter()
                .filter(|s| s.stats.current_load > 0.0)
                .count();
            active_sessions += role_active;

            role_stats.insert(
                role_key.clone(),
                RoleStatistics {
                    total_sessions: sessions.len(),
                    active_sessions: role_active,
                    average_load: Self::calculate_role_load(sessions),
                    total_tasks: sessions.iter().map(|s| s.stats.total_tasks_executed).sum(),
                },
            );
        }

        PoolStatistics {
            total_sessions,
            active_sessions,
            role_statistics: role_stats,
            global_performance: GlobalPerformanceStats {
                total_tasks_processed: performance_metrics.total_tasks_processed,
                total_execution_time: performance_metrics.total_execution_time,
                average_execution_time: if performance_metrics.total_tasks_processed > 0 {
                    performance_metrics.total_execution_time
                        / performance_metrics.total_tasks_processed as u32
                } else {
                    Duration::ZERO
                },
            },
        }
    }

  ./crates/ccswarm/src/session/session_pool.rs:607-642 method start_auto_scaling_task <-> ./crates/ccswarm/src/session/session_pool.rs:655-676 method start_performance_monitoring_task
  Similarity: 82.78%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_auto_scaling_task (lines 607-642) ---[0m
    async fn start_auto_scaling_task(&self) -> tokio::task::JoinHandle<()> {
        let pools = Arc::clone(&self.pools);
        let _scaling_state = Arc::clone(&self.scaling_state);
        let config = self.config.clone();

        tokio::spawn(async move {
            let mut interval = interval(Duration::from_secs(30));

            loop {
                interval.tick().await;

                // Check each role for scaling needs
                let pools_guard = pools.read().await;
                for (role_key, sessions) in pools_guard.iter() {
                    let current_load = Self::calculate_role_load(sessions);

                    // Implement scaling logic based on load
                    if current_load > config.auto_scaling.scale_up_threshold {
                        tracing::info!(
                            "High load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale up logic would go here
                    } else if current_load < config.auto_scaling.scale_down_threshold {
                        tracing::debug!(
                            "Low load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale down logic would go here
                    }
                }
            }
        })
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_performance_monitoring_task (lines 655-676) ---[0m
    async fn start_performance_monitoring_task(&self) -> tokio::task::JoinHandle<()> {
        let performance_metrics = Arc::clone(&self.performance_metrics);

        tokio::spawn(async move {
            let mut interval = interval(Duration::from_secs(60));

            loop {
                interval.tick().await;

                let metrics = performance_metrics.read().await;
                tracing::info!(
                    "Performance metrics - Total tasks: {}, Avg execution time: {:?}",
                    metrics.total_tasks_processed,
                    if metrics.total_tasks_processed > 0 {
                        metrics.total_execution_time / metrics.total_tasks_processed as u32
                    } else {
                        Duration::ZERO
                    }
                );
            }
        })
    }

  ./crates/ccswarm/src/session/session_pool.rs:443-467 method ensure_session_available <-> ./crates/ccswarm/src/session/session_pool.rs:564-591 method start_health_check_task
  Similarity: 90.52%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:ensure_session_available (lines 443-467) ---[0m
    async fn ensure_session_available(
        &self,
        role_key: &str,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        // Check if we're at max capacity
        let current_count = {
            let pools = self.pools.read().await;
            pools.get(role_key).map(|p| p.len()).unwrap_or(0)
        };

        if current_count >= self.config.max_sessions_per_role {
            // Wait for an available session or fail
            tokio::time::timeout(
                Duration::from_secs(30),
                self.wait_for_available_session(role_key),
            )
            .await?
        } else {
            // Create a new session
            self.create_new_pooled_session(role_key, role, claude_config)
                .await
        }
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_health_check_task (lines 564-591) ---[0m
    async fn start_health_check_task(&self) -> tokio::task::JoinHandle<()> {
        let pools = Arc::clone(&self.pools);
        let interval_duration = self.config.health_check_interval;

        tokio::spawn(async move {
            let mut interval = interval(interval_duration);

            loop {
                interval.tick().await;

                // Perform health checks on all sessions
                let pools_guard = pools.read().await;
                for (role_key, sessions) in pools_guard.iter() {
                    for session in sessions {
                        // Check session health
                        if let Err(e) = Self::health_check_session(session).await {
                            tracing::warn!(
                                "Health check failed for session {} in role {}: {}",
                                session.metadata.agent_id,
                                role_key,
                                e
                            );
                        }
                    }
                }
            }
        })
    }

  ./crates/ccswarm/src/session/session_pool.rs:425-440 method calculate_session_score <-> ./crates/ccswarm/src/session/session_pool.rs:607-642 method start_auto_scaling_task
  Similarity: 90.57%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:calculate_session_score (lines 425-440) ---[0m
    fn calculate_session_score(&self, session: &PooledSession) -> f64 {
        let load_factor = 1.0 - session.stats.current_load;
        let success_rate = if session.stats.total_tasks_executed > 0 {
            session.stats.successful_tasks as f64 / session.stats.total_tasks_executed as f64
        } else {
            1.0
        };
        let speed_factor = if session.stats.average_execution_time.as_millis() > 0 {
            1000.0 / session.stats.average_execution_time.as_millis() as f64
        } else {
            1.0
        };

        // Weighted score combining load, success rate, and speed
        (load_factor * 0.4) + (success_rate * 0.4) + (speed_factor.min(2.0) * 0.2)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_auto_scaling_task (lines 607-642) ---[0m
    async fn start_auto_scaling_task(&self) -> tokio::task::JoinHandle<()> {
        let pools = Arc::clone(&self.pools);
        let _scaling_state = Arc::clone(&self.scaling_state);
        let config = self.config.clone();

        tokio::spawn(async move {
            let mut interval = interval(Duration::from_secs(30));

            loop {
                interval.tick().await;

                // Check each role for scaling needs
                let pools_guard = pools.read().await;
                for (role_key, sessions) in pools_guard.iter() {
                    let current_load = Self::calculate_role_load(sessions);

                    // Implement scaling logic based on load
                    if current_load > config.auto_scaling.scale_up_threshold {
                        tracing::info!(
                            "High load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale up logic would go here
                    } else if current_load < config.auto_scaling.scale_down_threshold {
                        tracing::debug!(
                            "Low load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale down logic would go here
                    }
                }
            }
        })
    }

  ./crates/ccswarm/src/session/session_pool.rs:564-591 method start_health_check_task <-> ./crates/ccswarm/src/session/session_pool.rs:655-676 method start_performance_monitoring_task
  Similarity: 90.46%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_health_check_task (lines 564-591) ---[0m
    async fn start_health_check_task(&self) -> tokio::task::JoinHandle<()> {
        let pools = Arc::clone(&self.pools);
        let interval_duration = self.config.health_check_interval;

        tokio::spawn(async move {
            let mut interval = interval(interval_duration);

            loop {
                interval.tick().await;

                // Perform health checks on all sessions
                let pools_guard = pools.read().await;
                for (role_key, sessions) in pools_guard.iter() {
                    for session in sessions {
                        // Check session health
                        if let Err(e) = Self::health_check_session(session).await {
                            tracing::warn!(
                                "Health check failed for session {} in role {}: {}",
                                session.metadata.agent_id,
                                role_key,
                                e
                            );
                        }
                    }
                }
            }
        })
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_performance_monitoring_task (lines 655-676) ---[0m
    async fn start_performance_monitoring_task(&self) -> tokio::task::JoinHandle<()> {
        let performance_metrics = Arc::clone(&self.performance_metrics);

        tokio::spawn(async move {
            let mut interval = interval(Duration::from_secs(60));

            loop {
                interval.tick().await;

                let metrics = performance_metrics.read().await;
                tracing::info!(
                    "Performance metrics - Total tasks: {}, Avg execution time: {:?}",
                    metrics.total_tasks_processed,
                    if metrics.total_tasks_processed > 0 {
                        metrics.total_execution_time / metrics.total_tasks_processed as u32
                    } else {
                        Duration::ZERO
                    }
                );
            }
        })
    }

  ./crates/ccswarm/src/session/session_pool.rs:289-322 method execute_task_batch <-> ./crates/ccswarm/src/session/session_pool.rs:679-703 method warmup_essential_sessions
  Similarity: 74.70%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task_batch (lines 289-322) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        let start_time = Instant::now();

        // For batch operations, prefer a single session for maximum efficiency
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute batch
        let results = {
            let mut agent = session.agent.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Update statistics for batch
        for result in &results {
            self.update_session_stats(
                &session,
                result,
                start_time.elapsed() / results.len() as u32,
            )
            .await;
        }

        tracing::info!("Batch of {} tasks completed in session pool", results.len());
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:warmup_essential_sessions (lines 679-703) ---[0m
    async fn warmup_essential_sessions(&self) -> Result<()> {
        use crate::identity::{default_backend_role, default_frontend_role};

        tracing::info!("Warming up essential sessions");

        let essential_roles = vec![default_frontend_role(), default_backend_role()];

        for role in essential_roles {
            for _ in 0..self.config.min_sessions_per_role {
                let claude_config = ClaudeConfig::default();
                if let Err(e) = self
                    .create_new_pooled_session(
                        &role.name().to_lowercase(),
                        role.clone(),
                        claude_config,
                    )
                    .await
                {
                    tracing::warn!("Failed to warmup session for role {}: {}", role.name(), e);
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:213-232 method new <-> ./crates/ccswarm/src/session/session_pool.rs:564-591 method start_health_check_task
  Similarity: 90.64%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:new (lines 213-232) ---[0m
    pub async fn new(
        worktree_config: WorktreeSessionConfig,
        pool_config: SessionPoolConfig,
    ) -> Result<Self> {
        let mut worktree_manager = WorktreeSessionManager::new(worktree_config)?;
        worktree_manager.start().await?;

        let pool = Self {
            pools: Arc::new(RwLock::new(HashMap::new())),
            worktree_manager: Arc::new(Mutex::new(worktree_manager)),
            config: pool_config.clone(),
            load_balancer_state: Arc::new(RwLock::new(HashMap::new())),
            performance_metrics: Arc::new(RwLock::new(PerformanceMetrics::default())),
            background_tasks: Vec::new(),
            creation_semaphore: Arc::new(Semaphore::new(pool_config.max_sessions_per_role * 4)),
            scaling_state: Arc::new(RwLock::new(HashMap::new())),
        };

        Ok(pool)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_health_check_task (lines 564-591) ---[0m
    async fn start_health_check_task(&self) -> tokio::task::JoinHandle<()> {
        let pools = Arc::clone(&self.pools);
        let interval_duration = self.config.health_check_interval;

        tokio::spawn(async move {
            let mut interval = interval(interval_duration);

            loop {
                interval.tick().await;

                // Perform health checks on all sessions
                let pools_guard = pools.read().await;
                for (role_key, sessions) in pools_guard.iter() {
                    for session in sessions {
                        // Check session health
                        if let Err(e) = Self::health_check_session(session).await {
                            tracing::warn!(
                                "Health check failed for session {} in role {}: {}",
                                session.metadata.agent_id,
                                role_key,
                                e
                            );
                        }
                    }
                }
            }
        })
    }

  ./crates/ccswarm/src/session/session_pool.rs:607-642 method start_auto_scaling_task <-> ./crates/ccswarm/src/session/session_pool.rs:807-823 function test_session_pool_creation
  Similarity: 81.35%

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_auto_scaling_task (lines 607-642) ---[0m
    async fn start_auto_scaling_task(&self) -> tokio::task::JoinHandle<()> {
        let pools = Arc::clone(&self.pools);
        let _scaling_state = Arc::clone(&self.scaling_state);
        let config = self.config.clone();

        tokio::spawn(async move {
            let mut interval = interval(Duration::from_secs(30));

            loop {
                interval.tick().await;

                // Check each role for scaling needs
                let pools_guard = pools.read().await;
                for (role_key, sessions) in pools_guard.iter() {
                    let current_load = Self::calculate_role_load(sessions);

                    // Implement scaling logic based on load
                    if current_load > config.auto_scaling.scale_up_threshold {
                        tracing::info!(
                            "High load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale up logic would go here
                    } else if current_load < config.auto_scaling.scale_down_threshold {
                        tracing::debug!(
                            "Low load detected for role {}: {:.2}",
                            role_key,
                            current_load
                        );
                        // Scale down logic would go here
                    }
                }
            }
        })
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:test_session_pool_creation (lines 807-823) ---[0m
    async fn test_session_pool_creation() {
        let temp_dir = TempDir::new().unwrap();

        let worktree_config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let pool_config = SessionPoolConfig::default();

        let pool = SessionPool::new(worktree_config, pool_config)
            .await
            .unwrap();

        let stats = pool.get_pool_statistics().await;
        assert_eq!(stats.total_sessions, 0);
    }

  ./crates/ccswarm/src/session/session_pool.rs:235-261 method start <-> ./crates/ccswarm/src/session/session_pool.rs:751-773 method shutdown
  Similarity: 85.69%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start (lines 235-261) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting session pool");

        // Start health check task
        let health_check_task = self.start_health_check_task().await;
        self.background_tasks.push(health_check_task);

        // Start auto-scaling task if enabled
        if self.config.auto_scaling.enabled {
            let scaling_task = self.start_auto_scaling_task().await;
            self.background_tasks.push(scaling_task);
        }

        // Start performance monitoring task
        if self.config.enable_performance_monitoring {
            let monitoring_task = self.start_performance_monitoring_task().await;
            self.background_tasks.push(monitoring_task);
        }

        // Pre-warm sessions if using eager strategy
        if matches!(self.config.warmup_strategy, WarmupStrategy::Eager) {
            self.warmup_essential_sessions().await?;
        }

        tracing::info!("Session pool started successfully");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:shutdown (lines 751-773) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down session pool");

        // Cancel background tasks
        for handle in self.background_tasks.drain(..) {
            handle.abort();
        }

        // Shutdown worktree manager
        {
            let mut worktree_manager = self.worktree_manager.lock().await;
            worktree_manager.shutdown().await?;
        }

        // Clear pools
        {
            let mut pools = self.pools.write().await;
            pools.clear();
        }

        tracing::info!("Session pool shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:443-467 method ensure_session_available <-> ./crates/ccswarm/src/session/session_pool.rs:655-676 method start_performance_monitoring_task
  Similarity: 88.21%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:ensure_session_available (lines 443-467) ---[0m
    async fn ensure_session_available(
        &self,
        role_key: &str,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        // Check if we're at max capacity
        let current_count = {
            let pools = self.pools.read().await;
            pools.get(role_key).map(|p| p.len()).unwrap_or(0)
        };

        if current_count >= self.config.max_sessions_per_role {
            // Wait for an available session or fail
            tokio::time::timeout(
                Duration::from_secs(30),
                self.wait_for_available_session(role_key),
            )
            .await?
        } else {
            // Create a new session
            self.create_new_pooled_session(role_key, role, claude_config)
                .await
        }
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_performance_monitoring_task (lines 655-676) ---[0m
    async fn start_performance_monitoring_task(&self) -> tokio::task::JoinHandle<()> {
        let performance_metrics = Arc::clone(&self.performance_metrics);

        tokio::spawn(async move {
            let mut interval = interval(Duration::from_secs(60));

            loop {
                interval.tick().await;

                let metrics = performance_metrics.read().await;
                tracing::info!(
                    "Performance metrics - Total tasks: {}, Avg execution time: {:?}",
                    metrics.total_tasks_processed,
                    if metrics.total_tasks_processed > 0 {
                        metrics.total_execution_time / metrics.total_tasks_processed as u32
                    } else {
                        Duration::ZERO
                    }
                );
            }
        })
    }

  ./crates/ccswarm/src/session/session_pool.rs:564-591 method start_health_check_task <-> ./crates/ccswarm/src/session/session_pool.rs:679-703 method warmup_essential_sessions
  Similarity: 77.74%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_health_check_task (lines 564-591) ---[0m
    async fn start_health_check_task(&self) -> tokio::task::JoinHandle<()> {
        let pools = Arc::clone(&self.pools);
        let interval_duration = self.config.health_check_interval;

        tokio::spawn(async move {
            let mut interval = interval(interval_duration);

            loop {
                interval.tick().await;

                // Perform health checks on all sessions
                let pools_guard = pools.read().await;
                for (role_key, sessions) in pools_guard.iter() {
                    for session in sessions {
                        // Check session health
                        if let Err(e) = Self::health_check_session(session).await {
                            tracing::warn!(
                                "Health check failed for session {} in role {}: {}",
                                session.metadata.agent_id,
                                role_key,
                                e
                            );
                        }
                    }
                }
            }
        })
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:warmup_essential_sessions (lines 679-703) ---[0m
    async fn warmup_essential_sessions(&self) -> Result<()> {
        use crate::identity::{default_backend_role, default_frontend_role};

        tracing::info!("Warming up essential sessions");

        let essential_roles = vec![default_frontend_role(), default_backend_role()];

        for role in essential_roles {
            for _ in 0..self.config.min_sessions_per_role {
                let claude_config = ClaudeConfig::default();
                if let Err(e) = self
                    .create_new_pooled_session(
                        &role.name().to_lowercase(),
                        role.clone(),
                        claude_config,
                    )
                    .await
                {
                    tracing::warn!("Failed to warmup session for role {}: {}", role.name(), e);
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:264-286 method execute_task <-> ./crates/ccswarm/src/session/session_pool.rs:679-703 method warmup_essential_sessions
  Similarity: 84.86%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task (lines 264-286) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let start_time = Instant::now();

        // Get optimal session for the task
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute task
        let result = {
            let mut agent = session.agent.lock().await;
            agent.execute_task(task.clone()).await?
        };

        // Update statistics
        self.update_session_stats(&session, &result, start_time.elapsed())
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:warmup_essential_sessions (lines 679-703) ---[0m
    async fn warmup_essential_sessions(&self) -> Result<()> {
        use crate::identity::{default_backend_role, default_frontend_role};

        tracing::info!("Warming up essential sessions");

        let essential_roles = vec![default_frontend_role(), default_backend_role()];

        for role in essential_roles {
            for _ in 0..self.config.min_sessions_per_role {
                let claude_config = ClaudeConfig::default();
                if let Err(e) = self
                    .create_new_pooled_session(
                        &role.name().to_lowercase(),
                        role.clone(),
                        claude_config,
                    )
                    .await
                {
                    tracing::warn!("Failed to warmup session for role {}: {}", role.name(), e);
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:235-261 method start <-> ./crates/ccswarm/src/session/session_pool.rs:264-286 method execute_task
  Similarity: 81.29%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start (lines 235-261) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting session pool");

        // Start health check task
        let health_check_task = self.start_health_check_task().await;
        self.background_tasks.push(health_check_task);

        // Start auto-scaling task if enabled
        if self.config.auto_scaling.enabled {
            let scaling_task = self.start_auto_scaling_task().await;
            self.background_tasks.push(scaling_task);
        }

        // Start performance monitoring task
        if self.config.enable_performance_monitoring {
            let monitoring_task = self.start_performance_monitoring_task().await;
            self.background_tasks.push(monitoring_task);
        }

        // Pre-warm sessions if using eager strategy
        if matches!(self.config.warmup_strategy, WarmupStrategy::Eager) {
            self.warmup_essential_sessions().await?;
        }

        tracing::info!("Session pool started successfully");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task (lines 264-286) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let start_time = Instant::now();

        // Get optimal session for the task
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute task
        let result = {
            let mut agent = session.agent.lock().await;
            agent.execute_task(task.clone()).await?
        };

        // Update statistics
        self.update_session_stats(&session, &result, start_time.elapsed())
            .await;

        Ok(result)
    }

  ./crates/ccswarm/src/session/session_pool.rs:264-286 method execute_task <-> ./crates/ccswarm/src/session/session_pool.rs:564-591 method start_health_check_task
  Similarity: 79.31%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task (lines 264-286) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let start_time = Instant::now();

        // Get optimal session for the task
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute task
        let result = {
            let mut agent = session.agent.lock().await;
            agent.execute_task(task.clone()).await?
        };

        // Update statistics
        self.update_session_stats(&session, &result, start_time.elapsed())
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_health_check_task (lines 564-591) ---[0m
    async fn start_health_check_task(&self) -> tokio::task::JoinHandle<()> {
        let pools = Arc::clone(&self.pools);
        let interval_duration = self.config.health_check_interval;

        tokio::spawn(async move {
            let mut interval = interval(interval_duration);

            loop {
                interval.tick().await;

                // Perform health checks on all sessions
                let pools_guard = pools.read().await;
                for (role_key, sessions) in pools_guard.iter() {
                    for session in sessions {
                        // Check session health
                        if let Err(e) = Self::health_check_session(session).await {
                            tracing::warn!(
                                "Health check failed for session {} in role {}: {}",
                                session.metadata.agent_id,
                                role_key,
                                e
                            );
                        }
                    }
                }
            }
        })
    }

  ./crates/ccswarm/src/session/session_pool.rs:213-232 method new <-> ./crates/ccswarm/src/session/session_pool.rs:443-467 method ensure_session_available
  Similarity: 88.58%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:new (lines 213-232) ---[0m
    pub async fn new(
        worktree_config: WorktreeSessionConfig,
        pool_config: SessionPoolConfig,
    ) -> Result<Self> {
        let mut worktree_manager = WorktreeSessionManager::new(worktree_config)?;
        worktree_manager.start().await?;

        let pool = Self {
            pools: Arc::new(RwLock::new(HashMap::new())),
            worktree_manager: Arc::new(Mutex::new(worktree_manager)),
            config: pool_config.clone(),
            load_balancer_state: Arc::new(RwLock::new(HashMap::new())),
            performance_metrics: Arc::new(RwLock::new(PerformanceMetrics::default())),
            background_tasks: Vec::new(),
            creation_semaphore: Arc::new(Semaphore::new(pool_config.max_sessions_per_role * 4)),
            scaling_state: Arc::new(RwLock::new(HashMap::new())),
        };

        Ok(pool)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:ensure_session_available (lines 443-467) ---[0m
    async fn ensure_session_available(
        &self,
        role_key: &str,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        // Check if we're at max capacity
        let current_count = {
            let pools = self.pools.read().await;
            pools.get(role_key).map(|p| p.len()).unwrap_or(0)
        };

        if current_count >= self.config.max_sessions_per_role {
            // Wait for an available session or fail
            tokio::time::timeout(
                Duration::from_secs(30),
                self.wait_for_available_session(role_key),
            )
            .await?
        } else {
            // Create a new session
            self.create_new_pooled_session(role_key, role, claude_config)
                .await
        }
    }

  ./crates/ccswarm/src/session/session_pool.rs:443-467 method ensure_session_available <-> ./crates/ccswarm/src/session/session_pool.rs:679-703 method warmup_essential_sessions
  Similarity: 79.55%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:ensure_session_available (lines 443-467) ---[0m
    async fn ensure_session_available(
        &self,
        role_key: &str,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        // Check if we're at max capacity
        let current_count = {
            let pools = self.pools.read().await;
            pools.get(role_key).map(|p| p.len()).unwrap_or(0)
        };

        if current_count >= self.config.max_sessions_per_role {
            // Wait for an available session or fail
            tokio::time::timeout(
                Duration::from_secs(30),
                self.wait_for_available_session(role_key),
            )
            .await?
        } else {
            // Create a new session
            self.create_new_pooled_session(role_key, role, claude_config)
                .await
        }
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:warmup_essential_sessions (lines 679-703) ---[0m
    async fn warmup_essential_sessions(&self) -> Result<()> {
        use crate::identity::{default_backend_role, default_frontend_role};

        tracing::info!("Warming up essential sessions");

        let essential_roles = vec![default_frontend_role(), default_backend_role()];

        for role in essential_roles {
            for _ in 0..self.config.min_sessions_per_role {
                let claude_config = ClaudeConfig::default();
                if let Err(e) = self
                    .create_new_pooled_session(
                        &role.name().to_lowercase(),
                        role.clone(),
                        claude_config,
                    )
                    .await
                {
                    tracing::warn!("Failed to warmup session for role {}: {}", role.name(), e);
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:213-232 method new <-> ./crates/ccswarm/src/session/session_pool.rs:679-703 method warmup_essential_sessions
  Similarity: 88.28%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:new (lines 213-232) ---[0m
    pub async fn new(
        worktree_config: WorktreeSessionConfig,
        pool_config: SessionPoolConfig,
    ) -> Result<Self> {
        let mut worktree_manager = WorktreeSessionManager::new(worktree_config)?;
        worktree_manager.start().await?;

        let pool = Self {
            pools: Arc::new(RwLock::new(HashMap::new())),
            worktree_manager: Arc::new(Mutex::new(worktree_manager)),
            config: pool_config.clone(),
            load_balancer_state: Arc::new(RwLock::new(HashMap::new())),
            performance_metrics: Arc::new(RwLock::new(PerformanceMetrics::default())),
            background_tasks: Vec::new(),
            creation_semaphore: Arc::new(Semaphore::new(pool_config.max_sessions_per_role * 4)),
            scaling_state: Arc::new(RwLock::new(HashMap::new())),
        };

        Ok(pool)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:warmup_essential_sessions (lines 679-703) ---[0m
    async fn warmup_essential_sessions(&self) -> Result<()> {
        use crate::identity::{default_backend_role, default_frontend_role};

        tracing::info!("Warming up essential sessions");

        let essential_roles = vec![default_frontend_role(), default_backend_role()];

        for role in essential_roles {
            for _ in 0..self.config.min_sessions_per_role {
                let claude_config = ClaudeConfig::default();
                if let Err(e) = self
                    .create_new_pooled_session(
                        &role.name().to_lowercase(),
                        role.clone(),
                        claude_config,
                    )
                    .await
                {
                    tracing::warn!("Failed to warmup session for role {}: {}", role.name(), e);
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:264-286 method execute_task <-> ./crates/ccswarm/src/session/session_pool.rs:751-773 method shutdown
  Similarity: 85.84%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task (lines 264-286) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let start_time = Instant::now();

        // Get optimal session for the task
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute task
        let result = {
            let mut agent = session.agent.lock().await;
            agent.execute_task(task.clone()).await?
        };

        // Update statistics
        self.update_session_stats(&session, &result, start_time.elapsed())
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:shutdown (lines 751-773) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down session pool");

        // Cancel background tasks
        for handle in self.background_tasks.drain(..) {
            handle.abort();
        }

        // Shutdown worktree manager
        {
            let mut worktree_manager = self.worktree_manager.lock().await;
            worktree_manager.shutdown().await?;
        }

        // Clear pools
        {
            let mut pools = self.pools.write().await;
            pools.clear();
        }

        tracing::info!("Session pool shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:289-322 method execute_task_batch <-> ./crates/ccswarm/src/session/session_pool.rs:325-342 method get_optimal_session
  Similarity: 73.08%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task_batch (lines 289-322) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        let start_time = Instant::now();

        // For batch operations, prefer a single session for maximum efficiency
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute batch
        let results = {
            let mut agent = session.agent.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Update statistics for batch
        for result in &results {
            self.update_session_stats(
                &session,
                result,
                start_time.elapsed() / results.len() as u32,
            )
            .await;
        }

        tracing::info!("Batch of {} tasks completed in session pool", results.len());
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:get_optimal_session (lines 325-342) ---[0m
    async fn get_optimal_session(
        &self,
        role: &AgentRole,
        claude_config: &ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        let role_key = role.name().to_lowercase();

        // Check if we have available sessions
        let available_session = self.find_available_session(&role_key).await;

        if let Some(session) = available_session {
            return Ok(session);
        }

        // Need to create or wait for a session
        self.ensure_session_available(&role_key, role.clone(), claude_config.clone())
            .await
    }

  ./crates/ccswarm/src/session/session_pool.rs:213-232 method new <-> ./crates/ccswarm/src/session/session_pool.rs:264-286 method execute_task
  Similarity: 88.20%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:new (lines 213-232) ---[0m
    pub async fn new(
        worktree_config: WorktreeSessionConfig,
        pool_config: SessionPoolConfig,
    ) -> Result<Self> {
        let mut worktree_manager = WorktreeSessionManager::new(worktree_config)?;
        worktree_manager.start().await?;

        let pool = Self {
            pools: Arc::new(RwLock::new(HashMap::new())),
            worktree_manager: Arc::new(Mutex::new(worktree_manager)),
            config: pool_config.clone(),
            load_balancer_state: Arc::new(RwLock::new(HashMap::new())),
            performance_metrics: Arc::new(RwLock::new(PerformanceMetrics::default())),
            background_tasks: Vec::new(),
            creation_semaphore: Arc::new(Semaphore::new(pool_config.max_sessions_per_role * 4)),
            scaling_state: Arc::new(RwLock::new(HashMap::new())),
        };

        Ok(pool)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task (lines 264-286) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let start_time = Instant::now();

        // Get optimal session for the task
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute task
        let result = {
            let mut agent = session.agent.lock().await;
            agent.execute_task(task.clone()).await?
        };

        // Update statistics
        self.update_session_stats(&session, &result, start_time.elapsed())
            .await;

        Ok(result)
    }

  ./crates/ccswarm/src/session/session_pool.rs:325-342 method get_optimal_session <-> ./crates/ccswarm/src/session/session_pool.rs:679-703 method warmup_essential_sessions
  Similarity: 87.34%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:get_optimal_session (lines 325-342) ---[0m
    async fn get_optimal_session(
        &self,
        role: &AgentRole,
        claude_config: &ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        let role_key = role.name().to_lowercase();

        // Check if we have available sessions
        let available_session = self.find_available_session(&role_key).await;

        if let Some(session) = available_session {
            return Ok(session);
        }

        // Need to create or wait for a session
        self.ensure_session_available(&role_key, role.clone(), claude_config.clone())
            .await
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:warmup_essential_sessions (lines 679-703) ---[0m
    async fn warmup_essential_sessions(&self) -> Result<()> {
        use crate::identity::{default_backend_role, default_frontend_role};

        tracing::info!("Warming up essential sessions");

        let essential_roles = vec![default_frontend_role(), default_backend_role()];

        for role in essential_roles {
            for _ in 0..self.config.min_sessions_per_role {
                let claude_config = ClaudeConfig::default();
                if let Err(e) = self
                    .create_new_pooled_session(
                        &role.name().to_lowercase(),
                        role.clone(),
                        claude_config,
                    )
                    .await
                {
                    tracing::warn!("Failed to warmup session for role {}: {}", role.name(), e);
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:289-322 method execute_task_batch <-> ./crates/ccswarm/src/session/session_pool.rs:807-823 function test_session_pool_creation
  Similarity: 73.54%

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task_batch (lines 289-322) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        let start_time = Instant::now();

        // For batch operations, prefer a single session for maximum efficiency
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute batch
        let results = {
            let mut agent = session.agent.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Update statistics for batch
        for result in &results {
            self.update_session_stats(
                &session,
                result,
                start_time.elapsed() / results.len() as u32,
            )
            .await;
        }

        tracing::info!("Batch of {} tasks completed in session pool", results.len());
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:test_session_pool_creation (lines 807-823) ---[0m
    async fn test_session_pool_creation() {
        let temp_dir = TempDir::new().unwrap();

        let worktree_config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let pool_config = SessionPoolConfig::default();

        let pool = SessionPool::new(worktree_config, pool_config)
            .await
            .unwrap();

        let stats = pool.get_pool_statistics().await;
        assert_eq!(stats.total_sessions, 0);
    }

  ./crates/ccswarm/src/session/session_pool.rs:264-286 method execute_task <-> ./crates/ccswarm/src/session/session_pool.rs:542-561 method update_session_stats
  Similarity: 85.66%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task (lines 264-286) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let start_time = Instant::now();

        // Get optimal session for the task
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute task
        let result = {
            let mut agent = session.agent.lock().await;
            agent.execute_task(task.clone()).await?
        };

        // Update statistics
        self.update_session_stats(&session, &result, start_time.elapsed())
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:update_session_stats (lines 542-561) ---[0m
    async fn update_session_stats(
        &self,
        session: &Arc<PooledSession>,
        result: &TaskResult,
        execution_time: Duration,
    ) {
        // Note: In a real implementation, we would need mutable access to session stats
        // This is simplified for demonstration purposes
        tracing::debug!(
            "Task completed in session {}: success={}, duration={:?}",
            session.metadata.agent_id,
            result.success,
            execution_time
        );

        // Update global performance metrics
        let mut metrics = self.performance_metrics.write().await;
        metrics.total_tasks_processed += 1;
        metrics.total_execution_time += execution_time;
    }

  ./crates/ccswarm/src/session/session_pool.rs:679-703 method warmup_essential_sessions <-> ./crates/ccswarm/src/session/session_pool.rs:751-773 method shutdown
  Similarity: 76.50%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:warmup_essential_sessions (lines 679-703) ---[0m
    async fn warmup_essential_sessions(&self) -> Result<()> {
        use crate::identity::{default_backend_role, default_frontend_role};

        tracing::info!("Warming up essential sessions");

        let essential_roles = vec![default_frontend_role(), default_backend_role()];

        for role in essential_roles {
            for _ in 0..self.config.min_sessions_per_role {
                let claude_config = ClaudeConfig::default();
                if let Err(e) = self
                    .create_new_pooled_session(
                        &role.name().to_lowercase(),
                        role.clone(),
                        claude_config,
                    )
                    .await
                {
                    tracing::warn!("Failed to warmup session for role {}: {}", role.name(), e);
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:shutdown (lines 751-773) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down session pool");

        // Cancel background tasks
        for handle in self.background_tasks.drain(..) {
            handle.abort();
        }

        // Shutdown worktree manager
        {
            let mut worktree_manager = self.worktree_manager.lock().await;
            worktree_manager.shutdown().await?;
        }

        // Clear pools
        {
            let mut pools = self.pools.write().await;
            pools.clear();
        }

        tracing::info!("Session pool shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:443-467 method ensure_session_available <-> ./crates/ccswarm/src/session/session_pool.rs:542-561 method update_session_stats
  Similarity: 80.86%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:ensure_session_available (lines 443-467) ---[0m
    async fn ensure_session_available(
        &self,
        role_key: &str,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        // Check if we're at max capacity
        let current_count = {
            let pools = self.pools.read().await;
            pools.get(role_key).map(|p| p.len()).unwrap_or(0)
        };

        if current_count >= self.config.max_sessions_per_role {
            // Wait for an available session or fail
            tokio::time::timeout(
                Duration::from_secs(30),
                self.wait_for_available_session(role_key),
            )
            .await?
        } else {
            // Create a new session
            self.create_new_pooled_session(role_key, role, claude_config)
                .await
        }
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:update_session_stats (lines 542-561) ---[0m
    async fn update_session_stats(
        &self,
        session: &Arc<PooledSession>,
        result: &TaskResult,
        execution_time: Duration,
    ) {
        // Note: In a real implementation, we would need mutable access to session stats
        // This is simplified for demonstration purposes
        tracing::debug!(
            "Task completed in session {}: success={}, duration={:?}",
            session.metadata.agent_id,
            result.success,
            execution_time
        );

        // Update global performance metrics
        let mut metrics = self.performance_metrics.write().await;
        metrics.total_tasks_processed += 1;
        metrics.total_execution_time += execution_time;
    }

  ./crates/ccswarm/src/session/session_pool.rs:213-232 method new <-> ./crates/ccswarm/src/session/session_pool.rs:655-676 method start_performance_monitoring_task
  Similarity: 86.48%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:new (lines 213-232) ---[0m
    pub async fn new(
        worktree_config: WorktreeSessionConfig,
        pool_config: SessionPoolConfig,
    ) -> Result<Self> {
        let mut worktree_manager = WorktreeSessionManager::new(worktree_config)?;
        worktree_manager.start().await?;

        let pool = Self {
            pools: Arc::new(RwLock::new(HashMap::new())),
            worktree_manager: Arc::new(Mutex::new(worktree_manager)),
            config: pool_config.clone(),
            load_balancer_state: Arc::new(RwLock::new(HashMap::new())),
            performance_metrics: Arc::new(RwLock::new(PerformanceMetrics::default())),
            background_tasks: Vec::new(),
            creation_semaphore: Arc::new(Semaphore::new(pool_config.max_sessions_per_role * 4)),
            scaling_state: Arc::new(RwLock::new(HashMap::new())),
        };

        Ok(pool)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_performance_monitoring_task (lines 655-676) ---[0m
    async fn start_performance_monitoring_task(&self) -> tokio::task::JoinHandle<()> {
        let performance_metrics = Arc::clone(&self.performance_metrics);

        tokio::spawn(async move {
            let mut interval = interval(Duration::from_secs(60));

            loop {
                interval.tick().await;

                let metrics = performance_metrics.read().await;
                tracing::info!(
                    "Performance metrics - Total tasks: {}, Avg execution time: {:?}",
                    metrics.total_tasks_processed,
                    if metrics.total_tasks_processed > 0 {
                        metrics.total_execution_time / metrics.total_tasks_processed as u32
                    } else {
                        Duration::ZERO
                    }
                );
            }
        })
    }

  ./crates/ccswarm/src/session/session_pool.rs:425-440 method calculate_session_score <-> ./crates/ccswarm/src/session/session_pool.rs:679-703 method warmup_essential_sessions
  Similarity: 88.31%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:calculate_session_score (lines 425-440) ---[0m
    fn calculate_session_score(&self, session: &PooledSession) -> f64 {
        let load_factor = 1.0 - session.stats.current_load;
        let success_rate = if session.stats.total_tasks_executed > 0 {
            session.stats.successful_tasks as f64 / session.stats.total_tasks_executed as f64
        } else {
            1.0
        };
        let speed_factor = if session.stats.average_execution_time.as_millis() > 0 {
            1000.0 / session.stats.average_execution_time.as_millis() as f64
        } else {
            1.0
        };

        // Weighted score combining load, success rate, and speed
        (load_factor * 0.4) + (success_rate * 0.4) + (speed_factor.min(2.0) * 0.2)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:warmup_essential_sessions (lines 679-703) ---[0m
    async fn warmup_essential_sessions(&self) -> Result<()> {
        use crate::identity::{default_backend_role, default_frontend_role};

        tracing::info!("Warming up essential sessions");

        let essential_roles = vec![default_frontend_role(), default_backend_role()];

        for role in essential_roles {
            for _ in 0..self.config.min_sessions_per_role {
                let claude_config = ClaudeConfig::default();
                if let Err(e) = self
                    .create_new_pooled_session(
                        &role.name().to_lowercase(),
                        role.clone(),
                        claude_config,
                    )
                    .await
                {
                    tracing::warn!("Failed to warmup session for role {}: {}", role.name(), e);
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:289-322 method execute_task_batch <-> ./crates/ccswarm/src/session/session_pool.rs:425-440 method calculate_session_score
  Similarity: 72.12%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task_batch (lines 289-322) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        let start_time = Instant::now();

        // For batch operations, prefer a single session for maximum efficiency
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute batch
        let results = {
            let mut agent = session.agent.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Update statistics for batch
        for result in &results {
            self.update_session_stats(
                &session,
                result,
                start_time.elapsed() / results.len() as u32,
            )
            .await;
        }

        tracing::info!("Batch of {} tasks completed in session pool", results.len());
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:calculate_session_score (lines 425-440) ---[0m
    fn calculate_session_score(&self, session: &PooledSession) -> f64 {
        let load_factor = 1.0 - session.stats.current_load;
        let success_rate = if session.stats.total_tasks_executed > 0 {
            session.stats.successful_tasks as f64 / session.stats.total_tasks_executed as f64
        } else {
            1.0
        };
        let speed_factor = if session.stats.average_execution_time.as_millis() > 0 {
            1000.0 / session.stats.average_execution_time.as_millis() as f64
        } else {
            1.0
        };

        // Weighted score combining load, success rate, and speed
        (load_factor * 0.4) + (success_rate * 0.4) + (speed_factor.min(2.0) * 0.2)
    }

  ./crates/ccswarm/src/session/session_pool.rs:425-440 method calculate_session_score <-> ./crates/ccswarm/src/session/session_pool.rs:564-591 method start_health_check_task
  Similarity: 81.38%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:calculate_session_score (lines 425-440) ---[0m
    fn calculate_session_score(&self, session: &PooledSession) -> f64 {
        let load_factor = 1.0 - session.stats.current_load;
        let success_rate = if session.stats.total_tasks_executed > 0 {
            session.stats.successful_tasks as f64 / session.stats.total_tasks_executed as f64
        } else {
            1.0
        };
        let speed_factor = if session.stats.average_execution_time.as_millis() > 0 {
            1000.0 / session.stats.average_execution_time.as_millis() as f64
        } else {
            1.0
        };

        // Weighted score combining load, success rate, and speed
        (load_factor * 0.4) + (success_rate * 0.4) + (speed_factor.min(2.0) * 0.2)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_health_check_task (lines 564-591) ---[0m
    async fn start_health_check_task(&self) -> tokio::task::JoinHandle<()> {
        let pools = Arc::clone(&self.pools);
        let interval_duration = self.config.health_check_interval;

        tokio::spawn(async move {
            let mut interval = interval(interval_duration);

            loop {
                interval.tick().await;

                // Perform health checks on all sessions
                let pools_guard = pools.read().await;
                for (role_key, sessions) in pools_guard.iter() {
                    for session in sessions {
                        // Check session health
                        if let Err(e) = Self::health_check_session(session).await {
                            tracing::warn!(
                                "Health check failed for session {} in role {}: {}",
                                session.metadata.agent_id,
                                role_key,
                                e
                            );
                        }
                    }
                }
            }
        })
    }

  ./crates/ccswarm/src/session/session_pool.rs:325-342 method get_optimal_session <-> ./crates/ccswarm/src/session/session_pool.rs:564-591 method start_health_check_task
  Similarity: 76.78%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:get_optimal_session (lines 325-342) ---[0m
    async fn get_optimal_session(
        &self,
        role: &AgentRole,
        claude_config: &ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        let role_key = role.name().to_lowercase();

        // Check if we have available sessions
        let available_session = self.find_available_session(&role_key).await;

        if let Some(session) = available_session {
            return Ok(session);
        }

        // Need to create or wait for a session
        self.ensure_session_available(&role_key, role.clone(), claude_config.clone())
            .await
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_health_check_task (lines 564-591) ---[0m
    async fn start_health_check_task(&self) -> tokio::task::JoinHandle<()> {
        let pools = Arc::clone(&self.pools);
        let interval_duration = self.config.health_check_interval;

        tokio::spawn(async move {
            let mut interval = interval(interval_duration);

            loop {
                interval.tick().await;

                // Perform health checks on all sessions
                let pools_guard = pools.read().await;
                for (role_key, sessions) in pools_guard.iter() {
                    for session in sessions {
                        // Check session health
                        if let Err(e) = Self::health_check_session(session).await {
                            tracing::warn!(
                                "Health check failed for session {} in role {}: {}",
                                session.metadata.agent_id,
                                role_key,
                                e
                            );
                        }
                    }
                }
            }
        })
    }

  ./crates/ccswarm/src/session/session_pool.rs:264-286 method execute_task <-> ./crates/ccswarm/src/session/session_pool.rs:325-342 method get_optimal_session
  Similarity: 85.19%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task (lines 264-286) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let start_time = Instant::now();

        // Get optimal session for the task
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute task
        let result = {
            let mut agent = session.agent.lock().await;
            agent.execute_task(task.clone()).await?
        };

        // Update statistics
        self.update_session_stats(&session, &result, start_time.elapsed())
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:get_optimal_session (lines 325-342) ---[0m
    async fn get_optimal_session(
        &self,
        role: &AgentRole,
        claude_config: &ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        let role_key = role.name().to_lowercase();

        // Check if we have available sessions
        let available_session = self.find_available_session(&role_key).await;

        if let Some(session) = available_session {
            return Ok(session);
        }

        // Need to create or wait for a session
        self.ensure_session_available(&role_key, role.clone(), claude_config.clone())
            .await
    }

  ./crates/ccswarm/src/session/session_pool.rs:655-676 method start_performance_monitoring_task <-> ./crates/ccswarm/src/session/session_pool.rs:679-703 method warmup_essential_sessions
  Similarity: 71.07%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_performance_monitoring_task (lines 655-676) ---[0m
    async fn start_performance_monitoring_task(&self) -> tokio::task::JoinHandle<()> {
        let performance_metrics = Arc::clone(&self.performance_metrics);

        tokio::spawn(async move {
            let mut interval = interval(Duration::from_secs(60));

            loop {
                interval.tick().await;

                let metrics = performance_metrics.read().await;
                tracing::info!(
                    "Performance metrics - Total tasks: {}, Avg execution time: {:?}",
                    metrics.total_tasks_processed,
                    if metrics.total_tasks_processed > 0 {
                        metrics.total_execution_time / metrics.total_tasks_processed as u32
                    } else {
                        Duration::ZERO
                    }
                );
            }
        })
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:warmup_essential_sessions (lines 679-703) ---[0m
    async fn warmup_essential_sessions(&self) -> Result<()> {
        use crate::identity::{default_backend_role, default_frontend_role};

        tracing::info!("Warming up essential sessions");

        let essential_roles = vec![default_frontend_role(), default_backend_role()];

        for role in essential_roles {
            for _ in 0..self.config.min_sessions_per_role {
                let claude_config = ClaudeConfig::default();
                if let Err(e) = self
                    .create_new_pooled_session(
                        &role.name().to_lowercase(),
                        role.clone(),
                        claude_config,
                    )
                    .await
                {
                    tracing::warn!("Failed to warmup session for role {}: {}", role.name(), e);
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:213-232 method new <-> ./crates/ccswarm/src/session/session_pool.rs:425-440 method calculate_session_score
  Similarity: 91.93%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:new (lines 213-232) ---[0m
    pub async fn new(
        worktree_config: WorktreeSessionConfig,
        pool_config: SessionPoolConfig,
    ) -> Result<Self> {
        let mut worktree_manager = WorktreeSessionManager::new(worktree_config)?;
        worktree_manager.start().await?;

        let pool = Self {
            pools: Arc::new(RwLock::new(HashMap::new())),
            worktree_manager: Arc::new(Mutex::new(worktree_manager)),
            config: pool_config.clone(),
            load_balancer_state: Arc::new(RwLock::new(HashMap::new())),
            performance_metrics: Arc::new(RwLock::new(PerformanceMetrics::default())),
            background_tasks: Vec::new(),
            creation_semaphore: Arc::new(Semaphore::new(pool_config.max_sessions_per_role * 4)),
            scaling_state: Arc::new(RwLock::new(HashMap::new())),
        };

        Ok(pool)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:calculate_session_score (lines 425-440) ---[0m
    fn calculate_session_score(&self, session: &PooledSession) -> f64 {
        let load_factor = 1.0 - session.stats.current_load;
        let success_rate = if session.stats.total_tasks_executed > 0 {
            session.stats.successful_tasks as f64 / session.stats.total_tasks_executed as f64
        } else {
            1.0
        };
        let speed_factor = if session.stats.average_execution_time.as_millis() > 0 {
            1000.0 / session.stats.average_execution_time.as_millis() as f64
        } else {
            1.0
        };

        // Weighted score combining load, success rate, and speed
        (load_factor * 0.4) + (success_rate * 0.4) + (speed_factor.min(2.0) * 0.2)
    }

  ./crates/ccswarm/src/session/session_pool.rs:264-286 method execute_task <-> ./crates/ccswarm/src/session/session_pool.rs:807-823 function test_session_pool_creation
  Similarity: 82.40%

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task (lines 264-286) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let start_time = Instant::now();

        // Get optimal session for the task
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute task
        let result = {
            let mut agent = session.agent.lock().await;
            agent.execute_task(task.clone()).await?
        };

        // Update statistics
        self.update_session_stats(&session, &result, start_time.elapsed())
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:test_session_pool_creation (lines 807-823) ---[0m
    async fn test_session_pool_creation() {
        let temp_dir = TempDir::new().unwrap();

        let worktree_config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let pool_config = SessionPoolConfig::default();

        let pool = SessionPool::new(worktree_config, pool_config)
            .await
            .unwrap();

        let stats = pool.get_pool_statistics().await;
        assert_eq!(stats.total_sessions, 0);
    }

  ./crates/ccswarm/src/session/session_pool.rs:679-703 method warmup_essential_sessions <-> ./crates/ccswarm/src/session/session_pool.rs:807-823 function test_session_pool_creation
  Similarity: 78.31%

[36m--- ./crates/ccswarm/src/session/session_pool.rs:warmup_essential_sessions (lines 679-703) ---[0m
    async fn warmup_essential_sessions(&self) -> Result<()> {
        use crate::identity::{default_backend_role, default_frontend_role};

        tracing::info!("Warming up essential sessions");

        let essential_roles = vec![default_frontend_role(), default_backend_role()];

        for role in essential_roles {
            for _ in 0..self.config.min_sessions_per_role {
                let claude_config = ClaudeConfig::default();
                if let Err(e) = self
                    .create_new_pooled_session(
                        &role.name().to_lowercase(),
                        role.clone(),
                        claude_config,
                    )
                    .await
                {
                    tracing::warn!("Failed to warmup session for role {}: {}", role.name(), e);
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:test_session_pool_creation (lines 807-823) ---[0m
    async fn test_session_pool_creation() {
        let temp_dir = TempDir::new().unwrap();

        let worktree_config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let pool_config = SessionPoolConfig::default();

        let pool = SessionPool::new(worktree_config, pool_config)
            .await
            .unwrap();

        let stats = pool.get_pool_statistics().await;
        assert_eq!(stats.total_sessions, 0);
    }

  ./crates/ccswarm/src/session/session_pool.rs:264-286 method execute_task <-> ./crates/ccswarm/src/session/session_pool.rs:425-440 method calculate_session_score
  Similarity: 83.88%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task (lines 264-286) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let start_time = Instant::now();

        // Get optimal session for the task
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute task
        let result = {
            let mut agent = session.agent.lock().await;
            agent.execute_task(task.clone()).await?
        };

        // Update statistics
        self.update_session_stats(&session, &result, start_time.elapsed())
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:calculate_session_score (lines 425-440) ---[0m
    fn calculate_session_score(&self, session: &PooledSession) -> f64 {
        let load_factor = 1.0 - session.stats.current_load;
        let success_rate = if session.stats.total_tasks_executed > 0 {
            session.stats.successful_tasks as f64 / session.stats.total_tasks_executed as f64
        } else {
            1.0
        };
        let speed_factor = if session.stats.average_execution_time.as_millis() > 0 {
            1000.0 / session.stats.average_execution_time.as_millis() as f64
        } else {
            1.0
        };

        // Weighted score combining load, success rate, and speed
        (load_factor * 0.4) + (success_rate * 0.4) + (speed_factor.min(2.0) * 0.2)
    }

  ./crates/ccswarm/src/session/session_pool.rs:325-342 method get_optimal_session <-> ./crates/ccswarm/src/session/session_pool.rs:751-773 method shutdown
  Similarity: 77.11%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:get_optimal_session (lines 325-342) ---[0m
    async fn get_optimal_session(
        &self,
        role: &AgentRole,
        claude_config: &ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        let role_key = role.name().to_lowercase();

        // Check if we have available sessions
        let available_session = self.find_available_session(&role_key).await;

        if let Some(session) = available_session {
            return Ok(session);
        }

        // Need to create or wait for a session
        self.ensure_session_available(&role_key, role.clone(), claude_config.clone())
            .await
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:shutdown (lines 751-773) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down session pool");

        // Cancel background tasks
        for handle in self.background_tasks.drain(..) {
            handle.abort();
        }

        // Shutdown worktree manager
        {
            let mut worktree_manager = self.worktree_manager.lock().await;
            worktree_manager.shutdown().await?;
        }

        // Clear pools
        {
            let mut pools = self.pools.write().await;
            pools.clear();
        }

        tracing::info!("Session pool shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:542-561 method update_session_stats <-> ./crates/ccswarm/src/session/session_pool.rs:655-676 method start_performance_monitoring_task
  Similarity: 72.76%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:update_session_stats (lines 542-561) ---[0m
    async fn update_session_stats(
        &self,
        session: &Arc<PooledSession>,
        result: &TaskResult,
        execution_time: Duration,
    ) {
        // Note: In a real implementation, we would need mutable access to session stats
        // This is simplified for demonstration purposes
        tracing::debug!(
            "Task completed in session {}: success={}, duration={:?}",
            session.metadata.agent_id,
            result.success,
            execution_time
        );

        // Update global performance metrics
        let mut metrics = self.performance_metrics.write().await;
        metrics.total_tasks_processed += 1;
        metrics.total_execution_time += execution_time;
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_performance_monitoring_task (lines 655-676) ---[0m
    async fn start_performance_monitoring_task(&self) -> tokio::task::JoinHandle<()> {
        let performance_metrics = Arc::clone(&self.performance_metrics);

        tokio::spawn(async move {
            let mut interval = interval(Duration::from_secs(60));

            loop {
                interval.tick().await;

                let metrics = performance_metrics.read().await;
                tracing::info!(
                    "Performance metrics - Total tasks: {}, Avg execution time: {:?}",
                    metrics.total_tasks_processed,
                    if metrics.total_tasks_processed > 0 {
                        metrics.total_execution_time / metrics.total_tasks_processed as u32
                    } else {
                        Duration::ZERO
                    }
                );
            }
        })
    }

  ./crates/ccswarm/src/session/session_pool.rs:542-561 method update_session_stats <-> ./crates/ccswarm/src/session/session_pool.rs:807-823 function test_session_pool_creation
  Similarity: 82.19%

[36m--- ./crates/ccswarm/src/session/session_pool.rs:update_session_stats (lines 542-561) ---[0m
    async fn update_session_stats(
        &self,
        session: &Arc<PooledSession>,
        result: &TaskResult,
        execution_time: Duration,
    ) {
        // Note: In a real implementation, we would need mutable access to session stats
        // This is simplified for demonstration purposes
        tracing::debug!(
            "Task completed in session {}: success={}, duration={:?}",
            session.metadata.agent_id,
            result.success,
            execution_time
        );

        // Update global performance metrics
        let mut metrics = self.performance_metrics.write().await;
        metrics.total_tasks_processed += 1;
        metrics.total_execution_time += execution_time;
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:test_session_pool_creation (lines 807-823) ---[0m
    async fn test_session_pool_creation() {
        let temp_dir = TempDir::new().unwrap();

        let worktree_config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let pool_config = SessionPoolConfig::default();

        let pool = SessionPool::new(worktree_config, pool_config)
            .await
            .unwrap();

        let stats = pool.get_pool_statistics().await;
        assert_eq!(stats.total_sessions, 0);
    }

  ./crates/ccswarm/src/session/session_pool.rs:470-480 method wait_for_available_session <-> ./crates/ccswarm/src/session/session_pool.rs:655-676 method start_performance_monitoring_task
  Similarity: 91.64%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:wait_for_available_session (lines 470-480) ---[0m
    async fn wait_for_available_session(&self, role_key: &str) -> Result<Arc<PooledSession>> {
        let mut interval = interval(Duration::from_millis(100));

        loop {
            interval.tick().await;

            if let Some(session) = self.find_available_session(role_key).await {
                return Ok(session);
            }
        }
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_performance_monitoring_task (lines 655-676) ---[0m
    async fn start_performance_monitoring_task(&self) -> tokio::task::JoinHandle<()> {
        let performance_metrics = Arc::clone(&self.performance_metrics);

        tokio::spawn(async move {
            let mut interval = interval(Duration::from_secs(60));

            loop {
                interval.tick().await;

                let metrics = performance_metrics.read().await;
                tracing::info!(
                    "Performance metrics - Total tasks: {}, Avg execution time: {:?}",
                    metrics.total_tasks_processed,
                    if metrics.total_tasks_processed > 0 {
                        metrics.total_execution_time / metrics.total_tasks_processed as u32
                    } else {
                        Duration::ZERO
                    }
                );
            }
        })
    }

  ./crates/ccswarm/src/session/session_pool.rs:213-232 method new <-> ./crates/ccswarm/src/session/session_pool.rs:807-823 function test_session_pool_creation
  Similarity: 81.03%

[36m--- ./crates/ccswarm/src/session/session_pool.rs:new (lines 213-232) ---[0m
    pub async fn new(
        worktree_config: WorktreeSessionConfig,
        pool_config: SessionPoolConfig,
    ) -> Result<Self> {
        let mut worktree_manager = WorktreeSessionManager::new(worktree_config)?;
        worktree_manager.start().await?;

        let pool = Self {
            pools: Arc::new(RwLock::new(HashMap::new())),
            worktree_manager: Arc::new(Mutex::new(worktree_manager)),
            config: pool_config.clone(),
            load_balancer_state: Arc::new(RwLock::new(HashMap::new())),
            performance_metrics: Arc::new(RwLock::new(PerformanceMetrics::default())),
            background_tasks: Vec::new(),
            creation_semaphore: Arc::new(Semaphore::new(pool_config.max_sessions_per_role * 4)),
            scaling_state: Arc::new(RwLock::new(HashMap::new())),
        };

        Ok(pool)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:test_session_pool_creation (lines 807-823) ---[0m
    async fn test_session_pool_creation() {
        let temp_dir = TempDir::new().unwrap();

        let worktree_config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let pool_config = SessionPoolConfig::default();

        let pool = SessionPool::new(worktree_config, pool_config)
            .await
            .unwrap();

        let stats = pool.get_pool_statistics().await;
        assert_eq!(stats.total_sessions, 0);
    }

  ./crates/ccswarm/src/session/session_pool.rs:443-467 method ensure_session_available <-> ./crates/ccswarm/src/session/session_pool.rs:594-604 method health_check_session
  Similarity: 82.21%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:ensure_session_available (lines 443-467) ---[0m
    async fn ensure_session_available(
        &self,
        role_key: &str,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        // Check if we're at max capacity
        let current_count = {
            let pools = self.pools.read().await;
            pools.get(role_key).map(|p| p.len()).unwrap_or(0)
        };

        if current_count >= self.config.max_sessions_per_role {
            // Wait for an available session or fail
            tokio::time::timeout(
                Duration::from_secs(30),
                self.wait_for_available_session(role_key),
            )
            .await?
        } else {
            // Create a new session
            self.create_new_pooled_session(role_key, role, claude_config)
                .await
        }
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:health_check_session (lines 594-604) ---[0m
    async fn health_check_session(session: &PooledSession) -> Result<()> {
        let agent = session.agent.lock().await;
        let stats = agent.get_session_stats().await;

        if !stats.is_active {
            return Err(anyhow::anyhow!("Session is not active"));
        }

        // Additional health checks could be added here
        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:325-342 method get_optimal_session <-> ./crates/ccswarm/src/session/session_pool.rs:542-561 method update_session_stats
  Similarity: 77.82%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:get_optimal_session (lines 325-342) ---[0m
    async fn get_optimal_session(
        &self,
        role: &AgentRole,
        claude_config: &ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        let role_key = role.name().to_lowercase();

        // Check if we have available sessions
        let available_session = self.find_available_session(&role_key).await;

        if let Some(session) = available_session {
            return Ok(session);
        }

        // Need to create or wait for a session
        self.ensure_session_available(&role_key, role.clone(), claude_config.clone())
            .await
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:update_session_stats (lines 542-561) ---[0m
    async fn update_session_stats(
        &self,
        session: &Arc<PooledSession>,
        result: &TaskResult,
        execution_time: Duration,
    ) {
        // Note: In a real implementation, we would need mutable access to session stats
        // This is simplified for demonstration purposes
        tracing::debug!(
            "Task completed in session {}: success={}, duration={:?}",
            session.metadata.agent_id,
            result.success,
            execution_time
        );

        // Update global performance metrics
        let mut metrics = self.performance_metrics.write().await;
        metrics.total_tasks_processed += 1;
        metrics.total_execution_time += execution_time;
    }

  ./crates/ccswarm/src/session/session_pool.rs:443-467 method ensure_session_available <-> ./crates/ccswarm/src/session/session_pool.rs:645-652 method calculate_role_load
  Similarity: 88.29%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:ensure_session_available (lines 443-467) ---[0m
    async fn ensure_session_available(
        &self,
        role_key: &str,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        // Check if we're at max capacity
        let current_count = {
            let pools = self.pools.read().await;
            pools.get(role_key).map(|p| p.len()).unwrap_or(0)
        };

        if current_count >= self.config.max_sessions_per_role {
            // Wait for an available session or fail
            tokio::time::timeout(
                Duration::from_secs(30),
                self.wait_for_available_session(role_key),
            )
            .await?
        } else {
            // Create a new session
            self.create_new_pooled_session(role_key, role, claude_config)
                .await
        }
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:calculate_role_load (lines 645-652) ---[0m
    fn calculate_role_load(sessions: &[PooledSession]) -> f64 {
        if sessions.is_empty() {
            return 0.0;
        }

        let total_load: f64 = sessions.iter().map(|s| s.stats.current_load).sum();
        total_load / sessions.len() as f64
    }

  ./crates/ccswarm/src/session/session_pool.rs:443-467 method ensure_session_available <-> ./crates/ccswarm/src/session/session_pool.rs:470-480 method wait_for_available_session
  Similarity: 80.86%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:ensure_session_available (lines 443-467) ---[0m
    async fn ensure_session_available(
        &self,
        role_key: &str,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        // Check if we're at max capacity
        let current_count = {
            let pools = self.pools.read().await;
            pools.get(role_key).map(|p| p.len()).unwrap_or(0)
        };

        if current_count >= self.config.max_sessions_per_role {
            // Wait for an available session or fail
            tokio::time::timeout(
                Duration::from_secs(30),
                self.wait_for_available_session(role_key),
            )
            .await?
        } else {
            // Create a new session
            self.create_new_pooled_session(role_key, role, claude_config)
                .await
        }
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:wait_for_available_session (lines 470-480) ---[0m
    async fn wait_for_available_session(&self, role_key: &str) -> Result<Arc<PooledSession>> {
        let mut interval = interval(Duration::from_millis(100));

        loop {
            interval.tick().await;

            if let Some(session) = self.find_available_session(role_key).await {
                return Ok(session);
            }
        }
    }

  ./crates/ccswarm/src/session/session_pool.rs:325-342 method get_optimal_session <-> ./crates/ccswarm/src/session/session_pool.rs:425-440 method calculate_session_score
  Similarity: 83.09%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:get_optimal_session (lines 325-342) ---[0m
    async fn get_optimal_session(
        &self,
        role: &AgentRole,
        claude_config: &ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        let role_key = role.name().to_lowercase();

        // Check if we have available sessions
        let available_session = self.find_available_session(&role_key).await;

        if let Some(session) = available_session {
            return Ok(session);
        }

        // Need to create or wait for a session
        self.ensure_session_available(&role_key, role.clone(), claude_config.clone())
            .await
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:calculate_session_score (lines 425-440) ---[0m
    fn calculate_session_score(&self, session: &PooledSession) -> f64 {
        let load_factor = 1.0 - session.stats.current_load;
        let success_rate = if session.stats.total_tasks_executed > 0 {
            session.stats.successful_tasks as f64 / session.stats.total_tasks_executed as f64
        } else {
            1.0
        };
        let speed_factor = if session.stats.average_execution_time.as_millis() > 0 {
            1000.0 / session.stats.average_execution_time.as_millis() as f64
        } else {
            1.0
        };

        // Weighted score combining load, success rate, and speed
        (load_factor * 0.4) + (success_rate * 0.4) + (speed_factor.min(2.0) * 0.2)
    }

  ./crates/ccswarm/src/session/session_pool.rs:62-74 method default <-> ./crates/ccswarm/src/session/session_pool.rs:131-149 method default
  Similarity: 87.59%
  Classes: Default <-> Default

[36m--- ./crates/ccswarm/src/session/session_pool.rs:default (lines 62-74) ---[0m
    fn default() -> Self {
        Self {
            total_tasks_executed: 0,
            successful_tasks: 0,
            failed_tasks: 0,
            total_execution_time: Duration::ZERO,
            average_execution_time: Duration::ZERO,
            current_load: 0.0,
            peak_load: 0.0,
            uptime: Duration::ZERO,
            last_activity: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:default (lines 131-149) ---[0m
    fn default() -> Self {
        Self {
            min_sessions_per_role: 1,
            max_sessions_per_role: 5,
            max_concurrent_tasks_per_session: 3,
            health_check_interval: Duration::from_secs(30),
            warmup_strategy: WarmupStrategy::Lazy,
            load_balancing: LoadBalancingStrategy::LeastLoaded,
            auto_scaling: AutoScalingConfig {
                enabled: true,
                scale_up_threshold: 0.8,
                scale_down_threshold: 0.3,
                scale_up_cooldown: Duration::from_secs(60),
                scale_down_cooldown: Duration::from_secs(300),
                target_load: 0.6,
            },
            enable_performance_monitoring: true,
        }
    }

  ./crates/ccswarm/src/session/session_pool.rs:264-286 method execute_task <-> ./crates/ccswarm/src/session/session_pool.rs:594-604 method health_check_session
  Similarity: 81.24%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:execute_task (lines 264-286) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let start_time = Instant::now();

        // Get optimal session for the task
        let session = self.get_optimal_session(&role, &claude_config).await?;

        // Execute task
        let result = {
            let mut agent = session.agent.lock().await;
            agent.execute_task(task.clone()).await?
        };

        // Update statistics
        self.update_session_stats(&session, &result, start_time.elapsed())
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:health_check_session (lines 594-604) ---[0m
    async fn health_check_session(session: &PooledSession) -> Result<()> {
        let agent = session.agent.lock().await;
        let stats = agent.get_session_stats().await;

        if !stats.is_active {
            return Err(anyhow::anyhow!("Session is not active"));
        }

        // Additional health checks could be added here
        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:325-342 method get_optimal_session <-> ./crates/ccswarm/src/session/session_pool.rs:807-823 function test_session_pool_creation
  Similarity: 77.03%

[36m--- ./crates/ccswarm/src/session/session_pool.rs:get_optimal_session (lines 325-342) ---[0m
    async fn get_optimal_session(
        &self,
        role: &AgentRole,
        claude_config: &ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        let role_key = role.name().to_lowercase();

        // Check if we have available sessions
        let available_session = self.find_available_session(&role_key).await;

        if let Some(session) = available_session {
            return Ok(session);
        }

        // Need to create or wait for a session
        self.ensure_session_available(&role_key, role.clone(), claude_config.clone())
            .await
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:test_session_pool_creation (lines 807-823) ---[0m
    async fn test_session_pool_creation() {
        let temp_dir = TempDir::new().unwrap();

        let worktree_config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let pool_config = SessionPoolConfig::default();

        let pool = SessionPool::new(worktree_config, pool_config)
            .await
            .unwrap();

        let stats = pool.get_pool_statistics().await;
        assert_eq!(stats.total_sessions, 0);
    }

  ./crates/ccswarm/src/session/session_pool.rs:425-440 method calculate_session_score <-> ./crates/ccswarm/src/session/session_pool.rs:807-823 function test_session_pool_creation
  Similarity: 78.16%

[36m--- ./crates/ccswarm/src/session/session_pool.rs:calculate_session_score (lines 425-440) ---[0m
    fn calculate_session_score(&self, session: &PooledSession) -> f64 {
        let load_factor = 1.0 - session.stats.current_load;
        let success_rate = if session.stats.total_tasks_executed > 0 {
            session.stats.successful_tasks as f64 / session.stats.total_tasks_executed as f64
        } else {
            1.0
        };
        let speed_factor = if session.stats.average_execution_time.as_millis() > 0 {
            1000.0 / session.stats.average_execution_time.as_millis() as f64
        } else {
            1.0
        };

        // Weighted score combining load, success rate, and speed
        (load_factor * 0.4) + (success_rate * 0.4) + (speed_factor.min(2.0) * 0.2)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:test_session_pool_creation (lines 807-823) ---[0m
    async fn test_session_pool_creation() {
        let temp_dir = TempDir::new().unwrap();

        let worktree_config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let pool_config = SessionPoolConfig::default();

        let pool = SessionPool::new(worktree_config, pool_config)
            .await
            .unwrap();

        let stats = pool.get_pool_statistics().await;
        assert_eq!(stats.total_sessions, 0);
    }

  ./crates/ccswarm/src/session/session_pool.rs:594-604 method health_check_session <-> ./crates/ccswarm/src/session/session_pool.rs:751-773 method shutdown
  Similarity: 74.43%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:health_check_session (lines 594-604) ---[0m
    async fn health_check_session(session: &PooledSession) -> Result<()> {
        let agent = session.agent.lock().await;
        let stats = agent.get_session_stats().await;

        if !stats.is_active {
            return Err(anyhow::anyhow!("Session is not active"));
        }

        // Additional health checks could be added here
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:shutdown (lines 751-773) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down session pool");

        // Cancel background tasks
        for handle in self.background_tasks.drain(..) {
            handle.abort();
        }

        // Shutdown worktree manager
        {
            let mut worktree_manager = self.worktree_manager.lock().await;
            worktree_manager.shutdown().await?;
        }

        // Clear pools
        {
            let mut pools = self.pools.write().await;
            pools.clear();
        }

        tracing::info!("Session pool shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:325-342 method get_optimal_session <-> ./crates/ccswarm/src/session/session_pool.rs:594-604 method health_check_session
  Similarity: 82.08%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:get_optimal_session (lines 325-342) ---[0m
    async fn get_optimal_session(
        &self,
        role: &AgentRole,
        claude_config: &ClaudeConfig,
    ) -> Result<Arc<PooledSession>> {
        let role_key = role.name().to_lowercase();

        // Check if we have available sessions
        let available_session = self.find_available_session(&role_key).await;

        if let Some(session) = available_session {
            return Ok(session);
        }

        // Need to create or wait for a session
        self.ensure_session_available(&role_key, role.clone(), claude_config.clone())
            .await
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:health_check_session (lines 594-604) ---[0m
    async fn health_check_session(session: &PooledSession) -> Result<()> {
        let agent = session.agent.lock().await;
        let stats = agent.get_session_stats().await;

        if !stats.is_active {
            return Err(anyhow::anyhow!("Session is not active"));
        }

        // Additional health checks could be added here
        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:645-652 method calculate_role_load <-> ./crates/ccswarm/src/session/session_pool.rs:655-676 method start_performance_monitoring_task
  Similarity: 78.73%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:calculate_role_load (lines 645-652) ---[0m
    fn calculate_role_load(sessions: &[PooledSession]) -> f64 {
        if sessions.is_empty() {
            return 0.0;
        }

        let total_load: f64 = sessions.iter().map(|s| s.stats.current_load).sum();
        total_load / sessions.len() as f64
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:start_performance_monitoring_task (lines 655-676) ---[0m
    async fn start_performance_monitoring_task(&self) -> tokio::task::JoinHandle<()> {
        let performance_metrics = Arc::clone(&self.performance_metrics);

        tokio::spawn(async move {
            let mut interval = interval(Duration::from_secs(60));

            loop {
                interval.tick().await;

                let metrics = performance_metrics.read().await;
                tracing::info!(
                    "Performance metrics - Total tasks: {}, Avg execution time: {:?}",
                    metrics.total_tasks_processed,
                    if metrics.total_tasks_processed > 0 {
                        metrics.total_execution_time / metrics.total_tasks_processed as u32
                    } else {
                        Duration::ZERO
                    }
                );
            }
        })
    }

  ./crates/ccswarm/src/session/session_pool.rs:425-440 method calculate_session_score <-> ./crates/ccswarm/src/session/session_pool.rs:594-604 method health_check_session
  Similarity: 83.36%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:calculate_session_score (lines 425-440) ---[0m
    fn calculate_session_score(&self, session: &PooledSession) -> f64 {
        let load_factor = 1.0 - session.stats.current_load;
        let success_rate = if session.stats.total_tasks_executed > 0 {
            session.stats.successful_tasks as f64 / session.stats.total_tasks_executed as f64
        } else {
            1.0
        };
        let speed_factor = if session.stats.average_execution_time.as_millis() > 0 {
            1000.0 / session.stats.average_execution_time.as_millis() as f64
        } else {
            1.0
        };

        // Weighted score combining load, success rate, and speed
        (load_factor * 0.4) + (success_rate * 0.4) + (speed_factor.min(2.0) * 0.2)
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:health_check_session (lines 594-604) ---[0m
    async fn health_check_session(session: &PooledSession) -> Result<()> {
        let agent = session.agent.lock().await;
        let stats = agent.get_session_stats().await;

        if !stats.is_active {
            return Err(anyhow::anyhow!("Session is not active"));
        }

        // Additional health checks could be added here
        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:542-561 method update_session_stats <-> ./crates/ccswarm/src/session/session_pool.rs:594-604 method health_check_session
  Similarity: 71.88%
  Classes: SessionPool <-> SessionPool

[36m--- ./crates/ccswarm/src/session/session_pool.rs:update_session_stats (lines 542-561) ---[0m
    async fn update_session_stats(
        &self,
        session: &Arc<PooledSession>,
        result: &TaskResult,
        execution_time: Duration,
    ) {
        // Note: In a real implementation, we would need mutable access to session stats
        // This is simplified for demonstration purposes
        tracing::debug!(
            "Task completed in session {}: success={}, duration={:?}",
            session.metadata.agent_id,
            result.success,
            execution_time
        );

        // Update global performance metrics
        let mut metrics = self.performance_metrics.write().await;
        metrics.total_tasks_processed += 1;
        metrics.total_execution_time += execution_time;
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:health_check_session (lines 594-604) ---[0m
    async fn health_check_session(session: &PooledSession) -> Result<()> {
        let agent = session.agent.lock().await;
        let stats = agent.get_session_stats().await;

        if !stats.is_active {
            return Err(anyhow::anyhow!("Session is not active"));
        }

        // Additional health checks could be added here
        Ok(())
    }

  ./crates/ccswarm/src/session/session_pool.rs:594-604 method health_check_session <-> ./crates/ccswarm/src/session/session_pool.rs:807-823 function test_session_pool_creation
  Similarity: 71.33%

[36m--- ./crates/ccswarm/src/session/session_pool.rs:health_check_session (lines 594-604) ---[0m
    async fn health_check_session(session: &PooledSession) -> Result<()> {
        let agent = session.agent.lock().await;
        let stats = agent.get_session_stats().await;

        if !stats.is_active {
            return Err(anyhow::anyhow!("Session is not active"));
        }

        // Additional health checks could be added here
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/session_pool.rs:test_session_pool_creation (lines 807-823) ---[0m
    async fn test_session_pool_creation() {
        let temp_dir = TempDir::new().unwrap();

        let worktree_config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let pool_config = SessionPoolConfig::default();

        let pool = SessionPool::new(worktree_config, pool_config)
            .await
            .unwrap();

        let stats = pool.get_pool_statistics().await;
        assert_eq!(stats.total_sessions, 0);
    }


Duplicates in ./crates/ccswarm/src/execution/task_queue.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/execution/task_queue.rs:278-314 method cancel_task <-> ./crates/ccswarm/src/execution/task_queue.rs:348-399 method list_tasks
  Similarity: 86.45%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:cancel_task (lines 278-314) ---[0m
    pub async fn cancel_task(&self, task_id: &str, reason: Option<String>) -> anyhow::Result<()> {
        let now = Utc::now();

        // Try to find task in pending, active, or lookup table
        let mut pending = self.pending_tasks.write().await;
        let mut active = self.active_tasks.write().await;
        let mut tasks = self.tasks_by_id.write().await;
        let mut completed = self.completed_tasks.write().await;

        // Check active tasks first
        if let Some(mut task) = active.remove(task_id) {
            task.status = TaskStatus::Cancelled {
                cancelled_at: now,
                reason,
            };
            task.updated_at = now;
            completed.push_back(task);
            return Ok(());
        }

        // Check pending tasks
        for (_, queue) in pending.iter_mut() {
            if let Some(pos) = queue.iter().position(|t| t.task.id == task_id) {
                let mut task = queue.remove(pos).unwrap();
                task.status = TaskStatus::Cancelled {
                    cancelled_at: now,
                    reason,
                };
                task.updated_at = now;
                completed.push_back(task);
                tasks.remove(task_id);
                return Ok(());
            }
        }

        Err(anyhow::anyhow!("Task not found: {}", task_id))
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:list_tasks (lines 348-399) ---[0m
    pub async fn list_tasks(
        &self,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
    ) -> Vec<QueuedTask> {
        let mut result = Vec::new();

        // Collect from all sources
        {
            let active = self.active_tasks.read().await;
            result.extend(active.values().cloned());
        }

        {
            let tasks = self.tasks_by_id.read().await;
            result.extend(tasks.values().cloned());
        }

        {
            let completed = self.completed_tasks.read().await;
            result.extend(completed.iter().cloned());
        }

        // Apply filters
        if let Some(status) = status_filter {
            result.retain(|task| {
                matches!(
                    (&task.status, status),
                    (TaskStatus::Pending, "pending")
                        | (TaskStatus::Assigned { .. }, "assigned")
                        | (TaskStatus::InProgress { .. }, "in_progress")
                        | (TaskStatus::Completed { .. }, "completed")
                        | (TaskStatus::Failed { .. }, "failed")
                        | (TaskStatus::Cancelled { .. }, "cancelled")
                )
            });
        }

        if let Some(agent) = agent_filter {
            result.retain(|task| task.assigned_agent.as_deref() == Some(agent));
        }

        // Sort by priority and creation time
        result.sort_by(|a, b| {
            a.task
                .priority
                .cmp(&b.task.priority)
                .then(a.created_at.cmp(&b.created_at))
        });

        result
    }

  ./crates/ccswarm/src/execution/task_queue.rs:348-399 method list_tasks <-> ./crates/ccswarm/src/execution/task_queue.rs:467-499 function test_task_lifecycle
  Similarity: 85.55%

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:list_tasks (lines 348-399) ---[0m
    pub async fn list_tasks(
        &self,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
    ) -> Vec<QueuedTask> {
        let mut result = Vec::new();

        // Collect from all sources
        {
            let active = self.active_tasks.read().await;
            result.extend(active.values().cloned());
        }

        {
            let tasks = self.tasks_by_id.read().await;
            result.extend(tasks.values().cloned());
        }

        {
            let completed = self.completed_tasks.read().await;
            result.extend(completed.iter().cloned());
        }

        // Apply filters
        if let Some(status) = status_filter {
            result.retain(|task| {
                matches!(
                    (&task.status, status),
                    (TaskStatus::Pending, "pending")
                        | (TaskStatus::Assigned { .. }, "assigned")
                        | (TaskStatus::InProgress { .. }, "in_progress")
                        | (TaskStatus::Completed { .. }, "completed")
                        | (TaskStatus::Failed { .. }, "failed")
                        | (TaskStatus::Cancelled { .. }, "cancelled")
                )
            });
        }

        if let Some(agent) = agent_filter {
            result.retain(|task| task.assigned_agent.as_deref() == Some(agent));
        }

        // Sort by priority and creation time
        result.sort_by(|a, b| {
            a.task
                .priority
                .cmp(&b.task.priority)
                .then(a.created_at.cmp(&b.created_at))
        });

        result
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:test_task_lifecycle (lines 467-499) ---[0m
    async fn test_task_lifecycle() {
        let queue = TaskQueue::new();

        let task = TaskBuilder::new("Test task".to_string())
            .priority(Priority::Medium)
            .build();

        let task_id = queue.add_task(task).await;
        let agent_id = "test-agent";

        // Assign task
        queue.assign_task(&task_id, agent_id).await.unwrap();

        // Start execution
        queue
            .start_task_execution(&task_id, agent_id)
            .await
            .unwrap();

        // Complete task
        let result = TaskResult::success(
            serde_json::json!({"message": "completed"}),
            std::time::Duration::from_secs(1),
        );
        queue.complete_task(&task_id, result).await.unwrap();

        // Verify task is completed
        let completed_task = queue.get_task(&task_id).await.unwrap();
        assert!(matches!(
            completed_task.status,
            TaskStatus::Completed { .. }
        ));
    }

  ./crates/ccswarm/src/execution/task_queue.rs:102-131 method add_task <-> ./crates/ccswarm/src/execution/task_queue.rs:348-399 method list_tasks
  Similarity: 86.18%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:add_task (lines 102-131) ---[0m
    pub async fn add_task(&self, task: Task) -> String {
        let task_id = task.id.clone();
        let now = Utc::now();

        let queued_task = QueuedTask {
            task: task.clone(),
            status: TaskStatus::Pending,
            created_at: now,
            updated_at: now,
            assigned_agent: None,
            execution_history: Vec::new(),
            metadata: HashMap::new(),
        };

        // Add to priority queue
        {
            let mut pending = self.pending_tasks.write().await;
            if let Some(queue) = pending.get_mut(&task.priority) {
                queue.push_back(queued_task.clone());
            }
        }

        // Add to lookup table
        {
            let mut tasks = self.tasks_by_id.write().await;
            tasks.insert(task_id.clone(), queued_task);
        }

        task_id
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:list_tasks (lines 348-399) ---[0m
    pub async fn list_tasks(
        &self,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
    ) -> Vec<QueuedTask> {
        let mut result = Vec::new();

        // Collect from all sources
        {
            let active = self.active_tasks.read().await;
            result.extend(active.values().cloned());
        }

        {
            let tasks = self.tasks_by_id.read().await;
            result.extend(tasks.values().cloned());
        }

        {
            let completed = self.completed_tasks.read().await;
            result.extend(completed.iter().cloned());
        }

        // Apply filters
        if let Some(status) = status_filter {
            result.retain(|task| {
                matches!(
                    (&task.status, status),
                    (TaskStatus::Pending, "pending")
                        | (TaskStatus::Assigned { .. }, "assigned")
                        | (TaskStatus::InProgress { .. }, "in_progress")
                        | (TaskStatus::Completed { .. }, "completed")
                        | (TaskStatus::Failed { .. }, "failed")
                        | (TaskStatus::Cancelled { .. }, "cancelled")
                )
            });
        }

        if let Some(agent) = agent_filter {
            result.retain(|task| task.assigned_agent.as_deref() == Some(agent));
        }

        // Sort by priority and creation time
        result.sort_by(|a, b| {
            a.task
                .priority
                .cmp(&b.task.priority)
                .then(a.created_at.cmp(&b.created_at))
        });

        result
    }

  ./crates/ccswarm/src/execution/task_queue.rs:203-238 method complete_task <-> ./crates/ccswarm/src/execution/task_queue.rs:241-275 method fail_task
  Similarity: 99.45%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:complete_task (lines 203-238) ---[0m
    pub async fn complete_task(&self, task_id: &str, result: TaskResult) -> anyhow::Result<()> {
        let now = Utc::now();

        let mut active = self.active_tasks.write().await;
        let mut completed = self.completed_tasks.write().await;

        if let Some(mut task) = active.remove(task_id) {
            let agent_id = match &task.status {
                TaskStatus::InProgress { agent_id, .. } => agent_id.clone(),
                _ => return Err(anyhow::anyhow!("Task is not in progress")),
            };

            task.status = TaskStatus::Completed {
                agent_id: agent_id.clone(),
                completed_at: now,
                result: result.clone(),
            };
            task.updated_at = now;

            // Update last execution attempt
            if let Some(attempt) = task.execution_history.last_mut() {
                attempt.completed_at = Some(now);
                attempt.result = Some(result);
            }

            // Add to completed queue (maintain size limit)
            completed.push_back(task);
            if completed.len() > self.max_history {
                completed.pop_front();
            }

            Ok(())
        } else {
            Err(anyhow::anyhow!("Active task not found: {}", task_id))
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:fail_task (lines 241-275) ---[0m
    pub async fn fail_task(&self, task_id: &str, error: String) -> anyhow::Result<()> {
        let now = Utc::now();

        let mut active = self.active_tasks.write().await;
        let mut completed = self.completed_tasks.write().await;

        if let Some(mut task) = active.remove(task_id) {
            let agent_id = match &task.status {
                TaskStatus::InProgress { agent_id, .. } => agent_id.clone(),
                _ => return Err(anyhow::anyhow!("Task is not in progress")),
            };

            task.status = TaskStatus::Failed {
                agent_id: agent_id.clone(),
                failed_at: now,
                error: error.clone(),
            };
            task.updated_at = now;

            // Update last execution attempt
            if let Some(attempt) = task.execution_history.last_mut() {
                attempt.completed_at = Some(now);
                attempt.error = Some(error);
            }

            completed.push_back(task);
            if completed.len() > self.max_history {
                completed.pop_front();
            }

            Ok(())
        } else {
            Err(anyhow::anyhow!("Active task not found: {}", task_id))
        }
    }

  ./crates/ccswarm/src/execution/task_queue.rs:348-399 method list_tasks <-> ./crates/ccswarm/src/execution/task_queue.rs:402-433 method get_stats
  Similarity: 72.61%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:list_tasks (lines 348-399) ---[0m
    pub async fn list_tasks(
        &self,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
    ) -> Vec<QueuedTask> {
        let mut result = Vec::new();

        // Collect from all sources
        {
            let active = self.active_tasks.read().await;
            result.extend(active.values().cloned());
        }

        {
            let tasks = self.tasks_by_id.read().await;
            result.extend(tasks.values().cloned());
        }

        {
            let completed = self.completed_tasks.read().await;
            result.extend(completed.iter().cloned());
        }

        // Apply filters
        if let Some(status) = status_filter {
            result.retain(|task| {
                matches!(
                    (&task.status, status),
                    (TaskStatus::Pending, "pending")
                        | (TaskStatus::Assigned { .. }, "assigned")
                        | (TaskStatus::InProgress { .. }, "in_progress")
                        | (TaskStatus::Completed { .. }, "completed")
                        | (TaskStatus::Failed { .. }, "failed")
                        | (TaskStatus::Cancelled { .. }, "cancelled")
                )
            });
        }

        if let Some(agent) = agent_filter {
            result.retain(|task| task.assigned_agent.as_deref() == Some(agent));
        }

        // Sort by priority and creation time
        result.sort_by(|a, b| {
            a.task
                .priority
                .cmp(&b.task.priority)
                .then(a.created_at.cmp(&b.created_at))
        });

        result
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_stats (lines 402-433) ---[0m
    pub async fn get_stats(&self) -> TaskQueueStats {
        let pending_count = {
            let pending = self.pending_tasks.read().await;
            pending.values().map(|q| q.len()).sum()
        };

        let active_count = {
            let active = self.active_tasks.read().await;
            active.len()
        };

        let completed_count = {
            let completed = self.completed_tasks.read().await;
            completed.len()
        };

        let failed_count = {
            let completed = self.completed_tasks.read().await;
            completed
                .iter()
                .filter(|t| matches!(t.status, TaskStatus::Failed { .. }))
                .count()
        };

        TaskQueueStats {
            pending_count,
            active_count,
            completed_count,
            failed_count,
            total_count: pending_count + active_count + completed_count,
        }
    }

  ./crates/ccswarm/src/execution/task_queue.rs:102-131 method add_task <-> ./crates/ccswarm/src/execution/task_queue.rs:278-314 method cancel_task
  Similarity: 89.24%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:add_task (lines 102-131) ---[0m
    pub async fn add_task(&self, task: Task) -> String {
        let task_id = task.id.clone();
        let now = Utc::now();

        let queued_task = QueuedTask {
            task: task.clone(),
            status: TaskStatus::Pending,
            created_at: now,
            updated_at: now,
            assigned_agent: None,
            execution_history: Vec::new(),
            metadata: HashMap::new(),
        };

        // Add to priority queue
        {
            let mut pending = self.pending_tasks.write().await;
            if let Some(queue) = pending.get_mut(&task.priority) {
                queue.push_back(queued_task.clone());
            }
        }

        // Add to lookup table
        {
            let mut tasks = self.tasks_by_id.write().await;
            tasks.insert(task_id.clone(), queued_task);
        }

        task_id
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:cancel_task (lines 278-314) ---[0m
    pub async fn cancel_task(&self, task_id: &str, reason: Option<String>) -> anyhow::Result<()> {
        let now = Utc::now();

        // Try to find task in pending, active, or lookup table
        let mut pending = self.pending_tasks.write().await;
        let mut active = self.active_tasks.write().await;
        let mut tasks = self.tasks_by_id.write().await;
        let mut completed = self.completed_tasks.write().await;

        // Check active tasks first
        if let Some(mut task) = active.remove(task_id) {
            task.status = TaskStatus::Cancelled {
                cancelled_at: now,
                reason,
            };
            task.updated_at = now;
            completed.push_back(task);
            return Ok(());
        }

        // Check pending tasks
        for (_, queue) in pending.iter_mut() {
            if let Some(pos) = queue.iter().position(|t| t.task.id == task_id) {
                let mut task = queue.remove(pos).unwrap();
                task.status = TaskStatus::Cancelled {
                    cancelled_at: now,
                    reason,
                };
                task.updated_at = now;
                completed.push_back(task);
                tasks.remove(task_id);
                return Ok(());
            }
        }

        Err(anyhow::anyhow!("Task not found: {}", task_id))
    }

  ./crates/ccswarm/src/execution/task_queue.rs:170-200 method start_task_execution <-> ./crates/ccswarm/src/execution/task_queue.rs:203-238 method complete_task
  Similarity: 88.49%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:start_task_execution (lines 170-200) ---[0m
    pub async fn start_task_execution(&self, task_id: &str, agent_id: &str) -> anyhow::Result<()> {
        let now = Utc::now();

        // Move from tasks_by_id to active_tasks
        let mut tasks = self.tasks_by_id.write().await;
        let mut active = self.active_tasks.write().await;

        if let Some(mut task) = tasks.remove(task_id) {
            task.status = TaskStatus::InProgress {
                agent_id: agent_id.to_string(),
                started_at: now,
            };
            task.updated_at = now;

            // Add execution attempt
            let attempt = TaskExecutionAttempt {
                attempt_id: Uuid::new_v4().to_string(),
                agent_id: agent_id.to_string(),
                started_at: now,
                completed_at: None,
                result: None,
                error: None,
            };
            task.execution_history.push(attempt);

            active.insert(task_id.to_string(), task);
            Ok(())
        } else {
            Err(anyhow::anyhow!("Task not found: {}", task_id))
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:complete_task (lines 203-238) ---[0m
    pub async fn complete_task(&self, task_id: &str, result: TaskResult) -> anyhow::Result<()> {
        let now = Utc::now();

        let mut active = self.active_tasks.write().await;
        let mut completed = self.completed_tasks.write().await;

        if let Some(mut task) = active.remove(task_id) {
            let agent_id = match &task.status {
                TaskStatus::InProgress { agent_id, .. } => agent_id.clone(),
                _ => return Err(anyhow::anyhow!("Task is not in progress")),
            };

            task.status = TaskStatus::Completed {
                agent_id: agent_id.clone(),
                completed_at: now,
                result: result.clone(),
            };
            task.updated_at = now;

            // Update last execution attempt
            if let Some(attempt) = task.execution_history.last_mut() {
                attempt.completed_at = Some(now);
                attempt.result = Some(result);
            }

            // Add to completed queue (maintain size limit)
            completed.push_back(task);
            if completed.len() > self.max_history {
                completed.pop_front();
            }

            Ok(())
        } else {
            Err(anyhow::anyhow!("Active task not found: {}", task_id))
        }
    }

  ./crates/ccswarm/src/execution/task_queue.rs:170-200 method start_task_execution <-> ./crates/ccswarm/src/execution/task_queue.rs:241-275 method fail_task
  Similarity: 88.49%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:start_task_execution (lines 170-200) ---[0m
    pub async fn start_task_execution(&self, task_id: &str, agent_id: &str) -> anyhow::Result<()> {
        let now = Utc::now();

        // Move from tasks_by_id to active_tasks
        let mut tasks = self.tasks_by_id.write().await;
        let mut active = self.active_tasks.write().await;

        if let Some(mut task) = tasks.remove(task_id) {
            task.status = TaskStatus::InProgress {
                agent_id: agent_id.to_string(),
                started_at: now,
            };
            task.updated_at = now;

            // Add execution attempt
            let attempt = TaskExecutionAttempt {
                attempt_id: Uuid::new_v4().to_string(),
                agent_id: agent_id.to_string(),
                started_at: now,
                completed_at: None,
                result: None,
                error: None,
            };
            task.execution_history.push(attempt);

            active.insert(task_id.to_string(), task);
            Ok(())
        } else {
            Err(anyhow::anyhow!("Task not found: {}", task_id))
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:fail_task (lines 241-275) ---[0m
    pub async fn fail_task(&self, task_id: &str, error: String) -> anyhow::Result<()> {
        let now = Utc::now();

        let mut active = self.active_tasks.write().await;
        let mut completed = self.completed_tasks.write().await;

        if let Some(mut task) = active.remove(task_id) {
            let agent_id = match &task.status {
                TaskStatus::InProgress { agent_id, .. } => agent_id.clone(),
                _ => return Err(anyhow::anyhow!("Task is not in progress")),
            };

            task.status = TaskStatus::Failed {
                agent_id: agent_id.clone(),
                failed_at: now,
                error: error.clone(),
            };
            task.updated_at = now;

            // Update last execution attempt
            if let Some(attempt) = task.execution_history.last_mut() {
                attempt.completed_at = Some(now);
                attempt.error = Some(error);
            }

            completed.push_back(task);
            if completed.len() > self.max_history {
                completed.pop_front();
            }

            Ok(())
        } else {
            Err(anyhow::anyhow!("Active task not found: {}", task_id))
        }
    }

  ./crates/ccswarm/src/execution/task_queue.rs:278-314 method cancel_task <-> ./crates/ccswarm/src/execution/task_queue.rs:467-499 function test_task_lifecycle
  Similarity: 82.73%

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:cancel_task (lines 278-314) ---[0m
    pub async fn cancel_task(&self, task_id: &str, reason: Option<String>) -> anyhow::Result<()> {
        let now = Utc::now();

        // Try to find task in pending, active, or lookup table
        let mut pending = self.pending_tasks.write().await;
        let mut active = self.active_tasks.write().await;
        let mut tasks = self.tasks_by_id.write().await;
        let mut completed = self.completed_tasks.write().await;

        // Check active tasks first
        if let Some(mut task) = active.remove(task_id) {
            task.status = TaskStatus::Cancelled {
                cancelled_at: now,
                reason,
            };
            task.updated_at = now;
            completed.push_back(task);
            return Ok(());
        }

        // Check pending tasks
        for (_, queue) in pending.iter_mut() {
            if let Some(pos) = queue.iter().position(|t| t.task.id == task_id) {
                let mut task = queue.remove(pos).unwrap();
                task.status = TaskStatus::Cancelled {
                    cancelled_at: now,
                    reason,
                };
                task.updated_at = now;
                completed.push_back(task);
                tasks.remove(task_id);
                return Ok(());
            }
        }

        Err(anyhow::anyhow!("Task not found: {}", task_id))
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:test_task_lifecycle (lines 467-499) ---[0m
    async fn test_task_lifecycle() {
        let queue = TaskQueue::new();

        let task = TaskBuilder::new("Test task".to_string())
            .priority(Priority::Medium)
            .build();

        let task_id = queue.add_task(task).await;
        let agent_id = "test-agent";

        // Assign task
        queue.assign_task(&task_id, agent_id).await.unwrap();

        // Start execution
        queue
            .start_task_execution(&task_id, agent_id)
            .await
            .unwrap();

        // Complete task
        let result = TaskResult::success(
            serde_json::json!({"message": "completed"}),
            std::time::Duration::from_secs(1),
        );
        queue.complete_task(&task_id, result).await.unwrap();

        // Verify task is completed
        let completed_task = queue.get_task(&task_id).await.unwrap();
        assert!(matches!(
            completed_task.status,
            TaskStatus::Completed { .. }
        ));
    }

  ./crates/ccswarm/src/execution/task_queue.rs:203-238 method complete_task <-> ./crates/ccswarm/src/execution/task_queue.rs:317-345 method get_task
  Similarity: 88.15%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:complete_task (lines 203-238) ---[0m
    pub async fn complete_task(&self, task_id: &str, result: TaskResult) -> anyhow::Result<()> {
        let now = Utc::now();

        let mut active = self.active_tasks.write().await;
        let mut completed = self.completed_tasks.write().await;

        if let Some(mut task) = active.remove(task_id) {
            let agent_id = match &task.status {
                TaskStatus::InProgress { agent_id, .. } => agent_id.clone(),
                _ => return Err(anyhow::anyhow!("Task is not in progress")),
            };

            task.status = TaskStatus::Completed {
                agent_id: agent_id.clone(),
                completed_at: now,
                result: result.clone(),
            };
            task.updated_at = now;

            // Update last execution attempt
            if let Some(attempt) = task.execution_history.last_mut() {
                attempt.completed_at = Some(now);
                attempt.result = Some(result);
            }

            // Add to completed queue (maintain size limit)
            completed.push_back(task);
            if completed.len() > self.max_history {
                completed.pop_front();
            }

            Ok(())
        } else {
            Err(anyhow::anyhow!("Active task not found: {}", task_id))
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_task (lines 317-345) ---[0m
    pub async fn get_task(&self, task_id: &str) -> Option<QueuedTask> {
        // Check active tasks first
        {
            let active = self.active_tasks.read().await;
            if let Some(task) = active.get(task_id) {
                return Some(task.clone());
            }
        }

        // Check pending tasks
        {
            let tasks = self.tasks_by_id.read().await;
            if let Some(task) = tasks.get(task_id) {
                return Some(task.clone());
            }
        }

        // Check completed tasks
        {
            let completed = self.completed_tasks.read().await;
            for task in completed.iter() {
                if task.task.id == task_id {
                    return Some(task.clone());
                }
            }
        }

        None
    }

  ./crates/ccswarm/src/execution/task_queue.rs:241-275 method fail_task <-> ./crates/ccswarm/src/execution/task_queue.rs:317-345 method get_task
  Similarity: 88.15%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:fail_task (lines 241-275) ---[0m
    pub async fn fail_task(&self, task_id: &str, error: String) -> anyhow::Result<()> {
        let now = Utc::now();

        let mut active = self.active_tasks.write().await;
        let mut completed = self.completed_tasks.write().await;

        if let Some(mut task) = active.remove(task_id) {
            let agent_id = match &task.status {
                TaskStatus::InProgress { agent_id, .. } => agent_id.clone(),
                _ => return Err(anyhow::anyhow!("Task is not in progress")),
            };

            task.status = TaskStatus::Failed {
                agent_id: agent_id.clone(),
                failed_at: now,
                error: error.clone(),
            };
            task.updated_at = now;

            // Update last execution attempt
            if let Some(attempt) = task.execution_history.last_mut() {
                attempt.completed_at = Some(now);
                attempt.error = Some(error);
            }

            completed.push_back(task);
            if completed.len() > self.max_history {
                completed.pop_front();
            }

            Ok(())
        } else {
            Err(anyhow::anyhow!("Active task not found: {}", task_id))
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_task (lines 317-345) ---[0m
    pub async fn get_task(&self, task_id: &str) -> Option<QueuedTask> {
        // Check active tasks first
        {
            let active = self.active_tasks.read().await;
            if let Some(task) = active.get(task_id) {
                return Some(task.clone());
            }
        }

        // Check pending tasks
        {
            let tasks = self.tasks_by_id.read().await;
            if let Some(task) = tasks.get(task_id) {
                return Some(task.clone());
            }
        }

        // Check completed tasks
        {
            let completed = self.completed_tasks.read().await;
            for task in completed.iter() {
                if task.task.id == task_id {
                    return Some(task.clone());
                }
            }
        }

        None
    }

  ./crates/ccswarm/src/execution/task_queue.rs:278-314 method cancel_task <-> ./crates/ccswarm/src/execution/task_queue.rs:317-345 method get_task
  Similarity: 84.20%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:cancel_task (lines 278-314) ---[0m
    pub async fn cancel_task(&self, task_id: &str, reason: Option<String>) -> anyhow::Result<()> {
        let now = Utc::now();

        // Try to find task in pending, active, or lookup table
        let mut pending = self.pending_tasks.write().await;
        let mut active = self.active_tasks.write().await;
        let mut tasks = self.tasks_by_id.write().await;
        let mut completed = self.completed_tasks.write().await;

        // Check active tasks first
        if let Some(mut task) = active.remove(task_id) {
            task.status = TaskStatus::Cancelled {
                cancelled_at: now,
                reason,
            };
            task.updated_at = now;
            completed.push_back(task);
            return Ok(());
        }

        // Check pending tasks
        for (_, queue) in pending.iter_mut() {
            if let Some(pos) = queue.iter().position(|t| t.task.id == task_id) {
                let mut task = queue.remove(pos).unwrap();
                task.status = TaskStatus::Cancelled {
                    cancelled_at: now,
                    reason,
                };
                task.updated_at = now;
                completed.push_back(task);
                tasks.remove(task_id);
                return Ok(());
            }
        }

        Err(anyhow::anyhow!("Task not found: {}", task_id))
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_task (lines 317-345) ---[0m
    pub async fn get_task(&self, task_id: &str) -> Option<QueuedTask> {
        // Check active tasks first
        {
            let active = self.active_tasks.read().await;
            if let Some(task) = active.get(task_id) {
                return Some(task.clone());
            }
        }

        // Check pending tasks
        {
            let tasks = self.tasks_by_id.read().await;
            if let Some(task) = tasks.get(task_id) {
                return Some(task.clone());
            }
        }

        // Check completed tasks
        {
            let completed = self.completed_tasks.read().await;
            for task in completed.iter() {
                if task.task.id == task_id {
                    return Some(task.clone());
                }
            }
        }

        None
    }

  ./crates/ccswarm/src/execution/task_queue.rs:170-200 method start_task_execution <-> ./crates/ccswarm/src/execution/task_queue.rs:402-433 method get_stats
  Similarity: 86.73%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:start_task_execution (lines 170-200) ---[0m
    pub async fn start_task_execution(&self, task_id: &str, agent_id: &str) -> anyhow::Result<()> {
        let now = Utc::now();

        // Move from tasks_by_id to active_tasks
        let mut tasks = self.tasks_by_id.write().await;
        let mut active = self.active_tasks.write().await;

        if let Some(mut task) = tasks.remove(task_id) {
            task.status = TaskStatus::InProgress {
                agent_id: agent_id.to_string(),
                started_at: now,
            };
            task.updated_at = now;

            // Add execution attempt
            let attempt = TaskExecutionAttempt {
                attempt_id: Uuid::new_v4().to_string(),
                agent_id: agent_id.to_string(),
                started_at: now,
                completed_at: None,
                result: None,
                error: None,
            };
            task.execution_history.push(attempt);

            active.insert(task_id.to_string(), task);
            Ok(())
        } else {
            Err(anyhow::anyhow!("Task not found: {}", task_id))
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_stats (lines 402-433) ---[0m
    pub async fn get_stats(&self) -> TaskQueueStats {
        let pending_count = {
            let pending = self.pending_tasks.read().await;
            pending.values().map(|q| q.len()).sum()
        };

        let active_count = {
            let active = self.active_tasks.read().await;
            active.len()
        };

        let completed_count = {
            let completed = self.completed_tasks.read().await;
            completed.len()
        };

        let failed_count = {
            let completed = self.completed_tasks.read().await;
            completed
                .iter()
                .filter(|t| matches!(t.status, TaskStatus::Failed { .. }))
                .count()
        };

        TaskQueueStats {
            pending_count,
            active_count,
            completed_count,
            failed_count,
            total_count: pending_count + active_count + completed_count,
        }
    }

  ./crates/ccswarm/src/execution/task_queue.rs:102-131 method add_task <-> ./crates/ccswarm/src/execution/task_queue.rs:203-238 method complete_task
  Similarity: 82.31%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:add_task (lines 102-131) ---[0m
    pub async fn add_task(&self, task: Task) -> String {
        let task_id = task.id.clone();
        let now = Utc::now();

        let queued_task = QueuedTask {
            task: task.clone(),
            status: TaskStatus::Pending,
            created_at: now,
            updated_at: now,
            assigned_agent: None,
            execution_history: Vec::new(),
            metadata: HashMap::new(),
        };

        // Add to priority queue
        {
            let mut pending = self.pending_tasks.write().await;
            if let Some(queue) = pending.get_mut(&task.priority) {
                queue.push_back(queued_task.clone());
            }
        }

        // Add to lookup table
        {
            let mut tasks = self.tasks_by_id.write().await;
            tasks.insert(task_id.clone(), queued_task);
        }

        task_id
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:complete_task (lines 203-238) ---[0m
    pub async fn complete_task(&self, task_id: &str, result: TaskResult) -> anyhow::Result<()> {
        let now = Utc::now();

        let mut active = self.active_tasks.write().await;
        let mut completed = self.completed_tasks.write().await;

        if let Some(mut task) = active.remove(task_id) {
            let agent_id = match &task.status {
                TaskStatus::InProgress { agent_id, .. } => agent_id.clone(),
                _ => return Err(anyhow::anyhow!("Task is not in progress")),
            };

            task.status = TaskStatus::Completed {
                agent_id: agent_id.clone(),
                completed_at: now,
                result: result.clone(),
            };
            task.updated_at = now;

            // Update last execution attempt
            if let Some(attempt) = task.execution_history.last_mut() {
                attempt.completed_at = Some(now);
                attempt.result = Some(result);
            }

            // Add to completed queue (maintain size limit)
            completed.push_back(task);
            if completed.len() > self.max_history {
                completed.pop_front();
            }

            Ok(())
        } else {
            Err(anyhow::anyhow!("Active task not found: {}", task_id))
        }
    }

  ./crates/ccswarm/src/execution/task_queue.rs:203-238 method complete_task <-> ./crates/ccswarm/src/execution/task_queue.rs:402-433 method get_stats
  Similarity: 79.60%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:complete_task (lines 203-238) ---[0m
    pub async fn complete_task(&self, task_id: &str, result: TaskResult) -> anyhow::Result<()> {
        let now = Utc::now();

        let mut active = self.active_tasks.write().await;
        let mut completed = self.completed_tasks.write().await;

        if let Some(mut task) = active.remove(task_id) {
            let agent_id = match &task.status {
                TaskStatus::InProgress { agent_id, .. } => agent_id.clone(),
                _ => return Err(anyhow::anyhow!("Task is not in progress")),
            };

            task.status = TaskStatus::Completed {
                agent_id: agent_id.clone(),
                completed_at: now,
                result: result.clone(),
            };
            task.updated_at = now;

            // Update last execution attempt
            if let Some(attempt) = task.execution_history.last_mut() {
                attempt.completed_at = Some(now);
                attempt.result = Some(result);
            }

            // Add to completed queue (maintain size limit)
            completed.push_back(task);
            if completed.len() > self.max_history {
                completed.pop_front();
            }

            Ok(())
        } else {
            Err(anyhow::anyhow!("Active task not found: {}", task_id))
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_stats (lines 402-433) ---[0m
    pub async fn get_stats(&self) -> TaskQueueStats {
        let pending_count = {
            let pending = self.pending_tasks.read().await;
            pending.values().map(|q| q.len()).sum()
        };

        let active_count = {
            let active = self.active_tasks.read().await;
            active.len()
        };

        let completed_count = {
            let completed = self.completed_tasks.read().await;
            completed.len()
        };

        let failed_count = {
            let completed = self.completed_tasks.read().await;
            completed
                .iter()
                .filter(|t| matches!(t.status, TaskStatus::Failed { .. }))
                .count()
        };

        TaskQueueStats {
            pending_count,
            active_count,
            completed_count,
            failed_count,
            total_count: pending_count + active_count + completed_count,
        }
    }

  ./crates/ccswarm/src/execution/task_queue.rs:102-131 method add_task <-> ./crates/ccswarm/src/execution/task_queue.rs:241-275 method fail_task
  Similarity: 82.31%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:add_task (lines 102-131) ---[0m
    pub async fn add_task(&self, task: Task) -> String {
        let task_id = task.id.clone();
        let now = Utc::now();

        let queued_task = QueuedTask {
            task: task.clone(),
            status: TaskStatus::Pending,
            created_at: now,
            updated_at: now,
            assigned_agent: None,
            execution_history: Vec::new(),
            metadata: HashMap::new(),
        };

        // Add to priority queue
        {
            let mut pending = self.pending_tasks.write().await;
            if let Some(queue) = pending.get_mut(&task.priority) {
                queue.push_back(queued_task.clone());
            }
        }

        // Add to lookup table
        {
            let mut tasks = self.tasks_by_id.write().await;
            tasks.insert(task_id.clone(), queued_task);
        }

        task_id
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:fail_task (lines 241-275) ---[0m
    pub async fn fail_task(&self, task_id: &str, error: String) -> anyhow::Result<()> {
        let now = Utc::now();

        let mut active = self.active_tasks.write().await;
        let mut completed = self.completed_tasks.write().await;

        if let Some(mut task) = active.remove(task_id) {
            let agent_id = match &task.status {
                TaskStatus::InProgress { agent_id, .. } => agent_id.clone(),
                _ => return Err(anyhow::anyhow!("Task is not in progress")),
            };

            task.status = TaskStatus::Failed {
                agent_id: agent_id.clone(),
                failed_at: now,
                error: error.clone(),
            };
            task.updated_at = now;

            // Update last execution attempt
            if let Some(attempt) = task.execution_history.last_mut() {
                attempt.completed_at = Some(now);
                attempt.error = Some(error);
            }

            completed.push_back(task);
            if completed.len() > self.max_history {
                completed.pop_front();
            }

            Ok(())
        } else {
            Err(anyhow::anyhow!("Active task not found: {}", task_id))
        }
    }

  ./crates/ccswarm/src/execution/task_queue.rs:170-200 method start_task_execution <-> ./crates/ccswarm/src/execution/task_queue.rs:317-345 method get_task
  Similarity: 89.04%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:start_task_execution (lines 170-200) ---[0m
    pub async fn start_task_execution(&self, task_id: &str, agent_id: &str) -> anyhow::Result<()> {
        let now = Utc::now();

        // Move from tasks_by_id to active_tasks
        let mut tasks = self.tasks_by_id.write().await;
        let mut active = self.active_tasks.write().await;

        if let Some(mut task) = tasks.remove(task_id) {
            task.status = TaskStatus::InProgress {
                agent_id: agent_id.to_string(),
                started_at: now,
            };
            task.updated_at = now;

            // Add execution attempt
            let attempt = TaskExecutionAttempt {
                attempt_id: Uuid::new_v4().to_string(),
                agent_id: agent_id.to_string(),
                started_at: now,
                completed_at: None,
                result: None,
                error: None,
            };
            task.execution_history.push(attempt);

            active.insert(task_id.to_string(), task);
            Ok(())
        } else {
            Err(anyhow::anyhow!("Task not found: {}", task_id))
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_task (lines 317-345) ---[0m
    pub async fn get_task(&self, task_id: &str) -> Option<QueuedTask> {
        // Check active tasks first
        {
            let active = self.active_tasks.read().await;
            if let Some(task) = active.get(task_id) {
                return Some(task.clone());
            }
        }

        // Check pending tasks
        {
            let tasks = self.tasks_by_id.read().await;
            if let Some(task) = tasks.get(task_id) {
                return Some(task.clone());
            }
        }

        // Check completed tasks
        {
            let completed = self.completed_tasks.read().await;
            for task in completed.iter() {
                if task.task.id == task_id {
                    return Some(task.clone());
                }
            }
        }

        None
    }

  ./crates/ccswarm/src/execution/task_queue.rs:241-275 method fail_task <-> ./crates/ccswarm/src/execution/task_queue.rs:402-433 method get_stats
  Similarity: 79.60%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:fail_task (lines 241-275) ---[0m
    pub async fn fail_task(&self, task_id: &str, error: String) -> anyhow::Result<()> {
        let now = Utc::now();

        let mut active = self.active_tasks.write().await;
        let mut completed = self.completed_tasks.write().await;

        if let Some(mut task) = active.remove(task_id) {
            let agent_id = match &task.status {
                TaskStatus::InProgress { agent_id, .. } => agent_id.clone(),
                _ => return Err(anyhow::anyhow!("Task is not in progress")),
            };

            task.status = TaskStatus::Failed {
                agent_id: agent_id.clone(),
                failed_at: now,
                error: error.clone(),
            };
            task.updated_at = now;

            // Update last execution attempt
            if let Some(attempt) = task.execution_history.last_mut() {
                attempt.completed_at = Some(now);
                attempt.error = Some(error);
            }

            completed.push_back(task);
            if completed.len() > self.max_history {
                completed.pop_front();
            }

            Ok(())
        } else {
            Err(anyhow::anyhow!("Active task not found: {}", task_id))
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_stats (lines 402-433) ---[0m
    pub async fn get_stats(&self) -> TaskQueueStats {
        let pending_count = {
            let pending = self.pending_tasks.read().await;
            pending.values().map(|q| q.len()).sum()
        };

        let active_count = {
            let active = self.active_tasks.read().await;
            active.len()
        };

        let completed_count = {
            let completed = self.completed_tasks.read().await;
            completed.len()
        };

        let failed_count = {
            let completed = self.completed_tasks.read().await;
            completed
                .iter()
                .filter(|t| matches!(t.status, TaskStatus::Failed { .. }))
                .count()
        };

        TaskQueueStats {
            pending_count,
            active_count,
            completed_count,
            failed_count,
            total_count: pending_count + active_count + completed_count,
        }
    }

  ./crates/ccswarm/src/execution/task_queue.rs:85-99 method new <-> ./crates/ccswarm/src/execution/task_queue.rs:348-399 method list_tasks
  Similarity: 79.38%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:new (lines 85-99) ---[0m
    pub fn new() -> Self {
        let mut pending_tasks = HashMap::new();
        pending_tasks.insert(Priority::Critical, VecDeque::new());
        pending_tasks.insert(Priority::High, VecDeque::new());
        pending_tasks.insert(Priority::Medium, VecDeque::new());
        pending_tasks.insert(Priority::Low, VecDeque::new());

        Self {
            pending_tasks: Arc::new(RwLock::new(pending_tasks)),
            tasks_by_id: Arc::new(RwLock::new(HashMap::new())),
            active_tasks: Arc::new(RwLock::new(HashMap::new())),
            completed_tasks: Arc::new(RwLock::new(VecDeque::new())),
            max_history: 1000,
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:list_tasks (lines 348-399) ---[0m
    pub async fn list_tasks(
        &self,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
    ) -> Vec<QueuedTask> {
        let mut result = Vec::new();

        // Collect from all sources
        {
            let active = self.active_tasks.read().await;
            result.extend(active.values().cloned());
        }

        {
            let tasks = self.tasks_by_id.read().await;
            result.extend(tasks.values().cloned());
        }

        {
            let completed = self.completed_tasks.read().await;
            result.extend(completed.iter().cloned());
        }

        // Apply filters
        if let Some(status) = status_filter {
            result.retain(|task| {
                matches!(
                    (&task.status, status),
                    (TaskStatus::Pending, "pending")
                        | (TaskStatus::Assigned { .. }, "assigned")
                        | (TaskStatus::InProgress { .. }, "in_progress")
                        | (TaskStatus::Completed { .. }, "completed")
                        | (TaskStatus::Failed { .. }, "failed")
                        | (TaskStatus::Cancelled { .. }, "cancelled")
                )
            });
        }

        if let Some(agent) = agent_filter {
            result.retain(|task| task.assigned_agent.as_deref() == Some(agent));
        }

        // Sort by priority and creation time
        result.sort_by(|a, b| {
            a.task
                .priority
                .cmp(&b.task.priority)
                .then(a.created_at.cmp(&b.created_at))
        });

        result
    }

  ./crates/ccswarm/src/execution/task_queue.rs:102-131 method add_task <-> ./crates/ccswarm/src/execution/task_queue.rs:317-345 method get_task
  Similarity: 89.82%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:add_task (lines 102-131) ---[0m
    pub async fn add_task(&self, task: Task) -> String {
        let task_id = task.id.clone();
        let now = Utc::now();

        let queued_task = QueuedTask {
            task: task.clone(),
            status: TaskStatus::Pending,
            created_at: now,
            updated_at: now,
            assigned_agent: None,
            execution_history: Vec::new(),
            metadata: HashMap::new(),
        };

        // Add to priority queue
        {
            let mut pending = self.pending_tasks.write().await;
            if let Some(queue) = pending.get_mut(&task.priority) {
                queue.push_back(queued_task.clone());
            }
        }

        // Add to lookup table
        {
            let mut tasks = self.tasks_by_id.write().await;
            tasks.insert(task_id.clone(), queued_task);
        }

        task_id
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_task (lines 317-345) ---[0m
    pub async fn get_task(&self, task_id: &str) -> Option<QueuedTask> {
        // Check active tasks first
        {
            let active = self.active_tasks.read().await;
            if let Some(task) = active.get(task_id) {
                return Some(task.clone());
            }
        }

        // Check pending tasks
        {
            let tasks = self.tasks_by_id.read().await;
            if let Some(task) = tasks.get(task_id) {
                return Some(task.clone());
            }
        }

        // Check completed tasks
        {
            let completed = self.completed_tasks.read().await;
            for task in completed.iter() {
                if task.task.id == task_id {
                    return Some(task.clone());
                }
            }
        }

        None
    }

  ./crates/ccswarm/src/execution/task_queue.rs:203-238 method complete_task <-> ./crates/ccswarm/src/execution/task_queue.rs:278-314 method cancel_task
  Similarity: 70.20%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:complete_task (lines 203-238) ---[0m
    pub async fn complete_task(&self, task_id: &str, result: TaskResult) -> anyhow::Result<()> {
        let now = Utc::now();

        let mut active = self.active_tasks.write().await;
        let mut completed = self.completed_tasks.write().await;

        if let Some(mut task) = active.remove(task_id) {
            let agent_id = match &task.status {
                TaskStatus::InProgress { agent_id, .. } => agent_id.clone(),
                _ => return Err(anyhow::anyhow!("Task is not in progress")),
            };

            task.status = TaskStatus::Completed {
                agent_id: agent_id.clone(),
                completed_at: now,
                result: result.clone(),
            };
            task.updated_at = now;

            // Update last execution attempt
            if let Some(attempt) = task.execution_history.last_mut() {
                attempt.completed_at = Some(now);
                attempt.result = Some(result);
            }

            // Add to completed queue (maintain size limit)
            completed.push_back(task);
            if completed.len() > self.max_history {
                completed.pop_front();
            }

            Ok(())
        } else {
            Err(anyhow::anyhow!("Active task not found: {}", task_id))
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:cancel_task (lines 278-314) ---[0m
    pub async fn cancel_task(&self, task_id: &str, reason: Option<String>) -> anyhow::Result<()> {
        let now = Utc::now();

        // Try to find task in pending, active, or lookup table
        let mut pending = self.pending_tasks.write().await;
        let mut active = self.active_tasks.write().await;
        let mut tasks = self.tasks_by_id.write().await;
        let mut completed = self.completed_tasks.write().await;

        // Check active tasks first
        if let Some(mut task) = active.remove(task_id) {
            task.status = TaskStatus::Cancelled {
                cancelled_at: now,
                reason,
            };
            task.updated_at = now;
            completed.push_back(task);
            return Ok(());
        }

        // Check pending tasks
        for (_, queue) in pending.iter_mut() {
            if let Some(pos) = queue.iter().position(|t| t.task.id == task_id) {
                let mut task = queue.remove(pos).unwrap();
                task.status = TaskStatus::Cancelled {
                    cancelled_at: now,
                    reason,
                };
                task.updated_at = now;
                completed.push_back(task);
                tasks.remove(task_id);
                return Ok(());
            }
        }

        Err(anyhow::anyhow!("Task not found: {}", task_id))
    }

  ./crates/ccswarm/src/execution/task_queue.rs:170-200 method start_task_execution <-> ./crates/ccswarm/src/execution/task_queue.rs:278-314 method cancel_task
  Similarity: 75.27%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:start_task_execution (lines 170-200) ---[0m
    pub async fn start_task_execution(&self, task_id: &str, agent_id: &str) -> anyhow::Result<()> {
        let now = Utc::now();

        // Move from tasks_by_id to active_tasks
        let mut tasks = self.tasks_by_id.write().await;
        let mut active = self.active_tasks.write().await;

        if let Some(mut task) = tasks.remove(task_id) {
            task.status = TaskStatus::InProgress {
                agent_id: agent_id.to_string(),
                started_at: now,
            };
            task.updated_at = now;

            // Add execution attempt
            let attempt = TaskExecutionAttempt {
                attempt_id: Uuid::new_v4().to_string(),
                agent_id: agent_id.to_string(),
                started_at: now,
                completed_at: None,
                result: None,
                error: None,
            };
            task.execution_history.push(attempt);

            active.insert(task_id.to_string(), task);
            Ok(())
        } else {
            Err(anyhow::anyhow!("Task not found: {}", task_id))
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:cancel_task (lines 278-314) ---[0m
    pub async fn cancel_task(&self, task_id: &str, reason: Option<String>) -> anyhow::Result<()> {
        let now = Utc::now();

        // Try to find task in pending, active, or lookup table
        let mut pending = self.pending_tasks.write().await;
        let mut active = self.active_tasks.write().await;
        let mut tasks = self.tasks_by_id.write().await;
        let mut completed = self.completed_tasks.write().await;

        // Check active tasks first
        if let Some(mut task) = active.remove(task_id) {
            task.status = TaskStatus::Cancelled {
                cancelled_at: now,
                reason,
            };
            task.updated_at = now;
            completed.push_back(task);
            return Ok(());
        }

        // Check pending tasks
        for (_, queue) in pending.iter_mut() {
            if let Some(pos) = queue.iter().position(|t| t.task.id == task_id) {
                let mut task = queue.remove(pos).unwrap();
                task.status = TaskStatus::Cancelled {
                    cancelled_at: now,
                    reason,
                };
                task.updated_at = now;
                completed.push_back(task);
                tasks.remove(task_id);
                return Ok(());
            }
        }

        Err(anyhow::anyhow!("Task not found: {}", task_id))
    }

  ./crates/ccswarm/src/execution/task_queue.rs:102-131 method add_task <-> ./crates/ccswarm/src/execution/task_queue.rs:170-200 method start_task_execution
  Similarity: 83.59%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:add_task (lines 102-131) ---[0m
    pub async fn add_task(&self, task: Task) -> String {
        let task_id = task.id.clone();
        let now = Utc::now();

        let queued_task = QueuedTask {
            task: task.clone(),
            status: TaskStatus::Pending,
            created_at: now,
            updated_at: now,
            assigned_agent: None,
            execution_history: Vec::new(),
            metadata: HashMap::new(),
        };

        // Add to priority queue
        {
            let mut pending = self.pending_tasks.write().await;
            if let Some(queue) = pending.get_mut(&task.priority) {
                queue.push_back(queued_task.clone());
            }
        }

        // Add to lookup table
        {
            let mut tasks = self.tasks_by_id.write().await;
            tasks.insert(task_id.clone(), queued_task);
        }

        task_id
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:start_task_execution (lines 170-200) ---[0m
    pub async fn start_task_execution(&self, task_id: &str, agent_id: &str) -> anyhow::Result<()> {
        let now = Utc::now();

        // Move from tasks_by_id to active_tasks
        let mut tasks = self.tasks_by_id.write().await;
        let mut active = self.active_tasks.write().await;

        if let Some(mut task) = tasks.remove(task_id) {
            task.status = TaskStatus::InProgress {
                agent_id: agent_id.to_string(),
                started_at: now,
            };
            task.updated_at = now;

            // Add execution attempt
            let attempt = TaskExecutionAttempt {
                attempt_id: Uuid::new_v4().to_string(),
                agent_id: agent_id.to_string(),
                started_at: now,
                completed_at: None,
                result: None,
                error: None,
            };
            task.execution_history.push(attempt);

            active.insert(task_id.to_string(), task);
            Ok(())
        } else {
            Err(anyhow::anyhow!("Task not found: {}", task_id))
        }
    }

  ./crates/ccswarm/src/execution/task_queue.rs:241-275 method fail_task <-> ./crates/ccswarm/src/execution/task_queue.rs:278-314 method cancel_task
  Similarity: 70.20%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:fail_task (lines 241-275) ---[0m
    pub async fn fail_task(&self, task_id: &str, error: String) -> anyhow::Result<()> {
        let now = Utc::now();

        let mut active = self.active_tasks.write().await;
        let mut completed = self.completed_tasks.write().await;

        if let Some(mut task) = active.remove(task_id) {
            let agent_id = match &task.status {
                TaskStatus::InProgress { agent_id, .. } => agent_id.clone(),
                _ => return Err(anyhow::anyhow!("Task is not in progress")),
            };

            task.status = TaskStatus::Failed {
                agent_id: agent_id.clone(),
                failed_at: now,
                error: error.clone(),
            };
            task.updated_at = now;

            // Update last execution attempt
            if let Some(attempt) = task.execution_history.last_mut() {
                attempt.completed_at = Some(now);
                attempt.error = Some(error);
            }

            completed.push_back(task);
            if completed.len() > self.max_history {
                completed.pop_front();
            }

            Ok(())
        } else {
            Err(anyhow::anyhow!("Active task not found: {}", task_id))
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:cancel_task (lines 278-314) ---[0m
    pub async fn cancel_task(&self, task_id: &str, reason: Option<String>) -> anyhow::Result<()> {
        let now = Utc::now();

        // Try to find task in pending, active, or lookup table
        let mut pending = self.pending_tasks.write().await;
        let mut active = self.active_tasks.write().await;
        let mut tasks = self.tasks_by_id.write().await;
        let mut completed = self.completed_tasks.write().await;

        // Check active tasks first
        if let Some(mut task) = active.remove(task_id) {
            task.status = TaskStatus::Cancelled {
                cancelled_at: now,
                reason,
            };
            task.updated_at = now;
            completed.push_back(task);
            return Ok(());
        }

        // Check pending tasks
        for (_, queue) in pending.iter_mut() {
            if let Some(pos) = queue.iter().position(|t| t.task.id == task_id) {
                let mut task = queue.remove(pos).unwrap();
                task.status = TaskStatus::Cancelled {
                    cancelled_at: now,
                    reason,
                };
                task.updated_at = now;
                completed.push_back(task);
                tasks.remove(task_id);
                return Ok(());
            }
        }

        Err(anyhow::anyhow!("Task not found: {}", task_id))
    }

  ./crates/ccswarm/src/execution/task_queue.rs:317-345 method get_task <-> ./crates/ccswarm/src/execution/task_queue.rs:402-433 method get_stats
  Similarity: 82.37%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_task (lines 317-345) ---[0m
    pub async fn get_task(&self, task_id: &str) -> Option<QueuedTask> {
        // Check active tasks first
        {
            let active = self.active_tasks.read().await;
            if let Some(task) = active.get(task_id) {
                return Some(task.clone());
            }
        }

        // Check pending tasks
        {
            let tasks = self.tasks_by_id.read().await;
            if let Some(task) = tasks.get(task_id) {
                return Some(task.clone());
            }
        }

        // Check completed tasks
        {
            let completed = self.completed_tasks.read().await;
            for task in completed.iter() {
                if task.task.id == task_id {
                    return Some(task.clone());
                }
            }
        }

        None
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_stats (lines 402-433) ---[0m
    pub async fn get_stats(&self) -> TaskQueueStats {
        let pending_count = {
            let pending = self.pending_tasks.read().await;
            pending.values().map(|q| q.len()).sum()
        };

        let active_count = {
            let active = self.active_tasks.read().await;
            active.len()
        };

        let completed_count = {
            let completed = self.completed_tasks.read().await;
            completed.len()
        };

        let failed_count = {
            let completed = self.completed_tasks.read().await;
            completed
                .iter()
                .filter(|t| matches!(t.status, TaskStatus::Failed { .. }))
                .count()
        };

        TaskQueueStats {
            pending_count,
            active_count,
            completed_count,
            failed_count,
            total_count: pending_count + active_count + completed_count,
        }
    }

  ./crates/ccswarm/src/execution/task_queue.rs:102-131 method add_task <-> ./crates/ccswarm/src/execution/task_queue.rs:402-433 method get_stats
  Similarity: 79.61%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:add_task (lines 102-131) ---[0m
    pub async fn add_task(&self, task: Task) -> String {
        let task_id = task.id.clone();
        let now = Utc::now();

        let queued_task = QueuedTask {
            task: task.clone(),
            status: TaskStatus::Pending,
            created_at: now,
            updated_at: now,
            assigned_agent: None,
            execution_history: Vec::new(),
            metadata: HashMap::new(),
        };

        // Add to priority queue
        {
            let mut pending = self.pending_tasks.write().await;
            if let Some(queue) = pending.get_mut(&task.priority) {
                queue.push_back(queued_task.clone());
            }
        }

        // Add to lookup table
        {
            let mut tasks = self.tasks_by_id.write().await;
            tasks.insert(task_id.clone(), queued_task);
        }

        task_id
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_stats (lines 402-433) ---[0m
    pub async fn get_stats(&self) -> TaskQueueStats {
        let pending_count = {
            let pending = self.pending_tasks.read().await;
            pending.values().map(|q| q.len()).sum()
        };

        let active_count = {
            let active = self.active_tasks.read().await;
            active.len()
        };

        let completed_count = {
            let completed = self.completed_tasks.read().await;
            completed.len()
        };

        let failed_count = {
            let completed = self.completed_tasks.read().await;
            completed
                .iter()
                .filter(|t| matches!(t.status, TaskStatus::Failed { .. }))
                .count()
        };

        TaskQueueStats {
            pending_count,
            active_count,
            completed_count,
            failed_count,
            total_count: pending_count + active_count + completed_count,
        }
    }

  ./crates/ccswarm/src/execution/task_queue.rs:278-314 method cancel_task <-> ./crates/ccswarm/src/execution/task_queue.rs:402-433 method get_stats
  Similarity: 71.21%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:cancel_task (lines 278-314) ---[0m
    pub async fn cancel_task(&self, task_id: &str, reason: Option<String>) -> anyhow::Result<()> {
        let now = Utc::now();

        // Try to find task in pending, active, or lookup table
        let mut pending = self.pending_tasks.write().await;
        let mut active = self.active_tasks.write().await;
        let mut tasks = self.tasks_by_id.write().await;
        let mut completed = self.completed_tasks.write().await;

        // Check active tasks first
        if let Some(mut task) = active.remove(task_id) {
            task.status = TaskStatus::Cancelled {
                cancelled_at: now,
                reason,
            };
            task.updated_at = now;
            completed.push_back(task);
            return Ok(());
        }

        // Check pending tasks
        for (_, queue) in pending.iter_mut() {
            if let Some(pos) = queue.iter().position(|t| t.task.id == task_id) {
                let mut task = queue.remove(pos).unwrap();
                task.status = TaskStatus::Cancelled {
                    cancelled_at: now,
                    reason,
                };
                task.updated_at = now;
                completed.push_back(task);
                tasks.remove(task_id);
                return Ok(());
            }
        }

        Err(anyhow::anyhow!("Task not found: {}", task_id))
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_stats (lines 402-433) ---[0m
    pub async fn get_stats(&self) -> TaskQueueStats {
        let pending_count = {
            let pending = self.pending_tasks.read().await;
            pending.values().map(|q| q.len()).sum()
        };

        let active_count = {
            let active = self.active_tasks.read().await;
            active.len()
        };

        let completed_count = {
            let completed = self.completed_tasks.read().await;
            completed.len()
        };

        let failed_count = {
            let completed = self.completed_tasks.read().await;
            completed
                .iter()
                .filter(|t| matches!(t.status, TaskStatus::Failed { .. }))
                .count()
        };

        TaskQueueStats {
            pending_count,
            active_count,
            completed_count,
            failed_count,
            total_count: pending_count + active_count + completed_count,
        }
    }

  ./crates/ccswarm/src/execution/task_queue.rs:102-131 method add_task <-> ./crates/ccswarm/src/execution/task_queue.rs:467-499 function test_task_lifecycle
  Similarity: 71.54%

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:add_task (lines 102-131) ---[0m
    pub async fn add_task(&self, task: Task) -> String {
        let task_id = task.id.clone();
        let now = Utc::now();

        let queued_task = QueuedTask {
            task: task.clone(),
            status: TaskStatus::Pending,
            created_at: now,
            updated_at: now,
            assigned_agent: None,
            execution_history: Vec::new(),
            metadata: HashMap::new(),
        };

        // Add to priority queue
        {
            let mut pending = self.pending_tasks.write().await;
            if let Some(queue) = pending.get_mut(&task.priority) {
                queue.push_back(queued_task.clone());
            }
        }

        // Add to lookup table
        {
            let mut tasks = self.tasks_by_id.write().await;
            tasks.insert(task_id.clone(), queued_task);
        }

        task_id
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:test_task_lifecycle (lines 467-499) ---[0m
    async fn test_task_lifecycle() {
        let queue = TaskQueue::new();

        let task = TaskBuilder::new("Test task".to_string())
            .priority(Priority::Medium)
            .build();

        let task_id = queue.add_task(task).await;
        let agent_id = "test-agent";

        // Assign task
        queue.assign_task(&task_id, agent_id).await.unwrap();

        // Start execution
        queue
            .start_task_execution(&task_id, agent_id)
            .await
            .unwrap();

        // Complete task
        let result = TaskResult::success(
            serde_json::json!({"message": "completed"}),
            std::time::Duration::from_secs(1),
        );
        queue.complete_task(&task_id, result).await.unwrap();

        // Verify task is completed
        let completed_task = queue.get_task(&task_id).await.unwrap();
        assert!(matches!(
            completed_task.status,
            TaskStatus::Completed { .. }
        ));
    }

  ./crates/ccswarm/src/execution/task_queue.rs:134-151 method get_next_task <-> ./crates/ccswarm/src/execution/task_queue.rs:317-345 method get_task
  Similarity: 91.17%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_next_task (lines 134-151) ---[0m
    pub async fn get_next_task(&self) -> Option<QueuedTask> {
        let mut pending = self.pending_tasks.write().await;

        // Check priorities in order: Critical, High, Medium, Low
        for priority in [
            Priority::Critical,
            Priority::High,
            Priority::Medium,
            Priority::Low,
        ] {
            if let Some(queue) = pending.get_mut(&priority) {
                if let Some(task) = queue.pop_front() {
                    return Some(task);
                }
            }
        }
        None
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_task (lines 317-345) ---[0m
    pub async fn get_task(&self, task_id: &str) -> Option<QueuedTask> {
        // Check active tasks first
        {
            let active = self.active_tasks.read().await;
            if let Some(task) = active.get(task_id) {
                return Some(task.clone());
            }
        }

        // Check pending tasks
        {
            let tasks = self.tasks_by_id.read().await;
            if let Some(task) = tasks.get(task_id) {
                return Some(task.clone());
            }
        }

        // Check completed tasks
        {
            let completed = self.completed_tasks.read().await;
            for task in completed.iter() {
                if task.task.id == task_id {
                    return Some(task.clone());
                }
            }
        }

        None
    }

  ./crates/ccswarm/src/execution/task_queue.rs:102-131 method add_task <-> ./crates/ccswarm/src/execution/task_queue.rs:134-151 method get_next_task
  Similarity: 82.02%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:add_task (lines 102-131) ---[0m
    pub async fn add_task(&self, task: Task) -> String {
        let task_id = task.id.clone();
        let now = Utc::now();

        let queued_task = QueuedTask {
            task: task.clone(),
            status: TaskStatus::Pending,
            created_at: now,
            updated_at: now,
            assigned_agent: None,
            execution_history: Vec::new(),
            metadata: HashMap::new(),
        };

        // Add to priority queue
        {
            let mut pending = self.pending_tasks.write().await;
            if let Some(queue) = pending.get_mut(&task.priority) {
                queue.push_back(queued_task.clone());
            }
        }

        // Add to lookup table
        {
            let mut tasks = self.tasks_by_id.write().await;
            tasks.insert(task_id.clone(), queued_task);
        }

        task_id
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_next_task (lines 134-151) ---[0m
    pub async fn get_next_task(&self) -> Option<QueuedTask> {
        let mut pending = self.pending_tasks.write().await;

        // Check priorities in order: Critical, High, Medium, Low
        for priority in [
            Priority::Critical,
            Priority::High,
            Priority::Medium,
            Priority::Low,
        ] {
            if let Some(queue) = pending.get_mut(&priority) {
                if let Some(task) = queue.pop_front() {
                    return Some(task);
                }
            }
        }
        None
    }

  ./crates/ccswarm/src/execution/task_queue.rs:402-433 method get_stats <-> ./crates/ccswarm/src/execution/task_queue.rs:452-464 function test_task_queue_basic_operations
  Similarity: 85.56%

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_stats (lines 402-433) ---[0m
    pub async fn get_stats(&self) -> TaskQueueStats {
        let pending_count = {
            let pending = self.pending_tasks.read().await;
            pending.values().map(|q| q.len()).sum()
        };

        let active_count = {
            let active = self.active_tasks.read().await;
            active.len()
        };

        let completed_count = {
            let completed = self.completed_tasks.read().await;
            completed.len()
        };

        let failed_count = {
            let completed = self.completed_tasks.read().await;
            completed
                .iter()
                .filter(|t| matches!(t.status, TaskStatus::Failed { .. }))
                .count()
        };

        TaskQueueStats {
            pending_count,
            active_count,
            completed_count,
            failed_count,
            total_count: pending_count + active_count + completed_count,
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:test_task_queue_basic_operations (lines 452-464) ---[0m
    async fn test_task_queue_basic_operations() {
        let queue = TaskQueue::new();

        let task = TaskBuilder::new("Test task".to_string())
            .priority(Priority::High)
            .build();

        let task_id = queue.add_task(task).await;

        let retrieved_task = queue.get_next_task().await;
        assert!(retrieved_task.is_some());
        assert_eq!(retrieved_task.unwrap().task.id, task_id);
    }

  ./crates/ccswarm/src/execution/task_queue.rs:102-131 method add_task <-> ./crates/ccswarm/src/execution/task_queue.rs:452-464 function test_task_queue_basic_operations
  Similarity: 89.17%

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:add_task (lines 102-131) ---[0m
    pub async fn add_task(&self, task: Task) -> String {
        let task_id = task.id.clone();
        let now = Utc::now();

        let queued_task = QueuedTask {
            task: task.clone(),
            status: TaskStatus::Pending,
            created_at: now,
            updated_at: now,
            assigned_agent: None,
            execution_history: Vec::new(),
            metadata: HashMap::new(),
        };

        // Add to priority queue
        {
            let mut pending = self.pending_tasks.write().await;
            if let Some(queue) = pending.get_mut(&task.priority) {
                queue.push_back(queued_task.clone());
            }
        }

        // Add to lookup table
        {
            let mut tasks = self.tasks_by_id.write().await;
            tasks.insert(task_id.clone(), queued_task);
        }

        task_id
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:test_task_queue_basic_operations (lines 452-464) ---[0m
    async fn test_task_queue_basic_operations() {
        let queue = TaskQueue::new();

        let task = TaskBuilder::new("Test task".to_string())
            .priority(Priority::High)
            .build();

        let task_id = queue.add_task(task).await;

        let retrieved_task = queue.get_next_task().await;
        assert!(retrieved_task.is_some());
        assert_eq!(retrieved_task.unwrap().task.id, task_id);
    }

  ./crates/ccswarm/src/execution/task_queue.rs:85-99 method new <-> ./crates/ccswarm/src/execution/task_queue.rs:402-433 method get_stats
  Similarity: 80.69%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:new (lines 85-99) ---[0m
    pub fn new() -> Self {
        let mut pending_tasks = HashMap::new();
        pending_tasks.insert(Priority::Critical, VecDeque::new());
        pending_tasks.insert(Priority::High, VecDeque::new());
        pending_tasks.insert(Priority::Medium, VecDeque::new());
        pending_tasks.insert(Priority::Low, VecDeque::new());

        Self {
            pending_tasks: Arc::new(RwLock::new(pending_tasks)),
            tasks_by_id: Arc::new(RwLock::new(HashMap::new())),
            active_tasks: Arc::new(RwLock::new(HashMap::new())),
            completed_tasks: Arc::new(RwLock::new(VecDeque::new())),
            max_history: 1000,
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_stats (lines 402-433) ---[0m
    pub async fn get_stats(&self) -> TaskQueueStats {
        let pending_count = {
            let pending = self.pending_tasks.read().await;
            pending.values().map(|q| q.len()).sum()
        };

        let active_count = {
            let active = self.active_tasks.read().await;
            active.len()
        };

        let completed_count = {
            let completed = self.completed_tasks.read().await;
            completed.len()
        };

        let failed_count = {
            let completed = self.completed_tasks.read().await;
            completed
                .iter()
                .filter(|t| matches!(t.status, TaskStatus::Failed { .. }))
                .count()
        };

        TaskQueueStats {
            pending_count,
            active_count,
            completed_count,
            failed_count,
            total_count: pending_count + active_count + completed_count,
        }
    }

  ./crates/ccswarm/src/execution/task_queue.rs:85-99 method new <-> ./crates/ccswarm/src/execution/task_queue.rs:278-314 method cancel_task
  Similarity: 72.06%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:new (lines 85-99) ---[0m
    pub fn new() -> Self {
        let mut pending_tasks = HashMap::new();
        pending_tasks.insert(Priority::Critical, VecDeque::new());
        pending_tasks.insert(Priority::High, VecDeque::new());
        pending_tasks.insert(Priority::Medium, VecDeque::new());
        pending_tasks.insert(Priority::Low, VecDeque::new());

        Self {
            pending_tasks: Arc::new(RwLock::new(pending_tasks)),
            tasks_by_id: Arc::new(RwLock::new(HashMap::new())),
            active_tasks: Arc::new(RwLock::new(HashMap::new())),
            completed_tasks: Arc::new(RwLock::new(VecDeque::new())),
            max_history: 1000,
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:cancel_task (lines 278-314) ---[0m
    pub async fn cancel_task(&self, task_id: &str, reason: Option<String>) -> anyhow::Result<()> {
        let now = Utc::now();

        // Try to find task in pending, active, or lookup table
        let mut pending = self.pending_tasks.write().await;
        let mut active = self.active_tasks.write().await;
        let mut tasks = self.tasks_by_id.write().await;
        let mut completed = self.completed_tasks.write().await;

        // Check active tasks first
        if let Some(mut task) = active.remove(task_id) {
            task.status = TaskStatus::Cancelled {
                cancelled_at: now,
                reason,
            };
            task.updated_at = now;
            completed.push_back(task);
            return Ok(());
        }

        // Check pending tasks
        for (_, queue) in pending.iter_mut() {
            if let Some(pos) = queue.iter().position(|t| t.task.id == task_id) {
                let mut task = queue.remove(pos).unwrap();
                task.status = TaskStatus::Cancelled {
                    cancelled_at: now,
                    reason,
                };
                task.updated_at = now;
                completed.push_back(task);
                tasks.remove(task_id);
                return Ok(());
            }
        }

        Err(anyhow::anyhow!("Task not found: {}", task_id))
    }

  ./crates/ccswarm/src/execution/task_queue.rs:317-345 method get_task <-> ./crates/ccswarm/src/execution/task_queue.rs:452-464 function test_task_queue_basic_operations
  Similarity: 83.88%

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_task (lines 317-345) ---[0m
    pub async fn get_task(&self, task_id: &str) -> Option<QueuedTask> {
        // Check active tasks first
        {
            let active = self.active_tasks.read().await;
            if let Some(task) = active.get(task_id) {
                return Some(task.clone());
            }
        }

        // Check pending tasks
        {
            let tasks = self.tasks_by_id.read().await;
            if let Some(task) = tasks.get(task_id) {
                return Some(task.clone());
            }
        }

        // Check completed tasks
        {
            let completed = self.completed_tasks.read().await;
            for task in completed.iter() {
                if task.task.id == task_id {
                    return Some(task.clone());
                }
            }
        }

        None
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:test_task_queue_basic_operations (lines 452-464) ---[0m
    async fn test_task_queue_basic_operations() {
        let queue = TaskQueue::new();

        let task = TaskBuilder::new("Test task".to_string())
            .priority(Priority::High)
            .build();

        let task_id = queue.add_task(task).await;

        let retrieved_task = queue.get_next_task().await;
        assert!(retrieved_task.is_some());
        assert_eq!(retrieved_task.unwrap().task.id, task_id);
    }

  ./crates/ccswarm/src/execution/task_queue.rs:154-167 method assign_task <-> ./crates/ccswarm/src/execution/task_queue.rs:170-200 method start_task_execution
  Similarity: 78.05%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:assign_task (lines 154-167) ---[0m
    pub async fn assign_task(&self, task_id: &str, agent_id: &str) -> anyhow::Result<()> {
        let mut tasks = self.tasks_by_id.write().await;

        if let Some(task) = tasks.get_mut(task_id) {
            task.status = TaskStatus::Assigned {
                agent_id: agent_id.to_string(),
            };
            task.assigned_agent = Some(agent_id.to_string());
            task.updated_at = Utc::now();
            Ok(())
        } else {
            Err(anyhow::anyhow!("Task not found: {}", task_id))
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:start_task_execution (lines 170-200) ---[0m
    pub async fn start_task_execution(&self, task_id: &str, agent_id: &str) -> anyhow::Result<()> {
        let now = Utc::now();

        // Move from tasks_by_id to active_tasks
        let mut tasks = self.tasks_by_id.write().await;
        let mut active = self.active_tasks.write().await;

        if let Some(mut task) = tasks.remove(task_id) {
            task.status = TaskStatus::InProgress {
                agent_id: agent_id.to_string(),
                started_at: now,
            };
            task.updated_at = now;

            // Add execution attempt
            let attempt = TaskExecutionAttempt {
                attempt_id: Uuid::new_v4().to_string(),
                agent_id: agent_id.to_string(),
                started_at: now,
                completed_at: None,
                result: None,
                error: None,
            };
            task.execution_history.push(attempt);

            active.insert(task_id.to_string(), task);
            Ok(())
        } else {
            Err(anyhow::anyhow!("Task not found: {}", task_id))
        }
    }

  ./crates/ccswarm/src/execution/task_queue.rs:85-99 method new <-> ./crates/ccswarm/src/execution/task_queue.rs:102-131 method add_task
  Similarity: 75.27%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:new (lines 85-99) ---[0m
    pub fn new() -> Self {
        let mut pending_tasks = HashMap::new();
        pending_tasks.insert(Priority::Critical, VecDeque::new());
        pending_tasks.insert(Priority::High, VecDeque::new());
        pending_tasks.insert(Priority::Medium, VecDeque::new());
        pending_tasks.insert(Priority::Low, VecDeque::new());

        Self {
            pending_tasks: Arc::new(RwLock::new(pending_tasks)),
            tasks_by_id: Arc::new(RwLock::new(HashMap::new())),
            active_tasks: Arc::new(RwLock::new(HashMap::new())),
            completed_tasks: Arc::new(RwLock::new(VecDeque::new())),
            max_history: 1000,
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:add_task (lines 102-131) ---[0m
    pub async fn add_task(&self, task: Task) -> String {
        let task_id = task.id.clone();
        let now = Utc::now();

        let queued_task = QueuedTask {
            task: task.clone(),
            status: TaskStatus::Pending,
            created_at: now,
            updated_at: now,
            assigned_agent: None,
            execution_history: Vec::new(),
            metadata: HashMap::new(),
        };

        // Add to priority queue
        {
            let mut pending = self.pending_tasks.write().await;
            if let Some(queue) = pending.get_mut(&task.priority) {
                queue.push_back(queued_task.clone());
            }
        }

        // Add to lookup table
        {
            let mut tasks = self.tasks_by_id.write().await;
            tasks.insert(task_id.clone(), queued_task);
        }

        task_id
    }

  ./crates/ccswarm/src/execution/task_queue.rs:85-99 method new <-> ./crates/ccswarm/src/execution/task_queue.rs:317-345 method get_task
  Similarity: 71.44%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:new (lines 85-99) ---[0m
    pub fn new() -> Self {
        let mut pending_tasks = HashMap::new();
        pending_tasks.insert(Priority::Critical, VecDeque::new());
        pending_tasks.insert(Priority::High, VecDeque::new());
        pending_tasks.insert(Priority::Medium, VecDeque::new());
        pending_tasks.insert(Priority::Low, VecDeque::new());

        Self {
            pending_tasks: Arc::new(RwLock::new(pending_tasks)),
            tasks_by_id: Arc::new(RwLock::new(HashMap::new())),
            active_tasks: Arc::new(RwLock::new(HashMap::new())),
            completed_tasks: Arc::new(RwLock::new(VecDeque::new())),
            max_history: 1000,
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_task (lines 317-345) ---[0m
    pub async fn get_task(&self, task_id: &str) -> Option<QueuedTask> {
        // Check active tasks first
        {
            let active = self.active_tasks.read().await;
            if let Some(task) = active.get(task_id) {
                return Some(task.clone());
            }
        }

        // Check pending tasks
        {
            let tasks = self.tasks_by_id.read().await;
            if let Some(task) = tasks.get(task_id) {
                return Some(task.clone());
            }
        }

        // Check completed tasks
        {
            let completed = self.completed_tasks.read().await;
            for task in completed.iter() {
                if task.task.id == task_id {
                    return Some(task.clone());
                }
            }
        }

        None
    }

  ./crates/ccswarm/src/execution/task_queue.rs:134-151 method get_next_task <-> ./crates/ccswarm/src/execution/task_queue.rs:154-167 method assign_task
  Similarity: 83.80%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_next_task (lines 134-151) ---[0m
    pub async fn get_next_task(&self) -> Option<QueuedTask> {
        let mut pending = self.pending_tasks.write().await;

        // Check priorities in order: Critical, High, Medium, Low
        for priority in [
            Priority::Critical,
            Priority::High,
            Priority::Medium,
            Priority::Low,
        ] {
            if let Some(queue) = pending.get_mut(&priority) {
                if let Some(task) = queue.pop_front() {
                    return Some(task);
                }
            }
        }
        None
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:assign_task (lines 154-167) ---[0m
    pub async fn assign_task(&self, task_id: &str, agent_id: &str) -> anyhow::Result<()> {
        let mut tasks = self.tasks_by_id.write().await;

        if let Some(task) = tasks.get_mut(task_id) {
            task.status = TaskStatus::Assigned {
                agent_id: agent_id.to_string(),
            };
            task.assigned_agent = Some(agent_id.to_string());
            task.updated_at = Utc::now();
            Ok(())
        } else {
            Err(anyhow::anyhow!("Task not found: {}", task_id))
        }
    }

  ./crates/ccswarm/src/execution/task_queue.rs:85-99 method new <-> ./crates/ccswarm/src/execution/task_queue.rs:452-464 function test_task_queue_basic_operations
  Similarity: 87.97%

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:new (lines 85-99) ---[0m
    pub fn new() -> Self {
        let mut pending_tasks = HashMap::new();
        pending_tasks.insert(Priority::Critical, VecDeque::new());
        pending_tasks.insert(Priority::High, VecDeque::new());
        pending_tasks.insert(Priority::Medium, VecDeque::new());
        pending_tasks.insert(Priority::Low, VecDeque::new());

        Self {
            pending_tasks: Arc::new(RwLock::new(pending_tasks)),
            tasks_by_id: Arc::new(RwLock::new(HashMap::new())),
            active_tasks: Arc::new(RwLock::new(HashMap::new())),
            completed_tasks: Arc::new(RwLock::new(VecDeque::new())),
            max_history: 1000,
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:test_task_queue_basic_operations (lines 452-464) ---[0m
    async fn test_task_queue_basic_operations() {
        let queue = TaskQueue::new();

        let task = TaskBuilder::new("Test task".to_string())
            .priority(Priority::High)
            .build();

        let task_id = queue.add_task(task).await;

        let retrieved_task = queue.get_next_task().await;
        assert!(retrieved_task.is_some());
        assert_eq!(retrieved_task.unwrap().task.id, task_id);
    }

  ./crates/ccswarm/src/execution/task_queue.rs:85-99 method new <-> ./crates/ccswarm/src/execution/task_queue.rs:134-151 method get_next_task
  Similarity: 73.02%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:new (lines 85-99) ---[0m
    pub fn new() -> Self {
        let mut pending_tasks = HashMap::new();
        pending_tasks.insert(Priority::Critical, VecDeque::new());
        pending_tasks.insert(Priority::High, VecDeque::new());
        pending_tasks.insert(Priority::Medium, VecDeque::new());
        pending_tasks.insert(Priority::Low, VecDeque::new());

        Self {
            pending_tasks: Arc::new(RwLock::new(pending_tasks)),
            tasks_by_id: Arc::new(RwLock::new(HashMap::new())),
            active_tasks: Arc::new(RwLock::new(HashMap::new())),
            completed_tasks: Arc::new(RwLock::new(VecDeque::new())),
            max_history: 1000,
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_queue.rs:get_next_task (lines 134-151) ---[0m
    pub async fn get_next_task(&self) -> Option<QueuedTask> {
        let mut pending = self.pending_tasks.write().await;

        // Check priorities in order: Critical, High, Medium, Low
        for priority in [
            Priority::Critical,
            Priority::High,
            Priority::Medium,
            Priority::Low,
        ] {
            if let Some(queue) = pending.get_mut(&priority) {
                if let Some(task) = queue.pop_front() {
                    return Some(task);
                }
            }
        }
        None
    }


Duplicates in ./crates/ccswarm/src/execution/task_executor.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/execution/task_executor.rs:132-194 method execution_loop <-> ./crates/ccswarm/src/execution/task_executor.rs:375-424 method record_execution_success
  Similarity: 88.00%
  Classes: TaskExecutor <-> TaskExecutor

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:execution_loop (lines 132-194) ---[0m
    async fn execution_loop(
        task_queue: Arc<TaskQueue>,
        agent_pool: Arc<Mutex<AgentPool>>,
        delegation_engine: Arc<Mutex<MasterDelegationEngine>>,
        stats: Arc<RwLock<ExecutionStats>>,
        execution_history: Arc<RwLock<Vec<ExecutionResult>>>,
        active_executions: Arc<RwLock<HashMap<String, tokio::task::JoinHandle<()>>>>,
        max_concurrent: usize,
    ) {
        let mut interval = interval(Duration::from_secs(1)); // Check every second

        loop {
            interval.tick().await;

            // Clean up completed tasks
            Self::cleanup_completed_tasks(&active_executions).await;

            // Check if we can start new tasks
            let active_count = {
                let active = active_executions.read().await;
                active.len()
            };

            if active_count >= max_concurrent {
                continue; // Wait for some tasks to complete
            }

            // Get next task from queue
            if let Some(queued_task) = task_queue.get_next_task().await {
                info!("Processing task: {}", queued_task.task.description);

                // Clone necessary data for the execution task
                let task_queue_clone = task_queue.clone();
                let agent_pool_clone = agent_pool.clone();
                let delegation_engine_clone = delegation_engine.clone();
                let stats_clone = stats.clone();
                let execution_history_clone = execution_history.clone();
                let task_clone = queued_task.clone();

                // Spawn execution task
                let handle = tokio::spawn(async move {
                    Self::execute_single_task(
                        task_clone,
                        task_queue_clone,
                        agent_pool_clone,
                        delegation_engine_clone,
                        stats_clone,
                        execution_history_clone,
                    )
                    .await;
                });

                // Store the handle
                {
                    let mut active = active_executions.write().await;
                    active.insert(queued_task.task.id.clone(), handle);
                }
            }

            // Small delay to prevent busy waiting
            sleep(Duration::from_millis(100)).await;
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:record_execution_success (lines 375-424) ---[0m
    async fn record_execution_success(
        stats: &Arc<RwLock<ExecutionStats>>,
        execution_history: &Arc<RwLock<Vec<ExecutionResult>>>,
        task_id: &str,
        result: TaskResult,
        duration: Duration,
        agent_id: &str,
        orchestration_used: bool,
    ) {
        let execution_result = ExecutionResult {
            task_id: task_id.to_string(),
            success: true,
            result: Some(result),
            error: None,
            duration,
            agent_used: Some(agent_id.to_string()),
            orchestration_used,
        };

        // Update stats
        {
            let mut stats_guard = stats.write().await;
            stats_guard.tasks_executed += 1;
            stats_guard.tasks_succeeded += 1;
            stats_guard.total_duration += duration;
            stats_guard.average_duration =
                stats_guard.total_duration / stats_guard.tasks_executed as u32;

            let orchestration_count = execution_history
                .read()
                .await
                .iter()
                .filter(|r| r.orchestration_used)
                .count()
                + if orchestration_used { 1 } else { 0 };
            stats_guard.orchestration_usage =
                (orchestration_count as f64) / (stats_guard.tasks_executed as f64) * 100.0;
        }

        // Add to history
        {
            let mut history = execution_history.write().await;
            history.push(execution_result);

            // Keep history size manageable
            if history.len() > 1000 {
                history.drain(0..100); // Remove oldest 100 entries
            }
        }
    }

  ./crates/ccswarm/src/execution/task_executor.rs:132-194 method execution_loop <-> ./crates/ccswarm/src/execution/task_executor.rs:427-466 method record_execution_failure
  Similarity: 88.00%
  Classes: TaskExecutor <-> TaskExecutor

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:execution_loop (lines 132-194) ---[0m
    async fn execution_loop(
        task_queue: Arc<TaskQueue>,
        agent_pool: Arc<Mutex<AgentPool>>,
        delegation_engine: Arc<Mutex<MasterDelegationEngine>>,
        stats: Arc<RwLock<ExecutionStats>>,
        execution_history: Arc<RwLock<Vec<ExecutionResult>>>,
        active_executions: Arc<RwLock<HashMap<String, tokio::task::JoinHandle<()>>>>,
        max_concurrent: usize,
    ) {
        let mut interval = interval(Duration::from_secs(1)); // Check every second

        loop {
            interval.tick().await;

            // Clean up completed tasks
            Self::cleanup_completed_tasks(&active_executions).await;

            // Check if we can start new tasks
            let active_count = {
                let active = active_executions.read().await;
                active.len()
            };

            if active_count >= max_concurrent {
                continue; // Wait for some tasks to complete
            }

            // Get next task from queue
            if let Some(queued_task) = task_queue.get_next_task().await {
                info!("Processing task: {}", queued_task.task.description);

                // Clone necessary data for the execution task
                let task_queue_clone = task_queue.clone();
                let agent_pool_clone = agent_pool.clone();
                let delegation_engine_clone = delegation_engine.clone();
                let stats_clone = stats.clone();
                let execution_history_clone = execution_history.clone();
                let task_clone = queued_task.clone();

                // Spawn execution task
                let handle = tokio::spawn(async move {
                    Self::execute_single_task(
                        task_clone,
                        task_queue_clone,
                        agent_pool_clone,
                        delegation_engine_clone,
                        stats_clone,
                        execution_history_clone,
                    )
                    .await;
                });

                // Store the handle
                {
                    let mut active = active_executions.write().await;
                    active.insert(queued_task.task.id.clone(), handle);
                }
            }

            // Small delay to prevent busy waiting
            sleep(Duration::from_millis(100)).await;
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:record_execution_failure (lines 427-466) ---[0m
    async fn record_execution_failure(
        _task_queue: &Arc<TaskQueue>,
        stats: &Arc<RwLock<ExecutionStats>>,
        execution_history: &Arc<RwLock<Vec<ExecutionResult>>>,
        task_id: &str,
        error: String,
        duration: Duration,
    ) {
        let execution_result = ExecutionResult {
            task_id: task_id.to_string(),
            success: false,
            result: None,
            error: Some(error),
            duration,
            agent_used: None,
            orchestration_used: false,
        };

        // Update stats
        {
            let mut stats_guard = stats.write().await;
            stats_guard.tasks_executed += 1;
            stats_guard.tasks_failed += 1;
            stats_guard.total_duration += duration;
            if stats_guard.tasks_executed > 0 {
                stats_guard.average_duration =
                    stats_guard.total_duration / stats_guard.tasks_executed as u32;
            }
        }

        // Add to history
        {
            let mut history = execution_history.write().await;
            history.push(execution_result);

            if history.len() > 1000 {
                history.drain(0..100);
            }
        }
    }

  ./crates/ccswarm/src/execution/task_executor.rs:375-424 method record_execution_success <-> ./crates/ccswarm/src/execution/task_executor.rs:427-466 method record_execution_failure
  Similarity: 96.39%
  Classes: TaskExecutor <-> TaskExecutor

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:record_execution_success (lines 375-424) ---[0m
    async fn record_execution_success(
        stats: &Arc<RwLock<ExecutionStats>>,
        execution_history: &Arc<RwLock<Vec<ExecutionResult>>>,
        task_id: &str,
        result: TaskResult,
        duration: Duration,
        agent_id: &str,
        orchestration_used: bool,
    ) {
        let execution_result = ExecutionResult {
            task_id: task_id.to_string(),
            success: true,
            result: Some(result),
            error: None,
            duration,
            agent_used: Some(agent_id.to_string()),
            orchestration_used,
        };

        // Update stats
        {
            let mut stats_guard = stats.write().await;
            stats_guard.tasks_executed += 1;
            stats_guard.tasks_succeeded += 1;
            stats_guard.total_duration += duration;
            stats_guard.average_duration =
                stats_guard.total_duration / stats_guard.tasks_executed as u32;

            let orchestration_count = execution_history
                .read()
                .await
                .iter()
                .filter(|r| r.orchestration_used)
                .count()
                + if orchestration_used { 1 } else { 0 };
            stats_guard.orchestration_usage =
                (orchestration_count as f64) / (stats_guard.tasks_executed as f64) * 100.0;
        }

        // Add to history
        {
            let mut history = execution_history.write().await;
            history.push(execution_result);

            // Keep history size manageable
            if history.len() > 1000 {
                history.drain(0..100); // Remove oldest 100 entries
            }
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:record_execution_failure (lines 427-466) ---[0m
    async fn record_execution_failure(
        _task_queue: &Arc<TaskQueue>,
        stats: &Arc<RwLock<ExecutionStats>>,
        execution_history: &Arc<RwLock<Vec<ExecutionResult>>>,
        task_id: &str,
        error: String,
        duration: Duration,
    ) {
        let execution_result = ExecutionResult {
            task_id: task_id.to_string(),
            success: false,
            result: None,
            error: Some(error),
            duration,
            agent_used: None,
            orchestration_used: false,
        };

        // Update stats
        {
            let mut stats_guard = stats.write().await;
            stats_guard.tasks_executed += 1;
            stats_guard.tasks_failed += 1;
            stats_guard.total_duration += duration;
            if stats_guard.tasks_executed > 0 {
                stats_guard.average_duration =
                    stats_guard.total_duration / stats_guard.tasks_executed as u32;
            }
        }

        // Add to history
        {
            let mut history = execution_history.write().await;
            history.push(execution_result);

            if history.len() > 1000 {
                history.drain(0..100);
            }
        }
    }

  ./crates/ccswarm/src/execution/task_executor.rs:132-194 method execution_loop <-> ./crates/ccswarm/src/execution/task_executor.rs:334-372 method is_complex_task
  Similarity: 74.87%
  Classes: TaskExecutor <-> TaskExecutor

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:execution_loop (lines 132-194) ---[0m
    async fn execution_loop(
        task_queue: Arc<TaskQueue>,
        agent_pool: Arc<Mutex<AgentPool>>,
        delegation_engine: Arc<Mutex<MasterDelegationEngine>>,
        stats: Arc<RwLock<ExecutionStats>>,
        execution_history: Arc<RwLock<Vec<ExecutionResult>>>,
        active_executions: Arc<RwLock<HashMap<String, tokio::task::JoinHandle<()>>>>,
        max_concurrent: usize,
    ) {
        let mut interval = interval(Duration::from_secs(1)); // Check every second

        loop {
            interval.tick().await;

            // Clean up completed tasks
            Self::cleanup_completed_tasks(&active_executions).await;

            // Check if we can start new tasks
            let active_count = {
                let active = active_executions.read().await;
                active.len()
            };

            if active_count >= max_concurrent {
                continue; // Wait for some tasks to complete
            }

            // Get next task from queue
            if let Some(queued_task) = task_queue.get_next_task().await {
                info!("Processing task: {}", queued_task.task.description);

                // Clone necessary data for the execution task
                let task_queue_clone = task_queue.clone();
                let agent_pool_clone = agent_pool.clone();
                let delegation_engine_clone = delegation_engine.clone();
                let stats_clone = stats.clone();
                let execution_history_clone = execution_history.clone();
                let task_clone = queued_task.clone();

                // Spawn execution task
                let handle = tokio::spawn(async move {
                    Self::execute_single_task(
                        task_clone,
                        task_queue_clone,
                        agent_pool_clone,
                        delegation_engine_clone,
                        stats_clone,
                        execution_history_clone,
                    )
                    .await;
                });

                // Store the handle
                {
                    let mut active = active_executions.write().await;
                    active.insert(queued_task.task.id.clone(), handle);
                }
            }

            // Small delay to prevent busy waiting
            sleep(Duration::from_millis(100)).await;
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:is_complex_task (lines 334-372) ---[0m
    fn is_complex_task(task: &Task) -> bool {
        let description = task.description.to_lowercase();
        let details = task.details.as_deref().unwrap_or("").to_lowercase();
        let combined = format!("{} {}", description, details);

        // Complex indicators
        let complexity_keywords = [
            "implement",
            "create",
            "build",
            "design",
            "develop",
            "integrate",
            "migrate",
            "refactor",
            "comprehensive",
            "multiple",
            "several",
            "complete",
            "full",
            "and",
            "then",
            "also",
            "plus",
            "step",
        ];

        let keyword_count = complexity_keywords
            .iter()
            .filter(|&keyword| combined.contains(keyword))
            .count();

        // High priority tasks or those with many complexity indicators
        keyword_count >= 3
            || matches!(
                task.priority,
                crate::agent::Priority::High | crate::agent::Priority::Critical
            )
    }

  ./crates/ccswarm/src/execution/task_executor.rs:57-94 method new <-> ./crates/ccswarm/src/execution/task_executor.rs:334-372 method is_complex_task
  Similarity: 92.32%
  Classes: TaskExecutor <-> TaskExecutor

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:new (lines 57-94) ---[0m
    pub async fn new(config: &CcswarmConfig) -> anyhow::Result<Self> {
        let task_queue = Arc::new(TaskQueue::new());

        // Create agent pool and spawn configured agents
        let mut agent_pool = AgentPool::new().await?;
        for agent_type in config.agents.keys() {
            if let Err(e) = agent_pool.spawn_agent(agent_type, config).await {
                error!("Failed to spawn {} agent: {}", agent_type, e);
                // Continue with other agents rather than failing completely
            } else {
                info!("Successfully spawned {} agent", agent_type);
            }
        }
        let agent_pool = Arc::new(Mutex::new(agent_pool));

        let delegation_engine = Arc::new(Mutex::new(MasterDelegationEngine::new(
            crate::orchestrator::master_delegation::DelegationStrategy::Hybrid,
        )));

        let stats = Arc::new(RwLock::new(ExecutionStats {
            tasks_executed: 0,
            tasks_succeeded: 0,
            tasks_failed: 0,
            average_duration: Duration::from_secs(0),
            total_duration: Duration::from_secs(0),
            orchestration_usage: 0.0,
        }));

        Ok(Self {
            task_queue,
            agent_pool,
            delegation_engine,
            stats,
            execution_history: Arc::new(RwLock::new(Vec::new())),
            max_concurrent_tasks: 5, // Configurable
            active_executions: Arc::new(RwLock::new(HashMap::new())),
        })
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:is_complex_task (lines 334-372) ---[0m
    fn is_complex_task(task: &Task) -> bool {
        let description = task.description.to_lowercase();
        let details = task.details.as_deref().unwrap_or("").to_lowercase();
        let combined = format!("{} {}", description, details);

        // Complex indicators
        let complexity_keywords = [
            "implement",
            "create",
            "build",
            "design",
            "develop",
            "integrate",
            "migrate",
            "refactor",
            "comprehensive",
            "multiple",
            "several",
            "complete",
            "full",
            "and",
            "then",
            "also",
            "plus",
            "step",
        ];

        let keyword_count = complexity_keywords
            .iter()
            .filter(|&keyword| combined.contains(keyword))
            .count();

        // High priority tasks or those with many complexity indicators
        keyword_count >= 3
            || matches!(
                task.priority,
                crate::agent::Priority::High | crate::agent::Priority::Critical
            )
    }

  ./crates/ccswarm/src/execution/task_executor.rs:334-372 method is_complex_task <-> ./crates/ccswarm/src/execution/task_executor.rs:375-424 method record_execution_success
  Similarity: 71.39%
  Classes: TaskExecutor <-> TaskExecutor

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:is_complex_task (lines 334-372) ---[0m
    fn is_complex_task(task: &Task) -> bool {
        let description = task.description.to_lowercase();
        let details = task.details.as_deref().unwrap_or("").to_lowercase();
        let combined = format!("{} {}", description, details);

        // Complex indicators
        let complexity_keywords = [
            "implement",
            "create",
            "build",
            "design",
            "develop",
            "integrate",
            "migrate",
            "refactor",
            "comprehensive",
            "multiple",
            "several",
            "complete",
            "full",
            "and",
            "then",
            "also",
            "plus",
            "step",
        ];

        let keyword_count = complexity_keywords
            .iter()
            .filter(|&keyword| combined.contains(keyword))
            .count();

        // High priority tasks or those with many complexity indicators
        keyword_count >= 3
            || matches!(
                task.priority,
                crate::agent::Priority::High | crate::agent::Priority::Critical
            )
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:record_execution_success (lines 375-424) ---[0m
    async fn record_execution_success(
        stats: &Arc<RwLock<ExecutionStats>>,
        execution_history: &Arc<RwLock<Vec<ExecutionResult>>>,
        task_id: &str,
        result: TaskResult,
        duration: Duration,
        agent_id: &str,
        orchestration_used: bool,
    ) {
        let execution_result = ExecutionResult {
            task_id: task_id.to_string(),
            success: true,
            result: Some(result),
            error: None,
            duration,
            agent_used: Some(agent_id.to_string()),
            orchestration_used,
        };

        // Update stats
        {
            let mut stats_guard = stats.write().await;
            stats_guard.tasks_executed += 1;
            stats_guard.tasks_succeeded += 1;
            stats_guard.total_duration += duration;
            stats_guard.average_duration =
                stats_guard.total_duration / stats_guard.tasks_executed as u32;

            let orchestration_count = execution_history
                .read()
                .await
                .iter()
                .filter(|r| r.orchestration_used)
                .count()
                + if orchestration_used { 1 } else { 0 };
            stats_guard.orchestration_usage =
                (orchestration_count as f64) / (stats_guard.tasks_executed as f64) * 100.0;
        }

        // Add to history
        {
            let mut history = execution_history.write().await;
            history.push(execution_result);

            // Keep history size manageable
            if history.len() > 1000 {
                history.drain(0..100); // Remove oldest 100 entries
            }
        }
    }

  ./crates/ccswarm/src/execution/task_executor.rs:57-94 method new <-> ./crates/ccswarm/src/execution/task_executor.rs:375-424 method record_execution_success
  Similarity: 70.78%
  Classes: TaskExecutor <-> TaskExecutor

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:new (lines 57-94) ---[0m
    pub async fn new(config: &CcswarmConfig) -> anyhow::Result<Self> {
        let task_queue = Arc::new(TaskQueue::new());

        // Create agent pool and spawn configured agents
        let mut agent_pool = AgentPool::new().await?;
        for agent_type in config.agents.keys() {
            if let Err(e) = agent_pool.spawn_agent(agent_type, config).await {
                error!("Failed to spawn {} agent: {}", agent_type, e);
                // Continue with other agents rather than failing completely
            } else {
                info!("Successfully spawned {} agent", agent_type);
            }
        }
        let agent_pool = Arc::new(Mutex::new(agent_pool));

        let delegation_engine = Arc::new(Mutex::new(MasterDelegationEngine::new(
            crate::orchestrator::master_delegation::DelegationStrategy::Hybrid,
        )));

        let stats = Arc::new(RwLock::new(ExecutionStats {
            tasks_executed: 0,
            tasks_succeeded: 0,
            tasks_failed: 0,
            average_duration: Duration::from_secs(0),
            total_duration: Duration::from_secs(0),
            orchestration_usage: 0.0,
        }));

        Ok(Self {
            task_queue,
            agent_pool,
            delegation_engine,
            stats,
            execution_history: Arc::new(RwLock::new(Vec::new())),
            max_concurrent_tasks: 5, // Configurable
            active_executions: Arc::new(RwLock::new(HashMap::new())),
        })
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:record_execution_success (lines 375-424) ---[0m
    async fn record_execution_success(
        stats: &Arc<RwLock<ExecutionStats>>,
        execution_history: &Arc<RwLock<Vec<ExecutionResult>>>,
        task_id: &str,
        result: TaskResult,
        duration: Duration,
        agent_id: &str,
        orchestration_used: bool,
    ) {
        let execution_result = ExecutionResult {
            task_id: task_id.to_string(),
            success: true,
            result: Some(result),
            error: None,
            duration,
            agent_used: Some(agent_id.to_string()),
            orchestration_used,
        };

        // Update stats
        {
            let mut stats_guard = stats.write().await;
            stats_guard.tasks_executed += 1;
            stats_guard.tasks_succeeded += 1;
            stats_guard.total_duration += duration;
            stats_guard.average_duration =
                stats_guard.total_duration / stats_guard.tasks_executed as u32;

            let orchestration_count = execution_history
                .read()
                .await
                .iter()
                .filter(|r| r.orchestration_used)
                .count()
                + if orchestration_used { 1 } else { 0 };
            stats_guard.orchestration_usage =
                (orchestration_count as f64) / (stats_guard.tasks_executed as f64) * 100.0;
        }

        // Add to history
        {
            let mut history = execution_history.write().await;
            history.push(execution_result);

            // Keep history size manageable
            if history.len() > 1000 {
                history.drain(0..100); // Remove oldest 100 entries
            }
        }
    }

  ./crates/ccswarm/src/execution/task_executor.rs:57-94 method new <-> ./crates/ccswarm/src/execution/task_executor.rs:427-466 method record_execution_failure
  Similarity: 70.78%
  Classes: TaskExecutor <-> TaskExecutor

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:new (lines 57-94) ---[0m
    pub async fn new(config: &CcswarmConfig) -> anyhow::Result<Self> {
        let task_queue = Arc::new(TaskQueue::new());

        // Create agent pool and spawn configured agents
        let mut agent_pool = AgentPool::new().await?;
        for agent_type in config.agents.keys() {
            if let Err(e) = agent_pool.spawn_agent(agent_type, config).await {
                error!("Failed to spawn {} agent: {}", agent_type, e);
                // Continue with other agents rather than failing completely
            } else {
                info!("Successfully spawned {} agent", agent_type);
            }
        }
        let agent_pool = Arc::new(Mutex::new(agent_pool));

        let delegation_engine = Arc::new(Mutex::new(MasterDelegationEngine::new(
            crate::orchestrator::master_delegation::DelegationStrategy::Hybrid,
        )));

        let stats = Arc::new(RwLock::new(ExecutionStats {
            tasks_executed: 0,
            tasks_succeeded: 0,
            tasks_failed: 0,
            average_duration: Duration::from_secs(0),
            total_duration: Duration::from_secs(0),
            orchestration_usage: 0.0,
        }));

        Ok(Self {
            task_queue,
            agent_pool,
            delegation_engine,
            stats,
            execution_history: Arc::new(RwLock::new(Vec::new())),
            max_concurrent_tasks: 5, // Configurable
            active_executions: Arc::new(RwLock::new(HashMap::new())),
        })
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:record_execution_failure (lines 427-466) ---[0m
    async fn record_execution_failure(
        _task_queue: &Arc<TaskQueue>,
        stats: &Arc<RwLock<ExecutionStats>>,
        execution_history: &Arc<RwLock<Vec<ExecutionResult>>>,
        task_id: &str,
        error: String,
        duration: Duration,
    ) {
        let execution_result = ExecutionResult {
            task_id: task_id.to_string(),
            success: false,
            result: None,
            error: Some(error),
            duration,
            agent_used: None,
            orchestration_used: false,
        };

        // Update stats
        {
            let mut stats_guard = stats.write().await;
            stats_guard.tasks_executed += 1;
            stats_guard.tasks_failed += 1;
            stats_guard.total_duration += duration;
            if stats_guard.tasks_executed > 0 {
                stats_guard.average_duration =
                    stats_guard.total_duration / stats_guard.tasks_executed as u32;
            }
        }

        // Add to history
        {
            let mut history = execution_history.write().await;
            history.push(execution_result);

            if history.len() > 1000 {
                history.drain(0..100);
            }
        }
    }

  ./crates/ccswarm/src/execution/task_executor.rs:103-129 method start <-> ./crates/ccswarm/src/execution/task_executor.rs:334-372 method is_complex_task
  Similarity: 79.92%
  Classes: TaskExecutor <-> TaskExecutor

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:start (lines 103-129) ---[0m
    pub async fn start(&self) -> anyhow::Result<()> {
        info!("Starting task execution engine");

        // Start task processing loop
        let task_queue = self.task_queue.clone();
        let agent_pool = self.agent_pool.clone();
        let delegation_engine = self.delegation_engine.clone();
        let stats = self.stats.clone();
        let execution_history = self.execution_history.clone();
        let active_executions = self.active_executions.clone();
        let max_concurrent = self.max_concurrent_tasks;

        tokio::spawn(async move {
            Self::execution_loop(
                task_queue,
                agent_pool,
                delegation_engine,
                stats,
                execution_history,
                active_executions,
                max_concurrent,
            )
            .await;
        });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:is_complex_task (lines 334-372) ---[0m
    fn is_complex_task(task: &Task) -> bool {
        let description = task.description.to_lowercase();
        let details = task.details.as_deref().unwrap_or("").to_lowercase();
        let combined = format!("{} {}", description, details);

        // Complex indicators
        let complexity_keywords = [
            "implement",
            "create",
            "build",
            "design",
            "develop",
            "integrate",
            "migrate",
            "refactor",
            "comprehensive",
            "multiple",
            "several",
            "complete",
            "full",
            "and",
            "then",
            "also",
            "plus",
            "step",
        ];

        let keyword_count = complexity_keywords
            .iter()
            .filter(|&keyword| combined.contains(keyword))
            .count();

        // High priority tasks or those with many complexity indicators
        keyword_count >= 3
            || matches!(
                task.priority,
                crate::agent::Priority::High | crate::agent::Priority::Critical
            )
    }

  ./crates/ccswarm/src/execution/task_executor.rs:427-466 method record_execution_failure <-> ./crates/ccswarm/src/execution/task_executor.rs:469-489 method cleanup_completed_tasks
  Similarity: 73.29%
  Classes: TaskExecutor <-> TaskExecutor

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:record_execution_failure (lines 427-466) ---[0m
    async fn record_execution_failure(
        _task_queue: &Arc<TaskQueue>,
        stats: &Arc<RwLock<ExecutionStats>>,
        execution_history: &Arc<RwLock<Vec<ExecutionResult>>>,
        task_id: &str,
        error: String,
        duration: Duration,
    ) {
        let execution_result = ExecutionResult {
            task_id: task_id.to_string(),
            success: false,
            result: None,
            error: Some(error),
            duration,
            agent_used: None,
            orchestration_used: false,
        };

        // Update stats
        {
            let mut stats_guard = stats.write().await;
            stats_guard.tasks_executed += 1;
            stats_guard.tasks_failed += 1;
            stats_guard.total_duration += duration;
            if stats_guard.tasks_executed > 0 {
                stats_guard.average_duration =
                    stats_guard.total_duration / stats_guard.tasks_executed as u32;
            }
        }

        // Add to history
        {
            let mut history = execution_history.write().await;
            history.push(execution_result);

            if history.len() > 1000 {
                history.drain(0..100);
            }
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:cleanup_completed_tasks (lines 469-489) ---[0m
    async fn cleanup_completed_tasks(
        active_executions: &Arc<RwLock<HashMap<String, tokio::task::JoinHandle<()>>>>,
    ) {
        let mut to_remove = Vec::new();

        {
            let active = active_executions.read().await;
            for (task_id, handle) in active.iter() {
                if handle.is_finished() {
                    to_remove.push(task_id.clone());
                }
            }
        }

        if !to_remove.is_empty() {
            let mut active = active_executions.write().await;
            for task_id in to_remove {
                active.remove(&task_id);
            }
        }
    }

  ./crates/ccswarm/src/execution/task_executor.rs:305-317 method execute_with_orchestration <-> ./crates/ccswarm/src/execution/task_executor.rs:469-489 method cleanup_completed_tasks
  Similarity: 86.92%
  Classes: TaskExecutor <-> TaskExecutor

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:execute_with_orchestration (lines 305-317) ---[0m
    async fn execute_with_orchestration(
        task: &Task,
        agent_pool: &Arc<Mutex<AgentPool>>,
        _agent_id: &str,
    ) -> anyhow::Result<TaskResult> {
        let pool = agent_pool.lock().await;

        // Use the orchestrator interface
        match pool.orchestrate_task(task).await {
            Ok(result) => Ok(result),
            Err(e) => Err(anyhow::anyhow!("Orchestration failed: {}", e)),
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:cleanup_completed_tasks (lines 469-489) ---[0m
    async fn cleanup_completed_tasks(
        active_executions: &Arc<RwLock<HashMap<String, tokio::task::JoinHandle<()>>>>,
    ) {
        let mut to_remove = Vec::new();

        {
            let active = active_executions.read().await;
            for (task_id, handle) in active.iter() {
                if handle.is_finished() {
                    to_remove.push(task_id.clone());
                }
            }
        }

        if !to_remove.is_empty() {
            let mut active = active_executions.write().await;
            for task_id in to_remove {
                active.remove(&task_id);
            }
        }
    }

  ./crates/ccswarm/src/execution/task_executor.rs:320-331 method execute_directly <-> ./crates/ccswarm/src/execution/task_executor.rs:469-489 method cleanup_completed_tasks
  Similarity: 74.55%
  Classes: TaskExecutor <-> TaskExecutor

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:execute_directly (lines 320-331) ---[0m
    async fn execute_directly(
        task: &Task,
        agent_pool: &Arc<Mutex<AgentPool>>,
        agent_id: &str,
    ) -> anyhow::Result<TaskResult> {
        let pool = agent_pool.lock().await;

        match pool.execute_task_with_agent(agent_id, task).await {
            Ok(result) => Ok(result),
            Err(e) => Err(anyhow::anyhow!("Direct execution failed: {}", e)),
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:cleanup_completed_tasks (lines 469-489) ---[0m
    async fn cleanup_completed_tasks(
        active_executions: &Arc<RwLock<HashMap<String, tokio::task::JoinHandle<()>>>>,
    ) {
        let mut to_remove = Vec::new();

        {
            let active = active_executions.read().await;
            for (task_id, handle) in active.iter() {
                if handle.is_finished() {
                    to_remove.push(task_id.clone());
                }
            }
        }

        if !to_remove.is_empty() {
            let mut active = active_executions.write().await;
            for task_id in to_remove {
                active.remove(&task_id);
            }
        }
    }

  ./crates/ccswarm/src/execution/task_executor.rs:305-317 method execute_with_orchestration <-> ./crates/ccswarm/src/execution/task_executor.rs:320-331 method execute_directly
  Similarity: 94.32%
  Classes: TaskExecutor <-> TaskExecutor

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:execute_with_orchestration (lines 305-317) ---[0m
    async fn execute_with_orchestration(
        task: &Task,
        agent_pool: &Arc<Mutex<AgentPool>>,
        _agent_id: &str,
    ) -> anyhow::Result<TaskResult> {
        let pool = agent_pool.lock().await;

        // Use the orchestrator interface
        match pool.orchestrate_task(task).await {
            Ok(result) => Ok(result),
            Err(e) => Err(anyhow::anyhow!("Orchestration failed: {}", e)),
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:execute_directly (lines 320-331) ---[0m
    async fn execute_directly(
        task: &Task,
        agent_pool: &Arc<Mutex<AgentPool>>,
        agent_id: &str,
    ) -> anyhow::Result<TaskResult> {
        let pool = agent_pool.lock().await;

        match pool.execute_task_with_agent(agent_id, task).await {
            Ok(result) => Ok(result),
            Err(e) => Err(anyhow::anyhow!("Direct execution failed: {}", e)),
        }
    }

  ./crates/ccswarm/src/execution/task_executor.rs:469-489 method cleanup_completed_tasks <-> ./crates/ccswarm/src/execution/task_executor.rs:511-522 method cancel_task
  Similarity: 71.26%
  Classes: TaskExecutor <-> TaskExecutor

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:cleanup_completed_tasks (lines 469-489) ---[0m
    async fn cleanup_completed_tasks(
        active_executions: &Arc<RwLock<HashMap<String, tokio::task::JoinHandle<()>>>>,
    ) {
        let mut to_remove = Vec::new();

        {
            let active = active_executions.read().await;
            for (task_id, handle) in active.iter() {
                if handle.is_finished() {
                    to_remove.push(task_id.clone());
                }
            }
        }

        if !to_remove.is_empty() {
            let mut active = active_executions.write().await;
            for task_id in to_remove {
                active.remove(&task_id);
            }
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:cancel_task (lines 511-522) ---[0m
    pub async fn cancel_task(&self, task_id: &str, reason: Option<String>) -> anyhow::Result<()> {
        // Cancel in queue
        self.task_queue.cancel_task(task_id, reason).await?;

        // Cancel active execution if running
        let mut active = self.active_executions.write().await;
        if let Some(handle) = active.remove(task_id) {
            handle.abort();
        }

        Ok(())
    }

  ./crates/ccswarm/src/execution/task_executor.rs:469-489 method cleanup_completed_tasks <-> ./crates/ccswarm/src/execution/task_executor.rs:564-573 function test_complex_task_detection
  Similarity: 74.55%

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:cleanup_completed_tasks (lines 469-489) ---[0m
    async fn cleanup_completed_tasks(
        active_executions: &Arc<RwLock<HashMap<String, tokio::task::JoinHandle<()>>>>,
    ) {
        let mut to_remove = Vec::new();

        {
            let active = active_executions.read().await;
            for (task_id, handle) in active.iter() {
                if handle.is_finished() {
                    to_remove.push(task_id.clone());
                }
            }
        }

        if !to_remove.is_empty() {
            let mut active = active_executions.write().await;
            for task_id in to_remove {
                active.remove(&task_id);
            }
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:test_complex_task_detection (lines 564-573) ---[0m
    async fn test_complex_task_detection() {
        let simple_task = TaskBuilder::new("Fix typo".to_string()).build();
        assert!(!TaskExecutor::is_complex_task(&simple_task));

        let complex_task = TaskBuilder::new(
            "Implement complete user authentication system with database integration".to_string(),
        )
        .build();
        assert!(TaskExecutor::is_complex_task(&complex_task));
    }

  ./crates/ccswarm/src/execution/task_executor.rs:469-489 method cleanup_completed_tasks <-> ./crates/ccswarm/src/execution/task_executor.rs:502-508 method get_execution_history
  Similarity: 76.15%
  Classes: TaskExecutor <-> TaskExecutor

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:cleanup_completed_tasks (lines 469-489) ---[0m
    async fn cleanup_completed_tasks(
        active_executions: &Arc<RwLock<HashMap<String, tokio::task::JoinHandle<()>>>>,
    ) {
        let mut to_remove = Vec::new();

        {
            let active = active_executions.read().await;
            for (task_id, handle) in active.iter() {
                if handle.is_finished() {
                    to_remove.push(task_id.clone());
                }
            }
        }

        if !to_remove.is_empty() {
            let mut active = active_executions.write().await;
            for task_id in to_remove {
                active.remove(&task_id);
            }
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:get_execution_history (lines 502-508) ---[0m
    pub async fn get_execution_history(&self, limit: Option<usize>) -> Vec<ExecutionResult> {
        let history = self.execution_history.read().await;
        match limit {
            Some(n) => history.iter().rev().take(n).cloned().collect(),
            None => history.clone(),
        }
    }

  ./crates/ccswarm/src/execution/task_executor.rs:305-317 method execute_with_orchestration <-> ./crates/ccswarm/src/execution/task_executor.rs:502-508 method get_execution_history
  Similarity: 86.98%
  Classes: TaskExecutor <-> TaskExecutor

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:execute_with_orchestration (lines 305-317) ---[0m
    async fn execute_with_orchestration(
        task: &Task,
        agent_pool: &Arc<Mutex<AgentPool>>,
        _agent_id: &str,
    ) -> anyhow::Result<TaskResult> {
        let pool = agent_pool.lock().await;

        // Use the orchestrator interface
        match pool.orchestrate_task(task).await {
            Ok(result) => Ok(result),
            Err(e) => Err(anyhow::anyhow!("Orchestration failed: {}", e)),
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:get_execution_history (lines 502-508) ---[0m
    pub async fn get_execution_history(&self, limit: Option<usize>) -> Vec<ExecutionResult> {
        let history = self.execution_history.read().await;
        match limit {
            Some(n) => history.iter().rev().take(n).cloned().collect(),
            None => history.clone(),
        }
    }

  ./crates/ccswarm/src/execution/task_executor.rs:320-331 method execute_directly <-> ./crates/ccswarm/src/execution/task_executor.rs:502-508 method get_execution_history
  Similarity: 89.30%
  Classes: TaskExecutor <-> TaskExecutor

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:execute_directly (lines 320-331) ---[0m
    async fn execute_directly(
        task: &Task,
        agent_pool: &Arc<Mutex<AgentPool>>,
        agent_id: &str,
    ) -> anyhow::Result<TaskResult> {
        let pool = agent_pool.lock().await;

        match pool.execute_task_with_agent(agent_id, task).await {
            Ok(result) => Ok(result),
            Err(e) => Err(anyhow::anyhow!("Direct execution failed: {}", e)),
        }
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:get_execution_history (lines 502-508) ---[0m
    pub async fn get_execution_history(&self, limit: Option<usize>) -> Vec<ExecutionResult> {
        let history = self.execution_history.read().await;
        match limit {
            Some(n) => history.iter().rev().take(n).cloned().collect(),
            None => history.clone(),
        }
    }

  ./crates/ccswarm/src/execution/task_executor.rs:511-522 method cancel_task <-> ./crates/ccswarm/src/execution/task_executor.rs:553-561 function test_task_executor_creation
  Similarity: 77.86%

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:cancel_task (lines 511-522) ---[0m
    pub async fn cancel_task(&self, task_id: &str, reason: Option<String>) -> anyhow::Result<()> {
        // Cancel in queue
        self.task_queue.cancel_task(task_id, reason).await?;

        // Cancel active execution if running
        let mut active = self.active_executions.write().await;
        if let Some(handle) = active.remove(task_id) {
            handle.abort();
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:test_task_executor_creation (lines 553-561) ---[0m
    async fn test_task_executor_creation() {
        let config = CcswarmConfig::default();
        let executor = TaskExecutor::new(&config).await.unwrap();

        let task = TaskBuilder::new("Test task".to_string()).build();
        let task_id = executor.add_task(task).await;

        assert!(!task_id.is_empty());
    }

  ./crates/ccswarm/src/execution/task_executor.rs:511-522 method cancel_task <-> ./crates/ccswarm/src/execution/task_executor.rs:564-573 function test_complex_task_detection
  Similarity: 72.06%

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:cancel_task (lines 511-522) ---[0m
    pub async fn cancel_task(&self, task_id: &str, reason: Option<String>) -> anyhow::Result<()> {
        // Cancel in queue
        self.task_queue.cancel_task(task_id, reason).await?;

        // Cancel active execution if running
        let mut active = self.active_executions.write().await;
        if let Some(handle) = active.remove(task_id) {
            handle.abort();
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:test_complex_task_detection (lines 564-573) ---[0m
    async fn test_complex_task_detection() {
        let simple_task = TaskBuilder::new("Fix typo".to_string()).build();
        assert!(!TaskExecutor::is_complex_task(&simple_task));

        let complex_task = TaskBuilder::new(
            "Implement complete user authentication system with database integration".to_string(),
        )
        .build();
        assert!(TaskExecutor::is_complex_task(&complex_task));
    }

  ./crates/ccswarm/src/execution/task_executor.rs:553-561 function test_task_executor_creation <-> ./crates/ccswarm/src/execution/task_executor.rs:564-573 function test_complex_task_detection
  Similarity: 71.46%

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:test_task_executor_creation (lines 553-561) ---[0m
    async fn test_task_executor_creation() {
        let config = CcswarmConfig::default();
        let executor = TaskExecutor::new(&config).await.unwrap();

        let task = TaskBuilder::new("Test task".to_string()).build();
        let task_id = executor.add_task(task).await;

        assert!(!task_id.is_empty());
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:test_complex_task_detection (lines 564-573) ---[0m
    async fn test_complex_task_detection() {
        let simple_task = TaskBuilder::new("Fix typo".to_string()).build();
        assert!(!TaskExecutor::is_complex_task(&simple_task));

        let complex_task = TaskBuilder::new(
            "Implement complete user authentication system with database integration".to_string(),
        )
        .build();
        assert!(TaskExecutor::is_complex_task(&complex_task));
    }

  ./crates/ccswarm/src/execution/task_executor.rs:97-100 method add_task <-> ./crates/ccswarm/src/execution/task_executor.rs:532-535 method new
  Similarity: 78.50%
  Classes: TaskExecutor <-> ExecutionEngine

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:add_task (lines 97-100) ---[0m
    pub async fn add_task(&self, task: Task) -> String {
        info!("Adding task to execution queue: {}", task.description);
        self.task_queue.add_task(task).await
    }

[36m--- ./crates/ccswarm/src/execution/task_executor.rs:new (lines 532-535) ---[0m
    pub async fn new(config: &CcswarmConfig) -> anyhow::Result<Self> {
        let executor = Arc::new(TaskExecutor::new(config).await?);
        Ok(Self { executor })
    }


Duplicates in ./crates/ccswarm/src/utils/async_error_boundary_simple.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/utils/async_error_boundary_simple.rs:14-25 function run_safe <-> ./crates/ccswarm/src/utils/async_error_boundary_simple.rs:36-44 function boundary_with_fallback
  Similarity: 93.65%

[36m--- ./crates/ccswarm/src/utils/async_error_boundary_simple.rs:run_safe (lines 14-25) ---[0m
pub async fn run_safe<F, T>(f: F) -> Option<T>
where
    F: Future<Output = Result<T, CCSwarmError>>,
{
    match f.await {
        Ok(val) => Some(val),
        Err(err) => {
            eprintln!("Error in async boundary: {}", err);
            None
        }
    }
}

[36m--- ./crates/ccswarm/src/utils/async_error_boundary_simple.rs:boundary_with_fallback (lines 36-44) ---[0m
pub async fn boundary_with_fallback<F, T>(f: F, fallback: T) -> T
where
    F: Future<Output = Result<T, CCSwarmError>>,
{
    match f.await {
        Ok(val) => val,
        Err(_) => fallback,
    }
}


Duplicates in ./crates/ccswarm/src/agent/simple_orchestrator.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/agent/simple_orchestrator.rs:153-220 method create_quality_check_plan <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:223-279 method create_implementation_plan
  Similarity: 92.58%
  Classes: SimpleClaudeAgent <-> SimpleClaudeAgent

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_quality_check_plan (lines 153-220) ---[0m
    fn create_quality_check_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Initial Analysis
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Project Structure",
            vec![
                OrchestrationBuilder::parallel_task(
                    "check_cargo",
                    "Check Cargo.toml",
                    "cat Cargo.toml",
                    true,
                ),
                OrchestrationBuilder::parallel_task(
                    "check_src",
                    "Check source structure",
                    "ls -la src/",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "check_tests",
                    "Check test structure",
                    "ls -la tests/",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Run Quality Checks
        let mut quality_checks = TaskStep::new(
            "quality_checks".to_string(),
            "Run Quality Checks".to_string(),
            StepType::Validation,
        )
        .depends_on("analysis".to_string());

        quality_checks.add_parallel_task(OrchestrationBuilder::parallel_task(
            "run_tests",
            "Run tests",
            "cargo test",
            true,
        ));
        quality_checks.add_parallel_task(OrchestrationBuilder::parallel_task(
            "run_lint",
            "Run clippy",
            "cargo clippy -- -D warnings",
            true,
        ));
        quality_checks.add_parallel_task(OrchestrationBuilder::parallel_task(
            "check_fmt",
            "Check formatting",
            "cargo fmt -- --check",
            false,
        ));
        plan.add_step(quality_checks);

        // Step 3: Fix Issues (will be added adaptively if needed)

        // Step 4: Final Validation
        let validation =
            OrchestrationBuilder::validation_step("final_validation", "Final Validation")
                .depends_on("quality_checks".to_string());
        plan.add_step(validation);

        Ok(plan)
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_implementation_plan (lines 223-279) ---[0m
    fn create_implementation_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Analysis and Planning
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Requirements",
            vec![
                OrchestrationBuilder::parallel_task(
                    "analyze_existing",
                    "Analyze existing code",
                    "grep -r 'impl' src/",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "check_deps",
                    "Check dependencies",
                    "cargo tree",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Implementation
        let implementation = OrchestrationBuilder::execution_step(
            "implementation",
            "Implement Feature",
            vec!["analysis"],
        )
        .with_description(format!("Implement: {}", task.description));
        plan.add_step(implementation);

        // Step 3: Testing
        let mut testing = TaskStep::new(
            "testing".to_string(),
            "Test Implementation".to_string(),
            StepType::Validation,
        )
        .depends_on("implementation".to_string());

        testing.add_parallel_task(OrchestrationBuilder::parallel_task(
            "unit_tests",
            "Write unit tests",
            "cargo test",
            true,
        ));
        testing.add_parallel_task(OrchestrationBuilder::parallel_task(
            "integration_tests",
            "Run integration tests",
            "cargo test --test '*'",
            false,
        ));
        plan.add_step(testing);

        Ok(plan)
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:153-220 method create_quality_check_plan <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:282-338 method create_refactoring_plan
  Similarity: 92.58%
  Classes: SimpleClaudeAgent <-> SimpleClaudeAgent

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_quality_check_plan (lines 153-220) ---[0m
    fn create_quality_check_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Initial Analysis
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Project Structure",
            vec![
                OrchestrationBuilder::parallel_task(
                    "check_cargo",
                    "Check Cargo.toml",
                    "cat Cargo.toml",
                    true,
                ),
                OrchestrationBuilder::parallel_task(
                    "check_src",
                    "Check source structure",
                    "ls -la src/",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "check_tests",
                    "Check test structure",
                    "ls -la tests/",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Run Quality Checks
        let mut quality_checks = TaskStep::new(
            "quality_checks".to_string(),
            "Run Quality Checks".to_string(),
            StepType::Validation,
        )
        .depends_on("analysis".to_string());

        quality_checks.add_parallel_task(OrchestrationBuilder::parallel_task(
            "run_tests",
            "Run tests",
            "cargo test",
            true,
        ));
        quality_checks.add_parallel_task(OrchestrationBuilder::parallel_task(
            "run_lint",
            "Run clippy",
            "cargo clippy -- -D warnings",
            true,
        ));
        quality_checks.add_parallel_task(OrchestrationBuilder::parallel_task(
            "check_fmt",
            "Check formatting",
            "cargo fmt -- --check",
            false,
        ));
        plan.add_step(quality_checks);

        // Step 3: Fix Issues (will be added adaptively if needed)

        // Step 4: Final Validation
        let validation =
            OrchestrationBuilder::validation_step("final_validation", "Final Validation")
                .depends_on("quality_checks".to_string());
        plan.add_step(validation);

        Ok(plan)
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_refactoring_plan (lines 282-338) ---[0m
    fn create_refactoring_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Analysis
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Code Structure",
            vec![
                OrchestrationBuilder::parallel_task(
                    "complexity",
                    "Check complexity",
                    "cargo clippy -- -W clippy::cognitive_complexity",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "coverage",
                    "Check test coverage",
                    "cargo tarpaulin --print-summary",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Refactor
        let refactor = OrchestrationBuilder::execution_step(
            "refactor",
            "Perform Refactoring",
            vec!["analysis"],
        )
        .with_description("Refactor code while maintaining functionality".to_string());
        plan.add_step(refactor);

        // Step 3: Validate
        let mut validate = TaskStep::new(
            "validate".to_string(),
            "Validate Refactoring".to_string(),
            StepType::Validation,
        )
        .depends_on("refactor".to_string());

        validate.add_parallel_task(OrchestrationBuilder::parallel_task(
            "test_all",
            "Run all tests",
            "cargo test --all",
            true,
        ));
        validate.add_parallel_task(OrchestrationBuilder::parallel_task(
            "bench",
            "Run benchmarks",
            "cargo bench",
            false,
        ));
        plan.add_step(validate);

        Ok(plan)
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:223-279 method create_implementation_plan <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:282-338 method create_refactoring_plan
  Similarity: 96.87%
  Classes: SimpleClaudeAgent <-> SimpleClaudeAgent

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_implementation_plan (lines 223-279) ---[0m
    fn create_implementation_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Analysis and Planning
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Requirements",
            vec![
                OrchestrationBuilder::parallel_task(
                    "analyze_existing",
                    "Analyze existing code",
                    "grep -r 'impl' src/",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "check_deps",
                    "Check dependencies",
                    "cargo tree",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Implementation
        let implementation = OrchestrationBuilder::execution_step(
            "implementation",
            "Implement Feature",
            vec!["analysis"],
        )
        .with_description(format!("Implement: {}", task.description));
        plan.add_step(implementation);

        // Step 3: Testing
        let mut testing = TaskStep::new(
            "testing".to_string(),
            "Test Implementation".to_string(),
            StepType::Validation,
        )
        .depends_on("implementation".to_string());

        testing.add_parallel_task(OrchestrationBuilder::parallel_task(
            "unit_tests",
            "Write unit tests",
            "cargo test",
            true,
        ));
        testing.add_parallel_task(OrchestrationBuilder::parallel_task(
            "integration_tests",
            "Run integration tests",
            "cargo test --test '*'",
            false,
        ));
        plan.add_step(testing);

        Ok(plan)
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_refactoring_plan (lines 282-338) ---[0m
    fn create_refactoring_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Analysis
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Code Structure",
            vec![
                OrchestrationBuilder::parallel_task(
                    "complexity",
                    "Check complexity",
                    "cargo clippy -- -W clippy::cognitive_complexity",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "coverage",
                    "Check test coverage",
                    "cargo tarpaulin --print-summary",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Refactor
        let refactor = OrchestrationBuilder::execution_step(
            "refactor",
            "Perform Refactoring",
            vec!["analysis"],
        )
        .with_description("Refactor code while maintaining functionality".to_string());
        plan.add_step(refactor);

        // Step 3: Validate
        let mut validate = TaskStep::new(
            "validate".to_string(),
            "Validate Refactoring".to_string(),
            StepType::Validation,
        )
        .depends_on("refactor".to_string());

        validate.add_parallel_task(OrchestrationBuilder::parallel_task(
            "test_all",
            "Run all tests",
            "cargo test --all",
            true,
        ));
        validate.add_parallel_task(OrchestrationBuilder::parallel_task(
            "bench",
            "Run benchmarks",
            "cargo bench",
            false,
        ));
        plan.add_step(validate);

        Ok(plan)
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:74-108 method review_and_adapt <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:153-220 method create_quality_check_plan
  Similarity: 77.21%
  Classes: AgentOrchestrator <-> SimpleClaudeAgent

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:review_and_adapt (lines 74-108) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        // Analyze results to determine if adaptation is needed
        let last_result = results.last().unwrap();

        // If tests failed, add a fix step
        if last_result.outputs.contains_key("test_failures") {
            let fix_step = TaskStep::new(
                format!("fix_{}", results.len()),
                "Fix Test Failures".to_string(),
                StepType::Execution,
            )
            .with_description("Fix the test failures identified in previous step".to_string())
            .depends_on(last_result.step_id.clone());

            current_plan.insert_step_after(&last_result.step_id, fix_step);
        }

        // If linting errors found, add a fix step
        if last_result.outputs.contains_key("lint_errors") {
            let fix_step = TaskStep::new(
                format!("fix_lint_{}", results.len()),
                "Fix Linting Errors".to_string(),
                StepType::Execution,
            )
            .with_description("Fix the linting errors identified".to_string());

            current_plan.insert_step_after(&last_result.step_id, fix_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_quality_check_plan (lines 153-220) ---[0m
    fn create_quality_check_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Initial Analysis
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Project Structure",
            vec![
                OrchestrationBuilder::parallel_task(
                    "check_cargo",
                    "Check Cargo.toml",
                    "cat Cargo.toml",
                    true,
                ),
                OrchestrationBuilder::parallel_task(
                    "check_src",
                    "Check source structure",
                    "ls -la src/",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "check_tests",
                    "Check test structure",
                    "ls -la tests/",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Run Quality Checks
        let mut quality_checks = TaskStep::new(
            "quality_checks".to_string(),
            "Run Quality Checks".to_string(),
            StepType::Validation,
        )
        .depends_on("analysis".to_string());

        quality_checks.add_parallel_task(OrchestrationBuilder::parallel_task(
            "run_tests",
            "Run tests",
            "cargo test",
            true,
        ));
        quality_checks.add_parallel_task(OrchestrationBuilder::parallel_task(
            "run_lint",
            "Run clippy",
            "cargo clippy -- -D warnings",
            true,
        ));
        quality_checks.add_parallel_task(OrchestrationBuilder::parallel_task(
            "check_fmt",
            "Check formatting",
            "cargo fmt -- --check",
            false,
        ));
        plan.add_step(quality_checks);

        // Step 3: Fix Issues (will be added adaptively if needed)

        // Step 4: Final Validation
        let validation =
            OrchestrationBuilder::validation_step("final_validation", "Final Validation")
                .depends_on("quality_checks".to_string());
        plan.add_step(validation);

        Ok(plan)
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:110-148 method synthesize_results <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:153-220 method create_quality_check_plan
  Similarity: 73.70%
  Classes: AgentOrchestrator <-> SimpleClaudeAgent

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:synthesize_results (lines 110-148) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let all_success = results.iter().all(|r| r.is_success());

        let mut summary = format!("Task '{}' orchestration complete.\n", task.description);
        summary.push_str(&format!("Executed {} steps:\n", results.len()));

        for (i, result) in results.iter().enumerate() {
            summary.push_str(&format!(
                "  {}. {} - {} ({}ms)\n",
                i + 1,
                result.step_id,
                if result.success { "✓" } else { "✗" },
                result.duration_ms
            ));
        }

        let total_duration: u64 = results.iter().map(|r| r.duration_ms).sum();
        summary.push_str(&format!("\nTotal duration: {}ms", total_duration));

        if all_success {
            Ok(TaskResult::success(
                serde_json::json!({
                    "task_id": task.id.clone(),
                    "summary": summary,
                    "total_duration_ms": total_duration
                }),
                std::time::Duration::from_millis(total_duration),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                std::time::Duration::from_millis(total_duration),
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_quality_check_plan (lines 153-220) ---[0m
    fn create_quality_check_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Initial Analysis
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Project Structure",
            vec![
                OrchestrationBuilder::parallel_task(
                    "check_cargo",
                    "Check Cargo.toml",
                    "cat Cargo.toml",
                    true,
                ),
                OrchestrationBuilder::parallel_task(
                    "check_src",
                    "Check source structure",
                    "ls -la src/",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "check_tests",
                    "Check test structure",
                    "ls -la tests/",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Run Quality Checks
        let mut quality_checks = TaskStep::new(
            "quality_checks".to_string(),
            "Run Quality Checks".to_string(),
            StepType::Validation,
        )
        .depends_on("analysis".to_string());

        quality_checks.add_parallel_task(OrchestrationBuilder::parallel_task(
            "run_tests",
            "Run tests",
            "cargo test",
            true,
        ));
        quality_checks.add_parallel_task(OrchestrationBuilder::parallel_task(
            "run_lint",
            "Run clippy",
            "cargo clippy -- -D warnings",
            true,
        ));
        quality_checks.add_parallel_task(OrchestrationBuilder::parallel_task(
            "check_fmt",
            "Check formatting",
            "cargo fmt -- --check",
            false,
        ));
        plan.add_step(quality_checks);

        // Step 3: Fix Issues (will be added adaptively if needed)

        // Step 4: Final Validation
        let validation =
            OrchestrationBuilder::validation_step("final_validation", "Final Validation")
                .depends_on("quality_checks".to_string());
        plan.add_step(validation);

        Ok(plan)
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:110-148 method synthesize_results <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:223-279 method create_implementation_plan
  Similarity: 78.57%
  Classes: AgentOrchestrator <-> SimpleClaudeAgent

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:synthesize_results (lines 110-148) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let all_success = results.iter().all(|r| r.is_success());

        let mut summary = format!("Task '{}' orchestration complete.\n", task.description);
        summary.push_str(&format!("Executed {} steps:\n", results.len()));

        for (i, result) in results.iter().enumerate() {
            summary.push_str(&format!(
                "  {}. {} - {} ({}ms)\n",
                i + 1,
                result.step_id,
                if result.success { "✓" } else { "✗" },
                result.duration_ms
            ));
        }

        let total_duration: u64 = results.iter().map(|r| r.duration_ms).sum();
        summary.push_str(&format!("\nTotal duration: {}ms", total_duration));

        if all_success {
            Ok(TaskResult::success(
                serde_json::json!({
                    "task_id": task.id.clone(),
                    "summary": summary,
                    "total_duration_ms": total_duration
                }),
                std::time::Duration::from_millis(total_duration),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                std::time::Duration::from_millis(total_duration),
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_implementation_plan (lines 223-279) ---[0m
    fn create_implementation_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Analysis and Planning
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Requirements",
            vec![
                OrchestrationBuilder::parallel_task(
                    "analyze_existing",
                    "Analyze existing code",
                    "grep -r 'impl' src/",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "check_deps",
                    "Check dependencies",
                    "cargo tree",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Implementation
        let implementation = OrchestrationBuilder::execution_step(
            "implementation",
            "Implement Feature",
            vec!["analysis"],
        )
        .with_description(format!("Implement: {}", task.description));
        plan.add_step(implementation);

        // Step 3: Testing
        let mut testing = TaskStep::new(
            "testing".to_string(),
            "Test Implementation".to_string(),
            StepType::Validation,
        )
        .depends_on("implementation".to_string());

        testing.add_parallel_task(OrchestrationBuilder::parallel_task(
            "unit_tests",
            "Write unit tests",
            "cargo test",
            true,
        ));
        testing.add_parallel_task(OrchestrationBuilder::parallel_task(
            "integration_tests",
            "Run integration tests",
            "cargo test --test '*'",
            false,
        ));
        plan.add_step(testing);

        Ok(plan)
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:110-148 method synthesize_results <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:282-338 method create_refactoring_plan
  Similarity: 78.57%
  Classes: AgentOrchestrator <-> SimpleClaudeAgent

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:synthesize_results (lines 110-148) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let all_success = results.iter().all(|r| r.is_success());

        let mut summary = format!("Task '{}' orchestration complete.\n", task.description);
        summary.push_str(&format!("Executed {} steps:\n", results.len()));

        for (i, result) in results.iter().enumerate() {
            summary.push_str(&format!(
                "  {}. {} - {} ({}ms)\n",
                i + 1,
                result.step_id,
                if result.success { "✓" } else { "✗" },
                result.duration_ms
            ));
        }

        let total_duration: u64 = results.iter().map(|r| r.duration_ms).sum();
        summary.push_str(&format!("\nTotal duration: {}ms", total_duration));

        if all_success {
            Ok(TaskResult::success(
                serde_json::json!({
                    "task_id": task.id.clone(),
                    "summary": summary,
                    "total_duration_ms": total_duration
                }),
                std::time::Duration::from_millis(total_duration),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                std::time::Duration::from_millis(total_duration),
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_refactoring_plan (lines 282-338) ---[0m
    fn create_refactoring_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Analysis
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Code Structure",
            vec![
                OrchestrationBuilder::parallel_task(
                    "complexity",
                    "Check complexity",
                    "cargo clippy -- -W clippy::cognitive_complexity",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "coverage",
                    "Check test coverage",
                    "cargo tarpaulin --print-summary",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Refactor
        let refactor = OrchestrationBuilder::execution_step(
            "refactor",
            "Perform Refactoring",
            vec!["analysis"],
        )
        .with_description("Refactor code while maintaining functionality".to_string());
        plan.add_step(refactor);

        // Step 3: Validate
        let mut validate = TaskStep::new(
            "validate".to_string(),
            "Validate Refactoring".to_string(),
            StepType::Validation,
        )
        .depends_on("refactor".to_string());

        validate.add_parallel_task(OrchestrationBuilder::parallel_task(
            "test_all",
            "Run all tests",
            "cargo test --all",
            true,
        ));
        validate.add_parallel_task(OrchestrationBuilder::parallel_task(
            "bench",
            "Run benchmarks",
            "cargo bench",
            false,
        ));
        plan.add_step(validate);

        Ok(plan)
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:74-108 method review_and_adapt <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:223-279 method create_implementation_plan
  Similarity: 79.76%
  Classes: AgentOrchestrator <-> SimpleClaudeAgent

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:review_and_adapt (lines 74-108) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        // Analyze results to determine if adaptation is needed
        let last_result = results.last().unwrap();

        // If tests failed, add a fix step
        if last_result.outputs.contains_key("test_failures") {
            let fix_step = TaskStep::new(
                format!("fix_{}", results.len()),
                "Fix Test Failures".to_string(),
                StepType::Execution,
            )
            .with_description("Fix the test failures identified in previous step".to_string())
            .depends_on(last_result.step_id.clone());

            current_plan.insert_step_after(&last_result.step_id, fix_step);
        }

        // If linting errors found, add a fix step
        if last_result.outputs.contains_key("lint_errors") {
            let fix_step = TaskStep::new(
                format!("fix_lint_{}", results.len()),
                "Fix Linting Errors".to_string(),
                StepType::Execution,
            )
            .with_description("Fix the linting errors identified".to_string());

            current_plan.insert_step_after(&last_result.step_id, fix_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_implementation_plan (lines 223-279) ---[0m
    fn create_implementation_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Analysis and Planning
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Requirements",
            vec![
                OrchestrationBuilder::parallel_task(
                    "analyze_existing",
                    "Analyze existing code",
                    "grep -r 'impl' src/",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "check_deps",
                    "Check dependencies",
                    "cargo tree",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Implementation
        let implementation = OrchestrationBuilder::execution_step(
            "implementation",
            "Implement Feature",
            vec!["analysis"],
        )
        .with_description(format!("Implement: {}", task.description));
        plan.add_step(implementation);

        // Step 3: Testing
        let mut testing = TaskStep::new(
            "testing".to_string(),
            "Test Implementation".to_string(),
            StepType::Validation,
        )
        .depends_on("implementation".to_string());

        testing.add_parallel_task(OrchestrationBuilder::parallel_task(
            "unit_tests",
            "Write unit tests",
            "cargo test",
            true,
        ));
        testing.add_parallel_task(OrchestrationBuilder::parallel_task(
            "integration_tests",
            "Run integration tests",
            "cargo test --test '*'",
            false,
        ));
        plan.add_step(testing);

        Ok(plan)
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:74-108 method review_and_adapt <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:282-338 method create_refactoring_plan
  Similarity: 79.58%
  Classes: AgentOrchestrator <-> SimpleClaudeAgent

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:review_and_adapt (lines 74-108) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        // Analyze results to determine if adaptation is needed
        let last_result = results.last().unwrap();

        // If tests failed, add a fix step
        if last_result.outputs.contains_key("test_failures") {
            let fix_step = TaskStep::new(
                format!("fix_{}", results.len()),
                "Fix Test Failures".to_string(),
                StepType::Execution,
            )
            .with_description("Fix the test failures identified in previous step".to_string())
            .depends_on(last_result.step_id.clone());

            current_plan.insert_step_after(&last_result.step_id, fix_step);
        }

        // If linting errors found, add a fix step
        if last_result.outputs.contains_key("lint_errors") {
            let fix_step = TaskStep::new(
                format!("fix_lint_{}", results.len()),
                "Fix Linting Errors".to_string(),
                StepType::Execution,
            )
            .with_description("Fix the linting errors identified".to_string());

            current_plan.insert_step_after(&last_result.step_id, fix_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_refactoring_plan (lines 282-338) ---[0m
    fn create_refactoring_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Analysis
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Code Structure",
            vec![
                OrchestrationBuilder::parallel_task(
                    "complexity",
                    "Check complexity",
                    "cargo clippy -- -W clippy::cognitive_complexity",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "coverage",
                    "Check test coverage",
                    "cargo tarpaulin --print-summary",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Refactor
        let refactor = OrchestrationBuilder::execution_step(
            "refactor",
            "Perform Refactoring",
            vec!["analysis"],
        )
        .with_description("Refactor code while maintaining functionality".to_string());
        plan.add_step(refactor);

        // Step 3: Validate
        let mut validate = TaskStep::new(
            "validate".to_string(),
            "Validate Refactoring".to_string(),
            StepType::Validation,
        )
        .depends_on("refactor".to_string());

        validate.add_parallel_task(OrchestrationBuilder::parallel_task(
            "test_all",
            "Run all tests",
            "cargo test --all",
            true,
        ));
        validate.add_parallel_task(OrchestrationBuilder::parallel_task(
            "bench",
            "Run benchmarks",
            "cargo bench",
            false,
        ));
        plan.add_step(validate);

        Ok(plan)
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:153-220 method create_quality_check_plan <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:366-385 function test_simple_agent_orchestration
  Similarity: 82.79%

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_quality_check_plan (lines 153-220) ---[0m
    fn create_quality_check_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Initial Analysis
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Project Structure",
            vec![
                OrchestrationBuilder::parallel_task(
                    "check_cargo",
                    "Check Cargo.toml",
                    "cat Cargo.toml",
                    true,
                ),
                OrchestrationBuilder::parallel_task(
                    "check_src",
                    "Check source structure",
                    "ls -la src/",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "check_tests",
                    "Check test structure",
                    "ls -la tests/",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Run Quality Checks
        let mut quality_checks = TaskStep::new(
            "quality_checks".to_string(),
            "Run Quality Checks".to_string(),
            StepType::Validation,
        )
        .depends_on("analysis".to_string());

        quality_checks.add_parallel_task(OrchestrationBuilder::parallel_task(
            "run_tests",
            "Run tests",
            "cargo test",
            true,
        ));
        quality_checks.add_parallel_task(OrchestrationBuilder::parallel_task(
            "run_lint",
            "Run clippy",
            "cargo clippy -- -D warnings",
            true,
        ));
        quality_checks.add_parallel_task(OrchestrationBuilder::parallel_task(
            "check_fmt",
            "Check formatting",
            "cargo fmt -- --check",
            false,
        ));
        plan.add_step(quality_checks);

        // Step 3: Fix Issues (will be added adaptively if needed)

        // Step 4: Final Validation
        let validation =
            OrchestrationBuilder::validation_step("final_validation", "Final Validation")
                .depends_on("quality_checks".to_string());
        plan.add_step(validation);

        Ok(plan)
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:test_simple_agent_orchestration (lines 366-385) ---[0m
    async fn test_simple_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::default();

        let agent = SimpleClaudeAgent::new(
            crate::identity::default_backend_role(),
            temp_dir.path(),
            config,
        )
        .await
        .unwrap();

        // Test quality check plan
        let task = TaskBuilder::new("Run tests and linting".to_string()).build();
        let plan = agent.analyze_task(&task).await.unwrap();

        assert!(plan.steps.len() >= 3);
        assert_eq!(plan.steps[0].name, "Analyze Project Structure");
        assert_eq!(plan.steps[1].name, "Run Quality Checks");
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:153-220 method create_quality_check_plan <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:388-406 function test_implementation_plan
  Similarity: 79.10%

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_quality_check_plan (lines 153-220) ---[0m
    fn create_quality_check_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Initial Analysis
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Project Structure",
            vec![
                OrchestrationBuilder::parallel_task(
                    "check_cargo",
                    "Check Cargo.toml",
                    "cat Cargo.toml",
                    true,
                ),
                OrchestrationBuilder::parallel_task(
                    "check_src",
                    "Check source structure",
                    "ls -la src/",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "check_tests",
                    "Check test structure",
                    "ls -la tests/",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Run Quality Checks
        let mut quality_checks = TaskStep::new(
            "quality_checks".to_string(),
            "Run Quality Checks".to_string(),
            StepType::Validation,
        )
        .depends_on("analysis".to_string());

        quality_checks.add_parallel_task(OrchestrationBuilder::parallel_task(
            "run_tests",
            "Run tests",
            "cargo test",
            true,
        ));
        quality_checks.add_parallel_task(OrchestrationBuilder::parallel_task(
            "run_lint",
            "Run clippy",
            "cargo clippy -- -D warnings",
            true,
        ));
        quality_checks.add_parallel_task(OrchestrationBuilder::parallel_task(
            "check_fmt",
            "Check formatting",
            "cargo fmt -- --check",
            false,
        ));
        plan.add_step(quality_checks);

        // Step 3: Fix Issues (will be added adaptively if needed)

        // Step 4: Final Validation
        let validation =
            OrchestrationBuilder::validation_step("final_validation", "Final Validation")
                .depends_on("quality_checks".to_string());
        plan.add_step(validation);

        Ok(plan)
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:test_implementation_plan (lines 388-406) ---[0m
    async fn test_implementation_plan() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::default();

        let agent = SimpleClaudeAgent::new(
            crate::identity::default_frontend_role(),
            temp_dir.path(),
            config,
        )
        .await
        .unwrap();

        let task = TaskBuilder::new("Implement new dashboard component".to_string()).build();
        let plan = agent.analyze_task(&task).await.unwrap();

        assert!(plan.steps.len() >= 3);
        assert!(plan.steps.iter().any(|s| s.name.contains("Implement")));
        assert!(plan.steps.iter().any(|s| s.name.contains("Test")));
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:74-108 method review_and_adapt <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:110-148 method synthesize_results
  Similarity: 87.38%
  Classes: AgentOrchestrator <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:review_and_adapt (lines 74-108) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        // Analyze results to determine if adaptation is needed
        let last_result = results.last().unwrap();

        // If tests failed, add a fix step
        if last_result.outputs.contains_key("test_failures") {
            let fix_step = TaskStep::new(
                format!("fix_{}", results.len()),
                "Fix Test Failures".to_string(),
                StepType::Execution,
            )
            .with_description("Fix the test failures identified in previous step".to_string())
            .depends_on(last_result.step_id.clone());

            current_plan.insert_step_after(&last_result.step_id, fix_step);
        }

        // If linting errors found, add a fix step
        if last_result.outputs.contains_key("lint_errors") {
            let fix_step = TaskStep::new(
                format!("fix_lint_{}", results.len()),
                "Fix Linting Errors".to_string(),
                StepType::Execution,
            )
            .with_description("Fix the linting errors identified".to_string());

            current_plan.insert_step_after(&last_result.step_id, fix_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:synthesize_results (lines 110-148) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let all_success = results.iter().all(|r| r.is_success());

        let mut summary = format!("Task '{}' orchestration complete.\n", task.description);
        summary.push_str(&format!("Executed {} steps:\n", results.len()));

        for (i, result) in results.iter().enumerate() {
            summary.push_str(&format!(
                "  {}. {} - {} ({}ms)\n",
                i + 1,
                result.step_id,
                if result.success { "✓" } else { "✗" },
                result.duration_ms
            ));
        }

        let total_duration: u64 = results.iter().map(|r| r.duration_ms).sum();
        summary.push_str(&format!("\nTotal duration: {}ms", total_duration));

        if all_success {
            Ok(TaskResult::success(
                serde_json::json!({
                    "task_id": task.id.clone(),
                    "summary": summary,
                    "total_duration_ms": total_duration
                }),
                std::time::Duration::from_millis(total_duration),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                std::time::Duration::from_millis(total_duration),
            ))
        }
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:19-48 method analyze_task <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:223-279 method create_implementation_plan
  Similarity: 73.63%
  Classes: AgentOrchestrator <-> SimpleClaudeAgent

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:analyze_task (lines 19-48) ---[0m
    async fn analyze_task(&self, task: &Task) -> Result<TaskPlan> {
        info!("Analyzing task for orchestration: {}", task.description);

        let plan;

        // Determine task complexity and create appropriate steps
        let task_lower = task.description.to_lowercase();

        if task_lower.contains("test")
            || task_lower.contains("lint")
            || task_lower.contains("build")
        {
            // Quality check workflow
            plan = self.create_quality_check_plan(task)?;
        } else if task_lower.contains("implement")
            || task_lower.contains("create")
            || task_lower.contains("add")
        {
            // Implementation workflow
            plan = self.create_implementation_plan(task)?;
        } else if task_lower.contains("refactor") || task_lower.contains("optimize") {
            // Refactoring workflow
            plan = self.create_refactoring_plan(task)?;
        } else {
            // Default simple workflow
            plan = self.create_default_plan(task)?;
        }

        Ok(plan)
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_implementation_plan (lines 223-279) ---[0m
    fn create_implementation_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Analysis and Planning
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Requirements",
            vec![
                OrchestrationBuilder::parallel_task(
                    "analyze_existing",
                    "Analyze existing code",
                    "grep -r 'impl' src/",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "check_deps",
                    "Check dependencies",
                    "cargo tree",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Implementation
        let implementation = OrchestrationBuilder::execution_step(
            "implementation",
            "Implement Feature",
            vec!["analysis"],
        )
        .with_description(format!("Implement: {}", task.description));
        plan.add_step(implementation);

        // Step 3: Testing
        let mut testing = TaskStep::new(
            "testing".to_string(),
            "Test Implementation".to_string(),
            StepType::Validation,
        )
        .depends_on("implementation".to_string());

        testing.add_parallel_task(OrchestrationBuilder::parallel_task(
            "unit_tests",
            "Write unit tests",
            "cargo test",
            true,
        ));
        testing.add_parallel_task(OrchestrationBuilder::parallel_task(
            "integration_tests",
            "Run integration tests",
            "cargo test --test '*'",
            false,
        ));
        plan.add_step(testing);

        Ok(plan)
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:19-48 method analyze_task <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:282-338 method create_refactoring_plan
  Similarity: 73.39%
  Classes: AgentOrchestrator <-> SimpleClaudeAgent

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:analyze_task (lines 19-48) ---[0m
    async fn analyze_task(&self, task: &Task) -> Result<TaskPlan> {
        info!("Analyzing task for orchestration: {}", task.description);

        let plan;

        // Determine task complexity and create appropriate steps
        let task_lower = task.description.to_lowercase();

        if task_lower.contains("test")
            || task_lower.contains("lint")
            || task_lower.contains("build")
        {
            // Quality check workflow
            plan = self.create_quality_check_plan(task)?;
        } else if task_lower.contains("implement")
            || task_lower.contains("create")
            || task_lower.contains("add")
        {
            // Implementation workflow
            plan = self.create_implementation_plan(task)?;
        } else if task_lower.contains("refactor") || task_lower.contains("optimize") {
            // Refactoring workflow
            plan = self.create_refactoring_plan(task)?;
        } else {
            // Default simple workflow
            plan = self.create_default_plan(task)?;
        }

        Ok(plan)
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_refactoring_plan (lines 282-338) ---[0m
    fn create_refactoring_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Analysis
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Code Structure",
            vec![
                OrchestrationBuilder::parallel_task(
                    "complexity",
                    "Check complexity",
                    "cargo clippy -- -W clippy::cognitive_complexity",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "coverage",
                    "Check test coverage",
                    "cargo tarpaulin --print-summary",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Refactor
        let refactor = OrchestrationBuilder::execution_step(
            "refactor",
            "Perform Refactoring",
            vec!["analysis"],
        )
        .with_description("Refactor code while maintaining functionality".to_string());
        plan.add_step(refactor);

        // Step 3: Validate
        let mut validate = TaskStep::new(
            "validate".to_string(),
            "Validate Refactoring".to_string(),
            StepType::Validation,
        )
        .depends_on("refactor".to_string());

        validate.add_parallel_task(OrchestrationBuilder::parallel_task(
            "test_all",
            "Run all tests",
            "cargo test --all",
            true,
        ));
        validate.add_parallel_task(OrchestrationBuilder::parallel_task(
            "bench",
            "Run benchmarks",
            "cargo bench",
            false,
        ));
        plan.add_step(validate);

        Ok(plan)
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:223-279 method create_implementation_plan <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:366-385 function test_simple_agent_orchestration
  Similarity: 82.33%

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_implementation_plan (lines 223-279) ---[0m
    fn create_implementation_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Analysis and Planning
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Requirements",
            vec![
                OrchestrationBuilder::parallel_task(
                    "analyze_existing",
                    "Analyze existing code",
                    "grep -r 'impl' src/",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "check_deps",
                    "Check dependencies",
                    "cargo tree",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Implementation
        let implementation = OrchestrationBuilder::execution_step(
            "implementation",
            "Implement Feature",
            vec!["analysis"],
        )
        .with_description(format!("Implement: {}", task.description));
        plan.add_step(implementation);

        // Step 3: Testing
        let mut testing = TaskStep::new(
            "testing".to_string(),
            "Test Implementation".to_string(),
            StepType::Validation,
        )
        .depends_on("implementation".to_string());

        testing.add_parallel_task(OrchestrationBuilder::parallel_task(
            "unit_tests",
            "Write unit tests",
            "cargo test",
            true,
        ));
        testing.add_parallel_task(OrchestrationBuilder::parallel_task(
            "integration_tests",
            "Run integration tests",
            "cargo test --test '*'",
            false,
        ));
        plan.add_step(testing);

        Ok(plan)
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:test_simple_agent_orchestration (lines 366-385) ---[0m
    async fn test_simple_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::default();

        let agent = SimpleClaudeAgent::new(
            crate::identity::default_backend_role(),
            temp_dir.path(),
            config,
        )
        .await
        .unwrap();

        // Test quality check plan
        let task = TaskBuilder::new("Run tests and linting".to_string()).build();
        let plan = agent.analyze_task(&task).await.unwrap();

        assert!(plan.steps.len() >= 3);
        assert_eq!(plan.steps[0].name, "Analyze Project Structure");
        assert_eq!(plan.steps[1].name, "Run Quality Checks");
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:282-338 method create_refactoring_plan <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:366-385 function test_simple_agent_orchestration
  Similarity: 82.17%

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_refactoring_plan (lines 282-338) ---[0m
    fn create_refactoring_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Analysis
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Code Structure",
            vec![
                OrchestrationBuilder::parallel_task(
                    "complexity",
                    "Check complexity",
                    "cargo clippy -- -W clippy::cognitive_complexity",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "coverage",
                    "Check test coverage",
                    "cargo tarpaulin --print-summary",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Refactor
        let refactor = OrchestrationBuilder::execution_step(
            "refactor",
            "Perform Refactoring",
            vec!["analysis"],
        )
        .with_description("Refactor code while maintaining functionality".to_string());
        plan.add_step(refactor);

        // Step 3: Validate
        let mut validate = TaskStep::new(
            "validate".to_string(),
            "Validate Refactoring".to_string(),
            StepType::Validation,
        )
        .depends_on("refactor".to_string());

        validate.add_parallel_task(OrchestrationBuilder::parallel_task(
            "test_all",
            "Run all tests",
            "cargo test --all",
            true,
        ));
        validate.add_parallel_task(OrchestrationBuilder::parallel_task(
            "bench",
            "Run benchmarks",
            "cargo bench",
            false,
        ));
        plan.add_step(validate);

        Ok(plan)
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:test_simple_agent_orchestration (lines 366-385) ---[0m
    async fn test_simple_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::default();

        let agent = SimpleClaudeAgent::new(
            crate::identity::default_backend_role(),
            temp_dir.path(),
            config,
        )
        .await
        .unwrap();

        // Test quality check plan
        let task = TaskBuilder::new("Run tests and linting".to_string()).build();
        let plan = agent.analyze_task(&task).await.unwrap();

        assert!(plan.steps.len() >= 3);
        assert_eq!(plan.steps[0].name, "Analyze Project Structure");
        assert_eq!(plan.steps[1].name, "Run Quality Checks");
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:223-279 method create_implementation_plan <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:388-406 function test_implementation_plan
  Similarity: 79.68%

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_implementation_plan (lines 223-279) ---[0m
    fn create_implementation_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Analysis and Planning
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Requirements",
            vec![
                OrchestrationBuilder::parallel_task(
                    "analyze_existing",
                    "Analyze existing code",
                    "grep -r 'impl' src/",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "check_deps",
                    "Check dependencies",
                    "cargo tree",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Implementation
        let implementation = OrchestrationBuilder::execution_step(
            "implementation",
            "Implement Feature",
            vec!["analysis"],
        )
        .with_description(format!("Implement: {}", task.description));
        plan.add_step(implementation);

        // Step 3: Testing
        let mut testing = TaskStep::new(
            "testing".to_string(),
            "Test Implementation".to_string(),
            StepType::Validation,
        )
        .depends_on("implementation".to_string());

        testing.add_parallel_task(OrchestrationBuilder::parallel_task(
            "unit_tests",
            "Write unit tests",
            "cargo test",
            true,
        ));
        testing.add_parallel_task(OrchestrationBuilder::parallel_task(
            "integration_tests",
            "Run integration tests",
            "cargo test --test '*'",
            false,
        ));
        plan.add_step(testing);

        Ok(plan)
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:test_implementation_plan (lines 388-406) ---[0m
    async fn test_implementation_plan() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::default();

        let agent = SimpleClaudeAgent::new(
            crate::identity::default_frontend_role(),
            temp_dir.path(),
            config,
        )
        .await
        .unwrap();

        let task = TaskBuilder::new("Implement new dashboard component".to_string()).build();
        let plan = agent.analyze_task(&task).await.unwrap();

        assert!(plan.steps.len() >= 3);
        assert!(plan.steps.iter().any(|s| s.name.contains("Implement")));
        assert!(plan.steps.iter().any(|s| s.name.contains("Test")));
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:282-338 method create_refactoring_plan <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:388-406 function test_implementation_plan
  Similarity: 79.49%

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_refactoring_plan (lines 282-338) ---[0m
    fn create_refactoring_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Step 1: Analysis
        let analysis = OrchestrationBuilder::analysis_step(
            "analysis",
            "Analyze Code Structure",
            vec![
                OrchestrationBuilder::parallel_task(
                    "complexity",
                    "Check complexity",
                    "cargo clippy -- -W clippy::cognitive_complexity",
                    false,
                ),
                OrchestrationBuilder::parallel_task(
                    "coverage",
                    "Check test coverage",
                    "cargo tarpaulin --print-summary",
                    false,
                ),
            ],
        );
        plan.add_step(analysis);

        // Step 2: Refactor
        let refactor = OrchestrationBuilder::execution_step(
            "refactor",
            "Perform Refactoring",
            vec!["analysis"],
        )
        .with_description("Refactor code while maintaining functionality".to_string());
        plan.add_step(refactor);

        // Step 3: Validate
        let mut validate = TaskStep::new(
            "validate".to_string(),
            "Validate Refactoring".to_string(),
            StepType::Validation,
        )
        .depends_on("refactor".to_string());

        validate.add_parallel_task(OrchestrationBuilder::parallel_task(
            "test_all",
            "Run all tests",
            "cargo test --all",
            true,
        ));
        validate.add_parallel_task(OrchestrationBuilder::parallel_task(
            "bench",
            "Run benchmarks",
            "cargo bench",
            false,
        ));
        plan.add_step(validate);

        Ok(plan)
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:test_implementation_plan (lines 388-406) ---[0m
    async fn test_implementation_plan() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::default();

        let agent = SimpleClaudeAgent::new(
            crate::identity::default_frontend_role(),
            temp_dir.path(),
            config,
        )
        .await
        .unwrap();

        let task = TaskBuilder::new("Implement new dashboard component".to_string()).build();
        let plan = agent.analyze_task(&task).await.unwrap();

        assert!(plan.steps.len() >= 3);
        assert!(plan.steps.iter().any(|s| s.name.contains("Implement")));
        assert!(plan.steps.iter().any(|s| s.name.contains("Test")));
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:19-48 method analyze_task <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:74-108 method review_and_adapt
  Similarity: 86.79%
  Classes: AgentOrchestrator <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:analyze_task (lines 19-48) ---[0m
    async fn analyze_task(&self, task: &Task) -> Result<TaskPlan> {
        info!("Analyzing task for orchestration: {}", task.description);

        let plan;

        // Determine task complexity and create appropriate steps
        let task_lower = task.description.to_lowercase();

        if task_lower.contains("test")
            || task_lower.contains("lint")
            || task_lower.contains("build")
        {
            // Quality check workflow
            plan = self.create_quality_check_plan(task)?;
        } else if task_lower.contains("implement")
            || task_lower.contains("create")
            || task_lower.contains("add")
        {
            // Implementation workflow
            plan = self.create_implementation_plan(task)?;
        } else if task_lower.contains("refactor") || task_lower.contains("optimize") {
            // Refactoring workflow
            plan = self.create_refactoring_plan(task)?;
        } else {
            // Default simple workflow
            plan = self.create_default_plan(task)?;
        }

        Ok(plan)
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:review_and_adapt (lines 74-108) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        // Analyze results to determine if adaptation is needed
        let last_result = results.last().unwrap();

        // If tests failed, add a fix step
        if last_result.outputs.contains_key("test_failures") {
            let fix_step = TaskStep::new(
                format!("fix_{}", results.len()),
                "Fix Test Failures".to_string(),
                StepType::Execution,
            )
            .with_description("Fix the test failures identified in previous step".to_string())
            .depends_on(last_result.step_id.clone());

            current_plan.insert_step_after(&last_result.step_id, fix_step);
        }

        // If linting errors found, add a fix step
        if last_result.outputs.contains_key("lint_errors") {
            let fix_step = TaskStep::new(
                format!("fix_lint_{}", results.len()),
                "Fix Linting Errors".to_string(),
                StepType::Execution,
            )
            .with_description("Fix the linting errors identified".to_string());

            current_plan.insert_step_after(&last_result.step_id, fix_step);
        }

        Ok(current_plan.clone())
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:19-48 method analyze_task <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:110-148 method synthesize_results
  Similarity: 81.52%
  Classes: AgentOrchestrator <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:analyze_task (lines 19-48) ---[0m
    async fn analyze_task(&self, task: &Task) -> Result<TaskPlan> {
        info!("Analyzing task for orchestration: {}", task.description);

        let plan;

        // Determine task complexity and create appropriate steps
        let task_lower = task.description.to_lowercase();

        if task_lower.contains("test")
            || task_lower.contains("lint")
            || task_lower.contains("build")
        {
            // Quality check workflow
            plan = self.create_quality_check_plan(task)?;
        } else if task_lower.contains("implement")
            || task_lower.contains("create")
            || task_lower.contains("add")
        {
            // Implementation workflow
            plan = self.create_implementation_plan(task)?;
        } else if task_lower.contains("refactor") || task_lower.contains("optimize") {
            // Refactoring workflow
            plan = self.create_refactoring_plan(task)?;
        } else {
            // Default simple workflow
            plan = self.create_default_plan(task)?;
        }

        Ok(plan)
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:synthesize_results (lines 110-148) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let all_success = results.iter().all(|r| r.is_success());

        let mut summary = format!("Task '{}' orchestration complete.\n", task.description);
        summary.push_str(&format!("Executed {} steps:\n", results.len()));

        for (i, result) in results.iter().enumerate() {
            summary.push_str(&format!(
                "  {}. {} - {} ({}ms)\n",
                i + 1,
                result.step_id,
                if result.success { "✓" } else { "✗" },
                result.duration_ms
            ));
        }

        let total_duration: u64 = results.iter().map(|r| r.duration_ms).sum();
        summary.push_str(&format!("\nTotal duration: {}ms", total_duration));

        if all_success {
            Ok(TaskResult::success(
                serde_json::json!({
                    "task_id": task.id.clone(),
                    "summary": summary,
                    "total_duration_ms": total_duration
                }),
                std::time::Duration::from_millis(total_duration),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                std::time::Duration::from_millis(total_duration),
            ))
        }
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:110-148 method synthesize_results <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:366-385 function test_simple_agent_orchestration
  Similarity: 77.06%

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:synthesize_results (lines 110-148) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let all_success = results.iter().all(|r| r.is_success());

        let mut summary = format!("Task '{}' orchestration complete.\n", task.description);
        summary.push_str(&format!("Executed {} steps:\n", results.len()));

        for (i, result) in results.iter().enumerate() {
            summary.push_str(&format!(
                "  {}. {} - {} ({}ms)\n",
                i + 1,
                result.step_id,
                if result.success { "✓" } else { "✗" },
                result.duration_ms
            ));
        }

        let total_duration: u64 = results.iter().map(|r| r.duration_ms).sum();
        summary.push_str(&format!("\nTotal duration: {}ms", total_duration));

        if all_success {
            Ok(TaskResult::success(
                serde_json::json!({
                    "task_id": task.id.clone(),
                    "summary": summary,
                    "total_duration_ms": total_duration
                }),
                std::time::Duration::from_millis(total_duration),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                std::time::Duration::from_millis(total_duration),
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:test_simple_agent_orchestration (lines 366-385) ---[0m
    async fn test_simple_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::default();

        let agent = SimpleClaudeAgent::new(
            crate::identity::default_backend_role(),
            temp_dir.path(),
            config,
        )
        .await
        .unwrap();

        // Test quality check plan
        let task = TaskBuilder::new("Run tests and linting".to_string()).build();
        let plan = agent.analyze_task(&task).await.unwrap();

        assert!(plan.steps.len() >= 3);
        assert_eq!(plan.steps[0].name, "Analyze Project Structure");
        assert_eq!(plan.steps[1].name, "Run Quality Checks");
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:110-148 method synthesize_results <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:388-406 function test_implementation_plan
  Similarity: 75.57%

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:synthesize_results (lines 110-148) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let all_success = results.iter().all(|r| r.is_success());

        let mut summary = format!("Task '{}' orchestration complete.\n", task.description);
        summary.push_str(&format!("Executed {} steps:\n", results.len()));

        for (i, result) in results.iter().enumerate() {
            summary.push_str(&format!(
                "  {}. {} - {} ({}ms)\n",
                i + 1,
                result.step_id,
                if result.success { "✓" } else { "✗" },
                result.duration_ms
            ));
        }

        let total_duration: u64 = results.iter().map(|r| r.duration_ms).sum();
        summary.push_str(&format!("\nTotal duration: {}ms", total_duration));

        if all_success {
            Ok(TaskResult::success(
                serde_json::json!({
                    "task_id": task.id.clone(),
                    "summary": summary,
                    "total_duration_ms": total_duration
                }),
                std::time::Duration::from_millis(total_duration),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                std::time::Duration::from_millis(total_duration),
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:test_implementation_plan (lines 388-406) ---[0m
    async fn test_implementation_plan() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::default();

        let agent = SimpleClaudeAgent::new(
            crate::identity::default_frontend_role(),
            temp_dir.path(),
            config,
        )
        .await
        .unwrap();

        let task = TaskBuilder::new("Implement new dashboard component".to_string()).build();
        let plan = agent.analyze_task(&task).await.unwrap();

        assert!(plan.steps.len() >= 3);
        assert!(plan.steps.iter().any(|s| s.name.contains("Implement")));
        assert!(plan.steps.iter().any(|s| s.name.contains("Test")));
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:74-108 method review_and_adapt <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:366-385 function test_simple_agent_orchestration
  Similarity: 79.37%

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:review_and_adapt (lines 74-108) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        // Analyze results to determine if adaptation is needed
        let last_result = results.last().unwrap();

        // If tests failed, add a fix step
        if last_result.outputs.contains_key("test_failures") {
            let fix_step = TaskStep::new(
                format!("fix_{}", results.len()),
                "Fix Test Failures".to_string(),
                StepType::Execution,
            )
            .with_description("Fix the test failures identified in previous step".to_string())
            .depends_on(last_result.step_id.clone());

            current_plan.insert_step_after(&last_result.step_id, fix_step);
        }

        // If linting errors found, add a fix step
        if last_result.outputs.contains_key("lint_errors") {
            let fix_step = TaskStep::new(
                format!("fix_lint_{}", results.len()),
                "Fix Linting Errors".to_string(),
                StepType::Execution,
            )
            .with_description("Fix the linting errors identified".to_string());

            current_plan.insert_step_after(&last_result.step_id, fix_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:test_simple_agent_orchestration (lines 366-385) ---[0m
    async fn test_simple_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::default();

        let agent = SimpleClaudeAgent::new(
            crate::identity::default_backend_role(),
            temp_dir.path(),
            config,
        )
        .await
        .unwrap();

        // Test quality check plan
        let task = TaskBuilder::new("Run tests and linting".to_string()).build();
        let plan = agent.analyze_task(&task).await.unwrap();

        assert!(plan.steps.len() >= 3);
        assert_eq!(plan.steps[0].name, "Analyze Project Structure");
        assert_eq!(plan.steps[1].name, "Run Quality Checks");
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:74-108 method review_and_adapt <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:388-406 function test_implementation_plan
  Similarity: 79.78%

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:review_and_adapt (lines 74-108) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        // Analyze results to determine if adaptation is needed
        let last_result = results.last().unwrap();

        // If tests failed, add a fix step
        if last_result.outputs.contains_key("test_failures") {
            let fix_step = TaskStep::new(
                format!("fix_{}", results.len()),
                "Fix Test Failures".to_string(),
                StepType::Execution,
            )
            .with_description("Fix the test failures identified in previous step".to_string())
            .depends_on(last_result.step_id.clone());

            current_plan.insert_step_after(&last_result.step_id, fix_step);
        }

        // If linting errors found, add a fix step
        if last_result.outputs.contains_key("lint_errors") {
            let fix_step = TaskStep::new(
                format!("fix_lint_{}", results.len()),
                "Fix Linting Errors".to_string(),
                StepType::Execution,
            )
            .with_description("Fix the linting errors identified".to_string());

            current_plan.insert_step_after(&last_result.step_id, fix_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:test_implementation_plan (lines 388-406) ---[0m
    async fn test_implementation_plan() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::default();

        let agent = SimpleClaudeAgent::new(
            crate::identity::default_frontend_role(),
            temp_dir.path(),
            config,
        )
        .await
        .unwrap();

        let task = TaskBuilder::new("Implement new dashboard component".to_string()).build();
        let plan = agent.analyze_task(&task).await.unwrap();

        assert!(plan.steps.len() >= 3);
        assert!(plan.steps.iter().any(|s| s.name.contains("Implement")));
        assert!(plan.steps.iter().any(|s| s.name.contains("Test")));
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:50-72 method execute_parallel_task <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:388-406 function test_implementation_plan
  Similarity: 89.16%

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:execute_parallel_task (lines 50-72) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        debug!("Executing parallel task: {} - {}", task.id, task.command);

        // For simple agent, simulate execution
        // In a real implementation, this would use the provider system

        let start = std::time::Instant::now();
        let _duration = start.elapsed();

        // Simulate execution based on command
        let output = format!("Simulated output for: {}", task.command);

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success: true,
            output,
            error: None,
        })
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:test_implementation_plan (lines 388-406) ---[0m
    async fn test_implementation_plan() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::default();

        let agent = SimpleClaudeAgent::new(
            crate::identity::default_frontend_role(),
            temp_dir.path(),
            config,
        )
        .await
        .unwrap();

        let task = TaskBuilder::new("Implement new dashboard component".to_string()).build();
        let plan = agent.analyze_task(&task).await.unwrap();

        assert!(plan.steps.len() >= 3);
        assert!(plan.steps.iter().any(|s| s.name.contains("Implement")));
        assert!(plan.steps.iter().any(|s| s.name.contains("Test")));
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:50-72 method execute_parallel_task <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:366-385 function test_simple_agent_orchestration
  Similarity: 86.77%

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:execute_parallel_task (lines 50-72) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        debug!("Executing parallel task: {} - {}", task.id, task.command);

        // For simple agent, simulate execution
        // In a real implementation, this would use the provider system

        let start = std::time::Instant::now();
        let _duration = start.elapsed();

        // Simulate execution based on command
        let output = format!("Simulated output for: {}", task.command);

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success: true,
            output,
            error: None,
        })
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:test_simple_agent_orchestration (lines 366-385) ---[0m
    async fn test_simple_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::default();

        let agent = SimpleClaudeAgent::new(
            crate::identity::default_backend_role(),
            temp_dir.path(),
            config,
        )
        .await
        .unwrap();

        // Test quality check plan
        let task = TaskBuilder::new("Run tests and linting".to_string()).build();
        let plan = agent.analyze_task(&task).await.unwrap();

        assert!(plan.steps.len() >= 3);
        assert_eq!(plan.steps[0].name, "Analyze Project Structure");
        assert_eq!(plan.steps[1].name, "Run Quality Checks");
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:366-385 function test_simple_agent_orchestration <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:388-406 function test_implementation_plan
  Similarity: 93.66%

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:test_simple_agent_orchestration (lines 366-385) ---[0m
    async fn test_simple_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::default();

        let agent = SimpleClaudeAgent::new(
            crate::identity::default_backend_role(),
            temp_dir.path(),
            config,
        )
        .await
        .unwrap();

        // Test quality check plan
        let task = TaskBuilder::new("Run tests and linting".to_string()).build();
        let plan = agent.analyze_task(&task).await.unwrap();

        assert!(plan.steps.len() >= 3);
        assert_eq!(plan.steps[0].name, "Analyze Project Structure");
        assert_eq!(plan.steps[1].name, "Run Quality Checks");
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:test_implementation_plan (lines 388-406) ---[0m
    async fn test_implementation_plan() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::default();

        let agent = SimpleClaudeAgent::new(
            crate::identity::default_frontend_role(),
            temp_dir.path(),
            config,
        )
        .await
        .unwrap();

        let task = TaskBuilder::new("Implement new dashboard component".to_string()).build();
        let plan = agent.analyze_task(&task).await.unwrap();

        assert!(plan.steps.len() >= 3);
        assert!(plan.steps.iter().any(|s| s.name.contains("Implement")));
        assert!(plan.steps.iter().any(|s| s.name.contains("Test")));
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:19-48 method analyze_task <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:366-385 function test_simple_agent_orchestration
  Similarity: 72.75%

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:analyze_task (lines 19-48) ---[0m
    async fn analyze_task(&self, task: &Task) -> Result<TaskPlan> {
        info!("Analyzing task for orchestration: {}", task.description);

        let plan;

        // Determine task complexity and create appropriate steps
        let task_lower = task.description.to_lowercase();

        if task_lower.contains("test")
            || task_lower.contains("lint")
            || task_lower.contains("build")
        {
            // Quality check workflow
            plan = self.create_quality_check_plan(task)?;
        } else if task_lower.contains("implement")
            || task_lower.contains("create")
            || task_lower.contains("add")
        {
            // Implementation workflow
            plan = self.create_implementation_plan(task)?;
        } else if task_lower.contains("refactor") || task_lower.contains("optimize") {
            // Refactoring workflow
            plan = self.create_refactoring_plan(task)?;
        } else {
            // Default simple workflow
            plan = self.create_default_plan(task)?;
        }

        Ok(plan)
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:test_simple_agent_orchestration (lines 366-385) ---[0m
    async fn test_simple_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::default();

        let agent = SimpleClaudeAgent::new(
            crate::identity::default_backend_role(),
            temp_dir.path(),
            config,
        )
        .await
        .unwrap();

        // Test quality check plan
        let task = TaskBuilder::new("Run tests and linting".to_string()).build();
        let plan = agent.analyze_task(&task).await.unwrap();

        assert!(plan.steps.len() >= 3);
        assert_eq!(plan.steps[0].name, "Analyze Project Structure");
        assert_eq!(plan.steps[1].name, "Run Quality Checks");
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:19-48 method analyze_task <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:388-406 function test_implementation_plan
  Similarity: 73.58%

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:analyze_task (lines 19-48) ---[0m
    async fn analyze_task(&self, task: &Task) -> Result<TaskPlan> {
        info!("Analyzing task for orchestration: {}", task.description);

        let plan;

        // Determine task complexity and create appropriate steps
        let task_lower = task.description.to_lowercase();

        if task_lower.contains("test")
            || task_lower.contains("lint")
            || task_lower.contains("build")
        {
            // Quality check workflow
            plan = self.create_quality_check_plan(task)?;
        } else if task_lower.contains("implement")
            || task_lower.contains("create")
            || task_lower.contains("add")
        {
            // Implementation workflow
            plan = self.create_implementation_plan(task)?;
        } else if task_lower.contains("refactor") || task_lower.contains("optimize") {
            // Refactoring workflow
            plan = self.create_refactoring_plan(task)?;
        } else {
            // Default simple workflow
            plan = self.create_default_plan(task)?;
        }

        Ok(plan)
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:test_implementation_plan (lines 388-406) ---[0m
    async fn test_implementation_plan() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::default();

        let agent = SimpleClaudeAgent::new(
            crate::identity::default_frontend_role(),
            temp_dir.path(),
            config,
        )
        .await
        .unwrap();

        let task = TaskBuilder::new("Implement new dashboard component".to_string()).build();
        let plan = agent.analyze_task(&task).await.unwrap();

        assert!(plan.steps.len() >= 3);
        assert!(plan.steps.iter().any(|s| s.name.contains("Implement")));
        assert!(plan.steps.iter().any(|s| s.name.contains("Test")));
    }

  ./crates/ccswarm/src/agent/simple_orchestrator.rs:50-72 method execute_parallel_task <-> ./crates/ccswarm/src/agent/simple_orchestrator.rs:341-355 method create_default_plan
  Similarity: 79.32%
  Classes: AgentOrchestrator <-> SimpleClaudeAgent

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:execute_parallel_task (lines 50-72) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        debug!("Executing parallel task: {} - {}", task.id, task.command);

        // For simple agent, simulate execution
        // In a real implementation, this would use the provider system

        let start = std::time::Instant::now();
        let _duration = start.elapsed();

        // Simulate execution based on command
        let output = format!("Simulated output for: {}", task.command);

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success: true,
            output,
            error: None,
        })
    }

[36m--- ./crates/ccswarm/src/agent/simple_orchestrator.rs:create_default_plan (lines 341-355) ---[0m
    fn create_default_plan(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Single execution step
        let execution = TaskStep::new(
            "execution".to_string(),
            "Execute Task".to_string(),
            StepType::Execution,
        )
        .with_description(task.description.clone());

        plan.add_step(execution);

        Ok(plan)
    }


Duplicates in ./crates/ccswarm/src/cli/subagent_commands.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/cli/subagent_commands.rs:98-107 function test_create_command <-> ./crates/ccswarm/src/cli/subagent_commands.rs:118-126 function test_delegate_command
  Similarity: 96.04%

[36m--- ./crates/ccswarm/src/cli/subagent_commands.rs:test_create_command (lines 98-107) ---[0m
    async fn test_create_command() {
        let command = SubagentCommand::Create {
            name: "test-agent".to_string(),
            role: "TestRole".to_string(),
            tools: vec!["Read".to_string(), "Write".to_string()],
        };

        let result = execute_subagent_command(command).await;
        assert!(result.is_ok());
    }

[36m--- ./crates/ccswarm/src/cli/subagent_commands.rs:test_delegate_command (lines 118-126) ---[0m
    async fn test_delegate_command() {
        let command = SubagentCommand::Delegate {
            subagent: "frontend-specialist".to_string(),
            task: "Create a button component".to_string(),
        };

        let result = execute_subagent_command(command).await;
        assert!(result.is_ok());
    }

  ./crates/ccswarm/src/cli/subagent_commands.rs:98-107 function test_create_command <-> ./crates/ccswarm/src/cli/subagent_commands.rs:129-136 function test_status_command
  Similarity: 96.04%

[36m--- ./crates/ccswarm/src/cli/subagent_commands.rs:test_create_command (lines 98-107) ---[0m
    async fn test_create_command() {
        let command = SubagentCommand::Create {
            name: "test-agent".to_string(),
            role: "TestRole".to_string(),
            tools: vec!["Read".to_string(), "Write".to_string()],
        };

        let result = execute_subagent_command(command).await;
        assert!(result.is_ok());
    }

[36m--- ./crates/ccswarm/src/cli/subagent_commands.rs:test_status_command (lines 129-136) ---[0m
    async fn test_status_command() {
        let command = SubagentCommand::Status {
            name: "frontend-specialist".to_string(),
        };

        let result = execute_subagent_command(command).await;
        assert!(result.is_ok());
    }

  ./crates/ccswarm/src/cli/subagent_commands.rs:118-126 function test_delegate_command <-> ./crates/ccswarm/src/cli/subagent_commands.rs:129-136 function test_status_command
  Similarity: 95.00%

[36m--- ./crates/ccswarm/src/cli/subagent_commands.rs:test_delegate_command (lines 118-126) ---[0m
    async fn test_delegate_command() {
        let command = SubagentCommand::Delegate {
            subagent: "frontend-specialist".to_string(),
            task: "Create a button component".to_string(),
        };

        let result = execute_subagent_command(command).await;
        assert!(result.is_ok());
    }

[36m--- ./crates/ccswarm/src/cli/subagent_commands.rs:test_status_command (lines 129-136) ---[0m
    async fn test_status_command() {
        let command = SubagentCommand::Status {
            name: "frontend-specialist".to_string(),
        };

        let result = execute_subagent_command(command).await;
        assert!(result.is_ok());
    }

  ./crates/ccswarm/src/cli/subagent_commands.rs:110-115 function test_list_command <-> ./crates/ccswarm/src/cli/subagent_commands.rs:118-126 function test_delegate_command
  Similarity: 94.44%

[36m--- ./crates/ccswarm/src/cli/subagent_commands.rs:test_list_command (lines 110-115) ---[0m
    async fn test_list_command() {
        let command = SubagentCommand::List { detailed: false };

        let result = execute_subagent_command(command).await;
        assert!(result.is_ok());
    }

[36m--- ./crates/ccswarm/src/cli/subagent_commands.rs:test_delegate_command (lines 118-126) ---[0m
    async fn test_delegate_command() {
        let command = SubagentCommand::Delegate {
            subagent: "frontend-specialist".to_string(),
            task: "Create a button component".to_string(),
        };

        let result = execute_subagent_command(command).await;
        assert!(result.is_ok());
    }

  ./crates/ccswarm/src/cli/subagent_commands.rs:110-115 function test_list_command <-> ./crates/ccswarm/src/cli/subagent_commands.rs:129-136 function test_status_command
  Similarity: 94.21%

[36m--- ./crates/ccswarm/src/cli/subagent_commands.rs:test_list_command (lines 110-115) ---[0m
    async fn test_list_command() {
        let command = SubagentCommand::List { detailed: false };

        let result = execute_subagent_command(command).await;
        assert!(result.is_ok());
    }

[36m--- ./crates/ccswarm/src/cli/subagent_commands.rs:test_status_command (lines 129-136) ---[0m
    async fn test_status_command() {
        let command = SubagentCommand::Status {
            name: "frontend-specialist".to_string(),
        };

        let result = execute_subagent_command(command).await;
        assert!(result.is_ok());
    }


Duplicates in ./crates/ccswarm/src/utils/async_macros.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/utils/async_macros.rs:188-202 function test_concurrent_execute <-> ./crates/ccswarm/src/utils/async_macros.rs:217-229 function test_parallel_map
  Similarity: 81.62%

[36m--- ./crates/ccswarm/src/utils/async_macros.rs:test_concurrent_execute (lines 188-202) ---[0m
    async fn test_concurrent_execute() {
        use futures::Future;
        use std::pin::Pin;

        let futures: Vec<Pin<Box<dyn Future<Output = Result<i32>>>>> = vec![
            Box::pin(async { Ok::<_, anyhow::Error>(1) }),
            Box::pin(async { Ok::<_, anyhow::Error>(2) }),
            Box::pin(async { Ok::<_, anyhow::Error>(3) }),
        ];

        let results = futures::future::join_all(futures).await;

        assert_eq!(results.len(), 3);
        assert!(results.iter().all(|r| r.is_ok()));
    }

[36m--- ./crates/ccswarm/src/utils/async_macros.rs:test_parallel_map (lines 217-229) ---[0m
    async fn test_parallel_map() {
        let numbers = vec![1, 2, 3, 4, 5];

        let futures: Vec<_> = numbers
            .into_iter()
            .map(|n| async move { Ok::<_, anyhow::Error>(n * 2) })
            .collect();
        let results: Vec<Result<i32>> = futures::future::join_all(futures).await;

        assert_eq!(results.len(), 5);
        assert_eq!(results[0].as_ref().unwrap(), &2);
        assert_eq!(results[4].as_ref().unwrap(), &10);
    }

  ./crates/ccswarm/src/utils/async_macros.rs:188-202 function test_concurrent_execute <-> ./crates/ccswarm/src/utils/async_macros.rs:205-214 function test_spawn_logged
  Similarity: 83.40%

[36m--- ./crates/ccswarm/src/utils/async_macros.rs:test_concurrent_execute (lines 188-202) ---[0m
    async fn test_concurrent_execute() {
        use futures::Future;
        use std::pin::Pin;

        let futures: Vec<Pin<Box<dyn Future<Output = Result<i32>>>>> = vec![
            Box::pin(async { Ok::<_, anyhow::Error>(1) }),
            Box::pin(async { Ok::<_, anyhow::Error>(2) }),
            Box::pin(async { Ok::<_, anyhow::Error>(3) }),
        ];

        let results = futures::future::join_all(futures).await;

        assert_eq!(results.len(), 3);
        assert!(results.iter().all(|r| r.is_ok()));
    }

[36m--- ./crates/ccswarm/src/utils/async_macros.rs:test_spawn_logged (lines 205-214) ---[0m
    async fn test_spawn_logged() {
        let handle = tokio::spawn(async {
            tokio::time::sleep(tokio::time::Duration::from_millis(10)).await;
            Ok::<_, anyhow::Error>(42)
        });

        let result = handle.await.unwrap();
        assert!(result.is_ok());
        assert_eq!(result.unwrap(), 42);
    }


Duplicates in ./crates/ccswarm/src/monitoring/tests.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/monitoring/tests.rs:30-100 function test_output_entry_filtering <-> ./crates/ccswarm/src/monitoring/tests.rs:262-327 function test_output_subscribers
  Similarity: 86.61%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_output_entry_filtering (lines 30-100) ---[0m
    fn test_output_entry_filtering() {
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Backend".to_string(),
            OutputType::Error,
            "Error occurred in API".to_string(),
            Some("task-456".to_string()),
            "session-789".to_string(),
        );

        // Test agent ID filter
        let filter = OutputFilter {
            agent_ids: Some(vec!["agent-123".to_string()]),
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));

        let filter = OutputFilter {
            agent_ids: Some(vec!["agent-999".to_string()]),
            ..Default::default()
        };
        assert!(!entry.matches_filter(&filter));

        // Test output type filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error, OutputType::Warning]),
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));

        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Info]),
            ..Default::default()
        };
        assert!(!entry.matches_filter(&filter));

        // Test content pattern filter
        let filter = OutputFilter {
            content_pattern: Some("API".to_string()),
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));

        let filter = OutputFilter {
            content_pattern: Some("database".to_string()),
            ..Default::default()
        };
        assert!(!entry.matches_filter(&filter));

        // Test task ID filter
        let filter = OutputFilter {
            task_id: Some("task-456".to_string()),
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));

        let filter = OutputFilter {
            task_id: Some("task-999".to_string()),
            ..Default::default()
        };
        assert!(!entry.matches_filter(&filter));

        // Test combined filters
        let filter = OutputFilter {
            agent_ids: Some(vec!["agent-123".to_string()]),
            output_types: Some(vec![OutputType::Error]),
            content_pattern: Some("api".to_string()), // Case insensitive
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_output_subscribers (lines 262-327) ---[0m
    fn test_output_subscribers() {
        let system = MonitoringSystem::new();

        // Create test subscribers
        let subscriber1 = Arc::new(TestSubscriber {
            id: "sub-1".to_string(),
            received: Arc::new(AtomicUsize::new(0)),
            filter_type: None,
        });

        let subscriber2 = Arc::new(TestSubscriber {
            id: "sub-2".to_string(),
            received: Arc::new(AtomicUsize::new(0)),
            filter_type: Some(OutputType::Error),
        });

        // Add subscribers
        system.add_subscriber(subscriber1.clone()).unwrap();
        system.add_subscriber(subscriber2.clone()).unwrap();

        // Send outputs
        system
            .add_output(
                "agent-1".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Info message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        system
            .add_output(
                "agent-1".to_string(),
                "Test".to_string(),
                OutputType::Error,
                "Error message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Verify subscribers received correct outputs
        assert_eq!(subscriber1.received.load(Ordering::SeqCst), 2);
        assert_eq!(subscriber2.received.load(Ordering::SeqCst), 1);

        // Remove subscriber
        system.remove_subscriber("sub-1").unwrap();

        // Send another output
        system
            .add_output(
                "agent-1".to_string(),
                "Test".to_string(),
                OutputType::Error,
                "Another error".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Verify only subscriber2 received it
        assert_eq!(subscriber1.received.load(Ordering::SeqCst), 2);
        assert_eq!(subscriber2.received.load(Ordering::SeqCst), 2);
    }

  ./crates/ccswarm/src/monitoring/tests.rs:30-100 function test_output_entry_filtering <-> ./crates/ccswarm/src/monitoring/tests.rs:169-236 function test_monitoring_system
  Similarity: 77.14%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_output_entry_filtering (lines 30-100) ---[0m
    fn test_output_entry_filtering() {
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Backend".to_string(),
            OutputType::Error,
            "Error occurred in API".to_string(),
            Some("task-456".to_string()),
            "session-789".to_string(),
        );

        // Test agent ID filter
        let filter = OutputFilter {
            agent_ids: Some(vec!["agent-123".to_string()]),
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));

        let filter = OutputFilter {
            agent_ids: Some(vec!["agent-999".to_string()]),
            ..Default::default()
        };
        assert!(!entry.matches_filter(&filter));

        // Test output type filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error, OutputType::Warning]),
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));

        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Info]),
            ..Default::default()
        };
        assert!(!entry.matches_filter(&filter));

        // Test content pattern filter
        let filter = OutputFilter {
            content_pattern: Some("API".to_string()),
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));

        let filter = OutputFilter {
            content_pattern: Some("database".to_string()),
            ..Default::default()
        };
        assert!(!entry.matches_filter(&filter));

        // Test task ID filter
        let filter = OutputFilter {
            task_id: Some("task-456".to_string()),
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));

        let filter = OutputFilter {
            task_id: Some("task-999".to_string()),
            ..Default::default()
        };
        assert!(!entry.matches_filter(&filter));

        // Test combined filters
        let filter = OutputFilter {
            agent_ids: Some(vec!["agent-123".to_string()]),
            output_types: Some(vec![OutputType::Error]),
            content_pattern: Some("api".to_string()), // Case insensitive
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_monitoring_system (lines 169-236) ---[0m
    fn test_monitoring_system() {
        let system = MonitoringSystem::new();

        // Register agents
        let stream1 = system.register_agent("agent-1".to_string()).unwrap();
        let stream2 = system.register_agent("agent-2".to_string()).unwrap();

        // Add outputs through system
        system
            .add_output(
                "agent-1".to_string(),
                "Frontend".to_string(),
                OutputType::Info,
                "Frontend message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        system
            .add_output(
                "agent-2".to_string(),
                "Backend".to_string(),
                OutputType::Error,
                "Backend error".to_string(),
                Some("task-123".to_string()),
                "session-2".to_string(),
            )
            .unwrap();

        // Verify streams received outputs
        assert_eq!(stream1.len(), 1);
        assert_eq!(stream2.len(), 1);

        // Test getting all recent
        let all_recent = system.get_all_recent(10, None);
        assert_eq!(all_recent.len(), 2);

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let filtered = system.get_all_recent(10, Some(&filter));
        assert_eq!(filtered.len(), 1);
        assert_eq!(filtered[0].agent_id, "agent-2");

        // Test stats
        let stats = system.get_stats();
        assert_eq!(stats.total_entries, 2);
        assert_eq!(stats.active_streams, 2);
        assert_eq!(stats.entries_per_agent.get("agent-1"), Some(&1));
        assert_eq!(stats.entries_per_agent.get("agent-2"), Some(&1));

        // Test clearing
        system.clear_agent_output("agent-1").unwrap();
        assert_eq!(stream1.len(), 0);
        assert_eq!(stream2.len(), 1);

        system.clear_all_output().unwrap();
        assert_eq!(stream1.len(), 0);
        assert_eq!(stream2.len(), 0);

        // Test unregistering
        system.unregister_agent("agent-1").unwrap();
        let stats = system.get_stats();
        assert_eq!(stats.active_streams, 1);
    }

  ./crates/ccswarm/src/monitoring/tests.rs:30-100 function test_output_entry_filtering <-> ./crates/ccswarm/src/monitoring/tests.rs:331-377 function test_broadcast_subscriptions
  Similarity: 87.30%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_output_entry_filtering (lines 30-100) ---[0m
    fn test_output_entry_filtering() {
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Backend".to_string(),
            OutputType::Error,
            "Error occurred in API".to_string(),
            Some("task-456".to_string()),
            "session-789".to_string(),
        );

        // Test agent ID filter
        let filter = OutputFilter {
            agent_ids: Some(vec!["agent-123".to_string()]),
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));

        let filter = OutputFilter {
            agent_ids: Some(vec!["agent-999".to_string()]),
            ..Default::default()
        };
        assert!(!entry.matches_filter(&filter));

        // Test output type filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error, OutputType::Warning]),
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));

        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Info]),
            ..Default::default()
        };
        assert!(!entry.matches_filter(&filter));

        // Test content pattern filter
        let filter = OutputFilter {
            content_pattern: Some("API".to_string()),
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));

        let filter = OutputFilter {
            content_pattern: Some("database".to_string()),
            ..Default::default()
        };
        assert!(!entry.matches_filter(&filter));

        // Test task ID filter
        let filter = OutputFilter {
            task_id: Some("task-456".to_string()),
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));

        let filter = OutputFilter {
            task_id: Some("task-999".to_string()),
            ..Default::default()
        };
        assert!(!entry.matches_filter(&filter));

        // Test combined filters
        let filter = OutputFilter {
            agent_ids: Some(vec!["agent-123".to_string()]),
            output_types: Some(vec![OutputType::Error]),
            content_pattern: Some("api".to_string()), // Case insensitive
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_broadcast_subscriptions (lines 331-377) ---[0m
    async fn test_broadcast_subscriptions() {
        let system = Arc::new(MonitoringSystem::new());

        // Subscribe to global broadcasts
        let mut global_rx = system.subscribe_global();

        // Register agent and subscribe to its stream
        let stream = system.register_agent("test-agent".to_string()).unwrap();
        let mut agent_rx = stream.subscribe();

        // Clone system for the async task
        let system_clone = system.clone();

        // Spawn task to send outputs
        let sender = tokio::spawn(async move {
            sleep(TokioDuration::from_millis(5)).await;

            system_clone
                .add_output(
                    "test-agent".to_string(),
                    "Test".to_string(),
                    OutputType::Info,
                    "Broadcast test".to_string(),
                    None,
                    "session-1".to_string(),
                )
                .unwrap();
        });

        // Wait for outputs on both channels
        let global_result =
            tokio::time::timeout(TokioDuration::from_secs(1), global_rx.recv()).await;

        let agent_result = tokio::time::timeout(TokioDuration::from_secs(1), agent_rx.recv()).await;

        // Verify both received the output
        assert!(global_result.is_ok());
        assert!(agent_result.is_ok());

        let global_entry = global_result.unwrap().unwrap();
        let agent_entry = agent_result.unwrap().unwrap();

        assert_eq!(global_entry.content, "Broadcast test");
        assert_eq!(agent_entry.content, "Broadcast test");

        sender.await.unwrap();
    }

  ./crates/ccswarm/src/monitoring/tests.rs:262-327 function test_output_subscribers <-> ./crates/ccswarm/src/monitoring/tests.rs:331-377 function test_broadcast_subscriptions
  Similarity: 87.71%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_output_subscribers (lines 262-327) ---[0m
    fn test_output_subscribers() {
        let system = MonitoringSystem::new();

        // Create test subscribers
        let subscriber1 = Arc::new(TestSubscriber {
            id: "sub-1".to_string(),
            received: Arc::new(AtomicUsize::new(0)),
            filter_type: None,
        });

        let subscriber2 = Arc::new(TestSubscriber {
            id: "sub-2".to_string(),
            received: Arc::new(AtomicUsize::new(0)),
            filter_type: Some(OutputType::Error),
        });

        // Add subscribers
        system.add_subscriber(subscriber1.clone()).unwrap();
        system.add_subscriber(subscriber2.clone()).unwrap();

        // Send outputs
        system
            .add_output(
                "agent-1".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Info message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        system
            .add_output(
                "agent-1".to_string(),
                "Test".to_string(),
                OutputType::Error,
                "Error message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Verify subscribers received correct outputs
        assert_eq!(subscriber1.received.load(Ordering::SeqCst), 2);
        assert_eq!(subscriber2.received.load(Ordering::SeqCst), 1);

        // Remove subscriber
        system.remove_subscriber("sub-1").unwrap();

        // Send another output
        system
            .add_output(
                "agent-1".to_string(),
                "Test".to_string(),
                OutputType::Error,
                "Another error".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Verify only subscriber2 received it
        assert_eq!(subscriber1.received.load(Ordering::SeqCst), 2);
        assert_eq!(subscriber2.received.load(Ordering::SeqCst), 2);
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_broadcast_subscriptions (lines 331-377) ---[0m
    async fn test_broadcast_subscriptions() {
        let system = Arc::new(MonitoringSystem::new());

        // Subscribe to global broadcasts
        let mut global_rx = system.subscribe_global();

        // Register agent and subscribe to its stream
        let stream = system.register_agent("test-agent".to_string()).unwrap();
        let mut agent_rx = stream.subscribe();

        // Clone system for the async task
        let system_clone = system.clone();

        // Spawn task to send outputs
        let sender = tokio::spawn(async move {
            sleep(TokioDuration::from_millis(5)).await;

            system_clone
                .add_output(
                    "test-agent".to_string(),
                    "Test".to_string(),
                    OutputType::Info,
                    "Broadcast test".to_string(),
                    None,
                    "session-1".to_string(),
                )
                .unwrap();
        });

        // Wait for outputs on both channels
        let global_result =
            tokio::time::timeout(TokioDuration::from_secs(1), global_rx.recv()).await;

        let agent_result = tokio::time::timeout(TokioDuration::from_secs(1), agent_rx.recv()).await;

        // Verify both received the output
        assert!(global_result.is_ok());
        assert!(agent_result.is_ok());

        let global_entry = global_result.unwrap().unwrap();
        let agent_entry = agent_result.unwrap().unwrap();

        assert_eq!(global_entry.content, "Broadcast test");
        assert_eq!(agent_entry.content, "Broadcast test");

        sender.await.unwrap();
    }

  ./crates/ccswarm/src/monitoring/tests.rs:169-236 function test_monitoring_system <-> ./crates/ccswarm/src/monitoring/tests.rs:262-327 function test_output_subscribers
  Similarity: 72.17%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_monitoring_system (lines 169-236) ---[0m
    fn test_monitoring_system() {
        let system = MonitoringSystem::new();

        // Register agents
        let stream1 = system.register_agent("agent-1".to_string()).unwrap();
        let stream2 = system.register_agent("agent-2".to_string()).unwrap();

        // Add outputs through system
        system
            .add_output(
                "agent-1".to_string(),
                "Frontend".to_string(),
                OutputType::Info,
                "Frontend message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        system
            .add_output(
                "agent-2".to_string(),
                "Backend".to_string(),
                OutputType::Error,
                "Backend error".to_string(),
                Some("task-123".to_string()),
                "session-2".to_string(),
            )
            .unwrap();

        // Verify streams received outputs
        assert_eq!(stream1.len(), 1);
        assert_eq!(stream2.len(), 1);

        // Test getting all recent
        let all_recent = system.get_all_recent(10, None);
        assert_eq!(all_recent.len(), 2);

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let filtered = system.get_all_recent(10, Some(&filter));
        assert_eq!(filtered.len(), 1);
        assert_eq!(filtered[0].agent_id, "agent-2");

        // Test stats
        let stats = system.get_stats();
        assert_eq!(stats.total_entries, 2);
        assert_eq!(stats.active_streams, 2);
        assert_eq!(stats.entries_per_agent.get("agent-1"), Some(&1));
        assert_eq!(stats.entries_per_agent.get("agent-2"), Some(&1));

        // Test clearing
        system.clear_agent_output("agent-1").unwrap();
        assert_eq!(stream1.len(), 0);
        assert_eq!(stream2.len(), 1);

        system.clear_all_output().unwrap();
        assert_eq!(stream1.len(), 0);
        assert_eq!(stream2.len(), 0);

        // Test unregistering
        system.unregister_agent("agent-1").unwrap();
        let stats = system.get_stats();
        assert_eq!(stats.active_streams, 1);
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_output_subscribers (lines 262-327) ---[0m
    fn test_output_subscribers() {
        let system = MonitoringSystem::new();

        // Create test subscribers
        let subscriber1 = Arc::new(TestSubscriber {
            id: "sub-1".to_string(),
            received: Arc::new(AtomicUsize::new(0)),
            filter_type: None,
        });

        let subscriber2 = Arc::new(TestSubscriber {
            id: "sub-2".to_string(),
            received: Arc::new(AtomicUsize::new(0)),
            filter_type: Some(OutputType::Error),
        });

        // Add subscribers
        system.add_subscriber(subscriber1.clone()).unwrap();
        system.add_subscriber(subscriber2.clone()).unwrap();

        // Send outputs
        system
            .add_output(
                "agent-1".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Info message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        system
            .add_output(
                "agent-1".to_string(),
                "Test".to_string(),
                OutputType::Error,
                "Error message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Verify subscribers received correct outputs
        assert_eq!(subscriber1.received.load(Ordering::SeqCst), 2);
        assert_eq!(subscriber2.received.load(Ordering::SeqCst), 1);

        // Remove subscriber
        system.remove_subscriber("sub-1").unwrap();

        // Send another output
        system
            .add_output(
                "agent-1".to_string(),
                "Test".to_string(),
                OutputType::Error,
                "Another error".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Verify only subscriber2 received it
        assert_eq!(subscriber1.received.load(Ordering::SeqCst), 2);
        assert_eq!(subscriber2.received.load(Ordering::SeqCst), 2);
    }

  ./crates/ccswarm/src/monitoring/tests.rs:30-100 function test_output_entry_filtering <-> ./crates/ccswarm/src/monitoring/tests.rs:104-143 function test_agent_output_stream
  Similarity: 86.12%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_output_entry_filtering (lines 30-100) ---[0m
    fn test_output_entry_filtering() {
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Backend".to_string(),
            OutputType::Error,
            "Error occurred in API".to_string(),
            Some("task-456".to_string()),
            "session-789".to_string(),
        );

        // Test agent ID filter
        let filter = OutputFilter {
            agent_ids: Some(vec!["agent-123".to_string()]),
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));

        let filter = OutputFilter {
            agent_ids: Some(vec!["agent-999".to_string()]),
            ..Default::default()
        };
        assert!(!entry.matches_filter(&filter));

        // Test output type filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error, OutputType::Warning]),
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));

        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Info]),
            ..Default::default()
        };
        assert!(!entry.matches_filter(&filter));

        // Test content pattern filter
        let filter = OutputFilter {
            content_pattern: Some("API".to_string()),
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));

        let filter = OutputFilter {
            content_pattern: Some("database".to_string()),
            ..Default::default()
        };
        assert!(!entry.matches_filter(&filter));

        // Test task ID filter
        let filter = OutputFilter {
            task_id: Some("task-456".to_string()),
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));

        let filter = OutputFilter {
            task_id: Some("task-999".to_string()),
            ..Default::default()
        };
        assert!(!entry.matches_filter(&filter));

        // Test combined filters
        let filter = OutputFilter {
            agent_ids: Some(vec!["agent-123".to_string()]),
            output_types: Some(vec![OutputType::Error]),
            content_pattern: Some("api".to_string()), // Case insensitive
            ..Default::default()
        };
        assert!(entry.matches_filter(&filter));
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_agent_output_stream (lines 104-143) ---[0m
    fn test_agent_output_stream() {
        let stream = AgentOutputStream::new("test-agent".to_string());

        // Test adding entries
        for i in 0..5 {
            let entry = OutputEntry::new(
                "test-agent".to_string(),
                "Test".to_string(),
                OutputType::Info,
                format!("Message {}", i),
                None,
                "session-1".to_string(),
            );
            stream.add_output(entry).unwrap();
        }

        assert_eq!(stream.len(), 5);
        assert!(!stream.is_empty());

        // Test getting recent entries
        let recent = stream.get_recent(3, None);
        assert_eq!(recent.len(), 3);
        assert_eq!(recent[0].content, "Message 2");
        assert_eq!(recent[1].content, "Message 3");
        assert_eq!(recent[2].content, "Message 4");

        // Test with filter
        let filter = OutputFilter {
            content_pattern: Some("3".to_string()),
            ..Default::default()
        };
        let filtered = stream.get_recent(10, Some(&filter));
        assert_eq!(filtered.len(), 1);
        assert_eq!(filtered[0].content, "Message 3");

        // Test clearing
        stream.clear();
        assert_eq!(stream.len(), 0);
        assert!(stream.is_empty());
    }

  ./crates/ccswarm/src/monitoring/tests.rs:104-143 function test_agent_output_stream <-> ./crates/ccswarm/src/monitoring/tests.rs:262-327 function test_output_subscribers
  Similarity: 86.21%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_agent_output_stream (lines 104-143) ---[0m
    fn test_agent_output_stream() {
        let stream = AgentOutputStream::new("test-agent".to_string());

        // Test adding entries
        for i in 0..5 {
            let entry = OutputEntry::new(
                "test-agent".to_string(),
                "Test".to_string(),
                OutputType::Info,
                format!("Message {}", i),
                None,
                "session-1".to_string(),
            );
            stream.add_output(entry).unwrap();
        }

        assert_eq!(stream.len(), 5);
        assert!(!stream.is_empty());

        // Test getting recent entries
        let recent = stream.get_recent(3, None);
        assert_eq!(recent.len(), 3);
        assert_eq!(recent[0].content, "Message 2");
        assert_eq!(recent[1].content, "Message 3");
        assert_eq!(recent[2].content, "Message 4");

        // Test with filter
        let filter = OutputFilter {
            content_pattern: Some("3".to_string()),
            ..Default::default()
        };
        let filtered = stream.get_recent(10, Some(&filter));
        assert_eq!(filtered.len(), 1);
        assert_eq!(filtered[0].content, "Message 3");

        // Test clearing
        stream.clear();
        assert_eq!(stream.len(), 0);
        assert!(stream.is_empty());
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_output_subscribers (lines 262-327) ---[0m
    fn test_output_subscribers() {
        let system = MonitoringSystem::new();

        // Create test subscribers
        let subscriber1 = Arc::new(TestSubscriber {
            id: "sub-1".to_string(),
            received: Arc::new(AtomicUsize::new(0)),
            filter_type: None,
        });

        let subscriber2 = Arc::new(TestSubscriber {
            id: "sub-2".to_string(),
            received: Arc::new(AtomicUsize::new(0)),
            filter_type: Some(OutputType::Error),
        });

        // Add subscribers
        system.add_subscriber(subscriber1.clone()).unwrap();
        system.add_subscriber(subscriber2.clone()).unwrap();

        // Send outputs
        system
            .add_output(
                "agent-1".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Info message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        system
            .add_output(
                "agent-1".to_string(),
                "Test".to_string(),
                OutputType::Error,
                "Error message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Verify subscribers received correct outputs
        assert_eq!(subscriber1.received.load(Ordering::SeqCst), 2);
        assert_eq!(subscriber2.received.load(Ordering::SeqCst), 1);

        // Remove subscriber
        system.remove_subscriber("sub-1").unwrap();

        // Send another output
        system
            .add_output(
                "agent-1".to_string(),
                "Test".to_string(),
                OutputType::Error,
                "Another error".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Verify only subscriber2 received it
        assert_eq!(subscriber1.received.load(Ordering::SeqCst), 2);
        assert_eq!(subscriber2.received.load(Ordering::SeqCst), 2);
    }

  ./crates/ccswarm/src/monitoring/tests.rs:169-236 function test_monitoring_system <-> ./crates/ccswarm/src/monitoring/tests.rs:331-377 function test_broadcast_subscriptions
  Similarity: 73.81%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_monitoring_system (lines 169-236) ---[0m
    fn test_monitoring_system() {
        let system = MonitoringSystem::new();

        // Register agents
        let stream1 = system.register_agent("agent-1".to_string()).unwrap();
        let stream2 = system.register_agent("agent-2".to_string()).unwrap();

        // Add outputs through system
        system
            .add_output(
                "agent-1".to_string(),
                "Frontend".to_string(),
                OutputType::Info,
                "Frontend message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        system
            .add_output(
                "agent-2".to_string(),
                "Backend".to_string(),
                OutputType::Error,
                "Backend error".to_string(),
                Some("task-123".to_string()),
                "session-2".to_string(),
            )
            .unwrap();

        // Verify streams received outputs
        assert_eq!(stream1.len(), 1);
        assert_eq!(stream2.len(), 1);

        // Test getting all recent
        let all_recent = system.get_all_recent(10, None);
        assert_eq!(all_recent.len(), 2);

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let filtered = system.get_all_recent(10, Some(&filter));
        assert_eq!(filtered.len(), 1);
        assert_eq!(filtered[0].agent_id, "agent-2");

        // Test stats
        let stats = system.get_stats();
        assert_eq!(stats.total_entries, 2);
        assert_eq!(stats.active_streams, 2);
        assert_eq!(stats.entries_per_agent.get("agent-1"), Some(&1));
        assert_eq!(stats.entries_per_agent.get("agent-2"), Some(&1));

        // Test clearing
        system.clear_agent_output("agent-1").unwrap();
        assert_eq!(stream1.len(), 0);
        assert_eq!(stream2.len(), 1);

        system.clear_all_output().unwrap();
        assert_eq!(stream1.len(), 0);
        assert_eq!(stream2.len(), 0);

        // Test unregistering
        system.unregister_agent("agent-1").unwrap();
        let stats = system.get_stats();
        assert_eq!(stats.active_streams, 1);
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_broadcast_subscriptions (lines 331-377) ---[0m
    async fn test_broadcast_subscriptions() {
        let system = Arc::new(MonitoringSystem::new());

        // Subscribe to global broadcasts
        let mut global_rx = system.subscribe_global();

        // Register agent and subscribe to its stream
        let stream = system.register_agent("test-agent".to_string()).unwrap();
        let mut agent_rx = stream.subscribe();

        // Clone system for the async task
        let system_clone = system.clone();

        // Spawn task to send outputs
        let sender = tokio::spawn(async move {
            sleep(TokioDuration::from_millis(5)).await;

            system_clone
                .add_output(
                    "test-agent".to_string(),
                    "Test".to_string(),
                    OutputType::Info,
                    "Broadcast test".to_string(),
                    None,
                    "session-1".to_string(),
                )
                .unwrap();
        });

        // Wait for outputs on both channels
        let global_result =
            tokio::time::timeout(TokioDuration::from_secs(1), global_rx.recv()).await;

        let agent_result = tokio::time::timeout(TokioDuration::from_secs(1), agent_rx.recv()).await;

        // Verify both received the output
        assert!(global_result.is_ok());
        assert!(agent_result.is_ok());

        let global_entry = global_result.unwrap().unwrap();
        let agent_entry = agent_result.unwrap().unwrap();

        assert_eq!(global_entry.content, "Broadcast test");
        assert_eq!(agent_entry.content, "Broadcast test");

        sender.await.unwrap();
    }

  ./crates/ccswarm/src/monitoring/tests.rs:262-327 function test_output_subscribers <-> ./crates/ccswarm/src/monitoring/tests.rs:381-416 function test_console_output_subscriber
  Similarity: 80.97%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_output_subscribers (lines 262-327) ---[0m
    fn test_output_subscribers() {
        let system = MonitoringSystem::new();

        // Create test subscribers
        let subscriber1 = Arc::new(TestSubscriber {
            id: "sub-1".to_string(),
            received: Arc::new(AtomicUsize::new(0)),
            filter_type: None,
        });

        let subscriber2 = Arc::new(TestSubscriber {
            id: "sub-2".to_string(),
            received: Arc::new(AtomicUsize::new(0)),
            filter_type: Some(OutputType::Error),
        });

        // Add subscribers
        system.add_subscriber(subscriber1.clone()).unwrap();
        system.add_subscriber(subscriber2.clone()).unwrap();

        // Send outputs
        system
            .add_output(
                "agent-1".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Info message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        system
            .add_output(
                "agent-1".to_string(),
                "Test".to_string(),
                OutputType::Error,
                "Error message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Verify subscribers received correct outputs
        assert_eq!(subscriber1.received.load(Ordering::SeqCst), 2);
        assert_eq!(subscriber2.received.load(Ordering::SeqCst), 1);

        // Remove subscriber
        system.remove_subscriber("sub-1").unwrap();

        // Send another output
        system
            .add_output(
                "agent-1".to_string(),
                "Test".to_string(),
                OutputType::Error,
                "Another error".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Verify only subscriber2 received it
        assert_eq!(subscriber1.received.load(Ordering::SeqCst), 2);
        assert_eq!(subscriber2.received.load(Ordering::SeqCst), 2);
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_console_output_subscriber (lines 381-416) ---[0m
    fn test_console_output_subscriber() {
        let subscriber = ConsoleOutputSubscriber::new("console-1".to_string());
        assert_eq!(subscriber.id(), "console-1");

        let entry = OutputEntry::new(
            "agent-1".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        // Without filter, accepts all
        assert!(subscriber.accepts(&entry));

        // With filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let subscriber = ConsoleOutputSubscriber::new("console-2".to_string()).with_filter(filter);

        assert!(!subscriber.accepts(&entry));

        let error_entry = OutputEntry::new(
            "agent-1".to_string(),
            "Test".to_string(),
            OutputType::Error,
            "Error message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(subscriber.accepts(&error_entry));
    }

  ./crates/ccswarm/src/monitoring/tests.rs:104-143 function test_agent_output_stream <-> ./crates/ccswarm/src/monitoring/tests.rs:169-236 function test_monitoring_system
  Similarity: 72.49%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_agent_output_stream (lines 104-143) ---[0m
    fn test_agent_output_stream() {
        let stream = AgentOutputStream::new("test-agent".to_string());

        // Test adding entries
        for i in 0..5 {
            let entry = OutputEntry::new(
                "test-agent".to_string(),
                "Test".to_string(),
                OutputType::Info,
                format!("Message {}", i),
                None,
                "session-1".to_string(),
            );
            stream.add_output(entry).unwrap();
        }

        assert_eq!(stream.len(), 5);
        assert!(!stream.is_empty());

        // Test getting recent entries
        let recent = stream.get_recent(3, None);
        assert_eq!(recent.len(), 3);
        assert_eq!(recent[0].content, "Message 2");
        assert_eq!(recent[1].content, "Message 3");
        assert_eq!(recent[2].content, "Message 4");

        // Test with filter
        let filter = OutputFilter {
            content_pattern: Some("3".to_string()),
            ..Default::default()
        };
        let filtered = stream.get_recent(10, Some(&filter));
        assert_eq!(filtered.len(), 1);
        assert_eq!(filtered[0].content, "Message 3");

        // Test clearing
        stream.clear();
        assert_eq!(stream.len(), 0);
        assert!(stream.is_empty());
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_monitoring_system (lines 169-236) ---[0m
    fn test_monitoring_system() {
        let system = MonitoringSystem::new();

        // Register agents
        let stream1 = system.register_agent("agent-1".to_string()).unwrap();
        let stream2 = system.register_agent("agent-2".to_string()).unwrap();

        // Add outputs through system
        system
            .add_output(
                "agent-1".to_string(),
                "Frontend".to_string(),
                OutputType::Info,
                "Frontend message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        system
            .add_output(
                "agent-2".to_string(),
                "Backend".to_string(),
                OutputType::Error,
                "Backend error".to_string(),
                Some("task-123".to_string()),
                "session-2".to_string(),
            )
            .unwrap();

        // Verify streams received outputs
        assert_eq!(stream1.len(), 1);
        assert_eq!(stream2.len(), 1);

        // Test getting all recent
        let all_recent = system.get_all_recent(10, None);
        assert_eq!(all_recent.len(), 2);

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let filtered = system.get_all_recent(10, Some(&filter));
        assert_eq!(filtered.len(), 1);
        assert_eq!(filtered[0].agent_id, "agent-2");

        // Test stats
        let stats = system.get_stats();
        assert_eq!(stats.total_entries, 2);
        assert_eq!(stats.active_streams, 2);
        assert_eq!(stats.entries_per_agent.get("agent-1"), Some(&1));
        assert_eq!(stats.entries_per_agent.get("agent-2"), Some(&1));

        // Test clearing
        system.clear_agent_output("agent-1").unwrap();
        assert_eq!(stream1.len(), 0);
        assert_eq!(stream2.len(), 1);

        system.clear_all_output().unwrap();
        assert_eq!(stream1.len(), 0);
        assert_eq!(stream2.len(), 0);

        // Test unregistering
        system.unregister_agent("agent-1").unwrap();
        let stats = system.get_stats();
        assert_eq!(stats.active_streams, 1);
    }

  ./crates/ccswarm/src/monitoring/tests.rs:104-143 function test_agent_output_stream <-> ./crates/ccswarm/src/monitoring/tests.rs:331-377 function test_broadcast_subscriptions
  Similarity: 82.31%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_agent_output_stream (lines 104-143) ---[0m
    fn test_agent_output_stream() {
        let stream = AgentOutputStream::new("test-agent".to_string());

        // Test adding entries
        for i in 0..5 {
            let entry = OutputEntry::new(
                "test-agent".to_string(),
                "Test".to_string(),
                OutputType::Info,
                format!("Message {}", i),
                None,
                "session-1".to_string(),
            );
            stream.add_output(entry).unwrap();
        }

        assert_eq!(stream.len(), 5);
        assert!(!stream.is_empty());

        // Test getting recent entries
        let recent = stream.get_recent(3, None);
        assert_eq!(recent.len(), 3);
        assert_eq!(recent[0].content, "Message 2");
        assert_eq!(recent[1].content, "Message 3");
        assert_eq!(recent[2].content, "Message 4");

        // Test with filter
        let filter = OutputFilter {
            content_pattern: Some("3".to_string()),
            ..Default::default()
        };
        let filtered = stream.get_recent(10, Some(&filter));
        assert_eq!(filtered.len(), 1);
        assert_eq!(filtered[0].content, "Message 3");

        // Test clearing
        stream.clear();
        assert_eq!(stream.len(), 0);
        assert!(stream.is_empty());
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_broadcast_subscriptions (lines 331-377) ---[0m
    async fn test_broadcast_subscriptions() {
        let system = Arc::new(MonitoringSystem::new());

        // Subscribe to global broadcasts
        let mut global_rx = system.subscribe_global();

        // Register agent and subscribe to its stream
        let stream = system.register_agent("test-agent".to_string()).unwrap();
        let mut agent_rx = stream.subscribe();

        // Clone system for the async task
        let system_clone = system.clone();

        // Spawn task to send outputs
        let sender = tokio::spawn(async move {
            sleep(TokioDuration::from_millis(5)).await;

            system_clone
                .add_output(
                    "test-agent".to_string(),
                    "Test".to_string(),
                    OutputType::Info,
                    "Broadcast test".to_string(),
                    None,
                    "session-1".to_string(),
                )
                .unwrap();
        });

        // Wait for outputs on both channels
        let global_result =
            tokio::time::timeout(TokioDuration::from_secs(1), global_rx.recv()).await;

        let agent_result = tokio::time::timeout(TokioDuration::from_secs(1), agent_rx.recv()).await;

        // Verify both received the output
        assert!(global_result.is_ok());
        assert!(agent_result.is_ok());

        let global_entry = global_result.unwrap().unwrap();
        let agent_entry = agent_result.unwrap().unwrap();

        assert_eq!(global_entry.content, "Broadcast test");
        assert_eq!(agent_entry.content, "Broadcast test");

        sender.await.unwrap();
    }

  ./crates/ccswarm/src/monitoring/tests.rs:331-377 function test_broadcast_subscriptions <-> ./crates/ccswarm/src/monitoring/tests.rs:381-416 function test_console_output_subscriber
  Similarity: 72.68%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_broadcast_subscriptions (lines 331-377) ---[0m
    async fn test_broadcast_subscriptions() {
        let system = Arc::new(MonitoringSystem::new());

        // Subscribe to global broadcasts
        let mut global_rx = system.subscribe_global();

        // Register agent and subscribe to its stream
        let stream = system.register_agent("test-agent".to_string()).unwrap();
        let mut agent_rx = stream.subscribe();

        // Clone system for the async task
        let system_clone = system.clone();

        // Spawn task to send outputs
        let sender = tokio::spawn(async move {
            sleep(TokioDuration::from_millis(5)).await;

            system_clone
                .add_output(
                    "test-agent".to_string(),
                    "Test".to_string(),
                    OutputType::Info,
                    "Broadcast test".to_string(),
                    None,
                    "session-1".to_string(),
                )
                .unwrap();
        });

        // Wait for outputs on both channels
        let global_result =
            tokio::time::timeout(TokioDuration::from_secs(1), global_rx.recv()).await;

        let agent_result = tokio::time::timeout(TokioDuration::from_secs(1), agent_rx.recv()).await;

        // Verify both received the output
        assert!(global_result.is_ok());
        assert!(agent_result.is_ok());

        let global_entry = global_result.unwrap().unwrap();
        let agent_entry = agent_result.unwrap().unwrap();

        assert_eq!(global_entry.content, "Broadcast test");
        assert_eq!(agent_entry.content, "Broadcast test");

        sender.await.unwrap();
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_console_output_subscriber (lines 381-416) ---[0m
    fn test_console_output_subscriber() {
        let subscriber = ConsoleOutputSubscriber::new("console-1".to_string());
        assert_eq!(subscriber.id(), "console-1");

        let entry = OutputEntry::new(
            "agent-1".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        // Without filter, accepts all
        assert!(subscriber.accepts(&entry));

        // With filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let subscriber = ConsoleOutputSubscriber::new("console-2".to_string()).with_filter(filter);

        assert!(!subscriber.accepts(&entry));

        let error_entry = OutputEntry::new(
            "agent-1".to_string(),
            "Test".to_string(),
            OutputType::Error,
            "Error message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(subscriber.accepts(&error_entry));
    }

  ./crates/ccswarm/src/monitoring/tests.rs:381-416 function test_console_output_subscriber <-> ./crates/ccswarm/src/monitoring/tests.rs:443-476 function test_thread_safety
  Similarity: 85.54%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_console_output_subscriber (lines 381-416) ---[0m
    fn test_console_output_subscriber() {
        let subscriber = ConsoleOutputSubscriber::new("console-1".to_string());
        assert_eq!(subscriber.id(), "console-1");

        let entry = OutputEntry::new(
            "agent-1".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        // Without filter, accepts all
        assert!(subscriber.accepts(&entry));

        // With filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let subscriber = ConsoleOutputSubscriber::new("console-2".to_string()).with_filter(filter);

        assert!(!subscriber.accepts(&entry));

        let error_entry = OutputEntry::new(
            "agent-1".to_string(),
            "Test".to_string(),
            OutputType::Error,
            "Error message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(subscriber.accepts(&error_entry));
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_thread_safety (lines 443-476) ---[0m
    fn test_thread_safety() {
        let system = Arc::new(MonitoringSystem::new());
        let mut handles = vec![];

        // Spawn multiple threads adding outputs
        for i in 0..10 {
            let system_clone = system.clone();
            let handle = std::thread::spawn(move || {
                for j in 0..100 {
                    system_clone
                        .add_output(
                            format!("agent-{}", i),
                            "Test".to_string(),
                            OutputType::Info,
                            format!("Message {}", j),
                            None,
                            "session-1".to_string(),
                        )
                        .unwrap();
                }
            });
            handles.push(handle);
        }

        // Wait for all threads
        for handle in handles {
            handle.join().unwrap();
        }

        // Verify all outputs were recorded
        let stats = system.get_stats();
        assert_eq!(stats.total_entries, 1000);
        assert_eq!(stats.active_streams, 10);
    }

  ./crates/ccswarm/src/monitoring/tests.rs:331-377 function test_broadcast_subscriptions <-> ./crates/ccswarm/src/monitoring/tests.rs:443-476 function test_thread_safety
  Similarity: 71.86%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_broadcast_subscriptions (lines 331-377) ---[0m
    async fn test_broadcast_subscriptions() {
        let system = Arc::new(MonitoringSystem::new());

        // Subscribe to global broadcasts
        let mut global_rx = system.subscribe_global();

        // Register agent and subscribe to its stream
        let stream = system.register_agent("test-agent".to_string()).unwrap();
        let mut agent_rx = stream.subscribe();

        // Clone system for the async task
        let system_clone = system.clone();

        // Spawn task to send outputs
        let sender = tokio::spawn(async move {
            sleep(TokioDuration::from_millis(5)).await;

            system_clone
                .add_output(
                    "test-agent".to_string(),
                    "Test".to_string(),
                    OutputType::Info,
                    "Broadcast test".to_string(),
                    None,
                    "session-1".to_string(),
                )
                .unwrap();
        });

        // Wait for outputs on both channels
        let global_result =
            tokio::time::timeout(TokioDuration::from_secs(1), global_rx.recv()).await;

        let agent_result = tokio::time::timeout(TokioDuration::from_secs(1), agent_rx.recv()).await;

        // Verify both received the output
        assert!(global_result.is_ok());
        assert!(agent_result.is_ok());

        let global_entry = global_result.unwrap().unwrap();
        let agent_entry = agent_result.unwrap().unwrap();

        assert_eq!(global_entry.content, "Broadcast test");
        assert_eq!(agent_entry.content, "Broadcast test");

        sender.await.unwrap();
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_thread_safety (lines 443-476) ---[0m
    fn test_thread_safety() {
        let system = Arc::new(MonitoringSystem::new());
        let mut handles = vec![];

        // Spawn multiple threads adding outputs
        for i in 0..10 {
            let system_clone = system.clone();
            let handle = std::thread::spawn(move || {
                for j in 0..100 {
                    system_clone
                        .add_output(
                            format!("agent-{}", i),
                            "Test".to_string(),
                            OutputType::Info,
                            format!("Message {}", j),
                            None,
                            "session-1".to_string(),
                        )
                        .unwrap();
                }
            });
            handles.push(handle);
        }

        // Wait for all threads
        for handle in handles {
            handle.join().unwrap();
        }

        // Verify all outputs were recorded
        let stats = system.get_stats();
        assert_eq!(stats.total_entries, 1000);
        assert_eq!(stats.active_streams, 10);
    }

  ./crates/ccswarm/src/monitoring/tests.rs:420-439 function test_auto_registration <-> ./crates/ccswarm/src/monitoring/tests.rs:443-476 function test_thread_safety
  Similarity: 87.01%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_auto_registration (lines 420-439) ---[0m
    fn test_auto_registration() {
        let system = MonitoringSystem::new();

        // Add output for non-existent agent
        system
            .add_output(
                "new-agent".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Auto-registered".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Verify agent was auto-registered
        let stream = system.get_agent_stream("new-agent");
        assert!(stream.is_some());
        assert_eq!(stream.unwrap().len(), 1);
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_thread_safety (lines 443-476) ---[0m
    fn test_thread_safety() {
        let system = Arc::new(MonitoringSystem::new());
        let mut handles = vec![];

        // Spawn multiple threads adding outputs
        for i in 0..10 {
            let system_clone = system.clone();
            let handle = std::thread::spawn(move || {
                for j in 0..100 {
                    system_clone
                        .add_output(
                            format!("agent-{}", i),
                            "Test".to_string(),
                            OutputType::Info,
                            format!("Message {}", j),
                            None,
                            "session-1".to_string(),
                        )
                        .unwrap();
                }
            });
            handles.push(handle);
        }

        // Wait for all threads
        for handle in handles {
            handle.join().unwrap();
        }

        // Verify all outputs were recorded
        let stats = system.get_stats();
        assert_eq!(stats.total_entries, 1000);
        assert_eq!(stats.active_streams, 10);
    }

  ./crates/ccswarm/src/monitoring/tests.rs:147-165 function test_buffer_size_limit <-> ./crates/ccswarm/src/monitoring/tests.rs:443-476 function test_thread_safety
  Similarity: 82.35%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_buffer_size_limit (lines 147-165) ---[0m
    fn test_buffer_size_limit() {
        let stream = AgentOutputStream::new("test-agent".to_string());

        // Add more entries than MAX_BUFFER_SIZE
        for i in 0..MAX_BUFFER_SIZE + 100 {
            let entry = OutputEntry::new(
                "test-agent".to_string(),
                "Test".to_string(),
                OutputType::Info,
                format!("Message {}", i),
                None,
                "session-1".to_string(),
            );
            stream.add_output(entry).unwrap();
        }

        // Verify buffer doesn't exceed max size
        assert_eq!(stream.len(), MAX_BUFFER_SIZE);
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_thread_safety (lines 443-476) ---[0m
    fn test_thread_safety() {
        let system = Arc::new(MonitoringSystem::new());
        let mut handles = vec![];

        // Spawn multiple threads adding outputs
        for i in 0..10 {
            let system_clone = system.clone();
            let handle = std::thread::spawn(move || {
                for j in 0..100 {
                    system_clone
                        .add_output(
                            format!("agent-{}", i),
                            "Test".to_string(),
                            OutputType::Info,
                            format!("Message {}", j),
                            None,
                            "session-1".to_string(),
                        )
                        .unwrap();
                }
            });
            handles.push(handle);
        }

        // Wait for all threads
        for handle in handles {
            handle.join().unwrap();
        }

        // Verify all outputs were recorded
        let stats = system.get_stats();
        assert_eq!(stats.total_entries, 1000);
        assert_eq!(stats.active_streams, 10);
    }

  ./crates/ccswarm/src/monitoring/tests.rs:10-26 function test_output_entry_creation <-> ./crates/ccswarm/src/monitoring/tests.rs:443-476 function test_thread_safety
  Similarity: 80.44%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_output_entry_creation (lines 10-26) ---[0m
    fn test_output_entry_creation() {
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Frontend".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            Some("task-456".to_string()),
            "session-789".to_string(),
        );

        assert_eq!(entry.agent_id, "agent-123");
        assert_eq!(entry.agent_type, "Frontend");
        assert_eq!(entry.output_type, OutputType::Info);
        assert_eq!(entry.content, "Test message");
        assert_eq!(entry.task_id, Some("task-456".to_string()));
        assert_eq!(entry.session_id, "session-789");
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_thread_safety (lines 443-476) ---[0m
    fn test_thread_safety() {
        let system = Arc::new(MonitoringSystem::new());
        let mut handles = vec![];

        // Spawn multiple threads adding outputs
        for i in 0..10 {
            let system_clone = system.clone();
            let handle = std::thread::spawn(move || {
                for j in 0..100 {
                    system_clone
                        .add_output(
                            format!("agent-{}", i),
                            "Test".to_string(),
                            OutputType::Info,
                            format!("Message {}", j),
                            None,
                            "session-1".to_string(),
                        )
                        .unwrap();
                }
            });
            handles.push(handle);
        }

        // Wait for all threads
        for handle in handles {
            handle.join().unwrap();
        }

        // Verify all outputs were recorded
        let stats = system.get_stats();
        assert_eq!(stats.total_entries, 1000);
        assert_eq!(stats.active_streams, 10);
    }

  ./crates/ccswarm/src/monitoring/tests.rs:10-26 function test_output_entry_creation <-> ./crates/ccswarm/src/monitoring/tests.rs:381-416 function test_console_output_subscriber
  Similarity: 77.36%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_output_entry_creation (lines 10-26) ---[0m
    fn test_output_entry_creation() {
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Frontend".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            Some("task-456".to_string()),
            "session-789".to_string(),
        );

        assert_eq!(entry.agent_id, "agent-123");
        assert_eq!(entry.agent_type, "Frontend");
        assert_eq!(entry.output_type, OutputType::Info);
        assert_eq!(entry.content, "Test message");
        assert_eq!(entry.task_id, Some("task-456".to_string()));
        assert_eq!(entry.session_id, "session-789");
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_console_output_subscriber (lines 381-416) ---[0m
    fn test_console_output_subscriber() {
        let subscriber = ConsoleOutputSubscriber::new("console-1".to_string());
        assert_eq!(subscriber.id(), "console-1");

        let entry = OutputEntry::new(
            "agent-1".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        // Without filter, accepts all
        assert!(subscriber.accepts(&entry));

        // With filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let subscriber = ConsoleOutputSubscriber::new("console-2".to_string()).with_filter(filter);

        assert!(!subscriber.accepts(&entry));

        let error_entry = OutputEntry::new(
            "agent-1".to_string(),
            "Test".to_string(),
            OutputType::Error,
            "Error message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(subscriber.accepts(&error_entry));
    }

  ./crates/ccswarm/src/monitoring/tests.rs:443-476 function test_thread_safety <-> ./crates/ccswarm/src/monitoring/tests.rs:480-494 function test_empty_state
  Similarity: 82.03%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_thread_safety (lines 443-476) ---[0m
    fn test_thread_safety() {
        let system = Arc::new(MonitoringSystem::new());
        let mut handles = vec![];

        // Spawn multiple threads adding outputs
        for i in 0..10 {
            let system_clone = system.clone();
            let handle = std::thread::spawn(move || {
                for j in 0..100 {
                    system_clone
                        .add_output(
                            format!("agent-{}", i),
                            "Test".to_string(),
                            OutputType::Info,
                            format!("Message {}", j),
                            None,
                            "session-1".to_string(),
                        )
                        .unwrap();
                }
            });
            handles.push(handle);
        }

        // Wait for all threads
        for handle in handles {
            handle.join().unwrap();
        }

        // Verify all outputs were recorded
        let stats = system.get_stats();
        assert_eq!(stats.total_entries, 1000);
        assert_eq!(stats.active_streams, 10);
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_empty_state (lines 480-494) ---[0m
    fn test_empty_state() {
        let system = MonitoringSystem::new();

        // Test operations on empty system
        let stats = system.get_stats();
        assert_eq!(stats.total_entries, 0);
        assert_eq!(stats.active_streams, 0);

        let recent = system.get_all_recent(10, None);
        assert!(recent.is_empty());

        // Clear operations should not fail
        assert!(system.clear_agent_output("non-existent").is_err());
        assert!(system.clear_all_output().is_ok());
    }

  ./crates/ccswarm/src/monitoring/tests.rs:147-165 function test_buffer_size_limit <-> ./crates/ccswarm/src/monitoring/tests.rs:420-439 function test_auto_registration
  Similarity: 80.82%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_buffer_size_limit (lines 147-165) ---[0m
    fn test_buffer_size_limit() {
        let stream = AgentOutputStream::new("test-agent".to_string());

        // Add more entries than MAX_BUFFER_SIZE
        for i in 0..MAX_BUFFER_SIZE + 100 {
            let entry = OutputEntry::new(
                "test-agent".to_string(),
                "Test".to_string(),
                OutputType::Info,
                format!("Message {}", i),
                None,
                "session-1".to_string(),
            );
            stream.add_output(entry).unwrap();
        }

        // Verify buffer doesn't exceed max size
        assert_eq!(stream.len(), MAX_BUFFER_SIZE);
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_auto_registration (lines 420-439) ---[0m
    fn test_auto_registration() {
        let system = MonitoringSystem::new();

        // Add output for non-existent agent
        system
            .add_output(
                "new-agent".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Auto-registered".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Verify agent was auto-registered
        let stream = system.get_agent_stream("new-agent");
        assert!(stream.is_some());
        assert_eq!(stream.unwrap().len(), 1);
    }

  ./crates/ccswarm/src/monitoring/tests.rs:10-26 function test_output_entry_creation <-> ./crates/ccswarm/src/monitoring/tests.rs:420-439 function test_auto_registration
  Similarity: 83.99%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_output_entry_creation (lines 10-26) ---[0m
    fn test_output_entry_creation() {
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Frontend".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            Some("task-456".to_string()),
            "session-789".to_string(),
        );

        assert_eq!(entry.agent_id, "agent-123");
        assert_eq!(entry.agent_type, "Frontend");
        assert_eq!(entry.output_type, OutputType::Info);
        assert_eq!(entry.content, "Test message");
        assert_eq!(entry.task_id, Some("task-456".to_string()));
        assert_eq!(entry.session_id, "session-789");
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_auto_registration (lines 420-439) ---[0m
    fn test_auto_registration() {
        let system = MonitoringSystem::new();

        // Add output for non-existent agent
        system
            .add_output(
                "new-agent".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Auto-registered".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Verify agent was auto-registered
        let stream = system.get_agent_stream("new-agent");
        assert!(stream.is_some());
        assert_eq!(stream.unwrap().len(), 1);
    }

  ./crates/ccswarm/src/monitoring/tests.rs:10-26 function test_output_entry_creation <-> ./crates/ccswarm/src/monitoring/tests.rs:147-165 function test_buffer_size_limit
  Similarity: 71.60%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_output_entry_creation (lines 10-26) ---[0m
    fn test_output_entry_creation() {
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Frontend".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            Some("task-456".to_string()),
            "session-789".to_string(),
        );

        assert_eq!(entry.agent_id, "agent-123");
        assert_eq!(entry.agent_type, "Frontend");
        assert_eq!(entry.output_type, OutputType::Info);
        assert_eq!(entry.content, "Test message");
        assert_eq!(entry.task_id, Some("task-456".to_string()));
        assert_eq!(entry.session_id, "session-789");
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_buffer_size_limit (lines 147-165) ---[0m
    fn test_buffer_size_limit() {
        let stream = AgentOutputStream::new("test-agent".to_string());

        // Add more entries than MAX_BUFFER_SIZE
        for i in 0..MAX_BUFFER_SIZE + 100 {
            let entry = OutputEntry::new(
                "test-agent".to_string(),
                "Test".to_string(),
                OutputType::Info,
                format!("Message {}", i),
                None,
                "session-1".to_string(),
            );
            stream.add_output(entry).unwrap();
        }

        // Verify buffer doesn't exceed max size
        assert_eq!(stream.len(), MAX_BUFFER_SIZE);
    }

  ./crates/ccswarm/src/monitoring/tests.rs:420-439 function test_auto_registration <-> ./crates/ccswarm/src/monitoring/tests.rs:480-494 function test_empty_state
  Similarity: 72.96%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_auto_registration (lines 420-439) ---[0m
    fn test_auto_registration() {
        let system = MonitoringSystem::new();

        // Add output for non-existent agent
        system
            .add_output(
                "new-agent".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Auto-registered".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Verify agent was auto-registered
        let stream = system.get_agent_stream("new-agent");
        assert!(stream.is_some());
        assert_eq!(stream.unwrap().len(), 1);
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_empty_state (lines 480-494) ---[0m
    fn test_empty_state() {
        let system = MonitoringSystem::new();

        // Test operations on empty system
        let stats = system.get_stats();
        assert_eq!(stats.total_entries, 0);
        assert_eq!(stats.active_streams, 0);

        let recent = system.get_all_recent(10, None);
        assert!(recent.is_empty());

        // Clear operations should not fail
        assert!(system.clear_agent_output("non-existent").is_err());
        assert!(system.clear_all_output().is_ok());
    }

  ./crates/ccswarm/src/monitoring/tests.rs:10-26 function test_output_entry_creation <-> ./crates/ccswarm/src/monitoring/tests.rs:480-494 function test_empty_state
  Similarity: 76.65%

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_output_entry_creation (lines 10-26) ---[0m
    fn test_output_entry_creation() {
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Frontend".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            Some("task-456".to_string()),
            "session-789".to_string(),
        );

        assert_eq!(entry.agent_id, "agent-123");
        assert_eq!(entry.agent_type, "Frontend");
        assert_eq!(entry.output_type, OutputType::Info);
        assert_eq!(entry.content, "Test message");
        assert_eq!(entry.task_id, Some("task-456".to_string()));
        assert_eq!(entry.session_id, "session-789");
    }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:test_empty_state (lines 480-494) ---[0m
    fn test_empty_state() {
        let system = MonitoringSystem::new();

        // Test operations on empty system
        let stats = system.get_stats();
        assert_eq!(stats.total_entries, 0);
        assert_eq!(stats.active_streams, 0);

        let recent = system.get_all_recent(10, None);
        assert!(recent.is_empty());

        // Clear operations should not fail
        assert!(system.clear_agent_output("non-existent").is_err());
        assert!(system.clear_all_output().is_ok());
    }

  ./crates/ccswarm/src/monitoring/tests.rs:246-250 method on_output <-> ./crates/ccswarm/src/monitoring/tests.rs:256-258 method accepts
  Similarity: 70.66%
  Classes: OutputSubscriber <-> OutputSubscriber

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:on_output (lines 246-250) ---[0m
        fn on_output(&self, entry: &OutputEntry) {
            if self.filter_type.is_none() || self.filter_type == Some(entry.output_type.clone()) {
                self.received.fetch_add(1, Ordering::SeqCst);
            }
        }

[36m--- ./crates/ccswarm/src/monitoring/tests.rs:accepts (lines 256-258) ---[0m
        fn accepts(&self, entry: &OutputEntry) -> bool {
            self.filter_type.is_none() || self.filter_type == Some(entry.output_type.clone())
        }


Duplicates in ./crates/ccswarm/src/acp_claude/adapter.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/acp_claude/adapter.rs:86-119 method connect <-> ./crates/ccswarm/src/acp_claude/adapter.rs:122-154 method send_task
  Similarity: 83.18%
  Classes: SimplifiedClaudeAdapter <-> SimplifiedClaudeAdapter

[36m--- ./crates/ccswarm/src/acp_claude/adapter.rs:connect (lines 86-119) ---[0m
    pub async fn connect(&mut self) -> ACPResult<()> {
        debug!("Connecting to Claude Code at {}", self.config.url);

        // Connect directly with URL string
        let ws_future = connect_async(&self.config.url);
        let connection_result = timeout(Duration::from_secs(self.config.timeout), ws_future).await;

        let (ws_stream, _) = match connection_result {
            Ok(Ok((stream, response))) => {
                debug!("WebSocket connected: {:?}", response);
                (stream, response)
            }
            Ok(Err(e)) => {
                debug!("WebSocket connection error: {}", e);
                return Err(ACPError::WebSocketError(e.to_string()));
            }
            Err(_) => {
                debug!("Connection timeout after {} seconds", self.config.timeout);
                return Err(ACPError::Timeout(self.config.timeout));
            }
        };

        // Store connection
        self.ws_stream = Some(Arc::new(Mutex::new(ws_stream)));
        self.connected = true;

        // Initialize session (simplified)
        self.session_id = Some(format!("session-{}", uuid::Uuid::new_v4()));

        info!("✅ Connected to Claude Code!");
        debug!("Session created: {}", self.session_id.as_ref().unwrap());

        Ok(())
    }

[36m--- ./crates/ccswarm/src/acp_claude/adapter.rs:send_task (lines 122-154) ---[0m
    pub async fn send_task(&self, task: &str) -> ACPResult<String> {
        if !self.connected {
            return Err(ACPError::NotConnected);
        }

        info!("📤 Sending task to Claude Code: {}", task);

        // Get WebSocket stream
        let ws_stream = self.ws_stream.as_ref().ok_or(ACPError::NotConnected)?;

        // Send task message
        let message = Message::Text(task.to_string());

        {
            let mut stream = ws_stream.lock().await;
            stream
                .send(message)
                .await
                .map_err(|e| ACPError::WebSocketError(e.to_string()))?;
            stream
                .flush()
                .await
                .map_err(|e| ACPError::WebSocketError(e.to_string()))?;

            // Read response
            if let Some(Ok(Message::Text(response))) = stream.next().await {
                return Ok(response);
            }
        }

        // Fallback response
        Ok(format!("Task '{}' sent to Claude Code", task))
    }

  ./crates/ccswarm/src/acp_claude/adapter.rs:63-83 method connect_with_retry <-> ./crates/ccswarm/src/acp_claude/adapter.rs:202-212 function test_config_builder
  Similarity: 75.99%

[36m--- ./crates/ccswarm/src/acp_claude/adapter.rs:connect_with_retry (lines 63-83) ---[0m
    pub async fn connect_with_retry(&mut self) -> ACPResult<()> {
        for attempt in 1..=self.config.max_retries {
            info!(
                "🔄 Connection attempt {}/{} to {}...",
                attempt, self.config.max_retries, self.config.url
            );

            match self.connect().await {
                Ok(_) => return Ok(()),
                Err(e) if attempt < self.config.max_retries => {
                    warn!(
                        "⚠️  Retry in {} seconds... ({})",
                        self.config.retry_delay, e
                    );
                    tokio::time::sleep(Duration::from_secs(self.config.retry_delay)).await;
                }
                Err(e) => return Err(e),
            }
        }
        unreachable!()
    }

[36m--- ./crates/ccswarm/src/acp_claude/adapter.rs:test_config_builder (lines 202-212) ---[0m
    async fn test_config_builder() {
        let config = ClaudeACPConfig::builder()
            .url("ws://test:8080")
            .timeout(60)
            .max_retries(5)
            .build();

        assert_eq!(config.url, "ws://test:8080");
        assert_eq!(config.timeout, 60);
        assert_eq!(config.max_retries, 5);
    }

  ./crates/ccswarm/src/acp_claude/adapter.rs:194-199 function test_adapter_creation <-> ./crates/ccswarm/src/acp_claude/adapter.rs:202-212 function test_config_builder
  Similarity: 83.78%

[36m--- ./crates/ccswarm/src/acp_claude/adapter.rs:test_adapter_creation (lines 194-199) ---[0m
    async fn test_adapter_creation() {
        let config = ClaudeACPConfig::default();
        let adapter = SimplifiedClaudeAdapter::new(config);
        assert!(!adapter.is_connected());
        assert!(adapter.session_id().is_none());
    }

[36m--- ./crates/ccswarm/src/acp_claude/adapter.rs:test_config_builder (lines 202-212) ---[0m
    async fn test_config_builder() {
        let config = ClaudeACPConfig::builder()
            .url("ws://test:8080")
            .timeout(60)
            .max_retries(5)
            .build();

        assert_eq!(config.url, "ws://test:8080");
        assert_eq!(config.timeout, 60);
        assert_eq!(config.max_retries, 5);
    }

  ./crates/ccswarm/src/acp_claude/adapter.rs:166-171 method test_connection <-> ./crates/ccswarm/src/acp_claude/adapter.rs:202-212 function test_config_builder
  Similarity: 82.78%

[36m--- ./crates/ccswarm/src/acp_claude/adapter.rs:test_connection (lines 166-171) ---[0m
    pub async fn test_connection(&mut self) -> ACPResult<String> {
        self.connect().await?;
        let result = self.send_task("echo test").await?;
        self.disconnect().await;
        Ok(format!("Connection test successful: {}", result))
    }

[36m--- ./crates/ccswarm/src/acp_claude/adapter.rs:test_config_builder (lines 202-212) ---[0m
    async fn test_config_builder() {
        let config = ClaudeACPConfig::builder()
            .url("ws://test:8080")
            .timeout(60)
            .max_retries(5)
            .build();

        assert_eq!(config.url, "ws://test:8080");
        assert_eq!(config.timeout, 60);
        assert_eq!(config.max_retries, 5);
    }

  ./crates/ccswarm/src/acp_claude/adapter.rs:157-163 method disconnect <-> ./crates/ccswarm/src/acp_claude/adapter.rs:166-171 method test_connection
  Similarity: 76.91%
  Classes: SimplifiedClaudeAdapter <-> SimplifiedClaudeAdapter

[36m--- ./crates/ccswarm/src/acp_claude/adapter.rs:disconnect (lines 157-163) ---[0m
    pub async fn disconnect(&mut self) {
        self.ws_stream = None;
        self.session_id = None;
        self.connected = false;

        info!("👋 Disconnected from Claude Code");
    }

[36m--- ./crates/ccswarm/src/acp_claude/adapter.rs:test_connection (lines 166-171) ---[0m
    pub async fn test_connection(&mut self) -> ACPResult<String> {
        self.connect().await?;
        let result = self.send_task("echo test").await?;
        self.disconnect().await;
        Ok(format!("Connection test successful: {}", result))
    }

  ./crates/ccswarm/src/acp_claude/adapter.rs:166-171 method test_connection <-> ./crates/ccswarm/src/acp_claude/adapter.rs:194-199 function test_adapter_creation
  Similarity: 79.85%

[36m--- ./crates/ccswarm/src/acp_claude/adapter.rs:test_connection (lines 166-171) ---[0m
    pub async fn test_connection(&mut self) -> ACPResult<String> {
        self.connect().await?;
        let result = self.send_task("echo test").await?;
        self.disconnect().await;
        Ok(format!("Connection test successful: {}", result))
    }

[36m--- ./crates/ccswarm/src/acp_claude/adapter.rs:test_adapter_creation (lines 194-199) ---[0m
    async fn test_adapter_creation() {
        let config = ClaudeACPConfig::default();
        let adapter = SimplifiedClaudeAdapter::new(config);
        assert!(!adapter.is_connected());
        assert!(adapter.session_id().is_none());
    }


Duplicates in ./crates/ccswarm/src/cli/progress.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/cli/progress.rs:168-208 method complete <-> ./crates/ccswarm/src/cli/progress.rs:255-296 method display
  Similarity: 88.10%
  Classes: ProgressTracker <-> ProcessTracker

[36m--- ./crates/ccswarm/src/cli/progress.rs:complete (lines 168-208) ---[0m
    pub async fn complete(tracker: Arc<Mutex<Self>>, success: bool, final_message: Option<String>) {
        let (message, elapsed, _is_running) = {
            let t = tracker.lock().await;
            *t.is_running.lock().await = false;
            (
                t.message.clone(),
                t.start_time.elapsed(),
                Arc::clone(&t.is_running),
            )
        };

        // Wait a bit for the spinner to stop
        tokio::time::sleep(Duration::from_millis(150)).await;

        // Clear line and show final status
        print!("\r\x1B[K");

        if success {
            println!(
                "{} {} {}",
                "✅".bright_green(),
                message.white(),
                format!("(completed in {:.1}s)", elapsed.as_secs_f32()).bright_green()
            );

            if let Some(msg) = final_message {
                println!("   {}", msg.bright_white());
            }
        } else {
            println!(
                "{} {} {}",
                "❌".bright_red(),
                message.white(),
                format!("(failed after {:.1}s)", elapsed.as_secs_f32()).red()
            );

            if let Some(msg) = final_message {
                println!("   {}", msg.bright_red());
            }
        }
    }

[36m--- ./crates/ccswarm/src/cli/progress.rs:display (lines 255-296) ---[0m
    pub fn display(&self) {
        println!();
        println!("🚀 {}", self.title.bright_cyan().bold());
        println!();

        for step in self.steps.iter() {
            let icon = match step.status {
                StepStatus::Pending => "○".dimmed(),
                StepStatus::Running => "◉".bright_yellow(),
                StepStatus::Completed => "✓".bright_green(),
                StepStatus::Failed => "✗".bright_red(),
                StepStatus::Skipped => "⊘".dimmed(),
            };

            let name = match step.status {
                StepStatus::Running => step.name.bright_yellow(),
                StepStatus::Completed => step.name.bright_green(),
                StepStatus::Failed => step.name.bright_red(),
                _ => step.name.normal(),
            };

            print!("  {} {}", icon, name);

            if let Some(duration) = step.duration {
                print!(" {}", format!("({:.1}s)", duration.as_secs_f32()).dimmed());
            }

            if let Some(msg) = &step.message {
                print!(" - {}", msg.dimmed());
            }

            println!();
        }

        let elapsed = self.start_time.elapsed();
        println!();
        println!(
            "  {} Elapsed: {:.1}s",
            "⏱".bright_cyan(),
            elapsed.as_secs_f32()
        );
    }

  ./crates/ccswarm/src/cli/progress.rs:168-208 method complete <-> ./crates/ccswarm/src/cli/progress.rs:347-360 function wait_with_message
  Similarity: 81.56%

[36m--- ./crates/ccswarm/src/cli/progress.rs:complete (lines 168-208) ---[0m
    pub async fn complete(tracker: Arc<Mutex<Self>>, success: bool, final_message: Option<String>) {
        let (message, elapsed, _is_running) = {
            let t = tracker.lock().await;
            *t.is_running.lock().await = false;
            (
                t.message.clone(),
                t.start_time.elapsed(),
                Arc::clone(&t.is_running),
            )
        };

        // Wait a bit for the spinner to stop
        tokio::time::sleep(Duration::from_millis(150)).await;

        // Clear line and show final status
        print!("\r\x1B[K");

        if success {
            println!(
                "{} {} {}",
                "✅".bright_green(),
                message.white(),
                format!("(completed in {:.1}s)", elapsed.as_secs_f32()).bright_green()
            );

            if let Some(msg) = final_message {
                println!("   {}", msg.bright_white());
            }
        } else {
            println!(
                "{} {} {}",
                "❌".bright_red(),
                message.white(),
                format!("(failed after {:.1}s)", elapsed.as_secs_f32()).red()
            );

            if let Some(msg) = final_message {
                println!("   {}", msg.bright_red());
            }
        }
    }

[36m--- ./crates/ccswarm/src/cli/progress.rs:wait_with_message (lines 347-360) ---[0m
pub async fn wait_with_message(message: impl Into<String>) -> tokio::task::JoinHandle<()> {
    let msg = message.into();
    tokio::spawn(async move {
        let frames = ["   ", ".  ", ".. ", "..."];
        let mut i = 0;

        loop {
            print!("\r{} {}", msg, frames[i].bright_cyan());
            let _ = io::stdout().flush();
            i = (i + 1) % frames.len();
            tokio::time::sleep(Duration::from_millis(500)).await;
        }
    })
}

  ./crates/ccswarm/src/cli/progress.rs:236-253 method new <-> ./crates/ccswarm/src/cli/progress.rs:347-360 function wait_with_message
  Similarity: 89.38%

[36m--- ./crates/ccswarm/src/cli/progress.rs:new (lines 236-253) ---[0m
    pub fn new(title: impl Into<String>, steps: Vec<String>) -> Self {
        let process_steps = steps
            .into_iter()
            .map(|name| ProcessStep {
                name,
                status: StepStatus::Pending,
                duration: None,
                message: None,
            })
            .collect();

        Self {
            title: title.into(),
            steps: process_steps,
            current_step: 0,
            start_time: Instant::now(),
        }
    }

[36m--- ./crates/ccswarm/src/cli/progress.rs:wait_with_message (lines 347-360) ---[0m
pub async fn wait_with_message(message: impl Into<String>) -> tokio::task::JoinHandle<()> {
    let msg = message.into();
    tokio::spawn(async move {
        let frames = ["   ", ".  ", ".. ", "..."];
        let mut i = 0;

        loop {
            print!("\r{} {}", msg, frames[i].bright_cyan());
            let _ = io::stdout().flush();
            i = (i + 1) % frames.len();
            tokio::time::sleep(Duration::from_millis(500)).await;
        }
    })
}

  ./crates/ccswarm/src/cli/progress.rs:39-50 method with_steps <-> ./crates/ccswarm/src/cli/progress.rs:236-253 method new
  Similarity: 83.85%
  Classes: ProgressTracker <-> ProcessTracker

[36m--- ./crates/ccswarm/src/cli/progress.rs:with_steps (lines 39-50) ---[0m
    pub fn with_steps(message: impl Into<String>, total: usize) -> Arc<Mutex<Self>> {
        let tracker = Self {
            message: message.into(),
            style: ProgressStyle::Steps,
            start_time: Instant::now(),
            current_step: 0,
            total_steps: Some(total),
            sub_messages: Vec::new(),
            is_running: Arc::new(Mutex::new(true)),
        };
        Arc::new(Mutex::new(tracker))
    }

[36m--- ./crates/ccswarm/src/cli/progress.rs:new (lines 236-253) ---[0m
    pub fn new(title: impl Into<String>, steps: Vec<String>) -> Self {
        let process_steps = steps
            .into_iter()
            .map(|name| ProcessStep {
                name,
                status: StepStatus::Pending,
                duration: None,
                message: None,
            })
            .collect();

        Self {
            title: title.into(),
            steps: process_steps,
            current_step: 0,
            start_time: Instant::now(),
        }
    }

  ./crates/ccswarm/src/cli/progress.rs:39-50 method with_steps <-> ./crates/ccswarm/src/cli/progress.rs:347-360 function wait_with_message
  Similarity: 94.47%

[36m--- ./crates/ccswarm/src/cli/progress.rs:with_steps (lines 39-50) ---[0m
    pub fn with_steps(message: impl Into<String>, total: usize) -> Arc<Mutex<Self>> {
        let tracker = Self {
            message: message.into(),
            style: ProgressStyle::Steps,
            start_time: Instant::now(),
            current_step: 0,
            total_steps: Some(total),
            sub_messages: Vec::new(),
            is_running: Arc::new(Mutex::new(true)),
        };
        Arc::new(Mutex::new(tracker))
    }

[36m--- ./crates/ccswarm/src/cli/progress.rs:wait_with_message (lines 347-360) ---[0m
pub async fn wait_with_message(message: impl Into<String>) -> tokio::task::JoinHandle<()> {
    let msg = message.into();
    tokio::spawn(async move {
        let frames = ["   ", ".  ", ".. ", "..."];
        let mut i = 0;

        loop {
            print!("\r{} {}", msg, frames[i].bright_cyan());
            let _ = io::stdout().flush();
            i = (i + 1) % frames.len();
            tokio::time::sleep(Duration::from_millis(500)).await;
        }
    })
}

  ./crates/ccswarm/src/cli/progress.rs:27-37 method new <-> ./crates/ccswarm/src/cli/progress.rs:347-360 function wait_with_message
  Similarity: 89.07%

[36m--- ./crates/ccswarm/src/cli/progress.rs:new (lines 27-37) ---[0m
    pub fn new(message: impl Into<String>, style: ProgressStyle) -> Arc<Mutex<Self>> {
        Arc::new(Mutex::new(Self {
            message: message.into(),
            style,
            start_time: Instant::now(),
            current_step: 0,
            total_steps: None,
            sub_messages: Vec::new(),
            is_running: Arc::new(Mutex::new(true)),
        }))
    }

[36m--- ./crates/ccswarm/src/cli/progress.rs:wait_with_message (lines 347-360) ---[0m
pub async fn wait_with_message(message: impl Into<String>) -> tokio::task::JoinHandle<()> {
    let msg = message.into();
    tokio::spawn(async move {
        let frames = ["   ", ".  ", ".. ", "..."];
        let mut i = 0;

        loop {
            print!("\r{} {}", msg, frames[i].bright_cyan());
            let _ = io::stdout().flush();
            i = (i + 1) % frames.len();
            tokio::time::sleep(Duration::from_millis(500)).await;
        }
    })
}

  ./crates/ccswarm/src/cli/progress.rs:306-318 method complete_step <-> ./crates/ccswarm/src/cli/progress.rs:347-360 function wait_with_message
  Similarity: 80.87%

[36m--- ./crates/ccswarm/src/cli/progress.rs:complete_step (lines 306-318) ---[0m
    pub fn complete_step(&mut self, index: usize, success: bool, message: Option<String>) {
        if index < self.steps.len() {
            let step = &mut self.steps[index];
            step.status = if success {
                StepStatus::Completed
            } else {
                StepStatus::Failed
            };
            step.duration = Some(self.start_time.elapsed());
            step.message = message;
            self.display();
        }
    }

[36m--- ./crates/ccswarm/src/cli/progress.rs:wait_with_message (lines 347-360) ---[0m
pub async fn wait_with_message(message: impl Into<String>) -> tokio::task::JoinHandle<()> {
    let msg = message.into();
    tokio::spawn(async move {
        let frames = ["   ", ".  ", ".. ", "..."];
        let mut i = 0;

        loop {
            print!("\r{} {}", msg, frames[i].bright_cyan());
            let _ = io::stdout().flush();
            i = (i + 1) % frames.len();
            tokio::time::sleep(Duration::from_millis(500)).await;
        }
    })
}

  ./crates/ccswarm/src/cli/progress.rs:27-37 method new <-> ./crates/ccswarm/src/cli/progress.rs:306-318 method complete_step
  Similarity: 88.69%
  Classes: ProgressTracker <-> ProcessTracker

[36m--- ./crates/ccswarm/src/cli/progress.rs:new (lines 27-37) ---[0m
    pub fn new(message: impl Into<String>, style: ProgressStyle) -> Arc<Mutex<Self>> {
        Arc::new(Mutex::new(Self {
            message: message.into(),
            style,
            start_time: Instant::now(),
            current_step: 0,
            total_steps: None,
            sub_messages: Vec::new(),
            is_running: Arc::new(Mutex::new(true)),
        }))
    }

[36m--- ./crates/ccswarm/src/cli/progress.rs:complete_step (lines 306-318) ---[0m
    pub fn complete_step(&mut self, index: usize, success: bool, message: Option<String>) {
        if index < self.steps.len() {
            let step = &mut self.steps[index];
            step.status = if success {
                StepStatus::Completed
            } else {
                StepStatus::Failed
            };
            step.duration = Some(self.start_time.elapsed());
            step.message = message;
            self.display();
        }
    }

  ./crates/ccswarm/src/cli/progress.rs:306-318 method complete_step <-> ./crates/ccswarm/src/cli/progress.rs:320-327 method skip_step
  Similarity: 90.84%
  Classes: ProcessTracker <-> ProcessTracker

[36m--- ./crates/ccswarm/src/cli/progress.rs:complete_step (lines 306-318) ---[0m
    pub fn complete_step(&mut self, index: usize, success: bool, message: Option<String>) {
        if index < self.steps.len() {
            let step = &mut self.steps[index];
            step.status = if success {
                StepStatus::Completed
            } else {
                StepStatus::Failed
            };
            step.duration = Some(self.start_time.elapsed());
            step.message = message;
            self.display();
        }
    }

[36m--- ./crates/ccswarm/src/cli/progress.rs:skip_step (lines 320-327) ---[0m
    pub fn skip_step(&mut self, index: usize, reason: String) {
        if index < self.steps.len() {
            let step = &mut self.steps[index];
            step.status = StepStatus::Skipped;
            step.message = Some(reason);
            self.display();
        }
    }

  ./crates/ccswarm/src/cli/progress.rs:161-166 method add_message <-> ./crates/ccswarm/src/cli/progress.rs:236-253 method new
  Similarity: 77.14%
  Classes: ProgressTracker <-> ProcessTracker

[36m--- ./crates/ccswarm/src/cli/progress.rs:add_message (lines 161-166) ---[0m
    pub async fn add_message(&mut self, msg: impl Into<String>) {
        self.sub_messages.push(msg.into());
        if self.sub_messages.len() > 5 {
            self.sub_messages.remove(0);
        }
    }

[36m--- ./crates/ccswarm/src/cli/progress.rs:new (lines 236-253) ---[0m
    pub fn new(title: impl Into<String>, steps: Vec<String>) -> Self {
        let process_steps = steps
            .into_iter()
            .map(|name| ProcessStep {
                name,
                status: StepStatus::Pending,
                duration: None,
                message: None,
            })
            .collect();

        Self {
            title: title.into(),
            steps: process_steps,
            current_step: 0,
            start_time: Instant::now(),
        }
    }

  ./crates/ccswarm/src/cli/progress.rs:27-37 method new <-> ./crates/ccswarm/src/cli/progress.rs:39-50 method with_steps
  Similarity: 78.07%
  Classes: ProgressTracker <-> ProgressTracker

[36m--- ./crates/ccswarm/src/cli/progress.rs:new (lines 27-37) ---[0m
    pub fn new(message: impl Into<String>, style: ProgressStyle) -> Arc<Mutex<Self>> {
        Arc::new(Mutex::new(Self {
            message: message.into(),
            style,
            start_time: Instant::now(),
            current_step: 0,
            total_steps: None,
            sub_messages: Vec::new(),
            is_running: Arc::new(Mutex::new(true)),
        }))
    }

[36m--- ./crates/ccswarm/src/cli/progress.rs:with_steps (lines 39-50) ---[0m
    pub fn with_steps(message: impl Into<String>, total: usize) -> Arc<Mutex<Self>> {
        let tracker = Self {
            message: message.into(),
            style: ProgressStyle::Steps,
            start_time: Instant::now(),
            current_step: 0,
            total_steps: Some(total),
            sub_messages: Vec::new(),
            is_running: Arc::new(Mutex::new(true)),
        };
        Arc::new(Mutex::new(tracker))
    }

  ./crates/ccswarm/src/cli/progress.rs:298-304 method start_step <-> ./crates/ccswarm/src/cli/progress.rs:306-318 method complete_step
  Similarity: 87.76%
  Classes: ProcessTracker <-> ProcessTracker

[36m--- ./crates/ccswarm/src/cli/progress.rs:start_step (lines 298-304) ---[0m
    pub fn start_step(&mut self, index: usize) {
        if index < self.steps.len() {
            self.steps[index].status = StepStatus::Running;
            self.current_step = index;
            self.display();
        }
    }

[36m--- ./crates/ccswarm/src/cli/progress.rs:complete_step (lines 306-318) ---[0m
    pub fn complete_step(&mut self, index: usize, success: bool, message: Option<String>) {
        if index < self.steps.len() {
            let step = &mut self.steps[index];
            step.status = if success {
                StepStatus::Completed
            } else {
                StepStatus::Failed
            };
            step.duration = Some(self.start_time.elapsed());
            step.message = message;
            self.display();
        }
    }

  ./crates/ccswarm/src/cli/progress.rs:27-37 method new <-> ./crates/ccswarm/src/cli/progress.rs:320-327 method skip_step
  Similarity: 86.99%
  Classes: ProgressTracker <-> ProcessTracker

[36m--- ./crates/ccswarm/src/cli/progress.rs:new (lines 27-37) ---[0m
    pub fn new(message: impl Into<String>, style: ProgressStyle) -> Arc<Mutex<Self>> {
        Arc::new(Mutex::new(Self {
            message: message.into(),
            style,
            start_time: Instant::now(),
            current_step: 0,
            total_steps: None,
            sub_messages: Vec::new(),
            is_running: Arc::new(Mutex::new(true)),
        }))
    }

[36m--- ./crates/ccswarm/src/cli/progress.rs:skip_step (lines 320-327) ---[0m
    pub fn skip_step(&mut self, index: usize, reason: String) {
        if index < self.steps.len() {
            let step = &mut self.steps[index];
            step.status = StepStatus::Skipped;
            step.message = Some(reason);
            self.display();
        }
    }

  ./crates/ccswarm/src/cli/progress.rs:27-37 method new <-> ./crates/ccswarm/src/cli/progress.rs:298-304 method start_step
  Similarity: 86.99%
  Classes: ProgressTracker <-> ProcessTracker

[36m--- ./crates/ccswarm/src/cli/progress.rs:new (lines 27-37) ---[0m
    pub fn new(message: impl Into<String>, style: ProgressStyle) -> Arc<Mutex<Self>> {
        Arc::new(Mutex::new(Self {
            message: message.into(),
            style,
            start_time: Instant::now(),
            current_step: 0,
            total_steps: None,
            sub_messages: Vec::new(),
            is_running: Arc::new(Mutex::new(true)),
        }))
    }

[36m--- ./crates/ccswarm/src/cli/progress.rs:start_step (lines 298-304) ---[0m
    pub fn start_step(&mut self, index: usize) {
        if index < self.steps.len() {
            self.steps[index].status = StepStatus::Running;
            self.current_step = index;
            self.display();
        }
    }

  ./crates/ccswarm/src/cli/progress.rs:39-50 method with_steps <-> ./crates/ccswarm/src/cli/progress.rs:161-166 method add_message
  Similarity: 84.59%
  Classes: ProgressTracker <-> ProgressTracker

[36m--- ./crates/ccswarm/src/cli/progress.rs:with_steps (lines 39-50) ---[0m
    pub fn with_steps(message: impl Into<String>, total: usize) -> Arc<Mutex<Self>> {
        let tracker = Self {
            message: message.into(),
            style: ProgressStyle::Steps,
            start_time: Instant::now(),
            current_step: 0,
            total_steps: Some(total),
            sub_messages: Vec::new(),
            is_running: Arc::new(Mutex::new(true)),
        };
        Arc::new(Mutex::new(tracker))
    }

[36m--- ./crates/ccswarm/src/cli/progress.rs:add_message (lines 161-166) ---[0m
    pub async fn add_message(&mut self, msg: impl Into<String>) {
        self.sub_messages.push(msg.into());
        if self.sub_messages.len() > 5 {
            self.sub_messages.remove(0);
        }
    }

  ./crates/ccswarm/src/cli/progress.rs:298-304 method start_step <-> ./crates/ccswarm/src/cli/progress.rs:320-327 method skip_step
  Similarity: 93.29%
  Classes: ProcessTracker <-> ProcessTracker

[36m--- ./crates/ccswarm/src/cli/progress.rs:start_step (lines 298-304) ---[0m
    pub fn start_step(&mut self, index: usize) {
        if index < self.steps.len() {
            self.steps[index].status = StepStatus::Running;
            self.current_step = index;
            self.display();
        }
    }

[36m--- ./crates/ccswarm/src/cli/progress.rs:skip_step (lines 320-327) ---[0m
    pub fn skip_step(&mut self, index: usize, reason: String) {
        if index < self.steps.len() {
            let step = &mut self.steps[index];
            step.status = StepStatus::Skipped;
            step.message = Some(reason);
            self.display();
        }
    }

  ./crates/ccswarm/src/cli/progress.rs:334-337 method update <-> ./crates/ccswarm/src/cli/progress.rs:339-342 method complete
  Similarity: 85.00%
  Classes: StatusLine <-> StatusLine

[36m--- ./crates/ccswarm/src/cli/progress.rs:update (lines 334-337) ---[0m
    pub fn update(message: impl AsRef<str>) {
        print!("\r\x1B[K{}", message.as_ref());
        let _ = io::stdout().flush();
    }

[36m--- ./crates/ccswarm/src/cli/progress.rs:complete (lines 339-342) ---[0m
    pub fn complete() {
        print!("\r\x1B[K");
        let _ = io::stdout().flush();
    }


Duplicates in ./crates/ccswarm/src/subagent/dynamic_generation.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/subagent/dynamic_generation.rs:224-273 method analyze_and_suggest <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:276-318 method generate_from_request
  Similarity: 72.94%
  Classes: DynamicSubagentGenerator <-> DynamicSubagentGenerator

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:analyze_and_suggest (lines 224-273) ---[0m
    pub async fn analyze_and_suggest(
        &self,
        project_path: &Path,
    ) -> SubagentResult<Vec<GenerationRequest>> {
        let mut suggestions = Vec::new();

        // Analyze the project structure
        let context = self.analyze_project(project_path).await?;

        // Suggest framework specialists
        for framework in &context.frameworks {
            suggestions.push(GenerationRequest {
                agent_type: AgentType::FrameworkSpecialist,
                technologies: vec![framework.clone()],
                project_context: context.clone(),
                requirements: vec![
                    format!("Deep knowledge of {}", framework),
                    format!("{} best practices", framework),
                ],
            });
        }

        // Suggest language expert if complex
        if context.complexity.avg_complexity > 10.0 {
            suggestions.push(GenerationRequest {
                agent_type: AgentType::LanguageExpert,
                technologies: vec![context.primary_language.clone()],
                project_context: context.clone(),
                requirements: vec![
                    "Advanced language features".to_string(),
                    "Performance optimization".to_string(),
                ],
            });
        }

        // Suggest security specialist if low coverage
        if context.complexity.test_coverage < 50.0 {
            suggestions.push(GenerationRequest {
                agent_type: AgentType::SecuritySpecialist,
                technologies: vec![],
                project_context: context.clone(),
                requirements: vec![
                    "Security auditing".to_string(),
                    "Vulnerability detection".to_string(),
                ],
            });
        }

        Ok(suggestions)
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_from_request (lines 276-318) ---[0m
    pub fn generate_from_request(
        &self,
        request: &GenerationRequest,
    ) -> SubagentResult<(SubagentDefinition, String)> {
        let template = self.get_template_for_type(&request.agent_type)?;

        // Generate name
        let name = self.generate_name(template, request);

        // Generate description
        let description = self.generate_description(template, request);

        // Generate tools
        let tools = self.generate_tools(template, request);

        // Generate capabilities
        let capabilities = self.generate_capabilities(template, request);

        // Generate instructions
        let instructions = self.generate_instructions(template, request);

        // Create metadata
        let mut metadata = HashMap::new();
        metadata.insert("generated".to_string(), serde_json::json!(true));
        metadata.insert(
            "generation_type".to_string(),
            serde_json::json!(format!("{:?}", request.agent_type)),
        );
        metadata.insert(
            "technologies".to_string(),
            serde_json::json!(request.technologies),
        );

        let definition = SubagentDefinition {
            name,
            description,
            tools,
            capabilities,
            metadata,
        };

        Ok((definition, instructions))
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:224-273 method analyze_and_suggest <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:429-451 method generate_instructions
  Similarity: 91.19%
  Classes: DynamicSubagentGenerator <-> DynamicSubagentGenerator

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:analyze_and_suggest (lines 224-273) ---[0m
    pub async fn analyze_and_suggest(
        &self,
        project_path: &Path,
    ) -> SubagentResult<Vec<GenerationRequest>> {
        let mut suggestions = Vec::new();

        // Analyze the project structure
        let context = self.analyze_project(project_path).await?;

        // Suggest framework specialists
        for framework in &context.frameworks {
            suggestions.push(GenerationRequest {
                agent_type: AgentType::FrameworkSpecialist,
                technologies: vec![framework.clone()],
                project_context: context.clone(),
                requirements: vec![
                    format!("Deep knowledge of {}", framework),
                    format!("{} best practices", framework),
                ],
            });
        }

        // Suggest language expert if complex
        if context.complexity.avg_complexity > 10.0 {
            suggestions.push(GenerationRequest {
                agent_type: AgentType::LanguageExpert,
                technologies: vec![context.primary_language.clone()],
                project_context: context.clone(),
                requirements: vec![
                    "Advanced language features".to_string(),
                    "Performance optimization".to_string(),
                ],
            });
        }

        // Suggest security specialist if low coverage
        if context.complexity.test_coverage < 50.0 {
            suggestions.push(GenerationRequest {
                agent_type: AgentType::SecuritySpecialist,
                technologies: vec![],
                project_context: context.clone(),
                requirements: vec![
                    "Security auditing".to_string(),
                    "Vulnerability detection".to_string(),
                ],
            });
        }

        Ok(suggestions)
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_instructions (lines 429-451) ---[0m
    fn generate_instructions(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> String {
        let mut instructions = template.instructions_template.clone();

        // Replace placeholders
        if !request.technologies.is_empty() {
            instructions = instructions.replace("{tech}", &request.technologies[0]);
            instructions = instructions.replace("{lang}", &request.technologies[0]);
        }

        // Add specific requirements
        if !request.requirements.is_empty() {
            instructions.push_str("\n\n## Specific Requirements\n\n");
            for req in &request.requirements {
                instructions.push_str(&format!("- {}\n", req));
            }
        }

        instructions
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:224-273 method analyze_and_suggest <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:518-545 function test_tools_generation
  Similarity: 84.18%

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:analyze_and_suggest (lines 224-273) ---[0m
    pub async fn analyze_and_suggest(
        &self,
        project_path: &Path,
    ) -> SubagentResult<Vec<GenerationRequest>> {
        let mut suggestions = Vec::new();

        // Analyze the project structure
        let context = self.analyze_project(project_path).await?;

        // Suggest framework specialists
        for framework in &context.frameworks {
            suggestions.push(GenerationRequest {
                agent_type: AgentType::FrameworkSpecialist,
                technologies: vec![framework.clone()],
                project_context: context.clone(),
                requirements: vec![
                    format!("Deep knowledge of {}", framework),
                    format!("{} best practices", framework),
                ],
            });
        }

        // Suggest language expert if complex
        if context.complexity.avg_complexity > 10.0 {
            suggestions.push(GenerationRequest {
                agent_type: AgentType::LanguageExpert,
                technologies: vec![context.primary_language.clone()],
                project_context: context.clone(),
                requirements: vec![
                    "Advanced language features".to_string(),
                    "Performance optimization".to_string(),
                ],
            });
        }

        // Suggest security specialist if low coverage
        if context.complexity.test_coverage < 50.0 {
            suggestions.push(GenerationRequest {
                agent_type: AgentType::SecuritySpecialist,
                technologies: vec![],
                project_context: context.clone(),
                requirements: vec![
                    "Security auditing".to_string(),
                    "Vulnerability detection".to_string(),
                ],
            });
        }

        Ok(suggestions)
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:test_tools_generation (lines 518-545) ---[0m
    fn test_tools_generation() {
        let generator = DynamicSubagentGenerator::new();
        let template = generator.templates.get("framework").unwrap();

        let request = GenerationRequest {
            agent_type: AgentType::FrameworkSpecialist,
            technologies: vec!["Vue".to_string()],
            project_context: ProjectContext {
                primary_language: "JavaScript".to_string(),
                frameworks: vec!["Vue".to_string()],
                project_type: "web".to_string(),
                complexity: ComplexityMetrics {
                    lines_of_code: 3000,
                    module_count: 15,
                    avg_complexity: 4.0,
                    test_coverage: 70.0,
                },
            },
            requirements: vec![
                "performance optimization".to_string(),
                "testing frameworks".to_string(),
            ],
        };

        let tools = generator.generate_tools(template, &request);
        assert!(tools.custom.contains(&"performance_profiler".to_string()));
        assert!(tools.custom.contains(&"test_generator".to_string()));
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:224-273 method analyze_and_suggest <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:492-515 function test_name_generation
  Similarity: 80.95%

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:analyze_and_suggest (lines 224-273) ---[0m
    pub async fn analyze_and_suggest(
        &self,
        project_path: &Path,
    ) -> SubagentResult<Vec<GenerationRequest>> {
        let mut suggestions = Vec::new();

        // Analyze the project structure
        let context = self.analyze_project(project_path).await?;

        // Suggest framework specialists
        for framework in &context.frameworks {
            suggestions.push(GenerationRequest {
                agent_type: AgentType::FrameworkSpecialist,
                technologies: vec![framework.clone()],
                project_context: context.clone(),
                requirements: vec![
                    format!("Deep knowledge of {}", framework),
                    format!("{} best practices", framework),
                ],
            });
        }

        // Suggest language expert if complex
        if context.complexity.avg_complexity > 10.0 {
            suggestions.push(GenerationRequest {
                agent_type: AgentType::LanguageExpert,
                technologies: vec![context.primary_language.clone()],
                project_context: context.clone(),
                requirements: vec![
                    "Advanced language features".to_string(),
                    "Performance optimization".to_string(),
                ],
            });
        }

        // Suggest security specialist if low coverage
        if context.complexity.test_coverage < 50.0 {
            suggestions.push(GenerationRequest {
                agent_type: AgentType::SecuritySpecialist,
                technologies: vec![],
                project_context: context.clone(),
                requirements: vec![
                    "Security auditing".to_string(),
                    "Vulnerability detection".to_string(),
                ],
            });
        }

        Ok(suggestions)
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:test_name_generation (lines 492-515) ---[0m
    fn test_name_generation() {
        let generator = DynamicSubagentGenerator::new();
        let template = generator.templates.get("framework").unwrap();

        let request = GenerationRequest {
            agent_type: AgentType::FrameworkSpecialist,
            technologies: vec!["React".to_string()],
            project_context: ProjectContext {
                primary_language: "TypeScript".to_string(),
                frameworks: vec!["React".to_string()],
                project_type: "web".to_string(),
                complexity: ComplexityMetrics {
                    lines_of_code: 5000,
                    module_count: 20,
                    avg_complexity: 5.0,
                    test_coverage: 80.0,
                },
            },
            requirements: vec![],
        };

        let name = generator.generate_name(template, &request);
        assert_eq!(name, "react-specialist");
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:224-273 method analyze_and_suggest <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:345-360 method generate_name
  Similarity: 78.61%
  Classes: DynamicSubagentGenerator <-> DynamicSubagentGenerator

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:analyze_and_suggest (lines 224-273) ---[0m
    pub async fn analyze_and_suggest(
        &self,
        project_path: &Path,
    ) -> SubagentResult<Vec<GenerationRequest>> {
        let mut suggestions = Vec::new();

        // Analyze the project structure
        let context = self.analyze_project(project_path).await?;

        // Suggest framework specialists
        for framework in &context.frameworks {
            suggestions.push(GenerationRequest {
                agent_type: AgentType::FrameworkSpecialist,
                technologies: vec![framework.clone()],
                project_context: context.clone(),
                requirements: vec![
                    format!("Deep knowledge of {}", framework),
                    format!("{} best practices", framework),
                ],
            });
        }

        // Suggest language expert if complex
        if context.complexity.avg_complexity > 10.0 {
            suggestions.push(GenerationRequest {
                agent_type: AgentType::LanguageExpert,
                technologies: vec![context.primary_language.clone()],
                project_context: context.clone(),
                requirements: vec![
                    "Advanced language features".to_string(),
                    "Performance optimization".to_string(),
                ],
            });
        }

        // Suggest security specialist if low coverage
        if context.complexity.test_coverage < 50.0 {
            suggestions.push(GenerationRequest {
                agent_type: AgentType::SecuritySpecialist,
                technologies: vec![],
                project_context: context.clone(),
                requirements: vec![
                    "Security auditing".to_string(),
                    "Vulnerability detection".to_string(),
                ],
            });
        }

        Ok(suggestions)
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_name (lines 345-360) ---[0m
    fn generate_name(&self, template: &AgentTemplate, request: &GenerationRequest) -> String {
        let mut name = template.name_pattern.clone();

        if !request.technologies.is_empty() {
            name = name.replace("{tech}", &request.technologies[0].to_lowercase());
            name = name.replace("{lang}", &request.technologies[0].to_lowercase());
        } else {
            name = name.replace("{tech}", "specialized");
            name = name.replace(
                "{lang}",
                &request.project_context.primary_language.to_lowercase(),
            );
        }

        name
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:492-515 function test_name_generation <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:518-545 function test_tools_generation
  Similarity: 87.47%

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:test_name_generation (lines 492-515) ---[0m
    fn test_name_generation() {
        let generator = DynamicSubagentGenerator::new();
        let template = generator.templates.get("framework").unwrap();

        let request = GenerationRequest {
            agent_type: AgentType::FrameworkSpecialist,
            technologies: vec!["React".to_string()],
            project_context: ProjectContext {
                primary_language: "TypeScript".to_string(),
                frameworks: vec!["React".to_string()],
                project_type: "web".to_string(),
                complexity: ComplexityMetrics {
                    lines_of_code: 5000,
                    module_count: 20,
                    avg_complexity: 5.0,
                    test_coverage: 80.0,
                },
            },
            requirements: vec![],
        };

        let name = generator.generate_name(template, &request);
        assert_eq!(name, "react-specialist");
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:test_tools_generation (lines 518-545) ---[0m
    fn test_tools_generation() {
        let generator = DynamicSubagentGenerator::new();
        let template = generator.templates.get("framework").unwrap();

        let request = GenerationRequest {
            agent_type: AgentType::FrameworkSpecialist,
            technologies: vec!["Vue".to_string()],
            project_context: ProjectContext {
                primary_language: "JavaScript".to_string(),
                frameworks: vec!["Vue".to_string()],
                project_type: "web".to_string(),
                complexity: ComplexityMetrics {
                    lines_of_code: 3000,
                    module_count: 15,
                    avg_complexity: 4.0,
                    test_coverage: 70.0,
                },
            },
            requirements: vec![
                "performance optimization".to_string(),
                "testing frameworks".to_string(),
            ],
        };

        let tools = generator.generate_tools(template, &request);
        assert!(tools.custom.contains(&"performance_profiler".to_string()));
        assert!(tools.custom.contains(&"test_generator".to_string()));
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:429-451 method generate_instructions <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:518-545 function test_tools_generation
  Similarity: 84.94%

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_instructions (lines 429-451) ---[0m
    fn generate_instructions(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> String {
        let mut instructions = template.instructions_template.clone();

        // Replace placeholders
        if !request.technologies.is_empty() {
            instructions = instructions.replace("{tech}", &request.technologies[0]);
            instructions = instructions.replace("{lang}", &request.technologies[0]);
        }

        // Add specific requirements
        if !request.requirements.is_empty() {
            instructions.push_str("\n\n## Specific Requirements\n\n");
            for req in &request.requirements {
                instructions.push_str(&format!("- {}\n", req));
            }
        }

        instructions
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:test_tools_generation (lines 518-545) ---[0m
    fn test_tools_generation() {
        let generator = DynamicSubagentGenerator::new();
        let template = generator.templates.get("framework").unwrap();

        let request = GenerationRequest {
            agent_type: AgentType::FrameworkSpecialist,
            technologies: vec!["Vue".to_string()],
            project_context: ProjectContext {
                primary_language: "JavaScript".to_string(),
                frameworks: vec!["Vue".to_string()],
                project_type: "web".to_string(),
                complexity: ComplexityMetrics {
                    lines_of_code: 3000,
                    module_count: 15,
                    avg_complexity: 4.0,
                    test_coverage: 70.0,
                },
            },
            requirements: vec![
                "performance optimization".to_string(),
                "testing frameworks".to_string(),
            ],
        };

        let tools = generator.generate_tools(template, &request);
        assert!(tools.custom.contains(&"performance_profiler".to_string()));
        assert!(tools.custom.contains(&"test_generator".to_string()));
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:382-403 method generate_tools <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:429-451 method generate_instructions
  Similarity: 95.86%
  Classes: DynamicSubagentGenerator <-> DynamicSubagentGenerator

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_tools (lines 382-403) ---[0m
    fn generate_tools(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> SubagentTools {
        let mut tools = template.base_tools.clone();

        // Add custom tools based on requirements
        for req in &request.requirements {
            if req.contains("performance") {
                tools.custom.push("performance_profiler".to_string());
            }
            if req.contains("testing") {
                tools.custom.push("test_generator".to_string());
            }
            if req.contains("documentation") {
                tools.custom.push("doc_generator".to_string());
            }
        }

        tools
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_instructions (lines 429-451) ---[0m
    fn generate_instructions(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> String {
        let mut instructions = template.instructions_template.clone();

        // Replace placeholders
        if !request.technologies.is_empty() {
            instructions = instructions.replace("{tech}", &request.technologies[0]);
            instructions = instructions.replace("{lang}", &request.technologies[0]);
        }

        // Add specific requirements
        if !request.requirements.is_empty() {
            instructions.push_str("\n\n## Specific Requirements\n\n");
            for req in &request.requirements {
                instructions.push_str(&format!("- {}\n", req));
            }
        }

        instructions
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:406-426 method generate_capabilities <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:429-451 method generate_instructions
  Similarity: 94.94%
  Classes: DynamicSubagentGenerator <-> DynamicSubagentGenerator

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_capabilities (lines 406-426) ---[0m
    fn generate_capabilities(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> Vec<String> {
        let mut capabilities = Vec::new();

        for cap in &template.base_capabilities {
            let mut capability = cap.clone();
            if !request.technologies.is_empty() {
                capability = capability.replace("{tech}", &request.technologies[0]);
                capability = capability.replace("{lang}", &request.technologies[0]);
            }
            capabilities.push(capability);
        }

        // Add specific capabilities from requirements
        capabilities.extend(request.requirements.clone());

        capabilities
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_instructions (lines 429-451) ---[0m
    fn generate_instructions(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> String {
        let mut instructions = template.instructions_template.clone();

        // Replace placeholders
        if !request.technologies.is_empty() {
            instructions = instructions.replace("{tech}", &request.technologies[0]);
            instructions = instructions.replace("{lang}", &request.technologies[0]);
        }

        // Add specific requirements
        if !request.requirements.is_empty() {
            instructions.push_str("\n\n## Specific Requirements\n\n");
            for req in &request.requirements {
                instructions.push_str(&format!("- {}\n", req));
            }
        }

        instructions
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:382-403 method generate_tools <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:406-426 method generate_capabilities
  Similarity: 95.78%
  Classes: DynamicSubagentGenerator <-> DynamicSubagentGenerator

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_tools (lines 382-403) ---[0m
    fn generate_tools(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> SubagentTools {
        let mut tools = template.base_tools.clone();

        // Add custom tools based on requirements
        for req in &request.requirements {
            if req.contains("performance") {
                tools.custom.push("performance_profiler".to_string());
            }
            if req.contains("testing") {
                tools.custom.push("test_generator".to_string());
            }
            if req.contains("documentation") {
                tools.custom.push("doc_generator".to_string());
            }
        }

        tools
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_capabilities (lines 406-426) ---[0m
    fn generate_capabilities(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> Vec<String> {
        let mut capabilities = Vec::new();

        for cap in &template.base_capabilities {
            let mut capability = cap.clone();
            if !request.technologies.is_empty() {
                capability = capability.replace("{tech}", &request.technologies[0]);
                capability = capability.replace("{lang}", &request.technologies[0]);
            }
            capabilities.push(capability);
        }

        // Add specific capabilities from requirements
        capabilities.extend(request.requirements.clone());

        capabilities
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:382-403 method generate_tools <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:518-545 function test_tools_generation
  Similarity: 81.21%

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_tools (lines 382-403) ---[0m
    fn generate_tools(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> SubagentTools {
        let mut tools = template.base_tools.clone();

        // Add custom tools based on requirements
        for req in &request.requirements {
            if req.contains("performance") {
                tools.custom.push("performance_profiler".to_string());
            }
            if req.contains("testing") {
                tools.custom.push("test_generator".to_string());
            }
            if req.contains("documentation") {
                tools.custom.push("doc_generator".to_string());
            }
        }

        tools
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:test_tools_generation (lines 518-545) ---[0m
    fn test_tools_generation() {
        let generator = DynamicSubagentGenerator::new();
        let template = generator.templates.get("framework").unwrap();

        let request = GenerationRequest {
            agent_type: AgentType::FrameworkSpecialist,
            technologies: vec!["Vue".to_string()],
            project_context: ProjectContext {
                primary_language: "JavaScript".to_string(),
                frameworks: vec!["Vue".to_string()],
                project_type: "web".to_string(),
                complexity: ComplexityMetrics {
                    lines_of_code: 3000,
                    module_count: 15,
                    avg_complexity: 4.0,
                    test_coverage: 70.0,
                },
            },
            requirements: vec![
                "performance optimization".to_string(),
                "testing frameworks".to_string(),
            ],
        };

        let tools = generator.generate_tools(template, &request);
        assert!(tools.custom.contains(&"performance_profiler".to_string()));
        assert!(tools.custom.contains(&"test_generator".to_string()));
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:406-426 method generate_capabilities <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:518-545 function test_tools_generation
  Similarity: 82.60%

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_capabilities (lines 406-426) ---[0m
    fn generate_capabilities(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> Vec<String> {
        let mut capabilities = Vec::new();

        for cap in &template.base_capabilities {
            let mut capability = cap.clone();
            if !request.technologies.is_empty() {
                capability = capability.replace("{tech}", &request.technologies[0]);
                capability = capability.replace("{lang}", &request.technologies[0]);
            }
            capabilities.push(capability);
        }

        // Add specific capabilities from requirements
        capabilities.extend(request.requirements.clone());

        capabilities
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:test_tools_generation (lines 518-545) ---[0m
    fn test_tools_generation() {
        let generator = DynamicSubagentGenerator::new();
        let template = generator.templates.get("framework").unwrap();

        let request = GenerationRequest {
            agent_type: AgentType::FrameworkSpecialist,
            technologies: vec!["Vue".to_string()],
            project_context: ProjectContext {
                primary_language: "JavaScript".to_string(),
                frameworks: vec!["Vue".to_string()],
                project_type: "web".to_string(),
                complexity: ComplexityMetrics {
                    lines_of_code: 3000,
                    module_count: 15,
                    avg_complexity: 4.0,
                    test_coverage: 70.0,
                },
            },
            requirements: vec![
                "performance optimization".to_string(),
                "testing frameworks".to_string(),
            ],
        };

        let tools = generator.generate_tools(template, &request);
        assert!(tools.custom.contains(&"performance_profiler".to_string()));
        assert!(tools.custom.contains(&"test_generator".to_string()));
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:406-426 method generate_capabilities <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:492-515 function test_name_generation
  Similarity: 88.50%

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_capabilities (lines 406-426) ---[0m
    fn generate_capabilities(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> Vec<String> {
        let mut capabilities = Vec::new();

        for cap in &template.base_capabilities {
            let mut capability = cap.clone();
            if !request.technologies.is_empty() {
                capability = capability.replace("{tech}", &request.technologies[0]);
                capability = capability.replace("{lang}", &request.technologies[0]);
            }
            capabilities.push(capability);
        }

        // Add specific capabilities from requirements
        capabilities.extend(request.requirements.clone());

        capabilities
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:test_name_generation (lines 492-515) ---[0m
    fn test_name_generation() {
        let generator = DynamicSubagentGenerator::new();
        let template = generator.templates.get("framework").unwrap();

        let request = GenerationRequest {
            agent_type: AgentType::FrameworkSpecialist,
            technologies: vec!["React".to_string()],
            project_context: ProjectContext {
                primary_language: "TypeScript".to_string(),
                frameworks: vec!["React".to_string()],
                project_type: "web".to_string(),
                complexity: ComplexityMetrics {
                    lines_of_code: 5000,
                    module_count: 20,
                    avg_complexity: 5.0,
                    test_coverage: 80.0,
                },
            },
            requirements: vec![],
        };

        let name = generator.generate_name(template, &request);
        assert_eq!(name, "react-specialist");
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:429-451 method generate_instructions <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:492-515 function test_name_generation
  Similarity: 84.11%

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_instructions (lines 429-451) ---[0m
    fn generate_instructions(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> String {
        let mut instructions = template.instructions_template.clone();

        // Replace placeholders
        if !request.technologies.is_empty() {
            instructions = instructions.replace("{tech}", &request.technologies[0]);
            instructions = instructions.replace("{lang}", &request.technologies[0]);
        }

        // Add specific requirements
        if !request.requirements.is_empty() {
            instructions.push_str("\n\n## Specific Requirements\n\n");
            for req in &request.requirements {
                instructions.push_str(&format!("- {}\n", req));
            }
        }

        instructions
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:test_name_generation (lines 492-515) ---[0m
    fn test_name_generation() {
        let generator = DynamicSubagentGenerator::new();
        let template = generator.templates.get("framework").unwrap();

        let request = GenerationRequest {
            agent_type: AgentType::FrameworkSpecialist,
            technologies: vec!["React".to_string()],
            project_context: ProjectContext {
                primary_language: "TypeScript".to_string(),
                frameworks: vec!["React".to_string()],
                project_type: "web".to_string(),
                complexity: ComplexityMetrics {
                    lines_of_code: 5000,
                    module_count: 20,
                    avg_complexity: 5.0,
                    test_coverage: 80.0,
                },
            },
            requirements: vec![],
        };

        let name = generator.generate_name(template, &request);
        assert_eq!(name, "react-specialist");
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:382-403 method generate_tools <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:492-515 function test_name_generation
  Similarity: 83.09%

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_tools (lines 382-403) ---[0m
    fn generate_tools(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> SubagentTools {
        let mut tools = template.base_tools.clone();

        // Add custom tools based on requirements
        for req in &request.requirements {
            if req.contains("performance") {
                tools.custom.push("performance_profiler".to_string());
            }
            if req.contains("testing") {
                tools.custom.push("test_generator".to_string());
            }
            if req.contains("documentation") {
                tools.custom.push("doc_generator".to_string());
            }
        }

        tools
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:test_name_generation (lines 492-515) ---[0m
    fn test_name_generation() {
        let generator = DynamicSubagentGenerator::new();
        let template = generator.templates.get("framework").unwrap();

        let request = GenerationRequest {
            agent_type: AgentType::FrameworkSpecialist,
            technologies: vec!["React".to_string()],
            project_context: ProjectContext {
                primary_language: "TypeScript".to_string(),
                frameworks: vec!["React".to_string()],
                project_type: "web".to_string(),
                complexity: ComplexityMetrics {
                    lines_of_code: 5000,
                    module_count: 20,
                    avg_complexity: 5.0,
                    test_coverage: 80.0,
                },
            },
            requirements: vec![],
        };

        let name = generator.generate_name(template, &request);
        assert_eq!(name, "react-specialist");
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:363-379 method generate_description <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:382-403 method generate_tools
  Similarity: 97.58%
  Classes: DynamicSubagentGenerator <-> DynamicSubagentGenerator

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_description (lines 363-379) ---[0m
    fn generate_description(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> String {
        let mut description = template.description_template.clone();

        if !request.technologies.is_empty() {
            description = description.replace("{tech}", &request.technologies[0]);
            description = description.replace("{lang}", &request.technologies[0]);
        } else {
            description = description.replace("{tech}", "specialized technology");
            description = description.replace("{lang}", &request.project_context.primary_language);
        }

        description
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_tools (lines 382-403) ---[0m
    fn generate_tools(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> SubagentTools {
        let mut tools = template.base_tools.clone();

        // Add custom tools based on requirements
        for req in &request.requirements {
            if req.contains("performance") {
                tools.custom.push("performance_profiler".to_string());
            }
            if req.contains("testing") {
                tools.custom.push("test_generator".to_string());
            }
            if req.contains("documentation") {
                tools.custom.push("doc_generator".to_string());
            }
        }

        tools
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:345-360 method generate_name <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:382-403 method generate_tools
  Similarity: 97.71%
  Classes: DynamicSubagentGenerator <-> DynamicSubagentGenerator

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_name (lines 345-360) ---[0m
    fn generate_name(&self, template: &AgentTemplate, request: &GenerationRequest) -> String {
        let mut name = template.name_pattern.clone();

        if !request.technologies.is_empty() {
            name = name.replace("{tech}", &request.technologies[0].to_lowercase());
            name = name.replace("{lang}", &request.technologies[0].to_lowercase());
        } else {
            name = name.replace("{tech}", "specialized");
            name = name.replace(
                "{lang}",
                &request.project_context.primary_language.to_lowercase(),
            );
        }

        name
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_tools (lines 382-403) ---[0m
    fn generate_tools(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> SubagentTools {
        let mut tools = template.base_tools.clone();

        // Add custom tools based on requirements
        for req in &request.requirements {
            if req.contains("performance") {
                tools.custom.push("performance_profiler".to_string());
            }
            if req.contains("testing") {
                tools.custom.push("test_generator".to_string());
            }
            if req.contains("documentation") {
                tools.custom.push("doc_generator".to_string());
            }
        }

        tools
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:382-403 method generate_tools <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:454-468 method analyze_project
  Similarity: 96.09%
  Classes: DynamicSubagentGenerator <-> DynamicSubagentGenerator

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_tools (lines 382-403) ---[0m
    fn generate_tools(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> SubagentTools {
        let mut tools = template.base_tools.clone();

        // Add custom tools based on requirements
        for req in &request.requirements {
            if req.contains("performance") {
                tools.custom.push("performance_profiler".to_string());
            }
            if req.contains("testing") {
                tools.custom.push("test_generator".to_string());
            }
            if req.contains("documentation") {
                tools.custom.push("doc_generator".to_string());
            }
        }

        tools
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:analyze_project (lines 454-468) ---[0m
    async fn analyze_project(&self, _project_path: &Path) -> SubagentResult<ProjectContext> {
        // This would integrate with semantic analyzer in real implementation
        // For now, return mock data
        Ok(ProjectContext {
            primary_language: "Rust".to_string(),
            frameworks: vec!["tokio".to_string(), "serde".to_string()],
            project_type: "library".to_string(),
            complexity: ComplexityMetrics {
                lines_of_code: 10000,
                module_count: 50,
                avg_complexity: 8.5,
                test_coverage: 75.0,
            },
        })
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:363-379 method generate_description <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:429-451 method generate_instructions
  Similarity: 85.23%
  Classes: DynamicSubagentGenerator <-> DynamicSubagentGenerator

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_description (lines 363-379) ---[0m
    fn generate_description(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> String {
        let mut description = template.description_template.clone();

        if !request.technologies.is_empty() {
            description = description.replace("{tech}", &request.technologies[0]);
            description = description.replace("{lang}", &request.technologies[0]);
        } else {
            description = description.replace("{tech}", "specialized technology");
            description = description.replace("{lang}", &request.project_context.primary_language);
        }

        description
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_instructions (lines 429-451) ---[0m
    fn generate_instructions(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> String {
        let mut instructions = template.instructions_template.clone();

        // Replace placeholders
        if !request.technologies.is_empty() {
            instructions = instructions.replace("{tech}", &request.technologies[0]);
            instructions = instructions.replace("{lang}", &request.technologies[0]);
        }

        // Add specific requirements
        if !request.requirements.is_empty() {
            instructions.push_str("\n\n## Specific Requirements\n\n");
            for req in &request.requirements {
                instructions.push_str(&format!("- {}\n", req));
            }
        }

        instructions
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:406-426 method generate_capabilities <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:454-468 method analyze_project
  Similarity: 94.38%
  Classes: DynamicSubagentGenerator <-> DynamicSubagentGenerator

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_capabilities (lines 406-426) ---[0m
    fn generate_capabilities(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> Vec<String> {
        let mut capabilities = Vec::new();

        for cap in &template.base_capabilities {
            let mut capability = cap.clone();
            if !request.technologies.is_empty() {
                capability = capability.replace("{tech}", &request.technologies[0]);
                capability = capability.replace("{lang}", &request.technologies[0]);
            }
            capabilities.push(capability);
        }

        // Add specific capabilities from requirements
        capabilities.extend(request.requirements.clone());

        capabilities
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:analyze_project (lines 454-468) ---[0m
    async fn analyze_project(&self, _project_path: &Path) -> SubagentResult<ProjectContext> {
        // This would integrate with semantic analyzer in real implementation
        // For now, return mock data
        Ok(ProjectContext {
            primary_language: "Rust".to_string(),
            frameworks: vec!["tokio".to_string(), "serde".to_string()],
            project_type: "library".to_string(),
            complexity: ComplexityMetrics {
                lines_of_code: 10000,
                module_count: 50,
                avg_complexity: 8.5,
                test_coverage: 75.0,
            },
        })
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:345-360 method generate_name <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:429-451 method generate_instructions
  Similarity: 85.23%
  Classes: DynamicSubagentGenerator <-> DynamicSubagentGenerator

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_name (lines 345-360) ---[0m
    fn generate_name(&self, template: &AgentTemplate, request: &GenerationRequest) -> String {
        let mut name = template.name_pattern.clone();

        if !request.technologies.is_empty() {
            name = name.replace("{tech}", &request.technologies[0].to_lowercase());
            name = name.replace("{lang}", &request.technologies[0].to_lowercase());
        } else {
            name = name.replace("{tech}", "specialized");
            name = name.replace(
                "{lang}",
                &request.project_context.primary_language.to_lowercase(),
            );
        }

        name
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_instructions (lines 429-451) ---[0m
    fn generate_instructions(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> String {
        let mut instructions = template.instructions_template.clone();

        // Replace placeholders
        if !request.technologies.is_empty() {
            instructions = instructions.replace("{tech}", &request.technologies[0]);
            instructions = instructions.replace("{lang}", &request.technologies[0]);
        }

        // Add specific requirements
        if !request.requirements.is_empty() {
            instructions.push_str("\n\n## Specific Requirements\n\n");
            for req in &request.requirements {
                instructions.push_str(&format!("- {}\n", req));
            }
        }

        instructions
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:429-451 method generate_instructions <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:454-468 method analyze_project
  Similarity: 86.90%
  Classes: DynamicSubagentGenerator <-> DynamicSubagentGenerator

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_instructions (lines 429-451) ---[0m
    fn generate_instructions(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> String {
        let mut instructions = template.instructions_template.clone();

        // Replace placeholders
        if !request.technologies.is_empty() {
            instructions = instructions.replace("{tech}", &request.technologies[0]);
            instructions = instructions.replace("{lang}", &request.technologies[0]);
        }

        // Add specific requirements
        if !request.requirements.is_empty() {
            instructions.push_str("\n\n## Specific Requirements\n\n");
            for req in &request.requirements {
                instructions.push_str(&format!("- {}\n", req));
            }
        }

        instructions
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:analyze_project (lines 454-468) ---[0m
    async fn analyze_project(&self, _project_path: &Path) -> SubagentResult<ProjectContext> {
        // This would integrate with semantic analyzer in real implementation
        // For now, return mock data
        Ok(ProjectContext {
            primary_language: "Rust".to_string(),
            frameworks: vec!["tokio".to_string(), "serde".to_string()],
            project_type: "library".to_string(),
            complexity: ComplexityMetrics {
                lines_of_code: 10000,
                module_count: 50,
                avg_complexity: 8.5,
                test_coverage: 75.0,
            },
        })
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:363-379 method generate_description <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:518-545 function test_tools_generation
  Similarity: 71.62%

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_description (lines 363-379) ---[0m
    fn generate_description(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> String {
        let mut description = template.description_template.clone();

        if !request.technologies.is_empty() {
            description = description.replace("{tech}", &request.technologies[0]);
            description = description.replace("{lang}", &request.technologies[0]);
        } else {
            description = description.replace("{tech}", "specialized technology");
            description = description.replace("{lang}", &request.project_context.primary_language);
        }

        description
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:test_tools_generation (lines 518-545) ---[0m
    fn test_tools_generation() {
        let generator = DynamicSubagentGenerator::new();
        let template = generator.templates.get("framework").unwrap();

        let request = GenerationRequest {
            agent_type: AgentType::FrameworkSpecialist,
            technologies: vec!["Vue".to_string()],
            project_context: ProjectContext {
                primary_language: "JavaScript".to_string(),
                frameworks: vec!["Vue".to_string()],
                project_type: "web".to_string(),
                complexity: ComplexityMetrics {
                    lines_of_code: 3000,
                    module_count: 15,
                    avg_complexity: 4.0,
                    test_coverage: 70.0,
                },
            },
            requirements: vec![
                "performance optimization".to_string(),
                "testing frameworks".to_string(),
            ],
        };

        let tools = generator.generate_tools(template, &request);
        assert!(tools.custom.contains(&"performance_profiler".to_string()));
        assert!(tools.custom.contains(&"test_generator".to_string()));
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:345-360 method generate_name <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:518-545 function test_tools_generation
  Similarity: 71.62%

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_name (lines 345-360) ---[0m
    fn generate_name(&self, template: &AgentTemplate, request: &GenerationRequest) -> String {
        let mut name = template.name_pattern.clone();

        if !request.technologies.is_empty() {
            name = name.replace("{tech}", &request.technologies[0].to_lowercase());
            name = name.replace("{lang}", &request.technologies[0].to_lowercase());
        } else {
            name = name.replace("{tech}", "specialized");
            name = name.replace(
                "{lang}",
                &request.project_context.primary_language.to_lowercase(),
            );
        }

        name
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:test_tools_generation (lines 518-545) ---[0m
    fn test_tools_generation() {
        let generator = DynamicSubagentGenerator::new();
        let template = generator.templates.get("framework").unwrap();

        let request = GenerationRequest {
            agent_type: AgentType::FrameworkSpecialist,
            technologies: vec!["Vue".to_string()],
            project_context: ProjectContext {
                primary_language: "JavaScript".to_string(),
                frameworks: vec!["Vue".to_string()],
                project_type: "web".to_string(),
                complexity: ComplexityMetrics {
                    lines_of_code: 3000,
                    module_count: 15,
                    avg_complexity: 4.0,
                    test_coverage: 70.0,
                },
            },
            requirements: vec![
                "performance optimization".to_string(),
                "testing frameworks".to_string(),
            ],
        };

        let tools = generator.generate_tools(template, &request);
        assert!(tools.custom.contains(&"performance_profiler".to_string()));
        assert!(tools.custom.contains(&"test_generator".to_string()));
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:363-379 method generate_description <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:454-468 method analyze_project
  Similarity: 97.09%
  Classes: DynamicSubagentGenerator <-> DynamicSubagentGenerator

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_description (lines 363-379) ---[0m
    fn generate_description(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> String {
        let mut description = template.description_template.clone();

        if !request.technologies.is_empty() {
            description = description.replace("{tech}", &request.technologies[0]);
            description = description.replace("{lang}", &request.technologies[0]);
        } else {
            description = description.replace("{tech}", "specialized technology");
            description = description.replace("{lang}", &request.project_context.primary_language);
        }

        description
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:analyze_project (lines 454-468) ---[0m
    async fn analyze_project(&self, _project_path: &Path) -> SubagentResult<ProjectContext> {
        // This would integrate with semantic analyzer in real implementation
        // For now, return mock data
        Ok(ProjectContext {
            primary_language: "Rust".to_string(),
            frameworks: vec!["tokio".to_string(), "serde".to_string()],
            project_type: "library".to_string(),
            complexity: ComplexityMetrics {
                lines_of_code: 10000,
                module_count: 50,
                avg_complexity: 8.5,
                test_coverage: 75.0,
            },
        })
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:345-360 method generate_name <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:363-379 method generate_description
  Similarity: 91.59%
  Classes: DynamicSubagentGenerator <-> DynamicSubagentGenerator

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_name (lines 345-360) ---[0m
    fn generate_name(&self, template: &AgentTemplate, request: &GenerationRequest) -> String {
        let mut name = template.name_pattern.clone();

        if !request.technologies.is_empty() {
            name = name.replace("{tech}", &request.technologies[0].to_lowercase());
            name = name.replace("{lang}", &request.technologies[0].to_lowercase());
        } else {
            name = name.replace("{tech}", "specialized");
            name = name.replace(
                "{lang}",
                &request.project_context.primary_language.to_lowercase(),
            );
        }

        name
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_description (lines 363-379) ---[0m
    fn generate_description(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> String {
        let mut description = template.description_template.clone();

        if !request.technologies.is_empty() {
            description = description.replace("{tech}", &request.technologies[0]);
            description = description.replace("{lang}", &request.technologies[0]);
        } else {
            description = description.replace("{tech}", "specialized technology");
            description = description.replace("{lang}", &request.project_context.primary_language);
        }

        description
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:345-360 method generate_name <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:454-468 method analyze_project
  Similarity: 97.47%
  Classes: DynamicSubagentGenerator <-> DynamicSubagentGenerator

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_name (lines 345-360) ---[0m
    fn generate_name(&self, template: &AgentTemplate, request: &GenerationRequest) -> String {
        let mut name = template.name_pattern.clone();

        if !request.technologies.is_empty() {
            name = name.replace("{tech}", &request.technologies[0].to_lowercase());
            name = name.replace("{lang}", &request.technologies[0].to_lowercase());
        } else {
            name = name.replace("{tech}", "specialized");
            name = name.replace(
                "{lang}",
                &request.project_context.primary_language.to_lowercase(),
            );
        }

        name
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:analyze_project (lines 454-468) ---[0m
    async fn analyze_project(&self, _project_path: &Path) -> SubagentResult<ProjectContext> {
        // This would integrate with semantic analyzer in real implementation
        // For now, return mock data
        Ok(ProjectContext {
            primary_language: "Rust".to_string(),
            frameworks: vec!["tokio".to_string(), "serde".to_string()],
            project_type: "library".to_string(),
            complexity: ComplexityMetrics {
                lines_of_code: 10000,
                module_count: 50,
                avg_complexity: 8.5,
                test_coverage: 75.0,
            },
        })
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:363-379 method generate_description <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:406-426 method generate_capabilities
  Similarity: 78.95%
  Classes: DynamicSubagentGenerator <-> DynamicSubagentGenerator

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_description (lines 363-379) ---[0m
    fn generate_description(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> String {
        let mut description = template.description_template.clone();

        if !request.technologies.is_empty() {
            description = description.replace("{tech}", &request.technologies[0]);
            description = description.replace("{lang}", &request.technologies[0]);
        } else {
            description = description.replace("{tech}", "specialized technology");
            description = description.replace("{lang}", &request.project_context.primary_language);
        }

        description
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_capabilities (lines 406-426) ---[0m
    fn generate_capabilities(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> Vec<String> {
        let mut capabilities = Vec::new();

        for cap in &template.base_capabilities {
            let mut capability = cap.clone();
            if !request.technologies.is_empty() {
                capability = capability.replace("{tech}", &request.technologies[0]);
                capability = capability.replace("{lang}", &request.technologies[0]);
            }
            capabilities.push(capability);
        }

        // Add specific capabilities from requirements
        capabilities.extend(request.requirements.clone());

        capabilities
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:345-360 method generate_name <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:406-426 method generate_capabilities
  Similarity: 81.06%
  Classes: DynamicSubagentGenerator <-> DynamicSubagentGenerator

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_name (lines 345-360) ---[0m
    fn generate_name(&self, template: &AgentTemplate, request: &GenerationRequest) -> String {
        let mut name = template.name_pattern.clone();

        if !request.technologies.is_empty() {
            name = name.replace("{tech}", &request.technologies[0].to_lowercase());
            name = name.replace("{lang}", &request.technologies[0].to_lowercase());
        } else {
            name = name.replace("{tech}", "specialized");
            name = name.replace(
                "{lang}",
                &request.project_context.primary_language.to_lowercase(),
            );
        }

        name
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_capabilities (lines 406-426) ---[0m
    fn generate_capabilities(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> Vec<String> {
        let mut capabilities = Vec::new();

        for cap in &template.base_capabilities {
            let mut capability = cap.clone();
            if !request.technologies.is_empty() {
                capability = capability.replace("{tech}", &request.technologies[0]);
                capability = capability.replace("{lang}", &request.technologies[0]);
            }
            capabilities.push(capability);
        }

        // Add specific capabilities from requirements
        capabilities.extend(request.requirements.clone());

        capabilities
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:363-379 method generate_description <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:492-515 function test_name_generation
  Similarity: 70.68%

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_description (lines 363-379) ---[0m
    fn generate_description(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> String {
        let mut description = template.description_template.clone();

        if !request.technologies.is_empty() {
            description = description.replace("{tech}", &request.technologies[0]);
            description = description.replace("{lang}", &request.technologies[0]);
        } else {
            description = description.replace("{tech}", "specialized technology");
            description = description.replace("{lang}", &request.project_context.primary_language);
        }

        description
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:test_name_generation (lines 492-515) ---[0m
    fn test_name_generation() {
        let generator = DynamicSubagentGenerator::new();
        let template = generator.templates.get("framework").unwrap();

        let request = GenerationRequest {
            agent_type: AgentType::FrameworkSpecialist,
            technologies: vec!["React".to_string()],
            project_context: ProjectContext {
                primary_language: "TypeScript".to_string(),
                frameworks: vec!["React".to_string()],
                project_type: "web".to_string(),
                complexity: ComplexityMetrics {
                    lines_of_code: 5000,
                    module_count: 20,
                    avg_complexity: 5.0,
                    test_coverage: 80.0,
                },
            },
            requirements: vec![],
        };

        let name = generator.generate_name(template, &request);
        assert_eq!(name, "react-specialist");
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:345-360 method generate_name <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:492-515 function test_name_generation
  Similarity: 70.68%

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_name (lines 345-360) ---[0m
    fn generate_name(&self, template: &AgentTemplate, request: &GenerationRequest) -> String {
        let mut name = template.name_pattern.clone();

        if !request.technologies.is_empty() {
            name = name.replace("{tech}", &request.technologies[0].to_lowercase());
            name = name.replace("{lang}", &request.technologies[0].to_lowercase());
        } else {
            name = name.replace("{tech}", "specialized");
            name = name.replace(
                "{lang}",
                &request.project_context.primary_language.to_lowercase(),
            );
        }

        name
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:test_name_generation (lines 492-515) ---[0m
    fn test_name_generation() {
        let generator = DynamicSubagentGenerator::new();
        let template = generator.templates.get("framework").unwrap();

        let request = GenerationRequest {
            agent_type: AgentType::FrameworkSpecialist,
            technologies: vec!["React".to_string()],
            project_context: ProjectContext {
                primary_language: "TypeScript".to_string(),
                frameworks: vec!["React".to_string()],
                project_type: "web".to_string(),
                complexity: ComplexityMetrics {
                    lines_of_code: 5000,
                    module_count: 20,
                    avg_complexity: 5.0,
                    test_coverage: 80.0,
                },
            },
            requirements: vec![],
        };

        let name = generator.generate_name(template, &request);
        assert_eq!(name, "react-specialist");
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:382-403 method generate_tools <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:472-484 method clone
  Similarity: 78.39%
  Classes: DynamicSubagentGenerator <-> Clone

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_tools (lines 382-403) ---[0m
    fn generate_tools(
        &self,
        template: &AgentTemplate,
        request: &GenerationRequest,
    ) -> SubagentTools {
        let mut tools = template.base_tools.clone();

        // Add custom tools based on requirements
        for req in &request.requirements {
            if req.contains("performance") {
                tools.custom.push("performance_profiler".to_string());
            }
            if req.contains("testing") {
                tools.custom.push("test_generator".to_string());
            }
            if req.contains("documentation") {
                tools.custom.push("doc_generator".to_string());
            }
        }

        tools
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:clone (lines 472-484) ---[0m
    fn clone(&self) -> Self {
        Self {
            primary_language: self.primary_language.clone(),
            frameworks: self.frameworks.clone(),
            project_type: self.project_type.clone(),
            complexity: ComplexityMetrics {
                lines_of_code: self.complexity.lines_of_code,
                module_count: self.complexity.module_count,
                avg_complexity: self.complexity.avg_complexity,
                test_coverage: self.complexity.test_coverage,
            },
        }
    }

  ./crates/ccswarm/src/subagent/dynamic_generation.rs:345-360 method generate_name <-> ./crates/ccswarm/src/subagent/dynamic_generation.rs:472-484 method clone
  Similarity: 72.12%
  Classes: DynamicSubagentGenerator <-> Clone

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:generate_name (lines 345-360) ---[0m
    fn generate_name(&self, template: &AgentTemplate, request: &GenerationRequest) -> String {
        let mut name = template.name_pattern.clone();

        if !request.technologies.is_empty() {
            name = name.replace("{tech}", &request.technologies[0].to_lowercase());
            name = name.replace("{lang}", &request.technologies[0].to_lowercase());
        } else {
            name = name.replace("{tech}", "specialized");
            name = name.replace(
                "{lang}",
                &request.project_context.primary_language.to_lowercase(),
            );
        }

        name
    }

[36m--- ./crates/ccswarm/src/subagent/dynamic_generation.rs:clone (lines 472-484) ---[0m
    fn clone(&self) -> Self {
        Self {
            primary_language: self.primary_language.clone(),
            frameworks: self.frameworks.clone(),
            project_type: self.project_type.clone(),
            complexity: ComplexityMetrics {
                lines_of_code: self.complexity.lines_of_code,
                module_count: self.complexity.module_count,
                avg_complexity: self.complexity.avg_complexity,
                test_coverage: self.complexity.test_coverage,
            },
        }
    }


Duplicates in ./crates/ccswarm/src/cli/mod.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/cli/mod.rs:2931-3526 method handle_session <-> ./crates/ccswarm/src/cli/mod.rs:3806-4171 method handle_extend
  Similarity: 98.51%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_session (lines 2931-3526) ---[0m
    async fn handle_session(&self, action: &SessionAction) -> Result<()> {
        match action {
            SessionAction::Create {
                agent,
                workspace,
                background,
            } => {
                let workspace_path = workspace.as_deref().unwrap_or("./");
                let _workspace_pathbuf = std::path::Path::new(workspace_path).to_path_buf();

                // Determine agent role from agent type
                let _agent_role = match agent.to_lowercase().as_str() {
                    "frontend" => crate::identity::default_frontend_role(),
                    "backend" => crate::identity::default_backend_role(),
                    "devops" => crate::identity::default_devops_role(),
                    "qa" => crate::identity::default_qa_role(),
                    _ => {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Invalid agent type",
                                    "agent": agent,
                                    "valid_types": ["frontend", "backend", "devops", "qa"],
                                }))?
                            );
                        } else {
                            println!("❌ Invalid agent type: {}", agent);
                            println!("   Valid types: frontend, backend, devops, qa");
                        }
                        return Ok(());
                    }
                };

                // Create session directly using tmux command
                let session_id = uuid::Uuid::new_v4();
                let agent_id = format!("{}-{}", agent, &session_id.to_string()[..8]);
                let tmux_session_name =
                    format!("ccswarm-{}-{}", agent, &session_id.to_string()[..8]);

                // Create tmux session using command
                let create_result = tokio::process::Command::new("tmux")
                    .args(&[
                        "new-session",
                        "-d", // detached
                        "-s",
                        &tmux_session_name,
                        "-c",
                        workspace_path,
                    ])
                    .status()
                    .await;

                match create_result {
                    Ok(status) => {
                        if status.success() {
                            // Set some environment variables in the session
                            let _ = tokio::process::Command::new("tmux")
                                .args(&[
                                    "setenv",
                                    "-t",
                                    &tmux_session_name,
                                    "CCSWARM_AGENT_ID",
                                    &agent_id,
                                ])
                                .status()
                                .await;

                            let _ = tokio::process::Command::new("tmux")
                                .args(&[
                                    "setenv",
                                    "-t",
                                    &tmux_session_name,
                                    "CCSWARM_AGENT_ROLE",
                                    agent,
                                ])
                                .status()
                                .await;

                            if self.json_output {
                                println!(
                                    "{}",
                                    serde_json::to_string_pretty(&serde_json::json!({
                                        "status": "success",
                                        "message": "Session created",
                                        "session_id": session_id.to_string(),
                                        "agent_id": agent_id,
                                        "agent": agent,
                                        "workspace": workspace_path,
                                        "background": background,
                                        "tmux_session": tmux_session_name,
                                    }))?
                                );
                            } else {
                                println!("🚀 Creating session for {} agent", agent);
                                println!("   Session ID: {}", &session_id.to_string()[..8]);
                                println!("   Agent ID: {}", agent_id);
                                println!("   Workspace: {}", workspace_path);
                                println!("   AI Session: {}", tmux_session_name);
                                println!("   Background: {}", background);
                                println!("   ✅ Session created successfully");
                                println!();
                                println!("To interact with this session:");
                                println!(
                                    "   ccswarm session attach {}",
                                    &session_id.to_string()[..8]
                                );
                            }
                        } else {
                            if self.json_output {
                                println!(
                                    "{}",
                                    serde_json::to_string_pretty(&serde_json::json!({
                                        "status": "error",
                                        "message": "Failed to create tmux session",
                                    }))?
                                );
                            } else {
                                println!("❌ Failed to create tmux session");
                            }
                        }
                    }
                    Err(e) => {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Failed to run tmux command",
                                    "error": e.to_string(),
                                }))?
                            );
                        } else {
                            println!("❌ Failed to run tmux command: {}", e);
                            println!("   Make sure tmux is installed");
                        }
                    }
                }
            }

            SessionAction::List { all } => {
                // Use tmux command directly to avoid runtime conflicts
                let output = match tokio::process::Command::new("tmux")
                    .args(&[
                        "list-sessions",
                        "-F",
                        "#{session_name}:#{session_created}:#{session_attached}",
                    ])
                    .output()
                    .await
                {
                    Ok(output) => output,
                    Err(e) => {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Failed to run tmux",
                                    "error": e.to_string(),
                                }))?
                            );
                        } else {
                            println!("❌ Failed to run tmux: {}", e);
                            println!("   Make sure tmux is installed");
                        }
                        return Ok(());
                    }
                };

                if output.status.success() {
                    let sessions_str = String::from_utf8_lossy(&output.stdout);
                    let sessions: Vec<&str> = sessions_str.lines().collect();

                    // Filter for ccswarm or ai-session sessions
                    let ccswarm_sessions: Vec<_> = sessions
                        .iter()
                        .filter(|s| {
                            let parts: Vec<&str> = s.split(':').collect();
                            if !parts.is_empty() {
                                let name = parts[0];
                                name.starts_with("ccswarm-") || name.starts_with("ai-session-")
                            } else {
                                false
                            }
                        })
                        .collect();

                    if self.json_output {
                        let session_data: Vec<serde_json::Value> = ccswarm_sessions
                            .iter()
                            .map(|s| {
                                let parts: Vec<&str> = s.split(':').collect();
                                let name = parts.get(0).unwrap_or(&"");
                                let created = parts.get(1).unwrap_or(&"");
                                let attached = parts.get(2).unwrap_or(&"0");

                                // Parse agent info from session name
                                let name_parts: Vec<&str> = name.split('-').collect();
                                let agent_role = if name_parts.len() >= 2 {
                                    name_parts[1]
                                } else {
                                    "unknown"
                                };

                                serde_json::json!({
                                    "tmux_session": name,
                                    "agent_role": agent_role,
                                    "created_at": created,
                                    "attached": *attached != "0",
                                    "active": true,
                                })
                            })
                            .collect();

                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "Sessions listed",
                                "sessions": session_data,
                                "show_all": all,
                            }))?
                        );
                    } else {
                        println!("📋 Active Sessions");
                        println!("=================");

                        if ccswarm_sessions.is_empty() {
                            println!("No active sessions found");
                            if !*all {
                                println!("(Use --all to show all tmux sessions)");
                            }
                        } else {
                            for session in &ccswarm_sessions {
                                let parts: Vec<&str> = session.split(':').collect();
                                let name = parts.get(0).unwrap_or(&"");
                                let created = parts.get(1).unwrap_or(&"");
                                let attached = parts.get(2).unwrap_or(&"0");

                                // Parse agent info from session name
                                let name_parts: Vec<&str> = name.split('-').collect();
                                let agent_role = if name_parts.len() >= 2 {
                                    name_parts[1]
                                } else {
                                    "unknown"
                                };

                                println!("AI Session: {}", name);
                                println!("  Agent Role: {}", agent_role);
                                println!("  Created: {}", created);
                                if *attached != "0" {
                                    println!("  Status: Attached");
                                } else {
                                    println!("  Status: Detached");
                                }
                                println!();
                            }
                        }

                        // Also show all tmux sessions if requested
                        if *all && !sessions.is_empty() {
                            println!("\nAll Tmux Sessions:");
                            println!("==================");
                            for s in &sessions {
                                let parts: Vec<&str> = s.split(':').collect();
                                let name = parts.get(0).unwrap_or(&"");
                                let created = parts.get(1).unwrap_or(&"");

                                if !name.starts_with("ccswarm-") && !name.starts_with("ai-session-")
                                {
                                    println!("  {} (created: {})", name, created);
                                }
                            }
                        }
                    }
                } else {
                    let error_msg = String::from_utf8_lossy(&output.stderr);
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "tmux command failed",
                                "error": error_msg.trim(),
                            }))?
                        );
                    } else {
                        if error_msg.contains("no sessions") || error_msg.contains("no server") {
                            println!("📋 Active Sessions");
                            println!("=================");
                            println!("No tmux sessions found");
                            println!("(Start tmux server with 'tmux new-session -d -s temp && tmux kill-session -t temp')");
                        } else {
                            println!("❌ tmux command failed: {}", error_msg.trim());
                        }
                    }
                }
            }

            SessionAction::Pause { session_id } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session paused",
                            "session_id": session_id,
                        }))?
                    );
                } else {
                    println!("⏸️ Pausing session: {}", session_id);
                    println!("   ✅ Session paused successfully");
                }
            }

            SessionAction::Resume { session_id } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session resumed",
                            "session_id": session_id,
                        }))?
                    );
                } else {
                    println!("▶️ Resuming session: {}", session_id);
                    println!("   ✅ Session resumed successfully");
                }
            }

            SessionAction::Attach { session_id } => {
                // First check if the session exists
                let list_output = match tokio::process::Command::new("tmux")
                    .args(&["list-sessions", "-F", "#{session_name}"])
                    .output()
                    .await
                {
                    Ok(output) => output,
                    Err(e) => {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Failed to run tmux",
                                    "error": e.to_string(),
                                }))?
                            );
                        } else {
                            println!("❌ Failed to run tmux: {}", e);
                        }
                        return Ok(());
                    }
                };

                if list_output.status.success() {
                    let sessions_str = String::from_utf8_lossy(&list_output.stdout);
                    let sessions: Vec<&str> = sessions_str.lines().collect();

                    // Look for session that matches the ID or contains it as prefix
                    let matching_session = sessions.iter().find(|s| {
                        **s == *session_id
                            || s.contains(session_id)
                            || (session_id.len() >= 8 && s.contains(&session_id[..8]))
                    });

                    if let Some(session_name) = matching_session {
                        // Attach to the session using tmux directly
                        let attach_result = tokio::process::Command::new("tmux")
                            .args(&["attach-session", "-t", session_name])
                            .status()
                            .await;

                        match attach_result {
                            Ok(status) => {
                                if status.success() {
                                    // This won't be reached if attach is successful,
                                    // as we'll be in the tmux session
                                    if self.json_output {
                                        println!(
                                            "{}",
                                            serde_json::to_string_pretty(&serde_json::json!({
                                                "status": "success",
                                                "message": "Attached to session",
                                                "session_id": session_id,
                                                "tmux_session": session_name,
                                            }))?
                                        );
                                    }
                                } else {
                                    if self.json_output {
                                        println!(
                                            "{}",
                                            serde_json::to_string_pretty(&serde_json::json!({
                                                "status": "error",
                                                "message": "Failed to attach to session",
                                                "session_id": session_id,
                                            }))?
                                        );
                                    } else {
                                        println!("❌ Failed to attach to session");
                                    }
                                }
                            }
                            Err(e) => {
                                if self.json_output {
                                    println!(
                                        "{}",
                                        serde_json::to_string_pretty(&serde_json::json!({
                                            "status": "error",
                                            "message": "Failed to run tmux attach",
                                            "error": e.to_string(),
                                        }))?
                                    );
                                } else {
                                    println!("❌ Failed to run tmux attach: {}", e);
                                }
                            }
                        }
                    } else {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Session not found",
                                    "session_id": session_id,
                                }))?
                            );
                        } else {
                            println!("❌ Session not found: {}", session_id);
                            println!("   Use 'ccswarm session list' to see available sessions");
                        }
                    }
                } else {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "Failed to list tmux sessions",
                            }))?
                        );
                    } else {
                        println!("❌ Failed to list tmux sessions");
                    }
                }
            }

            SessionAction::Detach { session_id } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Detached from session",
                            "session_id": session_id,
                        }))?
                    );
                } else {
                    println!("🔌 Detaching from session: {}", session_id);
                    println!("   ✅ Detached successfully");
                }
            }

            SessionAction::Kill { session_id, force } => {
                // First check if the session exists
                let list_output = match tokio::process::Command::new("tmux")
                    .args(&["list-sessions", "-F", "#{session_name}"])
                    .output()
                    .await
                {
                    Ok(output) => output,
                    Err(e) => {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Failed to run tmux",
                                    "error": e.to_string(),
                                }))?
                            );
                        } else {
                            println!("❌ Failed to run tmux: {}", e);
                        }
                        return Ok(());
                    }
                };

                if list_output.status.success() {
                    let sessions_str = String::from_utf8_lossy(&list_output.stdout);
                    let sessions: Vec<&str> = sessions_str.lines().collect();

                    // Look for session that matches the ID or contains it as prefix
                    let matching_session = sessions.iter().find(|s| {
                        **s == *session_id
                            || s.contains(session_id)
                            || (session_id.len() >= 8 && s.contains(&session_id[..8]))
                    });

                    if let Some(session_name) = matching_session {
                        // Kill the session using tmux directly
                        let kill_result = tokio::process::Command::new("tmux")
                            .args(&["kill-session", "-t", session_name])
                            .status()
                            .await;

                        match kill_result {
                            Ok(status) => {
                                if status.success() {
                                    if self.json_output {
                                        println!(
                                            "{}",
                                            serde_json::to_string_pretty(&serde_json::json!({
                                                "status": "success",
                                                "message": "Session killed",
                                                "session_id": session_id,
                                                "tmux_session": session_name,
                                                "force": force,
                                            }))?
                                        );
                                    } else {
                                        println!("💀 Killing session: {}", session_id);
                                        println!("   Tmux session: {}", session_name);
                                        if *force {
                                            println!("   ⚠️ Force kill enabled");
                                        }
                                        println!("   ✅ Session killed successfully");
                                    }
                                } else {
                                    if self.json_output {
                                        println!(
                                            "{}",
                                            serde_json::to_string_pretty(&serde_json::json!({
                                                "status": "error",
                                                "message": "Failed to kill session",
                                                "session_id": session_id,
                                            }))?
                                        );
                                    } else {
                                        println!("❌ Failed to kill session");
                                    }
                                }
                            }
                            Err(e) => {
                                if self.json_output {
                                    println!(
                                        "{}",
                                        serde_json::to_string_pretty(&serde_json::json!({
                                            "status": "error",
                                            "message": "Failed to run tmux kill-session",
                                            "error": e.to_string(),
                                        }))?
                                    );
                                } else {
                                    println!("❌ Failed to run tmux kill-session: {}", e);
                                }
                            }
                        }
                    } else {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Session not found",
                                    "session_id": session_id,
                                }))?
                            );
                        } else {
                            println!("❌ Session not found: {}", session_id);
                            println!("   Use 'ccswarm session list' to see available sessions");
                        }
                    }
                } else {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "Failed to list tmux sessions",
                            }))?
                        );
                    } else {
                        println!("❌ Failed to list tmux sessions");
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_extend (lines 3806-4171) ---[0m
    async fn handle_extend(&self, action: &ExtendAction) -> Result<()> {
        use crate::extension_stub::meta_learning::MetaLearningSystem;
        use crate::extension_stub::{
            ExtensionManager, ExtensionProposal, ExtensionStatus, ExtensionType,
        };
        use chrono::Utc;

        // Create extension manager instance
        let mut extension_manager = ExtensionManager::new();
        let _meta_learning = MetaLearningSystem::new();

        match action {
            ExtendAction::Propose {
                agent,
                extension_type,
                file,
            } => {
                // Read extension specification from file
                let spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read extension specification file")?;

                // Parse extension type
                let ext_type = match extension_type.as_str() {
                    "capability" => ExtensionType::Capability,
                    "system" => ExtensionType::System,
                    "cognitive" => ExtensionType::Cognitive,
                    "collaborative" => ExtensionType::Collaborative,
                    _ => ExtensionType::Capability, // Default
                };

                // Create extension proposal
                let proposal = ExtensionProposal {
                    id: uuid::Uuid::new_v4().to_string(),
                    agent_id: agent.clone(),
                    proposer: agent.clone(),
                    extension_type: ext_type,
                    status: ExtensionStatus::Proposed,
                    specification: spec_content.clone(),
                    title: format!("{} extension for {}", extension_type, agent),
                    description: spec_content.lines().take(3).collect::<Vec<_>>().join(" "),
                    current_state: crate::extension_stub::CurrentState {
                        capabilities: vec!["basic functionality".to_string()],
                        limitations: vec!["needs enhancement".to_string()],
                        performance_metrics: std::collections::HashMap::new(),
                    },
                    proposed_state: crate::extension_stub::ProposedState {
                        new_capabilities: vec!["enhanced functionality".to_string()],
                        removed_limitations: vec!["slow processing".to_string()],
                        expected_improvements: {
                            let mut map = std::collections::HashMap::new();
                            map.insert("performance".to_string(), 25.0);
                            map
                        },
                        performance_targets: std::collections::HashMap::new(),
                    },
                    implementation_plan: crate::extension_stub::ImplementationPlan {
                        total_duration: std::time::Duration::from_secs(2419200), // 4 weeks
                        phases: vec![
                            crate::extension_stub::ImplementationPhase {
                                name: "Analysis & Design".to_string(),
                                description: "Analyze requirements and design the extension"
                                    .to_string(),
                                tasks: vec![
                                    "Design document".to_string(),
                                    "Technical specification".to_string(),
                                ],
                                validation_method: "Code review".to_string(),
                                estimated_duration: std::time::Duration::from_secs(604800), // 1 week
                                complexity: crate::extension_stub::ComplexityLevel::Medium,
                                dependencies: Vec::new(),
                            },
                            crate::extension_stub::ImplementationPhase {
                                name: "Implementation".to_string(),
                                description: "Implement the extension functionality".to_string(),
                                tasks: vec!["Working code".to_string(), "Unit tests".to_string()],
                                validation_method: "Testing".to_string(),
                                estimated_duration: std::time::Duration::from_secs(1209600), // 2 weeks
                                complexity: crate::extension_stub::ComplexityLevel::High,
                                dependencies: vec!["Analysis & Design".to_string()],
                            },
                            crate::extension_stub::ImplementationPhase {
                                name: "Testing & Deployment".to_string(),
                                description: "Test and deploy the extension".to_string(),
                                tasks: vec![
                                    "Test results".to_string(),
                                    "Deployed extension".to_string(),
                                ],
                                validation_method: "Production testing".to_string(),
                                estimated_duration: std::time::Duration::from_secs(604800), // 1 week
                                complexity: crate::extension_stub::ComplexityLevel::Medium,
                                dependencies: vec!["Implementation".to_string()],
                            },
                        ],
                        timeline: "2-4 weeks".to_string(),
                        resources_required: vec!["development time".to_string()],
                        dependencies: vec![],
                    },
                    risk_assessment: crate::extension_stub::RiskAssessment {
                        risk_level: crate::extension_stub::RiskLevel::Low,
                        potential_issues: vec!["Minor performance impact".to_string()],
                        mitigation_strategies: vec![],
                        rollback_plan: "Revert to previous version".to_string(),
                        overall_risk_score: 0.3,
                        categories: vec![crate::extension_stub::RiskCategory::Performance],
                    },
                    success_criteria: vec![
                        crate::extension_stub::SuccessCriterion {
                            name: "Functionality".to_string(),
                            description: "Extension functionality working correctly".to_string(),
                            target_value: 100.0,
                            measurement_method: "Automated testing".to_string(),
                            measurable: true,
                        },
                        crate::extension_stub::SuccessCriterion {
                            name: "Performance".to_string(),
                            description: "No performance degradation".to_string(),
                            target_value: 100.0, // milliseconds
                            measurement_method: "Performance benchmarks".to_string(),
                            measurable: true,
                        },
                    ],
                    created_at: Utc::now(),
                };

                // Submit proposal to extension manager
                let proposal_id = extension_manager.propose_extension(proposal).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension proposed",
                            "proposal_id": proposal_id,
                            "agent": agent,
                            "type": extension_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!("🔧 Proposing extension for agent: {}", agent);
                    println!("   Type: {}", extension_type);
                    println!("   Specification: {}", file.display());
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Extension proposal submitted");
                }
            }

            ExtendAction::Status {
                agent,
                extension_id,
            } => {
                // Get extension manager statistics
                let stats = extension_manager.get_stats().await;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension status retrieved",
                            "agent": agent,
                            "extension_id": extension_id,
                            "stats": {
                                "total_extensions": stats.total_extensions,
                                "active_extensions": stats.active_extensions,
                                "pending_proposals": stats.pending_proposals,
                                "successful_extensions": stats.successful_extensions,
                                "failed_extensions": stats.failed_extensions,
                            }
                        }))?
                    );
                } else {
                    println!("📊 Extension Status for agent: {}", agent);
                    if let Some(id) = extension_id {
                        println!("🆔 Extension ID: {}", id);
                    }
                    println!("📈 Statistics:");
                    println!("   Total Extensions: {}", stats.total_extensions);
                    println!("   Active: {}", stats.active_extensions);
                    println!("   Pending Proposals: {}", stats.pending_proposals);
                    println!("   Successful: {}", stats.successful_extensions);
                    println!("   Failed: {}", stats.failed_extensions);
                }
            }

            ExtendAction::History {
                agent,
                successful,
                failed,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension history",
                            "agent": agent,
                            "filter": {
                                "successful": successful,
                                "failed": failed,
                            },
                        }))?
                    );
                } else {
                    println!("📜 Extension History for agent: {}", agent);
                    println!("================================");
                    if *successful {
                        println!("Showing only successful extensions");
                    } else if *failed {
                        println!("Showing only failed extensions");
                    }
                    println!("No extensions found");
                }
            }

            ExtendAction::Rollback {
                agent,
                extension_id,
                force,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension rolled back",
                            "agent": agent,
                            "extension_id": extension_id,
                            "force": force,
                        }))?
                    );
                } else {
                    println!("⏪ Rolling back extension: {}", extension_id);
                    println!("   Agent: {}", agent);
                    if *force {
                        println!("   ⚠️ Force rollback enabled");
                    }
                    println!("✅ Rollback completed");
                }
            }

            ExtendAction::Discover {
                agent,
                discovery_type,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Discovery completed",
                            "agent": agent,
                            "discovery_type": discovery_type,
                        }))?
                    );
                } else {
                    println!("🔍 Discovering extension opportunities");
                    if let Some(a) = agent {
                        println!("   Agent: {}", a);
                    }
                    if let Some(dt) = discovery_type {
                        println!("   Type: {}", dt);
                    }
                    println!("✅ Discovery completed");
                }
            }

            ExtendAction::Autonomous {
                agent,
                continuous,
                dry_run,
            } => {
                // use crate::extension::autonomous_agent_extension::AutonomousAgentExtensionManager;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "started",
                            "message": "Autonomous extension reasoning initiated",
                            "agent": agent,
                            "continuous": continuous,
                            "dry_run": dry_run,
                        }))?
                    );
                } else {
                    println!("🤖 Initiating autonomous extension reasoning");
                    if let Some(a) = agent {
                        println!("   Agent: {}", a);
                    } else {
                        println!("   Agents: All agents");
                    }
                    if *continuous {
                        println!(
                            "   Mode: Continuous (agents will autonomously propose extensions)"
                        );
                    }
                    if *dry_run {
                        println!("   🔍 DRY RUN - proposals will not be submitted to Sangha");
                    }
                }

                // Get agent list
                let target_agents = if let Some(agent_id) = agent {
                    vec![agent_id.clone()]
                } else {
                    // Get all agents from config
                    self.config.agents.keys().cloned().collect()
                };

                // Process each agent
                for agent_id in target_agents {
                    if !self.json_output {
                        println!("\n🎯 Processing agent: {}", agent_id);
                    }

                    // Create autonomous extension manager for the agent
                    // In real implementation, would create proper provider and sangha client
                    // For now, just log the intent

                    if *dry_run {
                        if !self.json_output {
                            println!("   📋 Would propose extensions based on:");
                            println!("      - Past performance analysis");
                            println!("      - Identified capability gaps");
                            println!("      - Recurring failure patterns");
                            println!("      - Self-reflection insights");
                        }
                    } else if !self.json_output {
                        println!("   🧠 Analyzing experiences and performance...");
                        println!("   🔍 Identifying capability gaps...");
                        println!("   💡 Generating extension proposals...");
                        println!("   🏛️  Submitting proposals to Sangha for approval...");

                        // In real implementation:
                        // 1. Create AutonomousAgentExtensionManager
                        // 2. Call propose_extensions()
                        // 3. Wait for Sangha consensus
                        // 4. Report results
                    }
                }

                if *continuous && !self.json_output {
                    println!("\n♾️  Continuous mode enabled - agents will autonomously propose extensions as needed");
                    println!("   Press Ctrl+C to stop");
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "completed",
                            "message": "Autonomous extension reasoning completed",
                            "continuous_mode": continuous,
                        }))?
                    );
                } else {
                    println!("\n✅ Autonomous extension reasoning completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2931-3526 method handle_session <-> ./crates/ccswarm/src/cli/mod.rs:5008-5380 method handle_template
  Similarity: 97.47%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_session (lines 2931-3526) ---[0m
    async fn handle_session(&self, action: &SessionAction) -> Result<()> {
        match action {
            SessionAction::Create {
                agent,
                workspace,
                background,
            } => {
                let workspace_path = workspace.as_deref().unwrap_or("./");
                let _workspace_pathbuf = std::path::Path::new(workspace_path).to_path_buf();

                // Determine agent role from agent type
                let _agent_role = match agent.to_lowercase().as_str() {
                    "frontend" => crate::identity::default_frontend_role(),
                    "backend" => crate::identity::default_backend_role(),
                    "devops" => crate::identity::default_devops_role(),
                    "qa" => crate::identity::default_qa_role(),
                    _ => {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Invalid agent type",
                                    "agent": agent,
                                    "valid_types": ["frontend", "backend", "devops", "qa"],
                                }))?
                            );
                        } else {
                            println!("❌ Invalid agent type: {}", agent);
                            println!("   Valid types: frontend, backend, devops, qa");
                        }
                        return Ok(());
                    }
                };

                // Create session directly using tmux command
                let session_id = uuid::Uuid::new_v4();
                let agent_id = format!("{}-{}", agent, &session_id.to_string()[..8]);
                let tmux_session_name =
                    format!("ccswarm-{}-{}", agent, &session_id.to_string()[..8]);

                // Create tmux session using command
                let create_result = tokio::process::Command::new("tmux")
                    .args(&[
                        "new-session",
                        "-d", // detached
                        "-s",
                        &tmux_session_name,
                        "-c",
                        workspace_path,
                    ])
                    .status()
                    .await;

                match create_result {
                    Ok(status) => {
                        if status.success() {
                            // Set some environment variables in the session
                            let _ = tokio::process::Command::new("tmux")
                                .args(&[
                                    "setenv",
                                    "-t",
                                    &tmux_session_name,
                                    "CCSWARM_AGENT_ID",
                                    &agent_id,
                                ])
                                .status()
                                .await;

                            let _ = tokio::process::Command::new("tmux")
                                .args(&[
                                    "setenv",
                                    "-t",
                                    &tmux_session_name,
                                    "CCSWARM_AGENT_ROLE",
                                    agent,
                                ])
                                .status()
                                .await;

                            if self.json_output {
                                println!(
                                    "{}",
                                    serde_json::to_string_pretty(&serde_json::json!({
                                        "status": "success",
                                        "message": "Session created",
                                        "session_id": session_id.to_string(),
                                        "agent_id": agent_id,
                                        "agent": agent,
                                        "workspace": workspace_path,
                                        "background": background,
                                        "tmux_session": tmux_session_name,
                                    }))?
                                );
                            } else {
                                println!("🚀 Creating session for {} agent", agent);
                                println!("   Session ID: {}", &session_id.to_string()[..8]);
                                println!("   Agent ID: {}", agent_id);
                                println!("   Workspace: {}", workspace_path);
                                println!("   AI Session: {}", tmux_session_name);
                                println!("   Background: {}", background);
                                println!("   ✅ Session created successfully");
                                println!();
                                println!("To interact with this session:");
                                println!(
                                    "   ccswarm session attach {}",
                                    &session_id.to_string()[..8]
                                );
                            }
                        } else {
                            if self.json_output {
                                println!(
                                    "{}",
                                    serde_json::to_string_pretty(&serde_json::json!({
                                        "status": "error",
                                        "message": "Failed to create tmux session",
                                    }))?
                                );
                            } else {
                                println!("❌ Failed to create tmux session");
                            }
                        }
                    }
                    Err(e) => {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Failed to run tmux command",
                                    "error": e.to_string(),
                                }))?
                            );
                        } else {
                            println!("❌ Failed to run tmux command: {}", e);
                            println!("   Make sure tmux is installed");
                        }
                    }
                }
            }

            SessionAction::List { all } => {
                // Use tmux command directly to avoid runtime conflicts
                let output = match tokio::process::Command::new("tmux")
                    .args(&[
                        "list-sessions",
                        "-F",
                        "#{session_name}:#{session_created}:#{session_attached}",
                    ])
                    .output()
                    .await
                {
                    Ok(output) => output,
                    Err(e) => {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Failed to run tmux",
                                    "error": e.to_string(),
                                }))?
                            );
                        } else {
                            println!("❌ Failed to run tmux: {}", e);
                            println!("   Make sure tmux is installed");
                        }
                        return Ok(());
                    }
                };

                if output.status.success() {
                    let sessions_str = String::from_utf8_lossy(&output.stdout);
                    let sessions: Vec<&str> = sessions_str.lines().collect();

                    // Filter for ccswarm or ai-session sessions
                    let ccswarm_sessions: Vec<_> = sessions
                        .iter()
                        .filter(|s| {
                            let parts: Vec<&str> = s.split(':').collect();
                            if !parts.is_empty() {
                                let name = parts[0];
                                name.starts_with("ccswarm-") || name.starts_with("ai-session-")
                            } else {
                                false
                            }
                        })
                        .collect();

                    if self.json_output {
                        let session_data: Vec<serde_json::Value> = ccswarm_sessions
                            .iter()
                            .map(|s| {
                                let parts: Vec<&str> = s.split(':').collect();
                                let name = parts.get(0).unwrap_or(&"");
                                let created = parts.get(1).unwrap_or(&"");
                                let attached = parts.get(2).unwrap_or(&"0");

                                // Parse agent info from session name
                                let name_parts: Vec<&str> = name.split('-').collect();
                                let agent_role = if name_parts.len() >= 2 {
                                    name_parts[1]
                                } else {
                                    "unknown"
                                };

                                serde_json::json!({
                                    "tmux_session": name,
                                    "agent_role": agent_role,
                                    "created_at": created,
                                    "attached": *attached != "0",
                                    "active": true,
                                })
                            })
                            .collect();

                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "Sessions listed",
                                "sessions": session_data,
                                "show_all": all,
                            }))?
                        );
                    } else {
                        println!("📋 Active Sessions");
                        println!("=================");

                        if ccswarm_sessions.is_empty() {
                            println!("No active sessions found");
                            if !*all {
                                println!("(Use --all to show all tmux sessions)");
                            }
                        } else {
                            for session in &ccswarm_sessions {
                                let parts: Vec<&str> = session.split(':').collect();
                                let name = parts.get(0).unwrap_or(&"");
                                let created = parts.get(1).unwrap_or(&"");
                                let attached = parts.get(2).unwrap_or(&"0");

                                // Parse agent info from session name
                                let name_parts: Vec<&str> = name.split('-').collect();
                                let agent_role = if name_parts.len() >= 2 {
                                    name_parts[1]
                                } else {
                                    "unknown"
                                };

                                println!("AI Session: {}", name);
                                println!("  Agent Role: {}", agent_role);
                                println!("  Created: {}", created);
                                if *attached != "0" {
                                    println!("  Status: Attached");
                                } else {
                                    println!("  Status: Detached");
                                }
                                println!();
                            }
                        }

                        // Also show all tmux sessions if requested
                        if *all && !sessions.is_empty() {
                            println!("\nAll Tmux Sessions:");
                            println!("==================");
                            for s in &sessions {
                                let parts: Vec<&str> = s.split(':').collect();
                                let name = parts.get(0).unwrap_or(&"");
                                let created = parts.get(1).unwrap_or(&"");

                                if !name.starts_with("ccswarm-") && !name.starts_with("ai-session-")
                                {
                                    println!("  {} (created: {})", name, created);
                                }
                            }
                        }
                    }
                } else {
                    let error_msg = String::from_utf8_lossy(&output.stderr);
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "tmux command failed",
                                "error": error_msg.trim(),
                            }))?
                        );
                    } else {
                        if error_msg.contains("no sessions") || error_msg.contains("no server") {
                            println!("📋 Active Sessions");
                            println!("=================");
                            println!("No tmux sessions found");
                            println!("(Start tmux server with 'tmux new-session -d -s temp && tmux kill-session -t temp')");
                        } else {
                            println!("❌ tmux command failed: {}", error_msg.trim());
                        }
                    }
                }
            }

            SessionAction::Pause { session_id } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session paused",
                            "session_id": session_id,
                        }))?
                    );
                } else {
                    println!("⏸️ Pausing session: {}", session_id);
                    println!("   ✅ Session paused successfully");
                }
            }

            SessionAction::Resume { session_id } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session resumed",
                            "session_id": session_id,
                        }))?
                    );
                } else {
                    println!("▶️ Resuming session: {}", session_id);
                    println!("   ✅ Session resumed successfully");
                }
            }

            SessionAction::Attach { session_id } => {
                // First check if the session exists
                let list_output = match tokio::process::Command::new("tmux")
                    .args(&["list-sessions", "-F", "#{session_name}"])
                    .output()
                    .await
                {
                    Ok(output) => output,
                    Err(e) => {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Failed to run tmux",
                                    "error": e.to_string(),
                                }))?
                            );
                        } else {
                            println!("❌ Failed to run tmux: {}", e);
                        }
                        return Ok(());
                    }
                };

                if list_output.status.success() {
                    let sessions_str = String::from_utf8_lossy(&list_output.stdout);
                    let sessions: Vec<&str> = sessions_str.lines().collect();

                    // Look for session that matches the ID or contains it as prefix
                    let matching_session = sessions.iter().find(|s| {
                        **s == *session_id
                            || s.contains(session_id)
                            || (session_id.len() >= 8 && s.contains(&session_id[..8]))
                    });

                    if let Some(session_name) = matching_session {
                        // Attach to the session using tmux directly
                        let attach_result = tokio::process::Command::new("tmux")
                            .args(&["attach-session", "-t", session_name])
                            .status()
                            .await;

                        match attach_result {
                            Ok(status) => {
                                if status.success() {
                                    // This won't be reached if attach is successful,
                                    // as we'll be in the tmux session
                                    if self.json_output {
                                        println!(
                                            "{}",
                                            serde_json::to_string_pretty(&serde_json::json!({
                                                "status": "success",
                                                "message": "Attached to session",
                                                "session_id": session_id,
                                                "tmux_session": session_name,
                                            }))?
                                        );
                                    }
                                } else {
                                    if self.json_output {
                                        println!(
                                            "{}",
                                            serde_json::to_string_pretty(&serde_json::json!({
                                                "status": "error",
                                                "message": "Failed to attach to session",
                                                "session_id": session_id,
                                            }))?
                                        );
                                    } else {
                                        println!("❌ Failed to attach to session");
                                    }
                                }
                            }
                            Err(e) => {
                                if self.json_output {
                                    println!(
                                        "{}",
                                        serde_json::to_string_pretty(&serde_json::json!({
                                            "status": "error",
                                            "message": "Failed to run tmux attach",
                                            "error": e.to_string(),
                                        }))?
                                    );
                                } else {
                                    println!("❌ Failed to run tmux attach: {}", e);
                                }
                            }
                        }
                    } else {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Session not found",
                                    "session_id": session_id,
                                }))?
                            );
                        } else {
                            println!("❌ Session not found: {}", session_id);
                            println!("   Use 'ccswarm session list' to see available sessions");
                        }
                    }
                } else {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "Failed to list tmux sessions",
                            }))?
                        );
                    } else {
                        println!("❌ Failed to list tmux sessions");
                    }
                }
            }

            SessionAction::Detach { session_id } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Detached from session",
                            "session_id": session_id,
                        }))?
                    );
                } else {
                    println!("🔌 Detaching from session: {}", session_id);
                    println!("   ✅ Detached successfully");
                }
            }

            SessionAction::Kill { session_id, force } => {
                // First check if the session exists
                let list_output = match tokio::process::Command::new("tmux")
                    .args(&["list-sessions", "-F", "#{session_name}"])
                    .output()
                    .await
                {
                    Ok(output) => output,
                    Err(e) => {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Failed to run tmux",
                                    "error": e.to_string(),
                                }))?
                            );
                        } else {
                            println!("❌ Failed to run tmux: {}", e);
                        }
                        return Ok(());
                    }
                };

                if list_output.status.success() {
                    let sessions_str = String::from_utf8_lossy(&list_output.stdout);
                    let sessions: Vec<&str> = sessions_str.lines().collect();

                    // Look for session that matches the ID or contains it as prefix
                    let matching_session = sessions.iter().find(|s| {
                        **s == *session_id
                            || s.contains(session_id)
                            || (session_id.len() >= 8 && s.contains(&session_id[..8]))
                    });

                    if let Some(session_name) = matching_session {
                        // Kill the session using tmux directly
                        let kill_result = tokio::process::Command::new("tmux")
                            .args(&["kill-session", "-t", session_name])
                            .status()
                            .await;

                        match kill_result {
                            Ok(status) => {
                                if status.success() {
                                    if self.json_output {
                                        println!(
                                            "{}",
                                            serde_json::to_string_pretty(&serde_json::json!({
                                                "status": "success",
                                                "message": "Session killed",
                                                "session_id": session_id,
                                                "tmux_session": session_name,
                                                "force": force,
                                            }))?
                                        );
                                    } else {
                                        println!("💀 Killing session: {}", session_id);
                                        println!("   Tmux session: {}", session_name);
                                        if *force {
                                            println!("   ⚠️ Force kill enabled");
                                        }
                                        println!("   ✅ Session killed successfully");
                                    }
                                } else {
                                    if self.json_output {
                                        println!(
                                            "{}",
                                            serde_json::to_string_pretty(&serde_json::json!({
                                                "status": "error",
                                                "message": "Failed to kill session",
                                                "session_id": session_id,
                                            }))?
                                        );
                                    } else {
                                        println!("❌ Failed to kill session");
                                    }
                                }
                            }
                            Err(e) => {
                                if self.json_output {
                                    println!(
                                        "{}",
                                        serde_json::to_string_pretty(&serde_json::json!({
                                            "status": "error",
                                            "message": "Failed to run tmux kill-session",
                                            "error": e.to_string(),
                                        }))?
                                    );
                                } else {
                                    println!("❌ Failed to run tmux kill-session: {}", e);
                                }
                            }
                        }
                    } else {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Session not found",
                                    "session_id": session_id,
                                }))?
                            );
                        } else {
                            println!("❌ Session not found: {}", session_id);
                            println!("   Use 'ccswarm session list' to see available sessions");
                        }
                    }
                } else {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "Failed to list tmux sessions",
                            }))?
                        );
                    } else {
                        println!("❌ Failed to list tmux sessions");
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_template (lines 5008-5380) ---[0m
    async fn handle_template(&self, action: &TemplateAction) -> Result<()> {
        use crate::template::{
            FileSystemTemplateStorage, PredefinedTemplates, TemplateCategory, TemplateContext,
            TemplateManager, TemplateQuery,
        };
        use colored::Colorize;
        use std::io::{self, Write};
        use std::str::FromStr;

        // Initialize template storage
        let templates_dir = self.repo_path.join(".ccswarm").join("templates");
        let storage = FileSystemTemplateStorage::new(&templates_dir)
            .await
            .context("Failed to initialize template storage")?;
        let mut manager = TemplateManager::new(storage);

        match action {
            TemplateAction::List {
                all: _,
                category,
                tags,
                search,
                popular,
                quality,
                detailed,
            } => {
                let mut query = TemplateQuery::new();

                if let Some(cat_str) = category {
                    let cat = TemplateCategory::from_str(cat_str).context("Invalid category")?;
                    query = query.with_category(cat);
                }

                if !tags.is_empty() {
                    query = query.with_tags(tags.clone());
                }

                if let Some(search_term) = search {
                    query = query.with_search_term(search_term);
                }

                if *popular {
                    query = query.sort_by_popularity();
                } else if *quality {
                    query = query.sort_by_success_rate();
                }

                let templates = manager
                    .search_templates(query)
                    .await
                    .context("Failed to search templates")?;

                if templates.is_empty() {
                    println!("No templates found.");
                    return Ok(());
                }

                println!("Available Templates:");
                println!();

                for template in templates {
                    if *detailed {
                        println!(
                            "📋 {} ({})",
                            template.name.bright_cyan(),
                            template.id.as_str().bright_black()
                        );
                        println!("   Category: {}", template.category);
                        println!("   Description: {}", template.description);
                        if !template.tags.is_empty() {
                            println!(
                                "   Tags: {}",
                                template.tags.join(", ").as_str().bright_black()
                            );
                        }
                        if let Some(author) = &template.author {
                            println!("   Author: {}", author.as_str().bright_black());
                        }
                        println!("   Usage: {} times", template.usage_count);
                        if let Some(rate) = template.success_rate {
                            println!("   Success Rate: {:.1}%", rate * 100.0);
                        }
                        println!();
                    } else {
                        println!(
                            "  {} ({}) - {}",
                            template.name.bright_cyan(),
                            template.id.bright_black(),
                            template.description.chars().take(80).collect::<String>()
                        );
                    }
                }
            }

            TemplateAction::Show {
                template,
                source,
                stats,
            } => {
                let tmpl = manager
                    .get_template_by_name(template)
                    .await
                    .context("Template not found")?;

                println!(
                    "Template: {} ({})",
                    tmpl.name.bright_cyan(),
                    tmpl.id.bright_black()
                );
                println!("Category: {}", tmpl.category);
                println!("Description: {}", tmpl.description);
                println!("Version: {}", tmpl.version);

                if let Some(author) = &tmpl.author {
                    println!("Author: {}", author);
                }

                if !tmpl.tags.is_empty() {
                    println!("Tags: {}", tmpl.tags.join(", "));
                }

                println!("Priority: {:?}", tmpl.default_priority);
                println!("Task Type: {:?}", tmpl.default_task_type);

                if let Some(duration) = tmpl.estimated_duration {
                    println!("Estimated Duration: {} minutes", duration);
                }

                if !tmpl.variables.is_empty() {
                    println!();
                    println!("Variables:");
                    for var in &tmpl.variables {
                        let required = if var.required { " (required)" } else { "" };
                        println!(
                            "  • {}{}: {}",
                            var.name.bright_green(),
                            required,
                            var.description
                        );
                        if let Some(default) = &var.default_value {
                            println!("    Default: {}", default.clone().bright_black());
                        }
                    }
                }

                if *source {
                    println!();
                    println!("Task Description Template:");
                    println!("{}", tmpl.task_description.bright_white());

                    if let Some(details) = &tmpl.task_details {
                        println!();
                        println!("Task Details Template:");
                        println!("{}", details.bright_white());
                    }
                }

                if *stats {
                    println!();
                    println!("Statistics:");
                    println!("  Usage Count: {}", tmpl.usage_count);
                    if let Some(rate) = tmpl.success_rate {
                        println!("  Success Rate: {:.1}%", rate * 100.0);
                    }
                    println!("  Created: {}", tmpl.created_at.format("%Y-%m-%d %H:%M"));
                    println!("  Updated: {}", tmpl.updated_at.format("%Y-%m-%d %H:%M"));
                }
            }

            TemplateAction::Apply {
                template,
                vars,
                interactive,
                preview,
                auto_assign,
            } => {
                let tmpl = manager
                    .get_template_by_name(template)
                    .await
                    .context("Template not found")?;

                let mut context = TemplateContext::new();

                // Parse provided variables
                for var_str in vars {
                    if let Some((key, value)) = var_str.split_once('=') {
                        context = context.with_variable(key.trim(), value.trim());
                    }
                }

                // Interactive mode for missing variables
                if *interactive {
                    for var in &tmpl.variables {
                        if var.required
                            && !context.variables.contains_key(&var.name)
                            && var.default_value.is_none()
                        {
                            print!("{} ({}): ", var.name, var.description);
                            io::stdout().flush()?;

                            let mut input = String::new();
                            io::stdin().read_line(&mut input)?;
                            let value = input.trim();

                            if !value.is_empty() {
                                context = context.with_variable(&var.name, value);
                            }
                        }
                    }
                }

                // Apply template
                let applied = manager
                    .apply_template(&tmpl.id, context)
                    .await
                    .context("Failed to apply template")?;

                if *preview {
                    println!("Preview of generated task:");
                    println!();
                    println!("Description: {}", applied.description.bright_cyan());
                    if let Some(details) = &applied.details {
                        println!("Details: {}", details);
                    }
                    println!("Priority: {:?}", applied.priority);
                    println!("Type: {:?}", applied.task_type);
                    if let Some(duration) = applied.estimated_duration {
                        println!("Duration: {} minutes", duration);
                    }
                    if !applied.target_files.is_empty() {
                        println!("Target Files: {}", applied.target_files.join(", "));
                    }
                } else {
                    // Create the actual task
                    use crate::agent::TaskBuilder;

                    let task = TaskBuilder::new(applied.description.clone())
                        .priority(applied.priority)
                        .task_type(applied.task_type);

                    let task = if let Some(details) = &applied.details {
                        task.details(details.clone())
                    } else {
                        task
                    };

                    let task = if let Some(duration) = applied.estimated_duration {
                        task.estimated_duration(duration as u64)
                    } else {
                        task
                    };

                    let task = task.build();

                    println!(
                        "Created task from template: {}",
                        applied.description.bright_green()
                    );
                    println!("Task ID: {}", task.id.bright_cyan());

                    if *auto_assign {
                        println!("Auto-assigning to best agent...");
                        // TODO: Implement auto-assignment logic
                    }
                }
            }

            TemplateAction::Install {
                all,
                categories,
                force,
            } => {
                let predefined_templates = PredefinedTemplates::get_all();
                let mut installed = 0;
                let mut skipped = 0;

                for template in predefined_templates {
                    // Filter by categories if specified
                    if !categories.is_empty() && !*all {
                        let cat_str = template.category.to_string();
                        if !categories.iter().any(|c| c.eq_ignore_ascii_case(&cat_str)) {
                            continue;
                        }
                    }

                    match manager.save_template(template.clone()).await {
                        Ok(()) => {
                            println!("✅ Installed: {}", template.name.bright_green());
                            installed += 1;
                        }
                        Err(e) if e.to_string().contains("already exists") => {
                            if *force {
                                if let Err(e) = manager.update_template(template.clone()).await {
                                    println!("❌ Failed to update {}: {}", template.name.red(), e);
                                } else {
                                    println!("✅ Updated: {}", template.name.bright_green());
                                    installed += 1;
                                }
                            } else {
                                println!("⚠️  Skipped (exists): {}", template.name.bright_yellow());
                                skipped += 1;
                            }
                        }
                        Err(e) => {
                            println!("❌ Failed to install {}: {}", template.name.red(), e);
                        }
                    }
                }

                println!();
                println!(
                    "Installation complete: {} installed, {} skipped",
                    installed, skipped
                );
                if skipped > 0 && !*force {
                    println!("Use --force to overwrite existing templates");
                }
            }

            TemplateAction::Stats {
                global: _,
                template,
            } => {
                if let Some(tmpl_name) = template {
                    let tmpl = manager
                        .get_template_by_name(tmpl_name)
                        .await
                        .context("Template not found")?;

                    println!("Template Statistics: {}", tmpl.name.bright_cyan());
                    println!("Usage Count: {}", tmpl.usage_count);
                    if let Some(rate) = tmpl.success_rate {
                        println!("Success Rate: {:.1}%", rate * 100.0);
                    }
                    println!("Created: {}", tmpl.created_at.format("%Y-%m-%d %H:%M"));
                    println!("Updated: {}", tmpl.updated_at.format("%Y-%m-%d %H:%M"));
                } else {
                    let stats = manager
                        .get_template_stats()
                        .await
                        .context("Failed to get template statistics")?;

                    println!("Global Template Statistics:");
                    println!("Total Templates: {}", stats.total_templates);
                    println!("Total Usage: {}", stats.total_usage);
                    println!(
                        "Average Success Rate: {:.1}%",
                        stats.average_success_rate * 100.0
                    );

                    println!();
                    println!("By Category:");
                    for (category, count) in &stats.by_category {
                        println!("  {}: {}", category, count);
                    }

                    if !stats.most_popular.is_empty() {
                        println!();
                        println!("Most Popular:");
                        for (name, count) in stats.most_popular.iter().take(5) {
                            println!("  {}: {} uses", name, count);
                        }
                    }
                }
            }

            _ => {
                println!("Template command not yet implemented: {:?}", action);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2931-3526 method handle_session <-> ./crates/ccswarm/src/cli/mod.rs:4685-5006 method handle_quality
  Similarity: 99.14%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_session (lines 2931-3526) ---[0m
    async fn handle_session(&self, action: &SessionAction) -> Result<()> {
        match action {
            SessionAction::Create {
                agent,
                workspace,
                background,
            } => {
                let workspace_path = workspace.as_deref().unwrap_or("./");
                let _workspace_pathbuf = std::path::Path::new(workspace_path).to_path_buf();

                // Determine agent role from agent type
                let _agent_role = match agent.to_lowercase().as_str() {
                    "frontend" => crate::identity::default_frontend_role(),
                    "backend" => crate::identity::default_backend_role(),
                    "devops" => crate::identity::default_devops_role(),
                    "qa" => crate::identity::default_qa_role(),
                    _ => {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Invalid agent type",
                                    "agent": agent,
                                    "valid_types": ["frontend", "backend", "devops", "qa"],
                                }))?
                            );
                        } else {
                            println!("❌ Invalid agent type: {}", agent);
                            println!("   Valid types: frontend, backend, devops, qa");
                        }
                        return Ok(());
                    }
                };

                // Create session directly using tmux command
                let session_id = uuid::Uuid::new_v4();
                let agent_id = format!("{}-{}", agent, &session_id.to_string()[..8]);
                let tmux_session_name =
                    format!("ccswarm-{}-{}", agent, &session_id.to_string()[..8]);

                // Create tmux session using command
                let create_result = tokio::process::Command::new("tmux")
                    .args(&[
                        "new-session",
                        "-d", // detached
                        "-s",
                        &tmux_session_name,
                        "-c",
                        workspace_path,
                    ])
                    .status()
                    .await;

                match create_result {
                    Ok(status) => {
                        if status.success() {
                            // Set some environment variables in the session
                            let _ = tokio::process::Command::new("tmux")
                                .args(&[
                                    "setenv",
                                    "-t",
                                    &tmux_session_name,
                                    "CCSWARM_AGENT_ID",
                                    &agent_id,
                                ])
                                .status()
                                .await;

                            let _ = tokio::process::Command::new("tmux")
                                .args(&[
                                    "setenv",
                                    "-t",
                                    &tmux_session_name,
                                    "CCSWARM_AGENT_ROLE",
                                    agent,
                                ])
                                .status()
                                .await;

                            if self.json_output {
                                println!(
                                    "{}",
                                    serde_json::to_string_pretty(&serde_json::json!({
                                        "status": "success",
                                        "message": "Session created",
                                        "session_id": session_id.to_string(),
                                        "agent_id": agent_id,
                                        "agent": agent,
                                        "workspace": workspace_path,
                                        "background": background,
                                        "tmux_session": tmux_session_name,
                                    }))?
                                );
                            } else {
                                println!("🚀 Creating session for {} agent", agent);
                                println!("   Session ID: {}", &session_id.to_string()[..8]);
                                println!("   Agent ID: {}", agent_id);
                                println!("   Workspace: {}", workspace_path);
                                println!("   AI Session: {}", tmux_session_name);
                                println!("   Background: {}", background);
                                println!("   ✅ Session created successfully");
                                println!();
                                println!("To interact with this session:");
                                println!(
                                    "   ccswarm session attach {}",
                                    &session_id.to_string()[..8]
                                );
                            }
                        } else {
                            if self.json_output {
                                println!(
                                    "{}",
                                    serde_json::to_string_pretty(&serde_json::json!({
                                        "status": "error",
                                        "message": "Failed to create tmux session",
                                    }))?
                                );
                            } else {
                                println!("❌ Failed to create tmux session");
                            }
                        }
                    }
                    Err(e) => {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Failed to run tmux command",
                                    "error": e.to_string(),
                                }))?
                            );
                        } else {
                            println!("❌ Failed to run tmux command: {}", e);
                            println!("   Make sure tmux is installed");
                        }
                    }
                }
            }

            SessionAction::List { all } => {
                // Use tmux command directly to avoid runtime conflicts
                let output = match tokio::process::Command::new("tmux")
                    .args(&[
                        "list-sessions",
                        "-F",
                        "#{session_name}:#{session_created}:#{session_attached}",
                    ])
                    .output()
                    .await
                {
                    Ok(output) => output,
                    Err(e) => {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Failed to run tmux",
                                    "error": e.to_string(),
                                }))?
                            );
                        } else {
                            println!("❌ Failed to run tmux: {}", e);
                            println!("   Make sure tmux is installed");
                        }
                        return Ok(());
                    }
                };

                if output.status.success() {
                    let sessions_str = String::from_utf8_lossy(&output.stdout);
                    let sessions: Vec<&str> = sessions_str.lines().collect();

                    // Filter for ccswarm or ai-session sessions
                    let ccswarm_sessions: Vec<_> = sessions
                        .iter()
                        .filter(|s| {
                            let parts: Vec<&str> = s.split(':').collect();
                            if !parts.is_empty() {
                                let name = parts[0];
                                name.starts_with("ccswarm-") || name.starts_with("ai-session-")
                            } else {
                                false
                            }
                        })
                        .collect();

                    if self.json_output {
                        let session_data: Vec<serde_json::Value> = ccswarm_sessions
                            .iter()
                            .map(|s| {
                                let parts: Vec<&str> = s.split(':').collect();
                                let name = parts.get(0).unwrap_or(&"");
                                let created = parts.get(1).unwrap_or(&"");
                                let attached = parts.get(2).unwrap_or(&"0");

                                // Parse agent info from session name
                                let name_parts: Vec<&str> = name.split('-').collect();
                                let agent_role = if name_parts.len() >= 2 {
                                    name_parts[1]
                                } else {
                                    "unknown"
                                };

                                serde_json::json!({
                                    "tmux_session": name,
                                    "agent_role": agent_role,
                                    "created_at": created,
                                    "attached": *attached != "0",
                                    "active": true,
                                })
                            })
                            .collect();

                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "Sessions listed",
                                "sessions": session_data,
                                "show_all": all,
                            }))?
                        );
                    } else {
                        println!("📋 Active Sessions");
                        println!("=================");

                        if ccswarm_sessions.is_empty() {
                            println!("No active sessions found");
                            if !*all {
                                println!("(Use --all to show all tmux sessions)");
                            }
                        } else {
                            for session in &ccswarm_sessions {
                                let parts: Vec<&str> = session.split(':').collect();
                                let name = parts.get(0).unwrap_or(&"");
                                let created = parts.get(1).unwrap_or(&"");
                                let attached = parts.get(2).unwrap_or(&"0");

                                // Parse agent info from session name
                                let name_parts: Vec<&str> = name.split('-').collect();
                                let agent_role = if name_parts.len() >= 2 {
                                    name_parts[1]
                                } else {
                                    "unknown"
                                };

                                println!("AI Session: {}", name);
                                println!("  Agent Role: {}", agent_role);
                                println!("  Created: {}", created);
                                if *attached != "0" {
                                    println!("  Status: Attached");
                                } else {
                                    println!("  Status: Detached");
                                }
                                println!();
                            }
                        }

                        // Also show all tmux sessions if requested
                        if *all && !sessions.is_empty() {
                            println!("\nAll Tmux Sessions:");
                            println!("==================");
                            for s in &sessions {
                                let parts: Vec<&str> = s.split(':').collect();
                                let name = parts.get(0).unwrap_or(&"");
                                let created = parts.get(1).unwrap_or(&"");

                                if !name.starts_with("ccswarm-") && !name.starts_with("ai-session-")
                                {
                                    println!("  {} (created: {})", name, created);
                                }
                            }
                        }
                    }
                } else {
                    let error_msg = String::from_utf8_lossy(&output.stderr);
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "tmux command failed",
                                "error": error_msg.trim(),
                            }))?
                        );
                    } else {
                        if error_msg.contains("no sessions") || error_msg.contains("no server") {
                            println!("📋 Active Sessions");
                            println!("=================");
                            println!("No tmux sessions found");
                            println!("(Start tmux server with 'tmux new-session -d -s temp && tmux kill-session -t temp')");
                        } else {
                            println!("❌ tmux command failed: {}", error_msg.trim());
                        }
                    }
                }
            }

            SessionAction::Pause { session_id } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session paused",
                            "session_id": session_id,
                        }))?
                    );
                } else {
                    println!("⏸️ Pausing session: {}", session_id);
                    println!("   ✅ Session paused successfully");
                }
            }

            SessionAction::Resume { session_id } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session resumed",
                            "session_id": session_id,
                        }))?
                    );
                } else {
                    println!("▶️ Resuming session: {}", session_id);
                    println!("   ✅ Session resumed successfully");
                }
            }

            SessionAction::Attach { session_id } => {
                // First check if the session exists
                let list_output = match tokio::process::Command::new("tmux")
                    .args(&["list-sessions", "-F", "#{session_name}"])
                    .output()
                    .await
                {
                    Ok(output) => output,
                    Err(e) => {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Failed to run tmux",
                                    "error": e.to_string(),
                                }))?
                            );
                        } else {
                            println!("❌ Failed to run tmux: {}", e);
                        }
                        return Ok(());
                    }
                };

                if list_output.status.success() {
                    let sessions_str = String::from_utf8_lossy(&list_output.stdout);
                    let sessions: Vec<&str> = sessions_str.lines().collect();

                    // Look for session that matches the ID or contains it as prefix
                    let matching_session = sessions.iter().find(|s| {
                        **s == *session_id
                            || s.contains(session_id)
                            || (session_id.len() >= 8 && s.contains(&session_id[..8]))
                    });

                    if let Some(session_name) = matching_session {
                        // Attach to the session using tmux directly
                        let attach_result = tokio::process::Command::new("tmux")
                            .args(&["attach-session", "-t", session_name])
                            .status()
                            .await;

                        match attach_result {
                            Ok(status) => {
                                if status.success() {
                                    // This won't be reached if attach is successful,
                                    // as we'll be in the tmux session
                                    if self.json_output {
                                        println!(
                                            "{}",
                                            serde_json::to_string_pretty(&serde_json::json!({
                                                "status": "success",
                                                "message": "Attached to session",
                                                "session_id": session_id,
                                                "tmux_session": session_name,
                                            }))?
                                        );
                                    }
                                } else {
                                    if self.json_output {
                                        println!(
                                            "{}",
                                            serde_json::to_string_pretty(&serde_json::json!({
                                                "status": "error",
                                                "message": "Failed to attach to session",
                                                "session_id": session_id,
                                            }))?
                                        );
                                    } else {
                                        println!("❌ Failed to attach to session");
                                    }
                                }
                            }
                            Err(e) => {
                                if self.json_output {
                                    println!(
                                        "{}",
                                        serde_json::to_string_pretty(&serde_json::json!({
                                            "status": "error",
                                            "message": "Failed to run tmux attach",
                                            "error": e.to_string(),
                                        }))?
                                    );
                                } else {
                                    println!("❌ Failed to run tmux attach: {}", e);
                                }
                            }
                        }
                    } else {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Session not found",
                                    "session_id": session_id,
                                }))?
                            );
                        } else {
                            println!("❌ Session not found: {}", session_id);
                            println!("   Use 'ccswarm session list' to see available sessions");
                        }
                    }
                } else {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "Failed to list tmux sessions",
                            }))?
                        );
                    } else {
                        println!("❌ Failed to list tmux sessions");
                    }
                }
            }

            SessionAction::Detach { session_id } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Detached from session",
                            "session_id": session_id,
                        }))?
                    );
                } else {
                    println!("🔌 Detaching from session: {}", session_id);
                    println!("   ✅ Detached successfully");
                }
            }

            SessionAction::Kill { session_id, force } => {
                // First check if the session exists
                let list_output = match tokio::process::Command::new("tmux")
                    .args(&["list-sessions", "-F", "#{session_name}"])
                    .output()
                    .await
                {
                    Ok(output) => output,
                    Err(e) => {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Failed to run tmux",
                                    "error": e.to_string(),
                                }))?
                            );
                        } else {
                            println!("❌ Failed to run tmux: {}", e);
                        }
                        return Ok(());
                    }
                };

                if list_output.status.success() {
                    let sessions_str = String::from_utf8_lossy(&list_output.stdout);
                    let sessions: Vec<&str> = sessions_str.lines().collect();

                    // Look for session that matches the ID or contains it as prefix
                    let matching_session = sessions.iter().find(|s| {
                        **s == *session_id
                            || s.contains(session_id)
                            || (session_id.len() >= 8 && s.contains(&session_id[..8]))
                    });

                    if let Some(session_name) = matching_session {
                        // Kill the session using tmux directly
                        let kill_result = tokio::process::Command::new("tmux")
                            .args(&["kill-session", "-t", session_name])
                            .status()
                            .await;

                        match kill_result {
                            Ok(status) => {
                                if status.success() {
                                    if self.json_output {
                                        println!(
                                            "{}",
                                            serde_json::to_string_pretty(&serde_json::json!({
                                                "status": "success",
                                                "message": "Session killed",
                                                "session_id": session_id,
                                                "tmux_session": session_name,
                                                "force": force,
                                            }))?
                                        );
                                    } else {
                                        println!("💀 Killing session: {}", session_id);
                                        println!("   Tmux session: {}", session_name);
                                        if *force {
                                            println!("   ⚠️ Force kill enabled");
                                        }
                                        println!("   ✅ Session killed successfully");
                                    }
                                } else {
                                    if self.json_output {
                                        println!(
                                            "{}",
                                            serde_json::to_string_pretty(&serde_json::json!({
                                                "status": "error",
                                                "message": "Failed to kill session",
                                                "session_id": session_id,
                                            }))?
                                        );
                                    } else {
                                        println!("❌ Failed to kill session");
                                    }
                                }
                            }
                            Err(e) => {
                                if self.json_output {
                                    println!(
                                        "{}",
                                        serde_json::to_string_pretty(&serde_json::json!({
                                            "status": "error",
                                            "message": "Failed to run tmux kill-session",
                                            "error": e.to_string(),
                                        }))?
                                    );
                                } else {
                                    println!("❌ Failed to run tmux kill-session: {}", e);
                                }
                            }
                        }
                    } else {
                        if self.json_output {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": "Session not found",
                                    "session_id": session_id,
                                }))?
                            );
                        } else {
                            println!("❌ Session not found: {}", session_id);
                            println!("   Use 'ccswarm session list' to see available sessions");
                        }
                    }
                } else {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "Failed to list tmux sessions",
                            }))?
                        );
                    } else {
                        println!("❌ Failed to list tmux sessions");
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_quality (lines 4685-5006) ---[0m
    async fn handle_quality(&self, action: &QualityAction) -> Result<()> {
        use std::process::Command;

        let mut failed_checks = Vec::new();
        let mut completed_tasks = 0;
        let total_tasks;

        match action {
            QualityAction::Check {
                skip,
                only,
                fail_fast,
            } => {
                println!("🤖 ccswarm Agent-Managed Quality Checks");
                println!("=======================================");
                println!();

                let checks = if only.is_empty() {
                    vec!["format", "lint", "test", "build", "security"]
                } else {
                    only.iter().map(|s| s.as_str()).collect()
                };

                let filtered_checks: Vec<&str> = checks
                    .into_iter()
                    .filter(|check| !skip.contains(&check.to_string()))
                    .collect();

                total_tasks = filtered_checks.len();
                println!("🎯 Master Claude: Orchestrating {} quality checks through specialized agents...", total_tasks);
                println!();

                for check in filtered_checks {
                    let (agent, task, cmd) = match check {
                        "format" => (
                            "DevOps",
                            "Code Formatting Check",
                            vec!["cargo", "fmt", "--check"],
                        ),
                        "lint" => (
                            "DevOps",
                            "Clippy Code Quality Analysis",
                            vec![
                                "cargo",
                                "clippy",
                                "--all-targets",
                                "--all-features",
                                "--",
                                "-D",
                                "warnings",
                            ],
                        ),
                        "test" => (
                            "QA",
                            "Test Suite Execution",
                            vec!["cargo", "test", "--lib", "--verbose", "--no-fail-fast"],
                        ),
                        "build" => (
                            "DevOps",
                            "Build Verification",
                            vec!["cargo", "build", "--verbose"],
                        ),
                        "security" => (
                            "Backend",
                            "Security Analysis",
                            vec![
                                "cargo",
                                "test",
                                "security::owasp_checker::tests",
                                "--no-fail-fast",
                            ],
                        ),
                        _ => continue,
                    };

                    println!("🎯 Delegating to {} agent: {}", agent, task);

                    let mut command = Command::new(&cmd[0]);
                    for arg in &cmd[1..] {
                        command.arg(arg);
                    }

                    let output = command.output().context("Failed to execute command")?;
                    let success = output.status.success();

                    if success {
                        println!("✅ {} agent: {} completed successfully", agent, task);
                        completed_tasks += 1;
                    } else {
                        println!("❌ {} agent: {} failed", agent, task);
                        failed_checks.push((
                            agent,
                            task,
                            String::from_utf8_lossy(&output.stderr).to_string(),
                        ));

                        if *fail_fast {
                            break;
                        }
                    }
                    println!();
                }

                // Quality Gate Assessment
                println!("🎯 Master Claude - Quality Gate Assessment");
                println!("==========================================");
                println!("📊 Agent Task Completion Summary:");
                println!("   Completed: {}/{} tasks", completed_tasks, total_tasks);
                println!("   Failed: {}", failed_checks.len());
                println!();

                if failed_checks.is_empty() {
                    println!("✅ QUALITY GATE: PASSED");
                    println!("🎉 All quality checks passed through agent delegation");
                    println!("🚀 Code is ready for deployment");
                } else {
                    println!("❌ QUALITY GATE: FAILED");
                    println!("🔧 Some quality checks require attention from agents");
                    println!();
                    println!("📋 Failed Checks:");
                    for (agent, task, _error) in &failed_checks {
                        println!("   ❌ {} agent: {}", agent, task);
                    }
                    return Err(anyhow::anyhow!("Quality gate failed"));
                }
            }

            QualityAction::Format { fix } => {
                println!("🛠️ DevOps Agent - Code Formatting");
                println!("==================================");

                let cmd = if *fix {
                    vec!["cargo", "fmt"]
                } else {
                    vec!["cargo", "fmt", "--check"]
                };

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run cargo fmt")?;

                if output.status.success() {
                    println!(
                        "✅ DevOps Agent: Code formatting {} successfully",
                        if *fix { "applied" } else { "verified" }
                    );
                } else {
                    println!("❌ DevOps Agent: Code formatting issues detected");
                    if !fix {
                        println!("💡 Run with --fix to automatically format code");
                    }
                    return Err(anyhow::anyhow!("Formatting check failed"));
                }
            }

            QualityAction::Lint { fix } => {
                println!("🛠️ DevOps Agent - Clippy Analysis");
                println!("==================================");

                let mut cmd = vec!["cargo", "clippy", "--all-targets", "--all-features"];
                if *fix {
                    cmd.push("--fix");
                    cmd.push("--allow-dirty");
                }
                cmd.extend(&["--", "-D", "warnings"]);

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run cargo clippy")?;

                if output.status.success() {
                    println!("✅ DevOps Agent: Clippy analysis passed");
                } else {
                    println!("❌ DevOps Agent: Clippy found issues");
                    println!("{}", String::from_utf8_lossy(&output.stdout));
                    return Err(anyhow::anyhow!("Clippy check failed"));
                }
            }

            QualityAction::Test {
                pattern,
                unit,
                integration,
                security,
            } => {
                println!("🧪 QA Agent - Test Execution");
                println!("============================");

                let mut cmd = vec!["cargo", "test"];

                if *unit {
                    cmd.push("--lib");
                } else if *integration {
                    cmd.extend(&["--test", "*integration*"]);
                } else if *security {
                    cmd.push("security::owasp_checker::tests");
                }

                if let Some(p) = pattern {
                    cmd.push(p);
                }

                cmd.extend(&["--verbose", "--no-fail-fast"]);

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run tests")?;

                if output.status.success() {
                    println!("✅ QA Agent: All tests passed");
                } else {
                    println!("❌ QA Agent: Some tests failed");
                    println!("{}", String::from_utf8_lossy(&output.stdout));
                    return Err(anyhow::anyhow!("Tests failed"));
                }
            }

            QualityAction::Build {
                release,
                all_targets,
            } => {
                println!("🛠️ DevOps Agent - Build Verification");
                println!("=====================================");

                let mut cmd = vec!["cargo", "build", "--verbose"];

                if *release {
                    cmd.push("--release");
                }
                if *all_targets {
                    cmd.push("--all-targets");
                }

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to build")?;

                if output.status.success() {
                    println!("✅ DevOps Agent: Build completed successfully");
                } else {
                    println!("❌ DevOps Agent: Build failed");
                    println!("{}", String::from_utf8_lossy(&output.stderr));
                    return Err(anyhow::anyhow!("Build failed"));
                }
            }

            QualityAction::Security { audit, deps } => {
                println!("🦀 Backend Agent - Security Analysis");
                println!("====================================");

                if *audit {
                    let output = Command::new("cargo").args(&["audit"]).output();

                    match output {
                        Ok(out) if out.status.success() => {
                            println!("✅ Backend Agent: Security audit passed");
                        }
                        _ => {
                            println!("❌ Backend Agent: Security audit found issues (or cargo-audit not installed)");
                        }
                    }
                }

                if *deps {
                    println!("🔍 Backend Agent: Checking dependencies...");
                    // Run dependency checks
                }

                // Always run security tests
                let output = Command::new("cargo")
                    .args(&["test", "security::owasp_checker::tests", "--no-fail-fast"])
                    .output()
                    .context("Failed to run security tests")?;

                if output.status.success() {
                    println!("✅ Backend Agent: Security tests passed");
                } else {
                    println!("❌ Backend Agent: Security tests failed");
                    return Err(anyhow::anyhow!("Security tests failed"));
                }
            }

            QualityAction::Status { detailed } => {
                println!("📊 Quality Gate Status");
                println!("======================");

                // Run quick checks to show status
                let checks = [
                    ("Format", vec!["cargo", "fmt", "--check"]),
                    ("Clippy", vec!["cargo", "clippy", "--", "-D", "warnings"]),
                    ("Tests", vec!["cargo", "test", "--lib", "--no-run"]),
                    ("Build", vec!["cargo", "check"]),
                ];

                for (name, cmd) in &checks {
                    match Command::new(&cmd[0]).args(&cmd[1..]).output() {
                        Ok(output) => {
                            let status = if output.status.success() {
                                "✅ PASS"
                            } else {
                                "❌ FAIL"
                            };
                            println!("  {}: {}", name, status);

                            if *detailed && !output.status.success() {
                                println!("    Error: {}", String::from_utf8_lossy(&output.stderr));
                            }
                        }
                        Err(_) => {
                            println!("  {}: ❌ FAIL (command error)", name);
                        }
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:3806-4171 method handle_extend <-> ./crates/ccswarm/src/cli/mod.rs:5008-5380 method handle_template
  Similarity: 98.63%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_extend (lines 3806-4171) ---[0m
    async fn handle_extend(&self, action: &ExtendAction) -> Result<()> {
        use crate::extension_stub::meta_learning::MetaLearningSystem;
        use crate::extension_stub::{
            ExtensionManager, ExtensionProposal, ExtensionStatus, ExtensionType,
        };
        use chrono::Utc;

        // Create extension manager instance
        let mut extension_manager = ExtensionManager::new();
        let _meta_learning = MetaLearningSystem::new();

        match action {
            ExtendAction::Propose {
                agent,
                extension_type,
                file,
            } => {
                // Read extension specification from file
                let spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read extension specification file")?;

                // Parse extension type
                let ext_type = match extension_type.as_str() {
                    "capability" => ExtensionType::Capability,
                    "system" => ExtensionType::System,
                    "cognitive" => ExtensionType::Cognitive,
                    "collaborative" => ExtensionType::Collaborative,
                    _ => ExtensionType::Capability, // Default
                };

                // Create extension proposal
                let proposal = ExtensionProposal {
                    id: uuid::Uuid::new_v4().to_string(),
                    agent_id: agent.clone(),
                    proposer: agent.clone(),
                    extension_type: ext_type,
                    status: ExtensionStatus::Proposed,
                    specification: spec_content.clone(),
                    title: format!("{} extension for {}", extension_type, agent),
                    description: spec_content.lines().take(3).collect::<Vec<_>>().join(" "),
                    current_state: crate::extension_stub::CurrentState {
                        capabilities: vec!["basic functionality".to_string()],
                        limitations: vec!["needs enhancement".to_string()],
                        performance_metrics: std::collections::HashMap::new(),
                    },
                    proposed_state: crate::extension_stub::ProposedState {
                        new_capabilities: vec!["enhanced functionality".to_string()],
                        removed_limitations: vec!["slow processing".to_string()],
                        expected_improvements: {
                            let mut map = std::collections::HashMap::new();
                            map.insert("performance".to_string(), 25.0);
                            map
                        },
                        performance_targets: std::collections::HashMap::new(),
                    },
                    implementation_plan: crate::extension_stub::ImplementationPlan {
                        total_duration: std::time::Duration::from_secs(2419200), // 4 weeks
                        phases: vec![
                            crate::extension_stub::ImplementationPhase {
                                name: "Analysis & Design".to_string(),
                                description: "Analyze requirements and design the extension"
                                    .to_string(),
                                tasks: vec![
                                    "Design document".to_string(),
                                    "Technical specification".to_string(),
                                ],
                                validation_method: "Code review".to_string(),
                                estimated_duration: std::time::Duration::from_secs(604800), // 1 week
                                complexity: crate::extension_stub::ComplexityLevel::Medium,
                                dependencies: Vec::new(),
                            },
                            crate::extension_stub::ImplementationPhase {
                                name: "Implementation".to_string(),
                                description: "Implement the extension functionality".to_string(),
                                tasks: vec!["Working code".to_string(), "Unit tests".to_string()],
                                validation_method: "Testing".to_string(),
                                estimated_duration: std::time::Duration::from_secs(1209600), // 2 weeks
                                complexity: crate::extension_stub::ComplexityLevel::High,
                                dependencies: vec!["Analysis & Design".to_string()],
                            },
                            crate::extension_stub::ImplementationPhase {
                                name: "Testing & Deployment".to_string(),
                                description: "Test and deploy the extension".to_string(),
                                tasks: vec![
                                    "Test results".to_string(),
                                    "Deployed extension".to_string(),
                                ],
                                validation_method: "Production testing".to_string(),
                                estimated_duration: std::time::Duration::from_secs(604800), // 1 week
                                complexity: crate::extension_stub::ComplexityLevel::Medium,
                                dependencies: vec!["Implementation".to_string()],
                            },
                        ],
                        timeline: "2-4 weeks".to_string(),
                        resources_required: vec!["development time".to_string()],
                        dependencies: vec![],
                    },
                    risk_assessment: crate::extension_stub::RiskAssessment {
                        risk_level: crate::extension_stub::RiskLevel::Low,
                        potential_issues: vec!["Minor performance impact".to_string()],
                        mitigation_strategies: vec![],
                        rollback_plan: "Revert to previous version".to_string(),
                        overall_risk_score: 0.3,
                        categories: vec![crate::extension_stub::RiskCategory::Performance],
                    },
                    success_criteria: vec![
                        crate::extension_stub::SuccessCriterion {
                            name: "Functionality".to_string(),
                            description: "Extension functionality working correctly".to_string(),
                            target_value: 100.0,
                            measurement_method: "Automated testing".to_string(),
                            measurable: true,
                        },
                        crate::extension_stub::SuccessCriterion {
                            name: "Performance".to_string(),
                            description: "No performance degradation".to_string(),
                            target_value: 100.0, // milliseconds
                            measurement_method: "Performance benchmarks".to_string(),
                            measurable: true,
                        },
                    ],
                    created_at: Utc::now(),
                };

                // Submit proposal to extension manager
                let proposal_id = extension_manager.propose_extension(proposal).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension proposed",
                            "proposal_id": proposal_id,
                            "agent": agent,
                            "type": extension_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!("🔧 Proposing extension for agent: {}", agent);
                    println!("   Type: {}", extension_type);
                    println!("   Specification: {}", file.display());
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Extension proposal submitted");
                }
            }

            ExtendAction::Status {
                agent,
                extension_id,
            } => {
                // Get extension manager statistics
                let stats = extension_manager.get_stats().await;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension status retrieved",
                            "agent": agent,
                            "extension_id": extension_id,
                            "stats": {
                                "total_extensions": stats.total_extensions,
                                "active_extensions": stats.active_extensions,
                                "pending_proposals": stats.pending_proposals,
                                "successful_extensions": stats.successful_extensions,
                                "failed_extensions": stats.failed_extensions,
                            }
                        }))?
                    );
                } else {
                    println!("📊 Extension Status for agent: {}", agent);
                    if let Some(id) = extension_id {
                        println!("🆔 Extension ID: {}", id);
                    }
                    println!("📈 Statistics:");
                    println!("   Total Extensions: {}", stats.total_extensions);
                    println!("   Active: {}", stats.active_extensions);
                    println!("   Pending Proposals: {}", stats.pending_proposals);
                    println!("   Successful: {}", stats.successful_extensions);
                    println!("   Failed: {}", stats.failed_extensions);
                }
            }

            ExtendAction::History {
                agent,
                successful,
                failed,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension history",
                            "agent": agent,
                            "filter": {
                                "successful": successful,
                                "failed": failed,
                            },
                        }))?
                    );
                } else {
                    println!("📜 Extension History for agent: {}", agent);
                    println!("================================");
                    if *successful {
                        println!("Showing only successful extensions");
                    } else if *failed {
                        println!("Showing only failed extensions");
                    }
                    println!("No extensions found");
                }
            }

            ExtendAction::Rollback {
                agent,
                extension_id,
                force,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension rolled back",
                            "agent": agent,
                            "extension_id": extension_id,
                            "force": force,
                        }))?
                    );
                } else {
                    println!("⏪ Rolling back extension: {}", extension_id);
                    println!("   Agent: {}", agent);
                    if *force {
                        println!("   ⚠️ Force rollback enabled");
                    }
                    println!("✅ Rollback completed");
                }
            }

            ExtendAction::Discover {
                agent,
                discovery_type,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Discovery completed",
                            "agent": agent,
                            "discovery_type": discovery_type,
                        }))?
                    );
                } else {
                    println!("🔍 Discovering extension opportunities");
                    if let Some(a) = agent {
                        println!("   Agent: {}", a);
                    }
                    if let Some(dt) = discovery_type {
                        println!("   Type: {}", dt);
                    }
                    println!("✅ Discovery completed");
                }
            }

            ExtendAction::Autonomous {
                agent,
                continuous,
                dry_run,
            } => {
                // use crate::extension::autonomous_agent_extension::AutonomousAgentExtensionManager;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "started",
                            "message": "Autonomous extension reasoning initiated",
                            "agent": agent,
                            "continuous": continuous,
                            "dry_run": dry_run,
                        }))?
                    );
                } else {
                    println!("🤖 Initiating autonomous extension reasoning");
                    if let Some(a) = agent {
                        println!("   Agent: {}", a);
                    } else {
                        println!("   Agents: All agents");
                    }
                    if *continuous {
                        println!(
                            "   Mode: Continuous (agents will autonomously propose extensions)"
                        );
                    }
                    if *dry_run {
                        println!("   🔍 DRY RUN - proposals will not be submitted to Sangha");
                    }
                }

                // Get agent list
                let target_agents = if let Some(agent_id) = agent {
                    vec![agent_id.clone()]
                } else {
                    // Get all agents from config
                    self.config.agents.keys().cloned().collect()
                };

                // Process each agent
                for agent_id in target_agents {
                    if !self.json_output {
                        println!("\n🎯 Processing agent: {}", agent_id);
                    }

                    // Create autonomous extension manager for the agent
                    // In real implementation, would create proper provider and sangha client
                    // For now, just log the intent

                    if *dry_run {
                        if !self.json_output {
                            println!("   📋 Would propose extensions based on:");
                            println!("      - Past performance analysis");
                            println!("      - Identified capability gaps");
                            println!("      - Recurring failure patterns");
                            println!("      - Self-reflection insights");
                        }
                    } else if !self.json_output {
                        println!("   🧠 Analyzing experiences and performance...");
                        println!("   🔍 Identifying capability gaps...");
                        println!("   💡 Generating extension proposals...");
                        println!("   🏛️  Submitting proposals to Sangha for approval...");

                        // In real implementation:
                        // 1. Create AutonomousAgentExtensionManager
                        // 2. Call propose_extensions()
                        // 3. Wait for Sangha consensus
                        // 4. Report results
                    }
                }

                if *continuous && !self.json_output {
                    println!("\n♾️  Continuous mode enabled - agents will autonomously propose extensions as needed");
                    println!("   Press Ctrl+C to stop");
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "completed",
                            "message": "Autonomous extension reasoning completed",
                            "continuous_mode": continuous,
                        }))?
                    );
                } else {
                    println!("\n✅ Autonomous extension reasoning completed");
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_template (lines 5008-5380) ---[0m
    async fn handle_template(&self, action: &TemplateAction) -> Result<()> {
        use crate::template::{
            FileSystemTemplateStorage, PredefinedTemplates, TemplateCategory, TemplateContext,
            TemplateManager, TemplateQuery,
        };
        use colored::Colorize;
        use std::io::{self, Write};
        use std::str::FromStr;

        // Initialize template storage
        let templates_dir = self.repo_path.join(".ccswarm").join("templates");
        let storage = FileSystemTemplateStorage::new(&templates_dir)
            .await
            .context("Failed to initialize template storage")?;
        let mut manager = TemplateManager::new(storage);

        match action {
            TemplateAction::List {
                all: _,
                category,
                tags,
                search,
                popular,
                quality,
                detailed,
            } => {
                let mut query = TemplateQuery::new();

                if let Some(cat_str) = category {
                    let cat = TemplateCategory::from_str(cat_str).context("Invalid category")?;
                    query = query.with_category(cat);
                }

                if !tags.is_empty() {
                    query = query.with_tags(tags.clone());
                }

                if let Some(search_term) = search {
                    query = query.with_search_term(search_term);
                }

                if *popular {
                    query = query.sort_by_popularity();
                } else if *quality {
                    query = query.sort_by_success_rate();
                }

                let templates = manager
                    .search_templates(query)
                    .await
                    .context("Failed to search templates")?;

                if templates.is_empty() {
                    println!("No templates found.");
                    return Ok(());
                }

                println!("Available Templates:");
                println!();

                for template in templates {
                    if *detailed {
                        println!(
                            "📋 {} ({})",
                            template.name.bright_cyan(),
                            template.id.as_str().bright_black()
                        );
                        println!("   Category: {}", template.category);
                        println!("   Description: {}", template.description);
                        if !template.tags.is_empty() {
                            println!(
                                "   Tags: {}",
                                template.tags.join(", ").as_str().bright_black()
                            );
                        }
                        if let Some(author) = &template.author {
                            println!("   Author: {}", author.as_str().bright_black());
                        }
                        println!("   Usage: {} times", template.usage_count);
                        if let Some(rate) = template.success_rate {
                            println!("   Success Rate: {:.1}%", rate * 100.0);
                        }
                        println!();
                    } else {
                        println!(
                            "  {} ({}) - {}",
                            template.name.bright_cyan(),
                            template.id.bright_black(),
                            template.description.chars().take(80).collect::<String>()
                        );
                    }
                }
            }

            TemplateAction::Show {
                template,
                source,
                stats,
            } => {
                let tmpl = manager
                    .get_template_by_name(template)
                    .await
                    .context("Template not found")?;

                println!(
                    "Template: {} ({})",
                    tmpl.name.bright_cyan(),
                    tmpl.id.bright_black()
                );
                println!("Category: {}", tmpl.category);
                println!("Description: {}", tmpl.description);
                println!("Version: {}", tmpl.version);

                if let Some(author) = &tmpl.author {
                    println!("Author: {}", author);
                }

                if !tmpl.tags.is_empty() {
                    println!("Tags: {}", tmpl.tags.join(", "));
                }

                println!("Priority: {:?}", tmpl.default_priority);
                println!("Task Type: {:?}", tmpl.default_task_type);

                if let Some(duration) = tmpl.estimated_duration {
                    println!("Estimated Duration: {} minutes", duration);
                }

                if !tmpl.variables.is_empty() {
                    println!();
                    println!("Variables:");
                    for var in &tmpl.variables {
                        let required = if var.required { " (required)" } else { "" };
                        println!(
                            "  • {}{}: {}",
                            var.name.bright_green(),
                            required,
                            var.description
                        );
                        if let Some(default) = &var.default_value {
                            println!("    Default: {}", default.clone().bright_black());
                        }
                    }
                }

                if *source {
                    println!();
                    println!("Task Description Template:");
                    println!("{}", tmpl.task_description.bright_white());

                    if let Some(details) = &tmpl.task_details {
                        println!();
                        println!("Task Details Template:");
                        println!("{}", details.bright_white());
                    }
                }

                if *stats {
                    println!();
                    println!("Statistics:");
                    println!("  Usage Count: {}", tmpl.usage_count);
                    if let Some(rate) = tmpl.success_rate {
                        println!("  Success Rate: {:.1}%", rate * 100.0);
                    }
                    println!("  Created: {}", tmpl.created_at.format("%Y-%m-%d %H:%M"));
                    println!("  Updated: {}", tmpl.updated_at.format("%Y-%m-%d %H:%M"));
                }
            }

            TemplateAction::Apply {
                template,
                vars,
                interactive,
                preview,
                auto_assign,
            } => {
                let tmpl = manager
                    .get_template_by_name(template)
                    .await
                    .context("Template not found")?;

                let mut context = TemplateContext::new();

                // Parse provided variables
                for var_str in vars {
                    if let Some((key, value)) = var_str.split_once('=') {
                        context = context.with_variable(key.trim(), value.trim());
                    }
                }

                // Interactive mode for missing variables
                if *interactive {
                    for var in &tmpl.variables {
                        if var.required
                            && !context.variables.contains_key(&var.name)
                            && var.default_value.is_none()
                        {
                            print!("{} ({}): ", var.name, var.description);
                            io::stdout().flush()?;

                            let mut input = String::new();
                            io::stdin().read_line(&mut input)?;
                            let value = input.trim();

                            if !value.is_empty() {
                                context = context.with_variable(&var.name, value);
                            }
                        }
                    }
                }

                // Apply template
                let applied = manager
                    .apply_template(&tmpl.id, context)
                    .await
                    .context("Failed to apply template")?;

                if *preview {
                    println!("Preview of generated task:");
                    println!();
                    println!("Description: {}", applied.description.bright_cyan());
                    if let Some(details) = &applied.details {
                        println!("Details: {}", details);
                    }
                    println!("Priority: {:?}", applied.priority);
                    println!("Type: {:?}", applied.task_type);
                    if let Some(duration) = applied.estimated_duration {
                        println!("Duration: {} minutes", duration);
                    }
                    if !applied.target_files.is_empty() {
                        println!("Target Files: {}", applied.target_files.join(", "));
                    }
                } else {
                    // Create the actual task
                    use crate::agent::TaskBuilder;

                    let task = TaskBuilder::new(applied.description.clone())
                        .priority(applied.priority)
                        .task_type(applied.task_type);

                    let task = if let Some(details) = &applied.details {
                        task.details(details.clone())
                    } else {
                        task
                    };

                    let task = if let Some(duration) = applied.estimated_duration {
                        task.estimated_duration(duration as u64)
                    } else {
                        task
                    };

                    let task = task.build();

                    println!(
                        "Created task from template: {}",
                        applied.description.bright_green()
                    );
                    println!("Task ID: {}", task.id.bright_cyan());

                    if *auto_assign {
                        println!("Auto-assigning to best agent...");
                        // TODO: Implement auto-assignment logic
                    }
                }
            }

            TemplateAction::Install {
                all,
                categories,
                force,
            } => {
                let predefined_templates = PredefinedTemplates::get_all();
                let mut installed = 0;
                let mut skipped = 0;

                for template in predefined_templates {
                    // Filter by categories if specified
                    if !categories.is_empty() && !*all {
                        let cat_str = template.category.to_string();
                        if !categories.iter().any(|c| c.eq_ignore_ascii_case(&cat_str)) {
                            continue;
                        }
                    }

                    match manager.save_template(template.clone()).await {
                        Ok(()) => {
                            println!("✅ Installed: {}", template.name.bright_green());
                            installed += 1;
                        }
                        Err(e) if e.to_string().contains("already exists") => {
                            if *force {
                                if let Err(e) = manager.update_template(template.clone()).await {
                                    println!("❌ Failed to update {}: {}", template.name.red(), e);
                                } else {
                                    println!("✅ Updated: {}", template.name.bright_green());
                                    installed += 1;
                                }
                            } else {
                                println!("⚠️  Skipped (exists): {}", template.name.bright_yellow());
                                skipped += 1;
                            }
                        }
                        Err(e) => {
                            println!("❌ Failed to install {}: {}", template.name.red(), e);
                        }
                    }
                }

                println!();
                println!(
                    "Installation complete: {} installed, {} skipped",
                    installed, skipped
                );
                if skipped > 0 && !*force {
                    println!("Use --force to overwrite existing templates");
                }
            }

            TemplateAction::Stats {
                global: _,
                template,
            } => {
                if let Some(tmpl_name) = template {
                    let tmpl = manager
                        .get_template_by_name(tmpl_name)
                        .await
                        .context("Template not found")?;

                    println!("Template Statistics: {}", tmpl.name.bright_cyan());
                    println!("Usage Count: {}", tmpl.usage_count);
                    if let Some(rate) = tmpl.success_rate {
                        println!("Success Rate: {:.1}%", rate * 100.0);
                    }
                    println!("Created: {}", tmpl.created_at.format("%Y-%m-%d %H:%M"));
                    println!("Updated: {}", tmpl.updated_at.format("%Y-%m-%d %H:%M"));
                } else {
                    let stats = manager
                        .get_template_stats()
                        .await
                        .context("Failed to get template statistics")?;

                    println!("Global Template Statistics:");
                    println!("Total Templates: {}", stats.total_templates);
                    println!("Total Usage: {}", stats.total_usage);
                    println!(
                        "Average Success Rate: {:.1}%",
                        stats.average_success_rate * 100.0
                    );

                    println!();
                    println!("By Category:");
                    for (category, count) in &stats.by_category {
                        println!("  {}: {}", category, count);
                    }

                    if !stats.most_popular.is_empty() {
                        println!();
                        println!("Most Popular:");
                        for (name, count) in stats.most_popular.iter().take(5) {
                            println!("  {}: {} uses", name, count);
                        }
                    }
                }
            }

            _ => {
                println!("Template command not yet implemented: {:?}", action);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:4685-5006 method handle_quality <-> ./crates/ccswarm/src/cli/mod.rs:5008-5380 method handle_template
  Similarity: 98.22%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_quality (lines 4685-5006) ---[0m
    async fn handle_quality(&self, action: &QualityAction) -> Result<()> {
        use std::process::Command;

        let mut failed_checks = Vec::new();
        let mut completed_tasks = 0;
        let total_tasks;

        match action {
            QualityAction::Check {
                skip,
                only,
                fail_fast,
            } => {
                println!("🤖 ccswarm Agent-Managed Quality Checks");
                println!("=======================================");
                println!();

                let checks = if only.is_empty() {
                    vec!["format", "lint", "test", "build", "security"]
                } else {
                    only.iter().map(|s| s.as_str()).collect()
                };

                let filtered_checks: Vec<&str> = checks
                    .into_iter()
                    .filter(|check| !skip.contains(&check.to_string()))
                    .collect();

                total_tasks = filtered_checks.len();
                println!("🎯 Master Claude: Orchestrating {} quality checks through specialized agents...", total_tasks);
                println!();

                for check in filtered_checks {
                    let (agent, task, cmd) = match check {
                        "format" => (
                            "DevOps",
                            "Code Formatting Check",
                            vec!["cargo", "fmt", "--check"],
                        ),
                        "lint" => (
                            "DevOps",
                            "Clippy Code Quality Analysis",
                            vec![
                                "cargo",
                                "clippy",
                                "--all-targets",
                                "--all-features",
                                "--",
                                "-D",
                                "warnings",
                            ],
                        ),
                        "test" => (
                            "QA",
                            "Test Suite Execution",
                            vec!["cargo", "test", "--lib", "--verbose", "--no-fail-fast"],
                        ),
                        "build" => (
                            "DevOps",
                            "Build Verification",
                            vec!["cargo", "build", "--verbose"],
                        ),
                        "security" => (
                            "Backend",
                            "Security Analysis",
                            vec![
                                "cargo",
                                "test",
                                "security::owasp_checker::tests",
                                "--no-fail-fast",
                            ],
                        ),
                        _ => continue,
                    };

                    println!("🎯 Delegating to {} agent: {}", agent, task);

                    let mut command = Command::new(&cmd[0]);
                    for arg in &cmd[1..] {
                        command.arg(arg);
                    }

                    let output = command.output().context("Failed to execute command")?;
                    let success = output.status.success();

                    if success {
                        println!("✅ {} agent: {} completed successfully", agent, task);
                        completed_tasks += 1;
                    } else {
                        println!("❌ {} agent: {} failed", agent, task);
                        failed_checks.push((
                            agent,
                            task,
                            String::from_utf8_lossy(&output.stderr).to_string(),
                        ));

                        if *fail_fast {
                            break;
                        }
                    }
                    println!();
                }

                // Quality Gate Assessment
                println!("🎯 Master Claude - Quality Gate Assessment");
                println!("==========================================");
                println!("📊 Agent Task Completion Summary:");
                println!("   Completed: {}/{} tasks", completed_tasks, total_tasks);
                println!("   Failed: {}", failed_checks.len());
                println!();

                if failed_checks.is_empty() {
                    println!("✅ QUALITY GATE: PASSED");
                    println!("🎉 All quality checks passed through agent delegation");
                    println!("🚀 Code is ready for deployment");
                } else {
                    println!("❌ QUALITY GATE: FAILED");
                    println!("🔧 Some quality checks require attention from agents");
                    println!();
                    println!("📋 Failed Checks:");
                    for (agent, task, _error) in &failed_checks {
                        println!("   ❌ {} agent: {}", agent, task);
                    }
                    return Err(anyhow::anyhow!("Quality gate failed"));
                }
            }

            QualityAction::Format { fix } => {
                println!("🛠️ DevOps Agent - Code Formatting");
                println!("==================================");

                let cmd = if *fix {
                    vec!["cargo", "fmt"]
                } else {
                    vec!["cargo", "fmt", "--check"]
                };

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run cargo fmt")?;

                if output.status.success() {
                    println!(
                        "✅ DevOps Agent: Code formatting {} successfully",
                        if *fix { "applied" } else { "verified" }
                    );
                } else {
                    println!("❌ DevOps Agent: Code formatting issues detected");
                    if !fix {
                        println!("💡 Run with --fix to automatically format code");
                    }
                    return Err(anyhow::anyhow!("Formatting check failed"));
                }
            }

            QualityAction::Lint { fix } => {
                println!("🛠️ DevOps Agent - Clippy Analysis");
                println!("==================================");

                let mut cmd = vec!["cargo", "clippy", "--all-targets", "--all-features"];
                if *fix {
                    cmd.push("--fix");
                    cmd.push("--allow-dirty");
                }
                cmd.extend(&["--", "-D", "warnings"]);

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run cargo clippy")?;

                if output.status.success() {
                    println!("✅ DevOps Agent: Clippy analysis passed");
                } else {
                    println!("❌ DevOps Agent: Clippy found issues");
                    println!("{}", String::from_utf8_lossy(&output.stdout));
                    return Err(anyhow::anyhow!("Clippy check failed"));
                }
            }

            QualityAction::Test {
                pattern,
                unit,
                integration,
                security,
            } => {
                println!("🧪 QA Agent - Test Execution");
                println!("============================");

                let mut cmd = vec!["cargo", "test"];

                if *unit {
                    cmd.push("--lib");
                } else if *integration {
                    cmd.extend(&["--test", "*integration*"]);
                } else if *security {
                    cmd.push("security::owasp_checker::tests");
                }

                if let Some(p) = pattern {
                    cmd.push(p);
                }

                cmd.extend(&["--verbose", "--no-fail-fast"]);

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run tests")?;

                if output.status.success() {
                    println!("✅ QA Agent: All tests passed");
                } else {
                    println!("❌ QA Agent: Some tests failed");
                    println!("{}", String::from_utf8_lossy(&output.stdout));
                    return Err(anyhow::anyhow!("Tests failed"));
                }
            }

            QualityAction::Build {
                release,
                all_targets,
            } => {
                println!("🛠️ DevOps Agent - Build Verification");
                println!("=====================================");

                let mut cmd = vec!["cargo", "build", "--verbose"];

                if *release {
                    cmd.push("--release");
                }
                if *all_targets {
                    cmd.push("--all-targets");
                }

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to build")?;

                if output.status.success() {
                    println!("✅ DevOps Agent: Build completed successfully");
                } else {
                    println!("❌ DevOps Agent: Build failed");
                    println!("{}", String::from_utf8_lossy(&output.stderr));
                    return Err(anyhow::anyhow!("Build failed"));
                }
            }

            QualityAction::Security { audit, deps } => {
                println!("🦀 Backend Agent - Security Analysis");
                println!("====================================");

                if *audit {
                    let output = Command::new("cargo").args(&["audit"]).output();

                    match output {
                        Ok(out) if out.status.success() => {
                            println!("✅ Backend Agent: Security audit passed");
                        }
                        _ => {
                            println!("❌ Backend Agent: Security audit found issues (or cargo-audit not installed)");
                        }
                    }
                }

                if *deps {
                    println!("🔍 Backend Agent: Checking dependencies...");
                    // Run dependency checks
                }

                // Always run security tests
                let output = Command::new("cargo")
                    .args(&["test", "security::owasp_checker::tests", "--no-fail-fast"])
                    .output()
                    .context("Failed to run security tests")?;

                if output.status.success() {
                    println!("✅ Backend Agent: Security tests passed");
                } else {
                    println!("❌ Backend Agent: Security tests failed");
                    return Err(anyhow::anyhow!("Security tests failed"));
                }
            }

            QualityAction::Status { detailed } => {
                println!("📊 Quality Gate Status");
                println!("======================");

                // Run quick checks to show status
                let checks = [
                    ("Format", vec!["cargo", "fmt", "--check"]),
                    ("Clippy", vec!["cargo", "clippy", "--", "-D", "warnings"]),
                    ("Tests", vec!["cargo", "test", "--lib", "--no-run"]),
                    ("Build", vec!["cargo", "check"]),
                ];

                for (name, cmd) in &checks {
                    match Command::new(&cmd[0]).args(&cmd[1..]).output() {
                        Ok(output) => {
                            let status = if output.status.success() {
                                "✅ PASS"
                            } else {
                                "❌ FAIL"
                            };
                            println!("  {}: {}", name, status);

                            if *detailed && !output.status.success() {
                                println!("    Error: {}", String::from_utf8_lossy(&output.stderr));
                            }
                        }
                        Err(_) => {
                            println!("  {}: ❌ FAIL (command error)", name);
                        }
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_template (lines 5008-5380) ---[0m
    async fn handle_template(&self, action: &TemplateAction) -> Result<()> {
        use crate::template::{
            FileSystemTemplateStorage, PredefinedTemplates, TemplateCategory, TemplateContext,
            TemplateManager, TemplateQuery,
        };
        use colored::Colorize;
        use std::io::{self, Write};
        use std::str::FromStr;

        // Initialize template storage
        let templates_dir = self.repo_path.join(".ccswarm").join("templates");
        let storage = FileSystemTemplateStorage::new(&templates_dir)
            .await
            .context("Failed to initialize template storage")?;
        let mut manager = TemplateManager::new(storage);

        match action {
            TemplateAction::List {
                all: _,
                category,
                tags,
                search,
                popular,
                quality,
                detailed,
            } => {
                let mut query = TemplateQuery::new();

                if let Some(cat_str) = category {
                    let cat = TemplateCategory::from_str(cat_str).context("Invalid category")?;
                    query = query.with_category(cat);
                }

                if !tags.is_empty() {
                    query = query.with_tags(tags.clone());
                }

                if let Some(search_term) = search {
                    query = query.with_search_term(search_term);
                }

                if *popular {
                    query = query.sort_by_popularity();
                } else if *quality {
                    query = query.sort_by_success_rate();
                }

                let templates = manager
                    .search_templates(query)
                    .await
                    .context("Failed to search templates")?;

                if templates.is_empty() {
                    println!("No templates found.");
                    return Ok(());
                }

                println!("Available Templates:");
                println!();

                for template in templates {
                    if *detailed {
                        println!(
                            "📋 {} ({})",
                            template.name.bright_cyan(),
                            template.id.as_str().bright_black()
                        );
                        println!("   Category: {}", template.category);
                        println!("   Description: {}", template.description);
                        if !template.tags.is_empty() {
                            println!(
                                "   Tags: {}",
                                template.tags.join(", ").as_str().bright_black()
                            );
                        }
                        if let Some(author) = &template.author {
                            println!("   Author: {}", author.as_str().bright_black());
                        }
                        println!("   Usage: {} times", template.usage_count);
                        if let Some(rate) = template.success_rate {
                            println!("   Success Rate: {:.1}%", rate * 100.0);
                        }
                        println!();
                    } else {
                        println!(
                            "  {} ({}) - {}",
                            template.name.bright_cyan(),
                            template.id.bright_black(),
                            template.description.chars().take(80).collect::<String>()
                        );
                    }
                }
            }

            TemplateAction::Show {
                template,
                source,
                stats,
            } => {
                let tmpl = manager
                    .get_template_by_name(template)
                    .await
                    .context("Template not found")?;

                println!(
                    "Template: {} ({})",
                    tmpl.name.bright_cyan(),
                    tmpl.id.bright_black()
                );
                println!("Category: {}", tmpl.category);
                println!("Description: {}", tmpl.description);
                println!("Version: {}", tmpl.version);

                if let Some(author) = &tmpl.author {
                    println!("Author: {}", author);
                }

                if !tmpl.tags.is_empty() {
                    println!("Tags: {}", tmpl.tags.join(", "));
                }

                println!("Priority: {:?}", tmpl.default_priority);
                println!("Task Type: {:?}", tmpl.default_task_type);

                if let Some(duration) = tmpl.estimated_duration {
                    println!("Estimated Duration: {} minutes", duration);
                }

                if !tmpl.variables.is_empty() {
                    println!();
                    println!("Variables:");
                    for var in &tmpl.variables {
                        let required = if var.required { " (required)" } else { "" };
                        println!(
                            "  • {}{}: {}",
                            var.name.bright_green(),
                            required,
                            var.description
                        );
                        if let Some(default) = &var.default_value {
                            println!("    Default: {}", default.clone().bright_black());
                        }
                    }
                }

                if *source {
                    println!();
                    println!("Task Description Template:");
                    println!("{}", tmpl.task_description.bright_white());

                    if let Some(details) = &tmpl.task_details {
                        println!();
                        println!("Task Details Template:");
                        println!("{}", details.bright_white());
                    }
                }

                if *stats {
                    println!();
                    println!("Statistics:");
                    println!("  Usage Count: {}", tmpl.usage_count);
                    if let Some(rate) = tmpl.success_rate {
                        println!("  Success Rate: {:.1}%", rate * 100.0);
                    }
                    println!("  Created: {}", tmpl.created_at.format("%Y-%m-%d %H:%M"));
                    println!("  Updated: {}", tmpl.updated_at.format("%Y-%m-%d %H:%M"));
                }
            }

            TemplateAction::Apply {
                template,
                vars,
                interactive,
                preview,
                auto_assign,
            } => {
                let tmpl = manager
                    .get_template_by_name(template)
                    .await
                    .context("Template not found")?;

                let mut context = TemplateContext::new();

                // Parse provided variables
                for var_str in vars {
                    if let Some((key, value)) = var_str.split_once('=') {
                        context = context.with_variable(key.trim(), value.trim());
                    }
                }

                // Interactive mode for missing variables
                if *interactive {
                    for var in &tmpl.variables {
                        if var.required
                            && !context.variables.contains_key(&var.name)
                            && var.default_value.is_none()
                        {
                            print!("{} ({}): ", var.name, var.description);
                            io::stdout().flush()?;

                            let mut input = String::new();
                            io::stdin().read_line(&mut input)?;
                            let value = input.trim();

                            if !value.is_empty() {
                                context = context.with_variable(&var.name, value);
                            }
                        }
                    }
                }

                // Apply template
                let applied = manager
                    .apply_template(&tmpl.id, context)
                    .await
                    .context("Failed to apply template")?;

                if *preview {
                    println!("Preview of generated task:");
                    println!();
                    println!("Description: {}", applied.description.bright_cyan());
                    if let Some(details) = &applied.details {
                        println!("Details: {}", details);
                    }
                    println!("Priority: {:?}", applied.priority);
                    println!("Type: {:?}", applied.task_type);
                    if let Some(duration) = applied.estimated_duration {
                        println!("Duration: {} minutes", duration);
                    }
                    if !applied.target_files.is_empty() {
                        println!("Target Files: {}", applied.target_files.join(", "));
                    }
                } else {
                    // Create the actual task
                    use crate::agent::TaskBuilder;

                    let task = TaskBuilder::new(applied.description.clone())
                        .priority(applied.priority)
                        .task_type(applied.task_type);

                    let task = if let Some(details) = &applied.details {
                        task.details(details.clone())
                    } else {
                        task
                    };

                    let task = if let Some(duration) = applied.estimated_duration {
                        task.estimated_duration(duration as u64)
                    } else {
                        task
                    };

                    let task = task.build();

                    println!(
                        "Created task from template: {}",
                        applied.description.bright_green()
                    );
                    println!("Task ID: {}", task.id.bright_cyan());

                    if *auto_assign {
                        println!("Auto-assigning to best agent...");
                        // TODO: Implement auto-assignment logic
                    }
                }
            }

            TemplateAction::Install {
                all,
                categories,
                force,
            } => {
                let predefined_templates = PredefinedTemplates::get_all();
                let mut installed = 0;
                let mut skipped = 0;

                for template in predefined_templates {
                    // Filter by categories if specified
                    if !categories.is_empty() && !*all {
                        let cat_str = template.category.to_string();
                        if !categories.iter().any(|c| c.eq_ignore_ascii_case(&cat_str)) {
                            continue;
                        }
                    }

                    match manager.save_template(template.clone()).await {
                        Ok(()) => {
                            println!("✅ Installed: {}", template.name.bright_green());
                            installed += 1;
                        }
                        Err(e) if e.to_string().contains("already exists") => {
                            if *force {
                                if let Err(e) = manager.update_template(template.clone()).await {
                                    println!("❌ Failed to update {}: {}", template.name.red(), e);
                                } else {
                                    println!("✅ Updated: {}", template.name.bright_green());
                                    installed += 1;
                                }
                            } else {
                                println!("⚠️  Skipped (exists): {}", template.name.bright_yellow());
                                skipped += 1;
                            }
                        }
                        Err(e) => {
                            println!("❌ Failed to install {}: {}", template.name.red(), e);
                        }
                    }
                }

                println!();
                println!(
                    "Installation complete: {} installed, {} skipped",
                    installed, skipped
                );
                if skipped > 0 && !*force {
                    println!("Use --force to overwrite existing templates");
                }
            }

            TemplateAction::Stats {
                global: _,
                template,
            } => {
                if let Some(tmpl_name) = template {
                    let tmpl = manager
                        .get_template_by_name(tmpl_name)
                        .await
                        .context("Template not found")?;

                    println!("Template Statistics: {}", tmpl.name.bright_cyan());
                    println!("Usage Count: {}", tmpl.usage_count);
                    if let Some(rate) = tmpl.success_rate {
                        println!("Success Rate: {:.1}%", rate * 100.0);
                    }
                    println!("Created: {}", tmpl.created_at.format("%Y-%m-%d %H:%M"));
                    println!("Updated: {}", tmpl.updated_at.format("%Y-%m-%d %H:%M"));
                } else {
                    let stats = manager
                        .get_template_stats()
                        .await
                        .context("Failed to get template statistics")?;

                    println!("Global Template Statistics:");
                    println!("Total Templates: {}", stats.total_templates);
                    println!("Total Usage: {}", stats.total_usage);
                    println!(
                        "Average Success Rate: {:.1}%",
                        stats.average_success_rate * 100.0
                    );

                    println!();
                    println!("By Category:");
                    for (category, count) in &stats.by_category {
                        println!("  {}: {}", category, count);
                    }

                    if !stats.most_popular.is_empty() {
                        println!();
                        println!("Most Popular:");
                        for (name, count) in stats.most_popular.iter().take(5) {
                            println!("  {}: {} uses", name, count);
                        }
                    }
                }
            }

            _ => {
                println!("Template command not yet implemented: {:?}", action);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:3806-4171 method handle_extend <-> ./crates/ccswarm/src/cli/mod.rs:4685-5006 method handle_quality
  Similarity: 98.62%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_extend (lines 3806-4171) ---[0m
    async fn handle_extend(&self, action: &ExtendAction) -> Result<()> {
        use crate::extension_stub::meta_learning::MetaLearningSystem;
        use crate::extension_stub::{
            ExtensionManager, ExtensionProposal, ExtensionStatus, ExtensionType,
        };
        use chrono::Utc;

        // Create extension manager instance
        let mut extension_manager = ExtensionManager::new();
        let _meta_learning = MetaLearningSystem::new();

        match action {
            ExtendAction::Propose {
                agent,
                extension_type,
                file,
            } => {
                // Read extension specification from file
                let spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read extension specification file")?;

                // Parse extension type
                let ext_type = match extension_type.as_str() {
                    "capability" => ExtensionType::Capability,
                    "system" => ExtensionType::System,
                    "cognitive" => ExtensionType::Cognitive,
                    "collaborative" => ExtensionType::Collaborative,
                    _ => ExtensionType::Capability, // Default
                };

                // Create extension proposal
                let proposal = ExtensionProposal {
                    id: uuid::Uuid::new_v4().to_string(),
                    agent_id: agent.clone(),
                    proposer: agent.clone(),
                    extension_type: ext_type,
                    status: ExtensionStatus::Proposed,
                    specification: spec_content.clone(),
                    title: format!("{} extension for {}", extension_type, agent),
                    description: spec_content.lines().take(3).collect::<Vec<_>>().join(" "),
                    current_state: crate::extension_stub::CurrentState {
                        capabilities: vec!["basic functionality".to_string()],
                        limitations: vec!["needs enhancement".to_string()],
                        performance_metrics: std::collections::HashMap::new(),
                    },
                    proposed_state: crate::extension_stub::ProposedState {
                        new_capabilities: vec!["enhanced functionality".to_string()],
                        removed_limitations: vec!["slow processing".to_string()],
                        expected_improvements: {
                            let mut map = std::collections::HashMap::new();
                            map.insert("performance".to_string(), 25.0);
                            map
                        },
                        performance_targets: std::collections::HashMap::new(),
                    },
                    implementation_plan: crate::extension_stub::ImplementationPlan {
                        total_duration: std::time::Duration::from_secs(2419200), // 4 weeks
                        phases: vec![
                            crate::extension_stub::ImplementationPhase {
                                name: "Analysis & Design".to_string(),
                                description: "Analyze requirements and design the extension"
                                    .to_string(),
                                tasks: vec![
                                    "Design document".to_string(),
                                    "Technical specification".to_string(),
                                ],
                                validation_method: "Code review".to_string(),
                                estimated_duration: std::time::Duration::from_secs(604800), // 1 week
                                complexity: crate::extension_stub::ComplexityLevel::Medium,
                                dependencies: Vec::new(),
                            },
                            crate::extension_stub::ImplementationPhase {
                                name: "Implementation".to_string(),
                                description: "Implement the extension functionality".to_string(),
                                tasks: vec!["Working code".to_string(), "Unit tests".to_string()],
                                validation_method: "Testing".to_string(),
                                estimated_duration: std::time::Duration::from_secs(1209600), // 2 weeks
                                complexity: crate::extension_stub::ComplexityLevel::High,
                                dependencies: vec!["Analysis & Design".to_string()],
                            },
                            crate::extension_stub::ImplementationPhase {
                                name: "Testing & Deployment".to_string(),
                                description: "Test and deploy the extension".to_string(),
                                tasks: vec![
                                    "Test results".to_string(),
                                    "Deployed extension".to_string(),
                                ],
                                validation_method: "Production testing".to_string(),
                                estimated_duration: std::time::Duration::from_secs(604800), // 1 week
                                complexity: crate::extension_stub::ComplexityLevel::Medium,
                                dependencies: vec!["Implementation".to_string()],
                            },
                        ],
                        timeline: "2-4 weeks".to_string(),
                        resources_required: vec!["development time".to_string()],
                        dependencies: vec![],
                    },
                    risk_assessment: crate::extension_stub::RiskAssessment {
                        risk_level: crate::extension_stub::RiskLevel::Low,
                        potential_issues: vec!["Minor performance impact".to_string()],
                        mitigation_strategies: vec![],
                        rollback_plan: "Revert to previous version".to_string(),
                        overall_risk_score: 0.3,
                        categories: vec![crate::extension_stub::RiskCategory::Performance],
                    },
                    success_criteria: vec![
                        crate::extension_stub::SuccessCriterion {
                            name: "Functionality".to_string(),
                            description: "Extension functionality working correctly".to_string(),
                            target_value: 100.0,
                            measurement_method: "Automated testing".to_string(),
                            measurable: true,
                        },
                        crate::extension_stub::SuccessCriterion {
                            name: "Performance".to_string(),
                            description: "No performance degradation".to_string(),
                            target_value: 100.0, // milliseconds
                            measurement_method: "Performance benchmarks".to_string(),
                            measurable: true,
                        },
                    ],
                    created_at: Utc::now(),
                };

                // Submit proposal to extension manager
                let proposal_id = extension_manager.propose_extension(proposal).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension proposed",
                            "proposal_id": proposal_id,
                            "agent": agent,
                            "type": extension_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!("🔧 Proposing extension for agent: {}", agent);
                    println!("   Type: {}", extension_type);
                    println!("   Specification: {}", file.display());
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Extension proposal submitted");
                }
            }

            ExtendAction::Status {
                agent,
                extension_id,
            } => {
                // Get extension manager statistics
                let stats = extension_manager.get_stats().await;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension status retrieved",
                            "agent": agent,
                            "extension_id": extension_id,
                            "stats": {
                                "total_extensions": stats.total_extensions,
                                "active_extensions": stats.active_extensions,
                                "pending_proposals": stats.pending_proposals,
                                "successful_extensions": stats.successful_extensions,
                                "failed_extensions": stats.failed_extensions,
                            }
                        }))?
                    );
                } else {
                    println!("📊 Extension Status for agent: {}", agent);
                    if let Some(id) = extension_id {
                        println!("🆔 Extension ID: {}", id);
                    }
                    println!("📈 Statistics:");
                    println!("   Total Extensions: {}", stats.total_extensions);
                    println!("   Active: {}", stats.active_extensions);
                    println!("   Pending Proposals: {}", stats.pending_proposals);
                    println!("   Successful: {}", stats.successful_extensions);
                    println!("   Failed: {}", stats.failed_extensions);
                }
            }

            ExtendAction::History {
                agent,
                successful,
                failed,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension history",
                            "agent": agent,
                            "filter": {
                                "successful": successful,
                                "failed": failed,
                            },
                        }))?
                    );
                } else {
                    println!("📜 Extension History for agent: {}", agent);
                    println!("================================");
                    if *successful {
                        println!("Showing only successful extensions");
                    } else if *failed {
                        println!("Showing only failed extensions");
                    }
                    println!("No extensions found");
                }
            }

            ExtendAction::Rollback {
                agent,
                extension_id,
                force,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension rolled back",
                            "agent": agent,
                            "extension_id": extension_id,
                            "force": force,
                        }))?
                    );
                } else {
                    println!("⏪ Rolling back extension: {}", extension_id);
                    println!("   Agent: {}", agent);
                    if *force {
                        println!("   ⚠️ Force rollback enabled");
                    }
                    println!("✅ Rollback completed");
                }
            }

            ExtendAction::Discover {
                agent,
                discovery_type,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Discovery completed",
                            "agent": agent,
                            "discovery_type": discovery_type,
                        }))?
                    );
                } else {
                    println!("🔍 Discovering extension opportunities");
                    if let Some(a) = agent {
                        println!("   Agent: {}", a);
                    }
                    if let Some(dt) = discovery_type {
                        println!("   Type: {}", dt);
                    }
                    println!("✅ Discovery completed");
                }
            }

            ExtendAction::Autonomous {
                agent,
                continuous,
                dry_run,
            } => {
                // use crate::extension::autonomous_agent_extension::AutonomousAgentExtensionManager;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "started",
                            "message": "Autonomous extension reasoning initiated",
                            "agent": agent,
                            "continuous": continuous,
                            "dry_run": dry_run,
                        }))?
                    );
                } else {
                    println!("🤖 Initiating autonomous extension reasoning");
                    if let Some(a) = agent {
                        println!("   Agent: {}", a);
                    } else {
                        println!("   Agents: All agents");
                    }
                    if *continuous {
                        println!(
                            "   Mode: Continuous (agents will autonomously propose extensions)"
                        );
                    }
                    if *dry_run {
                        println!("   🔍 DRY RUN - proposals will not be submitted to Sangha");
                    }
                }

                // Get agent list
                let target_agents = if let Some(agent_id) = agent {
                    vec![agent_id.clone()]
                } else {
                    // Get all agents from config
                    self.config.agents.keys().cloned().collect()
                };

                // Process each agent
                for agent_id in target_agents {
                    if !self.json_output {
                        println!("\n🎯 Processing agent: {}", agent_id);
                    }

                    // Create autonomous extension manager for the agent
                    // In real implementation, would create proper provider and sangha client
                    // For now, just log the intent

                    if *dry_run {
                        if !self.json_output {
                            println!("   📋 Would propose extensions based on:");
                            println!("      - Past performance analysis");
                            println!("      - Identified capability gaps");
                            println!("      - Recurring failure patterns");
                            println!("      - Self-reflection insights");
                        }
                    } else if !self.json_output {
                        println!("   🧠 Analyzing experiences and performance...");
                        println!("   🔍 Identifying capability gaps...");
                        println!("   💡 Generating extension proposals...");
                        println!("   🏛️  Submitting proposals to Sangha for approval...");

                        // In real implementation:
                        // 1. Create AutonomousAgentExtensionManager
                        // 2. Call propose_extensions()
                        // 3. Wait for Sangha consensus
                        // 4. Report results
                    }
                }

                if *continuous && !self.json_output {
                    println!("\n♾️  Continuous mode enabled - agents will autonomously propose extensions as needed");
                    println!("   Press Ctrl+C to stop");
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "completed",
                            "message": "Autonomous extension reasoning completed",
                            "continuous_mode": continuous,
                        }))?
                    );
                } else {
                    println!("\n✅ Autonomous extension reasoning completed");
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_quality (lines 4685-5006) ---[0m
    async fn handle_quality(&self, action: &QualityAction) -> Result<()> {
        use std::process::Command;

        let mut failed_checks = Vec::new();
        let mut completed_tasks = 0;
        let total_tasks;

        match action {
            QualityAction::Check {
                skip,
                only,
                fail_fast,
            } => {
                println!("🤖 ccswarm Agent-Managed Quality Checks");
                println!("=======================================");
                println!();

                let checks = if only.is_empty() {
                    vec!["format", "lint", "test", "build", "security"]
                } else {
                    only.iter().map(|s| s.as_str()).collect()
                };

                let filtered_checks: Vec<&str> = checks
                    .into_iter()
                    .filter(|check| !skip.contains(&check.to_string()))
                    .collect();

                total_tasks = filtered_checks.len();
                println!("🎯 Master Claude: Orchestrating {} quality checks through specialized agents...", total_tasks);
                println!();

                for check in filtered_checks {
                    let (agent, task, cmd) = match check {
                        "format" => (
                            "DevOps",
                            "Code Formatting Check",
                            vec!["cargo", "fmt", "--check"],
                        ),
                        "lint" => (
                            "DevOps",
                            "Clippy Code Quality Analysis",
                            vec![
                                "cargo",
                                "clippy",
                                "--all-targets",
                                "--all-features",
                                "--",
                                "-D",
                                "warnings",
                            ],
                        ),
                        "test" => (
                            "QA",
                            "Test Suite Execution",
                            vec!["cargo", "test", "--lib", "--verbose", "--no-fail-fast"],
                        ),
                        "build" => (
                            "DevOps",
                            "Build Verification",
                            vec!["cargo", "build", "--verbose"],
                        ),
                        "security" => (
                            "Backend",
                            "Security Analysis",
                            vec![
                                "cargo",
                                "test",
                                "security::owasp_checker::tests",
                                "--no-fail-fast",
                            ],
                        ),
                        _ => continue,
                    };

                    println!("🎯 Delegating to {} agent: {}", agent, task);

                    let mut command = Command::new(&cmd[0]);
                    for arg in &cmd[1..] {
                        command.arg(arg);
                    }

                    let output = command.output().context("Failed to execute command")?;
                    let success = output.status.success();

                    if success {
                        println!("✅ {} agent: {} completed successfully", agent, task);
                        completed_tasks += 1;
                    } else {
                        println!("❌ {} agent: {} failed", agent, task);
                        failed_checks.push((
                            agent,
                            task,
                            String::from_utf8_lossy(&output.stderr).to_string(),
                        ));

                        if *fail_fast {
                            break;
                        }
                    }
                    println!();
                }

                // Quality Gate Assessment
                println!("🎯 Master Claude - Quality Gate Assessment");
                println!("==========================================");
                println!("📊 Agent Task Completion Summary:");
                println!("   Completed: {}/{} tasks", completed_tasks, total_tasks);
                println!("   Failed: {}", failed_checks.len());
                println!();

                if failed_checks.is_empty() {
                    println!("✅ QUALITY GATE: PASSED");
                    println!("🎉 All quality checks passed through agent delegation");
                    println!("🚀 Code is ready for deployment");
                } else {
                    println!("❌ QUALITY GATE: FAILED");
                    println!("🔧 Some quality checks require attention from agents");
                    println!();
                    println!("📋 Failed Checks:");
                    for (agent, task, _error) in &failed_checks {
                        println!("   ❌ {} agent: {}", agent, task);
                    }
                    return Err(anyhow::anyhow!("Quality gate failed"));
                }
            }

            QualityAction::Format { fix } => {
                println!("🛠️ DevOps Agent - Code Formatting");
                println!("==================================");

                let cmd = if *fix {
                    vec!["cargo", "fmt"]
                } else {
                    vec!["cargo", "fmt", "--check"]
                };

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run cargo fmt")?;

                if output.status.success() {
                    println!(
                        "✅ DevOps Agent: Code formatting {} successfully",
                        if *fix { "applied" } else { "verified" }
                    );
                } else {
                    println!("❌ DevOps Agent: Code formatting issues detected");
                    if !fix {
                        println!("💡 Run with --fix to automatically format code");
                    }
                    return Err(anyhow::anyhow!("Formatting check failed"));
                }
            }

            QualityAction::Lint { fix } => {
                println!("🛠️ DevOps Agent - Clippy Analysis");
                println!("==================================");

                let mut cmd = vec!["cargo", "clippy", "--all-targets", "--all-features"];
                if *fix {
                    cmd.push("--fix");
                    cmd.push("--allow-dirty");
                }
                cmd.extend(&["--", "-D", "warnings"]);

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run cargo clippy")?;

                if output.status.success() {
                    println!("✅ DevOps Agent: Clippy analysis passed");
                } else {
                    println!("❌ DevOps Agent: Clippy found issues");
                    println!("{}", String::from_utf8_lossy(&output.stdout));
                    return Err(anyhow::anyhow!("Clippy check failed"));
                }
            }

            QualityAction::Test {
                pattern,
                unit,
                integration,
                security,
            } => {
                println!("🧪 QA Agent - Test Execution");
                println!("============================");

                let mut cmd = vec!["cargo", "test"];

                if *unit {
                    cmd.push("--lib");
                } else if *integration {
                    cmd.extend(&["--test", "*integration*"]);
                } else if *security {
                    cmd.push("security::owasp_checker::tests");
                }

                if let Some(p) = pattern {
                    cmd.push(p);
                }

                cmd.extend(&["--verbose", "--no-fail-fast"]);

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run tests")?;

                if output.status.success() {
                    println!("✅ QA Agent: All tests passed");
                } else {
                    println!("❌ QA Agent: Some tests failed");
                    println!("{}", String::from_utf8_lossy(&output.stdout));
                    return Err(anyhow::anyhow!("Tests failed"));
                }
            }

            QualityAction::Build {
                release,
                all_targets,
            } => {
                println!("🛠️ DevOps Agent - Build Verification");
                println!("=====================================");

                let mut cmd = vec!["cargo", "build", "--verbose"];

                if *release {
                    cmd.push("--release");
                }
                if *all_targets {
                    cmd.push("--all-targets");
                }

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to build")?;

                if output.status.success() {
                    println!("✅ DevOps Agent: Build completed successfully");
                } else {
                    println!("❌ DevOps Agent: Build failed");
                    println!("{}", String::from_utf8_lossy(&output.stderr));
                    return Err(anyhow::anyhow!("Build failed"));
                }
            }

            QualityAction::Security { audit, deps } => {
                println!("🦀 Backend Agent - Security Analysis");
                println!("====================================");

                if *audit {
                    let output = Command::new("cargo").args(&["audit"]).output();

                    match output {
                        Ok(out) if out.status.success() => {
                            println!("✅ Backend Agent: Security audit passed");
                        }
                        _ => {
                            println!("❌ Backend Agent: Security audit found issues (or cargo-audit not installed)");
                        }
                    }
                }

                if *deps {
                    println!("🔍 Backend Agent: Checking dependencies...");
                    // Run dependency checks
                }

                // Always run security tests
                let output = Command::new("cargo")
                    .args(&["test", "security::owasp_checker::tests", "--no-fail-fast"])
                    .output()
                    .context("Failed to run security tests")?;

                if output.status.success() {
                    println!("✅ Backend Agent: Security tests passed");
                } else {
                    println!("❌ Backend Agent: Security tests failed");
                    return Err(anyhow::anyhow!("Security tests failed"));
                }
            }

            QualityAction::Status { detailed } => {
                println!("📊 Quality Gate Status");
                println!("======================");

                // Run quick checks to show status
                let checks = [
                    ("Format", vec!["cargo", "fmt", "--check"]),
                    ("Clippy", vec!["cargo", "clippy", "--", "-D", "warnings"]),
                    ("Tests", vec!["cargo", "test", "--lib", "--no-run"]),
                    ("Build", vec!["cargo", "check"]),
                ];

                for (name, cmd) in &checks {
                    match Command::new(&cmd[0]).args(&cmd[1..]).output() {
                        Ok(output) => {
                            let status = if output.status.success() {
                                "✅ PASS"
                            } else {
                                "❌ FAIL"
                            };
                            println!("  {}: {}", name, status);

                            if *detailed && !output.status.success() {
                                println!("    Error: {}", String::from_utf8_lossy(&output.stderr));
                            }
                        }
                        Err(_) => {
                            println!("  {}: ❌ FAIL (command error)", name);
                        }
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:4339-4579 method handle_claude_acp <-> ./crates/ccswarm/src/cli/mod.rs:5008-5380 method handle_template
  Similarity: 96.87%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_claude_acp (lines 4339-4579) ---[0m
    async fn handle_claude_acp(&self, command: &ClaudeACPCommands) -> Result<()> {
        use crate::acp_claude::{ClaudeACPConfig, SimplifiedClaudeAdapter};

        match command {
            ClaudeACPCommands::Start { url } => {
                let mut config = ClaudeACPConfig::default();
                config.url = url.clone();

                let mut adapter = SimplifiedClaudeAdapter::new(config);

                if self.json_output {
                    match adapter.connect_with_retry().await {
                        Ok(_) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "message": "Connected to Claude Code",
                                    "url": url,
                                    "session_id": adapter.session_id()
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Failed to connect: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("🚀 Starting Claude Code ACP adapter...");
                    println!("   URL: {}", url);

                    match adapter.connect_with_retry().await {
                        Ok(_) => {
                            println!("✅ Successfully connected to Claude Code!");
                            if let Some(session_id) = adapter.session_id() {
                                println!("   Session ID: {}", session_id);
                            }
                        }
                        Err(e) => {
                            println!("❌ Failed to connect: {}", e);
                            return Err(e.into());
                        }
                    }
                }
            }
            ClaudeACPCommands::Test => {
                let config = ClaudeACPConfig::default();
                let mut adapter = SimplifiedClaudeAdapter::new(config);

                if self.json_output {
                    match adapter.test_connection().await {
                        Ok(result) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "message": result
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Connection test failed: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("🧪 Testing connection to Claude Code...");
                    match adapter.test_connection().await {
                        Ok(result) => {
                            println!("✅ {}", result);
                        }
                        Err(e) => {
                            println!("❌ Connection test failed: {}", e);
                            return Err(e.into());
                        }
                    }
                }
            }
            ClaudeACPCommands::Send { task } => {
                let config = ClaudeACPConfig::default();
                let mut adapter = SimplifiedClaudeAdapter::new(config);

                // Connect first
                adapter.connect_with_retry().await?;

                if self.json_output {
                    match adapter.send_task(task).await {
                        Ok(response) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "response": response
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Failed to send task: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("📤 Sending task to Claude Code: {}", task);
                    match adapter.send_task(task).await {
                        Ok(response) => {
                            println!("📥 Response: {}", response);
                        }
                        Err(e) => {
                            println!("❌ Failed to send task: {}", e);
                            return Err(e.into());
                        }
                    }
                }

                // Disconnect after sending
                adapter.disconnect().await;
            }
            ClaudeACPCommands::Stop => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Claude ACP adapter stopped"
                        }))?
                    );
                } else {
                    println!("🛑 Stopping Claude Code ACP adapter...");
                    println!("✅ Claude ACP adapter stopped");
                }
            }
            ClaudeACPCommands::Status => {
                let config = ClaudeACPConfig::default();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "config": {
                                "url": config.url,
                                "auto_connect": config.auto_connect,
                                "timeout": config.timeout,
                                "max_retries": config.max_retries,
                                "prefer_claude": config.prefer_claude
                            }
                        }))?
                    );
                } else {
                    println!("📊 Claude ACP Status:");
                    println!("   URL: {}", config.url);
                    println!("   Auto-connect: {}", config.auto_connect);
                    println!("   Timeout: {}s", config.timeout);
                    println!("   Max retries: {}", config.max_retries);
                    println!("   Prefer Claude: {}", config.prefer_claude);
                }
            }
            ClaudeACPCommands::Diagnose => {
                let config = ClaudeACPConfig::from_env();
                let mut adapter = SimplifiedClaudeAdapter::new(config.clone());

                if self.json_output {
                    let mut diagnostics = serde_json::json!({
                        "environment": {
                            "url_env": std::env::var("CCSWARM_CLAUDE_ACP_URL").ok(),
                            "auto_connect_env": std::env::var("CCSWARM_CLAUDE_ACP_AUTO_CONNECT").ok(),
                            "timeout_env": std::env::var("CCSWARM_CLAUDE_ACP_TIMEOUT").ok(),
                        },
                        "config": config
                    });

                    // Test connection
                    if let Ok(_) = adapter.connect().await {
                        diagnostics["connection"] = serde_json::json!({
                            "status": "connected",
                            "session_id": adapter.session_id()
                        });
                        adapter.disconnect().await;
                    } else {
                        diagnostics["connection"] = serde_json::json!({
                            "status": "failed"
                        });
                    }

                    println!("{}", serde_json::to_string_pretty(&diagnostics)?);
                } else {
                    println!("🔍 Claude ACP Diagnostics:");
                    println!("\n📋 Environment Variables:");
                    if let Ok(url) = std::env::var("CCSWARM_CLAUDE_ACP_URL") {
                        println!("   CCSWARM_CLAUDE_ACP_URL: {}", url);
                    } else {
                        println!("   CCSWARM_CLAUDE_ACP_URL: <not set>");
                    }
                    if let Ok(auto) = std::env::var("CCSWARM_CLAUDE_ACP_AUTO_CONNECT") {
                        println!("   CCSWARM_CLAUDE_ACP_AUTO_CONNECT: {}", auto);
                    } else {
                        println!("   CCSWARM_CLAUDE_ACP_AUTO_CONNECT: <not set>");
                    }

                    println!("\n⚙️  Configuration:");
                    println!("   URL: {}", config.url);
                    println!("   Auto-connect: {}", config.auto_connect);
                    println!("   Timeout: {}s", config.timeout);
                    println!("   Max retries: {}", config.max_retries);

                    println!("\n🧪 Connection Test:");
                    print!("   Attempting to connect... ");
                    if let Ok(_) = adapter.connect().await {
                        println!("✅ Success!");
                        if let Some(session_id) = adapter.session_id() {
                            println!("   Session ID: {}", session_id);
                        }
                        adapter.disconnect().await;
                    } else {
                        println!("❌ Failed!");
                        println!("   Claude Code may not be running or accessible");
                    }
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_template (lines 5008-5380) ---[0m
    async fn handle_template(&self, action: &TemplateAction) -> Result<()> {
        use crate::template::{
            FileSystemTemplateStorage, PredefinedTemplates, TemplateCategory, TemplateContext,
            TemplateManager, TemplateQuery,
        };
        use colored::Colorize;
        use std::io::{self, Write};
        use std::str::FromStr;

        // Initialize template storage
        let templates_dir = self.repo_path.join(".ccswarm").join("templates");
        let storage = FileSystemTemplateStorage::new(&templates_dir)
            .await
            .context("Failed to initialize template storage")?;
        let mut manager = TemplateManager::new(storage);

        match action {
            TemplateAction::List {
                all: _,
                category,
                tags,
                search,
                popular,
                quality,
                detailed,
            } => {
                let mut query = TemplateQuery::new();

                if let Some(cat_str) = category {
                    let cat = TemplateCategory::from_str(cat_str).context("Invalid category")?;
                    query = query.with_category(cat);
                }

                if !tags.is_empty() {
                    query = query.with_tags(tags.clone());
                }

                if let Some(search_term) = search {
                    query = query.with_search_term(search_term);
                }

                if *popular {
                    query = query.sort_by_popularity();
                } else if *quality {
                    query = query.sort_by_success_rate();
                }

                let templates = manager
                    .search_templates(query)
                    .await
                    .context("Failed to search templates")?;

                if templates.is_empty() {
                    println!("No templates found.");
                    return Ok(());
                }

                println!("Available Templates:");
                println!();

                for template in templates {
                    if *detailed {
                        println!(
                            "📋 {} ({})",
                            template.name.bright_cyan(),
                            template.id.as_str().bright_black()
                        );
                        println!("   Category: {}", template.category);
                        println!("   Description: {}", template.description);
                        if !template.tags.is_empty() {
                            println!(
                                "   Tags: {}",
                                template.tags.join(", ").as_str().bright_black()
                            );
                        }
                        if let Some(author) = &template.author {
                            println!("   Author: {}", author.as_str().bright_black());
                        }
                        println!("   Usage: {} times", template.usage_count);
                        if let Some(rate) = template.success_rate {
                            println!("   Success Rate: {:.1}%", rate * 100.0);
                        }
                        println!();
                    } else {
                        println!(
                            "  {} ({}) - {}",
                            template.name.bright_cyan(),
                            template.id.bright_black(),
                            template.description.chars().take(80).collect::<String>()
                        );
                    }
                }
            }

            TemplateAction::Show {
                template,
                source,
                stats,
            } => {
                let tmpl = manager
                    .get_template_by_name(template)
                    .await
                    .context("Template not found")?;

                println!(
                    "Template: {} ({})",
                    tmpl.name.bright_cyan(),
                    tmpl.id.bright_black()
                );
                println!("Category: {}", tmpl.category);
                println!("Description: {}", tmpl.description);
                println!("Version: {}", tmpl.version);

                if let Some(author) = &tmpl.author {
                    println!("Author: {}", author);
                }

                if !tmpl.tags.is_empty() {
                    println!("Tags: {}", tmpl.tags.join(", "));
                }

                println!("Priority: {:?}", tmpl.default_priority);
                println!("Task Type: {:?}", tmpl.default_task_type);

                if let Some(duration) = tmpl.estimated_duration {
                    println!("Estimated Duration: {} minutes", duration);
                }

                if !tmpl.variables.is_empty() {
                    println!();
                    println!("Variables:");
                    for var in &tmpl.variables {
                        let required = if var.required { " (required)" } else { "" };
                        println!(
                            "  • {}{}: {}",
                            var.name.bright_green(),
                            required,
                            var.description
                        );
                        if let Some(default) = &var.default_value {
                            println!("    Default: {}", default.clone().bright_black());
                        }
                    }
                }

                if *source {
                    println!();
                    println!("Task Description Template:");
                    println!("{}", tmpl.task_description.bright_white());

                    if let Some(details) = &tmpl.task_details {
                        println!();
                        println!("Task Details Template:");
                        println!("{}", details.bright_white());
                    }
                }

                if *stats {
                    println!();
                    println!("Statistics:");
                    println!("  Usage Count: {}", tmpl.usage_count);
                    if let Some(rate) = tmpl.success_rate {
                        println!("  Success Rate: {:.1}%", rate * 100.0);
                    }
                    println!("  Created: {}", tmpl.created_at.format("%Y-%m-%d %H:%M"));
                    println!("  Updated: {}", tmpl.updated_at.format("%Y-%m-%d %H:%M"));
                }
            }

            TemplateAction::Apply {
                template,
                vars,
                interactive,
                preview,
                auto_assign,
            } => {
                let tmpl = manager
                    .get_template_by_name(template)
                    .await
                    .context("Template not found")?;

                let mut context = TemplateContext::new();

                // Parse provided variables
                for var_str in vars {
                    if let Some((key, value)) = var_str.split_once('=') {
                        context = context.with_variable(key.trim(), value.trim());
                    }
                }

                // Interactive mode for missing variables
                if *interactive {
                    for var in &tmpl.variables {
                        if var.required
                            && !context.variables.contains_key(&var.name)
                            && var.default_value.is_none()
                        {
                            print!("{} ({}): ", var.name, var.description);
                            io::stdout().flush()?;

                            let mut input = String::new();
                            io::stdin().read_line(&mut input)?;
                            let value = input.trim();

                            if !value.is_empty() {
                                context = context.with_variable(&var.name, value);
                            }
                        }
                    }
                }

                // Apply template
                let applied = manager
                    .apply_template(&tmpl.id, context)
                    .await
                    .context("Failed to apply template")?;

                if *preview {
                    println!("Preview of generated task:");
                    println!();
                    println!("Description: {}", applied.description.bright_cyan());
                    if let Some(details) = &applied.details {
                        println!("Details: {}", details);
                    }
                    println!("Priority: {:?}", applied.priority);
                    println!("Type: {:?}", applied.task_type);
                    if let Some(duration) = applied.estimated_duration {
                        println!("Duration: {} minutes", duration);
                    }
                    if !applied.target_files.is_empty() {
                        println!("Target Files: {}", applied.target_files.join(", "));
                    }
                } else {
                    // Create the actual task
                    use crate::agent::TaskBuilder;

                    let task = TaskBuilder::new(applied.description.clone())
                        .priority(applied.priority)
                        .task_type(applied.task_type);

                    let task = if let Some(details) = &applied.details {
                        task.details(details.clone())
                    } else {
                        task
                    };

                    let task = if let Some(duration) = applied.estimated_duration {
                        task.estimated_duration(duration as u64)
                    } else {
                        task
                    };

                    let task = task.build();

                    println!(
                        "Created task from template: {}",
                        applied.description.bright_green()
                    );
                    println!("Task ID: {}", task.id.bright_cyan());

                    if *auto_assign {
                        println!("Auto-assigning to best agent...");
                        // TODO: Implement auto-assignment logic
                    }
                }
            }

            TemplateAction::Install {
                all,
                categories,
                force,
            } => {
                let predefined_templates = PredefinedTemplates::get_all();
                let mut installed = 0;
                let mut skipped = 0;

                for template in predefined_templates {
                    // Filter by categories if specified
                    if !categories.is_empty() && !*all {
                        let cat_str = template.category.to_string();
                        if !categories.iter().any(|c| c.eq_ignore_ascii_case(&cat_str)) {
                            continue;
                        }
                    }

                    match manager.save_template(template.clone()).await {
                        Ok(()) => {
                            println!("✅ Installed: {}", template.name.bright_green());
                            installed += 1;
                        }
                        Err(e) if e.to_string().contains("already exists") => {
                            if *force {
                                if let Err(e) = manager.update_template(template.clone()).await {
                                    println!("❌ Failed to update {}: {}", template.name.red(), e);
                                } else {
                                    println!("✅ Updated: {}", template.name.bright_green());
                                    installed += 1;
                                }
                            } else {
                                println!("⚠️  Skipped (exists): {}", template.name.bright_yellow());
                                skipped += 1;
                            }
                        }
                        Err(e) => {
                            println!("❌ Failed to install {}: {}", template.name.red(), e);
                        }
                    }
                }

                println!();
                println!(
                    "Installation complete: {} installed, {} skipped",
                    installed, skipped
                );
                if skipped > 0 && !*force {
                    println!("Use --force to overwrite existing templates");
                }
            }

            TemplateAction::Stats {
                global: _,
                template,
            } => {
                if let Some(tmpl_name) = template {
                    let tmpl = manager
                        .get_template_by_name(tmpl_name)
                        .await
                        .context("Template not found")?;

                    println!("Template Statistics: {}", tmpl.name.bright_cyan());
                    println!("Usage Count: {}", tmpl.usage_count);
                    if let Some(rate) = tmpl.success_rate {
                        println!("Success Rate: {:.1}%", rate * 100.0);
                    }
                    println!("Created: {}", tmpl.created_at.format("%Y-%m-%d %H:%M"));
                    println!("Updated: {}", tmpl.updated_at.format("%Y-%m-%d %H:%M"));
                } else {
                    let stats = manager
                        .get_template_stats()
                        .await
                        .context("Failed to get template statistics")?;

                    println!("Global Template Statistics:");
                    println!("Total Templates: {}", stats.total_templates);
                    println!("Total Usage: {}", stats.total_usage);
                    println!(
                        "Average Success Rate: {:.1}%",
                        stats.average_success_rate * 100.0
                    );

                    println!();
                    println!("By Category:");
                    for (category, count) in &stats.by_category {
                        println!("  {}: {}", category, count);
                    }

                    if !stats.most_popular.is_empty() {
                        println!();
                        println!("Most Popular:");
                        for (name, count) in stats.most_popular.iter().take(5) {
                            println!("  {}: {} uses", name, count);
                        }
                    }
                }
            }

            _ => {
                println!("Template command not yet implemented: {:?}", action);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:3806-4171 method handle_extend <-> ./crates/ccswarm/src/cli/mod.rs:4339-4579 method handle_claude_acp
  Similarity: 97.82%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_extend (lines 3806-4171) ---[0m
    async fn handle_extend(&self, action: &ExtendAction) -> Result<()> {
        use crate::extension_stub::meta_learning::MetaLearningSystem;
        use crate::extension_stub::{
            ExtensionManager, ExtensionProposal, ExtensionStatus, ExtensionType,
        };
        use chrono::Utc;

        // Create extension manager instance
        let mut extension_manager = ExtensionManager::new();
        let _meta_learning = MetaLearningSystem::new();

        match action {
            ExtendAction::Propose {
                agent,
                extension_type,
                file,
            } => {
                // Read extension specification from file
                let spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read extension specification file")?;

                // Parse extension type
                let ext_type = match extension_type.as_str() {
                    "capability" => ExtensionType::Capability,
                    "system" => ExtensionType::System,
                    "cognitive" => ExtensionType::Cognitive,
                    "collaborative" => ExtensionType::Collaborative,
                    _ => ExtensionType::Capability, // Default
                };

                // Create extension proposal
                let proposal = ExtensionProposal {
                    id: uuid::Uuid::new_v4().to_string(),
                    agent_id: agent.clone(),
                    proposer: agent.clone(),
                    extension_type: ext_type,
                    status: ExtensionStatus::Proposed,
                    specification: spec_content.clone(),
                    title: format!("{} extension for {}", extension_type, agent),
                    description: spec_content.lines().take(3).collect::<Vec<_>>().join(" "),
                    current_state: crate::extension_stub::CurrentState {
                        capabilities: vec!["basic functionality".to_string()],
                        limitations: vec!["needs enhancement".to_string()],
                        performance_metrics: std::collections::HashMap::new(),
                    },
                    proposed_state: crate::extension_stub::ProposedState {
                        new_capabilities: vec!["enhanced functionality".to_string()],
                        removed_limitations: vec!["slow processing".to_string()],
                        expected_improvements: {
                            let mut map = std::collections::HashMap::new();
                            map.insert("performance".to_string(), 25.0);
                            map
                        },
                        performance_targets: std::collections::HashMap::new(),
                    },
                    implementation_plan: crate::extension_stub::ImplementationPlan {
                        total_duration: std::time::Duration::from_secs(2419200), // 4 weeks
                        phases: vec![
                            crate::extension_stub::ImplementationPhase {
                                name: "Analysis & Design".to_string(),
                                description: "Analyze requirements and design the extension"
                                    .to_string(),
                                tasks: vec![
                                    "Design document".to_string(),
                                    "Technical specification".to_string(),
                                ],
                                validation_method: "Code review".to_string(),
                                estimated_duration: std::time::Duration::from_secs(604800), // 1 week
                                complexity: crate::extension_stub::ComplexityLevel::Medium,
                                dependencies: Vec::new(),
                            },
                            crate::extension_stub::ImplementationPhase {
                                name: "Implementation".to_string(),
                                description: "Implement the extension functionality".to_string(),
                                tasks: vec!["Working code".to_string(), "Unit tests".to_string()],
                                validation_method: "Testing".to_string(),
                                estimated_duration: std::time::Duration::from_secs(1209600), // 2 weeks
                                complexity: crate::extension_stub::ComplexityLevel::High,
                                dependencies: vec!["Analysis & Design".to_string()],
                            },
                            crate::extension_stub::ImplementationPhase {
                                name: "Testing & Deployment".to_string(),
                                description: "Test and deploy the extension".to_string(),
                                tasks: vec![
                                    "Test results".to_string(),
                                    "Deployed extension".to_string(),
                                ],
                                validation_method: "Production testing".to_string(),
                                estimated_duration: std::time::Duration::from_secs(604800), // 1 week
                                complexity: crate::extension_stub::ComplexityLevel::Medium,
                                dependencies: vec!["Implementation".to_string()],
                            },
                        ],
                        timeline: "2-4 weeks".to_string(),
                        resources_required: vec!["development time".to_string()],
                        dependencies: vec![],
                    },
                    risk_assessment: crate::extension_stub::RiskAssessment {
                        risk_level: crate::extension_stub::RiskLevel::Low,
                        potential_issues: vec!["Minor performance impact".to_string()],
                        mitigation_strategies: vec![],
                        rollback_plan: "Revert to previous version".to_string(),
                        overall_risk_score: 0.3,
                        categories: vec![crate::extension_stub::RiskCategory::Performance],
                    },
                    success_criteria: vec![
                        crate::extension_stub::SuccessCriterion {
                            name: "Functionality".to_string(),
                            description: "Extension functionality working correctly".to_string(),
                            target_value: 100.0,
                            measurement_method: "Automated testing".to_string(),
                            measurable: true,
                        },
                        crate::extension_stub::SuccessCriterion {
                            name: "Performance".to_string(),
                            description: "No performance degradation".to_string(),
                            target_value: 100.0, // milliseconds
                            measurement_method: "Performance benchmarks".to_string(),
                            measurable: true,
                        },
                    ],
                    created_at: Utc::now(),
                };

                // Submit proposal to extension manager
                let proposal_id = extension_manager.propose_extension(proposal).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension proposed",
                            "proposal_id": proposal_id,
                            "agent": agent,
                            "type": extension_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!("🔧 Proposing extension for agent: {}", agent);
                    println!("   Type: {}", extension_type);
                    println!("   Specification: {}", file.display());
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Extension proposal submitted");
                }
            }

            ExtendAction::Status {
                agent,
                extension_id,
            } => {
                // Get extension manager statistics
                let stats = extension_manager.get_stats().await;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension status retrieved",
                            "agent": agent,
                            "extension_id": extension_id,
                            "stats": {
                                "total_extensions": stats.total_extensions,
                                "active_extensions": stats.active_extensions,
                                "pending_proposals": stats.pending_proposals,
                                "successful_extensions": stats.successful_extensions,
                                "failed_extensions": stats.failed_extensions,
                            }
                        }))?
                    );
                } else {
                    println!("📊 Extension Status for agent: {}", agent);
                    if let Some(id) = extension_id {
                        println!("🆔 Extension ID: {}", id);
                    }
                    println!("📈 Statistics:");
                    println!("   Total Extensions: {}", stats.total_extensions);
                    println!("   Active: {}", stats.active_extensions);
                    println!("   Pending Proposals: {}", stats.pending_proposals);
                    println!("   Successful: {}", stats.successful_extensions);
                    println!("   Failed: {}", stats.failed_extensions);
                }
            }

            ExtendAction::History {
                agent,
                successful,
                failed,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension history",
                            "agent": agent,
                            "filter": {
                                "successful": successful,
                                "failed": failed,
                            },
                        }))?
                    );
                } else {
                    println!("📜 Extension History for agent: {}", agent);
                    println!("================================");
                    if *successful {
                        println!("Showing only successful extensions");
                    } else if *failed {
                        println!("Showing only failed extensions");
                    }
                    println!("No extensions found");
                }
            }

            ExtendAction::Rollback {
                agent,
                extension_id,
                force,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension rolled back",
                            "agent": agent,
                            "extension_id": extension_id,
                            "force": force,
                        }))?
                    );
                } else {
                    println!("⏪ Rolling back extension: {}", extension_id);
                    println!("   Agent: {}", agent);
                    if *force {
                        println!("   ⚠️ Force rollback enabled");
                    }
                    println!("✅ Rollback completed");
                }
            }

            ExtendAction::Discover {
                agent,
                discovery_type,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Discovery completed",
                            "agent": agent,
                            "discovery_type": discovery_type,
                        }))?
                    );
                } else {
                    println!("🔍 Discovering extension opportunities");
                    if let Some(a) = agent {
                        println!("   Agent: {}", a);
                    }
                    if let Some(dt) = discovery_type {
                        println!("   Type: {}", dt);
                    }
                    println!("✅ Discovery completed");
                }
            }

            ExtendAction::Autonomous {
                agent,
                continuous,
                dry_run,
            } => {
                // use crate::extension::autonomous_agent_extension::AutonomousAgentExtensionManager;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "started",
                            "message": "Autonomous extension reasoning initiated",
                            "agent": agent,
                            "continuous": continuous,
                            "dry_run": dry_run,
                        }))?
                    );
                } else {
                    println!("🤖 Initiating autonomous extension reasoning");
                    if let Some(a) = agent {
                        println!("   Agent: {}", a);
                    } else {
                        println!("   Agents: All agents");
                    }
                    if *continuous {
                        println!(
                            "   Mode: Continuous (agents will autonomously propose extensions)"
                        );
                    }
                    if *dry_run {
                        println!("   🔍 DRY RUN - proposals will not be submitted to Sangha");
                    }
                }

                // Get agent list
                let target_agents = if let Some(agent_id) = agent {
                    vec![agent_id.clone()]
                } else {
                    // Get all agents from config
                    self.config.agents.keys().cloned().collect()
                };

                // Process each agent
                for agent_id in target_agents {
                    if !self.json_output {
                        println!("\n🎯 Processing agent: {}", agent_id);
                    }

                    // Create autonomous extension manager for the agent
                    // In real implementation, would create proper provider and sangha client
                    // For now, just log the intent

                    if *dry_run {
                        if !self.json_output {
                            println!("   📋 Would propose extensions based on:");
                            println!("      - Past performance analysis");
                            println!("      - Identified capability gaps");
                            println!("      - Recurring failure patterns");
                            println!("      - Self-reflection insights");
                        }
                    } else if !self.json_output {
                        println!("   🧠 Analyzing experiences and performance...");
                        println!("   🔍 Identifying capability gaps...");
                        println!("   💡 Generating extension proposals...");
                        println!("   🏛️  Submitting proposals to Sangha for approval...");

                        // In real implementation:
                        // 1. Create AutonomousAgentExtensionManager
                        // 2. Call propose_extensions()
                        // 3. Wait for Sangha consensus
                        // 4. Report results
                    }
                }

                if *continuous && !self.json_output {
                    println!("\n♾️  Continuous mode enabled - agents will autonomously propose extensions as needed");
                    println!("   Press Ctrl+C to stop");
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "completed",
                            "message": "Autonomous extension reasoning completed",
                            "continuous_mode": continuous,
                        }))?
                    );
                } else {
                    println!("\n✅ Autonomous extension reasoning completed");
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_claude_acp (lines 4339-4579) ---[0m
    async fn handle_claude_acp(&self, command: &ClaudeACPCommands) -> Result<()> {
        use crate::acp_claude::{ClaudeACPConfig, SimplifiedClaudeAdapter};

        match command {
            ClaudeACPCommands::Start { url } => {
                let mut config = ClaudeACPConfig::default();
                config.url = url.clone();

                let mut adapter = SimplifiedClaudeAdapter::new(config);

                if self.json_output {
                    match adapter.connect_with_retry().await {
                        Ok(_) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "message": "Connected to Claude Code",
                                    "url": url,
                                    "session_id": adapter.session_id()
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Failed to connect: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("🚀 Starting Claude Code ACP adapter...");
                    println!("   URL: {}", url);

                    match adapter.connect_with_retry().await {
                        Ok(_) => {
                            println!("✅ Successfully connected to Claude Code!");
                            if let Some(session_id) = adapter.session_id() {
                                println!("   Session ID: {}", session_id);
                            }
                        }
                        Err(e) => {
                            println!("❌ Failed to connect: {}", e);
                            return Err(e.into());
                        }
                    }
                }
            }
            ClaudeACPCommands::Test => {
                let config = ClaudeACPConfig::default();
                let mut adapter = SimplifiedClaudeAdapter::new(config);

                if self.json_output {
                    match adapter.test_connection().await {
                        Ok(result) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "message": result
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Connection test failed: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("🧪 Testing connection to Claude Code...");
                    match adapter.test_connection().await {
                        Ok(result) => {
                            println!("✅ {}", result);
                        }
                        Err(e) => {
                            println!("❌ Connection test failed: {}", e);
                            return Err(e.into());
                        }
                    }
                }
            }
            ClaudeACPCommands::Send { task } => {
                let config = ClaudeACPConfig::default();
                let mut adapter = SimplifiedClaudeAdapter::new(config);

                // Connect first
                adapter.connect_with_retry().await?;

                if self.json_output {
                    match adapter.send_task(task).await {
                        Ok(response) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "response": response
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Failed to send task: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("📤 Sending task to Claude Code: {}", task);
                    match adapter.send_task(task).await {
                        Ok(response) => {
                            println!("📥 Response: {}", response);
                        }
                        Err(e) => {
                            println!("❌ Failed to send task: {}", e);
                            return Err(e.into());
                        }
                    }
                }

                // Disconnect after sending
                adapter.disconnect().await;
            }
            ClaudeACPCommands::Stop => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Claude ACP adapter stopped"
                        }))?
                    );
                } else {
                    println!("🛑 Stopping Claude Code ACP adapter...");
                    println!("✅ Claude ACP adapter stopped");
                }
            }
            ClaudeACPCommands::Status => {
                let config = ClaudeACPConfig::default();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "config": {
                                "url": config.url,
                                "auto_connect": config.auto_connect,
                                "timeout": config.timeout,
                                "max_retries": config.max_retries,
                                "prefer_claude": config.prefer_claude
                            }
                        }))?
                    );
                } else {
                    println!("📊 Claude ACP Status:");
                    println!("   URL: {}", config.url);
                    println!("   Auto-connect: {}", config.auto_connect);
                    println!("   Timeout: {}s", config.timeout);
                    println!("   Max retries: {}", config.max_retries);
                    println!("   Prefer Claude: {}", config.prefer_claude);
                }
            }
            ClaudeACPCommands::Diagnose => {
                let config = ClaudeACPConfig::from_env();
                let mut adapter = SimplifiedClaudeAdapter::new(config.clone());

                if self.json_output {
                    let mut diagnostics = serde_json::json!({
                        "environment": {
                            "url_env": std::env::var("CCSWARM_CLAUDE_ACP_URL").ok(),
                            "auto_connect_env": std::env::var("CCSWARM_CLAUDE_ACP_AUTO_CONNECT").ok(),
                            "timeout_env": std::env::var("CCSWARM_CLAUDE_ACP_TIMEOUT").ok(),
                        },
                        "config": config
                    });

                    // Test connection
                    if let Ok(_) = adapter.connect().await {
                        diagnostics["connection"] = serde_json::json!({
                            "status": "connected",
                            "session_id": adapter.session_id()
                        });
                        adapter.disconnect().await;
                    } else {
                        diagnostics["connection"] = serde_json::json!({
                            "status": "failed"
                        });
                    }

                    println!("{}", serde_json::to_string_pretty(&diagnostics)?);
                } else {
                    println!("🔍 Claude ACP Diagnostics:");
                    println!("\n📋 Environment Variables:");
                    if let Ok(url) = std::env::var("CCSWARM_CLAUDE_ACP_URL") {
                        println!("   CCSWARM_CLAUDE_ACP_URL: {}", url);
                    } else {
                        println!("   CCSWARM_CLAUDE_ACP_URL: <not set>");
                    }
                    if let Ok(auto) = std::env::var("CCSWARM_CLAUDE_ACP_AUTO_CONNECT") {
                        println!("   CCSWARM_CLAUDE_ACP_AUTO_CONNECT: {}", auto);
                    } else {
                        println!("   CCSWARM_CLAUDE_ACP_AUTO_CONNECT: <not set>");
                    }

                    println!("\n⚙️  Configuration:");
                    println!("   URL: {}", config.url);
                    println!("   Auto-connect: {}", config.auto_connect);
                    println!("   Timeout: {}s", config.timeout);
                    println!("   Max retries: {}", config.max_retries);

                    println!("\n🧪 Connection Test:");
                    print!("   Attempting to connect... ");
                    if let Ok(_) = adapter.connect().await {
                        println!("✅ Success!");
                        if let Some(session_id) = adapter.session_id() {
                            println!("   Session ID: {}", session_id);
                        }
                        adapter.disconnect().await;
                    } else {
                        println!("❌ Failed!");
                        println!("   Claude Code may not be running or accessible");
                    }
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:4339-4579 method handle_claude_acp <-> ./crates/ccswarm/src/cli/mod.rs:4685-5006 method handle_quality
  Similarity: 98.75%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_claude_acp (lines 4339-4579) ---[0m
    async fn handle_claude_acp(&self, command: &ClaudeACPCommands) -> Result<()> {
        use crate::acp_claude::{ClaudeACPConfig, SimplifiedClaudeAdapter};

        match command {
            ClaudeACPCommands::Start { url } => {
                let mut config = ClaudeACPConfig::default();
                config.url = url.clone();

                let mut adapter = SimplifiedClaudeAdapter::new(config);

                if self.json_output {
                    match adapter.connect_with_retry().await {
                        Ok(_) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "message": "Connected to Claude Code",
                                    "url": url,
                                    "session_id": adapter.session_id()
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Failed to connect: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("🚀 Starting Claude Code ACP adapter...");
                    println!("   URL: {}", url);

                    match adapter.connect_with_retry().await {
                        Ok(_) => {
                            println!("✅ Successfully connected to Claude Code!");
                            if let Some(session_id) = adapter.session_id() {
                                println!("   Session ID: {}", session_id);
                            }
                        }
                        Err(e) => {
                            println!("❌ Failed to connect: {}", e);
                            return Err(e.into());
                        }
                    }
                }
            }
            ClaudeACPCommands::Test => {
                let config = ClaudeACPConfig::default();
                let mut adapter = SimplifiedClaudeAdapter::new(config);

                if self.json_output {
                    match adapter.test_connection().await {
                        Ok(result) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "message": result
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Connection test failed: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("🧪 Testing connection to Claude Code...");
                    match adapter.test_connection().await {
                        Ok(result) => {
                            println!("✅ {}", result);
                        }
                        Err(e) => {
                            println!("❌ Connection test failed: {}", e);
                            return Err(e.into());
                        }
                    }
                }
            }
            ClaudeACPCommands::Send { task } => {
                let config = ClaudeACPConfig::default();
                let mut adapter = SimplifiedClaudeAdapter::new(config);

                // Connect first
                adapter.connect_with_retry().await?;

                if self.json_output {
                    match adapter.send_task(task).await {
                        Ok(response) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "response": response
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Failed to send task: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("📤 Sending task to Claude Code: {}", task);
                    match adapter.send_task(task).await {
                        Ok(response) => {
                            println!("📥 Response: {}", response);
                        }
                        Err(e) => {
                            println!("❌ Failed to send task: {}", e);
                            return Err(e.into());
                        }
                    }
                }

                // Disconnect after sending
                adapter.disconnect().await;
            }
            ClaudeACPCommands::Stop => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Claude ACP adapter stopped"
                        }))?
                    );
                } else {
                    println!("🛑 Stopping Claude Code ACP adapter...");
                    println!("✅ Claude ACP adapter stopped");
                }
            }
            ClaudeACPCommands::Status => {
                let config = ClaudeACPConfig::default();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "config": {
                                "url": config.url,
                                "auto_connect": config.auto_connect,
                                "timeout": config.timeout,
                                "max_retries": config.max_retries,
                                "prefer_claude": config.prefer_claude
                            }
                        }))?
                    );
                } else {
                    println!("📊 Claude ACP Status:");
                    println!("   URL: {}", config.url);
                    println!("   Auto-connect: {}", config.auto_connect);
                    println!("   Timeout: {}s", config.timeout);
                    println!("   Max retries: {}", config.max_retries);
                    println!("   Prefer Claude: {}", config.prefer_claude);
                }
            }
            ClaudeACPCommands::Diagnose => {
                let config = ClaudeACPConfig::from_env();
                let mut adapter = SimplifiedClaudeAdapter::new(config.clone());

                if self.json_output {
                    let mut diagnostics = serde_json::json!({
                        "environment": {
                            "url_env": std::env::var("CCSWARM_CLAUDE_ACP_URL").ok(),
                            "auto_connect_env": std::env::var("CCSWARM_CLAUDE_ACP_AUTO_CONNECT").ok(),
                            "timeout_env": std::env::var("CCSWARM_CLAUDE_ACP_TIMEOUT").ok(),
                        },
                        "config": config
                    });

                    // Test connection
                    if let Ok(_) = adapter.connect().await {
                        diagnostics["connection"] = serde_json::json!({
                            "status": "connected",
                            "session_id": adapter.session_id()
                        });
                        adapter.disconnect().await;
                    } else {
                        diagnostics["connection"] = serde_json::json!({
                            "status": "failed"
                        });
                    }

                    println!("{}", serde_json::to_string_pretty(&diagnostics)?);
                } else {
                    println!("🔍 Claude ACP Diagnostics:");
                    println!("\n📋 Environment Variables:");
                    if let Ok(url) = std::env::var("CCSWARM_CLAUDE_ACP_URL") {
                        println!("   CCSWARM_CLAUDE_ACP_URL: {}", url);
                    } else {
                        println!("   CCSWARM_CLAUDE_ACP_URL: <not set>");
                    }
                    if let Ok(auto) = std::env::var("CCSWARM_CLAUDE_ACP_AUTO_CONNECT") {
                        println!("   CCSWARM_CLAUDE_ACP_AUTO_CONNECT: {}", auto);
                    } else {
                        println!("   CCSWARM_CLAUDE_ACP_AUTO_CONNECT: <not set>");
                    }

                    println!("\n⚙️  Configuration:");
                    println!("   URL: {}", config.url);
                    println!("   Auto-connect: {}", config.auto_connect);
                    println!("   Timeout: {}s", config.timeout);
                    println!("   Max retries: {}", config.max_retries);

                    println!("\n🧪 Connection Test:");
                    print!("   Attempting to connect... ");
                    if let Ok(_) = adapter.connect().await {
                        println!("✅ Success!");
                        if let Some(session_id) = adapter.session_id() {
                            println!("   Session ID: {}", session_id);
                        }
                        adapter.disconnect().await;
                    } else {
                        println!("❌ Failed!");
                        println!("   Claude Code may not be running or accessible");
                    }
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_quality (lines 4685-5006) ---[0m
    async fn handle_quality(&self, action: &QualityAction) -> Result<()> {
        use std::process::Command;

        let mut failed_checks = Vec::new();
        let mut completed_tasks = 0;
        let total_tasks;

        match action {
            QualityAction::Check {
                skip,
                only,
                fail_fast,
            } => {
                println!("🤖 ccswarm Agent-Managed Quality Checks");
                println!("=======================================");
                println!();

                let checks = if only.is_empty() {
                    vec!["format", "lint", "test", "build", "security"]
                } else {
                    only.iter().map(|s| s.as_str()).collect()
                };

                let filtered_checks: Vec<&str> = checks
                    .into_iter()
                    .filter(|check| !skip.contains(&check.to_string()))
                    .collect();

                total_tasks = filtered_checks.len();
                println!("🎯 Master Claude: Orchestrating {} quality checks through specialized agents...", total_tasks);
                println!();

                for check in filtered_checks {
                    let (agent, task, cmd) = match check {
                        "format" => (
                            "DevOps",
                            "Code Formatting Check",
                            vec!["cargo", "fmt", "--check"],
                        ),
                        "lint" => (
                            "DevOps",
                            "Clippy Code Quality Analysis",
                            vec![
                                "cargo",
                                "clippy",
                                "--all-targets",
                                "--all-features",
                                "--",
                                "-D",
                                "warnings",
                            ],
                        ),
                        "test" => (
                            "QA",
                            "Test Suite Execution",
                            vec!["cargo", "test", "--lib", "--verbose", "--no-fail-fast"],
                        ),
                        "build" => (
                            "DevOps",
                            "Build Verification",
                            vec!["cargo", "build", "--verbose"],
                        ),
                        "security" => (
                            "Backend",
                            "Security Analysis",
                            vec![
                                "cargo",
                                "test",
                                "security::owasp_checker::tests",
                                "--no-fail-fast",
                            ],
                        ),
                        _ => continue,
                    };

                    println!("🎯 Delegating to {} agent: {}", agent, task);

                    let mut command = Command::new(&cmd[0]);
                    for arg in &cmd[1..] {
                        command.arg(arg);
                    }

                    let output = command.output().context("Failed to execute command")?;
                    let success = output.status.success();

                    if success {
                        println!("✅ {} agent: {} completed successfully", agent, task);
                        completed_tasks += 1;
                    } else {
                        println!("❌ {} agent: {} failed", agent, task);
                        failed_checks.push((
                            agent,
                            task,
                            String::from_utf8_lossy(&output.stderr).to_string(),
                        ));

                        if *fail_fast {
                            break;
                        }
                    }
                    println!();
                }

                // Quality Gate Assessment
                println!("🎯 Master Claude - Quality Gate Assessment");
                println!("==========================================");
                println!("📊 Agent Task Completion Summary:");
                println!("   Completed: {}/{} tasks", completed_tasks, total_tasks);
                println!("   Failed: {}", failed_checks.len());
                println!();

                if failed_checks.is_empty() {
                    println!("✅ QUALITY GATE: PASSED");
                    println!("🎉 All quality checks passed through agent delegation");
                    println!("🚀 Code is ready for deployment");
                } else {
                    println!("❌ QUALITY GATE: FAILED");
                    println!("🔧 Some quality checks require attention from agents");
                    println!();
                    println!("📋 Failed Checks:");
                    for (agent, task, _error) in &failed_checks {
                        println!("   ❌ {} agent: {}", agent, task);
                    }
                    return Err(anyhow::anyhow!("Quality gate failed"));
                }
            }

            QualityAction::Format { fix } => {
                println!("🛠️ DevOps Agent - Code Formatting");
                println!("==================================");

                let cmd = if *fix {
                    vec!["cargo", "fmt"]
                } else {
                    vec!["cargo", "fmt", "--check"]
                };

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run cargo fmt")?;

                if output.status.success() {
                    println!(
                        "✅ DevOps Agent: Code formatting {} successfully",
                        if *fix { "applied" } else { "verified" }
                    );
                } else {
                    println!("❌ DevOps Agent: Code formatting issues detected");
                    if !fix {
                        println!("💡 Run with --fix to automatically format code");
                    }
                    return Err(anyhow::anyhow!("Formatting check failed"));
                }
            }

            QualityAction::Lint { fix } => {
                println!("🛠️ DevOps Agent - Clippy Analysis");
                println!("==================================");

                let mut cmd = vec!["cargo", "clippy", "--all-targets", "--all-features"];
                if *fix {
                    cmd.push("--fix");
                    cmd.push("--allow-dirty");
                }
                cmd.extend(&["--", "-D", "warnings"]);

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run cargo clippy")?;

                if output.status.success() {
                    println!("✅ DevOps Agent: Clippy analysis passed");
                } else {
                    println!("❌ DevOps Agent: Clippy found issues");
                    println!("{}", String::from_utf8_lossy(&output.stdout));
                    return Err(anyhow::anyhow!("Clippy check failed"));
                }
            }

            QualityAction::Test {
                pattern,
                unit,
                integration,
                security,
            } => {
                println!("🧪 QA Agent - Test Execution");
                println!("============================");

                let mut cmd = vec!["cargo", "test"];

                if *unit {
                    cmd.push("--lib");
                } else if *integration {
                    cmd.extend(&["--test", "*integration*"]);
                } else if *security {
                    cmd.push("security::owasp_checker::tests");
                }

                if let Some(p) = pattern {
                    cmd.push(p);
                }

                cmd.extend(&["--verbose", "--no-fail-fast"]);

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run tests")?;

                if output.status.success() {
                    println!("✅ QA Agent: All tests passed");
                } else {
                    println!("❌ QA Agent: Some tests failed");
                    println!("{}", String::from_utf8_lossy(&output.stdout));
                    return Err(anyhow::anyhow!("Tests failed"));
                }
            }

            QualityAction::Build {
                release,
                all_targets,
            } => {
                println!("🛠️ DevOps Agent - Build Verification");
                println!("=====================================");

                let mut cmd = vec!["cargo", "build", "--verbose"];

                if *release {
                    cmd.push("--release");
                }
                if *all_targets {
                    cmd.push("--all-targets");
                }

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to build")?;

                if output.status.success() {
                    println!("✅ DevOps Agent: Build completed successfully");
                } else {
                    println!("❌ DevOps Agent: Build failed");
                    println!("{}", String::from_utf8_lossy(&output.stderr));
                    return Err(anyhow::anyhow!("Build failed"));
                }
            }

            QualityAction::Security { audit, deps } => {
                println!("🦀 Backend Agent - Security Analysis");
                println!("====================================");

                if *audit {
                    let output = Command::new("cargo").args(&["audit"]).output();

                    match output {
                        Ok(out) if out.status.success() => {
                            println!("✅ Backend Agent: Security audit passed");
                        }
                        _ => {
                            println!("❌ Backend Agent: Security audit found issues (or cargo-audit not installed)");
                        }
                    }
                }

                if *deps {
                    println!("🔍 Backend Agent: Checking dependencies...");
                    // Run dependency checks
                }

                // Always run security tests
                let output = Command::new("cargo")
                    .args(&["test", "security::owasp_checker::tests", "--no-fail-fast"])
                    .output()
                    .context("Failed to run security tests")?;

                if output.status.success() {
                    println!("✅ Backend Agent: Security tests passed");
                } else {
                    println!("❌ Backend Agent: Security tests failed");
                    return Err(anyhow::anyhow!("Security tests failed"));
                }
            }

            QualityAction::Status { detailed } => {
                println!("📊 Quality Gate Status");
                println!("======================");

                // Run quick checks to show status
                let checks = [
                    ("Format", vec!["cargo", "fmt", "--check"]),
                    ("Clippy", vec!["cargo", "clippy", "--", "-D", "warnings"]),
                    ("Tests", vec!["cargo", "test", "--lib", "--no-run"]),
                    ("Build", vec!["cargo", "check"]),
                ];

                for (name, cmd) in &checks {
                    match Command::new(&cmd[0]).args(&cmd[1..]).output() {
                        Ok(output) => {
                            let status = if output.status.success() {
                                "✅ PASS"
                            } else {
                                "❌ FAIL"
                            };
                            println!("  {}: {}", name, status);

                            if *detailed && !output.status.success() {
                                println!("    Error: {}", String::from_utf8_lossy(&output.stderr));
                            }
                        }
                        Err(_) => {
                            println!("  {}: ❌ FAIL (command error)", name);
                        }
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:5008-5380 method handle_template <-> ./crates/ccswarm/src/cli/mod.rs:5584-5809 method handle_doctor
  Similarity: 91.20%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_template (lines 5008-5380) ---[0m
    async fn handle_template(&self, action: &TemplateAction) -> Result<()> {
        use crate::template::{
            FileSystemTemplateStorage, PredefinedTemplates, TemplateCategory, TemplateContext,
            TemplateManager, TemplateQuery,
        };
        use colored::Colorize;
        use std::io::{self, Write};
        use std::str::FromStr;

        // Initialize template storage
        let templates_dir = self.repo_path.join(".ccswarm").join("templates");
        let storage = FileSystemTemplateStorage::new(&templates_dir)
            .await
            .context("Failed to initialize template storage")?;
        let mut manager = TemplateManager::new(storage);

        match action {
            TemplateAction::List {
                all: _,
                category,
                tags,
                search,
                popular,
                quality,
                detailed,
            } => {
                let mut query = TemplateQuery::new();

                if let Some(cat_str) = category {
                    let cat = TemplateCategory::from_str(cat_str).context("Invalid category")?;
                    query = query.with_category(cat);
                }

                if !tags.is_empty() {
                    query = query.with_tags(tags.clone());
                }

                if let Some(search_term) = search {
                    query = query.with_search_term(search_term);
                }

                if *popular {
                    query = query.sort_by_popularity();
                } else if *quality {
                    query = query.sort_by_success_rate();
                }

                let templates = manager
                    .search_templates(query)
                    .await
                    .context("Failed to search templates")?;

                if templates.is_empty() {
                    println!("No templates found.");
                    return Ok(());
                }

                println!("Available Templates:");
                println!();

                for template in templates {
                    if *detailed {
                        println!(
                            "📋 {} ({})",
                            template.name.bright_cyan(),
                            template.id.as_str().bright_black()
                        );
                        println!("   Category: {}", template.category);
                        println!("   Description: {}", template.description);
                        if !template.tags.is_empty() {
                            println!(
                                "   Tags: {}",
                                template.tags.join(", ").as_str().bright_black()
                            );
                        }
                        if let Some(author) = &template.author {
                            println!("   Author: {}", author.as_str().bright_black());
                        }
                        println!("   Usage: {} times", template.usage_count);
                        if let Some(rate) = template.success_rate {
                            println!("   Success Rate: {:.1}%", rate * 100.0);
                        }
                        println!();
                    } else {
                        println!(
                            "  {} ({}) - {}",
                            template.name.bright_cyan(),
                            template.id.bright_black(),
                            template.description.chars().take(80).collect::<String>()
                        );
                    }
                }
            }

            TemplateAction::Show {
                template,
                source,
                stats,
            } => {
                let tmpl = manager
                    .get_template_by_name(template)
                    .await
                    .context("Template not found")?;

                println!(
                    "Template: {} ({})",
                    tmpl.name.bright_cyan(),
                    tmpl.id.bright_black()
                );
                println!("Category: {}", tmpl.category);
                println!("Description: {}", tmpl.description);
                println!("Version: {}", tmpl.version);

                if let Some(author) = &tmpl.author {
                    println!("Author: {}", author);
                }

                if !tmpl.tags.is_empty() {
                    println!("Tags: {}", tmpl.tags.join(", "));
                }

                println!("Priority: {:?}", tmpl.default_priority);
                println!("Task Type: {:?}", tmpl.default_task_type);

                if let Some(duration) = tmpl.estimated_duration {
                    println!("Estimated Duration: {} minutes", duration);
                }

                if !tmpl.variables.is_empty() {
                    println!();
                    println!("Variables:");
                    for var in &tmpl.variables {
                        let required = if var.required { " (required)" } else { "" };
                        println!(
                            "  • {}{}: {}",
                            var.name.bright_green(),
                            required,
                            var.description
                        );
                        if let Some(default) = &var.default_value {
                            println!("    Default: {}", default.clone().bright_black());
                        }
                    }
                }

                if *source {
                    println!();
                    println!("Task Description Template:");
                    println!("{}", tmpl.task_description.bright_white());

                    if let Some(details) = &tmpl.task_details {
                        println!();
                        println!("Task Details Template:");
                        println!("{}", details.bright_white());
                    }
                }

                if *stats {
                    println!();
                    println!("Statistics:");
                    println!("  Usage Count: {}", tmpl.usage_count);
                    if let Some(rate) = tmpl.success_rate {
                        println!("  Success Rate: {:.1}%", rate * 100.0);
                    }
                    println!("  Created: {}", tmpl.created_at.format("%Y-%m-%d %H:%M"));
                    println!("  Updated: {}", tmpl.updated_at.format("%Y-%m-%d %H:%M"));
                }
            }

            TemplateAction::Apply {
                template,
                vars,
                interactive,
                preview,
                auto_assign,
            } => {
                let tmpl = manager
                    .get_template_by_name(template)
                    .await
                    .context("Template not found")?;

                let mut context = TemplateContext::new();

                // Parse provided variables
                for var_str in vars {
                    if let Some((key, value)) = var_str.split_once('=') {
                        context = context.with_variable(key.trim(), value.trim());
                    }
                }

                // Interactive mode for missing variables
                if *interactive {
                    for var in &tmpl.variables {
                        if var.required
                            && !context.variables.contains_key(&var.name)
                            && var.default_value.is_none()
                        {
                            print!("{} ({}): ", var.name, var.description);
                            io::stdout().flush()?;

                            let mut input = String::new();
                            io::stdin().read_line(&mut input)?;
                            let value = input.trim();

                            if !value.is_empty() {
                                context = context.with_variable(&var.name, value);
                            }
                        }
                    }
                }

                // Apply template
                let applied = manager
                    .apply_template(&tmpl.id, context)
                    .await
                    .context("Failed to apply template")?;

                if *preview {
                    println!("Preview of generated task:");
                    println!();
                    println!("Description: {}", applied.description.bright_cyan());
                    if let Some(details) = &applied.details {
                        println!("Details: {}", details);
                    }
                    println!("Priority: {:?}", applied.priority);
                    println!("Type: {:?}", applied.task_type);
                    if let Some(duration) = applied.estimated_duration {
                        println!("Duration: {} minutes", duration);
                    }
                    if !applied.target_files.is_empty() {
                        println!("Target Files: {}", applied.target_files.join(", "));
                    }
                } else {
                    // Create the actual task
                    use crate::agent::TaskBuilder;

                    let task = TaskBuilder::new(applied.description.clone())
                        .priority(applied.priority)
                        .task_type(applied.task_type);

                    let task = if let Some(details) = &applied.details {
                        task.details(details.clone())
                    } else {
                        task
                    };

                    let task = if let Some(duration) = applied.estimated_duration {
                        task.estimated_duration(duration as u64)
                    } else {
                        task
                    };

                    let task = task.build();

                    println!(
                        "Created task from template: {}",
                        applied.description.bright_green()
                    );
                    println!("Task ID: {}", task.id.bright_cyan());

                    if *auto_assign {
                        println!("Auto-assigning to best agent...");
                        // TODO: Implement auto-assignment logic
                    }
                }
            }

            TemplateAction::Install {
                all,
                categories,
                force,
            } => {
                let predefined_templates = PredefinedTemplates::get_all();
                let mut installed = 0;
                let mut skipped = 0;

                for template in predefined_templates {
                    // Filter by categories if specified
                    if !categories.is_empty() && !*all {
                        let cat_str = template.category.to_string();
                        if !categories.iter().any(|c| c.eq_ignore_ascii_case(&cat_str)) {
                            continue;
                        }
                    }

                    match manager.save_template(template.clone()).await {
                        Ok(()) => {
                            println!("✅ Installed: {}", template.name.bright_green());
                            installed += 1;
                        }
                        Err(e) if e.to_string().contains("already exists") => {
                            if *force {
                                if let Err(e) = manager.update_template(template.clone()).await {
                                    println!("❌ Failed to update {}: {}", template.name.red(), e);
                                } else {
                                    println!("✅ Updated: {}", template.name.bright_green());
                                    installed += 1;
                                }
                            } else {
                                println!("⚠️  Skipped (exists): {}", template.name.bright_yellow());
                                skipped += 1;
                            }
                        }
                        Err(e) => {
                            println!("❌ Failed to install {}: {}", template.name.red(), e);
                        }
                    }
                }

                println!();
                println!(
                    "Installation complete: {} installed, {} skipped",
                    installed, skipped
                );
                if skipped > 0 && !*force {
                    println!("Use --force to overwrite existing templates");
                }
            }

            TemplateAction::Stats {
                global: _,
                template,
            } => {
                if let Some(tmpl_name) = template {
                    let tmpl = manager
                        .get_template_by_name(tmpl_name)
                        .await
                        .context("Template not found")?;

                    println!("Template Statistics: {}", tmpl.name.bright_cyan());
                    println!("Usage Count: {}", tmpl.usage_count);
                    if let Some(rate) = tmpl.success_rate {
                        println!("Success Rate: {:.1}%", rate * 100.0);
                    }
                    println!("Created: {}", tmpl.created_at.format("%Y-%m-%d %H:%M"));
                    println!("Updated: {}", tmpl.updated_at.format("%Y-%m-%d %H:%M"));
                } else {
                    let stats = manager
                        .get_template_stats()
                        .await
                        .context("Failed to get template statistics")?;

                    println!("Global Template Statistics:");
                    println!("Total Templates: {}", stats.total_templates);
                    println!("Total Usage: {}", stats.total_usage);
                    println!(
                        "Average Success Rate: {:.1}%",
                        stats.average_success_rate * 100.0
                    );

                    println!();
                    println!("By Category:");
                    for (category, count) in &stats.by_category {
                        println!("  {}: {}", category, count);
                    }

                    if !stats.most_popular.is_empty() {
                        println!();
                        println!("Most Popular:");
                        for (name, count) in stats.most_popular.iter().take(5) {
                            println!("  {}: {} uses", name, count);
                        }
                    }
                }
            }

            _ => {
                println!("Template command not yet implemented: {:?}", action);
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_doctor (lines 5584-5809) ---[0m
    async fn handle_doctor(
        &self,
        fix: bool,
        error_code: Option<&str>,
        check_api: bool,
    ) -> Result<()> {
        use crate::utils::error_recovery::ErrorRecoveryDB;
        use crate::utils::user_error::CommonErrors;

        // Handle specific error code diagnosis
        if let Some(code) = error_code {
            println!("{}", "🔍 Error Code Diagnosis".bright_cyan().bold());
            println!("{}", "=======================".bright_cyan());
            println!();
            println!("Analyzing error code: {}", code.bright_yellow());
            println!();

            let recovery_db = ErrorRecoveryDB::new();
            if let Some(recovery) = recovery_db.get_recovery(code) {
                match &recovery {
                    crate::utils::error_recovery::RecoveryStep::UserAction { description } => {
                        println!("📋 {}", description.bright_white());
                    },
                    crate::utils::error_recovery::RecoveryStep::Command { description, .. } => {
                        println!("📋 {}", description.bright_white());
                    },
                    crate::utils::error_recovery::RecoveryStep::FileCreate { path, .. } => {
                        println!("📋 Create file: {}", path.bright_white());
                    },
                    crate::utils::error_recovery::RecoveryStep::EnvVar { name, .. } => {
                        println!("📋 Set environment variable: {}", name.bright_white());
                    },
                }
                println!();
                println!("Recovery steps:");
                let steps = [recovery.clone()]; // Treat the single recovery step as the step list
                for (i, step) in steps.iter().enumerate() {
                    match step {
                        crate::utils::error_recovery::RecoveryStep::Command {
                            cmd,
                            description,
                        } => {
                            println!("  {}. {} {}", i + 1, "Run:".bright_yellow(), description);
                            println!("     {}", cmd.bright_white().on_black());
                        }
                        crate::utils::error_recovery::RecoveryStep::FileCreate { path, .. } => {
                            println!(
                                "  {}. {} {}",
                                i + 1,
                                "Create file:".bright_yellow(),
                                path.bright_white()
                            );
                        }
                        crate::utils::error_recovery::RecoveryStep::EnvVar { name, example } => {
                            println!(
                                "  {}. {} {}",
                                i + 1,
                                "Set environment variable:".bright_yellow(),
                                name.bright_white()
                            );
                            println!("     Example: {}={}", name, example.bright_black());
                        }
                        crate::utils::error_recovery::RecoveryStep::UserAction { description } => {
                            println!(
                                "  {}. {} {}",
                                i + 1,
                                "Action required:".bright_yellow(),
                                description
                            );
                        }
                    }
                    println!();
                }

                let can_auto_fix = matches!(recovery, crate::utils::error_recovery::RecoveryStep::Command { .. });
                if can_auto_fix && fix {
                    recovery_db.auto_fix(code).await?;
                } else if can_auto_fix {
                    println!(
                        "💡 This error can be auto-fixed! Run: ccswarm doctor --error {} --fix",
                        code
                    );
                }
            } else {
                println!("❌ Unknown error code: {}", code);
                println!("   See all error codes: ccswarm help errors");
            }
            return Ok(());
        }

        // Handle API connectivity check
        if check_api {
            println!("{}", "🌐 API Connectivity Check".bright_cyan().bold());
            println!("{}", "=========================".bright_cyan());
            println!();

            print!("Testing Anthropic API... ");
            match reqwest::get("https://api.anthropic.com/v1/health").await {
                Ok(resp) if resp.status().is_success() => {
                    println!("{}", "✅ Connected".bright_green());
                }
                Ok(resp) => {
                    println!(
                        "{}",
                        format!("⚠️  Status: {}", resp.status()).bright_yellow()
                    );
                }
                Err(e) => {
                    println!("{}", "❌ Failed".bright_red());
                    println!("   {}", e.to_string().bright_black());
                }
            }
            println!();
            return Ok(());
        }

        println!("{}", "🏥 ccswarm System Diagnosis".bright_cyan().bold());
        println!("{}", "===========================".bright_cyan());
        println!();

        let mut issues = Vec::new();

        // Check Git
        print!("Checking Git... ");
        match std::process::Command::new("git").arg("--version").output() {
            Ok(output) if output.status.success() => {
                println!("{}", "✅ OK".bright_green());
            }
            _ => {
                println!("{}", "❌ Not found".bright_red());
                issues.push("git");
            }
        }

        // Check API keys
        print!("Checking API keys... ");
        if std::env::var("ANTHROPIC_API_KEY").is_ok() {
            println!("{}", "✅ Set".bright_green());
        } else {
            println!("{}", "⚠️  Not set".bright_yellow());
            issues.push("api_key");
        }

        // Check config
        print!("Checking configuration... ");
        let config_path = self.repo_path.join("ccswarm.json");
        if config_path.exists() {
            match CcswarmConfig::from_file(config_path.clone()).await {
                Ok(_) => println!("{}", "✅ Valid".bright_green()),
                Err(e) => {
                    println!("{}", "❌ Invalid".bright_red());
                    println!("   {}", e.to_string().bright_black());
                    issues.push("config");
                }
            }
        } else {
            println!("{}", "❌ Not found".bright_red());
            issues.push("config");
        }

        // Check git repo
        print!("Checking git repository... ");
        let git_dir = self.repo_path.join(".git");
        if git_dir.exists() {
            println!("{}", "✅ Initialized".bright_green());
        } else {
            println!("{}", "⚠️  Not initialized".bright_yellow());
            issues.push("git_repo");
        }

        // Check disk space
        print!("Checking disk space... ");
        // Simple check - in real implementation would use proper system calls
        println!("{}", "✅ Sufficient".bright_green());

        println!();

        if issues.is_empty() {
            println!("{}", "✅ All systems operational!".bright_green().bold());
        } else {
            println!(
                "{}",
                format!("⚠️  Found {} issues", issues.len())
                    .bright_yellow()
                    .bold()
            );

            if fix {
                println!();
                println!("{}", "🔧 Attempting fixes...".bright_cyan());

                for issue in &issues {
                    match *issue {
                        "git" => {
                            println!("• Git: Please install git from https://git-scm.com");
                        }
                        "api_key" => {
                            CommonErrors::api_key_missing("Anthropic").display();
                        }
                        "config" => {
                            println!("• Config: Run 'ccswarm setup' to create configuration");
                        }
                        "git_repo" => {
                            if fix {
                                println!("• Initializing git repository...");
                                crate::git::shell::ShellWorktreeManager::init_if_needed(
                                    &self.repo_path,
                                )
                                .await?;
                                println!("  ✅ Git repository initialized");
                            }
                        }
                        _ => {}
                    }
                }
            } else {
                println!();
                println!(
                    "{}",
                    "💡 Run with --fix to attempt automatic fixes".bright_black()
                );
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2279-2458 method handle_worktree <-> ./crates/ccswarm/src/cli/mod.rs:3806-4171 method handle_extend
  Similarity: 97.80%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_worktree (lines 2279-2458) ---[0m
    async fn handle_worktree(&self, action: &WorktreeAction) -> Result<()> {
        let manager = crate::git::shell::ShellWorktreeManager::new(self.repo_path.clone())?;

        match action {
            WorktreeAction::List => {
                let worktrees = manager.list_worktrees().await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&worktrees)?);
                } else {
                    println!("🌳 Git Worktrees");
                    println!("===============");

                    for wt in &worktrees {
                        println!("Path: {}", wt.path.display());
                        println!("  Branch: {}", wt.branch);
                        println!("  Head: {}", wt.head_commit);
                        println!("  Locked: {}", wt.is_locked);
                        println!();
                    }
                }
            }
            WorktreeAction::Create {
                path,
                branch,
                new_branch,
            } => {
                let info = if *new_branch {
                    manager.create_worktree_full(path, branch, true).await?
                } else {
                    manager.create_worktree(path, branch).await?
                };

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&info)?);
                } else {
                    println!("✅ Worktree created");
                    println!("   Path: {}", info.path.display());
                    println!("   Branch: {}", info.branch);
                }
            }
            WorktreeAction::Remove { path, force } => {
                if *force {
                    manager.remove_worktree_full(path, true).await?
                } else {
                    manager.remove_worktree(path).await?
                };

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktree removed",
                            "path": path,
                        }))?
                    );
                } else {
                    println!("✅ Worktree removed: {}", path.display());
                }
            }
            WorktreeAction::Prune => {
                manager.prune_worktrees().await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktrees pruned",
                        }))?
                    );
                } else {
                    println!("✅ Stale worktrees pruned");
                }
            }
            WorktreeAction::Clean { force } => {
                use std::io::{self, Write};

                // Find all ccswarm-related worktrees
                let worktrees = manager.list_worktrees().await?;
                let ccswarm_worktrees: Vec<_> = worktrees
                    .iter()
                    .filter(|w| w.branch.contains("agent") || w.branch.contains("feature/"))
                    .collect();

                if ccswarm_worktrees.is_empty() {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "No ccswarm worktrees found",
                            }))?
                        );
                    } else {
                        println!("✅ No ccswarm worktrees to clean");
                    }
                    return Ok(());
                }

                // Ask for confirmation unless forced
                if !force {
                    println!("⚠️  Found {} ccswarm worktrees:", ccswarm_worktrees.len());
                    for w in &ccswarm_worktrees {
                        println!("   - {} ({})", w.path.display(), w.branch);
                    }
                    print!("\nAre you sure you want to remove all these worktrees? [y/N] ");
                    io::stdout().flush()?;

                    let mut response = String::new();
                    io::stdin().read_line(&mut response)?;

                    if !response.trim().eq_ignore_ascii_case("y") {
                        println!("❌ Cleanup cancelled");
                        return Ok(());
                    }
                }

                // Remove all ccswarm worktrees
                let mut removed_count = 0;
                for worktree in ccswarm_worktrees {
                    match manager.remove_worktree(&worktree.path).await {
                        Ok(_) => {
                            removed_count += 1;
                            if !self.json_output {
                                println!("   ✓ Removed {}", worktree.path.display());
                            }
                        }
                        Err(e) => {
                            if !self.json_output {
                                println!(
                                    "   ✗ Failed to remove {}: {}",
                                    worktree.path.display(),
                                    e
                                );
                            }
                        }
                    }
                }

                // Also clean up branches
                let output = tokio::process::Command::new("git")
                    .args(["branch", "--list", "*agent*", "feature/*"])
                    .output()
                    .await?;

                if output.status.success() {
                    let branches = String::from_utf8_lossy(&output.stdout);
                    let branch_count = branches.lines().count();

                    if branch_count > 0 {
                        tokio::process::Command::new("git")
                            .args(&["branch", "-D"])
                            .args(branches.lines().map(|b| b.trim().trim_start_matches("* ")))
                            .output()
                            .await?;
                    }
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Cleanup completed",
                            "worktrees_removed": removed_count,
                        }))?
                    );
                } else {
                    println!(
                        "\n✅ Cleanup completed: {} worktrees removed",
                        removed_count
                    );
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_extend (lines 3806-4171) ---[0m
    async fn handle_extend(&self, action: &ExtendAction) -> Result<()> {
        use crate::extension_stub::meta_learning::MetaLearningSystem;
        use crate::extension_stub::{
            ExtensionManager, ExtensionProposal, ExtensionStatus, ExtensionType,
        };
        use chrono::Utc;

        // Create extension manager instance
        let mut extension_manager = ExtensionManager::new();
        let _meta_learning = MetaLearningSystem::new();

        match action {
            ExtendAction::Propose {
                agent,
                extension_type,
                file,
            } => {
                // Read extension specification from file
                let spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read extension specification file")?;

                // Parse extension type
                let ext_type = match extension_type.as_str() {
                    "capability" => ExtensionType::Capability,
                    "system" => ExtensionType::System,
                    "cognitive" => ExtensionType::Cognitive,
                    "collaborative" => ExtensionType::Collaborative,
                    _ => ExtensionType::Capability, // Default
                };

                // Create extension proposal
                let proposal = ExtensionProposal {
                    id: uuid::Uuid::new_v4().to_string(),
                    agent_id: agent.clone(),
                    proposer: agent.clone(),
                    extension_type: ext_type,
                    status: ExtensionStatus::Proposed,
                    specification: spec_content.clone(),
                    title: format!("{} extension for {}", extension_type, agent),
                    description: spec_content.lines().take(3).collect::<Vec<_>>().join(" "),
                    current_state: crate::extension_stub::CurrentState {
                        capabilities: vec!["basic functionality".to_string()],
                        limitations: vec!["needs enhancement".to_string()],
                        performance_metrics: std::collections::HashMap::new(),
                    },
                    proposed_state: crate::extension_stub::ProposedState {
                        new_capabilities: vec!["enhanced functionality".to_string()],
                        removed_limitations: vec!["slow processing".to_string()],
                        expected_improvements: {
                            let mut map = std::collections::HashMap::new();
                            map.insert("performance".to_string(), 25.0);
                            map
                        },
                        performance_targets: std::collections::HashMap::new(),
                    },
                    implementation_plan: crate::extension_stub::ImplementationPlan {
                        total_duration: std::time::Duration::from_secs(2419200), // 4 weeks
                        phases: vec![
                            crate::extension_stub::ImplementationPhase {
                                name: "Analysis & Design".to_string(),
                                description: "Analyze requirements and design the extension"
                                    .to_string(),
                                tasks: vec![
                                    "Design document".to_string(),
                                    "Technical specification".to_string(),
                                ],
                                validation_method: "Code review".to_string(),
                                estimated_duration: std::time::Duration::from_secs(604800), // 1 week
                                complexity: crate::extension_stub::ComplexityLevel::Medium,
                                dependencies: Vec::new(),
                            },
                            crate::extension_stub::ImplementationPhase {
                                name: "Implementation".to_string(),
                                description: "Implement the extension functionality".to_string(),
                                tasks: vec!["Working code".to_string(), "Unit tests".to_string()],
                                validation_method: "Testing".to_string(),
                                estimated_duration: std::time::Duration::from_secs(1209600), // 2 weeks
                                complexity: crate::extension_stub::ComplexityLevel::High,
                                dependencies: vec!["Analysis & Design".to_string()],
                            },
                            crate::extension_stub::ImplementationPhase {
                                name: "Testing & Deployment".to_string(),
                                description: "Test and deploy the extension".to_string(),
                                tasks: vec![
                                    "Test results".to_string(),
                                    "Deployed extension".to_string(),
                                ],
                                validation_method: "Production testing".to_string(),
                                estimated_duration: std::time::Duration::from_secs(604800), // 1 week
                                complexity: crate::extension_stub::ComplexityLevel::Medium,
                                dependencies: vec!["Implementation".to_string()],
                            },
                        ],
                        timeline: "2-4 weeks".to_string(),
                        resources_required: vec!["development time".to_string()],
                        dependencies: vec![],
                    },
                    risk_assessment: crate::extension_stub::RiskAssessment {
                        risk_level: crate::extension_stub::RiskLevel::Low,
                        potential_issues: vec!["Minor performance impact".to_string()],
                        mitigation_strategies: vec![],
                        rollback_plan: "Revert to previous version".to_string(),
                        overall_risk_score: 0.3,
                        categories: vec![crate::extension_stub::RiskCategory::Performance],
                    },
                    success_criteria: vec![
                        crate::extension_stub::SuccessCriterion {
                            name: "Functionality".to_string(),
                            description: "Extension functionality working correctly".to_string(),
                            target_value: 100.0,
                            measurement_method: "Automated testing".to_string(),
                            measurable: true,
                        },
                        crate::extension_stub::SuccessCriterion {
                            name: "Performance".to_string(),
                            description: "No performance degradation".to_string(),
                            target_value: 100.0, // milliseconds
                            measurement_method: "Performance benchmarks".to_string(),
                            measurable: true,
                        },
                    ],
                    created_at: Utc::now(),
                };

                // Submit proposal to extension manager
                let proposal_id = extension_manager.propose_extension(proposal).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension proposed",
                            "proposal_id": proposal_id,
                            "agent": agent,
                            "type": extension_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!("🔧 Proposing extension for agent: {}", agent);
                    println!("   Type: {}", extension_type);
                    println!("   Specification: {}", file.display());
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Extension proposal submitted");
                }
            }

            ExtendAction::Status {
                agent,
                extension_id,
            } => {
                // Get extension manager statistics
                let stats = extension_manager.get_stats().await;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension status retrieved",
                            "agent": agent,
                            "extension_id": extension_id,
                            "stats": {
                                "total_extensions": stats.total_extensions,
                                "active_extensions": stats.active_extensions,
                                "pending_proposals": stats.pending_proposals,
                                "successful_extensions": stats.successful_extensions,
                                "failed_extensions": stats.failed_extensions,
                            }
                        }))?
                    );
                } else {
                    println!("📊 Extension Status for agent: {}", agent);
                    if let Some(id) = extension_id {
                        println!("🆔 Extension ID: {}", id);
                    }
                    println!("📈 Statistics:");
                    println!("   Total Extensions: {}", stats.total_extensions);
                    println!("   Active: {}", stats.active_extensions);
                    println!("   Pending Proposals: {}", stats.pending_proposals);
                    println!("   Successful: {}", stats.successful_extensions);
                    println!("   Failed: {}", stats.failed_extensions);
                }
            }

            ExtendAction::History {
                agent,
                successful,
                failed,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension history",
                            "agent": agent,
                            "filter": {
                                "successful": successful,
                                "failed": failed,
                            },
                        }))?
                    );
                } else {
                    println!("📜 Extension History for agent: {}", agent);
                    println!("================================");
                    if *successful {
                        println!("Showing only successful extensions");
                    } else if *failed {
                        println!("Showing only failed extensions");
                    }
                    println!("No extensions found");
                }
            }

            ExtendAction::Rollback {
                agent,
                extension_id,
                force,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension rolled back",
                            "agent": agent,
                            "extension_id": extension_id,
                            "force": force,
                        }))?
                    );
                } else {
                    println!("⏪ Rolling back extension: {}", extension_id);
                    println!("   Agent: {}", agent);
                    if *force {
                        println!("   ⚠️ Force rollback enabled");
                    }
                    println!("✅ Rollback completed");
                }
            }

            ExtendAction::Discover {
                agent,
                discovery_type,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Discovery completed",
                            "agent": agent,
                            "discovery_type": discovery_type,
                        }))?
                    );
                } else {
                    println!("🔍 Discovering extension opportunities");
                    if let Some(a) = agent {
                        println!("   Agent: {}", a);
                    }
                    if let Some(dt) = discovery_type {
                        println!("   Type: {}", dt);
                    }
                    println!("✅ Discovery completed");
                }
            }

            ExtendAction::Autonomous {
                agent,
                continuous,
                dry_run,
            } => {
                // use crate::extension::autonomous_agent_extension::AutonomousAgentExtensionManager;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "started",
                            "message": "Autonomous extension reasoning initiated",
                            "agent": agent,
                            "continuous": continuous,
                            "dry_run": dry_run,
                        }))?
                    );
                } else {
                    println!("🤖 Initiating autonomous extension reasoning");
                    if let Some(a) = agent {
                        println!("   Agent: {}", a);
                    } else {
                        println!("   Agents: All agents");
                    }
                    if *continuous {
                        println!(
                            "   Mode: Continuous (agents will autonomously propose extensions)"
                        );
                    }
                    if *dry_run {
                        println!("   🔍 DRY RUN - proposals will not be submitted to Sangha");
                    }
                }

                // Get agent list
                let target_agents = if let Some(agent_id) = agent {
                    vec![agent_id.clone()]
                } else {
                    // Get all agents from config
                    self.config.agents.keys().cloned().collect()
                };

                // Process each agent
                for agent_id in target_agents {
                    if !self.json_output {
                        println!("\n🎯 Processing agent: {}", agent_id);
                    }

                    // Create autonomous extension manager for the agent
                    // In real implementation, would create proper provider and sangha client
                    // For now, just log the intent

                    if *dry_run {
                        if !self.json_output {
                            println!("   📋 Would propose extensions based on:");
                            println!("      - Past performance analysis");
                            println!("      - Identified capability gaps");
                            println!("      - Recurring failure patterns");
                            println!("      - Self-reflection insights");
                        }
                    } else if !self.json_output {
                        println!("   🧠 Analyzing experiences and performance...");
                        println!("   🔍 Identifying capability gaps...");
                        println!("   💡 Generating extension proposals...");
                        println!("   🏛️  Submitting proposals to Sangha for approval...");

                        // In real implementation:
                        // 1. Create AutonomousAgentExtensionManager
                        // 2. Call propose_extensions()
                        // 3. Wait for Sangha consensus
                        // 4. Report results
                    }
                }

                if *continuous && !self.json_output {
                    println!("\n♾️  Continuous mode enabled - agents will autonomously propose extensions as needed");
                    println!("   Press Ctrl+C to stop");
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "completed",
                            "message": "Autonomous extension reasoning completed",
                            "continuous_mode": continuous,
                        }))?
                    );
                } else {
                    println!("\n✅ Autonomous extension reasoning completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:3806-4171 method handle_extend <-> ./crates/ccswarm/src/cli/mod.rs:5584-5809 method handle_doctor
  Similarity: 89.37%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_extend (lines 3806-4171) ---[0m
    async fn handle_extend(&self, action: &ExtendAction) -> Result<()> {
        use crate::extension_stub::meta_learning::MetaLearningSystem;
        use crate::extension_stub::{
            ExtensionManager, ExtensionProposal, ExtensionStatus, ExtensionType,
        };
        use chrono::Utc;

        // Create extension manager instance
        let mut extension_manager = ExtensionManager::new();
        let _meta_learning = MetaLearningSystem::new();

        match action {
            ExtendAction::Propose {
                agent,
                extension_type,
                file,
            } => {
                // Read extension specification from file
                let spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read extension specification file")?;

                // Parse extension type
                let ext_type = match extension_type.as_str() {
                    "capability" => ExtensionType::Capability,
                    "system" => ExtensionType::System,
                    "cognitive" => ExtensionType::Cognitive,
                    "collaborative" => ExtensionType::Collaborative,
                    _ => ExtensionType::Capability, // Default
                };

                // Create extension proposal
                let proposal = ExtensionProposal {
                    id: uuid::Uuid::new_v4().to_string(),
                    agent_id: agent.clone(),
                    proposer: agent.clone(),
                    extension_type: ext_type,
                    status: ExtensionStatus::Proposed,
                    specification: spec_content.clone(),
                    title: format!("{} extension for {}", extension_type, agent),
                    description: spec_content.lines().take(3).collect::<Vec<_>>().join(" "),
                    current_state: crate::extension_stub::CurrentState {
                        capabilities: vec!["basic functionality".to_string()],
                        limitations: vec!["needs enhancement".to_string()],
                        performance_metrics: std::collections::HashMap::new(),
                    },
                    proposed_state: crate::extension_stub::ProposedState {
                        new_capabilities: vec!["enhanced functionality".to_string()],
                        removed_limitations: vec!["slow processing".to_string()],
                        expected_improvements: {
                            let mut map = std::collections::HashMap::new();
                            map.insert("performance".to_string(), 25.0);
                            map
                        },
                        performance_targets: std::collections::HashMap::new(),
                    },
                    implementation_plan: crate::extension_stub::ImplementationPlan {
                        total_duration: std::time::Duration::from_secs(2419200), // 4 weeks
                        phases: vec![
                            crate::extension_stub::ImplementationPhase {
                                name: "Analysis & Design".to_string(),
                                description: "Analyze requirements and design the extension"
                                    .to_string(),
                                tasks: vec![
                                    "Design document".to_string(),
                                    "Technical specification".to_string(),
                                ],
                                validation_method: "Code review".to_string(),
                                estimated_duration: std::time::Duration::from_secs(604800), // 1 week
                                complexity: crate::extension_stub::ComplexityLevel::Medium,
                                dependencies: Vec::new(),
                            },
                            crate::extension_stub::ImplementationPhase {
                                name: "Implementation".to_string(),
                                description: "Implement the extension functionality".to_string(),
                                tasks: vec!["Working code".to_string(), "Unit tests".to_string()],
                                validation_method: "Testing".to_string(),
                                estimated_duration: std::time::Duration::from_secs(1209600), // 2 weeks
                                complexity: crate::extension_stub::ComplexityLevel::High,
                                dependencies: vec!["Analysis & Design".to_string()],
                            },
                            crate::extension_stub::ImplementationPhase {
                                name: "Testing & Deployment".to_string(),
                                description: "Test and deploy the extension".to_string(),
                                tasks: vec![
                                    "Test results".to_string(),
                                    "Deployed extension".to_string(),
                                ],
                                validation_method: "Production testing".to_string(),
                                estimated_duration: std::time::Duration::from_secs(604800), // 1 week
                                complexity: crate::extension_stub::ComplexityLevel::Medium,
                                dependencies: vec!["Implementation".to_string()],
                            },
                        ],
                        timeline: "2-4 weeks".to_string(),
                        resources_required: vec!["development time".to_string()],
                        dependencies: vec![],
                    },
                    risk_assessment: crate::extension_stub::RiskAssessment {
                        risk_level: crate::extension_stub::RiskLevel::Low,
                        potential_issues: vec!["Minor performance impact".to_string()],
                        mitigation_strategies: vec![],
                        rollback_plan: "Revert to previous version".to_string(),
                        overall_risk_score: 0.3,
                        categories: vec![crate::extension_stub::RiskCategory::Performance],
                    },
                    success_criteria: vec![
                        crate::extension_stub::SuccessCriterion {
                            name: "Functionality".to_string(),
                            description: "Extension functionality working correctly".to_string(),
                            target_value: 100.0,
                            measurement_method: "Automated testing".to_string(),
                            measurable: true,
                        },
                        crate::extension_stub::SuccessCriterion {
                            name: "Performance".to_string(),
                            description: "No performance degradation".to_string(),
                            target_value: 100.0, // milliseconds
                            measurement_method: "Performance benchmarks".to_string(),
                            measurable: true,
                        },
                    ],
                    created_at: Utc::now(),
                };

                // Submit proposal to extension manager
                let proposal_id = extension_manager.propose_extension(proposal).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension proposed",
                            "proposal_id": proposal_id,
                            "agent": agent,
                            "type": extension_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!("🔧 Proposing extension for agent: {}", agent);
                    println!("   Type: {}", extension_type);
                    println!("   Specification: {}", file.display());
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Extension proposal submitted");
                }
            }

            ExtendAction::Status {
                agent,
                extension_id,
            } => {
                // Get extension manager statistics
                let stats = extension_manager.get_stats().await;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension status retrieved",
                            "agent": agent,
                            "extension_id": extension_id,
                            "stats": {
                                "total_extensions": stats.total_extensions,
                                "active_extensions": stats.active_extensions,
                                "pending_proposals": stats.pending_proposals,
                                "successful_extensions": stats.successful_extensions,
                                "failed_extensions": stats.failed_extensions,
                            }
                        }))?
                    );
                } else {
                    println!("📊 Extension Status for agent: {}", agent);
                    if let Some(id) = extension_id {
                        println!("🆔 Extension ID: {}", id);
                    }
                    println!("📈 Statistics:");
                    println!("   Total Extensions: {}", stats.total_extensions);
                    println!("   Active: {}", stats.active_extensions);
                    println!("   Pending Proposals: {}", stats.pending_proposals);
                    println!("   Successful: {}", stats.successful_extensions);
                    println!("   Failed: {}", stats.failed_extensions);
                }
            }

            ExtendAction::History {
                agent,
                successful,
                failed,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension history",
                            "agent": agent,
                            "filter": {
                                "successful": successful,
                                "failed": failed,
                            },
                        }))?
                    );
                } else {
                    println!("📜 Extension History for agent: {}", agent);
                    println!("================================");
                    if *successful {
                        println!("Showing only successful extensions");
                    } else if *failed {
                        println!("Showing only failed extensions");
                    }
                    println!("No extensions found");
                }
            }

            ExtendAction::Rollback {
                agent,
                extension_id,
                force,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension rolled back",
                            "agent": agent,
                            "extension_id": extension_id,
                            "force": force,
                        }))?
                    );
                } else {
                    println!("⏪ Rolling back extension: {}", extension_id);
                    println!("   Agent: {}", agent);
                    if *force {
                        println!("   ⚠️ Force rollback enabled");
                    }
                    println!("✅ Rollback completed");
                }
            }

            ExtendAction::Discover {
                agent,
                discovery_type,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Discovery completed",
                            "agent": agent,
                            "discovery_type": discovery_type,
                        }))?
                    );
                } else {
                    println!("🔍 Discovering extension opportunities");
                    if let Some(a) = agent {
                        println!("   Agent: {}", a);
                    }
                    if let Some(dt) = discovery_type {
                        println!("   Type: {}", dt);
                    }
                    println!("✅ Discovery completed");
                }
            }

            ExtendAction::Autonomous {
                agent,
                continuous,
                dry_run,
            } => {
                // use crate::extension::autonomous_agent_extension::AutonomousAgentExtensionManager;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "started",
                            "message": "Autonomous extension reasoning initiated",
                            "agent": agent,
                            "continuous": continuous,
                            "dry_run": dry_run,
                        }))?
                    );
                } else {
                    println!("🤖 Initiating autonomous extension reasoning");
                    if let Some(a) = agent {
                        println!("   Agent: {}", a);
                    } else {
                        println!("   Agents: All agents");
                    }
                    if *continuous {
                        println!(
                            "   Mode: Continuous (agents will autonomously propose extensions)"
                        );
                    }
                    if *dry_run {
                        println!("   🔍 DRY RUN - proposals will not be submitted to Sangha");
                    }
                }

                // Get agent list
                let target_agents = if let Some(agent_id) = agent {
                    vec![agent_id.clone()]
                } else {
                    // Get all agents from config
                    self.config.agents.keys().cloned().collect()
                };

                // Process each agent
                for agent_id in target_agents {
                    if !self.json_output {
                        println!("\n🎯 Processing agent: {}", agent_id);
                    }

                    // Create autonomous extension manager for the agent
                    // In real implementation, would create proper provider and sangha client
                    // For now, just log the intent

                    if *dry_run {
                        if !self.json_output {
                            println!("   📋 Would propose extensions based on:");
                            println!("      - Past performance analysis");
                            println!("      - Identified capability gaps");
                            println!("      - Recurring failure patterns");
                            println!("      - Self-reflection insights");
                        }
                    } else if !self.json_output {
                        println!("   🧠 Analyzing experiences and performance...");
                        println!("   🔍 Identifying capability gaps...");
                        println!("   💡 Generating extension proposals...");
                        println!("   🏛️  Submitting proposals to Sangha for approval...");

                        // In real implementation:
                        // 1. Create AutonomousAgentExtensionManager
                        // 2. Call propose_extensions()
                        // 3. Wait for Sangha consensus
                        // 4. Report results
                    }
                }

                if *continuous && !self.json_output {
                    println!("\n♾️  Continuous mode enabled - agents will autonomously propose extensions as needed");
                    println!("   Press Ctrl+C to stop");
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "completed",
                            "message": "Autonomous extension reasoning completed",
                            "continuous_mode": continuous,
                        }))?
                    );
                } else {
                    println!("\n✅ Autonomous extension reasoning completed");
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_doctor (lines 5584-5809) ---[0m
    async fn handle_doctor(
        &self,
        fix: bool,
        error_code: Option<&str>,
        check_api: bool,
    ) -> Result<()> {
        use crate::utils::error_recovery::ErrorRecoveryDB;
        use crate::utils::user_error::CommonErrors;

        // Handle specific error code diagnosis
        if let Some(code) = error_code {
            println!("{}", "🔍 Error Code Diagnosis".bright_cyan().bold());
            println!("{}", "=======================".bright_cyan());
            println!();
            println!("Analyzing error code: {}", code.bright_yellow());
            println!();

            let recovery_db = ErrorRecoveryDB::new();
            if let Some(recovery) = recovery_db.get_recovery(code) {
                match &recovery {
                    crate::utils::error_recovery::RecoveryStep::UserAction { description } => {
                        println!("📋 {}", description.bright_white());
                    },
                    crate::utils::error_recovery::RecoveryStep::Command { description, .. } => {
                        println!("📋 {}", description.bright_white());
                    },
                    crate::utils::error_recovery::RecoveryStep::FileCreate { path, .. } => {
                        println!("📋 Create file: {}", path.bright_white());
                    },
                    crate::utils::error_recovery::RecoveryStep::EnvVar { name, .. } => {
                        println!("📋 Set environment variable: {}", name.bright_white());
                    },
                }
                println!();
                println!("Recovery steps:");
                let steps = [recovery.clone()]; // Treat the single recovery step as the step list
                for (i, step) in steps.iter().enumerate() {
                    match step {
                        crate::utils::error_recovery::RecoveryStep::Command {
                            cmd,
                            description,
                        } => {
                            println!("  {}. {} {}", i + 1, "Run:".bright_yellow(), description);
                            println!("     {}", cmd.bright_white().on_black());
                        }
                        crate::utils::error_recovery::RecoveryStep::FileCreate { path, .. } => {
                            println!(
                                "  {}. {} {}",
                                i + 1,
                                "Create file:".bright_yellow(),
                                path.bright_white()
                            );
                        }
                        crate::utils::error_recovery::RecoveryStep::EnvVar { name, example } => {
                            println!(
                                "  {}. {} {}",
                                i + 1,
                                "Set environment variable:".bright_yellow(),
                                name.bright_white()
                            );
                            println!("     Example: {}={}", name, example.bright_black());
                        }
                        crate::utils::error_recovery::RecoveryStep::UserAction { description } => {
                            println!(
                                "  {}. {} {}",
                                i + 1,
                                "Action required:".bright_yellow(),
                                description
                            );
                        }
                    }
                    println!();
                }

                let can_auto_fix = matches!(recovery, crate::utils::error_recovery::RecoveryStep::Command { .. });
                if can_auto_fix && fix {
                    recovery_db.auto_fix(code).await?;
                } else if can_auto_fix {
                    println!(
                        "💡 This error can be auto-fixed! Run: ccswarm doctor --error {} --fix",
                        code
                    );
                }
            } else {
                println!("❌ Unknown error code: {}", code);
                println!("   See all error codes: ccswarm help errors");
            }
            return Ok(());
        }

        // Handle API connectivity check
        if check_api {
            println!("{}", "🌐 API Connectivity Check".bright_cyan().bold());
            println!("{}", "=========================".bright_cyan());
            println!();

            print!("Testing Anthropic API... ");
            match reqwest::get("https://api.anthropic.com/v1/health").await {
                Ok(resp) if resp.status().is_success() => {
                    println!("{}", "✅ Connected".bright_green());
                }
                Ok(resp) => {
                    println!(
                        "{}",
                        format!("⚠️  Status: {}", resp.status()).bright_yellow()
                    );
                }
                Err(e) => {
                    println!("{}", "❌ Failed".bright_red());
                    println!("   {}", e.to_string().bright_black());
                }
            }
            println!();
            return Ok(());
        }

        println!("{}", "🏥 ccswarm System Diagnosis".bright_cyan().bold());
        println!("{}", "===========================".bright_cyan());
        println!();

        let mut issues = Vec::new();

        // Check Git
        print!("Checking Git... ");
        match std::process::Command::new("git").arg("--version").output() {
            Ok(output) if output.status.success() => {
                println!("{}", "✅ OK".bright_green());
            }
            _ => {
                println!("{}", "❌ Not found".bright_red());
                issues.push("git");
            }
        }

        // Check API keys
        print!("Checking API keys... ");
        if std::env::var("ANTHROPIC_API_KEY").is_ok() {
            println!("{}", "✅ Set".bright_green());
        } else {
            println!("{}", "⚠️  Not set".bright_yellow());
            issues.push("api_key");
        }

        // Check config
        print!("Checking configuration... ");
        let config_path = self.repo_path.join("ccswarm.json");
        if config_path.exists() {
            match CcswarmConfig::from_file(config_path.clone()).await {
                Ok(_) => println!("{}", "✅ Valid".bright_green()),
                Err(e) => {
                    println!("{}", "❌ Invalid".bright_red());
                    println!("   {}", e.to_string().bright_black());
                    issues.push("config");
                }
            }
        } else {
            println!("{}", "❌ Not found".bright_red());
            issues.push("config");
        }

        // Check git repo
        print!("Checking git repository... ");
        let git_dir = self.repo_path.join(".git");
        if git_dir.exists() {
            println!("{}", "✅ Initialized".bright_green());
        } else {
            println!("{}", "⚠️  Not initialized".bright_yellow());
            issues.push("git_repo");
        }

        // Check disk space
        print!("Checking disk space... ");
        // Simple check - in real implementation would use proper system calls
        println!("{}", "✅ Sufficient".bright_green());

        println!();

        if issues.is_empty() {
            println!("{}", "✅ All systems operational!".bright_green().bold());
        } else {
            println!(
                "{}",
                format!("⚠️  Found {} issues", issues.len())
                    .bright_yellow()
                    .bold()
            );

            if fix {
                println!();
                println!("{}", "🔧 Attempting fixes...".bright_cyan());

                for issue in &issues {
                    match *issue {
                        "git" => {
                            println!("• Git: Please install git from https://git-scm.com");
                        }
                        "api_key" => {
                            CommonErrors::api_key_missing("Anthropic").display();
                        }
                        "config" => {
                            println!("• Config: Run 'ccswarm setup' to create configuration");
                        }
                        "git_repo" => {
                            if fix {
                                println!("• Initializing git repository...");
                                crate::git::shell::ShellWorktreeManager::init_if_needed(
                                    &self.repo_path,
                                )
                                .await?;
                                println!("  ✅ Git repository initialized");
                            }
                        }
                        _ => {}
                    }
                }
            } else {
                println!();
                println!(
                    "{}",
                    "💡 Run with --fix to attempt automatic fixes".bright_black()
                );
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:4173-4336 method handle_search <-> ./crates/ccswarm/src/cli/mod.rs:5008-5380 method handle_template
  Similarity: 96.87%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_search (lines 4173-4336) ---[0m
    async fn handle_search(&self, action: &SearchAction) -> Result<()> {
        use crate::extension_stub::agent_extension::{
            DocumentationSearchStrategy, GitHubSearchStrategy, SearchContext, SearchFilters,
            SearchQuery, SearchStrategy, StackOverflowSearchStrategy,
        };

        match action {
            SearchAction::Docs {
                query,
                source,
                limit,
            } => {
                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: Some("rust".to_string()),
                        framework: Some("general".to_string()),
                        date_range: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec![source.clone()],
                        relevance_threshold: 0.5,
                    }),
                };

                let mut all_results = Vec::new();

                if source == "all" || source == "mdn" {
                    let mdn_strategy = DocumentationSearchStrategy::new();
                    match mdn_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("MDN search failed: {}", e),
                    }
                }

                if source == "all" || source == "github" {
                    let github_strategy = GitHubSearchStrategy::new();
                    match github_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("GitHub search failed: {}", e),
                    }
                }

                if source == "all" || source == "stackoverflow" {
                    let stackoverflow_strategy = StackOverflowSearchStrategy;
                    match stackoverflow_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("StackOverflow search failed: {}", e),
                    }
                }

                all_results.sort_by(|a, b| {
                    b.relevance_score
                        .partial_cmp(&a.relevance_score)
                        .unwrap_or(std::cmp::Ordering::Equal)
                });
                all_results.truncate(*limit);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "query": query,
                            "source": source,
                            "results": all_results.iter().map(|r| serde_json::json!({
                                "title": r.title,
                                "url": r.url,
                                "snippet": r.snippet,
                                "source": r.source,
                                "relevance_score": r.relevance_score,
                            })).collect::<Vec<_>>()
                        }))?
                    );
                } else {
                    println!("🔍 Searching documentation for: {}", query);
                    println!("   Source: {}", source);
                    println!("   Found {} results", all_results.len());
                    println!();

                    for (i, result) in all_results.iter().enumerate() {
                        println!("{}. {} ({})", i + 1, result.title, result.source);
                        println!("   🔗 {}", result.url);
                        println!("   📝 {}", result.snippet);
                        println!("   ⭐ Relevance: {:.2}", result.relevance_score);
                        println!();
                    }
                }
            }

            SearchAction::Test { query } => {
                println!("🧪 Testing search functionality with query: {}", query);

                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: None,
                        framework: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec!["all".to_string()],
                        relevance_threshold: 0.5,
                        date_range: None,
                    }),
                };

                // Test each search strategy
                println!("🔍 Testing MDN search...");
                let mdn_strategy = DocumentationSearchStrategy::new();
                match mdn_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ MDN: Found {} results", results.len()),
                    Err(e) => println!("❌ MDN: Error - {}", e),
                }

                println!("🔍 Testing GitHub search...");
                let github_strategy = GitHubSearchStrategy::new();
                match github_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ GitHub: Found {} results", results.len()),
                    Err(e) => println!("❌ GitHub: Error - {}", e),
                }

                println!("🔍 Testing StackOverflow search...");
                let stackoverflow_strategy = StackOverflowSearchStrategy;
                match stackoverflow_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ StackOverflow: Found {} results", results.len()),
                    Err(e) => println!("❌ StackOverflow: Error - {}", e),
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Search test completed",
                            "query": query,
                        }))?
                    );
                } else {
                    println!("✅ Search functionality test completed");
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_template (lines 5008-5380) ---[0m
    async fn handle_template(&self, action: &TemplateAction) -> Result<()> {
        use crate::template::{
            FileSystemTemplateStorage, PredefinedTemplates, TemplateCategory, TemplateContext,
            TemplateManager, TemplateQuery,
        };
        use colored::Colorize;
        use std::io::{self, Write};
        use std::str::FromStr;

        // Initialize template storage
        let templates_dir = self.repo_path.join(".ccswarm").join("templates");
        let storage = FileSystemTemplateStorage::new(&templates_dir)
            .await
            .context("Failed to initialize template storage")?;
        let mut manager = TemplateManager::new(storage);

        match action {
            TemplateAction::List {
                all: _,
                category,
                tags,
                search,
                popular,
                quality,
                detailed,
            } => {
                let mut query = TemplateQuery::new();

                if let Some(cat_str) = category {
                    let cat = TemplateCategory::from_str(cat_str).context("Invalid category")?;
                    query = query.with_category(cat);
                }

                if !tags.is_empty() {
                    query = query.with_tags(tags.clone());
                }

                if let Some(search_term) = search {
                    query = query.with_search_term(search_term);
                }

                if *popular {
                    query = query.sort_by_popularity();
                } else if *quality {
                    query = query.sort_by_success_rate();
                }

                let templates = manager
                    .search_templates(query)
                    .await
                    .context("Failed to search templates")?;

                if templates.is_empty() {
                    println!("No templates found.");
                    return Ok(());
                }

                println!("Available Templates:");
                println!();

                for template in templates {
                    if *detailed {
                        println!(
                            "📋 {} ({})",
                            template.name.bright_cyan(),
                            template.id.as_str().bright_black()
                        );
                        println!("   Category: {}", template.category);
                        println!("   Description: {}", template.description);
                        if !template.tags.is_empty() {
                            println!(
                                "   Tags: {}",
                                template.tags.join(", ").as_str().bright_black()
                            );
                        }
                        if let Some(author) = &template.author {
                            println!("   Author: {}", author.as_str().bright_black());
                        }
                        println!("   Usage: {} times", template.usage_count);
                        if let Some(rate) = template.success_rate {
                            println!("   Success Rate: {:.1}%", rate * 100.0);
                        }
                        println!();
                    } else {
                        println!(
                            "  {} ({}) - {}",
                            template.name.bright_cyan(),
                            template.id.bright_black(),
                            template.description.chars().take(80).collect::<String>()
                        );
                    }
                }
            }

            TemplateAction::Show {
                template,
                source,
                stats,
            } => {
                let tmpl = manager
                    .get_template_by_name(template)
                    .await
                    .context("Template not found")?;

                println!(
                    "Template: {} ({})",
                    tmpl.name.bright_cyan(),
                    tmpl.id.bright_black()
                );
                println!("Category: {}", tmpl.category);
                println!("Description: {}", tmpl.description);
                println!("Version: {}", tmpl.version);

                if let Some(author) = &tmpl.author {
                    println!("Author: {}", author);
                }

                if !tmpl.tags.is_empty() {
                    println!("Tags: {}", tmpl.tags.join(", "));
                }

                println!("Priority: {:?}", tmpl.default_priority);
                println!("Task Type: {:?}", tmpl.default_task_type);

                if let Some(duration) = tmpl.estimated_duration {
                    println!("Estimated Duration: {} minutes", duration);
                }

                if !tmpl.variables.is_empty() {
                    println!();
                    println!("Variables:");
                    for var in &tmpl.variables {
                        let required = if var.required { " (required)" } else { "" };
                        println!(
                            "  • {}{}: {}",
                            var.name.bright_green(),
                            required,
                            var.description
                        );
                        if let Some(default) = &var.default_value {
                            println!("    Default: {}", default.clone().bright_black());
                        }
                    }
                }

                if *source {
                    println!();
                    println!("Task Description Template:");
                    println!("{}", tmpl.task_description.bright_white());

                    if let Some(details) = &tmpl.task_details {
                        println!();
                        println!("Task Details Template:");
                        println!("{}", details.bright_white());
                    }
                }

                if *stats {
                    println!();
                    println!("Statistics:");
                    println!("  Usage Count: {}", tmpl.usage_count);
                    if let Some(rate) = tmpl.success_rate {
                        println!("  Success Rate: {:.1}%", rate * 100.0);
                    }
                    println!("  Created: {}", tmpl.created_at.format("%Y-%m-%d %H:%M"));
                    println!("  Updated: {}", tmpl.updated_at.format("%Y-%m-%d %H:%M"));
                }
            }

            TemplateAction::Apply {
                template,
                vars,
                interactive,
                preview,
                auto_assign,
            } => {
                let tmpl = manager
                    .get_template_by_name(template)
                    .await
                    .context("Template not found")?;

                let mut context = TemplateContext::new();

                // Parse provided variables
                for var_str in vars {
                    if let Some((key, value)) = var_str.split_once('=') {
                        context = context.with_variable(key.trim(), value.trim());
                    }
                }

                // Interactive mode for missing variables
                if *interactive {
                    for var in &tmpl.variables {
                        if var.required
                            && !context.variables.contains_key(&var.name)
                            && var.default_value.is_none()
                        {
                            print!("{} ({}): ", var.name, var.description);
                            io::stdout().flush()?;

                            let mut input = String::new();
                            io::stdin().read_line(&mut input)?;
                            let value = input.trim();

                            if !value.is_empty() {
                                context = context.with_variable(&var.name, value);
                            }
                        }
                    }
                }

                // Apply template
                let applied = manager
                    .apply_template(&tmpl.id, context)
                    .await
                    .context("Failed to apply template")?;

                if *preview {
                    println!("Preview of generated task:");
                    println!();
                    println!("Description: {}", applied.description.bright_cyan());
                    if let Some(details) = &applied.details {
                        println!("Details: {}", details);
                    }
                    println!("Priority: {:?}", applied.priority);
                    println!("Type: {:?}", applied.task_type);
                    if let Some(duration) = applied.estimated_duration {
                        println!("Duration: {} minutes", duration);
                    }
                    if !applied.target_files.is_empty() {
                        println!("Target Files: {}", applied.target_files.join(", "));
                    }
                } else {
                    // Create the actual task
                    use crate::agent::TaskBuilder;

                    let task = TaskBuilder::new(applied.description.clone())
                        .priority(applied.priority)
                        .task_type(applied.task_type);

                    let task = if let Some(details) = &applied.details {
                        task.details(details.clone())
                    } else {
                        task
                    };

                    let task = if let Some(duration) = applied.estimated_duration {
                        task.estimated_duration(duration as u64)
                    } else {
                        task
                    };

                    let task = task.build();

                    println!(
                        "Created task from template: {}",
                        applied.description.bright_green()
                    );
                    println!("Task ID: {}", task.id.bright_cyan());

                    if *auto_assign {
                        println!("Auto-assigning to best agent...");
                        // TODO: Implement auto-assignment logic
                    }
                }
            }

            TemplateAction::Install {
                all,
                categories,
                force,
            } => {
                let predefined_templates = PredefinedTemplates::get_all();
                let mut installed = 0;
                let mut skipped = 0;

                for template in predefined_templates {
                    // Filter by categories if specified
                    if !categories.is_empty() && !*all {
                        let cat_str = template.category.to_string();
                        if !categories.iter().any(|c| c.eq_ignore_ascii_case(&cat_str)) {
                            continue;
                        }
                    }

                    match manager.save_template(template.clone()).await {
                        Ok(()) => {
                            println!("✅ Installed: {}", template.name.bright_green());
                            installed += 1;
                        }
                        Err(e) if e.to_string().contains("already exists") => {
                            if *force {
                                if let Err(e) = manager.update_template(template.clone()).await {
                                    println!("❌ Failed to update {}: {}", template.name.red(), e);
                                } else {
                                    println!("✅ Updated: {}", template.name.bright_green());
                                    installed += 1;
                                }
                            } else {
                                println!("⚠️  Skipped (exists): {}", template.name.bright_yellow());
                                skipped += 1;
                            }
                        }
                        Err(e) => {
                            println!("❌ Failed to install {}: {}", template.name.red(), e);
                        }
                    }
                }

                println!();
                println!(
                    "Installation complete: {} installed, {} skipped",
                    installed, skipped
                );
                if skipped > 0 && !*force {
                    println!("Use --force to overwrite existing templates");
                }
            }

            TemplateAction::Stats {
                global: _,
                template,
            } => {
                if let Some(tmpl_name) = template {
                    let tmpl = manager
                        .get_template_by_name(tmpl_name)
                        .await
                        .context("Template not found")?;

                    println!("Template Statistics: {}", tmpl.name.bright_cyan());
                    println!("Usage Count: {}", tmpl.usage_count);
                    if let Some(rate) = tmpl.success_rate {
                        println!("Success Rate: {:.1}%", rate * 100.0);
                    }
                    println!("Created: {}", tmpl.created_at.format("%Y-%m-%d %H:%M"));
                    println!("Updated: {}", tmpl.updated_at.format("%Y-%m-%d %H:%M"));
                } else {
                    let stats = manager
                        .get_template_stats()
                        .await
                        .context("Failed to get template statistics")?;

                    println!("Global Template Statistics:");
                    println!("Total Templates: {}", stats.total_templates);
                    println!("Total Usage: {}", stats.total_usage);
                    println!(
                        "Average Success Rate: {:.1}%",
                        stats.average_success_rate * 100.0
                    );

                    println!();
                    println!("By Category:");
                    for (category, count) in &stats.by_category {
                        println!("  {}: {}", category, count);
                    }

                    if !stats.most_popular.is_empty() {
                        println!();
                        println!("Most Popular:");
                        for (name, count) in stats.most_popular.iter().take(5) {
                            println!("  {}: {} uses", name, count);
                        }
                    }
                }
            }

            _ => {
                println!("Template command not yet implemented: {:?}", action);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:3806-4171 method handle_extend <-> ./crates/ccswarm/src/cli/mod.rs:4173-4336 method handle_search
  Similarity: 97.82%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_extend (lines 3806-4171) ---[0m
    async fn handle_extend(&self, action: &ExtendAction) -> Result<()> {
        use crate::extension_stub::meta_learning::MetaLearningSystem;
        use crate::extension_stub::{
            ExtensionManager, ExtensionProposal, ExtensionStatus, ExtensionType,
        };
        use chrono::Utc;

        // Create extension manager instance
        let mut extension_manager = ExtensionManager::new();
        let _meta_learning = MetaLearningSystem::new();

        match action {
            ExtendAction::Propose {
                agent,
                extension_type,
                file,
            } => {
                // Read extension specification from file
                let spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read extension specification file")?;

                // Parse extension type
                let ext_type = match extension_type.as_str() {
                    "capability" => ExtensionType::Capability,
                    "system" => ExtensionType::System,
                    "cognitive" => ExtensionType::Cognitive,
                    "collaborative" => ExtensionType::Collaborative,
                    _ => ExtensionType::Capability, // Default
                };

                // Create extension proposal
                let proposal = ExtensionProposal {
                    id: uuid::Uuid::new_v4().to_string(),
                    agent_id: agent.clone(),
                    proposer: agent.clone(),
                    extension_type: ext_type,
                    status: ExtensionStatus::Proposed,
                    specification: spec_content.clone(),
                    title: format!("{} extension for {}", extension_type, agent),
                    description: spec_content.lines().take(3).collect::<Vec<_>>().join(" "),
                    current_state: crate::extension_stub::CurrentState {
                        capabilities: vec!["basic functionality".to_string()],
                        limitations: vec!["needs enhancement".to_string()],
                        performance_metrics: std::collections::HashMap::new(),
                    },
                    proposed_state: crate::extension_stub::ProposedState {
                        new_capabilities: vec!["enhanced functionality".to_string()],
                        removed_limitations: vec!["slow processing".to_string()],
                        expected_improvements: {
                            let mut map = std::collections::HashMap::new();
                            map.insert("performance".to_string(), 25.0);
                            map
                        },
                        performance_targets: std::collections::HashMap::new(),
                    },
                    implementation_plan: crate::extension_stub::ImplementationPlan {
                        total_duration: std::time::Duration::from_secs(2419200), // 4 weeks
                        phases: vec![
                            crate::extension_stub::ImplementationPhase {
                                name: "Analysis & Design".to_string(),
                                description: "Analyze requirements and design the extension"
                                    .to_string(),
                                tasks: vec![
                                    "Design document".to_string(),
                                    "Technical specification".to_string(),
                                ],
                                validation_method: "Code review".to_string(),
                                estimated_duration: std::time::Duration::from_secs(604800), // 1 week
                                complexity: crate::extension_stub::ComplexityLevel::Medium,
                                dependencies: Vec::new(),
                            },
                            crate::extension_stub::ImplementationPhase {
                                name: "Implementation".to_string(),
                                description: "Implement the extension functionality".to_string(),
                                tasks: vec!["Working code".to_string(), "Unit tests".to_string()],
                                validation_method: "Testing".to_string(),
                                estimated_duration: std::time::Duration::from_secs(1209600), // 2 weeks
                                complexity: crate::extension_stub::ComplexityLevel::High,
                                dependencies: vec!["Analysis & Design".to_string()],
                            },
                            crate::extension_stub::ImplementationPhase {
                                name: "Testing & Deployment".to_string(),
                                description: "Test and deploy the extension".to_string(),
                                tasks: vec![
                                    "Test results".to_string(),
                                    "Deployed extension".to_string(),
                                ],
                                validation_method: "Production testing".to_string(),
                                estimated_duration: std::time::Duration::from_secs(604800), // 1 week
                                complexity: crate::extension_stub::ComplexityLevel::Medium,
                                dependencies: vec!["Implementation".to_string()],
                            },
                        ],
                        timeline: "2-4 weeks".to_string(),
                        resources_required: vec!["development time".to_string()],
                        dependencies: vec![],
                    },
                    risk_assessment: crate::extension_stub::RiskAssessment {
                        risk_level: crate::extension_stub::RiskLevel::Low,
                        potential_issues: vec!["Minor performance impact".to_string()],
                        mitigation_strategies: vec![],
                        rollback_plan: "Revert to previous version".to_string(),
                        overall_risk_score: 0.3,
                        categories: vec![crate::extension_stub::RiskCategory::Performance],
                    },
                    success_criteria: vec![
                        crate::extension_stub::SuccessCriterion {
                            name: "Functionality".to_string(),
                            description: "Extension functionality working correctly".to_string(),
                            target_value: 100.0,
                            measurement_method: "Automated testing".to_string(),
                            measurable: true,
                        },
                        crate::extension_stub::SuccessCriterion {
                            name: "Performance".to_string(),
                            description: "No performance degradation".to_string(),
                            target_value: 100.0, // milliseconds
                            measurement_method: "Performance benchmarks".to_string(),
                            measurable: true,
                        },
                    ],
                    created_at: Utc::now(),
                };

                // Submit proposal to extension manager
                let proposal_id = extension_manager.propose_extension(proposal).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension proposed",
                            "proposal_id": proposal_id,
                            "agent": agent,
                            "type": extension_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!("🔧 Proposing extension for agent: {}", agent);
                    println!("   Type: {}", extension_type);
                    println!("   Specification: {}", file.display());
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Extension proposal submitted");
                }
            }

            ExtendAction::Status {
                agent,
                extension_id,
            } => {
                // Get extension manager statistics
                let stats = extension_manager.get_stats().await;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension status retrieved",
                            "agent": agent,
                            "extension_id": extension_id,
                            "stats": {
                                "total_extensions": stats.total_extensions,
                                "active_extensions": stats.active_extensions,
                                "pending_proposals": stats.pending_proposals,
                                "successful_extensions": stats.successful_extensions,
                                "failed_extensions": stats.failed_extensions,
                            }
                        }))?
                    );
                } else {
                    println!("📊 Extension Status for agent: {}", agent);
                    if let Some(id) = extension_id {
                        println!("🆔 Extension ID: {}", id);
                    }
                    println!("📈 Statistics:");
                    println!("   Total Extensions: {}", stats.total_extensions);
                    println!("   Active: {}", stats.active_extensions);
                    println!("   Pending Proposals: {}", stats.pending_proposals);
                    println!("   Successful: {}", stats.successful_extensions);
                    println!("   Failed: {}", stats.failed_extensions);
                }
            }

            ExtendAction::History {
                agent,
                successful,
                failed,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension history",
                            "agent": agent,
                            "filter": {
                                "successful": successful,
                                "failed": failed,
                            },
                        }))?
                    );
                } else {
                    println!("📜 Extension History for agent: {}", agent);
                    println!("================================");
                    if *successful {
                        println!("Showing only successful extensions");
                    } else if *failed {
                        println!("Showing only failed extensions");
                    }
                    println!("No extensions found");
                }
            }

            ExtendAction::Rollback {
                agent,
                extension_id,
                force,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension rolled back",
                            "agent": agent,
                            "extension_id": extension_id,
                            "force": force,
                        }))?
                    );
                } else {
                    println!("⏪ Rolling back extension: {}", extension_id);
                    println!("   Agent: {}", agent);
                    if *force {
                        println!("   ⚠️ Force rollback enabled");
                    }
                    println!("✅ Rollback completed");
                }
            }

            ExtendAction::Discover {
                agent,
                discovery_type,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Discovery completed",
                            "agent": agent,
                            "discovery_type": discovery_type,
                        }))?
                    );
                } else {
                    println!("🔍 Discovering extension opportunities");
                    if let Some(a) = agent {
                        println!("   Agent: {}", a);
                    }
                    if let Some(dt) = discovery_type {
                        println!("   Type: {}", dt);
                    }
                    println!("✅ Discovery completed");
                }
            }

            ExtendAction::Autonomous {
                agent,
                continuous,
                dry_run,
            } => {
                // use crate::extension::autonomous_agent_extension::AutonomousAgentExtensionManager;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "started",
                            "message": "Autonomous extension reasoning initiated",
                            "agent": agent,
                            "continuous": continuous,
                            "dry_run": dry_run,
                        }))?
                    );
                } else {
                    println!("🤖 Initiating autonomous extension reasoning");
                    if let Some(a) = agent {
                        println!("   Agent: {}", a);
                    } else {
                        println!("   Agents: All agents");
                    }
                    if *continuous {
                        println!(
                            "   Mode: Continuous (agents will autonomously propose extensions)"
                        );
                    }
                    if *dry_run {
                        println!("   🔍 DRY RUN - proposals will not be submitted to Sangha");
                    }
                }

                // Get agent list
                let target_agents = if let Some(agent_id) = agent {
                    vec![agent_id.clone()]
                } else {
                    // Get all agents from config
                    self.config.agents.keys().cloned().collect()
                };

                // Process each agent
                for agent_id in target_agents {
                    if !self.json_output {
                        println!("\n🎯 Processing agent: {}", agent_id);
                    }

                    // Create autonomous extension manager for the agent
                    // In real implementation, would create proper provider and sangha client
                    // For now, just log the intent

                    if *dry_run {
                        if !self.json_output {
                            println!("   📋 Would propose extensions based on:");
                            println!("      - Past performance analysis");
                            println!("      - Identified capability gaps");
                            println!("      - Recurring failure patterns");
                            println!("      - Self-reflection insights");
                        }
                    } else if !self.json_output {
                        println!("   🧠 Analyzing experiences and performance...");
                        println!("   🔍 Identifying capability gaps...");
                        println!("   💡 Generating extension proposals...");
                        println!("   🏛️  Submitting proposals to Sangha for approval...");

                        // In real implementation:
                        // 1. Create AutonomousAgentExtensionManager
                        // 2. Call propose_extensions()
                        // 3. Wait for Sangha consensus
                        // 4. Report results
                    }
                }

                if *continuous && !self.json_output {
                    println!("\n♾️  Continuous mode enabled - agents will autonomously propose extensions as needed");
                    println!("   Press Ctrl+C to stop");
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "completed",
                            "message": "Autonomous extension reasoning completed",
                            "continuous_mode": continuous,
                        }))?
                    );
                } else {
                    println!("\n✅ Autonomous extension reasoning completed");
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_search (lines 4173-4336) ---[0m
    async fn handle_search(&self, action: &SearchAction) -> Result<()> {
        use crate::extension_stub::agent_extension::{
            DocumentationSearchStrategy, GitHubSearchStrategy, SearchContext, SearchFilters,
            SearchQuery, SearchStrategy, StackOverflowSearchStrategy,
        };

        match action {
            SearchAction::Docs {
                query,
                source,
                limit,
            } => {
                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: Some("rust".to_string()),
                        framework: Some("general".to_string()),
                        date_range: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec![source.clone()],
                        relevance_threshold: 0.5,
                    }),
                };

                let mut all_results = Vec::new();

                if source == "all" || source == "mdn" {
                    let mdn_strategy = DocumentationSearchStrategy::new();
                    match mdn_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("MDN search failed: {}", e),
                    }
                }

                if source == "all" || source == "github" {
                    let github_strategy = GitHubSearchStrategy::new();
                    match github_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("GitHub search failed: {}", e),
                    }
                }

                if source == "all" || source == "stackoverflow" {
                    let stackoverflow_strategy = StackOverflowSearchStrategy;
                    match stackoverflow_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("StackOverflow search failed: {}", e),
                    }
                }

                all_results.sort_by(|a, b| {
                    b.relevance_score
                        .partial_cmp(&a.relevance_score)
                        .unwrap_or(std::cmp::Ordering::Equal)
                });
                all_results.truncate(*limit);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "query": query,
                            "source": source,
                            "results": all_results.iter().map(|r| serde_json::json!({
                                "title": r.title,
                                "url": r.url,
                                "snippet": r.snippet,
                                "source": r.source,
                                "relevance_score": r.relevance_score,
                            })).collect::<Vec<_>>()
                        }))?
                    );
                } else {
                    println!("🔍 Searching documentation for: {}", query);
                    println!("   Source: {}", source);
                    println!("   Found {} results", all_results.len());
                    println!();

                    for (i, result) in all_results.iter().enumerate() {
                        println!("{}. {} ({})", i + 1, result.title, result.source);
                        println!("   🔗 {}", result.url);
                        println!("   📝 {}", result.snippet);
                        println!("   ⭐ Relevance: {:.2}", result.relevance_score);
                        println!();
                    }
                }
            }

            SearchAction::Test { query } => {
                println!("🧪 Testing search functionality with query: {}", query);

                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: None,
                        framework: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec!["all".to_string()],
                        relevance_threshold: 0.5,
                        date_range: None,
                    }),
                };

                // Test each search strategy
                println!("🔍 Testing MDN search...");
                let mdn_strategy = DocumentationSearchStrategy::new();
                match mdn_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ MDN: Found {} results", results.len()),
                    Err(e) => println!("❌ MDN: Error - {}", e),
                }

                println!("🔍 Testing GitHub search...");
                let github_strategy = GitHubSearchStrategy::new();
                match github_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ GitHub: Found {} results", results.len()),
                    Err(e) => println!("❌ GitHub: Error - {}", e),
                }

                println!("🔍 Testing StackOverflow search...");
                let stackoverflow_strategy = StackOverflowSearchStrategy;
                match stackoverflow_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ StackOverflow: Found {} results", results.len()),
                    Err(e) => println!("❌ StackOverflow: Error - {}", e),
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Search test completed",
                            "query": query,
                        }))?
                    );
                } else {
                    println!("✅ Search functionality test completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2279-2458 method handle_worktree <-> ./crates/ccswarm/src/cli/mod.rs:4685-5006 method handle_quality
  Similarity: 98.74%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_worktree (lines 2279-2458) ---[0m
    async fn handle_worktree(&self, action: &WorktreeAction) -> Result<()> {
        let manager = crate::git::shell::ShellWorktreeManager::new(self.repo_path.clone())?;

        match action {
            WorktreeAction::List => {
                let worktrees = manager.list_worktrees().await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&worktrees)?);
                } else {
                    println!("🌳 Git Worktrees");
                    println!("===============");

                    for wt in &worktrees {
                        println!("Path: {}", wt.path.display());
                        println!("  Branch: {}", wt.branch);
                        println!("  Head: {}", wt.head_commit);
                        println!("  Locked: {}", wt.is_locked);
                        println!();
                    }
                }
            }
            WorktreeAction::Create {
                path,
                branch,
                new_branch,
            } => {
                let info = if *new_branch {
                    manager.create_worktree_full(path, branch, true).await?
                } else {
                    manager.create_worktree(path, branch).await?
                };

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&info)?);
                } else {
                    println!("✅ Worktree created");
                    println!("   Path: {}", info.path.display());
                    println!("   Branch: {}", info.branch);
                }
            }
            WorktreeAction::Remove { path, force } => {
                if *force {
                    manager.remove_worktree_full(path, true).await?
                } else {
                    manager.remove_worktree(path).await?
                };

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktree removed",
                            "path": path,
                        }))?
                    );
                } else {
                    println!("✅ Worktree removed: {}", path.display());
                }
            }
            WorktreeAction::Prune => {
                manager.prune_worktrees().await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktrees pruned",
                        }))?
                    );
                } else {
                    println!("✅ Stale worktrees pruned");
                }
            }
            WorktreeAction::Clean { force } => {
                use std::io::{self, Write};

                // Find all ccswarm-related worktrees
                let worktrees = manager.list_worktrees().await?;
                let ccswarm_worktrees: Vec<_> = worktrees
                    .iter()
                    .filter(|w| w.branch.contains("agent") || w.branch.contains("feature/"))
                    .collect();

                if ccswarm_worktrees.is_empty() {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "No ccswarm worktrees found",
                            }))?
                        );
                    } else {
                        println!("✅ No ccswarm worktrees to clean");
                    }
                    return Ok(());
                }

                // Ask for confirmation unless forced
                if !force {
                    println!("⚠️  Found {} ccswarm worktrees:", ccswarm_worktrees.len());
                    for w in &ccswarm_worktrees {
                        println!("   - {} ({})", w.path.display(), w.branch);
                    }
                    print!("\nAre you sure you want to remove all these worktrees? [y/N] ");
                    io::stdout().flush()?;

                    let mut response = String::new();
                    io::stdin().read_line(&mut response)?;

                    if !response.trim().eq_ignore_ascii_case("y") {
                        println!("❌ Cleanup cancelled");
                        return Ok(());
                    }
                }

                // Remove all ccswarm worktrees
                let mut removed_count = 0;
                for worktree in ccswarm_worktrees {
                    match manager.remove_worktree(&worktree.path).await {
                        Ok(_) => {
                            removed_count += 1;
                            if !self.json_output {
                                println!("   ✓ Removed {}", worktree.path.display());
                            }
                        }
                        Err(e) => {
                            if !self.json_output {
                                println!(
                                    "   ✗ Failed to remove {}: {}",
                                    worktree.path.display(),
                                    e
                                );
                            }
                        }
                    }
                }

                // Also clean up branches
                let output = tokio::process::Command::new("git")
                    .args(["branch", "--list", "*agent*", "feature/*"])
                    .output()
                    .await?;

                if output.status.success() {
                    let branches = String::from_utf8_lossy(&output.stdout);
                    let branch_count = branches.lines().count();

                    if branch_count > 0 {
                        tokio::process::Command::new("git")
                            .args(&["branch", "-D"])
                            .args(branches.lines().map(|b| b.trim().trim_start_matches("* ")))
                            .output()
                            .await?;
                    }
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Cleanup completed",
                            "worktrees_removed": removed_count,
                        }))?
                    );
                } else {
                    println!(
                        "\n✅ Cleanup completed: {} worktrees removed",
                        removed_count
                    );
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_quality (lines 4685-5006) ---[0m
    async fn handle_quality(&self, action: &QualityAction) -> Result<()> {
        use std::process::Command;

        let mut failed_checks = Vec::new();
        let mut completed_tasks = 0;
        let total_tasks;

        match action {
            QualityAction::Check {
                skip,
                only,
                fail_fast,
            } => {
                println!("🤖 ccswarm Agent-Managed Quality Checks");
                println!("=======================================");
                println!();

                let checks = if only.is_empty() {
                    vec!["format", "lint", "test", "build", "security"]
                } else {
                    only.iter().map(|s| s.as_str()).collect()
                };

                let filtered_checks: Vec<&str> = checks
                    .into_iter()
                    .filter(|check| !skip.contains(&check.to_string()))
                    .collect();

                total_tasks = filtered_checks.len();
                println!("🎯 Master Claude: Orchestrating {} quality checks through specialized agents...", total_tasks);
                println!();

                for check in filtered_checks {
                    let (agent, task, cmd) = match check {
                        "format" => (
                            "DevOps",
                            "Code Formatting Check",
                            vec!["cargo", "fmt", "--check"],
                        ),
                        "lint" => (
                            "DevOps",
                            "Clippy Code Quality Analysis",
                            vec![
                                "cargo",
                                "clippy",
                                "--all-targets",
                                "--all-features",
                                "--",
                                "-D",
                                "warnings",
                            ],
                        ),
                        "test" => (
                            "QA",
                            "Test Suite Execution",
                            vec!["cargo", "test", "--lib", "--verbose", "--no-fail-fast"],
                        ),
                        "build" => (
                            "DevOps",
                            "Build Verification",
                            vec!["cargo", "build", "--verbose"],
                        ),
                        "security" => (
                            "Backend",
                            "Security Analysis",
                            vec![
                                "cargo",
                                "test",
                                "security::owasp_checker::tests",
                                "--no-fail-fast",
                            ],
                        ),
                        _ => continue,
                    };

                    println!("🎯 Delegating to {} agent: {}", agent, task);

                    let mut command = Command::new(&cmd[0]);
                    for arg in &cmd[1..] {
                        command.arg(arg);
                    }

                    let output = command.output().context("Failed to execute command")?;
                    let success = output.status.success();

                    if success {
                        println!("✅ {} agent: {} completed successfully", agent, task);
                        completed_tasks += 1;
                    } else {
                        println!("❌ {} agent: {} failed", agent, task);
                        failed_checks.push((
                            agent,
                            task,
                            String::from_utf8_lossy(&output.stderr).to_string(),
                        ));

                        if *fail_fast {
                            break;
                        }
                    }
                    println!();
                }

                // Quality Gate Assessment
                println!("🎯 Master Claude - Quality Gate Assessment");
                println!("==========================================");
                println!("📊 Agent Task Completion Summary:");
                println!("   Completed: {}/{} tasks", completed_tasks, total_tasks);
                println!("   Failed: {}", failed_checks.len());
                println!();

                if failed_checks.is_empty() {
                    println!("✅ QUALITY GATE: PASSED");
                    println!("🎉 All quality checks passed through agent delegation");
                    println!("🚀 Code is ready for deployment");
                } else {
                    println!("❌ QUALITY GATE: FAILED");
                    println!("🔧 Some quality checks require attention from agents");
                    println!();
                    println!("📋 Failed Checks:");
                    for (agent, task, _error) in &failed_checks {
                        println!("   ❌ {} agent: {}", agent, task);
                    }
                    return Err(anyhow::anyhow!("Quality gate failed"));
                }
            }

            QualityAction::Format { fix } => {
                println!("🛠️ DevOps Agent - Code Formatting");
                println!("==================================");

                let cmd = if *fix {
                    vec!["cargo", "fmt"]
                } else {
                    vec!["cargo", "fmt", "--check"]
                };

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run cargo fmt")?;

                if output.status.success() {
                    println!(
                        "✅ DevOps Agent: Code formatting {} successfully",
                        if *fix { "applied" } else { "verified" }
                    );
                } else {
                    println!("❌ DevOps Agent: Code formatting issues detected");
                    if !fix {
                        println!("💡 Run with --fix to automatically format code");
                    }
                    return Err(anyhow::anyhow!("Formatting check failed"));
                }
            }

            QualityAction::Lint { fix } => {
                println!("🛠️ DevOps Agent - Clippy Analysis");
                println!("==================================");

                let mut cmd = vec!["cargo", "clippy", "--all-targets", "--all-features"];
                if *fix {
                    cmd.push("--fix");
                    cmd.push("--allow-dirty");
                }
                cmd.extend(&["--", "-D", "warnings"]);

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run cargo clippy")?;

                if output.status.success() {
                    println!("✅ DevOps Agent: Clippy analysis passed");
                } else {
                    println!("❌ DevOps Agent: Clippy found issues");
                    println!("{}", String::from_utf8_lossy(&output.stdout));
                    return Err(anyhow::anyhow!("Clippy check failed"));
                }
            }

            QualityAction::Test {
                pattern,
                unit,
                integration,
                security,
            } => {
                println!("🧪 QA Agent - Test Execution");
                println!("============================");

                let mut cmd = vec!["cargo", "test"];

                if *unit {
                    cmd.push("--lib");
                } else if *integration {
                    cmd.extend(&["--test", "*integration*"]);
                } else if *security {
                    cmd.push("security::owasp_checker::tests");
                }

                if let Some(p) = pattern {
                    cmd.push(p);
                }

                cmd.extend(&["--verbose", "--no-fail-fast"]);

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run tests")?;

                if output.status.success() {
                    println!("✅ QA Agent: All tests passed");
                } else {
                    println!("❌ QA Agent: Some tests failed");
                    println!("{}", String::from_utf8_lossy(&output.stdout));
                    return Err(anyhow::anyhow!("Tests failed"));
                }
            }

            QualityAction::Build {
                release,
                all_targets,
            } => {
                println!("🛠️ DevOps Agent - Build Verification");
                println!("=====================================");

                let mut cmd = vec!["cargo", "build", "--verbose"];

                if *release {
                    cmd.push("--release");
                }
                if *all_targets {
                    cmd.push("--all-targets");
                }

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to build")?;

                if output.status.success() {
                    println!("✅ DevOps Agent: Build completed successfully");
                } else {
                    println!("❌ DevOps Agent: Build failed");
                    println!("{}", String::from_utf8_lossy(&output.stderr));
                    return Err(anyhow::anyhow!("Build failed"));
                }
            }

            QualityAction::Security { audit, deps } => {
                println!("🦀 Backend Agent - Security Analysis");
                println!("====================================");

                if *audit {
                    let output = Command::new("cargo").args(&["audit"]).output();

                    match output {
                        Ok(out) if out.status.success() => {
                            println!("✅ Backend Agent: Security audit passed");
                        }
                        _ => {
                            println!("❌ Backend Agent: Security audit found issues (or cargo-audit not installed)");
                        }
                    }
                }

                if *deps {
                    println!("🔍 Backend Agent: Checking dependencies...");
                    // Run dependency checks
                }

                // Always run security tests
                let output = Command::new("cargo")
                    .args(&["test", "security::owasp_checker::tests", "--no-fail-fast"])
                    .output()
                    .context("Failed to run security tests")?;

                if output.status.success() {
                    println!("✅ Backend Agent: Security tests passed");
                } else {
                    println!("❌ Backend Agent: Security tests failed");
                    return Err(anyhow::anyhow!("Security tests failed"));
                }
            }

            QualityAction::Status { detailed } => {
                println!("📊 Quality Gate Status");
                println!("======================");

                // Run quick checks to show status
                let checks = [
                    ("Format", vec!["cargo", "fmt", "--check"]),
                    ("Clippy", vec!["cargo", "clippy", "--", "-D", "warnings"]),
                    ("Tests", vec!["cargo", "test", "--lib", "--no-run"]),
                    ("Build", vec!["cargo", "check"]),
                ];

                for (name, cmd) in &checks {
                    match Command::new(&cmd[0]).args(&cmd[1..]).output() {
                        Ok(output) => {
                            let status = if output.status.success() {
                                "✅ PASS"
                            } else {
                                "❌ FAIL"
                            };
                            println!("  {}: {}", name, status);

                            if *detailed && !output.status.success() {
                                println!("    Error: {}", String::from_utf8_lossy(&output.stderr));
                            }
                        }
                        Err(_) => {
                            println!("  {}: ❌ FAIL (command error)", name);
                        }
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:4173-4336 method handle_search <-> ./crates/ccswarm/src/cli/mod.rs:4685-5006 method handle_quality
  Similarity: 98.73%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_search (lines 4173-4336) ---[0m
    async fn handle_search(&self, action: &SearchAction) -> Result<()> {
        use crate::extension_stub::agent_extension::{
            DocumentationSearchStrategy, GitHubSearchStrategy, SearchContext, SearchFilters,
            SearchQuery, SearchStrategy, StackOverflowSearchStrategy,
        };

        match action {
            SearchAction::Docs {
                query,
                source,
                limit,
            } => {
                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: Some("rust".to_string()),
                        framework: Some("general".to_string()),
                        date_range: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec![source.clone()],
                        relevance_threshold: 0.5,
                    }),
                };

                let mut all_results = Vec::new();

                if source == "all" || source == "mdn" {
                    let mdn_strategy = DocumentationSearchStrategy::new();
                    match mdn_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("MDN search failed: {}", e),
                    }
                }

                if source == "all" || source == "github" {
                    let github_strategy = GitHubSearchStrategy::new();
                    match github_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("GitHub search failed: {}", e),
                    }
                }

                if source == "all" || source == "stackoverflow" {
                    let stackoverflow_strategy = StackOverflowSearchStrategy;
                    match stackoverflow_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("StackOverflow search failed: {}", e),
                    }
                }

                all_results.sort_by(|a, b| {
                    b.relevance_score
                        .partial_cmp(&a.relevance_score)
                        .unwrap_or(std::cmp::Ordering::Equal)
                });
                all_results.truncate(*limit);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "query": query,
                            "source": source,
                            "results": all_results.iter().map(|r| serde_json::json!({
                                "title": r.title,
                                "url": r.url,
                                "snippet": r.snippet,
                                "source": r.source,
                                "relevance_score": r.relevance_score,
                            })).collect::<Vec<_>>()
                        }))?
                    );
                } else {
                    println!("🔍 Searching documentation for: {}", query);
                    println!("   Source: {}", source);
                    println!("   Found {} results", all_results.len());
                    println!();

                    for (i, result) in all_results.iter().enumerate() {
                        println!("{}. {} ({})", i + 1, result.title, result.source);
                        println!("   🔗 {}", result.url);
                        println!("   📝 {}", result.snippet);
                        println!("   ⭐ Relevance: {:.2}", result.relevance_score);
                        println!();
                    }
                }
            }

            SearchAction::Test { query } => {
                println!("🧪 Testing search functionality with query: {}", query);

                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: None,
                        framework: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec!["all".to_string()],
                        relevance_threshold: 0.5,
                        date_range: None,
                    }),
                };

                // Test each search strategy
                println!("🔍 Testing MDN search...");
                let mdn_strategy = DocumentationSearchStrategy::new();
                match mdn_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ MDN: Found {} results", results.len()),
                    Err(e) => println!("❌ MDN: Error - {}", e),
                }

                println!("🔍 Testing GitHub search...");
                let github_strategy = GitHubSearchStrategy::new();
                match github_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ GitHub: Found {} results", results.len()),
                    Err(e) => println!("❌ GitHub: Error - {}", e),
                }

                println!("🔍 Testing StackOverflow search...");
                let stackoverflow_strategy = StackOverflowSearchStrategy;
                match stackoverflow_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ StackOverflow: Found {} results", results.len()),
                    Err(e) => println!("❌ StackOverflow: Error - {}", e),
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Search test completed",
                            "query": query,
                        }))?
                    );
                } else {
                    println!("✅ Search functionality test completed");
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_quality (lines 4685-5006) ---[0m
    async fn handle_quality(&self, action: &QualityAction) -> Result<()> {
        use std::process::Command;

        let mut failed_checks = Vec::new();
        let mut completed_tasks = 0;
        let total_tasks;

        match action {
            QualityAction::Check {
                skip,
                only,
                fail_fast,
            } => {
                println!("🤖 ccswarm Agent-Managed Quality Checks");
                println!("=======================================");
                println!();

                let checks = if only.is_empty() {
                    vec!["format", "lint", "test", "build", "security"]
                } else {
                    only.iter().map(|s| s.as_str()).collect()
                };

                let filtered_checks: Vec<&str> = checks
                    .into_iter()
                    .filter(|check| !skip.contains(&check.to_string()))
                    .collect();

                total_tasks = filtered_checks.len();
                println!("🎯 Master Claude: Orchestrating {} quality checks through specialized agents...", total_tasks);
                println!();

                for check in filtered_checks {
                    let (agent, task, cmd) = match check {
                        "format" => (
                            "DevOps",
                            "Code Formatting Check",
                            vec!["cargo", "fmt", "--check"],
                        ),
                        "lint" => (
                            "DevOps",
                            "Clippy Code Quality Analysis",
                            vec![
                                "cargo",
                                "clippy",
                                "--all-targets",
                                "--all-features",
                                "--",
                                "-D",
                                "warnings",
                            ],
                        ),
                        "test" => (
                            "QA",
                            "Test Suite Execution",
                            vec!["cargo", "test", "--lib", "--verbose", "--no-fail-fast"],
                        ),
                        "build" => (
                            "DevOps",
                            "Build Verification",
                            vec!["cargo", "build", "--verbose"],
                        ),
                        "security" => (
                            "Backend",
                            "Security Analysis",
                            vec![
                                "cargo",
                                "test",
                                "security::owasp_checker::tests",
                                "--no-fail-fast",
                            ],
                        ),
                        _ => continue,
                    };

                    println!("🎯 Delegating to {} agent: {}", agent, task);

                    let mut command = Command::new(&cmd[0]);
                    for arg in &cmd[1..] {
                        command.arg(arg);
                    }

                    let output = command.output().context("Failed to execute command")?;
                    let success = output.status.success();

                    if success {
                        println!("✅ {} agent: {} completed successfully", agent, task);
                        completed_tasks += 1;
                    } else {
                        println!("❌ {} agent: {} failed", agent, task);
                        failed_checks.push((
                            agent,
                            task,
                            String::from_utf8_lossy(&output.stderr).to_string(),
                        ));

                        if *fail_fast {
                            break;
                        }
                    }
                    println!();
                }

                // Quality Gate Assessment
                println!("🎯 Master Claude - Quality Gate Assessment");
                println!("==========================================");
                println!("📊 Agent Task Completion Summary:");
                println!("   Completed: {}/{} tasks", completed_tasks, total_tasks);
                println!("   Failed: {}", failed_checks.len());
                println!();

                if failed_checks.is_empty() {
                    println!("✅ QUALITY GATE: PASSED");
                    println!("🎉 All quality checks passed through agent delegation");
                    println!("🚀 Code is ready for deployment");
                } else {
                    println!("❌ QUALITY GATE: FAILED");
                    println!("🔧 Some quality checks require attention from agents");
                    println!();
                    println!("📋 Failed Checks:");
                    for (agent, task, _error) in &failed_checks {
                        println!("   ❌ {} agent: {}", agent, task);
                    }
                    return Err(anyhow::anyhow!("Quality gate failed"));
                }
            }

            QualityAction::Format { fix } => {
                println!("🛠️ DevOps Agent - Code Formatting");
                println!("==================================");

                let cmd = if *fix {
                    vec!["cargo", "fmt"]
                } else {
                    vec!["cargo", "fmt", "--check"]
                };

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run cargo fmt")?;

                if output.status.success() {
                    println!(
                        "✅ DevOps Agent: Code formatting {} successfully",
                        if *fix { "applied" } else { "verified" }
                    );
                } else {
                    println!("❌ DevOps Agent: Code formatting issues detected");
                    if !fix {
                        println!("💡 Run with --fix to automatically format code");
                    }
                    return Err(anyhow::anyhow!("Formatting check failed"));
                }
            }

            QualityAction::Lint { fix } => {
                println!("🛠️ DevOps Agent - Clippy Analysis");
                println!("==================================");

                let mut cmd = vec!["cargo", "clippy", "--all-targets", "--all-features"];
                if *fix {
                    cmd.push("--fix");
                    cmd.push("--allow-dirty");
                }
                cmd.extend(&["--", "-D", "warnings"]);

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run cargo clippy")?;

                if output.status.success() {
                    println!("✅ DevOps Agent: Clippy analysis passed");
                } else {
                    println!("❌ DevOps Agent: Clippy found issues");
                    println!("{}", String::from_utf8_lossy(&output.stdout));
                    return Err(anyhow::anyhow!("Clippy check failed"));
                }
            }

            QualityAction::Test {
                pattern,
                unit,
                integration,
                security,
            } => {
                println!("🧪 QA Agent - Test Execution");
                println!("============================");

                let mut cmd = vec!["cargo", "test"];

                if *unit {
                    cmd.push("--lib");
                } else if *integration {
                    cmd.extend(&["--test", "*integration*"]);
                } else if *security {
                    cmd.push("security::owasp_checker::tests");
                }

                if let Some(p) = pattern {
                    cmd.push(p);
                }

                cmd.extend(&["--verbose", "--no-fail-fast"]);

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run tests")?;

                if output.status.success() {
                    println!("✅ QA Agent: All tests passed");
                } else {
                    println!("❌ QA Agent: Some tests failed");
                    println!("{}", String::from_utf8_lossy(&output.stdout));
                    return Err(anyhow::anyhow!("Tests failed"));
                }
            }

            QualityAction::Build {
                release,
                all_targets,
            } => {
                println!("🛠️ DevOps Agent - Build Verification");
                println!("=====================================");

                let mut cmd = vec!["cargo", "build", "--verbose"];

                if *release {
                    cmd.push("--release");
                }
                if *all_targets {
                    cmd.push("--all-targets");
                }

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to build")?;

                if output.status.success() {
                    println!("✅ DevOps Agent: Build completed successfully");
                } else {
                    println!("❌ DevOps Agent: Build failed");
                    println!("{}", String::from_utf8_lossy(&output.stderr));
                    return Err(anyhow::anyhow!("Build failed"));
                }
            }

            QualityAction::Security { audit, deps } => {
                println!("🦀 Backend Agent - Security Analysis");
                println!("====================================");

                if *audit {
                    let output = Command::new("cargo").args(&["audit"]).output();

                    match output {
                        Ok(out) if out.status.success() => {
                            println!("✅ Backend Agent: Security audit passed");
                        }
                        _ => {
                            println!("❌ Backend Agent: Security audit found issues (or cargo-audit not installed)");
                        }
                    }
                }

                if *deps {
                    println!("🔍 Backend Agent: Checking dependencies...");
                    // Run dependency checks
                }

                // Always run security tests
                let output = Command::new("cargo")
                    .args(&["test", "security::owasp_checker::tests", "--no-fail-fast"])
                    .output()
                    .context("Failed to run security tests")?;

                if output.status.success() {
                    println!("✅ Backend Agent: Security tests passed");
                } else {
                    println!("❌ Backend Agent: Security tests failed");
                    return Err(anyhow::anyhow!("Security tests failed"));
                }
            }

            QualityAction::Status { detailed } => {
                println!("📊 Quality Gate Status");
                println!("======================");

                // Run quick checks to show status
                let checks = [
                    ("Format", vec!["cargo", "fmt", "--check"]),
                    ("Clippy", vec!["cargo", "clippy", "--", "-D", "warnings"]),
                    ("Tests", vec!["cargo", "test", "--lib", "--no-run"]),
                    ("Build", vec!["cargo", "check"]),
                ];

                for (name, cmd) in &checks {
                    match Command::new(&cmd[0]).args(&cmd[1..]).output() {
                        Ok(output) => {
                            let status = if output.status.success() {
                                "✅ PASS"
                            } else {
                                "❌ FAIL"
                            };
                            println!("  {}: {}", name, status);

                            if *detailed && !output.status.success() {
                                println!("    Error: {}", String::from_utf8_lossy(&output.stderr));
                            }
                        }
                        Err(_) => {
                            println!("  {}: ❌ FAIL (command error)", name);
                        }
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:4685-5006 method handle_quality <-> ./crates/ccswarm/src/cli/mod.rs:5584-5809 method handle_doctor
  Similarity: 85.73%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_quality (lines 4685-5006) ---[0m
    async fn handle_quality(&self, action: &QualityAction) -> Result<()> {
        use std::process::Command;

        let mut failed_checks = Vec::new();
        let mut completed_tasks = 0;
        let total_tasks;

        match action {
            QualityAction::Check {
                skip,
                only,
                fail_fast,
            } => {
                println!("🤖 ccswarm Agent-Managed Quality Checks");
                println!("=======================================");
                println!();

                let checks = if only.is_empty() {
                    vec!["format", "lint", "test", "build", "security"]
                } else {
                    only.iter().map(|s| s.as_str()).collect()
                };

                let filtered_checks: Vec<&str> = checks
                    .into_iter()
                    .filter(|check| !skip.contains(&check.to_string()))
                    .collect();

                total_tasks = filtered_checks.len();
                println!("🎯 Master Claude: Orchestrating {} quality checks through specialized agents...", total_tasks);
                println!();

                for check in filtered_checks {
                    let (agent, task, cmd) = match check {
                        "format" => (
                            "DevOps",
                            "Code Formatting Check",
                            vec!["cargo", "fmt", "--check"],
                        ),
                        "lint" => (
                            "DevOps",
                            "Clippy Code Quality Analysis",
                            vec![
                                "cargo",
                                "clippy",
                                "--all-targets",
                                "--all-features",
                                "--",
                                "-D",
                                "warnings",
                            ],
                        ),
                        "test" => (
                            "QA",
                            "Test Suite Execution",
                            vec!["cargo", "test", "--lib", "--verbose", "--no-fail-fast"],
                        ),
                        "build" => (
                            "DevOps",
                            "Build Verification",
                            vec!["cargo", "build", "--verbose"],
                        ),
                        "security" => (
                            "Backend",
                            "Security Analysis",
                            vec![
                                "cargo",
                                "test",
                                "security::owasp_checker::tests",
                                "--no-fail-fast",
                            ],
                        ),
                        _ => continue,
                    };

                    println!("🎯 Delegating to {} agent: {}", agent, task);

                    let mut command = Command::new(&cmd[0]);
                    for arg in &cmd[1..] {
                        command.arg(arg);
                    }

                    let output = command.output().context("Failed to execute command")?;
                    let success = output.status.success();

                    if success {
                        println!("✅ {} agent: {} completed successfully", agent, task);
                        completed_tasks += 1;
                    } else {
                        println!("❌ {} agent: {} failed", agent, task);
                        failed_checks.push((
                            agent,
                            task,
                            String::from_utf8_lossy(&output.stderr).to_string(),
                        ));

                        if *fail_fast {
                            break;
                        }
                    }
                    println!();
                }

                // Quality Gate Assessment
                println!("🎯 Master Claude - Quality Gate Assessment");
                println!("==========================================");
                println!("📊 Agent Task Completion Summary:");
                println!("   Completed: {}/{} tasks", completed_tasks, total_tasks);
                println!("   Failed: {}", failed_checks.len());
                println!();

                if failed_checks.is_empty() {
                    println!("✅ QUALITY GATE: PASSED");
                    println!("🎉 All quality checks passed through agent delegation");
                    println!("🚀 Code is ready for deployment");
                } else {
                    println!("❌ QUALITY GATE: FAILED");
                    println!("🔧 Some quality checks require attention from agents");
                    println!();
                    println!("📋 Failed Checks:");
                    for (agent, task, _error) in &failed_checks {
                        println!("   ❌ {} agent: {}", agent, task);
                    }
                    return Err(anyhow::anyhow!("Quality gate failed"));
                }
            }

            QualityAction::Format { fix } => {
                println!("🛠️ DevOps Agent - Code Formatting");
                println!("==================================");

                let cmd = if *fix {
                    vec!["cargo", "fmt"]
                } else {
                    vec!["cargo", "fmt", "--check"]
                };

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run cargo fmt")?;

                if output.status.success() {
                    println!(
                        "✅ DevOps Agent: Code formatting {} successfully",
                        if *fix { "applied" } else { "verified" }
                    );
                } else {
                    println!("❌ DevOps Agent: Code formatting issues detected");
                    if !fix {
                        println!("💡 Run with --fix to automatically format code");
                    }
                    return Err(anyhow::anyhow!("Formatting check failed"));
                }
            }

            QualityAction::Lint { fix } => {
                println!("🛠️ DevOps Agent - Clippy Analysis");
                println!("==================================");

                let mut cmd = vec!["cargo", "clippy", "--all-targets", "--all-features"];
                if *fix {
                    cmd.push("--fix");
                    cmd.push("--allow-dirty");
                }
                cmd.extend(&["--", "-D", "warnings"]);

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run cargo clippy")?;

                if output.status.success() {
                    println!("✅ DevOps Agent: Clippy analysis passed");
                } else {
                    println!("❌ DevOps Agent: Clippy found issues");
                    println!("{}", String::from_utf8_lossy(&output.stdout));
                    return Err(anyhow::anyhow!("Clippy check failed"));
                }
            }

            QualityAction::Test {
                pattern,
                unit,
                integration,
                security,
            } => {
                println!("🧪 QA Agent - Test Execution");
                println!("============================");

                let mut cmd = vec!["cargo", "test"];

                if *unit {
                    cmd.push("--lib");
                } else if *integration {
                    cmd.extend(&["--test", "*integration*"]);
                } else if *security {
                    cmd.push("security::owasp_checker::tests");
                }

                if let Some(p) = pattern {
                    cmd.push(p);
                }

                cmd.extend(&["--verbose", "--no-fail-fast"]);

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to run tests")?;

                if output.status.success() {
                    println!("✅ QA Agent: All tests passed");
                } else {
                    println!("❌ QA Agent: Some tests failed");
                    println!("{}", String::from_utf8_lossy(&output.stdout));
                    return Err(anyhow::anyhow!("Tests failed"));
                }
            }

            QualityAction::Build {
                release,
                all_targets,
            } => {
                println!("🛠️ DevOps Agent - Build Verification");
                println!("=====================================");

                let mut cmd = vec!["cargo", "build", "--verbose"];

                if *release {
                    cmd.push("--release");
                }
                if *all_targets {
                    cmd.push("--all-targets");
                }

                let output = Command::new(&cmd[0])
                    .args(&cmd[1..])
                    .output()
                    .context("Failed to build")?;

                if output.status.success() {
                    println!("✅ DevOps Agent: Build completed successfully");
                } else {
                    println!("❌ DevOps Agent: Build failed");
                    println!("{}", String::from_utf8_lossy(&output.stderr));
                    return Err(anyhow::anyhow!("Build failed"));
                }
            }

            QualityAction::Security { audit, deps } => {
                println!("🦀 Backend Agent - Security Analysis");
                println!("====================================");

                if *audit {
                    let output = Command::new("cargo").args(&["audit"]).output();

                    match output {
                        Ok(out) if out.status.success() => {
                            println!("✅ Backend Agent: Security audit passed");
                        }
                        _ => {
                            println!("❌ Backend Agent: Security audit found issues (or cargo-audit not installed)");
                        }
                    }
                }

                if *deps {
                    println!("🔍 Backend Agent: Checking dependencies...");
                    // Run dependency checks
                }

                // Always run security tests
                let output = Command::new("cargo")
                    .args(&["test", "security::owasp_checker::tests", "--no-fail-fast"])
                    .output()
                    .context("Failed to run security tests")?;

                if output.status.success() {
                    println!("✅ Backend Agent: Security tests passed");
                } else {
                    println!("❌ Backend Agent: Security tests failed");
                    return Err(anyhow::anyhow!("Security tests failed"));
                }
            }

            QualityAction::Status { detailed } => {
                println!("📊 Quality Gate Status");
                println!("======================");

                // Run quick checks to show status
                let checks = [
                    ("Format", vec!["cargo", "fmt", "--check"]),
                    ("Clippy", vec!["cargo", "clippy", "--", "-D", "warnings"]),
                    ("Tests", vec!["cargo", "test", "--lib", "--no-run"]),
                    ("Build", vec!["cargo", "check"]),
                ];

                for (name, cmd) in &checks {
                    match Command::new(&cmd[0]).args(&cmd[1..]).output() {
                        Ok(output) => {
                            let status = if output.status.success() {
                                "✅ PASS"
                            } else {
                                "❌ FAIL"
                            };
                            println!("  {}: {}", name, status);

                            if *detailed && !output.status.success() {
                                println!("    Error: {}", String::from_utf8_lossy(&output.stderr));
                            }
                        }
                        Err(_) => {
                            println!("  {}: ❌ FAIL (command error)", name);
                        }
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_doctor (lines 5584-5809) ---[0m
    async fn handle_doctor(
        &self,
        fix: bool,
        error_code: Option<&str>,
        check_api: bool,
    ) -> Result<()> {
        use crate::utils::error_recovery::ErrorRecoveryDB;
        use crate::utils::user_error::CommonErrors;

        // Handle specific error code diagnosis
        if let Some(code) = error_code {
            println!("{}", "🔍 Error Code Diagnosis".bright_cyan().bold());
            println!("{}", "=======================".bright_cyan());
            println!();
            println!("Analyzing error code: {}", code.bright_yellow());
            println!();

            let recovery_db = ErrorRecoveryDB::new();
            if let Some(recovery) = recovery_db.get_recovery(code) {
                match &recovery {
                    crate::utils::error_recovery::RecoveryStep::UserAction { description } => {
                        println!("📋 {}", description.bright_white());
                    },
                    crate::utils::error_recovery::RecoveryStep::Command { description, .. } => {
                        println!("📋 {}", description.bright_white());
                    },
                    crate::utils::error_recovery::RecoveryStep::FileCreate { path, .. } => {
                        println!("📋 Create file: {}", path.bright_white());
                    },
                    crate::utils::error_recovery::RecoveryStep::EnvVar { name, .. } => {
                        println!("📋 Set environment variable: {}", name.bright_white());
                    },
                }
                println!();
                println!("Recovery steps:");
                let steps = [recovery.clone()]; // Treat the single recovery step as the step list
                for (i, step) in steps.iter().enumerate() {
                    match step {
                        crate::utils::error_recovery::RecoveryStep::Command {
                            cmd,
                            description,
                        } => {
                            println!("  {}. {} {}", i + 1, "Run:".bright_yellow(), description);
                            println!("     {}", cmd.bright_white().on_black());
                        }
                        crate::utils::error_recovery::RecoveryStep::FileCreate { path, .. } => {
                            println!(
                                "  {}. {} {}",
                                i + 1,
                                "Create file:".bright_yellow(),
                                path.bright_white()
                            );
                        }
                        crate::utils::error_recovery::RecoveryStep::EnvVar { name, example } => {
                            println!(
                                "  {}. {} {}",
                                i + 1,
                                "Set environment variable:".bright_yellow(),
                                name.bright_white()
                            );
                            println!("     Example: {}={}", name, example.bright_black());
                        }
                        crate::utils::error_recovery::RecoveryStep::UserAction { description } => {
                            println!(
                                "  {}. {} {}",
                                i + 1,
                                "Action required:".bright_yellow(),
                                description
                            );
                        }
                    }
                    println!();
                }

                let can_auto_fix = matches!(recovery, crate::utils::error_recovery::RecoveryStep::Command { .. });
                if can_auto_fix && fix {
                    recovery_db.auto_fix(code).await?;
                } else if can_auto_fix {
                    println!(
                        "💡 This error can be auto-fixed! Run: ccswarm doctor --error {} --fix",
                        code
                    );
                }
            } else {
                println!("❌ Unknown error code: {}", code);
                println!("   See all error codes: ccswarm help errors");
            }
            return Ok(());
        }

        // Handle API connectivity check
        if check_api {
            println!("{}", "🌐 API Connectivity Check".bright_cyan().bold());
            println!("{}", "=========================".bright_cyan());
            println!();

            print!("Testing Anthropic API... ");
            match reqwest::get("https://api.anthropic.com/v1/health").await {
                Ok(resp) if resp.status().is_success() => {
                    println!("{}", "✅ Connected".bright_green());
                }
                Ok(resp) => {
                    println!(
                        "{}",
                        format!("⚠️  Status: {}", resp.status()).bright_yellow()
                    );
                }
                Err(e) => {
                    println!("{}", "❌ Failed".bright_red());
                    println!("   {}", e.to_string().bright_black());
                }
            }
            println!();
            return Ok(());
        }

        println!("{}", "🏥 ccswarm System Diagnosis".bright_cyan().bold());
        println!("{}", "===========================".bright_cyan());
        println!();

        let mut issues = Vec::new();

        // Check Git
        print!("Checking Git... ");
        match std::process::Command::new("git").arg("--version").output() {
            Ok(output) if output.status.success() => {
                println!("{}", "✅ OK".bright_green());
            }
            _ => {
                println!("{}", "❌ Not found".bright_red());
                issues.push("git");
            }
        }

        // Check API keys
        print!("Checking API keys... ");
        if std::env::var("ANTHROPIC_API_KEY").is_ok() {
            println!("{}", "✅ Set".bright_green());
        } else {
            println!("{}", "⚠️  Not set".bright_yellow());
            issues.push("api_key");
        }

        // Check config
        print!("Checking configuration... ");
        let config_path = self.repo_path.join("ccswarm.json");
        if config_path.exists() {
            match CcswarmConfig::from_file(config_path.clone()).await {
                Ok(_) => println!("{}", "✅ Valid".bright_green()),
                Err(e) => {
                    println!("{}", "❌ Invalid".bright_red());
                    println!("   {}", e.to_string().bright_black());
                    issues.push("config");
                }
            }
        } else {
            println!("{}", "❌ Not found".bright_red());
            issues.push("config");
        }

        // Check git repo
        print!("Checking git repository... ");
        let git_dir = self.repo_path.join(".git");
        if git_dir.exists() {
            println!("{}", "✅ Initialized".bright_green());
        } else {
            println!("{}", "⚠️  Not initialized".bright_yellow());
            issues.push("git_repo");
        }

        // Check disk space
        print!("Checking disk space... ");
        // Simple check - in real implementation would use proper system calls
        println!("{}", "✅ Sufficient".bright_green());

        println!();

        if issues.is_empty() {
            println!("{}", "✅ All systems operational!".bright_green().bold());
        } else {
            println!(
                "{}",
                format!("⚠️  Found {} issues", issues.len())
                    .bright_yellow()
                    .bold()
            );

            if fix {
                println!();
                println!("{}", "🔧 Attempting fixes...".bright_cyan());

                for issue in &issues {
                    match *issue {
                        "git" => {
                            println!("• Git: Please install git from https://git-scm.com");
                        }
                        "api_key" => {
                            CommonErrors::api_key_missing("Anthropic").display();
                        }
                        "config" => {
                            println!("• Config: Run 'ccswarm setup' to create configuration");
                        }
                        "git_repo" => {
                            if fix {
                                println!("• Initializing git repository...");
                                crate::git::shell::ShellWorktreeManager::init_if_needed(
                                    &self.repo_path,
                                )
                                .await?;
                                println!("  ✅ Git repository initialized");
                            }
                        }
                        _ => {}
                    }
                }
            } else {
                println!();
                println!(
                    "{}",
                    "💡 Run with --fix to attempt automatic fixes".bright_black()
                );
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2279-2458 method handle_worktree <-> ./crates/ccswarm/src/cli/mod.rs:4339-4579 method handle_claude_acp
  Similarity: 98.34%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_worktree (lines 2279-2458) ---[0m
    async fn handle_worktree(&self, action: &WorktreeAction) -> Result<()> {
        let manager = crate::git::shell::ShellWorktreeManager::new(self.repo_path.clone())?;

        match action {
            WorktreeAction::List => {
                let worktrees = manager.list_worktrees().await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&worktrees)?);
                } else {
                    println!("🌳 Git Worktrees");
                    println!("===============");

                    for wt in &worktrees {
                        println!("Path: {}", wt.path.display());
                        println!("  Branch: {}", wt.branch);
                        println!("  Head: {}", wt.head_commit);
                        println!("  Locked: {}", wt.is_locked);
                        println!();
                    }
                }
            }
            WorktreeAction::Create {
                path,
                branch,
                new_branch,
            } => {
                let info = if *new_branch {
                    manager.create_worktree_full(path, branch, true).await?
                } else {
                    manager.create_worktree(path, branch).await?
                };

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&info)?);
                } else {
                    println!("✅ Worktree created");
                    println!("   Path: {}", info.path.display());
                    println!("   Branch: {}", info.branch);
                }
            }
            WorktreeAction::Remove { path, force } => {
                if *force {
                    manager.remove_worktree_full(path, true).await?
                } else {
                    manager.remove_worktree(path).await?
                };

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktree removed",
                            "path": path,
                        }))?
                    );
                } else {
                    println!("✅ Worktree removed: {}", path.display());
                }
            }
            WorktreeAction::Prune => {
                manager.prune_worktrees().await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktrees pruned",
                        }))?
                    );
                } else {
                    println!("✅ Stale worktrees pruned");
                }
            }
            WorktreeAction::Clean { force } => {
                use std::io::{self, Write};

                // Find all ccswarm-related worktrees
                let worktrees = manager.list_worktrees().await?;
                let ccswarm_worktrees: Vec<_> = worktrees
                    .iter()
                    .filter(|w| w.branch.contains("agent") || w.branch.contains("feature/"))
                    .collect();

                if ccswarm_worktrees.is_empty() {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "No ccswarm worktrees found",
                            }))?
                        );
                    } else {
                        println!("✅ No ccswarm worktrees to clean");
                    }
                    return Ok(());
                }

                // Ask for confirmation unless forced
                if !force {
                    println!("⚠️  Found {} ccswarm worktrees:", ccswarm_worktrees.len());
                    for w in &ccswarm_worktrees {
                        println!("   - {} ({})", w.path.display(), w.branch);
                    }
                    print!("\nAre you sure you want to remove all these worktrees? [y/N] ");
                    io::stdout().flush()?;

                    let mut response = String::new();
                    io::stdin().read_line(&mut response)?;

                    if !response.trim().eq_ignore_ascii_case("y") {
                        println!("❌ Cleanup cancelled");
                        return Ok(());
                    }
                }

                // Remove all ccswarm worktrees
                let mut removed_count = 0;
                for worktree in ccswarm_worktrees {
                    match manager.remove_worktree(&worktree.path).await {
                        Ok(_) => {
                            removed_count += 1;
                            if !self.json_output {
                                println!("   ✓ Removed {}", worktree.path.display());
                            }
                        }
                        Err(e) => {
                            if !self.json_output {
                                println!(
                                    "   ✗ Failed to remove {}: {}",
                                    worktree.path.display(),
                                    e
                                );
                            }
                        }
                    }
                }

                // Also clean up branches
                let output = tokio::process::Command::new("git")
                    .args(["branch", "--list", "*agent*", "feature/*"])
                    .output()
                    .await?;

                if output.status.success() {
                    let branches = String::from_utf8_lossy(&output.stdout);
                    let branch_count = branches.lines().count();

                    if branch_count > 0 {
                        tokio::process::Command::new("git")
                            .args(&["branch", "-D"])
                            .args(branches.lines().map(|b| b.trim().trim_start_matches("* ")))
                            .output()
                            .await?;
                    }
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Cleanup completed",
                            "worktrees_removed": removed_count,
                        }))?
                    );
                } else {
                    println!(
                        "\n✅ Cleanup completed: {} worktrees removed",
                        removed_count
                    );
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_claude_acp (lines 4339-4579) ---[0m
    async fn handle_claude_acp(&self, command: &ClaudeACPCommands) -> Result<()> {
        use crate::acp_claude::{ClaudeACPConfig, SimplifiedClaudeAdapter};

        match command {
            ClaudeACPCommands::Start { url } => {
                let mut config = ClaudeACPConfig::default();
                config.url = url.clone();

                let mut adapter = SimplifiedClaudeAdapter::new(config);

                if self.json_output {
                    match adapter.connect_with_retry().await {
                        Ok(_) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "message": "Connected to Claude Code",
                                    "url": url,
                                    "session_id": adapter.session_id()
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Failed to connect: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("🚀 Starting Claude Code ACP adapter...");
                    println!("   URL: {}", url);

                    match adapter.connect_with_retry().await {
                        Ok(_) => {
                            println!("✅ Successfully connected to Claude Code!");
                            if let Some(session_id) = adapter.session_id() {
                                println!("   Session ID: {}", session_id);
                            }
                        }
                        Err(e) => {
                            println!("❌ Failed to connect: {}", e);
                            return Err(e.into());
                        }
                    }
                }
            }
            ClaudeACPCommands::Test => {
                let config = ClaudeACPConfig::default();
                let mut adapter = SimplifiedClaudeAdapter::new(config);

                if self.json_output {
                    match adapter.test_connection().await {
                        Ok(result) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "message": result
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Connection test failed: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("🧪 Testing connection to Claude Code...");
                    match adapter.test_connection().await {
                        Ok(result) => {
                            println!("✅ {}", result);
                        }
                        Err(e) => {
                            println!("❌ Connection test failed: {}", e);
                            return Err(e.into());
                        }
                    }
                }
            }
            ClaudeACPCommands::Send { task } => {
                let config = ClaudeACPConfig::default();
                let mut adapter = SimplifiedClaudeAdapter::new(config);

                // Connect first
                adapter.connect_with_retry().await?;

                if self.json_output {
                    match adapter.send_task(task).await {
                        Ok(response) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "response": response
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Failed to send task: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("📤 Sending task to Claude Code: {}", task);
                    match adapter.send_task(task).await {
                        Ok(response) => {
                            println!("📥 Response: {}", response);
                        }
                        Err(e) => {
                            println!("❌ Failed to send task: {}", e);
                            return Err(e.into());
                        }
                    }
                }

                // Disconnect after sending
                adapter.disconnect().await;
            }
            ClaudeACPCommands::Stop => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Claude ACP adapter stopped"
                        }))?
                    );
                } else {
                    println!("🛑 Stopping Claude Code ACP adapter...");
                    println!("✅ Claude ACP adapter stopped");
                }
            }
            ClaudeACPCommands::Status => {
                let config = ClaudeACPConfig::default();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "config": {
                                "url": config.url,
                                "auto_connect": config.auto_connect,
                                "timeout": config.timeout,
                                "max_retries": config.max_retries,
                                "prefer_claude": config.prefer_claude
                            }
                        }))?
                    );
                } else {
                    println!("📊 Claude ACP Status:");
                    println!("   URL: {}", config.url);
                    println!("   Auto-connect: {}", config.auto_connect);
                    println!("   Timeout: {}s", config.timeout);
                    println!("   Max retries: {}", config.max_retries);
                    println!("   Prefer Claude: {}", config.prefer_claude);
                }
            }
            ClaudeACPCommands::Diagnose => {
                let config = ClaudeACPConfig::from_env();
                let mut adapter = SimplifiedClaudeAdapter::new(config.clone());

                if self.json_output {
                    let mut diagnostics = serde_json::json!({
                        "environment": {
                            "url_env": std::env::var("CCSWARM_CLAUDE_ACP_URL").ok(),
                            "auto_connect_env": std::env::var("CCSWARM_CLAUDE_ACP_AUTO_CONNECT").ok(),
                            "timeout_env": std::env::var("CCSWARM_CLAUDE_ACP_TIMEOUT").ok(),
                        },
                        "config": config
                    });

                    // Test connection
                    if let Ok(_) = adapter.connect().await {
                        diagnostics["connection"] = serde_json::json!({
                            "status": "connected",
                            "session_id": adapter.session_id()
                        });
                        adapter.disconnect().await;
                    } else {
                        diagnostics["connection"] = serde_json::json!({
                            "status": "failed"
                        });
                    }

                    println!("{}", serde_json::to_string_pretty(&diagnostics)?);
                } else {
                    println!("🔍 Claude ACP Diagnostics:");
                    println!("\n📋 Environment Variables:");
                    if let Ok(url) = std::env::var("CCSWARM_CLAUDE_ACP_URL") {
                        println!("   CCSWARM_CLAUDE_ACP_URL: {}", url);
                    } else {
                        println!("   CCSWARM_CLAUDE_ACP_URL: <not set>");
                    }
                    if let Ok(auto) = std::env::var("CCSWARM_CLAUDE_ACP_AUTO_CONNECT") {
                        println!("   CCSWARM_CLAUDE_ACP_AUTO_CONNECT: {}", auto);
                    } else {
                        println!("   CCSWARM_CLAUDE_ACP_AUTO_CONNECT: <not set>");
                    }

                    println!("\n⚙️  Configuration:");
                    println!("   URL: {}", config.url);
                    println!("   Auto-connect: {}", config.auto_connect);
                    println!("   Timeout: {}s", config.timeout);
                    println!("   Max retries: {}", config.max_retries);

                    println!("\n🧪 Connection Test:");
                    print!("   Attempting to connect... ");
                    if let Ok(_) = adapter.connect().await {
                        println!("✅ Success!");
                        if let Some(session_id) = adapter.session_id() {
                            println!("   Session ID: {}", session_id);
                        }
                        adapter.disconnect().await;
                    } else {
                        println!("❌ Failed!");
                        println!("   Claude Code may not be running or accessible");
                    }
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:3628-3804 method handle_sangha <-> ./crates/ccswarm/src/cli/mod.rs:4339-4579 method handle_claude_acp
  Similarity: 98.33%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_sangha (lines 3628-3804) ---[0m
    async fn handle_sangha(&self, action: &SanghaAction) -> Result<()> {
        // Using stub implementation for now
        use crate::extension_stub::sangha::{Vote, VoteType};
        use std::str::FromStr;

        // Stub implementation - just log the action
        info!("🏛️  Sangha action requested (stub implementation)");

        match action {
            SanghaAction::Propose {
                proposal_type,
                file,
            } => {
                info!("📋 Submitting proposal to Sangha (stub implementation)");

                // Read proposal specification from file
                let _spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read proposal file")?;

                info!("Proposal type: {}", proposal_type);

                // Stub: Generate a fake proposal ID
                let proposal_id = uuid::Uuid::new_v4();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Proposal submitted",
                            "proposal_id": proposal_id,
                            "type": proposal_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!(
                        "📋 Submitting {} proposal from: {}",
                        proposal_type,
                        file.display()
                    );
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Proposal submitted successfully");
                }
            }

            SanghaAction::Vote {
                proposal_id,
                choice,
                reason,
            } => {
                info!("🗳️ Casting vote (stub implementation)");

                // Parse vote choice
                let vote_type = match choice.to_lowercase().as_str() {
                    "aye" | "yes" | "approve" => VoteType::Approve,
                    "nay" | "no" | "reject" => VoteType::Reject,
                    "abstain" => VoteType::Abstain,
                    "veto" | "needs_changes" => VoteType::NeedsChanges,
                    _ => {
                        anyhow::bail!(
                            "Invalid vote choice: {}. Use aye, nay, abstain, or veto",
                            choice
                        );
                    }
                };

                // Parse proposal ID
                let prop_id =
                    uuid::Uuid::from_str(proposal_id).context("Invalid proposal ID format")?;

                // Create vote (stub)
                let _vote = Vote::new("cli-user".to_string(), prop_id.to_string(), vote_type);

                info!("Vote created for proposal: {}", proposal_id);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Vote cast",
                            "proposal_id": proposal_id,
                            "choice": choice,
                            "reason": reason,
                        }))?
                    );
                } else {
                    println!("🗳️ Casting vote on proposal: {}", proposal_id);
                    println!("   Choice: {}", choice);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                    println!("✅ Vote cast successfully");
                }
            }

            SanghaAction::List { all, status } => {
                info!("📊 Listing Sangha status (stub implementation)");

                // Stub statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Sangha status retrieved (stub)",
                            "stats": {
                                "total_members": 1,
                                "active_members": 1,
                                "total_proposals": 0,
                                "active_proposals": 0,
                                "consensus_algorithm": "stub",
                            },
                            "show_all": all,
                            "filter_status": status,
                        }))?
                    );
                } else {
                    println!("📊 Sangha Status (Stub Implementation)");
                    println!("=====================================");
                    println!("👥 Members: 1 total, 1 active");
                    println!("📋 Proposals: 0 total, 0 active");
                    println!("🧠 Consensus Algorithm: stub");

                    println!(
                        "\n💡 No active proposals. Use 'ccswarm sangha propose' to create one."
                    );
                }
            }

            SanghaAction::Session { id, active } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session info",
                            "session_id": id,
                            "active": active,
                        }))?
                    );
                } else {
                    println!("🏛️ Sangha Session");
                    if *active {
                        println!("No active session");
                    }
                }
            }

            SanghaAction::ExtensionReview {
                proposal_id,
                technical_check,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension reviewed",
                            "proposal_id": proposal_id,
                            "technical_check": technical_check,
                        }))?
                    );
                } else {
                    println!("🔍 Reviewing extension proposal: {}", proposal_id);
                    if *technical_check {
                        println!("   Performing technical validation...");
                    }
                    println!("✅ Review completed");
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_claude_acp (lines 4339-4579) ---[0m
    async fn handle_claude_acp(&self, command: &ClaudeACPCommands) -> Result<()> {
        use crate::acp_claude::{ClaudeACPConfig, SimplifiedClaudeAdapter};

        match command {
            ClaudeACPCommands::Start { url } => {
                let mut config = ClaudeACPConfig::default();
                config.url = url.clone();

                let mut adapter = SimplifiedClaudeAdapter::new(config);

                if self.json_output {
                    match adapter.connect_with_retry().await {
                        Ok(_) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "message": "Connected to Claude Code",
                                    "url": url,
                                    "session_id": adapter.session_id()
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Failed to connect: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("🚀 Starting Claude Code ACP adapter...");
                    println!("   URL: {}", url);

                    match adapter.connect_with_retry().await {
                        Ok(_) => {
                            println!("✅ Successfully connected to Claude Code!");
                            if let Some(session_id) = adapter.session_id() {
                                println!("   Session ID: {}", session_id);
                            }
                        }
                        Err(e) => {
                            println!("❌ Failed to connect: {}", e);
                            return Err(e.into());
                        }
                    }
                }
            }
            ClaudeACPCommands::Test => {
                let config = ClaudeACPConfig::default();
                let mut adapter = SimplifiedClaudeAdapter::new(config);

                if self.json_output {
                    match adapter.test_connection().await {
                        Ok(result) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "message": result
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Connection test failed: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("🧪 Testing connection to Claude Code...");
                    match adapter.test_connection().await {
                        Ok(result) => {
                            println!("✅ {}", result);
                        }
                        Err(e) => {
                            println!("❌ Connection test failed: {}", e);
                            return Err(e.into());
                        }
                    }
                }
            }
            ClaudeACPCommands::Send { task } => {
                let config = ClaudeACPConfig::default();
                let mut adapter = SimplifiedClaudeAdapter::new(config);

                // Connect first
                adapter.connect_with_retry().await?;

                if self.json_output {
                    match adapter.send_task(task).await {
                        Ok(response) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "response": response
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Failed to send task: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("📤 Sending task to Claude Code: {}", task);
                    match adapter.send_task(task).await {
                        Ok(response) => {
                            println!("📥 Response: {}", response);
                        }
                        Err(e) => {
                            println!("❌ Failed to send task: {}", e);
                            return Err(e.into());
                        }
                    }
                }

                // Disconnect after sending
                adapter.disconnect().await;
            }
            ClaudeACPCommands::Stop => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Claude ACP adapter stopped"
                        }))?
                    );
                } else {
                    println!("🛑 Stopping Claude Code ACP adapter...");
                    println!("✅ Claude ACP adapter stopped");
                }
            }
            ClaudeACPCommands::Status => {
                let config = ClaudeACPConfig::default();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "config": {
                                "url": config.url,
                                "auto_connect": config.auto_connect,
                                "timeout": config.timeout,
                                "max_retries": config.max_retries,
                                "prefer_claude": config.prefer_claude
                            }
                        }))?
                    );
                } else {
                    println!("📊 Claude ACP Status:");
                    println!("   URL: {}", config.url);
                    println!("   Auto-connect: {}", config.auto_connect);
                    println!("   Timeout: {}s", config.timeout);
                    println!("   Max retries: {}", config.max_retries);
                    println!("   Prefer Claude: {}", config.prefer_claude);
                }
            }
            ClaudeACPCommands::Diagnose => {
                let config = ClaudeACPConfig::from_env();
                let mut adapter = SimplifiedClaudeAdapter::new(config.clone());

                if self.json_output {
                    let mut diagnostics = serde_json::json!({
                        "environment": {
                            "url_env": std::env::var("CCSWARM_CLAUDE_ACP_URL").ok(),
                            "auto_connect_env": std::env::var("CCSWARM_CLAUDE_ACP_AUTO_CONNECT").ok(),
                            "timeout_env": std::env::var("CCSWARM_CLAUDE_ACP_TIMEOUT").ok(),
                        },
                        "config": config
                    });

                    // Test connection
                    if let Ok(_) = adapter.connect().await {
                        diagnostics["connection"] = serde_json::json!({
                            "status": "connected",
                            "session_id": adapter.session_id()
                        });
                        adapter.disconnect().await;
                    } else {
                        diagnostics["connection"] = serde_json::json!({
                            "status": "failed"
                        });
                    }

                    println!("{}", serde_json::to_string_pretty(&diagnostics)?);
                } else {
                    println!("🔍 Claude ACP Diagnostics:");
                    println!("\n📋 Environment Variables:");
                    if let Ok(url) = std::env::var("CCSWARM_CLAUDE_ACP_URL") {
                        println!("   CCSWARM_CLAUDE_ACP_URL: {}", url);
                    } else {
                        println!("   CCSWARM_CLAUDE_ACP_URL: <not set>");
                    }
                    if let Ok(auto) = std::env::var("CCSWARM_CLAUDE_ACP_AUTO_CONNECT") {
                        println!("   CCSWARM_CLAUDE_ACP_AUTO_CONNECT: {}", auto);
                    } else {
                        println!("   CCSWARM_CLAUDE_ACP_AUTO_CONNECT: <not set>");
                    }

                    println!("\n⚙️  Configuration:");
                    println!("   URL: {}", config.url);
                    println!("   Auto-connect: {}", config.auto_connect);
                    println!("   Timeout: {}s", config.timeout);
                    println!("   Max retries: {}", config.max_retries);

                    println!("\n🧪 Connection Test:");
                    print!("   Attempting to connect... ");
                    if let Ok(_) = adapter.connect().await {
                        println!("✅ Success!");
                        if let Some(session_id) = adapter.session_id() {
                            println!("   Session ID: {}", session_id);
                        }
                        adapter.disconnect().await;
                    } else {
                        println!("❌ Failed!");
                        println!("   Claude Code may not be running or accessible");
                    }
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:4173-4336 method handle_search <-> ./crates/ccswarm/src/cli/mod.rs:4339-4579 method handle_claude_acp
  Similarity: 97.83%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_search (lines 4173-4336) ---[0m
    async fn handle_search(&self, action: &SearchAction) -> Result<()> {
        use crate::extension_stub::agent_extension::{
            DocumentationSearchStrategy, GitHubSearchStrategy, SearchContext, SearchFilters,
            SearchQuery, SearchStrategy, StackOverflowSearchStrategy,
        };

        match action {
            SearchAction::Docs {
                query,
                source,
                limit,
            } => {
                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: Some("rust".to_string()),
                        framework: Some("general".to_string()),
                        date_range: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec![source.clone()],
                        relevance_threshold: 0.5,
                    }),
                };

                let mut all_results = Vec::new();

                if source == "all" || source == "mdn" {
                    let mdn_strategy = DocumentationSearchStrategy::new();
                    match mdn_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("MDN search failed: {}", e),
                    }
                }

                if source == "all" || source == "github" {
                    let github_strategy = GitHubSearchStrategy::new();
                    match github_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("GitHub search failed: {}", e),
                    }
                }

                if source == "all" || source == "stackoverflow" {
                    let stackoverflow_strategy = StackOverflowSearchStrategy;
                    match stackoverflow_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("StackOverflow search failed: {}", e),
                    }
                }

                all_results.sort_by(|a, b| {
                    b.relevance_score
                        .partial_cmp(&a.relevance_score)
                        .unwrap_or(std::cmp::Ordering::Equal)
                });
                all_results.truncate(*limit);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "query": query,
                            "source": source,
                            "results": all_results.iter().map(|r| serde_json::json!({
                                "title": r.title,
                                "url": r.url,
                                "snippet": r.snippet,
                                "source": r.source,
                                "relevance_score": r.relevance_score,
                            })).collect::<Vec<_>>()
                        }))?
                    );
                } else {
                    println!("🔍 Searching documentation for: {}", query);
                    println!("   Source: {}", source);
                    println!("   Found {} results", all_results.len());
                    println!();

                    for (i, result) in all_results.iter().enumerate() {
                        println!("{}. {} ({})", i + 1, result.title, result.source);
                        println!("   🔗 {}", result.url);
                        println!("   📝 {}", result.snippet);
                        println!("   ⭐ Relevance: {:.2}", result.relevance_score);
                        println!();
                    }
                }
            }

            SearchAction::Test { query } => {
                println!("🧪 Testing search functionality with query: {}", query);

                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: None,
                        framework: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec!["all".to_string()],
                        relevance_threshold: 0.5,
                        date_range: None,
                    }),
                };

                // Test each search strategy
                println!("🔍 Testing MDN search...");
                let mdn_strategy = DocumentationSearchStrategy::new();
                match mdn_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ MDN: Found {} results", results.len()),
                    Err(e) => println!("❌ MDN: Error - {}", e),
                }

                println!("🔍 Testing GitHub search...");
                let github_strategy = GitHubSearchStrategy::new();
                match github_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ GitHub: Found {} results", results.len()),
                    Err(e) => println!("❌ GitHub: Error - {}", e),
                }

                println!("🔍 Testing StackOverflow search...");
                let stackoverflow_strategy = StackOverflowSearchStrategy;
                match stackoverflow_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ StackOverflow: Found {} results", results.len()),
                    Err(e) => println!("❌ StackOverflow: Error - {}", e),
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Search test completed",
                            "query": query,
                        }))?
                    );
                } else {
                    println!("✅ Search functionality test completed");
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_claude_acp (lines 4339-4579) ---[0m
    async fn handle_claude_acp(&self, command: &ClaudeACPCommands) -> Result<()> {
        use crate::acp_claude::{ClaudeACPConfig, SimplifiedClaudeAdapter};

        match command {
            ClaudeACPCommands::Start { url } => {
                let mut config = ClaudeACPConfig::default();
                config.url = url.clone();

                let mut adapter = SimplifiedClaudeAdapter::new(config);

                if self.json_output {
                    match adapter.connect_with_retry().await {
                        Ok(_) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "message": "Connected to Claude Code",
                                    "url": url,
                                    "session_id": adapter.session_id()
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Failed to connect: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("🚀 Starting Claude Code ACP adapter...");
                    println!("   URL: {}", url);

                    match adapter.connect_with_retry().await {
                        Ok(_) => {
                            println!("✅ Successfully connected to Claude Code!");
                            if let Some(session_id) = adapter.session_id() {
                                println!("   Session ID: {}", session_id);
                            }
                        }
                        Err(e) => {
                            println!("❌ Failed to connect: {}", e);
                            return Err(e.into());
                        }
                    }
                }
            }
            ClaudeACPCommands::Test => {
                let config = ClaudeACPConfig::default();
                let mut adapter = SimplifiedClaudeAdapter::new(config);

                if self.json_output {
                    match adapter.test_connection().await {
                        Ok(result) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "message": result
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Connection test failed: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("🧪 Testing connection to Claude Code...");
                    match adapter.test_connection().await {
                        Ok(result) => {
                            println!("✅ {}", result);
                        }
                        Err(e) => {
                            println!("❌ Connection test failed: {}", e);
                            return Err(e.into());
                        }
                    }
                }
            }
            ClaudeACPCommands::Send { task } => {
                let config = ClaudeACPConfig::default();
                let mut adapter = SimplifiedClaudeAdapter::new(config);

                // Connect first
                adapter.connect_with_retry().await?;

                if self.json_output {
                    match adapter.send_task(task).await {
                        Ok(response) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "response": response
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Failed to send task: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("📤 Sending task to Claude Code: {}", task);
                    match adapter.send_task(task).await {
                        Ok(response) => {
                            println!("📥 Response: {}", response);
                        }
                        Err(e) => {
                            println!("❌ Failed to send task: {}", e);
                            return Err(e.into());
                        }
                    }
                }

                // Disconnect after sending
                adapter.disconnect().await;
            }
            ClaudeACPCommands::Stop => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Claude ACP adapter stopped"
                        }))?
                    );
                } else {
                    println!("🛑 Stopping Claude Code ACP adapter...");
                    println!("✅ Claude ACP adapter stopped");
                }
            }
            ClaudeACPCommands::Status => {
                let config = ClaudeACPConfig::default();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "config": {
                                "url": config.url,
                                "auto_connect": config.auto_connect,
                                "timeout": config.timeout,
                                "max_retries": config.max_retries,
                                "prefer_claude": config.prefer_claude
                            }
                        }))?
                    );
                } else {
                    println!("📊 Claude ACP Status:");
                    println!("   URL: {}", config.url);
                    println!("   Auto-connect: {}", config.auto_connect);
                    println!("   Timeout: {}s", config.timeout);
                    println!("   Max retries: {}", config.max_retries);
                    println!("   Prefer Claude: {}", config.prefer_claude);
                }
            }
            ClaudeACPCommands::Diagnose => {
                let config = ClaudeACPConfig::from_env();
                let mut adapter = SimplifiedClaudeAdapter::new(config.clone());

                if self.json_output {
                    let mut diagnostics = serde_json::json!({
                        "environment": {
                            "url_env": std::env::var("CCSWARM_CLAUDE_ACP_URL").ok(),
                            "auto_connect_env": std::env::var("CCSWARM_CLAUDE_ACP_AUTO_CONNECT").ok(),
                            "timeout_env": std::env::var("CCSWARM_CLAUDE_ACP_TIMEOUT").ok(),
                        },
                        "config": config
                    });

                    // Test connection
                    if let Ok(_) = adapter.connect().await {
                        diagnostics["connection"] = serde_json::json!({
                            "status": "connected",
                            "session_id": adapter.session_id()
                        });
                        adapter.disconnect().await;
                    } else {
                        diagnostics["connection"] = serde_json::json!({
                            "status": "failed"
                        });
                    }

                    println!("{}", serde_json::to_string_pretty(&diagnostics)?);
                } else {
                    println!("🔍 Claude ACP Diagnostics:");
                    println!("\n📋 Environment Variables:");
                    if let Ok(url) = std::env::var("CCSWARM_CLAUDE_ACP_URL") {
                        println!("   CCSWARM_CLAUDE_ACP_URL: {}", url);
                    } else {
                        println!("   CCSWARM_CLAUDE_ACP_URL: <not set>");
                    }
                    if let Ok(auto) = std::env::var("CCSWARM_CLAUDE_ACP_AUTO_CONNECT") {
                        println!("   CCSWARM_CLAUDE_ACP_AUTO_CONNECT: {}", auto);
                    } else {
                        println!("   CCSWARM_CLAUDE_ACP_AUTO_CONNECT: <not set>");
                    }

                    println!("\n⚙️  Configuration:");
                    println!("   URL: {}", config.url);
                    println!("   Auto-connect: {}", config.auto_connect);
                    println!("   Timeout: {}s", config.timeout);
                    println!("   Max retries: {}", config.max_retries);

                    println!("\n🧪 Connection Test:");
                    print!("   Attempting to connect... ");
                    if let Ok(_) = adapter.connect().await {
                        println!("✅ Success!");
                        if let Some(session_id) = adapter.session_id() {
                            println!("   Session ID: {}", session_id);
                        }
                        adapter.disconnect().await;
                    } else {
                        println!("❌ Failed!");
                        println!("   Claude Code may not be running or accessible");
                    }
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1406-1543 method show_status <-> ./crates/ccswarm/src/cli/mod.rs:4339-4579 method handle_claude_acp
  Similarity: 98.81%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_status (lines 1406-1543) ---[0m
    async fn show_status(&self, detailed: bool, agent: Option<&str>) -> Result<()> {
        // Read status from coordination files
        let status_tracker = crate::coordination::StatusTracker::new().await?;

        if let Some(agent_id) = agent {
            // Show specific agent status
            if let Some(status) = status_tracker.get_status(agent_id).await? {
                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&status)?);
                } else {
                    println!("Agent: {}", agent_id);
                    println!("Status: {}", status["status"]);
                    println!("Updated: {}", status["timestamp"]);

                    // Check if this is a backend agent and show backend-specific info
                    if let Some(role) = status.get("role") {
                        if role.as_str() == Some("Backend") {
                            if let Some(backend_info) = status.get("backend_specific") {
                                println!("\n🔧 Backend Status:");
                                if let Some(api_health) = backend_info.get("api_health") {
                                    println!(
                                        "  API Health: {:.1}%",
                                        api_health.as_f64().unwrap_or(0.0) * 100.0
                                    );
                                }
                                if let Some(db) = backend_info.get("database") {
                                    println!(
                                        "  Database: {} ({})",
                                        if db["is_connected"].as_bool().unwrap_or(false) {
                                            "Connected"
                                        } else {
                                            "Disconnected"
                                        },
                                        db["database_type"].as_str().unwrap_or("Unknown")
                                    );
                                }
                                if let Some(server) = backend_info.get("server") {
                                    println!(
                                        "  Server: {:.1}MB RAM, {:.1}% CPU",
                                        server["memory_usage_mb"].as_f64().unwrap_or(0.0),
                                        server["cpu_usage_percent"].as_f64().unwrap_or(0.0)
                                    );
                                }
                                if let Some(services) =
                                    backend_info.get("services").and_then(|s| s.as_array())
                                {
                                    println!("  Active Services: {}", services.len());
                                }
                                if let Some(activity) = backend_info.get("recent_activity") {
                                    println!(
                                        "  Recent API Calls: {}",
                                        activity.as_u64().unwrap_or(0)
                                    );
                                }
                            }
                        }
                    }

                    if detailed {
                        println!(
                            "\nDetails: {}",
                            serde_json::to_string_pretty(&status["additional_info"])?
                        );
                    }
                }
            } else if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "Agent not found",
                        "agent": agent_id,
                    }))?
                );
            } else {
                println!("❌ Agent '{}' not found", agent_id);
            }
        } else {
            // Show all agent statuses
            let statuses = status_tracker.get_all_statuses().await?;

            if self.json_output {
                println!("{}", serde_json::to_string_pretty(&statuses)?);
            } else {
                println!("📊 ccswarm Status");
                println!("================");

                if statuses.is_empty() {
                    println!("No agents found");
                } else {
                    for status in &statuses {
                        println!("Agent: {}", status["agent_id"]);
                        println!("  Status: {}", status["status"]);
                        println!("  Updated: {}", status["timestamp"]);

                        // Show role-specific summary for backend agents
                        if let Some(role) = status.get("role") {
                            if role.as_str() == Some("Backend") {
                                if let Some(backend_info) = status.get("backend_specific") {
                                    if let Some(api_health) = backend_info.get("api_health") {
                                        print!(
                                            "  API Health: {:.0}% | ",
                                            api_health.as_f64().unwrap_or(0.0) * 100.0
                                        );
                                    }
                                    if let Some(db) = backend_info.get("database") {
                                        print!(
                                            "DB: {} | ",
                                            if db["is_connected"].as_bool().unwrap_or(false) {
                                                "✓"
                                            } else {
                                                "✗"
                                            }
                                        );
                                    }
                                    if let Some(services) =
                                        backend_info.get("services").and_then(|s| s.as_array())
                                    {
                                        print!("Services: {}", services.len());
                                    }
                                    println!();
                                }
                            }
                        }

                        if detailed {
                            println!(
                                "  Details: {}",
                                serde_json::to_string_pretty(&status["additional_info"])?
                            );
                        }
                        println!();
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_claude_acp (lines 4339-4579) ---[0m
    async fn handle_claude_acp(&self, command: &ClaudeACPCommands) -> Result<()> {
        use crate::acp_claude::{ClaudeACPConfig, SimplifiedClaudeAdapter};

        match command {
            ClaudeACPCommands::Start { url } => {
                let mut config = ClaudeACPConfig::default();
                config.url = url.clone();

                let mut adapter = SimplifiedClaudeAdapter::new(config);

                if self.json_output {
                    match adapter.connect_with_retry().await {
                        Ok(_) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "message": "Connected to Claude Code",
                                    "url": url,
                                    "session_id": adapter.session_id()
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Failed to connect: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("🚀 Starting Claude Code ACP adapter...");
                    println!("   URL: {}", url);

                    match adapter.connect_with_retry().await {
                        Ok(_) => {
                            println!("✅ Successfully connected to Claude Code!");
                            if let Some(session_id) = adapter.session_id() {
                                println!("   Session ID: {}", session_id);
                            }
                        }
                        Err(e) => {
                            println!("❌ Failed to connect: {}", e);
                            return Err(e.into());
                        }
                    }
                }
            }
            ClaudeACPCommands::Test => {
                let config = ClaudeACPConfig::default();
                let mut adapter = SimplifiedClaudeAdapter::new(config);

                if self.json_output {
                    match adapter.test_connection().await {
                        Ok(result) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "message": result
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Connection test failed: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("🧪 Testing connection to Claude Code...");
                    match adapter.test_connection().await {
                        Ok(result) => {
                            println!("✅ {}", result);
                        }
                        Err(e) => {
                            println!("❌ Connection test failed: {}", e);
                            return Err(e.into());
                        }
                    }
                }
            }
            ClaudeACPCommands::Send { task } => {
                let config = ClaudeACPConfig::default();
                let mut adapter = SimplifiedClaudeAdapter::new(config);

                // Connect first
                adapter.connect_with_retry().await?;

                if self.json_output {
                    match adapter.send_task(task).await {
                        Ok(response) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "success",
                                    "response": response
                                }))?
                            );
                        }
                        Err(e) => {
                            println!(
                                "{}",
                                serde_json::to_string_pretty(&serde_json::json!({
                                    "status": "error",
                                    "message": format!("Failed to send task: {}", e)
                                }))?
                            );
                            return Err(e.into());
                        }
                    }
                } else {
                    println!("📤 Sending task to Claude Code: {}", task);
                    match adapter.send_task(task).await {
                        Ok(response) => {
                            println!("📥 Response: {}", response);
                        }
                        Err(e) => {
                            println!("❌ Failed to send task: {}", e);
                            return Err(e.into());
                        }
                    }
                }

                // Disconnect after sending
                adapter.disconnect().await;
            }
            ClaudeACPCommands::Stop => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Claude ACP adapter stopped"
                        }))?
                    );
                } else {
                    println!("🛑 Stopping Claude Code ACP adapter...");
                    println!("✅ Claude ACP adapter stopped");
                }
            }
            ClaudeACPCommands::Status => {
                let config = ClaudeACPConfig::default();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "config": {
                                "url": config.url,
                                "auto_connect": config.auto_connect,
                                "timeout": config.timeout,
                                "max_retries": config.max_retries,
                                "prefer_claude": config.prefer_claude
                            }
                        }))?
                    );
                } else {
                    println!("📊 Claude ACP Status:");
                    println!("   URL: {}", config.url);
                    println!("   Auto-connect: {}", config.auto_connect);
                    println!("   Timeout: {}s", config.timeout);
                    println!("   Max retries: {}", config.max_retries);
                    println!("   Prefer Claude: {}", config.prefer_claude);
                }
            }
            ClaudeACPCommands::Diagnose => {
                let config = ClaudeACPConfig::from_env();
                let mut adapter = SimplifiedClaudeAdapter::new(config.clone());

                if self.json_output {
                    let mut diagnostics = serde_json::json!({
                        "environment": {
                            "url_env": std::env::var("CCSWARM_CLAUDE_ACP_URL").ok(),
                            "auto_connect_env": std::env::var("CCSWARM_CLAUDE_ACP_AUTO_CONNECT").ok(),
                            "timeout_env": std::env::var("CCSWARM_CLAUDE_ACP_TIMEOUT").ok(),
                        },
                        "config": config
                    });

                    // Test connection
                    if let Ok(_) = adapter.connect().await {
                        diagnostics["connection"] = serde_json::json!({
                            "status": "connected",
                            "session_id": adapter.session_id()
                        });
                        adapter.disconnect().await;
                    } else {
                        diagnostics["connection"] = serde_json::json!({
                            "status": "failed"
                        });
                    }

                    println!("{}", serde_json::to_string_pretty(&diagnostics)?);
                } else {
                    println!("🔍 Claude ACP Diagnostics:");
                    println!("\n📋 Environment Variables:");
                    if let Ok(url) = std::env::var("CCSWARM_CLAUDE_ACP_URL") {
                        println!("   CCSWARM_CLAUDE_ACP_URL: {}", url);
                    } else {
                        println!("   CCSWARM_CLAUDE_ACP_URL: <not set>");
                    }
                    if let Ok(auto) = std::env::var("CCSWARM_CLAUDE_ACP_AUTO_CONNECT") {
                        println!("   CCSWARM_CLAUDE_ACP_AUTO_CONNECT: {}", auto);
                    } else {
                        println!("   CCSWARM_CLAUDE_ACP_AUTO_CONNECT: <not set>");
                    }

                    println!("\n⚙️  Configuration:");
                    println!("   URL: {}", config.url);
                    println!("   Auto-connect: {}", config.auto_connect);
                    println!("   Timeout: {}s", config.timeout);
                    println!("   Max retries: {}", config.max_retries);

                    println!("\n🧪 Connection Test:");
                    print!("   Attempting to connect... ");
                    if let Ok(_) = adapter.connect().await {
                        println!("✅ Success!");
                        if let Some(session_id) = adapter.session_id() {
                            println!("   Session ID: {}", session_id);
                        }
                        adapter.disconnect().await;
                    } else {
                        println!("❌ Failed!");
                        println!("   Claude Code may not be running or accessible");
                    }
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2279-2458 method handle_worktree <-> ./crates/ccswarm/src/cli/mod.rs:3628-3804 method handle_sangha
  Similarity: 97.52%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_worktree (lines 2279-2458) ---[0m
    async fn handle_worktree(&self, action: &WorktreeAction) -> Result<()> {
        let manager = crate::git::shell::ShellWorktreeManager::new(self.repo_path.clone())?;

        match action {
            WorktreeAction::List => {
                let worktrees = manager.list_worktrees().await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&worktrees)?);
                } else {
                    println!("🌳 Git Worktrees");
                    println!("===============");

                    for wt in &worktrees {
                        println!("Path: {}", wt.path.display());
                        println!("  Branch: {}", wt.branch);
                        println!("  Head: {}", wt.head_commit);
                        println!("  Locked: {}", wt.is_locked);
                        println!();
                    }
                }
            }
            WorktreeAction::Create {
                path,
                branch,
                new_branch,
            } => {
                let info = if *new_branch {
                    manager.create_worktree_full(path, branch, true).await?
                } else {
                    manager.create_worktree(path, branch).await?
                };

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&info)?);
                } else {
                    println!("✅ Worktree created");
                    println!("   Path: {}", info.path.display());
                    println!("   Branch: {}", info.branch);
                }
            }
            WorktreeAction::Remove { path, force } => {
                if *force {
                    manager.remove_worktree_full(path, true).await?
                } else {
                    manager.remove_worktree(path).await?
                };

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktree removed",
                            "path": path,
                        }))?
                    );
                } else {
                    println!("✅ Worktree removed: {}", path.display());
                }
            }
            WorktreeAction::Prune => {
                manager.prune_worktrees().await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktrees pruned",
                        }))?
                    );
                } else {
                    println!("✅ Stale worktrees pruned");
                }
            }
            WorktreeAction::Clean { force } => {
                use std::io::{self, Write};

                // Find all ccswarm-related worktrees
                let worktrees = manager.list_worktrees().await?;
                let ccswarm_worktrees: Vec<_> = worktrees
                    .iter()
                    .filter(|w| w.branch.contains("agent") || w.branch.contains("feature/"))
                    .collect();

                if ccswarm_worktrees.is_empty() {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "No ccswarm worktrees found",
                            }))?
                        );
                    } else {
                        println!("✅ No ccswarm worktrees to clean");
                    }
                    return Ok(());
                }

                // Ask for confirmation unless forced
                if !force {
                    println!("⚠️  Found {} ccswarm worktrees:", ccswarm_worktrees.len());
                    for w in &ccswarm_worktrees {
                        println!("   - {} ({})", w.path.display(), w.branch);
                    }
                    print!("\nAre you sure you want to remove all these worktrees? [y/N] ");
                    io::stdout().flush()?;

                    let mut response = String::new();
                    io::stdin().read_line(&mut response)?;

                    if !response.trim().eq_ignore_ascii_case("y") {
                        println!("❌ Cleanup cancelled");
                        return Ok(());
                    }
                }

                // Remove all ccswarm worktrees
                let mut removed_count = 0;
                for worktree in ccswarm_worktrees {
                    match manager.remove_worktree(&worktree.path).await {
                        Ok(_) => {
                            removed_count += 1;
                            if !self.json_output {
                                println!("   ✓ Removed {}", worktree.path.display());
                            }
                        }
                        Err(e) => {
                            if !self.json_output {
                                println!(
                                    "   ✗ Failed to remove {}: {}",
                                    worktree.path.display(),
                                    e
                                );
                            }
                        }
                    }
                }

                // Also clean up branches
                let output = tokio::process::Command::new("git")
                    .args(["branch", "--list", "*agent*", "feature/*"])
                    .output()
                    .await?;

                if output.status.success() {
                    let branches = String::from_utf8_lossy(&output.stdout);
                    let branch_count = branches.lines().count();

                    if branch_count > 0 {
                        tokio::process::Command::new("git")
                            .args(&["branch", "-D"])
                            .args(branches.lines().map(|b| b.trim().trim_start_matches("* ")))
                            .output()
                            .await?;
                    }
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Cleanup completed",
                            "worktrees_removed": removed_count,
                        }))?
                    );
                } else {
                    println!(
                        "\n✅ Cleanup completed: {} worktrees removed",
                        removed_count
                    );
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_sangha (lines 3628-3804) ---[0m
    async fn handle_sangha(&self, action: &SanghaAction) -> Result<()> {
        // Using stub implementation for now
        use crate::extension_stub::sangha::{Vote, VoteType};
        use std::str::FromStr;

        // Stub implementation - just log the action
        info!("🏛️  Sangha action requested (stub implementation)");

        match action {
            SanghaAction::Propose {
                proposal_type,
                file,
            } => {
                info!("📋 Submitting proposal to Sangha (stub implementation)");

                // Read proposal specification from file
                let _spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read proposal file")?;

                info!("Proposal type: {}", proposal_type);

                // Stub: Generate a fake proposal ID
                let proposal_id = uuid::Uuid::new_v4();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Proposal submitted",
                            "proposal_id": proposal_id,
                            "type": proposal_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!(
                        "📋 Submitting {} proposal from: {}",
                        proposal_type,
                        file.display()
                    );
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Proposal submitted successfully");
                }
            }

            SanghaAction::Vote {
                proposal_id,
                choice,
                reason,
            } => {
                info!("🗳️ Casting vote (stub implementation)");

                // Parse vote choice
                let vote_type = match choice.to_lowercase().as_str() {
                    "aye" | "yes" | "approve" => VoteType::Approve,
                    "nay" | "no" | "reject" => VoteType::Reject,
                    "abstain" => VoteType::Abstain,
                    "veto" | "needs_changes" => VoteType::NeedsChanges,
                    _ => {
                        anyhow::bail!(
                            "Invalid vote choice: {}. Use aye, nay, abstain, or veto",
                            choice
                        );
                    }
                };

                // Parse proposal ID
                let prop_id =
                    uuid::Uuid::from_str(proposal_id).context("Invalid proposal ID format")?;

                // Create vote (stub)
                let _vote = Vote::new("cli-user".to_string(), prop_id.to_string(), vote_type);

                info!("Vote created for proposal: {}", proposal_id);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Vote cast",
                            "proposal_id": proposal_id,
                            "choice": choice,
                            "reason": reason,
                        }))?
                    );
                } else {
                    println!("🗳️ Casting vote on proposal: {}", proposal_id);
                    println!("   Choice: {}", choice);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                    println!("✅ Vote cast successfully");
                }
            }

            SanghaAction::List { all, status } => {
                info!("📊 Listing Sangha status (stub implementation)");

                // Stub statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Sangha status retrieved (stub)",
                            "stats": {
                                "total_members": 1,
                                "active_members": 1,
                                "total_proposals": 0,
                                "active_proposals": 0,
                                "consensus_algorithm": "stub",
                            },
                            "show_all": all,
                            "filter_status": status,
                        }))?
                    );
                } else {
                    println!("📊 Sangha Status (Stub Implementation)");
                    println!("=====================================");
                    println!("👥 Members: 1 total, 1 active");
                    println!("📋 Proposals: 0 total, 0 active");
                    println!("🧠 Consensus Algorithm: stub");

                    println!(
                        "\n💡 No active proposals. Use 'ccswarm sangha propose' to create one."
                    );
                }
            }

            SanghaAction::Session { id, active } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session info",
                            "session_id": id,
                            "active": active,
                        }))?
                    );
                } else {
                    println!("🏛️ Sangha Session");
                    if *active {
                        println!("No active session");
                    }
                }
            }

            SanghaAction::ExtensionReview {
                proposal_id,
                technical_check,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension reviewed",
                            "proposal_id": proposal_id,
                            "technical_check": technical_check,
                        }))?
                    );
                } else {
                    println!("🔍 Reviewing extension proposal: {}", proposal_id);
                    if *technical_check {
                        println!("   Performing technical validation...");
                    }
                    println!("✅ Review completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:3628-3804 method handle_sangha <-> ./crates/ccswarm/src/cli/mod.rs:5584-5809 method handle_doctor
  Similarity: 84.57%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_sangha (lines 3628-3804) ---[0m
    async fn handle_sangha(&self, action: &SanghaAction) -> Result<()> {
        // Using stub implementation for now
        use crate::extension_stub::sangha::{Vote, VoteType};
        use std::str::FromStr;

        // Stub implementation - just log the action
        info!("🏛️  Sangha action requested (stub implementation)");

        match action {
            SanghaAction::Propose {
                proposal_type,
                file,
            } => {
                info!("📋 Submitting proposal to Sangha (stub implementation)");

                // Read proposal specification from file
                let _spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read proposal file")?;

                info!("Proposal type: {}", proposal_type);

                // Stub: Generate a fake proposal ID
                let proposal_id = uuid::Uuid::new_v4();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Proposal submitted",
                            "proposal_id": proposal_id,
                            "type": proposal_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!(
                        "📋 Submitting {} proposal from: {}",
                        proposal_type,
                        file.display()
                    );
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Proposal submitted successfully");
                }
            }

            SanghaAction::Vote {
                proposal_id,
                choice,
                reason,
            } => {
                info!("🗳️ Casting vote (stub implementation)");

                // Parse vote choice
                let vote_type = match choice.to_lowercase().as_str() {
                    "aye" | "yes" | "approve" => VoteType::Approve,
                    "nay" | "no" | "reject" => VoteType::Reject,
                    "abstain" => VoteType::Abstain,
                    "veto" | "needs_changes" => VoteType::NeedsChanges,
                    _ => {
                        anyhow::bail!(
                            "Invalid vote choice: {}. Use aye, nay, abstain, or veto",
                            choice
                        );
                    }
                };

                // Parse proposal ID
                let prop_id =
                    uuid::Uuid::from_str(proposal_id).context("Invalid proposal ID format")?;

                // Create vote (stub)
                let _vote = Vote::new("cli-user".to_string(), prop_id.to_string(), vote_type);

                info!("Vote created for proposal: {}", proposal_id);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Vote cast",
                            "proposal_id": proposal_id,
                            "choice": choice,
                            "reason": reason,
                        }))?
                    );
                } else {
                    println!("🗳️ Casting vote on proposal: {}", proposal_id);
                    println!("   Choice: {}", choice);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                    println!("✅ Vote cast successfully");
                }
            }

            SanghaAction::List { all, status } => {
                info!("📊 Listing Sangha status (stub implementation)");

                // Stub statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Sangha status retrieved (stub)",
                            "stats": {
                                "total_members": 1,
                                "active_members": 1,
                                "total_proposals": 0,
                                "active_proposals": 0,
                                "consensus_algorithm": "stub",
                            },
                            "show_all": all,
                            "filter_status": status,
                        }))?
                    );
                } else {
                    println!("📊 Sangha Status (Stub Implementation)");
                    println!("=====================================");
                    println!("👥 Members: 1 total, 1 active");
                    println!("📋 Proposals: 0 total, 0 active");
                    println!("🧠 Consensus Algorithm: stub");

                    println!(
                        "\n💡 No active proposals. Use 'ccswarm sangha propose' to create one."
                    );
                }
            }

            SanghaAction::Session { id, active } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session info",
                            "session_id": id,
                            "active": active,
                        }))?
                    );
                } else {
                    println!("🏛️ Sangha Session");
                    if *active {
                        println!("No active session");
                    }
                }
            }

            SanghaAction::ExtensionReview {
                proposal_id,
                technical_check,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension reviewed",
                            "proposal_id": proposal_id,
                            "technical_check": technical_check,
                        }))?
                    );
                } else {
                    println!("🔍 Reviewing extension proposal: {}", proposal_id);
                    if *technical_check {
                        println!("   Performing technical validation...");
                    }
                    println!("✅ Review completed");
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_doctor (lines 5584-5809) ---[0m
    async fn handle_doctor(
        &self,
        fix: bool,
        error_code: Option<&str>,
        check_api: bool,
    ) -> Result<()> {
        use crate::utils::error_recovery::ErrorRecoveryDB;
        use crate::utils::user_error::CommonErrors;

        // Handle specific error code diagnosis
        if let Some(code) = error_code {
            println!("{}", "🔍 Error Code Diagnosis".bright_cyan().bold());
            println!("{}", "=======================".bright_cyan());
            println!();
            println!("Analyzing error code: {}", code.bright_yellow());
            println!();

            let recovery_db = ErrorRecoveryDB::new();
            if let Some(recovery) = recovery_db.get_recovery(code) {
                match &recovery {
                    crate::utils::error_recovery::RecoveryStep::UserAction { description } => {
                        println!("📋 {}", description.bright_white());
                    },
                    crate::utils::error_recovery::RecoveryStep::Command { description, .. } => {
                        println!("📋 {}", description.bright_white());
                    },
                    crate::utils::error_recovery::RecoveryStep::FileCreate { path, .. } => {
                        println!("📋 Create file: {}", path.bright_white());
                    },
                    crate::utils::error_recovery::RecoveryStep::EnvVar { name, .. } => {
                        println!("📋 Set environment variable: {}", name.bright_white());
                    },
                }
                println!();
                println!("Recovery steps:");
                let steps = [recovery.clone()]; // Treat the single recovery step as the step list
                for (i, step) in steps.iter().enumerate() {
                    match step {
                        crate::utils::error_recovery::RecoveryStep::Command {
                            cmd,
                            description,
                        } => {
                            println!("  {}. {} {}", i + 1, "Run:".bright_yellow(), description);
                            println!("     {}", cmd.bright_white().on_black());
                        }
                        crate::utils::error_recovery::RecoveryStep::FileCreate { path, .. } => {
                            println!(
                                "  {}. {} {}",
                                i + 1,
                                "Create file:".bright_yellow(),
                                path.bright_white()
                            );
                        }
                        crate::utils::error_recovery::RecoveryStep::EnvVar { name, example } => {
                            println!(
                                "  {}. {} {}",
                                i + 1,
                                "Set environment variable:".bright_yellow(),
                                name.bright_white()
                            );
                            println!("     Example: {}={}", name, example.bright_black());
                        }
                        crate::utils::error_recovery::RecoveryStep::UserAction { description } => {
                            println!(
                                "  {}. {} {}",
                                i + 1,
                                "Action required:".bright_yellow(),
                                description
                            );
                        }
                    }
                    println!();
                }

                let can_auto_fix = matches!(recovery, crate::utils::error_recovery::RecoveryStep::Command { .. });
                if can_auto_fix && fix {
                    recovery_db.auto_fix(code).await?;
                } else if can_auto_fix {
                    println!(
                        "💡 This error can be auto-fixed! Run: ccswarm doctor --error {} --fix",
                        code
                    );
                }
            } else {
                println!("❌ Unknown error code: {}", code);
                println!("   See all error codes: ccswarm help errors");
            }
            return Ok(());
        }

        // Handle API connectivity check
        if check_api {
            println!("{}", "🌐 API Connectivity Check".bright_cyan().bold());
            println!("{}", "=========================".bright_cyan());
            println!();

            print!("Testing Anthropic API... ");
            match reqwest::get("https://api.anthropic.com/v1/health").await {
                Ok(resp) if resp.status().is_success() => {
                    println!("{}", "✅ Connected".bright_green());
                }
                Ok(resp) => {
                    println!(
                        "{}",
                        format!("⚠️  Status: {}", resp.status()).bright_yellow()
                    );
                }
                Err(e) => {
                    println!("{}", "❌ Failed".bright_red());
                    println!("   {}", e.to_string().bright_black());
                }
            }
            println!();
            return Ok(());
        }

        println!("{}", "🏥 ccswarm System Diagnosis".bright_cyan().bold());
        println!("{}", "===========================".bright_cyan());
        println!();

        let mut issues = Vec::new();

        // Check Git
        print!("Checking Git... ");
        match std::process::Command::new("git").arg("--version").output() {
            Ok(output) if output.status.success() => {
                println!("{}", "✅ OK".bright_green());
            }
            _ => {
                println!("{}", "❌ Not found".bright_red());
                issues.push("git");
            }
        }

        // Check API keys
        print!("Checking API keys... ");
        if std::env::var("ANTHROPIC_API_KEY").is_ok() {
            println!("{}", "✅ Set".bright_green());
        } else {
            println!("{}", "⚠️  Not set".bright_yellow());
            issues.push("api_key");
        }

        // Check config
        print!("Checking configuration... ");
        let config_path = self.repo_path.join("ccswarm.json");
        if config_path.exists() {
            match CcswarmConfig::from_file(config_path.clone()).await {
                Ok(_) => println!("{}", "✅ Valid".bright_green()),
                Err(e) => {
                    println!("{}", "❌ Invalid".bright_red());
                    println!("   {}", e.to_string().bright_black());
                    issues.push("config");
                }
            }
        } else {
            println!("{}", "❌ Not found".bright_red());
            issues.push("config");
        }

        // Check git repo
        print!("Checking git repository... ");
        let git_dir = self.repo_path.join(".git");
        if git_dir.exists() {
            println!("{}", "✅ Initialized".bright_green());
        } else {
            println!("{}", "⚠️  Not initialized".bright_yellow());
            issues.push("git_repo");
        }

        // Check disk space
        print!("Checking disk space... ");
        // Simple check - in real implementation would use proper system calls
        println!("{}", "✅ Sufficient".bright_green());

        println!();

        if issues.is_empty() {
            println!("{}", "✅ All systems operational!".bright_green().bold());
        } else {
            println!(
                "{}",
                format!("⚠️  Found {} issues", issues.len())
                    .bright_yellow()
                    .bold()
            );

            if fix {
                println!();
                println!("{}", "🔧 Attempting fixes...".bright_cyan());

                for issue in &issues {
                    match *issue {
                        "git" => {
                            println!("• Git: Please install git from https://git-scm.com");
                        }
                        "api_key" => {
                            CommonErrors::api_key_missing("Anthropic").display();
                        }
                        "config" => {
                            println!("• Config: Run 'ccswarm setup' to create configuration");
                        }
                        "git_repo" => {
                            if fix {
                                println!("• Initializing git repository...");
                                crate::git::shell::ShellWorktreeManager::init_if_needed(
                                    &self.repo_path,
                                )
                                .await?;
                                println!("  ✅ Git repository initialized");
                            }
                        }
                        _ => {}
                    }
                }
            } else {
                println!();
                println!(
                    "{}",
                    "💡 Run with --fix to attempt automatic fixes".bright_black()
                );
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2279-2458 method handle_worktree <-> ./crates/ccswarm/src/cli/mod.rs:4173-4336 method handle_search
  Similarity: 96.96%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_worktree (lines 2279-2458) ---[0m
    async fn handle_worktree(&self, action: &WorktreeAction) -> Result<()> {
        let manager = crate::git::shell::ShellWorktreeManager::new(self.repo_path.clone())?;

        match action {
            WorktreeAction::List => {
                let worktrees = manager.list_worktrees().await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&worktrees)?);
                } else {
                    println!("🌳 Git Worktrees");
                    println!("===============");

                    for wt in &worktrees {
                        println!("Path: {}", wt.path.display());
                        println!("  Branch: {}", wt.branch);
                        println!("  Head: {}", wt.head_commit);
                        println!("  Locked: {}", wt.is_locked);
                        println!();
                    }
                }
            }
            WorktreeAction::Create {
                path,
                branch,
                new_branch,
            } => {
                let info = if *new_branch {
                    manager.create_worktree_full(path, branch, true).await?
                } else {
                    manager.create_worktree(path, branch).await?
                };

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&info)?);
                } else {
                    println!("✅ Worktree created");
                    println!("   Path: {}", info.path.display());
                    println!("   Branch: {}", info.branch);
                }
            }
            WorktreeAction::Remove { path, force } => {
                if *force {
                    manager.remove_worktree_full(path, true).await?
                } else {
                    manager.remove_worktree(path).await?
                };

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktree removed",
                            "path": path,
                        }))?
                    );
                } else {
                    println!("✅ Worktree removed: {}", path.display());
                }
            }
            WorktreeAction::Prune => {
                manager.prune_worktrees().await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktrees pruned",
                        }))?
                    );
                } else {
                    println!("✅ Stale worktrees pruned");
                }
            }
            WorktreeAction::Clean { force } => {
                use std::io::{self, Write};

                // Find all ccswarm-related worktrees
                let worktrees = manager.list_worktrees().await?;
                let ccswarm_worktrees: Vec<_> = worktrees
                    .iter()
                    .filter(|w| w.branch.contains("agent") || w.branch.contains("feature/"))
                    .collect();

                if ccswarm_worktrees.is_empty() {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "No ccswarm worktrees found",
                            }))?
                        );
                    } else {
                        println!("✅ No ccswarm worktrees to clean");
                    }
                    return Ok(());
                }

                // Ask for confirmation unless forced
                if !force {
                    println!("⚠️  Found {} ccswarm worktrees:", ccswarm_worktrees.len());
                    for w in &ccswarm_worktrees {
                        println!("   - {} ({})", w.path.display(), w.branch);
                    }
                    print!("\nAre you sure you want to remove all these worktrees? [y/N] ");
                    io::stdout().flush()?;

                    let mut response = String::new();
                    io::stdin().read_line(&mut response)?;

                    if !response.trim().eq_ignore_ascii_case("y") {
                        println!("❌ Cleanup cancelled");
                        return Ok(());
                    }
                }

                // Remove all ccswarm worktrees
                let mut removed_count = 0;
                for worktree in ccswarm_worktrees {
                    match manager.remove_worktree(&worktree.path).await {
                        Ok(_) => {
                            removed_count += 1;
                            if !self.json_output {
                                println!("   ✓ Removed {}", worktree.path.display());
                            }
                        }
                        Err(e) => {
                            if !self.json_output {
                                println!(
                                    "   ✗ Failed to remove {}: {}",
                                    worktree.path.display(),
                                    e
                                );
                            }
                        }
                    }
                }

                // Also clean up branches
                let output = tokio::process::Command::new("git")
                    .args(["branch", "--list", "*agent*", "feature/*"])
                    .output()
                    .await?;

                if output.status.success() {
                    let branches = String::from_utf8_lossy(&output.stdout);
                    let branch_count = branches.lines().count();

                    if branch_count > 0 {
                        tokio::process::Command::new("git")
                            .args(&["branch", "-D"])
                            .args(branches.lines().map(|b| b.trim().trim_start_matches("* ")))
                            .output()
                            .await?;
                    }
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Cleanup completed",
                            "worktrees_removed": removed_count,
                        }))?
                    );
                } else {
                    println!(
                        "\n✅ Cleanup completed: {} worktrees removed",
                        removed_count
                    );
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_search (lines 4173-4336) ---[0m
    async fn handle_search(&self, action: &SearchAction) -> Result<()> {
        use crate::extension_stub::agent_extension::{
            DocumentationSearchStrategy, GitHubSearchStrategy, SearchContext, SearchFilters,
            SearchQuery, SearchStrategy, StackOverflowSearchStrategy,
        };

        match action {
            SearchAction::Docs {
                query,
                source,
                limit,
            } => {
                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: Some("rust".to_string()),
                        framework: Some("general".to_string()),
                        date_range: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec![source.clone()],
                        relevance_threshold: 0.5,
                    }),
                };

                let mut all_results = Vec::new();

                if source == "all" || source == "mdn" {
                    let mdn_strategy = DocumentationSearchStrategy::new();
                    match mdn_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("MDN search failed: {}", e),
                    }
                }

                if source == "all" || source == "github" {
                    let github_strategy = GitHubSearchStrategy::new();
                    match github_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("GitHub search failed: {}", e),
                    }
                }

                if source == "all" || source == "stackoverflow" {
                    let stackoverflow_strategy = StackOverflowSearchStrategy;
                    match stackoverflow_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("StackOverflow search failed: {}", e),
                    }
                }

                all_results.sort_by(|a, b| {
                    b.relevance_score
                        .partial_cmp(&a.relevance_score)
                        .unwrap_or(std::cmp::Ordering::Equal)
                });
                all_results.truncate(*limit);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "query": query,
                            "source": source,
                            "results": all_results.iter().map(|r| serde_json::json!({
                                "title": r.title,
                                "url": r.url,
                                "snippet": r.snippet,
                                "source": r.source,
                                "relevance_score": r.relevance_score,
                            })).collect::<Vec<_>>()
                        }))?
                    );
                } else {
                    println!("🔍 Searching documentation for: {}", query);
                    println!("   Source: {}", source);
                    println!("   Found {} results", all_results.len());
                    println!();

                    for (i, result) in all_results.iter().enumerate() {
                        println!("{}. {} ({})", i + 1, result.title, result.source);
                        println!("   🔗 {}", result.url);
                        println!("   📝 {}", result.snippet);
                        println!("   ⭐ Relevance: {:.2}", result.relevance_score);
                        println!();
                    }
                }
            }

            SearchAction::Test { query } => {
                println!("🧪 Testing search functionality with query: {}", query);

                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: None,
                        framework: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec!["all".to_string()],
                        relevance_threshold: 0.5,
                        date_range: None,
                    }),
                };

                // Test each search strategy
                println!("🔍 Testing MDN search...");
                let mdn_strategy = DocumentationSearchStrategy::new();
                match mdn_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ MDN: Found {} results", results.len()),
                    Err(e) => println!("❌ MDN: Error - {}", e),
                }

                println!("🔍 Testing GitHub search...");
                let github_strategy = GitHubSearchStrategy::new();
                match github_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ GitHub: Found {} results", results.len()),
                    Err(e) => println!("❌ GitHub: Error - {}", e),
                }

                println!("🔍 Testing StackOverflow search...");
                let stackoverflow_strategy = StackOverflowSearchStrategy;
                match stackoverflow_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ StackOverflow: Found {} results", results.len()),
                    Err(e) => println!("❌ StackOverflow: Error - {}", e),
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Search test completed",
                            "query": query,
                        }))?
                    );
                } else {
                    println!("✅ Search functionality test completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:3628-3804 method handle_sangha <-> ./crates/ccswarm/src/cli/mod.rs:4173-4336 method handle_search
  Similarity: 97.69%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_sangha (lines 3628-3804) ---[0m
    async fn handle_sangha(&self, action: &SanghaAction) -> Result<()> {
        // Using stub implementation for now
        use crate::extension_stub::sangha::{Vote, VoteType};
        use std::str::FromStr;

        // Stub implementation - just log the action
        info!("🏛️  Sangha action requested (stub implementation)");

        match action {
            SanghaAction::Propose {
                proposal_type,
                file,
            } => {
                info!("📋 Submitting proposal to Sangha (stub implementation)");

                // Read proposal specification from file
                let _spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read proposal file")?;

                info!("Proposal type: {}", proposal_type);

                // Stub: Generate a fake proposal ID
                let proposal_id = uuid::Uuid::new_v4();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Proposal submitted",
                            "proposal_id": proposal_id,
                            "type": proposal_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!(
                        "📋 Submitting {} proposal from: {}",
                        proposal_type,
                        file.display()
                    );
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Proposal submitted successfully");
                }
            }

            SanghaAction::Vote {
                proposal_id,
                choice,
                reason,
            } => {
                info!("🗳️ Casting vote (stub implementation)");

                // Parse vote choice
                let vote_type = match choice.to_lowercase().as_str() {
                    "aye" | "yes" | "approve" => VoteType::Approve,
                    "nay" | "no" | "reject" => VoteType::Reject,
                    "abstain" => VoteType::Abstain,
                    "veto" | "needs_changes" => VoteType::NeedsChanges,
                    _ => {
                        anyhow::bail!(
                            "Invalid vote choice: {}. Use aye, nay, abstain, or veto",
                            choice
                        );
                    }
                };

                // Parse proposal ID
                let prop_id =
                    uuid::Uuid::from_str(proposal_id).context("Invalid proposal ID format")?;

                // Create vote (stub)
                let _vote = Vote::new("cli-user".to_string(), prop_id.to_string(), vote_type);

                info!("Vote created for proposal: {}", proposal_id);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Vote cast",
                            "proposal_id": proposal_id,
                            "choice": choice,
                            "reason": reason,
                        }))?
                    );
                } else {
                    println!("🗳️ Casting vote on proposal: {}", proposal_id);
                    println!("   Choice: {}", choice);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                    println!("✅ Vote cast successfully");
                }
            }

            SanghaAction::List { all, status } => {
                info!("📊 Listing Sangha status (stub implementation)");

                // Stub statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Sangha status retrieved (stub)",
                            "stats": {
                                "total_members": 1,
                                "active_members": 1,
                                "total_proposals": 0,
                                "active_proposals": 0,
                                "consensus_algorithm": "stub",
                            },
                            "show_all": all,
                            "filter_status": status,
                        }))?
                    );
                } else {
                    println!("📊 Sangha Status (Stub Implementation)");
                    println!("=====================================");
                    println!("👥 Members: 1 total, 1 active");
                    println!("📋 Proposals: 0 total, 0 active");
                    println!("🧠 Consensus Algorithm: stub");

                    println!(
                        "\n💡 No active proposals. Use 'ccswarm sangha propose' to create one."
                    );
                }
            }

            SanghaAction::Session { id, active } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session info",
                            "session_id": id,
                            "active": active,
                        }))?
                    );
                } else {
                    println!("🏛️ Sangha Session");
                    if *active {
                        println!("No active session");
                    }
                }
            }

            SanghaAction::ExtensionReview {
                proposal_id,
                technical_check,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension reviewed",
                            "proposal_id": proposal_id,
                            "technical_check": technical_check,
                        }))?
                    );
                } else {
                    println!("🔍 Reviewing extension proposal: {}", proposal_id);
                    if *technical_check {
                        println!("   Performing technical validation...");
                    }
                    println!("✅ Review completed");
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_search (lines 4173-4336) ---[0m
    async fn handle_search(&self, action: &SearchAction) -> Result<()> {
        use crate::extension_stub::agent_extension::{
            DocumentationSearchStrategy, GitHubSearchStrategy, SearchContext, SearchFilters,
            SearchQuery, SearchStrategy, StackOverflowSearchStrategy,
        };

        match action {
            SearchAction::Docs {
                query,
                source,
                limit,
            } => {
                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: Some("rust".to_string()),
                        framework: Some("general".to_string()),
                        date_range: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec![source.clone()],
                        relevance_threshold: 0.5,
                    }),
                };

                let mut all_results = Vec::new();

                if source == "all" || source == "mdn" {
                    let mdn_strategy = DocumentationSearchStrategy::new();
                    match mdn_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("MDN search failed: {}", e),
                    }
                }

                if source == "all" || source == "github" {
                    let github_strategy = GitHubSearchStrategy::new();
                    match github_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("GitHub search failed: {}", e),
                    }
                }

                if source == "all" || source == "stackoverflow" {
                    let stackoverflow_strategy = StackOverflowSearchStrategy;
                    match stackoverflow_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("StackOverflow search failed: {}", e),
                    }
                }

                all_results.sort_by(|a, b| {
                    b.relevance_score
                        .partial_cmp(&a.relevance_score)
                        .unwrap_or(std::cmp::Ordering::Equal)
                });
                all_results.truncate(*limit);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "query": query,
                            "source": source,
                            "results": all_results.iter().map(|r| serde_json::json!({
                                "title": r.title,
                                "url": r.url,
                                "snippet": r.snippet,
                                "source": r.source,
                                "relevance_score": r.relevance_score,
                            })).collect::<Vec<_>>()
                        }))?
                    );
                } else {
                    println!("🔍 Searching documentation for: {}", query);
                    println!("   Source: {}", source);
                    println!("   Found {} results", all_results.len());
                    println!();

                    for (i, result) in all_results.iter().enumerate() {
                        println!("{}. {} ({})", i + 1, result.title, result.source);
                        println!("   🔗 {}", result.url);
                        println!("   📝 {}", result.snippet);
                        println!("   ⭐ Relevance: {:.2}", result.relevance_score);
                        println!();
                    }
                }
            }

            SearchAction::Test { query } => {
                println!("🧪 Testing search functionality with query: {}", query);

                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: None,
                        framework: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec!["all".to_string()],
                        relevance_threshold: 0.5,
                        date_range: None,
                    }),
                };

                // Test each search strategy
                println!("🔍 Testing MDN search...");
                let mdn_strategy = DocumentationSearchStrategy::new();
                match mdn_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ MDN: Found {} results", results.len()),
                    Err(e) => println!("❌ MDN: Error - {}", e),
                }

                println!("🔍 Testing GitHub search...");
                let github_strategy = GitHubSearchStrategy::new();
                match github_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ GitHub: Found {} results", results.len()),
                    Err(e) => println!("❌ GitHub: Error - {}", e),
                }

                println!("🔍 Testing StackOverflow search...");
                let stackoverflow_strategy = StackOverflowSearchStrategy;
                match stackoverflow_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ StackOverflow: Found {} results", results.len()),
                    Err(e) => println!("❌ StackOverflow: Error - {}", e),
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Search test completed",
                            "query": query,
                        }))?
                    );
                } else {
                    println!("✅ Search functionality test completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1406-1543 method show_status <-> ./crates/ccswarm/src/cli/mod.rs:2279-2458 method handle_worktree
  Similarity: 98.36%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_status (lines 1406-1543) ---[0m
    async fn show_status(&self, detailed: bool, agent: Option<&str>) -> Result<()> {
        // Read status from coordination files
        let status_tracker = crate::coordination::StatusTracker::new().await?;

        if let Some(agent_id) = agent {
            // Show specific agent status
            if let Some(status) = status_tracker.get_status(agent_id).await? {
                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&status)?);
                } else {
                    println!("Agent: {}", agent_id);
                    println!("Status: {}", status["status"]);
                    println!("Updated: {}", status["timestamp"]);

                    // Check if this is a backend agent and show backend-specific info
                    if let Some(role) = status.get("role") {
                        if role.as_str() == Some("Backend") {
                            if let Some(backend_info) = status.get("backend_specific") {
                                println!("\n🔧 Backend Status:");
                                if let Some(api_health) = backend_info.get("api_health") {
                                    println!(
                                        "  API Health: {:.1}%",
                                        api_health.as_f64().unwrap_or(0.0) * 100.0
                                    );
                                }
                                if let Some(db) = backend_info.get("database") {
                                    println!(
                                        "  Database: {} ({})",
                                        if db["is_connected"].as_bool().unwrap_or(false) {
                                            "Connected"
                                        } else {
                                            "Disconnected"
                                        },
                                        db["database_type"].as_str().unwrap_or("Unknown")
                                    );
                                }
                                if let Some(server) = backend_info.get("server") {
                                    println!(
                                        "  Server: {:.1}MB RAM, {:.1}% CPU",
                                        server["memory_usage_mb"].as_f64().unwrap_or(0.0),
                                        server["cpu_usage_percent"].as_f64().unwrap_or(0.0)
                                    );
                                }
                                if let Some(services) =
                                    backend_info.get("services").and_then(|s| s.as_array())
                                {
                                    println!("  Active Services: {}", services.len());
                                }
                                if let Some(activity) = backend_info.get("recent_activity") {
                                    println!(
                                        "  Recent API Calls: {}",
                                        activity.as_u64().unwrap_or(0)
                                    );
                                }
                            }
                        }
                    }

                    if detailed {
                        println!(
                            "\nDetails: {}",
                            serde_json::to_string_pretty(&status["additional_info"])?
                        );
                    }
                }
            } else if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "Agent not found",
                        "agent": agent_id,
                    }))?
                );
            } else {
                println!("❌ Agent '{}' not found", agent_id);
            }
        } else {
            // Show all agent statuses
            let statuses = status_tracker.get_all_statuses().await?;

            if self.json_output {
                println!("{}", serde_json::to_string_pretty(&statuses)?);
            } else {
                println!("📊 ccswarm Status");
                println!("================");

                if statuses.is_empty() {
                    println!("No agents found");
                } else {
                    for status in &statuses {
                        println!("Agent: {}", status["agent_id"]);
                        println!("  Status: {}", status["status"]);
                        println!("  Updated: {}", status["timestamp"]);

                        // Show role-specific summary for backend agents
                        if let Some(role) = status.get("role") {
                            if role.as_str() == Some("Backend") {
                                if let Some(backend_info) = status.get("backend_specific") {
                                    if let Some(api_health) = backend_info.get("api_health") {
                                        print!(
                                            "  API Health: {:.0}% | ",
                                            api_health.as_f64().unwrap_or(0.0) * 100.0
                                        );
                                    }
                                    if let Some(db) = backend_info.get("database") {
                                        print!(
                                            "DB: {} | ",
                                            if db["is_connected"].as_bool().unwrap_or(false) {
                                                "✓"
                                            } else {
                                                "✗"
                                            }
                                        );
                                    }
                                    if let Some(services) =
                                        backend_info.get("services").and_then(|s| s.as_array())
                                    {
                                        print!("Services: {}", services.len());
                                    }
                                    println!();
                                }
                            }
                        }

                        if detailed {
                            println!(
                                "  Details: {}",
                                serde_json::to_string_pretty(&status["additional_info"])?
                            );
                        }
                        println!();
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_worktree (lines 2279-2458) ---[0m
    async fn handle_worktree(&self, action: &WorktreeAction) -> Result<()> {
        let manager = crate::git::shell::ShellWorktreeManager::new(self.repo_path.clone())?;

        match action {
            WorktreeAction::List => {
                let worktrees = manager.list_worktrees().await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&worktrees)?);
                } else {
                    println!("🌳 Git Worktrees");
                    println!("===============");

                    for wt in &worktrees {
                        println!("Path: {}", wt.path.display());
                        println!("  Branch: {}", wt.branch);
                        println!("  Head: {}", wt.head_commit);
                        println!("  Locked: {}", wt.is_locked);
                        println!();
                    }
                }
            }
            WorktreeAction::Create {
                path,
                branch,
                new_branch,
            } => {
                let info = if *new_branch {
                    manager.create_worktree_full(path, branch, true).await?
                } else {
                    manager.create_worktree(path, branch).await?
                };

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&info)?);
                } else {
                    println!("✅ Worktree created");
                    println!("   Path: {}", info.path.display());
                    println!("   Branch: {}", info.branch);
                }
            }
            WorktreeAction::Remove { path, force } => {
                if *force {
                    manager.remove_worktree_full(path, true).await?
                } else {
                    manager.remove_worktree(path).await?
                };

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktree removed",
                            "path": path,
                        }))?
                    );
                } else {
                    println!("✅ Worktree removed: {}", path.display());
                }
            }
            WorktreeAction::Prune => {
                manager.prune_worktrees().await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktrees pruned",
                        }))?
                    );
                } else {
                    println!("✅ Stale worktrees pruned");
                }
            }
            WorktreeAction::Clean { force } => {
                use std::io::{self, Write};

                // Find all ccswarm-related worktrees
                let worktrees = manager.list_worktrees().await?;
                let ccswarm_worktrees: Vec<_> = worktrees
                    .iter()
                    .filter(|w| w.branch.contains("agent") || w.branch.contains("feature/"))
                    .collect();

                if ccswarm_worktrees.is_empty() {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "No ccswarm worktrees found",
                            }))?
                        );
                    } else {
                        println!("✅ No ccswarm worktrees to clean");
                    }
                    return Ok(());
                }

                // Ask for confirmation unless forced
                if !force {
                    println!("⚠️  Found {} ccswarm worktrees:", ccswarm_worktrees.len());
                    for w in &ccswarm_worktrees {
                        println!("   - {} ({})", w.path.display(), w.branch);
                    }
                    print!("\nAre you sure you want to remove all these worktrees? [y/N] ");
                    io::stdout().flush()?;

                    let mut response = String::new();
                    io::stdin().read_line(&mut response)?;

                    if !response.trim().eq_ignore_ascii_case("y") {
                        println!("❌ Cleanup cancelled");
                        return Ok(());
                    }
                }

                // Remove all ccswarm worktrees
                let mut removed_count = 0;
                for worktree in ccswarm_worktrees {
                    match manager.remove_worktree(&worktree.path).await {
                        Ok(_) => {
                            removed_count += 1;
                            if !self.json_output {
                                println!("   ✓ Removed {}", worktree.path.display());
                            }
                        }
                        Err(e) => {
                            if !self.json_output {
                                println!(
                                    "   ✗ Failed to remove {}: {}",
                                    worktree.path.display(),
                                    e
                                );
                            }
                        }
                    }
                }

                // Also clean up branches
                let output = tokio::process::Command::new("git")
                    .args(["branch", "--list", "*agent*", "feature/*"])
                    .output()
                    .await?;

                if output.status.success() {
                    let branches = String::from_utf8_lossy(&output.stdout);
                    let branch_count = branches.lines().count();

                    if branch_count > 0 {
                        tokio::process::Command::new("git")
                            .args(&["branch", "-D"])
                            .args(branches.lines().map(|b| b.trim().trim_start_matches("* ")))
                            .output()
                            .await?;
                    }
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Cleanup completed",
                            "worktrees_removed": removed_count,
                        }))?
                    );
                } else {
                    println!(
                        "\n✅ Cleanup completed: {} worktrees removed",
                        removed_count
                    );
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1406-1543 method show_status <-> ./crates/ccswarm/src/cli/mod.rs:3628-3804 method handle_sangha
  Similarity: 97.56%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_status (lines 1406-1543) ---[0m
    async fn show_status(&self, detailed: bool, agent: Option<&str>) -> Result<()> {
        // Read status from coordination files
        let status_tracker = crate::coordination::StatusTracker::new().await?;

        if let Some(agent_id) = agent {
            // Show specific agent status
            if let Some(status) = status_tracker.get_status(agent_id).await? {
                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&status)?);
                } else {
                    println!("Agent: {}", agent_id);
                    println!("Status: {}", status["status"]);
                    println!("Updated: {}", status["timestamp"]);

                    // Check if this is a backend agent and show backend-specific info
                    if let Some(role) = status.get("role") {
                        if role.as_str() == Some("Backend") {
                            if let Some(backend_info) = status.get("backend_specific") {
                                println!("\n🔧 Backend Status:");
                                if let Some(api_health) = backend_info.get("api_health") {
                                    println!(
                                        "  API Health: {:.1}%",
                                        api_health.as_f64().unwrap_or(0.0) * 100.0
                                    );
                                }
                                if let Some(db) = backend_info.get("database") {
                                    println!(
                                        "  Database: {} ({})",
                                        if db["is_connected"].as_bool().unwrap_or(false) {
                                            "Connected"
                                        } else {
                                            "Disconnected"
                                        },
                                        db["database_type"].as_str().unwrap_or("Unknown")
                                    );
                                }
                                if let Some(server) = backend_info.get("server") {
                                    println!(
                                        "  Server: {:.1}MB RAM, {:.1}% CPU",
                                        server["memory_usage_mb"].as_f64().unwrap_or(0.0),
                                        server["cpu_usage_percent"].as_f64().unwrap_or(0.0)
                                    );
                                }
                                if let Some(services) =
                                    backend_info.get("services").and_then(|s| s.as_array())
                                {
                                    println!("  Active Services: {}", services.len());
                                }
                                if let Some(activity) = backend_info.get("recent_activity") {
                                    println!(
                                        "  Recent API Calls: {}",
                                        activity.as_u64().unwrap_or(0)
                                    );
                                }
                            }
                        }
                    }

                    if detailed {
                        println!(
                            "\nDetails: {}",
                            serde_json::to_string_pretty(&status["additional_info"])?
                        );
                    }
                }
            } else if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "Agent not found",
                        "agent": agent_id,
                    }))?
                );
            } else {
                println!("❌ Agent '{}' not found", agent_id);
            }
        } else {
            // Show all agent statuses
            let statuses = status_tracker.get_all_statuses().await?;

            if self.json_output {
                println!("{}", serde_json::to_string_pretty(&statuses)?);
            } else {
                println!("📊 ccswarm Status");
                println!("================");

                if statuses.is_empty() {
                    println!("No agents found");
                } else {
                    for status in &statuses {
                        println!("Agent: {}", status["agent_id"]);
                        println!("  Status: {}", status["status"]);
                        println!("  Updated: {}", status["timestamp"]);

                        // Show role-specific summary for backend agents
                        if let Some(role) = status.get("role") {
                            if role.as_str() == Some("Backend") {
                                if let Some(backend_info) = status.get("backend_specific") {
                                    if let Some(api_health) = backend_info.get("api_health") {
                                        print!(
                                            "  API Health: {:.0}% | ",
                                            api_health.as_f64().unwrap_or(0.0) * 100.0
                                        );
                                    }
                                    if let Some(db) = backend_info.get("database") {
                                        print!(
                                            "DB: {} | ",
                                            if db["is_connected"].as_bool().unwrap_or(false) {
                                                "✓"
                                            } else {
                                                "✗"
                                            }
                                        );
                                    }
                                    if let Some(services) =
                                        backend_info.get("services").and_then(|s| s.as_array())
                                    {
                                        print!("Services: {}", services.len());
                                    }
                                    println!();
                                }
                            }
                        }

                        if detailed {
                            println!(
                                "  Details: {}",
                                serde_json::to_string_pretty(&status["additional_info"])?
                            );
                        }
                        println!();
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_sangha (lines 3628-3804) ---[0m
    async fn handle_sangha(&self, action: &SanghaAction) -> Result<()> {
        // Using stub implementation for now
        use crate::extension_stub::sangha::{Vote, VoteType};
        use std::str::FromStr;

        // Stub implementation - just log the action
        info!("🏛️  Sangha action requested (stub implementation)");

        match action {
            SanghaAction::Propose {
                proposal_type,
                file,
            } => {
                info!("📋 Submitting proposal to Sangha (stub implementation)");

                // Read proposal specification from file
                let _spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read proposal file")?;

                info!("Proposal type: {}", proposal_type);

                // Stub: Generate a fake proposal ID
                let proposal_id = uuid::Uuid::new_v4();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Proposal submitted",
                            "proposal_id": proposal_id,
                            "type": proposal_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!(
                        "📋 Submitting {} proposal from: {}",
                        proposal_type,
                        file.display()
                    );
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Proposal submitted successfully");
                }
            }

            SanghaAction::Vote {
                proposal_id,
                choice,
                reason,
            } => {
                info!("🗳️ Casting vote (stub implementation)");

                // Parse vote choice
                let vote_type = match choice.to_lowercase().as_str() {
                    "aye" | "yes" | "approve" => VoteType::Approve,
                    "nay" | "no" | "reject" => VoteType::Reject,
                    "abstain" => VoteType::Abstain,
                    "veto" | "needs_changes" => VoteType::NeedsChanges,
                    _ => {
                        anyhow::bail!(
                            "Invalid vote choice: {}. Use aye, nay, abstain, or veto",
                            choice
                        );
                    }
                };

                // Parse proposal ID
                let prop_id =
                    uuid::Uuid::from_str(proposal_id).context("Invalid proposal ID format")?;

                // Create vote (stub)
                let _vote = Vote::new("cli-user".to_string(), prop_id.to_string(), vote_type);

                info!("Vote created for proposal: {}", proposal_id);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Vote cast",
                            "proposal_id": proposal_id,
                            "choice": choice,
                            "reason": reason,
                        }))?
                    );
                } else {
                    println!("🗳️ Casting vote on proposal: {}", proposal_id);
                    println!("   Choice: {}", choice);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                    println!("✅ Vote cast successfully");
                }
            }

            SanghaAction::List { all, status } => {
                info!("📊 Listing Sangha status (stub implementation)");

                // Stub statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Sangha status retrieved (stub)",
                            "stats": {
                                "total_members": 1,
                                "active_members": 1,
                                "total_proposals": 0,
                                "active_proposals": 0,
                                "consensus_algorithm": "stub",
                            },
                            "show_all": all,
                            "filter_status": status,
                        }))?
                    );
                } else {
                    println!("📊 Sangha Status (Stub Implementation)");
                    println!("=====================================");
                    println!("👥 Members: 1 total, 1 active");
                    println!("📋 Proposals: 0 total, 0 active");
                    println!("🧠 Consensus Algorithm: stub");

                    println!(
                        "\n💡 No active proposals. Use 'ccswarm sangha propose' to create one."
                    );
                }
            }

            SanghaAction::Session { id, active } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session info",
                            "session_id": id,
                            "active": active,
                        }))?
                    );
                } else {
                    println!("🏛️ Sangha Session");
                    if *active {
                        println!("No active session");
                    }
                }
            }

            SanghaAction::ExtensionReview {
                proposal_id,
                technical_check,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension reviewed",
                            "proposal_id": proposal_id,
                            "technical_check": technical_check,
                        }))?
                    );
                } else {
                    println!("🔍 Reviewing extension proposal: {}", proposal_id);
                    if *technical_check {
                        println!("   Performing technical validation...");
                    }
                    println!("✅ Review completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2279-2458 method handle_worktree <-> ./crates/ccswarm/src/cli/mod.rs:2637-2770 method handle_delegate
  Similarity: 97.51%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_worktree (lines 2279-2458) ---[0m
    async fn handle_worktree(&self, action: &WorktreeAction) -> Result<()> {
        let manager = crate::git::shell::ShellWorktreeManager::new(self.repo_path.clone())?;

        match action {
            WorktreeAction::List => {
                let worktrees = manager.list_worktrees().await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&worktrees)?);
                } else {
                    println!("🌳 Git Worktrees");
                    println!("===============");

                    for wt in &worktrees {
                        println!("Path: {}", wt.path.display());
                        println!("  Branch: {}", wt.branch);
                        println!("  Head: {}", wt.head_commit);
                        println!("  Locked: {}", wt.is_locked);
                        println!();
                    }
                }
            }
            WorktreeAction::Create {
                path,
                branch,
                new_branch,
            } => {
                let info = if *new_branch {
                    manager.create_worktree_full(path, branch, true).await?
                } else {
                    manager.create_worktree(path, branch).await?
                };

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&info)?);
                } else {
                    println!("✅ Worktree created");
                    println!("   Path: {}", info.path.display());
                    println!("   Branch: {}", info.branch);
                }
            }
            WorktreeAction::Remove { path, force } => {
                if *force {
                    manager.remove_worktree_full(path, true).await?
                } else {
                    manager.remove_worktree(path).await?
                };

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktree removed",
                            "path": path,
                        }))?
                    );
                } else {
                    println!("✅ Worktree removed: {}", path.display());
                }
            }
            WorktreeAction::Prune => {
                manager.prune_worktrees().await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktrees pruned",
                        }))?
                    );
                } else {
                    println!("✅ Stale worktrees pruned");
                }
            }
            WorktreeAction::Clean { force } => {
                use std::io::{self, Write};

                // Find all ccswarm-related worktrees
                let worktrees = manager.list_worktrees().await?;
                let ccswarm_worktrees: Vec<_> = worktrees
                    .iter()
                    .filter(|w| w.branch.contains("agent") || w.branch.contains("feature/"))
                    .collect();

                if ccswarm_worktrees.is_empty() {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "No ccswarm worktrees found",
                            }))?
                        );
                    } else {
                        println!("✅ No ccswarm worktrees to clean");
                    }
                    return Ok(());
                }

                // Ask for confirmation unless forced
                if !force {
                    println!("⚠️  Found {} ccswarm worktrees:", ccswarm_worktrees.len());
                    for w in &ccswarm_worktrees {
                        println!("   - {} ({})", w.path.display(), w.branch);
                    }
                    print!("\nAre you sure you want to remove all these worktrees? [y/N] ");
                    io::stdout().flush()?;

                    let mut response = String::new();
                    io::stdin().read_line(&mut response)?;

                    if !response.trim().eq_ignore_ascii_case("y") {
                        println!("❌ Cleanup cancelled");
                        return Ok(());
                    }
                }

                // Remove all ccswarm worktrees
                let mut removed_count = 0;
                for worktree in ccswarm_worktrees {
                    match manager.remove_worktree(&worktree.path).await {
                        Ok(_) => {
                            removed_count += 1;
                            if !self.json_output {
                                println!("   ✓ Removed {}", worktree.path.display());
                            }
                        }
                        Err(e) => {
                            if !self.json_output {
                                println!(
                                    "   ✗ Failed to remove {}: {}",
                                    worktree.path.display(),
                                    e
                                );
                            }
                        }
                    }
                }

                // Also clean up branches
                let output = tokio::process::Command::new("git")
                    .args(["branch", "--list", "*agent*", "feature/*"])
                    .output()
                    .await?;

                if output.status.success() {
                    let branches = String::from_utf8_lossy(&output.stdout);
                    let branch_count = branches.lines().count();

                    if branch_count > 0 {
                        tokio::process::Command::new("git")
                            .args(&["branch", "-D"])
                            .args(branches.lines().map(|b| b.trim().trim_start_matches("* ")))
                            .output()
                            .await?;
                    }
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Cleanup completed",
                            "worktrees_removed": removed_count,
                        }))?
                    );
                } else {
                    println!(
                        "\n✅ Cleanup completed: {} worktrees removed",
                        removed_count
                    );
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_delegate (lines 2637-2770) ---[0m
    async fn handle_delegate(&self, action: &DelegateAction) -> Result<()> {
        use crate::orchestrator::master_delegation::{DelegationStrategy, MasterDelegationEngine};

        match action {
            DelegateAction::Task {
                description,
                agent,
                priority,
                task_type,
                details,
                force,
            } => {
                let task = self.create_task_from_args(
                    description,
                    priority,
                    task_type,
                    details.as_deref(),
                    None,
                )?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Task delegated",
                            "task": task,
                            "target_agent": agent,
                            "forced": force,
                        }))?
                    );
                } else {
                    println!("🎯 Delegating task to {} agent", agent);
                    println!("   Task: {}", task.description);
                    println!("   Priority: {:?}", task.priority);
                    println!("   Type: {:?}", task.task_type);
                    if *force {
                        println!("   ⚠️ Forced delegation");
                    }
                }
            }

            DelegateAction::Analyze {
                description,
                verbose,
                strategy,
            } => {
                let strategy = match strategy.as_str() {
                    "content" => DelegationStrategy::ContentBased,
                    "load" => DelegationStrategy::LoadBalanced,
                    "expertise" => DelegationStrategy::ExpertiseBased,
                    "workflow" => DelegationStrategy::WorkflowBased,
                    "hybrid" => DelegationStrategy::Hybrid,
                    _ => DelegationStrategy::Hybrid,
                };

                let mut engine = MasterDelegationEngine::new(strategy);
                let task = Task::new(
                    "analysis".to_string(),
                    description.clone(),
                    Priority::Medium,
                    TaskType::Development,
                );

                let decision = engine.delegate_task(task)?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&decision)?);
                } else {
                    println!("🔍 Task Analysis Results");
                    println!("   Task: {}", description);
                    println!("   Recommended Agent: {}", decision.target_agent.name());
                    println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                    if *verbose {
                        println!("   Reasoning: {}", decision.reasoning);
                        if let Some(duration) = decision.estimated_duration {
                            println!("   Estimated Duration: {:?}", duration);
                        }
                    }
                }
            }

            DelegateAction::Stats { detailed, period } => {
                // TODO: Implement delegation statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Delegation statistics",
                            "period_hours": period,
                            "detailed": detailed,
                        }))?
                    );
                } else {
                    println!("📊 Delegation Statistics (last {} hours)", period);
                    println!("   Feature not yet implemented");
                }
            }

            DelegateAction::Interactive => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "error": "Interactive mode not available in JSON output",
                        }))?
                    );
                } else {
                    println!("🖥️ Interactive Delegation Mode");
                    self.run_interactive_delegation().await?;
                }
            }

            DelegateAction::Show { file } => {
                let config = CcswarmConfig::from_file(file.clone()).await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&config)?);
                } else {
                    println!("📄 Delegation Configuration: {}", file.display());
                    println!("========================");
                    println!("Project: {}", config.project.name);
                    println!("Repository: {}", config.project.repository.url);
                    println!("Agents: {}", config.agents.len());
                    for (name, agent_config) in &config.agents {
                        println!("  - {}: {}", name, agent_config.specialization);
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2637-2770 method handle_delegate <-> ./crates/ccswarm/src/cli/mod.rs:3628-3804 method handle_sangha
  Similarity: 97.15%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_delegate (lines 2637-2770) ---[0m
    async fn handle_delegate(&self, action: &DelegateAction) -> Result<()> {
        use crate::orchestrator::master_delegation::{DelegationStrategy, MasterDelegationEngine};

        match action {
            DelegateAction::Task {
                description,
                agent,
                priority,
                task_type,
                details,
                force,
            } => {
                let task = self.create_task_from_args(
                    description,
                    priority,
                    task_type,
                    details.as_deref(),
                    None,
                )?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Task delegated",
                            "task": task,
                            "target_agent": agent,
                            "forced": force,
                        }))?
                    );
                } else {
                    println!("🎯 Delegating task to {} agent", agent);
                    println!("   Task: {}", task.description);
                    println!("   Priority: {:?}", task.priority);
                    println!("   Type: {:?}", task.task_type);
                    if *force {
                        println!("   ⚠️ Forced delegation");
                    }
                }
            }

            DelegateAction::Analyze {
                description,
                verbose,
                strategy,
            } => {
                let strategy = match strategy.as_str() {
                    "content" => DelegationStrategy::ContentBased,
                    "load" => DelegationStrategy::LoadBalanced,
                    "expertise" => DelegationStrategy::ExpertiseBased,
                    "workflow" => DelegationStrategy::WorkflowBased,
                    "hybrid" => DelegationStrategy::Hybrid,
                    _ => DelegationStrategy::Hybrid,
                };

                let mut engine = MasterDelegationEngine::new(strategy);
                let task = Task::new(
                    "analysis".to_string(),
                    description.clone(),
                    Priority::Medium,
                    TaskType::Development,
                );

                let decision = engine.delegate_task(task)?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&decision)?);
                } else {
                    println!("🔍 Task Analysis Results");
                    println!("   Task: {}", description);
                    println!("   Recommended Agent: {}", decision.target_agent.name());
                    println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                    if *verbose {
                        println!("   Reasoning: {}", decision.reasoning);
                        if let Some(duration) = decision.estimated_duration {
                            println!("   Estimated Duration: {:?}", duration);
                        }
                    }
                }
            }

            DelegateAction::Stats { detailed, period } => {
                // TODO: Implement delegation statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Delegation statistics",
                            "period_hours": period,
                            "detailed": detailed,
                        }))?
                    );
                } else {
                    println!("📊 Delegation Statistics (last {} hours)", period);
                    println!("   Feature not yet implemented");
                }
            }

            DelegateAction::Interactive => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "error": "Interactive mode not available in JSON output",
                        }))?
                    );
                } else {
                    println!("🖥️ Interactive Delegation Mode");
                    self.run_interactive_delegation().await?;
                }
            }

            DelegateAction::Show { file } => {
                let config = CcswarmConfig::from_file(file.clone()).await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&config)?);
                } else {
                    println!("📄 Delegation Configuration: {}", file.display());
                    println!("========================");
                    println!("Project: {}", config.project.name);
                    println!("Repository: {}", config.project.repository.url);
                    println!("Agents: {}", config.agents.len());
                    for (name, agent_config) in &config.agents {
                        println!("  - {}: {}", name, agent_config.specialization);
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_sangha (lines 3628-3804) ---[0m
    async fn handle_sangha(&self, action: &SanghaAction) -> Result<()> {
        // Using stub implementation for now
        use crate::extension_stub::sangha::{Vote, VoteType};
        use std::str::FromStr;

        // Stub implementation - just log the action
        info!("🏛️  Sangha action requested (stub implementation)");

        match action {
            SanghaAction::Propose {
                proposal_type,
                file,
            } => {
                info!("📋 Submitting proposal to Sangha (stub implementation)");

                // Read proposal specification from file
                let _spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read proposal file")?;

                info!("Proposal type: {}", proposal_type);

                // Stub: Generate a fake proposal ID
                let proposal_id = uuid::Uuid::new_v4();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Proposal submitted",
                            "proposal_id": proposal_id,
                            "type": proposal_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!(
                        "📋 Submitting {} proposal from: {}",
                        proposal_type,
                        file.display()
                    );
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Proposal submitted successfully");
                }
            }

            SanghaAction::Vote {
                proposal_id,
                choice,
                reason,
            } => {
                info!("🗳️ Casting vote (stub implementation)");

                // Parse vote choice
                let vote_type = match choice.to_lowercase().as_str() {
                    "aye" | "yes" | "approve" => VoteType::Approve,
                    "nay" | "no" | "reject" => VoteType::Reject,
                    "abstain" => VoteType::Abstain,
                    "veto" | "needs_changes" => VoteType::NeedsChanges,
                    _ => {
                        anyhow::bail!(
                            "Invalid vote choice: {}. Use aye, nay, abstain, or veto",
                            choice
                        );
                    }
                };

                // Parse proposal ID
                let prop_id =
                    uuid::Uuid::from_str(proposal_id).context("Invalid proposal ID format")?;

                // Create vote (stub)
                let _vote = Vote::new("cli-user".to_string(), prop_id.to_string(), vote_type);

                info!("Vote created for proposal: {}", proposal_id);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Vote cast",
                            "proposal_id": proposal_id,
                            "choice": choice,
                            "reason": reason,
                        }))?
                    );
                } else {
                    println!("🗳️ Casting vote on proposal: {}", proposal_id);
                    println!("   Choice: {}", choice);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                    println!("✅ Vote cast successfully");
                }
            }

            SanghaAction::List { all, status } => {
                info!("📊 Listing Sangha status (stub implementation)");

                // Stub statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Sangha status retrieved (stub)",
                            "stats": {
                                "total_members": 1,
                                "active_members": 1,
                                "total_proposals": 0,
                                "active_proposals": 0,
                                "consensus_algorithm": "stub",
                            },
                            "show_all": all,
                            "filter_status": status,
                        }))?
                    );
                } else {
                    println!("📊 Sangha Status (Stub Implementation)");
                    println!("=====================================");
                    println!("👥 Members: 1 total, 1 active");
                    println!("📋 Proposals: 0 total, 0 active");
                    println!("🧠 Consensus Algorithm: stub");

                    println!(
                        "\n💡 No active proposals. Use 'ccswarm sangha propose' to create one."
                    );
                }
            }

            SanghaAction::Session { id, active } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session info",
                            "session_id": id,
                            "active": active,
                        }))?
                    );
                } else {
                    println!("🏛️ Sangha Session");
                    if *active {
                        println!("No active session");
                    }
                }
            }

            SanghaAction::ExtensionReview {
                proposal_id,
                technical_check,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension reviewed",
                            "proposal_id": proposal_id,
                            "technical_check": technical_check,
                        }))?
                    );
                } else {
                    println!("🔍 Reviewing extension proposal: {}", proposal_id);
                    if *technical_check {
                        println!("   Performing technical validation...");
                    }
                    println!("✅ Review completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1406-1543 method show_status <-> ./crates/ccswarm/src/cli/mod.rs:4173-4336 method handle_search
  Similarity: 98.44%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_status (lines 1406-1543) ---[0m
    async fn show_status(&self, detailed: bool, agent: Option<&str>) -> Result<()> {
        // Read status from coordination files
        let status_tracker = crate::coordination::StatusTracker::new().await?;

        if let Some(agent_id) = agent {
            // Show specific agent status
            if let Some(status) = status_tracker.get_status(agent_id).await? {
                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&status)?);
                } else {
                    println!("Agent: {}", agent_id);
                    println!("Status: {}", status["status"]);
                    println!("Updated: {}", status["timestamp"]);

                    // Check if this is a backend agent and show backend-specific info
                    if let Some(role) = status.get("role") {
                        if role.as_str() == Some("Backend") {
                            if let Some(backend_info) = status.get("backend_specific") {
                                println!("\n🔧 Backend Status:");
                                if let Some(api_health) = backend_info.get("api_health") {
                                    println!(
                                        "  API Health: {:.1}%",
                                        api_health.as_f64().unwrap_or(0.0) * 100.0
                                    );
                                }
                                if let Some(db) = backend_info.get("database") {
                                    println!(
                                        "  Database: {} ({})",
                                        if db["is_connected"].as_bool().unwrap_or(false) {
                                            "Connected"
                                        } else {
                                            "Disconnected"
                                        },
                                        db["database_type"].as_str().unwrap_or("Unknown")
                                    );
                                }
                                if let Some(server) = backend_info.get("server") {
                                    println!(
                                        "  Server: {:.1}MB RAM, {:.1}% CPU",
                                        server["memory_usage_mb"].as_f64().unwrap_or(0.0),
                                        server["cpu_usage_percent"].as_f64().unwrap_or(0.0)
                                    );
                                }
                                if let Some(services) =
                                    backend_info.get("services").and_then(|s| s.as_array())
                                {
                                    println!("  Active Services: {}", services.len());
                                }
                                if let Some(activity) = backend_info.get("recent_activity") {
                                    println!(
                                        "  Recent API Calls: {}",
                                        activity.as_u64().unwrap_or(0)
                                    );
                                }
                            }
                        }
                    }

                    if detailed {
                        println!(
                            "\nDetails: {}",
                            serde_json::to_string_pretty(&status["additional_info"])?
                        );
                    }
                }
            } else if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "Agent not found",
                        "agent": agent_id,
                    }))?
                );
            } else {
                println!("❌ Agent '{}' not found", agent_id);
            }
        } else {
            // Show all agent statuses
            let statuses = status_tracker.get_all_statuses().await?;

            if self.json_output {
                println!("{}", serde_json::to_string_pretty(&statuses)?);
            } else {
                println!("📊 ccswarm Status");
                println!("================");

                if statuses.is_empty() {
                    println!("No agents found");
                } else {
                    for status in &statuses {
                        println!("Agent: {}", status["agent_id"]);
                        println!("  Status: {}", status["status"]);
                        println!("  Updated: {}", status["timestamp"]);

                        // Show role-specific summary for backend agents
                        if let Some(role) = status.get("role") {
                            if role.as_str() == Some("Backend") {
                                if let Some(backend_info) = status.get("backend_specific") {
                                    if let Some(api_health) = backend_info.get("api_health") {
                                        print!(
                                            "  API Health: {:.0}% | ",
                                            api_health.as_f64().unwrap_or(0.0) * 100.0
                                        );
                                    }
                                    if let Some(db) = backend_info.get("database") {
                                        print!(
                                            "DB: {} | ",
                                            if db["is_connected"].as_bool().unwrap_or(false) {
                                                "✓"
                                            } else {
                                                "✗"
                                            }
                                        );
                                    }
                                    if let Some(services) =
                                        backend_info.get("services").and_then(|s| s.as_array())
                                    {
                                        print!("Services: {}", services.len());
                                    }
                                    println!();
                                }
                            }
                        }

                        if detailed {
                            println!(
                                "  Details: {}",
                                serde_json::to_string_pretty(&status["additional_info"])?
                            );
                        }
                        println!();
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_search (lines 4173-4336) ---[0m
    async fn handle_search(&self, action: &SearchAction) -> Result<()> {
        use crate::extension_stub::agent_extension::{
            DocumentationSearchStrategy, GitHubSearchStrategy, SearchContext, SearchFilters,
            SearchQuery, SearchStrategy, StackOverflowSearchStrategy,
        };

        match action {
            SearchAction::Docs {
                query,
                source,
                limit,
            } => {
                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: Some("rust".to_string()),
                        framework: Some("general".to_string()),
                        date_range: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec![source.clone()],
                        relevance_threshold: 0.5,
                    }),
                };

                let mut all_results = Vec::new();

                if source == "all" || source == "mdn" {
                    let mdn_strategy = DocumentationSearchStrategy::new();
                    match mdn_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("MDN search failed: {}", e),
                    }
                }

                if source == "all" || source == "github" {
                    let github_strategy = GitHubSearchStrategy::new();
                    match github_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("GitHub search failed: {}", e),
                    }
                }

                if source == "all" || source == "stackoverflow" {
                    let stackoverflow_strategy = StackOverflowSearchStrategy;
                    match stackoverflow_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("StackOverflow search failed: {}", e),
                    }
                }

                all_results.sort_by(|a, b| {
                    b.relevance_score
                        .partial_cmp(&a.relevance_score)
                        .unwrap_or(std::cmp::Ordering::Equal)
                });
                all_results.truncate(*limit);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "query": query,
                            "source": source,
                            "results": all_results.iter().map(|r| serde_json::json!({
                                "title": r.title,
                                "url": r.url,
                                "snippet": r.snippet,
                                "source": r.source,
                                "relevance_score": r.relevance_score,
                            })).collect::<Vec<_>>()
                        }))?
                    );
                } else {
                    println!("🔍 Searching documentation for: {}", query);
                    println!("   Source: {}", source);
                    println!("   Found {} results", all_results.len());
                    println!();

                    for (i, result) in all_results.iter().enumerate() {
                        println!("{}. {} ({})", i + 1, result.title, result.source);
                        println!("   🔗 {}", result.url);
                        println!("   📝 {}", result.snippet);
                        println!("   ⭐ Relevance: {:.2}", result.relevance_score);
                        println!();
                    }
                }
            }

            SearchAction::Test { query } => {
                println!("🧪 Testing search functionality with query: {}", query);

                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: None,
                        framework: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec!["all".to_string()],
                        relevance_threshold: 0.5,
                        date_range: None,
                    }),
                };

                // Test each search strategy
                println!("🔍 Testing MDN search...");
                let mdn_strategy = DocumentationSearchStrategy::new();
                match mdn_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ MDN: Found {} results", results.len()),
                    Err(e) => println!("❌ MDN: Error - {}", e),
                }

                println!("🔍 Testing GitHub search...");
                let github_strategy = GitHubSearchStrategy::new();
                match github_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ GitHub: Found {} results", results.len()),
                    Err(e) => println!("❌ GitHub: Error - {}", e),
                }

                println!("🔍 Testing StackOverflow search...");
                let stackoverflow_strategy = StackOverflowSearchStrategy;
                match stackoverflow_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ StackOverflow: Found {} results", results.len()),
                    Err(e) => println!("❌ StackOverflow: Error - {}", e),
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Search test completed",
                            "query": query,
                        }))?
                    );
                } else {
                    println!("✅ Search functionality test completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2637-2770 method handle_delegate <-> ./crates/ccswarm/src/cli/mod.rs:4173-4336 method handle_search
  Similarity: 96.98%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_delegate (lines 2637-2770) ---[0m
    async fn handle_delegate(&self, action: &DelegateAction) -> Result<()> {
        use crate::orchestrator::master_delegation::{DelegationStrategy, MasterDelegationEngine};

        match action {
            DelegateAction::Task {
                description,
                agent,
                priority,
                task_type,
                details,
                force,
            } => {
                let task = self.create_task_from_args(
                    description,
                    priority,
                    task_type,
                    details.as_deref(),
                    None,
                )?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Task delegated",
                            "task": task,
                            "target_agent": agent,
                            "forced": force,
                        }))?
                    );
                } else {
                    println!("🎯 Delegating task to {} agent", agent);
                    println!("   Task: {}", task.description);
                    println!("   Priority: {:?}", task.priority);
                    println!("   Type: {:?}", task.task_type);
                    if *force {
                        println!("   ⚠️ Forced delegation");
                    }
                }
            }

            DelegateAction::Analyze {
                description,
                verbose,
                strategy,
            } => {
                let strategy = match strategy.as_str() {
                    "content" => DelegationStrategy::ContentBased,
                    "load" => DelegationStrategy::LoadBalanced,
                    "expertise" => DelegationStrategy::ExpertiseBased,
                    "workflow" => DelegationStrategy::WorkflowBased,
                    "hybrid" => DelegationStrategy::Hybrid,
                    _ => DelegationStrategy::Hybrid,
                };

                let mut engine = MasterDelegationEngine::new(strategy);
                let task = Task::new(
                    "analysis".to_string(),
                    description.clone(),
                    Priority::Medium,
                    TaskType::Development,
                );

                let decision = engine.delegate_task(task)?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&decision)?);
                } else {
                    println!("🔍 Task Analysis Results");
                    println!("   Task: {}", description);
                    println!("   Recommended Agent: {}", decision.target_agent.name());
                    println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                    if *verbose {
                        println!("   Reasoning: {}", decision.reasoning);
                        if let Some(duration) = decision.estimated_duration {
                            println!("   Estimated Duration: {:?}", duration);
                        }
                    }
                }
            }

            DelegateAction::Stats { detailed, period } => {
                // TODO: Implement delegation statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Delegation statistics",
                            "period_hours": period,
                            "detailed": detailed,
                        }))?
                    );
                } else {
                    println!("📊 Delegation Statistics (last {} hours)", period);
                    println!("   Feature not yet implemented");
                }
            }

            DelegateAction::Interactive => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "error": "Interactive mode not available in JSON output",
                        }))?
                    );
                } else {
                    println!("🖥️ Interactive Delegation Mode");
                    self.run_interactive_delegation().await?;
                }
            }

            DelegateAction::Show { file } => {
                let config = CcswarmConfig::from_file(file.clone()).await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&config)?);
                } else {
                    println!("📄 Delegation Configuration: {}", file.display());
                    println!("========================");
                    println!("Project: {}", config.project.name);
                    println!("Repository: {}", config.project.repository.url);
                    println!("Agents: {}", config.agents.len());
                    for (name, agent_config) in &config.agents {
                        println!("  - {}: {}", name, agent_config.specialization);
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_search (lines 4173-4336) ---[0m
    async fn handle_search(&self, action: &SearchAction) -> Result<()> {
        use crate::extension_stub::agent_extension::{
            DocumentationSearchStrategy, GitHubSearchStrategy, SearchContext, SearchFilters,
            SearchQuery, SearchStrategy, StackOverflowSearchStrategy,
        };

        match action {
            SearchAction::Docs {
                query,
                source,
                limit,
            } => {
                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: Some("rust".to_string()),
                        framework: Some("general".to_string()),
                        date_range: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec![source.clone()],
                        relevance_threshold: 0.5,
                    }),
                };

                let mut all_results = Vec::new();

                if source == "all" || source == "mdn" {
                    let mdn_strategy = DocumentationSearchStrategy::new();
                    match mdn_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("MDN search failed: {}", e),
                    }
                }

                if source == "all" || source == "github" {
                    let github_strategy = GitHubSearchStrategy::new();
                    match github_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("GitHub search failed: {}", e),
                    }
                }

                if source == "all" || source == "stackoverflow" {
                    let stackoverflow_strategy = StackOverflowSearchStrategy;
                    match stackoverflow_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("StackOverflow search failed: {}", e),
                    }
                }

                all_results.sort_by(|a, b| {
                    b.relevance_score
                        .partial_cmp(&a.relevance_score)
                        .unwrap_or(std::cmp::Ordering::Equal)
                });
                all_results.truncate(*limit);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "query": query,
                            "source": source,
                            "results": all_results.iter().map(|r| serde_json::json!({
                                "title": r.title,
                                "url": r.url,
                                "snippet": r.snippet,
                                "source": r.source,
                                "relevance_score": r.relevance_score,
                            })).collect::<Vec<_>>()
                        }))?
                    );
                } else {
                    println!("🔍 Searching documentation for: {}", query);
                    println!("   Source: {}", source);
                    println!("   Found {} results", all_results.len());
                    println!();

                    for (i, result) in all_results.iter().enumerate() {
                        println!("{}. {} ({})", i + 1, result.title, result.source);
                        println!("   🔗 {}", result.url);
                        println!("   📝 {}", result.snippet);
                        println!("   ⭐ Relevance: {:.2}", result.relevance_score);
                        println!();
                    }
                }
            }

            SearchAction::Test { query } => {
                println!("🧪 Testing search functionality with query: {}", query);

                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: None,
                        framework: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec!["all".to_string()],
                        relevance_threshold: 0.5,
                        date_range: None,
                    }),
                };

                // Test each search strategy
                println!("🔍 Testing MDN search...");
                let mdn_strategy = DocumentationSearchStrategy::new();
                match mdn_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ MDN: Found {} results", results.len()),
                    Err(e) => println!("❌ MDN: Error - {}", e),
                }

                println!("🔍 Testing GitHub search...");
                let github_strategy = GitHubSearchStrategy::new();
                match github_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ GitHub: Found {} results", results.len()),
                    Err(e) => println!("❌ GitHub: Error - {}", e),
                }

                println!("🔍 Testing StackOverflow search...");
                let stackoverflow_strategy = StackOverflowSearchStrategy;
                match stackoverflow_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ StackOverflow: Found {} results", results.len()),
                    Err(e) => println!("❌ StackOverflow: Error - {}", e),
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Search test completed",
                            "query": query,
                        }))?
                    );
                } else {
                    println!("✅ Search functionality test completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2279-2458 method handle_worktree <-> ./crates/ccswarm/src/cli/mod.rs:2772-2884 method run_interactive_delegation
  Similarity: 96.94%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_worktree (lines 2279-2458) ---[0m
    async fn handle_worktree(&self, action: &WorktreeAction) -> Result<()> {
        let manager = crate::git::shell::ShellWorktreeManager::new(self.repo_path.clone())?;

        match action {
            WorktreeAction::List => {
                let worktrees = manager.list_worktrees().await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&worktrees)?);
                } else {
                    println!("🌳 Git Worktrees");
                    println!("===============");

                    for wt in &worktrees {
                        println!("Path: {}", wt.path.display());
                        println!("  Branch: {}", wt.branch);
                        println!("  Head: {}", wt.head_commit);
                        println!("  Locked: {}", wt.is_locked);
                        println!();
                    }
                }
            }
            WorktreeAction::Create {
                path,
                branch,
                new_branch,
            } => {
                let info = if *new_branch {
                    manager.create_worktree_full(path, branch, true).await?
                } else {
                    manager.create_worktree(path, branch).await?
                };

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&info)?);
                } else {
                    println!("✅ Worktree created");
                    println!("   Path: {}", info.path.display());
                    println!("   Branch: {}", info.branch);
                }
            }
            WorktreeAction::Remove { path, force } => {
                if *force {
                    manager.remove_worktree_full(path, true).await?
                } else {
                    manager.remove_worktree(path).await?
                };

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktree removed",
                            "path": path,
                        }))?
                    );
                } else {
                    println!("✅ Worktree removed: {}", path.display());
                }
            }
            WorktreeAction::Prune => {
                manager.prune_worktrees().await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktrees pruned",
                        }))?
                    );
                } else {
                    println!("✅ Stale worktrees pruned");
                }
            }
            WorktreeAction::Clean { force } => {
                use std::io::{self, Write};

                // Find all ccswarm-related worktrees
                let worktrees = manager.list_worktrees().await?;
                let ccswarm_worktrees: Vec<_> = worktrees
                    .iter()
                    .filter(|w| w.branch.contains("agent") || w.branch.contains("feature/"))
                    .collect();

                if ccswarm_worktrees.is_empty() {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "No ccswarm worktrees found",
                            }))?
                        );
                    } else {
                        println!("✅ No ccswarm worktrees to clean");
                    }
                    return Ok(());
                }

                // Ask for confirmation unless forced
                if !force {
                    println!("⚠️  Found {} ccswarm worktrees:", ccswarm_worktrees.len());
                    for w in &ccswarm_worktrees {
                        println!("   - {} ({})", w.path.display(), w.branch);
                    }
                    print!("\nAre you sure you want to remove all these worktrees? [y/N] ");
                    io::stdout().flush()?;

                    let mut response = String::new();
                    io::stdin().read_line(&mut response)?;

                    if !response.trim().eq_ignore_ascii_case("y") {
                        println!("❌ Cleanup cancelled");
                        return Ok(());
                    }
                }

                // Remove all ccswarm worktrees
                let mut removed_count = 0;
                for worktree in ccswarm_worktrees {
                    match manager.remove_worktree(&worktree.path).await {
                        Ok(_) => {
                            removed_count += 1;
                            if !self.json_output {
                                println!("   ✓ Removed {}", worktree.path.display());
                            }
                        }
                        Err(e) => {
                            if !self.json_output {
                                println!(
                                    "   ✗ Failed to remove {}: {}",
                                    worktree.path.display(),
                                    e
                                );
                            }
                        }
                    }
                }

                // Also clean up branches
                let output = tokio::process::Command::new("git")
                    .args(["branch", "--list", "*agent*", "feature/*"])
                    .output()
                    .await?;

                if output.status.success() {
                    let branches = String::from_utf8_lossy(&output.stdout);
                    let branch_count = branches.lines().count();

                    if branch_count > 0 {
                        tokio::process::Command::new("git")
                            .args(&["branch", "-D"])
                            .args(branches.lines().map(|b| b.trim().trim_start_matches("* ")))
                            .output()
                            .await?;
                    }
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Cleanup completed",
                            "worktrees_removed": removed_count,
                        }))?
                    );
                } else {
                    println!(
                        "\n✅ Cleanup completed: {} worktrees removed",
                        removed_count
                    );
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_interactive_delegation (lines 2772-2884) ---[0m
    async fn run_interactive_delegation(&self) -> Result<()> {
        use std::io::{self, Write};

        println!("🎯 Welcome to Interactive Delegation Mode");
        println!("   Type 'help' for commands, 'quit' to exit");
        println!();

        loop {
            print!("ccswarm> ");
            io::stdout().flush()?;

            let mut input = String::new();
            io::stdin().read_line(&mut input)?;
            let input = input.trim();

            match input {
                "quit" | "exit" => {
                    println!("👋 Goodbye!");
                    break;
                }
                "help" => {
                    println!("📚 Interactive Delegation Commands:");
                    println!("   analyze <task_description>  - Analyze task and suggest agent");
                    println!("   delegate <agent> <task>     - Delegate task to specific agent");
                    println!("   stats                       - Show delegation statistics");
                    println!("   agents                      - List available agents");
                    println!("   quit                        - Exit interactive mode");
                    println!();
                }
                "stats" => {
                    println!("📊 Delegation Statistics");
                    println!("   Feature not yet implemented");
                    println!();
                }
                "agents" => {
                    println!("🤖 Available Agents:");
                    println!("   • Frontend - React/TypeScript UI development");
                    println!("   • Backend - Node.js/Express API development");
                    println!("   • DevOps - Infrastructure and deployment");
                    println!("   • QA - Testing and quality assurance");
                    println!();
                }
                _ if input.starts_with("analyze ") => {
                    let task_desc = &input[8..];
                    if !task_desc.is_empty() {
                        // Directly call delegation analysis to avoid recursion
                        use crate::orchestrator::master_delegation::{
                            DelegationStrategy, MasterDelegationEngine,
                        };
                        let mut engine = MasterDelegationEngine::new(DelegationStrategy::Hybrid);
                        let task = Task::new(
                            "interactive-analysis".to_string(),
                            task_desc.to_string(),
                            Priority::Medium,
                            TaskType::Development,
                        );

                        match engine.delegate_task(task) {
                            Ok(decision) => {
                                println!("🔍 Task Analysis Results");
                                println!("   Task: {}", task_desc);
                                println!("   Recommended Agent: {}", decision.target_agent.name());
                                println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                                println!("   Reasoning: {}", decision.reasoning);
                                if let Some(duration) = decision.estimated_duration {
                                    println!("   Estimated Duration: {} seconds", duration);
                                }
                            }
                            Err(e) => {
                                println!("❌ Analysis failed: {}", e);
                            }
                        }
                        println!();
                    } else {
                        println!("❌ Please provide a task description");
                        println!("   Example: analyze Create login form with validation");
                        println!();
                    }
                }
                _ if input.starts_with("delegate ") => {
                    let parts: Vec<&str> = input[9..].splitn(2, ' ').collect();
                    if parts.len() == 2 {
                        let agent = parts[0];
                        let task_desc = parts[1];

                        if ["frontend", "backend", "devops", "qa"].contains(&agent) {
                            println!("🎯 Delegating '{}' to {} agent", task_desc, agent);
                            println!("   ✅ Task queued for delegation");
                            println!();
                        } else {
                            println!("❌ Unknown agent: {}", agent);
                            println!("   Available: frontend, backend, devops, qa");
                            println!();
                        }
                    } else {
                        println!("❌ Usage: delegate <agent> <task_description>");
                        println!("   Example: delegate frontend Create responsive navigation bar");
                        println!();
                    }
                }
                "" => {
                    // Empty input, continue
                }
                _ => {
                    println!("❓ Unknown command: {}", input);
                    println!("   Type 'help' for available commands");
                    println!();
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2772-2884 method run_interactive_delegation <-> ./crates/ccswarm/src/cli/mod.rs:3628-3804 method handle_sangha
  Similarity: 97.71%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_interactive_delegation (lines 2772-2884) ---[0m
    async fn run_interactive_delegation(&self) -> Result<()> {
        use std::io::{self, Write};

        println!("🎯 Welcome to Interactive Delegation Mode");
        println!("   Type 'help' for commands, 'quit' to exit");
        println!();

        loop {
            print!("ccswarm> ");
            io::stdout().flush()?;

            let mut input = String::new();
            io::stdin().read_line(&mut input)?;
            let input = input.trim();

            match input {
                "quit" | "exit" => {
                    println!("👋 Goodbye!");
                    break;
                }
                "help" => {
                    println!("📚 Interactive Delegation Commands:");
                    println!("   analyze <task_description>  - Analyze task and suggest agent");
                    println!("   delegate <agent> <task>     - Delegate task to specific agent");
                    println!("   stats                       - Show delegation statistics");
                    println!("   agents                      - List available agents");
                    println!("   quit                        - Exit interactive mode");
                    println!();
                }
                "stats" => {
                    println!("📊 Delegation Statistics");
                    println!("   Feature not yet implemented");
                    println!();
                }
                "agents" => {
                    println!("🤖 Available Agents:");
                    println!("   • Frontend - React/TypeScript UI development");
                    println!("   • Backend - Node.js/Express API development");
                    println!("   • DevOps - Infrastructure and deployment");
                    println!("   • QA - Testing and quality assurance");
                    println!();
                }
                _ if input.starts_with("analyze ") => {
                    let task_desc = &input[8..];
                    if !task_desc.is_empty() {
                        // Directly call delegation analysis to avoid recursion
                        use crate::orchestrator::master_delegation::{
                            DelegationStrategy, MasterDelegationEngine,
                        };
                        let mut engine = MasterDelegationEngine::new(DelegationStrategy::Hybrid);
                        let task = Task::new(
                            "interactive-analysis".to_string(),
                            task_desc.to_string(),
                            Priority::Medium,
                            TaskType::Development,
                        );

                        match engine.delegate_task(task) {
                            Ok(decision) => {
                                println!("🔍 Task Analysis Results");
                                println!("   Task: {}", task_desc);
                                println!("   Recommended Agent: {}", decision.target_agent.name());
                                println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                                println!("   Reasoning: {}", decision.reasoning);
                                if let Some(duration) = decision.estimated_duration {
                                    println!("   Estimated Duration: {} seconds", duration);
                                }
                            }
                            Err(e) => {
                                println!("❌ Analysis failed: {}", e);
                            }
                        }
                        println!();
                    } else {
                        println!("❌ Please provide a task description");
                        println!("   Example: analyze Create login form with validation");
                        println!();
                    }
                }
                _ if input.starts_with("delegate ") => {
                    let parts: Vec<&str> = input[9..].splitn(2, ' ').collect();
                    if parts.len() == 2 {
                        let agent = parts[0];
                        let task_desc = parts[1];

                        if ["frontend", "backend", "devops", "qa"].contains(&agent) {
                            println!("🎯 Delegating '{}' to {} agent", task_desc, agent);
                            println!("   ✅ Task queued for delegation");
                            println!();
                        } else {
                            println!("❌ Unknown agent: {}", agent);
                            println!("   Available: frontend, backend, devops, qa");
                            println!();
                        }
                    } else {
                        println!("❌ Usage: delegate <agent> <task_description>");
                        println!("   Example: delegate frontend Create responsive navigation bar");
                        println!();
                    }
                }
                "" => {
                    // Empty input, continue
                }
                _ => {
                    println!("❓ Unknown command: {}", input);
                    println!("   Type 'help' for available commands");
                    println!();
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_sangha (lines 3628-3804) ---[0m
    async fn handle_sangha(&self, action: &SanghaAction) -> Result<()> {
        // Using stub implementation for now
        use crate::extension_stub::sangha::{Vote, VoteType};
        use std::str::FromStr;

        // Stub implementation - just log the action
        info!("🏛️  Sangha action requested (stub implementation)");

        match action {
            SanghaAction::Propose {
                proposal_type,
                file,
            } => {
                info!("📋 Submitting proposal to Sangha (stub implementation)");

                // Read proposal specification from file
                let _spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read proposal file")?;

                info!("Proposal type: {}", proposal_type);

                // Stub: Generate a fake proposal ID
                let proposal_id = uuid::Uuid::new_v4();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Proposal submitted",
                            "proposal_id": proposal_id,
                            "type": proposal_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!(
                        "📋 Submitting {} proposal from: {}",
                        proposal_type,
                        file.display()
                    );
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Proposal submitted successfully");
                }
            }

            SanghaAction::Vote {
                proposal_id,
                choice,
                reason,
            } => {
                info!("🗳️ Casting vote (stub implementation)");

                // Parse vote choice
                let vote_type = match choice.to_lowercase().as_str() {
                    "aye" | "yes" | "approve" => VoteType::Approve,
                    "nay" | "no" | "reject" => VoteType::Reject,
                    "abstain" => VoteType::Abstain,
                    "veto" | "needs_changes" => VoteType::NeedsChanges,
                    _ => {
                        anyhow::bail!(
                            "Invalid vote choice: {}. Use aye, nay, abstain, or veto",
                            choice
                        );
                    }
                };

                // Parse proposal ID
                let prop_id =
                    uuid::Uuid::from_str(proposal_id).context("Invalid proposal ID format")?;

                // Create vote (stub)
                let _vote = Vote::new("cli-user".to_string(), prop_id.to_string(), vote_type);

                info!("Vote created for proposal: {}", proposal_id);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Vote cast",
                            "proposal_id": proposal_id,
                            "choice": choice,
                            "reason": reason,
                        }))?
                    );
                } else {
                    println!("🗳️ Casting vote on proposal: {}", proposal_id);
                    println!("   Choice: {}", choice);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                    println!("✅ Vote cast successfully");
                }
            }

            SanghaAction::List { all, status } => {
                info!("📊 Listing Sangha status (stub implementation)");

                // Stub statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Sangha status retrieved (stub)",
                            "stats": {
                                "total_members": 1,
                                "active_members": 1,
                                "total_proposals": 0,
                                "active_proposals": 0,
                                "consensus_algorithm": "stub",
                            },
                            "show_all": all,
                            "filter_status": status,
                        }))?
                    );
                } else {
                    println!("📊 Sangha Status (Stub Implementation)");
                    println!("=====================================");
                    println!("👥 Members: 1 total, 1 active");
                    println!("📋 Proposals: 0 total, 0 active");
                    println!("🧠 Consensus Algorithm: stub");

                    println!(
                        "\n💡 No active proposals. Use 'ccswarm sangha propose' to create one."
                    );
                }
            }

            SanghaAction::Session { id, active } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session info",
                            "session_id": id,
                            "active": active,
                        }))?
                    );
                } else {
                    println!("🏛️ Sangha Session");
                    if *active {
                        println!("No active session");
                    }
                }
            }

            SanghaAction::ExtensionReview {
                proposal_id,
                technical_check,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension reviewed",
                            "proposal_id": proposal_id,
                            "technical_check": technical_check,
                        }))?
                    );
                } else {
                    println!("🔍 Reviewing extension proposal: {}", proposal_id);
                    if *technical_check {
                        println!("   Performing technical validation...");
                    }
                    println!("✅ Review completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2130-2238 method show_task_stats <-> ./crates/ccswarm/src/cli/mod.rs:3628-3804 method handle_sangha
  Similarity: 95.68%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_stats (lines 2130-2238) ---[0m
    async fn show_task_stats(&self, detailed: bool, performance: bool) -> Result<()> {
        if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "queue_stats": queue_stats,
                        "execution_stats": execution_stats,
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("📊 Task Queue Statistics");
                println!("========================");
                println!("⏳ Pending: {}", queue_stats.pending_count);
                println!("🏃 In Progress: {}", queue_stats.active_count);
                println!("✅ Completed: {}", queue_stats.completed_count);
                println!("❌ Failed: {}", queue_stats.failed_count);
                println!("📋 Total: {}", queue_stats.total_count);

                if performance || detailed {
                    println!();
                    println!("🎯 Execution Statistics");
                    println!("=======================");
                    println!("Tasks Executed: {}", execution_stats.tasks_executed);
                    println!(
                        "Success Rate: {:.1}%",
                        if execution_stats.tasks_executed > 0 {
                            (execution_stats.tasks_succeeded as f64
                                / execution_stats.tasks_executed as f64)
                                * 100.0
                        } else {
                            0.0
                        }
                    );
                    println!(
                        "Average Duration: {:.2}s",
                        execution_stats.average_duration.as_secs_f64()
                    );
                    println!(
                        "Total Duration: {:.2}s",
                        execution_stats.total_duration.as_secs_f64()
                    );
                    println!(
                        "Orchestration Usage: {:.1}%",
                        execution_stats.orchestration_usage
                    );
                }

                if detailed {
                    println!();
                    println!("📈 Queue Health");
                    println!("===============");
                    let failure_rate = if queue_stats.total_count > 0 {
                        (queue_stats.failed_count as f64 / queue_stats.total_count as f64) * 100.0
                    } else {
                        0.0
                    };

                    let health_emoji = if failure_rate < 5.0 {
                        "🟢"
                    } else if failure_rate < 15.0 {
                        "🟡"
                    } else {
                        "🔴"
                    };

                    println!(
                        "{} Overall Health: {:.1}% failure rate",
                        health_emoji, failure_rate
                    );

                    if queue_stats.active_count > 10 {
                        println!(
                            "⚠️  High concurrent load: {} tasks",
                            queue_stats.active_count
                        );
                    }

                    if queue_stats.pending_count > 50 {
                        println!(
                            "⚠️  Queue backlog: {} pending tasks",
                            queue_stats.pending_count
                        );
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "execution_engine_unavailable",
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot display task statistics");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_sangha (lines 3628-3804) ---[0m
    async fn handle_sangha(&self, action: &SanghaAction) -> Result<()> {
        // Using stub implementation for now
        use crate::extension_stub::sangha::{Vote, VoteType};
        use std::str::FromStr;

        // Stub implementation - just log the action
        info!("🏛️  Sangha action requested (stub implementation)");

        match action {
            SanghaAction::Propose {
                proposal_type,
                file,
            } => {
                info!("📋 Submitting proposal to Sangha (stub implementation)");

                // Read proposal specification from file
                let _spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read proposal file")?;

                info!("Proposal type: {}", proposal_type);

                // Stub: Generate a fake proposal ID
                let proposal_id = uuid::Uuid::new_v4();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Proposal submitted",
                            "proposal_id": proposal_id,
                            "type": proposal_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!(
                        "📋 Submitting {} proposal from: {}",
                        proposal_type,
                        file.display()
                    );
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Proposal submitted successfully");
                }
            }

            SanghaAction::Vote {
                proposal_id,
                choice,
                reason,
            } => {
                info!("🗳️ Casting vote (stub implementation)");

                // Parse vote choice
                let vote_type = match choice.to_lowercase().as_str() {
                    "aye" | "yes" | "approve" => VoteType::Approve,
                    "nay" | "no" | "reject" => VoteType::Reject,
                    "abstain" => VoteType::Abstain,
                    "veto" | "needs_changes" => VoteType::NeedsChanges,
                    _ => {
                        anyhow::bail!(
                            "Invalid vote choice: {}. Use aye, nay, abstain, or veto",
                            choice
                        );
                    }
                };

                // Parse proposal ID
                let prop_id =
                    uuid::Uuid::from_str(proposal_id).context("Invalid proposal ID format")?;

                // Create vote (stub)
                let _vote = Vote::new("cli-user".to_string(), prop_id.to_string(), vote_type);

                info!("Vote created for proposal: {}", proposal_id);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Vote cast",
                            "proposal_id": proposal_id,
                            "choice": choice,
                            "reason": reason,
                        }))?
                    );
                } else {
                    println!("🗳️ Casting vote on proposal: {}", proposal_id);
                    println!("   Choice: {}", choice);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                    println!("✅ Vote cast successfully");
                }
            }

            SanghaAction::List { all, status } => {
                info!("📊 Listing Sangha status (stub implementation)");

                // Stub statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Sangha status retrieved (stub)",
                            "stats": {
                                "total_members": 1,
                                "active_members": 1,
                                "total_proposals": 0,
                                "active_proposals": 0,
                                "consensus_algorithm": "stub",
                            },
                            "show_all": all,
                            "filter_status": status,
                        }))?
                    );
                } else {
                    println!("📊 Sangha Status (Stub Implementation)");
                    println!("=====================================");
                    println!("👥 Members: 1 total, 1 active");
                    println!("📋 Proposals: 0 total, 0 active");
                    println!("🧠 Consensus Algorithm: stub");

                    println!(
                        "\n💡 No active proposals. Use 'ccswarm sangha propose' to create one."
                    );
                }
            }

            SanghaAction::Session { id, active } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session info",
                            "session_id": id,
                            "active": active,
                        }))?
                    );
                } else {
                    println!("🏛️ Sangha Session");
                    if *active {
                        println!("No active session");
                    }
                }
            }

            SanghaAction::ExtensionReview {
                proposal_id,
                technical_check,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension reviewed",
                            "proposal_id": proposal_id,
                            "technical_check": technical_check,
                        }))?
                    );
                } else {
                    println!("🔍 Reviewing extension proposal: {}", proposal_id);
                    if *technical_check {
                        println!("   Performing technical validation...");
                    }
                    println!("✅ Review completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:3628-3804 method handle_sangha <-> ./crates/ccswarm/src/cli/mod.rs:5468-5582 method handle_health
  Similarity: 93.44%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_sangha (lines 3628-3804) ---[0m
    async fn handle_sangha(&self, action: &SanghaAction) -> Result<()> {
        // Using stub implementation for now
        use crate::extension_stub::sangha::{Vote, VoteType};
        use std::str::FromStr;

        // Stub implementation - just log the action
        info!("🏛️  Sangha action requested (stub implementation)");

        match action {
            SanghaAction::Propose {
                proposal_type,
                file,
            } => {
                info!("📋 Submitting proposal to Sangha (stub implementation)");

                // Read proposal specification from file
                let _spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read proposal file")?;

                info!("Proposal type: {}", proposal_type);

                // Stub: Generate a fake proposal ID
                let proposal_id = uuid::Uuid::new_v4();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Proposal submitted",
                            "proposal_id": proposal_id,
                            "type": proposal_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!(
                        "📋 Submitting {} proposal from: {}",
                        proposal_type,
                        file.display()
                    );
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Proposal submitted successfully");
                }
            }

            SanghaAction::Vote {
                proposal_id,
                choice,
                reason,
            } => {
                info!("🗳️ Casting vote (stub implementation)");

                // Parse vote choice
                let vote_type = match choice.to_lowercase().as_str() {
                    "aye" | "yes" | "approve" => VoteType::Approve,
                    "nay" | "no" | "reject" => VoteType::Reject,
                    "abstain" => VoteType::Abstain,
                    "veto" | "needs_changes" => VoteType::NeedsChanges,
                    _ => {
                        anyhow::bail!(
                            "Invalid vote choice: {}. Use aye, nay, abstain, or veto",
                            choice
                        );
                    }
                };

                // Parse proposal ID
                let prop_id =
                    uuid::Uuid::from_str(proposal_id).context("Invalid proposal ID format")?;

                // Create vote (stub)
                let _vote = Vote::new("cli-user".to_string(), prop_id.to_string(), vote_type);

                info!("Vote created for proposal: {}", proposal_id);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Vote cast",
                            "proposal_id": proposal_id,
                            "choice": choice,
                            "reason": reason,
                        }))?
                    );
                } else {
                    println!("🗳️ Casting vote on proposal: {}", proposal_id);
                    println!("   Choice: {}", choice);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                    println!("✅ Vote cast successfully");
                }
            }

            SanghaAction::List { all, status } => {
                info!("📊 Listing Sangha status (stub implementation)");

                // Stub statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Sangha status retrieved (stub)",
                            "stats": {
                                "total_members": 1,
                                "active_members": 1,
                                "total_proposals": 0,
                                "active_proposals": 0,
                                "consensus_algorithm": "stub",
                            },
                            "show_all": all,
                            "filter_status": status,
                        }))?
                    );
                } else {
                    println!("📊 Sangha Status (Stub Implementation)");
                    println!("=====================================");
                    println!("👥 Members: 1 total, 1 active");
                    println!("📋 Proposals: 0 total, 0 active");
                    println!("🧠 Consensus Algorithm: stub");

                    println!(
                        "\n💡 No active proposals. Use 'ccswarm sangha propose' to create one."
                    );
                }
            }

            SanghaAction::Session { id, active } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session info",
                            "session_id": id,
                            "active": active,
                        }))?
                    );
                } else {
                    println!("🏛️ Sangha Session");
                    if *active {
                        println!("No active session");
                    }
                }
            }

            SanghaAction::ExtensionReview {
                proposal_id,
                technical_check,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension reviewed",
                            "proposal_id": proposal_id,
                            "technical_check": technical_check,
                        }))?
                    );
                } else {
                    println!("🔍 Reviewing extension proposal: {}", proposal_id);
                    if *technical_check {
                        println!("   Performing technical validation...");
                    }
                    println!("✅ Review completed");
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_health (lines 5468-5582) ---[0m
    async fn handle_health(
        &self,
        check_agents: bool,
        check_sessions: bool,
        resources: bool,
        diagnose: bool,
        detailed: bool,
        format: &str,
    ) -> Result<()> {
        use crate::cli::health::{run_diagnostics, HealthChecker};
        use crate::coordination::StatusTracker;

        // Run diagnostics if requested
        if diagnose {
            return run_diagnostics(&self.repo_path).await;
        }

        // Initialize health checker
        let status_tracker = StatusTracker::new().await?;
        let health_checker = HealthChecker::new(status_tracker);

        // Add monitoring system if available
        // TODO: Get monitoring system from running orchestrator
        // let monitoring = MonitoringSystem::new();
        // let health_checker = health_checker.with_monitoring(monitoring);

        // Perform health checks based on flags
        let report = if check_agents && !check_sessions && !resources {
            // Only check agents
            let checks = health_checker.check_agents_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let total_agents = checks.len();
            let healthy_agents = checks
                .iter()
                .filter(|c| c.status == health::HealthStatus::Healthy)
                .count();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents,
                healthy_agents,
                active_tasks: 0,
                session_count: 0,
            }
        } else if check_sessions && !check_agents && !resources {
            // Only check sessions
            let checks = health_checker.check_sessions_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let session_count = checks.len();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents: 0,
                healthy_agents: 0,
                active_tasks: 0,
                session_count,
            }
        } else {
            // Full health check
            health_checker.check_all().await?
        };

        // Output results based on format
        match format {
            "json" => {
                println!("{}", serde_json::to_string_pretty(&report)?);
            }
            _ => {
                if detailed {
                    report.print_detailed();
                } else {
                    report.print_summary();
                }
            }
        }

        // Exit with non-zero code if unhealthy
        if report.overall_status == health::HealthStatus::Critical {
            std::process::exit(1);
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2772-2884 method run_interactive_delegation <-> ./crates/ccswarm/src/cli/mod.rs:4173-4336 method handle_search
  Similarity: 97.26%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_interactive_delegation (lines 2772-2884) ---[0m
    async fn run_interactive_delegation(&self) -> Result<()> {
        use std::io::{self, Write};

        println!("🎯 Welcome to Interactive Delegation Mode");
        println!("   Type 'help' for commands, 'quit' to exit");
        println!();

        loop {
            print!("ccswarm> ");
            io::stdout().flush()?;

            let mut input = String::new();
            io::stdin().read_line(&mut input)?;
            let input = input.trim();

            match input {
                "quit" | "exit" => {
                    println!("👋 Goodbye!");
                    break;
                }
                "help" => {
                    println!("📚 Interactive Delegation Commands:");
                    println!("   analyze <task_description>  - Analyze task and suggest agent");
                    println!("   delegate <agent> <task>     - Delegate task to specific agent");
                    println!("   stats                       - Show delegation statistics");
                    println!("   agents                      - List available agents");
                    println!("   quit                        - Exit interactive mode");
                    println!();
                }
                "stats" => {
                    println!("📊 Delegation Statistics");
                    println!("   Feature not yet implemented");
                    println!();
                }
                "agents" => {
                    println!("🤖 Available Agents:");
                    println!("   • Frontend - React/TypeScript UI development");
                    println!("   • Backend - Node.js/Express API development");
                    println!("   • DevOps - Infrastructure and deployment");
                    println!("   • QA - Testing and quality assurance");
                    println!();
                }
                _ if input.starts_with("analyze ") => {
                    let task_desc = &input[8..];
                    if !task_desc.is_empty() {
                        // Directly call delegation analysis to avoid recursion
                        use crate::orchestrator::master_delegation::{
                            DelegationStrategy, MasterDelegationEngine,
                        };
                        let mut engine = MasterDelegationEngine::new(DelegationStrategy::Hybrid);
                        let task = Task::new(
                            "interactive-analysis".to_string(),
                            task_desc.to_string(),
                            Priority::Medium,
                            TaskType::Development,
                        );

                        match engine.delegate_task(task) {
                            Ok(decision) => {
                                println!("🔍 Task Analysis Results");
                                println!("   Task: {}", task_desc);
                                println!("   Recommended Agent: {}", decision.target_agent.name());
                                println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                                println!("   Reasoning: {}", decision.reasoning);
                                if let Some(duration) = decision.estimated_duration {
                                    println!("   Estimated Duration: {} seconds", duration);
                                }
                            }
                            Err(e) => {
                                println!("❌ Analysis failed: {}", e);
                            }
                        }
                        println!();
                    } else {
                        println!("❌ Please provide a task description");
                        println!("   Example: analyze Create login form with validation");
                        println!();
                    }
                }
                _ if input.starts_with("delegate ") => {
                    let parts: Vec<&str> = input[9..].splitn(2, ' ').collect();
                    if parts.len() == 2 {
                        let agent = parts[0];
                        let task_desc = parts[1];

                        if ["frontend", "backend", "devops", "qa"].contains(&agent) {
                            println!("🎯 Delegating '{}' to {} agent", task_desc, agent);
                            println!("   ✅ Task queued for delegation");
                            println!();
                        } else {
                            println!("❌ Unknown agent: {}", agent);
                            println!("   Available: frontend, backend, devops, qa");
                            println!();
                        }
                    } else {
                        println!("❌ Usage: delegate <agent> <task_description>");
                        println!("   Example: delegate frontend Create responsive navigation bar");
                        println!();
                    }
                }
                "" => {
                    // Empty input, continue
                }
                _ => {
                    println!("❓ Unknown command: {}", input);
                    println!("   Type 'help' for available commands");
                    println!();
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_search (lines 4173-4336) ---[0m
    async fn handle_search(&self, action: &SearchAction) -> Result<()> {
        use crate::extension_stub::agent_extension::{
            DocumentationSearchStrategy, GitHubSearchStrategy, SearchContext, SearchFilters,
            SearchQuery, SearchStrategy, StackOverflowSearchStrategy,
        };

        match action {
            SearchAction::Docs {
                query,
                source,
                limit,
            } => {
                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: Some("rust".to_string()),
                        framework: Some("general".to_string()),
                        date_range: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec![source.clone()],
                        relevance_threshold: 0.5,
                    }),
                };

                let mut all_results = Vec::new();

                if source == "all" || source == "mdn" {
                    let mdn_strategy = DocumentationSearchStrategy::new();
                    match mdn_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("MDN search failed: {}", e),
                    }
                }

                if source == "all" || source == "github" {
                    let github_strategy = GitHubSearchStrategy::new();
                    match github_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("GitHub search failed: {}", e),
                    }
                }

                if source == "all" || source == "stackoverflow" {
                    let stackoverflow_strategy = StackOverflowSearchStrategy;
                    match stackoverflow_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("StackOverflow search failed: {}", e),
                    }
                }

                all_results.sort_by(|a, b| {
                    b.relevance_score
                        .partial_cmp(&a.relevance_score)
                        .unwrap_or(std::cmp::Ordering::Equal)
                });
                all_results.truncate(*limit);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "query": query,
                            "source": source,
                            "results": all_results.iter().map(|r| serde_json::json!({
                                "title": r.title,
                                "url": r.url,
                                "snippet": r.snippet,
                                "source": r.source,
                                "relevance_score": r.relevance_score,
                            })).collect::<Vec<_>>()
                        }))?
                    );
                } else {
                    println!("🔍 Searching documentation for: {}", query);
                    println!("   Source: {}", source);
                    println!("   Found {} results", all_results.len());
                    println!();

                    for (i, result) in all_results.iter().enumerate() {
                        println!("{}. {} ({})", i + 1, result.title, result.source);
                        println!("   🔗 {}", result.url);
                        println!("   📝 {}", result.snippet);
                        println!("   ⭐ Relevance: {:.2}", result.relevance_score);
                        println!();
                    }
                }
            }

            SearchAction::Test { query } => {
                println!("🧪 Testing search functionality with query: {}", query);

                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: None,
                        framework: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec!["all".to_string()],
                        relevance_threshold: 0.5,
                        date_range: None,
                    }),
                };

                // Test each search strategy
                println!("🔍 Testing MDN search...");
                let mdn_strategy = DocumentationSearchStrategy::new();
                match mdn_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ MDN: Found {} results", results.len()),
                    Err(e) => println!("❌ MDN: Error - {}", e),
                }

                println!("🔍 Testing GitHub search...");
                let github_strategy = GitHubSearchStrategy::new();
                match github_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ GitHub: Found {} results", results.len()),
                    Err(e) => println!("❌ GitHub: Error - {}", e),
                }

                println!("🔍 Testing StackOverflow search...");
                let stackoverflow_strategy = StackOverflowSearchStrategy;
                match stackoverflow_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ StackOverflow: Found {} results", results.len()),
                    Err(e) => println!("❌ StackOverflow: Error - {}", e),
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Search test completed",
                            "query": query,
                        }))?
                    );
                } else {
                    println!("✅ Search functionality test completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:3628-3804 method handle_sangha <-> ./crates/ccswarm/src/cli/mod.rs:4581-4682 method handle_evolution
  Similarity: 96.05%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_sangha (lines 3628-3804) ---[0m
    async fn handle_sangha(&self, action: &SanghaAction) -> Result<()> {
        // Using stub implementation for now
        use crate::extension_stub::sangha::{Vote, VoteType};
        use std::str::FromStr;

        // Stub implementation - just log the action
        info!("🏛️  Sangha action requested (stub implementation)");

        match action {
            SanghaAction::Propose {
                proposal_type,
                file,
            } => {
                info!("📋 Submitting proposal to Sangha (stub implementation)");

                // Read proposal specification from file
                let _spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read proposal file")?;

                info!("Proposal type: {}", proposal_type);

                // Stub: Generate a fake proposal ID
                let proposal_id = uuid::Uuid::new_v4();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Proposal submitted",
                            "proposal_id": proposal_id,
                            "type": proposal_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!(
                        "📋 Submitting {} proposal from: {}",
                        proposal_type,
                        file.display()
                    );
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Proposal submitted successfully");
                }
            }

            SanghaAction::Vote {
                proposal_id,
                choice,
                reason,
            } => {
                info!("🗳️ Casting vote (stub implementation)");

                // Parse vote choice
                let vote_type = match choice.to_lowercase().as_str() {
                    "aye" | "yes" | "approve" => VoteType::Approve,
                    "nay" | "no" | "reject" => VoteType::Reject,
                    "abstain" => VoteType::Abstain,
                    "veto" | "needs_changes" => VoteType::NeedsChanges,
                    _ => {
                        anyhow::bail!(
                            "Invalid vote choice: {}. Use aye, nay, abstain, or veto",
                            choice
                        );
                    }
                };

                // Parse proposal ID
                let prop_id =
                    uuid::Uuid::from_str(proposal_id).context("Invalid proposal ID format")?;

                // Create vote (stub)
                let _vote = Vote::new("cli-user".to_string(), prop_id.to_string(), vote_type);

                info!("Vote created for proposal: {}", proposal_id);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Vote cast",
                            "proposal_id": proposal_id,
                            "choice": choice,
                            "reason": reason,
                        }))?
                    );
                } else {
                    println!("🗳️ Casting vote on proposal: {}", proposal_id);
                    println!("   Choice: {}", choice);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                    println!("✅ Vote cast successfully");
                }
            }

            SanghaAction::List { all, status } => {
                info!("📊 Listing Sangha status (stub implementation)");

                // Stub statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Sangha status retrieved (stub)",
                            "stats": {
                                "total_members": 1,
                                "active_members": 1,
                                "total_proposals": 0,
                                "active_proposals": 0,
                                "consensus_algorithm": "stub",
                            },
                            "show_all": all,
                            "filter_status": status,
                        }))?
                    );
                } else {
                    println!("📊 Sangha Status (Stub Implementation)");
                    println!("=====================================");
                    println!("👥 Members: 1 total, 1 active");
                    println!("📋 Proposals: 0 total, 0 active");
                    println!("🧠 Consensus Algorithm: stub");

                    println!(
                        "\n💡 No active proposals. Use 'ccswarm sangha propose' to create one."
                    );
                }
            }

            SanghaAction::Session { id, active } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session info",
                            "session_id": id,
                            "active": active,
                        }))?
                    );
                } else {
                    println!("🏛️ Sangha Session");
                    if *active {
                        println!("No active session");
                    }
                }
            }

            SanghaAction::ExtensionReview {
                proposal_id,
                technical_check,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension reviewed",
                            "proposal_id": proposal_id,
                            "technical_check": technical_check,
                        }))?
                    );
                } else {
                    println!("🔍 Reviewing extension proposal: {}", proposal_id);
                    if *technical_check {
                        println!("   Performing technical validation...");
                    }
                    println!("✅ Review completed");
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_evolution (lines 4581-4682) ---[0m
    async fn handle_evolution(&self, action: &EvolutionAction) -> Result<()> {
        match action {
            EvolutionAction::Metrics { agent, all, period } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution metrics",
                            "agent": agent,
                            "all": all,
                            "period_days": period,
                        }))?
                    );
                } else {
                    println!("📈 Evolution Metrics");
                    println!("==================");
                    if *all {
                        println!("Showing metrics for all agents");
                    } else if let Some(a) = agent {
                        println!("Agent: {}", a);
                    }
                    println!("Period: {} days", period);
                    println!("\nNo metrics available");
                }
            }

            EvolutionAction::Patterns {
                pattern_type,
                successful,
                failed,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution patterns",
                            "pattern_type": pattern_type,
                            "filter": {
                                "successful": successful,
                                "failed": failed,
                            },
                        }))?
                    );
                } else {
                    println!("🔮 Evolution Patterns");
                    println!("===================");
                    if *successful {
                        println!("Showing successful patterns");
                    } else if *failed {
                        println!("Showing failed patterns");
                    }
                    println!("No patterns found");
                }
            }

            EvolutionAction::Genealogy { extension, full } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension genealogy",
                            "extension": extension,
                            "full_tree": full,
                        }))?
                    );
                } else {
                    println!("🌳 Extension Genealogy: {}", extension);
                    println!("======================");
                    if *full {
                        println!("Showing full genealogy tree");
                    }
                    println!("No genealogy data available");
                }
            }

            EvolutionAction::Report { format, output } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution report generated",
                            "format": format,
                            "output": output,
                        }))?
                    );
                } else {
                    println!("📊 Generating evolution report");
                    println!("   Format: {}", format);
                    if let Some(o) = output {
                        println!("   Output: {}", o.display());
                    }
                    println!("✅ Report generated");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1835-1933 method show_task_status <-> ./crates/ccswarm/src/cli/mod.rs:2279-2458 method handle_worktree
  Similarity: 95.77%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_status (lines 1835-1933) ---[0m
    async fn show_task_status(
        &self,
        task_id: &str,
        history: bool,
        orchestration: bool,
    ) -> Result<()> {
        let task = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.get_task(task_id).await
        } else {
            None
        };

        if let Some(task) = task {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "task": task,
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");

                let status_emoji = match &task.status {
                    TaskStatus::Pending => "⏳",
                    TaskStatus::Assigned { .. } => "📋",
                    TaskStatus::InProgress { .. } => "🏃",
                    TaskStatus::Completed { .. } => "✅",
                    TaskStatus::Failed { .. } => "❌",
                    TaskStatus::Cancelled { .. } => "🚫",
                };

                println!("{} Status: {:?}", status_emoji, task.status);
                println!("📝 Description: {}", task.task.description);
                println!("🎯 Priority: {:?}", task.task.priority);
                println!("📋 Type: {:?}", task.task.task_type);
                println!(
                    "⏰ Created: {}",
                    task.created_at.format("%Y-%m-%d %H:%M:%S")
                );
                println!(
                    "🔄 Updated: {}",
                    task.updated_at.format("%Y-%m-%d %H:%M:%S")
                );

                if let Some(agent) = &task.assigned_agent {
                    println!("👤 Assigned to: {}", agent);
                }

                if let Some(details) = &task.task.details {
                    println!("📝 Details: {}", details);
                }

                if history && !task.execution_history.is_empty() {
                    println!();
                    println!("📚 Execution History:");
                    for (i, attempt) in task.execution_history.iter().enumerate() {
                        println!(
                            "  {}. Agent: {} | Started: {}",
                            i + 1,
                            attempt.agent_id,
                            attempt.started_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(completed) = attempt.completed_at {
                            println!("     Completed: {}", completed.format("%Y-%m-%d %H:%M:%S"));
                        }
                        if let Some(error) = &attempt.error {
                            println!("     Error: {}", error);
                        }
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "status": "not_found",
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");
                println!("❌ Task not found");
                println!();
                println!("💡 Use 'ccswarm task list' to see available tasks");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_worktree (lines 2279-2458) ---[0m
    async fn handle_worktree(&self, action: &WorktreeAction) -> Result<()> {
        let manager = crate::git::shell::ShellWorktreeManager::new(self.repo_path.clone())?;

        match action {
            WorktreeAction::List => {
                let worktrees = manager.list_worktrees().await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&worktrees)?);
                } else {
                    println!("🌳 Git Worktrees");
                    println!("===============");

                    for wt in &worktrees {
                        println!("Path: {}", wt.path.display());
                        println!("  Branch: {}", wt.branch);
                        println!("  Head: {}", wt.head_commit);
                        println!("  Locked: {}", wt.is_locked);
                        println!();
                    }
                }
            }
            WorktreeAction::Create {
                path,
                branch,
                new_branch,
            } => {
                let info = if *new_branch {
                    manager.create_worktree_full(path, branch, true).await?
                } else {
                    manager.create_worktree(path, branch).await?
                };

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&info)?);
                } else {
                    println!("✅ Worktree created");
                    println!("   Path: {}", info.path.display());
                    println!("   Branch: {}", info.branch);
                }
            }
            WorktreeAction::Remove { path, force } => {
                if *force {
                    manager.remove_worktree_full(path, true).await?
                } else {
                    manager.remove_worktree(path).await?
                };

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktree removed",
                            "path": path,
                        }))?
                    );
                } else {
                    println!("✅ Worktree removed: {}", path.display());
                }
            }
            WorktreeAction::Prune => {
                manager.prune_worktrees().await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktrees pruned",
                        }))?
                    );
                } else {
                    println!("✅ Stale worktrees pruned");
                }
            }
            WorktreeAction::Clean { force } => {
                use std::io::{self, Write};

                // Find all ccswarm-related worktrees
                let worktrees = manager.list_worktrees().await?;
                let ccswarm_worktrees: Vec<_> = worktrees
                    .iter()
                    .filter(|w| w.branch.contains("agent") || w.branch.contains("feature/"))
                    .collect();

                if ccswarm_worktrees.is_empty() {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "No ccswarm worktrees found",
                            }))?
                        );
                    } else {
                        println!("✅ No ccswarm worktrees to clean");
                    }
                    return Ok(());
                }

                // Ask for confirmation unless forced
                if !force {
                    println!("⚠️  Found {} ccswarm worktrees:", ccswarm_worktrees.len());
                    for w in &ccswarm_worktrees {
                        println!("   - {} ({})", w.path.display(), w.branch);
                    }
                    print!("\nAre you sure you want to remove all these worktrees? [y/N] ");
                    io::stdout().flush()?;

                    let mut response = String::new();
                    io::stdin().read_line(&mut response)?;

                    if !response.trim().eq_ignore_ascii_case("y") {
                        println!("❌ Cleanup cancelled");
                        return Ok(());
                    }
                }

                // Remove all ccswarm worktrees
                let mut removed_count = 0;
                for worktree in ccswarm_worktrees {
                    match manager.remove_worktree(&worktree.path).await {
                        Ok(_) => {
                            removed_count += 1;
                            if !self.json_output {
                                println!("   ✓ Removed {}", worktree.path.display());
                            }
                        }
                        Err(e) => {
                            if !self.json_output {
                                println!(
                                    "   ✗ Failed to remove {}: {}",
                                    worktree.path.display(),
                                    e
                                );
                            }
                        }
                    }
                }

                // Also clean up branches
                let output = tokio::process::Command::new("git")
                    .args(["branch", "--list", "*agent*", "feature/*"])
                    .output()
                    .await?;

                if output.status.success() {
                    let branches = String::from_utf8_lossy(&output.stdout);
                    let branch_count = branches.lines().count();

                    if branch_count > 0 {
                        tokio::process::Command::new("git")
                            .args(&["branch", "-D"])
                            .args(branches.lines().map(|b| b.trim().trim_start_matches("* ")))
                            .output()
                            .await?;
                    }
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Cleanup completed",
                            "worktrees_removed": removed_count,
                        }))?
                    );
                } else {
                    println!(
                        "\n✅ Cleanup completed: {} worktrees removed",
                        removed_count
                    );
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1835-1933 method show_task_status <-> ./crates/ccswarm/src/cli/mod.rs:3628-3804 method handle_sangha
  Similarity: 96.80%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_status (lines 1835-1933) ---[0m
    async fn show_task_status(
        &self,
        task_id: &str,
        history: bool,
        orchestration: bool,
    ) -> Result<()> {
        let task = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.get_task(task_id).await
        } else {
            None
        };

        if let Some(task) = task {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "task": task,
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");

                let status_emoji = match &task.status {
                    TaskStatus::Pending => "⏳",
                    TaskStatus::Assigned { .. } => "📋",
                    TaskStatus::InProgress { .. } => "🏃",
                    TaskStatus::Completed { .. } => "✅",
                    TaskStatus::Failed { .. } => "❌",
                    TaskStatus::Cancelled { .. } => "🚫",
                };

                println!("{} Status: {:?}", status_emoji, task.status);
                println!("📝 Description: {}", task.task.description);
                println!("🎯 Priority: {:?}", task.task.priority);
                println!("📋 Type: {:?}", task.task.task_type);
                println!(
                    "⏰ Created: {}",
                    task.created_at.format("%Y-%m-%d %H:%M:%S")
                );
                println!(
                    "🔄 Updated: {}",
                    task.updated_at.format("%Y-%m-%d %H:%M:%S")
                );

                if let Some(agent) = &task.assigned_agent {
                    println!("👤 Assigned to: {}", agent);
                }

                if let Some(details) = &task.task.details {
                    println!("📝 Details: {}", details);
                }

                if history && !task.execution_history.is_empty() {
                    println!();
                    println!("📚 Execution History:");
                    for (i, attempt) in task.execution_history.iter().enumerate() {
                        println!(
                            "  {}. Agent: {} | Started: {}",
                            i + 1,
                            attempt.agent_id,
                            attempt.started_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(completed) = attempt.completed_at {
                            println!("     Completed: {}", completed.format("%Y-%m-%d %H:%M:%S"));
                        }
                        if let Some(error) = &attempt.error {
                            println!("     Error: {}", error);
                        }
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "status": "not_found",
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");
                println!("❌ Task not found");
                println!();
                println!("💡 Use 'ccswarm task list' to see available tasks");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_sangha (lines 3628-3804) ---[0m
    async fn handle_sangha(&self, action: &SanghaAction) -> Result<()> {
        // Using stub implementation for now
        use crate::extension_stub::sangha::{Vote, VoteType};
        use std::str::FromStr;

        // Stub implementation - just log the action
        info!("🏛️  Sangha action requested (stub implementation)");

        match action {
            SanghaAction::Propose {
                proposal_type,
                file,
            } => {
                info!("📋 Submitting proposal to Sangha (stub implementation)");

                // Read proposal specification from file
                let _spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read proposal file")?;

                info!("Proposal type: {}", proposal_type);

                // Stub: Generate a fake proposal ID
                let proposal_id = uuid::Uuid::new_v4();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Proposal submitted",
                            "proposal_id": proposal_id,
                            "type": proposal_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!(
                        "📋 Submitting {} proposal from: {}",
                        proposal_type,
                        file.display()
                    );
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Proposal submitted successfully");
                }
            }

            SanghaAction::Vote {
                proposal_id,
                choice,
                reason,
            } => {
                info!("🗳️ Casting vote (stub implementation)");

                // Parse vote choice
                let vote_type = match choice.to_lowercase().as_str() {
                    "aye" | "yes" | "approve" => VoteType::Approve,
                    "nay" | "no" | "reject" => VoteType::Reject,
                    "abstain" => VoteType::Abstain,
                    "veto" | "needs_changes" => VoteType::NeedsChanges,
                    _ => {
                        anyhow::bail!(
                            "Invalid vote choice: {}. Use aye, nay, abstain, or veto",
                            choice
                        );
                    }
                };

                // Parse proposal ID
                let prop_id =
                    uuid::Uuid::from_str(proposal_id).context("Invalid proposal ID format")?;

                // Create vote (stub)
                let _vote = Vote::new("cli-user".to_string(), prop_id.to_string(), vote_type);

                info!("Vote created for proposal: {}", proposal_id);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Vote cast",
                            "proposal_id": proposal_id,
                            "choice": choice,
                            "reason": reason,
                        }))?
                    );
                } else {
                    println!("🗳️ Casting vote on proposal: {}", proposal_id);
                    println!("   Choice: {}", choice);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                    println!("✅ Vote cast successfully");
                }
            }

            SanghaAction::List { all, status } => {
                info!("📊 Listing Sangha status (stub implementation)");

                // Stub statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Sangha status retrieved (stub)",
                            "stats": {
                                "total_members": 1,
                                "active_members": 1,
                                "total_proposals": 0,
                                "active_proposals": 0,
                                "consensus_algorithm": "stub",
                            },
                            "show_all": all,
                            "filter_status": status,
                        }))?
                    );
                } else {
                    println!("📊 Sangha Status (Stub Implementation)");
                    println!("=====================================");
                    println!("👥 Members: 1 total, 1 active");
                    println!("📋 Proposals: 0 total, 0 active");
                    println!("🧠 Consensus Algorithm: stub");

                    println!(
                        "\n💡 No active proposals. Use 'ccswarm sangha propose' to create one."
                    );
                }
            }

            SanghaAction::Session { id, active } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session info",
                            "session_id": id,
                            "active": active,
                        }))?
                    );
                } else {
                    println!("🏛️ Sangha Session");
                    if *active {
                        println!("No active session");
                    }
                }
            }

            SanghaAction::ExtensionReview {
                proposal_id,
                technical_check,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension reviewed",
                            "proposal_id": proposal_id,
                            "technical_check": technical_check,
                        }))?
                    );
                } else {
                    println!("🔍 Reviewing extension proposal: {}", proposal_id);
                    if *technical_check {
                        println!("   Performing technical validation...");
                    }
                    println!("✅ Review completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1406-1543 method show_status <-> ./crates/ccswarm/src/cli/mod.rs:2637-2770 method handle_delegate
  Similarity: 97.99%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_status (lines 1406-1543) ---[0m
    async fn show_status(&self, detailed: bool, agent: Option<&str>) -> Result<()> {
        // Read status from coordination files
        let status_tracker = crate::coordination::StatusTracker::new().await?;

        if let Some(agent_id) = agent {
            // Show specific agent status
            if let Some(status) = status_tracker.get_status(agent_id).await? {
                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&status)?);
                } else {
                    println!("Agent: {}", agent_id);
                    println!("Status: {}", status["status"]);
                    println!("Updated: {}", status["timestamp"]);

                    // Check if this is a backend agent and show backend-specific info
                    if let Some(role) = status.get("role") {
                        if role.as_str() == Some("Backend") {
                            if let Some(backend_info) = status.get("backend_specific") {
                                println!("\n🔧 Backend Status:");
                                if let Some(api_health) = backend_info.get("api_health") {
                                    println!(
                                        "  API Health: {:.1}%",
                                        api_health.as_f64().unwrap_or(0.0) * 100.0
                                    );
                                }
                                if let Some(db) = backend_info.get("database") {
                                    println!(
                                        "  Database: {} ({})",
                                        if db["is_connected"].as_bool().unwrap_or(false) {
                                            "Connected"
                                        } else {
                                            "Disconnected"
                                        },
                                        db["database_type"].as_str().unwrap_or("Unknown")
                                    );
                                }
                                if let Some(server) = backend_info.get("server") {
                                    println!(
                                        "  Server: {:.1}MB RAM, {:.1}% CPU",
                                        server["memory_usage_mb"].as_f64().unwrap_or(0.0),
                                        server["cpu_usage_percent"].as_f64().unwrap_or(0.0)
                                    );
                                }
                                if let Some(services) =
                                    backend_info.get("services").and_then(|s| s.as_array())
                                {
                                    println!("  Active Services: {}", services.len());
                                }
                                if let Some(activity) = backend_info.get("recent_activity") {
                                    println!(
                                        "  Recent API Calls: {}",
                                        activity.as_u64().unwrap_or(0)
                                    );
                                }
                            }
                        }
                    }

                    if detailed {
                        println!(
                            "\nDetails: {}",
                            serde_json::to_string_pretty(&status["additional_info"])?
                        );
                    }
                }
            } else if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "Agent not found",
                        "agent": agent_id,
                    }))?
                );
            } else {
                println!("❌ Agent '{}' not found", agent_id);
            }
        } else {
            // Show all agent statuses
            let statuses = status_tracker.get_all_statuses().await?;

            if self.json_output {
                println!("{}", serde_json::to_string_pretty(&statuses)?);
            } else {
                println!("📊 ccswarm Status");
                println!("================");

                if statuses.is_empty() {
                    println!("No agents found");
                } else {
                    for status in &statuses {
                        println!("Agent: {}", status["agent_id"]);
                        println!("  Status: {}", status["status"]);
                        println!("  Updated: {}", status["timestamp"]);

                        // Show role-specific summary for backend agents
                        if let Some(role) = status.get("role") {
                            if role.as_str() == Some("Backend") {
                                if let Some(backend_info) = status.get("backend_specific") {
                                    if let Some(api_health) = backend_info.get("api_health") {
                                        print!(
                                            "  API Health: {:.0}% | ",
                                            api_health.as_f64().unwrap_or(0.0) * 100.0
                                        );
                                    }
                                    if let Some(db) = backend_info.get("database") {
                                        print!(
                                            "DB: {} | ",
                                            if db["is_connected"].as_bool().unwrap_or(false) {
                                                "✓"
                                            } else {
                                                "✗"
                                            }
                                        );
                                    }
                                    if let Some(services) =
                                        backend_info.get("services").and_then(|s| s.as_array())
                                    {
                                        print!("Services: {}", services.len());
                                    }
                                    println!();
                                }
                            }
                        }

                        if detailed {
                            println!(
                                "  Details: {}",
                                serde_json::to_string_pretty(&status["additional_info"])?
                            );
                        }
                        println!();
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_delegate (lines 2637-2770) ---[0m
    async fn handle_delegate(&self, action: &DelegateAction) -> Result<()> {
        use crate::orchestrator::master_delegation::{DelegationStrategy, MasterDelegationEngine};

        match action {
            DelegateAction::Task {
                description,
                agent,
                priority,
                task_type,
                details,
                force,
            } => {
                let task = self.create_task_from_args(
                    description,
                    priority,
                    task_type,
                    details.as_deref(),
                    None,
                )?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Task delegated",
                            "task": task,
                            "target_agent": agent,
                            "forced": force,
                        }))?
                    );
                } else {
                    println!("🎯 Delegating task to {} agent", agent);
                    println!("   Task: {}", task.description);
                    println!("   Priority: {:?}", task.priority);
                    println!("   Type: {:?}", task.task_type);
                    if *force {
                        println!("   ⚠️ Forced delegation");
                    }
                }
            }

            DelegateAction::Analyze {
                description,
                verbose,
                strategy,
            } => {
                let strategy = match strategy.as_str() {
                    "content" => DelegationStrategy::ContentBased,
                    "load" => DelegationStrategy::LoadBalanced,
                    "expertise" => DelegationStrategy::ExpertiseBased,
                    "workflow" => DelegationStrategy::WorkflowBased,
                    "hybrid" => DelegationStrategy::Hybrid,
                    _ => DelegationStrategy::Hybrid,
                };

                let mut engine = MasterDelegationEngine::new(strategy);
                let task = Task::new(
                    "analysis".to_string(),
                    description.clone(),
                    Priority::Medium,
                    TaskType::Development,
                );

                let decision = engine.delegate_task(task)?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&decision)?);
                } else {
                    println!("🔍 Task Analysis Results");
                    println!("   Task: {}", description);
                    println!("   Recommended Agent: {}", decision.target_agent.name());
                    println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                    if *verbose {
                        println!("   Reasoning: {}", decision.reasoning);
                        if let Some(duration) = decision.estimated_duration {
                            println!("   Estimated Duration: {:?}", duration);
                        }
                    }
                }
            }

            DelegateAction::Stats { detailed, period } => {
                // TODO: Implement delegation statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Delegation statistics",
                            "period_hours": period,
                            "detailed": detailed,
                        }))?
                    );
                } else {
                    println!("📊 Delegation Statistics (last {} hours)", period);
                    println!("   Feature not yet implemented");
                }
            }

            DelegateAction::Interactive => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "error": "Interactive mode not available in JSON output",
                        }))?
                    );
                } else {
                    println!("🖥️ Interactive Delegation Mode");
                    self.run_interactive_delegation().await?;
                }
            }

            DelegateAction::Show { file } => {
                let config = CcswarmConfig::from_file(file.clone()).await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&config)?);
                } else {
                    println!("📄 Delegation Configuration: {}", file.display());
                    println!("========================");
                    println!("Project: {}", config.project.name);
                    println!("Repository: {}", config.project.repository.url);
                    println!("Agents: {}", config.agents.len());
                    for (name, agent_config) in &config.agents {
                        println!("  - {}: {}", name, agent_config.specialization);
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1737-1832 method list_tasks <-> ./crates/ccswarm/src/cli/mod.rs:3628-3804 method handle_sangha
  Similarity: 97.25%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_tasks (lines 1737-1832) ---[0m
    async fn list_tasks(
        &self,
        all: bool,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
        detailed: bool,
    ) -> Result<()> {
        let tasks = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.list_tasks(status_filter, agent_filter).await
        } else {
            Vec::new()
        };

        let displayed_tasks = if all {
            tasks
        } else {
            tasks.into_iter().take(50).collect()
        };

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "tasks": displayed_tasks,
                    "total": displayed_tasks.len(),
                    "filters": {
                        "all": all,
                        "status": status_filter,
                        "agent": agent_filter,
                        "detailed": detailed
                    }
                }))?
            );
        } else {
            println!("📋 Task List");
            println!("============");

            if displayed_tasks.is_empty() {
                println!("No tasks in queue currently.");
                println!();
                println!("💡 Add a task with: ccswarm task add \"Your task description\"");
            } else {
                println!("Found {} tasks:", displayed_tasks.len());
                println!();

                for task in &displayed_tasks {
                    let status_emoji = match &task.status {
                        TaskStatus::Pending => "⏳",
                        TaskStatus::Assigned { .. } => "📋",
                        TaskStatus::InProgress { .. } => "🏃",
                        TaskStatus::Completed { .. } => "✅",
                        TaskStatus::Failed { .. } => "❌",
                        TaskStatus::Cancelled { .. } => "🚫",
                    };

                    let priority_emoji = match task.task.priority {
                        Priority::Critical => "🚨",
                        Priority::High => "🔥",
                        Priority::Medium => "📅",
                        Priority::Low => "💤",
                    };

                    println!(
                        "{} {} {} [{}] {}",
                        status_emoji,
                        priority_emoji,
                        &task.task.id[..8], // Short ID
                        task.task.task_type,
                        task.task.description
                    );

                    if let Some(agent) = &task.assigned_agent {
                        println!("   👤 Assigned to: {}", agent);
                    }

                    if detailed {
                        println!(
                            "   ⏰ Created: {}",
                            task.created_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        println!(
                            "   🔄 Updated: {}",
                            task.updated_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(details) = &task.task.details {
                            println!("   📝 Details: {}", details);
                        }
                    }
                    println!();
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_sangha (lines 3628-3804) ---[0m
    async fn handle_sangha(&self, action: &SanghaAction) -> Result<()> {
        // Using stub implementation for now
        use crate::extension_stub::sangha::{Vote, VoteType};
        use std::str::FromStr;

        // Stub implementation - just log the action
        info!("🏛️  Sangha action requested (stub implementation)");

        match action {
            SanghaAction::Propose {
                proposal_type,
                file,
            } => {
                info!("📋 Submitting proposal to Sangha (stub implementation)");

                // Read proposal specification from file
                let _spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read proposal file")?;

                info!("Proposal type: {}", proposal_type);

                // Stub: Generate a fake proposal ID
                let proposal_id = uuid::Uuid::new_v4();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Proposal submitted",
                            "proposal_id": proposal_id,
                            "type": proposal_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!(
                        "📋 Submitting {} proposal from: {}",
                        proposal_type,
                        file.display()
                    );
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Proposal submitted successfully");
                }
            }

            SanghaAction::Vote {
                proposal_id,
                choice,
                reason,
            } => {
                info!("🗳️ Casting vote (stub implementation)");

                // Parse vote choice
                let vote_type = match choice.to_lowercase().as_str() {
                    "aye" | "yes" | "approve" => VoteType::Approve,
                    "nay" | "no" | "reject" => VoteType::Reject,
                    "abstain" => VoteType::Abstain,
                    "veto" | "needs_changes" => VoteType::NeedsChanges,
                    _ => {
                        anyhow::bail!(
                            "Invalid vote choice: {}. Use aye, nay, abstain, or veto",
                            choice
                        );
                    }
                };

                // Parse proposal ID
                let prop_id =
                    uuid::Uuid::from_str(proposal_id).context("Invalid proposal ID format")?;

                // Create vote (stub)
                let _vote = Vote::new("cli-user".to_string(), prop_id.to_string(), vote_type);

                info!("Vote created for proposal: {}", proposal_id);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Vote cast",
                            "proposal_id": proposal_id,
                            "choice": choice,
                            "reason": reason,
                        }))?
                    );
                } else {
                    println!("🗳️ Casting vote on proposal: {}", proposal_id);
                    println!("   Choice: {}", choice);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                    println!("✅ Vote cast successfully");
                }
            }

            SanghaAction::List { all, status } => {
                info!("📊 Listing Sangha status (stub implementation)");

                // Stub statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Sangha status retrieved (stub)",
                            "stats": {
                                "total_members": 1,
                                "active_members": 1,
                                "total_proposals": 0,
                                "active_proposals": 0,
                                "consensus_algorithm": "stub",
                            },
                            "show_all": all,
                            "filter_status": status,
                        }))?
                    );
                } else {
                    println!("📊 Sangha Status (Stub Implementation)");
                    println!("=====================================");
                    println!("👥 Members: 1 total, 1 active");
                    println!("📋 Proposals: 0 total, 0 active");
                    println!("🧠 Consensus Algorithm: stub");

                    println!(
                        "\n💡 No active proposals. Use 'ccswarm sangha propose' to create one."
                    );
                }
            }

            SanghaAction::Session { id, active } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session info",
                            "session_id": id,
                            "active": active,
                        }))?
                    );
                } else {
                    println!("🏛️ Sangha Session");
                    if *active {
                        println!("No active session");
                    }
                }
            }

            SanghaAction::ExtensionReview {
                proposal_id,
                technical_check,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension reviewed",
                            "proposal_id": proposal_id,
                            "technical_check": technical_check,
                        }))?
                    );
                } else {
                    println!("🔍 Reviewing extension proposal: {}", proposal_id);
                    if *technical_check {
                        println!("   Performing technical validation...");
                    }
                    println!("✅ Review completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1406-1543 method show_status <-> ./crates/ccswarm/src/cli/mod.rs:5584-5809 method handle_doctor
  Similarity: 72.55%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_status (lines 1406-1543) ---[0m
    async fn show_status(&self, detailed: bool, agent: Option<&str>) -> Result<()> {
        // Read status from coordination files
        let status_tracker = crate::coordination::StatusTracker::new().await?;

        if let Some(agent_id) = agent {
            // Show specific agent status
            if let Some(status) = status_tracker.get_status(agent_id).await? {
                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&status)?);
                } else {
                    println!("Agent: {}", agent_id);
                    println!("Status: {}", status["status"]);
                    println!("Updated: {}", status["timestamp"]);

                    // Check if this is a backend agent and show backend-specific info
                    if let Some(role) = status.get("role") {
                        if role.as_str() == Some("Backend") {
                            if let Some(backend_info) = status.get("backend_specific") {
                                println!("\n🔧 Backend Status:");
                                if let Some(api_health) = backend_info.get("api_health") {
                                    println!(
                                        "  API Health: {:.1}%",
                                        api_health.as_f64().unwrap_or(0.0) * 100.0
                                    );
                                }
                                if let Some(db) = backend_info.get("database") {
                                    println!(
                                        "  Database: {} ({})",
                                        if db["is_connected"].as_bool().unwrap_or(false) {
                                            "Connected"
                                        } else {
                                            "Disconnected"
                                        },
                                        db["database_type"].as_str().unwrap_or("Unknown")
                                    );
                                }
                                if let Some(server) = backend_info.get("server") {
                                    println!(
                                        "  Server: {:.1}MB RAM, {:.1}% CPU",
                                        server["memory_usage_mb"].as_f64().unwrap_or(0.0),
                                        server["cpu_usage_percent"].as_f64().unwrap_or(0.0)
                                    );
                                }
                                if let Some(services) =
                                    backend_info.get("services").and_then(|s| s.as_array())
                                {
                                    println!("  Active Services: {}", services.len());
                                }
                                if let Some(activity) = backend_info.get("recent_activity") {
                                    println!(
                                        "  Recent API Calls: {}",
                                        activity.as_u64().unwrap_or(0)
                                    );
                                }
                            }
                        }
                    }

                    if detailed {
                        println!(
                            "\nDetails: {}",
                            serde_json::to_string_pretty(&status["additional_info"])?
                        );
                    }
                }
            } else if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "Agent not found",
                        "agent": agent_id,
                    }))?
                );
            } else {
                println!("❌ Agent '{}' not found", agent_id);
            }
        } else {
            // Show all agent statuses
            let statuses = status_tracker.get_all_statuses().await?;

            if self.json_output {
                println!("{}", serde_json::to_string_pretty(&statuses)?);
            } else {
                println!("📊 ccswarm Status");
                println!("================");

                if statuses.is_empty() {
                    println!("No agents found");
                } else {
                    for status in &statuses {
                        println!("Agent: {}", status["agent_id"]);
                        println!("  Status: {}", status["status"]);
                        println!("  Updated: {}", status["timestamp"]);

                        // Show role-specific summary for backend agents
                        if let Some(role) = status.get("role") {
                            if role.as_str() == Some("Backend") {
                                if let Some(backend_info) = status.get("backend_specific") {
                                    if let Some(api_health) = backend_info.get("api_health") {
                                        print!(
                                            "  API Health: {:.0}% | ",
                                            api_health.as_f64().unwrap_or(0.0) * 100.0
                                        );
                                    }
                                    if let Some(db) = backend_info.get("database") {
                                        print!(
                                            "DB: {} | ",
                                            if db["is_connected"].as_bool().unwrap_or(false) {
                                                "✓"
                                            } else {
                                                "✗"
                                            }
                                        );
                                    }
                                    if let Some(services) =
                                        backend_info.get("services").and_then(|s| s.as_array())
                                    {
                                        print!("Services: {}", services.len());
                                    }
                                    println!();
                                }
                            }
                        }

                        if detailed {
                            println!(
                                "  Details: {}",
                                serde_json::to_string_pretty(&status["additional_info"])?
                            );
                        }
                        println!();
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_doctor (lines 5584-5809) ---[0m
    async fn handle_doctor(
        &self,
        fix: bool,
        error_code: Option<&str>,
        check_api: bool,
    ) -> Result<()> {
        use crate::utils::error_recovery::ErrorRecoveryDB;
        use crate::utils::user_error::CommonErrors;

        // Handle specific error code diagnosis
        if let Some(code) = error_code {
            println!("{}", "🔍 Error Code Diagnosis".bright_cyan().bold());
            println!("{}", "=======================".bright_cyan());
            println!();
            println!("Analyzing error code: {}", code.bright_yellow());
            println!();

            let recovery_db = ErrorRecoveryDB::new();
            if let Some(recovery) = recovery_db.get_recovery(code) {
                match &recovery {
                    crate::utils::error_recovery::RecoveryStep::UserAction { description } => {
                        println!("📋 {}", description.bright_white());
                    },
                    crate::utils::error_recovery::RecoveryStep::Command { description, .. } => {
                        println!("📋 {}", description.bright_white());
                    },
                    crate::utils::error_recovery::RecoveryStep::FileCreate { path, .. } => {
                        println!("📋 Create file: {}", path.bright_white());
                    },
                    crate::utils::error_recovery::RecoveryStep::EnvVar { name, .. } => {
                        println!("📋 Set environment variable: {}", name.bright_white());
                    },
                }
                println!();
                println!("Recovery steps:");
                let steps = [recovery.clone()]; // Treat the single recovery step as the step list
                for (i, step) in steps.iter().enumerate() {
                    match step {
                        crate::utils::error_recovery::RecoveryStep::Command {
                            cmd,
                            description,
                        } => {
                            println!("  {}. {} {}", i + 1, "Run:".bright_yellow(), description);
                            println!("     {}", cmd.bright_white().on_black());
                        }
                        crate::utils::error_recovery::RecoveryStep::FileCreate { path, .. } => {
                            println!(
                                "  {}. {} {}",
                                i + 1,
                                "Create file:".bright_yellow(),
                                path.bright_white()
                            );
                        }
                        crate::utils::error_recovery::RecoveryStep::EnvVar { name, example } => {
                            println!(
                                "  {}. {} {}",
                                i + 1,
                                "Set environment variable:".bright_yellow(),
                                name.bright_white()
                            );
                            println!("     Example: {}={}", name, example.bright_black());
                        }
                        crate::utils::error_recovery::RecoveryStep::UserAction { description } => {
                            println!(
                                "  {}. {} {}",
                                i + 1,
                                "Action required:".bright_yellow(),
                                description
                            );
                        }
                    }
                    println!();
                }

                let can_auto_fix = matches!(recovery, crate::utils::error_recovery::RecoveryStep::Command { .. });
                if can_auto_fix && fix {
                    recovery_db.auto_fix(code).await?;
                } else if can_auto_fix {
                    println!(
                        "💡 This error can be auto-fixed! Run: ccswarm doctor --error {} --fix",
                        code
                    );
                }
            } else {
                println!("❌ Unknown error code: {}", code);
                println!("   See all error codes: ccswarm help errors");
            }
            return Ok(());
        }

        // Handle API connectivity check
        if check_api {
            println!("{}", "🌐 API Connectivity Check".bright_cyan().bold());
            println!("{}", "=========================".bright_cyan());
            println!();

            print!("Testing Anthropic API... ");
            match reqwest::get("https://api.anthropic.com/v1/health").await {
                Ok(resp) if resp.status().is_success() => {
                    println!("{}", "✅ Connected".bright_green());
                }
                Ok(resp) => {
                    println!(
                        "{}",
                        format!("⚠️  Status: {}", resp.status()).bright_yellow()
                    );
                }
                Err(e) => {
                    println!("{}", "❌ Failed".bright_red());
                    println!("   {}", e.to_string().bright_black());
                }
            }
            println!();
            return Ok(());
        }

        println!("{}", "🏥 ccswarm System Diagnosis".bright_cyan().bold());
        println!("{}", "===========================".bright_cyan());
        println!();

        let mut issues = Vec::new();

        // Check Git
        print!("Checking Git... ");
        match std::process::Command::new("git").arg("--version").output() {
            Ok(output) if output.status.success() => {
                println!("{}", "✅ OK".bright_green());
            }
            _ => {
                println!("{}", "❌ Not found".bright_red());
                issues.push("git");
            }
        }

        // Check API keys
        print!("Checking API keys... ");
        if std::env::var("ANTHROPIC_API_KEY").is_ok() {
            println!("{}", "✅ Set".bright_green());
        } else {
            println!("{}", "⚠️  Not set".bright_yellow());
            issues.push("api_key");
        }

        // Check config
        print!("Checking configuration... ");
        let config_path = self.repo_path.join("ccswarm.json");
        if config_path.exists() {
            match CcswarmConfig::from_file(config_path.clone()).await {
                Ok(_) => println!("{}", "✅ Valid".bright_green()),
                Err(e) => {
                    println!("{}", "❌ Invalid".bright_red());
                    println!("   {}", e.to_string().bright_black());
                    issues.push("config");
                }
            }
        } else {
            println!("{}", "❌ Not found".bright_red());
            issues.push("config");
        }

        // Check git repo
        print!("Checking git repository... ");
        let git_dir = self.repo_path.join(".git");
        if git_dir.exists() {
            println!("{}", "✅ Initialized".bright_green());
        } else {
            println!("{}", "⚠️  Not initialized".bright_yellow());
            issues.push("git_repo");
        }

        // Check disk space
        print!("Checking disk space... ");
        // Simple check - in real implementation would use proper system calls
        println!("{}", "✅ Sufficient".bright_green());

        println!();

        if issues.is_empty() {
            println!("{}", "✅ All systems operational!".bright_green().bold());
        } else {
            println!(
                "{}",
                format!("⚠️  Found {} issues", issues.len())
                    .bright_yellow()
                    .bold()
            );

            if fix {
                println!();
                println!("{}", "🔧 Attempting fixes...".bright_cyan());

                for issue in &issues {
                    match *issue {
                        "git" => {
                            println!("• Git: Please install git from https://git-scm.com");
                        }
                        "api_key" => {
                            CommonErrors::api_key_missing("Anthropic").display();
                        }
                        "config" => {
                            println!("• Config: Run 'ccswarm setup' to create configuration");
                        }
                        "git_repo" => {
                            if fix {
                                println!("• Initializing git repository...");
                                crate::git::shell::ShellWorktreeManager::init_if_needed(
                                    &self.repo_path,
                                )
                                .await?;
                                println!("  ✅ Git repository initialized");
                            }
                        }
                        _ => {}
                    }
                }
            } else {
                println!();
                println!(
                    "{}",
                    "💡 Run with --fix to attempt automatic fixes".bright_black()
                );
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1835-1933 method show_task_status <-> ./crates/ccswarm/src/cli/mod.rs:4173-4336 method handle_search
  Similarity: 99.09%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_status (lines 1835-1933) ---[0m
    async fn show_task_status(
        &self,
        task_id: &str,
        history: bool,
        orchestration: bool,
    ) -> Result<()> {
        let task = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.get_task(task_id).await
        } else {
            None
        };

        if let Some(task) = task {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "task": task,
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");

                let status_emoji = match &task.status {
                    TaskStatus::Pending => "⏳",
                    TaskStatus::Assigned { .. } => "📋",
                    TaskStatus::InProgress { .. } => "🏃",
                    TaskStatus::Completed { .. } => "✅",
                    TaskStatus::Failed { .. } => "❌",
                    TaskStatus::Cancelled { .. } => "🚫",
                };

                println!("{} Status: {:?}", status_emoji, task.status);
                println!("📝 Description: {}", task.task.description);
                println!("🎯 Priority: {:?}", task.task.priority);
                println!("📋 Type: {:?}", task.task.task_type);
                println!(
                    "⏰ Created: {}",
                    task.created_at.format("%Y-%m-%d %H:%M:%S")
                );
                println!(
                    "🔄 Updated: {}",
                    task.updated_at.format("%Y-%m-%d %H:%M:%S")
                );

                if let Some(agent) = &task.assigned_agent {
                    println!("👤 Assigned to: {}", agent);
                }

                if let Some(details) = &task.task.details {
                    println!("📝 Details: {}", details);
                }

                if history && !task.execution_history.is_empty() {
                    println!();
                    println!("📚 Execution History:");
                    for (i, attempt) in task.execution_history.iter().enumerate() {
                        println!(
                            "  {}. Agent: {} | Started: {}",
                            i + 1,
                            attempt.agent_id,
                            attempt.started_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(completed) = attempt.completed_at {
                            println!("     Completed: {}", completed.format("%Y-%m-%d %H:%M:%S"));
                        }
                        if let Some(error) = &attempt.error {
                            println!("     Error: {}", error);
                        }
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "status": "not_found",
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");
                println!("❌ Task not found");
                println!();
                println!("💡 Use 'ccswarm task list' to see available tasks");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_search (lines 4173-4336) ---[0m
    async fn handle_search(&self, action: &SearchAction) -> Result<()> {
        use crate::extension_stub::agent_extension::{
            DocumentationSearchStrategy, GitHubSearchStrategy, SearchContext, SearchFilters,
            SearchQuery, SearchStrategy, StackOverflowSearchStrategy,
        };

        match action {
            SearchAction::Docs {
                query,
                source,
                limit,
            } => {
                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: Some("rust".to_string()),
                        framework: Some("general".to_string()),
                        date_range: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec![source.clone()],
                        relevance_threshold: 0.5,
                    }),
                };

                let mut all_results = Vec::new();

                if source == "all" || source == "mdn" {
                    let mdn_strategy = DocumentationSearchStrategy::new();
                    match mdn_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("MDN search failed: {}", e),
                    }
                }

                if source == "all" || source == "github" {
                    let github_strategy = GitHubSearchStrategy::new();
                    match github_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("GitHub search failed: {}", e),
                    }
                }

                if source == "all" || source == "stackoverflow" {
                    let stackoverflow_strategy = StackOverflowSearchStrategy;
                    match stackoverflow_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("StackOverflow search failed: {}", e),
                    }
                }

                all_results.sort_by(|a, b| {
                    b.relevance_score
                        .partial_cmp(&a.relevance_score)
                        .unwrap_or(std::cmp::Ordering::Equal)
                });
                all_results.truncate(*limit);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "query": query,
                            "source": source,
                            "results": all_results.iter().map(|r| serde_json::json!({
                                "title": r.title,
                                "url": r.url,
                                "snippet": r.snippet,
                                "source": r.source,
                                "relevance_score": r.relevance_score,
                            })).collect::<Vec<_>>()
                        }))?
                    );
                } else {
                    println!("🔍 Searching documentation for: {}", query);
                    println!("   Source: {}", source);
                    println!("   Found {} results", all_results.len());
                    println!();

                    for (i, result) in all_results.iter().enumerate() {
                        println!("{}. {} ({})", i + 1, result.title, result.source);
                        println!("   🔗 {}", result.url);
                        println!("   📝 {}", result.snippet);
                        println!("   ⭐ Relevance: {:.2}", result.relevance_score);
                        println!();
                    }
                }
            }

            SearchAction::Test { query } => {
                println!("🧪 Testing search functionality with query: {}", query);

                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: None,
                        framework: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec!["all".to_string()],
                        relevance_threshold: 0.5,
                        date_range: None,
                    }),
                };

                // Test each search strategy
                println!("🔍 Testing MDN search...");
                let mdn_strategy = DocumentationSearchStrategy::new();
                match mdn_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ MDN: Found {} results", results.len()),
                    Err(e) => println!("❌ MDN: Error - {}", e),
                }

                println!("🔍 Testing GitHub search...");
                let github_strategy = GitHubSearchStrategy::new();
                match github_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ GitHub: Found {} results", results.len()),
                    Err(e) => println!("❌ GitHub: Error - {}", e),
                }

                println!("🔍 Testing StackOverflow search...");
                let stackoverflow_strategy = StackOverflowSearchStrategy;
                match stackoverflow_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ StackOverflow: Found {} results", results.len()),
                    Err(e) => println!("❌ StackOverflow: Error - {}", e),
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Search test completed",
                            "query": query,
                        }))?
                    );
                } else {
                    println!("✅ Search functionality test completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1545-1666 method add_task <-> ./crates/ccswarm/src/cli/mod.rs:3628-3804 method handle_sangha
  Similarity: 86.37%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:add_task (lines 1545-1666) ---[0m
    async fn add_task(
        &self,
        description: &str,
        priority: &str,
        task_type: &str,
        details: Option<&str>,
        duration: Option<u32>,
    ) -> Result<()> {
        // use crate::cli::progress::{ProgressStyle, ProgressTracker};
        use crate::utils::user_error::CommonErrors;

        // Validate task description
        if description.trim().is_empty() {
            CommonErrors::invalid_task_format()
                .with_details("Task description cannot be empty")
                .with_suggestion("Provide a clear, actionable task description")
                .display();
            return Err(anyhow!("Invalid task description"));
        }

        // Create task (simplified progress display)
        println!(
            "Creating task: {}...",
            description.chars().take(50).collect::<String>()
        );

        let priority = match priority.to_lowercase().as_str() {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };

        let task_type = match task_type.to_lowercase().as_str() {
            "development" | "dev" => TaskType::Development,
            "testing" | "test" => TaskType::Testing,
            "documentation" | "docs" => TaskType::Documentation,
            "infrastructure" | "infra" => TaskType::Infrastructure,
            "coordination" => TaskType::Coordination,
            "review" => TaskType::Review,
            "bugfix" | "bug" => TaskType::Bugfix,
            "feature" => TaskType::Feature,
            _ => TaskType::Development,
        };

        let task_type_clone = task_type;
        let mut task = Task::new(
            uuid::Uuid::new_v4().to_string(),
            description.to_string(),
            priority,
            task_type,
        );

        if let Some(details) = details {
            task = task.with_details(details.to_string());
        }

        if let Some(duration) = duration {
            task = task.with_duration(duration);
        }

        // Add to task queue via execution engine
        let task_id = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.add_task(task.clone()).await
        } else {
            // Fall back to creating task with ID but warn user
            warn!("Execution engine not available, task will not be executed");
            task.id.clone()
        };

        // Complete progress indicator
        // ProgressTracker::complete(progress, true, Some("Task created successfully".to_string())).await;

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Task added",
                    "task_id": task_id,
                    "description": description,
                    "priority": priority,
                }))?
            );
        } else {
            println!();
            println!("{}", "✅ Task created successfully!".bright_green().bold());
            println!();
            println!(
                "   {} {}",
                "Task ID:".bright_cyan(),
                task_id[..8].bright_white()
            );
            println!("   {} {}", "Description:".bright_cyan(), description);
            println!(
                "   {} {}",
                "Priority:".bright_cyan(),
                match priority {
                    Priority::Critical => "🔴 Critical".bright_red(),
                    Priority::High => "🟡 High".bright_yellow(),
                    Priority::Medium => "🟢 Medium".bright_green(),
                    Priority::Low => "🔵 Low".bright_blue(),
                }
            );
            println!(
                "   {} {}",
                "Type:".bright_cyan(),
                format!("{:?}", task_type_clone).bright_white()
            );

            if let Some(duration) = task.estimated_duration {
                println!("   {} {} minutes", "Est. Duration:".bright_cyan(), duration);
            }

            // Show helpful next steps
            show_quick_help("task-created");
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_sangha (lines 3628-3804) ---[0m
    async fn handle_sangha(&self, action: &SanghaAction) -> Result<()> {
        // Using stub implementation for now
        use crate::extension_stub::sangha::{Vote, VoteType};
        use std::str::FromStr;

        // Stub implementation - just log the action
        info!("🏛️  Sangha action requested (stub implementation)");

        match action {
            SanghaAction::Propose {
                proposal_type,
                file,
            } => {
                info!("📋 Submitting proposal to Sangha (stub implementation)");

                // Read proposal specification from file
                let _spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read proposal file")?;

                info!("Proposal type: {}", proposal_type);

                // Stub: Generate a fake proposal ID
                let proposal_id = uuid::Uuid::new_v4();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Proposal submitted",
                            "proposal_id": proposal_id,
                            "type": proposal_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!(
                        "📋 Submitting {} proposal from: {}",
                        proposal_type,
                        file.display()
                    );
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Proposal submitted successfully");
                }
            }

            SanghaAction::Vote {
                proposal_id,
                choice,
                reason,
            } => {
                info!("🗳️ Casting vote (stub implementation)");

                // Parse vote choice
                let vote_type = match choice.to_lowercase().as_str() {
                    "aye" | "yes" | "approve" => VoteType::Approve,
                    "nay" | "no" | "reject" => VoteType::Reject,
                    "abstain" => VoteType::Abstain,
                    "veto" | "needs_changes" => VoteType::NeedsChanges,
                    _ => {
                        anyhow::bail!(
                            "Invalid vote choice: {}. Use aye, nay, abstain, or veto",
                            choice
                        );
                    }
                };

                // Parse proposal ID
                let prop_id =
                    uuid::Uuid::from_str(proposal_id).context("Invalid proposal ID format")?;

                // Create vote (stub)
                let _vote = Vote::new("cli-user".to_string(), prop_id.to_string(), vote_type);

                info!("Vote created for proposal: {}", proposal_id);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Vote cast",
                            "proposal_id": proposal_id,
                            "choice": choice,
                            "reason": reason,
                        }))?
                    );
                } else {
                    println!("🗳️ Casting vote on proposal: {}", proposal_id);
                    println!("   Choice: {}", choice);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                    println!("✅ Vote cast successfully");
                }
            }

            SanghaAction::List { all, status } => {
                info!("📊 Listing Sangha status (stub implementation)");

                // Stub statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Sangha status retrieved (stub)",
                            "stats": {
                                "total_members": 1,
                                "active_members": 1,
                                "total_proposals": 0,
                                "active_proposals": 0,
                                "consensus_algorithm": "stub",
                            },
                            "show_all": all,
                            "filter_status": status,
                        }))?
                    );
                } else {
                    println!("📊 Sangha Status (Stub Implementation)");
                    println!("=====================================");
                    println!("👥 Members: 1 total, 1 active");
                    println!("📋 Proposals: 0 total, 0 active");
                    println!("🧠 Consensus Algorithm: stub");

                    println!(
                        "\n💡 No active proposals. Use 'ccswarm sangha propose' to create one."
                    );
                }
            }

            SanghaAction::Session { id, active } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session info",
                            "session_id": id,
                            "active": active,
                        }))?
                    );
                } else {
                    println!("🏛️ Sangha Session");
                    if *active {
                        println!("No active session");
                    }
                }
            }

            SanghaAction::ExtensionReview {
                proposal_id,
                technical_check,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension reviewed",
                            "proposal_id": proposal_id,
                            "technical_check": technical_check,
                        }))?
                    );
                } else {
                    println!("🔍 Reviewing extension proposal: {}", proposal_id);
                    if *technical_check {
                        println!("   Performing technical validation...");
                    }
                    println!("✅ Review completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1406-1543 method show_status <-> ./crates/ccswarm/src/cli/mod.rs:2772-2884 method run_interactive_delegation
  Similarity: 97.12%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_status (lines 1406-1543) ---[0m
    async fn show_status(&self, detailed: bool, agent: Option<&str>) -> Result<()> {
        // Read status from coordination files
        let status_tracker = crate::coordination::StatusTracker::new().await?;

        if let Some(agent_id) = agent {
            // Show specific agent status
            if let Some(status) = status_tracker.get_status(agent_id).await? {
                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&status)?);
                } else {
                    println!("Agent: {}", agent_id);
                    println!("Status: {}", status["status"]);
                    println!("Updated: {}", status["timestamp"]);

                    // Check if this is a backend agent and show backend-specific info
                    if let Some(role) = status.get("role") {
                        if role.as_str() == Some("Backend") {
                            if let Some(backend_info) = status.get("backend_specific") {
                                println!("\n🔧 Backend Status:");
                                if let Some(api_health) = backend_info.get("api_health") {
                                    println!(
                                        "  API Health: {:.1}%",
                                        api_health.as_f64().unwrap_or(0.0) * 100.0
                                    );
                                }
                                if let Some(db) = backend_info.get("database") {
                                    println!(
                                        "  Database: {} ({})",
                                        if db["is_connected"].as_bool().unwrap_or(false) {
                                            "Connected"
                                        } else {
                                            "Disconnected"
                                        },
                                        db["database_type"].as_str().unwrap_or("Unknown")
                                    );
                                }
                                if let Some(server) = backend_info.get("server") {
                                    println!(
                                        "  Server: {:.1}MB RAM, {:.1}% CPU",
                                        server["memory_usage_mb"].as_f64().unwrap_or(0.0),
                                        server["cpu_usage_percent"].as_f64().unwrap_or(0.0)
                                    );
                                }
                                if let Some(services) =
                                    backend_info.get("services").and_then(|s| s.as_array())
                                {
                                    println!("  Active Services: {}", services.len());
                                }
                                if let Some(activity) = backend_info.get("recent_activity") {
                                    println!(
                                        "  Recent API Calls: {}",
                                        activity.as_u64().unwrap_or(0)
                                    );
                                }
                            }
                        }
                    }

                    if detailed {
                        println!(
                            "\nDetails: {}",
                            serde_json::to_string_pretty(&status["additional_info"])?
                        );
                    }
                }
            } else if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "Agent not found",
                        "agent": agent_id,
                    }))?
                );
            } else {
                println!("❌ Agent '{}' not found", agent_id);
            }
        } else {
            // Show all agent statuses
            let statuses = status_tracker.get_all_statuses().await?;

            if self.json_output {
                println!("{}", serde_json::to_string_pretty(&statuses)?);
            } else {
                println!("📊 ccswarm Status");
                println!("================");

                if statuses.is_empty() {
                    println!("No agents found");
                } else {
                    for status in &statuses {
                        println!("Agent: {}", status["agent_id"]);
                        println!("  Status: {}", status["status"]);
                        println!("  Updated: {}", status["timestamp"]);

                        // Show role-specific summary for backend agents
                        if let Some(role) = status.get("role") {
                            if role.as_str() == Some("Backend") {
                                if let Some(backend_info) = status.get("backend_specific") {
                                    if let Some(api_health) = backend_info.get("api_health") {
                                        print!(
                                            "  API Health: {:.0}% | ",
                                            api_health.as_f64().unwrap_or(0.0) * 100.0
                                        );
                                    }
                                    if let Some(db) = backend_info.get("database") {
                                        print!(
                                            "DB: {} | ",
                                            if db["is_connected"].as_bool().unwrap_or(false) {
                                                "✓"
                                            } else {
                                                "✗"
                                            }
                                        );
                                    }
                                    if let Some(services) =
                                        backend_info.get("services").and_then(|s| s.as_array())
                                    {
                                        print!("Services: {}", services.len());
                                    }
                                    println!();
                                }
                            }
                        }

                        if detailed {
                            println!(
                                "  Details: {}",
                                serde_json::to_string_pretty(&status["additional_info"])?
                            );
                        }
                        println!();
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_interactive_delegation (lines 2772-2884) ---[0m
    async fn run_interactive_delegation(&self) -> Result<()> {
        use std::io::{self, Write};

        println!("🎯 Welcome to Interactive Delegation Mode");
        println!("   Type 'help' for commands, 'quit' to exit");
        println!();

        loop {
            print!("ccswarm> ");
            io::stdout().flush()?;

            let mut input = String::new();
            io::stdin().read_line(&mut input)?;
            let input = input.trim();

            match input {
                "quit" | "exit" => {
                    println!("👋 Goodbye!");
                    break;
                }
                "help" => {
                    println!("📚 Interactive Delegation Commands:");
                    println!("   analyze <task_description>  - Analyze task and suggest agent");
                    println!("   delegate <agent> <task>     - Delegate task to specific agent");
                    println!("   stats                       - Show delegation statistics");
                    println!("   agents                      - List available agents");
                    println!("   quit                        - Exit interactive mode");
                    println!();
                }
                "stats" => {
                    println!("📊 Delegation Statistics");
                    println!("   Feature not yet implemented");
                    println!();
                }
                "agents" => {
                    println!("🤖 Available Agents:");
                    println!("   • Frontend - React/TypeScript UI development");
                    println!("   • Backend - Node.js/Express API development");
                    println!("   • DevOps - Infrastructure and deployment");
                    println!("   • QA - Testing and quality assurance");
                    println!();
                }
                _ if input.starts_with("analyze ") => {
                    let task_desc = &input[8..];
                    if !task_desc.is_empty() {
                        // Directly call delegation analysis to avoid recursion
                        use crate::orchestrator::master_delegation::{
                            DelegationStrategy, MasterDelegationEngine,
                        };
                        let mut engine = MasterDelegationEngine::new(DelegationStrategy::Hybrid);
                        let task = Task::new(
                            "interactive-analysis".to_string(),
                            task_desc.to_string(),
                            Priority::Medium,
                            TaskType::Development,
                        );

                        match engine.delegate_task(task) {
                            Ok(decision) => {
                                println!("🔍 Task Analysis Results");
                                println!("   Task: {}", task_desc);
                                println!("   Recommended Agent: {}", decision.target_agent.name());
                                println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                                println!("   Reasoning: {}", decision.reasoning);
                                if let Some(duration) = decision.estimated_duration {
                                    println!("   Estimated Duration: {} seconds", duration);
                                }
                            }
                            Err(e) => {
                                println!("❌ Analysis failed: {}", e);
                            }
                        }
                        println!();
                    } else {
                        println!("❌ Please provide a task description");
                        println!("   Example: analyze Create login form with validation");
                        println!();
                    }
                }
                _ if input.starts_with("delegate ") => {
                    let parts: Vec<&str> = input[9..].splitn(2, ' ').collect();
                    if parts.len() == 2 {
                        let agent = parts[0];
                        let task_desc = parts[1];

                        if ["frontend", "backend", "devops", "qa"].contains(&agent) {
                            println!("🎯 Delegating '{}' to {} agent", task_desc, agent);
                            println!("   ✅ Task queued for delegation");
                            println!();
                        } else {
                            println!("❌ Unknown agent: {}", agent);
                            println!("   Available: frontend, backend, devops, qa");
                            println!();
                        }
                    } else {
                        println!("❌ Usage: delegate <agent> <task_description>");
                        println!("   Example: delegate frontend Create responsive navigation bar");
                        println!();
                    }
                }
                "" => {
                    // Empty input, continue
                }
                _ => {
                    println!("❓ Unknown command: {}", input);
                    println!("   Type 'help' for available commands");
                    println!();
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1406-1543 method show_status <-> ./crates/ccswarm/src/cli/mod.rs:2130-2238 method show_task_stats
  Similarity: 96.71%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_status (lines 1406-1543) ---[0m
    async fn show_status(&self, detailed: bool, agent: Option<&str>) -> Result<()> {
        // Read status from coordination files
        let status_tracker = crate::coordination::StatusTracker::new().await?;

        if let Some(agent_id) = agent {
            // Show specific agent status
            if let Some(status) = status_tracker.get_status(agent_id).await? {
                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&status)?);
                } else {
                    println!("Agent: {}", agent_id);
                    println!("Status: {}", status["status"]);
                    println!("Updated: {}", status["timestamp"]);

                    // Check if this is a backend agent and show backend-specific info
                    if let Some(role) = status.get("role") {
                        if role.as_str() == Some("Backend") {
                            if let Some(backend_info) = status.get("backend_specific") {
                                println!("\n🔧 Backend Status:");
                                if let Some(api_health) = backend_info.get("api_health") {
                                    println!(
                                        "  API Health: {:.1}%",
                                        api_health.as_f64().unwrap_or(0.0) * 100.0
                                    );
                                }
                                if let Some(db) = backend_info.get("database") {
                                    println!(
                                        "  Database: {} ({})",
                                        if db["is_connected"].as_bool().unwrap_or(false) {
                                            "Connected"
                                        } else {
                                            "Disconnected"
                                        },
                                        db["database_type"].as_str().unwrap_or("Unknown")
                                    );
                                }
                                if let Some(server) = backend_info.get("server") {
                                    println!(
                                        "  Server: {:.1}MB RAM, {:.1}% CPU",
                                        server["memory_usage_mb"].as_f64().unwrap_or(0.0),
                                        server["cpu_usage_percent"].as_f64().unwrap_or(0.0)
                                    );
                                }
                                if let Some(services) =
                                    backend_info.get("services").and_then(|s| s.as_array())
                                {
                                    println!("  Active Services: {}", services.len());
                                }
                                if let Some(activity) = backend_info.get("recent_activity") {
                                    println!(
                                        "  Recent API Calls: {}",
                                        activity.as_u64().unwrap_or(0)
                                    );
                                }
                            }
                        }
                    }

                    if detailed {
                        println!(
                            "\nDetails: {}",
                            serde_json::to_string_pretty(&status["additional_info"])?
                        );
                    }
                }
            } else if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "Agent not found",
                        "agent": agent_id,
                    }))?
                );
            } else {
                println!("❌ Agent '{}' not found", agent_id);
            }
        } else {
            // Show all agent statuses
            let statuses = status_tracker.get_all_statuses().await?;

            if self.json_output {
                println!("{}", serde_json::to_string_pretty(&statuses)?);
            } else {
                println!("📊 ccswarm Status");
                println!("================");

                if statuses.is_empty() {
                    println!("No agents found");
                } else {
                    for status in &statuses {
                        println!("Agent: {}", status["agent_id"]);
                        println!("  Status: {}", status["status"]);
                        println!("  Updated: {}", status["timestamp"]);

                        // Show role-specific summary for backend agents
                        if let Some(role) = status.get("role") {
                            if role.as_str() == Some("Backend") {
                                if let Some(backend_info) = status.get("backend_specific") {
                                    if let Some(api_health) = backend_info.get("api_health") {
                                        print!(
                                            "  API Health: {:.0}% | ",
                                            api_health.as_f64().unwrap_or(0.0) * 100.0
                                        );
                                    }
                                    if let Some(db) = backend_info.get("database") {
                                        print!(
                                            "DB: {} | ",
                                            if db["is_connected"].as_bool().unwrap_or(false) {
                                                "✓"
                                            } else {
                                                "✗"
                                            }
                                        );
                                    }
                                    if let Some(services) =
                                        backend_info.get("services").and_then(|s| s.as_array())
                                    {
                                        print!("Services: {}", services.len());
                                    }
                                    println!();
                                }
                            }
                        }

                        if detailed {
                            println!(
                                "  Details: {}",
                                serde_json::to_string_pretty(&status["additional_info"])?
                            );
                        }
                        println!();
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_stats (lines 2130-2238) ---[0m
    async fn show_task_stats(&self, detailed: bool, performance: bool) -> Result<()> {
        if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "queue_stats": queue_stats,
                        "execution_stats": execution_stats,
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("📊 Task Queue Statistics");
                println!("========================");
                println!("⏳ Pending: {}", queue_stats.pending_count);
                println!("🏃 In Progress: {}", queue_stats.active_count);
                println!("✅ Completed: {}", queue_stats.completed_count);
                println!("❌ Failed: {}", queue_stats.failed_count);
                println!("📋 Total: {}", queue_stats.total_count);

                if performance || detailed {
                    println!();
                    println!("🎯 Execution Statistics");
                    println!("=======================");
                    println!("Tasks Executed: {}", execution_stats.tasks_executed);
                    println!(
                        "Success Rate: {:.1}%",
                        if execution_stats.tasks_executed > 0 {
                            (execution_stats.tasks_succeeded as f64
                                / execution_stats.tasks_executed as f64)
                                * 100.0
                        } else {
                            0.0
                        }
                    );
                    println!(
                        "Average Duration: {:.2}s",
                        execution_stats.average_duration.as_secs_f64()
                    );
                    println!(
                        "Total Duration: {:.2}s",
                        execution_stats.total_duration.as_secs_f64()
                    );
                    println!(
                        "Orchestration Usage: {:.1}%",
                        execution_stats.orchestration_usage
                    );
                }

                if detailed {
                    println!();
                    println!("📈 Queue Health");
                    println!("===============");
                    let failure_rate = if queue_stats.total_count > 0 {
                        (queue_stats.failed_count as f64 / queue_stats.total_count as f64) * 100.0
                    } else {
                        0.0
                    };

                    let health_emoji = if failure_rate < 5.0 {
                        "🟢"
                    } else if failure_rate < 15.0 {
                        "🟡"
                    } else {
                        "🔴"
                    };

                    println!(
                        "{} Overall Health: {:.1}% failure rate",
                        health_emoji, failure_rate
                    );

                    if queue_stats.active_count > 10 {
                        println!(
                            "⚠️  High concurrent load: {} tasks",
                            queue_stats.active_count
                        );
                    }

                    if queue_stats.pending_count > 50 {
                        println!(
                            "⚠️  Queue backlog: {} pending tasks",
                            queue_stats.pending_count
                        );
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "execution_engine_unavailable",
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot display task statistics");
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2130-2238 method show_task_stats <-> ./crates/ccswarm/src/cli/mod.rs:2637-2770 method handle_delegate
  Similarity: 97.54%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_stats (lines 2130-2238) ---[0m
    async fn show_task_stats(&self, detailed: bool, performance: bool) -> Result<()> {
        if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "queue_stats": queue_stats,
                        "execution_stats": execution_stats,
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("📊 Task Queue Statistics");
                println!("========================");
                println!("⏳ Pending: {}", queue_stats.pending_count);
                println!("🏃 In Progress: {}", queue_stats.active_count);
                println!("✅ Completed: {}", queue_stats.completed_count);
                println!("❌ Failed: {}", queue_stats.failed_count);
                println!("📋 Total: {}", queue_stats.total_count);

                if performance || detailed {
                    println!();
                    println!("🎯 Execution Statistics");
                    println!("=======================");
                    println!("Tasks Executed: {}", execution_stats.tasks_executed);
                    println!(
                        "Success Rate: {:.1}%",
                        if execution_stats.tasks_executed > 0 {
                            (execution_stats.tasks_succeeded as f64
                                / execution_stats.tasks_executed as f64)
                                * 100.0
                        } else {
                            0.0
                        }
                    );
                    println!(
                        "Average Duration: {:.2}s",
                        execution_stats.average_duration.as_secs_f64()
                    );
                    println!(
                        "Total Duration: {:.2}s",
                        execution_stats.total_duration.as_secs_f64()
                    );
                    println!(
                        "Orchestration Usage: {:.1}%",
                        execution_stats.orchestration_usage
                    );
                }

                if detailed {
                    println!();
                    println!("📈 Queue Health");
                    println!("===============");
                    let failure_rate = if queue_stats.total_count > 0 {
                        (queue_stats.failed_count as f64 / queue_stats.total_count as f64) * 100.0
                    } else {
                        0.0
                    };

                    let health_emoji = if failure_rate < 5.0 {
                        "🟢"
                    } else if failure_rate < 15.0 {
                        "🟡"
                    } else {
                        "🔴"
                    };

                    println!(
                        "{} Overall Health: {:.1}% failure rate",
                        health_emoji, failure_rate
                    );

                    if queue_stats.active_count > 10 {
                        println!(
                            "⚠️  High concurrent load: {} tasks",
                            queue_stats.active_count
                        );
                    }

                    if queue_stats.pending_count > 50 {
                        println!(
                            "⚠️  Queue backlog: {} pending tasks",
                            queue_stats.pending_count
                        );
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "execution_engine_unavailable",
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot display task statistics");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_delegate (lines 2637-2770) ---[0m
    async fn handle_delegate(&self, action: &DelegateAction) -> Result<()> {
        use crate::orchestrator::master_delegation::{DelegationStrategy, MasterDelegationEngine};

        match action {
            DelegateAction::Task {
                description,
                agent,
                priority,
                task_type,
                details,
                force,
            } => {
                let task = self.create_task_from_args(
                    description,
                    priority,
                    task_type,
                    details.as_deref(),
                    None,
                )?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Task delegated",
                            "task": task,
                            "target_agent": agent,
                            "forced": force,
                        }))?
                    );
                } else {
                    println!("🎯 Delegating task to {} agent", agent);
                    println!("   Task: {}", task.description);
                    println!("   Priority: {:?}", task.priority);
                    println!("   Type: {:?}", task.task_type);
                    if *force {
                        println!("   ⚠️ Forced delegation");
                    }
                }
            }

            DelegateAction::Analyze {
                description,
                verbose,
                strategy,
            } => {
                let strategy = match strategy.as_str() {
                    "content" => DelegationStrategy::ContentBased,
                    "load" => DelegationStrategy::LoadBalanced,
                    "expertise" => DelegationStrategy::ExpertiseBased,
                    "workflow" => DelegationStrategy::WorkflowBased,
                    "hybrid" => DelegationStrategy::Hybrid,
                    _ => DelegationStrategy::Hybrid,
                };

                let mut engine = MasterDelegationEngine::new(strategy);
                let task = Task::new(
                    "analysis".to_string(),
                    description.clone(),
                    Priority::Medium,
                    TaskType::Development,
                );

                let decision = engine.delegate_task(task)?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&decision)?);
                } else {
                    println!("🔍 Task Analysis Results");
                    println!("   Task: {}", description);
                    println!("   Recommended Agent: {}", decision.target_agent.name());
                    println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                    if *verbose {
                        println!("   Reasoning: {}", decision.reasoning);
                        if let Some(duration) = decision.estimated_duration {
                            println!("   Estimated Duration: {:?}", duration);
                        }
                    }
                }
            }

            DelegateAction::Stats { detailed, period } => {
                // TODO: Implement delegation statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Delegation statistics",
                            "period_hours": period,
                            "detailed": detailed,
                        }))?
                    );
                } else {
                    println!("📊 Delegation Statistics (last {} hours)", period);
                    println!("   Feature not yet implemented");
                }
            }

            DelegateAction::Interactive => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "error": "Interactive mode not available in JSON output",
                        }))?
                    );
                } else {
                    println!("🖥️ Interactive Delegation Mode");
                    self.run_interactive_delegation().await?;
                }
            }

            DelegateAction::Show { file } => {
                let config = CcswarmConfig::from_file(file.clone()).await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&config)?);
                } else {
                    println!("📄 Delegation Configuration: {}", file.display());
                    println!("========================");
                    println!("Project: {}", config.project.name);
                    println!("Repository: {}", config.project.repository.url);
                    println!("Agents: {}", config.agents.len());
                    for (name, agent_config) in &config.agents {
                        println!("  - {}: {}", name, agent_config.specialization);
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2637-2770 method handle_delegate <-> ./crates/ccswarm/src/cli/mod.rs:2772-2884 method run_interactive_delegation
  Similarity: 95.79%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_delegate (lines 2637-2770) ---[0m
    async fn handle_delegate(&self, action: &DelegateAction) -> Result<()> {
        use crate::orchestrator::master_delegation::{DelegationStrategy, MasterDelegationEngine};

        match action {
            DelegateAction::Task {
                description,
                agent,
                priority,
                task_type,
                details,
                force,
            } => {
                let task = self.create_task_from_args(
                    description,
                    priority,
                    task_type,
                    details.as_deref(),
                    None,
                )?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Task delegated",
                            "task": task,
                            "target_agent": agent,
                            "forced": force,
                        }))?
                    );
                } else {
                    println!("🎯 Delegating task to {} agent", agent);
                    println!("   Task: {}", task.description);
                    println!("   Priority: {:?}", task.priority);
                    println!("   Type: {:?}", task.task_type);
                    if *force {
                        println!("   ⚠️ Forced delegation");
                    }
                }
            }

            DelegateAction::Analyze {
                description,
                verbose,
                strategy,
            } => {
                let strategy = match strategy.as_str() {
                    "content" => DelegationStrategy::ContentBased,
                    "load" => DelegationStrategy::LoadBalanced,
                    "expertise" => DelegationStrategy::ExpertiseBased,
                    "workflow" => DelegationStrategy::WorkflowBased,
                    "hybrid" => DelegationStrategy::Hybrid,
                    _ => DelegationStrategy::Hybrid,
                };

                let mut engine = MasterDelegationEngine::new(strategy);
                let task = Task::new(
                    "analysis".to_string(),
                    description.clone(),
                    Priority::Medium,
                    TaskType::Development,
                );

                let decision = engine.delegate_task(task)?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&decision)?);
                } else {
                    println!("🔍 Task Analysis Results");
                    println!("   Task: {}", description);
                    println!("   Recommended Agent: {}", decision.target_agent.name());
                    println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                    if *verbose {
                        println!("   Reasoning: {}", decision.reasoning);
                        if let Some(duration) = decision.estimated_duration {
                            println!("   Estimated Duration: {:?}", duration);
                        }
                    }
                }
            }

            DelegateAction::Stats { detailed, period } => {
                // TODO: Implement delegation statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Delegation statistics",
                            "period_hours": period,
                            "detailed": detailed,
                        }))?
                    );
                } else {
                    println!("📊 Delegation Statistics (last {} hours)", period);
                    println!("   Feature not yet implemented");
                }
            }

            DelegateAction::Interactive => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "error": "Interactive mode not available in JSON output",
                        }))?
                    );
                } else {
                    println!("🖥️ Interactive Delegation Mode");
                    self.run_interactive_delegation().await?;
                }
            }

            DelegateAction::Show { file } => {
                let config = CcswarmConfig::from_file(file.clone()).await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&config)?);
                } else {
                    println!("📄 Delegation Configuration: {}", file.display());
                    println!("========================");
                    println!("Project: {}", config.project.name);
                    println!("Repository: {}", config.project.repository.url);
                    println!("Agents: {}", config.agents.len());
                    for (name, agent_config) in &config.agents {
                        println!("  - {}: {}", name, agent_config.specialization);
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_interactive_delegation (lines 2772-2884) ---[0m
    async fn run_interactive_delegation(&self) -> Result<()> {
        use std::io::{self, Write};

        println!("🎯 Welcome to Interactive Delegation Mode");
        println!("   Type 'help' for commands, 'quit' to exit");
        println!();

        loop {
            print!("ccswarm> ");
            io::stdout().flush()?;

            let mut input = String::new();
            io::stdin().read_line(&mut input)?;
            let input = input.trim();

            match input {
                "quit" | "exit" => {
                    println!("👋 Goodbye!");
                    break;
                }
                "help" => {
                    println!("📚 Interactive Delegation Commands:");
                    println!("   analyze <task_description>  - Analyze task and suggest agent");
                    println!("   delegate <agent> <task>     - Delegate task to specific agent");
                    println!("   stats                       - Show delegation statistics");
                    println!("   agents                      - List available agents");
                    println!("   quit                        - Exit interactive mode");
                    println!();
                }
                "stats" => {
                    println!("📊 Delegation Statistics");
                    println!("   Feature not yet implemented");
                    println!();
                }
                "agents" => {
                    println!("🤖 Available Agents:");
                    println!("   • Frontend - React/TypeScript UI development");
                    println!("   • Backend - Node.js/Express API development");
                    println!("   • DevOps - Infrastructure and deployment");
                    println!("   • QA - Testing and quality assurance");
                    println!();
                }
                _ if input.starts_with("analyze ") => {
                    let task_desc = &input[8..];
                    if !task_desc.is_empty() {
                        // Directly call delegation analysis to avoid recursion
                        use crate::orchestrator::master_delegation::{
                            DelegationStrategy, MasterDelegationEngine,
                        };
                        let mut engine = MasterDelegationEngine::new(DelegationStrategy::Hybrid);
                        let task = Task::new(
                            "interactive-analysis".to_string(),
                            task_desc.to_string(),
                            Priority::Medium,
                            TaskType::Development,
                        );

                        match engine.delegate_task(task) {
                            Ok(decision) => {
                                println!("🔍 Task Analysis Results");
                                println!("   Task: {}", task_desc);
                                println!("   Recommended Agent: {}", decision.target_agent.name());
                                println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                                println!("   Reasoning: {}", decision.reasoning);
                                if let Some(duration) = decision.estimated_duration {
                                    println!("   Estimated Duration: {} seconds", duration);
                                }
                            }
                            Err(e) => {
                                println!("❌ Analysis failed: {}", e);
                            }
                        }
                        println!();
                    } else {
                        println!("❌ Please provide a task description");
                        println!("   Example: analyze Create login form with validation");
                        println!();
                    }
                }
                _ if input.starts_with("delegate ") => {
                    let parts: Vec<&str> = input[9..].splitn(2, ' ').collect();
                    if parts.len() == 2 {
                        let agent = parts[0];
                        let task_desc = parts[1];

                        if ["frontend", "backend", "devops", "qa"].contains(&agent) {
                            println!("🎯 Delegating '{}' to {} agent", task_desc, agent);
                            println!("   ✅ Task queued for delegation");
                            println!();
                        } else {
                            println!("❌ Unknown agent: {}", agent);
                            println!("   Available: frontend, backend, devops, qa");
                            println!();
                        }
                    } else {
                        println!("❌ Usage: delegate <agent> <task_description>");
                        println!("   Example: delegate frontend Create responsive navigation bar");
                        println!();
                    }
                }
                "" => {
                    // Empty input, continue
                }
                _ => {
                    println!("❓ Unknown command: {}", input);
                    println!("   Type 'help' for available commands");
                    println!();
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1406-1543 method show_status <-> ./crates/ccswarm/src/cli/mod.rs:4581-4682 method handle_evolution
  Similarity: 97.99%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_status (lines 1406-1543) ---[0m
    async fn show_status(&self, detailed: bool, agent: Option<&str>) -> Result<()> {
        // Read status from coordination files
        let status_tracker = crate::coordination::StatusTracker::new().await?;

        if let Some(agent_id) = agent {
            // Show specific agent status
            if let Some(status) = status_tracker.get_status(agent_id).await? {
                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&status)?);
                } else {
                    println!("Agent: {}", agent_id);
                    println!("Status: {}", status["status"]);
                    println!("Updated: {}", status["timestamp"]);

                    // Check if this is a backend agent and show backend-specific info
                    if let Some(role) = status.get("role") {
                        if role.as_str() == Some("Backend") {
                            if let Some(backend_info) = status.get("backend_specific") {
                                println!("\n🔧 Backend Status:");
                                if let Some(api_health) = backend_info.get("api_health") {
                                    println!(
                                        "  API Health: {:.1}%",
                                        api_health.as_f64().unwrap_or(0.0) * 100.0
                                    );
                                }
                                if let Some(db) = backend_info.get("database") {
                                    println!(
                                        "  Database: {} ({})",
                                        if db["is_connected"].as_bool().unwrap_or(false) {
                                            "Connected"
                                        } else {
                                            "Disconnected"
                                        },
                                        db["database_type"].as_str().unwrap_or("Unknown")
                                    );
                                }
                                if let Some(server) = backend_info.get("server") {
                                    println!(
                                        "  Server: {:.1}MB RAM, {:.1}% CPU",
                                        server["memory_usage_mb"].as_f64().unwrap_or(0.0),
                                        server["cpu_usage_percent"].as_f64().unwrap_or(0.0)
                                    );
                                }
                                if let Some(services) =
                                    backend_info.get("services").and_then(|s| s.as_array())
                                {
                                    println!("  Active Services: {}", services.len());
                                }
                                if let Some(activity) = backend_info.get("recent_activity") {
                                    println!(
                                        "  Recent API Calls: {}",
                                        activity.as_u64().unwrap_or(0)
                                    );
                                }
                            }
                        }
                    }

                    if detailed {
                        println!(
                            "\nDetails: {}",
                            serde_json::to_string_pretty(&status["additional_info"])?
                        );
                    }
                }
            } else if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "Agent not found",
                        "agent": agent_id,
                    }))?
                );
            } else {
                println!("❌ Agent '{}' not found", agent_id);
            }
        } else {
            // Show all agent statuses
            let statuses = status_tracker.get_all_statuses().await?;

            if self.json_output {
                println!("{}", serde_json::to_string_pretty(&statuses)?);
            } else {
                println!("📊 ccswarm Status");
                println!("================");

                if statuses.is_empty() {
                    println!("No agents found");
                } else {
                    for status in &statuses {
                        println!("Agent: {}", status["agent_id"]);
                        println!("  Status: {}", status["status"]);
                        println!("  Updated: {}", status["timestamp"]);

                        // Show role-specific summary for backend agents
                        if let Some(role) = status.get("role") {
                            if role.as_str() == Some("Backend") {
                                if let Some(backend_info) = status.get("backend_specific") {
                                    if let Some(api_health) = backend_info.get("api_health") {
                                        print!(
                                            "  API Health: {:.0}% | ",
                                            api_health.as_f64().unwrap_or(0.0) * 100.0
                                        );
                                    }
                                    if let Some(db) = backend_info.get("database") {
                                        print!(
                                            "DB: {} | ",
                                            if db["is_connected"].as_bool().unwrap_or(false) {
                                                "✓"
                                            } else {
                                                "✗"
                                            }
                                        );
                                    }
                                    if let Some(services) =
                                        backend_info.get("services").and_then(|s| s.as_array())
                                    {
                                        print!("Services: {}", services.len());
                                    }
                                    println!();
                                }
                            }
                        }

                        if detailed {
                            println!(
                                "  Details: {}",
                                serde_json::to_string_pretty(&status["additional_info"])?
                            );
                        }
                        println!();
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_evolution (lines 4581-4682) ---[0m
    async fn handle_evolution(&self, action: &EvolutionAction) -> Result<()> {
        match action {
            EvolutionAction::Metrics { agent, all, period } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution metrics",
                            "agent": agent,
                            "all": all,
                            "period_days": period,
                        }))?
                    );
                } else {
                    println!("📈 Evolution Metrics");
                    println!("==================");
                    if *all {
                        println!("Showing metrics for all agents");
                    } else if let Some(a) = agent {
                        println!("Agent: {}", a);
                    }
                    println!("Period: {} days", period);
                    println!("\nNo metrics available");
                }
            }

            EvolutionAction::Patterns {
                pattern_type,
                successful,
                failed,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution patterns",
                            "pattern_type": pattern_type,
                            "filter": {
                                "successful": successful,
                                "failed": failed,
                            },
                        }))?
                    );
                } else {
                    println!("🔮 Evolution Patterns");
                    println!("===================");
                    if *successful {
                        println!("Showing successful patterns");
                    } else if *failed {
                        println!("Showing failed patterns");
                    }
                    println!("No patterns found");
                }
            }

            EvolutionAction::Genealogy { extension, full } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension genealogy",
                            "extension": extension,
                            "full_tree": full,
                        }))?
                    );
                } else {
                    println!("🌳 Extension Genealogy: {}", extension);
                    println!("======================");
                    if *full {
                        println!("Showing full genealogy tree");
                    }
                    println!("No genealogy data available");
                }
            }

            EvolutionAction::Report { format, output } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution report generated",
                            "format": format,
                            "output": output,
                        }))?
                    );
                } else {
                    println!("📊 Generating evolution report");
                    println!("   Format: {}", format);
                    if let Some(o) = output {
                        println!("   Output: {}", o.display());
                    }
                    println!("✅ Report generated");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2546-2609 function create_default_config <-> ./crates/ccswarm/src/cli/mod.rs:3628-3804 method handle_sangha
  Similarity: 97.50%

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_default_config (lines 2546-2609) ---[0m
fn create_default_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut agents = std::collections::HashMap::new();

    // Add common agent configurations
    agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend-ui".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    agents.insert(
        "backend".to_string(),
        crate::config::AgentConfig {
            specialization: "node_microservices".to_string(),
            worktree: "agents/backend-agent".to_string(),
            branch: "feature/backend-api".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("backend"),
            claude_md_template: "backend_specialist".to_string(),
        },
    );

    agents.insert(
        "devops".to_string(),
        crate::config::AgentConfig {
            specialization: "aws_kubernetes".to_string(),
            worktree: "agents/devops-agent".to_string(),
            branch: "feature/infrastructure".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("devops"),
            claude_md_template: "devops_specialist".to_string(),
        },
    );

    Ok(CcswarmConfig {
        project: crate::config::ProjectConfig {
            name: "New ccswarm Project".to_string(),
            repository: crate::config::RepositoryConfig {
                url: repo_path.to_string_lossy().to_string(),
                main_branch: "main".to_string(),
            },
            master_claude: crate::config::MasterClaudeConfig {
                role: "technical_lead".to_string(),
                quality_threshold: 0.90,
                think_mode: crate::config::ThinkMode::UltraThink,
                permission_level: "supervised".to_string(),
                claude_config: crate::config::ClaudeConfig::for_master(),
                enable_proactive_mode: true, // デフォルト有効
                proactive_frequency: 30,     // 30秒間隔
                high_frequency: 15,          // 高頻度15秒間隔
            },
        },
        agents,
        coordination: crate::config::CoordinationConfig {
            communication_method: "json_files".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_commit".to_string(),
            master_review_trigger: "all_tasks_complete".to_string(),
        },
    })
}

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_sangha (lines 3628-3804) ---[0m
    async fn handle_sangha(&self, action: &SanghaAction) -> Result<()> {
        // Using stub implementation for now
        use crate::extension_stub::sangha::{Vote, VoteType};
        use std::str::FromStr;

        // Stub implementation - just log the action
        info!("🏛️  Sangha action requested (stub implementation)");

        match action {
            SanghaAction::Propose {
                proposal_type,
                file,
            } => {
                info!("📋 Submitting proposal to Sangha (stub implementation)");

                // Read proposal specification from file
                let _spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read proposal file")?;

                info!("Proposal type: {}", proposal_type);

                // Stub: Generate a fake proposal ID
                let proposal_id = uuid::Uuid::new_v4();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Proposal submitted",
                            "proposal_id": proposal_id,
                            "type": proposal_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!(
                        "📋 Submitting {} proposal from: {}",
                        proposal_type,
                        file.display()
                    );
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Proposal submitted successfully");
                }
            }

            SanghaAction::Vote {
                proposal_id,
                choice,
                reason,
            } => {
                info!("🗳️ Casting vote (stub implementation)");

                // Parse vote choice
                let vote_type = match choice.to_lowercase().as_str() {
                    "aye" | "yes" | "approve" => VoteType::Approve,
                    "nay" | "no" | "reject" => VoteType::Reject,
                    "abstain" => VoteType::Abstain,
                    "veto" | "needs_changes" => VoteType::NeedsChanges,
                    _ => {
                        anyhow::bail!(
                            "Invalid vote choice: {}. Use aye, nay, abstain, or veto",
                            choice
                        );
                    }
                };

                // Parse proposal ID
                let prop_id =
                    uuid::Uuid::from_str(proposal_id).context("Invalid proposal ID format")?;

                // Create vote (stub)
                let _vote = Vote::new("cli-user".to_string(), prop_id.to_string(), vote_type);

                info!("Vote created for proposal: {}", proposal_id);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Vote cast",
                            "proposal_id": proposal_id,
                            "choice": choice,
                            "reason": reason,
                        }))?
                    );
                } else {
                    println!("🗳️ Casting vote on proposal: {}", proposal_id);
                    println!("   Choice: {}", choice);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                    println!("✅ Vote cast successfully");
                }
            }

            SanghaAction::List { all, status } => {
                info!("📊 Listing Sangha status (stub implementation)");

                // Stub statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Sangha status retrieved (stub)",
                            "stats": {
                                "total_members": 1,
                                "active_members": 1,
                                "total_proposals": 0,
                                "active_proposals": 0,
                                "consensus_algorithm": "stub",
                            },
                            "show_all": all,
                            "filter_status": status,
                        }))?
                    );
                } else {
                    println!("📊 Sangha Status (Stub Implementation)");
                    println!("=====================================");
                    println!("👥 Members: 1 total, 1 active");
                    println!("📋 Proposals: 0 total, 0 active");
                    println!("🧠 Consensus Algorithm: stub");

                    println!(
                        "\n💡 No active proposals. Use 'ccswarm sangha propose' to create one."
                    );
                }
            }

            SanghaAction::Session { id, active } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session info",
                            "session_id": id,
                            "active": active,
                        }))?
                    );
                } else {
                    println!("🏛️ Sangha Session");
                    if *active {
                        println!("No active session");
                    }
                }
            }

            SanghaAction::ExtensionReview {
                proposal_id,
                technical_check,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension reviewed",
                            "proposal_id": proposal_id,
                            "technical_check": technical_check,
                        }))?
                    );
                } else {
                    println!("🔍 Reviewing extension proposal: {}", proposal_id);
                    if *technical_check {
                        println!("   Performing technical validation...");
                    }
                    println!("✅ Review completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1406-1543 method show_status <-> ./crates/ccswarm/src/cli/mod.rs:1835-1933 method show_task_status
  Similarity: 97.88%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_status (lines 1406-1543) ---[0m
    async fn show_status(&self, detailed: bool, agent: Option<&str>) -> Result<()> {
        // Read status from coordination files
        let status_tracker = crate::coordination::StatusTracker::new().await?;

        if let Some(agent_id) = agent {
            // Show specific agent status
            if let Some(status) = status_tracker.get_status(agent_id).await? {
                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&status)?);
                } else {
                    println!("Agent: {}", agent_id);
                    println!("Status: {}", status["status"]);
                    println!("Updated: {}", status["timestamp"]);

                    // Check if this is a backend agent and show backend-specific info
                    if let Some(role) = status.get("role") {
                        if role.as_str() == Some("Backend") {
                            if let Some(backend_info) = status.get("backend_specific") {
                                println!("\n🔧 Backend Status:");
                                if let Some(api_health) = backend_info.get("api_health") {
                                    println!(
                                        "  API Health: {:.1}%",
                                        api_health.as_f64().unwrap_or(0.0) * 100.0
                                    );
                                }
                                if let Some(db) = backend_info.get("database") {
                                    println!(
                                        "  Database: {} ({})",
                                        if db["is_connected"].as_bool().unwrap_or(false) {
                                            "Connected"
                                        } else {
                                            "Disconnected"
                                        },
                                        db["database_type"].as_str().unwrap_or("Unknown")
                                    );
                                }
                                if let Some(server) = backend_info.get("server") {
                                    println!(
                                        "  Server: {:.1}MB RAM, {:.1}% CPU",
                                        server["memory_usage_mb"].as_f64().unwrap_or(0.0),
                                        server["cpu_usage_percent"].as_f64().unwrap_or(0.0)
                                    );
                                }
                                if let Some(services) =
                                    backend_info.get("services").and_then(|s| s.as_array())
                                {
                                    println!("  Active Services: {}", services.len());
                                }
                                if let Some(activity) = backend_info.get("recent_activity") {
                                    println!(
                                        "  Recent API Calls: {}",
                                        activity.as_u64().unwrap_or(0)
                                    );
                                }
                            }
                        }
                    }

                    if detailed {
                        println!(
                            "\nDetails: {}",
                            serde_json::to_string_pretty(&status["additional_info"])?
                        );
                    }
                }
            } else if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "Agent not found",
                        "agent": agent_id,
                    }))?
                );
            } else {
                println!("❌ Agent '{}' not found", agent_id);
            }
        } else {
            // Show all agent statuses
            let statuses = status_tracker.get_all_statuses().await?;

            if self.json_output {
                println!("{}", serde_json::to_string_pretty(&statuses)?);
            } else {
                println!("📊 ccswarm Status");
                println!("================");

                if statuses.is_empty() {
                    println!("No agents found");
                } else {
                    for status in &statuses {
                        println!("Agent: {}", status["agent_id"]);
                        println!("  Status: {}", status["status"]);
                        println!("  Updated: {}", status["timestamp"]);

                        // Show role-specific summary for backend agents
                        if let Some(role) = status.get("role") {
                            if role.as_str() == Some("Backend") {
                                if let Some(backend_info) = status.get("backend_specific") {
                                    if let Some(api_health) = backend_info.get("api_health") {
                                        print!(
                                            "  API Health: {:.0}% | ",
                                            api_health.as_f64().unwrap_or(0.0) * 100.0
                                        );
                                    }
                                    if let Some(db) = backend_info.get("database") {
                                        print!(
                                            "DB: {} | ",
                                            if db["is_connected"].as_bool().unwrap_or(false) {
                                                "✓"
                                            } else {
                                                "✗"
                                            }
                                        );
                                    }
                                    if let Some(services) =
                                        backend_info.get("services").and_then(|s| s.as_array())
                                    {
                                        print!("Services: {}", services.len());
                                    }
                                    println!();
                                }
                            }
                        }

                        if detailed {
                            println!(
                                "  Details: {}",
                                serde_json::to_string_pretty(&status["additional_info"])?
                            );
                        }
                        println!();
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_status (lines 1835-1933) ---[0m
    async fn show_task_status(
        &self,
        task_id: &str,
        history: bool,
        orchestration: bool,
    ) -> Result<()> {
        let task = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.get_task(task_id).await
        } else {
            None
        };

        if let Some(task) = task {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "task": task,
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");

                let status_emoji = match &task.status {
                    TaskStatus::Pending => "⏳",
                    TaskStatus::Assigned { .. } => "📋",
                    TaskStatus::InProgress { .. } => "🏃",
                    TaskStatus::Completed { .. } => "✅",
                    TaskStatus::Failed { .. } => "❌",
                    TaskStatus::Cancelled { .. } => "🚫",
                };

                println!("{} Status: {:?}", status_emoji, task.status);
                println!("📝 Description: {}", task.task.description);
                println!("🎯 Priority: {:?}", task.task.priority);
                println!("📋 Type: {:?}", task.task.task_type);
                println!(
                    "⏰ Created: {}",
                    task.created_at.format("%Y-%m-%d %H:%M:%S")
                );
                println!(
                    "🔄 Updated: {}",
                    task.updated_at.format("%Y-%m-%d %H:%M:%S")
                );

                if let Some(agent) = &task.assigned_agent {
                    println!("👤 Assigned to: {}", agent);
                }

                if let Some(details) = &task.task.details {
                    println!("📝 Details: {}", details);
                }

                if history && !task.execution_history.is_empty() {
                    println!();
                    println!("📚 Execution History:");
                    for (i, attempt) in task.execution_history.iter().enumerate() {
                        println!(
                            "  {}. Agent: {} | Started: {}",
                            i + 1,
                            attempt.agent_id,
                            attempt.started_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(completed) = attempt.completed_at {
                            println!("     Completed: {}", completed.format("%Y-%m-%d %H:%M:%S"));
                        }
                        if let Some(error) = &attempt.error {
                            println!("     Error: {}", error);
                        }
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "status": "not_found",
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");
                println!("❌ Task not found");
                println!();
                println!("💡 Use 'ccswarm task list' to see available tasks");
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2637-2770 method handle_delegate <-> ./crates/ccswarm/src/cli/mod.rs:4581-4682 method handle_evolution
  Similarity: 97.96%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_delegate (lines 2637-2770) ---[0m
    async fn handle_delegate(&self, action: &DelegateAction) -> Result<()> {
        use crate::orchestrator::master_delegation::{DelegationStrategy, MasterDelegationEngine};

        match action {
            DelegateAction::Task {
                description,
                agent,
                priority,
                task_type,
                details,
                force,
            } => {
                let task = self.create_task_from_args(
                    description,
                    priority,
                    task_type,
                    details.as_deref(),
                    None,
                )?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Task delegated",
                            "task": task,
                            "target_agent": agent,
                            "forced": force,
                        }))?
                    );
                } else {
                    println!("🎯 Delegating task to {} agent", agent);
                    println!("   Task: {}", task.description);
                    println!("   Priority: {:?}", task.priority);
                    println!("   Type: {:?}", task.task_type);
                    if *force {
                        println!("   ⚠️ Forced delegation");
                    }
                }
            }

            DelegateAction::Analyze {
                description,
                verbose,
                strategy,
            } => {
                let strategy = match strategy.as_str() {
                    "content" => DelegationStrategy::ContentBased,
                    "load" => DelegationStrategy::LoadBalanced,
                    "expertise" => DelegationStrategy::ExpertiseBased,
                    "workflow" => DelegationStrategy::WorkflowBased,
                    "hybrid" => DelegationStrategy::Hybrid,
                    _ => DelegationStrategy::Hybrid,
                };

                let mut engine = MasterDelegationEngine::new(strategy);
                let task = Task::new(
                    "analysis".to_string(),
                    description.clone(),
                    Priority::Medium,
                    TaskType::Development,
                );

                let decision = engine.delegate_task(task)?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&decision)?);
                } else {
                    println!("🔍 Task Analysis Results");
                    println!("   Task: {}", description);
                    println!("   Recommended Agent: {}", decision.target_agent.name());
                    println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                    if *verbose {
                        println!("   Reasoning: {}", decision.reasoning);
                        if let Some(duration) = decision.estimated_duration {
                            println!("   Estimated Duration: {:?}", duration);
                        }
                    }
                }
            }

            DelegateAction::Stats { detailed, period } => {
                // TODO: Implement delegation statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Delegation statistics",
                            "period_hours": period,
                            "detailed": detailed,
                        }))?
                    );
                } else {
                    println!("📊 Delegation Statistics (last {} hours)", period);
                    println!("   Feature not yet implemented");
                }
            }

            DelegateAction::Interactive => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "error": "Interactive mode not available in JSON output",
                        }))?
                    );
                } else {
                    println!("🖥️ Interactive Delegation Mode");
                    self.run_interactive_delegation().await?;
                }
            }

            DelegateAction::Show { file } => {
                let config = CcswarmConfig::from_file(file.clone()).await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&config)?);
                } else {
                    println!("📄 Delegation Configuration: {}", file.display());
                    println!("========================");
                    println!("Project: {}", config.project.name);
                    println!("Repository: {}", config.project.repository.url);
                    println!("Agents: {}", config.agents.len());
                    for (name, agent_config) in &config.agents {
                        println!("  - {}: {}", name, agent_config.specialization);
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_evolution (lines 4581-4682) ---[0m
    async fn handle_evolution(&self, action: &EvolutionAction) -> Result<()> {
        match action {
            EvolutionAction::Metrics { agent, all, period } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution metrics",
                            "agent": agent,
                            "all": all,
                            "period_days": period,
                        }))?
                    );
                } else {
                    println!("📈 Evolution Metrics");
                    println!("==================");
                    if *all {
                        println!("Showing metrics for all agents");
                    } else if let Some(a) = agent {
                        println!("Agent: {}", a);
                    }
                    println!("Period: {} days", period);
                    println!("\nNo metrics available");
                }
            }

            EvolutionAction::Patterns {
                pattern_type,
                successful,
                failed,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution patterns",
                            "pattern_type": pattern_type,
                            "filter": {
                                "successful": successful,
                                "failed": failed,
                            },
                        }))?
                    );
                } else {
                    println!("🔮 Evolution Patterns");
                    println!("===================");
                    if *successful {
                        println!("Showing successful patterns");
                    } else if *failed {
                        println!("Showing failed patterns");
                    }
                    println!("No patterns found");
                }
            }

            EvolutionAction::Genealogy { extension, full } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension genealogy",
                            "extension": extension,
                            "full_tree": full,
                        }))?
                    );
                } else {
                    println!("🌳 Extension Genealogy: {}", extension);
                    println!("======================");
                    if *full {
                        println!("Showing full genealogy tree");
                    }
                    println!("No genealogy data available");
                }
            }

            EvolutionAction::Report { format, output } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution report generated",
                            "format": format,
                            "output": output,
                        }))?
                    );
                } else {
                    println!("📊 Generating evolution report");
                    println!("   Format: {}", format);
                    if let Some(o) = output {
                        println!("   Output: {}", o.display());
                    }
                    println!("✅ Report generated");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1406-1543 method show_status <-> ./crates/ccswarm/src/cli/mod.rs:5468-5582 method handle_health
  Similarity: 91.12%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_status (lines 1406-1543) ---[0m
    async fn show_status(&self, detailed: bool, agent: Option<&str>) -> Result<()> {
        // Read status from coordination files
        let status_tracker = crate::coordination::StatusTracker::new().await?;

        if let Some(agent_id) = agent {
            // Show specific agent status
            if let Some(status) = status_tracker.get_status(agent_id).await? {
                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&status)?);
                } else {
                    println!("Agent: {}", agent_id);
                    println!("Status: {}", status["status"]);
                    println!("Updated: {}", status["timestamp"]);

                    // Check if this is a backend agent and show backend-specific info
                    if let Some(role) = status.get("role") {
                        if role.as_str() == Some("Backend") {
                            if let Some(backend_info) = status.get("backend_specific") {
                                println!("\n🔧 Backend Status:");
                                if let Some(api_health) = backend_info.get("api_health") {
                                    println!(
                                        "  API Health: {:.1}%",
                                        api_health.as_f64().unwrap_or(0.0) * 100.0
                                    );
                                }
                                if let Some(db) = backend_info.get("database") {
                                    println!(
                                        "  Database: {} ({})",
                                        if db["is_connected"].as_bool().unwrap_or(false) {
                                            "Connected"
                                        } else {
                                            "Disconnected"
                                        },
                                        db["database_type"].as_str().unwrap_or("Unknown")
                                    );
                                }
                                if let Some(server) = backend_info.get("server") {
                                    println!(
                                        "  Server: {:.1}MB RAM, {:.1}% CPU",
                                        server["memory_usage_mb"].as_f64().unwrap_or(0.0),
                                        server["cpu_usage_percent"].as_f64().unwrap_or(0.0)
                                    );
                                }
                                if let Some(services) =
                                    backend_info.get("services").and_then(|s| s.as_array())
                                {
                                    println!("  Active Services: {}", services.len());
                                }
                                if let Some(activity) = backend_info.get("recent_activity") {
                                    println!(
                                        "  Recent API Calls: {}",
                                        activity.as_u64().unwrap_or(0)
                                    );
                                }
                            }
                        }
                    }

                    if detailed {
                        println!(
                            "\nDetails: {}",
                            serde_json::to_string_pretty(&status["additional_info"])?
                        );
                    }
                }
            } else if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "Agent not found",
                        "agent": agent_id,
                    }))?
                );
            } else {
                println!("❌ Agent '{}' not found", agent_id);
            }
        } else {
            // Show all agent statuses
            let statuses = status_tracker.get_all_statuses().await?;

            if self.json_output {
                println!("{}", serde_json::to_string_pretty(&statuses)?);
            } else {
                println!("📊 ccswarm Status");
                println!("================");

                if statuses.is_empty() {
                    println!("No agents found");
                } else {
                    for status in &statuses {
                        println!("Agent: {}", status["agent_id"]);
                        println!("  Status: {}", status["status"]);
                        println!("  Updated: {}", status["timestamp"]);

                        // Show role-specific summary for backend agents
                        if let Some(role) = status.get("role") {
                            if role.as_str() == Some("Backend") {
                                if let Some(backend_info) = status.get("backend_specific") {
                                    if let Some(api_health) = backend_info.get("api_health") {
                                        print!(
                                            "  API Health: {:.0}% | ",
                                            api_health.as_f64().unwrap_or(0.0) * 100.0
                                        );
                                    }
                                    if let Some(db) = backend_info.get("database") {
                                        print!(
                                            "DB: {} | ",
                                            if db["is_connected"].as_bool().unwrap_or(false) {
                                                "✓"
                                            } else {
                                                "✗"
                                            }
                                        );
                                    }
                                    if let Some(services) =
                                        backend_info.get("services").and_then(|s| s.as_array())
                                    {
                                        print!("Services: {}", services.len());
                                    }
                                    println!();
                                }
                            }
                        }

                        if detailed {
                            println!(
                                "  Details: {}",
                                serde_json::to_string_pretty(&status["additional_info"])?
                            );
                        }
                        println!();
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_health (lines 5468-5582) ---[0m
    async fn handle_health(
        &self,
        check_agents: bool,
        check_sessions: bool,
        resources: bool,
        diagnose: bool,
        detailed: bool,
        format: &str,
    ) -> Result<()> {
        use crate::cli::health::{run_diagnostics, HealthChecker};
        use crate::coordination::StatusTracker;

        // Run diagnostics if requested
        if diagnose {
            return run_diagnostics(&self.repo_path).await;
        }

        // Initialize health checker
        let status_tracker = StatusTracker::new().await?;
        let health_checker = HealthChecker::new(status_tracker);

        // Add monitoring system if available
        // TODO: Get monitoring system from running orchestrator
        // let monitoring = MonitoringSystem::new();
        // let health_checker = health_checker.with_monitoring(monitoring);

        // Perform health checks based on flags
        let report = if check_agents && !check_sessions && !resources {
            // Only check agents
            let checks = health_checker.check_agents_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let total_agents = checks.len();
            let healthy_agents = checks
                .iter()
                .filter(|c| c.status == health::HealthStatus::Healthy)
                .count();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents,
                healthy_agents,
                active_tasks: 0,
                session_count: 0,
            }
        } else if check_sessions && !check_agents && !resources {
            // Only check sessions
            let checks = health_checker.check_sessions_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let session_count = checks.len();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents: 0,
                healthy_agents: 0,
                active_tasks: 0,
                session_count,
            }
        } else {
            // Full health check
            health_checker.check_all().await?
        };

        // Output results based on format
        match format {
            "json" => {
                println!("{}", serde_json::to_string_pretty(&report)?);
            }
            _ => {
                if detailed {
                    report.print_detailed();
                } else {
                    report.print_summary();
                }
            }
        }

        // Exit with non-zero code if unhealthy
        if report.overall_status == health::HealthStatus::Critical {
            std::process::exit(1);
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1406-1543 method show_status <-> ./crates/ccswarm/src/cli/mod.rs:1737-1832 method list_tasks
  Similarity: 97.72%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_status (lines 1406-1543) ---[0m
    async fn show_status(&self, detailed: bool, agent: Option<&str>) -> Result<()> {
        // Read status from coordination files
        let status_tracker = crate::coordination::StatusTracker::new().await?;

        if let Some(agent_id) = agent {
            // Show specific agent status
            if let Some(status) = status_tracker.get_status(agent_id).await? {
                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&status)?);
                } else {
                    println!("Agent: {}", agent_id);
                    println!("Status: {}", status["status"]);
                    println!("Updated: {}", status["timestamp"]);

                    // Check if this is a backend agent and show backend-specific info
                    if let Some(role) = status.get("role") {
                        if role.as_str() == Some("Backend") {
                            if let Some(backend_info) = status.get("backend_specific") {
                                println!("\n🔧 Backend Status:");
                                if let Some(api_health) = backend_info.get("api_health") {
                                    println!(
                                        "  API Health: {:.1}%",
                                        api_health.as_f64().unwrap_or(0.0) * 100.0
                                    );
                                }
                                if let Some(db) = backend_info.get("database") {
                                    println!(
                                        "  Database: {} ({})",
                                        if db["is_connected"].as_bool().unwrap_or(false) {
                                            "Connected"
                                        } else {
                                            "Disconnected"
                                        },
                                        db["database_type"].as_str().unwrap_or("Unknown")
                                    );
                                }
                                if let Some(server) = backend_info.get("server") {
                                    println!(
                                        "  Server: {:.1}MB RAM, {:.1}% CPU",
                                        server["memory_usage_mb"].as_f64().unwrap_or(0.0),
                                        server["cpu_usage_percent"].as_f64().unwrap_or(0.0)
                                    );
                                }
                                if let Some(services) =
                                    backend_info.get("services").and_then(|s| s.as_array())
                                {
                                    println!("  Active Services: {}", services.len());
                                }
                                if let Some(activity) = backend_info.get("recent_activity") {
                                    println!(
                                        "  Recent API Calls: {}",
                                        activity.as_u64().unwrap_or(0)
                                    );
                                }
                            }
                        }
                    }

                    if detailed {
                        println!(
                            "\nDetails: {}",
                            serde_json::to_string_pretty(&status["additional_info"])?
                        );
                    }
                }
            } else if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "Agent not found",
                        "agent": agent_id,
                    }))?
                );
            } else {
                println!("❌ Agent '{}' not found", agent_id);
            }
        } else {
            // Show all agent statuses
            let statuses = status_tracker.get_all_statuses().await?;

            if self.json_output {
                println!("{}", serde_json::to_string_pretty(&statuses)?);
            } else {
                println!("📊 ccswarm Status");
                println!("================");

                if statuses.is_empty() {
                    println!("No agents found");
                } else {
                    for status in &statuses {
                        println!("Agent: {}", status["agent_id"]);
                        println!("  Status: {}", status["status"]);
                        println!("  Updated: {}", status["timestamp"]);

                        // Show role-specific summary for backend agents
                        if let Some(role) = status.get("role") {
                            if role.as_str() == Some("Backend") {
                                if let Some(backend_info) = status.get("backend_specific") {
                                    if let Some(api_health) = backend_info.get("api_health") {
                                        print!(
                                            "  API Health: {:.0}% | ",
                                            api_health.as_f64().unwrap_or(0.0) * 100.0
                                        );
                                    }
                                    if let Some(db) = backend_info.get("database") {
                                        print!(
                                            "DB: {} | ",
                                            if db["is_connected"].as_bool().unwrap_or(false) {
                                                "✓"
                                            } else {
                                                "✗"
                                            }
                                        );
                                    }
                                    if let Some(services) =
                                        backend_info.get("services").and_then(|s| s.as_array())
                                    {
                                        print!("Services: {}", services.len());
                                    }
                                    println!();
                                }
                            }
                        }

                        if detailed {
                            println!(
                                "  Details: {}",
                                serde_json::to_string_pretty(&status["additional_info"])?
                            );
                        }
                        println!();
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_tasks (lines 1737-1832) ---[0m
    async fn list_tasks(
        &self,
        all: bool,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
        detailed: bool,
    ) -> Result<()> {
        let tasks = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.list_tasks(status_filter, agent_filter).await
        } else {
            Vec::new()
        };

        let displayed_tasks = if all {
            tasks
        } else {
            tasks.into_iter().take(50).collect()
        };

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "tasks": displayed_tasks,
                    "total": displayed_tasks.len(),
                    "filters": {
                        "all": all,
                        "status": status_filter,
                        "agent": agent_filter,
                        "detailed": detailed
                    }
                }))?
            );
        } else {
            println!("📋 Task List");
            println!("============");

            if displayed_tasks.is_empty() {
                println!("No tasks in queue currently.");
                println!();
                println!("💡 Add a task with: ccswarm task add \"Your task description\"");
            } else {
                println!("Found {} tasks:", displayed_tasks.len());
                println!();

                for task in &displayed_tasks {
                    let status_emoji = match &task.status {
                        TaskStatus::Pending => "⏳",
                        TaskStatus::Assigned { .. } => "📋",
                        TaskStatus::InProgress { .. } => "🏃",
                        TaskStatus::Completed { .. } => "✅",
                        TaskStatus::Failed { .. } => "❌",
                        TaskStatus::Cancelled { .. } => "🚫",
                    };

                    let priority_emoji = match task.task.priority {
                        Priority::Critical => "🚨",
                        Priority::High => "🔥",
                        Priority::Medium => "📅",
                        Priority::Low => "💤",
                    };

                    println!(
                        "{} {} {} [{}] {}",
                        status_emoji,
                        priority_emoji,
                        &task.task.id[..8], // Short ID
                        task.task.task_type,
                        task.task.description
                    );

                    if let Some(agent) = &task.assigned_agent {
                        println!("   👤 Assigned to: {}", agent);
                    }

                    if detailed {
                        println!(
                            "   ⏰ Created: {}",
                            task.created_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        println!(
                            "   🔄 Updated: {}",
                            task.updated_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(details) = &task.task.details {
                            println!("   📝 Details: {}", details);
                        }
                    }
                    println!();
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1545-1666 method add_task <-> ./crates/ccswarm/src/cli/mod.rs:5468-5582 method handle_health
  Similarity: 95.67%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:add_task (lines 1545-1666) ---[0m
    async fn add_task(
        &self,
        description: &str,
        priority: &str,
        task_type: &str,
        details: Option<&str>,
        duration: Option<u32>,
    ) -> Result<()> {
        // use crate::cli::progress::{ProgressStyle, ProgressTracker};
        use crate::utils::user_error::CommonErrors;

        // Validate task description
        if description.trim().is_empty() {
            CommonErrors::invalid_task_format()
                .with_details("Task description cannot be empty")
                .with_suggestion("Provide a clear, actionable task description")
                .display();
            return Err(anyhow!("Invalid task description"));
        }

        // Create task (simplified progress display)
        println!(
            "Creating task: {}...",
            description.chars().take(50).collect::<String>()
        );

        let priority = match priority.to_lowercase().as_str() {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };

        let task_type = match task_type.to_lowercase().as_str() {
            "development" | "dev" => TaskType::Development,
            "testing" | "test" => TaskType::Testing,
            "documentation" | "docs" => TaskType::Documentation,
            "infrastructure" | "infra" => TaskType::Infrastructure,
            "coordination" => TaskType::Coordination,
            "review" => TaskType::Review,
            "bugfix" | "bug" => TaskType::Bugfix,
            "feature" => TaskType::Feature,
            _ => TaskType::Development,
        };

        let task_type_clone = task_type;
        let mut task = Task::new(
            uuid::Uuid::new_v4().to_string(),
            description.to_string(),
            priority,
            task_type,
        );

        if let Some(details) = details {
            task = task.with_details(details.to_string());
        }

        if let Some(duration) = duration {
            task = task.with_duration(duration);
        }

        // Add to task queue via execution engine
        let task_id = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.add_task(task.clone()).await
        } else {
            // Fall back to creating task with ID but warn user
            warn!("Execution engine not available, task will not be executed");
            task.id.clone()
        };

        // Complete progress indicator
        // ProgressTracker::complete(progress, true, Some("Task created successfully".to_string())).await;

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Task added",
                    "task_id": task_id,
                    "description": description,
                    "priority": priority,
                }))?
            );
        } else {
            println!();
            println!("{}", "✅ Task created successfully!".bright_green().bold());
            println!();
            println!(
                "   {} {}",
                "Task ID:".bright_cyan(),
                task_id[..8].bright_white()
            );
            println!("   {} {}", "Description:".bright_cyan(), description);
            println!(
                "   {} {}",
                "Priority:".bright_cyan(),
                match priority {
                    Priority::Critical => "🔴 Critical".bright_red(),
                    Priority::High => "🟡 High".bright_yellow(),
                    Priority::Medium => "🟢 Medium".bright_green(),
                    Priority::Low => "🔵 Low".bright_blue(),
                }
            );
            println!(
                "   {} {}",
                "Type:".bright_cyan(),
                format!("{:?}", task_type_clone).bright_white()
            );

            if let Some(duration) = task.estimated_duration {
                println!("   {} {} minutes", "Est. Duration:".bright_cyan(), duration);
            }

            // Show helpful next steps
            show_quick_help("task-created");
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_health (lines 5468-5582) ---[0m
    async fn handle_health(
        &self,
        check_agents: bool,
        check_sessions: bool,
        resources: bool,
        diagnose: bool,
        detailed: bool,
        format: &str,
    ) -> Result<()> {
        use crate::cli::health::{run_diagnostics, HealthChecker};
        use crate::coordination::StatusTracker;

        // Run diagnostics if requested
        if diagnose {
            return run_diagnostics(&self.repo_path).await;
        }

        // Initialize health checker
        let status_tracker = StatusTracker::new().await?;
        let health_checker = HealthChecker::new(status_tracker);

        // Add monitoring system if available
        // TODO: Get monitoring system from running orchestrator
        // let monitoring = MonitoringSystem::new();
        // let health_checker = health_checker.with_monitoring(monitoring);

        // Perform health checks based on flags
        let report = if check_agents && !check_sessions && !resources {
            // Only check agents
            let checks = health_checker.check_agents_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let total_agents = checks.len();
            let healthy_agents = checks
                .iter()
                .filter(|c| c.status == health::HealthStatus::Healthy)
                .count();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents,
                healthy_agents,
                active_tasks: 0,
                session_count: 0,
            }
        } else if check_sessions && !check_agents && !resources {
            // Only check sessions
            let checks = health_checker.check_sessions_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let session_count = checks.len();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents: 0,
                healthy_agents: 0,
                active_tasks: 0,
                session_count,
            }
        } else {
            // Full health check
            health_checker.check_all().await?
        };

        // Output results based on format
        match format {
            "json" => {
                println!("{}", serde_json::to_string_pretty(&report)?);
            }
            _ => {
                if detailed {
                    report.print_detailed();
                } else {
                    report.print_summary();
                }
            }
        }

        // Exit with non-zero code if unhealthy
        if report.overall_status == health::HealthStatus::Critical {
            std::process::exit(1);
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1545-1666 method add_task <-> ./crates/ccswarm/src/cli/mod.rs:2279-2458 method handle_worktree
  Similarity: 74.00%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:add_task (lines 1545-1666) ---[0m
    async fn add_task(
        &self,
        description: &str,
        priority: &str,
        task_type: &str,
        details: Option<&str>,
        duration: Option<u32>,
    ) -> Result<()> {
        // use crate::cli::progress::{ProgressStyle, ProgressTracker};
        use crate::utils::user_error::CommonErrors;

        // Validate task description
        if description.trim().is_empty() {
            CommonErrors::invalid_task_format()
                .with_details("Task description cannot be empty")
                .with_suggestion("Provide a clear, actionable task description")
                .display();
            return Err(anyhow!("Invalid task description"));
        }

        // Create task (simplified progress display)
        println!(
            "Creating task: {}...",
            description.chars().take(50).collect::<String>()
        );

        let priority = match priority.to_lowercase().as_str() {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };

        let task_type = match task_type.to_lowercase().as_str() {
            "development" | "dev" => TaskType::Development,
            "testing" | "test" => TaskType::Testing,
            "documentation" | "docs" => TaskType::Documentation,
            "infrastructure" | "infra" => TaskType::Infrastructure,
            "coordination" => TaskType::Coordination,
            "review" => TaskType::Review,
            "bugfix" | "bug" => TaskType::Bugfix,
            "feature" => TaskType::Feature,
            _ => TaskType::Development,
        };

        let task_type_clone = task_type;
        let mut task = Task::new(
            uuid::Uuid::new_v4().to_string(),
            description.to_string(),
            priority,
            task_type,
        );

        if let Some(details) = details {
            task = task.with_details(details.to_string());
        }

        if let Some(duration) = duration {
            task = task.with_duration(duration);
        }

        // Add to task queue via execution engine
        let task_id = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.add_task(task.clone()).await
        } else {
            // Fall back to creating task with ID but warn user
            warn!("Execution engine not available, task will not be executed");
            task.id.clone()
        };

        // Complete progress indicator
        // ProgressTracker::complete(progress, true, Some("Task created successfully".to_string())).await;

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Task added",
                    "task_id": task_id,
                    "description": description,
                    "priority": priority,
                }))?
            );
        } else {
            println!();
            println!("{}", "✅ Task created successfully!".bright_green().bold());
            println!();
            println!(
                "   {} {}",
                "Task ID:".bright_cyan(),
                task_id[..8].bright_white()
            );
            println!("   {} {}", "Description:".bright_cyan(), description);
            println!(
                "   {} {}",
                "Priority:".bright_cyan(),
                match priority {
                    Priority::Critical => "🔴 Critical".bright_red(),
                    Priority::High => "🟡 High".bright_yellow(),
                    Priority::Medium => "🟢 Medium".bright_green(),
                    Priority::Low => "🔵 Low".bright_blue(),
                }
            );
            println!(
                "   {} {}",
                "Type:".bright_cyan(),
                format!("{:?}", task_type_clone).bright_white()
            );

            if let Some(duration) = task.estimated_duration {
                println!("   {} {} minutes", "Est. Duration:".bright_cyan(), duration);
            }

            // Show helpful next steps
            show_quick_help("task-created");
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_worktree (lines 2279-2458) ---[0m
    async fn handle_worktree(&self, action: &WorktreeAction) -> Result<()> {
        let manager = crate::git::shell::ShellWorktreeManager::new(self.repo_path.clone())?;

        match action {
            WorktreeAction::List => {
                let worktrees = manager.list_worktrees().await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&worktrees)?);
                } else {
                    println!("🌳 Git Worktrees");
                    println!("===============");

                    for wt in &worktrees {
                        println!("Path: {}", wt.path.display());
                        println!("  Branch: {}", wt.branch);
                        println!("  Head: {}", wt.head_commit);
                        println!("  Locked: {}", wt.is_locked);
                        println!();
                    }
                }
            }
            WorktreeAction::Create {
                path,
                branch,
                new_branch,
            } => {
                let info = if *new_branch {
                    manager.create_worktree_full(path, branch, true).await?
                } else {
                    manager.create_worktree(path, branch).await?
                };

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&info)?);
                } else {
                    println!("✅ Worktree created");
                    println!("   Path: {}", info.path.display());
                    println!("   Branch: {}", info.branch);
                }
            }
            WorktreeAction::Remove { path, force } => {
                if *force {
                    manager.remove_worktree_full(path, true).await?
                } else {
                    manager.remove_worktree(path).await?
                };

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktree removed",
                            "path": path,
                        }))?
                    );
                } else {
                    println!("✅ Worktree removed: {}", path.display());
                }
            }
            WorktreeAction::Prune => {
                manager.prune_worktrees().await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Worktrees pruned",
                        }))?
                    );
                } else {
                    println!("✅ Stale worktrees pruned");
                }
            }
            WorktreeAction::Clean { force } => {
                use std::io::{self, Write};

                // Find all ccswarm-related worktrees
                let worktrees = manager.list_worktrees().await?;
                let ccswarm_worktrees: Vec<_> = worktrees
                    .iter()
                    .filter(|w| w.branch.contains("agent") || w.branch.contains("feature/"))
                    .collect();

                if ccswarm_worktrees.is_empty() {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "No ccswarm worktrees found",
                            }))?
                        );
                    } else {
                        println!("✅ No ccswarm worktrees to clean");
                    }
                    return Ok(());
                }

                // Ask for confirmation unless forced
                if !force {
                    println!("⚠️  Found {} ccswarm worktrees:", ccswarm_worktrees.len());
                    for w in &ccswarm_worktrees {
                        println!("   - {} ({})", w.path.display(), w.branch);
                    }
                    print!("\nAre you sure you want to remove all these worktrees? [y/N] ");
                    io::stdout().flush()?;

                    let mut response = String::new();
                    io::stdin().read_line(&mut response)?;

                    if !response.trim().eq_ignore_ascii_case("y") {
                        println!("❌ Cleanup cancelled");
                        return Ok(());
                    }
                }

                // Remove all ccswarm worktrees
                let mut removed_count = 0;
                for worktree in ccswarm_worktrees {
                    match manager.remove_worktree(&worktree.path).await {
                        Ok(_) => {
                            removed_count += 1;
                            if !self.json_output {
                                println!("   ✓ Removed {}", worktree.path.display());
                            }
                        }
                        Err(e) => {
                            if !self.json_output {
                                println!(
                                    "   ✗ Failed to remove {}: {}",
                                    worktree.path.display(),
                                    e
                                );
                            }
                        }
                    }
                }

                // Also clean up branches
                let output = tokio::process::Command::new("git")
                    .args(["branch", "--list", "*agent*", "feature/*"])
                    .output()
                    .await?;

                if output.status.success() {
                    let branches = String::from_utf8_lossy(&output.stdout);
                    let branch_count = branches.lines().count();

                    if branch_count > 0 {
                        tokio::process::Command::new("git")
                            .args(&["branch", "-D"])
                            .args(branches.lines().map(|b| b.trim().trim_start_matches("* ")))
                            .output()
                            .await?;
                    }
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Cleanup completed",
                            "worktrees_removed": removed_count,
                        }))?
                    );
                } else {
                    println!(
                        "\n✅ Cleanup completed: {} worktrees removed",
                        removed_count
                    );
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1835-1933 method show_task_status <-> ./crates/ccswarm/src/cli/mod.rs:2637-2770 method handle_delegate
  Similarity: 95.47%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_status (lines 1835-1933) ---[0m
    async fn show_task_status(
        &self,
        task_id: &str,
        history: bool,
        orchestration: bool,
    ) -> Result<()> {
        let task = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.get_task(task_id).await
        } else {
            None
        };

        if let Some(task) = task {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "task": task,
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");

                let status_emoji = match &task.status {
                    TaskStatus::Pending => "⏳",
                    TaskStatus::Assigned { .. } => "📋",
                    TaskStatus::InProgress { .. } => "🏃",
                    TaskStatus::Completed { .. } => "✅",
                    TaskStatus::Failed { .. } => "❌",
                    TaskStatus::Cancelled { .. } => "🚫",
                };

                println!("{} Status: {:?}", status_emoji, task.status);
                println!("📝 Description: {}", task.task.description);
                println!("🎯 Priority: {:?}", task.task.priority);
                println!("📋 Type: {:?}", task.task.task_type);
                println!(
                    "⏰ Created: {}",
                    task.created_at.format("%Y-%m-%d %H:%M:%S")
                );
                println!(
                    "🔄 Updated: {}",
                    task.updated_at.format("%Y-%m-%d %H:%M:%S")
                );

                if let Some(agent) = &task.assigned_agent {
                    println!("👤 Assigned to: {}", agent);
                }

                if let Some(details) = &task.task.details {
                    println!("📝 Details: {}", details);
                }

                if history && !task.execution_history.is_empty() {
                    println!();
                    println!("📚 Execution History:");
                    for (i, attempt) in task.execution_history.iter().enumerate() {
                        println!(
                            "  {}. Agent: {} | Started: {}",
                            i + 1,
                            attempt.agent_id,
                            attempt.started_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(completed) = attempt.completed_at {
                            println!("     Completed: {}", completed.format("%Y-%m-%d %H:%M:%S"));
                        }
                        if let Some(error) = &attempt.error {
                            println!("     Error: {}", error);
                        }
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "status": "not_found",
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");
                println!("❌ Task not found");
                println!();
                println!("💡 Use 'ccswarm task list' to see available tasks");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_delegate (lines 2637-2770) ---[0m
    async fn handle_delegate(&self, action: &DelegateAction) -> Result<()> {
        use crate::orchestrator::master_delegation::{DelegationStrategy, MasterDelegationEngine};

        match action {
            DelegateAction::Task {
                description,
                agent,
                priority,
                task_type,
                details,
                force,
            } => {
                let task = self.create_task_from_args(
                    description,
                    priority,
                    task_type,
                    details.as_deref(),
                    None,
                )?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Task delegated",
                            "task": task,
                            "target_agent": agent,
                            "forced": force,
                        }))?
                    );
                } else {
                    println!("🎯 Delegating task to {} agent", agent);
                    println!("   Task: {}", task.description);
                    println!("   Priority: {:?}", task.priority);
                    println!("   Type: {:?}", task.task_type);
                    if *force {
                        println!("   ⚠️ Forced delegation");
                    }
                }
            }

            DelegateAction::Analyze {
                description,
                verbose,
                strategy,
            } => {
                let strategy = match strategy.as_str() {
                    "content" => DelegationStrategy::ContentBased,
                    "load" => DelegationStrategy::LoadBalanced,
                    "expertise" => DelegationStrategy::ExpertiseBased,
                    "workflow" => DelegationStrategy::WorkflowBased,
                    "hybrid" => DelegationStrategy::Hybrid,
                    _ => DelegationStrategy::Hybrid,
                };

                let mut engine = MasterDelegationEngine::new(strategy);
                let task = Task::new(
                    "analysis".to_string(),
                    description.clone(),
                    Priority::Medium,
                    TaskType::Development,
                );

                let decision = engine.delegate_task(task)?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&decision)?);
                } else {
                    println!("🔍 Task Analysis Results");
                    println!("   Task: {}", description);
                    println!("   Recommended Agent: {}", decision.target_agent.name());
                    println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                    if *verbose {
                        println!("   Reasoning: {}", decision.reasoning);
                        if let Some(duration) = decision.estimated_duration {
                            println!("   Estimated Duration: {:?}", duration);
                        }
                    }
                }
            }

            DelegateAction::Stats { detailed, period } => {
                // TODO: Implement delegation statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Delegation statistics",
                            "period_hours": period,
                            "detailed": detailed,
                        }))?
                    );
                } else {
                    println!("📊 Delegation Statistics (last {} hours)", period);
                    println!("   Feature not yet implemented");
                }
            }

            DelegateAction::Interactive => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "error": "Interactive mode not available in JSON output",
                        }))?
                    );
                } else {
                    println!("🖥️ Interactive Delegation Mode");
                    self.run_interactive_delegation().await?;
                }
            }

            DelegateAction::Show { file } => {
                let config = CcswarmConfig::from_file(file.clone()).await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&config)?);
                } else {
                    println!("📄 Delegation Configuration: {}", file.display());
                    println!("========================");
                    println!("Project: {}", config.project.name);
                    println!("Repository: {}", config.project.repository.url);
                    println!("Agents: {}", config.agents.len());
                    for (name, agent_config) in &config.agents {
                        println!("  - {}: {}", name, agent_config.specialization);
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1545-1666 method add_task <-> ./crates/ccswarm/src/cli/mod.rs:4173-4336 method handle_search
  Similarity: 76.36%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:add_task (lines 1545-1666) ---[0m
    async fn add_task(
        &self,
        description: &str,
        priority: &str,
        task_type: &str,
        details: Option<&str>,
        duration: Option<u32>,
    ) -> Result<()> {
        // use crate::cli::progress::{ProgressStyle, ProgressTracker};
        use crate::utils::user_error::CommonErrors;

        // Validate task description
        if description.trim().is_empty() {
            CommonErrors::invalid_task_format()
                .with_details("Task description cannot be empty")
                .with_suggestion("Provide a clear, actionable task description")
                .display();
            return Err(anyhow!("Invalid task description"));
        }

        // Create task (simplified progress display)
        println!(
            "Creating task: {}...",
            description.chars().take(50).collect::<String>()
        );

        let priority = match priority.to_lowercase().as_str() {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };

        let task_type = match task_type.to_lowercase().as_str() {
            "development" | "dev" => TaskType::Development,
            "testing" | "test" => TaskType::Testing,
            "documentation" | "docs" => TaskType::Documentation,
            "infrastructure" | "infra" => TaskType::Infrastructure,
            "coordination" => TaskType::Coordination,
            "review" => TaskType::Review,
            "bugfix" | "bug" => TaskType::Bugfix,
            "feature" => TaskType::Feature,
            _ => TaskType::Development,
        };

        let task_type_clone = task_type;
        let mut task = Task::new(
            uuid::Uuid::new_v4().to_string(),
            description.to_string(),
            priority,
            task_type,
        );

        if let Some(details) = details {
            task = task.with_details(details.to_string());
        }

        if let Some(duration) = duration {
            task = task.with_duration(duration);
        }

        // Add to task queue via execution engine
        let task_id = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.add_task(task.clone()).await
        } else {
            // Fall back to creating task with ID but warn user
            warn!("Execution engine not available, task will not be executed");
            task.id.clone()
        };

        // Complete progress indicator
        // ProgressTracker::complete(progress, true, Some("Task created successfully".to_string())).await;

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Task added",
                    "task_id": task_id,
                    "description": description,
                    "priority": priority,
                }))?
            );
        } else {
            println!();
            println!("{}", "✅ Task created successfully!".bright_green().bold());
            println!();
            println!(
                "   {} {}",
                "Task ID:".bright_cyan(),
                task_id[..8].bright_white()
            );
            println!("   {} {}", "Description:".bright_cyan(), description);
            println!(
                "   {} {}",
                "Priority:".bright_cyan(),
                match priority {
                    Priority::Critical => "🔴 Critical".bright_red(),
                    Priority::High => "🟡 High".bright_yellow(),
                    Priority::Medium => "🟢 Medium".bright_green(),
                    Priority::Low => "🔵 Low".bright_blue(),
                }
            );
            println!(
                "   {} {}",
                "Type:".bright_cyan(),
                format!("{:?}", task_type_clone).bright_white()
            );

            if let Some(duration) = task.estimated_duration {
                println!("   {} {} minutes", "Est. Duration:".bright_cyan(), duration);
            }

            // Show helpful next steps
            show_quick_help("task-created");
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_search (lines 4173-4336) ---[0m
    async fn handle_search(&self, action: &SearchAction) -> Result<()> {
        use crate::extension_stub::agent_extension::{
            DocumentationSearchStrategy, GitHubSearchStrategy, SearchContext, SearchFilters,
            SearchQuery, SearchStrategy, StackOverflowSearchStrategy,
        };

        match action {
            SearchAction::Docs {
                query,
                source,
                limit,
            } => {
                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: Some("rust".to_string()),
                        framework: Some("general".to_string()),
                        date_range: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec![source.clone()],
                        relevance_threshold: 0.5,
                    }),
                };

                let mut all_results = Vec::new();

                if source == "all" || source == "mdn" {
                    let mdn_strategy = DocumentationSearchStrategy::new();
                    match mdn_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("MDN search failed: {}", e),
                    }
                }

                if source == "all" || source == "github" {
                    let github_strategy = GitHubSearchStrategy::new();
                    match github_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("GitHub search failed: {}", e),
                    }
                }

                if source == "all" || source == "stackoverflow" {
                    let stackoverflow_strategy = StackOverflowSearchStrategy;
                    match stackoverflow_strategy.search(&search_query).await {
                        Ok(mut results) => {
                            results.truncate(*limit / 3);
                            all_results.extend(results);
                        }
                        Err(e) => eprintln!("StackOverflow search failed: {}", e),
                    }
                }

                all_results.sort_by(|a, b| {
                    b.relevance_score
                        .partial_cmp(&a.relevance_score)
                        .unwrap_or(std::cmp::Ordering::Equal)
                });
                all_results.truncate(*limit);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "query": query,
                            "source": source,
                            "results": all_results.iter().map(|r| serde_json::json!({
                                "title": r.title,
                                "url": r.url,
                                "snippet": r.snippet,
                                "source": r.source,
                                "relevance_score": r.relevance_score,
                            })).collect::<Vec<_>>()
                        }))?
                    );
                } else {
                    println!("🔍 Searching documentation for: {}", query);
                    println!("   Source: {}", source);
                    println!("   Found {} results", all_results.len());
                    println!();

                    for (i, result) in all_results.iter().enumerate() {
                        println!("{}. {} ({})", i + 1, result.title, result.source);
                        println!("   🔗 {}", result.url);
                        println!("   📝 {}", result.snippet);
                        println!("   ⭐ Relevance: {:.2}", result.relevance_score);
                        println!();
                    }
                }
            }

            SearchAction::Test { query } => {
                println!("🧪 Testing search functionality with query: {}", query);

                let search_query = SearchQuery {
                    keywords: query.split_whitespace().map(|s| s.to_string()).collect(),
                    context: Some(SearchContext::CapabilityGap {
                        current: vec![],
                        required: query.split_whitespace().map(|s| s.to_string()).collect(),
                        desired: vec![],
                    }),
                    filters: Some(SearchFilters {
                        language: None,
                        framework: None,
                        min_relevance: 0.5,
                        max_complexity: 0.8,
                        preferred_sources: vec!["all".to_string()],
                        relevance_threshold: 0.5,
                        date_range: None,
                    }),
                };

                // Test each search strategy
                println!("🔍 Testing MDN search...");
                let mdn_strategy = DocumentationSearchStrategy::new();
                match mdn_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ MDN: Found {} results", results.len()),
                    Err(e) => println!("❌ MDN: Error - {}", e),
                }

                println!("🔍 Testing GitHub search...");
                let github_strategy = GitHubSearchStrategy::new();
                match github_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ GitHub: Found {} results", results.len()),
                    Err(e) => println!("❌ GitHub: Error - {}", e),
                }

                println!("🔍 Testing StackOverflow search...");
                let stackoverflow_strategy = StackOverflowSearchStrategy;
                match stackoverflow_strategy.search(&search_query).await {
                    Ok(results) => println!("✅ StackOverflow: Found {} results", results.len()),
                    Err(e) => println!("❌ StackOverflow: Error - {}", e),
                }

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Search test completed",
                            "query": query,
                        }))?
                    );
                } else {
                    println!("✅ Search functionality test completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1737-1832 method list_tasks <-> ./crates/ccswarm/src/cli/mod.rs:2637-2770 method handle_delegate
  Similarity: 94.70%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_tasks (lines 1737-1832) ---[0m
    async fn list_tasks(
        &self,
        all: bool,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
        detailed: bool,
    ) -> Result<()> {
        let tasks = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.list_tasks(status_filter, agent_filter).await
        } else {
            Vec::new()
        };

        let displayed_tasks = if all {
            tasks
        } else {
            tasks.into_iter().take(50).collect()
        };

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "tasks": displayed_tasks,
                    "total": displayed_tasks.len(),
                    "filters": {
                        "all": all,
                        "status": status_filter,
                        "agent": agent_filter,
                        "detailed": detailed
                    }
                }))?
            );
        } else {
            println!("📋 Task List");
            println!("============");

            if displayed_tasks.is_empty() {
                println!("No tasks in queue currently.");
                println!();
                println!("💡 Add a task with: ccswarm task add \"Your task description\"");
            } else {
                println!("Found {} tasks:", displayed_tasks.len());
                println!();

                for task in &displayed_tasks {
                    let status_emoji = match &task.status {
                        TaskStatus::Pending => "⏳",
                        TaskStatus::Assigned { .. } => "📋",
                        TaskStatus::InProgress { .. } => "🏃",
                        TaskStatus::Completed { .. } => "✅",
                        TaskStatus::Failed { .. } => "❌",
                        TaskStatus::Cancelled { .. } => "🚫",
                    };

                    let priority_emoji = match task.task.priority {
                        Priority::Critical => "🚨",
                        Priority::High => "🔥",
                        Priority::Medium => "📅",
                        Priority::Low => "💤",
                    };

                    println!(
                        "{} {} {} [{}] {}",
                        status_emoji,
                        priority_emoji,
                        &task.task.id[..8], // Short ID
                        task.task.task_type,
                        task.task.description
                    );

                    if let Some(agent) = &task.assigned_agent {
                        println!("   👤 Assigned to: {}", agent);
                    }

                    if detailed {
                        println!(
                            "   ⏰ Created: {}",
                            task.created_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        println!(
                            "   🔄 Updated: {}",
                            task.updated_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(details) = &task.task.details {
                            println!("   📝 Details: {}", details);
                        }
                    }
                    println!();
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_delegate (lines 2637-2770) ---[0m
    async fn handle_delegate(&self, action: &DelegateAction) -> Result<()> {
        use crate::orchestrator::master_delegation::{DelegationStrategy, MasterDelegationEngine};

        match action {
            DelegateAction::Task {
                description,
                agent,
                priority,
                task_type,
                details,
                force,
            } => {
                let task = self.create_task_from_args(
                    description,
                    priority,
                    task_type,
                    details.as_deref(),
                    None,
                )?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Task delegated",
                            "task": task,
                            "target_agent": agent,
                            "forced": force,
                        }))?
                    );
                } else {
                    println!("🎯 Delegating task to {} agent", agent);
                    println!("   Task: {}", task.description);
                    println!("   Priority: {:?}", task.priority);
                    println!("   Type: {:?}", task.task_type);
                    if *force {
                        println!("   ⚠️ Forced delegation");
                    }
                }
            }

            DelegateAction::Analyze {
                description,
                verbose,
                strategy,
            } => {
                let strategy = match strategy.as_str() {
                    "content" => DelegationStrategy::ContentBased,
                    "load" => DelegationStrategy::LoadBalanced,
                    "expertise" => DelegationStrategy::ExpertiseBased,
                    "workflow" => DelegationStrategy::WorkflowBased,
                    "hybrid" => DelegationStrategy::Hybrid,
                    _ => DelegationStrategy::Hybrid,
                };

                let mut engine = MasterDelegationEngine::new(strategy);
                let task = Task::new(
                    "analysis".to_string(),
                    description.clone(),
                    Priority::Medium,
                    TaskType::Development,
                );

                let decision = engine.delegate_task(task)?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&decision)?);
                } else {
                    println!("🔍 Task Analysis Results");
                    println!("   Task: {}", description);
                    println!("   Recommended Agent: {}", decision.target_agent.name());
                    println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                    if *verbose {
                        println!("   Reasoning: {}", decision.reasoning);
                        if let Some(duration) = decision.estimated_duration {
                            println!("   Estimated Duration: {:?}", duration);
                        }
                    }
                }
            }

            DelegateAction::Stats { detailed, period } => {
                // TODO: Implement delegation statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Delegation statistics",
                            "period_hours": period,
                            "detailed": detailed,
                        }))?
                    );
                } else {
                    println!("📊 Delegation Statistics (last {} hours)", period);
                    println!("   Feature not yet implemented");
                }
            }

            DelegateAction::Interactive => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "error": "Interactive mode not available in JSON output",
                        }))?
                    );
                } else {
                    println!("🖥️ Interactive Delegation Mode");
                    self.run_interactive_delegation().await?;
                }
            }

            DelegateAction::Show { file } => {
                let config = CcswarmConfig::from_file(file.clone()).await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&config)?);
                } else {
                    println!("📄 Delegation Configuration: {}", file.display());
                    println!("========================");
                    println!("Project: {}", config.project.name);
                    println!("Repository: {}", config.project.repository.url);
                    println!("Agents: {}", config.agents.len());
                    for (name, agent_config) in &config.agents {
                        println!("  - {}: {}", name, agent_config.specialization);
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2637-2770 method handle_delegate <-> ./crates/ccswarm/src/cli/mod.rs:5468-5582 method handle_health
  Similarity: 85.29%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_delegate (lines 2637-2770) ---[0m
    async fn handle_delegate(&self, action: &DelegateAction) -> Result<()> {
        use crate::orchestrator::master_delegation::{DelegationStrategy, MasterDelegationEngine};

        match action {
            DelegateAction::Task {
                description,
                agent,
                priority,
                task_type,
                details,
                force,
            } => {
                let task = self.create_task_from_args(
                    description,
                    priority,
                    task_type,
                    details.as_deref(),
                    None,
                )?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Task delegated",
                            "task": task,
                            "target_agent": agent,
                            "forced": force,
                        }))?
                    );
                } else {
                    println!("🎯 Delegating task to {} agent", agent);
                    println!("   Task: {}", task.description);
                    println!("   Priority: {:?}", task.priority);
                    println!("   Type: {:?}", task.task_type);
                    if *force {
                        println!("   ⚠️ Forced delegation");
                    }
                }
            }

            DelegateAction::Analyze {
                description,
                verbose,
                strategy,
            } => {
                let strategy = match strategy.as_str() {
                    "content" => DelegationStrategy::ContentBased,
                    "load" => DelegationStrategy::LoadBalanced,
                    "expertise" => DelegationStrategy::ExpertiseBased,
                    "workflow" => DelegationStrategy::WorkflowBased,
                    "hybrid" => DelegationStrategy::Hybrid,
                    _ => DelegationStrategy::Hybrid,
                };

                let mut engine = MasterDelegationEngine::new(strategy);
                let task = Task::new(
                    "analysis".to_string(),
                    description.clone(),
                    Priority::Medium,
                    TaskType::Development,
                );

                let decision = engine.delegate_task(task)?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&decision)?);
                } else {
                    println!("🔍 Task Analysis Results");
                    println!("   Task: {}", description);
                    println!("   Recommended Agent: {}", decision.target_agent.name());
                    println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                    if *verbose {
                        println!("   Reasoning: {}", decision.reasoning);
                        if let Some(duration) = decision.estimated_duration {
                            println!("   Estimated Duration: {:?}", duration);
                        }
                    }
                }
            }

            DelegateAction::Stats { detailed, period } => {
                // TODO: Implement delegation statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Delegation statistics",
                            "period_hours": period,
                            "detailed": detailed,
                        }))?
                    );
                } else {
                    println!("📊 Delegation Statistics (last {} hours)", period);
                    println!("   Feature not yet implemented");
                }
            }

            DelegateAction::Interactive => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "error": "Interactive mode not available in JSON output",
                        }))?
                    );
                } else {
                    println!("🖥️ Interactive Delegation Mode");
                    self.run_interactive_delegation().await?;
                }
            }

            DelegateAction::Show { file } => {
                let config = CcswarmConfig::from_file(file.clone()).await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&config)?);
                } else {
                    println!("📄 Delegation Configuration: {}", file.display());
                    println!("========================");
                    println!("Project: {}", config.project.name);
                    println!("Repository: {}", config.project.repository.url);
                    println!("Agents: {}", config.agents.len());
                    for (name, agent_config) in &config.agents {
                        println!("  - {}: {}", name, agent_config.specialization);
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_health (lines 5468-5582) ---[0m
    async fn handle_health(
        &self,
        check_agents: bool,
        check_sessions: bool,
        resources: bool,
        diagnose: bool,
        detailed: bool,
        format: &str,
    ) -> Result<()> {
        use crate::cli::health::{run_diagnostics, HealthChecker};
        use crate::coordination::StatusTracker;

        // Run diagnostics if requested
        if diagnose {
            return run_diagnostics(&self.repo_path).await;
        }

        // Initialize health checker
        let status_tracker = StatusTracker::new().await?;
        let health_checker = HealthChecker::new(status_tracker);

        // Add monitoring system if available
        // TODO: Get monitoring system from running orchestrator
        // let monitoring = MonitoringSystem::new();
        // let health_checker = health_checker.with_monitoring(monitoring);

        // Perform health checks based on flags
        let report = if check_agents && !check_sessions && !resources {
            // Only check agents
            let checks = health_checker.check_agents_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let total_agents = checks.len();
            let healthy_agents = checks
                .iter()
                .filter(|c| c.status == health::HealthStatus::Healthy)
                .count();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents,
                healthy_agents,
                active_tasks: 0,
                session_count: 0,
            }
        } else if check_sessions && !check_agents && !resources {
            // Only check sessions
            let checks = health_checker.check_sessions_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let session_count = checks.len();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents: 0,
                healthy_agents: 0,
                active_tasks: 0,
                session_count,
            }
        } else {
            // Full health check
            health_checker.check_all().await?
        };

        // Output results based on format
        match format {
            "json" => {
                println!("{}", serde_json::to_string_pretty(&report)?);
            }
            _ => {
                if detailed {
                    report.print_detailed();
                } else {
                    report.print_summary();
                }
            }
        }

        // Exit with non-zero code if unhealthy
        if report.overall_status == health::HealthStatus::Critical {
            std::process::exit(1);
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1993-2078 method show_task_history <-> ./crates/ccswarm/src/cli/mod.rs:2637-2770 method handle_delegate
  Similarity: 96.47%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_history (lines 1993-2078) ---[0m
    async fn show_task_history(
        &self,
        limit: usize,
        agent_filter: Option<&str>,
        failed_only: bool,
    ) -> Result<()> {
        let history = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.get_execution_history(Some(limit)).await
        } else {
            Vec::new()
        };

        let filtered_history: Vec<_> = history
            .into_iter()
            .filter(|result| {
                if failed_only && result.success {
                    return false;
                }
                if let Some(agent) = agent_filter {
                    if let Some(ref result_agent) = result.agent_used {
                        return result_agent == agent;
                    }
                    return false;
                }
                true
            })
            .collect();

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "history": filtered_history,
                    "limit": limit,
                    "agent_filter": agent_filter,
                    "failed_only": failed_only,
                    "total_count": filtered_history.len()
                }))?
            );
        } else {
            println!("📚 Task History (Last {} tasks)", limit);
            println!("================================");

            if filtered_history.is_empty() {
                println!("No task history available.");
            } else {
                println!("Found {} execution records:", filtered_history.len());
                println!();

                for result in &filtered_history {
                    let status_emoji = if result.success { "✅" } else { "❌" };
                    let orchestration_indicator = if result.orchestration_used {
                        "🎯"
                    } else {
                        "🔄"
                    };

                    println!(
                        "{} {} Task: {} | Duration: {:.2}s",
                        status_emoji,
                        orchestration_indicator,
                        &result.task_id[..8],
                        result.duration.as_secs_f64()
                    );

                    if let Some(agent) = &result.agent_used {
                        println!("   👤 Agent: {}", agent);
                    }

                    if let Some(error) = &result.error {
                        println!("   ❌ Error: {}", error);
                    }
                    println!();
                }
            }

            if let Some(agent) = agent_filter {
                println!("Filter: Agent = {}", agent);
            }
            if failed_only {
                println!("Filter: Failed tasks only");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_delegate (lines 2637-2770) ---[0m
    async fn handle_delegate(&self, action: &DelegateAction) -> Result<()> {
        use crate::orchestrator::master_delegation::{DelegationStrategy, MasterDelegationEngine};

        match action {
            DelegateAction::Task {
                description,
                agent,
                priority,
                task_type,
                details,
                force,
            } => {
                let task = self.create_task_from_args(
                    description,
                    priority,
                    task_type,
                    details.as_deref(),
                    None,
                )?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Task delegated",
                            "task": task,
                            "target_agent": agent,
                            "forced": force,
                        }))?
                    );
                } else {
                    println!("🎯 Delegating task to {} agent", agent);
                    println!("   Task: {}", task.description);
                    println!("   Priority: {:?}", task.priority);
                    println!("   Type: {:?}", task.task_type);
                    if *force {
                        println!("   ⚠️ Forced delegation");
                    }
                }
            }

            DelegateAction::Analyze {
                description,
                verbose,
                strategy,
            } => {
                let strategy = match strategy.as_str() {
                    "content" => DelegationStrategy::ContentBased,
                    "load" => DelegationStrategy::LoadBalanced,
                    "expertise" => DelegationStrategy::ExpertiseBased,
                    "workflow" => DelegationStrategy::WorkflowBased,
                    "hybrid" => DelegationStrategy::Hybrid,
                    _ => DelegationStrategy::Hybrid,
                };

                let mut engine = MasterDelegationEngine::new(strategy);
                let task = Task::new(
                    "analysis".to_string(),
                    description.clone(),
                    Priority::Medium,
                    TaskType::Development,
                );

                let decision = engine.delegate_task(task)?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&decision)?);
                } else {
                    println!("🔍 Task Analysis Results");
                    println!("   Task: {}", description);
                    println!("   Recommended Agent: {}", decision.target_agent.name());
                    println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                    if *verbose {
                        println!("   Reasoning: {}", decision.reasoning);
                        if let Some(duration) = decision.estimated_duration {
                            println!("   Estimated Duration: {:?}", duration);
                        }
                    }
                }
            }

            DelegateAction::Stats { detailed, period } => {
                // TODO: Implement delegation statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Delegation statistics",
                            "period_hours": period,
                            "detailed": detailed,
                        }))?
                    );
                } else {
                    println!("📊 Delegation Statistics (last {} hours)", period);
                    println!("   Feature not yet implemented");
                }
            }

            DelegateAction::Interactive => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "error": "Interactive mode not available in JSON output",
                        }))?
                    );
                } else {
                    println!("🖥️ Interactive Delegation Mode");
                    self.run_interactive_delegation().await?;
                }
            }

            DelegateAction::Show { file } => {
                let config = CcswarmConfig::from_file(file.clone()).await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&config)?);
                } else {
                    println!("📄 Delegation Configuration: {}", file.display());
                    println!("========================");
                    println!("Project: {}", config.project.name);
                    println!("Repository: {}", config.project.repository.url);
                    println!("Agents: {}", config.agents.len());
                    for (name, agent_config) in &config.agents {
                        println!("  - {}: {}", name, agent_config.specialization);
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2130-2238 method show_task_stats <-> ./crates/ccswarm/src/cli/mod.rs:2772-2884 method run_interactive_delegation
  Similarity: 94.63%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_stats (lines 2130-2238) ---[0m
    async fn show_task_stats(&self, detailed: bool, performance: bool) -> Result<()> {
        if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "queue_stats": queue_stats,
                        "execution_stats": execution_stats,
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("📊 Task Queue Statistics");
                println!("========================");
                println!("⏳ Pending: {}", queue_stats.pending_count);
                println!("🏃 In Progress: {}", queue_stats.active_count);
                println!("✅ Completed: {}", queue_stats.completed_count);
                println!("❌ Failed: {}", queue_stats.failed_count);
                println!("📋 Total: {}", queue_stats.total_count);

                if performance || detailed {
                    println!();
                    println!("🎯 Execution Statistics");
                    println!("=======================");
                    println!("Tasks Executed: {}", execution_stats.tasks_executed);
                    println!(
                        "Success Rate: {:.1}%",
                        if execution_stats.tasks_executed > 0 {
                            (execution_stats.tasks_succeeded as f64
                                / execution_stats.tasks_executed as f64)
                                * 100.0
                        } else {
                            0.0
                        }
                    );
                    println!(
                        "Average Duration: {:.2}s",
                        execution_stats.average_duration.as_secs_f64()
                    );
                    println!(
                        "Total Duration: {:.2}s",
                        execution_stats.total_duration.as_secs_f64()
                    );
                    println!(
                        "Orchestration Usage: {:.1}%",
                        execution_stats.orchestration_usage
                    );
                }

                if detailed {
                    println!();
                    println!("📈 Queue Health");
                    println!("===============");
                    let failure_rate = if queue_stats.total_count > 0 {
                        (queue_stats.failed_count as f64 / queue_stats.total_count as f64) * 100.0
                    } else {
                        0.0
                    };

                    let health_emoji = if failure_rate < 5.0 {
                        "🟢"
                    } else if failure_rate < 15.0 {
                        "🟡"
                    } else {
                        "🔴"
                    };

                    println!(
                        "{} Overall Health: {:.1}% failure rate",
                        health_emoji, failure_rate
                    );

                    if queue_stats.active_count > 10 {
                        println!(
                            "⚠️  High concurrent load: {} tasks",
                            queue_stats.active_count
                        );
                    }

                    if queue_stats.pending_count > 50 {
                        println!(
                            "⚠️  Queue backlog: {} pending tasks",
                            queue_stats.pending_count
                        );
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "execution_engine_unavailable",
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot display task statistics");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_interactive_delegation (lines 2772-2884) ---[0m
    async fn run_interactive_delegation(&self) -> Result<()> {
        use std::io::{self, Write};

        println!("🎯 Welcome to Interactive Delegation Mode");
        println!("   Type 'help' for commands, 'quit' to exit");
        println!();

        loop {
            print!("ccswarm> ");
            io::stdout().flush()?;

            let mut input = String::new();
            io::stdin().read_line(&mut input)?;
            let input = input.trim();

            match input {
                "quit" | "exit" => {
                    println!("👋 Goodbye!");
                    break;
                }
                "help" => {
                    println!("📚 Interactive Delegation Commands:");
                    println!("   analyze <task_description>  - Analyze task and suggest agent");
                    println!("   delegate <agent> <task>     - Delegate task to specific agent");
                    println!("   stats                       - Show delegation statistics");
                    println!("   agents                      - List available agents");
                    println!("   quit                        - Exit interactive mode");
                    println!();
                }
                "stats" => {
                    println!("📊 Delegation Statistics");
                    println!("   Feature not yet implemented");
                    println!();
                }
                "agents" => {
                    println!("🤖 Available Agents:");
                    println!("   • Frontend - React/TypeScript UI development");
                    println!("   • Backend - Node.js/Express API development");
                    println!("   • DevOps - Infrastructure and deployment");
                    println!("   • QA - Testing and quality assurance");
                    println!();
                }
                _ if input.starts_with("analyze ") => {
                    let task_desc = &input[8..];
                    if !task_desc.is_empty() {
                        // Directly call delegation analysis to avoid recursion
                        use crate::orchestrator::master_delegation::{
                            DelegationStrategy, MasterDelegationEngine,
                        };
                        let mut engine = MasterDelegationEngine::new(DelegationStrategy::Hybrid);
                        let task = Task::new(
                            "interactive-analysis".to_string(),
                            task_desc.to_string(),
                            Priority::Medium,
                            TaskType::Development,
                        );

                        match engine.delegate_task(task) {
                            Ok(decision) => {
                                println!("🔍 Task Analysis Results");
                                println!("   Task: {}", task_desc);
                                println!("   Recommended Agent: {}", decision.target_agent.name());
                                println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                                println!("   Reasoning: {}", decision.reasoning);
                                if let Some(duration) = decision.estimated_duration {
                                    println!("   Estimated Duration: {} seconds", duration);
                                }
                            }
                            Err(e) => {
                                println!("❌ Analysis failed: {}", e);
                            }
                        }
                        println!();
                    } else {
                        println!("❌ Please provide a task description");
                        println!("   Example: analyze Create login form with validation");
                        println!();
                    }
                }
                _ if input.starts_with("delegate ") => {
                    let parts: Vec<&str> = input[9..].splitn(2, ' ').collect();
                    if parts.len() == 2 {
                        let agent = parts[0];
                        let task_desc = parts[1];

                        if ["frontend", "backend", "devops", "qa"].contains(&agent) {
                            println!("🎯 Delegating '{}' to {} agent", task_desc, agent);
                            println!("   ✅ Task queued for delegation");
                            println!();
                        } else {
                            println!("❌ Unknown agent: {}", agent);
                            println!("   Available: frontend, backend, devops, qa");
                            println!();
                        }
                    } else {
                        println!("❌ Usage: delegate <agent> <task_description>");
                        println!("   Example: delegate frontend Create responsive navigation bar");
                        println!();
                    }
                }
                "" => {
                    // Empty input, continue
                }
                _ => {
                    println!("❓ Unknown command: {}", input);
                    println!("   Type 'help' for available commands");
                    println!();
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2772-2884 method run_interactive_delegation <-> ./crates/ccswarm/src/cli/mod.rs:5468-5582 method handle_health
  Similarity: 91.17%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_interactive_delegation (lines 2772-2884) ---[0m
    async fn run_interactive_delegation(&self) -> Result<()> {
        use std::io::{self, Write};

        println!("🎯 Welcome to Interactive Delegation Mode");
        println!("   Type 'help' for commands, 'quit' to exit");
        println!();

        loop {
            print!("ccswarm> ");
            io::stdout().flush()?;

            let mut input = String::new();
            io::stdin().read_line(&mut input)?;
            let input = input.trim();

            match input {
                "quit" | "exit" => {
                    println!("👋 Goodbye!");
                    break;
                }
                "help" => {
                    println!("📚 Interactive Delegation Commands:");
                    println!("   analyze <task_description>  - Analyze task and suggest agent");
                    println!("   delegate <agent> <task>     - Delegate task to specific agent");
                    println!("   stats                       - Show delegation statistics");
                    println!("   agents                      - List available agents");
                    println!("   quit                        - Exit interactive mode");
                    println!();
                }
                "stats" => {
                    println!("📊 Delegation Statistics");
                    println!("   Feature not yet implemented");
                    println!();
                }
                "agents" => {
                    println!("🤖 Available Agents:");
                    println!("   • Frontend - React/TypeScript UI development");
                    println!("   • Backend - Node.js/Express API development");
                    println!("   • DevOps - Infrastructure and deployment");
                    println!("   • QA - Testing and quality assurance");
                    println!();
                }
                _ if input.starts_with("analyze ") => {
                    let task_desc = &input[8..];
                    if !task_desc.is_empty() {
                        // Directly call delegation analysis to avoid recursion
                        use crate::orchestrator::master_delegation::{
                            DelegationStrategy, MasterDelegationEngine,
                        };
                        let mut engine = MasterDelegationEngine::new(DelegationStrategy::Hybrid);
                        let task = Task::new(
                            "interactive-analysis".to_string(),
                            task_desc.to_string(),
                            Priority::Medium,
                            TaskType::Development,
                        );

                        match engine.delegate_task(task) {
                            Ok(decision) => {
                                println!("🔍 Task Analysis Results");
                                println!("   Task: {}", task_desc);
                                println!("   Recommended Agent: {}", decision.target_agent.name());
                                println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                                println!("   Reasoning: {}", decision.reasoning);
                                if let Some(duration) = decision.estimated_duration {
                                    println!("   Estimated Duration: {} seconds", duration);
                                }
                            }
                            Err(e) => {
                                println!("❌ Analysis failed: {}", e);
                            }
                        }
                        println!();
                    } else {
                        println!("❌ Please provide a task description");
                        println!("   Example: analyze Create login form with validation");
                        println!();
                    }
                }
                _ if input.starts_with("delegate ") => {
                    let parts: Vec<&str> = input[9..].splitn(2, ' ').collect();
                    if parts.len() == 2 {
                        let agent = parts[0];
                        let task_desc = parts[1];

                        if ["frontend", "backend", "devops", "qa"].contains(&agent) {
                            println!("🎯 Delegating '{}' to {} agent", task_desc, agent);
                            println!("   ✅ Task queued for delegation");
                            println!();
                        } else {
                            println!("❌ Unknown agent: {}", agent);
                            println!("   Available: frontend, backend, devops, qa");
                            println!();
                        }
                    } else {
                        println!("❌ Usage: delegate <agent> <task_description>");
                        println!("   Example: delegate frontend Create responsive navigation bar");
                        println!();
                    }
                }
                "" => {
                    // Empty input, continue
                }
                _ => {
                    println!("❓ Unknown command: {}", input);
                    println!("   Type 'help' for available commands");
                    println!();
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_health (lines 5468-5582) ---[0m
    async fn handle_health(
        &self,
        check_agents: bool,
        check_sessions: bool,
        resources: bool,
        diagnose: bool,
        detailed: bool,
        format: &str,
    ) -> Result<()> {
        use crate::cli::health::{run_diagnostics, HealthChecker};
        use crate::coordination::StatusTracker;

        // Run diagnostics if requested
        if diagnose {
            return run_diagnostics(&self.repo_path).await;
        }

        // Initialize health checker
        let status_tracker = StatusTracker::new().await?;
        let health_checker = HealthChecker::new(status_tracker);

        // Add monitoring system if available
        // TODO: Get monitoring system from running orchestrator
        // let monitoring = MonitoringSystem::new();
        // let health_checker = health_checker.with_monitoring(monitoring);

        // Perform health checks based on flags
        let report = if check_agents && !check_sessions && !resources {
            // Only check agents
            let checks = health_checker.check_agents_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let total_agents = checks.len();
            let healthy_agents = checks
                .iter()
                .filter(|c| c.status == health::HealthStatus::Healthy)
                .count();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents,
                healthy_agents,
                active_tasks: 0,
                session_count: 0,
            }
        } else if check_sessions && !check_agents && !resources {
            // Only check sessions
            let checks = health_checker.check_sessions_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let session_count = checks.len();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents: 0,
                healthy_agents: 0,
                active_tasks: 0,
                session_count,
            }
        } else {
            // Full health check
            health_checker.check_all().await?
        };

        // Output results based on format
        match format {
            "json" => {
                println!("{}", serde_json::to_string_pretty(&report)?);
            }
            _ => {
                if detailed {
                    report.print_detailed();
                } else {
                    report.print_summary();
                }
            }
        }

        // Exit with non-zero code if unhealthy
        if report.overall_status == health::HealthStatus::Critical {
            std::process::exit(1);
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1218-1294 method init_project <-> ./crates/ccswarm/src/cli/mod.rs:3628-3804 method handle_sangha
  Similarity: 81.01%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:init_project (lines 1218-1294) ---[0m
    async fn init_project(
        &self,
        name: &str,
        repo_url: Option<&str>,
        agents: &[String],
    ) -> Result<()> {
        use crate::utils::user_error::CommonErrors;

        info!("Initializing ccswarm project: {}", name);

        // Show progress to user
        println!(
            "{}",
            format!("🚀 Initializing ccswarm project: {}", name)
                .bright_cyan()
                .bold()
        );
        println!();

        // Check if git is available
        if !crate::git::shell::ShellWorktreeManager::is_git_available() {
            CommonErrors::git_not_initialized().display();
            return Err(anyhow!("Git is required for ccswarm"));
        }

        // Initialize Git repository if needed
        crate::utils::user_error::show_progress("Setting up git repository...");
        crate::git::shell::ShellWorktreeManager::init_if_needed(&self.repo_path)
            .await
            .inspect_err(|e| {
                eprintln!();
                CommonErrors::git_not_initialized()
                    .with_details(e.to_string())
                    .display();
            })?;
        println!("✅ Git repository ready");

        // Create configuration
        crate::utils::user_error::show_progress("Creating project configuration...");
        let mut config = create_default_config(&self.repo_path)?;
        config.project.name = name.to_string();

        if let Some(url) = repo_url {
            config.project.repository.url = url.to_string();
        }

        // Add requested agents
        for agent_type in agents {
            let agent_config = crate::config::AgentConfig {
                specialization: agent_type.clone(),
                worktree: format!("agents/{}-agent", agent_type),
                branch: format!("feature/{}", agent_type),
                claude_config: crate::config::ClaudeConfig::for_agent(agent_type),
                claude_md_template: format!("{}_specialist", agent_type),
            };
            config.agents.insert(agent_type.clone(), agent_config);
        }

        // Save configuration
        let config_file = self.repo_path.join("ccswarm.json");
        config.to_file(config_file).await?;

        let data = serde_json::json!({
            "project": name,
            "agents": agents,
        });

        println!(
            "{}",
            self.formatter.format_success(
                &format!("ccswarm project '{}' initialized", name),
                Some(data)
            )
        );

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_sangha (lines 3628-3804) ---[0m
    async fn handle_sangha(&self, action: &SanghaAction) -> Result<()> {
        // Using stub implementation for now
        use crate::extension_stub::sangha::{Vote, VoteType};
        use std::str::FromStr;

        // Stub implementation - just log the action
        info!("🏛️  Sangha action requested (stub implementation)");

        match action {
            SanghaAction::Propose {
                proposal_type,
                file,
            } => {
                info!("📋 Submitting proposal to Sangha (stub implementation)");

                // Read proposal specification from file
                let _spec_content = tokio::fs::read_to_string(file)
                    .await
                    .context("Failed to read proposal file")?;

                info!("Proposal type: {}", proposal_type);

                // Stub: Generate a fake proposal ID
                let proposal_id = uuid::Uuid::new_v4();

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Proposal submitted",
                            "proposal_id": proposal_id,
                            "type": proposal_type,
                            "file": file,
                        }))?
                    );
                } else {
                    println!(
                        "📋 Submitting {} proposal from: {}",
                        proposal_type,
                        file.display()
                    );
                    println!("🆔 Proposal ID: {}", proposal_id);
                    println!("✅ Proposal submitted successfully");
                }
            }

            SanghaAction::Vote {
                proposal_id,
                choice,
                reason,
            } => {
                info!("🗳️ Casting vote (stub implementation)");

                // Parse vote choice
                let vote_type = match choice.to_lowercase().as_str() {
                    "aye" | "yes" | "approve" => VoteType::Approve,
                    "nay" | "no" | "reject" => VoteType::Reject,
                    "abstain" => VoteType::Abstain,
                    "veto" | "needs_changes" => VoteType::NeedsChanges,
                    _ => {
                        anyhow::bail!(
                            "Invalid vote choice: {}. Use aye, nay, abstain, or veto",
                            choice
                        );
                    }
                };

                // Parse proposal ID
                let prop_id =
                    uuid::Uuid::from_str(proposal_id).context("Invalid proposal ID format")?;

                // Create vote (stub)
                let _vote = Vote::new("cli-user".to_string(), prop_id.to_string(), vote_type);

                info!("Vote created for proposal: {}", proposal_id);

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Vote cast",
                            "proposal_id": proposal_id,
                            "choice": choice,
                            "reason": reason,
                        }))?
                    );
                } else {
                    println!("🗳️ Casting vote on proposal: {}", proposal_id);
                    println!("   Choice: {}", choice);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                    println!("✅ Vote cast successfully");
                }
            }

            SanghaAction::List { all, status } => {
                info!("📊 Listing Sangha status (stub implementation)");

                // Stub statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Sangha status retrieved (stub)",
                            "stats": {
                                "total_members": 1,
                                "active_members": 1,
                                "total_proposals": 0,
                                "active_proposals": 0,
                                "consensus_algorithm": "stub",
                            },
                            "show_all": all,
                            "filter_status": status,
                        }))?
                    );
                } else {
                    println!("📊 Sangha Status (Stub Implementation)");
                    println!("=====================================");
                    println!("👥 Members: 1 total, 1 active");
                    println!("📋 Proposals: 0 total, 0 active");
                    println!("🧠 Consensus Algorithm: stub");

                    println!(
                        "\n💡 No active proposals. Use 'ccswarm sangha propose' to create one."
                    );
                }
            }

            SanghaAction::Session { id, active } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Session info",
                            "session_id": id,
                            "active": active,
                        }))?
                    );
                } else {
                    println!("🏛️ Sangha Session");
                    if *active {
                        println!("No active session");
                    }
                }
            }

            SanghaAction::ExtensionReview {
                proposal_id,
                technical_check,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension reviewed",
                            "proposal_id": proposal_id,
                            "technical_check": technical_check,
                        }))?
                    );
                } else {
                    println!("🔍 Reviewing extension proposal: {}", proposal_id);
                    if *technical_check {
                        println!("   Performing technical validation...");
                    }
                    println!("✅ Review completed");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2772-2884 method run_interactive_delegation <-> ./crates/ccswarm/src/cli/mod.rs:4581-4682 method handle_evolution
  Similarity: 94.76%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_interactive_delegation (lines 2772-2884) ---[0m
    async fn run_interactive_delegation(&self) -> Result<()> {
        use std::io::{self, Write};

        println!("🎯 Welcome to Interactive Delegation Mode");
        println!("   Type 'help' for commands, 'quit' to exit");
        println!();

        loop {
            print!("ccswarm> ");
            io::stdout().flush()?;

            let mut input = String::new();
            io::stdin().read_line(&mut input)?;
            let input = input.trim();

            match input {
                "quit" | "exit" => {
                    println!("👋 Goodbye!");
                    break;
                }
                "help" => {
                    println!("📚 Interactive Delegation Commands:");
                    println!("   analyze <task_description>  - Analyze task and suggest agent");
                    println!("   delegate <agent> <task>     - Delegate task to specific agent");
                    println!("   stats                       - Show delegation statistics");
                    println!("   agents                      - List available agents");
                    println!("   quit                        - Exit interactive mode");
                    println!();
                }
                "stats" => {
                    println!("📊 Delegation Statistics");
                    println!("   Feature not yet implemented");
                    println!();
                }
                "agents" => {
                    println!("🤖 Available Agents:");
                    println!("   • Frontend - React/TypeScript UI development");
                    println!("   • Backend - Node.js/Express API development");
                    println!("   • DevOps - Infrastructure and deployment");
                    println!("   • QA - Testing and quality assurance");
                    println!();
                }
                _ if input.starts_with("analyze ") => {
                    let task_desc = &input[8..];
                    if !task_desc.is_empty() {
                        // Directly call delegation analysis to avoid recursion
                        use crate::orchestrator::master_delegation::{
                            DelegationStrategy, MasterDelegationEngine,
                        };
                        let mut engine = MasterDelegationEngine::new(DelegationStrategy::Hybrid);
                        let task = Task::new(
                            "interactive-analysis".to_string(),
                            task_desc.to_string(),
                            Priority::Medium,
                            TaskType::Development,
                        );

                        match engine.delegate_task(task) {
                            Ok(decision) => {
                                println!("🔍 Task Analysis Results");
                                println!("   Task: {}", task_desc);
                                println!("   Recommended Agent: {}", decision.target_agent.name());
                                println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                                println!("   Reasoning: {}", decision.reasoning);
                                if let Some(duration) = decision.estimated_duration {
                                    println!("   Estimated Duration: {} seconds", duration);
                                }
                            }
                            Err(e) => {
                                println!("❌ Analysis failed: {}", e);
                            }
                        }
                        println!();
                    } else {
                        println!("❌ Please provide a task description");
                        println!("   Example: analyze Create login form with validation");
                        println!();
                    }
                }
                _ if input.starts_with("delegate ") => {
                    let parts: Vec<&str> = input[9..].splitn(2, ' ').collect();
                    if parts.len() == 2 {
                        let agent = parts[0];
                        let task_desc = parts[1];

                        if ["frontend", "backend", "devops", "qa"].contains(&agent) {
                            println!("🎯 Delegating '{}' to {} agent", task_desc, agent);
                            println!("   ✅ Task queued for delegation");
                            println!();
                        } else {
                            println!("❌ Unknown agent: {}", agent);
                            println!("   Available: frontend, backend, devops, qa");
                            println!();
                        }
                    } else {
                        println!("❌ Usage: delegate <agent> <task_description>");
                        println!("   Example: delegate frontend Create responsive navigation bar");
                        println!();
                    }
                }
                "" => {
                    // Empty input, continue
                }
                _ => {
                    println!("❓ Unknown command: {}", input);
                    println!("   Type 'help' for available commands");
                    println!();
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_evolution (lines 4581-4682) ---[0m
    async fn handle_evolution(&self, action: &EvolutionAction) -> Result<()> {
        match action {
            EvolutionAction::Metrics { agent, all, period } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution metrics",
                            "agent": agent,
                            "all": all,
                            "period_days": period,
                        }))?
                    );
                } else {
                    println!("📈 Evolution Metrics");
                    println!("==================");
                    if *all {
                        println!("Showing metrics for all agents");
                    } else if let Some(a) = agent {
                        println!("Agent: {}", a);
                    }
                    println!("Period: {} days", period);
                    println!("\nNo metrics available");
                }
            }

            EvolutionAction::Patterns {
                pattern_type,
                successful,
                failed,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution patterns",
                            "pattern_type": pattern_type,
                            "filter": {
                                "successful": successful,
                                "failed": failed,
                            },
                        }))?
                    );
                } else {
                    println!("🔮 Evolution Patterns");
                    println!("===================");
                    if *successful {
                        println!("Showing successful patterns");
                    } else if *failed {
                        println!("Showing failed patterns");
                    }
                    println!("No patterns found");
                }
            }

            EvolutionAction::Genealogy { extension, full } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension genealogy",
                            "extension": extension,
                            "full_tree": full,
                        }))?
                    );
                } else {
                    println!("🌳 Extension Genealogy: {}", extension);
                    println!("======================");
                    if *full {
                        println!("Showing full genealogy tree");
                    }
                    println!("No genealogy data available");
                }
            }

            EvolutionAction::Report { format, output } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution report generated",
                            "format": format,
                            "output": output,
                        }))?
                    );
                } else {
                    println!("📊 Generating evolution report");
                    println!("   Format: {}", format);
                    if let Some(o) = output {
                        println!("   Output: {}", o.display());
                    }
                    println!("✅ Report generated");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1835-1933 method show_task_status <-> ./crates/ccswarm/src/cli/mod.rs:2772-2884 method run_interactive_delegation
  Similarity: 95.64%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_status (lines 1835-1933) ---[0m
    async fn show_task_status(
        &self,
        task_id: &str,
        history: bool,
        orchestration: bool,
    ) -> Result<()> {
        let task = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.get_task(task_id).await
        } else {
            None
        };

        if let Some(task) = task {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "task": task,
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");

                let status_emoji = match &task.status {
                    TaskStatus::Pending => "⏳",
                    TaskStatus::Assigned { .. } => "📋",
                    TaskStatus::InProgress { .. } => "🏃",
                    TaskStatus::Completed { .. } => "✅",
                    TaskStatus::Failed { .. } => "❌",
                    TaskStatus::Cancelled { .. } => "🚫",
                };

                println!("{} Status: {:?}", status_emoji, task.status);
                println!("📝 Description: {}", task.task.description);
                println!("🎯 Priority: {:?}", task.task.priority);
                println!("📋 Type: {:?}", task.task.task_type);
                println!(
                    "⏰ Created: {}",
                    task.created_at.format("%Y-%m-%d %H:%M:%S")
                );
                println!(
                    "🔄 Updated: {}",
                    task.updated_at.format("%Y-%m-%d %H:%M:%S")
                );

                if let Some(agent) = &task.assigned_agent {
                    println!("👤 Assigned to: {}", agent);
                }

                if let Some(details) = &task.task.details {
                    println!("📝 Details: {}", details);
                }

                if history && !task.execution_history.is_empty() {
                    println!();
                    println!("📚 Execution History:");
                    for (i, attempt) in task.execution_history.iter().enumerate() {
                        println!(
                            "  {}. Agent: {} | Started: {}",
                            i + 1,
                            attempt.agent_id,
                            attempt.started_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(completed) = attempt.completed_at {
                            println!("     Completed: {}", completed.format("%Y-%m-%d %H:%M:%S"));
                        }
                        if let Some(error) = &attempt.error {
                            println!("     Error: {}", error);
                        }
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "status": "not_found",
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");
                println!("❌ Task not found");
                println!();
                println!("💡 Use 'ccswarm task list' to see available tasks");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_interactive_delegation (lines 2772-2884) ---[0m
    async fn run_interactive_delegation(&self) -> Result<()> {
        use std::io::{self, Write};

        println!("🎯 Welcome to Interactive Delegation Mode");
        println!("   Type 'help' for commands, 'quit' to exit");
        println!();

        loop {
            print!("ccswarm> ");
            io::stdout().flush()?;

            let mut input = String::new();
            io::stdin().read_line(&mut input)?;
            let input = input.trim();

            match input {
                "quit" | "exit" => {
                    println!("👋 Goodbye!");
                    break;
                }
                "help" => {
                    println!("📚 Interactive Delegation Commands:");
                    println!("   analyze <task_description>  - Analyze task and suggest agent");
                    println!("   delegate <agent> <task>     - Delegate task to specific agent");
                    println!("   stats                       - Show delegation statistics");
                    println!("   agents                      - List available agents");
                    println!("   quit                        - Exit interactive mode");
                    println!();
                }
                "stats" => {
                    println!("📊 Delegation Statistics");
                    println!("   Feature not yet implemented");
                    println!();
                }
                "agents" => {
                    println!("🤖 Available Agents:");
                    println!("   • Frontend - React/TypeScript UI development");
                    println!("   • Backend - Node.js/Express API development");
                    println!("   • DevOps - Infrastructure and deployment");
                    println!("   • QA - Testing and quality assurance");
                    println!();
                }
                _ if input.starts_with("analyze ") => {
                    let task_desc = &input[8..];
                    if !task_desc.is_empty() {
                        // Directly call delegation analysis to avoid recursion
                        use crate::orchestrator::master_delegation::{
                            DelegationStrategy, MasterDelegationEngine,
                        };
                        let mut engine = MasterDelegationEngine::new(DelegationStrategy::Hybrid);
                        let task = Task::new(
                            "interactive-analysis".to_string(),
                            task_desc.to_string(),
                            Priority::Medium,
                            TaskType::Development,
                        );

                        match engine.delegate_task(task) {
                            Ok(decision) => {
                                println!("🔍 Task Analysis Results");
                                println!("   Task: {}", task_desc);
                                println!("   Recommended Agent: {}", decision.target_agent.name());
                                println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                                println!("   Reasoning: {}", decision.reasoning);
                                if let Some(duration) = decision.estimated_duration {
                                    println!("   Estimated Duration: {} seconds", duration);
                                }
                            }
                            Err(e) => {
                                println!("❌ Analysis failed: {}", e);
                            }
                        }
                        println!();
                    } else {
                        println!("❌ Please provide a task description");
                        println!("   Example: analyze Create login form with validation");
                        println!();
                    }
                }
                _ if input.starts_with("delegate ") => {
                    let parts: Vec<&str> = input[9..].splitn(2, ' ').collect();
                    if parts.len() == 2 {
                        let agent = parts[0];
                        let task_desc = parts[1];

                        if ["frontend", "backend", "devops", "qa"].contains(&agent) {
                            println!("🎯 Delegating '{}' to {} agent", task_desc, agent);
                            println!("   ✅ Task queued for delegation");
                            println!();
                        } else {
                            println!("❌ Unknown agent: {}", agent);
                            println!("   Available: frontend, backend, devops, qa");
                            println!();
                        }
                    } else {
                        println!("❌ Usage: delegate <agent> <task_description>");
                        println!("   Example: delegate frontend Create responsive navigation bar");
                        println!();
                    }
                }
                "" => {
                    // Empty input, continue
                }
                _ => {
                    println!("❓ Unknown command: {}", input);
                    println!("   Type 'help' for available commands");
                    println!();
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2637-2770 method handle_delegate <-> ./crates/ccswarm/src/cli/mod.rs:3545-3626 method handle_auto_create
  Similarity: 93.59%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_delegate (lines 2637-2770) ---[0m
    async fn handle_delegate(&self, action: &DelegateAction) -> Result<()> {
        use crate::orchestrator::master_delegation::{DelegationStrategy, MasterDelegationEngine};

        match action {
            DelegateAction::Task {
                description,
                agent,
                priority,
                task_type,
                details,
                force,
            } => {
                let task = self.create_task_from_args(
                    description,
                    priority,
                    task_type,
                    details.as_deref(),
                    None,
                )?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Task delegated",
                            "task": task,
                            "target_agent": agent,
                            "forced": force,
                        }))?
                    );
                } else {
                    println!("🎯 Delegating task to {} agent", agent);
                    println!("   Task: {}", task.description);
                    println!("   Priority: {:?}", task.priority);
                    println!("   Type: {:?}", task.task_type);
                    if *force {
                        println!("   ⚠️ Forced delegation");
                    }
                }
            }

            DelegateAction::Analyze {
                description,
                verbose,
                strategy,
            } => {
                let strategy = match strategy.as_str() {
                    "content" => DelegationStrategy::ContentBased,
                    "load" => DelegationStrategy::LoadBalanced,
                    "expertise" => DelegationStrategy::ExpertiseBased,
                    "workflow" => DelegationStrategy::WorkflowBased,
                    "hybrid" => DelegationStrategy::Hybrid,
                    _ => DelegationStrategy::Hybrid,
                };

                let mut engine = MasterDelegationEngine::new(strategy);
                let task = Task::new(
                    "analysis".to_string(),
                    description.clone(),
                    Priority::Medium,
                    TaskType::Development,
                );

                let decision = engine.delegate_task(task)?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&decision)?);
                } else {
                    println!("🔍 Task Analysis Results");
                    println!("   Task: {}", description);
                    println!("   Recommended Agent: {}", decision.target_agent.name());
                    println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                    if *verbose {
                        println!("   Reasoning: {}", decision.reasoning);
                        if let Some(duration) = decision.estimated_duration {
                            println!("   Estimated Duration: {:?}", duration);
                        }
                    }
                }
            }

            DelegateAction::Stats { detailed, period } => {
                // TODO: Implement delegation statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Delegation statistics",
                            "period_hours": period,
                            "detailed": detailed,
                        }))?
                    );
                } else {
                    println!("📊 Delegation Statistics (last {} hours)", period);
                    println!("   Feature not yet implemented");
                }
            }

            DelegateAction::Interactive => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "error": "Interactive mode not available in JSON output",
                        }))?
                    );
                } else {
                    println!("🖥️ Interactive Delegation Mode");
                    self.run_interactive_delegation().await?;
                }
            }

            DelegateAction::Show { file } => {
                let config = CcswarmConfig::from_file(file.clone()).await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&config)?);
                } else {
                    println!("📄 Delegation Configuration: {}", file.display());
                    println!("========================");
                    println!("Project: {}", config.project.name);
                    println!("Repository: {}", config.project.repository.url);
                    println!("Agents: {}", config.agents.len());
                    for (name, agent_config) in &config.agents {
                        println!("  - {}: {}", name, agent_config.specialization);
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_auto_create (lines 3545-3626) ---[0m
    async fn handle_auto_create(
        &self,
        description: &str,
        template: Option<&str>,
        auto_deploy: bool,
        output: &PathBuf,
    ) -> Result<()> {
        use crate::orchestrator::auto_create::AutoCreateEngine;

        info!("🚀 Starting auto-create for: {}", description);

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "started",
                    "message": "Auto-create process initiated",
                    "description": description,
                    "template": template,
                    "auto_deploy": auto_deploy,
                    "output": output,
                }))?
            );
        } else {
            println!("🚀 ccswarm Auto-Create");
            println!("====================");
            println!("📋 Request: {}", description);
            if let Some(tmpl) = template {
                println!("📄 Template: {}", tmpl);
            }
            println!("📂 Output: {}", output.display());
            println!();
        }

        // Create auto-create engine
        let mut engine = AutoCreateEngine::new();

        // Execute auto-create workflow
        match engine
            .execute_auto_create(description, &self.config, output)
            .await
        {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Application created successfully",
                            "output": output,
                        }))?
                    );
                } else {
                    println!("\n✅ Application created successfully!");
                    println!("📂 Location: {}", output.display());

                    if auto_deploy {
                        println!("\n🚀 Auto-deploying application...");
                        // TODO: Implement auto-deployment
                        println!("   ⚠️ Auto-deployment not yet implemented");
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "error",
                            "message": "Auto-create failed",
                            "error": e.to_string(),
                        }))?
                    );
                } else {
                    println!("\n❌ Auto-create failed: {}", e);
                }
                return Err(e);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1737-1832 method list_tasks <-> ./crates/ccswarm/src/cli/mod.rs:2772-2884 method run_interactive_delegation
  Similarity: 96.29%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_tasks (lines 1737-1832) ---[0m
    async fn list_tasks(
        &self,
        all: bool,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
        detailed: bool,
    ) -> Result<()> {
        let tasks = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.list_tasks(status_filter, agent_filter).await
        } else {
            Vec::new()
        };

        let displayed_tasks = if all {
            tasks
        } else {
            tasks.into_iter().take(50).collect()
        };

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "tasks": displayed_tasks,
                    "total": displayed_tasks.len(),
                    "filters": {
                        "all": all,
                        "status": status_filter,
                        "agent": agent_filter,
                        "detailed": detailed
                    }
                }))?
            );
        } else {
            println!("📋 Task List");
            println!("============");

            if displayed_tasks.is_empty() {
                println!("No tasks in queue currently.");
                println!();
                println!("💡 Add a task with: ccswarm task add \"Your task description\"");
            } else {
                println!("Found {} tasks:", displayed_tasks.len());
                println!();

                for task in &displayed_tasks {
                    let status_emoji = match &task.status {
                        TaskStatus::Pending => "⏳",
                        TaskStatus::Assigned { .. } => "📋",
                        TaskStatus::InProgress { .. } => "🏃",
                        TaskStatus::Completed { .. } => "✅",
                        TaskStatus::Failed { .. } => "❌",
                        TaskStatus::Cancelled { .. } => "🚫",
                    };

                    let priority_emoji = match task.task.priority {
                        Priority::Critical => "🚨",
                        Priority::High => "🔥",
                        Priority::Medium => "📅",
                        Priority::Low => "💤",
                    };

                    println!(
                        "{} {} {} [{}] {}",
                        status_emoji,
                        priority_emoji,
                        &task.task.id[..8], // Short ID
                        task.task.task_type,
                        task.task.description
                    );

                    if let Some(agent) = &task.assigned_agent {
                        println!("   👤 Assigned to: {}", agent);
                    }

                    if detailed {
                        println!(
                            "   ⏰ Created: {}",
                            task.created_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        println!(
                            "   🔄 Updated: {}",
                            task.updated_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(details) = &task.task.details {
                            println!("   📝 Details: {}", details);
                        }
                    }
                    println!();
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_interactive_delegation (lines 2772-2884) ---[0m
    async fn run_interactive_delegation(&self) -> Result<()> {
        use std::io::{self, Write};

        println!("🎯 Welcome to Interactive Delegation Mode");
        println!("   Type 'help' for commands, 'quit' to exit");
        println!();

        loop {
            print!("ccswarm> ");
            io::stdout().flush()?;

            let mut input = String::new();
            io::stdin().read_line(&mut input)?;
            let input = input.trim();

            match input {
                "quit" | "exit" => {
                    println!("👋 Goodbye!");
                    break;
                }
                "help" => {
                    println!("📚 Interactive Delegation Commands:");
                    println!("   analyze <task_description>  - Analyze task and suggest agent");
                    println!("   delegate <agent> <task>     - Delegate task to specific agent");
                    println!("   stats                       - Show delegation statistics");
                    println!("   agents                      - List available agents");
                    println!("   quit                        - Exit interactive mode");
                    println!();
                }
                "stats" => {
                    println!("📊 Delegation Statistics");
                    println!("   Feature not yet implemented");
                    println!();
                }
                "agents" => {
                    println!("🤖 Available Agents:");
                    println!("   • Frontend - React/TypeScript UI development");
                    println!("   • Backend - Node.js/Express API development");
                    println!("   • DevOps - Infrastructure and deployment");
                    println!("   • QA - Testing and quality assurance");
                    println!();
                }
                _ if input.starts_with("analyze ") => {
                    let task_desc = &input[8..];
                    if !task_desc.is_empty() {
                        // Directly call delegation analysis to avoid recursion
                        use crate::orchestrator::master_delegation::{
                            DelegationStrategy, MasterDelegationEngine,
                        };
                        let mut engine = MasterDelegationEngine::new(DelegationStrategy::Hybrid);
                        let task = Task::new(
                            "interactive-analysis".to_string(),
                            task_desc.to_string(),
                            Priority::Medium,
                            TaskType::Development,
                        );

                        match engine.delegate_task(task) {
                            Ok(decision) => {
                                println!("🔍 Task Analysis Results");
                                println!("   Task: {}", task_desc);
                                println!("   Recommended Agent: {}", decision.target_agent.name());
                                println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                                println!("   Reasoning: {}", decision.reasoning);
                                if let Some(duration) = decision.estimated_duration {
                                    println!("   Estimated Duration: {} seconds", duration);
                                }
                            }
                            Err(e) => {
                                println!("❌ Analysis failed: {}", e);
                            }
                        }
                        println!();
                    } else {
                        println!("❌ Please provide a task description");
                        println!("   Example: analyze Create login form with validation");
                        println!();
                    }
                }
                _ if input.starts_with("delegate ") => {
                    let parts: Vec<&str> = input[9..].splitn(2, ' ').collect();
                    if parts.len() == 2 {
                        let agent = parts[0];
                        let task_desc = parts[1];

                        if ["frontend", "backend", "devops", "qa"].contains(&agent) {
                            println!("🎯 Delegating '{}' to {} agent", task_desc, agent);
                            println!("   ✅ Task queued for delegation");
                            println!();
                        } else {
                            println!("❌ Unknown agent: {}", agent);
                            println!("   Available: frontend, backend, devops, qa");
                            println!();
                        }
                    } else {
                        println!("❌ Usage: delegate <agent> <task_description>");
                        println!("   Example: delegate frontend Create responsive navigation bar");
                        println!();
                    }
                }
                "" => {
                    // Empty input, continue
                }
                _ => {
                    println!("❓ Unknown command: {}", input);
                    println!("   Type 'help' for available commands");
                    println!();
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1835-1933 method show_task_status <-> ./crates/ccswarm/src/cli/mod.rs:2130-2238 method show_task_stats
  Similarity: 96.16%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_status (lines 1835-1933) ---[0m
    async fn show_task_status(
        &self,
        task_id: &str,
        history: bool,
        orchestration: bool,
    ) -> Result<()> {
        let task = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.get_task(task_id).await
        } else {
            None
        };

        if let Some(task) = task {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "task": task,
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");

                let status_emoji = match &task.status {
                    TaskStatus::Pending => "⏳",
                    TaskStatus::Assigned { .. } => "📋",
                    TaskStatus::InProgress { .. } => "🏃",
                    TaskStatus::Completed { .. } => "✅",
                    TaskStatus::Failed { .. } => "❌",
                    TaskStatus::Cancelled { .. } => "🚫",
                };

                println!("{} Status: {:?}", status_emoji, task.status);
                println!("📝 Description: {}", task.task.description);
                println!("🎯 Priority: {:?}", task.task.priority);
                println!("📋 Type: {:?}", task.task.task_type);
                println!(
                    "⏰ Created: {}",
                    task.created_at.format("%Y-%m-%d %H:%M:%S")
                );
                println!(
                    "🔄 Updated: {}",
                    task.updated_at.format("%Y-%m-%d %H:%M:%S")
                );

                if let Some(agent) = &task.assigned_agent {
                    println!("👤 Assigned to: {}", agent);
                }

                if let Some(details) = &task.task.details {
                    println!("📝 Details: {}", details);
                }

                if history && !task.execution_history.is_empty() {
                    println!();
                    println!("📚 Execution History:");
                    for (i, attempt) in task.execution_history.iter().enumerate() {
                        println!(
                            "  {}. Agent: {} | Started: {}",
                            i + 1,
                            attempt.agent_id,
                            attempt.started_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(completed) = attempt.completed_at {
                            println!("     Completed: {}", completed.format("%Y-%m-%d %H:%M:%S"));
                        }
                        if let Some(error) = &attempt.error {
                            println!("     Error: {}", error);
                        }
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "status": "not_found",
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");
                println!("❌ Task not found");
                println!();
                println!("💡 Use 'ccswarm task list' to see available tasks");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_stats (lines 2130-2238) ---[0m
    async fn show_task_stats(&self, detailed: bool, performance: bool) -> Result<()> {
        if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "queue_stats": queue_stats,
                        "execution_stats": execution_stats,
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("📊 Task Queue Statistics");
                println!("========================");
                println!("⏳ Pending: {}", queue_stats.pending_count);
                println!("🏃 In Progress: {}", queue_stats.active_count);
                println!("✅ Completed: {}", queue_stats.completed_count);
                println!("❌ Failed: {}", queue_stats.failed_count);
                println!("📋 Total: {}", queue_stats.total_count);

                if performance || detailed {
                    println!();
                    println!("🎯 Execution Statistics");
                    println!("=======================");
                    println!("Tasks Executed: {}", execution_stats.tasks_executed);
                    println!(
                        "Success Rate: {:.1}%",
                        if execution_stats.tasks_executed > 0 {
                            (execution_stats.tasks_succeeded as f64
                                / execution_stats.tasks_executed as f64)
                                * 100.0
                        } else {
                            0.0
                        }
                    );
                    println!(
                        "Average Duration: {:.2}s",
                        execution_stats.average_duration.as_secs_f64()
                    );
                    println!(
                        "Total Duration: {:.2}s",
                        execution_stats.total_duration.as_secs_f64()
                    );
                    println!(
                        "Orchestration Usage: {:.1}%",
                        execution_stats.orchestration_usage
                    );
                }

                if detailed {
                    println!();
                    println!("📈 Queue Health");
                    println!("===============");
                    let failure_rate = if queue_stats.total_count > 0 {
                        (queue_stats.failed_count as f64 / queue_stats.total_count as f64) * 100.0
                    } else {
                        0.0
                    };

                    let health_emoji = if failure_rate < 5.0 {
                        "🟢"
                    } else if failure_rate < 15.0 {
                        "🟡"
                    } else {
                        "🔴"
                    };

                    println!(
                        "{} Overall Health: {:.1}% failure rate",
                        health_emoji, failure_rate
                    );

                    if queue_stats.active_count > 10 {
                        println!(
                            "⚠️  High concurrent load: {} tasks",
                            queue_stats.active_count
                        );
                    }

                    if queue_stats.pending_count > 50 {
                        println!(
                            "⚠️  Queue backlog: {} pending tasks",
                            queue_stats.pending_count
                        );
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "execution_engine_unavailable",
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot display task statistics");
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1406-1543 method show_status <-> ./crates/ccswarm/src/cli/mod.rs:1545-1666 method add_task
  Similarity: 75.08%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_status (lines 1406-1543) ---[0m
    async fn show_status(&self, detailed: bool, agent: Option<&str>) -> Result<()> {
        // Read status from coordination files
        let status_tracker = crate::coordination::StatusTracker::new().await?;

        if let Some(agent_id) = agent {
            // Show specific agent status
            if let Some(status) = status_tracker.get_status(agent_id).await? {
                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&status)?);
                } else {
                    println!("Agent: {}", agent_id);
                    println!("Status: {}", status["status"]);
                    println!("Updated: {}", status["timestamp"]);

                    // Check if this is a backend agent and show backend-specific info
                    if let Some(role) = status.get("role") {
                        if role.as_str() == Some("Backend") {
                            if let Some(backend_info) = status.get("backend_specific") {
                                println!("\n🔧 Backend Status:");
                                if let Some(api_health) = backend_info.get("api_health") {
                                    println!(
                                        "  API Health: {:.1}%",
                                        api_health.as_f64().unwrap_or(0.0) * 100.0
                                    );
                                }
                                if let Some(db) = backend_info.get("database") {
                                    println!(
                                        "  Database: {} ({})",
                                        if db["is_connected"].as_bool().unwrap_or(false) {
                                            "Connected"
                                        } else {
                                            "Disconnected"
                                        },
                                        db["database_type"].as_str().unwrap_or("Unknown")
                                    );
                                }
                                if let Some(server) = backend_info.get("server") {
                                    println!(
                                        "  Server: {:.1}MB RAM, {:.1}% CPU",
                                        server["memory_usage_mb"].as_f64().unwrap_or(0.0),
                                        server["cpu_usage_percent"].as_f64().unwrap_or(0.0)
                                    );
                                }
                                if let Some(services) =
                                    backend_info.get("services").and_then(|s| s.as_array())
                                {
                                    println!("  Active Services: {}", services.len());
                                }
                                if let Some(activity) = backend_info.get("recent_activity") {
                                    println!(
                                        "  Recent API Calls: {}",
                                        activity.as_u64().unwrap_or(0)
                                    );
                                }
                            }
                        }
                    }

                    if detailed {
                        println!(
                            "\nDetails: {}",
                            serde_json::to_string_pretty(&status["additional_info"])?
                        );
                    }
                }
            } else if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "Agent not found",
                        "agent": agent_id,
                    }))?
                );
            } else {
                println!("❌ Agent '{}' not found", agent_id);
            }
        } else {
            // Show all agent statuses
            let statuses = status_tracker.get_all_statuses().await?;

            if self.json_output {
                println!("{}", serde_json::to_string_pretty(&statuses)?);
            } else {
                println!("📊 ccswarm Status");
                println!("================");

                if statuses.is_empty() {
                    println!("No agents found");
                } else {
                    for status in &statuses {
                        println!("Agent: {}", status["agent_id"]);
                        println!("  Status: {}", status["status"]);
                        println!("  Updated: {}", status["timestamp"]);

                        // Show role-specific summary for backend agents
                        if let Some(role) = status.get("role") {
                            if role.as_str() == Some("Backend") {
                                if let Some(backend_info) = status.get("backend_specific") {
                                    if let Some(api_health) = backend_info.get("api_health") {
                                        print!(
                                            "  API Health: {:.0}% | ",
                                            api_health.as_f64().unwrap_or(0.0) * 100.0
                                        );
                                    }
                                    if let Some(db) = backend_info.get("database") {
                                        print!(
                                            "DB: {} | ",
                                            if db["is_connected"].as_bool().unwrap_or(false) {
                                                "✓"
                                            } else {
                                                "✗"
                                            }
                                        );
                                    }
                                    if let Some(services) =
                                        backend_info.get("services").and_then(|s| s.as_array())
                                    {
                                        print!("Services: {}", services.len());
                                    }
                                    println!();
                                }
                            }
                        }

                        if detailed {
                            println!(
                                "  Details: {}",
                                serde_json::to_string_pretty(&status["additional_info"])?
                            );
                        }
                        println!();
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:add_task (lines 1545-1666) ---[0m
    async fn add_task(
        &self,
        description: &str,
        priority: &str,
        task_type: &str,
        details: Option<&str>,
        duration: Option<u32>,
    ) -> Result<()> {
        // use crate::cli::progress::{ProgressStyle, ProgressTracker};
        use crate::utils::user_error::CommonErrors;

        // Validate task description
        if description.trim().is_empty() {
            CommonErrors::invalid_task_format()
                .with_details("Task description cannot be empty")
                .with_suggestion("Provide a clear, actionable task description")
                .display();
            return Err(anyhow!("Invalid task description"));
        }

        // Create task (simplified progress display)
        println!(
            "Creating task: {}...",
            description.chars().take(50).collect::<String>()
        );

        let priority = match priority.to_lowercase().as_str() {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };

        let task_type = match task_type.to_lowercase().as_str() {
            "development" | "dev" => TaskType::Development,
            "testing" | "test" => TaskType::Testing,
            "documentation" | "docs" => TaskType::Documentation,
            "infrastructure" | "infra" => TaskType::Infrastructure,
            "coordination" => TaskType::Coordination,
            "review" => TaskType::Review,
            "bugfix" | "bug" => TaskType::Bugfix,
            "feature" => TaskType::Feature,
            _ => TaskType::Development,
        };

        let task_type_clone = task_type;
        let mut task = Task::new(
            uuid::Uuid::new_v4().to_string(),
            description.to_string(),
            priority,
            task_type,
        );

        if let Some(details) = details {
            task = task.with_details(details.to_string());
        }

        if let Some(duration) = duration {
            task = task.with_duration(duration);
        }

        // Add to task queue via execution engine
        let task_id = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.add_task(task.clone()).await
        } else {
            // Fall back to creating task with ID but warn user
            warn!("Execution engine not available, task will not be executed");
            task.id.clone()
        };

        // Complete progress indicator
        // ProgressTracker::complete(progress, true, Some("Task created successfully".to_string())).await;

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Task added",
                    "task_id": task_id,
                    "description": description,
                    "priority": priority,
                }))?
            );
        } else {
            println!();
            println!("{}", "✅ Task created successfully!".bright_green().bold());
            println!();
            println!(
                "   {} {}",
                "Task ID:".bright_cyan(),
                task_id[..8].bright_white()
            );
            println!("   {} {}", "Description:".bright_cyan(), description);
            println!(
                "   {} {}",
                "Priority:".bright_cyan(),
                match priority {
                    Priority::Critical => "🔴 Critical".bright_red(),
                    Priority::High => "🟡 High".bright_yellow(),
                    Priority::Medium => "🟢 Medium".bright_green(),
                    Priority::Low => "🔵 Low".bright_blue(),
                }
            );
            println!(
                "   {} {}",
                "Type:".bright_cyan(),
                format!("{:?}", task_type_clone).bright_white()
            );

            if let Some(duration) = task.estimated_duration {
                println!("   {} {} minutes", "Est. Duration:".bright_cyan(), duration);
            }

            // Show helpful next steps
            show_quick_help("task-created");
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2480-2543 method handle_config <-> ./crates/ccswarm/src/cli/mod.rs:2637-2770 method handle_delegate
  Similarity: 98.15%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_config (lines 2480-2543) ---[0m
    async fn handle_config(&self, action: &ConfigAction) -> Result<()> {
        match action {
            ConfigAction::Generate { output, template } => {
                let config = match template.as_str() {
                    "minimal" => create_minimal_config(&self.repo_path)?,
                    "frontend-only" => create_frontend_only_config(&self.repo_path)?,
                    "full-stack" => create_default_config(&self.repo_path)?,
                    _ => create_default_config(&self.repo_path)?,
                };

                config.to_file(output.clone()).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Configuration generated",
                            "file": output,
                            "template": template,
                        }))?
                    );
                } else {
                    println!("✅ Configuration generated: {}", output.display());
                    println!("   Template: {}", template);
                }
            }
            ConfigAction::Validate { file } => match CcswarmConfig::from_file(file.clone()).await {
                Ok(_) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "Configuration is valid",
                                "file": file,
                            }))?
                        );
                    } else {
                        println!("✅ Configuration is valid: {}", file.display());
                    }
                }
                Err(e) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "Configuration is invalid",
                                "file": file,
                                "error": e.to_string(),
                            }))?
                        );
                    } else {
                        println!("❌ Configuration is invalid: {}", file.display());
                        println!("   Error: {}", e);
                    }
                    return Err(e);
                }
            },
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_delegate (lines 2637-2770) ---[0m
    async fn handle_delegate(&self, action: &DelegateAction) -> Result<()> {
        use crate::orchestrator::master_delegation::{DelegationStrategy, MasterDelegationEngine};

        match action {
            DelegateAction::Task {
                description,
                agent,
                priority,
                task_type,
                details,
                force,
            } => {
                let task = self.create_task_from_args(
                    description,
                    priority,
                    task_type,
                    details.as_deref(),
                    None,
                )?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Task delegated",
                            "task": task,
                            "target_agent": agent,
                            "forced": force,
                        }))?
                    );
                } else {
                    println!("🎯 Delegating task to {} agent", agent);
                    println!("   Task: {}", task.description);
                    println!("   Priority: {:?}", task.priority);
                    println!("   Type: {:?}", task.task_type);
                    if *force {
                        println!("   ⚠️ Forced delegation");
                    }
                }
            }

            DelegateAction::Analyze {
                description,
                verbose,
                strategy,
            } => {
                let strategy = match strategy.as_str() {
                    "content" => DelegationStrategy::ContentBased,
                    "load" => DelegationStrategy::LoadBalanced,
                    "expertise" => DelegationStrategy::ExpertiseBased,
                    "workflow" => DelegationStrategy::WorkflowBased,
                    "hybrid" => DelegationStrategy::Hybrid,
                    _ => DelegationStrategy::Hybrid,
                };

                let mut engine = MasterDelegationEngine::new(strategy);
                let task = Task::new(
                    "analysis".to_string(),
                    description.clone(),
                    Priority::Medium,
                    TaskType::Development,
                );

                let decision = engine.delegate_task(task)?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&decision)?);
                } else {
                    println!("🔍 Task Analysis Results");
                    println!("   Task: {}", description);
                    println!("   Recommended Agent: {}", decision.target_agent.name());
                    println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                    if *verbose {
                        println!("   Reasoning: {}", decision.reasoning);
                        if let Some(duration) = decision.estimated_duration {
                            println!("   Estimated Duration: {:?}", duration);
                        }
                    }
                }
            }

            DelegateAction::Stats { detailed, period } => {
                // TODO: Implement delegation statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Delegation statistics",
                            "period_hours": period,
                            "detailed": detailed,
                        }))?
                    );
                } else {
                    println!("📊 Delegation Statistics (last {} hours)", period);
                    println!("   Feature not yet implemented");
                }
            }

            DelegateAction::Interactive => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "error": "Interactive mode not available in JSON output",
                        }))?
                    );
                } else {
                    println!("🖥️ Interactive Delegation Mode");
                    self.run_interactive_delegation().await?;
                }
            }

            DelegateAction::Show { file } => {
                let config = CcswarmConfig::from_file(file.clone()).await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&config)?);
                } else {
                    println!("📄 Delegation Configuration: {}", file.display());
                    println!("========================");
                    println!("Project: {}", config.project.name);
                    println!("Repository: {}", config.project.repository.url);
                    println!("Agents: {}", config.agents.len());
                    for (name, agent_config) in &config.agents {
                        println!("  - {}: {}", name, agent_config.specialization);
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1835-1933 method show_task_status <-> ./crates/ccswarm/src/cli/mod.rs:4581-4682 method handle_evolution
  Similarity: 96.47%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_status (lines 1835-1933) ---[0m
    async fn show_task_status(
        &self,
        task_id: &str,
        history: bool,
        orchestration: bool,
    ) -> Result<()> {
        let task = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.get_task(task_id).await
        } else {
            None
        };

        if let Some(task) = task {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "task": task,
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");

                let status_emoji = match &task.status {
                    TaskStatus::Pending => "⏳",
                    TaskStatus::Assigned { .. } => "📋",
                    TaskStatus::InProgress { .. } => "🏃",
                    TaskStatus::Completed { .. } => "✅",
                    TaskStatus::Failed { .. } => "❌",
                    TaskStatus::Cancelled { .. } => "🚫",
                };

                println!("{} Status: {:?}", status_emoji, task.status);
                println!("📝 Description: {}", task.task.description);
                println!("🎯 Priority: {:?}", task.task.priority);
                println!("📋 Type: {:?}", task.task.task_type);
                println!(
                    "⏰ Created: {}",
                    task.created_at.format("%Y-%m-%d %H:%M:%S")
                );
                println!(
                    "🔄 Updated: {}",
                    task.updated_at.format("%Y-%m-%d %H:%M:%S")
                );

                if let Some(agent) = &task.assigned_agent {
                    println!("👤 Assigned to: {}", agent);
                }

                if let Some(details) = &task.task.details {
                    println!("📝 Details: {}", details);
                }

                if history && !task.execution_history.is_empty() {
                    println!();
                    println!("📚 Execution History:");
                    for (i, attempt) in task.execution_history.iter().enumerate() {
                        println!(
                            "  {}. Agent: {} | Started: {}",
                            i + 1,
                            attempt.agent_id,
                            attempt.started_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(completed) = attempt.completed_at {
                            println!("     Completed: {}", completed.format("%Y-%m-%d %H:%M:%S"));
                        }
                        if let Some(error) = &attempt.error {
                            println!("     Error: {}", error);
                        }
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "status": "not_found",
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");
                println!("❌ Task not found");
                println!();
                println!("💡 Use 'ccswarm task list' to see available tasks");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_evolution (lines 4581-4682) ---[0m
    async fn handle_evolution(&self, action: &EvolutionAction) -> Result<()> {
        match action {
            EvolutionAction::Metrics { agent, all, period } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution metrics",
                            "agent": agent,
                            "all": all,
                            "period_days": period,
                        }))?
                    );
                } else {
                    println!("📈 Evolution Metrics");
                    println!("==================");
                    if *all {
                        println!("Showing metrics for all agents");
                    } else if let Some(a) = agent {
                        println!("Agent: {}", a);
                    }
                    println!("Period: {} days", period);
                    println!("\nNo metrics available");
                }
            }

            EvolutionAction::Patterns {
                pattern_type,
                successful,
                failed,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution patterns",
                            "pattern_type": pattern_type,
                            "filter": {
                                "successful": successful,
                                "failed": failed,
                            },
                        }))?
                    );
                } else {
                    println!("🔮 Evolution Patterns");
                    println!("===================");
                    if *successful {
                        println!("Showing successful patterns");
                    } else if *failed {
                        println!("Showing failed patterns");
                    }
                    println!("No patterns found");
                }
            }

            EvolutionAction::Genealogy { extension, full } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension genealogy",
                            "extension": extension,
                            "full_tree": full,
                        }))?
                    );
                } else {
                    println!("🌳 Extension Genealogy: {}", extension);
                    println!("======================");
                    if *full {
                        println!("Showing full genealogy tree");
                    }
                    println!("No genealogy data available");
                }
            }

            EvolutionAction::Report { format, output } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution report generated",
                            "format": format,
                            "output": output,
                        }))?
                    );
                } else {
                    println!("📊 Generating evolution report");
                    println!("   Format: {}", format);
                    if let Some(o) = output {
                        println!("   Output: {}", o.display());
                    }
                    println!("✅ Report generated");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1406-1543 method show_status <-> ./crates/ccswarm/src/cli/mod.rs:2546-2609 function create_default_config
  Similarity: 95.10%

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_status (lines 1406-1543) ---[0m
    async fn show_status(&self, detailed: bool, agent: Option<&str>) -> Result<()> {
        // Read status from coordination files
        let status_tracker = crate::coordination::StatusTracker::new().await?;

        if let Some(agent_id) = agent {
            // Show specific agent status
            if let Some(status) = status_tracker.get_status(agent_id).await? {
                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&status)?);
                } else {
                    println!("Agent: {}", agent_id);
                    println!("Status: {}", status["status"]);
                    println!("Updated: {}", status["timestamp"]);

                    // Check if this is a backend agent and show backend-specific info
                    if let Some(role) = status.get("role") {
                        if role.as_str() == Some("Backend") {
                            if let Some(backend_info) = status.get("backend_specific") {
                                println!("\n🔧 Backend Status:");
                                if let Some(api_health) = backend_info.get("api_health") {
                                    println!(
                                        "  API Health: {:.1}%",
                                        api_health.as_f64().unwrap_or(0.0) * 100.0
                                    );
                                }
                                if let Some(db) = backend_info.get("database") {
                                    println!(
                                        "  Database: {} ({})",
                                        if db["is_connected"].as_bool().unwrap_or(false) {
                                            "Connected"
                                        } else {
                                            "Disconnected"
                                        },
                                        db["database_type"].as_str().unwrap_or("Unknown")
                                    );
                                }
                                if let Some(server) = backend_info.get("server") {
                                    println!(
                                        "  Server: {:.1}MB RAM, {:.1}% CPU",
                                        server["memory_usage_mb"].as_f64().unwrap_or(0.0),
                                        server["cpu_usage_percent"].as_f64().unwrap_or(0.0)
                                    );
                                }
                                if let Some(services) =
                                    backend_info.get("services").and_then(|s| s.as_array())
                                {
                                    println!("  Active Services: {}", services.len());
                                }
                                if let Some(activity) = backend_info.get("recent_activity") {
                                    println!(
                                        "  Recent API Calls: {}",
                                        activity.as_u64().unwrap_or(0)
                                    );
                                }
                            }
                        }
                    }

                    if detailed {
                        println!(
                            "\nDetails: {}",
                            serde_json::to_string_pretty(&status["additional_info"])?
                        );
                    }
                }
            } else if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "Agent not found",
                        "agent": agent_id,
                    }))?
                );
            } else {
                println!("❌ Agent '{}' not found", agent_id);
            }
        } else {
            // Show all agent statuses
            let statuses = status_tracker.get_all_statuses().await?;

            if self.json_output {
                println!("{}", serde_json::to_string_pretty(&statuses)?);
            } else {
                println!("📊 ccswarm Status");
                println!("================");

                if statuses.is_empty() {
                    println!("No agents found");
                } else {
                    for status in &statuses {
                        println!("Agent: {}", status["agent_id"]);
                        println!("  Status: {}", status["status"]);
                        println!("  Updated: {}", status["timestamp"]);

                        // Show role-specific summary for backend agents
                        if let Some(role) = status.get("role") {
                            if role.as_str() == Some("Backend") {
                                if let Some(backend_info) = status.get("backend_specific") {
                                    if let Some(api_health) = backend_info.get("api_health") {
                                        print!(
                                            "  API Health: {:.0}% | ",
                                            api_health.as_f64().unwrap_or(0.0) * 100.0
                                        );
                                    }
                                    if let Some(db) = backend_info.get("database") {
                                        print!(
                                            "DB: {} | ",
                                            if db["is_connected"].as_bool().unwrap_or(false) {
                                                "✓"
                                            } else {
                                                "✗"
                                            }
                                        );
                                    }
                                    if let Some(services) =
                                        backend_info.get("services").and_then(|s| s.as_array())
                                    {
                                        print!("Services: {}", services.len());
                                    }
                                    println!();
                                }
                            }
                        }

                        if detailed {
                            println!(
                                "  Details: {}",
                                serde_json::to_string_pretty(&status["additional_info"])?
                            );
                        }
                        println!();
                    }
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_default_config (lines 2546-2609) ---[0m
fn create_default_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut agents = std::collections::HashMap::new();

    // Add common agent configurations
    agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend-ui".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    agents.insert(
        "backend".to_string(),
        crate::config::AgentConfig {
            specialization: "node_microservices".to_string(),
            worktree: "agents/backend-agent".to_string(),
            branch: "feature/backend-api".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("backend"),
            claude_md_template: "backend_specialist".to_string(),
        },
    );

    agents.insert(
        "devops".to_string(),
        crate::config::AgentConfig {
            specialization: "aws_kubernetes".to_string(),
            worktree: "agents/devops-agent".to_string(),
            branch: "feature/infrastructure".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("devops"),
            claude_md_template: "devops_specialist".to_string(),
        },
    );

    Ok(CcswarmConfig {
        project: crate::config::ProjectConfig {
            name: "New ccswarm Project".to_string(),
            repository: crate::config::RepositoryConfig {
                url: repo_path.to_string_lossy().to_string(),
                main_branch: "main".to_string(),
            },
            master_claude: crate::config::MasterClaudeConfig {
                role: "technical_lead".to_string(),
                quality_threshold: 0.90,
                think_mode: crate::config::ThinkMode::UltraThink,
                permission_level: "supervised".to_string(),
                claude_config: crate::config::ClaudeConfig::for_master(),
                enable_proactive_mode: true, // デフォルト有効
                proactive_frequency: 30,     // 30秒間隔
                high_frequency: 15,          // 高頻度15秒間隔
            },
        },
        agents,
        coordination: crate::config::CoordinationConfig {
            communication_method: "json_files".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_commit".to_string(),
            master_review_trigger: "all_tasks_complete".to_string(),
        },
    })
}

  ./crates/ccswarm/src/cli/mod.rs:1993-2078 method show_task_history <-> ./crates/ccswarm/src/cli/mod.rs:2772-2884 method run_interactive_delegation
  Similarity: 95.85%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_history (lines 1993-2078) ---[0m
    async fn show_task_history(
        &self,
        limit: usize,
        agent_filter: Option<&str>,
        failed_only: bool,
    ) -> Result<()> {
        let history = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.get_execution_history(Some(limit)).await
        } else {
            Vec::new()
        };

        let filtered_history: Vec<_> = history
            .into_iter()
            .filter(|result| {
                if failed_only && result.success {
                    return false;
                }
                if let Some(agent) = agent_filter {
                    if let Some(ref result_agent) = result.agent_used {
                        return result_agent == agent;
                    }
                    return false;
                }
                true
            })
            .collect();

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "history": filtered_history,
                    "limit": limit,
                    "agent_filter": agent_filter,
                    "failed_only": failed_only,
                    "total_count": filtered_history.len()
                }))?
            );
        } else {
            println!("📚 Task History (Last {} tasks)", limit);
            println!("================================");

            if filtered_history.is_empty() {
                println!("No task history available.");
            } else {
                println!("Found {} execution records:", filtered_history.len());
                println!();

                for result in &filtered_history {
                    let status_emoji = if result.success { "✅" } else { "❌" };
                    let orchestration_indicator = if result.orchestration_used {
                        "🎯"
                    } else {
                        "🔄"
                    };

                    println!(
                        "{} {} Task: {} | Duration: {:.2}s",
                        status_emoji,
                        orchestration_indicator,
                        &result.task_id[..8],
                        result.duration.as_secs_f64()
                    );

                    if let Some(agent) = &result.agent_used {
                        println!("   👤 Agent: {}", agent);
                    }

                    if let Some(error) = &result.error {
                        println!("   ❌ Error: {}", error);
                    }
                    println!();
                }
            }

            if let Some(agent) = agent_filter {
                println!("Filter: Agent = {}", agent);
            }
            if failed_only {
                println!("Filter: Failed tasks only");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_interactive_delegation (lines 2772-2884) ---[0m
    async fn run_interactive_delegation(&self) -> Result<()> {
        use std::io::{self, Write};

        println!("🎯 Welcome to Interactive Delegation Mode");
        println!("   Type 'help' for commands, 'quit' to exit");
        println!();

        loop {
            print!("ccswarm> ");
            io::stdout().flush()?;

            let mut input = String::new();
            io::stdin().read_line(&mut input)?;
            let input = input.trim();

            match input {
                "quit" | "exit" => {
                    println!("👋 Goodbye!");
                    break;
                }
                "help" => {
                    println!("📚 Interactive Delegation Commands:");
                    println!("   analyze <task_description>  - Analyze task and suggest agent");
                    println!("   delegate <agent> <task>     - Delegate task to specific agent");
                    println!("   stats                       - Show delegation statistics");
                    println!("   agents                      - List available agents");
                    println!("   quit                        - Exit interactive mode");
                    println!();
                }
                "stats" => {
                    println!("📊 Delegation Statistics");
                    println!("   Feature not yet implemented");
                    println!();
                }
                "agents" => {
                    println!("🤖 Available Agents:");
                    println!("   • Frontend - React/TypeScript UI development");
                    println!("   • Backend - Node.js/Express API development");
                    println!("   • DevOps - Infrastructure and deployment");
                    println!("   • QA - Testing and quality assurance");
                    println!();
                }
                _ if input.starts_with("analyze ") => {
                    let task_desc = &input[8..];
                    if !task_desc.is_empty() {
                        // Directly call delegation analysis to avoid recursion
                        use crate::orchestrator::master_delegation::{
                            DelegationStrategy, MasterDelegationEngine,
                        };
                        let mut engine = MasterDelegationEngine::new(DelegationStrategy::Hybrid);
                        let task = Task::new(
                            "interactive-analysis".to_string(),
                            task_desc.to_string(),
                            Priority::Medium,
                            TaskType::Development,
                        );

                        match engine.delegate_task(task) {
                            Ok(decision) => {
                                println!("🔍 Task Analysis Results");
                                println!("   Task: {}", task_desc);
                                println!("   Recommended Agent: {}", decision.target_agent.name());
                                println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                                println!("   Reasoning: {}", decision.reasoning);
                                if let Some(duration) = decision.estimated_duration {
                                    println!("   Estimated Duration: {} seconds", duration);
                                }
                            }
                            Err(e) => {
                                println!("❌ Analysis failed: {}", e);
                            }
                        }
                        println!();
                    } else {
                        println!("❌ Please provide a task description");
                        println!("   Example: analyze Create login form with validation");
                        println!();
                    }
                }
                _ if input.starts_with("delegate ") => {
                    let parts: Vec<&str> = input[9..].splitn(2, ' ').collect();
                    if parts.len() == 2 {
                        let agent = parts[0];
                        let task_desc = parts[1];

                        if ["frontend", "backend", "devops", "qa"].contains(&agent) {
                            println!("🎯 Delegating '{}' to {} agent", task_desc, agent);
                            println!("   ✅ Task queued for delegation");
                            println!();
                        } else {
                            println!("❌ Unknown agent: {}", agent);
                            println!("   Available: frontend, backend, devops, qa");
                            println!();
                        }
                    } else {
                        println!("❌ Usage: delegate <agent> <task_description>");
                        println!("   Example: delegate frontend Create responsive navigation bar");
                        println!();
                    }
                }
                "" => {
                    // Empty input, continue
                }
                _ => {
                    println!("❓ Unknown command: {}", input);
                    println!("   Type 'help' for available commands");
                    println!();
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2772-2884 method run_interactive_delegation <-> ./crates/ccswarm/src/cli/mod.rs:3545-3626 method handle_auto_create
  Similarity: 96.50%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_interactive_delegation (lines 2772-2884) ---[0m
    async fn run_interactive_delegation(&self) -> Result<()> {
        use std::io::{self, Write};

        println!("🎯 Welcome to Interactive Delegation Mode");
        println!("   Type 'help' for commands, 'quit' to exit");
        println!();

        loop {
            print!("ccswarm> ");
            io::stdout().flush()?;

            let mut input = String::new();
            io::stdin().read_line(&mut input)?;
            let input = input.trim();

            match input {
                "quit" | "exit" => {
                    println!("👋 Goodbye!");
                    break;
                }
                "help" => {
                    println!("📚 Interactive Delegation Commands:");
                    println!("   analyze <task_description>  - Analyze task and suggest agent");
                    println!("   delegate <agent> <task>     - Delegate task to specific agent");
                    println!("   stats                       - Show delegation statistics");
                    println!("   agents                      - List available agents");
                    println!("   quit                        - Exit interactive mode");
                    println!();
                }
                "stats" => {
                    println!("📊 Delegation Statistics");
                    println!("   Feature not yet implemented");
                    println!();
                }
                "agents" => {
                    println!("🤖 Available Agents:");
                    println!("   • Frontend - React/TypeScript UI development");
                    println!("   • Backend - Node.js/Express API development");
                    println!("   • DevOps - Infrastructure and deployment");
                    println!("   • QA - Testing and quality assurance");
                    println!();
                }
                _ if input.starts_with("analyze ") => {
                    let task_desc = &input[8..];
                    if !task_desc.is_empty() {
                        // Directly call delegation analysis to avoid recursion
                        use crate::orchestrator::master_delegation::{
                            DelegationStrategy, MasterDelegationEngine,
                        };
                        let mut engine = MasterDelegationEngine::new(DelegationStrategy::Hybrid);
                        let task = Task::new(
                            "interactive-analysis".to_string(),
                            task_desc.to_string(),
                            Priority::Medium,
                            TaskType::Development,
                        );

                        match engine.delegate_task(task) {
                            Ok(decision) => {
                                println!("🔍 Task Analysis Results");
                                println!("   Task: {}", task_desc);
                                println!("   Recommended Agent: {}", decision.target_agent.name());
                                println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                                println!("   Reasoning: {}", decision.reasoning);
                                if let Some(duration) = decision.estimated_duration {
                                    println!("   Estimated Duration: {} seconds", duration);
                                }
                            }
                            Err(e) => {
                                println!("❌ Analysis failed: {}", e);
                            }
                        }
                        println!();
                    } else {
                        println!("❌ Please provide a task description");
                        println!("   Example: analyze Create login form with validation");
                        println!();
                    }
                }
                _ if input.starts_with("delegate ") => {
                    let parts: Vec<&str> = input[9..].splitn(2, ' ').collect();
                    if parts.len() == 2 {
                        let agent = parts[0];
                        let task_desc = parts[1];

                        if ["frontend", "backend", "devops", "qa"].contains(&agent) {
                            println!("🎯 Delegating '{}' to {} agent", task_desc, agent);
                            println!("   ✅ Task queued for delegation");
                            println!();
                        } else {
                            println!("❌ Unknown agent: {}", agent);
                            println!("   Available: frontend, backend, devops, qa");
                            println!();
                        }
                    } else {
                        println!("❌ Usage: delegate <agent> <task_description>");
                        println!("   Example: delegate frontend Create responsive navigation bar");
                        println!();
                    }
                }
                "" => {
                    // Empty input, continue
                }
                _ => {
                    println!("❓ Unknown command: {}", input);
                    println!("   Type 'help' for available commands");
                    println!();
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_auto_create (lines 3545-3626) ---[0m
    async fn handle_auto_create(
        &self,
        description: &str,
        template: Option<&str>,
        auto_deploy: bool,
        output: &PathBuf,
    ) -> Result<()> {
        use crate::orchestrator::auto_create::AutoCreateEngine;

        info!("🚀 Starting auto-create for: {}", description);

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "started",
                    "message": "Auto-create process initiated",
                    "description": description,
                    "template": template,
                    "auto_deploy": auto_deploy,
                    "output": output,
                }))?
            );
        } else {
            println!("🚀 ccswarm Auto-Create");
            println!("====================");
            println!("📋 Request: {}", description);
            if let Some(tmpl) = template {
                println!("📄 Template: {}", tmpl);
            }
            println!("📂 Output: {}", output.display());
            println!();
        }

        // Create auto-create engine
        let mut engine = AutoCreateEngine::new();

        // Execute auto-create workflow
        match engine
            .execute_auto_create(description, &self.config, output)
            .await
        {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Application created successfully",
                            "output": output,
                        }))?
                    );
                } else {
                    println!("\n✅ Application created successfully!");
                    println!("📂 Location: {}", output.display());

                    if auto_deploy {
                        println!("\n🚀 Auto-deploying application...");
                        // TODO: Implement auto-deployment
                        println!("   ⚠️ Auto-deployment not yet implemented");
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "error",
                            "message": "Auto-create failed",
                            "error": e.to_string(),
                        }))?
                    );
                } else {
                    println!("\n❌ Auto-create failed: {}", e);
                }
                return Err(e);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1737-1832 method list_tasks <-> ./crates/ccswarm/src/cli/mod.rs:1835-1933 method show_task_status
  Similarity: 96.40%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_tasks (lines 1737-1832) ---[0m
    async fn list_tasks(
        &self,
        all: bool,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
        detailed: bool,
    ) -> Result<()> {
        let tasks = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.list_tasks(status_filter, agent_filter).await
        } else {
            Vec::new()
        };

        let displayed_tasks = if all {
            tasks
        } else {
            tasks.into_iter().take(50).collect()
        };

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "tasks": displayed_tasks,
                    "total": displayed_tasks.len(),
                    "filters": {
                        "all": all,
                        "status": status_filter,
                        "agent": agent_filter,
                        "detailed": detailed
                    }
                }))?
            );
        } else {
            println!("📋 Task List");
            println!("============");

            if displayed_tasks.is_empty() {
                println!("No tasks in queue currently.");
                println!();
                println!("💡 Add a task with: ccswarm task add \"Your task description\"");
            } else {
                println!("Found {} tasks:", displayed_tasks.len());
                println!();

                for task in &displayed_tasks {
                    let status_emoji = match &task.status {
                        TaskStatus::Pending => "⏳",
                        TaskStatus::Assigned { .. } => "📋",
                        TaskStatus::InProgress { .. } => "🏃",
                        TaskStatus::Completed { .. } => "✅",
                        TaskStatus::Failed { .. } => "❌",
                        TaskStatus::Cancelled { .. } => "🚫",
                    };

                    let priority_emoji = match task.task.priority {
                        Priority::Critical => "🚨",
                        Priority::High => "🔥",
                        Priority::Medium => "📅",
                        Priority::Low => "💤",
                    };

                    println!(
                        "{} {} {} [{}] {}",
                        status_emoji,
                        priority_emoji,
                        &task.task.id[..8], // Short ID
                        task.task.task_type,
                        task.task.description
                    );

                    if let Some(agent) = &task.assigned_agent {
                        println!("   👤 Assigned to: {}", agent);
                    }

                    if detailed {
                        println!(
                            "   ⏰ Created: {}",
                            task.created_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        println!(
                            "   🔄 Updated: {}",
                            task.updated_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(details) = &task.task.details {
                            println!("   📝 Details: {}", details);
                        }
                    }
                    println!();
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_status (lines 1835-1933) ---[0m
    async fn show_task_status(
        &self,
        task_id: &str,
        history: bool,
        orchestration: bool,
    ) -> Result<()> {
        let task = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.get_task(task_id).await
        } else {
            None
        };

        if let Some(task) = task {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "task": task,
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");

                let status_emoji = match &task.status {
                    TaskStatus::Pending => "⏳",
                    TaskStatus::Assigned { .. } => "📋",
                    TaskStatus::InProgress { .. } => "🏃",
                    TaskStatus::Completed { .. } => "✅",
                    TaskStatus::Failed { .. } => "❌",
                    TaskStatus::Cancelled { .. } => "🚫",
                };

                println!("{} Status: {:?}", status_emoji, task.status);
                println!("📝 Description: {}", task.task.description);
                println!("🎯 Priority: {:?}", task.task.priority);
                println!("📋 Type: {:?}", task.task.task_type);
                println!(
                    "⏰ Created: {}",
                    task.created_at.format("%Y-%m-%d %H:%M:%S")
                );
                println!(
                    "🔄 Updated: {}",
                    task.updated_at.format("%Y-%m-%d %H:%M:%S")
                );

                if let Some(agent) = &task.assigned_agent {
                    println!("👤 Assigned to: {}", agent);
                }

                if let Some(details) = &task.task.details {
                    println!("📝 Details: {}", details);
                }

                if history && !task.execution_history.is_empty() {
                    println!();
                    println!("📚 Execution History:");
                    for (i, attempt) in task.execution_history.iter().enumerate() {
                        println!(
                            "  {}. Agent: {} | Started: {}",
                            i + 1,
                            attempt.agent_id,
                            attempt.started_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(completed) = attempt.completed_at {
                            println!("     Completed: {}", completed.format("%Y-%m-%d %H:%M:%S"));
                        }
                        if let Some(error) = &attempt.error {
                            println!("     Error: {}", error);
                        }
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "status": "not_found",
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");
                println!("❌ Task not found");
                println!();
                println!("💡 Use 'ccswarm task list' to see available tasks");
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1545-1666 method add_task <-> ./crates/ccswarm/src/cli/mod.rs:2772-2884 method run_interactive_delegation
  Similarity: 79.13%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:add_task (lines 1545-1666) ---[0m
    async fn add_task(
        &self,
        description: &str,
        priority: &str,
        task_type: &str,
        details: Option<&str>,
        duration: Option<u32>,
    ) -> Result<()> {
        // use crate::cli::progress::{ProgressStyle, ProgressTracker};
        use crate::utils::user_error::CommonErrors;

        // Validate task description
        if description.trim().is_empty() {
            CommonErrors::invalid_task_format()
                .with_details("Task description cannot be empty")
                .with_suggestion("Provide a clear, actionable task description")
                .display();
            return Err(anyhow!("Invalid task description"));
        }

        // Create task (simplified progress display)
        println!(
            "Creating task: {}...",
            description.chars().take(50).collect::<String>()
        );

        let priority = match priority.to_lowercase().as_str() {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };

        let task_type = match task_type.to_lowercase().as_str() {
            "development" | "dev" => TaskType::Development,
            "testing" | "test" => TaskType::Testing,
            "documentation" | "docs" => TaskType::Documentation,
            "infrastructure" | "infra" => TaskType::Infrastructure,
            "coordination" => TaskType::Coordination,
            "review" => TaskType::Review,
            "bugfix" | "bug" => TaskType::Bugfix,
            "feature" => TaskType::Feature,
            _ => TaskType::Development,
        };

        let task_type_clone = task_type;
        let mut task = Task::new(
            uuid::Uuid::new_v4().to_string(),
            description.to_string(),
            priority,
            task_type,
        );

        if let Some(details) = details {
            task = task.with_details(details.to_string());
        }

        if let Some(duration) = duration {
            task = task.with_duration(duration);
        }

        // Add to task queue via execution engine
        let task_id = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.add_task(task.clone()).await
        } else {
            // Fall back to creating task with ID but warn user
            warn!("Execution engine not available, task will not be executed");
            task.id.clone()
        };

        // Complete progress indicator
        // ProgressTracker::complete(progress, true, Some("Task created successfully".to_string())).await;

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Task added",
                    "task_id": task_id,
                    "description": description,
                    "priority": priority,
                }))?
            );
        } else {
            println!();
            println!("{}", "✅ Task created successfully!".bright_green().bold());
            println!();
            println!(
                "   {} {}",
                "Task ID:".bright_cyan(),
                task_id[..8].bright_white()
            );
            println!("   {} {}", "Description:".bright_cyan(), description);
            println!(
                "   {} {}",
                "Priority:".bright_cyan(),
                match priority {
                    Priority::Critical => "🔴 Critical".bright_red(),
                    Priority::High => "🟡 High".bright_yellow(),
                    Priority::Medium => "🟢 Medium".bright_green(),
                    Priority::Low => "🔵 Low".bright_blue(),
                }
            );
            println!(
                "   {} {}",
                "Type:".bright_cyan(),
                format!("{:?}", task_type_clone).bright_white()
            );

            if let Some(duration) = task.estimated_duration {
                println!("   {} {} minutes", "Est. Duration:".bright_cyan(), duration);
            }

            // Show helpful next steps
            show_quick_help("task-created");
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_interactive_delegation (lines 2772-2884) ---[0m
    async fn run_interactive_delegation(&self) -> Result<()> {
        use std::io::{self, Write};

        println!("🎯 Welcome to Interactive Delegation Mode");
        println!("   Type 'help' for commands, 'quit' to exit");
        println!();

        loop {
            print!("ccswarm> ");
            io::stdout().flush()?;

            let mut input = String::new();
            io::stdin().read_line(&mut input)?;
            let input = input.trim();

            match input {
                "quit" | "exit" => {
                    println!("👋 Goodbye!");
                    break;
                }
                "help" => {
                    println!("📚 Interactive Delegation Commands:");
                    println!("   analyze <task_description>  - Analyze task and suggest agent");
                    println!("   delegate <agent> <task>     - Delegate task to specific agent");
                    println!("   stats                       - Show delegation statistics");
                    println!("   agents                      - List available agents");
                    println!("   quit                        - Exit interactive mode");
                    println!();
                }
                "stats" => {
                    println!("📊 Delegation Statistics");
                    println!("   Feature not yet implemented");
                    println!();
                }
                "agents" => {
                    println!("🤖 Available Agents:");
                    println!("   • Frontend - React/TypeScript UI development");
                    println!("   • Backend - Node.js/Express API development");
                    println!("   • DevOps - Infrastructure and deployment");
                    println!("   • QA - Testing and quality assurance");
                    println!();
                }
                _ if input.starts_with("analyze ") => {
                    let task_desc = &input[8..];
                    if !task_desc.is_empty() {
                        // Directly call delegation analysis to avoid recursion
                        use crate::orchestrator::master_delegation::{
                            DelegationStrategy, MasterDelegationEngine,
                        };
                        let mut engine = MasterDelegationEngine::new(DelegationStrategy::Hybrid);
                        let task = Task::new(
                            "interactive-analysis".to_string(),
                            task_desc.to_string(),
                            Priority::Medium,
                            TaskType::Development,
                        );

                        match engine.delegate_task(task) {
                            Ok(decision) => {
                                println!("🔍 Task Analysis Results");
                                println!("   Task: {}", task_desc);
                                println!("   Recommended Agent: {}", decision.target_agent.name());
                                println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                                println!("   Reasoning: {}", decision.reasoning);
                                if let Some(duration) = decision.estimated_duration {
                                    println!("   Estimated Duration: {} seconds", duration);
                                }
                            }
                            Err(e) => {
                                println!("❌ Analysis failed: {}", e);
                            }
                        }
                        println!();
                    } else {
                        println!("❌ Please provide a task description");
                        println!("   Example: analyze Create login form with validation");
                        println!();
                    }
                }
                _ if input.starts_with("delegate ") => {
                    let parts: Vec<&str> = input[9..].splitn(2, ' ').collect();
                    if parts.len() == 2 {
                        let agent = parts[0];
                        let task_desc = parts[1];

                        if ["frontend", "backend", "devops", "qa"].contains(&agent) {
                            println!("🎯 Delegating '{}' to {} agent", task_desc, agent);
                            println!("   ✅ Task queued for delegation");
                            println!();
                        } else {
                            println!("❌ Unknown agent: {}", agent);
                            println!("   Available: frontend, backend, devops, qa");
                            println!();
                        }
                    } else {
                        println!("❌ Usage: delegate <agent> <task_description>");
                        println!("   Example: delegate frontend Create responsive navigation bar");
                        println!();
                    }
                }
                "" => {
                    // Empty input, continue
                }
                _ => {
                    println!("❓ Unknown command: {}", input);
                    println!("   Type 'help' for available commands");
                    println!();
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1737-1832 method list_tasks <-> ./crates/ccswarm/src/cli/mod.rs:2130-2238 method show_task_stats
  Similarity: 89.59%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_tasks (lines 1737-1832) ---[0m
    async fn list_tasks(
        &self,
        all: bool,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
        detailed: bool,
    ) -> Result<()> {
        let tasks = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.list_tasks(status_filter, agent_filter).await
        } else {
            Vec::new()
        };

        let displayed_tasks = if all {
            tasks
        } else {
            tasks.into_iter().take(50).collect()
        };

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "tasks": displayed_tasks,
                    "total": displayed_tasks.len(),
                    "filters": {
                        "all": all,
                        "status": status_filter,
                        "agent": agent_filter,
                        "detailed": detailed
                    }
                }))?
            );
        } else {
            println!("📋 Task List");
            println!("============");

            if displayed_tasks.is_empty() {
                println!("No tasks in queue currently.");
                println!();
                println!("💡 Add a task with: ccswarm task add \"Your task description\"");
            } else {
                println!("Found {} tasks:", displayed_tasks.len());
                println!();

                for task in &displayed_tasks {
                    let status_emoji = match &task.status {
                        TaskStatus::Pending => "⏳",
                        TaskStatus::Assigned { .. } => "📋",
                        TaskStatus::InProgress { .. } => "🏃",
                        TaskStatus::Completed { .. } => "✅",
                        TaskStatus::Failed { .. } => "❌",
                        TaskStatus::Cancelled { .. } => "🚫",
                    };

                    let priority_emoji = match task.task.priority {
                        Priority::Critical => "🚨",
                        Priority::High => "🔥",
                        Priority::Medium => "📅",
                        Priority::Low => "💤",
                    };

                    println!(
                        "{} {} {} [{}] {}",
                        status_emoji,
                        priority_emoji,
                        &task.task.id[..8], // Short ID
                        task.task.task_type,
                        task.task.description
                    );

                    if let Some(agent) = &task.assigned_agent {
                        println!("   👤 Assigned to: {}", agent);
                    }

                    if detailed {
                        println!(
                            "   ⏰ Created: {}",
                            task.created_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        println!(
                            "   🔄 Updated: {}",
                            task.updated_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(details) = &task.task.details {
                            println!("   📝 Details: {}", details);
                        }
                    }
                    println!();
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_stats (lines 2130-2238) ---[0m
    async fn show_task_stats(&self, detailed: bool, performance: bool) -> Result<()> {
        if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "queue_stats": queue_stats,
                        "execution_stats": execution_stats,
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("📊 Task Queue Statistics");
                println!("========================");
                println!("⏳ Pending: {}", queue_stats.pending_count);
                println!("🏃 In Progress: {}", queue_stats.active_count);
                println!("✅ Completed: {}", queue_stats.completed_count);
                println!("❌ Failed: {}", queue_stats.failed_count);
                println!("📋 Total: {}", queue_stats.total_count);

                if performance || detailed {
                    println!();
                    println!("🎯 Execution Statistics");
                    println!("=======================");
                    println!("Tasks Executed: {}", execution_stats.tasks_executed);
                    println!(
                        "Success Rate: {:.1}%",
                        if execution_stats.tasks_executed > 0 {
                            (execution_stats.tasks_succeeded as f64
                                / execution_stats.tasks_executed as f64)
                                * 100.0
                        } else {
                            0.0
                        }
                    );
                    println!(
                        "Average Duration: {:.2}s",
                        execution_stats.average_duration.as_secs_f64()
                    );
                    println!(
                        "Total Duration: {:.2}s",
                        execution_stats.total_duration.as_secs_f64()
                    );
                    println!(
                        "Orchestration Usage: {:.1}%",
                        execution_stats.orchestration_usage
                    );
                }

                if detailed {
                    println!();
                    println!("📈 Queue Health");
                    println!("===============");
                    let failure_rate = if queue_stats.total_count > 0 {
                        (queue_stats.failed_count as f64 / queue_stats.total_count as f64) * 100.0
                    } else {
                        0.0
                    };

                    let health_emoji = if failure_rate < 5.0 {
                        "🟢"
                    } else if failure_rate < 15.0 {
                        "🟡"
                    } else {
                        "🔴"
                    };

                    println!(
                        "{} Overall Health: {:.1}% failure rate",
                        health_emoji, failure_rate
                    );

                    if queue_stats.active_count > 10 {
                        println!(
                            "⚠️  High concurrent load: {} tasks",
                            queue_stats.active_count
                        );
                    }

                    if queue_stats.pending_count > 50 {
                        println!(
                            "⚠️  Queue backlog: {} pending tasks",
                            queue_stats.pending_count
                        );
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "execution_engine_unavailable",
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot display task statistics");
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2130-2238 method show_task_stats <-> ./crates/ccswarm/src/cli/mod.rs:4581-4682 method handle_evolution
  Similarity: 86.49%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_stats (lines 2130-2238) ---[0m
    async fn show_task_stats(&self, detailed: bool, performance: bool) -> Result<()> {
        if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "queue_stats": queue_stats,
                        "execution_stats": execution_stats,
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("📊 Task Queue Statistics");
                println!("========================");
                println!("⏳ Pending: {}", queue_stats.pending_count);
                println!("🏃 In Progress: {}", queue_stats.active_count);
                println!("✅ Completed: {}", queue_stats.completed_count);
                println!("❌ Failed: {}", queue_stats.failed_count);
                println!("📋 Total: {}", queue_stats.total_count);

                if performance || detailed {
                    println!();
                    println!("🎯 Execution Statistics");
                    println!("=======================");
                    println!("Tasks Executed: {}", execution_stats.tasks_executed);
                    println!(
                        "Success Rate: {:.1}%",
                        if execution_stats.tasks_executed > 0 {
                            (execution_stats.tasks_succeeded as f64
                                / execution_stats.tasks_executed as f64)
                                * 100.0
                        } else {
                            0.0
                        }
                    );
                    println!(
                        "Average Duration: {:.2}s",
                        execution_stats.average_duration.as_secs_f64()
                    );
                    println!(
                        "Total Duration: {:.2}s",
                        execution_stats.total_duration.as_secs_f64()
                    );
                    println!(
                        "Orchestration Usage: {:.1}%",
                        execution_stats.orchestration_usage
                    );
                }

                if detailed {
                    println!();
                    println!("📈 Queue Health");
                    println!("===============");
                    let failure_rate = if queue_stats.total_count > 0 {
                        (queue_stats.failed_count as f64 / queue_stats.total_count as f64) * 100.0
                    } else {
                        0.0
                    };

                    let health_emoji = if failure_rate < 5.0 {
                        "🟢"
                    } else if failure_rate < 15.0 {
                        "🟡"
                    } else {
                        "🔴"
                    };

                    println!(
                        "{} Overall Health: {:.1}% failure rate",
                        health_emoji, failure_rate
                    );

                    if queue_stats.active_count > 10 {
                        println!(
                            "⚠️  High concurrent load: {} tasks",
                            queue_stats.active_count
                        );
                    }

                    if queue_stats.pending_count > 50 {
                        println!(
                            "⚠️  Queue backlog: {} pending tasks",
                            queue_stats.pending_count
                        );
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "execution_engine_unavailable",
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot display task statistics");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_evolution (lines 4581-4682) ---[0m
    async fn handle_evolution(&self, action: &EvolutionAction) -> Result<()> {
        match action {
            EvolutionAction::Metrics { agent, all, period } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution metrics",
                            "agent": agent,
                            "all": all,
                            "period_days": period,
                        }))?
                    );
                } else {
                    println!("📈 Evolution Metrics");
                    println!("==================");
                    if *all {
                        println!("Showing metrics for all agents");
                    } else if let Some(a) = agent {
                        println!("Agent: {}", a);
                    }
                    println!("Period: {} days", period);
                    println!("\nNo metrics available");
                }
            }

            EvolutionAction::Patterns {
                pattern_type,
                successful,
                failed,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution patterns",
                            "pattern_type": pattern_type,
                            "filter": {
                                "successful": successful,
                                "failed": failed,
                            },
                        }))?
                    );
                } else {
                    println!("🔮 Evolution Patterns");
                    println!("===================");
                    if *successful {
                        println!("Showing successful patterns");
                    } else if *failed {
                        println!("Showing failed patterns");
                    }
                    println!("No patterns found");
                }
            }

            EvolutionAction::Genealogy { extension, full } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension genealogy",
                            "extension": extension,
                            "full_tree": full,
                        }))?
                    );
                } else {
                    println!("🌳 Extension Genealogy: {}", extension);
                    println!("======================");
                    if *full {
                        println!("Showing full genealogy tree");
                    }
                    println!("No genealogy data available");
                }
            }

            EvolutionAction::Report { format, output } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution report generated",
                            "format": format,
                            "output": output,
                        }))?
                    );
                } else {
                    println!("📊 Generating evolution report");
                    println!("   Format: {}", format);
                    if let Some(o) = output {
                        println!("   Output: {}", o.display());
                    }
                    println!("✅ Report generated");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:3545-3626 method handle_auto_create <-> ./crates/ccswarm/src/cli/mod.rs:5468-5582 method handle_health
  Similarity: 91.66%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_auto_create (lines 3545-3626) ---[0m
    async fn handle_auto_create(
        &self,
        description: &str,
        template: Option<&str>,
        auto_deploy: bool,
        output: &PathBuf,
    ) -> Result<()> {
        use crate::orchestrator::auto_create::AutoCreateEngine;

        info!("🚀 Starting auto-create for: {}", description);

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "started",
                    "message": "Auto-create process initiated",
                    "description": description,
                    "template": template,
                    "auto_deploy": auto_deploy,
                    "output": output,
                }))?
            );
        } else {
            println!("🚀 ccswarm Auto-Create");
            println!("====================");
            println!("📋 Request: {}", description);
            if let Some(tmpl) = template {
                println!("📄 Template: {}", tmpl);
            }
            println!("📂 Output: {}", output.display());
            println!();
        }

        // Create auto-create engine
        let mut engine = AutoCreateEngine::new();

        // Execute auto-create workflow
        match engine
            .execute_auto_create(description, &self.config, output)
            .await
        {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Application created successfully",
                            "output": output,
                        }))?
                    );
                } else {
                    println!("\n✅ Application created successfully!");
                    println!("📂 Location: {}", output.display());

                    if auto_deploy {
                        println!("\n🚀 Auto-deploying application...");
                        // TODO: Implement auto-deployment
                        println!("   ⚠️ Auto-deployment not yet implemented");
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "error",
                            "message": "Auto-create failed",
                            "error": e.to_string(),
                        }))?
                    );
                } else {
                    println!("\n❌ Auto-create failed: {}", e);
                }
                return Err(e);
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_health (lines 5468-5582) ---[0m
    async fn handle_health(
        &self,
        check_agents: bool,
        check_sessions: bool,
        resources: bool,
        diagnose: bool,
        detailed: bool,
        format: &str,
    ) -> Result<()> {
        use crate::cli::health::{run_diagnostics, HealthChecker};
        use crate::coordination::StatusTracker;

        // Run diagnostics if requested
        if diagnose {
            return run_diagnostics(&self.repo_path).await;
        }

        // Initialize health checker
        let status_tracker = StatusTracker::new().await?;
        let health_checker = HealthChecker::new(status_tracker);

        // Add monitoring system if available
        // TODO: Get monitoring system from running orchestrator
        // let monitoring = MonitoringSystem::new();
        // let health_checker = health_checker.with_monitoring(monitoring);

        // Perform health checks based on flags
        let report = if check_agents && !check_sessions && !resources {
            // Only check agents
            let checks = health_checker.check_agents_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let total_agents = checks.len();
            let healthy_agents = checks
                .iter()
                .filter(|c| c.status == health::HealthStatus::Healthy)
                .count();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents,
                healthy_agents,
                active_tasks: 0,
                session_count: 0,
            }
        } else if check_sessions && !check_agents && !resources {
            // Only check sessions
            let checks = health_checker.check_sessions_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let session_count = checks.len();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents: 0,
                healthy_agents: 0,
                active_tasks: 0,
                session_count,
            }
        } else {
            // Full health check
            health_checker.check_all().await?
        };

        // Output results based on format
        match format {
            "json" => {
                println!("{}", serde_json::to_string_pretty(&report)?);
            }
            _ => {
                if detailed {
                    report.print_detailed();
                } else {
                    report.print_summary();
                }
            }
        }

        // Exit with non-zero code if unhealthy
        if report.overall_status == health::HealthStatus::Critical {
            std::process::exit(1);
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1218-1294 method init_project <-> ./crates/ccswarm/src/cli/mod.rs:1545-1666 method add_task
  Similarity: 90.59%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:init_project (lines 1218-1294) ---[0m
    async fn init_project(
        &self,
        name: &str,
        repo_url: Option<&str>,
        agents: &[String],
    ) -> Result<()> {
        use crate::utils::user_error::CommonErrors;

        info!("Initializing ccswarm project: {}", name);

        // Show progress to user
        println!(
            "{}",
            format!("🚀 Initializing ccswarm project: {}", name)
                .bright_cyan()
                .bold()
        );
        println!();

        // Check if git is available
        if !crate::git::shell::ShellWorktreeManager::is_git_available() {
            CommonErrors::git_not_initialized().display();
            return Err(anyhow!("Git is required for ccswarm"));
        }

        // Initialize Git repository if needed
        crate::utils::user_error::show_progress("Setting up git repository...");
        crate::git::shell::ShellWorktreeManager::init_if_needed(&self.repo_path)
            .await
            .inspect_err(|e| {
                eprintln!();
                CommonErrors::git_not_initialized()
                    .with_details(e.to_string())
                    .display();
            })?;
        println!("✅ Git repository ready");

        // Create configuration
        crate::utils::user_error::show_progress("Creating project configuration...");
        let mut config = create_default_config(&self.repo_path)?;
        config.project.name = name.to_string();

        if let Some(url) = repo_url {
            config.project.repository.url = url.to_string();
        }

        // Add requested agents
        for agent_type in agents {
            let agent_config = crate::config::AgentConfig {
                specialization: agent_type.clone(),
                worktree: format!("agents/{}-agent", agent_type),
                branch: format!("feature/{}", agent_type),
                claude_config: crate::config::ClaudeConfig::for_agent(agent_type),
                claude_md_template: format!("{}_specialist", agent_type),
            };
            config.agents.insert(agent_type.clone(), agent_config);
        }

        // Save configuration
        let config_file = self.repo_path.join("ccswarm.json");
        config.to_file(config_file).await?;

        let data = serde_json::json!({
            "project": name,
            "agents": agents,
        });

        println!(
            "{}",
            self.formatter.format_success(
                &format!("ccswarm project '{}' initialized", name),
                Some(data)
            )
        );

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:add_task (lines 1545-1666) ---[0m
    async fn add_task(
        &self,
        description: &str,
        priority: &str,
        task_type: &str,
        details: Option<&str>,
        duration: Option<u32>,
    ) -> Result<()> {
        // use crate::cli::progress::{ProgressStyle, ProgressTracker};
        use crate::utils::user_error::CommonErrors;

        // Validate task description
        if description.trim().is_empty() {
            CommonErrors::invalid_task_format()
                .with_details("Task description cannot be empty")
                .with_suggestion("Provide a clear, actionable task description")
                .display();
            return Err(anyhow!("Invalid task description"));
        }

        // Create task (simplified progress display)
        println!(
            "Creating task: {}...",
            description.chars().take(50).collect::<String>()
        );

        let priority = match priority.to_lowercase().as_str() {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };

        let task_type = match task_type.to_lowercase().as_str() {
            "development" | "dev" => TaskType::Development,
            "testing" | "test" => TaskType::Testing,
            "documentation" | "docs" => TaskType::Documentation,
            "infrastructure" | "infra" => TaskType::Infrastructure,
            "coordination" => TaskType::Coordination,
            "review" => TaskType::Review,
            "bugfix" | "bug" => TaskType::Bugfix,
            "feature" => TaskType::Feature,
            _ => TaskType::Development,
        };

        let task_type_clone = task_type;
        let mut task = Task::new(
            uuid::Uuid::new_v4().to_string(),
            description.to_string(),
            priority,
            task_type,
        );

        if let Some(details) = details {
            task = task.with_details(details.to_string());
        }

        if let Some(duration) = duration {
            task = task.with_duration(duration);
        }

        // Add to task queue via execution engine
        let task_id = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.add_task(task.clone()).await
        } else {
            // Fall back to creating task with ID but warn user
            warn!("Execution engine not available, task will not be executed");
            task.id.clone()
        };

        // Complete progress indicator
        // ProgressTracker::complete(progress, true, Some("Task created successfully".to_string())).await;

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Task added",
                    "task_id": task_id,
                    "description": description,
                    "priority": priority,
                }))?
            );
        } else {
            println!();
            println!("{}", "✅ Task created successfully!".bright_green().bold());
            println!();
            println!(
                "   {} {}",
                "Task ID:".bright_cyan(),
                task_id[..8].bright_white()
            );
            println!("   {} {}", "Description:".bright_cyan(), description);
            println!(
                "   {} {}",
                "Priority:".bright_cyan(),
                match priority {
                    Priority::Critical => "🔴 Critical".bright_red(),
                    Priority::High => "🟡 High".bright_yellow(),
                    Priority::Medium => "🟢 Medium".bright_green(),
                    Priority::Low => "🔵 Low".bright_blue(),
                }
            );
            println!(
                "   {} {}",
                "Type:".bright_cyan(),
                format!("{:?}", task_type_clone).bright_white()
            );

            if let Some(duration) = task.estimated_duration {
                println!("   {} {} minutes", "Est. Duration:".bright_cyan(), duration);
            }

            // Show helpful next steps
            show_quick_help("task-created");
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1835-1933 method show_task_status <-> ./crates/ccswarm/src/cli/mod.rs:5468-5582 method handle_health
  Similarity: 83.95%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_status (lines 1835-1933) ---[0m
    async fn show_task_status(
        &self,
        task_id: &str,
        history: bool,
        orchestration: bool,
    ) -> Result<()> {
        let task = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.get_task(task_id).await
        } else {
            None
        };

        if let Some(task) = task {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "task": task,
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");

                let status_emoji = match &task.status {
                    TaskStatus::Pending => "⏳",
                    TaskStatus::Assigned { .. } => "📋",
                    TaskStatus::InProgress { .. } => "🏃",
                    TaskStatus::Completed { .. } => "✅",
                    TaskStatus::Failed { .. } => "❌",
                    TaskStatus::Cancelled { .. } => "🚫",
                };

                println!("{} Status: {:?}", status_emoji, task.status);
                println!("📝 Description: {}", task.task.description);
                println!("🎯 Priority: {:?}", task.task.priority);
                println!("📋 Type: {:?}", task.task.task_type);
                println!(
                    "⏰ Created: {}",
                    task.created_at.format("%Y-%m-%d %H:%M:%S")
                );
                println!(
                    "🔄 Updated: {}",
                    task.updated_at.format("%Y-%m-%d %H:%M:%S")
                );

                if let Some(agent) = &task.assigned_agent {
                    println!("👤 Assigned to: {}", agent);
                }

                if let Some(details) = &task.task.details {
                    println!("📝 Details: {}", details);
                }

                if history && !task.execution_history.is_empty() {
                    println!();
                    println!("📚 Execution History:");
                    for (i, attempt) in task.execution_history.iter().enumerate() {
                        println!(
                            "  {}. Agent: {} | Started: {}",
                            i + 1,
                            attempt.agent_id,
                            attempt.started_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(completed) = attempt.completed_at {
                            println!("     Completed: {}", completed.format("%Y-%m-%d %H:%M:%S"));
                        }
                        if let Some(error) = &attempt.error {
                            println!("     Error: {}", error);
                        }
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "status": "not_found",
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");
                println!("❌ Task not found");
                println!();
                println!("💡 Use 'ccswarm task list' to see available tasks");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_health (lines 5468-5582) ---[0m
    async fn handle_health(
        &self,
        check_agents: bool,
        check_sessions: bool,
        resources: bool,
        diagnose: bool,
        detailed: bool,
        format: &str,
    ) -> Result<()> {
        use crate::cli::health::{run_diagnostics, HealthChecker};
        use crate::coordination::StatusTracker;

        // Run diagnostics if requested
        if diagnose {
            return run_diagnostics(&self.repo_path).await;
        }

        // Initialize health checker
        let status_tracker = StatusTracker::new().await?;
        let health_checker = HealthChecker::new(status_tracker);

        // Add monitoring system if available
        // TODO: Get monitoring system from running orchestrator
        // let monitoring = MonitoringSystem::new();
        // let health_checker = health_checker.with_monitoring(monitoring);

        // Perform health checks based on flags
        let report = if check_agents && !check_sessions && !resources {
            // Only check agents
            let checks = health_checker.check_agents_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let total_agents = checks.len();
            let healthy_agents = checks
                .iter()
                .filter(|c| c.status == health::HealthStatus::Healthy)
                .count();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents,
                healthy_agents,
                active_tasks: 0,
                session_count: 0,
            }
        } else if check_sessions && !check_agents && !resources {
            // Only check sessions
            let checks = health_checker.check_sessions_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let session_count = checks.len();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents: 0,
                healthy_agents: 0,
                active_tasks: 0,
                session_count,
            }
        } else {
            // Full health check
            health_checker.check_all().await?
        };

        // Output results based on format
        match format {
            "json" => {
                println!("{}", serde_json::to_string_pretty(&report)?);
            }
            _ => {
                if detailed {
                    report.print_detailed();
                } else {
                    report.print_summary();
                }
            }
        }

        // Exit with non-zero code if unhealthy
        if report.overall_status == health::HealthStatus::Critical {
            std::process::exit(1);
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1737-1832 method list_tasks <-> ./crates/ccswarm/src/cli/mod.rs:4581-4682 method handle_evolution
  Similarity: 89.89%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_tasks (lines 1737-1832) ---[0m
    async fn list_tasks(
        &self,
        all: bool,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
        detailed: bool,
    ) -> Result<()> {
        let tasks = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.list_tasks(status_filter, agent_filter).await
        } else {
            Vec::new()
        };

        let displayed_tasks = if all {
            tasks
        } else {
            tasks.into_iter().take(50).collect()
        };

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "tasks": displayed_tasks,
                    "total": displayed_tasks.len(),
                    "filters": {
                        "all": all,
                        "status": status_filter,
                        "agent": agent_filter,
                        "detailed": detailed
                    }
                }))?
            );
        } else {
            println!("📋 Task List");
            println!("============");

            if displayed_tasks.is_empty() {
                println!("No tasks in queue currently.");
                println!();
                println!("💡 Add a task with: ccswarm task add \"Your task description\"");
            } else {
                println!("Found {} tasks:", displayed_tasks.len());
                println!();

                for task in &displayed_tasks {
                    let status_emoji = match &task.status {
                        TaskStatus::Pending => "⏳",
                        TaskStatus::Assigned { .. } => "📋",
                        TaskStatus::InProgress { .. } => "🏃",
                        TaskStatus::Completed { .. } => "✅",
                        TaskStatus::Failed { .. } => "❌",
                        TaskStatus::Cancelled { .. } => "🚫",
                    };

                    let priority_emoji = match task.task.priority {
                        Priority::Critical => "🚨",
                        Priority::High => "🔥",
                        Priority::Medium => "📅",
                        Priority::Low => "💤",
                    };

                    println!(
                        "{} {} {} [{}] {}",
                        status_emoji,
                        priority_emoji,
                        &task.task.id[..8], // Short ID
                        task.task.task_type,
                        task.task.description
                    );

                    if let Some(agent) = &task.assigned_agent {
                        println!("   👤 Assigned to: {}", agent);
                    }

                    if detailed {
                        println!(
                            "   ⏰ Created: {}",
                            task.created_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        println!(
                            "   🔄 Updated: {}",
                            task.updated_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(details) = &task.task.details {
                            println!("   📝 Details: {}", details);
                        }
                    }
                    println!();
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_evolution (lines 4581-4682) ---[0m
    async fn handle_evolution(&self, action: &EvolutionAction) -> Result<()> {
        match action {
            EvolutionAction::Metrics { agent, all, period } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution metrics",
                            "agent": agent,
                            "all": all,
                            "period_days": period,
                        }))?
                    );
                } else {
                    println!("📈 Evolution Metrics");
                    println!("==================");
                    if *all {
                        println!("Showing metrics for all agents");
                    } else if let Some(a) = agent {
                        println!("Agent: {}", a);
                    }
                    println!("Period: {} days", period);
                    println!("\nNo metrics available");
                }
            }

            EvolutionAction::Patterns {
                pattern_type,
                successful,
                failed,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution patterns",
                            "pattern_type": pattern_type,
                            "filter": {
                                "successful": successful,
                                "failed": failed,
                            },
                        }))?
                    );
                } else {
                    println!("🔮 Evolution Patterns");
                    println!("===================");
                    if *successful {
                        println!("Showing successful patterns");
                    } else if *failed {
                        println!("Showing failed patterns");
                    }
                    println!("No patterns found");
                }
            }

            EvolutionAction::Genealogy { extension, full } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension genealogy",
                            "extension": extension,
                            "full_tree": full,
                        }))?
                    );
                } else {
                    println!("🌳 Extension Genealogy: {}", extension);
                    println!("======================");
                    if *full {
                        println!("Showing full genealogy tree");
                    }
                    println!("No genealogy data available");
                }
            }

            EvolutionAction::Report { format, output } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution report generated",
                            "format": format,
                            "output": output,
                        }))?
                    );
                } else {
                    println!("📊 Generating evolution report");
                    println!("   Format: {}", format);
                    if let Some(o) = output {
                        println!("   Output: {}", o.display());
                    }
                    println!("✅ Report generated");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1835-1933 method show_task_status <-> ./crates/ccswarm/src/cli/mod.rs:1993-2078 method show_task_history
  Similarity: 94.46%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_status (lines 1835-1933) ---[0m
    async fn show_task_status(
        &self,
        task_id: &str,
        history: bool,
        orchestration: bool,
    ) -> Result<()> {
        let task = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.get_task(task_id).await
        } else {
            None
        };

        if let Some(task) = task {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "task": task,
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");

                let status_emoji = match &task.status {
                    TaskStatus::Pending => "⏳",
                    TaskStatus::Assigned { .. } => "📋",
                    TaskStatus::InProgress { .. } => "🏃",
                    TaskStatus::Completed { .. } => "✅",
                    TaskStatus::Failed { .. } => "❌",
                    TaskStatus::Cancelled { .. } => "🚫",
                };

                println!("{} Status: {:?}", status_emoji, task.status);
                println!("📝 Description: {}", task.task.description);
                println!("🎯 Priority: {:?}", task.task.priority);
                println!("📋 Type: {:?}", task.task.task_type);
                println!(
                    "⏰ Created: {}",
                    task.created_at.format("%Y-%m-%d %H:%M:%S")
                );
                println!(
                    "🔄 Updated: {}",
                    task.updated_at.format("%Y-%m-%d %H:%M:%S")
                );

                if let Some(agent) = &task.assigned_agent {
                    println!("👤 Assigned to: {}", agent);
                }

                if let Some(details) = &task.task.details {
                    println!("📝 Details: {}", details);
                }

                if history && !task.execution_history.is_empty() {
                    println!();
                    println!("📚 Execution History:");
                    for (i, attempt) in task.execution_history.iter().enumerate() {
                        println!(
                            "  {}. Agent: {} | Started: {}",
                            i + 1,
                            attempt.agent_id,
                            attempt.started_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(completed) = attempt.completed_at {
                            println!("     Completed: {}", completed.format("%Y-%m-%d %H:%M:%S"));
                        }
                        if let Some(error) = &attempt.error {
                            println!("     Error: {}", error);
                        }
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "status": "not_found",
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");
                println!("❌ Task not found");
                println!();
                println!("💡 Use 'ccswarm task list' to see available tasks");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_history (lines 1993-2078) ---[0m
    async fn show_task_history(
        &self,
        limit: usize,
        agent_filter: Option<&str>,
        failed_only: bool,
    ) -> Result<()> {
        let history = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.get_execution_history(Some(limit)).await
        } else {
            Vec::new()
        };

        let filtered_history: Vec<_> = history
            .into_iter()
            .filter(|result| {
                if failed_only && result.success {
                    return false;
                }
                if let Some(agent) = agent_filter {
                    if let Some(ref result_agent) = result.agent_used {
                        return result_agent == agent;
                    }
                    return false;
                }
                true
            })
            .collect();

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "history": filtered_history,
                    "limit": limit,
                    "agent_filter": agent_filter,
                    "failed_only": failed_only,
                    "total_count": filtered_history.len()
                }))?
            );
        } else {
            println!("📚 Task History (Last {} tasks)", limit);
            println!("================================");

            if filtered_history.is_empty() {
                println!("No task history available.");
            } else {
                println!("Found {} execution records:", filtered_history.len());
                println!();

                for result in &filtered_history {
                    let status_emoji = if result.success { "✅" } else { "❌" };
                    let orchestration_indicator = if result.orchestration_used {
                        "🎯"
                    } else {
                        "🔄"
                    };

                    println!(
                        "{} {} Task: {} | Duration: {:.2}s",
                        status_emoji,
                        orchestration_indicator,
                        &result.task_id[..8],
                        result.duration.as_secs_f64()
                    );

                    if let Some(agent) = &result.agent_used {
                        println!("   👤 Agent: {}", agent);
                    }

                    if let Some(error) = &result.error {
                        println!("   ❌ Error: {}", error);
                    }
                    println!();
                }
            }

            if let Some(agent) = agent_filter {
                println!("Filter: Agent = {}", agent);
            }
            if failed_only {
                println!("Filter: Failed tasks only");
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1737-1832 method list_tasks <-> ./crates/ccswarm/src/cli/mod.rs:5468-5582 method handle_health
  Similarity: 82.00%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_tasks (lines 1737-1832) ---[0m
    async fn list_tasks(
        &self,
        all: bool,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
        detailed: bool,
    ) -> Result<()> {
        let tasks = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.list_tasks(status_filter, agent_filter).await
        } else {
            Vec::new()
        };

        let displayed_tasks = if all {
            tasks
        } else {
            tasks.into_iter().take(50).collect()
        };

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "tasks": displayed_tasks,
                    "total": displayed_tasks.len(),
                    "filters": {
                        "all": all,
                        "status": status_filter,
                        "agent": agent_filter,
                        "detailed": detailed
                    }
                }))?
            );
        } else {
            println!("📋 Task List");
            println!("============");

            if displayed_tasks.is_empty() {
                println!("No tasks in queue currently.");
                println!();
                println!("💡 Add a task with: ccswarm task add \"Your task description\"");
            } else {
                println!("Found {} tasks:", displayed_tasks.len());
                println!();

                for task in &displayed_tasks {
                    let status_emoji = match &task.status {
                        TaskStatus::Pending => "⏳",
                        TaskStatus::Assigned { .. } => "📋",
                        TaskStatus::InProgress { .. } => "🏃",
                        TaskStatus::Completed { .. } => "✅",
                        TaskStatus::Failed { .. } => "❌",
                        TaskStatus::Cancelled { .. } => "🚫",
                    };

                    let priority_emoji = match task.task.priority {
                        Priority::Critical => "🚨",
                        Priority::High => "🔥",
                        Priority::Medium => "📅",
                        Priority::Low => "💤",
                    };

                    println!(
                        "{} {} {} [{}] {}",
                        status_emoji,
                        priority_emoji,
                        &task.task.id[..8], // Short ID
                        task.task.task_type,
                        task.task.description
                    );

                    if let Some(agent) = &task.assigned_agent {
                        println!("   👤 Assigned to: {}", agent);
                    }

                    if detailed {
                        println!(
                            "   ⏰ Created: {}",
                            task.created_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        println!(
                            "   🔄 Updated: {}",
                            task.updated_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(details) = &task.task.details {
                            println!("   📝 Details: {}", details);
                        }
                    }
                    println!();
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_health (lines 5468-5582) ---[0m
    async fn handle_health(
        &self,
        check_agents: bool,
        check_sessions: bool,
        resources: bool,
        diagnose: bool,
        detailed: bool,
        format: &str,
    ) -> Result<()> {
        use crate::cli::health::{run_diagnostics, HealthChecker};
        use crate::coordination::StatusTracker;

        // Run diagnostics if requested
        if diagnose {
            return run_diagnostics(&self.repo_path).await;
        }

        // Initialize health checker
        let status_tracker = StatusTracker::new().await?;
        let health_checker = HealthChecker::new(status_tracker);

        // Add monitoring system if available
        // TODO: Get monitoring system from running orchestrator
        // let monitoring = MonitoringSystem::new();
        // let health_checker = health_checker.with_monitoring(monitoring);

        // Perform health checks based on flags
        let report = if check_agents && !check_sessions && !resources {
            // Only check agents
            let checks = health_checker.check_agents_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let total_agents = checks.len();
            let healthy_agents = checks
                .iter()
                .filter(|c| c.status == health::HealthStatus::Healthy)
                .count();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents,
                healthy_agents,
                active_tasks: 0,
                session_count: 0,
            }
        } else if check_sessions && !check_agents && !resources {
            // Only check sessions
            let checks = health_checker.check_sessions_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let session_count = checks.len();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents: 0,
                healthy_agents: 0,
                active_tasks: 0,
                session_count,
            }
        } else {
            // Full health check
            health_checker.check_all().await?
        };

        // Output results based on format
        match format {
            "json" => {
                println!("{}", serde_json::to_string_pretty(&report)?);
            }
            _ => {
                if detailed {
                    report.print_detailed();
                } else {
                    report.print_summary();
                }
            }
        }

        // Exit with non-zero code if unhealthy
        if report.overall_status == health::HealthStatus::Critical {
            std::process::exit(1);
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1737-1832 method list_tasks <-> ./crates/ccswarm/src/cli/mod.rs:1993-2078 method show_task_history
  Similarity: 94.54%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_tasks (lines 1737-1832) ---[0m
    async fn list_tasks(
        &self,
        all: bool,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
        detailed: bool,
    ) -> Result<()> {
        let tasks = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.list_tasks(status_filter, agent_filter).await
        } else {
            Vec::new()
        };

        let displayed_tasks = if all {
            tasks
        } else {
            tasks.into_iter().take(50).collect()
        };

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "tasks": displayed_tasks,
                    "total": displayed_tasks.len(),
                    "filters": {
                        "all": all,
                        "status": status_filter,
                        "agent": agent_filter,
                        "detailed": detailed
                    }
                }))?
            );
        } else {
            println!("📋 Task List");
            println!("============");

            if displayed_tasks.is_empty() {
                println!("No tasks in queue currently.");
                println!();
                println!("💡 Add a task with: ccswarm task add \"Your task description\"");
            } else {
                println!("Found {} tasks:", displayed_tasks.len());
                println!();

                for task in &displayed_tasks {
                    let status_emoji = match &task.status {
                        TaskStatus::Pending => "⏳",
                        TaskStatus::Assigned { .. } => "📋",
                        TaskStatus::InProgress { .. } => "🏃",
                        TaskStatus::Completed { .. } => "✅",
                        TaskStatus::Failed { .. } => "❌",
                        TaskStatus::Cancelled { .. } => "🚫",
                    };

                    let priority_emoji = match task.task.priority {
                        Priority::Critical => "🚨",
                        Priority::High => "🔥",
                        Priority::Medium => "📅",
                        Priority::Low => "💤",
                    };

                    println!(
                        "{} {} {} [{}] {}",
                        status_emoji,
                        priority_emoji,
                        &task.task.id[..8], // Short ID
                        task.task.task_type,
                        task.task.description
                    );

                    if let Some(agent) = &task.assigned_agent {
                        println!("   👤 Assigned to: {}", agent);
                    }

                    if detailed {
                        println!(
                            "   ⏰ Created: {}",
                            task.created_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        println!(
                            "   🔄 Updated: {}",
                            task.updated_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(details) = &task.task.details {
                            println!("   📝 Details: {}", details);
                        }
                    }
                    println!();
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_history (lines 1993-2078) ---[0m
    async fn show_task_history(
        &self,
        limit: usize,
        agent_filter: Option<&str>,
        failed_only: bool,
    ) -> Result<()> {
        let history = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.get_execution_history(Some(limit)).await
        } else {
            Vec::new()
        };

        let filtered_history: Vec<_> = history
            .into_iter()
            .filter(|result| {
                if failed_only && result.success {
                    return false;
                }
                if let Some(agent) = agent_filter {
                    if let Some(ref result_agent) = result.agent_used {
                        return result_agent == agent;
                    }
                    return false;
                }
                true
            })
            .collect();

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "history": filtered_history,
                    "limit": limit,
                    "agent_filter": agent_filter,
                    "failed_only": failed_only,
                    "total_count": filtered_history.len()
                }))?
            );
        } else {
            println!("📚 Task History (Last {} tasks)", limit);
            println!("================================");

            if filtered_history.is_empty() {
                println!("No task history available.");
            } else {
                println!("Found {} execution records:", filtered_history.len());
                println!();

                for result in &filtered_history {
                    let status_emoji = if result.success { "✅" } else { "❌" };
                    let orchestration_indicator = if result.orchestration_used {
                        "🎯"
                    } else {
                        "🔄"
                    };

                    println!(
                        "{} {} Task: {} | Duration: {:.2}s",
                        status_emoji,
                        orchestration_indicator,
                        &result.task_id[..8],
                        result.duration.as_secs_f64()
                    );

                    if let Some(agent) = &result.agent_used {
                        println!("   👤 Agent: {}", agent);
                    }

                    if let Some(error) = &result.error {
                        println!("   ❌ Error: {}", error);
                    }
                    println!();
                }
            }

            if let Some(agent) = agent_filter {
                println!("Filter: Agent = {}", agent);
            }
            if failed_only {
                println!("Filter: Failed tasks only");
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1218-1294 method init_project <-> ./crates/ccswarm/src/cli/mod.rs:5468-5582 method handle_health
  Similarity: 89.44%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:init_project (lines 1218-1294) ---[0m
    async fn init_project(
        &self,
        name: &str,
        repo_url: Option<&str>,
        agents: &[String],
    ) -> Result<()> {
        use crate::utils::user_error::CommonErrors;

        info!("Initializing ccswarm project: {}", name);

        // Show progress to user
        println!(
            "{}",
            format!("🚀 Initializing ccswarm project: {}", name)
                .bright_cyan()
                .bold()
        );
        println!();

        // Check if git is available
        if !crate::git::shell::ShellWorktreeManager::is_git_available() {
            CommonErrors::git_not_initialized().display();
            return Err(anyhow!("Git is required for ccswarm"));
        }

        // Initialize Git repository if needed
        crate::utils::user_error::show_progress("Setting up git repository...");
        crate::git::shell::ShellWorktreeManager::init_if_needed(&self.repo_path)
            .await
            .inspect_err(|e| {
                eprintln!();
                CommonErrors::git_not_initialized()
                    .with_details(e.to_string())
                    .display();
            })?;
        println!("✅ Git repository ready");

        // Create configuration
        crate::utils::user_error::show_progress("Creating project configuration...");
        let mut config = create_default_config(&self.repo_path)?;
        config.project.name = name.to_string();

        if let Some(url) = repo_url {
            config.project.repository.url = url.to_string();
        }

        // Add requested agents
        for agent_type in agents {
            let agent_config = crate::config::AgentConfig {
                specialization: agent_type.clone(),
                worktree: format!("agents/{}-agent", agent_type),
                branch: format!("feature/{}", agent_type),
                claude_config: crate::config::ClaudeConfig::for_agent(agent_type),
                claude_md_template: format!("{}_specialist", agent_type),
            };
            config.agents.insert(agent_type.clone(), agent_config);
        }

        // Save configuration
        let config_file = self.repo_path.join("ccswarm.json");
        config.to_file(config_file).await?;

        let data = serde_json::json!({
            "project": name,
            "agents": agents,
        });

        println!(
            "{}",
            self.formatter.format_success(
                &format!("ccswarm project '{}' initialized", name),
                Some(data)
            )
        );

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_health (lines 5468-5582) ---[0m
    async fn handle_health(
        &self,
        check_agents: bool,
        check_sessions: bool,
        resources: bool,
        diagnose: bool,
        detailed: bool,
        format: &str,
    ) -> Result<()> {
        use crate::cli::health::{run_diagnostics, HealthChecker};
        use crate::coordination::StatusTracker;

        // Run diagnostics if requested
        if diagnose {
            return run_diagnostics(&self.repo_path).await;
        }

        // Initialize health checker
        let status_tracker = StatusTracker::new().await?;
        let health_checker = HealthChecker::new(status_tracker);

        // Add monitoring system if available
        // TODO: Get monitoring system from running orchestrator
        // let monitoring = MonitoringSystem::new();
        // let health_checker = health_checker.with_monitoring(monitoring);

        // Perform health checks based on flags
        let report = if check_agents && !check_sessions && !resources {
            // Only check agents
            let checks = health_checker.check_agents_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let total_agents = checks.len();
            let healthy_agents = checks
                .iter()
                .filter(|c| c.status == health::HealthStatus::Healthy)
                .count();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents,
                healthy_agents,
                active_tasks: 0,
                session_count: 0,
            }
        } else if check_sessions && !check_agents && !resources {
            // Only check sessions
            let checks = health_checker.check_sessions_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let session_count = checks.len();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents: 0,
                healthy_agents: 0,
                active_tasks: 0,
                session_count,
            }
        } else {
            // Full health check
            health_checker.check_all().await?
        };

        // Output results based on format
        match format {
            "json" => {
                println!("{}", serde_json::to_string_pretty(&report)?);
            }
            _ => {
                if detailed {
                    report.print_detailed();
                } else {
                    report.print_summary();
                }
            }
        }

        // Exit with non-zero code if unhealthy
        if report.overall_status == health::HealthStatus::Critical {
            std::process::exit(1);
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1296-1362 method start_orchestrator <-> ./crates/ccswarm/src/cli/mod.rs:5468-5582 method handle_health
  Similarity: 93.62%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_orchestrator (lines 1296-1362) ---[0m
    async fn start_orchestrator(
        &self,
        _daemon: bool,
        _port: u16,
        isolation: &str,
        use_real_api: bool,
    ) -> Result<()> {
        info!(
            "Starting ccswarm orchestrator with isolation mode: {} (real_api: {})",
            isolation, use_real_api
        );

        // Parse isolation mode
        let isolation_mode = match isolation {
            "container" => crate::agent::IsolationMode::Container,
            "hybrid" => crate::agent::IsolationMode::Hybrid,
            _ => crate::agent::IsolationMode::GitWorktree,
        };

        // Update configuration to use real API if requested
        let mut config = self.config.clone();
        if use_real_api {
            // Check for API key
            if std::env::var("ANTHROPIC_API_KEY").is_err() {
                return Err(anyhow::anyhow!(
                    "ANTHROPIC_API_KEY environment variable must be set when using --use-real-api"
                ));
            }

            // Update all agent configurations to use real API
            for agent_config in config.agents.values_mut() {
                agent_config.claude_config.use_real_api = true;
            }

            // Update master configuration
            config.project.master_claude.claude_config.use_real_api = true;
        }

        let mut master = MasterClaude::new(config, self.repo_path.clone()).await?;

        // Set isolation mode for all agents
        master.set_isolation_mode(isolation_mode);

        // Initialize agents
        master.initialize().await?;

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Orchestrator started",
                    "master_id": master.id,
                    "agents": master.agents.len(),
                }))?
            );
        } else {
            println!("🚀 ccswarm orchestrator started");
            println!("   Master ID: {}", master.id);
            println!("   Agents: {}", master.agents.len());
        }

        // Start coordination (this would run indefinitely in real usage)
        master.start_coordination().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_health (lines 5468-5582) ---[0m
    async fn handle_health(
        &self,
        check_agents: bool,
        check_sessions: bool,
        resources: bool,
        diagnose: bool,
        detailed: bool,
        format: &str,
    ) -> Result<()> {
        use crate::cli::health::{run_diagnostics, HealthChecker};
        use crate::coordination::StatusTracker;

        // Run diagnostics if requested
        if diagnose {
            return run_diagnostics(&self.repo_path).await;
        }

        // Initialize health checker
        let status_tracker = StatusTracker::new().await?;
        let health_checker = HealthChecker::new(status_tracker);

        // Add monitoring system if available
        // TODO: Get monitoring system from running orchestrator
        // let monitoring = MonitoringSystem::new();
        // let health_checker = health_checker.with_monitoring(monitoring);

        // Perform health checks based on flags
        let report = if check_agents && !check_sessions && !resources {
            // Only check agents
            let checks = health_checker.check_agents_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let total_agents = checks.len();
            let healthy_agents = checks
                .iter()
                .filter(|c| c.status == health::HealthStatus::Healthy)
                .count();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents,
                healthy_agents,
                active_tasks: 0,
                session_count: 0,
            }
        } else if check_sessions && !check_agents && !resources {
            // Only check sessions
            let checks = health_checker.check_sessions_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let session_count = checks.len();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents: 0,
                healthy_agents: 0,
                active_tasks: 0,
                session_count,
            }
        } else {
            // Full health check
            health_checker.check_all().await?
        };

        // Output results based on format
        match format {
            "json" => {
                println!("{}", serde_json::to_string_pretty(&report)?);
            }
            _ => {
                if detailed {
                    report.print_detailed();
                } else {
                    report.print_summary();
                }
            }
        }

        // Exit with non-zero code if unhealthy
        if report.overall_status == health::HealthStatus::Critical {
            std::process::exit(1);
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2130-2238 method show_task_stats <-> ./crates/ccswarm/src/cli/mod.rs:2480-2543 method handle_config
  Similarity: 98.48%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_stats (lines 2130-2238) ---[0m
    async fn show_task_stats(&self, detailed: bool, performance: bool) -> Result<()> {
        if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "queue_stats": queue_stats,
                        "execution_stats": execution_stats,
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("📊 Task Queue Statistics");
                println!("========================");
                println!("⏳ Pending: {}", queue_stats.pending_count);
                println!("🏃 In Progress: {}", queue_stats.active_count);
                println!("✅ Completed: {}", queue_stats.completed_count);
                println!("❌ Failed: {}", queue_stats.failed_count);
                println!("📋 Total: {}", queue_stats.total_count);

                if performance || detailed {
                    println!();
                    println!("🎯 Execution Statistics");
                    println!("=======================");
                    println!("Tasks Executed: {}", execution_stats.tasks_executed);
                    println!(
                        "Success Rate: {:.1}%",
                        if execution_stats.tasks_executed > 0 {
                            (execution_stats.tasks_succeeded as f64
                                / execution_stats.tasks_executed as f64)
                                * 100.0
                        } else {
                            0.0
                        }
                    );
                    println!(
                        "Average Duration: {:.2}s",
                        execution_stats.average_duration.as_secs_f64()
                    );
                    println!(
                        "Total Duration: {:.2}s",
                        execution_stats.total_duration.as_secs_f64()
                    );
                    println!(
                        "Orchestration Usage: {:.1}%",
                        execution_stats.orchestration_usage
                    );
                }

                if detailed {
                    println!();
                    println!("📈 Queue Health");
                    println!("===============");
                    let failure_rate = if queue_stats.total_count > 0 {
                        (queue_stats.failed_count as f64 / queue_stats.total_count as f64) * 100.0
                    } else {
                        0.0
                    };

                    let health_emoji = if failure_rate < 5.0 {
                        "🟢"
                    } else if failure_rate < 15.0 {
                        "🟡"
                    } else {
                        "🔴"
                    };

                    println!(
                        "{} Overall Health: {:.1}% failure rate",
                        health_emoji, failure_rate
                    );

                    if queue_stats.active_count > 10 {
                        println!(
                            "⚠️  High concurrent load: {} tasks",
                            queue_stats.active_count
                        );
                    }

                    if queue_stats.pending_count > 50 {
                        println!(
                            "⚠️  Queue backlog: {} pending tasks",
                            queue_stats.pending_count
                        );
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "execution_engine_unavailable",
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot display task statistics");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_config (lines 2480-2543) ---[0m
    async fn handle_config(&self, action: &ConfigAction) -> Result<()> {
        match action {
            ConfigAction::Generate { output, template } => {
                let config = match template.as_str() {
                    "minimal" => create_minimal_config(&self.repo_path)?,
                    "frontend-only" => create_frontend_only_config(&self.repo_path)?,
                    "full-stack" => create_default_config(&self.repo_path)?,
                    _ => create_default_config(&self.repo_path)?,
                };

                config.to_file(output.clone()).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Configuration generated",
                            "file": output,
                            "template": template,
                        }))?
                    );
                } else {
                    println!("✅ Configuration generated: {}", output.display());
                    println!("   Template: {}", template);
                }
            }
            ConfigAction::Validate { file } => match CcswarmConfig::from_file(file.clone()).await {
                Ok(_) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "Configuration is valid",
                                "file": file,
                            }))?
                        );
                    } else {
                        println!("✅ Configuration is valid: {}", file.display());
                    }
                }
                Err(e) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "Configuration is invalid",
                                "file": file,
                                "error": e.to_string(),
                            }))?
                        );
                    } else {
                        println!("❌ Configuration is invalid: {}", file.display());
                        println!("   Error: {}", e);
                    }
                    return Err(e);
                }
            },
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2546-2609 function create_default_config <-> ./crates/ccswarm/src/cli/mod.rs:2772-2884 method run_interactive_delegation
  Similarity: 95.84%

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_default_config (lines 2546-2609) ---[0m
fn create_default_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut agents = std::collections::HashMap::new();

    // Add common agent configurations
    agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend-ui".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    agents.insert(
        "backend".to_string(),
        crate::config::AgentConfig {
            specialization: "node_microservices".to_string(),
            worktree: "agents/backend-agent".to_string(),
            branch: "feature/backend-api".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("backend"),
            claude_md_template: "backend_specialist".to_string(),
        },
    );

    agents.insert(
        "devops".to_string(),
        crate::config::AgentConfig {
            specialization: "aws_kubernetes".to_string(),
            worktree: "agents/devops-agent".to_string(),
            branch: "feature/infrastructure".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("devops"),
            claude_md_template: "devops_specialist".to_string(),
        },
    );

    Ok(CcswarmConfig {
        project: crate::config::ProjectConfig {
            name: "New ccswarm Project".to_string(),
            repository: crate::config::RepositoryConfig {
                url: repo_path.to_string_lossy().to_string(),
                main_branch: "main".to_string(),
            },
            master_claude: crate::config::MasterClaudeConfig {
                role: "technical_lead".to_string(),
                quality_threshold: 0.90,
                think_mode: crate::config::ThinkMode::UltraThink,
                permission_level: "supervised".to_string(),
                claude_config: crate::config::ClaudeConfig::for_master(),
                enable_proactive_mode: true, // デフォルト有効
                proactive_frequency: 30,     // 30秒間隔
                high_frequency: 15,          // 高頻度15秒間隔
            },
        },
        agents,
        coordination: crate::config::CoordinationConfig {
            communication_method: "json_files".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_commit".to_string(),
            master_review_trigger: "all_tasks_complete".to_string(),
        },
    })
}

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_interactive_delegation (lines 2772-2884) ---[0m
    async fn run_interactive_delegation(&self) -> Result<()> {
        use std::io::{self, Write};

        println!("🎯 Welcome to Interactive Delegation Mode");
        println!("   Type 'help' for commands, 'quit' to exit");
        println!();

        loop {
            print!("ccswarm> ");
            io::stdout().flush()?;

            let mut input = String::new();
            io::stdin().read_line(&mut input)?;
            let input = input.trim();

            match input {
                "quit" | "exit" => {
                    println!("👋 Goodbye!");
                    break;
                }
                "help" => {
                    println!("📚 Interactive Delegation Commands:");
                    println!("   analyze <task_description>  - Analyze task and suggest agent");
                    println!("   delegate <agent> <task>     - Delegate task to specific agent");
                    println!("   stats                       - Show delegation statistics");
                    println!("   agents                      - List available agents");
                    println!("   quit                        - Exit interactive mode");
                    println!();
                }
                "stats" => {
                    println!("📊 Delegation Statistics");
                    println!("   Feature not yet implemented");
                    println!();
                }
                "agents" => {
                    println!("🤖 Available Agents:");
                    println!("   • Frontend - React/TypeScript UI development");
                    println!("   • Backend - Node.js/Express API development");
                    println!("   • DevOps - Infrastructure and deployment");
                    println!("   • QA - Testing and quality assurance");
                    println!();
                }
                _ if input.starts_with("analyze ") => {
                    let task_desc = &input[8..];
                    if !task_desc.is_empty() {
                        // Directly call delegation analysis to avoid recursion
                        use crate::orchestrator::master_delegation::{
                            DelegationStrategy, MasterDelegationEngine,
                        };
                        let mut engine = MasterDelegationEngine::new(DelegationStrategy::Hybrid);
                        let task = Task::new(
                            "interactive-analysis".to_string(),
                            task_desc.to_string(),
                            Priority::Medium,
                            TaskType::Development,
                        );

                        match engine.delegate_task(task) {
                            Ok(decision) => {
                                println!("🔍 Task Analysis Results");
                                println!("   Task: {}", task_desc);
                                println!("   Recommended Agent: {}", decision.target_agent.name());
                                println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                                println!("   Reasoning: {}", decision.reasoning);
                                if let Some(duration) = decision.estimated_duration {
                                    println!("   Estimated Duration: {} seconds", duration);
                                }
                            }
                            Err(e) => {
                                println!("❌ Analysis failed: {}", e);
                            }
                        }
                        println!();
                    } else {
                        println!("❌ Please provide a task description");
                        println!("   Example: analyze Create login form with validation");
                        println!();
                    }
                }
                _ if input.starts_with("delegate ") => {
                    let parts: Vec<&str> = input[9..].splitn(2, ' ').collect();
                    if parts.len() == 2 {
                        let agent = parts[0];
                        let task_desc = parts[1];

                        if ["frontend", "backend", "devops", "qa"].contains(&agent) {
                            println!("🎯 Delegating '{}' to {} agent", task_desc, agent);
                            println!("   ✅ Task queued for delegation");
                            println!();
                        } else {
                            println!("❌ Unknown agent: {}", agent);
                            println!("   Available: frontend, backend, devops, qa");
                            println!();
                        }
                    } else {
                        println!("❌ Usage: delegate <agent> <task_description>");
                        println!("   Example: delegate frontend Create responsive navigation bar");
                        println!();
                    }
                }
                "" => {
                    // Empty input, continue
                }
                _ => {
                    println!("❓ Unknown command: {}", input);
                    println!("   Type 'help' for available commands");
                    println!();
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1545-1666 method add_task <-> ./crates/ccswarm/src/cli/mod.rs:3545-3626 method handle_auto_create
  Similarity: 82.68%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:add_task (lines 1545-1666) ---[0m
    async fn add_task(
        &self,
        description: &str,
        priority: &str,
        task_type: &str,
        details: Option<&str>,
        duration: Option<u32>,
    ) -> Result<()> {
        // use crate::cli::progress::{ProgressStyle, ProgressTracker};
        use crate::utils::user_error::CommonErrors;

        // Validate task description
        if description.trim().is_empty() {
            CommonErrors::invalid_task_format()
                .with_details("Task description cannot be empty")
                .with_suggestion("Provide a clear, actionable task description")
                .display();
            return Err(anyhow!("Invalid task description"));
        }

        // Create task (simplified progress display)
        println!(
            "Creating task: {}...",
            description.chars().take(50).collect::<String>()
        );

        let priority = match priority.to_lowercase().as_str() {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };

        let task_type = match task_type.to_lowercase().as_str() {
            "development" | "dev" => TaskType::Development,
            "testing" | "test" => TaskType::Testing,
            "documentation" | "docs" => TaskType::Documentation,
            "infrastructure" | "infra" => TaskType::Infrastructure,
            "coordination" => TaskType::Coordination,
            "review" => TaskType::Review,
            "bugfix" | "bug" => TaskType::Bugfix,
            "feature" => TaskType::Feature,
            _ => TaskType::Development,
        };

        let task_type_clone = task_type;
        let mut task = Task::new(
            uuid::Uuid::new_v4().to_string(),
            description.to_string(),
            priority,
            task_type,
        );

        if let Some(details) = details {
            task = task.with_details(details.to_string());
        }

        if let Some(duration) = duration {
            task = task.with_duration(duration);
        }

        // Add to task queue via execution engine
        let task_id = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.add_task(task.clone()).await
        } else {
            // Fall back to creating task with ID but warn user
            warn!("Execution engine not available, task will not be executed");
            task.id.clone()
        };

        // Complete progress indicator
        // ProgressTracker::complete(progress, true, Some("Task created successfully".to_string())).await;

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Task added",
                    "task_id": task_id,
                    "description": description,
                    "priority": priority,
                }))?
            );
        } else {
            println!();
            println!("{}", "✅ Task created successfully!".bright_green().bold());
            println!();
            println!(
                "   {} {}",
                "Task ID:".bright_cyan(),
                task_id[..8].bright_white()
            );
            println!("   {} {}", "Description:".bright_cyan(), description);
            println!(
                "   {} {}",
                "Priority:".bright_cyan(),
                match priority {
                    Priority::Critical => "🔴 Critical".bright_red(),
                    Priority::High => "🟡 High".bright_yellow(),
                    Priority::Medium => "🟢 Medium".bright_green(),
                    Priority::Low => "🔵 Low".bright_blue(),
                }
            );
            println!(
                "   {} {}",
                "Type:".bright_cyan(),
                format!("{:?}", task_type_clone).bright_white()
            );

            if let Some(duration) = task.estimated_duration {
                println!("   {} {} minutes", "Est. Duration:".bright_cyan(), duration);
            }

            // Show helpful next steps
            show_quick_help("task-created");
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_auto_create (lines 3545-3626) ---[0m
    async fn handle_auto_create(
        &self,
        description: &str,
        template: Option<&str>,
        auto_deploy: bool,
        output: &PathBuf,
    ) -> Result<()> {
        use crate::orchestrator::auto_create::AutoCreateEngine;

        info!("🚀 Starting auto-create for: {}", description);

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "started",
                    "message": "Auto-create process initiated",
                    "description": description,
                    "template": template,
                    "auto_deploy": auto_deploy,
                    "output": output,
                }))?
            );
        } else {
            println!("🚀 ccswarm Auto-Create");
            println!("====================");
            println!("📋 Request: {}", description);
            if let Some(tmpl) = template {
                println!("📄 Template: {}", tmpl);
            }
            println!("📂 Output: {}", output.display());
            println!();
        }

        // Create auto-create engine
        let mut engine = AutoCreateEngine::new();

        // Execute auto-create workflow
        match engine
            .execute_auto_create(description, &self.config, output)
            .await
        {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Application created successfully",
                            "output": output,
                        }))?
                    );
                } else {
                    println!("\n✅ Application created successfully!");
                    println!("📂 Location: {}", output.display());

                    if auto_deploy {
                        println!("\n🚀 Auto-deploying application...");
                        // TODO: Implement auto-deployment
                        println!("   ⚠️ Auto-deployment not yet implemented");
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "error",
                            "message": "Auto-create failed",
                            "error": e.to_string(),
                        }))?
                    );
                } else {
                    println!("\n❌ Auto-create failed: {}", e);
                }
                return Err(e);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2480-2543 method handle_config <-> ./crates/ccswarm/src/cli/mod.rs:2772-2884 method run_interactive_delegation
  Similarity: 94.96%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_config (lines 2480-2543) ---[0m
    async fn handle_config(&self, action: &ConfigAction) -> Result<()> {
        match action {
            ConfigAction::Generate { output, template } => {
                let config = match template.as_str() {
                    "minimal" => create_minimal_config(&self.repo_path)?,
                    "frontend-only" => create_frontend_only_config(&self.repo_path)?,
                    "full-stack" => create_default_config(&self.repo_path)?,
                    _ => create_default_config(&self.repo_path)?,
                };

                config.to_file(output.clone()).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Configuration generated",
                            "file": output,
                            "template": template,
                        }))?
                    );
                } else {
                    println!("✅ Configuration generated: {}", output.display());
                    println!("   Template: {}", template);
                }
            }
            ConfigAction::Validate { file } => match CcswarmConfig::from_file(file.clone()).await {
                Ok(_) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "Configuration is valid",
                                "file": file,
                            }))?
                        );
                    } else {
                        println!("✅ Configuration is valid: {}", file.display());
                    }
                }
                Err(e) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "Configuration is invalid",
                                "file": file,
                                "error": e.to_string(),
                            }))?
                        );
                    } else {
                        println!("❌ Configuration is invalid: {}", file.display());
                        println!("   Error: {}", e);
                    }
                    return Err(e);
                }
            },
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_interactive_delegation (lines 2772-2884) ---[0m
    async fn run_interactive_delegation(&self) -> Result<()> {
        use std::io::{self, Write};

        println!("🎯 Welcome to Interactive Delegation Mode");
        println!("   Type 'help' for commands, 'quit' to exit");
        println!();

        loop {
            print!("ccswarm> ");
            io::stdout().flush()?;

            let mut input = String::new();
            io::stdin().read_line(&mut input)?;
            let input = input.trim();

            match input {
                "quit" | "exit" => {
                    println!("👋 Goodbye!");
                    break;
                }
                "help" => {
                    println!("📚 Interactive Delegation Commands:");
                    println!("   analyze <task_description>  - Analyze task and suggest agent");
                    println!("   delegate <agent> <task>     - Delegate task to specific agent");
                    println!("   stats                       - Show delegation statistics");
                    println!("   agents                      - List available agents");
                    println!("   quit                        - Exit interactive mode");
                    println!();
                }
                "stats" => {
                    println!("📊 Delegation Statistics");
                    println!("   Feature not yet implemented");
                    println!();
                }
                "agents" => {
                    println!("🤖 Available Agents:");
                    println!("   • Frontend - React/TypeScript UI development");
                    println!("   • Backend - Node.js/Express API development");
                    println!("   • DevOps - Infrastructure and deployment");
                    println!("   • QA - Testing and quality assurance");
                    println!();
                }
                _ if input.starts_with("analyze ") => {
                    let task_desc = &input[8..];
                    if !task_desc.is_empty() {
                        // Directly call delegation analysis to avoid recursion
                        use crate::orchestrator::master_delegation::{
                            DelegationStrategy, MasterDelegationEngine,
                        };
                        let mut engine = MasterDelegationEngine::new(DelegationStrategy::Hybrid);
                        let task = Task::new(
                            "interactive-analysis".to_string(),
                            task_desc.to_string(),
                            Priority::Medium,
                            TaskType::Development,
                        );

                        match engine.delegate_task(task) {
                            Ok(decision) => {
                                println!("🔍 Task Analysis Results");
                                println!("   Task: {}", task_desc);
                                println!("   Recommended Agent: {}", decision.target_agent.name());
                                println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                                println!("   Reasoning: {}", decision.reasoning);
                                if let Some(duration) = decision.estimated_duration {
                                    println!("   Estimated Duration: {} seconds", duration);
                                }
                            }
                            Err(e) => {
                                println!("❌ Analysis failed: {}", e);
                            }
                        }
                        println!();
                    } else {
                        println!("❌ Please provide a task description");
                        println!("   Example: analyze Create login form with validation");
                        println!();
                    }
                }
                _ if input.starts_with("delegate ") => {
                    let parts: Vec<&str> = input[9..].splitn(2, ' ').collect();
                    if parts.len() == 2 {
                        let agent = parts[0];
                        let task_desc = parts[1];

                        if ["frontend", "backend", "devops", "qa"].contains(&agent) {
                            println!("🎯 Delegating '{}' to {} agent", task_desc, agent);
                            println!("   ✅ Task queued for delegation");
                            println!();
                        } else {
                            println!("❌ Unknown agent: {}", agent);
                            println!("   Available: frontend, backend, devops, qa");
                            println!();
                        }
                    } else {
                        println!("❌ Usage: delegate <agent> <task_description>");
                        println!("   Example: delegate frontend Create responsive navigation bar");
                        println!();
                    }
                }
                "" => {
                    // Empty input, continue
                }
                _ => {
                    println!("❓ Unknown command: {}", input);
                    println!("   Type 'help' for available commands");
                    println!();
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1993-2078 method show_task_history <-> ./crates/ccswarm/src/cli/mod.rs:5468-5582 method handle_health
  Similarity: 81.57%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_history (lines 1993-2078) ---[0m
    async fn show_task_history(
        &self,
        limit: usize,
        agent_filter: Option<&str>,
        failed_only: bool,
    ) -> Result<()> {
        let history = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.get_execution_history(Some(limit)).await
        } else {
            Vec::new()
        };

        let filtered_history: Vec<_> = history
            .into_iter()
            .filter(|result| {
                if failed_only && result.success {
                    return false;
                }
                if let Some(agent) = agent_filter {
                    if let Some(ref result_agent) = result.agent_used {
                        return result_agent == agent;
                    }
                    return false;
                }
                true
            })
            .collect();

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "history": filtered_history,
                    "limit": limit,
                    "agent_filter": agent_filter,
                    "failed_only": failed_only,
                    "total_count": filtered_history.len()
                }))?
            );
        } else {
            println!("📚 Task History (Last {} tasks)", limit);
            println!("================================");

            if filtered_history.is_empty() {
                println!("No task history available.");
            } else {
                println!("Found {} execution records:", filtered_history.len());
                println!();

                for result in &filtered_history {
                    let status_emoji = if result.success { "✅" } else { "❌" };
                    let orchestration_indicator = if result.orchestration_used {
                        "🎯"
                    } else {
                        "🔄"
                    };

                    println!(
                        "{} {} Task: {} | Duration: {:.2}s",
                        status_emoji,
                        orchestration_indicator,
                        &result.task_id[..8],
                        result.duration.as_secs_f64()
                    );

                    if let Some(agent) = &result.agent_used {
                        println!("   👤 Agent: {}", agent);
                    }

                    if let Some(error) = &result.error {
                        println!("   ❌ Error: {}", error);
                    }
                    println!();
                }
            }

            if let Some(agent) = agent_filter {
                println!("Filter: Agent = {}", agent);
            }
            if failed_only {
                println!("Filter: Failed tasks only");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_health (lines 5468-5582) ---[0m
    async fn handle_health(
        &self,
        check_agents: bool,
        check_sessions: bool,
        resources: bool,
        diagnose: bool,
        detailed: bool,
        format: &str,
    ) -> Result<()> {
        use crate::cli::health::{run_diagnostics, HealthChecker};
        use crate::coordination::StatusTracker;

        // Run diagnostics if requested
        if diagnose {
            return run_diagnostics(&self.repo_path).await;
        }

        // Initialize health checker
        let status_tracker = StatusTracker::new().await?;
        let health_checker = HealthChecker::new(status_tracker);

        // Add monitoring system if available
        // TODO: Get monitoring system from running orchestrator
        // let monitoring = MonitoringSystem::new();
        // let health_checker = health_checker.with_monitoring(monitoring);

        // Perform health checks based on flags
        let report = if check_agents && !check_sessions && !resources {
            // Only check agents
            let checks = health_checker.check_agents_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let total_agents = checks.len();
            let healthy_agents = checks
                .iter()
                .filter(|c| c.status == health::HealthStatus::Healthy)
                .count();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents,
                healthy_agents,
                active_tasks: 0,
                session_count: 0,
            }
        } else if check_sessions && !check_agents && !resources {
            // Only check sessions
            let checks = health_checker.check_sessions_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let session_count = checks.len();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents: 0,
                healthy_agents: 0,
                active_tasks: 0,
                session_count,
            }
        } else {
            // Full health check
            health_checker.check_all().await?
        };

        // Output results based on format
        match format {
            "json" => {
                println!("{}", serde_json::to_string_pretty(&report)?);
            }
            _ => {
                if detailed {
                    report.print_detailed();
                } else {
                    report.print_summary();
                }
            }
        }

        // Exit with non-zero code if unhealthy
        if report.overall_status == health::HealthStatus::Critical {
            std::process::exit(1);
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1835-1933 method show_task_status <-> ./crates/ccswarm/src/cli/mod.rs:3545-3626 method handle_auto_create
  Similarity: 90.49%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_status (lines 1835-1933) ---[0m
    async fn show_task_status(
        &self,
        task_id: &str,
        history: bool,
        orchestration: bool,
    ) -> Result<()> {
        let task = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.get_task(task_id).await
        } else {
            None
        };

        if let Some(task) = task {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "task": task,
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");

                let status_emoji = match &task.status {
                    TaskStatus::Pending => "⏳",
                    TaskStatus::Assigned { .. } => "📋",
                    TaskStatus::InProgress { .. } => "🏃",
                    TaskStatus::Completed { .. } => "✅",
                    TaskStatus::Failed { .. } => "❌",
                    TaskStatus::Cancelled { .. } => "🚫",
                };

                println!("{} Status: {:?}", status_emoji, task.status);
                println!("📝 Description: {}", task.task.description);
                println!("🎯 Priority: {:?}", task.task.priority);
                println!("📋 Type: {:?}", task.task.task_type);
                println!(
                    "⏰ Created: {}",
                    task.created_at.format("%Y-%m-%d %H:%M:%S")
                );
                println!(
                    "🔄 Updated: {}",
                    task.updated_at.format("%Y-%m-%d %H:%M:%S")
                );

                if let Some(agent) = &task.assigned_agent {
                    println!("👤 Assigned to: {}", agent);
                }

                if let Some(details) = &task.task.details {
                    println!("📝 Details: {}", details);
                }

                if history && !task.execution_history.is_empty() {
                    println!();
                    println!("📚 Execution History:");
                    for (i, attempt) in task.execution_history.iter().enumerate() {
                        println!(
                            "  {}. Agent: {} | Started: {}",
                            i + 1,
                            attempt.agent_id,
                            attempt.started_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(completed) = attempt.completed_at {
                            println!("     Completed: {}", completed.format("%Y-%m-%d %H:%M:%S"));
                        }
                        if let Some(error) = &attempt.error {
                            println!("     Error: {}", error);
                        }
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "status": "not_found",
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");
                println!("❌ Task not found");
                println!();
                println!("💡 Use 'ccswarm task list' to see available tasks");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_auto_create (lines 3545-3626) ---[0m
    async fn handle_auto_create(
        &self,
        description: &str,
        template: Option<&str>,
        auto_deploy: bool,
        output: &PathBuf,
    ) -> Result<()> {
        use crate::orchestrator::auto_create::AutoCreateEngine;

        info!("🚀 Starting auto-create for: {}", description);

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "started",
                    "message": "Auto-create process initiated",
                    "description": description,
                    "template": template,
                    "auto_deploy": auto_deploy,
                    "output": output,
                }))?
            );
        } else {
            println!("🚀 ccswarm Auto-Create");
            println!("====================");
            println!("📋 Request: {}", description);
            if let Some(tmpl) = template {
                println!("📄 Template: {}", tmpl);
            }
            println!("📂 Output: {}", output.display());
            println!();
        }

        // Create auto-create engine
        let mut engine = AutoCreateEngine::new();

        // Execute auto-create workflow
        match engine
            .execute_auto_create(description, &self.config, output)
            .await
        {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Application created successfully",
                            "output": output,
                        }))?
                    );
                } else {
                    println!("\n✅ Application created successfully!");
                    println!("📂 Location: {}", output.display());

                    if auto_deploy {
                        println!("\n🚀 Auto-deploying application...");
                        // TODO: Implement auto-deployment
                        println!("   ⚠️ Auto-deployment not yet implemented");
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "error",
                            "message": "Auto-create failed",
                            "error": e.to_string(),
                        }))?
                    );
                } else {
                    println!("\n❌ Auto-create failed: {}", e);
                }
                return Err(e);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1993-2078 method show_task_history <-> ./crates/ccswarm/src/cli/mod.rs:4581-4682 method handle_evolution
  Similarity: 86.83%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_history (lines 1993-2078) ---[0m
    async fn show_task_history(
        &self,
        limit: usize,
        agent_filter: Option<&str>,
        failed_only: bool,
    ) -> Result<()> {
        let history = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.get_execution_history(Some(limit)).await
        } else {
            Vec::new()
        };

        let filtered_history: Vec<_> = history
            .into_iter()
            .filter(|result| {
                if failed_only && result.success {
                    return false;
                }
                if let Some(agent) = agent_filter {
                    if let Some(ref result_agent) = result.agent_used {
                        return result_agent == agent;
                    }
                    return false;
                }
                true
            })
            .collect();

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "history": filtered_history,
                    "limit": limit,
                    "agent_filter": agent_filter,
                    "failed_only": failed_only,
                    "total_count": filtered_history.len()
                }))?
            );
        } else {
            println!("📚 Task History (Last {} tasks)", limit);
            println!("================================");

            if filtered_history.is_empty() {
                println!("No task history available.");
            } else {
                println!("Found {} execution records:", filtered_history.len());
                println!();

                for result in &filtered_history {
                    let status_emoji = if result.success { "✅" } else { "❌" };
                    let orchestration_indicator = if result.orchestration_used {
                        "🎯"
                    } else {
                        "🔄"
                    };

                    println!(
                        "{} {} Task: {} | Duration: {:.2}s",
                        status_emoji,
                        orchestration_indicator,
                        &result.task_id[..8],
                        result.duration.as_secs_f64()
                    );

                    if let Some(agent) = &result.agent_used {
                        println!("   👤 Agent: {}", agent);
                    }

                    if let Some(error) = &result.error {
                        println!("   ❌ Error: {}", error);
                    }
                    println!();
                }
            }

            if let Some(agent) = agent_filter {
                println!("Filter: Agent = {}", agent);
            }
            if failed_only {
                println!("Filter: Failed tasks only");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_evolution (lines 4581-4682) ---[0m
    async fn handle_evolution(&self, action: &EvolutionAction) -> Result<()> {
        match action {
            EvolutionAction::Metrics { agent, all, period } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution metrics",
                            "agent": agent,
                            "all": all,
                            "period_days": period,
                        }))?
                    );
                } else {
                    println!("📈 Evolution Metrics");
                    println!("==================");
                    if *all {
                        println!("Showing metrics for all agents");
                    } else if let Some(a) = agent {
                        println!("Agent: {}", a);
                    }
                    println!("Period: {} days", period);
                    println!("\nNo metrics available");
                }
            }

            EvolutionAction::Patterns {
                pattern_type,
                successful,
                failed,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution patterns",
                            "pattern_type": pattern_type,
                            "filter": {
                                "successful": successful,
                                "failed": failed,
                            },
                        }))?
                    );
                } else {
                    println!("🔮 Evolution Patterns");
                    println!("===================");
                    if *successful {
                        println!("Showing successful patterns");
                    } else if *failed {
                        println!("Showing failed patterns");
                    }
                    println!("No patterns found");
                }
            }

            EvolutionAction::Genealogy { extension, full } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension genealogy",
                            "extension": extension,
                            "full_tree": full,
                        }))?
                    );
                } else {
                    println!("🌳 Extension Genealogy: {}", extension);
                    println!("======================");
                    if *full {
                        println!("Showing full genealogy tree");
                    }
                    println!("No genealogy data available");
                }
            }

            EvolutionAction::Report { format, output } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution report generated",
                            "format": format,
                            "output": output,
                        }))?
                    );
                } else {
                    println!("📊 Generating evolution report");
                    println!("   Format: {}", format);
                    if let Some(o) = output {
                        println!("   Output: {}", o.display());
                    }
                    println!("✅ Report generated");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2480-2543 method handle_config <-> ./crates/ccswarm/src/cli/mod.rs:4581-4682 method handle_evolution
  Similarity: 96.12%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_config (lines 2480-2543) ---[0m
    async fn handle_config(&self, action: &ConfigAction) -> Result<()> {
        match action {
            ConfigAction::Generate { output, template } => {
                let config = match template.as_str() {
                    "minimal" => create_minimal_config(&self.repo_path)?,
                    "frontend-only" => create_frontend_only_config(&self.repo_path)?,
                    "full-stack" => create_default_config(&self.repo_path)?,
                    _ => create_default_config(&self.repo_path)?,
                };

                config.to_file(output.clone()).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Configuration generated",
                            "file": output,
                            "template": template,
                        }))?
                    );
                } else {
                    println!("✅ Configuration generated: {}", output.display());
                    println!("   Template: {}", template);
                }
            }
            ConfigAction::Validate { file } => match CcswarmConfig::from_file(file.clone()).await {
                Ok(_) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "Configuration is valid",
                                "file": file,
                            }))?
                        );
                    } else {
                        println!("✅ Configuration is valid: {}", file.display());
                    }
                }
                Err(e) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "Configuration is invalid",
                                "file": file,
                                "error": e.to_string(),
                            }))?
                        );
                    } else {
                        println!("❌ Configuration is invalid: {}", file.display());
                        println!("   Error: {}", e);
                    }
                    return Err(e);
                }
            },
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_evolution (lines 4581-4682) ---[0m
    async fn handle_evolution(&self, action: &EvolutionAction) -> Result<()> {
        match action {
            EvolutionAction::Metrics { agent, all, period } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution metrics",
                            "agent": agent,
                            "all": all,
                            "period_days": period,
                        }))?
                    );
                } else {
                    println!("📈 Evolution Metrics");
                    println!("==================");
                    if *all {
                        println!("Showing metrics for all agents");
                    } else if let Some(a) = agent {
                        println!("Agent: {}", a);
                    }
                    println!("Period: {} days", period);
                    println!("\nNo metrics available");
                }
            }

            EvolutionAction::Patterns {
                pattern_type,
                successful,
                failed,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution patterns",
                            "pattern_type": pattern_type,
                            "filter": {
                                "successful": successful,
                                "failed": failed,
                            },
                        }))?
                    );
                } else {
                    println!("🔮 Evolution Patterns");
                    println!("===================");
                    if *successful {
                        println!("Showing successful patterns");
                    } else if *failed {
                        println!("Showing failed patterns");
                    }
                    println!("No patterns found");
                }
            }

            EvolutionAction::Genealogy { extension, full } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension genealogy",
                            "extension": extension,
                            "full_tree": full,
                        }))?
                    );
                } else {
                    println!("🌳 Extension Genealogy: {}", extension);
                    println!("======================");
                    if *full {
                        println!("Showing full genealogy tree");
                    }
                    println!("No genealogy data available");
                }
            }

            EvolutionAction::Report { format, output } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution report generated",
                            "format": format,
                            "output": output,
                        }))?
                    );
                } else {
                    println!("📊 Generating evolution report");
                    println!("   Format: {}", format);
                    if let Some(o) = output {
                        println!("   Output: {}", o.display());
                    }
                    println!("✅ Report generated");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1737-1832 method list_tasks <-> ./crates/ccswarm/src/cli/mod.rs:3545-3626 method handle_auto_create
  Similarity: 89.58%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_tasks (lines 1737-1832) ---[0m
    async fn list_tasks(
        &self,
        all: bool,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
        detailed: bool,
    ) -> Result<()> {
        let tasks = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.list_tasks(status_filter, agent_filter).await
        } else {
            Vec::new()
        };

        let displayed_tasks = if all {
            tasks
        } else {
            tasks.into_iter().take(50).collect()
        };

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "tasks": displayed_tasks,
                    "total": displayed_tasks.len(),
                    "filters": {
                        "all": all,
                        "status": status_filter,
                        "agent": agent_filter,
                        "detailed": detailed
                    }
                }))?
            );
        } else {
            println!("📋 Task List");
            println!("============");

            if displayed_tasks.is_empty() {
                println!("No tasks in queue currently.");
                println!();
                println!("💡 Add a task with: ccswarm task add \"Your task description\"");
            } else {
                println!("Found {} tasks:", displayed_tasks.len());
                println!();

                for task in &displayed_tasks {
                    let status_emoji = match &task.status {
                        TaskStatus::Pending => "⏳",
                        TaskStatus::Assigned { .. } => "📋",
                        TaskStatus::InProgress { .. } => "🏃",
                        TaskStatus::Completed { .. } => "✅",
                        TaskStatus::Failed { .. } => "❌",
                        TaskStatus::Cancelled { .. } => "🚫",
                    };

                    let priority_emoji = match task.task.priority {
                        Priority::Critical => "🚨",
                        Priority::High => "🔥",
                        Priority::Medium => "📅",
                        Priority::Low => "💤",
                    };

                    println!(
                        "{} {} {} [{}] {}",
                        status_emoji,
                        priority_emoji,
                        &task.task.id[..8], // Short ID
                        task.task.task_type,
                        task.task.description
                    );

                    if let Some(agent) = &task.assigned_agent {
                        println!("   👤 Assigned to: {}", agent);
                    }

                    if detailed {
                        println!(
                            "   ⏰ Created: {}",
                            task.created_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        println!(
                            "   🔄 Updated: {}",
                            task.updated_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(details) = &task.task.details {
                            println!("   📝 Details: {}", details);
                        }
                    }
                    println!();
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_auto_create (lines 3545-3626) ---[0m
    async fn handle_auto_create(
        &self,
        description: &str,
        template: Option<&str>,
        auto_deploy: bool,
        output: &PathBuf,
    ) -> Result<()> {
        use crate::orchestrator::auto_create::AutoCreateEngine;

        info!("🚀 Starting auto-create for: {}", description);

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "started",
                    "message": "Auto-create process initiated",
                    "description": description,
                    "template": template,
                    "auto_deploy": auto_deploy,
                    "output": output,
                }))?
            );
        } else {
            println!("🚀 ccswarm Auto-Create");
            println!("====================");
            println!("📋 Request: {}", description);
            if let Some(tmpl) = template {
                println!("📄 Template: {}", tmpl);
            }
            println!("📂 Output: {}", output.display());
            println!();
        }

        // Create auto-create engine
        let mut engine = AutoCreateEngine::new();

        // Execute auto-create workflow
        match engine
            .execute_auto_create(description, &self.config, output)
            .await
        {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Application created successfully",
                            "output": output,
                        }))?
                    );
                } else {
                    println!("\n✅ Application created successfully!");
                    println!("📂 Location: {}", output.display());

                    if auto_deploy {
                        println!("\n🚀 Auto-deploying application...");
                        // TODO: Implement auto-deployment
                        println!("   ⚠️ Auto-deployment not yet implemented");
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "error",
                            "message": "Auto-create failed",
                            "error": e.to_string(),
                        }))?
                    );
                } else {
                    println!("\n❌ Auto-create failed: {}", e);
                }
                return Err(e);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2130-2238 method show_task_stats <-> ./crates/ccswarm/src/cli/mod.rs:5468-5582 method handle_health
  Similarity: 71.18%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_stats (lines 2130-2238) ---[0m
    async fn show_task_stats(&self, detailed: bool, performance: bool) -> Result<()> {
        if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "queue_stats": queue_stats,
                        "execution_stats": execution_stats,
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("📊 Task Queue Statistics");
                println!("========================");
                println!("⏳ Pending: {}", queue_stats.pending_count);
                println!("🏃 In Progress: {}", queue_stats.active_count);
                println!("✅ Completed: {}", queue_stats.completed_count);
                println!("❌ Failed: {}", queue_stats.failed_count);
                println!("📋 Total: {}", queue_stats.total_count);

                if performance || detailed {
                    println!();
                    println!("🎯 Execution Statistics");
                    println!("=======================");
                    println!("Tasks Executed: {}", execution_stats.tasks_executed);
                    println!(
                        "Success Rate: {:.1}%",
                        if execution_stats.tasks_executed > 0 {
                            (execution_stats.tasks_succeeded as f64
                                / execution_stats.tasks_executed as f64)
                                * 100.0
                        } else {
                            0.0
                        }
                    );
                    println!(
                        "Average Duration: {:.2}s",
                        execution_stats.average_duration.as_secs_f64()
                    );
                    println!(
                        "Total Duration: {:.2}s",
                        execution_stats.total_duration.as_secs_f64()
                    );
                    println!(
                        "Orchestration Usage: {:.1}%",
                        execution_stats.orchestration_usage
                    );
                }

                if detailed {
                    println!();
                    println!("📈 Queue Health");
                    println!("===============");
                    let failure_rate = if queue_stats.total_count > 0 {
                        (queue_stats.failed_count as f64 / queue_stats.total_count as f64) * 100.0
                    } else {
                        0.0
                    };

                    let health_emoji = if failure_rate < 5.0 {
                        "🟢"
                    } else if failure_rate < 15.0 {
                        "🟡"
                    } else {
                        "🔴"
                    };

                    println!(
                        "{} Overall Health: {:.1}% failure rate",
                        health_emoji, failure_rate
                    );

                    if queue_stats.active_count > 10 {
                        println!(
                            "⚠️  High concurrent load: {} tasks",
                            queue_stats.active_count
                        );
                    }

                    if queue_stats.pending_count > 50 {
                        println!(
                            "⚠️  Queue backlog: {} pending tasks",
                            queue_stats.pending_count
                        );
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "execution_engine_unavailable",
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot display task statistics");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_health (lines 5468-5582) ---[0m
    async fn handle_health(
        &self,
        check_agents: bool,
        check_sessions: bool,
        resources: bool,
        diagnose: bool,
        detailed: bool,
        format: &str,
    ) -> Result<()> {
        use crate::cli::health::{run_diagnostics, HealthChecker};
        use crate::coordination::StatusTracker;

        // Run diagnostics if requested
        if diagnose {
            return run_diagnostics(&self.repo_path).await;
        }

        // Initialize health checker
        let status_tracker = StatusTracker::new().await?;
        let health_checker = HealthChecker::new(status_tracker);

        // Add monitoring system if available
        // TODO: Get monitoring system from running orchestrator
        // let monitoring = MonitoringSystem::new();
        // let health_checker = health_checker.with_monitoring(monitoring);

        // Perform health checks based on flags
        let report = if check_agents && !check_sessions && !resources {
            // Only check agents
            let checks = health_checker.check_agents_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let total_agents = checks.len();
            let healthy_agents = checks
                .iter()
                .filter(|c| c.status == health::HealthStatus::Healthy)
                .count();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents,
                healthy_agents,
                active_tasks: 0,
                session_count: 0,
            }
        } else if check_sessions && !check_agents && !resources {
            // Only check sessions
            let checks = health_checker.check_sessions_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let session_count = checks.len();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents: 0,
                healthy_agents: 0,
                active_tasks: 0,
                session_count,
            }
        } else {
            // Full health check
            health_checker.check_all().await?
        };

        // Output results based on format
        match format {
            "json" => {
                println!("{}", serde_json::to_string_pretty(&report)?);
            }
            _ => {
                if detailed {
                    report.print_detailed();
                } else {
                    report.print_summary();
                }
            }
        }

        // Exit with non-zero code if unhealthy
        if report.overall_status == health::HealthStatus::Critical {
            std::process::exit(1);
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1993-2078 method show_task_history <-> ./crates/ccswarm/src/cli/mod.rs:2130-2238 method show_task_stats
  Similarity: 81.27%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_history (lines 1993-2078) ---[0m
    async fn show_task_history(
        &self,
        limit: usize,
        agent_filter: Option<&str>,
        failed_only: bool,
    ) -> Result<()> {
        let history = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.get_execution_history(Some(limit)).await
        } else {
            Vec::new()
        };

        let filtered_history: Vec<_> = history
            .into_iter()
            .filter(|result| {
                if failed_only && result.success {
                    return false;
                }
                if let Some(agent) = agent_filter {
                    if let Some(ref result_agent) = result.agent_used {
                        return result_agent == agent;
                    }
                    return false;
                }
                true
            })
            .collect();

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "history": filtered_history,
                    "limit": limit,
                    "agent_filter": agent_filter,
                    "failed_only": failed_only,
                    "total_count": filtered_history.len()
                }))?
            );
        } else {
            println!("📚 Task History (Last {} tasks)", limit);
            println!("================================");

            if filtered_history.is_empty() {
                println!("No task history available.");
            } else {
                println!("Found {} execution records:", filtered_history.len());
                println!();

                for result in &filtered_history {
                    let status_emoji = if result.success { "✅" } else { "❌" };
                    let orchestration_indicator = if result.orchestration_used {
                        "🎯"
                    } else {
                        "🔄"
                    };

                    println!(
                        "{} {} Task: {} | Duration: {:.2}s",
                        status_emoji,
                        orchestration_indicator,
                        &result.task_id[..8],
                        result.duration.as_secs_f64()
                    );

                    if let Some(agent) = &result.agent_used {
                        println!("   👤 Agent: {}", agent);
                    }

                    if let Some(error) = &result.error {
                        println!("   ❌ Error: {}", error);
                    }
                    println!();
                }
            }

            if let Some(agent) = agent_filter {
                println!("Filter: Agent = {}", agent);
            }
            if failed_only {
                println!("Filter: Failed tasks only");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_stats (lines 2130-2238) ---[0m
    async fn show_task_stats(&self, detailed: bool, performance: bool) -> Result<()> {
        if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "queue_stats": queue_stats,
                        "execution_stats": execution_stats,
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("📊 Task Queue Statistics");
                println!("========================");
                println!("⏳ Pending: {}", queue_stats.pending_count);
                println!("🏃 In Progress: {}", queue_stats.active_count);
                println!("✅ Completed: {}", queue_stats.completed_count);
                println!("❌ Failed: {}", queue_stats.failed_count);
                println!("📋 Total: {}", queue_stats.total_count);

                if performance || detailed {
                    println!();
                    println!("🎯 Execution Statistics");
                    println!("=======================");
                    println!("Tasks Executed: {}", execution_stats.tasks_executed);
                    println!(
                        "Success Rate: {:.1}%",
                        if execution_stats.tasks_executed > 0 {
                            (execution_stats.tasks_succeeded as f64
                                / execution_stats.tasks_executed as f64)
                                * 100.0
                        } else {
                            0.0
                        }
                    );
                    println!(
                        "Average Duration: {:.2}s",
                        execution_stats.average_duration.as_secs_f64()
                    );
                    println!(
                        "Total Duration: {:.2}s",
                        execution_stats.total_duration.as_secs_f64()
                    );
                    println!(
                        "Orchestration Usage: {:.1}%",
                        execution_stats.orchestration_usage
                    );
                }

                if detailed {
                    println!();
                    println!("📈 Queue Health");
                    println!("===============");
                    let failure_rate = if queue_stats.total_count > 0 {
                        (queue_stats.failed_count as f64 / queue_stats.total_count as f64) * 100.0
                    } else {
                        0.0
                    };

                    let health_emoji = if failure_rate < 5.0 {
                        "🟢"
                    } else if failure_rate < 15.0 {
                        "🟡"
                    } else {
                        "🔴"
                    };

                    println!(
                        "{} Overall Health: {:.1}% failure rate",
                        health_emoji, failure_rate
                    );

                    if queue_stats.active_count > 10 {
                        println!(
                            "⚠️  High concurrent load: {} tasks",
                            queue_stats.active_count
                        );
                    }

                    if queue_stats.pending_count > 50 {
                        println!(
                            "⚠️  Queue backlog: {} pending tasks",
                            queue_stats.pending_count
                        );
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "execution_engine_unavailable",
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot display task statistics");
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1835-1933 method show_task_status <-> ./crates/ccswarm/src/cli/mod.rs:2480-2543 method handle_config
  Similarity: 96.47%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_status (lines 1835-1933) ---[0m
    async fn show_task_status(
        &self,
        task_id: &str,
        history: bool,
        orchestration: bool,
    ) -> Result<()> {
        let task = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.get_task(task_id).await
        } else {
            None
        };

        if let Some(task) = task {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "task": task,
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");

                let status_emoji = match &task.status {
                    TaskStatus::Pending => "⏳",
                    TaskStatus::Assigned { .. } => "📋",
                    TaskStatus::InProgress { .. } => "🏃",
                    TaskStatus::Completed { .. } => "✅",
                    TaskStatus::Failed { .. } => "❌",
                    TaskStatus::Cancelled { .. } => "🚫",
                };

                println!("{} Status: {:?}", status_emoji, task.status);
                println!("📝 Description: {}", task.task.description);
                println!("🎯 Priority: {:?}", task.task.priority);
                println!("📋 Type: {:?}", task.task.task_type);
                println!(
                    "⏰ Created: {}",
                    task.created_at.format("%Y-%m-%d %H:%M:%S")
                );
                println!(
                    "🔄 Updated: {}",
                    task.updated_at.format("%Y-%m-%d %H:%M:%S")
                );

                if let Some(agent) = &task.assigned_agent {
                    println!("👤 Assigned to: {}", agent);
                }

                if let Some(details) = &task.task.details {
                    println!("📝 Details: {}", details);
                }

                if history && !task.execution_history.is_empty() {
                    println!();
                    println!("📚 Execution History:");
                    for (i, attempt) in task.execution_history.iter().enumerate() {
                        println!(
                            "  {}. Agent: {} | Started: {}",
                            i + 1,
                            attempt.agent_id,
                            attempt.started_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(completed) = attempt.completed_at {
                            println!("     Completed: {}", completed.format("%Y-%m-%d %H:%M:%S"));
                        }
                        if let Some(error) = &attempt.error {
                            println!("     Error: {}", error);
                        }
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "status": "not_found",
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");
                println!("❌ Task not found");
                println!();
                println!("💡 Use 'ccswarm task list' to see available tasks");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_config (lines 2480-2543) ---[0m
    async fn handle_config(&self, action: &ConfigAction) -> Result<()> {
        match action {
            ConfigAction::Generate { output, template } => {
                let config = match template.as_str() {
                    "minimal" => create_minimal_config(&self.repo_path)?,
                    "frontend-only" => create_frontend_only_config(&self.repo_path)?,
                    "full-stack" => create_default_config(&self.repo_path)?,
                    _ => create_default_config(&self.repo_path)?,
                };

                config.to_file(output.clone()).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Configuration generated",
                            "file": output,
                            "template": template,
                        }))?
                    );
                } else {
                    println!("✅ Configuration generated: {}", output.display());
                    println!("   Template: {}", template);
                }
            }
            ConfigAction::Validate { file } => match CcswarmConfig::from_file(file.clone()).await {
                Ok(_) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "Configuration is valid",
                                "file": file,
                            }))?
                        );
                    } else {
                        println!("✅ Configuration is valid: {}", file.display());
                    }
                }
                Err(e) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "Configuration is invalid",
                                "file": file,
                                "error": e.to_string(),
                            }))?
                        );
                    } else {
                        println!("❌ Configuration is invalid: {}", file.display());
                        println!("   Error: {}", e);
                    }
                    return Err(e);
                }
            },
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2546-2609 function create_default_config <-> ./crates/ccswarm/src/cli/mod.rs:2637-2770 method handle_delegate
  Similarity: 79.20%

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_default_config (lines 2546-2609) ---[0m
fn create_default_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut agents = std::collections::HashMap::new();

    // Add common agent configurations
    agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend-ui".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    agents.insert(
        "backend".to_string(),
        crate::config::AgentConfig {
            specialization: "node_microservices".to_string(),
            worktree: "agents/backend-agent".to_string(),
            branch: "feature/backend-api".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("backend"),
            claude_md_template: "backend_specialist".to_string(),
        },
    );

    agents.insert(
        "devops".to_string(),
        crate::config::AgentConfig {
            specialization: "aws_kubernetes".to_string(),
            worktree: "agents/devops-agent".to_string(),
            branch: "feature/infrastructure".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("devops"),
            claude_md_template: "devops_specialist".to_string(),
        },
    );

    Ok(CcswarmConfig {
        project: crate::config::ProjectConfig {
            name: "New ccswarm Project".to_string(),
            repository: crate::config::RepositoryConfig {
                url: repo_path.to_string_lossy().to_string(),
                main_branch: "main".to_string(),
            },
            master_claude: crate::config::MasterClaudeConfig {
                role: "technical_lead".to_string(),
                quality_threshold: 0.90,
                think_mode: crate::config::ThinkMode::UltraThink,
                permission_level: "supervised".to_string(),
                claude_config: crate::config::ClaudeConfig::for_master(),
                enable_proactive_mode: true, // デフォルト有効
                proactive_frequency: 30,     // 30秒間隔
                high_frequency: 15,          // 高頻度15秒間隔
            },
        },
        agents,
        coordination: crate::config::CoordinationConfig {
            communication_method: "json_files".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_commit".to_string(),
            master_review_trigger: "all_tasks_complete".to_string(),
        },
    })
}

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_delegate (lines 2637-2770) ---[0m
    async fn handle_delegate(&self, action: &DelegateAction) -> Result<()> {
        use crate::orchestrator::master_delegation::{DelegationStrategy, MasterDelegationEngine};

        match action {
            DelegateAction::Task {
                description,
                agent,
                priority,
                task_type,
                details,
                force,
            } => {
                let task = self.create_task_from_args(
                    description,
                    priority,
                    task_type,
                    details.as_deref(),
                    None,
                )?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Task delegated",
                            "task": task,
                            "target_agent": agent,
                            "forced": force,
                        }))?
                    );
                } else {
                    println!("🎯 Delegating task to {} agent", agent);
                    println!("   Task: {}", task.description);
                    println!("   Priority: {:?}", task.priority);
                    println!("   Type: {:?}", task.task_type);
                    if *force {
                        println!("   ⚠️ Forced delegation");
                    }
                }
            }

            DelegateAction::Analyze {
                description,
                verbose,
                strategy,
            } => {
                let strategy = match strategy.as_str() {
                    "content" => DelegationStrategy::ContentBased,
                    "load" => DelegationStrategy::LoadBalanced,
                    "expertise" => DelegationStrategy::ExpertiseBased,
                    "workflow" => DelegationStrategy::WorkflowBased,
                    "hybrid" => DelegationStrategy::Hybrid,
                    _ => DelegationStrategy::Hybrid,
                };

                let mut engine = MasterDelegationEngine::new(strategy);
                let task = Task::new(
                    "analysis".to_string(),
                    description.clone(),
                    Priority::Medium,
                    TaskType::Development,
                );

                let decision = engine.delegate_task(task)?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&decision)?);
                } else {
                    println!("🔍 Task Analysis Results");
                    println!("   Task: {}", description);
                    println!("   Recommended Agent: {}", decision.target_agent.name());
                    println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                    if *verbose {
                        println!("   Reasoning: {}", decision.reasoning);
                        if let Some(duration) = decision.estimated_duration {
                            println!("   Estimated Duration: {:?}", duration);
                        }
                    }
                }
            }

            DelegateAction::Stats { detailed, period } => {
                // TODO: Implement delegation statistics
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Delegation statistics",
                            "period_hours": period,
                            "detailed": detailed,
                        }))?
                    );
                } else {
                    println!("📊 Delegation Statistics (last {} hours)", period);
                    println!("   Feature not yet implemented");
                }
            }

            DelegateAction::Interactive => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "error": "Interactive mode not available in JSON output",
                        }))?
                    );
                } else {
                    println!("🖥️ Interactive Delegation Mode");
                    self.run_interactive_delegation().await?;
                }
            }

            DelegateAction::Show { file } => {
                let config = CcswarmConfig::from_file(file.clone()).await?;

                if self.json_output {
                    println!("{}", serde_json::to_string_pretty(&config)?);
                } else {
                    println!("📄 Delegation Configuration: {}", file.display());
                    println!("========================");
                    println!("Project: {}", config.project.name);
                    println!("Repository: {}", config.project.repository.url);
                    println!("Agents: {}", config.agents.len());
                    for (name, agent_config) in &config.agents {
                        println!("  - {}: {}", name, agent_config.specialization);
                    }
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2546-2609 function create_default_config <-> ./crates/ccswarm/src/cli/mod.rs:5468-5582 method handle_health
  Similarity: 87.51%

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_default_config (lines 2546-2609) ---[0m
fn create_default_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut agents = std::collections::HashMap::new();

    // Add common agent configurations
    agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend-ui".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    agents.insert(
        "backend".to_string(),
        crate::config::AgentConfig {
            specialization: "node_microservices".to_string(),
            worktree: "agents/backend-agent".to_string(),
            branch: "feature/backend-api".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("backend"),
            claude_md_template: "backend_specialist".to_string(),
        },
    );

    agents.insert(
        "devops".to_string(),
        crate::config::AgentConfig {
            specialization: "aws_kubernetes".to_string(),
            worktree: "agents/devops-agent".to_string(),
            branch: "feature/infrastructure".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("devops"),
            claude_md_template: "devops_specialist".to_string(),
        },
    );

    Ok(CcswarmConfig {
        project: crate::config::ProjectConfig {
            name: "New ccswarm Project".to_string(),
            repository: crate::config::RepositoryConfig {
                url: repo_path.to_string_lossy().to_string(),
                main_branch: "main".to_string(),
            },
            master_claude: crate::config::MasterClaudeConfig {
                role: "technical_lead".to_string(),
                quality_threshold: 0.90,
                think_mode: crate::config::ThinkMode::UltraThink,
                permission_level: "supervised".to_string(),
                claude_config: crate::config::ClaudeConfig::for_master(),
                enable_proactive_mode: true, // デフォルト有効
                proactive_frequency: 30,     // 30秒間隔
                high_frequency: 15,          // 高頻度15秒間隔
            },
        },
        agents,
        coordination: crate::config::CoordinationConfig {
            communication_method: "json_files".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_commit".to_string(),
            master_review_trigger: "all_tasks_complete".to_string(),
        },
    })
}

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_health (lines 5468-5582) ---[0m
    async fn handle_health(
        &self,
        check_agents: bool,
        check_sessions: bool,
        resources: bool,
        diagnose: bool,
        detailed: bool,
        format: &str,
    ) -> Result<()> {
        use crate::cli::health::{run_diagnostics, HealthChecker};
        use crate::coordination::StatusTracker;

        // Run diagnostics if requested
        if diagnose {
            return run_diagnostics(&self.repo_path).await;
        }

        // Initialize health checker
        let status_tracker = StatusTracker::new().await?;
        let health_checker = HealthChecker::new(status_tracker);

        // Add monitoring system if available
        // TODO: Get monitoring system from running orchestrator
        // let monitoring = MonitoringSystem::new();
        // let health_checker = health_checker.with_monitoring(monitoring);

        // Perform health checks based on flags
        let report = if check_agents && !check_sessions && !resources {
            // Only check agents
            let checks = health_checker.check_agents_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let total_agents = checks.len();
            let healthy_agents = checks
                .iter()
                .filter(|c| c.status == health::HealthStatus::Healthy)
                .count();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents,
                healthy_agents,
                active_tasks: 0,
                session_count: 0,
            }
        } else if check_sessions && !check_agents && !resources {
            // Only check sessions
            let checks = health_checker.check_sessions_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let session_count = checks.len();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents: 0,
                healthy_agents: 0,
                active_tasks: 0,
                session_count,
            }
        } else {
            // Full health check
            health_checker.check_all().await?
        };

        // Output results based on format
        match format {
            "json" => {
                println!("{}", serde_json::to_string_pretty(&report)?);
            }
            _ => {
                if detailed {
                    report.print_detailed();
                } else {
                    report.print_summary();
                }
            }
        }

        // Exit with non-zero code if unhealthy
        if report.overall_status == health::HealthStatus::Critical {
            std::process::exit(1);
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2130-2238 method show_task_stats <-> ./crates/ccswarm/src/cli/mod.rs:3545-3626 method handle_auto_create
  Similarity: 81.92%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_stats (lines 2130-2238) ---[0m
    async fn show_task_stats(&self, detailed: bool, performance: bool) -> Result<()> {
        if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "queue_stats": queue_stats,
                        "execution_stats": execution_stats,
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("📊 Task Queue Statistics");
                println!("========================");
                println!("⏳ Pending: {}", queue_stats.pending_count);
                println!("🏃 In Progress: {}", queue_stats.active_count);
                println!("✅ Completed: {}", queue_stats.completed_count);
                println!("❌ Failed: {}", queue_stats.failed_count);
                println!("📋 Total: {}", queue_stats.total_count);

                if performance || detailed {
                    println!();
                    println!("🎯 Execution Statistics");
                    println!("=======================");
                    println!("Tasks Executed: {}", execution_stats.tasks_executed);
                    println!(
                        "Success Rate: {:.1}%",
                        if execution_stats.tasks_executed > 0 {
                            (execution_stats.tasks_succeeded as f64
                                / execution_stats.tasks_executed as f64)
                                * 100.0
                        } else {
                            0.0
                        }
                    );
                    println!(
                        "Average Duration: {:.2}s",
                        execution_stats.average_duration.as_secs_f64()
                    );
                    println!(
                        "Total Duration: {:.2}s",
                        execution_stats.total_duration.as_secs_f64()
                    );
                    println!(
                        "Orchestration Usage: {:.1}%",
                        execution_stats.orchestration_usage
                    );
                }

                if detailed {
                    println!();
                    println!("📈 Queue Health");
                    println!("===============");
                    let failure_rate = if queue_stats.total_count > 0 {
                        (queue_stats.failed_count as f64 / queue_stats.total_count as f64) * 100.0
                    } else {
                        0.0
                    };

                    let health_emoji = if failure_rate < 5.0 {
                        "🟢"
                    } else if failure_rate < 15.0 {
                        "🟡"
                    } else {
                        "🔴"
                    };

                    println!(
                        "{} Overall Health: {:.1}% failure rate",
                        health_emoji, failure_rate
                    );

                    if queue_stats.active_count > 10 {
                        println!(
                            "⚠️  High concurrent load: {} tasks",
                            queue_stats.active_count
                        );
                    }

                    if queue_stats.pending_count > 50 {
                        println!(
                            "⚠️  Queue backlog: {} pending tasks",
                            queue_stats.pending_count
                        );
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "execution_engine_unavailable",
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot display task statistics");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_auto_create (lines 3545-3626) ---[0m
    async fn handle_auto_create(
        &self,
        description: &str,
        template: Option<&str>,
        auto_deploy: bool,
        output: &PathBuf,
    ) -> Result<()> {
        use crate::orchestrator::auto_create::AutoCreateEngine;

        info!("🚀 Starting auto-create for: {}", description);

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "started",
                    "message": "Auto-create process initiated",
                    "description": description,
                    "template": template,
                    "auto_deploy": auto_deploy,
                    "output": output,
                }))?
            );
        } else {
            println!("🚀 ccswarm Auto-Create");
            println!("====================");
            println!("📋 Request: {}", description);
            if let Some(tmpl) = template {
                println!("📄 Template: {}", tmpl);
            }
            println!("📂 Output: {}", output.display());
            println!();
        }

        // Create auto-create engine
        let mut engine = AutoCreateEngine::new();

        // Execute auto-create workflow
        match engine
            .execute_auto_create(description, &self.config, output)
            .await
        {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Application created successfully",
                            "output": output,
                        }))?
                    );
                } else {
                    println!("\n✅ Application created successfully!");
                    println!("📂 Location: {}", output.display());

                    if auto_deploy {
                        println!("\n🚀 Auto-deploying application...");
                        // TODO: Implement auto-deployment
                        println!("   ⚠️ Auto-deployment not yet implemented");
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "error",
                            "message": "Auto-create failed",
                            "error": e.to_string(),
                        }))?
                    );
                } else {
                    println!("\n❌ Auto-create failed: {}", e);
                }
                return Err(e);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1936-1990 method cancel_task <-> ./crates/ccswarm/src/cli/mod.rs:2130-2238 method show_task_stats
  Similarity: 94.95%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:cancel_task (lines 1936-1990) ---[0m
    async fn cancel_task(&self, task_id: &str, force: bool, reason: Option<&str>) -> Result<()> {
        let result = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            match executor
                .cancel_task(task_id, reason.map(|s| s.to_string()))
                .await
            {
                Ok(()) => Ok(()),
                Err(e) => Err(e),
            }
        } else {
            Err(anyhow::anyhow!("Execution engine not available"))
        };

        match result {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": true,
                            "reason": reason,
                            "force": force
                        }))?
                    );
                } else {
                    println!("✅ Task cancelled successfully: {}", task_id);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": false,
                            "reason": e.to_string(),
                            "force": force
                        }))?
                    );
                } else {
                    println!("❌ Failed to cancel task: {}", task_id);
                    println!("   Reason: {}", e);
                    if let Some(r) = reason {
                        println!("   User reason: {}", r);
                    }
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_stats (lines 2130-2238) ---[0m
    async fn show_task_stats(&self, detailed: bool, performance: bool) -> Result<()> {
        if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "queue_stats": queue_stats,
                        "execution_stats": execution_stats,
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("📊 Task Queue Statistics");
                println!("========================");
                println!("⏳ Pending: {}", queue_stats.pending_count);
                println!("🏃 In Progress: {}", queue_stats.active_count);
                println!("✅ Completed: {}", queue_stats.completed_count);
                println!("❌ Failed: {}", queue_stats.failed_count);
                println!("📋 Total: {}", queue_stats.total_count);

                if performance || detailed {
                    println!();
                    println!("🎯 Execution Statistics");
                    println!("=======================");
                    println!("Tasks Executed: {}", execution_stats.tasks_executed);
                    println!(
                        "Success Rate: {:.1}%",
                        if execution_stats.tasks_executed > 0 {
                            (execution_stats.tasks_succeeded as f64
                                / execution_stats.tasks_executed as f64)
                                * 100.0
                        } else {
                            0.0
                        }
                    );
                    println!(
                        "Average Duration: {:.2}s",
                        execution_stats.average_duration.as_secs_f64()
                    );
                    println!(
                        "Total Duration: {:.2}s",
                        execution_stats.total_duration.as_secs_f64()
                    );
                    println!(
                        "Orchestration Usage: {:.1}%",
                        execution_stats.orchestration_usage
                    );
                }

                if detailed {
                    println!();
                    println!("📈 Queue Health");
                    println!("===============");
                    let failure_rate = if queue_stats.total_count > 0 {
                        (queue_stats.failed_count as f64 / queue_stats.total_count as f64) * 100.0
                    } else {
                        0.0
                    };

                    let health_emoji = if failure_rate < 5.0 {
                        "🟢"
                    } else if failure_rate < 15.0 {
                        "🟡"
                    } else {
                        "🔴"
                    };

                    println!(
                        "{} Overall Health: {:.1}% failure rate",
                        health_emoji, failure_rate
                    );

                    if queue_stats.active_count > 10 {
                        println!(
                            "⚠️  High concurrent load: {} tasks",
                            queue_stats.active_count
                        );
                    }

                    if queue_stats.pending_count > 50 {
                        println!(
                            "⚠️  Queue backlog: {} pending tasks",
                            queue_stats.pending_count
                        );
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "execution_engine_unavailable",
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot display task statistics");
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:4581-4682 method handle_evolution <-> ./crates/ccswarm/src/cli/mod.rs:5468-5582 method handle_health
  Similarity: 71.08%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_evolution (lines 4581-4682) ---[0m
    async fn handle_evolution(&self, action: &EvolutionAction) -> Result<()> {
        match action {
            EvolutionAction::Metrics { agent, all, period } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution metrics",
                            "agent": agent,
                            "all": all,
                            "period_days": period,
                        }))?
                    );
                } else {
                    println!("📈 Evolution Metrics");
                    println!("==================");
                    if *all {
                        println!("Showing metrics for all agents");
                    } else if let Some(a) = agent {
                        println!("Agent: {}", a);
                    }
                    println!("Period: {} days", period);
                    println!("\nNo metrics available");
                }
            }

            EvolutionAction::Patterns {
                pattern_type,
                successful,
                failed,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution patterns",
                            "pattern_type": pattern_type,
                            "filter": {
                                "successful": successful,
                                "failed": failed,
                            },
                        }))?
                    );
                } else {
                    println!("🔮 Evolution Patterns");
                    println!("===================");
                    if *successful {
                        println!("Showing successful patterns");
                    } else if *failed {
                        println!("Showing failed patterns");
                    }
                    println!("No patterns found");
                }
            }

            EvolutionAction::Genealogy { extension, full } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension genealogy",
                            "extension": extension,
                            "full_tree": full,
                        }))?
                    );
                } else {
                    println!("🌳 Extension Genealogy: {}", extension);
                    println!("======================");
                    if *full {
                        println!("Showing full genealogy tree");
                    }
                    println!("No genealogy data available");
                }
            }

            EvolutionAction::Report { format, output } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution report generated",
                            "format": format,
                            "output": output,
                        }))?
                    );
                } else {
                    println!("📊 Generating evolution report");
                    println!("   Format: {}", format);
                    if let Some(o) = output {
                        println!("   Output: {}", o.display());
                    }
                    println!("✅ Report generated");
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_health (lines 5468-5582) ---[0m
    async fn handle_health(
        &self,
        check_agents: bool,
        check_sessions: bool,
        resources: bool,
        diagnose: bool,
        detailed: bool,
        format: &str,
    ) -> Result<()> {
        use crate::cli::health::{run_diagnostics, HealthChecker};
        use crate::coordination::StatusTracker;

        // Run diagnostics if requested
        if diagnose {
            return run_diagnostics(&self.repo_path).await;
        }

        // Initialize health checker
        let status_tracker = StatusTracker::new().await?;
        let health_checker = HealthChecker::new(status_tracker);

        // Add monitoring system if available
        // TODO: Get monitoring system from running orchestrator
        // let monitoring = MonitoringSystem::new();
        // let health_checker = health_checker.with_monitoring(monitoring);

        // Perform health checks based on flags
        let report = if check_agents && !check_sessions && !resources {
            // Only check agents
            let checks = health_checker.check_agents_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let total_agents = checks.len();
            let healthy_agents = checks
                .iter()
                .filter(|c| c.status == health::HealthStatus::Healthy)
                .count();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents,
                healthy_agents,
                active_tasks: 0,
                session_count: 0,
            }
        } else if check_sessions && !check_agents && !resources {
            // Only check sessions
            let checks = health_checker.check_sessions_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let session_count = checks.len();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents: 0,
                healthy_agents: 0,
                active_tasks: 0,
                session_count,
            }
        } else {
            // Full health check
            health_checker.check_all().await?
        };

        // Output results based on format
        match format {
            "json" => {
                println!("{}", serde_json::to_string_pretty(&report)?);
            }
            _ => {
                if detailed {
                    report.print_detailed();
                } else {
                    report.print_summary();
                }
            }
        }

        // Exit with non-zero code if unhealthy
        if report.overall_status == health::HealthStatus::Critical {
            std::process::exit(1);
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1835-1933 method show_task_status <-> ./crates/ccswarm/src/cli/mod.rs:1936-1990 method cancel_task
  Similarity: 98.26%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_status (lines 1835-1933) ---[0m
    async fn show_task_status(
        &self,
        task_id: &str,
        history: bool,
        orchestration: bool,
    ) -> Result<()> {
        let task = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.get_task(task_id).await
        } else {
            None
        };

        if let Some(task) = task {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "task": task,
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");

                let status_emoji = match &task.status {
                    TaskStatus::Pending => "⏳",
                    TaskStatus::Assigned { .. } => "📋",
                    TaskStatus::InProgress { .. } => "🏃",
                    TaskStatus::Completed { .. } => "✅",
                    TaskStatus::Failed { .. } => "❌",
                    TaskStatus::Cancelled { .. } => "🚫",
                };

                println!("{} Status: {:?}", status_emoji, task.status);
                println!("📝 Description: {}", task.task.description);
                println!("🎯 Priority: {:?}", task.task.priority);
                println!("📋 Type: {:?}", task.task.task_type);
                println!(
                    "⏰ Created: {}",
                    task.created_at.format("%Y-%m-%d %H:%M:%S")
                );
                println!(
                    "🔄 Updated: {}",
                    task.updated_at.format("%Y-%m-%d %H:%M:%S")
                );

                if let Some(agent) = &task.assigned_agent {
                    println!("👤 Assigned to: {}", agent);
                }

                if let Some(details) = &task.task.details {
                    println!("📝 Details: {}", details);
                }

                if history && !task.execution_history.is_empty() {
                    println!();
                    println!("📚 Execution History:");
                    for (i, attempt) in task.execution_history.iter().enumerate() {
                        println!(
                            "  {}. Agent: {} | Started: {}",
                            i + 1,
                            attempt.agent_id,
                            attempt.started_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(completed) = attempt.completed_at {
                            println!("     Completed: {}", completed.format("%Y-%m-%d %H:%M:%S"));
                        }
                        if let Some(error) = &attempt.error {
                            println!("     Error: {}", error);
                        }
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "status": "not_found",
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");
                println!("❌ Task not found");
                println!();
                println!("💡 Use 'ccswarm task list' to see available tasks");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:cancel_task (lines 1936-1990) ---[0m
    async fn cancel_task(&self, task_id: &str, force: bool, reason: Option<&str>) -> Result<()> {
        let result = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            match executor
                .cancel_task(task_id, reason.map(|s| s.to_string()))
                .await
            {
                Ok(()) => Ok(()),
                Err(e) => Err(e),
            }
        } else {
            Err(anyhow::anyhow!("Execution engine not available"))
        };

        match result {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": true,
                            "reason": reason,
                            "force": force
                        }))?
                    );
                } else {
                    println!("✅ Task cancelled successfully: {}", task_id);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": false,
                            "reason": e.to_string(),
                            "force": force
                        }))?
                    );
                } else {
                    println!("❌ Failed to cancel task: {}", task_id);
                    println!("   Reason: {}", e);
                    if let Some(r) = reason {
                        println!("   User reason: {}", r);
                    }
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1545-1666 method add_task <-> ./crates/ccswarm/src/cli/mod.rs:2546-2609 function create_default_config
  Similarity: 81.04%

[36m--- ./crates/ccswarm/src/cli/mod.rs:add_task (lines 1545-1666) ---[0m
    async fn add_task(
        &self,
        description: &str,
        priority: &str,
        task_type: &str,
        details: Option<&str>,
        duration: Option<u32>,
    ) -> Result<()> {
        // use crate::cli::progress::{ProgressStyle, ProgressTracker};
        use crate::utils::user_error::CommonErrors;

        // Validate task description
        if description.trim().is_empty() {
            CommonErrors::invalid_task_format()
                .with_details("Task description cannot be empty")
                .with_suggestion("Provide a clear, actionable task description")
                .display();
            return Err(anyhow!("Invalid task description"));
        }

        // Create task (simplified progress display)
        println!(
            "Creating task: {}...",
            description.chars().take(50).collect::<String>()
        );

        let priority = match priority.to_lowercase().as_str() {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };

        let task_type = match task_type.to_lowercase().as_str() {
            "development" | "dev" => TaskType::Development,
            "testing" | "test" => TaskType::Testing,
            "documentation" | "docs" => TaskType::Documentation,
            "infrastructure" | "infra" => TaskType::Infrastructure,
            "coordination" => TaskType::Coordination,
            "review" => TaskType::Review,
            "bugfix" | "bug" => TaskType::Bugfix,
            "feature" => TaskType::Feature,
            _ => TaskType::Development,
        };

        let task_type_clone = task_type;
        let mut task = Task::new(
            uuid::Uuid::new_v4().to_string(),
            description.to_string(),
            priority,
            task_type,
        );

        if let Some(details) = details {
            task = task.with_details(details.to_string());
        }

        if let Some(duration) = duration {
            task = task.with_duration(duration);
        }

        // Add to task queue via execution engine
        let task_id = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.add_task(task.clone()).await
        } else {
            // Fall back to creating task with ID but warn user
            warn!("Execution engine not available, task will not be executed");
            task.id.clone()
        };

        // Complete progress indicator
        // ProgressTracker::complete(progress, true, Some("Task created successfully".to_string())).await;

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Task added",
                    "task_id": task_id,
                    "description": description,
                    "priority": priority,
                }))?
            );
        } else {
            println!();
            println!("{}", "✅ Task created successfully!".bright_green().bold());
            println!();
            println!(
                "   {} {}",
                "Task ID:".bright_cyan(),
                task_id[..8].bright_white()
            );
            println!("   {} {}", "Description:".bright_cyan(), description);
            println!(
                "   {} {}",
                "Priority:".bright_cyan(),
                match priority {
                    Priority::Critical => "🔴 Critical".bright_red(),
                    Priority::High => "🟡 High".bright_yellow(),
                    Priority::Medium => "🟢 Medium".bright_green(),
                    Priority::Low => "🔵 Low".bright_blue(),
                }
            );
            println!(
                "   {} {}",
                "Type:".bright_cyan(),
                format!("{:?}", task_type_clone).bright_white()
            );

            if let Some(duration) = task.estimated_duration {
                println!("   {} {} minutes", "Est. Duration:".bright_cyan(), duration);
            }

            // Show helpful next steps
            show_quick_help("task-created");
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_default_config (lines 2546-2609) ---[0m
fn create_default_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut agents = std::collections::HashMap::new();

    // Add common agent configurations
    agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend-ui".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    agents.insert(
        "backend".to_string(),
        crate::config::AgentConfig {
            specialization: "node_microservices".to_string(),
            worktree: "agents/backend-agent".to_string(),
            branch: "feature/backend-api".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("backend"),
            claude_md_template: "backend_specialist".to_string(),
        },
    );

    agents.insert(
        "devops".to_string(),
        crate::config::AgentConfig {
            specialization: "aws_kubernetes".to_string(),
            worktree: "agents/devops-agent".to_string(),
            branch: "feature/infrastructure".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("devops"),
            claude_md_template: "devops_specialist".to_string(),
        },
    );

    Ok(CcswarmConfig {
        project: crate::config::ProjectConfig {
            name: "New ccswarm Project".to_string(),
            repository: crate::config::RepositoryConfig {
                url: repo_path.to_string_lossy().to_string(),
                main_branch: "main".to_string(),
            },
            master_claude: crate::config::MasterClaudeConfig {
                role: "technical_lead".to_string(),
                quality_threshold: 0.90,
                think_mode: crate::config::ThinkMode::UltraThink,
                permission_level: "supervised".to_string(),
                claude_config: crate::config::ClaudeConfig::for_master(),
                enable_proactive_mode: true, // デフォルト有効
                proactive_frequency: 30,     // 30秒間隔
                high_frequency: 15,          // 高頻度15秒間隔
            },
        },
        agents,
        coordination: crate::config::CoordinationConfig {
            communication_method: "json_files".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_commit".to_string(),
            master_review_trigger: "all_tasks_complete".to_string(),
        },
    })
}

  ./crates/ccswarm/src/cli/mod.rs:1669-1734 method handle_task <-> ./crates/ccswarm/src/cli/mod.rs:2130-2238 method show_task_stats
  Similarity: 84.71%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_task (lines 1669-1734) ---[0m
    async fn handle_task(&self, action: &TaskAction) -> Result<()> {
        match action {
            TaskAction::Add {
                description,
                priority,
                task_type,
                details,
                duration,
                auto_assign: _,
                template: _,
                template_vars: _,
                interactive: _,
            } => {
                self.add_task(
                    description,
                    priority,
                    task_type,
                    details.as_deref(),
                    *duration,
                )
                .await
            }
            TaskAction::List {
                all,
                status,
                agent,
                detailed,
            } => {
                self.list_tasks(*all, status.as_deref(), agent.as_deref(), *detailed)
                    .await
            }
            TaskAction::Status {
                task_id,
                history,
                orchestration,
            } => {
                self.show_task_status(task_id, *history, *orchestration)
                    .await
            }
            TaskAction::Cancel {
                task_id,
                force,
                reason,
            } => self.cancel_task(task_id, *force, reason.as_deref()).await,
            TaskAction::History {
                limit,
                agent,
                failed_only,
            } => {
                self.show_task_history(*limit, agent.as_deref(), *failed_only)
                    .await
            }
            TaskAction::Execute {
                task,
                agent,
                orchestrate,
            } => {
                self.execute_task_immediate(task, agent.as_deref(), *orchestrate)
                    .await
            }
            TaskAction::Stats {
                detailed,
                performance,
            } => self.show_task_stats(*detailed, *performance).await,
        }
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_stats (lines 2130-2238) ---[0m
    async fn show_task_stats(&self, detailed: bool, performance: bool) -> Result<()> {
        if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let queue_stats = executor.get_task_queue().get_stats().await;
            let execution_stats = executor.get_stats().await;

            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "queue_stats": queue_stats,
                        "execution_stats": execution_stats,
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("📊 Task Queue Statistics");
                println!("========================");
                println!("⏳ Pending: {}", queue_stats.pending_count);
                println!("🏃 In Progress: {}", queue_stats.active_count);
                println!("✅ Completed: {}", queue_stats.completed_count);
                println!("❌ Failed: {}", queue_stats.failed_count);
                println!("📋 Total: {}", queue_stats.total_count);

                if performance || detailed {
                    println!();
                    println!("🎯 Execution Statistics");
                    println!("=======================");
                    println!("Tasks Executed: {}", execution_stats.tasks_executed);
                    println!(
                        "Success Rate: {:.1}%",
                        if execution_stats.tasks_executed > 0 {
                            (execution_stats.tasks_succeeded as f64
                                / execution_stats.tasks_executed as f64)
                                * 100.0
                        } else {
                            0.0
                        }
                    );
                    println!(
                        "Average Duration: {:.2}s",
                        execution_stats.average_duration.as_secs_f64()
                    );
                    println!(
                        "Total Duration: {:.2}s",
                        execution_stats.total_duration.as_secs_f64()
                    );
                    println!(
                        "Orchestration Usage: {:.1}%",
                        execution_stats.orchestration_usage
                    );
                }

                if detailed {
                    println!();
                    println!("📈 Queue Health");
                    println!("===============");
                    let failure_rate = if queue_stats.total_count > 0 {
                        (queue_stats.failed_count as f64 / queue_stats.total_count as f64) * 100.0
                    } else {
                        0.0
                    };

                    let health_emoji = if failure_rate < 5.0 {
                        "🟢"
                    } else if failure_rate < 15.0 {
                        "🟡"
                    } else {
                        "🔴"
                    };

                    println!(
                        "{} Overall Health: {:.1}% failure rate",
                        health_emoji, failure_rate
                    );

                    if queue_stats.active_count > 10 {
                        println!(
                            "⚠️  High concurrent load: {} tasks",
                            queue_stats.active_count
                        );
                    }

                    if queue_stats.pending_count > 50 {
                        println!(
                            "⚠️  Queue backlog: {} pending tasks",
                            queue_stats.pending_count
                        );
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "error": "execution_engine_unavailable",
                        "detailed": detailed,
                        "performance": performance
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot display task statistics");
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1936-1990 method cancel_task <-> ./crates/ccswarm/src/cli/mod.rs:4581-4682 method handle_evolution
  Similarity: 94.33%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:cancel_task (lines 1936-1990) ---[0m
    async fn cancel_task(&self, task_id: &str, force: bool, reason: Option<&str>) -> Result<()> {
        let result = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            match executor
                .cancel_task(task_id, reason.map(|s| s.to_string()))
                .await
            {
                Ok(()) => Ok(()),
                Err(e) => Err(e),
            }
        } else {
            Err(anyhow::anyhow!("Execution engine not available"))
        };

        match result {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": true,
                            "reason": reason,
                            "force": force
                        }))?
                    );
                } else {
                    println!("✅ Task cancelled successfully: {}", task_id);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": false,
                            "reason": e.to_string(),
                            "force": force
                        }))?
                    );
                } else {
                    println!("❌ Failed to cancel task: {}", task_id);
                    println!("   Reason: {}", e);
                    if let Some(r) = reason {
                        println!("   User reason: {}", r);
                    }
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_evolution (lines 4581-4682) ---[0m
    async fn handle_evolution(&self, action: &EvolutionAction) -> Result<()> {
        match action {
            EvolutionAction::Metrics { agent, all, period } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution metrics",
                            "agent": agent,
                            "all": all,
                            "period_days": period,
                        }))?
                    );
                } else {
                    println!("📈 Evolution Metrics");
                    println!("==================");
                    if *all {
                        println!("Showing metrics for all agents");
                    } else if let Some(a) = agent {
                        println!("Agent: {}", a);
                    }
                    println!("Period: {} days", period);
                    println!("\nNo metrics available");
                }
            }

            EvolutionAction::Patterns {
                pattern_type,
                successful,
                failed,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution patterns",
                            "pattern_type": pattern_type,
                            "filter": {
                                "successful": successful,
                                "failed": failed,
                            },
                        }))?
                    );
                } else {
                    println!("🔮 Evolution Patterns");
                    println!("===================");
                    if *successful {
                        println!("Showing successful patterns");
                    } else if *failed {
                        println!("Showing failed patterns");
                    }
                    println!("No patterns found");
                }
            }

            EvolutionAction::Genealogy { extension, full } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension genealogy",
                            "extension": extension,
                            "full_tree": full,
                        }))?
                    );
                } else {
                    println!("🌳 Extension Genealogy: {}", extension);
                    println!("======================");
                    if *full {
                        println!("Showing full genealogy tree");
                    }
                    println!("No genealogy data available");
                }
            }

            EvolutionAction::Report { format, output } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution report generated",
                            "format": format,
                            "output": output,
                        }))?
                    );
                } else {
                    println!("📊 Generating evolution report");
                    println!("   Format: {}", format);
                    if let Some(o) = output {
                        println!("   Output: {}", o.display());
                    }
                    println!("✅ Report generated");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1993-2078 method show_task_history <-> ./crates/ccswarm/src/cli/mod.rs:3545-3626 method handle_auto_create
  Similarity: 87.97%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_history (lines 1993-2078) ---[0m
    async fn show_task_history(
        &self,
        limit: usize,
        agent_filter: Option<&str>,
        failed_only: bool,
    ) -> Result<()> {
        let history = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.get_execution_history(Some(limit)).await
        } else {
            Vec::new()
        };

        let filtered_history: Vec<_> = history
            .into_iter()
            .filter(|result| {
                if failed_only && result.success {
                    return false;
                }
                if let Some(agent) = agent_filter {
                    if let Some(ref result_agent) = result.agent_used {
                        return result_agent == agent;
                    }
                    return false;
                }
                true
            })
            .collect();

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "history": filtered_history,
                    "limit": limit,
                    "agent_filter": agent_filter,
                    "failed_only": failed_only,
                    "total_count": filtered_history.len()
                }))?
            );
        } else {
            println!("📚 Task History (Last {} tasks)", limit);
            println!("================================");

            if filtered_history.is_empty() {
                println!("No task history available.");
            } else {
                println!("Found {} execution records:", filtered_history.len());
                println!();

                for result in &filtered_history {
                    let status_emoji = if result.success { "✅" } else { "❌" };
                    let orchestration_indicator = if result.orchestration_used {
                        "🎯"
                    } else {
                        "🔄"
                    };

                    println!(
                        "{} {} Task: {} | Duration: {:.2}s",
                        status_emoji,
                        orchestration_indicator,
                        &result.task_id[..8],
                        result.duration.as_secs_f64()
                    );

                    if let Some(agent) = &result.agent_used {
                        println!("   👤 Agent: {}", agent);
                    }

                    if let Some(error) = &result.error {
                        println!("   ❌ Error: {}", error);
                    }
                    println!();
                }
            }

            if let Some(agent) = agent_filter {
                println!("Filter: Agent = {}", agent);
            }
            if failed_only {
                println!("Filter: Failed tasks only");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_auto_create (lines 3545-3626) ---[0m
    async fn handle_auto_create(
        &self,
        description: &str,
        template: Option<&str>,
        auto_deploy: bool,
        output: &PathBuf,
    ) -> Result<()> {
        use crate::orchestrator::auto_create::AutoCreateEngine;

        info!("🚀 Starting auto-create for: {}", description);

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "started",
                    "message": "Auto-create process initiated",
                    "description": description,
                    "template": template,
                    "auto_deploy": auto_deploy,
                    "output": output,
                }))?
            );
        } else {
            println!("🚀 ccswarm Auto-Create");
            println!("====================");
            println!("📋 Request: {}", description);
            if let Some(tmpl) = template {
                println!("📄 Template: {}", tmpl);
            }
            println!("📂 Output: {}", output.display());
            println!();
        }

        // Create auto-create engine
        let mut engine = AutoCreateEngine::new();

        // Execute auto-create workflow
        match engine
            .execute_auto_create(description, &self.config, output)
            .await
        {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Application created successfully",
                            "output": output,
                        }))?
                    );
                } else {
                    println!("\n✅ Application created successfully!");
                    println!("📂 Location: {}", output.display());

                    if auto_deploy {
                        println!("\n🚀 Auto-deploying application...");
                        // TODO: Implement auto-deployment
                        println!("   ⚠️ Auto-deployment not yet implemented");
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "error",
                            "message": "Auto-create failed",
                            "error": e.to_string(),
                        }))?
                    );
                } else {
                    println!("\n❌ Auto-create failed: {}", e);
                }
                return Err(e);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1737-1832 method list_tasks <-> ./crates/ccswarm/src/cli/mod.rs:2546-2609 function create_default_config
  Similarity: 91.57%

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_tasks (lines 1737-1832) ---[0m
    async fn list_tasks(
        &self,
        all: bool,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
        detailed: bool,
    ) -> Result<()> {
        let tasks = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.list_tasks(status_filter, agent_filter).await
        } else {
            Vec::new()
        };

        let displayed_tasks = if all {
            tasks
        } else {
            tasks.into_iter().take(50).collect()
        };

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "tasks": displayed_tasks,
                    "total": displayed_tasks.len(),
                    "filters": {
                        "all": all,
                        "status": status_filter,
                        "agent": agent_filter,
                        "detailed": detailed
                    }
                }))?
            );
        } else {
            println!("📋 Task List");
            println!("============");

            if displayed_tasks.is_empty() {
                println!("No tasks in queue currently.");
                println!();
                println!("💡 Add a task with: ccswarm task add \"Your task description\"");
            } else {
                println!("Found {} tasks:", displayed_tasks.len());
                println!();

                for task in &displayed_tasks {
                    let status_emoji = match &task.status {
                        TaskStatus::Pending => "⏳",
                        TaskStatus::Assigned { .. } => "📋",
                        TaskStatus::InProgress { .. } => "🏃",
                        TaskStatus::Completed { .. } => "✅",
                        TaskStatus::Failed { .. } => "❌",
                        TaskStatus::Cancelled { .. } => "🚫",
                    };

                    let priority_emoji = match task.task.priority {
                        Priority::Critical => "🚨",
                        Priority::High => "🔥",
                        Priority::Medium => "📅",
                        Priority::Low => "💤",
                    };

                    println!(
                        "{} {} {} [{}] {}",
                        status_emoji,
                        priority_emoji,
                        &task.task.id[..8], // Short ID
                        task.task.task_type,
                        task.task.description
                    );

                    if let Some(agent) = &task.assigned_agent {
                        println!("   👤 Assigned to: {}", agent);
                    }

                    if detailed {
                        println!(
                            "   ⏰ Created: {}",
                            task.created_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        println!(
                            "   🔄 Updated: {}",
                            task.updated_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(details) = &task.task.details {
                            println!("   📝 Details: {}", details);
                        }
                    }
                    println!();
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_default_config (lines 2546-2609) ---[0m
fn create_default_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut agents = std::collections::HashMap::new();

    // Add common agent configurations
    agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend-ui".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    agents.insert(
        "backend".to_string(),
        crate::config::AgentConfig {
            specialization: "node_microservices".to_string(),
            worktree: "agents/backend-agent".to_string(),
            branch: "feature/backend-api".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("backend"),
            claude_md_template: "backend_specialist".to_string(),
        },
    );

    agents.insert(
        "devops".to_string(),
        crate::config::AgentConfig {
            specialization: "aws_kubernetes".to_string(),
            worktree: "agents/devops-agent".to_string(),
            branch: "feature/infrastructure".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("devops"),
            claude_md_template: "devops_specialist".to_string(),
        },
    );

    Ok(CcswarmConfig {
        project: crate::config::ProjectConfig {
            name: "New ccswarm Project".to_string(),
            repository: crate::config::RepositoryConfig {
                url: repo_path.to_string_lossy().to_string(),
                main_branch: "main".to_string(),
            },
            master_claude: crate::config::MasterClaudeConfig {
                role: "technical_lead".to_string(),
                quality_threshold: 0.90,
                think_mode: crate::config::ThinkMode::UltraThink,
                permission_level: "supervised".to_string(),
                claude_config: crate::config::ClaudeConfig::for_master(),
                enable_proactive_mode: true, // デフォルト有効
                proactive_frequency: 30,     // 30秒間隔
                high_frequency: 15,          // 高頻度15秒間隔
            },
        },
        agents,
        coordination: crate::config::CoordinationConfig {
            communication_method: "json_files".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_commit".to_string(),
            master_review_trigger: "all_tasks_complete".to_string(),
        },
    })
}

  ./crates/ccswarm/src/cli/mod.rs:3545-3626 method handle_auto_create <-> ./crates/ccswarm/src/cli/mod.rs:4581-4682 method handle_evolution
  Similarity: 78.56%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_auto_create (lines 3545-3626) ---[0m
    async fn handle_auto_create(
        &self,
        description: &str,
        template: Option<&str>,
        auto_deploy: bool,
        output: &PathBuf,
    ) -> Result<()> {
        use crate::orchestrator::auto_create::AutoCreateEngine;

        info!("🚀 Starting auto-create for: {}", description);

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "started",
                    "message": "Auto-create process initiated",
                    "description": description,
                    "template": template,
                    "auto_deploy": auto_deploy,
                    "output": output,
                }))?
            );
        } else {
            println!("🚀 ccswarm Auto-Create");
            println!("====================");
            println!("📋 Request: {}", description);
            if let Some(tmpl) = template {
                println!("📄 Template: {}", tmpl);
            }
            println!("📂 Output: {}", output.display());
            println!();
        }

        // Create auto-create engine
        let mut engine = AutoCreateEngine::new();

        // Execute auto-create workflow
        match engine
            .execute_auto_create(description, &self.config, output)
            .await
        {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Application created successfully",
                            "output": output,
                        }))?
                    );
                } else {
                    println!("\n✅ Application created successfully!");
                    println!("📂 Location: {}", output.display());

                    if auto_deploy {
                        println!("\n🚀 Auto-deploying application...");
                        // TODO: Implement auto-deployment
                        println!("   ⚠️ Auto-deployment not yet implemented");
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "error",
                            "message": "Auto-create failed",
                            "error": e.to_string(),
                        }))?
                    );
                } else {
                    println!("\n❌ Auto-create failed: {}", e);
                }
                return Err(e);
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_evolution (lines 4581-4682) ---[0m
    async fn handle_evolution(&self, action: &EvolutionAction) -> Result<()> {
        match action {
            EvolutionAction::Metrics { agent, all, period } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution metrics",
                            "agent": agent,
                            "all": all,
                            "period_days": period,
                        }))?
                    );
                } else {
                    println!("📈 Evolution Metrics");
                    println!("==================");
                    if *all {
                        println!("Showing metrics for all agents");
                    } else if let Some(a) = agent {
                        println!("Agent: {}", a);
                    }
                    println!("Period: {} days", period);
                    println!("\nNo metrics available");
                }
            }

            EvolutionAction::Patterns {
                pattern_type,
                successful,
                failed,
            } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution patterns",
                            "pattern_type": pattern_type,
                            "filter": {
                                "successful": successful,
                                "failed": failed,
                            },
                        }))?
                    );
                } else {
                    println!("🔮 Evolution Patterns");
                    println!("===================");
                    if *successful {
                        println!("Showing successful patterns");
                    } else if *failed {
                        println!("Showing failed patterns");
                    }
                    println!("No patterns found");
                }
            }

            EvolutionAction::Genealogy { extension, full } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Extension genealogy",
                            "extension": extension,
                            "full_tree": full,
                        }))?
                    );
                } else {
                    println!("🌳 Extension Genealogy: {}", extension);
                    println!("======================");
                    if *full {
                        println!("Showing full genealogy tree");
                    }
                    println!("No genealogy data available");
                }
            }

            EvolutionAction::Report { format, output } => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Evolution report generated",
                            "format": format,
                            "output": output,
                        }))?
                    );
                } else {
                    println!("📊 Generating evolution report");
                    println!("   Format: {}", format);
                    if let Some(o) = output {
                        println!("   Output: {}", o.display());
                    }
                    println!("✅ Report generated");
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1737-1832 method list_tasks <-> ./crates/ccswarm/src/cli/mod.rs:1936-1990 method cancel_task
  Similarity: 95.26%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_tasks (lines 1737-1832) ---[0m
    async fn list_tasks(
        &self,
        all: bool,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
        detailed: bool,
    ) -> Result<()> {
        let tasks = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.list_tasks(status_filter, agent_filter).await
        } else {
            Vec::new()
        };

        let displayed_tasks = if all {
            tasks
        } else {
            tasks.into_iter().take(50).collect()
        };

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "tasks": displayed_tasks,
                    "total": displayed_tasks.len(),
                    "filters": {
                        "all": all,
                        "status": status_filter,
                        "agent": agent_filter,
                        "detailed": detailed
                    }
                }))?
            );
        } else {
            println!("📋 Task List");
            println!("============");

            if displayed_tasks.is_empty() {
                println!("No tasks in queue currently.");
                println!();
                println!("💡 Add a task with: ccswarm task add \"Your task description\"");
            } else {
                println!("Found {} tasks:", displayed_tasks.len());
                println!();

                for task in &displayed_tasks {
                    let status_emoji = match &task.status {
                        TaskStatus::Pending => "⏳",
                        TaskStatus::Assigned { .. } => "📋",
                        TaskStatus::InProgress { .. } => "🏃",
                        TaskStatus::Completed { .. } => "✅",
                        TaskStatus::Failed { .. } => "❌",
                        TaskStatus::Cancelled { .. } => "🚫",
                    };

                    let priority_emoji = match task.task.priority {
                        Priority::Critical => "🚨",
                        Priority::High => "🔥",
                        Priority::Medium => "📅",
                        Priority::Low => "💤",
                    };

                    println!(
                        "{} {} {} [{}] {}",
                        status_emoji,
                        priority_emoji,
                        &task.task.id[..8], // Short ID
                        task.task.task_type,
                        task.task.description
                    );

                    if let Some(agent) = &task.assigned_agent {
                        println!("   👤 Assigned to: {}", agent);
                    }

                    if detailed {
                        println!(
                            "   ⏰ Created: {}",
                            task.created_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        println!(
                            "   🔄 Updated: {}",
                            task.updated_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(details) = &task.task.details {
                            println!("   📝 Details: {}", details);
                        }
                    }
                    println!();
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:cancel_task (lines 1936-1990) ---[0m
    async fn cancel_task(&self, task_id: &str, force: bool, reason: Option<&str>) -> Result<()> {
        let result = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            match executor
                .cancel_task(task_id, reason.map(|s| s.to_string()))
                .await
            {
                Ok(()) => Ok(()),
                Err(e) => Err(e),
            }
        } else {
            Err(anyhow::anyhow!("Execution engine not available"))
        };

        match result {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": true,
                            "reason": reason,
                            "force": force
                        }))?
                    );
                } else {
                    println!("✅ Task cancelled successfully: {}", task_id);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": false,
                            "reason": e.to_string(),
                            "force": force
                        }))?
                    );
                } else {
                    println!("❌ Failed to cancel task: {}", task_id);
                    println!("   Reason: {}", e);
                    if let Some(r) = reason {
                        println!("   User reason: {}", r);
                    }
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1737-1832 method list_tasks <-> ./crates/ccswarm/src/cli/mod.rs:2480-2543 method handle_config
  Similarity: 89.89%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_tasks (lines 1737-1832) ---[0m
    async fn list_tasks(
        &self,
        all: bool,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
        detailed: bool,
    ) -> Result<()> {
        let tasks = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.list_tasks(status_filter, agent_filter).await
        } else {
            Vec::new()
        };

        let displayed_tasks = if all {
            tasks
        } else {
            tasks.into_iter().take(50).collect()
        };

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "tasks": displayed_tasks,
                    "total": displayed_tasks.len(),
                    "filters": {
                        "all": all,
                        "status": status_filter,
                        "agent": agent_filter,
                        "detailed": detailed
                    }
                }))?
            );
        } else {
            println!("📋 Task List");
            println!("============");

            if displayed_tasks.is_empty() {
                println!("No tasks in queue currently.");
                println!();
                println!("💡 Add a task with: ccswarm task add \"Your task description\"");
            } else {
                println!("Found {} tasks:", displayed_tasks.len());
                println!();

                for task in &displayed_tasks {
                    let status_emoji = match &task.status {
                        TaskStatus::Pending => "⏳",
                        TaskStatus::Assigned { .. } => "📋",
                        TaskStatus::InProgress { .. } => "🏃",
                        TaskStatus::Completed { .. } => "✅",
                        TaskStatus::Failed { .. } => "❌",
                        TaskStatus::Cancelled { .. } => "🚫",
                    };

                    let priority_emoji = match task.task.priority {
                        Priority::Critical => "🚨",
                        Priority::High => "🔥",
                        Priority::Medium => "📅",
                        Priority::Low => "💤",
                    };

                    println!(
                        "{} {} {} [{}] {}",
                        status_emoji,
                        priority_emoji,
                        &task.task.id[..8], // Short ID
                        task.task.task_type,
                        task.task.description
                    );

                    if let Some(agent) = &task.assigned_agent {
                        println!("   👤 Assigned to: {}", agent);
                    }

                    if detailed {
                        println!(
                            "   ⏰ Created: {}",
                            task.created_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        println!(
                            "   🔄 Updated: {}",
                            task.updated_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(details) = &task.task.details {
                            println!("   📝 Details: {}", details);
                        }
                    }
                    println!();
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_config (lines 2480-2543) ---[0m
    async fn handle_config(&self, action: &ConfigAction) -> Result<()> {
        match action {
            ConfigAction::Generate { output, template } => {
                let config = match template.as_str() {
                    "minimal" => create_minimal_config(&self.repo_path)?,
                    "frontend-only" => create_frontend_only_config(&self.repo_path)?,
                    "full-stack" => create_default_config(&self.repo_path)?,
                    _ => create_default_config(&self.repo_path)?,
                };

                config.to_file(output.clone()).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Configuration generated",
                            "file": output,
                            "template": template,
                        }))?
                    );
                } else {
                    println!("✅ Configuration generated: {}", output.display());
                    println!("   Template: {}", template);
                }
            }
            ConfigAction::Validate { file } => match CcswarmConfig::from_file(file.clone()).await {
                Ok(_) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "Configuration is valid",
                                "file": file,
                            }))?
                        );
                    } else {
                        println!("✅ Configuration is valid: {}", file.display());
                    }
                }
                Err(e) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "Configuration is invalid",
                                "file": file,
                                "error": e.to_string(),
                            }))?
                        );
                    } else {
                        println!("❌ Configuration is invalid: {}", file.display());
                        println!("   Error: {}", e);
                    }
                    return Err(e);
                }
            },
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1218-1294 method init_project <-> ./crates/ccswarm/src/cli/mod.rs:2772-2884 method run_interactive_delegation
  Similarity: 74.70%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:init_project (lines 1218-1294) ---[0m
    async fn init_project(
        &self,
        name: &str,
        repo_url: Option<&str>,
        agents: &[String],
    ) -> Result<()> {
        use crate::utils::user_error::CommonErrors;

        info!("Initializing ccswarm project: {}", name);

        // Show progress to user
        println!(
            "{}",
            format!("🚀 Initializing ccswarm project: {}", name)
                .bright_cyan()
                .bold()
        );
        println!();

        // Check if git is available
        if !crate::git::shell::ShellWorktreeManager::is_git_available() {
            CommonErrors::git_not_initialized().display();
            return Err(anyhow!("Git is required for ccswarm"));
        }

        // Initialize Git repository if needed
        crate::utils::user_error::show_progress("Setting up git repository...");
        crate::git::shell::ShellWorktreeManager::init_if_needed(&self.repo_path)
            .await
            .inspect_err(|e| {
                eprintln!();
                CommonErrors::git_not_initialized()
                    .with_details(e.to_string())
                    .display();
            })?;
        println!("✅ Git repository ready");

        // Create configuration
        crate::utils::user_error::show_progress("Creating project configuration...");
        let mut config = create_default_config(&self.repo_path)?;
        config.project.name = name.to_string();

        if let Some(url) = repo_url {
            config.project.repository.url = url.to_string();
        }

        // Add requested agents
        for agent_type in agents {
            let agent_config = crate::config::AgentConfig {
                specialization: agent_type.clone(),
                worktree: format!("agents/{}-agent", agent_type),
                branch: format!("feature/{}", agent_type),
                claude_config: crate::config::ClaudeConfig::for_agent(agent_type),
                claude_md_template: format!("{}_specialist", agent_type),
            };
            config.agents.insert(agent_type.clone(), agent_config);
        }

        // Save configuration
        let config_file = self.repo_path.join("ccswarm.json");
        config.to_file(config_file).await?;

        let data = serde_json::json!({
            "project": name,
            "agents": agents,
        });

        println!(
            "{}",
            self.formatter.format_success(
                &format!("ccswarm project '{}' initialized", name),
                Some(data)
            )
        );

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_interactive_delegation (lines 2772-2884) ---[0m
    async fn run_interactive_delegation(&self) -> Result<()> {
        use std::io::{self, Write};

        println!("🎯 Welcome to Interactive Delegation Mode");
        println!("   Type 'help' for commands, 'quit' to exit");
        println!();

        loop {
            print!("ccswarm> ");
            io::stdout().flush()?;

            let mut input = String::new();
            io::stdin().read_line(&mut input)?;
            let input = input.trim();

            match input {
                "quit" | "exit" => {
                    println!("👋 Goodbye!");
                    break;
                }
                "help" => {
                    println!("📚 Interactive Delegation Commands:");
                    println!("   analyze <task_description>  - Analyze task and suggest agent");
                    println!("   delegate <agent> <task>     - Delegate task to specific agent");
                    println!("   stats                       - Show delegation statistics");
                    println!("   agents                      - List available agents");
                    println!("   quit                        - Exit interactive mode");
                    println!();
                }
                "stats" => {
                    println!("📊 Delegation Statistics");
                    println!("   Feature not yet implemented");
                    println!();
                }
                "agents" => {
                    println!("🤖 Available Agents:");
                    println!("   • Frontend - React/TypeScript UI development");
                    println!("   • Backend - Node.js/Express API development");
                    println!("   • DevOps - Infrastructure and deployment");
                    println!("   • QA - Testing and quality assurance");
                    println!();
                }
                _ if input.starts_with("analyze ") => {
                    let task_desc = &input[8..];
                    if !task_desc.is_empty() {
                        // Directly call delegation analysis to avoid recursion
                        use crate::orchestrator::master_delegation::{
                            DelegationStrategy, MasterDelegationEngine,
                        };
                        let mut engine = MasterDelegationEngine::new(DelegationStrategy::Hybrid);
                        let task = Task::new(
                            "interactive-analysis".to_string(),
                            task_desc.to_string(),
                            Priority::Medium,
                            TaskType::Development,
                        );

                        match engine.delegate_task(task) {
                            Ok(decision) => {
                                println!("🔍 Task Analysis Results");
                                println!("   Task: {}", task_desc);
                                println!("   Recommended Agent: {}", decision.target_agent.name());
                                println!("   Confidence: {:.1}%", decision.confidence * 100.0);
                                println!("   Reasoning: {}", decision.reasoning);
                                if let Some(duration) = decision.estimated_duration {
                                    println!("   Estimated Duration: {} seconds", duration);
                                }
                            }
                            Err(e) => {
                                println!("❌ Analysis failed: {}", e);
                            }
                        }
                        println!();
                    } else {
                        println!("❌ Please provide a task description");
                        println!("   Example: analyze Create login form with validation");
                        println!();
                    }
                }
                _ if input.starts_with("delegate ") => {
                    let parts: Vec<&str> = input[9..].splitn(2, ' ').collect();
                    if parts.len() == 2 {
                        let agent = parts[0];
                        let task_desc = parts[1];

                        if ["frontend", "backend", "devops", "qa"].contains(&agent) {
                            println!("🎯 Delegating '{}' to {} agent", task_desc, agent);
                            println!("   ✅ Task queued for delegation");
                            println!();
                        } else {
                            println!("❌ Unknown agent: {}", agent);
                            println!("   Available: frontend, backend, devops, qa");
                            println!();
                        }
                    } else {
                        println!("❌ Usage: delegate <agent> <task_description>");
                        println!("   Example: delegate frontend Create responsive navigation bar");
                        println!();
                    }
                }
                "" => {
                    // Empty input, continue
                }
                _ => {
                    println!("❓ Unknown command: {}", input);
                    println!("   Type 'help' for available commands");
                    println!();
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1936-1990 method cancel_task <-> ./crates/ccswarm/src/cli/mod.rs:5468-5582 method handle_health
  Similarity: 81.85%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:cancel_task (lines 1936-1990) ---[0m
    async fn cancel_task(&self, task_id: &str, force: bool, reason: Option<&str>) -> Result<()> {
        let result = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            match executor
                .cancel_task(task_id, reason.map(|s| s.to_string()))
                .await
            {
                Ok(()) => Ok(()),
                Err(e) => Err(e),
            }
        } else {
            Err(anyhow::anyhow!("Execution engine not available"))
        };

        match result {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": true,
                            "reason": reason,
                            "force": force
                        }))?
                    );
                } else {
                    println!("✅ Task cancelled successfully: {}", task_id);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": false,
                            "reason": e.to_string(),
                            "force": force
                        }))?
                    );
                } else {
                    println!("❌ Failed to cancel task: {}", task_id);
                    println!("   Reason: {}", e);
                    if let Some(r) = reason {
                        println!("   User reason: {}", r);
                    }
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_health (lines 5468-5582) ---[0m
    async fn handle_health(
        &self,
        check_agents: bool,
        check_sessions: bool,
        resources: bool,
        diagnose: bool,
        detailed: bool,
        format: &str,
    ) -> Result<()> {
        use crate::cli::health::{run_diagnostics, HealthChecker};
        use crate::coordination::StatusTracker;

        // Run diagnostics if requested
        if diagnose {
            return run_diagnostics(&self.repo_path).await;
        }

        // Initialize health checker
        let status_tracker = StatusTracker::new().await?;
        let health_checker = HealthChecker::new(status_tracker);

        // Add monitoring system if available
        // TODO: Get monitoring system from running orchestrator
        // let monitoring = MonitoringSystem::new();
        // let health_checker = health_checker.with_monitoring(monitoring);

        // Perform health checks based on flags
        let report = if check_agents && !check_sessions && !resources {
            // Only check agents
            let checks = health_checker.check_agents_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let total_agents = checks.len();
            let healthy_agents = checks
                .iter()
                .filter(|c| c.status == health::HealthStatus::Healthy)
                .count();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents,
                healthy_agents,
                active_tasks: 0,
                session_count: 0,
            }
        } else if check_sessions && !check_agents && !resources {
            // Only check sessions
            let checks = health_checker.check_sessions_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let session_count = checks.len();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents: 0,
                healthy_agents: 0,
                active_tasks: 0,
                session_count,
            }
        } else {
            // Full health check
            health_checker.check_all().await?
        };

        // Output results based on format
        match format {
            "json" => {
                println!("{}", serde_json::to_string_pretty(&report)?);
            }
            _ => {
                if detailed {
                    report.print_detailed();
                } else {
                    report.print_summary();
                }
            }
        }

        // Exit with non-zero code if unhealthy
        if report.overall_status == health::HealthStatus::Critical {
            std::process::exit(1);
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2546-2609 function create_default_config <-> ./crates/ccswarm/src/cli/mod.rs:3545-3626 method handle_auto_create
  Similarity: 92.80%

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_default_config (lines 2546-2609) ---[0m
fn create_default_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut agents = std::collections::HashMap::new();

    // Add common agent configurations
    agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend-ui".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    agents.insert(
        "backend".to_string(),
        crate::config::AgentConfig {
            specialization: "node_microservices".to_string(),
            worktree: "agents/backend-agent".to_string(),
            branch: "feature/backend-api".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("backend"),
            claude_md_template: "backend_specialist".to_string(),
        },
    );

    agents.insert(
        "devops".to_string(),
        crate::config::AgentConfig {
            specialization: "aws_kubernetes".to_string(),
            worktree: "agents/devops-agent".to_string(),
            branch: "feature/infrastructure".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("devops"),
            claude_md_template: "devops_specialist".to_string(),
        },
    );

    Ok(CcswarmConfig {
        project: crate::config::ProjectConfig {
            name: "New ccswarm Project".to_string(),
            repository: crate::config::RepositoryConfig {
                url: repo_path.to_string_lossy().to_string(),
                main_branch: "main".to_string(),
            },
            master_claude: crate::config::MasterClaudeConfig {
                role: "technical_lead".to_string(),
                quality_threshold: 0.90,
                think_mode: crate::config::ThinkMode::UltraThink,
                permission_level: "supervised".to_string(),
                claude_config: crate::config::ClaudeConfig::for_master(),
                enable_proactive_mode: true, // デフォルト有効
                proactive_frequency: 30,     // 30秒間隔
                high_frequency: 15,          // 高頻度15秒間隔
            },
        },
        agents,
        coordination: crate::config::CoordinationConfig {
            communication_method: "json_files".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_commit".to_string(),
            master_review_trigger: "all_tasks_complete".to_string(),
        },
    })
}

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_auto_create (lines 3545-3626) ---[0m
    async fn handle_auto_create(
        &self,
        description: &str,
        template: Option<&str>,
        auto_deploy: bool,
        output: &PathBuf,
    ) -> Result<()> {
        use crate::orchestrator::auto_create::AutoCreateEngine;

        info!("🚀 Starting auto-create for: {}", description);

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "started",
                    "message": "Auto-create process initiated",
                    "description": description,
                    "template": template,
                    "auto_deploy": auto_deploy,
                    "output": output,
                }))?
            );
        } else {
            println!("🚀 ccswarm Auto-Create");
            println!("====================");
            println!("📋 Request: {}", description);
            if let Some(tmpl) = template {
                println!("📄 Template: {}", tmpl);
            }
            println!("📂 Output: {}", output.display());
            println!();
        }

        // Create auto-create engine
        let mut engine = AutoCreateEngine::new();

        // Execute auto-create workflow
        match engine
            .execute_auto_create(description, &self.config, output)
            .await
        {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Application created successfully",
                            "output": output,
                        }))?
                    );
                } else {
                    println!("\n✅ Application created successfully!");
                    println!("📂 Location: {}", output.display());

                    if auto_deploy {
                        println!("\n🚀 Auto-deploying application...");
                        // TODO: Implement auto-deployment
                        println!("   ⚠️ Auto-deployment not yet implemented");
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "error",
                            "message": "Auto-create failed",
                            "error": e.to_string(),
                        }))?
                    );
                } else {
                    println!("\n❌ Auto-create failed: {}", e);
                }
                return Err(e);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1993-2078 method show_task_history <-> ./crates/ccswarm/src/cli/mod.rs:2546-2609 function create_default_config
  Similarity: 89.62%

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_history (lines 1993-2078) ---[0m
    async fn show_task_history(
        &self,
        limit: usize,
        agent_filter: Option<&str>,
        failed_only: bool,
    ) -> Result<()> {
        let history = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.get_execution_history(Some(limit)).await
        } else {
            Vec::new()
        };

        let filtered_history: Vec<_> = history
            .into_iter()
            .filter(|result| {
                if failed_only && result.success {
                    return false;
                }
                if let Some(agent) = agent_filter {
                    if let Some(ref result_agent) = result.agent_used {
                        return result_agent == agent;
                    }
                    return false;
                }
                true
            })
            .collect();

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "history": filtered_history,
                    "limit": limit,
                    "agent_filter": agent_filter,
                    "failed_only": failed_only,
                    "total_count": filtered_history.len()
                }))?
            );
        } else {
            println!("📚 Task History (Last {} tasks)", limit);
            println!("================================");

            if filtered_history.is_empty() {
                println!("No task history available.");
            } else {
                println!("Found {} execution records:", filtered_history.len());
                println!();

                for result in &filtered_history {
                    let status_emoji = if result.success { "✅" } else { "❌" };
                    let orchestration_indicator = if result.orchestration_used {
                        "🎯"
                    } else {
                        "🔄"
                    };

                    println!(
                        "{} {} Task: {} | Duration: {:.2}s",
                        status_emoji,
                        orchestration_indicator,
                        &result.task_id[..8],
                        result.duration.as_secs_f64()
                    );

                    if let Some(agent) = &result.agent_used {
                        println!("   👤 Agent: {}", agent);
                    }

                    if let Some(error) = &result.error {
                        println!("   ❌ Error: {}", error);
                    }
                    println!();
                }
            }

            if let Some(agent) = agent_filter {
                println!("Filter: Agent = {}", agent);
            }
            if failed_only {
                println!("Filter: Failed tasks only");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_default_config (lines 2546-2609) ---[0m
fn create_default_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut agents = std::collections::HashMap::new();

    // Add common agent configurations
    agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend-ui".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    agents.insert(
        "backend".to_string(),
        crate::config::AgentConfig {
            specialization: "node_microservices".to_string(),
            worktree: "agents/backend-agent".to_string(),
            branch: "feature/backend-api".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("backend"),
            claude_md_template: "backend_specialist".to_string(),
        },
    );

    agents.insert(
        "devops".to_string(),
        crate::config::AgentConfig {
            specialization: "aws_kubernetes".to_string(),
            worktree: "agents/devops-agent".to_string(),
            branch: "feature/infrastructure".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("devops"),
            claude_md_template: "devops_specialist".to_string(),
        },
    );

    Ok(CcswarmConfig {
        project: crate::config::ProjectConfig {
            name: "New ccswarm Project".to_string(),
            repository: crate::config::RepositoryConfig {
                url: repo_path.to_string_lossy().to_string(),
                main_branch: "main".to_string(),
            },
            master_claude: crate::config::MasterClaudeConfig {
                role: "technical_lead".to_string(),
                quality_threshold: 0.90,
                think_mode: crate::config::ThinkMode::UltraThink,
                permission_level: "supervised".to_string(),
                claude_config: crate::config::ClaudeConfig::for_master(),
                enable_proactive_mode: true, // デフォルト有効
                proactive_frequency: 30,     // 30秒間隔
                high_frequency: 15,          // 高頻度15秒間隔
            },
        },
        agents,
        coordination: crate::config::CoordinationConfig {
            communication_method: "json_files".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_commit".to_string(),
            master_review_trigger: "all_tasks_complete".to_string(),
        },
    })
}

  ./crates/ccswarm/src/cli/mod.rs:2480-2543 method handle_config <-> ./crates/ccswarm/src/cli/mod.rs:5468-5582 method handle_health
  Similarity: 74.48%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_config (lines 2480-2543) ---[0m
    async fn handle_config(&self, action: &ConfigAction) -> Result<()> {
        match action {
            ConfigAction::Generate { output, template } => {
                let config = match template.as_str() {
                    "minimal" => create_minimal_config(&self.repo_path)?,
                    "frontend-only" => create_frontend_only_config(&self.repo_path)?,
                    "full-stack" => create_default_config(&self.repo_path)?,
                    _ => create_default_config(&self.repo_path)?,
                };

                config.to_file(output.clone()).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Configuration generated",
                            "file": output,
                            "template": template,
                        }))?
                    );
                } else {
                    println!("✅ Configuration generated: {}", output.display());
                    println!("   Template: {}", template);
                }
            }
            ConfigAction::Validate { file } => match CcswarmConfig::from_file(file.clone()).await {
                Ok(_) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "Configuration is valid",
                                "file": file,
                            }))?
                        );
                    } else {
                        println!("✅ Configuration is valid: {}", file.display());
                    }
                }
                Err(e) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "Configuration is invalid",
                                "file": file,
                                "error": e.to_string(),
                            }))?
                        );
                    } else {
                        println!("❌ Configuration is invalid: {}", file.display());
                        println!("   Error: {}", e);
                    }
                    return Err(e);
                }
            },
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_health (lines 5468-5582) ---[0m
    async fn handle_health(
        &self,
        check_agents: bool,
        check_sessions: bool,
        resources: bool,
        diagnose: bool,
        detailed: bool,
        format: &str,
    ) -> Result<()> {
        use crate::cli::health::{run_diagnostics, HealthChecker};
        use crate::coordination::StatusTracker;

        // Run diagnostics if requested
        if diagnose {
            return run_diagnostics(&self.repo_path).await;
        }

        // Initialize health checker
        let status_tracker = StatusTracker::new().await?;
        let health_checker = HealthChecker::new(status_tracker);

        // Add monitoring system if available
        // TODO: Get monitoring system from running orchestrator
        // let monitoring = MonitoringSystem::new();
        // let health_checker = health_checker.with_monitoring(monitoring);

        // Perform health checks based on flags
        let report = if check_agents && !check_sessions && !resources {
            // Only check agents
            let checks = health_checker.check_agents_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let total_agents = checks.len();
            let healthy_agents = checks
                .iter()
                .filter(|c| c.status == health::HealthStatus::Healthy)
                .count();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents,
                healthy_agents,
                active_tasks: 0,
                session_count: 0,
            }
        } else if check_sessions && !check_agents && !resources {
            // Only check sessions
            let checks = health_checker.check_sessions_only().await?;
            let overall_status = if checks.iter().any(|c| {
                matches!(
                    c.status,
                    health::HealthStatus::Down | health::HealthStatus::Critical
                )
            }) {
                health::HealthStatus::Critical
            } else if checks
                .iter()
                .any(|c| c.status == health::HealthStatus::Warning)
            {
                health::HealthStatus::Warning
            } else {
                health::HealthStatus::Healthy
            };

            let session_count = checks.len();
            health::SystemHealthReport {
                timestamp: chrono::Utc::now(),
                overall_status,
                checks,
                total_agents: 0,
                healthy_agents: 0,
                active_tasks: 0,
                session_count,
            }
        } else {
            // Full health check
            health_checker.check_all().await?
        };

        // Output results based on format
        match format {
            "json" => {
                println!("{}", serde_json::to_string_pretty(&report)?);
            }
            _ => {
                if detailed {
                    report.print_detailed();
                } else {
                    report.print_summary();
                }
            }
        }

        // Exit with non-zero code if unhealthy
        if report.overall_status == health::HealthStatus::Critical {
            std::process::exit(1);
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1936-1990 method cancel_task <-> ./crates/ccswarm/src/cli/mod.rs:1993-2078 method show_task_history
  Similarity: 93.79%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:cancel_task (lines 1936-1990) ---[0m
    async fn cancel_task(&self, task_id: &str, force: bool, reason: Option<&str>) -> Result<()> {
        let result = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            match executor
                .cancel_task(task_id, reason.map(|s| s.to_string()))
                .await
            {
                Ok(()) => Ok(()),
                Err(e) => Err(e),
            }
        } else {
            Err(anyhow::anyhow!("Execution engine not available"))
        };

        match result {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": true,
                            "reason": reason,
                            "force": force
                        }))?
                    );
                } else {
                    println!("✅ Task cancelled successfully: {}", task_id);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": false,
                            "reason": e.to_string(),
                            "force": force
                        }))?
                    );
                } else {
                    println!("❌ Failed to cancel task: {}", task_id);
                    println!("   Reason: {}", e);
                    if let Some(r) = reason {
                        println!("   User reason: {}", r);
                    }
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_history (lines 1993-2078) ---[0m
    async fn show_task_history(
        &self,
        limit: usize,
        agent_filter: Option<&str>,
        failed_only: bool,
    ) -> Result<()> {
        let history = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.get_execution_history(Some(limit)).await
        } else {
            Vec::new()
        };

        let filtered_history: Vec<_> = history
            .into_iter()
            .filter(|result| {
                if failed_only && result.success {
                    return false;
                }
                if let Some(agent) = agent_filter {
                    if let Some(ref result_agent) = result.agent_used {
                        return result_agent == agent;
                    }
                    return false;
                }
                true
            })
            .collect();

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "history": filtered_history,
                    "limit": limit,
                    "agent_filter": agent_filter,
                    "failed_only": failed_only,
                    "total_count": filtered_history.len()
                }))?
            );
        } else {
            println!("📚 Task History (Last {} tasks)", limit);
            println!("================================");

            if filtered_history.is_empty() {
                println!("No task history available.");
            } else {
                println!("Found {} execution records:", filtered_history.len());
                println!();

                for result in &filtered_history {
                    let status_emoji = if result.success { "✅" } else { "❌" };
                    let orchestration_indicator = if result.orchestration_used {
                        "🎯"
                    } else {
                        "🔄"
                    };

                    println!(
                        "{} {} Task: {} | Duration: {:.2}s",
                        status_emoji,
                        orchestration_indicator,
                        &result.task_id[..8],
                        result.duration.as_secs_f64()
                    );

                    if let Some(agent) = &result.agent_used {
                        println!("   👤 Agent: {}", agent);
                    }

                    if let Some(error) = &result.error {
                        println!("   ❌ Error: {}", error);
                    }
                    println!();
                }
            }

            if let Some(agent) = agent_filter {
                println!("Filter: Agent = {}", agent);
            }
            if failed_only {
                println!("Filter: Failed tasks only");
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1296-1362 method start_orchestrator <-> ./crates/ccswarm/src/cli/mod.rs:1835-1933 method show_task_status
  Similarity: 78.32%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_orchestrator (lines 1296-1362) ---[0m
    async fn start_orchestrator(
        &self,
        _daemon: bool,
        _port: u16,
        isolation: &str,
        use_real_api: bool,
    ) -> Result<()> {
        info!(
            "Starting ccswarm orchestrator with isolation mode: {} (real_api: {})",
            isolation, use_real_api
        );

        // Parse isolation mode
        let isolation_mode = match isolation {
            "container" => crate::agent::IsolationMode::Container,
            "hybrid" => crate::agent::IsolationMode::Hybrid,
            _ => crate::agent::IsolationMode::GitWorktree,
        };

        // Update configuration to use real API if requested
        let mut config = self.config.clone();
        if use_real_api {
            // Check for API key
            if std::env::var("ANTHROPIC_API_KEY").is_err() {
                return Err(anyhow::anyhow!(
                    "ANTHROPIC_API_KEY environment variable must be set when using --use-real-api"
                ));
            }

            // Update all agent configurations to use real API
            for agent_config in config.agents.values_mut() {
                agent_config.claude_config.use_real_api = true;
            }

            // Update master configuration
            config.project.master_claude.claude_config.use_real_api = true;
        }

        let mut master = MasterClaude::new(config, self.repo_path.clone()).await?;

        // Set isolation mode for all agents
        master.set_isolation_mode(isolation_mode);

        // Initialize agents
        master.initialize().await?;

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Orchestrator started",
                    "master_id": master.id,
                    "agents": master.agents.len(),
                }))?
            );
        } else {
            println!("🚀 ccswarm orchestrator started");
            println!("   Master ID: {}", master.id);
            println!("   Agents: {}", master.agents.len());
        }

        // Start coordination (this would run indefinitely in real usage)
        master.start_coordination().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_status (lines 1835-1933) ---[0m
    async fn show_task_status(
        &self,
        task_id: &str,
        history: bool,
        orchestration: bool,
    ) -> Result<()> {
        let task = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.get_task(task_id).await
        } else {
            None
        };

        if let Some(task) = task {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "task": task,
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");

                let status_emoji = match &task.status {
                    TaskStatus::Pending => "⏳",
                    TaskStatus::Assigned { .. } => "📋",
                    TaskStatus::InProgress { .. } => "🏃",
                    TaskStatus::Completed { .. } => "✅",
                    TaskStatus::Failed { .. } => "❌",
                    TaskStatus::Cancelled { .. } => "🚫",
                };

                println!("{} Status: {:?}", status_emoji, task.status);
                println!("📝 Description: {}", task.task.description);
                println!("🎯 Priority: {:?}", task.task.priority);
                println!("📋 Type: {:?}", task.task.task_type);
                println!(
                    "⏰ Created: {}",
                    task.created_at.format("%Y-%m-%d %H:%M:%S")
                );
                println!(
                    "🔄 Updated: {}",
                    task.updated_at.format("%Y-%m-%d %H:%M:%S")
                );

                if let Some(agent) = &task.assigned_agent {
                    println!("👤 Assigned to: {}", agent);
                }

                if let Some(details) = &task.task.details {
                    println!("📝 Details: {}", details);
                }

                if history && !task.execution_history.is_empty() {
                    println!();
                    println!("📚 Execution History:");
                    for (i, attempt) in task.execution_history.iter().enumerate() {
                        println!(
                            "  {}. Agent: {} | Started: {}",
                            i + 1,
                            attempt.agent_id,
                            attempt.started_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(completed) = attempt.completed_at {
                            println!("     Completed: {}", completed.format("%Y-%m-%d %H:%M:%S"));
                        }
                        if let Some(error) = &attempt.error {
                            println!("     Error: {}", error);
                        }
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "status": "not_found",
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");
                println!("❌ Task not found");
                println!();
                println!("💡 Use 'ccswarm task list' to see available tasks");
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1296-1362 method start_orchestrator <-> ./crates/ccswarm/src/cli/mod.rs:3545-3626 method handle_auto_create
  Similarity: 86.42%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_orchestrator (lines 1296-1362) ---[0m
    async fn start_orchestrator(
        &self,
        _daemon: bool,
        _port: u16,
        isolation: &str,
        use_real_api: bool,
    ) -> Result<()> {
        info!(
            "Starting ccswarm orchestrator with isolation mode: {} (real_api: {})",
            isolation, use_real_api
        );

        // Parse isolation mode
        let isolation_mode = match isolation {
            "container" => crate::agent::IsolationMode::Container,
            "hybrid" => crate::agent::IsolationMode::Hybrid,
            _ => crate::agent::IsolationMode::GitWorktree,
        };

        // Update configuration to use real API if requested
        let mut config = self.config.clone();
        if use_real_api {
            // Check for API key
            if std::env::var("ANTHROPIC_API_KEY").is_err() {
                return Err(anyhow::anyhow!(
                    "ANTHROPIC_API_KEY environment variable must be set when using --use-real-api"
                ));
            }

            // Update all agent configurations to use real API
            for agent_config in config.agents.values_mut() {
                agent_config.claude_config.use_real_api = true;
            }

            // Update master configuration
            config.project.master_claude.claude_config.use_real_api = true;
        }

        let mut master = MasterClaude::new(config, self.repo_path.clone()).await?;

        // Set isolation mode for all agents
        master.set_isolation_mode(isolation_mode);

        // Initialize agents
        master.initialize().await?;

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Orchestrator started",
                    "master_id": master.id,
                    "agents": master.agents.len(),
                }))?
            );
        } else {
            println!("🚀 ccswarm orchestrator started");
            println!("   Master ID: {}", master.id);
            println!("   Agents: {}", master.agents.len());
        }

        // Start coordination (this would run indefinitely in real usage)
        master.start_coordination().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_auto_create (lines 3545-3626) ---[0m
    async fn handle_auto_create(
        &self,
        description: &str,
        template: Option<&str>,
        auto_deploy: bool,
        output: &PathBuf,
    ) -> Result<()> {
        use crate::orchestrator::auto_create::AutoCreateEngine;

        info!("🚀 Starting auto-create for: {}", description);

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "started",
                    "message": "Auto-create process initiated",
                    "description": description,
                    "template": template,
                    "auto_deploy": auto_deploy,
                    "output": output,
                }))?
            );
        } else {
            println!("🚀 ccswarm Auto-Create");
            println!("====================");
            println!("📋 Request: {}", description);
            if let Some(tmpl) = template {
                println!("📄 Template: {}", tmpl);
            }
            println!("📂 Output: {}", output.display());
            println!();
        }

        // Create auto-create engine
        let mut engine = AutoCreateEngine::new();

        // Execute auto-create workflow
        match engine
            .execute_auto_create(description, &self.config, output)
            .await
        {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Application created successfully",
                            "output": output,
                        }))?
                    );
                } else {
                    println!("\n✅ Application created successfully!");
                    println!("📂 Location: {}", output.display());

                    if auto_deploy {
                        println!("\n🚀 Auto-deploying application...");
                        // TODO: Implement auto-deployment
                        println!("   ⚠️ Auto-deployment not yet implemented");
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "error",
                            "message": "Auto-create failed",
                            "error": e.to_string(),
                        }))?
                    );
                } else {
                    println!("\n❌ Auto-create failed: {}", e);
                }
                return Err(e);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1835-1933 method show_task_status <-> ./crates/ccswarm/src/cli/mod.rs:2546-2609 function create_default_config
  Similarity: 78.95%

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_status (lines 1835-1933) ---[0m
    async fn show_task_status(
        &self,
        task_id: &str,
        history: bool,
        orchestration: bool,
    ) -> Result<()> {
        let task = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.get_task(task_id).await
        } else {
            None
        };

        if let Some(task) = task {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "task": task,
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");

                let status_emoji = match &task.status {
                    TaskStatus::Pending => "⏳",
                    TaskStatus::Assigned { .. } => "📋",
                    TaskStatus::InProgress { .. } => "🏃",
                    TaskStatus::Completed { .. } => "✅",
                    TaskStatus::Failed { .. } => "❌",
                    TaskStatus::Cancelled { .. } => "🚫",
                };

                println!("{} Status: {:?}", status_emoji, task.status);
                println!("📝 Description: {}", task.task.description);
                println!("🎯 Priority: {:?}", task.task.priority);
                println!("📋 Type: {:?}", task.task.task_type);
                println!(
                    "⏰ Created: {}",
                    task.created_at.format("%Y-%m-%d %H:%M:%S")
                );
                println!(
                    "🔄 Updated: {}",
                    task.updated_at.format("%Y-%m-%d %H:%M:%S")
                );

                if let Some(agent) = &task.assigned_agent {
                    println!("👤 Assigned to: {}", agent);
                }

                if let Some(details) = &task.task.details {
                    println!("📝 Details: {}", details);
                }

                if history && !task.execution_history.is_empty() {
                    println!();
                    println!("📚 Execution History:");
                    for (i, attempt) in task.execution_history.iter().enumerate() {
                        println!(
                            "  {}. Agent: {} | Started: {}",
                            i + 1,
                            attempt.agent_id,
                            attempt.started_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(completed) = attempt.completed_at {
                            println!("     Completed: {}", completed.format("%Y-%m-%d %H:%M:%S"));
                        }
                        if let Some(error) = &attempt.error {
                            println!("     Error: {}", error);
                        }
                    }
                }
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task_id": task_id,
                        "status": "not_found",
                        "history": history,
                        "orchestration": orchestration
                    }))?
                );
            } else {
                println!("🔍 Task Status: {}", task_id);
                println!("===============");
                println!("❌ Task not found");
                println!();
                println!("💡 Use 'ccswarm task list' to see available tasks");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_default_config (lines 2546-2609) ---[0m
fn create_default_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut agents = std::collections::HashMap::new();

    // Add common agent configurations
    agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend-ui".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    agents.insert(
        "backend".to_string(),
        crate::config::AgentConfig {
            specialization: "node_microservices".to_string(),
            worktree: "agents/backend-agent".to_string(),
            branch: "feature/backend-api".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("backend"),
            claude_md_template: "backend_specialist".to_string(),
        },
    );

    agents.insert(
        "devops".to_string(),
        crate::config::AgentConfig {
            specialization: "aws_kubernetes".to_string(),
            worktree: "agents/devops-agent".to_string(),
            branch: "feature/infrastructure".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("devops"),
            claude_md_template: "devops_specialist".to_string(),
        },
    );

    Ok(CcswarmConfig {
        project: crate::config::ProjectConfig {
            name: "New ccswarm Project".to_string(),
            repository: crate::config::RepositoryConfig {
                url: repo_path.to_string_lossy().to_string(),
                main_branch: "main".to_string(),
            },
            master_claude: crate::config::MasterClaudeConfig {
                role: "technical_lead".to_string(),
                quality_threshold: 0.90,
                think_mode: crate::config::ThinkMode::UltraThink,
                permission_level: "supervised".to_string(),
                claude_config: crate::config::ClaudeConfig::for_master(),
                enable_proactive_mode: true, // デフォルト有効
                proactive_frequency: 30,     // 30秒間隔
                high_frequency: 15,          // 高頻度15秒間隔
            },
        },
        agents,
        coordination: crate::config::CoordinationConfig {
            communication_method: "json_files".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_commit".to_string(),
            master_review_trigger: "all_tasks_complete".to_string(),
        },
    })
}

  ./crates/ccswarm/src/cli/mod.rs:1296-1362 method start_orchestrator <-> ./crates/ccswarm/src/cli/mod.rs:1737-1832 method list_tasks
  Similarity: 78.16%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_orchestrator (lines 1296-1362) ---[0m
    async fn start_orchestrator(
        &self,
        _daemon: bool,
        _port: u16,
        isolation: &str,
        use_real_api: bool,
    ) -> Result<()> {
        info!(
            "Starting ccswarm orchestrator with isolation mode: {} (real_api: {})",
            isolation, use_real_api
        );

        // Parse isolation mode
        let isolation_mode = match isolation {
            "container" => crate::agent::IsolationMode::Container,
            "hybrid" => crate::agent::IsolationMode::Hybrid,
            _ => crate::agent::IsolationMode::GitWorktree,
        };

        // Update configuration to use real API if requested
        let mut config = self.config.clone();
        if use_real_api {
            // Check for API key
            if std::env::var("ANTHROPIC_API_KEY").is_err() {
                return Err(anyhow::anyhow!(
                    "ANTHROPIC_API_KEY environment variable must be set when using --use-real-api"
                ));
            }

            // Update all agent configurations to use real API
            for agent_config in config.agents.values_mut() {
                agent_config.claude_config.use_real_api = true;
            }

            // Update master configuration
            config.project.master_claude.claude_config.use_real_api = true;
        }

        let mut master = MasterClaude::new(config, self.repo_path.clone()).await?;

        // Set isolation mode for all agents
        master.set_isolation_mode(isolation_mode);

        // Initialize agents
        master.initialize().await?;

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Orchestrator started",
                    "master_id": master.id,
                    "agents": master.agents.len(),
                }))?
            );
        } else {
            println!("🚀 ccswarm orchestrator started");
            println!("   Master ID: {}", master.id);
            println!("   Agents: {}", master.agents.len());
        }

        // Start coordination (this would run indefinitely in real usage)
        master.start_coordination().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_tasks (lines 1737-1832) ---[0m
    async fn list_tasks(
        &self,
        all: bool,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
        detailed: bool,
    ) -> Result<()> {
        let tasks = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.list_tasks(status_filter, agent_filter).await
        } else {
            Vec::new()
        };

        let displayed_tasks = if all {
            tasks
        } else {
            tasks.into_iter().take(50).collect()
        };

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "tasks": displayed_tasks,
                    "total": displayed_tasks.len(),
                    "filters": {
                        "all": all,
                        "status": status_filter,
                        "agent": agent_filter,
                        "detailed": detailed
                    }
                }))?
            );
        } else {
            println!("📋 Task List");
            println!("============");

            if displayed_tasks.is_empty() {
                println!("No tasks in queue currently.");
                println!();
                println!("💡 Add a task with: ccswarm task add \"Your task description\"");
            } else {
                println!("Found {} tasks:", displayed_tasks.len());
                println!();

                for task in &displayed_tasks {
                    let status_emoji = match &task.status {
                        TaskStatus::Pending => "⏳",
                        TaskStatus::Assigned { .. } => "📋",
                        TaskStatus::InProgress { .. } => "🏃",
                        TaskStatus::Completed { .. } => "✅",
                        TaskStatus::Failed { .. } => "❌",
                        TaskStatus::Cancelled { .. } => "🚫",
                    };

                    let priority_emoji = match task.task.priority {
                        Priority::Critical => "🚨",
                        Priority::High => "🔥",
                        Priority::Medium => "📅",
                        Priority::Low => "💤",
                    };

                    println!(
                        "{} {} {} [{}] {}",
                        status_emoji,
                        priority_emoji,
                        &task.task.id[..8], // Short ID
                        task.task.task_type,
                        task.task.description
                    );

                    if let Some(agent) = &task.assigned_agent {
                        println!("   👤 Assigned to: {}", agent);
                    }

                    if detailed {
                        println!(
                            "   ⏰ Created: {}",
                            task.created_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        println!(
                            "   🔄 Updated: {}",
                            task.updated_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(details) = &task.task.details {
                            println!("   📝 Details: {}", details);
                        }
                    }
                    println!();
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1993-2078 method show_task_history <-> ./crates/ccswarm/src/cli/mod.rs:2480-2543 method handle_config
  Similarity: 82.99%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_history (lines 1993-2078) ---[0m
    async fn show_task_history(
        &self,
        limit: usize,
        agent_filter: Option<&str>,
        failed_only: bool,
    ) -> Result<()> {
        let history = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.get_execution_history(Some(limit)).await
        } else {
            Vec::new()
        };

        let filtered_history: Vec<_> = history
            .into_iter()
            .filter(|result| {
                if failed_only && result.success {
                    return false;
                }
                if let Some(agent) = agent_filter {
                    if let Some(ref result_agent) = result.agent_used {
                        return result_agent == agent;
                    }
                    return false;
                }
                true
            })
            .collect();

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "history": filtered_history,
                    "limit": limit,
                    "agent_filter": agent_filter,
                    "failed_only": failed_only,
                    "total_count": filtered_history.len()
                }))?
            );
        } else {
            println!("📚 Task History (Last {} tasks)", limit);
            println!("================================");

            if filtered_history.is_empty() {
                println!("No task history available.");
            } else {
                println!("Found {} execution records:", filtered_history.len());
                println!();

                for result in &filtered_history {
                    let status_emoji = if result.success { "✅" } else { "❌" };
                    let orchestration_indicator = if result.orchestration_used {
                        "🎯"
                    } else {
                        "🔄"
                    };

                    println!(
                        "{} {} Task: {} | Duration: {:.2}s",
                        status_emoji,
                        orchestration_indicator,
                        &result.task_id[..8],
                        result.duration.as_secs_f64()
                    );

                    if let Some(agent) = &result.agent_used {
                        println!("   👤 Agent: {}", agent);
                    }

                    if let Some(error) = &result.error {
                        println!("   ❌ Error: {}", error);
                    }
                    println!();
                }
            }

            if let Some(agent) = agent_filter {
                println!("Filter: Agent = {}", agent);
            }
            if failed_only {
                println!("Filter: Failed tasks only");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_config (lines 2480-2543) ---[0m
    async fn handle_config(&self, action: &ConfigAction) -> Result<()> {
        match action {
            ConfigAction::Generate { output, template } => {
                let config = match template.as_str() {
                    "minimal" => create_minimal_config(&self.repo_path)?,
                    "frontend-only" => create_frontend_only_config(&self.repo_path)?,
                    "full-stack" => create_default_config(&self.repo_path)?,
                    _ => create_default_config(&self.repo_path)?,
                };

                config.to_file(output.clone()).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Configuration generated",
                            "file": output,
                            "template": template,
                        }))?
                    );
                } else {
                    println!("✅ Configuration generated: {}", output.display());
                    println!("   Template: {}", template);
                }
            }
            ConfigAction::Validate { file } => match CcswarmConfig::from_file(file.clone()).await {
                Ok(_) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "Configuration is valid",
                                "file": file,
                            }))?
                        );
                    } else {
                        println!("✅ Configuration is valid: {}", file.display());
                    }
                }
                Err(e) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "Configuration is invalid",
                                "file": file,
                                "error": e.to_string(),
                            }))?
                        );
                    } else {
                        println!("❌ Configuration is invalid: {}", file.display());
                        println!("   Error: {}", e);
                    }
                    return Err(e);
                }
            },
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1737-1832 method list_tasks <-> ./crates/ccswarm/src/cli/mod.rs:2886-2929 method create_task_from_args
  Similarity: 85.10%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_tasks (lines 1737-1832) ---[0m
    async fn list_tasks(
        &self,
        all: bool,
        status_filter: Option<&str>,
        agent_filter: Option<&str>,
        detailed: bool,
    ) -> Result<()> {
        let tasks = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            let task_queue = executor.get_task_queue();
            task_queue.list_tasks(status_filter, agent_filter).await
        } else {
            Vec::new()
        };

        let displayed_tasks = if all {
            tasks
        } else {
            tasks.into_iter().take(50).collect()
        };

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "tasks": displayed_tasks,
                    "total": displayed_tasks.len(),
                    "filters": {
                        "all": all,
                        "status": status_filter,
                        "agent": agent_filter,
                        "detailed": detailed
                    }
                }))?
            );
        } else {
            println!("📋 Task List");
            println!("============");

            if displayed_tasks.is_empty() {
                println!("No tasks in queue currently.");
                println!();
                println!("💡 Add a task with: ccswarm task add \"Your task description\"");
            } else {
                println!("Found {} tasks:", displayed_tasks.len());
                println!();

                for task in &displayed_tasks {
                    let status_emoji = match &task.status {
                        TaskStatus::Pending => "⏳",
                        TaskStatus::Assigned { .. } => "📋",
                        TaskStatus::InProgress { .. } => "🏃",
                        TaskStatus::Completed { .. } => "✅",
                        TaskStatus::Failed { .. } => "❌",
                        TaskStatus::Cancelled { .. } => "🚫",
                    };

                    let priority_emoji = match task.task.priority {
                        Priority::Critical => "🚨",
                        Priority::High => "🔥",
                        Priority::Medium => "📅",
                        Priority::Low => "💤",
                    };

                    println!(
                        "{} {} {} [{}] {}",
                        status_emoji,
                        priority_emoji,
                        &task.task.id[..8], // Short ID
                        task.task.task_type,
                        task.task.description
                    );

                    if let Some(agent) = &task.assigned_agent {
                        println!("   👤 Assigned to: {}", agent);
                    }

                    if detailed {
                        println!(
                            "   ⏰ Created: {}",
                            task.created_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        println!(
                            "   🔄 Updated: {}",
                            task.updated_at.format("%Y-%m-%d %H:%M:%S")
                        );
                        if let Some(details) = &task.task.details {
                            println!("   📝 Details: {}", details);
                        }
                    }
                    println!();
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_task_from_args (lines 2886-2929) ---[0m
    fn create_task_from_args(
        &self,
        description: &str,
        priority: &str,
        task_type: &str,
        details: Option<&str>,
        duration: Option<u32>,
    ) -> Result<Task> {
        let priority = match priority.to_lowercase().as_str() {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            _ => Priority::Medium,
        };

        let task_type = match task_type.to_lowercase().as_str() {
            "development" => TaskType::Development,
            "testing" => TaskType::Testing,
            "infrastructure" => TaskType::Infrastructure,
            "documentation" => TaskType::Documentation,
            "bugfix" => TaskType::Bugfix,
            "feature" => TaskType::Feature,
            "review" => TaskType::Review,
            "coordination" => TaskType::Coordination,
            _ => TaskType::Development,
        };

        let estimated_duration = duration.map(|d| std::time::Duration::from_secs(d as u64));

        let mut task = Task::new(
            uuid::Uuid::new_v4().to_string(),
            description.to_string(),
            priority,
            task_type,
        );

        if let Some(details) = details {
            task = task.with_details(details.to_string());
        }

        task.estimated_duration = estimated_duration.map(|d| d.as_secs() as u32);

        Ok(task)
    }

  ./crates/ccswarm/src/cli/mod.rs:2480-2543 method handle_config <-> ./crates/ccswarm/src/cli/mod.rs:3545-3626 method handle_auto_create
  Similarity: 81.04%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_config (lines 2480-2543) ---[0m
    async fn handle_config(&self, action: &ConfigAction) -> Result<()> {
        match action {
            ConfigAction::Generate { output, template } => {
                let config = match template.as_str() {
                    "minimal" => create_minimal_config(&self.repo_path)?,
                    "frontend-only" => create_frontend_only_config(&self.repo_path)?,
                    "full-stack" => create_default_config(&self.repo_path)?,
                    _ => create_default_config(&self.repo_path)?,
                };

                config.to_file(output.clone()).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Configuration generated",
                            "file": output,
                            "template": template,
                        }))?
                    );
                } else {
                    println!("✅ Configuration generated: {}", output.display());
                    println!("   Template: {}", template);
                }
            }
            ConfigAction::Validate { file } => match CcswarmConfig::from_file(file.clone()).await {
                Ok(_) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "Configuration is valid",
                                "file": file,
                            }))?
                        );
                    } else {
                        println!("✅ Configuration is valid: {}", file.display());
                    }
                }
                Err(e) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "Configuration is invalid",
                                "file": file,
                                "error": e.to_string(),
                            }))?
                        );
                    } else {
                        println!("❌ Configuration is invalid: {}", file.display());
                        println!("   Error: {}", e);
                    }
                    return Err(e);
                }
            },
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_auto_create (lines 3545-3626) ---[0m
    async fn handle_auto_create(
        &self,
        description: &str,
        template: Option<&str>,
        auto_deploy: bool,
        output: &PathBuf,
    ) -> Result<()> {
        use crate::orchestrator::auto_create::AutoCreateEngine;

        info!("🚀 Starting auto-create for: {}", description);

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "started",
                    "message": "Auto-create process initiated",
                    "description": description,
                    "template": template,
                    "auto_deploy": auto_deploy,
                    "output": output,
                }))?
            );
        } else {
            println!("🚀 ccswarm Auto-Create");
            println!("====================");
            println!("📋 Request: {}", description);
            if let Some(tmpl) = template {
                println!("📄 Template: {}", tmpl);
            }
            println!("📂 Output: {}", output.display());
            println!();
        }

        // Create auto-create engine
        let mut engine = AutoCreateEngine::new();

        // Execute auto-create workflow
        match engine
            .execute_auto_create(description, &self.config, output)
            .await
        {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Application created successfully",
                            "output": output,
                        }))?
                    );
                } else {
                    println!("\n✅ Application created successfully!");
                    println!("📂 Location: {}", output.display());

                    if auto_deploy {
                        println!("\n🚀 Auto-deploying application...");
                        // TODO: Implement auto-deployment
                        println!("   ⚠️ Auto-deployment not yet implemented");
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "error",
                            "message": "Auto-create failed",
                            "error": e.to_string(),
                        }))?
                    );
                } else {
                    println!("\n❌ Auto-create failed: {}", e);
                }
                return Err(e);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1218-1294 method init_project <-> ./crates/ccswarm/src/cli/mod.rs:1296-1362 method start_orchestrator
  Similarity: 81.71%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:init_project (lines 1218-1294) ---[0m
    async fn init_project(
        &self,
        name: &str,
        repo_url: Option<&str>,
        agents: &[String],
    ) -> Result<()> {
        use crate::utils::user_error::CommonErrors;

        info!("Initializing ccswarm project: {}", name);

        // Show progress to user
        println!(
            "{}",
            format!("🚀 Initializing ccswarm project: {}", name)
                .bright_cyan()
                .bold()
        );
        println!();

        // Check if git is available
        if !crate::git::shell::ShellWorktreeManager::is_git_available() {
            CommonErrors::git_not_initialized().display();
            return Err(anyhow!("Git is required for ccswarm"));
        }

        // Initialize Git repository if needed
        crate::utils::user_error::show_progress("Setting up git repository...");
        crate::git::shell::ShellWorktreeManager::init_if_needed(&self.repo_path)
            .await
            .inspect_err(|e| {
                eprintln!();
                CommonErrors::git_not_initialized()
                    .with_details(e.to_string())
                    .display();
            })?;
        println!("✅ Git repository ready");

        // Create configuration
        crate::utils::user_error::show_progress("Creating project configuration...");
        let mut config = create_default_config(&self.repo_path)?;
        config.project.name = name.to_string();

        if let Some(url) = repo_url {
            config.project.repository.url = url.to_string();
        }

        // Add requested agents
        for agent_type in agents {
            let agent_config = crate::config::AgentConfig {
                specialization: agent_type.clone(),
                worktree: format!("agents/{}-agent", agent_type),
                branch: format!("feature/{}", agent_type),
                claude_config: crate::config::ClaudeConfig::for_agent(agent_type),
                claude_md_template: format!("{}_specialist", agent_type),
            };
            config.agents.insert(agent_type.clone(), agent_config);
        }

        // Save configuration
        let config_file = self.repo_path.join("ccswarm.json");
        config.to_file(config_file).await?;

        let data = serde_json::json!({
            "project": name,
            "agents": agents,
        });

        println!(
            "{}",
            self.formatter.format_success(
                &format!("ccswarm project '{}' initialized", name),
                Some(data)
            )
        );

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_orchestrator (lines 1296-1362) ---[0m
    async fn start_orchestrator(
        &self,
        _daemon: bool,
        _port: u16,
        isolation: &str,
        use_real_api: bool,
    ) -> Result<()> {
        info!(
            "Starting ccswarm orchestrator with isolation mode: {} (real_api: {})",
            isolation, use_real_api
        );

        // Parse isolation mode
        let isolation_mode = match isolation {
            "container" => crate::agent::IsolationMode::Container,
            "hybrid" => crate::agent::IsolationMode::Hybrid,
            _ => crate::agent::IsolationMode::GitWorktree,
        };

        // Update configuration to use real API if requested
        let mut config = self.config.clone();
        if use_real_api {
            // Check for API key
            if std::env::var("ANTHROPIC_API_KEY").is_err() {
                return Err(anyhow::anyhow!(
                    "ANTHROPIC_API_KEY environment variable must be set when using --use-real-api"
                ));
            }

            // Update all agent configurations to use real API
            for agent_config in config.agents.values_mut() {
                agent_config.claude_config.use_real_api = true;
            }

            // Update master configuration
            config.project.master_claude.claude_config.use_real_api = true;
        }

        let mut master = MasterClaude::new(config, self.repo_path.clone()).await?;

        // Set isolation mode for all agents
        master.set_isolation_mode(isolation_mode);

        // Initialize agents
        master.initialize().await?;

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Orchestrator started",
                    "master_id": master.id,
                    "agents": master.agents.len(),
                }))?
            );
        } else {
            println!("🚀 ccswarm orchestrator started");
            println!("   Master ID: {}", master.id);
            println!("   Agents: {}", master.agents.len());
        }

        // Start coordination (this would run indefinitely in real usage)
        master.start_coordination().await?;

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1296-1362 method start_orchestrator <-> ./crates/ccswarm/src/cli/mod.rs:1993-2078 method show_task_history
  Similarity: 76.88%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_orchestrator (lines 1296-1362) ---[0m
    async fn start_orchestrator(
        &self,
        _daemon: bool,
        _port: u16,
        isolation: &str,
        use_real_api: bool,
    ) -> Result<()> {
        info!(
            "Starting ccswarm orchestrator with isolation mode: {} (real_api: {})",
            isolation, use_real_api
        );

        // Parse isolation mode
        let isolation_mode = match isolation {
            "container" => crate::agent::IsolationMode::Container,
            "hybrid" => crate::agent::IsolationMode::Hybrid,
            _ => crate::agent::IsolationMode::GitWorktree,
        };

        // Update configuration to use real API if requested
        let mut config = self.config.clone();
        if use_real_api {
            // Check for API key
            if std::env::var("ANTHROPIC_API_KEY").is_err() {
                return Err(anyhow::anyhow!(
                    "ANTHROPIC_API_KEY environment variable must be set when using --use-real-api"
                ));
            }

            // Update all agent configurations to use real API
            for agent_config in config.agents.values_mut() {
                agent_config.claude_config.use_real_api = true;
            }

            // Update master configuration
            config.project.master_claude.claude_config.use_real_api = true;
        }

        let mut master = MasterClaude::new(config, self.repo_path.clone()).await?;

        // Set isolation mode for all agents
        master.set_isolation_mode(isolation_mode);

        // Initialize agents
        master.initialize().await?;

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Orchestrator started",
                    "master_id": master.id,
                    "agents": master.agents.len(),
                }))?
            );
        } else {
            println!("🚀 ccswarm orchestrator started");
            println!("   Master ID: {}", master.id);
            println!("   Agents: {}", master.agents.len());
        }

        // Start coordination (this would run indefinitely in real usage)
        master.start_coordination().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_history (lines 1993-2078) ---[0m
    async fn show_task_history(
        &self,
        limit: usize,
        agent_filter: Option<&str>,
        failed_only: bool,
    ) -> Result<()> {
        let history = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.get_execution_history(Some(limit)).await
        } else {
            Vec::new()
        };

        let filtered_history: Vec<_> = history
            .into_iter()
            .filter(|result| {
                if failed_only && result.success {
                    return false;
                }
                if let Some(agent) = agent_filter {
                    if let Some(ref result_agent) = result.agent_used {
                        return result_agent == agent;
                    }
                    return false;
                }
                true
            })
            .collect();

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "history": filtered_history,
                    "limit": limit,
                    "agent_filter": agent_filter,
                    "failed_only": failed_only,
                    "total_count": filtered_history.len()
                }))?
            );
        } else {
            println!("📚 Task History (Last {} tasks)", limit);
            println!("================================");

            if filtered_history.is_empty() {
                println!("No task history available.");
            } else {
                println!("Found {} execution records:", filtered_history.len());
                println!();

                for result in &filtered_history {
                    let status_emoji = if result.success { "✅" } else { "❌" };
                    let orchestration_indicator = if result.orchestration_used {
                        "🎯"
                    } else {
                        "🔄"
                    };

                    println!(
                        "{} {} Task: {} | Duration: {:.2}s",
                        status_emoji,
                        orchestration_indicator,
                        &result.task_id[..8],
                        result.duration.as_secs_f64()
                    );

                    if let Some(agent) = &result.agent_used {
                        println!("   👤 Agent: {}", agent);
                    }

                    if let Some(error) = &result.error {
                        println!("   ❌ Error: {}", error);
                    }
                    println!();
                }
            }

            if let Some(agent) = agent_filter {
                println!("Filter: Agent = {}", agent);
            }
            if failed_only {
                println!("Filter: Failed tasks only");
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2886-2929 method create_task_from_args <-> ./crates/ccswarm/src/cli/mod.rs:3545-3626 method handle_auto_create
  Similarity: 91.73%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_task_from_args (lines 2886-2929) ---[0m
    fn create_task_from_args(
        &self,
        description: &str,
        priority: &str,
        task_type: &str,
        details: Option<&str>,
        duration: Option<u32>,
    ) -> Result<Task> {
        let priority = match priority.to_lowercase().as_str() {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            _ => Priority::Medium,
        };

        let task_type = match task_type.to_lowercase().as_str() {
            "development" => TaskType::Development,
            "testing" => TaskType::Testing,
            "infrastructure" => TaskType::Infrastructure,
            "documentation" => TaskType::Documentation,
            "bugfix" => TaskType::Bugfix,
            "feature" => TaskType::Feature,
            "review" => TaskType::Review,
            "coordination" => TaskType::Coordination,
            _ => TaskType::Development,
        };

        let estimated_duration = duration.map(|d| std::time::Duration::from_secs(d as u64));

        let mut task = Task::new(
            uuid::Uuid::new_v4().to_string(),
            description.to_string(),
            priority,
            task_type,
        );

        if let Some(details) = details {
            task = task.with_details(details.to_string());
        }

        task.estimated_duration = estimated_duration.map(|d| d.as_secs() as u32);

        Ok(task)
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_auto_create (lines 3545-3626) ---[0m
    async fn handle_auto_create(
        &self,
        description: &str,
        template: Option<&str>,
        auto_deploy: bool,
        output: &PathBuf,
    ) -> Result<()> {
        use crate::orchestrator::auto_create::AutoCreateEngine;

        info!("🚀 Starting auto-create for: {}", description);

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "started",
                    "message": "Auto-create process initiated",
                    "description": description,
                    "template": template,
                    "auto_deploy": auto_deploy,
                    "output": output,
                }))?
            );
        } else {
            println!("🚀 ccswarm Auto-Create");
            println!("====================");
            println!("📋 Request: {}", description);
            if let Some(tmpl) = template {
                println!("📄 Template: {}", tmpl);
            }
            println!("📂 Output: {}", output.display());
            println!();
        }

        // Create auto-create engine
        let mut engine = AutoCreateEngine::new();

        // Execute auto-create workflow
        match engine
            .execute_auto_create(description, &self.config, output)
            .await
        {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Application created successfully",
                            "output": output,
                        }))?
                    );
                } else {
                    println!("\n✅ Application created successfully!");
                    println!("📂 Location: {}", output.display());

                    if auto_deploy {
                        println!("\n🚀 Auto-deploying application...");
                        // TODO: Implement auto-deployment
                        println!("   ⚠️ Auto-deployment not yet implemented");
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "error",
                            "message": "Auto-create failed",
                            "error": e.to_string(),
                        }))?
                    );
                } else {
                    println!("\n❌ Auto-create failed: {}", e);
                }
                return Err(e);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1993-2078 method show_task_history <-> ./crates/ccswarm/src/cli/mod.rs:5428-5466 method handle_help
  Similarity: 91.91%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_history (lines 1993-2078) ---[0m
    async fn show_task_history(
        &self,
        limit: usize,
        agent_filter: Option<&str>,
        failed_only: bool,
    ) -> Result<()> {
        let history = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.get_execution_history(Some(limit)).await
        } else {
            Vec::new()
        };

        let filtered_history: Vec<_> = history
            .into_iter()
            .filter(|result| {
                if failed_only && result.success {
                    return false;
                }
                if let Some(agent) = agent_filter {
                    if let Some(ref result_agent) = result.agent_used {
                        return result_agent == agent;
                    }
                    return false;
                }
                true
            })
            .collect();

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "history": filtered_history,
                    "limit": limit,
                    "agent_filter": agent_filter,
                    "failed_only": failed_only,
                    "total_count": filtered_history.len()
                }))?
            );
        } else {
            println!("📚 Task History (Last {} tasks)", limit);
            println!("================================");

            if filtered_history.is_empty() {
                println!("No task history available.");
            } else {
                println!("Found {} execution records:", filtered_history.len());
                println!();

                for result in &filtered_history {
                    let status_emoji = if result.success { "✅" } else { "❌" };
                    let orchestration_indicator = if result.orchestration_used {
                        "🎯"
                    } else {
                        "🔄"
                    };

                    println!(
                        "{} {} Task: {} | Duration: {:.2}s",
                        status_emoji,
                        orchestration_indicator,
                        &result.task_id[..8],
                        result.duration.as_secs_f64()
                    );

                    if let Some(agent) = &result.agent_used {
                        println!("   👤 Agent: {}", agent);
                    }

                    if let Some(error) = &result.error {
                        println!("   ❌ Error: {}", error);
                    }
                    println!();
                }
            }

            if let Some(agent) = agent_filter {
                println!("Filter: Agent = {}", agent);
            }
            if failed_only {
                println!("Filter: Failed tasks only");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_help (lines 5428-5466) ---[0m
    async fn handle_help(&self, topic: Option<&str>, search: Option<&str>) -> Result<()> {
        let help = InteractiveHelp::new();

        if let Some(query) = search {
            // Search help topics
            let results = help.search(query);

            if results.is_empty() {
                println!();
                println!(
                    "{}",
                    "❌ No help topics found matching your search.".bright_red()
                );
                println!();
                println!("Try one of these topics:");
                help.show_topic_list();
            } else {
                println!();
                println!(
                    "{}",
                    format!("🔍 Found {} topics matching '{}'", results.len(), query).bright_cyan()
                );
                println!();

                for (key, topic) in results.iter().take(3) {
                    println!("{}", format!("📖 {}", topic.title).bright_yellow());
                    println!("   {}", topic.description.bright_black());
                    println!("   Run: ccswarm help {}", key.bright_white());
                    println!();
                }
            }
        } else if let Some(t) = topic {
            help.show_topic(t);
        } else {
            help.show_topic_list();
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1936-1990 method cancel_task <-> ./crates/ccswarm/src/cli/mod.rs:3545-3626 method handle_auto_create
  Similarity: 82.87%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:cancel_task (lines 1936-1990) ---[0m
    async fn cancel_task(&self, task_id: &str, force: bool, reason: Option<&str>) -> Result<()> {
        let result = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            match executor
                .cancel_task(task_id, reason.map(|s| s.to_string()))
                .await
            {
                Ok(()) => Ok(()),
                Err(e) => Err(e),
            }
        } else {
            Err(anyhow::anyhow!("Execution engine not available"))
        };

        match result {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": true,
                            "reason": reason,
                            "force": force
                        }))?
                    );
                } else {
                    println!("✅ Task cancelled successfully: {}", task_id);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": false,
                            "reason": e.to_string(),
                            "force": force
                        }))?
                    );
                } else {
                    println!("❌ Failed to cancel task: {}", task_id);
                    println!("   Reason: {}", e);
                    if let Some(r) = reason {
                        println!("   User reason: {}", r);
                    }
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_auto_create (lines 3545-3626) ---[0m
    async fn handle_auto_create(
        &self,
        description: &str,
        template: Option<&str>,
        auto_deploy: bool,
        output: &PathBuf,
    ) -> Result<()> {
        use crate::orchestrator::auto_create::AutoCreateEngine;

        info!("🚀 Starting auto-create for: {}", description);

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "started",
                    "message": "Auto-create process initiated",
                    "description": description,
                    "template": template,
                    "auto_deploy": auto_deploy,
                    "output": output,
                }))?
            );
        } else {
            println!("🚀 ccswarm Auto-Create");
            println!("====================");
            println!("📋 Request: {}", description);
            if let Some(tmpl) = template {
                println!("📄 Template: {}", tmpl);
            }
            println!("📂 Output: {}", output.display());
            println!();
        }

        // Create auto-create engine
        let mut engine = AutoCreateEngine::new();

        // Execute auto-create workflow
        match engine
            .execute_auto_create(description, &self.config, output)
            .await
        {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Application created successfully",
                            "output": output,
                        }))?
                    );
                } else {
                    println!("\n✅ Application created successfully!");
                    println!("📂 Location: {}", output.display());

                    if auto_deploy {
                        println!("\n🚀 Auto-deploying application...");
                        // TODO: Implement auto-deployment
                        println!("   ⚠️ Auto-deployment not yet implemented");
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "error",
                            "message": "Auto-create failed",
                            "error": e.to_string(),
                        }))?
                    );
                } else {
                    println!("\n❌ Auto-create failed: {}", e);
                }
                return Err(e);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1936-1990 method cancel_task <-> ./crates/ccswarm/src/cli/mod.rs:2480-2543 method handle_config
  Similarity: 95.05%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:cancel_task (lines 1936-1990) ---[0m
    async fn cancel_task(&self, task_id: &str, force: bool, reason: Option<&str>) -> Result<()> {
        let result = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            match executor
                .cancel_task(task_id, reason.map(|s| s.to_string()))
                .await
            {
                Ok(()) => Ok(()),
                Err(e) => Err(e),
            }
        } else {
            Err(anyhow::anyhow!("Execution engine not available"))
        };

        match result {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": true,
                            "reason": reason,
                            "force": force
                        }))?
                    );
                } else {
                    println!("✅ Task cancelled successfully: {}", task_id);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": false,
                            "reason": e.to_string(),
                            "force": force
                        }))?
                    );
                } else {
                    println!("❌ Failed to cancel task: {}", task_id);
                    println!("   Reason: {}", e);
                    if let Some(r) = reason {
                        println!("   User reason: {}", r);
                    }
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_config (lines 2480-2543) ---[0m
    async fn handle_config(&self, action: &ConfigAction) -> Result<()> {
        match action {
            ConfigAction::Generate { output, template } => {
                let config = match template.as_str() {
                    "minimal" => create_minimal_config(&self.repo_path)?,
                    "frontend-only" => create_frontend_only_config(&self.repo_path)?,
                    "full-stack" => create_default_config(&self.repo_path)?,
                    _ => create_default_config(&self.repo_path)?,
                };

                config.to_file(output.clone()).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Configuration generated",
                            "file": output,
                            "template": template,
                        }))?
                    );
                } else {
                    println!("✅ Configuration generated: {}", output.display());
                    println!("   Template: {}", template);
                }
            }
            ConfigAction::Validate { file } => match CcswarmConfig::from_file(file.clone()).await {
                Ok(_) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "Configuration is valid",
                                "file": file,
                            }))?
                        );
                    } else {
                        println!("✅ Configuration is valid: {}", file.display());
                    }
                }
                Err(e) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "Configuration is invalid",
                                "file": file,
                                "error": e.to_string(),
                            }))?
                        );
                    } else {
                        println!("❌ Configuration is invalid: {}", file.display());
                        println!("   Error: {}", e);
                    }
                    return Err(e);
                }
            },
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1296-1362 method start_orchestrator <-> ./crates/ccswarm/src/cli/mod.rs:2546-2609 function create_default_config
  Similarity: 85.21%

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_orchestrator (lines 1296-1362) ---[0m
    async fn start_orchestrator(
        &self,
        _daemon: bool,
        _port: u16,
        isolation: &str,
        use_real_api: bool,
    ) -> Result<()> {
        info!(
            "Starting ccswarm orchestrator with isolation mode: {} (real_api: {})",
            isolation, use_real_api
        );

        // Parse isolation mode
        let isolation_mode = match isolation {
            "container" => crate::agent::IsolationMode::Container,
            "hybrid" => crate::agent::IsolationMode::Hybrid,
            _ => crate::agent::IsolationMode::GitWorktree,
        };

        // Update configuration to use real API if requested
        let mut config = self.config.clone();
        if use_real_api {
            // Check for API key
            if std::env::var("ANTHROPIC_API_KEY").is_err() {
                return Err(anyhow::anyhow!(
                    "ANTHROPIC_API_KEY environment variable must be set when using --use-real-api"
                ));
            }

            // Update all agent configurations to use real API
            for agent_config in config.agents.values_mut() {
                agent_config.claude_config.use_real_api = true;
            }

            // Update master configuration
            config.project.master_claude.claude_config.use_real_api = true;
        }

        let mut master = MasterClaude::new(config, self.repo_path.clone()).await?;

        // Set isolation mode for all agents
        master.set_isolation_mode(isolation_mode);

        // Initialize agents
        master.initialize().await?;

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Orchestrator started",
                    "master_id": master.id,
                    "agents": master.agents.len(),
                }))?
            );
        } else {
            println!("🚀 ccswarm orchestrator started");
            println!("   Master ID: {}", master.id);
            println!("   Agents: {}", master.agents.len());
        }

        // Start coordination (this would run indefinitely in real usage)
        master.start_coordination().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_default_config (lines 2546-2609) ---[0m
fn create_default_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut agents = std::collections::HashMap::new();

    // Add common agent configurations
    agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend-ui".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    agents.insert(
        "backend".to_string(),
        crate::config::AgentConfig {
            specialization: "node_microservices".to_string(),
            worktree: "agents/backend-agent".to_string(),
            branch: "feature/backend-api".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("backend"),
            claude_md_template: "backend_specialist".to_string(),
        },
    );

    agents.insert(
        "devops".to_string(),
        crate::config::AgentConfig {
            specialization: "aws_kubernetes".to_string(),
            worktree: "agents/devops-agent".to_string(),
            branch: "feature/infrastructure".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("devops"),
            claude_md_template: "devops_specialist".to_string(),
        },
    );

    Ok(CcswarmConfig {
        project: crate::config::ProjectConfig {
            name: "New ccswarm Project".to_string(),
            repository: crate::config::RepositoryConfig {
                url: repo_path.to_string_lossy().to_string(),
                main_branch: "main".to_string(),
            },
            master_claude: crate::config::MasterClaudeConfig {
                role: "technical_lead".to_string(),
                quality_threshold: 0.90,
                think_mode: crate::config::ThinkMode::UltraThink,
                permission_level: "supervised".to_string(),
                claude_config: crate::config::ClaudeConfig::for_master(),
                enable_proactive_mode: true, // デフォルト有効
                proactive_frequency: 30,     // 30秒間隔
                high_frequency: 15,          // 高頻度15秒間隔
            },
        },
        agents,
        coordination: crate::config::CoordinationConfig {
            communication_method: "json_files".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_commit".to_string(),
            master_review_trigger: "all_tasks_complete".to_string(),
        },
    })
}

  ./crates/ccswarm/src/cli/mod.rs:2081-2127 method execute_task_immediate <-> ./crates/ccswarm/src/cli/mod.rs:2480-2543 method handle_config
  Similarity: 98.01%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:execute_task_immediate (lines 2081-2127) ---[0m
    async fn execute_task_immediate(
        &self,
        task: &str,
        agent: Option<&str>,
        orchestrate: bool,
    ) -> Result<()> {
        if let Some(ref _engine) = self.execution_engine {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task": task,
                        "agent": agent,
                        "orchestrate": orchestrate,
                        "status": "not_implemented"
                    }))?
                );
            } else {
                println!("⚡ Immediate Task Execution");
                println!("==========================");
                println!("Task: {}", task);
                if let Some(a) = agent {
                    println!("Agent: {}", a);
                }
                println!("Orchestration: {}", if orchestrate { "Yes" } else { "No" });
                println!();
                println!("❌ Immediate execution not yet implemented");
                println!("💡 Use 'ccswarm task add' to add task to queue for execution");
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task": task,
                        "agent": agent,
                        "orchestrate": orchestrate,
                        "status": "execution_engine_unavailable"
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot execute tasks without execution engine");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_config (lines 2480-2543) ---[0m
    async fn handle_config(&self, action: &ConfigAction) -> Result<()> {
        match action {
            ConfigAction::Generate { output, template } => {
                let config = match template.as_str() {
                    "minimal" => create_minimal_config(&self.repo_path)?,
                    "frontend-only" => create_frontend_only_config(&self.repo_path)?,
                    "full-stack" => create_default_config(&self.repo_path)?,
                    _ => create_default_config(&self.repo_path)?,
                };

                config.to_file(output.clone()).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Configuration generated",
                            "file": output,
                            "template": template,
                        }))?
                    );
                } else {
                    println!("✅ Configuration generated: {}", output.display());
                    println!("   Template: {}", template);
                }
            }
            ConfigAction::Validate { file } => match CcswarmConfig::from_file(file.clone()).await {
                Ok(_) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "Configuration is valid",
                                "file": file,
                            }))?
                        );
                    } else {
                        println!("✅ Configuration is valid: {}", file.display());
                    }
                }
                Err(e) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "Configuration is invalid",
                                "file": file,
                                "error": e.to_string(),
                            }))?
                        );
                    } else {
                        println!("❌ Configuration is invalid: {}", file.display());
                        println!("   Error: {}", e);
                    }
                    return Err(e);
                }
            },
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1993-2078 method show_task_history <-> ./crates/ccswarm/src/cli/mod.rs:2081-2127 method execute_task_immediate
  Similarity: 80.55%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_history (lines 1993-2078) ---[0m
    async fn show_task_history(
        &self,
        limit: usize,
        agent_filter: Option<&str>,
        failed_only: bool,
    ) -> Result<()> {
        let history = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.get_execution_history(Some(limit)).await
        } else {
            Vec::new()
        };

        let filtered_history: Vec<_> = history
            .into_iter()
            .filter(|result| {
                if failed_only && result.success {
                    return false;
                }
                if let Some(agent) = agent_filter {
                    if let Some(ref result_agent) = result.agent_used {
                        return result_agent == agent;
                    }
                    return false;
                }
                true
            })
            .collect();

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "history": filtered_history,
                    "limit": limit,
                    "agent_filter": agent_filter,
                    "failed_only": failed_only,
                    "total_count": filtered_history.len()
                }))?
            );
        } else {
            println!("📚 Task History (Last {} tasks)", limit);
            println!("================================");

            if filtered_history.is_empty() {
                println!("No task history available.");
            } else {
                println!("Found {} execution records:", filtered_history.len());
                println!();

                for result in &filtered_history {
                    let status_emoji = if result.success { "✅" } else { "❌" };
                    let orchestration_indicator = if result.orchestration_used {
                        "🎯"
                    } else {
                        "🔄"
                    };

                    println!(
                        "{} {} Task: {} | Duration: {:.2}s",
                        status_emoji,
                        orchestration_indicator,
                        &result.task_id[..8],
                        result.duration.as_secs_f64()
                    );

                    if let Some(agent) = &result.agent_used {
                        println!("   👤 Agent: {}", agent);
                    }

                    if let Some(error) = &result.error {
                        println!("   ❌ Error: {}", error);
                    }
                    println!();
                }
            }

            if let Some(agent) = agent_filter {
                println!("Filter: Agent = {}", agent);
            }
            if failed_only {
                println!("Filter: Failed tasks only");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:execute_task_immediate (lines 2081-2127) ---[0m
    async fn execute_task_immediate(
        &self,
        task: &str,
        agent: Option<&str>,
        orchestrate: bool,
    ) -> Result<()> {
        if let Some(ref _engine) = self.execution_engine {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task": task,
                        "agent": agent,
                        "orchestrate": orchestrate,
                        "status": "not_implemented"
                    }))?
                );
            } else {
                println!("⚡ Immediate Task Execution");
                println!("==========================");
                println!("Task: {}", task);
                if let Some(a) = agent {
                    println!("Agent: {}", a);
                }
                println!("Orchestration: {}", if orchestrate { "Yes" } else { "No" });
                println!();
                println!("❌ Immediate execution not yet implemented");
                println!("💡 Use 'ccswarm task add' to add task to queue for execution");
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task": task,
                        "agent": agent,
                        "orchestrate": orchestrate,
                        "status": "execution_engine_unavailable"
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot execute tasks without execution engine");
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1993-2078 method show_task_history <-> ./crates/ccswarm/src/cli/mod.rs:2886-2929 method create_task_from_args
  Similarity: 81.98%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_task_history (lines 1993-2078) ---[0m
    async fn show_task_history(
        &self,
        limit: usize,
        agent_filter: Option<&str>,
        failed_only: bool,
    ) -> Result<()> {
        let history = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            executor.get_execution_history(Some(limit)).await
        } else {
            Vec::new()
        };

        let filtered_history: Vec<_> = history
            .into_iter()
            .filter(|result| {
                if failed_only && result.success {
                    return false;
                }
                if let Some(agent) = agent_filter {
                    if let Some(ref result_agent) = result.agent_used {
                        return result_agent == agent;
                    }
                    return false;
                }
                true
            })
            .collect();

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "history": filtered_history,
                    "limit": limit,
                    "agent_filter": agent_filter,
                    "failed_only": failed_only,
                    "total_count": filtered_history.len()
                }))?
            );
        } else {
            println!("📚 Task History (Last {} tasks)", limit);
            println!("================================");

            if filtered_history.is_empty() {
                println!("No task history available.");
            } else {
                println!("Found {} execution records:", filtered_history.len());
                println!();

                for result in &filtered_history {
                    let status_emoji = if result.success { "✅" } else { "❌" };
                    let orchestration_indicator = if result.orchestration_used {
                        "🎯"
                    } else {
                        "🔄"
                    };

                    println!(
                        "{} {} Task: {} | Duration: {:.2}s",
                        status_emoji,
                        orchestration_indicator,
                        &result.task_id[..8],
                        result.duration.as_secs_f64()
                    );

                    if let Some(agent) = &result.agent_used {
                        println!("   👤 Agent: {}", agent);
                    }

                    if let Some(error) = &result.error {
                        println!("   ❌ Error: {}", error);
                    }
                    println!();
                }
            }

            if let Some(agent) = agent_filter {
                println!("Filter: Agent = {}", agent);
            }
            if failed_only {
                println!("Filter: Failed tasks only");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_task_from_args (lines 2886-2929) ---[0m
    fn create_task_from_args(
        &self,
        description: &str,
        priority: &str,
        task_type: &str,
        details: Option<&str>,
        duration: Option<u32>,
    ) -> Result<Task> {
        let priority = match priority.to_lowercase().as_str() {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            _ => Priority::Medium,
        };

        let task_type = match task_type.to_lowercase().as_str() {
            "development" => TaskType::Development,
            "testing" => TaskType::Testing,
            "infrastructure" => TaskType::Infrastructure,
            "documentation" => TaskType::Documentation,
            "bugfix" => TaskType::Bugfix,
            "feature" => TaskType::Feature,
            "review" => TaskType::Review,
            "coordination" => TaskType::Coordination,
            _ => TaskType::Development,
        };

        let estimated_duration = duration.map(|d| std::time::Duration::from_secs(d as u64));

        let mut task = Task::new(
            uuid::Uuid::new_v4().to_string(),
            description.to_string(),
            priority,
            task_type,
        );

        if let Some(details) = details {
            task = task.with_details(details.to_string());
        }

        task.estimated_duration = estimated_duration.map(|d| d.as_secs() as u32);

        Ok(task)
    }

  ./crates/ccswarm/src/cli/mod.rs:3545-3626 method handle_auto_create <-> ./crates/ccswarm/src/cli/mod.rs:5428-5466 method handle_help
  Similarity: 86.85%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_auto_create (lines 3545-3626) ---[0m
    async fn handle_auto_create(
        &self,
        description: &str,
        template: Option<&str>,
        auto_deploy: bool,
        output: &PathBuf,
    ) -> Result<()> {
        use crate::orchestrator::auto_create::AutoCreateEngine;

        info!("🚀 Starting auto-create for: {}", description);

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "started",
                    "message": "Auto-create process initiated",
                    "description": description,
                    "template": template,
                    "auto_deploy": auto_deploy,
                    "output": output,
                }))?
            );
        } else {
            println!("🚀 ccswarm Auto-Create");
            println!("====================");
            println!("📋 Request: {}", description);
            if let Some(tmpl) = template {
                println!("📄 Template: {}", tmpl);
            }
            println!("📂 Output: {}", output.display());
            println!();
        }

        // Create auto-create engine
        let mut engine = AutoCreateEngine::new();

        // Execute auto-create workflow
        match engine
            .execute_auto_create(description, &self.config, output)
            .await
        {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Application created successfully",
                            "output": output,
                        }))?
                    );
                } else {
                    println!("\n✅ Application created successfully!");
                    println!("📂 Location: {}", output.display());

                    if auto_deploy {
                        println!("\n🚀 Auto-deploying application...");
                        // TODO: Implement auto-deployment
                        println!("   ⚠️ Auto-deployment not yet implemented");
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "error",
                            "message": "Auto-create failed",
                            "error": e.to_string(),
                        }))?
                    );
                } else {
                    println!("\n❌ Auto-create failed: {}", e);
                }
                return Err(e);
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_help (lines 5428-5466) ---[0m
    async fn handle_help(&self, topic: Option<&str>, search: Option<&str>) -> Result<()> {
        let help = InteractiveHelp::new();

        if let Some(query) = search {
            // Search help topics
            let results = help.search(query);

            if results.is_empty() {
                println!();
                println!(
                    "{}",
                    "❌ No help topics found matching your search.".bright_red()
                );
                println!();
                println!("Try one of these topics:");
                help.show_topic_list();
            } else {
                println!();
                println!(
                    "{}",
                    format!("🔍 Found {} topics matching '{}'", results.len(), query).bright_cyan()
                );
                println!();

                for (key, topic) in results.iter().take(3) {
                    println!("{}", format!("📖 {}", topic.title).bright_yellow());
                    println!("   {}", topic.description.bright_black());
                    println!("   Run: ccswarm help {}", key.bright_white());
                    println!();
                }
            }
        } else if let Some(t) = topic {
            help.show_topic(t);
        } else {
            help.show_topic_list();
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2081-2127 method execute_task_immediate <-> ./crates/ccswarm/src/cli/mod.rs:3545-3626 method handle_auto_create
  Similarity: 79.17%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:execute_task_immediate (lines 2081-2127) ---[0m
    async fn execute_task_immediate(
        &self,
        task: &str,
        agent: Option<&str>,
        orchestrate: bool,
    ) -> Result<()> {
        if let Some(ref _engine) = self.execution_engine {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task": task,
                        "agent": agent,
                        "orchestrate": orchestrate,
                        "status": "not_implemented"
                    }))?
                );
            } else {
                println!("⚡ Immediate Task Execution");
                println!("==========================");
                println!("Task: {}", task);
                if let Some(a) = agent {
                    println!("Agent: {}", a);
                }
                println!("Orchestration: {}", if orchestrate { "Yes" } else { "No" });
                println!();
                println!("❌ Immediate execution not yet implemented");
                println!("💡 Use 'ccswarm task add' to add task to queue for execution");
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task": task,
                        "agent": agent,
                        "orchestrate": orchestrate,
                        "status": "execution_engine_unavailable"
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot execute tasks without execution engine");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_auto_create (lines 3545-3626) ---[0m
    async fn handle_auto_create(
        &self,
        description: &str,
        template: Option<&str>,
        auto_deploy: bool,
        output: &PathBuf,
    ) -> Result<()> {
        use crate::orchestrator::auto_create::AutoCreateEngine;

        info!("🚀 Starting auto-create for: {}", description);

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "started",
                    "message": "Auto-create process initiated",
                    "description": description,
                    "template": template,
                    "auto_deploy": auto_deploy,
                    "output": output,
                }))?
            );
        } else {
            println!("🚀 ccswarm Auto-Create");
            println!("====================");
            println!("📋 Request: {}", description);
            if let Some(tmpl) = template {
                println!("📄 Template: {}", tmpl);
            }
            println!("📂 Output: {}", output.display());
            println!();
        }

        // Create auto-create engine
        let mut engine = AutoCreateEngine::new();

        // Execute auto-create workflow
        match engine
            .execute_auto_create(description, &self.config, output)
            .await
        {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Application created successfully",
                            "output": output,
                        }))?
                    );
                } else {
                    println!("\n✅ Application created successfully!");
                    println!("📂 Location: {}", output.display());

                    if auto_deploy {
                        println!("\n🚀 Auto-deploying application...");
                        // TODO: Implement auto-deployment
                        println!("   ⚠️ Auto-deployment not yet implemented");
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "error",
                            "message": "Auto-create failed",
                            "error": e.to_string(),
                        }))?
                    );
                } else {
                    println!("\n❌ Auto-create failed: {}", e);
                }
                return Err(e);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2546-2609 function create_default_config <-> ./crates/ccswarm/src/cli/mod.rs:2886-2929 method create_task_from_args
  Similarity: 94.09%

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_default_config (lines 2546-2609) ---[0m
fn create_default_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut agents = std::collections::HashMap::new();

    // Add common agent configurations
    agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend-ui".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    agents.insert(
        "backend".to_string(),
        crate::config::AgentConfig {
            specialization: "node_microservices".to_string(),
            worktree: "agents/backend-agent".to_string(),
            branch: "feature/backend-api".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("backend"),
            claude_md_template: "backend_specialist".to_string(),
        },
    );

    agents.insert(
        "devops".to_string(),
        crate::config::AgentConfig {
            specialization: "aws_kubernetes".to_string(),
            worktree: "agents/devops-agent".to_string(),
            branch: "feature/infrastructure".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("devops"),
            claude_md_template: "devops_specialist".to_string(),
        },
    );

    Ok(CcswarmConfig {
        project: crate::config::ProjectConfig {
            name: "New ccswarm Project".to_string(),
            repository: crate::config::RepositoryConfig {
                url: repo_path.to_string_lossy().to_string(),
                main_branch: "main".to_string(),
            },
            master_claude: crate::config::MasterClaudeConfig {
                role: "technical_lead".to_string(),
                quality_threshold: 0.90,
                think_mode: crate::config::ThinkMode::UltraThink,
                permission_level: "supervised".to_string(),
                claude_config: crate::config::ClaudeConfig::for_master(),
                enable_proactive_mode: true, // デフォルト有効
                proactive_frequency: 30,     // 30秒間隔
                high_frequency: 15,          // 高頻度15秒間隔
            },
        },
        agents,
        coordination: crate::config::CoordinationConfig {
            communication_method: "json_files".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_commit".to_string(),
            master_review_trigger: "all_tasks_complete".to_string(),
        },
    })
}

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_task_from_args (lines 2886-2929) ---[0m
    fn create_task_from_args(
        &self,
        description: &str,
        priority: &str,
        task_type: &str,
        details: Option<&str>,
        duration: Option<u32>,
    ) -> Result<Task> {
        let priority = match priority.to_lowercase().as_str() {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            _ => Priority::Medium,
        };

        let task_type = match task_type.to_lowercase().as_str() {
            "development" => TaskType::Development,
            "testing" => TaskType::Testing,
            "infrastructure" => TaskType::Infrastructure,
            "documentation" => TaskType::Documentation,
            "bugfix" => TaskType::Bugfix,
            "feature" => TaskType::Feature,
            "review" => TaskType::Review,
            "coordination" => TaskType::Coordination,
            _ => TaskType::Development,
        };

        let estimated_duration = duration.map(|d| std::time::Duration::from_secs(d as u64));

        let mut task = Task::new(
            uuid::Uuid::new_v4().to_string(),
            description.to_string(),
            priority,
            task_type,
        );

        if let Some(details) = details {
            task = task.with_details(details.to_string());
        }

        task.estimated_duration = estimated_duration.map(|d| d.as_secs() as u32);

        Ok(task)
    }

  ./crates/ccswarm/src/cli/mod.rs:3545-3626 method handle_auto_create <-> ./crates/ccswarm/src/cli/mod.rs:5382-5407 method handle_setup
  Similarity: 93.43%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_auto_create (lines 3545-3626) ---[0m
    async fn handle_auto_create(
        &self,
        description: &str,
        template: Option<&str>,
        auto_deploy: bool,
        output: &PathBuf,
    ) -> Result<()> {
        use crate::orchestrator::auto_create::AutoCreateEngine;

        info!("🚀 Starting auto-create for: {}", description);

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "started",
                    "message": "Auto-create process initiated",
                    "description": description,
                    "template": template,
                    "auto_deploy": auto_deploy,
                    "output": output,
                }))?
            );
        } else {
            println!("🚀 ccswarm Auto-Create");
            println!("====================");
            println!("📋 Request: {}", description);
            if let Some(tmpl) = template {
                println!("📄 Template: {}", tmpl);
            }
            println!("📂 Output: {}", output.display());
            println!();
        }

        // Create auto-create engine
        let mut engine = AutoCreateEngine::new();

        // Execute auto-create workflow
        match engine
            .execute_auto_create(description, &self.config, output)
            .await
        {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Application created successfully",
                            "output": output,
                        }))?
                    );
                } else {
                    println!("\n✅ Application created successfully!");
                    println!("📂 Location: {}", output.display());

                    if auto_deploy {
                        println!("\n🚀 Auto-deploying application...");
                        // TODO: Implement auto-deployment
                        println!("   ⚠️ Auto-deployment not yet implemented");
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "error",
                            "message": "Auto-create failed",
                            "error": e.to_string(),
                        }))?
                    );
                } else {
                    println!("\n❌ Auto-create failed: {}", e);
                }
                return Err(e);
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_setup (lines 5382-5407) ---[0m
    async fn handle_setup(&self) -> Result<()> {
        // Check if config already exists
        let config_path = self.repo_path.join("ccswarm.json");
        if config_path.exists() {
            println!("{}", "⚠️  Configuration already exists!".bright_yellow());
            println!();
            print!("Overwrite existing configuration? [y/N] ");
            std::io::stdout().flush()?;

            let mut input = String::new();
            std::io::stdin().read_line(&mut input)?;
            if !input.trim().eq_ignore_ascii_case("y") {
                println!("Setup cancelled.");
                return Ok(());
            }
        }

        // Run setup wizard
        let _config = SetupWizard::run().await?;

        // Initialize project
        crate::utils::user_error::show_progress("Initializing project structure...");
        crate::git::shell::ShellWorktreeManager::init_if_needed(&self.repo_path).await?;

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2480-2543 method handle_config <-> ./crates/ccswarm/src/cli/mod.rs:5428-5466 method handle_help
  Similarity: 95.46%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_config (lines 2480-2543) ---[0m
    async fn handle_config(&self, action: &ConfigAction) -> Result<()> {
        match action {
            ConfigAction::Generate { output, template } => {
                let config = match template.as_str() {
                    "minimal" => create_minimal_config(&self.repo_path)?,
                    "frontend-only" => create_frontend_only_config(&self.repo_path)?,
                    "full-stack" => create_default_config(&self.repo_path)?,
                    _ => create_default_config(&self.repo_path)?,
                };

                config.to_file(output.clone()).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Configuration generated",
                            "file": output,
                            "template": template,
                        }))?
                    );
                } else {
                    println!("✅ Configuration generated: {}", output.display());
                    println!("   Template: {}", template);
                }
            }
            ConfigAction::Validate { file } => match CcswarmConfig::from_file(file.clone()).await {
                Ok(_) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "Configuration is valid",
                                "file": file,
                            }))?
                        );
                    } else {
                        println!("✅ Configuration is valid: {}", file.display());
                    }
                }
                Err(e) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "Configuration is invalid",
                                "file": file,
                                "error": e.to_string(),
                            }))?
                        );
                    } else {
                        println!("❌ Configuration is invalid: {}", file.display());
                        println!("   Error: {}", e);
                    }
                    return Err(e);
                }
            },
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_help (lines 5428-5466) ---[0m
    async fn handle_help(&self, topic: Option<&str>, search: Option<&str>) -> Result<()> {
        let help = InteractiveHelp::new();

        if let Some(query) = search {
            // Search help topics
            let results = help.search(query);

            if results.is_empty() {
                println!();
                println!(
                    "{}",
                    "❌ No help topics found matching your search.".bright_red()
                );
                println!();
                println!("Try one of these topics:");
                help.show_topic_list();
            } else {
                println!();
                println!(
                    "{}",
                    format!("🔍 Found {} topics matching '{}'", results.len(), query).bright_cyan()
                );
                println!();

                for (key, topic) in results.iter().take(3) {
                    println!("{}", format!("📖 {}", topic.title).bright_yellow());
                    println!("   {}", topic.description.bright_black());
                    println!("   Run: ccswarm help {}", key.bright_white());
                    println!();
                }
            }
        } else if let Some(t) = topic {
            help.show_topic(t);
        } else {
            help.show_topic_list();
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1936-1990 method cancel_task <-> ./crates/ccswarm/src/cli/mod.rs:2081-2127 method execute_task_immediate
  Similarity: 91.85%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:cancel_task (lines 1936-1990) ---[0m
    async fn cancel_task(&self, task_id: &str, force: bool, reason: Option<&str>) -> Result<()> {
        let result = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            match executor
                .cancel_task(task_id, reason.map(|s| s.to_string()))
                .await
            {
                Ok(()) => Ok(()),
                Err(e) => Err(e),
            }
        } else {
            Err(anyhow::anyhow!("Execution engine not available"))
        };

        match result {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": true,
                            "reason": reason,
                            "force": force
                        }))?
                    );
                } else {
                    println!("✅ Task cancelled successfully: {}", task_id);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": false,
                            "reason": e.to_string(),
                            "force": force
                        }))?
                    );
                } else {
                    println!("❌ Failed to cancel task: {}", task_id);
                    println!("   Reason: {}", e);
                    if let Some(r) = reason {
                        println!("   User reason: {}", r);
                    }
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:execute_task_immediate (lines 2081-2127) ---[0m
    async fn execute_task_immediate(
        &self,
        task: &str,
        agent: Option<&str>,
        orchestrate: bool,
    ) -> Result<()> {
        if let Some(ref _engine) = self.execution_engine {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task": task,
                        "agent": agent,
                        "orchestrate": orchestrate,
                        "status": "not_implemented"
                    }))?
                );
            } else {
                println!("⚡ Immediate Task Execution");
                println!("==========================");
                println!("Task: {}", task);
                if let Some(a) = agent {
                    println!("Agent: {}", a);
                }
                println!("Orchestration: {}", if orchestrate { "Yes" } else { "No" });
                println!();
                println!("❌ Immediate execution not yet implemented");
                println!("💡 Use 'ccswarm task add' to add task to queue for execution");
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task": task,
                        "agent": agent,
                        "orchestrate": orchestrate,
                        "status": "execution_engine_unavailable"
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot execute tasks without execution engine");
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1669-1734 method handle_task <-> ./crates/ccswarm/src/cli/mod.rs:2480-2543 method handle_config
  Similarity: 70.68%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_task (lines 1669-1734) ---[0m
    async fn handle_task(&self, action: &TaskAction) -> Result<()> {
        match action {
            TaskAction::Add {
                description,
                priority,
                task_type,
                details,
                duration,
                auto_assign: _,
                template: _,
                template_vars: _,
                interactive: _,
            } => {
                self.add_task(
                    description,
                    priority,
                    task_type,
                    details.as_deref(),
                    *duration,
                )
                .await
            }
            TaskAction::List {
                all,
                status,
                agent,
                detailed,
            } => {
                self.list_tasks(*all, status.as_deref(), agent.as_deref(), *detailed)
                    .await
            }
            TaskAction::Status {
                task_id,
                history,
                orchestration,
            } => {
                self.show_task_status(task_id, *history, *orchestration)
                    .await
            }
            TaskAction::Cancel {
                task_id,
                force,
                reason,
            } => self.cancel_task(task_id, *force, reason.as_deref()).await,
            TaskAction::History {
                limit,
                agent,
                failed_only,
            } => {
                self.show_task_history(*limit, agent.as_deref(), *failed_only)
                    .await
            }
            TaskAction::Execute {
                task,
                agent,
                orchestrate,
            } => {
                self.execute_task_immediate(task, agent.as_deref(), *orchestrate)
                    .await
            }
            TaskAction::Stats {
                detailed,
                performance,
            } => self.show_task_stats(*detailed, *performance).await,
        }
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_config (lines 2480-2543) ---[0m
    async fn handle_config(&self, action: &ConfigAction) -> Result<()> {
        match action {
            ConfigAction::Generate { output, template } => {
                let config = match template.as_str() {
                    "minimal" => create_minimal_config(&self.repo_path)?,
                    "frontend-only" => create_frontend_only_config(&self.repo_path)?,
                    "full-stack" => create_default_config(&self.repo_path)?,
                    _ => create_default_config(&self.repo_path)?,
                };

                config.to_file(output.clone()).await?;

                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "status": "success",
                            "message": "Configuration generated",
                            "file": output,
                            "template": template,
                        }))?
                    );
                } else {
                    println!("✅ Configuration generated: {}", output.display());
                    println!("   Template: {}", template);
                }
            }
            ConfigAction::Validate { file } => match CcswarmConfig::from_file(file.clone()).await {
                Ok(_) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "success",
                                "message": "Configuration is valid",
                                "file": file,
                            }))?
                        );
                    } else {
                        println!("✅ Configuration is valid: {}", file.display());
                    }
                }
                Err(e) => {
                    if self.json_output {
                        println!(
                            "{}",
                            serde_json::to_string_pretty(&serde_json::json!({
                                "status": "error",
                                "message": "Configuration is invalid",
                                "file": file,
                                "error": e.to_string(),
                            }))?
                        );
                    } else {
                        println!("❌ Configuration is invalid: {}", file.display());
                        println!("   Error: {}", e);
                    }
                    return Err(e);
                }
            },
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1936-1990 method cancel_task <-> ./crates/ccswarm/src/cli/mod.rs:5428-5466 method handle_help
  Similarity: 95.22%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:cancel_task (lines 1936-1990) ---[0m
    async fn cancel_task(&self, task_id: &str, force: bool, reason: Option<&str>) -> Result<()> {
        let result = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            match executor
                .cancel_task(task_id, reason.map(|s| s.to_string()))
                .await
            {
                Ok(()) => Ok(()),
                Err(e) => Err(e),
            }
        } else {
            Err(anyhow::anyhow!("Execution engine not available"))
        };

        match result {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": true,
                            "reason": reason,
                            "force": force
                        }))?
                    );
                } else {
                    println!("✅ Task cancelled successfully: {}", task_id);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": false,
                            "reason": e.to_string(),
                            "force": force
                        }))?
                    );
                } else {
                    println!("❌ Failed to cancel task: {}", task_id);
                    println!("   Reason: {}", e);
                    if let Some(r) = reason {
                        println!("   User reason: {}", r);
                    }
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_help (lines 5428-5466) ---[0m
    async fn handle_help(&self, topic: Option<&str>, search: Option<&str>) -> Result<()> {
        let help = InteractiveHelp::new();

        if let Some(query) = search {
            // Search help topics
            let results = help.search(query);

            if results.is_empty() {
                println!();
                println!(
                    "{}",
                    "❌ No help topics found matching your search.".bright_red()
                );
                println!();
                println!("Try one of these topics:");
                help.show_topic_list();
            } else {
                println!();
                println!(
                    "{}",
                    format!("🔍 Found {} topics matching '{}'", results.len(), query).bright_cyan()
                );
                println!();

                for (key, topic) in results.iter().take(3) {
                    println!("{}", format!("📖 {}", topic.title).bright_yellow());
                    println!("   {}", topic.description.bright_black());
                    println!("   Run: ccswarm help {}", key.bright_white());
                    println!();
                }
            }
        } else if let Some(t) = topic {
            help.show_topic(t);
        } else {
            help.show_topic_list();
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1936-1990 method cancel_task <-> ./crates/ccswarm/src/cli/mod.rs:2546-2609 function create_default_config
  Similarity: 74.14%

[36m--- ./crates/ccswarm/src/cli/mod.rs:cancel_task (lines 1936-1990) ---[0m
    async fn cancel_task(&self, task_id: &str, force: bool, reason: Option<&str>) -> Result<()> {
        let result = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            match executor
                .cancel_task(task_id, reason.map(|s| s.to_string()))
                .await
            {
                Ok(()) => Ok(()),
                Err(e) => Err(e),
            }
        } else {
            Err(anyhow::anyhow!("Execution engine not available"))
        };

        match result {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": true,
                            "reason": reason,
                            "force": force
                        }))?
                    );
                } else {
                    println!("✅ Task cancelled successfully: {}", task_id);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": false,
                            "reason": e.to_string(),
                            "force": force
                        }))?
                    );
                } else {
                    println!("❌ Failed to cancel task: {}", task_id);
                    println!("   Reason: {}", e);
                    if let Some(r) = reason {
                        println!("   User reason: {}", r);
                    }
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_default_config (lines 2546-2609) ---[0m
fn create_default_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut agents = std::collections::HashMap::new();

    // Add common agent configurations
    agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend-ui".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    agents.insert(
        "backend".to_string(),
        crate::config::AgentConfig {
            specialization: "node_microservices".to_string(),
            worktree: "agents/backend-agent".to_string(),
            branch: "feature/backend-api".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("backend"),
            claude_md_template: "backend_specialist".to_string(),
        },
    );

    agents.insert(
        "devops".to_string(),
        crate::config::AgentConfig {
            specialization: "aws_kubernetes".to_string(),
            worktree: "agents/devops-agent".to_string(),
            branch: "feature/infrastructure".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("devops"),
            claude_md_template: "devops_specialist".to_string(),
        },
    );

    Ok(CcswarmConfig {
        project: crate::config::ProjectConfig {
            name: "New ccswarm Project".to_string(),
            repository: crate::config::RepositoryConfig {
                url: repo_path.to_string_lossy().to_string(),
                main_branch: "main".to_string(),
            },
            master_claude: crate::config::MasterClaudeConfig {
                role: "technical_lead".to_string(),
                quality_threshold: 0.90,
                think_mode: crate::config::ThinkMode::UltraThink,
                permission_level: "supervised".to_string(),
                claude_config: crate::config::ClaudeConfig::for_master(),
                enable_proactive_mode: true, // デフォルト有効
                proactive_frequency: 30,     // 30秒間隔
                high_frequency: 15,          // 高頻度15秒間隔
            },
        },
        agents,
        coordination: crate::config::CoordinationConfig {
            communication_method: "json_files".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_commit".to_string(),
            master_review_trigger: "all_tasks_complete".to_string(),
        },
    })
}

  ./crates/ccswarm/src/cli/mod.rs:1669-1734 method handle_task <-> ./crates/ccswarm/src/cli/mod.rs:5428-5466 method handle_help
  Similarity: 81.84%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_task (lines 1669-1734) ---[0m
    async fn handle_task(&self, action: &TaskAction) -> Result<()> {
        match action {
            TaskAction::Add {
                description,
                priority,
                task_type,
                details,
                duration,
                auto_assign: _,
                template: _,
                template_vars: _,
                interactive: _,
            } => {
                self.add_task(
                    description,
                    priority,
                    task_type,
                    details.as_deref(),
                    *duration,
                )
                .await
            }
            TaskAction::List {
                all,
                status,
                agent,
                detailed,
            } => {
                self.list_tasks(*all, status.as_deref(), agent.as_deref(), *detailed)
                    .await
            }
            TaskAction::Status {
                task_id,
                history,
                orchestration,
            } => {
                self.show_task_status(task_id, *history, *orchestration)
                    .await
            }
            TaskAction::Cancel {
                task_id,
                force,
                reason,
            } => self.cancel_task(task_id, *force, reason.as_deref()).await,
            TaskAction::History {
                limit,
                agent,
                failed_only,
            } => {
                self.show_task_history(*limit, agent.as_deref(), *failed_only)
                    .await
            }
            TaskAction::Execute {
                task,
                agent,
                orchestrate,
            } => {
                self.execute_task_immediate(task, agent.as_deref(), *orchestrate)
                    .await
            }
            TaskAction::Stats {
                detailed,
                performance,
            } => self.show_task_stats(*detailed, *performance).await,
        }
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_help (lines 5428-5466) ---[0m
    async fn handle_help(&self, topic: Option<&str>, search: Option<&str>) -> Result<()> {
        let help = InteractiveHelp::new();

        if let Some(query) = search {
            // Search help topics
            let results = help.search(query);

            if results.is_empty() {
                println!();
                println!(
                    "{}",
                    "❌ No help topics found matching your search.".bright_red()
                );
                println!();
                println!("Try one of these topics:");
                help.show_topic_list();
            } else {
                println!();
                println!(
                    "{}",
                    format!("🔍 Found {} topics matching '{}'", results.len(), query).bright_cyan()
                );
                println!();

                for (key, topic) in results.iter().take(3) {
                    println!("{}", format!("📖 {}", topic.title).bright_yellow());
                    println!("   {}", topic.description.bright_black());
                    println!("   Run: ccswarm help {}", key.bright_white());
                    println!();
                }
            }
        } else if let Some(t) = topic {
            help.show_topic(t);
        } else {
            help.show_topic_list();
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1296-1362 method start_orchestrator <-> ./crates/ccswarm/src/cli/mod.rs:2886-2929 method create_task_from_args
  Similarity: 77.14%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_orchestrator (lines 1296-1362) ---[0m
    async fn start_orchestrator(
        &self,
        _daemon: bool,
        _port: u16,
        isolation: &str,
        use_real_api: bool,
    ) -> Result<()> {
        info!(
            "Starting ccswarm orchestrator with isolation mode: {} (real_api: {})",
            isolation, use_real_api
        );

        // Parse isolation mode
        let isolation_mode = match isolation {
            "container" => crate::agent::IsolationMode::Container,
            "hybrid" => crate::agent::IsolationMode::Hybrid,
            _ => crate::agent::IsolationMode::GitWorktree,
        };

        // Update configuration to use real API if requested
        let mut config = self.config.clone();
        if use_real_api {
            // Check for API key
            if std::env::var("ANTHROPIC_API_KEY").is_err() {
                return Err(anyhow::anyhow!(
                    "ANTHROPIC_API_KEY environment variable must be set when using --use-real-api"
                ));
            }

            // Update all agent configurations to use real API
            for agent_config in config.agents.values_mut() {
                agent_config.claude_config.use_real_api = true;
            }

            // Update master configuration
            config.project.master_claude.claude_config.use_real_api = true;
        }

        let mut master = MasterClaude::new(config, self.repo_path.clone()).await?;

        // Set isolation mode for all agents
        master.set_isolation_mode(isolation_mode);

        // Initialize agents
        master.initialize().await?;

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Orchestrator started",
                    "master_id": master.id,
                    "agents": master.agents.len(),
                }))?
            );
        } else {
            println!("🚀 ccswarm orchestrator started");
            println!("   Master ID: {}", master.id);
            println!("   Agents: {}", master.agents.len());
        }

        // Start coordination (this would run indefinitely in real usage)
        master.start_coordination().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_task_from_args (lines 2886-2929) ---[0m
    fn create_task_from_args(
        &self,
        description: &str,
        priority: &str,
        task_type: &str,
        details: Option<&str>,
        duration: Option<u32>,
    ) -> Result<Task> {
        let priority = match priority.to_lowercase().as_str() {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            _ => Priority::Medium,
        };

        let task_type = match task_type.to_lowercase().as_str() {
            "development" => TaskType::Development,
            "testing" => TaskType::Testing,
            "infrastructure" => TaskType::Infrastructure,
            "documentation" => TaskType::Documentation,
            "bugfix" => TaskType::Bugfix,
            "feature" => TaskType::Feature,
            "review" => TaskType::Review,
            "coordination" => TaskType::Coordination,
            _ => TaskType::Development,
        };

        let estimated_duration = duration.map(|d| std::time::Duration::from_secs(d as u64));

        let mut task = Task::new(
            uuid::Uuid::new_v4().to_string(),
            description.to_string(),
            priority,
            task_type,
        );

        if let Some(details) = details {
            task = task.with_details(details.to_string());
        }

        task.estimated_duration = estimated_duration.map(|d| d.as_secs() as u32);

        Ok(task)
    }

  ./crates/ccswarm/src/cli/mod.rs:1173-1209 method new <-> ./crates/ccswarm/src/cli/mod.rs:1296-1362 method start_orchestrator
  Similarity: 81.13%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:new (lines 1173-1209) ---[0m
    pub async fn new(cli: &Cli) -> Result<Self> {
        // Load configuration
        let config = if cli.config.exists() {
            CcswarmConfig::from_file(cli.config.clone())
                .await
                .context("Failed to load configuration")?
        } else {
            warn!("Configuration file not found, using defaults");
            create_default_config(&cli.repo)?
        };

        let formatter = create_formatter(cli.json);

        // Initialize execution engine for task management
        let execution_engine = match ExecutionEngine::new(&config).await {
            Ok(engine) => {
                if let Err(e) = engine.start().await {
                    warn!("Failed to start execution engine: {}", e);
                    None
                } else {
                    Some(engine)
                }
            }
            Err(e) => {
                warn!("Failed to create execution engine: {}", e);
                None
            }
        };

        Ok(Self {
            config,
            repo_path: cli.repo.clone(),
            json_output: cli.json,
            formatter,
            execution_engine,
        })
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_orchestrator (lines 1296-1362) ---[0m
    async fn start_orchestrator(
        &self,
        _daemon: bool,
        _port: u16,
        isolation: &str,
        use_real_api: bool,
    ) -> Result<()> {
        info!(
            "Starting ccswarm orchestrator with isolation mode: {} (real_api: {})",
            isolation, use_real_api
        );

        // Parse isolation mode
        let isolation_mode = match isolation {
            "container" => crate::agent::IsolationMode::Container,
            "hybrid" => crate::agent::IsolationMode::Hybrid,
            _ => crate::agent::IsolationMode::GitWorktree,
        };

        // Update configuration to use real API if requested
        let mut config = self.config.clone();
        if use_real_api {
            // Check for API key
            if std::env::var("ANTHROPIC_API_KEY").is_err() {
                return Err(anyhow::anyhow!(
                    "ANTHROPIC_API_KEY environment variable must be set when using --use-real-api"
                ));
            }

            // Update all agent configurations to use real API
            for agent_config in config.agents.values_mut() {
                agent_config.claude_config.use_real_api = true;
            }

            // Update master configuration
            config.project.master_claude.claude_config.use_real_api = true;
        }

        let mut master = MasterClaude::new(config, self.repo_path.clone()).await?;

        // Set isolation mode for all agents
        master.set_isolation_mode(isolation_mode);

        // Initialize agents
        master.initialize().await?;

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Orchestrator started",
                    "master_id": master.id,
                    "agents": master.agents.len(),
                }))?
            );
        } else {
            println!("🚀 ccswarm orchestrator started");
            println!("   Master ID: {}", master.id);
            println!("   Agents: {}", master.agents.len());
        }

        // Start coordination (this would run indefinitely in real usage)
        master.start_coordination().await?;

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1173-1209 method new <-> ./crates/ccswarm/src/cli/mod.rs:1936-1990 method cancel_task
  Similarity: 91.29%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:new (lines 1173-1209) ---[0m
    pub async fn new(cli: &Cli) -> Result<Self> {
        // Load configuration
        let config = if cli.config.exists() {
            CcswarmConfig::from_file(cli.config.clone())
                .await
                .context("Failed to load configuration")?
        } else {
            warn!("Configuration file not found, using defaults");
            create_default_config(&cli.repo)?
        };

        let formatter = create_formatter(cli.json);

        // Initialize execution engine for task management
        let execution_engine = match ExecutionEngine::new(&config).await {
            Ok(engine) => {
                if let Err(e) = engine.start().await {
                    warn!("Failed to start execution engine: {}", e);
                    None
                } else {
                    Some(engine)
                }
            }
            Err(e) => {
                warn!("Failed to create execution engine: {}", e);
                None
            }
        };

        Ok(Self {
            config,
            repo_path: cli.repo.clone(),
            json_output: cli.json,
            formatter,
            execution_engine,
        })
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:cancel_task (lines 1936-1990) ---[0m
    async fn cancel_task(&self, task_id: &str, force: bool, reason: Option<&str>) -> Result<()> {
        let result = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            match executor
                .cancel_task(task_id, reason.map(|s| s.to_string()))
                .await
            {
                Ok(()) => Ok(()),
                Err(e) => Err(e),
            }
        } else {
            Err(anyhow::anyhow!("Execution engine not available"))
        };

        match result {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": true,
                            "reason": reason,
                            "force": force
                        }))?
                    );
                } else {
                    println!("✅ Task cancelled successfully: {}", task_id);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": false,
                            "reason": e.to_string(),
                            "force": force
                        }))?
                    );
                } else {
                    println!("❌ Failed to cancel task: {}", task_id);
                    println!("   Reason: {}", e);
                    if let Some(r) = reason {
                        println!("   User reason: {}", r);
                    }
                }
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1296-1362 method start_orchestrator <-> ./crates/ccswarm/src/cli/mod.rs:5382-5407 method handle_setup
  Similarity: 85.77%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_orchestrator (lines 1296-1362) ---[0m
    async fn start_orchestrator(
        &self,
        _daemon: bool,
        _port: u16,
        isolation: &str,
        use_real_api: bool,
    ) -> Result<()> {
        info!(
            "Starting ccswarm orchestrator with isolation mode: {} (real_api: {})",
            isolation, use_real_api
        );

        // Parse isolation mode
        let isolation_mode = match isolation {
            "container" => crate::agent::IsolationMode::Container,
            "hybrid" => crate::agent::IsolationMode::Hybrid,
            _ => crate::agent::IsolationMode::GitWorktree,
        };

        // Update configuration to use real API if requested
        let mut config = self.config.clone();
        if use_real_api {
            // Check for API key
            if std::env::var("ANTHROPIC_API_KEY").is_err() {
                return Err(anyhow::anyhow!(
                    "ANTHROPIC_API_KEY environment variable must be set when using --use-real-api"
                ));
            }

            // Update all agent configurations to use real API
            for agent_config in config.agents.values_mut() {
                agent_config.claude_config.use_real_api = true;
            }

            // Update master configuration
            config.project.master_claude.claude_config.use_real_api = true;
        }

        let mut master = MasterClaude::new(config, self.repo_path.clone()).await?;

        // Set isolation mode for all agents
        master.set_isolation_mode(isolation_mode);

        // Initialize agents
        master.initialize().await?;

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Orchestrator started",
                    "master_id": master.id,
                    "agents": master.agents.len(),
                }))?
            );
        } else {
            println!("🚀 ccswarm orchestrator started");
            println!("   Master ID: {}", master.id);
            println!("   Agents: {}", master.agents.len());
        }

        // Start coordination (this would run indefinitely in real usage)
        master.start_coordination().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_setup (lines 5382-5407) ---[0m
    async fn handle_setup(&self) -> Result<()> {
        // Check if config already exists
        let config_path = self.repo_path.join("ccswarm.json");
        if config_path.exists() {
            println!("{}", "⚠️  Configuration already exists!".bright_yellow());
            println!();
            print!("Overwrite existing configuration? [y/N] ");
            std::io::stdout().flush()?;

            let mut input = String::new();
            std::io::stdin().read_line(&mut input)?;
            if !input.trim().eq_ignore_ascii_case("y") {
                println!("Setup cancelled.");
                return Ok(());
            }
        }

        // Run setup wizard
        let _config = SetupWizard::run().await?;

        // Initialize project
        crate::utils::user_error::show_progress("Initializing project structure...");
        crate::git::shell::ShellWorktreeManager::init_if_needed(&self.repo_path).await?;

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2081-2127 method execute_task_immediate <-> ./crates/ccswarm/src/cli/mod.rs:5428-5466 method handle_help
  Similarity: 91.86%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:execute_task_immediate (lines 2081-2127) ---[0m
    async fn execute_task_immediate(
        &self,
        task: &str,
        agent: Option<&str>,
        orchestrate: bool,
    ) -> Result<()> {
        if let Some(ref _engine) = self.execution_engine {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task": task,
                        "agent": agent,
                        "orchestrate": orchestrate,
                        "status": "not_implemented"
                    }))?
                );
            } else {
                println!("⚡ Immediate Task Execution");
                println!("==========================");
                println!("Task: {}", task);
                if let Some(a) = agent {
                    println!("Agent: {}", a);
                }
                println!("Orchestration: {}", if orchestrate { "Yes" } else { "No" });
                println!();
                println!("❌ Immediate execution not yet implemented");
                println!("💡 Use 'ccswarm task add' to add task to queue for execution");
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task": task,
                        "agent": agent,
                        "orchestrate": orchestrate,
                        "status": "execution_engine_unavailable"
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot execute tasks without execution engine");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_help (lines 5428-5466) ---[0m
    async fn handle_help(&self, topic: Option<&str>, search: Option<&str>) -> Result<()> {
        let help = InteractiveHelp::new();

        if let Some(query) = search {
            // Search help topics
            let results = help.search(query);

            if results.is_empty() {
                println!();
                println!(
                    "{}",
                    "❌ No help topics found matching your search.".bright_red()
                );
                println!();
                println!("Try one of these topics:");
                help.show_topic_list();
            } else {
                println!();
                println!(
                    "{}",
                    format!("🔍 Found {} topics matching '{}'", results.len(), query).bright_cyan()
                );
                println!();

                for (key, topic) in results.iter().take(3) {
                    println!("{}", format!("📖 {}", topic.title).bright_yellow());
                    println!("   {}", topic.description.bright_black());
                    println!("   Run: ccswarm help {}", key.bright_white());
                    println!();
                }
            }
        } else if let Some(t) = topic {
            help.show_topic(t);
        } else {
            help.show_topic_list();
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2546-2609 function create_default_config <-> ./crates/ccswarm/src/cli/mod.rs:5428-5466 method handle_help
  Similarity: 71.87%

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_default_config (lines 2546-2609) ---[0m
fn create_default_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut agents = std::collections::HashMap::new();

    // Add common agent configurations
    agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend-ui".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    agents.insert(
        "backend".to_string(),
        crate::config::AgentConfig {
            specialization: "node_microservices".to_string(),
            worktree: "agents/backend-agent".to_string(),
            branch: "feature/backend-api".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("backend"),
            claude_md_template: "backend_specialist".to_string(),
        },
    );

    agents.insert(
        "devops".to_string(),
        crate::config::AgentConfig {
            specialization: "aws_kubernetes".to_string(),
            worktree: "agents/devops-agent".to_string(),
            branch: "feature/infrastructure".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("devops"),
            claude_md_template: "devops_specialist".to_string(),
        },
    );

    Ok(CcswarmConfig {
        project: crate::config::ProjectConfig {
            name: "New ccswarm Project".to_string(),
            repository: crate::config::RepositoryConfig {
                url: repo_path.to_string_lossy().to_string(),
                main_branch: "main".to_string(),
            },
            master_claude: crate::config::MasterClaudeConfig {
                role: "technical_lead".to_string(),
                quality_threshold: 0.90,
                think_mode: crate::config::ThinkMode::UltraThink,
                permission_level: "supervised".to_string(),
                claude_config: crate::config::ClaudeConfig::for_master(),
                enable_proactive_mode: true, // デフォルト有効
                proactive_frequency: 30,     // 30秒間隔
                high_frequency: 15,          // 高頻度15秒間隔
            },
        },
        agents,
        coordination: crate::config::CoordinationConfig {
            communication_method: "json_files".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_commit".to_string(),
            master_review_trigger: "all_tasks_complete".to_string(),
        },
    })
}

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_help (lines 5428-5466) ---[0m
    async fn handle_help(&self, topic: Option<&str>, search: Option<&str>) -> Result<()> {
        let help = InteractiveHelp::new();

        if let Some(query) = search {
            // Search help topics
            let results = help.search(query);

            if results.is_empty() {
                println!();
                println!(
                    "{}",
                    "❌ No help topics found matching your search.".bright_red()
                );
                println!();
                println!("Try one of these topics:");
                help.show_topic_list();
            } else {
                println!();
                println!(
                    "{}",
                    format!("🔍 Found {} topics matching '{}'", results.len(), query).bright_cyan()
                );
                println!();

                for (key, topic) in results.iter().take(3) {
                    println!("{}", format!("📖 {}", topic.title).bright_yellow());
                    println!("   {}", topic.description.bright_black());
                    println!("   Run: ccswarm help {}", key.bright_white());
                    println!();
                }
            }
        } else if let Some(t) = topic {
            help.show_topic(t);
        } else {
            help.show_topic_list();
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1173-1209 method new <-> ./crates/ccswarm/src/cli/mod.rs:2886-2929 method create_task_from_args
  Similarity: 88.80%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:new (lines 1173-1209) ---[0m
    pub async fn new(cli: &Cli) -> Result<Self> {
        // Load configuration
        let config = if cli.config.exists() {
            CcswarmConfig::from_file(cli.config.clone())
                .await
                .context("Failed to load configuration")?
        } else {
            warn!("Configuration file not found, using defaults");
            create_default_config(&cli.repo)?
        };

        let formatter = create_formatter(cli.json);

        // Initialize execution engine for task management
        let execution_engine = match ExecutionEngine::new(&config).await {
            Ok(engine) => {
                if let Err(e) = engine.start().await {
                    warn!("Failed to start execution engine: {}", e);
                    None
                } else {
                    Some(engine)
                }
            }
            Err(e) => {
                warn!("Failed to create execution engine: {}", e);
                None
            }
        };

        Ok(Self {
            config,
            repo_path: cli.repo.clone(),
            json_output: cli.json,
            formatter,
            execution_engine,
        })
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_task_from_args (lines 2886-2929) ---[0m
    fn create_task_from_args(
        &self,
        description: &str,
        priority: &str,
        task_type: &str,
        details: Option<&str>,
        duration: Option<u32>,
    ) -> Result<Task> {
        let priority = match priority.to_lowercase().as_str() {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            _ => Priority::Medium,
        };

        let task_type = match task_type.to_lowercase().as_str() {
            "development" => TaskType::Development,
            "testing" => TaskType::Testing,
            "infrastructure" => TaskType::Infrastructure,
            "documentation" => TaskType::Documentation,
            "bugfix" => TaskType::Bugfix,
            "feature" => TaskType::Feature,
            "review" => TaskType::Review,
            "coordination" => TaskType::Coordination,
            _ => TaskType::Development,
        };

        let estimated_duration = duration.map(|d| std::time::Duration::from_secs(d as u64));

        let mut task = Task::new(
            uuid::Uuid::new_v4().to_string(),
            description.to_string(),
            priority,
            task_type,
        );

        if let Some(details) = details {
            task = task.with_details(details.to_string());
        }

        task.estimated_duration = estimated_duration.map(|d| d.as_secs() as u32);

        Ok(task)
    }

  ./crates/ccswarm/src/cli/mod.rs:1173-1209 method new <-> ./crates/ccswarm/src/cli/mod.rs:5428-5466 method handle_help
  Similarity: 85.36%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:new (lines 1173-1209) ---[0m
    pub async fn new(cli: &Cli) -> Result<Self> {
        // Load configuration
        let config = if cli.config.exists() {
            CcswarmConfig::from_file(cli.config.clone())
                .await
                .context("Failed to load configuration")?
        } else {
            warn!("Configuration file not found, using defaults");
            create_default_config(&cli.repo)?
        };

        let formatter = create_formatter(cli.json);

        // Initialize execution engine for task management
        let execution_engine = match ExecutionEngine::new(&config).await {
            Ok(engine) => {
                if let Err(e) = engine.start().await {
                    warn!("Failed to start execution engine: {}", e);
                    None
                } else {
                    Some(engine)
                }
            }
            Err(e) => {
                warn!("Failed to create execution engine: {}", e);
                None
            }
        };

        Ok(Self {
            config,
            repo_path: cli.repo.clone(),
            json_output: cli.json,
            formatter,
            execution_engine,
        })
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_help (lines 5428-5466) ---[0m
    async fn handle_help(&self, topic: Option<&str>, search: Option<&str>) -> Result<()> {
        let help = InteractiveHelp::new();

        if let Some(query) = search {
            // Search help topics
            let results = help.search(query);

            if results.is_empty() {
                println!();
                println!(
                    "{}",
                    "❌ No help topics found matching your search.".bright_red()
                );
                println!();
                println!("Try one of these topics:");
                help.show_topic_list();
            } else {
                println!();
                println!(
                    "{}",
                    format!("🔍 Found {} topics matching '{}'", results.len(), query).bright_cyan()
                );
                println!();

                for (key, topic) in results.iter().take(3) {
                    println!("{}", format!("📖 {}", topic.title).bright_yellow());
                    println!("   {}", topic.description.bright_black());
                    println!("   Run: ccswarm help {}", key.bright_white());
                    println!();
                }
            }
        } else if let Some(t) = topic {
            help.show_topic(t);
        } else {
            help.show_topic_list();
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2081-2127 method execute_task_immediate <-> ./crates/ccswarm/src/cli/mod.rs:2240-2258 method list_agents
  Similarity: 94.34%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:execute_task_immediate (lines 2081-2127) ---[0m
    async fn execute_task_immediate(
        &self,
        task: &str,
        agent: Option<&str>,
        orchestrate: bool,
    ) -> Result<()> {
        if let Some(ref _engine) = self.execution_engine {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task": task,
                        "agent": agent,
                        "orchestrate": orchestrate,
                        "status": "not_implemented"
                    }))?
                );
            } else {
                println!("⚡ Immediate Task Execution");
                println!("==========================");
                println!("Task: {}", task);
                if let Some(a) = agent {
                    println!("Agent: {}", a);
                }
                println!("Orchestration: {}", if orchestrate { "Yes" } else { "No" });
                println!();
                println!("❌ Immediate execution not yet implemented");
                println!("💡 Use 'ccswarm task add' to add task to queue for execution");
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task": task,
                        "agent": agent,
                        "orchestrate": orchestrate,
                        "status": "execution_engine_unavailable"
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot execute tasks without execution engine");
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_agents (lines 2240-2258) ---[0m
    async fn list_agents(&self, _all: bool) -> Result<()> {
        if self.json_output {
            println!("{}", serde_json::to_string_pretty(&self.config.agents)?);
        } else {
            println!("🤖 Configured Agents");
            println!("==================");

            for (name, config) in &self.config.agents {
                println!("Agent: {}", name);
                println!("  Specialization: {}", config.specialization);
                println!("  Worktree: {}", config.worktree);
                println!("  Branch: {}", config.branch);
                println!("  Think Mode: {:?}", config.claude_config.think_mode);
                println!();
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1936-1990 method cancel_task <-> ./crates/ccswarm/src/cli/mod.rs:5382-5407 method handle_setup
  Similarity: 76.81%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:cancel_task (lines 1936-1990) ---[0m
    async fn cancel_task(&self, task_id: &str, force: bool, reason: Option<&str>) -> Result<()> {
        let result = if let Some(ref engine) = self.execution_engine {
            let executor = engine.get_executor();
            match executor
                .cancel_task(task_id, reason.map(|s| s.to_string()))
                .await
            {
                Ok(()) => Ok(()),
                Err(e) => Err(e),
            }
        } else {
            Err(anyhow::anyhow!("Execution engine not available"))
        };

        match result {
            Ok(()) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": true,
                            "reason": reason,
                            "force": force
                        }))?
                    );
                } else {
                    println!("✅ Task cancelled successfully: {}", task_id);
                    if let Some(r) = reason {
                        println!("   Reason: {}", r);
                    }
                }
            }
            Err(e) => {
                if self.json_output {
                    println!(
                        "{}",
                        serde_json::to_string_pretty(&serde_json::json!({
                            "task_id": task_id,
                            "cancelled": false,
                            "reason": e.to_string(),
                            "force": force
                        }))?
                    );
                } else {
                    println!("❌ Failed to cancel task: {}", task_id);
                    println!("   Reason: {}", e);
                    if let Some(r) = reason {
                        println!("   User reason: {}", r);
                    }
                }
            }
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_setup (lines 5382-5407) ---[0m
    async fn handle_setup(&self) -> Result<()> {
        // Check if config already exists
        let config_path = self.repo_path.join("ccswarm.json");
        if config_path.exists() {
            println!("{}", "⚠️  Configuration already exists!".bright_yellow());
            println!();
            print!("Overwrite existing configuration? [y/N] ");
            std::io::stdout().flush()?;

            let mut input = String::new();
            std::io::stdin().read_line(&mut input)?;
            if !input.trim().eq_ignore_ascii_case("y") {
                println!("Setup cancelled.");
                return Ok(());
            }
        }

        // Run setup wizard
        let _config = SetupWizard::run().await?;

        // Initialize project
        crate::utils::user_error::show_progress("Initializing project structure...");
        crate::git::shell::ShellWorktreeManager::init_if_needed(&self.repo_path).await?;

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1364-1388 method start_tui <-> ./crates/ccswarm/src/cli/mod.rs:2081-2127 method execute_task_immediate
  Similarity: 86.31%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_tui (lines 1364-1388) ---[0m
    async fn start_tui(&self) -> Result<()> {
        info!("Starting ccswarm TUI");

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Starting TUI mode",
                }))?
            );
        } else {
            println!("🖥️  Starting ccswarm TUI...");
            println!("   Press 'q' to quit");
        }

        // Start TUI with execution engine if available
        if let Some(ref execution_engine) = self.execution_engine {
            crate::tui::run_tui_with_engine(execution_engine.clone()).await?;
        } else {
            crate::tui::run_tui().await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:execute_task_immediate (lines 2081-2127) ---[0m
    async fn execute_task_immediate(
        &self,
        task: &str,
        agent: Option<&str>,
        orchestrate: bool,
    ) -> Result<()> {
        if let Some(ref _engine) = self.execution_engine {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task": task,
                        "agent": agent,
                        "orchestrate": orchestrate,
                        "status": "not_implemented"
                    }))?
                );
            } else {
                println!("⚡ Immediate Task Execution");
                println!("==========================");
                println!("Task: {}", task);
                if let Some(a) = agent {
                    println!("Agent: {}", a);
                }
                println!("Orchestration: {}", if orchestrate { "Yes" } else { "No" });
                println!();
                println!("❌ Immediate execution not yet implemented");
                println!("💡 Use 'ccswarm task add' to add task to queue for execution");
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task": task,
                        "agent": agent,
                        "orchestrate": orchestrate,
                        "status": "execution_engine_unavailable"
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot execute tasks without execution engine");
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1173-1209 method new <-> ./crates/ccswarm/src/cli/mod.rs:2081-2127 method execute_task_immediate
  Similarity: 73.16%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:new (lines 1173-1209) ---[0m
    pub async fn new(cli: &Cli) -> Result<Self> {
        // Load configuration
        let config = if cli.config.exists() {
            CcswarmConfig::from_file(cli.config.clone())
                .await
                .context("Failed to load configuration")?
        } else {
            warn!("Configuration file not found, using defaults");
            create_default_config(&cli.repo)?
        };

        let formatter = create_formatter(cli.json);

        // Initialize execution engine for task management
        let execution_engine = match ExecutionEngine::new(&config).await {
            Ok(engine) => {
                if let Err(e) = engine.start().await {
                    warn!("Failed to start execution engine: {}", e);
                    None
                } else {
                    Some(engine)
                }
            }
            Err(e) => {
                warn!("Failed to create execution engine: {}", e);
                None
            }
        };

        Ok(Self {
            config,
            repo_path: cli.repo.clone(),
            json_output: cli.json,
            formatter,
            execution_engine,
        })
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:execute_task_immediate (lines 2081-2127) ---[0m
    async fn execute_task_immediate(
        &self,
        task: &str,
        agent: Option<&str>,
        orchestrate: bool,
    ) -> Result<()> {
        if let Some(ref _engine) = self.execution_engine {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task": task,
                        "agent": agent,
                        "orchestrate": orchestrate,
                        "status": "not_implemented"
                    }))?
                );
            } else {
                println!("⚡ Immediate Task Execution");
                println!("==========================");
                println!("Task: {}", task);
                if let Some(a) = agent {
                    println!("Agent: {}", a);
                }
                println!("Orchestration: {}", if orchestrate { "Yes" } else { "No" });
                println!();
                println!("❌ Immediate execution not yet implemented");
                println!("💡 Use 'ccswarm task add' to add task to queue for execution");
            }
        } else {
            if self.json_output {
                println!(
                    "{}",
                    serde_json::to_string_pretty(&serde_json::json!({
                        "task": task,
                        "agent": agent,
                        "orchestrate": orchestrate,
                        "status": "execution_engine_unavailable"
                    }))?
                );
            } else {
                println!("❌ Execution engine not available");
                println!("   Cannot execute tasks without execution engine");
            }
        }
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2886-2929 method create_task_from_args <-> ./crates/ccswarm/src/cli/mod.rs:5382-5407 method handle_setup
  Similarity: 82.32%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_task_from_args (lines 2886-2929) ---[0m
    fn create_task_from_args(
        &self,
        description: &str,
        priority: &str,
        task_type: &str,
        details: Option<&str>,
        duration: Option<u32>,
    ) -> Result<Task> {
        let priority = match priority.to_lowercase().as_str() {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            _ => Priority::Medium,
        };

        let task_type = match task_type.to_lowercase().as_str() {
            "development" => TaskType::Development,
            "testing" => TaskType::Testing,
            "infrastructure" => TaskType::Infrastructure,
            "documentation" => TaskType::Documentation,
            "bugfix" => TaskType::Bugfix,
            "feature" => TaskType::Feature,
            "review" => TaskType::Review,
            "coordination" => TaskType::Coordination,
            _ => TaskType::Development,
        };

        let estimated_duration = duration.map(|d| std::time::Duration::from_secs(d as u64));

        let mut task = Task::new(
            uuid::Uuid::new_v4().to_string(),
            description.to_string(),
            priority,
            task_type,
        );

        if let Some(details) = details {
            task = task.with_details(details.to_string());
        }

        task.estimated_duration = estimated_duration.map(|d| d.as_secs() as u32);

        Ok(task)
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_setup (lines 5382-5407) ---[0m
    async fn handle_setup(&self) -> Result<()> {
        // Check if config already exists
        let config_path = self.repo_path.join("ccswarm.json");
        if config_path.exists() {
            println!("{}", "⚠️  Configuration already exists!".bright_yellow());
            println!();
            print!("Overwrite existing configuration? [y/N] ");
            std::io::stdout().flush()?;

            let mut input = String::new();
            std::io::stdin().read_line(&mut input)?;
            if !input.trim().eq_ignore_ascii_case("y") {
                println!("Setup cancelled.");
                return Ok(());
            }
        }

        // Run setup wizard
        let _config = SetupWizard::run().await?;

        // Initialize project
        crate::utils::user_error::show_progress("Initializing project structure...");
        crate::git::shell::ShellWorktreeManager::init_if_needed(&self.repo_path).await?;

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1364-1388 method start_tui <-> ./crates/ccswarm/src/cli/mod.rs:5428-5466 method handle_help
  Similarity: 87.71%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_tui (lines 1364-1388) ---[0m
    async fn start_tui(&self) -> Result<()> {
        info!("Starting ccswarm TUI");

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Starting TUI mode",
                }))?
            );
        } else {
            println!("🖥️  Starting ccswarm TUI...");
            println!("   Press 'q' to quit");
        }

        // Start TUI with execution engine if available
        if let Some(ref execution_engine) = self.execution_engine {
            crate::tui::run_tui_with_engine(execution_engine.clone()).await?;
        } else {
            crate::tui::run_tui().await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_help (lines 5428-5466) ---[0m
    async fn handle_help(&self, topic: Option<&str>, search: Option<&str>) -> Result<()> {
        let help = InteractiveHelp::new();

        if let Some(query) = search {
            // Search help topics
            let results = help.search(query);

            if results.is_empty() {
                println!();
                println!(
                    "{}",
                    "❌ No help topics found matching your search.".bright_red()
                );
                println!();
                println!("Try one of these topics:");
                help.show_topic_list();
            } else {
                println!();
                println!(
                    "{}",
                    format!("🔍 Found {} topics matching '{}'", results.len(), query).bright_cyan()
                );
                println!();

                for (key, topic) in results.iter().take(3) {
                    println!("{}", format!("📖 {}", topic.title).bright_yellow());
                    println!("   {}", topic.description.bright_black());
                    println!("   Run: ccswarm help {}", key.bright_white());
                    println!();
                }
            }
        } else if let Some(t) = topic {
            help.show_topic(t);
        } else {
            help.show_topic_list();
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1173-1209 method new <-> ./crates/ccswarm/src/cli/mod.rs:5382-5407 method handle_setup
  Similarity: 85.74%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:new (lines 1173-1209) ---[0m
    pub async fn new(cli: &Cli) -> Result<Self> {
        // Load configuration
        let config = if cli.config.exists() {
            CcswarmConfig::from_file(cli.config.clone())
                .await
                .context("Failed to load configuration")?
        } else {
            warn!("Configuration file not found, using defaults");
            create_default_config(&cli.repo)?
        };

        let formatter = create_formatter(cli.json);

        // Initialize execution engine for task management
        let execution_engine = match ExecutionEngine::new(&config).await {
            Ok(engine) => {
                if let Err(e) = engine.start().await {
                    warn!("Failed to start execution engine: {}", e);
                    None
                } else {
                    Some(engine)
                }
            }
            Err(e) => {
                warn!("Failed to create execution engine: {}", e);
                None
            }
        };

        Ok(Self {
            config,
            repo_path: cli.repo.clone(),
            json_output: cli.json,
            formatter,
            execution_engine,
        })
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_setup (lines 5382-5407) ---[0m
    async fn handle_setup(&self) -> Result<()> {
        // Check if config already exists
        let config_path = self.repo_path.join("ccswarm.json");
        if config_path.exists() {
            println!("{}", "⚠️  Configuration already exists!".bright_yellow());
            println!();
            print!("Overwrite existing configuration? [y/N] ");
            std::io::stdout().flush()?;

            let mut input = String::new();
            std::io::stdin().read_line(&mut input)?;
            if !input.trim().eq_ignore_ascii_case("y") {
                println!("Setup cancelled.");
                return Ok(());
            }
        }

        // Run setup wizard
        let _config = SetupWizard::run().await?;

        // Initialize project
        crate::utils::user_error::show_progress("Initializing project structure...");
        crate::git::shell::ShellWorktreeManager::init_if_needed(&self.repo_path).await?;

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2240-2258 method list_agents <-> ./crates/ccswarm/src/cli/mod.rs:5428-5466 method handle_help
  Similarity: 91.69%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_agents (lines 2240-2258) ---[0m
    async fn list_agents(&self, _all: bool) -> Result<()> {
        if self.json_output {
            println!("{}", serde_json::to_string_pretty(&self.config.agents)?);
        } else {
            println!("🤖 Configured Agents");
            println!("==================");

            for (name, config) in &self.config.agents {
                println!("Agent: {}", name);
                println!("  Specialization: {}", config.specialization);
                println!("  Worktree: {}", config.worktree);
                println!("  Branch: {}", config.branch);
                println!("  Think Mode: {:?}", config.claude_config.think_mode);
                println!();
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_help (lines 5428-5466) ---[0m
    async fn handle_help(&self, topic: Option<&str>, search: Option<&str>) -> Result<()> {
        let help = InteractiveHelp::new();

        if let Some(query) = search {
            // Search help topics
            let results = help.search(query);

            if results.is_empty() {
                println!();
                println!(
                    "{}",
                    "❌ No help topics found matching your search.".bright_red()
                );
                println!();
                println!("Try one of these topics:");
                help.show_topic_list();
            } else {
                println!();
                println!(
                    "{}",
                    format!("🔍 Found {} topics matching '{}'", results.len(), query).bright_cyan()
                );
                println!();

                for (key, topic) in results.iter().take(3) {
                    println!("{}", format!("📖 {}", topic.title).bright_yellow());
                    println!("   {}", topic.description.bright_black());
                    println!("   Run: ccswarm help {}", key.bright_white());
                    println!();
                }
            }
        } else if let Some(t) = topic {
            help.show_topic(t);
        } else {
            help.show_topic_list();
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1173-1209 method new <-> ./crates/ccswarm/src/cli/mod.rs:1364-1388 method start_tui
  Similarity: 85.33%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:new (lines 1173-1209) ---[0m
    pub async fn new(cli: &Cli) -> Result<Self> {
        // Load configuration
        let config = if cli.config.exists() {
            CcswarmConfig::from_file(cli.config.clone())
                .await
                .context("Failed to load configuration")?
        } else {
            warn!("Configuration file not found, using defaults");
            create_default_config(&cli.repo)?
        };

        let formatter = create_formatter(cli.json);

        // Initialize execution engine for task management
        let execution_engine = match ExecutionEngine::new(&config).await {
            Ok(engine) => {
                if let Err(e) = engine.start().await {
                    warn!("Failed to start execution engine: {}", e);
                    None
                } else {
                    Some(engine)
                }
            }
            Err(e) => {
                warn!("Failed to create execution engine: {}", e);
                None
            }
        };

        Ok(Self {
            config,
            repo_path: cli.repo.clone(),
            json_output: cli.json,
            formatter,
            execution_engine,
        })
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_tui (lines 1364-1388) ---[0m
    async fn start_tui(&self) -> Result<()> {
        info!("Starting ccswarm TUI");

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Starting TUI mode",
                }))?
            );
        } else {
            println!("🖥️  Starting ccswarm TUI...");
            println!("   Press 'q' to quit");
        }

        // Start TUI with execution engine if available
        if let Some(ref execution_engine) = self.execution_engine {
            crate::tui::run_tui_with_engine(execution_engine.clone()).await?;
        } else {
            crate::tui::run_tui().await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1173-1209 method new <-> ./crates/ccswarm/src/cli/mod.rs:2618-2634 function create_frontend_only_config
  Similarity: 92.59%

[36m--- ./crates/ccswarm/src/cli/mod.rs:new (lines 1173-1209) ---[0m
    pub async fn new(cli: &Cli) -> Result<Self> {
        // Load configuration
        let config = if cli.config.exists() {
            CcswarmConfig::from_file(cli.config.clone())
                .await
                .context("Failed to load configuration")?
        } else {
            warn!("Configuration file not found, using defaults");
            create_default_config(&cli.repo)?
        };

        let formatter = create_formatter(cli.json);

        // Initialize execution engine for task management
        let execution_engine = match ExecutionEngine::new(&config).await {
            Ok(engine) => {
                if let Err(e) = engine.start().await {
                    warn!("Failed to start execution engine: {}", e);
                    None
                } else {
                    Some(engine)
                }
            }
            Err(e) => {
                warn!("Failed to create execution engine: {}", e);
                None
            }
        };

        Ok(Self {
            config,
            repo_path: cli.repo.clone(),
            json_output: cli.json,
            formatter,
            execution_engine,
        })
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_frontend_only_config (lines 2618-2634) ---[0m
fn create_frontend_only_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut config = create_minimal_config(repo_path)?;
    config.project.name = "Frontend ccswarm Project".to_string();

    config.agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    Ok(config)
}

  ./crates/ccswarm/src/cli/mod.rs:1364-1388 method start_tui <-> ./crates/ccswarm/src/cli/mod.rs:2886-2929 method create_task_from_args
  Similarity: 71.99%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_tui (lines 1364-1388) ---[0m
    async fn start_tui(&self) -> Result<()> {
        info!("Starting ccswarm TUI");

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Starting TUI mode",
                }))?
            );
        } else {
            println!("🖥️  Starting ccswarm TUI...");
            println!("   Press 'q' to quit");
        }

        // Start TUI with execution engine if available
        if let Some(ref execution_engine) = self.execution_engine {
            crate::tui::run_tui_with_engine(execution_engine.clone()).await?;
        } else {
            crate::tui::run_tui().await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_task_from_args (lines 2886-2929) ---[0m
    fn create_task_from_args(
        &self,
        description: &str,
        priority: &str,
        task_type: &str,
        details: Option<&str>,
        duration: Option<u32>,
    ) -> Result<Task> {
        let priority = match priority.to_lowercase().as_str() {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            _ => Priority::Medium,
        };

        let task_type = match task_type.to_lowercase().as_str() {
            "development" => TaskType::Development,
            "testing" => TaskType::Testing,
            "infrastructure" => TaskType::Infrastructure,
            "documentation" => TaskType::Documentation,
            "bugfix" => TaskType::Bugfix,
            "feature" => TaskType::Feature,
            "review" => TaskType::Review,
            "coordination" => TaskType::Coordination,
            _ => TaskType::Development,
        };

        let estimated_duration = duration.map(|d| std::time::Duration::from_secs(d as u64));

        let mut task = Task::new(
            uuid::Uuid::new_v4().to_string(),
            description.to_string(),
            priority,
            task_type,
        );

        if let Some(details) = details {
            task = task.with_details(details.to_string());
        }

        task.estimated_duration = estimated_duration.map(|d| d.as_secs() as u32);

        Ok(task)
    }

  ./crates/ccswarm/src/cli/mod.rs:1173-1209 method new <-> ./crates/ccswarm/src/cli/mod.rs:5409-5426 method handle_tutorial
  Similarity: 89.85%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:new (lines 1173-1209) ---[0m
    pub async fn new(cli: &Cli) -> Result<Self> {
        // Load configuration
        let config = if cli.config.exists() {
            CcswarmConfig::from_file(cli.config.clone())
                .await
                .context("Failed to load configuration")?
        } else {
            warn!("Configuration file not found, using defaults");
            create_default_config(&cli.repo)?
        };

        let formatter = create_formatter(cli.json);

        // Initialize execution engine for task management
        let execution_engine = match ExecutionEngine::new(&config).await {
            Ok(engine) => {
                if let Err(e) = engine.start().await {
                    warn!("Failed to start execution engine: {}", e);
                    None
                } else {
                    Some(engine)
                }
            }
            Err(e) => {
                warn!("Failed to create execution engine: {}", e);
                None
            }
        };

        Ok(Self {
            config,
            repo_path: cli.repo.clone(),
            json_output: cli.json,
            formatter,
            execution_engine,
        })
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_tutorial (lines 5409-5426) ---[0m
    async fn handle_tutorial(&self, chapter: Option<u8>) -> Result<()> {
        let mut tutorial = InteractiveTutorial::new();

        if let Some(ch) = chapter {
            if !(1..=4).contains(&ch) {
                println!(
                    "{}",
                    "❌ Invalid chapter number. Please choose 1-4.".bright_red()
                );
                return Ok(());
            }
            // Set starting chapter (adjusting for 0-based index)
            tutorial.current_chapter = (ch - 1) as usize;
        }

        tutorial.start().await?;
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:5382-5407 method handle_setup <-> ./crates/ccswarm/src/cli/mod.rs:5428-5466 method handle_help
  Similarity: 74.37%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_setup (lines 5382-5407) ---[0m
    async fn handle_setup(&self) -> Result<()> {
        // Check if config already exists
        let config_path = self.repo_path.join("ccswarm.json");
        if config_path.exists() {
            println!("{}", "⚠️  Configuration already exists!".bright_yellow());
            println!();
            print!("Overwrite existing configuration? [y/N] ");
            std::io::stdout().flush()?;

            let mut input = String::new();
            std::io::stdin().read_line(&mut input)?;
            if !input.trim().eq_ignore_ascii_case("y") {
                println!("Setup cancelled.");
                return Ok(());
            }
        }

        // Run setup wizard
        let _config = SetupWizard::run().await?;

        // Initialize project
        crate::utils::user_error::show_progress("Initializing project structure...");
        crate::git::shell::ShellWorktreeManager::init_if_needed(&self.repo_path).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_help (lines 5428-5466) ---[0m
    async fn handle_help(&self, topic: Option<&str>, search: Option<&str>) -> Result<()> {
        let help = InteractiveHelp::new();

        if let Some(query) = search {
            // Search help topics
            let results = help.search(query);

            if results.is_empty() {
                println!();
                println!(
                    "{}",
                    "❌ No help topics found matching your search.".bright_red()
                );
                println!();
                println!("Try one of these topics:");
                help.show_topic_list();
            } else {
                println!();
                println!(
                    "{}",
                    format!("🔍 Found {} topics matching '{}'", results.len(), query).bright_cyan()
                );
                println!();

                for (key, topic) in results.iter().take(3) {
                    println!("{}", format!("📖 {}", topic.title).bright_yellow());
                    println!("   {}", topic.description.bright_black());
                    println!("   Run: ccswarm help {}", key.bright_white());
                    println!();
                }
            }
        } else if let Some(t) = topic {
            help.show_topic(t);
        } else {
            help.show_topic_list();
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1364-1388 method start_tui <-> ./crates/ccswarm/src/cli/mod.rs:5382-5407 method handle_setup
  Similarity: 75.66%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_tui (lines 1364-1388) ---[0m
    async fn start_tui(&self) -> Result<()> {
        info!("Starting ccswarm TUI");

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Starting TUI mode",
                }))?
            );
        } else {
            println!("🖥️  Starting ccswarm TUI...");
            println!("   Press 'q' to quit");
        }

        // Start TUI with execution engine if available
        if let Some(ref execution_engine) = self.execution_engine {
            crate::tui::run_tui_with_engine(execution_engine.clone()).await?;
        } else {
            crate::tui::run_tui().await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_setup (lines 5382-5407) ---[0m
    async fn handle_setup(&self) -> Result<()> {
        // Check if config already exists
        let config_path = self.repo_path.join("ccswarm.json");
        if config_path.exists() {
            println!("{}", "⚠️  Configuration already exists!".bright_yellow());
            println!();
            print!("Overwrite existing configuration? [y/N] ");
            std::io::stdout().flush()?;

            let mut input = String::new();
            std::io::stdin().read_line(&mut input)?;
            if !input.trim().eq_ignore_ascii_case("y") {
                println!("Setup cancelled.");
                return Ok(());
            }
        }

        // Run setup wizard
        let _config = SetupWizard::run().await?;

        // Initialize project
        crate::utils::user_error::show_progress("Initializing project structure...");
        crate::git::shell::ShellWorktreeManager::init_if_needed(&self.repo_path).await?;

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1364-1388 method start_tui <-> ./crates/ccswarm/src/cli/mod.rs:2460-2478 method show_logs
  Similarity: 85.82%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_tui (lines 1364-1388) ---[0m
    async fn start_tui(&self) -> Result<()> {
        info!("Starting ccswarm TUI");

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Starting TUI mode",
                }))?
            );
        } else {
            println!("🖥️  Starting ccswarm TUI...");
            println!("   Press 'q' to quit");
        }

        // Start TUI with execution engine if available
        if let Some(ref execution_engine) = self.execution_engine {
            crate::tui::run_tui_with_engine(execution_engine.clone()).await?;
        } else {
            crate::tui::run_tui().await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_logs (lines 2460-2478) ---[0m
    async fn show_logs(&self, _follow: bool, _agent: Option<&str>, _lines: usize) -> Result<()> {
        // TODO: Implement log viewing
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Logs displayed",
                    "lines": 0,
                }))?
            );
        } else {
            println!("📝 Logs");
            println!("======");
            println!("No logs available yet");
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1364-1388 method start_tui <-> ./crates/ccswarm/src/cli/mod.rs:2260-2277 method run_review
  Similarity: 86.92%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_tui (lines 1364-1388) ---[0m
    async fn start_tui(&self) -> Result<()> {
        info!("Starting ccswarm TUI");

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Starting TUI mode",
                }))?
            );
        } else {
            println!("🖥️  Starting ccswarm TUI...");
            println!("   Press 'q' to quit");
        }

        // Start TUI with execution engine if available
        if let Some(ref execution_engine) = self.execution_engine {
            crate::tui::run_tui_with_engine(execution_engine.clone()).await?;
        } else {
            crate::tui::run_tui().await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_review (lines 2260-2277) ---[0m
    async fn run_review(&self, _agent: Option<&str>, _strict: bool) -> Result<()> {
        // TODO: Implement quality review
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Quality review completed",
                    "issues": 0,
                }))?
            );
        } else {
            println!("🔍 Quality review completed");
            println!("   No issues found");
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2240-2258 method list_agents <-> ./crates/ccswarm/src/cli/mod.rs:2460-2478 method show_logs
  Similarity: 95.22%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_agents (lines 2240-2258) ---[0m
    async fn list_agents(&self, _all: bool) -> Result<()> {
        if self.json_output {
            println!("{}", serde_json::to_string_pretty(&self.config.agents)?);
        } else {
            println!("🤖 Configured Agents");
            println!("==================");

            for (name, config) in &self.config.agents {
                println!("Agent: {}", name);
                println!("  Specialization: {}", config.specialization);
                println!("  Worktree: {}", config.worktree);
                println!("  Branch: {}", config.branch);
                println!("  Think Mode: {:?}", config.claude_config.think_mode);
                println!();
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_logs (lines 2460-2478) ---[0m
    async fn show_logs(&self, _follow: bool, _agent: Option<&str>, _lines: usize) -> Result<()> {
        // TODO: Implement log viewing
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Logs displayed",
                    "lines": 0,
                }))?
            );
        } else {
            println!("📝 Logs");
            println!("======");
            println!("No logs available yet");
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2260-2277 method run_review <-> ./crates/ccswarm/src/cli/mod.rs:2460-2478 method show_logs
  Similarity: 97.33%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_review (lines 2260-2277) ---[0m
    async fn run_review(&self, _agent: Option<&str>, _strict: bool) -> Result<()> {
        // TODO: Implement quality review
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Quality review completed",
                    "issues": 0,
                }))?
            );
        } else {
            println!("🔍 Quality review completed");
            println!("   No issues found");
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_logs (lines 2460-2478) ---[0m
    async fn show_logs(&self, _follow: bool, _agent: Option<&str>, _lines: usize) -> Result<()> {
        // TODO: Implement log viewing
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Logs displayed",
                    "lines": 0,
                }))?
            );
        } else {
            println!("📝 Logs");
            println!("======");
            println!("No logs available yet");
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1364-1388 method start_tui <-> ./crates/ccswarm/src/cli/mod.rs:2240-2258 method list_agents
  Similarity: 81.52%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_tui (lines 1364-1388) ---[0m
    async fn start_tui(&self) -> Result<()> {
        info!("Starting ccswarm TUI");

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Starting TUI mode",
                }))?
            );
        } else {
            println!("🖥️  Starting ccswarm TUI...");
            println!("   Press 'q' to quit");
        }

        // Start TUI with execution engine if available
        if let Some(ref execution_engine) = self.execution_engine {
            crate::tui::run_tui_with_engine(execution_engine.clone()).await?;
        } else {
            crate::tui::run_tui().await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_agents (lines 2240-2258) ---[0m
    async fn list_agents(&self, _all: bool) -> Result<()> {
        if self.json_output {
            println!("{}", serde_json::to_string_pretty(&self.config.agents)?);
        } else {
            println!("🤖 Configured Agents");
            println!("==================");

            for (name, config) in &self.config.agents {
                println!("Agent: {}", name);
                println!("  Specialization: {}", config.specialization);
                println!("  Worktree: {}", config.worktree);
                println!("  Branch: {}", config.branch);
                println!("  Think Mode: {:?}", config.claude_config.think_mode);
                println!();
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2240-2258 method list_agents <-> ./crates/ccswarm/src/cli/mod.rs:2260-2277 method run_review
  Similarity: 96.30%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_agents (lines 2240-2258) ---[0m
    async fn list_agents(&self, _all: bool) -> Result<()> {
        if self.json_output {
            println!("{}", serde_json::to_string_pretty(&self.config.agents)?);
        } else {
            println!("🤖 Configured Agents");
            println!("==================");

            for (name, config) in &self.config.agents {
                println!("Agent: {}", name);
                println!("  Specialization: {}", config.specialization);
                println!("  Worktree: {}", config.worktree);
                println!("  Branch: {}", config.branch);
                println!("  Think Mode: {:?}", config.claude_config.think_mode);
                println!();
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_review (lines 2260-2277) ---[0m
    async fn run_review(&self, _agent: Option<&str>, _strict: bool) -> Result<()> {
        // TODO: Implement quality review
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Quality review completed",
                    "issues": 0,
                }))?
            );
        } else {
            println!("🔍 Quality review completed");
            println!("   No issues found");
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1364-1388 method start_tui <-> ./crates/ccswarm/src/cli/mod.rs:2618-2634 function create_frontend_only_config
  Similarity: 83.24%

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_tui (lines 1364-1388) ---[0m
    async fn start_tui(&self) -> Result<()> {
        info!("Starting ccswarm TUI");

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Starting TUI mode",
                }))?
            );
        } else {
            println!("🖥️  Starting ccswarm TUI...");
            println!("   Press 'q' to quit");
        }

        // Start TUI with execution engine if available
        if let Some(ref execution_engine) = self.execution_engine {
            crate::tui::run_tui_with_engine(execution_engine.clone()).await?;
        } else {
            crate::tui::run_tui().await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_frontend_only_config (lines 2618-2634) ---[0m
fn create_frontend_only_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut config = create_minimal_config(repo_path)?;
    config.project.name = "Frontend ccswarm Project".to_string();

    config.agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    Ok(config)
}

  ./crates/ccswarm/src/cli/mod.rs:1364-1388 method start_tui <-> ./crates/ccswarm/src/cli/mod.rs:5409-5426 method handle_tutorial
  Similarity: 78.74%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:start_tui (lines 1364-1388) ---[0m
    async fn start_tui(&self) -> Result<()> {
        info!("Starting ccswarm TUI");

        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Starting TUI mode",
                }))?
            );
        } else {
            println!("🖥️  Starting ccswarm TUI...");
            println!("   Press 'q' to quit");
        }

        // Start TUI with execution engine if available
        if let Some(ref execution_engine) = self.execution_engine {
            crate::tui::run_tui_with_engine(execution_engine.clone()).await?;
        } else {
            crate::tui::run_tui().await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_tutorial (lines 5409-5426) ---[0m
    async fn handle_tutorial(&self, chapter: Option<u8>) -> Result<()> {
        let mut tutorial = InteractiveTutorial::new();

        if let Some(ch) = chapter {
            if !(1..=4).contains(&ch) {
                println!(
                    "{}",
                    "❌ Invalid chapter number. Please choose 1-4.".bright_red()
                );
                return Ok(());
            }
            // Set starting chapter (adjusting for 0-based index)
            tutorial.current_chapter = (ch - 1) as usize;
        }

        tutorial.start().await?;
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2618-2634 function create_frontend_only_config <-> ./crates/ccswarm/src/cli/mod.rs:5382-5407 method handle_setup
  Similarity: 78.12%

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_frontend_only_config (lines 2618-2634) ---[0m
fn create_frontend_only_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut config = create_minimal_config(repo_path)?;
    config.project.name = "Frontend ccswarm Project".to_string();

    config.agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    Ok(config)
}

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_setup (lines 5382-5407) ---[0m
    async fn handle_setup(&self) -> Result<()> {
        // Check if config already exists
        let config_path = self.repo_path.join("ccswarm.json");
        if config_path.exists() {
            println!("{}", "⚠️  Configuration already exists!".bright_yellow());
            println!();
            print!("Overwrite existing configuration? [y/N] ");
            std::io::stdout().flush()?;

            let mut input = String::new();
            std::io::stdin().read_line(&mut input)?;
            if !input.trim().eq_ignore_ascii_case("y") {
                println!("Setup cancelled.");
                return Ok(());
            }
        }

        // Run setup wizard
        let _config = SetupWizard::run().await?;

        // Initialize project
        crate::utils::user_error::show_progress("Initializing project structure...");
        crate::git::shell::ShellWorktreeManager::init_if_needed(&self.repo_path).await?;

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2460-2478 method show_logs <-> ./crates/ccswarm/src/cli/mod.rs:2618-2634 function create_frontend_only_config
  Similarity: 88.15%

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_logs (lines 2460-2478) ---[0m
    async fn show_logs(&self, _follow: bool, _agent: Option<&str>, _lines: usize) -> Result<()> {
        // TODO: Implement log viewing
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Logs displayed",
                    "lines": 0,
                }))?
            );
        } else {
            println!("📝 Logs");
            println!("======");
            println!("No logs available yet");
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_frontend_only_config (lines 2618-2634) ---[0m
fn create_frontend_only_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut config = create_minimal_config(repo_path)?;
    config.project.name = "Frontend ccswarm Project".to_string();

    config.agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    Ok(config)
}

  ./crates/ccswarm/src/cli/mod.rs:1390-1404 method stop_orchestrator <-> ./crates/ccswarm/src/cli/mod.rs:2460-2478 method show_logs
  Similarity: 93.08%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:stop_orchestrator (lines 1390-1404) ---[0m
    async fn stop_orchestrator(&self) -> Result<()> {
        // TODO: Implement graceful shutdown via signal/file
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Stop signal sent",
                }))?
            );
        } else {
            println!("🛑 ccswarm orchestrator stop signal sent");
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_logs (lines 2460-2478) ---[0m
    async fn show_logs(&self, _follow: bool, _agent: Option<&str>, _lines: usize) -> Result<()> {
        // TODO: Implement log viewing
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Logs displayed",
                    "lines": 0,
                }))?
            );
        } else {
            println!("📝 Logs");
            println!("======");
            println!("No logs available yet");
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1390-1404 method stop_orchestrator <-> ./crates/ccswarm/src/cli/mod.rs:2260-2277 method run_review
  Similarity: 94.17%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:stop_orchestrator (lines 1390-1404) ---[0m
    async fn stop_orchestrator(&self) -> Result<()> {
        // TODO: Implement graceful shutdown via signal/file
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Stop signal sent",
                }))?
            );
        } else {
            println!("🛑 ccswarm orchestrator stop signal sent");
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_review (lines 2260-2277) ---[0m
    async fn run_review(&self, _agent: Option<&str>, _strict: bool) -> Result<()> {
        // TODO: Implement quality review
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Quality review completed",
                    "issues": 0,
                }))?
            );
        } else {
            println!("🔍 Quality review completed");
            println!("   No issues found");
        }

        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2260-2277 method run_review <-> ./crates/ccswarm/src/cli/mod.rs:2618-2634 function create_frontend_only_config
  Similarity: 88.15%

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_review (lines 2260-2277) ---[0m
    async fn run_review(&self, _agent: Option<&str>, _strict: bool) -> Result<()> {
        // TODO: Implement quality review
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Quality review completed",
                    "issues": 0,
                }))?
            );
        } else {
            println!("🔍 Quality review completed");
            println!("   No issues found");
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_frontend_only_config (lines 2618-2634) ---[0m
fn create_frontend_only_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut config = create_minimal_config(repo_path)?;
    config.project.name = "Frontend ccswarm Project".to_string();

    config.agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    Ok(config)
}

  ./crates/ccswarm/src/cli/mod.rs:2618-2634 function create_frontend_only_config <-> ./crates/ccswarm/src/cli/mod.rs:5409-5426 method handle_tutorial
  Similarity: 86.11%

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_frontend_only_config (lines 2618-2634) ---[0m
fn create_frontend_only_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut config = create_minimal_config(repo_path)?;
    config.project.name = "Frontend ccswarm Project".to_string();

    config.agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    Ok(config)
}

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_tutorial (lines 5409-5426) ---[0m
    async fn handle_tutorial(&self, chapter: Option<u8>) -> Result<()> {
        let mut tutorial = InteractiveTutorial::new();

        if let Some(ch) = chapter {
            if !(1..=4).contains(&ch) {
                println!(
                    "{}",
                    "❌ Invalid chapter number. Please choose 1-4.".bright_red()
                );
                return Ok(());
            }
            // Set starting chapter (adjusting for 0-based index)
            tutorial.current_chapter = (ch - 1) as usize;
        }

        tutorial.start().await?;
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:3528-3543 method handle_resource <-> ./crates/ccswarm/src/cli/mod.rs:5409-5426 method handle_tutorial
  Similarity: 88.08%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_resource (lines 3528-3543) ---[0m
    async fn handle_resource(&self, action: &resource_commands::ResourceSubcommand) -> Result<()> {
        // Get or create session manager
        let session_manager = Arc::new(
            crate::session::SessionManager::with_resource_monitoring(
                crate::resource::ResourceLimits::default(),
            )
            .await?,
        );

        // Create resource command and execute
        let resource_cmd = resource_commands::ResourceCommand {
            subcommand: action.clone(),
        };

        resource_cmd.execute(session_manager).await
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_tutorial (lines 5409-5426) ---[0m
    async fn handle_tutorial(&self, chapter: Option<u8>) -> Result<()> {
        let mut tutorial = InteractiveTutorial::new();

        if let Some(ch) = chapter {
            if !(1..=4).contains(&ch) {
                println!(
                    "{}",
                    "❌ Invalid chapter number. Please choose 1-4.".bright_red()
                );
                return Ok(());
            }
            // Set starting chapter (adjusting for 0-based index)
            tutorial.current_chapter = (ch - 1) as usize;
        }

        tutorial.start().await?;
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2240-2258 method list_agents <-> ./crates/ccswarm/src/cli/mod.rs:2618-2634 function create_frontend_only_config
  Similarity: 83.04%

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_agents (lines 2240-2258) ---[0m
    async fn list_agents(&self, _all: bool) -> Result<()> {
        if self.json_output {
            println!("{}", serde_json::to_string_pretty(&self.config.agents)?);
        } else {
            println!("🤖 Configured Agents");
            println!("==================");

            for (name, config) in &self.config.agents {
                println!("Agent: {}", name);
                println!("  Specialization: {}", config.specialization);
                println!("  Worktree: {}", config.worktree);
                println!("  Branch: {}", config.branch);
                println!("  Think Mode: {:?}", config.claude_config.think_mode);
                println!();
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_frontend_only_config (lines 2618-2634) ---[0m
fn create_frontend_only_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut config = create_minimal_config(repo_path)?;
    config.project.name = "Frontend ccswarm Project".to_string();

    config.agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    Ok(config)
}

  ./crates/ccswarm/src/cli/mod.rs:2460-2478 method show_logs <-> ./crates/ccswarm/src/cli/mod.rs:5409-5426 method handle_tutorial
  Similarity: 80.62%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_logs (lines 2460-2478) ---[0m
    async fn show_logs(&self, _follow: bool, _agent: Option<&str>, _lines: usize) -> Result<()> {
        // TODO: Implement log viewing
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Logs displayed",
                    "lines": 0,
                }))?
            );
        } else {
            println!("📝 Logs");
            println!("======");
            println!("No logs available yet");
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_tutorial (lines 5409-5426) ---[0m
    async fn handle_tutorial(&self, chapter: Option<u8>) -> Result<()> {
        let mut tutorial = InteractiveTutorial::new();

        if let Some(ch) = chapter {
            if !(1..=4).contains(&ch) {
                println!(
                    "{}",
                    "❌ Invalid chapter number. Please choose 1-4.".bright_red()
                );
                return Ok(());
            }
            // Set starting chapter (adjusting for 0-based index)
            tutorial.current_chapter = (ch - 1) as usize;
        }

        tutorial.start().await?;
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2618-2634 function create_frontend_only_config <-> ./crates/ccswarm/src/cli/mod.rs:3528-3543 method handle_resource
  Similarity: 90.00%

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_frontend_only_config (lines 2618-2634) ---[0m
fn create_frontend_only_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut config = create_minimal_config(repo_path)?;
    config.project.name = "Frontend ccswarm Project".to_string();

    config.agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    Ok(config)
}

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_resource (lines 3528-3543) ---[0m
    async fn handle_resource(&self, action: &resource_commands::ResourceSubcommand) -> Result<()> {
        // Get or create session manager
        let session_manager = Arc::new(
            crate::session::SessionManager::with_resource_monitoring(
                crate::resource::ResourceLimits::default(),
            )
            .await?,
        );

        // Create resource command and execute
        let resource_cmd = resource_commands::ResourceCommand {
            subcommand: action.clone(),
        };

        resource_cmd.execute(session_manager).await
    }

  ./crates/ccswarm/src/cli/mod.rs:2260-2277 method run_review <-> ./crates/ccswarm/src/cli/mod.rs:5409-5426 method handle_tutorial
  Similarity: 80.62%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_review (lines 2260-2277) ---[0m
    async fn run_review(&self, _agent: Option<&str>, _strict: bool) -> Result<()> {
        // TODO: Implement quality review
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Quality review completed",
                    "issues": 0,
                }))?
            );
        } else {
            println!("🔍 Quality review completed");
            println!("   No issues found");
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_tutorial (lines 5409-5426) ---[0m
    async fn handle_tutorial(&self, chapter: Option<u8>) -> Result<()> {
        let mut tutorial = InteractiveTutorial::new();

        if let Some(ch) = chapter {
            if !(1..=4).contains(&ch) {
                println!(
                    "{}",
                    "❌ Invalid chapter number. Please choose 1-4.".bright_red()
                );
                return Ok(());
            }
            // Set starting chapter (adjusting for 0-based index)
            tutorial.current_chapter = (ch - 1) as usize;
        }

        tutorial.start().await?;
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2240-2258 method list_agents <-> ./crates/ccswarm/src/cli/mod.rs:5409-5426 method handle_tutorial
  Similarity: 77.07%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:list_agents (lines 2240-2258) ---[0m
    async fn list_agents(&self, _all: bool) -> Result<()> {
        if self.json_output {
            println!("{}", serde_json::to_string_pretty(&self.config.agents)?);
        } else {
            println!("🤖 Configured Agents");
            println!("==================");

            for (name, config) in &self.config.agents {
                println!("Agent: {}", name);
                println!("  Specialization: {}", config.specialization);
                println!("  Worktree: {}", config.worktree);
                println!("  Branch: {}", config.branch);
                println!("  Think Mode: {:?}", config.claude_config.think_mode);
                println!();
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_tutorial (lines 5409-5426) ---[0m
    async fn handle_tutorial(&self, chapter: Option<u8>) -> Result<()> {
        let mut tutorial = InteractiveTutorial::new();

        if let Some(ch) = chapter {
            if !(1..=4).contains(&ch) {
                println!(
                    "{}",
                    "❌ Invalid chapter number. Please choose 1-4.".bright_red()
                );
                return Ok(());
            }
            // Set starting chapter (adjusting for 0-based index)
            tutorial.current_chapter = (ch - 1) as usize;
        }

        tutorial.start().await?;
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:2460-2478 method show_logs <-> ./crates/ccswarm/src/cli/mod.rs:3528-3543 method handle_resource
  Similarity: 81.17%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:show_logs (lines 2460-2478) ---[0m
    async fn show_logs(&self, _follow: bool, _agent: Option<&str>, _lines: usize) -> Result<()> {
        // TODO: Implement log viewing
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Logs displayed",
                    "lines": 0,
                }))?
            );
        } else {
            println!("📝 Logs");
            println!("======");
            println!("No logs available yet");
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_resource (lines 3528-3543) ---[0m
    async fn handle_resource(&self, action: &resource_commands::ResourceSubcommand) -> Result<()> {
        // Get or create session manager
        let session_manager = Arc::new(
            crate::session::SessionManager::with_resource_monitoring(
                crate::resource::ResourceLimits::default(),
            )
            .await?,
        );

        // Create resource command and execute
        let resource_cmd = resource_commands::ResourceCommand {
            subcommand: action.clone(),
        };

        resource_cmd.execute(session_manager).await
    }

  ./crates/ccswarm/src/cli/mod.rs:1390-1404 method stop_orchestrator <-> ./crates/ccswarm/src/cli/mod.rs:2618-2634 function create_frontend_only_config
  Similarity: 88.40%

[36m--- ./crates/ccswarm/src/cli/mod.rs:stop_orchestrator (lines 1390-1404) ---[0m
    async fn stop_orchestrator(&self) -> Result<()> {
        // TODO: Implement graceful shutdown via signal/file
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Stop signal sent",
                }))?
            );
        } else {
            println!("🛑 ccswarm orchestrator stop signal sent");
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_frontend_only_config (lines 2618-2634) ---[0m
fn create_frontend_only_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut config = create_minimal_config(repo_path)?;
    config.project.name = "Frontend ccswarm Project".to_string();

    config.agents.insert(
        "frontend".to_string(),
        crate::config::AgentConfig {
            specialization: "react_typescript".to_string(),
            worktree: "agents/frontend-agent".to_string(),
            branch: "feature/frontend".to_string(),
            claude_config: crate::config::ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );

    Ok(config)
}

  ./crates/ccswarm/src/cli/mod.rs:2260-2277 method run_review <-> ./crates/ccswarm/src/cli/mod.rs:3528-3543 method handle_resource
  Similarity: 79.07%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_review (lines 2260-2277) ---[0m
    async fn run_review(&self, _agent: Option<&str>, _strict: bool) -> Result<()> {
        // TODO: Implement quality review
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Quality review completed",
                    "issues": 0,
                }))?
            );
        } else {
            println!("🔍 Quality review completed");
            println!("   No issues found");
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_resource (lines 3528-3543) ---[0m
    async fn handle_resource(&self, action: &resource_commands::ResourceSubcommand) -> Result<()> {
        // Get or create session manager
        let session_manager = Arc::new(
            crate::session::SessionManager::with_resource_monitoring(
                crate::resource::ResourceLimits::default(),
            )
            .await?,
        );

        // Create resource command and execute
        let resource_cmd = resource_commands::ResourceCommand {
            subcommand: action.clone(),
        };

        resource_cmd.execute(session_manager).await
    }

  ./crates/ccswarm/src/cli/mod.rs:1390-1404 method stop_orchestrator <-> ./crates/ccswarm/src/cli/mod.rs:5409-5426 method handle_tutorial
  Similarity: 81.15%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:stop_orchestrator (lines 1390-1404) ---[0m
    async fn stop_orchestrator(&self) -> Result<()> {
        // TODO: Implement graceful shutdown via signal/file
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Stop signal sent",
                }))?
            );
        } else {
            println!("🛑 ccswarm orchestrator stop signal sent");
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_tutorial (lines 5409-5426) ---[0m
    async fn handle_tutorial(&self, chapter: Option<u8>) -> Result<()> {
        let mut tutorial = InteractiveTutorial::new();

        if let Some(ch) = chapter {
            if !(1..=4).contains(&ch) {
                println!(
                    "{}",
                    "❌ Invalid chapter number. Please choose 1-4.".bright_red()
                );
                return Ok(());
            }
            // Set starting chapter (adjusting for 0-based index)
            tutorial.current_chapter = (ch - 1) as usize;
        }

        tutorial.start().await?;
        Ok(())
    }

  ./crates/ccswarm/src/cli/mod.rs:1390-1404 method stop_orchestrator <-> ./crates/ccswarm/src/cli/mod.rs:3528-3543 method handle_resource
  Similarity: 74.89%
  Classes: CliRunner <-> CliRunner

[36m--- ./crates/ccswarm/src/cli/mod.rs:stop_orchestrator (lines 1390-1404) ---[0m
    async fn stop_orchestrator(&self) -> Result<()> {
        // TODO: Implement graceful shutdown via signal/file
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Stop signal sent",
                }))?
            );
        } else {
            println!("🛑 ccswarm orchestrator stop signal sent");
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_resource (lines 3528-3543) ---[0m
    async fn handle_resource(&self, action: &resource_commands::ResourceSubcommand) -> Result<()> {
        // Get or create session manager
        let session_manager = Arc::new(
            crate::session::SessionManager::with_resource_monitoring(
                crate::resource::ResourceLimits::default(),
            )
            .await?,
        );

        // Create resource command and execute
        let resource_cmd = resource_commands::ResourceCommand {
            subcommand: action.clone(),
        };

        resource_cmd.execute(session_manager).await
    }

  ./crates/ccswarm/src/cli/mod.rs:2260-2277 method run_review <-> ./crates/ccswarm/src/cli/mod.rs:2611-2616 function create_minimal_config
  Similarity: 80.74%

[36m--- ./crates/ccswarm/src/cli/mod.rs:run_review (lines 2260-2277) ---[0m
    async fn run_review(&self, _agent: Option<&str>, _strict: bool) -> Result<()> {
        // TODO: Implement quality review
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Quality review completed",
                    "issues": 0,
                }))?
            );
        } else {
            println!("🔍 Quality review completed");
            println!("   No issues found");
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_minimal_config (lines 2611-2616) ---[0m
fn create_minimal_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut config = create_default_config(repo_path)?;
    config.agents.clear();
    config.project.name = "Minimal ccswarm Project".to_string();
    Ok(config)
}

  ./crates/ccswarm/src/cli/mod.rs:2611-2616 function create_minimal_config <-> ./crates/ccswarm/src/cli/mod.rs:3528-3543 method handle_resource
  Similarity: 82.00%

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_minimal_config (lines 2611-2616) ---[0m
fn create_minimal_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut config = create_default_config(repo_path)?;
    config.agents.clear();
    config.project.name = "Minimal ccswarm Project".to_string();
    Ok(config)
}

[36m--- ./crates/ccswarm/src/cli/mod.rs:handle_resource (lines 3528-3543) ---[0m
    async fn handle_resource(&self, action: &resource_commands::ResourceSubcommand) -> Result<()> {
        // Get or create session manager
        let session_manager = Arc::new(
            crate::session::SessionManager::with_resource_monitoring(
                crate::resource::ResourceLimits::default(),
            )
            .await?,
        );

        // Create resource command and execute
        let resource_cmd = resource_commands::ResourceCommand {
            subcommand: action.clone(),
        };

        resource_cmd.execute(session_manager).await
    }

  ./crates/ccswarm/src/cli/mod.rs:1390-1404 method stop_orchestrator <-> ./crates/ccswarm/src/cli/mod.rs:2611-2616 function create_minimal_config
  Similarity: 76.63%

[36m--- ./crates/ccswarm/src/cli/mod.rs:stop_orchestrator (lines 1390-1404) ---[0m
    async fn stop_orchestrator(&self) -> Result<()> {
        // TODO: Implement graceful shutdown via signal/file
        if self.json_output {
            println!(
                "{}",
                serde_json::to_string_pretty(&serde_json::json!({
                    "status": "success",
                    "message": "Stop signal sent",
                }))?
            );
        } else {
            println!("🛑 ccswarm orchestrator stop signal sent");
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/mod.rs:create_minimal_config (lines 2611-2616) ---[0m
fn create_minimal_config(repo_path: &Path) -> Result<CcswarmConfig> {
    let mut config = create_default_config(repo_path)?;
    config.agents.clear();
    config.project.name = "Minimal ccswarm Project".to_string();
    Ok(config)
}


Duplicates in ./crates/ccswarm/src/utils/user_error.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/utils/user_error.rs:78-84 method invalid_agent_type <-> ./crates/ccswarm/src/utils/user_error.rs:94-100 method port_in_use
  Similarity: 97.33%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:invalid_agent_type (lines 78-84) ---[0m
    pub fn invalid_agent_type(agent_type: &str) -> UserError {
        UserError::new(
            format!("Invalid agent type: {}", agent_type),
            "Supported agent types are: frontend, backend, devops, qa",
        )
        .with_suggestion("Try 'ccswarm agents spawn --type frontend'".to_string())
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:port_in_use (lines 94-100) ---[0m
    pub fn port_in_use(port: u16) -> UserError {
        UserError::new(
            format!("Port {} is already in use", port),
            "Another process is using this port",
        )
        .with_suggestion("Try a different port with --port option".to_string())
    }

  ./crates/ccswarm/src/utils/user_error.rs:62-68 method git_not_initialized <-> ./crates/ccswarm/src/utils/user_error.rs:70-76 method config_not_found
  Similarity: 97.00%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:git_not_initialized (lines 62-68) ---[0m
    pub fn git_not_initialized() -> UserError {
        UserError::new(
            "Git repository not initialized",
            "ccswarm requires a Git repository to manage agent worktrees",
        )
        .with_suggestion("Run 'git init' in your project directory first")
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:config_not_found (lines 70-76) ---[0m
    pub fn config_not_found() -> UserError {
        UserError::new(
            "Configuration file not found",
            "ccswarm.json is required to run this command",
        )
        .with_suggestion("Run 'ccswarm init' to create a configuration file")
    }

  ./crates/ccswarm/src/utils/user_error.rs:62-68 method git_not_initialized <-> ./crates/ccswarm/src/utils/user_error.rs:102-108 method invalid_task_format
  Similarity: 97.00%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:git_not_initialized (lines 62-68) ---[0m
    pub fn git_not_initialized() -> UserError {
        UserError::new(
            "Git repository not initialized",
            "ccswarm requires a Git repository to manage agent worktrees",
        )
        .with_suggestion("Run 'git init' in your project directory first")
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:invalid_task_format (lines 102-108) ---[0m
    pub fn invalid_task_format() -> UserError {
        UserError::new(
            "Invalid task format",
            "The task format is not valid",
        )
        .with_suggestion("Use format: 'task description [priority] [type]'")
    }

  ./crates/ccswarm/src/utils/user_error.rs:70-76 method config_not_found <-> ./crates/ccswarm/src/utils/user_error.rs:102-108 method invalid_task_format
  Similarity: 97.00%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:config_not_found (lines 70-76) ---[0m
    pub fn config_not_found() -> UserError {
        UserError::new(
            "Configuration file not found",
            "ccswarm.json is required to run this command",
        )
        .with_suggestion("Run 'ccswarm init' to create a configuration file")
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:invalid_task_format (lines 102-108) ---[0m
    pub fn invalid_task_format() -> UserError {
        UserError::new(
            "Invalid task format",
            "The task format is not valid",
        )
        .with_suggestion("Use format: 'task description [priority] [type]'")
    }

  ./crates/ccswarm/src/utils/user_error.rs:62-68 method git_not_initialized <-> ./crates/ccswarm/src/utils/user_error.rs:86-92 method session_not_found
  Similarity: 93.42%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:git_not_initialized (lines 62-68) ---[0m
    pub fn git_not_initialized() -> UserError {
        UserError::new(
            "Git repository not initialized",
            "ccswarm requires a Git repository to manage agent worktrees",
        )
        .with_suggestion("Run 'git init' in your project directory first")
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:session_not_found (lines 86-92) ---[0m
    pub fn session_not_found(session_id: &str) -> UserError {
        UserError::new(
            format!("Session not found: {}", session_id),
            "The specified session does not exist or has been terminated",
        )
        .with_suggestion("Run 'ccswarm session list' to see active sessions")
    }

  ./crates/ccswarm/src/utils/user_error.rs:70-76 method config_not_found <-> ./crates/ccswarm/src/utils/user_error.rs:86-92 method session_not_found
  Similarity: 93.42%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:config_not_found (lines 70-76) ---[0m
    pub fn config_not_found() -> UserError {
        UserError::new(
            "Configuration file not found",
            "ccswarm.json is required to run this command",
        )
        .with_suggestion("Run 'ccswarm init' to create a configuration file")
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:session_not_found (lines 86-92) ---[0m
    pub fn session_not_found(session_id: &str) -> UserError {
        UserError::new(
            format!("Session not found: {}", session_id),
            "The specified session does not exist or has been terminated",
        )
        .with_suggestion("Run 'ccswarm session list' to see active sessions")
    }

  ./crates/ccswarm/src/utils/user_error.rs:86-92 method session_not_found <-> ./crates/ccswarm/src/utils/user_error.rs:102-108 method invalid_task_format
  Similarity: 93.42%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:session_not_found (lines 86-92) ---[0m
    pub fn session_not_found(session_id: &str) -> UserError {
        UserError::new(
            format!("Session not found: {}", session_id),
            "The specified session does not exist or has been terminated",
        )
        .with_suggestion("Run 'ccswarm session list' to see active sessions")
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:invalid_task_format (lines 102-108) ---[0m
    pub fn invalid_task_format() -> UserError {
        UserError::new(
            "Invalid task format",
            "The task format is not valid",
        )
        .with_suggestion("Use format: 'task description [priority] [type]'")
    }

  ./crates/ccswarm/src/utils/user_error.rs:86-92 method session_not_found <-> ./crates/ccswarm/src/utils/user_error.rs:110-116 method api_key_missing
  Similarity: 91.85%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:session_not_found (lines 86-92) ---[0m
    pub fn session_not_found(session_id: &str) -> UserError {
        UserError::new(
            format!("Session not found: {}", session_id),
            "The specified session does not exist or has been terminated",
        )
        .with_suggestion("Run 'ccswarm session list' to see active sessions")
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:api_key_missing (lines 110-116) ---[0m
    pub fn api_key_missing(provider: &str) -> UserError {
        UserError::new(
            format!("API key missing for {}", provider),
            format!("The {} API key is required but not found", provider),
        )
        .with_suggestion(format!("Set the environment variable for {} API key", provider))
    }

  ./crates/ccswarm/src/utils/user_error.rs:78-84 method invalid_agent_type <-> ./crates/ccswarm/src/utils/user_error.rs:86-92 method session_not_found
  Similarity: 90.67%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:invalid_agent_type (lines 78-84) ---[0m
    pub fn invalid_agent_type(agent_type: &str) -> UserError {
        UserError::new(
            format!("Invalid agent type: {}", agent_type),
            "Supported agent types are: frontend, backend, devops, qa",
        )
        .with_suggestion("Try 'ccswarm agents spawn --type frontend'".to_string())
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:session_not_found (lines 86-92) ---[0m
    pub fn session_not_found(session_id: &str) -> UserError {
        UserError::new(
            format!("Session not found: {}", session_id),
            "The specified session does not exist or has been terminated",
        )
        .with_suggestion("Run 'ccswarm session list' to see active sessions")
    }

  ./crates/ccswarm/src/utils/user_error.rs:86-92 method session_not_found <-> ./crates/ccswarm/src/utils/user_error.rs:94-100 method port_in_use
  Similarity: 90.67%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:session_not_found (lines 86-92) ---[0m
    pub fn session_not_found(session_id: &str) -> UserError {
        UserError::new(
            format!("Session not found: {}", session_id),
            "The specified session does not exist or has been terminated",
        )
        .with_suggestion("Run 'ccswarm session list' to see active sessions")
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:port_in_use (lines 94-100) ---[0m
    pub fn port_in_use(port: u16) -> UserError {
        UserError::new(
            format!("Port {} is already in use", port),
            "Another process is using this port",
        )
        .with_suggestion("Try a different port with --port option".to_string())
    }

  ./crates/ccswarm/src/utils/user_error.rs:62-68 method git_not_initialized <-> ./crates/ccswarm/src/utils/user_error.rs:110-116 method api_key_missing
  Similarity: 89.44%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:git_not_initialized (lines 62-68) ---[0m
    pub fn git_not_initialized() -> UserError {
        UserError::new(
            "Git repository not initialized",
            "ccswarm requires a Git repository to manage agent worktrees",
        )
        .with_suggestion("Run 'git init' in your project directory first")
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:api_key_missing (lines 110-116) ---[0m
    pub fn api_key_missing(provider: &str) -> UserError {
        UserError::new(
            format!("API key missing for {}", provider),
            format!("The {} API key is required but not found", provider),
        )
        .with_suggestion(format!("Set the environment variable for {} API key", provider))
    }

  ./crates/ccswarm/src/utils/user_error.rs:70-76 method config_not_found <-> ./crates/ccswarm/src/utils/user_error.rs:110-116 method api_key_missing
  Similarity: 89.44%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:config_not_found (lines 70-76) ---[0m
    pub fn config_not_found() -> UserError {
        UserError::new(
            "Configuration file not found",
            "ccswarm.json is required to run this command",
        )
        .with_suggestion("Run 'ccswarm init' to create a configuration file")
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:api_key_missing (lines 110-116) ---[0m
    pub fn api_key_missing(provider: &str) -> UserError {
        UserError::new(
            format!("API key missing for {}", provider),
            format!("The {} API key is required but not found", provider),
        )
        .with_suggestion(format!("Set the environment variable for {} API key", provider))
    }

  ./crates/ccswarm/src/utils/user_error.rs:102-108 method invalid_task_format <-> ./crates/ccswarm/src/utils/user_error.rs:110-116 method api_key_missing
  Similarity: 89.44%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:invalid_task_format (lines 102-108) ---[0m
    pub fn invalid_task_format() -> UserError {
        UserError::new(
            "Invalid task format",
            "The task format is not valid",
        )
        .with_suggestion("Use format: 'task description [priority] [type]'")
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:api_key_missing (lines 110-116) ---[0m
    pub fn api_key_missing(provider: &str) -> UserError {
        UserError::new(
            format!("API key missing for {}", provider),
            format!("The {} API key is required but not found", provider),
        )
        .with_suggestion(format!("Set the environment variable for {} API key", provider))
    }

  ./crates/ccswarm/src/utils/user_error.rs:78-84 method invalid_agent_type <-> ./crates/ccswarm/src/utils/user_error.rs:110-116 method api_key_missing
  Similarity: 87.96%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:invalid_agent_type (lines 78-84) ---[0m
    pub fn invalid_agent_type(agent_type: &str) -> UserError {
        UserError::new(
            format!("Invalid agent type: {}", agent_type),
            "Supported agent types are: frontend, backend, devops, qa",
        )
        .with_suggestion("Try 'ccswarm agents spawn --type frontend'".to_string())
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:api_key_missing (lines 110-116) ---[0m
    pub fn api_key_missing(provider: &str) -> UserError {
        UserError::new(
            format!("API key missing for {}", provider),
            format!("The {} API key is required but not found", provider),
        )
        .with_suggestion(format!("Set the environment variable for {} API key", provider))
    }

  ./crates/ccswarm/src/utils/user_error.rs:94-100 method port_in_use <-> ./crates/ccswarm/src/utils/user_error.rs:110-116 method api_key_missing
  Similarity: 87.96%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:port_in_use (lines 94-100) ---[0m
    pub fn port_in_use(port: u16) -> UserError {
        UserError::new(
            format!("Port {} is already in use", port),
            "Another process is using this port",
        )
        .with_suggestion("Try a different port with --port option".to_string())
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:api_key_missing (lines 110-116) ---[0m
    pub fn api_key_missing(provider: &str) -> UserError {
        UserError::new(
            format!("API key missing for {}", provider),
            format!("The {} API key is required but not found", provider),
        )
        .with_suggestion(format!("Set the environment variable for {} API key", provider))
    }

  ./crates/ccswarm/src/utils/user_error.rs:62-68 method git_not_initialized <-> ./crates/ccswarm/src/utils/user_error.rs:78-84 method invalid_agent_type
  Similarity: 87.78%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:git_not_initialized (lines 62-68) ---[0m
    pub fn git_not_initialized() -> UserError {
        UserError::new(
            "Git repository not initialized",
            "ccswarm requires a Git repository to manage agent worktrees",
        )
        .with_suggestion("Run 'git init' in your project directory first")
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:invalid_agent_type (lines 78-84) ---[0m
    pub fn invalid_agent_type(agent_type: &str) -> UserError {
        UserError::new(
            format!("Invalid agent type: {}", agent_type),
            "Supported agent types are: frontend, backend, devops, qa",
        )
        .with_suggestion("Try 'ccswarm agents spawn --type frontend'".to_string())
    }

  ./crates/ccswarm/src/utils/user_error.rs:62-68 method git_not_initialized <-> ./crates/ccswarm/src/utils/user_error.rs:94-100 method port_in_use
  Similarity: 87.78%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:git_not_initialized (lines 62-68) ---[0m
    pub fn git_not_initialized() -> UserError {
        UserError::new(
            "Git repository not initialized",
            "ccswarm requires a Git repository to manage agent worktrees",
        )
        .with_suggestion("Run 'git init' in your project directory first")
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:port_in_use (lines 94-100) ---[0m
    pub fn port_in_use(port: u16) -> UserError {
        UserError::new(
            format!("Port {} is already in use", port),
            "Another process is using this port",
        )
        .with_suggestion("Try a different port with --port option".to_string())
    }

  ./crates/ccswarm/src/utils/user_error.rs:70-76 method config_not_found <-> ./crates/ccswarm/src/utils/user_error.rs:78-84 method invalid_agent_type
  Similarity: 87.78%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:config_not_found (lines 70-76) ---[0m
    pub fn config_not_found() -> UserError {
        UserError::new(
            "Configuration file not found",
            "ccswarm.json is required to run this command",
        )
        .with_suggestion("Run 'ccswarm init' to create a configuration file")
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:invalid_agent_type (lines 78-84) ---[0m
    pub fn invalid_agent_type(agent_type: &str) -> UserError {
        UserError::new(
            format!("Invalid agent type: {}", agent_type),
            "Supported agent types are: frontend, backend, devops, qa",
        )
        .with_suggestion("Try 'ccswarm agents spawn --type frontend'".to_string())
    }

  ./crates/ccswarm/src/utils/user_error.rs:70-76 method config_not_found <-> ./crates/ccswarm/src/utils/user_error.rs:94-100 method port_in_use
  Similarity: 87.78%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:config_not_found (lines 70-76) ---[0m
    pub fn config_not_found() -> UserError {
        UserError::new(
            "Configuration file not found",
            "ccswarm.json is required to run this command",
        )
        .with_suggestion("Run 'ccswarm init' to create a configuration file")
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:port_in_use (lines 94-100) ---[0m
    pub fn port_in_use(port: u16) -> UserError {
        UserError::new(
            format!("Port {} is already in use", port),
            "Another process is using this port",
        )
        .with_suggestion("Try a different port with --port option".to_string())
    }

  ./crates/ccswarm/src/utils/user_error.rs:78-84 method invalid_agent_type <-> ./crates/ccswarm/src/utils/user_error.rs:102-108 method invalid_task_format
  Similarity: 87.78%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:invalid_agent_type (lines 78-84) ---[0m
    pub fn invalid_agent_type(agent_type: &str) -> UserError {
        UserError::new(
            format!("Invalid agent type: {}", agent_type),
            "Supported agent types are: frontend, backend, devops, qa",
        )
        .with_suggestion("Try 'ccswarm agents spawn --type frontend'".to_string())
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:invalid_task_format (lines 102-108) ---[0m
    pub fn invalid_task_format() -> UserError {
        UserError::new(
            "Invalid task format",
            "The task format is not valid",
        )
        .with_suggestion("Use format: 'task description [priority] [type]'")
    }

  ./crates/ccswarm/src/utils/user_error.rs:94-100 method port_in_use <-> ./crates/ccswarm/src/utils/user_error.rs:102-108 method invalid_task_format
  Similarity: 87.78%
  Classes: CommonErrors <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:port_in_use (lines 94-100) ---[0m
    pub fn port_in_use(port: u16) -> UserError {
        UserError::new(
            format!("Port {} is already in use", port),
            "Another process is using this port",
        )
        .with_suggestion("Try a different port with --port option".to_string())
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:invalid_task_format (lines 102-108) ---[0m
    pub fn invalid_task_format() -> UserError {
        UserError::new(
            "Invalid task format",
            "The task format is not valid",
        )
        .with_suggestion("Use format: 'task description [priority] [type]'")
    }

  ./crates/ccswarm/src/utils/user_error.rs:14-21 method new <-> ./crates/ccswarm/src/utils/user_error.rs:62-68 method git_not_initialized
  Similarity: 79.25%
  Classes: UserError <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:new (lines 14-21) ---[0m
    pub fn new(title: impl Into<String>, message: impl Into<String>) -> Self {
        Self {
            title: title.into(),
            message: message.into(),
            suggestion: None,
            details: None,
        }
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:git_not_initialized (lines 62-68) ---[0m
    pub fn git_not_initialized() -> UserError {
        UserError::new(
            "Git repository not initialized",
            "ccswarm requires a Git repository to manage agent worktrees",
        )
        .with_suggestion("Run 'git init' in your project directory first")
    }

  ./crates/ccswarm/src/utils/user_error.rs:14-21 method new <-> ./crates/ccswarm/src/utils/user_error.rs:70-76 method config_not_found
  Similarity: 79.25%
  Classes: UserError <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:new (lines 14-21) ---[0m
    pub fn new(title: impl Into<String>, message: impl Into<String>) -> Self {
        Self {
            title: title.into(),
            message: message.into(),
            suggestion: None,
            details: None,
        }
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:config_not_found (lines 70-76) ---[0m
    pub fn config_not_found() -> UserError {
        UserError::new(
            "Configuration file not found",
            "ccswarm.json is required to run this command",
        )
        .with_suggestion("Run 'ccswarm init' to create a configuration file")
    }

  ./crates/ccswarm/src/utils/user_error.rs:14-21 method new <-> ./crates/ccswarm/src/utils/user_error.rs:86-92 method session_not_found
  Similarity: 79.25%
  Classes: UserError <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:new (lines 14-21) ---[0m
    pub fn new(title: impl Into<String>, message: impl Into<String>) -> Self {
        Self {
            title: title.into(),
            message: message.into(),
            suggestion: None,
            details: None,
        }
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:session_not_found (lines 86-92) ---[0m
    pub fn session_not_found(session_id: &str) -> UserError {
        UserError::new(
            format!("Session not found: {}", session_id),
            "The specified session does not exist or has been terminated",
        )
        .with_suggestion("Run 'ccswarm session list' to see active sessions")
    }

  ./crates/ccswarm/src/utils/user_error.rs:14-21 method new <-> ./crates/ccswarm/src/utils/user_error.rs:102-108 method invalid_task_format
  Similarity: 79.25%
  Classes: UserError <-> CommonErrors

[36m--- ./crates/ccswarm/src/utils/user_error.rs:new (lines 14-21) ---[0m
    pub fn new(title: impl Into<String>, message: impl Into<String>) -> Self {
        Self {
            title: title.into(),
            message: message.into(),
            suggestion: None,
            details: None,
        }
    }

[36m--- ./crates/ccswarm/src/utils/user_error.rs:invalid_task_format (lines 102-108) ---[0m
    pub fn invalid_task_format() -> UserError {
        UserError::new(
            "Invalid task format",
            "The task format is not valid",
        )
        .with_suggestion("Use format: 'task description [priority] [type]'")
    }


Duplicates in ./crates/ccswarm/src/auto_accept/mod.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/auto_accept/mod.rs:285-353 method should_auto_accept <-> ./crates/ccswarm/src/auto_accept/mod.rs:356-408 method validate_changes
  Similarity: 91.65%
  Classes: AutoAcceptEngine <-> AutoAcceptEngine

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:should_auto_accept (lines 285-353) ---[0m
    pub fn should_auto_accept(&self, operation: &Operation) -> Result<AutoAcceptDecision> {
        // Quick rejections
        if self.config.emergency_stop {
            return Ok(AutoAcceptDecision::Reject(
                "Emergency stop is active".to_string(),
            ));
        }

        if !self.config.enabled {
            return Ok(AutoAcceptDecision::Reject(
                "Auto-accept is disabled".to_string(),
            ));
        }

        // Check if operation type is trusted
        if !self
            .config
            .trusted_operations
            .contains(&operation.operation_type)
        {
            return Ok(AutoAcceptDecision::Reject(format!(
                "Operation type {:?} is not in trusted operations list",
                operation.operation_type
            )));
        }

        // Check risk level (reject anything above 5 for auto-accept)
        if operation.risk_level > 5 {
            return Ok(AutoAcceptDecision::Reject(format!(
                "Risk level too high: {} > 5",
                operation.risk_level
            )));
        }

        // Check file change limits
        if operation.affected_files.len() > self.config.max_file_changes {
            return Ok(AutoAcceptDecision::Reject(format!(
                "Too many file changes: {} > {}",
                operation.affected_files.len(),
                self.config.max_file_changes
            )));
        }

        // Check for restricted files
        for file in &operation.affected_files {
            let file_str = file.to_string_lossy();
            for pattern in &self.config.restricted_files {
                if self.matches_pattern(&file_str, pattern) {
                    return Ok(AutoAcceptDecision::Reject(format!(
                        "File {} matches restricted pattern {}",
                        file_str, pattern
                    )));
                }
            }
        }

        // All checks passed - auto-accept with conditions
        let mut conditions = Vec::new();

        if self.config.require_tests_pass {
            conditions.push("Tests must pass".to_string());
        }

        if self.config.require_clean_git {
            conditions.push("Git working directory must be clean".to_string());
        }

        Ok(AutoAcceptDecision::Accept(conditions))
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_changes (lines 356-408) ---[0m
    pub async fn validate_changes(
        &self,
        operation: &Operation,
        execution_time: u32,
    ) -> Result<ValidationResult> {
        let mut issues = Vec::new();

        // Check execution time
        if execution_time > self.config.max_execution_time {
            issues.push(format!(
                "Execution time exceeded limit: {}s > {}s",
                execution_time, self.config.max_execution_time
            ));
        }

        // Additional validation checks

        // Check if tests still pass
        if self.config.check_tests_pass {
            if let Err(test_error) = self.validate_tests().await {
                issues.push(format!("Tests failed: {}", test_error));
            }
        }

        // Verify no unexpected files were changed
        if self.config.check_file_changes {
            if let Err(file_error) = self.validate_file_changes(operation).await {
                issues.push(format!("Unexpected file changes: {}", file_error));
            }
        }

        // Check git status if required
        if self.config.check_git_status {
            if let Err(git_error) = self.validate_git_status().await {
                issues.push(format!("Git repository issues: {}", git_error));
            }
        }

        // TODO: Validate exit codes once Operation struct has these fields
        // if operation.expected_exit_code.is_some() && operation.actual_exit_code != operation.expected_exit_code {
        //     issues.push(format!(
        //         "Exit code mismatch: expected {:?}, got {:?}",
        //         operation.expected_exit_code,
        //         operation.actual_exit_code
        //     ));
        // }

        if issues.is_empty() {
            Ok(ValidationResult::Valid)
        } else {
            Ok(ValidationResult::Invalid(issues))
        }
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:457-494 method validate_file_changes <-> ./crates/ccswarm/src/auto_accept/mod.rs:497-529 method validate_git_status
  Similarity: 94.07%
  Classes: AutoAcceptEngine <-> AutoAcceptEngine

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_file_changes (lines 457-494) ---[0m
    async fn validate_file_changes(&self, operation: &Operation) -> Result<(), String> {
        // Check if files changed are in allowed patterns
        for file_path in &operation.affected_files {
            let path_str = file_path.to_string_lossy();

            // Check against protected patterns
            let protected_patterns = [
                ".env",
                "*.key",
                ".git/",
                "/etc/",
                "~/.ssh/",
                "credentials",
                "secrets",
                "passwords",
            ];

            for pattern in &protected_patterns {
                if path_str.contains(pattern) {
                    return Err(format!("Protected file modified: {}", path_str));
                }
            }

            // Check file size limits
            if let Ok(metadata) = std::fs::metadata(file_path) {
                if metadata.len() > 10_000_000 {
                    // 10MB limit
                    return Err(format!(
                        "File too large: {} ({} bytes)",
                        path_str,
                        metadata.len()
                    ));
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_git_status (lines 497-529) ---[0m
    async fn validate_git_status(&self) -> Result<(), String> {
        // Check git status
        if let Ok(output) = tokio::process::Command::new("git")
            .args(&["status", "--porcelain"])
            .output()
            .await
        {
            let status_output = String::from_utf8_lossy(&output.stdout);

            // Check for unexpected changes
            for line in status_output.lines() {
                if line.starts_with("??") {
                    // Untracked files - could be okay
                    continue;
                }
                if line.starts_with(" D") || line.starts_with("D ") {
                    return Err(format!("Files deleted: {}", line));
                }
                if line.contains(".git/") {
                    return Err(format!("Git metadata changed: {}", line));
                }
            }

            // Check for conflicts
            if status_output.contains("UU ") || status_output.contains("AA ") {
                return Err("Git merge conflicts detected".to_string());
            }
        } else {
            return Err("Failed to check git status".to_string());
        }

        Ok(())
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:356-408 method validate_changes <-> ./crates/ccswarm/src/auto_accept/mod.rs:429-454 method validate_tests
  Similarity: 74.45%
  Classes: AutoAcceptEngine <-> AutoAcceptEngine

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_changes (lines 356-408) ---[0m
    pub async fn validate_changes(
        &self,
        operation: &Operation,
        execution_time: u32,
    ) -> Result<ValidationResult> {
        let mut issues = Vec::new();

        // Check execution time
        if execution_time > self.config.max_execution_time {
            issues.push(format!(
                "Execution time exceeded limit: {}s > {}s",
                execution_time, self.config.max_execution_time
            ));
        }

        // Additional validation checks

        // Check if tests still pass
        if self.config.check_tests_pass {
            if let Err(test_error) = self.validate_tests().await {
                issues.push(format!("Tests failed: {}", test_error));
            }
        }

        // Verify no unexpected files were changed
        if self.config.check_file_changes {
            if let Err(file_error) = self.validate_file_changes(operation).await {
                issues.push(format!("Unexpected file changes: {}", file_error));
            }
        }

        // Check git status if required
        if self.config.check_git_status {
            if let Err(git_error) = self.validate_git_status().await {
                issues.push(format!("Git repository issues: {}", git_error));
            }
        }

        // TODO: Validate exit codes once Operation struct has these fields
        // if operation.expected_exit_code.is_some() && operation.actual_exit_code != operation.expected_exit_code {
        //     issues.push(format!(
        //         "Exit code mismatch: expected {:?}, got {:?}",
        //         operation.expected_exit_code,
        //         operation.actual_exit_code
        //     ));
        // }

        if issues.is_empty() {
            Ok(ValidationResult::Valid)
        } else {
            Ok(ValidationResult::Invalid(issues))
        }
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_tests (lines 429-454) ---[0m
    async fn validate_tests(&self) -> Result<(), String> {
        // Try common test commands
        let test_commands = ["cargo test", "npm test", "pytest", "go test"];

        for cmd in &test_commands {
            if let Ok(output) = tokio::process::Command::new("sh")
                .arg("-c")
                .arg(cmd)
                .output()
                .await
            {
                if !output.status.success() {
                    return Err(format!(
                        "Test command '{}' failed with exit code: {:?}",
                        cmd,
                        output.status.code()
                    ));
                }
                // If one test command succeeds, we're good
                return Ok(());
            }
        }

        // No test command worked
        Err("No valid test command found or all tests failed".to_string())
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:457-494 method validate_file_changes <-> ./crates/ccswarm/src/auto_accept/mod.rs:532-556 method matches_pattern
  Similarity: 92.31%
  Classes: AutoAcceptEngine <-> AutoAcceptEngine

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_file_changes (lines 457-494) ---[0m
    async fn validate_file_changes(&self, operation: &Operation) -> Result<(), String> {
        // Check if files changed are in allowed patterns
        for file_path in &operation.affected_files {
            let path_str = file_path.to_string_lossy();

            // Check against protected patterns
            let protected_patterns = [
                ".env",
                "*.key",
                ".git/",
                "/etc/",
                "~/.ssh/",
                "credentials",
                "secrets",
                "passwords",
            ];

            for pattern in &protected_patterns {
                if path_str.contains(pattern) {
                    return Err(format!("Protected file modified: {}", path_str));
                }
            }

            // Check file size limits
            if let Ok(metadata) = std::fs::metadata(file_path) {
                if metadata.len() > 10_000_000 {
                    // 10MB limit
                    return Err(format!(
                        "File too large: {} ({} bytes)",
                        path_str,
                        metadata.len()
                    ));
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:matches_pattern (lines 532-556) ---[0m
    pub fn matches_pattern(&self, file: &str, pattern: &str) -> bool {
        if pattern.contains('*') {
            // Simple glob matching - could be improved with a proper glob library
            if pattern.starts_with("**/") && pattern.ends_with("/*") {
                // Pattern like "**/migrations/*" - match any path containing the directory
                let dir_name = &pattern[3..pattern.len() - 2]; // Remove "**/" and "/*"
                file.contains(&format!("/{}/", dir_name))
                    || file.starts_with(&format!("{}/", dir_name))
                    || file.contains(&format!("/{}", dir_name)) // Also match if at end
            } else if let Some(suffix) = pattern.strip_prefix("**/") {
                file.contains(suffix)
            } else if pattern.starts_with('*') && pattern.ends_with('*') {
                let middle = &pattern[1..pattern.len() - 1];
                file.contains(middle)
            } else if let Some(suffix) = pattern.strip_prefix('*') {
                file.ends_with(suffix)
            } else if let Some(prefix) = pattern.strip_suffix('*') {
                file.starts_with(prefix)
            } else {
                file == pattern
            }
        } else {
            file == pattern
        }
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:356-408 method validate_changes <-> ./crates/ccswarm/src/auto_accept/mod.rs:692-714 function test_emergency_stop
  Similarity: 74.74%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_changes (lines 356-408) ---[0m
    pub async fn validate_changes(
        &self,
        operation: &Operation,
        execution_time: u32,
    ) -> Result<ValidationResult> {
        let mut issues = Vec::new();

        // Check execution time
        if execution_time > self.config.max_execution_time {
            issues.push(format!(
                "Execution time exceeded limit: {}s > {}s",
                execution_time, self.config.max_execution_time
            ));
        }

        // Additional validation checks

        // Check if tests still pass
        if self.config.check_tests_pass {
            if let Err(test_error) = self.validate_tests().await {
                issues.push(format!("Tests failed: {}", test_error));
            }
        }

        // Verify no unexpected files were changed
        if self.config.check_file_changes {
            if let Err(file_error) = self.validate_file_changes(operation).await {
                issues.push(format!("Unexpected file changes: {}", file_error));
            }
        }

        // Check git status if required
        if self.config.check_git_status {
            if let Err(git_error) = self.validate_git_status().await {
                issues.push(format!("Git repository issues: {}", git_error));
            }
        }

        // TODO: Validate exit codes once Operation struct has these fields
        // if operation.expected_exit_code.is_some() && operation.actual_exit_code != operation.expected_exit_code {
        //     issues.push(format!(
        //         "Exit code mismatch: expected {:?}, got {:?}",
        //         operation.expected_exit_code,
        //         operation.actual_exit_code
        //     ));
        // }

        if issues.is_empty() {
            Ok(ValidationResult::Valid)
        } else {
            Ok(ValidationResult::Invalid(issues))
        }
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_emergency_stop (lines 692-714) ---[0m
    fn test_emergency_stop() {
        let mut engine = AutoAcceptEngine::new(AutoAcceptConfig {
            enabled: true,
            ..AutoAcceptConfig::default()
        });

        engine.emergency_stop();
        assert!(engine.config.emergency_stop);
        assert!(!engine.config.enabled);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Reject(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:429-454 method validate_tests <-> ./crates/ccswarm/src/auto_accept/mod.rs:497-529 method validate_git_status
  Similarity: 94.17%
  Classes: AutoAcceptEngine <-> AutoAcceptEngine

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_tests (lines 429-454) ---[0m
    async fn validate_tests(&self) -> Result<(), String> {
        // Try common test commands
        let test_commands = ["cargo test", "npm test", "pytest", "go test"];

        for cmd in &test_commands {
            if let Ok(output) = tokio::process::Command::new("sh")
                .arg("-c")
                .arg(cmd)
                .output()
                .await
            {
                if !output.status.success() {
                    return Err(format!(
                        "Test command '{}' failed with exit code: {:?}",
                        cmd,
                        output.status.code()
                    ));
                }
                // If one test command succeeds, we're good
                return Ok(());
            }
        }

        // No test command worked
        Err("No valid test command found or all tests failed".to_string())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_git_status (lines 497-529) ---[0m
    async fn validate_git_status(&self) -> Result<(), String> {
        // Check git status
        if let Ok(output) = tokio::process::Command::new("git")
            .args(&["status", "--porcelain"])
            .output()
            .await
        {
            let status_output = String::from_utf8_lossy(&output.stdout);

            // Check for unexpected changes
            for line in status_output.lines() {
                if line.starts_with("??") {
                    // Untracked files - could be okay
                    continue;
                }
                if line.starts_with(" D") || line.starts_with("D ") {
                    return Err(format!("Files deleted: {}", line));
                }
                if line.contains(".git/") {
                    return Err(format!("Git metadata changed: {}", line));
                }
            }

            // Check for conflicts
            if status_output.contains("UU ") || status_output.contains("AA ") {
                return Err("Git merge conflicts detected".to_string());
            }
        } else {
            return Err("Failed to check git status".to_string());
        }

        Ok(())
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:429-454 method validate_tests <-> ./crates/ccswarm/src/auto_accept/mod.rs:457-494 method validate_file_changes
  Similarity: 83.24%
  Classes: AutoAcceptEngine <-> AutoAcceptEngine

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_tests (lines 429-454) ---[0m
    async fn validate_tests(&self) -> Result<(), String> {
        // Try common test commands
        let test_commands = ["cargo test", "npm test", "pytest", "go test"];

        for cmd in &test_commands {
            if let Ok(output) = tokio::process::Command::new("sh")
                .arg("-c")
                .arg(cmd)
                .output()
                .await
            {
                if !output.status.success() {
                    return Err(format!(
                        "Test command '{}' failed with exit code: {:?}",
                        cmd,
                        output.status.code()
                    ));
                }
                // If one test command succeeds, we're good
                return Ok(());
            }
        }

        // No test command worked
        Err("No valid test command found or all tests failed".to_string())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_file_changes (lines 457-494) ---[0m
    async fn validate_file_changes(&self, operation: &Operation) -> Result<(), String> {
        // Check if files changed are in allowed patterns
        for file_path in &operation.affected_files {
            let path_str = file_path.to_string_lossy();

            // Check against protected patterns
            let protected_patterns = [
                ".env",
                "*.key",
                ".git/",
                "/etc/",
                "~/.ssh/",
                "credentials",
                "secrets",
                "passwords",
            ];

            for pattern in &protected_patterns {
                if path_str.contains(pattern) {
                    return Err(format!("Protected file modified: {}", path_str));
                }
            }

            // Check file size limits
            if let Ok(metadata) = std::fs::metadata(file_path) {
                if metadata.len() > 10_000_000 {
                    // 10MB limit
                    return Err(format!(
                        "File too large: {} ({} bytes)",
                        path_str,
                        metadata.len()
                    ));
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:497-529 method validate_git_status <-> ./crates/ccswarm/src/auto_accept/mod.rs:532-556 method matches_pattern
  Similarity: 89.72%
  Classes: AutoAcceptEngine <-> AutoAcceptEngine

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_git_status (lines 497-529) ---[0m
    async fn validate_git_status(&self) -> Result<(), String> {
        // Check git status
        if let Ok(output) = tokio::process::Command::new("git")
            .args(&["status", "--porcelain"])
            .output()
            .await
        {
            let status_output = String::from_utf8_lossy(&output.stdout);

            // Check for unexpected changes
            for line in status_output.lines() {
                if line.starts_with("??") {
                    // Untracked files - could be okay
                    continue;
                }
                if line.starts_with(" D") || line.starts_with("D ") {
                    return Err(format!("Files deleted: {}", line));
                }
                if line.contains(".git/") {
                    return Err(format!("Git metadata changed: {}", line));
                }
            }

            // Check for conflicts
            if status_output.contains("UU ") || status_output.contains("AA ") {
                return Err("Git merge conflicts detected".to_string());
            }
        } else {
            return Err("Failed to check git status".to_string());
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:matches_pattern (lines 532-556) ---[0m
    pub fn matches_pattern(&self, file: &str, pattern: &str) -> bool {
        if pattern.contains('*') {
            // Simple glob matching - could be improved with a proper glob library
            if pattern.starts_with("**/") && pattern.ends_with("/*") {
                // Pattern like "**/migrations/*" - match any path containing the directory
                let dir_name = &pattern[3..pattern.len() - 2]; // Remove "**/" and "/*"
                file.contains(&format!("/{}/", dir_name))
                    || file.starts_with(&format!("{}/", dir_name))
                    || file.contains(&format!("/{}", dir_name)) // Also match if at end
            } else if let Some(suffix) = pattern.strip_prefix("**/") {
                file.contains(suffix)
            } else if pattern.starts_with('*') && pattern.ends_with('*') {
                let middle = &pattern[1..pattern.len() - 1];
                file.contains(middle)
            } else if let Some(suffix) = pattern.strip_prefix('*') {
                file.ends_with(suffix)
            } else if let Some(prefix) = pattern.strip_suffix('*') {
                file.starts_with(prefix)
            } else {
                file == pattern
            }
        } else {
            file == pattern
        }
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:497-529 method validate_git_status <-> ./crates/ccswarm/src/auto_accept/mod.rs:559-580 method extract_file_paths
  Similarity: 93.03%
  Classes: AutoAcceptEngine <-> AutoAcceptEngine

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_git_status (lines 497-529) ---[0m
    async fn validate_git_status(&self) -> Result<(), String> {
        // Check git status
        if let Ok(output) = tokio::process::Command::new("git")
            .args(&["status", "--porcelain"])
            .output()
            .await
        {
            let status_output = String::from_utf8_lossy(&output.stdout);

            // Check for unexpected changes
            for line in status_output.lines() {
                if line.starts_with("??") {
                    // Untracked files - could be okay
                    continue;
                }
                if line.starts_with(" D") || line.starts_with("D ") {
                    return Err(format!("Files deleted: {}", line));
                }
                if line.contains(".git/") {
                    return Err(format!("Git metadata changed: {}", line));
                }
            }

            // Check for conflicts
            if status_output.contains("UU ") || status_output.contains("AA ") {
                return Err("Git merge conflicts detected".to_string());
            }
        } else {
            return Err("Failed to check git status".to_string());
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:extract_file_paths (lines 559-580) ---[0m
    fn extract_file_paths(&self, command: &str, paths: &mut Vec<PathBuf>) {
        // This is a simplified implementation - a more robust version would
        // properly parse shell commands and extract file arguments
        let parts: Vec<&str> = command.split_whitespace().collect();

        for part in parts {
            if part.contains('/') || part.contains('.') {
                // Likely a file path
                if let Ok(path) = std::path::Path::new(part).canonicalize() {
                    if !paths.contains(&path) {
                        paths.push(path);
                    }
                } else {
                    // Add as-is if we can't canonicalize (file might not exist yet)
                    let path = PathBuf::from(part);
                    if !paths.contains(&path) {
                        paths.push(path);
                    }
                }
            }
        }
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:457-494 method validate_file_changes <-> ./crates/ccswarm/src/auto_accept/mod.rs:559-580 method extract_file_paths
  Similarity: 84.90%
  Classes: AutoAcceptEngine <-> AutoAcceptEngine

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_file_changes (lines 457-494) ---[0m
    async fn validate_file_changes(&self, operation: &Operation) -> Result<(), String> {
        // Check if files changed are in allowed patterns
        for file_path in &operation.affected_files {
            let path_str = file_path.to_string_lossy();

            // Check against protected patterns
            let protected_patterns = [
                ".env",
                "*.key",
                ".git/",
                "/etc/",
                "~/.ssh/",
                "credentials",
                "secrets",
                "passwords",
            ];

            for pattern in &protected_patterns {
                if path_str.contains(pattern) {
                    return Err(format!("Protected file modified: {}", path_str));
                }
            }

            // Check file size limits
            if let Ok(metadata) = std::fs::metadata(file_path) {
                if metadata.len() > 10_000_000 {
                    // 10MB limit
                    return Err(format!(
                        "File too large: {} ({} bytes)",
                        path_str,
                        metadata.len()
                    ));
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:extract_file_paths (lines 559-580) ---[0m
    fn extract_file_paths(&self, command: &str, paths: &mut Vec<PathBuf>) {
        // This is a simplified implementation - a more robust version would
        // properly parse shell commands and extract file arguments
        let parts: Vec<&str> = command.split_whitespace().collect();

        for part in parts {
            if part.contains('/') || part.contains('.') {
                // Likely a file path
                if let Ok(path) = std::path::Path::new(part).canonicalize() {
                    if !paths.contains(&path) {
                        paths.push(path);
                    }
                } else {
                    // Add as-is if we can't canonicalize (file might not exist yet)
                    let path = PathBuf::from(part);
                    if !paths.contains(&path) {
                        paths.push(path);
                    }
                }
            }
        }
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:429-454 method validate_tests <-> ./crates/ccswarm/src/auto_accept/mod.rs:559-580 method extract_file_paths
  Similarity: 92.83%
  Classes: AutoAcceptEngine <-> AutoAcceptEngine

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_tests (lines 429-454) ---[0m
    async fn validate_tests(&self) -> Result<(), String> {
        // Try common test commands
        let test_commands = ["cargo test", "npm test", "pytest", "go test"];

        for cmd in &test_commands {
            if let Ok(output) = tokio::process::Command::new("sh")
                .arg("-c")
                .arg(cmd)
                .output()
                .await
            {
                if !output.status.success() {
                    return Err(format!(
                        "Test command '{}' failed with exit code: {:?}",
                        cmd,
                        output.status.code()
                    ));
                }
                // If one test command succeeds, we're good
                return Ok(());
            }
        }

        // No test command worked
        Err("No valid test command found or all tests failed".to_string())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:extract_file_paths (lines 559-580) ---[0m
    fn extract_file_paths(&self, command: &str, paths: &mut Vec<PathBuf>) {
        // This is a simplified implementation - a more robust version would
        // properly parse shell commands and extract file arguments
        let parts: Vec<&str> = command.split_whitespace().collect();

        for part in parts {
            if part.contains('/') || part.contains('.') {
                // Likely a file path
                if let Ok(path) = std::path::Path::new(part).canonicalize() {
                    if !paths.contains(&path) {
                        paths.push(path);
                    }
                } else {
                    // Add as-is if we can't canonicalize (file might not exist yet)
                    let path = PathBuf::from(part);
                    if !paths.contains(&path) {
                        paths.push(path);
                    }
                }
            }
        }
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:497-529 method validate_git_status <-> ./crates/ccswarm/src/auto_accept/mod.rs:659-679 function test_should_auto_accept_trusted_operation
  Similarity: 79.80%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_git_status (lines 497-529) ---[0m
    async fn validate_git_status(&self) -> Result<(), String> {
        // Check git status
        if let Ok(output) = tokio::process::Command::new("git")
            .args(&["status", "--porcelain"])
            .output()
            .await
        {
            let status_output = String::from_utf8_lossy(&output.stdout);

            // Check for unexpected changes
            for line in status_output.lines() {
                if line.starts_with("??") {
                    // Untracked files - could be okay
                    continue;
                }
                if line.starts_with(" D") || line.starts_with("D ") {
                    return Err(format!("Files deleted: {}", line));
                }
                if line.contains(".git/") {
                    return Err(format!("Git metadata changed: {}", line));
                }
            }

            // Check for conflicts
            if status_output.contains("UU ") || status_output.contains("AA ") {
                return Err("Git merge conflicts detected".to_string());
            }
        } else {
            return Err("Failed to check git status".to_string());
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_should_auto_accept_trusted_operation (lines 659-679) ---[0m
    fn test_should_auto_accept_trusted_operation() {
        let config = AutoAcceptConfig {
            enabled: true,
            trusted_operations: vec![OperationType::ReadFile],
            ..AutoAcceptConfig::default()
        };
        let engine = AutoAcceptEngine::new(config);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Accept(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:497-529 method validate_git_status <-> ./crates/ccswarm/src/auto_accept/mod.rs:637-656 function test_should_auto_accept_disabled
  Similarity: 79.80%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_git_status (lines 497-529) ---[0m
    async fn validate_git_status(&self) -> Result<(), String> {
        // Check git status
        if let Ok(output) = tokio::process::Command::new("git")
            .args(&["status", "--porcelain"])
            .output()
            .await
        {
            let status_output = String::from_utf8_lossy(&output.stdout);

            // Check for unexpected changes
            for line in status_output.lines() {
                if line.starts_with("??") {
                    // Untracked files - could be okay
                    continue;
                }
                if line.starts_with(" D") || line.starts_with("D ") {
                    return Err(format!("Files deleted: {}", line));
                }
                if line.contains(".git/") {
                    return Err(format!("Git metadata changed: {}", line));
                }
            }

            // Check for conflicts
            if status_output.contains("UU ") || status_output.contains("AA ") {
                return Err("Git merge conflicts detected".to_string());
            }
        } else {
            return Err("Failed to check git status".to_string());
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_should_auto_accept_disabled (lines 637-656) ---[0m
    fn test_should_auto_accept_disabled() {
        let config = AutoAcceptConfig {
            enabled: false,
            ..AutoAcceptConfig::default()
        };
        let engine = AutoAcceptEngine::new(config);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Reject(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:429-454 method validate_tests <-> ./crates/ccswarm/src/auto_accept/mod.rs:532-556 method matches_pattern
  Similarity: 82.87%
  Classes: AutoAcceptEngine <-> AutoAcceptEngine

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_tests (lines 429-454) ---[0m
    async fn validate_tests(&self) -> Result<(), String> {
        // Try common test commands
        let test_commands = ["cargo test", "npm test", "pytest", "go test"];

        for cmd in &test_commands {
            if let Ok(output) = tokio::process::Command::new("sh")
                .arg("-c")
                .arg(cmd)
                .output()
                .await
            {
                if !output.status.success() {
                    return Err(format!(
                        "Test command '{}' failed with exit code: {:?}",
                        cmd,
                        output.status.code()
                    ));
                }
                // If one test command succeeds, we're good
                return Ok(());
            }
        }

        // No test command worked
        Err("No valid test command found or all tests failed".to_string())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:matches_pattern (lines 532-556) ---[0m
    pub fn matches_pattern(&self, file: &str, pattern: &str) -> bool {
        if pattern.contains('*') {
            // Simple glob matching - could be improved with a proper glob library
            if pattern.starts_with("**/") && pattern.ends_with("/*") {
                // Pattern like "**/migrations/*" - match any path containing the directory
                let dir_name = &pattern[3..pattern.len() - 2]; // Remove "**/" and "/*"
                file.contains(&format!("/{}/", dir_name))
                    || file.starts_with(&format!("{}/", dir_name))
                    || file.contains(&format!("/{}", dir_name)) // Also match if at end
            } else if let Some(suffix) = pattern.strip_prefix("**/") {
                file.contains(suffix)
            } else if pattern.starts_with('*') && pattern.ends_with('*') {
                let middle = &pattern[1..pattern.len() - 1];
                file.contains(middle)
            } else if let Some(suffix) = pattern.strip_prefix('*') {
                file.ends_with(suffix)
            } else if let Some(prefix) = pattern.strip_suffix('*') {
                file.starts_with(prefix)
            } else {
                file == pattern
            }
        } else {
            file == pattern
        }
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:457-494 method validate_file_changes <-> ./crates/ccswarm/src/auto_accept/mod.rs:659-679 function test_should_auto_accept_trusted_operation
  Similarity: 70.29%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_file_changes (lines 457-494) ---[0m
    async fn validate_file_changes(&self, operation: &Operation) -> Result<(), String> {
        // Check if files changed are in allowed patterns
        for file_path in &operation.affected_files {
            let path_str = file_path.to_string_lossy();

            // Check against protected patterns
            let protected_patterns = [
                ".env",
                "*.key",
                ".git/",
                "/etc/",
                "~/.ssh/",
                "credentials",
                "secrets",
                "passwords",
            ];

            for pattern in &protected_patterns {
                if path_str.contains(pattern) {
                    return Err(format!("Protected file modified: {}", path_str));
                }
            }

            // Check file size limits
            if let Ok(metadata) = std::fs::metadata(file_path) {
                if metadata.len() > 10_000_000 {
                    // 10MB limit
                    return Err(format!(
                        "File too large: {} ({} bytes)",
                        path_str,
                        metadata.len()
                    ));
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_should_auto_accept_trusted_operation (lines 659-679) ---[0m
    fn test_should_auto_accept_trusted_operation() {
        let config = AutoAcceptConfig {
            enabled: true,
            trusted_operations: vec![OperationType::ReadFile],
            ..AutoAcceptConfig::default()
        };
        let engine = AutoAcceptEngine::new(config);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Accept(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:457-494 method validate_file_changes <-> ./crates/ccswarm/src/auto_accept/mod.rs:637-656 function test_should_auto_accept_disabled
  Similarity: 71.47%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_file_changes (lines 457-494) ---[0m
    async fn validate_file_changes(&self, operation: &Operation) -> Result<(), String> {
        // Check if files changed are in allowed patterns
        for file_path in &operation.affected_files {
            let path_str = file_path.to_string_lossy();

            // Check against protected patterns
            let protected_patterns = [
                ".env",
                "*.key",
                ".git/",
                "/etc/",
                "~/.ssh/",
                "credentials",
                "secrets",
                "passwords",
            ];

            for pattern in &protected_patterns {
                if path_str.contains(pattern) {
                    return Err(format!("Protected file modified: {}", path_str));
                }
            }

            // Check file size limits
            if let Ok(metadata) = std::fs::metadata(file_path) {
                if metadata.len() > 10_000_000 {
                    // 10MB limit
                    return Err(format!(
                        "File too large: {} ({} bytes)",
                        path_str,
                        metadata.len()
                    ));
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_should_auto_accept_disabled (lines 637-656) ---[0m
    fn test_should_auto_accept_disabled() {
        let config = AutoAcceptConfig {
            enabled: false,
            ..AutoAcceptConfig::default()
        };
        let engine = AutoAcceptEngine::new(config);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Reject(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:532-556 method matches_pattern <-> ./crates/ccswarm/src/auto_accept/mod.rs:559-580 method extract_file_paths
  Similarity: 87.69%
  Classes: AutoAcceptEngine <-> AutoAcceptEngine

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:matches_pattern (lines 532-556) ---[0m
    pub fn matches_pattern(&self, file: &str, pattern: &str) -> bool {
        if pattern.contains('*') {
            // Simple glob matching - could be improved with a proper glob library
            if pattern.starts_with("**/") && pattern.ends_with("/*") {
                // Pattern like "**/migrations/*" - match any path containing the directory
                let dir_name = &pattern[3..pattern.len() - 2]; // Remove "**/" and "/*"
                file.contains(&format!("/{}/", dir_name))
                    || file.starts_with(&format!("{}/", dir_name))
                    || file.contains(&format!("/{}", dir_name)) // Also match if at end
            } else if let Some(suffix) = pattern.strip_prefix("**/") {
                file.contains(suffix)
            } else if pattern.starts_with('*') && pattern.ends_with('*') {
                let middle = &pattern[1..pattern.len() - 1];
                file.contains(middle)
            } else if let Some(suffix) = pattern.strip_prefix('*') {
                file.ends_with(suffix)
            } else if let Some(prefix) = pattern.strip_suffix('*') {
                file.starts_with(prefix)
            } else {
                file == pattern
            }
        } else {
            file == pattern
        }
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:extract_file_paths (lines 559-580) ---[0m
    fn extract_file_paths(&self, command: &str, paths: &mut Vec<PathBuf>) {
        // This is a simplified implementation - a more robust version would
        // properly parse shell commands and extract file arguments
        let parts: Vec<&str> = command.split_whitespace().collect();

        for part in parts {
            if part.contains('/') || part.contains('.') {
                // Likely a file path
                if let Ok(path) = std::path::Path::new(part).canonicalize() {
                    if !paths.contains(&path) {
                        paths.push(path);
                    }
                } else {
                    // Add as-is if we can't canonicalize (file might not exist yet)
                    let path = PathBuf::from(part);
                    if !paths.contains(&path) {
                        paths.push(path);
                    }
                }
            }
        }
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:497-529 method validate_git_status <-> ./crates/ccswarm/src/auto_accept/mod.rs:692-714 function test_emergency_stop
  Similarity: 72.60%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_git_status (lines 497-529) ---[0m
    async fn validate_git_status(&self) -> Result<(), String> {
        // Check git status
        if let Ok(output) = tokio::process::Command::new("git")
            .args(&["status", "--porcelain"])
            .output()
            .await
        {
            let status_output = String::from_utf8_lossy(&output.stdout);

            // Check for unexpected changes
            for line in status_output.lines() {
                if line.starts_with("??") {
                    // Untracked files - could be okay
                    continue;
                }
                if line.starts_with(" D") || line.starts_with("D ") {
                    return Err(format!("Files deleted: {}", line));
                }
                if line.contains(".git/") {
                    return Err(format!("Git metadata changed: {}", line));
                }
            }

            // Check for conflicts
            if status_output.contains("UU ") || status_output.contains("AA ") {
                return Err("Git merge conflicts detected".to_string());
            }
        } else {
            return Err("Failed to check git status".to_string());
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_emergency_stop (lines 692-714) ---[0m
    fn test_emergency_stop() {
        let mut engine = AutoAcceptEngine::new(AutoAcceptConfig {
            enabled: true,
            ..AutoAcceptConfig::default()
        });

        engine.emergency_stop();
        assert!(engine.config.emergency_stop);
        assert!(!engine.config.enabled);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Reject(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:637-656 function test_should_auto_accept_disabled <-> ./crates/ccswarm/src/auto_accept/mod.rs:659-679 function test_should_auto_accept_trusted_operation
  Similarity: 97.91%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_should_auto_accept_disabled (lines 637-656) ---[0m
    fn test_should_auto_accept_disabled() {
        let config = AutoAcceptConfig {
            enabled: false,
            ..AutoAcceptConfig::default()
        };
        let engine = AutoAcceptEngine::new(config);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Reject(_));
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_should_auto_accept_trusted_operation (lines 659-679) ---[0m
    fn test_should_auto_accept_trusted_operation() {
        let config = AutoAcceptConfig {
            enabled: true,
            trusted_operations: vec![OperationType::ReadFile],
            ..AutoAcceptConfig::default()
        };
        let engine = AutoAcceptEngine::new(config);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Accept(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:429-454 method validate_tests <-> ./crates/ccswarm/src/auto_accept/mod.rs:659-679 function test_should_auto_accept_trusted_operation
  Similarity: 79.34%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_tests (lines 429-454) ---[0m
    async fn validate_tests(&self) -> Result<(), String> {
        // Try common test commands
        let test_commands = ["cargo test", "npm test", "pytest", "go test"];

        for cmd in &test_commands {
            if let Ok(output) = tokio::process::Command::new("sh")
                .arg("-c")
                .arg(cmd)
                .output()
                .await
            {
                if !output.status.success() {
                    return Err(format!(
                        "Test command '{}' failed with exit code: {:?}",
                        cmd,
                        output.status.code()
                    ));
                }
                // If one test command succeeds, we're good
                return Ok(());
            }
        }

        // No test command worked
        Err("No valid test command found or all tests failed".to_string())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_should_auto_accept_trusted_operation (lines 659-679) ---[0m
    fn test_should_auto_accept_trusted_operation() {
        let config = AutoAcceptConfig {
            enabled: true,
            trusted_operations: vec![OperationType::ReadFile],
            ..AutoAcceptConfig::default()
        };
        let engine = AutoAcceptEngine::new(config);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Accept(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:659-679 function test_should_auto_accept_trusted_operation <-> ./crates/ccswarm/src/auto_accept/mod.rs:692-714 function test_emergency_stop
  Similarity: 83.14%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_should_auto_accept_trusted_operation (lines 659-679) ---[0m
    fn test_should_auto_accept_trusted_operation() {
        let config = AutoAcceptConfig {
            enabled: true,
            trusted_operations: vec![OperationType::ReadFile],
            ..AutoAcceptConfig::default()
        };
        let engine = AutoAcceptEngine::new(config);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Accept(_));
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_emergency_stop (lines 692-714) ---[0m
    fn test_emergency_stop() {
        let mut engine = AutoAcceptEngine::new(AutoAcceptConfig {
            enabled: true,
            ..AutoAcceptConfig::default()
        });

        engine.emergency_stop();
        assert!(engine.config.emergency_stop);
        assert!(!engine.config.enabled);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Reject(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:429-454 method validate_tests <-> ./crates/ccswarm/src/auto_accept/mod.rs:637-656 function test_should_auto_accept_disabled
  Similarity: 79.34%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_tests (lines 429-454) ---[0m
    async fn validate_tests(&self) -> Result<(), String> {
        // Try common test commands
        let test_commands = ["cargo test", "npm test", "pytest", "go test"];

        for cmd in &test_commands {
            if let Ok(output) = tokio::process::Command::new("sh")
                .arg("-c")
                .arg(cmd)
                .output()
                .await
            {
                if !output.status.success() {
                    return Err(format!(
                        "Test command '{}' failed with exit code: {:?}",
                        cmd,
                        output.status.code()
                    ));
                }
                // If one test command succeeds, we're good
                return Ok(());
            }
        }

        // No test command worked
        Err("No valid test command found or all tests failed".to_string())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_should_auto_accept_disabled (lines 637-656) ---[0m
    fn test_should_auto_accept_disabled() {
        let config = AutoAcceptConfig {
            enabled: false,
            ..AutoAcceptConfig::default()
        };
        let engine = AutoAcceptEngine::new(config);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Reject(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:637-656 function test_should_auto_accept_disabled <-> ./crates/ccswarm/src/auto_accept/mod.rs:692-714 function test_emergency_stop
  Similarity: 83.30%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_should_auto_accept_disabled (lines 637-656) ---[0m
    fn test_should_auto_accept_disabled() {
        let config = AutoAcceptConfig {
            enabled: false,
            ..AutoAcceptConfig::default()
        };
        let engine = AutoAcceptEngine::new(config);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Reject(_));
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_emergency_stop (lines 692-714) ---[0m
    fn test_emergency_stop() {
        let mut engine = AutoAcceptEngine::new(AutoAcceptConfig {
            enabled: true,
            ..AutoAcceptConfig::default()
        });

        engine.emergency_stop();
        assert!(engine.config.emergency_stop);
        assert!(!engine.config.enabled);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Reject(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:429-454 method validate_tests <-> ./crates/ccswarm/src/auto_accept/mod.rs:692-714 function test_emergency_stop
  Similarity: 72.20%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_tests (lines 429-454) ---[0m
    async fn validate_tests(&self) -> Result<(), String> {
        // Try common test commands
        let test_commands = ["cargo test", "npm test", "pytest", "go test"];

        for cmd in &test_commands {
            if let Ok(output) = tokio::process::Command::new("sh")
                .arg("-c")
                .arg(cmd)
                .output()
                .await
            {
                if !output.status.success() {
                    return Err(format!(
                        "Test command '{}' failed with exit code: {:?}",
                        cmd,
                        output.status.code()
                    ));
                }
                // If one test command succeeds, we're good
                return Ok(());
            }
        }

        // No test command worked
        Err("No valid test command found or all tests failed".to_string())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_emergency_stop (lines 692-714) ---[0m
    fn test_emergency_stop() {
        let mut engine = AutoAcceptEngine::new(AutoAcceptConfig {
            enabled: true,
            ..AutoAcceptConfig::default()
        });

        engine.emergency_stop();
        assert!(engine.config.emergency_stop);
        assert!(!engine.config.enabled);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Reject(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:559-580 method extract_file_paths <-> ./crates/ccswarm/src/auto_accept/mod.rs:659-679 function test_should_auto_accept_trusted_operation
  Similarity: 81.09%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:extract_file_paths (lines 559-580) ---[0m
    fn extract_file_paths(&self, command: &str, paths: &mut Vec<PathBuf>) {
        // This is a simplified implementation - a more robust version would
        // properly parse shell commands and extract file arguments
        let parts: Vec<&str> = command.split_whitespace().collect();

        for part in parts {
            if part.contains('/') || part.contains('.') {
                // Likely a file path
                if let Ok(path) = std::path::Path::new(part).canonicalize() {
                    if !paths.contains(&path) {
                        paths.push(path);
                    }
                } else {
                    // Add as-is if we can't canonicalize (file might not exist yet)
                    let path = PathBuf::from(part);
                    if !paths.contains(&path) {
                        paths.push(path);
                    }
                }
            }
        }
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_should_auto_accept_trusted_operation (lines 659-679) ---[0m
    fn test_should_auto_accept_trusted_operation() {
        let config = AutoAcceptConfig {
            enabled: true,
            trusted_operations: vec![OperationType::ReadFile],
            ..AutoAcceptConfig::default()
        };
        let engine = AutoAcceptEngine::new(config);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Accept(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:559-580 method extract_file_paths <-> ./crates/ccswarm/src/auto_accept/mod.rs:637-656 function test_should_auto_accept_disabled
  Similarity: 81.09%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:extract_file_paths (lines 559-580) ---[0m
    fn extract_file_paths(&self, command: &str, paths: &mut Vec<PathBuf>) {
        // This is a simplified implementation - a more robust version would
        // properly parse shell commands and extract file arguments
        let parts: Vec<&str> = command.split_whitespace().collect();

        for part in parts {
            if part.contains('/') || part.contains('.') {
                // Likely a file path
                if let Ok(path) = std::path::Path::new(part).canonicalize() {
                    if !paths.contains(&path) {
                        paths.push(path);
                    }
                } else {
                    // Add as-is if we can't canonicalize (file might not exist yet)
                    let path = PathBuf::from(part);
                    if !paths.contains(&path) {
                        paths.push(path);
                    }
                }
            }
        }
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_should_auto_accept_disabled (lines 637-656) ---[0m
    fn test_should_auto_accept_disabled() {
        let config = AutoAcceptConfig {
            enabled: false,
            ..AutoAcceptConfig::default()
        };
        let engine = AutoAcceptEngine::new(config);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Reject(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:457-494 method validate_file_changes <-> ./crates/ccswarm/src/auto_accept/mod.rs:682-689 function test_pattern_matching
  Similarity: 70.49%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_file_changes (lines 457-494) ---[0m
    async fn validate_file_changes(&self, operation: &Operation) -> Result<(), String> {
        // Check if files changed are in allowed patterns
        for file_path in &operation.affected_files {
            let path_str = file_path.to_string_lossy();

            // Check against protected patterns
            let protected_patterns = [
                ".env",
                "*.key",
                ".git/",
                "/etc/",
                "~/.ssh/",
                "credentials",
                "secrets",
                "passwords",
            ];

            for pattern in &protected_patterns {
                if path_str.contains(pattern) {
                    return Err(format!("Protected file modified: {}", path_str));
                }
            }

            // Check file size limits
            if let Ok(metadata) = std::fs::metadata(file_path) {
                if metadata.len() > 10_000_000 {
                    // 10MB limit
                    return Err(format!(
                        "File too large: {} ({} bytes)",
                        path_str,
                        metadata.len()
                    ));
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_pattern_matching (lines 682-689) ---[0m
    fn test_pattern_matching() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());

        assert!(engine.matches_pattern("test.sql", "*.sql"));
        assert!(engine.matches_pattern("src/migrations/001.sql", "**/migrations/*"));
        assert!(engine.matches_pattern("Cargo.toml", "Cargo.toml"));
        assert!(!engine.matches_pattern("test.rs", "*.sql"));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:429-454 method validate_tests <-> ./crates/ccswarm/src/auto_accept/mod.rs:682-689 function test_pattern_matching
  Similarity: 87.96%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_tests (lines 429-454) ---[0m
    async fn validate_tests(&self) -> Result<(), String> {
        // Try common test commands
        let test_commands = ["cargo test", "npm test", "pytest", "go test"];

        for cmd in &test_commands {
            if let Ok(output) = tokio::process::Command::new("sh")
                .arg("-c")
                .arg(cmd)
                .output()
                .await
            {
                if !output.status.success() {
                    return Err(format!(
                        "Test command '{}' failed with exit code: {:?}",
                        cmd,
                        output.status.code()
                    ));
                }
                // If one test command succeeds, we're good
                return Ok(());
            }
        }

        // No test command worked
        Err("No valid test command found or all tests failed".to_string())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_pattern_matching (lines 682-689) ---[0m
    fn test_pattern_matching() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());

        assert!(engine.matches_pattern("test.sql", "*.sql"));
        assert!(engine.matches_pattern("src/migrations/001.sql", "**/migrations/*"));
        assert!(engine.matches_pattern("Cargo.toml", "Cargo.toml"));
        assert!(!engine.matches_pattern("test.rs", "*.sql"));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:429-454 method validate_tests <-> ./crates/ccswarm/src/auto_accept/mod.rs:627-634 function test_analyze_dangerous_operation
  Similarity: 87.07%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_tests (lines 429-454) ---[0m
    async fn validate_tests(&self) -> Result<(), String> {
        // Try common test commands
        let test_commands = ["cargo test", "npm test", "pytest", "go test"];

        for cmd in &test_commands {
            if let Ok(output) = tokio::process::Command::new("sh")
                .arg("-c")
                .arg(cmd)
                .output()
                .await
            {
                if !output.status.success() {
                    return Err(format!(
                        "Test command '{}' failed with exit code: {:?}",
                        cmd,
                        output.status.code()
                    ));
                }
                // If one test command succeeds, we're good
                return Ok(());
            }
        }

        // No test command worked
        Err("No valid test command found or all tests failed".to_string())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_analyze_dangerous_operation (lines 627-634) ---[0m
    fn test_analyze_dangerous_operation() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
        let commands = vec!["rm -rf /".to_string()];

        let operation = engine.analyze_operation(&commands, None).unwrap();
        assert_eq!(operation.operation_type, OperationType::DeleteFile);
        assert!(operation.risk_level >= 8);
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:429-454 method validate_tests <-> ./crates/ccswarm/src/auto_accept/mod.rs:616-624 function test_analyze_read_operation
  Similarity: 80.99%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:validate_tests (lines 429-454) ---[0m
    async fn validate_tests(&self) -> Result<(), String> {
        // Try common test commands
        let test_commands = ["cargo test", "npm test", "pytest", "go test"];

        for cmd in &test_commands {
            if let Ok(output) = tokio::process::Command::new("sh")
                .arg("-c")
                .arg(cmd)
                .output()
                .await
            {
                if !output.status.success() {
                    return Err(format!(
                        "Test command '{}' failed with exit code: {:?}",
                        cmd,
                        output.status.code()
                    ));
                }
                // If one test command succeeds, we're good
                return Ok(());
            }
        }

        // No test command worked
        Err("No valid test command found or all tests failed".to_string())
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_analyze_read_operation (lines 616-624) ---[0m
    fn test_analyze_read_operation() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
        let commands = vec!["cat src/main.rs".to_string()];

        let operation = engine.analyze_operation(&commands, None).unwrap();
        assert_eq!(operation.operation_type, OperationType::ReadFile);
        assert_eq!(operation.risk_level, 1);
        assert!(operation.reversible);
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:627-634 function test_analyze_dangerous_operation <-> ./crates/ccswarm/src/auto_accept/mod.rs:659-679 function test_should_auto_accept_trusted_operation
  Similarity: 87.56%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_analyze_dangerous_operation (lines 627-634) ---[0m
    fn test_analyze_dangerous_operation() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
        let commands = vec!["rm -rf /".to_string()];

        let operation = engine.analyze_operation(&commands, None).unwrap();
        assert_eq!(operation.operation_type, OperationType::DeleteFile);
        assert!(operation.risk_level >= 8);
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_should_auto_accept_trusted_operation (lines 659-679) ---[0m
    fn test_should_auto_accept_trusted_operation() {
        let config = AutoAcceptConfig {
            enabled: true,
            trusted_operations: vec![OperationType::ReadFile],
            ..AutoAcceptConfig::default()
        };
        let engine = AutoAcceptEngine::new(config);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Accept(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:616-624 function test_analyze_read_operation <-> ./crates/ccswarm/src/auto_accept/mod.rs:692-714 function test_emergency_stop
  Similarity: 79.21%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_analyze_read_operation (lines 616-624) ---[0m
    fn test_analyze_read_operation() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
        let commands = vec!["cat src/main.rs".to_string()];

        let operation = engine.analyze_operation(&commands, None).unwrap();
        assert_eq!(operation.operation_type, OperationType::ReadFile);
        assert_eq!(operation.risk_level, 1);
        assert!(operation.reversible);
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_emergency_stop (lines 692-714) ---[0m
    fn test_emergency_stop() {
        let mut engine = AutoAcceptEngine::new(AutoAcceptConfig {
            enabled: true,
            ..AutoAcceptConfig::default()
        });

        engine.emergency_stop();
        assert!(engine.config.emergency_stop);
        assert!(!engine.config.enabled);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Reject(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:659-679 function test_should_auto_accept_trusted_operation <-> ./crates/ccswarm/src/auto_accept/mod.rs:682-689 function test_pattern_matching
  Similarity: 85.64%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_should_auto_accept_trusted_operation (lines 659-679) ---[0m
    fn test_should_auto_accept_trusted_operation() {
        let config = AutoAcceptConfig {
            enabled: true,
            trusted_operations: vec![OperationType::ReadFile],
            ..AutoAcceptConfig::default()
        };
        let engine = AutoAcceptEngine::new(config);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Accept(_));
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_pattern_matching (lines 682-689) ---[0m
    fn test_pattern_matching() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());

        assert!(engine.matches_pattern("test.sql", "*.sql"));
        assert!(engine.matches_pattern("src/migrations/001.sql", "**/migrations/*"));
        assert!(engine.matches_pattern("Cargo.toml", "Cargo.toml"));
        assert!(!engine.matches_pattern("test.rs", "*.sql"));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:559-580 method extract_file_paths <-> ./crates/ccswarm/src/auto_accept/mod.rs:627-634 function test_analyze_dangerous_operation
  Similarity: 81.47%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:extract_file_paths (lines 559-580) ---[0m
    fn extract_file_paths(&self, command: &str, paths: &mut Vec<PathBuf>) {
        // This is a simplified implementation - a more robust version would
        // properly parse shell commands and extract file arguments
        let parts: Vec<&str> = command.split_whitespace().collect();

        for part in parts {
            if part.contains('/') || part.contains('.') {
                // Likely a file path
                if let Ok(path) = std::path::Path::new(part).canonicalize() {
                    if !paths.contains(&path) {
                        paths.push(path);
                    }
                } else {
                    // Add as-is if we can't canonicalize (file might not exist yet)
                    let path = PathBuf::from(part);
                    if !paths.contains(&path) {
                        paths.push(path);
                    }
                }
            }
        }
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_analyze_dangerous_operation (lines 627-634) ---[0m
    fn test_analyze_dangerous_operation() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
        let commands = vec!["rm -rf /".to_string()];

        let operation = engine.analyze_operation(&commands, None).unwrap();
        assert_eq!(operation.operation_type, OperationType::DeleteFile);
        assert!(operation.risk_level >= 8);
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:616-624 function test_analyze_read_operation <-> ./crates/ccswarm/src/auto_accept/mod.rs:659-679 function test_should_auto_accept_trusted_operation
  Similarity: 81.16%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_analyze_read_operation (lines 616-624) ---[0m
    fn test_analyze_read_operation() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
        let commands = vec!["cat src/main.rs".to_string()];

        let operation = engine.analyze_operation(&commands, None).unwrap();
        assert_eq!(operation.operation_type, OperationType::ReadFile);
        assert_eq!(operation.risk_level, 1);
        assert!(operation.reversible);
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_should_auto_accept_trusted_operation (lines 659-679) ---[0m
    fn test_should_auto_accept_trusted_operation() {
        let config = AutoAcceptConfig {
            enabled: true,
            trusted_operations: vec![OperationType::ReadFile],
            ..AutoAcceptConfig::default()
        };
        let engine = AutoAcceptEngine::new(config);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Accept(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:627-634 function test_analyze_dangerous_operation <-> ./crates/ccswarm/src/auto_accept/mod.rs:637-656 function test_should_auto_accept_disabled
  Similarity: 86.54%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_analyze_dangerous_operation (lines 627-634) ---[0m
    fn test_analyze_dangerous_operation() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
        let commands = vec!["rm -rf /".to_string()];

        let operation = engine.analyze_operation(&commands, None).unwrap();
        assert_eq!(operation.operation_type, OperationType::DeleteFile);
        assert!(operation.risk_level >= 8);
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_should_auto_accept_disabled (lines 637-656) ---[0m
    fn test_should_auto_accept_disabled() {
        let config = AutoAcceptConfig {
            enabled: false,
            ..AutoAcceptConfig::default()
        };
        let engine = AutoAcceptEngine::new(config);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Reject(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:682-689 function test_pattern_matching <-> ./crates/ccswarm/src/auto_accept/mod.rs:692-714 function test_emergency_stop
  Similarity: 76.44%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_pattern_matching (lines 682-689) ---[0m
    fn test_pattern_matching() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());

        assert!(engine.matches_pattern("test.sql", "*.sql"));
        assert!(engine.matches_pattern("src/migrations/001.sql", "**/migrations/*"));
        assert!(engine.matches_pattern("Cargo.toml", "Cargo.toml"));
        assert!(!engine.matches_pattern("test.rs", "*.sql"));
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_emergency_stop (lines 692-714) ---[0m
    fn test_emergency_stop() {
        let mut engine = AutoAcceptEngine::new(AutoAcceptConfig {
            enabled: true,
            ..AutoAcceptConfig::default()
        });

        engine.emergency_stop();
        assert!(engine.config.emergency_stop);
        assert!(!engine.config.enabled);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Reject(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:637-656 function test_should_auto_accept_disabled <-> ./crates/ccswarm/src/auto_accept/mod.rs:682-689 function test_pattern_matching
  Similarity: 84.47%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_should_auto_accept_disabled (lines 637-656) ---[0m
    fn test_should_auto_accept_disabled() {
        let config = AutoAcceptConfig {
            enabled: false,
            ..AutoAcceptConfig::default()
        };
        let engine = AutoAcceptEngine::new(config);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Reject(_));
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_pattern_matching (lines 682-689) ---[0m
    fn test_pattern_matching() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());

        assert!(engine.matches_pattern("test.sql", "*.sql"));
        assert!(engine.matches_pattern("src/migrations/001.sql", "**/migrations/*"));
        assert!(engine.matches_pattern("Cargo.toml", "Cargo.toml"));
        assert!(!engine.matches_pattern("test.rs", "*.sql"));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:559-580 method extract_file_paths <-> ./crates/ccswarm/src/auto_accept/mod.rs:616-624 function test_analyze_read_operation
  Similarity: 75.49%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:extract_file_paths (lines 559-580) ---[0m
    fn extract_file_paths(&self, command: &str, paths: &mut Vec<PathBuf>) {
        // This is a simplified implementation - a more robust version would
        // properly parse shell commands and extract file arguments
        let parts: Vec<&str> = command.split_whitespace().collect();

        for part in parts {
            if part.contains('/') || part.contains('.') {
                // Likely a file path
                if let Ok(path) = std::path::Path::new(part).canonicalize() {
                    if !paths.contains(&path) {
                        paths.push(path);
                    }
                } else {
                    // Add as-is if we can't canonicalize (file might not exist yet)
                    let path = PathBuf::from(part);
                    if !paths.contains(&path) {
                        paths.push(path);
                    }
                }
            }
        }
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_analyze_read_operation (lines 616-624) ---[0m
    fn test_analyze_read_operation() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
        let commands = vec!["cat src/main.rs".to_string()];

        let operation = engine.analyze_operation(&commands, None).unwrap();
        assert_eq!(operation.operation_type, OperationType::ReadFile);
        assert_eq!(operation.risk_level, 1);
        assert!(operation.reversible);
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:616-624 function test_analyze_read_operation <-> ./crates/ccswarm/src/auto_accept/mod.rs:637-656 function test_should_auto_accept_disabled
  Similarity: 79.62%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_analyze_read_operation (lines 616-624) ---[0m
    fn test_analyze_read_operation() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
        let commands = vec!["cat src/main.rs".to_string()];

        let operation = engine.analyze_operation(&commands, None).unwrap();
        assert_eq!(operation.operation_type, OperationType::ReadFile);
        assert_eq!(operation.risk_level, 1);
        assert!(operation.reversible);
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_should_auto_accept_disabled (lines 637-656) ---[0m
    fn test_should_auto_accept_disabled() {
        let config = AutoAcceptConfig {
            enabled: false,
            ..AutoAcceptConfig::default()
        };
        let engine = AutoAcceptEngine::new(config);

        let operation = Operation {
            operation_type: OperationType::ReadFile,
            description: "Read file".to_string(),
            affected_files: vec![],
            commands: vec!["cat test.txt".to_string()],
            risk_level: 1,
            reversible: true,
            task: None,
        };

        let decision = engine.should_auto_accept(&operation).unwrap();
        matches!(decision, AutoAcceptDecision::Reject(_));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:559-580 method extract_file_paths <-> ./crates/ccswarm/src/auto_accept/mod.rs:682-689 function test_pattern_matching
  Similarity: 73.37%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:extract_file_paths (lines 559-580) ---[0m
    fn extract_file_paths(&self, command: &str, paths: &mut Vec<PathBuf>) {
        // This is a simplified implementation - a more robust version would
        // properly parse shell commands and extract file arguments
        let parts: Vec<&str> = command.split_whitespace().collect();

        for part in parts {
            if part.contains('/') || part.contains('.') {
                // Likely a file path
                if let Ok(path) = std::path::Path::new(part).canonicalize() {
                    if !paths.contains(&path) {
                        paths.push(path);
                    }
                } else {
                    // Add as-is if we can't canonicalize (file might not exist yet)
                    let path = PathBuf::from(part);
                    if !paths.contains(&path) {
                        paths.push(path);
                    }
                }
            }
        }
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_pattern_matching (lines 682-689) ---[0m
    fn test_pattern_matching() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());

        assert!(engine.matches_pattern("test.sql", "*.sql"));
        assert!(engine.matches_pattern("src/migrations/001.sql", "**/migrations/*"));
        assert!(engine.matches_pattern("Cargo.toml", "Cargo.toml"));
        assert!(!engine.matches_pattern("test.rs", "*.sql"));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:616-624 function test_analyze_read_operation <-> ./crates/ccswarm/src/auto_accept/mod.rs:627-634 function test_analyze_dangerous_operation
  Similarity: 88.21%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_analyze_read_operation (lines 616-624) ---[0m
    fn test_analyze_read_operation() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
        let commands = vec!["cat src/main.rs".to_string()];

        let operation = engine.analyze_operation(&commands, None).unwrap();
        assert_eq!(operation.operation_type, OperationType::ReadFile);
        assert_eq!(operation.risk_level, 1);
        assert!(operation.reversible);
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_analyze_dangerous_operation (lines 627-634) ---[0m
    fn test_analyze_dangerous_operation() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
        let commands = vec!["rm -rf /".to_string()];

        let operation = engine.analyze_operation(&commands, None).unwrap();
        assert_eq!(operation.operation_type, OperationType::DeleteFile);
        assert!(operation.risk_level >= 8);
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:627-634 function test_analyze_dangerous_operation <-> ./crates/ccswarm/src/auto_accept/mod.rs:682-689 function test_pattern_matching
  Similarity: 84.61%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_analyze_dangerous_operation (lines 627-634) ---[0m
    fn test_analyze_dangerous_operation() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
        let commands = vec!["rm -rf /".to_string()];

        let operation = engine.analyze_operation(&commands, None).unwrap();
        assert_eq!(operation.operation_type, OperationType::DeleteFile);
        assert!(operation.risk_level >= 8);
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_pattern_matching (lines 682-689) ---[0m
    fn test_pattern_matching() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());

        assert!(engine.matches_pattern("test.sql", "*.sql"));
        assert!(engine.matches_pattern("src/migrations/001.sql", "**/migrations/*"));
        assert!(engine.matches_pattern("Cargo.toml", "Cargo.toml"));
        assert!(!engine.matches_pattern("test.rs", "*.sql"));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:616-624 function test_analyze_read_operation <-> ./crates/ccswarm/src/auto_accept/mod.rs:682-689 function test_pattern_matching
  Similarity: 74.78%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_analyze_read_operation (lines 616-624) ---[0m
    fn test_analyze_read_operation() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
        let commands = vec!["cat src/main.rs".to_string()];

        let operation = engine.analyze_operation(&commands, None).unwrap();
        assert_eq!(operation.operation_type, OperationType::ReadFile);
        assert_eq!(operation.risk_level, 1);
        assert!(operation.reversible);
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_pattern_matching (lines 682-689) ---[0m
    fn test_pattern_matching() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());

        assert!(engine.matches_pattern("test.sql", "*.sql"));
        assert!(engine.matches_pattern("src/migrations/001.sql", "**/migrations/*"));
        assert!(engine.matches_pattern("Cargo.toml", "Cargo.toml"));
        assert!(!engine.matches_pattern("test.rs", "*.sql"));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:608-613 function test_default_config <-> ./crates/ccswarm/src/auto_accept/mod.rs:682-689 function test_pattern_matching
  Similarity: 87.91%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_default_config (lines 608-613) ---[0m
    fn test_default_config() {
        let config = AutoAcceptConfig::default();
        assert!(!config.enabled); // Should be disabled by default
        assert!(config.trusted_operations.contains(&OperationType::ReadFile));
        assert_eq!(config.max_file_changes, 5);
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_pattern_matching (lines 682-689) ---[0m
    fn test_pattern_matching() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());

        assert!(engine.matches_pattern("test.sql", "*.sql"));
        assert!(engine.matches_pattern("src/migrations/001.sql", "**/migrations/*"));
        assert!(engine.matches_pattern("Cargo.toml", "Cargo.toml"));
        assert!(!engine.matches_pattern("test.rs", "*.sql"));
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:608-613 function test_default_config <-> ./crates/ccswarm/src/auto_accept/mod.rs:627-634 function test_analyze_dangerous_operation
  Similarity: 84.00%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_default_config (lines 608-613) ---[0m
    fn test_default_config() {
        let config = AutoAcceptConfig::default();
        assert!(!config.enabled); // Should be disabled by default
        assert!(config.trusted_operations.contains(&OperationType::ReadFile));
        assert_eq!(config.max_file_changes, 5);
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_analyze_dangerous_operation (lines 627-634) ---[0m
    fn test_analyze_dangerous_operation() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
        let commands = vec!["rm -rf /".to_string()];

        let operation = engine.analyze_operation(&commands, None).unwrap();
        assert_eq!(operation.operation_type, OperationType::DeleteFile);
        assert!(operation.risk_level >= 8);
    }

  ./crates/ccswarm/src/auto_accept/mod.rs:608-613 function test_default_config <-> ./crates/ccswarm/src/auto_accept/mod.rs:616-624 function test_analyze_read_operation
  Similarity: 75.85%

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_default_config (lines 608-613) ---[0m
    fn test_default_config() {
        let config = AutoAcceptConfig::default();
        assert!(!config.enabled); // Should be disabled by default
        assert!(config.trusted_operations.contains(&OperationType::ReadFile));
        assert_eq!(config.max_file_changes, 5);
    }

[36m--- ./crates/ccswarm/src/auto_accept/mod.rs:test_analyze_read_operation (lines 616-624) ---[0m
    fn test_analyze_read_operation() {
        let engine = AutoAcceptEngine::new(AutoAcceptConfig::default());
        let commands = vec!["cat src/main.rs".to_string()];

        let operation = engine.analyze_operation(&commands, None).unwrap();
        assert_eq!(operation.operation_type, OperationType::ReadFile);
        assert_eq!(operation.risk_level, 1);
        assert!(operation.reversible);
    }


Duplicates in ./crates/ccswarm/src/template/manager.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/template/manager.rs:195-226 method validate_variable_definition <-> ./crates/ccswarm/src/template/manager.rs:327-377 method substitute_variables
  Similarity: 93.16%

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_variable_definition (lines 195-226) ---[0m
    fn validate_variable_definition(
        &self,
        variable: &TemplateVariable,
    ) -> Result<(), TemplateError> {
        // Check name format
        if variable.name.is_empty()
            || !variable
                .name
                .chars()
                .all(|c| c.is_alphanumeric() || c == '_')
        {
            return Err(TemplateError::ValidationFailed {
                reason: format!("Invalid variable name: '{}'", variable.name),
            });
        }

        // Validate choice options
        if let VariableType::Choice(ref choices) = variable.variable_type {
            if choices.is_empty() {
                return Err(TemplateError::ValidationFailed {
                    reason: format!("Choice variable '{}' has no options", variable.name),
                });
            }
        }

        // Validate default value against type
        if let Some(ref default) = variable.default_value {
            self.validate_variable_value(&variable.name, default, &variable.variable_type)?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:substitute_variables (lines 327-377) ---[0m
    fn substitute_variables(
        &mut self,
        text: &str,
        template: &Template,
        context: &TemplateContext,
    ) -> Result<String, TemplateError> {
        let var_regex = if let Some(regex) = self.regex_cache.get("variable") {
            regex.clone()
        } else {
            let regex = Regex::new(r"\{\{(\w+)\}\}").unwrap();
            self.regex_cache
                .insert("variable".to_string(), regex.clone());
            regex
        };

        let mut result = text.to_string();

        for capture in var_regex.captures_iter(text) {
            let var_name = capture[1].to_string();
            let placeholder = &capture[0];

            // Find variable definition
            let variable = template
                .variables
                .iter()
                .find(|v| v.name == var_name)
                .ok_or_else(|| TemplateError::SubstitutionFailed {
                    variable: var_name.clone(),
                })?;

            // Get value from context or use default
            let value = context
                .variables
                .get(&var_name)
                .or(variable.default_value.as_ref())
                .ok_or_else(|| TemplateError::SubstitutionFailed {
                    variable: var_name.clone(),
                })?;

            // Apply variable-specific transformations
            let transformed_value =
                self.transform_variable_value(value, &variable.variable_type)?;

            result = result.replace(placeholder, &transformed_value);
        }

        // Handle built-in variables
        result = self.substitute_builtin_variables(&result, context)?;

        Ok(result)
    }

  ./crates/ccswarm/src/template/manager.rs:62-100 method apply_template <-> ./crates/ccswarm/src/template/manager.rs:327-377 method substitute_variables
  Similarity: 84.05%

[36m--- ./crates/ccswarm/src/template/manager.rs:apply_template (lines 62-100) ---[0m
    pub async fn apply_template(
        &mut self,
        template_id: &str,
        context: TemplateContext,
    ) -> Result<AppliedTemplate, TemplateError> {
        let template = self.load_template(template_id).await?;

        // Validate that all required variables are provided
        self.validate_context(&template, &context)?;

        // Substitute variables in template
        let description =
            self.substitute_variables(&template.task_description, &template, &context)?;
        let details = if let Some(ref details_template) = template.task_details {
            Some(self.substitute_variables(details_template, &template, &context)?)
        } else {
            None
        };

        // Substitute variables in target files
        let target_files = template
            .target_files
            .iter()
            .map(|file| self.substitute_variables(file, &template, &context))
            .collect::<Result<Vec<_>, _>>()?;

        // Update usage statistics
        self.storage.update_usage(&template.id, true).await?;

        Ok(AppliedTemplate {
            description,
            details,
            priority: template.default_priority,
            task_type: template.default_task_type,
            estimated_duration: template.estimated_duration,
            target_files,
            metadata: template.metadata.clone(),
        })
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:substitute_variables (lines 327-377) ---[0m
    fn substitute_variables(
        &mut self,
        text: &str,
        template: &Template,
        context: &TemplateContext,
    ) -> Result<String, TemplateError> {
        let var_regex = if let Some(regex) = self.regex_cache.get("variable") {
            regex.clone()
        } else {
            let regex = Regex::new(r"\{\{(\w+)\}\}").unwrap();
            self.regex_cache
                .insert("variable".to_string(), regex.clone());
            regex
        };

        let mut result = text.to_string();

        for capture in var_regex.captures_iter(text) {
            let var_name = capture[1].to_string();
            let placeholder = &capture[0];

            // Find variable definition
            let variable = template
                .variables
                .iter()
                .find(|v| v.name == var_name)
                .ok_or_else(|| TemplateError::SubstitutionFailed {
                    variable: var_name.clone(),
                })?;

            // Get value from context or use default
            let value = context
                .variables
                .get(&var_name)
                .or(variable.default_value.as_ref())
                .ok_or_else(|| TemplateError::SubstitutionFailed {
                    variable: var_name.clone(),
                })?;

            // Apply variable-specific transformations
            let transformed_value =
                self.transform_variable_value(value, &variable.variable_type)?;

            result = result.replace(placeholder, &transformed_value);
        }

        // Handle built-in variables
        result = self.substitute_builtin_variables(&result, context)?;

        Ok(result)
    }

  ./crates/ccswarm/src/template/manager.rs:327-377 method substitute_variables <-> ./crates/ccswarm/src/template/manager.rs:454-479 method import_templates
  Similarity: 89.62%

[36m--- ./crates/ccswarm/src/template/manager.rs:substitute_variables (lines 327-377) ---[0m
    fn substitute_variables(
        &mut self,
        text: &str,
        template: &Template,
        context: &TemplateContext,
    ) -> Result<String, TemplateError> {
        let var_regex = if let Some(regex) = self.regex_cache.get("variable") {
            regex.clone()
        } else {
            let regex = Regex::new(r"\{\{(\w+)\}\}").unwrap();
            self.regex_cache
                .insert("variable".to_string(), regex.clone());
            regex
        };

        let mut result = text.to_string();

        for capture in var_regex.captures_iter(text) {
            let var_name = capture[1].to_string();
            let placeholder = &capture[0];

            // Find variable definition
            let variable = template
                .variables
                .iter()
                .find(|v| v.name == var_name)
                .ok_or_else(|| TemplateError::SubstitutionFailed {
                    variable: var_name.clone(),
                })?;

            // Get value from context or use default
            let value = context
                .variables
                .get(&var_name)
                .or(variable.default_value.as_ref())
                .ok_or_else(|| TemplateError::SubstitutionFailed {
                    variable: var_name.clone(),
                })?;

            // Apply variable-specific transformations
            let transformed_value =
                self.transform_variable_value(value, &variable.variable_type)?;

            result = result.replace(placeholder, &transformed_value);
        }

        // Handle built-in variables
        result = self.substitute_builtin_variables(&result, context)?;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:import_templates (lines 454-479) ---[0m
    pub async fn import_templates(
        &mut self,
        templates: Vec<Template>,
    ) -> Result<Vec<String>, TemplateError> {
        let mut imported = Vec::new();
        let mut errors = Vec::new();

        for template in templates {
            match self.save_template(template.clone()).await {
                Ok(()) => {
                    imported.push(template.id.clone());
                    info!("Imported template: {}", template.id);
                }
                Err(e) => {
                    warn!("Failed to import template {}: {}", template.id, e);
                    errors.push(format!("{}: {}", template.id, e));
                }
            }
        }

        if !errors.is_empty() {
            warn!("Failed to import {} templates: {:?}", errors.len(), errors);
        }

        Ok(imported)
    }

  ./crates/ccswarm/src/template/manager.rs:146-170 method validate_template <-> ./crates/ccswarm/src/template/manager.rs:327-377 method substitute_variables
  Similarity: 86.97%

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_template (lines 146-170) ---[0m
    fn validate_template(&self, template: &Template) -> Result<(), TemplateError> {
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template is missing required fields".to_string(),
            });
        }

        // Validate variable references in template
        self.validate_variable_references(&template.task_description, &template.variables)?;

        if let Some(ref details) = template.task_details {
            self.validate_variable_references(details, &template.variables)?;
        }

        for file in &template.target_files {
            self.validate_variable_references(file, &template.variables)?;
        }

        // Validate variable definitions
        for variable in &template.variables {
            self.validate_variable_definition(variable)?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:substitute_variables (lines 327-377) ---[0m
    fn substitute_variables(
        &mut self,
        text: &str,
        template: &Template,
        context: &TemplateContext,
    ) -> Result<String, TemplateError> {
        let var_regex = if let Some(regex) = self.regex_cache.get("variable") {
            regex.clone()
        } else {
            let regex = Regex::new(r"\{\{(\w+)\}\}").unwrap();
            self.regex_cache
                .insert("variable".to_string(), regex.clone());
            regex
        };

        let mut result = text.to_string();

        for capture in var_regex.captures_iter(text) {
            let var_name = capture[1].to_string();
            let placeholder = &capture[0];

            // Find variable definition
            let variable = template
                .variables
                .iter()
                .find(|v| v.name == var_name)
                .ok_or_else(|| TemplateError::SubstitutionFailed {
                    variable: var_name.clone(),
                })?;

            // Get value from context or use default
            let value = context
                .variables
                .get(&var_name)
                .or(variable.default_value.as_ref())
                .ok_or_else(|| TemplateError::SubstitutionFailed {
                    variable: var_name.clone(),
                })?;

            // Apply variable-specific transformations
            let transformed_value =
                self.transform_variable_value(value, &variable.variable_type)?;

            result = result.replace(placeholder, &transformed_value);
        }

        // Handle built-in variables
        result = self.substitute_builtin_variables(&result, context)?;

        Ok(result)
    }

  ./crates/ccswarm/src/template/manager.rs:327-377 method substitute_variables <-> ./crates/ccswarm/src/template/manager.rs:404-431 method substitute_builtin_variables
  Similarity: 79.54%

[36m--- ./crates/ccswarm/src/template/manager.rs:substitute_variables (lines 327-377) ---[0m
    fn substitute_variables(
        &mut self,
        text: &str,
        template: &Template,
        context: &TemplateContext,
    ) -> Result<String, TemplateError> {
        let var_regex = if let Some(regex) = self.regex_cache.get("variable") {
            regex.clone()
        } else {
            let regex = Regex::new(r"\{\{(\w+)\}\}").unwrap();
            self.regex_cache
                .insert("variable".to_string(), regex.clone());
            regex
        };

        let mut result = text.to_string();

        for capture in var_regex.captures_iter(text) {
            let var_name = capture[1].to_string();
            let placeholder = &capture[0];

            // Find variable definition
            let variable = template
                .variables
                .iter()
                .find(|v| v.name == var_name)
                .ok_or_else(|| TemplateError::SubstitutionFailed {
                    variable: var_name.clone(),
                })?;

            // Get value from context or use default
            let value = context
                .variables
                .get(&var_name)
                .or(variable.default_value.as_ref())
                .ok_or_else(|| TemplateError::SubstitutionFailed {
                    variable: var_name.clone(),
                })?;

            // Apply variable-specific transformations
            let transformed_value =
                self.transform_variable_value(value, &variable.variable_type)?;

            result = result.replace(placeholder, &transformed_value);
        }

        // Handle built-in variables
        result = self.substitute_builtin_variables(&result, context)?;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:substitute_builtin_variables (lines 404-431) ---[0m
    fn substitute_builtin_variables(
        &self,
        text: &str,
        context: &TemplateContext,
    ) -> Result<String, TemplateError> {
        let mut result = text.to_string();

        // Project name
        if let Some(ref project_name) = context.project_name {
            result = result.replace("{{project_name}}", project_name);
        }

        // Agent role
        if let Some(ref agent_role) = context.agent_role {
            result = result.replace("{{agent_role}}", agent_role);
        }

        // Current date/time
        let now = chrono::Utc::now();
        result = result.replace("{{current_date}}", &now.format("%Y-%m-%d").to_string());
        result = result.replace("{{current_time}}", &now.format("%H:%M:%S").to_string());
        result = result.replace(
            "{{current_datetime}}",
            &now.format("%Y-%m-%d %H:%M:%S").to_string(),
        );

        Ok(result)
    }

  ./crates/ccswarm/src/template/manager.rs:62-100 method apply_template <-> ./crates/ccswarm/src/template/manager.rs:404-431 method substitute_builtin_variables
  Similarity: 89.51%

[36m--- ./crates/ccswarm/src/template/manager.rs:apply_template (lines 62-100) ---[0m
    pub async fn apply_template(
        &mut self,
        template_id: &str,
        context: TemplateContext,
    ) -> Result<AppliedTemplate, TemplateError> {
        let template = self.load_template(template_id).await?;

        // Validate that all required variables are provided
        self.validate_context(&template, &context)?;

        // Substitute variables in template
        let description =
            self.substitute_variables(&template.task_description, &template, &context)?;
        let details = if let Some(ref details_template) = template.task_details {
            Some(self.substitute_variables(details_template, &template, &context)?)
        } else {
            None
        };

        // Substitute variables in target files
        let target_files = template
            .target_files
            .iter()
            .map(|file| self.substitute_variables(file, &template, &context))
            .collect::<Result<Vec<_>, _>>()?;

        // Update usage statistics
        self.storage.update_usage(&template.id, true).await?;

        Ok(AppliedTemplate {
            description,
            details,
            priority: template.default_priority,
            task_type: template.default_task_type,
            estimated_duration: template.estimated_duration,
            target_files,
            metadata: template.metadata.clone(),
        })
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:substitute_builtin_variables (lines 404-431) ---[0m
    fn substitute_builtin_variables(
        &self,
        text: &str,
        context: &TemplateContext,
    ) -> Result<String, TemplateError> {
        let mut result = text.to_string();

        // Project name
        if let Some(ref project_name) = context.project_name {
            result = result.replace("{{project_name}}", project_name);
        }

        // Agent role
        if let Some(ref agent_role) = context.agent_role {
            result = result.replace("{{agent_role}}", agent_role);
        }

        // Current date/time
        let now = chrono::Utc::now();
        result = result.replace("{{current_date}}", &now.format("%Y-%m-%d").to_string());
        result = result.replace("{{current_time}}", &now.format("%H:%M:%S").to_string());
        result = result.replace(
            "{{current_datetime}}",
            &now.format("%Y-%m-%d %H:%M:%S").to_string(),
        );

        Ok(result)
    }

  ./crates/ccswarm/src/template/manager.rs:62-100 method apply_template <-> ./crates/ccswarm/src/template/manager.rs:195-226 method validate_variable_definition
  Similarity: 82.02%

[36m--- ./crates/ccswarm/src/template/manager.rs:apply_template (lines 62-100) ---[0m
    pub async fn apply_template(
        &mut self,
        template_id: &str,
        context: TemplateContext,
    ) -> Result<AppliedTemplate, TemplateError> {
        let template = self.load_template(template_id).await?;

        // Validate that all required variables are provided
        self.validate_context(&template, &context)?;

        // Substitute variables in template
        let description =
            self.substitute_variables(&template.task_description, &template, &context)?;
        let details = if let Some(ref details_template) = template.task_details {
            Some(self.substitute_variables(details_template, &template, &context)?)
        } else {
            None
        };

        // Substitute variables in target files
        let target_files = template
            .target_files
            .iter()
            .map(|file| self.substitute_variables(file, &template, &context))
            .collect::<Result<Vec<_>, _>>()?;

        // Update usage statistics
        self.storage.update_usage(&template.id, true).await?;

        Ok(AppliedTemplate {
            description,
            details,
            priority: template.default_priority,
            task_type: template.default_task_type,
            estimated_duration: template.estimated_duration,
            target_files,
            metadata: template.metadata.clone(),
        })
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_variable_definition (lines 195-226) ---[0m
    fn validate_variable_definition(
        &self,
        variable: &TemplateVariable,
    ) -> Result<(), TemplateError> {
        // Check name format
        if variable.name.is_empty()
            || !variable
                .name
                .chars()
                .all(|c| c.is_alphanumeric() || c == '_')
        {
            return Err(TemplateError::ValidationFailed {
                reason: format!("Invalid variable name: '{}'", variable.name),
            });
        }

        // Validate choice options
        if let VariableType::Choice(ref choices) = variable.variable_type {
            if choices.is_empty() {
                return Err(TemplateError::ValidationFailed {
                    reason: format!("Choice variable '{}' has no options", variable.name),
                });
            }
        }

        // Validate default value against type
        if let Some(ref default) = variable.default_value {
            self.validate_variable_value(&variable.name, default, &variable.variable_type)?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/template/manager.rs:62-100 method apply_template <-> ./crates/ccswarm/src/template/manager.rs:487-513 method clone_template
  Similarity: 88.12%

[36m--- ./crates/ccswarm/src/template/manager.rs:apply_template (lines 62-100) ---[0m
    pub async fn apply_template(
        &mut self,
        template_id: &str,
        context: TemplateContext,
    ) -> Result<AppliedTemplate, TemplateError> {
        let template = self.load_template(template_id).await?;

        // Validate that all required variables are provided
        self.validate_context(&template, &context)?;

        // Substitute variables in template
        let description =
            self.substitute_variables(&template.task_description, &template, &context)?;
        let details = if let Some(ref details_template) = template.task_details {
            Some(self.substitute_variables(details_template, &template, &context)?)
        } else {
            None
        };

        // Substitute variables in target files
        let target_files = template
            .target_files
            .iter()
            .map(|file| self.substitute_variables(file, &template, &context))
            .collect::<Result<Vec<_>, _>>()?;

        // Update usage statistics
        self.storage.update_usage(&template.id, true).await?;

        Ok(AppliedTemplate {
            description,
            details,
            priority: template.default_priority,
            task_type: template.default_task_type,
            estimated_duration: template.estimated_duration,
            target_files,
            metadata: template.metadata.clone(),
        })
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:clone_template (lines 487-513) ---[0m
    pub async fn clone_template(
        &mut self,
        source_id: &str,
        new_id: &str,
        new_name: Option<String>,
    ) -> Result<Template, TemplateError> {
        let mut template = self.load_template(source_id).await?;

        // Update ID and name
        template.id = new_id.to_string();
        if let Some(name) = new_name {
            template.name = name;
        } else {
            template.name = format!("{} (Copy)", template.name);
        }

        // Reset statistics
        template.usage_count = 0;
        template.success_rate = None;
        template.created_at = chrono::Utc::now();
        template.updated_at = chrono::Utc::now();

        // Save cloned template
        self.save_template(template.clone()).await?;

        Ok(template)
    }

  ./crates/ccswarm/src/template/manager.rs:62-100 method apply_template <-> ./crates/ccswarm/src/template/manager.rs:146-170 method validate_template
  Similarity: 82.72%

[36m--- ./crates/ccswarm/src/template/manager.rs:apply_template (lines 62-100) ---[0m
    pub async fn apply_template(
        &mut self,
        template_id: &str,
        context: TemplateContext,
    ) -> Result<AppliedTemplate, TemplateError> {
        let template = self.load_template(template_id).await?;

        // Validate that all required variables are provided
        self.validate_context(&template, &context)?;

        // Substitute variables in template
        let description =
            self.substitute_variables(&template.task_description, &template, &context)?;
        let details = if let Some(ref details_template) = template.task_details {
            Some(self.substitute_variables(details_template, &template, &context)?)
        } else {
            None
        };

        // Substitute variables in target files
        let target_files = template
            .target_files
            .iter()
            .map(|file| self.substitute_variables(file, &template, &context))
            .collect::<Result<Vec<_>, _>>()?;

        // Update usage statistics
        self.storage.update_usage(&template.id, true).await?;

        Ok(AppliedTemplate {
            description,
            details,
            priority: template.default_priority,
            task_type: template.default_task_type,
            estimated_duration: template.estimated_duration,
            target_files,
            metadata: template.metadata.clone(),
        })
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_template (lines 146-170) ---[0m
    fn validate_template(&self, template: &Template) -> Result<(), TemplateError> {
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template is missing required fields".to_string(),
            });
        }

        // Validate variable references in template
        self.validate_variable_references(&template.task_description, &template.variables)?;

        if let Some(ref details) = template.task_details {
            self.validate_variable_references(details, &template.variables)?;
        }

        for file in &template.target_files {
            self.validate_variable_references(file, &template.variables)?;
        }

        // Validate variable definitions
        for variable in &template.variables {
            self.validate_variable_definition(variable)?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/template/manager.rs:146-170 method validate_template <-> ./crates/ccswarm/src/template/manager.rs:195-226 method validate_variable_definition
  Similarity: 89.30%

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_template (lines 146-170) ---[0m
    fn validate_template(&self, template: &Template) -> Result<(), TemplateError> {
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template is missing required fields".to_string(),
            });
        }

        // Validate variable references in template
        self.validate_variable_references(&template.task_description, &template.variables)?;

        if let Some(ref details) = template.task_details {
            self.validate_variable_references(details, &template.variables)?;
        }

        for file in &template.target_files {
            self.validate_variable_references(file, &template.variables)?;
        }

        // Validate variable definitions
        for variable in &template.variables {
            self.validate_variable_definition(variable)?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_variable_definition (lines 195-226) ---[0m
    fn validate_variable_definition(
        &self,
        variable: &TemplateVariable,
    ) -> Result<(), TemplateError> {
        // Check name format
        if variable.name.is_empty()
            || !variable
                .name
                .chars()
                .all(|c| c.is_alphanumeric() || c == '_')
        {
            return Err(TemplateError::ValidationFailed {
                reason: format!("Invalid variable name: '{}'", variable.name),
            });
        }

        // Validate choice options
        if let VariableType::Choice(ref choices) = variable.variable_type {
            if choices.is_empty() {
                return Err(TemplateError::ValidationFailed {
                    reason: format!("Choice variable '{}' has no options", variable.name),
                });
            }
        }

        // Validate default value against type
        if let Some(ref default) = variable.default_value {
            self.validate_variable_value(&variable.name, default, &variable.variable_type)?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/template/manager.rs:62-100 method apply_template <-> ./crates/ccswarm/src/template/manager.rs:454-479 method import_templates
  Similarity: 75.47%

[36m--- ./crates/ccswarm/src/template/manager.rs:apply_template (lines 62-100) ---[0m
    pub async fn apply_template(
        &mut self,
        template_id: &str,
        context: TemplateContext,
    ) -> Result<AppliedTemplate, TemplateError> {
        let template = self.load_template(template_id).await?;

        // Validate that all required variables are provided
        self.validate_context(&template, &context)?;

        // Substitute variables in template
        let description =
            self.substitute_variables(&template.task_description, &template, &context)?;
        let details = if let Some(ref details_template) = template.task_details {
            Some(self.substitute_variables(details_template, &template, &context)?)
        } else {
            None
        };

        // Substitute variables in target files
        let target_files = template
            .target_files
            .iter()
            .map(|file| self.substitute_variables(file, &template, &context))
            .collect::<Result<Vec<_>, _>>()?;

        // Update usage statistics
        self.storage.update_usage(&template.id, true).await?;

        Ok(AppliedTemplate {
            description,
            details,
            priority: template.default_priority,
            task_type: template.default_task_type,
            estimated_duration: template.estimated_duration,
            target_files,
            metadata: template.metadata.clone(),
        })
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:import_templates (lines 454-479) ---[0m
    pub async fn import_templates(
        &mut self,
        templates: Vec<Template>,
    ) -> Result<Vec<String>, TemplateError> {
        let mut imported = Vec::new();
        let mut errors = Vec::new();

        for template in templates {
            match self.save_template(template.clone()).await {
                Ok(()) => {
                    imported.push(template.id.clone());
                    info!("Imported template: {}", template.id);
                }
                Err(e) => {
                    warn!("Failed to import template {}: {}", template.id, e);
                    errors.push(format!("{}: {}", template.id, e));
                }
            }
        }

        if !errors.is_empty() {
            warn!("Failed to import {} templates: {:?}", errors.len(), errors);
        }

        Ok(imported)
    }

  ./crates/ccswarm/src/template/manager.rs:195-226 method validate_variable_definition <-> ./crates/ccswarm/src/template/manager.rs:454-479 method import_templates
  Similarity: 84.01%

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_variable_definition (lines 195-226) ---[0m
    fn validate_variable_definition(
        &self,
        variable: &TemplateVariable,
    ) -> Result<(), TemplateError> {
        // Check name format
        if variable.name.is_empty()
            || !variable
                .name
                .chars()
                .all(|c| c.is_alphanumeric() || c == '_')
        {
            return Err(TemplateError::ValidationFailed {
                reason: format!("Invalid variable name: '{}'", variable.name),
            });
        }

        // Validate choice options
        if let VariableType::Choice(ref choices) = variable.variable_type {
            if choices.is_empty() {
                return Err(TemplateError::ValidationFailed {
                    reason: format!("Choice variable '{}' has no options", variable.name),
                });
            }
        }

        // Validate default value against type
        if let Some(ref default) = variable.default_value {
            self.validate_variable_value(&variable.name, default, &variable.variable_type)?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:import_templates (lines 454-479) ---[0m
    pub async fn import_templates(
        &mut self,
        templates: Vec<Template>,
    ) -> Result<Vec<String>, TemplateError> {
        let mut imported = Vec::new();
        let mut errors = Vec::new();

        for template in templates {
            match self.save_template(template.clone()).await {
                Ok(()) => {
                    imported.push(template.id.clone());
                    info!("Imported template: {}", template.id);
                }
                Err(e) => {
                    warn!("Failed to import template {}: {}", template.id, e);
                    errors.push(format!("{}: {}", template.id, e));
                }
            }
        }

        if !errors.is_empty() {
            warn!("Failed to import {} templates: {:?}", errors.len(), errors);
        }

        Ok(imported)
    }

  ./crates/ccswarm/src/template/manager.rs:195-226 method validate_variable_definition <-> ./crates/ccswarm/src/template/manager.rs:404-431 method substitute_builtin_variables
  Similarity: 79.79%

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_variable_definition (lines 195-226) ---[0m
    fn validate_variable_definition(
        &self,
        variable: &TemplateVariable,
    ) -> Result<(), TemplateError> {
        // Check name format
        if variable.name.is_empty()
            || !variable
                .name
                .chars()
                .all(|c| c.is_alphanumeric() || c == '_')
        {
            return Err(TemplateError::ValidationFailed {
                reason: format!("Invalid variable name: '{}'", variable.name),
            });
        }

        // Validate choice options
        if let VariableType::Choice(ref choices) = variable.variable_type {
            if choices.is_empty() {
                return Err(TemplateError::ValidationFailed {
                    reason: format!("Choice variable '{}' has no options", variable.name),
                });
            }
        }

        // Validate default value against type
        if let Some(ref default) = variable.default_value {
            self.validate_variable_value(&variable.name, default, &variable.variable_type)?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:substitute_builtin_variables (lines 404-431) ---[0m
    fn substitute_builtin_variables(
        &self,
        text: &str,
        context: &TemplateContext,
    ) -> Result<String, TemplateError> {
        let mut result = text.to_string();

        // Project name
        if let Some(ref project_name) = context.project_name {
            result = result.replace("{{project_name}}", project_name);
        }

        // Agent role
        if let Some(ref agent_role) = context.agent_role {
            result = result.replace("{{agent_role}}", agent_role);
        }

        // Current date/time
        let now = chrono::Utc::now();
        result = result.replace("{{current_date}}", &now.format("%Y-%m-%d").to_string());
        result = result.replace("{{current_time}}", &now.format("%H:%M:%S").to_string());
        result = result.replace(
            "{{current_datetime}}",
            &now.format("%Y-%m-%d %H:%M:%S").to_string(),
        );

        Ok(result)
    }

  ./crates/ccswarm/src/template/manager.rs:195-226 method validate_variable_definition <-> ./crates/ccswarm/src/template/manager.rs:487-513 method clone_template
  Similarity: 79.16%

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_variable_definition (lines 195-226) ---[0m
    fn validate_variable_definition(
        &self,
        variable: &TemplateVariable,
    ) -> Result<(), TemplateError> {
        // Check name format
        if variable.name.is_empty()
            || !variable
                .name
                .chars()
                .all(|c| c.is_alphanumeric() || c == '_')
        {
            return Err(TemplateError::ValidationFailed {
                reason: format!("Invalid variable name: '{}'", variable.name),
            });
        }

        // Validate choice options
        if let VariableType::Choice(ref choices) = variable.variable_type {
            if choices.is_empty() {
                return Err(TemplateError::ValidationFailed {
                    reason: format!("Choice variable '{}' has no options", variable.name),
                });
            }
        }

        // Validate default value against type
        if let Some(ref default) = variable.default_value {
            self.validate_variable_value(&variable.name, default, &variable.variable_type)?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:clone_template (lines 487-513) ---[0m
    pub async fn clone_template(
        &mut self,
        source_id: &str,
        new_id: &str,
        new_name: Option<String>,
    ) -> Result<Template, TemplateError> {
        let mut template = self.load_template(source_id).await?;

        // Update ID and name
        template.id = new_id.to_string();
        if let Some(name) = new_name {
            template.name = name;
        } else {
            template.name = format!("{} (Copy)", template.name);
        }

        // Reset statistics
        template.usage_count = 0;
        template.success_rate = None;
        template.created_at = chrono::Utc::now();
        template.updated_at = chrono::Utc::now();

        // Save cloned template
        self.save_template(template.clone()).await?;

        Ok(template)
    }

  ./crates/ccswarm/src/template/manager.rs:404-431 method substitute_builtin_variables <-> ./crates/ccswarm/src/template/manager.rs:487-513 method clone_template
  Similarity: 84.60%

[36m--- ./crates/ccswarm/src/template/manager.rs:substitute_builtin_variables (lines 404-431) ---[0m
    fn substitute_builtin_variables(
        &self,
        text: &str,
        context: &TemplateContext,
    ) -> Result<String, TemplateError> {
        let mut result = text.to_string();

        // Project name
        if let Some(ref project_name) = context.project_name {
            result = result.replace("{{project_name}}", project_name);
        }

        // Agent role
        if let Some(ref agent_role) = context.agent_role {
            result = result.replace("{{agent_role}}", agent_role);
        }

        // Current date/time
        let now = chrono::Utc::now();
        result = result.replace("{{current_date}}", &now.format("%Y-%m-%d").to_string());
        result = result.replace("{{current_time}}", &now.format("%H:%M:%S").to_string());
        result = result.replace(
            "{{current_datetime}}",
            &now.format("%Y-%m-%d %H:%M:%S").to_string(),
        );

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:clone_template (lines 487-513) ---[0m
    pub async fn clone_template(
        &mut self,
        source_id: &str,
        new_id: &str,
        new_name: Option<String>,
    ) -> Result<Template, TemplateError> {
        let mut template = self.load_template(source_id).await?;

        // Update ID and name
        template.id = new_id.to_string();
        if let Some(name) = new_name {
            template.name = name;
        } else {
            template.name = format!("{} (Copy)", template.name);
        }

        // Reset statistics
        template.usage_count = 0;
        template.success_rate = None;
        template.created_at = chrono::Utc::now();
        template.updated_at = chrono::Utc::now();

        // Save cloned template
        self.save_template(template.clone()).await?;

        Ok(template)
    }

  ./crates/ccswarm/src/template/manager.rs:103-119 method get_template_by_name <-> ./crates/ccswarm/src/template/manager.rs:195-226 method validate_variable_definition
  Similarity: 90.26%

[36m--- ./crates/ccswarm/src/template/manager.rs:get_template_by_name (lines 103-119) ---[0m
    pub async fn get_template_by_name(&self, name: &str) -> Result<Template, TemplateError> {
        // First try to load by ID
        if let Ok(template) = self.load_template(name).await {
            return Ok(template);
        }

        // Search by name
        let query = TemplateQuery::new().with_search_term(name).with_limit(1);
        let templates = self.search_templates(query).await?;

        templates
            .into_iter()
            .next()
            .ok_or_else(|| TemplateError::NotFound {
                name: name.to_string(),
            })
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_variable_definition (lines 195-226) ---[0m
    fn validate_variable_definition(
        &self,
        variable: &TemplateVariable,
    ) -> Result<(), TemplateError> {
        // Check name format
        if variable.name.is_empty()
            || !variable
                .name
                .chars()
                .all(|c| c.is_alphanumeric() || c == '_')
        {
            return Err(TemplateError::ValidationFailed {
                reason: format!("Invalid variable name: '{}'", variable.name),
            });
        }

        // Validate choice options
        if let VariableType::Choice(ref choices) = variable.variable_type {
            if choices.is_empty() {
                return Err(TemplateError::ValidationFailed {
                    reason: format!("Choice variable '{}' has no options", variable.name),
                });
            }
        }

        // Validate default value against type
        if let Some(ref default) = variable.default_value {
            self.validate_variable_value(&variable.name, default, &variable.variable_type)?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/template/manager.rs:195-226 method validate_variable_definition <-> ./crates/ccswarm/src/template/manager.rs:439-451 method update_template
  Similarity: 90.70%

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_variable_definition (lines 195-226) ---[0m
    fn validate_variable_definition(
        &self,
        variable: &TemplateVariable,
    ) -> Result<(), TemplateError> {
        // Check name format
        if variable.name.is_empty()
            || !variable
                .name
                .chars()
                .all(|c| c.is_alphanumeric() || c == '_')
        {
            return Err(TemplateError::ValidationFailed {
                reason: format!("Invalid variable name: '{}'", variable.name),
            });
        }

        // Validate choice options
        if let VariableType::Choice(ref choices) = variable.variable_type {
            if choices.is_empty() {
                return Err(TemplateError::ValidationFailed {
                    reason: format!("Choice variable '{}' has no options", variable.name),
                });
            }
        }

        // Validate default value against type
        if let Some(ref default) = variable.default_value {
            self.validate_variable_value(&variable.name, default, &variable.variable_type)?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:update_template (lines 439-451) ---[0m
    pub async fn update_template(&mut self, template: Template) -> Result<(), TemplateError> {
        self.validate_template(&template)?;

        // Delete existing template
        if self.storage.exists(&template.id).await? {
            self.storage.delete_template(&template.id).await?;
        }

        // Save updated template
        self.storage.save_template(&template).await?;
        info!("Updated template: {} ({})", template.name, template.id);
        Ok(())
    }

  ./crates/ccswarm/src/template/manager.rs:146-170 method validate_template <-> ./crates/ccswarm/src/template/manager.rs:404-431 method substitute_builtin_variables
  Similarity: 76.32%

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_template (lines 146-170) ---[0m
    fn validate_template(&self, template: &Template) -> Result<(), TemplateError> {
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template is missing required fields".to_string(),
            });
        }

        // Validate variable references in template
        self.validate_variable_references(&template.task_description, &template.variables)?;

        if let Some(ref details) = template.task_details {
            self.validate_variable_references(details, &template.variables)?;
        }

        for file in &template.target_files {
            self.validate_variable_references(file, &template.variables)?;
        }

        // Validate variable definitions
        for variable in &template.variables {
            self.validate_variable_definition(variable)?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:substitute_builtin_variables (lines 404-431) ---[0m
    fn substitute_builtin_variables(
        &self,
        text: &str,
        context: &TemplateContext,
    ) -> Result<String, TemplateError> {
        let mut result = text.to_string();

        // Project name
        if let Some(ref project_name) = context.project_name {
            result = result.replace("{{project_name}}", project_name);
        }

        // Agent role
        if let Some(ref agent_role) = context.agent_role {
            result = result.replace("{{agent_role}}", agent_role);
        }

        // Current date/time
        let now = chrono::Utc::now();
        result = result.replace("{{current_date}}", &now.format("%Y-%m-%d").to_string());
        result = result.replace("{{current_time}}", &now.format("%H:%M:%S").to_string());
        result = result.replace(
            "{{current_datetime}}",
            &now.format("%Y-%m-%d %H:%M:%S").to_string(),
        );

        Ok(result)
    }

  ./crates/ccswarm/src/template/manager.rs:173-192 method validate_variable_references <-> ./crates/ccswarm/src/template/manager.rs:195-226 method validate_variable_definition
  Similarity: 77.31%

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_variable_references (lines 173-192) ---[0m
    fn validate_variable_references(
        &self,
        text: &str,
        variables: &[TemplateVariable],
    ) -> Result<(), TemplateError> {
        let var_regex = Regex::new(r"\{\{(\w+)\}\}").unwrap();
        let defined_vars: std::collections::HashSet<_> =
            variables.iter().map(|v| &v.name).collect();

        for capture in var_regex.captures_iter(text) {
            let var_name = capture[1].to_string();
            if !defined_vars.contains(&var_name) {
                return Err(TemplateError::ValidationFailed {
                    reason: format!("Variable '{}' is referenced but not defined", var_name),
                });
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_variable_definition (lines 195-226) ---[0m
    fn validate_variable_definition(
        &self,
        variable: &TemplateVariable,
    ) -> Result<(), TemplateError> {
        // Check name format
        if variable.name.is_empty()
            || !variable
                .name
                .chars()
                .all(|c| c.is_alphanumeric() || c == '_')
        {
            return Err(TemplateError::ValidationFailed {
                reason: format!("Invalid variable name: '{}'", variable.name),
            });
        }

        // Validate choice options
        if let VariableType::Choice(ref choices) = variable.variable_type {
            if choices.is_empty() {
                return Err(TemplateError::ValidationFailed {
                    reason: format!("Choice variable '{}' has no options", variable.name),
                });
            }
        }

        // Validate default value against type
        if let Some(ref default) = variable.default_value {
            self.validate_variable_value(&variable.name, default, &variable.variable_type)?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/template/manager.rs:173-192 method validate_variable_references <-> ./crates/ccswarm/src/template/manager.rs:454-479 method import_templates
  Similarity: 83.74%

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_variable_references (lines 173-192) ---[0m
    fn validate_variable_references(
        &self,
        text: &str,
        variables: &[TemplateVariable],
    ) -> Result<(), TemplateError> {
        let var_regex = Regex::new(r"\{\{(\w+)\}\}").unwrap();
        let defined_vars: std::collections::HashSet<_> =
            variables.iter().map(|v| &v.name).collect();

        for capture in var_regex.captures_iter(text) {
            let var_name = capture[1].to_string();
            if !defined_vars.contains(&var_name) {
                return Err(TemplateError::ValidationFailed {
                    reason: format!("Variable '{}' is referenced but not defined", var_name),
                });
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:import_templates (lines 454-479) ---[0m
    pub async fn import_templates(
        &mut self,
        templates: Vec<Template>,
    ) -> Result<Vec<String>, TemplateError> {
        let mut imported = Vec::new();
        let mut errors = Vec::new();

        for template in templates {
            match self.save_template(template.clone()).await {
                Ok(()) => {
                    imported.push(template.id.clone());
                    info!("Imported template: {}", template.id);
                }
                Err(e) => {
                    warn!("Failed to import template {}: {}", template.id, e);
                    errors.push(format!("{}: {}", template.id, e));
                }
            }
        }

        if !errors.is_empty() {
            warn!("Failed to import {} templates: {:?}", errors.len(), errors);
        }

        Ok(imported)
    }

  ./crates/ccswarm/src/template/manager.rs:146-170 method validate_template <-> ./crates/ccswarm/src/template/manager.rs:454-479 method import_templates
  Similarity: 74.98%

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_template (lines 146-170) ---[0m
    fn validate_template(&self, template: &Template) -> Result<(), TemplateError> {
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template is missing required fields".to_string(),
            });
        }

        // Validate variable references in template
        self.validate_variable_references(&template.task_description, &template.variables)?;

        if let Some(ref details) = template.task_details {
            self.validate_variable_references(details, &template.variables)?;
        }

        for file in &template.target_files {
            self.validate_variable_references(file, &template.variables)?;
        }

        // Validate variable definitions
        for variable in &template.variables {
            self.validate_variable_definition(variable)?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:import_templates (lines 454-479) ---[0m
    pub async fn import_templates(
        &mut self,
        templates: Vec<Template>,
    ) -> Result<Vec<String>, TemplateError> {
        let mut imported = Vec::new();
        let mut errors = Vec::new();

        for template in templates {
            match self.save_template(template.clone()).await {
                Ok(()) => {
                    imported.push(template.id.clone());
                    info!("Imported template: {}", template.id);
                }
                Err(e) => {
                    warn!("Failed to import template {}: {}", template.id, e);
                    errors.push(format!("{}: {}", template.id, e));
                }
            }
        }

        if !errors.is_empty() {
            warn!("Failed to import {} templates: {:?}", errors.len(), errors);
        }

        Ok(imported)
    }

  ./crates/ccswarm/src/template/manager.rs:302-324 method validate_context <-> ./crates/ccswarm/src/template/manager.rs:380-401 method transform_variable_value
  Similarity: 82.71%

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_context (lines 302-324) ---[0m
    fn validate_context(
        &self,
        template: &Template,
        context: &TemplateContext,
    ) -> Result<(), TemplateError> {
        for variable in &template.variables {
            if variable.required {
                let value = context.variables.get(&variable.name);
                if value.is_none() && variable.default_value.is_none() {
                    return Err(TemplateError::ValidationFailed {
                        reason: format!("Required variable '{}' not provided", variable.name),
                    });
                }
            }

            // Validate provided values
            if let Some(value) = context.variables.get(&variable.name) {
                self.validate_variable_value(&variable.name, value, &variable.variable_type)?;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:transform_variable_value (lines 380-401) ---[0m
    fn transform_variable_value(
        &self,
        value: &str,
        var_type: &VariableType,
    ) -> Result<String, TemplateError> {
        match var_type {
            VariableType::Boolean => {
                let normalized = match value.to_lowercase().as_str() {
                    "true" | "1" | "yes" => "true",
                    "false" | "0" | "no" => "false",
                    _ => value,
                };
                Ok(normalized.to_string())
            }
            VariableType::List => {
                // Convert comma-separated values to array format
                let items: Vec<&str> = value.split(',').map(|s| s.trim()).collect();
                Ok(format!("[{}]", items.join(", ")))
            }
            _ => Ok(value.to_string()),
        }
    }

  ./crates/ccswarm/src/template/manager.rs:302-324 method validate_context <-> ./crates/ccswarm/src/template/manager.rs:454-479 method import_templates
  Similarity: 74.60%

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_context (lines 302-324) ---[0m
    fn validate_context(
        &self,
        template: &Template,
        context: &TemplateContext,
    ) -> Result<(), TemplateError> {
        for variable in &template.variables {
            if variable.required {
                let value = context.variables.get(&variable.name);
                if value.is_none() && variable.default_value.is_none() {
                    return Err(TemplateError::ValidationFailed {
                        reason: format!("Required variable '{}' not provided", variable.name),
                    });
                }
            }

            // Validate provided values
            if let Some(value) = context.variables.get(&variable.name) {
                self.validate_variable_value(&variable.name, value, &variable.variable_type)?;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:import_templates (lines 454-479) ---[0m
    pub async fn import_templates(
        &mut self,
        templates: Vec<Template>,
    ) -> Result<Vec<String>, TemplateError> {
        let mut imported = Vec::new();
        let mut errors = Vec::new();

        for template in templates {
            match self.save_template(template.clone()).await {
                Ok(()) => {
                    imported.push(template.id.clone());
                    info!("Imported template: {}", template.id);
                }
                Err(e) => {
                    warn!("Failed to import template {}: {}", template.id, e);
                    errors.push(format!("{}: {}", template.id, e));
                }
            }
        }

        if !errors.is_empty() {
            warn!("Failed to import {} templates: {:?}", errors.len(), errors);
        }

        Ok(imported)
    }

  ./crates/ccswarm/src/template/manager.rs:103-119 method get_template_by_name <-> ./crates/ccswarm/src/template/manager.rs:404-431 method substitute_builtin_variables
  Similarity: 78.79%

[36m--- ./crates/ccswarm/src/template/manager.rs:get_template_by_name (lines 103-119) ---[0m
    pub async fn get_template_by_name(&self, name: &str) -> Result<Template, TemplateError> {
        // First try to load by ID
        if let Ok(template) = self.load_template(name).await {
            return Ok(template);
        }

        // Search by name
        let query = TemplateQuery::new().with_search_term(name).with_limit(1);
        let templates = self.search_templates(query).await?;

        templates
            .into_iter()
            .next()
            .ok_or_else(|| TemplateError::NotFound {
                name: name.to_string(),
            })
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:substitute_builtin_variables (lines 404-431) ---[0m
    fn substitute_builtin_variables(
        &self,
        text: &str,
        context: &TemplateContext,
    ) -> Result<String, TemplateError> {
        let mut result = text.to_string();

        // Project name
        if let Some(ref project_name) = context.project_name {
            result = result.replace("{{project_name}}", project_name);
        }

        // Agent role
        if let Some(ref agent_role) = context.agent_role {
            result = result.replace("{{agent_role}}", agent_role);
        }

        // Current date/time
        let now = chrono::Utc::now();
        result = result.replace("{{current_date}}", &now.format("%Y-%m-%d").to_string());
        result = result.replace("{{current_time}}", &now.format("%H:%M:%S").to_string());
        result = result.replace(
            "{{current_datetime}}",
            &now.format("%Y-%m-%d %H:%M:%S").to_string(),
        );

        Ok(result)
    }

  ./crates/ccswarm/src/template/manager.rs:103-119 method get_template_by_name <-> ./crates/ccswarm/src/template/manager.rs:454-479 method import_templates
  Similarity: 81.42%

[36m--- ./crates/ccswarm/src/template/manager.rs:get_template_by_name (lines 103-119) ---[0m
    pub async fn get_template_by_name(&self, name: &str) -> Result<Template, TemplateError> {
        // First try to load by ID
        if let Ok(template) = self.load_template(name).await {
            return Ok(template);
        }

        // Search by name
        let query = TemplateQuery::new().with_search_term(name).with_limit(1);
        let templates = self.search_templates(query).await?;

        templates
            .into_iter()
            .next()
            .ok_or_else(|| TemplateError::NotFound {
                name: name.to_string(),
            })
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:import_templates (lines 454-479) ---[0m
    pub async fn import_templates(
        &mut self,
        templates: Vec<Template>,
    ) -> Result<Vec<String>, TemplateError> {
        let mut imported = Vec::new();
        let mut errors = Vec::new();

        for template in templates {
            match self.save_template(template.clone()).await {
                Ok(()) => {
                    imported.push(template.id.clone());
                    info!("Imported template: {}", template.id);
                }
                Err(e) => {
                    warn!("Failed to import template {}: {}", template.id, e);
                    errors.push(format!("{}: {}", template.id, e));
                }
            }
        }

        if !errors.is_empty() {
            warn!("Failed to import {} templates: {:?}", errors.len(), errors);
        }

        Ok(imported)
    }

  ./crates/ccswarm/src/template/manager.rs:439-451 method update_template <-> ./crates/ccswarm/src/template/manager.rs:454-479 method import_templates
  Similarity: 83.51%

[36m--- ./crates/ccswarm/src/template/manager.rs:update_template (lines 439-451) ---[0m
    pub async fn update_template(&mut self, template: Template) -> Result<(), TemplateError> {
        self.validate_template(&template)?;

        // Delete existing template
        if self.storage.exists(&template.id).await? {
            self.storage.delete_template(&template.id).await?;
        }

        // Save updated template
        self.storage.save_template(&template).await?;
        info!("Updated template: {} ({})", template.name, template.id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:import_templates (lines 454-479) ---[0m
    pub async fn import_templates(
        &mut self,
        templates: Vec<Template>,
    ) -> Result<Vec<String>, TemplateError> {
        let mut imported = Vec::new();
        let mut errors = Vec::new();

        for template in templates {
            match self.save_template(template.clone()).await {
                Ok(()) => {
                    imported.push(template.id.clone());
                    info!("Imported template: {}", template.id);
                }
                Err(e) => {
                    warn!("Failed to import template {}: {}", template.id, e);
                    errors.push(format!("{}: {}", template.id, e));
                }
            }
        }

        if !errors.is_empty() {
            warn!("Failed to import {} templates: {:?}", errors.len(), errors);
        }

        Ok(imported)
    }

  ./crates/ccswarm/src/template/manager.rs:404-431 method substitute_builtin_variables <-> ./crates/ccswarm/src/template/manager.rs:439-451 method update_template
  Similarity: 79.16%

[36m--- ./crates/ccswarm/src/template/manager.rs:substitute_builtin_variables (lines 404-431) ---[0m
    fn substitute_builtin_variables(
        &self,
        text: &str,
        context: &TemplateContext,
    ) -> Result<String, TemplateError> {
        let mut result = text.to_string();

        // Project name
        if let Some(ref project_name) = context.project_name {
            result = result.replace("{{project_name}}", project_name);
        }

        // Agent role
        if let Some(ref agent_role) = context.agent_role {
            result = result.replace("{{agent_role}}", agent_role);
        }

        // Current date/time
        let now = chrono::Utc::now();
        result = result.replace("{{current_date}}", &now.format("%Y-%m-%d").to_string());
        result = result.replace("{{current_time}}", &now.format("%H:%M:%S").to_string());
        result = result.replace(
            "{{current_datetime}}",
            &now.format("%Y-%m-%d %H:%M:%S").to_string(),
        );

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:update_template (lines 439-451) ---[0m
    pub async fn update_template(&mut self, template: Template) -> Result<(), TemplateError> {
        self.validate_template(&template)?;

        // Delete existing template
        if self.storage.exists(&template.id).await? {
            self.storage.delete_template(&template.id).await?;
        }

        // Save updated template
        self.storage.save_template(&template).await?;
        info!("Updated template: {} ({})", template.name, template.id);
        Ok(())
    }

  ./crates/ccswarm/src/template/manager.rs:103-119 method get_template_by_name <-> ./crates/ccswarm/src/template/manager.rs:146-170 method validate_template
  Similarity: 74.49%

[36m--- ./crates/ccswarm/src/template/manager.rs:get_template_by_name (lines 103-119) ---[0m
    pub async fn get_template_by_name(&self, name: &str) -> Result<Template, TemplateError> {
        // First try to load by ID
        if let Ok(template) = self.load_template(name).await {
            return Ok(template);
        }

        // Search by name
        let query = TemplateQuery::new().with_search_term(name).with_limit(1);
        let templates = self.search_templates(query).await?;

        templates
            .into_iter()
            .next()
            .ok_or_else(|| TemplateError::NotFound {
                name: name.to_string(),
            })
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_template (lines 146-170) ---[0m
    fn validate_template(&self, template: &Template) -> Result<(), TemplateError> {
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template is missing required fields".to_string(),
            });
        }

        // Validate variable references in template
        self.validate_variable_references(&template.task_description, &template.variables)?;

        if let Some(ref details) = template.task_details {
            self.validate_variable_references(details, &template.variables)?;
        }

        for file in &template.target_files {
            self.validate_variable_references(file, &template.variables)?;
        }

        // Validate variable definitions
        for variable in &template.variables {
            self.validate_variable_definition(variable)?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/template/manager.rs:146-170 method validate_template <-> ./crates/ccswarm/src/template/manager.rs:439-451 method update_template
  Similarity: 78.40%

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_template (lines 146-170) ---[0m
    fn validate_template(&self, template: &Template) -> Result<(), TemplateError> {
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template is missing required fields".to_string(),
            });
        }

        // Validate variable references in template
        self.validate_variable_references(&template.task_description, &template.variables)?;

        if let Some(ref details) = template.task_details {
            self.validate_variable_references(details, &template.variables)?;
        }

        for file in &template.target_files {
            self.validate_variable_references(file, &template.variables)?;
        }

        // Validate variable definitions
        for variable in &template.variables {
            self.validate_variable_definition(variable)?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:update_template (lines 439-451) ---[0m
    pub async fn update_template(&mut self, template: Template) -> Result<(), TemplateError> {
        self.validate_template(&template)?;

        // Delete existing template
        if self.storage.exists(&template.id).await? {
            self.storage.delete_template(&template.id).await?;
        }

        // Save updated template
        self.storage.save_template(&template).await?;
        info!("Updated template: {} ({})", template.name, template.id);
        Ok(())
    }

  ./crates/ccswarm/src/template/manager.rs:439-451 method update_template <-> ./crates/ccswarm/src/template/manager.rs:487-513 method clone_template
  Similarity: 71.45%

[36m--- ./crates/ccswarm/src/template/manager.rs:update_template (lines 439-451) ---[0m
    pub async fn update_template(&mut self, template: Template) -> Result<(), TemplateError> {
        self.validate_template(&template)?;

        // Delete existing template
        if self.storage.exists(&template.id).await? {
            self.storage.delete_template(&template.id).await?;
        }

        // Save updated template
        self.storage.save_template(&template).await?;
        info!("Updated template: {} ({})", template.name, template.id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:clone_template (lines 487-513) ---[0m
    pub async fn clone_template(
        &mut self,
        source_id: &str,
        new_id: &str,
        new_name: Option<String>,
    ) -> Result<Template, TemplateError> {
        let mut template = self.load_template(source_id).await?;

        // Update ID and name
        template.id = new_id.to_string();
        if let Some(name) = new_name {
            template.name = name;
        } else {
            template.name = format!("{} (Copy)", template.name);
        }

        // Reset statistics
        template.usage_count = 0;
        template.success_rate = None;
        template.created_at = chrono::Utc::now();
        template.updated_at = chrono::Utc::now();

        // Save cloned template
        self.save_template(template.clone()).await?;

        Ok(template)
    }

  ./crates/ccswarm/src/template/manager.rs:103-119 method get_template_by_name <-> ./crates/ccswarm/src/template/manager.rs:173-192 method validate_variable_references
  Similarity: 76.61%

[36m--- ./crates/ccswarm/src/template/manager.rs:get_template_by_name (lines 103-119) ---[0m
    pub async fn get_template_by_name(&self, name: &str) -> Result<Template, TemplateError> {
        // First try to load by ID
        if let Ok(template) = self.load_template(name).await {
            return Ok(template);
        }

        // Search by name
        let query = TemplateQuery::new().with_search_term(name).with_limit(1);
        let templates = self.search_templates(query).await?;

        templates
            .into_iter()
            .next()
            .ok_or_else(|| TemplateError::NotFound {
                name: name.to_string(),
            })
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:validate_variable_references (lines 173-192) ---[0m
    fn validate_variable_references(
        &self,
        text: &str,
        variables: &[TemplateVariable],
    ) -> Result<(), TemplateError> {
        let var_regex = Regex::new(r"\{\{(\w+)\}\}").unwrap();
        let defined_vars: std::collections::HashSet<_> =
            variables.iter().map(|v| &v.name).collect();

        for capture in var_regex.captures_iter(text) {
            let var_name = capture[1].to_string();
            if !defined_vars.contains(&var_name) {
                return Err(TemplateError::ValidationFailed {
                    reason: format!("Variable '{}' is referenced but not defined", var_name),
                });
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/template/manager.rs:103-119 method get_template_by_name <-> ./crates/ccswarm/src/template/manager.rs:439-451 method update_template
  Similarity: 84.81%

[36m--- ./crates/ccswarm/src/template/manager.rs:get_template_by_name (lines 103-119) ---[0m
    pub async fn get_template_by_name(&self, name: &str) -> Result<Template, TemplateError> {
        // First try to load by ID
        if let Ok(template) = self.load_template(name).await {
            return Ok(template);
        }

        // Search by name
        let query = TemplateQuery::new().with_search_term(name).with_limit(1);
        let templates = self.search_templates(query).await?;

        templates
            .into_iter()
            .next()
            .ok_or_else(|| TemplateError::NotFound {
                name: name.to_string(),
            })
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:update_template (lines 439-451) ---[0m
    pub async fn update_template(&mut self, template: Template) -> Result<(), TemplateError> {
        self.validate_template(&template)?;

        // Delete existing template
        if self.storage.exists(&template.id).await? {
            self.storage.delete_template(&template.id).await?;
        }

        // Save updated template
        self.storage.save_template(&template).await?;
        info!("Updated template: {} ({})", template.name, template.id);
        Ok(())
    }

  ./crates/ccswarm/src/template/manager.rs:29-34 method save_template <-> ./crates/ccswarm/src/template/manager.rs:439-451 method update_template
  Similarity: 78.93%

[36m--- ./crates/ccswarm/src/template/manager.rs:save_template (lines 29-34) ---[0m
    pub async fn save_template(&mut self, template: Template) -> Result<(), TemplateError> {
        self.validate_template(&template)?;
        self.storage.save_template(&template).await?;
        info!("Saved template: {} ({})", template.name, template.id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:update_template (lines 439-451) ---[0m
    pub async fn update_template(&mut self, template: Template) -> Result<(), TemplateError> {
        self.validate_template(&template)?;

        // Delete existing template
        if self.storage.exists(&template.id).await? {
            self.storage.delete_template(&template.id).await?;
        }

        // Save updated template
        self.storage.save_template(&template).await?;
        info!("Updated template: {} ({})", template.name, template.id);
        Ok(())
    }

  ./crates/ccswarm/src/template/manager.rs:122-128 method get_templates_by_category <-> ./crates/ccswarm/src/template/manager.rs:131-137 method get_popular_templates
  Similarity: 93.90%

[36m--- ./crates/ccswarm/src/template/manager.rs:get_templates_by_category (lines 122-128) ---[0m
    pub async fn get_templates_by_category(
        &self,
        category: TemplateCategory,
    ) -> Result<Vec<Template>, TemplateError> {
        let query = TemplateQuery::new().with_category(category);
        self.search_templates(query).await
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:get_popular_templates (lines 131-137) ---[0m
    pub async fn get_popular_templates(
        &self,
        limit: usize,
    ) -> Result<Vec<Template>, TemplateError> {
        let query = TemplateQuery::new().sort_by_popularity().with_limit(limit);
        self.search_templates(query).await
    }

  ./crates/ccswarm/src/template/manager.rs:131-137 method get_popular_templates <-> ./crates/ccswarm/src/template/manager.rs:140-143 method get_recent_templates
  Similarity: 99.27%

[36m--- ./crates/ccswarm/src/template/manager.rs:get_popular_templates (lines 131-137) ---[0m
    pub async fn get_popular_templates(
        &self,
        limit: usize,
    ) -> Result<Vec<Template>, TemplateError> {
        let query = TemplateQuery::new().sort_by_popularity().with_limit(limit);
        self.search_templates(query).await
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:get_recent_templates (lines 140-143) ---[0m
    pub async fn get_recent_templates(&self, limit: usize) -> Result<Vec<Template>, TemplateError> {
        let query = TemplateQuery::new().sort_by_date().with_limit(limit);
        self.search_templates(query).await
    }

  ./crates/ccswarm/src/template/manager.rs:122-128 method get_templates_by_category <-> ./crates/ccswarm/src/template/manager.rs:140-143 method get_recent_templates
  Similarity: 93.90%

[36m--- ./crates/ccswarm/src/template/manager.rs:get_templates_by_category (lines 122-128) ---[0m
    pub async fn get_templates_by_category(
        &self,
        category: TemplateCategory,
    ) -> Result<Vec<Template>, TemplateError> {
        let query = TemplateQuery::new().with_category(category);
        self.search_templates(query).await
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:get_recent_templates (lines 140-143) ---[0m
    pub async fn get_recent_templates(&self, limit: usize) -> Result<Vec<Template>, TemplateError> {
        let query = TemplateQuery::new().sort_by_date().with_limit(limit);
        self.search_templates(query).await
    }

  ./crates/ccswarm/src/template/manager.rs:29-34 method save_template <-> ./crates/ccswarm/src/template/manager.rs:42-46 method delete_template
  Similarity: 70.46%

[36m--- ./crates/ccswarm/src/template/manager.rs:save_template (lines 29-34) ---[0m
    pub async fn save_template(&mut self, template: Template) -> Result<(), TemplateError> {
        self.validate_template(&template)?;
        self.storage.save_template(&template).await?;
        info!("Saved template: {} ({})", template.name, template.id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/manager.rs:delete_template (lines 42-46) ---[0m
    pub async fn delete_template(&mut self, id: &str) -> Result<(), TemplateError> {
        self.storage.delete_template(id).await?;
        info!("Deleted template: {}", id);
        Ok(())
    }


Duplicates in ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:167-180 function test_task_plan_creation <-> ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:199-212 function test_step_result
  Similarity: 82.29%

[36m--- ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:test_task_plan_creation (lines 167-180) ---[0m
    fn test_task_plan_creation() {
        let mut plan = TaskPlan::new("test-task".to_string());
        assert_eq!(plan.task_id, "test-task");
        assert!(plan.steps.is_empty());
        assert!(plan.adaptive);

        let step = TaskStep::new(
            "step1".to_string(),
            "Initial Analysis".to_string(),
            StepType::Analysis,
        );
        plan.add_step(step);
        assert_eq!(plan.steps.len(), 1);
    }

[36m--- ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:test_step_result (lines 199-212) ---[0m
    fn test_step_result() {
        let mut result = StepResult::new("step1".to_string());
        assert!(result.is_success());

        result = result
            .with_summary("Step completed successfully".to_string())
            .add_output("test_count".to_string(), "42".to_string());

        assert_eq!(result.outputs.get("test_count"), Some(&"42".to_string()));

        // Test failure
        let failed_result = StepResult::new("step2".to_string()).failed("Test failed".to_string());
        assert!(!failed_result.is_success());
    }

  ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:183-196 function test_task_step_builder <-> ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:199-212 function test_step_result
  Similarity: 71.38%

[36m--- ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:test_task_step_builder (lines 183-196) ---[0m
    fn test_task_step_builder() {
        let step = TaskStep::new(
            "step1".to_string(),
            "Test Step".to_string(),
            StepType::Execution,
        )
        .with_description("Test description".to_string())
        .depends_on("step0".to_string())
        .requires_context("previous_result".to_string());

        assert_eq!(step.description, "Test description");
        assert_eq!(step.dependencies.len(), 1);
        assert_eq!(step.required_context.len(), 1);
    }

[36m--- ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:test_step_result (lines 199-212) ---[0m
    fn test_step_result() {
        let mut result = StepResult::new("step1".to_string());
        assert!(result.is_success());

        result = result
            .with_summary("Step completed successfully".to_string())
            .add_output("test_count".to_string(), "42".to_string());

        assert_eq!(result.outputs.get("test_count"), Some(&"42".to_string()));

        // Test failure
        let failed_result = StepResult::new("step2".to_string()).failed("Test failed".to_string());
        assert!(!failed_result.is_success());
    }

  ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:96-106 method new <-> ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:129-139 method new
  Similarity: 90.44%
  Classes: TaskStep <-> StepResult

[36m--- ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:new (lines 96-106) ---[0m
    pub fn new(id: String, name: String, step_type: StepType) -> Self {
        Self {
            id,
            name,
            description: String::new(),
            step_type,
            parallel_tasks: Vec::new(),
            dependencies: Vec::new(),
            required_context: Vec::new(),
        }
    }

[36m--- ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:new (lines 129-139) ---[0m
    pub fn new(step_id: String) -> Self {
        Self {
            step_id,
            success: true,
            summary: String::new(),
            outputs: HashMap::new(),
            errors: Vec::new(),
            duration_ms: 0,
            parallel_results: Vec::new(),
        }
    }

  ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:67-74 method new <-> ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:129-139 method new
  Similarity: 83.09%
  Classes: TaskPlan <-> StepResult

[36m--- ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:new (lines 67-74) ---[0m
    pub fn new(task_id: String) -> Self {
        Self {
            task_id,
            steps: Vec::new(),
            context: HashMap::new(),
            adaptive: true,
        }
    }

[36m--- ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:new (lines 129-139) ---[0m
    pub fn new(step_id: String) -> Self {
        Self {
            step_id,
            success: true,
            summary: String::new(),
            outputs: HashMap::new(),
            errors: Vec::new(),
            duration_ms: 0,
            parallel_results: Vec::new(),
        }
    }

  ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:67-74 method new <-> ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:96-106 method new
  Similarity: 80.16%
  Classes: TaskPlan <-> TaskStep

[36m--- ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:new (lines 67-74) ---[0m
    pub fn new(task_id: String) -> Self {
        Self {
            task_id,
            steps: Vec::new(),
            context: HashMap::new(),
            adaptive: true,
        }
    }

[36m--- ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:new (lines 96-106) ---[0m
    pub fn new(id: String, name: String, step_type: StepType) -> Self {
        Self {
            id,
            name,
            description: String::new(),
            step_type,
            parallel_tasks: Vec::new(),
            dependencies: Vec::new(),
            required_context: Vec::new(),
        }
    }

  ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:67-74 method new <-> ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:88-92 method insert_step_after
  Similarity: 71.56%
  Classes: TaskPlan <-> TaskPlan

[36m--- ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:new (lines 67-74) ---[0m
    pub fn new(task_id: String) -> Self {
        Self {
            task_id,
            steps: Vec::new(),
            context: HashMap::new(),
            adaptive: true,
        }
    }

[36m--- ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:insert_step_after (lines 88-92) ---[0m
    pub fn insert_step_after(&mut self, after_id: &str, step: TaskStep) {
        if let Some(pos) = self.steps.iter().position(|s| s.id == after_id) {
            self.steps.insert(pos + 1, step);
        }
    }

  ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:88-92 method insert_step_after <-> ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:157-159 method is_success
  Similarity: 76.25%
  Classes: TaskPlan <-> StepResult

[36m--- ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:insert_step_after (lines 88-92) ---[0m
    pub fn insert_step_after(&mut self, after_id: &str, step: TaskStep) {
        if let Some(pos) = self.steps.iter().position(|s| s.id == after_id) {
            self.steps.insert(pos + 1, step);
        }
    }

[36m--- ./crates/ccswarm/src/agent/orchestrator/task_plan.rs:is_success (lines 157-159) ---[0m
    pub fn is_success(&self) -> bool {
        self.success && self.parallel_results.iter().all(|r| r.success)
    }


Duplicates in ./crates/ccswarm/src/tui/mod.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/tui/mod.rs:26-51 function run_tui <-> ./crates/ccswarm/src/tui/mod.rs:54-80 function run_tui_with_engine
  Similarity: 94.85%

[36m--- ./crates/ccswarm/src/tui/mod.rs:run_tui (lines 26-51) ---[0m
pub async fn run_tui() -> Result<()> {
    // Setup terminal
    enable_raw_mode()?;
    let mut stdout = io::stdout();
    execute!(stdout, EnterAlternateScreen, EnableMouseCapture)?;
    let backend = CrosstermBackend::new(stdout);
    let mut terminal = Terminal::new(backend)?;

    // Create app state
    let mut app = App::new().await?;
    let mut event_handler = EventHandler::new(Duration::from_millis(100));

    // Main event loop
    let result = run_app(&mut terminal, &mut app, &mut event_handler).await;

    // Restore terminal
    disable_raw_mode()?;
    execute!(
        terminal.backend_mut(),
        LeaveAlternateScreen,
        DisableMouseCapture
    )?;
    terminal.show_cursor()?;

    result
}

[36m--- ./crates/ccswarm/src/tui/mod.rs:run_tui_with_engine (lines 54-80) ---[0m
pub async fn run_tui_with_engine(execution_engine: ExecutionEngine) -> Result<()> {
    // Setup terminal
    enable_raw_mode()?;
    let mut stdout = io::stdout();
    execute!(stdout, EnterAlternateScreen, EnableMouseCapture)?;
    let backend = CrosstermBackend::new(stdout);
    let mut terminal = Terminal::new(backend)?;

    // Create app state with execution engine
    let mut app = App::new().await?;
    app.set_execution_engine(execution_engine);
    let mut event_handler = EventHandler::new(Duration::from_millis(100));

    // Main event loop
    let result = run_app(&mut terminal, &mut app, &mut event_handler).await;

    // Restore terminal
    disable_raw_mode()?;
    execute!(
        terminal.backend_mut(),
        LeaveAlternateScreen,
        DisableMouseCapture
    )?;
    terminal.show_cursor()?;

    result
}


Duplicates in ./crates/ccswarm/src/session/base_session.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/session/base_session.rs:40-65 method new <-> ./crates/ccswarm/src/session/base_session.rs:101-129 method execute_task
  Similarity: 72.72%
  Classes: BaseSession <-> TaskExecutor

[36m--- ./crates/ccswarm/src/session/base_session.rs:new (lines 40-65) ---[0m
    pub fn new(config: SessionConfig) -> Self {
        let id = format!("session-{}", Uuid::new_v4());
        let working_directory = config
            .working_directory
            .map(PathBuf::from)
            .unwrap_or_else(|| PathBuf::from("."));

        let mut env_vars = config.env_vars;
        // Add standard environment variables
        env_vars.extend(<Self as SessionEnvironment>::create_env_vars(
            &config.agent_id,
            &id,
        ));

        Self {
            id,
            session_type: "base".to_string(),
            status: SessionStatus::Initializing,
            created_at: Utc::now(),
            last_activity: Utc::now(),
            working_directory,
            env_vars,
            stats: EfficiencyStats::default(),
            tasks_completed: 0,
        }
    }

[36m--- ./crates/ccswarm/src/session/base_session.rs:execute_task (lines 101-129) ---[0m
    async fn execute_task(&mut self, task: Task) -> Result<TaskResult> {
        // Update status
        self.update_status(SessionStatus::Busy);

        // This is a placeholder - derived classes should override
        let start_time = std::time::Instant::now();

        // Simulate task execution
        tokio::time::sleep(Duration::from_millis(100)).await;

        let result = TaskResult {
            success: true,
            output: serde_json::json!({
                "task_id": task.id,
                "message": "Task completed"
            }),
            error: None,
            duration: start_time.elapsed(),
        };

        // Update statistics
        let duration = start_time.elapsed();
        self.update_task_stats(&result, duration);

        // Update status back to active
        self.update_status(SessionStatus::Active);

        Ok(result)
    }

  ./crates/ccswarm/src/session/base_session.rs:40-65 method new <-> ./crates/ccswarm/src/session/base_session.rs:173-189 method update_task_stats
  Similarity: 82.46%
  Classes: BaseSession <-> SessionStatistics

[36m--- ./crates/ccswarm/src/session/base_session.rs:new (lines 40-65) ---[0m
    pub fn new(config: SessionConfig) -> Self {
        let id = format!("session-{}", Uuid::new_v4());
        let working_directory = config
            .working_directory
            .map(PathBuf::from)
            .unwrap_or_else(|| PathBuf::from("."));

        let mut env_vars = config.env_vars;
        // Add standard environment variables
        env_vars.extend(<Self as SessionEnvironment>::create_env_vars(
            &config.agent_id,
            &id,
        ));

        Self {
            id,
            session_type: "base".to_string(),
            status: SessionStatus::Initializing,
            created_at: Utc::now(),
            last_activity: Utc::now(),
            working_directory,
            env_vars,
            stats: EfficiencyStats::default(),
            tasks_completed: 0,
        }
    }

[36m--- ./crates/ccswarm/src/session/base_session.rs:update_task_stats (lines 173-189) ---[0m
    fn update_task_stats(&mut self, result: &TaskResult, duration: Duration) {
        self.stats.total_tasks += 1;
        if result.success {
            self.stats.successful_tasks += 1;
        } else {
            self.stats.failed_tasks += 1;
        }

        self.stats.total_duration += duration;
        if self.stats.total_tasks > 0 {
            self.stats.average_task_duration =
                self.stats.total_duration / self.stats.total_tasks as u32;
        }

        self.tasks_completed += 1;
        self.touch();
    }

  ./crates/ccswarm/src/session/base_session.rs:75-80 method initialize <-> ./crates/ccswarm/src/session/base_session.rs:82-87 method shutdown
  Similarity: 78.57%
  Classes: SessionLifecycle <-> SessionLifecycle

[36m--- ./crates/ccswarm/src/session/base_session.rs:initialize (lines 75-80) ---[0m
    async fn initialize(&mut self) -> Result<()> {
        // Basic initialization
        self.status = SessionStatus::Active;
        self.touch();
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/base_session.rs:shutdown (lines 82-87) ---[0m
    async fn shutdown(&mut self) -> Result<()> {
        self.status = SessionStatus::Terminating;
        // Perform cleanup...
        self.status = SessionStatus::Terminated;
        Ok(())
    }


Duplicates in ./crates/ccswarm/src/utils/error_handling_macros.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/utils/error_handling_macros.rs:137-151 function test_chain_result <-> ./crates/ccswarm/src/utils/error_handling_macros.rs:154-168 function test_retry_on_error
  Similarity: 86.38%

[36m--- ./crates/ccswarm/src/utils/error_handling_macros.rs:test_chain_result (lines 137-151) ---[0m
    fn test_chain_result() {
        let result = chain_result!(
            Ok::<_, anyhow::Error>(1),
            Ok::<_, anyhow::Error>(2),
            Ok::<_, anyhow::Error>(3)
        );
        assert!(result.is_ok());

        let error_result = chain_result!(
            Ok::<_, anyhow::Error>(1),
            Err::<i32, _>(anyhow::anyhow!("error")),
            Ok::<_, anyhow::Error>(3)
        );
        assert!(error_result.is_err());
    }

[36m--- ./crates/ccswarm/src/utils/error_handling_macros.rs:test_retry_on_error (lines 154-168) ---[0m
    fn test_retry_on_error() {
        let mut counter = 0;
        let result = retry_on_error!({
            counter += 1;
            if counter < 3 {
                Err::<i32, _>(anyhow::anyhow!("error"))
            } else {
                Ok(42)
            }
        }, 5);

        assert!(result.is_ok());
        assert_eq!(result.unwrap(), 42);
        assert_eq!(counter, 3);
    }

  ./crates/ccswarm/src/utils/error_handling_macros.rs:126-134 function test_try_or_default <-> ./crates/ccswarm/src/utils/error_handling_macros.rs:137-151 function test_chain_result
  Similarity: 76.03%

[36m--- ./crates/ccswarm/src/utils/error_handling_macros.rs:test_try_or_default (lines 126-134) ---[0m
    fn test_try_or_default() {
        let result: Result<i32> = Ok(42);
        assert_eq!(try_or_default!(result), 42);

        let error_result: Result<i32> = Err(anyhow::anyhow!("error"));
        assert_eq!(try_or_default!(error_result), 0);

        assert_eq!(try_or_default!(error_result, 99), 99);
    }

[36m--- ./crates/ccswarm/src/utils/error_handling_macros.rs:test_chain_result (lines 137-151) ---[0m
    fn test_chain_result() {
        let result = chain_result!(
            Ok::<_, anyhow::Error>(1),
            Ok::<_, anyhow::Error>(2),
            Ok::<_, anyhow::Error>(3)
        );
        assert!(result.is_ok());

        let error_result = chain_result!(
            Ok::<_, anyhow::Error>(1),
            Err::<i32, _>(anyhow::anyhow!("error")),
            Ok::<_, anyhow::Error>(3)
        );
        assert!(error_result.is_err());
    }

  ./crates/ccswarm/src/utils/error_handling_macros.rs:126-134 function test_try_or_default <-> ./crates/ccswarm/src/utils/error_handling_macros.rs:154-168 function test_retry_on_error
  Similarity: 74.81%

[36m--- ./crates/ccswarm/src/utils/error_handling_macros.rs:test_try_or_default (lines 126-134) ---[0m
    fn test_try_or_default() {
        let result: Result<i32> = Ok(42);
        assert_eq!(try_or_default!(result), 42);

        let error_result: Result<i32> = Err(anyhow::anyhow!("error"));
        assert_eq!(try_or_default!(error_result), 0);

        assert_eq!(try_or_default!(error_result, 99), 99);
    }

[36m--- ./crates/ccswarm/src/utils/error_handling_macros.rs:test_retry_on_error (lines 154-168) ---[0m
    fn test_retry_on_error() {
        let mut counter = 0;
        let result = retry_on_error!({
            counter += 1;
            if counter < 3 {
                Err::<i32, _>(anyhow::anyhow!("error"))
            } else {
                Ok(42)
            }
        }, 5);

        assert!(result.is_ok());
        assert_eq!(result.unwrap(), 42);
        assert_eq!(counter, 3);
    }


Duplicates in ./crates/ccswarm/src/orchestrator/proactive_master.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/orchestrator/proactive_master.rs:307-382 method initialize_pattern_library <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:634-721 method predict_next_tasks
  Similarity: 98.44%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:initialize_pattern_library (lines 307-382) ---[0m
    fn initialize_pattern_library() -> HashMap<String, TaskPattern> {
        let mut patterns = HashMap::new();

        // Frontend component pattern
        patterns.insert(
            "frontend_component".to_string(),
            TaskPattern {
                pattern_id: "frontend_component".to_string(),
                trigger_conditions: vec!["component created".to_string()],
                generated_tasks: vec![
                    TaskTemplate {
                        description_template: "Write unit tests for {component_name} component"
                            .to_string(),
                        task_type: TaskType::Testing,
                        priority: Priority::High,
                        estimated_duration: 30,
                        required_agent_type: "QA".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add {component_name} to component library docs"
                            .to_string(),
                        task_type: TaskType::Documentation,
                        priority: Priority::Medium,
                        estimated_duration: 15,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                confidence: 0.95,
                usage_count: 0,
            },
        );

        // API endpoint pattern
        patterns.insert(
            "api_endpoint".to_string(),
            TaskPattern {
                pattern_id: "api_endpoint".to_string(),
                trigger_conditions: vec!["API endpoint created".to_string()],
                generated_tasks: vec![
                    TaskTemplate {
                        description_template: "Write integration tests for {endpoint_name} API"
                            .to_string(),
                        task_type: TaskType::Testing,
                        priority: Priority::High,
                        estimated_duration: 45,
                        required_agent_type: "QA".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Update API documentation for {endpoint_name}"
                            .to_string(),
                        task_type: TaskType::Documentation,
                        priority: Priority::Medium,
                        estimated_duration: 20,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add rate limiting to {endpoint_name} endpoint"
                            .to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 25,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                confidence: 0.9,
                usage_count: 0,
            },
        );

        patterns
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:predict_next_tasks (lines 634-721) ---[0m
    async fn predict_next_tasks(
        &self,
        agents: &DashMap<String, ClaudeCodeAgent>,
    ) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let predictor = self.task_predictor.read().await;

        // Analyze recent completions
        for entry in agents.iter() {
            let agent = entry.value();

            // Check last completed task
            if let Some((last_task, last_result)) = agent.task_history.last() {
                if last_result.success {
                    // Find matching completion patterns
                    for pattern in &predictor.completion_patterns {
                        if pattern.completed_task_type == last_task.task_type
                            && pattern.probability > 0.7
                        {
                            for task_template in &pattern.follow_up_tasks {
                                decisions.push(ProactiveDecision {
                                    decision_type: DecisionType::GenerateTask,
                                    reasoning: format!(
                                        "Pattern match: {:?} completion typically requires {}",
                                        last_task.task_type, task_template.description_template
                                    ),
                                    confidence: pattern.probability,
                                    suggested_actions: vec![SuggestedAction {
                                        action_type: "create_task".to_string(),
                                        description: format!(
                                            "Create follow-up task: {}",
                                            task_template.description_template
                                        ),
                                        parameters: HashMap::from([
                                            (
                                                "template".to_string(),
                                                serde_json::to_string(task_template)?,
                                            ),
                                            ("parent_task".to_string(), last_task.id.clone()),
                                        ]),
                                        expected_impact: "Maintain development momentum"
                                            .to_string(),
                                    }],
                                    risk_assessment: RiskLevel::Low,
                                });
                            }
                        }
                    }

                    // Check for pattern triggers in task description
                    let description_lower = last_task.description.to_lowercase();
                    for (pattern_id, pattern) in &predictor.pattern_library {
                        for trigger in &pattern.trigger_conditions {
                            if description_lower.contains(&trigger.to_lowercase()) {
                                for task_template in &pattern.generated_tasks {
                                    decisions.push(ProactiveDecision {
                                        decision_type: DecisionType::GenerateTask,
                                        reasoning: format!(
                                            "Pattern '{}' triggered by: {}",
                                            pattern_id, trigger
                                        ),
                                        confidence: pattern.confidence,
                                        suggested_actions: vec![
                                            SuggestedAction {
                                                action_type: "create_task".to_string(),
                                                description: format!(
                                                    "Auto-generate: {}",
                                                    task_template.description_template
                                                ),
                                                parameters: HashMap::from([
                                                    ("template".to_string(), serde_json::to_string(task_template)?),
                                                    ("trigger_task".to_string(), last_task.id.clone()),
                                                ]),
                                                expected_impact: "Ensure complete feature implementation".to_string(),
                                            },
                                        ],
                                        risk_assessment: RiskLevel::Low,
                                    });
                                }
                            }
                        }
                    }
                }
            }
        }

        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:634-721 method predict_next_tasks <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:724-790 method monitor_goals
  Similarity: 97.45%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:predict_next_tasks (lines 634-721) ---[0m
    async fn predict_next_tasks(
        &self,
        agents: &DashMap<String, ClaudeCodeAgent>,
    ) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let predictor = self.task_predictor.read().await;

        // Analyze recent completions
        for entry in agents.iter() {
            let agent = entry.value();

            // Check last completed task
            if let Some((last_task, last_result)) = agent.task_history.last() {
                if last_result.success {
                    // Find matching completion patterns
                    for pattern in &predictor.completion_patterns {
                        if pattern.completed_task_type == last_task.task_type
                            && pattern.probability > 0.7
                        {
                            for task_template in &pattern.follow_up_tasks {
                                decisions.push(ProactiveDecision {
                                    decision_type: DecisionType::GenerateTask,
                                    reasoning: format!(
                                        "Pattern match: {:?} completion typically requires {}",
                                        last_task.task_type, task_template.description_template
                                    ),
                                    confidence: pattern.probability,
                                    suggested_actions: vec![SuggestedAction {
                                        action_type: "create_task".to_string(),
                                        description: format!(
                                            "Create follow-up task: {}",
                                            task_template.description_template
                                        ),
                                        parameters: HashMap::from([
                                            (
                                                "template".to_string(),
                                                serde_json::to_string(task_template)?,
                                            ),
                                            ("parent_task".to_string(), last_task.id.clone()),
                                        ]),
                                        expected_impact: "Maintain development momentum"
                                            .to_string(),
                                    }],
                                    risk_assessment: RiskLevel::Low,
                                });
                            }
                        }
                    }

                    // Check for pattern triggers in task description
                    let description_lower = last_task.description.to_lowercase();
                    for (pattern_id, pattern) in &predictor.pattern_library {
                        for trigger in &pattern.trigger_conditions {
                            if description_lower.contains(&trigger.to_lowercase()) {
                                for task_template in &pattern.generated_tasks {
                                    decisions.push(ProactiveDecision {
                                        decision_type: DecisionType::GenerateTask,
                                        reasoning: format!(
                                            "Pattern '{}' triggered by: {}",
                                            pattern_id, trigger
                                        ),
                                        confidence: pattern.confidence,
                                        suggested_actions: vec![
                                            SuggestedAction {
                                                action_type: "create_task".to_string(),
                                                description: format!(
                                                    "Auto-generate: {}",
                                                    task_template.description_template
                                                ),
                                                parameters: HashMap::from([
                                                    ("template".to_string(), serde_json::to_string(task_template)?),
                                                    ("trigger_task".to_string(), last_task.id.clone()),
                                                ]),
                                                expected_impact: "Ensure complete feature implementation".to_string(),
                                            },
                                        ],
                                        risk_assessment: RiskLevel::Low,
                                    });
                                }
                            }
                        }
                    }
                }
            }
        }

        Ok(decisions)
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:monitor_goals (lines 724-790) ---[0m
    async fn monitor_goals(&self) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let goals = self.goal_tracker.read().await;

        // Check objective progress
        for objective in &goals.objectives {
            if objective.progress < 0.5 {
                if let Some(deadline) = objective.deadline {
                    let time_remaining = deadline - Utc::now();
                    let days_remaining = time_remaining.num_days();

                    if days_remaining <= 7 && objective.progress < 0.8 {
                        decisions.push(ProactiveDecision {
                            decision_type: DecisionType::ChangeStrategy,
                            reasoning: format!(
                                "Objective '{}' is behind schedule: {}% complete with {} days remaining",
                                objective.title, (objective.progress * 100.0) as u32, days_remaining
                            ),
                            confidence: 0.85,
                            suggested_actions: vec![
                                SuggestedAction {
                                    action_type: "reprioritize_tasks".to_string(),
                                    description: "Focus resources on critical objective".to_string(),
                                    parameters: HashMap::from([
                                        ("objective_id".to_string(), objective.id.clone()),
                                    ]),
                                    expected_impact: "Improve deadline adherence".to_string(),
                                },
                            ],
                            risk_assessment: RiskLevel::Medium,
                        });
                    }
                }
            }
        }

        // Check sprint velocity
        if let Some(sprint) = &goals.current_sprint {
            let sprint_progress = (Utc::now() - sprint.start_date).num_days() as f64
                / (sprint.end_date - sprint.start_date).num_days() as f64;

            let velocity_progress = sprint.current_velocity as f64 / sprint.velocity_target as f64;

            if sprint_progress > 0.5 && velocity_progress < 0.3 {
                decisions.push(ProactiveDecision {
                    decision_type: DecisionType::ScaleTeam,
                    reasoning: format!(
                        "Sprint velocity is significantly behind: {}% of target with {}% time remaining",
                        (velocity_progress * 100.0) as u32,
                        ((1.0 - sprint_progress) * 100.0) as u32
                    ),
                    confidence: 0.7,
                    suggested_actions: vec![
                        SuggestedAction {
                            action_type: "add_agent".to_string(),
                            description: "Consider adding additional agents".to_string(),
                            parameters: HashMap::new(),
                            expected_impact: "Increase development velocity".to_string(),
                        },
                    ],
                    risk_assessment: RiskLevel::Medium,
                });
            }
        }

        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:415-479 method initialize_feature_templates <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:634-721 method predict_next_tasks
  Similarity: 97.48%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:initialize_feature_templates (lines 415-479) ---[0m
    fn initialize_feature_templates() -> HashMap<String, FeatureTemplate> {
        let mut templates = HashMap::new();

        templates.insert(
            "user_authentication".to_string(),
            FeatureTemplate {
                feature_name: "User Authentication".to_string(),
                required_tasks: vec![
                    TaskTemplate {
                        description_template: "Create user registration API endpoint".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 120,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create user login API endpoint".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 90,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create registration form component".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 60,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create login form component".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 45,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                optional_tasks: vec![
                    TaskTemplate {
                        description_template: "Add social login integration".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 180,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add password reset functionality".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 90,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                typical_duration: 8, // hours
            },
        );

        templates
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:predict_next_tasks (lines 634-721) ---[0m
    async fn predict_next_tasks(
        &self,
        agents: &DashMap<String, ClaudeCodeAgent>,
    ) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let predictor = self.task_predictor.read().await;

        // Analyze recent completions
        for entry in agents.iter() {
            let agent = entry.value();

            // Check last completed task
            if let Some((last_task, last_result)) = agent.task_history.last() {
                if last_result.success {
                    // Find matching completion patterns
                    for pattern in &predictor.completion_patterns {
                        if pattern.completed_task_type == last_task.task_type
                            && pattern.probability > 0.7
                        {
                            for task_template in &pattern.follow_up_tasks {
                                decisions.push(ProactiveDecision {
                                    decision_type: DecisionType::GenerateTask,
                                    reasoning: format!(
                                        "Pattern match: {:?} completion typically requires {}",
                                        last_task.task_type, task_template.description_template
                                    ),
                                    confidence: pattern.probability,
                                    suggested_actions: vec![SuggestedAction {
                                        action_type: "create_task".to_string(),
                                        description: format!(
                                            "Create follow-up task: {}",
                                            task_template.description_template
                                        ),
                                        parameters: HashMap::from([
                                            (
                                                "template".to_string(),
                                                serde_json::to_string(task_template)?,
                                            ),
                                            ("parent_task".to_string(), last_task.id.clone()),
                                        ]),
                                        expected_impact: "Maintain development momentum"
                                            .to_string(),
                                    }],
                                    risk_assessment: RiskLevel::Low,
                                });
                            }
                        }
                    }

                    // Check for pattern triggers in task description
                    let description_lower = last_task.description.to_lowercase();
                    for (pattern_id, pattern) in &predictor.pattern_library {
                        for trigger in &pattern.trigger_conditions {
                            if description_lower.contains(&trigger.to_lowercase()) {
                                for task_template in &pattern.generated_tasks {
                                    decisions.push(ProactiveDecision {
                                        decision_type: DecisionType::GenerateTask,
                                        reasoning: format!(
                                            "Pattern '{}' triggered by: {}",
                                            pattern_id, trigger
                                        ),
                                        confidence: pattern.confidence,
                                        suggested_actions: vec![
                                            SuggestedAction {
                                                action_type: "create_task".to_string(),
                                                description: format!(
                                                    "Auto-generate: {}",
                                                    task_template.description_template
                                                ),
                                                parameters: HashMap::from([
                                                    ("template".to_string(), serde_json::to_string(task_template)?),
                                                    ("trigger_task".to_string(), last_task.id.clone()),
                                                ]),
                                                expected_impact: "Ensure complete feature implementation".to_string(),
                                            },
                                        ],
                                        risk_assessment: RiskLevel::Low,
                                    });
                                }
                            }
                        }
                    }
                }
            }
        }

        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:523-582 method analyze_agent_progress <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:634-721 method predict_next_tasks
  Similarity: 97.55%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:analyze_agent_progress (lines 523-582) ---[0m
    async fn analyze_agent_progress(
        &self,
        agents: &DashMap<String, ClaudeCodeAgent>,
    ) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let mut analyzer = self.progress_analyzer.write().await;

        for entry in agents.iter() {
            let agent = entry.value();
            let agent_id = &agent.identity.agent_id;

            // Check if agent is stuck
            if matches!(agent.status, AgentStatus::Working) {
                let time_since_activity = Utc::now() - agent.last_activity;
                if time_since_activity.num_minutes() > 15 {
                    decisions.push(ProactiveDecision {
                        decision_type: DecisionType::RequestIntervention,
                        reasoning: format!(
                            "Agent {} has been working on the same task for {} minutes without progress",
                            agent_id, time_since_activity.num_minutes()
                        ),
                        confidence: 0.9,
                        suggested_actions: vec![
                            SuggestedAction {
                                action_type: "check_agent_status".to_string(),
                                description: "Check if agent needs assistance".to_string(),
                                parameters: HashMap::from([
                                    ("agent_id".to_string(), agent_id.clone()),
                                ]),
                                expected_impact: "Unblock agent or reassign task".to_string(),
                            },
                        ],
                        risk_assessment: RiskLevel::Low,
                    });
                }
            }

            // Analyze agent efficiency
            let recent_tasks: Vec<_> = agent.task_history.iter().rev().take(5).collect();
            if recent_tasks.len() >= 3 {
                let avg_completion_time: f64 = recent_tasks
                    .iter()
                    .filter_map(|(_task, result)| {
                        if result.success {
                            Some(result.duration.as_secs() as f64 / 60.0)
                        } else {
                            None
                        }
                    })
                    .sum::<f64>()
                    / recent_tasks.len() as f64;

                analyzer
                    .efficiency_metrics
                    .insert(agent_id.clone(), avg_completion_time);
            }
        }

        Ok(decisions)
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:predict_next_tasks (lines 634-721) ---[0m
    async fn predict_next_tasks(
        &self,
        agents: &DashMap<String, ClaudeCodeAgent>,
    ) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let predictor = self.task_predictor.read().await;

        // Analyze recent completions
        for entry in agents.iter() {
            let agent = entry.value();

            // Check last completed task
            if let Some((last_task, last_result)) = agent.task_history.last() {
                if last_result.success {
                    // Find matching completion patterns
                    for pattern in &predictor.completion_patterns {
                        if pattern.completed_task_type == last_task.task_type
                            && pattern.probability > 0.7
                        {
                            for task_template in &pattern.follow_up_tasks {
                                decisions.push(ProactiveDecision {
                                    decision_type: DecisionType::GenerateTask,
                                    reasoning: format!(
                                        "Pattern match: {:?} completion typically requires {}",
                                        last_task.task_type, task_template.description_template
                                    ),
                                    confidence: pattern.probability,
                                    suggested_actions: vec![SuggestedAction {
                                        action_type: "create_task".to_string(),
                                        description: format!(
                                            "Create follow-up task: {}",
                                            task_template.description_template
                                        ),
                                        parameters: HashMap::from([
                                            (
                                                "template".to_string(),
                                                serde_json::to_string(task_template)?,
                                            ),
                                            ("parent_task".to_string(), last_task.id.clone()),
                                        ]),
                                        expected_impact: "Maintain development momentum"
                                            .to_string(),
                                    }],
                                    risk_assessment: RiskLevel::Low,
                                });
                            }
                        }
                    }

                    // Check for pattern triggers in task description
                    let description_lower = last_task.description.to_lowercase();
                    for (pattern_id, pattern) in &predictor.pattern_library {
                        for trigger in &pattern.trigger_conditions {
                            if description_lower.contains(&trigger.to_lowercase()) {
                                for task_template in &pattern.generated_tasks {
                                    decisions.push(ProactiveDecision {
                                        decision_type: DecisionType::GenerateTask,
                                        reasoning: format!(
                                            "Pattern '{}' triggered by: {}",
                                            pattern_id, trigger
                                        ),
                                        confidence: pattern.confidence,
                                        suggested_actions: vec![
                                            SuggestedAction {
                                                action_type: "create_task".to_string(),
                                                description: format!(
                                                    "Auto-generate: {}",
                                                    task_template.description_template
                                                ),
                                                parameters: HashMap::from([
                                                    ("template".to_string(), serde_json::to_string(task_template)?),
                                                    ("trigger_task".to_string(), last_task.id.clone()),
                                                ]),
                                                expected_impact: "Ensure complete feature implementation".to_string(),
                                            },
                                        ],
                                        risk_assessment: RiskLevel::Low,
                                    });
                                }
                            }
                        }
                    }
                }
            }
        }

        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:307-382 method initialize_pattern_library <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:724-790 method monitor_goals
  Similarity: 97.22%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:initialize_pattern_library (lines 307-382) ---[0m
    fn initialize_pattern_library() -> HashMap<String, TaskPattern> {
        let mut patterns = HashMap::new();

        // Frontend component pattern
        patterns.insert(
            "frontend_component".to_string(),
            TaskPattern {
                pattern_id: "frontend_component".to_string(),
                trigger_conditions: vec!["component created".to_string()],
                generated_tasks: vec![
                    TaskTemplate {
                        description_template: "Write unit tests for {component_name} component"
                            .to_string(),
                        task_type: TaskType::Testing,
                        priority: Priority::High,
                        estimated_duration: 30,
                        required_agent_type: "QA".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add {component_name} to component library docs"
                            .to_string(),
                        task_type: TaskType::Documentation,
                        priority: Priority::Medium,
                        estimated_duration: 15,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                confidence: 0.95,
                usage_count: 0,
            },
        );

        // API endpoint pattern
        patterns.insert(
            "api_endpoint".to_string(),
            TaskPattern {
                pattern_id: "api_endpoint".to_string(),
                trigger_conditions: vec!["API endpoint created".to_string()],
                generated_tasks: vec![
                    TaskTemplate {
                        description_template: "Write integration tests for {endpoint_name} API"
                            .to_string(),
                        task_type: TaskType::Testing,
                        priority: Priority::High,
                        estimated_duration: 45,
                        required_agent_type: "QA".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Update API documentation for {endpoint_name}"
                            .to_string(),
                        task_type: TaskType::Documentation,
                        priority: Priority::Medium,
                        estimated_duration: 20,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add rate limiting to {endpoint_name} endpoint"
                            .to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 25,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                confidence: 0.9,
                usage_count: 0,
            },
        );

        patterns
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:monitor_goals (lines 724-790) ---[0m
    async fn monitor_goals(&self) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let goals = self.goal_tracker.read().await;

        // Check objective progress
        for objective in &goals.objectives {
            if objective.progress < 0.5 {
                if let Some(deadline) = objective.deadline {
                    let time_remaining = deadline - Utc::now();
                    let days_remaining = time_remaining.num_days();

                    if days_remaining <= 7 && objective.progress < 0.8 {
                        decisions.push(ProactiveDecision {
                            decision_type: DecisionType::ChangeStrategy,
                            reasoning: format!(
                                "Objective '{}' is behind schedule: {}% complete with {} days remaining",
                                objective.title, (objective.progress * 100.0) as u32, days_remaining
                            ),
                            confidence: 0.85,
                            suggested_actions: vec![
                                SuggestedAction {
                                    action_type: "reprioritize_tasks".to_string(),
                                    description: "Focus resources on critical objective".to_string(),
                                    parameters: HashMap::from([
                                        ("objective_id".to_string(), objective.id.clone()),
                                    ]),
                                    expected_impact: "Improve deadline adherence".to_string(),
                                },
                            ],
                            risk_assessment: RiskLevel::Medium,
                        });
                    }
                }
            }
        }

        // Check sprint velocity
        if let Some(sprint) = &goals.current_sprint {
            let sprint_progress = (Utc::now() - sprint.start_date).num_days() as f64
                / (sprint.end_date - sprint.start_date).num_days() as f64;

            let velocity_progress = sprint.current_velocity as f64 / sprint.velocity_target as f64;

            if sprint_progress > 0.5 && velocity_progress < 0.3 {
                decisions.push(ProactiveDecision {
                    decision_type: DecisionType::ScaleTeam,
                    reasoning: format!(
                        "Sprint velocity is significantly behind: {}% of target with {}% time remaining",
                        (velocity_progress * 100.0) as u32,
                        ((1.0 - sprint_progress) * 100.0) as u32
                    ),
                    confidence: 0.7,
                    suggested_actions: vec![
                        SuggestedAction {
                            action_type: "add_agent".to_string(),
                            description: "Consider adding additional agents".to_string(),
                            parameters: HashMap::new(),
                            expected_impact: "Increase development velocity".to_string(),
                        },
                    ],
                    risk_assessment: RiskLevel::Medium,
                });
            }
        }

        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:307-382 method initialize_pattern_library <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:415-479 method initialize_feature_templates
  Similarity: 97.60%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:initialize_pattern_library (lines 307-382) ---[0m
    fn initialize_pattern_library() -> HashMap<String, TaskPattern> {
        let mut patterns = HashMap::new();

        // Frontend component pattern
        patterns.insert(
            "frontend_component".to_string(),
            TaskPattern {
                pattern_id: "frontend_component".to_string(),
                trigger_conditions: vec!["component created".to_string()],
                generated_tasks: vec![
                    TaskTemplate {
                        description_template: "Write unit tests for {component_name} component"
                            .to_string(),
                        task_type: TaskType::Testing,
                        priority: Priority::High,
                        estimated_duration: 30,
                        required_agent_type: "QA".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add {component_name} to component library docs"
                            .to_string(),
                        task_type: TaskType::Documentation,
                        priority: Priority::Medium,
                        estimated_duration: 15,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                confidence: 0.95,
                usage_count: 0,
            },
        );

        // API endpoint pattern
        patterns.insert(
            "api_endpoint".to_string(),
            TaskPattern {
                pattern_id: "api_endpoint".to_string(),
                trigger_conditions: vec!["API endpoint created".to_string()],
                generated_tasks: vec![
                    TaskTemplate {
                        description_template: "Write integration tests for {endpoint_name} API"
                            .to_string(),
                        task_type: TaskType::Testing,
                        priority: Priority::High,
                        estimated_duration: 45,
                        required_agent_type: "QA".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Update API documentation for {endpoint_name}"
                            .to_string(),
                        task_type: TaskType::Documentation,
                        priority: Priority::Medium,
                        estimated_duration: 20,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add rate limiting to {endpoint_name} endpoint"
                            .to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 25,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                confidence: 0.9,
                usage_count: 0,
            },
        );

        patterns
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:initialize_feature_templates (lines 415-479) ---[0m
    fn initialize_feature_templates() -> HashMap<String, FeatureTemplate> {
        let mut templates = HashMap::new();

        templates.insert(
            "user_authentication".to_string(),
            FeatureTemplate {
                feature_name: "User Authentication".to_string(),
                required_tasks: vec![
                    TaskTemplate {
                        description_template: "Create user registration API endpoint".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 120,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create user login API endpoint".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 90,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create registration form component".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 60,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create login form component".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 45,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                optional_tasks: vec![
                    TaskTemplate {
                        description_template: "Add social login integration".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 180,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add password reset functionality".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 90,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                typical_duration: 8, // hours
            },
        );

        templates
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:634-721 method predict_next_tasks <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:1116-1168 method handle_search_response
  Similarity: 96.14%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:predict_next_tasks (lines 634-721) ---[0m
    async fn predict_next_tasks(
        &self,
        agents: &DashMap<String, ClaudeCodeAgent>,
    ) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let predictor = self.task_predictor.read().await;

        // Analyze recent completions
        for entry in agents.iter() {
            let agent = entry.value();

            // Check last completed task
            if let Some((last_task, last_result)) = agent.task_history.last() {
                if last_result.success {
                    // Find matching completion patterns
                    for pattern in &predictor.completion_patterns {
                        if pattern.completed_task_type == last_task.task_type
                            && pattern.probability > 0.7
                        {
                            for task_template in &pattern.follow_up_tasks {
                                decisions.push(ProactiveDecision {
                                    decision_type: DecisionType::GenerateTask,
                                    reasoning: format!(
                                        "Pattern match: {:?} completion typically requires {}",
                                        last_task.task_type, task_template.description_template
                                    ),
                                    confidence: pattern.probability,
                                    suggested_actions: vec![SuggestedAction {
                                        action_type: "create_task".to_string(),
                                        description: format!(
                                            "Create follow-up task: {}",
                                            task_template.description_template
                                        ),
                                        parameters: HashMap::from([
                                            (
                                                "template".to_string(),
                                                serde_json::to_string(task_template)?,
                                            ),
                                            ("parent_task".to_string(), last_task.id.clone()),
                                        ]),
                                        expected_impact: "Maintain development momentum"
                                            .to_string(),
                                    }],
                                    risk_assessment: RiskLevel::Low,
                                });
                            }
                        }
                    }

                    // Check for pattern triggers in task description
                    let description_lower = last_task.description.to_lowercase();
                    for (pattern_id, pattern) in &predictor.pattern_library {
                        for trigger in &pattern.trigger_conditions {
                            if description_lower.contains(&trigger.to_lowercase()) {
                                for task_template in &pattern.generated_tasks {
                                    decisions.push(ProactiveDecision {
                                        decision_type: DecisionType::GenerateTask,
                                        reasoning: format!(
                                            "Pattern '{}' triggered by: {}",
                                            pattern_id, trigger
                                        ),
                                        confidence: pattern.confidence,
                                        suggested_actions: vec![
                                            SuggestedAction {
                                                action_type: "create_task".to_string(),
                                                description: format!(
                                                    "Auto-generate: {}",
                                                    task_template.description_template
                                                ),
                                                parameters: HashMap::from([
                                                    ("template".to_string(), serde_json::to_string(task_template)?),
                                                    ("trigger_task".to_string(), last_task.id.clone()),
                                                ]),
                                                expected_impact: "Ensure complete feature implementation".to_string(),
                                            },
                                        ],
                                        risk_assessment: RiskLevel::Low,
                                    });
                                }
                            }
                        }
                    }
                }
            }
        }

        Ok(decisions)
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:handle_search_response (lines 1116-1168) ---[0m
    pub async fn handle_search_response(
        &self,
        response: SearchResponse,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!(
            "Received search response with {} results",
            response.results.len()
        );

        // Analyze search results and create appropriate tasks or insights
        if !response.results.is_empty() {
            let mut insights = Vec::new();

            for (i, result) in response.results.iter().take(5).enumerate() {
                insights.push(format!(
                    "{}. {} - {} (relevance: {:?})",
                    i + 1,
                    result.title,
                    result.snippet,
                    result.relevance_score
                ));
            }

            // Create a task to review and apply the findings
            let review_task = Task::new(
                format!("review-search-{}", Uuid::new_v4()),
                format!("Review search findings for: {}", response.query_used),
                Priority::Medium,
                TaskType::Research,
            )
            .with_details(format!(
                "Search query: {}\nTop findings:\n{}\n\nPlease review these findings and apply relevant insights to the current work.",
                response.query_used,
                insights.join("\n")
            ))
            .with_duration(1200); // 20 minutes

            // Send task generation message
            coordination_bus
                .send_message(AgentMessage::TaskGenerated {
                    task_id: review_task.id.clone(),
                    description: review_task.description.clone(),
                    reasoning: format!(
                        "Search completed with {} relevant results",
                        response.results.len()
                    ),
                })
                .await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:307-382 method initialize_pattern_library <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:523-582 method analyze_agent_progress
  Similarity: 97.10%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:initialize_pattern_library (lines 307-382) ---[0m
    fn initialize_pattern_library() -> HashMap<String, TaskPattern> {
        let mut patterns = HashMap::new();

        // Frontend component pattern
        patterns.insert(
            "frontend_component".to_string(),
            TaskPattern {
                pattern_id: "frontend_component".to_string(),
                trigger_conditions: vec!["component created".to_string()],
                generated_tasks: vec![
                    TaskTemplate {
                        description_template: "Write unit tests for {component_name} component"
                            .to_string(),
                        task_type: TaskType::Testing,
                        priority: Priority::High,
                        estimated_duration: 30,
                        required_agent_type: "QA".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add {component_name} to component library docs"
                            .to_string(),
                        task_type: TaskType::Documentation,
                        priority: Priority::Medium,
                        estimated_duration: 15,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                confidence: 0.95,
                usage_count: 0,
            },
        );

        // API endpoint pattern
        patterns.insert(
            "api_endpoint".to_string(),
            TaskPattern {
                pattern_id: "api_endpoint".to_string(),
                trigger_conditions: vec!["API endpoint created".to_string()],
                generated_tasks: vec![
                    TaskTemplate {
                        description_template: "Write integration tests for {endpoint_name} API"
                            .to_string(),
                        task_type: TaskType::Testing,
                        priority: Priority::High,
                        estimated_duration: 45,
                        required_agent_type: "QA".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Update API documentation for {endpoint_name}"
                            .to_string(),
                        task_type: TaskType::Documentation,
                        priority: Priority::Medium,
                        estimated_duration: 20,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add rate limiting to {endpoint_name} endpoint"
                            .to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 25,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                confidence: 0.9,
                usage_count: 0,
            },
        );

        patterns
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:analyze_agent_progress (lines 523-582) ---[0m
    async fn analyze_agent_progress(
        &self,
        agents: &DashMap<String, ClaudeCodeAgent>,
    ) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let mut analyzer = self.progress_analyzer.write().await;

        for entry in agents.iter() {
            let agent = entry.value();
            let agent_id = &agent.identity.agent_id;

            // Check if agent is stuck
            if matches!(agent.status, AgentStatus::Working) {
                let time_since_activity = Utc::now() - agent.last_activity;
                if time_since_activity.num_minutes() > 15 {
                    decisions.push(ProactiveDecision {
                        decision_type: DecisionType::RequestIntervention,
                        reasoning: format!(
                            "Agent {} has been working on the same task for {} minutes without progress",
                            agent_id, time_since_activity.num_minutes()
                        ),
                        confidence: 0.9,
                        suggested_actions: vec![
                            SuggestedAction {
                                action_type: "check_agent_status".to_string(),
                                description: "Check if agent needs assistance".to_string(),
                                parameters: HashMap::from([
                                    ("agent_id".to_string(), agent_id.clone()),
                                ]),
                                expected_impact: "Unblock agent or reassign task".to_string(),
                            },
                        ],
                        risk_assessment: RiskLevel::Low,
                    });
                }
            }

            // Analyze agent efficiency
            let recent_tasks: Vec<_> = agent.task_history.iter().rev().take(5).collect();
            if recent_tasks.len() >= 3 {
                let avg_completion_time: f64 = recent_tasks
                    .iter()
                    .filter_map(|(_task, result)| {
                        if result.success {
                            Some(result.duration.as_secs() as f64 / 60.0)
                        } else {
                            None
                        }
                    })
                    .sum::<f64>()
                    / recent_tasks.len() as f64;

                analyzer
                    .efficiency_metrics
                    .insert(agent_id.clone(), avg_completion_time);
            }
        }

        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:585-631 method resolve_dependencies <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:634-721 method predict_next_tasks
  Similarity: 97.43%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:resolve_dependencies (lines 585-631) ---[0m
    async fn resolve_dependencies(&self) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let graph = self.dependency_graph.read().await;

        // Find blocked tasks
        for (task_id, node) in &graph.nodes {
            if node.status == TaskNodeStatus::Blocked {
                // Check if blocking dependencies are resolved
                if let Some(prerequisites) = graph.reverse_edges.get(task_id) {
                    let unresolved: Vec<_> = prerequisites
                        .iter()
                        .filter(|&prereq_id| {
                            graph
                                .nodes
                                .get(prereq_id)
                                .map(|n| n.status != TaskNodeStatus::Completed)
                                .unwrap_or(true)
                        })
                        .collect();

                    if unresolved.is_empty() {
                        // All dependencies resolved, task can be unblocked
                        decisions.push(ProactiveDecision {
                            decision_type: DecisionType::GenerateTask,
                            reasoning: format!(
                                "Task {} can be unblocked - all dependencies completed",
                                task_id
                            ),
                            confidence: 0.95,
                            suggested_actions: vec![SuggestedAction {
                                action_type: "unblock_task".to_string(),
                                description: "Move task to ready queue".to_string(),
                                parameters: HashMap::from([(
                                    "task_id".to_string(),
                                    task_id.clone(),
                                )]),
                                expected_impact: "Enable task execution".to_string(),
                            }],
                            risk_assessment: RiskLevel::Low,
                        });
                    }
                }
            }
        }

        Ok(decisions)
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:predict_next_tasks (lines 634-721) ---[0m
    async fn predict_next_tasks(
        &self,
        agents: &DashMap<String, ClaudeCodeAgent>,
    ) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let predictor = self.task_predictor.read().await;

        // Analyze recent completions
        for entry in agents.iter() {
            let agent = entry.value();

            // Check last completed task
            if let Some((last_task, last_result)) = agent.task_history.last() {
                if last_result.success {
                    // Find matching completion patterns
                    for pattern in &predictor.completion_patterns {
                        if pattern.completed_task_type == last_task.task_type
                            && pattern.probability > 0.7
                        {
                            for task_template in &pattern.follow_up_tasks {
                                decisions.push(ProactiveDecision {
                                    decision_type: DecisionType::GenerateTask,
                                    reasoning: format!(
                                        "Pattern match: {:?} completion typically requires {}",
                                        last_task.task_type, task_template.description_template
                                    ),
                                    confidence: pattern.probability,
                                    suggested_actions: vec![SuggestedAction {
                                        action_type: "create_task".to_string(),
                                        description: format!(
                                            "Create follow-up task: {}",
                                            task_template.description_template
                                        ),
                                        parameters: HashMap::from([
                                            (
                                                "template".to_string(),
                                                serde_json::to_string(task_template)?,
                                            ),
                                            ("parent_task".to_string(), last_task.id.clone()),
                                        ]),
                                        expected_impact: "Maintain development momentum"
                                            .to_string(),
                                    }],
                                    risk_assessment: RiskLevel::Low,
                                });
                            }
                        }
                    }

                    // Check for pattern triggers in task description
                    let description_lower = last_task.description.to_lowercase();
                    for (pattern_id, pattern) in &predictor.pattern_library {
                        for trigger in &pattern.trigger_conditions {
                            if description_lower.contains(&trigger.to_lowercase()) {
                                for task_template in &pattern.generated_tasks {
                                    decisions.push(ProactiveDecision {
                                        decision_type: DecisionType::GenerateTask,
                                        reasoning: format!(
                                            "Pattern '{}' triggered by: {}",
                                            pattern_id, trigger
                                        ),
                                        confidence: pattern.confidence,
                                        suggested_actions: vec![
                                            SuggestedAction {
                                                action_type: "create_task".to_string(),
                                                description: format!(
                                                    "Auto-generate: {}",
                                                    task_template.description_template
                                                ),
                                                parameters: HashMap::from([
                                                    ("template".to_string(), serde_json::to_string(task_template)?),
                                                    ("trigger_task".to_string(), last_task.id.clone()),
                                                ]),
                                                expected_impact: "Ensure complete feature implementation".to_string(),
                                            },
                                        ],
                                        risk_assessment: RiskLevel::Low,
                                    });
                                }
                            }
                        }
                    }
                }
            }
        }

        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:634-721 method predict_next_tasks <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:793-839 method execute_decision
  Similarity: 94.74%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:predict_next_tasks (lines 634-721) ---[0m
    async fn predict_next_tasks(
        &self,
        agents: &DashMap<String, ClaudeCodeAgent>,
    ) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let predictor = self.task_predictor.read().await;

        // Analyze recent completions
        for entry in agents.iter() {
            let agent = entry.value();

            // Check last completed task
            if let Some((last_task, last_result)) = agent.task_history.last() {
                if last_result.success {
                    // Find matching completion patterns
                    for pattern in &predictor.completion_patterns {
                        if pattern.completed_task_type == last_task.task_type
                            && pattern.probability > 0.7
                        {
                            for task_template in &pattern.follow_up_tasks {
                                decisions.push(ProactiveDecision {
                                    decision_type: DecisionType::GenerateTask,
                                    reasoning: format!(
                                        "Pattern match: {:?} completion typically requires {}",
                                        last_task.task_type, task_template.description_template
                                    ),
                                    confidence: pattern.probability,
                                    suggested_actions: vec![SuggestedAction {
                                        action_type: "create_task".to_string(),
                                        description: format!(
                                            "Create follow-up task: {}",
                                            task_template.description_template
                                        ),
                                        parameters: HashMap::from([
                                            (
                                                "template".to_string(),
                                                serde_json::to_string(task_template)?,
                                            ),
                                            ("parent_task".to_string(), last_task.id.clone()),
                                        ]),
                                        expected_impact: "Maintain development momentum"
                                            .to_string(),
                                    }],
                                    risk_assessment: RiskLevel::Low,
                                });
                            }
                        }
                    }

                    // Check for pattern triggers in task description
                    let description_lower = last_task.description.to_lowercase();
                    for (pattern_id, pattern) in &predictor.pattern_library {
                        for trigger in &pattern.trigger_conditions {
                            if description_lower.contains(&trigger.to_lowercase()) {
                                for task_template in &pattern.generated_tasks {
                                    decisions.push(ProactiveDecision {
                                        decision_type: DecisionType::GenerateTask,
                                        reasoning: format!(
                                            "Pattern '{}' triggered by: {}",
                                            pattern_id, trigger
                                        ),
                                        confidence: pattern.confidence,
                                        suggested_actions: vec![
                                            SuggestedAction {
                                                action_type: "create_task".to_string(),
                                                description: format!(
                                                    "Auto-generate: {}",
                                                    task_template.description_template
                                                ),
                                                parameters: HashMap::from([
                                                    ("template".to_string(), serde_json::to_string(task_template)?),
                                                    ("trigger_task".to_string(), last_task.id.clone()),
                                                ]),
                                                expected_impact: "Ensure complete feature implementation".to_string(),
                                            },
                                        ],
                                        risk_assessment: RiskLevel::Low,
                                    });
                                }
                            }
                        }
                    }
                }
            }
        }

        Ok(decisions)
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:execute_decision (lines 793-839) ---[0m
    async fn execute_decision(
        &self,
        decision: &ProactiveDecision,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Executing proactive decision: {:?}", decision.decision_type);

        for action in &decision.suggested_actions {
            match action.action_type.as_str() {
                "create_task" => {
                    if let Some(template_json) = action.parameters.get("template") {
                        let template: TaskTemplate = serde_json::from_str(template_json)?;
                        let task = self.create_task_from_template(&template).await?;

                        // Send task creation message
                        coordination_bus
                            .send_message(AgentMessage::TaskGenerated {
                                task_id: task.id.clone(),
                                description: task.description.clone(),
                                reasoning: decision.reasoning.clone(),
                            })
                            .await?;

                        info!("Auto-generated task: {}", task.description);
                    }
                }
                "unblock_task" => {
                    if let Some(task_id) = action.parameters.get("task_id") {
                        self.unblock_task(task_id).await?;
                    }
                }
                "request_search" => {
                    if let Some(query) = action.parameters.get("query") {
                        if let Some(context) = action.parameters.get("context") {
                            self.request_search(query, context, coordination_bus)
                                .await?;
                        }
                    }
                }
                _ => {
                    debug!("Skipping execution of action: {}", action.action_type);
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:307-382 method initialize_pattern_library <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:1116-1168 method handle_search_response
  Similarity: 96.72%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:initialize_pattern_library (lines 307-382) ---[0m
    fn initialize_pattern_library() -> HashMap<String, TaskPattern> {
        let mut patterns = HashMap::new();

        // Frontend component pattern
        patterns.insert(
            "frontend_component".to_string(),
            TaskPattern {
                pattern_id: "frontend_component".to_string(),
                trigger_conditions: vec!["component created".to_string()],
                generated_tasks: vec![
                    TaskTemplate {
                        description_template: "Write unit tests for {component_name} component"
                            .to_string(),
                        task_type: TaskType::Testing,
                        priority: Priority::High,
                        estimated_duration: 30,
                        required_agent_type: "QA".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add {component_name} to component library docs"
                            .to_string(),
                        task_type: TaskType::Documentation,
                        priority: Priority::Medium,
                        estimated_duration: 15,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                confidence: 0.95,
                usage_count: 0,
            },
        );

        // API endpoint pattern
        patterns.insert(
            "api_endpoint".to_string(),
            TaskPattern {
                pattern_id: "api_endpoint".to_string(),
                trigger_conditions: vec!["API endpoint created".to_string()],
                generated_tasks: vec![
                    TaskTemplate {
                        description_template: "Write integration tests for {endpoint_name} API"
                            .to_string(),
                        task_type: TaskType::Testing,
                        priority: Priority::High,
                        estimated_duration: 45,
                        required_agent_type: "QA".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Update API documentation for {endpoint_name}"
                            .to_string(),
                        task_type: TaskType::Documentation,
                        priority: Priority::Medium,
                        estimated_duration: 20,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add rate limiting to {endpoint_name} endpoint"
                            .to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 25,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                confidence: 0.9,
                usage_count: 0,
            },
        );

        patterns
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:handle_search_response (lines 1116-1168) ---[0m
    pub async fn handle_search_response(
        &self,
        response: SearchResponse,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!(
            "Received search response with {} results",
            response.results.len()
        );

        // Analyze search results and create appropriate tasks or insights
        if !response.results.is_empty() {
            let mut insights = Vec::new();

            for (i, result) in response.results.iter().take(5).enumerate() {
                insights.push(format!(
                    "{}. {} - {} (relevance: {:?})",
                    i + 1,
                    result.title,
                    result.snippet,
                    result.relevance_score
                ));
            }

            // Create a task to review and apply the findings
            let review_task = Task::new(
                format!("review-search-{}", Uuid::new_v4()),
                format!("Review search findings for: {}", response.query_used),
                Priority::Medium,
                TaskType::Research,
            )
            .with_details(format!(
                "Search query: {}\nTop findings:\n{}\n\nPlease review these findings and apply relevant insights to the current work.",
                response.query_used,
                insights.join("\n")
            ))
            .with_duration(1200); // 20 minutes

            // Send task generation message
            coordination_bus
                .send_message(AgentMessage::TaskGenerated {
                    task_id: review_task.id.clone(),
                    description: review_task.description.clone(),
                    reasoning: format!(
                        "Search completed with {} relevant results",
                        response.results.len()
                    ),
                })
                .await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:257-304 method new <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:634-721 method predict_next_tasks
  Similarity: 89.63%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:new (lines 257-304) ---[0m
    pub async fn new() -> Result<Self> {
        let project_context = Arc::new(RwLock::new(ProjectContext {
            project_type: "web_application".to_string(),
            tech_stack: vec![
                "React".to_string(),
                "Node.js".to_string(),
                "PostgreSQL".to_string(),
            ],
            features: vec![],
            current_phase: DevelopmentPhase::Planning,
            milestones: vec![],
            constraints: vec![],
        }));

        let dependency_graph = Arc::new(RwLock::new(DependencyGraph {
            nodes: HashMap::new(),
            edges: HashMap::new(),
            reverse_edges: HashMap::new(),
        }));

        let progress_analyzer = Arc::new(RwLock::new(ProgressAnalyzer {
            velocity_history: vec![],
            bottlenecks: vec![],
            efficiency_metrics: HashMap::new(),
            prediction_accuracy: 0.8,
        }));

        let task_predictor = Arc::new(RwLock::new(TaskPredictor {
            pattern_library: Self::initialize_pattern_library(),
            completion_patterns: Self::initialize_completion_patterns(),
            feature_templates: Self::initialize_feature_templates(),
        }));

        let goal_tracker = Arc::new(RwLock::new(GoalTracker {
            objectives: vec![],
            key_results: vec![],
            current_sprint: None,
            backlog: vec![],
        }));

        Ok(Self {
            project_context,
            dependency_graph,
            progress_analyzer,
            task_predictor,
            goal_tracker,
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:predict_next_tasks (lines 634-721) ---[0m
    async fn predict_next_tasks(
        &self,
        agents: &DashMap<String, ClaudeCodeAgent>,
    ) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let predictor = self.task_predictor.read().await;

        // Analyze recent completions
        for entry in agents.iter() {
            let agent = entry.value();

            // Check last completed task
            if let Some((last_task, last_result)) = agent.task_history.last() {
                if last_result.success {
                    // Find matching completion patterns
                    for pattern in &predictor.completion_patterns {
                        if pattern.completed_task_type == last_task.task_type
                            && pattern.probability > 0.7
                        {
                            for task_template in &pattern.follow_up_tasks {
                                decisions.push(ProactiveDecision {
                                    decision_type: DecisionType::GenerateTask,
                                    reasoning: format!(
                                        "Pattern match: {:?} completion typically requires {}",
                                        last_task.task_type, task_template.description_template
                                    ),
                                    confidence: pattern.probability,
                                    suggested_actions: vec![SuggestedAction {
                                        action_type: "create_task".to_string(),
                                        description: format!(
                                            "Create follow-up task: {}",
                                            task_template.description_template
                                        ),
                                        parameters: HashMap::from([
                                            (
                                                "template".to_string(),
                                                serde_json::to_string(task_template)?,
                                            ),
                                            ("parent_task".to_string(), last_task.id.clone()),
                                        ]),
                                        expected_impact: "Maintain development momentum"
                                            .to_string(),
                                    }],
                                    risk_assessment: RiskLevel::Low,
                                });
                            }
                        }
                    }

                    // Check for pattern triggers in task description
                    let description_lower = last_task.description.to_lowercase();
                    for (pattern_id, pattern) in &predictor.pattern_library {
                        for trigger in &pattern.trigger_conditions {
                            if description_lower.contains(&trigger.to_lowercase()) {
                                for task_template in &pattern.generated_tasks {
                                    decisions.push(ProactiveDecision {
                                        decision_type: DecisionType::GenerateTask,
                                        reasoning: format!(
                                            "Pattern '{}' triggered by: {}",
                                            pattern_id, trigger
                                        ),
                                        confidence: pattern.confidence,
                                        suggested_actions: vec![
                                            SuggestedAction {
                                                action_type: "create_task".to_string(),
                                                description: format!(
                                                    "Auto-generate: {}",
                                                    task_template.description_template
                                                ),
                                                parameters: HashMap::from([
                                                    ("template".to_string(), serde_json::to_string(task_template)?),
                                                    ("trigger_task".to_string(), last_task.id.clone()),
                                                ]),
                                                expected_impact: "Ensure complete feature implementation".to_string(),
                                            },
                                        ],
                                        risk_assessment: RiskLevel::Low,
                                    });
                                }
                            }
                        }
                    }
                }
            }
        }

        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:415-479 method initialize_feature_templates <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:724-790 method monitor_goals
  Similarity: 92.20%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:initialize_feature_templates (lines 415-479) ---[0m
    fn initialize_feature_templates() -> HashMap<String, FeatureTemplate> {
        let mut templates = HashMap::new();

        templates.insert(
            "user_authentication".to_string(),
            FeatureTemplate {
                feature_name: "User Authentication".to_string(),
                required_tasks: vec![
                    TaskTemplate {
                        description_template: "Create user registration API endpoint".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 120,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create user login API endpoint".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 90,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create registration form component".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 60,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create login form component".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 45,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                optional_tasks: vec![
                    TaskTemplate {
                        description_template: "Add social login integration".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 180,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add password reset functionality".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 90,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                typical_duration: 8, // hours
            },
        );

        templates
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:monitor_goals (lines 724-790) ---[0m
    async fn monitor_goals(&self) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let goals = self.goal_tracker.read().await;

        // Check objective progress
        for objective in &goals.objectives {
            if objective.progress < 0.5 {
                if let Some(deadline) = objective.deadline {
                    let time_remaining = deadline - Utc::now();
                    let days_remaining = time_remaining.num_days();

                    if days_remaining <= 7 && objective.progress < 0.8 {
                        decisions.push(ProactiveDecision {
                            decision_type: DecisionType::ChangeStrategy,
                            reasoning: format!(
                                "Objective '{}' is behind schedule: {}% complete with {} days remaining",
                                objective.title, (objective.progress * 100.0) as u32, days_remaining
                            ),
                            confidence: 0.85,
                            suggested_actions: vec![
                                SuggestedAction {
                                    action_type: "reprioritize_tasks".to_string(),
                                    description: "Focus resources on critical objective".to_string(),
                                    parameters: HashMap::from([
                                        ("objective_id".to_string(), objective.id.clone()),
                                    ]),
                                    expected_impact: "Improve deadline adherence".to_string(),
                                },
                            ],
                            risk_assessment: RiskLevel::Medium,
                        });
                    }
                }
            }
        }

        // Check sprint velocity
        if let Some(sprint) = &goals.current_sprint {
            let sprint_progress = (Utc::now() - sprint.start_date).num_days() as f64
                / (sprint.end_date - sprint.start_date).num_days() as f64;

            let velocity_progress = sprint.current_velocity as f64 / sprint.velocity_target as f64;

            if sprint_progress > 0.5 && velocity_progress < 0.3 {
                decisions.push(ProactiveDecision {
                    decision_type: DecisionType::ScaleTeam,
                    reasoning: format!(
                        "Sprint velocity is significantly behind: {}% of target with {}% time remaining",
                        (velocity_progress * 100.0) as u32,
                        ((1.0 - sprint_progress) * 100.0) as u32
                    ),
                    confidence: 0.7,
                    suggested_actions: vec![
                        SuggestedAction {
                            action_type: "add_agent".to_string(),
                            description: "Consider adding additional agents".to_string(),
                            parameters: HashMap::new(),
                            expected_impact: "Increase development velocity".to_string(),
                        },
                    ],
                    risk_assessment: RiskLevel::Medium,
                });
            }
        }

        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:415-479 method initialize_feature_templates <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:523-582 method analyze_agent_progress
  Similarity: 97.01%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:initialize_feature_templates (lines 415-479) ---[0m
    fn initialize_feature_templates() -> HashMap<String, FeatureTemplate> {
        let mut templates = HashMap::new();

        templates.insert(
            "user_authentication".to_string(),
            FeatureTemplate {
                feature_name: "User Authentication".to_string(),
                required_tasks: vec![
                    TaskTemplate {
                        description_template: "Create user registration API endpoint".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 120,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create user login API endpoint".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 90,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create registration form component".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 60,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create login form component".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 45,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                optional_tasks: vec![
                    TaskTemplate {
                        description_template: "Add social login integration".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 180,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add password reset functionality".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 90,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                typical_duration: 8, // hours
            },
        );

        templates
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:analyze_agent_progress (lines 523-582) ---[0m
    async fn analyze_agent_progress(
        &self,
        agents: &DashMap<String, ClaudeCodeAgent>,
    ) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let mut analyzer = self.progress_analyzer.write().await;

        for entry in agents.iter() {
            let agent = entry.value();
            let agent_id = &agent.identity.agent_id;

            // Check if agent is stuck
            if matches!(agent.status, AgentStatus::Working) {
                let time_since_activity = Utc::now() - agent.last_activity;
                if time_since_activity.num_minutes() > 15 {
                    decisions.push(ProactiveDecision {
                        decision_type: DecisionType::RequestIntervention,
                        reasoning: format!(
                            "Agent {} has been working on the same task for {} minutes without progress",
                            agent_id, time_since_activity.num_minutes()
                        ),
                        confidence: 0.9,
                        suggested_actions: vec![
                            SuggestedAction {
                                action_type: "check_agent_status".to_string(),
                                description: "Check if agent needs assistance".to_string(),
                                parameters: HashMap::from([
                                    ("agent_id".to_string(), agent_id.clone()),
                                ]),
                                expected_impact: "Unblock agent or reassign task".to_string(),
                            },
                        ],
                        risk_assessment: RiskLevel::Low,
                    });
                }
            }

            // Analyze agent efficiency
            let recent_tasks: Vec<_> = agent.task_history.iter().rev().take(5).collect();
            if recent_tasks.len() >= 3 {
                let avg_completion_time: f64 = recent_tasks
                    .iter()
                    .filter_map(|(_task, result)| {
                        if result.success {
                            Some(result.duration.as_secs() as f64 / 60.0)
                        } else {
                            None
                        }
                    })
                    .sum::<f64>()
                    / recent_tasks.len() as f64;

                analyzer
                    .efficiency_metrics
                    .insert(agent_id.clone(), avg_completion_time);
            }
        }

        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:307-382 method initialize_pattern_library <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:585-631 method resolve_dependencies
  Similarity: 97.97%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:initialize_pattern_library (lines 307-382) ---[0m
    fn initialize_pattern_library() -> HashMap<String, TaskPattern> {
        let mut patterns = HashMap::new();

        // Frontend component pattern
        patterns.insert(
            "frontend_component".to_string(),
            TaskPattern {
                pattern_id: "frontend_component".to_string(),
                trigger_conditions: vec!["component created".to_string()],
                generated_tasks: vec![
                    TaskTemplate {
                        description_template: "Write unit tests for {component_name} component"
                            .to_string(),
                        task_type: TaskType::Testing,
                        priority: Priority::High,
                        estimated_duration: 30,
                        required_agent_type: "QA".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add {component_name} to component library docs"
                            .to_string(),
                        task_type: TaskType::Documentation,
                        priority: Priority::Medium,
                        estimated_duration: 15,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                confidence: 0.95,
                usage_count: 0,
            },
        );

        // API endpoint pattern
        patterns.insert(
            "api_endpoint".to_string(),
            TaskPattern {
                pattern_id: "api_endpoint".to_string(),
                trigger_conditions: vec!["API endpoint created".to_string()],
                generated_tasks: vec![
                    TaskTemplate {
                        description_template: "Write integration tests for {endpoint_name} API"
                            .to_string(),
                        task_type: TaskType::Testing,
                        priority: Priority::High,
                        estimated_duration: 45,
                        required_agent_type: "QA".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Update API documentation for {endpoint_name}"
                            .to_string(),
                        task_type: TaskType::Documentation,
                        priority: Priority::Medium,
                        estimated_duration: 20,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add rate limiting to {endpoint_name} endpoint"
                            .to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 25,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                confidence: 0.9,
                usage_count: 0,
            },
        );

        patterns
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:resolve_dependencies (lines 585-631) ---[0m
    async fn resolve_dependencies(&self) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let graph = self.dependency_graph.read().await;

        // Find blocked tasks
        for (task_id, node) in &graph.nodes {
            if node.status == TaskNodeStatus::Blocked {
                // Check if blocking dependencies are resolved
                if let Some(prerequisites) = graph.reverse_edges.get(task_id) {
                    let unresolved: Vec<_> = prerequisites
                        .iter()
                        .filter(|&prereq_id| {
                            graph
                                .nodes
                                .get(prereq_id)
                                .map(|n| n.status != TaskNodeStatus::Completed)
                                .unwrap_or(true)
                        })
                        .collect();

                    if unresolved.is_empty() {
                        // All dependencies resolved, task can be unblocked
                        decisions.push(ProactiveDecision {
                            decision_type: DecisionType::GenerateTask,
                            reasoning: format!(
                                "Task {} can be unblocked - all dependencies completed",
                                task_id
                            ),
                            confidence: 0.95,
                            suggested_actions: vec![SuggestedAction {
                                action_type: "unblock_task".to_string(),
                                description: "Move task to ready queue".to_string(),
                                parameters: HashMap::from([(
                                    "task_id".to_string(),
                                    task_id.clone(),
                                )]),
                                expected_impact: "Enable task execution".to_string(),
                            }],
                            risk_assessment: RiskLevel::Low,
                        });
                    }
                }
            }
        }

        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:257-304 method new <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:307-382 method initialize_pattern_library
  Similarity: 95.77%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:new (lines 257-304) ---[0m
    pub async fn new() -> Result<Self> {
        let project_context = Arc::new(RwLock::new(ProjectContext {
            project_type: "web_application".to_string(),
            tech_stack: vec![
                "React".to_string(),
                "Node.js".to_string(),
                "PostgreSQL".to_string(),
            ],
            features: vec![],
            current_phase: DevelopmentPhase::Planning,
            milestones: vec![],
            constraints: vec![],
        }));

        let dependency_graph = Arc::new(RwLock::new(DependencyGraph {
            nodes: HashMap::new(),
            edges: HashMap::new(),
            reverse_edges: HashMap::new(),
        }));

        let progress_analyzer = Arc::new(RwLock::new(ProgressAnalyzer {
            velocity_history: vec![],
            bottlenecks: vec![],
            efficiency_metrics: HashMap::new(),
            prediction_accuracy: 0.8,
        }));

        let task_predictor = Arc::new(RwLock::new(TaskPredictor {
            pattern_library: Self::initialize_pattern_library(),
            completion_patterns: Self::initialize_completion_patterns(),
            feature_templates: Self::initialize_feature_templates(),
        }));

        let goal_tracker = Arc::new(RwLock::new(GoalTracker {
            objectives: vec![],
            key_results: vec![],
            current_sprint: None,
            backlog: vec![],
        }));

        Ok(Self {
            project_context,
            dependency_graph,
            progress_analyzer,
            task_predictor,
            goal_tracker,
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:initialize_pattern_library (lines 307-382) ---[0m
    fn initialize_pattern_library() -> HashMap<String, TaskPattern> {
        let mut patterns = HashMap::new();

        // Frontend component pattern
        patterns.insert(
            "frontend_component".to_string(),
            TaskPattern {
                pattern_id: "frontend_component".to_string(),
                trigger_conditions: vec!["component created".to_string()],
                generated_tasks: vec![
                    TaskTemplate {
                        description_template: "Write unit tests for {component_name} component"
                            .to_string(),
                        task_type: TaskType::Testing,
                        priority: Priority::High,
                        estimated_duration: 30,
                        required_agent_type: "QA".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add {component_name} to component library docs"
                            .to_string(),
                        task_type: TaskType::Documentation,
                        priority: Priority::Medium,
                        estimated_duration: 15,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                confidence: 0.95,
                usage_count: 0,
            },
        );

        // API endpoint pattern
        patterns.insert(
            "api_endpoint".to_string(),
            TaskPattern {
                pattern_id: "api_endpoint".to_string(),
                trigger_conditions: vec!["API endpoint created".to_string()],
                generated_tasks: vec![
                    TaskTemplate {
                        description_template: "Write integration tests for {endpoint_name} API"
                            .to_string(),
                        task_type: TaskType::Testing,
                        priority: Priority::High,
                        estimated_duration: 45,
                        required_agent_type: "QA".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Update API documentation for {endpoint_name}"
                            .to_string(),
                        task_type: TaskType::Documentation,
                        priority: Priority::Medium,
                        estimated_duration: 20,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add rate limiting to {endpoint_name} endpoint"
                            .to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 25,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                confidence: 0.9,
                usage_count: 0,
            },
        );

        patterns
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:523-582 method analyze_agent_progress <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:724-790 method monitor_goals
  Similarity: 92.28%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:analyze_agent_progress (lines 523-582) ---[0m
    async fn analyze_agent_progress(
        &self,
        agents: &DashMap<String, ClaudeCodeAgent>,
    ) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let mut analyzer = self.progress_analyzer.write().await;

        for entry in agents.iter() {
            let agent = entry.value();
            let agent_id = &agent.identity.agent_id;

            // Check if agent is stuck
            if matches!(agent.status, AgentStatus::Working) {
                let time_since_activity = Utc::now() - agent.last_activity;
                if time_since_activity.num_minutes() > 15 {
                    decisions.push(ProactiveDecision {
                        decision_type: DecisionType::RequestIntervention,
                        reasoning: format!(
                            "Agent {} has been working on the same task for {} minutes without progress",
                            agent_id, time_since_activity.num_minutes()
                        ),
                        confidence: 0.9,
                        suggested_actions: vec![
                            SuggestedAction {
                                action_type: "check_agent_status".to_string(),
                                description: "Check if agent needs assistance".to_string(),
                                parameters: HashMap::from([
                                    ("agent_id".to_string(), agent_id.clone()),
                                ]),
                                expected_impact: "Unblock agent or reassign task".to_string(),
                            },
                        ],
                        risk_assessment: RiskLevel::Low,
                    });
                }
            }

            // Analyze agent efficiency
            let recent_tasks: Vec<_> = agent.task_history.iter().rev().take(5).collect();
            if recent_tasks.len() >= 3 {
                let avg_completion_time: f64 = recent_tasks
                    .iter()
                    .filter_map(|(_task, result)| {
                        if result.success {
                            Some(result.duration.as_secs() as f64 / 60.0)
                        } else {
                            None
                        }
                    })
                    .sum::<f64>()
                    / recent_tasks.len() as f64;

                analyzer
                    .efficiency_metrics
                    .insert(agent_id.clone(), avg_completion_time);
            }
        }

        Ok(decisions)
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:monitor_goals (lines 724-790) ---[0m
    async fn monitor_goals(&self) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let goals = self.goal_tracker.read().await;

        // Check objective progress
        for objective in &goals.objectives {
            if objective.progress < 0.5 {
                if let Some(deadline) = objective.deadline {
                    let time_remaining = deadline - Utc::now();
                    let days_remaining = time_remaining.num_days();

                    if days_remaining <= 7 && objective.progress < 0.8 {
                        decisions.push(ProactiveDecision {
                            decision_type: DecisionType::ChangeStrategy,
                            reasoning: format!(
                                "Objective '{}' is behind schedule: {}% complete with {} days remaining",
                                objective.title, (objective.progress * 100.0) as u32, days_remaining
                            ),
                            confidence: 0.85,
                            suggested_actions: vec![
                                SuggestedAction {
                                    action_type: "reprioritize_tasks".to_string(),
                                    description: "Focus resources on critical objective".to_string(),
                                    parameters: HashMap::from([
                                        ("objective_id".to_string(), objective.id.clone()),
                                    ]),
                                    expected_impact: "Improve deadline adherence".to_string(),
                                },
                            ],
                            risk_assessment: RiskLevel::Medium,
                        });
                    }
                }
            }
        }

        // Check sprint velocity
        if let Some(sprint) = &goals.current_sprint {
            let sprint_progress = (Utc::now() - sprint.start_date).num_days() as f64
                / (sprint.end_date - sprint.start_date).num_days() as f64;

            let velocity_progress = sprint.current_velocity as f64 / sprint.velocity_target as f64;

            if sprint_progress > 0.5 && velocity_progress < 0.3 {
                decisions.push(ProactiveDecision {
                    decision_type: DecisionType::ScaleTeam,
                    reasoning: format!(
                        "Sprint velocity is significantly behind: {}% of target with {}% time remaining",
                        (velocity_progress * 100.0) as u32,
                        ((1.0 - sprint_progress) * 100.0) as u32
                    ),
                    confidence: 0.7,
                    suggested_actions: vec![
                        SuggestedAction {
                            action_type: "add_agent".to_string(),
                            description: "Consider adding additional agents".to_string(),
                            parameters: HashMap::new(),
                            expected_impact: "Increase development velocity".to_string(),
                        },
                    ],
                    risk_assessment: RiskLevel::Medium,
                });
            }
        }

        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:307-382 method initialize_pattern_library <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:793-839 method execute_decision
  Similarity: 94.10%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:initialize_pattern_library (lines 307-382) ---[0m
    fn initialize_pattern_library() -> HashMap<String, TaskPattern> {
        let mut patterns = HashMap::new();

        // Frontend component pattern
        patterns.insert(
            "frontend_component".to_string(),
            TaskPattern {
                pattern_id: "frontend_component".to_string(),
                trigger_conditions: vec!["component created".to_string()],
                generated_tasks: vec![
                    TaskTemplate {
                        description_template: "Write unit tests for {component_name} component"
                            .to_string(),
                        task_type: TaskType::Testing,
                        priority: Priority::High,
                        estimated_duration: 30,
                        required_agent_type: "QA".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add {component_name} to component library docs"
                            .to_string(),
                        task_type: TaskType::Documentation,
                        priority: Priority::Medium,
                        estimated_duration: 15,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                confidence: 0.95,
                usage_count: 0,
            },
        );

        // API endpoint pattern
        patterns.insert(
            "api_endpoint".to_string(),
            TaskPattern {
                pattern_id: "api_endpoint".to_string(),
                trigger_conditions: vec!["API endpoint created".to_string()],
                generated_tasks: vec![
                    TaskTemplate {
                        description_template: "Write integration tests for {endpoint_name} API"
                            .to_string(),
                        task_type: TaskType::Testing,
                        priority: Priority::High,
                        estimated_duration: 45,
                        required_agent_type: "QA".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Update API documentation for {endpoint_name}"
                            .to_string(),
                        task_type: TaskType::Documentation,
                        priority: Priority::Medium,
                        estimated_duration: 20,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add rate limiting to {endpoint_name} endpoint"
                            .to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 25,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                confidence: 0.9,
                usage_count: 0,
            },
        );

        patterns
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:execute_decision (lines 793-839) ---[0m
    async fn execute_decision(
        &self,
        decision: &ProactiveDecision,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Executing proactive decision: {:?}", decision.decision_type);

        for action in &decision.suggested_actions {
            match action.action_type.as_str() {
                "create_task" => {
                    if let Some(template_json) = action.parameters.get("template") {
                        let template: TaskTemplate = serde_json::from_str(template_json)?;
                        let task = self.create_task_from_template(&template).await?;

                        // Send task creation message
                        coordination_bus
                            .send_message(AgentMessage::TaskGenerated {
                                task_id: task.id.clone(),
                                description: task.description.clone(),
                                reasoning: decision.reasoning.clone(),
                            })
                            .await?;

                        info!("Auto-generated task: {}", task.description);
                    }
                }
                "unblock_task" => {
                    if let Some(task_id) = action.parameters.get("task_id") {
                        self.unblock_task(task_id).await?;
                    }
                }
                "request_search" => {
                    if let Some(query) = action.parameters.get("query") {
                        if let Some(context) = action.parameters.get("context") {
                            self.request_search(query, context, coordination_bus)
                                .await?;
                        }
                    }
                }
                _ => {
                    debug!("Skipping execution of action: {}", action.action_type);
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:415-479 method initialize_feature_templates <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:1116-1168 method handle_search_response
  Similarity: 95.76%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:initialize_feature_templates (lines 415-479) ---[0m
    fn initialize_feature_templates() -> HashMap<String, FeatureTemplate> {
        let mut templates = HashMap::new();

        templates.insert(
            "user_authentication".to_string(),
            FeatureTemplate {
                feature_name: "User Authentication".to_string(),
                required_tasks: vec![
                    TaskTemplate {
                        description_template: "Create user registration API endpoint".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 120,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create user login API endpoint".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 90,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create registration form component".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 60,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create login form component".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 45,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                optional_tasks: vec![
                    TaskTemplate {
                        description_template: "Add social login integration".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 180,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add password reset functionality".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 90,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                typical_duration: 8, // hours
            },
        );

        templates
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:handle_search_response (lines 1116-1168) ---[0m
    pub async fn handle_search_response(
        &self,
        response: SearchResponse,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!(
            "Received search response with {} results",
            response.results.len()
        );

        // Analyze search results and create appropriate tasks or insights
        if !response.results.is_empty() {
            let mut insights = Vec::new();

            for (i, result) in response.results.iter().take(5).enumerate() {
                insights.push(format!(
                    "{}. {} - {} (relevance: {:?})",
                    i + 1,
                    result.title,
                    result.snippet,
                    result.relevance_score
                ));
            }

            // Create a task to review and apply the findings
            let review_task = Task::new(
                format!("review-search-{}", Uuid::new_v4()),
                format!("Review search findings for: {}", response.query_used),
                Priority::Medium,
                TaskType::Research,
            )
            .with_details(format!(
                "Search query: {}\nTop findings:\n{}\n\nPlease review these findings and apply relevant insights to the current work.",
                response.query_used,
                insights.join("\n")
            ))
            .with_duration(1200); // 20 minutes

            // Send task generation message
            coordination_bus
                .send_message(AgentMessage::TaskGenerated {
                    task_id: review_task.id.clone(),
                    description: review_task.description.clone(),
                    reasoning: format!(
                        "Search completed with {} relevant results",
                        response.results.len()
                    ),
                })
                .await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:724-790 method monitor_goals <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:1116-1168 method handle_search_response
  Similarity: 93.32%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:monitor_goals (lines 724-790) ---[0m
    async fn monitor_goals(&self) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let goals = self.goal_tracker.read().await;

        // Check objective progress
        for objective in &goals.objectives {
            if objective.progress < 0.5 {
                if let Some(deadline) = objective.deadline {
                    let time_remaining = deadline - Utc::now();
                    let days_remaining = time_remaining.num_days();

                    if days_remaining <= 7 && objective.progress < 0.8 {
                        decisions.push(ProactiveDecision {
                            decision_type: DecisionType::ChangeStrategy,
                            reasoning: format!(
                                "Objective '{}' is behind schedule: {}% complete with {} days remaining",
                                objective.title, (objective.progress * 100.0) as u32, days_remaining
                            ),
                            confidence: 0.85,
                            suggested_actions: vec![
                                SuggestedAction {
                                    action_type: "reprioritize_tasks".to_string(),
                                    description: "Focus resources on critical objective".to_string(),
                                    parameters: HashMap::from([
                                        ("objective_id".to_string(), objective.id.clone()),
                                    ]),
                                    expected_impact: "Improve deadline adherence".to_string(),
                                },
                            ],
                            risk_assessment: RiskLevel::Medium,
                        });
                    }
                }
            }
        }

        // Check sprint velocity
        if let Some(sprint) = &goals.current_sprint {
            let sprint_progress = (Utc::now() - sprint.start_date).num_days() as f64
                / (sprint.end_date - sprint.start_date).num_days() as f64;

            let velocity_progress = sprint.current_velocity as f64 / sprint.velocity_target as f64;

            if sprint_progress > 0.5 && velocity_progress < 0.3 {
                decisions.push(ProactiveDecision {
                    decision_type: DecisionType::ScaleTeam,
                    reasoning: format!(
                        "Sprint velocity is significantly behind: {}% of target with {}% time remaining",
                        (velocity_progress * 100.0) as u32,
                        ((1.0 - sprint_progress) * 100.0) as u32
                    ),
                    confidence: 0.7,
                    suggested_actions: vec![
                        SuggestedAction {
                            action_type: "add_agent".to_string(),
                            description: "Consider adding additional agents".to_string(),
                            parameters: HashMap::new(),
                            expected_impact: "Increase development velocity".to_string(),
                        },
                    ],
                    risk_assessment: RiskLevel::Medium,
                });
            }
        }

        Ok(decisions)
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:handle_search_response (lines 1116-1168) ---[0m
    pub async fn handle_search_response(
        &self,
        response: SearchResponse,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!(
            "Received search response with {} results",
            response.results.len()
        );

        // Analyze search results and create appropriate tasks or insights
        if !response.results.is_empty() {
            let mut insights = Vec::new();

            for (i, result) in response.results.iter().take(5).enumerate() {
                insights.push(format!(
                    "{}. {} - {} (relevance: {:?})",
                    i + 1,
                    result.title,
                    result.snippet,
                    result.relevance_score
                ));
            }

            // Create a task to review and apply the findings
            let review_task = Task::new(
                format!("review-search-{}", Uuid::new_v4()),
                format!("Review search findings for: {}", response.query_used),
                Priority::Medium,
                TaskType::Research,
            )
            .with_details(format!(
                "Search query: {}\nTop findings:\n{}\n\nPlease review these findings and apply relevant insights to the current work.",
                response.query_used,
                insights.join("\n")
            ))
            .with_duration(1200); // 20 minutes

            // Send task generation message
            coordination_bus
                .send_message(AgentMessage::TaskGenerated {
                    task_id: review_task.id.clone(),
                    description: review_task.description.clone(),
                    reasoning: format!(
                        "Search completed with {} relevant results",
                        response.results.len()
                    ),
                })
                .await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:585-631 method resolve_dependencies <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:724-790 method monitor_goals
  Similarity: 97.18%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:resolve_dependencies (lines 585-631) ---[0m
    async fn resolve_dependencies(&self) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let graph = self.dependency_graph.read().await;

        // Find blocked tasks
        for (task_id, node) in &graph.nodes {
            if node.status == TaskNodeStatus::Blocked {
                // Check if blocking dependencies are resolved
                if let Some(prerequisites) = graph.reverse_edges.get(task_id) {
                    let unresolved: Vec<_> = prerequisites
                        .iter()
                        .filter(|&prereq_id| {
                            graph
                                .nodes
                                .get(prereq_id)
                                .map(|n| n.status != TaskNodeStatus::Completed)
                                .unwrap_or(true)
                        })
                        .collect();

                    if unresolved.is_empty() {
                        // All dependencies resolved, task can be unblocked
                        decisions.push(ProactiveDecision {
                            decision_type: DecisionType::GenerateTask,
                            reasoning: format!(
                                "Task {} can be unblocked - all dependencies completed",
                                task_id
                            ),
                            confidence: 0.95,
                            suggested_actions: vec![SuggestedAction {
                                action_type: "unblock_task".to_string(),
                                description: "Move task to ready queue".to_string(),
                                parameters: HashMap::from([(
                                    "task_id".to_string(),
                                    task_id.clone(),
                                )]),
                                expected_impact: "Enable task execution".to_string(),
                            }],
                            risk_assessment: RiskLevel::Low,
                        });
                    }
                }
            }
        }

        Ok(decisions)
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:monitor_goals (lines 724-790) ---[0m
    async fn monitor_goals(&self) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let goals = self.goal_tracker.read().await;

        // Check objective progress
        for objective in &goals.objectives {
            if objective.progress < 0.5 {
                if let Some(deadline) = objective.deadline {
                    let time_remaining = deadline - Utc::now();
                    let days_remaining = time_remaining.num_days();

                    if days_remaining <= 7 && objective.progress < 0.8 {
                        decisions.push(ProactiveDecision {
                            decision_type: DecisionType::ChangeStrategy,
                            reasoning: format!(
                                "Objective '{}' is behind schedule: {}% complete with {} days remaining",
                                objective.title, (objective.progress * 100.0) as u32, days_remaining
                            ),
                            confidence: 0.85,
                            suggested_actions: vec![
                                SuggestedAction {
                                    action_type: "reprioritize_tasks".to_string(),
                                    description: "Focus resources on critical objective".to_string(),
                                    parameters: HashMap::from([
                                        ("objective_id".to_string(), objective.id.clone()),
                                    ]),
                                    expected_impact: "Improve deadline adherence".to_string(),
                                },
                            ],
                            risk_assessment: RiskLevel::Medium,
                        });
                    }
                }
            }
        }

        // Check sprint velocity
        if let Some(sprint) = &goals.current_sprint {
            let sprint_progress = (Utc::now() - sprint.start_date).num_days() as f64
                / (sprint.end_date - sprint.start_date).num_days() as f64;

            let velocity_progress = sprint.current_velocity as f64 / sprint.velocity_target as f64;

            if sprint_progress > 0.5 && velocity_progress < 0.3 {
                decisions.push(ProactiveDecision {
                    decision_type: DecisionType::ScaleTeam,
                    reasoning: format!(
                        "Sprint velocity is significantly behind: {}% of target with {}% time remaining",
                        (velocity_progress * 100.0) as u32,
                        ((1.0 - sprint_progress) * 100.0) as u32
                    ),
                    confidence: 0.7,
                    suggested_actions: vec![
                        SuggestedAction {
                            action_type: "add_agent".to_string(),
                            description: "Consider adding additional agents".to_string(),
                            parameters: HashMap::new(),
                            expected_impact: "Increase development velocity".to_string(),
                        },
                    ],
                    risk_assessment: RiskLevel::Medium,
                });
            }
        }

        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:415-479 method initialize_feature_templates <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:585-631 method resolve_dependencies
  Similarity: 96.35%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:initialize_feature_templates (lines 415-479) ---[0m
    fn initialize_feature_templates() -> HashMap<String, FeatureTemplate> {
        let mut templates = HashMap::new();

        templates.insert(
            "user_authentication".to_string(),
            FeatureTemplate {
                feature_name: "User Authentication".to_string(),
                required_tasks: vec![
                    TaskTemplate {
                        description_template: "Create user registration API endpoint".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 120,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create user login API endpoint".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 90,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create registration form component".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 60,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create login form component".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 45,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                optional_tasks: vec![
                    TaskTemplate {
                        description_template: "Add social login integration".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 180,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add password reset functionality".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 90,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                typical_duration: 8, // hours
            },
        );

        templates
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:resolve_dependencies (lines 585-631) ---[0m
    async fn resolve_dependencies(&self) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let graph = self.dependency_graph.read().await;

        // Find blocked tasks
        for (task_id, node) in &graph.nodes {
            if node.status == TaskNodeStatus::Blocked {
                // Check if blocking dependencies are resolved
                if let Some(prerequisites) = graph.reverse_edges.get(task_id) {
                    let unresolved: Vec<_> = prerequisites
                        .iter()
                        .filter(|&prereq_id| {
                            graph
                                .nodes
                                .get(prereq_id)
                                .map(|n| n.status != TaskNodeStatus::Completed)
                                .unwrap_or(true)
                        })
                        .collect();

                    if unresolved.is_empty() {
                        // All dependencies resolved, task can be unblocked
                        decisions.push(ProactiveDecision {
                            decision_type: DecisionType::GenerateTask,
                            reasoning: format!(
                                "Task {} can be unblocked - all dependencies completed",
                                task_id
                            ),
                            confidence: 0.95,
                            suggested_actions: vec![SuggestedAction {
                                action_type: "unblock_task".to_string(),
                                description: "Move task to ready queue".to_string(),
                                parameters: HashMap::from([(
                                    "task_id".to_string(),
                                    task_id.clone(),
                                )]),
                                expected_impact: "Enable task execution".to_string(),
                            }],
                            risk_assessment: RiskLevel::Low,
                        });
                    }
                }
            }
        }

        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:415-479 method initialize_feature_templates <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:793-839 method execute_decision
  Similarity: 94.98%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:initialize_feature_templates (lines 415-479) ---[0m
    fn initialize_feature_templates() -> HashMap<String, FeatureTemplate> {
        let mut templates = HashMap::new();

        templates.insert(
            "user_authentication".to_string(),
            FeatureTemplate {
                feature_name: "User Authentication".to_string(),
                required_tasks: vec![
                    TaskTemplate {
                        description_template: "Create user registration API endpoint".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 120,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create user login API endpoint".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 90,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create registration form component".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 60,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create login form component".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 45,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                optional_tasks: vec![
                    TaskTemplate {
                        description_template: "Add social login integration".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 180,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add password reset functionality".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 90,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                typical_duration: 8, // hours
            },
        );

        templates
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:execute_decision (lines 793-839) ---[0m
    async fn execute_decision(
        &self,
        decision: &ProactiveDecision,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Executing proactive decision: {:?}", decision.decision_type);

        for action in &decision.suggested_actions {
            match action.action_type.as_str() {
                "create_task" => {
                    if let Some(template_json) = action.parameters.get("template") {
                        let template: TaskTemplate = serde_json::from_str(template_json)?;
                        let task = self.create_task_from_template(&template).await?;

                        // Send task creation message
                        coordination_bus
                            .send_message(AgentMessage::TaskGenerated {
                                task_id: task.id.clone(),
                                description: task.description.clone(),
                                reasoning: decision.reasoning.clone(),
                            })
                            .await?;

                        info!("Auto-generated task: {}", task.description);
                    }
                }
                "unblock_task" => {
                    if let Some(task_id) = action.parameters.get("task_id") {
                        self.unblock_task(task_id).await?;
                    }
                }
                "request_search" => {
                    if let Some(query) = action.parameters.get("query") {
                        if let Some(context) = action.parameters.get("context") {
                            self.request_search(query, context, coordination_bus)
                                .await?;
                        }
                    }
                }
                _ => {
                    debug!("Skipping execution of action: {}", action.action_type);
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:523-582 method analyze_agent_progress <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:1116-1168 method handle_search_response
  Similarity: 93.53%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:analyze_agent_progress (lines 523-582) ---[0m
    async fn analyze_agent_progress(
        &self,
        agents: &DashMap<String, ClaudeCodeAgent>,
    ) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let mut analyzer = self.progress_analyzer.write().await;

        for entry in agents.iter() {
            let agent = entry.value();
            let agent_id = &agent.identity.agent_id;

            // Check if agent is stuck
            if matches!(agent.status, AgentStatus::Working) {
                let time_since_activity = Utc::now() - agent.last_activity;
                if time_since_activity.num_minutes() > 15 {
                    decisions.push(ProactiveDecision {
                        decision_type: DecisionType::RequestIntervention,
                        reasoning: format!(
                            "Agent {} has been working on the same task for {} minutes without progress",
                            agent_id, time_since_activity.num_minutes()
                        ),
                        confidence: 0.9,
                        suggested_actions: vec![
                            SuggestedAction {
                                action_type: "check_agent_status".to_string(),
                                description: "Check if agent needs assistance".to_string(),
                                parameters: HashMap::from([
                                    ("agent_id".to_string(), agent_id.clone()),
                                ]),
                                expected_impact: "Unblock agent or reassign task".to_string(),
                            },
                        ],
                        risk_assessment: RiskLevel::Low,
                    });
                }
            }

            // Analyze agent efficiency
            let recent_tasks: Vec<_> = agent.task_history.iter().rev().take(5).collect();
            if recent_tasks.len() >= 3 {
                let avg_completion_time: f64 = recent_tasks
                    .iter()
                    .filter_map(|(_task, result)| {
                        if result.success {
                            Some(result.duration.as_secs() as f64 / 60.0)
                        } else {
                            None
                        }
                    })
                    .sum::<f64>()
                    / recent_tasks.len() as f64;

                analyzer
                    .efficiency_metrics
                    .insert(agent_id.clone(), avg_completion_time);
            }
        }

        Ok(decisions)
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:handle_search_response (lines 1116-1168) ---[0m
    pub async fn handle_search_response(
        &self,
        response: SearchResponse,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!(
            "Received search response with {} results",
            response.results.len()
        );

        // Analyze search results and create appropriate tasks or insights
        if !response.results.is_empty() {
            let mut insights = Vec::new();

            for (i, result) in response.results.iter().take(5).enumerate() {
                insights.push(format!(
                    "{}. {} - {} (relevance: {:?})",
                    i + 1,
                    result.title,
                    result.snippet,
                    result.relevance_score
                ));
            }

            // Create a task to review and apply the findings
            let review_task = Task::new(
                format!("review-search-{}", Uuid::new_v4()),
                format!("Review search findings for: {}", response.query_used),
                Priority::Medium,
                TaskType::Research,
            )
            .with_details(format!(
                "Search query: {}\nTop findings:\n{}\n\nPlease review these findings and apply relevant insights to the current work.",
                response.query_used,
                insights.join("\n")
            ))
            .with_duration(1200); // 20 minutes

            // Send task generation message
            coordination_bus
                .send_message(AgentMessage::TaskGenerated {
                    task_id: review_task.id.clone(),
                    description: review_task.description.clone(),
                    reasoning: format!(
                        "Search completed with {} relevant results",
                        response.results.len()
                    ),
                })
                .await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:724-790 method monitor_goals <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:793-839 method execute_decision
  Similarity: 92.52%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:monitor_goals (lines 724-790) ---[0m
    async fn monitor_goals(&self) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let goals = self.goal_tracker.read().await;

        // Check objective progress
        for objective in &goals.objectives {
            if objective.progress < 0.5 {
                if let Some(deadline) = objective.deadline {
                    let time_remaining = deadline - Utc::now();
                    let days_remaining = time_remaining.num_days();

                    if days_remaining <= 7 && objective.progress < 0.8 {
                        decisions.push(ProactiveDecision {
                            decision_type: DecisionType::ChangeStrategy,
                            reasoning: format!(
                                "Objective '{}' is behind schedule: {}% complete with {} days remaining",
                                objective.title, (objective.progress * 100.0) as u32, days_remaining
                            ),
                            confidence: 0.85,
                            suggested_actions: vec![
                                SuggestedAction {
                                    action_type: "reprioritize_tasks".to_string(),
                                    description: "Focus resources on critical objective".to_string(),
                                    parameters: HashMap::from([
                                        ("objective_id".to_string(), objective.id.clone()),
                                    ]),
                                    expected_impact: "Improve deadline adherence".to_string(),
                                },
                            ],
                            risk_assessment: RiskLevel::Medium,
                        });
                    }
                }
            }
        }

        // Check sprint velocity
        if let Some(sprint) = &goals.current_sprint {
            let sprint_progress = (Utc::now() - sprint.start_date).num_days() as f64
                / (sprint.end_date - sprint.start_date).num_days() as f64;

            let velocity_progress = sprint.current_velocity as f64 / sprint.velocity_target as f64;

            if sprint_progress > 0.5 && velocity_progress < 0.3 {
                decisions.push(ProactiveDecision {
                    decision_type: DecisionType::ScaleTeam,
                    reasoning: format!(
                        "Sprint velocity is significantly behind: {}% of target with {}% time remaining",
                        (velocity_progress * 100.0) as u32,
                        ((1.0 - sprint_progress) * 100.0) as u32
                    ),
                    confidence: 0.7,
                    suggested_actions: vec![
                        SuggestedAction {
                            action_type: "add_agent".to_string(),
                            description: "Consider adding additional agents".to_string(),
                            parameters: HashMap::new(),
                            expected_impact: "Increase development velocity".to_string(),
                        },
                    ],
                    risk_assessment: RiskLevel::Medium,
                });
            }
        }

        Ok(decisions)
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:execute_decision (lines 793-839) ---[0m
    async fn execute_decision(
        &self,
        decision: &ProactiveDecision,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Executing proactive decision: {:?}", decision.decision_type);

        for action in &decision.suggested_actions {
            match action.action_type.as_str() {
                "create_task" => {
                    if let Some(template_json) = action.parameters.get("template") {
                        let template: TaskTemplate = serde_json::from_str(template_json)?;
                        let task = self.create_task_from_template(&template).await?;

                        // Send task creation message
                        coordination_bus
                            .send_message(AgentMessage::TaskGenerated {
                                task_id: task.id.clone(),
                                description: task.description.clone(),
                                reasoning: decision.reasoning.clone(),
                            })
                            .await?;

                        info!("Auto-generated task: {}", task.description);
                    }
                }
                "unblock_task" => {
                    if let Some(task_id) = action.parameters.get("task_id") {
                        self.unblock_task(task_id).await?;
                    }
                }
                "request_search" => {
                    if let Some(query) = action.parameters.get("query") {
                        if let Some(context) = action.parameters.get("context") {
                            self.request_search(query, context, coordination_bus)
                                .await?;
                        }
                    }
                }
                _ => {
                    debug!("Skipping execution of action: {}", action.action_type);
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:257-304 method new <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:724-790 method monitor_goals
  Similarity: 91.68%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:new (lines 257-304) ---[0m
    pub async fn new() -> Result<Self> {
        let project_context = Arc::new(RwLock::new(ProjectContext {
            project_type: "web_application".to_string(),
            tech_stack: vec![
                "React".to_string(),
                "Node.js".to_string(),
                "PostgreSQL".to_string(),
            ],
            features: vec![],
            current_phase: DevelopmentPhase::Planning,
            milestones: vec![],
            constraints: vec![],
        }));

        let dependency_graph = Arc::new(RwLock::new(DependencyGraph {
            nodes: HashMap::new(),
            edges: HashMap::new(),
            reverse_edges: HashMap::new(),
        }));

        let progress_analyzer = Arc::new(RwLock::new(ProgressAnalyzer {
            velocity_history: vec![],
            bottlenecks: vec![],
            efficiency_metrics: HashMap::new(),
            prediction_accuracy: 0.8,
        }));

        let task_predictor = Arc::new(RwLock::new(TaskPredictor {
            pattern_library: Self::initialize_pattern_library(),
            completion_patterns: Self::initialize_completion_patterns(),
            feature_templates: Self::initialize_feature_templates(),
        }));

        let goal_tracker = Arc::new(RwLock::new(GoalTracker {
            objectives: vec![],
            key_results: vec![],
            current_sprint: None,
            backlog: vec![],
        }));

        Ok(Self {
            project_context,
            dependency_graph,
            progress_analyzer,
            task_predictor,
            goal_tracker,
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:monitor_goals (lines 724-790) ---[0m
    async fn monitor_goals(&self) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let goals = self.goal_tracker.read().await;

        // Check objective progress
        for objective in &goals.objectives {
            if objective.progress < 0.5 {
                if let Some(deadline) = objective.deadline {
                    let time_remaining = deadline - Utc::now();
                    let days_remaining = time_remaining.num_days();

                    if days_remaining <= 7 && objective.progress < 0.8 {
                        decisions.push(ProactiveDecision {
                            decision_type: DecisionType::ChangeStrategy,
                            reasoning: format!(
                                "Objective '{}' is behind schedule: {}% complete with {} days remaining",
                                objective.title, (objective.progress * 100.0) as u32, days_remaining
                            ),
                            confidence: 0.85,
                            suggested_actions: vec![
                                SuggestedAction {
                                    action_type: "reprioritize_tasks".to_string(),
                                    description: "Focus resources on critical objective".to_string(),
                                    parameters: HashMap::from([
                                        ("objective_id".to_string(), objective.id.clone()),
                                    ]),
                                    expected_impact: "Improve deadline adherence".to_string(),
                                },
                            ],
                            risk_assessment: RiskLevel::Medium,
                        });
                    }
                }
            }
        }

        // Check sprint velocity
        if let Some(sprint) = &goals.current_sprint {
            let sprint_progress = (Utc::now() - sprint.start_date).num_days() as f64
                / (sprint.end_date - sprint.start_date).num_days() as f64;

            let velocity_progress = sprint.current_velocity as f64 / sprint.velocity_target as f64;

            if sprint_progress > 0.5 && velocity_progress < 0.3 {
                decisions.push(ProactiveDecision {
                    decision_type: DecisionType::ScaleTeam,
                    reasoning: format!(
                        "Sprint velocity is significantly behind: {}% of target with {}% time remaining",
                        (velocity_progress * 100.0) as u32,
                        ((1.0 - sprint_progress) * 100.0) as u32
                    ),
                    confidence: 0.7,
                    suggested_actions: vec![
                        SuggestedAction {
                            action_type: "add_agent".to_string(),
                            description: "Consider adding additional agents".to_string(),
                            parameters: HashMap::new(),
                            expected_impact: "Increase development velocity".to_string(),
                        },
                    ],
                    risk_assessment: RiskLevel::Medium,
                });
            }
        }

        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:523-582 method analyze_agent_progress <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:585-631 method resolve_dependencies
  Similarity: 96.88%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:analyze_agent_progress (lines 523-582) ---[0m
    async fn analyze_agent_progress(
        &self,
        agents: &DashMap<String, ClaudeCodeAgent>,
    ) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let mut analyzer = self.progress_analyzer.write().await;

        for entry in agents.iter() {
            let agent = entry.value();
            let agent_id = &agent.identity.agent_id;

            // Check if agent is stuck
            if matches!(agent.status, AgentStatus::Working) {
                let time_since_activity = Utc::now() - agent.last_activity;
                if time_since_activity.num_minutes() > 15 {
                    decisions.push(ProactiveDecision {
                        decision_type: DecisionType::RequestIntervention,
                        reasoning: format!(
                            "Agent {} has been working on the same task for {} minutes without progress",
                            agent_id, time_since_activity.num_minutes()
                        ),
                        confidence: 0.9,
                        suggested_actions: vec![
                            SuggestedAction {
                                action_type: "check_agent_status".to_string(),
                                description: "Check if agent needs assistance".to_string(),
                                parameters: HashMap::from([
                                    ("agent_id".to_string(), agent_id.clone()),
                                ]),
                                expected_impact: "Unblock agent or reassign task".to_string(),
                            },
                        ],
                        risk_assessment: RiskLevel::Low,
                    });
                }
            }

            // Analyze agent efficiency
            let recent_tasks: Vec<_> = agent.task_history.iter().rev().take(5).collect();
            if recent_tasks.len() >= 3 {
                let avg_completion_time: f64 = recent_tasks
                    .iter()
                    .filter_map(|(_task, result)| {
                        if result.success {
                            Some(result.duration.as_secs() as f64 / 60.0)
                        } else {
                            None
                        }
                    })
                    .sum::<f64>()
                    / recent_tasks.len() as f64;

                analyzer
                    .efficiency_metrics
                    .insert(agent_id.clone(), avg_completion_time);
            }
        }

        Ok(decisions)
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:resolve_dependencies (lines 585-631) ---[0m
    async fn resolve_dependencies(&self) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let graph = self.dependency_graph.read().await;

        // Find blocked tasks
        for (task_id, node) in &graph.nodes {
            if node.status == TaskNodeStatus::Blocked {
                // Check if blocking dependencies are resolved
                if let Some(prerequisites) = graph.reverse_edges.get(task_id) {
                    let unresolved: Vec<_> = prerequisites
                        .iter()
                        .filter(|&prereq_id| {
                            graph
                                .nodes
                                .get(prereq_id)
                                .map(|n| n.status != TaskNodeStatus::Completed)
                                .unwrap_or(true)
                        })
                        .collect();

                    if unresolved.is_empty() {
                        // All dependencies resolved, task can be unblocked
                        decisions.push(ProactiveDecision {
                            decision_type: DecisionType::GenerateTask,
                            reasoning: format!(
                                "Task {} can be unblocked - all dependencies completed",
                                task_id
                            ),
                            confidence: 0.95,
                            suggested_actions: vec![SuggestedAction {
                                action_type: "unblock_task".to_string(),
                                description: "Move task to ready queue".to_string(),
                                parameters: HashMap::from([(
                                    "task_id".to_string(),
                                    task_id.clone(),
                                )]),
                                expected_impact: "Enable task execution".to_string(),
                            }],
                            risk_assessment: RiskLevel::Low,
                        });
                    }
                }
            }
        }

        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:523-582 method analyze_agent_progress <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:793-839 method execute_decision
  Similarity: 93.53%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:analyze_agent_progress (lines 523-582) ---[0m
    async fn analyze_agent_progress(
        &self,
        agents: &DashMap<String, ClaudeCodeAgent>,
    ) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let mut analyzer = self.progress_analyzer.write().await;

        for entry in agents.iter() {
            let agent = entry.value();
            let agent_id = &agent.identity.agent_id;

            // Check if agent is stuck
            if matches!(agent.status, AgentStatus::Working) {
                let time_since_activity = Utc::now() - agent.last_activity;
                if time_since_activity.num_minutes() > 15 {
                    decisions.push(ProactiveDecision {
                        decision_type: DecisionType::RequestIntervention,
                        reasoning: format!(
                            "Agent {} has been working on the same task for {} minutes without progress",
                            agent_id, time_since_activity.num_minutes()
                        ),
                        confidence: 0.9,
                        suggested_actions: vec![
                            SuggestedAction {
                                action_type: "check_agent_status".to_string(),
                                description: "Check if agent needs assistance".to_string(),
                                parameters: HashMap::from([
                                    ("agent_id".to_string(), agent_id.clone()),
                                ]),
                                expected_impact: "Unblock agent or reassign task".to_string(),
                            },
                        ],
                        risk_assessment: RiskLevel::Low,
                    });
                }
            }

            // Analyze agent efficiency
            let recent_tasks: Vec<_> = agent.task_history.iter().rev().take(5).collect();
            if recent_tasks.len() >= 3 {
                let avg_completion_time: f64 = recent_tasks
                    .iter()
                    .filter_map(|(_task, result)| {
                        if result.success {
                            Some(result.duration.as_secs() as f64 / 60.0)
                        } else {
                            None
                        }
                    })
                    .sum::<f64>()
                    / recent_tasks.len() as f64;

                analyzer
                    .efficiency_metrics
                    .insert(agent_id.clone(), avg_completion_time);
            }
        }

        Ok(decisions)
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:execute_decision (lines 793-839) ---[0m
    async fn execute_decision(
        &self,
        decision: &ProactiveDecision,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Executing proactive decision: {:?}", decision.decision_type);

        for action in &decision.suggested_actions {
            match action.action_type.as_str() {
                "create_task" => {
                    if let Some(template_json) = action.parameters.get("template") {
                        let template: TaskTemplate = serde_json::from_str(template_json)?;
                        let task = self.create_task_from_template(&template).await?;

                        // Send task creation message
                        coordination_bus
                            .send_message(AgentMessage::TaskGenerated {
                                task_id: task.id.clone(),
                                description: task.description.clone(),
                                reasoning: decision.reasoning.clone(),
                            })
                            .await?;

                        info!("Auto-generated task: {}", task.description);
                    }
                }
                "unblock_task" => {
                    if let Some(task_id) = action.parameters.get("task_id") {
                        self.unblock_task(task_id).await?;
                    }
                }
                "request_search" => {
                    if let Some(query) = action.parameters.get("query") {
                        if let Some(context) = action.parameters.get("context") {
                            self.request_search(query, context, coordination_bus)
                                .await?;
                        }
                    }
                }
                _ => {
                    debug!("Skipping execution of action: {}", action.action_type);
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:793-839 method execute_decision <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:1116-1168 method handle_search_response
  Similarity: 95.21%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:execute_decision (lines 793-839) ---[0m
    async fn execute_decision(
        &self,
        decision: &ProactiveDecision,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Executing proactive decision: {:?}", decision.decision_type);

        for action in &decision.suggested_actions {
            match action.action_type.as_str() {
                "create_task" => {
                    if let Some(template_json) = action.parameters.get("template") {
                        let template: TaskTemplate = serde_json::from_str(template_json)?;
                        let task = self.create_task_from_template(&template).await?;

                        // Send task creation message
                        coordination_bus
                            .send_message(AgentMessage::TaskGenerated {
                                task_id: task.id.clone(),
                                description: task.description.clone(),
                                reasoning: decision.reasoning.clone(),
                            })
                            .await?;

                        info!("Auto-generated task: {}", task.description);
                    }
                }
                "unblock_task" => {
                    if let Some(task_id) = action.parameters.get("task_id") {
                        self.unblock_task(task_id).await?;
                    }
                }
                "request_search" => {
                    if let Some(query) = action.parameters.get("query") {
                        if let Some(context) = action.parameters.get("context") {
                            self.request_search(query, context, coordination_bus)
                                .await?;
                        }
                    }
                }
                _ => {
                    debug!("Skipping execution of action: {}", action.action_type);
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:handle_search_response (lines 1116-1168) ---[0m
    pub async fn handle_search_response(
        &self,
        response: SearchResponse,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!(
            "Received search response with {} results",
            response.results.len()
        );

        // Analyze search results and create appropriate tasks or insights
        if !response.results.is_empty() {
            let mut insights = Vec::new();

            for (i, result) in response.results.iter().take(5).enumerate() {
                insights.push(format!(
                    "{}. {} - {} (relevance: {:?})",
                    i + 1,
                    result.title,
                    result.snippet,
                    result.relevance_score
                ));
            }

            // Create a task to review and apply the findings
            let review_task = Task::new(
                format!("review-search-{}", Uuid::new_v4()),
                format!("Review search findings for: {}", response.query_used),
                Priority::Medium,
                TaskType::Research,
            )
            .with_details(format!(
                "Search query: {}\nTop findings:\n{}\n\nPlease review these findings and apply relevant insights to the current work.",
                response.query_used,
                insights.join("\n")
            ))
            .with_duration(1200); // 20 minutes

            // Send task generation message
            coordination_bus
                .send_message(AgentMessage::TaskGenerated {
                    task_id: review_task.id.clone(),
                    description: review_task.description.clone(),
                    reasoning: format!(
                        "Search completed with {} relevant results",
                        response.results.len()
                    ),
                })
                .await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:585-631 method resolve_dependencies <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:1116-1168 method handle_search_response
  Similarity: 93.25%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:resolve_dependencies (lines 585-631) ---[0m
    async fn resolve_dependencies(&self) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let graph = self.dependency_graph.read().await;

        // Find blocked tasks
        for (task_id, node) in &graph.nodes {
            if node.status == TaskNodeStatus::Blocked {
                // Check if blocking dependencies are resolved
                if let Some(prerequisites) = graph.reverse_edges.get(task_id) {
                    let unresolved: Vec<_> = prerequisites
                        .iter()
                        .filter(|&prereq_id| {
                            graph
                                .nodes
                                .get(prereq_id)
                                .map(|n| n.status != TaskNodeStatus::Completed)
                                .unwrap_or(true)
                        })
                        .collect();

                    if unresolved.is_empty() {
                        // All dependencies resolved, task can be unblocked
                        decisions.push(ProactiveDecision {
                            decision_type: DecisionType::GenerateTask,
                            reasoning: format!(
                                "Task {} can be unblocked - all dependencies completed",
                                task_id
                            ),
                            confidence: 0.95,
                            suggested_actions: vec![SuggestedAction {
                                action_type: "unblock_task".to_string(),
                                description: "Move task to ready queue".to_string(),
                                parameters: HashMap::from([(
                                    "task_id".to_string(),
                                    task_id.clone(),
                                )]),
                                expected_impact: "Enable task execution".to_string(),
                            }],
                            risk_assessment: RiskLevel::Low,
                        });
                    }
                }
            }
        }

        Ok(decisions)
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:handle_search_response (lines 1116-1168) ---[0m
    pub async fn handle_search_response(
        &self,
        response: SearchResponse,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!(
            "Received search response with {} results",
            response.results.len()
        );

        // Analyze search results and create appropriate tasks or insights
        if !response.results.is_empty() {
            let mut insights = Vec::new();

            for (i, result) in response.results.iter().take(5).enumerate() {
                insights.push(format!(
                    "{}. {} - {} (relevance: {:?})",
                    i + 1,
                    result.title,
                    result.snippet,
                    result.relevance_score
                ));
            }

            // Create a task to review and apply the findings
            let review_task = Task::new(
                format!("review-search-{}", Uuid::new_v4()),
                format!("Review search findings for: {}", response.query_used),
                Priority::Medium,
                TaskType::Research,
            )
            .with_details(format!(
                "Search query: {}\nTop findings:\n{}\n\nPlease review these findings and apply relevant insights to the current work.",
                response.query_used,
                insights.join("\n")
            ))
            .with_duration(1200); // 20 minutes

            // Send task generation message
            coordination_bus
                .send_message(AgentMessage::TaskGenerated {
                    task_id: review_task.id.clone(),
                    description: review_task.description.clone(),
                    reasoning: format!(
                        "Search completed with {} relevant results",
                        response.results.len()
                    ),
                })
                .await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:415-479 method initialize_feature_templates <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:865-890 method update_context_from_completion
  Similarity: 96.54%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:initialize_feature_templates (lines 415-479) ---[0m
    fn initialize_feature_templates() -> HashMap<String, FeatureTemplate> {
        let mut templates = HashMap::new();

        templates.insert(
            "user_authentication".to_string(),
            FeatureTemplate {
                feature_name: "User Authentication".to_string(),
                required_tasks: vec![
                    TaskTemplate {
                        description_template: "Create user registration API endpoint".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 120,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create user login API endpoint".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 90,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create registration form component".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 60,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create login form component".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 45,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                optional_tasks: vec![
                    TaskTemplate {
                        description_template: "Add social login integration".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 180,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add password reset functionality".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 90,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                typical_duration: 8, // hours
            },
        );

        templates
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:update_context_from_completion (lines 865-890) ---[0m
    pub async fn update_context_from_completion(
        &self,
        task: &Task,
        result: &TaskResult,
    ) -> Result<()> {
        if result.success {
            let mut context = self.project_context.write().await;

            // Analyze task to understand what was built
            if task.description.to_lowercase().contains("component") {
                let component_name = self.extract_component_name(&task.description);
                if !context.features.contains(&component_name) {
                    context.features.push(component_name);
                }
            }

            // Update dependency graph
            let mut graph = self.dependency_graph.write().await;
            if let Some(node) = graph.nodes.get_mut(&task.id) {
                node.status = TaskNodeStatus::Completed;
                node.actual_duration = Some(result.duration.as_secs() / 60); // convert to minutes
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:585-631 method resolve_dependencies <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:793-839 method execute_decision
  Similarity: 92.73%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:resolve_dependencies (lines 585-631) ---[0m
    async fn resolve_dependencies(&self) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let graph = self.dependency_graph.read().await;

        // Find blocked tasks
        for (task_id, node) in &graph.nodes {
            if node.status == TaskNodeStatus::Blocked {
                // Check if blocking dependencies are resolved
                if let Some(prerequisites) = graph.reverse_edges.get(task_id) {
                    let unresolved: Vec<_> = prerequisites
                        .iter()
                        .filter(|&prereq_id| {
                            graph
                                .nodes
                                .get(prereq_id)
                                .map(|n| n.status != TaskNodeStatus::Completed)
                                .unwrap_or(true)
                        })
                        .collect();

                    if unresolved.is_empty() {
                        // All dependencies resolved, task can be unblocked
                        decisions.push(ProactiveDecision {
                            decision_type: DecisionType::GenerateTask,
                            reasoning: format!(
                                "Task {} can be unblocked - all dependencies completed",
                                task_id
                            ),
                            confidence: 0.95,
                            suggested_actions: vec![SuggestedAction {
                                action_type: "unblock_task".to_string(),
                                description: "Move task to ready queue".to_string(),
                                parameters: HashMap::from([(
                                    "task_id".to_string(),
                                    task_id.clone(),
                                )]),
                                expected_impact: "Enable task execution".to_string(),
                            }],
                            risk_assessment: RiskLevel::Low,
                        });
                    }
                }
            }
        }

        Ok(decisions)
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:execute_decision (lines 793-839) ---[0m
    async fn execute_decision(
        &self,
        decision: &ProactiveDecision,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Executing proactive decision: {:?}", decision.decision_type);

        for action in &decision.suggested_actions {
            match action.action_type.as_str() {
                "create_task" => {
                    if let Some(template_json) = action.parameters.get("template") {
                        let template: TaskTemplate = serde_json::from_str(template_json)?;
                        let task = self.create_task_from_template(&template).await?;

                        // Send task creation message
                        coordination_bus
                            .send_message(AgentMessage::TaskGenerated {
                                task_id: task.id.clone(),
                                description: task.description.clone(),
                                reasoning: decision.reasoning.clone(),
                            })
                            .await?;

                        info!("Auto-generated task: {}", task.description);
                    }
                }
                "unblock_task" => {
                    if let Some(task_id) = action.parameters.get("task_id") {
                        self.unblock_task(task_id).await?;
                    }
                }
                "request_search" => {
                    if let Some(query) = action.parameters.get("query") {
                        if let Some(context) = action.parameters.get("context") {
                            self.request_search(query, context, coordination_bus)
                                .await?;
                        }
                    }
                }
                _ => {
                    debug!("Skipping execution of action: {}", action.action_type);
                }
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:415-479 method initialize_feature_templates <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:1086-1113 method request_search
  Similarity: 92.18%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:initialize_feature_templates (lines 415-479) ---[0m
    fn initialize_feature_templates() -> HashMap<String, FeatureTemplate> {
        let mut templates = HashMap::new();

        templates.insert(
            "user_authentication".to_string(),
            FeatureTemplate {
                feature_name: "User Authentication".to_string(),
                required_tasks: vec![
                    TaskTemplate {
                        description_template: "Create user registration API endpoint".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 120,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create user login API endpoint".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 90,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create registration form component".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 60,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Create login form component".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::High,
                        estimated_duration: 45,
                        required_agent_type: "Frontend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                optional_tasks: vec![
                    TaskTemplate {
                        description_template: "Add social login integration".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 180,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                    TaskTemplate {
                        description_template: "Add password reset functionality".to_string(),
                        task_type: TaskType::Development,
                        priority: Priority::Medium,
                        estimated_duration: 90,
                        required_agent_type: "Backend".to_string(),
                        variables: HashMap::new(),
                    },
                ],
                typical_duration: 8, // hours
            },
        );

        templates
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:request_search (lines 1086-1113) ---[0m
    async fn request_search(
        &self,
        query: &str,
        context: &str,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Requesting search for: {}", query);

        let search_request = SearchRequest {
            requesting_agent: "master-claude".to_string(),
            query: query.to_string(),
            scope: crate::agent::search_agent::SearchScope::All,
            max_results: Some(10),
            filters: None,
            context: Some(context.to_string()),
        };

        let message = AgentMessage::Coordination {
            from_agent: "master-claude".to_string(),
            to_agent: "search".to_string(),
            message_type: CoordinationType::Custom("search_request".to_string()),
            payload: serde_json::to_value(search_request)?,
        };

        coordination_bus.send_message(message).await?;

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:257-304 method new <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:585-631 method resolve_dependencies
  Similarity: 84.81%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:new (lines 257-304) ---[0m
    pub async fn new() -> Result<Self> {
        let project_context = Arc::new(RwLock::new(ProjectContext {
            project_type: "web_application".to_string(),
            tech_stack: vec![
                "React".to_string(),
                "Node.js".to_string(),
                "PostgreSQL".to_string(),
            ],
            features: vec![],
            current_phase: DevelopmentPhase::Planning,
            milestones: vec![],
            constraints: vec![],
        }));

        let dependency_graph = Arc::new(RwLock::new(DependencyGraph {
            nodes: HashMap::new(),
            edges: HashMap::new(),
            reverse_edges: HashMap::new(),
        }));

        let progress_analyzer = Arc::new(RwLock::new(ProgressAnalyzer {
            velocity_history: vec![],
            bottlenecks: vec![],
            efficiency_metrics: HashMap::new(),
            prediction_accuracy: 0.8,
        }));

        let task_predictor = Arc::new(RwLock::new(TaskPredictor {
            pattern_library: Self::initialize_pattern_library(),
            completion_patterns: Self::initialize_completion_patterns(),
            feature_templates: Self::initialize_feature_templates(),
        }));

        let goal_tracker = Arc::new(RwLock::new(GoalTracker {
            objectives: vec![],
            key_results: vec![],
            current_sprint: None,
            backlog: vec![],
        }));

        Ok(Self {
            project_context,
            dependency_graph,
            progress_analyzer,
            task_predictor,
            goal_tracker,
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:resolve_dependencies (lines 585-631) ---[0m
    async fn resolve_dependencies(&self) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let graph = self.dependency_graph.read().await;

        // Find blocked tasks
        for (task_id, node) in &graph.nodes {
            if node.status == TaskNodeStatus::Blocked {
                // Check if blocking dependencies are resolved
                if let Some(prerequisites) = graph.reverse_edges.get(task_id) {
                    let unresolved: Vec<_> = prerequisites
                        .iter()
                        .filter(|&prereq_id| {
                            graph
                                .nodes
                                .get(prereq_id)
                                .map(|n| n.status != TaskNodeStatus::Completed)
                                .unwrap_or(true)
                        })
                        .collect();

                    if unresolved.is_empty() {
                        // All dependencies resolved, task can be unblocked
                        decisions.push(ProactiveDecision {
                            decision_type: DecisionType::GenerateTask,
                            reasoning: format!(
                                "Task {} can be unblocked - all dependencies completed",
                                task_id
                            ),
                            confidence: 0.95,
                            suggested_actions: vec![SuggestedAction {
                                action_type: "unblock_task".to_string(),
                                description: "Move task to ready queue".to_string(),
                                parameters: HashMap::from([(
                                    "task_id".to_string(),
                                    task_id.clone(),
                                )]),
                                expected_impact: "Enable task execution".to_string(),
                            }],
                            risk_assessment: RiskLevel::Low,
                        });
                    }
                }
            }
        }

        Ok(decisions)
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:1086-1113 method request_search <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:1116-1168 method handle_search_response
  Similarity: 91.25%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:request_search (lines 1086-1113) ---[0m
    async fn request_search(
        &self,
        query: &str,
        context: &str,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Requesting search for: {}", query);

        let search_request = SearchRequest {
            requesting_agent: "master-claude".to_string(),
            query: query.to_string(),
            scope: crate::agent::search_agent::SearchScope::All,
            max_results: Some(10),
            filters: None,
            context: Some(context.to_string()),
        };

        let message = AgentMessage::Coordination {
            from_agent: "master-claude".to_string(),
            to_agent: "search".to_string(),
            message_type: CoordinationType::Custom("search_request".to_string()),
            payload: serde_json::to_value(search_request)?,
        };

        coordination_bus.send_message(message).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:handle_search_response (lines 1116-1168) ---[0m
    pub async fn handle_search_response(
        &self,
        response: SearchResponse,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!(
            "Received search response with {} results",
            response.results.len()
        );

        // Analyze search results and create appropriate tasks or insights
        if !response.results.is_empty() {
            let mut insights = Vec::new();

            for (i, result) in response.results.iter().take(5).enumerate() {
                insights.push(format!(
                    "{}. {} - {} (relevance: {:?})",
                    i + 1,
                    result.title,
                    result.snippet,
                    result.relevance_score
                ));
            }

            // Create a task to review and apply the findings
            let review_task = Task::new(
                format!("review-search-{}", Uuid::new_v4()),
                format!("Review search findings for: {}", response.query_used),
                Priority::Medium,
                TaskType::Research,
            )
            .with_details(format!(
                "Search query: {}\nTop findings:\n{}\n\nPlease review these findings and apply relevant insights to the current work.",
                response.query_used,
                insights.join("\n")
            ))
            .with_duration(1200); // 20 minutes

            // Send task generation message
            coordination_bus
                .send_message(AgentMessage::TaskGenerated {
                    task_id: review_task.id.clone(),
                    description: review_task.description.clone(),
                    reasoning: format!(
                        "Search completed with {} relevant results",
                        response.results.len()
                    ),
                })
                .await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:865-890 method update_context_from_completion <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:1116-1168 method handle_search_response
  Similarity: 91.41%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:update_context_from_completion (lines 865-890) ---[0m
    pub async fn update_context_from_completion(
        &self,
        task: &Task,
        result: &TaskResult,
    ) -> Result<()> {
        if result.success {
            let mut context = self.project_context.write().await;

            // Analyze task to understand what was built
            if task.description.to_lowercase().contains("component") {
                let component_name = self.extract_component_name(&task.description);
                if !context.features.contains(&component_name) {
                    context.features.push(component_name);
                }
            }

            // Update dependency graph
            let mut graph = self.dependency_graph.write().await;
            if let Some(node) = graph.nodes.get_mut(&task.id) {
                node.status = TaskNodeStatus::Completed;
                node.actual_duration = Some(result.duration.as_secs() / 60); // convert to minutes
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:handle_search_response (lines 1116-1168) ---[0m
    pub async fn handle_search_response(
        &self,
        response: SearchResponse,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!(
            "Received search response with {} results",
            response.results.len()
        );

        // Analyze search results and create appropriate tasks or insights
        if !response.results.is_empty() {
            let mut insights = Vec::new();

            for (i, result) in response.results.iter().take(5).enumerate() {
                insights.push(format!(
                    "{}. {} - {} (relevance: {:?})",
                    i + 1,
                    result.title,
                    result.snippet,
                    result.relevance_score
                ));
            }

            // Create a task to review and apply the findings
            let review_task = Task::new(
                format!("review-search-{}", Uuid::new_v4()),
                format!("Review search findings for: {}", response.query_used),
                Priority::Medium,
                TaskType::Research,
            )
            .with_details(format!(
                "Search query: {}\nTop findings:\n{}\n\nPlease review these findings and apply relevant insights to the current work.",
                response.query_used,
                insights.join("\n")
            ))
            .with_duration(1200); // 20 minutes

            // Send task generation message
            coordination_bus
                .send_message(AgentMessage::TaskGenerated {
                    task_id: review_task.id.clone(),
                    description: review_task.description.clone(),
                    reasoning: format!(
                        "Search completed with {} relevant results",
                        response.results.len()
                    ),
                })
                .await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:585-631 method resolve_dependencies <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:1086-1113 method request_search
  Similarity: 92.16%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:resolve_dependencies (lines 585-631) ---[0m
    async fn resolve_dependencies(&self) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let graph = self.dependency_graph.read().await;

        // Find blocked tasks
        for (task_id, node) in &graph.nodes {
            if node.status == TaskNodeStatus::Blocked {
                // Check if blocking dependencies are resolved
                if let Some(prerequisites) = graph.reverse_edges.get(task_id) {
                    let unresolved: Vec<_> = prerequisites
                        .iter()
                        .filter(|&prereq_id| {
                            graph
                                .nodes
                                .get(prereq_id)
                                .map(|n| n.status != TaskNodeStatus::Completed)
                                .unwrap_or(true)
                        })
                        .collect();

                    if unresolved.is_empty() {
                        // All dependencies resolved, task can be unblocked
                        decisions.push(ProactiveDecision {
                            decision_type: DecisionType::GenerateTask,
                            reasoning: format!(
                                "Task {} can be unblocked - all dependencies completed",
                                task_id
                            ),
                            confidence: 0.95,
                            suggested_actions: vec![SuggestedAction {
                                action_type: "unblock_task".to_string(),
                                description: "Move task to ready queue".to_string(),
                                parameters: HashMap::from([(
                                    "task_id".to_string(),
                                    task_id.clone(),
                                )]),
                                expected_impact: "Enable task execution".to_string(),
                            }],
                            risk_assessment: RiskLevel::Low,
                        });
                    }
                }
            }
        }

        Ok(decisions)
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:request_search (lines 1086-1113) ---[0m
    async fn request_search(
        &self,
        query: &str,
        context: &str,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Requesting search for: {}", query);

        let search_request = SearchRequest {
            requesting_agent: "master-claude".to_string(),
            query: query.to_string(),
            scope: crate::agent::search_agent::SearchScope::All,
            max_results: Some(10),
            filters: None,
            context: Some(context.to_string()),
        };

        let message = AgentMessage::Coordination {
            from_agent: "master-claude".to_string(),
            to_agent: "search".to_string(),
            message_type: CoordinationType::Custom("search_request".to_string()),
            payload: serde_json::to_value(search_request)?,
        };

        coordination_bus.send_message(message).await?;

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:793-839 method execute_decision <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:865-890 method update_context_from_completion
  Similarity: 93.83%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:execute_decision (lines 793-839) ---[0m
    async fn execute_decision(
        &self,
        decision: &ProactiveDecision,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Executing proactive decision: {:?}", decision.decision_type);

        for action in &decision.suggested_actions {
            match action.action_type.as_str() {
                "create_task" => {
                    if let Some(template_json) = action.parameters.get("template") {
                        let template: TaskTemplate = serde_json::from_str(template_json)?;
                        let task = self.create_task_from_template(&template).await?;

                        // Send task creation message
                        coordination_bus
                            .send_message(AgentMessage::TaskGenerated {
                                task_id: task.id.clone(),
                                description: task.description.clone(),
                                reasoning: decision.reasoning.clone(),
                            })
                            .await?;

                        info!("Auto-generated task: {}", task.description);
                    }
                }
                "unblock_task" => {
                    if let Some(task_id) = action.parameters.get("task_id") {
                        self.unblock_task(task_id).await?;
                    }
                }
                "request_search" => {
                    if let Some(query) = action.parameters.get("query") {
                        if let Some(context) = action.parameters.get("context") {
                            self.request_search(query, context, coordination_bus)
                                .await?;
                        }
                    }
                }
                _ => {
                    debug!("Skipping execution of action: {}", action.action_type);
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:update_context_from_completion (lines 865-890) ---[0m
    pub async fn update_context_from_completion(
        &self,
        task: &Task,
        result: &TaskResult,
    ) -> Result<()> {
        if result.success {
            let mut context = self.project_context.write().await;

            // Analyze task to understand what was built
            if task.description.to_lowercase().contains("component") {
                let component_name = self.extract_component_name(&task.description);
                if !context.features.contains(&component_name) {
                    context.features.push(component_name);
                }
            }

            // Update dependency graph
            let mut graph = self.dependency_graph.write().await;
            if let Some(node) = graph.nodes.get_mut(&task.id) {
                node.status = TaskNodeStatus::Completed;
                node.actual_duration = Some(result.duration.as_secs() / 60); // convert to minutes
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:257-304 method new <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:1086-1113 method request_search
  Similarity: 87.40%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:new (lines 257-304) ---[0m
    pub async fn new() -> Result<Self> {
        let project_context = Arc::new(RwLock::new(ProjectContext {
            project_type: "web_application".to_string(),
            tech_stack: vec![
                "React".to_string(),
                "Node.js".to_string(),
                "PostgreSQL".to_string(),
            ],
            features: vec![],
            current_phase: DevelopmentPhase::Planning,
            milestones: vec![],
            constraints: vec![],
        }));

        let dependency_graph = Arc::new(RwLock::new(DependencyGraph {
            nodes: HashMap::new(),
            edges: HashMap::new(),
            reverse_edges: HashMap::new(),
        }));

        let progress_analyzer = Arc::new(RwLock::new(ProgressAnalyzer {
            velocity_history: vec![],
            bottlenecks: vec![],
            efficiency_metrics: HashMap::new(),
            prediction_accuracy: 0.8,
        }));

        let task_predictor = Arc::new(RwLock::new(TaskPredictor {
            pattern_library: Self::initialize_pattern_library(),
            completion_patterns: Self::initialize_completion_patterns(),
            feature_templates: Self::initialize_feature_templates(),
        }));

        let goal_tracker = Arc::new(RwLock::new(GoalTracker {
            objectives: vec![],
            key_results: vec![],
            current_sprint: None,
            backlog: vec![],
        }));

        Ok(Self {
            project_context,
            dependency_graph,
            progress_analyzer,
            task_predictor,
            goal_tracker,
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:request_search (lines 1086-1113) ---[0m
    async fn request_search(
        &self,
        query: &str,
        context: &str,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Requesting search for: {}", query);

        let search_request = SearchRequest {
            requesting_agent: "master-claude".to_string(),
            query: query.to_string(),
            scope: crate::agent::search_agent::SearchScope::All,
            max_results: Some(10),
            filters: None,
            context: Some(context.to_string()),
        };

        let message = AgentMessage::Coordination {
            from_agent: "master-claude".to_string(),
            to_agent: "search".to_string(),
            message_type: CoordinationType::Custom("search_request".to_string()),
            payload: serde_json::to_value(search_request)?,
        };

        coordination_bus.send_message(message).await?;

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:585-631 method resolve_dependencies <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:865-890 method update_context_from_completion
  Similarity: 89.11%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:resolve_dependencies (lines 585-631) ---[0m
    async fn resolve_dependencies(&self) -> Result<Vec<ProactiveDecision>> {
        let mut decisions = Vec::new();
        let graph = self.dependency_graph.read().await;

        // Find blocked tasks
        for (task_id, node) in &graph.nodes {
            if node.status == TaskNodeStatus::Blocked {
                // Check if blocking dependencies are resolved
                if let Some(prerequisites) = graph.reverse_edges.get(task_id) {
                    let unresolved: Vec<_> = prerequisites
                        .iter()
                        .filter(|&prereq_id| {
                            graph
                                .nodes
                                .get(prereq_id)
                                .map(|n| n.status != TaskNodeStatus::Completed)
                                .unwrap_or(true)
                        })
                        .collect();

                    if unresolved.is_empty() {
                        // All dependencies resolved, task can be unblocked
                        decisions.push(ProactiveDecision {
                            decision_type: DecisionType::GenerateTask,
                            reasoning: format!(
                                "Task {} can be unblocked - all dependencies completed",
                                task_id
                            ),
                            confidence: 0.95,
                            suggested_actions: vec![SuggestedAction {
                                action_type: "unblock_task".to_string(),
                                description: "Move task to ready queue".to_string(),
                                parameters: HashMap::from([(
                                    "task_id".to_string(),
                                    task_id.clone(),
                                )]),
                                expected_impact: "Enable task execution".to_string(),
                            }],
                            risk_assessment: RiskLevel::Low,
                        });
                    }
                }
            }
        }

        Ok(decisions)
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:update_context_from_completion (lines 865-890) ---[0m
    pub async fn update_context_from_completion(
        &self,
        task: &Task,
        result: &TaskResult,
    ) -> Result<()> {
        if result.success {
            let mut context = self.project_context.write().await;

            // Analyze task to understand what was built
            if task.description.to_lowercase().contains("component") {
                let component_name = self.extract_component_name(&task.description);
                if !context.features.contains(&component_name) {
                    context.features.push(component_name);
                }
            }

            // Update dependency graph
            let mut graph = self.dependency_graph.write().await;
            if let Some(node) = graph.nodes.get_mut(&task.id) {
                node.status = TaskNodeStatus::Completed;
                node.actual_duration = Some(result.duration.as_secs() / 60); // convert to minutes
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:793-839 method execute_decision <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:1086-1113 method request_search
  Similarity: 81.02%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:execute_decision (lines 793-839) ---[0m
    async fn execute_decision(
        &self,
        decision: &ProactiveDecision,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Executing proactive decision: {:?}", decision.decision_type);

        for action in &decision.suggested_actions {
            match action.action_type.as_str() {
                "create_task" => {
                    if let Some(template_json) = action.parameters.get("template") {
                        let template: TaskTemplate = serde_json::from_str(template_json)?;
                        let task = self.create_task_from_template(&template).await?;

                        // Send task creation message
                        coordination_bus
                            .send_message(AgentMessage::TaskGenerated {
                                task_id: task.id.clone(),
                                description: task.description.clone(),
                                reasoning: decision.reasoning.clone(),
                            })
                            .await?;

                        info!("Auto-generated task: {}", task.description);
                    }
                }
                "unblock_task" => {
                    if let Some(task_id) = action.parameters.get("task_id") {
                        self.unblock_task(task_id).await?;
                    }
                }
                "request_search" => {
                    if let Some(query) = action.parameters.get("query") {
                        if let Some(context) = action.parameters.get("context") {
                            self.request_search(query, context, coordination_bus)
                                .await?;
                        }
                    }
                }
                _ => {
                    debug!("Skipping execution of action: {}", action.action_type);
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:request_search (lines 1086-1113) ---[0m
    async fn request_search(
        &self,
        query: &str,
        context: &str,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Requesting search for: {}", query);

        let search_request = SearchRequest {
            requesting_agent: "master-claude".to_string(),
            query: query.to_string(),
            scope: crate::agent::search_agent::SearchScope::All,
            max_results: Some(10),
            filters: None,
            context: Some(context.to_string()),
        };

        let message = AgentMessage::Coordination {
            from_agent: "master-claude".to_string(),
            to_agent: "search".to_string(),
            message_type: CoordinationType::Custom("search_request".to_string()),
            payload: serde_json::to_value(search_request)?,
        };

        coordination_bus.send_message(message).await?;

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:385-412 method initialize_completion_patterns <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:865-890 method update_context_from_completion
  Similarity: 89.81%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:initialize_completion_patterns (lines 385-412) ---[0m
    fn initialize_completion_patterns() -> Vec<CompletionPattern> {
        vec![
            CompletionPattern {
                completed_task_type: TaskType::Development,
                follow_up_tasks: vec![TaskTemplate {
                    description_template: "Test the implemented functionality".to_string(),
                    task_type: TaskType::Testing,
                    priority: Priority::High,
                    estimated_duration: 30,
                    required_agent_type: "QA".to_string(),
                    variables: HashMap::new(),
                }],
                probability: 0.85,
            },
            CompletionPattern {
                completed_task_type: TaskType::Testing,
                follow_up_tasks: vec![TaskTemplate {
                    description_template: "Update documentation with test results".to_string(),
                    task_type: TaskType::Documentation,
                    priority: Priority::Low,
                    estimated_duration: 15,
                    required_agent_type: "QA".to_string(),
                    variables: HashMap::new(),
                }],
                probability: 0.6,
            },
        ]
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:update_context_from_completion (lines 865-890) ---[0m
    pub async fn update_context_from_completion(
        &self,
        task: &Task,
        result: &TaskResult,
    ) -> Result<()> {
        if result.success {
            let mut context = self.project_context.write().await;

            // Analyze task to understand what was built
            if task.description.to_lowercase().contains("component") {
                let component_name = self.extract_component_name(&task.description);
                if !context.features.contains(&component_name) {
                    context.features.push(component_name);
                }
            }

            // Update dependency graph
            let mut graph = self.dependency_graph.write().await;
            if let Some(node) = graph.nodes.get_mut(&task.id) {
                node.status = TaskNodeStatus::Completed;
                node.actual_duration = Some(result.duration.as_secs() / 60); // convert to minutes
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:893-904 method extract_component_name <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:1086-1113 method request_search
  Similarity: 80.92%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:extract_component_name (lines 893-904) ---[0m
    fn extract_component_name(&self, description: &str) -> String {
        // Simple heuristic to extract component names
        let words: Vec<&str> = description.split_whitespace().collect();

        for (i, word) in words.iter().enumerate() {
            if word.to_lowercase() == "component" && i > 0 {
                return words[i - 1].to_string();
            }
        }

        "Unknown Component".to_string()
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:request_search (lines 1086-1113) ---[0m
    async fn request_search(
        &self,
        query: &str,
        context: &str,
        coordination_bus: &CoordinationBus,
    ) -> Result<()> {
        info!("Requesting search for: {}", query);

        let search_request = SearchRequest {
            requesting_agent: "master-claude".to_string(),
            query: query.to_string(),
            scope: crate::agent::search_agent::SearchScope::All,
            max_results: Some(10),
            filters: None,
            context: Some(context.to_string()),
        };

        let message = AgentMessage::Coordination {
            from_agent: "master-claude".to_string(),
            to_agent: "search".to_string(),
            message_type: CoordinationType::Custom("search_request".to_string()),
            payload: serde_json::to_value(search_request)?,
        };

        coordination_bus.send_message(message).await?;

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:865-890 method update_context_from_completion <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:893-904 method extract_component_name
  Similarity: 81.51%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:update_context_from_completion (lines 865-890) ---[0m
    pub async fn update_context_from_completion(
        &self,
        task: &Task,
        result: &TaskResult,
    ) -> Result<()> {
        if result.success {
            let mut context = self.project_context.write().await;

            // Analyze task to understand what was built
            if task.description.to_lowercase().contains("component") {
                let component_name = self.extract_component_name(&task.description);
                if !context.features.contains(&component_name) {
                    context.features.push(component_name);
                }
            }

            // Update dependency graph
            let mut graph = self.dependency_graph.write().await;
            if let Some(node) = graph.nodes.get_mut(&task.id) {
                node.status = TaskNodeStatus::Completed;
                node.actual_duration = Some(result.duration.as_secs() / 60); // convert to minutes
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:extract_component_name (lines 893-904) ---[0m
    fn extract_component_name(&self, description: &str) -> String {
        // Simple heuristic to extract component names
        let words: Vec<&str> = description.split_whitespace().collect();

        for (i, word) in words.iter().enumerate() {
            if word.to_lowercase() == "component" && i > 0 {
                return words[i - 1].to_string();
            }
        }

        "Unknown Component".to_string()
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:853-862 method unblock_task <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:893-904 method extract_component_name
  Similarity: 76.21%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:unblock_task (lines 853-862) ---[0m
    async fn unblock_task(&self, task_id: &str) -> Result<()> {
        let mut graph = self.dependency_graph.write().await;

        if let Some(node) = graph.nodes.get_mut(task_id) {
            node.status = TaskNodeStatus::NotStarted;
            info!("Unblocked task: {}", task_id);
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:extract_component_name (lines 893-904) ---[0m
    fn extract_component_name(&self, description: &str) -> String {
        // Simple heuristic to extract component names
        let words: Vec<&str> = description.split_whitespace().collect();

        for (i, word) in words.iter().enumerate() {
            if word.to_lowercase() == "component" && i > 0 {
                return words[i - 1].to_string();
            }
        }

        "Unknown Component".to_string()
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:842-850 method create_task_from_template <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:893-904 method extract_component_name
  Similarity: 74.57%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:create_task_from_template (lines 842-850) ---[0m
    async fn create_task_from_template(&self, template: &TaskTemplate) -> Result<Task> {
        let task_id = format!("auto-{}", Uuid::new_v4());
        let description = template.description_template.clone();

        Ok(
            Task::new(task_id, description, template.priority, template.task_type)
                .with_duration((template.estimated_duration * 60) as u32),
        ) // convert minutes to seconds
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:extract_component_name (lines 893-904) ---[0m
    fn extract_component_name(&self, description: &str) -> String {
        // Simple heuristic to extract component names
        let words: Vec<&str> = description.split_whitespace().collect();

        for (i, word) in words.iter().enumerate() {
            if word.to_lowercase() == "component" && i > 0 {
                return words[i - 1].to_string();
            }
        }

        "Unknown Component".to_string()
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:842-850 method create_task_from_template <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:853-862 method unblock_task
  Similarity: 71.96%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:create_task_from_template (lines 842-850) ---[0m
    async fn create_task_from_template(&self, template: &TaskTemplate) -> Result<Task> {
        let task_id = format!("auto-{}", Uuid::new_v4());
        let description = template.description_template.clone();

        Ok(
            Task::new(task_id, description, template.priority, template.task_type)
                .with_duration((template.estimated_duration * 60) as u32),
        ) // convert minutes to seconds
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:unblock_task (lines 853-862) ---[0m
    async fn unblock_task(&self, task_id: &str) -> Result<()> {
        let mut graph = self.dependency_graph.write().await;

        if let Some(node) = graph.nodes.get_mut(task_id) {
            node.status = TaskNodeStatus::NotStarted;
            info!("Unblocked task: {}", task_id);
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:853-862 method unblock_task <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:907-911 method add_milestone
  Similarity: 86.67%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:unblock_task (lines 853-862) ---[0m
    async fn unblock_task(&self, task_id: &str) -> Result<()> {
        let mut graph = self.dependency_graph.write().await;

        if let Some(node) = graph.nodes.get_mut(task_id) {
            node.status = TaskNodeStatus::NotStarted;
            info!("Unblocked task: {}", task_id);
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:add_milestone (lines 907-911) ---[0m
    pub async fn add_milestone(&self, milestone: Milestone) -> Result<()> {
        let mut context = self.project_context.write().await;
        context.milestones.push(milestone);
        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:853-862 method unblock_task <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:914-918 method set_objective
  Similarity: 86.67%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:unblock_task (lines 853-862) ---[0m
    async fn unblock_task(&self, task_id: &str) -> Result<()> {
        let mut graph = self.dependency_graph.write().await;

        if let Some(node) = graph.nodes.get_mut(task_id) {
            node.status = TaskNodeStatus::NotStarted;
            info!("Unblocked task: {}", task_id);
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:set_objective (lines 914-918) ---[0m
    pub async fn set_objective(&self, objective: Objective) -> Result<()> {
        let mut goals = self.goal_tracker.write().await;
        goals.objectives.push(objective);
        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/proactive_master.rs:907-911 method add_milestone <-> ./crates/ccswarm/src/orchestrator/proactive_master.rs:914-918 method set_objective
  Similarity: 96.51%
  Classes: ProactiveMaster <-> ProactiveMaster

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:add_milestone (lines 907-911) ---[0m
    pub async fn add_milestone(&self, milestone: Milestone) -> Result<()> {
        let mut context = self.project_context.write().await;
        context.milestones.push(milestone);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/proactive_master.rs:set_objective (lines 914-918) ---[0m
    pub async fn set_objective(&self, objective: Objective) -> Result<()> {
        let mut goals = self.goal_tracker.write().await;
        goals.objectives.push(objective);
        Ok(())
    }


Duplicates in ./crates/ccswarm/src/session/coordinator.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/session/coordinator.rs:280-355 method execute_coordinated_task <-> ./crates/ccswarm/src/session/coordinator.rs:358-420 method execute_coordinated_batch
  Similarity: 91.12%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:execute_coordinated_task (lines 280-355) ---[0m
    pub async fn execute_coordinated_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let task_id = task.id.clone();
        let start_time = std::time::Instant::now();

        // Check for batch optimization opportunities
        if self.config.batch_optimization.enabled {
            if let Some(batch_result) = self.try_batch_optimization(&role, &task).await? {
                return Ok(batch_result);
            }
        }

        // Track task execution
        let execution = TaskExecution {
            task: task.clone(),
            assigned_session: "pending".to_string(),
            started_at: Utc::now(),
            estimated_completion: None,
            status: TaskExecutionStatus::Queued,
        };

        {
            let mut active_tasks = self.active_tasks.write().await;
            active_tasks.insert(task_id.clone(), execution);
        }

        // Execute through session pool
        let result = {
            let session_pool = self.session_pool.lock().await;
            session_pool
                .execute_task(role.clone(), task, claude_config)
                .await?
        };

        // Update performance tracking
        {
            let mut tracker = self.performance_tracker.lock().await;
            tracker.total_tasks += 1;
            tracker.total_execution_time += start_time.elapsed();

            if !result.success {
                tracker.error_count += 1;
            }

            // Estimate token savings (simplified calculation)
            let estimated_savings = self.calculate_token_savings(&result).await;
            tracker.total_tokens_saved += estimated_savings;
        }

        // Update task status
        {
            let mut active_tasks = self.active_tasks.write().await;
            if let Some(mut execution) = active_tasks.remove(&task_id) {
                execution.status = if result.success {
                    TaskExecutionStatus::Completed
                } else {
                    TaskExecutionStatus::Failed(
                        result
                            .error
                            .as_deref()
                            .unwrap_or("Unknown error")
                            .to_string(),
                    )
                };
            }
        }

        // Send coordination update
        self.send_coordination_update(&role, &result).await?;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:execute_coordinated_batch (lines 358-420) ---[0m
    pub async fn execute_coordinated_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing coordinated batch of {} tasks for role: {}",
            tasks.len(),
            role.name()
        );
        let start_time = std::time::Instant::now();

        // Track all tasks
        for task in &tasks {
            let execution = TaskExecution {
                task: task.clone(),
                assigned_session: "batch".to_string(),
                started_at: Utc::now(),
                estimated_completion: None,
                status: TaskExecutionStatus::Executing,
            };

            let mut active_tasks = self.active_tasks.write().await;
            active_tasks.insert(task.id.clone(), execution);
        }

        // Execute batch through session pool
        let results = {
            let session_pool = self.session_pool.lock().await;
            session_pool
                .execute_task_batch(role.clone(), tasks.clone(), claude_config)
                .await?
        };

        // Calculate massive token savings from batch execution
        let estimated_savings = (tasks.len().saturating_sub(1)) * 3400; // ~3400 tokens saved per additional task

        // Update performance tracking
        {
            let mut tracker = self.performance_tracker.lock().await;
            tracker.total_tasks += results.len();
            tracker.total_execution_time += start_time.elapsed();
            tracker.total_tokens_saved += estimated_savings;

            let error_count = results.iter().filter(|r| !r.success).count();
            tracker.error_count += error_count;
        }

        // Send efficiency report
        self.send_efficiency_report(estimated_savings, results.len())
            .await?;

        tracing::info!(
            "Batch execution completed - estimated {} tokens saved",
            estimated_savings
        );
        Ok(results)
    }

  ./crates/ccswarm/src/session/coordinator.rs:231-261 method new <-> ./crates/ccswarm/src/session/coordinator.rs:358-420 method execute_coordinated_batch
  Similarity: 85.09%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:new (lines 231-261) ---[0m
    pub async fn new(
        worktree_config: WorktreeSessionConfig,
        pool_config: SessionPoolConfig,
        coordinator_config: SessionCoordinatorConfig,
    ) -> Result<Self> {
        // Create session pool
        let mut session_pool = SessionPool::new(worktree_config, pool_config).await?;
        session_pool.start().await?;

        // Initialize coordination bus
        let coordination_bus =
            CoordinationBus::new(coordinator_config.coordination_dir.clone()).await?;

        // Initialize performance tracker
        let performance_tracker = PerformanceTracker {
            start_time: Utc::now(),
            total_tasks: 0,
            total_sessions_created: 0,
            total_tokens_saved: 0,
            total_execution_time: std::time::Duration::ZERO,
            error_count: 0,
        };

        Ok(Self {
            session_pool: Arc::new(Mutex::new(session_pool)),
            coordination_bus: Arc::new(RwLock::new(coordination_bus)),
            active_tasks: Arc::new(RwLock::new(HashMap::new())),
            performance_tracker: Arc::new(Mutex::new(performance_tracker)),
            config: coordinator_config,
        })
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:execute_coordinated_batch (lines 358-420) ---[0m
    pub async fn execute_coordinated_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing coordinated batch of {} tasks for role: {}",
            tasks.len(),
            role.name()
        );
        let start_time = std::time::Instant::now();

        // Track all tasks
        for task in &tasks {
            let execution = TaskExecution {
                task: task.clone(),
                assigned_session: "batch".to_string(),
                started_at: Utc::now(),
                estimated_completion: None,
                status: TaskExecutionStatus::Executing,
            };

            let mut active_tasks = self.active_tasks.write().await;
            active_tasks.insert(task.id.clone(), execution);
        }

        // Execute batch through session pool
        let results = {
            let session_pool = self.session_pool.lock().await;
            session_pool
                .execute_task_batch(role.clone(), tasks.clone(), claude_config)
                .await?
        };

        // Calculate massive token savings from batch execution
        let estimated_savings = (tasks.len().saturating_sub(1)) * 3400; // ~3400 tokens saved per additional task

        // Update performance tracking
        {
            let mut tracker = self.performance_tracker.lock().await;
            tracker.total_tasks += results.len();
            tracker.total_execution_time += start_time.elapsed();
            tracker.total_tokens_saved += estimated_savings;

            let error_count = results.iter().filter(|r| !r.success).count();
            tracker.error_count += error_count;
        }

        // Send efficiency report
        self.send_efficiency_report(estimated_savings, results.len())
            .await?;

        tracing::info!(
            "Batch execution completed - estimated {} tokens saved",
            estimated_savings
        );
        Ok(results)
    }

  ./crates/ccswarm/src/session/coordinator.rs:471-521 method send_efficiency_report <-> ./crates/ccswarm/src/session/coordinator.rs:554-586 method start_efficiency_monitoring
  Similarity: 94.83%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:send_efficiency_report (lines 471-521) ---[0m
    async fn send_efficiency_report(
        &self,
        _tokens_saved: usize,
        _tasks_processed: usize,
    ) -> Result<()> {
        let tracker = self.performance_tracker.lock().await;

        let report = SessionCoordinationMessage::EfficiencyReport {
            timestamp: Utc::now(),
            token_savings: TokenSavingsReport {
                total_tasks_processed: tracker.total_tasks,
                sessions_created: tracker.total_sessions_created,
                estimated_traditional_tokens: tracker.total_tasks * 3600, // Traditional approach
                actual_tokens_used: (tracker.total_tasks * 3600)
                    .saturating_sub(tracker.total_tokens_saved),
                savings_percentage: if tracker.total_tasks > 0 {
                    (tracker.total_tokens_saved as f64 / (tracker.total_tasks * 3600) as f64)
                        * 100.0
                } else {
                    0.0
                },
                efficiency_score: if tracker.total_tasks > 0 {
                    tracker.total_tokens_saved as f64 / tracker.total_tasks as f64
                } else {
                    0.0
                },
            },
            performance_metrics: PerformanceReport {
                total_execution_time: tracker.total_execution_time,
                average_task_time: if tracker.total_tasks > 0 {
                    tracker.total_execution_time / tracker.total_tasks as u32
                } else {
                    std::time::Duration::ZERO
                },
                throughput_tasks_per_minute: if tracker.total_execution_time.as_secs() > 0 {
                    (tracker.total_tasks as f64 * 60.0) / tracker.total_execution_time.as_secs_f64()
                } else {
                    0.0
                },
                error_rate_percentage: if tracker.total_tasks > 0 {
                    (tracker.error_count as f64 / tracker.total_tasks as f64) * 100.0
                } else {
                    0.0
                },
                session_utilization: 0.75, // Would calculate from pool statistics
            },
        };

        self.send_coordination_message(report).await?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:start_efficiency_monitoring (lines 554-586) ---[0m
    async fn start_efficiency_monitoring(&self) {
        let performance_tracker = Arc::clone(&self.performance_tracker);
        let config = self.config.clone();

        tokio::spawn(async move {
            let mut interval = tokio::time::interval(config.reporting_interval);

            loop {
                interval.tick().await;

                let tracker = performance_tracker.lock().await;

                if tracker.total_tasks > 0 {
                    let efficiency_percentage = (tracker.total_tokens_saved as f64
                        / (tracker.total_tasks * 3600) as f64)
                        * 100.0;
                    let efficiency_score = if tracker.total_tasks > 0 {
                        tracker.total_tokens_saved as f64 / tracker.total_tasks as f64
                    } else {
                        0.0
                    };

                    tracing::info!(
                        "Session Efficiency Report - Tasks: {}, Context Optimized: {}, Efficiency: {:.1}%, Score: {:.2}",
                        tracker.total_tasks,
                        tracker.total_tokens_saved,
                        efficiency_percentage,
                        efficiency_score
                    );
                }
            }
        });
    }

  ./crates/ccswarm/src/session/coordinator.rs:358-420 method execute_coordinated_batch <-> ./crates/ccswarm/src/session/coordinator.rs:711-739 function test_token_savings_calculation
  Similarity: 85.66%

[36m--- ./crates/ccswarm/src/session/coordinator.rs:execute_coordinated_batch (lines 358-420) ---[0m
    pub async fn execute_coordinated_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing coordinated batch of {} tasks for role: {}",
            tasks.len(),
            role.name()
        );
        let start_time = std::time::Instant::now();

        // Track all tasks
        for task in &tasks {
            let execution = TaskExecution {
                task: task.clone(),
                assigned_session: "batch".to_string(),
                started_at: Utc::now(),
                estimated_completion: None,
                status: TaskExecutionStatus::Executing,
            };

            let mut active_tasks = self.active_tasks.write().await;
            active_tasks.insert(task.id.clone(), execution);
        }

        // Execute batch through session pool
        let results = {
            let session_pool = self.session_pool.lock().await;
            session_pool
                .execute_task_batch(role.clone(), tasks.clone(), claude_config)
                .await?
        };

        // Calculate massive token savings from batch execution
        let estimated_savings = (tasks.len().saturating_sub(1)) * 3400; // ~3400 tokens saved per additional task

        // Update performance tracking
        {
            let mut tracker = self.performance_tracker.lock().await;
            tracker.total_tasks += results.len();
            tracker.total_execution_time += start_time.elapsed();
            tracker.total_tokens_saved += estimated_savings;

            let error_count = results.iter().filter(|r| !r.success).count();
            tracker.error_count += error_count;
        }

        // Send efficiency report
        self.send_efficiency_report(estimated_savings, results.len())
            .await?;

        tracing::info!(
            "Batch execution completed - estimated {} tokens saved",
            estimated_savings
        );
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:test_token_savings_calculation (lines 711-739) ---[0m
    async fn test_token_savings_calculation() {
        let temp_dir = TempDir::new().unwrap();

        let mut worktree_config = WorktreeSessionConfig::default();
        worktree_config.repo_path = temp_dir.path().to_path_buf();

        let pool_config = SessionPoolConfig::default();

        let mut coordinator_config = SessionCoordinatorConfig::default();
        coordinator_config.coordination_dir = temp_dir.path().join("coordination");

        let coordinator = SessionCoordinator::new(worktree_config, pool_config, coordinator_config)
            .await
            .unwrap();

        // Test token savings calculation
        let result = TaskResult {
            success: true,
            output: serde_json::json!({
                "session_id": "test-session",
                "response": "task completed"
            }),
            error: None,
            duration: std::time::Duration::from_secs(1),
        };

        let savings = coordinator.calculate_token_savings(&result).await;
        assert_eq!(savings, 3400); // Expected savings for session reuse
    }

  ./crates/ccswarm/src/session/coordinator.rs:231-261 method new <-> ./crates/ccswarm/src/session/coordinator.rs:471-521 method send_efficiency_report
  Similarity: 90.86%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:new (lines 231-261) ---[0m
    pub async fn new(
        worktree_config: WorktreeSessionConfig,
        pool_config: SessionPoolConfig,
        coordinator_config: SessionCoordinatorConfig,
    ) -> Result<Self> {
        // Create session pool
        let mut session_pool = SessionPool::new(worktree_config, pool_config).await?;
        session_pool.start().await?;

        // Initialize coordination bus
        let coordination_bus =
            CoordinationBus::new(coordinator_config.coordination_dir.clone()).await?;

        // Initialize performance tracker
        let performance_tracker = PerformanceTracker {
            start_time: Utc::now(),
            total_tasks: 0,
            total_sessions_created: 0,
            total_tokens_saved: 0,
            total_execution_time: std::time::Duration::ZERO,
            error_count: 0,
        };

        Ok(Self {
            session_pool: Arc::new(Mutex::new(session_pool)),
            coordination_bus: Arc::new(RwLock::new(coordination_bus)),
            active_tasks: Arc::new(RwLock::new(HashMap::new())),
            performance_tracker: Arc::new(Mutex::new(performance_tracker)),
            config: coordinator_config,
        })
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:send_efficiency_report (lines 471-521) ---[0m
    async fn send_efficiency_report(
        &self,
        _tokens_saved: usize,
        _tasks_processed: usize,
    ) -> Result<()> {
        let tracker = self.performance_tracker.lock().await;

        let report = SessionCoordinationMessage::EfficiencyReport {
            timestamp: Utc::now(),
            token_savings: TokenSavingsReport {
                total_tasks_processed: tracker.total_tasks,
                sessions_created: tracker.total_sessions_created,
                estimated_traditional_tokens: tracker.total_tasks * 3600, // Traditional approach
                actual_tokens_used: (tracker.total_tasks * 3600)
                    .saturating_sub(tracker.total_tokens_saved),
                savings_percentage: if tracker.total_tasks > 0 {
                    (tracker.total_tokens_saved as f64 / (tracker.total_tasks * 3600) as f64)
                        * 100.0
                } else {
                    0.0
                },
                efficiency_score: if tracker.total_tasks > 0 {
                    tracker.total_tokens_saved as f64 / tracker.total_tasks as f64
                } else {
                    0.0
                },
            },
            performance_metrics: PerformanceReport {
                total_execution_time: tracker.total_execution_time,
                average_task_time: if tracker.total_tasks > 0 {
                    tracker.total_execution_time / tracker.total_tasks as u32
                } else {
                    std::time::Duration::ZERO
                },
                throughput_tasks_per_minute: if tracker.total_execution_time.as_secs() > 0 {
                    (tracker.total_tasks as f64 * 60.0) / tracker.total_execution_time.as_secs_f64()
                } else {
                    0.0
                },
                error_rate_percentage: if tracker.total_tasks > 0 {
                    (tracker.error_count as f64 / tracker.total_tasks as f64) * 100.0
                } else {
                    0.0
                },
                session_utilization: 0.75, // Would calculate from pool statistics
            },
        };

        self.send_coordination_message(report).await?;
        Ok(())
    }

  ./crates/ccswarm/src/session/coordinator.rs:471-521 method send_efficiency_report <-> ./crates/ccswarm/src/session/coordinator.rs:524-551 method send_coordination_message
  Similarity: 92.56%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:send_efficiency_report (lines 471-521) ---[0m
    async fn send_efficiency_report(
        &self,
        _tokens_saved: usize,
        _tasks_processed: usize,
    ) -> Result<()> {
        let tracker = self.performance_tracker.lock().await;

        let report = SessionCoordinationMessage::EfficiencyReport {
            timestamp: Utc::now(),
            token_savings: TokenSavingsReport {
                total_tasks_processed: tracker.total_tasks,
                sessions_created: tracker.total_sessions_created,
                estimated_traditional_tokens: tracker.total_tasks * 3600, // Traditional approach
                actual_tokens_used: (tracker.total_tasks * 3600)
                    .saturating_sub(tracker.total_tokens_saved),
                savings_percentage: if tracker.total_tasks > 0 {
                    (tracker.total_tokens_saved as f64 / (tracker.total_tasks * 3600) as f64)
                        * 100.0
                } else {
                    0.0
                },
                efficiency_score: if tracker.total_tasks > 0 {
                    tracker.total_tokens_saved as f64 / tracker.total_tasks as f64
                } else {
                    0.0
                },
            },
            performance_metrics: PerformanceReport {
                total_execution_time: tracker.total_execution_time,
                average_task_time: if tracker.total_tasks > 0 {
                    tracker.total_execution_time / tracker.total_tasks as u32
                } else {
                    std::time::Duration::ZERO
                },
                throughput_tasks_per_minute: if tracker.total_execution_time.as_secs() > 0 {
                    (tracker.total_tasks as f64 * 60.0) / tracker.total_execution_time.as_secs_f64()
                } else {
                    0.0
                },
                error_rate_percentage: if tracker.total_tasks > 0 {
                    (tracker.error_count as f64 / tracker.total_tasks as f64) * 100.0
                } else {
                    0.0
                },
                session_utilization: 0.75, // Would calculate from pool statistics
            },
        };

        self.send_coordination_message(report).await?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:send_coordination_message (lines 524-551) ---[0m
    async fn send_coordination_message(&self, message: SessionCoordinationMessage) -> Result<()> {
        let coordination_message = AgentMessage::Coordination {
            from_agent: "session-coordinator".to_string(),
            to_agent: "system".to_string(),
            message_type: CoordinationType::StatusUpdate, // Assuming this exists
            payload: serde_json::to_value(message)?,
        };

        // Write to coordination directory
        let message_file = self
            .config
            .coordination_dir
            .join("messages")
            .join(format!("session-{}.json", Uuid::new_v4()));

        if let Some(parent) = message_file.parent() {
            tokio::fs::create_dir_all(parent).await?;
        }

        tokio::fs::write(
            &message_file,
            serde_json::to_string_pretty(&coordination_message)?,
        )
        .await
        .context("Failed to write coordination message")?;

        Ok(())
    }

  ./crates/ccswarm/src/session/coordinator.rs:231-261 method new <-> ./crates/ccswarm/src/session/coordinator.rs:524-551 method send_coordination_message
  Similarity: 90.60%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:new (lines 231-261) ---[0m
    pub async fn new(
        worktree_config: WorktreeSessionConfig,
        pool_config: SessionPoolConfig,
        coordinator_config: SessionCoordinatorConfig,
    ) -> Result<Self> {
        // Create session pool
        let mut session_pool = SessionPool::new(worktree_config, pool_config).await?;
        session_pool.start().await?;

        // Initialize coordination bus
        let coordination_bus =
            CoordinationBus::new(coordinator_config.coordination_dir.clone()).await?;

        // Initialize performance tracker
        let performance_tracker = PerformanceTracker {
            start_time: Utc::now(),
            total_tasks: 0,
            total_sessions_created: 0,
            total_tokens_saved: 0,
            total_execution_time: std::time::Duration::ZERO,
            error_count: 0,
        };

        Ok(Self {
            session_pool: Arc::new(Mutex::new(session_pool)),
            coordination_bus: Arc::new(RwLock::new(coordination_bus)),
            active_tasks: Arc::new(RwLock::new(HashMap::new())),
            performance_tracker: Arc::new(Mutex::new(performance_tracker)),
            config: coordinator_config,
        })
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:send_coordination_message (lines 524-551) ---[0m
    async fn send_coordination_message(&self, message: SessionCoordinationMessage) -> Result<()> {
        let coordination_message = AgentMessage::Coordination {
            from_agent: "session-coordinator".to_string(),
            to_agent: "system".to_string(),
            message_type: CoordinationType::StatusUpdate, // Assuming this exists
            payload: serde_json::to_value(message)?,
        };

        // Write to coordination directory
        let message_file = self
            .config
            .coordination_dir
            .join("messages")
            .join(format!("session-{}.json", Uuid::new_v4()));

        if let Some(parent) = message_file.parent() {
            tokio::fs::create_dir_all(parent).await?;
        }

        tokio::fs::write(
            &message_file,
            serde_json::to_string_pretty(&coordination_message)?,
        )
        .await
        .context("Failed to write coordination message")?;

        Ok(())
    }

  ./crates/ccswarm/src/session/coordinator.rs:554-586 method start_efficiency_monitoring <-> ./crates/ccswarm/src/session/coordinator.rs:620-647 method get_coordination_statistics
  Similarity: 86.41%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:start_efficiency_monitoring (lines 554-586) ---[0m
    async fn start_efficiency_monitoring(&self) {
        let performance_tracker = Arc::clone(&self.performance_tracker);
        let config = self.config.clone();

        tokio::spawn(async move {
            let mut interval = tokio::time::interval(config.reporting_interval);

            loop {
                interval.tick().await;

                let tracker = performance_tracker.lock().await;

                if tracker.total_tasks > 0 {
                    let efficiency_percentage = (tracker.total_tokens_saved as f64
                        / (tracker.total_tasks * 3600) as f64)
                        * 100.0;
                    let efficiency_score = if tracker.total_tasks > 0 {
                        tracker.total_tokens_saved as f64 / tracker.total_tasks as f64
                    } else {
                        0.0
                    };

                    tracing::info!(
                        "Session Efficiency Report - Tasks: {}, Context Optimized: {}, Efficiency: {:.1}%, Score: {:.2}",
                        tracker.total_tasks,
                        tracker.total_tokens_saved,
                        efficiency_percentage,
                        efficiency_score
                    );
                }
            }
        });
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:get_coordination_statistics (lines 620-647) ---[0m
    pub async fn get_coordination_statistics(&self) -> CoordinationStatistics {
        let pool_stats = {
            let session_pool = self.session_pool.lock().await;
            session_pool.get_pool_statistics().await
        };

        let performance_tracker = self.performance_tracker.lock().await;
        let active_tasks = self.active_tasks.read().await;

        CoordinationStatistics {
            pool_statistics: pool_stats,
            efficiency_metrics: EfficiencyMetrics {
                sessions_reused: performance_tracker
                    .total_tasks
                    .saturating_sub(performance_tracker.total_sessions_created),
                tokens_saved: performance_tracker.total_tokens_saved,
                context_continuity_score: 0.95, // Would calculate from session data
                average_response_time: if performance_tracker.total_tasks > 0 {
                    performance_tracker.total_execution_time.as_secs_f64()
                        / performance_tracker.total_tasks as f64
                } else {
                    0.0
                },
            },
            active_task_count: active_tasks.len(),
            total_coordination_messages: 0, // Would track from coordination bus
        }
    }

  ./crates/ccswarm/src/session/coordinator.rs:231-261 method new <-> ./crates/ccswarm/src/session/coordinator.rs:554-586 method start_efficiency_monitoring
  Similarity: 77.40%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:new (lines 231-261) ---[0m
    pub async fn new(
        worktree_config: WorktreeSessionConfig,
        pool_config: SessionPoolConfig,
        coordinator_config: SessionCoordinatorConfig,
    ) -> Result<Self> {
        // Create session pool
        let mut session_pool = SessionPool::new(worktree_config, pool_config).await?;
        session_pool.start().await?;

        // Initialize coordination bus
        let coordination_bus =
            CoordinationBus::new(coordinator_config.coordination_dir.clone()).await?;

        // Initialize performance tracker
        let performance_tracker = PerformanceTracker {
            start_time: Utc::now(),
            total_tasks: 0,
            total_sessions_created: 0,
            total_tokens_saved: 0,
            total_execution_time: std::time::Duration::ZERO,
            error_count: 0,
        };

        Ok(Self {
            session_pool: Arc::new(Mutex::new(session_pool)),
            coordination_bus: Arc::new(RwLock::new(coordination_bus)),
            active_tasks: Arc::new(RwLock::new(HashMap::new())),
            performance_tracker: Arc::new(Mutex::new(performance_tracker)),
            config: coordinator_config,
        })
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:start_efficiency_monitoring (lines 554-586) ---[0m
    async fn start_efficiency_monitoring(&self) {
        let performance_tracker = Arc::clone(&self.performance_tracker);
        let config = self.config.clone();

        tokio::spawn(async move {
            let mut interval = tokio::time::interval(config.reporting_interval);

            loop {
                interval.tick().await;

                let tracker = performance_tracker.lock().await;

                if tracker.total_tasks > 0 {
                    let efficiency_percentage = (tracker.total_tokens_saved as f64
                        / (tracker.total_tasks * 3600) as f64)
                        * 100.0;
                    let efficiency_score = if tracker.total_tasks > 0 {
                        tracker.total_tokens_saved as f64 / tracker.total_tasks as f64
                    } else {
                        0.0
                    };

                    tracing::info!(
                        "Session Efficiency Report - Tasks: {}, Context Optimized: {}, Efficiency: {:.1}%, Score: {:.2}",
                        tracker.total_tasks,
                        tracker.total_tokens_saved,
                        efficiency_percentage,
                        efficiency_score
                    );
                }
            }
        });
    }

  ./crates/ccswarm/src/session/coordinator.rs:524-551 method send_coordination_message <-> ./crates/ccswarm/src/session/coordinator.rs:620-647 method get_coordination_statistics
  Similarity: 86.56%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:send_coordination_message (lines 524-551) ---[0m
    async fn send_coordination_message(&self, message: SessionCoordinationMessage) -> Result<()> {
        let coordination_message = AgentMessage::Coordination {
            from_agent: "session-coordinator".to_string(),
            to_agent: "system".to_string(),
            message_type: CoordinationType::StatusUpdate, // Assuming this exists
            payload: serde_json::to_value(message)?,
        };

        // Write to coordination directory
        let message_file = self
            .config
            .coordination_dir
            .join("messages")
            .join(format!("session-{}.json", Uuid::new_v4()));

        if let Some(parent) = message_file.parent() {
            tokio::fs::create_dir_all(parent).await?;
        }

        tokio::fs::write(
            &message_file,
            serde_json::to_string_pretty(&coordination_message)?,
        )
        .await
        .context("Failed to write coordination message")?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:get_coordination_statistics (lines 620-647) ---[0m
    pub async fn get_coordination_statistics(&self) -> CoordinationStatistics {
        let pool_stats = {
            let session_pool = self.session_pool.lock().await;
            session_pool.get_pool_statistics().await
        };

        let performance_tracker = self.performance_tracker.lock().await;
        let active_tasks = self.active_tasks.read().await;

        CoordinationStatistics {
            pool_statistics: pool_stats,
            efficiency_metrics: EfficiencyMetrics {
                sessions_reused: performance_tracker
                    .total_tasks
                    .saturating_sub(performance_tracker.total_sessions_created),
                tokens_saved: performance_tracker.total_tokens_saved,
                context_continuity_score: 0.95, // Would calculate from session data
                average_response_time: if performance_tracker.total_tasks > 0 {
                    performance_tracker.total_execution_time.as_secs_f64()
                        / performance_tracker.total_tasks as f64
                } else {
                    0.0
                },
            },
            active_task_count: active_tasks.len(),
            total_coordination_messages: 0, // Would track from coordination bus
        }
    }

  ./crates/ccswarm/src/session/coordinator.rs:451-468 method send_coordination_update <-> ./crates/ccswarm/src/session/coordinator.rs:554-586 method start_efficiency_monitoring
  Similarity: 93.68%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:send_coordination_update (lines 451-468) ---[0m
    async fn send_coordination_update(&self, _role: &AgentRole, result: &TaskResult) -> Result<()> {
        let message = SessionCoordinationMessage::SessionStatusUpdate {
            session_id: "session_id".to_string(), // Would extract from result
            agent_id: "agent_id".to_string(),     // Would extract from result
            load: 0.5,                            // Would calculate actual load
            tasks_completed: 1,
            efficiency_metrics: EfficiencyMetrics {
                sessions_reused: if result.success { 1 } else { 0 },
                tokens_saved: 3400,
                context_continuity_score: 0.95,
                average_response_time: result.duration.as_secs_f64(),
            },
        };

        // Send to coordination system
        self.send_coordination_message(message).await?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:start_efficiency_monitoring (lines 554-586) ---[0m
    async fn start_efficiency_monitoring(&self) {
        let performance_tracker = Arc::clone(&self.performance_tracker);
        let config = self.config.clone();

        tokio::spawn(async move {
            let mut interval = tokio::time::interval(config.reporting_interval);

            loop {
                interval.tick().await;

                let tracker = performance_tracker.lock().await;

                if tracker.total_tasks > 0 {
                    let efficiency_percentage = (tracker.total_tokens_saved as f64
                        / (tracker.total_tasks * 3600) as f64)
                        * 100.0;
                    let efficiency_score = if tracker.total_tasks > 0 {
                        tracker.total_tokens_saved as f64 / tracker.total_tasks as f64
                    } else {
                        0.0
                    };

                    tracing::info!(
                        "Session Efficiency Report - Tasks: {}, Context Optimized: {}, Efficiency: {:.1}%, Score: {:.2}",
                        tracker.total_tasks,
                        tracker.total_tokens_saved,
                        efficiency_percentage,
                        efficiency_score
                    );
                }
            }
        });
    }

  ./crates/ccswarm/src/session/coordinator.rs:231-261 method new <-> ./crates/ccswarm/src/session/coordinator.rs:620-647 method get_coordination_statistics
  Similarity: 78.98%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:new (lines 231-261) ---[0m
    pub async fn new(
        worktree_config: WorktreeSessionConfig,
        pool_config: SessionPoolConfig,
        coordinator_config: SessionCoordinatorConfig,
    ) -> Result<Self> {
        // Create session pool
        let mut session_pool = SessionPool::new(worktree_config, pool_config).await?;
        session_pool.start().await?;

        // Initialize coordination bus
        let coordination_bus =
            CoordinationBus::new(coordinator_config.coordination_dir.clone()).await?;

        // Initialize performance tracker
        let performance_tracker = PerformanceTracker {
            start_time: Utc::now(),
            total_tasks: 0,
            total_sessions_created: 0,
            total_tokens_saved: 0,
            total_execution_time: std::time::Duration::ZERO,
            error_count: 0,
        };

        Ok(Self {
            session_pool: Arc::new(Mutex::new(session_pool)),
            coordination_bus: Arc::new(RwLock::new(coordination_bus)),
            active_tasks: Arc::new(RwLock::new(HashMap::new())),
            performance_tracker: Arc::new(Mutex::new(performance_tracker)),
            config: coordinator_config,
        })
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:get_coordination_statistics (lines 620-647) ---[0m
    pub async fn get_coordination_statistics(&self) -> CoordinationStatistics {
        let pool_stats = {
            let session_pool = self.session_pool.lock().await;
            session_pool.get_pool_statistics().await
        };

        let performance_tracker = self.performance_tracker.lock().await;
        let active_tasks = self.active_tasks.read().await;

        CoordinationStatistics {
            pool_statistics: pool_stats,
            efficiency_metrics: EfficiencyMetrics {
                sessions_reused: performance_tracker
                    .total_tasks
                    .saturating_sub(performance_tracker.total_sessions_created),
                tokens_saved: performance_tracker.total_tokens_saved,
                context_continuity_score: 0.95, // Would calculate from session data
                average_response_time: if performance_tracker.total_tasks > 0 {
                    performance_tracker.total_execution_time.as_secs_f64()
                        / performance_tracker.total_tasks as f64
                } else {
                    0.0
                },
            },
            active_task_count: active_tasks.len(),
            total_coordination_messages: 0, // Would track from coordination bus
        }
    }

  ./crates/ccswarm/src/session/coordinator.rs:231-261 method new <-> ./crates/ccswarm/src/session/coordinator.rs:711-739 function test_token_savings_calculation
  Similarity: 77.33%

[36m--- ./crates/ccswarm/src/session/coordinator.rs:new (lines 231-261) ---[0m
    pub async fn new(
        worktree_config: WorktreeSessionConfig,
        pool_config: SessionPoolConfig,
        coordinator_config: SessionCoordinatorConfig,
    ) -> Result<Self> {
        // Create session pool
        let mut session_pool = SessionPool::new(worktree_config, pool_config).await?;
        session_pool.start().await?;

        // Initialize coordination bus
        let coordination_bus =
            CoordinationBus::new(coordinator_config.coordination_dir.clone()).await?;

        // Initialize performance tracker
        let performance_tracker = PerformanceTracker {
            start_time: Utc::now(),
            total_tasks: 0,
            total_sessions_created: 0,
            total_tokens_saved: 0,
            total_execution_time: std::time::Duration::ZERO,
            error_count: 0,
        };

        Ok(Self {
            session_pool: Arc::new(Mutex::new(session_pool)),
            coordination_bus: Arc::new(RwLock::new(coordination_bus)),
            active_tasks: Arc::new(RwLock::new(HashMap::new())),
            performance_tracker: Arc::new(Mutex::new(performance_tracker)),
            config: coordinator_config,
        })
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:test_token_savings_calculation (lines 711-739) ---[0m
    async fn test_token_savings_calculation() {
        let temp_dir = TempDir::new().unwrap();

        let mut worktree_config = WorktreeSessionConfig::default();
        worktree_config.repo_path = temp_dir.path().to_path_buf();

        let pool_config = SessionPoolConfig::default();

        let mut coordinator_config = SessionCoordinatorConfig::default();
        coordinator_config.coordination_dir = temp_dir.path().join("coordination");

        let coordinator = SessionCoordinator::new(worktree_config, pool_config, coordinator_config)
            .await
            .unwrap();

        // Test token savings calculation
        let result = TaskResult {
            success: true,
            output: serde_json::json!({
                "session_id": "test-session",
                "response": "task completed"
            }),
            error: None,
            duration: std::time::Duration::from_secs(1),
        };

        let savings = coordinator.calculate_token_savings(&result).await;
        assert_eq!(savings, 3400); // Expected savings for session reuse
    }

  ./crates/ccswarm/src/session/coordinator.rs:554-586 method start_efficiency_monitoring <-> ./crates/ccswarm/src/session/coordinator.rs:589-604 method start_coordination_processing
  Similarity: 89.00%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:start_efficiency_monitoring (lines 554-586) ---[0m
    async fn start_efficiency_monitoring(&self) {
        let performance_tracker = Arc::clone(&self.performance_tracker);
        let config = self.config.clone();

        tokio::spawn(async move {
            let mut interval = tokio::time::interval(config.reporting_interval);

            loop {
                interval.tick().await;

                let tracker = performance_tracker.lock().await;

                if tracker.total_tasks > 0 {
                    let efficiency_percentage = (tracker.total_tokens_saved as f64
                        / (tracker.total_tasks * 3600) as f64)
                        * 100.0;
                    let efficiency_score = if tracker.total_tasks > 0 {
                        tracker.total_tokens_saved as f64 / tracker.total_tasks as f64
                    } else {
                        0.0
                    };

                    tracing::info!(
                        "Session Efficiency Report - Tasks: {}, Context Optimized: {}, Efficiency: {:.1}%, Score: {:.2}",
                        tracker.total_tasks,
                        tracker.total_tokens_saved,
                        efficiency_percentage,
                        efficiency_score
                    );
                }
            }
        });
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:start_coordination_processing (lines 589-604) ---[0m
    async fn start_coordination_processing(&self) {
        let coordination_dir = self.config.coordination_dir.clone();

        tokio::spawn(async move {
            let mut interval = tokio::time::interval(std::time::Duration::from_secs(5));

            loop {
                interval.tick().await;

                // Process incoming coordination messages
                if let Err(e) = Self::process_coordination_messages(&coordination_dir).await {
                    tracing::error!("Failed to process coordination messages: {}", e);
                }
            }
        });
    }

  ./crates/ccswarm/src/session/coordinator.rs:524-551 method send_coordination_message <-> ./crates/ccswarm/src/session/coordinator.rs:554-586 method start_efficiency_monitoring
  Similarity: 70.82%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:send_coordination_message (lines 524-551) ---[0m
    async fn send_coordination_message(&self, message: SessionCoordinationMessage) -> Result<()> {
        let coordination_message = AgentMessage::Coordination {
            from_agent: "session-coordinator".to_string(),
            to_agent: "system".to_string(),
            message_type: CoordinationType::StatusUpdate, // Assuming this exists
            payload: serde_json::to_value(message)?,
        };

        // Write to coordination directory
        let message_file = self
            .config
            .coordination_dir
            .join("messages")
            .join(format!("session-{}.json", Uuid::new_v4()));

        if let Some(parent) = message_file.parent() {
            tokio::fs::create_dir_all(parent).await?;
        }

        tokio::fs::write(
            &message_file,
            serde_json::to_string_pretty(&coordination_message)?,
        )
        .await
        .context("Failed to write coordination message")?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:start_efficiency_monitoring (lines 554-586) ---[0m
    async fn start_efficiency_monitoring(&self) {
        let performance_tracker = Arc::clone(&self.performance_tracker);
        let config = self.config.clone();

        tokio::spawn(async move {
            let mut interval = tokio::time::interval(config.reporting_interval);

            loop {
                interval.tick().await;

                let tracker = performance_tracker.lock().await;

                if tracker.total_tasks > 0 {
                    let efficiency_percentage = (tracker.total_tokens_saved as f64
                        / (tracker.total_tasks * 3600) as f64)
                        * 100.0;
                    let efficiency_score = if tracker.total_tasks > 0 {
                        tracker.total_tokens_saved as f64 / tracker.total_tasks as f64
                    } else {
                        0.0
                    };

                    tracing::info!(
                        "Session Efficiency Report - Tasks: {}, Context Optimized: {}, Efficiency: {:.1}%, Score: {:.2}",
                        tracker.total_tasks,
                        tracker.total_tokens_saved,
                        efficiency_percentage,
                        efficiency_score
                    );
                }
            }
        });
    }

  ./crates/ccswarm/src/session/coordinator.rs:691-708 function test_session_coordinator_creation <-> ./crates/ccswarm/src/session/coordinator.rs:711-739 function test_token_savings_calculation
  Similarity: 89.56%

[36m--- ./crates/ccswarm/src/session/coordinator.rs:test_session_coordinator_creation (lines 691-708) ---[0m
    async fn test_session_coordinator_creation() {
        let temp_dir = TempDir::new().unwrap();

        let mut worktree_config = WorktreeSessionConfig::default();
        worktree_config.repo_path = temp_dir.path().to_path_buf();

        let pool_config = SessionPoolConfig::default();

        let mut coordinator_config = SessionCoordinatorConfig::default();
        coordinator_config.coordination_dir = temp_dir.path().join("coordination");

        let coordinator = SessionCoordinator::new(worktree_config, pool_config, coordinator_config)
            .await
            .unwrap();

        let stats = coordinator.get_coordination_statistics().await;
        assert_eq!(stats.active_task_count, 0);
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:test_token_savings_calculation (lines 711-739) ---[0m
    async fn test_token_savings_calculation() {
        let temp_dir = TempDir::new().unwrap();

        let mut worktree_config = WorktreeSessionConfig::default();
        worktree_config.repo_path = temp_dir.path().to_path_buf();

        let pool_config = SessionPoolConfig::default();

        let mut coordinator_config = SessionCoordinatorConfig::default();
        coordinator_config.coordination_dir = temp_dir.path().join("coordination");

        let coordinator = SessionCoordinator::new(worktree_config, pool_config, coordinator_config)
            .await
            .unwrap();

        // Test token savings calculation
        let result = TaskResult {
            success: true,
            output: serde_json::json!({
                "session_id": "test-session",
                "response": "task completed"
            }),
            error: None,
            duration: std::time::Duration::from_secs(1),
        };

        let savings = coordinator.calculate_token_savings(&result).await;
        assert_eq!(savings, 3400); // Expected savings for session reuse
    }

  ./crates/ccswarm/src/session/coordinator.rs:231-261 method new <-> ./crates/ccswarm/src/session/coordinator.rs:451-468 method send_coordination_update
  Similarity: 85.74%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:new (lines 231-261) ---[0m
    pub async fn new(
        worktree_config: WorktreeSessionConfig,
        pool_config: SessionPoolConfig,
        coordinator_config: SessionCoordinatorConfig,
    ) -> Result<Self> {
        // Create session pool
        let mut session_pool = SessionPool::new(worktree_config, pool_config).await?;
        session_pool.start().await?;

        // Initialize coordination bus
        let coordination_bus =
            CoordinationBus::new(coordinator_config.coordination_dir.clone()).await?;

        // Initialize performance tracker
        let performance_tracker = PerformanceTracker {
            start_time: Utc::now(),
            total_tasks: 0,
            total_sessions_created: 0,
            total_tokens_saved: 0,
            total_execution_time: std::time::Duration::ZERO,
            error_count: 0,
        };

        Ok(Self {
            session_pool: Arc::new(Mutex::new(session_pool)),
            coordination_bus: Arc::new(RwLock::new(coordination_bus)),
            active_tasks: Arc::new(RwLock::new(HashMap::new())),
            performance_tracker: Arc::new(Mutex::new(performance_tracker)),
            config: coordinator_config,
        })
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:send_coordination_update (lines 451-468) ---[0m
    async fn send_coordination_update(&self, _role: &AgentRole, result: &TaskResult) -> Result<()> {
        let message = SessionCoordinationMessage::SessionStatusUpdate {
            session_id: "session_id".to_string(), // Would extract from result
            agent_id: "agent_id".to_string(),     // Would extract from result
            load: 0.5,                            // Would calculate actual load
            tasks_completed: 1,
            efficiency_metrics: EfficiencyMetrics {
                sessions_reused: if result.success { 1 } else { 0 },
                tokens_saved: 3400,
                context_continuity_score: 0.95,
                average_response_time: result.duration.as_secs_f64(),
            },
        };

        // Send to coordination system
        self.send_coordination_message(message).await?;
        Ok(())
    }

  ./crates/ccswarm/src/session/coordinator.rs:451-468 method send_coordination_update <-> ./crates/ccswarm/src/session/coordinator.rs:620-647 method get_coordination_statistics
  Similarity: 90.27%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:send_coordination_update (lines 451-468) ---[0m
    async fn send_coordination_update(&self, _role: &AgentRole, result: &TaskResult) -> Result<()> {
        let message = SessionCoordinationMessage::SessionStatusUpdate {
            session_id: "session_id".to_string(), // Would extract from result
            agent_id: "agent_id".to_string(),     // Would extract from result
            load: 0.5,                            // Would calculate actual load
            tasks_completed: 1,
            efficiency_metrics: EfficiencyMetrics {
                sessions_reused: if result.success { 1 } else { 0 },
                tokens_saved: 3400,
                context_continuity_score: 0.95,
                average_response_time: result.duration.as_secs_f64(),
            },
        };

        // Send to coordination system
        self.send_coordination_message(message).await?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:get_coordination_statistics (lines 620-647) ---[0m
    pub async fn get_coordination_statistics(&self) -> CoordinationStatistics {
        let pool_stats = {
            let session_pool = self.session_pool.lock().await;
            session_pool.get_pool_statistics().await
        };

        let performance_tracker = self.performance_tracker.lock().await;
        let active_tasks = self.active_tasks.read().await;

        CoordinationStatistics {
            pool_statistics: pool_stats,
            efficiency_metrics: EfficiencyMetrics {
                sessions_reused: performance_tracker
                    .total_tasks
                    .saturating_sub(performance_tracker.total_sessions_created),
                tokens_saved: performance_tracker.total_tokens_saved,
                context_continuity_score: 0.95, // Would calculate from session data
                average_response_time: if performance_tracker.total_tasks > 0 {
                    performance_tracker.total_execution_time.as_secs_f64()
                        / performance_tracker.total_tasks as f64
                } else {
                    0.0
                },
            },
            active_task_count: active_tasks.len(),
            total_coordination_messages: 0, // Would track from coordination bus
        }
    }

  ./crates/ccswarm/src/session/coordinator.rs:231-261 method new <-> ./crates/ccswarm/src/session/coordinator.rs:691-708 function test_session_coordinator_creation
  Similarity: 84.00%

[36m--- ./crates/ccswarm/src/session/coordinator.rs:new (lines 231-261) ---[0m
    pub async fn new(
        worktree_config: WorktreeSessionConfig,
        pool_config: SessionPoolConfig,
        coordinator_config: SessionCoordinatorConfig,
    ) -> Result<Self> {
        // Create session pool
        let mut session_pool = SessionPool::new(worktree_config, pool_config).await?;
        session_pool.start().await?;

        // Initialize coordination bus
        let coordination_bus =
            CoordinationBus::new(coordinator_config.coordination_dir.clone()).await?;

        // Initialize performance tracker
        let performance_tracker = PerformanceTracker {
            start_time: Utc::now(),
            total_tasks: 0,
            total_sessions_created: 0,
            total_tokens_saved: 0,
            total_execution_time: std::time::Duration::ZERO,
            error_count: 0,
        };

        Ok(Self {
            session_pool: Arc::new(Mutex::new(session_pool)),
            coordination_bus: Arc::new(RwLock::new(coordination_bus)),
            active_tasks: Arc::new(RwLock::new(HashMap::new())),
            performance_tracker: Arc::new(Mutex::new(performance_tracker)),
            config: coordinator_config,
        })
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:test_session_coordinator_creation (lines 691-708) ---[0m
    async fn test_session_coordinator_creation() {
        let temp_dir = TempDir::new().unwrap();

        let mut worktree_config = WorktreeSessionConfig::default();
        worktree_config.repo_path = temp_dir.path().to_path_buf();

        let pool_config = SessionPoolConfig::default();

        let mut coordinator_config = SessionCoordinatorConfig::default();
        coordinator_config.coordination_dir = temp_dir.path().join("coordination");

        let coordinator = SessionCoordinator::new(worktree_config, pool_config, coordinator_config)
            .await
            .unwrap();

        let stats = coordinator.get_coordination_statistics().await;
        assert_eq!(stats.active_task_count, 0);
    }

  ./crates/ccswarm/src/session/coordinator.rs:451-468 method send_coordination_update <-> ./crates/ccswarm/src/session/coordinator.rs:524-551 method send_coordination_message
  Similarity: 87.64%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:send_coordination_update (lines 451-468) ---[0m
    async fn send_coordination_update(&self, _role: &AgentRole, result: &TaskResult) -> Result<()> {
        let message = SessionCoordinationMessage::SessionStatusUpdate {
            session_id: "session_id".to_string(), // Would extract from result
            agent_id: "agent_id".to_string(),     // Would extract from result
            load: 0.5,                            // Would calculate actual load
            tasks_completed: 1,
            efficiency_metrics: EfficiencyMetrics {
                sessions_reused: if result.success { 1 } else { 0 },
                tokens_saved: 3400,
                context_continuity_score: 0.95,
                average_response_time: result.duration.as_secs_f64(),
            },
        };

        // Send to coordination system
        self.send_coordination_message(message).await?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:send_coordination_message (lines 524-551) ---[0m
    async fn send_coordination_message(&self, message: SessionCoordinationMessage) -> Result<()> {
        let coordination_message = AgentMessage::Coordination {
            from_agent: "session-coordinator".to_string(),
            to_agent: "system".to_string(),
            message_type: CoordinationType::StatusUpdate, // Assuming this exists
            payload: serde_json::to_value(message)?,
        };

        // Write to coordination directory
        let message_file = self
            .config
            .coordination_dir
            .join("messages")
            .join(format!("session-{}.json", Uuid::new_v4()));

        if let Some(parent) = message_file.parent() {
            tokio::fs::create_dir_all(parent).await?;
        }

        tokio::fs::write(
            &message_file,
            serde_json::to_string_pretty(&coordination_message)?,
        )
        .await
        .context("Failed to write coordination message")?;

        Ok(())
    }

  ./crates/ccswarm/src/session/coordinator.rs:589-604 method start_coordination_processing <-> ./crates/ccswarm/src/session/coordinator.rs:620-647 method get_coordination_statistics
  Similarity: 76.15%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:start_coordination_processing (lines 589-604) ---[0m
    async fn start_coordination_processing(&self) {
        let coordination_dir = self.config.coordination_dir.clone();

        tokio::spawn(async move {
            let mut interval = tokio::time::interval(std::time::Duration::from_secs(5));

            loop {
                interval.tick().await;

                // Process incoming coordination messages
                if let Err(e) = Self::process_coordination_messages(&coordination_dir).await {
                    tracing::error!("Failed to process coordination messages: {}", e);
                }
            }
        });
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:get_coordination_statistics (lines 620-647) ---[0m
    pub async fn get_coordination_statistics(&self) -> CoordinationStatistics {
        let pool_stats = {
            let session_pool = self.session_pool.lock().await;
            session_pool.get_pool_statistics().await
        };

        let performance_tracker = self.performance_tracker.lock().await;
        let active_tasks = self.active_tasks.read().await;

        CoordinationStatistics {
            pool_statistics: pool_stats,
            efficiency_metrics: EfficiencyMetrics {
                sessions_reused: performance_tracker
                    .total_tasks
                    .saturating_sub(performance_tracker.total_sessions_created),
                tokens_saved: performance_tracker.total_tokens_saved,
                context_continuity_score: 0.95, // Would calculate from session data
                average_response_time: if performance_tracker.total_tasks > 0 {
                    performance_tracker.total_execution_time.as_secs_f64()
                        / performance_tracker.total_tasks as f64
                } else {
                    0.0
                },
            },
            active_task_count: active_tasks.len(),
            total_coordination_messages: 0, // Would track from coordination bus
        }
    }

  ./crates/ccswarm/src/session/coordinator.rs:451-468 method send_coordination_update <-> ./crates/ccswarm/src/session/coordinator.rs:589-604 method start_coordination_processing
  Similarity: 86.94%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:send_coordination_update (lines 451-468) ---[0m
    async fn send_coordination_update(&self, _role: &AgentRole, result: &TaskResult) -> Result<()> {
        let message = SessionCoordinationMessage::SessionStatusUpdate {
            session_id: "session_id".to_string(), // Would extract from result
            agent_id: "agent_id".to_string(),     // Would extract from result
            load: 0.5,                            // Would calculate actual load
            tasks_completed: 1,
            efficiency_metrics: EfficiencyMetrics {
                sessions_reused: if result.success { 1 } else { 0 },
                tokens_saved: 3400,
                context_continuity_score: 0.95,
                average_response_time: result.duration.as_secs_f64(),
            },
        };

        // Send to coordination system
        self.send_coordination_message(message).await?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:start_coordination_processing (lines 589-604) ---[0m
    async fn start_coordination_processing(&self) {
        let coordination_dir = self.config.coordination_dir.clone();

        tokio::spawn(async move {
            let mut interval = tokio::time::interval(std::time::Duration::from_secs(5));

            loop {
                interval.tick().await;

                // Process incoming coordination messages
                if let Err(e) = Self::process_coordination_messages(&coordination_dir).await {
                    tracing::error!("Failed to process coordination messages: {}", e);
                }
            }
        });
    }

  ./crates/ccswarm/src/session/coordinator.rs:451-468 method send_coordination_update <-> ./crates/ccswarm/src/session/coordinator.rs:650-661 method shutdown
  Similarity: 90.45%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:send_coordination_update (lines 451-468) ---[0m
    async fn send_coordination_update(&self, _role: &AgentRole, result: &TaskResult) -> Result<()> {
        let message = SessionCoordinationMessage::SessionStatusUpdate {
            session_id: "session_id".to_string(), // Would extract from result
            agent_id: "agent_id".to_string(),     // Would extract from result
            load: 0.5,                            // Would calculate actual load
            tasks_completed: 1,
            efficiency_metrics: EfficiencyMetrics {
                sessions_reused: if result.success { 1 } else { 0 },
                tokens_saved: 3400,
                context_continuity_score: 0.95,
                average_response_time: result.duration.as_secs_f64(),
            },
        };

        // Send to coordination system
        self.send_coordination_message(message).await?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:shutdown (lines 650-661) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down session coordinator");

        // Shutdown session pool
        {
            let mut session_pool = self.session_pool.lock().await;
            session_pool.shutdown().await?;
        }

        tracing::info!("Session coordinator shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/coordinator.rs:264-277 method start <-> ./crates/ccswarm/src/session/coordinator.rs:451-468 method send_coordination_update
  Similarity: 79.25%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:start (lines 264-277) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting session coordinator");

        // Start performance reporting task
        if self.config.efficiency_optimization {
            self.start_efficiency_monitoring().await;
        }

        // Start coordination message processing
        self.start_coordination_processing().await;

        tracing::info!("Session coordinator started successfully");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:send_coordination_update (lines 451-468) ---[0m
    async fn send_coordination_update(&self, _role: &AgentRole, result: &TaskResult) -> Result<()> {
        let message = SessionCoordinationMessage::SessionStatusUpdate {
            session_id: "session_id".to_string(), // Would extract from result
            agent_id: "agent_id".to_string(),     // Would extract from result
            load: 0.5,                            // Would calculate actual load
            tasks_completed: 1,
            efficiency_metrics: EfficiencyMetrics {
                sessions_reused: if result.success { 1 } else { 0 },
                tokens_saved: 3400,
                context_continuity_score: 0.95,
                average_response_time: result.duration.as_secs_f64(),
            },
        };

        // Send to coordination system
        self.send_coordination_message(message).await?;
        Ok(())
    }

  ./crates/ccswarm/src/session/coordinator.rs:213-226 method default <-> ./crates/ccswarm/src/session/coordinator.rs:589-604 method start_coordination_processing
  Similarity: 83.86%
  Classes: Default <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:default (lines 213-226) ---[0m
    fn default() -> Self {
        Self {
            coordination_dir: PathBuf::from("coordination"),
            reporting_interval: std::time::Duration::from_secs(60),
            efficiency_optimization: true,
            efficiency_baseline: 1.0, // Baseline efficiency metric
            batch_optimization: BatchOptimizationConfig {
                enabled: true,
                max_batch_size: 10,
                batch_timeout: std::time::Duration::from_secs(30),
                compatibility_threshold: 0.8,
            },
        }
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:start_coordination_processing (lines 589-604) ---[0m
    async fn start_coordination_processing(&self) {
        let coordination_dir = self.config.coordination_dir.clone();

        tokio::spawn(async move {
            let mut interval = tokio::time::interval(std::time::Duration::from_secs(5));

            loop {
                interval.tick().await;

                // Process incoming coordination messages
                if let Err(e) = Self::process_coordination_messages(&coordination_dir).await {
                    tracing::error!("Failed to process coordination messages: {}", e);
                }
            }
        });
    }

  ./crates/ccswarm/src/session/coordinator.rs:213-226 method default <-> ./crates/ccswarm/src/session/coordinator.rs:451-468 method send_coordination_update
  Similarity: 76.64%
  Classes: Default <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:default (lines 213-226) ---[0m
    fn default() -> Self {
        Self {
            coordination_dir: PathBuf::from("coordination"),
            reporting_interval: std::time::Duration::from_secs(60),
            efficiency_optimization: true,
            efficiency_baseline: 1.0, // Baseline efficiency metric
            batch_optimization: BatchOptimizationConfig {
                enabled: true,
                max_batch_size: 10,
                batch_timeout: std::time::Duration::from_secs(30),
                compatibility_threshold: 0.8,
            },
        }
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:send_coordination_update (lines 451-468) ---[0m
    async fn send_coordination_update(&self, _role: &AgentRole, result: &TaskResult) -> Result<()> {
        let message = SessionCoordinationMessage::SessionStatusUpdate {
            session_id: "session_id".to_string(), // Would extract from result
            agent_id: "agent_id".to_string(),     // Would extract from result
            load: 0.5,                            // Would calculate actual load
            tasks_completed: 1,
            efficiency_metrics: EfficiencyMetrics {
                sessions_reused: if result.success { 1 } else { 0 },
                tokens_saved: 3400,
                context_continuity_score: 0.95,
                average_response_time: result.duration.as_secs_f64(),
            },
        };

        // Send to coordination system
        self.send_coordination_message(message).await?;
        Ok(())
    }

  ./crates/ccswarm/src/session/coordinator.rs:264-277 method start <-> ./crates/ccswarm/src/session/coordinator.rs:650-661 method shutdown
  Similarity: 86.34%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:start (lines 264-277) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting session coordinator");

        // Start performance reporting task
        if self.config.efficiency_optimization {
            self.start_efficiency_monitoring().await;
        }

        // Start coordination message processing
        self.start_coordination_processing().await;

        tracing::info!("Session coordinator started successfully");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:shutdown (lines 650-661) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down session coordinator");

        // Shutdown session pool
        {
            let mut session_pool = self.session_pool.lock().await;
            session_pool.shutdown().await?;
        }

        tracing::info!("Session coordinator shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/coordinator.rs:264-277 method start <-> ./crates/ccswarm/src/session/coordinator.rs:436-448 method calculate_token_savings
  Similarity: 81.22%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:start (lines 264-277) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting session coordinator");

        // Start performance reporting task
        if self.config.efficiency_optimization {
            self.start_efficiency_monitoring().await;
        }

        // Start coordination message processing
        self.start_coordination_processing().await;

        tracing::info!("Session coordinator started successfully");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:calculate_token_savings (lines 436-448) ---[0m
    async fn calculate_token_savings(&self, result: &TaskResult) -> usize {
        // Simplified calculation - in practice would be more sophisticated
        // Based on whether this was a new session or reused session

        // If session was reused (indicated by session_id in output), significant savings
        if let Some(output) = result.output.as_object() {
            if output.contains_key("session_id") {
                return 3400; // Estimated savings from session reuse
            }
        }

        0 // No savings if new session was created
    }

  ./crates/ccswarm/src/session/coordinator.rs:436-448 method calculate_token_savings <-> ./crates/ccswarm/src/session/coordinator.rs:650-661 method shutdown
  Similarity: 84.38%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:calculate_token_savings (lines 436-448) ---[0m
    async fn calculate_token_savings(&self, result: &TaskResult) -> usize {
        // Simplified calculation - in practice would be more sophisticated
        // Based on whether this was a new session or reused session

        // If session was reused (indicated by session_id in output), significant savings
        if let Some(output) = result.output.as_object() {
            if output.contains_key("session_id") {
                return 3400; // Estimated savings from session reuse
            }
        }

        0 // No savings if new session was created
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:shutdown (lines 650-661) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down session coordinator");

        // Shutdown session pool
        {
            let mut session_pool = self.session_pool.lock().await;
            session_pool.shutdown().await?;
        }

        tracing::info!("Session coordinator shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/coordinator.rs:436-448 method calculate_token_savings <-> ./crates/ccswarm/src/session/coordinator.rs:607-617 method process_coordination_messages
  Similarity: 87.59%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:calculate_token_savings (lines 436-448) ---[0m
    async fn calculate_token_savings(&self, result: &TaskResult) -> usize {
        // Simplified calculation - in practice would be more sophisticated
        // Based on whether this was a new session or reused session

        // If session was reused (indicated by session_id in output), significant savings
        if let Some(output) = result.output.as_object() {
            if output.contains_key("session_id") {
                return 3400; // Estimated savings from session reuse
            }
        }

        0 // No savings if new session was created
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:process_coordination_messages (lines 607-617) ---[0m
    async fn process_coordination_messages(coordination_dir: &Path) -> Result<()> {
        let messages_dir = coordination_dir.join("messages");

        if !messages_dir.exists() {
            return Ok(());
        }

        // Read and process coordination messages
        // This would integrate with the existing coordination system
        Ok(())
    }

  ./crates/ccswarm/src/session/coordinator.rs:607-617 method process_coordination_messages <-> ./crates/ccswarm/src/session/coordinator.rs:650-661 method shutdown
  Similarity: 87.62%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:process_coordination_messages (lines 607-617) ---[0m
    async fn process_coordination_messages(coordination_dir: &Path) -> Result<()> {
        let messages_dir = coordination_dir.join("messages");

        if !messages_dir.exists() {
            return Ok(());
        }

        // Read and process coordination messages
        // This would integrate with the existing coordination system
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:shutdown (lines 650-661) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down session coordinator");

        // Shutdown session pool
        {
            let mut session_pool = self.session_pool.lock().await;
            session_pool.shutdown().await?;
        }

        tracing::info!("Session coordinator shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/coordinator.rs:264-277 method start <-> ./crates/ccswarm/src/session/coordinator.rs:607-617 method process_coordination_messages
  Similarity: 75.73%
  Classes: SessionCoordinator <-> SessionCoordinator

[36m--- ./crates/ccswarm/src/session/coordinator.rs:start (lines 264-277) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting session coordinator");

        // Start performance reporting task
        if self.config.efficiency_optimization {
            self.start_efficiency_monitoring().await;
        }

        // Start coordination message processing
        self.start_coordination_processing().await;

        tracing::info!("Session coordinator started successfully");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:process_coordination_messages (lines 607-617) ---[0m
    async fn process_coordination_messages(coordination_dir: &Path) -> Result<()> {
        let messages_dir = coordination_dir.join("messages");

        if !messages_dir.exists() {
            return Ok(());
        }

        // Read and process coordination messages
        // This would integrate with the existing coordination system
        Ok(())
    }

  ./crates/ccswarm/src/session/coordinator.rs:436-448 method calculate_token_savings <-> ./crates/ccswarm/src/session/coordinator.rs:674-681 method new
  Similarity: 75.36%
  Classes: SessionCoordinator <-> CoordinationBus

[36m--- ./crates/ccswarm/src/session/coordinator.rs:calculate_token_savings (lines 436-448) ---[0m
    async fn calculate_token_savings(&self, result: &TaskResult) -> usize {
        // Simplified calculation - in practice would be more sophisticated
        // Based on whether this was a new session or reused session

        // If session was reused (indicated by session_id in output), significant savings
        if let Some(output) = result.output.as_object() {
            if output.contains_key("session_id") {
                return 3400; // Estimated savings from session reuse
            }
        }

        0 // No savings if new session was created
    }

[36m--- ./crates/ccswarm/src/session/coordinator.rs:new (lines 674-681) ---[0m
    async fn new(coordination_dir: PathBuf) -> Result<Self> {
        tokio::fs::create_dir_all(&coordination_dir).await?;

        Ok(Self {
            outbound_queue: Vec::new(),
            coordination_dir,
        })
    }


Duplicates in ./crates/ccswarm/src/coordination/dialogue.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/coordination/dialogue.rs:328-435 method add_dialogue_message <-> ./crates/ccswarm/src/coordination/dialogue.rs:558-635 method update_agent_profile_with_topic
  Similarity: 83.79%
  Classes: DialogueCoordinationBus <-> DialogueCoordinationBus

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:add_dialogue_message (lines 328-435) ---[0m
    pub async fn add_dialogue_message(
        &mut self,
        conversation_id: &str,
        speaker_id: String,
        content: String,
        message_type: DialogueMessageType,
        emotional_tone: EmotionalTone,
        response_expectation: ResponseExpectation,
    ) -> Result<()> {
        let conversation = self
            .conversations
            .get_mut(conversation_id)
            .ok_or_else(|| anyhow::anyhow!("Conversation not found: {}", conversation_id))?;

        let entry = DialogueEntry {
            id: Uuid::new_v4().to_string(),
            timestamp: Utc::now(),
            speaker_id: speaker_id.clone(),
            recipients: conversation
                .participants
                .iter()
                .filter(|&p| p != &speaker_id)
                .cloned()
                .collect(),
            message_type,
            content: content.clone(),
            context_references: Vec::new(),
            emotional_tone,
            response_expectation,
            thread_reference: None,
            urgency_level: UrgencyLevel::Normal,
        };

        conversation.dialogue_history.push_back(entry.clone());
        conversation.last_activity = Utc::now();

        // Update conversation state - store needed data first
        let speaker_id = entry.speaker_id.clone();
        let message_type = entry.message_type.clone();

        // Release the mutable borrow on conversation before calling the method
        conversation.conversation_state.current_speaker = Some(speaker_id.clone());

        // Update turn order
        if let Some(pos) = conversation
            .conversation_state
            .turn_order
            .iter()
            .position(|id| id == &speaker_id)
        {
            let speaker = conversation
                .conversation_state
                .turn_order
                .remove(pos)
                .unwrap();
            conversation
                .conversation_state
                .turn_order
                .push_front(speaker);
        }

        // Update conversation phase based on message type
        conversation.conversation_state.phase = match message_type {
            DialogueMessageType::Question => ConversationPhase::InformationGathering,
            DialogueMessageType::Suggestion | DialogueMessageType::Proposal => {
                ConversationPhase::ProblemSolving
            }
            DialogueMessageType::Agreement | DialogueMessageType::Disagreement => {
                ConversationPhase::DecisionMaking
            }
            DialogueMessageType::StatusUpdate => ConversationPhase::Planning,
            DialogueMessageType::Acknowledgment => ConversationPhase::Closing,
            _ => conversation.conversation_state.phase.clone(),
        };

        // Update engagement levels
        let engagement_boost = match message_type {
            DialogueMessageType::Question | DialogueMessageType::Suggestion => 0.2,
            DialogueMessageType::Information | DialogueMessageType::StatusUpdate => 0.1,
            _ => 0.05,
        };

        conversation
            .conversation_state
            .engagement_levels
            .entry(speaker_id.to_string())
            .and_modify(|e| *e = (*e + engagement_boost).min(1.0))
            .or_insert(0.5 + engagement_boost);

        // Send coordination messages to recipients
        for recipient in &entry.recipients {
            let message = AgentMessage::Coordination {
                from_agent: speaker_id.clone(),
                to_agent: recipient.clone(),
                message_type: CoordinationType::InformationRequest,
                payload: serde_json::json!({
                    "conversation_id": conversation_id,
                    "dialogue_entry": entry,
                }),
            };
            self.coordination_bus.send_message(message).await?;
        }

        // Learn from dialogue patterns
        self.learn_dialogue_patterns(conversation_id).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:update_agent_profile_with_topic (lines 558-635) ---[0m
    async fn update_agent_profile_with_topic(
        &mut self,
        agent_id: &str,
        entry: &DialogueEntry,
        conversation_topic: &str,
    ) -> Result<()> {
        let profile = self
            .agent_profiles
            .entry(agent_id.to_string())
            .or_insert_with(|| AgentDialogueProfile {
                agent_id: agent_id.to_string(),
                communication_style: CommunicationStyle {
                    formality_level: 0.5,
                    verbosity: 0.5,
                    directness: 0.5,
                    supportiveness: 0.5,
                    inquisitiveness: 0.5,
                    patience_level: 0.5,
                },
                response_patterns: ResponsePatterns {
                    average_response_time: Duration::from_secs(120),
                    preferred_message_length: MessageLength::Medium,
                    question_asking_frequency: 0.3,
                    agreement_tendency: 0.6,
                    detail_providing_level: 0.5,
                    follow_up_likelihood: 0.4,
                },
                topic_expertise: HashMap::new(),
                collaboration_preferences: CollaborationPreferences {
                    prefers_async: false,
                    comfortable_with_interruptions: true,
                    likes_consensus_building: true,
                    prefers_structured_discussions: false,
                    open_to_peer_review: true,
                },
                conversation_history: ConversationHistory {
                    total_conversations: 0,
                    successful_collaborations: 0,
                    preferred_conversation_types: Vec::new(),
                    effective_partnerships: HashMap::new(),
                    conversation_outcomes: HashMap::new(),
                },
            });

        // Update communication style based on message characteristics
        let message_length = entry.content.len();
        if message_length < 50 {
            profile.communication_style.verbosity =
                (profile.communication_style.verbosity * 0.9 + 0.2 * 0.1).max(0.0);
        } else if message_length > 200 {
            profile.communication_style.verbosity =
                (profile.communication_style.verbosity * 0.9 + 0.8 * 0.1).min(1.0);
        }

        // Update directness based on message type
        match entry.message_type {
            DialogueMessageType::Request | DialogueMessageType::Suggestion => {
                profile.communication_style.directness =
                    (profile.communication_style.directness * 0.9 + 0.7 * 0.1).min(1.0);
            }
            DialogueMessageType::Question => {
                profile.communication_style.inquisitiveness =
                    (profile.communication_style.inquisitiveness * 0.9 + 0.8 * 0.1).min(1.0);
            }
            _ => {}
        }

        // Update topic expertise
        profile
            .topic_expertise
            .entry(conversation_topic.to_string())
            .and_modify(|e| *e = (*e + 0.1).min(1.0))
            .or_insert(0.6);

        profile.conversation_history.total_conversations += 1;

        Ok(())
    }

  ./crates/ccswarm/src/coordination/dialogue.rs:272-325 method start_conversation <-> ./crates/ccswarm/src/coordination/dialogue.rs:328-435 method add_dialogue_message
  Similarity: 84.15%
  Classes: DialogueCoordinationBus <-> DialogueCoordinationBus

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:start_conversation (lines 272-325) ---[0m
    pub async fn start_conversation(
        &mut self,
        participants: Vec<String>,
        topic: String,
        conversation_type: ConversationType,
        context: ConversationContext,
    ) -> Result<String> {
        let conversation_id = Uuid::new_v4().to_string();
        let now = Utc::now();

        let conversation = Conversation {
            id: conversation_id.clone(),
            participants: participants.clone(),
            topic,
            context,
            dialogue_history: VecDeque::new(),
            conversation_state: ConversationState {
                phase: ConversationPhase::Opening,
                turn_order: participants.into_iter().collect(),
                current_speaker: None,
                waiting_for_response: Vec::new(),
                consensus_level: 0.0,
                engagement_levels: HashMap::new(),
                progress_indicators: HashMap::new(),
            },
            started_at: now,
            last_activity: now,
            conversation_type,
        };

        // Store conversation data for notifications
        let participants_clone = conversation.participants.clone();
        let topic_clone = conversation.topic.clone();

        self.conversations
            .insert(conversation_id.clone(), conversation);

        // Notify all participants
        for participant in &participants_clone {
            let message = AgentMessage::Coordination {
                from_agent: "system".to_string(),
                to_agent: participant.clone(),
                message_type: CoordinationType::Custom("conversation_started".to_string()),
                payload: serde_json::json!({
                    "conversation_id": conversation_id,
                    "topic": topic_clone,
                    "participants": participants_clone,
                }),
            };
            self.coordination_bus.send_message(message).await?;
        }

        Ok(conversation_id)
    }

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:add_dialogue_message (lines 328-435) ---[0m
    pub async fn add_dialogue_message(
        &mut self,
        conversation_id: &str,
        speaker_id: String,
        content: String,
        message_type: DialogueMessageType,
        emotional_tone: EmotionalTone,
        response_expectation: ResponseExpectation,
    ) -> Result<()> {
        let conversation = self
            .conversations
            .get_mut(conversation_id)
            .ok_or_else(|| anyhow::anyhow!("Conversation not found: {}", conversation_id))?;

        let entry = DialogueEntry {
            id: Uuid::new_v4().to_string(),
            timestamp: Utc::now(),
            speaker_id: speaker_id.clone(),
            recipients: conversation
                .participants
                .iter()
                .filter(|&p| p != &speaker_id)
                .cloned()
                .collect(),
            message_type,
            content: content.clone(),
            context_references: Vec::new(),
            emotional_tone,
            response_expectation,
            thread_reference: None,
            urgency_level: UrgencyLevel::Normal,
        };

        conversation.dialogue_history.push_back(entry.clone());
        conversation.last_activity = Utc::now();

        // Update conversation state - store needed data first
        let speaker_id = entry.speaker_id.clone();
        let message_type = entry.message_type.clone();

        // Release the mutable borrow on conversation before calling the method
        conversation.conversation_state.current_speaker = Some(speaker_id.clone());

        // Update turn order
        if let Some(pos) = conversation
            .conversation_state
            .turn_order
            .iter()
            .position(|id| id == &speaker_id)
        {
            let speaker = conversation
                .conversation_state
                .turn_order
                .remove(pos)
                .unwrap();
            conversation
                .conversation_state
                .turn_order
                .push_front(speaker);
        }

        // Update conversation phase based on message type
        conversation.conversation_state.phase = match message_type {
            DialogueMessageType::Question => ConversationPhase::InformationGathering,
            DialogueMessageType::Suggestion | DialogueMessageType::Proposal => {
                ConversationPhase::ProblemSolving
            }
            DialogueMessageType::Agreement | DialogueMessageType::Disagreement => {
                ConversationPhase::DecisionMaking
            }
            DialogueMessageType::StatusUpdate => ConversationPhase::Planning,
            DialogueMessageType::Acknowledgment => ConversationPhase::Closing,
            _ => conversation.conversation_state.phase.clone(),
        };

        // Update engagement levels
        let engagement_boost = match message_type {
            DialogueMessageType::Question | DialogueMessageType::Suggestion => 0.2,
            DialogueMessageType::Information | DialogueMessageType::StatusUpdate => 0.1,
            _ => 0.05,
        };

        conversation
            .conversation_state
            .engagement_levels
            .entry(speaker_id.to_string())
            .and_modify(|e| *e = (*e + engagement_boost).min(1.0))
            .or_insert(0.5 + engagement_boost);

        // Send coordination messages to recipients
        for recipient in &entry.recipients {
            let message = AgentMessage::Coordination {
                from_agent: speaker_id.clone(),
                to_agent: recipient.clone(),
                message_type: CoordinationType::InformationRequest,
                payload: serde_json::json!({
                    "conversation_id": conversation_id,
                    "dialogue_entry": entry,
                }),
            };
            self.coordination_bus.send_message(message).await?;
        }

        // Learn from dialogue patterns
        self.learn_dialogue_patterns(conversation_id).await?;

        Ok(())
    }

  ./crates/ccswarm/src/coordination/dialogue.rs:439-493 method update_conversation_state_static <-> ./crates/ccswarm/src/coordination/dialogue.rs:558-635 method update_agent_profile_with_topic
  Similarity: 95.64%
  Classes: DialogueCoordinationBus <-> DialogueCoordinationBus

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:update_conversation_state_static (lines 439-493) ---[0m
    fn update_conversation_state_static(
        &mut self,
        conversation: &mut Conversation,
        speaker_id: &str,
        message_type: &DialogueMessageType,
    ) {
        // Update current speaker
        conversation.conversation_state.current_speaker = Some(speaker_id.to_string());

        // Update turn order
        if let Some(pos) = conversation
            .conversation_state
            .turn_order
            .iter()
            .position(|id| id == speaker_id)
        {
            let speaker = conversation
                .conversation_state
                .turn_order
                .remove(pos)
                .unwrap();
            conversation
                .conversation_state
                .turn_order
                .push_front(speaker);
        }

        // Update conversation phase based on message type
        conversation.conversation_state.phase = match message_type {
            DialogueMessageType::Question => ConversationPhase::InformationGathering,
            DialogueMessageType::Suggestion | DialogueMessageType::Proposal => {
                ConversationPhase::ProblemSolving
            }
            DialogueMessageType::Agreement | DialogueMessageType::Disagreement => {
                ConversationPhase::DecisionMaking
            }
            DialogueMessageType::StatusUpdate => ConversationPhase::Planning,
            DialogueMessageType::Acknowledgment => ConversationPhase::Closing,
            _ => conversation.conversation_state.phase.clone(),
        };

        // Update engagement levels
        let engagement_boost = match message_type {
            DialogueMessageType::Question | DialogueMessageType::Suggestion => 0.2,
            DialogueMessageType::Information | DialogueMessageType::StatusUpdate => 0.1,
            _ => 0.05,
        };

        conversation
            .conversation_state
            .engagement_levels
            .entry(speaker_id.to_string())
            .and_modify(|e| *e = (*e + engagement_boost).min(1.0))
            .or_insert(0.5 + engagement_boost);
    }

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:update_agent_profile_with_topic (lines 558-635) ---[0m
    async fn update_agent_profile_with_topic(
        &mut self,
        agent_id: &str,
        entry: &DialogueEntry,
        conversation_topic: &str,
    ) -> Result<()> {
        let profile = self
            .agent_profiles
            .entry(agent_id.to_string())
            .or_insert_with(|| AgentDialogueProfile {
                agent_id: agent_id.to_string(),
                communication_style: CommunicationStyle {
                    formality_level: 0.5,
                    verbosity: 0.5,
                    directness: 0.5,
                    supportiveness: 0.5,
                    inquisitiveness: 0.5,
                    patience_level: 0.5,
                },
                response_patterns: ResponsePatterns {
                    average_response_time: Duration::from_secs(120),
                    preferred_message_length: MessageLength::Medium,
                    question_asking_frequency: 0.3,
                    agreement_tendency: 0.6,
                    detail_providing_level: 0.5,
                    follow_up_likelihood: 0.4,
                },
                topic_expertise: HashMap::new(),
                collaboration_preferences: CollaborationPreferences {
                    prefers_async: false,
                    comfortable_with_interruptions: true,
                    likes_consensus_building: true,
                    prefers_structured_discussions: false,
                    open_to_peer_review: true,
                },
                conversation_history: ConversationHistory {
                    total_conversations: 0,
                    successful_collaborations: 0,
                    preferred_conversation_types: Vec::new(),
                    effective_partnerships: HashMap::new(),
                    conversation_outcomes: HashMap::new(),
                },
            });

        // Update communication style based on message characteristics
        let message_length = entry.content.len();
        if message_length < 50 {
            profile.communication_style.verbosity =
                (profile.communication_style.verbosity * 0.9 + 0.2 * 0.1).max(0.0);
        } else if message_length > 200 {
            profile.communication_style.verbosity =
                (profile.communication_style.verbosity * 0.9 + 0.8 * 0.1).min(1.0);
        }

        // Update directness based on message type
        match entry.message_type {
            DialogueMessageType::Request | DialogueMessageType::Suggestion => {
                profile.communication_style.directness =
                    (profile.communication_style.directness * 0.9 + 0.7 * 0.1).min(1.0);
            }
            DialogueMessageType::Question => {
                profile.communication_style.inquisitiveness =
                    (profile.communication_style.inquisitiveness * 0.9 + 0.8 * 0.1).min(1.0);
            }
            _ => {}
        }

        // Update topic expertise
        profile
            .topic_expertise
            .entry(conversation_topic.to_string())
            .and_modify(|e| *e = (*e + 0.1).min(1.0))
            .or_insert(0.6);

        profile.conversation_history.total_conversations += 1;

        Ok(())
    }

  ./crates/ccswarm/src/coordination/dialogue.rs:272-325 method start_conversation <-> ./crates/ccswarm/src/coordination/dialogue.rs:558-635 method update_agent_profile_with_topic
  Similarity: 93.83%
  Classes: DialogueCoordinationBus <-> DialogueCoordinationBus

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:start_conversation (lines 272-325) ---[0m
    pub async fn start_conversation(
        &mut self,
        participants: Vec<String>,
        topic: String,
        conversation_type: ConversationType,
        context: ConversationContext,
    ) -> Result<String> {
        let conversation_id = Uuid::new_v4().to_string();
        let now = Utc::now();

        let conversation = Conversation {
            id: conversation_id.clone(),
            participants: participants.clone(),
            topic,
            context,
            dialogue_history: VecDeque::new(),
            conversation_state: ConversationState {
                phase: ConversationPhase::Opening,
                turn_order: participants.into_iter().collect(),
                current_speaker: None,
                waiting_for_response: Vec::new(),
                consensus_level: 0.0,
                engagement_levels: HashMap::new(),
                progress_indicators: HashMap::new(),
            },
            started_at: now,
            last_activity: now,
            conversation_type,
        };

        // Store conversation data for notifications
        let participants_clone = conversation.participants.clone();
        let topic_clone = conversation.topic.clone();

        self.conversations
            .insert(conversation_id.clone(), conversation);

        // Notify all participants
        for participant in &participants_clone {
            let message = AgentMessage::Coordination {
                from_agent: "system".to_string(),
                to_agent: participant.clone(),
                message_type: CoordinationType::Custom("conversation_started".to_string()),
                payload: serde_json::json!({
                    "conversation_id": conversation_id,
                    "topic": topic_clone,
                    "participants": participants_clone,
                }),
            };
            self.coordination_bus.send_message(message).await?;
        }

        Ok(conversation_id)
    }

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:update_agent_profile_with_topic (lines 558-635) ---[0m
    async fn update_agent_profile_with_topic(
        &mut self,
        agent_id: &str,
        entry: &DialogueEntry,
        conversation_topic: &str,
    ) -> Result<()> {
        let profile = self
            .agent_profiles
            .entry(agent_id.to_string())
            .or_insert_with(|| AgentDialogueProfile {
                agent_id: agent_id.to_string(),
                communication_style: CommunicationStyle {
                    formality_level: 0.5,
                    verbosity: 0.5,
                    directness: 0.5,
                    supportiveness: 0.5,
                    inquisitiveness: 0.5,
                    patience_level: 0.5,
                },
                response_patterns: ResponsePatterns {
                    average_response_time: Duration::from_secs(120),
                    preferred_message_length: MessageLength::Medium,
                    question_asking_frequency: 0.3,
                    agreement_tendency: 0.6,
                    detail_providing_level: 0.5,
                    follow_up_likelihood: 0.4,
                },
                topic_expertise: HashMap::new(),
                collaboration_preferences: CollaborationPreferences {
                    prefers_async: false,
                    comfortable_with_interruptions: true,
                    likes_consensus_building: true,
                    prefers_structured_discussions: false,
                    open_to_peer_review: true,
                },
                conversation_history: ConversationHistory {
                    total_conversations: 0,
                    successful_collaborations: 0,
                    preferred_conversation_types: Vec::new(),
                    effective_partnerships: HashMap::new(),
                    conversation_outcomes: HashMap::new(),
                },
            });

        // Update communication style based on message characteristics
        let message_length = entry.content.len();
        if message_length < 50 {
            profile.communication_style.verbosity =
                (profile.communication_style.verbosity * 0.9 + 0.2 * 0.1).max(0.0);
        } else if message_length > 200 {
            profile.communication_style.verbosity =
                (profile.communication_style.verbosity * 0.9 + 0.8 * 0.1).min(1.0);
        }

        // Update directness based on message type
        match entry.message_type {
            DialogueMessageType::Request | DialogueMessageType::Suggestion => {
                profile.communication_style.directness =
                    (profile.communication_style.directness * 0.9 + 0.7 * 0.1).min(1.0);
            }
            DialogueMessageType::Question => {
                profile.communication_style.inquisitiveness =
                    (profile.communication_style.inquisitiveness * 0.9 + 0.8 * 0.1).min(1.0);
            }
            _ => {}
        }

        // Update topic expertise
        profile
            .topic_expertise
            .entry(conversation_topic.to_string())
            .and_modify(|e| *e = (*e + 0.1).min(1.0))
            .or_insert(0.6);

        profile.conversation_history.total_conversations += 1;

        Ok(())
    }

  ./crates/ccswarm/src/coordination/dialogue.rs:558-635 method update_agent_profile_with_topic <-> ./crates/ccswarm/src/coordination/dialogue.rs:734-775 function test_dialogue_coordination_bus
  Similarity: 91.20%

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:update_agent_profile_with_topic (lines 558-635) ---[0m
    async fn update_agent_profile_with_topic(
        &mut self,
        agent_id: &str,
        entry: &DialogueEntry,
        conversation_topic: &str,
    ) -> Result<()> {
        let profile = self
            .agent_profiles
            .entry(agent_id.to_string())
            .or_insert_with(|| AgentDialogueProfile {
                agent_id: agent_id.to_string(),
                communication_style: CommunicationStyle {
                    formality_level: 0.5,
                    verbosity: 0.5,
                    directness: 0.5,
                    supportiveness: 0.5,
                    inquisitiveness: 0.5,
                    patience_level: 0.5,
                },
                response_patterns: ResponsePatterns {
                    average_response_time: Duration::from_secs(120),
                    preferred_message_length: MessageLength::Medium,
                    question_asking_frequency: 0.3,
                    agreement_tendency: 0.6,
                    detail_providing_level: 0.5,
                    follow_up_likelihood: 0.4,
                },
                topic_expertise: HashMap::new(),
                collaboration_preferences: CollaborationPreferences {
                    prefers_async: false,
                    comfortable_with_interruptions: true,
                    likes_consensus_building: true,
                    prefers_structured_discussions: false,
                    open_to_peer_review: true,
                },
                conversation_history: ConversationHistory {
                    total_conversations: 0,
                    successful_collaborations: 0,
                    preferred_conversation_types: Vec::new(),
                    effective_partnerships: HashMap::new(),
                    conversation_outcomes: HashMap::new(),
                },
            });

        // Update communication style based on message characteristics
        let message_length = entry.content.len();
        if message_length < 50 {
            profile.communication_style.verbosity =
                (profile.communication_style.verbosity * 0.9 + 0.2 * 0.1).max(0.0);
        } else if message_length > 200 {
            profile.communication_style.verbosity =
                (profile.communication_style.verbosity * 0.9 + 0.8 * 0.1).min(1.0);
        }

        // Update directness based on message type
        match entry.message_type {
            DialogueMessageType::Request | DialogueMessageType::Suggestion => {
                profile.communication_style.directness =
                    (profile.communication_style.directness * 0.9 + 0.7 * 0.1).min(1.0);
            }
            DialogueMessageType::Question => {
                profile.communication_style.inquisitiveness =
                    (profile.communication_style.inquisitiveness * 0.9 + 0.8 * 0.1).min(1.0);
            }
            _ => {}
        }

        // Update topic expertise
        profile
            .topic_expertise
            .entry(conversation_topic.to_string())
            .and_modify(|e| *e = (*e + 0.1).min(1.0))
            .or_insert(0.6);

        profile.conversation_history.total_conversations += 1;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:test_dialogue_coordination_bus (lines 734-775) ---[0m
    async fn test_dialogue_coordination_bus() {
        let mut bus = DialogueCoordinationBus::new().await.unwrap();

        let participants = vec!["agent1".to_string(), "agent2".to_string()];
        let context = ConversationContext {
            project_context: Some("Test project".to_string()),
            task_context: None,
            shared_resources: Vec::new(),
            constraints: Vec::new(),
            goals: vec!["Complete testing".to_string()],
            related_conversations: Vec::new(),
        };

        let conversation_id = bus
            .start_conversation(
                participants,
                "Test coordination".to_string(),
                ConversationType::TaskCoordination,
                context,
            )
            .await
            .unwrap();

        assert!(!conversation_id.is_empty());
        assert!(bus.conversations.contains_key(&conversation_id));

        // Add a dialogue message
        bus.add_dialogue_message(
            &conversation_id,
            "agent1".to_string(),
            "Hello, let's coordinate on this task".to_string(),
            DialogueMessageType::Information,
            EmotionalTone::Positive,
            ResponseExpectation::Acknowledgment,
        )
        .await
        .unwrap();

        let summary = bus.get_conversation_summary(&conversation_id).unwrap();
        assert_eq!(summary.message_count, 1);
        assert_eq!(summary.participants.len(), 2);
    }

  ./crates/ccswarm/src/coordination/dialogue.rs:272-325 method start_conversation <-> ./crates/ccswarm/src/coordination/dialogue.rs:439-493 method update_conversation_state_static
  Similarity: 89.36%
  Classes: DialogueCoordinationBus <-> DialogueCoordinationBus

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:start_conversation (lines 272-325) ---[0m
    pub async fn start_conversation(
        &mut self,
        participants: Vec<String>,
        topic: String,
        conversation_type: ConversationType,
        context: ConversationContext,
    ) -> Result<String> {
        let conversation_id = Uuid::new_v4().to_string();
        let now = Utc::now();

        let conversation = Conversation {
            id: conversation_id.clone(),
            participants: participants.clone(),
            topic,
            context,
            dialogue_history: VecDeque::new(),
            conversation_state: ConversationState {
                phase: ConversationPhase::Opening,
                turn_order: participants.into_iter().collect(),
                current_speaker: None,
                waiting_for_response: Vec::new(),
                consensus_level: 0.0,
                engagement_levels: HashMap::new(),
                progress_indicators: HashMap::new(),
            },
            started_at: now,
            last_activity: now,
            conversation_type,
        };

        // Store conversation data for notifications
        let participants_clone = conversation.participants.clone();
        let topic_clone = conversation.topic.clone();

        self.conversations
            .insert(conversation_id.clone(), conversation);

        // Notify all participants
        for participant in &participants_clone {
            let message = AgentMessage::Coordination {
                from_agent: "system".to_string(),
                to_agent: participant.clone(),
                message_type: CoordinationType::Custom("conversation_started".to_string()),
                payload: serde_json::json!({
                    "conversation_id": conversation_id,
                    "topic": topic_clone,
                    "participants": participants_clone,
                }),
            };
            self.coordination_bus.send_message(message).await?;
        }

        Ok(conversation_id)
    }

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:update_conversation_state_static (lines 439-493) ---[0m
    fn update_conversation_state_static(
        &mut self,
        conversation: &mut Conversation,
        speaker_id: &str,
        message_type: &DialogueMessageType,
    ) {
        // Update current speaker
        conversation.conversation_state.current_speaker = Some(speaker_id.to_string());

        // Update turn order
        if let Some(pos) = conversation
            .conversation_state
            .turn_order
            .iter()
            .position(|id| id == speaker_id)
        {
            let speaker = conversation
                .conversation_state
                .turn_order
                .remove(pos)
                .unwrap();
            conversation
                .conversation_state
                .turn_order
                .push_front(speaker);
        }

        // Update conversation phase based on message type
        conversation.conversation_state.phase = match message_type {
            DialogueMessageType::Question => ConversationPhase::InformationGathering,
            DialogueMessageType::Suggestion | DialogueMessageType::Proposal => {
                ConversationPhase::ProblemSolving
            }
            DialogueMessageType::Agreement | DialogueMessageType::Disagreement => {
                ConversationPhase::DecisionMaking
            }
            DialogueMessageType::StatusUpdate => ConversationPhase::Planning,
            DialogueMessageType::Acknowledgment => ConversationPhase::Closing,
            _ => conversation.conversation_state.phase.clone(),
        };

        // Update engagement levels
        let engagement_boost = match message_type {
            DialogueMessageType::Question | DialogueMessageType::Suggestion => 0.2,
            DialogueMessageType::Information | DialogueMessageType::StatusUpdate => 0.1,
            _ => 0.05,
        };

        conversation
            .conversation_state
            .engagement_levels
            .entry(speaker_id.to_string())
            .and_modify(|e| *e = (*e + engagement_boost).min(1.0))
            .or_insert(0.5 + engagement_boost);
    }

  ./crates/ccswarm/src/coordination/dialogue.rs:272-325 method start_conversation <-> ./crates/ccswarm/src/coordination/dialogue.rs:734-775 function test_dialogue_coordination_bus
  Similarity: 87.63%

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:start_conversation (lines 272-325) ---[0m
    pub async fn start_conversation(
        &mut self,
        participants: Vec<String>,
        topic: String,
        conversation_type: ConversationType,
        context: ConversationContext,
    ) -> Result<String> {
        let conversation_id = Uuid::new_v4().to_string();
        let now = Utc::now();

        let conversation = Conversation {
            id: conversation_id.clone(),
            participants: participants.clone(),
            topic,
            context,
            dialogue_history: VecDeque::new(),
            conversation_state: ConversationState {
                phase: ConversationPhase::Opening,
                turn_order: participants.into_iter().collect(),
                current_speaker: None,
                waiting_for_response: Vec::new(),
                consensus_level: 0.0,
                engagement_levels: HashMap::new(),
                progress_indicators: HashMap::new(),
            },
            started_at: now,
            last_activity: now,
            conversation_type,
        };

        // Store conversation data for notifications
        let participants_clone = conversation.participants.clone();
        let topic_clone = conversation.topic.clone();

        self.conversations
            .insert(conversation_id.clone(), conversation);

        // Notify all participants
        for participant in &participants_clone {
            let message = AgentMessage::Coordination {
                from_agent: "system".to_string(),
                to_agent: participant.clone(),
                message_type: CoordinationType::Custom("conversation_started".to_string()),
                payload: serde_json::json!({
                    "conversation_id": conversation_id,
                    "topic": topic_clone,
                    "participants": participants_clone,
                }),
            };
            self.coordination_bus.send_message(message).await?;
        }

        Ok(conversation_id)
    }

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:test_dialogue_coordination_bus (lines 734-775) ---[0m
    async fn test_dialogue_coordination_bus() {
        let mut bus = DialogueCoordinationBus::new().await.unwrap();

        let participants = vec!["agent1".to_string(), "agent2".to_string()];
        let context = ConversationContext {
            project_context: Some("Test project".to_string()),
            task_context: None,
            shared_resources: Vec::new(),
            constraints: Vec::new(),
            goals: vec!["Complete testing".to_string()],
            related_conversations: Vec::new(),
        };

        let conversation_id = bus
            .start_conversation(
                participants,
                "Test coordination".to_string(),
                ConversationType::TaskCoordination,
                context,
            )
            .await
            .unwrap();

        assert!(!conversation_id.is_empty());
        assert!(bus.conversations.contains_key(&conversation_id));

        // Add a dialogue message
        bus.add_dialogue_message(
            &conversation_id,
            "agent1".to_string(),
            "Hello, let's coordinate on this task".to_string(),
            DialogueMessageType::Information,
            EmotionalTone::Positive,
            ResponseExpectation::Acknowledgment,
        )
        .await
        .unwrap();

        let summary = bus.get_conversation_summary(&conversation_id).unwrap();
        assert_eq!(summary.message_count, 1);
        assert_eq!(summary.participants.len(), 2);
    }

  ./crates/ccswarm/src/coordination/dialogue.rs:524-555 method extract_dialogue_pattern <-> ./crates/ccswarm/src/coordination/dialogue.rs:558-635 method update_agent_profile_with_topic
  Similarity: 72.50%
  Classes: DialogueCoordinationBus <-> DialogueCoordinationBus

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:extract_dialogue_pattern (lines 524-555) ---[0m
    fn extract_dialogue_pattern(&self, conversation: &Conversation) -> DialoguePattern {
        let recent_messages: Vec<_> = conversation.dialogue_history.iter().rev().take(5).collect();

        let flow_steps = recent_messages
            .iter()
            .map(|entry| ConversationFlowStep {
                step_type: match entry.message_type {
                    DialogueMessageType::Question => FlowStepType::InformationExchange,
                    DialogueMessageType::Suggestion => FlowStepType::SolutionBrainstorming,
                    DialogueMessageType::Agreement => FlowStepType::DecisionPoint,
                    DialogueMessageType::StatusUpdate => FlowStepType::ProgressCheck,
                    _ => FlowStepType::InformationExchange,
                },
                typical_messages: vec![entry.content.clone()],
                expected_responses: Vec::new(),
                transition_conditions: Vec::new(),
            })
            .collect();

        DialoguePattern {
            pattern_id: Uuid::new_v4().to_string(),
            pattern_name: format!("{:?}_pattern", conversation.conversation_type),
            trigger_conditions: vec![conversation.topic.clone()],
            conversation_flow: flow_steps,
            success_rate: 0.8, // Default success rate
            participants_count: conversation.participants.len(),
            typical_duration: Duration::from_secs(
                (Utc::now() - conversation.started_at).num_seconds() as u64,
            ),
            effectiveness_metrics: HashMap::new(),
        }
    }

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:update_agent_profile_with_topic (lines 558-635) ---[0m
    async fn update_agent_profile_with_topic(
        &mut self,
        agent_id: &str,
        entry: &DialogueEntry,
        conversation_topic: &str,
    ) -> Result<()> {
        let profile = self
            .agent_profiles
            .entry(agent_id.to_string())
            .or_insert_with(|| AgentDialogueProfile {
                agent_id: agent_id.to_string(),
                communication_style: CommunicationStyle {
                    formality_level: 0.5,
                    verbosity: 0.5,
                    directness: 0.5,
                    supportiveness: 0.5,
                    inquisitiveness: 0.5,
                    patience_level: 0.5,
                },
                response_patterns: ResponsePatterns {
                    average_response_time: Duration::from_secs(120),
                    preferred_message_length: MessageLength::Medium,
                    question_asking_frequency: 0.3,
                    agreement_tendency: 0.6,
                    detail_providing_level: 0.5,
                    follow_up_likelihood: 0.4,
                },
                topic_expertise: HashMap::new(),
                collaboration_preferences: CollaborationPreferences {
                    prefers_async: false,
                    comfortable_with_interruptions: true,
                    likes_consensus_building: true,
                    prefers_structured_discussions: false,
                    open_to_peer_review: true,
                },
                conversation_history: ConversationHistory {
                    total_conversations: 0,
                    successful_collaborations: 0,
                    preferred_conversation_types: Vec::new(),
                    effective_partnerships: HashMap::new(),
                    conversation_outcomes: HashMap::new(),
                },
            });

        // Update communication style based on message characteristics
        let message_length = entry.content.len();
        if message_length < 50 {
            profile.communication_style.verbosity =
                (profile.communication_style.verbosity * 0.9 + 0.2 * 0.1).max(0.0);
        } else if message_length > 200 {
            profile.communication_style.verbosity =
                (profile.communication_style.verbosity * 0.9 + 0.8 * 0.1).min(1.0);
        }

        // Update directness based on message type
        match entry.message_type {
            DialogueMessageType::Request | DialogueMessageType::Suggestion => {
                profile.communication_style.directness =
                    (profile.communication_style.directness * 0.9 + 0.7 * 0.1).min(1.0);
            }
            DialogueMessageType::Question => {
                profile.communication_style.inquisitiveness =
                    (profile.communication_style.inquisitiveness * 0.9 + 0.8 * 0.1).min(1.0);
            }
            _ => {}
        }

        // Update topic expertise
        profile
            .topic_expertise
            .entry(conversation_topic.to_string())
            .and_modify(|e| *e = (*e + 0.1).min(1.0))
            .or_insert(0.6);

        profile.conversation_history.total_conversations += 1;

        Ok(())
    }

  ./crates/ccswarm/src/coordination/dialogue.rs:439-493 method update_conversation_state_static <-> ./crates/ccswarm/src/coordination/dialogue.rs:734-775 function test_dialogue_coordination_bus
  Similarity: 81.78%

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:update_conversation_state_static (lines 439-493) ---[0m
    fn update_conversation_state_static(
        &mut self,
        conversation: &mut Conversation,
        speaker_id: &str,
        message_type: &DialogueMessageType,
    ) {
        // Update current speaker
        conversation.conversation_state.current_speaker = Some(speaker_id.to_string());

        // Update turn order
        if let Some(pos) = conversation
            .conversation_state
            .turn_order
            .iter()
            .position(|id| id == speaker_id)
        {
            let speaker = conversation
                .conversation_state
                .turn_order
                .remove(pos)
                .unwrap();
            conversation
                .conversation_state
                .turn_order
                .push_front(speaker);
        }

        // Update conversation phase based on message type
        conversation.conversation_state.phase = match message_type {
            DialogueMessageType::Question => ConversationPhase::InformationGathering,
            DialogueMessageType::Suggestion | DialogueMessageType::Proposal => {
                ConversationPhase::ProblemSolving
            }
            DialogueMessageType::Agreement | DialogueMessageType::Disagreement => {
                ConversationPhase::DecisionMaking
            }
            DialogueMessageType::StatusUpdate => ConversationPhase::Planning,
            DialogueMessageType::Acknowledgment => ConversationPhase::Closing,
            _ => conversation.conversation_state.phase.clone(),
        };

        // Update engagement levels
        let engagement_boost = match message_type {
            DialogueMessageType::Question | DialogueMessageType::Suggestion => 0.2,
            DialogueMessageType::Information | DialogueMessageType::StatusUpdate => 0.1,
            _ => 0.05,
        };

        conversation
            .conversation_state
            .engagement_levels
            .entry(speaker_id.to_string())
            .and_modify(|e| *e = (*e + engagement_boost).min(1.0))
            .or_insert(0.5 + engagement_boost);
    }

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:test_dialogue_coordination_bus (lines 734-775) ---[0m
    async fn test_dialogue_coordination_bus() {
        let mut bus = DialogueCoordinationBus::new().await.unwrap();

        let participants = vec!["agent1".to_string(), "agent2".to_string()];
        let context = ConversationContext {
            project_context: Some("Test project".to_string()),
            task_context: None,
            shared_resources: Vec::new(),
            constraints: Vec::new(),
            goals: vec!["Complete testing".to_string()],
            related_conversations: Vec::new(),
        };

        let conversation_id = bus
            .start_conversation(
                participants,
                "Test coordination".to_string(),
                ConversationType::TaskCoordination,
                context,
            )
            .await
            .unwrap();

        assert!(!conversation_id.is_empty());
        assert!(bus.conversations.contains_key(&conversation_id));

        // Add a dialogue message
        bus.add_dialogue_message(
            &conversation_id,
            "agent1".to_string(),
            "Hello, let's coordinate on this task".to_string(),
            DialogueMessageType::Information,
            EmotionalTone::Positive,
            ResponseExpectation::Acknowledgment,
        )
        .await
        .unwrap();

        let summary = bus.get_conversation_summary(&conversation_id).unwrap();
        assert_eq!(summary.message_count, 1);
        assert_eq!(summary.participants.len(), 2);
    }

  ./crates/ccswarm/src/coordination/dialogue.rs:439-493 method update_conversation_state_static <-> ./crates/ccswarm/src/coordination/dialogue.rs:496-521 method learn_dialogue_patterns
  Similarity: 88.48%
  Classes: DialogueCoordinationBus <-> DialogueCoordinationBus

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:update_conversation_state_static (lines 439-493) ---[0m
    fn update_conversation_state_static(
        &mut self,
        conversation: &mut Conversation,
        speaker_id: &str,
        message_type: &DialogueMessageType,
    ) {
        // Update current speaker
        conversation.conversation_state.current_speaker = Some(speaker_id.to_string());

        // Update turn order
        if let Some(pos) = conversation
            .conversation_state
            .turn_order
            .iter()
            .position(|id| id == speaker_id)
        {
            let speaker = conversation
                .conversation_state
                .turn_order
                .remove(pos)
                .unwrap();
            conversation
                .conversation_state
                .turn_order
                .push_front(speaker);
        }

        // Update conversation phase based on message type
        conversation.conversation_state.phase = match message_type {
            DialogueMessageType::Question => ConversationPhase::InformationGathering,
            DialogueMessageType::Suggestion | DialogueMessageType::Proposal => {
                ConversationPhase::ProblemSolving
            }
            DialogueMessageType::Agreement | DialogueMessageType::Disagreement => {
                ConversationPhase::DecisionMaking
            }
            DialogueMessageType::StatusUpdate => ConversationPhase::Planning,
            DialogueMessageType::Acknowledgment => ConversationPhase::Closing,
            _ => conversation.conversation_state.phase.clone(),
        };

        // Update engagement levels
        let engagement_boost = match message_type {
            DialogueMessageType::Question | DialogueMessageType::Suggestion => 0.2,
            DialogueMessageType::Information | DialogueMessageType::StatusUpdate => 0.1,
            _ => 0.05,
        };

        conversation
            .conversation_state
            .engagement_levels
            .entry(speaker_id.to_string())
            .and_modify(|e| *e = (*e + engagement_boost).min(1.0))
            .or_insert(0.5 + engagement_boost);
    }

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:learn_dialogue_patterns (lines 496-521) ---[0m
    async fn learn_dialogue_patterns(&mut self, conversation_id: &str) -> Result<()> {
        let conversation = self
            .conversations
            .get(conversation_id)
            .ok_or_else(|| anyhow::anyhow!("Conversation not found"))?;

        // Analyze conversation flow
        if conversation.dialogue_history.len() >= 5 {
            let pattern = self.extract_dialogue_pattern(conversation);
            self.dialogue_patterns.push(pattern);
        }

        // Update agent profiles - collect data first to avoid borrow conflicts
        let mut profile_updates = Vec::new();
        for entry in &conversation.dialogue_history {
            profile_updates.push((entry.speaker_id.clone(), entry.clone()));
        }
        let conversation_topic = conversation.topic.clone();

        for (speaker_id, entry) in profile_updates {
            self.update_agent_profile_with_topic(&speaker_id, &entry, &conversation_topic)
                .await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/coordination/dialogue.rs:272-325 method start_conversation <-> ./crates/ccswarm/src/coordination/dialogue.rs:496-521 method learn_dialogue_patterns
  Similarity: 86.62%
  Classes: DialogueCoordinationBus <-> DialogueCoordinationBus

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:start_conversation (lines 272-325) ---[0m
    pub async fn start_conversation(
        &mut self,
        participants: Vec<String>,
        topic: String,
        conversation_type: ConversationType,
        context: ConversationContext,
    ) -> Result<String> {
        let conversation_id = Uuid::new_v4().to_string();
        let now = Utc::now();

        let conversation = Conversation {
            id: conversation_id.clone(),
            participants: participants.clone(),
            topic,
            context,
            dialogue_history: VecDeque::new(),
            conversation_state: ConversationState {
                phase: ConversationPhase::Opening,
                turn_order: participants.into_iter().collect(),
                current_speaker: None,
                waiting_for_response: Vec::new(),
                consensus_level: 0.0,
                engagement_levels: HashMap::new(),
                progress_indicators: HashMap::new(),
            },
            started_at: now,
            last_activity: now,
            conversation_type,
        };

        // Store conversation data for notifications
        let participants_clone = conversation.participants.clone();
        let topic_clone = conversation.topic.clone();

        self.conversations
            .insert(conversation_id.clone(), conversation);

        // Notify all participants
        for participant in &participants_clone {
            let message = AgentMessage::Coordination {
                from_agent: "system".to_string(),
                to_agent: participant.clone(),
                message_type: CoordinationType::Custom("conversation_started".to_string()),
                payload: serde_json::json!({
                    "conversation_id": conversation_id,
                    "topic": topic_clone,
                    "participants": participants_clone,
                }),
            };
            self.coordination_bus.send_message(message).await?;
        }

        Ok(conversation_id)
    }

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:learn_dialogue_patterns (lines 496-521) ---[0m
    async fn learn_dialogue_patterns(&mut self, conversation_id: &str) -> Result<()> {
        let conversation = self
            .conversations
            .get(conversation_id)
            .ok_or_else(|| anyhow::anyhow!("Conversation not found"))?;

        // Analyze conversation flow
        if conversation.dialogue_history.len() >= 5 {
            let pattern = self.extract_dialogue_pattern(conversation);
            self.dialogue_patterns.push(pattern);
        }

        // Update agent profiles - collect data first to avoid borrow conflicts
        let mut profile_updates = Vec::new();
        for entry in &conversation.dialogue_history {
            profile_updates.push((entry.speaker_id.clone(), entry.clone()));
        }
        let conversation_topic = conversation.topic.clone();

        for (speaker_id, entry) in profile_updates {
            self.update_agent_profile_with_topic(&speaker_id, &entry, &conversation_topic)
                .await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/coordination/dialogue.rs:272-325 method start_conversation <-> ./crates/ccswarm/src/coordination/dialogue.rs:669-701 method get_agent_dialogue_insights
  Similarity: 71.52%
  Classes: DialogueCoordinationBus <-> DialogueCoordinationBus

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:start_conversation (lines 272-325) ---[0m
    pub async fn start_conversation(
        &mut self,
        participants: Vec<String>,
        topic: String,
        conversation_type: ConversationType,
        context: ConversationContext,
    ) -> Result<String> {
        let conversation_id = Uuid::new_v4().to_string();
        let now = Utc::now();

        let conversation = Conversation {
            id: conversation_id.clone(),
            participants: participants.clone(),
            topic,
            context,
            dialogue_history: VecDeque::new(),
            conversation_state: ConversationState {
                phase: ConversationPhase::Opening,
                turn_order: participants.into_iter().collect(),
                current_speaker: None,
                waiting_for_response: Vec::new(),
                consensus_level: 0.0,
                engagement_levels: HashMap::new(),
                progress_indicators: HashMap::new(),
            },
            started_at: now,
            last_activity: now,
            conversation_type,
        };

        // Store conversation data for notifications
        let participants_clone = conversation.participants.clone();
        let topic_clone = conversation.topic.clone();

        self.conversations
            .insert(conversation_id.clone(), conversation);

        // Notify all participants
        for participant in &participants_clone {
            let message = AgentMessage::Coordination {
                from_agent: "system".to_string(),
                to_agent: participant.clone(),
                message_type: CoordinationType::Custom("conversation_started".to_string()),
                payload: serde_json::json!({
                    "conversation_id": conversation_id,
                    "topic": topic_clone,
                    "participants": participants_clone,
                }),
            };
            self.coordination_bus.send_message(message).await?;
        }

        Ok(conversation_id)
    }

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:get_agent_dialogue_insights (lines 669-701) ---[0m
    pub fn get_agent_dialogue_insights(&self, agent_id: &str) -> Option<AgentDialogueInsights> {
        let profile = self.agent_profiles.get(agent_id)?;

        Some(AgentDialogueInsights {
            agent_id: agent_id.to_string(),
            communication_style: profile.communication_style.clone(),
            total_conversations: profile.conversation_history.total_conversations,
            success_rate: if profile.conversation_history.total_conversations > 0 {
                profile.conversation_history.successful_collaborations as f32
                    / profile.conversation_history.total_conversations as f32
            } else {
                0.0
            },
            top_expertise_areas: profile
                .topic_expertise
                .iter()
                .map(|(topic, expertise)| (topic.clone(), *expertise))
                .collect::<Vec<_>>()
                .into_iter()
                .fold(Vec::new(), |mut acc, (topic, expertise)| {
                    acc.push((topic, expertise));
                    acc.sort_by(|a, b| b.1.partial_cmp(&a.1).unwrap());
                    acc.truncate(5);
                    acc
                }),
            preferred_partners: profile
                .conversation_history
                .effective_partnerships
                .iter()
                .map(|(partner, effectiveness)| (partner.clone(), *effectiveness))
                .collect(),
        })
    }

  ./crates/ccswarm/src/coordination/dialogue.rs:496-521 method learn_dialogue_patterns <-> ./crates/ccswarm/src/coordination/dialogue.rs:734-775 function test_dialogue_coordination_bus
  Similarity: 76.10%

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:learn_dialogue_patterns (lines 496-521) ---[0m
    async fn learn_dialogue_patterns(&mut self, conversation_id: &str) -> Result<()> {
        let conversation = self
            .conversations
            .get(conversation_id)
            .ok_or_else(|| anyhow::anyhow!("Conversation not found"))?;

        // Analyze conversation flow
        if conversation.dialogue_history.len() >= 5 {
            let pattern = self.extract_dialogue_pattern(conversation);
            self.dialogue_patterns.push(pattern);
        }

        // Update agent profiles - collect data first to avoid borrow conflicts
        let mut profile_updates = Vec::new();
        for entry in &conversation.dialogue_history {
            profile_updates.push((entry.speaker_id.clone(), entry.clone()));
        }
        let conversation_topic = conversation.topic.clone();

        for (speaker_id, entry) in profile_updates {
            self.update_agent_profile_with_topic(&speaker_id, &entry, &conversation_topic)
                .await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:test_dialogue_coordination_bus (lines 734-775) ---[0m
    async fn test_dialogue_coordination_bus() {
        let mut bus = DialogueCoordinationBus::new().await.unwrap();

        let participants = vec!["agent1".to_string(), "agent2".to_string()];
        let context = ConversationContext {
            project_context: Some("Test project".to_string()),
            task_context: None,
            shared_resources: Vec::new(),
            constraints: Vec::new(),
            goals: vec!["Complete testing".to_string()],
            related_conversations: Vec::new(),
        };

        let conversation_id = bus
            .start_conversation(
                participants,
                "Test coordination".to_string(),
                ConversationType::TaskCoordination,
                context,
            )
            .await
            .unwrap();

        assert!(!conversation_id.is_empty());
        assert!(bus.conversations.contains_key(&conversation_id));

        // Add a dialogue message
        bus.add_dialogue_message(
            &conversation_id,
            "agent1".to_string(),
            "Hello, let's coordinate on this task".to_string(),
            DialogueMessageType::Information,
            EmotionalTone::Positive,
            ResponseExpectation::Acknowledgment,
        )
        .await
        .unwrap();

        let summary = bus.get_conversation_summary(&conversation_id).unwrap();
        assert_eq!(summary.message_count, 1);
        assert_eq!(summary.participants.len(), 2);
    }

  ./crates/ccswarm/src/coordination/dialogue.rs:638-654 method get_conversation_summary <-> ./crates/ccswarm/src/coordination/dialogue.rs:669-701 method get_agent_dialogue_insights
  Similarity: 98.58%
  Classes: DialogueCoordinationBus <-> DialogueCoordinationBus

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:get_conversation_summary (lines 638-654) ---[0m
    pub fn get_conversation_summary(&self, conversation_id: &str) -> Option<ConversationSummary> {
        let conversation = self.conversations.get(conversation_id)?;

        Some(ConversationSummary {
            id: conversation.id.clone(),
            topic: conversation.topic.clone(),
            participants: conversation.participants.clone(),
            message_count: conversation.dialogue_history.len(),
            duration: (Utc::now() - conversation.started_at)
                .to_std()
                .unwrap_or(Duration::ZERO),
            current_phase: conversation.conversation_state.phase.clone(),
            consensus_level: conversation.conversation_state.consensus_level,
            last_activity: conversation.last_activity,
            conversation_type: conversation.conversation_type.clone(),
        })
    }

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:get_agent_dialogue_insights (lines 669-701) ---[0m
    pub fn get_agent_dialogue_insights(&self, agent_id: &str) -> Option<AgentDialogueInsights> {
        let profile = self.agent_profiles.get(agent_id)?;

        Some(AgentDialogueInsights {
            agent_id: agent_id.to_string(),
            communication_style: profile.communication_style.clone(),
            total_conversations: profile.conversation_history.total_conversations,
            success_rate: if profile.conversation_history.total_conversations > 0 {
                profile.conversation_history.successful_collaborations as f32
                    / profile.conversation_history.total_conversations as f32
            } else {
                0.0
            },
            top_expertise_areas: profile
                .topic_expertise
                .iter()
                .map(|(topic, expertise)| (topic.clone(), *expertise))
                .collect::<Vec<_>>()
                .into_iter()
                .fold(Vec::new(), |mut acc, (topic, expertise)| {
                    acc.push((topic, expertise));
                    acc.sort_by(|a, b| b.1.partial_cmp(&a.1).unwrap());
                    acc.truncate(5);
                    acc
                }),
            preferred_partners: profile
                .conversation_history
                .effective_partnerships
                .iter()
                .map(|(partner, effectiveness)| (partner.clone(), *effectiveness))
                .collect(),
        })
    }

  ./crates/ccswarm/src/coordination/dialogue.rs:262-269 method new <-> ./crates/ccswarm/src/coordination/dialogue.rs:657-666 method get_active_conversations
  Similarity: 96.67%
  Classes: DialogueCoordinationBus <-> DialogueCoordinationBus

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:new (lines 262-269) ---[0m
    pub async fn new() -> Result<Self> {
        Ok(Self {
            coordination_bus: CoordinationBus::new().await?,
            conversations: HashMap::new(),
            dialogue_patterns: Vec::new(),
            agent_profiles: HashMap::new(),
        })
    }

[36m--- ./crates/ccswarm/src/coordination/dialogue.rs:get_active_conversations (lines 657-666) ---[0m
    pub fn get_active_conversations(&self) -> Vec<ConversationSummary> {
        self.conversations
            .values()
            .filter(|conv| {
                let time_since_activity = Utc::now() - conv.last_activity;
                time_since_activity.num_hours() < 24 // Active if activity within 24 hours
            })
            .filter_map(|conv| self.get_conversation_summary(&conv.id))
            .collect()
    }


Duplicates in ./crates/ccswarm/src/mcp/transport.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/mcp/transport.rs:55-75 method send <-> ./crates/ccswarm/src/mcp/transport.rs:176-191 method pair
  Similarity: 76.41%
  Classes: Transport <-> InMemoryTransport

[36m--- ./crates/ccswarm/src/mcp/transport.rs:send (lines 55-75) ---[0m
    async fn send(&mut self, message: &JsonRpcMessage) -> Result<()> {
        let json_str = message.to_string()?;

        let response = self
            .client
            .post(&self.base_url)
            .header("Content-Type", "application/json")
            .body(json_str)
            .timeout(self.timeout)
            .send()
            .await?;

        if !response.status().is_success() {
            return Err(anyhow::anyhow!(
                "HTTP request failed with status: {}",
                response.status()
            ));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/transport.rs:pair (lines 176-191) ---[0m
    pub fn pair() -> (Self, Self) {
        let (tx1, rx1) = tokio::sync::mpsc::unbounded_channel();
        let (tx2, rx2) = tokio::sync::mpsc::unbounded_channel();

        let transport1 = Self {
            send_queue: tx2,
            recv_queue: rx1,
        };

        let transport2 = Self {
            send_queue: tx1,
            recv_queue: rx2,
        };

        (transport1, transport2)
    }

  ./crates/ccswarm/src/mcp/transport.rs:55-75 method send <-> ./crates/ccswarm/src/mcp/transport.rs:137-153 method receive
  Similarity: 71.45%
  Classes: Transport <-> Transport

[36m--- ./crates/ccswarm/src/mcp/transport.rs:send (lines 55-75) ---[0m
    async fn send(&mut self, message: &JsonRpcMessage) -> Result<()> {
        let json_str = message.to_string()?;

        let response = self
            .client
            .post(&self.base_url)
            .header("Content-Type", "application/json")
            .body(json_str)
            .timeout(self.timeout)
            .send()
            .await?;

        if !response.status().is_success() {
            return Err(anyhow::anyhow!(
                "HTTP request failed with status: {}",
                response.status()
            ));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/transport.rs:receive (lines 137-153) ---[0m
    async fn receive(&mut self) -> Result<Option<JsonRpcMessage>> {
        let reader = self
            .reader
            .as_mut()
            .ok_or_else(|| anyhow::anyhow!("Unix socket not connected"))?;

        let mut line = String::new();
        let bytes_read = reader.read_line(&mut line).await?;

        if bytes_read == 0 {
            // Connection closed
            return Ok(None);
        }

        let message = line.trim().parse::<JsonRpcMessage>()?;
        Ok(Some(message))
    }

  ./crates/ccswarm/src/mcp/transport.rs:122-135 method send <-> ./crates/ccswarm/src/mcp/transport.rs:137-153 method receive
  Similarity: 85.58%
  Classes: Transport <-> Transport

[36m--- ./crates/ccswarm/src/mcp/transport.rs:send (lines 122-135) ---[0m
    async fn send(&mut self, message: &JsonRpcMessage) -> Result<()> {
        let writer = self
            .writer
            .as_mut()
            .ok_or_else(|| anyhow::anyhow!("Unix socket not connected"))?;

        let json_str = message.to_string()?;
        let message_with_delimiter = format!("{}\n", json_str);

        writer.write_all(message_with_delimiter.as_bytes()).await?;
        writer.flush().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/transport.rs:receive (lines 137-153) ---[0m
    async fn receive(&mut self) -> Result<Option<JsonRpcMessage>> {
        let reader = self
            .reader
            .as_mut()
            .ok_or_else(|| anyhow::anyhow!("Unix socket not connected"))?;

        let mut line = String::new();
        let bytes_read = reader.read_line(&mut line).await?;

        if bytes_read == 0 {
            // Connection closed
            return Ok(None);
        }

        let message = line.trim().parse::<JsonRpcMessage>()?;
        Ok(Some(message))
    }

  ./crates/ccswarm/src/mcp/transport.rs:55-75 method send <-> ./crates/ccswarm/src/mcp/transport.rs:155-165 method close
  Similarity: 80.28%
  Classes: Transport <-> Transport

[36m--- ./crates/ccswarm/src/mcp/transport.rs:send (lines 55-75) ---[0m
    async fn send(&mut self, message: &JsonRpcMessage) -> Result<()> {
        let json_str = message.to_string()?;

        let response = self
            .client
            .post(&self.base_url)
            .header("Content-Type", "application/json")
            .body(json_str)
            .timeout(self.timeout)
            .send()
            .await?;

        if !response.status().is_success() {
            return Err(anyhow::anyhow!(
                "HTTP request failed with status: {}",
                response.status()
            ));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/transport.rs:close (lines 155-165) ---[0m
    async fn close(&mut self) -> Result<()> {
        if let Some(writer) = self.writer.take() {
            drop(writer);
        }
        if let Some(reader) = self.reader.take() {
            drop(reader);
        }
        self.stream = None;

        Ok(())
    }

  ./crates/ccswarm/src/mcp/transport.rs:55-75 method send <-> ./crates/ccswarm/src/mcp/transport.rs:122-135 method send
  Similarity: 73.03%
  Classes: Transport <-> Transport

[36m--- ./crates/ccswarm/src/mcp/transport.rs:send (lines 55-75) ---[0m
    async fn send(&mut self, message: &JsonRpcMessage) -> Result<()> {
        let json_str = message.to_string()?;

        let response = self
            .client
            .post(&self.base_url)
            .header("Content-Type", "application/json")
            .body(json_str)
            .timeout(self.timeout)
            .send()
            .await?;

        if !response.status().is_success() {
            return Err(anyhow::anyhow!(
                "HTTP request failed with status: {}",
                response.status()
            ));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/transport.rs:send (lines 122-135) ---[0m
    async fn send(&mut self, message: &JsonRpcMessage) -> Result<()> {
        let writer = self
            .writer
            .as_mut()
            .ok_or_else(|| anyhow::anyhow!("Unix socket not connected"))?;

        let json_str = message.to_string()?;
        let message_with_delimiter = format!("{}\n", json_str);

        writer.write_all(message_with_delimiter.as_bytes()).await?;
        writer.flush().await?;

        Ok(())
    }

  ./crates/ccswarm/src/mcp/transport.rs:55-75 method send <-> ./crates/ccswarm/src/mcp/transport.rs:109-117 method connect
  Similarity: 82.69%
  Classes: Transport <-> UnixSocketTransport

[36m--- ./crates/ccswarm/src/mcp/transport.rs:send (lines 55-75) ---[0m
    async fn send(&mut self, message: &JsonRpcMessage) -> Result<()> {
        let json_str = message.to_string()?;

        let response = self
            .client
            .post(&self.base_url)
            .header("Content-Type", "application/json")
            .body(json_str)
            .timeout(self.timeout)
            .send()
            .await?;

        if !response.status().is_success() {
            return Err(anyhow::anyhow!(
                "HTTP request failed with status: {}",
                response.status()
            ));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/transport.rs:connect (lines 109-117) ---[0m
    pub async fn connect(&mut self) -> Result<()> {
        let stream = UnixStream::connect(&self.socket_path).await?;
        let (read_half, write_half) = stream.into_split();

        self.reader = Some(BufReader::new(read_half));
        self.writer = Some(write_half);

        Ok(())
    }

  ./crates/ccswarm/src/mcp/transport.rs:33-44 method new <-> ./crates/ccswarm/src/mcp/transport.rs:55-75 method send
  Similarity: 73.17%
  Classes: HttpTransport <-> Transport

[36m--- ./crates/ccswarm/src/mcp/transport.rs:new (lines 33-44) ---[0m
    pub fn new(base_url: String) -> Self {
        let client = reqwest::Client::builder()
            .timeout(Duration::from_secs(30))
            .build()
            .expect("Failed to create HTTP client");

        Self {
            base_url,
            client,
            timeout: Duration::from_secs(30),
        }
    }

[36m--- ./crates/ccswarm/src/mcp/transport.rs:send (lines 55-75) ---[0m
    async fn send(&mut self, message: &JsonRpcMessage) -> Result<()> {
        let json_str = message.to_string()?;

        let response = self
            .client
            .post(&self.base_url)
            .header("Content-Type", "application/json")
            .body(json_str)
            .timeout(self.timeout)
            .send()
            .await?;

        if !response.status().is_success() {
            return Err(anyhow::anyhow!(
                "HTTP request failed with status: {}",
                response.status()
            ));
        }

        Ok(())
    }

  ./crates/ccswarm/src/mcp/transport.rs:137-153 method receive <-> ./crates/ccswarm/src/mcp/transport.rs:176-191 method pair
  Similarity: 72.97%
  Classes: Transport <-> InMemoryTransport

[36m--- ./crates/ccswarm/src/mcp/transport.rs:receive (lines 137-153) ---[0m
    async fn receive(&mut self) -> Result<Option<JsonRpcMessage>> {
        let reader = self
            .reader
            .as_mut()
            .ok_or_else(|| anyhow::anyhow!("Unix socket not connected"))?;

        let mut line = String::new();
        let bytes_read = reader.read_line(&mut line).await?;

        if bytes_read == 0 {
            // Connection closed
            return Ok(None);
        }

        let message = line.trim().parse::<JsonRpcMessage>()?;
        Ok(Some(message))
    }

[36m--- ./crates/ccswarm/src/mcp/transport.rs:pair (lines 176-191) ---[0m
    pub fn pair() -> (Self, Self) {
        let (tx1, rx1) = tokio::sync::mpsc::unbounded_channel();
        let (tx2, rx2) = tokio::sync::mpsc::unbounded_channel();

        let transport1 = Self {
            send_queue: tx2,
            recv_queue: rx1,
        };

        let transport2 = Self {
            send_queue: tx1,
            recv_queue: rx2,
        };

        (transport1, transport2)
    }

  ./crates/ccswarm/src/mcp/transport.rs:122-135 method send <-> ./crates/ccswarm/src/mcp/transport.rs:176-191 method pair
  Similarity: 73.61%
  Classes: Transport <-> InMemoryTransport

[36m--- ./crates/ccswarm/src/mcp/transport.rs:send (lines 122-135) ---[0m
    async fn send(&mut self, message: &JsonRpcMessage) -> Result<()> {
        let writer = self
            .writer
            .as_mut()
            .ok_or_else(|| anyhow::anyhow!("Unix socket not connected"))?;

        let json_str = message.to_string()?;
        let message_with_delimiter = format!("{}\n", json_str);

        writer.write_all(message_with_delimiter.as_bytes()).await?;
        writer.flush().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/transport.rs:pair (lines 176-191) ---[0m
    pub fn pair() -> (Self, Self) {
        let (tx1, rx1) = tokio::sync::mpsc::unbounded_channel();
        let (tx2, rx2) = tokio::sync::mpsc::unbounded_channel();

        let transport1 = Self {
            send_queue: tx2,
            recv_queue: rx1,
        };

        let transport2 = Self {
            send_queue: tx1,
            recv_queue: rx2,
        };

        (transport1, transport2)
    }

  ./crates/ccswarm/src/mcp/transport.rs:109-117 method connect <-> ./crates/ccswarm/src/mcp/transport.rs:137-153 method receive
  Similarity: 74.13%
  Classes: UnixSocketTransport <-> Transport

[36m--- ./crates/ccswarm/src/mcp/transport.rs:connect (lines 109-117) ---[0m
    pub async fn connect(&mut self) -> Result<()> {
        let stream = UnixStream::connect(&self.socket_path).await?;
        let (read_half, write_half) = stream.into_split();

        self.reader = Some(BufReader::new(read_half));
        self.writer = Some(write_half);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/transport.rs:receive (lines 137-153) ---[0m
    async fn receive(&mut self) -> Result<Option<JsonRpcMessage>> {
        let reader = self
            .reader
            .as_mut()
            .ok_or_else(|| anyhow::anyhow!("Unix socket not connected"))?;

        let mut line = String::new();
        let bytes_read = reader.read_line(&mut line).await?;

        if bytes_read == 0 {
            // Connection closed
            return Ok(None);
        }

        let message = line.trim().parse::<JsonRpcMessage>()?;
        Ok(Some(message))
    }

  ./crates/ccswarm/src/mcp/transport.rs:109-117 method connect <-> ./crates/ccswarm/src/mcp/transport.rs:176-191 method pair
  Similarity: 76.11%
  Classes: UnixSocketTransport <-> InMemoryTransport

[36m--- ./crates/ccswarm/src/mcp/transport.rs:connect (lines 109-117) ---[0m
    pub async fn connect(&mut self) -> Result<()> {
        let stream = UnixStream::connect(&self.socket_path).await?;
        let (read_half, write_half) = stream.into_split();

        self.reader = Some(BufReader::new(read_half));
        self.writer = Some(write_half);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/transport.rs:pair (lines 176-191) ---[0m
    pub fn pair() -> (Self, Self) {
        let (tx1, rx1) = tokio::sync::mpsc::unbounded_channel();
        let (tx2, rx2) = tokio::sync::mpsc::unbounded_channel();

        let transport1 = Self {
            send_queue: tx2,
            recv_queue: rx1,
        };

        let transport2 = Self {
            send_queue: tx1,
            recv_queue: rx2,
        };

        (transport1, transport2)
    }

  ./crates/ccswarm/src/mcp/transport.rs:109-117 method connect <-> ./crates/ccswarm/src/mcp/transport.rs:122-135 method send
  Similarity: 76.39%
  Classes: UnixSocketTransport <-> Transport

[36m--- ./crates/ccswarm/src/mcp/transport.rs:connect (lines 109-117) ---[0m
    pub async fn connect(&mut self) -> Result<()> {
        let stream = UnixStream::connect(&self.socket_path).await?;
        let (read_half, write_half) = stream.into_split();

        self.reader = Some(BufReader::new(read_half));
        self.writer = Some(write_half);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/transport.rs:send (lines 122-135) ---[0m
    async fn send(&mut self, message: &JsonRpcMessage) -> Result<()> {
        let writer = self
            .writer
            .as_mut()
            .ok_or_else(|| anyhow::anyhow!("Unix socket not connected"))?;

        let json_str = message.to_string()?;
        let message_with_delimiter = format!("{}\n", json_str);

        writer.write_all(message_with_delimiter.as_bytes()).await?;
        writer.flush().await?;

        Ok(())
    }

  ./crates/ccswarm/src/mcp/transport.rs:33-44 method new <-> ./crates/ccswarm/src/mcp/transport.rs:196-201 method send
  Similarity: 84.13%
  Classes: HttpTransport <-> Transport

[36m--- ./crates/ccswarm/src/mcp/transport.rs:new (lines 33-44) ---[0m
    pub fn new(base_url: String) -> Self {
        let client = reqwest::Client::builder()
            .timeout(Duration::from_secs(30))
            .build()
            .expect("Failed to create HTTP client");

        Self {
            base_url,
            client,
            timeout: Duration::from_secs(30),
        }
    }

[36m--- ./crates/ccswarm/src/mcp/transport.rs:send (lines 196-201) ---[0m
    async fn send(&mut self, message: &JsonRpcMessage) -> Result<()> {
        self.send_queue
            .send(message.clone())
            .map_err(|e| anyhow::anyhow!("Failed to send message: {}", e))?;
        Ok(())
    }


Duplicates in ./crates/ccswarm/src/cli/tests.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/cli/tests.rs:105-145 function test_cli_parsing_task_command <-> ./crates/ccswarm/src/cli/tests.rs:257-288 function test_cli_parsing_delegate_task
  Similarity: 97.17%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_task_command (lines 105-145) ---[0m
    fn test_cli_parsing_task_command() {
        let args = parse_args(&[
            "task",
            "add",
            "Implement login feature",
            "--priority",
            "high",
            "--task-type",
            "feature",
            "--details",
            "Add OAuth support",
            "--duration",
            "7200",
        ])
        .unwrap();

        match args.command {
            Commands::Task { action } => {
                match action {
                    crate::cli::TaskAction::Add {
                        description,
                        priority,
                        task_type,
                        details,
                        duration,
                        auto_assign,
                        ..
                    } => {
                        assert_eq!(description, "Implement login feature");
                        assert_eq!(priority, "high");
                        assert_eq!(task_type, "feature");
                        assert_eq!(details, Some("Add OAuth support".to_string()));
                        assert_eq!(duration, Some(7200));
                        assert!(!auto_assign); // Default should be false
                    }
                    _ => panic!("Expected Add task action"),
                }
            }
            _ => panic!("Expected Task command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_delegate_task (lines 257-288) ---[0m
    fn test_cli_parsing_delegate_task() {
        let args = parse_args(&[
            "delegate",
            "task",
            "Create API endpoint",
            "--agent",
            "backend",
            "--priority",
            "high",
            "--force",
        ])
        .unwrap();

        match args.command {
            Commands::Delegate { action } => match action {
                DelegateAction::Task {
                    description,
                    agent,
                    priority,
                    force,
                    ..
                } => {
                    assert_eq!(description, "Create API endpoint");
                    assert_eq!(agent, "backend");
                    assert_eq!(priority, "high");
                    assert!(force);
                }
                _ => panic!("Expected Task action"),
            },
            _ => panic!("Expected Delegate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:105-145 function test_cli_parsing_task_command <-> ./crates/ccswarm/src/cli/tests.rs:470-502 function test_task_type_parsing
  Similarity: 95.01%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_task_command (lines 105-145) ---[0m
    fn test_cli_parsing_task_command() {
        let args = parse_args(&[
            "task",
            "add",
            "Implement login feature",
            "--priority",
            "high",
            "--task-type",
            "feature",
            "--details",
            "Add OAuth support",
            "--duration",
            "7200",
        ])
        .unwrap();

        match args.command {
            Commands::Task { action } => {
                match action {
                    crate::cli::TaskAction::Add {
                        description,
                        priority,
                        task_type,
                        details,
                        duration,
                        auto_assign,
                        ..
                    } => {
                        assert_eq!(description, "Implement login feature");
                        assert_eq!(priority, "high");
                        assert_eq!(task_type, "feature");
                        assert_eq!(details, Some("Add OAuth support".to_string()));
                        assert_eq!(duration, Some(7200));
                        assert!(!auto_assign); // Default should be false
                    }
                    _ => panic!("Expected Add task action"),
                }
            }
            _ => panic!("Expected Task command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_task_type_parsing (lines 470-502) ---[0m
    fn test_task_type_parsing() {
        let test_cases = vec![
            ("development", TaskType::Development),
            ("dev", TaskType::Development),
            ("testing", TaskType::Testing),
            ("test", TaskType::Testing),
            ("documentation", TaskType::Documentation),
            ("docs", TaskType::Documentation),
            ("infrastructure", TaskType::Infrastructure),
            ("infra", TaskType::Infrastructure),
            ("coordination", TaskType::Coordination),
            ("review", TaskType::Review),
            ("bugfix", TaskType::Bugfix),
            ("bug", TaskType::Bugfix),
            ("feature", TaskType::Feature),
            ("unknown", TaskType::Development), // Default
        ];

        for (input, expected) in test_cases {
            let task_type = match input {
                "development" | "dev" => TaskType::Development,
                "testing" | "test" => TaskType::Testing,
                "documentation" | "docs" => TaskType::Documentation,
                "infrastructure" | "infra" => TaskType::Infrastructure,
                "coordination" => TaskType::Coordination,
                "review" => TaskType::Review,
                "bugfix" | "bug" => TaskType::Bugfix,
                "feature" => TaskType::Feature,
                _ => TaskType::Development,
            };
            assert_eq!(task_type, expected);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:105-145 function test_cli_parsing_task_command <-> ./crates/ccswarm/src/cli/tests.rs:291-318 function test_cli_parsing_session_create
  Similarity: 97.17%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_task_command (lines 105-145) ---[0m
    fn test_cli_parsing_task_command() {
        let args = parse_args(&[
            "task",
            "add",
            "Implement login feature",
            "--priority",
            "high",
            "--task-type",
            "feature",
            "--details",
            "Add OAuth support",
            "--duration",
            "7200",
        ])
        .unwrap();

        match args.command {
            Commands::Task { action } => {
                match action {
                    crate::cli::TaskAction::Add {
                        description,
                        priority,
                        task_type,
                        details,
                        duration,
                        auto_assign,
                        ..
                    } => {
                        assert_eq!(description, "Implement login feature");
                        assert_eq!(priority, "high");
                        assert_eq!(task_type, "feature");
                        assert_eq!(details, Some("Add OAuth support".to_string()));
                        assert_eq!(duration, Some(7200));
                        assert!(!auto_assign); // Default should be false
                    }
                    _ => panic!("Expected Add task action"),
                }
            }
            _ => panic!("Expected Task command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_session_create (lines 291-318) ---[0m
    fn test_cli_parsing_session_create() {
        let args = parse_args(&[
            "session",
            "create",
            "--agent",
            "frontend",
            "--workspace",
            "./workspace",
            "--background",
        ])
        .unwrap();

        match args.command {
            Commands::Session { action } => match action {
                SessionAction::Create {
                    agent,
                    workspace,
                    background,
                } => {
                    assert_eq!(agent, "frontend");
                    assert_eq!(workspace, Some("./workspace".to_string()));
                    assert!(background);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Session command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:105-145 function test_cli_parsing_task_command <-> ./crates/ccswarm/src/cli/tests.rs:186-211 function test_cli_parsing_worktree_create
  Similarity: 97.17%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_task_command (lines 105-145) ---[0m
    fn test_cli_parsing_task_command() {
        let args = parse_args(&[
            "task",
            "add",
            "Implement login feature",
            "--priority",
            "high",
            "--task-type",
            "feature",
            "--details",
            "Add OAuth support",
            "--duration",
            "7200",
        ])
        .unwrap();

        match args.command {
            Commands::Task { action } => {
                match action {
                    crate::cli::TaskAction::Add {
                        description,
                        priority,
                        task_type,
                        details,
                        duration,
                        auto_assign,
                        ..
                    } => {
                        assert_eq!(description, "Implement login feature");
                        assert_eq!(priority, "high");
                        assert_eq!(task_type, "feature");
                        assert_eq!(details, Some("Add OAuth support".to_string()));
                        assert_eq!(duration, Some(7200));
                        assert!(!auto_assign); // Default should be false
                    }
                    _ => panic!("Expected Add task action"),
                }
            }
            _ => panic!("Expected Task command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_create (lines 186-211) ---[0m
    fn test_cli_parsing_worktree_create() {
        let args = parse_args(&[
            "worktree",
            "create",
            "/path/to/worktree",
            "feature-branch",
            "--new-branch",
        ])
        .unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::Create {
                    path,
                    branch,
                    new_branch,
                } => {
                    assert_eq!(path, PathBuf::from("/path/to/worktree"));
                    assert_eq!(branch, "feature-branch");
                    assert!(new_branch);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:105-145 function test_cli_parsing_task_command <-> ./crates/ccswarm/src/cli/tests.rs:321-347 function test_cli_parsing_auto_create
  Similarity: 95.54%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_task_command (lines 105-145) ---[0m
    fn test_cli_parsing_task_command() {
        let args = parse_args(&[
            "task",
            "add",
            "Implement login feature",
            "--priority",
            "high",
            "--task-type",
            "feature",
            "--details",
            "Add OAuth support",
            "--duration",
            "7200",
        ])
        .unwrap();

        match args.command {
            Commands::Task { action } => {
                match action {
                    crate::cli::TaskAction::Add {
                        description,
                        priority,
                        task_type,
                        details,
                        duration,
                        auto_assign,
                        ..
                    } => {
                        assert_eq!(description, "Implement login feature");
                        assert_eq!(priority, "high");
                        assert_eq!(task_type, "feature");
                        assert_eq!(details, Some("Add OAuth support".to_string()));
                        assert_eq!(duration, Some(7200));
                        assert!(!auto_assign); // Default should be false
                    }
                    _ => panic!("Expected Add task action"),
                }
            }
            _ => panic!("Expected Task command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_auto_create (lines 321-347) ---[0m
    fn test_cli_parsing_auto_create() {
        let args = parse_args(&[
            "auto-create",
            "Todo app with authentication",
            "--template",
            "full-stack",
            "--auto-deploy",
            "--output",
            "./my-app",
        ])
        .unwrap();

        match args.command {
            Commands::AutoCreate {
                description,
                template,
                auto_deploy,
                output,
            } => {
                assert_eq!(description, "Todo app with authentication");
                assert_eq!(template, Some("full-stack".to_string()));
                assert!(auto_deploy);
                assert_eq!(output, PathBuf::from("./my-app"));
            }
            _ => panic!("Expected AutoCreate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:15-39 function test_cli_parsing_init_command <-> ./crates/ccswarm/src/cli/tests.rs:105-145 function test_cli_parsing_task_command
  Similarity: 95.54%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_init_command (lines 15-39) ---[0m
    fn test_cli_parsing_init_command() {
        let args = parse_args(&[
            "init",
            "--name",
            "test-project",
            "--repo-url",
            "https://github.com/test/repo",
            "--agents",
            "frontend,backend",
        ])
        .unwrap();

        match args.command {
            Commands::Init {
                name,
                repo_url,
                agents,
            } => {
                assert_eq!(name, "test-project");
                assert_eq!(repo_url, Some("https://github.com/test/repo".to_string()));
                assert_eq!(agents, vec!["frontend", "backend"]);
            }
            _ => panic!("Expected Init command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_task_command (lines 105-145) ---[0m
    fn test_cli_parsing_task_command() {
        let args = parse_args(&[
            "task",
            "add",
            "Implement login feature",
            "--priority",
            "high",
            "--task-type",
            "feature",
            "--details",
            "Add OAuth support",
            "--duration",
            "7200",
        ])
        .unwrap();

        match args.command {
            Commands::Task { action } => {
                match action {
                    crate::cli::TaskAction::Add {
                        description,
                        priority,
                        task_type,
                        details,
                        duration,
                        auto_assign,
                        ..
                    } => {
                        assert_eq!(description, "Implement login feature");
                        assert_eq!(priority, "high");
                        assert_eq!(task_type, "feature");
                        assert_eq!(details, Some("Add OAuth support".to_string()));
                        assert_eq!(duration, Some(7200));
                        assert!(!auto_assign); // Default should be false
                    }
                    _ => panic!("Expected Add task action"),
                }
            }
            _ => panic!("Expected Task command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:257-288 function test_cli_parsing_delegate_task <-> ./crates/ccswarm/src/cli/tests.rs:470-502 function test_task_type_parsing
  Similarity: 95.01%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_delegate_task (lines 257-288) ---[0m
    fn test_cli_parsing_delegate_task() {
        let args = parse_args(&[
            "delegate",
            "task",
            "Create API endpoint",
            "--agent",
            "backend",
            "--priority",
            "high",
            "--force",
        ])
        .unwrap();

        match args.command {
            Commands::Delegate { action } => match action {
                DelegateAction::Task {
                    description,
                    agent,
                    priority,
                    force,
                    ..
                } => {
                    assert_eq!(description, "Create API endpoint");
                    assert_eq!(agent, "backend");
                    assert_eq!(priority, "high");
                    assert!(force);
                }
                _ => panic!("Expected Task action"),
            },
            _ => panic!("Expected Delegate command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_task_type_parsing (lines 470-502) ---[0m
    fn test_task_type_parsing() {
        let test_cases = vec![
            ("development", TaskType::Development),
            ("dev", TaskType::Development),
            ("testing", TaskType::Testing),
            ("test", TaskType::Testing),
            ("documentation", TaskType::Documentation),
            ("docs", TaskType::Documentation),
            ("infrastructure", TaskType::Infrastructure),
            ("infra", TaskType::Infrastructure),
            ("coordination", TaskType::Coordination),
            ("review", TaskType::Review),
            ("bugfix", TaskType::Bugfix),
            ("bug", TaskType::Bugfix),
            ("feature", TaskType::Feature),
            ("unknown", TaskType::Development), // Default
        ];

        for (input, expected) in test_cases {
            let task_type = match input {
                "development" | "dev" => TaskType::Development,
                "testing" | "test" => TaskType::Testing,
                "documentation" | "docs" => TaskType::Documentation,
                "infrastructure" | "infra" => TaskType::Infrastructure,
                "coordination" => TaskType::Coordination,
                "review" => TaskType::Review,
                "bugfix" | "bug" => TaskType::Bugfix,
                "feature" => TaskType::Feature,
                _ => TaskType::Development,
            };
            assert_eq!(task_type, expected);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:105-145 function test_cli_parsing_task_command <-> ./crates/ccswarm/src/cli/tests.rs:233-254 function test_cli_parsing_config_generate
  Similarity: 97.17%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_task_command (lines 105-145) ---[0m
    fn test_cli_parsing_task_command() {
        let args = parse_args(&[
            "task",
            "add",
            "Implement login feature",
            "--priority",
            "high",
            "--task-type",
            "feature",
            "--details",
            "Add OAuth support",
            "--duration",
            "7200",
        ])
        .unwrap();

        match args.command {
            Commands::Task { action } => {
                match action {
                    crate::cli::TaskAction::Add {
                        description,
                        priority,
                        task_type,
                        details,
                        duration,
                        auto_assign,
                        ..
                    } => {
                        assert_eq!(description, "Implement login feature");
                        assert_eq!(priority, "high");
                        assert_eq!(task_type, "feature");
                        assert_eq!(details, Some("Add OAuth support".to_string()));
                        assert_eq!(duration, Some(7200));
                        assert!(!auto_assign); // Default should be false
                    }
                    _ => panic!("Expected Add task action"),
                }
            }
            _ => panic!("Expected Task command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_config_generate (lines 233-254) ---[0m
    fn test_cli_parsing_config_generate() {
        let args = parse_args(&[
            "config",
            "generate",
            "--output",
            "custom.json",
            "--template",
            "minimal",
        ])
        .unwrap();

        match args.command {
            Commands::Config { action } => match action {
                ConfigAction::Generate { output, template } => {
                    assert_eq!(output, PathBuf::from("custom.json"));
                    assert_eq!(template, "minimal");
                }
                _ => panic!("Expected Generate action"),
            },
            _ => panic!("Expected Config command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:291-318 function test_cli_parsing_session_create <-> ./crates/ccswarm/src/cli/tests.rs:470-502 function test_task_type_parsing
  Similarity: 95.01%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_session_create (lines 291-318) ---[0m
    fn test_cli_parsing_session_create() {
        let args = parse_args(&[
            "session",
            "create",
            "--agent",
            "frontend",
            "--workspace",
            "./workspace",
            "--background",
        ])
        .unwrap();

        match args.command {
            Commands::Session { action } => match action {
                SessionAction::Create {
                    agent,
                    workspace,
                    background,
                } => {
                    assert_eq!(agent, "frontend");
                    assert_eq!(workspace, Some("./workspace".to_string()));
                    assert!(background);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Session command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_task_type_parsing (lines 470-502) ---[0m
    fn test_task_type_parsing() {
        let test_cases = vec![
            ("development", TaskType::Development),
            ("dev", TaskType::Development),
            ("testing", TaskType::Testing),
            ("test", TaskType::Testing),
            ("documentation", TaskType::Documentation),
            ("docs", TaskType::Documentation),
            ("infrastructure", TaskType::Infrastructure),
            ("infra", TaskType::Infrastructure),
            ("coordination", TaskType::Coordination),
            ("review", TaskType::Review),
            ("bugfix", TaskType::Bugfix),
            ("bug", TaskType::Bugfix),
            ("feature", TaskType::Feature),
            ("unknown", TaskType::Development), // Default
        ];

        for (input, expected) in test_cases {
            let task_type = match input {
                "development" | "dev" => TaskType::Development,
                "testing" | "test" => TaskType::Testing,
                "documentation" | "docs" => TaskType::Documentation,
                "infrastructure" | "infra" => TaskType::Infrastructure,
                "coordination" => TaskType::Coordination,
                "review" => TaskType::Review,
                "bugfix" | "bug" => TaskType::Bugfix,
                "feature" => TaskType::Feature,
                _ => TaskType::Development,
            };
            assert_eq!(task_type, expected);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:105-145 function test_cli_parsing_task_command <-> ./crates/ccswarm/src/cli/tests.rs:560-580 function test_interactive_command_parsing
  Similarity: 92.75%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_task_command (lines 105-145) ---[0m
    fn test_cli_parsing_task_command() {
        let args = parse_args(&[
            "task",
            "add",
            "Implement login feature",
            "--priority",
            "high",
            "--task-type",
            "feature",
            "--details",
            "Add OAuth support",
            "--duration",
            "7200",
        ])
        .unwrap();

        match args.command {
            Commands::Task { action } => {
                match action {
                    crate::cli::TaskAction::Add {
                        description,
                        priority,
                        task_type,
                        details,
                        duration,
                        auto_assign,
                        ..
                    } => {
                        assert_eq!(description, "Implement login feature");
                        assert_eq!(priority, "high");
                        assert_eq!(task_type, "feature");
                        assert_eq!(details, Some("Add OAuth support".to_string()));
                        assert_eq!(duration, Some(7200));
                        assert!(!auto_assign); // Default should be false
                    }
                    _ => panic!("Expected Add task action"),
                }
            }
            _ => panic!("Expected Task command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_interactive_command_parsing (lines 560-580) ---[0m
    fn test_interactive_command_parsing() {
        // Test various interactive commands
        let commands = vec![
            ("quit", true),
            ("exit", true),
            ("help", true),
            ("stats", true),
            ("agents", true),
            ("analyze task description", true),
            ("delegate frontend Create UI", true),
            ("unknown command", false),
        ];

        for (cmd, should_be_valid) in commands {
            let is_valid = matches!(cmd, "quit" | "exit" | "help" | "stats" | "agents")
                || cmd.starts_with("analyze ")
                || cmd.starts_with("delegate ");

            assert_eq!(is_valid, should_be_valid);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:257-288 function test_cli_parsing_delegate_task <-> ./crates/ccswarm/src/cli/tests.rs:291-318 function test_cli_parsing_session_create
  Similarity: 95.70%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_delegate_task (lines 257-288) ---[0m
    fn test_cli_parsing_delegate_task() {
        let args = parse_args(&[
            "delegate",
            "task",
            "Create API endpoint",
            "--agent",
            "backend",
            "--priority",
            "high",
            "--force",
        ])
        .unwrap();

        match args.command {
            Commands::Delegate { action } => match action {
                DelegateAction::Task {
                    description,
                    agent,
                    priority,
                    force,
                    ..
                } => {
                    assert_eq!(description, "Create API endpoint");
                    assert_eq!(agent, "backend");
                    assert_eq!(priority, "high");
                    assert!(force);
                }
                _ => panic!("Expected Task action"),
            },
            _ => panic!("Expected Delegate command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_session_create (lines 291-318) ---[0m
    fn test_cli_parsing_session_create() {
        let args = parse_args(&[
            "session",
            "create",
            "--agent",
            "frontend",
            "--workspace",
            "./workspace",
            "--background",
        ])
        .unwrap();

        match args.command {
            Commands::Session { action } => match action {
                SessionAction::Create {
                    agent,
                    workspace,
                    background,
                } => {
                    assert_eq!(agent, "frontend");
                    assert_eq!(workspace, Some("./workspace".to_string()));
                    assert!(background);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Session command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:105-145 function test_cli_parsing_task_command <-> ./crates/ccswarm/src/cli/tests.rs:448-467 function test_priority_parsing
  Similarity: 93.49%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_task_command (lines 105-145) ---[0m
    fn test_cli_parsing_task_command() {
        let args = parse_args(&[
            "task",
            "add",
            "Implement login feature",
            "--priority",
            "high",
            "--task-type",
            "feature",
            "--details",
            "Add OAuth support",
            "--duration",
            "7200",
        ])
        .unwrap();

        match args.command {
            Commands::Task { action } => {
                match action {
                    crate::cli::TaskAction::Add {
                        description,
                        priority,
                        task_type,
                        details,
                        duration,
                        auto_assign,
                        ..
                    } => {
                        assert_eq!(description, "Implement login feature");
                        assert_eq!(priority, "high");
                        assert_eq!(task_type, "feature");
                        assert_eq!(details, Some("Add OAuth support".to_string()));
                        assert_eq!(duration, Some(7200));
                        assert!(!auto_assign); // Default should be false
                    }
                    _ => panic!("Expected Add task action"),
                }
            }
            _ => panic!("Expected Task command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_priority_parsing (lines 448-467) ---[0m
    fn test_priority_parsing() {
        let test_cases = vec![
            ("low", Priority::Low),
            ("medium", Priority::Medium),
            ("high", Priority::High),
            ("critical", Priority::Critical),
            ("unknown", Priority::Medium), // Default
        ];

        for (input, expected) in test_cases {
            let priority = match input {
                "low" => Priority::Low,
                "medium" => Priority::Medium,
                "high" => Priority::High,
                "critical" => Priority::Critical,
                _ => Priority::Medium,
            };
            assert_eq!(priority, expected);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:42-59 function test_cli_parsing_start_command <-> ./crates/ccswarm/src/cli/tests.rs:105-145 function test_cli_parsing_task_command
  Similarity: 95.54%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command (lines 42-59) ---[0m
    fn test_cli_parsing_start_command() {
        let args = parse_args(&["start", "--daemon", "--port", "9090"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(daemon);
                assert_eq!(port, 9090);
                assert_eq!(isolation, "worktree"); // default value
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_task_command (lines 105-145) ---[0m
    fn test_cli_parsing_task_command() {
        let args = parse_args(&[
            "task",
            "add",
            "Implement login feature",
            "--priority",
            "high",
            "--task-type",
            "feature",
            "--details",
            "Add OAuth support",
            "--duration",
            "7200",
        ])
        .unwrap();

        match args.command {
            Commands::Task { action } => {
                match action {
                    crate::cli::TaskAction::Add {
                        description,
                        priority,
                        task_type,
                        details,
                        duration,
                        auto_assign,
                        ..
                    } => {
                        assert_eq!(description, "Implement login feature");
                        assert_eq!(priority, "high");
                        assert_eq!(task_type, "feature");
                        assert_eq!(details, Some("Add OAuth support".to_string()));
                        assert_eq!(duration, Some(7200));
                        assert!(!auto_assign); // Default should be false
                    }
                    _ => panic!("Expected Add task action"),
                }
            }
            _ => panic!("Expected Task command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:105-145 function test_cli_parsing_task_command <-> ./crates/ccswarm/src/cli/tests.rs:526-544 function test_delegation_strategy_parsing
  Similarity: 92.75%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_task_command (lines 105-145) ---[0m
    fn test_cli_parsing_task_command() {
        let args = parse_args(&[
            "task",
            "add",
            "Implement login feature",
            "--priority",
            "high",
            "--task-type",
            "feature",
            "--details",
            "Add OAuth support",
            "--duration",
            "7200",
        ])
        .unwrap();

        match args.command {
            Commands::Task { action } => {
                match action {
                    crate::cli::TaskAction::Add {
                        description,
                        priority,
                        task_type,
                        details,
                        duration,
                        auto_assign,
                        ..
                    } => {
                        assert_eq!(description, "Implement login feature");
                        assert_eq!(priority, "high");
                        assert_eq!(task_type, "feature");
                        assert_eq!(details, Some("Add OAuth support".to_string()));
                        assert_eq!(duration, Some(7200));
                        assert!(!auto_assign); // Default should be false
                    }
                    _ => panic!("Expected Add task action"),
                }
            }
            _ => panic!("Expected Task command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_delegation_strategy_parsing (lines 526-544) ---[0m
    fn test_delegation_strategy_parsing() {
        // Test strategy string parsing logic
        let test_cases = vec![
            ("content", true),
            ("load", true),
            ("expertise", true),
            ("workflow", true),
            ("hybrid", true),
            ("unknown", false), // Should default to hybrid
        ];

        for (input, is_valid_strategy) in test_cases {
            let is_valid = matches!(
                input,
                "content" | "load" | "expertise" | "workflow" | "hybrid"
            );
            assert_eq!(is_valid, is_valid_strategy);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:186-211 function test_cli_parsing_worktree_create <-> ./crates/ccswarm/src/cli/tests.rs:257-288 function test_cli_parsing_delegate_task
  Similarity: 95.70%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_create (lines 186-211) ---[0m
    fn test_cli_parsing_worktree_create() {
        let args = parse_args(&[
            "worktree",
            "create",
            "/path/to/worktree",
            "feature-branch",
            "--new-branch",
        ])
        .unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::Create {
                    path,
                    branch,
                    new_branch,
                } => {
                    assert_eq!(path, PathBuf::from("/path/to/worktree"));
                    assert_eq!(branch, "feature-branch");
                    assert!(new_branch);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_delegate_task (lines 257-288) ---[0m
    fn test_cli_parsing_delegate_task() {
        let args = parse_args(&[
            "delegate",
            "task",
            "Create API endpoint",
            "--agent",
            "backend",
            "--priority",
            "high",
            "--force",
        ])
        .unwrap();

        match args.command {
            Commands::Delegate { action } => match action {
                DelegateAction::Task {
                    description,
                    agent,
                    priority,
                    force,
                    ..
                } => {
                    assert_eq!(description, "Create API endpoint");
                    assert_eq!(agent, "backend");
                    assert_eq!(priority, "high");
                    assert!(force);
                }
                _ => panic!("Expected Task action"),
            },
            _ => panic!("Expected Delegate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:257-288 function test_cli_parsing_delegate_task <-> ./crates/ccswarm/src/cli/tests.rs:321-347 function test_cli_parsing_auto_create
  Similarity: 94.00%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_delegate_task (lines 257-288) ---[0m
    fn test_cli_parsing_delegate_task() {
        let args = parse_args(&[
            "delegate",
            "task",
            "Create API endpoint",
            "--agent",
            "backend",
            "--priority",
            "high",
            "--force",
        ])
        .unwrap();

        match args.command {
            Commands::Delegate { action } => match action {
                DelegateAction::Task {
                    description,
                    agent,
                    priority,
                    force,
                    ..
                } => {
                    assert_eq!(description, "Create API endpoint");
                    assert_eq!(agent, "backend");
                    assert_eq!(priority, "high");
                    assert!(force);
                }
                _ => panic!("Expected Task action"),
            },
            _ => panic!("Expected Delegate command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_auto_create (lines 321-347) ---[0m
    fn test_cli_parsing_auto_create() {
        let args = parse_args(&[
            "auto-create",
            "Todo app with authentication",
            "--template",
            "full-stack",
            "--auto-deploy",
            "--output",
            "./my-app",
        ])
        .unwrap();

        match args.command {
            Commands::AutoCreate {
                description,
                template,
                auto_deploy,
                output,
            } => {
                assert_eq!(description, "Todo app with authentication");
                assert_eq!(template, Some("full-stack".to_string()));
                assert!(auto_deploy);
                assert_eq!(output, PathBuf::from("./my-app"));
            }
            _ => panic!("Expected AutoCreate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:105-145 function test_cli_parsing_task_command <-> ./crates/ccswarm/src/cli/tests.rs:214-230 function test_cli_parsing_logs_command
  Similarity: 95.54%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_task_command (lines 105-145) ---[0m
    fn test_cli_parsing_task_command() {
        let args = parse_args(&[
            "task",
            "add",
            "Implement login feature",
            "--priority",
            "high",
            "--task-type",
            "feature",
            "--details",
            "Add OAuth support",
            "--duration",
            "7200",
        ])
        .unwrap();

        match args.command {
            Commands::Task { action } => {
                match action {
                    crate::cli::TaskAction::Add {
                        description,
                        priority,
                        task_type,
                        details,
                        duration,
                        auto_assign,
                        ..
                    } => {
                        assert_eq!(description, "Implement login feature");
                        assert_eq!(priority, "high");
                        assert_eq!(task_type, "feature");
                        assert_eq!(details, Some("Add OAuth support".to_string()));
                        assert_eq!(duration, Some(7200));
                        assert!(!auto_assign); // Default should be false
                    }
                    _ => panic!("Expected Add task action"),
                }
            }
            _ => panic!("Expected Task command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_logs_command (lines 214-230) ---[0m
    fn test_cli_parsing_logs_command() {
        let args =
            parse_args(&["logs", "--follow", "--agent", "frontend", "--lines", "200"]).unwrap();

        match args.command {
            Commands::Logs {
                follow,
                agent,
                lines,
            } => {
                assert!(follow);
                assert_eq!(agent, Some("frontend".to_string()));
                assert_eq!(lines, 200);
            }
            _ => panic!("Expected Logs command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:15-39 function test_cli_parsing_init_command <-> ./crates/ccswarm/src/cli/tests.rs:257-288 function test_cli_parsing_delegate_task
  Similarity: 94.00%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_init_command (lines 15-39) ---[0m
    fn test_cli_parsing_init_command() {
        let args = parse_args(&[
            "init",
            "--name",
            "test-project",
            "--repo-url",
            "https://github.com/test/repo",
            "--agents",
            "frontend,backend",
        ])
        .unwrap();

        match args.command {
            Commands::Init {
                name,
                repo_url,
                agents,
            } => {
                assert_eq!(name, "test-project");
                assert_eq!(repo_url, Some("https://github.com/test/repo".to_string()));
                assert_eq!(agents, vec!["frontend", "backend"]);
            }
            _ => panic!("Expected Init command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_delegate_task (lines 257-288) ---[0m
    fn test_cli_parsing_delegate_task() {
        let args = parse_args(&[
            "delegate",
            "task",
            "Create API endpoint",
            "--agent",
            "backend",
            "--priority",
            "high",
            "--force",
        ])
        .unwrap();

        match args.command {
            Commands::Delegate { action } => match action {
                DelegateAction::Task {
                    description,
                    agent,
                    priority,
                    force,
                    ..
                } => {
                    assert_eq!(description, "Create API endpoint");
                    assert_eq!(agent, "backend");
                    assert_eq!(priority, "high");
                    assert!(force);
                }
                _ => panic!("Expected Task action"),
            },
            _ => panic!("Expected Delegate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:470-502 function test_task_type_parsing <-> ./crates/ccswarm/src/cli/tests.rs:560-580 function test_interactive_command_parsing
  Similarity: 96.55%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_task_type_parsing (lines 470-502) ---[0m
    fn test_task_type_parsing() {
        let test_cases = vec![
            ("development", TaskType::Development),
            ("dev", TaskType::Development),
            ("testing", TaskType::Testing),
            ("test", TaskType::Testing),
            ("documentation", TaskType::Documentation),
            ("docs", TaskType::Documentation),
            ("infrastructure", TaskType::Infrastructure),
            ("infra", TaskType::Infrastructure),
            ("coordination", TaskType::Coordination),
            ("review", TaskType::Review),
            ("bugfix", TaskType::Bugfix),
            ("bug", TaskType::Bugfix),
            ("feature", TaskType::Feature),
            ("unknown", TaskType::Development), // Default
        ];

        for (input, expected) in test_cases {
            let task_type = match input {
                "development" | "dev" => TaskType::Development,
                "testing" | "test" => TaskType::Testing,
                "documentation" | "docs" => TaskType::Documentation,
                "infrastructure" | "infra" => TaskType::Infrastructure,
                "coordination" => TaskType::Coordination,
                "review" => TaskType::Review,
                "bugfix" | "bug" => TaskType::Bugfix,
                "feature" => TaskType::Feature,
                _ => TaskType::Development,
            };
            assert_eq!(task_type, expected);
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_interactive_command_parsing (lines 560-580) ---[0m
    fn test_interactive_command_parsing() {
        // Test various interactive commands
        let commands = vec![
            ("quit", true),
            ("exit", true),
            ("help", true),
            ("stats", true),
            ("agents", true),
            ("analyze task description", true),
            ("delegate frontend Create UI", true),
            ("unknown command", false),
        ];

        for (cmd, should_be_valid) in commands {
            let is_valid = matches!(cmd, "quit" | "exit" | "help" | "stats" | "agents")
                || cmd.starts_with("analyze ")
                || cmd.starts_with("delegate ");

            assert_eq!(is_valid, should_be_valid);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:291-318 function test_cli_parsing_session_create <-> ./crates/ccswarm/src/cli/tests.rs:321-347 function test_cli_parsing_auto_create
  Similarity: 94.54%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_session_create (lines 291-318) ---[0m
    fn test_cli_parsing_session_create() {
        let args = parse_args(&[
            "session",
            "create",
            "--agent",
            "frontend",
            "--workspace",
            "./workspace",
            "--background",
        ])
        .unwrap();

        match args.command {
            Commands::Session { action } => match action {
                SessionAction::Create {
                    agent,
                    workspace,
                    background,
                } => {
                    assert_eq!(agent, "frontend");
                    assert_eq!(workspace, Some("./workspace".to_string()));
                    assert!(background);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Session command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_auto_create (lines 321-347) ---[0m
    fn test_cli_parsing_auto_create() {
        let args = parse_args(&[
            "auto-create",
            "Todo app with authentication",
            "--template",
            "full-stack",
            "--auto-deploy",
            "--output",
            "./my-app",
        ])
        .unwrap();

        match args.command {
            Commands::AutoCreate {
                description,
                template,
                auto_deploy,
                output,
            } => {
                assert_eq!(description, "Todo app with authentication");
                assert_eq!(template, Some("full-stack".to_string()));
                assert!(auto_deploy);
                assert_eq!(output, PathBuf::from("./my-app"));
            }
            _ => panic!("Expected AutoCreate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:233-254 function test_cli_parsing_config_generate <-> ./crates/ccswarm/src/cli/tests.rs:257-288 function test_cli_parsing_delegate_task
  Similarity: 95.70%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_config_generate (lines 233-254) ---[0m
    fn test_cli_parsing_config_generate() {
        let args = parse_args(&[
            "config",
            "generate",
            "--output",
            "custom.json",
            "--template",
            "minimal",
        ])
        .unwrap();

        match args.command {
            Commands::Config { action } => match action {
                ConfigAction::Generate { output, template } => {
                    assert_eq!(output, PathBuf::from("custom.json"));
                    assert_eq!(template, "minimal");
                }
                _ => panic!("Expected Generate action"),
            },
            _ => panic!("Expected Config command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_delegate_task (lines 257-288) ---[0m
    fn test_cli_parsing_delegate_task() {
        let args = parse_args(&[
            "delegate",
            "task",
            "Create API endpoint",
            "--agent",
            "backend",
            "--priority",
            "high",
            "--force",
        ])
        .unwrap();

        match args.command {
            Commands::Delegate { action } => match action {
                DelegateAction::Task {
                    description,
                    agent,
                    priority,
                    force,
                    ..
                } => {
                    assert_eq!(description, "Create API endpoint");
                    assert_eq!(agent, "backend");
                    assert_eq!(priority, "high");
                    assert!(force);
                }
                _ => panic!("Expected Task action"),
            },
            _ => panic!("Expected Delegate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:186-211 function test_cli_parsing_worktree_create <-> ./crates/ccswarm/src/cli/tests.rs:291-318 function test_cli_parsing_session_create
  Similarity: 95.35%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_create (lines 186-211) ---[0m
    fn test_cli_parsing_worktree_create() {
        let args = parse_args(&[
            "worktree",
            "create",
            "/path/to/worktree",
            "feature-branch",
            "--new-branch",
        ])
        .unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::Create {
                    path,
                    branch,
                    new_branch,
                } => {
                    assert_eq!(path, PathBuf::from("/path/to/worktree"));
                    assert_eq!(branch, "feature-branch");
                    assert!(new_branch);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_session_create (lines 291-318) ---[0m
    fn test_cli_parsing_session_create() {
        let args = parse_args(&[
            "session",
            "create",
            "--agent",
            "frontend",
            "--workspace",
            "./workspace",
            "--background",
        ])
        .unwrap();

        match args.command {
            Commands::Session { action } => match action {
                SessionAction::Create {
                    agent,
                    workspace,
                    background,
                } => {
                    assert_eq!(agent, "frontend");
                    assert_eq!(workspace, Some("./workspace".to_string()));
                    assert!(background);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Session command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:15-39 function test_cli_parsing_init_command <-> ./crates/ccswarm/src/cli/tests.rs:291-318 function test_cli_parsing_session_create
  Similarity: 94.54%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_init_command (lines 15-39) ---[0m
    fn test_cli_parsing_init_command() {
        let args = parse_args(&[
            "init",
            "--name",
            "test-project",
            "--repo-url",
            "https://github.com/test/repo",
            "--agents",
            "frontend,backend",
        ])
        .unwrap();

        match args.command {
            Commands::Init {
                name,
                repo_url,
                agents,
            } => {
                assert_eq!(name, "test-project");
                assert_eq!(repo_url, Some("https://github.com/test/repo".to_string()));
                assert_eq!(agents, vec!["frontend", "backend"]);
            }
            _ => panic!("Expected Init command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_session_create (lines 291-318) ---[0m
    fn test_cli_parsing_session_create() {
        let args = parse_args(&[
            "session",
            "create",
            "--agent",
            "frontend",
            "--workspace",
            "./workspace",
            "--background",
        ])
        .unwrap();

        match args.command {
            Commands::Session { action } => match action {
                SessionAction::Create {
                    agent,
                    workspace,
                    background,
                } => {
                    assert_eq!(agent, "frontend");
                    assert_eq!(workspace, Some("./workspace".to_string()));
                    assert!(background);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Session command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:186-211 function test_cli_parsing_worktree_create <-> ./crates/ccswarm/src/cli/tests.rs:321-347 function test_cli_parsing_auto_create
  Similarity: 93.10%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_create (lines 186-211) ---[0m
    fn test_cli_parsing_worktree_create() {
        let args = parse_args(&[
            "worktree",
            "create",
            "/path/to/worktree",
            "feature-branch",
            "--new-branch",
        ])
        .unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::Create {
                    path,
                    branch,
                    new_branch,
                } => {
                    assert_eq!(path, PathBuf::from("/path/to/worktree"));
                    assert_eq!(branch, "feature-branch");
                    assert!(new_branch);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_auto_create (lines 321-347) ---[0m
    fn test_cli_parsing_auto_create() {
        let args = parse_args(&[
            "auto-create",
            "Todo app with authentication",
            "--template",
            "full-stack",
            "--auto-deploy",
            "--output",
            "./my-app",
        ])
        .unwrap();

        match args.command {
            Commands::AutoCreate {
                description,
                template,
                auto_deploy,
                output,
            } => {
                assert_eq!(description, "Todo app with authentication");
                assert_eq!(template, Some("full-stack".to_string()));
                assert!(auto_deploy);
                assert_eq!(output, PathBuf::from("./my-app"));
            }
            _ => panic!("Expected AutoCreate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:15-39 function test_cli_parsing_init_command <-> ./crates/ccswarm/src/cli/tests.rs:321-347 function test_cli_parsing_auto_create
  Similarity: 94.20%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_init_command (lines 15-39) ---[0m
    fn test_cli_parsing_init_command() {
        let args = parse_args(&[
            "init",
            "--name",
            "test-project",
            "--repo-url",
            "https://github.com/test/repo",
            "--agents",
            "frontend,backend",
        ])
        .unwrap();

        match args.command {
            Commands::Init {
                name,
                repo_url,
                agents,
            } => {
                assert_eq!(name, "test-project");
                assert_eq!(repo_url, Some("https://github.com/test/repo".to_string()));
                assert_eq!(agents, vec!["frontend", "backend"]);
            }
            _ => panic!("Expected Init command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_auto_create (lines 321-347) ---[0m
    fn test_cli_parsing_auto_create() {
        let args = parse_args(&[
            "auto-create",
            "Todo app with authentication",
            "--template",
            "full-stack",
            "--auto-deploy",
            "--output",
            "./my-app",
        ])
        .unwrap();

        match args.command {
            Commands::AutoCreate {
                description,
                template,
                auto_deploy,
                output,
            } => {
                assert_eq!(description, "Todo app with authentication");
                assert_eq!(template, Some("full-stack".to_string()));
                assert!(auto_deploy);
                assert_eq!(output, PathBuf::from("./my-app"));
            }
            _ => panic!("Expected AutoCreate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:257-288 function test_cli_parsing_delegate_task <-> ./crates/ccswarm/src/cli/tests.rs:560-580 function test_interactive_command_parsing
  Similarity: 90.25%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_delegate_task (lines 257-288) ---[0m
    fn test_cli_parsing_delegate_task() {
        let args = parse_args(&[
            "delegate",
            "task",
            "Create API endpoint",
            "--agent",
            "backend",
            "--priority",
            "high",
            "--force",
        ])
        .unwrap();

        match args.command {
            Commands::Delegate { action } => match action {
                DelegateAction::Task {
                    description,
                    agent,
                    priority,
                    force,
                    ..
                } => {
                    assert_eq!(description, "Create API endpoint");
                    assert_eq!(agent, "backend");
                    assert_eq!(priority, "high");
                    assert!(force);
                }
                _ => panic!("Expected Task action"),
            },
            _ => panic!("Expected Delegate command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_interactive_command_parsing (lines 560-580) ---[0m
    fn test_interactive_command_parsing() {
        // Test various interactive commands
        let commands = vec![
            ("quit", true),
            ("exit", true),
            ("help", true),
            ("stats", true),
            ("agents", true),
            ("analyze task description", true),
            ("delegate frontend Create UI", true),
            ("unknown command", false),
        ];

        for (cmd, should_be_valid) in commands {
            let is_valid = matches!(cmd, "quit" | "exit" | "help" | "stats" | "agents")
                || cmd.starts_with("analyze ")
                || cmd.starts_with("delegate ");

            assert_eq!(is_valid, should_be_valid);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:233-254 function test_cli_parsing_config_generate <-> ./crates/ccswarm/src/cli/tests.rs:291-318 function test_cli_parsing_session_create
  Similarity: 95.35%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_config_generate (lines 233-254) ---[0m
    fn test_cli_parsing_config_generate() {
        let args = parse_args(&[
            "config",
            "generate",
            "--output",
            "custom.json",
            "--template",
            "minimal",
        ])
        .unwrap();

        match args.command {
            Commands::Config { action } => match action {
                ConfigAction::Generate { output, template } => {
                    assert_eq!(output, PathBuf::from("custom.json"));
                    assert_eq!(template, "minimal");
                }
                _ => panic!("Expected Generate action"),
            },
            _ => panic!("Expected Config command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_session_create (lines 291-318) ---[0m
    fn test_cli_parsing_session_create() {
        let args = parse_args(&[
            "session",
            "create",
            "--agent",
            "frontend",
            "--workspace",
            "./workspace",
            "--background",
        ])
        .unwrap();

        match args.command {
            Commands::Session { action } => match action {
                SessionAction::Create {
                    agent,
                    workspace,
                    background,
                } => {
                    assert_eq!(agent, "frontend");
                    assert_eq!(workspace, Some("./workspace".to_string()));
                    assert!(background);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Session command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:257-288 function test_cli_parsing_delegate_task <-> ./crates/ccswarm/src/cli/tests.rs:448-467 function test_priority_parsing
  Similarity: 91.25%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_delegate_task (lines 257-288) ---[0m
    fn test_cli_parsing_delegate_task() {
        let args = parse_args(&[
            "delegate",
            "task",
            "Create API endpoint",
            "--agent",
            "backend",
            "--priority",
            "high",
            "--force",
        ])
        .unwrap();

        match args.command {
            Commands::Delegate { action } => match action {
                DelegateAction::Task {
                    description,
                    agent,
                    priority,
                    force,
                    ..
                } => {
                    assert_eq!(description, "Create API endpoint");
                    assert_eq!(agent, "backend");
                    assert_eq!(priority, "high");
                    assert!(force);
                }
                _ => panic!("Expected Task action"),
            },
            _ => panic!("Expected Delegate command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_priority_parsing (lines 448-467) ---[0m
    fn test_priority_parsing() {
        let test_cases = vec![
            ("low", Priority::Low),
            ("medium", Priority::Medium),
            ("high", Priority::High),
            ("critical", Priority::Critical),
            ("unknown", Priority::Medium), // Default
        ];

        for (input, expected) in test_cases {
            let priority = match input {
                "low" => Priority::Low,
                "medium" => Priority::Medium,
                "high" => Priority::High,
                "critical" => Priority::Critical,
                _ => Priority::Medium,
            };
            assert_eq!(priority, expected);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:15-39 function test_cli_parsing_init_command <-> ./crates/ccswarm/src/cli/tests.rs:186-211 function test_cli_parsing_worktree_create
  Similarity: 93.02%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_init_command (lines 15-39) ---[0m
    fn test_cli_parsing_init_command() {
        let args = parse_args(&[
            "init",
            "--name",
            "test-project",
            "--repo-url",
            "https://github.com/test/repo",
            "--agents",
            "frontend,backend",
        ])
        .unwrap();

        match args.command {
            Commands::Init {
                name,
                repo_url,
                agents,
            } => {
                assert_eq!(name, "test-project");
                assert_eq!(repo_url, Some("https://github.com/test/repo".to_string()));
                assert_eq!(agents, vec!["frontend", "backend"]);
            }
            _ => panic!("Expected Init command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_create (lines 186-211) ---[0m
    fn test_cli_parsing_worktree_create() {
        let args = parse_args(&[
            "worktree",
            "create",
            "/path/to/worktree",
            "feature-branch",
            "--new-branch",
        ])
        .unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::Create {
                    path,
                    branch,
                    new_branch,
                } => {
                    assert_eq!(path, PathBuf::from("/path/to/worktree"));
                    assert_eq!(branch, "feature-branch");
                    assert!(new_branch);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:42-59 function test_cli_parsing_start_command <-> ./crates/ccswarm/src/cli/tests.rs:257-288 function test_cli_parsing_delegate_task
  Similarity: 94.00%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command (lines 42-59) ---[0m
    fn test_cli_parsing_start_command() {
        let args = parse_args(&["start", "--daemon", "--port", "9090"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(daemon);
                assert_eq!(port, 9090);
                assert_eq!(isolation, "worktree"); // default value
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_delegate_task (lines 257-288) ---[0m
    fn test_cli_parsing_delegate_task() {
        let args = parse_args(&[
            "delegate",
            "task",
            "Create API endpoint",
            "--agent",
            "backend",
            "--priority",
            "high",
            "--force",
        ])
        .unwrap();

        match args.command {
            Commands::Delegate { action } => match action {
                DelegateAction::Task {
                    description,
                    agent,
                    priority,
                    force,
                    ..
                } => {
                    assert_eq!(description, "Create API endpoint");
                    assert_eq!(agent, "backend");
                    assert_eq!(priority, "high");
                    assert!(force);
                }
                _ => panic!("Expected Task action"),
            },
            _ => panic!("Expected Delegate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:62-79 function test_cli_parsing_start_command_with_isolation <-> ./crates/ccswarm/src/cli/tests.rs:257-288 function test_cli_parsing_delegate_task
  Similarity: 94.00%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command_with_isolation (lines 62-79) ---[0m
    fn test_cli_parsing_start_command_with_isolation() {
        let args = parse_args(&["start", "--isolation", "container"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(!daemon); // default false
                assert_eq!(port, 8080); // default port
                assert_eq!(isolation, "container");
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_delegate_task (lines 257-288) ---[0m
    fn test_cli_parsing_delegate_task() {
        let args = parse_args(&[
            "delegate",
            "task",
            "Create API endpoint",
            "--agent",
            "backend",
            "--priority",
            "high",
            "--force",
        ])
        .unwrap();

        match args.command {
            Commands::Delegate { action } => match action {
                DelegateAction::Task {
                    description,
                    agent,
                    priority,
                    force,
                    ..
                } => {
                    assert_eq!(description, "Create API endpoint");
                    assert_eq!(agent, "backend");
                    assert_eq!(priority, "high");
                    assert!(force);
                }
                _ => panic!("Expected Task action"),
            },
            _ => panic!("Expected Delegate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:214-230 function test_cli_parsing_logs_command <-> ./crates/ccswarm/src/cli/tests.rs:257-288 function test_cli_parsing_delegate_task
  Similarity: 94.00%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_logs_command (lines 214-230) ---[0m
    fn test_cli_parsing_logs_command() {
        let args =
            parse_args(&["logs", "--follow", "--agent", "frontend", "--lines", "200"]).unwrap();

        match args.command {
            Commands::Logs {
                follow,
                agent,
                lines,
            } => {
                assert!(follow);
                assert_eq!(agent, Some("frontend".to_string()));
                assert_eq!(lines, 200);
            }
            _ => panic!("Expected Logs command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_delegate_task (lines 257-288) ---[0m
    fn test_cli_parsing_delegate_task() {
        let args = parse_args(&[
            "delegate",
            "task",
            "Create API endpoint",
            "--agent",
            "backend",
            "--priority",
            "high",
            "--force",
        ])
        .unwrap();

        match args.command {
            Commands::Delegate { action } => match action {
                DelegateAction::Task {
                    description,
                    agent,
                    priority,
                    force,
                    ..
                } => {
                    assert_eq!(description, "Create API endpoint");
                    assert_eq!(agent, "backend");
                    assert_eq!(priority, "high");
                    assert!(force);
                }
                _ => panic!("Expected Task action"),
            },
            _ => panic!("Expected Delegate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:257-288 function test_cli_parsing_delegate_task <-> ./crates/ccswarm/src/cli/tests.rs:526-544 function test_delegation_strategy_parsing
  Similarity: 90.25%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_delegate_task (lines 257-288) ---[0m
    fn test_cli_parsing_delegate_task() {
        let args = parse_args(&[
            "delegate",
            "task",
            "Create API endpoint",
            "--agent",
            "backend",
            "--priority",
            "high",
            "--force",
        ])
        .unwrap();

        match args.command {
            Commands::Delegate { action } => match action {
                DelegateAction::Task {
                    description,
                    agent,
                    priority,
                    force,
                    ..
                } => {
                    assert_eq!(description, "Create API endpoint");
                    assert_eq!(agent, "backend");
                    assert_eq!(priority, "high");
                    assert!(force);
                }
                _ => panic!("Expected Task action"),
            },
            _ => panic!("Expected Delegate command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_delegation_strategy_parsing (lines 526-544) ---[0m
    fn test_delegation_strategy_parsing() {
        // Test strategy string parsing logic
        let test_cases = vec![
            ("content", true),
            ("load", true),
            ("expertise", true),
            ("workflow", true),
            ("hybrid", true),
            ("unknown", false), // Should default to hybrid
        ];

        for (input, is_valid_strategy) in test_cases {
            let is_valid = matches!(
                input,
                "content" | "load" | "expertise" | "workflow" | "hybrid"
            );
            assert_eq!(is_valid, is_valid_strategy);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:233-254 function test_cli_parsing_config_generate <-> ./crates/ccswarm/src/cli/tests.rs:321-347 function test_cli_parsing_auto_create
  Similarity: 93.10%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_config_generate (lines 233-254) ---[0m
    fn test_cli_parsing_config_generate() {
        let args = parse_args(&[
            "config",
            "generate",
            "--output",
            "custom.json",
            "--template",
            "minimal",
        ])
        .unwrap();

        match args.command {
            Commands::Config { action } => match action {
                ConfigAction::Generate { output, template } => {
                    assert_eq!(output, PathBuf::from("custom.json"));
                    assert_eq!(template, "minimal");
                }
                _ => panic!("Expected Generate action"),
            },
            _ => panic!("Expected Config command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_auto_create (lines 321-347) ---[0m
    fn test_cli_parsing_auto_create() {
        let args = parse_args(&[
            "auto-create",
            "Todo app with authentication",
            "--template",
            "full-stack",
            "--auto-deploy",
            "--output",
            "./my-app",
        ])
        .unwrap();

        match args.command {
            Commands::AutoCreate {
                description,
                template,
                auto_deploy,
                output,
            } => {
                assert_eq!(description, "Todo app with authentication");
                assert_eq!(template, Some("full-stack".to_string()));
                assert!(auto_deploy);
                assert_eq!(output, PathBuf::from("./my-app"));
            }
            _ => panic!("Expected AutoCreate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:186-211 function test_cli_parsing_worktree_create <-> ./crates/ccswarm/src/cli/tests.rs:233-254 function test_cli_parsing_config_generate
  Similarity: 95.00%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_create (lines 186-211) ---[0m
    fn test_cli_parsing_worktree_create() {
        let args = parse_args(&[
            "worktree",
            "create",
            "/path/to/worktree",
            "feature-branch",
            "--new-branch",
        ])
        .unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::Create {
                    path,
                    branch,
                    new_branch,
                } => {
                    assert_eq!(path, PathBuf::from("/path/to/worktree"));
                    assert_eq!(branch, "feature-branch");
                    assert!(new_branch);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_config_generate (lines 233-254) ---[0m
    fn test_cli_parsing_config_generate() {
        let args = parse_args(&[
            "config",
            "generate",
            "--output",
            "custom.json",
            "--template",
            "minimal",
        ])
        .unwrap();

        match args.command {
            Commands::Config { action } => match action {
                ConfigAction::Generate { output, template } => {
                    assert_eq!(output, PathBuf::from("custom.json"));
                    assert_eq!(template, "minimal");
                }
                _ => panic!("Expected Generate action"),
            },
            _ => panic!("Expected Config command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:291-318 function test_cli_parsing_session_create <-> ./crates/ccswarm/src/cli/tests.rs:560-580 function test_interactive_command_parsing
  Similarity: 89.84%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_session_create (lines 291-318) ---[0m
    fn test_cli_parsing_session_create() {
        let args = parse_args(&[
            "session",
            "create",
            "--agent",
            "frontend",
            "--workspace",
            "./workspace",
            "--background",
        ])
        .unwrap();

        match args.command {
            Commands::Session { action } => match action {
                SessionAction::Create {
                    agent,
                    workspace,
                    background,
                } => {
                    assert_eq!(agent, "frontend");
                    assert_eq!(workspace, Some("./workspace".to_string()));
                    assert!(background);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Session command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_interactive_command_parsing (lines 560-580) ---[0m
    fn test_interactive_command_parsing() {
        // Test various interactive commands
        let commands = vec![
            ("quit", true),
            ("exit", true),
            ("help", true),
            ("stats", true),
            ("agents", true),
            ("analyze task description", true),
            ("delegate frontend Create UI", true),
            ("unknown command", false),
        ];

        for (cmd, should_be_valid) in commands {
            let is_valid = matches!(cmd, "quit" | "exit" | "help" | "stats" | "agents")
                || cmd.starts_with("analyze ")
                || cmd.starts_with("delegate ");

            assert_eq!(is_valid, should_be_valid);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:15-39 function test_cli_parsing_init_command <-> ./crates/ccswarm/src/cli/tests.rs:233-254 function test_cli_parsing_config_generate
  Similarity: 92.77%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_init_command (lines 15-39) ---[0m
    fn test_cli_parsing_init_command() {
        let args = parse_args(&[
            "init",
            "--name",
            "test-project",
            "--repo-url",
            "https://github.com/test/repo",
            "--agents",
            "frontend,backend",
        ])
        .unwrap();

        match args.command {
            Commands::Init {
                name,
                repo_url,
                agents,
            } => {
                assert_eq!(name, "test-project");
                assert_eq!(repo_url, Some("https://github.com/test/repo".to_string()));
                assert_eq!(agents, vec!["frontend", "backend"]);
            }
            _ => panic!("Expected Init command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_config_generate (lines 233-254) ---[0m
    fn test_cli_parsing_config_generate() {
        let args = parse_args(&[
            "config",
            "generate",
            "--output",
            "custom.json",
            "--template",
            "minimal",
        ])
        .unwrap();

        match args.command {
            Commands::Config { action } => match action {
                ConfigAction::Generate { output, template } => {
                    assert_eq!(output, PathBuf::from("custom.json"));
                    assert_eq!(template, "minimal");
                }
                _ => panic!("Expected Generate action"),
            },
            _ => panic!("Expected Config command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:291-318 function test_cli_parsing_session_create <-> ./crates/ccswarm/src/cli/tests.rs:448-467 function test_priority_parsing
  Similarity: 90.54%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_session_create (lines 291-318) ---[0m
    fn test_cli_parsing_session_create() {
        let args = parse_args(&[
            "session",
            "create",
            "--agent",
            "frontend",
            "--workspace",
            "./workspace",
            "--background",
        ])
        .unwrap();

        match args.command {
            Commands::Session { action } => match action {
                SessionAction::Create {
                    agent,
                    workspace,
                    background,
                } => {
                    assert_eq!(agent, "frontend");
                    assert_eq!(workspace, Some("./workspace".to_string()));
                    assert!(background);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Session command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_priority_parsing (lines 448-467) ---[0m
    fn test_priority_parsing() {
        let test_cases = vec![
            ("low", Priority::Low),
            ("medium", Priority::Medium),
            ("high", Priority::High),
            ("critical", Priority::Critical),
            ("unknown", Priority::Medium), // Default
        ];

        for (input, expected) in test_cases {
            let priority = match input {
                "low" => Priority::Low,
                "medium" => Priority::Medium,
                "high" => Priority::High,
                "critical" => Priority::Critical,
                _ => Priority::Medium,
            };
            assert_eq!(priority, expected);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:321-347 function test_cli_parsing_auto_create <-> ./crates/ccswarm/src/cli/tests.rs:560-580 function test_interactive_command_parsing
  Similarity: 89.84%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_auto_create (lines 321-347) ---[0m
    fn test_cli_parsing_auto_create() {
        let args = parse_args(&[
            "auto-create",
            "Todo app with authentication",
            "--template",
            "full-stack",
            "--auto-deploy",
            "--output",
            "./my-app",
        ])
        .unwrap();

        match args.command {
            Commands::AutoCreate {
                description,
                template,
                auto_deploy,
                output,
            } => {
                assert_eq!(description, "Todo app with authentication");
                assert_eq!(template, Some("full-stack".to_string()));
                assert!(auto_deploy);
                assert_eq!(output, PathBuf::from("./my-app"));
            }
            _ => panic!("Expected AutoCreate command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_interactive_command_parsing (lines 560-580) ---[0m
    fn test_interactive_command_parsing() {
        // Test various interactive commands
        let commands = vec![
            ("quit", true),
            ("exit", true),
            ("help", true),
            ("stats", true),
            ("agents", true),
            ("analyze task description", true),
            ("delegate frontend Create UI", true),
            ("unknown command", false),
        ];

        for (cmd, should_be_valid) in commands {
            let is_valid = matches!(cmd, "quit" | "exit" | "help" | "stats" | "agents")
                || cmd.starts_with("analyze ")
                || cmd.starts_with("delegate ");

            assert_eq!(is_valid, should_be_valid);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:42-59 function test_cli_parsing_start_command <-> ./crates/ccswarm/src/cli/tests.rs:291-318 function test_cli_parsing_session_create
  Similarity: 93.51%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command (lines 42-59) ---[0m
    fn test_cli_parsing_start_command() {
        let args = parse_args(&["start", "--daemon", "--port", "9090"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(daemon);
                assert_eq!(port, 9090);
                assert_eq!(isolation, "worktree"); // default value
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_session_create (lines 291-318) ---[0m
    fn test_cli_parsing_session_create() {
        let args = parse_args(&[
            "session",
            "create",
            "--agent",
            "frontend",
            "--workspace",
            "./workspace",
            "--background",
        ])
        .unwrap();

        match args.command {
            Commands::Session { action } => match action {
                SessionAction::Create {
                    agent,
                    workspace,
                    background,
                } => {
                    assert_eq!(agent, "frontend");
                    assert_eq!(workspace, Some("./workspace".to_string()));
                    assert!(background);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Session command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:62-79 function test_cli_parsing_start_command_with_isolation <-> ./crates/ccswarm/src/cli/tests.rs:291-318 function test_cli_parsing_session_create
  Similarity: 93.51%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command_with_isolation (lines 62-79) ---[0m
    fn test_cli_parsing_start_command_with_isolation() {
        let args = parse_args(&["start", "--isolation", "container"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(!daemon); // default false
                assert_eq!(port, 8080); // default port
                assert_eq!(isolation, "container");
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_session_create (lines 291-318) ---[0m
    fn test_cli_parsing_session_create() {
        let args = parse_args(&[
            "session",
            "create",
            "--agent",
            "frontend",
            "--workspace",
            "./workspace",
            "--background",
        ])
        .unwrap();

        match args.command {
            Commands::Session { action } => match action {
                SessionAction::Create {
                    agent,
                    workspace,
                    background,
                } => {
                    assert_eq!(agent, "frontend");
                    assert_eq!(workspace, Some("./workspace".to_string()));
                    assert!(background);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Session command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:105-145 function test_cli_parsing_task_command <-> ./crates/ccswarm/src/cli/tests.rs:426-445 function test_cli_runner_new_missing_config
  Similarity: 70.19%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_task_command (lines 105-145) ---[0m
    fn test_cli_parsing_task_command() {
        let args = parse_args(&[
            "task",
            "add",
            "Implement login feature",
            "--priority",
            "high",
            "--task-type",
            "feature",
            "--details",
            "Add OAuth support",
            "--duration",
            "7200",
        ])
        .unwrap();

        match args.command {
            Commands::Task { action } => {
                match action {
                    crate::cli::TaskAction::Add {
                        description,
                        priority,
                        task_type,
                        details,
                        duration,
                        auto_assign,
                        ..
                    } => {
                        assert_eq!(description, "Implement login feature");
                        assert_eq!(priority, "high");
                        assert_eq!(task_type, "feature");
                        assert_eq!(details, Some("Add OAuth support".to_string()));
                        assert_eq!(duration, Some(7200));
                        assert!(!auto_assign); // Default should be false
                    }
                    _ => panic!("Expected Add task action"),
                }
            }
            _ => panic!("Expected Task command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_runner_new_missing_config (lines 426-445) ---[0m
    async fn test_cli_runner_new_missing_config() {
        let temp_dir = TempDir::new().unwrap();
        let config_path = temp_dir.path().join("nonexistent.json");

        let cli = Cli {
            config: config_path,
            repo: temp_dir.path().to_path_buf(),
            verbose: false,
            json: false,
            fix: false,
            command: Commands::Status {
                detailed: false,
                agent: None,
            },
        };

        // Should use default config when file doesn't exist
        let runner = CliRunner::new(&cli).await.unwrap();
        assert_eq!(runner.config.project.name, "New ccswarm Project");
    }

  ./crates/ccswarm/src/cli/tests.rs:321-347 function test_cli_parsing_auto_create <-> ./crates/ccswarm/src/cli/tests.rs:448-467 function test_priority_parsing
  Similarity: 89.94%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_auto_create (lines 321-347) ---[0m
    fn test_cli_parsing_auto_create() {
        let args = parse_args(&[
            "auto-create",
            "Todo app with authentication",
            "--template",
            "full-stack",
            "--auto-deploy",
            "--output",
            "./my-app",
        ])
        .unwrap();

        match args.command {
            Commands::AutoCreate {
                description,
                template,
                auto_deploy,
                output,
            } => {
                assert_eq!(description, "Todo app with authentication");
                assert_eq!(template, Some("full-stack".to_string()));
                assert!(auto_deploy);
                assert_eq!(output, PathBuf::from("./my-app"));
            }
            _ => panic!("Expected AutoCreate command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_priority_parsing (lines 448-467) ---[0m
    fn test_priority_parsing() {
        let test_cases = vec![
            ("low", Priority::Low),
            ("medium", Priority::Medium),
            ("high", Priority::High),
            ("critical", Priority::Critical),
            ("unknown", Priority::Medium), // Default
        ];

        for (input, expected) in test_cases {
            let priority = match input {
                "low" => Priority::Low,
                "medium" => Priority::Medium,
                "high" => Priority::High,
                "critical" => Priority::Critical,
                _ => Priority::Medium,
            };
            assert_eq!(priority, expected);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:186-211 function test_cli_parsing_worktree_create <-> ./crates/ccswarm/src/cli/tests.rs:560-580 function test_interactive_command_parsing
  Similarity: 89.84%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_create (lines 186-211) ---[0m
    fn test_cli_parsing_worktree_create() {
        let args = parse_args(&[
            "worktree",
            "create",
            "/path/to/worktree",
            "feature-branch",
            "--new-branch",
        ])
        .unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::Create {
                    path,
                    branch,
                    new_branch,
                } => {
                    assert_eq!(path, PathBuf::from("/path/to/worktree"));
                    assert_eq!(branch, "feature-branch");
                    assert!(new_branch);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_interactive_command_parsing (lines 560-580) ---[0m
    fn test_interactive_command_parsing() {
        // Test various interactive commands
        let commands = vec![
            ("quit", true),
            ("exit", true),
            ("help", true),
            ("stats", true),
            ("agents", true),
            ("analyze task description", true),
            ("delegate frontend Create UI", true),
            ("unknown command", false),
        ];

        for (cmd, should_be_valid) in commands {
            let is_valid = matches!(cmd, "quit" | "exit" | "help" | "stats" | "agents")
                || cmd.starts_with("analyze ")
                || cmd.starts_with("delegate ");

            assert_eq!(is_valid, should_be_valid);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:214-230 function test_cli_parsing_logs_command <-> ./crates/ccswarm/src/cli/tests.rs:291-318 function test_cli_parsing_session_create
  Similarity: 93.51%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_logs_command (lines 214-230) ---[0m
    fn test_cli_parsing_logs_command() {
        let args =
            parse_args(&["logs", "--follow", "--agent", "frontend", "--lines", "200"]).unwrap();

        match args.command {
            Commands::Logs {
                follow,
                agent,
                lines,
            } => {
                assert!(follow);
                assert_eq!(agent, Some("frontend".to_string()));
                assert_eq!(lines, 200);
            }
            _ => panic!("Expected Logs command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_session_create (lines 291-318) ---[0m
    fn test_cli_parsing_session_create() {
        let args = parse_args(&[
            "session",
            "create",
            "--agent",
            "frontend",
            "--workspace",
            "./workspace",
            "--background",
        ])
        .unwrap();

        match args.command {
            Commands::Session { action } => match action {
                SessionAction::Create {
                    agent,
                    workspace,
                    background,
                } => {
                    assert_eq!(agent, "frontend");
                    assert_eq!(workspace, Some("./workspace".to_string()));
                    assert!(background);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Session command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:291-318 function test_cli_parsing_session_create <-> ./crates/ccswarm/src/cli/tests.rs:526-544 function test_delegation_strategy_parsing
  Similarity: 89.46%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_session_create (lines 291-318) ---[0m
    fn test_cli_parsing_session_create() {
        let args = parse_args(&[
            "session",
            "create",
            "--agent",
            "frontend",
            "--workspace",
            "./workspace",
            "--background",
        ])
        .unwrap();

        match args.command {
            Commands::Session { action } => match action {
                SessionAction::Create {
                    agent,
                    workspace,
                    background,
                } => {
                    assert_eq!(agent, "frontend");
                    assert_eq!(workspace, Some("./workspace".to_string()));
                    assert!(background);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Session command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_delegation_strategy_parsing (lines 526-544) ---[0m
    fn test_delegation_strategy_parsing() {
        // Test strategy string parsing logic
        let test_cases = vec![
            ("content", true),
            ("load", true),
            ("expertise", true),
            ("workflow", true),
            ("hybrid", true),
            ("unknown", false), // Should default to hybrid
        ];

        for (input, is_valid_strategy) in test_cases {
            let is_valid = matches!(
                input,
                "content" | "load" | "expertise" | "workflow" | "hybrid"
            );
            assert_eq!(is_valid, is_valid_strategy);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:42-59 function test_cli_parsing_start_command <-> ./crates/ccswarm/src/cli/tests.rs:321-347 function test_cli_parsing_auto_create
  Similarity: 93.10%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command (lines 42-59) ---[0m
    fn test_cli_parsing_start_command() {
        let args = parse_args(&["start", "--daemon", "--port", "9090"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(daemon);
                assert_eq!(port, 9090);
                assert_eq!(isolation, "worktree"); // default value
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_auto_create (lines 321-347) ---[0m
    fn test_cli_parsing_auto_create() {
        let args = parse_args(&[
            "auto-create",
            "Todo app with authentication",
            "--template",
            "full-stack",
            "--auto-deploy",
            "--output",
            "./my-app",
        ])
        .unwrap();

        match args.command {
            Commands::AutoCreate {
                description,
                template,
                auto_deploy,
                output,
            } => {
                assert_eq!(description, "Todo app with authentication");
                assert_eq!(template, Some("full-stack".to_string()));
                assert!(auto_deploy);
                assert_eq!(output, PathBuf::from("./my-app"));
            }
            _ => panic!("Expected AutoCreate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:62-79 function test_cli_parsing_start_command_with_isolation <-> ./crates/ccswarm/src/cli/tests.rs:321-347 function test_cli_parsing_auto_create
  Similarity: 93.10%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command_with_isolation (lines 62-79) ---[0m
    fn test_cli_parsing_start_command_with_isolation() {
        let args = parse_args(&["start", "--isolation", "container"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(!daemon); // default false
                assert_eq!(port, 8080); // default port
                assert_eq!(isolation, "container");
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_auto_create (lines 321-347) ---[0m
    fn test_cli_parsing_auto_create() {
        let args = parse_args(&[
            "auto-create",
            "Todo app with authentication",
            "--template",
            "full-stack",
            "--auto-deploy",
            "--output",
            "./my-app",
        ])
        .unwrap();

        match args.command {
            Commands::AutoCreate {
                description,
                template,
                auto_deploy,
                output,
            } => {
                assert_eq!(description, "Todo app with authentication");
                assert_eq!(template, Some("full-stack".to_string()));
                assert!(auto_deploy);
                assert_eq!(output, PathBuf::from("./my-app"));
            }
            _ => panic!("Expected AutoCreate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:15-39 function test_cli_parsing_init_command <-> ./crates/ccswarm/src/cli/tests.rs:560-580 function test_interactive_command_parsing
  Similarity: 89.84%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_init_command (lines 15-39) ---[0m
    fn test_cli_parsing_init_command() {
        let args = parse_args(&[
            "init",
            "--name",
            "test-project",
            "--repo-url",
            "https://github.com/test/repo",
            "--agents",
            "frontend,backend",
        ])
        .unwrap();

        match args.command {
            Commands::Init {
                name,
                repo_url,
                agents,
            } => {
                assert_eq!(name, "test-project");
                assert_eq!(repo_url, Some("https://github.com/test/repo".to_string()));
                assert_eq!(agents, vec!["frontend", "backend"]);
            }
            _ => panic!("Expected Init command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_interactive_command_parsing (lines 560-580) ---[0m
    fn test_interactive_command_parsing() {
        // Test various interactive commands
        let commands = vec![
            ("quit", true),
            ("exit", true),
            ("help", true),
            ("stats", true),
            ("agents", true),
            ("analyze task description", true),
            ("delegate frontend Create UI", true),
            ("unknown command", false),
        ];

        for (cmd, should_be_valid) in commands {
            let is_valid = matches!(cmd, "quit" | "exit" | "help" | "stats" | "agents")
                || cmd.starts_with("analyze ")
                || cmd.starts_with("delegate ");

            assert_eq!(is_valid, should_be_valid);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:186-211 function test_cli_parsing_worktree_create <-> ./crates/ccswarm/src/cli/tests.rs:448-467 function test_priority_parsing
  Similarity: 89.83%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_create (lines 186-211) ---[0m
    fn test_cli_parsing_worktree_create() {
        let args = parse_args(&[
            "worktree",
            "create",
            "/path/to/worktree",
            "feature-branch",
            "--new-branch",
        ])
        .unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::Create {
                    path,
                    branch,
                    new_branch,
                } => {
                    assert_eq!(path, PathBuf::from("/path/to/worktree"));
                    assert_eq!(branch, "feature-branch");
                    assert!(new_branch);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_priority_parsing (lines 448-467) ---[0m
    fn test_priority_parsing() {
        let test_cases = vec![
            ("low", Priority::Low),
            ("medium", Priority::Medium),
            ("high", Priority::High),
            ("critical", Priority::Critical),
            ("unknown", Priority::Medium), // Default
        ];

        for (input, expected) in test_cases {
            let priority = match input {
                "low" => Priority::Low,
                "medium" => Priority::Medium,
                "high" => Priority::High,
                "critical" => Priority::Critical,
                _ => Priority::Medium,
            };
            assert_eq!(priority, expected);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:173-183 function test_cli_parsing_worktree_list <-> ./crates/ccswarm/src/cli/tests.rs:257-288 function test_cli_parsing_delegate_task
  Similarity: 95.70%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_list (lines 173-183) ---[0m
    fn test_cli_parsing_worktree_list() {
        let args = parse_args(&["worktree", "list"]).unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::List => {}
                _ => panic!("Expected List action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_delegate_task (lines 257-288) ---[0m
    fn test_cli_parsing_delegate_task() {
        let args = parse_args(&[
            "delegate",
            "task",
            "Create API endpoint",
            "--agent",
            "backend",
            "--priority",
            "high",
            "--force",
        ])
        .unwrap();

        match args.command {
            Commands::Delegate { action } => match action {
                DelegateAction::Task {
                    description,
                    agent,
                    priority,
                    force,
                    ..
                } => {
                    assert_eq!(description, "Create API endpoint");
                    assert_eq!(agent, "backend");
                    assert_eq!(priority, "high");
                    assert!(force);
                }
                _ => panic!("Expected Task action"),
            },
            _ => panic!("Expected Delegate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:214-230 function test_cli_parsing_logs_command <-> ./crates/ccswarm/src/cli/tests.rs:321-347 function test_cli_parsing_auto_create
  Similarity: 93.10%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_logs_command (lines 214-230) ---[0m
    fn test_cli_parsing_logs_command() {
        let args =
            parse_args(&["logs", "--follow", "--agent", "frontend", "--lines", "200"]).unwrap();

        match args.command {
            Commands::Logs {
                follow,
                agent,
                lines,
            } => {
                assert!(follow);
                assert_eq!(agent, Some("frontend".to_string()));
                assert_eq!(lines, 200);
            }
            _ => panic!("Expected Logs command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_auto_create (lines 321-347) ---[0m
    fn test_cli_parsing_auto_create() {
        let args = parse_args(&[
            "auto-create",
            "Todo app with authentication",
            "--template",
            "full-stack",
            "--auto-deploy",
            "--output",
            "./my-app",
        ])
        .unwrap();

        match args.command {
            Commands::AutoCreate {
                description,
                template,
                auto_deploy,
                output,
            } => {
                assert_eq!(description, "Todo app with authentication");
                assert_eq!(template, Some("full-stack".to_string()));
                assert!(auto_deploy);
                assert_eq!(output, PathBuf::from("./my-app"));
            }
            _ => panic!("Expected AutoCreate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:42-59 function test_cli_parsing_start_command <-> ./crates/ccswarm/src/cli/tests.rs:186-211 function test_cli_parsing_worktree_create
  Similarity: 93.02%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command (lines 42-59) ---[0m
    fn test_cli_parsing_start_command() {
        let args = parse_args(&["start", "--daemon", "--port", "9090"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(daemon);
                assert_eq!(port, 9090);
                assert_eq!(isolation, "worktree"); // default value
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_create (lines 186-211) ---[0m
    fn test_cli_parsing_worktree_create() {
        let args = parse_args(&[
            "worktree",
            "create",
            "/path/to/worktree",
            "feature-branch",
            "--new-branch",
        ])
        .unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::Create {
                    path,
                    branch,
                    new_branch,
                } => {
                    assert_eq!(path, PathBuf::from("/path/to/worktree"));
                    assert_eq!(branch, "feature-branch");
                    assert!(new_branch);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:62-79 function test_cli_parsing_start_command_with_isolation <-> ./crates/ccswarm/src/cli/tests.rs:186-211 function test_cli_parsing_worktree_create
  Similarity: 93.02%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command_with_isolation (lines 62-79) ---[0m
    fn test_cli_parsing_start_command_with_isolation() {
        let args = parse_args(&["start", "--isolation", "container"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(!daemon); // default false
                assert_eq!(port, 8080); // default port
                assert_eq!(isolation, "container");
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_create (lines 186-211) ---[0m
    fn test_cli_parsing_worktree_create() {
        let args = parse_args(&[
            "worktree",
            "create",
            "/path/to/worktree",
            "feature-branch",
            "--new-branch",
        ])
        .unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::Create {
                    path,
                    branch,
                    new_branch,
                } => {
                    assert_eq!(path, PathBuf::from("/path/to/worktree"));
                    assert_eq!(branch, "feature-branch");
                    assert!(new_branch);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:321-347 function test_cli_parsing_auto_create <-> ./crates/ccswarm/src/cli/tests.rs:526-544 function test_delegation_strategy_parsing
  Similarity: 88.79%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_auto_create (lines 321-347) ---[0m
    fn test_cli_parsing_auto_create() {
        let args = parse_args(&[
            "auto-create",
            "Todo app with authentication",
            "--template",
            "full-stack",
            "--auto-deploy",
            "--output",
            "./my-app",
        ])
        .unwrap();

        match args.command {
            Commands::AutoCreate {
                description,
                template,
                auto_deploy,
                output,
            } => {
                assert_eq!(description, "Todo app with authentication");
                assert_eq!(template, Some("full-stack".to_string()));
                assert!(auto_deploy);
                assert_eq!(output, PathBuf::from("./my-app"));
            }
            _ => panic!("Expected AutoCreate command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_delegation_strategy_parsing (lines 526-544) ---[0m
    fn test_delegation_strategy_parsing() {
        // Test strategy string parsing logic
        let test_cases = vec![
            ("content", true),
            ("load", true),
            ("expertise", true),
            ("workflow", true),
            ("hybrid", true),
            ("unknown", false), // Should default to hybrid
        ];

        for (input, is_valid_strategy) in test_cases {
            let is_valid = matches!(
                input,
                "content" | "load" | "expertise" | "workflow" | "hybrid"
            );
            assert_eq!(is_valid, is_valid_strategy);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:92-102 function test_cli_parsing_status_command <-> ./crates/ccswarm/src/cli/tests.rs:257-288 function test_cli_parsing_delegate_task
  Similarity: 94.70%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_status_command (lines 92-102) ---[0m
    fn test_cli_parsing_status_command() {
        let args = parse_args(&["status", "--detailed", "--agent", "frontend"]).unwrap();

        match args.command {
            Commands::Status { detailed, agent } => {
                assert!(detailed);
                assert_eq!(agent, Some("frontend".to_string()));
            }
            _ => panic!("Expected Status command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_delegate_task (lines 257-288) ---[0m
    fn test_cli_parsing_delegate_task() {
        let args = parse_args(&[
            "delegate",
            "task",
            "Create API endpoint",
            "--agent",
            "backend",
            "--priority",
            "high",
            "--force",
        ])
        .unwrap();

        match args.command {
            Commands::Delegate { action } => match action {
                DelegateAction::Task {
                    description,
                    agent,
                    priority,
                    force,
                    ..
                } => {
                    assert_eq!(description, "Create API endpoint");
                    assert_eq!(agent, "backend");
                    assert_eq!(priority, "high");
                    assert!(force);
                }
                _ => panic!("Expected Task action"),
            },
            _ => panic!("Expected Delegate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:160-170 function test_cli_parsing_review_command <-> ./crates/ccswarm/src/cli/tests.rs:257-288 function test_cli_parsing_delegate_task
  Similarity: 94.70%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_review_command (lines 160-170) ---[0m
    fn test_cli_parsing_review_command() {
        let args = parse_args(&["review", "--agent", "backend", "--strict"]).unwrap();

        match args.command {
            Commands::Review { agent, strict } => {
                assert_eq!(agent, Some("backend".to_string()));
                assert!(strict);
            }
            _ => panic!("Expected Review command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_delegate_task (lines 257-288) ---[0m
    fn test_cli_parsing_delegate_task() {
        let args = parse_args(&[
            "delegate",
            "task",
            "Create API endpoint",
            "--agent",
            "backend",
            "--priority",
            "high",
            "--force",
        ])
        .unwrap();

        match args.command {
            Commands::Delegate { action } => match action {
                DelegateAction::Task {
                    description,
                    agent,
                    priority,
                    force,
                    ..
                } => {
                    assert_eq!(description, "Create API endpoint");
                    assert_eq!(agent, "backend");
                    assert_eq!(priority, "high");
                    assert!(force);
                }
                _ => panic!("Expected Task action"),
            },
            _ => panic!("Expected Delegate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:15-39 function test_cli_parsing_init_command <-> ./crates/ccswarm/src/cli/tests.rs:448-467 function test_priority_parsing
  Similarity: 89.46%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_init_command (lines 15-39) ---[0m
    fn test_cli_parsing_init_command() {
        let args = parse_args(&[
            "init",
            "--name",
            "test-project",
            "--repo-url",
            "https://github.com/test/repo",
            "--agents",
            "frontend,backend",
        ])
        .unwrap();

        match args.command {
            Commands::Init {
                name,
                repo_url,
                agents,
            } => {
                assert_eq!(name, "test-project");
                assert_eq!(repo_url, Some("https://github.com/test/repo".to_string()));
                assert_eq!(agents, vec!["frontend", "backend"]);
            }
            _ => panic!("Expected Init command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_priority_parsing (lines 448-467) ---[0m
    fn test_priority_parsing() {
        let test_cases = vec![
            ("low", Priority::Low),
            ("medium", Priority::Medium),
            ("high", Priority::High),
            ("critical", Priority::Critical),
            ("unknown", Priority::Medium), // Default
        ];

        for (input, expected) in test_cases {
            let priority = match input {
                "low" => Priority::Low,
                "medium" => Priority::Medium,
                "high" => Priority::High,
                "critical" => Priority::Critical,
                _ => Priority::Medium,
            };
            assert_eq!(priority, expected);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:186-211 function test_cli_parsing_worktree_create <-> ./crates/ccswarm/src/cli/tests.rs:214-230 function test_cli_parsing_logs_command
  Similarity: 93.31%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_create (lines 186-211) ---[0m
    fn test_cli_parsing_worktree_create() {
        let args = parse_args(&[
            "worktree",
            "create",
            "/path/to/worktree",
            "feature-branch",
            "--new-branch",
        ])
        .unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::Create {
                    path,
                    branch,
                    new_branch,
                } => {
                    assert_eq!(path, PathBuf::from("/path/to/worktree"));
                    assert_eq!(branch, "feature-branch");
                    assert!(new_branch);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_logs_command (lines 214-230) ---[0m
    fn test_cli_parsing_logs_command() {
        let args =
            parse_args(&["logs", "--follow", "--agent", "frontend", "--lines", "200"]).unwrap();

        match args.command {
            Commands::Logs {
                follow,
                agent,
                lines,
            } => {
                assert!(follow);
                assert_eq!(agent, Some("frontend".to_string()));
                assert_eq!(lines, 200);
            }
            _ => panic!("Expected Logs command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:186-211 function test_cli_parsing_worktree_create <-> ./crates/ccswarm/src/cli/tests.rs:526-544 function test_delegation_strategy_parsing
  Similarity: 88.66%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_create (lines 186-211) ---[0m
    fn test_cli_parsing_worktree_create() {
        let args = parse_args(&[
            "worktree",
            "create",
            "/path/to/worktree",
            "feature-branch",
            "--new-branch",
        ])
        .unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::Create {
                    path,
                    branch,
                    new_branch,
                } => {
                    assert_eq!(path, PathBuf::from("/path/to/worktree"));
                    assert_eq!(branch, "feature-branch");
                    assert!(new_branch);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_delegation_strategy_parsing (lines 526-544) ---[0m
    fn test_delegation_strategy_parsing() {
        // Test strategy string parsing logic
        let test_cases = vec![
            ("content", true),
            ("load", true),
            ("expertise", true),
            ("workflow", true),
            ("hybrid", true),
            ("unknown", false), // Should default to hybrid
        ];

        for (input, is_valid_strategy) in test_cases {
            let is_valid = matches!(
                input,
                "content" | "load" | "expertise" | "workflow" | "hybrid"
            );
            assert_eq!(is_valid, is_valid_strategy);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:15-39 function test_cli_parsing_init_command <-> ./crates/ccswarm/src/cli/tests.rs:42-59 function test_cli_parsing_start_command
  Similarity: 92.77%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_init_command (lines 15-39) ---[0m
    fn test_cli_parsing_init_command() {
        let args = parse_args(&[
            "init",
            "--name",
            "test-project",
            "--repo-url",
            "https://github.com/test/repo",
            "--agents",
            "frontend,backend",
        ])
        .unwrap();

        match args.command {
            Commands::Init {
                name,
                repo_url,
                agents,
            } => {
                assert_eq!(name, "test-project");
                assert_eq!(repo_url, Some("https://github.com/test/repo".to_string()));
                assert_eq!(agents, vec!["frontend", "backend"]);
            }
            _ => panic!("Expected Init command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command (lines 42-59) ---[0m
    fn test_cli_parsing_start_command() {
        let args = parse_args(&["start", "--daemon", "--port", "9090"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(daemon);
                assert_eq!(port, 9090);
                assert_eq!(isolation, "worktree"); // default value
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:15-39 function test_cli_parsing_init_command <-> ./crates/ccswarm/src/cli/tests.rs:62-79 function test_cli_parsing_start_command_with_isolation
  Similarity: 92.77%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_init_command (lines 15-39) ---[0m
    fn test_cli_parsing_init_command() {
        let args = parse_args(&[
            "init",
            "--name",
            "test-project",
            "--repo-url",
            "https://github.com/test/repo",
            "--agents",
            "frontend,backend",
        ])
        .unwrap();

        match args.command {
            Commands::Init {
                name,
                repo_url,
                agents,
            } => {
                assert_eq!(name, "test-project");
                assert_eq!(repo_url, Some("https://github.com/test/repo".to_string()));
                assert_eq!(agents, vec!["frontend", "backend"]);
            }
            _ => panic!("Expected Init command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command_with_isolation (lines 62-79) ---[0m
    fn test_cli_parsing_start_command_with_isolation() {
        let args = parse_args(&["start", "--isolation", "container"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(!daemon); // default false
                assert_eq!(port, 8080); // default port
                assert_eq!(isolation, "container");
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:15-39 function test_cli_parsing_init_command <-> ./crates/ccswarm/src/cli/tests.rs:214-230 function test_cli_parsing_logs_command
  Similarity: 92.89%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_init_command (lines 15-39) ---[0m
    fn test_cli_parsing_init_command() {
        let args = parse_args(&[
            "init",
            "--name",
            "test-project",
            "--repo-url",
            "https://github.com/test/repo",
            "--agents",
            "frontend,backend",
        ])
        .unwrap();

        match args.command {
            Commands::Init {
                name,
                repo_url,
                agents,
            } => {
                assert_eq!(name, "test-project");
                assert_eq!(repo_url, Some("https://github.com/test/repo".to_string()));
                assert_eq!(agents, vec!["frontend", "backend"]);
            }
            _ => panic!("Expected Init command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_logs_command (lines 214-230) ---[0m
    fn test_cli_parsing_logs_command() {
        let args =
            parse_args(&["logs", "--follow", "--agent", "frontend", "--lines", "200"]).unwrap();

        match args.command {
            Commands::Logs {
                follow,
                agent,
                lines,
            } => {
                assert!(follow);
                assert_eq!(agent, Some("frontend".to_string()));
                assert_eq!(lines, 200);
            }
            _ => panic!("Expected Logs command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:15-39 function test_cli_parsing_init_command <-> ./crates/ccswarm/src/cli/tests.rs:526-544 function test_delegation_strategy_parsing
  Similarity: 88.25%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_init_command (lines 15-39) ---[0m
    fn test_cli_parsing_init_command() {
        let args = parse_args(&[
            "init",
            "--name",
            "test-project",
            "--repo-url",
            "https://github.com/test/repo",
            "--agents",
            "frontend,backend",
        ])
        .unwrap();

        match args.command {
            Commands::Init {
                name,
                repo_url,
                agents,
            } => {
                assert_eq!(name, "test-project");
                assert_eq!(repo_url, Some("https://github.com/test/repo".to_string()));
                assert_eq!(agents, vec!["frontend", "backend"]);
            }
            _ => panic!("Expected Init command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_delegation_strategy_parsing (lines 526-544) ---[0m
    fn test_delegation_strategy_parsing() {
        // Test strategy string parsing logic
        let test_cases = vec![
            ("content", true),
            ("load", true),
            ("expertise", true),
            ("workflow", true),
            ("hybrid", true),
            ("unknown", false), // Should default to hybrid
        ];

        for (input, is_valid_strategy) in test_cases {
            let is_valid = matches!(
                input,
                "content" | "load" | "expertise" | "workflow" | "hybrid"
            );
            assert_eq!(is_valid, is_valid_strategy);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:233-254 function test_cli_parsing_config_generate <-> ./crates/ccswarm/src/cli/tests.rs:560-580 function test_interactive_command_parsing
  Similarity: 89.84%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_config_generate (lines 233-254) ---[0m
    fn test_cli_parsing_config_generate() {
        let args = parse_args(&[
            "config",
            "generate",
            "--output",
            "custom.json",
            "--template",
            "minimal",
        ])
        .unwrap();

        match args.command {
            Commands::Config { action } => match action {
                ConfigAction::Generate { output, template } => {
                    assert_eq!(output, PathBuf::from("custom.json"));
                    assert_eq!(template, "minimal");
                }
                _ => panic!("Expected Generate action"),
            },
            _ => panic!("Expected Config command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_interactive_command_parsing (lines 560-580) ---[0m
    fn test_interactive_command_parsing() {
        // Test various interactive commands
        let commands = vec![
            ("quit", true),
            ("exit", true),
            ("help", true),
            ("stats", true),
            ("agents", true),
            ("analyze task description", true),
            ("delegate frontend Create UI", true),
            ("unknown command", false),
        ];

        for (cmd, should_be_valid) in commands {
            let is_valid = matches!(cmd, "quit" | "exit" | "help" | "stats" | "agents")
                || cmd.starts_with("analyze ")
                || cmd.starts_with("delegate ");

            assert_eq!(is_valid, should_be_valid);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:448-467 function test_priority_parsing <-> ./crates/ccswarm/src/cli/tests.rs:560-580 function test_interactive_command_parsing
  Similarity: 93.70%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_priority_parsing (lines 448-467) ---[0m
    fn test_priority_parsing() {
        let test_cases = vec![
            ("low", Priority::Low),
            ("medium", Priority::Medium),
            ("high", Priority::High),
            ("critical", Priority::Critical),
            ("unknown", Priority::Medium), // Default
        ];

        for (input, expected) in test_cases {
            let priority = match input {
                "low" => Priority::Low,
                "medium" => Priority::Medium,
                "high" => Priority::High,
                "critical" => Priority::Critical,
                _ => Priority::Medium,
            };
            assert_eq!(priority, expected);
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_interactive_command_parsing (lines 560-580) ---[0m
    fn test_interactive_command_parsing() {
        // Test various interactive commands
        let commands = vec![
            ("quit", true),
            ("exit", true),
            ("help", true),
            ("stats", true),
            ("agents", true),
            ("analyze task description", true),
            ("delegate frontend Create UI", true),
            ("unknown command", false),
        ];

        for (cmd, should_be_valid) in commands {
            let is_valid = matches!(cmd, "quit" | "exit" | "help" | "stats" | "agents")
                || cmd.starts_with("analyze ")
                || cmd.starts_with("delegate ");

            assert_eq!(is_valid, should_be_valid);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:526-544 function test_delegation_strategy_parsing <-> ./crates/ccswarm/src/cli/tests.rs:560-580 function test_interactive_command_parsing
  Similarity: 95.05%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_delegation_strategy_parsing (lines 526-544) ---[0m
    fn test_delegation_strategy_parsing() {
        // Test strategy string parsing logic
        let test_cases = vec![
            ("content", true),
            ("load", true),
            ("expertise", true),
            ("workflow", true),
            ("hybrid", true),
            ("unknown", false), // Should default to hybrid
        ];

        for (input, is_valid_strategy) in test_cases {
            let is_valid = matches!(
                input,
                "content" | "load" | "expertise" | "workflow" | "hybrid"
            );
            assert_eq!(is_valid, is_valid_strategy);
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_interactive_command_parsing (lines 560-580) ---[0m
    fn test_interactive_command_parsing() {
        // Test various interactive commands
        let commands = vec![
            ("quit", true),
            ("exit", true),
            ("help", true),
            ("stats", true),
            ("agents", true),
            ("analyze task description", true),
            ("delegate frontend Create UI", true),
            ("unknown command", false),
        ];

        for (cmd, should_be_valid) in commands {
            let is_valid = matches!(cmd, "quit" | "exit" | "help" | "stats" | "agents")
                || cmd.starts_with("analyze ")
                || cmd.starts_with("delegate ");

            assert_eq!(is_valid, should_be_valid);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:233-254 function test_cli_parsing_config_generate <-> ./crates/ccswarm/src/cli/tests.rs:448-467 function test_priority_parsing
  Similarity: 89.39%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_config_generate (lines 233-254) ---[0m
    fn test_cli_parsing_config_generate() {
        let args = parse_args(&[
            "config",
            "generate",
            "--output",
            "custom.json",
            "--template",
            "minimal",
        ])
        .unwrap();

        match args.command {
            Commands::Config { action } => match action {
                ConfigAction::Generate { output, template } => {
                    assert_eq!(output, PathBuf::from("custom.json"));
                    assert_eq!(template, "minimal");
                }
                _ => panic!("Expected Generate action"),
            },
            _ => panic!("Expected Config command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_priority_parsing (lines 448-467) ---[0m
    fn test_priority_parsing() {
        let test_cases = vec![
            ("low", Priority::Low),
            ("medium", Priority::Medium),
            ("high", Priority::High),
            ("critical", Priority::Critical),
            ("unknown", Priority::Medium), // Default
        ];

        for (input, expected) in test_cases {
            let priority = match input {
                "low" => Priority::Low,
                "medium" => Priority::Medium,
                "high" => Priority::High,
                "critical" => Priority::Critical,
                _ => Priority::Medium,
            };
            assert_eq!(priority, expected);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:173-183 function test_cli_parsing_worktree_list <-> ./crates/ccswarm/src/cli/tests.rs:291-318 function test_cli_parsing_session_create
  Similarity: 95.35%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_list (lines 173-183) ---[0m
    fn test_cli_parsing_worktree_list() {
        let args = parse_args(&["worktree", "list"]).unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::List => {}
                _ => panic!("Expected List action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_session_create (lines 291-318) ---[0m
    fn test_cli_parsing_session_create() {
        let args = parse_args(&[
            "session",
            "create",
            "--agent",
            "frontend",
            "--workspace",
            "./workspace",
            "--background",
        ])
        .unwrap();

        match args.command {
            Commands::Session { action } => match action {
                SessionAction::Create {
                    agent,
                    workspace,
                    background,
                } => {
                    assert_eq!(agent, "frontend");
                    assert_eq!(workspace, Some("./workspace".to_string()));
                    assert!(background);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Session command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:42-59 function test_cli_parsing_start_command <-> ./crates/ccswarm/src/cli/tests.rs:233-254 function test_cli_parsing_config_generate
  Similarity: 92.68%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command (lines 42-59) ---[0m
    fn test_cli_parsing_start_command() {
        let args = parse_args(&["start", "--daemon", "--port", "9090"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(daemon);
                assert_eq!(port, 9090);
                assert_eq!(isolation, "worktree"); // default value
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_config_generate (lines 233-254) ---[0m
    fn test_cli_parsing_config_generate() {
        let args = parse_args(&[
            "config",
            "generate",
            "--output",
            "custom.json",
            "--template",
            "minimal",
        ])
        .unwrap();

        match args.command {
            Commands::Config { action } => match action {
                ConfigAction::Generate { output, template } => {
                    assert_eq!(output, PathBuf::from("custom.json"));
                    assert_eq!(template, "minimal");
                }
                _ => panic!("Expected Generate action"),
            },
            _ => panic!("Expected Config command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:62-79 function test_cli_parsing_start_command_with_isolation <-> ./crates/ccswarm/src/cli/tests.rs:233-254 function test_cli_parsing_config_generate
  Similarity: 92.68%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command_with_isolation (lines 62-79) ---[0m
    fn test_cli_parsing_start_command_with_isolation() {
        let args = parse_args(&["start", "--isolation", "container"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(!daemon); // default false
                assert_eq!(port, 8080); // default port
                assert_eq!(isolation, "container");
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_config_generate (lines 233-254) ---[0m
    fn test_cli_parsing_config_generate() {
        let args = parse_args(&[
            "config",
            "generate",
            "--output",
            "custom.json",
            "--template",
            "minimal",
        ])
        .unwrap();

        match args.command {
            Commands::Config { action } => match action {
                ConfigAction::Generate { output, template } => {
                    assert_eq!(output, PathBuf::from("custom.json"));
                    assert_eq!(template, "minimal");
                }
                _ => panic!("Expected Generate action"),
            },
            _ => panic!("Expected Config command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:92-102 function test_cli_parsing_status_command <-> ./crates/ccswarm/src/cli/tests.rs:291-318 function test_cli_parsing_session_create
  Similarity: 94.27%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_status_command (lines 92-102) ---[0m
    fn test_cli_parsing_status_command() {
        let args = parse_args(&["status", "--detailed", "--agent", "frontend"]).unwrap();

        match args.command {
            Commands::Status { detailed, agent } => {
                assert!(detailed);
                assert_eq!(agent, Some("frontend".to_string()));
            }
            _ => panic!("Expected Status command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_session_create (lines 291-318) ---[0m
    fn test_cli_parsing_session_create() {
        let args = parse_args(&[
            "session",
            "create",
            "--agent",
            "frontend",
            "--workspace",
            "./workspace",
            "--background",
        ])
        .unwrap();

        match args.command {
            Commands::Session { action } => match action {
                SessionAction::Create {
                    agent,
                    workspace,
                    background,
                } => {
                    assert_eq!(agent, "frontend");
                    assert_eq!(workspace, Some("./workspace".to_string()));
                    assert!(background);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Session command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:160-170 function test_cli_parsing_review_command <-> ./crates/ccswarm/src/cli/tests.rs:291-318 function test_cli_parsing_session_create
  Similarity: 94.27%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_review_command (lines 160-170) ---[0m
    fn test_cli_parsing_review_command() {
        let args = parse_args(&["review", "--agent", "backend", "--strict"]).unwrap();

        match args.command {
            Commands::Review { agent, strict } => {
                assert_eq!(agent, Some("backend".to_string()));
                assert!(strict);
            }
            _ => panic!("Expected Review command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_session_create (lines 291-318) ---[0m
    fn test_cli_parsing_session_create() {
        let args = parse_args(&[
            "session",
            "create",
            "--agent",
            "frontend",
            "--workspace",
            "./workspace",
            "--background",
        ])
        .unwrap();

        match args.command {
            Commands::Session { action } => match action {
                SessionAction::Create {
                    agent,
                    workspace,
                    background,
                } => {
                    assert_eq!(agent, "frontend");
                    assert_eq!(workspace, Some("./workspace".to_string()));
                    assert!(background);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Session command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:214-230 function test_cli_parsing_logs_command <-> ./crates/ccswarm/src/cli/tests.rs:233-254 function test_cli_parsing_config_generate
  Similarity: 93.41%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_logs_command (lines 214-230) ---[0m
    fn test_cli_parsing_logs_command() {
        let args =
            parse_args(&["logs", "--follow", "--agent", "frontend", "--lines", "200"]).unwrap();

        match args.command {
            Commands::Logs {
                follow,
                agent,
                lines,
            } => {
                assert!(follow);
                assert_eq!(agent, Some("frontend".to_string()));
                assert_eq!(lines, 200);
            }
            _ => panic!("Expected Logs command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_config_generate (lines 233-254) ---[0m
    fn test_cli_parsing_config_generate() {
        let args = parse_args(&[
            "config",
            "generate",
            "--output",
            "custom.json",
            "--template",
            "minimal",
        ])
        .unwrap();

        match args.command {
            Commands::Config { action } => match action {
                ConfigAction::Generate { output, template } => {
                    assert_eq!(output, PathBuf::from("custom.json"));
                    assert_eq!(template, "minimal");
                }
                _ => panic!("Expected Generate action"),
            },
            _ => panic!("Expected Config command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:448-467 function test_priority_parsing <-> ./crates/ccswarm/src/cli/tests.rs:526-544 function test_delegation_strategy_parsing
  Similarity: 93.21%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_priority_parsing (lines 448-467) ---[0m
    fn test_priority_parsing() {
        let test_cases = vec![
            ("low", Priority::Low),
            ("medium", Priority::Medium),
            ("high", Priority::High),
            ("critical", Priority::Critical),
            ("unknown", Priority::Medium), // Default
        ];

        for (input, expected) in test_cases {
            let priority = match input {
                "low" => Priority::Low,
                "medium" => Priority::Medium,
                "high" => Priority::High,
                "critical" => Priority::Critical,
                _ => Priority::Medium,
            };
            assert_eq!(priority, expected);
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_delegation_strategy_parsing (lines 526-544) ---[0m
    fn test_delegation_strategy_parsing() {
        // Test strategy string parsing logic
        let test_cases = vec![
            ("content", true),
            ("load", true),
            ("expertise", true),
            ("workflow", true),
            ("hybrid", true),
            ("unknown", false), // Should default to hybrid
        ];

        for (input, is_valid_strategy) in test_cases {
            let is_valid = matches!(
                input,
                "content" | "load" | "expertise" | "workflow" | "hybrid"
            );
            assert_eq!(is_valid, is_valid_strategy);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:233-254 function test_cli_parsing_config_generate <-> ./crates/ccswarm/src/cli/tests.rs:526-544 function test_delegation_strategy_parsing
  Similarity: 88.11%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_config_generate (lines 233-254) ---[0m
    fn test_cli_parsing_config_generate() {
        let args = parse_args(&[
            "config",
            "generate",
            "--output",
            "custom.json",
            "--template",
            "minimal",
        ])
        .unwrap();

        match args.command {
            Commands::Config { action } => match action {
                ConfigAction::Generate { output, template } => {
                    assert_eq!(output, PathBuf::from("custom.json"));
                    assert_eq!(template, "minimal");
                }
                _ => panic!("Expected Generate action"),
            },
            _ => panic!("Expected Config command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_delegation_strategy_parsing (lines 526-544) ---[0m
    fn test_delegation_strategy_parsing() {
        // Test strategy string parsing logic
        let test_cases = vec![
            ("content", true),
            ("load", true),
            ("expertise", true),
            ("workflow", true),
            ("hybrid", true),
            ("unknown", false), // Should default to hybrid
        ];

        for (input, is_valid_strategy) in test_cases {
            let is_valid = matches!(
                input,
                "content" | "load" | "expertise" | "workflow" | "hybrid"
            );
            assert_eq!(is_valid, is_valid_strategy);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:92-102 function test_cli_parsing_status_command <-> ./crates/ccswarm/src/cli/tests.rs:321-347 function test_cli_parsing_auto_create
  Similarity: 93.10%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_status_command (lines 92-102) ---[0m
    fn test_cli_parsing_status_command() {
        let args = parse_args(&["status", "--detailed", "--agent", "frontend"]).unwrap();

        match args.command {
            Commands::Status { detailed, agent } => {
                assert!(detailed);
                assert_eq!(agent, Some("frontend".to_string()));
            }
            _ => panic!("Expected Status command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_auto_create (lines 321-347) ---[0m
    fn test_cli_parsing_auto_create() {
        let args = parse_args(&[
            "auto-create",
            "Todo app with authentication",
            "--template",
            "full-stack",
            "--auto-deploy",
            "--output",
            "./my-app",
        ])
        .unwrap();

        match args.command {
            Commands::AutoCreate {
                description,
                template,
                auto_deploy,
                output,
            } => {
                assert_eq!(description, "Todo app with authentication");
                assert_eq!(template, Some("full-stack".to_string()));
                assert!(auto_deploy);
                assert_eq!(output, PathBuf::from("./my-app"));
            }
            _ => panic!("Expected AutoCreate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:160-170 function test_cli_parsing_review_command <-> ./crates/ccswarm/src/cli/tests.rs:321-347 function test_cli_parsing_auto_create
  Similarity: 93.10%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_review_command (lines 160-170) ---[0m
    fn test_cli_parsing_review_command() {
        let args = parse_args(&["review", "--agent", "backend", "--strict"]).unwrap();

        match args.command {
            Commands::Review { agent, strict } => {
                assert_eq!(agent, Some("backend".to_string()));
                assert!(strict);
            }
            _ => panic!("Expected Review command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_auto_create (lines 321-347) ---[0m
    fn test_cli_parsing_auto_create() {
        let args = parse_args(&[
            "auto-create",
            "Todo app with authentication",
            "--template",
            "full-stack",
            "--auto-deploy",
            "--output",
            "./my-app",
        ])
        .unwrap();

        match args.command {
            Commands::AutoCreate {
                description,
                template,
                auto_deploy,
                output,
            } => {
                assert_eq!(description, "Todo app with authentication");
                assert_eq!(template, Some("full-stack".to_string()));
                assert!(auto_deploy);
                assert_eq!(output, PathBuf::from("./my-app"));
            }
            _ => panic!("Expected AutoCreate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:173-183 function test_cli_parsing_worktree_list <-> ./crates/ccswarm/src/cli/tests.rs:321-347 function test_cli_parsing_auto_create
  Similarity: 93.10%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_list (lines 173-183) ---[0m
    fn test_cli_parsing_worktree_list() {
        let args = parse_args(&["worktree", "list"]).unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::List => {}
                _ => panic!("Expected List action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_auto_create (lines 321-347) ---[0m
    fn test_cli_parsing_auto_create() {
        let args = parse_args(&[
            "auto-create",
            "Todo app with authentication",
            "--template",
            "full-stack",
            "--auto-deploy",
            "--output",
            "./my-app",
        ])
        .unwrap();

        match args.command {
            Commands::AutoCreate {
                description,
                template,
                auto_deploy,
                output,
            } => {
                assert_eq!(description, "Todo app with authentication");
                assert_eq!(template, Some("full-stack".to_string()));
                assert!(auto_deploy);
                assert_eq!(output, PathBuf::from("./my-app"));
            }
            _ => panic!("Expected AutoCreate command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:173-183 function test_cli_parsing_worktree_list <-> ./crates/ccswarm/src/cli/tests.rs:186-211 function test_cli_parsing_worktree_create
  Similarity: 95.35%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_list (lines 173-183) ---[0m
    fn test_cli_parsing_worktree_list() {
        let args = parse_args(&["worktree", "list"]).unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::List => {}
                _ => panic!("Expected List action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_create (lines 186-211) ---[0m
    fn test_cli_parsing_worktree_create() {
        let args = parse_args(&[
            "worktree",
            "create",
            "/path/to/worktree",
            "feature-branch",
            "--new-branch",
        ])
        .unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::Create {
                    path,
                    branch,
                    new_branch,
                } => {
                    assert_eq!(path, PathBuf::from("/path/to/worktree"));
                    assert_eq!(branch, "feature-branch");
                    assert!(new_branch);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:42-59 function test_cli_parsing_start_command <-> ./crates/ccswarm/src/cli/tests.rs:560-580 function test_interactive_command_parsing
  Similarity: 89.84%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command (lines 42-59) ---[0m
    fn test_cli_parsing_start_command() {
        let args = parse_args(&["start", "--daemon", "--port", "9090"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(daemon);
                assert_eq!(port, 9090);
                assert_eq!(isolation, "worktree"); // default value
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_interactive_command_parsing (lines 560-580) ---[0m
    fn test_interactive_command_parsing() {
        // Test various interactive commands
        let commands = vec![
            ("quit", true),
            ("exit", true),
            ("help", true),
            ("stats", true),
            ("agents", true),
            ("analyze task description", true),
            ("delegate frontend Create UI", true),
            ("unknown command", false),
        ];

        for (cmd, should_be_valid) in commands {
            let is_valid = matches!(cmd, "quit" | "exit" | "help" | "stats" | "agents")
                || cmd.starts_with("analyze ")
                || cmd.starts_with("delegate ");

            assert_eq!(is_valid, should_be_valid);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:62-79 function test_cli_parsing_start_command_with_isolation <-> ./crates/ccswarm/src/cli/tests.rs:560-580 function test_interactive_command_parsing
  Similarity: 89.84%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command_with_isolation (lines 62-79) ---[0m
    fn test_cli_parsing_start_command_with_isolation() {
        let args = parse_args(&["start", "--isolation", "container"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(!daemon); // default false
                assert_eq!(port, 8080); // default port
                assert_eq!(isolation, "container");
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_interactive_command_parsing (lines 560-580) ---[0m
    fn test_interactive_command_parsing() {
        // Test various interactive commands
        let commands = vec![
            ("quit", true),
            ("exit", true),
            ("help", true),
            ("stats", true),
            ("agents", true),
            ("analyze task description", true),
            ("delegate frontend Create UI", true),
            ("unknown command", false),
        ];

        for (cmd, should_be_valid) in commands {
            let is_valid = matches!(cmd, "quit" | "exit" | "help" | "stats" | "agents")
                || cmd.starts_with("analyze ")
                || cmd.starts_with("delegate ");

            assert_eq!(is_valid, should_be_valid);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:321-347 function test_cli_parsing_auto_create <-> ./crates/ccswarm/src/cli/tests.rs:547-557 function test_error_handling_invalid_priority
  Similarity: 91.49%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_auto_create (lines 321-347) ---[0m
    fn test_cli_parsing_auto_create() {
        let args = parse_args(&[
            "auto-create",
            "Todo app with authentication",
            "--template",
            "full-stack",
            "--auto-deploy",
            "--output",
            "./my-app",
        ])
        .unwrap();

        match args.command {
            Commands::AutoCreate {
                description,
                template,
                auto_deploy,
                output,
            } => {
                assert_eq!(description, "Todo app with authentication");
                assert_eq!(template, Some("full-stack".to_string()));
                assert!(auto_deploy);
                assert_eq!(output, PathBuf::from("./my-app"));
            }
            _ => panic!("Expected AutoCreate command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_error_handling_invalid_priority (lines 547-557) ---[0m
    fn test_error_handling_invalid_priority() {
        // Priority parsing should default to Medium for invalid values
        let priority = match "invalid-priority" {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };
        assert_eq!(priority, Priority::Medium);
    }

  ./crates/ccswarm/src/cli/tests.rs:92-102 function test_cli_parsing_status_command <-> ./crates/ccswarm/src/cli/tests.rs:186-211 function test_cli_parsing_worktree_create
  Similarity: 93.84%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_status_command (lines 92-102) ---[0m
    fn test_cli_parsing_status_command() {
        let args = parse_args(&["status", "--detailed", "--agent", "frontend"]).unwrap();

        match args.command {
            Commands::Status { detailed, agent } => {
                assert!(detailed);
                assert_eq!(agent, Some("frontend".to_string()));
            }
            _ => panic!("Expected Status command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_create (lines 186-211) ---[0m
    fn test_cli_parsing_worktree_create() {
        let args = parse_args(&[
            "worktree",
            "create",
            "/path/to/worktree",
            "feature-branch",
            "--new-branch",
        ])
        .unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::Create {
                    path,
                    branch,
                    new_branch,
                } => {
                    assert_eq!(path, PathBuf::from("/path/to/worktree"));
                    assert_eq!(branch, "feature-branch");
                    assert!(new_branch);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:160-170 function test_cli_parsing_review_command <-> ./crates/ccswarm/src/cli/tests.rs:186-211 function test_cli_parsing_worktree_create
  Similarity: 93.84%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_review_command (lines 160-170) ---[0m
    fn test_cli_parsing_review_command() {
        let args = parse_args(&["review", "--agent", "backend", "--strict"]).unwrap();

        match args.command {
            Commands::Review { agent, strict } => {
                assert_eq!(agent, Some("backend".to_string()));
                assert!(strict);
            }
            _ => panic!("Expected Review command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_create (lines 186-211) ---[0m
    fn test_cli_parsing_worktree_create() {
        let args = parse_args(&[
            "worktree",
            "create",
            "/path/to/worktree",
            "feature-branch",
            "--new-branch",
        ])
        .unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::Create {
                    path,
                    branch,
                    new_branch,
                } => {
                    assert_eq!(path, PathBuf::from("/path/to/worktree"));
                    assert_eq!(branch, "feature-branch");
                    assert!(new_branch);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:42-59 function test_cli_parsing_start_command <-> ./crates/ccswarm/src/cli/tests.rs:62-79 function test_cli_parsing_start_command_with_isolation
  Similarity: 96.18%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command (lines 42-59) ---[0m
    fn test_cli_parsing_start_command() {
        let args = parse_args(&["start", "--daemon", "--port", "9090"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(daemon);
                assert_eq!(port, 9090);
                assert_eq!(isolation, "worktree"); // default value
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command_with_isolation (lines 62-79) ---[0m
    fn test_cli_parsing_start_command_with_isolation() {
        let args = parse_args(&["start", "--isolation", "container"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(!daemon); // default false
                assert_eq!(port, 8080); // default port
                assert_eq!(isolation, "container");
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:400-423 function test_cli_runner_new <-> ./crates/ccswarm/src/cli/tests.rs:426-445 function test_cli_runner_new_missing_config
  Similarity: 77.87%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_runner_new (lines 400-423) ---[0m
    async fn test_cli_runner_new() {
        let temp_dir = TempDir::new().unwrap();
        let config_path = temp_dir.path().join("ccswarm.json");

        // Create a simple config file
        let config = create_minimal_config(temp_dir.path()).unwrap();
        config.to_file(config_path.clone()).await.unwrap();

        let cli = Cli {
            config: config_path,
            repo: temp_dir.path().to_path_buf(),
            verbose: false,
            json: false,
            fix: false,
            command: Commands::Status {
                detailed: false,
                agent: None,
            },
        };

        let runner = CliRunner::new(&cli).await.unwrap();
        assert_eq!(runner.config.project.name, "Minimal ccswarm Project");
        assert!(runner.config.agents.is_empty());
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_runner_new_missing_config (lines 426-445) ---[0m
    async fn test_cli_runner_new_missing_config() {
        let temp_dir = TempDir::new().unwrap();
        let config_path = temp_dir.path().join("nonexistent.json");

        let cli = Cli {
            config: config_path,
            repo: temp_dir.path().to_path_buf(),
            verbose: false,
            json: false,
            fix: false,
            command: Commands::Status {
                detailed: false,
                agent: None,
            },
        };

        // Should use default config when file doesn't exist
        let runner = CliRunner::new(&cli).await.unwrap();
        assert_eq!(runner.config.project.name, "New ccswarm Project");
    }

  ./crates/ccswarm/src/cli/tests.rs:214-230 function test_cli_parsing_logs_command <-> ./crates/ccswarm/src/cli/tests.rs:560-580 function test_interactive_command_parsing
  Similarity: 89.84%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_logs_command (lines 214-230) ---[0m
    fn test_cli_parsing_logs_command() {
        let args =
            parse_args(&["logs", "--follow", "--agent", "frontend", "--lines", "200"]).unwrap();

        match args.command {
            Commands::Logs {
                follow,
                agent,
                lines,
            } => {
                assert!(follow);
                assert_eq!(agent, Some("frontend".to_string()));
                assert_eq!(lines, 200);
            }
            _ => panic!("Expected Logs command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_interactive_command_parsing (lines 560-580) ---[0m
    fn test_interactive_command_parsing() {
        // Test various interactive commands
        let commands = vec![
            ("quit", true),
            ("exit", true),
            ("help", true),
            ("stats", true),
            ("agents", true),
            ("analyze task description", true),
            ("delegate frontend Create UI", true),
            ("unknown command", false),
        ];

        for (cmd, should_be_valid) in commands {
            let is_valid = matches!(cmd, "quit" | "exit" | "help" | "stats" | "agents")
                || cmd.starts_with("analyze ")
                || cmd.starts_with("delegate ");

            assert_eq!(is_valid, should_be_valid);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:42-59 function test_cli_parsing_start_command <-> ./crates/ccswarm/src/cli/tests.rs:448-467 function test_priority_parsing
  Similarity: 89.39%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command (lines 42-59) ---[0m
    fn test_cli_parsing_start_command() {
        let args = parse_args(&["start", "--daemon", "--port", "9090"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(daemon);
                assert_eq!(port, 9090);
                assert_eq!(isolation, "worktree"); // default value
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_priority_parsing (lines 448-467) ---[0m
    fn test_priority_parsing() {
        let test_cases = vec![
            ("low", Priority::Low),
            ("medium", Priority::Medium),
            ("high", Priority::High),
            ("critical", Priority::Critical),
            ("unknown", Priority::Medium), // Default
        ];

        for (input, expected) in test_cases {
            let priority = match input {
                "low" => Priority::Low,
                "medium" => Priority::Medium,
                "high" => Priority::High,
                "critical" => Priority::Critical,
                _ => Priority::Medium,
            };
            assert_eq!(priority, expected);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:62-79 function test_cli_parsing_start_command_with_isolation <-> ./crates/ccswarm/src/cli/tests.rs:448-467 function test_priority_parsing
  Similarity: 89.39%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command_with_isolation (lines 62-79) ---[0m
    fn test_cli_parsing_start_command_with_isolation() {
        let args = parse_args(&["start", "--isolation", "container"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(!daemon); // default false
                assert_eq!(port, 8080); // default port
                assert_eq!(isolation, "container");
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_priority_parsing (lines 448-467) ---[0m
    fn test_priority_parsing() {
        let test_cases = vec![
            ("low", Priority::Low),
            ("medium", Priority::Medium),
            ("high", Priority::High),
            ("critical", Priority::Critical),
            ("unknown", Priority::Medium), // Default
        ];

        for (input, expected) in test_cases {
            let priority = match input {
                "low" => Priority::Low,
                "medium" => Priority::Medium,
                "high" => Priority::High,
                "critical" => Priority::Critical,
                _ => Priority::Medium,
            };
            assert_eq!(priority, expected);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:186-211 function test_cli_parsing_worktree_create <-> ./crates/ccswarm/src/cli/tests.rs:547-557 function test_error_handling_invalid_priority
  Similarity: 91.40%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_create (lines 186-211) ---[0m
    fn test_cli_parsing_worktree_create() {
        let args = parse_args(&[
            "worktree",
            "create",
            "/path/to/worktree",
            "feature-branch",
            "--new-branch",
        ])
        .unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::Create {
                    path,
                    branch,
                    new_branch,
                } => {
                    assert_eq!(path, PathBuf::from("/path/to/worktree"));
                    assert_eq!(branch, "feature-branch");
                    assert!(new_branch);
                }
                _ => panic!("Expected Create action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_error_handling_invalid_priority (lines 547-557) ---[0m
    fn test_error_handling_invalid_priority() {
        // Priority parsing should default to Medium for invalid values
        let priority = match "invalid-priority" {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };
        assert_eq!(priority, Priority::Medium);
    }

  ./crates/ccswarm/src/cli/tests.rs:15-39 function test_cli_parsing_init_command <-> ./crates/ccswarm/src/cli/tests.rs:92-102 function test_cli_parsing_status_command
  Similarity: 92.77%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_init_command (lines 15-39) ---[0m
    fn test_cli_parsing_init_command() {
        let args = parse_args(&[
            "init",
            "--name",
            "test-project",
            "--repo-url",
            "https://github.com/test/repo",
            "--agents",
            "frontend,backend",
        ])
        .unwrap();

        match args.command {
            Commands::Init {
                name,
                repo_url,
                agents,
            } => {
                assert_eq!(name, "test-project");
                assert_eq!(repo_url, Some("https://github.com/test/repo".to_string()));
                assert_eq!(agents, vec!["frontend", "backend"]);
            }
            _ => panic!("Expected Init command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_status_command (lines 92-102) ---[0m
    fn test_cli_parsing_status_command() {
        let args = parse_args(&["status", "--detailed", "--agent", "frontend"]).unwrap();

        match args.command {
            Commands::Status { detailed, agent } => {
                assert!(detailed);
                assert_eq!(agent, Some("frontend".to_string()));
            }
            _ => panic!("Expected Status command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:15-39 function test_cli_parsing_init_command <-> ./crates/ccswarm/src/cli/tests.rs:160-170 function test_cli_parsing_review_command
  Similarity: 92.77%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_init_command (lines 15-39) ---[0m
    fn test_cli_parsing_init_command() {
        let args = parse_args(&[
            "init",
            "--name",
            "test-project",
            "--repo-url",
            "https://github.com/test/repo",
            "--agents",
            "frontend,backend",
        ])
        .unwrap();

        match args.command {
            Commands::Init {
                name,
                repo_url,
                agents,
            } => {
                assert_eq!(name, "test-project");
                assert_eq!(repo_url, Some("https://github.com/test/repo".to_string()));
                assert_eq!(agents, vec!["frontend", "backend"]);
            }
            _ => panic!("Expected Init command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_review_command (lines 160-170) ---[0m
    fn test_cli_parsing_review_command() {
        let args = parse_args(&["review", "--agent", "backend", "--strict"]).unwrap();

        match args.command {
            Commands::Review { agent, strict } => {
                assert_eq!(agent, Some("backend".to_string()));
                assert!(strict);
            }
            _ => panic!("Expected Review command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:15-39 function test_cli_parsing_init_command <-> ./crates/ccswarm/src/cli/tests.rs:173-183 function test_cli_parsing_worktree_list
  Similarity: 92.77%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_init_command (lines 15-39) ---[0m
    fn test_cli_parsing_init_command() {
        let args = parse_args(&[
            "init",
            "--name",
            "test-project",
            "--repo-url",
            "https://github.com/test/repo",
            "--agents",
            "frontend,backend",
        ])
        .unwrap();

        match args.command {
            Commands::Init {
                name,
                repo_url,
                agents,
            } => {
                assert_eq!(name, "test-project");
                assert_eq!(repo_url, Some("https://github.com/test/repo".to_string()));
                assert_eq!(agents, vec!["frontend", "backend"]);
            }
            _ => panic!("Expected Init command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_list (lines 173-183) ---[0m
    fn test_cli_parsing_worktree_list() {
        let args = parse_args(&["worktree", "list"]).unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::List => {}
                _ => panic!("Expected List action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:214-230 function test_cli_parsing_logs_command <-> ./crates/ccswarm/src/cli/tests.rs:448-467 function test_priority_parsing
  Similarity: 89.39%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_logs_command (lines 214-230) ---[0m
    fn test_cli_parsing_logs_command() {
        let args =
            parse_args(&["logs", "--follow", "--agent", "frontend", "--lines", "200"]).unwrap();

        match args.command {
            Commands::Logs {
                follow,
                agent,
                lines,
            } => {
                assert!(follow);
                assert_eq!(agent, Some("frontend".to_string()));
                assert_eq!(lines, 200);
            }
            _ => panic!("Expected Logs command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_priority_parsing (lines 448-467) ---[0m
    fn test_priority_parsing() {
        let test_cases = vec![
            ("low", Priority::Low),
            ("medium", Priority::Medium),
            ("high", Priority::High),
            ("critical", Priority::Critical),
            ("unknown", Priority::Medium), // Default
        ];

        for (input, expected) in test_cases {
            let priority = match input {
                "low" => Priority::Low,
                "medium" => Priority::Medium,
                "high" => Priority::High,
                "critical" => Priority::Critical,
                _ => Priority::Medium,
            };
            assert_eq!(priority, expected);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:15-39 function test_cli_parsing_init_command <-> ./crates/ccswarm/src/cli/tests.rs:547-557 function test_error_handling_invalid_priority
  Similarity: 91.08%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_init_command (lines 15-39) ---[0m
    fn test_cli_parsing_init_command() {
        let args = parse_args(&[
            "init",
            "--name",
            "test-project",
            "--repo-url",
            "https://github.com/test/repo",
            "--agents",
            "frontend,backend",
        ])
        .unwrap();

        match args.command {
            Commands::Init {
                name,
                repo_url,
                agents,
            } => {
                assert_eq!(name, "test-project");
                assert_eq!(repo_url, Some("https://github.com/test/repo".to_string()));
                assert_eq!(agents, vec!["frontend", "backend"]);
            }
            _ => panic!("Expected Init command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_error_handling_invalid_priority (lines 547-557) ---[0m
    fn test_error_handling_invalid_priority() {
        // Priority parsing should default to Medium for invalid values
        let priority = match "invalid-priority" {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };
        assert_eq!(priority, Priority::Medium);
    }

  ./crates/ccswarm/src/cli/tests.rs:42-59 function test_cli_parsing_start_command <-> ./crates/ccswarm/src/cli/tests.rs:214-230 function test_cli_parsing_logs_command
  Similarity: 91.55%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command (lines 42-59) ---[0m
    fn test_cli_parsing_start_command() {
        let args = parse_args(&["start", "--daemon", "--port", "9090"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(daemon);
                assert_eq!(port, 9090);
                assert_eq!(isolation, "worktree"); // default value
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_logs_command (lines 214-230) ---[0m
    fn test_cli_parsing_logs_command() {
        let args =
            parse_args(&["logs", "--follow", "--agent", "frontend", "--lines", "200"]).unwrap();

        match args.command {
            Commands::Logs {
                follow,
                agent,
                lines,
            } => {
                assert!(follow);
                assert_eq!(agent, Some("frontend".to_string()));
                assert_eq!(lines, 200);
            }
            _ => panic!("Expected Logs command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:62-79 function test_cli_parsing_start_command_with_isolation <-> ./crates/ccswarm/src/cli/tests.rs:214-230 function test_cli_parsing_logs_command
  Similarity: 91.55%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command_with_isolation (lines 62-79) ---[0m
    fn test_cli_parsing_start_command_with_isolation() {
        let args = parse_args(&["start", "--isolation", "container"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(!daemon); // default false
                assert_eq!(port, 8080); // default port
                assert_eq!(isolation, "container");
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_logs_command (lines 214-230) ---[0m
    fn test_cli_parsing_logs_command() {
        let args =
            parse_args(&["logs", "--follow", "--agent", "frontend", "--lines", "200"]).unwrap();

        match args.command {
            Commands::Logs {
                follow,
                agent,
                lines,
            } => {
                assert!(follow);
                assert_eq!(agent, Some("frontend".to_string()));
                assert_eq!(lines, 200);
            }
            _ => panic!("Expected Logs command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:42-59 function test_cli_parsing_start_command <-> ./crates/ccswarm/src/cli/tests.rs:526-544 function test_delegation_strategy_parsing
  Similarity: 86.27%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command (lines 42-59) ---[0m
    fn test_cli_parsing_start_command() {
        let args = parse_args(&["start", "--daemon", "--port", "9090"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(daemon);
                assert_eq!(port, 9090);
                assert_eq!(isolation, "worktree"); // default value
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_delegation_strategy_parsing (lines 526-544) ---[0m
    fn test_delegation_strategy_parsing() {
        // Test strategy string parsing logic
        let test_cases = vec![
            ("content", true),
            ("load", true),
            ("expertise", true),
            ("workflow", true),
            ("hybrid", true),
            ("unknown", false), // Should default to hybrid
        ];

        for (input, is_valid_strategy) in test_cases {
            let is_valid = matches!(
                input,
                "content" | "load" | "expertise" | "workflow" | "hybrid"
            );
            assert_eq!(is_valid, is_valid_strategy);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:62-79 function test_cli_parsing_start_command_with_isolation <-> ./crates/ccswarm/src/cli/tests.rs:526-544 function test_delegation_strategy_parsing
  Similarity: 86.27%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command_with_isolation (lines 62-79) ---[0m
    fn test_cli_parsing_start_command_with_isolation() {
        let args = parse_args(&["start", "--isolation", "container"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(!daemon); // default false
                assert_eq!(port, 8080); // default port
                assert_eq!(isolation, "container");
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_delegation_strategy_parsing (lines 526-544) ---[0m
    fn test_delegation_strategy_parsing() {
        // Test strategy string parsing logic
        let test_cases = vec![
            ("content", true),
            ("load", true),
            ("expertise", true),
            ("workflow", true),
            ("hybrid", true),
            ("unknown", false), // Should default to hybrid
        ];

        for (input, is_valid_strategy) in test_cases {
            let is_valid = matches!(
                input,
                "content" | "load" | "expertise" | "workflow" | "hybrid"
            );
            assert_eq!(is_valid, is_valid_strategy);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:173-183 function test_cli_parsing_worktree_list <-> ./crates/ccswarm/src/cli/tests.rs:233-254 function test_cli_parsing_config_generate
  Similarity: 94.76%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_list (lines 173-183) ---[0m
    fn test_cli_parsing_worktree_list() {
        let args = parse_args(&["worktree", "list"]).unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::List => {}
                _ => panic!("Expected List action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_config_generate (lines 233-254) ---[0m
    fn test_cli_parsing_config_generate() {
        let args = parse_args(&[
            "config",
            "generate",
            "--output",
            "custom.json",
            "--template",
            "minimal",
        ])
        .unwrap();

        match args.command {
            Commands::Config { action } => match action {
                ConfigAction::Generate { output, template } => {
                    assert_eq!(output, PathBuf::from("custom.json"));
                    assert_eq!(template, "minimal");
                }
                _ => panic!("Expected Generate action"),
            },
            _ => panic!("Expected Config command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:214-230 function test_cli_parsing_logs_command <-> ./crates/ccswarm/src/cli/tests.rs:526-544 function test_delegation_strategy_parsing
  Similarity: 86.27%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_logs_command (lines 214-230) ---[0m
    fn test_cli_parsing_logs_command() {
        let args =
            parse_args(&["logs", "--follow", "--agent", "frontend", "--lines", "200"]).unwrap();

        match args.command {
            Commands::Logs {
                follow,
                agent,
                lines,
            } => {
                assert!(follow);
                assert_eq!(agent, Some("frontend".to_string()));
                assert_eq!(lines, 200);
            }
            _ => panic!("Expected Logs command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_delegation_strategy_parsing (lines 526-544) ---[0m
    fn test_delegation_strategy_parsing() {
        // Test strategy string parsing logic
        let test_cases = vec![
            ("content", true),
            ("load", true),
            ("expertise", true),
            ("workflow", true),
            ("hybrid", true),
            ("unknown", false), // Should default to hybrid
        ];

        for (input, is_valid_strategy) in test_cases {
            let is_valid = matches!(
                input,
                "content" | "load" | "expertise" | "workflow" | "hybrid"
            );
            assert_eq!(is_valid, is_valid_strategy);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:350-366 function test_cli_global_options <-> ./crates/ccswarm/src/cli/tests.rs:426-445 function test_cli_runner_new_missing_config
  Similarity: 83.63%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_global_options (lines 350-366) ---[0m
    fn test_cli_global_options() {
        let args = parse_args(&[
            "--config",
            "custom.json",
            "--repo",
            "/path/to/repo",
            "--verbose",
            "--json",
            "status",
        ])
        .unwrap();

        assert_eq!(args.config, PathBuf::from("custom.json"));
        assert_eq!(args.repo, PathBuf::from("/path/to/repo"));
        assert!(args.verbose);
        assert!(args.json);
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_runner_new_missing_config (lines 426-445) ---[0m
    async fn test_cli_runner_new_missing_config() {
        let temp_dir = TempDir::new().unwrap();
        let config_path = temp_dir.path().join("nonexistent.json");

        let cli = Cli {
            config: config_path,
            repo: temp_dir.path().to_path_buf(),
            verbose: false,
            json: false,
            fix: false,
            command: Commands::Status {
                detailed: false,
                agent: None,
            },
        };

        // Should use default config when file doesn't exist
        let runner = CliRunner::new(&cli).await.unwrap();
        assert_eq!(runner.config.project.name, "New ccswarm Project");
    }

  ./crates/ccswarm/src/cli/tests.rs:92-102 function test_cli_parsing_status_command <-> ./crates/ccswarm/src/cli/tests.rs:233-254 function test_cli_parsing_config_generate
  Similarity: 93.54%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_status_command (lines 92-102) ---[0m
    fn test_cli_parsing_status_command() {
        let args = parse_args(&["status", "--detailed", "--agent", "frontend"]).unwrap();

        match args.command {
            Commands::Status { detailed, agent } => {
                assert!(detailed);
                assert_eq!(agent, Some("frontend".to_string()));
            }
            _ => panic!("Expected Status command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_config_generate (lines 233-254) ---[0m
    fn test_cli_parsing_config_generate() {
        let args = parse_args(&[
            "config",
            "generate",
            "--output",
            "custom.json",
            "--template",
            "minimal",
        ])
        .unwrap();

        match args.command {
            Commands::Config { action } => match action {
                ConfigAction::Generate { output, template } => {
                    assert_eq!(output, PathBuf::from("custom.json"));
                    assert_eq!(template, "minimal");
                }
                _ => panic!("Expected Generate action"),
            },
            _ => panic!("Expected Config command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:160-170 function test_cli_parsing_review_command <-> ./crates/ccswarm/src/cli/tests.rs:233-254 function test_cli_parsing_config_generate
  Similarity: 93.54%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_review_command (lines 160-170) ---[0m
    fn test_cli_parsing_review_command() {
        let args = parse_args(&["review", "--agent", "backend", "--strict"]).unwrap();

        match args.command {
            Commands::Review { agent, strict } => {
                assert_eq!(agent, Some("backend".to_string()));
                assert!(strict);
            }
            _ => panic!("Expected Review command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_config_generate (lines 233-254) ---[0m
    fn test_cli_parsing_config_generate() {
        let args = parse_args(&[
            "config",
            "generate",
            "--output",
            "custom.json",
            "--template",
            "minimal",
        ])
        .unwrap();

        match args.command {
            Commands::Config { action } => match action {
                ConfigAction::Generate { output, template } => {
                    assert_eq!(output, PathBuf::from("custom.json"));
                    assert_eq!(template, "minimal");
                }
                _ => panic!("Expected Generate action"),
            },
            _ => panic!("Expected Config command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:233-254 function test_cli_parsing_config_generate <-> ./crates/ccswarm/src/cli/tests.rs:547-557 function test_error_handling_invalid_priority
  Similarity: 90.98%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_config_generate (lines 233-254) ---[0m
    fn test_cli_parsing_config_generate() {
        let args = parse_args(&[
            "config",
            "generate",
            "--output",
            "custom.json",
            "--template",
            "minimal",
        ])
        .unwrap();

        match args.command {
            Commands::Config { action } => match action {
                ConfigAction::Generate { output, template } => {
                    assert_eq!(output, PathBuf::from("custom.json"));
                    assert_eq!(template, "minimal");
                }
                _ => panic!("Expected Generate action"),
            },
            _ => panic!("Expected Config command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_error_handling_invalid_priority (lines 547-557) ---[0m
    fn test_error_handling_invalid_priority() {
        // Priority parsing should default to Medium for invalid values
        let priority = match "invalid-priority" {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };
        assert_eq!(priority, Priority::Medium);
    }

  ./crates/ccswarm/src/cli/tests.rs:350-366 function test_cli_global_options <-> ./crates/ccswarm/src/cli/tests.rs:560-580 function test_interactive_command_parsing
  Similarity: 77.34%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_global_options (lines 350-366) ---[0m
    fn test_cli_global_options() {
        let args = parse_args(&[
            "--config",
            "custom.json",
            "--repo",
            "/path/to/repo",
            "--verbose",
            "--json",
            "status",
        ])
        .unwrap();

        assert_eq!(args.config, PathBuf::from("custom.json"));
        assert_eq!(args.repo, PathBuf::from("/path/to/repo"));
        assert!(args.verbose);
        assert!(args.json);
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_interactive_command_parsing (lines 560-580) ---[0m
    fn test_interactive_command_parsing() {
        // Test various interactive commands
        let commands = vec![
            ("quit", true),
            ("exit", true),
            ("help", true),
            ("stats", true),
            ("agents", true),
            ("analyze task description", true),
            ("delegate frontend Create UI", true),
            ("unknown command", false),
        ];

        for (cmd, should_be_valid) in commands {
            let is_valid = matches!(cmd, "quit" | "exit" | "help" | "stats" | "agents")
                || cmd.starts_with("analyze ")
                || cmd.starts_with("delegate ");

            assert_eq!(is_valid, should_be_valid);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:92-102 function test_cli_parsing_status_command <-> ./crates/ccswarm/src/cli/tests.rs:560-580 function test_interactive_command_parsing
  Similarity: 89.84%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_status_command (lines 92-102) ---[0m
    fn test_cli_parsing_status_command() {
        let args = parse_args(&["status", "--detailed", "--agent", "frontend"]).unwrap();

        match args.command {
            Commands::Status { detailed, agent } => {
                assert!(detailed);
                assert_eq!(agent, Some("frontend".to_string()));
            }
            _ => panic!("Expected Status command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_interactive_command_parsing (lines 560-580) ---[0m
    fn test_interactive_command_parsing() {
        // Test various interactive commands
        let commands = vec![
            ("quit", true),
            ("exit", true),
            ("help", true),
            ("stats", true),
            ("agents", true),
            ("analyze task description", true),
            ("delegate frontend Create UI", true),
            ("unknown command", false),
        ];

        for (cmd, should_be_valid) in commands {
            let is_valid = matches!(cmd, "quit" | "exit" | "help" | "stats" | "agents")
                || cmd.starts_with("analyze ")
                || cmd.starts_with("delegate ");

            assert_eq!(is_valid, should_be_valid);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:160-170 function test_cli_parsing_review_command <-> ./crates/ccswarm/src/cli/tests.rs:560-580 function test_interactive_command_parsing
  Similarity: 89.84%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_review_command (lines 160-170) ---[0m
    fn test_cli_parsing_review_command() {
        let args = parse_args(&["review", "--agent", "backend", "--strict"]).unwrap();

        match args.command {
            Commands::Review { agent, strict } => {
                assert_eq!(agent, Some("backend".to_string()));
                assert!(strict);
            }
            _ => panic!("Expected Review command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_interactive_command_parsing (lines 560-580) ---[0m
    fn test_interactive_command_parsing() {
        // Test various interactive commands
        let commands = vec![
            ("quit", true),
            ("exit", true),
            ("help", true),
            ("stats", true),
            ("agents", true),
            ("analyze task description", true),
            ("delegate frontend Create UI", true),
            ("unknown command", false),
        ];

        for (cmd, should_be_valid) in commands {
            let is_valid = matches!(cmd, "quit" | "exit" | "help" | "stats" | "agents")
                || cmd.starts_with("analyze ")
                || cmd.starts_with("delegate ");

            assert_eq!(is_valid, should_be_valid);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:173-183 function test_cli_parsing_worktree_list <-> ./crates/ccswarm/src/cli/tests.rs:560-580 function test_interactive_command_parsing
  Similarity: 89.84%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_list (lines 173-183) ---[0m
    fn test_cli_parsing_worktree_list() {
        let args = parse_args(&["worktree", "list"]).unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::List => {}
                _ => panic!("Expected List action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_interactive_command_parsing (lines 560-580) ---[0m
    fn test_interactive_command_parsing() {
        // Test various interactive commands
        let commands = vec![
            ("quit", true),
            ("exit", true),
            ("help", true),
            ("stats", true),
            ("agents", true),
            ("analyze task description", true),
            ("delegate frontend Create UI", true),
            ("unknown command", false),
        ];

        for (cmd, should_be_valid) in commands {
            let is_valid = matches!(cmd, "quit" | "exit" | "help" | "stats" | "agents")
                || cmd.starts_with("analyze ")
                || cmd.starts_with("delegate ");

            assert_eq!(is_valid, should_be_valid);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:448-467 function test_priority_parsing <-> ./crates/ccswarm/src/cli/tests.rs:547-557 function test_error_handling_invalid_priority
  Similarity: 89.82%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_priority_parsing (lines 448-467) ---[0m
    fn test_priority_parsing() {
        let test_cases = vec![
            ("low", Priority::Low),
            ("medium", Priority::Medium),
            ("high", Priority::High),
            ("critical", Priority::Critical),
            ("unknown", Priority::Medium), // Default
        ];

        for (input, expected) in test_cases {
            let priority = match input {
                "low" => Priority::Low,
                "medium" => Priority::Medium,
                "high" => Priority::High,
                "critical" => Priority::Critical,
                _ => Priority::Medium,
            };
            assert_eq!(priority, expected);
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_error_handling_invalid_priority (lines 547-557) ---[0m
    fn test_error_handling_invalid_priority() {
        // Priority parsing should default to Medium for invalid values
        let priority = match "invalid-priority" {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };
        assert_eq!(priority, Priority::Medium);
    }

  ./crates/ccswarm/src/cli/tests.rs:92-102 function test_cli_parsing_status_command <-> ./crates/ccswarm/src/cli/tests.rs:448-467 function test_priority_parsing
  Similarity: 89.39%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_status_command (lines 92-102) ---[0m
    fn test_cli_parsing_status_command() {
        let args = parse_args(&["status", "--detailed", "--agent", "frontend"]).unwrap();

        match args.command {
            Commands::Status { detailed, agent } => {
                assert!(detailed);
                assert_eq!(agent, Some("frontend".to_string()));
            }
            _ => panic!("Expected Status command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_priority_parsing (lines 448-467) ---[0m
    fn test_priority_parsing() {
        let test_cases = vec![
            ("low", Priority::Low),
            ("medium", Priority::Medium),
            ("high", Priority::High),
            ("critical", Priority::Critical),
            ("unknown", Priority::Medium), // Default
        ];

        for (input, expected) in test_cases {
            let priority = match input {
                "low" => Priority::Low,
                "medium" => Priority::Medium,
                "high" => Priority::High,
                "critical" => Priority::Critical,
                _ => Priority::Medium,
            };
            assert_eq!(priority, expected);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:160-170 function test_cli_parsing_review_command <-> ./crates/ccswarm/src/cli/tests.rs:448-467 function test_priority_parsing
  Similarity: 89.39%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_review_command (lines 160-170) ---[0m
    fn test_cli_parsing_review_command() {
        let args = parse_args(&["review", "--agent", "backend", "--strict"]).unwrap();

        match args.command {
            Commands::Review { agent, strict } => {
                assert_eq!(agent, Some("backend".to_string()));
                assert!(strict);
            }
            _ => panic!("Expected Review command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_priority_parsing (lines 448-467) ---[0m
    fn test_priority_parsing() {
        let test_cases = vec![
            ("low", Priority::Low),
            ("medium", Priority::Medium),
            ("high", Priority::High),
            ("critical", Priority::Critical),
            ("unknown", Priority::Medium), // Default
        ];

        for (input, expected) in test_cases {
            let priority = match input {
                "low" => Priority::Low,
                "medium" => Priority::Medium,
                "high" => Priority::High,
                "critical" => Priority::Critical,
                _ => Priority::Medium,
            };
            assert_eq!(priority, expected);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:173-183 function test_cli_parsing_worktree_list <-> ./crates/ccswarm/src/cli/tests.rs:448-467 function test_priority_parsing
  Similarity: 89.39%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_list (lines 173-183) ---[0m
    fn test_cli_parsing_worktree_list() {
        let args = parse_args(&["worktree", "list"]).unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::List => {}
                _ => panic!("Expected List action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_priority_parsing (lines 448-467) ---[0m
    fn test_priority_parsing() {
        let test_cases = vec![
            ("low", Priority::Low),
            ("medium", Priority::Medium),
            ("high", Priority::High),
            ("critical", Priority::Critical),
            ("unknown", Priority::Medium), // Default
        ];

        for (input, expected) in test_cases {
            let priority = match input {
                "low" => Priority::Low,
                "medium" => Priority::Medium,
                "high" => Priority::High,
                "critical" => Priority::Critical,
                _ => Priority::Medium,
            };
            assert_eq!(priority, expected);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:369-378 function test_create_default_config <-> ./crates/ccswarm/src/cli/tests.rs:400-423 function test_cli_runner_new
  Similarity: 79.75%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_create_default_config (lines 369-378) ---[0m
    fn test_create_default_config() {
        let temp_dir = TempDir::new().unwrap();
        let config = create_default_config(temp_dir.path()).unwrap();

        assert_eq!(config.project.name, "New ccswarm Project");
        assert!(config.agents.contains_key("frontend"));
        assert!(config.agents.contains_key("backend"));
        assert!(config.agents.contains_key("devops"));
        assert_eq!(config.coordination.communication_method, "json_files");
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_runner_new (lines 400-423) ---[0m
    async fn test_cli_runner_new() {
        let temp_dir = TempDir::new().unwrap();
        let config_path = temp_dir.path().join("ccswarm.json");

        // Create a simple config file
        let config = create_minimal_config(temp_dir.path()).unwrap();
        config.to_file(config_path.clone()).await.unwrap();

        let cli = Cli {
            config: config_path,
            repo: temp_dir.path().to_path_buf(),
            verbose: false,
            json: false,
            fix: false,
            command: Commands::Status {
                detailed: false,
                agent: None,
            },
        };

        let runner = CliRunner::new(&cli).await.unwrap();
        assert_eq!(runner.config.project.name, "Minimal ccswarm Project");
        assert!(runner.config.agents.is_empty());
    }

  ./crates/ccswarm/src/cli/tests.rs:42-59 function test_cli_parsing_start_command <-> ./crates/ccswarm/src/cli/tests.rs:92-102 function test_cli_parsing_status_command
  Similarity: 93.24%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command (lines 42-59) ---[0m
    fn test_cli_parsing_start_command() {
        let args = parse_args(&["start", "--daemon", "--port", "9090"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(daemon);
                assert_eq!(port, 9090);
                assert_eq!(isolation, "worktree"); // default value
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_status_command (lines 92-102) ---[0m
    fn test_cli_parsing_status_command() {
        let args = parse_args(&["status", "--detailed", "--agent", "frontend"]).unwrap();

        match args.command {
            Commands::Status { detailed, agent } => {
                assert!(detailed);
                assert_eq!(agent, Some("frontend".to_string()));
            }
            _ => panic!("Expected Status command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:42-59 function test_cli_parsing_start_command <-> ./crates/ccswarm/src/cli/tests.rs:160-170 function test_cli_parsing_review_command
  Similarity: 93.24%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command (lines 42-59) ---[0m
    fn test_cli_parsing_start_command() {
        let args = parse_args(&["start", "--daemon", "--port", "9090"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(daemon);
                assert_eq!(port, 9090);
                assert_eq!(isolation, "worktree"); // default value
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_review_command (lines 160-170) ---[0m
    fn test_cli_parsing_review_command() {
        let args = parse_args(&["review", "--agent", "backend", "--strict"]).unwrap();

        match args.command {
            Commands::Review { agent, strict } => {
                assert_eq!(agent, Some("backend".to_string()));
                assert!(strict);
            }
            _ => panic!("Expected Review command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:526-544 function test_delegation_strategy_parsing <-> ./crates/ccswarm/src/cli/tests.rs:547-557 function test_error_handling_invalid_priority
  Similarity: 89.58%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_delegation_strategy_parsing (lines 526-544) ---[0m
    fn test_delegation_strategy_parsing() {
        // Test strategy string parsing logic
        let test_cases = vec![
            ("content", true),
            ("load", true),
            ("expertise", true),
            ("workflow", true),
            ("hybrid", true),
            ("unknown", false), // Should default to hybrid
        ];

        for (input, is_valid_strategy) in test_cases {
            let is_valid = matches!(
                input,
                "content" | "load" | "expertise" | "workflow" | "hybrid"
            );
            assert_eq!(is_valid, is_valid_strategy);
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_error_handling_invalid_priority (lines 547-557) ---[0m
    fn test_error_handling_invalid_priority() {
        // Priority parsing should default to Medium for invalid values
        let priority = match "invalid-priority" {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };
        assert_eq!(priority, Priority::Medium);
    }

  ./crates/ccswarm/src/cli/tests.rs:62-79 function test_cli_parsing_start_command_with_isolation <-> ./crates/ccswarm/src/cli/tests.rs:173-183 function test_cli_parsing_worktree_list
  Similarity: 91.21%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command_with_isolation (lines 62-79) ---[0m
    fn test_cli_parsing_start_command_with_isolation() {
        let args = parse_args(&["start", "--isolation", "container"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(!daemon); // default false
                assert_eq!(port, 8080); // default port
                assert_eq!(isolation, "container");
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_list (lines 173-183) ---[0m
    fn test_cli_parsing_worktree_list() {
        let args = parse_args(&["worktree", "list"]).unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::List => {}
                _ => panic!("Expected List action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:42-59 function test_cli_parsing_start_command <-> ./crates/ccswarm/src/cli/tests.rs:173-183 function test_cli_parsing_worktree_list
  Similarity: 91.18%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command (lines 42-59) ---[0m
    fn test_cli_parsing_start_command() {
        let args = parse_args(&["start", "--daemon", "--port", "9090"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(daemon);
                assert_eq!(port, 9090);
                assert_eq!(isolation, "worktree"); // default value
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_list (lines 173-183) ---[0m
    fn test_cli_parsing_worktree_list() {
        let args = parse_args(&["worktree", "list"]).unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::List => {}
                _ => panic!("Expected List action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:62-79 function test_cli_parsing_start_command_with_isolation <-> ./crates/ccswarm/src/cli/tests.rs:92-102 function test_cli_parsing_status_command
  Similarity: 90.98%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command_with_isolation (lines 62-79) ---[0m
    fn test_cli_parsing_start_command_with_isolation() {
        let args = parse_args(&["start", "--isolation", "container"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(!daemon); // default false
                assert_eq!(port, 8080); // default port
                assert_eq!(isolation, "container");
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_status_command (lines 92-102) ---[0m
    fn test_cli_parsing_status_command() {
        let args = parse_args(&["status", "--detailed", "--agent", "frontend"]).unwrap();

        match args.command {
            Commands::Status { detailed, agent } => {
                assert!(detailed);
                assert_eq!(agent, Some("frontend".to_string()));
            }
            _ => panic!("Expected Status command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:62-79 function test_cli_parsing_start_command_with_isolation <-> ./crates/ccswarm/src/cli/tests.rs:160-170 function test_cli_parsing_review_command
  Similarity: 90.98%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command_with_isolation (lines 62-79) ---[0m
    fn test_cli_parsing_start_command_with_isolation() {
        let args = parse_args(&["start", "--isolation", "container"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(!daemon); // default false
                assert_eq!(port, 8080); // default port
                assert_eq!(isolation, "container");
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_review_command (lines 160-170) ---[0m
    fn test_cli_parsing_review_command() {
        let args = parse_args(&["review", "--agent", "backend", "--strict"]).unwrap();

        match args.command {
            Commands::Review { agent, strict } => {
                assert_eq!(agent, Some("backend".to_string()));
                assert!(strict);
            }
            _ => panic!("Expected Review command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:92-102 function test_cli_parsing_status_command <-> ./crates/ccswarm/src/cli/tests.rs:526-544 function test_delegation_strategy_parsing
  Similarity: 86.27%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_status_command (lines 92-102) ---[0m
    fn test_cli_parsing_status_command() {
        let args = parse_args(&["status", "--detailed", "--agent", "frontend"]).unwrap();

        match args.command {
            Commands::Status { detailed, agent } => {
                assert!(detailed);
                assert_eq!(agent, Some("frontend".to_string()));
            }
            _ => panic!("Expected Status command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_delegation_strategy_parsing (lines 526-544) ---[0m
    fn test_delegation_strategy_parsing() {
        // Test strategy string parsing logic
        let test_cases = vec![
            ("content", true),
            ("load", true),
            ("expertise", true),
            ("workflow", true),
            ("hybrid", true),
            ("unknown", false), // Should default to hybrid
        ];

        for (input, is_valid_strategy) in test_cases {
            let is_valid = matches!(
                input,
                "content" | "load" | "expertise" | "workflow" | "hybrid"
            );
            assert_eq!(is_valid, is_valid_strategy);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:160-170 function test_cli_parsing_review_command <-> ./crates/ccswarm/src/cli/tests.rs:526-544 function test_delegation_strategy_parsing
  Similarity: 86.27%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_review_command (lines 160-170) ---[0m
    fn test_cli_parsing_review_command() {
        let args = parse_args(&["review", "--agent", "backend", "--strict"]).unwrap();

        match args.command {
            Commands::Review { agent, strict } => {
                assert_eq!(agent, Some("backend".to_string()));
                assert!(strict);
            }
            _ => panic!("Expected Review command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_delegation_strategy_parsing (lines 526-544) ---[0m
    fn test_delegation_strategy_parsing() {
        // Test strategy string parsing logic
        let test_cases = vec![
            ("content", true),
            ("load", true),
            ("expertise", true),
            ("workflow", true),
            ("hybrid", true),
            ("unknown", false), // Should default to hybrid
        ];

        for (input, is_valid_strategy) in test_cases {
            let is_valid = matches!(
                input,
                "content" | "load" | "expertise" | "workflow" | "hybrid"
            );
            assert_eq!(is_valid, is_valid_strategy);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:173-183 function test_cli_parsing_worktree_list <-> ./crates/ccswarm/src/cli/tests.rs:526-544 function test_delegation_strategy_parsing
  Similarity: 86.27%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_list (lines 173-183) ---[0m
    fn test_cli_parsing_worktree_list() {
        let args = parse_args(&["worktree", "list"]).unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::List => {}
                _ => panic!("Expected List action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_delegation_strategy_parsing (lines 526-544) ---[0m
    fn test_delegation_strategy_parsing() {
        // Test strategy string parsing logic
        let test_cases = vec![
            ("content", true),
            ("load", true),
            ("expertise", true),
            ("workflow", true),
            ("hybrid", true),
            ("unknown", false), // Should default to hybrid
        ];

        for (input, is_valid_strategy) in test_cases {
            let is_valid = matches!(
                input,
                "content" | "load" | "expertise" | "workflow" | "hybrid"
            );
            assert_eq!(is_valid, is_valid_strategy);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:92-102 function test_cli_parsing_status_command <-> ./crates/ccswarm/src/cli/tests.rs:214-230 function test_cli_parsing_logs_command
  Similarity: 92.32%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_status_command (lines 92-102) ---[0m
    fn test_cli_parsing_status_command() {
        let args = parse_args(&["status", "--detailed", "--agent", "frontend"]).unwrap();

        match args.command {
            Commands::Status { detailed, agent } => {
                assert!(detailed);
                assert_eq!(agent, Some("frontend".to_string()));
            }
            _ => panic!("Expected Status command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_logs_command (lines 214-230) ---[0m
    fn test_cli_parsing_logs_command() {
        let args =
            parse_args(&["logs", "--follow", "--agent", "frontend", "--lines", "200"]).unwrap();

        match args.command {
            Commands::Logs {
                follow,
                agent,
                lines,
            } => {
                assert!(follow);
                assert_eq!(agent, Some("frontend".to_string()));
                assert_eq!(lines, 200);
            }
            _ => panic!("Expected Logs command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:42-59 function test_cli_parsing_start_command <-> ./crates/ccswarm/src/cli/tests.rs:547-557 function test_error_handling_invalid_priority
  Similarity: 89.12%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command (lines 42-59) ---[0m
    fn test_cli_parsing_start_command() {
        let args = parse_args(&["start", "--daemon", "--port", "9090"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(daemon);
                assert_eq!(port, 9090);
                assert_eq!(isolation, "worktree"); // default value
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_error_handling_invalid_priority (lines 547-557) ---[0m
    fn test_error_handling_invalid_priority() {
        // Priority parsing should default to Medium for invalid values
        let priority = match "invalid-priority" {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };
        assert_eq!(priority, Priority::Medium);
    }

  ./crates/ccswarm/src/cli/tests.rs:62-79 function test_cli_parsing_start_command_with_isolation <-> ./crates/ccswarm/src/cli/tests.rs:547-557 function test_error_handling_invalid_priority
  Similarity: 88.79%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command_with_isolation (lines 62-79) ---[0m
    fn test_cli_parsing_start_command_with_isolation() {
        let args = parse_args(&["start", "--isolation", "container"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(!daemon); // default false
                assert_eq!(port, 8080); // default port
                assert_eq!(isolation, "container");
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_error_handling_invalid_priority (lines 547-557) ---[0m
    fn test_error_handling_invalid_priority() {
        // Priority parsing should default to Medium for invalid values
        let priority = match "invalid-priority" {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };
        assert_eq!(priority, Priority::Medium);
    }

  ./crates/ccswarm/src/cli/tests.rs:160-170 function test_cli_parsing_review_command <-> ./crates/ccswarm/src/cli/tests.rs:214-230 function test_cli_parsing_logs_command
  Similarity: 91.55%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_review_command (lines 160-170) ---[0m
    fn test_cli_parsing_review_command() {
        let args = parse_args(&["review", "--agent", "backend", "--strict"]).unwrap();

        match args.command {
            Commands::Review { agent, strict } => {
                assert_eq!(agent, Some("backend".to_string()));
                assert!(strict);
            }
            _ => panic!("Expected Review command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_logs_command (lines 214-230) ---[0m
    fn test_cli_parsing_logs_command() {
        let args =
            parse_args(&["logs", "--follow", "--agent", "frontend", "--lines", "200"]).unwrap();

        match args.command {
            Commands::Logs {
                follow,
                agent,
                lines,
            } => {
                assert!(follow);
                assert_eq!(agent, Some("frontend".to_string()));
                assert_eq!(lines, 200);
            }
            _ => panic!("Expected Logs command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:173-183 function test_cli_parsing_worktree_list <-> ./crates/ccswarm/src/cli/tests.rs:214-230 function test_cli_parsing_logs_command
  Similarity: 91.55%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_list (lines 173-183) ---[0m
    fn test_cli_parsing_worktree_list() {
        let args = parse_args(&["worktree", "list"]).unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::List => {}
                _ => panic!("Expected List action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_logs_command (lines 214-230) ---[0m
    fn test_cli_parsing_logs_command() {
        let args =
            parse_args(&["logs", "--follow", "--agent", "frontend", "--lines", "200"]).unwrap();

        match args.command {
            Commands::Logs {
                follow,
                agent,
                lines,
            } => {
                assert!(follow);
                assert_eq!(agent, Some("frontend".to_string()));
                assert_eq!(lines, 200);
            }
            _ => panic!("Expected Logs command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:62-79 function test_cli_parsing_start_command_with_isolation <-> ./crates/ccswarm/src/cli/tests.rs:148-157 function test_cli_parsing_agents_command
  Similarity: 91.21%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command_with_isolation (lines 62-79) ---[0m
    fn test_cli_parsing_start_command_with_isolation() {
        let args = parse_args(&["start", "--isolation", "container"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(!daemon); // default false
                assert_eq!(port, 8080); // default port
                assert_eq!(isolation, "container");
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_agents_command (lines 148-157) ---[0m
    fn test_cli_parsing_agents_command() {
        let args = parse_args(&["agents", "--all"]).unwrap();

        match args.command {
            Commands::Agents { all } => {
                assert!(all);
            }
            _ => panic!("Expected Agents command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:42-59 function test_cli_parsing_start_command <-> ./crates/ccswarm/src/cli/tests.rs:148-157 function test_cli_parsing_agents_command
  Similarity: 91.18%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_start_command (lines 42-59) ---[0m
    fn test_cli_parsing_start_command() {
        let args = parse_args(&["start", "--daemon", "--port", "9090"]).unwrap();

        match args.command {
            Commands::Start {
                daemon,
                port,
                isolation,
                use_real_api,
            } => {
                assert!(daemon);
                assert_eq!(port, 9090);
                assert_eq!(isolation, "worktree"); // default value
                assert!(!use_real_api); // default false
            }
            _ => panic!("Expected Start command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_agents_command (lines 148-157) ---[0m
    fn test_cli_parsing_agents_command() {
        let args = parse_args(&["agents", "--all"]).unwrap();

        match args.command {
            Commands::Agents { all } => {
                assert!(all);
            }
            _ => panic!("Expected Agents command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:214-230 function test_cli_parsing_logs_command <-> ./crates/ccswarm/src/cli/tests.rs:547-557 function test_error_handling_invalid_priority
  Similarity: 89.58%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_logs_command (lines 214-230) ---[0m
    fn test_cli_parsing_logs_command() {
        let args =
            parse_args(&["logs", "--follow", "--agent", "frontend", "--lines", "200"]).unwrap();

        match args.command {
            Commands::Logs {
                follow,
                agent,
                lines,
            } => {
                assert!(follow);
                assert_eq!(agent, Some("frontend".to_string()));
                assert_eq!(lines, 200);
            }
            _ => panic!("Expected Logs command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_error_handling_invalid_priority (lines 547-557) ---[0m
    fn test_error_handling_invalid_priority() {
        // Priority parsing should default to Medium for invalid values
        let priority = match "invalid-priority" {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };
        assert_eq!(priority, Priority::Medium);
    }

  ./crates/ccswarm/src/cli/tests.rs:148-157 function test_cli_parsing_agents_command <-> ./crates/ccswarm/src/cli/tests.rs:526-544 function test_delegation_strategy_parsing
  Similarity: 86.27%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_agents_command (lines 148-157) ---[0m
    fn test_cli_parsing_agents_command() {
        let args = parse_args(&["agents", "--all"]).unwrap();

        match args.command {
            Commands::Agents { all } => {
                assert!(all);
            }
            _ => panic!("Expected Agents command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_delegation_strategy_parsing (lines 526-544) ---[0m
    fn test_delegation_strategy_parsing() {
        // Test strategy string parsing logic
        let test_cases = vec![
            ("content", true),
            ("load", true),
            ("expertise", true),
            ("workflow", true),
            ("hybrid", true),
            ("unknown", false), // Should default to hybrid
        ];

        for (input, is_valid_strategy) in test_cases {
            let is_valid = matches!(
                input,
                "content" | "load" | "expertise" | "workflow" | "hybrid"
            );
            assert_eq!(is_valid, is_valid_strategy);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:148-157 function test_cli_parsing_agents_command <-> ./crates/ccswarm/src/cli/tests.rs:214-230 function test_cli_parsing_logs_command
  Similarity: 91.55%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_agents_command (lines 148-157) ---[0m
    fn test_cli_parsing_agents_command() {
        let args = parse_args(&["agents", "--all"]).unwrap();

        match args.command {
            Commands::Agents { all } => {
                assert!(all);
            }
            _ => panic!("Expected Agents command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_logs_command (lines 214-230) ---[0m
    fn test_cli_parsing_logs_command() {
        let args =
            parse_args(&["logs", "--follow", "--agent", "frontend", "--lines", "200"]).unwrap();

        match args.command {
            Commands::Logs {
                follow,
                agent,
                lines,
            } => {
                assert!(follow);
                assert_eq!(agent, Some("frontend".to_string()));
                assert_eq!(lines, 200);
            }
            _ => panic!("Expected Logs command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:390-397 function test_create_frontend_only_config <-> ./crates/ccswarm/src/cli/tests.rs:426-445 function test_cli_runner_new_missing_config
  Similarity: 86.49%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_create_frontend_only_config (lines 390-397) ---[0m
    fn test_create_frontend_only_config() {
        let temp_dir = TempDir::new().unwrap();
        let config = create_frontend_only_config(temp_dir.path()).unwrap();

        assert_eq!(config.project.name, "Frontend ccswarm Project");
        assert_eq!(config.agents.len(), 1);
        assert!(config.agents.contains_key("frontend"));
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_runner_new_missing_config (lines 426-445) ---[0m
    async fn test_cli_runner_new_missing_config() {
        let temp_dir = TempDir::new().unwrap();
        let config_path = temp_dir.path().join("nonexistent.json");

        let cli = Cli {
            config: config_path,
            repo: temp_dir.path().to_path_buf(),
            verbose: false,
            json: false,
            fix: false,
            command: Commands::Status {
                detailed: false,
                agent: None,
            },
        };

        // Should use default config when file doesn't exist
        let runner = CliRunner::new(&cli).await.unwrap();
        assert_eq!(runner.config.project.name, "New ccswarm Project");
    }

  ./crates/ccswarm/src/cli/tests.rs:369-378 function test_create_default_config <-> ./crates/ccswarm/src/cli/tests.rs:426-445 function test_cli_runner_new_missing_config
  Similarity: 77.50%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_create_default_config (lines 369-378) ---[0m
    fn test_create_default_config() {
        let temp_dir = TempDir::new().unwrap();
        let config = create_default_config(temp_dir.path()).unwrap();

        assert_eq!(config.project.name, "New ccswarm Project");
        assert!(config.agents.contains_key("frontend"));
        assert!(config.agents.contains_key("backend"));
        assert!(config.agents.contains_key("devops"));
        assert_eq!(config.coordination.communication_method, "json_files");
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_runner_new_missing_config (lines 426-445) ---[0m
    async fn test_cli_runner_new_missing_config() {
        let temp_dir = TempDir::new().unwrap();
        let config_path = temp_dir.path().join("nonexistent.json");

        let cli = Cli {
            config: config_path,
            repo: temp_dir.path().to_path_buf(),
            verbose: false,
            json: false,
            fix: false,
            command: Commands::Status {
                detailed: false,
                agent: None,
            },
        };

        // Should use default config when file doesn't exist
        let runner = CliRunner::new(&cli).await.unwrap();
        assert_eq!(runner.config.project.name, "New ccswarm Project");
    }

  ./crates/ccswarm/src/cli/tests.rs:92-102 function test_cli_parsing_status_command <-> ./crates/ccswarm/src/cli/tests.rs:160-170 function test_cli_parsing_review_command
  Similarity: 94.79%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_status_command (lines 92-102) ---[0m
    fn test_cli_parsing_status_command() {
        let args = parse_args(&["status", "--detailed", "--agent", "frontend"]).unwrap();

        match args.command {
            Commands::Status { detailed, agent } => {
                assert!(detailed);
                assert_eq!(agent, Some("frontend".to_string()));
            }
            _ => panic!("Expected Status command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_review_command (lines 160-170) ---[0m
    fn test_cli_parsing_review_command() {
        let args = parse_args(&["review", "--agent", "backend", "--strict"]).unwrap();

        match args.command {
            Commands::Review { agent, strict } => {
                assert_eq!(agent, Some("backend".to_string()));
                assert!(strict);
            }
            _ => panic!("Expected Review command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:390-397 function test_create_frontend_only_config <-> ./crates/ccswarm/src/cli/tests.rs:560-580 function test_interactive_command_parsing
  Similarity: 71.61%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_create_frontend_only_config (lines 390-397) ---[0m
    fn test_create_frontend_only_config() {
        let temp_dir = TempDir::new().unwrap();
        let config = create_frontend_only_config(temp_dir.path()).unwrap();

        assert_eq!(config.project.name, "Frontend ccswarm Project");
        assert_eq!(config.agents.len(), 1);
        assert!(config.agents.contains_key("frontend"));
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_interactive_command_parsing (lines 560-580) ---[0m
    fn test_interactive_command_parsing() {
        // Test various interactive commands
        let commands = vec![
            ("quit", true),
            ("exit", true),
            ("help", true),
            ("stats", true),
            ("agents", true),
            ("analyze task description", true),
            ("delegate frontend Create UI", true),
            ("unknown command", false),
        ];

        for (cmd, should_be_valid) in commands {
            let is_valid = matches!(cmd, "quit" | "exit" | "help" | "stats" | "agents")
                || cmd.starts_with("analyze ")
                || cmd.starts_with("delegate ");

            assert_eq!(is_valid, should_be_valid);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:350-366 function test_cli_global_options <-> ./crates/ccswarm/src/cli/tests.rs:547-557 function test_error_handling_invalid_priority
  Similarity: 72.66%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_global_options (lines 350-366) ---[0m
    fn test_cli_global_options() {
        let args = parse_args(&[
            "--config",
            "custom.json",
            "--repo",
            "/path/to/repo",
            "--verbose",
            "--json",
            "status",
        ])
        .unwrap();

        assert_eq!(args.config, PathBuf::from("custom.json"));
        assert_eq!(args.repo, PathBuf::from("/path/to/repo"));
        assert!(args.verbose);
        assert!(args.json);
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_error_handling_invalid_priority (lines 547-557) ---[0m
    fn test_error_handling_invalid_priority() {
        // Priority parsing should default to Medium for invalid values
        let priority = match "invalid-priority" {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };
        assert_eq!(priority, Priority::Medium);
    }

  ./crates/ccswarm/src/cli/tests.rs:350-366 function test_cli_global_options <-> ./crates/ccswarm/src/cli/tests.rs:390-397 function test_create_frontend_only_config
  Similarity: 80.97%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_global_options (lines 350-366) ---[0m
    fn test_cli_global_options() {
        let args = parse_args(&[
            "--config",
            "custom.json",
            "--repo",
            "/path/to/repo",
            "--verbose",
            "--json",
            "status",
        ])
        .unwrap();

        assert_eq!(args.config, PathBuf::from("custom.json"));
        assert_eq!(args.repo, PathBuf::from("/path/to/repo"));
        assert!(args.verbose);
        assert!(args.json);
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_create_frontend_only_config (lines 390-397) ---[0m
    fn test_create_frontend_only_config() {
        let temp_dir = TempDir::new().unwrap();
        let config = create_frontend_only_config(temp_dir.path()).unwrap();

        assert_eq!(config.project.name, "Frontend ccswarm Project");
        assert_eq!(config.agents.len(), 1);
        assert!(config.agents.contains_key("frontend"));
    }

  ./crates/ccswarm/src/cli/tests.rs:148-157 function test_cli_parsing_agents_command <-> ./crates/ccswarm/src/cli/tests.rs:173-183 function test_cli_parsing_worktree_list
  Similarity: 95.84%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_agents_command (lines 148-157) ---[0m
    fn test_cli_parsing_agents_command() {
        let args = parse_args(&["agents", "--all"]).unwrap();

        match args.command {
            Commands::Agents { all } => {
                assert!(all);
            }
            _ => panic!("Expected Agents command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_list (lines 173-183) ---[0m
    fn test_cli_parsing_worktree_list() {
        let args = parse_args(&["worktree", "list"]).unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::List => {}
                _ => panic!("Expected List action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:92-102 function test_cli_parsing_status_command <-> ./crates/ccswarm/src/cli/tests.rs:173-183 function test_cli_parsing_worktree_list
  Similarity: 90.94%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_status_command (lines 92-102) ---[0m
    fn test_cli_parsing_status_command() {
        let args = parse_args(&["status", "--detailed", "--agent", "frontend"]).unwrap();

        match args.command {
            Commands::Status { detailed, agent } => {
                assert!(detailed);
                assert_eq!(agent, Some("frontend".to_string()));
            }
            _ => panic!("Expected Status command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_list (lines 173-183) ---[0m
    fn test_cli_parsing_worktree_list() {
        let args = parse_args(&["worktree", "list"]).unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::List => {}
                _ => panic!("Expected List action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:160-170 function test_cli_parsing_review_command <-> ./crates/ccswarm/src/cli/tests.rs:173-183 function test_cli_parsing_worktree_list
  Similarity: 90.94%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_review_command (lines 160-170) ---[0m
    fn test_cli_parsing_review_command() {
        let args = parse_args(&["review", "--agent", "backend", "--strict"]).unwrap();

        match args.command {
            Commands::Review { agent, strict } => {
                assert_eq!(agent, Some("backend".to_string()));
                assert!(strict);
            }
            _ => panic!("Expected Review command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_list (lines 173-183) ---[0m
    fn test_cli_parsing_worktree_list() {
        let args = parse_args(&["worktree", "list"]).unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::List => {}
                _ => panic!("Expected List action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:381-387 function test_create_minimal_config <-> ./crates/ccswarm/src/cli/tests.rs:526-544 function test_delegation_strategy_parsing
  Similarity: 75.00%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_create_minimal_config (lines 381-387) ---[0m
    fn test_create_minimal_config() {
        let temp_dir = TempDir::new().unwrap();
        let config = create_minimal_config(temp_dir.path()).unwrap();

        assert_eq!(config.project.name, "Minimal ccswarm Project");
        assert!(config.agents.is_empty());
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_delegation_strategy_parsing (lines 526-544) ---[0m
    fn test_delegation_strategy_parsing() {
        // Test strategy string parsing logic
        let test_cases = vec![
            ("content", true),
            ("load", true),
            ("expertise", true),
            ("workflow", true),
            ("hybrid", true),
            ("unknown", false), // Should default to hybrid
        ];

        for (input, is_valid_strategy) in test_cases {
            let is_valid = matches!(
                input,
                "content" | "load" | "expertise" | "workflow" | "hybrid"
            );
            assert_eq!(is_valid, is_valid_strategy);
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:92-102 function test_cli_parsing_status_command <-> ./crates/ccswarm/src/cli/tests.rs:148-157 function test_cli_parsing_agents_command
  Similarity: 91.54%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_status_command (lines 92-102) ---[0m
    fn test_cli_parsing_status_command() {
        let args = parse_args(&["status", "--detailed", "--agent", "frontend"]).unwrap();

        match args.command {
            Commands::Status { detailed, agent } => {
                assert!(detailed);
                assert_eq!(agent, Some("frontend".to_string()));
            }
            _ => panic!("Expected Status command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_agents_command (lines 148-157) ---[0m
    fn test_cli_parsing_agents_command() {
        let args = parse_args(&["agents", "--all"]).unwrap();

        match args.command {
            Commands::Agents { all } => {
                assert!(all);
            }
            _ => panic!("Expected Agents command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:148-157 function test_cli_parsing_agents_command <-> ./crates/ccswarm/src/cli/tests.rs:160-170 function test_cli_parsing_review_command
  Similarity: 91.54%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_agents_command (lines 148-157) ---[0m
    fn test_cli_parsing_agents_command() {
        let args = parse_args(&["agents", "--all"]).unwrap();

        match args.command {
            Commands::Agents { all } => {
                assert!(all);
            }
            _ => panic!("Expected Agents command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_review_command (lines 160-170) ---[0m
    fn test_cli_parsing_review_command() {
        let args = parse_args(&["review", "--agent", "backend", "--strict"]).unwrap();

        match args.command {
            Commands::Review { agent, strict } => {
                assert_eq!(agent, Some("backend".to_string()));
                assert!(strict);
            }
            _ => panic!("Expected Review command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:92-102 function test_cli_parsing_status_command <-> ./crates/ccswarm/src/cli/tests.rs:547-557 function test_error_handling_invalid_priority
  Similarity: 87.35%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_status_command (lines 92-102) ---[0m
    fn test_cli_parsing_status_command() {
        let args = parse_args(&["status", "--detailed", "--agent", "frontend"]).unwrap();

        match args.command {
            Commands::Status { detailed, agent } => {
                assert!(detailed);
                assert_eq!(agent, Some("frontend".to_string()));
            }
            _ => panic!("Expected Status command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_error_handling_invalid_priority (lines 547-557) ---[0m
    fn test_error_handling_invalid_priority() {
        // Priority parsing should default to Medium for invalid values
        let priority = match "invalid-priority" {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };
        assert_eq!(priority, Priority::Medium);
    }

  ./crates/ccswarm/src/cli/tests.rs:160-170 function test_cli_parsing_review_command <-> ./crates/ccswarm/src/cli/tests.rs:547-557 function test_error_handling_invalid_priority
  Similarity: 87.35%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_review_command (lines 160-170) ---[0m
    fn test_cli_parsing_review_command() {
        let args = parse_args(&["review", "--agent", "backend", "--strict"]).unwrap();

        match args.command {
            Commands::Review { agent, strict } => {
                assert_eq!(agent, Some("backend".to_string()));
                assert!(strict);
            }
            _ => panic!("Expected Review command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_error_handling_invalid_priority (lines 547-557) ---[0m
    fn test_error_handling_invalid_priority() {
        // Priority parsing should default to Medium for invalid values
        let priority = match "invalid-priority" {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };
        assert_eq!(priority, Priority::Medium);
    }

  ./crates/ccswarm/src/cli/tests.rs:173-183 function test_cli_parsing_worktree_list <-> ./crates/ccswarm/src/cli/tests.rs:547-557 function test_error_handling_invalid_priority
  Similarity: 85.35%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_list (lines 173-183) ---[0m
    fn test_cli_parsing_worktree_list() {
        let args = parse_args(&["worktree", "list"]).unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::List => {}
                _ => panic!("Expected List action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_error_handling_invalid_priority (lines 547-557) ---[0m
    fn test_error_handling_invalid_priority() {
        // Priority parsing should default to Medium for invalid values
        let priority = match "invalid-priority" {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };
        assert_eq!(priority, Priority::Medium);
    }

  ./crates/ccswarm/src/cli/tests.rs:350-366 function test_cli_global_options <-> ./crates/ccswarm/src/cli/tests.rs:381-387 function test_create_minimal_config
  Similarity: 77.18%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_global_options (lines 350-366) ---[0m
    fn test_cli_global_options() {
        let args = parse_args(&[
            "--config",
            "custom.json",
            "--repo",
            "/path/to/repo",
            "--verbose",
            "--json",
            "status",
        ])
        .unwrap();

        assert_eq!(args.config, PathBuf::from("custom.json"));
        assert_eq!(args.repo, PathBuf::from("/path/to/repo"));
        assert!(args.verbose);
        assert!(args.json);
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_create_minimal_config (lines 381-387) ---[0m
    fn test_create_minimal_config() {
        let temp_dir = TempDir::new().unwrap();
        let config = create_minimal_config(temp_dir.path()).unwrap();

        assert_eq!(config.project.name, "Minimal ccswarm Project");
        assert!(config.agents.is_empty());
    }

  ./crates/ccswarm/src/cli/tests.rs:148-157 function test_cli_parsing_agents_command <-> ./crates/ccswarm/src/cli/tests.rs:547-557 function test_error_handling_invalid_priority
  Similarity: 82.99%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_agents_command (lines 148-157) ---[0m
    fn test_cli_parsing_agents_command() {
        let args = parse_args(&["agents", "--all"]).unwrap();

        match args.command {
            Commands::Agents { all } => {
                assert!(all);
            }
            _ => panic!("Expected Agents command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_error_handling_invalid_priority (lines 547-557) ---[0m
    fn test_error_handling_invalid_priority() {
        // Priority parsing should default to Medium for invalid values
        let priority = match "invalid-priority" {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };
        assert_eq!(priority, Priority::Medium);
    }

  ./crates/ccswarm/src/cli/tests.rs:82-89 function test_cli_parsing_tui_command <-> ./crates/ccswarm/src/cli/tests.rs:92-102 function test_cli_parsing_status_command
  Similarity: 90.34%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_tui_command (lines 82-89) ---[0m
    fn test_cli_parsing_tui_command() {
        let args = parse_args(&["tui"]).unwrap();

        match args.command {
            Commands::Tui => {}
            _ => panic!("Expected Tui command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_status_command (lines 92-102) ---[0m
    fn test_cli_parsing_status_command() {
        let args = parse_args(&["status", "--detailed", "--agent", "frontend"]).unwrap();

        match args.command {
            Commands::Status { detailed, agent } => {
                assert!(detailed);
                assert_eq!(agent, Some("frontend".to_string()));
            }
            _ => panic!("Expected Status command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:82-89 function test_cli_parsing_tui_command <-> ./crates/ccswarm/src/cli/tests.rs:160-170 function test_cli_parsing_review_command
  Similarity: 90.34%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_tui_command (lines 82-89) ---[0m
    fn test_cli_parsing_tui_command() {
        let args = parse_args(&["tui"]).unwrap();

        match args.command {
            Commands::Tui => {}
            _ => panic!("Expected Tui command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_review_command (lines 160-170) ---[0m
    fn test_cli_parsing_review_command() {
        let args = parse_args(&["review", "--agent", "backend", "--strict"]).unwrap();

        match args.command {
            Commands::Review { agent, strict } => {
                assert_eq!(agent, Some("backend".to_string()));
                assert!(strict);
            }
            _ => panic!("Expected Review command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:82-89 function test_cli_parsing_tui_command <-> ./crates/ccswarm/src/cli/tests.rs:173-183 function test_cli_parsing_worktree_list
  Similarity: 89.90%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_tui_command (lines 82-89) ---[0m
    fn test_cli_parsing_tui_command() {
        let args = parse_args(&["tui"]).unwrap();

        match args.command {
            Commands::Tui => {}
            _ => panic!("Expected Tui command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_worktree_list (lines 173-183) ---[0m
    fn test_cli_parsing_worktree_list() {
        let args = parse_args(&["worktree", "list"]).unwrap();

        match args.command {
            Commands::Worktree { action } => match action {
                WorktreeAction::List => {}
                _ => panic!("Expected List action"),
            },
            _ => panic!("Expected Worktree command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:82-89 function test_cli_parsing_tui_command <-> ./crates/ccswarm/src/cli/tests.rs:148-157 function test_cli_parsing_agents_command
  Similarity: 88.64%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_tui_command (lines 82-89) ---[0m
    fn test_cli_parsing_tui_command() {
        let args = parse_args(&["tui"]).unwrap();

        match args.command {
            Commands::Tui => {}
            _ => panic!("Expected Tui command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_agents_command (lines 148-157) ---[0m
    fn test_cli_parsing_agents_command() {
        let args = parse_args(&["agents", "--all"]).unwrap();

        match args.command {
            Commands::Agents { all } => {
                assert!(all);
            }
            _ => panic!("Expected Agents command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:82-89 function test_cli_parsing_tui_command <-> ./crates/ccswarm/src/cli/tests.rs:547-557 function test_error_handling_invalid_priority
  Similarity: 82.99%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_tui_command (lines 82-89) ---[0m
    fn test_cli_parsing_tui_command() {
        let args = parse_args(&["tui"]).unwrap();

        match args.command {
            Commands::Tui => {}
            _ => panic!("Expected Tui command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_error_handling_invalid_priority (lines 547-557) ---[0m
    fn test_error_handling_invalid_priority() {
        // Priority parsing should default to Medium for invalid values
        let priority = match "invalid-priority" {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };
        assert_eq!(priority, Priority::Medium);
    }

  ./crates/ccswarm/src/cli/tests.rs:8-12 function parse_args <-> ./crates/ccswarm/src/cli/tests.rs:547-557 function test_error_handling_invalid_priority
  Similarity: 88.97%

[36m--- ./crates/ccswarm/src/cli/tests.rs:parse_args (lines 8-12) ---[0m
    fn parse_args(args: &[&str]) -> Result<Cli> {
        let mut full_args = vec!["ccswarm"];
        full_args.extend_from_slice(args);
        Ok(Cli::try_parse_from(full_args)?)
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_error_handling_invalid_priority (lines 547-557) ---[0m
    fn test_error_handling_invalid_priority() {
        // Priority parsing should default to Medium for invalid values
        let priority = match "invalid-priority" {
            "low" => Priority::Low,
            "medium" => Priority::Medium,
            "high" => Priority::High,
            "critical" => Priority::Critical,
            _ => Priority::Medium,
        };
        assert_eq!(priority, Priority::Medium);
    }

  ./crates/ccswarm/src/cli/tests.rs:148-157 function test_cli_parsing_agents_command <-> ./crates/ccswarm/src/cli/tests.rs:519-523 function test_subcommand_help
  Similarity: 83.70%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_agents_command (lines 148-157) ---[0m
    fn test_cli_parsing_agents_command() {
        let args = parse_args(&["agents", "--all"]).unwrap();

        match args.command {
            Commands::Agents { all } => {
                assert!(all);
            }
            _ => panic!("Expected Agents command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_subcommand_help (lines 519-523) ---[0m
    fn test_subcommand_help() {
        let args = parse_args(&["task", "--help"]);
        // Should fail because help is handled by clap automatically
        assert!(args.is_err());
    }

  ./crates/ccswarm/src/cli/tests.rs:381-387 function test_create_minimal_config <-> ./crates/ccswarm/src/cli/tests.rs:390-397 function test_create_frontend_only_config
  Similarity: 79.51%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_create_minimal_config (lines 381-387) ---[0m
    fn test_create_minimal_config() {
        let temp_dir = TempDir::new().unwrap();
        let config = create_minimal_config(temp_dir.path()).unwrap();

        assert_eq!(config.project.name, "Minimal ccswarm Project");
        assert!(config.agents.is_empty());
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_create_frontend_only_config (lines 390-397) ---[0m
    fn test_create_frontend_only_config() {
        let temp_dir = TempDir::new().unwrap();
        let config = create_frontend_only_config(temp_dir.path()).unwrap();

        assert_eq!(config.project.name, "Frontend ccswarm Project");
        assert_eq!(config.agents.len(), 1);
        assert!(config.agents.contains_key("frontend"));
    }

  ./crates/ccswarm/src/cli/tests.rs:8-12 function parse_args <-> ./crates/ccswarm/src/cli/tests.rs:148-157 function test_cli_parsing_agents_command
  Similarity: 75.19%

[36m--- ./crates/ccswarm/src/cli/tests.rs:parse_args (lines 8-12) ---[0m
    fn parse_args(args: &[&str]) -> Result<Cli> {
        let mut full_args = vec!["ccswarm"];
        full_args.extend_from_slice(args);
        Ok(Cli::try_parse_from(full_args)?)
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_agents_command (lines 148-157) ---[0m
    fn test_cli_parsing_agents_command() {
        let args = parse_args(&["agents", "--all"]).unwrap();

        match args.command {
            Commands::Agents { all } => {
                assert!(all);
            }
            _ => panic!("Expected Agents command"),
        }
    }

  ./crates/ccswarm/src/cli/tests.rs:82-89 function test_cli_parsing_tui_command <-> ./crates/ccswarm/src/cli/tests.rs:505-509 function test_version_display
  Similarity: 78.71%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_tui_command (lines 82-89) ---[0m
    fn test_cli_parsing_tui_command() {
        let args = parse_args(&["tui"]).unwrap();

        match args.command {
            Commands::Tui => {}
            _ => panic!("Expected Tui command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_version_display (lines 505-509) ---[0m
    fn test_version_display() {
        let args = parse_args(&["--version"]);
        // Should fail because version is handled by clap automatically
        assert!(args.is_err());
    }

  ./crates/ccswarm/src/cli/tests.rs:82-89 function test_cli_parsing_tui_command <-> ./crates/ccswarm/src/cli/tests.rs:512-516 function test_help_display
  Similarity: 78.71%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_tui_command (lines 82-89) ---[0m
    fn test_cli_parsing_tui_command() {
        let args = parse_args(&["tui"]).unwrap();

        match args.command {
            Commands::Tui => {}
            _ => panic!("Expected Tui command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_help_display (lines 512-516) ---[0m
    fn test_help_display() {
        let args = parse_args(&["--help"]);
        // Should fail because help is handled by clap automatically
        assert!(args.is_err());
    }

  ./crates/ccswarm/src/cli/tests.rs:82-89 function test_cli_parsing_tui_command <-> ./crates/ccswarm/src/cli/tests.rs:519-523 function test_subcommand_help
  Similarity: 78.71%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_cli_parsing_tui_command (lines 82-89) ---[0m
    fn test_cli_parsing_tui_command() {
        let args = parse_args(&["tui"]).unwrap();

        match args.command {
            Commands::Tui => {}
            _ => panic!("Expected Tui command"),
        }
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_subcommand_help (lines 519-523) ---[0m
    fn test_subcommand_help() {
        let args = parse_args(&["task", "--help"]);
        // Should fail because help is handled by clap automatically
        assert!(args.is_err());
    }

  ./crates/ccswarm/src/cli/tests.rs:505-509 function test_version_display <-> ./crates/ccswarm/src/cli/tests.rs:512-516 function test_help_display
  Similarity: 99.17%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_version_display (lines 505-509) ---[0m
    fn test_version_display() {
        let args = parse_args(&["--version"]);
        // Should fail because version is handled by clap automatically
        assert!(args.is_err());
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_help_display (lines 512-516) ---[0m
    fn test_help_display() {
        let args = parse_args(&["--help"]);
        // Should fail because help is handled by clap automatically
        assert!(args.is_err());
    }

  ./crates/ccswarm/src/cli/tests.rs:512-516 function test_help_display <-> ./crates/ccswarm/src/cli/tests.rs:519-523 function test_subcommand_help
  Similarity: 92.68%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_help_display (lines 512-516) ---[0m
    fn test_help_display() {
        let args = parse_args(&["--help"]);
        // Should fail because help is handled by clap automatically
        assert!(args.is_err());
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_subcommand_help (lines 519-523) ---[0m
    fn test_subcommand_help() {
        let args = parse_args(&["task", "--help"]);
        // Should fail because help is handled by clap automatically
        assert!(args.is_err());
    }

  ./crates/ccswarm/src/cli/tests.rs:505-509 function test_version_display <-> ./crates/ccswarm/src/cli/tests.rs:519-523 function test_subcommand_help
  Similarity: 91.95%

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_version_display (lines 505-509) ---[0m
    fn test_version_display() {
        let args = parse_args(&["--version"]);
        // Should fail because version is handled by clap automatically
        assert!(args.is_err());
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_subcommand_help (lines 519-523) ---[0m
    fn test_subcommand_help() {
        let args = parse_args(&["task", "--help"]);
        // Should fail because help is handled by clap automatically
        assert!(args.is_err());
    }

  ./crates/ccswarm/src/cli/tests.rs:8-12 function parse_args <-> ./crates/ccswarm/src/cli/tests.rs:505-509 function test_version_display
  Similarity: 82.05%

[36m--- ./crates/ccswarm/src/cli/tests.rs:parse_args (lines 8-12) ---[0m
    fn parse_args(args: &[&str]) -> Result<Cli> {
        let mut full_args = vec!["ccswarm"];
        full_args.extend_from_slice(args);
        Ok(Cli::try_parse_from(full_args)?)
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_version_display (lines 505-509) ---[0m
    fn test_version_display() {
        let args = parse_args(&["--version"]);
        // Should fail because version is handled by clap automatically
        assert!(args.is_err());
    }

  ./crates/ccswarm/src/cli/tests.rs:8-12 function parse_args <-> ./crates/ccswarm/src/cli/tests.rs:512-516 function test_help_display
  Similarity: 82.05%

[36m--- ./crates/ccswarm/src/cli/tests.rs:parse_args (lines 8-12) ---[0m
    fn parse_args(args: &[&str]) -> Result<Cli> {
        let mut full_args = vec!["ccswarm"];
        full_args.extend_from_slice(args);
        Ok(Cli::try_parse_from(full_args)?)
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_help_display (lines 512-516) ---[0m
    fn test_help_display() {
        let args = parse_args(&["--help"]);
        // Should fail because help is handled by clap automatically
        assert!(args.is_err());
    }

  ./crates/ccswarm/src/cli/tests.rs:8-12 function parse_args <-> ./crates/ccswarm/src/cli/tests.rs:519-523 function test_subcommand_help
  Similarity: 82.05%

[36m--- ./crates/ccswarm/src/cli/tests.rs:parse_args (lines 8-12) ---[0m
    fn parse_args(args: &[&str]) -> Result<Cli> {
        let mut full_args = vec!["ccswarm"];
        full_args.extend_from_slice(args);
        Ok(Cli::try_parse_from(full_args)?)
    }

[36m--- ./crates/ccswarm/src/cli/tests.rs:test_subcommand_help (lines 519-523) ---[0m
    fn test_subcommand_help() {
        let args = parse_args(&["task", "--help"]);
        // Should fail because help is handled by clap automatically
        assert!(args.is_err());
    }


Duplicates in ./crates/ccswarm/src/providers/aider.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/providers/aider.rs:163-250 method generate_aider_instructions <-> ./crates/ccswarm/src/providers/aider.rs:455-512 method execute_task
  Similarity: 91.96%
  Classes: AiderExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/aider.rs:generate_aider_instructions (lines 163-250) ---[0m
    fn generate_aider_instructions(&self, task: &Task) -> String {
        let base_instructions = "## Aider Instructions\n\
                                Please analyze the codebase and implement the requested changes.\n";

        let task_specific = match task.task_type {
            TaskType::Development => {
                "- Focus on implementing clean, maintainable code\n\
                 - Follow existing code patterns and conventions\n\
                 - Add appropriate error handling\n\
                 - Include relevant tests if applicable\n"
            }
            TaskType::Testing => {
                "- Write comprehensive tests for the feature/bug\n\
                 - Ensure good test coverage\n\
                 - Follow testing best practices\n\
                 - Use appropriate testing frameworks\n"
            }
            TaskType::Documentation => {
                "- Create clear, comprehensive documentation\n\
                 - Include code examples where appropriate\n\
                 - Follow documentation standards\n\
                 - Update related documentation files\n"
            }
            TaskType::Bugfix => {
                "- Identify and fix the root cause\n\
                 - Add regression tests\n\
                 - Ensure the fix doesn't break existing functionality\n\
                 - Document the fix if necessary\n"
            }
            TaskType::Infrastructure => {
                "- Focus on infrastructure and deployment concerns\n\
                 - Ensure configurations are secure and scalable\n\
                 - Follow infrastructure best practices\n\
                 - Test configuration changes thoroughly\n"
            }
            TaskType::Coordination => {
                "- This is a coordination task\n\
                 - Focus on planning and organizing\n\
                 - Do not implement code directly\n\
                 - Provide clear guidance for other agents\n"
            }
            TaskType::Review => {
                "- Review code and documentation quality\n\
                 - Check for security issues and best practices\n\
                 - Provide constructive feedback\n\
                 - Ensure compliance with coding standards\n"
            }
            TaskType::Feature => {
                "- Implement new feature functionality\n\
                 - Ensure proper integration with existing code\n\
                 - Follow feature specifications carefully\n\
                 - Add appropriate tests and documentation\n"
            }
            TaskType::Remediation => {
                "- Fix the quality issues identified in the review\n\
                 - Follow the specific instructions provided\n\
                 - Ensure all issues are resolved completely\n\
                 - Add tests to prevent regression\n\
                 - Improve code quality as needed\n"
            }
            TaskType::Bug => {
                "- Identify and fix the bug\n\
                 - Add regression tests\n\
                 - Ensure the fix doesn't break existing functionality\n\
                 - Document the fix if necessary\n"
            }
            TaskType::Assistance => {
                "- Provide help and support for the requested task\n\
                 - Collaborate with the requesting agent\n\
                 - Share expertise and knowledge\n\
                 - Help unblock the original task\n"
            }
            TaskType::Research => {
                "- Research and gather information\n\
                 - Analyze search results and findings\n\
                 - Apply insights to improve implementation\n\
                 - Document key findings and recommendations\n"
            }
        };

        let git_instructions = if self.config.auto_commit {
            "\n**Git Behavior**: Auto-commit is enabled. Changes will be committed automatically."
        } else {
            "\n**Git Behavior**: Auto-commit is disabled. Review changes before committing."
        };

        format!("{}{}{}", base_instructions, task_specific, git_instructions)
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:execute_task (lines 455-512) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate enhanced prompt with context and boundaries
        let prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with Aider (model: {}) for agent {}",
            task.description,
            self.config.model,
            identity.agent_id
        );

        // Execute the prompt
        match self.execute_prompt(&prompt, identity, working_dir).await {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_aider_output(output, task, duration);

                tracing::info!(
                    "Aider task completed in {:?} for agent {} (files changed: {})",
                    duration,
                    identity.agent_id,
                    result.output["files_changed"]
                        .as_array()
                        .map(|a| a.len())
                        .unwrap_or(0)
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Aider task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "aider",
                        "model": self.config.model,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/aider.rs:163-250 method generate_aider_instructions <-> ./crates/ccswarm/src/providers/aider.rs:514-568 method health_check
  Similarity: 89.61%
  Classes: AiderExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/aider.rs:generate_aider_instructions (lines 163-250) ---[0m
    fn generate_aider_instructions(&self, task: &Task) -> String {
        let base_instructions = "## Aider Instructions\n\
                                Please analyze the codebase and implement the requested changes.\n";

        let task_specific = match task.task_type {
            TaskType::Development => {
                "- Focus on implementing clean, maintainable code\n\
                 - Follow existing code patterns and conventions\n\
                 - Add appropriate error handling\n\
                 - Include relevant tests if applicable\n"
            }
            TaskType::Testing => {
                "- Write comprehensive tests for the feature/bug\n\
                 - Ensure good test coverage\n\
                 - Follow testing best practices\n\
                 - Use appropriate testing frameworks\n"
            }
            TaskType::Documentation => {
                "- Create clear, comprehensive documentation\n\
                 - Include code examples where appropriate\n\
                 - Follow documentation standards\n\
                 - Update related documentation files\n"
            }
            TaskType::Bugfix => {
                "- Identify and fix the root cause\n\
                 - Add regression tests\n\
                 - Ensure the fix doesn't break existing functionality\n\
                 - Document the fix if necessary\n"
            }
            TaskType::Infrastructure => {
                "- Focus on infrastructure and deployment concerns\n\
                 - Ensure configurations are secure and scalable\n\
                 - Follow infrastructure best practices\n\
                 - Test configuration changes thoroughly\n"
            }
            TaskType::Coordination => {
                "- This is a coordination task\n\
                 - Focus on planning and organizing\n\
                 - Do not implement code directly\n\
                 - Provide clear guidance for other agents\n"
            }
            TaskType::Review => {
                "- Review code and documentation quality\n\
                 - Check for security issues and best practices\n\
                 - Provide constructive feedback\n\
                 - Ensure compliance with coding standards\n"
            }
            TaskType::Feature => {
                "- Implement new feature functionality\n\
                 - Ensure proper integration with existing code\n\
                 - Follow feature specifications carefully\n\
                 - Add appropriate tests and documentation\n"
            }
            TaskType::Remediation => {
                "- Fix the quality issues identified in the review\n\
                 - Follow the specific instructions provided\n\
                 - Ensure all issues are resolved completely\n\
                 - Add tests to prevent regression\n\
                 - Improve code quality as needed\n"
            }
            TaskType::Bug => {
                "- Identify and fix the bug\n\
                 - Add regression tests\n\
                 - Ensure the fix doesn't break existing functionality\n\
                 - Document the fix if necessary\n"
            }
            TaskType::Assistance => {
                "- Provide help and support for the requested task\n\
                 - Collaborate with the requesting agent\n\
                 - Share expertise and knowledge\n\
                 - Help unblock the original task\n"
            }
            TaskType::Research => {
                "- Research and gather information\n\
                 - Analyze search results and findings\n\
                 - Apply insights to improve implementation\n\
                 - Document key findings and recommendations\n"
            }
        };

        let git_instructions = if self.config.auto_commit {
            "\n**Git Behavior**: Auto-commit is enabled. Changes will be committed automatically."
        } else {
            "\n**Git Behavior**: Auto-commit is disabled. Review changes before committing."
        };

        format!("{}{}{}", base_instructions, task_specific, git_instructions)
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:health_check (lines 514-568) ---[0m
    async fn health_check(&self, working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        let executable = if let Some(path) = &self.config.executable_path {
            path.to_string_lossy().to_string()
        } else {
            "aider".to_string()
        };

        // Try to execute version check
        let result = Command::new(&executable)
            .arg("--version")
            .current_dir(working_dir)
            .output()
            .await;

        let duration = start.elapsed();
        let response_time_ms = duration.as_millis() as u64;

        match result {
            Ok(output) if output.status.success() => {
                let version_output = String::from_utf8_lossy(&output.stdout);
                let version = version_output
                    .lines()
                    .find(|line| line.contains("aider"))
                    .unwrap_or(version_output.trim())
                    .to_string();

                Ok(ProviderHealthStatus {
                    is_healthy: true,
                    version: Some(version),
                    last_check: chrono::Utc::now(),
                    error_message: None,
                    response_time_ms: Some(response_time_ms),
                })
            }
            Ok(output) => {
                let error = String::from_utf8_lossy(&output.stderr).to_string();
                Ok(ProviderHealthStatus {
                    is_healthy: false,
                    version: None,
                    last_check: chrono::Utc::now(),
                    error_message: Some(format!("Command failed: {}", error)),
                    response_time_ms: Some(response_time_ms),
                })
            }
            Err(e) => Ok(ProviderHealthStatus {
                is_healthy: false,
                version: None,
                last_check: chrono::Utc::now(),
                error_message: Some(format!("Failed to execute Aider: {}", e)),
                response_time_ms: Some(response_time_ms),
            }),
        }
    }

  ./crates/ccswarm/src/providers/aider.rs:58-160 method generate_agent_boundaries <-> ./crates/ccswarm/src/providers/aider.rs:570-599 method get_capabilities
  Similarity: 91.75%
  Classes: AiderExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/aider.rs:generate_agent_boundaries (lines 58-160) ---[0m
    fn generate_agent_boundaries(&self, specialization: &crate::identity::AgentRole) -> String {
        match specialization {
            crate::identity::AgentRole::Frontend {
                technologies,
                responsibilities,
                ..
            } => {
                format!(
                    "## Agent Specialization: Frontend\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     \n\
                     **IMPORTANT**: Focus only on frontend concerns. Do not modify:\n\
                     - Backend API endpoints or server code\n\
                     - Database schemas or migrations\n\
                     - Infrastructure or deployment configurations\n\
                     \n",
                    technologies.join(", "),
                    responsibilities.join(", ")
                )
            }
            crate::identity::AgentRole::Backend {
                technologies,
                responsibilities,
                ..
            } => {
                format!(
                    "## Agent Specialization: Backend\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     \n\
                     **IMPORTANT**: Focus only on backend concerns. Do not modify:\n\
                     - Frontend components or UI code\n\
                     - Client-side styling or layouts\n\
                     - Frontend build configurations\n\
                     \n",
                    technologies.join(", "),
                    responsibilities.join(", ")
                )
            }
            crate::identity::AgentRole::DevOps {
                technologies,
                responsibilities,
                ..
            } => {
                format!(
                    "## Agent Specialization: DevOps\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     \n\
                     **IMPORTANT**: Focus only on infrastructure and deployment. Do not modify:\n\
                     - Application business logic\n\
                     - Frontend or backend feature code\n\
                     - Database application schemas\n\
                     \n",
                    technologies.join(", "),
                    responsibilities.join(", ")
                )
            }
            crate::identity::AgentRole::QA {
                responsibilities, ..
            } => {
                format!(
                    "## Agent Specialization: QA\n\
                     Responsibilities: {}\n\
                     \n\
                     **IMPORTANT**: Focus only on testing and quality assurance. Do not modify:\n\
                     - Production application code\n\
                     - Core business logic\n\
                     - Infrastructure configurations\n\
                     \n",
                    responsibilities.join(", ")
                )
            }
            crate::identity::AgentRole::Master { .. } => {
                "## Agent Specialization: Master Orchestrator\n\
                 \n\
                 **IMPORTANT**: You are the orchestrator. Do not modify code directly.\n\
                 Instead, coordinate between other agents and provide guidance.\n\
                 \n"
                .to_string()
            }
            crate::identity::AgentRole::Search {
                technologies,
                responsibilities,
                ..
            } => {
                format!(
                    "## Agent Specialization: Search\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     \n\
                     **IMPORTANT**: Focus only on information retrieval. Do not modify:\n\
                     - Any source code files\n\
                     - Configuration files\n\
                     - Infrastructure settings\n\
                     \n",
                    technologies.join(", "),
                    responsibilities.join(", ")
                )
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:get_capabilities (lines 570-599) ---[0m
    fn get_capabilities(&self) -> ProviderCapabilities {
        ProviderCapabilities {
            supports_json_output: false, // Aider doesn't have structured JSON output
            supports_streaming: false,
            supports_file_operations: true,
            supports_git_operations: true,
            supports_code_execution: false, // Aider doesn't execute code directly
            max_context_length: Some(128_000), // Depends on the model used
            supported_languages: vec![
                "python".to_string(),
                "javascript".to_string(),
                "typescript".to_string(),
                "java".to_string(),
                "c++".to_string(),
                "c".to_string(),
                "c#".to_string(),
                "go".to_string(),
                "rust".to_string(),
                "php".to_string(),
                "ruby".to_string(),
                "html".to_string(),
                "css".to_string(),
                "sql".to_string(),
                "bash".to_string(),
                "yaml".to_string(),
                "json".to_string(),
                "markdown".to_string(),
            ],
        }
    }

  ./crates/ccswarm/src/providers/aider.rs:163-250 method generate_aider_instructions <-> ./crates/ccswarm/src/providers/aider.rs:358-404 method parse_aider_output
  Similarity: 82.69%
  Classes: AiderExecutor <-> AiderExecutor

[36m--- ./crates/ccswarm/src/providers/aider.rs:generate_aider_instructions (lines 163-250) ---[0m
    fn generate_aider_instructions(&self, task: &Task) -> String {
        let base_instructions = "## Aider Instructions\n\
                                Please analyze the codebase and implement the requested changes.\n";

        let task_specific = match task.task_type {
            TaskType::Development => {
                "- Focus on implementing clean, maintainable code\n\
                 - Follow existing code patterns and conventions\n\
                 - Add appropriate error handling\n\
                 - Include relevant tests if applicable\n"
            }
            TaskType::Testing => {
                "- Write comprehensive tests for the feature/bug\n\
                 - Ensure good test coverage\n\
                 - Follow testing best practices\n\
                 - Use appropriate testing frameworks\n"
            }
            TaskType::Documentation => {
                "- Create clear, comprehensive documentation\n\
                 - Include code examples where appropriate\n\
                 - Follow documentation standards\n\
                 - Update related documentation files\n"
            }
            TaskType::Bugfix => {
                "- Identify and fix the root cause\n\
                 - Add regression tests\n\
                 - Ensure the fix doesn't break existing functionality\n\
                 - Document the fix if necessary\n"
            }
            TaskType::Infrastructure => {
                "- Focus on infrastructure and deployment concerns\n\
                 - Ensure configurations are secure and scalable\n\
                 - Follow infrastructure best practices\n\
                 - Test configuration changes thoroughly\n"
            }
            TaskType::Coordination => {
                "- This is a coordination task\n\
                 - Focus on planning and organizing\n\
                 - Do not implement code directly\n\
                 - Provide clear guidance for other agents\n"
            }
            TaskType::Review => {
                "- Review code and documentation quality\n\
                 - Check for security issues and best practices\n\
                 - Provide constructive feedback\n\
                 - Ensure compliance with coding standards\n"
            }
            TaskType::Feature => {
                "- Implement new feature functionality\n\
                 - Ensure proper integration with existing code\n\
                 - Follow feature specifications carefully\n\
                 - Add appropriate tests and documentation\n"
            }
            TaskType::Remediation => {
                "- Fix the quality issues identified in the review\n\
                 - Follow the specific instructions provided\n\
                 - Ensure all issues are resolved completely\n\
                 - Add tests to prevent regression\n\
                 - Improve code quality as needed\n"
            }
            TaskType::Bug => {
                "- Identify and fix the bug\n\
                 - Add regression tests\n\
                 - Ensure the fix doesn't break existing functionality\n\
                 - Document the fix if necessary\n"
            }
            TaskType::Assistance => {
                "- Provide help and support for the requested task\n\
                 - Collaborate with the requesting agent\n\
                 - Share expertise and knowledge\n\
                 - Help unblock the original task\n"
            }
            TaskType::Research => {
                "- Research and gather information\n\
                 - Analyze search results and findings\n\
                 - Apply insights to improve implementation\n\
                 - Document key findings and recommendations\n"
            }
        };

        let git_instructions = if self.config.auto_commit {
            "\n**Git Behavior**: Auto-commit is enabled. Changes will be committed automatically."
        } else {
            "\n**Git Behavior**: Auto-commit is disabled. Review changes before committing."
        };

        format!("{}{}{}", base_instructions, task_specific, git_instructions)
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:parse_aider_output (lines 358-404) ---[0m
    fn parse_aider_output(
        &self,
        output: String,
        task: &Task,
        duration: std::time::Duration,
    ) -> TaskResult {
        let mut files_changed = Vec::new();
        let mut commits_made = Vec::new();
        let mut errors = Vec::new();

        // Parse output for common Aider patterns
        for line in output.lines() {
            if line.contains("Modified ") || line.contains("Created ") {
                if let Some(file) = self.extract_filename_from_line(line) {
                    files_changed.push(file);
                }
            } else if line.contains("Commit ") && line.contains("hash:") {
                if let Some(commit) = self.extract_commit_from_line(line) {
                    commits_made.push(commit);
                }
            } else if line.contains("Error:") || line.contains("WARNING:") {
                errors.push(line.to_string());
            }
        }

        let success = errors.is_empty() || errors.iter().all(|e| !e.contains("Error:"));

        TaskResult {
            success,
            output: serde_json::json!({
                "aider_output": output,
                "files_changed": files_changed,
                "commits_made": commits_made,
                "warnings": errors.iter().filter(|e| e.contains("WARNING")).collect::<Vec<_>>(),
                "task_id": task.id,
                "provider": "aider",
                "model": self.config.model,
                "auto_commit": self.config.auto_commit,
            }),
            error: if success {
                None
            } else {
                Some(errors.join("; "))
            },
            duration,
        }
    }

  ./crates/ccswarm/src/providers/aider.rs:455-512 method execute_task <-> ./crates/ccswarm/src/providers/aider.rs:514-568 method health_check
  Similarity: 93.46%
  Classes: ProviderExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/aider.rs:execute_task (lines 455-512) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate enhanced prompt with context and boundaries
        let prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with Aider (model: {}) for agent {}",
            task.description,
            self.config.model,
            identity.agent_id
        );

        // Execute the prompt
        match self.execute_prompt(&prompt, identity, working_dir).await {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_aider_output(output, task, duration);

                tracing::info!(
                    "Aider task completed in {:?} for agent {} (files changed: {})",
                    duration,
                    identity.agent_id,
                    result.output["files_changed"]
                        .as_array()
                        .map(|a| a.len())
                        .unwrap_or(0)
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Aider task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "aider",
                        "model": self.config.model,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:health_check (lines 514-568) ---[0m
    async fn health_check(&self, working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        let executable = if let Some(path) = &self.config.executable_path {
            path.to_string_lossy().to_string()
        } else {
            "aider".to_string()
        };

        // Try to execute version check
        let result = Command::new(&executable)
            .arg("--version")
            .current_dir(working_dir)
            .output()
            .await;

        let duration = start.elapsed();
        let response_time_ms = duration.as_millis() as u64;

        match result {
            Ok(output) if output.status.success() => {
                let version_output = String::from_utf8_lossy(&output.stdout);
                let version = version_output
                    .lines()
                    .find(|line| line.contains("aider"))
                    .unwrap_or(version_output.trim())
                    .to_string();

                Ok(ProviderHealthStatus {
                    is_healthy: true,
                    version: Some(version),
                    last_check: chrono::Utc::now(),
                    error_message: None,
                    response_time_ms: Some(response_time_ms),
                })
            }
            Ok(output) => {
                let error = String::from_utf8_lossy(&output.stderr).to_string();
                Ok(ProviderHealthStatus {
                    is_healthy: false,
                    version: None,
                    last_check: chrono::Utc::now(),
                    error_message: Some(format!("Command failed: {}", error)),
                    response_time_ms: Some(response_time_ms),
                })
            }
            Err(e) => Ok(ProviderHealthStatus {
                is_healthy: false,
                version: None,
                last_check: chrono::Utc::now(),
                error_message: Some(format!("Failed to execute Aider: {}", e)),
                response_time_ms: Some(response_time_ms),
            }),
        }
    }

  ./crates/ccswarm/src/providers/aider.rs:25-55 method generate_task_prompt <-> ./crates/ccswarm/src/providers/aider.rs:163-250 method generate_aider_instructions
  Similarity: 85.61%
  Classes: AiderExecutor <-> AiderExecutor

[36m--- ./crates/ccswarm/src/providers/aider.rs:generate_task_prompt (lines 25-55) ---[0m
    fn generate_task_prompt(&self, identity: &AgentIdentity, task: &Task) -> String {
        let context_header = format!(
            "# ccswarm Agent Context\n\
             Agent: {} ({})\n\
             Task: {}\n\
             Priority: {:?}\n\
             Type: {:?}\n\n",
            identity.agent_id,
            identity.specialization.name(),
            task.id,
            task.priority,
            task.task_type
        );

        let boundaries = self.generate_agent_boundaries(&identity.specialization);

        let task_description = format!("## Task Description\n{}\n\n", task.description);

        let task_details = if let Some(details) = &task.details {
            format!("## Additional Details\n{}\n\n", details)
        } else {
            String::new()
        };

        let aider_instructions = self.generate_aider_instructions(task);

        format!(
            "{}{}{}{}\n{}",
            context_header, boundaries, task_description, task_details, aider_instructions
        )
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:generate_aider_instructions (lines 163-250) ---[0m
    fn generate_aider_instructions(&self, task: &Task) -> String {
        let base_instructions = "## Aider Instructions\n\
                                Please analyze the codebase and implement the requested changes.\n";

        let task_specific = match task.task_type {
            TaskType::Development => {
                "- Focus on implementing clean, maintainable code\n\
                 - Follow existing code patterns and conventions\n\
                 - Add appropriate error handling\n\
                 - Include relevant tests if applicable\n"
            }
            TaskType::Testing => {
                "- Write comprehensive tests for the feature/bug\n\
                 - Ensure good test coverage\n\
                 - Follow testing best practices\n\
                 - Use appropriate testing frameworks\n"
            }
            TaskType::Documentation => {
                "- Create clear, comprehensive documentation\n\
                 - Include code examples where appropriate\n\
                 - Follow documentation standards\n\
                 - Update related documentation files\n"
            }
            TaskType::Bugfix => {
                "- Identify and fix the root cause\n\
                 - Add regression tests\n\
                 - Ensure the fix doesn't break existing functionality\n\
                 - Document the fix if necessary\n"
            }
            TaskType::Infrastructure => {
                "- Focus on infrastructure and deployment concerns\n\
                 - Ensure configurations are secure and scalable\n\
                 - Follow infrastructure best practices\n\
                 - Test configuration changes thoroughly\n"
            }
            TaskType::Coordination => {
                "- This is a coordination task\n\
                 - Focus on planning and organizing\n\
                 - Do not implement code directly\n\
                 - Provide clear guidance for other agents\n"
            }
            TaskType::Review => {
                "- Review code and documentation quality\n\
                 - Check for security issues and best practices\n\
                 - Provide constructive feedback\n\
                 - Ensure compliance with coding standards\n"
            }
            TaskType::Feature => {
                "- Implement new feature functionality\n\
                 - Ensure proper integration with existing code\n\
                 - Follow feature specifications carefully\n\
                 - Add appropriate tests and documentation\n"
            }
            TaskType::Remediation => {
                "- Fix the quality issues identified in the review\n\
                 - Follow the specific instructions provided\n\
                 - Ensure all issues are resolved completely\n\
                 - Add tests to prevent regression\n\
                 - Improve code quality as needed\n"
            }
            TaskType::Bug => {
                "- Identify and fix the bug\n\
                 - Add regression tests\n\
                 - Ensure the fix doesn't break existing functionality\n\
                 - Document the fix if necessary\n"
            }
            TaskType::Assistance => {
                "- Provide help and support for the requested task\n\
                 - Collaborate with the requesting agent\n\
                 - Share expertise and knowledge\n\
                 - Help unblock the original task\n"
            }
            TaskType::Research => {
                "- Research and gather information\n\
                 - Analyze search results and findings\n\
                 - Apply insights to improve implementation\n\
                 - Document key findings and recommendations\n"
            }
        };

        let git_instructions = if self.config.auto_commit {
            "\n**Git Behavior**: Auto-commit is enabled. Changes will be committed automatically."
        } else {
            "\n**Git Behavior**: Auto-commit is disabled. Review changes before committing."
        };

        format!("{}{}{}", base_instructions, task_specific, git_instructions)
    }

  ./crates/ccswarm/src/providers/aider.rs:163-250 method generate_aider_instructions <-> ./crates/ccswarm/src/providers/aider.rs:570-599 method get_capabilities
  Similarity: 81.93%
  Classes: AiderExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/aider.rs:generate_aider_instructions (lines 163-250) ---[0m
    fn generate_aider_instructions(&self, task: &Task) -> String {
        let base_instructions = "## Aider Instructions\n\
                                Please analyze the codebase and implement the requested changes.\n";

        let task_specific = match task.task_type {
            TaskType::Development => {
                "- Focus on implementing clean, maintainable code\n\
                 - Follow existing code patterns and conventions\n\
                 - Add appropriate error handling\n\
                 - Include relevant tests if applicable\n"
            }
            TaskType::Testing => {
                "- Write comprehensive tests for the feature/bug\n\
                 - Ensure good test coverage\n\
                 - Follow testing best practices\n\
                 - Use appropriate testing frameworks\n"
            }
            TaskType::Documentation => {
                "- Create clear, comprehensive documentation\n\
                 - Include code examples where appropriate\n\
                 - Follow documentation standards\n\
                 - Update related documentation files\n"
            }
            TaskType::Bugfix => {
                "- Identify and fix the root cause\n\
                 - Add regression tests\n\
                 - Ensure the fix doesn't break existing functionality\n\
                 - Document the fix if necessary\n"
            }
            TaskType::Infrastructure => {
                "- Focus on infrastructure and deployment concerns\n\
                 - Ensure configurations are secure and scalable\n\
                 - Follow infrastructure best practices\n\
                 - Test configuration changes thoroughly\n"
            }
            TaskType::Coordination => {
                "- This is a coordination task\n\
                 - Focus on planning and organizing\n\
                 - Do not implement code directly\n\
                 - Provide clear guidance for other agents\n"
            }
            TaskType::Review => {
                "- Review code and documentation quality\n\
                 - Check for security issues and best practices\n\
                 - Provide constructive feedback\n\
                 - Ensure compliance with coding standards\n"
            }
            TaskType::Feature => {
                "- Implement new feature functionality\n\
                 - Ensure proper integration with existing code\n\
                 - Follow feature specifications carefully\n\
                 - Add appropriate tests and documentation\n"
            }
            TaskType::Remediation => {
                "- Fix the quality issues identified in the review\n\
                 - Follow the specific instructions provided\n\
                 - Ensure all issues are resolved completely\n\
                 - Add tests to prevent regression\n\
                 - Improve code quality as needed\n"
            }
            TaskType::Bug => {
                "- Identify and fix the bug\n\
                 - Add regression tests\n\
                 - Ensure the fix doesn't break existing functionality\n\
                 - Document the fix if necessary\n"
            }
            TaskType::Assistance => {
                "- Provide help and support for the requested task\n\
                 - Collaborate with the requesting agent\n\
                 - Share expertise and knowledge\n\
                 - Help unblock the original task\n"
            }
            TaskType::Research => {
                "- Research and gather information\n\
                 - Analyze search results and findings\n\
                 - Apply insights to improve implementation\n\
                 - Document key findings and recommendations\n"
            }
        };

        let git_instructions = if self.config.auto_commit {
            "\n**Git Behavior**: Auto-commit is enabled. Changes will be committed automatically."
        } else {
            "\n**Git Behavior**: Auto-commit is disabled. Review changes before committing."
        };

        format!("{}{}{}", base_instructions, task_specific, git_instructions)
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:get_capabilities (lines 570-599) ---[0m
    fn get_capabilities(&self) -> ProviderCapabilities {
        ProviderCapabilities {
            supports_json_output: false, // Aider doesn't have structured JSON output
            supports_streaming: false,
            supports_file_operations: true,
            supports_git_operations: true,
            supports_code_execution: false, // Aider doesn't execute code directly
            max_context_length: Some(128_000), // Depends on the model used
            supported_languages: vec![
                "python".to_string(),
                "javascript".to_string(),
                "typescript".to_string(),
                "java".to_string(),
                "c++".to_string(),
                "c".to_string(),
                "c#".to_string(),
                "go".to_string(),
                "rust".to_string(),
                "php".to_string(),
                "ruby".to_string(),
                "html".to_string(),
                "css".to_string(),
                "sql".to_string(),
                "bash".to_string(),
                "yaml".to_string(),
                "json".to_string(),
                "markdown".to_string(),
            ],
        }
    }

  ./crates/ccswarm/src/providers/aider.rs:358-404 method parse_aider_output <-> ./crates/ccswarm/src/providers/aider.rs:514-568 method health_check
  Similarity: 92.30%
  Classes: AiderExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/aider.rs:parse_aider_output (lines 358-404) ---[0m
    fn parse_aider_output(
        &self,
        output: String,
        task: &Task,
        duration: std::time::Duration,
    ) -> TaskResult {
        let mut files_changed = Vec::new();
        let mut commits_made = Vec::new();
        let mut errors = Vec::new();

        // Parse output for common Aider patterns
        for line in output.lines() {
            if line.contains("Modified ") || line.contains("Created ") {
                if let Some(file) = self.extract_filename_from_line(line) {
                    files_changed.push(file);
                }
            } else if line.contains("Commit ") && line.contains("hash:") {
                if let Some(commit) = self.extract_commit_from_line(line) {
                    commits_made.push(commit);
                }
            } else if line.contains("Error:") || line.contains("WARNING:") {
                errors.push(line.to_string());
            }
        }

        let success = errors.is_empty() || errors.iter().all(|e| !e.contains("Error:"));

        TaskResult {
            success,
            output: serde_json::json!({
                "aider_output": output,
                "files_changed": files_changed,
                "commits_made": commits_made,
                "warnings": errors.iter().filter(|e| e.contains("WARNING")).collect::<Vec<_>>(),
                "task_id": task.id,
                "provider": "aider",
                "model": self.config.model,
                "auto_commit": self.config.auto_commit,
            }),
            error: if success {
                None
            } else {
                Some(errors.join("; "))
            },
            duration,
        }
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:health_check (lines 514-568) ---[0m
    async fn health_check(&self, working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        let executable = if let Some(path) = &self.config.executable_path {
            path.to_string_lossy().to_string()
        } else {
            "aider".to_string()
        };

        // Try to execute version check
        let result = Command::new(&executable)
            .arg("--version")
            .current_dir(working_dir)
            .output()
            .await;

        let duration = start.elapsed();
        let response_time_ms = duration.as_millis() as u64;

        match result {
            Ok(output) if output.status.success() => {
                let version_output = String::from_utf8_lossy(&output.stdout);
                let version = version_output
                    .lines()
                    .find(|line| line.contains("aider"))
                    .unwrap_or(version_output.trim())
                    .to_string();

                Ok(ProviderHealthStatus {
                    is_healthy: true,
                    version: Some(version),
                    last_check: chrono::Utc::now(),
                    error_message: None,
                    response_time_ms: Some(response_time_ms),
                })
            }
            Ok(output) => {
                let error = String::from_utf8_lossy(&output.stderr).to_string();
                Ok(ProviderHealthStatus {
                    is_healthy: false,
                    version: None,
                    last_check: chrono::Utc::now(),
                    error_message: Some(format!("Command failed: {}", error)),
                    response_time_ms: Some(response_time_ms),
                })
            }
            Err(e) => Ok(ProviderHealthStatus {
                is_healthy: false,
                version: None,
                last_check: chrono::Utc::now(),
                error_message: Some(format!("Failed to execute Aider: {}", e)),
                response_time_ms: Some(response_time_ms),
            }),
        }
    }

  ./crates/ccswarm/src/providers/aider.rs:358-404 method parse_aider_output <-> ./crates/ccswarm/src/providers/aider.rs:455-512 method execute_task
  Similarity: 89.37%
  Classes: AiderExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/aider.rs:parse_aider_output (lines 358-404) ---[0m
    fn parse_aider_output(
        &self,
        output: String,
        task: &Task,
        duration: std::time::Duration,
    ) -> TaskResult {
        let mut files_changed = Vec::new();
        let mut commits_made = Vec::new();
        let mut errors = Vec::new();

        // Parse output for common Aider patterns
        for line in output.lines() {
            if line.contains("Modified ") || line.contains("Created ") {
                if let Some(file) = self.extract_filename_from_line(line) {
                    files_changed.push(file);
                }
            } else if line.contains("Commit ") && line.contains("hash:") {
                if let Some(commit) = self.extract_commit_from_line(line) {
                    commits_made.push(commit);
                }
            } else if line.contains("Error:") || line.contains("WARNING:") {
                errors.push(line.to_string());
            }
        }

        let success = errors.is_empty() || errors.iter().all(|e| !e.contains("Error:"));

        TaskResult {
            success,
            output: serde_json::json!({
                "aider_output": output,
                "files_changed": files_changed,
                "commits_made": commits_made,
                "warnings": errors.iter().filter(|e| e.contains("WARNING")).collect::<Vec<_>>(),
                "task_id": task.id,
                "provider": "aider",
                "model": self.config.model,
                "auto_commit": self.config.auto_commit,
            }),
            error: if success {
                None
            } else {
                Some(errors.join("; "))
            },
            duration,
        }
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:execute_task (lines 455-512) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate enhanced prompt with context and boundaries
        let prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with Aider (model: {}) for agent {}",
            task.description,
            self.config.model,
            identity.agent_id
        );

        // Execute the prompt
        match self.execute_prompt(&prompt, identity, working_dir).await {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_aider_output(output, task, duration);

                tracing::info!(
                    "Aider task completed in {:?} for agent {} (files changed: {})",
                    duration,
                    identity.agent_id,
                    result.output["files_changed"]
                        .as_array()
                        .map(|a| a.len())
                        .unwrap_or(0)
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Aider task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "aider",
                        "model": self.config.model,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/aider.rs:163-250 method generate_aider_instructions <-> ./crates/ccswarm/src/providers/aider.rs:611-626 function create_test_identity
  Similarity: 89.48%

[36m--- ./crates/ccswarm/src/providers/aider.rs:generate_aider_instructions (lines 163-250) ---[0m
    fn generate_aider_instructions(&self, task: &Task) -> String {
        let base_instructions = "## Aider Instructions\n\
                                Please analyze the codebase and implement the requested changes.\n";

        let task_specific = match task.task_type {
            TaskType::Development => {
                "- Focus on implementing clean, maintainable code\n\
                 - Follow existing code patterns and conventions\n\
                 - Add appropriate error handling\n\
                 - Include relevant tests if applicable\n"
            }
            TaskType::Testing => {
                "- Write comprehensive tests for the feature/bug\n\
                 - Ensure good test coverage\n\
                 - Follow testing best practices\n\
                 - Use appropriate testing frameworks\n"
            }
            TaskType::Documentation => {
                "- Create clear, comprehensive documentation\n\
                 - Include code examples where appropriate\n\
                 - Follow documentation standards\n\
                 - Update related documentation files\n"
            }
            TaskType::Bugfix => {
                "- Identify and fix the root cause\n\
                 - Add regression tests\n\
                 - Ensure the fix doesn't break existing functionality\n\
                 - Document the fix if necessary\n"
            }
            TaskType::Infrastructure => {
                "- Focus on infrastructure and deployment concerns\n\
                 - Ensure configurations are secure and scalable\n\
                 - Follow infrastructure best practices\n\
                 - Test configuration changes thoroughly\n"
            }
            TaskType::Coordination => {
                "- This is a coordination task\n\
                 - Focus on planning and organizing\n\
                 - Do not implement code directly\n\
                 - Provide clear guidance for other agents\n"
            }
            TaskType::Review => {
                "- Review code and documentation quality\n\
                 - Check for security issues and best practices\n\
                 - Provide constructive feedback\n\
                 - Ensure compliance with coding standards\n"
            }
            TaskType::Feature => {
                "- Implement new feature functionality\n\
                 - Ensure proper integration with existing code\n\
                 - Follow feature specifications carefully\n\
                 - Add appropriate tests and documentation\n"
            }
            TaskType::Remediation => {
                "- Fix the quality issues identified in the review\n\
                 - Follow the specific instructions provided\n\
                 - Ensure all issues are resolved completely\n\
                 - Add tests to prevent regression\n\
                 - Improve code quality as needed\n"
            }
            TaskType::Bug => {
                "- Identify and fix the bug\n\
                 - Add regression tests\n\
                 - Ensure the fix doesn't break existing functionality\n\
                 - Document the fix if necessary\n"
            }
            TaskType::Assistance => {
                "- Provide help and support for the requested task\n\
                 - Collaborate with the requesting agent\n\
                 - Share expertise and knowledge\n\
                 - Help unblock the original task\n"
            }
            TaskType::Research => {
                "- Research and gather information\n\
                 - Analyze search results and findings\n\
                 - Apply insights to improve implementation\n\
                 - Document key findings and recommendations\n"
            }
        };

        let git_instructions = if self.config.auto_commit {
            "\n**Git Behavior**: Auto-commit is enabled. Changes will be committed automatically."
        } else {
            "\n**Git Behavior**: Auto-commit is disabled. Review changes before committing."
        };

        format!("{}{}{}", base_instructions, task_specific, git_instructions)
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:create_test_identity (lines 611-626) ---[0m
    fn create_test_identity() -> AgentIdentity {
        let temp_dir = TempDir::new().unwrap();
        AgentIdentity {
            agent_id: "test-agent".to_string(),
            specialization: AgentRole::Backend {
                technologies: vec!["Node.js".to_string(), "Express".to_string()],
                responsibilities: vec!["API development".to_string()],
                boundaries: vec!["No frontend work".to_string()],
            },
            workspace_path: temp_dir.path().to_path_buf(),
            env_vars: HashMap::new(),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: "12345".to_string(),
            initialized_at: chrono::Utc::now(),
        }
    }

  ./crates/ccswarm/src/providers/aider.rs:253-283 method build_command_args <-> ./crates/ccswarm/src/providers/aider.rs:286-355 method execute_aider_command
  Similarity: 91.14%
  Classes: AiderExecutor <-> AiderExecutor

[36m--- ./crates/ccswarm/src/providers/aider.rs:build_command_args (lines 253-283) ---[0m
    fn build_command_args(&self, prompt: &str, _working_dir: &Path) -> Vec<String> {
        let mut args = Vec::new();

        // Add model specification
        args.push("--model".to_string());
        args.push(self.config.model.clone());

        // Add auto-commit behavior
        if self.config.auto_commit {
            args.push("--auto-commits".to_string());
        } else {
            args.push("--no-auto-commits".to_string());
        }

        // Add git behavior
        if !self.config.git {
            args.push("--no-git".to_string());
        }

        // Add message with prompt
        args.push("--message".to_string());
        args.push(prompt.to_string());

        // Add additional custom arguments
        args.extend(self.config.additional_args.clone());

        // Add yes flag for non-interactive execution
        args.push("--yes".to_string());

        args
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:execute_aider_command (lines 286-355) ---[0m
    async fn execute_aider_command(
        &self,
        args: Vec<String>,
        working_dir: &Path,
        identity: &AgentIdentity,
    ) -> Result<String> {
        let executable = if let Some(path) = &self.config.executable_path {
            path.to_string_lossy().to_string()
        } else {
            "aider".to_string()
        };

        let mut cmd = Command::new(&executable);

        // Set working directory
        cmd.current_dir(working_dir);

        // Add identity environment variables
        for (key, value) in &identity.env_vars {
            cmd.env(key, value);
        }

        // Add provider environment variables
        for (key, value) in self.config.get_env_vars() {
            cmd.env(key, value);
        }

        // Add command arguments
        cmd.args(&args);

        // Execute command with timeout
        let start = Instant::now();
        let output = tokio::time::timeout(
            std::time::Duration::from_secs(600), // 10 minute timeout for Aider
            cmd.output(),
        )
        .await
        .context("Aider command timed out")?
        .context("Failed to execute Aider")?;

        let duration = start.elapsed();

        tracing::debug!(
            "Aider execution completed in {:?} for agent {}",
            duration,
            identity.agent_id
        );

        if output.status.success() {
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let stderr = String::from_utf8_lossy(&output.stderr).to_string();

            // Aider often outputs useful information to stderr even on success
            if !stderr.trim().is_empty() {
                Ok(format!("{}\n\nAider Output:\n{}", stdout, stderr))
            } else {
                Ok(stdout)
            }
        } else {
            let stderr = String::from_utf8_lossy(&output.stderr);
            let stdout = String::from_utf8_lossy(&output.stdout);

            Err(anyhow::anyhow!(
                "Aider execution failed (exit code: {:?})\nStderr: {}\nStdout: {}",
                output.status.code(),
                stderr,
                stdout
            ))
        }
    }

  ./crates/ccswarm/src/providers/aider.rs:286-355 method execute_aider_command <-> ./crates/ccswarm/src/providers/aider.rs:358-404 method parse_aider_output
  Similarity: 77.58%
  Classes: AiderExecutor <-> AiderExecutor

[36m--- ./crates/ccswarm/src/providers/aider.rs:execute_aider_command (lines 286-355) ---[0m
    async fn execute_aider_command(
        &self,
        args: Vec<String>,
        working_dir: &Path,
        identity: &AgentIdentity,
    ) -> Result<String> {
        let executable = if let Some(path) = &self.config.executable_path {
            path.to_string_lossy().to_string()
        } else {
            "aider".to_string()
        };

        let mut cmd = Command::new(&executable);

        // Set working directory
        cmd.current_dir(working_dir);

        // Add identity environment variables
        for (key, value) in &identity.env_vars {
            cmd.env(key, value);
        }

        // Add provider environment variables
        for (key, value) in self.config.get_env_vars() {
            cmd.env(key, value);
        }

        // Add command arguments
        cmd.args(&args);

        // Execute command with timeout
        let start = Instant::now();
        let output = tokio::time::timeout(
            std::time::Duration::from_secs(600), // 10 minute timeout for Aider
            cmd.output(),
        )
        .await
        .context("Aider command timed out")?
        .context("Failed to execute Aider")?;

        let duration = start.elapsed();

        tracing::debug!(
            "Aider execution completed in {:?} for agent {}",
            duration,
            identity.agent_id
        );

        if output.status.success() {
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let stderr = String::from_utf8_lossy(&output.stderr).to_string();

            // Aider often outputs useful information to stderr even on success
            if !stderr.trim().is_empty() {
                Ok(format!("{}\n\nAider Output:\n{}", stdout, stderr))
            } else {
                Ok(stdout)
            }
        } else {
            let stderr = String::from_utf8_lossy(&output.stderr);
            let stdout = String::from_utf8_lossy(&output.stdout);

            Err(anyhow::anyhow!(
                "Aider execution failed (exit code: {:?})\nStderr: {}\nStdout: {}",
                output.status.code(),
                stderr,
                stdout
            ))
        }
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:parse_aider_output (lines 358-404) ---[0m
    fn parse_aider_output(
        &self,
        output: String,
        task: &Task,
        duration: std::time::Duration,
    ) -> TaskResult {
        let mut files_changed = Vec::new();
        let mut commits_made = Vec::new();
        let mut errors = Vec::new();

        // Parse output for common Aider patterns
        for line in output.lines() {
            if line.contains("Modified ") || line.contains("Created ") {
                if let Some(file) = self.extract_filename_from_line(line) {
                    files_changed.push(file);
                }
            } else if line.contains("Commit ") && line.contains("hash:") {
                if let Some(commit) = self.extract_commit_from_line(line) {
                    commits_made.push(commit);
                }
            } else if line.contains("Error:") || line.contains("WARNING:") {
                errors.push(line.to_string());
            }
        }

        let success = errors.is_empty() || errors.iter().all(|e| !e.contains("Error:"));

        TaskResult {
            success,
            output: serde_json::json!({
                "aider_output": output,
                "files_changed": files_changed,
                "commits_made": commits_made,
                "warnings": errors.iter().filter(|e| e.contains("WARNING")).collect::<Vec<_>>(),
                "task_id": task.id,
                "provider": "aider",
                "model": self.config.model,
                "auto_commit": self.config.auto_commit,
            }),
            error: if success {
                None
            } else {
                Some(errors.join("; "))
            },
            duration,
        }
    }

  ./crates/ccswarm/src/providers/aider.rs:25-55 method generate_task_prompt <-> ./crates/ccswarm/src/providers/aider.rs:455-512 method execute_task
  Similarity: 89.26%
  Classes: AiderExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/aider.rs:generate_task_prompt (lines 25-55) ---[0m
    fn generate_task_prompt(&self, identity: &AgentIdentity, task: &Task) -> String {
        let context_header = format!(
            "# ccswarm Agent Context\n\
             Agent: {} ({})\n\
             Task: {}\n\
             Priority: {:?}\n\
             Type: {:?}\n\n",
            identity.agent_id,
            identity.specialization.name(),
            task.id,
            task.priority,
            task.task_type
        );

        let boundaries = self.generate_agent_boundaries(&identity.specialization);

        let task_description = format!("## Task Description\n{}\n\n", task.description);

        let task_details = if let Some(details) = &task.details {
            format!("## Additional Details\n{}\n\n", details)
        } else {
            String::new()
        };

        let aider_instructions = self.generate_aider_instructions(task);

        format!(
            "{}{}{}{}\n{}",
            context_header, boundaries, task_description, task_details, aider_instructions
        )
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:execute_task (lines 455-512) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate enhanced prompt with context and boundaries
        let prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with Aider (model: {}) for agent {}",
            task.description,
            self.config.model,
            identity.agent_id
        );

        // Execute the prompt
        match self.execute_prompt(&prompt, identity, working_dir).await {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_aider_output(output, task, duration);

                tracing::info!(
                    "Aider task completed in {:?} for agent {} (files changed: {})",
                    duration,
                    identity.agent_id,
                    result.output["files_changed"]
                        .as_array()
                        .map(|a| a.len())
                        .unwrap_or(0)
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Aider task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "aider",
                        "model": self.config.model,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/aider.rs:286-355 method execute_aider_command <-> ./crates/ccswarm/src/providers/aider.rs:718-743 function test_parse_aider_output
  Similarity: 82.53%

[36m--- ./crates/ccswarm/src/providers/aider.rs:execute_aider_command (lines 286-355) ---[0m
    async fn execute_aider_command(
        &self,
        args: Vec<String>,
        working_dir: &Path,
        identity: &AgentIdentity,
    ) -> Result<String> {
        let executable = if let Some(path) = &self.config.executable_path {
            path.to_string_lossy().to_string()
        } else {
            "aider".to_string()
        };

        let mut cmd = Command::new(&executable);

        // Set working directory
        cmd.current_dir(working_dir);

        // Add identity environment variables
        for (key, value) in &identity.env_vars {
            cmd.env(key, value);
        }

        // Add provider environment variables
        for (key, value) in self.config.get_env_vars() {
            cmd.env(key, value);
        }

        // Add command arguments
        cmd.args(&args);

        // Execute command with timeout
        let start = Instant::now();
        let output = tokio::time::timeout(
            std::time::Duration::from_secs(600), // 10 minute timeout for Aider
            cmd.output(),
        )
        .await
        .context("Aider command timed out")?
        .context("Failed to execute Aider")?;

        let duration = start.elapsed();

        tracing::debug!(
            "Aider execution completed in {:?} for agent {}",
            duration,
            identity.agent_id
        );

        if output.status.success() {
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let stderr = String::from_utf8_lossy(&output.stderr).to_string();

            // Aider often outputs useful information to stderr even on success
            if !stderr.trim().is_empty() {
                Ok(format!("{}\n\nAider Output:\n{}", stdout, stderr))
            } else {
                Ok(stdout)
            }
        } else {
            let stderr = String::from_utf8_lossy(&output.stderr);
            let stdout = String::from_utf8_lossy(&output.stdout);

            Err(anyhow::anyhow!(
                "Aider execution failed (exit code: {:?})\nStderr: {}\nStdout: {}",
                output.status.code(),
                stderr,
                stdout
            ))
        }
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_parse_aider_output (lines 718-743) ---[0m
    fn test_parse_aider_output() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_secs(10);

        let output = "Modified src/auth.js with authentication\n\
                     Created test/auth.test.js for testing\n\
                     Commit abc123 hash: def456789 completed\n\
                     All changes committed successfully";

        let result = executor.parse_aider_output(output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        let output_obj = result.output.as_object().unwrap();
        let files_changed = output_obj["files_changed"].as_array().unwrap();
        assert_eq!(files_changed.len(), 2);
        assert!(files_changed.contains(&serde_json::Value::String("src/auth.js".to_string())));
        assert!(files_changed.contains(&serde_json::Value::String("test/auth.test.js".to_string())));

        let commits_made = output_obj["commits_made"].as_array().unwrap();
        assert_eq!(commits_made.len(), 1);
        assert!(commits_made.contains(&serde_json::Value::String("def456789".to_string())));
    }

  ./crates/ccswarm/src/providers/aider.rs:163-250 method generate_aider_instructions <-> ./crates/ccswarm/src/providers/aider.rs:766-782 function test_generate_agent_boundaries
  Similarity: 73.51%

[36m--- ./crates/ccswarm/src/providers/aider.rs:generate_aider_instructions (lines 163-250) ---[0m
    fn generate_aider_instructions(&self, task: &Task) -> String {
        let base_instructions = "## Aider Instructions\n\
                                Please analyze the codebase and implement the requested changes.\n";

        let task_specific = match task.task_type {
            TaskType::Development => {
                "- Focus on implementing clean, maintainable code\n\
                 - Follow existing code patterns and conventions\n\
                 - Add appropriate error handling\n\
                 - Include relevant tests if applicable\n"
            }
            TaskType::Testing => {
                "- Write comprehensive tests for the feature/bug\n\
                 - Ensure good test coverage\n\
                 - Follow testing best practices\n\
                 - Use appropriate testing frameworks\n"
            }
            TaskType::Documentation => {
                "- Create clear, comprehensive documentation\n\
                 - Include code examples where appropriate\n\
                 - Follow documentation standards\n\
                 - Update related documentation files\n"
            }
            TaskType::Bugfix => {
                "- Identify and fix the root cause\n\
                 - Add regression tests\n\
                 - Ensure the fix doesn't break existing functionality\n\
                 - Document the fix if necessary\n"
            }
            TaskType::Infrastructure => {
                "- Focus on infrastructure and deployment concerns\n\
                 - Ensure configurations are secure and scalable\n\
                 - Follow infrastructure best practices\n\
                 - Test configuration changes thoroughly\n"
            }
            TaskType::Coordination => {
                "- This is a coordination task\n\
                 - Focus on planning and organizing\n\
                 - Do not implement code directly\n\
                 - Provide clear guidance for other agents\n"
            }
            TaskType::Review => {
                "- Review code and documentation quality\n\
                 - Check for security issues and best practices\n\
                 - Provide constructive feedback\n\
                 - Ensure compliance with coding standards\n"
            }
            TaskType::Feature => {
                "- Implement new feature functionality\n\
                 - Ensure proper integration with existing code\n\
                 - Follow feature specifications carefully\n\
                 - Add appropriate tests and documentation\n"
            }
            TaskType::Remediation => {
                "- Fix the quality issues identified in the review\n\
                 - Follow the specific instructions provided\n\
                 - Ensure all issues are resolved completely\n\
                 - Add tests to prevent regression\n\
                 - Improve code quality as needed\n"
            }
            TaskType::Bug => {
                "- Identify and fix the bug\n\
                 - Add regression tests\n\
                 - Ensure the fix doesn't break existing functionality\n\
                 - Document the fix if necessary\n"
            }
            TaskType::Assistance => {
                "- Provide help and support for the requested task\n\
                 - Collaborate with the requesting agent\n\
                 - Share expertise and knowledge\n\
                 - Help unblock the original task\n"
            }
            TaskType::Research => {
                "- Research and gather information\n\
                 - Analyze search results and findings\n\
                 - Apply insights to improve implementation\n\
                 - Document key findings and recommendations\n"
            }
        };

        let git_instructions = if self.config.auto_commit {
            "\n**Git Behavior**: Auto-commit is enabled. Changes will be committed automatically."
        } else {
            "\n**Git Behavior**: Auto-commit is disabled. Review changes before committing."
        };

        format!("{}{}{}", base_instructions, task_specific, git_instructions)
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_generate_agent_boundaries (lines 766-782) ---[0m
    fn test_generate_agent_boundaries() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);

        let backend_role = AgentRole::Backend {
            technologies: vec!["Node.js".to_string(), "Express".to_string()],
            responsibilities: vec!["API development".to_string()],
            boundaries: vec!["No frontend".to_string()],
        };

        let boundaries = executor.generate_agent_boundaries(&backend_role);
        assert!(boundaries.contains("Backend"));
        assert!(boundaries.contains("Node.js, Express"));
        assert!(boundaries.contains("API development"));
        assert!(boundaries.contains("Do not modify"));
        assert!(boundaries.contains("Frontend components"));
    }

  ./crates/ccswarm/src/providers/aider.rs:286-355 method execute_aider_command <-> ./crates/ccswarm/src/providers/aider.rs:663-683 function test_build_command_args
  Similarity: 82.02%

[36m--- ./crates/ccswarm/src/providers/aider.rs:execute_aider_command (lines 286-355) ---[0m
    async fn execute_aider_command(
        &self,
        args: Vec<String>,
        working_dir: &Path,
        identity: &AgentIdentity,
    ) -> Result<String> {
        let executable = if let Some(path) = &self.config.executable_path {
            path.to_string_lossy().to_string()
        } else {
            "aider".to_string()
        };

        let mut cmd = Command::new(&executable);

        // Set working directory
        cmd.current_dir(working_dir);

        // Add identity environment variables
        for (key, value) in &identity.env_vars {
            cmd.env(key, value);
        }

        // Add provider environment variables
        for (key, value) in self.config.get_env_vars() {
            cmd.env(key, value);
        }

        // Add command arguments
        cmd.args(&args);

        // Execute command with timeout
        let start = Instant::now();
        let output = tokio::time::timeout(
            std::time::Duration::from_secs(600), // 10 minute timeout for Aider
            cmd.output(),
        )
        .await
        .context("Aider command timed out")?
        .context("Failed to execute Aider")?;

        let duration = start.elapsed();

        tracing::debug!(
            "Aider execution completed in {:?} for agent {}",
            duration,
            identity.agent_id
        );

        if output.status.success() {
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let stderr = String::from_utf8_lossy(&output.stderr).to_string();

            // Aider often outputs useful information to stderr even on success
            if !stderr.trim().is_empty() {
                Ok(format!("{}\n\nAider Output:\n{}", stdout, stderr))
            } else {
                Ok(stdout)
            }
        } else {
            let stderr = String::from_utf8_lossy(&output.stderr);
            let stdout = String::from_utf8_lossy(&output.stdout);

            Err(anyhow::anyhow!(
                "Aider execution failed (exit code: {:?})\nStderr: {}\nStdout: {}",
                output.status.code(),
                stderr,
                stdout
            ))
        }
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_build_command_args (lines 663-683) ---[0m
    fn test_build_command_args() {
        let config = AiderConfig {
            model: "claude-3.5-sonnet".to_string(),
            auto_commit: false,
            git: true,
            additional_args: vec!["--no-stream".to_string()],
            ..Default::default()
        };

        let executor = AiderExecutor::new(config);
        let working_dir = PathBuf::from("/tmp");
        let args = executor.build_command_args("test prompt", &working_dir);

        assert!(args.contains(&"--model".to_string()));
        assert!(args.contains(&"claude-3.5-sonnet".to_string()));
        assert!(args.contains(&"--no-auto-commits".to_string()));
        assert!(args.contains(&"--message".to_string()));
        assert!(args.contains(&"test prompt".to_string()));
        assert!(args.contains(&"--no-stream".to_string()));
        assert!(args.contains(&"--yes".to_string()));
    }

  ./crates/ccswarm/src/providers/aider.rs:253-283 method build_command_args <-> ./crates/ccswarm/src/providers/aider.rs:514-568 method health_check
  Similarity: 81.40%
  Classes: AiderExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/aider.rs:build_command_args (lines 253-283) ---[0m
    fn build_command_args(&self, prompt: &str, _working_dir: &Path) -> Vec<String> {
        let mut args = Vec::new();

        // Add model specification
        args.push("--model".to_string());
        args.push(self.config.model.clone());

        // Add auto-commit behavior
        if self.config.auto_commit {
            args.push("--auto-commits".to_string());
        } else {
            args.push("--no-auto-commits".to_string());
        }

        // Add git behavior
        if !self.config.git {
            args.push("--no-git".to_string());
        }

        // Add message with prompt
        args.push("--message".to_string());
        args.push(prompt.to_string());

        // Add additional custom arguments
        args.extend(self.config.additional_args.clone());

        // Add yes flag for non-interactive execution
        args.push("--yes".to_string());

        args
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:health_check (lines 514-568) ---[0m
    async fn health_check(&self, working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        let executable = if let Some(path) = &self.config.executable_path {
            path.to_string_lossy().to_string()
        } else {
            "aider".to_string()
        };

        // Try to execute version check
        let result = Command::new(&executable)
            .arg("--version")
            .current_dir(working_dir)
            .output()
            .await;

        let duration = start.elapsed();
        let response_time_ms = duration.as_millis() as u64;

        match result {
            Ok(output) if output.status.success() => {
                let version_output = String::from_utf8_lossy(&output.stdout);
                let version = version_output
                    .lines()
                    .find(|line| line.contains("aider"))
                    .unwrap_or(version_output.trim())
                    .to_string();

                Ok(ProviderHealthStatus {
                    is_healthy: true,
                    version: Some(version),
                    last_check: chrono::Utc::now(),
                    error_message: None,
                    response_time_ms: Some(response_time_ms),
                })
            }
            Ok(output) => {
                let error = String::from_utf8_lossy(&output.stderr).to_string();
                Ok(ProviderHealthStatus {
                    is_healthy: false,
                    version: None,
                    last_check: chrono::Utc::now(),
                    error_message: Some(format!("Command failed: {}", error)),
                    response_time_ms: Some(response_time_ms),
                })
            }
            Err(e) => Ok(ProviderHealthStatus {
                is_healthy: false,
                version: None,
                last_check: chrono::Utc::now(),
                error_message: Some(format!("Failed to execute Aider: {}", e)),
                response_time_ms: Some(response_time_ms),
            }),
        }
    }

  ./crates/ccswarm/src/providers/aider.rs:407-424 method extract_filename_from_line <-> ./crates/ccswarm/src/providers/aider.rs:455-512 method execute_task
  Similarity: 84.78%
  Classes: AiderExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/aider.rs:extract_filename_from_line (lines 407-424) ---[0m
    fn extract_filename_from_line(&self, line: &str) -> Option<String> {
        // Look for patterns like "Modified src/main.rs" or "Created test.py"
        if let Some(start) = line.find("Modified ").or_else(|| line.find("Created ")) {
            let start_pos = if line[start..].starts_with("Modified ") {
                start + 9
            } else {
                start + 8
            };

            if let Some(end) = line[start_pos..].find(' ') {
                Some(line[start_pos..start_pos + end].to_string())
            } else {
                Some(line[start_pos..].to_string())
            }
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:execute_task (lines 455-512) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate enhanced prompt with context and boundaries
        let prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with Aider (model: {}) for agent {}",
            task.description,
            self.config.model,
            identity.agent_id
        );

        // Execute the prompt
        match self.execute_prompt(&prompt, identity, working_dir).await {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_aider_output(output, task, duration);

                tracing::info!(
                    "Aider task completed in {:?} for agent {} (files changed: {})",
                    duration,
                    identity.agent_id,
                    result.output["files_changed"]
                        .as_array()
                        .map(|a| a.len())
                        .unwrap_or(0)
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Aider task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "aider",
                        "model": self.config.model,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/aider.rs:253-283 method build_command_args <-> ./crates/ccswarm/src/providers/aider.rs:358-404 method parse_aider_output
  Similarity: 81.55%
  Classes: AiderExecutor <-> AiderExecutor

[36m--- ./crates/ccswarm/src/providers/aider.rs:build_command_args (lines 253-283) ---[0m
    fn build_command_args(&self, prompt: &str, _working_dir: &Path) -> Vec<String> {
        let mut args = Vec::new();

        // Add model specification
        args.push("--model".to_string());
        args.push(self.config.model.clone());

        // Add auto-commit behavior
        if self.config.auto_commit {
            args.push("--auto-commits".to_string());
        } else {
            args.push("--no-auto-commits".to_string());
        }

        // Add git behavior
        if !self.config.git {
            args.push("--no-git".to_string());
        }

        // Add message with prompt
        args.push("--message".to_string());
        args.push(prompt.to_string());

        // Add additional custom arguments
        args.extend(self.config.additional_args.clone());

        // Add yes flag for non-interactive execution
        args.push("--yes".to_string());

        args
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:parse_aider_output (lines 358-404) ---[0m
    fn parse_aider_output(
        &self,
        output: String,
        task: &Task,
        duration: std::time::Duration,
    ) -> TaskResult {
        let mut files_changed = Vec::new();
        let mut commits_made = Vec::new();
        let mut errors = Vec::new();

        // Parse output for common Aider patterns
        for line in output.lines() {
            if line.contains("Modified ") || line.contains("Created ") {
                if let Some(file) = self.extract_filename_from_line(line) {
                    files_changed.push(file);
                }
            } else if line.contains("Commit ") && line.contains("hash:") {
                if let Some(commit) = self.extract_commit_from_line(line) {
                    commits_made.push(commit);
                }
            } else if line.contains("Error:") || line.contains("WARNING:") {
                errors.push(line.to_string());
            }
        }

        let success = errors.is_empty() || errors.iter().all(|e| !e.contains("Error:"));

        TaskResult {
            success,
            output: serde_json::json!({
                "aider_output": output,
                "files_changed": files_changed,
                "commits_made": commits_made,
                "warnings": errors.iter().filter(|e| e.contains("WARNING")).collect::<Vec<_>>(),
                "task_id": task.id,
                "provider": "aider",
                "model": self.config.model,
                "auto_commit": self.config.auto_commit,
            }),
            error: if success {
                None
            } else {
                Some(errors.join("; "))
            },
            duration,
        }
    }

  ./crates/ccswarm/src/providers/aider.rs:514-568 method health_check <-> ./crates/ccswarm/src/providers/aider.rs:663-683 function test_build_command_args
  Similarity: 77.44%

[36m--- ./crates/ccswarm/src/providers/aider.rs:health_check (lines 514-568) ---[0m
    async fn health_check(&self, working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        let executable = if let Some(path) = &self.config.executable_path {
            path.to_string_lossy().to_string()
        } else {
            "aider".to_string()
        };

        // Try to execute version check
        let result = Command::new(&executable)
            .arg("--version")
            .current_dir(working_dir)
            .output()
            .await;

        let duration = start.elapsed();
        let response_time_ms = duration.as_millis() as u64;

        match result {
            Ok(output) if output.status.success() => {
                let version_output = String::from_utf8_lossy(&output.stdout);
                let version = version_output
                    .lines()
                    .find(|line| line.contains("aider"))
                    .unwrap_or(version_output.trim())
                    .to_string();

                Ok(ProviderHealthStatus {
                    is_healthy: true,
                    version: Some(version),
                    last_check: chrono::Utc::now(),
                    error_message: None,
                    response_time_ms: Some(response_time_ms),
                })
            }
            Ok(output) => {
                let error = String::from_utf8_lossy(&output.stderr).to_string();
                Ok(ProviderHealthStatus {
                    is_healthy: false,
                    version: None,
                    last_check: chrono::Utc::now(),
                    error_message: Some(format!("Command failed: {}", error)),
                    response_time_ms: Some(response_time_ms),
                })
            }
            Err(e) => Ok(ProviderHealthStatus {
                is_healthy: false,
                version: None,
                last_check: chrono::Utc::now(),
                error_message: Some(format!("Failed to execute Aider: {}", e)),
                response_time_ms: Some(response_time_ms),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_build_command_args (lines 663-683) ---[0m
    fn test_build_command_args() {
        let config = AiderConfig {
            model: "claude-3.5-sonnet".to_string(),
            auto_commit: false,
            git: true,
            additional_args: vec!["--no-stream".to_string()],
            ..Default::default()
        };

        let executor = AiderExecutor::new(config);
        let working_dir = PathBuf::from("/tmp");
        let args = executor.build_command_args("test prompt", &working_dir);

        assert!(args.contains(&"--model".to_string()));
        assert!(args.contains(&"claude-3.5-sonnet".to_string()));
        assert!(args.contains(&"--no-auto-commits".to_string()));
        assert!(args.contains(&"--message".to_string()));
        assert!(args.contains(&"test prompt".to_string()));
        assert!(args.contains(&"--no-stream".to_string()));
        assert!(args.contains(&"--yes".to_string()));
    }

  ./crates/ccswarm/src/providers/aider.rs:455-512 method execute_task <-> ./crates/ccswarm/src/providers/aider.rs:766-782 function test_generate_agent_boundaries
  Similarity: 75.42%

[36m--- ./crates/ccswarm/src/providers/aider.rs:execute_task (lines 455-512) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate enhanced prompt with context and boundaries
        let prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with Aider (model: {}) for agent {}",
            task.description,
            self.config.model,
            identity.agent_id
        );

        // Execute the prompt
        match self.execute_prompt(&prompt, identity, working_dir).await {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_aider_output(output, task, duration);

                tracing::info!(
                    "Aider task completed in {:?} for agent {} (files changed: {})",
                    duration,
                    identity.agent_id,
                    result.output["files_changed"]
                        .as_array()
                        .map(|a| a.len())
                        .unwrap_or(0)
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Aider task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "aider",
                        "model": self.config.model,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_generate_agent_boundaries (lines 766-782) ---[0m
    fn test_generate_agent_boundaries() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);

        let backend_role = AgentRole::Backend {
            technologies: vec!["Node.js".to_string(), "Express".to_string()],
            responsibilities: vec!["API development".to_string()],
            boundaries: vec!["No frontend".to_string()],
        };

        let boundaries = executor.generate_agent_boundaries(&backend_role);
        assert!(boundaries.contains("Backend"));
        assert!(boundaries.contains("Node.js, Express"));
        assert!(boundaries.contains("API development"));
        assert!(boundaries.contains("Do not modify"));
        assert!(boundaries.contains("Frontend components"));
    }

  ./crates/ccswarm/src/providers/aider.rs:455-512 method execute_task <-> ./crates/ccswarm/src/providers/aider.rs:746-763 function test_get_capabilities
  Similarity: 71.28%

[36m--- ./crates/ccswarm/src/providers/aider.rs:execute_task (lines 455-512) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate enhanced prompt with context and boundaries
        let prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with Aider (model: {}) for agent {}",
            task.description,
            self.config.model,
            identity.agent_id
        );

        // Execute the prompt
        match self.execute_prompt(&prompt, identity, working_dir).await {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_aider_output(output, task, duration);

                tracing::info!(
                    "Aider task completed in {:?} for agent {} (files changed: {})",
                    duration,
                    identity.agent_id,
                    result.output["files_changed"]
                        .as_array()
                        .map(|a| a.len())
                        .unwrap_or(0)
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Aider task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "aider",
                        "model": self.config.model,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_get_capabilities (lines 746-763) ---[0m
    fn test_get_capabilities() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(!capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(!capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert_eq!(capabilities.max_context_length, Some(128_000));
        assert!(capabilities
            .supported_languages
            .contains(&"python".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"rust".to_string()));
    }

  ./crates/ccswarm/src/providers/aider.rs:455-512 method execute_task <-> ./crates/ccswarm/src/providers/aider.rs:647-660 function test_generate_task_prompt
  Similarity: 73.46%

[36m--- ./crates/ccswarm/src/providers/aider.rs:execute_task (lines 455-512) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate enhanced prompt with context and boundaries
        let prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with Aider (model: {}) for agent {}",
            task.description,
            self.config.model,
            identity.agent_id
        );

        // Execute the prompt
        match self.execute_prompt(&prompt, identity, working_dir).await {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_aider_output(output, task, duration);

                tracing::info!(
                    "Aider task completed in {:?} for agent {} (files changed: {})",
                    duration,
                    identity.agent_id,
                    result.output["files_changed"]
                        .as_array()
                        .map(|a| a.len())
                        .unwrap_or(0)
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Aider task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "aider",
                        "model": self.config.model,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_generate_task_prompt (lines 647-660) ---[0m
    fn test_generate_task_prompt() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let prompt = executor.generate_task_prompt(&identity, &task);

        assert!(prompt.contains("# ccswarm Agent Context"));
        assert!(prompt.contains("Backend"));
        assert!(prompt.contains("Add authentication endpoint"));
        assert!(prompt.contains("**IMPORTANT**: Focus only on backend"));
        assert!(prompt.contains("## Aider Instructions"));
    }

  ./crates/ccswarm/src/providers/aider.rs:358-404 method parse_aider_output <-> ./crates/ccswarm/src/providers/aider.rs:663-683 function test_build_command_args
  Similarity: 75.49%

[36m--- ./crates/ccswarm/src/providers/aider.rs:parse_aider_output (lines 358-404) ---[0m
    fn parse_aider_output(
        &self,
        output: String,
        task: &Task,
        duration: std::time::Duration,
    ) -> TaskResult {
        let mut files_changed = Vec::new();
        let mut commits_made = Vec::new();
        let mut errors = Vec::new();

        // Parse output for common Aider patterns
        for line in output.lines() {
            if line.contains("Modified ") || line.contains("Created ") {
                if let Some(file) = self.extract_filename_from_line(line) {
                    files_changed.push(file);
                }
            } else if line.contains("Commit ") && line.contains("hash:") {
                if let Some(commit) = self.extract_commit_from_line(line) {
                    commits_made.push(commit);
                }
            } else if line.contains("Error:") || line.contains("WARNING:") {
                errors.push(line.to_string());
            }
        }

        let success = errors.is_empty() || errors.iter().all(|e| !e.contains("Error:"));

        TaskResult {
            success,
            output: serde_json::json!({
                "aider_output": output,
                "files_changed": files_changed,
                "commits_made": commits_made,
                "warnings": errors.iter().filter(|e| e.contains("WARNING")).collect::<Vec<_>>(),
                "task_id": task.id,
                "provider": "aider",
                "model": self.config.model,
                "auto_commit": self.config.auto_commit,
            }),
            error: if success {
                None
            } else {
                Some(errors.join("; "))
            },
            duration,
        }
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_build_command_args (lines 663-683) ---[0m
    fn test_build_command_args() {
        let config = AiderConfig {
            model: "claude-3.5-sonnet".to_string(),
            auto_commit: false,
            git: true,
            additional_args: vec!["--no-stream".to_string()],
            ..Default::default()
        };

        let executor = AiderExecutor::new(config);
        let working_dir = PathBuf::from("/tmp");
        let args = executor.build_command_args("test prompt", &working_dir);

        assert!(args.contains(&"--model".to_string()));
        assert!(args.contains(&"claude-3.5-sonnet".to_string()));
        assert!(args.contains(&"--no-auto-commits".to_string()));
        assert!(args.contains(&"--message".to_string()));
        assert!(args.contains(&"test prompt".to_string()));
        assert!(args.contains(&"--no-stream".to_string()));
        assert!(args.contains(&"--yes".to_string()));
    }

  ./crates/ccswarm/src/providers/aider.rs:253-283 method build_command_args <-> ./crates/ccswarm/src/providers/aider.rs:718-743 function test_parse_aider_output
  Similarity: 74.33%

[36m--- ./crates/ccswarm/src/providers/aider.rs:build_command_args (lines 253-283) ---[0m
    fn build_command_args(&self, prompt: &str, _working_dir: &Path) -> Vec<String> {
        let mut args = Vec::new();

        // Add model specification
        args.push("--model".to_string());
        args.push(self.config.model.clone());

        // Add auto-commit behavior
        if self.config.auto_commit {
            args.push("--auto-commits".to_string());
        } else {
            args.push("--no-auto-commits".to_string());
        }

        // Add git behavior
        if !self.config.git {
            args.push("--no-git".to_string());
        }

        // Add message with prompt
        args.push("--message".to_string());
        args.push(prompt.to_string());

        // Add additional custom arguments
        args.extend(self.config.additional_args.clone());

        // Add yes flag for non-interactive execution
        args.push("--yes".to_string());

        args
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_parse_aider_output (lines 718-743) ---[0m
    fn test_parse_aider_output() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_secs(10);

        let output = "Modified src/auth.js with authentication\n\
                     Created test/auth.test.js for testing\n\
                     Commit abc123 hash: def456789 completed\n\
                     All changes committed successfully";

        let result = executor.parse_aider_output(output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        let output_obj = result.output.as_object().unwrap();
        let files_changed = output_obj["files_changed"].as_array().unwrap();
        assert_eq!(files_changed.len(), 2);
        assert!(files_changed.contains(&serde_json::Value::String("src/auth.js".to_string())));
        assert!(files_changed.contains(&serde_json::Value::String("test/auth.test.js".to_string())));

        let commits_made = output_obj["commits_made"].as_array().unwrap();
        assert_eq!(commits_made.len(), 1);
        assert!(commits_made.contains(&serde_json::Value::String("def456789".to_string())));
    }

  ./crates/ccswarm/src/providers/aider.rs:407-424 method extract_filename_from_line <-> ./crates/ccswarm/src/providers/aider.rs:570-599 method get_capabilities
  Similarity: 82.63%
  Classes: AiderExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/aider.rs:extract_filename_from_line (lines 407-424) ---[0m
    fn extract_filename_from_line(&self, line: &str) -> Option<String> {
        // Look for patterns like "Modified src/main.rs" or "Created test.py"
        if let Some(start) = line.find("Modified ").or_else(|| line.find("Created ")) {
            let start_pos = if line[start..].starts_with("Modified ") {
                start + 9
            } else {
                start + 8
            };

            if let Some(end) = line[start_pos..].find(' ') {
                Some(line[start_pos..start_pos + end].to_string())
            } else {
                Some(line[start_pos..].to_string())
            }
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:get_capabilities (lines 570-599) ---[0m
    fn get_capabilities(&self) -> ProviderCapabilities {
        ProviderCapabilities {
            supports_json_output: false, // Aider doesn't have structured JSON output
            supports_streaming: false,
            supports_file_operations: true,
            supports_git_operations: true,
            supports_code_execution: false, // Aider doesn't execute code directly
            max_context_length: Some(128_000), // Depends on the model used
            supported_languages: vec![
                "python".to_string(),
                "javascript".to_string(),
                "typescript".to_string(),
                "java".to_string(),
                "c++".to_string(),
                "c".to_string(),
                "c#".to_string(),
                "go".to_string(),
                "rust".to_string(),
                "php".to_string(),
                "ruby".to_string(),
                "html".to_string(),
                "css".to_string(),
                "sql".to_string(),
                "bash".to_string(),
                "yaml".to_string(),
                "json".to_string(),
                "markdown".to_string(),
            ],
        }
    }

  ./crates/ccswarm/src/providers/aider.rs:570-599 method get_capabilities <-> ./crates/ccswarm/src/providers/aider.rs:611-626 function create_test_identity
  Similarity: 84.62%

[36m--- ./crates/ccswarm/src/providers/aider.rs:get_capabilities (lines 570-599) ---[0m
    fn get_capabilities(&self) -> ProviderCapabilities {
        ProviderCapabilities {
            supports_json_output: false, // Aider doesn't have structured JSON output
            supports_streaming: false,
            supports_file_operations: true,
            supports_git_operations: true,
            supports_code_execution: false, // Aider doesn't execute code directly
            max_context_length: Some(128_000), // Depends on the model used
            supported_languages: vec![
                "python".to_string(),
                "javascript".to_string(),
                "typescript".to_string(),
                "java".to_string(),
                "c++".to_string(),
                "c".to_string(),
                "c#".to_string(),
                "go".to_string(),
                "rust".to_string(),
                "php".to_string(),
                "ruby".to_string(),
                "html".to_string(),
                "css".to_string(),
                "sql".to_string(),
                "bash".to_string(),
                "yaml".to_string(),
                "json".to_string(),
                "markdown".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:create_test_identity (lines 611-626) ---[0m
    fn create_test_identity() -> AgentIdentity {
        let temp_dir = TempDir::new().unwrap();
        AgentIdentity {
            agent_id: "test-agent".to_string(),
            specialization: AgentRole::Backend {
                technologies: vec!["Node.js".to_string(), "Express".to_string()],
                responsibilities: vec!["API development".to_string()],
                boundaries: vec!["No frontend work".to_string()],
            },
            workspace_path: temp_dir.path().to_path_buf(),
            env_vars: HashMap::new(),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: "12345".to_string(),
            initialized_at: chrono::Utc::now(),
        }
    }

  ./crates/ccswarm/src/providers/aider.rs:253-283 method build_command_args <-> ./crates/ccswarm/src/providers/aider.rs:663-683 function test_build_command_args
  Similarity: 73.38%

[36m--- ./crates/ccswarm/src/providers/aider.rs:build_command_args (lines 253-283) ---[0m
    fn build_command_args(&self, prompt: &str, _working_dir: &Path) -> Vec<String> {
        let mut args = Vec::new();

        // Add model specification
        args.push("--model".to_string());
        args.push(self.config.model.clone());

        // Add auto-commit behavior
        if self.config.auto_commit {
            args.push("--auto-commits".to_string());
        } else {
            args.push("--no-auto-commits".to_string());
        }

        // Add git behavior
        if !self.config.git {
            args.push("--no-git".to_string());
        }

        // Add message with prompt
        args.push("--message".to_string());
        args.push(prompt.to_string());

        // Add additional custom arguments
        args.extend(self.config.additional_args.clone());

        // Add yes flag for non-interactive execution
        args.push("--yes".to_string());

        args
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_build_command_args (lines 663-683) ---[0m
    fn test_build_command_args() {
        let config = AiderConfig {
            model: "claude-3.5-sonnet".to_string(),
            auto_commit: false,
            git: true,
            additional_args: vec!["--no-stream".to_string()],
            ..Default::default()
        };

        let executor = AiderExecutor::new(config);
        let working_dir = PathBuf::from("/tmp");
        let args = executor.build_command_args("test prompt", &working_dir);

        assert!(args.contains(&"--model".to_string()));
        assert!(args.contains(&"claude-3.5-sonnet".to_string()));
        assert!(args.contains(&"--no-auto-commits".to_string()));
        assert!(args.contains(&"--message".to_string()));
        assert!(args.contains(&"test prompt".to_string()));
        assert!(args.contains(&"--no-stream".to_string()));
        assert!(args.contains(&"--yes".to_string()));
    }

  ./crates/ccswarm/src/providers/aider.rs:427-439 method extract_commit_from_line <-> ./crates/ccswarm/src/providers/aider.rs:570-599 method get_capabilities
  Similarity: 82.63%
  Classes: AiderExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/aider.rs:extract_commit_from_line (lines 427-439) ---[0m
    fn extract_commit_from_line(&self, line: &str) -> Option<String> {
        // Look for patterns like "Commit abc123 hash: def456"
        if let Some(hash_pos) = line.find("hash: ") {
            let start = hash_pos + 6;
            if let Some(end) = line[start..].find(' ') {
                Some(line[start..start + end].to_string())
            } else {
                Some(line[start..].to_string())
            }
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:get_capabilities (lines 570-599) ---[0m
    fn get_capabilities(&self) -> ProviderCapabilities {
        ProviderCapabilities {
            supports_json_output: false, // Aider doesn't have structured JSON output
            supports_streaming: false,
            supports_file_operations: true,
            supports_git_operations: true,
            supports_code_execution: false, // Aider doesn't execute code directly
            max_context_length: Some(128_000), // Depends on the model used
            supported_languages: vec![
                "python".to_string(),
                "javascript".to_string(),
                "typescript".to_string(),
                "java".to_string(),
                "c++".to_string(),
                "c".to_string(),
                "c#".to_string(),
                "go".to_string(),
                "rust".to_string(),
                "php".to_string(),
                "ruby".to_string(),
                "html".to_string(),
                "css".to_string(),
                "sql".to_string(),
                "bash".to_string(),
                "yaml".to_string(),
                "json".to_string(),
                "markdown".to_string(),
            ],
        }
    }

  ./crates/ccswarm/src/providers/aider.rs:253-283 method build_command_args <-> ./crates/ccswarm/src/providers/aider.rs:746-763 function test_get_capabilities
  Similarity: 72.21%

[36m--- ./crates/ccswarm/src/providers/aider.rs:build_command_args (lines 253-283) ---[0m
    fn build_command_args(&self, prompt: &str, _working_dir: &Path) -> Vec<String> {
        let mut args = Vec::new();

        // Add model specification
        args.push("--model".to_string());
        args.push(self.config.model.clone());

        // Add auto-commit behavior
        if self.config.auto_commit {
            args.push("--auto-commits".to_string());
        } else {
            args.push("--no-auto-commits".to_string());
        }

        // Add git behavior
        if !self.config.git {
            args.push("--no-git".to_string());
        }

        // Add message with prompt
        args.push("--message".to_string());
        args.push(prompt.to_string());

        // Add additional custom arguments
        args.extend(self.config.additional_args.clone());

        // Add yes flag for non-interactive execution
        args.push("--yes".to_string());

        args
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_get_capabilities (lines 746-763) ---[0m
    fn test_get_capabilities() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(!capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(!capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert_eq!(capabilities.max_context_length, Some(128_000));
        assert!(capabilities
            .supported_languages
            .contains(&"python".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"rust".to_string()));
    }

  ./crates/ccswarm/src/providers/aider.rs:253-283 method build_command_args <-> ./crates/ccswarm/src/providers/aider.rs:686-701 function test_extract_filename_from_line
  Similarity: 74.19%

[36m--- ./crates/ccswarm/src/providers/aider.rs:build_command_args (lines 253-283) ---[0m
    fn build_command_args(&self, prompt: &str, _working_dir: &Path) -> Vec<String> {
        let mut args = Vec::new();

        // Add model specification
        args.push("--model".to_string());
        args.push(self.config.model.clone());

        // Add auto-commit behavior
        if self.config.auto_commit {
            args.push("--auto-commits".to_string());
        } else {
            args.push("--no-auto-commits".to_string());
        }

        // Add git behavior
        if !self.config.git {
            args.push("--no-git".to_string());
        }

        // Add message with prompt
        args.push("--message".to_string());
        args.push(prompt.to_string());

        // Add additional custom arguments
        args.extend(self.config.additional_args.clone());

        // Add yes flag for non-interactive execution
        args.push("--yes".to_string());

        args
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_extract_filename_from_line (lines 686-701) ---[0m
    fn test_extract_filename_from_line() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);

        let line1 = "Modified src/auth.js with new JWT implementation";
        let filename1 = executor.extract_filename_from_line(line1);
        assert_eq!(filename1, Some("src/auth.js".to_string()));

        let line2 = "Created test/auth.test.js";
        let filename2 = executor.extract_filename_from_line(line2);
        assert_eq!(filename2, Some("test/auth.test.js".to_string()));

        let line3 = "Some other output without file modification";
        let filename3 = executor.extract_filename_from_line(line3);
        assert_eq!(filename3, None);
    }

  ./crates/ccswarm/src/providers/aider.rs:663-683 function test_build_command_args <-> ./crates/ccswarm/src/providers/aider.rs:746-763 function test_get_capabilities
  Similarity: 89.09%

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_build_command_args (lines 663-683) ---[0m
    fn test_build_command_args() {
        let config = AiderConfig {
            model: "claude-3.5-sonnet".to_string(),
            auto_commit: false,
            git: true,
            additional_args: vec!["--no-stream".to_string()],
            ..Default::default()
        };

        let executor = AiderExecutor::new(config);
        let working_dir = PathBuf::from("/tmp");
        let args = executor.build_command_args("test prompt", &working_dir);

        assert!(args.contains(&"--model".to_string()));
        assert!(args.contains(&"claude-3.5-sonnet".to_string()));
        assert!(args.contains(&"--no-auto-commits".to_string()));
        assert!(args.contains(&"--message".to_string()));
        assert!(args.contains(&"test prompt".to_string()));
        assert!(args.contains(&"--no-stream".to_string()));
        assert!(args.contains(&"--yes".to_string()));
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_get_capabilities (lines 746-763) ---[0m
    fn test_get_capabilities() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(!capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(!capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert_eq!(capabilities.max_context_length, Some(128_000));
        assert!(capabilities
            .supported_languages
            .contains(&"python".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"rust".to_string()));
    }

  ./crates/ccswarm/src/providers/aider.rs:25-55 method generate_task_prompt <-> ./crates/ccswarm/src/providers/aider.rs:704-715 function test_extract_commit_from_line
  Similarity: 79.09%

[36m--- ./crates/ccswarm/src/providers/aider.rs:generate_task_prompt (lines 25-55) ---[0m
    fn generate_task_prompt(&self, identity: &AgentIdentity, task: &Task) -> String {
        let context_header = format!(
            "# ccswarm Agent Context\n\
             Agent: {} ({})\n\
             Task: {}\n\
             Priority: {:?}\n\
             Type: {:?}\n\n",
            identity.agent_id,
            identity.specialization.name(),
            task.id,
            task.priority,
            task.task_type
        );

        let boundaries = self.generate_agent_boundaries(&identity.specialization);

        let task_description = format!("## Task Description\n{}\n\n", task.description);

        let task_details = if let Some(details) = &task.details {
            format!("## Additional Details\n{}\n\n", details)
        } else {
            String::new()
        };

        let aider_instructions = self.generate_aider_instructions(task);

        format!(
            "{}{}{}{}\n{}",
            context_header, boundaries, task_description, task_details, aider_instructions
        )
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_extract_commit_from_line (lines 704-715) ---[0m
    fn test_extract_commit_from_line() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);

        let line = "Commit abc123 hash: def456789 completed successfully";
        let commit = executor.extract_commit_from_line(line);
        assert_eq!(commit, Some("def456789".to_string()));

        let line_no_hash = "Some other output without commit hash";
        let no_commit = executor.extract_commit_from_line(line_no_hash);
        assert_eq!(no_commit, None);
    }

  ./crates/ccswarm/src/providers/aider.rs:253-283 method build_command_args <-> ./crates/ccswarm/src/providers/aider.rs:647-660 function test_generate_task_prompt
  Similarity: 70.99%

[36m--- ./crates/ccswarm/src/providers/aider.rs:build_command_args (lines 253-283) ---[0m
    fn build_command_args(&self, prompt: &str, _working_dir: &Path) -> Vec<String> {
        let mut args = Vec::new();

        // Add model specification
        args.push("--model".to_string());
        args.push(self.config.model.clone());

        // Add auto-commit behavior
        if self.config.auto_commit {
            args.push("--auto-commits".to_string());
        } else {
            args.push("--no-auto-commits".to_string());
        }

        // Add git behavior
        if !self.config.git {
            args.push("--no-git".to_string());
        }

        // Add message with prompt
        args.push("--message".to_string());
        args.push(prompt.to_string());

        // Add additional custom arguments
        args.extend(self.config.additional_args.clone());

        // Add yes flag for non-interactive execution
        args.push("--yes".to_string());

        args
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_generate_task_prompt (lines 647-660) ---[0m
    fn test_generate_task_prompt() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let prompt = executor.generate_task_prompt(&identity, &task);

        assert!(prompt.contains("# ccswarm Agent Context"));
        assert!(prompt.contains("Backend"));
        assert!(prompt.contains("Add authentication endpoint"));
        assert!(prompt.contains("**IMPORTANT**: Focus only on backend"));
        assert!(prompt.contains("## Aider Instructions"));
    }

  ./crates/ccswarm/src/providers/aider.rs:663-683 function test_build_command_args <-> ./crates/ccswarm/src/providers/aider.rs:686-701 function test_extract_filename_from_line
  Similarity: 85.78%

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_build_command_args (lines 663-683) ---[0m
    fn test_build_command_args() {
        let config = AiderConfig {
            model: "claude-3.5-sonnet".to_string(),
            auto_commit: false,
            git: true,
            additional_args: vec!["--no-stream".to_string()],
            ..Default::default()
        };

        let executor = AiderExecutor::new(config);
        let working_dir = PathBuf::from("/tmp");
        let args = executor.build_command_args("test prompt", &working_dir);

        assert!(args.contains(&"--model".to_string()));
        assert!(args.contains(&"claude-3.5-sonnet".to_string()));
        assert!(args.contains(&"--no-auto-commits".to_string()));
        assert!(args.contains(&"--message".to_string()));
        assert!(args.contains(&"test prompt".to_string()));
        assert!(args.contains(&"--no-stream".to_string()));
        assert!(args.contains(&"--yes".to_string()));
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_extract_filename_from_line (lines 686-701) ---[0m
    fn test_extract_filename_from_line() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);

        let line1 = "Modified src/auth.js with new JWT implementation";
        let filename1 = executor.extract_filename_from_line(line1);
        assert_eq!(filename1, Some("src/auth.js".to_string()));

        let line2 = "Created test/auth.test.js";
        let filename2 = executor.extract_filename_from_line(line2);
        assert_eq!(filename2, Some("test/auth.test.js".to_string()));

        let line3 = "Some other output without file modification";
        let filename3 = executor.extract_filename_from_line(line3);
        assert_eq!(filename3, None);
    }

  ./crates/ccswarm/src/providers/aider.rs:407-424 method extract_filename_from_line <-> ./crates/ccswarm/src/providers/aider.rs:427-439 method extract_commit_from_line
  Similarity: 94.06%
  Classes: AiderExecutor <-> AiderExecutor

[36m--- ./crates/ccswarm/src/providers/aider.rs:extract_filename_from_line (lines 407-424) ---[0m
    fn extract_filename_from_line(&self, line: &str) -> Option<String> {
        // Look for patterns like "Modified src/main.rs" or "Created test.py"
        if let Some(start) = line.find("Modified ").or_else(|| line.find("Created ")) {
            let start_pos = if line[start..].starts_with("Modified ") {
                start + 9
            } else {
                start + 8
            };

            if let Some(end) = line[start_pos..].find(' ') {
                Some(line[start_pos..start_pos + end].to_string())
            } else {
                Some(line[start_pos..].to_string())
            }
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:extract_commit_from_line (lines 427-439) ---[0m
    fn extract_commit_from_line(&self, line: &str) -> Option<String> {
        // Look for patterns like "Commit abc123 hash: def456"
        if let Some(hash_pos) = line.find("hash: ") {
            let start = hash_pos + 6;
            if let Some(end) = line[start..].find(' ') {
                Some(line[start..start + end].to_string())
            } else {
                Some(line[start..].to_string())
            }
        } else {
            None
        }
    }

  ./crates/ccswarm/src/providers/aider.rs:663-683 function test_build_command_args <-> ./crates/ccswarm/src/providers/aider.rs:766-782 function test_generate_agent_boundaries
  Similarity: 75.12%

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_build_command_args (lines 663-683) ---[0m
    fn test_build_command_args() {
        let config = AiderConfig {
            model: "claude-3.5-sonnet".to_string(),
            auto_commit: false,
            git: true,
            additional_args: vec!["--no-stream".to_string()],
            ..Default::default()
        };

        let executor = AiderExecutor::new(config);
        let working_dir = PathBuf::from("/tmp");
        let args = executor.build_command_args("test prompt", &working_dir);

        assert!(args.contains(&"--model".to_string()));
        assert!(args.contains(&"claude-3.5-sonnet".to_string()));
        assert!(args.contains(&"--no-auto-commits".to_string()));
        assert!(args.contains(&"--message".to_string()));
        assert!(args.contains(&"test prompt".to_string()));
        assert!(args.contains(&"--no-stream".to_string()));
        assert!(args.contains(&"--yes".to_string()));
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_generate_agent_boundaries (lines 766-782) ---[0m
    fn test_generate_agent_boundaries() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);

        let backend_role = AgentRole::Backend {
            technologies: vec!["Node.js".to_string(), "Express".to_string()],
            responsibilities: vec!["API development".to_string()],
            boundaries: vec!["No frontend".to_string()],
        };

        let boundaries = executor.generate_agent_boundaries(&backend_role);
        assert!(boundaries.contains("Backend"));
        assert!(boundaries.contains("Node.js, Express"));
        assert!(boundaries.contains("API development"));
        assert!(boundaries.contains("Do not modify"));
        assert!(boundaries.contains("Frontend components"));
    }

  ./crates/ccswarm/src/providers/aider.rs:647-660 function test_generate_task_prompt <-> ./crates/ccswarm/src/providers/aider.rs:663-683 function test_build_command_args
  Similarity: 80.38%

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_generate_task_prompt (lines 647-660) ---[0m
    fn test_generate_task_prompt() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let prompt = executor.generate_task_prompt(&identity, &task);

        assert!(prompt.contains("# ccswarm Agent Context"));
        assert!(prompt.contains("Backend"));
        assert!(prompt.contains("Add authentication endpoint"));
        assert!(prompt.contains("**IMPORTANT**: Focus only on backend"));
        assert!(prompt.contains("## Aider Instructions"));
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_build_command_args (lines 663-683) ---[0m
    fn test_build_command_args() {
        let config = AiderConfig {
            model: "claude-3.5-sonnet".to_string(),
            auto_commit: false,
            git: true,
            additional_args: vec!["--no-stream".to_string()],
            ..Default::default()
        };

        let executor = AiderExecutor::new(config);
        let working_dir = PathBuf::from("/tmp");
        let args = executor.build_command_args("test prompt", &working_dir);

        assert!(args.contains(&"--model".to_string()));
        assert!(args.contains(&"claude-3.5-sonnet".to_string()));
        assert!(args.contains(&"--no-auto-commits".to_string()));
        assert!(args.contains(&"--message".to_string()));
        assert!(args.contains(&"test prompt".to_string()));
        assert!(args.contains(&"--no-stream".to_string()));
        assert!(args.contains(&"--yes".to_string()));
    }

  ./crates/ccswarm/src/providers/aider.rs:647-660 function test_generate_task_prompt <-> ./crates/ccswarm/src/providers/aider.rs:766-782 function test_generate_agent_boundaries
  Similarity: 90.09%

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_generate_task_prompt (lines 647-660) ---[0m
    fn test_generate_task_prompt() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let prompt = executor.generate_task_prompt(&identity, &task);

        assert!(prompt.contains("# ccswarm Agent Context"));
        assert!(prompt.contains("Backend"));
        assert!(prompt.contains("Add authentication endpoint"));
        assert!(prompt.contains("**IMPORTANT**: Focus only on backend"));
        assert!(prompt.contains("## Aider Instructions"));
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_generate_agent_boundaries (lines 766-782) ---[0m
    fn test_generate_agent_boundaries() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);

        let backend_role = AgentRole::Backend {
            technologies: vec!["Node.js".to_string(), "Express".to_string()],
            responsibilities: vec!["API development".to_string()],
            boundaries: vec!["No frontend".to_string()],
        };

        let boundaries = executor.generate_agent_boundaries(&backend_role);
        assert!(boundaries.contains("Backend"));
        assert!(boundaries.contains("Node.js, Express"));
        assert!(boundaries.contains("API development"));
        assert!(boundaries.contains("Do not modify"));
        assert!(boundaries.contains("Frontend components"));
    }

  ./crates/ccswarm/src/providers/aider.rs:746-763 function test_get_capabilities <-> ./crates/ccswarm/src/providers/aider.rs:766-782 function test_generate_agent_boundaries
  Similarity: 77.34%

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_get_capabilities (lines 746-763) ---[0m
    fn test_get_capabilities() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(!capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(!capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert_eq!(capabilities.max_context_length, Some(128_000));
        assert!(capabilities
            .supported_languages
            .contains(&"python".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"rust".to_string()));
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_generate_agent_boundaries (lines 766-782) ---[0m
    fn test_generate_agent_boundaries() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);

        let backend_role = AgentRole::Backend {
            technologies: vec!["Node.js".to_string(), "Express".to_string()],
            responsibilities: vec!["API development".to_string()],
            boundaries: vec!["No frontend".to_string()],
        };

        let boundaries = executor.generate_agent_boundaries(&backend_role);
        assert!(boundaries.contains("Backend"));
        assert!(boundaries.contains("Node.js, Express"));
        assert!(boundaries.contains("API development"));
        assert!(boundaries.contains("Do not modify"));
        assert!(boundaries.contains("Frontend components"));
    }

  ./crates/ccswarm/src/providers/aider.rs:686-701 function test_extract_filename_from_line <-> ./crates/ccswarm/src/providers/aider.rs:746-763 function test_get_capabilities
  Similarity: 76.25%

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_extract_filename_from_line (lines 686-701) ---[0m
    fn test_extract_filename_from_line() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);

        let line1 = "Modified src/auth.js with new JWT implementation";
        let filename1 = executor.extract_filename_from_line(line1);
        assert_eq!(filename1, Some("src/auth.js".to_string()));

        let line2 = "Created test/auth.test.js";
        let filename2 = executor.extract_filename_from_line(line2);
        assert_eq!(filename2, Some("test/auth.test.js".to_string()));

        let line3 = "Some other output without file modification";
        let filename3 = executor.extract_filename_from_line(line3);
        assert_eq!(filename3, None);
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_get_capabilities (lines 746-763) ---[0m
    fn test_get_capabilities() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(!capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(!capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert_eq!(capabilities.max_context_length, Some(128_000));
        assert!(capabilities
            .supported_languages
            .contains(&"python".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"rust".to_string()));
    }

  ./crates/ccswarm/src/providers/aider.rs:686-701 function test_extract_filename_from_line <-> ./crates/ccswarm/src/providers/aider.rs:766-782 function test_generate_agent_boundaries
  Similarity: 78.27%

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_extract_filename_from_line (lines 686-701) ---[0m
    fn test_extract_filename_from_line() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);

        let line1 = "Modified src/auth.js with new JWT implementation";
        let filename1 = executor.extract_filename_from_line(line1);
        assert_eq!(filename1, Some("src/auth.js".to_string()));

        let line2 = "Created test/auth.test.js";
        let filename2 = executor.extract_filename_from_line(line2);
        assert_eq!(filename2, Some("test/auth.test.js".to_string()));

        let line3 = "Some other output without file modification";
        let filename3 = executor.extract_filename_from_line(line3);
        assert_eq!(filename3, None);
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_generate_agent_boundaries (lines 766-782) ---[0m
    fn test_generate_agent_boundaries() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);

        let backend_role = AgentRole::Backend {
            technologies: vec!["Node.js".to_string(), "Express".to_string()],
            responsibilities: vec!["API development".to_string()],
            boundaries: vec!["No frontend".to_string()],
        };

        let boundaries = executor.generate_agent_boundaries(&backend_role);
        assert!(boundaries.contains("Backend"));
        assert!(boundaries.contains("Node.js, Express"));
        assert!(boundaries.contains("API development"));
        assert!(boundaries.contains("Do not modify"));
        assert!(boundaries.contains("Frontend components"));
    }

  ./crates/ccswarm/src/providers/aider.rs:647-660 function test_generate_task_prompt <-> ./crates/ccswarm/src/providers/aider.rs:746-763 function test_get_capabilities
  Similarity: 76.31%

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_generate_task_prompt (lines 647-660) ---[0m
    fn test_generate_task_prompt() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let prompt = executor.generate_task_prompt(&identity, &task);

        assert!(prompt.contains("# ccswarm Agent Context"));
        assert!(prompt.contains("Backend"));
        assert!(prompt.contains("Add authentication endpoint"));
        assert!(prompt.contains("**IMPORTANT**: Focus only on backend"));
        assert!(prompt.contains("## Aider Instructions"));
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_get_capabilities (lines 746-763) ---[0m
    fn test_get_capabilities() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(!capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(!capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert_eq!(capabilities.max_context_length, Some(128_000));
        assert!(capabilities
            .supported_languages
            .contains(&"python".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"rust".to_string()));
    }

  ./crates/ccswarm/src/providers/aider.rs:704-715 function test_extract_commit_from_line <-> ./crates/ccswarm/src/providers/aider.rs:766-782 function test_generate_agent_boundaries
  Similarity: 80.61%

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_extract_commit_from_line (lines 704-715) ---[0m
    fn test_extract_commit_from_line() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);

        let line = "Commit abc123 hash: def456789 completed successfully";
        let commit = executor.extract_commit_from_line(line);
        assert_eq!(commit, Some("def456789".to_string()));

        let line_no_hash = "Some other output without commit hash";
        let no_commit = executor.extract_commit_from_line(line_no_hash);
        assert_eq!(no_commit, None);
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_generate_agent_boundaries (lines 766-782) ---[0m
    fn test_generate_agent_boundaries() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);

        let backend_role = AgentRole::Backend {
            technologies: vec!["Node.js".to_string(), "Express".to_string()],
            responsibilities: vec!["API development".to_string()],
            boundaries: vec!["No frontend".to_string()],
        };

        let boundaries = executor.generate_agent_boundaries(&backend_role);
        assert!(boundaries.contains("Backend"));
        assert!(boundaries.contains("Node.js, Express"));
        assert!(boundaries.contains("API development"));
        assert!(boundaries.contains("Do not modify"));
        assert!(boundaries.contains("Frontend components"));
    }

  ./crates/ccswarm/src/providers/aider.rs:647-660 function test_generate_task_prompt <-> ./crates/ccswarm/src/providers/aider.rs:686-701 function test_extract_filename_from_line
  Similarity: 72.72%

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_generate_task_prompt (lines 647-660) ---[0m
    fn test_generate_task_prompt() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let prompt = executor.generate_task_prompt(&identity, &task);

        assert!(prompt.contains("# ccswarm Agent Context"));
        assert!(prompt.contains("Backend"));
        assert!(prompt.contains("Add authentication endpoint"));
        assert!(prompt.contains("**IMPORTANT**: Focus only on backend"));
        assert!(prompt.contains("## Aider Instructions"));
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_extract_filename_from_line (lines 686-701) ---[0m
    fn test_extract_filename_from_line() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);

        let line1 = "Modified src/auth.js with new JWT implementation";
        let filename1 = executor.extract_filename_from_line(line1);
        assert_eq!(filename1, Some("src/auth.js".to_string()));

        let line2 = "Created test/auth.test.js";
        let filename2 = executor.extract_filename_from_line(line2);
        assert_eq!(filename2, Some("test/auth.test.js".to_string()));

        let line3 = "Some other output without file modification";
        let filename3 = executor.extract_filename_from_line(line3);
        assert_eq!(filename3, None);
    }

  ./crates/ccswarm/src/providers/aider.rs:686-701 function test_extract_filename_from_line <-> ./crates/ccswarm/src/providers/aider.rs:704-715 function test_extract_commit_from_line
  Similarity: 74.09%

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_extract_filename_from_line (lines 686-701) ---[0m
    fn test_extract_filename_from_line() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);

        let line1 = "Modified src/auth.js with new JWT implementation";
        let filename1 = executor.extract_filename_from_line(line1);
        assert_eq!(filename1, Some("src/auth.js".to_string()));

        let line2 = "Created test/auth.test.js";
        let filename2 = executor.extract_filename_from_line(line2);
        assert_eq!(filename2, Some("test/auth.test.js".to_string()));

        let line3 = "Some other output without file modification";
        let filename3 = executor.extract_filename_from_line(line3);
        assert_eq!(filename3, None);
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_extract_commit_from_line (lines 704-715) ---[0m
    fn test_extract_commit_from_line() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);

        let line = "Commit abc123 hash: def456789 completed successfully";
        let commit = executor.extract_commit_from_line(line);
        assert_eq!(commit, Some("def456789".to_string()));

        let line_no_hash = "Some other output without commit hash";
        let no_commit = executor.extract_commit_from_line(line_no_hash);
        assert_eq!(no_commit, None);
    }

  ./crates/ccswarm/src/providers/aider.rs:647-660 function test_generate_task_prompt <-> ./crates/ccswarm/src/providers/aider.rs:704-715 function test_extract_commit_from_line
  Similarity: 70.13%

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_generate_task_prompt (lines 647-660) ---[0m
    fn test_generate_task_prompt() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let prompt = executor.generate_task_prompt(&identity, &task);

        assert!(prompt.contains("# ccswarm Agent Context"));
        assert!(prompt.contains("Backend"));
        assert!(prompt.contains("Add authentication endpoint"));
        assert!(prompt.contains("**IMPORTANT**: Focus only on backend"));
        assert!(prompt.contains("## Aider Instructions"));
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:test_extract_commit_from_line (lines 704-715) ---[0m
    fn test_extract_commit_from_line() {
        let config = AiderConfig::default();
        let executor = AiderExecutor::new(config);

        let line = "Commit abc123 hash: def456789 completed successfully";
        let commit = executor.extract_commit_from_line(line);
        assert_eq!(commit, Some("def456789".to_string()));

        let line_no_hash = "Some other output without commit hash";
        let no_commit = executor.extract_commit_from_line(line_no_hash);
        assert_eq!(no_commit, None);
    }

  ./crates/ccswarm/src/providers/aider.rs:427-439 method extract_commit_from_line <-> ./crates/ccswarm/src/providers/aider.rs:628-636 function create_test_task
  Similarity: 78.67%

[36m--- ./crates/ccswarm/src/providers/aider.rs:extract_commit_from_line (lines 427-439) ---[0m
    fn extract_commit_from_line(&self, line: &str) -> Option<String> {
        // Look for patterns like "Commit abc123 hash: def456"
        if let Some(hash_pos) = line.find("hash: ") {
            let start = hash_pos + 6;
            if let Some(end) = line[start..].find(' ') {
                Some(line[start..start + end].to_string())
            } else {
                Some(line[start..].to_string())
            }
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/providers/aider.rs:create_test_task (lines 628-636) ---[0m
    fn create_test_task() -> Task {
        Task::new(
            Uuid::new_v4().to_string(),
            "Add authentication endpoint".to_string(),
            crate::agent::Priority::High,
            TaskType::Development,
        )
        .with_details("Implement JWT-based authentication".to_string())
    }


Duplicates in ./crates/ccswarm/src/agent/persistent.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/agent/persistent.rs:178-235 method build_contextual_prompt <-> ./crates/ccswarm/src/agent/persistent.rs:811-865 method review_and_adapt
  Similarity: 91.48%
  Classes: ClaudeCodeSession <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/persistent.rs:build_contextual_prompt (lines 178-235) ---[0m
    fn build_contextual_prompt(
        &self,
        task: &Task,
        history: &VecDeque<ConversationMessage>,
    ) -> String {
        let mut prompt = String::new();

        // Add recent relevant context (last 3 messages)
        let recent_context: Vec<_> = history
            .iter()
            .rev()
            .take(3)
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .collect();

        if !recent_context.is_empty() {
            prompt.push_str("## Recent Context:\n");
            for msg in recent_context.iter().rev() {
                match msg.message_type {
                    MessageType::TaskPrompt => {
                        prompt.push_str(&format!(
                            "Previous Task: {}\n",
                            msg.content.lines().next().unwrap_or("")
                        ));
                    }
                    MessageType::Response => {
                        let summary = msg
                            .content
                            .lines()
                            .next()
                            .unwrap_or("")
                            .chars()
                            .take(100)
                            .collect::<String>();
                        prompt.push_str(&format!("Previous Result: {}...\n", summary));
                    }
                    _ => {}
                }
            }
            prompt.push('\n');
        }

        // Add current task
        prompt.push_str(&format!(
            "## Current Task:\n**{}**\n\n{}\n\nPriority: {:?}\nType: {:?}\n",
            task.description,
            task.details.as_deref().unwrap_or(""),
            task.priority,
            task.task_type
        ));

        prompt
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:review_and_adapt (lines 811-865) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Persistent agent reviewing and adapting plan based on {} results",
            results.len()
        );

        // Leverage conversation history for intelligent adaptation
        let history = self.conversation_history.lock().await;
        let failed_steps: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_steps.is_empty() {
            warn!(
                "Persistent agent detected {} failed steps",
                failed_steps.len()
            );

            // Analyze failure patterns from session history
            let failure_analysis = self.analyze_failure_patterns(&history, &failed_steps);

            // Add adaptive recovery steps based on session context
            if failure_analysis.contains("context_loss") {
                let recovery_step = TaskStep::new(
                    "context_recovery".to_string(),
                    "Recover session context".to_string(),
                    StepType::Execution,
                )
                .with_description("Recover lost session context".to_string());
                current_plan.steps.push(recovery_step);
            }

            if failure_analysis.contains("identity_drift") {
                let identity_step = TaskStep::new(
                    "identity_reinforcement".to_string(),
                    "Reinforce agent identity".to_string(),
                    StepType::Execution,
                )
                .with_description("Reinforce agent identity and boundaries".to_string());
                current_plan.steps.push(identity_step);
            }

            let validation_step = TaskStep::new(
                "adaptive_validation".to_string(),
                "Validate adaptive recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Validate recovery steps".to_string());
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

  ./crates/ccswarm/src/agent/persistent.rs:314-365 method establish_identity_once <-> ./crates/ccswarm/src/agent/persistent.rs:404-476 method execute_task
  Similarity: 81.91%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:establish_identity_once (lines 314-365) ---[0m
    pub async fn establish_identity_once(&self) -> Result<()> {
        if self.identity_established.load(Ordering::Relaxed) {
            tracing::debug!(
                "Identity already established for agent: {}",
                self.identity.agent_id
            );
            return Ok(());
        }

        tracing::info!(
            "Establishing identity for agent: {}",
            self.identity.agent_id
        );

        // Generate lightweight identity prompt (200 tokens vs 2000+ in original)
        let compact_prompt = self.generate_compact_identity_prompt();

        // Send identity establishment message
        let mut session = self.session.lock().await;
        let response = session.send_message(&compact_prompt).await?;

        // Verify response contains identity markers
        if !self.verify_identity_response(&response) {
            return Err(anyhow::anyhow!("Failed to establish agent identity"));
        }

        // Record in conversation history
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityEstablishment,
            content: compact_prompt,
            task_id: None,
        });
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::Response,
            content: response,
            task_id: None,
        });

        // Trim history if needed
        self.trim_history(&mut history);

        self.identity_established.store(true, Ordering::Relaxed);
        tracing::info!(
            "Identity established successfully for agent: {}",
            self.identity.agent_id
        );

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_task (lines 404-476) ---[0m
    pub async fn execute_task(&mut self, task: Task) -> Result<TaskResult> {
        self.status = AgentStatus::Working;
        self.current_task = Some(task.clone());
        self.last_activity = Utc::now();

        // Ensure identity is established
        self.establish_identity_once().await?;

        let start_time = Instant::now();
        let mut identity_monitor = IdentityMonitor::new(&self.identity.agent_id);

        // Execute task with conversation context
        let history = self.conversation_history.lock().await;
        let history_clone = history.clone();
        drop(history); // Release lock

        let mut session = self.session.lock().await;
        let response = session.execute_with_context(&task, &history_clone).await?;
        drop(session); // Release lock

        // Monitor identity in response
        let identity_status = identity_monitor.monitor_response(&response).await?;
        match identity_status {
            IdentityStatus::Healthy => {
                tracing::debug!("Identity maintained during task execution");
            }
            IdentityStatus::DriftDetected(msg) => {
                tracing::warn!("Identity drift detected: {}", msg);
                self.send_identity_reminder().await?;
            }
            IdentityStatus::BoundaryViolation(msg) => {
                return Err(anyhow::anyhow!("Boundary violation detected: {}", msg));
            }
            IdentityStatus::CriticalFailure(msg) => {
                return Err(anyhow::anyhow!("Critical identity failure: {}", msg));
            }
        }

        // Record conversation
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::TaskPrompt,
            content: format!("Task: {}", task.description),
            task_id: Some(task.id.clone()),
        });
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::Response,
            content: response.clone(),
            task_id: Some(task.id.clone()),
        });
        self.trim_history(&mut history);

        let result = TaskResult {
            success: true,
            output: serde_json::json!({
                "response": response,
                "agent": self.identity.agent_id,
                "task_id": task.id,
                "session_id": self.session.lock().await.session_id(),
            }),
            error: None,
            duration: start_time.elapsed(),
        };

        // Update status and history
        self.status = AgentStatus::WaitingForReview;
        self.current_task = None;
        self.task_history.push((task, result.clone()));

        Ok(result)
    }

  ./crates/ccswarm/src/agent/persistent.rs:178-235 method build_contextual_prompt <-> ./crates/ccswarm/src/agent/persistent.rs:903-956 method synthesize_results
  Similarity: 90.84%
  Classes: ClaudeCodeSession <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/persistent.rs:build_contextual_prompt (lines 178-235) ---[0m
    fn build_contextual_prompt(
        &self,
        task: &Task,
        history: &VecDeque<ConversationMessage>,
    ) -> String {
        let mut prompt = String::new();

        // Add recent relevant context (last 3 messages)
        let recent_context: Vec<_> = history
            .iter()
            .rev()
            .take(3)
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .collect();

        if !recent_context.is_empty() {
            prompt.push_str("## Recent Context:\n");
            for msg in recent_context.iter().rev() {
                match msg.message_type {
                    MessageType::TaskPrompt => {
                        prompt.push_str(&format!(
                            "Previous Task: {}\n",
                            msg.content.lines().next().unwrap_or("")
                        ));
                    }
                    MessageType::Response => {
                        let summary = msg
                            .content
                            .lines()
                            .next()
                            .unwrap_or("")
                            .chars()
                            .take(100)
                            .collect::<String>();
                        prompt.push_str(&format!("Previous Result: {}...\n", summary));
                    }
                    _ => {}
                }
            }
            prompt.push('\n');
        }

        // Add current task
        prompt.push_str(&format!(
            "## Current Task:\n**{}**\n\n{}\n\nPriority: {:?}\nType: {:?}\n",
            task.description,
            task.details.as_deref().unwrap_or(""),
            task.priority,
            task.task_type
        ));

        prompt
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:synthesize_results (lines 903-956) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Persistent agent orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Persistent agent orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let session_id = {
            let session = self.session.lock().await;
            session.session_id().to_string()
        };

        let output = serde_json::json!({
            "orchestration_type": "persistent",
            "task_id": task.id,
            "session_id": session_id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "conversation_history_length": self.conversation_history.lock().await.len(),
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

  ./crates/ccswarm/src/agent/persistent.rs:404-476 method execute_task <-> ./crates/ccswarm/src/agent/persistent.rs:479-533 method execute_task_batch
  Similarity: 77.50%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_task (lines 404-476) ---[0m
    pub async fn execute_task(&mut self, task: Task) -> Result<TaskResult> {
        self.status = AgentStatus::Working;
        self.current_task = Some(task.clone());
        self.last_activity = Utc::now();

        // Ensure identity is established
        self.establish_identity_once().await?;

        let start_time = Instant::now();
        let mut identity_monitor = IdentityMonitor::new(&self.identity.agent_id);

        // Execute task with conversation context
        let history = self.conversation_history.lock().await;
        let history_clone = history.clone();
        drop(history); // Release lock

        let mut session = self.session.lock().await;
        let response = session.execute_with_context(&task, &history_clone).await?;
        drop(session); // Release lock

        // Monitor identity in response
        let identity_status = identity_monitor.monitor_response(&response).await?;
        match identity_status {
            IdentityStatus::Healthy => {
                tracing::debug!("Identity maintained during task execution");
            }
            IdentityStatus::DriftDetected(msg) => {
                tracing::warn!("Identity drift detected: {}", msg);
                self.send_identity_reminder().await?;
            }
            IdentityStatus::BoundaryViolation(msg) => {
                return Err(anyhow::anyhow!("Boundary violation detected: {}", msg));
            }
            IdentityStatus::CriticalFailure(msg) => {
                return Err(anyhow::anyhow!("Critical identity failure: {}", msg));
            }
        }

        // Record conversation
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::TaskPrompt,
            content: format!("Task: {}", task.description),
            task_id: Some(task.id.clone()),
        });
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::Response,
            content: response.clone(),
            task_id: Some(task.id.clone()),
        });
        self.trim_history(&mut history);

        let result = TaskResult {
            success: true,
            output: serde_json::json!({
                "response": response,
                "agent": self.identity.agent_id,
                "task_id": task.id,
                "session_id": self.session.lock().await.session_id(),
            }),
            error: None,
            duration: start_time.elapsed(),
        };

        // Update status and history
        self.status = AgentStatus::WaitingForReview;
        self.current_task = None;
        self.task_history.push((task, result.clone()));

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_task_batch (lines 479-533) ---[0m
    pub async fn execute_task_batch(&mut self, tasks: Vec<Task>) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks for agent: {}",
            tasks.len(),
            self.identity.agent_id
        );

        // Ensure identity is established once for the entire batch
        self.establish_identity_once().await?;

        // Record batch start
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchStart,
            content: format!("Starting batch of {} tasks", tasks.len()),
            task_id: None,
        });
        drop(history);

        let mut results = Vec::new();

        for (i, task) in tasks.into_iter().enumerate() {
            tracing::debug!(
                "Executing batch task {}/{} for agent: {}",
                i + 1,
                results.capacity(),
                self.identity.agent_id
            );

            // For batch processing, we can be more efficient by reusing context
            let result = self.execute_task(task).await?;
            results.push(result);
        }

        // Record batch end
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchEnd,
            content: format!("Completed batch of {} tasks", results.len()),
            task_id: None,
        });
        self.trim_history(&mut history);

        tracing::info!(
            "Batch execution completed for agent: {}",
            self.identity.agent_id
        );
        Ok(results)
    }

  ./crates/ccswarm/src/agent/persistent.rs:314-365 method establish_identity_once <-> ./crates/ccswarm/src/agent/persistent.rs:479-533 method execute_task_batch
  Similarity: 82.45%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:establish_identity_once (lines 314-365) ---[0m
    pub async fn establish_identity_once(&self) -> Result<()> {
        if self.identity_established.load(Ordering::Relaxed) {
            tracing::debug!(
                "Identity already established for agent: {}",
                self.identity.agent_id
            );
            return Ok(());
        }

        tracing::info!(
            "Establishing identity for agent: {}",
            self.identity.agent_id
        );

        // Generate lightweight identity prompt (200 tokens vs 2000+ in original)
        let compact_prompt = self.generate_compact_identity_prompt();

        // Send identity establishment message
        let mut session = self.session.lock().await;
        let response = session.send_message(&compact_prompt).await?;

        // Verify response contains identity markers
        if !self.verify_identity_response(&response) {
            return Err(anyhow::anyhow!("Failed to establish agent identity"));
        }

        // Record in conversation history
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityEstablishment,
            content: compact_prompt,
            task_id: None,
        });
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::Response,
            content: response,
            task_id: None,
        });

        // Trim history if needed
        self.trim_history(&mut history);

        self.identity_established.store(true, Ordering::Relaxed);
        tracing::info!(
            "Identity established successfully for agent: {}",
            self.identity.agent_id
        );

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_task_batch (lines 479-533) ---[0m
    pub async fn execute_task_batch(&mut self, tasks: Vec<Task>) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks for agent: {}",
            tasks.len(),
            self.identity.agent_id
        );

        // Ensure identity is established once for the entire batch
        self.establish_identity_once().await?;

        // Record batch start
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchStart,
            content: format!("Starting batch of {} tasks", tasks.len()),
            task_id: None,
        });
        drop(history);

        let mut results = Vec::new();

        for (i, task) in tasks.into_iter().enumerate() {
            tracing::debug!(
                "Executing batch task {}/{} for agent: {}",
                i + 1,
                results.capacity(),
                self.identity.agent_id
            );

            // For batch processing, we can be more efficient by reusing context
            let result = self.execute_task(task).await?;
            results.push(result);
        }

        // Record batch end
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchEnd,
            content: format!("Completed batch of {} tasks", results.len()),
            task_id: None,
        });
        self.trim_history(&mut history);

        tracing::info!(
            "Batch execution completed for agent: {}",
            self.identity.agent_id
        );
        Ok(results)
    }

  ./crates/ccswarm/src/agent/persistent.rs:178-235 method build_contextual_prompt <-> ./crates/ccswarm/src/agent/persistent.rs:961-995 method execute_context_analysis_step
  Similarity: 94.83%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:build_contextual_prompt (lines 178-235) ---[0m
    fn build_contextual_prompt(
        &self,
        task: &Task,
        history: &VecDeque<ConversationMessage>,
    ) -> String {
        let mut prompt = String::new();

        // Add recent relevant context (last 3 messages)
        let recent_context: Vec<_> = history
            .iter()
            .rev()
            .take(3)
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .collect();

        if !recent_context.is_empty() {
            prompt.push_str("## Recent Context:\n");
            for msg in recent_context.iter().rev() {
                match msg.message_type {
                    MessageType::TaskPrompt => {
                        prompt.push_str(&format!(
                            "Previous Task: {}\n",
                            msg.content.lines().next().unwrap_or("")
                        ));
                    }
                    MessageType::Response => {
                        let summary = msg
                            .content
                            .lines()
                            .next()
                            .unwrap_or("")
                            .chars()
                            .take(100)
                            .collect::<String>();
                        prompt.push_str(&format!("Previous Result: {}...\n", summary));
                    }
                    _ => {}
                }
            }
            prompt.push('\n');
        }

        // Add current task
        prompt.push_str(&format!(
            "## Current Task:\n**{}**\n\n{}\n\nPriority: {:?}\nType: {:?}\n",
            task.description,
            task.details.as_deref().unwrap_or(""),
            task.priority,
            task.task_type
        ));

        prompt
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_context_analysis_step (lines 961-995) ---[0m
    async fn execute_context_analysis_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Analyze conversation history for relevant context
        let history = self.conversation_history.lock().await;
        let relevant_messages = history
            .iter()
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .count();

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Context analysis for task {} completed", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "history_messages".to_string(),
                relevant_messages.to_string(),
            ))
    }

  ./crates/ccswarm/src/agent/persistent.rs:88-133 method send_message <-> ./crates/ccswarm/src/agent/persistent.rs:404-476 method execute_task
  Similarity: 73.64%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_message (lines 88-133) ---[0m
    pub async fn send_message(&mut self, prompt: &str) -> Result<String> {
        self.last_activity = Instant::now();

        // For tests and simulation mode, return a mock response
        if !self.claude_config.use_real_api {
            return Ok(self.generate_mock_response(prompt));
        }

        let mut cmd = Command::new("claude");
        cmd.current_dir(&self.working_dir);

        // Add environment variables
        for (key, value) in &self.env_vars {
            cmd.env(key, value);
        }

        // Add Claude arguments
        cmd.arg("-p").arg(prompt);

        if self.claude_config.json_output {
            cmd.arg("--json");
        }

        if self.claude_config.dangerous_skip {
            cmd.arg("--dangerously-skip-permissions");
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.claude_config.think_mode {
            cmd.arg("--think").arg(think_mode.to_string());
        }

        let output = cmd
            .output()
            .await
            .context("Failed to execute Claude Code")?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).to_string())
        } else {
            Err(anyhow::anyhow!(
                "Claude Code execution failed: {}",
                String::from_utf8_lossy(&output.stderr)
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_task (lines 404-476) ---[0m
    pub async fn execute_task(&mut self, task: Task) -> Result<TaskResult> {
        self.status = AgentStatus::Working;
        self.current_task = Some(task.clone());
        self.last_activity = Utc::now();

        // Ensure identity is established
        self.establish_identity_once().await?;

        let start_time = Instant::now();
        let mut identity_monitor = IdentityMonitor::new(&self.identity.agent_id);

        // Execute task with conversation context
        let history = self.conversation_history.lock().await;
        let history_clone = history.clone();
        drop(history); // Release lock

        let mut session = self.session.lock().await;
        let response = session.execute_with_context(&task, &history_clone).await?;
        drop(session); // Release lock

        // Monitor identity in response
        let identity_status = identity_monitor.monitor_response(&response).await?;
        match identity_status {
            IdentityStatus::Healthy => {
                tracing::debug!("Identity maintained during task execution");
            }
            IdentityStatus::DriftDetected(msg) => {
                tracing::warn!("Identity drift detected: {}", msg);
                self.send_identity_reminder().await?;
            }
            IdentityStatus::BoundaryViolation(msg) => {
                return Err(anyhow::anyhow!("Boundary violation detected: {}", msg));
            }
            IdentityStatus::CriticalFailure(msg) => {
                return Err(anyhow::anyhow!("Critical identity failure: {}", msg));
            }
        }

        // Record conversation
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::TaskPrompt,
            content: format!("Task: {}", task.description),
            task_id: Some(task.id.clone()),
        });
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::Response,
            content: response.clone(),
            task_id: Some(task.id.clone()),
        });
        self.trim_history(&mut history);

        let result = TaskResult {
            success: true,
            output: serde_json::json!({
                "response": response,
                "agent": self.identity.agent_id,
                "task_id": task.id,
                "session_id": self.session.lock().await.session_id(),
            }),
            error: None,
            duration: start_time.elapsed(),
        };

        // Update status and history
        self.status = AgentStatus::WaitingForReview;
        self.current_task = None;
        self.task_history.push((task, result.clone()));

        Ok(result)
    }

  ./crates/ccswarm/src/agent/persistent.rs:178-235 method build_contextual_prompt <-> ./crates/ccswarm/src/agent/persistent.rs:611-655 method orchestrate_task
  Similarity: 82.61%
  Classes: ClaudeCodeSession <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/persistent.rs:build_contextual_prompt (lines 178-235) ---[0m
    fn build_contextual_prompt(
        &self,
        task: &Task,
        history: &VecDeque<ConversationMessage>,
    ) -> String {
        let mut prompt = String::new();

        // Add recent relevant context (last 3 messages)
        let recent_context: Vec<_> = history
            .iter()
            .rev()
            .take(3)
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .collect();

        if !recent_context.is_empty() {
            prompt.push_str("## Recent Context:\n");
            for msg in recent_context.iter().rev() {
                match msg.message_type {
                    MessageType::TaskPrompt => {
                        prompt.push_str(&format!(
                            "Previous Task: {}\n",
                            msg.content.lines().next().unwrap_or("")
                        ));
                    }
                    MessageType::Response => {
                        let summary = msg
                            .content
                            .lines()
                            .next()
                            .unwrap_or("")
                            .chars()
                            .take(100)
                            .collect::<String>();
                        prompt.push_str(&format!("Previous Result: {}...\n", summary));
                    }
                    _ => {}
                }
            }
            prompt.push('\n');
        }

        // Add current task
        prompt.push_str(&format!(
            "## Current Task:\n**{}**\n\n{}\n\nPriority: {:?}\nType: {:?}\n",
            task.description,
            task.details.as_deref().unwrap_or(""),
            task.priority,
            task.task_type
        ));

        prompt
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:orchestrate_task (lines 611-655) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!(
            "🎭 Persistent agent orchestrating task: {}",
            task.description
        );

        // Leverage persistent session for complex task orchestration
        let plan = self.analyze_task(task).await?;

        // Execute plan with session persistence for context continuity
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("persistent_mode".to_string(), "true".to_string());
        context.insert("session_id".to_string(), {
            let session = self.session.lock().await;
            session.session_id().to_string()
        });

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results and preserve in session history
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Persistent agent step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Persistent orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({
                "orchestration": "persistent_success",
                "steps_completed": plan.steps.len(),
                "session_id": context.get("session_id")
            }),
            Duration::from_secs(15),
        ))
    }

  ./crates/ccswarm/src/agent/persistent.rs:811-865 method review_and_adapt <-> ./crates/ccswarm/src/agent/persistent.rs:961-995 method execute_context_analysis_step
  Similarity: 92.60%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:review_and_adapt (lines 811-865) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Persistent agent reviewing and adapting plan based on {} results",
            results.len()
        );

        // Leverage conversation history for intelligent adaptation
        let history = self.conversation_history.lock().await;
        let failed_steps: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_steps.is_empty() {
            warn!(
                "Persistent agent detected {} failed steps",
                failed_steps.len()
            );

            // Analyze failure patterns from session history
            let failure_analysis = self.analyze_failure_patterns(&history, &failed_steps);

            // Add adaptive recovery steps based on session context
            if failure_analysis.contains("context_loss") {
                let recovery_step = TaskStep::new(
                    "context_recovery".to_string(),
                    "Recover session context".to_string(),
                    StepType::Execution,
                )
                .with_description("Recover lost session context".to_string());
                current_plan.steps.push(recovery_step);
            }

            if failure_analysis.contains("identity_drift") {
                let identity_step = TaskStep::new(
                    "identity_reinforcement".to_string(),
                    "Reinforce agent identity".to_string(),
                    StepType::Execution,
                )
                .with_description("Reinforce agent identity and boundaries".to_string());
                current_plan.steps.push(identity_step);
            }

            let validation_step = TaskStep::new(
                "adaptive_validation".to_string(),
                "Validate adaptive recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Validate recovery steps".to_string());
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_context_analysis_step (lines 961-995) ---[0m
    async fn execute_context_analysis_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Analyze conversation history for relevant context
        let history = self.conversation_history.lock().await;
        let relevant_messages = history
            .iter()
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .count();

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Context analysis for task {} completed", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "history_messages".to_string(),
                relevant_messages.to_string(),
            ))
    }

  ./crates/ccswarm/src/agent/persistent.rs:178-235 method build_contextual_prompt <-> ./crates/ccswarm/src/agent/persistent.rs:867-901 method execute_parallel_task
  Similarity: 89.43%
  Classes: ClaudeCodeSession <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/persistent.rs:build_contextual_prompt (lines 178-235) ---[0m
    fn build_contextual_prompt(
        &self,
        task: &Task,
        history: &VecDeque<ConversationMessage>,
    ) -> String {
        let mut prompt = String::new();

        // Add recent relevant context (last 3 messages)
        let recent_context: Vec<_> = history
            .iter()
            .rev()
            .take(3)
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .collect();

        if !recent_context.is_empty() {
            prompt.push_str("## Recent Context:\n");
            for msg in recent_context.iter().rev() {
                match msg.message_type {
                    MessageType::TaskPrompt => {
                        prompt.push_str(&format!(
                            "Previous Task: {}\n",
                            msg.content.lines().next().unwrap_or("")
                        ));
                    }
                    MessageType::Response => {
                        let summary = msg
                            .content
                            .lines()
                            .next()
                            .unwrap_or("")
                            .chars()
                            .take(100)
                            .collect::<String>();
                        prompt.push_str(&format!("Previous Result: {}...\n", summary));
                    }
                    _ => {}
                }
            }
            prompt.push('\n');
        }

        // Add current task
        prompt.push_str(&format!(
            "## Current Task:\n**{}**\n\n{}\n\nPriority: {:?}\nType: {:?}\n",
            task.description,
            task.details.as_deref().unwrap_or(""),
            task.priority,
            task.task_type
        ));

        prompt
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_parallel_task (lines 867-901) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task with session context
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use conversation history for context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session
            .execute_with_context(&execution_task, &history)
            .await?;
        drop(session);
        drop(history);

        let success = !response.contains("error") && !response.contains("failed");

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success,
            output: response,
            error: if success {
                None
            } else {
                Some("Task execution failed".to_string())
            },
        })
    }

  ./crates/ccswarm/src/agent/persistent.rs:178-235 method build_contextual_prompt <-> ./crates/ccswarm/src/agent/persistent.rs:1145-1176 method analyze_failure_patterns
  Similarity: 91.85%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:build_contextual_prompt (lines 178-235) ---[0m
    fn build_contextual_prompt(
        &self,
        task: &Task,
        history: &VecDeque<ConversationMessage>,
    ) -> String {
        let mut prompt = String::new();

        // Add recent relevant context (last 3 messages)
        let recent_context: Vec<_> = history
            .iter()
            .rev()
            .take(3)
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .collect();

        if !recent_context.is_empty() {
            prompt.push_str("## Recent Context:\n");
            for msg in recent_context.iter().rev() {
                match msg.message_type {
                    MessageType::TaskPrompt => {
                        prompt.push_str(&format!(
                            "Previous Task: {}\n",
                            msg.content.lines().next().unwrap_or("")
                        ));
                    }
                    MessageType::Response => {
                        let summary = msg
                            .content
                            .lines()
                            .next()
                            .unwrap_or("")
                            .chars()
                            .take(100)
                            .collect::<String>();
                        prompt.push_str(&format!("Previous Result: {}...\n", summary));
                    }
                    _ => {}
                }
            }
            prompt.push('\n');
        }

        // Add current task
        prompt.push_str(&format!(
            "## Current Task:\n**{}**\n\n{}\n\nPriority: {:?}\nType: {:?}\n",
            task.description,
            task.details.as_deref().unwrap_or(""),
            task.priority,
            task.task_type
        ));

        prompt
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:analyze_failure_patterns (lines 1145-1176) ---[0m
    fn analyze_failure_patterns(
        &self,
        history: &VecDeque<ConversationMessage>,
        failed_steps: &[&StepResult],
    ) -> String {
        let mut patterns = Vec::new();

        // Check for context loss patterns
        if failed_steps
            .iter()
            .any(|step| step.errors.iter().any(|e| e.contains("context")))
        {
            patterns.push("context_loss");
        }

        // Check for identity drift in recent messages
        let recent_responses: Vec<_> = history
            .iter()
            .rev()
            .take(5)
            .filter(|msg| matches!(msg.message_type, MessageType::Response))
            .collect();

        if recent_responses
            .iter()
            .any(|msg| !msg.content.contains("🤖 AGENT:"))
        {
            patterns.push("identity_drift");
        }

        patterns.join(",")
    }

  ./crates/ccswarm/src/agent/persistent.rs:903-956 method synthesize_results <-> ./crates/ccswarm/src/agent/persistent.rs:961-995 method execute_context_analysis_step
  Similarity: 91.63%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:synthesize_results (lines 903-956) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Persistent agent orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Persistent agent orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let session_id = {
            let session = self.session.lock().await;
            session.session_id().to_string()
        };

        let output = serde_json::json!({
            "orchestration_type": "persistent",
            "task_id": task.id,
            "session_id": session_id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "conversation_history_length": self.conversation_history.lock().await.len(),
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_context_analysis_step (lines 961-995) ---[0m
    async fn execute_context_analysis_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Analyze conversation history for relevant context
        let history = self.conversation_history.lock().await;
        let relevant_messages = history
            .iter()
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .count();

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Context analysis for task {} completed", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "history_messages".to_string(),
                relevant_messages.to_string(),
            ))
    }

  ./crates/ccswarm/src/agent/persistent.rs:178-235 method build_contextual_prompt <-> ./crates/ccswarm/src/agent/persistent.rs:1113-1142 method execute_persistent_default_step
  Similarity: 91.67%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:build_contextual_prompt (lines 178-235) ---[0m
    fn build_contextual_prompt(
        &self,
        task: &Task,
        history: &VecDeque<ConversationMessage>,
    ) -> String {
        let mut prompt = String::new();

        // Add recent relevant context (last 3 messages)
        let recent_context: Vec<_> = history
            .iter()
            .rev()
            .take(3)
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .collect();

        if !recent_context.is_empty() {
            prompt.push_str("## Recent Context:\n");
            for msg in recent_context.iter().rev() {
                match msg.message_type {
                    MessageType::TaskPrompt => {
                        prompt.push_str(&format!(
                            "Previous Task: {}\n",
                            msg.content.lines().next().unwrap_or("")
                        ));
                    }
                    MessageType::Response => {
                        let summary = msg
                            .content
                            .lines()
                            .next()
                            .unwrap_or("")
                            .chars()
                            .take(100)
                            .collect::<String>();
                        prompt.push_str(&format!("Previous Result: {}...\n", summary));
                    }
                    _ => {}
                }
            }
            prompt.push('\n');
        }

        // Add current task
        prompt.push_str(&format!(
            "## Current Task:\n**{}**\n\n{}\n\nPriority: {:?}\nType: {:?}\n",
            task.description,
            task.details.as_deref().unwrap_or(""),
            task.priority,
            task.task_type
        ));

        prompt
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_default_step (lines 1113-1142) ---[0m
    async fn execute_persistent_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("persistent_default_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Execute with full session context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent default step completed".to_string())
            .add_output("response".to_string(), response)
            .add_output("session_context_preserved".to_string(), "true".to_string())
            .add_output("task_id".to_string(), task_id))
    }

  ./crates/ccswarm/src/agent/persistent.rs:867-901 method execute_parallel_task <-> ./crates/ccswarm/src/agent/persistent.rs:903-956 method synthesize_results
  Similarity: 89.67%
  Classes: AgentOrchestrator <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_parallel_task (lines 867-901) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task with session context
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use conversation history for context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session
            .execute_with_context(&execution_task, &history)
            .await?;
        drop(session);
        drop(history);

        let success = !response.contains("error") && !response.contains("failed");

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success,
            output: response,
            error: if success {
                None
            } else {
                Some("Task execution failed".to_string())
            },
        })
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:synthesize_results (lines 903-956) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Persistent agent orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Persistent agent orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let session_id = {
            let session = self.session.lock().await;
            session.session_id().to_string()
        };

        let output = serde_json::json!({
            "orchestration_type": "persistent",
            "task_id": task.id,
            "session_id": session_id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "conversation_history_length": self.conversation_history.lock().await.len(),
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

  ./crates/ccswarm/src/agent/persistent.rs:88-133 method send_message <-> ./crates/ccswarm/src/agent/persistent.rs:479-533 method execute_task_batch
  Similarity: 78.61%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_message (lines 88-133) ---[0m
    pub async fn send_message(&mut self, prompt: &str) -> Result<String> {
        self.last_activity = Instant::now();

        // For tests and simulation mode, return a mock response
        if !self.claude_config.use_real_api {
            return Ok(self.generate_mock_response(prompt));
        }

        let mut cmd = Command::new("claude");
        cmd.current_dir(&self.working_dir);

        // Add environment variables
        for (key, value) in &self.env_vars {
            cmd.env(key, value);
        }

        // Add Claude arguments
        cmd.arg("-p").arg(prompt);

        if self.claude_config.json_output {
            cmd.arg("--json");
        }

        if self.claude_config.dangerous_skip {
            cmd.arg("--dangerously-skip-permissions");
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.claude_config.think_mode {
            cmd.arg("--think").arg(think_mode.to_string());
        }

        let output = cmd
            .output()
            .await
            .context("Failed to execute Claude Code")?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).to_string())
        } else {
            Err(anyhow::anyhow!(
                "Claude Code execution failed: {}",
                String::from_utf8_lossy(&output.stderr)
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_task_batch (lines 479-533) ---[0m
    pub async fn execute_task_batch(&mut self, tasks: Vec<Task>) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks for agent: {}",
            tasks.len(),
            self.identity.agent_id
        );

        // Ensure identity is established once for the entire batch
        self.establish_identity_once().await?;

        // Record batch start
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchStart,
            content: format!("Starting batch of {} tasks", tasks.len()),
            task_id: None,
        });
        drop(history);

        let mut results = Vec::new();

        for (i, task) in tasks.into_iter().enumerate() {
            tracing::debug!(
                "Executing batch task {}/{} for agent: {}",
                i + 1,
                results.capacity(),
                self.identity.agent_id
            );

            // For batch processing, we can be more efficient by reusing context
            let result = self.execute_task(task).await?;
            results.push(result);
        }

        // Record batch end
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchEnd,
            content: format!("Completed batch of {} tasks", results.len()),
            task_id: None,
        });
        self.trim_history(&mut history);

        tracing::info!(
            "Batch execution completed for agent: {}",
            self.identity.agent_id
        );
        Ok(results)
    }

  ./crates/ccswarm/src/agent/persistent.rs:178-235 method build_contextual_prompt <-> ./crates/ccswarm/src/agent/persistent.rs:1081-1110 method execute_iterative_debugging_step
  Similarity: 89.43%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:build_contextual_prompt (lines 178-235) ---[0m
    fn build_contextual_prompt(
        &self,
        task: &Task,
        history: &VecDeque<ConversationMessage>,
    ) -> String {
        let mut prompt = String::new();

        // Add recent relevant context (last 3 messages)
        let recent_context: Vec<_> = history
            .iter()
            .rev()
            .take(3)
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .collect();

        if !recent_context.is_empty() {
            prompt.push_str("## Recent Context:\n");
            for msg in recent_context.iter().rev() {
                match msg.message_type {
                    MessageType::TaskPrompt => {
                        prompt.push_str(&format!(
                            "Previous Task: {}\n",
                            msg.content.lines().next().unwrap_or("")
                        ));
                    }
                    MessageType::Response => {
                        let summary = msg
                            .content
                            .lines()
                            .next()
                            .unwrap_or("")
                            .chars()
                            .take(100)
                            .collect::<String>();
                        prompt.push_str(&format!("Previous Result: {}...\n", summary));
                    }
                    _ => {}
                }
            }
            prompt.push('\n');
        }

        // Add current task
        prompt.push_str(&format!(
            "## Current Task:\n**{}**\n\n{}\n\nPriority: {:?}\nType: {:?}\n",
            task.description,
            task.details.as_deref().unwrap_or(""),
            task.priority,
            task.task_type
        ));

        prompt
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_debugging_step (lines 1081-1110) ---[0m
    async fn execute_iterative_debugging_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_debug_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::High,
            TaskType::Bugfix,
        );

        // Use conversation history for debugging context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Analyze debugging progress
        let debug_progress = response.contains("identified")
            || response.contains("fixed")
            || response.contains("resolved");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative debugging step completed".to_string())
            .add_output("debug_response".to_string(), response)
            .add_output("progress_made".to_string(), debug_progress.to_string())
            .add_output("context_leveraged".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:811-865 method review_and_adapt <-> ./crates/ccswarm/src/agent/persistent.rs:903-956 method synthesize_results
  Similarity: 72.20%
  Classes: AgentOrchestrator <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/persistent.rs:review_and_adapt (lines 811-865) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Persistent agent reviewing and adapting plan based on {} results",
            results.len()
        );

        // Leverage conversation history for intelligent adaptation
        let history = self.conversation_history.lock().await;
        let failed_steps: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_steps.is_empty() {
            warn!(
                "Persistent agent detected {} failed steps",
                failed_steps.len()
            );

            // Analyze failure patterns from session history
            let failure_analysis = self.analyze_failure_patterns(&history, &failed_steps);

            // Add adaptive recovery steps based on session context
            if failure_analysis.contains("context_loss") {
                let recovery_step = TaskStep::new(
                    "context_recovery".to_string(),
                    "Recover session context".to_string(),
                    StepType::Execution,
                )
                .with_description("Recover lost session context".to_string());
                current_plan.steps.push(recovery_step);
            }

            if failure_analysis.contains("identity_drift") {
                let identity_step = TaskStep::new(
                    "identity_reinforcement".to_string(),
                    "Reinforce agent identity".to_string(),
                    StepType::Execution,
                )
                .with_description("Reinforce agent identity and boundaries".to_string());
                current_plan.steps.push(identity_step);
            }

            let validation_step = TaskStep::new(
                "adaptive_validation".to_string(),
                "Validate adaptive recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Validate recovery steps".to_string());
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:synthesize_results (lines 903-956) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Persistent agent orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Persistent agent orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let session_id = {
            let session = self.session.lock().await;
            session.session_id().to_string()
        };

        let output = serde_json::json!({
            "orchestration_type": "persistent",
            "task_id": task.id,
            "session_id": session_id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "conversation_history_length": self.conversation_history.lock().await.len(),
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

  ./crates/ccswarm/src/agent/persistent.rs:178-235 method build_contextual_prompt <-> ./crates/ccswarm/src/agent/persistent.rs:1024-1048 method execute_session_validation_step
  Similarity: 94.50%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:build_contextual_prompt (lines 178-235) ---[0m
    fn build_contextual_prompt(
        &self,
        task: &Task,
        history: &VecDeque<ConversationMessage>,
    ) -> String {
        let mut prompt = String::new();

        // Add recent relevant context (last 3 messages)
        let recent_context: Vec<_> = history
            .iter()
            .rev()
            .take(3)
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .collect();

        if !recent_context.is_empty() {
            prompt.push_str("## Recent Context:\n");
            for msg in recent_context.iter().rev() {
                match msg.message_type {
                    MessageType::TaskPrompt => {
                        prompt.push_str(&format!(
                            "Previous Task: {}\n",
                            msg.content.lines().next().unwrap_or("")
                        ));
                    }
                    MessageType::Response => {
                        let summary = msg
                            .content
                            .lines()
                            .next()
                            .unwrap_or("")
                            .chars()
                            .take(100)
                            .collect::<String>();
                        prompt.push_str(&format!("Previous Result: {}...\n", summary));
                    }
                    _ => {}
                }
            }
            prompt.push('\n');
        }

        // Add current task
        prompt.push_str(&format!(
            "## Current Task:\n**{}**\n\n{}\n\nPriority: {:?}\nType: {:?}\n",
            task.description,
            task.details.as_deref().unwrap_or(""),
            task.priority,
            task.task_type
        ));

        prompt
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_session_validation_step (lines 1024-1048) ---[0m
    async fn execute_session_validation_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Validate session state and identity
        let identity_established = self.identity_established.load(Ordering::Relaxed);
        let history = self.conversation_history.lock().await;
        let session_health = !history.is_empty() && identity_established;

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Session validation completed for {}", session_id))
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "identity_established".to_string(),
                identity_established.to_string(),
            )
            .add_output("session_healthy".to_string(), session_health.to_string())
            .add_output("conversation_length".to_string(), history.len().to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:903-956 method synthesize_results <-> ./crates/ccswarm/src/agent/persistent.rs:1145-1176 method analyze_failure_patterns
  Similarity: 90.81%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:synthesize_results (lines 903-956) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Persistent agent orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Persistent agent orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let session_id = {
            let session = self.session.lock().await;
            session.session_id().to_string()
        };

        let output = serde_json::json!({
            "orchestration_type": "persistent",
            "task_id": task.id,
            "session_id": session_id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "conversation_history_length": self.conversation_history.lock().await.len(),
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:analyze_failure_patterns (lines 1145-1176) ---[0m
    fn analyze_failure_patterns(
        &self,
        history: &VecDeque<ConversationMessage>,
        failed_steps: &[&StepResult],
    ) -> String {
        let mut patterns = Vec::new();

        // Check for context loss patterns
        if failed_steps
            .iter()
            .any(|step| step.errors.iter().any(|e| e.contains("context")))
        {
            patterns.push("context_loss");
        }

        // Check for identity drift in recent messages
        let recent_responses: Vec<_> = history
            .iter()
            .rev()
            .take(5)
            .filter(|msg| matches!(msg.message_type, MessageType::Response))
            .collect();

        if recent_responses
            .iter()
            .any(|msg| !msg.content.contains("🤖 AGENT:"))
        {
            patterns.push("identity_drift");
        }

        patterns.join(",")
    }

  ./crates/ccswarm/src/agent/persistent.rs:811-865 method review_and_adapt <-> ./crates/ccswarm/src/agent/persistent.rs:1145-1176 method analyze_failure_patterns
  Similarity: 89.54%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:review_and_adapt (lines 811-865) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Persistent agent reviewing and adapting plan based on {} results",
            results.len()
        );

        // Leverage conversation history for intelligent adaptation
        let history = self.conversation_history.lock().await;
        let failed_steps: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_steps.is_empty() {
            warn!(
                "Persistent agent detected {} failed steps",
                failed_steps.len()
            );

            // Analyze failure patterns from session history
            let failure_analysis = self.analyze_failure_patterns(&history, &failed_steps);

            // Add adaptive recovery steps based on session context
            if failure_analysis.contains("context_loss") {
                let recovery_step = TaskStep::new(
                    "context_recovery".to_string(),
                    "Recover session context".to_string(),
                    StepType::Execution,
                )
                .with_description("Recover lost session context".to_string());
                current_plan.steps.push(recovery_step);
            }

            if failure_analysis.contains("identity_drift") {
                let identity_step = TaskStep::new(
                    "identity_reinforcement".to_string(),
                    "Reinforce agent identity".to_string(),
                    StepType::Execution,
                )
                .with_description("Reinforce agent identity and boundaries".to_string());
                current_plan.steps.push(identity_step);
            }

            let validation_step = TaskStep::new(
                "adaptive_validation".to_string(),
                "Validate adaptive recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Validate recovery steps".to_string());
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:analyze_failure_patterns (lines 1145-1176) ---[0m
    fn analyze_failure_patterns(
        &self,
        history: &VecDeque<ConversationMessage>,
        failed_steps: &[&StepResult],
    ) -> String {
        let mut patterns = Vec::new();

        // Check for context loss patterns
        if failed_steps
            .iter()
            .any(|step| step.errors.iter().any(|e| e.contains("context")))
        {
            patterns.push("context_loss");
        }

        // Check for identity drift in recent messages
        let recent_responses: Vec<_> = history
            .iter()
            .rev()
            .take(5)
            .filter(|msg| matches!(msg.message_type, MessageType::Response))
            .collect();

        if recent_responses
            .iter()
            .any(|msg| !msg.content.contains("🤖 AGENT:"))
        {
            patterns.push("identity_drift");
        }

        patterns.join(",")
    }

  ./crates/ccswarm/src/agent/persistent.rs:811-865 method review_and_adapt <-> ./crates/ccswarm/src/agent/persistent.rs:867-901 method execute_parallel_task
  Similarity: 86.12%
  Classes: AgentOrchestrator <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/persistent.rs:review_and_adapt (lines 811-865) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Persistent agent reviewing and adapting plan based on {} results",
            results.len()
        );

        // Leverage conversation history for intelligent adaptation
        let history = self.conversation_history.lock().await;
        let failed_steps: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_steps.is_empty() {
            warn!(
                "Persistent agent detected {} failed steps",
                failed_steps.len()
            );

            // Analyze failure patterns from session history
            let failure_analysis = self.analyze_failure_patterns(&history, &failed_steps);

            // Add adaptive recovery steps based on session context
            if failure_analysis.contains("context_loss") {
                let recovery_step = TaskStep::new(
                    "context_recovery".to_string(),
                    "Recover session context".to_string(),
                    StepType::Execution,
                )
                .with_description("Recover lost session context".to_string());
                current_plan.steps.push(recovery_step);
            }

            if failure_analysis.contains("identity_drift") {
                let identity_step = TaskStep::new(
                    "identity_reinforcement".to_string(),
                    "Reinforce agent identity".to_string(),
                    StepType::Execution,
                )
                .with_description("Reinforce agent identity and boundaries".to_string());
                current_plan.steps.push(identity_step);
            }

            let validation_step = TaskStep::new(
                "adaptive_validation".to_string(),
                "Validate adaptive recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Validate recovery steps".to_string());
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_parallel_task (lines 867-901) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task with session context
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use conversation history for context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session
            .execute_with_context(&execution_task, &history)
            .await?;
        drop(session);
        drop(history);

        let success = !response.contains("error") && !response.contains("failed");

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success,
            output: response,
            error: if success {
                None
            } else {
                Some("Task execution failed".to_string())
            },
        })
    }

  ./crates/ccswarm/src/agent/persistent.rs:178-235 method build_contextual_prompt <-> ./crates/ccswarm/src/agent/persistent.rs:1051-1078 method execute_persistent_test_step
  Similarity: 89.43%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:build_contextual_prompt (lines 178-235) ---[0m
    fn build_contextual_prompt(
        &self,
        task: &Task,
        history: &VecDeque<ConversationMessage>,
    ) -> String {
        let mut prompt = String::new();

        // Add recent relevant context (last 3 messages)
        let recent_context: Vec<_> = history
            .iter()
            .rev()
            .take(3)
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .collect();

        if !recent_context.is_empty() {
            prompt.push_str("## Recent Context:\n");
            for msg in recent_context.iter().rev() {
                match msg.message_type {
                    MessageType::TaskPrompt => {
                        prompt.push_str(&format!(
                            "Previous Task: {}\n",
                            msg.content.lines().next().unwrap_or("")
                        ));
                    }
                    MessageType::Response => {
                        let summary = msg
                            .content
                            .lines()
                            .next()
                            .unwrap_or("")
                            .chars()
                            .take(100)
                            .collect::<String>();
                        prompt.push_str(&format!("Previous Result: {}...\n", summary));
                    }
                    _ => {}
                }
            }
            prompt.push('\n');
        }

        // Add current task
        prompt.push_str(&format!(
            "## Current Task:\n**{}**\n\n{}\n\nPriority: {:?}\nType: {:?}\n",
            task.description,
            task.details.as_deref().unwrap_or(""),
            task.priority,
            task.task_type
        ));

        prompt
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_test_step (lines 1051-1078) ---[0m
    async fn execute_persistent_test_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_test_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Testing,
        );

        // Execute tests with session persistence
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Parse test results from response
        let test_success = response.contains("✅") || response.contains("passed");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent test execution completed".to_string())
            .add_output("test_response".to_string(), response)
            .add_output("test_passed".to_string(), test_success.to_string())
            .add_output("persistent_execution".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:88-133 method send_message <-> ./crates/ccswarm/src/agent/persistent.rs:314-365 method establish_identity_once
  Similarity: 77.77%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_message (lines 88-133) ---[0m
    pub async fn send_message(&mut self, prompt: &str) -> Result<String> {
        self.last_activity = Instant::now();

        // For tests and simulation mode, return a mock response
        if !self.claude_config.use_real_api {
            return Ok(self.generate_mock_response(prompt));
        }

        let mut cmd = Command::new("claude");
        cmd.current_dir(&self.working_dir);

        // Add environment variables
        for (key, value) in &self.env_vars {
            cmd.env(key, value);
        }

        // Add Claude arguments
        cmd.arg("-p").arg(prompt);

        if self.claude_config.json_output {
            cmd.arg("--json");
        }

        if self.claude_config.dangerous_skip {
            cmd.arg("--dangerously-skip-permissions");
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.claude_config.think_mode {
            cmd.arg("--think").arg(think_mode.to_string());
        }

        let output = cmd
            .output()
            .await
            .context("Failed to execute Claude Code")?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).to_string())
        } else {
            Err(anyhow::anyhow!(
                "Claude Code execution failed: {}",
                String::from_utf8_lossy(&output.stderr)
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:establish_identity_once (lines 314-365) ---[0m
    pub async fn establish_identity_once(&self) -> Result<()> {
        if self.identity_established.load(Ordering::Relaxed) {
            tracing::debug!(
                "Identity already established for agent: {}",
                self.identity.agent_id
            );
            return Ok(());
        }

        tracing::info!(
            "Establishing identity for agent: {}",
            self.identity.agent_id
        );

        // Generate lightweight identity prompt (200 tokens vs 2000+ in original)
        let compact_prompt = self.generate_compact_identity_prompt();

        // Send identity establishment message
        let mut session = self.session.lock().await;
        let response = session.send_message(&compact_prompt).await?;

        // Verify response contains identity markers
        if !self.verify_identity_response(&response) {
            return Err(anyhow::anyhow!("Failed to establish agent identity"));
        }

        // Record in conversation history
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityEstablishment,
            content: compact_prompt,
            task_id: None,
        });
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::Response,
            content: response,
            task_id: None,
        });

        // Trim history if needed
        self.trim_history(&mut history);

        self.identity_established.store(true, Ordering::Relaxed);
        tracing::info!(
            "Identity established successfully for agent: {}",
            self.identity.agent_id
        );

        Ok(())
    }

  ./crates/ccswarm/src/agent/persistent.rs:178-235 method build_contextual_prompt <-> ./crates/ccswarm/src/agent/persistent.rs:998-1021 method execute_iterative_development_step
  Similarity: 92.85%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:build_contextual_prompt (lines 178-235) ---[0m
    fn build_contextual_prompt(
        &self,
        task: &Task,
        history: &VecDeque<ConversationMessage>,
    ) -> String {
        let mut prompt = String::new();

        // Add recent relevant context (last 3 messages)
        let recent_context: Vec<_> = history
            .iter()
            .rev()
            .take(3)
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .collect();

        if !recent_context.is_empty() {
            prompt.push_str("## Recent Context:\n");
            for msg in recent_context.iter().rev() {
                match msg.message_type {
                    MessageType::TaskPrompt => {
                        prompt.push_str(&format!(
                            "Previous Task: {}\n",
                            msg.content.lines().next().unwrap_or("")
                        ));
                    }
                    MessageType::Response => {
                        let summary = msg
                            .content
                            .lines()
                            .next()
                            .unwrap_or("")
                            .chars()
                            .take(100)
                            .collect::<String>();
                        prompt.push_str(&format!("Previous Result: {}...\n", summary));
                    }
                    _ => {}
                }
            }
            prompt.push('\n');
        }

        // Add current task
        prompt.push_str(&format!(
            "## Current Task:\n**{}**\n\n{}\n\nPriority: {:?}\nType: {:?}\n",
            task.description,
            task.details.as_deref().unwrap_or(""),
            task.priority,
            task.task_type
        ));

        prompt
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_development_step (lines 998-1021) ---[0m
    async fn execute_iterative_development_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_dev_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use session context for development
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative development step completed with session context".to_string())
            .add_output("development_response".to_string(), response)
            .add_output("session_context_used".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:811-865 method review_and_adapt <-> ./crates/ccswarm/src/agent/persistent.rs:1113-1142 method execute_persistent_default_step
  Similarity: 88.81%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:review_and_adapt (lines 811-865) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Persistent agent reviewing and adapting plan based on {} results",
            results.len()
        );

        // Leverage conversation history for intelligent adaptation
        let history = self.conversation_history.lock().await;
        let failed_steps: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_steps.is_empty() {
            warn!(
                "Persistent agent detected {} failed steps",
                failed_steps.len()
            );

            // Analyze failure patterns from session history
            let failure_analysis = self.analyze_failure_patterns(&history, &failed_steps);

            // Add adaptive recovery steps based on session context
            if failure_analysis.contains("context_loss") {
                let recovery_step = TaskStep::new(
                    "context_recovery".to_string(),
                    "Recover session context".to_string(),
                    StepType::Execution,
                )
                .with_description("Recover lost session context".to_string());
                current_plan.steps.push(recovery_step);
            }

            if failure_analysis.contains("identity_drift") {
                let identity_step = TaskStep::new(
                    "identity_reinforcement".to_string(),
                    "Reinforce agent identity".to_string(),
                    StepType::Execution,
                )
                .with_description("Reinforce agent identity and boundaries".to_string());
                current_plan.steps.push(identity_step);
            }

            let validation_step = TaskStep::new(
                "adaptive_validation".to_string(),
                "Validate adaptive recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Validate recovery steps".to_string());
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_default_step (lines 1113-1142) ---[0m
    async fn execute_persistent_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("persistent_default_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Execute with full session context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent default step completed".to_string())
            .add_output("response".to_string(), response)
            .add_output("session_context_preserved".to_string(), "true".to_string())
            .add_output("task_id".to_string(), task_id))
    }

  ./crates/ccswarm/src/agent/persistent.rs:903-956 method synthesize_results <-> ./crates/ccswarm/src/agent/persistent.rs:1113-1142 method execute_persistent_default_step
  Similarity: 89.83%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:synthesize_results (lines 903-956) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Persistent agent orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Persistent agent orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let session_id = {
            let session = self.session.lock().await;
            session.session_id().to_string()
        };

        let output = serde_json::json!({
            "orchestration_type": "persistent",
            "task_id": task.id,
            "session_id": session_id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "conversation_history_length": self.conversation_history.lock().await.len(),
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_default_step (lines 1113-1142) ---[0m
    async fn execute_persistent_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("persistent_default_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Execute with full session context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent default step completed".to_string())
            .add_output("response".to_string(), response)
            .add_output("session_context_preserved".to_string(), "true".to_string())
            .add_output("task_id".to_string(), task_id))
    }

  ./crates/ccswarm/src/agent/persistent.rs:903-956 method synthesize_results <-> ./crates/ccswarm/src/agent/persistent.rs:1081-1110 method execute_iterative_debugging_step
  Similarity: 89.11%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:synthesize_results (lines 903-956) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Persistent agent orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Persistent agent orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let session_id = {
            let session = self.session.lock().await;
            session.session_id().to_string()
        };

        let output = serde_json::json!({
            "orchestration_type": "persistent",
            "task_id": task.id,
            "session_id": session_id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "conversation_history_length": self.conversation_history.lock().await.len(),
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_debugging_step (lines 1081-1110) ---[0m
    async fn execute_iterative_debugging_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_debug_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::High,
            TaskType::Bugfix,
        );

        // Use conversation history for debugging context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Analyze debugging progress
        let debug_progress = response.contains("identified")
            || response.contains("fixed")
            || response.contains("resolved");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative debugging step completed".to_string())
            .add_output("debug_response".to_string(), response)
            .add_output("progress_made".to_string(), debug_progress.to_string())
            .add_output("context_leveraged".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:811-865 method review_and_adapt <-> ./crates/ccswarm/src/agent/persistent.rs:1024-1048 method execute_session_validation_step
  Similarity: 93.57%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:review_and_adapt (lines 811-865) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Persistent agent reviewing and adapting plan based on {} results",
            results.len()
        );

        // Leverage conversation history for intelligent adaptation
        let history = self.conversation_history.lock().await;
        let failed_steps: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_steps.is_empty() {
            warn!(
                "Persistent agent detected {} failed steps",
                failed_steps.len()
            );

            // Analyze failure patterns from session history
            let failure_analysis = self.analyze_failure_patterns(&history, &failed_steps);

            // Add adaptive recovery steps based on session context
            if failure_analysis.contains("context_loss") {
                let recovery_step = TaskStep::new(
                    "context_recovery".to_string(),
                    "Recover session context".to_string(),
                    StepType::Execution,
                )
                .with_description("Recover lost session context".to_string());
                current_plan.steps.push(recovery_step);
            }

            if failure_analysis.contains("identity_drift") {
                let identity_step = TaskStep::new(
                    "identity_reinforcement".to_string(),
                    "Reinforce agent identity".to_string(),
                    StepType::Execution,
                )
                .with_description("Reinforce agent identity and boundaries".to_string());
                current_plan.steps.push(identity_step);
            }

            let validation_step = TaskStep::new(
                "adaptive_validation".to_string(),
                "Validate adaptive recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Validate recovery steps".to_string());
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_session_validation_step (lines 1024-1048) ---[0m
    async fn execute_session_validation_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Validate session state and identity
        let identity_established = self.identity_established.load(Ordering::Relaxed);
        let history = self.conversation_history.lock().await;
        let session_health = !history.is_empty() && identity_established;

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Session validation completed for {}", session_id))
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "identity_established".to_string(),
                identity_established.to_string(),
            )
            .add_output("session_healthy".to_string(), session_health.to_string())
            .add_output("conversation_length".to_string(), history.len().to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:479-533 method execute_task_batch <-> ./crates/ccswarm/src/agent/persistent.rs:611-655 method orchestrate_task
  Similarity: 74.59%
  Classes: PersistentClaudeAgent <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_task_batch (lines 479-533) ---[0m
    pub async fn execute_task_batch(&mut self, tasks: Vec<Task>) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks for agent: {}",
            tasks.len(),
            self.identity.agent_id
        );

        // Ensure identity is established once for the entire batch
        self.establish_identity_once().await?;

        // Record batch start
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchStart,
            content: format!("Starting batch of {} tasks", tasks.len()),
            task_id: None,
        });
        drop(history);

        let mut results = Vec::new();

        for (i, task) in tasks.into_iter().enumerate() {
            tracing::debug!(
                "Executing batch task {}/{} for agent: {}",
                i + 1,
                results.capacity(),
                self.identity.agent_id
            );

            // For batch processing, we can be more efficient by reusing context
            let result = self.execute_task(task).await?;
            results.push(result);
        }

        // Record batch end
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchEnd,
            content: format!("Completed batch of {} tasks", results.len()),
            task_id: None,
        });
        self.trim_history(&mut history);

        tracing::info!(
            "Batch execution completed for agent: {}",
            self.identity.agent_id
        );
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:orchestrate_task (lines 611-655) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!(
            "🎭 Persistent agent orchestrating task: {}",
            task.description
        );

        // Leverage persistent session for complex task orchestration
        let plan = self.analyze_task(task).await?;

        // Execute plan with session persistence for context continuity
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("persistent_mode".to_string(), "true".to_string());
        context.insert("session_id".to_string(), {
            let session = self.session.lock().await;
            session.session_id().to_string()
        });

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results and preserve in session history
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Persistent agent step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Persistent orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({
                "orchestration": "persistent_success",
                "steps_completed": plan.steps.len(),
                "session_id": context.get("session_id")
            }),
            Duration::from_secs(15),
        ))
    }

  ./crates/ccswarm/src/agent/persistent.rs:611-655 method orchestrate_task <-> ./crates/ccswarm/src/agent/persistent.rs:903-956 method synthesize_results
  Similarity: 74.64%
  Classes: AgentOrchestrator <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/persistent.rs:orchestrate_task (lines 611-655) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!(
            "🎭 Persistent agent orchestrating task: {}",
            task.description
        );

        // Leverage persistent session for complex task orchestration
        let plan = self.analyze_task(task).await?;

        // Execute plan with session persistence for context continuity
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("persistent_mode".to_string(), "true".to_string());
        context.insert("session_id".to_string(), {
            let session = self.session.lock().await;
            session.session_id().to_string()
        });

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results and preserve in session history
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Persistent agent step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Persistent orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({
                "orchestration": "persistent_success",
                "steps_completed": plan.steps.len(),
                "session_id": context.get("session_id")
            }),
            Duration::from_secs(15),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:synthesize_results (lines 903-956) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Persistent agent orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Persistent agent orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let session_id = {
            let session = self.session.lock().await;
            session.session_id().to_string()
        };

        let output = serde_json::json!({
            "orchestration_type": "persistent",
            "task_id": task.id,
            "session_id": session_id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "conversation_history_length": self.conversation_history.lock().await.len(),
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

  ./crates/ccswarm/src/agent/persistent.rs:811-865 method review_and_adapt <-> ./crates/ccswarm/src/agent/persistent.rs:1081-1110 method execute_iterative_debugging_step
  Similarity: 86.60%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:review_and_adapt (lines 811-865) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Persistent agent reviewing and adapting plan based on {} results",
            results.len()
        );

        // Leverage conversation history for intelligent adaptation
        let history = self.conversation_history.lock().await;
        let failed_steps: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_steps.is_empty() {
            warn!(
                "Persistent agent detected {} failed steps",
                failed_steps.len()
            );

            // Analyze failure patterns from session history
            let failure_analysis = self.analyze_failure_patterns(&history, &failed_steps);

            // Add adaptive recovery steps based on session context
            if failure_analysis.contains("context_loss") {
                let recovery_step = TaskStep::new(
                    "context_recovery".to_string(),
                    "Recover session context".to_string(),
                    StepType::Execution,
                )
                .with_description("Recover lost session context".to_string());
                current_plan.steps.push(recovery_step);
            }

            if failure_analysis.contains("identity_drift") {
                let identity_step = TaskStep::new(
                    "identity_reinforcement".to_string(),
                    "Reinforce agent identity".to_string(),
                    StepType::Execution,
                )
                .with_description("Reinforce agent identity and boundaries".to_string());
                current_plan.steps.push(identity_step);
            }

            let validation_step = TaskStep::new(
                "adaptive_validation".to_string(),
                "Validate adaptive recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Validate recovery steps".to_string());
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_debugging_step (lines 1081-1110) ---[0m
    async fn execute_iterative_debugging_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_debug_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::High,
            TaskType::Bugfix,
        );

        // Use conversation history for debugging context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Analyze debugging progress
        let debug_progress = response.contains("identified")
            || response.contains("fixed")
            || response.contains("resolved");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative debugging step completed".to_string())
            .add_output("debug_response".to_string(), response)
            .add_output("progress_made".to_string(), debug_progress.to_string())
            .add_output("context_leveraged".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:479-533 method execute_task_batch <-> ./crates/ccswarm/src/agent/persistent.rs:867-901 method execute_parallel_task
  Similarity: 81.57%
  Classes: PersistentClaudeAgent <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_task_batch (lines 479-533) ---[0m
    pub async fn execute_task_batch(&mut self, tasks: Vec<Task>) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks for agent: {}",
            tasks.len(),
            self.identity.agent_id
        );

        // Ensure identity is established once for the entire batch
        self.establish_identity_once().await?;

        // Record batch start
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchStart,
            content: format!("Starting batch of {} tasks", tasks.len()),
            task_id: None,
        });
        drop(history);

        let mut results = Vec::new();

        for (i, task) in tasks.into_iter().enumerate() {
            tracing::debug!(
                "Executing batch task {}/{} for agent: {}",
                i + 1,
                results.capacity(),
                self.identity.agent_id
            );

            // For batch processing, we can be more efficient by reusing context
            let result = self.execute_task(task).await?;
            results.push(result);
        }

        // Record batch end
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchEnd,
            content: format!("Completed batch of {} tasks", results.len()),
            task_id: None,
        });
        self.trim_history(&mut history);

        tracing::info!(
            "Batch execution completed for agent: {}",
            self.identity.agent_id
        );
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_parallel_task (lines 867-901) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task with session context
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use conversation history for context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session
            .execute_with_context(&execution_task, &history)
            .await?;
        drop(session);
        drop(history);

        let success = !response.contains("error") && !response.contains("failed");

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success,
            output: response,
            error: if success {
                None
            } else {
                Some("Task execution failed".to_string())
            },
        })
    }

  ./crates/ccswarm/src/agent/persistent.rs:903-956 method synthesize_results <-> ./crates/ccswarm/src/agent/persistent.rs:1051-1078 method execute_persistent_test_step
  Similarity: 89.11%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:synthesize_results (lines 903-956) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Persistent agent orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Persistent agent orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let session_id = {
            let session = self.session.lock().await;
            session.session_id().to_string()
        };

        let output = serde_json::json!({
            "orchestration_type": "persistent",
            "task_id": task.id,
            "session_id": session_id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "conversation_history_length": self.conversation_history.lock().await.len(),
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_test_step (lines 1051-1078) ---[0m
    async fn execute_persistent_test_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_test_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Testing,
        );

        // Execute tests with session persistence
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Parse test results from response
        let test_success = response.contains("✅") || response.contains("passed");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent test execution completed".to_string())
            .add_output("test_response".to_string(), response)
            .add_output("test_passed".to_string(), test_success.to_string())
            .add_output("persistent_execution".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:178-235 method build_contextual_prompt <-> ./crates/ccswarm/src/agent/persistent.rs:1234-1261 function test_persistent_agent_orchestration
  Similarity: 84.40%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:build_contextual_prompt (lines 178-235) ---[0m
    fn build_contextual_prompt(
        &self,
        task: &Task,
        history: &VecDeque<ConversationMessage>,
    ) -> String {
        let mut prompt = String::new();

        // Add recent relevant context (last 3 messages)
        let recent_context: Vec<_> = history
            .iter()
            .rev()
            .take(3)
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .collect();

        if !recent_context.is_empty() {
            prompt.push_str("## Recent Context:\n");
            for msg in recent_context.iter().rev() {
                match msg.message_type {
                    MessageType::TaskPrompt => {
                        prompt.push_str(&format!(
                            "Previous Task: {}\n",
                            msg.content.lines().next().unwrap_or("")
                        ));
                    }
                    MessageType::Response => {
                        let summary = msg
                            .content
                            .lines()
                            .next()
                            .unwrap_or("")
                            .chars()
                            .take(100)
                            .collect::<String>();
                        prompt.push_str(&format!("Previous Result: {}...\n", summary));
                    }
                    _ => {}
                }
            }
            prompt.push('\n');
        }

        // Add current task
        prompt.push_str(&format!(
            "## Current Task:\n**{}**\n\n{}\n\nPriority: {:?}\nType: {:?}\n",
            task.description,
            task.details.as_deref().unwrap_or(""),
            task.priority,
            task.task_type
        ));

        prompt
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_orchestration (lines 1234-1261) ---[0m
    async fn test_persistent_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-persistent-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let task = Task::new(
            "test_persistent_orchestration".to_string(),
            "Test persistent agent orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = agent.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
        assert!(plan.steps.iter().any(|step| step.name.contains("session")));
    }

  ./crates/ccswarm/src/agent/persistent.rs:811-865 method review_and_adapt <-> ./crates/ccswarm/src/agent/persistent.rs:998-1021 method execute_iterative_development_step
  Similarity: 91.84%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:review_and_adapt (lines 811-865) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Persistent agent reviewing and adapting plan based on {} results",
            results.len()
        );

        // Leverage conversation history for intelligent adaptation
        let history = self.conversation_history.lock().await;
        let failed_steps: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_steps.is_empty() {
            warn!(
                "Persistent agent detected {} failed steps",
                failed_steps.len()
            );

            // Analyze failure patterns from session history
            let failure_analysis = self.analyze_failure_patterns(&history, &failed_steps);

            // Add adaptive recovery steps based on session context
            if failure_analysis.contains("context_loss") {
                let recovery_step = TaskStep::new(
                    "context_recovery".to_string(),
                    "Recover session context".to_string(),
                    StepType::Execution,
                )
                .with_description("Recover lost session context".to_string());
                current_plan.steps.push(recovery_step);
            }

            if failure_analysis.contains("identity_drift") {
                let identity_step = TaskStep::new(
                    "identity_reinforcement".to_string(),
                    "Reinforce agent identity".to_string(),
                    StepType::Execution,
                )
                .with_description("Reinforce agent identity and boundaries".to_string());
                current_plan.steps.push(identity_step);
            }

            let validation_step = TaskStep::new(
                "adaptive_validation".to_string(),
                "Validate adaptive recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Validate recovery steps".to_string());
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_development_step (lines 998-1021) ---[0m
    async fn execute_iterative_development_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_dev_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use session context for development
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative development step completed with session context".to_string())
            .add_output("development_response".to_string(), response)
            .add_output("session_context_used".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:903-956 method synthesize_results <-> ./crates/ccswarm/src/agent/persistent.rs:1024-1048 method execute_session_validation_step
  Similarity: 91.63%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:synthesize_results (lines 903-956) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Persistent agent orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Persistent agent orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let session_id = {
            let session = self.session.lock().await;
            session.session_id().to_string()
        };

        let output = serde_json::json!({
            "orchestration_type": "persistent",
            "task_id": task.id,
            "session_id": session_id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "conversation_history_length": self.conversation_history.lock().await.len(),
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_session_validation_step (lines 1024-1048) ---[0m
    async fn execute_session_validation_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Validate session state and identity
        let identity_established = self.identity_established.load(Ordering::Relaxed);
        let history = self.conversation_history.lock().await;
        let session_health = !history.is_empty() && identity_established;

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Session validation completed for {}", session_id))
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "identity_established".to_string(),
                identity_established.to_string(),
            )
            .add_output("session_healthy".to_string(), session_health.to_string())
            .add_output("conversation_length".to_string(), history.len().to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:314-365 method establish_identity_once <-> ./crates/ccswarm/src/agent/persistent.rs:611-655 method orchestrate_task
  Similarity: 74.29%
  Classes: PersistentClaudeAgent <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/persistent.rs:establish_identity_once (lines 314-365) ---[0m
    pub async fn establish_identity_once(&self) -> Result<()> {
        if self.identity_established.load(Ordering::Relaxed) {
            tracing::debug!(
                "Identity already established for agent: {}",
                self.identity.agent_id
            );
            return Ok(());
        }

        tracing::info!(
            "Establishing identity for agent: {}",
            self.identity.agent_id
        );

        // Generate lightweight identity prompt (200 tokens vs 2000+ in original)
        let compact_prompt = self.generate_compact_identity_prompt();

        // Send identity establishment message
        let mut session = self.session.lock().await;
        let response = session.send_message(&compact_prompt).await?;

        // Verify response contains identity markers
        if !self.verify_identity_response(&response) {
            return Err(anyhow::anyhow!("Failed to establish agent identity"));
        }

        // Record in conversation history
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityEstablishment,
            content: compact_prompt,
            task_id: None,
        });
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::Response,
            content: response,
            task_id: None,
        });

        // Trim history if needed
        self.trim_history(&mut history);

        self.identity_established.store(true, Ordering::Relaxed);
        tracing::info!(
            "Identity established successfully for agent: {}",
            self.identity.agent_id
        );

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:orchestrate_task (lines 611-655) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!(
            "🎭 Persistent agent orchestrating task: {}",
            task.description
        );

        // Leverage persistent session for complex task orchestration
        let plan = self.analyze_task(task).await?;

        // Execute plan with session persistence for context continuity
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("persistent_mode".to_string(), "true".to_string());
        context.insert("session_id".to_string(), {
            let session = self.session.lock().await;
            session.session_id().to_string()
        });

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results and preserve in session history
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Persistent agent step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Persistent orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({
                "orchestration": "persistent_success",
                "steps_completed": plan.steps.len(),
                "session_id": context.get("session_id")
            }),
            Duration::from_secs(15),
        ))
    }

  ./crates/ccswarm/src/agent/persistent.rs:811-865 method review_and_adapt <-> ./crates/ccswarm/src/agent/persistent.rs:1051-1078 method execute_persistent_test_step
  Similarity: 86.60%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:review_and_adapt (lines 811-865) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Persistent agent reviewing and adapting plan based on {} results",
            results.len()
        );

        // Leverage conversation history for intelligent adaptation
        let history = self.conversation_history.lock().await;
        let failed_steps: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_steps.is_empty() {
            warn!(
                "Persistent agent detected {} failed steps",
                failed_steps.len()
            );

            // Analyze failure patterns from session history
            let failure_analysis = self.analyze_failure_patterns(&history, &failed_steps);

            // Add adaptive recovery steps based on session context
            if failure_analysis.contains("context_loss") {
                let recovery_step = TaskStep::new(
                    "context_recovery".to_string(),
                    "Recover session context".to_string(),
                    StepType::Execution,
                )
                .with_description("Recover lost session context".to_string());
                current_plan.steps.push(recovery_step);
            }

            if failure_analysis.contains("identity_drift") {
                let identity_step = TaskStep::new(
                    "identity_reinforcement".to_string(),
                    "Reinforce agent identity".to_string(),
                    StepType::Execution,
                )
                .with_description("Reinforce agent identity and boundaries".to_string());
                current_plan.steps.push(identity_step);
            }

            let validation_step = TaskStep::new(
                "adaptive_validation".to_string(),
                "Validate adaptive recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Validate recovery steps".to_string());
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_test_step (lines 1051-1078) ---[0m
    async fn execute_persistent_test_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_test_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Testing,
        );

        // Execute tests with session persistence
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Parse test results from response
        let test_success = response.contains("✅") || response.contains("passed");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent test execution completed".to_string())
            .add_output("test_response".to_string(), response)
            .add_output("test_passed".to_string(), test_success.to_string())
            .add_output("persistent_execution".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:611-655 method orchestrate_task <-> ./crates/ccswarm/src/agent/persistent.rs:811-865 method review_and_adapt
  Similarity: 71.84%
  Classes: AgentOrchestrator <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/persistent.rs:orchestrate_task (lines 611-655) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!(
            "🎭 Persistent agent orchestrating task: {}",
            task.description
        );

        // Leverage persistent session for complex task orchestration
        let plan = self.analyze_task(task).await?;

        // Execute plan with session persistence for context continuity
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("persistent_mode".to_string(), "true".to_string());
        context.insert("session_id".to_string(), {
            let session = self.session.lock().await;
            session.session_id().to_string()
        });

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results and preserve in session history
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Persistent agent step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Persistent orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({
                "orchestration": "persistent_success",
                "steps_completed": plan.steps.len(),
                "session_id": context.get("session_id")
            }),
            Duration::from_secs(15),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:review_and_adapt (lines 811-865) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Persistent agent reviewing and adapting plan based on {} results",
            results.len()
        );

        // Leverage conversation history for intelligent adaptation
        let history = self.conversation_history.lock().await;
        let failed_steps: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_steps.is_empty() {
            warn!(
                "Persistent agent detected {} failed steps",
                failed_steps.len()
            );

            // Analyze failure patterns from session history
            let failure_analysis = self.analyze_failure_patterns(&history, &failed_steps);

            // Add adaptive recovery steps based on session context
            if failure_analysis.contains("context_loss") {
                let recovery_step = TaskStep::new(
                    "context_recovery".to_string(),
                    "Recover session context".to_string(),
                    StepType::Execution,
                )
                .with_description("Recover lost session context".to_string());
                current_plan.steps.push(recovery_step);
            }

            if failure_analysis.contains("identity_drift") {
                let identity_step = TaskStep::new(
                    "identity_reinforcement".to_string(),
                    "Reinforce agent identity".to_string(),
                    StepType::Execution,
                )
                .with_description("Reinforce agent identity and boundaries".to_string());
                current_plan.steps.push(identity_step);
            }

            let validation_step = TaskStep::new(
                "adaptive_validation".to_string(),
                "Validate adaptive recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Validate recovery steps".to_string());
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

  ./crates/ccswarm/src/agent/persistent.rs:903-956 method synthesize_results <-> ./crates/ccswarm/src/agent/persistent.rs:998-1021 method execute_iterative_development_step
  Similarity: 91.27%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:synthesize_results (lines 903-956) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Persistent agent orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Persistent agent orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let session_id = {
            let session = self.session.lock().await;
            session.session_id().to_string()
        };

        let output = serde_json::json!({
            "orchestration_type": "persistent",
            "task_id": task.id,
            "session_id": session_id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "conversation_history_length": self.conversation_history.lock().await.len(),
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_development_step (lines 998-1021) ---[0m
    async fn execute_iterative_development_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_dev_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use session context for development
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative development step completed with session context".to_string())
            .add_output("development_response".to_string(), response)
            .add_output("session_context_used".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:178-235 method build_contextual_prompt <-> ./crates/ccswarm/src/agent/persistent.rs:1186-1205 function test_persistent_agent_creation
  Similarity: 91.18%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:build_contextual_prompt (lines 178-235) ---[0m
    fn build_contextual_prompt(
        &self,
        task: &Task,
        history: &VecDeque<ConversationMessage>,
    ) -> String {
        let mut prompt = String::new();

        // Add recent relevant context (last 3 messages)
        let recent_context: Vec<_> = history
            .iter()
            .rev()
            .take(3)
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .collect();

        if !recent_context.is_empty() {
            prompt.push_str("## Recent Context:\n");
            for msg in recent_context.iter().rev() {
                match msg.message_type {
                    MessageType::TaskPrompt => {
                        prompt.push_str(&format!(
                            "Previous Task: {}\n",
                            msg.content.lines().next().unwrap_or("")
                        ));
                    }
                    MessageType::Response => {
                        let summary = msg
                            .content
                            .lines()
                            .next()
                            .unwrap_or("")
                            .chars()
                            .take(100)
                            .collect::<String>();
                        prompt.push_str(&format!("Previous Result: {}...\n", summary));
                    }
                    _ => {}
                }
            }
            prompt.push('\n');
        }

        // Add current task
        prompt.push_str(&format!(
            "## Current Task:\n**{}**\n\n{}\n\nPriority: {:?}\nType: {:?}\n",
            task.description,
            task.details.as_deref().unwrap_or(""),
            task.priority,
            task.task_type
        ));

        prompt
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_creation (lines 1186-1205) ---[0m
    async fn test_persistent_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        assert_eq!(agent.status, AgentStatus::Initializing);
        assert!(!agent.identity_established.load(Ordering::Relaxed));
    }

  ./crates/ccswarm/src/agent/persistent.rs:178-235 method build_contextual_prompt <-> ./crates/ccswarm/src/agent/persistent.rs:290-311 method new
  Similarity: 87.76%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:build_contextual_prompt (lines 178-235) ---[0m
    fn build_contextual_prompt(
        &self,
        task: &Task,
        history: &VecDeque<ConversationMessage>,
    ) -> String {
        let mut prompt = String::new();

        // Add recent relevant context (last 3 messages)
        let recent_context: Vec<_> = history
            .iter()
            .rev()
            .take(3)
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .collect();

        if !recent_context.is_empty() {
            prompt.push_str("## Recent Context:\n");
            for msg in recent_context.iter().rev() {
                match msg.message_type {
                    MessageType::TaskPrompt => {
                        prompt.push_str(&format!(
                            "Previous Task: {}\n",
                            msg.content.lines().next().unwrap_or("")
                        ));
                    }
                    MessageType::Response => {
                        let summary = msg
                            .content
                            .lines()
                            .next()
                            .unwrap_or("")
                            .chars()
                            .take(100)
                            .collect::<String>();
                        prompt.push_str(&format!("Previous Result: {}...\n", summary));
                    }
                    _ => {}
                }
            }
            prompt.push('\n');
        }

        // Add current task
        prompt.push_str(&format!(
            "## Current Task:\n**{}**\n\n{}\n\nPriority: {:?}\nType: {:?}\n",
            task.description,
            task.details.as_deref().unwrap_or(""),
            task.priority,
            task.task_type
        ));

        prompt
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:new (lines 290-311) ---[0m
    pub async fn new(identity: AgentIdentity, claude_config: ClaudeConfig) -> Result<Self> {
        let session_id = Uuid::new_v4().to_string();
        let session = ClaudeCodeSession::new(
            session_id,
            identity.workspace_path.clone(),
            identity.env_vars.clone(),
            claude_config,
        );

        Ok(Self {
            identity,
            session: Arc::new(Mutex::new(session)),
            identity_established: Arc::new(AtomicBool::new(false)),
            conversation_history: Arc::new(Mutex::new(VecDeque::new())),
            max_history_size: 50, // Keep last 50 messages
            status: AgentStatus::Initializing,
            current_task: None,
            task_history: Vec::new(),
            last_activity: Utc::now(),
            session_created_at: Utc::now(),
        })
    }

  ./crates/ccswarm/src/agent/persistent.rs:611-655 method orchestrate_task <-> ./crates/ccswarm/src/agent/persistent.rs:867-901 method execute_parallel_task
  Similarity: 86.72%
  Classes: AgentOrchestrator <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/persistent.rs:orchestrate_task (lines 611-655) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!(
            "🎭 Persistent agent orchestrating task: {}",
            task.description
        );

        // Leverage persistent session for complex task orchestration
        let plan = self.analyze_task(task).await?;

        // Execute plan with session persistence for context continuity
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("persistent_mode".to_string(), "true".to_string());
        context.insert("session_id".to_string(), {
            let session = self.session.lock().await;
            session.session_id().to_string()
        });

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results and preserve in session history
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Persistent agent step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Persistent orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({
                "orchestration": "persistent_success",
                "steps_completed": plan.steps.len(),
                "session_id": context.get("session_id")
            }),
            Duration::from_secs(15),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_parallel_task (lines 867-901) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task with session context
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use conversation history for context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session
            .execute_with_context(&execution_task, &history)
            .await?;
        drop(session);
        drop(history);

        let success = !response.contains("error") && !response.contains("failed");

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success,
            output: response,
            error: if success {
                None
            } else {
                Some("Task execution failed".to_string())
            },
        })
    }

  ./crates/ccswarm/src/agent/persistent.rs:88-133 method send_message <-> ./crates/ccswarm/src/agent/persistent.rs:611-655 method orchestrate_task
  Similarity: 76.20%
  Classes: ClaudeCodeSession <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_message (lines 88-133) ---[0m
    pub async fn send_message(&mut self, prompt: &str) -> Result<String> {
        self.last_activity = Instant::now();

        // For tests and simulation mode, return a mock response
        if !self.claude_config.use_real_api {
            return Ok(self.generate_mock_response(prompt));
        }

        let mut cmd = Command::new("claude");
        cmd.current_dir(&self.working_dir);

        // Add environment variables
        for (key, value) in &self.env_vars {
            cmd.env(key, value);
        }

        // Add Claude arguments
        cmd.arg("-p").arg(prompt);

        if self.claude_config.json_output {
            cmd.arg("--json");
        }

        if self.claude_config.dangerous_skip {
            cmd.arg("--dangerously-skip-permissions");
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.claude_config.think_mode {
            cmd.arg("--think").arg(think_mode.to_string());
        }

        let output = cmd
            .output()
            .await
            .context("Failed to execute Claude Code")?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).to_string())
        } else {
            Err(anyhow::anyhow!(
                "Claude Code execution failed: {}",
                String::from_utf8_lossy(&output.stderr)
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:orchestrate_task (lines 611-655) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!(
            "🎭 Persistent agent orchestrating task: {}",
            task.description
        );

        // Leverage persistent session for complex task orchestration
        let plan = self.analyze_task(task).await?;

        // Execute plan with session persistence for context continuity
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("persistent_mode".to_string(), "true".to_string());
        context.insert("session_id".to_string(), {
            let session = self.session.lock().await;
            session.session_id().to_string()
        });

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results and preserve in session history
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Persistent agent step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Persistent orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({
                "orchestration": "persistent_success",
                "steps_completed": plan.steps.len(),
                "session_id": context.get("session_id")
            }),
            Duration::from_secs(15),
        ))
    }

  ./crates/ccswarm/src/agent/persistent.rs:178-235 method build_contextual_prompt <-> ./crates/ccswarm/src/agent/persistent.rs:1208-1231 function test_compact_identity_prompt
  Similarity: 83.80%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:build_contextual_prompt (lines 178-235) ---[0m
    fn build_contextual_prompt(
        &self,
        task: &Task,
        history: &VecDeque<ConversationMessage>,
    ) -> String {
        let mut prompt = String::new();

        // Add recent relevant context (last 3 messages)
        let recent_context: Vec<_> = history
            .iter()
            .rev()
            .take(3)
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .collect();

        if !recent_context.is_empty() {
            prompt.push_str("## Recent Context:\n");
            for msg in recent_context.iter().rev() {
                match msg.message_type {
                    MessageType::TaskPrompt => {
                        prompt.push_str(&format!(
                            "Previous Task: {}\n",
                            msg.content.lines().next().unwrap_or("")
                        ));
                    }
                    MessageType::Response => {
                        let summary = msg
                            .content
                            .lines()
                            .next()
                            .unwrap_or("")
                            .chars()
                            .take(100)
                            .collect::<String>();
                        prompt.push_str(&format!("Previous Result: {}...\n", summary));
                    }
                    _ => {}
                }
            }
            prompt.push('\n');
        }

        // Add current task
        prompt.push_str(&format!(
            "## Current Task:\n**{}**\n\n{}\n\nPriority: {:?}\nType: {:?}\n",
            task.description,
            task.details.as_deref().unwrap_or(""),
            task.priority,
            task.task_type
        ));

        prompt
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_compact_identity_prompt (lines 1208-1231) ---[0m
    async fn test_compact_identity_prompt() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let prompt = agent.generate_compact_identity_prompt();

        // Verify it's much more compact than the original CLAUDE.md approach
        assert!(prompt.len() < 500); // Much less than 2000+ tokens
        assert!(prompt.contains("🤖 AGENT:"));
        assert!(prompt.contains("Frontend"));
    }

  ./crates/ccswarm/src/agent/persistent.rs:903-956 method synthesize_results <-> ./crates/ccswarm/src/agent/persistent.rs:1234-1261 function test_persistent_agent_orchestration
  Similarity: 83.68%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:synthesize_results (lines 903-956) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Persistent agent orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Persistent agent orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let session_id = {
            let session = self.session.lock().await;
            session.session_id().to_string()
        };

        let output = serde_json::json!({
            "orchestration_type": "persistent",
            "task_id": task.id,
            "session_id": session_id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "conversation_history_length": self.conversation_history.lock().await.len(),
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_orchestration (lines 1234-1261) ---[0m
    async fn test_persistent_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-persistent-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let task = Task::new(
            "test_persistent_orchestration".to_string(),
            "Test persistent agent orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = agent.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
        assert!(plan.steps.iter().any(|step| step.name.contains("session")));
    }

  ./crates/ccswarm/src/agent/persistent.rs:811-865 method review_and_adapt <-> ./crates/ccswarm/src/agent/persistent.rs:1186-1205 function test_persistent_agent_creation
  Similarity: 89.81%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:review_and_adapt (lines 811-865) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Persistent agent reviewing and adapting plan based on {} results",
            results.len()
        );

        // Leverage conversation history for intelligent adaptation
        let history = self.conversation_history.lock().await;
        let failed_steps: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_steps.is_empty() {
            warn!(
                "Persistent agent detected {} failed steps",
                failed_steps.len()
            );

            // Analyze failure patterns from session history
            let failure_analysis = self.analyze_failure_patterns(&history, &failed_steps);

            // Add adaptive recovery steps based on session context
            if failure_analysis.contains("context_loss") {
                let recovery_step = TaskStep::new(
                    "context_recovery".to_string(),
                    "Recover session context".to_string(),
                    StepType::Execution,
                )
                .with_description("Recover lost session context".to_string());
                current_plan.steps.push(recovery_step);
            }

            if failure_analysis.contains("identity_drift") {
                let identity_step = TaskStep::new(
                    "identity_reinforcement".to_string(),
                    "Reinforce agent identity".to_string(),
                    StepType::Execution,
                )
                .with_description("Reinforce agent identity and boundaries".to_string());
                current_plan.steps.push(identity_step);
            }

            let validation_step = TaskStep::new(
                "adaptive_validation".to_string(),
                "Validate adaptive recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Validate recovery steps".to_string());
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_creation (lines 1186-1205) ---[0m
    async fn test_persistent_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        assert_eq!(agent.status, AgentStatus::Initializing);
        assert!(!agent.identity_established.load(Ordering::Relaxed));
    }

  ./crates/ccswarm/src/agent/persistent.rs:314-365 method establish_identity_once <-> ./crates/ccswarm/src/agent/persistent.rs:867-901 method execute_parallel_task
  Similarity: 77.40%
  Classes: PersistentClaudeAgent <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/persistent.rs:establish_identity_once (lines 314-365) ---[0m
    pub async fn establish_identity_once(&self) -> Result<()> {
        if self.identity_established.load(Ordering::Relaxed) {
            tracing::debug!(
                "Identity already established for agent: {}",
                self.identity.agent_id
            );
            return Ok(());
        }

        tracing::info!(
            "Establishing identity for agent: {}",
            self.identity.agent_id
        );

        // Generate lightweight identity prompt (200 tokens vs 2000+ in original)
        let compact_prompt = self.generate_compact_identity_prompt();

        // Send identity establishment message
        let mut session = self.session.lock().await;
        let response = session.send_message(&compact_prompt).await?;

        // Verify response contains identity markers
        if !self.verify_identity_response(&response) {
            return Err(anyhow::anyhow!("Failed to establish agent identity"));
        }

        // Record in conversation history
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityEstablishment,
            content: compact_prompt,
            task_id: None,
        });
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::Response,
            content: response,
            task_id: None,
        });

        // Trim history if needed
        self.trim_history(&mut history);

        self.identity_established.store(true, Ordering::Relaxed);
        tracing::info!(
            "Identity established successfully for agent: {}",
            self.identity.agent_id
        );

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_parallel_task (lines 867-901) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task with session context
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use conversation history for context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session
            .execute_with_context(&execution_task, &history)
            .await?;
        drop(session);
        drop(history);

        let success = !response.contains("error") && !response.contains("failed");

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success,
            output: response,
            error: if success {
                None
            } else {
                Some("Task execution failed".to_string())
            },
        })
    }

  ./crates/ccswarm/src/agent/persistent.rs:611-655 method orchestrate_task <-> ./crates/ccswarm/src/agent/persistent.rs:1113-1142 method execute_persistent_default_step
  Similarity: 89.65%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:orchestrate_task (lines 611-655) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!(
            "🎭 Persistent agent orchestrating task: {}",
            task.description
        );

        // Leverage persistent session for complex task orchestration
        let plan = self.analyze_task(task).await?;

        // Execute plan with session persistence for context continuity
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("persistent_mode".to_string(), "true".to_string());
        context.insert("session_id".to_string(), {
            let session = self.session.lock().await;
            session.session_id().to_string()
        });

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results and preserve in session history
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Persistent agent step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Persistent orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({
                "orchestration": "persistent_success",
                "steps_completed": plan.steps.len(),
                "session_id": context.get("session_id")
            }),
            Duration::from_secs(15),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_default_step (lines 1113-1142) ---[0m
    async fn execute_persistent_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("persistent_default_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Execute with full session context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent default step completed".to_string())
            .add_output("response".to_string(), response)
            .add_output("session_context_preserved".to_string(), "true".to_string())
            .add_output("task_id".to_string(), task_id))
    }

  ./crates/ccswarm/src/agent/persistent.rs:811-865 method review_and_adapt <-> ./crates/ccswarm/src/agent/persistent.rs:1234-1261 function test_persistent_agent_orchestration
  Similarity: 80.32%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:review_and_adapt (lines 811-865) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Persistent agent reviewing and adapting plan based on {} results",
            results.len()
        );

        // Leverage conversation history for intelligent adaptation
        let history = self.conversation_history.lock().await;
        let failed_steps: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_steps.is_empty() {
            warn!(
                "Persistent agent detected {} failed steps",
                failed_steps.len()
            );

            // Analyze failure patterns from session history
            let failure_analysis = self.analyze_failure_patterns(&history, &failed_steps);

            // Add adaptive recovery steps based on session context
            if failure_analysis.contains("context_loss") {
                let recovery_step = TaskStep::new(
                    "context_recovery".to_string(),
                    "Recover session context".to_string(),
                    StepType::Execution,
                )
                .with_description("Recover lost session context".to_string());
                current_plan.steps.push(recovery_step);
            }

            if failure_analysis.contains("identity_drift") {
                let identity_step = TaskStep::new(
                    "identity_reinforcement".to_string(),
                    "Reinforce agent identity".to_string(),
                    StepType::Execution,
                )
                .with_description("Reinforce agent identity and boundaries".to_string());
                current_plan.steps.push(identity_step);
            }

            let validation_step = TaskStep::new(
                "adaptive_validation".to_string(),
                "Validate adaptive recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Validate recovery steps".to_string());
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_orchestration (lines 1234-1261) ---[0m
    async fn test_persistent_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-persistent-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let task = Task::new(
            "test_persistent_orchestration".to_string(),
            "Test persistent agent orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = agent.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
        assert!(plan.steps.iter().any(|step| step.name.contains("session")));
    }

  ./crates/ccswarm/src/agent/persistent.rs:611-655 method orchestrate_task <-> ./crates/ccswarm/src/agent/persistent.rs:961-995 method execute_context_analysis_step
  Similarity: 82.93%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:orchestrate_task (lines 611-655) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!(
            "🎭 Persistent agent orchestrating task: {}",
            task.description
        );

        // Leverage persistent session for complex task orchestration
        let plan = self.analyze_task(task).await?;

        // Execute plan with session persistence for context continuity
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("persistent_mode".to_string(), "true".to_string());
        context.insert("session_id".to_string(), {
            let session = self.session.lock().await;
            session.session_id().to_string()
        });

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results and preserve in session history
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Persistent agent step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Persistent orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({
                "orchestration": "persistent_success",
                "steps_completed": plan.steps.len(),
                "session_id": context.get("session_id")
            }),
            Duration::from_secs(15),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_context_analysis_step (lines 961-995) ---[0m
    async fn execute_context_analysis_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Analyze conversation history for relevant context
        let history = self.conversation_history.lock().await;
        let relevant_messages = history
            .iter()
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .count();

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Context analysis for task {} completed", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "history_messages".to_string(),
                relevant_messages.to_string(),
            ))
    }

  ./crates/ccswarm/src/agent/persistent.rs:903-956 method synthesize_results <-> ./crates/ccswarm/src/agent/persistent.rs:1208-1231 function test_compact_identity_prompt
  Similarity: 84.88%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:synthesize_results (lines 903-956) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Persistent agent orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Persistent agent orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let session_id = {
            let session = self.session.lock().await;
            session.session_id().to_string()
        };

        let output = serde_json::json!({
            "orchestration_type": "persistent",
            "task_id": task.id,
            "session_id": session_id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "conversation_history_length": self.conversation_history.lock().await.len(),
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_compact_identity_prompt (lines 1208-1231) ---[0m
    async fn test_compact_identity_prompt() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let prompt = agent.generate_compact_identity_prompt();

        // Verify it's much more compact than the original CLAUDE.md approach
        assert!(prompt.len() < 500); // Much less than 2000+ tokens
        assert!(prompt.contains("🤖 AGENT:"));
        assert!(prompt.contains("Frontend"));
    }

  ./crates/ccswarm/src/agent/persistent.rs:479-533 method execute_task_batch <-> ./crates/ccswarm/src/agent/persistent.rs:1081-1110 method execute_iterative_debugging_step
  Similarity: 77.10%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_task_batch (lines 479-533) ---[0m
    pub async fn execute_task_batch(&mut self, tasks: Vec<Task>) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks for agent: {}",
            tasks.len(),
            self.identity.agent_id
        );

        // Ensure identity is established once for the entire batch
        self.establish_identity_once().await?;

        // Record batch start
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchStart,
            content: format!("Starting batch of {} tasks", tasks.len()),
            task_id: None,
        });
        drop(history);

        let mut results = Vec::new();

        for (i, task) in tasks.into_iter().enumerate() {
            tracing::debug!(
                "Executing batch task {}/{} for agent: {}",
                i + 1,
                results.capacity(),
                self.identity.agent_id
            );

            // For batch processing, we can be more efficient by reusing context
            let result = self.execute_task(task).await?;
            results.push(result);
        }

        // Record batch end
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchEnd,
            content: format!("Completed batch of {} tasks", results.len()),
            task_id: None,
        });
        self.trim_history(&mut history);

        tracing::info!(
            "Batch execution completed for agent: {}",
            self.identity.agent_id
        );
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_debugging_step (lines 1081-1110) ---[0m
    async fn execute_iterative_debugging_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_debug_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::High,
            TaskType::Bugfix,
        );

        // Use conversation history for debugging context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Analyze debugging progress
        let debug_progress = response.contains("identified")
            || response.contains("fixed")
            || response.contains("resolved");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative debugging step completed".to_string())
            .add_output("debug_response".to_string(), response)
            .add_output("progress_made".to_string(), debug_progress.to_string())
            .add_output("context_leveraged".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:611-655 method orchestrate_task <-> ./crates/ccswarm/src/agent/persistent.rs:1081-1110 method execute_iterative_debugging_step
  Similarity: 87.32%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:orchestrate_task (lines 611-655) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!(
            "🎭 Persistent agent orchestrating task: {}",
            task.description
        );

        // Leverage persistent session for complex task orchestration
        let plan = self.analyze_task(task).await?;

        // Execute plan with session persistence for context continuity
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("persistent_mode".to_string(), "true".to_string());
        context.insert("session_id".to_string(), {
            let session = self.session.lock().await;
            session.session_id().to_string()
        });

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results and preserve in session history
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Persistent agent step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Persistent orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({
                "orchestration": "persistent_success",
                "steps_completed": plan.steps.len(),
                "session_id": context.get("session_id")
            }),
            Duration::from_secs(15),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_debugging_step (lines 1081-1110) ---[0m
    async fn execute_iterative_debugging_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_debug_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::High,
            TaskType::Bugfix,
        );

        // Use conversation history for debugging context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Analyze debugging progress
        let debug_progress = response.contains("identified")
            || response.contains("fixed")
            || response.contains("resolved");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative debugging step completed".to_string())
            .add_output("debug_response".to_string(), response)
            .add_output("progress_made".to_string(), debug_progress.to_string())
            .add_output("context_leveraged".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:611-655 method orchestrate_task <-> ./crates/ccswarm/src/agent/persistent.rs:1234-1261 function test_persistent_agent_orchestration
  Similarity: 88.78%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:orchestrate_task (lines 611-655) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!(
            "🎭 Persistent agent orchestrating task: {}",
            task.description
        );

        // Leverage persistent session for complex task orchestration
        let plan = self.analyze_task(task).await?;

        // Execute plan with session persistence for context continuity
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("persistent_mode".to_string(), "true".to_string());
        context.insert("session_id".to_string(), {
            let session = self.session.lock().await;
            session.session_id().to_string()
        });

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results and preserve in session history
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Persistent agent step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Persistent orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({
                "orchestration": "persistent_success",
                "steps_completed": plan.steps.len(),
                "session_id": context.get("session_id")
            }),
            Duration::from_secs(15),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_orchestration (lines 1234-1261) ---[0m
    async fn test_persistent_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-persistent-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let task = Task::new(
            "test_persistent_orchestration".to_string(),
            "Test persistent agent orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = agent.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
        assert!(plan.steps.iter().any(|step| step.name.contains("session")));
    }

  ./crates/ccswarm/src/agent/persistent.rs:479-533 method execute_task_batch <-> ./crates/ccswarm/src/agent/persistent.rs:1113-1142 method execute_persistent_default_step
  Similarity: 75.78%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_task_batch (lines 479-533) ---[0m
    pub async fn execute_task_batch(&mut self, tasks: Vec<Task>) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks for agent: {}",
            tasks.len(),
            self.identity.agent_id
        );

        // Ensure identity is established once for the entire batch
        self.establish_identity_once().await?;

        // Record batch start
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchStart,
            content: format!("Starting batch of {} tasks", tasks.len()),
            task_id: None,
        });
        drop(history);

        let mut results = Vec::new();

        for (i, task) in tasks.into_iter().enumerate() {
            tracing::debug!(
                "Executing batch task {}/{} for agent: {}",
                i + 1,
                results.capacity(),
                self.identity.agent_id
            );

            // For batch processing, we can be more efficient by reusing context
            let result = self.execute_task(task).await?;
            results.push(result);
        }

        // Record batch end
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchEnd,
            content: format!("Completed batch of {} tasks", results.len()),
            task_id: None,
        });
        self.trim_history(&mut history);

        tracing::info!(
            "Batch execution completed for agent: {}",
            self.identity.agent_id
        );
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_default_step (lines 1113-1142) ---[0m
    async fn execute_persistent_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("persistent_default_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Execute with full session context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent default step completed".to_string())
            .add_output("response".to_string(), response)
            .add_output("session_context_preserved".to_string(), "true".to_string())
            .add_output("task_id".to_string(), task_id))
    }

  ./crates/ccswarm/src/agent/persistent.rs:811-865 method review_and_adapt <-> ./crates/ccswarm/src/agent/persistent.rs:1208-1231 function test_compact_identity_prompt
  Similarity: 81.24%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:review_and_adapt (lines 811-865) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Persistent agent reviewing and adapting plan based on {} results",
            results.len()
        );

        // Leverage conversation history for intelligent adaptation
        let history = self.conversation_history.lock().await;
        let failed_steps: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_steps.is_empty() {
            warn!(
                "Persistent agent detected {} failed steps",
                failed_steps.len()
            );

            // Analyze failure patterns from session history
            let failure_analysis = self.analyze_failure_patterns(&history, &failed_steps);

            // Add adaptive recovery steps based on session context
            if failure_analysis.contains("context_loss") {
                let recovery_step = TaskStep::new(
                    "context_recovery".to_string(),
                    "Recover session context".to_string(),
                    StepType::Execution,
                )
                .with_description("Recover lost session context".to_string());
                current_plan.steps.push(recovery_step);
            }

            if failure_analysis.contains("identity_drift") {
                let identity_step = TaskStep::new(
                    "identity_reinforcement".to_string(),
                    "Reinforce agent identity".to_string(),
                    StepType::Execution,
                )
                .with_description("Reinforce agent identity and boundaries".to_string());
                current_plan.steps.push(identity_step);
            }

            let validation_step = TaskStep::new(
                "adaptive_validation".to_string(),
                "Validate adaptive recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Validate recovery steps".to_string());
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_compact_identity_prompt (lines 1208-1231) ---[0m
    async fn test_compact_identity_prompt() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let prompt = agent.generate_compact_identity_prompt();

        // Verify it's much more compact than the original CLAUDE.md approach
        assert!(prompt.len() < 500); // Much less than 2000+ tokens
        assert!(prompt.contains("🤖 AGENT:"));
        assert!(prompt.contains("Frontend"));
    }

  ./crates/ccswarm/src/agent/persistent.rs:479-533 method execute_task_batch <-> ./crates/ccswarm/src/agent/persistent.rs:1051-1078 method execute_persistent_test_step
  Similarity: 77.10%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_task_batch (lines 479-533) ---[0m
    pub async fn execute_task_batch(&mut self, tasks: Vec<Task>) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks for agent: {}",
            tasks.len(),
            self.identity.agent_id
        );

        // Ensure identity is established once for the entire batch
        self.establish_identity_once().await?;

        // Record batch start
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchStart,
            content: format!("Starting batch of {} tasks", tasks.len()),
            task_id: None,
        });
        drop(history);

        let mut results = Vec::new();

        for (i, task) in tasks.into_iter().enumerate() {
            tracing::debug!(
                "Executing batch task {}/{} for agent: {}",
                i + 1,
                results.capacity(),
                self.identity.agent_id
            );

            // For batch processing, we can be more efficient by reusing context
            let result = self.execute_task(task).await?;
            results.push(result);
        }

        // Record batch end
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchEnd,
            content: format!("Completed batch of {} tasks", results.len()),
            task_id: None,
        });
        self.trim_history(&mut history);

        tracing::info!(
            "Batch execution completed for agent: {}",
            self.identity.agent_id
        );
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_test_step (lines 1051-1078) ---[0m
    async fn execute_persistent_test_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_test_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Testing,
        );

        // Execute tests with session persistence
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Parse test results from response
        let test_success = response.contains("✅") || response.contains("passed");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent test execution completed".to_string())
            .add_output("test_response".to_string(), response)
            .add_output("test_passed".to_string(), test_success.to_string())
            .add_output("persistent_execution".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:611-655 method orchestrate_task <-> ./crates/ccswarm/src/agent/persistent.rs:1051-1078 method execute_persistent_test_step
  Similarity: 87.32%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:orchestrate_task (lines 611-655) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!(
            "🎭 Persistent agent orchestrating task: {}",
            task.description
        );

        // Leverage persistent session for complex task orchestration
        let plan = self.analyze_task(task).await?;

        // Execute plan with session persistence for context continuity
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("persistent_mode".to_string(), "true".to_string());
        context.insert("session_id".to_string(), {
            let session = self.session.lock().await;
            session.session_id().to_string()
        });

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results and preserve in session history
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Persistent agent step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Persistent orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({
                "orchestration": "persistent_success",
                "steps_completed": plan.steps.len(),
                "session_id": context.get("session_id")
            }),
            Duration::from_secs(15),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_test_step (lines 1051-1078) ---[0m
    async fn execute_persistent_test_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_test_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Testing,
        );

        // Execute tests with session persistence
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Parse test results from response
        let test_success = response.contains("✅") || response.contains("passed");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent test execution completed".to_string())
            .add_output("test_response".to_string(), response)
            .add_output("test_passed".to_string(), test_success.to_string())
            .add_output("persistent_execution".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:611-655 method orchestrate_task <-> ./crates/ccswarm/src/agent/persistent.rs:1145-1176 method analyze_failure_patterns
  Similarity: 82.46%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:orchestrate_task (lines 611-655) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!(
            "🎭 Persistent agent orchestrating task: {}",
            task.description
        );

        // Leverage persistent session for complex task orchestration
        let plan = self.analyze_task(task).await?;

        // Execute plan with session persistence for context continuity
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("persistent_mode".to_string(), "true".to_string());
        context.insert("session_id".to_string(), {
            let session = self.session.lock().await;
            session.session_id().to_string()
        });

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results and preserve in session history
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Persistent agent step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Persistent orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({
                "orchestration": "persistent_success",
                "steps_completed": plan.steps.len(),
                "session_id": context.get("session_id")
            }),
            Duration::from_secs(15),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:analyze_failure_patterns (lines 1145-1176) ---[0m
    fn analyze_failure_patterns(
        &self,
        history: &VecDeque<ConversationMessage>,
        failed_steps: &[&StepResult],
    ) -> String {
        let mut patterns = Vec::new();

        // Check for context loss patterns
        if failed_steps
            .iter()
            .any(|step| step.errors.iter().any(|e| e.contains("context")))
        {
            patterns.push("context_loss");
        }

        // Check for identity drift in recent messages
        let recent_responses: Vec<_> = history
            .iter()
            .rev()
            .take(5)
            .filter(|msg| matches!(msg.message_type, MessageType::Response))
            .collect();

        if recent_responses
            .iter()
            .any(|msg| !msg.content.contains("🤖 AGENT:"))
        {
            patterns.push("identity_drift");
        }

        patterns.join(",")
    }

  ./crates/ccswarm/src/agent/persistent.rs:479-533 method execute_task_batch <-> ./crates/ccswarm/src/agent/persistent.rs:1208-1231 function test_compact_identity_prompt
  Similarity: 80.00%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_task_batch (lines 479-533) ---[0m
    pub async fn execute_task_batch(&mut self, tasks: Vec<Task>) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks for agent: {}",
            tasks.len(),
            self.identity.agent_id
        );

        // Ensure identity is established once for the entire batch
        self.establish_identity_once().await?;

        // Record batch start
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchStart,
            content: format!("Starting batch of {} tasks", tasks.len()),
            task_id: None,
        });
        drop(history);

        let mut results = Vec::new();

        for (i, task) in tasks.into_iter().enumerate() {
            tracing::debug!(
                "Executing batch task {}/{} for agent: {}",
                i + 1,
                results.capacity(),
                self.identity.agent_id
            );

            // For batch processing, we can be more efficient by reusing context
            let result = self.execute_task(task).await?;
            results.push(result);
        }

        // Record batch end
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchEnd,
            content: format!("Completed batch of {} tasks", results.len()),
            task_id: None,
        });
        self.trim_history(&mut history);

        tracing::info!(
            "Batch execution completed for agent: {}",
            self.identity.agent_id
        );
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_compact_identity_prompt (lines 1208-1231) ---[0m
    async fn test_compact_identity_prompt() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let prompt = agent.generate_compact_identity_prompt();

        // Verify it's much more compact than the original CLAUDE.md approach
        assert!(prompt.len() < 500); // Much less than 2000+ tokens
        assert!(prompt.contains("🤖 AGENT:"));
        assert!(prompt.contains("Frontend"));
    }

  ./crates/ccswarm/src/agent/persistent.rs:88-133 method send_message <-> ./crates/ccswarm/src/agent/persistent.rs:867-901 method execute_parallel_task
  Similarity: 76.48%
  Classes: ClaudeCodeSession <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_message (lines 88-133) ---[0m
    pub async fn send_message(&mut self, prompt: &str) -> Result<String> {
        self.last_activity = Instant::now();

        // For tests and simulation mode, return a mock response
        if !self.claude_config.use_real_api {
            return Ok(self.generate_mock_response(prompt));
        }

        let mut cmd = Command::new("claude");
        cmd.current_dir(&self.working_dir);

        // Add environment variables
        for (key, value) in &self.env_vars {
            cmd.env(key, value);
        }

        // Add Claude arguments
        cmd.arg("-p").arg(prompt);

        if self.claude_config.json_output {
            cmd.arg("--json");
        }

        if self.claude_config.dangerous_skip {
            cmd.arg("--dangerously-skip-permissions");
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.claude_config.think_mode {
            cmd.arg("--think").arg(think_mode.to_string());
        }

        let output = cmd
            .output()
            .await
            .context("Failed to execute Claude Code")?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).to_string())
        } else {
            Err(anyhow::anyhow!(
                "Claude Code execution failed: {}",
                String::from_utf8_lossy(&output.stderr)
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_parallel_task (lines 867-901) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task with session context
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use conversation history for context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session
            .execute_with_context(&execution_task, &history)
            .await?;
        drop(session);
        drop(history);

        let success = !response.contains("error") && !response.contains("failed");

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success,
            output: response,
            error: if success {
                None
            } else {
                Some("Task execution failed".to_string())
            },
        })
    }

  ./crates/ccswarm/src/agent/persistent.rs:611-655 method orchestrate_task <-> ./crates/ccswarm/src/agent/persistent.rs:998-1021 method execute_iterative_development_step
  Similarity: 89.53%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:orchestrate_task (lines 611-655) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!(
            "🎭 Persistent agent orchestrating task: {}",
            task.description
        );

        // Leverage persistent session for complex task orchestration
        let plan = self.analyze_task(task).await?;

        // Execute plan with session persistence for context continuity
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("persistent_mode".to_string(), "true".to_string());
        context.insert("session_id".to_string(), {
            let session = self.session.lock().await;
            session.session_id().to_string()
        });

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results and preserve in session history
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Persistent agent step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Persistent orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({
                "orchestration": "persistent_success",
                "steps_completed": plan.steps.len(),
                "session_id": context.get("session_id")
            }),
            Duration::from_secs(15),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_development_step (lines 998-1021) ---[0m
    async fn execute_iterative_development_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_dev_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use session context for development
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative development step completed with session context".to_string())
            .add_output("development_response".to_string(), response)
            .add_output("session_context_used".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:867-901 method execute_parallel_task <-> ./crates/ccswarm/src/agent/persistent.rs:1081-1110 method execute_iterative_debugging_step
  Similarity: 94.56%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_parallel_task (lines 867-901) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task with session context
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use conversation history for context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session
            .execute_with_context(&execution_task, &history)
            .await?;
        drop(session);
        drop(history);

        let success = !response.contains("error") && !response.contains("failed");

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success,
            output: response,
            error: if success {
                None
            } else {
                Some("Task execution failed".to_string())
            },
        })
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_debugging_step (lines 1081-1110) ---[0m
    async fn execute_iterative_debugging_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_debug_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::High,
            TaskType::Bugfix,
        );

        // Use conversation history for debugging context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Analyze debugging progress
        let debug_progress = response.contains("identified")
            || response.contains("fixed")
            || response.contains("resolved");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative debugging step completed".to_string())
            .add_output("debug_response".to_string(), response)
            .add_output("progress_made".to_string(), debug_progress.to_string())
            .add_output("context_leveraged".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:611-655 method orchestrate_task <-> ./crates/ccswarm/src/agent/persistent.rs:1208-1231 function test_compact_identity_prompt
  Similarity: 88.71%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:orchestrate_task (lines 611-655) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!(
            "🎭 Persistent agent orchestrating task: {}",
            task.description
        );

        // Leverage persistent session for complex task orchestration
        let plan = self.analyze_task(task).await?;

        // Execute plan with session persistence for context continuity
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("persistent_mode".to_string(), "true".to_string());
        context.insert("session_id".to_string(), {
            let session = self.session.lock().await;
            session.session_id().to_string()
        });

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results and preserve in session history
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Persistent agent step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Persistent orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({
                "orchestration": "persistent_success",
                "steps_completed": plan.steps.len(),
                "session_id": context.get("session_id")
            }),
            Duration::from_secs(15),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_compact_identity_prompt (lines 1208-1231) ---[0m
    async fn test_compact_identity_prompt() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let prompt = agent.generate_compact_identity_prompt();

        // Verify it's much more compact than the original CLAUDE.md approach
        assert!(prompt.len() < 500); // Much less than 2000+ tokens
        assert!(prompt.contains("🤖 AGENT:"));
        assert!(prompt.contains("Frontend"));
    }

  ./crates/ccswarm/src/agent/persistent.rs:867-901 method execute_parallel_task <-> ./crates/ccswarm/src/agent/persistent.rs:1113-1142 method execute_persistent_default_step
  Similarity: 91.95%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_parallel_task (lines 867-901) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task with session context
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use conversation history for context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session
            .execute_with_context(&execution_task, &history)
            .await?;
        drop(session);
        drop(history);

        let success = !response.contains("error") && !response.contains("failed");

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success,
            output: response,
            error: if success {
                None
            } else {
                Some("Task execution failed".to_string())
            },
        })
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_default_step (lines 1113-1142) ---[0m
    async fn execute_persistent_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("persistent_default_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Execute with full session context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent default step completed".to_string())
            .add_output("response".to_string(), response)
            .add_output("session_context_preserved".to_string(), "true".to_string())
            .add_output("task_id".to_string(), task_id))
    }

  ./crates/ccswarm/src/agent/persistent.rs:867-901 method execute_parallel_task <-> ./crates/ccswarm/src/agent/persistent.rs:1051-1078 method execute_persistent_test_step
  Similarity: 94.30%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_parallel_task (lines 867-901) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task with session context
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use conversation history for context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session
            .execute_with_context(&execution_task, &history)
            .await?;
        drop(session);
        drop(history);

        let success = !response.contains("error") && !response.contains("failed");

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success,
            output: response,
            error: if success {
                None
            } else {
                Some("Task execution failed".to_string())
            },
        })
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_test_step (lines 1051-1078) ---[0m
    async fn execute_persistent_test_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_test_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Testing,
        );

        // Execute tests with session persistence
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Parse test results from response
        let test_success = response.contains("✅") || response.contains("passed");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent test execution completed".to_string())
            .add_output("test_response".to_string(), response)
            .add_output("test_passed".to_string(), test_success.to_string())
            .add_output("persistent_execution".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:867-901 method execute_parallel_task <-> ./crates/ccswarm/src/agent/persistent.rs:961-995 method execute_context_analysis_step
  Similarity: 84.43%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_parallel_task (lines 867-901) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task with session context
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use conversation history for context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session
            .execute_with_context(&execution_task, &history)
            .await?;
        drop(session);
        drop(history);

        let success = !response.contains("error") && !response.contains("failed");

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success,
            output: response,
            error: if success {
                None
            } else {
                Some("Task execution failed".to_string())
            },
        })
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_context_analysis_step (lines 961-995) ---[0m
    async fn execute_context_analysis_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Analyze conversation history for relevant context
        let history = self.conversation_history.lock().await;
        let relevant_messages = history
            .iter()
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .count();

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Context analysis for task {} completed", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "history_messages".to_string(),
                relevant_messages.to_string(),
            ))
    }

  ./crates/ccswarm/src/agent/persistent.rs:88-133 method send_message <-> ./crates/ccswarm/src/agent/persistent.rs:1081-1110 method execute_iterative_debugging_step
  Similarity: 77.57%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_message (lines 88-133) ---[0m
    pub async fn send_message(&mut self, prompt: &str) -> Result<String> {
        self.last_activity = Instant::now();

        // For tests and simulation mode, return a mock response
        if !self.claude_config.use_real_api {
            return Ok(self.generate_mock_response(prompt));
        }

        let mut cmd = Command::new("claude");
        cmd.current_dir(&self.working_dir);

        // Add environment variables
        for (key, value) in &self.env_vars {
            cmd.env(key, value);
        }

        // Add Claude arguments
        cmd.arg("-p").arg(prompt);

        if self.claude_config.json_output {
            cmd.arg("--json");
        }

        if self.claude_config.dangerous_skip {
            cmd.arg("--dangerously-skip-permissions");
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.claude_config.think_mode {
            cmd.arg("--think").arg(think_mode.to_string());
        }

        let output = cmd
            .output()
            .await
            .context("Failed to execute Claude Code")?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).to_string())
        } else {
            Err(anyhow::anyhow!(
                "Claude Code execution failed: {}",
                String::from_utf8_lossy(&output.stderr)
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_debugging_step (lines 1081-1110) ---[0m
    async fn execute_iterative_debugging_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_debug_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::High,
            TaskType::Bugfix,
        );

        // Use conversation history for debugging context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Analyze debugging progress
        let debug_progress = response.contains("identified")
            || response.contains("fixed")
            || response.contains("resolved");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative debugging step completed".to_string())
            .add_output("debug_response".to_string(), response)
            .add_output("progress_made".to_string(), debug_progress.to_string())
            .add_output("context_leveraged".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:611-655 method orchestrate_task <-> ./crates/ccswarm/src/agent/persistent.rs:1024-1048 method execute_session_validation_step
  Similarity: 84.00%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:orchestrate_task (lines 611-655) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!(
            "🎭 Persistent agent orchestrating task: {}",
            task.description
        );

        // Leverage persistent session for complex task orchestration
        let plan = self.analyze_task(task).await?;

        // Execute plan with session persistence for context continuity
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("persistent_mode".to_string(), "true".to_string());
        context.insert("session_id".to_string(), {
            let session = self.session.lock().await;
            session.session_id().to_string()
        });

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results and preserve in session history
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Persistent agent step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Persistent orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({
                "orchestration": "persistent_success",
                "steps_completed": plan.steps.len(),
                "session_id": context.get("session_id")
            }),
            Duration::from_secs(15),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_session_validation_step (lines 1024-1048) ---[0m
    async fn execute_session_validation_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Validate session state and identity
        let identity_established = self.identity_established.load(Ordering::Relaxed);
        let history = self.conversation_history.lock().await;
        let session_health = !history.is_empty() && identity_established;

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Session validation completed for {}", session_id))
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "identity_established".to_string(),
                identity_established.to_string(),
            )
            .add_output("session_healthy".to_string(), session_health.to_string())
            .add_output("conversation_length".to_string(), history.len().to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:961-995 method execute_context_analysis_step <-> ./crates/ccswarm/src/agent/persistent.rs:1145-1176 method analyze_failure_patterns
  Similarity: 86.63%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_context_analysis_step (lines 961-995) ---[0m
    async fn execute_context_analysis_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Analyze conversation history for relevant context
        let history = self.conversation_history.lock().await;
        let relevant_messages = history
            .iter()
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .count();

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Context analysis for task {} completed", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "history_messages".to_string(),
                relevant_messages.to_string(),
            ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:analyze_failure_patterns (lines 1145-1176) ---[0m
    fn analyze_failure_patterns(
        &self,
        history: &VecDeque<ConversationMessage>,
        failed_steps: &[&StepResult],
    ) -> String {
        let mut patterns = Vec::new();

        // Check for context loss patterns
        if failed_steps
            .iter()
            .any(|step| step.errors.iter().any(|e| e.contains("context")))
        {
            patterns.push("context_loss");
        }

        // Check for identity drift in recent messages
        let recent_responses: Vec<_> = history
            .iter()
            .rev()
            .take(5)
            .filter(|msg| matches!(msg.message_type, MessageType::Response))
            .collect();

        if recent_responses
            .iter()
            .any(|msg| !msg.content.contains("🤖 AGENT:"))
        {
            patterns.push("identity_drift");
        }

        patterns.join(",")
    }

  ./crates/ccswarm/src/agent/persistent.rs:314-365 method establish_identity_once <-> ./crates/ccswarm/src/agent/persistent.rs:1081-1110 method execute_iterative_debugging_step
  Similarity: 70.59%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:establish_identity_once (lines 314-365) ---[0m
    pub async fn establish_identity_once(&self) -> Result<()> {
        if self.identity_established.load(Ordering::Relaxed) {
            tracing::debug!(
                "Identity already established for agent: {}",
                self.identity.agent_id
            );
            return Ok(());
        }

        tracing::info!(
            "Establishing identity for agent: {}",
            self.identity.agent_id
        );

        // Generate lightweight identity prompt (200 tokens vs 2000+ in original)
        let compact_prompt = self.generate_compact_identity_prompt();

        // Send identity establishment message
        let mut session = self.session.lock().await;
        let response = session.send_message(&compact_prompt).await?;

        // Verify response contains identity markers
        if !self.verify_identity_response(&response) {
            return Err(anyhow::anyhow!("Failed to establish agent identity"));
        }

        // Record in conversation history
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityEstablishment,
            content: compact_prompt,
            task_id: None,
        });
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::Response,
            content: response,
            task_id: None,
        });

        // Trim history if needed
        self.trim_history(&mut history);

        self.identity_established.store(true, Ordering::Relaxed);
        tracing::info!(
            "Identity established successfully for agent: {}",
            self.identity.agent_id
        );

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_debugging_step (lines 1081-1110) ---[0m
    async fn execute_iterative_debugging_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_debug_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::High,
            TaskType::Bugfix,
        );

        // Use conversation history for debugging context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Analyze debugging progress
        let debug_progress = response.contains("identified")
            || response.contains("fixed")
            || response.contains("resolved");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative debugging step completed".to_string())
            .add_output("debug_response".to_string(), response)
            .add_output("progress_made".to_string(), debug_progress.to_string())
            .add_output("context_leveraged".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:961-995 method execute_context_analysis_step <-> ./crates/ccswarm/src/agent/persistent.rs:1113-1142 method execute_persistent_default_step
  Similarity: 89.02%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_context_analysis_step (lines 961-995) ---[0m
    async fn execute_context_analysis_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Analyze conversation history for relevant context
        let history = self.conversation_history.lock().await;
        let relevant_messages = history
            .iter()
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .count();

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Context analysis for task {} completed", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "history_messages".to_string(),
                relevant_messages.to_string(),
            ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_default_step (lines 1113-1142) ---[0m
    async fn execute_persistent_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("persistent_default_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Execute with full session context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent default step completed".to_string())
            .add_output("response".to_string(), response)
            .add_output("session_context_preserved".to_string(), "true".to_string())
            .add_output("task_id".to_string(), task_id))
    }

  ./crates/ccswarm/src/agent/persistent.rs:88-133 method send_message <-> ./crates/ccswarm/src/agent/persistent.rs:1051-1078 method execute_persistent_test_step
  Similarity: 77.60%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_message (lines 88-133) ---[0m
    pub async fn send_message(&mut self, prompt: &str) -> Result<String> {
        self.last_activity = Instant::now();

        // For tests and simulation mode, return a mock response
        if !self.claude_config.use_real_api {
            return Ok(self.generate_mock_response(prompt));
        }

        let mut cmd = Command::new("claude");
        cmd.current_dir(&self.working_dir);

        // Add environment variables
        for (key, value) in &self.env_vars {
            cmd.env(key, value);
        }

        // Add Claude arguments
        cmd.arg("-p").arg(prompt);

        if self.claude_config.json_output {
            cmd.arg("--json");
        }

        if self.claude_config.dangerous_skip {
            cmd.arg("--dangerously-skip-permissions");
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.claude_config.think_mode {
            cmd.arg("--think").arg(think_mode.to_string());
        }

        let output = cmd
            .output()
            .await
            .context("Failed to execute Claude Code")?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).to_string())
        } else {
            Err(anyhow::anyhow!(
                "Claude Code execution failed: {}",
                String::from_utf8_lossy(&output.stderr)
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_test_step (lines 1051-1078) ---[0m
    async fn execute_persistent_test_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_test_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Testing,
        );

        // Execute tests with session persistence
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Parse test results from response
        let test_success = response.contains("✅") || response.contains("passed");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent test execution completed".to_string())
            .add_output("test_response".to_string(), response)
            .add_output("test_passed".to_string(), test_success.to_string())
            .add_output("persistent_execution".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:961-995 method execute_context_analysis_step <-> ./crates/ccswarm/src/agent/persistent.rs:1024-1048 method execute_session_validation_step
  Similarity: 95.27%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_context_analysis_step (lines 961-995) ---[0m
    async fn execute_context_analysis_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Analyze conversation history for relevant context
        let history = self.conversation_history.lock().await;
        let relevant_messages = history
            .iter()
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .count();

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Context analysis for task {} completed", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "history_messages".to_string(),
                relevant_messages.to_string(),
            ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_session_validation_step (lines 1024-1048) ---[0m
    async fn execute_session_validation_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Validate session state and identity
        let identity_established = self.identity_established.load(Ordering::Relaxed);
        let history = self.conversation_history.lock().await;
        let session_health = !history.is_empty() && identity_established;

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Session validation completed for {}", session_id))
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "identity_established".to_string(),
                identity_established.to_string(),
            )
            .add_output("session_healthy".to_string(), session_health.to_string())
            .add_output("conversation_length".to_string(), history.len().to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:1051-1078 method execute_persistent_test_step <-> ./crates/ccswarm/src/agent/persistent.rs:1081-1110 method execute_iterative_debugging_step
  Similarity: 97.80%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_test_step (lines 1051-1078) ---[0m
    async fn execute_persistent_test_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_test_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Testing,
        );

        // Execute tests with session persistence
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Parse test results from response
        let test_success = response.contains("✅") || response.contains("passed");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent test execution completed".to_string())
            .add_output("test_response".to_string(), response)
            .add_output("test_passed".to_string(), test_success.to_string())
            .add_output("persistent_execution".to_string(), "true".to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_debugging_step (lines 1081-1110) ---[0m
    async fn execute_iterative_debugging_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_debug_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::High,
            TaskType::Bugfix,
        );

        // Use conversation history for debugging context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Analyze debugging progress
        let debug_progress = response.contains("identified")
            || response.contains("fixed")
            || response.contains("resolved");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative debugging step completed".to_string())
            .add_output("debug_response".to_string(), response)
            .add_output("progress_made".to_string(), debug_progress.to_string())
            .add_output("context_leveraged".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:314-365 method establish_identity_once <-> ./crates/ccswarm/src/agent/persistent.rs:1051-1078 method execute_persistent_test_step
  Similarity: 70.59%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:establish_identity_once (lines 314-365) ---[0m
    pub async fn establish_identity_once(&self) -> Result<()> {
        if self.identity_established.load(Ordering::Relaxed) {
            tracing::debug!(
                "Identity already established for agent: {}",
                self.identity.agent_id
            );
            return Ok(());
        }

        tracing::info!(
            "Establishing identity for agent: {}",
            self.identity.agent_id
        );

        // Generate lightweight identity prompt (200 tokens vs 2000+ in original)
        let compact_prompt = self.generate_compact_identity_prompt();

        // Send identity establishment message
        let mut session = self.session.lock().await;
        let response = session.send_message(&compact_prompt).await?;

        // Verify response contains identity markers
        if !self.verify_identity_response(&response) {
            return Err(anyhow::anyhow!("Failed to establish agent identity"));
        }

        // Record in conversation history
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityEstablishment,
            content: compact_prompt,
            task_id: None,
        });
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::Response,
            content: response,
            task_id: None,
        });

        // Trim history if needed
        self.trim_history(&mut history);

        self.identity_established.store(true, Ordering::Relaxed);
        tracing::info!(
            "Identity established successfully for agent: {}",
            self.identity.agent_id
        );

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_test_step (lines 1051-1078) ---[0m
    async fn execute_persistent_test_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_test_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Testing,
        );

        // Execute tests with session persistence
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Parse test results from response
        let test_success = response.contains("✅") || response.contains("passed");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent test execution completed".to_string())
            .add_output("test_response".to_string(), response)
            .add_output("test_passed".to_string(), test_success.to_string())
            .add_output("persistent_execution".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:479-533 method execute_task_batch <-> ./crates/ccswarm/src/agent/persistent.rs:998-1021 method execute_iterative_development_step
  Similarity: 71.44%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_task_batch (lines 479-533) ---[0m
    pub async fn execute_task_batch(&mut self, tasks: Vec<Task>) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks for agent: {}",
            tasks.len(),
            self.identity.agent_id
        );

        // Ensure identity is established once for the entire batch
        self.establish_identity_once().await?;

        // Record batch start
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchStart,
            content: format!("Starting batch of {} tasks", tasks.len()),
            task_id: None,
        });
        drop(history);

        let mut results = Vec::new();

        for (i, task) in tasks.into_iter().enumerate() {
            tracing::debug!(
                "Executing batch task {}/{} for agent: {}",
                i + 1,
                results.capacity(),
                self.identity.agent_id
            );

            // For batch processing, we can be more efficient by reusing context
            let result = self.execute_task(task).await?;
            results.push(result);
        }

        // Record batch end
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::BatchEnd,
            content: format!("Completed batch of {} tasks", results.len()),
            task_id: None,
        });
        self.trim_history(&mut history);

        tracing::info!(
            "Batch execution completed for agent: {}",
            self.identity.agent_id
        );
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_development_step (lines 998-1021) ---[0m
    async fn execute_iterative_development_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_dev_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use session context for development
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative development step completed with session context".to_string())
            .add_output("development_response".to_string(), response)
            .add_output("session_context_used".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:961-995 method execute_context_analysis_step <-> ./crates/ccswarm/src/agent/persistent.rs:1081-1110 method execute_iterative_debugging_step
  Similarity: 86.07%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_context_analysis_step (lines 961-995) ---[0m
    async fn execute_context_analysis_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Analyze conversation history for relevant context
        let history = self.conversation_history.lock().await;
        let relevant_messages = history
            .iter()
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .count();

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Context analysis for task {} completed", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "history_messages".to_string(),
                relevant_messages.to_string(),
            ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_debugging_step (lines 1081-1110) ---[0m
    async fn execute_iterative_debugging_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_debug_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::High,
            TaskType::Bugfix,
        );

        // Use conversation history for debugging context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Analyze debugging progress
        let debug_progress = response.contains("identified")
            || response.contains("fixed")
            || response.contains("resolved");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative debugging step completed".to_string())
            .add_output("debug_response".to_string(), response)
            .add_output("progress_made".to_string(), debug_progress.to_string())
            .add_output("context_leveraged".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:611-655 method orchestrate_task <-> ./crates/ccswarm/src/agent/persistent.rs:1186-1205 function test_persistent_agent_creation
  Similarity: 85.66%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:orchestrate_task (lines 611-655) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!(
            "🎭 Persistent agent orchestrating task: {}",
            task.description
        );

        // Leverage persistent session for complex task orchestration
        let plan = self.analyze_task(task).await?;

        // Execute plan with session persistence for context continuity
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("persistent_mode".to_string(), "true".to_string());
        context.insert("session_id".to_string(), {
            let session = self.session.lock().await;
            session.session_id().to_string()
        });

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results and preserve in session history
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Persistent agent step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Persistent orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({
                "orchestration": "persistent_success",
                "steps_completed": plan.steps.len(),
                "session_id": context.get("session_id")
            }),
            Duration::from_secs(15),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_creation (lines 1186-1205) ---[0m
    async fn test_persistent_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        assert_eq!(agent.status, AgentStatus::Initializing);
        assert!(!agent.identity_established.load(Ordering::Relaxed));
    }

  ./crates/ccswarm/src/agent/persistent.rs:867-901 method execute_parallel_task <-> ./crates/ccswarm/src/agent/persistent.rs:1234-1261 function test_persistent_agent_orchestration
  Similarity: 87.23%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_parallel_task (lines 867-901) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task with session context
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use conversation history for context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session
            .execute_with_context(&execution_task, &history)
            .await?;
        drop(session);
        drop(history);

        let success = !response.contains("error") && !response.contains("failed");

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success,
            output: response,
            error: if success {
                None
            } else {
                Some("Task execution failed".to_string())
            },
        })
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_orchestration (lines 1234-1261) ---[0m
    async fn test_persistent_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-persistent-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let task = Task::new(
            "test_persistent_orchestration".to_string(),
            "Test persistent agent orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = agent.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
        assert!(plan.steps.iter().any(|step| step.name.contains("session")));
    }

  ./crates/ccswarm/src/agent/persistent.rs:1081-1110 method execute_iterative_debugging_step <-> ./crates/ccswarm/src/agent/persistent.rs:1113-1142 method execute_persistent_default_step
  Similarity: 91.21%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_debugging_step (lines 1081-1110) ---[0m
    async fn execute_iterative_debugging_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_debug_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::High,
            TaskType::Bugfix,
        );

        // Use conversation history for debugging context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Analyze debugging progress
        let debug_progress = response.contains("identified")
            || response.contains("fixed")
            || response.contains("resolved");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative debugging step completed".to_string())
            .add_output("debug_response".to_string(), response)
            .add_output("progress_made".to_string(), debug_progress.to_string())
            .add_output("context_leveraged".to_string(), "true".to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_default_step (lines 1113-1142) ---[0m
    async fn execute_persistent_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("persistent_default_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Execute with full session context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent default step completed".to_string())
            .add_output("response".to_string(), response)
            .add_output("session_context_preserved".to_string(), "true".to_string())
            .add_output("task_id".to_string(), task_id))
    }

  ./crates/ccswarm/src/agent/persistent.rs:314-365 method establish_identity_once <-> ./crates/ccswarm/src/agent/persistent.rs:1208-1231 function test_compact_identity_prompt
  Similarity: 71.98%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:establish_identity_once (lines 314-365) ---[0m
    pub async fn establish_identity_once(&self) -> Result<()> {
        if self.identity_established.load(Ordering::Relaxed) {
            tracing::debug!(
                "Identity already established for agent: {}",
                self.identity.agent_id
            );
            return Ok(());
        }

        tracing::info!(
            "Establishing identity for agent: {}",
            self.identity.agent_id
        );

        // Generate lightweight identity prompt (200 tokens vs 2000+ in original)
        let compact_prompt = self.generate_compact_identity_prompt();

        // Send identity establishment message
        let mut session = self.session.lock().await;
        let response = session.send_message(&compact_prompt).await?;

        // Verify response contains identity markers
        if !self.verify_identity_response(&response) {
            return Err(anyhow::anyhow!("Failed to establish agent identity"));
        }

        // Record in conversation history
        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityEstablishment,
            content: compact_prompt,
            task_id: None,
        });
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::Response,
            content: response,
            task_id: None,
        });

        // Trim history if needed
        self.trim_history(&mut history);

        self.identity_established.store(true, Ordering::Relaxed);
        tracing::info!(
            "Identity established successfully for agent: {}",
            self.identity.agent_id
        );

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_compact_identity_prompt (lines 1208-1231) ---[0m
    async fn test_compact_identity_prompt() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let prompt = agent.generate_compact_identity_prompt();

        // Verify it's much more compact than the original CLAUDE.md approach
        assert!(prompt.len() < 500); // Much less than 2000+ tokens
        assert!(prompt.contains("🤖 AGENT:"));
        assert!(prompt.contains("Frontend"));
    }

  ./crates/ccswarm/src/agent/persistent.rs:88-133 method send_message <-> ./crates/ccswarm/src/agent/persistent.rs:1113-1142 method execute_persistent_default_step
  Similarity: 71.65%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_message (lines 88-133) ---[0m
    pub async fn send_message(&mut self, prompt: &str) -> Result<String> {
        self.last_activity = Instant::now();

        // For tests and simulation mode, return a mock response
        if !self.claude_config.use_real_api {
            return Ok(self.generate_mock_response(prompt));
        }

        let mut cmd = Command::new("claude");
        cmd.current_dir(&self.working_dir);

        // Add environment variables
        for (key, value) in &self.env_vars {
            cmd.env(key, value);
        }

        // Add Claude arguments
        cmd.arg("-p").arg(prompt);

        if self.claude_config.json_output {
            cmd.arg("--json");
        }

        if self.claude_config.dangerous_skip {
            cmd.arg("--dangerously-skip-permissions");
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.claude_config.think_mode {
            cmd.arg("--think").arg(think_mode.to_string());
        }

        let output = cmd
            .output()
            .await
            .context("Failed to execute Claude Code")?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).to_string())
        } else {
            Err(anyhow::anyhow!(
                "Claude Code execution failed: {}",
                String::from_utf8_lossy(&output.stderr)
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_default_step (lines 1113-1142) ---[0m
    async fn execute_persistent_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("persistent_default_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Execute with full session context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent default step completed".to_string())
            .add_output("response".to_string(), response)
            .add_output("session_context_preserved".to_string(), "true".to_string())
            .add_output("task_id".to_string(), task_id))
    }

  ./crates/ccswarm/src/agent/persistent.rs:961-995 method execute_context_analysis_step <-> ./crates/ccswarm/src/agent/persistent.rs:1051-1078 method execute_persistent_test_step
  Similarity: 85.40%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_context_analysis_step (lines 961-995) ---[0m
    async fn execute_context_analysis_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Analyze conversation history for relevant context
        let history = self.conversation_history.lock().await;
        let relevant_messages = history
            .iter()
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .count();

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Context analysis for task {} completed", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "history_messages".to_string(),
                relevant_messages.to_string(),
            ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_test_step (lines 1051-1078) ---[0m
    async fn execute_persistent_test_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_test_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Testing,
        );

        // Execute tests with session persistence
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Parse test results from response
        let test_success = response.contains("✅") || response.contains("passed");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent test execution completed".to_string())
            .add_output("test_response".to_string(), response)
            .add_output("test_passed".to_string(), test_success.to_string())
            .add_output("persistent_execution".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:1113-1142 method execute_persistent_default_step <-> ./crates/ccswarm/src/agent/persistent.rs:1145-1176 method analyze_failure_patterns
  Similarity: 86.33%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_default_step (lines 1113-1142) ---[0m
    async fn execute_persistent_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("persistent_default_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Execute with full session context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent default step completed".to_string())
            .add_output("response".to_string(), response)
            .add_output("session_context_preserved".to_string(), "true".to_string())
            .add_output("task_id".to_string(), task_id))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:analyze_failure_patterns (lines 1145-1176) ---[0m
    fn analyze_failure_patterns(
        &self,
        history: &VecDeque<ConversationMessage>,
        failed_steps: &[&StepResult],
    ) -> String {
        let mut patterns = Vec::new();

        // Check for context loss patterns
        if failed_steps
            .iter()
            .any(|step| step.errors.iter().any(|e| e.contains("context")))
        {
            patterns.push("context_loss");
        }

        // Check for identity drift in recent messages
        let recent_responses: Vec<_> = history
            .iter()
            .rev()
            .take(5)
            .filter(|msg| matches!(msg.message_type, MessageType::Response))
            .collect();

        if recent_responses
            .iter()
            .any(|msg| !msg.content.contains("🤖 AGENT:"))
        {
            patterns.push("identity_drift");
        }

        patterns.join(",")
    }

  ./crates/ccswarm/src/agent/persistent.rs:867-901 method execute_parallel_task <-> ./crates/ccswarm/src/agent/persistent.rs:1145-1176 method analyze_failure_patterns
  Similarity: 79.54%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_parallel_task (lines 867-901) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task with session context
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use conversation history for context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session
            .execute_with_context(&execution_task, &history)
            .await?;
        drop(session);
        drop(history);

        let success = !response.contains("error") && !response.contains("failed");

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success,
            output: response,
            error: if success {
                None
            } else {
                Some("Task execution failed".to_string())
            },
        })
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:analyze_failure_patterns (lines 1145-1176) ---[0m
    fn analyze_failure_patterns(
        &self,
        history: &VecDeque<ConversationMessage>,
        failed_steps: &[&StepResult],
    ) -> String {
        let mut patterns = Vec::new();

        // Check for context loss patterns
        if failed_steps
            .iter()
            .any(|step| step.errors.iter().any(|e| e.contains("context")))
        {
            patterns.push("context_loss");
        }

        // Check for identity drift in recent messages
        let recent_responses: Vec<_> = history
            .iter()
            .rev()
            .take(5)
            .filter(|msg| matches!(msg.message_type, MessageType::Response))
            .collect();

        if recent_responses
            .iter()
            .any(|msg| !msg.content.contains("🤖 AGENT:"))
        {
            patterns.push("identity_drift");
        }

        patterns.join(",")
    }

  ./crates/ccswarm/src/agent/persistent.rs:88-133 method send_message <-> ./crates/ccswarm/src/agent/persistent.rs:1208-1231 function test_compact_identity_prompt
  Similarity: 75.95%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_message (lines 88-133) ---[0m
    pub async fn send_message(&mut self, prompt: &str) -> Result<String> {
        self.last_activity = Instant::now();

        // For tests and simulation mode, return a mock response
        if !self.claude_config.use_real_api {
            return Ok(self.generate_mock_response(prompt));
        }

        let mut cmd = Command::new("claude");
        cmd.current_dir(&self.working_dir);

        // Add environment variables
        for (key, value) in &self.env_vars {
            cmd.env(key, value);
        }

        // Add Claude arguments
        cmd.arg("-p").arg(prompt);

        if self.claude_config.json_output {
            cmd.arg("--json");
        }

        if self.claude_config.dangerous_skip {
            cmd.arg("--dangerously-skip-permissions");
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.claude_config.think_mode {
            cmd.arg("--think").arg(think_mode.to_string());
        }

        let output = cmd
            .output()
            .await
            .context("Failed to execute Claude Code")?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).to_string())
        } else {
            Err(anyhow::anyhow!(
                "Claude Code execution failed: {}",
                String::from_utf8_lossy(&output.stderr)
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_compact_identity_prompt (lines 1208-1231) ---[0m
    async fn test_compact_identity_prompt() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let prompt = agent.generate_compact_identity_prompt();

        // Verify it's much more compact than the original CLAUDE.md approach
        assert!(prompt.len() < 500); // Much less than 2000+ tokens
        assert!(prompt.contains("🤖 AGENT:"));
        assert!(prompt.contains("Frontend"));
    }

  ./crates/ccswarm/src/agent/persistent.rs:867-901 method execute_parallel_task <-> ./crates/ccswarm/src/agent/persistent.rs:998-1021 method execute_iterative_development_step
  Similarity: 90.08%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_parallel_task (lines 867-901) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task with session context
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use conversation history for context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session
            .execute_with_context(&execution_task, &history)
            .await?;
        drop(session);
        drop(history);

        let success = !response.contains("error") && !response.contains("failed");

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success,
            output: response,
            error: if success {
                None
            } else {
                Some("Task execution failed".to_string())
            },
        })
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_development_step (lines 998-1021) ---[0m
    async fn execute_iterative_development_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_dev_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use session context for development
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative development step completed with session context".to_string())
            .add_output("development_response".to_string(), response)
            .add_output("session_context_used".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:1051-1078 method execute_persistent_test_step <-> ./crates/ccswarm/src/agent/persistent.rs:1113-1142 method execute_persistent_default_step
  Similarity: 90.98%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_test_step (lines 1051-1078) ---[0m
    async fn execute_persistent_test_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_test_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Testing,
        );

        // Execute tests with session persistence
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Parse test results from response
        let test_success = response.contains("✅") || response.contains("passed");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent test execution completed".to_string())
            .add_output("test_response".to_string(), response)
            .add_output("test_passed".to_string(), test_success.to_string())
            .add_output("persistent_execution".to_string(), "true".to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_default_step (lines 1113-1142) ---[0m
    async fn execute_persistent_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("persistent_default_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Execute with full session context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent default step completed".to_string())
            .add_output("response".to_string(), response)
            .add_output("session_context_preserved".to_string(), "true".to_string())
            .add_output("task_id".to_string(), task_id))
    }

  ./crates/ccswarm/src/agent/persistent.rs:136-164 method generate_mock_response <-> ./crates/ccswarm/src/agent/persistent.rs:961-995 method execute_context_analysis_step
  Similarity: 81.47%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:generate_mock_response (lines 136-164) ---[0m
    fn generate_mock_response(&self, prompt: &str) -> String {
        // Check if this is an identity establishment prompt
        if prompt.contains("🤖 **AGENT IDENTITY**") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Identity established successfully",
                self.session_id,
                self.working_dir
                    .file_name()
                    .unwrap_or_default()
                    .to_string_lossy()
            );
        }

        // Generate a simple mock response for tasks
        if prompt.contains("## Current Task:") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Task within agent boundaries\n\nTask completed successfully in simulation mode.",
                self.session_id,
                self.working_dir.file_name().unwrap_or_default().to_string_lossy()
            );
        }

        // Default mock response
        format!(
            "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: General response\n\nMock response generated for testing.",
            self.session_id,
            self.working_dir.file_name().unwrap_or_default().to_string_lossy()
        )
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_context_analysis_step (lines 961-995) ---[0m
    async fn execute_context_analysis_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Analyze conversation history for relevant context
        let history = self.conversation_history.lock().await;
        let relevant_messages = history
            .iter()
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .count();

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Context analysis for task {} completed", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "history_messages".to_string(),
                relevant_messages.to_string(),
            ))
    }

  ./crates/ccswarm/src/agent/persistent.rs:961-995 method execute_context_analysis_step <-> ./crates/ccswarm/src/agent/persistent.rs:998-1021 method execute_iterative_development_step
  Similarity: 88.06%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_context_analysis_step (lines 961-995) ---[0m
    async fn execute_context_analysis_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Analyze conversation history for relevant context
        let history = self.conversation_history.lock().await;
        let relevant_messages = history
            .iter()
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .count();

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Context analysis for task {} completed", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "history_messages".to_string(),
                relevant_messages.to_string(),
            ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_development_step (lines 998-1021) ---[0m
    async fn execute_iterative_development_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_dev_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use session context for development
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative development step completed with session context".to_string())
            .add_output("development_response".to_string(), response)
            .add_output("session_context_used".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:998-1021 method execute_iterative_development_step <-> ./crates/ccswarm/src/agent/persistent.rs:1113-1142 method execute_persistent_default_step
  Similarity: 95.86%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_development_step (lines 998-1021) ---[0m
    async fn execute_iterative_development_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_dev_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use session context for development
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative development step completed with session context".to_string())
            .add_output("development_response".to_string(), response)
            .add_output("session_context_used".to_string(), "true".to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_default_step (lines 1113-1142) ---[0m
    async fn execute_persistent_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("persistent_default_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Execute with full session context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent default step completed".to_string())
            .add_output("response".to_string(), response)
            .add_output("session_context_preserved".to_string(), "true".to_string())
            .add_output("task_id".to_string(), task_id))
    }

  ./crates/ccswarm/src/agent/persistent.rs:1081-1110 method execute_iterative_debugging_step <-> ./crates/ccswarm/src/agent/persistent.rs:1145-1176 method analyze_failure_patterns
  Similarity: 83.28%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_debugging_step (lines 1081-1110) ---[0m
    async fn execute_iterative_debugging_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_debug_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::High,
            TaskType::Bugfix,
        );

        // Use conversation history for debugging context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Analyze debugging progress
        let debug_progress = response.contains("identified")
            || response.contains("fixed")
            || response.contains("resolved");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative debugging step completed".to_string())
            .add_output("debug_response".to_string(), response)
            .add_output("progress_made".to_string(), debug_progress.to_string())
            .add_output("context_leveraged".to_string(), "true".to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:analyze_failure_patterns (lines 1145-1176) ---[0m
    fn analyze_failure_patterns(
        &self,
        history: &VecDeque<ConversationMessage>,
        failed_steps: &[&StepResult],
    ) -> String {
        let mut patterns = Vec::new();

        // Check for context loss patterns
        if failed_steps
            .iter()
            .any(|step| step.errors.iter().any(|e| e.contains("context")))
        {
            patterns.push("context_loss");
        }

        // Check for identity drift in recent messages
        let recent_responses: Vec<_> = history
            .iter()
            .rev()
            .take(5)
            .filter(|msg| matches!(msg.message_type, MessageType::Response))
            .collect();

        if recent_responses
            .iter()
            .any(|msg| !msg.content.contains("🤖 AGENT:"))
        {
            patterns.push("identity_drift");
        }

        patterns.join(",")
    }

  ./crates/ccswarm/src/agent/persistent.rs:136-164 method generate_mock_response <-> ./crates/ccswarm/src/agent/persistent.rs:1145-1176 method analyze_failure_patterns
  Similarity: 84.22%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:generate_mock_response (lines 136-164) ---[0m
    fn generate_mock_response(&self, prompt: &str) -> String {
        // Check if this is an identity establishment prompt
        if prompt.contains("🤖 **AGENT IDENTITY**") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Identity established successfully",
                self.session_id,
                self.working_dir
                    .file_name()
                    .unwrap_or_default()
                    .to_string_lossy()
            );
        }

        // Generate a simple mock response for tasks
        if prompt.contains("## Current Task:") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Task within agent boundaries\n\nTask completed successfully in simulation mode.",
                self.session_id,
                self.working_dir.file_name().unwrap_or_default().to_string_lossy()
            );
        }

        // Default mock response
        format!(
            "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: General response\n\nMock response generated for testing.",
            self.session_id,
            self.working_dir.file_name().unwrap_or_default().to_string_lossy()
        )
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:analyze_failure_patterns (lines 1145-1176) ---[0m
    fn analyze_failure_patterns(
        &self,
        history: &VecDeque<ConversationMessage>,
        failed_steps: &[&StepResult],
    ) -> String {
        let mut patterns = Vec::new();

        // Check for context loss patterns
        if failed_steps
            .iter()
            .any(|step| step.errors.iter().any(|e| e.contains("context")))
        {
            patterns.push("context_loss");
        }

        // Check for identity drift in recent messages
        let recent_responses: Vec<_> = history
            .iter()
            .rev()
            .take(5)
            .filter(|msg| matches!(msg.message_type, MessageType::Response))
            .collect();

        if recent_responses
            .iter()
            .any(|msg| !msg.content.contains("🤖 AGENT:"))
        {
            patterns.push("identity_drift");
        }

        patterns.join(",")
    }

  ./crates/ccswarm/src/agent/persistent.rs:1024-1048 method execute_session_validation_step <-> ./crates/ccswarm/src/agent/persistent.rs:1145-1176 method analyze_failure_patterns
  Similarity: 89.96%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_session_validation_step (lines 1024-1048) ---[0m
    async fn execute_session_validation_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Validate session state and identity
        let identity_established = self.identity_established.load(Ordering::Relaxed);
        let history = self.conversation_history.lock().await;
        let session_health = !history.is_empty() && identity_established;

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Session validation completed for {}", session_id))
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "identity_established".to_string(),
                identity_established.to_string(),
            )
            .add_output("session_healthy".to_string(), session_health.to_string())
            .add_output("conversation_length".to_string(), history.len().to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:analyze_failure_patterns (lines 1145-1176) ---[0m
    fn analyze_failure_patterns(
        &self,
        history: &VecDeque<ConversationMessage>,
        failed_steps: &[&StepResult],
    ) -> String {
        let mut patterns = Vec::new();

        // Check for context loss patterns
        if failed_steps
            .iter()
            .any(|step| step.errors.iter().any(|e| e.contains("context")))
        {
            patterns.push("context_loss");
        }

        // Check for identity drift in recent messages
        let recent_responses: Vec<_> = history
            .iter()
            .rev()
            .take(5)
            .filter(|msg| matches!(msg.message_type, MessageType::Response))
            .collect();

        if recent_responses
            .iter()
            .any(|msg| !msg.content.contains("🤖 AGENT:"))
        {
            patterns.push("identity_drift");
        }

        patterns.join(",")
    }

  ./crates/ccswarm/src/agent/persistent.rs:1113-1142 method execute_persistent_default_step <-> ./crates/ccswarm/src/agent/persistent.rs:1234-1261 function test_persistent_agent_orchestration
  Similarity: 88.38%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_default_step (lines 1113-1142) ---[0m
    async fn execute_persistent_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("persistent_default_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Execute with full session context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent default step completed".to_string())
            .add_output("response".to_string(), response)
            .add_output("session_context_preserved".to_string(), "true".to_string())
            .add_output("task_id".to_string(), task_id))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_orchestration (lines 1234-1261) ---[0m
    async fn test_persistent_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-persistent-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let task = Task::new(
            "test_persistent_orchestration".to_string(),
            "Test persistent agent orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = agent.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
        assert!(plan.steps.iter().any(|step| step.name.contains("session")));
    }

  ./crates/ccswarm/src/agent/persistent.rs:1081-1110 method execute_iterative_debugging_step <-> ./crates/ccswarm/src/agent/persistent.rs:1234-1261 function test_persistent_agent_orchestration
  Similarity: 87.80%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_debugging_step (lines 1081-1110) ---[0m
    async fn execute_iterative_debugging_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_debug_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::High,
            TaskType::Bugfix,
        );

        // Use conversation history for debugging context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Analyze debugging progress
        let debug_progress = response.contains("identified")
            || response.contains("fixed")
            || response.contains("resolved");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative debugging step completed".to_string())
            .add_output("debug_response".to_string(), response)
            .add_output("progress_made".to_string(), debug_progress.to_string())
            .add_output("context_leveraged".to_string(), "true".to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_orchestration (lines 1234-1261) ---[0m
    async fn test_persistent_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-persistent-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let task = Task::new(
            "test_persistent_orchestration".to_string(),
            "Test persistent agent orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = agent.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
        assert!(plan.steps.iter().any(|step| step.name.contains("session")));
    }

  ./crates/ccswarm/src/agent/persistent.rs:536-556 method send_identity_reminder <-> ./crates/ccswarm/src/agent/persistent.rs:961-995 method execute_context_analysis_step
  Similarity: 90.29%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_identity_reminder (lines 536-556) ---[0m
    async fn send_identity_reminder(&self) -> Result<()> {
        let reminder_prompt = format!(
            "🤖 IDENTITY REMINDER: You are a {} agent working in {}. Maintain specialization boundaries.",
            self.identity.specialization.name(),
            self.identity.workspace_path.file_name().unwrap_or_default().to_string_lossy()
        );

        let mut session = self.session.lock().await;
        let _response = session.send_message(&reminder_prompt).await?;

        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityReminder,
            content: reminder_prompt,
            task_id: None,
        });
        self.trim_history(&mut history);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_context_analysis_step (lines 961-995) ---[0m
    async fn execute_context_analysis_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Analyze conversation history for relevant context
        let history = self.conversation_history.lock().await;
        let relevant_messages = history
            .iter()
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .count();

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Context analysis for task {} completed", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "history_messages".to_string(),
                relevant_messages.to_string(),
            ))
    }

  ./crates/ccswarm/src/agent/persistent.rs:998-1021 method execute_iterative_development_step <-> ./crates/ccswarm/src/agent/persistent.rs:1081-1110 method execute_iterative_debugging_step
  Similarity: 93.38%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_development_step (lines 998-1021) ---[0m
    async fn execute_iterative_development_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_dev_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use session context for development
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative development step completed with session context".to_string())
            .add_output("development_response".to_string(), response)
            .add_output("session_context_used".to_string(), "true".to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_debugging_step (lines 1081-1110) ---[0m
    async fn execute_iterative_debugging_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_debug_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::High,
            TaskType::Bugfix,
        );

        // Use conversation history for debugging context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Analyze debugging progress
        let debug_progress = response.contains("identified")
            || response.contains("fixed")
            || response.contains("resolved");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative debugging step completed".to_string())
            .add_output("debug_response".to_string(), response)
            .add_output("progress_made".to_string(), debug_progress.to_string())
            .add_output("context_leveraged".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:867-901 method execute_parallel_task <-> ./crates/ccswarm/src/agent/persistent.rs:1208-1231 function test_compact_identity_prompt
  Similarity: 84.75%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_parallel_task (lines 867-901) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task with session context
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use conversation history for context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session
            .execute_with_context(&execution_task, &history)
            .await?;
        drop(session);
        drop(history);

        let success = !response.contains("error") && !response.contains("failed");

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success,
            output: response,
            error: if success {
                None
            } else {
                Some("Task execution failed".to_string())
            },
        })
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_compact_identity_prompt (lines 1208-1231) ---[0m
    async fn test_compact_identity_prompt() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let prompt = agent.generate_compact_identity_prompt();

        // Verify it's much more compact than the original CLAUDE.md approach
        assert!(prompt.len() < 500); // Much less than 2000+ tokens
        assert!(prompt.contains("🤖 AGENT:"));
        assert!(prompt.contains("Frontend"));
    }

  ./crates/ccswarm/src/agent/persistent.rs:867-901 method execute_parallel_task <-> ./crates/ccswarm/src/agent/persistent.rs:1024-1048 method execute_session_validation_step
  Similarity: 83.28%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_parallel_task (lines 867-901) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task with session context
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use conversation history for context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session
            .execute_with_context(&execution_task, &history)
            .await?;
        drop(session);
        drop(history);

        let success = !response.contains("error") && !response.contains("failed");

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success,
            output: response,
            error: if success {
                None
            } else {
                Some("Task execution failed".to_string())
            },
        })
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_session_validation_step (lines 1024-1048) ---[0m
    async fn execute_session_validation_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Validate session state and identity
        let identity_established = self.identity_established.load(Ordering::Relaxed);
        let history = self.conversation_history.lock().await;
        let session_health = !history.is_empty() && identity_established;

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Session validation completed for {}", session_id))
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "identity_established".to_string(),
                identity_established.to_string(),
            )
            .add_output("session_healthy".to_string(), session_health.to_string())
            .add_output("conversation_length".to_string(), history.len().to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:1051-1078 method execute_persistent_test_step <-> ./crates/ccswarm/src/agent/persistent.rs:1145-1176 method analyze_failure_patterns
  Similarity: 82.47%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_test_step (lines 1051-1078) ---[0m
    async fn execute_persistent_test_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_test_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Testing,
        );

        // Execute tests with session persistence
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Parse test results from response
        let test_success = response.contains("✅") || response.contains("passed");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent test execution completed".to_string())
            .add_output("test_response".to_string(), response)
            .add_output("test_passed".to_string(), test_success.to_string())
            .add_output("persistent_execution".to_string(), "true".to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:analyze_failure_patterns (lines 1145-1176) ---[0m
    fn analyze_failure_patterns(
        &self,
        history: &VecDeque<ConversationMessage>,
        failed_steps: &[&StepResult],
    ) -> String {
        let mut patterns = Vec::new();

        // Check for context loss patterns
        if failed_steps
            .iter()
            .any(|step| step.errors.iter().any(|e| e.contains("context")))
        {
            patterns.push("context_loss");
        }

        // Check for identity drift in recent messages
        let recent_responses: Vec<_> = history
            .iter()
            .rev()
            .take(5)
            .filter(|msg| matches!(msg.message_type, MessageType::Response))
            .collect();

        if recent_responses
            .iter()
            .any(|msg| !msg.content.contains("🤖 AGENT:"))
        {
            patterns.push("identity_drift");
        }

        patterns.join(",")
    }

  ./crates/ccswarm/src/agent/persistent.rs:1051-1078 method execute_persistent_test_step <-> ./crates/ccswarm/src/agent/persistent.rs:1234-1261 function test_persistent_agent_orchestration
  Similarity: 87.72%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_test_step (lines 1051-1078) ---[0m
    async fn execute_persistent_test_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_test_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Testing,
        );

        // Execute tests with session persistence
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Parse test results from response
        let test_success = response.contains("✅") || response.contains("passed");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent test execution completed".to_string())
            .add_output("test_response".to_string(), response)
            .add_output("test_passed".to_string(), test_success.to_string())
            .add_output("persistent_execution".to_string(), "true".to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_orchestration (lines 1234-1261) ---[0m
    async fn test_persistent_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-persistent-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let task = Task::new(
            "test_persistent_orchestration".to_string(),
            "Test persistent agent orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = agent.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
        assert!(plan.steps.iter().any(|step| step.name.contains("session")));
    }

  ./crates/ccswarm/src/agent/persistent.rs:1024-1048 method execute_session_validation_step <-> ./crates/ccswarm/src/agent/persistent.rs:1113-1142 method execute_persistent_default_step
  Similarity: 89.06%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_session_validation_step (lines 1024-1048) ---[0m
    async fn execute_session_validation_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Validate session state and identity
        let identity_established = self.identity_established.load(Ordering::Relaxed);
        let history = self.conversation_history.lock().await;
        let session_health = !history.is_empty() && identity_established;

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Session validation completed for {}", session_id))
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "identity_established".to_string(),
                identity_established.to_string(),
            )
            .add_output("session_healthy".to_string(), session_health.to_string())
            .add_output("conversation_length".to_string(), history.len().to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_default_step (lines 1113-1142) ---[0m
    async fn execute_persistent_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("persistent_default_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Execute with full session context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent default step completed".to_string())
            .add_output("response".to_string(), response)
            .add_output("session_context_preserved".to_string(), "true".to_string())
            .add_output("task_id".to_string(), task_id))
    }

  ./crates/ccswarm/src/agent/persistent.rs:998-1021 method execute_iterative_development_step <-> ./crates/ccswarm/src/agent/persistent.rs:1051-1078 method execute_persistent_test_step
  Similarity: 93.16%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_development_step (lines 998-1021) ---[0m
    async fn execute_iterative_development_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_dev_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use session context for development
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative development step completed with session context".to_string())
            .add_output("development_response".to_string(), response)
            .add_output("session_context_used".to_string(), "true".to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_test_step (lines 1051-1078) ---[0m
    async fn execute_persistent_test_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_test_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Testing,
        );

        // Execute tests with session persistence
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Parse test results from response
        let test_success = response.contains("✅") || response.contains("passed");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent test execution completed".to_string())
            .add_output("test_response".to_string(), response)
            .add_output("test_passed".to_string(), test_success.to_string())
            .add_output("persistent_execution".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:961-995 method execute_context_analysis_step <-> ./crates/ccswarm/src/agent/persistent.rs:1234-1261 function test_persistent_agent_orchestration
  Similarity: 76.53%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_context_analysis_step (lines 961-995) ---[0m
    async fn execute_context_analysis_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Analyze conversation history for relevant context
        let history = self.conversation_history.lock().await;
        let relevant_messages = history
            .iter()
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .count();

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Context analysis for task {} completed", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "history_messages".to_string(),
                relevant_messages.to_string(),
            ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_orchestration (lines 1234-1261) ---[0m
    async fn test_persistent_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-persistent-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let task = Task::new(
            "test_persistent_orchestration".to_string(),
            "Test persistent agent orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = agent.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
        assert!(plan.steps.iter().any(|step| step.name.contains("session")));
    }

  ./crates/ccswarm/src/agent/persistent.rs:536-556 method send_identity_reminder <-> ./crates/ccswarm/src/agent/persistent.rs:867-901 method execute_parallel_task
  Similarity: 86.09%
  Classes: PersistentClaudeAgent <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_identity_reminder (lines 536-556) ---[0m
    async fn send_identity_reminder(&self) -> Result<()> {
        let reminder_prompt = format!(
            "🤖 IDENTITY REMINDER: You are a {} agent working in {}. Maintain specialization boundaries.",
            self.identity.specialization.name(),
            self.identity.workspace_path.file_name().unwrap_or_default().to_string_lossy()
        );

        let mut session = self.session.lock().await;
        let _response = session.send_message(&reminder_prompt).await?;

        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityReminder,
            content: reminder_prompt,
            task_id: None,
        });
        self.trim_history(&mut history);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_parallel_task (lines 867-901) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task with session context
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use conversation history for context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session
            .execute_with_context(&execution_task, &history)
            .await?;
        drop(session);
        drop(history);

        let success = !response.contains("error") && !response.contains("failed");

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success,
            output: response,
            error: if success {
                None
            } else {
                Some("Task execution failed".to_string())
            },
        })
    }

  ./crates/ccswarm/src/agent/persistent.rs:961-995 method execute_context_analysis_step <-> ./crates/ccswarm/src/agent/persistent.rs:1186-1205 function test_persistent_agent_creation
  Similarity: 87.14%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_context_analysis_step (lines 961-995) ---[0m
    async fn execute_context_analysis_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Analyze conversation history for relevant context
        let history = self.conversation_history.lock().await;
        let relevant_messages = history
            .iter()
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .count();

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Context analysis for task {} completed", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "history_messages".to_string(),
                relevant_messages.to_string(),
            ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_creation (lines 1186-1205) ---[0m
    async fn test_persistent_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        assert_eq!(agent.status, AgentStatus::Initializing);
        assert!(!agent.identity_established.load(Ordering::Relaxed));
    }

  ./crates/ccswarm/src/agent/persistent.rs:998-1021 method execute_iterative_development_step <-> ./crates/ccswarm/src/agent/persistent.rs:1145-1176 method analyze_failure_patterns
  Similarity: 84.98%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_development_step (lines 998-1021) ---[0m
    async fn execute_iterative_development_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_dev_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use session context for development
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative development step completed with session context".to_string())
            .add_output("development_response".to_string(), response)
            .add_output("session_context_used".to_string(), "true".to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:analyze_failure_patterns (lines 1145-1176) ---[0m
    fn analyze_failure_patterns(
        &self,
        history: &VecDeque<ConversationMessage>,
        failed_steps: &[&StepResult],
    ) -> String {
        let mut patterns = Vec::new();

        // Check for context loss patterns
        if failed_steps
            .iter()
            .any(|step| step.errors.iter().any(|e| e.contains("context")))
        {
            patterns.push("context_loss");
        }

        // Check for identity drift in recent messages
        let recent_responses: Vec<_> = history
            .iter()
            .rev()
            .take(5)
            .filter(|msg| matches!(msg.message_type, MessageType::Response))
            .collect();

        if recent_responses
            .iter()
            .any(|msg| !msg.content.contains("🤖 AGENT:"))
        {
            patterns.push("identity_drift");
        }

        patterns.join(",")
    }

  ./crates/ccswarm/src/agent/persistent.rs:1081-1110 method execute_iterative_debugging_step <-> ./crates/ccswarm/src/agent/persistent.rs:1208-1231 function test_compact_identity_prompt
  Similarity: 87.74%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_debugging_step (lines 1081-1110) ---[0m
    async fn execute_iterative_debugging_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_debug_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::High,
            TaskType::Bugfix,
        );

        // Use conversation history for debugging context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Analyze debugging progress
        let debug_progress = response.contains("identified")
            || response.contains("fixed")
            || response.contains("resolved");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative debugging step completed".to_string())
            .add_output("debug_response".to_string(), response)
            .add_output("progress_made".to_string(), debug_progress.to_string())
            .add_output("context_leveraged".to_string(), "true".to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_compact_identity_prompt (lines 1208-1231) ---[0m
    async fn test_compact_identity_prompt() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let prompt = agent.generate_compact_identity_prompt();

        // Verify it's much more compact than the original CLAUDE.md approach
        assert!(prompt.len() < 500); // Much less than 2000+ tokens
        assert!(prompt.contains("🤖 AGENT:"));
        assert!(prompt.contains("Frontend"));
    }

  ./crates/ccswarm/src/agent/persistent.rs:1208-1231 function test_compact_identity_prompt <-> ./crates/ccswarm/src/agent/persistent.rs:1234-1261 function test_persistent_agent_orchestration
  Similarity: 90.83%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_compact_identity_prompt (lines 1208-1231) ---[0m
    async fn test_compact_identity_prompt() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let prompt = agent.generate_compact_identity_prompt();

        // Verify it's much more compact than the original CLAUDE.md approach
        assert!(prompt.len() < 500); // Much less than 2000+ tokens
        assert!(prompt.contains("🤖 AGENT:"));
        assert!(prompt.contains("Frontend"));
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_orchestration (lines 1234-1261) ---[0m
    async fn test_persistent_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-persistent-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let task = Task::new(
            "test_persistent_orchestration".to_string(),
            "Test persistent agent orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = agent.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
        assert!(plan.steps.iter().any(|step| step.name.contains("session")));
    }

  ./crates/ccswarm/src/agent/persistent.rs:1024-1048 method execute_session_validation_step <-> ./crates/ccswarm/src/agent/persistent.rs:1081-1110 method execute_iterative_debugging_step
  Similarity: 85.84%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_session_validation_step (lines 1024-1048) ---[0m
    async fn execute_session_validation_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Validate session state and identity
        let identity_established = self.identity_established.load(Ordering::Relaxed);
        let history = self.conversation_history.lock().await;
        let session_health = !history.is_empty() && identity_established;

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Session validation completed for {}", session_id))
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "identity_established".to_string(),
                identity_established.to_string(),
            )
            .add_output("session_healthy".to_string(), session_health.to_string())
            .add_output("conversation_length".to_string(), history.len().to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_debugging_step (lines 1081-1110) ---[0m
    async fn execute_iterative_debugging_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_debug_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::High,
            TaskType::Bugfix,
        );

        // Use conversation history for debugging context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Analyze debugging progress
        let debug_progress = response.contains("identified")
            || response.contains("fixed")
            || response.contains("resolved");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative debugging step completed".to_string())
            .add_output("debug_response".to_string(), response)
            .add_output("progress_made".to_string(), debug_progress.to_string())
            .add_output("context_leveraged".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:1113-1142 method execute_persistent_default_step <-> ./crates/ccswarm/src/agent/persistent.rs:1208-1231 function test_compact_identity_prompt
  Similarity: 86.80%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_default_step (lines 1113-1142) ---[0m
    async fn execute_persistent_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("persistent_default_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Execute with full session context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent default step completed".to_string())
            .add_output("response".to_string(), response)
            .add_output("session_context_preserved".to_string(), "true".to_string())
            .add_output("task_id".to_string(), task_id))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_compact_identity_prompt (lines 1208-1231) ---[0m
    async fn test_compact_identity_prompt() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let prompt = agent.generate_compact_identity_prompt();

        // Verify it's much more compact than the original CLAUDE.md approach
        assert!(prompt.len() < 500); // Much less than 2000+ tokens
        assert!(prompt.contains("🤖 AGENT:"));
        assert!(prompt.contains("Frontend"));
    }

  ./crates/ccswarm/src/agent/persistent.rs:867-901 method execute_parallel_task <-> ./crates/ccswarm/src/agent/persistent.rs:1186-1205 function test_persistent_agent_creation
  Similarity: 83.24%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_parallel_task (lines 867-901) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task with session context
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use conversation history for context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session
            .execute_with_context(&execution_task, &history)
            .await?;
        drop(session);
        drop(history);

        let success = !response.contains("error") && !response.contains("failed");

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success,
            output: response,
            error: if success {
                None
            } else {
                Some("Task execution failed".to_string())
            },
        })
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_creation (lines 1186-1205) ---[0m
    async fn test_persistent_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        assert_eq!(agent.status, AgentStatus::Initializing);
        assert!(!agent.identity_established.load(Ordering::Relaxed));
    }

  ./crates/ccswarm/src/agent/persistent.rs:136-164 method generate_mock_response <-> ./crates/ccswarm/src/agent/persistent.rs:1024-1048 method execute_session_validation_step
  Similarity: 84.34%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:generate_mock_response (lines 136-164) ---[0m
    fn generate_mock_response(&self, prompt: &str) -> String {
        // Check if this is an identity establishment prompt
        if prompt.contains("🤖 **AGENT IDENTITY**") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Identity established successfully",
                self.session_id,
                self.working_dir
                    .file_name()
                    .unwrap_or_default()
                    .to_string_lossy()
            );
        }

        // Generate a simple mock response for tasks
        if prompt.contains("## Current Task:") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Task within agent boundaries\n\nTask completed successfully in simulation mode.",
                self.session_id,
                self.working_dir.file_name().unwrap_or_default().to_string_lossy()
            );
        }

        // Default mock response
        format!(
            "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: General response\n\nMock response generated for testing.",
            self.session_id,
            self.working_dir.file_name().unwrap_or_default().to_string_lossy()
        )
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_session_validation_step (lines 1024-1048) ---[0m
    async fn execute_session_validation_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Validate session state and identity
        let identity_established = self.identity_established.load(Ordering::Relaxed);
        let history = self.conversation_history.lock().await;
        let session_health = !history.is_empty() && identity_established;

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Session validation completed for {}", session_id))
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "identity_established".to_string(),
                identity_established.to_string(),
            )
            .add_output("session_healthy".to_string(), session_health.to_string())
            .add_output("conversation_length".to_string(), history.len().to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:536-556 method send_identity_reminder <-> ./crates/ccswarm/src/agent/persistent.rs:1113-1142 method execute_persistent_default_step
  Similarity: 89.19%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_identity_reminder (lines 536-556) ---[0m
    async fn send_identity_reminder(&self) -> Result<()> {
        let reminder_prompt = format!(
            "🤖 IDENTITY REMINDER: You are a {} agent working in {}. Maintain specialization boundaries.",
            self.identity.specialization.name(),
            self.identity.workspace_path.file_name().unwrap_or_default().to_string_lossy()
        );

        let mut session = self.session.lock().await;
        let _response = session.send_message(&reminder_prompt).await?;

        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityReminder,
            content: reminder_prompt,
            task_id: None,
        });
        self.trim_history(&mut history);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_default_step (lines 1113-1142) ---[0m
    async fn execute_persistent_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("persistent_default_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Execute with full session context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent default step completed".to_string())
            .add_output("response".to_string(), response)
            .add_output("session_context_preserved".to_string(), "true".to_string())
            .add_output("task_id".to_string(), task_id))
    }

  ./crates/ccswarm/src/agent/persistent.rs:536-556 method send_identity_reminder <-> ./crates/ccswarm/src/agent/persistent.rs:1081-1110 method execute_iterative_debugging_step
  Similarity: 88.92%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_identity_reminder (lines 536-556) ---[0m
    async fn send_identity_reminder(&self) -> Result<()> {
        let reminder_prompt = format!(
            "🤖 IDENTITY REMINDER: You are a {} agent working in {}. Maintain specialization boundaries.",
            self.identity.specialization.name(),
            self.identity.workspace_path.file_name().unwrap_or_default().to_string_lossy()
        );

        let mut session = self.session.lock().await;
        let _response = session.send_message(&reminder_prompt).await?;

        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityReminder,
            content: reminder_prompt,
            task_id: None,
        });
        self.trim_history(&mut history);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_debugging_step (lines 1081-1110) ---[0m
    async fn execute_iterative_debugging_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_debug_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::High,
            TaskType::Bugfix,
        );

        // Use conversation history for debugging context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Analyze debugging progress
        let debug_progress = response.contains("identified")
            || response.contains("fixed")
            || response.contains("resolved");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative debugging step completed".to_string())
            .add_output("debug_response".to_string(), response)
            .add_output("progress_made".to_string(), debug_progress.to_string())
            .add_output("context_leveraged".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:1051-1078 method execute_persistent_test_step <-> ./crates/ccswarm/src/agent/persistent.rs:1208-1231 function test_compact_identity_prompt
  Similarity: 87.15%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_test_step (lines 1051-1078) ---[0m
    async fn execute_persistent_test_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_test_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Testing,
        );

        // Execute tests with session persistence
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Parse test results from response
        let test_success = response.contains("✅") || response.contains("passed");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent test execution completed".to_string())
            .add_output("test_response".to_string(), response)
            .add_output("test_passed".to_string(), test_success.to_string())
            .add_output("persistent_execution".to_string(), "true".to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_compact_identity_prompt (lines 1208-1231) ---[0m
    async fn test_compact_identity_prompt() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let prompt = agent.generate_compact_identity_prompt();

        // Verify it's much more compact than the original CLAUDE.md approach
        assert!(prompt.len() < 500); // Much less than 2000+ tokens
        assert!(prompt.contains("🤖 AGENT:"));
        assert!(prompt.contains("Frontend"));
    }

  ./crates/ccswarm/src/agent/persistent.rs:1024-1048 method execute_session_validation_step <-> ./crates/ccswarm/src/agent/persistent.rs:1051-1078 method execute_persistent_test_step
  Similarity: 85.15%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_session_validation_step (lines 1024-1048) ---[0m
    async fn execute_session_validation_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Validate session state and identity
        let identity_established = self.identity_established.load(Ordering::Relaxed);
        let history = self.conversation_history.lock().await;
        let session_health = !history.is_empty() && identity_established;

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Session validation completed for {}", session_id))
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "identity_established".to_string(),
                identity_established.to_string(),
            )
            .add_output("session_healthy".to_string(), session_health.to_string())
            .add_output("conversation_length".to_string(), history.len().to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_test_step (lines 1051-1078) ---[0m
    async fn execute_persistent_test_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_test_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Testing,
        );

        // Execute tests with session persistence
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Parse test results from response
        let test_success = response.contains("✅") || response.contains("passed");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent test execution completed".to_string())
            .add_output("test_response".to_string(), response)
            .add_output("test_passed".to_string(), test_success.to_string())
            .add_output("persistent_execution".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:1145-1176 method analyze_failure_patterns <-> ./crates/ccswarm/src/agent/persistent.rs:1234-1261 function test_persistent_agent_orchestration
  Similarity: 75.21%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:analyze_failure_patterns (lines 1145-1176) ---[0m
    fn analyze_failure_patterns(
        &self,
        history: &VecDeque<ConversationMessage>,
        failed_steps: &[&StepResult],
    ) -> String {
        let mut patterns = Vec::new();

        // Check for context loss patterns
        if failed_steps
            .iter()
            .any(|step| step.errors.iter().any(|e| e.contains("context")))
        {
            patterns.push("context_loss");
        }

        // Check for identity drift in recent messages
        let recent_responses: Vec<_> = history
            .iter()
            .rev()
            .take(5)
            .filter(|msg| matches!(msg.message_type, MessageType::Response))
            .collect();

        if recent_responses
            .iter()
            .any(|msg| !msg.content.contains("🤖 AGENT:"))
        {
            patterns.push("identity_drift");
        }

        patterns.join(",")
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_orchestration (lines 1234-1261) ---[0m
    async fn test_persistent_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-persistent-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let task = Task::new(
            "test_persistent_orchestration".to_string(),
            "Test persistent agent orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = agent.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
        assert!(plan.steps.iter().any(|step| step.name.contains("session")));
    }

  ./crates/ccswarm/src/agent/persistent.rs:136-164 method generate_mock_response <-> ./crates/ccswarm/src/agent/persistent.rs:1081-1110 method execute_iterative_debugging_step
  Similarity: 76.39%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:generate_mock_response (lines 136-164) ---[0m
    fn generate_mock_response(&self, prompt: &str) -> String {
        // Check if this is an identity establishment prompt
        if prompt.contains("🤖 **AGENT IDENTITY**") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Identity established successfully",
                self.session_id,
                self.working_dir
                    .file_name()
                    .unwrap_or_default()
                    .to_string_lossy()
            );
        }

        // Generate a simple mock response for tasks
        if prompt.contains("## Current Task:") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Task within agent boundaries\n\nTask completed successfully in simulation mode.",
                self.session_id,
                self.working_dir.file_name().unwrap_or_default().to_string_lossy()
            );
        }

        // Default mock response
        format!(
            "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: General response\n\nMock response generated for testing.",
            self.session_id,
            self.working_dir.file_name().unwrap_or_default().to_string_lossy()
        )
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_debugging_step (lines 1081-1110) ---[0m
    async fn execute_iterative_debugging_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_debug_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::High,
            TaskType::Bugfix,
        );

        // Use conversation history for debugging context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Analyze debugging progress
        let debug_progress = response.contains("identified")
            || response.contains("fixed")
            || response.contains("resolved");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative debugging step completed".to_string())
            .add_output("debug_response".to_string(), response)
            .add_output("progress_made".to_string(), debug_progress.to_string())
            .add_output("context_leveraged".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:136-164 method generate_mock_response <-> ./crates/ccswarm/src/agent/persistent.rs:867-901 method execute_parallel_task
  Similarity: 70.04%
  Classes: ClaudeCodeSession <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/persistent.rs:generate_mock_response (lines 136-164) ---[0m
    fn generate_mock_response(&self, prompt: &str) -> String {
        // Check if this is an identity establishment prompt
        if prompt.contains("🤖 **AGENT IDENTITY**") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Identity established successfully",
                self.session_id,
                self.working_dir
                    .file_name()
                    .unwrap_or_default()
                    .to_string_lossy()
            );
        }

        // Generate a simple mock response for tasks
        if prompt.contains("## Current Task:") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Task within agent boundaries\n\nTask completed successfully in simulation mode.",
                self.session_id,
                self.working_dir.file_name().unwrap_or_default().to_string_lossy()
            );
        }

        // Default mock response
        format!(
            "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: General response\n\nMock response generated for testing.",
            self.session_id,
            self.working_dir.file_name().unwrap_or_default().to_string_lossy()
        )
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_parallel_task (lines 867-901) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task with session context
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use conversation history for context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session
            .execute_with_context(&execution_task, &history)
            .await?;
        drop(session);
        drop(history);

        let success = !response.contains("error") && !response.contains("failed");

        Ok(ParallelTaskResult {
            task_id: task.id.clone(),
            success,
            output: response,
            error: if success {
                None
            } else {
                Some("Task execution failed".to_string())
            },
        })
    }

  ./crates/ccswarm/src/agent/persistent.rs:998-1021 method execute_iterative_development_step <-> ./crates/ccswarm/src/agent/persistent.rs:1234-1261 function test_persistent_agent_orchestration
  Similarity: 85.51%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_development_step (lines 998-1021) ---[0m
    async fn execute_iterative_development_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_dev_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use session context for development
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative development step completed with session context".to_string())
            .add_output("development_response".to_string(), response)
            .add_output("session_context_used".to_string(), "true".to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_orchestration (lines 1234-1261) ---[0m
    async fn test_persistent_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-persistent-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let task = Task::new(
            "test_persistent_orchestration".to_string(),
            "Test persistent agent orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = agent.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
        assert!(plan.steps.iter().any(|step| step.name.contains("session")));
    }

  ./crates/ccswarm/src/agent/persistent.rs:961-995 method execute_context_analysis_step <-> ./crates/ccswarm/src/agent/persistent.rs:1208-1231 function test_compact_identity_prompt
  Similarity: 75.05%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_context_analysis_step (lines 961-995) ---[0m
    async fn execute_context_analysis_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Analyze conversation history for relevant context
        let history = self.conversation_history.lock().await;
        let relevant_messages = history
            .iter()
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .count();

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Context analysis for task {} completed", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "history_messages".to_string(),
                relevant_messages.to_string(),
            ))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_compact_identity_prompt (lines 1208-1231) ---[0m
    async fn test_compact_identity_prompt() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let prompt = agent.generate_compact_identity_prompt();

        // Verify it's much more compact than the original CLAUDE.md approach
        assert!(prompt.len() < 500); // Much less than 2000+ tokens
        assert!(prompt.contains("🤖 AGENT:"));
        assert!(prompt.contains("Frontend"));
    }

  ./crates/ccswarm/src/agent/persistent.rs:1113-1142 method execute_persistent_default_step <-> ./crates/ccswarm/src/agent/persistent.rs:1186-1205 function test_persistent_agent_creation
  Similarity: 86.97%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_default_step (lines 1113-1142) ---[0m
    async fn execute_persistent_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("persistent_default_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Execute with full session context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent default step completed".to_string())
            .add_output("response".to_string(), response)
            .add_output("session_context_preserved".to_string(), "true".to_string())
            .add_output("task_id".to_string(), task_id))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_creation (lines 1186-1205) ---[0m
    async fn test_persistent_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        assert_eq!(agent.status, AgentStatus::Initializing);
        assert!(!agent.identity_established.load(Ordering::Relaxed));
    }

  ./crates/ccswarm/src/agent/persistent.rs:1081-1110 method execute_iterative_debugging_step <-> ./crates/ccswarm/src/agent/persistent.rs:1186-1205 function test_persistent_agent_creation
  Similarity: 86.92%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_debugging_step (lines 1081-1110) ---[0m
    async fn execute_iterative_debugging_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_debug_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::High,
            TaskType::Bugfix,
        );

        // Use conversation history for debugging context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Analyze debugging progress
        let debug_progress = response.contains("identified")
            || response.contains("fixed")
            || response.contains("resolved");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative debugging step completed".to_string())
            .add_output("debug_response".to_string(), response)
            .add_output("progress_made".to_string(), debug_progress.to_string())
            .add_output("context_leveraged".to_string(), "true".to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_creation (lines 1186-1205) ---[0m
    async fn test_persistent_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        assert_eq!(agent.status, AgentStatus::Initializing);
        assert!(!agent.identity_established.load(Ordering::Relaxed));
    }

  ./crates/ccswarm/src/agent/persistent.rs:536-556 method send_identity_reminder <-> ./crates/ccswarm/src/agent/persistent.rs:1051-1078 method execute_persistent_test_step
  Similarity: 88.38%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_identity_reminder (lines 536-556) ---[0m
    async fn send_identity_reminder(&self) -> Result<()> {
        let reminder_prompt = format!(
            "🤖 IDENTITY REMINDER: You are a {} agent working in {}. Maintain specialization boundaries.",
            self.identity.specialization.name(),
            self.identity.workspace_path.file_name().unwrap_or_default().to_string_lossy()
        );

        let mut session = self.session.lock().await;
        let _response = session.send_message(&reminder_prompt).await?;

        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityReminder,
            content: reminder_prompt,
            task_id: None,
        });
        self.trim_history(&mut history);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_test_step (lines 1051-1078) ---[0m
    async fn execute_persistent_test_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_test_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Testing,
        );

        // Execute tests with session persistence
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Parse test results from response
        let test_success = response.contains("✅") || response.contains("passed");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent test execution completed".to_string())
            .add_output("test_response".to_string(), response)
            .add_output("test_passed".to_string(), test_success.to_string())
            .add_output("persistent_execution".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:536-556 method send_identity_reminder <-> ./crates/ccswarm/src/agent/persistent.rs:1145-1176 method analyze_failure_patterns
  Similarity: 81.65%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_identity_reminder (lines 536-556) ---[0m
    async fn send_identity_reminder(&self) -> Result<()> {
        let reminder_prompt = format!(
            "🤖 IDENTITY REMINDER: You are a {} agent working in {}. Maintain specialization boundaries.",
            self.identity.specialization.name(),
            self.identity.workspace_path.file_name().unwrap_or_default().to_string_lossy()
        );

        let mut session = self.session.lock().await;
        let _response = session.send_message(&reminder_prompt).await?;

        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityReminder,
            content: reminder_prompt,
            task_id: None,
        });
        self.trim_history(&mut history);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:analyze_failure_patterns (lines 1145-1176) ---[0m
    fn analyze_failure_patterns(
        &self,
        history: &VecDeque<ConversationMessage>,
        failed_steps: &[&StepResult],
    ) -> String {
        let mut patterns = Vec::new();

        // Check for context loss patterns
        if failed_steps
            .iter()
            .any(|step| step.errors.iter().any(|e| e.contains("context")))
        {
            patterns.push("context_loss");
        }

        // Check for identity drift in recent messages
        let recent_responses: Vec<_> = history
            .iter()
            .rev()
            .take(5)
            .filter(|msg| matches!(msg.message_type, MessageType::Response))
            .collect();

        if recent_responses
            .iter()
            .any(|msg| !msg.content.contains("🤖 AGENT:"))
        {
            patterns.push("identity_drift");
        }

        patterns.join(",")
    }

  ./crates/ccswarm/src/agent/persistent.rs:136-164 method generate_mock_response <-> ./crates/ccswarm/src/agent/persistent.rs:1051-1078 method execute_persistent_test_step
  Similarity: 75.26%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:generate_mock_response (lines 136-164) ---[0m
    fn generate_mock_response(&self, prompt: &str) -> String {
        // Check if this is an identity establishment prompt
        if prompt.contains("🤖 **AGENT IDENTITY**") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Identity established successfully",
                self.session_id,
                self.working_dir
                    .file_name()
                    .unwrap_or_default()
                    .to_string_lossy()
            );
        }

        // Generate a simple mock response for tasks
        if prompt.contains("## Current Task:") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Task within agent boundaries\n\nTask completed successfully in simulation mode.",
                self.session_id,
                self.working_dir.file_name().unwrap_or_default().to_string_lossy()
            );
        }

        // Default mock response
        format!(
            "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: General response\n\nMock response generated for testing.",
            self.session_id,
            self.working_dir.file_name().unwrap_or_default().to_string_lossy()
        )
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_test_step (lines 1051-1078) ---[0m
    async fn execute_persistent_test_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_test_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Testing,
        );

        // Execute tests with session persistence
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Parse test results from response
        let test_success = response.contains("✅") || response.contains("passed");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent test execution completed".to_string())
            .add_output("test_response".to_string(), response)
            .add_output("test_passed".to_string(), test_success.to_string())
            .add_output("persistent_execution".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:998-1021 method execute_iterative_development_step <-> ./crates/ccswarm/src/agent/persistent.rs:1024-1048 method execute_session_validation_step
  Similarity: 87.42%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_development_step (lines 998-1021) ---[0m
    async fn execute_iterative_development_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_dev_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use session context for development
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative development step completed with session context".to_string())
            .add_output("development_response".to_string(), response)
            .add_output("session_context_used".to_string(), "true".to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_session_validation_step (lines 1024-1048) ---[0m
    async fn execute_session_validation_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Validate session state and identity
        let identity_established = self.identity_established.load(Ordering::Relaxed);
        let history = self.conversation_history.lock().await;
        let session_health = !history.is_empty() && identity_established;

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Session validation completed for {}", session_id))
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "identity_established".to_string(),
                identity_established.to_string(),
            )
            .add_output("session_healthy".to_string(), session_health.to_string())
            .add_output("conversation_length".to_string(), history.len().to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:290-311 method new <-> ./crates/ccswarm/src/agent/persistent.rs:781-809 method execute_step
  Similarity: 83.75%
  Classes: PersistentClaudeAgent <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/persistent.rs:new (lines 290-311) ---[0m
    pub async fn new(identity: AgentIdentity, claude_config: ClaudeConfig) -> Result<Self> {
        let session_id = Uuid::new_v4().to_string();
        let session = ClaudeCodeSession::new(
            session_id,
            identity.workspace_path.clone(),
            identity.env_vars.clone(),
            claude_config,
        );

        Ok(Self {
            identity,
            session: Arc::new(Mutex::new(session)),
            identity_established: Arc::new(AtomicBool::new(false)),
            conversation_history: Arc::new(Mutex::new(VecDeque::new())),
            max_history_size: 50, // Keep last 50 messages
            status: AgentStatus::Initializing,
            current_task: None,
            task_history: Vec::new(),
            last_activity: Utc::now(),
            session_created_at: Utc::now(),
        })
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_step (lines 781-809) ---[0m
    async fn execute_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        info!("🎭 Persistent agent executing step: {}", step.name);

        match step.name.as_str() {
            "Analyze task with session context" => {
                self.execute_context_analysis_step(step, context).await
            }
            "Execute with session persistence" => {
                self.execute_iterative_development_step(step, context).await
            }
            "Validate within session context" => {
                self.execute_session_validation_step(step, context).await
            }
            "Execute tests with session state" => {
                self.execute_persistent_test_step(step, context).await
            }
            "Debug with persistent context" => {
                self.execute_iterative_debugging_step(step, context).await
            }
            _ => {
                // Default execution with session persistence
                self.execute_persistent_default_step(step, context).await
            }
        }
    }

  ./crates/ccswarm/src/agent/persistent.rs:136-164 method generate_mock_response <-> ./crates/ccswarm/src/agent/persistent.rs:1113-1142 method execute_persistent_default_step
  Similarity: 72.19%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:generate_mock_response (lines 136-164) ---[0m
    fn generate_mock_response(&self, prompt: &str) -> String {
        // Check if this is an identity establishment prompt
        if prompt.contains("🤖 **AGENT IDENTITY**") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Identity established successfully",
                self.session_id,
                self.working_dir
                    .file_name()
                    .unwrap_or_default()
                    .to_string_lossy()
            );
        }

        // Generate a simple mock response for tasks
        if prompt.contains("## Current Task:") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Task within agent boundaries\n\nTask completed successfully in simulation mode.",
                self.session_id,
                self.working_dir.file_name().unwrap_or_default().to_string_lossy()
            );
        }

        // Default mock response
        format!(
            "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: General response\n\nMock response generated for testing.",
            self.session_id,
            self.working_dir.file_name().unwrap_or_default().to_string_lossy()
        )
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_default_step (lines 1113-1142) ---[0m
    async fn execute_persistent_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("persistent_default_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Execute with full session context
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent default step completed".to_string())
            .add_output("response".to_string(), response)
            .add_output("session_context_preserved".to_string(), "true".to_string())
            .add_output("task_id".to_string(), task_id))
    }

  ./crates/ccswarm/src/agent/persistent.rs:1051-1078 method execute_persistent_test_step <-> ./crates/ccswarm/src/agent/persistent.rs:1186-1205 function test_persistent_agent_creation
  Similarity: 86.29%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_persistent_test_step (lines 1051-1078) ---[0m
    async fn execute_persistent_test_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_test_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Testing,
        );

        // Execute tests with session persistence
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        // Parse test results from response
        let test_success = response.contains("✅") || response.contains("passed");

        Ok(StepResult::new(step.id.clone())
            .with_summary("Persistent test execution completed".to_string())
            .add_output("test_response".to_string(), response)
            .add_output("test_passed".to_string(), test_success.to_string())
            .add_output("persistent_execution".to_string(), "true".to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_creation (lines 1186-1205) ---[0m
    async fn test_persistent_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        assert_eq!(agent.status, AgentStatus::Initializing);
        assert!(!agent.identity_established.load(Ordering::Relaxed));
    }

  ./crates/ccswarm/src/agent/persistent.rs:536-556 method send_identity_reminder <-> ./crates/ccswarm/src/agent/persistent.rs:1024-1048 method execute_session_validation_step
  Similarity: 89.38%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_identity_reminder (lines 536-556) ---[0m
    async fn send_identity_reminder(&self) -> Result<()> {
        let reminder_prompt = format!(
            "🤖 IDENTITY REMINDER: You are a {} agent working in {}. Maintain specialization boundaries.",
            self.identity.specialization.name(),
            self.identity.workspace_path.file_name().unwrap_or_default().to_string_lossy()
        );

        let mut session = self.session.lock().await;
        let _response = session.send_message(&reminder_prompt).await?;

        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityReminder,
            content: reminder_prompt,
            task_id: None,
        });
        self.trim_history(&mut history);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_session_validation_step (lines 1024-1048) ---[0m
    async fn execute_session_validation_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Validate session state and identity
        let identity_established = self.identity_established.load(Ordering::Relaxed);
        let history = self.conversation_history.lock().await;
        let session_health = !history.is_empty() && identity_established;

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Session validation completed for {}", session_id))
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "identity_established".to_string(),
                identity_established.to_string(),
            )
            .add_output("session_healthy".to_string(), session_health.to_string())
            .add_output("conversation_length".to_string(), history.len().to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:536-556 method send_identity_reminder <-> ./crates/ccswarm/src/agent/persistent.rs:998-1021 method execute_iterative_development_step
  Similarity: 91.13%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_identity_reminder (lines 536-556) ---[0m
    async fn send_identity_reminder(&self) -> Result<()> {
        let reminder_prompt = format!(
            "🤖 IDENTITY REMINDER: You are a {} agent working in {}. Maintain specialization boundaries.",
            self.identity.specialization.name(),
            self.identity.workspace_path.file_name().unwrap_or_default().to_string_lossy()
        );

        let mut session = self.session.lock().await;
        let _response = session.send_message(&reminder_prompt).await?;

        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityReminder,
            content: reminder_prompt,
            task_id: None,
        });
        self.trim_history(&mut history);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_development_step (lines 998-1021) ---[0m
    async fn execute_iterative_development_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_dev_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use session context for development
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative development step completed with session context".to_string())
            .add_output("development_response".to_string(), response)
            .add_output("session_context_used".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:290-311 method new <-> ./crates/ccswarm/src/agent/persistent.rs:961-995 method execute_context_analysis_step
  Similarity: 71.68%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:new (lines 290-311) ---[0m
    pub async fn new(identity: AgentIdentity, claude_config: ClaudeConfig) -> Result<Self> {
        let session_id = Uuid::new_v4().to_string();
        let session = ClaudeCodeSession::new(
            session_id,
            identity.workspace_path.clone(),
            identity.env_vars.clone(),
            claude_config,
        );

        Ok(Self {
            identity,
            session: Arc::new(Mutex::new(session)),
            identity_established: Arc::new(AtomicBool::new(false)),
            conversation_history: Arc::new(Mutex::new(VecDeque::new())),
            max_history_size: 50, // Keep last 50 messages
            status: AgentStatus::Initializing,
            current_task: None,
            task_history: Vec::new(),
            last_activity: Utc::now(),
            session_created_at: Utc::now(),
        })
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_context_analysis_step (lines 961-995) ---[0m
    async fn execute_context_analysis_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Analyze conversation history for relevant context
        let history = self.conversation_history.lock().await;
        let relevant_messages = history
            .iter()
            .filter(|msg| {
                matches!(
                    msg.message_type,
                    MessageType::TaskPrompt | MessageType::Response
                )
            })
            .count();

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Context analysis for task {} completed", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "history_messages".to_string(),
                relevant_messages.to_string(),
            ))
    }

  ./crates/ccswarm/src/agent/persistent.rs:1145-1176 method analyze_failure_patterns <-> ./crates/ccswarm/src/agent/persistent.rs:1186-1205 function test_persistent_agent_creation
  Similarity: 78.35%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:analyze_failure_patterns (lines 1145-1176) ---[0m
    fn analyze_failure_patterns(
        &self,
        history: &VecDeque<ConversationMessage>,
        failed_steps: &[&StepResult],
    ) -> String {
        let mut patterns = Vec::new();

        // Check for context loss patterns
        if failed_steps
            .iter()
            .any(|step| step.errors.iter().any(|e| e.contains("context")))
        {
            patterns.push("context_loss");
        }

        // Check for identity drift in recent messages
        let recent_responses: Vec<_> = history
            .iter()
            .rev()
            .take(5)
            .filter(|msg| matches!(msg.message_type, MessageType::Response))
            .collect();

        if recent_responses
            .iter()
            .any(|msg| !msg.content.contains("🤖 AGENT:"))
        {
            patterns.push("identity_drift");
        }

        patterns.join(",")
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_creation (lines 1186-1205) ---[0m
    async fn test_persistent_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        assert_eq!(agent.status, AgentStatus::Initializing);
        assert!(!agent.identity_established.load(Ordering::Relaxed));
    }

  ./crates/ccswarm/src/agent/persistent.rs:136-164 method generate_mock_response <-> ./crates/ccswarm/src/agent/persistent.rs:1234-1261 function test_persistent_agent_orchestration
  Similarity: 71.45%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:generate_mock_response (lines 136-164) ---[0m
    fn generate_mock_response(&self, prompt: &str) -> String {
        // Check if this is an identity establishment prompt
        if prompt.contains("🤖 **AGENT IDENTITY**") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Identity established successfully",
                self.session_id,
                self.working_dir
                    .file_name()
                    .unwrap_or_default()
                    .to_string_lossy()
            );
        }

        // Generate a simple mock response for tasks
        if prompt.contains("## Current Task:") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Task within agent boundaries\n\nTask completed successfully in simulation mode.",
                self.session_id,
                self.working_dir.file_name().unwrap_or_default().to_string_lossy()
            );
        }

        // Default mock response
        format!(
            "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: General response\n\nMock response generated for testing.",
            self.session_id,
            self.working_dir.file_name().unwrap_or_default().to_string_lossy()
        )
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_orchestration (lines 1234-1261) ---[0m
    async fn test_persistent_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-persistent-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let task = Task::new(
            "test_persistent_orchestration".to_string(),
            "Test persistent agent orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = agent.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
        assert!(plan.steps.iter().any(|step| step.name.contains("session")));
    }

  ./crates/ccswarm/src/agent/persistent.rs:1024-1048 method execute_session_validation_step <-> ./crates/ccswarm/src/agent/persistent.rs:1234-1261 function test_persistent_agent_orchestration
  Similarity: 76.57%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_session_validation_step (lines 1024-1048) ---[0m
    async fn execute_session_validation_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Validate session state and identity
        let identity_established = self.identity_established.load(Ordering::Relaxed);
        let history = self.conversation_history.lock().await;
        let session_health = !history.is_empty() && identity_established;

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Session validation completed for {}", session_id))
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "identity_established".to_string(),
                identity_established.to_string(),
            )
            .add_output("session_healthy".to_string(), session_health.to_string())
            .add_output("conversation_length".to_string(), history.len().to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_orchestration (lines 1234-1261) ---[0m
    async fn test_persistent_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-persistent-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let task = Task::new(
            "test_persistent_orchestration".to_string(),
            "Test persistent agent orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = agent.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
        assert!(plan.steps.iter().any(|step| step.name.contains("session")));
    }

  ./crates/ccswarm/src/agent/persistent.rs:1186-1205 function test_persistent_agent_creation <-> ./crates/ccswarm/src/agent/persistent.rs:1234-1261 function test_persistent_agent_orchestration
  Similarity: 84.06%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_creation (lines 1186-1205) ---[0m
    async fn test_persistent_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        assert_eq!(agent.status, AgentStatus::Initializing);
        assert!(!agent.identity_established.load(Ordering::Relaxed));
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_orchestration (lines 1234-1261) ---[0m
    async fn test_persistent_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-persistent-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let task = Task::new(
            "test_persistent_orchestration".to_string(),
            "Test persistent agent orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = agent.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
        assert!(plan.steps.iter().any(|step| step.name.contains("session")));
    }

  ./crates/ccswarm/src/agent/persistent.rs:136-164 method generate_mock_response <-> ./crates/ccswarm/src/agent/persistent.rs:536-556 method send_identity_reminder
  Similarity: 80.40%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:generate_mock_response (lines 136-164) ---[0m
    fn generate_mock_response(&self, prompt: &str) -> String {
        // Check if this is an identity establishment prompt
        if prompt.contains("🤖 **AGENT IDENTITY**") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Identity established successfully",
                self.session_id,
                self.working_dir
                    .file_name()
                    .unwrap_or_default()
                    .to_string_lossy()
            );
        }

        // Generate a simple mock response for tasks
        if prompt.contains("## Current Task:") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Task within agent boundaries\n\nTask completed successfully in simulation mode.",
                self.session_id,
                self.working_dir.file_name().unwrap_or_default().to_string_lossy()
            );
        }

        // Default mock response
        format!(
            "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: General response\n\nMock response generated for testing.",
            self.session_id,
            self.working_dir.file_name().unwrap_or_default().to_string_lossy()
        )
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_identity_reminder (lines 536-556) ---[0m
    async fn send_identity_reminder(&self) -> Result<()> {
        let reminder_prompt = format!(
            "🤖 IDENTITY REMINDER: You are a {} agent working in {}. Maintain specialization boundaries.",
            self.identity.specialization.name(),
            self.identity.workspace_path.file_name().unwrap_or_default().to_string_lossy()
        );

        let mut session = self.session.lock().await;
        let _response = session.send_message(&reminder_prompt).await?;

        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityReminder,
            content: reminder_prompt,
            task_id: None,
        });
        self.trim_history(&mut history);

        Ok(())
    }

  ./crates/ccswarm/src/agent/persistent.rs:136-164 method generate_mock_response <-> ./crates/ccswarm/src/agent/persistent.rs:998-1021 method execute_iterative_development_step
  Similarity: 75.71%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:generate_mock_response (lines 136-164) ---[0m
    fn generate_mock_response(&self, prompt: &str) -> String {
        // Check if this is an identity establishment prompt
        if prompt.contains("🤖 **AGENT IDENTITY**") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Identity established successfully",
                self.session_id,
                self.working_dir
                    .file_name()
                    .unwrap_or_default()
                    .to_string_lossy()
            );
        }

        // Generate a simple mock response for tasks
        if prompt.contains("## Current Task:") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Task within agent boundaries\n\nTask completed successfully in simulation mode.",
                self.session_id,
                self.working_dir.file_name().unwrap_or_default().to_string_lossy()
            );
        }

        // Default mock response
        format!(
            "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: General response\n\nMock response generated for testing.",
            self.session_id,
            self.working_dir.file_name().unwrap_or_default().to_string_lossy()
        )
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_development_step (lines 998-1021) ---[0m
    async fn execute_iterative_development_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_dev_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use session context for development
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative development step completed with session context".to_string())
            .add_output("development_response".to_string(), response)
            .add_output("session_context_used".to_string(), "true".to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:290-311 method new <-> ./crates/ccswarm/src/agent/persistent.rs:1145-1176 method analyze_failure_patterns
  Similarity: 74.22%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:new (lines 290-311) ---[0m
    pub async fn new(identity: AgentIdentity, claude_config: ClaudeConfig) -> Result<Self> {
        let session_id = Uuid::new_v4().to_string();
        let session = ClaudeCodeSession::new(
            session_id,
            identity.workspace_path.clone(),
            identity.env_vars.clone(),
            claude_config,
        );

        Ok(Self {
            identity,
            session: Arc::new(Mutex::new(session)),
            identity_established: Arc::new(AtomicBool::new(false)),
            conversation_history: Arc::new(Mutex::new(VecDeque::new())),
            max_history_size: 50, // Keep last 50 messages
            status: AgentStatus::Initializing,
            current_task: None,
            task_history: Vec::new(),
            last_activity: Utc::now(),
            session_created_at: Utc::now(),
        })
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:analyze_failure_patterns (lines 1145-1176) ---[0m
    fn analyze_failure_patterns(
        &self,
        history: &VecDeque<ConversationMessage>,
        failed_steps: &[&StepResult],
    ) -> String {
        let mut patterns = Vec::new();

        // Check for context loss patterns
        if failed_steps
            .iter()
            .any(|step| step.errors.iter().any(|e| e.contains("context")))
        {
            patterns.push("context_loss");
        }

        // Check for identity drift in recent messages
        let recent_responses: Vec<_> = history
            .iter()
            .rev()
            .take(5)
            .filter(|msg| matches!(msg.message_type, MessageType::Response))
            .collect();

        if recent_responses
            .iter()
            .any(|msg| !msg.content.contains("🤖 AGENT:"))
        {
            patterns.push("identity_drift");
        }

        patterns.join(",")
    }

  ./crates/ccswarm/src/agent/persistent.rs:536-556 method send_identity_reminder <-> ./crates/ccswarm/src/agent/persistent.rs:1234-1261 function test_persistent_agent_orchestration
  Similarity: 81.19%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_identity_reminder (lines 536-556) ---[0m
    async fn send_identity_reminder(&self) -> Result<()> {
        let reminder_prompt = format!(
            "🤖 IDENTITY REMINDER: You are a {} agent working in {}. Maintain specialization boundaries.",
            self.identity.specialization.name(),
            self.identity.workspace_path.file_name().unwrap_or_default().to_string_lossy()
        );

        let mut session = self.session.lock().await;
        let _response = session.send_message(&reminder_prompt).await?;

        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityReminder,
            content: reminder_prompt,
            task_id: None,
        });
        self.trim_history(&mut history);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_orchestration (lines 1234-1261) ---[0m
    async fn test_persistent_agent_orchestration() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-persistent-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let task = Task::new(
            "test_persistent_orchestration".to_string(),
            "Test persistent agent orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = agent.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
        assert!(plan.steps.iter().any(|step| step.name.contains("session")));
    }

  ./crates/ccswarm/src/agent/persistent.rs:1024-1048 method execute_session_validation_step <-> ./crates/ccswarm/src/agent/persistent.rs:1186-1205 function test_persistent_agent_creation
  Similarity: 86.13%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_session_validation_step (lines 1024-1048) ---[0m
    async fn execute_session_validation_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Validate session state and identity
        let identity_established = self.identity_established.load(Ordering::Relaxed);
        let history = self.conversation_history.lock().await;
        let session_health = !history.is_empty() && identity_established;

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Session validation completed for {}", session_id))
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "identity_established".to_string(),
                identity_established.to_string(),
            )
            .add_output("session_healthy".to_string(), session_health.to_string())
            .add_output("conversation_length".to_string(), history.len().to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_creation (lines 1186-1205) ---[0m
    async fn test_persistent_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        assert_eq!(agent.status, AgentStatus::Initializing);
        assert!(!agent.identity_established.load(Ordering::Relaxed));
    }

  ./crates/ccswarm/src/agent/persistent.rs:781-809 method execute_step <-> ./crates/ccswarm/src/agent/persistent.rs:1024-1048 method execute_session_validation_step
  Similarity: 71.17%
  Classes: AgentOrchestrator <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_step (lines 781-809) ---[0m
    async fn execute_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        info!("🎭 Persistent agent executing step: {}", step.name);

        match step.name.as_str() {
            "Analyze task with session context" => {
                self.execute_context_analysis_step(step, context).await
            }
            "Execute with session persistence" => {
                self.execute_iterative_development_step(step, context).await
            }
            "Validate within session context" => {
                self.execute_session_validation_step(step, context).await
            }
            "Execute tests with session state" => {
                self.execute_persistent_test_step(step, context).await
            }
            "Debug with persistent context" => {
                self.execute_iterative_debugging_step(step, context).await
            }
            _ => {
                // Default execution with session persistence
                self.execute_persistent_default_step(step, context).await
            }
        }
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_session_validation_step (lines 1024-1048) ---[0m
    async fn execute_session_validation_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Validate session state and identity
        let identity_established = self.identity_established.load(Ordering::Relaxed);
        let history = self.conversation_history.lock().await;
        let session_health = !history.is_empty() && identity_established;

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Session validation completed for {}", session_id))
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "identity_established".to_string(),
                identity_established.to_string(),
            )
            .add_output("session_healthy".to_string(), session_health.to_string())
            .add_output("conversation_length".to_string(), history.len().to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:998-1021 method execute_iterative_development_step <-> ./crates/ccswarm/src/agent/persistent.rs:1208-1231 function test_compact_identity_prompt
  Similarity: 80.04%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_development_step (lines 998-1021) ---[0m
    async fn execute_iterative_development_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_dev_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use session context for development
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative development step completed with session context".to_string())
            .add_output("development_response".to_string(), response)
            .add_output("session_context_used".to_string(), "true".to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_compact_identity_prompt (lines 1208-1231) ---[0m
    async fn test_compact_identity_prompt() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let prompt = agent.generate_compact_identity_prompt();

        // Verify it's much more compact than the original CLAUDE.md approach
        assert!(prompt.len() < 500); // Much less than 2000+ tokens
        assert!(prompt.contains("🤖 AGENT:"));
        assert!(prompt.contains("Frontend"));
    }

  ./crates/ccswarm/src/agent/persistent.rs:998-1021 method execute_iterative_development_step <-> ./crates/ccswarm/src/agent/persistent.rs:1186-1205 function test_persistent_agent_creation
  Similarity: 86.93%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_development_step (lines 998-1021) ---[0m
    async fn execute_iterative_development_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_dev_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use session context for development
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative development step completed with session context".to_string())
            .add_output("development_response".to_string(), response)
            .add_output("session_context_used".to_string(), "true".to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_creation (lines 1186-1205) ---[0m
    async fn test_persistent_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        assert_eq!(agent.status, AgentStatus::Initializing);
        assert!(!agent.identity_established.load(Ordering::Relaxed));
    }

  ./crates/ccswarm/src/agent/persistent.rs:290-311 method new <-> ./crates/ccswarm/src/agent/persistent.rs:1024-1048 method execute_session_validation_step
  Similarity: 79.96%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:new (lines 290-311) ---[0m
    pub async fn new(identity: AgentIdentity, claude_config: ClaudeConfig) -> Result<Self> {
        let session_id = Uuid::new_v4().to_string();
        let session = ClaudeCodeSession::new(
            session_id,
            identity.workspace_path.clone(),
            identity.env_vars.clone(),
            claude_config,
        );

        Ok(Self {
            identity,
            session: Arc::new(Mutex::new(session)),
            identity_established: Arc::new(AtomicBool::new(false)),
            conversation_history: Arc::new(Mutex::new(VecDeque::new())),
            max_history_size: 50, // Keep last 50 messages
            status: AgentStatus::Initializing,
            current_task: None,
            task_history: Vec::new(),
            last_activity: Utc::now(),
            session_created_at: Utc::now(),
        })
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_session_validation_step (lines 1024-1048) ---[0m
    async fn execute_session_validation_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Validate session state and identity
        let identity_established = self.identity_established.load(Ordering::Relaxed);
        let history = self.conversation_history.lock().await;
        let session_health = !history.is_empty() && identity_established;

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Session validation completed for {}", session_id))
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "identity_established".to_string(),
                identity_established.to_string(),
            )
            .add_output("session_healthy".to_string(), session_health.to_string())
            .add_output("conversation_length".to_string(), history.len().to_string()))
    }

  ./crates/ccswarm/src/agent/persistent.rs:136-164 method generate_mock_response <-> ./crates/ccswarm/src/agent/persistent.rs:1186-1205 function test_persistent_agent_creation
  Similarity: 76.60%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:generate_mock_response (lines 136-164) ---[0m
    fn generate_mock_response(&self, prompt: &str) -> String {
        // Check if this is an identity establishment prompt
        if prompt.contains("🤖 **AGENT IDENTITY**") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Identity established successfully",
                self.session_id,
                self.working_dir
                    .file_name()
                    .unwrap_or_default()
                    .to_string_lossy()
            );
        }

        // Generate a simple mock response for tasks
        if prompt.contains("## Current Task:") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Task within agent boundaries\n\nTask completed successfully in simulation mode.",
                self.session_id,
                self.working_dir.file_name().unwrap_or_default().to_string_lossy()
            );
        }

        // Default mock response
        format!(
            "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: General response\n\nMock response generated for testing.",
            self.session_id,
            self.working_dir.file_name().unwrap_or_default().to_string_lossy()
        )
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_creation (lines 1186-1205) ---[0m
    async fn test_persistent_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        assert_eq!(agent.status, AgentStatus::Initializing);
        assert!(!agent.identity_established.load(Ordering::Relaxed));
    }

  ./crates/ccswarm/src/agent/persistent.rs:136-164 method generate_mock_response <-> ./crates/ccswarm/src/agent/persistent.rs:566-583 method get_session_stats
  Similarity: 78.64%
  Classes: ClaudeCodeSession <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:generate_mock_response (lines 136-164) ---[0m
    fn generate_mock_response(&self, prompt: &str) -> String {
        // Check if this is an identity establishment prompt
        if prompt.contains("🤖 **AGENT IDENTITY**") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Identity established successfully",
                self.session_id,
                self.working_dir
                    .file_name()
                    .unwrap_or_default()
                    .to_string_lossy()
            );
        }

        // Generate a simple mock response for tasks
        if prompt.contains("## Current Task:") {
            return format!(
                "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: Task within agent boundaries\n\nTask completed successfully in simulation mode.",
                self.session_id,
                self.working_dir.file_name().unwrap_or_default().to_string_lossy()
            );
        }

        // Default mock response
        format!(
            "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: General response\n\nMock response generated for testing.",
            self.session_id,
            self.working_dir.file_name().unwrap_or_default().to_string_lossy()
        )
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:get_session_stats (lines 566-583) ---[0m
    pub async fn get_session_stats(&self) -> SessionStats {
        let session = self.session.lock().await;
        let history = self.conversation_history.lock().await;

        SessionStats {
            session_id: session.session_id().to_string(),
            created_at: self.session_created_at,
            last_activity: DateTime::from_timestamp(
                session.last_activity().elapsed().as_secs() as i64,
                0,
            )
            .unwrap_or(Utc::now()),
            identity_established: self.identity_established.load(Ordering::Relaxed),
            conversation_messages: history.len(),
            tasks_completed: self.task_history.len(),
            is_active: session.is_active(),
        }
    }

  ./crates/ccswarm/src/agent/persistent.rs:1186-1205 function test_persistent_agent_creation <-> ./crates/ccswarm/src/agent/persistent.rs:1208-1231 function test_compact_identity_prompt
  Similarity: 81.88%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_creation (lines 1186-1205) ---[0m
    async fn test_persistent_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        assert_eq!(agent.status, AgentStatus::Initializing);
        assert!(!agent.identity_established.load(Ordering::Relaxed));
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_compact_identity_prompt (lines 1208-1231) ---[0m
    async fn test_compact_identity_prompt() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let prompt = agent.generate_compact_identity_prompt();

        // Verify it's much more compact than the original CLAUDE.md approach
        assert!(prompt.len() < 500); // Much less than 2000+ tokens
        assert!(prompt.contains("🤖 AGENT:"));
        assert!(prompt.contains("Frontend"));
    }

  ./crates/ccswarm/src/agent/persistent.rs:1024-1048 method execute_session_validation_step <-> ./crates/ccswarm/src/agent/persistent.rs:1208-1231 function test_compact_identity_prompt
  Similarity: 73.24%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_session_validation_step (lines 1024-1048) ---[0m
    async fn execute_session_validation_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let session_id = context
            .get("session_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Validate session state and identity
        let identity_established = self.identity_established.load(Ordering::Relaxed);
        let history = self.conversation_history.lock().await;
        let session_health = !history.is_empty() && identity_established;

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Session validation completed for {}", session_id))
            .add_output("session_id".to_string(), session_id)
            .add_output(
                "identity_established".to_string(),
                identity_established.to_string(),
            )
            .add_output("session_healthy".to_string(), session_health.to_string())
            .add_output("conversation_length".to_string(), history.len().to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_compact_identity_prompt (lines 1208-1231) ---[0m
    async fn test_compact_identity_prompt() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let prompt = agent.generate_compact_identity_prompt();

        // Verify it's much more compact than the original CLAUDE.md approach
        assert!(prompt.len() < 500); // Much less than 2000+ tokens
        assert!(prompt.contains("🤖 AGENT:"));
        assert!(prompt.contains("Frontend"));
    }

  ./crates/ccswarm/src/agent/persistent.rs:536-556 method send_identity_reminder <-> ./crates/ccswarm/src/agent/persistent.rs:1186-1205 function test_persistent_agent_creation
  Similarity: 84.95%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_identity_reminder (lines 536-556) ---[0m
    async fn send_identity_reminder(&self) -> Result<()> {
        let reminder_prompt = format!(
            "🤖 IDENTITY REMINDER: You are a {} agent working in {}. Maintain specialization boundaries.",
            self.identity.specialization.name(),
            self.identity.workspace_path.file_name().unwrap_or_default().to_string_lossy()
        );

        let mut session = self.session.lock().await;
        let _response = session.send_message(&reminder_prompt).await?;

        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityReminder,
            content: reminder_prompt,
            task_id: None,
        });
        self.trim_history(&mut history);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_persistent_agent_creation (lines 1186-1205) ---[0m
    async fn test_persistent_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        assert_eq!(agent.status, AgentStatus::Initializing);
        assert!(!agent.identity_established.load(Ordering::Relaxed));
    }

  ./crates/ccswarm/src/agent/persistent.rs:536-556 method send_identity_reminder <-> ./crates/ccswarm/src/agent/persistent.rs:1208-1231 function test_compact_identity_prompt
  Similarity: 74.08%

[36m--- ./crates/ccswarm/src/agent/persistent.rs:send_identity_reminder (lines 536-556) ---[0m
    async fn send_identity_reminder(&self) -> Result<()> {
        let reminder_prompt = format!(
            "🤖 IDENTITY REMINDER: You are a {} agent working in {}. Maintain specialization boundaries.",
            self.identity.specialization.name(),
            self.identity.workspace_path.file_name().unwrap_or_default().to_string_lossy()
        );

        let mut session = self.session.lock().await;
        let _response = session.send_message(&reminder_prompt).await?;

        let mut history = self.conversation_history.lock().await;
        history.push_back(ConversationMessage {
            timestamp: Utc::now(),
            message_type: MessageType::IdentityReminder,
            content: reminder_prompt,
            task_id: None,
        });
        self.trim_history(&mut history);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:test_compact_identity_prompt (lines 1208-1231) ---[0m
    async fn test_compact_identity_prompt() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_path = temp_dir.path().to_path_buf();

        let identity = AgentIdentity {
            agent_id: "test-frontend-agent".to_string(),
            specialization: default_frontend_role(),
            workspace_path,
            env_vars: HashMap::new(),
            session_id: "test-session".to_string(),
            parent_process_id: "test-parent".to_string(),
            initialized_at: Utc::now(),
        };

        let config = ClaudeConfig::default();
        let agent = PersistentClaudeAgent::new(identity, config).await.unwrap();

        let prompt = agent.generate_compact_identity_prompt();

        // Verify it's much more compact than the original CLAUDE.md approach
        assert!(prompt.len() < 500); // Much less than 2000+ tokens
        assert!(prompt.contains("🤖 AGENT:"));
        assert!(prompt.contains("Frontend"));
    }

  ./crates/ccswarm/src/agent/persistent.rs:290-311 method new <-> ./crates/ccswarm/src/agent/persistent.rs:998-1021 method execute_iterative_development_step
  Similarity: 70.22%
  Classes: PersistentClaudeAgent <-> PersistentClaudeAgent

[36m--- ./crates/ccswarm/src/agent/persistent.rs:new (lines 290-311) ---[0m
    pub async fn new(identity: AgentIdentity, claude_config: ClaudeConfig) -> Result<Self> {
        let session_id = Uuid::new_v4().to_string();
        let session = ClaudeCodeSession::new(
            session_id,
            identity.workspace_path.clone(),
            identity.env_vars.clone(),
            claude_config,
        );

        Ok(Self {
            identity,
            session: Arc::new(Mutex::new(session)),
            identity_established: Arc::new(AtomicBool::new(false)),
            conversation_history: Arc::new(Mutex::new(VecDeque::new())),
            max_history_size: 50, // Keep last 50 messages
            status: AgentStatus::Initializing,
            current_task: None,
            task_history: Vec::new(),
            last_activity: Utc::now(),
            session_created_at: Utc::now(),
        })
    }

[36m--- ./crates/ccswarm/src/agent/persistent.rs:execute_iterative_development_step (lines 998-1021) ---[0m
    async fn execute_iterative_development_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task = Task::new(
            format!("persistent_dev_{}", step.id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Use session context for development
        let mut session = self.session.lock().await;
        let history = self.conversation_history.lock().await;
        let response = session.execute_with_context(&task, &history).await?;
        drop(session);
        drop(history);

        Ok(StepResult::new(step.id.clone())
            .with_summary("Iterative development step completed with session context".to_string())
            .add_output("development_response".to_string(), response)
            .add_output("session_context_used".to_string(), "true".to_string()))
    }


Duplicates in ./crates/ccswarm/src/streaming/tests.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/streaming/tests.rs:291-343 function test_agent_subscription <-> ./crates/ccswarm/src/streaming/tests.rs:347-398 function test_filtered_subscription
  Similarity: 88.67%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_agent_subscription (lines 291-343) ---[0m
    async fn test_agent_subscription() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe to specific agent first
        let mut rx = manager
            .subscribe_agent("test-sub".to_string(), "agent-123".to_string())
            .unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add outputs
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Message from agent-123".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        monitoring
            .add_output(
                "agent-456".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Message from agent-456".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Should only receive from agent-123
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        let entry = entry.unwrap();
        assert_eq!(entry.agent_id, "agent-123");
        assert_eq!(entry.content, "Message from agent-123");

        // Should not receive any more messages
        let timeout = tokio::time::timeout(Duration::from_millis(50), rx.recv()).await;

        assert!(timeout.is_err()); // Timeout
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_filtered_subscription (lines 347-398) ---[0m
    async fn test_filtered_subscription() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe with error filter first
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let mut rx = manager
            .subscribe_filtered("test-sub".to_string(), filter)
            .unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add various outputs
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Info message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Error,
                "Error message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Should only receive error
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        let entry = entry.unwrap();
        assert_eq!(entry.output_type, OutputType::Error);
        assert_eq!(entry.content, "Error message");
    }

  ./crates/ccswarm/src/streaming/tests.rs:170-224 function test_highlight_patterns <-> ./crates/ccswarm/src/streaming/tests.rs:291-343 function test_agent_subscription
  Similarity: 85.89%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_highlight_patterns (lines 170-224) ---[0m
    fn test_highlight_patterns() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        // Test error highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "An error occurred in the system".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let error_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Error));
        assert!(error_span.is_some());

        // Test warning highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Warning: deprecated function".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let warning_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Warning));
        assert!(warning_span.is_some());

        // Test success highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Task completed successfully".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let success_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Success));
        assert!(success_span.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_agent_subscription (lines 291-343) ---[0m
    async fn test_agent_subscription() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe to specific agent first
        let mut rx = manager
            .subscribe_agent("test-sub".to_string(), "agent-123".to_string())
            .unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add outputs
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Message from agent-123".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        monitoring
            .add_output(
                "agent-456".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Message from agent-456".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Should only receive from agent-123
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        let entry = entry.unwrap();
        assert_eq!(entry.agent_id, "agent-123");
        assert_eq!(entry.content, "Message from agent-123");

        // Should not receive any more messages
        let timeout = tokio::time::timeout(Duration::from_millis(50), rx.recv()).await;

        assert!(timeout.is_err()); // Timeout
    }

  ./crates/ccswarm/src/streaming/tests.rs:170-224 function test_highlight_patterns <-> ./crates/ccswarm/src/streaming/tests.rs:347-398 function test_filtered_subscription
  Similarity: 86.23%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_highlight_patterns (lines 170-224) ---[0m
    fn test_highlight_patterns() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        // Test error highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "An error occurred in the system".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let error_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Error));
        assert!(error_span.is_some());

        // Test warning highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Warning: deprecated function".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let warning_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Warning));
        assert!(warning_span.is_some());

        // Test success highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Task completed successfully".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let success_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Success));
        assert!(success_span.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_filtered_subscription (lines 347-398) ---[0m
    async fn test_filtered_subscription() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe with error filter first
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let mut rx = manager
            .subscribe_filtered("test-sub".to_string(), filter)
            .unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add various outputs
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Info message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Error,
                "Error message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Should only receive error
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        let entry = entry.unwrap();
        assert_eq!(entry.output_type, OutputType::Error);
        assert_eq!(entry.content, "Error message");
    }

  ./crates/ccswarm/src/streaming/tests.rs:45-91 function test_subscription_filtering <-> ./crates/ccswarm/src/streaming/tests.rs:170-224 function test_highlight_patterns
  Similarity: 87.78%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_subscription_filtering (lines 45-91) ---[0m
    fn test_subscription_filtering() {
        let (tx, _rx) = mpsc::unbounded_channel();
        let sub =
            StreamSubscription::new("test-sub".to_string(), tx).with_agent("agent-123".to_string());

        // Create test entries
        let matching_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        let non_matching_entry = OutputEntry::new(
            "agent-456".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&matching_entry));
        assert!(!sub.should_receive(&non_matching_entry));

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let sub = sub.with_filter(filter);

        assert!(!sub.should_receive(&matching_entry)); // Wrong type

        let error_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Error,
            "Error message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&error_entry));
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_highlight_patterns (lines 170-224) ---[0m
    fn test_highlight_patterns() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        // Test error highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "An error occurred in the system".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let error_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Error));
        assert!(error_span.is_some());

        // Test warning highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Warning: deprecated function".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let warning_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Warning));
        assert!(warning_span.is_some());

        // Test success highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Task completed successfully".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let success_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Success));
        assert!(success_span.is_some());
    }

  ./crates/ccswarm/src/streaming/tests.rs:170-224 function test_highlight_patterns <-> ./crates/ccswarm/src/streaming/tests.rs:508-550 function test_formatting_edge_cases
  Similarity: 90.29%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_highlight_patterns (lines 170-224) ---[0m
    fn test_highlight_patterns() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        // Test error highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "An error occurred in the system".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let error_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Error));
        assert!(error_span.is_some());

        // Test warning highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Warning: deprecated function".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let warning_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Warning));
        assert!(warning_span.is_some());

        // Test success highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Task completed successfully".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let success_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Success));
        assert!(success_span.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_formatting_edge_cases (lines 508-550) ---[0m
    fn test_formatting_edge_cases() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        // Test empty content
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "");

        // Test content with tabs
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Line\twith\ttabs".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "Line    with    tabs");

        // Test very short agent ID
        let entry = OutputEntry::new(
            "a".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert!(formatted.display_prefix.contains(" a"));
    }

  ./crates/ccswarm/src/streaming/tests.rs:251-287 function test_streaming_manager <-> ./crates/ccswarm/src/streaming/tests.rs:347-398 function test_filtered_subscription
  Similarity: 94.92%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_manager (lines 251-287) ---[0m
    async fn test_streaming_manager() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe to stream first
        let mut rx = manager.subscribe("test-sub".to_string()).unwrap();

        // Start the manager
        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add output to monitoring system
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Test message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Wait for message with longer timeout
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        assert_eq!(entry.unwrap().content, "Test message");

        // Unsubscribe
        manager.unsubscribe("test-sub").unwrap();
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_filtered_subscription (lines 347-398) ---[0m
    async fn test_filtered_subscription() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe with error filter first
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let mut rx = manager
            .subscribe_filtered("test-sub".to_string(), filter)
            .unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add various outputs
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Info message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Error,
                "Error message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Should only receive error
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        let entry = entry.unwrap();
        assert_eq!(entry.output_type, OutputType::Error);
        assert_eq!(entry.content, "Error message");
    }

  ./crates/ccswarm/src/streaming/tests.rs:347-398 function test_filtered_subscription <-> ./crates/ccswarm/src/streaming/tests.rs:462-504 function test_multiple_subscribers
  Similarity: 88.85%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_filtered_subscription (lines 347-398) ---[0m
    async fn test_filtered_subscription() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe with error filter first
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let mut rx = manager
            .subscribe_filtered("test-sub".to_string(), filter)
            .unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add various outputs
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Info message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Error,
                "Error message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Should only receive error
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        let entry = entry.unwrap();
        assert_eq!(entry.output_type, OutputType::Error);
        assert_eq!(entry.content, "Error message");
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_multiple_subscribers (lines 462-504) ---[0m
    async fn test_multiple_subscribers() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Create multiple subscribers first
        let mut rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let mut rx2 = manager.subscribe("sub-2".to_string()).unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Send a message
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Broadcast message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Both should receive it
        let timeout1 = tokio::time::timeout(Duration::from_millis(100), rx1.recv()).await;

        let timeout2 = tokio::time::timeout(Duration::from_millis(100), rx2.recv()).await;

        assert!(timeout1.is_ok());
        assert!(timeout2.is_ok());

        let entry1 = timeout1.unwrap();
        let entry2 = timeout2.unwrap();

        assert!(entry1.is_some());
        assert!(entry2.is_some());

        assert_eq!(entry1.unwrap().content, "Broadcast message");
        assert_eq!(entry2.unwrap().content, "Broadcast message");
    }

  ./crates/ccswarm/src/streaming/tests.rs:291-343 function test_agent_subscription <-> ./crates/ccswarm/src/streaming/tests.rs:462-504 function test_multiple_subscribers
  Similarity: 86.65%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_agent_subscription (lines 291-343) ---[0m
    async fn test_agent_subscription() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe to specific agent first
        let mut rx = manager
            .subscribe_agent("test-sub".to_string(), "agent-123".to_string())
            .unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add outputs
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Message from agent-123".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        monitoring
            .add_output(
                "agent-456".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Message from agent-456".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Should only receive from agent-123
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        let entry = entry.unwrap();
        assert_eq!(entry.agent_id, "agent-123");
        assert_eq!(entry.content, "Message from agent-123");

        // Should not receive any more messages
        let timeout = tokio::time::timeout(Duration::from_millis(50), rx.recv()).await;

        assert!(timeout.is_err()); // Timeout
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_multiple_subscribers (lines 462-504) ---[0m
    async fn test_multiple_subscribers() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Create multiple subscribers first
        let mut rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let mut rx2 = manager.subscribe("sub-2".to_string()).unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Send a message
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Broadcast message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Both should receive it
        let timeout1 = tokio::time::timeout(Duration::from_millis(100), rx1.recv()).await;

        let timeout2 = tokio::time::timeout(Duration::from_millis(100), rx2.recv()).await;

        assert!(timeout1.is_ok());
        assert!(timeout2.is_ok());

        let entry1 = timeout1.unwrap();
        let entry2 = timeout2.unwrap();

        assert!(entry1.is_some());
        assert!(entry2.is_some());

        assert_eq!(entry1.unwrap().content, "Broadcast message");
        assert_eq!(entry2.unwrap().content, "Broadcast message");
    }

  ./crates/ccswarm/src/streaming/tests.rs:45-91 function test_subscription_filtering <-> ./crates/ccswarm/src/streaming/tests.rs:508-550 function test_formatting_edge_cases
  Similarity: 89.49%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_subscription_filtering (lines 45-91) ---[0m
    fn test_subscription_filtering() {
        let (tx, _rx) = mpsc::unbounded_channel();
        let sub =
            StreamSubscription::new("test-sub".to_string(), tx).with_agent("agent-123".to_string());

        // Create test entries
        let matching_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        let non_matching_entry = OutputEntry::new(
            "agent-456".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&matching_entry));
        assert!(!sub.should_receive(&non_matching_entry));

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let sub = sub.with_filter(filter);

        assert!(!sub.should_receive(&matching_entry)); // Wrong type

        let error_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Error,
            "Error message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&error_entry));
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_formatting_edge_cases (lines 508-550) ---[0m
    fn test_formatting_edge_cases() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        // Test empty content
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "");

        // Test content with tabs
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Line\twith\ttabs".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "Line    with    tabs");

        // Test very short agent ID
        let entry = OutputEntry::new(
            "a".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert!(formatted.display_prefix.contains(" a"));
    }

  ./crates/ccswarm/src/streaming/tests.rs:170-224 function test_highlight_patterns <-> ./crates/ccswarm/src/streaming/tests.rs:251-287 function test_streaming_manager
  Similarity: 87.49%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_highlight_patterns (lines 170-224) ---[0m
    fn test_highlight_patterns() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        // Test error highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "An error occurred in the system".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let error_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Error));
        assert!(error_span.is_some());

        // Test warning highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Warning: deprecated function".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let warning_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Warning));
        assert!(warning_span.is_some());

        // Test success highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Task completed successfully".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let success_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Success));
        assert!(success_span.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_manager (lines 251-287) ---[0m
    async fn test_streaming_manager() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe to stream first
        let mut rx = manager.subscribe("test-sub".to_string()).unwrap();

        // Start the manager
        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add output to monitoring system
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Test message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Wait for message with longer timeout
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        assert_eq!(entry.unwrap().content, "Test message");

        // Unsubscribe
        manager.unsubscribe("test-sub").unwrap();
    }

  ./crates/ccswarm/src/streaming/tests.rs:45-91 function test_subscription_filtering <-> ./crates/ccswarm/src/streaming/tests.rs:347-398 function test_filtered_subscription
  Similarity: 81.04%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_subscription_filtering (lines 45-91) ---[0m
    fn test_subscription_filtering() {
        let (tx, _rx) = mpsc::unbounded_channel();
        let sub =
            StreamSubscription::new("test-sub".to_string(), tx).with_agent("agent-123".to_string());

        // Create test entries
        let matching_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        let non_matching_entry = OutputEntry::new(
            "agent-456".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&matching_entry));
        assert!(!sub.should_receive(&non_matching_entry));

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let sub = sub.with_filter(filter);

        assert!(!sub.should_receive(&matching_entry)); // Wrong type

        let error_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Error,
            "Error message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&error_entry));
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_filtered_subscription (lines 347-398) ---[0m
    async fn test_filtered_subscription() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe with error filter first
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let mut rx = manager
            .subscribe_filtered("test-sub".to_string(), filter)
            .unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add various outputs
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Info message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Error,
                "Error message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Should only receive error
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        let entry = entry.unwrap();
        assert_eq!(entry.output_type, OutputType::Error);
        assert_eq!(entry.content, "Error message");
    }

  ./crates/ccswarm/src/streaming/tests.rs:251-287 function test_streaming_manager <-> ./crates/ccswarm/src/streaming/tests.rs:291-343 function test_agent_subscription
  Similarity: 89.08%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_manager (lines 251-287) ---[0m
    async fn test_streaming_manager() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe to stream first
        let mut rx = manager.subscribe("test-sub".to_string()).unwrap();

        // Start the manager
        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add output to monitoring system
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Test message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Wait for message with longer timeout
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        assert_eq!(entry.unwrap().content, "Test message");

        // Unsubscribe
        manager.unsubscribe("test-sub").unwrap();
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_agent_subscription (lines 291-343) ---[0m
    async fn test_agent_subscription() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe to specific agent first
        let mut rx = manager
            .subscribe_agent("test-sub".to_string(), "agent-123".to_string())
            .unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add outputs
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Message from agent-123".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        monitoring
            .add_output(
                "agent-456".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Message from agent-456".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Should only receive from agent-123
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        let entry = entry.unwrap();
        assert_eq!(entry.agent_id, "agent-123");
        assert_eq!(entry.content, "Message from agent-123");

        // Should not receive any more messages
        let timeout = tokio::time::timeout(Duration::from_millis(50), rx.recv()).await;

        assert!(timeout.is_err()); // Timeout
    }

  ./crates/ccswarm/src/streaming/tests.rs:170-224 function test_highlight_patterns <-> ./crates/ccswarm/src/streaming/tests.rs:462-504 function test_multiple_subscribers
  Similarity: 81.55%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_highlight_patterns (lines 170-224) ---[0m
    fn test_highlight_patterns() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        // Test error highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "An error occurred in the system".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let error_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Error));
        assert!(error_span.is_some());

        // Test warning highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Warning: deprecated function".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let warning_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Warning));
        assert!(warning_span.is_some());

        // Test success highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Task completed successfully".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let success_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Success));
        assert!(success_span.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_multiple_subscribers (lines 462-504) ---[0m
    async fn test_multiple_subscribers() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Create multiple subscribers first
        let mut rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let mut rx2 = manager.subscribe("sub-2".to_string()).unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Send a message
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Broadcast message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Both should receive it
        let timeout1 = tokio::time::timeout(Duration::from_millis(100), rx1.recv()).await;

        let timeout2 = tokio::time::timeout(Duration::from_millis(100), rx2.recv()).await;

        assert!(timeout1.is_ok());
        assert!(timeout2.is_ok());

        let entry1 = timeout1.unwrap();
        let entry2 = timeout2.unwrap();

        assert!(entry1.is_some());
        assert!(entry2.is_some());

        assert_eq!(entry1.unwrap().content, "Broadcast message");
        assert_eq!(entry2.unwrap().content, "Broadcast message");
    }

  ./crates/ccswarm/src/streaming/tests.rs:45-91 function test_subscription_filtering <-> ./crates/ccswarm/src/streaming/tests.rs:291-343 function test_agent_subscription
  Similarity: 79.78%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_subscription_filtering (lines 45-91) ---[0m
    fn test_subscription_filtering() {
        let (tx, _rx) = mpsc::unbounded_channel();
        let sub =
            StreamSubscription::new("test-sub".to_string(), tx).with_agent("agent-123".to_string());

        // Create test entries
        let matching_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        let non_matching_entry = OutputEntry::new(
            "agent-456".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&matching_entry));
        assert!(!sub.should_receive(&non_matching_entry));

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let sub = sub.with_filter(filter);

        assert!(!sub.should_receive(&matching_entry)); // Wrong type

        let error_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Error,
            "Error message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&error_entry));
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_agent_subscription (lines 291-343) ---[0m
    async fn test_agent_subscription() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe to specific agent first
        let mut rx = manager
            .subscribe_agent("test-sub".to_string(), "agent-123".to_string())
            .unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add outputs
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Message from agent-123".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        monitoring
            .add_output(
                "agent-456".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Message from agent-456".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Should only receive from agent-123
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        let entry = entry.unwrap();
        assert_eq!(entry.agent_id, "agent-123");
        assert_eq!(entry.content, "Message from agent-123");

        // Should not receive any more messages
        let timeout = tokio::time::timeout(Duration::from_millis(50), rx.recv()).await;

        assert!(timeout.is_err()); // Timeout
    }

  ./crates/ccswarm/src/streaming/tests.rs:95-126 function test_output_formatter <-> ./crates/ccswarm/src/streaming/tests.rs:170-224 function test_highlight_patterns
  Similarity: 91.60%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_output_formatter (lines 95-126) ---[0m
    fn test_output_formatter() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        let entry = OutputEntry::new(
            "agent-123-very-long-id".to_string(),
            "Frontend".to_string(),
            OutputType::Error,
            "Error: Failed to compile".to_string(),
            Some("task-456".to_string()),
            "session-789".to_string(),
        );

        let formatted = formatter.format_entry(&entry);

        // Check prefix formatting
        assert!(formatted.display_prefix.contains("❌"));
        assert!(formatted.display_prefix.contains("Frontend"));
        assert!(formatted.display_prefix.contains("agent-12")); // Truncated
        assert!(formatted.display_prefix.contains("[task-456]"));

        // Check content formatting
        assert_eq!(formatted.formatted_content, "Error: Failed to compile");

        // Check highlighting
        assert!(!formatted.highlight_spans.is_empty());
        let error_highlight = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Error));
        assert!(error_highlight.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_highlight_patterns (lines 170-224) ---[0m
    fn test_highlight_patterns() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        // Test error highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "An error occurred in the system".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let error_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Error));
        assert!(error_span.is_some());

        // Test warning highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Warning: deprecated function".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let warning_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Warning));
        assert!(warning_span.is_some());

        // Test success highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Task completed successfully".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let success_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Success));
        assert!(success_span.is_some());
    }

  ./crates/ccswarm/src/streaming/tests.rs:347-398 function test_filtered_subscription <-> ./crates/ccswarm/src/streaming/tests.rs:508-550 function test_formatting_edge_cases
  Similarity: 80.86%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_filtered_subscription (lines 347-398) ---[0m
    async fn test_filtered_subscription() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe with error filter first
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let mut rx = manager
            .subscribe_filtered("test-sub".to_string(), filter)
            .unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add various outputs
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Info message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Error,
                "Error message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Should only receive error
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        let entry = entry.unwrap();
        assert_eq!(entry.output_type, OutputType::Error);
        assert_eq!(entry.content, "Error message");
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_formatting_edge_cases (lines 508-550) ---[0m
    fn test_formatting_edge_cases() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        // Test empty content
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "");

        // Test content with tabs
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Line\twith\ttabs".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "Line    with    tabs");

        // Test very short agent ID
        let entry = OutputEntry::new(
            "a".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert!(formatted.display_prefix.contains(" a"));
    }

  ./crates/ccswarm/src/streaming/tests.rs:291-343 function test_agent_subscription <-> ./crates/ccswarm/src/streaming/tests.rs:508-550 function test_formatting_edge_cases
  Similarity: 79.31%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_agent_subscription (lines 291-343) ---[0m
    async fn test_agent_subscription() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe to specific agent first
        let mut rx = manager
            .subscribe_agent("test-sub".to_string(), "agent-123".to_string())
            .unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add outputs
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Message from agent-123".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        monitoring
            .add_output(
                "agent-456".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Message from agent-456".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Should only receive from agent-123
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        let entry = entry.unwrap();
        assert_eq!(entry.agent_id, "agent-123");
        assert_eq!(entry.content, "Message from agent-123");

        // Should not receive any more messages
        let timeout = tokio::time::timeout(Duration::from_millis(50), rx.recv()).await;

        assert!(timeout.is_err()); // Timeout
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_formatting_edge_cases (lines 508-550) ---[0m
    fn test_formatting_edge_cases() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        // Test empty content
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "");

        // Test content with tabs
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Line\twith\ttabs".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "Line    with    tabs");

        // Test very short agent ID
        let entry = OutputEntry::new(
            "a".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert!(formatted.display_prefix.contains(" a"));
    }

  ./crates/ccswarm/src/streaming/tests.rs:95-126 function test_output_formatter <-> ./crates/ccswarm/src/streaming/tests.rs:291-343 function test_agent_subscription
  Similarity: 84.60%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_output_formatter (lines 95-126) ---[0m
    fn test_output_formatter() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        let entry = OutputEntry::new(
            "agent-123-very-long-id".to_string(),
            "Frontend".to_string(),
            OutputType::Error,
            "Error: Failed to compile".to_string(),
            Some("task-456".to_string()),
            "session-789".to_string(),
        );

        let formatted = formatter.format_entry(&entry);

        // Check prefix formatting
        assert!(formatted.display_prefix.contains("❌"));
        assert!(formatted.display_prefix.contains("Frontend"));
        assert!(formatted.display_prefix.contains("agent-12")); // Truncated
        assert!(formatted.display_prefix.contains("[task-456]"));

        // Check content formatting
        assert_eq!(formatted.formatted_content, "Error: Failed to compile");

        // Check highlighting
        assert!(!formatted.highlight_spans.is_empty());
        let error_highlight = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Error));
        assert!(error_highlight.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_agent_subscription (lines 291-343) ---[0m
    async fn test_agent_subscription() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe to specific agent first
        let mut rx = manager
            .subscribe_agent("test-sub".to_string(), "agent-123".to_string())
            .unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add outputs
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Message from agent-123".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        monitoring
            .add_output(
                "agent-456".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Message from agent-456".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Should only receive from agent-123
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        let entry = entry.unwrap();
        assert_eq!(entry.agent_id, "agent-123");
        assert_eq!(entry.content, "Message from agent-123");

        // Should not receive any more messages
        let timeout = tokio::time::timeout(Duration::from_millis(50), rx.recv()).await;

        assert!(timeout.is_err()); // Timeout
    }

  ./crates/ccswarm/src/streaming/tests.rs:170-224 function test_highlight_patterns <-> ./crates/ccswarm/src/streaming/tests.rs:432-458 function test_clear_subscriptions
  Similarity: 87.16%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_highlight_patterns (lines 170-224) ---[0m
    fn test_highlight_patterns() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        // Test error highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "An error occurred in the system".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let error_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Error));
        assert!(error_span.is_some());

        // Test warning highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Warning: deprecated function".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let warning_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Warning));
        assert!(warning_span.is_some());

        // Test success highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Task completed successfully".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let success_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Success));
        assert!(success_span.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_clear_subscriptions (lines 432-458) ---[0m
    async fn test_clear_subscriptions() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create multiple subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();
        let _rx3 = manager.subscribe("sub-3".to_string()).unwrap();

        // Wait for stats updater to run
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 3);

        // Clear all
        manager.clear_subscriptions().unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 0);
    }

  ./crates/ccswarm/src/streaming/tests.rs:45-91 function test_subscription_filtering <-> ./crates/ccswarm/src/streaming/tests.rs:251-287 function test_streaming_manager
  Similarity: 84.99%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_subscription_filtering (lines 45-91) ---[0m
    fn test_subscription_filtering() {
        let (tx, _rx) = mpsc::unbounded_channel();
        let sub =
            StreamSubscription::new("test-sub".to_string(), tx).with_agent("agent-123".to_string());

        // Create test entries
        let matching_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        let non_matching_entry = OutputEntry::new(
            "agent-456".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&matching_entry));
        assert!(!sub.should_receive(&non_matching_entry));

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let sub = sub.with_filter(filter);

        assert!(!sub.should_receive(&matching_entry)); // Wrong type

        let error_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Error,
            "Error message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&error_entry));
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_manager (lines 251-287) ---[0m
    async fn test_streaming_manager() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe to stream first
        let mut rx = manager.subscribe("test-sub".to_string()).unwrap();

        // Start the manager
        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add output to monitoring system
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Test message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Wait for message with longer timeout
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        assert_eq!(entry.unwrap().content, "Test message");

        // Unsubscribe
        manager.unsubscribe("test-sub").unwrap();
    }

  ./crates/ccswarm/src/streaming/tests.rs:170-224 function test_highlight_patterns <-> ./crates/ccswarm/src/streaming/tests.rs:402-428 function test_streaming_stats
  Similarity: 86.92%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_highlight_patterns (lines 170-224) ---[0m
    fn test_highlight_patterns() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        // Test error highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "An error occurred in the system".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let error_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Error));
        assert!(error_span.is_some());

        // Test warning highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Warning: deprecated function".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let warning_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Warning));
        assert!(warning_span.is_some());

        // Test success highlighting
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Task completed successfully".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let success_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Success));
        assert!(success_span.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_stats (lines 402-428) ---[0m
    async fn test_streaming_stats() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();

        // Wait for stats updater to run (runs every second)
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 2);
        assert_eq!(stats.subscription_details.len(), 2);

        // Unsubscribe one
        manager.unsubscribe("sub-1").unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 1);
    }

  ./crates/ccswarm/src/streaming/tests.rs:95-126 function test_output_formatter <-> ./crates/ccswarm/src/streaming/tests.rs:347-398 function test_filtered_subscription
  Similarity: 84.74%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_output_formatter (lines 95-126) ---[0m
    fn test_output_formatter() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        let entry = OutputEntry::new(
            "agent-123-very-long-id".to_string(),
            "Frontend".to_string(),
            OutputType::Error,
            "Error: Failed to compile".to_string(),
            Some("task-456".to_string()),
            "session-789".to_string(),
        );

        let formatted = formatter.format_entry(&entry);

        // Check prefix formatting
        assert!(formatted.display_prefix.contains("❌"));
        assert!(formatted.display_prefix.contains("Frontend"));
        assert!(formatted.display_prefix.contains("agent-12")); // Truncated
        assert!(formatted.display_prefix.contains("[task-456]"));

        // Check content formatting
        assert_eq!(formatted.formatted_content, "Error: Failed to compile");

        // Check highlighting
        assert!(!formatted.highlight_spans.is_empty());
        let error_highlight = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Error));
        assert!(error_highlight.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_filtered_subscription (lines 347-398) ---[0m
    async fn test_filtered_subscription() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe with error filter first
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let mut rx = manager
            .subscribe_filtered("test-sub".to_string(), filter)
            .unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add various outputs
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Info message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Error,
                "Error message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Should only receive error
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        let entry = entry.unwrap();
        assert_eq!(entry.output_type, OutputType::Error);
        assert_eq!(entry.content, "Error message");
    }

  ./crates/ccswarm/src/streaming/tests.rs:251-287 function test_streaming_manager <-> ./crates/ccswarm/src/streaming/tests.rs:462-504 function test_multiple_subscribers
  Similarity: 88.93%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_manager (lines 251-287) ---[0m
    async fn test_streaming_manager() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe to stream first
        let mut rx = manager.subscribe("test-sub".to_string()).unwrap();

        // Start the manager
        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add output to monitoring system
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Test message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Wait for message with longer timeout
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        assert_eq!(entry.unwrap().content, "Test message");

        // Unsubscribe
        manager.unsubscribe("test-sub").unwrap();
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_multiple_subscribers (lines 462-504) ---[0m
    async fn test_multiple_subscribers() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Create multiple subscribers first
        let mut rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let mut rx2 = manager.subscribe("sub-2".to_string()).unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Send a message
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Broadcast message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Both should receive it
        let timeout1 = tokio::time::timeout(Duration::from_millis(100), rx1.recv()).await;

        let timeout2 = tokio::time::timeout(Duration::from_millis(100), rx2.recv()).await;

        assert!(timeout1.is_ok());
        assert!(timeout2.is_ok());

        let entry1 = timeout1.unwrap();
        let entry2 = timeout2.unwrap();

        assert!(entry1.is_some());
        assert!(entry2.is_some());

        assert_eq!(entry1.unwrap().content, "Broadcast message");
        assert_eq!(entry2.unwrap().content, "Broadcast message");
    }

  ./crates/ccswarm/src/streaming/tests.rs:291-343 function test_agent_subscription <-> ./crates/ccswarm/src/streaming/tests.rs:432-458 function test_clear_subscriptions
  Similarity: 87.66%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_agent_subscription (lines 291-343) ---[0m
    async fn test_agent_subscription() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe to specific agent first
        let mut rx = manager
            .subscribe_agent("test-sub".to_string(), "agent-123".to_string())
            .unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add outputs
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Message from agent-123".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        monitoring
            .add_output(
                "agent-456".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Message from agent-456".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Should only receive from agent-123
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        let entry = entry.unwrap();
        assert_eq!(entry.agent_id, "agent-123");
        assert_eq!(entry.content, "Message from agent-123");

        // Should not receive any more messages
        let timeout = tokio::time::timeout(Duration::from_millis(50), rx.recv()).await;

        assert!(timeout.is_err()); // Timeout
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_clear_subscriptions (lines 432-458) ---[0m
    async fn test_clear_subscriptions() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create multiple subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();
        let _rx3 = manager.subscribe("sub-3".to_string()).unwrap();

        // Wait for stats updater to run
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 3);

        // Clear all
        manager.clear_subscriptions().unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 0);
    }

  ./crates/ccswarm/src/streaming/tests.rs:347-398 function test_filtered_subscription <-> ./crates/ccswarm/src/streaming/tests.rs:402-428 function test_streaming_stats
  Similarity: 87.90%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_filtered_subscription (lines 347-398) ---[0m
    async fn test_filtered_subscription() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe with error filter first
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let mut rx = manager
            .subscribe_filtered("test-sub".to_string(), filter)
            .unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add various outputs
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Info message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Error,
                "Error message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Should only receive error
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        let entry = entry.unwrap();
        assert_eq!(entry.output_type, OutputType::Error);
        assert_eq!(entry.content, "Error message");
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_stats (lines 402-428) ---[0m
    async fn test_streaming_stats() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();

        // Wait for stats updater to run (runs every second)
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 2);
        assert_eq!(stats.subscription_details.len(), 2);

        // Unsubscribe one
        manager.unsubscribe("sub-1").unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 1);
    }

  ./crates/ccswarm/src/streaming/tests.rs:45-91 function test_subscription_filtering <-> ./crates/ccswarm/src/streaming/tests.rs:95-126 function test_output_formatter
  Similarity: 87.85%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_subscription_filtering (lines 45-91) ---[0m
    fn test_subscription_filtering() {
        let (tx, _rx) = mpsc::unbounded_channel();
        let sub =
            StreamSubscription::new("test-sub".to_string(), tx).with_agent("agent-123".to_string());

        // Create test entries
        let matching_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        let non_matching_entry = OutputEntry::new(
            "agent-456".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&matching_entry));
        assert!(!sub.should_receive(&non_matching_entry));

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let sub = sub.with_filter(filter);

        assert!(!sub.should_receive(&matching_entry)); // Wrong type

        let error_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Error,
            "Error message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&error_entry));
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_output_formatter (lines 95-126) ---[0m
    fn test_output_formatter() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        let entry = OutputEntry::new(
            "agent-123-very-long-id".to_string(),
            "Frontend".to_string(),
            OutputType::Error,
            "Error: Failed to compile".to_string(),
            Some("task-456".to_string()),
            "session-789".to_string(),
        );

        let formatted = formatter.format_entry(&entry);

        // Check prefix formatting
        assert!(formatted.display_prefix.contains("❌"));
        assert!(formatted.display_prefix.contains("Frontend"));
        assert!(formatted.display_prefix.contains("agent-12")); // Truncated
        assert!(formatted.display_prefix.contains("[task-456]"));

        // Check content formatting
        assert_eq!(formatted.formatted_content, "Error: Failed to compile");

        // Check highlighting
        assert!(!formatted.highlight_spans.is_empty());
        let error_highlight = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Error));
        assert!(error_highlight.is_some());
    }

  ./crates/ccswarm/src/streaming/tests.rs:291-343 function test_agent_subscription <-> ./crates/ccswarm/src/streaming/tests.rs:402-428 function test_streaming_stats
  Similarity: 86.37%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_agent_subscription (lines 291-343) ---[0m
    async fn test_agent_subscription() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe to specific agent first
        let mut rx = manager
            .subscribe_agent("test-sub".to_string(), "agent-123".to_string())
            .unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add outputs
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Message from agent-123".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        monitoring
            .add_output(
                "agent-456".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Message from agent-456".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Should only receive from agent-123
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        let entry = entry.unwrap();
        assert_eq!(entry.agent_id, "agent-123");
        assert_eq!(entry.content, "Message from agent-123");

        // Should not receive any more messages
        let timeout = tokio::time::timeout(Duration::from_millis(50), rx.recv()).await;

        assert!(timeout.is_err()); // Timeout
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_stats (lines 402-428) ---[0m
    async fn test_streaming_stats() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();

        // Wait for stats updater to run (runs every second)
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 2);
        assert_eq!(stats.subscription_details.len(), 2);

        // Unsubscribe one
        manager.unsubscribe("sub-1").unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 1);
    }

  ./crates/ccswarm/src/streaming/tests.rs:347-398 function test_filtered_subscription <-> ./crates/ccswarm/src/streaming/tests.rs:432-458 function test_clear_subscriptions
  Similarity: 87.00%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_filtered_subscription (lines 347-398) ---[0m
    async fn test_filtered_subscription() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe with error filter first
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let mut rx = manager
            .subscribe_filtered("test-sub".to_string(), filter)
            .unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add various outputs
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Info message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Error,
                "Error message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Should only receive error
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        let entry = entry.unwrap();
        assert_eq!(entry.output_type, OutputType::Error);
        assert_eq!(entry.content, "Error message");
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_clear_subscriptions (lines 432-458) ---[0m
    async fn test_clear_subscriptions() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create multiple subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();
        let _rx3 = manager.subscribe("sub-3".to_string()).unwrap();

        // Wait for stats updater to run
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 3);

        // Clear all
        manager.clear_subscriptions().unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 0);
    }

  ./crates/ccswarm/src/streaming/tests.rs:45-91 function test_subscription_filtering <-> ./crates/ccswarm/src/streaming/tests.rs:462-504 function test_multiple_subscribers
  Similarity: 73.87%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_subscription_filtering (lines 45-91) ---[0m
    fn test_subscription_filtering() {
        let (tx, _rx) = mpsc::unbounded_channel();
        let sub =
            StreamSubscription::new("test-sub".to_string(), tx).with_agent("agent-123".to_string());

        // Create test entries
        let matching_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        let non_matching_entry = OutputEntry::new(
            "agent-456".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&matching_entry));
        assert!(!sub.should_receive(&non_matching_entry));

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let sub = sub.with_filter(filter);

        assert!(!sub.should_receive(&matching_entry)); // Wrong type

        let error_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Error,
            "Error message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&error_entry));
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_multiple_subscribers (lines 462-504) ---[0m
    async fn test_multiple_subscribers() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Create multiple subscribers first
        let mut rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let mut rx2 = manager.subscribe("sub-2".to_string()).unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Send a message
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Broadcast message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Both should receive it
        let timeout1 = tokio::time::timeout(Duration::from_millis(100), rx1.recv()).await;

        let timeout2 = tokio::time::timeout(Duration::from_millis(100), rx2.recv()).await;

        assert!(timeout1.is_ok());
        assert!(timeout2.is_ok());

        let entry1 = timeout1.unwrap();
        let entry2 = timeout2.unwrap();

        assert!(entry1.is_some());
        assert!(entry2.is_some());

        assert_eq!(entry1.unwrap().content, "Broadcast message");
        assert_eq!(entry2.unwrap().content, "Broadcast message");
    }

  ./crates/ccswarm/src/streaming/tests.rs:251-287 function test_streaming_manager <-> ./crates/ccswarm/src/streaming/tests.rs:508-550 function test_formatting_edge_cases
  Similarity: 82.85%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_manager (lines 251-287) ---[0m
    async fn test_streaming_manager() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe to stream first
        let mut rx = manager.subscribe("test-sub".to_string()).unwrap();

        // Start the manager
        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add output to monitoring system
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Test message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Wait for message with longer timeout
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        assert_eq!(entry.unwrap().content, "Test message");

        // Unsubscribe
        manager.unsubscribe("test-sub").unwrap();
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_formatting_edge_cases (lines 508-550) ---[0m
    fn test_formatting_edge_cases() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        // Test empty content
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "");

        // Test content with tabs
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Line\twith\ttabs".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "Line    with    tabs");

        // Test very short agent ID
        let entry = OutputEntry::new(
            "a".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert!(formatted.display_prefix.contains(" a"));
    }

  ./crates/ccswarm/src/streaming/tests.rs:95-126 function test_output_formatter <-> ./crates/ccswarm/src/streaming/tests.rs:508-550 function test_formatting_edge_cases
  Similarity: 87.62%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_output_formatter (lines 95-126) ---[0m
    fn test_output_formatter() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        let entry = OutputEntry::new(
            "agent-123-very-long-id".to_string(),
            "Frontend".to_string(),
            OutputType::Error,
            "Error: Failed to compile".to_string(),
            Some("task-456".to_string()),
            "session-789".to_string(),
        );

        let formatted = formatter.format_entry(&entry);

        // Check prefix formatting
        assert!(formatted.display_prefix.contains("❌"));
        assert!(formatted.display_prefix.contains("Frontend"));
        assert!(formatted.display_prefix.contains("agent-12")); // Truncated
        assert!(formatted.display_prefix.contains("[task-456]"));

        // Check content formatting
        assert_eq!(formatted.formatted_content, "Error: Failed to compile");

        // Check highlighting
        assert!(!formatted.highlight_spans.is_empty());
        let error_highlight = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Error));
        assert!(error_highlight.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_formatting_edge_cases (lines 508-550) ---[0m
    fn test_formatting_edge_cases() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        // Test empty content
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "");

        // Test content with tabs
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Line\twith\ttabs".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "Line    with    tabs");

        // Test very short agent ID
        let entry = OutputEntry::new(
            "a".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert!(formatted.display_prefix.contains(" a"));
    }

  ./crates/ccswarm/src/streaming/tests.rs:462-504 function test_multiple_subscribers <-> ./crates/ccswarm/src/streaming/tests.rs:508-550 function test_formatting_edge_cases
  Similarity: 74.08%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_multiple_subscribers (lines 462-504) ---[0m
    async fn test_multiple_subscribers() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Create multiple subscribers first
        let mut rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let mut rx2 = manager.subscribe("sub-2".to_string()).unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Send a message
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Broadcast message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Both should receive it
        let timeout1 = tokio::time::timeout(Duration::from_millis(100), rx1.recv()).await;

        let timeout2 = tokio::time::timeout(Duration::from_millis(100), rx2.recv()).await;

        assert!(timeout1.is_ok());
        assert!(timeout2.is_ok());

        let entry1 = timeout1.unwrap();
        let entry2 = timeout2.unwrap();

        assert!(entry1.is_some());
        assert!(entry2.is_some());

        assert_eq!(entry1.unwrap().content, "Broadcast message");
        assert_eq!(entry2.unwrap().content, "Broadcast message");
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_formatting_edge_cases (lines 508-550) ---[0m
    fn test_formatting_edge_cases() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        // Test empty content
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "");

        // Test content with tabs
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Line\twith\ttabs".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "Line    with    tabs");

        // Test very short agent ID
        let entry = OutputEntry::new(
            "a".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert!(formatted.display_prefix.contains(" a"));
    }

  ./crates/ccswarm/src/streaming/tests.rs:45-91 function test_subscription_filtering <-> ./crates/ccswarm/src/streaming/tests.rs:402-428 function test_streaming_stats
  Similarity: 84.04%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_subscription_filtering (lines 45-91) ---[0m
    fn test_subscription_filtering() {
        let (tx, _rx) = mpsc::unbounded_channel();
        let sub =
            StreamSubscription::new("test-sub".to_string(), tx).with_agent("agent-123".to_string());

        // Create test entries
        let matching_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        let non_matching_entry = OutputEntry::new(
            "agent-456".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&matching_entry));
        assert!(!sub.should_receive(&non_matching_entry));

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let sub = sub.with_filter(filter);

        assert!(!sub.should_receive(&matching_entry)); // Wrong type

        let error_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Error,
            "Error message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&error_entry));
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_stats (lines 402-428) ---[0m
    async fn test_streaming_stats() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();

        // Wait for stats updater to run (runs every second)
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 2);
        assert_eq!(stats.subscription_details.len(), 2);

        // Unsubscribe one
        manager.unsubscribe("sub-1").unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 1);
    }

  ./crates/ccswarm/src/streaming/tests.rs:45-91 function test_subscription_filtering <-> ./crates/ccswarm/src/streaming/tests.rs:432-458 function test_clear_subscriptions
  Similarity: 83.61%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_subscription_filtering (lines 45-91) ---[0m
    fn test_subscription_filtering() {
        let (tx, _rx) = mpsc::unbounded_channel();
        let sub =
            StreamSubscription::new("test-sub".to_string(), tx).with_agent("agent-123".to_string());

        // Create test entries
        let matching_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        let non_matching_entry = OutputEntry::new(
            "agent-456".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&matching_entry));
        assert!(!sub.should_receive(&non_matching_entry));

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let sub = sub.with_filter(filter);

        assert!(!sub.should_receive(&matching_entry)); // Wrong type

        let error_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Error,
            "Error message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&error_entry));
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_clear_subscriptions (lines 432-458) ---[0m
    async fn test_clear_subscriptions() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create multiple subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();
        let _rx3 = manager.subscribe("sub-3".to_string()).unwrap();

        // Wait for stats updater to run
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 3);

        // Clear all
        manager.clear_subscriptions().unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 0);
    }

  ./crates/ccswarm/src/streaming/tests.rs:95-126 function test_output_formatter <-> ./crates/ccswarm/src/streaming/tests.rs:462-504 function test_multiple_subscribers
  Similarity: 79.09%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_output_formatter (lines 95-126) ---[0m
    fn test_output_formatter() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        let entry = OutputEntry::new(
            "agent-123-very-long-id".to_string(),
            "Frontend".to_string(),
            OutputType::Error,
            "Error: Failed to compile".to_string(),
            Some("task-456".to_string()),
            "session-789".to_string(),
        );

        let formatted = formatter.format_entry(&entry);

        // Check prefix formatting
        assert!(formatted.display_prefix.contains("❌"));
        assert!(formatted.display_prefix.contains("Frontend"));
        assert!(formatted.display_prefix.contains("agent-12")); // Truncated
        assert!(formatted.display_prefix.contains("[task-456]"));

        // Check content formatting
        assert_eq!(formatted.formatted_content, "Error: Failed to compile");

        // Check highlighting
        assert!(!formatted.highlight_spans.is_empty());
        let error_highlight = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Error));
        assert!(error_highlight.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_multiple_subscribers (lines 462-504) ---[0m
    async fn test_multiple_subscribers() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Create multiple subscribers first
        let mut rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let mut rx2 = manager.subscribe("sub-2".to_string()).unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Send a message
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Broadcast message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Both should receive it
        let timeout1 = tokio::time::timeout(Duration::from_millis(100), rx1.recv()).await;

        let timeout2 = tokio::time::timeout(Duration::from_millis(100), rx2.recv()).await;

        assert!(timeout1.is_ok());
        assert!(timeout2.is_ok());

        let entry1 = timeout1.unwrap();
        let entry2 = timeout2.unwrap();

        assert!(entry1.is_some());
        assert!(entry2.is_some());

        assert_eq!(entry1.unwrap().content, "Broadcast message");
        assert_eq!(entry2.unwrap().content, "Broadcast message");
    }

  ./crates/ccswarm/src/streaming/tests.rs:402-428 function test_streaming_stats <-> ./crates/ccswarm/src/streaming/tests.rs:462-504 function test_multiple_subscribers
  Similarity: 84.56%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_stats (lines 402-428) ---[0m
    async fn test_streaming_stats() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();

        // Wait for stats updater to run (runs every second)
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 2);
        assert_eq!(stats.subscription_details.len(), 2);

        // Unsubscribe one
        manager.unsubscribe("sub-1").unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 1);
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_multiple_subscribers (lines 462-504) ---[0m
    async fn test_multiple_subscribers() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Create multiple subscribers first
        let mut rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let mut rx2 = manager.subscribe("sub-2".to_string()).unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Send a message
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Broadcast message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Both should receive it
        let timeout1 = tokio::time::timeout(Duration::from_millis(100), rx1.recv()).await;

        let timeout2 = tokio::time::timeout(Duration::from_millis(100), rx2.recv()).await;

        assert!(timeout1.is_ok());
        assert!(timeout2.is_ok());

        let entry1 = timeout1.unwrap();
        let entry2 = timeout2.unwrap();

        assert!(entry1.is_some());
        assert!(entry2.is_some());

        assert_eq!(entry1.unwrap().content, "Broadcast message");
        assert_eq!(entry2.unwrap().content, "Broadcast message");
    }

  ./crates/ccswarm/src/streaming/tests.rs:432-458 function test_clear_subscriptions <-> ./crates/ccswarm/src/streaming/tests.rs:462-504 function test_multiple_subscribers
  Similarity: 84.24%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_clear_subscriptions (lines 432-458) ---[0m
    async fn test_clear_subscriptions() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create multiple subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();
        let _rx3 = manager.subscribe("sub-3".to_string()).unwrap();

        // Wait for stats updater to run
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 3);

        // Clear all
        manager.clear_subscriptions().unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 0);
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_multiple_subscribers (lines 462-504) ---[0m
    async fn test_multiple_subscribers() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Create multiple subscribers first
        let mut rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let mut rx2 = manager.subscribe("sub-2".to_string()).unwrap();

        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Send a message
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Broadcast message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Both should receive it
        let timeout1 = tokio::time::timeout(Duration::from_millis(100), rx1.recv()).await;

        let timeout2 = tokio::time::timeout(Duration::from_millis(100), rx2.recv()).await;

        assert!(timeout1.is_ok());
        assert!(timeout2.is_ok());

        let entry1 = timeout1.unwrap();
        let entry2 = timeout2.unwrap();

        assert!(entry1.is_some());
        assert!(entry2.is_some());

        assert_eq!(entry1.unwrap().content, "Broadcast message");
        assert_eq!(entry2.unwrap().content, "Broadcast message");
    }

  ./crates/ccswarm/src/streaming/tests.rs:22-41 function test_stream_subscription <-> ./crates/ccswarm/src/streaming/tests.rs:45-91 function test_subscription_filtering
  Similarity: 87.95%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_stream_subscription (lines 22-41) ---[0m
    fn test_stream_subscription() {
        let (tx, _rx) = mpsc::unbounded_channel();
        let sub = StreamSubscription::new("test-sub".to_string(), tx);

        assert_eq!(sub.id, "test-sub");
        assert!(sub.agent_id.is_none());
        assert!(sub.filter.is_none());

        // Test with agent
        let sub = sub.with_agent("agent-123".to_string());
        assert_eq!(sub.agent_id, Some("agent-123".to_string()));

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let sub = sub.with_filter(filter);
        assert!(sub.filter.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_subscription_filtering (lines 45-91) ---[0m
    fn test_subscription_filtering() {
        let (tx, _rx) = mpsc::unbounded_channel();
        let sub =
            StreamSubscription::new("test-sub".to_string(), tx).with_agent("agent-123".to_string());

        // Create test entries
        let matching_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        let non_matching_entry = OutputEntry::new(
            "agent-456".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&matching_entry));
        assert!(!sub.should_receive(&non_matching_entry));

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let sub = sub.with_filter(filter);

        assert!(!sub.should_receive(&matching_entry)); // Wrong type

        let error_entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Error,
            "Error message".to_string(),
            None,
            "session-1".to_string(),
        );

        assert!(sub.should_receive(&error_entry));
    }

  ./crates/ccswarm/src/streaming/tests.rs:432-458 function test_clear_subscriptions <-> ./crates/ccswarm/src/streaming/tests.rs:508-550 function test_formatting_edge_cases
  Similarity: 83.65%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_clear_subscriptions (lines 432-458) ---[0m
    async fn test_clear_subscriptions() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create multiple subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();
        let _rx3 = manager.subscribe("sub-3".to_string()).unwrap();

        // Wait for stats updater to run
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 3);

        // Clear all
        manager.clear_subscriptions().unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 0);
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_formatting_edge_cases (lines 508-550) ---[0m
    fn test_formatting_edge_cases() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        // Test empty content
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "");

        // Test content with tabs
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Line\twith\ttabs".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "Line    with    tabs");

        // Test very short agent ID
        let entry = OutputEntry::new(
            "a".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert!(formatted.display_prefix.contains(" a"));
    }

  ./crates/ccswarm/src/streaming/tests.rs:95-126 function test_output_formatter <-> ./crates/ccswarm/src/streaming/tests.rs:251-287 function test_streaming_manager
  Similarity: 83.99%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_output_formatter (lines 95-126) ---[0m
    fn test_output_formatter() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        let entry = OutputEntry::new(
            "agent-123-very-long-id".to_string(),
            "Frontend".to_string(),
            OutputType::Error,
            "Error: Failed to compile".to_string(),
            Some("task-456".to_string()),
            "session-789".to_string(),
        );

        let formatted = formatter.format_entry(&entry);

        // Check prefix formatting
        assert!(formatted.display_prefix.contains("❌"));
        assert!(formatted.display_prefix.contains("Frontend"));
        assert!(formatted.display_prefix.contains("agent-12")); // Truncated
        assert!(formatted.display_prefix.contains("[task-456]"));

        // Check content formatting
        assert_eq!(formatted.formatted_content, "Error: Failed to compile");

        // Check highlighting
        assert!(!formatted.highlight_spans.is_empty());
        let error_highlight = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Error));
        assert!(error_highlight.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_manager (lines 251-287) ---[0m
    async fn test_streaming_manager() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe to stream first
        let mut rx = manager.subscribe("test-sub".to_string()).unwrap();

        // Start the manager
        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add output to monitoring system
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Test message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Wait for message with longer timeout
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        assert_eq!(entry.unwrap().content, "Test message");

        // Unsubscribe
        manager.unsubscribe("test-sub").unwrap();
    }

  ./crates/ccswarm/src/streaming/tests.rs:402-428 function test_streaming_stats <-> ./crates/ccswarm/src/streaming/tests.rs:508-550 function test_formatting_edge_cases
  Similarity: 82.35%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_stats (lines 402-428) ---[0m
    async fn test_streaming_stats() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();

        // Wait for stats updater to run (runs every second)
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 2);
        assert_eq!(stats.subscription_details.len(), 2);

        // Unsubscribe one
        manager.unsubscribe("sub-1").unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 1);
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_formatting_edge_cases (lines 508-550) ---[0m
    fn test_formatting_edge_cases() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        // Test empty content
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "");

        // Test content with tabs
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Line\twith\ttabs".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "Line    with    tabs");

        // Test very short agent ID
        let entry = OutputEntry::new(
            "a".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert!(formatted.display_prefix.contains(" a"));
    }

  ./crates/ccswarm/src/streaming/tests.rs:251-287 function test_streaming_manager <-> ./crates/ccswarm/src/streaming/tests.rs:402-428 function test_streaming_stats
  Similarity: 86.67%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_manager (lines 251-287) ---[0m
    async fn test_streaming_manager() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe to stream first
        let mut rx = manager.subscribe("test-sub".to_string()).unwrap();

        // Start the manager
        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add output to monitoring system
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Test message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Wait for message with longer timeout
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        assert_eq!(entry.unwrap().content, "Test message");

        // Unsubscribe
        manager.unsubscribe("test-sub").unwrap();
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_stats (lines 402-428) ---[0m
    async fn test_streaming_stats() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();

        // Wait for stats updater to run (runs every second)
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 2);
        assert_eq!(stats.subscription_details.len(), 2);

        // Unsubscribe one
        manager.unsubscribe("sub-1").unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 1);
    }

  ./crates/ccswarm/src/streaming/tests.rs:22-41 function test_stream_subscription <-> ./crates/ccswarm/src/streaming/tests.rs:508-550 function test_formatting_edge_cases
  Similarity: 88.00%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_stream_subscription (lines 22-41) ---[0m
    fn test_stream_subscription() {
        let (tx, _rx) = mpsc::unbounded_channel();
        let sub = StreamSubscription::new("test-sub".to_string(), tx);

        assert_eq!(sub.id, "test-sub");
        assert!(sub.agent_id.is_none());
        assert!(sub.filter.is_none());

        // Test with agent
        let sub = sub.with_agent("agent-123".to_string());
        assert_eq!(sub.agent_id, Some("agent-123".to_string()));

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let sub = sub.with_filter(filter);
        assert!(sub.filter.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_formatting_edge_cases (lines 508-550) ---[0m
    fn test_formatting_edge_cases() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        // Test empty content
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "");

        // Test content with tabs
        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Line\twith\ttabs".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "Line    with    tabs");

        // Test very short agent ID
        let entry = OutputEntry::new(
            "a".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Test".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert!(formatted.display_prefix.contains(" a"));
    }

  ./crates/ccswarm/src/streaming/tests.rs:251-287 function test_streaming_manager <-> ./crates/ccswarm/src/streaming/tests.rs:432-458 function test_clear_subscriptions
  Similarity: 86.19%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_manager (lines 251-287) ---[0m
    async fn test_streaming_manager() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe to stream first
        let mut rx = manager.subscribe("test-sub".to_string()).unwrap();

        // Start the manager
        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add output to monitoring system
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Test message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Wait for message with longer timeout
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        assert_eq!(entry.unwrap().content, "Test message");

        // Unsubscribe
        manager.unsubscribe("test-sub").unwrap();
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_clear_subscriptions (lines 432-458) ---[0m
    async fn test_clear_subscriptions() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create multiple subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();
        let _rx3 = manager.subscribe("sub-3".to_string()).unwrap();

        // Wait for stats updater to run
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 3);

        // Clear all
        manager.clear_subscriptions().unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 0);
    }

  ./crates/ccswarm/src/streaming/tests.rs:402-428 function test_streaming_stats <-> ./crates/ccswarm/src/streaming/tests.rs:432-458 function test_clear_subscriptions
  Similarity: 96.37%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_stats (lines 402-428) ---[0m
    async fn test_streaming_stats() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();

        // Wait for stats updater to run (runs every second)
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 2);
        assert_eq!(stats.subscription_details.len(), 2);

        // Unsubscribe one
        manager.unsubscribe("sub-1").unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 1);
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_clear_subscriptions (lines 432-458) ---[0m
    async fn test_clear_subscriptions() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create multiple subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();
        let _rx3 = manager.subscribe("sub-3".to_string()).unwrap();

        // Wait for stats updater to run
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 3);

        // Clear all
        manager.clear_subscriptions().unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 0);
    }

  ./crates/ccswarm/src/streaming/tests.rs:95-126 function test_output_formatter <-> ./crates/ccswarm/src/streaming/tests.rs:402-428 function test_streaming_stats
  Similarity: 83.20%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_output_formatter (lines 95-126) ---[0m
    fn test_output_formatter() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        let entry = OutputEntry::new(
            "agent-123-very-long-id".to_string(),
            "Frontend".to_string(),
            OutputType::Error,
            "Error: Failed to compile".to_string(),
            Some("task-456".to_string()),
            "session-789".to_string(),
        );

        let formatted = formatter.format_entry(&entry);

        // Check prefix formatting
        assert!(formatted.display_prefix.contains("❌"));
        assert!(formatted.display_prefix.contains("Frontend"));
        assert!(formatted.display_prefix.contains("agent-12")); // Truncated
        assert!(formatted.display_prefix.contains("[task-456]"));

        // Check content formatting
        assert_eq!(formatted.formatted_content, "Error: Failed to compile");

        // Check highlighting
        assert!(!formatted.highlight_spans.is_empty());
        let error_highlight = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Error));
        assert!(error_highlight.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_stats (lines 402-428) ---[0m
    async fn test_streaming_stats() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();

        // Wait for stats updater to run (runs every second)
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 2);
        assert_eq!(stats.subscription_details.len(), 2);

        // Unsubscribe one
        manager.unsubscribe("sub-1").unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 1);
    }

  ./crates/ccswarm/src/streaming/tests.rs:95-126 function test_output_formatter <-> ./crates/ccswarm/src/streaming/tests.rs:432-458 function test_clear_subscriptions
  Similarity: 82.87%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_output_formatter (lines 95-126) ---[0m
    fn test_output_formatter() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        let entry = OutputEntry::new(
            "agent-123-very-long-id".to_string(),
            "Frontend".to_string(),
            OutputType::Error,
            "Error: Failed to compile".to_string(),
            Some("task-456".to_string()),
            "session-789".to_string(),
        );

        let formatted = formatter.format_entry(&entry);

        // Check prefix formatting
        assert!(formatted.display_prefix.contains("❌"));
        assert!(formatted.display_prefix.contains("Frontend"));
        assert!(formatted.display_prefix.contains("agent-12")); // Truncated
        assert!(formatted.display_prefix.contains("[task-456]"));

        // Check content formatting
        assert_eq!(formatted.formatted_content, "Error: Failed to compile");

        // Check highlighting
        assert!(!formatted.highlight_spans.is_empty());
        let error_highlight = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Error));
        assert!(error_highlight.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_clear_subscriptions (lines 432-458) ---[0m
    async fn test_clear_subscriptions() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create multiple subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();
        let _rx3 = manager.subscribe("sub-3".to_string()).unwrap();

        // Wait for stats updater to run
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 3);

        // Clear all
        manager.clear_subscriptions().unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 0);
    }

  ./crates/ccswarm/src/streaming/tests.rs:22-41 function test_stream_subscription <-> ./crates/ccswarm/src/streaming/tests.rs:251-287 function test_streaming_manager
  Similarity: 77.75%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_stream_subscription (lines 22-41) ---[0m
    fn test_stream_subscription() {
        let (tx, _rx) = mpsc::unbounded_channel();
        let sub = StreamSubscription::new("test-sub".to_string(), tx);

        assert_eq!(sub.id, "test-sub");
        assert!(sub.agent_id.is_none());
        assert!(sub.filter.is_none());

        // Test with agent
        let sub = sub.with_agent("agent-123".to_string());
        assert_eq!(sub.agent_id, Some("agent-123".to_string()));

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let sub = sub.with_filter(filter);
        assert!(sub.filter.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_manager (lines 251-287) ---[0m
    async fn test_streaming_manager() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        // Subscribe to stream first
        let mut rx = manager.subscribe("test-sub".to_string()).unwrap();

        // Start the manager
        manager.start().await.unwrap();

        // Give the streaming loop time to start
        sleep(Duration::from_millis(10)).await;

        // Add output to monitoring system
        monitoring
            .add_output(
                "agent-123".to_string(),
                "Test".to_string(),
                OutputType::Info,
                "Test message".to_string(),
                None,
                "session-1".to_string(),
            )
            .unwrap();

        // Wait for message with longer timeout
        let timeout = tokio::time::timeout(Duration::from_millis(100), rx.recv()).await;

        assert!(timeout.is_ok());
        let entry = timeout.unwrap();
        assert!(entry.is_some());
        assert_eq!(entry.unwrap().content, "Test message");

        // Unsubscribe
        manager.unsubscribe("test-sub").unwrap();
    }

  ./crates/ccswarm/src/streaming/tests.rs:22-41 function test_stream_subscription <-> ./crates/ccswarm/src/streaming/tests.rs:95-126 function test_output_formatter
  Similarity: 83.04%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_stream_subscription (lines 22-41) ---[0m
    fn test_stream_subscription() {
        let (tx, _rx) = mpsc::unbounded_channel();
        let sub = StreamSubscription::new("test-sub".to_string(), tx);

        assert_eq!(sub.id, "test-sub");
        assert!(sub.agent_id.is_none());
        assert!(sub.filter.is_none());

        // Test with agent
        let sub = sub.with_agent("agent-123".to_string());
        assert_eq!(sub.agent_id, Some("agent-123".to_string()));

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let sub = sub.with_filter(filter);
        assert!(sub.filter.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_output_formatter (lines 95-126) ---[0m
    fn test_output_formatter() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        let entry = OutputEntry::new(
            "agent-123-very-long-id".to_string(),
            "Frontend".to_string(),
            OutputType::Error,
            "Error: Failed to compile".to_string(),
            Some("task-456".to_string()),
            "session-789".to_string(),
        );

        let formatted = formatter.format_entry(&entry);

        // Check prefix formatting
        assert!(formatted.display_prefix.contains("❌"));
        assert!(formatted.display_prefix.contains("Frontend"));
        assert!(formatted.display_prefix.contains("agent-12")); // Truncated
        assert!(formatted.display_prefix.contains("[task-456]"));

        // Check content formatting
        assert_eq!(formatted.formatted_content, "Error: Failed to compile");

        // Check highlighting
        assert!(!formatted.highlight_spans.is_empty());
        let error_highlight = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Error));
        assert!(error_highlight.is_some());
    }

  ./crates/ccswarm/src/streaming/tests.rs:22-41 function test_stream_subscription <-> ./crates/ccswarm/src/streaming/tests.rs:432-458 function test_clear_subscriptions
  Similarity: 72.87%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_stream_subscription (lines 22-41) ---[0m
    fn test_stream_subscription() {
        let (tx, _rx) = mpsc::unbounded_channel();
        let sub = StreamSubscription::new("test-sub".to_string(), tx);

        assert_eq!(sub.id, "test-sub");
        assert!(sub.agent_id.is_none());
        assert!(sub.filter.is_none());

        // Test with agent
        let sub = sub.with_agent("agent-123".to_string());
        assert_eq!(sub.agent_id, Some("agent-123".to_string()));

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let sub = sub.with_filter(filter);
        assert!(sub.filter.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_clear_subscriptions (lines 432-458) ---[0m
    async fn test_clear_subscriptions() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create multiple subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();
        let _rx3 = manager.subscribe("sub-3".to_string()).unwrap();

        // Wait for stats updater to run
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 3);

        // Clear all
        manager.clear_subscriptions().unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 0);
    }

  ./crates/ccswarm/src/streaming/tests.rs:22-41 function test_stream_subscription <-> ./crates/ccswarm/src/streaming/tests.rs:402-428 function test_streaming_stats
  Similarity: 72.75%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_stream_subscription (lines 22-41) ---[0m
    fn test_stream_subscription() {
        let (tx, _rx) = mpsc::unbounded_channel();
        let sub = StreamSubscription::new("test-sub".to_string(), tx);

        assert_eq!(sub.id, "test-sub");
        assert!(sub.agent_id.is_none());
        assert!(sub.filter.is_none());

        // Test with agent
        let sub = sub.with_agent("agent-123".to_string());
        assert_eq!(sub.agent_id, Some("agent-123".to_string()));

        // Test with filter
        let filter = OutputFilter {
            output_types: Some(vec![OutputType::Error]),
            ..Default::default()
        };
        let sub = sub.with_filter(filter);
        assert!(sub.filter.is_some());
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_streaming_stats (lines 402-428) ---[0m
    async fn test_streaming_stats() {
        let monitoring = Arc::new(MonitoringSystem::new());
        let config = StreamConfig::default();
        let manager = StreamingManager::new(monitoring.clone(), config);

        manager.start().await.unwrap();

        // Create subscriptions
        let _rx1 = manager.subscribe("sub-1".to_string()).unwrap();
        let _rx2 = manager.subscribe("sub-2".to_string()).unwrap();

        // Wait for stats updater to run (runs every second)
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 2);
        assert_eq!(stats.subscription_details.len(), 2);

        // Unsubscribe one
        manager.unsubscribe("sub-1").unwrap();

        // Wait for stats updater to run again
        sleep(Duration::from_millis(1100)).await;

        let stats = manager.get_stats().unwrap();
        assert_eq!(stats.active_subscriptions, 1);
    }

  ./crates/ccswarm/src/streaming/tests.rs:130-147 function test_content_truncation <-> ./crates/ccswarm/src/streaming/tests.rs:228-247 function test_json_highlighting
  Similarity: 86.55%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_content_truncation (lines 130-147) ---[0m
    fn test_content_truncation() {
        let mut config = StreamConfig::default();
        config.max_line_length = 20;
        let formatter = OutputFormatter::new(config);

        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "This is a very long message that should be truncated".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "This is a very lo...");
        assert_eq!(formatted.formatted_content.len(), 20);
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_json_highlighting (lines 228-247) ---[0m
    fn test_json_highlighting() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            r#"{"status": "ok", "count": 42}"#.to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let json_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Json));
        assert!(json_span.is_some());
    }

  ./crates/ccswarm/src/streaming/tests.rs:151-166 function test_multiline_formatting <-> ./crates/ccswarm/src/streaming/tests.rs:228-247 function test_json_highlighting
  Similarity: 87.09%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_multiline_formatting (lines 151-166) ---[0m
    fn test_multiline_formatting() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Line 1\nLine 2\nLine 3".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "  Line 1\n  Line 2\n  Line 3");
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_json_highlighting (lines 228-247) ---[0m
    fn test_json_highlighting() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            r#"{"status": "ok", "count": 42}"#.to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        let json_span = formatted
            .highlight_spans
            .iter()
            .find(|span| matches!(span.style, HighlightStyle::Json));
        assert!(json_span.is_some());
    }

  ./crates/ccswarm/src/streaming/tests.rs:130-147 function test_content_truncation <-> ./crates/ccswarm/src/streaming/tests.rs:151-166 function test_multiline_formatting
  Similarity: 77.56%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_content_truncation (lines 130-147) ---[0m
    fn test_content_truncation() {
        let mut config = StreamConfig::default();
        config.max_line_length = 20;
        let formatter = OutputFormatter::new(config);

        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "This is a very long message that should be truncated".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "This is a very lo...");
        assert_eq!(formatted.formatted_content.len(), 20);
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_multiline_formatting (lines 151-166) ---[0m
    fn test_multiline_formatting() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Line 1\nLine 2\nLine 3".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "  Line 1\n  Line 2\n  Line 3");
    }

  ./crates/ccswarm/src/streaming/tests.rs:11-18 function test_stream_config_default <-> ./crates/ccswarm/src/streaming/tests.rs:151-166 function test_multiline_formatting
  Similarity: 79.04%

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_stream_config_default (lines 11-18) ---[0m
    fn test_stream_config_default() {
        let config = StreamConfig::default();
        assert_eq!(config.buffer_size, 1000);
        assert_eq!(config.max_line_length, 2000);
        assert!(config.enable_filtering);
        assert!(config.enable_highlighting);
        assert_eq!(config.refresh_rate_ms, 100);
    }

[36m--- ./crates/ccswarm/src/streaming/tests.rs:test_multiline_formatting (lines 151-166) ---[0m
    fn test_multiline_formatting() {
        let config = StreamConfig::default();
        let formatter = OutputFormatter::new(config);

        let entry = OutputEntry::new(
            "agent-123".to_string(),
            "Test".to_string(),
            OutputType::Info,
            "Line 1\nLine 2\nLine 3".to_string(),
            None,
            "session-1".to_string(),
        );

        let formatted = formatter.format_entry(&entry);
        assert_eq!(formatted.formatted_content, "  Line 1\n  Line 2\n  Line 3");
    }


Duplicates in ./crates/ccswarm/src/tui/tests.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/tui/tests.rs:28-54 function test_tab_navigation <-> ./crates/ccswarm/src/tui/tests.rs:58-94 function test_item_selection
  Similarity: 84.44%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_tab_navigation (lines 28-54) ---[0m
    async fn test_tab_navigation() {
        let mut app = App::new().await.unwrap();

        // Test next tab
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Tasks);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Logs);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Delegation);

        // Wrap around
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Overview);

        // Test previous tab
        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Delegation);

        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Logs);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_item_selection (lines 58-94) ---[0m
    async fn test_item_selection() {
        let mut app = App::new().await.unwrap();

        // Add some test data for agents
        app.agents = vec![
            create_test_agent_info("agent-1"),
            create_test_agent_info("agent-2"),
            create_test_agent_info("agent-3"),
        ];

        app.current_tab = Tab::Agents;

        // Initially at index 0
        assert_eq!(app.selected_agent, 0);

        // Navigate down
        app.next_item();
        assert_eq!(app.selected_agent, 1);

        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Should not go beyond last item
        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Navigate up
        app.previous_item();
        assert_eq!(app.selected_agent, 1);

        app.previous_item();
        assert_eq!(app.selected_agent, 0);

        // Should not go below 0
        app.previous_item();
        assert_eq!(app.selected_agent, 0);
    }

  ./crates/ccswarm/src/tui/tests.rs:58-94 function test_item_selection <-> ./crates/ccswarm/src/tui/tests.rs:175-203 function test_ui_rendering
  Similarity: 79.00%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_item_selection (lines 58-94) ---[0m
    async fn test_item_selection() {
        let mut app = App::new().await.unwrap();

        // Add some test data for agents
        app.agents = vec![
            create_test_agent_info("agent-1"),
            create_test_agent_info("agent-2"),
            create_test_agent_info("agent-3"),
        ];

        app.current_tab = Tab::Agents;

        // Initially at index 0
        assert_eq!(app.selected_agent, 0);

        // Navigate down
        app.next_item();
        assert_eq!(app.selected_agent, 1);

        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Should not go beyond last item
        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Navigate up
        app.previous_item();
        assert_eq!(app.selected_agent, 1);

        app.previous_item();
        assert_eq!(app.selected_agent, 0);

        // Should not go below 0
        app.previous_item();
        assert_eq!(app.selected_agent, 0);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_ui_rendering (lines 175-203) ---[0m
    async fn test_ui_rendering() {
        let backend = TestBackend::new(80, 30);
        let mut terminal = Terminal::new(backend).unwrap();

        let mut app = App::new().await.unwrap();
        app.system_status = "Running".to_string();
        app.agents = vec![create_test_agent_info("test-agent")];
        app.logs = vec![LogEntry {
            timestamp: Utc::now(),
            level: "INFO".to_string(),
            agent: Some("test-agent".to_string()),
            message: "Test log entry".to_string(),
        }];

        // Draw the UI
        terminal.draw(|f| ui::draw(f, &app)).unwrap();

        // Get the buffer
        let buffer = terminal.backend().buffer();

        // Verify some content is rendered
        let content = buffer_to_string(buffer);
        assert!(content.contains("ccswarm"));
        assert!(content.contains("Overview"));
        assert!(content.contains("Agents"));
        assert!(content.contains("Tasks"));
        assert!(content.contains("Logs"));
        assert!(content.contains("Delegation"));
    }

  ./crates/ccswarm/src/tui/tests.rs:58-94 function test_item_selection <-> ./crates/ccswarm/src/tui/tests.rs:98-126 function test_input_mode
  Similarity: 77.92%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_item_selection (lines 58-94) ---[0m
    async fn test_item_selection() {
        let mut app = App::new().await.unwrap();

        // Add some test data for agents
        app.agents = vec![
            create_test_agent_info("agent-1"),
            create_test_agent_info("agent-2"),
            create_test_agent_info("agent-3"),
        ];

        app.current_tab = Tab::Agents;

        // Initially at index 0
        assert_eq!(app.selected_agent, 0);

        // Navigate down
        app.next_item();
        assert_eq!(app.selected_agent, 1);

        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Should not go beyond last item
        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Navigate up
        app.previous_item();
        assert_eq!(app.selected_agent, 1);

        app.previous_item();
        assert_eq!(app.selected_agent, 0);

        // Should not go below 0
        app.previous_item();
        assert_eq!(app.selected_agent, 0);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_input_mode (lines 98-126) ---[0m
    async fn test_input_mode() {
        let mut app = App::new().await.unwrap();

        // Start in normal mode
        assert_eq!(app.input_mode, InputMode::Normal);
        assert!(app.input_buffer.is_empty());

        // Switch to command input mode
        app.input_mode = InputMode::Command;
        assert_eq!(app.input_mode, InputMode::Command);
        assert_eq!(app.input_buffer, "");

        // Add character input
        app.input_buffer.push('t');
        app.input_buffer.push('e');
        app.input_buffer.push('s');
        app.input_buffer.push('t');
        assert_eq!(app.input_buffer, "test");

        // Handle backspace
        app.input_buffer.pop();
        assert_eq!(app.input_buffer, "tes");

        // Clear input
        app.input_buffer.clear();
        app.input_mode = InputMode::Normal;
        assert_eq!(app.input_mode, InputMode::Normal);
        assert!(app.input_buffer.is_empty());
    }

  ./crates/ccswarm/src/tui/tests.rs:58-94 function test_item_selection <-> ./crates/ccswarm/src/tui/tests.rs:303-323 function test_empty_state_navigation
  Similarity: 81.41%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_item_selection (lines 58-94) ---[0m
    async fn test_item_selection() {
        let mut app = App::new().await.unwrap();

        // Add some test data for agents
        app.agents = vec![
            create_test_agent_info("agent-1"),
            create_test_agent_info("agent-2"),
            create_test_agent_info("agent-3"),
        ];

        app.current_tab = Tab::Agents;

        // Initially at index 0
        assert_eq!(app.selected_agent, 0);

        // Navigate down
        app.next_item();
        assert_eq!(app.selected_agent, 1);

        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Should not go beyond last item
        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Navigate up
        app.previous_item();
        assert_eq!(app.selected_agent, 1);

        app.previous_item();
        assert_eq!(app.selected_agent, 0);

        // Should not go below 0
        app.previous_item();
        assert_eq!(app.selected_agent, 0);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_empty_state_navigation (lines 303-323) ---[0m
    async fn test_empty_state_navigation() {
        let mut app = App::new().await.unwrap();

        // With empty lists, navigation should not panic
        app.current_tab = Tab::Agents;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_agent, 0);

        app.current_tab = Tab::Tasks;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_task, 0);

        app.current_tab = Tab::Logs;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_log, 0);

        // Should complete without panicking
    }

  ./crates/ccswarm/src/tui/tests.rs:58-94 function test_item_selection <-> ./crates/ccswarm/src/tui/tests.rs:388-409 function test_key_sequences
  Similarity: 75.77%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_item_selection (lines 58-94) ---[0m
    async fn test_item_selection() {
        let mut app = App::new().await.unwrap();

        // Add some test data for agents
        app.agents = vec![
            create_test_agent_info("agent-1"),
            create_test_agent_info("agent-2"),
            create_test_agent_info("agent-3"),
        ];

        app.current_tab = Tab::Agents;

        // Initially at index 0
        assert_eq!(app.selected_agent, 0);

        // Navigate down
        app.next_item();
        assert_eq!(app.selected_agent, 1);

        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Should not go beyond last item
        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Navigate up
        app.previous_item();
        assert_eq!(app.selected_agent, 1);

        app.previous_item();
        assert_eq!(app.selected_agent, 0);

        // Should not go below 0
        app.previous_item();
        assert_eq!(app.selected_agent, 0);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_key_sequences (lines 388-409) ---[0m
    async fn test_key_sequences() {
        let mut app = App::new().await.unwrap();

        // Start at Overview
        assert_eq!(app.current_tab, Tab::Overview);

        // Tab to Agents
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        // Tab to Tasks
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Tasks);

        // BackTab to Agents
        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        // Set input mode
        app.input_mode = InputMode::Command;
        assert_eq!(app.input_mode, InputMode::Command);
    }

  ./crates/ccswarm/src/tui/tests.rs:58-94 function test_item_selection <-> ./crates/ccswarm/src/tui/tests.rs:151-171 function test_system_status
  Similarity: 76.64%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_item_selection (lines 58-94) ---[0m
    async fn test_item_selection() {
        let mut app = App::new().await.unwrap();

        // Add some test data for agents
        app.agents = vec![
            create_test_agent_info("agent-1"),
            create_test_agent_info("agent-2"),
            create_test_agent_info("agent-3"),
        ];

        app.current_tab = Tab::Agents;

        // Initially at index 0
        assert_eq!(app.selected_agent, 0);

        // Navigate down
        app.next_item();
        assert_eq!(app.selected_agent, 1);

        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Should not go beyond last item
        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Navigate up
        app.previous_item();
        assert_eq!(app.selected_agent, 1);

        app.previous_item();
        assert_eq!(app.selected_agent, 0);

        // Should not go below 0
        app.previous_item();
        assert_eq!(app.selected_agent, 0);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_system_status (lines 151-171) ---[0m
    async fn test_system_status() {
        let mut app = App::new().await.unwrap();

        // Initial state
        assert_eq!(app.system_status, "Starting...");

        // Update status
        app.system_status = "Running".to_string();
        assert_eq!(app.system_status, "Running");

        // Update metrics
        app.total_agents = 4;
        app.active_agents = 3;
        app.pending_tasks = 5;
        app.completed_tasks = 10;

        assert_eq!(app.total_agents, 4);
        assert_eq!(app.active_agents, 3);
        assert_eq!(app.pending_tasks, 5);
        assert_eq!(app.completed_tasks, 10);
    }

  ./crates/ccswarm/src/tui/tests.rs:28-54 function test_tab_navigation <-> ./crates/ccswarm/src/tui/tests.rs:175-203 function test_ui_rendering
  Similarity: 78.06%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_tab_navigation (lines 28-54) ---[0m
    async fn test_tab_navigation() {
        let mut app = App::new().await.unwrap();

        // Test next tab
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Tasks);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Logs);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Delegation);

        // Wrap around
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Overview);

        // Test previous tab
        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Delegation);

        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Logs);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_ui_rendering (lines 175-203) ---[0m
    async fn test_ui_rendering() {
        let backend = TestBackend::new(80, 30);
        let mut terminal = Terminal::new(backend).unwrap();

        let mut app = App::new().await.unwrap();
        app.system_status = "Running".to_string();
        app.agents = vec![create_test_agent_info("test-agent")];
        app.logs = vec![LogEntry {
            timestamp: Utc::now(),
            level: "INFO".to_string(),
            agent: Some("test-agent".to_string()),
            message: "Test log entry".to_string(),
        }];

        // Draw the UI
        terminal.draw(|f| ui::draw(f, &app)).unwrap();

        // Get the buffer
        let buffer = terminal.backend().buffer();

        // Verify some content is rendered
        let content = buffer_to_string(buffer);
        assert!(content.contains("ccswarm"));
        assert!(content.contains("Overview"));
        assert!(content.contains("Agents"));
        assert!(content.contains("Tasks"));
        assert!(content.contains("Logs"));
        assert!(content.contains("Delegation"));
    }

  ./crates/ccswarm/src/tui/tests.rs:98-126 function test_input_mode <-> ./crates/ccswarm/src/tui/tests.rs:175-203 function test_ui_rendering
  Similarity: 74.76%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_input_mode (lines 98-126) ---[0m
    async fn test_input_mode() {
        let mut app = App::new().await.unwrap();

        // Start in normal mode
        assert_eq!(app.input_mode, InputMode::Normal);
        assert!(app.input_buffer.is_empty());

        // Switch to command input mode
        app.input_mode = InputMode::Command;
        assert_eq!(app.input_mode, InputMode::Command);
        assert_eq!(app.input_buffer, "");

        // Add character input
        app.input_buffer.push('t');
        app.input_buffer.push('e');
        app.input_buffer.push('s');
        app.input_buffer.push('t');
        assert_eq!(app.input_buffer, "test");

        // Handle backspace
        app.input_buffer.pop();
        assert_eq!(app.input_buffer, "tes");

        // Clear input
        app.input_buffer.clear();
        app.input_mode = InputMode::Normal;
        assert_eq!(app.input_mode, InputMode::Normal);
        assert!(app.input_buffer.is_empty());
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_ui_rendering (lines 175-203) ---[0m
    async fn test_ui_rendering() {
        let backend = TestBackend::new(80, 30);
        let mut terminal = Terminal::new(backend).unwrap();

        let mut app = App::new().await.unwrap();
        app.system_status = "Running".to_string();
        app.agents = vec![create_test_agent_info("test-agent")];
        app.logs = vec![LogEntry {
            timestamp: Utc::now(),
            level: "INFO".to_string(),
            agent: Some("test-agent".to_string()),
            message: "Test log entry".to_string(),
        }];

        // Draw the UI
        terminal.draw(|f| ui::draw(f, &app)).unwrap();

        // Get the buffer
        let buffer = terminal.backend().buffer();

        // Verify some content is rendered
        let content = buffer_to_string(buffer);
        assert!(content.contains("ccswarm"));
        assert!(content.contains("Overview"));
        assert!(content.contains("Agents"));
        assert!(content.contains("Tasks"));
        assert!(content.contains("Logs"));
        assert!(content.contains("Delegation"));
    }

  ./crates/ccswarm/src/tui/tests.rs:58-94 function test_item_selection <-> ./crates/ccswarm/src/tui/tests.rs:221-239 function test_resize_handling
  Similarity: 76.35%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_item_selection (lines 58-94) ---[0m
    async fn test_item_selection() {
        let mut app = App::new().await.unwrap();

        // Add some test data for agents
        app.agents = vec![
            create_test_agent_info("agent-1"),
            create_test_agent_info("agent-2"),
            create_test_agent_info("agent-3"),
        ];

        app.current_tab = Tab::Agents;

        // Initially at index 0
        assert_eq!(app.selected_agent, 0);

        // Navigate down
        app.next_item();
        assert_eq!(app.selected_agent, 1);

        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Should not go beyond last item
        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Navigate up
        app.previous_item();
        assert_eq!(app.selected_agent, 1);

        app.previous_item();
        assert_eq!(app.selected_agent, 0);

        // Should not go below 0
        app.previous_item();
        assert_eq!(app.selected_agent, 0);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_resize_handling (lines 221-239) ---[0m
    async fn test_resize_handling() {
        let mut app = App::new().await.unwrap();

        // Initial size
        assert_eq!(app.terminal_width, 80);
        assert_eq!(app.terminal_height, 24);

        // Update size
        app.terminal_width = 120;
        app.terminal_height = 40;
        assert_eq!(app.terminal_width, 120);
        assert_eq!(app.terminal_height, 40);

        // Update again
        app.terminal_width = 80;
        app.terminal_height = 24;
        assert_eq!(app.terminal_width, 80);
        assert_eq!(app.terminal_height, 24);
    }

  ./crates/ccswarm/src/tui/tests.rs:175-203 function test_ui_rendering <-> ./crates/ccswarm/src/tui/tests.rs:275-299 function test_tab_selection_indices
  Similarity: 78.18%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_ui_rendering (lines 175-203) ---[0m
    async fn test_ui_rendering() {
        let backend = TestBackend::new(80, 30);
        let mut terminal = Terminal::new(backend).unwrap();

        let mut app = App::new().await.unwrap();
        app.system_status = "Running".to_string();
        app.agents = vec![create_test_agent_info("test-agent")];
        app.logs = vec![LogEntry {
            timestamp: Utc::now(),
            level: "INFO".to_string(),
            agent: Some("test-agent".to_string()),
            message: "Test log entry".to_string(),
        }];

        // Draw the UI
        terminal.draw(|f| ui::draw(f, &app)).unwrap();

        // Get the buffer
        let buffer = terminal.backend().buffer();

        // Verify some content is rendered
        let content = buffer_to_string(buffer);
        assert!(content.contains("ccswarm"));
        assert!(content.contains("Overview"));
        assert!(content.contains("Agents"));
        assert!(content.contains("Tasks"));
        assert!(content.contains("Logs"));
        assert!(content.contains("Delegation"));
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_tab_selection_indices (lines 275-299) ---[0m
    async fn test_tab_selection_indices() {
        let mut app = App::new().await.unwrap();

        // Add test data for different tabs
        app.agents = vec![
            create_test_agent_info("a1"),
            create_test_agent_info("a2"),
            create_test_agent_info("a3"),
        ];
        app.tasks = vec![create_test_task_info("t1")];

        // Test independent selection per tab
        app.current_tab = Tab::Agents;
        app.next_item();
        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Switch to tasks
        app.current_tab = Tab::Tasks;
        assert_eq!(app.selected_task, 0);

        // Switch back to agents - selection should be reset after tab change
        app.current_tab = Tab::Agents;
        // After reset_selection() is called in next_tab/previous_tab
    }

  ./crates/ccswarm/src/tui/tests.rs:28-54 function test_tab_navigation <-> ./crates/ccswarm/src/tui/tests.rs:98-126 function test_input_mode
  Similarity: 74.08%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_tab_navigation (lines 28-54) ---[0m
    async fn test_tab_navigation() {
        let mut app = App::new().await.unwrap();

        // Test next tab
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Tasks);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Logs);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Delegation);

        // Wrap around
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Overview);

        // Test previous tab
        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Delegation);

        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Logs);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_input_mode (lines 98-126) ---[0m
    async fn test_input_mode() {
        let mut app = App::new().await.unwrap();

        // Start in normal mode
        assert_eq!(app.input_mode, InputMode::Normal);
        assert!(app.input_buffer.is_empty());

        // Switch to command input mode
        app.input_mode = InputMode::Command;
        assert_eq!(app.input_mode, InputMode::Command);
        assert_eq!(app.input_buffer, "");

        // Add character input
        app.input_buffer.push('t');
        app.input_buffer.push('e');
        app.input_buffer.push('s');
        app.input_buffer.push('t');
        assert_eq!(app.input_buffer, "test");

        // Handle backspace
        app.input_buffer.pop();
        assert_eq!(app.input_buffer, "tes");

        // Clear input
        app.input_buffer.clear();
        app.input_mode = InputMode::Normal;
        assert_eq!(app.input_mode, InputMode::Normal);
        assert!(app.input_buffer.is_empty());
    }

  ./crates/ccswarm/src/tui/tests.rs:28-54 function test_tab_navigation <-> ./crates/ccswarm/src/tui/tests.rs:388-409 function test_key_sequences
  Similarity: 81.75%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_tab_navigation (lines 28-54) ---[0m
    async fn test_tab_navigation() {
        let mut app = App::new().await.unwrap();

        // Test next tab
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Tasks);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Logs);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Delegation);

        // Wrap around
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Overview);

        // Test previous tab
        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Delegation);

        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Logs);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_key_sequences (lines 388-409) ---[0m
    async fn test_key_sequences() {
        let mut app = App::new().await.unwrap();

        // Start at Overview
        assert_eq!(app.current_tab, Tab::Overview);

        // Tab to Agents
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        // Tab to Tasks
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Tasks);

        // BackTab to Agents
        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        // Set input mode
        app.input_mode = InputMode::Command;
        assert_eq!(app.input_mode, InputMode::Command);
    }

  ./crates/ccswarm/src/tui/tests.rs:98-126 function test_input_mode <-> ./crates/ccswarm/src/tui/tests.rs:303-323 function test_empty_state_navigation
  Similarity: 75.25%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_input_mode (lines 98-126) ---[0m
    async fn test_input_mode() {
        let mut app = App::new().await.unwrap();

        // Start in normal mode
        assert_eq!(app.input_mode, InputMode::Normal);
        assert!(app.input_buffer.is_empty());

        // Switch to command input mode
        app.input_mode = InputMode::Command;
        assert_eq!(app.input_mode, InputMode::Command);
        assert_eq!(app.input_buffer, "");

        // Add character input
        app.input_buffer.push('t');
        app.input_buffer.push('e');
        app.input_buffer.push('s');
        app.input_buffer.push('t');
        assert_eq!(app.input_buffer, "test");

        // Handle backspace
        app.input_buffer.pop();
        assert_eq!(app.input_buffer, "tes");

        // Clear input
        app.input_buffer.clear();
        app.input_mode = InputMode::Normal;
        assert_eq!(app.input_mode, InputMode::Normal);
        assert!(app.input_buffer.is_empty());
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_empty_state_navigation (lines 303-323) ---[0m
    async fn test_empty_state_navigation() {
        let mut app = App::new().await.unwrap();

        // With empty lists, navigation should not panic
        app.current_tab = Tab::Agents;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_agent, 0);

        app.current_tab = Tab::Tasks;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_task, 0);

        app.current_tab = Tab::Logs;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_log, 0);

        // Should complete without panicking
    }

  ./crates/ccswarm/src/tui/tests.rs:28-54 function test_tab_navigation <-> ./crates/ccswarm/src/tui/tests.rs:303-323 function test_empty_state_navigation
  Similarity: 77.57%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_tab_navigation (lines 28-54) ---[0m
    async fn test_tab_navigation() {
        let mut app = App::new().await.unwrap();

        // Test next tab
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Tasks);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Logs);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Delegation);

        // Wrap around
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Overview);

        // Test previous tab
        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Delegation);

        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Logs);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_empty_state_navigation (lines 303-323) ---[0m
    async fn test_empty_state_navigation() {
        let mut app = App::new().await.unwrap();

        // With empty lists, navigation should not panic
        app.current_tab = Tab::Agents;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_agent, 0);

        app.current_tab = Tab::Tasks;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_task, 0);

        app.current_tab = Tab::Logs;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_log, 0);

        // Should complete without panicking
    }

  ./crates/ccswarm/src/tui/tests.rs:151-171 function test_system_status <-> ./crates/ccswarm/src/tui/tests.rs:275-299 function test_tab_selection_indices
  Similarity: 79.89%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_system_status (lines 151-171) ---[0m
    async fn test_system_status() {
        let mut app = App::new().await.unwrap();

        // Initial state
        assert_eq!(app.system_status, "Starting...");

        // Update status
        app.system_status = "Running".to_string();
        assert_eq!(app.system_status, "Running");

        // Update metrics
        app.total_agents = 4;
        app.active_agents = 3;
        app.pending_tasks = 5;
        app.completed_tasks = 10;

        assert_eq!(app.total_agents, 4);
        assert_eq!(app.active_agents, 3);
        assert_eq!(app.pending_tasks, 5);
        assert_eq!(app.completed_tasks, 10);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_tab_selection_indices (lines 275-299) ---[0m
    async fn test_tab_selection_indices() {
        let mut app = App::new().await.unwrap();

        // Add test data for different tabs
        app.agents = vec![
            create_test_agent_info("a1"),
            create_test_agent_info("a2"),
            create_test_agent_info("a3"),
        ];
        app.tasks = vec![create_test_task_info("t1")];

        // Test independent selection per tab
        app.current_tab = Tab::Agents;
        app.next_item();
        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Switch to tasks
        app.current_tab = Tab::Tasks;
        assert_eq!(app.selected_task, 0);

        // Switch back to agents - selection should be reset after tab change
        app.current_tab = Tab::Agents;
        // After reset_selection() is called in next_tab/previous_tab
    }

  ./crates/ccswarm/src/tui/tests.rs:98-126 function test_input_mode <-> ./crates/ccswarm/src/tui/tests.rs:388-409 function test_key_sequences
  Similarity: 71.47%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_input_mode (lines 98-126) ---[0m
    async fn test_input_mode() {
        let mut app = App::new().await.unwrap();

        // Start in normal mode
        assert_eq!(app.input_mode, InputMode::Normal);
        assert!(app.input_buffer.is_empty());

        // Switch to command input mode
        app.input_mode = InputMode::Command;
        assert_eq!(app.input_mode, InputMode::Command);
        assert_eq!(app.input_buffer, "");

        // Add character input
        app.input_buffer.push('t');
        app.input_buffer.push('e');
        app.input_buffer.push('s');
        app.input_buffer.push('t');
        assert_eq!(app.input_buffer, "test");

        // Handle backspace
        app.input_buffer.pop();
        assert_eq!(app.input_buffer, "tes");

        // Clear input
        app.input_buffer.clear();
        app.input_mode = InputMode::Normal;
        assert_eq!(app.input_mode, InputMode::Normal);
        assert!(app.input_buffer.is_empty());
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_key_sequences (lines 388-409) ---[0m
    async fn test_key_sequences() {
        let mut app = App::new().await.unwrap();

        // Start at Overview
        assert_eq!(app.current_tab, Tab::Overview);

        // Tab to Agents
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        // Tab to Tasks
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Tasks);

        // BackTab to Agents
        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        // Set input mode
        app.input_mode = InputMode::Command;
        assert_eq!(app.input_mode, InputMode::Command);
    }

  ./crates/ccswarm/src/tui/tests.rs:221-239 function test_resize_handling <-> ./crates/ccswarm/src/tui/tests.rs:275-299 function test_tab_selection_indices
  Similarity: 81.53%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_resize_handling (lines 221-239) ---[0m
    async fn test_resize_handling() {
        let mut app = App::new().await.unwrap();

        // Initial size
        assert_eq!(app.terminal_width, 80);
        assert_eq!(app.terminal_height, 24);

        // Update size
        app.terminal_width = 120;
        app.terminal_height = 40;
        assert_eq!(app.terminal_width, 120);
        assert_eq!(app.terminal_height, 40);

        // Update again
        app.terminal_width = 80;
        app.terminal_height = 24;
        assert_eq!(app.terminal_width, 80);
        assert_eq!(app.terminal_height, 24);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_tab_selection_indices (lines 275-299) ---[0m
    async fn test_tab_selection_indices() {
        let mut app = App::new().await.unwrap();

        // Add test data for different tabs
        app.agents = vec![
            create_test_agent_info("a1"),
            create_test_agent_info("a2"),
            create_test_agent_info("a3"),
        ];
        app.tasks = vec![create_test_task_info("t1")];

        // Test independent selection per tab
        app.current_tab = Tab::Agents;
        app.next_item();
        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Switch to tasks
        app.current_tab = Tab::Tasks;
        assert_eq!(app.selected_task, 0);

        // Switch back to agents - selection should be reset after tab change
        app.current_tab = Tab::Agents;
        // After reset_selection() is called in next_tab/previous_tab
    }

  ./crates/ccswarm/src/tui/tests.rs:275-299 function test_tab_selection_indices <-> ./crates/ccswarm/src/tui/tests.rs:388-409 function test_key_sequences
  Similarity: 75.51%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_tab_selection_indices (lines 275-299) ---[0m
    async fn test_tab_selection_indices() {
        let mut app = App::new().await.unwrap();

        // Add test data for different tabs
        app.agents = vec![
            create_test_agent_info("a1"),
            create_test_agent_info("a2"),
            create_test_agent_info("a3"),
        ];
        app.tasks = vec![create_test_task_info("t1")];

        // Test independent selection per tab
        app.current_tab = Tab::Agents;
        app.next_item();
        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Switch to tasks
        app.current_tab = Tab::Tasks;
        assert_eq!(app.selected_task, 0);

        // Switch back to agents - selection should be reset after tab change
        app.current_tab = Tab::Agents;
        // After reset_selection() is called in next_tab/previous_tab
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_key_sequences (lines 388-409) ---[0m
    async fn test_key_sequences() {
        let mut app = App::new().await.unwrap();

        // Start at Overview
        assert_eq!(app.current_tab, Tab::Overview);

        // Tab to Agents
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        // Tab to Tasks
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Tasks);

        // BackTab to Agents
        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        // Set input mode
        app.input_mode = InputMode::Command;
        assert_eq!(app.input_mode, InputMode::Command);
    }

  ./crates/ccswarm/src/tui/tests.rs:28-54 function test_tab_navigation <-> ./crates/ccswarm/src/tui/tests.rs:151-171 function test_system_status
  Similarity: 73.79%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_tab_navigation (lines 28-54) ---[0m
    async fn test_tab_navigation() {
        let mut app = App::new().await.unwrap();

        // Test next tab
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Tasks);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Logs);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Delegation);

        // Wrap around
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Overview);

        // Test previous tab
        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Delegation);

        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Logs);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_system_status (lines 151-171) ---[0m
    async fn test_system_status() {
        let mut app = App::new().await.unwrap();

        // Initial state
        assert_eq!(app.system_status, "Starting...");

        // Update status
        app.system_status = "Running".to_string();
        assert_eq!(app.system_status, "Running");

        // Update metrics
        app.total_agents = 4;
        app.active_agents = 3;
        app.pending_tasks = 5;
        app.completed_tasks = 10;

        assert_eq!(app.total_agents, 4);
        assert_eq!(app.active_agents, 3);
        assert_eq!(app.pending_tasks, 5);
        assert_eq!(app.completed_tasks, 10);
    }

  ./crates/ccswarm/src/tui/tests.rs:151-171 function test_system_status <-> ./crates/ccswarm/src/tui/tests.rs:388-409 function test_key_sequences
  Similarity: 80.53%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_system_status (lines 151-171) ---[0m
    async fn test_system_status() {
        let mut app = App::new().await.unwrap();

        // Initial state
        assert_eq!(app.system_status, "Starting...");

        // Update status
        app.system_status = "Running".to_string();
        assert_eq!(app.system_status, "Running");

        // Update metrics
        app.total_agents = 4;
        app.active_agents = 3;
        app.pending_tasks = 5;
        app.completed_tasks = 10;

        assert_eq!(app.total_agents, 4);
        assert_eq!(app.active_agents, 3);
        assert_eq!(app.pending_tasks, 5);
        assert_eq!(app.completed_tasks, 10);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_key_sequences (lines 388-409) ---[0m
    async fn test_key_sequences() {
        let mut app = App::new().await.unwrap();

        // Start at Overview
        assert_eq!(app.current_tab, Tab::Overview);

        // Tab to Agents
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        // Tab to Tasks
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Tasks);

        // BackTab to Agents
        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        // Set input mode
        app.input_mode = InputMode::Command;
        assert_eq!(app.input_mode, InputMode::Command);
    }

  ./crates/ccswarm/src/tui/tests.rs:151-171 function test_system_status <-> ./crates/ccswarm/src/tui/tests.rs:221-239 function test_resize_handling
  Similarity: 85.82%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_system_status (lines 151-171) ---[0m
    async fn test_system_status() {
        let mut app = App::new().await.unwrap();

        // Initial state
        assert_eq!(app.system_status, "Starting...");

        // Update status
        app.system_status = "Running".to_string();
        assert_eq!(app.system_status, "Running");

        // Update metrics
        app.total_agents = 4;
        app.active_agents = 3;
        app.pending_tasks = 5;
        app.completed_tasks = 10;

        assert_eq!(app.total_agents, 4);
        assert_eq!(app.active_agents, 3);
        assert_eq!(app.pending_tasks, 5);
        assert_eq!(app.completed_tasks, 10);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_resize_handling (lines 221-239) ---[0m
    async fn test_resize_handling() {
        let mut app = App::new().await.unwrap();

        // Initial size
        assert_eq!(app.terminal_width, 80);
        assert_eq!(app.terminal_height, 24);

        // Update size
        app.terminal_width = 120;
        app.terminal_height = 40;
        assert_eq!(app.terminal_width, 120);
        assert_eq!(app.terminal_height, 40);

        // Update again
        app.terminal_width = 80;
        app.terminal_height = 24;
        assert_eq!(app.terminal_width, 80);
        assert_eq!(app.terminal_height, 24);
    }

  ./crates/ccswarm/src/tui/tests.rs:28-54 function test_tab_navigation <-> ./crates/ccswarm/src/tui/tests.rs:221-239 function test_resize_handling
  Similarity: 74.08%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_tab_navigation (lines 28-54) ---[0m
    async fn test_tab_navigation() {
        let mut app = App::new().await.unwrap();

        // Test next tab
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Tasks);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Logs);

        app.next_tab();
        assert_eq!(app.current_tab, Tab::Delegation);

        // Wrap around
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Overview);

        // Test previous tab
        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Delegation);

        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Logs);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_resize_handling (lines 221-239) ---[0m
    async fn test_resize_handling() {
        let mut app = App::new().await.unwrap();

        // Initial size
        assert_eq!(app.terminal_width, 80);
        assert_eq!(app.terminal_height, 24);

        // Update size
        app.terminal_width = 120;
        app.terminal_height = 40;
        assert_eq!(app.terminal_width, 120);
        assert_eq!(app.terminal_height, 40);

        // Update again
        app.terminal_width = 80;
        app.terminal_height = 24;
        assert_eq!(app.terminal_width, 80);
        assert_eq!(app.terminal_height, 24);
    }

  ./crates/ccswarm/src/tui/tests.rs:275-299 function test_tab_selection_indices <-> ./crates/ccswarm/src/tui/tests.rs:303-323 function test_empty_state_navigation
  Similarity: 73.47%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_tab_selection_indices (lines 275-299) ---[0m
    async fn test_tab_selection_indices() {
        let mut app = App::new().await.unwrap();

        // Add test data for different tabs
        app.agents = vec![
            create_test_agent_info("a1"),
            create_test_agent_info("a2"),
            create_test_agent_info("a3"),
        ];
        app.tasks = vec![create_test_task_info("t1")];

        // Test independent selection per tab
        app.current_tab = Tab::Agents;
        app.next_item();
        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Switch to tasks
        app.current_tab = Tab::Tasks;
        assert_eq!(app.selected_task, 0);

        // Switch back to agents - selection should be reset after tab change
        app.current_tab = Tab::Agents;
        // After reset_selection() is called in next_tab/previous_tab
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_empty_state_navigation (lines 303-323) ---[0m
    async fn test_empty_state_navigation() {
        let mut app = App::new().await.unwrap();

        // With empty lists, navigation should not panic
        app.current_tab = Tab::Agents;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_agent, 0);

        app.current_tab = Tab::Tasks;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_task, 0);

        app.current_tab = Tab::Logs;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_log, 0);

        // Should complete without panicking
    }

  ./crates/ccswarm/src/tui/tests.rs:130-147 function test_delegation_mode <-> ./crates/ccswarm/src/tui/tests.rs:388-409 function test_key_sequences
  Similarity: 82.17%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_delegation_mode (lines 130-147) ---[0m
    async fn test_delegation_mode() {
        let mut app = App::new().await.unwrap();

        app.current_tab = Tab::Delegation;

        // Initial mode
        assert_eq!(app.delegation_mode, DelegationMode::Analyze);

        // Test different delegation modes exist
        app.delegation_mode = DelegationMode::Delegate;
        assert_eq!(app.delegation_mode, DelegationMode::Delegate);

        app.delegation_mode = DelegationMode::ViewStats;
        assert_eq!(app.delegation_mode, DelegationMode::ViewStats);

        app.delegation_mode = DelegationMode::Analyze;
        assert_eq!(app.delegation_mode, DelegationMode::Analyze);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_key_sequences (lines 388-409) ---[0m
    async fn test_key_sequences() {
        let mut app = App::new().await.unwrap();

        // Start at Overview
        assert_eq!(app.current_tab, Tab::Overview);

        // Tab to Agents
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        // Tab to Tasks
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Tasks);

        // BackTab to Agents
        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        // Set input mode
        app.input_mode = InputMode::Command;
        assert_eq!(app.input_mode, InputMode::Command);
    }

  ./crates/ccswarm/src/tui/tests.rs:130-147 function test_delegation_mode <-> ./crates/ccswarm/src/tui/tests.rs:275-299 function test_tab_selection_indices
  Similarity: 75.23%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_delegation_mode (lines 130-147) ---[0m
    async fn test_delegation_mode() {
        let mut app = App::new().await.unwrap();

        app.current_tab = Tab::Delegation;

        // Initial mode
        assert_eq!(app.delegation_mode, DelegationMode::Analyze);

        // Test different delegation modes exist
        app.delegation_mode = DelegationMode::Delegate;
        assert_eq!(app.delegation_mode, DelegationMode::Delegate);

        app.delegation_mode = DelegationMode::ViewStats;
        assert_eq!(app.delegation_mode, DelegationMode::ViewStats);

        app.delegation_mode = DelegationMode::Analyze;
        assert_eq!(app.delegation_mode, DelegationMode::Analyze);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_tab_selection_indices (lines 275-299) ---[0m
    async fn test_tab_selection_indices() {
        let mut app = App::new().await.unwrap();

        // Add test data for different tabs
        app.agents = vec![
            create_test_agent_info("a1"),
            create_test_agent_info("a2"),
            create_test_agent_info("a3"),
        ];
        app.tasks = vec![create_test_task_info("t1")];

        // Test independent selection per tab
        app.current_tab = Tab::Agents;
        app.next_item();
        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Switch to tasks
        app.current_tab = Tab::Tasks;
        assert_eq!(app.selected_task, 0);

        // Switch back to agents - selection should be reset after tab change
        app.current_tab = Tab::Agents;
        // After reset_selection() is called in next_tab/previous_tab
    }

  ./crates/ccswarm/src/tui/tests.rs:256-271 function test_input_processing <-> ./crates/ccswarm/src/tui/tests.rs:275-299 function test_tab_selection_indices
  Similarity: 78.52%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_input_processing (lines 256-271) ---[0m
    async fn test_input_processing() {
        let mut app = App::new().await.unwrap();

        // Test command input
        app.input_mode = InputMode::Command;
        app.input_buffer = "test command".to_string();

        // Verify input buffer works
        assert_eq!(app.input_buffer, "test command");

        // Clear input
        app.input_buffer.clear();
        app.input_mode = InputMode::Normal;
        assert_eq!(app.input_mode, InputMode::Normal);
        assert!(app.input_buffer.is_empty());
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_tab_selection_indices (lines 275-299) ---[0m
    async fn test_tab_selection_indices() {
        let mut app = App::new().await.unwrap();

        // Add test data for different tabs
        app.agents = vec![
            create_test_agent_info("a1"),
            create_test_agent_info("a2"),
            create_test_agent_info("a3"),
        ];
        app.tasks = vec![create_test_task_info("t1")];

        // Test independent selection per tab
        app.current_tab = Tab::Agents;
        app.next_item();
        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Switch to tasks
        app.current_tab = Tab::Tasks;
        assert_eq!(app.selected_task, 0);

        // Switch back to agents - selection should be reset after tab change
        app.current_tab = Tab::Agents;
        // After reset_selection() is called in next_tab/previous_tab
    }

  ./crates/ccswarm/src/tui/tests.rs:275-299 function test_tab_selection_indices <-> ./crates/ccswarm/src/tui/tests.rs:365-384 function test_ui_component_rendering
  Similarity: 71.34%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_tab_selection_indices (lines 275-299) ---[0m
    async fn test_tab_selection_indices() {
        let mut app = App::new().await.unwrap();

        // Add test data for different tabs
        app.agents = vec![
            create_test_agent_info("a1"),
            create_test_agent_info("a2"),
            create_test_agent_info("a3"),
        ];
        app.tasks = vec![create_test_task_info("t1")];

        // Test independent selection per tab
        app.current_tab = Tab::Agents;
        app.next_item();
        app.next_item();
        assert_eq!(app.selected_agent, 2);

        // Switch to tasks
        app.current_tab = Tab::Tasks;
        assert_eq!(app.selected_task, 0);

        // Switch back to agents - selection should be reset after tab change
        app.current_tab = Tab::Agents;
        // After reset_selection() is called in next_tab/previous_tab
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_ui_component_rendering (lines 365-384) ---[0m
    fn test_ui_component_rendering() {
        use ratatui::widgets::{Block, Borders};

        let backend = TestBackend::new(80, 30);
        let mut terminal = Terminal::new(backend).unwrap();

        // Test rendering a simple block
        terminal
            .draw(|f| {
                let block = Block::default().title("Test Block").borders(Borders::ALL);
                f.render_widget(block, f.area());
            })
            .unwrap();

        let buffer = terminal.backend().buffer();
        let content = buffer_to_string(buffer);

        // Verify block title is rendered
        assert!(content.contains("Test Block"));
    }

  ./crates/ccswarm/src/tui/tests.rs:221-239 function test_resize_handling <-> ./crates/ccswarm/src/tui/tests.rs:388-409 function test_key_sequences
  Similarity: 78.16%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_resize_handling (lines 221-239) ---[0m
    async fn test_resize_handling() {
        let mut app = App::new().await.unwrap();

        // Initial size
        assert_eq!(app.terminal_width, 80);
        assert_eq!(app.terminal_height, 24);

        // Update size
        app.terminal_width = 120;
        app.terminal_height = 40;
        assert_eq!(app.terminal_width, 120);
        assert_eq!(app.terminal_height, 40);

        // Update again
        app.terminal_width = 80;
        app.terminal_height = 24;
        assert_eq!(app.terminal_width, 80);
        assert_eq!(app.terminal_height, 24);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_key_sequences (lines 388-409) ---[0m
    async fn test_key_sequences() {
        let mut app = App::new().await.unwrap();

        // Start at Overview
        assert_eq!(app.current_tab, Tab::Overview);

        // Tab to Agents
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        // Tab to Tasks
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Tasks);

        // BackTab to Agents
        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        // Set input mode
        app.input_mode = InputMode::Command;
        assert_eq!(app.input_mode, InputMode::Command);
    }

  ./crates/ccswarm/src/tui/tests.rs:303-323 function test_empty_state_navigation <-> ./crates/ccswarm/src/tui/tests.rs:388-409 function test_key_sequences
  Similarity: 72.89%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_empty_state_navigation (lines 303-323) ---[0m
    async fn test_empty_state_navigation() {
        let mut app = App::new().await.unwrap();

        // With empty lists, navigation should not panic
        app.current_tab = Tab::Agents;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_agent, 0);

        app.current_tab = Tab::Tasks;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_task, 0);

        app.current_tab = Tab::Logs;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_log, 0);

        // Should complete without panicking
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_key_sequences (lines 388-409) ---[0m
    async fn test_key_sequences() {
        let mut app = App::new().await.unwrap();

        // Start at Overview
        assert_eq!(app.current_tab, Tab::Overview);

        // Tab to Agents
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        // Tab to Tasks
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Tasks);

        // BackTab to Agents
        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        // Set input mode
        app.input_mode = InputMode::Command;
        assert_eq!(app.input_mode, InputMode::Command);
    }

  ./crates/ccswarm/src/tui/tests.rs:151-171 function test_system_status <-> ./crates/ccswarm/src/tui/tests.rs:303-323 function test_empty_state_navigation
  Similarity: 71.35%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_system_status (lines 151-171) ---[0m
    async fn test_system_status() {
        let mut app = App::new().await.unwrap();

        // Initial state
        assert_eq!(app.system_status, "Starting...");

        // Update status
        app.system_status = "Running".to_string();
        assert_eq!(app.system_status, "Running");

        // Update metrics
        app.total_agents = 4;
        app.active_agents = 3;
        app.pending_tasks = 5;
        app.completed_tasks = 10;

        assert_eq!(app.total_agents, 4);
        assert_eq!(app.active_agents, 3);
        assert_eq!(app.pending_tasks, 5);
        assert_eq!(app.completed_tasks, 10);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_empty_state_navigation (lines 303-323) ---[0m
    async fn test_empty_state_navigation() {
        let mut app = App::new().await.unwrap();

        // With empty lists, navigation should not panic
        app.current_tab = Tab::Agents;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_agent, 0);

        app.current_tab = Tab::Tasks;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_task, 0);

        app.current_tab = Tab::Logs;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_log, 0);

        // Should complete without panicking
    }

  ./crates/ccswarm/src/tui/tests.rs:221-239 function test_resize_handling <-> ./crates/ccswarm/src/tui/tests.rs:303-323 function test_empty_state_navigation
  Similarity: 74.40%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_resize_handling (lines 221-239) ---[0m
    async fn test_resize_handling() {
        let mut app = App::new().await.unwrap();

        // Initial size
        assert_eq!(app.terminal_width, 80);
        assert_eq!(app.terminal_height, 24);

        // Update size
        app.terminal_width = 120;
        app.terminal_height = 40;
        assert_eq!(app.terminal_width, 120);
        assert_eq!(app.terminal_height, 40);

        // Update again
        app.terminal_width = 80;
        app.terminal_height = 24;
        assert_eq!(app.terminal_width, 80);
        assert_eq!(app.terminal_height, 24);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_empty_state_navigation (lines 303-323) ---[0m
    async fn test_empty_state_navigation() {
        let mut app = App::new().await.unwrap();

        // With empty lists, navigation should not panic
        app.current_tab = Tab::Agents;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_agent, 0);

        app.current_tab = Tab::Tasks;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_task, 0);

        app.current_tab = Tab::Logs;
        app.next_item();
        app.previous_item();
        assert_eq!(app.selected_log, 0);

        // Should complete without panicking
    }

  ./crates/ccswarm/src/tui/tests.rs:130-147 function test_delegation_mode <-> ./crates/ccswarm/src/tui/tests.rs:365-384 function test_ui_component_rendering
  Similarity: 75.40%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_delegation_mode (lines 130-147) ---[0m
    async fn test_delegation_mode() {
        let mut app = App::new().await.unwrap();

        app.current_tab = Tab::Delegation;

        // Initial mode
        assert_eq!(app.delegation_mode, DelegationMode::Analyze);

        // Test different delegation modes exist
        app.delegation_mode = DelegationMode::Delegate;
        assert_eq!(app.delegation_mode, DelegationMode::Delegate);

        app.delegation_mode = DelegationMode::ViewStats;
        assert_eq!(app.delegation_mode, DelegationMode::ViewStats);

        app.delegation_mode = DelegationMode::Analyze;
        assert_eq!(app.delegation_mode, DelegationMode::Analyze);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_ui_component_rendering (lines 365-384) ---[0m
    fn test_ui_component_rendering() {
        use ratatui::widgets::{Block, Borders};

        let backend = TestBackend::new(80, 30);
        let mut terminal = Terminal::new(backend).unwrap();

        // Test rendering a simple block
        terminal
            .draw(|f| {
                let block = Block::default().title("Test Block").borders(Borders::ALL);
                f.render_widget(block, f.area());
            })
            .unwrap();

        let buffer = terminal.backend().buffer();
        let content = buffer_to_string(buffer);

        // Verify block title is rendered
        assert!(content.contains("Test Block"));
    }

  ./crates/ccswarm/src/tui/tests.rs:130-147 function test_delegation_mode <-> ./crates/ccswarm/src/tui/tests.rs:151-171 function test_system_status
  Similarity: 71.82%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_delegation_mode (lines 130-147) ---[0m
    async fn test_delegation_mode() {
        let mut app = App::new().await.unwrap();

        app.current_tab = Tab::Delegation;

        // Initial mode
        assert_eq!(app.delegation_mode, DelegationMode::Analyze);

        // Test different delegation modes exist
        app.delegation_mode = DelegationMode::Delegate;
        assert_eq!(app.delegation_mode, DelegationMode::Delegate);

        app.delegation_mode = DelegationMode::ViewStats;
        assert_eq!(app.delegation_mode, DelegationMode::ViewStats);

        app.delegation_mode = DelegationMode::Analyze;
        assert_eq!(app.delegation_mode, DelegationMode::Analyze);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_system_status (lines 151-171) ---[0m
    async fn test_system_status() {
        let mut app = App::new().await.unwrap();

        // Initial state
        assert_eq!(app.system_status, "Starting...");

        // Update status
        app.system_status = "Running".to_string();
        assert_eq!(app.system_status, "Running");

        // Update metrics
        app.total_agents = 4;
        app.active_agents = 3;
        app.pending_tasks = 5;
        app.completed_tasks = 10;

        assert_eq!(app.total_agents, 4);
        assert_eq!(app.active_agents, 3);
        assert_eq!(app.pending_tasks, 5);
        assert_eq!(app.completed_tasks, 10);
    }

  ./crates/ccswarm/src/tui/tests.rs:256-271 function test_input_processing <-> ./crates/ccswarm/src/tui/tests.rs:365-384 function test_ui_component_rendering
  Similarity: 77.33%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_input_processing (lines 256-271) ---[0m
    async fn test_input_processing() {
        let mut app = App::new().await.unwrap();

        // Test command input
        app.input_mode = InputMode::Command;
        app.input_buffer = "test command".to_string();

        // Verify input buffer works
        assert_eq!(app.input_buffer, "test command");

        // Clear input
        app.input_buffer.clear();
        app.input_mode = InputMode::Normal;
        assert_eq!(app.input_mode, InputMode::Normal);
        assert!(app.input_buffer.is_empty());
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_ui_component_rendering (lines 365-384) ---[0m
    fn test_ui_component_rendering() {
        use ratatui::widgets::{Block, Borders};

        let backend = TestBackend::new(80, 30);
        let mut terminal = Terminal::new(backend).unwrap();

        // Test rendering a simple block
        terminal
            .draw(|f| {
                let block = Block::default().title("Test Block").borders(Borders::ALL);
                f.render_widget(block, f.area());
            })
            .unwrap();

        let buffer = terminal.backend().buffer();
        let content = buffer_to_string(buffer);

        // Verify block title is rendered
        assert!(content.contains("Test Block"));
    }

  ./crates/ccswarm/src/tui/tests.rs:256-271 function test_input_processing <-> ./crates/ccswarm/src/tui/tests.rs:388-409 function test_key_sequences
  Similarity: 73.09%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_input_processing (lines 256-271) ---[0m
    async fn test_input_processing() {
        let mut app = App::new().await.unwrap();

        // Test command input
        app.input_mode = InputMode::Command;
        app.input_buffer = "test command".to_string();

        // Verify input buffer works
        assert_eq!(app.input_buffer, "test command");

        // Clear input
        app.input_buffer.clear();
        app.input_mode = InputMode::Normal;
        assert_eq!(app.input_mode, InputMode::Normal);
        assert!(app.input_buffer.is_empty());
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_key_sequences (lines 388-409) ---[0m
    async fn test_key_sequences() {
        let mut app = App::new().await.unwrap();

        // Start at Overview
        assert_eq!(app.current_tab, Tab::Overview);

        // Tab to Agents
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        // Tab to Tasks
        app.next_tab();
        assert_eq!(app.current_tab, Tab::Tasks);

        // BackTab to Agents
        app.previous_tab();
        assert_eq!(app.current_tab, Tab::Agents);

        // Set input mode
        app.input_mode = InputMode::Command;
        assert_eq!(app.input_mode, InputMode::Command);
    }

  ./crates/ccswarm/src/tui/tests.rs:221-239 function test_resize_handling <-> ./crates/ccswarm/src/tui/tests.rs:256-271 function test_input_processing
  Similarity: 78.90%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_resize_handling (lines 221-239) ---[0m
    async fn test_resize_handling() {
        let mut app = App::new().await.unwrap();

        // Initial size
        assert_eq!(app.terminal_width, 80);
        assert_eq!(app.terminal_height, 24);

        // Update size
        app.terminal_width = 120;
        app.terminal_height = 40;
        assert_eq!(app.terminal_width, 120);
        assert_eq!(app.terminal_height, 40);

        // Update again
        app.terminal_width = 80;
        app.terminal_height = 24;
        assert_eq!(app.terminal_width, 80);
        assert_eq!(app.terminal_height, 24);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_input_processing (lines 256-271) ---[0m
    async fn test_input_processing() {
        let mut app = App::new().await.unwrap();

        // Test command input
        app.input_mode = InputMode::Command;
        app.input_buffer = "test command".to_string();

        // Verify input buffer works
        assert_eq!(app.input_buffer, "test command");

        // Clear input
        app.input_buffer.clear();
        app.input_mode = InputMode::Normal;
        assert_eq!(app.input_mode, InputMode::Normal);
        assert!(app.input_buffer.is_empty());
    }

  ./crates/ccswarm/src/tui/tests.rs:130-147 function test_delegation_mode <-> ./crates/ccswarm/src/tui/tests.rs:221-239 function test_resize_handling
  Similarity: 73.45%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_delegation_mode (lines 130-147) ---[0m
    async fn test_delegation_mode() {
        let mut app = App::new().await.unwrap();

        app.current_tab = Tab::Delegation;

        // Initial mode
        assert_eq!(app.delegation_mode, DelegationMode::Analyze);

        // Test different delegation modes exist
        app.delegation_mode = DelegationMode::Delegate;
        assert_eq!(app.delegation_mode, DelegationMode::Delegate);

        app.delegation_mode = DelegationMode::ViewStats;
        assert_eq!(app.delegation_mode, DelegationMode::ViewStats);

        app.delegation_mode = DelegationMode::Analyze;
        assert_eq!(app.delegation_mode, DelegationMode::Analyze);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_resize_handling (lines 221-239) ---[0m
    async fn test_resize_handling() {
        let mut app = App::new().await.unwrap();

        // Initial size
        assert_eq!(app.terminal_width, 80);
        assert_eq!(app.terminal_height, 24);

        // Update size
        app.terminal_width = 120;
        app.terminal_height = 40;
        assert_eq!(app.terminal_width, 120);
        assert_eq!(app.terminal_height, 40);

        // Update again
        app.terminal_width = 80;
        app.terminal_height = 24;
        assert_eq!(app.terminal_width, 80);
        assert_eq!(app.terminal_height, 24);
    }

  ./crates/ccswarm/src/tui/tests.rs:151-171 function test_system_status <-> ./crates/ccswarm/src/tui/tests.rs:256-271 function test_input_processing
  Similarity: 70.82%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_system_status (lines 151-171) ---[0m
    async fn test_system_status() {
        let mut app = App::new().await.unwrap();

        // Initial state
        assert_eq!(app.system_status, "Starting...");

        // Update status
        app.system_status = "Running".to_string();
        assert_eq!(app.system_status, "Running");

        // Update metrics
        app.total_agents = 4;
        app.active_agents = 3;
        app.pending_tasks = 5;
        app.completed_tasks = 10;

        assert_eq!(app.total_agents, 4);
        assert_eq!(app.active_agents, 3);
        assert_eq!(app.pending_tasks, 5);
        assert_eq!(app.completed_tasks, 10);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_input_processing (lines 256-271) ---[0m
    async fn test_input_processing() {
        let mut app = App::new().await.unwrap();

        // Test command input
        app.input_mode = InputMode::Command;
        app.input_buffer = "test command".to_string();

        // Verify input buffer works
        assert_eq!(app.input_buffer, "test command");

        // Clear input
        app.input_buffer.clear();
        app.input_mode = InputMode::Normal;
        assert_eq!(app.input_mode, InputMode::Normal);
        assert!(app.input_buffer.is_empty());
    }

  ./crates/ccswarm/src/tui/tests.rs:130-147 function test_delegation_mode <-> ./crates/ccswarm/src/tui/tests.rs:256-271 function test_input_processing
  Similarity: 75.77%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_delegation_mode (lines 130-147) ---[0m
    async fn test_delegation_mode() {
        let mut app = App::new().await.unwrap();

        app.current_tab = Tab::Delegation;

        // Initial mode
        assert_eq!(app.delegation_mode, DelegationMode::Analyze);

        // Test different delegation modes exist
        app.delegation_mode = DelegationMode::Delegate;
        assert_eq!(app.delegation_mode, DelegationMode::Delegate);

        app.delegation_mode = DelegationMode::ViewStats;
        assert_eq!(app.delegation_mode, DelegationMode::ViewStats);

        app.delegation_mode = DelegationMode::Analyze;
        assert_eq!(app.delegation_mode, DelegationMode::Analyze);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_input_processing (lines 256-271) ---[0m
    async fn test_input_processing() {
        let mut app = App::new().await.unwrap();

        // Test command input
        app.input_mode = InputMode::Command;
        app.input_buffer = "test command".to_string();

        // Verify input buffer works
        assert_eq!(app.input_buffer, "test command");

        // Clear input
        app.input_buffer.clear();
        app.input_mode = InputMode::Normal;
        assert_eq!(app.input_mode, InputMode::Normal);
        assert!(app.input_buffer.is_empty());
    }

  ./crates/ccswarm/src/tui/tests.rs:13-24 function test_app_new <-> ./crates/ccswarm/src/tui/tests.rs:365-384 function test_ui_component_rendering
  Similarity: 79.14%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_app_new (lines 13-24) ---[0m
    async fn test_app_new() {
        let app = App::new().await.unwrap();

        assert_eq!(app.input_mode, InputMode::Normal);
        assert_eq!(app.current_tab, Tab::Overview);
        assert!(app.agents.is_empty());
        assert!(app.tasks.is_empty());
        assert!(app.logs.is_empty());
        assert_eq!(app.selected_agent, 0);
        assert_eq!(app.selected_task, 0);
        assert_eq!(app.selected_log, 0);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_ui_component_rendering (lines 365-384) ---[0m
    fn test_ui_component_rendering() {
        use ratatui::widgets::{Block, Borders};

        let backend = TestBackend::new(80, 30);
        let mut terminal = Terminal::new(backend).unwrap();

        // Test rendering a simple block
        terminal
            .draw(|f| {
                let block = Block::default().title("Test Block").borders(Borders::ALL);
                f.render_widget(block, f.area());
            })
            .unwrap();

        let buffer = terminal.backend().buffer();
        let content = buffer_to_string(buffer);

        // Verify block title is rendered
        assert!(content.contains("Test Block"));
    }

  ./crates/ccswarm/src/tui/tests.rs:339-349 function test_data_refresh <-> ./crates/ccswarm/src/tui/tests.rs:365-384 function test_ui_component_rendering
  Similarity: 80.21%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_data_refresh (lines 339-349) ---[0m
    async fn test_data_refresh() {
        let mut app = App::new().await.unwrap();

        // Add some test data
        app.agents = vec![create_test_agent_info("agent-1")];
        app.tasks = vec![create_test_task_info("task-1")];

        // Verify data exists
        assert_eq!(app.agents.len(), 1);
        assert_eq!(app.tasks.len(), 1);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_ui_component_rendering (lines 365-384) ---[0m
    fn test_ui_component_rendering() {
        use ratatui::widgets::{Block, Borders};

        let backend = TestBackend::new(80, 30);
        let mut terminal = Terminal::new(backend).unwrap();

        // Test rendering a simple block
        terminal
            .draw(|f| {
                let block = Block::default().title("Test Block").borders(Borders::ALL);
                f.render_widget(block, f.area());
            })
            .unwrap();

        let buffer = terminal.backend().buffer();
        let content = buffer_to_string(buffer);

        // Verify block title is rendered
        assert!(content.contains("Test Block"));
    }

  ./crates/ccswarm/src/tui/tests.rs:412-426 function create_test_agent_info <-> ./crates/ccswarm/src/tui/tests.rs:429-439 function create_test_task_info
  Similarity: 88.99%

[36m--- ./crates/ccswarm/src/tui/tests.rs:create_test_agent_info (lines 412-426) ---[0m
    fn create_test_agent_info(id: &str) -> AgentInfo {
        AgentInfo {
            id: id.to_string(),
            name: format!("Agent {}", id),
            specialization: "Backend".to_string(),
            provider_type: "claude_code".to_string(),
            provider_icon: "🤖".to_string(),
            provider_color: "blue".to_string(),
            status: AgentStatus::Available,
            current_task: None,
            tasks_completed: 0,
            last_activity: Utc::now(),
            workspace: "/tmp/workspace".to_string(),
        }
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:create_test_task_info (lines 429-439) ---[0m
    fn create_test_task_info(id: &str) -> TaskInfo {
        TaskInfo {
            id: id.to_string(),
            description: format!("Task {}", id),
            priority: "Medium".to_string(),
            task_type: "Development".to_string(),
            status: "Pending".to_string(),
            assigned_agent: None,
            created_at: Utc::now(),
        }
    }

  ./crates/ccswarm/src/tui/tests.rs:13-24 function test_app_new <-> ./crates/ccswarm/src/tui/tests.rs:221-239 function test_resize_handling
  Similarity: 72.34%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_app_new (lines 13-24) ---[0m
    async fn test_app_new() {
        let app = App::new().await.unwrap();

        assert_eq!(app.input_mode, InputMode::Normal);
        assert_eq!(app.current_tab, Tab::Overview);
        assert!(app.agents.is_empty());
        assert!(app.tasks.is_empty());
        assert!(app.logs.is_empty());
        assert_eq!(app.selected_agent, 0);
        assert_eq!(app.selected_task, 0);
        assert_eq!(app.selected_log, 0);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_resize_handling (lines 221-239) ---[0m
    async fn test_resize_handling() {
        let mut app = App::new().await.unwrap();

        // Initial size
        assert_eq!(app.terminal_width, 80);
        assert_eq!(app.terminal_height, 24);

        // Update size
        app.terminal_width = 120;
        app.terminal_height = 40;
        assert_eq!(app.terminal_width, 120);
        assert_eq!(app.terminal_height, 40);

        // Update again
        app.terminal_width = 80;
        app.terminal_height = 24;
        assert_eq!(app.terminal_width, 80);
        assert_eq!(app.terminal_height, 24);
    }

  ./crates/ccswarm/src/tui/tests.rs:13-24 function test_app_new <-> ./crates/ccswarm/src/tui/tests.rs:130-147 function test_delegation_mode
  Similarity: 73.97%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_app_new (lines 13-24) ---[0m
    async fn test_app_new() {
        let app = App::new().await.unwrap();

        assert_eq!(app.input_mode, InputMode::Normal);
        assert_eq!(app.current_tab, Tab::Overview);
        assert!(app.agents.is_empty());
        assert!(app.tasks.is_empty());
        assert!(app.logs.is_empty());
        assert_eq!(app.selected_agent, 0);
        assert_eq!(app.selected_task, 0);
        assert_eq!(app.selected_log, 0);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_delegation_mode (lines 130-147) ---[0m
    async fn test_delegation_mode() {
        let mut app = App::new().await.unwrap();

        app.current_tab = Tab::Delegation;

        // Initial mode
        assert_eq!(app.delegation_mode, DelegationMode::Analyze);

        // Test different delegation modes exist
        app.delegation_mode = DelegationMode::Delegate;
        assert_eq!(app.delegation_mode, DelegationMode::Delegate);

        app.delegation_mode = DelegationMode::ViewStats;
        assert_eq!(app.delegation_mode, DelegationMode::ViewStats);

        app.delegation_mode = DelegationMode::Analyze;
        assert_eq!(app.delegation_mode, DelegationMode::Analyze);
    }

  ./crates/ccswarm/src/tui/tests.rs:207-217 function test_event_handler <-> ./crates/ccswarm/src/tui/tests.rs:256-271 function test_input_processing
  Similarity: 77.54%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_event_handler (lines 207-217) ---[0m
    async fn test_event_handler() {
        let mut handler = EventHandler::new(Duration::from_millis(100));

        // Since we can't easily simulate actual keyboard events in tests,
        // we just verify the handler is created and can be polled
        let event = handler.next().await;
        // Event might be None if no actual events occurred
        // The event handler returns either None or Some(Event)
        // We can't easily check for specific event types in tests
        assert!(event.is_none() || event.is_some());
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_input_processing (lines 256-271) ---[0m
    async fn test_input_processing() {
        let mut app = App::new().await.unwrap();

        // Test command input
        app.input_mode = InputMode::Command;
        app.input_buffer = "test command".to_string();

        // Verify input buffer works
        assert_eq!(app.input_buffer, "test command");

        // Clear input
        app.input_buffer.clear();
        app.input_mode = InputMode::Normal;
        assert_eq!(app.input_mode, InputMode::Normal);
        assert!(app.input_buffer.is_empty());
    }

  ./crates/ccswarm/src/tui/tests.rs:130-147 function test_delegation_mode <-> ./crates/ccswarm/src/tui/tests.rs:207-217 function test_event_handler
  Similarity: 70.07%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_delegation_mode (lines 130-147) ---[0m
    async fn test_delegation_mode() {
        let mut app = App::new().await.unwrap();

        app.current_tab = Tab::Delegation;

        // Initial mode
        assert_eq!(app.delegation_mode, DelegationMode::Analyze);

        // Test different delegation modes exist
        app.delegation_mode = DelegationMode::Delegate;
        assert_eq!(app.delegation_mode, DelegationMode::Delegate);

        app.delegation_mode = DelegationMode::ViewStats;
        assert_eq!(app.delegation_mode, DelegationMode::ViewStats);

        app.delegation_mode = DelegationMode::Analyze;
        assert_eq!(app.delegation_mode, DelegationMode::Analyze);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_event_handler (lines 207-217) ---[0m
    async fn test_event_handler() {
        let mut handler = EventHandler::new(Duration::from_millis(100));

        // Since we can't easily simulate actual keyboard events in tests,
        // we just verify the handler is created and can be polled
        let event = handler.next().await;
        // Event might be None if no actual events occurred
        // The event handler returns either None or Some(Event)
        // We can't easily check for specific event types in tests
        assert!(event.is_none() || event.is_some());
    }

  ./crates/ccswarm/src/tui/tests.rs:13-24 function test_app_new <-> ./crates/ccswarm/src/tui/tests.rs:256-271 function test_input_processing
  Similarity: 70.21%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_app_new (lines 13-24) ---[0m
    async fn test_app_new() {
        let app = App::new().await.unwrap();

        assert_eq!(app.input_mode, InputMode::Normal);
        assert_eq!(app.current_tab, Tab::Overview);
        assert!(app.agents.is_empty());
        assert!(app.tasks.is_empty());
        assert!(app.logs.is_empty());
        assert_eq!(app.selected_agent, 0);
        assert_eq!(app.selected_task, 0);
        assert_eq!(app.selected_log, 0);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_input_processing (lines 256-271) ---[0m
    async fn test_input_processing() {
        let mut app = App::new().await.unwrap();

        // Test command input
        app.input_mode = InputMode::Command;
        app.input_buffer = "test command".to_string();

        // Verify input buffer works
        assert_eq!(app.input_buffer, "test command");

        // Clear input
        app.input_buffer.clear();
        app.input_mode = InputMode::Normal;
        assert_eq!(app.input_mode, InputMode::Normal);
        assert!(app.input_buffer.is_empty());
    }

  ./crates/ccswarm/src/tui/tests.rs:207-217 function test_event_handler <-> ./crates/ccswarm/src/tui/tests.rs:339-349 function test_data_refresh
  Similarity: 80.73%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_event_handler (lines 207-217) ---[0m
    async fn test_event_handler() {
        let mut handler = EventHandler::new(Duration::from_millis(100));

        // Since we can't easily simulate actual keyboard events in tests,
        // we just verify the handler is created and can be polled
        let event = handler.next().await;
        // Event might be None if no actual events occurred
        // The event handler returns either None or Some(Event)
        // We can't easily check for specific event types in tests
        assert!(event.is_none() || event.is_some());
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_data_refresh (lines 339-349) ---[0m
    async fn test_data_refresh() {
        let mut app = App::new().await.unwrap();

        // Add some test data
        app.agents = vec![create_test_agent_info("agent-1")];
        app.tasks = vec![create_test_task_info("task-1")];

        // Verify data exists
        assert_eq!(app.agents.len(), 1);
        assert_eq!(app.tasks.len(), 1);
    }

  ./crates/ccswarm/src/tui/tests.rs:207-217 function test_event_handler <-> ./crates/ccswarm/src/tui/tests.rs:242-252 function buffer_to_string
  Similarity: 76.33%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_event_handler (lines 207-217) ---[0m
    async fn test_event_handler() {
        let mut handler = EventHandler::new(Duration::from_millis(100));

        // Since we can't easily simulate actual keyboard events in tests,
        // we just verify the handler is created and can be polled
        let event = handler.next().await;
        // Event might be None if no actual events occurred
        // The event handler returns either None or Some(Event)
        // We can't easily check for specific event types in tests
        assert!(event.is_none() || event.is_some());
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:buffer_to_string (lines 242-252) ---[0m
    fn buffer_to_string(buffer: &Buffer) -> String {
        let mut result = String::new();
        for y in 0..buffer.area.height {
            for x in 0..buffer.area.width {
                let cell = &buffer[(x, y)];
                result.push_str(cell.symbol());
            }
            result.push('\n');
        }
        result
    }

  ./crates/ccswarm/src/tui/tests.rs:13-24 function test_app_new <-> ./crates/ccswarm/src/tui/tests.rs:207-217 function test_event_handler
  Similarity: 72.48%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_app_new (lines 13-24) ---[0m
    async fn test_app_new() {
        let app = App::new().await.unwrap();

        assert_eq!(app.input_mode, InputMode::Normal);
        assert_eq!(app.current_tab, Tab::Overview);
        assert!(app.agents.is_empty());
        assert!(app.tasks.is_empty());
        assert!(app.logs.is_empty());
        assert_eq!(app.selected_agent, 0);
        assert_eq!(app.selected_task, 0);
        assert_eq!(app.selected_log, 0);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_event_handler (lines 207-217) ---[0m
    async fn test_event_handler() {
        let mut handler = EventHandler::new(Duration::from_millis(100));

        // Since we can't easily simulate actual keyboard events in tests,
        // we just verify the handler is created and can be polled
        let event = handler.next().await;
        // Event might be None if no actual events occurred
        // The event handler returns either None or Some(Event)
        // We can't easily check for specific event types in tests
        assert!(event.is_none() || event.is_some());
    }

  ./crates/ccswarm/src/tui/tests.rs:327-335 function test_status_display <-> ./crates/ccswarm/src/tui/tests.rs:339-349 function test_data_refresh
  Similarity: 82.94%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_status_display (lines 327-335) ---[0m
    async fn test_status_display() {
        let mut app = App::new().await.unwrap();

        // Update system status
        app.system_status = "Operational".to_string();

        // Verify status is set
        assert_eq!(app.system_status, "Operational");
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_data_refresh (lines 339-349) ---[0m
    async fn test_data_refresh() {
        let mut app = App::new().await.unwrap();

        // Add some test data
        app.agents = vec![create_test_agent_info("agent-1")];
        app.tasks = vec![create_test_task_info("task-1")];

        // Verify data exists
        assert_eq!(app.agents.len(), 1);
        assert_eq!(app.tasks.len(), 1);
    }

  ./crates/ccswarm/src/tui/tests.rs:339-349 function test_data_refresh <-> ./crates/ccswarm/src/tui/tests.rs:353-361 function test_metrics_initialization
  Similarity: 78.81%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_data_refresh (lines 339-349) ---[0m
    async fn test_data_refresh() {
        let mut app = App::new().await.unwrap();

        // Add some test data
        app.agents = vec![create_test_agent_info("agent-1")];
        app.tasks = vec![create_test_task_info("task-1")];

        // Verify data exists
        assert_eq!(app.agents.len(), 1);
        assert_eq!(app.tasks.len(), 1);
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_metrics_initialization (lines 353-361) ---[0m
    async fn test_metrics_initialization() {
        let app = App::new().await.unwrap();

        // Verify metrics are initialized
        assert_eq!(app.total_agents, 0);
        assert_eq!(app.active_agents, 0);
        assert_eq!(app.pending_tasks, 0);
        assert_eq!(app.completed_tasks, 0);
    }

  ./crates/ccswarm/src/tui/tests.rs:242-252 function buffer_to_string <-> ./crates/ccswarm/src/tui/tests.rs:327-335 function test_status_display
  Similarity: 77.04%

[36m--- ./crates/ccswarm/src/tui/tests.rs:buffer_to_string (lines 242-252) ---[0m
    fn buffer_to_string(buffer: &Buffer) -> String {
        let mut result = String::new();
        for y in 0..buffer.area.height {
            for x in 0..buffer.area.width {
                let cell = &buffer[(x, y)];
                result.push_str(cell.symbol());
            }
            result.push('\n');
        }
        result
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_status_display (lines 327-335) ---[0m
    async fn test_status_display() {
        let mut app = App::new().await.unwrap();

        // Update system status
        app.system_status = "Operational".to_string();

        // Verify status is set
        assert_eq!(app.system_status, "Operational");
    }

  ./crates/ccswarm/src/tui/tests.rs:207-217 function test_event_handler <-> ./crates/ccswarm/src/tui/tests.rs:353-361 function test_metrics_initialization
  Similarity: 71.15%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_event_handler (lines 207-217) ---[0m
    async fn test_event_handler() {
        let mut handler = EventHandler::new(Duration::from_millis(100));

        // Since we can't easily simulate actual keyboard events in tests,
        // we just verify the handler is created and can be polled
        let event = handler.next().await;
        // Event might be None if no actual events occurred
        // The event handler returns either None or Some(Event)
        // We can't easily check for specific event types in tests
        assert!(event.is_none() || event.is_some());
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_metrics_initialization (lines 353-361) ---[0m
    async fn test_metrics_initialization() {
        let app = App::new().await.unwrap();

        // Verify metrics are initialized
        assert_eq!(app.total_agents, 0);
        assert_eq!(app.active_agents, 0);
        assert_eq!(app.pending_tasks, 0);
        assert_eq!(app.completed_tasks, 0);
    }

  ./crates/ccswarm/src/tui/tests.rs:327-335 function test_status_display <-> ./crates/ccswarm/src/tui/tests.rs:353-361 function test_metrics_initialization
  Similarity: 77.95%

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_status_display (lines 327-335) ---[0m
    async fn test_status_display() {
        let mut app = App::new().await.unwrap();

        // Update system status
        app.system_status = "Operational".to_string();

        // Verify status is set
        assert_eq!(app.system_status, "Operational");
    }

[36m--- ./crates/ccswarm/src/tui/tests.rs:test_metrics_initialization (lines 353-361) ---[0m
    async fn test_metrics_initialization() {
        let app = App::new().await.unwrap();

        // Verify metrics are initialized
        assert_eq!(app.total_agents, 0);
        assert_eq!(app.active_agents, 0);
        assert_eq!(app.pending_tasks, 0);
        assert_eq!(app.completed_tasks, 0);
    }


Duplicates in ./crates/ccswarm/tests/quickstart_test.rs:
------------------------------------------------------------
  ./crates/ccswarm/tests/quickstart_test.rs:5-24 function test_quickstart_command_parsing <-> ./crates/ccswarm/tests/quickstart_test.rs:27-54 function test_quickstart_with_options
  Similarity: 93.31%

[36m--- ./crates/ccswarm/tests/quickstart_test.rs:test_quickstart_command_parsing (lines 5-24) ---[0m
fn test_quickstart_command_parsing() {
    // Test basic quickstart command
    let args = vec!["ccswarm", "quickstart"];
    let cli = Cli::parse_from(args);

    match cli.command {
        Commands::Quickstart {
            name,
            no_prompt,
            all_agents,
            with_tests,
        } => {
            assert!(name.is_none());
            assert!(!no_prompt);
            assert!(!all_agents);
            assert!(!with_tests);
        }
        _ => panic!("Expected Quickstart command"),
    }
}

[36m--- ./crates/ccswarm/tests/quickstart_test.rs:test_quickstart_with_options (lines 27-54) ---[0m
fn test_quickstart_with_options() {
    // Test quickstart with all options
    let args = vec![
        "ccswarm",
        "quickstart",
        "--name",
        "test-project",
        "--no-prompt",
        "--all-agents",
        "--with-tests",
    ];
    let cli = Cli::parse_from(args);

    match cli.command {
        Commands::Quickstart {
            name,
            no_prompt,
            all_agents,
            with_tests,
        } => {
            assert_eq!(name, Some("test-project".to_string()));
            assert!(no_prompt);
            assert!(all_agents);
            assert!(with_tests);
        }
        _ => panic!("Expected Quickstart command"),
    }
}

  ./crates/ccswarm/tests/quickstart_test.rs:27-54 function test_quickstart_with_options <-> ./crates/ccswarm/tests/quickstart_test.rs:57-68 function test_quickstart_short_options
  Similarity: 92.64%

[36m--- ./crates/ccswarm/tests/quickstart_test.rs:test_quickstart_with_options (lines 27-54) ---[0m
fn test_quickstart_with_options() {
    // Test quickstart with all options
    let args = vec![
        "ccswarm",
        "quickstart",
        "--name",
        "test-project",
        "--no-prompt",
        "--all-agents",
        "--with-tests",
    ];
    let cli = Cli::parse_from(args);

    match cli.command {
        Commands::Quickstart {
            name,
            no_prompt,
            all_agents,
            with_tests,
        } => {
            assert_eq!(name, Some("test-project".to_string()));
            assert!(no_prompt);
            assert!(all_agents);
            assert!(with_tests);
        }
        _ => panic!("Expected Quickstart command"),
    }
}

[36m--- ./crates/ccswarm/tests/quickstart_test.rs:test_quickstart_short_options (lines 57-68) ---[0m
fn test_quickstart_short_options() {
    // Test quickstart with short option for name
    let args = vec!["ccswarm", "quickstart", "-n", "my-app"];
    let cli = Cli::parse_from(args);

    match cli.command {
        Commands::Quickstart { name, .. } => {
            assert_eq!(name, Some("my-app".to_string()));
        }
        _ => panic!("Expected Quickstart command"),
    }
}

  ./crates/ccswarm/tests/quickstart_test.rs:5-24 function test_quickstart_command_parsing <-> ./crates/ccswarm/tests/quickstart_test.rs:57-68 function test_quickstart_short_options
  Similarity: 90.84%

[36m--- ./crates/ccswarm/tests/quickstart_test.rs:test_quickstart_command_parsing (lines 5-24) ---[0m
fn test_quickstart_command_parsing() {
    // Test basic quickstart command
    let args = vec!["ccswarm", "quickstart"];
    let cli = Cli::parse_from(args);

    match cli.command {
        Commands::Quickstart {
            name,
            no_prompt,
            all_agents,
            with_tests,
        } => {
            assert!(name.is_none());
            assert!(!no_prompt);
            assert!(!all_agents);
            assert!(!with_tests);
        }
        _ => panic!("Expected Quickstart command"),
    }
}

[36m--- ./crates/ccswarm/tests/quickstart_test.rs:test_quickstart_short_options (lines 57-68) ---[0m
fn test_quickstart_short_options() {
    // Test quickstart with short option for name
    let args = vec!["ccswarm", "quickstart", "-n", "my-app"];
    let cli = Cli::parse_from(args);

    match cli.command {
        Commands::Quickstart { name, .. } => {
            assert_eq!(name, Some("my-app".to_string()));
        }
        _ => panic!("Expected Quickstart command"),
    }
}


Duplicates in ./crates/ccswarm/src/coordination/mod.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/coordination/mod.rs:225-251 method cleanup_old_messages <-> ./crates/ccswarm/src/coordination/mod.rs:254-274 method load_persisted_messages
  Similarity: 89.60%
  Classes: CoordinationBus <-> CoordinationBus

[36m--- ./crates/ccswarm/src/coordination/mod.rs:cleanup_old_messages (lines 225-251) ---[0m
    async fn cleanup_old_messages(&self) -> Result<()> {
        let mut entries = fs::read_dir(&self.message_dir).await?;
        let mut files = Vec::new();

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(metadata) = entry.metadata().await {
                if metadata.is_file() {
                    files.push((entry.path(), metadata.modified()?));
                }
            }
        }

        // Sort by modification time
        files.sort_by_key(|(_, time)| *time);

        // Remove oldest files if we have more than 1000
        if files.len() > 1000 {
            let to_remove = files.len() - 1000;
            for (path, _) in files.into_iter().take(to_remove) {
                if let Err(e) = fs::remove_file(&path).await {
                    error!("Failed to remove old message file: {}", e);
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:load_persisted_messages (lines 254-274) ---[0m
    pub async fn load_persisted_messages(&self) -> Result<Vec<AgentMessage>> {
        let mut messages = Vec::new();
        let mut entries = fs::read_dir(&self.message_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(message) = serde_json::from_str::<AgentMessage>(&content) {
                    messages.push(message);
                }
            }
        }

        // Sort by timestamp if available
        messages.sort_by_key(|m| match m {
            AgentMessage::Heartbeat { timestamp, .. } => *timestamp,
            AgentMessage::InterAgentMessage { timestamp, .. } => *timestamp,
            _ => Utc::now(),
        });

        Ok(messages)
    }

  ./crates/ccswarm/src/coordination/mod.rs:225-251 method cleanup_old_messages <-> ./crates/ccswarm/src/coordination/mod.rs:322-338 method get_pending_tasks
  Similarity: 89.06%
  Classes: CoordinationBus <-> TaskQueue

[36m--- ./crates/ccswarm/src/coordination/mod.rs:cleanup_old_messages (lines 225-251) ---[0m
    async fn cleanup_old_messages(&self) -> Result<()> {
        let mut entries = fs::read_dir(&self.message_dir).await?;
        let mut files = Vec::new();

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(metadata) = entry.metadata().await {
                if metadata.is_file() {
                    files.push((entry.path(), metadata.modified()?));
                }
            }
        }

        // Sort by modification time
        files.sort_by_key(|(_, time)| *time);

        // Remove oldest files if we have more than 1000
        if files.len() > 1000 {
            let to_remove = files.len() - 1000;
            for (path, _) in files.into_iter().take(to_remove) {
                if let Err(e) = fs::remove_file(&path).await {
                    error!("Failed to remove old message file: {}", e);
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_pending_tasks (lines 322-338) ---[0m
    pub async fn get_pending_tasks(&self) -> Result<Vec<crate::agent::Task>> {
        let mut tasks: Vec<crate::agent::Task> = Vec::new();
        let mut entries = fs::read_dir(&self.task_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(task) = serde_json::from_str(&content) {
                    tasks.push(task);
                }
            }
        }

        // Sort by priority
        tasks.sort_by_key(|t| std::cmp::Reverse(t.priority));

        Ok(tasks)
    }

  ./crates/ccswarm/src/coordination/mod.rs:225-251 method cleanup_old_messages <-> ./crates/ccswarm/src/coordination/mod.rs:427-445 function test_coordination_bus
  Similarity: 84.04%

[36m--- ./crates/ccswarm/src/coordination/mod.rs:cleanup_old_messages (lines 225-251) ---[0m
    async fn cleanup_old_messages(&self) -> Result<()> {
        let mut entries = fs::read_dir(&self.message_dir).await?;
        let mut files = Vec::new();

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(metadata) = entry.metadata().await {
                if metadata.is_file() {
                    files.push((entry.path(), metadata.modified()?));
                }
            }
        }

        // Sort by modification time
        files.sort_by_key(|(_, time)| *time);

        // Remove oldest files if we have more than 1000
        if files.len() > 1000 {
            let to_remove = files.len() - 1000;
            for (path, _) in files.into_iter().take(to_remove) {
                if let Err(e) = fs::remove_file(&path).await {
                    error!("Failed to remove old message file: {}", e);
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:test_coordination_bus (lines 427-445) ---[0m
    async fn test_coordination_bus() {
        let bus = CoordinationBus::new().await.unwrap();

        let message = AgentMessage::Heartbeat {
            agent_id: "test-agent".to_string(),
            timestamp: Utc::now(),
        };

        bus.send_message(message.clone()).await.unwrap();

        let received = bus.receive_message().await.unwrap();

        match received {
            AgentMessage::Heartbeat { agent_id, .. } => {
                assert_eq!(agent_id, "test-agent");
            }
            _ => panic!("Wrong message type received"),
        }
    }

  ./crates/ccswarm/src/coordination/mod.rs:254-274 method load_persisted_messages <-> ./crates/ccswarm/src/coordination/mod.rs:368-388 method update_status
  Similarity: 89.19%
  Classes: CoordinationBus <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:load_persisted_messages (lines 254-274) ---[0m
    pub async fn load_persisted_messages(&self) -> Result<Vec<AgentMessage>> {
        let mut messages = Vec::new();
        let mut entries = fs::read_dir(&self.message_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(message) = serde_json::from_str::<AgentMessage>(&content) {
                    messages.push(message);
                }
            }
        }

        // Sort by timestamp if available
        messages.sort_by_key(|m| match m {
            AgentMessage::Heartbeat { timestamp, .. } => *timestamp,
            AgentMessage::InterAgentMessage { timestamp, .. } => *timestamp,
            _ => Utc::now(),
        });

        Ok(messages)
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:update_status (lines 368-388) ---[0m
    pub async fn update_status(
        &self,
        agent_id: &str,
        status: &AgentStatus,
        additional_info: serde_json::Value,
    ) -> Result<()> {
        let filename = format!("{}.json", agent_id);
        let filepath = self.status_dir.join(&filename);

        let status_info = serde_json::json!({
            "agent_id": agent_id,
            "status": status,
            "timestamp": Utc::now(),
            "additional_info": additional_info,
        });

        let content = serde_json::to_string_pretty(&status_info)?;
        fs::write(&filepath, content).await?;

        Ok(())
    }

  ./crates/ccswarm/src/coordination/mod.rs:254-274 method load_persisted_messages <-> ./crates/ccswarm/src/coordination/mod.rs:322-338 method get_pending_tasks
  Similarity: 95.00%
  Classes: CoordinationBus <-> TaskQueue

[36m--- ./crates/ccswarm/src/coordination/mod.rs:load_persisted_messages (lines 254-274) ---[0m
    pub async fn load_persisted_messages(&self) -> Result<Vec<AgentMessage>> {
        let mut messages = Vec::new();
        let mut entries = fs::read_dir(&self.message_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(message) = serde_json::from_str::<AgentMessage>(&content) {
                    messages.push(message);
                }
            }
        }

        // Sort by timestamp if available
        messages.sort_by_key(|m| match m {
            AgentMessage::Heartbeat { timestamp, .. } => *timestamp,
            AgentMessage::InterAgentMessage { timestamp, .. } => *timestamp,
            _ => Utc::now(),
        });

        Ok(messages)
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_pending_tasks (lines 322-338) ---[0m
    pub async fn get_pending_tasks(&self) -> Result<Vec<crate::agent::Task>> {
        let mut tasks: Vec<crate::agent::Task> = Vec::new();
        let mut entries = fs::read_dir(&self.task_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(task) = serde_json::from_str(&content) {
                    tasks.push(task);
                }
            }
        }

        // Sort by priority
        tasks.sort_by_key(|t| std::cmp::Reverse(t.priority));

        Ok(tasks)
    }

  ./crates/ccswarm/src/coordination/mod.rs:254-274 method load_persisted_messages <-> ./crates/ccswarm/src/coordination/mod.rs:427-445 function test_coordination_bus
  Similarity: 88.06%

[36m--- ./crates/ccswarm/src/coordination/mod.rs:load_persisted_messages (lines 254-274) ---[0m
    pub async fn load_persisted_messages(&self) -> Result<Vec<AgentMessage>> {
        let mut messages = Vec::new();
        let mut entries = fs::read_dir(&self.message_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(message) = serde_json::from_str::<AgentMessage>(&content) {
                    messages.push(message);
                }
            }
        }

        // Sort by timestamp if available
        messages.sort_by_key(|m| match m {
            AgentMessage::Heartbeat { timestamp, .. } => *timestamp,
            AgentMessage::InterAgentMessage { timestamp, .. } => *timestamp,
            _ => Utc::now(),
        });

        Ok(messages)
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:test_coordination_bus (lines 427-445) ---[0m
    async fn test_coordination_bus() {
        let bus = CoordinationBus::new().await.unwrap();

        let message = AgentMessage::Heartbeat {
            agent_id: "test-agent".to_string(),
            timestamp: Utc::now(),
        };

        bus.send_message(message.clone()).await.unwrap();

        let received = bus.receive_message().await.unwrap();

        match received {
            AgentMessage::Heartbeat { agent_id, .. } => {
                assert_eq!(agent_id, "test-agent");
            }
            _ => panic!("Wrong message type received"),
        }
    }

  ./crates/ccswarm/src/coordination/mod.rs:225-251 method cleanup_old_messages <-> ./crates/ccswarm/src/coordination/mod.rs:406-419 method get_all_statuses
  Similarity: 81.99%
  Classes: CoordinationBus <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:cleanup_old_messages (lines 225-251) ---[0m
    async fn cleanup_old_messages(&self) -> Result<()> {
        let mut entries = fs::read_dir(&self.message_dir).await?;
        let mut files = Vec::new();

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(metadata) = entry.metadata().await {
                if metadata.is_file() {
                    files.push((entry.path(), metadata.modified()?));
                }
            }
        }

        // Sort by modification time
        files.sort_by_key(|(_, time)| *time);

        // Remove oldest files if we have more than 1000
        if files.len() > 1000 {
            let to_remove = files.len() - 1000;
            for (path, _) in files.into_iter().take(to_remove) {
                if let Err(e) = fs::remove_file(&path).await {
                    error!("Failed to remove old message file: {}", e);
                }
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_all_statuses (lines 406-419) ---[0m
    pub async fn get_all_statuses(&self) -> Result<Vec<serde_json::Value>> {
        let mut statuses = Vec::new();
        let mut entries = fs::read_dir(&self.status_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(status) = serde_json::from_str(&content) {
                    statuses.push(status);
                }
            }
        }

        Ok(statuses)
    }

  ./crates/ccswarm/src/coordination/mod.rs:204-222 method persist_message <-> ./crates/ccswarm/src/coordination/mod.rs:254-274 method load_persisted_messages
  Similarity: 83.11%
  Classes: CoordinationBus <-> CoordinationBus

[36m--- ./crates/ccswarm/src/coordination/mod.rs:persist_message (lines 204-222) ---[0m
    async fn persist_message(&self, message: &AgentMessage) -> Result<()> {
        let timestamp = Utc::now();
        let filename = format!(
            "{}-{}.json",
            timestamp.timestamp_millis(),
            uuid::Uuid::new_v4()
        );
        let filepath = self.message_dir.join(&filename);

        let content = serde_json::to_string_pretty(message)?;
        fs::write(&filepath, content)
            .await
            .context("Failed to persist message")?;

        // Clean up old messages (keep last 1000)
        self.cleanup_old_messages().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:load_persisted_messages (lines 254-274) ---[0m
    pub async fn load_persisted_messages(&self) -> Result<Vec<AgentMessage>> {
        let mut messages = Vec::new();
        let mut entries = fs::read_dir(&self.message_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(message) = serde_json::from_str::<AgentMessage>(&content) {
                    messages.push(message);
                }
            }
        }

        // Sort by timestamp if available
        messages.sort_by_key(|m| match m {
            AgentMessage::Heartbeat { timestamp, .. } => *timestamp,
            AgentMessage::InterAgentMessage { timestamp, .. } => *timestamp,
            _ => Utc::now(),
        });

        Ok(messages)
    }

  ./crates/ccswarm/src/coordination/mod.rs:368-388 method update_status <-> ./crates/ccswarm/src/coordination/mod.rs:427-445 function test_coordination_bus
  Similarity: 81.95%

[36m--- ./crates/ccswarm/src/coordination/mod.rs:update_status (lines 368-388) ---[0m
    pub async fn update_status(
        &self,
        agent_id: &str,
        status: &AgentStatus,
        additional_info: serde_json::Value,
    ) -> Result<()> {
        let filename = format!("{}.json", agent_id);
        let filepath = self.status_dir.join(&filename);

        let status_info = serde_json::json!({
            "agent_id": agent_id,
            "status": status,
            "timestamp": Utc::now(),
            "additional_info": additional_info,
        });

        let content = serde_json::to_string_pretty(&status_info)?;
        fs::write(&filepath, content).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:test_coordination_bus (lines 427-445) ---[0m
    async fn test_coordination_bus() {
        let bus = CoordinationBus::new().await.unwrap();

        let message = AgentMessage::Heartbeat {
            agent_id: "test-agent".to_string(),
            timestamp: Utc::now(),
        };

        bus.send_message(message.clone()).await.unwrap();

        let received = bus.receive_message().await.unwrap();

        match received {
            AgentMessage::Heartbeat { agent_id, .. } => {
                assert_eq!(agent_id, "test-agent");
            }
            _ => panic!("Wrong message type received"),
        }
    }

  ./crates/ccswarm/src/coordination/mod.rs:254-274 method load_persisted_messages <-> ./crates/ccswarm/src/coordination/mod.rs:406-419 method get_all_statuses
  Similarity: 93.65%
  Classes: CoordinationBus <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:load_persisted_messages (lines 254-274) ---[0m
    pub async fn load_persisted_messages(&self) -> Result<Vec<AgentMessage>> {
        let mut messages = Vec::new();
        let mut entries = fs::read_dir(&self.message_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(message) = serde_json::from_str::<AgentMessage>(&content) {
                    messages.push(message);
                }
            }
        }

        // Sort by timestamp if available
        messages.sort_by_key(|m| match m {
            AgentMessage::Heartbeat { timestamp, .. } => *timestamp,
            AgentMessage::InterAgentMessage { timestamp, .. } => *timestamp,
            _ => Utc::now(),
        });

        Ok(messages)
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_all_statuses (lines 406-419) ---[0m
    pub async fn get_all_statuses(&self) -> Result<Vec<serde_json::Value>> {
        let mut statuses = Vec::new();
        let mut entries = fs::read_dir(&self.status_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(status) = serde_json::from_str(&content) {
                    statuses.push(status);
                }
            }
        }

        Ok(statuses)
    }

  ./crates/ccswarm/src/coordination/mod.rs:322-338 method get_pending_tasks <-> ./crates/ccswarm/src/coordination/mod.rs:368-388 method update_status
  Similarity: 85.72%
  Classes: TaskQueue <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_pending_tasks (lines 322-338) ---[0m
    pub async fn get_pending_tasks(&self) -> Result<Vec<crate::agent::Task>> {
        let mut tasks: Vec<crate::agent::Task> = Vec::new();
        let mut entries = fs::read_dir(&self.task_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(task) = serde_json::from_str(&content) {
                    tasks.push(task);
                }
            }
        }

        // Sort by priority
        tasks.sort_by_key(|t| std::cmp::Reverse(t.priority));

        Ok(tasks)
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:update_status (lines 368-388) ---[0m
    pub async fn update_status(
        &self,
        agent_id: &str,
        status: &AgentStatus,
        additional_info: serde_json::Value,
    ) -> Result<()> {
        let filename = format!("{}.json", agent_id);
        let filepath = self.status_dir.join(&filename);

        let status_info = serde_json::json!({
            "agent_id": agent_id,
            "status": status,
            "timestamp": Utc::now(),
            "additional_info": additional_info,
        });

        let content = serde_json::to_string_pretty(&status_info)?;
        fs::write(&filepath, content).await?;

        Ok(())
    }

  ./crates/ccswarm/src/coordination/mod.rs:175-188 method send_message <-> ./crates/ccswarm/src/coordination/mod.rs:368-388 method update_status
  Similarity: 87.22%
  Classes: CoordinationBus <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:send_message (lines 175-188) ---[0m
    pub async fn send_message(&self, message: AgentMessage) -> Result<()> {
        debug!("Sending message: {:?}", message);

        // Send through channel
        self.sender
            .send(message.clone())
            .await
            .context("Failed to send message through channel")?;

        // Also persist to disk for durability
        self.persist_message(&message).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:update_status (lines 368-388) ---[0m
    pub async fn update_status(
        &self,
        agent_id: &str,
        status: &AgentStatus,
        additional_info: serde_json::Value,
    ) -> Result<()> {
        let filename = format!("{}.json", agent_id);
        let filepath = self.status_dir.join(&filename);

        let status_info = serde_json::json!({
            "agent_id": agent_id,
            "status": status,
            "timestamp": Utc::now(),
            "additional_info": additional_info,
        });

        let content = serde_json::to_string_pretty(&status_info)?;
        fs::write(&filepath, content).await?;

        Ok(())
    }

  ./crates/ccswarm/src/coordination/mod.rs:204-222 method persist_message <-> ./crates/ccswarm/src/coordination/mod.rs:368-388 method update_status
  Similarity: 75.96%
  Classes: CoordinationBus <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:persist_message (lines 204-222) ---[0m
    async fn persist_message(&self, message: &AgentMessage) -> Result<()> {
        let timestamp = Utc::now();
        let filename = format!(
            "{}-{}.json",
            timestamp.timestamp_millis(),
            uuid::Uuid::new_v4()
        );
        let filepath = self.message_dir.join(&filename);

        let content = serde_json::to_string_pretty(message)?;
        fs::write(&filepath, content)
            .await
            .context("Failed to persist message")?;

        // Clean up old messages (keep last 1000)
        self.cleanup_old_messages().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:update_status (lines 368-388) ---[0m
    pub async fn update_status(
        &self,
        agent_id: &str,
        status: &AgentStatus,
        additional_info: serde_json::Value,
    ) -> Result<()> {
        let filename = format!("{}.json", agent_id);
        let filepath = self.status_dir.join(&filename);

        let status_info = serde_json::json!({
            "agent_id": agent_id,
            "status": status,
            "timestamp": Utc::now(),
            "additional_info": additional_info,
        });

        let content = serde_json::to_string_pretty(&status_info)?;
        fs::write(&filepath, content).await?;

        Ok(())
    }

  ./crates/ccswarm/src/coordination/mod.rs:322-338 method get_pending_tasks <-> ./crates/ccswarm/src/coordination/mod.rs:427-445 function test_coordination_bus
  Similarity: 84.39%

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_pending_tasks (lines 322-338) ---[0m
    pub async fn get_pending_tasks(&self) -> Result<Vec<crate::agent::Task>> {
        let mut tasks: Vec<crate::agent::Task> = Vec::new();
        let mut entries = fs::read_dir(&self.task_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(task) = serde_json::from_str(&content) {
                    tasks.push(task);
                }
            }
        }

        // Sort by priority
        tasks.sort_by_key(|t| std::cmp::Reverse(t.priority));

        Ok(tasks)
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:test_coordination_bus (lines 427-445) ---[0m
    async fn test_coordination_bus() {
        let bus = CoordinationBus::new().await.unwrap();

        let message = AgentMessage::Heartbeat {
            agent_id: "test-agent".to_string(),
            timestamp: Utc::now(),
        };

        bus.send_message(message.clone()).await.unwrap();

        let received = bus.receive_message().await.unwrap();

        match received {
            AgentMessage::Heartbeat { agent_id, .. } => {
                assert_eq!(agent_id, "test-agent");
            }
            _ => panic!("Wrong message type received"),
        }
    }

  ./crates/ccswarm/src/coordination/mod.rs:158-172 method new <-> ./crates/ccswarm/src/coordination/mod.rs:427-445 function test_coordination_bus
  Similarity: 88.52%

[36m--- ./crates/ccswarm/src/coordination/mod.rs:new (lines 158-172) ---[0m
    pub async fn new() -> Result<Self> {
        let (sender, receiver) = async_channel::bounded(1000);
        let message_dir = PathBuf::from("coordination/messages");

        // Create message directory
        fs::create_dir_all(&message_dir)
            .await
            .context("Failed to create message directory")?;

        Ok(Self {
            sender,
            receiver,
            message_dir,
        })
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:test_coordination_bus (lines 427-445) ---[0m
    async fn test_coordination_bus() {
        let bus = CoordinationBus::new().await.unwrap();

        let message = AgentMessage::Heartbeat {
            agent_id: "test-agent".to_string(),
            timestamp: Utc::now(),
        };

        bus.send_message(message.clone()).await.unwrap();

        let received = bus.receive_message().await.unwrap();

        match received {
            AgentMessage::Heartbeat { agent_id, .. } => {
                assert_eq!(agent_id, "test-agent");
            }
            _ => panic!("Wrong message type received"),
        }
    }

  ./crates/ccswarm/src/coordination/mod.rs:254-274 method load_persisted_messages <-> ./crates/ccswarm/src/coordination/mod.rs:391-403 method get_status
  Similarity: 86.22%
  Classes: CoordinationBus <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:load_persisted_messages (lines 254-274) ---[0m
    pub async fn load_persisted_messages(&self) -> Result<Vec<AgentMessage>> {
        let mut messages = Vec::new();
        let mut entries = fs::read_dir(&self.message_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(message) = serde_json::from_str::<AgentMessage>(&content) {
                    messages.push(message);
                }
            }
        }

        // Sort by timestamp if available
        messages.sort_by_key(|m| match m {
            AgentMessage::Heartbeat { timestamp, .. } => *timestamp,
            AgentMessage::InterAgentMessage { timestamp, .. } => *timestamp,
            _ => Utc::now(),
        });

        Ok(messages)
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_status (lines 391-403) ---[0m
    pub async fn get_status(&self, agent_id: &str) -> Result<Option<serde_json::Value>> {
        let filename = format!("{}.json", agent_id);
        let filepath = self.status_dir.join(&filename);

        if !filepath.exists() {
            return Ok(None);
        }

        let content = fs::read_to_string(&filepath).await?;
        let status: serde_json::Value = serde_json::from_str(&content)?;

        Ok(Some(status))
    }

  ./crates/ccswarm/src/coordination/mod.rs:158-172 method new <-> ./crates/ccswarm/src/coordination/mod.rs:368-388 method update_status
  Similarity: 79.92%
  Classes: CoordinationBus <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:new (lines 158-172) ---[0m
    pub async fn new() -> Result<Self> {
        let (sender, receiver) = async_channel::bounded(1000);
        let message_dir = PathBuf::from("coordination/messages");

        // Create message directory
        fs::create_dir_all(&message_dir)
            .await
            .context("Failed to create message directory")?;

        Ok(Self {
            sender,
            receiver,
            message_dir,
        })
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:update_status (lines 368-388) ---[0m
    pub async fn update_status(
        &self,
        agent_id: &str,
        status: &AgentStatus,
        additional_info: serde_json::Value,
    ) -> Result<()> {
        let filename = format!("{}.json", agent_id);
        let filepath = self.status_dir.join(&filename);

        let status_info = serde_json::json!({
            "agent_id": agent_id,
            "status": status,
            "timestamp": Utc::now(),
            "additional_info": additional_info,
        });

        let content = serde_json::to_string_pretty(&status_info)?;
        fs::write(&filepath, content).await?;

        Ok(())
    }

  ./crates/ccswarm/src/coordination/mod.rs:322-338 method get_pending_tasks <-> ./crates/ccswarm/src/coordination/mod.rs:406-419 method get_all_statuses
  Similarity: 92.25%
  Classes: TaskQueue <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_pending_tasks (lines 322-338) ---[0m
    pub async fn get_pending_tasks(&self) -> Result<Vec<crate::agent::Task>> {
        let mut tasks: Vec<crate::agent::Task> = Vec::new();
        let mut entries = fs::read_dir(&self.task_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(task) = serde_json::from_str(&content) {
                    tasks.push(task);
                }
            }
        }

        // Sort by priority
        tasks.sort_by_key(|t| std::cmp::Reverse(t.priority));

        Ok(tasks)
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_all_statuses (lines 406-419) ---[0m
    pub async fn get_all_statuses(&self) -> Result<Vec<serde_json::Value>> {
        let mut statuses = Vec::new();
        let mut entries = fs::read_dir(&self.status_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(status) = serde_json::from_str(&content) {
                    statuses.push(status);
                }
            }
        }

        Ok(statuses)
    }

  ./crates/ccswarm/src/coordination/mod.rs:204-222 method persist_message <-> ./crates/ccswarm/src/coordination/mod.rs:322-338 method get_pending_tasks
  Similarity: 78.50%
  Classes: CoordinationBus <-> TaskQueue

[36m--- ./crates/ccswarm/src/coordination/mod.rs:persist_message (lines 204-222) ---[0m
    async fn persist_message(&self, message: &AgentMessage) -> Result<()> {
        let timestamp = Utc::now();
        let filename = format!(
            "{}-{}.json",
            timestamp.timestamp_millis(),
            uuid::Uuid::new_v4()
        );
        let filepath = self.message_dir.join(&filename);

        let content = serde_json::to_string_pretty(message)?;
        fs::write(&filepath, content)
            .await
            .context("Failed to persist message")?;

        // Clean up old messages (keep last 1000)
        self.cleanup_old_messages().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_pending_tasks (lines 322-338) ---[0m
    pub async fn get_pending_tasks(&self) -> Result<Vec<crate::agent::Task>> {
        let mut tasks: Vec<crate::agent::Task> = Vec::new();
        let mut entries = fs::read_dir(&self.task_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(task) = serde_json::from_str(&content) {
                    tasks.push(task);
                }
            }
        }

        // Sort by priority
        tasks.sort_by_key(|t| std::cmp::Reverse(t.priority));

        Ok(tasks)
    }

  ./crates/ccswarm/src/coordination/mod.rs:204-222 method persist_message <-> ./crates/ccswarm/src/coordination/mod.rs:427-445 function test_coordination_bus
  Similarity: 73.14%

[36m--- ./crates/ccswarm/src/coordination/mod.rs:persist_message (lines 204-222) ---[0m
    async fn persist_message(&self, message: &AgentMessage) -> Result<()> {
        let timestamp = Utc::now();
        let filename = format!(
            "{}-{}.json",
            timestamp.timestamp_millis(),
            uuid::Uuid::new_v4()
        );
        let filepath = self.message_dir.join(&filename);

        let content = serde_json::to_string_pretty(message)?;
        fs::write(&filepath, content)
            .await
            .context("Failed to persist message")?;

        // Clean up old messages (keep last 1000)
        self.cleanup_old_messages().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:test_coordination_bus (lines 427-445) ---[0m
    async fn test_coordination_bus() {
        let bus = CoordinationBus::new().await.unwrap();

        let message = AgentMessage::Heartbeat {
            agent_id: "test-agent".to_string(),
            timestamp: Utc::now(),
        };

        bus.send_message(message.clone()).await.unwrap();

        let received = bus.receive_message().await.unwrap();

        match received {
            AgentMessage::Heartbeat { agent_id, .. } => {
                assert_eq!(agent_id, "test-agent");
            }
            _ => panic!("Wrong message type received"),
        }
    }

  ./crates/ccswarm/src/coordination/mod.rs:368-388 method update_status <-> ./crates/ccswarm/src/coordination/mod.rs:391-403 method get_status
  Similarity: 80.90%
  Classes: StatusTracker <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:update_status (lines 368-388) ---[0m
    pub async fn update_status(
        &self,
        agent_id: &str,
        status: &AgentStatus,
        additional_info: serde_json::Value,
    ) -> Result<()> {
        let filename = format!("{}.json", agent_id);
        let filepath = self.status_dir.join(&filename);

        let status_info = serde_json::json!({
            "agent_id": agent_id,
            "status": status,
            "timestamp": Utc::now(),
            "additional_info": additional_info,
        });

        let content = serde_json::to_string_pretty(&status_info)?;
        fs::write(&filepath, content).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_status (lines 391-403) ---[0m
    pub async fn get_status(&self, agent_id: &str) -> Result<Option<serde_json::Value>> {
        let filename = format!("{}.json", agent_id);
        let filepath = self.status_dir.join(&filename);

        if !filepath.exists() {
            return Ok(None);
        }

        let content = fs::read_to_string(&filepath).await?;
        let status: serde_json::Value = serde_json::from_str(&content)?;

        Ok(Some(status))
    }

  ./crates/ccswarm/src/coordination/mod.rs:310-319 method add_task <-> ./crates/ccswarm/src/coordination/mod.rs:368-388 method update_status
  Similarity: 88.35%
  Classes: TaskQueue <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:add_task (lines 310-319) ---[0m
    pub async fn add_task(&self, task: &crate::agent::Task) -> Result<()> {
        let filename = format!("{}.json", task.id);
        let filepath = self.task_dir.join(&filename);

        let content = serde_json::to_string_pretty(task)?;
        fs::write(&filepath, content).await?;

        info!("Task {} added to queue", task.id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:update_status (lines 368-388) ---[0m
    pub async fn update_status(
        &self,
        agent_id: &str,
        status: &AgentStatus,
        additional_info: serde_json::Value,
    ) -> Result<()> {
        let filename = format!("{}.json", agent_id);
        let filepath = self.status_dir.join(&filename);

        let status_info = serde_json::json!({
            "agent_id": agent_id,
            "status": status,
            "timestamp": Utc::now(),
            "additional_info": additional_info,
        });

        let content = serde_json::to_string_pretty(&status_info)?;
        fs::write(&filepath, content).await?;

        Ok(())
    }

  ./crates/ccswarm/src/coordination/mod.rs:175-188 method send_message <-> ./crates/ccswarm/src/coordination/mod.rs:204-222 method persist_message
  Similarity: 76.54%
  Classes: CoordinationBus <-> CoordinationBus

[36m--- ./crates/ccswarm/src/coordination/mod.rs:send_message (lines 175-188) ---[0m
    pub async fn send_message(&self, message: AgentMessage) -> Result<()> {
        debug!("Sending message: {:?}", message);

        // Send through channel
        self.sender
            .send(message.clone())
            .await
            .context("Failed to send message through channel")?;

        // Also persist to disk for durability
        self.persist_message(&message).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:persist_message (lines 204-222) ---[0m
    async fn persist_message(&self, message: &AgentMessage) -> Result<()> {
        let timestamp = Utc::now();
        let filename = format!(
            "{}-{}.json",
            timestamp.timestamp_millis(),
            uuid::Uuid::new_v4()
        );
        let filepath = self.message_dir.join(&filename);

        let content = serde_json::to_string_pretty(message)?;
        fs::write(&filepath, content)
            .await
            .context("Failed to persist message")?;

        // Clean up old messages (keep last 1000)
        self.cleanup_old_messages().await?;

        Ok(())
    }

  ./crates/ccswarm/src/coordination/mod.rs:341-349 method remove_task <-> ./crates/ccswarm/src/coordination/mod.rs:368-388 method update_status
  Similarity: 83.83%
  Classes: TaskQueue <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:remove_task (lines 341-349) ---[0m
    pub async fn remove_task(&self, task_id: &str) -> Result<()> {
        let filename = format!("{}.json", task_id);
        let filepath = self.task_dir.join(&filename);

        fs::remove_file(&filepath).await?;
        info!("Task {} removed from queue", task_id);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:update_status (lines 368-388) ---[0m
    pub async fn update_status(
        &self,
        agent_id: &str,
        status: &AgentStatus,
        additional_info: serde_json::Value,
    ) -> Result<()> {
        let filename = format!("{}.json", agent_id);
        let filepath = self.status_dir.join(&filename);

        let status_info = serde_json::json!({
            "agent_id": agent_id,
            "status": status,
            "timestamp": Utc::now(),
            "additional_info": additional_info,
        });

        let content = serde_json::to_string_pretty(&status_info)?;
        fs::write(&filepath, content).await?;

        Ok(())
    }

  ./crates/ccswarm/src/coordination/mod.rs:406-419 method get_all_statuses <-> ./crates/ccswarm/src/coordination/mod.rs:427-445 function test_coordination_bus
  Similarity: 75.03%

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_all_statuses (lines 406-419) ---[0m
    pub async fn get_all_statuses(&self) -> Result<Vec<serde_json::Value>> {
        let mut statuses = Vec::new();
        let mut entries = fs::read_dir(&self.status_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(status) = serde_json::from_str(&content) {
                    statuses.push(status);
                }
            }
        }

        Ok(statuses)
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:test_coordination_bus (lines 427-445) ---[0m
    async fn test_coordination_bus() {
        let bus = CoordinationBus::new().await.unwrap();

        let message = AgentMessage::Heartbeat {
            agent_id: "test-agent".to_string(),
            timestamp: Utc::now(),
        };

        bus.send_message(message.clone()).await.unwrap();

        let received = bus.receive_message().await.unwrap();

        match received {
            AgentMessage::Heartbeat { agent_id, .. } => {
                assert_eq!(agent_id, "test-agent");
            }
            _ => panic!("Wrong message type received"),
        }
    }

  ./crates/ccswarm/src/coordination/mod.rs:322-338 method get_pending_tasks <-> ./crates/ccswarm/src/coordination/mod.rs:391-403 method get_status
  Similarity: 82.19%
  Classes: TaskQueue <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_pending_tasks (lines 322-338) ---[0m
    pub async fn get_pending_tasks(&self) -> Result<Vec<crate::agent::Task>> {
        let mut tasks: Vec<crate::agent::Task> = Vec::new();
        let mut entries = fs::read_dir(&self.task_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(task) = serde_json::from_str(&content) {
                    tasks.push(task);
                }
            }
        }

        // Sort by priority
        tasks.sort_by_key(|t| std::cmp::Reverse(t.priority));

        Ok(tasks)
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_status (lines 391-403) ---[0m
    pub async fn get_status(&self, agent_id: &str) -> Result<Option<serde_json::Value>> {
        let filename = format!("{}.json", agent_id);
        let filepath = self.status_dir.join(&filename);

        if !filepath.exists() {
            return Ok(None);
        }

        let content = fs::read_to_string(&filepath).await?;
        let status: serde_json::Value = serde_json::from_str(&content)?;

        Ok(Some(status))
    }

  ./crates/ccswarm/src/coordination/mod.rs:158-172 method new <-> ./crates/ccswarm/src/coordination/mod.rs:406-419 method get_all_statuses
  Similarity: 84.51%
  Classes: CoordinationBus <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:new (lines 158-172) ---[0m
    pub async fn new() -> Result<Self> {
        let (sender, receiver) = async_channel::bounded(1000);
        let message_dir = PathBuf::from("coordination/messages");

        // Create message directory
        fs::create_dir_all(&message_dir)
            .await
            .context("Failed to create message directory")?;

        Ok(Self {
            sender,
            receiver,
            message_dir,
        })
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_all_statuses (lines 406-419) ---[0m
    pub async fn get_all_statuses(&self) -> Result<Vec<serde_json::Value>> {
        let mut statuses = Vec::new();
        let mut entries = fs::read_dir(&self.status_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(status) = serde_json::from_str(&content) {
                    statuses.push(status);
                }
            }
        }

        Ok(statuses)
    }

  ./crates/ccswarm/src/coordination/mod.rs:158-172 method new <-> ./crates/ccswarm/src/coordination/mod.rs:204-222 method persist_message
  Similarity: 71.10%
  Classes: CoordinationBus <-> CoordinationBus

[36m--- ./crates/ccswarm/src/coordination/mod.rs:new (lines 158-172) ---[0m
    pub async fn new() -> Result<Self> {
        let (sender, receiver) = async_channel::bounded(1000);
        let message_dir = PathBuf::from("coordination/messages");

        // Create message directory
        fs::create_dir_all(&message_dir)
            .await
            .context("Failed to create message directory")?;

        Ok(Self {
            sender,
            receiver,
            message_dir,
        })
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:persist_message (lines 204-222) ---[0m
    async fn persist_message(&self, message: &AgentMessage) -> Result<()> {
        let timestamp = Utc::now();
        let filename = format!(
            "{}-{}.json",
            timestamp.timestamp_millis(),
            uuid::Uuid::new_v4()
        );
        let filepath = self.message_dir.join(&filename);

        let content = serde_json::to_string_pretty(message)?;
        fs::write(&filepath, content)
            .await
            .context("Failed to persist message")?;

        // Clean up old messages (keep last 1000)
        self.cleanup_old_messages().await?;

        Ok(())
    }

  ./crates/ccswarm/src/coordination/mod.rs:391-403 method get_status <-> ./crates/ccswarm/src/coordination/mod.rs:427-445 function test_coordination_bus
  Similarity: 73.55%

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_status (lines 391-403) ---[0m
    pub async fn get_status(&self, agent_id: &str) -> Result<Option<serde_json::Value>> {
        let filename = format!("{}.json", agent_id);
        let filepath = self.status_dir.join(&filename);

        if !filepath.exists() {
            return Ok(None);
        }

        let content = fs::read_to_string(&filepath).await?;
        let status: serde_json::Value = serde_json::from_str(&content)?;

        Ok(Some(status))
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:test_coordination_bus (lines 427-445) ---[0m
    async fn test_coordination_bus() {
        let bus = CoordinationBus::new().await.unwrap();

        let message = AgentMessage::Heartbeat {
            agent_id: "test-agent".to_string(),
            timestamp: Utc::now(),
        };

        bus.send_message(message.clone()).await.unwrap();

        let received = bus.receive_message().await.unwrap();

        match received {
            AgentMessage::Heartbeat { agent_id, .. } => {
                assert_eq!(agent_id, "test-agent");
            }
            _ => panic!("Wrong message type received"),
        }
    }

  ./crates/ccswarm/src/coordination/mod.rs:175-188 method send_message <-> ./crates/ccswarm/src/coordination/mod.rs:406-419 method get_all_statuses
  Similarity: 82.25%
  Classes: CoordinationBus <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:send_message (lines 175-188) ---[0m
    pub async fn send_message(&self, message: AgentMessage) -> Result<()> {
        debug!("Sending message: {:?}", message);

        // Send through channel
        self.sender
            .send(message.clone())
            .await
            .context("Failed to send message through channel")?;

        // Also persist to disk for durability
        self.persist_message(&message).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_all_statuses (lines 406-419) ---[0m
    pub async fn get_all_statuses(&self) -> Result<Vec<serde_json::Value>> {
        let mut statuses = Vec::new();
        let mut entries = fs::read_dir(&self.status_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(status) = serde_json::from_str(&content) {
                    statuses.push(status);
                }
            }
        }

        Ok(statuses)
    }

  ./crates/ccswarm/src/coordination/mod.rs:310-319 method add_task <-> ./crates/ccswarm/src/coordination/mod.rs:427-445 function test_coordination_bus
  Similarity: 78.88%

[36m--- ./crates/ccswarm/src/coordination/mod.rs:add_task (lines 310-319) ---[0m
    pub async fn add_task(&self, task: &crate::agent::Task) -> Result<()> {
        let filename = format!("{}.json", task.id);
        let filepath = self.task_dir.join(&filename);

        let content = serde_json::to_string_pretty(task)?;
        fs::write(&filepath, content).await?;

        info!("Task {} added to queue", task.id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:test_coordination_bus (lines 427-445) ---[0m
    async fn test_coordination_bus() {
        let bus = CoordinationBus::new().await.unwrap();

        let message = AgentMessage::Heartbeat {
            agent_id: "test-agent".to_string(),
            timestamp: Utc::now(),
        };

        bus.send_message(message.clone()).await.unwrap();

        let received = bus.receive_message().await.unwrap();

        match received {
            AgentMessage::Heartbeat { agent_id, .. } => {
                assert_eq!(agent_id, "test-agent");
            }
            _ => panic!("Wrong message type received"),
        }
    }

  ./crates/ccswarm/src/coordination/mod.rs:204-222 method persist_message <-> ./crates/ccswarm/src/coordination/mod.rs:310-319 method add_task
  Similarity: 78.38%
  Classes: CoordinationBus <-> TaskQueue

[36m--- ./crates/ccswarm/src/coordination/mod.rs:persist_message (lines 204-222) ---[0m
    async fn persist_message(&self, message: &AgentMessage) -> Result<()> {
        let timestamp = Utc::now();
        let filename = format!(
            "{}-{}.json",
            timestamp.timestamp_millis(),
            uuid::Uuid::new_v4()
        );
        let filepath = self.message_dir.join(&filename);

        let content = serde_json::to_string_pretty(message)?;
        fs::write(&filepath, content)
            .await
            .context("Failed to persist message")?;

        // Clean up old messages (keep last 1000)
        self.cleanup_old_messages().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:add_task (lines 310-319) ---[0m
    pub async fn add_task(&self, task: &crate::agent::Task) -> Result<()> {
        let filename = format!("{}.json", task.id);
        let filepath = self.task_dir.join(&filename);

        let content = serde_json::to_string_pretty(task)?;
        fs::write(&filepath, content).await?;

        info!("Task {} added to queue", task.id);
        Ok(())
    }

  ./crates/ccswarm/src/coordination/mod.rs:310-319 method add_task <-> ./crates/ccswarm/src/coordination/mod.rs:322-338 method get_pending_tasks
  Similarity: 81.18%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/coordination/mod.rs:add_task (lines 310-319) ---[0m
    pub async fn add_task(&self, task: &crate::agent::Task) -> Result<()> {
        let filename = format!("{}.json", task.id);
        let filepath = self.task_dir.join(&filename);

        let content = serde_json::to_string_pretty(task)?;
        fs::write(&filepath, content).await?;

        info!("Task {} added to queue", task.id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_pending_tasks (lines 322-338) ---[0m
    pub async fn get_pending_tasks(&self) -> Result<Vec<crate::agent::Task>> {
        let mut tasks: Vec<crate::agent::Task> = Vec::new();
        let mut entries = fs::read_dir(&self.task_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(task) = serde_json::from_str(&content) {
                    tasks.push(task);
                }
            }
        }

        // Sort by priority
        tasks.sort_by_key(|t| std::cmp::Reverse(t.priority));

        Ok(tasks)
    }

  ./crates/ccswarm/src/coordination/mod.rs:158-172 method new <-> ./crates/ccswarm/src/coordination/mod.rs:175-188 method send_message
  Similarity: 74.71%
  Classes: CoordinationBus <-> CoordinationBus

[36m--- ./crates/ccswarm/src/coordination/mod.rs:new (lines 158-172) ---[0m
    pub async fn new() -> Result<Self> {
        let (sender, receiver) = async_channel::bounded(1000);
        let message_dir = PathBuf::from("coordination/messages");

        // Create message directory
        fs::create_dir_all(&message_dir)
            .await
            .context("Failed to create message directory")?;

        Ok(Self {
            sender,
            receiver,
            message_dir,
        })
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:send_message (lines 175-188) ---[0m
    pub async fn send_message(&self, message: AgentMessage) -> Result<()> {
        debug!("Sending message: {:?}", message);

        // Send through channel
        self.sender
            .send(message.clone())
            .await
            .context("Failed to send message through channel")?;

        // Also persist to disk for durability
        self.persist_message(&message).await?;

        Ok(())
    }

  ./crates/ccswarm/src/coordination/mod.rs:175-188 method send_message <-> ./crates/ccswarm/src/coordination/mod.rs:391-403 method get_status
  Similarity: 77.29%
  Classes: CoordinationBus <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:send_message (lines 175-188) ---[0m
    pub async fn send_message(&self, message: AgentMessage) -> Result<()> {
        debug!("Sending message: {:?}", message);

        // Send through channel
        self.sender
            .send(message.clone())
            .await
            .context("Failed to send message through channel")?;

        // Also persist to disk for durability
        self.persist_message(&message).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_status (lines 391-403) ---[0m
    pub async fn get_status(&self, agent_id: &str) -> Result<Option<serde_json::Value>> {
        let filename = format!("{}.json", agent_id);
        let filepath = self.status_dir.join(&filename);

        if !filepath.exists() {
            return Ok(None);
        }

        let content = fs::read_to_string(&filepath).await?;
        let status: serde_json::Value = serde_json::from_str(&content)?;

        Ok(Some(status))
    }

  ./crates/ccswarm/src/coordination/mod.rs:204-222 method persist_message <-> ./crates/ccswarm/src/coordination/mod.rs:341-349 method remove_task
  Similarity: 71.91%
  Classes: CoordinationBus <-> TaskQueue

[36m--- ./crates/ccswarm/src/coordination/mod.rs:persist_message (lines 204-222) ---[0m
    async fn persist_message(&self, message: &AgentMessage) -> Result<()> {
        let timestamp = Utc::now();
        let filename = format!(
            "{}-{}.json",
            timestamp.timestamp_millis(),
            uuid::Uuid::new_v4()
        );
        let filepath = self.message_dir.join(&filename);

        let content = serde_json::to_string_pretty(message)?;
        fs::write(&filepath, content)
            .await
            .context("Failed to persist message")?;

        // Clean up old messages (keep last 1000)
        self.cleanup_old_messages().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:remove_task (lines 341-349) ---[0m
    pub async fn remove_task(&self, task_id: &str) -> Result<()> {
        let filename = format!("{}.json", task_id);
        let filepath = self.task_dir.join(&filename);

        fs::remove_file(&filepath).await?;
        info!("Task {} removed from queue", task_id);

        Ok(())
    }

  ./crates/ccswarm/src/coordination/mod.rs:175-188 method send_message <-> ./crates/ccswarm/src/coordination/mod.rs:310-319 method add_task
  Similarity: 81.92%
  Classes: CoordinationBus <-> TaskQueue

[36m--- ./crates/ccswarm/src/coordination/mod.rs:send_message (lines 175-188) ---[0m
    pub async fn send_message(&self, message: AgentMessage) -> Result<()> {
        debug!("Sending message: {:?}", message);

        // Send through channel
        self.sender
            .send(message.clone())
            .await
            .context("Failed to send message through channel")?;

        // Also persist to disk for durability
        self.persist_message(&message).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:add_task (lines 310-319) ---[0m
    pub async fn add_task(&self, task: &crate::agent::Task) -> Result<()> {
        let filename = format!("{}.json", task.id);
        let filepath = self.task_dir.join(&filename);

        let content = serde_json::to_string_pretty(task)?;
        fs::write(&filepath, content).await?;

        info!("Task {} added to queue", task.id);
        Ok(())
    }

  ./crates/ccswarm/src/coordination/mod.rs:158-172 method new <-> ./crates/ccswarm/src/coordination/mod.rs:341-349 method remove_task
  Similarity: 81.61%
  Classes: CoordinationBus <-> TaskQueue

[36m--- ./crates/ccswarm/src/coordination/mod.rs:new (lines 158-172) ---[0m
    pub async fn new() -> Result<Self> {
        let (sender, receiver) = async_channel::bounded(1000);
        let message_dir = PathBuf::from("coordination/messages");

        // Create message directory
        fs::create_dir_all(&message_dir)
            .await
            .context("Failed to create message directory")?;

        Ok(Self {
            sender,
            receiver,
            message_dir,
        })
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:remove_task (lines 341-349) ---[0m
    pub async fn remove_task(&self, task_id: &str) -> Result<()> {
        let filename = format!("{}.json", task_id);
        let filepath = self.task_dir.join(&filename);

        fs::remove_file(&filepath).await?;
        info!("Task {} removed from queue", task_id);

        Ok(())
    }

  ./crates/ccswarm/src/coordination/mod.rs:341-349 method remove_task <-> ./crates/ccswarm/src/coordination/mod.rs:406-419 method get_all_statuses
  Similarity: 82.32%
  Classes: TaskQueue <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:remove_task (lines 341-349) ---[0m
    pub async fn remove_task(&self, task_id: &str) -> Result<()> {
        let filename = format!("{}.json", task_id);
        let filepath = self.task_dir.join(&filename);

        fs::remove_file(&filepath).await?;
        info!("Task {} removed from queue", task_id);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_all_statuses (lines 406-419) ---[0m
    pub async fn get_all_statuses(&self) -> Result<Vec<serde_json::Value>> {
        let mut statuses = Vec::new();
        let mut entries = fs::read_dir(&self.status_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(status) = serde_json::from_str(&content) {
                    statuses.push(status);
                }
            }
        }

        Ok(statuses)
    }

  ./crates/ccswarm/src/coordination/mod.rs:175-188 method send_message <-> ./crates/ccswarm/src/coordination/mod.rs:341-349 method remove_task
  Similarity: 79.51%
  Classes: CoordinationBus <-> TaskQueue

[36m--- ./crates/ccswarm/src/coordination/mod.rs:send_message (lines 175-188) ---[0m
    pub async fn send_message(&self, message: AgentMessage) -> Result<()> {
        debug!("Sending message: {:?}", message);

        // Send through channel
        self.sender
            .send(message.clone())
            .await
            .context("Failed to send message through channel")?;

        // Also persist to disk for durability
        self.persist_message(&message).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:remove_task (lines 341-349) ---[0m
    pub async fn remove_task(&self, task_id: &str) -> Result<()> {
        let filename = format!("{}.json", task_id);
        let filepath = self.task_dir.join(&filename);

        fs::remove_file(&filepath).await?;
        info!("Task {} removed from queue", task_id);

        Ok(())
    }

  ./crates/ccswarm/src/coordination/mod.rs:158-172 method new <-> ./crates/ccswarm/src/coordination/mod.rs:310-319 method add_task
  Similarity: 71.82%
  Classes: CoordinationBus <-> TaskQueue

[36m--- ./crates/ccswarm/src/coordination/mod.rs:new (lines 158-172) ---[0m
    pub async fn new() -> Result<Self> {
        let (sender, receiver) = async_channel::bounded(1000);
        let message_dir = PathBuf::from("coordination/messages");

        // Create message directory
        fs::create_dir_all(&message_dir)
            .await
            .context("Failed to create message directory")?;

        Ok(Self {
            sender,
            receiver,
            message_dir,
        })
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:add_task (lines 310-319) ---[0m
    pub async fn add_task(&self, task: &crate::agent::Task) -> Result<()> {
        let filename = format!("{}.json", task.id);
        let filepath = self.task_dir.join(&filename);

        let content = serde_json::to_string_pretty(task)?;
        fs::write(&filepath, content).await?;

        info!("Task {} added to queue", task.id);
        Ok(())
    }

  ./crates/ccswarm/src/coordination/mod.rs:310-319 method add_task <-> ./crates/ccswarm/src/coordination/mod.rs:391-403 method get_status
  Similarity: 75.51%
  Classes: TaskQueue <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:add_task (lines 310-319) ---[0m
    pub async fn add_task(&self, task: &crate::agent::Task) -> Result<()> {
        let filename = format!("{}.json", task.id);
        let filepath = self.task_dir.join(&filename);

        let content = serde_json::to_string_pretty(task)?;
        fs::write(&filepath, content).await?;

        info!("Task {} added to queue", task.id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_status (lines 391-403) ---[0m
    pub async fn get_status(&self, agent_id: &str) -> Result<Option<serde_json::Value>> {
        let filename = format!("{}.json", agent_id);
        let filepath = self.status_dir.join(&filename);

        if !filepath.exists() {
            return Ok(None);
        }

        let content = fs::read_to_string(&filepath).await?;
        let status: serde_json::Value = serde_json::from_str(&content)?;

        Ok(Some(status))
    }

  ./crates/ccswarm/src/coordination/mod.rs:310-319 method add_task <-> ./crates/ccswarm/src/coordination/mod.rs:406-419 method get_all_statuses
  Similarity: 71.06%
  Classes: TaskQueue <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:add_task (lines 310-319) ---[0m
    pub async fn add_task(&self, task: &crate::agent::Task) -> Result<()> {
        let filename = format!("{}.json", task.id);
        let filepath = self.task_dir.join(&filename);

        let content = serde_json::to_string_pretty(task)?;
        fs::write(&filepath, content).await?;

        info!("Task {} added to queue", task.id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_all_statuses (lines 406-419) ---[0m
    pub async fn get_all_statuses(&self) -> Result<Vec<serde_json::Value>> {
        let mut statuses = Vec::new();
        let mut entries = fs::read_dir(&self.status_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if let Ok(content) = fs::read_to_string(entry.path()).await {
                if let Ok(status) = serde_json::from_str(&content) {
                    statuses.push(status);
                }
            }
        }

        Ok(statuses)
    }

  ./crates/ccswarm/src/coordination/mod.rs:341-349 method remove_task <-> ./crates/ccswarm/src/coordination/mod.rs:391-403 method get_status
  Similarity: 73.31%
  Classes: TaskQueue <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:remove_task (lines 341-349) ---[0m
    pub async fn remove_task(&self, task_id: &str) -> Result<()> {
        let filename = format!("{}.json", task_id);
        let filepath = self.task_dir.join(&filename);

        fs::remove_file(&filepath).await?;
        info!("Task {} removed from queue", task_id);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:get_status (lines 391-403) ---[0m
    pub async fn get_status(&self, agent_id: &str) -> Result<Option<serde_json::Value>> {
        let filename = format!("{}.json", agent_id);
        let filepath = self.status_dir.join(&filename);

        if !filepath.exists() {
            return Ok(None);
        }

        let content = fs::read_to_string(&filepath).await?;
        let status: serde_json::Value = serde_json::from_str(&content)?;

        Ok(Some(status))
    }

  ./crates/ccswarm/src/coordination/mod.rs:310-319 method add_task <-> ./crates/ccswarm/src/coordination/mod.rs:341-349 method remove_task
  Similarity: 78.59%
  Classes: TaskQueue <-> TaskQueue

[36m--- ./crates/ccswarm/src/coordination/mod.rs:add_task (lines 310-319) ---[0m
    pub async fn add_task(&self, task: &crate::agent::Task) -> Result<()> {
        let filename = format!("{}.json", task.id);
        let filepath = self.task_dir.join(&filename);

        let content = serde_json::to_string_pretty(task)?;
        fs::write(&filepath, content).await?;

        info!("Task {} added to queue", task.id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:remove_task (lines 341-349) ---[0m
    pub async fn remove_task(&self, task_id: &str) -> Result<()> {
        let filename = format!("{}.json", task_id);
        let filepath = self.task_dir.join(&filename);

        fs::remove_file(&filepath).await?;
        info!("Task {} removed from queue", task_id);

        Ok(())
    }

  ./crates/ccswarm/src/coordination/mod.rs:302-307 method with_dir <-> ./crates/ccswarm/src/coordination/mod.rs:360-365 method new
  Similarity: 96.04%
  Classes: TaskQueue <-> StatusTracker

[36m--- ./crates/ccswarm/src/coordination/mod.rs:with_dir (lines 302-307) ---[0m
    pub async fn with_dir(dir: &str) -> Result<Self> {
        let task_dir = PathBuf::from(dir);
        fs::create_dir_all(&task_dir).await?;

        Ok(Self { task_dir })
    }

[36m--- ./crates/ccswarm/src/coordination/mod.rs:new (lines 360-365) ---[0m
    pub async fn new() -> Result<Self> {
        let status_dir = PathBuf::from("coordination/agent-status");
        fs::create_dir_all(&status_dir).await?;

        Ok(Self { status_dir })
    }


Duplicates in ./crates/ccswarm/src/streaming/mod.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/streaming/mod.rs:148-174 method create_display_prefix <-> ./crates/ccswarm/src/streaming/mod.rs:359-382 method start
  Similarity: 78.11%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:create_display_prefix (lines 148-174) ---[0m
    fn create_display_prefix(&self, entry: &OutputEntry) -> String {
        let time_str = entry.timestamp.format("%H:%M:%S").to_string();
        let type_icon = match entry.output_type {
            OutputType::Error => "❌",
            OutputType::Warning => "⚠️",
            OutputType::Info => "ℹ️",
            OutputType::Debug => "🔍",
            OutputType::System => "⚙️",
            OutputType::Stdout => "📤",
            OutputType::Stderr => "📥",
        };

        let task_suffix = entry
            .task_id
            .as_ref()
            .map(|t| format!(" [{}]", &t[..8.min(t.len())]))
            .unwrap_or_default();

        format!(
            "[{}] {} {} {}{}",
            time_str,
            type_icon,
            entry.agent_type,
            &entry.agent_id[..8.min(entry.agent_id.len())],
            task_suffix
        )
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:start (lines 359-382) ---[0m
    pub async fn start(&self) -> Result<(), String> {
        // Initialize global receiver
        {
            let mut receiver = self
                .global_receiver
                .write()
                .map_err(|e| format!("Failed to lock global receiver: {}", e))?;
            *receiver = Some(self.monitoring.subscribe_global());
        }

        // Start the main streaming loop
        let streaming_manager = self.clone();
        tokio::spawn(async move {
            streaming_manager.run_streaming_loop().await;
        });

        // Start statistics updater
        let stats_manager = self.clone();
        tokio::spawn(async move {
            stats_manager.run_stats_updater().await;
        });

        Ok(())
    }

  ./crates/ccswarm/src/streaming/mod.rs:176-198 method format_content <-> ./crates/ccswarm/src/streaming/mod.rs:359-382 method start
  Similarity: 84.41%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_content (lines 176-198) ---[0m
    fn format_content(&self, content: &str) -> String {
        let mut formatted = content.to_string();

        // Truncate if too long
        if formatted.len() > self.config.max_line_length {
            formatted.truncate(self.config.max_line_length - 3);
            formatted.push_str("...");
        }

        // Replace tabs with spaces for consistent display
        formatted = formatted.replace('\t', "    ");

        // Handle multi-line content
        if formatted.contains('\n') {
            formatted = formatted
                .lines()
                .map(|line| format!("  {}", line))
                .collect::<Vec<_>>()
                .join("\n");
        }

        formatted
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:start (lines 359-382) ---[0m
    pub async fn start(&self) -> Result<(), String> {
        // Initialize global receiver
        {
            let mut receiver = self
                .global_receiver
                .write()
                .map_err(|e| format!("Failed to lock global receiver: {}", e))?;
            *receiver = Some(self.monitoring.subscribe_global());
        }

        // Start the main streaming loop
        let streaming_manager = self.clone();
        tokio::spawn(async move {
            streaming_manager.run_streaming_loop().await;
        });

        // Start statistics updater
        let stats_manager = self.clone();
        tokio::spawn(async move {
            stats_manager.run_stats_updater().await;
        });

        Ok(())
    }

  ./crates/ccswarm/src/streaming/mod.rs:148-174 method create_display_prefix <-> ./crates/ccswarm/src/streaming/mod.rs:468-486 method run_streaming_loop
  Similarity: 86.18%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:create_display_prefix (lines 148-174) ---[0m
    fn create_display_prefix(&self, entry: &OutputEntry) -> String {
        let time_str = entry.timestamp.format("%H:%M:%S").to_string();
        let type_icon = match entry.output_type {
            OutputType::Error => "❌",
            OutputType::Warning => "⚠️",
            OutputType::Info => "ℹ️",
            OutputType::Debug => "🔍",
            OutputType::System => "⚙️",
            OutputType::Stdout => "📤",
            OutputType::Stderr => "📥",
        };

        let task_suffix = entry
            .task_id
            .as_ref()
            .map(|t| format!(" [{}]", &t[..8.min(t.len())]))
            .unwrap_or_default();

        format!(
            "[{}] {} {} {}{}",
            time_str,
            type_icon,
            entry.agent_type,
            &entry.agent_id[..8.min(entry.agent_id.len())],
            task_suffix
        )
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:run_streaming_loop (lines 468-486) ---[0m
    async fn run_streaming_loop(&self) {
        // Get the receiver once at startup
        let mut receiver = {
            let global_receiver = match self.global_receiver.read() {
                Ok(gr) => gr,
                Err(_) => return,
            };

            match global_receiver.as_ref() {
                Some(r) => r.resubscribe(),
                None => return,
            }
        };

        // Main loop to process messages
        while let Ok(entry) = receiver.recv().await {
            self.distribute_entry(entry).await;
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:148-174 method create_display_prefix <-> ./crates/ccswarm/src/streaming/mod.rs:176-198 method format_content
  Similarity: 77.00%
  Classes: OutputFormatter <-> OutputFormatter

[36m--- ./crates/ccswarm/src/streaming/mod.rs:create_display_prefix (lines 148-174) ---[0m
    fn create_display_prefix(&self, entry: &OutputEntry) -> String {
        let time_str = entry.timestamp.format("%H:%M:%S").to_string();
        let type_icon = match entry.output_type {
            OutputType::Error => "❌",
            OutputType::Warning => "⚠️",
            OutputType::Info => "ℹ️",
            OutputType::Debug => "🔍",
            OutputType::System => "⚙️",
            OutputType::Stdout => "📤",
            OutputType::Stderr => "📥",
        };

        let task_suffix = entry
            .task_id
            .as_ref()
            .map(|t| format!(" [{}]", &t[..8.min(t.len())]))
            .unwrap_or_default();

        format!(
            "[{}] {} {} {}{}",
            time_str,
            type_icon,
            entry.agent_type,
            &entry.agent_id[..8.min(entry.agent_id.len())],
            task_suffix
        )
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_content (lines 176-198) ---[0m
    fn format_content(&self, content: &str) -> String {
        let mut formatted = content.to_string();

        // Truncate if too long
        if formatted.len() > self.config.max_line_length {
            formatted.truncate(self.config.max_line_length - 3);
            formatted.push_str("...");
        }

        // Replace tabs with spaces for consistent display
        formatted = formatted.replace('\t', "    ");

        // Handle multi-line content
        if formatted.contains('\n') {
            formatted = formatted
                .lines()
                .map(|line| format!("  {}", line))
                .collect::<Vec<_>>()
                .join("\n");
        }

        formatted
    }

  ./crates/ccswarm/src/streaming/mod.rs:468-486 method run_streaming_loop <-> ./crates/ccswarm/src/streaming/mod.rs:518-541 method update_stats
  Similarity: 88.98%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:run_streaming_loop (lines 468-486) ---[0m
    async fn run_streaming_loop(&self) {
        // Get the receiver once at startup
        let mut receiver = {
            let global_receiver = match self.global_receiver.read() {
                Ok(gr) => gr,
                Err(_) => return,
            };

            match global_receiver.as_ref() {
                Some(r) => r.resubscribe(),
                None => return,
            }
        };

        // Main loop to process messages
        while let Ok(entry) = receiver.recv().await {
            self.distribute_entry(entry).await;
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:update_stats (lines 518-541) ---[0m
    fn update_stats(&self) {
        let subscriptions = match self.subscriptions.read() {
            Ok(s) => s,
            Err(_) => return,
        };

        let subscription_details: Vec<SubscriptionStats> = subscriptions
            .values()
            .map(|sub| SubscriptionStats {
                id: sub.id.clone(),
                agent_id: sub.agent_id.clone(),
                messages_received: 0, // This would need to be tracked separately
                last_message_at: None, // This would need to be tracked separately
                created_at: sub.created_at,
            })
            .collect();

        if let Ok(mut stats) = self.stats.write() {
            stats.active_subscriptions = subscriptions.len();
            stats.subscription_details = subscription_details;
            // messages_per_second and average_latency_ms would be calculated
            // based on additional tracking that would be implemented
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:148-174 method create_display_prefix <-> ./crates/ccswarm/src/streaming/mod.rs:518-541 method update_stats
  Similarity: 73.73%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:create_display_prefix (lines 148-174) ---[0m
    fn create_display_prefix(&self, entry: &OutputEntry) -> String {
        let time_str = entry.timestamp.format("%H:%M:%S").to_string();
        let type_icon = match entry.output_type {
            OutputType::Error => "❌",
            OutputType::Warning => "⚠️",
            OutputType::Info => "ℹ️",
            OutputType::Debug => "🔍",
            OutputType::System => "⚙️",
            OutputType::Stdout => "📤",
            OutputType::Stderr => "📥",
        };

        let task_suffix = entry
            .task_id
            .as_ref()
            .map(|t| format!(" [{}]", &t[..8.min(t.len())]))
            .unwrap_or_default();

        format!(
            "[{}] {} {} {}{}",
            time_str,
            type_icon,
            entry.agent_type,
            &entry.agent_id[..8.min(entry.agent_id.len())],
            task_suffix
        )
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:update_stats (lines 518-541) ---[0m
    fn update_stats(&self) {
        let subscriptions = match self.subscriptions.read() {
            Ok(s) => s,
            Err(_) => return,
        };

        let subscription_details: Vec<SubscriptionStats> = subscriptions
            .values()
            .map(|sub| SubscriptionStats {
                id: sub.id.clone(),
                agent_id: sub.agent_id.clone(),
                messages_received: 0, // This would need to be tracked separately
                last_message_at: None, // This would need to be tracked separately
                created_at: sub.created_at,
            })
            .collect();

        if let Ok(mut stats) = self.stats.write() {
            stats.active_subscriptions = subscriptions.len();
            stats.subscription_details = subscription_details;
            // messages_per_second and average_latency_ms would be calculated
            // based on additional tracking that would be implemented
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:148-174 method create_display_prefix <-> ./crates/ccswarm/src/streaming/mod.rs:338-356 method new
  Similarity: 77.98%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:create_display_prefix (lines 148-174) ---[0m
    fn create_display_prefix(&self, entry: &OutputEntry) -> String {
        let time_str = entry.timestamp.format("%H:%M:%S").to_string();
        let type_icon = match entry.output_type {
            OutputType::Error => "❌",
            OutputType::Warning => "⚠️",
            OutputType::Info => "ℹ️",
            OutputType::Debug => "🔍",
            OutputType::System => "⚙️",
            OutputType::Stdout => "📤",
            OutputType::Stderr => "📥",
        };

        let task_suffix = entry
            .task_id
            .as_ref()
            .map(|t| format!(" [{}]", &t[..8.min(t.len())]))
            .unwrap_or_default();

        format!(
            "[{}] {} {} {}{}",
            time_str,
            type_icon,
            entry.agent_type,
            &entry.agent_id[..8.min(entry.agent_id.len())],
            task_suffix
        )
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:new (lines 338-356) ---[0m
    pub fn new(monitoring: Arc<MonitoringSystem>, config: StreamConfig) -> Self {
        let formatter = OutputFormatter::new(config.clone());
        let stats = Arc::new(RwLock::new(StreamingStats {
            active_subscriptions: 0,
            total_messages_sent: 0,
            messages_per_second: 0.0,
            average_latency_ms: 0.0,
            subscription_details: Vec::new(),
        }));

        Self {
            monitoring,
            subscriptions: Arc::new(RwLock::new(HashMap::new())),
            formatter,
            config,
            stats,
            global_receiver: Arc::new(RwLock::new(None)),
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:69-85 method should_receive <-> ./crates/ccswarm/src/streaming/mod.rs:176-198 method format_content
  Similarity: 89.33%
  Classes: StreamSubscription <-> OutputFormatter

[36m--- ./crates/ccswarm/src/streaming/mod.rs:should_receive (lines 69-85) ---[0m
    pub fn should_receive(&self, entry: &OutputEntry) -> bool {
        // Check agent filter
        if let Some(ref agent_id) = self.agent_id {
            if entry.agent_id != *agent_id {
                return false;
            }
        }

        // Check output filter
        if let Some(ref filter) = self.filter {
            if !entry.matches_filter(filter) {
                return false;
            }
        }

        true
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_content (lines 176-198) ---[0m
    fn format_content(&self, content: &str) -> String {
        let mut formatted = content.to_string();

        // Truncate if too long
        if formatted.len() > self.config.max_line_length {
            formatted.truncate(self.config.max_line_length - 3);
            formatted.push_str("...");
        }

        // Replace tabs with spaces for consistent display
        formatted = formatted.replace('\t', "    ");

        // Handle multi-line content
        if formatted.contains('\n') {
            formatted = formatted
                .lines()
                .map(|line| format!("  {}", line))
                .collect::<Vec<_>>()
                .join("\n");
        }

        formatted
    }

  ./crates/ccswarm/src/streaming/mod.rs:148-174 method create_display_prefix <-> ./crates/ccswarm/src/streaming/mod.rs:489-506 method distribute_entry
  Similarity: 78.97%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:create_display_prefix (lines 148-174) ---[0m
    fn create_display_prefix(&self, entry: &OutputEntry) -> String {
        let time_str = entry.timestamp.format("%H:%M:%S").to_string();
        let type_icon = match entry.output_type {
            OutputType::Error => "❌",
            OutputType::Warning => "⚠️",
            OutputType::Info => "ℹ️",
            OutputType::Debug => "🔍",
            OutputType::System => "⚙️",
            OutputType::Stdout => "📤",
            OutputType::Stderr => "📥",
        };

        let task_suffix = entry
            .task_id
            .as_ref()
            .map(|t| format!(" [{}]", &t[..8.min(t.len())]))
            .unwrap_or_default();

        format!(
            "[{}] {} {} {}{}",
            time_str,
            type_icon,
            entry.agent_type,
            &entry.agent_id[..8.min(entry.agent_id.len())],
            task_suffix
        )
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:distribute_entry (lines 489-506) ---[0m
    async fn distribute_entry(&self, entry: OutputEntry) {
        let subscriptions = match self.subscriptions.read() {
            Ok(s) => s,
            Err(_) => return,
        };

        let mut sent_count = 0;
        for subscription in subscriptions.values() {
            if subscription.should_receive(&entry) && subscription.send(entry.clone()).is_ok() {
                sent_count += 1;
            }
        }

        // Update statistics
        if let Ok(mut stats) = self.stats.write() {
            stats.total_messages_sent += sent_count;
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:338-356 method new <-> ./crates/ccswarm/src/streaming/mod.rs:518-541 method update_stats
  Similarity: 82.62%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:new (lines 338-356) ---[0m
    pub fn new(monitoring: Arc<MonitoringSystem>, config: StreamConfig) -> Self {
        let formatter = OutputFormatter::new(config.clone());
        let stats = Arc::new(RwLock::new(StreamingStats {
            active_subscriptions: 0,
            total_messages_sent: 0,
            messages_per_second: 0.0,
            average_latency_ms: 0.0,
            subscription_details: Vec::new(),
        }));

        Self {
            monitoring,
            subscriptions: Arc::new(RwLock::new(HashMap::new())),
            formatter,
            config,
            stats,
            global_receiver: Arc::new(RwLock::new(None)),
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:update_stats (lines 518-541) ---[0m
    fn update_stats(&self) {
        let subscriptions = match self.subscriptions.read() {
            Ok(s) => s,
            Err(_) => return,
        };

        let subscription_details: Vec<SubscriptionStats> = subscriptions
            .values()
            .map(|sub| SubscriptionStats {
                id: sub.id.clone(),
                agent_id: sub.agent_id.clone(),
                messages_received: 0, // This would need to be tracked separately
                last_message_at: None, // This would need to be tracked separately
                created_at: sub.created_at,
            })
            .collect();

        if let Ok(mut stats) = self.stats.write() {
            stats.active_subscriptions = subscriptions.len();
            stats.subscription_details = subscription_details;
            // messages_per_second and average_latency_ms would be calculated
            // based on additional tracking that would be implemented
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:176-198 method format_content <-> ./crates/ccswarm/src/streaming/mod.rs:468-486 method run_streaming_loop
  Similarity: 83.30%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_content (lines 176-198) ---[0m
    fn format_content(&self, content: &str) -> String {
        let mut formatted = content.to_string();

        // Truncate if too long
        if formatted.len() > self.config.max_line_length {
            formatted.truncate(self.config.max_line_length - 3);
            formatted.push_str("...");
        }

        // Replace tabs with spaces for consistent display
        formatted = formatted.replace('\t', "    ");

        // Handle multi-line content
        if formatted.contains('\n') {
            formatted = formatted
                .lines()
                .map(|line| format!("  {}", line))
                .collect::<Vec<_>>()
                .join("\n");
        }

        formatted
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:run_streaming_loop (lines 468-486) ---[0m
    async fn run_streaming_loop(&self) {
        // Get the receiver once at startup
        let mut receiver = {
            let global_receiver = match self.global_receiver.read() {
                Ok(gr) => gr,
                Err(_) => return,
            };

            match global_receiver.as_ref() {
                Some(r) => r.resubscribe(),
                None => return,
            }
        };

        // Main loop to process messages
        while let Ok(entry) = receiver.recv().await {
            self.distribute_entry(entry).await;
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:176-198 method format_content <-> ./crates/ccswarm/src/streaming/mod.rs:489-506 method distribute_entry
  Similarity: 84.13%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_content (lines 176-198) ---[0m
    fn format_content(&self, content: &str) -> String {
        let mut formatted = content.to_string();

        // Truncate if too long
        if formatted.len() > self.config.max_line_length {
            formatted.truncate(self.config.max_line_length - 3);
            formatted.push_str("...");
        }

        // Replace tabs with spaces for consistent display
        formatted = formatted.replace('\t', "    ");

        // Handle multi-line content
        if formatted.contains('\n') {
            formatted = formatted
                .lines()
                .map(|line| format!("  {}", line))
                .collect::<Vec<_>>()
                .join("\n");
        }

        formatted
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:distribute_entry (lines 489-506) ---[0m
    async fn distribute_entry(&self, entry: OutputEntry) {
        let subscriptions = match self.subscriptions.read() {
            Ok(s) => s,
            Err(_) => return,
        };

        let mut sent_count = 0;
        for subscription in subscriptions.values() {
            if subscription.should_receive(&entry) && subscription.send(entry.clone()).is_ok() {
                sent_count += 1;
            }
        }

        // Update statistics
        if let Ok(mut stats) = self.stats.write() {
            stats.total_messages_sent += sent_count;
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:489-506 method distribute_entry <-> ./crates/ccswarm/src/streaming/mod.rs:518-541 method update_stats
  Similarity: 81.28%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:distribute_entry (lines 489-506) ---[0m
    async fn distribute_entry(&self, entry: OutputEntry) {
        let subscriptions = match self.subscriptions.read() {
            Ok(s) => s,
            Err(_) => return,
        };

        let mut sent_count = 0;
        for subscription in subscriptions.values() {
            if subscription.should_receive(&entry) && subscription.send(entry.clone()).is_ok() {
                sent_count += 1;
            }
        }

        // Update statistics
        if let Ok(mut stats) = self.stats.write() {
            stats.total_messages_sent += sent_count;
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:update_stats (lines 518-541) ---[0m
    fn update_stats(&self) {
        let subscriptions = match self.subscriptions.read() {
            Ok(s) => s,
            Err(_) => return,
        };

        let subscription_details: Vec<SubscriptionStats> = subscriptions
            .values()
            .map(|sub| SubscriptionStats {
                id: sub.id.clone(),
                agent_id: sub.agent_id.clone(),
                messages_received: 0, // This would need to be tracked separately
                last_message_at: None, // This would need to be tracked separately
                created_at: sub.created_at,
            })
            .collect();

        if let Ok(mut stats) = self.stats.write() {
            stats.active_subscriptions = subscriptions.len();
            stats.subscription_details = subscription_details;
            // messages_per_second and average_latency_ms would be calculated
            // based on additional tracking that would be implemented
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:69-85 method should_receive <-> ./crates/ccswarm/src/streaming/mod.rs:468-486 method run_streaming_loop
  Similarity: 94.06%
  Classes: StreamSubscription <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:should_receive (lines 69-85) ---[0m
    pub fn should_receive(&self, entry: &OutputEntry) -> bool {
        // Check agent filter
        if let Some(ref agent_id) = self.agent_id {
            if entry.agent_id != *agent_id {
                return false;
            }
        }

        // Check output filter
        if let Some(ref filter) = self.filter {
            if !entry.matches_filter(filter) {
                return false;
            }
        }

        true
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:run_streaming_loop (lines 468-486) ---[0m
    async fn run_streaming_loop(&self) {
        // Get the receiver once at startup
        let mut receiver = {
            let global_receiver = match self.global_receiver.read() {
                Ok(gr) => gr,
                Err(_) => return,
            };

            match global_receiver.as_ref() {
                Some(r) => r.resubscribe(),
                None => return,
            }
        };

        // Main loop to process messages
        while let Ok(entry) = receiver.recv().await {
            self.distribute_entry(entry).await;
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:468-486 method run_streaming_loop <-> ./crates/ccswarm/src/streaming/mod.rs:489-506 method distribute_entry
  Similarity: 88.55%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:run_streaming_loop (lines 468-486) ---[0m
    async fn run_streaming_loop(&self) {
        // Get the receiver once at startup
        let mut receiver = {
            let global_receiver = match self.global_receiver.read() {
                Ok(gr) => gr,
                Err(_) => return,
            };

            match global_receiver.as_ref() {
                Some(r) => r.resubscribe(),
                None => return,
            }
        };

        // Main loop to process messages
        while let Ok(entry) = receiver.recv().await {
            self.distribute_entry(entry).await;
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:distribute_entry (lines 489-506) ---[0m
    async fn distribute_entry(&self, entry: OutputEntry) {
        let subscriptions = match self.subscriptions.read() {
            Ok(s) => s,
            Err(_) => return,
        };

        let mut sent_count = 0;
        for subscription in subscriptions.values() {
            if subscription.should_receive(&entry) && subscription.send(entry.clone()).is_ok() {
                sent_count += 1;
            }
        }

        // Update statistics
        if let Ok(mut stats) = self.stats.write() {
            stats.total_messages_sent += sent_count;
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:69-85 method should_receive <-> ./crates/ccswarm/src/streaming/mod.rs:489-506 method distribute_entry
  Similarity: 91.97%
  Classes: StreamSubscription <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:should_receive (lines 69-85) ---[0m
    pub fn should_receive(&self, entry: &OutputEntry) -> bool {
        // Check agent filter
        if let Some(ref agent_id) = self.agent_id {
            if entry.agent_id != *agent_id {
                return false;
            }
        }

        // Check output filter
        if let Some(ref filter) = self.filter {
            if !entry.matches_filter(filter) {
                return false;
            }
        }

        true
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:distribute_entry (lines 489-506) ---[0m
    async fn distribute_entry(&self, entry: OutputEntry) {
        let subscriptions = match self.subscriptions.read() {
            Ok(s) => s,
            Err(_) => return,
        };

        let mut sent_count = 0;
        for subscription in subscriptions.values() {
            if subscription.should_receive(&entry) && subscription.send(entry.clone()).is_ok() {
                sent_count += 1;
            }
        }

        // Update statistics
        if let Ok(mut stats) = self.stats.write() {
            stats.total_messages_sent += sent_count;
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:131-146 method format_entry <-> ./crates/ccswarm/src/streaming/mod.rs:518-541 method update_stats
  Similarity: 79.57%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_entry (lines 131-146) ---[0m
    pub fn format_entry(&self, entry: &OutputEntry) -> FormattedOutputEntry {
        let display_prefix = self.create_display_prefix(entry);
        let formatted_content = self.format_content(&entry.content);
        let highlight_spans = if self.config.enable_highlighting {
            self.generate_highlights(entry, &formatted_content)
        } else {
            vec![]
        };

        FormattedOutputEntry {
            original: entry.clone(),
            formatted_content,
            highlight_spans,
            display_prefix,
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:update_stats (lines 518-541) ---[0m
    fn update_stats(&self) {
        let subscriptions = match self.subscriptions.read() {
            Ok(s) => s,
            Err(_) => return,
        };

        let subscription_details: Vec<SubscriptionStats> = subscriptions
            .values()
            .map(|sub| SubscriptionStats {
                id: sub.id.clone(),
                agent_id: sub.agent_id.clone(),
                messages_received: 0, // This would need to be tracked separately
                last_message_at: None, // This would need to be tracked separately
                created_at: sub.created_at,
            })
            .collect();

        if let Ok(mut stats) = self.stats.write() {
            stats.active_subscriptions = subscriptions.len();
            stats.subscription_details = subscription_details;
            // messages_per_second and average_latency_ms would be calculated
            // based on additional tracking that would be implemented
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:399-414 method subscribe_agent <-> ./crates/ccswarm/src/streaming/mod.rs:417-432 method subscribe_filtered
  Similarity: 99.46%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_agent (lines 399-414) ---[0m
    pub fn subscribe_agent(
        &self,
        id: String,
        agent_id: String,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_agent(agent_id);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_filtered (lines 417-432) ---[0m
    pub fn subscribe_filtered(
        &self,
        id: String,
        filter: OutputFilter,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_filter(filter);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:359-382 method start <-> ./crates/ccswarm/src/streaming/mod.rs:468-486 method run_streaming_loop
  Similarity: 73.96%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:start (lines 359-382) ---[0m
    pub async fn start(&self) -> Result<(), String> {
        // Initialize global receiver
        {
            let mut receiver = self
                .global_receiver
                .write()
                .map_err(|e| format!("Failed to lock global receiver: {}", e))?;
            *receiver = Some(self.monitoring.subscribe_global());
        }

        // Start the main streaming loop
        let streaming_manager = self.clone();
        tokio::spawn(async move {
            streaming_manager.run_streaming_loop().await;
        });

        // Start statistics updater
        let stats_manager = self.clone();
        tokio::spawn(async move {
            stats_manager.run_stats_updater().await;
        });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:run_streaming_loop (lines 468-486) ---[0m
    async fn run_streaming_loop(&self) {
        // Get the receiver once at startup
        let mut receiver = {
            let global_receiver = match self.global_receiver.read() {
                Ok(gr) => gr,
                Err(_) => return,
            };

            match global_receiver.as_ref() {
                Some(r) => r.resubscribe(),
                None => return,
            }
        };

        // Main loop to process messages
        while let Ok(entry) = receiver.recv().await {
            self.distribute_entry(entry).await;
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:176-198 method format_content <-> ./crates/ccswarm/src/streaming/mod.rs:399-414 method subscribe_agent
  Similarity: 80.22%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_content (lines 176-198) ---[0m
    fn format_content(&self, content: &str) -> String {
        let mut formatted = content.to_string();

        // Truncate if too long
        if formatted.len() > self.config.max_line_length {
            formatted.truncate(self.config.max_line_length - 3);
            formatted.push_str("...");
        }

        // Replace tabs with spaces for consistent display
        formatted = formatted.replace('\t', "    ");

        // Handle multi-line content
        if formatted.contains('\n') {
            formatted = formatted
                .lines()
                .map(|line| format!("  {}", line))
                .collect::<Vec<_>>()
                .join("\n");
        }

        formatted
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_agent (lines 399-414) ---[0m
    pub fn subscribe_agent(
        &self,
        id: String,
        agent_id: String,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_agent(agent_id);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:176-198 method format_content <-> ./crates/ccswarm/src/streaming/mod.rs:417-432 method subscribe_filtered
  Similarity: 80.22%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_content (lines 176-198) ---[0m
    fn format_content(&self, content: &str) -> String {
        let mut formatted = content.to_string();

        // Truncate if too long
        if formatted.len() > self.config.max_line_length {
            formatted.truncate(self.config.max_line_length - 3);
            formatted.push_str("...");
        }

        // Replace tabs with spaces for consistent display
        formatted = formatted.replace('\t', "    ");

        // Handle multi-line content
        if formatted.contains('\n') {
            formatted = formatted
                .lines()
                .map(|line| format!("  {}", line))
                .collect::<Vec<_>>()
                .join("\n");
        }

        formatted
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_filtered (lines 417-432) ---[0m
    pub fn subscribe_filtered(
        &self,
        id: String,
        filter: OutputFilter,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_filter(filter);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:399-414 method subscribe_agent <-> ./crates/ccswarm/src/streaming/mod.rs:518-541 method update_stats
  Similarity: 77.17%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_agent (lines 399-414) ---[0m
    pub fn subscribe_agent(
        &self,
        id: String,
        agent_id: String,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_agent(agent_id);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:update_stats (lines 518-541) ---[0m
    fn update_stats(&self) {
        let subscriptions = match self.subscriptions.read() {
            Ok(s) => s,
            Err(_) => return,
        };

        let subscription_details: Vec<SubscriptionStats> = subscriptions
            .values()
            .map(|sub| SubscriptionStats {
                id: sub.id.clone(),
                agent_id: sub.agent_id.clone(),
                messages_received: 0, // This would need to be tracked separately
                last_message_at: None, // This would need to be tracked separately
                created_at: sub.created_at,
            })
            .collect();

        if let Ok(mut stats) = self.stats.write() {
            stats.active_subscriptions = subscriptions.len();
            stats.subscription_details = subscription_details;
            // messages_per_second and average_latency_ms would be calculated
            // based on additional tracking that would be implemented
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:417-432 method subscribe_filtered <-> ./crates/ccswarm/src/streaming/mod.rs:518-541 method update_stats
  Similarity: 77.17%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_filtered (lines 417-432) ---[0m
    pub fn subscribe_filtered(
        &self,
        id: String,
        filter: OutputFilter,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_filter(filter);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:update_stats (lines 518-541) ---[0m
    fn update_stats(&self) {
        let subscriptions = match self.subscriptions.read() {
            Ok(s) => s,
            Err(_) => return,
        };

        let subscription_details: Vec<SubscriptionStats> = subscriptions
            .values()
            .map(|sub| SubscriptionStats {
                id: sub.id.clone(),
                agent_id: sub.agent_id.clone(),
                messages_received: 0, // This would need to be tracked separately
                last_message_at: None, // This would need to be tracked separately
                created_at: sub.created_at,
            })
            .collect();

        if let Ok(mut stats) = self.stats.write() {
            stats.active_subscriptions = subscriptions.len();
            stats.subscription_details = subscription_details;
            // messages_per_second and average_latency_ms would be calculated
            // based on additional tracking that would be implemented
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:69-85 method should_receive <-> ./crates/ccswarm/src/streaming/mod.rs:359-382 method start
  Similarity: 75.27%
  Classes: StreamSubscription <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:should_receive (lines 69-85) ---[0m
    pub fn should_receive(&self, entry: &OutputEntry) -> bool {
        // Check agent filter
        if let Some(ref agent_id) = self.agent_id {
            if entry.agent_id != *agent_id {
                return false;
            }
        }

        // Check output filter
        if let Some(ref filter) = self.filter {
            if !entry.matches_filter(filter) {
                return false;
            }
        }

        true
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:start (lines 359-382) ---[0m
    pub async fn start(&self) -> Result<(), String> {
        // Initialize global receiver
        {
            let mut receiver = self
                .global_receiver
                .write()
                .map_err(|e| format!("Failed to lock global receiver: {}", e))?;
            *receiver = Some(self.monitoring.subscribe_global());
        }

        // Start the main streaming loop
        let streaming_manager = self.clone();
        tokio::spawn(async move {
            streaming_manager.run_streaming_loop().await;
        });

        // Start statistics updater
        let stats_manager = self.clone();
        tokio::spawn(async move {
            stats_manager.run_stats_updater().await;
        });

        Ok(())
    }

  ./crates/ccswarm/src/streaming/mod.rs:338-356 method new <-> ./crates/ccswarm/src/streaming/mod.rs:468-486 method run_streaming_loop
  Similarity: 80.27%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:new (lines 338-356) ---[0m
    pub fn new(monitoring: Arc<MonitoringSystem>, config: StreamConfig) -> Self {
        let formatter = OutputFormatter::new(config.clone());
        let stats = Arc::new(RwLock::new(StreamingStats {
            active_subscriptions: 0,
            total_messages_sent: 0,
            messages_per_second: 0.0,
            average_latency_ms: 0.0,
            subscription_details: Vec::new(),
        }));

        Self {
            monitoring,
            subscriptions: Arc::new(RwLock::new(HashMap::new())),
            formatter,
            config,
            stats,
            global_receiver: Arc::new(RwLock::new(None)),
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:run_streaming_loop (lines 468-486) ---[0m
    async fn run_streaming_loop(&self) {
        // Get the receiver once at startup
        let mut receiver = {
            let global_receiver = match self.global_receiver.read() {
                Ok(gr) => gr,
                Err(_) => return,
            };

            match global_receiver.as_ref() {
                Some(r) => r.resubscribe(),
                None => return,
            }
        };

        // Main loop to process messages
        while let Ok(entry) = receiver.recv().await {
            self.distribute_entry(entry).await;
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:131-146 method format_entry <-> ./crates/ccswarm/src/streaming/mod.rs:176-198 method format_content
  Similarity: 76.03%
  Classes: OutputFormatter <-> OutputFormatter

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_entry (lines 131-146) ---[0m
    pub fn format_entry(&self, entry: &OutputEntry) -> FormattedOutputEntry {
        let display_prefix = self.create_display_prefix(entry);
        let formatted_content = self.format_content(&entry.content);
        let highlight_spans = if self.config.enable_highlighting {
            self.generate_highlights(entry, &formatted_content)
        } else {
            vec![]
        };

        FormattedOutputEntry {
            original: entry.clone(),
            formatted_content,
            highlight_spans,
            display_prefix,
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_content (lines 176-198) ---[0m
    fn format_content(&self, content: &str) -> String {
        let mut formatted = content.to_string();

        // Truncate if too long
        if formatted.len() > self.config.max_line_length {
            formatted.truncate(self.config.max_line_length - 3);
            formatted.push_str("...");
        }

        // Replace tabs with spaces for consistent display
        formatted = formatted.replace('\t', "    ");

        // Handle multi-line content
        if formatted.contains('\n') {
            formatted = formatted
                .lines()
                .map(|line| format!("  {}", line))
                .collect::<Vec<_>>()
                .join("\n");
        }

        formatted
    }

  ./crates/ccswarm/src/streaming/mod.rs:359-382 method start <-> ./crates/ccswarm/src/streaming/mod.rs:399-414 method subscribe_agent
  Similarity: 72.96%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:start (lines 359-382) ---[0m
    pub async fn start(&self) -> Result<(), String> {
        // Initialize global receiver
        {
            let mut receiver = self
                .global_receiver
                .write()
                .map_err(|e| format!("Failed to lock global receiver: {}", e))?;
            *receiver = Some(self.monitoring.subscribe_global());
        }

        // Start the main streaming loop
        let streaming_manager = self.clone();
        tokio::spawn(async move {
            streaming_manager.run_streaming_loop().await;
        });

        // Start statistics updater
        let stats_manager = self.clone();
        tokio::spawn(async move {
            stats_manager.run_stats_updater().await;
        });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_agent (lines 399-414) ---[0m
    pub fn subscribe_agent(
        &self,
        id: String,
        agent_id: String,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_agent(agent_id);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:359-382 method start <-> ./crates/ccswarm/src/streaming/mod.rs:417-432 method subscribe_filtered
  Similarity: 72.96%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:start (lines 359-382) ---[0m
    pub async fn start(&self) -> Result<(), String> {
        // Initialize global receiver
        {
            let mut receiver = self
                .global_receiver
                .write()
                .map_err(|e| format!("Failed to lock global receiver: {}", e))?;
            *receiver = Some(self.monitoring.subscribe_global());
        }

        // Start the main streaming loop
        let streaming_manager = self.clone();
        tokio::spawn(async move {
            streaming_manager.run_streaming_loop().await;
        });

        // Start statistics updater
        let stats_manager = self.clone();
        tokio::spawn(async move {
            stats_manager.run_stats_updater().await;
        });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_filtered (lines 417-432) ---[0m
    pub fn subscribe_filtered(
        &self,
        id: String,
        filter: OutputFilter,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_filter(filter);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:131-146 method format_entry <-> ./crates/ccswarm/src/streaming/mod.rs:468-486 method run_streaming_loop
  Similarity: 83.31%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_entry (lines 131-146) ---[0m
    pub fn format_entry(&self, entry: &OutputEntry) -> FormattedOutputEntry {
        let display_prefix = self.create_display_prefix(entry);
        let formatted_content = self.format_content(&entry.content);
        let highlight_spans = if self.config.enable_highlighting {
            self.generate_highlights(entry, &formatted_content)
        } else {
            vec![]
        };

        FormattedOutputEntry {
            original: entry.clone(),
            formatted_content,
            highlight_spans,
            display_prefix,
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:run_streaming_loop (lines 468-486) ---[0m
    async fn run_streaming_loop(&self) {
        // Get the receiver once at startup
        let mut receiver = {
            let global_receiver = match self.global_receiver.read() {
                Ok(gr) => gr,
                Err(_) => return,
            };

            match global_receiver.as_ref() {
                Some(r) => r.resubscribe(),
                None => return,
            }
        };

        // Main loop to process messages
        while let Ok(entry) = receiver.recv().await {
            self.distribute_entry(entry).await;
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:399-414 method subscribe_agent <-> ./crates/ccswarm/src/streaming/mod.rs:468-486 method run_streaming_loop
  Similarity: 82.41%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_agent (lines 399-414) ---[0m
    pub fn subscribe_agent(
        &self,
        id: String,
        agent_id: String,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_agent(agent_id);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:run_streaming_loop (lines 468-486) ---[0m
    async fn run_streaming_loop(&self) {
        // Get the receiver once at startup
        let mut receiver = {
            let global_receiver = match self.global_receiver.read() {
                Ok(gr) => gr,
                Err(_) => return,
            };

            match global_receiver.as_ref() {
                Some(r) => r.resubscribe(),
                None => return,
            }
        };

        // Main loop to process messages
        while let Ok(entry) = receiver.recv().await {
            self.distribute_entry(entry).await;
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:417-432 method subscribe_filtered <-> ./crates/ccswarm/src/streaming/mod.rs:468-486 method run_streaming_loop
  Similarity: 82.41%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_filtered (lines 417-432) ---[0m
    pub fn subscribe_filtered(
        &self,
        id: String,
        filter: OutputFilter,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_filter(filter);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:run_streaming_loop (lines 468-486) ---[0m
    async fn run_streaming_loop(&self) {
        // Get the receiver once at startup
        let mut receiver = {
            let global_receiver = match self.global_receiver.read() {
                Ok(gr) => gr,
                Err(_) => return,
            };

            match global_receiver.as_ref() {
                Some(r) => r.resubscribe(),
                None => return,
            }
        };

        // Main loop to process messages
        while let Ok(entry) = receiver.recv().await {
            self.distribute_entry(entry).await;
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:176-198 method format_content <-> ./crates/ccswarm/src/streaming/mod.rs:385-396 method subscribe
  Similarity: 81.45%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_content (lines 176-198) ---[0m
    fn format_content(&self, content: &str) -> String {
        let mut formatted = content.to_string();

        // Truncate if too long
        if formatted.len() > self.config.max_line_length {
            formatted.truncate(self.config.max_line_length - 3);
            formatted.push_str("...");
        }

        // Replace tabs with spaces for consistent display
        formatted = formatted.replace('\t', "    ");

        // Handle multi-line content
        if formatted.contains('\n') {
            formatted = formatted
                .lines()
                .map(|line| format!("  {}", line))
                .collect::<Vec<_>>()
                .join("\n");
        }

        formatted
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe (lines 385-396) ---[0m
    pub fn subscribe(&self, id: String) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:294-304 method find_all_paths <-> ./crates/ccswarm/src/streaming/mod.rs:468-486 method run_streaming_loop
  Similarity: 94.06%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:find_all_paths (lines 294-304) ---[0m
    fn find_all_paths(&self, content: &str) -> Vec<(usize, usize)> {
        let mut paths = Vec::new();
        let path_regex =
            regex::Regex::new(r"[/\\]?[\w.-]+(?:[/\\][\w.-]+)*\.[a-zA-Z0-9]+").unwrap();

        for mat in path_regex.find_iter(content) {
            paths.push((mat.start(), mat.end()));
        }

        paths
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:run_streaming_loop (lines 468-486) ---[0m
    async fn run_streaming_loop(&self) {
        // Get the receiver once at startup
        let mut receiver = {
            let global_receiver = match self.global_receiver.read() {
                Ok(gr) => gr,
                Err(_) => return,
            };

            match global_receiver.as_ref() {
                Some(r) => r.resubscribe(),
                None => return,
            }
        };

        // Main loop to process messages
        while let Ok(entry) = receiver.recv().await {
            self.distribute_entry(entry).await;
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:399-414 method subscribe_agent <-> ./crates/ccswarm/src/streaming/mod.rs:489-506 method distribute_entry
  Similarity: 82.83%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_agent (lines 399-414) ---[0m
    pub fn subscribe_agent(
        &self,
        id: String,
        agent_id: String,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_agent(agent_id);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:distribute_entry (lines 489-506) ---[0m
    async fn distribute_entry(&self, entry: OutputEntry) {
        let subscriptions = match self.subscriptions.read() {
            Ok(s) => s,
            Err(_) => return,
        };

        let mut sent_count = 0;
        for subscription in subscriptions.values() {
            if subscription.should_receive(&entry) && subscription.send(entry.clone()).is_ok() {
                sent_count += 1;
            }
        }

        // Update statistics
        if let Ok(mut stats) = self.stats.write() {
            stats.total_messages_sent += sent_count;
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:417-432 method subscribe_filtered <-> ./crates/ccswarm/src/streaming/mod.rs:489-506 method distribute_entry
  Similarity: 82.83%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_filtered (lines 417-432) ---[0m
    pub fn subscribe_filtered(
        &self,
        id: String,
        filter: OutputFilter,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_filter(filter);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:distribute_entry (lines 489-506) ---[0m
    async fn distribute_entry(&self, entry: OutputEntry) {
        let subscriptions = match self.subscriptions.read() {
            Ok(s) => s,
            Err(_) => return,
        };

        let mut sent_count = 0;
        for subscription in subscriptions.values() {
            if subscription.should_receive(&entry) && subscription.send(entry.clone()).is_ok() {
                sent_count += 1;
            }
        }

        // Update statistics
        if let Ok(mut stats) = self.stats.write() {
            stats.total_messages_sent += sent_count;
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:131-146 method format_entry <-> ./crates/ccswarm/src/streaming/mod.rs:359-382 method start
  Similarity: 70.18%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_entry (lines 131-146) ---[0m
    pub fn format_entry(&self, entry: &OutputEntry) -> FormattedOutputEntry {
        let display_prefix = self.create_display_prefix(entry);
        let formatted_content = self.format_content(&entry.content);
        let highlight_spans = if self.config.enable_highlighting {
            self.generate_highlights(entry, &formatted_content)
        } else {
            vec![]
        };

        FormattedOutputEntry {
            original: entry.clone(),
            formatted_content,
            highlight_spans,
            display_prefix,
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:start (lines 359-382) ---[0m
    pub async fn start(&self) -> Result<(), String> {
        // Initialize global receiver
        {
            let mut receiver = self
                .global_receiver
                .write()
                .map_err(|e| format!("Failed to lock global receiver: {}", e))?;
            *receiver = Some(self.monitoring.subscribe_global());
        }

        // Start the main streaming loop
        let streaming_manager = self.clone();
        tokio::spawn(async move {
            streaming_manager.run_streaming_loop().await;
        });

        // Start statistics updater
        let stats_manager = self.clone();
        tokio::spawn(async move {
            stats_manager.run_stats_updater().await;
        });

        Ok(())
    }

  ./crates/ccswarm/src/streaming/mod.rs:69-85 method should_receive <-> ./crates/ccswarm/src/streaming/mod.rs:338-356 method new
  Similarity: 77.40%
  Classes: StreamSubscription <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:should_receive (lines 69-85) ---[0m
    pub fn should_receive(&self, entry: &OutputEntry) -> bool {
        // Check agent filter
        if let Some(ref agent_id) = self.agent_id {
            if entry.agent_id != *agent_id {
                return false;
            }
        }

        // Check output filter
        if let Some(ref filter) = self.filter {
            if !entry.matches_filter(filter) {
                return false;
            }
        }

        true
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:new (lines 338-356) ---[0m
    pub fn new(monitoring: Arc<MonitoringSystem>, config: StreamConfig) -> Self {
        let formatter = OutputFormatter::new(config.clone());
        let stats = Arc::new(RwLock::new(StreamingStats {
            active_subscriptions: 0,
            total_messages_sent: 0,
            messages_per_second: 0.0,
            average_latency_ms: 0.0,
            subscription_details: Vec::new(),
        }));

        Self {
            monitoring,
            subscriptions: Arc::new(RwLock::new(HashMap::new())),
            formatter,
            config,
            stats,
            global_receiver: Arc::new(RwLock::new(None)),
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:385-396 method subscribe <-> ./crates/ccswarm/src/streaming/mod.rs:518-541 method update_stats
  Similarity: 77.17%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe (lines 385-396) ---[0m
    pub fn subscribe(&self, id: String) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:update_stats (lines 518-541) ---[0m
    fn update_stats(&self) {
        let subscriptions = match self.subscriptions.read() {
            Ok(s) => s,
            Err(_) => return,
        };

        let subscription_details: Vec<SubscriptionStats> = subscriptions
            .values()
            .map(|sub| SubscriptionStats {
                id: sub.id.clone(),
                agent_id: sub.agent_id.clone(),
                messages_received: 0, // This would need to be tracked separately
                last_message_at: None, // This would need to be tracked separately
                created_at: sub.created_at,
            })
            .collect();

        if let Ok(mut stats) = self.stats.write() {
            stats.active_subscriptions = subscriptions.len();
            stats.subscription_details = subscription_details;
            // messages_per_second and average_latency_ms would be calculated
            // based on additional tracking that would be implemented
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:131-146 method format_entry <-> ./crates/ccswarm/src/streaming/mod.rs:338-356 method new
  Similarity: 78.22%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_entry (lines 131-146) ---[0m
    pub fn format_entry(&self, entry: &OutputEntry) -> FormattedOutputEntry {
        let display_prefix = self.create_display_prefix(entry);
        let formatted_content = self.format_content(&entry.content);
        let highlight_spans = if self.config.enable_highlighting {
            self.generate_highlights(entry, &formatted_content)
        } else {
            vec![]
        };

        FormattedOutputEntry {
            original: entry.clone(),
            formatted_content,
            highlight_spans,
            display_prefix,
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:new (lines 338-356) ---[0m
    pub fn new(monitoring: Arc<MonitoringSystem>, config: StreamConfig) -> Self {
        let formatter = OutputFormatter::new(config.clone());
        let stats = Arc::new(RwLock::new(StreamingStats {
            active_subscriptions: 0,
            total_messages_sent: 0,
            messages_per_second: 0.0,
            average_latency_ms: 0.0,
            subscription_details: Vec::new(),
        }));

        Self {
            monitoring,
            subscriptions: Arc::new(RwLock::new(HashMap::new())),
            formatter,
            config,
            stats,
            global_receiver: Arc::new(RwLock::new(None)),
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:338-356 method new <-> ./crates/ccswarm/src/streaming/mod.rs:399-414 method subscribe_agent
  Similarity: 78.01%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:new (lines 338-356) ---[0m
    pub fn new(monitoring: Arc<MonitoringSystem>, config: StreamConfig) -> Self {
        let formatter = OutputFormatter::new(config.clone());
        let stats = Arc::new(RwLock::new(StreamingStats {
            active_subscriptions: 0,
            total_messages_sent: 0,
            messages_per_second: 0.0,
            average_latency_ms: 0.0,
            subscription_details: Vec::new(),
        }));

        Self {
            monitoring,
            subscriptions: Arc::new(RwLock::new(HashMap::new())),
            formatter,
            config,
            stats,
            global_receiver: Arc::new(RwLock::new(None)),
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_agent (lines 399-414) ---[0m
    pub fn subscribe_agent(
        &self,
        id: String,
        agent_id: String,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_agent(agent_id);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:338-356 method new <-> ./crates/ccswarm/src/streaming/mod.rs:417-432 method subscribe_filtered
  Similarity: 78.01%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:new (lines 338-356) ---[0m
    pub fn new(monitoring: Arc<MonitoringSystem>, config: StreamConfig) -> Self {
        let formatter = OutputFormatter::new(config.clone());
        let stats = Arc::new(RwLock::new(StreamingStats {
            active_subscriptions: 0,
            total_messages_sent: 0,
            messages_per_second: 0.0,
            average_latency_ms: 0.0,
            subscription_details: Vec::new(),
        }));

        Self {
            monitoring,
            subscriptions: Arc::new(RwLock::new(HashMap::new())),
            formatter,
            config,
            stats,
            global_receiver: Arc::new(RwLock::new(None)),
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_filtered (lines 417-432) ---[0m
    pub fn subscribe_filtered(
        &self,
        id: String,
        filter: OutputFilter,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_filter(filter);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:385-396 method subscribe <-> ./crates/ccswarm/src/streaming/mod.rs:399-414 method subscribe_agent
  Similarity: 96.43%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe (lines 385-396) ---[0m
    pub fn subscribe(&self, id: String) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_agent (lines 399-414) ---[0m
    pub fn subscribe_agent(
        &self,
        id: String,
        agent_id: String,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_agent(agent_id);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:385-396 method subscribe <-> ./crates/ccswarm/src/streaming/mod.rs:417-432 method subscribe_filtered
  Similarity: 96.43%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe (lines 385-396) ---[0m
    pub fn subscribe(&self, id: String) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_filtered (lines 417-432) ---[0m
    pub fn subscribe_filtered(
        &self,
        id: String,
        filter: OutputFilter,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_filter(filter);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:131-146 method format_entry <-> ./crates/ccswarm/src/streaming/mod.rs:399-414 method subscribe_agent
  Similarity: 84.20%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_entry (lines 131-146) ---[0m
    pub fn format_entry(&self, entry: &OutputEntry) -> FormattedOutputEntry {
        let display_prefix = self.create_display_prefix(entry);
        let formatted_content = self.format_content(&entry.content);
        let highlight_spans = if self.config.enable_highlighting {
            self.generate_highlights(entry, &formatted_content)
        } else {
            vec![]
        };

        FormattedOutputEntry {
            original: entry.clone(),
            formatted_content,
            highlight_spans,
            display_prefix,
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_agent (lines 399-414) ---[0m
    pub fn subscribe_agent(
        &self,
        id: String,
        agent_id: String,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_agent(agent_id);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:131-146 method format_entry <-> ./crates/ccswarm/src/streaming/mod.rs:417-432 method subscribe_filtered
  Similarity: 84.20%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_entry (lines 131-146) ---[0m
    pub fn format_entry(&self, entry: &OutputEntry) -> FormattedOutputEntry {
        let display_prefix = self.create_display_prefix(entry);
        let formatted_content = self.format_content(&entry.content);
        let highlight_spans = if self.config.enable_highlighting {
            self.generate_highlights(entry, &formatted_content)
        } else {
            vec![]
        };

        FormattedOutputEntry {
            original: entry.clone(),
            formatted_content,
            highlight_spans,
            display_prefix,
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_filtered (lines 417-432) ---[0m
    pub fn subscribe_filtered(
        &self,
        id: String,
        filter: OutputFilter,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_filter(filter);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:294-304 method find_all_paths <-> ./crates/ccswarm/src/streaming/mod.rs:489-506 method distribute_entry
  Similarity: 90.26%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:find_all_paths (lines 294-304) ---[0m
    fn find_all_paths(&self, content: &str) -> Vec<(usize, usize)> {
        let mut paths = Vec::new();
        let path_regex =
            regex::Regex::new(r"[/\\]?[\w.-]+(?:[/\\][\w.-]+)*\.[a-zA-Z0-9]+").unwrap();

        for mat in path_regex.find_iter(content) {
            paths.push((mat.start(), mat.end()));
        }

        paths
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:distribute_entry (lines 489-506) ---[0m
    async fn distribute_entry(&self, entry: OutputEntry) {
        let subscriptions = match self.subscriptions.read() {
            Ok(s) => s,
            Err(_) => return,
        };

        let mut sent_count = 0;
        for subscription in subscriptions.values() {
            if subscription.should_receive(&entry) && subscription.send(entry.clone()).is_ok() {
                sent_count += 1;
            }
        }

        // Update statistics
        if let Ok(mut stats) = self.stats.write() {
            stats.total_messages_sent += sent_count;
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:359-382 method start <-> ./crates/ccswarm/src/streaming/mod.rs:385-396 method subscribe
  Similarity: 71.95%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:start (lines 359-382) ---[0m
    pub async fn start(&self) -> Result<(), String> {
        // Initialize global receiver
        {
            let mut receiver = self
                .global_receiver
                .write()
                .map_err(|e| format!("Failed to lock global receiver: {}", e))?;
            *receiver = Some(self.monitoring.subscribe_global());
        }

        // Start the main streaming loop
        let streaming_manager = self.clone();
        tokio::spawn(async move {
            streaming_manager.run_streaming_loop().await;
        });

        // Start statistics updater
        let stats_manager = self.clone();
        tokio::spawn(async move {
            stats_manager.run_stats_updater().await;
        });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe (lines 385-396) ---[0m
    pub fn subscribe(&self, id: String) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:385-396 method subscribe <-> ./crates/ccswarm/src/streaming/mod.rs:468-486 method run_streaming_loop
  Similarity: 82.41%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe (lines 385-396) ---[0m
    pub fn subscribe(&self, id: String) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:run_streaming_loop (lines 468-486) ---[0m
    async fn run_streaming_loop(&self) {
        // Get the receiver once at startup
        let mut receiver = {
            let global_receiver = match self.global_receiver.read() {
                Ok(gr) => gr,
                Err(_) => return,
            };

            match global_receiver.as_ref() {
                Some(r) => r.resubscribe(),
                None => return,
            }
        };

        // Main loop to process messages
        while let Ok(entry) = receiver.recv().await {
            self.distribute_entry(entry).await;
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:131-146 method format_entry <-> ./crates/ccswarm/src/streaming/mod.rs:489-506 method distribute_entry
  Similarity: 75.00%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_entry (lines 131-146) ---[0m
    pub fn format_entry(&self, entry: &OutputEntry) -> FormattedOutputEntry {
        let display_prefix = self.create_display_prefix(entry);
        let formatted_content = self.format_content(&entry.content);
        let highlight_spans = if self.config.enable_highlighting {
            self.generate_highlights(entry, &formatted_content)
        } else {
            vec![]
        };

        FormattedOutputEntry {
            original: entry.clone(),
            formatted_content,
            highlight_spans,
            display_prefix,
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:distribute_entry (lines 489-506) ---[0m
    async fn distribute_entry(&self, entry: OutputEntry) {
        let subscriptions = match self.subscriptions.read() {
            Ok(s) => s,
            Err(_) => return,
        };

        let mut sent_count = 0;
        for subscription in subscriptions.values() {
            if subscription.should_receive(&entry) && subscription.send(entry.clone()).is_ok() {
                sent_count += 1;
            }
        }

        // Update statistics
        if let Ok(mut stats) = self.stats.write() {
            stats.total_messages_sent += sent_count;
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:69-85 method should_receive <-> ./crates/ccswarm/src/streaming/mod.rs:399-414 method subscribe_agent
  Similarity: 75.36%
  Classes: StreamSubscription <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:should_receive (lines 69-85) ---[0m
    pub fn should_receive(&self, entry: &OutputEntry) -> bool {
        // Check agent filter
        if let Some(ref agent_id) = self.agent_id {
            if entry.agent_id != *agent_id {
                return false;
            }
        }

        // Check output filter
        if let Some(ref filter) = self.filter {
            if !entry.matches_filter(filter) {
                return false;
            }
        }

        true
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_agent (lines 399-414) ---[0m
    pub fn subscribe_agent(
        &self,
        id: String,
        agent_id: String,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_agent(agent_id);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:69-85 method should_receive <-> ./crates/ccswarm/src/streaming/mod.rs:417-432 method subscribe_filtered
  Similarity: 75.36%
  Classes: StreamSubscription <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:should_receive (lines 69-85) ---[0m
    pub fn should_receive(&self, entry: &OutputEntry) -> bool {
        // Check agent filter
        if let Some(ref agent_id) = self.agent_id {
            if entry.agent_id != *agent_id {
                return false;
            }
        }

        // Check output filter
        if let Some(ref filter) = self.filter {
            if !entry.matches_filter(filter) {
                return false;
            }
        }

        true
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_filtered (lines 417-432) ---[0m
    pub fn subscribe_filtered(
        &self,
        id: String,
        filter: OutputFilter,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_filter(filter);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:385-396 method subscribe <-> ./crates/ccswarm/src/streaming/mod.rs:489-506 method distribute_entry
  Similarity: 82.83%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe (lines 385-396) ---[0m
    pub fn subscribe(&self, id: String) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:distribute_entry (lines 489-506) ---[0m
    async fn distribute_entry(&self, entry: OutputEntry) {
        let subscriptions = match self.subscriptions.read() {
            Ok(s) => s,
            Err(_) => return,
        };

        let mut sent_count = 0;
        for subscription in subscriptions.values() {
            if subscription.should_receive(&entry) && subscription.send(entry.clone()).is_ok() {
                sent_count += 1;
            }
        }

        // Update statistics
        if let Ok(mut stats) = self.stats.write() {
            stats.total_messages_sent += sent_count;
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:294-304 method find_all_paths <-> ./crates/ccswarm/src/streaming/mod.rs:338-356 method new
  Similarity: 81.23%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:find_all_paths (lines 294-304) ---[0m
    fn find_all_paths(&self, content: &str) -> Vec<(usize, usize)> {
        let mut paths = Vec::new();
        let path_regex =
            regex::Regex::new(r"[/\\]?[\w.-]+(?:[/\\][\w.-]+)*\.[a-zA-Z0-9]+").unwrap();

        for mat in path_regex.find_iter(content) {
            paths.push((mat.start(), mat.end()));
        }

        paths
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:new (lines 338-356) ---[0m
    pub fn new(monitoring: Arc<MonitoringSystem>, config: StreamConfig) -> Self {
        let formatter = OutputFormatter::new(config.clone());
        let stats = Arc::new(RwLock::new(StreamingStats {
            active_subscriptions: 0,
            total_messages_sent: 0,
            messages_per_second: 0.0,
            average_latency_ms: 0.0,
            subscription_details: Vec::new(),
        }));

        Self {
            monitoring,
            subscriptions: Arc::new(RwLock::new(HashMap::new())),
            formatter,
            config,
            stats,
            global_receiver: Arc::new(RwLock::new(None)),
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:338-356 method new <-> ./crates/ccswarm/src/streaming/mod.rs:385-396 method subscribe
  Similarity: 77.53%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:new (lines 338-356) ---[0m
    pub fn new(monitoring: Arc<MonitoringSystem>, config: StreamConfig) -> Self {
        let formatter = OutputFormatter::new(config.clone());
        let stats = Arc::new(RwLock::new(StreamingStats {
            active_subscriptions: 0,
            total_messages_sent: 0,
            messages_per_second: 0.0,
            average_latency_ms: 0.0,
            subscription_details: Vec::new(),
        }));

        Self {
            monitoring,
            subscriptions: Arc::new(RwLock::new(HashMap::new())),
            formatter,
            config,
            stats,
            global_receiver: Arc::new(RwLock::new(None)),
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe (lines 385-396) ---[0m
    pub fn subscribe(&self, id: String) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:131-146 method format_entry <-> ./crates/ccswarm/src/streaming/mod.rs:385-396 method subscribe
  Similarity: 81.35%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_entry (lines 131-146) ---[0m
    pub fn format_entry(&self, entry: &OutputEntry) -> FormattedOutputEntry {
        let display_prefix = self.create_display_prefix(entry);
        let formatted_content = self.format_content(&entry.content);
        let highlight_spans = if self.config.enable_highlighting {
            self.generate_highlights(entry, &formatted_content)
        } else {
            vec![]
        };

        FormattedOutputEntry {
            original: entry.clone(),
            formatted_content,
            highlight_spans,
            display_prefix,
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe (lines 385-396) ---[0m
    pub fn subscribe(&self, id: String) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:69-85 method should_receive <-> ./crates/ccswarm/src/streaming/mod.rs:294-304 method find_all_paths
  Similarity: 79.34%
  Classes: StreamSubscription <-> OutputFormatter

[36m--- ./crates/ccswarm/src/streaming/mod.rs:should_receive (lines 69-85) ---[0m
    pub fn should_receive(&self, entry: &OutputEntry) -> bool {
        // Check agent filter
        if let Some(ref agent_id) = self.agent_id {
            if entry.agent_id != *agent_id {
                return false;
            }
        }

        // Check output filter
        if let Some(ref filter) = self.filter {
            if !entry.matches_filter(filter) {
                return false;
            }
        }

        true
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:find_all_paths (lines 294-304) ---[0m
    fn find_all_paths(&self, content: &str) -> Vec<(usize, usize)> {
        let mut paths = Vec::new();
        let path_regex =
            regex::Regex::new(r"[/\\]?[\w.-]+(?:[/\\][\w.-]+)*\.[a-zA-Z0-9]+").unwrap();

        for mat in path_regex.find_iter(content) {
            paths.push((mat.start(), mat.end()));
        }

        paths
    }

  ./crates/ccswarm/src/streaming/mod.rs:69-85 method should_receive <-> ./crates/ccswarm/src/streaming/mod.rs:285-292 method find_pattern
  Similarity: 83.96%
  Classes: StreamSubscription <-> OutputFormatter

[36m--- ./crates/ccswarm/src/streaming/mod.rs:should_receive (lines 69-85) ---[0m
    pub fn should_receive(&self, entry: &OutputEntry) -> bool {
        // Check agent filter
        if let Some(ref agent_id) = self.agent_id {
            if entry.agent_id != *agent_id {
                return false;
            }
        }

        // Check output filter
        if let Some(ref filter) = self.filter {
            if !entry.matches_filter(filter) {
                return false;
            }
        }

        true
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:find_pattern (lines 285-292) ---[0m
    fn find_pattern(&self, content: &str, patterns: &[&str]) -> Option<(usize, usize)> {
        for pattern in patterns {
            if let Some(pos) = content.find(pattern) {
                return Some((pos, pos + pattern.len()));
            }
        }
        None
    }

  ./crates/ccswarm/src/streaming/mod.rs:294-304 method find_all_paths <-> ./crates/ccswarm/src/streaming/mod.rs:399-414 method subscribe_agent
  Similarity: 76.96%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:find_all_paths (lines 294-304) ---[0m
    fn find_all_paths(&self, content: &str) -> Vec<(usize, usize)> {
        let mut paths = Vec::new();
        let path_regex =
            regex::Regex::new(r"[/\\]?[\w.-]+(?:[/\\][\w.-]+)*\.[a-zA-Z0-9]+").unwrap();

        for mat in path_regex.find_iter(content) {
            paths.push((mat.start(), mat.end()));
        }

        paths
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_agent (lines 399-414) ---[0m
    pub fn subscribe_agent(
        &self,
        id: String,
        agent_id: String,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_agent(agent_id);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:294-304 method find_all_paths <-> ./crates/ccswarm/src/streaming/mod.rs:417-432 method subscribe_filtered
  Similarity: 76.96%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:find_all_paths (lines 294-304) ---[0m
    fn find_all_paths(&self, content: &str) -> Vec<(usize, usize)> {
        let mut paths = Vec::new();
        let path_regex =
            regex::Regex::new(r"[/\\]?[\w.-]+(?:[/\\][\w.-]+)*\.[a-zA-Z0-9]+").unwrap();

        for mat in path_regex.find_iter(content) {
            paths.push((mat.start(), mat.end()));
        }

        paths
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe_filtered (lines 417-432) ---[0m
    pub fn subscribe_filtered(
        &self,
        id: String,
        filter: OutputFilter,
    ) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx).with_filter(filter);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:69-85 method should_receive <-> ./crates/ccswarm/src/streaming/mod.rs:385-396 method subscribe
  Similarity: 71.54%
  Classes: StreamSubscription <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:should_receive (lines 69-85) ---[0m
    pub fn should_receive(&self, entry: &OutputEntry) -> bool {
        // Check agent filter
        if let Some(ref agent_id) = self.agent_id {
            if entry.agent_id != *agent_id {
                return false;
            }
        }

        // Check output filter
        if let Some(ref filter) = self.filter {
            if !entry.matches_filter(filter) {
                return false;
            }
        }

        true
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe (lines 385-396) ---[0m
    pub fn subscribe(&self, id: String) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:131-146 method format_entry <-> ./crates/ccswarm/src/streaming/mod.rs:294-304 method find_all_paths
  Similarity: 76.34%
  Classes: OutputFormatter <-> OutputFormatter

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_entry (lines 131-146) ---[0m
    pub fn format_entry(&self, entry: &OutputEntry) -> FormattedOutputEntry {
        let display_prefix = self.create_display_prefix(entry);
        let formatted_content = self.format_content(&entry.content);
        let highlight_spans = if self.config.enable_highlighting {
            self.generate_highlights(entry, &formatted_content)
        } else {
            vec![]
        };

        FormattedOutputEntry {
            original: entry.clone(),
            formatted_content,
            highlight_spans,
            display_prefix,
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:find_all_paths (lines 294-304) ---[0m
    fn find_all_paths(&self, content: &str) -> Vec<(usize, usize)> {
        let mut paths = Vec::new();
        let path_regex =
            regex::Regex::new(r"[/\\]?[\w.-]+(?:[/\\][\w.-]+)*\.[a-zA-Z0-9]+").unwrap();

        for mat in path_regex.find_iter(content) {
            paths.push((mat.start(), mat.end()));
        }

        paths
    }

  ./crates/ccswarm/src/streaming/mod.rs:69-85 method should_receive <-> ./crates/ccswarm/src/streaming/mod.rs:435-442 method unsubscribe
  Similarity: 78.35%
  Classes: StreamSubscription <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:should_receive (lines 69-85) ---[0m
    pub fn should_receive(&self, entry: &OutputEntry) -> bool {
        // Check agent filter
        if let Some(ref agent_id) = self.agent_id {
            if entry.agent_id != *agent_id {
                return false;
            }
        }

        // Check output filter
        if let Some(ref filter) = self.filter {
            if !entry.matches_filter(filter) {
                return false;
            }
        }

        true
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:unsubscribe (lines 435-442) ---[0m
    pub fn unsubscribe(&self, id: &str) -> Result<(), String> {
        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.remove(id);
        Ok(())
    }

  ./crates/ccswarm/src/streaming/mod.rs:69-85 method should_receive <-> ./crates/ccswarm/src/streaming/mod.rs:458-465 method clear_subscriptions
  Similarity: 77.58%
  Classes: StreamSubscription <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:should_receive (lines 69-85) ---[0m
    pub fn should_receive(&self, entry: &OutputEntry) -> bool {
        // Check agent filter
        if let Some(ref agent_id) = self.agent_id {
            if entry.agent_id != *agent_id {
                return false;
            }
        }

        // Check output filter
        if let Some(ref filter) = self.filter {
            if !entry.matches_filter(filter) {
                return false;
            }
        }

        true
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:clear_subscriptions (lines 458-465) ---[0m
    pub fn clear_subscriptions(&self) -> Result<(), String> {
        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.clear();
        Ok(())
    }

  ./crates/ccswarm/src/streaming/mod.rs:69-85 method should_receive <-> ./crates/ccswarm/src/streaming/mod.rs:509-516 method run_stats_updater
  Similarity: 70.88%
  Classes: StreamSubscription <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:should_receive (lines 69-85) ---[0m
    pub fn should_receive(&self, entry: &OutputEntry) -> bool {
        // Check agent filter
        if let Some(ref agent_id) = self.agent_id {
            if entry.agent_id != *agent_id {
                return false;
            }
        }

        // Check output filter
        if let Some(ref filter) = self.filter {
            if !entry.matches_filter(filter) {
                return false;
            }
        }

        true
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:run_stats_updater (lines 509-516) ---[0m
    async fn run_stats_updater(&self) {
        let mut interval = interval(Duration::from_secs(1));

        loop {
            interval.tick().await;
            self.update_stats();
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:294-304 method find_all_paths <-> ./crates/ccswarm/src/streaming/mod.rs:385-396 method subscribe
  Similarity: 75.19%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:find_all_paths (lines 294-304) ---[0m
    fn find_all_paths(&self, content: &str) -> Vec<(usize, usize)> {
        let mut paths = Vec::new();
        let path_regex =
            regex::Regex::new(r"[/\\]?[\w.-]+(?:[/\\][\w.-]+)*\.[a-zA-Z0-9]+").unwrap();

        for mat in path_regex.find_iter(content) {
            paths.push((mat.start(), mat.end()));
        }

        paths
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:subscribe (lines 385-396) ---[0m
    pub fn subscribe(&self, id: String) -> Result<mpsc::UnboundedReceiver<OutputEntry>, String> {
        let (tx, rx) = mpsc::unbounded_channel();
        let subscription = StreamSubscription::new(id.clone(), tx);

        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.insert(id, subscription);

        Ok(rx)
    }

  ./crates/ccswarm/src/streaming/mod.rs:131-146 method format_entry <-> ./crates/ccswarm/src/streaming/mod.rs:435-442 method unsubscribe
  Similarity: 70.79%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_entry (lines 131-146) ---[0m
    pub fn format_entry(&self, entry: &OutputEntry) -> FormattedOutputEntry {
        let display_prefix = self.create_display_prefix(entry);
        let formatted_content = self.format_content(&entry.content);
        let highlight_spans = if self.config.enable_highlighting {
            self.generate_highlights(entry, &formatted_content)
        } else {
            vec![]
        };

        FormattedOutputEntry {
            original: entry.clone(),
            formatted_content,
            highlight_spans,
            display_prefix,
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:unsubscribe (lines 435-442) ---[0m
    pub fn unsubscribe(&self, id: &str) -> Result<(), String> {
        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.remove(id);
        Ok(())
    }

  ./crates/ccswarm/src/streaming/mod.rs:131-146 method format_entry <-> ./crates/ccswarm/src/streaming/mod.rs:458-465 method clear_subscriptions
  Similarity: 70.79%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:format_entry (lines 131-146) ---[0m
    pub fn format_entry(&self, entry: &OutputEntry) -> FormattedOutputEntry {
        let display_prefix = self.create_display_prefix(entry);
        let formatted_content = self.format_content(&entry.content);
        let highlight_spans = if self.config.enable_highlighting {
            self.generate_highlights(entry, &formatted_content)
        } else {
            vec![]
        };

        FormattedOutputEntry {
            original: entry.clone(),
            formatted_content,
            highlight_spans,
            display_prefix,
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:clear_subscriptions (lines 458-465) ---[0m
    pub fn clear_subscriptions(&self) -> Result<(), String> {
        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.clear();
        Ok(())
    }

  ./crates/ccswarm/src/streaming/mod.rs:435-442 method unsubscribe <-> ./crates/ccswarm/src/streaming/mod.rs:458-465 method clear_subscriptions
  Similarity: 97.94%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:unsubscribe (lines 435-442) ---[0m
    pub fn unsubscribe(&self, id: &str) -> Result<(), String> {
        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.remove(id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:clear_subscriptions (lines 458-465) ---[0m
    pub fn clear_subscriptions(&self) -> Result<(), String> {
        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.clear();
        Ok(())
    }

  ./crates/ccswarm/src/streaming/mod.rs:294-304 method find_all_paths <-> ./crates/ccswarm/src/streaming/mod.rs:458-465 method clear_subscriptions
  Similarity: 80.57%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:find_all_paths (lines 294-304) ---[0m
    fn find_all_paths(&self, content: &str) -> Vec<(usize, usize)> {
        let mut paths = Vec::new();
        let path_regex =
            regex::Regex::new(r"[/\\]?[\w.-]+(?:[/\\][\w.-]+)*\.[a-zA-Z0-9]+").unwrap();

        for mat in path_regex.find_iter(content) {
            paths.push((mat.start(), mat.end()));
        }

        paths
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:clear_subscriptions (lines 458-465) ---[0m
    pub fn clear_subscriptions(&self) -> Result<(), String> {
        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.clear();
        Ok(())
    }

  ./crates/ccswarm/src/streaming/mod.rs:294-304 method find_all_paths <-> ./crates/ccswarm/src/streaming/mod.rs:435-442 method unsubscribe
  Similarity: 79.77%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:find_all_paths (lines 294-304) ---[0m
    fn find_all_paths(&self, content: &str) -> Vec<(usize, usize)> {
        let mut paths = Vec::new();
        let path_regex =
            regex::Regex::new(r"[/\\]?[\w.-]+(?:[/\\][\w.-]+)*\.[a-zA-Z0-9]+").unwrap();

        for mat in path_regex.find_iter(content) {
            paths.push((mat.start(), mat.end()));
        }

        paths
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:unsubscribe (lines 435-442) ---[0m
    pub fn unsubscribe(&self, id: &str) -> Result<(), String> {
        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.remove(id);
        Ok(())
    }

  ./crates/ccswarm/src/streaming/mod.rs:458-465 method clear_subscriptions <-> ./crates/ccswarm/src/streaming/mod.rs:545-554 method clone
  Similarity: 79.05%
  Classes: StreamingManager <-> Clone

[36m--- ./crates/ccswarm/src/streaming/mod.rs:clear_subscriptions (lines 458-465) ---[0m
    pub fn clear_subscriptions(&self) -> Result<(), String> {
        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.clear();
        Ok(())
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:clone (lines 545-554) ---[0m
    fn clone(&self) -> Self {
        Self {
            monitoring: Arc::clone(&self.monitoring),
            subscriptions: Arc::clone(&self.subscriptions),
            formatter: OutputFormatter::new(self.config.clone()),
            config: self.config.clone(),
            stats: Arc::clone(&self.stats),
            global_receiver: Arc::clone(&self.global_receiver),
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:435-442 method unsubscribe <-> ./crates/ccswarm/src/streaming/mod.rs:545-554 method clone
  Similarity: 78.19%
  Classes: StreamingManager <-> Clone

[36m--- ./crates/ccswarm/src/streaming/mod.rs:unsubscribe (lines 435-442) ---[0m
    pub fn unsubscribe(&self, id: &str) -> Result<(), String> {
        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.remove(id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:clone (lines 545-554) ---[0m
    fn clone(&self) -> Self {
        Self {
            monitoring: Arc::clone(&self.monitoring),
            subscriptions: Arc::clone(&self.subscriptions),
            formatter: OutputFormatter::new(self.config.clone()),
            config: self.config.clone(),
            stats: Arc::clone(&self.stats),
            global_receiver: Arc::clone(&self.global_receiver),
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:21-29 method default <-> ./crates/ccswarm/src/streaming/mod.rs:43-51 method new
  Similarity: 75.88%
  Classes: Default <-> StreamSubscription

[36m--- ./crates/ccswarm/src/streaming/mod.rs:default (lines 21-29) ---[0m
    fn default() -> Self {
        Self {
            buffer_size: 1000,
            max_line_length: 2000,
            enable_filtering: true,
            enable_highlighting: true,
            refresh_rate_ms: 100,
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:new (lines 43-51) ---[0m
    pub fn new(id: String, tx: mpsc::UnboundedSender<OutputEntry>) -> Self {
        Self {
            id,
            agent_id: None,
            filter: None,
            created_at: Utc::now(),
            tx,
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:285-292 method find_pattern <-> ./crates/ccswarm/src/streaming/mod.rs:545-554 method clone
  Similarity: 73.10%
  Classes: OutputFormatter <-> Clone

[36m--- ./crates/ccswarm/src/streaming/mod.rs:find_pattern (lines 285-292) ---[0m
    fn find_pattern(&self, content: &str, patterns: &[&str]) -> Option<(usize, usize)> {
        for pattern in patterns {
            if let Some(pos) = content.find(pattern) {
                return Some((pos, pos + pattern.len()));
            }
        }
        None
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:clone (lines 545-554) ---[0m
    fn clone(&self) -> Self {
        Self {
            monitoring: Arc::clone(&self.monitoring),
            subscriptions: Arc::clone(&self.subscriptions),
            formatter: OutputFormatter::new(self.config.clone()),
            config: self.config.clone(),
            stats: Arc::clone(&self.stats),
            global_receiver: Arc::clone(&self.global_receiver),
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:21-29 method default <-> ./crates/ccswarm/src/streaming/mod.rs:285-292 method find_pattern
  Similarity: 73.73%
  Classes: Default <-> OutputFormatter

[36m--- ./crates/ccswarm/src/streaming/mod.rs:default (lines 21-29) ---[0m
    fn default() -> Self {
        Self {
            buffer_size: 1000,
            max_line_length: 2000,
            enable_filtering: true,
            enable_highlighting: true,
            refresh_rate_ms: 100,
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:find_pattern (lines 285-292) ---[0m
    fn find_pattern(&self, content: &str, patterns: &[&str]) -> Option<(usize, usize)> {
        for pattern in patterns {
            if let Some(pos) = content.find(pattern) {
                return Some((pos, pos + pattern.len()));
            }
        }
        None
    }

  ./crates/ccswarm/src/streaming/mod.rs:43-51 method new <-> ./crates/ccswarm/src/streaming/mod.rs:285-292 method find_pattern
  Similarity: 73.56%
  Classes: StreamSubscription <-> OutputFormatter

[36m--- ./crates/ccswarm/src/streaming/mod.rs:new (lines 43-51) ---[0m
    pub fn new(id: String, tx: mpsc::UnboundedSender<OutputEntry>) -> Self {
        Self {
            id,
            agent_id: None,
            filter: None,
            created_at: Utc::now(),
            tx,
        }
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:find_pattern (lines 285-292) ---[0m
    fn find_pattern(&self, content: &str, patterns: &[&str]) -> Option<(usize, usize)> {
        for pattern in patterns {
            if let Some(pos) = content.find(pattern) {
                return Some((pos, pos + pattern.len()));
            }
        }
        None
    }

  ./crates/ccswarm/src/streaming/mod.rs:285-292 method find_pattern <-> ./crates/ccswarm/src/streaming/mod.rs:509-516 method run_stats_updater
  Similarity: 75.93%
  Classes: OutputFormatter <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:find_pattern (lines 285-292) ---[0m
    fn find_pattern(&self, content: &str, patterns: &[&str]) -> Option<(usize, usize)> {
        for pattern in patterns {
            if let Some(pos) = content.find(pattern) {
                return Some((pos, pos + pattern.len()));
            }
        }
        None
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:run_stats_updater (lines 509-516) ---[0m
    async fn run_stats_updater(&self) {
        let mut interval = interval(Duration::from_secs(1));

        loop {
            interval.tick().await;
            self.update_stats();
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:458-465 method clear_subscriptions <-> ./crates/ccswarm/src/streaming/mod.rs:509-516 method run_stats_updater
  Similarity: 74.35%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:clear_subscriptions (lines 458-465) ---[0m
    pub fn clear_subscriptions(&self) -> Result<(), String> {
        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.clear();
        Ok(())
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:run_stats_updater (lines 509-516) ---[0m
    async fn run_stats_updater(&self) {
        let mut interval = interval(Duration::from_secs(1));

        loop {
            interval.tick().await;
            self.update_stats();
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:435-442 method unsubscribe <-> ./crates/ccswarm/src/streaming/mod.rs:509-516 method run_stats_updater
  Similarity: 73.81%
  Classes: StreamingManager <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:unsubscribe (lines 435-442) ---[0m
    pub fn unsubscribe(&self, id: &str) -> Result<(), String> {
        let mut subscriptions = self
            .subscriptions
            .write()
            .map_err(|e| format!("Failed to lock subscriptions: {}", e))?;
        subscriptions.remove(id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:run_stats_updater (lines 509-516) ---[0m
    async fn run_stats_updater(&self) {
        let mut interval = interval(Duration::from_secs(1));

        loop {
            interval.tick().await;
            self.update_stats();
        }
    }

  ./crates/ccswarm/src/streaming/mod.rs:63-67 method send <-> ./crates/ccswarm/src/streaming/mod.rs:450-455 method get_stats
  Similarity: 84.11%
  Classes: StreamSubscription <-> StreamingManager

[36m--- ./crates/ccswarm/src/streaming/mod.rs:send (lines 63-67) ---[0m
    pub fn send(&self, entry: OutputEntry) -> Result<(), String> {
        self.tx
            .send(entry)
            .map_err(|e| format!("Failed to send to subscriber: {}", e))
    }

[36m--- ./crates/ccswarm/src/streaming/mod.rs:get_stats (lines 450-455) ---[0m
    pub fn get_stats(&self) -> Result<StreamingStats, String> {
        self.stats
            .read()
            .map_err(|e| format!("Failed to lock stats: {}", e))
            .map(|stats| stats.clone())
    }


Duplicates in ./crates/ccswarm/src/traits.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/traits.rs:468-484 function test_persistable <-> ./crates/ccswarm/src/traits.rs:497-508 function test_cleanup_report
  Similarity: 74.61%

[36m--- ./crates/ccswarm/src/traits.rs:test_persistable (lines 468-484) ---[0m
    async fn test_persistable() {
        let entity = TestEntity {
            id: "test-123".to_string(),
            name: "Test Entity".to_string(),
            created_at: chrono::Utc::now(),
            updated_at: chrono::Utc::now(),
        };

        // Test serialization
        let bytes = entity.to_bytes().unwrap();
        assert!(!bytes.is_empty());

        // Test deserialization
        let restored: TestEntity = TestEntity::from_bytes(&bytes).unwrap();
        assert_eq!(restored.id, entity.id);
        assert_eq!(restored.name, entity.name);
    }

[36m--- ./crates/ccswarm/src/traits.rs:test_cleanup_report (lines 497-508) ---[0m
    fn test_cleanup_report() {
        let report = CleanupReport::new()
            .with_items(10)
            .with_bytes_freed(1024)
            .with_duration(Duration::from_millis(500))
            .add_error("Test error");

        assert_eq!(report.items_cleaned, 10);
        assert_eq!(report.bytes_freed, 1024);
        assert_eq!(report.duration, Duration::from_millis(500));
        assert_eq!(report.errors.len(), 1);
    }

  ./crates/ccswarm/src/traits.rs:206-214 method new <-> ./crates/ccswarm/src/traits.rs:267-275 method default
  Similarity: 89.38%
  Classes: CacheStats <-> Default

[36m--- ./crates/ccswarm/src/traits.rs:new (lines 206-214) ---[0m
    pub fn new() -> Self {
        Self {
            hits: 0,
            misses: 0,
            entries: 0,
            memory_usage: 0,
            hit_rate: 0.0,
        }
    }

[36m--- ./crates/ccswarm/src/traits.rs:default (lines 267-275) ---[0m
    fn default() -> Self {
        Self {
            max_attempts: 3,
            initial_delay: Duration::from_millis(100),
            max_delay: Duration::from_secs(30),
            backoff_multiplier: 2.0,
            jitter: true,
        }
    }

  ./crates/ccswarm/src/traits.rs:458-465 function test_generate_id_macro <-> ./crates/ccswarm/src/traits.rs:497-508 function test_cleanup_report
  Similarity: 80.33%

[36m--- ./crates/ccswarm/src/traits.rs:test_generate_id_macro (lines 458-465) ---[0m
    fn test_generate_id_macro() {
        let id1 = generate_id!("test");
        let id2 = generate_id!("test");

        assert!(id1.starts_with("test-"));
        assert!(id2.starts_with("test-"));
        assert_ne!(id1, id2);
    }

[36m--- ./crates/ccswarm/src/traits.rs:test_cleanup_report (lines 497-508) ---[0m
    fn test_cleanup_report() {
        let report = CleanupReport::new()
            .with_items(10)
            .with_bytes_freed(1024)
            .with_duration(Duration::from_millis(500))
            .add_error("Test error");

        assert_eq!(report.items_cleaned, 10);
        assert_eq!(report.bytes_freed, 1024);
        assert_eq!(report.duration, Duration::from_millis(500));
        assert_eq!(report.errors.len(), 1);
    }

  ./crates/ccswarm/src/traits.rs:267-275 method default <-> ./crates/ccswarm/src/traits.rs:305-312 method new
  Similarity: 85.21%
  Classes: Default <-> CleanupReport

[36m--- ./crates/ccswarm/src/traits.rs:default (lines 267-275) ---[0m
    fn default() -> Self {
        Self {
            max_attempts: 3,
            initial_delay: Duration::from_millis(100),
            max_delay: Duration::from_secs(30),
            backoff_multiplier: 2.0,
            jitter: true,
        }
    }

[36m--- ./crates/ccswarm/src/traits.rs:new (lines 305-312) ---[0m
    pub fn new() -> Self {
        Self {
            items_cleaned: 0,
            bytes_freed: 0,
            duration: Duration::from_millis(0),
            errors: Vec::new(),
        }
    }

  ./crates/ccswarm/src/traits.rs:206-214 method new <-> ./crates/ccswarm/src/traits.rs:305-312 method new
  Similarity: 84.88%
  Classes: CacheStats <-> CleanupReport

[36m--- ./crates/ccswarm/src/traits.rs:new (lines 206-214) ---[0m
    pub fn new() -> Self {
        Self {
            hits: 0,
            misses: 0,
            entries: 0,
            memory_usage: 0,
            hit_rate: 0.0,
        }
    }

[36m--- ./crates/ccswarm/src/traits.rs:new (lines 305-312) ---[0m
    pub fn new() -> Self {
        Self {
            items_cleaned: 0,
            bytes_freed: 0,
            duration: Duration::from_millis(0),
            errors: Vec::new(),
        }
    }

  ./crates/ccswarm/src/traits.rs:445-455 function test_identifiable <-> ./crates/ccswarm/src/traits.rs:487-494 function test_cache_stats
  Similarity: 73.85%

[36m--- ./crates/ccswarm/src/traits.rs:test_identifiable (lines 445-455) ---[0m
    fn test_identifiable() {
        let entity = TestEntity {
            id: "test-123".to_string(),
            name: "Test Entity".to_string(),
            created_at: chrono::Utc::now(),
            updated_at: chrono::Utc::now(),
        };

        assert_eq!(entity.id(), "test-123");
        assert_eq!(entity.name(), "Test Entity");
    }

[36m--- ./crates/ccswarm/src/traits.rs:test_cache_stats (lines 487-494) ---[0m
    fn test_cache_stats() {
        let mut stats = CacheStats::new();
        stats.hits = 80;
        stats.misses = 20;
        stats.calculate_hit_rate();

        assert_eq!(stats.hit_rate, 0.8);
    }

  ./crates/ccswarm/src/traits.rs:216-223 method calculate_hit_rate <-> ./crates/ccswarm/src/traits.rs:363-369 function load_from_file
  Similarity: 84.23%

[36m--- ./crates/ccswarm/src/traits.rs:calculate_hit_rate (lines 216-223) ---[0m
    pub fn calculate_hit_rate(&mut self) {
        let total = self.hits + self.misses;
        self.hit_rate = if total > 0 {
            self.hits as f64 / total as f64
        } else {
            0.0
        };
    }

[36m--- ./crates/ccswarm/src/traits.rs:load_from_file (lines 363-369) ---[0m
    async fn load_from_file<P: AsRef<std::path::Path> + Send>(path: P) -> Result<Self>
    where
        Self: Sized,
    {
        let bytes = tokio::fs::read(path).await.map_err(CCSwarmError::from)?;
        Self::from_bytes(&bytes)
    }

  ./crates/ccswarm/src/traits.rs:458-465 function test_generate_id_macro <-> ./crates/ccswarm/src/traits.rs:487-494 function test_cache_stats
  Similarity: 74.74%

[36m--- ./crates/ccswarm/src/traits.rs:test_generate_id_macro (lines 458-465) ---[0m
    fn test_generate_id_macro() {
        let id1 = generate_id!("test");
        let id2 = generate_id!("test");

        assert!(id1.starts_with("test-"));
        assert!(id2.starts_with("test-"));
        assert_ne!(id1, id2);
    }

[36m--- ./crates/ccswarm/src/traits.rs:test_cache_stats (lines 487-494) ---[0m
    fn test_cache_stats() {
        let mut stats = CacheStats::new();
        stats.hits = 80;
        stats.misses = 20;
        stats.calculate_hit_rate();

        assert_eq!(stats.hit_rate, 0.8);
    }

  ./crates/ccswarm/src/traits.rs:216-223 method calculate_hit_rate <-> ./crates/ccswarm/src/traits.rs:357-360 function save_to_file
  Similarity: 83.08%

[36m--- ./crates/ccswarm/src/traits.rs:calculate_hit_rate (lines 216-223) ---[0m
    pub fn calculate_hit_rate(&mut self) {
        let total = self.hits + self.misses;
        self.hit_rate = if total > 0 {
            self.hits as f64 / total as f64
        } else {
            0.0
        };
    }

[36m--- ./crates/ccswarm/src/traits.rs:save_to_file (lines 357-360) ---[0m
    async fn save_to_file<P: AsRef<std::path::Path> + Send>(&self, path: P) -> Result<()> {
        let bytes = self.to_bytes()?;
        tokio::fs::write(path, bytes).await.map_err(CCSwarmError::from)
    }

  ./crates/ccswarm/src/traits.rs:357-360 function save_to_file <-> ./crates/ccswarm/src/traits.rs:363-369 function load_from_file
  Similarity: 84.22%

[36m--- ./crates/ccswarm/src/traits.rs:save_to_file (lines 357-360) ---[0m
    async fn save_to_file<P: AsRef<std::path::Path> + Send>(&self, path: P) -> Result<()> {
        let bytes = self.to_bytes()?;
        tokio::fs::write(path, bytes).await.map_err(CCSwarmError::from)
    }

[36m--- ./crates/ccswarm/src/traits.rs:load_from_file (lines 363-369) ---[0m
    async fn load_from_file<P: AsRef<std::path::Path> + Send>(path: P) -> Result<Self>
    where
        Self: Sized,
    {
        let bytes = tokio::fs::read(path).await.map_err(CCSwarmError::from)?;
        Self::from_bytes(&bytes)
    }


Duplicates in ./crates/ccswarm/src/template/storage.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/template/storage.rs:182-257 method apply_query_filters <-> ./crates/ccswarm/src/template/storage.rs:348-392 method get_stats
  Similarity: 83.98%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:apply_query_filters (lines 182-257) ---[0m
    fn apply_query_filters(
        &self,
        templates: Vec<Template>,
        query: &TemplateQuery,
    ) -> Vec<Template> {
        let mut filtered: Vec<Template> = templates
            .into_iter()
            .filter(|template| {
                // Search term filter
                if let Some(term) = &query.search_term {
                    let term_lower = term.to_lowercase();
                    if !template.name.to_lowercase().contains(&term_lower)
                        && !template.description.to_lowercase().contains(&term_lower)
                        && !template
                            .tags
                            .iter()
                            .any(|tag| tag.to_lowercase().contains(&term_lower))
                    {
                        return false;
                    }
                }

                // Category filter
                if let Some(ref category) = query.category {
                    if template.category != *category {
                        return false;
                    }
                }

                // Tags filter
                if !query.tags.is_empty()
                    && !query.tags.iter().any(|tag| template.tags.contains(tag))
                {
                    return false;
                }

                // Author filter
                if let Some(ref author) = query.author {
                    if template.author.as_ref() != Some(author) {
                        return false;
                    }
                }

                // Success rate filter
                if let Some(min_rate) = query.min_success_rate {
                    if template.success_rate.unwrap_or(0.0) < min_rate {
                        return false;
                    }
                }

                true
            })
            .collect();

        // Apply sorting
        if query.sort_by_popularity {
            filtered.sort_by(|a, b| b.usage_count.cmp(&a.usage_count));
        } else if query.sort_by_success_rate {
            filtered.sort_by(|a, b| {
                let a_rate = a.success_rate.unwrap_or(0.0);
                let b_rate = b.success_rate.unwrap_or(0.0);
                b_rate
                    .partial_cmp(&a_rate)
                    .unwrap_or(std::cmp::Ordering::Equal)
            });
        } else if query.sort_by_date {
            filtered.sort_by(|a, b| b.created_at.cmp(&a.created_at));
        }

        // Apply limit
        if let Some(limit) = query.limit {
            filtered.truncate(limit);
        }

        filtered
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:get_stats (lines 348-392) ---[0m
    async fn get_stats(&self) -> Result<TemplateStats, TemplateError> {
        let templates = self.list_templates().await?;

        let mut by_category = HashMap::new();
        let mut total_usage = 0;
        let mut total_success_rate = 0.0;
        let mut templates_with_rate = 0;

        for template in &templates {
            // Count by category
            let category_name = template.category.to_string();
            *by_category.entry(category_name).or_insert(0) += 1;

            // Total usage
            total_usage += template.usage_count;

            // Average success rate
            if let Some(rate) = template.success_rate {
                total_success_rate += rate;
                templates_with_rate += 1;
            }
        }

        // Most popular templates
        let mut popularity: Vec<_> = templates
            .iter()
            .map(|t| (t.name.clone(), t.usage_count))
            .collect();
        popularity.sort_by(|a, b| b.1.cmp(&a.1));
        popularity.truncate(10); // Top 10

        let average_success_rate = if templates_with_rate > 0 {
            total_success_rate / templates_with_rate as f64
        } else {
            0.0
        };

        Ok(TemplateStats {
            total_templates: templates.len(),
            by_category,
            most_popular: popularity,
            average_success_rate,
            total_usage,
        })
    }

  ./crates/ccswarm/src/template/storage.rs:104-139 method refresh_cache <-> ./crates/ccswarm/src/template/storage.rs:182-257 method apply_query_filters
  Similarity: 89.28%
  Classes: FileSystemTemplateStorage <-> FileSystemTemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:refresh_cache (lines 104-139) ---[0m
    async fn refresh_cache(&mut self) -> Result<(), TemplateError> {
        debug!("Refreshing template cache");

        let templates_dir = self.templates_dir();
        if !templates_dir.exists() {
            return Ok(());
        }

        let mut entries = async_fs::read_dir(&templates_dir)
            .await
            .context("Failed to read templates directory")?;

        self.cache.clear();

        while let Some(entry) = entries
            .next_entry()
            .await
            .context("Failed to read directory entry")?
        {
            let path = entry.path();
            if path.extension().is_some_and(|ext| ext == "json") {
                match self.load_template_from_file(&path).await {
                    Ok(template) => {
                        self.cache.insert(template.id.clone(), template);
                    }
                    Err(e) => {
                        warn!("Failed to load template from {:?}: {}", path, e);
                    }
                }
            }
        }

        self.cache_valid = true;
        info!("Loaded {} templates into cache", self.cache.len());
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:apply_query_filters (lines 182-257) ---[0m
    fn apply_query_filters(
        &self,
        templates: Vec<Template>,
        query: &TemplateQuery,
    ) -> Vec<Template> {
        let mut filtered: Vec<Template> = templates
            .into_iter()
            .filter(|template| {
                // Search term filter
                if let Some(term) = &query.search_term {
                    let term_lower = term.to_lowercase();
                    if !template.name.to_lowercase().contains(&term_lower)
                        && !template.description.to_lowercase().contains(&term_lower)
                        && !template
                            .tags
                            .iter()
                            .any(|tag| tag.to_lowercase().contains(&term_lower))
                    {
                        return false;
                    }
                }

                // Category filter
                if let Some(ref category) = query.category {
                    if template.category != *category {
                        return false;
                    }
                }

                // Tags filter
                if !query.tags.is_empty()
                    && !query.tags.iter().any(|tag| template.tags.contains(tag))
                {
                    return false;
                }

                // Author filter
                if let Some(ref author) = query.author {
                    if template.author.as_ref() != Some(author) {
                        return false;
                    }
                }

                // Success rate filter
                if let Some(min_rate) = query.min_success_rate {
                    if template.success_rate.unwrap_or(0.0) < min_rate {
                        return false;
                    }
                }

                true
            })
            .collect();

        // Apply sorting
        if query.sort_by_popularity {
            filtered.sort_by(|a, b| b.usage_count.cmp(&a.usage_count));
        } else if query.sort_by_success_rate {
            filtered.sort_by(|a, b| {
                let a_rate = a.success_rate.unwrap_or(0.0);
                let b_rate = b.success_rate.unwrap_or(0.0);
                b_rate
                    .partial_cmp(&a_rate)
                    .unwrap_or(std::cmp::Ordering::Equal)
            });
        } else if query.sort_by_date {
            filtered.sort_by(|a, b| b.created_at.cmp(&a.created_at));
        }

        // Apply limit
        if let Some(limit) = query.limit {
            filtered.truncate(limit);
        }

        filtered
    }

  ./crates/ccswarm/src/template/storage.rs:182-257 method apply_query_filters <-> ./crates/ccswarm/src/template/storage.rs:491-530 method get_stats
  Similarity: 84.83%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:apply_query_filters (lines 182-257) ---[0m
    fn apply_query_filters(
        &self,
        templates: Vec<Template>,
        query: &TemplateQuery,
    ) -> Vec<Template> {
        let mut filtered: Vec<Template> = templates
            .into_iter()
            .filter(|template| {
                // Search term filter
                if let Some(term) = &query.search_term {
                    let term_lower = term.to_lowercase();
                    if !template.name.to_lowercase().contains(&term_lower)
                        && !template.description.to_lowercase().contains(&term_lower)
                        && !template
                            .tags
                            .iter()
                            .any(|tag| tag.to_lowercase().contains(&term_lower))
                    {
                        return false;
                    }
                }

                // Category filter
                if let Some(ref category) = query.category {
                    if template.category != *category {
                        return false;
                    }
                }

                // Tags filter
                if !query.tags.is_empty()
                    && !query.tags.iter().any(|tag| template.tags.contains(tag))
                {
                    return false;
                }

                // Author filter
                if let Some(ref author) = query.author {
                    if template.author.as_ref() != Some(author) {
                        return false;
                    }
                }

                // Success rate filter
                if let Some(min_rate) = query.min_success_rate {
                    if template.success_rate.unwrap_or(0.0) < min_rate {
                        return false;
                    }
                }

                true
            })
            .collect();

        // Apply sorting
        if query.sort_by_popularity {
            filtered.sort_by(|a, b| b.usage_count.cmp(&a.usage_count));
        } else if query.sort_by_success_rate {
            filtered.sort_by(|a, b| {
                let a_rate = a.success_rate.unwrap_or(0.0);
                let b_rate = b.success_rate.unwrap_or(0.0);
                b_rate
                    .partial_cmp(&a_rate)
                    .unwrap_or(std::cmp::Ordering::Equal)
            });
        } else if query.sort_by_date {
            filtered.sort_by(|a, b| b.created_at.cmp(&a.created_at));
        }

        // Apply limit
        if let Some(limit) = query.limit {
            filtered.truncate(limit);
        }

        filtered
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:get_stats (lines 491-530) ---[0m
    async fn get_stats(&self) -> Result<TemplateStats, TemplateError> {
        let templates: Vec<_> = self.templates.values().cloned().collect();

        let mut by_category = HashMap::new();
        let mut total_usage = 0;
        let mut total_success_rate = 0.0;
        let mut templates_with_rate = 0;

        for template in &templates {
            let category_name = template.category.to_string();
            *by_category.entry(category_name).or_insert(0) += 1;
            total_usage += template.usage_count;

            if let Some(rate) = template.success_rate {
                total_success_rate += rate;
                templates_with_rate += 1;
            }
        }

        let mut popularity: Vec<_> = templates
            .iter()
            .map(|t| (t.name.clone(), t.usage_count))
            .collect();
        popularity.sort_by(|a, b| b.1.cmp(&a.1));
        popularity.truncate(10);

        let average_success_rate = if templates_with_rate > 0 {
            total_success_rate / templates_with_rate as f64
        } else {
            0.0
        };

        Ok(TemplateStats {
            total_templates: templates.len(),
            by_category,
            most_popular: popularity,
            average_success_rate,
            total_usage,
        })
    }

  ./crates/ccswarm/src/template/storage.rs:348-392 method get_stats <-> ./crates/ccswarm/src/template/storage.rs:491-530 method get_stats
  Similarity: 97.27%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:get_stats (lines 348-392) ---[0m
    async fn get_stats(&self) -> Result<TemplateStats, TemplateError> {
        let templates = self.list_templates().await?;

        let mut by_category = HashMap::new();
        let mut total_usage = 0;
        let mut total_success_rate = 0.0;
        let mut templates_with_rate = 0;

        for template in &templates {
            // Count by category
            let category_name = template.category.to_string();
            *by_category.entry(category_name).or_insert(0) += 1;

            // Total usage
            total_usage += template.usage_count;

            // Average success rate
            if let Some(rate) = template.success_rate {
                total_success_rate += rate;
                templates_with_rate += 1;
            }
        }

        // Most popular templates
        let mut popularity: Vec<_> = templates
            .iter()
            .map(|t| (t.name.clone(), t.usage_count))
            .collect();
        popularity.sort_by(|a, b| b.1.cmp(&a.1));
        popularity.truncate(10); // Top 10

        let average_success_rate = if templates_with_rate > 0 {
            total_success_rate / templates_with_rate as f64
        } else {
            0.0
        };

        Ok(TemplateStats {
            total_templates: templates.len(),
            by_category,
            most_popular: popularity,
            average_success_rate,
            total_usage,
        })
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:get_stats (lines 491-530) ---[0m
    async fn get_stats(&self) -> Result<TemplateStats, TemplateError> {
        let templates: Vec<_> = self.templates.values().cloned().collect();

        let mut by_category = HashMap::new();
        let mut total_usage = 0;
        let mut total_success_rate = 0.0;
        let mut templates_with_rate = 0;

        for template in &templates {
            let category_name = template.category.to_string();
            *by_category.entry(category_name).or_insert(0) += 1;
            total_usage += template.usage_count;

            if let Some(rate) = template.success_rate {
                total_success_rate += rate;
                templates_with_rate += 1;
            }
        }

        let mut popularity: Vec<_> = templates
            .iter()
            .map(|t| (t.name.clone(), t.usage_count))
            .collect();
        popularity.sort_by(|a, b| b.1.cmp(&a.1));
        popularity.truncate(10);

        let average_success_rate = if templates_with_rate > 0 {
            total_success_rate / templates_with_rate as f64
        } else {
            0.0
        };

        Ok(TemplateStats {
            total_templates: templates.len(),
            by_category,
            most_popular: popularity,
            average_success_rate,
            total_usage,
        })
    }

  ./crates/ccswarm/src/template/storage.rs:104-139 method refresh_cache <-> ./crates/ccswarm/src/template/storage.rs:348-392 method get_stats
  Similarity: 75.56%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:refresh_cache (lines 104-139) ---[0m
    async fn refresh_cache(&mut self) -> Result<(), TemplateError> {
        debug!("Refreshing template cache");

        let templates_dir = self.templates_dir();
        if !templates_dir.exists() {
            return Ok(());
        }

        let mut entries = async_fs::read_dir(&templates_dir)
            .await
            .context("Failed to read templates directory")?;

        self.cache.clear();

        while let Some(entry) = entries
            .next_entry()
            .await
            .context("Failed to read directory entry")?
        {
            let path = entry.path();
            if path.extension().is_some_and(|ext| ext == "json") {
                match self.load_template_from_file(&path).await {
                    Ok(template) => {
                        self.cache.insert(template.id.clone(), template);
                    }
                    Err(e) => {
                        warn!("Failed to load template from {:?}: {}", path, e);
                    }
                }
            }
        }

        self.cache_valid = true;
        info!("Loaded {} templates into cache", self.cache.len());
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:get_stats (lines 348-392) ---[0m
    async fn get_stats(&self) -> Result<TemplateStats, TemplateError> {
        let templates = self.list_templates().await?;

        let mut by_category = HashMap::new();
        let mut total_usage = 0;
        let mut total_success_rate = 0.0;
        let mut templates_with_rate = 0;

        for template in &templates {
            // Count by category
            let category_name = template.category.to_string();
            *by_category.entry(category_name).or_insert(0) += 1;

            // Total usage
            total_usage += template.usage_count;

            // Average success rate
            if let Some(rate) = template.success_rate {
                total_success_rate += rate;
                templates_with_rate += 1;
            }
        }

        // Most popular templates
        let mut popularity: Vec<_> = templates
            .iter()
            .map(|t| (t.name.clone(), t.usage_count))
            .collect();
        popularity.sort_by(|a, b| b.1.cmp(&a.1));
        popularity.truncate(10); // Top 10

        let average_success_rate = if templates_with_rate > 0 {
            total_success_rate / templates_with_rate as f64
        } else {
            0.0
        };

        Ok(TemplateStats {
            total_templates: templates.len(),
            by_category,
            most_popular: popularity,
            average_success_rate,
            total_usage,
        })
    }

  ./crates/ccswarm/src/template/storage.rs:104-139 method refresh_cache <-> ./crates/ccswarm/src/template/storage.rs:491-530 method get_stats
  Similarity: 75.91%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:refresh_cache (lines 104-139) ---[0m
    async fn refresh_cache(&mut self) -> Result<(), TemplateError> {
        debug!("Refreshing template cache");

        let templates_dir = self.templates_dir();
        if !templates_dir.exists() {
            return Ok(());
        }

        let mut entries = async_fs::read_dir(&templates_dir)
            .await
            .context("Failed to read templates directory")?;

        self.cache.clear();

        while let Some(entry) = entries
            .next_entry()
            .await
            .context("Failed to read directory entry")?
        {
            let path = entry.path();
            if path.extension().is_some_and(|ext| ext == "json") {
                match self.load_template_from_file(&path).await {
                    Ok(template) => {
                        self.cache.insert(template.id.clone(), template);
                    }
                    Err(e) => {
                        warn!("Failed to load template from {:?}: {}", path, e);
                    }
                }
            }
        }

        self.cache_valid = true;
        info!("Loaded {} templates into cache", self.cache.len());
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:get_stats (lines 491-530) ---[0m
    async fn get_stats(&self) -> Result<TemplateStats, TemplateError> {
        let templates: Vec<_> = self.templates.values().cloned().collect();

        let mut by_category = HashMap::new();
        let mut total_usage = 0;
        let mut total_success_rate = 0.0;
        let mut templates_with_rate = 0;

        for template in &templates {
            let category_name = template.category.to_string();
            *by_category.entry(category_name).or_insert(0) += 1;
            total_usage += template.usage_count;

            if let Some(rate) = template.success_rate {
                total_success_rate += rate;
                templates_with_rate += 1;
            }
        }

        let mut popularity: Vec<_> = templates
            .iter()
            .map(|t| (t.name.clone(), t.usage_count))
            .collect();
        popularity.sort_by(|a, b| b.1.cmp(&a.1));
        popularity.truncate(10);

        let average_success_rate = if templates_with_rate > 0 {
            total_success_rate / templates_with_rate as f64
        } else {
            0.0
        };

        Ok(TemplateStats {
            total_templates: templates.len(),
            by_category,
            most_popular: popularity,
            average_success_rate,
            total_usage,
        })
    }

  ./crates/ccswarm/src/template/storage.rs:262-285 method save_template <-> ./crates/ccswarm/src/template/storage.rs:348-392 method get_stats
  Similarity: 83.11%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:save_template (lines 262-285) ---[0m
    async fn save_template(&mut self, template: &Template) -> Result<(), TemplateError> {
        // Validate template
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template has invalid or missing required fields".to_string(),
            });
        }

        // Check if template already exists
        if self.exists(&template.id).await? {
            return Err(TemplateError::AlreadyExists {
                name: template.id.clone(),
            });
        }

        // Save to file
        self.save_template_to_file(template).await?;

        // Update cache
        self.cache.insert(template.id.clone(), template.clone());

        info!("Saved template: {}", template.id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:get_stats (lines 348-392) ---[0m
    async fn get_stats(&self) -> Result<TemplateStats, TemplateError> {
        let templates = self.list_templates().await?;

        let mut by_category = HashMap::new();
        let mut total_usage = 0;
        let mut total_success_rate = 0.0;
        let mut templates_with_rate = 0;

        for template in &templates {
            // Count by category
            let category_name = template.category.to_string();
            *by_category.entry(category_name).or_insert(0) += 1;

            // Total usage
            total_usage += template.usage_count;

            // Average success rate
            if let Some(rate) = template.success_rate {
                total_success_rate += rate;
                templates_with_rate += 1;
            }
        }

        // Most popular templates
        let mut popularity: Vec<_> = templates
            .iter()
            .map(|t| (t.name.clone(), t.usage_count))
            .collect();
        popularity.sort_by(|a, b| b.1.cmp(&a.1));
        popularity.truncate(10); // Top 10

        let average_success_rate = if templates_with_rate > 0 {
            total_success_rate / templates_with_rate as f64
        } else {
            0.0
        };

        Ok(TemplateStats {
            total_templates: templates.len(),
            by_category,
            most_popular: popularity,
            average_success_rate,
            total_usage,
        })
    }

  ./crates/ccswarm/src/template/storage.rs:104-139 method refresh_cache <-> ./crates/ccswarm/src/template/storage.rs:262-285 method save_template
  Similarity: 89.35%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:refresh_cache (lines 104-139) ---[0m
    async fn refresh_cache(&mut self) -> Result<(), TemplateError> {
        debug!("Refreshing template cache");

        let templates_dir = self.templates_dir();
        if !templates_dir.exists() {
            return Ok(());
        }

        let mut entries = async_fs::read_dir(&templates_dir)
            .await
            .context("Failed to read templates directory")?;

        self.cache.clear();

        while let Some(entry) = entries
            .next_entry()
            .await
            .context("Failed to read directory entry")?
        {
            let path = entry.path();
            if path.extension().is_some_and(|ext| ext == "json") {
                match self.load_template_from_file(&path).await {
                    Ok(template) => {
                        self.cache.insert(template.id.clone(), template);
                    }
                    Err(e) => {
                        warn!("Failed to load template from {:?}: {}", path, e);
                    }
                }
            }
        }

        self.cache_valid = true;
        info!("Loaded {} templates into cache", self.cache.len());
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:save_template (lines 262-285) ---[0m
    async fn save_template(&mut self, template: &Template) -> Result<(), TemplateError> {
        // Validate template
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template has invalid or missing required fields".to_string(),
            });
        }

        // Check if template already exists
        if self.exists(&template.id).await? {
            return Err(TemplateError::AlreadyExists {
                name: template.id.clone(),
            });
        }

        // Save to file
        self.save_template_to_file(template).await?;

        // Update cache
        self.cache.insert(template.id.clone(), template.clone());

        info!("Saved template: {}", template.id);
        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:262-285 method save_template <-> ./crates/ccswarm/src/template/storage.rs:491-530 method get_stats
  Similarity: 83.58%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:save_template (lines 262-285) ---[0m
    async fn save_template(&mut self, template: &Template) -> Result<(), TemplateError> {
        // Validate template
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template has invalid or missing required fields".to_string(),
            });
        }

        // Check if template already exists
        if self.exists(&template.id).await? {
            return Err(TemplateError::AlreadyExists {
                name: template.id.clone(),
            });
        }

        // Save to file
        self.save_template_to_file(template).await?;

        // Update cache
        self.cache.insert(template.id.clone(), template.clone());

        info!("Saved template: {}", template.id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:get_stats (lines 491-530) ---[0m
    async fn get_stats(&self) -> Result<TemplateStats, TemplateError> {
        let templates: Vec<_> = self.templates.values().cloned().collect();

        let mut by_category = HashMap::new();
        let mut total_usage = 0;
        let mut total_success_rate = 0.0;
        let mut templates_with_rate = 0;

        for template in &templates {
            let category_name = template.category.to_string();
            *by_category.entry(category_name).or_insert(0) += 1;
            total_usage += template.usage_count;

            if let Some(rate) = template.success_rate {
                total_success_rate += rate;
                templates_with_rate += 1;
            }
        }

        let mut popularity: Vec<_> = templates
            .iter()
            .map(|t| (t.name.clone(), t.usage_count))
            .collect();
        popularity.sort_by(|a, b| b.1.cmp(&a.1));
        popularity.truncate(10);

        let average_success_rate = if templates_with_rate > 0 {
            total_success_rate / templates_with_rate as f64
        } else {
            0.0
        };

        Ok(TemplateStats {
            total_templates: templates.len(),
            by_category,
            most_popular: popularity,
            average_success_rate,
            total_usage,
        })
    }

  ./crates/ccswarm/src/template/storage.rs:68-91 method new <-> ./crates/ccswarm/src/template/storage.rs:262-285 method save_template
  Similarity: 82.57%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:new (lines 68-91) ---[0m
    pub async fn new(base_dir: impl AsRef<Path>) -> Result<Self, TemplateError> {
        let base_dir = base_dir.as_ref().to_path_buf();

        // Create directories if they don't exist
        async_fs::create_dir_all(&base_dir)
            .await
            .context("Failed to create template storage directory")?;

        let templates_dir = base_dir.join("templates");
        async_fs::create_dir_all(&templates_dir)
            .await
            .context("Failed to create templates directory")?;

        let mut storage = Self {
            base_dir,
            cache: HashMap::new(),
            cache_valid: false,
        };

        // Load existing templates into cache
        storage.refresh_cache().await?;

        Ok(storage)
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:save_template (lines 262-285) ---[0m
    async fn save_template(&mut self, template: &Template) -> Result<(), TemplateError> {
        // Validate template
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template has invalid or missing required fields".to_string(),
            });
        }

        // Check if template already exists
        if self.exists(&template.id).await? {
            return Err(TemplateError::AlreadyExists {
                name: template.id.clone(),
            });
        }

        // Save to file
        self.save_template_to_file(template).await?;

        // Update cache
        self.cache.insert(template.id.clone(), template.clone());

        info!("Saved template: {}", template.id);
        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:262-285 method save_template <-> ./crates/ccswarm/src/template/storage.rs:306-324 method delete_template
  Similarity: 89.25%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:save_template (lines 262-285) ---[0m
    async fn save_template(&mut self, template: &Template) -> Result<(), TemplateError> {
        // Validate template
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template has invalid or missing required fields".to_string(),
            });
        }

        // Check if template already exists
        if self.exists(&template.id).await? {
            return Err(TemplateError::AlreadyExists {
                name: template.id.clone(),
            });
        }

        // Save to file
        self.save_template_to_file(template).await?;

        // Update cache
        self.cache.insert(template.id.clone(), template.clone());

        info!("Saved template: {}", template.id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:delete_template (lines 306-324) ---[0m
    async fn delete_template(&mut self, id: &str) -> Result<(), TemplateError> {
        let path = self.template_path(id);
        if !path.exists() {
            return Err(TemplateError::NotFound {
                name: id.to_string(),
            });
        }

        // Delete file
        async_fs::remove_file(&path)
            .await
            .context("Failed to delete template file")?;

        // Remove from cache
        self.cache.remove(id);

        info!("Deleted template: {}", id);
        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:68-91 method new <-> ./crates/ccswarm/src/template/storage.rs:306-324 method delete_template
  Similarity: 84.38%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:new (lines 68-91) ---[0m
    pub async fn new(base_dir: impl AsRef<Path>) -> Result<Self, TemplateError> {
        let base_dir = base_dir.as_ref().to_path_buf();

        // Create directories if they don't exist
        async_fs::create_dir_all(&base_dir)
            .await
            .context("Failed to create template storage directory")?;

        let templates_dir = base_dir.join("templates");
        async_fs::create_dir_all(&templates_dir)
            .await
            .context("Failed to create templates directory")?;

        let mut storage = Self {
            base_dir,
            cache: HashMap::new(),
            cache_valid: false,
        };

        // Load existing templates into cache
        storage.refresh_cache().await?;

        Ok(storage)
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:delete_template (lines 306-324) ---[0m
    async fn delete_template(&mut self, id: &str) -> Result<(), TemplateError> {
        let path = self.template_path(id);
        if !path.exists() {
            return Err(TemplateError::NotFound {
                name: id.to_string(),
            });
        }

        // Delete file
        async_fs::remove_file(&path)
            .await
            .context("Failed to delete template file")?;

        // Remove from cache
        self.cache.remove(id);

        info!("Deleted template: {}", id);
        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:262-285 method save_template <-> ./crates/ccswarm/src/template/storage.rs:394-409 method update_usage
  Similarity: 86.79%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:save_template (lines 262-285) ---[0m
    async fn save_template(&mut self, template: &Template) -> Result<(), TemplateError> {
        // Validate template
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template has invalid or missing required fields".to_string(),
            });
        }

        // Check if template already exists
        if self.exists(&template.id).await? {
            return Err(TemplateError::AlreadyExists {
                name: template.id.clone(),
            });
        }

        // Save to file
        self.save_template_to_file(template).await?;

        // Update cache
        self.cache.insert(template.id.clone(), template.clone());

        info!("Saved template: {}", template.id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:update_usage (lines 394-409) ---[0m
    async fn update_usage(&mut self, id: &str, success: bool) -> Result<(), TemplateError> {
        // Load template
        let mut template = self.load_template(id).await?;

        // Update statistics
        template.increment_usage();
        template.update_success_rate(success);

        // Save updated template
        self.save_template_to_file(&template).await?;

        // Update cache
        self.cache.insert(id.to_string(), template);

        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:262-285 method save_template <-> ./crates/ccswarm/src/template/storage.rs:287-304 method load_template
  Similarity: 80.91%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:save_template (lines 262-285) ---[0m
    async fn save_template(&mut self, template: &Template) -> Result<(), TemplateError> {
        // Validate template
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template has invalid or missing required fields".to_string(),
            });
        }

        // Check if template already exists
        if self.exists(&template.id).await? {
            return Err(TemplateError::AlreadyExists {
                name: template.id.clone(),
            });
        }

        // Save to file
        self.save_template_to_file(template).await?;

        // Update cache
        self.cache.insert(template.id.clone(), template.clone());

        info!("Saved template: {}", template.id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:load_template (lines 287-304) ---[0m
    async fn load_template(&self, id: &str) -> Result<Template, TemplateError> {
        // Try cache first
        if self.cache_valid {
            if let Some(template) = self.cache.get(id) {
                return Ok(template.clone());
            }
        }

        // Load from file
        let path = self.template_path(id);
        if !path.exists() {
            return Err(TemplateError::NotFound {
                name: id.to_string(),
            });
        }

        self.load_template_from_file(&path).await
    }

  ./crates/ccswarm/src/template/storage.rs:68-91 method new <-> ./crates/ccswarm/src/template/storage.rs:394-409 method update_usage
  Similarity: 81.63%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:new (lines 68-91) ---[0m
    pub async fn new(base_dir: impl AsRef<Path>) -> Result<Self, TemplateError> {
        let base_dir = base_dir.as_ref().to_path_buf();

        // Create directories if they don't exist
        async_fs::create_dir_all(&base_dir)
            .await
            .context("Failed to create template storage directory")?;

        let templates_dir = base_dir.join("templates");
        async_fs::create_dir_all(&templates_dir)
            .await
            .context("Failed to create templates directory")?;

        let mut storage = Self {
            base_dir,
            cache: HashMap::new(),
            cache_valid: false,
        };

        // Load existing templates into cache
        storage.refresh_cache().await?;

        Ok(storage)
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:update_usage (lines 394-409) ---[0m
    async fn update_usage(&mut self, id: &str, success: bool) -> Result<(), TemplateError> {
        // Load template
        let mut template = self.load_template(id).await?;

        // Update statistics
        template.increment_usage();
        template.update_success_rate(success);

        // Save updated template
        self.save_template_to_file(&template).await?;

        // Update cache
        self.cache.insert(id.to_string(), template);

        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:262-285 method save_template <-> ./crates/ccswarm/src/template/storage.rs:434-449 method save_template
  Similarity: 78.72%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:save_template (lines 262-285) ---[0m
    async fn save_template(&mut self, template: &Template) -> Result<(), TemplateError> {
        // Validate template
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template has invalid or missing required fields".to_string(),
            });
        }

        // Check if template already exists
        if self.exists(&template.id).await? {
            return Err(TemplateError::AlreadyExists {
                name: template.id.clone(),
            });
        }

        // Save to file
        self.save_template_to_file(template).await?;

        // Update cache
        self.cache.insert(template.id.clone(), template.clone());

        info!("Saved template: {}", template.id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:save_template (lines 434-449) ---[0m
    async fn save_template(&mut self, template: &Template) -> Result<(), TemplateError> {
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template has invalid or missing required fields".to_string(),
            });
        }

        if self.templates.contains_key(&template.id) {
            return Err(TemplateError::AlreadyExists {
                name: template.id.clone(),
            });
        }

        self.templates.insert(template.id.clone(), template.clone());
        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:68-91 method new <-> ./crates/ccswarm/src/template/storage.rs:287-304 method load_template
  Similarity: 71.31%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:new (lines 68-91) ---[0m
    pub async fn new(base_dir: impl AsRef<Path>) -> Result<Self, TemplateError> {
        let base_dir = base_dir.as_ref().to_path_buf();

        // Create directories if they don't exist
        async_fs::create_dir_all(&base_dir)
            .await
            .context("Failed to create template storage directory")?;

        let templates_dir = base_dir.join("templates");
        async_fs::create_dir_all(&templates_dir)
            .await
            .context("Failed to create templates directory")?;

        let mut storage = Self {
            base_dir,
            cache: HashMap::new(),
            cache_valid: false,
        };

        // Load existing templates into cache
        storage.refresh_cache().await?;

        Ok(storage)
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:load_template (lines 287-304) ---[0m
    async fn load_template(&self, id: &str) -> Result<Template, TemplateError> {
        // Try cache first
        if self.cache_valid {
            if let Some(template) = self.cache.get(id) {
                return Ok(template.clone());
            }
        }

        // Load from file
        let path = self.template_path(id);
        if !path.exists() {
            return Err(TemplateError::NotFound {
                name: id.to_string(),
            });
        }

        self.load_template_from_file(&path).await
    }

  ./crates/ccswarm/src/template/storage.rs:287-304 method load_template <-> ./crates/ccswarm/src/template/storage.rs:306-324 method delete_template
  Similarity: 77.33%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:load_template (lines 287-304) ---[0m
    async fn load_template(&self, id: &str) -> Result<Template, TemplateError> {
        // Try cache first
        if self.cache_valid {
            if let Some(template) = self.cache.get(id) {
                return Ok(template.clone());
            }
        }

        // Load from file
        let path = self.template_path(id);
        if !path.exists() {
            return Err(TemplateError::NotFound {
                name: id.to_string(),
            });
        }

        self.load_template_from_file(&path).await
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:delete_template (lines 306-324) ---[0m
    async fn delete_template(&mut self, id: &str) -> Result<(), TemplateError> {
        let path = self.template_path(id);
        if !path.exists() {
            return Err(TemplateError::NotFound {
                name: id.to_string(),
            });
        }

        // Delete file
        async_fs::remove_file(&path)
            .await
            .context("Failed to delete template file")?;

        // Remove from cache
        self.cache.remove(id);

        info!("Deleted template: {}", id);
        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:306-324 method delete_template <-> ./crates/ccswarm/src/template/storage.rs:394-409 method update_usage
  Similarity: 80.08%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:delete_template (lines 306-324) ---[0m
    async fn delete_template(&mut self, id: &str) -> Result<(), TemplateError> {
        let path = self.template_path(id);
        if !path.exists() {
            return Err(TemplateError::NotFound {
                name: id.to_string(),
            });
        }

        // Delete file
        async_fs::remove_file(&path)
            .await
            .context("Failed to delete template file")?;

        // Remove from cache
        self.cache.remove(id);

        info!("Deleted template: {}", id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:update_usage (lines 394-409) ---[0m
    async fn update_usage(&mut self, id: &str, success: bool) -> Result<(), TemplateError> {
        // Load template
        let mut template = self.load_template(id).await?;

        // Update statistics
        template.increment_usage();
        template.update_success_rate(success);

        // Save updated template
        self.save_template_to_file(&template).await?;

        // Update cache
        self.cache.insert(id.to_string(), template);

        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:287-304 method load_template <-> ./crates/ccswarm/src/template/storage.rs:434-449 method save_template
  Similarity: 76.76%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:load_template (lines 287-304) ---[0m
    async fn load_template(&self, id: &str) -> Result<Template, TemplateError> {
        // Try cache first
        if self.cache_valid {
            if let Some(template) = self.cache.get(id) {
                return Ok(template.clone());
            }
        }

        // Load from file
        let path = self.template_path(id);
        if !path.exists() {
            return Err(TemplateError::NotFound {
                name: id.to_string(),
            });
        }

        self.load_template_from_file(&path).await
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:save_template (lines 434-449) ---[0m
    async fn save_template(&mut self, template: &Template) -> Result<(), TemplateError> {
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template has invalid or missing required fields".to_string(),
            });
        }

        if self.templates.contains_key(&template.id) {
            return Err(TemplateError::AlreadyExists {
                name: template.id.clone(),
            });
        }

        self.templates.insert(template.id.clone(), template.clone());
        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:434-449 method save_template <-> ./crates/ccswarm/src/template/storage.rs:473-485 method search_templates
  Similarity: 87.03%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:save_template (lines 434-449) ---[0m
    async fn save_template(&mut self, template: &Template) -> Result<(), TemplateError> {
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template has invalid or missing required fields".to_string(),
            });
        }

        if self.templates.contains_key(&template.id) {
            return Err(TemplateError::AlreadyExists {
                name: template.id.clone(),
            });
        }

        self.templates.insert(template.id.clone(), template.clone());
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:search_templates (lines 473-485) ---[0m
    async fn search_templates(
        &self,
        query: &TemplateQuery,
    ) -> Result<Vec<Template>, TemplateError> {
        let all_templates = self.list_templates().await?;
        // Reuse the filtering logic from FileSystemTemplateStorage
        let fs_storage = FileSystemTemplateStorage {
            base_dir: PathBuf::new(),
            cache: HashMap::new(),
            cache_valid: true,
        };
        Ok(fs_storage.apply_query_filters(all_templates, query))
    }

  ./crates/ccswarm/src/template/storage.rs:306-324 method delete_template <-> ./crates/ccswarm/src/template/storage.rs:434-449 method save_template
  Similarity: 70.41%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:delete_template (lines 306-324) ---[0m
    async fn delete_template(&mut self, id: &str) -> Result<(), TemplateError> {
        let path = self.template_path(id);
        if !path.exists() {
            return Err(TemplateError::NotFound {
                name: id.to_string(),
            });
        }

        // Delete file
        async_fs::remove_file(&path)
            .await
            .context("Failed to delete template file")?;

        // Remove from cache
        self.cache.remove(id);

        info!("Deleted template: {}", id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:save_template (lines 434-449) ---[0m
    async fn save_template(&mut self, template: &Template) -> Result<(), TemplateError> {
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template has invalid or missing required fields".to_string(),
            });
        }

        if self.templates.contains_key(&template.id) {
            return Err(TemplateError::AlreadyExists {
                name: template.id.clone(),
            });
        }

        self.templates.insert(template.id.clone(), template.clone());
        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:287-304 method load_template <-> ./crates/ccswarm/src/template/storage.rs:394-409 method update_usage
  Similarity: 72.29%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:load_template (lines 287-304) ---[0m
    async fn load_template(&self, id: &str) -> Result<Template, TemplateError> {
        // Try cache first
        if self.cache_valid {
            if let Some(template) = self.cache.get(id) {
                return Ok(template.clone());
            }
        }

        // Load from file
        let path = self.template_path(id);
        if !path.exists() {
            return Err(TemplateError::NotFound {
                name: id.to_string(),
            });
        }

        self.load_template_from_file(&path).await
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:update_usage (lines 394-409) ---[0m
    async fn update_usage(&mut self, id: &str, success: bool) -> Result<(), TemplateError> {
        // Load template
        let mut template = self.load_template(id).await?;

        // Update statistics
        template.increment_usage();
        template.update_success_rate(success);

        // Save updated template
        self.save_template_to_file(&template).await?;

        // Update cache
        self.cache.insert(id.to_string(), template);

        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:287-304 method load_template <-> ./crates/ccswarm/src/template/storage.rs:532-544 method update_usage
  Similarity: 77.94%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:load_template (lines 287-304) ---[0m
    async fn load_template(&self, id: &str) -> Result<Template, TemplateError> {
        // Try cache first
        if self.cache_valid {
            if let Some(template) = self.cache.get(id) {
                return Ok(template.clone());
            }
        }

        // Load from file
        let path = self.template_path(id);
        if !path.exists() {
            return Err(TemplateError::NotFound {
                name: id.to_string(),
            });
        }

        self.load_template_from_file(&path).await
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:update_usage (lines 532-544) ---[0m
    async fn update_usage(&mut self, id: &str, success: bool) -> Result<(), TemplateError> {
        let template = self
            .templates
            .get_mut(id)
            .ok_or_else(|| TemplateError::NotFound {
                name: id.to_string(),
            })?;

        template.increment_usage();
        template.update_success_rate(success);

        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:434-449 method save_template <-> ./crates/ccswarm/src/template/storage.rs:532-544 method update_usage
  Similarity: 82.07%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:save_template (lines 434-449) ---[0m
    async fn save_template(&mut self, template: &Template) -> Result<(), TemplateError> {
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template has invalid or missing required fields".to_string(),
            });
        }

        if self.templates.contains_key(&template.id) {
            return Err(TemplateError::AlreadyExists {
                name: template.id.clone(),
            });
        }

        self.templates.insert(template.id.clone(), template.clone());
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:update_usage (lines 532-544) ---[0m
    async fn update_usage(&mut self, id: &str, success: bool) -> Result<(), TemplateError> {
        let template = self
            .templates
            .get_mut(id)
            .ok_or_else(|| TemplateError::NotFound {
                name: id.to_string(),
            })?;

        template.increment_usage();
        template.update_success_rate(success);

        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:287-304 method load_template <-> ./crates/ccswarm/src/template/storage.rs:473-485 method search_templates
  Similarity: 74.73%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:load_template (lines 287-304) ---[0m
    async fn load_template(&self, id: &str) -> Result<Template, TemplateError> {
        // Try cache first
        if self.cache_valid {
            if let Some(template) = self.cache.get(id) {
                return Ok(template.clone());
            }
        }

        // Load from file
        let path = self.template_path(id);
        if !path.exists() {
            return Err(TemplateError::NotFound {
                name: id.to_string(),
            });
        }

        self.load_template_from_file(&path).await
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:search_templates (lines 473-485) ---[0m
    async fn search_templates(
        &self,
        query: &TemplateQuery,
    ) -> Result<Vec<Template>, TemplateError> {
        let all_templates = self.list_templates().await?;
        // Reuse the filtering logic from FileSystemTemplateStorage
        let fs_storage = FileSystemTemplateStorage {
            base_dir: PathBuf::new(),
            cache: HashMap::new(),
            cache_valid: true,
        };
        Ok(fs_storage.apply_query_filters(all_templates, query))
    }

  ./crates/ccswarm/src/template/storage.rs:306-324 method delete_template <-> ./crates/ccswarm/src/template/storage.rs:473-485 method search_templates
  Similarity: 70.47%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:delete_template (lines 306-324) ---[0m
    async fn delete_template(&mut self, id: &str) -> Result<(), TemplateError> {
        let path = self.template_path(id);
        if !path.exists() {
            return Err(TemplateError::NotFound {
                name: id.to_string(),
            });
        }

        // Delete file
        async_fs::remove_file(&path)
            .await
            .context("Failed to delete template file")?;

        // Remove from cache
        self.cache.remove(id);

        info!("Deleted template: {}", id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:search_templates (lines 473-485) ---[0m
    async fn search_templates(
        &self,
        query: &TemplateQuery,
    ) -> Result<Vec<Template>, TemplateError> {
        let all_templates = self.list_templates().await?;
        // Reuse the filtering logic from FileSystemTemplateStorage
        let fs_storage = FileSystemTemplateStorage {
            base_dir: PathBuf::new(),
            cache: HashMap::new(),
            cache_valid: true,
        };
        Ok(fs_storage.apply_query_filters(all_templates, query))
    }

  ./crates/ccswarm/src/template/storage.rs:154-164 method save_template_to_file <-> ./crates/ccswarm/src/template/storage.rs:287-304 method load_template
  Similarity: 77.18%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:save_template_to_file (lines 154-164) ---[0m
    async fn save_template_to_file(&self, template: &Template) -> Result<(), TemplateError> {
        let path = self.template_path(&template.id);
        let content =
            serde_json::to_string_pretty(template).context("Failed to serialize template")?;

        async_fs::write(&path, content)
            .await
            .context("Failed to write template file")?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:load_template (lines 287-304) ---[0m
    async fn load_template(&self, id: &str) -> Result<Template, TemplateError> {
        // Try cache first
        if self.cache_valid {
            if let Some(template) = self.cache.get(id) {
                return Ok(template.clone());
            }
        }

        // Load from file
        let path = self.template_path(id);
        if !path.exists() {
            return Err(TemplateError::NotFound {
                name: id.to_string(),
            });
        }

        self.load_template_from_file(&path).await
    }

  ./crates/ccswarm/src/template/storage.rs:154-164 method save_template_to_file <-> ./crates/ccswarm/src/template/storage.rs:434-449 method save_template
  Similarity: 79.93%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:save_template_to_file (lines 154-164) ---[0m
    async fn save_template_to_file(&self, template: &Template) -> Result<(), TemplateError> {
        let path = self.template_path(&template.id);
        let content =
            serde_json::to_string_pretty(template).context("Failed to serialize template")?;

        async_fs::write(&path, content)
            .await
            .context("Failed to write template file")?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:save_template (lines 434-449) ---[0m
    async fn save_template(&mut self, template: &Template) -> Result<(), TemplateError> {
        if !template.is_valid() {
            return Err(TemplateError::ValidationFailed {
                reason: "Template has invalid or missing required fields".to_string(),
            });
        }

        if self.templates.contains_key(&template.id) {
            return Err(TemplateError::AlreadyExists {
                name: template.id.clone(),
            });
        }

        self.templates.insert(template.id.clone(), template.clone());
        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:473-485 method search_templates <-> ./crates/ccswarm/src/template/storage.rs:532-544 method update_usage
  Similarity: 82.62%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:search_templates (lines 473-485) ---[0m
    async fn search_templates(
        &self,
        query: &TemplateQuery,
    ) -> Result<Vec<Template>, TemplateError> {
        let all_templates = self.list_templates().await?;
        // Reuse the filtering logic from FileSystemTemplateStorage
        let fs_storage = FileSystemTemplateStorage {
            base_dir: PathBuf::new(),
            cache: HashMap::new(),
            cache_valid: true,
        };
        Ok(fs_storage.apply_query_filters(all_templates, query))
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:update_usage (lines 532-544) ---[0m
    async fn update_usage(&mut self, id: &str, success: bool) -> Result<(), TemplateError> {
        let template = self
            .templates
            .get_mut(id)
            .ok_or_else(|| TemplateError::NotFound {
                name: id.to_string(),
            })?;

        template.increment_usage();
        template.update_success_rate(success);

        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:154-164 method save_template_to_file <-> ./crates/ccswarm/src/template/storage.rs:473-485 method search_templates
  Similarity: 85.12%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:save_template_to_file (lines 154-164) ---[0m
    async fn save_template_to_file(&self, template: &Template) -> Result<(), TemplateError> {
        let path = self.template_path(&template.id);
        let content =
            serde_json::to_string_pretty(template).context("Failed to serialize template")?;

        async_fs::write(&path, content)
            .await
            .context("Failed to write template file")?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:search_templates (lines 473-485) ---[0m
    async fn search_templates(
        &self,
        query: &TemplateQuery,
    ) -> Result<Vec<Template>, TemplateError> {
        let all_templates = self.list_templates().await?;
        // Reuse the filtering logic from FileSystemTemplateStorage
        let fs_storage = FileSystemTemplateStorage {
            base_dir: PathBuf::new(),
            cache: HashMap::new(),
            cache_valid: true,
        };
        Ok(fs_storage.apply_query_filters(all_templates, query))
    }

  ./crates/ccswarm/src/template/storage.rs:142-151 method load_template_from_file <-> ./crates/ccswarm/src/template/storage.rs:287-304 method load_template
  Similarity: 72.21%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:load_template_from_file (lines 142-151) ---[0m
    async fn load_template_from_file(&self, path: &Path) -> Result<Template, TemplateError> {
        let content = async_fs::read_to_string(path)
            .await
            .context("Failed to read template file")?;

        let template: Template =
            serde_json::from_str(&content).context("Failed to parse template JSON")?;

        Ok(template)
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:load_template (lines 287-304) ---[0m
    async fn load_template(&self, id: &str) -> Result<Template, TemplateError> {
        // Try cache first
        if self.cache_valid {
            if let Some(template) = self.cache.get(id) {
                return Ok(template.clone());
            }
        }

        // Load from file
        let path = self.template_path(id);
        if !path.exists() {
            return Err(TemplateError::NotFound {
                name: id.to_string(),
            });
        }

        self.load_template_from_file(&path).await
    }

  ./crates/ccswarm/src/template/storage.rs:154-164 method save_template_to_file <-> ./crates/ccswarm/src/template/storage.rs:532-544 method update_usage
  Similarity: 83.49%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:save_template_to_file (lines 154-164) ---[0m
    async fn save_template_to_file(&self, template: &Template) -> Result<(), TemplateError> {
        let path = self.template_path(&template.id);
        let content =
            serde_json::to_string_pretty(template).context("Failed to serialize template")?;

        async_fs::write(&path, content)
            .await
            .context("Failed to write template file")?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:update_usage (lines 532-544) ---[0m
    async fn update_usage(&mut self, id: &str, success: bool) -> Result<(), TemplateError> {
        let template = self
            .templates
            .get_mut(id)
            .ok_or_else(|| TemplateError::NotFound {
                name: id.to_string(),
            })?;

        template.increment_usage();
        template.update_success_rate(success);

        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:142-151 method load_template_from_file <-> ./crates/ccswarm/src/template/storage.rs:473-485 method search_templates
  Similarity: 84.55%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:load_template_from_file (lines 142-151) ---[0m
    async fn load_template_from_file(&self, path: &Path) -> Result<Template, TemplateError> {
        let content = async_fs::read_to_string(path)
            .await
            .context("Failed to read template file")?;

        let template: Template =
            serde_json::from_str(&content).context("Failed to parse template JSON")?;

        Ok(template)
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:search_templates (lines 473-485) ---[0m
    async fn search_templates(
        &self,
        query: &TemplateQuery,
    ) -> Result<Vec<Template>, TemplateError> {
        let all_templates = self.list_templates().await?;
        // Reuse the filtering logic from FileSystemTemplateStorage
        let fs_storage = FileSystemTemplateStorage {
            base_dir: PathBuf::new(),
            cache: HashMap::new(),
            cache_valid: true,
        };
        Ok(fs_storage.apply_query_filters(all_templates, query))
    }

  ./crates/ccswarm/src/template/storage.rs:142-151 method load_template_from_file <-> ./crates/ccswarm/src/template/storage.rs:532-544 method update_usage
  Similarity: 72.62%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:load_template_from_file (lines 142-151) ---[0m
    async fn load_template_from_file(&self, path: &Path) -> Result<Template, TemplateError> {
        let content = async_fs::read_to_string(path)
            .await
            .context("Failed to read template file")?;

        let template: Template =
            serde_json::from_str(&content).context("Failed to parse template JSON")?;

        Ok(template)
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:update_usage (lines 532-544) ---[0m
    async fn update_usage(&mut self, id: &str, success: bool) -> Result<(), TemplateError> {
        let template = self
            .templates
            .get_mut(id)
            .ok_or_else(|| TemplateError::NotFound {
                name: id.to_string(),
            })?;

        template.increment_usage();
        template.update_success_rate(success);

        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:142-151 method load_template_from_file <-> ./crates/ccswarm/src/template/storage.rs:154-164 method save_template_to_file
  Similarity: 78.49%
  Classes: FileSystemTemplateStorage <-> FileSystemTemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:load_template_from_file (lines 142-151) ---[0m
    async fn load_template_from_file(&self, path: &Path) -> Result<Template, TemplateError> {
        let content = async_fs::read_to_string(path)
            .await
            .context("Failed to read template file")?;

        let template: Template =
            serde_json::from_str(&content).context("Failed to parse template JSON")?;

        Ok(template)
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:save_template_to_file (lines 154-164) ---[0m
    async fn save_template_to_file(&self, template: &Template) -> Result<(), TemplateError> {
        let path = self.template_path(&template.id);
        let content =
            serde_json::to_string_pretty(template).context("Failed to serialize template")?;

        async_fs::write(&path, content)
            .await
            .context("Failed to write template file")?;

        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:339-346 method exists <-> ./crates/ccswarm/src/template/storage.rs:473-485 method search_templates
  Similarity: 72.21%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:exists (lines 339-346) ---[0m
    async fn exists(&self, id: &str) -> Result<bool, TemplateError> {
        if self.cache_valid && self.cache.contains_key(id) {
            return Ok(true);
        }

        let path = self.template_path(id);
        Ok(path.exists())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:search_templates (lines 473-485) ---[0m
    async fn search_templates(
        &self,
        query: &TemplateQuery,
    ) -> Result<Vec<Template>, TemplateError> {
        let all_templates = self.list_templates().await?;
        // Reuse the filtering logic from FileSystemTemplateStorage
        let fs_storage = FileSystemTemplateStorage {
            base_dir: PathBuf::new(),
            cache: HashMap::new(),
            cache_valid: true,
        };
        Ok(fs_storage.apply_query_filters(all_templates, query))
    }

  ./crates/ccswarm/src/template/storage.rs:142-151 method load_template_from_file <-> ./crates/ccswarm/src/template/storage.rs:339-346 method exists
  Similarity: 73.09%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:load_template_from_file (lines 142-151) ---[0m
    async fn load_template_from_file(&self, path: &Path) -> Result<Template, TemplateError> {
        let content = async_fs::read_to_string(path)
            .await
            .context("Failed to read template file")?;

        let template: Template =
            serde_json::from_str(&content).context("Failed to parse template JSON")?;

        Ok(template)
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:exists (lines 339-346) ---[0m
    async fn exists(&self, id: &str) -> Result<bool, TemplateError> {
        if self.cache_valid && self.cache.contains_key(id) {
            return Ok(true);
        }

        let path = self.template_path(id);
        Ok(path.exists())
    }

  ./crates/ccswarm/src/template/storage.rs:174-179 method ensure_cache_valid <-> ./crates/ccswarm/src/template/storage.rs:460-467 method delete_template
  Similarity: 91.27%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:ensure_cache_valid (lines 174-179) ---[0m
    async fn ensure_cache_valid(&mut self) -> Result<(), TemplateError> {
        if !self.cache_valid {
            self.refresh_cache().await?;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:delete_template (lines 460-467) ---[0m
    async fn delete_template(&mut self, id: &str) -> Result<(), TemplateError> {
        if self.templates.remove(id).is_none() {
            return Err(TemplateError::NotFound {
                name: id.to_string(),
            });
        }
        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:142-151 method load_template_from_file <-> ./crates/ccswarm/src/template/storage.rs:331-337 method search_templates
  Similarity: 74.12%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:load_template_from_file (lines 142-151) ---[0m
    async fn load_template_from_file(&self, path: &Path) -> Result<Template, TemplateError> {
        let content = async_fs::read_to_string(path)
            .await
            .context("Failed to read template file")?;

        let template: Template =
            serde_json::from_str(&content).context("Failed to parse template JSON")?;

        Ok(template)
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:search_templates (lines 331-337) ---[0m
    async fn search_templates(
        &self,
        query: &TemplateQuery,
    ) -> Result<Vec<Template>, TemplateError> {
        let all_templates = self.list_templates().await?;
        Ok(self.apply_query_filters(all_templates, query))
    }

  ./crates/ccswarm/src/template/storage.rs:326-329 method list_templates <-> ./crates/ccswarm/src/template/storage.rs:460-467 method delete_template
  Similarity: 94.29%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:list_templates (lines 326-329) ---[0m
    async fn list_templates(&self) -> Result<Vec<Template>, TemplateError> {
        // For now, just return cached values without mutation
        Ok(self.cache.values().cloned().collect())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:delete_template (lines 460-467) ---[0m
    async fn delete_template(&mut self, id: &str) -> Result<(), TemplateError> {
        if self.templates.remove(id).is_none() {
            return Err(TemplateError::NotFound {
                name: id.to_string(),
            });
        }
        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:331-337 method search_templates <-> ./crates/ccswarm/src/template/storage.rs:460-467 method delete_template
  Similarity: 73.33%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:search_templates (lines 331-337) ---[0m
    async fn search_templates(
        &self,
        query: &TemplateQuery,
    ) -> Result<Vec<Template>, TemplateError> {
        let all_templates = self.list_templates().await?;
        Ok(self.apply_query_filters(all_templates, query))
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:delete_template (lines 460-467) ---[0m
    async fn delete_template(&mut self, id: &str) -> Result<(), TemplateError> {
        if self.templates.remove(id).is_none() {
            return Err(TemplateError::NotFound {
                name: id.to_string(),
            });
        }
        Ok(())
    }

  ./crates/ccswarm/src/template/storage.rs:326-329 method list_templates <-> ./crates/ccswarm/src/template/storage.rs:451-458 method load_template
  Similarity: 86.27%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:list_templates (lines 326-329) ---[0m
    async fn list_templates(&self) -> Result<Vec<Template>, TemplateError> {
        // For now, just return cached values without mutation
        Ok(self.cache.values().cloned().collect())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:load_template (lines 451-458) ---[0m
    async fn load_template(&self, id: &str) -> Result<Template, TemplateError> {
        self.templates
            .get(id)
            .cloned()
            .ok_or_else(|| TemplateError::NotFound {
                name: id.to_string(),
            })
    }

  ./crates/ccswarm/src/template/storage.rs:451-458 method load_template <-> ./crates/ccswarm/src/template/storage.rs:469-471 method list_templates
  Similarity: 90.20%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:load_template (lines 451-458) ---[0m
    async fn load_template(&self, id: &str) -> Result<Template, TemplateError> {
        self.templates
            .get(id)
            .cloned()
            .ok_or_else(|| TemplateError::NotFound {
                name: id.to_string(),
            })
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:list_templates (lines 469-471) ---[0m
    async fn list_templates(&self) -> Result<Vec<Template>, TemplateError> {
        Ok(self.templates.values().cloned().collect())
    }

  ./crates/ccswarm/src/template/storage.rs:326-329 method list_templates <-> ./crates/ccswarm/src/template/storage.rs:331-337 method search_templates
  Similarity: 82.86%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:list_templates (lines 326-329) ---[0m
    async fn list_templates(&self) -> Result<Vec<Template>, TemplateError> {
        // For now, just return cached values without mutation
        Ok(self.cache.values().cloned().collect())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:search_templates (lines 331-337) ---[0m
    async fn search_templates(
        &self,
        query: &TemplateQuery,
    ) -> Result<Vec<Template>, TemplateError> {
        let all_templates = self.list_templates().await?;
        Ok(self.apply_query_filters(all_templates, query))
    }

  ./crates/ccswarm/src/template/storage.rs:174-179 method ensure_cache_valid <-> ./crates/ccswarm/src/template/storage.rs:326-329 method list_templates
  Similarity: 90.00%
  Classes: FileSystemTemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:ensure_cache_valid (lines 174-179) ---[0m
    async fn ensure_cache_valid(&mut self) -> Result<(), TemplateError> {
        if !self.cache_valid {
            self.refresh_cache().await?;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:list_templates (lines 326-329) ---[0m
    async fn list_templates(&self) -> Result<Vec<Template>, TemplateError> {
        // For now, just return cached values without mutation
        Ok(self.cache.values().cloned().collect())
    }

  ./crates/ccswarm/src/template/storage.rs:326-329 method list_templates <-> ./crates/ccswarm/src/template/storage.rs:339-346 method exists
  Similarity: 73.65%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:list_templates (lines 326-329) ---[0m
    async fn list_templates(&self) -> Result<Vec<Template>, TemplateError> {
        // For now, just return cached values without mutation
        Ok(self.cache.values().cloned().collect())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:exists (lines 339-346) ---[0m
    async fn exists(&self, id: &str) -> Result<bool, TemplateError> {
        if self.cache_valid && self.cache.contains_key(id) {
            return Ok(true);
        }

        let path = self.template_path(id);
        Ok(path.exists())
    }

  ./crates/ccswarm/src/template/storage.rs:326-329 method list_templates <-> ./crates/ccswarm/src/template/storage.rs:469-471 method list_templates
  Similarity: 93.03%
  Classes: TemplateStorage <-> TemplateStorage

[36m--- ./crates/ccswarm/src/template/storage.rs:list_templates (lines 326-329) ---[0m
    async fn list_templates(&self) -> Result<Vec<Template>, TemplateError> {
        // For now, just return cached values without mutation
        Ok(self.cache.values().cloned().collect())
    }

[36m--- ./crates/ccswarm/src/template/storage.rs:list_templates (lines 469-471) ---[0m
    async fn list_templates(&self) -> Result<Vec<Template>, TemplateError> {
        Ok(self.templates.values().cloned().collect())
    }


Duplicates in ./crates/ccswarm/src/orchestrator/master_delegation.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/orchestrator/master_delegation.rs:106-204 method default_delegation_rules <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:393-445 method evaluate_condition
  Similarity: 71.54%
  Classes: MasterDelegationEngine <-> MasterDelegationEngine

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:default_delegation_rules (lines 106-204) ---[0m
    fn default_delegation_rules() -> Vec<DelegationRule> {
        vec![
            DelegationRule {
                name: "Frontend UI Tasks".to_string(),
                priority: 10,
                condition: DelegationCondition::Or(vec![
                    DelegationCondition::DescriptionContains(vec![
                        "html".to_string(),
                        "css".to_string(),
                        "javascript".to_string(),
                        "ui".to_string(),
                        "component".to_string(),
                        "frontend".to_string(),
                        "react".to_string(),
                        "vue".to_string(),
                        "angular".to_string(),
                    ]),
                    DelegationCondition::TaskTypeEquals(TaskType::Feature),
                ]),
                target_agent: AgentRole::Frontend {
                    technologies: vec![
                        "HTML".to_string(),
                        "CSS".to_string(),
                        "JavaScript".to_string(),
                    ],
                    responsibilities: vec!["UI Development".to_string()],
                    boundaries: vec!["No backend work".to_string()],
                },
                confidence_boost: 0.8,
            },
            DelegationRule {
                name: "Backend API Tasks".to_string(),
                priority: 10,
                condition: DelegationCondition::And(vec![
                    DelegationCondition::DescriptionContains(vec![
                        "api".to_string(),
                        "server".to_string(),
                        "database".to_string(),
                        "backend".to_string(),
                        "endpoint".to_string(),
                        "rest".to_string(),
                        "node".to_string(),
                        "express".to_string(),
                    ]),
                    DelegationCondition::TaskTypeEquals(TaskType::Development),
                ]),
                target_agent: AgentRole::Backend {
                    technologies: vec!["Node.js".to_string(), "Express".to_string()],
                    responsibilities: vec!["API Development".to_string()],
                    boundaries: vec!["No frontend work".to_string()],
                },
                confidence_boost: 0.8,
            },
            DelegationRule {
                name: "Testing Tasks".to_string(),
                priority: 9,
                condition: DelegationCondition::Or(vec![
                    DelegationCondition::DescriptionContains(vec![
                        "test".to_string(),
                        "testing".to_string(),
                        "qa".to_string(),
                        "quality".to_string(),
                        "validation".to_string(),
                    ]),
                    DelegationCondition::TaskTypeEquals(TaskType::Testing),
                ]),
                target_agent: AgentRole::QA {
                    technologies: vec![
                        "Jest".to_string(),
                        "Mocha".to_string(),
                        "Puppeteer".to_string(),
                    ],
                    responsibilities: vec!["Testing".to_string(), "Quality Assurance".to_string()],
                    boundaries: vec!["No production code".to_string()],
                },
                confidence_boost: 0.9,
            },
            DelegationRule {
                name: "Infrastructure Tasks".to_string(),
                priority: 9,
                condition: DelegationCondition::Or(vec![
                    DelegationCondition::DescriptionContains(vec![
                        "deploy".to_string(),
                        "ci/cd".to_string(),
                        "docker".to_string(),
                        "infrastructure".to_string(),
                        "pipeline".to_string(),
                    ]),
                    DelegationCondition::TaskTypeEquals(TaskType::Infrastructure),
                ]),
                target_agent: AgentRole::DevOps {
                    technologies: vec!["Docker".to_string(), "GitHub Actions".to_string()],
                    responsibilities: vec!["Deployment".to_string(), "Infrastructure".to_string()],
                    boundaries: vec!["No application code".to_string()],
                },
                confidence_boost: 0.9,
            },
        ]
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:evaluate_condition (lines 393-445) ---[0m
    fn evaluate_condition(
        condition: &DelegationCondition,
        task: &Task,
        task_lower: &str,
    ) -> Option<f64> {
        match condition {
            DelegationCondition::DescriptionContains(keywords) => {
                let matches = keywords
                    .iter()
                    .filter(|keyword| task_lower.contains(&keyword.to_lowercase()))
                    .count();
                if matches > 0 {
                    Some(matches as f64 / keywords.len() as f64)
                } else {
                    None
                }
            }
            DelegationCondition::TaskTypeEquals(task_type) => {
                if task.task_type == *task_type {
                    Some(1.0)
                } else {
                    None
                }
            }
            DelegationCondition::PriorityAbove(priority) => {
                if task.priority as u8 >= *priority as u8 {
                    Some(0.5 + (task.priority as u8 as f64 / 10.0))
                } else {
                    None
                }
            }
            DelegationCondition::AgentWorkloadBelow(_threshold) => {
                // This would require current agent workload info
                Some(0.5) // Placeholder
            }
            DelegationCondition::And(conditions) => {
                let scores: Vec<f64> = conditions
                    .iter()
                    .filter_map(|c| Self::evaluate_condition(c, task, task_lower))
                    .collect();

                if scores.len() == conditions.len() {
                    Some(scores.iter().sum::<f64>() / scores.len() as f64)
                } else {
                    None
                }
            }
            DelegationCondition::Or(conditions) => conditions
                .iter()
                .filter_map(|c| Self::evaluate_condition(c, task, task_lower))
                .max_by(|a, b| a.partial_cmp(b).unwrap_or(std::cmp::Ordering::Equal)),
        }
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:241-288 method delegate_content_based <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:291-324 method delegate_load_balanced
  Similarity: 91.35%
  Classes: MasterDelegationEngine <-> MasterDelegationEngine

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_content_based (lines 241-288) ---[0m
    fn delegate_content_based(&self, task: &Task) -> Result<DelegationDecision> {
        let mut best_match: Option<(DelegationRule, f64)> = None;
        let task_lower = task.description.to_lowercase();

        for rule in &self.delegation_rules {
            if let Some(confidence) = Self::evaluate_condition(&rule.condition, task, &task_lower) {
                let total_confidence = confidence + rule.confidence_boost;

                if best_match
                    .as_ref()
                    .is_none_or(|(_, prev_confidence)| total_confidence > *prev_confidence)
                {
                    best_match = Some((rule.clone(), total_confidence.min(1.0)));
                }
            }
        }

        if let Some((rule, confidence)) = best_match {
            Ok(DelegationDecision {
                task: task.clone(),
                target_agent: rule.target_agent,
                confidence,
                reasoning: format!(
                    "Matched rule: {} with {:.1}% confidence",
                    rule.name,
                    confidence * 100.0
                ),
                priority_adjustment: None,
                estimated_duration: task.estimated_duration,
                dependencies: vec![],
            })
        } else {
            // Default to backend agent as most versatile
            Ok(DelegationDecision {
                task: task.clone(),
                target_agent: AgentRole::Backend {
                    technologies: vec!["Node.js".to_string()],
                    responsibilities: vec!["General Development".to_string()],
                    boundaries: vec![],
                },
                confidence: 0.3,
                reasoning: "No specific rule matched, defaulting to backend agent".to_string(),
                priority_adjustment: None,
                estimated_duration: task.estimated_duration,
                dependencies: vec![],
            })
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_load_balanced (lines 291-324) ---[0m
    fn delegate_load_balanced(&self, task: &Task) -> Result<DelegationDecision> {
        // Find agent with lowest current workload
        let mut best_agent: Option<(AgentRole, f64)> = None;

        for metrics in self.agent_metrics.values() {
            let workload = metrics.current_tasks as f64 / 10.0; // Normalize to 0-1
            let availability_score = metrics.availability * (1.0 - workload);

            if best_agent
                .as_ref()
                .is_none_or(|(_, prev_score)| availability_score > *prev_score)
            {
                best_agent = Some((metrics.agent_role.clone(), availability_score));
            }
        }

        if let Some((agent, score)) = best_agent {
            Ok(DelegationDecision {
                task: task.clone(),
                target_agent: agent,
                confidence: score,
                reasoning: format!(
                    "Load-balanced assignment with availability score {:.1}%",
                    score * 100.0
                ),
                priority_adjustment: None,
                estimated_duration: task.estimated_duration,
                dependencies: vec![],
            })
        } else {
            // Fallback to content-based
            self.delegate_content_based(task)
        }
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:241-288 method delegate_content_based <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:327-358 method delegate_expertise_based
  Similarity: 90.15%
  Classes: MasterDelegationEngine <-> MasterDelegationEngine

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_content_based (lines 241-288) ---[0m
    fn delegate_content_based(&self, task: &Task) -> Result<DelegationDecision> {
        let mut best_match: Option<(DelegationRule, f64)> = None;
        let task_lower = task.description.to_lowercase();

        for rule in &self.delegation_rules {
            if let Some(confidence) = Self::evaluate_condition(&rule.condition, task, &task_lower) {
                let total_confidence = confidence + rule.confidence_boost;

                if best_match
                    .as_ref()
                    .is_none_or(|(_, prev_confidence)| total_confidence > *prev_confidence)
                {
                    best_match = Some((rule.clone(), total_confidence.min(1.0)));
                }
            }
        }

        if let Some((rule, confidence)) = best_match {
            Ok(DelegationDecision {
                task: task.clone(),
                target_agent: rule.target_agent,
                confidence,
                reasoning: format!(
                    "Matched rule: {} with {:.1}% confidence",
                    rule.name,
                    confidence * 100.0
                ),
                priority_adjustment: None,
                estimated_duration: task.estimated_duration,
                dependencies: vec![],
            })
        } else {
            // Default to backend agent as most versatile
            Ok(DelegationDecision {
                task: task.clone(),
                target_agent: AgentRole::Backend {
                    technologies: vec!["Node.js".to_string()],
                    responsibilities: vec!["General Development".to_string()],
                    boundaries: vec![],
                },
                confidence: 0.3,
                reasoning: "No specific rule matched, defaulting to backend agent".to_string(),
                priority_adjustment: None,
                estimated_duration: task.estimated_duration,
                dependencies: vec![],
            })
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_expertise_based (lines 327-358) ---[0m
    fn delegate_expertise_based(&self, task: &Task) -> Result<DelegationDecision> {
        let mut best_match: Option<(AgentRole, f64)> = None;

        for metrics in self.agent_metrics.values() {
            let expertise_score = metrics.specialization_score * metrics.success_rate;

            if best_match
                .as_ref()
                .is_none_or(|(_, prev_score)| expertise_score > *prev_score)
            {
                best_match = Some((metrics.agent_role.clone(), expertise_score));
            }
        }

        if let Some((agent, score)) = best_match {
            Ok(DelegationDecision {
                task: task.clone(),
                target_agent: agent,
                confidence: score,
                reasoning: format!(
                    "Expertise-based assignment with score {:.1}%",
                    score * 100.0
                ),
                priority_adjustment: None,
                estimated_duration: task.estimated_duration,
                dependencies: vec![],
            })
        } else {
            // Fallback to content-based
            self.delegate_content_based(task)
        }
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:291-324 method delegate_load_balanced <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:327-358 method delegate_expertise_based
  Similarity: 96.48%
  Classes: MasterDelegationEngine <-> MasterDelegationEngine

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_load_balanced (lines 291-324) ---[0m
    fn delegate_load_balanced(&self, task: &Task) -> Result<DelegationDecision> {
        // Find agent with lowest current workload
        let mut best_agent: Option<(AgentRole, f64)> = None;

        for metrics in self.agent_metrics.values() {
            let workload = metrics.current_tasks as f64 / 10.0; // Normalize to 0-1
            let availability_score = metrics.availability * (1.0 - workload);

            if best_agent
                .as_ref()
                .is_none_or(|(_, prev_score)| availability_score > *prev_score)
            {
                best_agent = Some((metrics.agent_role.clone(), availability_score));
            }
        }

        if let Some((agent, score)) = best_agent {
            Ok(DelegationDecision {
                task: task.clone(),
                target_agent: agent,
                confidence: score,
                reasoning: format!(
                    "Load-balanced assignment with availability score {:.1}%",
                    score * 100.0
                ),
                priority_adjustment: None,
                estimated_duration: task.estimated_duration,
                dependencies: vec![],
            })
        } else {
            // Fallback to content-based
            self.delegate_content_based(task)
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_expertise_based (lines 327-358) ---[0m
    fn delegate_expertise_based(&self, task: &Task) -> Result<DelegationDecision> {
        let mut best_match: Option<(AgentRole, f64)> = None;

        for metrics in self.agent_metrics.values() {
            let expertise_score = metrics.specialization_score * metrics.success_rate;

            if best_match
                .as_ref()
                .is_none_or(|(_, prev_score)| expertise_score > *prev_score)
            {
                best_match = Some((metrics.agent_role.clone(), expertise_score));
            }
        }

        if let Some((agent, score)) = best_match {
            Ok(DelegationDecision {
                task: task.clone(),
                target_agent: agent,
                confidence: score,
                reasoning: format!(
                    "Expertise-based assignment with score {:.1}%",
                    score * 100.0
                ),
                priority_adjustment: None,
                estimated_duration: task.estimated_duration,
                dependencies: vec![],
            })
        } else {
            // Fallback to content-based
            self.delegate_content_based(task)
        }
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:213-238 method delegate_task <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:241-288 method delegate_content_based
  Similarity: 85.08%
  Classes: MasterDelegationEngine <-> MasterDelegationEngine

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_task (lines 213-238) ---[0m
    pub fn delegate_task(&mut self, task: Task) -> Result<DelegationDecision> {
        info!(
            "🎯 Master analyzing task for delegation: {}",
            task.description
        );

        let decision = match self.strategy {
            DelegationStrategy::ContentBased => self.delegate_content_based(&task)?,
            DelegationStrategy::LoadBalanced => self.delegate_load_balanced(&task)?,
            DelegationStrategy::ExpertiseBased => self.delegate_expertise_based(&task)?,
            DelegationStrategy::WorkflowBased => self.delegate_workflow_based(&task)?,
            DelegationStrategy::Hybrid => self.delegate_hybrid(&task)?,
        };

        // Record the delegation decision
        self.task_history.push(decision.clone());

        info!(
            "✅ Master delegated task '{}' to {} with {:.1}% confidence",
            task.description,
            decision.target_agent.name(),
            decision.confidence * 100.0
        );

        Ok(decision)
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_content_based (lines 241-288) ---[0m
    fn delegate_content_based(&self, task: &Task) -> Result<DelegationDecision> {
        let mut best_match: Option<(DelegationRule, f64)> = None;
        let task_lower = task.description.to_lowercase();

        for rule in &self.delegation_rules {
            if let Some(confidence) = Self::evaluate_condition(&rule.condition, task, &task_lower) {
                let total_confidence = confidence + rule.confidence_boost;

                if best_match
                    .as_ref()
                    .is_none_or(|(_, prev_confidence)| total_confidence > *prev_confidence)
                {
                    best_match = Some((rule.clone(), total_confidence.min(1.0)));
                }
            }
        }

        if let Some((rule, confidence)) = best_match {
            Ok(DelegationDecision {
                task: task.clone(),
                target_agent: rule.target_agent,
                confidence,
                reasoning: format!(
                    "Matched rule: {} with {:.1}% confidence",
                    rule.name,
                    confidence * 100.0
                ),
                priority_adjustment: None,
                estimated_duration: task.estimated_duration,
                dependencies: vec![],
            })
        } else {
            // Default to backend agent as most versatile
            Ok(DelegationDecision {
                task: task.clone(),
                target_agent: AgentRole::Backend {
                    technologies: vec!["Node.js".to_string()],
                    responsibilities: vec!["General Development".to_string()],
                    boundaries: vec![],
                },
                confidence: 0.3,
                reasoning: "No specific rule matched, defaulting to backend agent".to_string(),
                priority_adjustment: None,
                estimated_duration: task.estimated_duration,
                dependencies: vec![],
            })
        }
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:213-238 method delegate_task <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:291-324 method delegate_load_balanced
  Similarity: 78.61%
  Classes: MasterDelegationEngine <-> MasterDelegationEngine

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_task (lines 213-238) ---[0m
    pub fn delegate_task(&mut self, task: Task) -> Result<DelegationDecision> {
        info!(
            "🎯 Master analyzing task for delegation: {}",
            task.description
        );

        let decision = match self.strategy {
            DelegationStrategy::ContentBased => self.delegate_content_based(&task)?,
            DelegationStrategy::LoadBalanced => self.delegate_load_balanced(&task)?,
            DelegationStrategy::ExpertiseBased => self.delegate_expertise_based(&task)?,
            DelegationStrategy::WorkflowBased => self.delegate_workflow_based(&task)?,
            DelegationStrategy::Hybrid => self.delegate_hybrid(&task)?,
        };

        // Record the delegation decision
        self.task_history.push(decision.clone());

        info!(
            "✅ Master delegated task '{}' to {} with {:.1}% confidence",
            task.description,
            decision.target_agent.name(),
            decision.confidence * 100.0
        );

        Ok(decision)
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_load_balanced (lines 291-324) ---[0m
    fn delegate_load_balanced(&self, task: &Task) -> Result<DelegationDecision> {
        // Find agent with lowest current workload
        let mut best_agent: Option<(AgentRole, f64)> = None;

        for metrics in self.agent_metrics.values() {
            let workload = metrics.current_tasks as f64 / 10.0; // Normalize to 0-1
            let availability_score = metrics.availability * (1.0 - workload);

            if best_agent
                .as_ref()
                .is_none_or(|(_, prev_score)| availability_score > *prev_score)
            {
                best_agent = Some((metrics.agent_role.clone(), availability_score));
            }
        }

        if let Some((agent, score)) = best_agent {
            Ok(DelegationDecision {
                task: task.clone(),
                target_agent: agent,
                confidence: score,
                reasoning: format!(
                    "Load-balanced assignment with availability score {:.1}%",
                    score * 100.0
                ),
                priority_adjustment: None,
                estimated_duration: task.estimated_duration,
                dependencies: vec![],
            })
        } else {
            // Fallback to content-based
            self.delegate_content_based(task)
        }
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:291-324 method delegate_load_balanced <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:448-470 method get_delegation_stats
  Similarity: 81.80%
  Classes: MasterDelegationEngine <-> MasterDelegationEngine

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_load_balanced (lines 291-324) ---[0m
    fn delegate_load_balanced(&self, task: &Task) -> Result<DelegationDecision> {
        // Find agent with lowest current workload
        let mut best_agent: Option<(AgentRole, f64)> = None;

        for metrics in self.agent_metrics.values() {
            let workload = metrics.current_tasks as f64 / 10.0; // Normalize to 0-1
            let availability_score = metrics.availability * (1.0 - workload);

            if best_agent
                .as_ref()
                .is_none_or(|(_, prev_score)| availability_score > *prev_score)
            {
                best_agent = Some((metrics.agent_role.clone(), availability_score));
            }
        }

        if let Some((agent, score)) = best_agent {
            Ok(DelegationDecision {
                task: task.clone(),
                target_agent: agent,
                confidence: score,
                reasoning: format!(
                    "Load-balanced assignment with availability score {:.1}%",
                    score * 100.0
                ),
                priority_adjustment: None,
                estimated_duration: task.estimated_duration,
                dependencies: vec![],
            })
        } else {
            // Fallback to content-based
            self.delegate_content_based(task)
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:get_delegation_stats (lines 448-470) ---[0m
    pub fn get_delegation_stats(&self) -> DelegationStats {
        let total_delegations = self.task_history.len();
        let mut agent_counts = HashMap::new();
        let mut total_confidence = 0.0;

        for decision in &self.task_history {
            *agent_counts
                .entry(decision.target_agent.name().to_string())
                .or_insert(0) += 1;
            total_confidence += decision.confidence;
        }

        DelegationStats {
            total_delegations,
            average_confidence: if total_delegations > 0 {
                total_confidence / total_delegations as f64
            } else {
                0.0
            },
            agent_distribution: agent_counts,
            strategy: self.strategy.clone(),
        }
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:327-358 method delegate_expertise_based <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:373-390 method delegate_hybrid
  Similarity: 86.52%
  Classes: MasterDelegationEngine <-> MasterDelegationEngine

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_expertise_based (lines 327-358) ---[0m
    fn delegate_expertise_based(&self, task: &Task) -> Result<DelegationDecision> {
        let mut best_match: Option<(AgentRole, f64)> = None;

        for metrics in self.agent_metrics.values() {
            let expertise_score = metrics.specialization_score * metrics.success_rate;

            if best_match
                .as_ref()
                .is_none_or(|(_, prev_score)| expertise_score > *prev_score)
            {
                best_match = Some((metrics.agent_role.clone(), expertise_score));
            }
        }

        if let Some((agent, score)) = best_match {
            Ok(DelegationDecision {
                task: task.clone(),
                target_agent: agent,
                confidence: score,
                reasoning: format!(
                    "Expertise-based assignment with score {:.1}%",
                    score * 100.0
                ),
                priority_adjustment: None,
                estimated_duration: task.estimated_duration,
                dependencies: vec![],
            })
        } else {
            // Fallback to content-based
            self.delegate_content_based(task)
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_hybrid (lines 373-390) ---[0m
    fn delegate_hybrid(&self, task: &Task) -> Result<DelegationDecision> {
        // Combine multiple strategies
        let content_decision = self.delegate_content_based(task)?;
        let load_decision = self.delegate_load_balanced(task)?;

        // Choose the decision with higher confidence
        if content_decision.confidence >= load_decision.confidence {
            Ok(DelegationDecision {
                reasoning: format!("Hybrid: Content-based ({})", content_decision.reasoning),
                ..content_decision
            })
        } else {
            Ok(DelegationDecision {
                reasoning: format!("Hybrid: Load-based ({})", load_decision.reasoning),
                ..load_decision
            })
        }
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:327-358 method delegate_expertise_based <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:448-470 method get_delegation_stats
  Similarity: 75.94%
  Classes: MasterDelegationEngine <-> MasterDelegationEngine

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_expertise_based (lines 327-358) ---[0m
    fn delegate_expertise_based(&self, task: &Task) -> Result<DelegationDecision> {
        let mut best_match: Option<(AgentRole, f64)> = None;

        for metrics in self.agent_metrics.values() {
            let expertise_score = metrics.specialization_score * metrics.success_rate;

            if best_match
                .as_ref()
                .is_none_or(|(_, prev_score)| expertise_score > *prev_score)
            {
                best_match = Some((metrics.agent_role.clone(), expertise_score));
            }
        }

        if let Some((agent, score)) = best_match {
            Ok(DelegationDecision {
                task: task.clone(),
                target_agent: agent,
                confidence: score,
                reasoning: format!(
                    "Expertise-based assignment with score {:.1}%",
                    score * 100.0
                ),
                priority_adjustment: None,
                estimated_duration: task.estimated_duration,
                dependencies: vec![],
            })
        } else {
            // Fallback to content-based
            self.delegate_content_based(task)
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:get_delegation_stats (lines 448-470) ---[0m
    pub fn get_delegation_stats(&self) -> DelegationStats {
        let total_delegations = self.task_history.len();
        let mut agent_counts = HashMap::new();
        let mut total_confidence = 0.0;

        for decision in &self.task_history {
            *agent_counts
                .entry(decision.target_agent.name().to_string())
                .or_insert(0) += 1;
            total_confidence += decision.confidence;
        }

        DelegationStats {
            total_delegations,
            average_confidence: if total_delegations > 0 {
                total_confidence / total_delegations as f64
            } else {
                0.0
            },
            agent_distribution: agent_counts,
            strategy: self.strategy.clone(),
        }
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:213-238 method delegate_task <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:327-358 method delegate_expertise_based
  Similarity: 71.56%
  Classes: MasterDelegationEngine <-> MasterDelegationEngine

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_task (lines 213-238) ---[0m
    pub fn delegate_task(&mut self, task: Task) -> Result<DelegationDecision> {
        info!(
            "🎯 Master analyzing task for delegation: {}",
            task.description
        );

        let decision = match self.strategy {
            DelegationStrategy::ContentBased => self.delegate_content_based(&task)?,
            DelegationStrategy::LoadBalanced => self.delegate_load_balanced(&task)?,
            DelegationStrategy::ExpertiseBased => self.delegate_expertise_based(&task)?,
            DelegationStrategy::WorkflowBased => self.delegate_workflow_based(&task)?,
            DelegationStrategy::Hybrid => self.delegate_hybrid(&task)?,
        };

        // Record the delegation decision
        self.task_history.push(decision.clone());

        info!(
            "✅ Master delegated task '{}' to {} with {:.1}% confidence",
            task.description,
            decision.target_agent.name(),
            decision.confidence * 100.0
        );

        Ok(decision)
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_expertise_based (lines 327-358) ---[0m
    fn delegate_expertise_based(&self, task: &Task) -> Result<DelegationDecision> {
        let mut best_match: Option<(AgentRole, f64)> = None;

        for metrics in self.agent_metrics.values() {
            let expertise_score = metrics.specialization_score * metrics.success_rate;

            if best_match
                .as_ref()
                .is_none_or(|(_, prev_score)| expertise_score > *prev_score)
            {
                best_match = Some((metrics.agent_role.clone(), expertise_score));
            }
        }

        if let Some((agent, score)) = best_match {
            Ok(DelegationDecision {
                task: task.clone(),
                target_agent: agent,
                confidence: score,
                reasoning: format!(
                    "Expertise-based assignment with score {:.1}%",
                    score * 100.0
                ),
                priority_adjustment: None,
                estimated_duration: task.estimated_duration,
                dependencies: vec![],
            })
        } else {
            // Fallback to content-based
            self.delegate_content_based(task)
        }
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:213-238 method delegate_task <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:448-470 method get_delegation_stats
  Similarity: 83.37%
  Classes: MasterDelegationEngine <-> MasterDelegationEngine

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_task (lines 213-238) ---[0m
    pub fn delegate_task(&mut self, task: Task) -> Result<DelegationDecision> {
        info!(
            "🎯 Master analyzing task for delegation: {}",
            task.description
        );

        let decision = match self.strategy {
            DelegationStrategy::ContentBased => self.delegate_content_based(&task)?,
            DelegationStrategy::LoadBalanced => self.delegate_load_balanced(&task)?,
            DelegationStrategy::ExpertiseBased => self.delegate_expertise_based(&task)?,
            DelegationStrategy::WorkflowBased => self.delegate_workflow_based(&task)?,
            DelegationStrategy::Hybrid => self.delegate_hybrid(&task)?,
        };

        // Record the delegation decision
        self.task_history.push(decision.clone());

        info!(
            "✅ Master delegated task '{}' to {} with {:.1}% confidence",
            task.description,
            decision.target_agent.name(),
            decision.confidence * 100.0
        );

        Ok(decision)
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:get_delegation_stats (lines 448-470) ---[0m
    pub fn get_delegation_stats(&self) -> DelegationStats {
        let total_delegations = self.task_history.len();
        let mut agent_counts = HashMap::new();
        let mut total_confidence = 0.0;

        for decision in &self.task_history {
            *agent_counts
                .entry(decision.target_agent.name().to_string())
                .or_insert(0) += 1;
            total_confidence += decision.confidence;
        }

        DelegationStats {
            total_delegations,
            average_confidence: if total_delegations > 0 {
                total_confidence / total_delegations as f64
            } else {
                0.0
            },
            agent_distribution: agent_counts,
            strategy: self.strategy.clone(),
        }
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:291-324 method delegate_load_balanced <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:512-526 function test_testing_task_delegation
  Similarity: 81.39%

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_load_balanced (lines 291-324) ---[0m
    fn delegate_load_balanced(&self, task: &Task) -> Result<DelegationDecision> {
        // Find agent with lowest current workload
        let mut best_agent: Option<(AgentRole, f64)> = None;

        for metrics in self.agent_metrics.values() {
            let workload = metrics.current_tasks as f64 / 10.0; // Normalize to 0-1
            let availability_score = metrics.availability * (1.0 - workload);

            if best_agent
                .as_ref()
                .is_none_or(|(_, prev_score)| availability_score > *prev_score)
            {
                best_agent = Some((metrics.agent_role.clone(), availability_score));
            }
        }

        if let Some((agent, score)) = best_agent {
            Ok(DelegationDecision {
                task: task.clone(),
                target_agent: agent,
                confidence: score,
                reasoning: format!(
                    "Load-balanced assignment with availability score {:.1}%",
                    score * 100.0
                ),
                priority_adjustment: None,
                estimated_duration: task.estimated_duration,
                dependencies: vec![],
            })
        } else {
            // Fallback to content-based
            self.delegate_content_based(task)
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:test_testing_task_delegation (lines 512-526) ---[0m
    fn test_testing_task_delegation() {
        let mut engine = MasterDelegationEngine::new(DelegationStrategy::ContentBased);

        let task = Task::new(
            "test-2".to_string(),
            "Write unit tests for API endpoints".to_string(),
            Priority::High,
            TaskType::Testing,
        );

        let decision = engine.delegate_task(task).unwrap();

        assert!(matches!(decision.target_agent, AgentRole::QA { .. }));
        assert!(decision.confidence > 0.8);
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:213-238 method delegate_task <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:373-390 method delegate_hybrid
  Similarity: 89.34%
  Classes: MasterDelegationEngine <-> MasterDelegationEngine

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_task (lines 213-238) ---[0m
    pub fn delegate_task(&mut self, task: Task) -> Result<DelegationDecision> {
        info!(
            "🎯 Master analyzing task for delegation: {}",
            task.description
        );

        let decision = match self.strategy {
            DelegationStrategy::ContentBased => self.delegate_content_based(&task)?,
            DelegationStrategy::LoadBalanced => self.delegate_load_balanced(&task)?,
            DelegationStrategy::ExpertiseBased => self.delegate_expertise_based(&task)?,
            DelegationStrategy::WorkflowBased => self.delegate_workflow_based(&task)?,
            DelegationStrategy::Hybrid => self.delegate_hybrid(&task)?,
        };

        // Record the delegation decision
        self.task_history.push(decision.clone());

        info!(
            "✅ Master delegated task '{}' to {} with {:.1}% confidence",
            task.description,
            decision.target_agent.name(),
            decision.confidence * 100.0
        );

        Ok(decision)
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_hybrid (lines 373-390) ---[0m
    fn delegate_hybrid(&self, task: &Task) -> Result<DelegationDecision> {
        // Combine multiple strategies
        let content_decision = self.delegate_content_based(task)?;
        let load_decision = self.delegate_load_balanced(task)?;

        // Choose the decision with higher confidence
        if content_decision.confidence >= load_decision.confidence {
            Ok(DelegationDecision {
                reasoning: format!("Hybrid: Content-based ({})", content_decision.reasoning),
                ..content_decision
            })
        } else {
            Ok(DelegationDecision {
                reasoning: format!("Hybrid: Load-based ({})", load_decision.reasoning),
                ..load_decision
            })
        }
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:291-324 method delegate_load_balanced <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:494-509 function test_content_based_delegation
  Similarity: 74.80%

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_load_balanced (lines 291-324) ---[0m
    fn delegate_load_balanced(&self, task: &Task) -> Result<DelegationDecision> {
        // Find agent with lowest current workload
        let mut best_agent: Option<(AgentRole, f64)> = None;

        for metrics in self.agent_metrics.values() {
            let workload = metrics.current_tasks as f64 / 10.0; // Normalize to 0-1
            let availability_score = metrics.availability * (1.0 - workload);

            if best_agent
                .as_ref()
                .is_none_or(|(_, prev_score)| availability_score > *prev_score)
            {
                best_agent = Some((metrics.agent_role.clone(), availability_score));
            }
        }

        if let Some((agent, score)) = best_agent {
            Ok(DelegationDecision {
                task: task.clone(),
                target_agent: agent,
                confidence: score,
                reasoning: format!(
                    "Load-balanced assignment with availability score {:.1}%",
                    score * 100.0
                ),
                priority_adjustment: None,
                estimated_duration: task.estimated_duration,
                dependencies: vec![],
            })
        } else {
            // Fallback to content-based
            self.delegate_content_based(task)
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:test_content_based_delegation (lines 494-509) ---[0m
    fn test_content_based_delegation() {
        let mut engine = MasterDelegationEngine::new(DelegationStrategy::ContentBased);

        let task = Task::new(
            "test-1".to_string(),
            "Create React component with CSS styling for user interface".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        let decision = engine.delegate_task(task).unwrap();

        assert!(matches!(decision.target_agent, AgentRole::Frontend { .. }));
        assert!(decision.confidence > 0.5);
        assert!(decision.reasoning.contains("Frontend"));
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:213-238 method delegate_task <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:494-509 function test_content_based_delegation
  Similarity: 84.80%

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_task (lines 213-238) ---[0m
    pub fn delegate_task(&mut self, task: Task) -> Result<DelegationDecision> {
        info!(
            "🎯 Master analyzing task for delegation: {}",
            task.description
        );

        let decision = match self.strategy {
            DelegationStrategy::ContentBased => self.delegate_content_based(&task)?,
            DelegationStrategy::LoadBalanced => self.delegate_load_balanced(&task)?,
            DelegationStrategy::ExpertiseBased => self.delegate_expertise_based(&task)?,
            DelegationStrategy::WorkflowBased => self.delegate_workflow_based(&task)?,
            DelegationStrategy::Hybrid => self.delegate_hybrid(&task)?,
        };

        // Record the delegation decision
        self.task_history.push(decision.clone());

        info!(
            "✅ Master delegated task '{}' to {} with {:.1}% confidence",
            task.description,
            decision.target_agent.name(),
            decision.confidence * 100.0
        );

        Ok(decision)
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:test_content_based_delegation (lines 494-509) ---[0m
    fn test_content_based_delegation() {
        let mut engine = MasterDelegationEngine::new(DelegationStrategy::ContentBased);

        let task = Task::new(
            "test-1".to_string(),
            "Create React component with CSS styling for user interface".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        let decision = engine.delegate_task(task).unwrap();

        assert!(matches!(decision.target_agent, AgentRole::Frontend { .. }));
        assert!(decision.confidence > 0.5);
        assert!(decision.reasoning.contains("Frontend"));
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:373-390 method delegate_hybrid <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:448-470 method get_delegation_stats
  Similarity: 83.97%
  Classes: MasterDelegationEngine <-> MasterDelegationEngine

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_hybrid (lines 373-390) ---[0m
    fn delegate_hybrid(&self, task: &Task) -> Result<DelegationDecision> {
        // Combine multiple strategies
        let content_decision = self.delegate_content_based(task)?;
        let load_decision = self.delegate_load_balanced(task)?;

        // Choose the decision with higher confidence
        if content_decision.confidence >= load_decision.confidence {
            Ok(DelegationDecision {
                reasoning: format!("Hybrid: Content-based ({})", content_decision.reasoning),
                ..content_decision
            })
        } else {
            Ok(DelegationDecision {
                reasoning: format!("Hybrid: Load-based ({})", load_decision.reasoning),
                ..load_decision
            })
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:get_delegation_stats (lines 448-470) ---[0m
    pub fn get_delegation_stats(&self) -> DelegationStats {
        let total_delegations = self.task_history.len();
        let mut agent_counts = HashMap::new();
        let mut total_confidence = 0.0;

        for decision in &self.task_history {
            *agent_counts
                .entry(decision.target_agent.name().to_string())
                .or_insert(0) += 1;
            total_confidence += decision.confidence;
        }

        DelegationStats {
            total_delegations,
            average_confidence: if total_delegations > 0 {
                total_confidence / total_delegations as f64
            } else {
                0.0
            },
            agent_distribution: agent_counts,
            strategy: self.strategy.clone(),
        }
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:327-358 method delegate_expertise_based <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:512-526 function test_testing_task_delegation
  Similarity: 72.59%

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_expertise_based (lines 327-358) ---[0m
    fn delegate_expertise_based(&self, task: &Task) -> Result<DelegationDecision> {
        let mut best_match: Option<(AgentRole, f64)> = None;

        for metrics in self.agent_metrics.values() {
            let expertise_score = metrics.specialization_score * metrics.success_rate;

            if best_match
                .as_ref()
                .is_none_or(|(_, prev_score)| expertise_score > *prev_score)
            {
                best_match = Some((metrics.agent_role.clone(), expertise_score));
            }
        }

        if let Some((agent, score)) = best_match {
            Ok(DelegationDecision {
                task: task.clone(),
                target_agent: agent,
                confidence: score,
                reasoning: format!(
                    "Expertise-based assignment with score {:.1}%",
                    score * 100.0
                ),
                priority_adjustment: None,
                estimated_duration: task.estimated_duration,
                dependencies: vec![],
            })
        } else {
            // Fallback to content-based
            self.delegate_content_based(task)
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:test_testing_task_delegation (lines 512-526) ---[0m
    fn test_testing_task_delegation() {
        let mut engine = MasterDelegationEngine::new(DelegationStrategy::ContentBased);

        let task = Task::new(
            "test-2".to_string(),
            "Write unit tests for API endpoints".to_string(),
            Priority::High,
            TaskType::Testing,
        );

        let decision = engine.delegate_task(task).unwrap();

        assert!(matches!(decision.target_agent, AgentRole::QA { .. }));
        assert!(decision.confidence > 0.8);
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:213-238 method delegate_task <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:512-526 function test_testing_task_delegation
  Similarity: 82.91%

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_task (lines 213-238) ---[0m
    pub fn delegate_task(&mut self, task: Task) -> Result<DelegationDecision> {
        info!(
            "🎯 Master analyzing task for delegation: {}",
            task.description
        );

        let decision = match self.strategy {
            DelegationStrategy::ContentBased => self.delegate_content_based(&task)?,
            DelegationStrategy::LoadBalanced => self.delegate_load_balanced(&task)?,
            DelegationStrategy::ExpertiseBased => self.delegate_expertise_based(&task)?,
            DelegationStrategy::WorkflowBased => self.delegate_workflow_based(&task)?,
            DelegationStrategy::Hybrid => self.delegate_hybrid(&task)?,
        };

        // Record the delegation decision
        self.task_history.push(decision.clone());

        info!(
            "✅ Master delegated task '{}' to {} with {:.1}% confidence",
            task.description,
            decision.target_agent.name(),
            decision.confidence * 100.0
        );

        Ok(decision)
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:test_testing_task_delegation (lines 512-526) ---[0m
    fn test_testing_task_delegation() {
        let mut engine = MasterDelegationEngine::new(DelegationStrategy::ContentBased);

        let task = Task::new(
            "test-2".to_string(),
            "Write unit tests for API endpoints".to_string(),
            Priority::High,
            TaskType::Testing,
        );

        let decision = engine.delegate_task(task).unwrap();

        assert!(matches!(decision.target_agent, AgentRole::QA { .. }));
        assert!(decision.confidence > 0.8);
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:448-470 method get_delegation_stats <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:512-526 function test_testing_task_delegation
  Similarity: 86.54%

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:get_delegation_stats (lines 448-470) ---[0m
    pub fn get_delegation_stats(&self) -> DelegationStats {
        let total_delegations = self.task_history.len();
        let mut agent_counts = HashMap::new();
        let mut total_confidence = 0.0;

        for decision in &self.task_history {
            *agent_counts
                .entry(decision.target_agent.name().to_string())
                .or_insert(0) += 1;
            total_confidence += decision.confidence;
        }

        DelegationStats {
            total_delegations,
            average_confidence: if total_delegations > 0 {
                total_confidence / total_delegations as f64
            } else {
                0.0
            },
            agent_distribution: agent_counts,
            strategy: self.strategy.clone(),
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:test_testing_task_delegation (lines 512-526) ---[0m
    fn test_testing_task_delegation() {
        let mut engine = MasterDelegationEngine::new(DelegationStrategy::ContentBased);

        let task = Task::new(
            "test-2".to_string(),
            "Write unit tests for API endpoints".to_string(),
            Priority::High,
            TaskType::Testing,
        );

        let decision = engine.delegate_task(task).unwrap();

        assert!(matches!(decision.target_agent, AgentRole::QA { .. }));
        assert!(decision.confidence > 0.8);
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:448-470 method get_delegation_stats <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:494-509 function test_content_based_delegation
  Similarity: 78.21%

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:get_delegation_stats (lines 448-470) ---[0m
    pub fn get_delegation_stats(&self) -> DelegationStats {
        let total_delegations = self.task_history.len();
        let mut agent_counts = HashMap::new();
        let mut total_confidence = 0.0;

        for decision in &self.task_history {
            *agent_counts
                .entry(decision.target_agent.name().to_string())
                .or_insert(0) += 1;
            total_confidence += decision.confidence;
        }

        DelegationStats {
            total_delegations,
            average_confidence: if total_delegations > 0 {
                total_confidence / total_delegations as f64
            } else {
                0.0
            },
            agent_distribution: agent_counts,
            strategy: self.strategy.clone(),
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:test_content_based_delegation (lines 494-509) ---[0m
    fn test_content_based_delegation() {
        let mut engine = MasterDelegationEngine::new(DelegationStrategy::ContentBased);

        let task = Task::new(
            "test-1".to_string(),
            "Create React component with CSS styling for user interface".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        let decision = engine.delegate_task(task).unwrap();

        assert!(matches!(decision.target_agent, AgentRole::Frontend { .. }));
        assert!(decision.confidence > 0.5);
        assert!(decision.reasoning.contains("Frontend"));
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:494-509 function test_content_based_delegation <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:512-526 function test_testing_task_delegation
  Similarity: 88.33%

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:test_content_based_delegation (lines 494-509) ---[0m
    fn test_content_based_delegation() {
        let mut engine = MasterDelegationEngine::new(DelegationStrategy::ContentBased);

        let task = Task::new(
            "test-1".to_string(),
            "Create React component with CSS styling for user interface".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        let decision = engine.delegate_task(task).unwrap();

        assert!(matches!(decision.target_agent, AgentRole::Frontend { .. }));
        assert!(decision.confidence > 0.5);
        assert!(decision.reasoning.contains("Frontend"));
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:test_testing_task_delegation (lines 512-526) ---[0m
    fn test_testing_task_delegation() {
        let mut engine = MasterDelegationEngine::new(DelegationStrategy::ContentBased);

        let task = Task::new(
            "test-2".to_string(),
            "Write unit tests for API endpoints".to_string(),
            Priority::High,
            TaskType::Testing,
        );

        let decision = engine.delegate_task(task).unwrap();

        assert!(matches!(decision.target_agent, AgentRole::QA { .. }));
        assert!(decision.confidence > 0.8);
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:373-390 method delegate_hybrid <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:512-526 function test_testing_task_delegation
  Similarity: 81.05%

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_hybrid (lines 373-390) ---[0m
    fn delegate_hybrid(&self, task: &Task) -> Result<DelegationDecision> {
        // Combine multiple strategies
        let content_decision = self.delegate_content_based(task)?;
        let load_decision = self.delegate_load_balanced(task)?;

        // Choose the decision with higher confidence
        if content_decision.confidence >= load_decision.confidence {
            Ok(DelegationDecision {
                reasoning: format!("Hybrid: Content-based ({})", content_decision.reasoning),
                ..content_decision
            })
        } else {
            Ok(DelegationDecision {
                reasoning: format!("Hybrid: Load-based ({})", load_decision.reasoning),
                ..load_decision
            })
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:test_testing_task_delegation (lines 512-526) ---[0m
    fn test_testing_task_delegation() {
        let mut engine = MasterDelegationEngine::new(DelegationStrategy::ContentBased);

        let task = Task::new(
            "test-2".to_string(),
            "Write unit tests for API endpoints".to_string(),
            Priority::High,
            TaskType::Testing,
        );

        let decision = engine.delegate_task(task).unwrap();

        assert!(matches!(decision.target_agent, AgentRole::QA { .. }));
        assert!(decision.confidence > 0.8);
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:373-390 method delegate_hybrid <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:494-509 function test_content_based_delegation
  Similarity: 73.47%

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:delegate_hybrid (lines 373-390) ---[0m
    fn delegate_hybrid(&self, task: &Task) -> Result<DelegationDecision> {
        // Combine multiple strategies
        let content_decision = self.delegate_content_based(task)?;
        let load_decision = self.delegate_load_balanced(task)?;

        // Choose the decision with higher confidence
        if content_decision.confidence >= load_decision.confidence {
            Ok(DelegationDecision {
                reasoning: format!("Hybrid: Content-based ({})", content_decision.reasoning),
                ..content_decision
            })
        } else {
            Ok(DelegationDecision {
                reasoning: format!("Hybrid: Load-based ({})", load_decision.reasoning),
                ..load_decision
            })
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:test_content_based_delegation (lines 494-509) ---[0m
    fn test_content_based_delegation() {
        let mut engine = MasterDelegationEngine::new(DelegationStrategy::ContentBased);

        let task = Task::new(
            "test-1".to_string(),
            "Create React component with CSS styling for user interface".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        let decision = engine.delegate_task(task).unwrap();

        assert!(matches!(decision.target_agent, AgentRole::Frontend { .. }));
        assert!(decision.confidence > 0.5);
        assert!(decision.reasoning.contains("Frontend"));
    }

  ./crates/ccswarm/src/orchestrator/master_delegation.rs:96-103 method new <-> ./crates/ccswarm/src/orchestrator/master_delegation.rs:207-210 method update_agent_metrics
  Similarity: 94.89%
  Classes: MasterDelegationEngine <-> MasterDelegationEngine

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:new (lines 96-103) ---[0m
    pub fn new(strategy: DelegationStrategy) -> Self {
        Self {
            strategy,
            agent_metrics: HashMap::new(),
            task_history: Vec::new(),
            delegation_rules: Self::default_delegation_rules(),
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/master_delegation.rs:update_agent_metrics (lines 207-210) ---[0m
    pub fn update_agent_metrics(&mut self, agent_role: AgentRole, metrics: AgentMetrics) {
        self.agent_metrics
            .insert(agent_role.name().to_string(), metrics);
    }


Duplicates in ./crates/ccswarm/src/template/tests.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/template/tests.rs:62-102 function test_in_memory_storage <-> ./crates/ccswarm/src/template/tests.rs:123-166 function test_template_manager_basic
  Similarity: 87.52%

[36m--- ./crates/ccswarm/src/template/tests.rs:test_in_memory_storage (lines 62-102) ---[0m
    async fn test_in_memory_storage() {
        let mut storage = InMemoryTemplateStorage::new();

        let template = Template::new(
            "test",
            "Test Template",
            "A test template",
            TemplateCategory::General,
        )
        .with_task_description("Test task");

        // Test save
        storage
            .save_template(&template)
            .await
            .expect("Failed to save template");

        // Test load
        let loaded = storage
            .load_template("test")
            .await
            .expect("Failed to load template");
        assert_eq!(loaded.id, "test");

        // Test list
        let templates = storage
            .list_templates()
            .await
            .expect("Failed to list templates");
        assert_eq!(templates.len(), 1);

        // Test exists
        assert!(storage
            .exists("test")
            .await
            .expect("Failed to check existence"));
        assert!(!storage
            .exists("nonexistent")
            .await
            .expect("Failed to check existence"));
    }

[36m--- ./crates/ccswarm/src/template/tests.rs:test_template_manager_basic (lines 123-166) ---[0m
    async fn test_template_manager_basic() {
        let storage = InMemoryTemplateStorage::new();
        let mut manager = TemplateManager::new(storage);

        let template = Template::new(
            "test-manager",
            "Test Manager Template",
            "A template for testing the manager",
            TemplateCategory::General,
        )
        .with_task_description("Create {{name}} with {{description}}")
        .with_variables(vec![
            TemplateVariable::text("name", "The name"),
            TemplateVariable::text("description", "The description"),
        ]);

        // Save template
        manager
            .save_template(template)
            .await
            .expect("Failed to save template");

        // Load template
        let loaded = manager
            .load_template("test-manager")
            .await
            .expect("Failed to load template");
        assert_eq!(loaded.id, "test-manager");

        // Test context and application
        let context = TemplateContext::new()
            .with_variable("name", "TestComponent")
            .with_variable("description", "A test component");

        let applied = manager
            .apply_template("test-manager", context)
            .await
            .expect("Failed to apply template");

        assert_eq!(
            applied.description,
            "Create TestComponent with A test component"
        );
    }

  ./crates/ccswarm/src/template/tests.rs:105-120 function test_predefined_templates <-> ./crates/ccswarm/src/template/tests.rs:123-166 function test_template_manager_basic
  Similarity: 84.03%

[36m--- ./crates/ccswarm/src/template/tests.rs:test_predefined_templates (lines 105-120) ---[0m
    fn test_predefined_templates() {
        let templates = PredefinedTemplates::get_all();
        assert!(!templates.is_empty());

        // Check that all templates are valid
        for template in &templates {
            assert!(template.is_valid(), "Template {} is invalid", template.id);
        }

        // Check specific templates exist
        let react_component = templates.iter().find(|t| t.id == "react-component");
        assert!(react_component.is_some());

        let api_endpoint = templates.iter().find(|t| t.id == "api-endpoint");
        assert!(api_endpoint.is_some());
    }

[36m--- ./crates/ccswarm/src/template/tests.rs:test_template_manager_basic (lines 123-166) ---[0m
    async fn test_template_manager_basic() {
        let storage = InMemoryTemplateStorage::new();
        let mut manager = TemplateManager::new(storage);

        let template = Template::new(
            "test-manager",
            "Test Manager Template",
            "A template for testing the manager",
            TemplateCategory::General,
        )
        .with_task_description("Create {{name}} with {{description}}")
        .with_variables(vec![
            TemplateVariable::text("name", "The name"),
            TemplateVariable::text("description", "The description"),
        ]);

        // Save template
        manager
            .save_template(template)
            .await
            .expect("Failed to save template");

        // Load template
        let loaded = manager
            .load_template("test-manager")
            .await
            .expect("Failed to load template");
        assert_eq!(loaded.id, "test-manager");

        // Test context and application
        let context = TemplateContext::new()
            .with_variable("name", "TestComponent")
            .with_variable("description", "A test component");

        let applied = manager
            .apply_template("test-manager", context)
            .await
            .expect("Failed to apply template");

        assert_eq!(
            applied.description,
            "Create TestComponent with A test component"
        );
    }

  ./crates/ccswarm/src/template/tests.rs:62-102 function test_in_memory_storage <-> ./crates/ccswarm/src/template/tests.rs:105-120 function test_predefined_templates
  Similarity: 77.61%

[36m--- ./crates/ccswarm/src/template/tests.rs:test_in_memory_storage (lines 62-102) ---[0m
    async fn test_in_memory_storage() {
        let mut storage = InMemoryTemplateStorage::new();

        let template = Template::new(
            "test",
            "Test Template",
            "A test template",
            TemplateCategory::General,
        )
        .with_task_description("Test task");

        // Test save
        storage
            .save_template(&template)
            .await
            .expect("Failed to save template");

        // Test load
        let loaded = storage
            .load_template("test")
            .await
            .expect("Failed to load template");
        assert_eq!(loaded.id, "test");

        // Test list
        let templates = storage
            .list_templates()
            .await
            .expect("Failed to list templates");
        assert_eq!(templates.len(), 1);

        // Test exists
        assert!(storage
            .exists("test")
            .await
            .expect("Failed to check existence"));
        assert!(!storage
            .exists("nonexistent")
            .await
            .expect("Failed to check existence"));
    }

[36m--- ./crates/ccswarm/src/template/tests.rs:test_predefined_templates (lines 105-120) ---[0m
    fn test_predefined_templates() {
        let templates = PredefinedTemplates::get_all();
        assert!(!templates.is_empty());

        // Check that all templates are valid
        for template in &templates {
            assert!(template.is_valid(), "Template {} is invalid", template.id);
        }

        // Check specific templates exist
        let react_component = templates.iter().find(|t| t.id == "react-component");
        assert!(react_component.is_some());

        let api_endpoint = templates.iter().find(|t| t.id == "api-endpoint");
        assert!(api_endpoint.is_some());
    }

  ./crates/ccswarm/src/template/tests.rs:13-31 function test_template_creation <-> ./crates/ccswarm/src/template/tests.rs:34-48 function test_template_validation
  Similarity: 77.29%

[36m--- ./crates/ccswarm/src/template/tests.rs:test_template_creation (lines 13-31) ---[0m
    fn test_template_creation() {
        let template = Template::new(
            "test-template",
            "Test Template",
            "A test template",
            TemplateCategory::General,
        )
        .with_task_description("Create {{component_name}} component")
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development);

        assert_eq!(template.id, "test-template");
        assert_eq!(template.name, "Test Template");
        assert_eq!(
            template.task_description,
            "Create {{component_name}} component"
        );
        assert!(template.is_valid());
    }

[36m--- ./crates/ccswarm/src/template/tests.rs:test_template_validation (lines 34-48) ---[0m
    fn test_template_validation() {
        let invalid_template = Template::new("", "", "", TemplateCategory::General);

        assert!(!invalid_template.is_valid());

        let valid_template = Template::new(
            "valid-template",
            "Valid Template",
            "A valid template",
            TemplateCategory::General,
        )
        .with_task_description("Do something");

        assert!(valid_template.is_valid());
    }

  ./crates/ccswarm/src/template/tests.rs:13-31 function test_template_creation <-> ./crates/ccswarm/src/template/tests.rs:51-59 function test_template_variable
  Similarity: 88.68%

[36m--- ./crates/ccswarm/src/template/tests.rs:test_template_creation (lines 13-31) ---[0m
    fn test_template_creation() {
        let template = Template::new(
            "test-template",
            "Test Template",
            "A test template",
            TemplateCategory::General,
        )
        .with_task_description("Create {{component_name}} component")
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development);

        assert_eq!(template.id, "test-template");
        assert_eq!(template.name, "Test Template");
        assert_eq!(
            template.task_description,
            "Create {{component_name}} component"
        );
        assert!(template.is_valid());
    }

[36m--- ./crates/ccswarm/src/template/tests.rs:test_template_variable (lines 51-59) ---[0m
    fn test_template_variable() {
        let var = TemplateVariable::text("component_name", "Name of the component")
            .with_example("MyComponent");

        assert_eq!(var.name, "component_name");
        assert_eq!(var.description, "Name of the component");
        assert!(var.required);
        assert_eq!(var.example, Some("MyComponent".to_string()));
    }

  ./crates/ccswarm/src/template/tests.rs:34-48 function test_template_validation <-> ./crates/ccswarm/src/template/tests.rs:51-59 function test_template_variable
  Similarity: 71.94%

[36m--- ./crates/ccswarm/src/template/tests.rs:test_template_validation (lines 34-48) ---[0m
    fn test_template_validation() {
        let invalid_template = Template::new("", "", "", TemplateCategory::General);

        assert!(!invalid_template.is_valid());

        let valid_template = Template::new(
            "valid-template",
            "Valid Template",
            "A valid template",
            TemplateCategory::General,
        )
        .with_task_description("Do something");

        assert!(valid_template.is_valid());
    }

[36m--- ./crates/ccswarm/src/template/tests.rs:test_template_variable (lines 51-59) ---[0m
    fn test_template_variable() {
        let var = TemplateVariable::text("component_name", "Name of the component")
            .with_example("MyComponent");

        assert_eq!(var.name, "component_name");
        assert_eq!(var.description, "Name of the component");
        assert!(var.required);
        assert_eq!(var.example, Some("MyComponent".to_string()));
    }


Duplicates in ./crates/ccswarm/tests/integration_masterclaude.rs:
------------------------------------------------------------
  ./crates/ccswarm/tests/integration_masterclaude.rs:14-63 function create_test_config <-> ./crates/ccswarm/tests/integration_masterclaude.rs:204-250 function test_quality_review_flow
  Similarity: 88.50%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:create_test_config (lines 14-63) ---[0m
fn create_test_config() -> CcswarmConfig {
    let mut agents = HashMap::new();
    agents.insert(
        "frontend".to_string(),
        AgentConfig {
            specialization: "frontend".to_string(),
            worktree: "agents/frontend".to_string(),
            branch: "feature/frontend".to_string(),
            claude_config: ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );
    agents.insert(
        "backend".to_string(),
        AgentConfig {
            specialization: "backend".to_string(),
            worktree: "agents/backend".to_string(),
            branch: "feature/backend".to_string(),
            claude_config: ClaudeConfig::for_agent("backend"),
            claude_md_template: "backend_specialist".to_string(),
        },
    );

    CcswarmConfig {
        project: ProjectConfig {
            name: "TestProject".to_string(),
            repository: RepositoryConfig {
                url: "https://github.com/test/repo".to_string(),
                main_branch: "main".to_string(),
            },
            master_claude: MasterClaudeConfig {
                role: "technical_lead".to_string(),
                quality_threshold: 0.9,
                think_mode: ThinkMode::UltraThink,
                permission_level: "supervised".to_string(),
                claude_config: ClaudeConfig::for_master(),
                enable_proactive_mode: false, // Disable for tests
                proactive_frequency: 300,
                high_frequency: 60,
            },
        },
        agents,
        coordination: CoordinationConfig {
            communication_method: "json_files".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_commit".to_string(),
            master_review_trigger: "all_tasks_complete".to_string(),
        },
    }
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_quality_review_flow (lines 204-250) ---[0m
async fn test_quality_review_flow() {
    use ccswarm::agent::{Priority, Task, TaskResult, TaskType};

    // Create a task result
    let task = Task::new(
        "test-task-001".to_string(),
        "Implement user login".to_string(),
        Priority::High,
        TaskType::Feature,
    );

    let result = TaskResult {
        success: true,
        output: json!({
            "message": "Login component created successfully",
            "files_created": ["src/components/Login.tsx"],
            "files_modified": [],
            "files_deleted": []
        }),
        error: None,
        duration: Duration::from_secs(120),
    };

    // Create quality issue message
    let quality_msg = AgentMessage::QualityIssue {
        agent_id: "frontend-agent".to_string(),
        task_id: task.id.clone(),
        issues: vec![
            "Low test coverage".to_string(),
            "Missing documentation".to_string(),
        ],
    };

    // Verify message structure
    match quality_msg {
        AgentMessage::QualityIssue {
            agent_id,
            task_id,
            issues,
        } => {
            assert_eq!(agent_id, "frontend-agent");
            assert_eq!(task_id, "test-task-001");
            assert_eq!(issues.len(), 2);
        }
        _ => panic!("Unexpected message type"),
    }
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:164-201 function test_agent_message_handling <-> ./crates/ccswarm/tests/integration_masterclaude.rs:204-250 function test_quality_review_flow
  Similarity: 90.00%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_agent_message_handling (lines 164-201) ---[0m
async fn test_agent_message_handling() {
    // Create coordination bus
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Test different message types
    let messages = vec![
        AgentMessage::StatusUpdate {
            agent_id: "test-agent".to_string(),
            status: ccswarm::agent::AgentStatus::Available,
            metrics: json!({"cpu": 45.2, "memory": 1024}),
        },
        AgentMessage::TaskProgress {
            agent_id: "test-agent".to_string(),
            task_id: "task-001".to_string(),
            progress: 0.75,
            message: "75% complete".to_string(),
        },
        AgentMessage::HelpRequest {
            agent_id: "test-agent".to_string(),
            context: "Need help with API integration".to_string(),
            priority: ccswarm::coordination::MessagePriority::High,
        },
    ];

    // Send all messages
    for msg in &messages {
        bus.send_message(msg.clone()).await.unwrap();
    }

    // Wait for propagation
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Verify we can receive messages
    for _ in 0..messages.len() {
        let result = bus.receive_message().await;
        assert!(result.is_ok(), "Failed to receive message");
    }
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_quality_review_flow (lines 204-250) ---[0m
async fn test_quality_review_flow() {
    use ccswarm::agent::{Priority, Task, TaskResult, TaskType};

    // Create a task result
    let task = Task::new(
        "test-task-001".to_string(),
        "Implement user login".to_string(),
        Priority::High,
        TaskType::Feature,
    );

    let result = TaskResult {
        success: true,
        output: json!({
            "message": "Login component created successfully",
            "files_created": ["src/components/Login.tsx"],
            "files_modified": [],
            "files_deleted": []
        }),
        error: None,
        duration: Duration::from_secs(120),
    };

    // Create quality issue message
    let quality_msg = AgentMessage::QualityIssue {
        agent_id: "frontend-agent".to_string(),
        task_id: task.id.clone(),
        issues: vec![
            "Low test coverage".to_string(),
            "Missing documentation".to_string(),
        ],
    };

    // Verify message structure
    match quality_msg {
        AgentMessage::QualityIssue {
            agent_id,
            task_id,
            issues,
        } => {
            assert_eq!(agent_id, "frontend-agent");
            assert_eq!(task_id, "test-task-001");
            assert_eq!(issues.len(), 2);
        }
        _ => panic!("Unexpected message type"),
    }
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:14-63 function create_test_config <-> ./crates/ccswarm/tests/integration_masterclaude.rs:164-201 function test_agent_message_handling
  Similarity: 84.76%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:create_test_config (lines 14-63) ---[0m
fn create_test_config() -> CcswarmConfig {
    let mut agents = HashMap::new();
    agents.insert(
        "frontend".to_string(),
        AgentConfig {
            specialization: "frontend".to_string(),
            worktree: "agents/frontend".to_string(),
            branch: "feature/frontend".to_string(),
            claude_config: ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );
    agents.insert(
        "backend".to_string(),
        AgentConfig {
            specialization: "backend".to_string(),
            worktree: "agents/backend".to_string(),
            branch: "feature/backend".to_string(),
            claude_config: ClaudeConfig::for_agent("backend"),
            claude_md_template: "backend_specialist".to_string(),
        },
    );

    CcswarmConfig {
        project: ProjectConfig {
            name: "TestProject".to_string(),
            repository: RepositoryConfig {
                url: "https://github.com/test/repo".to_string(),
                main_branch: "main".to_string(),
            },
            master_claude: MasterClaudeConfig {
                role: "technical_lead".to_string(),
                quality_threshold: 0.9,
                think_mode: ThinkMode::UltraThink,
                permission_level: "supervised".to_string(),
                claude_config: ClaudeConfig::for_master(),
                enable_proactive_mode: false, // Disable for tests
                proactive_frequency: 300,
                high_frequency: 60,
            },
        },
        agents,
        coordination: CoordinationConfig {
            communication_method: "json_files".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_commit".to_string(),
            master_review_trigger: "all_tasks_complete".to_string(),
        },
    }
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_agent_message_handling (lines 164-201) ---[0m
async fn test_agent_message_handling() {
    // Create coordination bus
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Test different message types
    let messages = vec![
        AgentMessage::StatusUpdate {
            agent_id: "test-agent".to_string(),
            status: ccswarm::agent::AgentStatus::Available,
            metrics: json!({"cpu": 45.2, "memory": 1024}),
        },
        AgentMessage::TaskProgress {
            agent_id: "test-agent".to_string(),
            task_id: "task-001".to_string(),
            progress: 0.75,
            message: "75% complete".to_string(),
        },
        AgentMessage::HelpRequest {
            agent_id: "test-agent".to_string(),
            context: "Need help with API integration".to_string(),
            priority: ccswarm::coordination::MessagePriority::High,
        },
    ];

    // Send all messages
    for msg in &messages {
        bus.send_message(msg.clone()).await.unwrap();
    }

    // Wait for propagation
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Verify we can receive messages
    for _ in 0..messages.len() {
        let result = bus.receive_message().await;
        assert!(result.is_ok(), "Failed to receive message");
    }
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:204-250 function test_quality_review_flow <-> ./crates/ccswarm/tests/integration_masterclaude.rs:352-394 function test_coordination_message_types
  Similarity: 81.62%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_quality_review_flow (lines 204-250) ---[0m
async fn test_quality_review_flow() {
    use ccswarm::agent::{Priority, Task, TaskResult, TaskType};

    // Create a task result
    let task = Task::new(
        "test-task-001".to_string(),
        "Implement user login".to_string(),
        Priority::High,
        TaskType::Feature,
    );

    let result = TaskResult {
        success: true,
        output: json!({
            "message": "Login component created successfully",
            "files_created": ["src/components/Login.tsx"],
            "files_modified": [],
            "files_deleted": []
        }),
        error: None,
        duration: Duration::from_secs(120),
    };

    // Create quality issue message
    let quality_msg = AgentMessage::QualityIssue {
        agent_id: "frontend-agent".to_string(),
        task_id: task.id.clone(),
        issues: vec![
            "Low test coverage".to_string(),
            "Missing documentation".to_string(),
        ],
    };

    // Verify message structure
    match quality_msg {
        AgentMessage::QualityIssue {
            agent_id,
            task_id,
            issues,
        } => {
            assert_eq!(agent_id, "frontend-agent");
            assert_eq!(task_id, "test-task-001");
            assert_eq!(issues.len(), 2);
        }
        _ => panic!("Unexpected message type"),
    }
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_coordination_message_types (lines 352-394) ---[0m
async fn test_coordination_message_types() {
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Test TaskGenerated message
    let task_gen_msg = AgentMessage::TaskGenerated {
        task_id: "gen-task-001".to_string(),
        description: "Implement caching layer".to_string(),
        reasoning: "Performance optimization needed".to_string(),
    };

    bus.send_message(task_gen_msg).await.unwrap();

    // Test RequestAssistance message
    let assist_msg = AgentMessage::RequestAssistance {
        agent_id: "backend-agent".to_string(),
        task_id: "task-002".to_string(),
        reason: "Need expertise on GraphQL implementation".to_string(),
    };

    bus.send_message(assist_msg).await.unwrap();

    // Test Custom message
    let custom_msg = AgentMessage::Custom {
        message_type: "performance_metric".to_string(),
        data: json!({
            "agent_id": "frontend-agent",
            "metric": "render_time",
            "value": 250,
            "unit": "ms"
        }),
    };

    bus.send_message(custom_msg).await.unwrap();

    // Wait and verify all messages were sent
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Receive and verify messages
    for _ in 0..3 {
        let result = bus.receive_message().await;
        assert!(result.is_ok(), "Should receive all message types");
    }
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:97-129 function test_coordination_bus_message_flow <-> ./crates/ccswarm/tests/integration_masterclaude.rs:204-250 function test_quality_review_flow
  Similarity: 91.18%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_coordination_bus_message_flow (lines 97-129) ---[0m
async fn test_coordination_bus_message_flow() {
    // Create coordination bus
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Send a task assignment message
    let task_msg = AgentMessage::TaskAssignment {
        task_id: "test-task-001".to_string(),
        agent_id: "frontend-agent".to_string(),
        task_data: json!({
            "description": "Create login component",
            "priority": "high"
        }),
    };

    bus.send_message(task_msg.clone()).await.unwrap();

    // Wait for message propagation
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Try to receive the message
    match bus.receive_message().await {
        Ok(received_msg) => match received_msg {
            AgentMessage::TaskAssignment {
                task_id, agent_id, ..
            } => {
                assert_eq!(task_id, "test-task-001");
                assert_eq!(agent_id, "frontend-agent");
            }
            _ => panic!("Unexpected message type received"),
        },
        Err(e) => panic!("Failed to receive message: {}", e),
    }
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_quality_review_flow (lines 204-250) ---[0m
async fn test_quality_review_flow() {
    use ccswarm::agent::{Priority, Task, TaskResult, TaskType};

    // Create a task result
    let task = Task::new(
        "test-task-001".to_string(),
        "Implement user login".to_string(),
        Priority::High,
        TaskType::Feature,
    );

    let result = TaskResult {
        success: true,
        output: json!({
            "message": "Login component created successfully",
            "files_created": ["src/components/Login.tsx"],
            "files_modified": [],
            "files_deleted": []
        }),
        error: None,
        duration: Duration::from_secs(120),
    };

    // Create quality issue message
    let quality_msg = AgentMessage::QualityIssue {
        agent_id: "frontend-agent".to_string(),
        task_id: task.id.clone(),
        issues: vec![
            "Low test coverage".to_string(),
            "Missing documentation".to_string(),
        ],
    };

    // Verify message structure
    match quality_msg {
        AgentMessage::QualityIssue {
            agent_id,
            task_id,
            issues,
        } => {
            assert_eq!(agent_id, "frontend-agent");
            assert_eq!(task_id, "test-task-001");
            assert_eq!(issues.len(), 2);
        }
        _ => panic!("Unexpected message type"),
    }
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:164-201 function test_agent_message_handling <-> ./crates/ccswarm/tests/integration_masterclaude.rs:352-394 function test_coordination_message_types
  Similarity: 89.73%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_agent_message_handling (lines 164-201) ---[0m
async fn test_agent_message_handling() {
    // Create coordination bus
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Test different message types
    let messages = vec![
        AgentMessage::StatusUpdate {
            agent_id: "test-agent".to_string(),
            status: ccswarm::agent::AgentStatus::Available,
            metrics: json!({"cpu": 45.2, "memory": 1024}),
        },
        AgentMessage::TaskProgress {
            agent_id: "test-agent".to_string(),
            task_id: "task-001".to_string(),
            progress: 0.75,
            message: "75% complete".to_string(),
        },
        AgentMessage::HelpRequest {
            agent_id: "test-agent".to_string(),
            context: "Need help with API integration".to_string(),
            priority: ccswarm::coordination::MessagePriority::High,
        },
    ];

    // Send all messages
    for msg in &messages {
        bus.send_message(msg.clone()).await.unwrap();
    }

    // Wait for propagation
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Verify we can receive messages
    for _ in 0..messages.len() {
        let result = bus.receive_message().await;
        assert!(result.is_ok(), "Failed to receive message");
    }
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_coordination_message_types (lines 352-394) ---[0m
async fn test_coordination_message_types() {
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Test TaskGenerated message
    let task_gen_msg = AgentMessage::TaskGenerated {
        task_id: "gen-task-001".to_string(),
        description: "Implement caching layer".to_string(),
        reasoning: "Performance optimization needed".to_string(),
    };

    bus.send_message(task_gen_msg).await.unwrap();

    // Test RequestAssistance message
    let assist_msg = AgentMessage::RequestAssistance {
        agent_id: "backend-agent".to_string(),
        task_id: "task-002".to_string(),
        reason: "Need expertise on GraphQL implementation".to_string(),
    };

    bus.send_message(assist_msg).await.unwrap();

    // Test Custom message
    let custom_msg = AgentMessage::Custom {
        message_type: "performance_metric".to_string(),
        data: json!({
            "agent_id": "frontend-agent",
            "metric": "render_time",
            "value": 250,
            "unit": "ms"
        }),
    };

    bus.send_message(custom_msg).await.unwrap();

    // Wait and verify all messages were sent
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Receive and verify messages
    for _ in 0..3 {
        let result = bus.receive_message().await;
        assert!(result.is_ok(), "Should receive all message types");
    }
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:14-63 function create_test_config <-> ./crates/ccswarm/tests/integration_masterclaude.rs:97-129 function test_coordination_bus_message_flow
  Similarity: 86.51%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:create_test_config (lines 14-63) ---[0m
fn create_test_config() -> CcswarmConfig {
    let mut agents = HashMap::new();
    agents.insert(
        "frontend".to_string(),
        AgentConfig {
            specialization: "frontend".to_string(),
            worktree: "agents/frontend".to_string(),
            branch: "feature/frontend".to_string(),
            claude_config: ClaudeConfig::for_agent("frontend"),
            claude_md_template: "frontend_specialist".to_string(),
        },
    );
    agents.insert(
        "backend".to_string(),
        AgentConfig {
            specialization: "backend".to_string(),
            worktree: "agents/backend".to_string(),
            branch: "feature/backend".to_string(),
            claude_config: ClaudeConfig::for_agent("backend"),
            claude_md_template: "backend_specialist".to_string(),
        },
    );

    CcswarmConfig {
        project: ProjectConfig {
            name: "TestProject".to_string(),
            repository: RepositoryConfig {
                url: "https://github.com/test/repo".to_string(),
                main_branch: "main".to_string(),
            },
            master_claude: MasterClaudeConfig {
                role: "technical_lead".to_string(),
                quality_threshold: 0.9,
                think_mode: ThinkMode::UltraThink,
                permission_level: "supervised".to_string(),
                claude_config: ClaudeConfig::for_master(),
                enable_proactive_mode: false, // Disable for tests
                proactive_frequency: 300,
                high_frequency: 60,
            },
        },
        agents,
        coordination: CoordinationConfig {
            communication_method: "json_files".to_string(),
            sync_interval: 30,
            quality_gate_frequency: "on_commit".to_string(),
            master_review_trigger: "all_tasks_complete".to_string(),
        },
    }
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_coordination_bus_message_flow (lines 97-129) ---[0m
async fn test_coordination_bus_message_flow() {
    // Create coordination bus
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Send a task assignment message
    let task_msg = AgentMessage::TaskAssignment {
        task_id: "test-task-001".to_string(),
        agent_id: "frontend-agent".to_string(),
        task_data: json!({
            "description": "Create login component",
            "priority": "high"
        }),
    };

    bus.send_message(task_msg.clone()).await.unwrap();

    // Wait for message propagation
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Try to receive the message
    match bus.receive_message().await {
        Ok(received_msg) => match received_msg {
            AgentMessage::TaskAssignment {
                task_id, agent_id, ..
            } => {
                assert_eq!(task_id, "test-task-001");
                assert_eq!(agent_id, "frontend-agent");
            }
            _ => panic!("Unexpected message type received"),
        },
        Err(e) => panic!("Failed to receive message: {}", e),
    }
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:204-250 function test_quality_review_flow <-> ./crates/ccswarm/tests/integration_masterclaude.rs:318-349 function test_task_result_creation
  Similarity: 84.52%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_quality_review_flow (lines 204-250) ---[0m
async fn test_quality_review_flow() {
    use ccswarm::agent::{Priority, Task, TaskResult, TaskType};

    // Create a task result
    let task = Task::new(
        "test-task-001".to_string(),
        "Implement user login".to_string(),
        Priority::High,
        TaskType::Feature,
    );

    let result = TaskResult {
        success: true,
        output: json!({
            "message": "Login component created successfully",
            "files_created": ["src/components/Login.tsx"],
            "files_modified": [],
            "files_deleted": []
        }),
        error: None,
        duration: Duration::from_secs(120),
    };

    // Create quality issue message
    let quality_msg = AgentMessage::QualityIssue {
        agent_id: "frontend-agent".to_string(),
        task_id: task.id.clone(),
        issues: vec![
            "Low test coverage".to_string(),
            "Missing documentation".to_string(),
        ],
    };

    // Verify message structure
    match quality_msg {
        AgentMessage::QualityIssue {
            agent_id,
            task_id,
            issues,
        } => {
            assert_eq!(agent_id, "frontend-agent");
            assert_eq!(task_id, "test-task-001");
            assert_eq!(issues.len(), 2);
        }
        _ => panic!("Unexpected message type"),
    }
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_task_result_creation (lines 318-349) ---[0m
async fn test_task_result_creation() {
    use ccswarm::agent::TaskResult;

    // Test successful result
    let success_result = TaskResult::success(
        json!({
            "message": "Task completed successfully",
            "details": {
                "files_created": 5,
                "tests_passed": 10
            }
        }),
        Duration::from_secs(60),
    );

    assert!(success_result.success);
    assert!(success_result.error.is_none());
    assert_eq!(success_result.duration, Duration::from_secs(60));

    // Test failure result
    let failure_result = TaskResult::failure(
        "Failed to connect to database".to_string(),
        Duration::from_secs(30),
    );

    assert!(!failure_result.success);
    assert_eq!(
        failure_result.error,
        Some("Failed to connect to database".to_string())
    );
    assert_eq!(failure_result.duration, Duration::from_secs(30));
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:97-129 function test_coordination_bus_message_flow <-> ./crates/ccswarm/tests/integration_masterclaude.rs:352-394 function test_coordination_message_types
  Similarity: 86.54%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_coordination_bus_message_flow (lines 97-129) ---[0m
async fn test_coordination_bus_message_flow() {
    // Create coordination bus
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Send a task assignment message
    let task_msg = AgentMessage::TaskAssignment {
        task_id: "test-task-001".to_string(),
        agent_id: "frontend-agent".to_string(),
        task_data: json!({
            "description": "Create login component",
            "priority": "high"
        }),
    };

    bus.send_message(task_msg.clone()).await.unwrap();

    // Wait for message propagation
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Try to receive the message
    match bus.receive_message().await {
        Ok(received_msg) => match received_msg {
            AgentMessage::TaskAssignment {
                task_id, agent_id, ..
            } => {
                assert_eq!(task_id, "test-task-001");
                assert_eq!(agent_id, "frontend-agent");
            }
            _ => panic!("Unexpected message type received"),
        },
        Err(e) => panic!("Failed to receive message: {}", e),
    }
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_coordination_message_types (lines 352-394) ---[0m
async fn test_coordination_message_types() {
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Test TaskGenerated message
    let task_gen_msg = AgentMessage::TaskGenerated {
        task_id: "gen-task-001".to_string(),
        description: "Implement caching layer".to_string(),
        reasoning: "Performance optimization needed".to_string(),
    };

    bus.send_message(task_gen_msg).await.unwrap();

    // Test RequestAssistance message
    let assist_msg = AgentMessage::RequestAssistance {
        agent_id: "backend-agent".to_string(),
        task_id: "task-002".to_string(),
        reason: "Need expertise on GraphQL implementation".to_string(),
    };

    bus.send_message(assist_msg).await.unwrap();

    // Test Custom message
    let custom_msg = AgentMessage::Custom {
        message_type: "performance_metric".to_string(),
        data: json!({
            "agent_id": "frontend-agent",
            "metric": "render_time",
            "value": 250,
            "unit": "ms"
        }),
    };

    bus.send_message(custom_msg).await.unwrap();

    // Wait and verify all messages were sent
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Receive and verify messages
    for _ in 0..3 {
        let result = bus.receive_message().await;
        assert!(result.is_ok(), "Should receive all message types");
    }
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:97-129 function test_coordination_bus_message_flow <-> ./crates/ccswarm/tests/integration_masterclaude.rs:164-201 function test_agent_message_handling
  Similarity: 92.40%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_coordination_bus_message_flow (lines 97-129) ---[0m
async fn test_coordination_bus_message_flow() {
    // Create coordination bus
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Send a task assignment message
    let task_msg = AgentMessage::TaskAssignment {
        task_id: "test-task-001".to_string(),
        agent_id: "frontend-agent".to_string(),
        task_data: json!({
            "description": "Create login component",
            "priority": "high"
        }),
    };

    bus.send_message(task_msg.clone()).await.unwrap();

    // Wait for message propagation
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Try to receive the message
    match bus.receive_message().await {
        Ok(received_msg) => match received_msg {
            AgentMessage::TaskAssignment {
                task_id, agent_id, ..
            } => {
                assert_eq!(task_id, "test-task-001");
                assert_eq!(agent_id, "frontend-agent");
            }
            _ => panic!("Unexpected message type received"),
        },
        Err(e) => panic!("Failed to receive message: {}", e),
    }
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_agent_message_handling (lines 164-201) ---[0m
async fn test_agent_message_handling() {
    // Create coordination bus
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Test different message types
    let messages = vec![
        AgentMessage::StatusUpdate {
            agent_id: "test-agent".to_string(),
            status: ccswarm::agent::AgentStatus::Available,
            metrics: json!({"cpu": 45.2, "memory": 1024}),
        },
        AgentMessage::TaskProgress {
            agent_id: "test-agent".to_string(),
            task_id: "task-001".to_string(),
            progress: 0.75,
            message: "75% complete".to_string(),
        },
        AgentMessage::HelpRequest {
            agent_id: "test-agent".to_string(),
            context: "Need help with API integration".to_string(),
            priority: ccswarm::coordination::MessagePriority::High,
        },
    ];

    // Send all messages
    for msg in &messages {
        bus.send_message(msg.clone()).await.unwrap();
    }

    // Wait for propagation
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Verify we can receive messages
    for _ in 0..messages.len() {
        let result = bus.receive_message().await;
        assert!(result.is_ok(), "Failed to receive message");
    }
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:318-349 function test_task_result_creation <-> ./crates/ccswarm/tests/integration_masterclaude.rs:352-394 function test_coordination_message_types
  Similarity: 84.89%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_task_result_creation (lines 318-349) ---[0m
async fn test_task_result_creation() {
    use ccswarm::agent::TaskResult;

    // Test successful result
    let success_result = TaskResult::success(
        json!({
            "message": "Task completed successfully",
            "details": {
                "files_created": 5,
                "tests_passed": 10
            }
        }),
        Duration::from_secs(60),
    );

    assert!(success_result.success);
    assert!(success_result.error.is_none());
    assert_eq!(success_result.duration, Duration::from_secs(60));

    // Test failure result
    let failure_result = TaskResult::failure(
        "Failed to connect to database".to_string(),
        Duration::from_secs(30),
    );

    assert!(!failure_result.success);
    assert_eq!(
        failure_result.error,
        Some("Failed to connect to database".to_string())
    );
    assert_eq!(failure_result.duration, Duration::from_secs(30));
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_coordination_message_types (lines 352-394) ---[0m
async fn test_coordination_message_types() {
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Test TaskGenerated message
    let task_gen_msg = AgentMessage::TaskGenerated {
        task_id: "gen-task-001".to_string(),
        description: "Implement caching layer".to_string(),
        reasoning: "Performance optimization needed".to_string(),
    };

    bus.send_message(task_gen_msg).await.unwrap();

    // Test RequestAssistance message
    let assist_msg = AgentMessage::RequestAssistance {
        agent_id: "backend-agent".to_string(),
        task_id: "task-002".to_string(),
        reason: "Need expertise on GraphQL implementation".to_string(),
    };

    bus.send_message(assist_msg).await.unwrap();

    // Test Custom message
    let custom_msg = AgentMessage::Custom {
        message_type: "performance_metric".to_string(),
        data: json!({
            "agent_id": "frontend-agent",
            "metric": "render_time",
            "value": 250,
            "unit": "ms"
        }),
    };

    bus.send_message(custom_msg).await.unwrap();

    // Wait and verify all messages were sent
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Receive and verify messages
    for _ in 0..3 {
        let result = bus.receive_message().await;
        assert!(result.is_ok(), "Should receive all message types");
    }
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:253-296 function test_master_claude_with_ai_session_integration <-> ./crates/ccswarm/tests/integration_masterclaude.rs:352-394 function test_coordination_message_types
  Similarity: 72.75%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_master_claude_with_ai_session_integration (lines 253-296) ---[0m
async fn test_master_claude_with_ai_session_integration() {
    let temp_dir = TempDir::new().unwrap();
    let repo_path = temp_dir.path().to_path_buf();

    // Initialize git repo
    ccswarm::git::WorktreeManager::init_if_needed(&repo_path)
        .await
        .unwrap();

    // Create MasterClaude with AI-session enabled
    let mut config = create_test_config();
    config.project.master_claude.enable_proactive_mode = false; // Disable for testing

    let master = MasterClaude::new(config, repo_path.clone()).await.unwrap();

    // Create a task that would use AI-session
    use ccswarm::agent::{Priority, Task, TaskType};
    let task = Task::new(
        "ai-session-task".to_string(),
        "Create API endpoint with AI-session support".to_string(),
        Priority::High,
        TaskType::Development,
    );

    // Add metadata indicating AI-session usage
    let mut enhanced_task = task.clone();
    enhanced_task.metadata = Some(serde_json::Map::new());
    if let Some(metadata) = enhanced_task.metadata.as_mut() {
        metadata.insert("ai_session_enabled".to_string(), json!(true));
        metadata.insert("context_compression".to_string(), json!("enabled"));
    }

    // Add task
    master.add_task(enhanced_task).await.unwrap();

    // Verify task has AI-session metadata
    let state = master.state.read().await;
    assert_eq!(state.pending_tasks.len(), 1);
    let pending_task = &state.pending_tasks[0];
    assert!(pending_task.metadata.is_some());

    let metadata = pending_task.metadata.as_ref().unwrap();
    assert_eq!(metadata.get("ai_session_enabled"), Some(&json!(true)));
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_coordination_message_types (lines 352-394) ---[0m
async fn test_coordination_message_types() {
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Test TaskGenerated message
    let task_gen_msg = AgentMessage::TaskGenerated {
        task_id: "gen-task-001".to_string(),
        description: "Implement caching layer".to_string(),
        reasoning: "Performance optimization needed".to_string(),
    };

    bus.send_message(task_gen_msg).await.unwrap();

    // Test RequestAssistance message
    let assist_msg = AgentMessage::RequestAssistance {
        agent_id: "backend-agent".to_string(),
        task_id: "task-002".to_string(),
        reason: "Need expertise on GraphQL implementation".to_string(),
    };

    bus.send_message(assist_msg).await.unwrap();

    // Test Custom message
    let custom_msg = AgentMessage::Custom {
        message_type: "performance_metric".to_string(),
        data: json!({
            "agent_id": "frontend-agent",
            "metric": "render_time",
            "value": 250,
            "unit": "ms"
        }),
    };

    bus.send_message(custom_msg).await.unwrap();

    // Wait and verify all messages were sent
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Receive and verify messages
    for _ in 0..3 {
        let result = bus.receive_message().await;
        assert!(result.is_ok(), "Should receive all message types");
    }
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:132-161 function test_master_claude_task_assignment <-> ./crates/ccswarm/tests/integration_masterclaude.rs:352-394 function test_coordination_message_types
  Similarity: 85.81%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_master_claude_task_assignment (lines 132-161) ---[0m
async fn test_master_claude_task_assignment() {
    let temp_dir = TempDir::new().unwrap();
    let repo_path = temp_dir.path().to_path_buf();

    // Initialize git repo
    ccswarm::git::WorktreeManager::init_if_needed(&repo_path)
        .await
        .unwrap();

    // Create and initialize MasterClaude
    let config = create_test_config();
    let master = MasterClaude::new(config, repo_path.clone()).await.unwrap();

    // Create a test task
    use ccswarm::agent::{Priority, Task, TaskType};
    let task = Task::new(
        "test-task-001".to_string(),
        "Create user authentication system".to_string(),
        Priority::High,
        TaskType::Feature,
    );

    // Add task to queue
    master.add_task(task.clone()).await.unwrap();

    // Verify task was added to pending tasks
    let state = master.state.read().await;
    assert_eq!(state.pending_tasks.len(), 1);
    assert_eq!(state.pending_tasks[0].id, "test-task-001");
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_coordination_message_types (lines 352-394) ---[0m
async fn test_coordination_message_types() {
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Test TaskGenerated message
    let task_gen_msg = AgentMessage::TaskGenerated {
        task_id: "gen-task-001".to_string(),
        description: "Implement caching layer".to_string(),
        reasoning: "Performance optimization needed".to_string(),
    };

    bus.send_message(task_gen_msg).await.unwrap();

    // Test RequestAssistance message
    let assist_msg = AgentMessage::RequestAssistance {
        agent_id: "backend-agent".to_string(),
        task_id: "task-002".to_string(),
        reason: "Need expertise on GraphQL implementation".to_string(),
    };

    bus.send_message(assist_msg).await.unwrap();

    // Test Custom message
    let custom_msg = AgentMessage::Custom {
        message_type: "performance_metric".to_string(),
        data: json!({
            "agent_id": "frontend-agent",
            "metric": "render_time",
            "value": 250,
            "unit": "ms"
        }),
    };

    bus.send_message(custom_msg).await.unwrap();

    // Wait and verify all messages were sent
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Receive and verify messages
    for _ in 0..3 {
        let result = bus.receive_message().await;
        assert!(result.is_ok(), "Should receive all message types");
    }
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:66-94 function test_master_claude_creation_and_initialization <-> ./crates/ccswarm/tests/integration_masterclaude.rs:352-394 function test_coordination_message_types
  Similarity: 85.65%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_master_claude_creation_and_initialization (lines 66-94) ---[0m
async fn test_master_claude_creation_and_initialization() {
    let temp_dir = TempDir::new().unwrap();
    let repo_path = temp_dir.path().to_path_buf();

    // Initialize git repo
    ccswarm::git::WorktreeManager::init_if_needed(&repo_path)
        .await
        .unwrap();

    // Create MasterClaude instance
    let config = create_test_config();
    let master = MasterClaude::new(config.clone(), repo_path.clone())
        .await
        .unwrap();

    // Verify basic properties
    assert!(master.id.starts_with("master-claude-"));
    assert_eq!(master.config.project.name, "TestProject");

    // Check initial state
    let state = master.state.read().await;
    assert_eq!(
        state.status,
        ccswarm::orchestrator::OrchestratorStatus::Initializing
    );
    assert_eq!(state.total_tasks_processed, 0);
    assert_eq!(state.successful_tasks, 0);
    assert_eq!(state.failed_tasks, 0);
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_coordination_message_types (lines 352-394) ---[0m
async fn test_coordination_message_types() {
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Test TaskGenerated message
    let task_gen_msg = AgentMessage::TaskGenerated {
        task_id: "gen-task-001".to_string(),
        description: "Implement caching layer".to_string(),
        reasoning: "Performance optimization needed".to_string(),
    };

    bus.send_message(task_gen_msg).await.unwrap();

    // Test RequestAssistance message
    let assist_msg = AgentMessage::RequestAssistance {
        agent_id: "backend-agent".to_string(),
        task_id: "task-002".to_string(),
        reason: "Need expertise on GraphQL implementation".to_string(),
    };

    bus.send_message(assist_msg).await.unwrap();

    // Test Custom message
    let custom_msg = AgentMessage::Custom {
        message_type: "performance_metric".to_string(),
        data: json!({
            "agent_id": "frontend-agent",
            "metric": "render_time",
            "value": 250,
            "unit": "ms"
        }),
    };

    bus.send_message(custom_msg).await.unwrap();

    // Wait and verify all messages were sent
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Receive and verify messages
    for _ in 0..3 {
        let result = bus.receive_message().await;
        assert!(result.is_ok(), "Should receive all message types");
    }
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:164-201 function test_agent_message_handling <-> ./crates/ccswarm/tests/integration_masterclaude.rs:318-349 function test_task_result_creation
  Similarity: 87.28%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_agent_message_handling (lines 164-201) ---[0m
async fn test_agent_message_handling() {
    // Create coordination bus
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Test different message types
    let messages = vec![
        AgentMessage::StatusUpdate {
            agent_id: "test-agent".to_string(),
            status: ccswarm::agent::AgentStatus::Available,
            metrics: json!({"cpu": 45.2, "memory": 1024}),
        },
        AgentMessage::TaskProgress {
            agent_id: "test-agent".to_string(),
            task_id: "task-001".to_string(),
            progress: 0.75,
            message: "75% complete".to_string(),
        },
        AgentMessage::HelpRequest {
            agent_id: "test-agent".to_string(),
            context: "Need help with API integration".to_string(),
            priority: ccswarm::coordination::MessagePriority::High,
        },
    ];

    // Send all messages
    for msg in &messages {
        bus.send_message(msg.clone()).await.unwrap();
    }

    // Wait for propagation
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Verify we can receive messages
    for _ in 0..messages.len() {
        let result = bus.receive_message().await;
        assert!(result.is_ok(), "Failed to receive message");
    }
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_task_result_creation (lines 318-349) ---[0m
async fn test_task_result_creation() {
    use ccswarm::agent::TaskResult;

    // Test successful result
    let success_result = TaskResult::success(
        json!({
            "message": "Task completed successfully",
            "details": {
                "files_created": 5,
                "tests_passed": 10
            }
        }),
        Duration::from_secs(60),
    );

    assert!(success_result.success);
    assert!(success_result.error.is_none());
    assert_eq!(success_result.duration, Duration::from_secs(60));

    // Test failure result
    let failure_result = TaskResult::failure(
        "Failed to connect to database".to_string(),
        Duration::from_secs(30),
    );

    assert!(!failure_result.success);
    assert_eq!(
        failure_result.error,
        Some("Failed to connect to database".to_string())
    );
    assert_eq!(failure_result.duration, Duration::from_secs(30));
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:132-161 function test_master_claude_task_assignment <-> ./crates/ccswarm/tests/integration_masterclaude.rs:253-296 function test_master_claude_with_ai_session_integration
  Similarity: 80.29%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_master_claude_task_assignment (lines 132-161) ---[0m
async fn test_master_claude_task_assignment() {
    let temp_dir = TempDir::new().unwrap();
    let repo_path = temp_dir.path().to_path_buf();

    // Initialize git repo
    ccswarm::git::WorktreeManager::init_if_needed(&repo_path)
        .await
        .unwrap();

    // Create and initialize MasterClaude
    let config = create_test_config();
    let master = MasterClaude::new(config, repo_path.clone()).await.unwrap();

    // Create a test task
    use ccswarm::agent::{Priority, Task, TaskType};
    let task = Task::new(
        "test-task-001".to_string(),
        "Create user authentication system".to_string(),
        Priority::High,
        TaskType::Feature,
    );

    // Add task to queue
    master.add_task(task.clone()).await.unwrap();

    // Verify task was added to pending tasks
    let state = master.state.read().await;
    assert_eq!(state.pending_tasks.len(), 1);
    assert_eq!(state.pending_tasks[0].id, "test-task-001");
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_master_claude_with_ai_session_integration (lines 253-296) ---[0m
async fn test_master_claude_with_ai_session_integration() {
    let temp_dir = TempDir::new().unwrap();
    let repo_path = temp_dir.path().to_path_buf();

    // Initialize git repo
    ccswarm::git::WorktreeManager::init_if_needed(&repo_path)
        .await
        .unwrap();

    // Create MasterClaude with AI-session enabled
    let mut config = create_test_config();
    config.project.master_claude.enable_proactive_mode = false; // Disable for testing

    let master = MasterClaude::new(config, repo_path.clone()).await.unwrap();

    // Create a task that would use AI-session
    use ccswarm::agent::{Priority, Task, TaskType};
    let task = Task::new(
        "ai-session-task".to_string(),
        "Create API endpoint with AI-session support".to_string(),
        Priority::High,
        TaskType::Development,
    );

    // Add metadata indicating AI-session usage
    let mut enhanced_task = task.clone();
    enhanced_task.metadata = Some(serde_json::Map::new());
    if let Some(metadata) = enhanced_task.metadata.as_mut() {
        metadata.insert("ai_session_enabled".to_string(), json!(true));
        metadata.insert("context_compression".to_string(), json!("enabled"));
    }

    // Add task
    master.add_task(enhanced_task).await.unwrap();

    // Verify task has AI-session metadata
    let state = master.state.read().await;
    assert_eq!(state.pending_tasks.len(), 1);
    let pending_task = &state.pending_tasks[0];
    assert!(pending_task.metadata.is_some());

    let metadata = pending_task.metadata.as_ref().unwrap();
    assert_eq!(metadata.get("ai_session_enabled"), Some(&json!(true)));
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:66-94 function test_master_claude_creation_and_initialization <-> ./crates/ccswarm/tests/integration_masterclaude.rs:253-296 function test_master_claude_with_ai_session_integration
  Similarity: 78.97%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_master_claude_creation_and_initialization (lines 66-94) ---[0m
async fn test_master_claude_creation_and_initialization() {
    let temp_dir = TempDir::new().unwrap();
    let repo_path = temp_dir.path().to_path_buf();

    // Initialize git repo
    ccswarm::git::WorktreeManager::init_if_needed(&repo_path)
        .await
        .unwrap();

    // Create MasterClaude instance
    let config = create_test_config();
    let master = MasterClaude::new(config.clone(), repo_path.clone())
        .await
        .unwrap();

    // Verify basic properties
    assert!(master.id.starts_with("master-claude-"));
    assert_eq!(master.config.project.name, "TestProject");

    // Check initial state
    let state = master.state.read().await;
    assert_eq!(
        state.status,
        ccswarm::orchestrator::OrchestratorStatus::Initializing
    );
    assert_eq!(state.total_tasks_processed, 0);
    assert_eq!(state.successful_tasks, 0);
    assert_eq!(state.failed_tasks, 0);
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_master_claude_with_ai_session_integration (lines 253-296) ---[0m
async fn test_master_claude_with_ai_session_integration() {
    let temp_dir = TempDir::new().unwrap();
    let repo_path = temp_dir.path().to_path_buf();

    // Initialize git repo
    ccswarm::git::WorktreeManager::init_if_needed(&repo_path)
        .await
        .unwrap();

    // Create MasterClaude with AI-session enabled
    let mut config = create_test_config();
    config.project.master_claude.enable_proactive_mode = false; // Disable for testing

    let master = MasterClaude::new(config, repo_path.clone()).await.unwrap();

    // Create a task that would use AI-session
    use ccswarm::agent::{Priority, Task, TaskType};
    let task = Task::new(
        "ai-session-task".to_string(),
        "Create API endpoint with AI-session support".to_string(),
        Priority::High,
        TaskType::Development,
    );

    // Add metadata indicating AI-session usage
    let mut enhanced_task = task.clone();
    enhanced_task.metadata = Some(serde_json::Map::new());
    if let Some(metadata) = enhanced_task.metadata.as_mut() {
        metadata.insert("ai_session_enabled".to_string(), json!(true));
        metadata.insert("context_compression".to_string(), json!("enabled"));
    }

    // Add task
    master.add_task(enhanced_task).await.unwrap();

    // Verify task has AI-session metadata
    let state = master.state.read().await;
    assert_eq!(state.pending_tasks.len(), 1);
    let pending_task = &state.pending_tasks[0];
    assert!(pending_task.metadata.is_some());

    let metadata = pending_task.metadata.as_ref().unwrap();
    assert_eq!(metadata.get("ai_session_enabled"), Some(&json!(true)));
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:97-129 function test_coordination_bus_message_flow <-> ./crates/ccswarm/tests/integration_masterclaude.rs:318-349 function test_task_result_creation
  Similarity: 85.37%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_coordination_bus_message_flow (lines 97-129) ---[0m
async fn test_coordination_bus_message_flow() {
    // Create coordination bus
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Send a task assignment message
    let task_msg = AgentMessage::TaskAssignment {
        task_id: "test-task-001".to_string(),
        agent_id: "frontend-agent".to_string(),
        task_data: json!({
            "description": "Create login component",
            "priority": "high"
        }),
    };

    bus.send_message(task_msg.clone()).await.unwrap();

    // Wait for message propagation
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Try to receive the message
    match bus.receive_message().await {
        Ok(received_msg) => match received_msg {
            AgentMessage::TaskAssignment {
                task_id, agent_id, ..
            } => {
                assert_eq!(task_id, "test-task-001");
                assert_eq!(agent_id, "frontend-agent");
            }
            _ => panic!("Unexpected message type received"),
        },
        Err(e) => panic!("Failed to receive message: {}", e),
    }
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_task_result_creation (lines 318-349) ---[0m
async fn test_task_result_creation() {
    use ccswarm::agent::TaskResult;

    // Test successful result
    let success_result = TaskResult::success(
        json!({
            "message": "Task completed successfully",
            "details": {
                "files_created": 5,
                "tests_passed": 10
            }
        }),
        Duration::from_secs(60),
    );

    assert!(success_result.success);
    assert!(success_result.error.is_none());
    assert_eq!(success_result.duration, Duration::from_secs(60));

    // Test failure result
    let failure_result = TaskResult::failure(
        "Failed to connect to database".to_string(),
        Duration::from_secs(30),
    );

    assert!(!failure_result.success);
    assert_eq!(
        failure_result.error,
        Some("Failed to connect to database".to_string())
    );
    assert_eq!(failure_result.duration, Duration::from_secs(30));
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:132-161 function test_master_claude_task_assignment <-> ./crates/ccswarm/tests/integration_masterclaude.rs:204-250 function test_quality_review_flow
  Similarity: 71.53%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_master_claude_task_assignment (lines 132-161) ---[0m
async fn test_master_claude_task_assignment() {
    let temp_dir = TempDir::new().unwrap();
    let repo_path = temp_dir.path().to_path_buf();

    // Initialize git repo
    ccswarm::git::WorktreeManager::init_if_needed(&repo_path)
        .await
        .unwrap();

    // Create and initialize MasterClaude
    let config = create_test_config();
    let master = MasterClaude::new(config, repo_path.clone()).await.unwrap();

    // Create a test task
    use ccswarm::agent::{Priority, Task, TaskType};
    let task = Task::new(
        "test-task-001".to_string(),
        "Create user authentication system".to_string(),
        Priority::High,
        TaskType::Feature,
    );

    // Add task to queue
    master.add_task(task.clone()).await.unwrap();

    // Verify task was added to pending tasks
    let state = master.state.read().await;
    assert_eq!(state.pending_tasks.len(), 1);
    assert_eq!(state.pending_tasks[0].id, "test-task-001");
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_quality_review_flow (lines 204-250) ---[0m
async fn test_quality_review_flow() {
    use ccswarm::agent::{Priority, Task, TaskResult, TaskType};

    // Create a task result
    let task = Task::new(
        "test-task-001".to_string(),
        "Implement user login".to_string(),
        Priority::High,
        TaskType::Feature,
    );

    let result = TaskResult {
        success: true,
        output: json!({
            "message": "Login component created successfully",
            "files_created": ["src/components/Login.tsx"],
            "files_modified": [],
            "files_deleted": []
        }),
        error: None,
        duration: Duration::from_secs(120),
    };

    // Create quality issue message
    let quality_msg = AgentMessage::QualityIssue {
        agent_id: "frontend-agent".to_string(),
        task_id: task.id.clone(),
        issues: vec![
            "Low test coverage".to_string(),
            "Missing documentation".to_string(),
        ],
    };

    // Verify message structure
    match quality_msg {
        AgentMessage::QualityIssue {
            agent_id,
            task_id,
            issues,
        } => {
            assert_eq!(agent_id, "frontend-agent");
            assert_eq!(task_id, "test-task-001");
            assert_eq!(issues.len(), 2);
        }
        _ => panic!("Unexpected message type"),
    }
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:66-94 function test_master_claude_creation_and_initialization <-> ./crates/ccswarm/tests/integration_masterclaude.rs:204-250 function test_quality_review_flow
  Similarity: 70.00%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_master_claude_creation_and_initialization (lines 66-94) ---[0m
async fn test_master_claude_creation_and_initialization() {
    let temp_dir = TempDir::new().unwrap();
    let repo_path = temp_dir.path().to_path_buf();

    // Initialize git repo
    ccswarm::git::WorktreeManager::init_if_needed(&repo_path)
        .await
        .unwrap();

    // Create MasterClaude instance
    let config = create_test_config();
    let master = MasterClaude::new(config.clone(), repo_path.clone())
        .await
        .unwrap();

    // Verify basic properties
    assert!(master.id.starts_with("master-claude-"));
    assert_eq!(master.config.project.name, "TestProject");

    // Check initial state
    let state = master.state.read().await;
    assert_eq!(
        state.status,
        ccswarm::orchestrator::OrchestratorStatus::Initializing
    );
    assert_eq!(state.total_tasks_processed, 0);
    assert_eq!(state.successful_tasks, 0);
    assert_eq!(state.failed_tasks, 0);
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_quality_review_flow (lines 204-250) ---[0m
async fn test_quality_review_flow() {
    use ccswarm::agent::{Priority, Task, TaskResult, TaskType};

    // Create a task result
    let task = Task::new(
        "test-task-001".to_string(),
        "Implement user login".to_string(),
        Priority::High,
        TaskType::Feature,
    );

    let result = TaskResult {
        success: true,
        output: json!({
            "message": "Login component created successfully",
            "files_created": ["src/components/Login.tsx"],
            "files_modified": [],
            "files_deleted": []
        }),
        error: None,
        duration: Duration::from_secs(120),
    };

    // Create quality issue message
    let quality_msg = AgentMessage::QualityIssue {
        agent_id: "frontend-agent".to_string(),
        task_id: task.id.clone(),
        issues: vec![
            "Low test coverage".to_string(),
            "Missing documentation".to_string(),
        ],
    };

    // Verify message structure
    match quality_msg {
        AgentMessage::QualityIssue {
            agent_id,
            task_id,
            issues,
        } => {
            assert_eq!(agent_id, "frontend-agent");
            assert_eq!(task_id, "test-task-001");
            assert_eq!(issues.len(), 2);
        }
        _ => panic!("Unexpected message type"),
    }
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:66-94 function test_master_claude_creation_and_initialization <-> ./crates/ccswarm/tests/integration_masterclaude.rs:132-161 function test_master_claude_task_assignment
  Similarity: 88.50%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_master_claude_creation_and_initialization (lines 66-94) ---[0m
async fn test_master_claude_creation_and_initialization() {
    let temp_dir = TempDir::new().unwrap();
    let repo_path = temp_dir.path().to_path_buf();

    // Initialize git repo
    ccswarm::git::WorktreeManager::init_if_needed(&repo_path)
        .await
        .unwrap();

    // Create MasterClaude instance
    let config = create_test_config();
    let master = MasterClaude::new(config.clone(), repo_path.clone())
        .await
        .unwrap();

    // Verify basic properties
    assert!(master.id.starts_with("master-claude-"));
    assert_eq!(master.config.project.name, "TestProject");

    // Check initial state
    let state = master.state.read().await;
    assert_eq!(
        state.status,
        ccswarm::orchestrator::OrchestratorStatus::Initializing
    );
    assert_eq!(state.total_tasks_processed, 0);
    assert_eq!(state.successful_tasks, 0);
    assert_eq!(state.failed_tasks, 0);
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_master_claude_task_assignment (lines 132-161) ---[0m
async fn test_master_claude_task_assignment() {
    let temp_dir = TempDir::new().unwrap();
    let repo_path = temp_dir.path().to_path_buf();

    // Initialize git repo
    ccswarm::git::WorktreeManager::init_if_needed(&repo_path)
        .await
        .unwrap();

    // Create and initialize MasterClaude
    let config = create_test_config();
    let master = MasterClaude::new(config, repo_path.clone()).await.unwrap();

    // Create a test task
    use ccswarm::agent::{Priority, Task, TaskType};
    let task = Task::new(
        "test-task-001".to_string(),
        "Create user authentication system".to_string(),
        Priority::High,
        TaskType::Feature,
    );

    // Add task to queue
    master.add_task(task.clone()).await.unwrap();

    // Verify task was added to pending tasks
    let state = master.state.read().await;
    assert_eq!(state.pending_tasks.len(), 1);
    assert_eq!(state.pending_tasks[0].id, "test-task-001");
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:132-161 function test_master_claude_task_assignment <-> ./crates/ccswarm/tests/integration_masterclaude.rs:164-201 function test_agent_message_handling
  Similarity: 75.96%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_master_claude_task_assignment (lines 132-161) ---[0m
async fn test_master_claude_task_assignment() {
    let temp_dir = TempDir::new().unwrap();
    let repo_path = temp_dir.path().to_path_buf();

    // Initialize git repo
    ccswarm::git::WorktreeManager::init_if_needed(&repo_path)
        .await
        .unwrap();

    // Create and initialize MasterClaude
    let config = create_test_config();
    let master = MasterClaude::new(config, repo_path.clone()).await.unwrap();

    // Create a test task
    use ccswarm::agent::{Priority, Task, TaskType};
    let task = Task::new(
        "test-task-001".to_string(),
        "Create user authentication system".to_string(),
        Priority::High,
        TaskType::Feature,
    );

    // Add task to queue
    master.add_task(task.clone()).await.unwrap();

    // Verify task was added to pending tasks
    let state = master.state.read().await;
    assert_eq!(state.pending_tasks.len(), 1);
    assert_eq!(state.pending_tasks[0].id, "test-task-001");
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_agent_message_handling (lines 164-201) ---[0m
async fn test_agent_message_handling() {
    // Create coordination bus
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Test different message types
    let messages = vec![
        AgentMessage::StatusUpdate {
            agent_id: "test-agent".to_string(),
            status: ccswarm::agent::AgentStatus::Available,
            metrics: json!({"cpu": 45.2, "memory": 1024}),
        },
        AgentMessage::TaskProgress {
            agent_id: "test-agent".to_string(),
            task_id: "task-001".to_string(),
            progress: 0.75,
            message: "75% complete".to_string(),
        },
        AgentMessage::HelpRequest {
            agent_id: "test-agent".to_string(),
            context: "Need help with API integration".to_string(),
            priority: ccswarm::coordination::MessagePriority::High,
        },
    ];

    // Send all messages
    for msg in &messages {
        bus.send_message(msg.clone()).await.unwrap();
    }

    // Wait for propagation
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Verify we can receive messages
    for _ in 0..messages.len() {
        let result = bus.receive_message().await;
        assert!(result.is_ok(), "Failed to receive message");
    }
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:66-94 function test_master_claude_creation_and_initialization <-> ./crates/ccswarm/tests/integration_masterclaude.rs:164-201 function test_agent_message_handling
  Similarity: 75.06%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_master_claude_creation_and_initialization (lines 66-94) ---[0m
async fn test_master_claude_creation_and_initialization() {
    let temp_dir = TempDir::new().unwrap();
    let repo_path = temp_dir.path().to_path_buf();

    // Initialize git repo
    ccswarm::git::WorktreeManager::init_if_needed(&repo_path)
        .await
        .unwrap();

    // Create MasterClaude instance
    let config = create_test_config();
    let master = MasterClaude::new(config.clone(), repo_path.clone())
        .await
        .unwrap();

    // Verify basic properties
    assert!(master.id.starts_with("master-claude-"));
    assert_eq!(master.config.project.name, "TestProject");

    // Check initial state
    let state = master.state.read().await;
    assert_eq!(
        state.status,
        ccswarm::orchestrator::OrchestratorStatus::Initializing
    );
    assert_eq!(state.total_tasks_processed, 0);
    assert_eq!(state.successful_tasks, 0);
    assert_eq!(state.failed_tasks, 0);
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_agent_message_handling (lines 164-201) ---[0m
async fn test_agent_message_handling() {
    // Create coordination bus
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Test different message types
    let messages = vec![
        AgentMessage::StatusUpdate {
            agent_id: "test-agent".to_string(),
            status: ccswarm::agent::AgentStatus::Available,
            metrics: json!({"cpu": 45.2, "memory": 1024}),
        },
        AgentMessage::TaskProgress {
            agent_id: "test-agent".to_string(),
            task_id: "task-001".to_string(),
            progress: 0.75,
            message: "75% complete".to_string(),
        },
        AgentMessage::HelpRequest {
            agent_id: "test-agent".to_string(),
            context: "Need help with API integration".to_string(),
            priority: ccswarm::coordination::MessagePriority::High,
        },
    ];

    // Send all messages
    for msg in &messages {
        bus.send_message(msg.clone()).await.unwrap();
    }

    // Wait for propagation
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Verify we can receive messages
    for _ in 0..messages.len() {
        let result = bus.receive_message().await;
        assert!(result.is_ok(), "Failed to receive message");
    }
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:132-161 function test_master_claude_task_assignment <-> ./crates/ccswarm/tests/integration_masterclaude.rs:318-349 function test_task_result_creation
  Similarity: 79.20%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_master_claude_task_assignment (lines 132-161) ---[0m
async fn test_master_claude_task_assignment() {
    let temp_dir = TempDir::new().unwrap();
    let repo_path = temp_dir.path().to_path_buf();

    // Initialize git repo
    ccswarm::git::WorktreeManager::init_if_needed(&repo_path)
        .await
        .unwrap();

    // Create and initialize MasterClaude
    let config = create_test_config();
    let master = MasterClaude::new(config, repo_path.clone()).await.unwrap();

    // Create a test task
    use ccswarm::agent::{Priority, Task, TaskType};
    let task = Task::new(
        "test-task-001".to_string(),
        "Create user authentication system".to_string(),
        Priority::High,
        TaskType::Feature,
    );

    // Add task to queue
    master.add_task(task.clone()).await.unwrap();

    // Verify task was added to pending tasks
    let state = master.state.read().await;
    assert_eq!(state.pending_tasks.len(), 1);
    assert_eq!(state.pending_tasks[0].id, "test-task-001");
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_task_result_creation (lines 318-349) ---[0m
async fn test_task_result_creation() {
    use ccswarm::agent::TaskResult;

    // Test successful result
    let success_result = TaskResult::success(
        json!({
            "message": "Task completed successfully",
            "details": {
                "files_created": 5,
                "tests_passed": 10
            }
        }),
        Duration::from_secs(60),
    );

    assert!(success_result.success);
    assert!(success_result.error.is_none());
    assert_eq!(success_result.duration, Duration::from_secs(60));

    // Test failure result
    let failure_result = TaskResult::failure(
        "Failed to connect to database".to_string(),
        Duration::from_secs(30),
    );

    assert!(!failure_result.success);
    assert_eq!(
        failure_result.error,
        Some("Failed to connect to database".to_string())
    );
    assert_eq!(failure_result.duration, Duration::from_secs(30));
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:66-94 function test_master_claude_creation_and_initialization <-> ./crates/ccswarm/tests/integration_masterclaude.rs:318-349 function test_task_result_creation
  Similarity: 75.88%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_master_claude_creation_and_initialization (lines 66-94) ---[0m
async fn test_master_claude_creation_and_initialization() {
    let temp_dir = TempDir::new().unwrap();
    let repo_path = temp_dir.path().to_path_buf();

    // Initialize git repo
    ccswarm::git::WorktreeManager::init_if_needed(&repo_path)
        .await
        .unwrap();

    // Create MasterClaude instance
    let config = create_test_config();
    let master = MasterClaude::new(config.clone(), repo_path.clone())
        .await
        .unwrap();

    // Verify basic properties
    assert!(master.id.starts_with("master-claude-"));
    assert_eq!(master.config.project.name, "TestProject");

    // Check initial state
    let state = master.state.read().await;
    assert_eq!(
        state.status,
        ccswarm::orchestrator::OrchestratorStatus::Initializing
    );
    assert_eq!(state.total_tasks_processed, 0);
    assert_eq!(state.successful_tasks, 0);
    assert_eq!(state.failed_tasks, 0);
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_task_result_creation (lines 318-349) ---[0m
async fn test_task_result_creation() {
    use ccswarm::agent::TaskResult;

    // Test successful result
    let success_result = TaskResult::success(
        json!({
            "message": "Task completed successfully",
            "details": {
                "files_created": 5,
                "tests_passed": 10
            }
        }),
        Duration::from_secs(60),
    );

    assert!(success_result.success);
    assert!(success_result.error.is_none());
    assert_eq!(success_result.duration, Duration::from_secs(60));

    // Test failure result
    let failure_result = TaskResult::failure(
        "Failed to connect to database".to_string(),
        Duration::from_secs(30),
    );

    assert!(!failure_result.success);
    assert_eq!(
        failure_result.error,
        Some("Failed to connect to database".to_string())
    );
    assert_eq!(failure_result.duration, Duration::from_secs(30));
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:97-129 function test_coordination_bus_message_flow <-> ./crates/ccswarm/tests/integration_masterclaude.rs:132-161 function test_master_claude_task_assignment
  Similarity: 72.98%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_coordination_bus_message_flow (lines 97-129) ---[0m
async fn test_coordination_bus_message_flow() {
    // Create coordination bus
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Send a task assignment message
    let task_msg = AgentMessage::TaskAssignment {
        task_id: "test-task-001".to_string(),
        agent_id: "frontend-agent".to_string(),
        task_data: json!({
            "description": "Create login component",
            "priority": "high"
        }),
    };

    bus.send_message(task_msg.clone()).await.unwrap();

    // Wait for message propagation
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Try to receive the message
    match bus.receive_message().await {
        Ok(received_msg) => match received_msg {
            AgentMessage::TaskAssignment {
                task_id, agent_id, ..
            } => {
                assert_eq!(task_id, "test-task-001");
                assert_eq!(agent_id, "frontend-agent");
            }
            _ => panic!("Unexpected message type received"),
        },
        Err(e) => panic!("Failed to receive message: {}", e),
    }
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_master_claude_task_assignment (lines 132-161) ---[0m
async fn test_master_claude_task_assignment() {
    let temp_dir = TempDir::new().unwrap();
    let repo_path = temp_dir.path().to_path_buf();

    // Initialize git repo
    ccswarm::git::WorktreeManager::init_if_needed(&repo_path)
        .await
        .unwrap();

    // Create and initialize MasterClaude
    let config = create_test_config();
    let master = MasterClaude::new(config, repo_path.clone()).await.unwrap();

    // Create a test task
    use ccswarm::agent::{Priority, Task, TaskType};
    let task = Task::new(
        "test-task-001".to_string(),
        "Create user authentication system".to_string(),
        Priority::High,
        TaskType::Feature,
    );

    // Add task to queue
    master.add_task(task.clone()).await.unwrap();

    // Verify task was added to pending tasks
    let state = master.state.read().await;
    assert_eq!(state.pending_tasks.len(), 1);
    assert_eq!(state.pending_tasks[0].id, "test-task-001");
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:66-94 function test_master_claude_creation_and_initialization <-> ./crates/ccswarm/tests/integration_masterclaude.rs:97-129 function test_coordination_bus_message_flow
  Similarity: 72.07%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_master_claude_creation_and_initialization (lines 66-94) ---[0m
async fn test_master_claude_creation_and_initialization() {
    let temp_dir = TempDir::new().unwrap();
    let repo_path = temp_dir.path().to_path_buf();

    // Initialize git repo
    ccswarm::git::WorktreeManager::init_if_needed(&repo_path)
        .await
        .unwrap();

    // Create MasterClaude instance
    let config = create_test_config();
    let master = MasterClaude::new(config.clone(), repo_path.clone())
        .await
        .unwrap();

    // Verify basic properties
    assert!(master.id.starts_with("master-claude-"));
    assert_eq!(master.config.project.name, "TestProject");

    // Check initial state
    let state = master.state.read().await;
    assert_eq!(
        state.status,
        ccswarm::orchestrator::OrchestratorStatus::Initializing
    );
    assert_eq!(state.total_tasks_processed, 0);
    assert_eq!(state.successful_tasks, 0);
    assert_eq!(state.failed_tasks, 0);
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_coordination_bus_message_flow (lines 97-129) ---[0m
async fn test_coordination_bus_message_flow() {
    // Create coordination bus
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Send a task assignment message
    let task_msg = AgentMessage::TaskAssignment {
        task_id: "test-task-001".to_string(),
        agent_id: "frontend-agent".to_string(),
        task_data: json!({
            "description": "Create login component",
            "priority": "high"
        }),
    };

    bus.send_message(task_msg.clone()).await.unwrap();

    // Wait for message propagation
    tokio::time::sleep(Duration::from_millis(100)).await;

    // Try to receive the message
    match bus.receive_message().await {
        Ok(received_msg) => match received_msg {
            AgentMessage::TaskAssignment {
                task_id, agent_id, ..
            } => {
                assert_eq!(task_id, "test-task-001");
                assert_eq!(agent_id, "frontend-agent");
            }
            _ => panic!("Unexpected message type received"),
        },
        Err(e) => panic!("Failed to receive message: {}", e),
    }
}

  ./crates/ccswarm/tests/integration_masterclaude.rs:299-315 function test_error_handling_and_recovery <-> ./crates/ccswarm/tests/integration_masterclaude.rs:318-349 function test_task_result_creation
  Similarity: 71.62%

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_error_handling_and_recovery (lines 299-315) ---[0m
async fn test_error_handling_and_recovery() {
    // Test coordination bus error handling
    let bus = Arc::new(CoordinationBus::new().await.unwrap());

    // Send message with empty agent_id (should still work)
    let invalid_msg = AgentMessage::StatusUpdate {
        agent_id: "".to_string(),
        status: ccswarm::agent::AgentStatus::Available,
        metrics: json!({}),
    };

    let send_result = bus.send_message(invalid_msg).await;
    assert!(
        send_result.is_ok(),
        "Should handle empty agent_id gracefully"
    );
}

[36m--- ./crates/ccswarm/tests/integration_masterclaude.rs:test_task_result_creation (lines 318-349) ---[0m
async fn test_task_result_creation() {
    use ccswarm::agent::TaskResult;

    // Test successful result
    let success_result = TaskResult::success(
        json!({
            "message": "Task completed successfully",
            "details": {
                "files_created": 5,
                "tests_passed": 10
            }
        }),
        Duration::from_secs(60),
    );

    assert!(success_result.success);
    assert!(success_result.error.is_none());
    assert_eq!(success_result.duration, Duration::from_secs(60));

    // Test failure result
    let failure_result = TaskResult::failure(
        "Failed to connect to database".to_string(),
        Duration::from_secs(30),
    );

    assert!(!failure_result.success);
    assert_eq!(
        failure_result.error,
        Some("Failed to connect to database".to_string())
    );
    assert_eq!(failure_result.duration, Duration::from_secs(30));
}


Duplicates in ./crates/ccswarm/src/session/mod.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/session/mod.rs:277-316 method create_session <-> ./crates/ccswarm/src/session/mod.rs:454-487 method terminate_session
  Similarity: 87.87%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:create_session (lines 277-316) ---[0m
    pub async fn create_session(
        &self,
        agent_id: String,
        agent_role: AgentRole,
        working_directory: String,
        description: Option<String>,
        auto_start: bool,
    ) -> SessionResult<AgentSession> {
        let session =
            AgentSession::new(agent_id, agent_role, working_directory.clone(), description);

        // Create the native session
        // TODO: Create native session when available

        // Set up the session environment
        self.setup_session_environment(&session).await?;

        if auto_start {
            // Start the agent in the session
            self.start_agent_in_session(&session).await?;
        }

        // Store the session
        {
            let mut sessions = self.sessions.lock().map_lock_error()?;
            sessions.insert(session.id.clone(), session.clone());
        }

        // Start resource monitoring if enabled
        if let Some(ref integration) = self.resource_integration {
            if let Err(e) = integration
                .on_session_created(&session.id, &session.agent_id, None)
                .await
            {
                tracing::warn!("Failed to start resource monitoring: {}", e);
            }
        }

        Ok(session)
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:terminate_session (lines 454-487) ---[0m
    pub async fn terminate_session(&self, session_id: &str) -> SessionResult<()> {
        let (_tmux_session, agent_id) = {
            let sessions = self.sessions.lock().map_lock_error()?;
            let session = sessions
                .get(session_id)
                .ok_or_else(|| SessionError::NotFound {
                    id: session_id.to_string(),
                })?;
            (session.tmux_session.clone(), session.agent_id.clone())
        };

        // Stop resource monitoring if enabled
        if let Some(ref integration) = self.resource_integration {
            if let Err(e) = integration
                .on_session_terminated(session_id, &agent_id)
                .await
            {
                tracing::warn!("Failed to stop resource monitoring: {}", e);
            }
        }

        // TODO: Terminate the native session when available

        // Update session status
        {
            let mut sessions = self.sessions.lock().map_lock_error()?;
            if let Some(session) = sessions.get_mut(session_id) {
                session.status = SessionStatus::Terminated;
                session.touch();
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:454-487 method terminate_session <-> ./crates/ccswarm/src/session/mod.rs:497-524 method set_background_mode
  Similarity: 91.34%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:terminate_session (lines 454-487) ---[0m
    pub async fn terminate_session(&self, session_id: &str) -> SessionResult<()> {
        let (_tmux_session, agent_id) = {
            let sessions = self.sessions.lock().map_lock_error()?;
            let session = sessions
                .get(session_id)
                .ok_or_else(|| SessionError::NotFound {
                    id: session_id.to_string(),
                })?;
            (session.tmux_session.clone(), session.agent_id.clone())
        };

        // Stop resource monitoring if enabled
        if let Some(ref integration) = self.resource_integration {
            if let Err(e) = integration
                .on_session_terminated(session_id, &agent_id)
                .await
            {
                tracing::warn!("Failed to stop resource monitoring: {}", e);
            }
        }

        // TODO: Terminate the native session when available

        // Update session status
        {
            let mut sessions = self.sessions.lock().map_lock_error()?;
            if let Some(session) = sessions.get_mut(session_id) {
                session.status = SessionStatus::Terminated;
                session.touch();
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:set_background_mode (lines 497-524) ---[0m
    pub async fn set_background_mode(
        &self,
        session_id: &str,
        auto_accept: bool,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.background_mode = true;
        if auto_accept && session.auto_accept_config.is_none() {
            // Enable auto-accept with default configuration if not already configured
            session.enable_auto_accept(AutoAcceptConfig::default());
        } else {
            session.auto_accept = auto_accept;
        }

        if session.status == SessionStatus::Active {
            session.status = SessionStatus::Background;
        }
        session.touch();

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:277-316 method create_session <-> ./crates/ccswarm/src/session/mod.rs:497-524 method set_background_mode
  Similarity: 81.61%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:create_session (lines 277-316) ---[0m
    pub async fn create_session(
        &self,
        agent_id: String,
        agent_role: AgentRole,
        working_directory: String,
        description: Option<String>,
        auto_start: bool,
    ) -> SessionResult<AgentSession> {
        let session =
            AgentSession::new(agent_id, agent_role, working_directory.clone(), description);

        // Create the native session
        // TODO: Create native session when available

        // Set up the session environment
        self.setup_session_environment(&session).await?;

        if auto_start {
            // Start the agent in the session
            self.start_agent_in_session(&session).await?;
        }

        // Store the session
        {
            let mut sessions = self.sessions.lock().map_lock_error()?;
            sessions.insert(session.id.clone(), session.clone());
        }

        // Start resource monitoring if enabled
        if let Some(ref integration) = self.resource_integration {
            if let Err(e) = integration
                .on_session_created(&session.id, &session.agent_id, None)
                .await
            {
                tracing::warn!("Failed to start resource monitoring: {}", e);
            }
        }

        Ok(session)
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:set_background_mode (lines 497-524) ---[0m
    pub async fn set_background_mode(
        &self,
        session_id: &str,
        auto_accept: bool,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.background_mode = true;
        if auto_accept && session.auto_accept_config.is_none() {
            // Enable auto-accept with default configuration if not already configured
            session.enable_auto_accept(AutoAcceptConfig::default());
        } else {
            session.auto_accept = auto_accept;
        }

        if session.status == SessionStatus::Active {
            session.status = SessionStatus::Background;
        }
        session.touch();

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:454-487 method terminate_session <-> ./crates/ccswarm/src/session/mod.rs:696-721 method check_and_suspend_idle_agents
  Similarity: 87.50%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:terminate_session (lines 454-487) ---[0m
    pub async fn terminate_session(&self, session_id: &str) -> SessionResult<()> {
        let (_tmux_session, agent_id) = {
            let sessions = self.sessions.lock().map_lock_error()?;
            let session = sessions
                .get(session_id)
                .ok_or_else(|| SessionError::NotFound {
                    id: session_id.to_string(),
                })?;
            (session.tmux_session.clone(), session.agent_id.clone())
        };

        // Stop resource monitoring if enabled
        if let Some(ref integration) = self.resource_integration {
            if let Err(e) = integration
                .on_session_terminated(session_id, &agent_id)
                .await
            {
                tracing::warn!("Failed to stop resource monitoring: {}", e);
            }
        }

        // TODO: Terminate the native session when available

        // Update session status
        {
            let mut sessions = self.sessions.lock().map_lock_error()?;
            if let Some(session) = sessions.get_mut(session_id) {
                session.status = SessionStatus::Terminated;
                session.touch();
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:check_and_suspend_idle_agents (lines 696-721) ---[0m
    pub async fn check_and_suspend_idle_agents(&self) -> SessionResult<Vec<String>> {
        let mut suspended_agents = Vec::new();

        if let Some(ref integration) = self.resource_integration {
            let agents_to_check: Vec<(String, String)> = {
                let sessions = self.sessions.lock().map_lock_error()?;
                sessions
                    .values()
                    .filter(|s| s.is_runnable())
                    .map(|s| (s.id.clone(), s.agent_id.clone()))
                    .collect()
            };

            for (session_id, agent_id) in agents_to_check {
                if integration.check_agent_suspension(&agent_id).await {
                    if let Err(e) = self.pause_session(&session_id).await {
                        tracing::warn!("Failed to suspend idle agent {}: {}", agent_id, e);
                    } else {
                        suspended_agents.push(agent_id);
                    }
                }
            }
        }

        Ok(suspended_agents)
    }

  ./crates/ccswarm/src/session/mod.rs:356-381 method resume_session <-> ./crates/ccswarm/src/session/mod.rs:420-445 method attach_session
  Similarity: 99.69%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:resume_session (lines 356-381) ---[0m
    pub async fn resume_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Paused => {
                // For native sessions, we just update the status
                session.status = if session.background_mode {
                    SessionStatus::Background
                } else {
                    SessionStatus::Active
                };
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "resume".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:attach_session (lines 420-445) ---[0m
    pub async fn attach_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Detached => {
                // For native sessions, attaching just changes status
                session.status = if session.background_mode {
                    SessionStatus::Background
                } else {
                    SessionStatus::Active
                };
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "attach".to_string(),
            }),
        }
    }

  ./crates/ccswarm/src/session/mod.rs:277-316 method create_session <-> ./crates/ccswarm/src/session/mod.rs:812-832 function test_session_is_runnable
  Similarity: 84.29%

[36m--- ./crates/ccswarm/src/session/mod.rs:create_session (lines 277-316) ---[0m
    pub async fn create_session(
        &self,
        agent_id: String,
        agent_role: AgentRole,
        working_directory: String,
        description: Option<String>,
        auto_start: bool,
    ) -> SessionResult<AgentSession> {
        let session =
            AgentSession::new(agent_id, agent_role, working_directory.clone(), description);

        // Create the native session
        // TODO: Create native session when available

        // Set up the session environment
        self.setup_session_environment(&session).await?;

        if auto_start {
            // Start the agent in the session
            self.start_agent_in_session(&session).await?;
        }

        // Store the session
        {
            let mut sessions = self.sessions.lock().map_lock_error()?;
            sessions.insert(session.id.clone(), session.clone());
        }

        // Start resource monitoring if enabled
        if let Some(ref integration) = self.resource_integration {
            if let Err(e) = integration
                .on_session_created(&session.id, &session.agent_id, None)
                .await
            {
                tracing::warn!("Failed to start resource monitoring: {}", e);
            }
        }

        Ok(session)
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:test_session_is_runnable (lines 812-832) ---[0m
    fn test_session_is_runnable() {
        use crate::identity::default_backend_role;

        let mut session = AgentSession::new(
            "agent-123".to_string(),
            default_backend_role(),
            "/tmp/test".to_string(),
            None,
        );

        assert!(session.is_runnable());

        session.status = SessionStatus::Paused;
        assert!(!session.is_runnable());

        session.status = SessionStatus::Background;
        assert!(session.is_runnable());

        session.status = SessionStatus::Terminated;
        assert!(!session.is_runnable());
    }

  ./crates/ccswarm/src/session/mod.rs:356-381 method resume_session <-> ./crates/ccswarm/src/session/mod.rs:454-487 method terminate_session
  Similarity: 85.34%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:resume_session (lines 356-381) ---[0m
    pub async fn resume_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Paused => {
                // For native sessions, we just update the status
                session.status = if session.background_mode {
                    SessionStatus::Background
                } else {
                    SessionStatus::Active
                };
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "resume".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:terminate_session (lines 454-487) ---[0m
    pub async fn terminate_session(&self, session_id: &str) -> SessionResult<()> {
        let (_tmux_session, agent_id) = {
            let sessions = self.sessions.lock().map_lock_error()?;
            let session = sessions
                .get(session_id)
                .ok_or_else(|| SessionError::NotFound {
                    id: session_id.to_string(),
                })?;
            (session.tmux_session.clone(), session.agent_id.clone())
        };

        // Stop resource monitoring if enabled
        if let Some(ref integration) = self.resource_integration {
            if let Err(e) = integration
                .on_session_terminated(session_id, &agent_id)
                .await
            {
                tracing::warn!("Failed to stop resource monitoring: {}", e);
            }
        }

        // TODO: Terminate the native session when available

        // Update session status
        {
            let mut sessions = self.sessions.lock().map_lock_error()?;
            if let Some(session) = sessions.get_mut(session_id) {
                session.status = SessionStatus::Terminated;
                session.touch();
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:420-445 method attach_session <-> ./crates/ccswarm/src/session/mod.rs:454-487 method terminate_session
  Similarity: 85.34%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:attach_session (lines 420-445) ---[0m
    pub async fn attach_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Detached => {
                // For native sessions, attaching just changes status
                session.status = if session.background_mode {
                    SessionStatus::Background
                } else {
                    SessionStatus::Active
                };
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "attach".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:terminate_session (lines 454-487) ---[0m
    pub async fn terminate_session(&self, session_id: &str) -> SessionResult<()> {
        let (_tmux_session, agent_id) = {
            let sessions = self.sessions.lock().map_lock_error()?;
            let session = sessions
                .get(session_id)
                .ok_or_else(|| SessionError::NotFound {
                    id: session_id.to_string(),
                })?;
            (session.tmux_session.clone(), session.agent_id.clone())
        };

        // Stop resource monitoring if enabled
        if let Some(ref integration) = self.resource_integration {
            if let Err(e) = integration
                .on_session_terminated(session_id, &agent_id)
                .await
            {
                tracing::warn!("Failed to stop resource monitoring: {}", e);
            }
        }

        // TODO: Terminate the native session when available

        // Update session status
        {
            let mut sessions = self.sessions.lock().map_lock_error()?;
            if let Some(session) = sessions.get_mut(session_id) {
                session.status = SessionStatus::Terminated;
                session.touch();
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:97-129 method new <-> ./crates/ccswarm/src/session/mod.rs:454-487 method terminate_session
  Similarity: 73.36%
  Classes: AgentSession <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:new (lines 97-129) ---[0m
    pub fn new(
        agent_id: String,
        agent_role: AgentRole,
        working_directory: String,
        description: Option<String>,
    ) -> Self {
        let session_id = Uuid::new_v4().to_string();
        let tmux_session = format!(
            "ccswarm-{}-{}",
            agent_role.name().to_lowercase(),
            &session_id[..8]
        );

        let memory = SessionMemory::new(session_id.clone(), agent_id.clone());

        Self {
            id: session_id,
            agent_id: agent_id.clone(),
            agent_role,
            tmux_session,
            status: SessionStatus::Active,
            background_mode: false,
            auto_accept: false,
            auto_accept_config: None,
            created_at: Utc::now(),
            last_activity: Utc::now(),
            description,
            working_directory,
            tasks_processed: 0,
            tasks_queued: 0,
            memory,
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:terminate_session (lines 454-487) ---[0m
    pub async fn terminate_session(&self, session_id: &str) -> SessionResult<()> {
        let (_tmux_session, agent_id) = {
            let sessions = self.sessions.lock().map_lock_error()?;
            let session = sessions
                .get(session_id)
                .ok_or_else(|| SessionError::NotFound {
                    id: session_id.to_string(),
                })?;
            (session.tmux_session.clone(), session.agent_id.clone())
        };

        // Stop resource monitoring if enabled
        if let Some(ref integration) = self.resource_integration {
            if let Err(e) = integration
                .on_session_terminated(session_id, &agent_id)
                .await
            {
                tracing::warn!("Failed to stop resource monitoring: {}", e);
            }
        }

        // TODO: Terminate the native session when available

        // Update session status
        {
            let mut sessions = self.sessions.lock().map_lock_error()?;
            if let Some(session) = sessions.get_mut(session_id) {
                session.status = SessionStatus::Terminated;
                session.touch();
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:325-347 method pause_session <-> ./crates/ccswarm/src/session/mod.rs:454-487 method terminate_session
  Similarity: 85.34%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:pause_session (lines 325-347) ---[0m
    pub async fn pause_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, we just update the status
                // The actual pause/resume will be handled by the session itself
                session.status = SessionStatus::Paused;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "pause".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:terminate_session (lines 454-487) ---[0m
    pub async fn terminate_session(&self, session_id: &str) -> SessionResult<()> {
        let (_tmux_session, agent_id) = {
            let sessions = self.sessions.lock().map_lock_error()?;
            let session = sessions
                .get(session_id)
                .ok_or_else(|| SessionError::NotFound {
                    id: session_id.to_string(),
                })?;
            (session.tmux_session.clone(), session.agent_id.clone())
        };

        // Stop resource monitoring if enabled
        if let Some(ref integration) = self.resource_integration {
            if let Err(e) = integration
                .on_session_terminated(session_id, &agent_id)
                .await
            {
                tracing::warn!("Failed to stop resource monitoring: {}", e);
            }
        }

        // TODO: Terminate the native session when available

        // Update session status
        {
            let mut sessions = self.sessions.lock().map_lock_error()?;
            if let Some(session) = sessions.get_mut(session_id) {
                session.status = SessionStatus::Terminated;
                session.touch();
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:97-129 method new <-> ./crates/ccswarm/src/session/mod.rs:696-721 method check_and_suspend_idle_agents
  Similarity: 81.13%
  Classes: AgentSession <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:new (lines 97-129) ---[0m
    pub fn new(
        agent_id: String,
        agent_role: AgentRole,
        working_directory: String,
        description: Option<String>,
    ) -> Self {
        let session_id = Uuid::new_v4().to_string();
        let tmux_session = format!(
            "ccswarm-{}-{}",
            agent_role.name().to_lowercase(),
            &session_id[..8]
        );

        let memory = SessionMemory::new(session_id.clone(), agent_id.clone());

        Self {
            id: session_id,
            agent_id: agent_id.clone(),
            agent_role,
            tmux_session,
            status: SessionStatus::Active,
            background_mode: false,
            auto_accept: false,
            auto_accept_config: None,
            created_at: Utc::now(),
            last_activity: Utc::now(),
            description,
            working_directory,
            tasks_processed: 0,
            tasks_queued: 0,
            memory,
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:check_and_suspend_idle_agents (lines 696-721) ---[0m
    pub async fn check_and_suspend_idle_agents(&self) -> SessionResult<Vec<String>> {
        let mut suspended_agents = Vec::new();

        if let Some(ref integration) = self.resource_integration {
            let agents_to_check: Vec<(String, String)> = {
                let sessions = self.sessions.lock().map_lock_error()?;
                sessions
                    .values()
                    .filter(|s| s.is_runnable())
                    .map(|s| (s.id.clone(), s.agent_id.clone()))
                    .collect()
            };

            for (session_id, agent_id) in agents_to_check {
                if integration.check_agent_suspension(&agent_id).await {
                    if let Err(e) = self.pause_session(&session_id).await {
                        tracing::warn!("Failed to suspend idle agent {}: {}", agent_id, e);
                    } else {
                        suspended_agents.push(agent_id);
                    }
                }
            }
        }

        Ok(suspended_agents)
    }

  ./crates/ccswarm/src/session/mod.rs:390-411 method detach_session <-> ./crates/ccswarm/src/session/mod.rs:454-487 method terminate_session
  Similarity: 85.34%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:detach_session (lines 390-411) ---[0m
    pub async fn detach_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, detaching just changes status
                session.status = SessionStatus::Detached;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "detach".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:terminate_session (lines 454-487) ---[0m
    pub async fn terminate_session(&self, session_id: &str) -> SessionResult<()> {
        let (_tmux_session, agent_id) = {
            let sessions = self.sessions.lock().map_lock_error()?;
            let session = sessions
                .get(session_id)
                .ok_or_else(|| SessionError::NotFound {
                    id: session_id.to_string(),
                })?;
            (session.tmux_session.clone(), session.agent_id.clone())
        };

        // Stop resource monitoring if enabled
        if let Some(ref integration) = self.resource_integration {
            if let Err(e) = integration
                .on_session_terminated(session_id, &agent_id)
                .await
            {
                tracing::warn!("Failed to stop resource monitoring: {}", e);
            }
        }

        // TODO: Terminate the native session when available

        // Update session status
        {
            let mut sessions = self.sessions.lock().map_lock_error()?;
            if let Some(session) = sessions.get_mut(session_id) {
                session.status = SessionStatus::Terminated;
                session.touch();
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:356-381 method resume_session <-> ./crates/ccswarm/src/session/mod.rs:696-721 method check_and_suspend_idle_agents
  Similarity: 91.17%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:resume_session (lines 356-381) ---[0m
    pub async fn resume_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Paused => {
                // For native sessions, we just update the status
                session.status = if session.background_mode {
                    SessionStatus::Background
                } else {
                    SessionStatus::Active
                };
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "resume".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:check_and_suspend_idle_agents (lines 696-721) ---[0m
    pub async fn check_and_suspend_idle_agents(&self) -> SessionResult<Vec<String>> {
        let mut suspended_agents = Vec::new();

        if let Some(ref integration) = self.resource_integration {
            let agents_to_check: Vec<(String, String)> = {
                let sessions = self.sessions.lock().map_lock_error()?;
                sessions
                    .values()
                    .filter(|s| s.is_runnable())
                    .map(|s| (s.id.clone(), s.agent_id.clone()))
                    .collect()
            };

            for (session_id, agent_id) in agents_to_check {
                if integration.check_agent_suspension(&agent_id).await {
                    if let Err(e) = self.pause_session(&session_id).await {
                        tracing::warn!("Failed to suspend idle agent {}: {}", agent_id, e);
                    } else {
                        suspended_agents.push(agent_id);
                    }
                }
            }
        }

        Ok(suspended_agents)
    }

  ./crates/ccswarm/src/session/mod.rs:420-445 method attach_session <-> ./crates/ccswarm/src/session/mod.rs:696-721 method check_and_suspend_idle_agents
  Similarity: 91.17%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:attach_session (lines 420-445) ---[0m
    pub async fn attach_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Detached => {
                // For native sessions, attaching just changes status
                session.status = if session.background_mode {
                    SessionStatus::Background
                } else {
                    SessionStatus::Active
                };
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "attach".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:check_and_suspend_idle_agents (lines 696-721) ---[0m
    pub async fn check_and_suspend_idle_agents(&self) -> SessionResult<Vec<String>> {
        let mut suspended_agents = Vec::new();

        if let Some(ref integration) = self.resource_integration {
            let agents_to_check: Vec<(String, String)> = {
                let sessions = self.sessions.lock().map_lock_error()?;
                sessions
                    .values()
                    .filter(|s| s.is_runnable())
                    .map(|s| (s.id.clone(), s.agent_id.clone()))
                    .collect()
            };

            for (session_id, agent_id) in agents_to_check {
                if integration.check_agent_suspension(&agent_id).await {
                    if let Err(e) = self.pause_session(&session_id).await {
                        tracing::warn!("Failed to suspend idle agent {}: {}", agent_id, e);
                    } else {
                        suspended_agents.push(agent_id);
                    }
                }
            }
        }

        Ok(suspended_agents)
    }

  ./crates/ccswarm/src/session/mod.rs:246-264 method with_resource_monitoring <-> ./crates/ccswarm/src/session/mod.rs:277-316 method create_session
  Similarity: 80.22%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:with_resource_monitoring (lines 246-264) ---[0m
    pub async fn with_resource_monitoring(
        resource_limits: crate::resource::ResourceLimits,
    ) -> SessionResult<Self> {
        let resource_monitor = Arc::new(ResourceMonitor::new(resource_limits));
        let resource_integration =
            Arc::new(SessionResourceIntegration::new(resource_monitor.clone()));

        // Start the monitoring loop
        let monitor_clone = resource_monitor.clone();
        tokio::spawn(async move {
            monitor_clone.start_monitoring_loop().await;
        });

        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: Some(resource_monitor),
            resource_integration: Some(resource_integration),
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:create_session (lines 277-316) ---[0m
    pub async fn create_session(
        &self,
        agent_id: String,
        agent_role: AgentRole,
        working_directory: String,
        description: Option<String>,
        auto_start: bool,
    ) -> SessionResult<AgentSession> {
        let session =
            AgentSession::new(agent_id, agent_role, working_directory.clone(), description);

        // Create the native session
        // TODO: Create native session when available

        // Set up the session environment
        self.setup_session_environment(&session).await?;

        if auto_start {
            // Start the agent in the session
            self.start_agent_in_session(&session).await?;
        }

        // Store the session
        {
            let mut sessions = self.sessions.lock().map_lock_error()?;
            sessions.insert(session.id.clone(), session.clone());
        }

        // Start resource monitoring if enabled
        if let Some(ref integration) = self.resource_integration {
            if let Err(e) = integration
                .on_session_created(&session.id, &session.agent_id, None)
                .await
            {
                tracing::warn!("Failed to start resource monitoring: {}", e);
            }
        }

        Ok(session)
    }

  ./crates/ccswarm/src/session/mod.rs:325-347 method pause_session <-> ./crates/ccswarm/src/session/mod.rs:356-381 method resume_session
  Similarity: 96.42%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:pause_session (lines 325-347) ---[0m
    pub async fn pause_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, we just update the status
                // The actual pause/resume will be handled by the session itself
                session.status = SessionStatus::Paused;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "pause".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:resume_session (lines 356-381) ---[0m
    pub async fn resume_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Paused => {
                // For native sessions, we just update the status
                session.status = if session.background_mode {
                    SessionStatus::Background
                } else {
                    SessionStatus::Active
                };
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "resume".to_string(),
            }),
        }
    }

  ./crates/ccswarm/src/session/mod.rs:325-347 method pause_session <-> ./crates/ccswarm/src/session/mod.rs:420-445 method attach_session
  Similarity: 96.42%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:pause_session (lines 325-347) ---[0m
    pub async fn pause_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, we just update the status
                // The actual pause/resume will be handled by the session itself
                session.status = SessionStatus::Paused;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "pause".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:attach_session (lines 420-445) ---[0m
    pub async fn attach_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Detached => {
                // For native sessions, attaching just changes status
                session.status = if session.background_mode {
                    SessionStatus::Background
                } else {
                    SessionStatus::Active
                };
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "attach".to_string(),
            }),
        }
    }

  ./crates/ccswarm/src/session/mod.rs:277-316 method create_session <-> ./crates/ccswarm/src/session/mod.rs:792-809 function test_agent_session_creation
  Similarity: 81.03%

[36m--- ./crates/ccswarm/src/session/mod.rs:create_session (lines 277-316) ---[0m
    pub async fn create_session(
        &self,
        agent_id: String,
        agent_role: AgentRole,
        working_directory: String,
        description: Option<String>,
        auto_start: bool,
    ) -> SessionResult<AgentSession> {
        let session =
            AgentSession::new(agent_id, agent_role, working_directory.clone(), description);

        // Create the native session
        // TODO: Create native session when available

        // Set up the session environment
        self.setup_session_environment(&session).await?;

        if auto_start {
            // Start the agent in the session
            self.start_agent_in_session(&session).await?;
        }

        // Store the session
        {
            let mut sessions = self.sessions.lock().map_lock_error()?;
            sessions.insert(session.id.clone(), session.clone());
        }

        // Start resource monitoring if enabled
        if let Some(ref integration) = self.resource_integration {
            if let Err(e) = integration
                .on_session_created(&session.id, &session.agent_id, None)
                .await
            {
                tracing::warn!("Failed to start resource monitoring: {}", e);
            }
        }

        Ok(session)
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:test_agent_session_creation (lines 792-809) ---[0m
    fn test_agent_session_creation() {
        use crate::identity::default_frontend_role;

        let session = AgentSession::new(
            "agent-123".to_string(),
            default_frontend_role(),
            "/tmp/test".to_string(),
            Some("Test session".to_string()),
        );

        assert_eq!(session.agent_id, "agent-123");
        assert_eq!(session.agent_role.name(), "Frontend");
        assert_eq!(session.status, SessionStatus::Active);
        assert!(!session.background_mode);
        assert!(!session.auto_accept);
        assert!(session.auto_accept_config.is_none());
        assert_eq!(session.tasks_processed, 0);
    }

  ./crates/ccswarm/src/session/mod.rs:246-264 method with_resource_monitoring <-> ./crates/ccswarm/src/session/mod.rs:454-487 method terminate_session
  Similarity: 88.62%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:with_resource_monitoring (lines 246-264) ---[0m
    pub async fn with_resource_monitoring(
        resource_limits: crate::resource::ResourceLimits,
    ) -> SessionResult<Self> {
        let resource_monitor = Arc::new(ResourceMonitor::new(resource_limits));
        let resource_integration =
            Arc::new(SessionResourceIntegration::new(resource_monitor.clone()));

        // Start the monitoring loop
        let monitor_clone = resource_monitor.clone();
        tokio::spawn(async move {
            monitor_clone.start_monitoring_loop().await;
        });

        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: Some(resource_monitor),
            resource_integration: Some(resource_integration),
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:terminate_session (lines 454-487) ---[0m
    pub async fn terminate_session(&self, session_id: &str) -> SessionResult<()> {
        let (_tmux_session, agent_id) = {
            let sessions = self.sessions.lock().map_lock_error()?;
            let session = sessions
                .get(session_id)
                .ok_or_else(|| SessionError::NotFound {
                    id: session_id.to_string(),
                })?;
            (session.tmux_session.clone(), session.agent_id.clone())
        };

        // Stop resource monitoring if enabled
        if let Some(ref integration) = self.resource_integration {
            if let Err(e) = integration
                .on_session_terminated(session_id, &agent_id)
                .await
            {
                tracing::warn!("Failed to stop resource monitoring: {}", e);
            }
        }

        // TODO: Terminate the native session when available

        // Update session status
        {
            let mut sessions = self.sessions.lock().map_lock_error()?;
            if let Some(session) = sessions.get_mut(session_id) {
                session.status = SessionStatus::Terminated;
                session.touch();
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:356-381 method resume_session <-> ./crates/ccswarm/src/session/mod.rs:390-411 method detach_session
  Similarity: 96.42%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:resume_session (lines 356-381) ---[0m
    pub async fn resume_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Paused => {
                // For native sessions, we just update the status
                session.status = if session.background_mode {
                    SessionStatus::Background
                } else {
                    SessionStatus::Active
                };
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "resume".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:detach_session (lines 390-411) ---[0m
    pub async fn detach_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, detaching just changes status
                session.status = SessionStatus::Detached;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "detach".to_string(),
            }),
        }
    }

  ./crates/ccswarm/src/session/mod.rs:390-411 method detach_session <-> ./crates/ccswarm/src/session/mod.rs:420-445 method attach_session
  Similarity: 96.42%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:detach_session (lines 390-411) ---[0m
    pub async fn detach_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, detaching just changes status
                session.status = SessionStatus::Detached;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "detach".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:attach_session (lines 420-445) ---[0m
    pub async fn attach_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Detached => {
                // For native sessions, attaching just changes status
                session.status = if session.background_mode {
                    SessionStatus::Background
                } else {
                    SessionStatus::Active
                };
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "attach".to_string(),
            }),
        }
    }

  ./crates/ccswarm/src/session/mod.rs:454-487 method terminate_session <-> ./crates/ccswarm/src/session/mod.rs:812-832 function test_session_is_runnable
  Similarity: 83.17%

[36m--- ./crates/ccswarm/src/session/mod.rs:terminate_session (lines 454-487) ---[0m
    pub async fn terminate_session(&self, session_id: &str) -> SessionResult<()> {
        let (_tmux_session, agent_id) = {
            let sessions = self.sessions.lock().map_lock_error()?;
            let session = sessions
                .get(session_id)
                .ok_or_else(|| SessionError::NotFound {
                    id: session_id.to_string(),
                })?;
            (session.tmux_session.clone(), session.agent_id.clone())
        };

        // Stop resource monitoring if enabled
        if let Some(ref integration) = self.resource_integration {
            if let Err(e) = integration
                .on_session_terminated(session_id, &agent_id)
                .await
            {
                tracing::warn!("Failed to stop resource monitoring: {}", e);
            }
        }

        // TODO: Terminate the native session when available

        // Update session status
        {
            let mut sessions = self.sessions.lock().map_lock_error()?;
            if let Some(session) = sessions.get_mut(session_id) {
                session.status = SessionStatus::Terminated;
                session.touch();
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:test_session_is_runnable (lines 812-832) ---[0m
    fn test_session_is_runnable() {
        use crate::identity::default_backend_role;

        let mut session = AgentSession::new(
            "agent-123".to_string(),
            default_backend_role(),
            "/tmp/test".to_string(),
            None,
        );

        assert!(session.is_runnable());

        session.status = SessionStatus::Paused;
        assert!(!session.is_runnable());

        session.status = SessionStatus::Background;
        assert!(session.is_runnable());

        session.status = SessionStatus::Terminated;
        assert!(!session.is_runnable());
    }

  ./crates/ccswarm/src/session/mod.rs:325-347 method pause_session <-> ./crates/ccswarm/src/session/mod.rs:390-411 method detach_session
  Similarity: 99.67%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:pause_session (lines 325-347) ---[0m
    pub async fn pause_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, we just update the status
                // The actual pause/resume will be handled by the session itself
                session.status = SessionStatus::Paused;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "pause".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:detach_session (lines 390-411) ---[0m
    pub async fn detach_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, detaching just changes status
                session.status = SessionStatus::Detached;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "detach".to_string(),
            }),
        }
    }

  ./crates/ccswarm/src/session/mod.rs:97-129 method new <-> ./crates/ccswarm/src/session/mod.rs:534-550 method enable_auto_accept
  Similarity: 89.51%
  Classes: AgentSession <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:new (lines 97-129) ---[0m
    pub fn new(
        agent_id: String,
        agent_role: AgentRole,
        working_directory: String,
        description: Option<String>,
    ) -> Self {
        let session_id = Uuid::new_v4().to_string();
        let tmux_session = format!(
            "ccswarm-{}-{}",
            agent_role.name().to_lowercase(),
            &session_id[..8]
        );

        let memory = SessionMemory::new(session_id.clone(), agent_id.clone());

        Self {
            id: session_id,
            agent_id: agent_id.clone(),
            agent_role,
            tmux_session,
            status: SessionStatus::Active,
            background_mode: false,
            auto_accept: false,
            auto_accept_config: None,
            created_at: Utc::now(),
            last_activity: Utc::now(),
            description,
            working_directory,
            tasks_processed: 0,
            tasks_queued: 0,
            memory,
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:enable_auto_accept (lines 534-550) ---[0m
    pub async fn enable_auto_accept(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.enable_auto_accept(config);

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:97-129 method new <-> ./crates/ccswarm/src/session/mod.rs:581-597 method update_auto_accept_config
  Similarity: 89.51%
  Classes: AgentSession <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:new (lines 97-129) ---[0m
    pub fn new(
        agent_id: String,
        agent_role: AgentRole,
        working_directory: String,
        description: Option<String>,
    ) -> Self {
        let session_id = Uuid::new_v4().to_string();
        let tmux_session = format!(
            "ccswarm-{}-{}",
            agent_role.name().to_lowercase(),
            &session_id[..8]
        );

        let memory = SessionMemory::new(session_id.clone(), agent_id.clone());

        Self {
            id: session_id,
            agent_id: agent_id.clone(),
            agent_role,
            tmux_session,
            status: SessionStatus::Active,
            background_mode: false,
            auto_accept: false,
            auto_accept_config: None,
            created_at: Utc::now(),
            last_activity: Utc::now(),
            description,
            working_directory,
            tasks_processed: 0,
            tasks_queued: 0,
            memory,
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:update_auto_accept_config (lines 581-597) ---[0m
    pub async fn update_auto_accept_config(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.update_auto_accept_config(config);

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:325-347 method pause_session <-> ./crates/ccswarm/src/session/mod.rs:696-721 method check_and_suspend_idle_agents
  Similarity: 91.17%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:pause_session (lines 325-347) ---[0m
    pub async fn pause_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, we just update the status
                // The actual pause/resume will be handled by the session itself
                session.status = SessionStatus::Paused;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "pause".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:check_and_suspend_idle_agents (lines 696-721) ---[0m
    pub async fn check_and_suspend_idle_agents(&self) -> SessionResult<Vec<String>> {
        let mut suspended_agents = Vec::new();

        if let Some(ref integration) = self.resource_integration {
            let agents_to_check: Vec<(String, String)> = {
                let sessions = self.sessions.lock().map_lock_error()?;
                sessions
                    .values()
                    .filter(|s| s.is_runnable())
                    .map(|s| (s.id.clone(), s.agent_id.clone()))
                    .collect()
            };

            for (session_id, agent_id) in agents_to_check {
                if integration.check_agent_suspension(&agent_id).await {
                    if let Err(e) = self.pause_session(&session_id).await {
                        tracing::warn!("Failed to suspend idle agent {}: {}", agent_id, e);
                    } else {
                        suspended_agents.push(agent_id);
                    }
                }
            }
        }

        Ok(suspended_agents)
    }

  ./crates/ccswarm/src/session/mod.rs:454-487 method terminate_session <-> ./crates/ccswarm/src/session/mod.rs:679-693 method cleanup_terminated_sessions
  Similarity: 89.66%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:terminate_session (lines 454-487) ---[0m
    pub async fn terminate_session(&self, session_id: &str) -> SessionResult<()> {
        let (_tmux_session, agent_id) = {
            let sessions = self.sessions.lock().map_lock_error()?;
            let session = sessions
                .get(session_id)
                .ok_or_else(|| SessionError::NotFound {
                    id: session_id.to_string(),
                })?;
            (session.tmux_session.clone(), session.agent_id.clone())
        };

        // Stop resource monitoring if enabled
        if let Some(ref integration) = self.resource_integration {
            if let Err(e) = integration
                .on_session_terminated(session_id, &agent_id)
                .await
            {
                tracing::warn!("Failed to stop resource monitoring: {}", e);
            }
        }

        // TODO: Terminate the native session when available

        // Update session status
        {
            let mut sessions = self.sessions.lock().map_lock_error()?;
            if let Some(session) = sessions.get_mut(session_id) {
                session.status = SessionStatus::Terminated;
                session.touch();
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:cleanup_terminated_sessions (lines 679-693) ---[0m
    pub async fn cleanup_terminated_sessions(&self) -> SessionResult<usize> {
        let mut sessions = self.sessions.lock().map_lock_error()?;
        let terminated: Vec<String> = sessions
            .iter()
            .filter(|(_, s)| s.status == SessionStatus::Terminated)
            .map(|(id, _)| id.clone())
            .collect();

        let count = terminated.len();
        for id in terminated {
            sessions.remove(&id);
        }

        Ok(count)
    }

  ./crates/ccswarm/src/session/mod.rs:390-411 method detach_session <-> ./crates/ccswarm/src/session/mod.rs:696-721 method check_and_suspend_idle_agents
  Similarity: 91.17%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:detach_session (lines 390-411) ---[0m
    pub async fn detach_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, detaching just changes status
                session.status = SessionStatus::Detached;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "detach".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:check_and_suspend_idle_agents (lines 696-721) ---[0m
    pub async fn check_and_suspend_idle_agents(&self) -> SessionResult<Vec<String>> {
        let mut suspended_agents = Vec::new();

        if let Some(ref integration) = self.resource_integration {
            let agents_to_check: Vec<(String, String)> = {
                let sessions = self.sessions.lock().map_lock_error()?;
                sessions
                    .values()
                    .filter(|s| s.is_runnable())
                    .map(|s| (s.id.clone(), s.agent_id.clone()))
                    .collect()
            };

            for (session_id, agent_id) in agents_to_check {
                if integration.check_agent_suspension(&agent_id).await {
                    if let Err(e) = self.pause_session(&session_id).await {
                        tracing::warn!("Failed to suspend idle agent {}: {}", agent_id, e);
                    } else {
                        suspended_agents.push(agent_id);
                    }
                }
            }
        }

        Ok(suspended_agents)
    }

  ./crates/ccswarm/src/session/mod.rs:277-316 method create_session <-> ./crates/ccswarm/src/session/mod.rs:679-693 method cleanup_terminated_sessions
  Similarity: 79.51%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:create_session (lines 277-316) ---[0m
    pub async fn create_session(
        &self,
        agent_id: String,
        agent_role: AgentRole,
        working_directory: String,
        description: Option<String>,
        auto_start: bool,
    ) -> SessionResult<AgentSession> {
        let session =
            AgentSession::new(agent_id, agent_role, working_directory.clone(), description);

        // Create the native session
        // TODO: Create native session when available

        // Set up the session environment
        self.setup_session_environment(&session).await?;

        if auto_start {
            // Start the agent in the session
            self.start_agent_in_session(&session).await?;
        }

        // Store the session
        {
            let mut sessions = self.sessions.lock().map_lock_error()?;
            sessions.insert(session.id.clone(), session.clone());
        }

        // Start resource monitoring if enabled
        if let Some(ref integration) = self.resource_integration {
            if let Err(e) = integration
                .on_session_created(&session.id, &session.agent_id, None)
                .await
            {
                tracing::warn!("Failed to start resource monitoring: {}", e);
            }
        }

        Ok(session)
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:cleanup_terminated_sessions (lines 679-693) ---[0m
    pub async fn cleanup_terminated_sessions(&self) -> SessionResult<usize> {
        let mut sessions = self.sessions.lock().map_lock_error()?;
        let terminated: Vec<String> = sessions
            .iter()
            .filter(|(_, s)| s.status == SessionStatus::Terminated)
            .map(|(id, _)| id.clone())
            .collect();

        let count = terminated.len();
        for id in terminated {
            sessions.remove(&id);
        }

        Ok(count)
    }

  ./crates/ccswarm/src/session/mod.rs:454-487 method terminate_session <-> ./crates/ccswarm/src/session/mod.rs:792-809 function test_agent_session_creation
  Similarity: 80.56%

[36m--- ./crates/ccswarm/src/session/mod.rs:terminate_session (lines 454-487) ---[0m
    pub async fn terminate_session(&self, session_id: &str) -> SessionResult<()> {
        let (_tmux_session, agent_id) = {
            let sessions = self.sessions.lock().map_lock_error()?;
            let session = sessions
                .get(session_id)
                .ok_or_else(|| SessionError::NotFound {
                    id: session_id.to_string(),
                })?;
            (session.tmux_session.clone(), session.agent_id.clone())
        };

        // Stop resource monitoring if enabled
        if let Some(ref integration) = self.resource_integration {
            if let Err(e) = integration
                .on_session_terminated(session_id, &agent_id)
                .await
            {
                tracing::warn!("Failed to stop resource monitoring: {}", e);
            }
        }

        // TODO: Terminate the native session when available

        // Update session status
        {
            let mut sessions = self.sessions.lock().map_lock_error()?;
            if let Some(session) = sessions.get_mut(session_id) {
                session.status = SessionStatus::Terminated;
                session.touch();
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:test_agent_session_creation (lines 792-809) ---[0m
    fn test_agent_session_creation() {
        use crate::identity::default_frontend_role;

        let session = AgentSession::new(
            "agent-123".to_string(),
            default_frontend_role(),
            "/tmp/test".to_string(),
            Some("Test session".to_string()),
        );

        assert_eq!(session.agent_id, "agent-123");
        assert_eq!(session.agent_role.name(), "Frontend");
        assert_eq!(session.status, SessionStatus::Active);
        assert!(!session.background_mode);
        assert!(!session.auto_accept);
        assert!(session.auto_accept_config.is_none());
        assert_eq!(session.tasks_processed, 0);
    }

  ./crates/ccswarm/src/session/mod.rs:97-129 method new <-> ./crates/ccswarm/src/session/mod.rs:559-571 method disable_auto_accept
  Similarity: 89.51%
  Classes: AgentSession <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:new (lines 97-129) ---[0m
    pub fn new(
        agent_id: String,
        agent_role: AgentRole,
        working_directory: String,
        description: Option<String>,
    ) -> Self {
        let session_id = Uuid::new_v4().to_string();
        let tmux_session = format!(
            "ccswarm-{}-{}",
            agent_role.name().to_lowercase(),
            &session_id[..8]
        );

        let memory = SessionMemory::new(session_id.clone(), agent_id.clone());

        Self {
            id: session_id,
            agent_id: agent_id.clone(),
            agent_role,
            tmux_session,
            status: SessionStatus::Active,
            background_mode: false,
            auto_accept: false,
            auto_accept_config: None,
            created_at: Utc::now(),
            last_activity: Utc::now(),
            description,
            working_directory,
            tasks_processed: 0,
            tasks_queued: 0,
            memory,
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:disable_auto_accept (lines 559-571) ---[0m
    pub async fn disable_auto_accept(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.disable_auto_accept();

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:246-264 method with_resource_monitoring <-> ./crates/ccswarm/src/session/mod.rs:497-524 method set_background_mode
  Similarity: 80.88%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:with_resource_monitoring (lines 246-264) ---[0m
    pub async fn with_resource_monitoring(
        resource_limits: crate::resource::ResourceLimits,
    ) -> SessionResult<Self> {
        let resource_monitor = Arc::new(ResourceMonitor::new(resource_limits));
        let resource_integration =
            Arc::new(SessionResourceIntegration::new(resource_monitor.clone()));

        // Start the monitoring loop
        let monitor_clone = resource_monitor.clone();
        tokio::spawn(async move {
            monitor_clone.start_monitoring_loop().await;
        });

        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: Some(resource_monitor),
            resource_integration: Some(resource_integration),
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:set_background_mode (lines 497-524) ---[0m
    pub async fn set_background_mode(
        &self,
        session_id: &str,
        auto_accept: bool,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.background_mode = true;
        if auto_accept && session.auto_accept_config.is_none() {
            // Enable auto-accept with default configuration if not already configured
            session.enable_auto_accept(AutoAcceptConfig::default());
        } else {
            session.auto_accept = auto_accept;
        }

        if session.status == SessionStatus::Active {
            session.status = SessionStatus::Background;
        }
        session.touch();

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:97-129 method new <-> ./crates/ccswarm/src/session/mod.rs:835-843 function test_session_status_display
  Similarity: 90.00%

[36m--- ./crates/ccswarm/src/session/mod.rs:new (lines 97-129) ---[0m
    pub fn new(
        agent_id: String,
        agent_role: AgentRole,
        working_directory: String,
        description: Option<String>,
    ) -> Self {
        let session_id = Uuid::new_v4().to_string();
        let tmux_session = format!(
            "ccswarm-{}-{}",
            agent_role.name().to_lowercase(),
            &session_id[..8]
        );

        let memory = SessionMemory::new(session_id.clone(), agent_id.clone());

        Self {
            id: session_id,
            agent_id: agent_id.clone(),
            agent_role,
            tmux_session,
            status: SessionStatus::Active,
            background_mode: false,
            auto_accept: false,
            auto_accept_config: None,
            created_at: Utc::now(),
            last_activity: Utc::now(),
            description,
            working_directory,
            tasks_processed: 0,
            tasks_queued: 0,
            memory,
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:test_session_status_display (lines 835-843) ---[0m
    fn test_session_status_display() {
        assert_eq!(SessionStatus::Active.to_string(), "Active");
        assert_eq!(SessionStatus::Paused.to_string(), "Paused");
        assert_eq!(SessionStatus::Background.to_string(), "Background");
        assert_eq!(
            SessionStatus::Error("test error".to_string()).to_string(),
            "Error: test error"
        );
    }

  ./crates/ccswarm/src/session/mod.rs:97-129 method new <-> ./crates/ccswarm/src/session/mod.rs:679-693 method cleanup_terminated_sessions
  Similarity: 78.13%
  Classes: AgentSession <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:new (lines 97-129) ---[0m
    pub fn new(
        agent_id: String,
        agent_role: AgentRole,
        working_directory: String,
        description: Option<String>,
    ) -> Self {
        let session_id = Uuid::new_v4().to_string();
        let tmux_session = format!(
            "ccswarm-{}-{}",
            agent_role.name().to_lowercase(),
            &session_id[..8]
        );

        let memory = SessionMemory::new(session_id.clone(), agent_id.clone());

        Self {
            id: session_id,
            agent_id: agent_id.clone(),
            agent_role,
            tmux_session,
            status: SessionStatus::Active,
            background_mode: false,
            auto_accept: false,
            auto_accept_config: None,
            created_at: Utc::now(),
            last_activity: Utc::now(),
            description,
            working_directory,
            tasks_processed: 0,
            tasks_queued: 0,
            memory,
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:cleanup_terminated_sessions (lines 679-693) ---[0m
    pub async fn cleanup_terminated_sessions(&self) -> SessionResult<usize> {
        let mut sessions = self.sessions.lock().map_lock_error()?;
        let terminated: Vec<String> = sessions
            .iter()
            .filter(|(_, s)| s.status == SessionStatus::Terminated)
            .map(|(id, _)| id.clone())
            .collect();

        let count = terminated.len();
        for id in terminated {
            sessions.remove(&id);
        }

        Ok(count)
    }

  ./crates/ccswarm/src/session/mod.rs:325-347 method pause_session <-> ./crates/ccswarm/src/session/mod.rs:534-550 method enable_auto_accept
  Similarity: 92.15%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:pause_session (lines 325-347) ---[0m
    pub async fn pause_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, we just update the status
                // The actual pause/resume will be handled by the session itself
                session.status = SessionStatus::Paused;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "pause".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:enable_auto_accept (lines 534-550) ---[0m
    pub async fn enable_auto_accept(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.enable_auto_accept(config);

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:325-347 method pause_session <-> ./crates/ccswarm/src/session/mod.rs:581-597 method update_auto_accept_config
  Similarity: 92.15%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:pause_session (lines 325-347) ---[0m
    pub async fn pause_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, we just update the status
                // The actual pause/resume will be handled by the session itself
                session.status = SessionStatus::Paused;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "pause".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:update_auto_accept_config (lines 581-597) ---[0m
    pub async fn update_auto_accept_config(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.update_auto_accept_config(config);

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:390-411 method detach_session <-> ./crates/ccswarm/src/session/mod.rs:534-550 method enable_auto_accept
  Similarity: 92.15%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:detach_session (lines 390-411) ---[0m
    pub async fn detach_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, detaching just changes status
                session.status = SessionStatus::Detached;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "detach".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:enable_auto_accept (lines 534-550) ---[0m
    pub async fn enable_auto_accept(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.enable_auto_accept(config);

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:390-411 method detach_session <-> ./crates/ccswarm/src/session/mod.rs:581-597 method update_auto_accept_config
  Similarity: 92.15%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:detach_session (lines 390-411) ---[0m
    pub async fn detach_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, detaching just changes status
                session.status = SessionStatus::Detached;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "detach".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:update_auto_accept_config (lines 581-597) ---[0m
    pub async fn update_auto_accept_config(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.update_auto_accept_config(config);

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:246-264 method with_resource_monitoring <-> ./crates/ccswarm/src/session/mod.rs:696-721 method check_and_suspend_idle_agents
  Similarity: 77.58%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:with_resource_monitoring (lines 246-264) ---[0m
    pub async fn with_resource_monitoring(
        resource_limits: crate::resource::ResourceLimits,
    ) -> SessionResult<Self> {
        let resource_monitor = Arc::new(ResourceMonitor::new(resource_limits));
        let resource_integration =
            Arc::new(SessionResourceIntegration::new(resource_monitor.clone()));

        // Start the monitoring loop
        let monitor_clone = resource_monitor.clone();
        tokio::spawn(async move {
            monitor_clone.start_monitoring_loop().await;
        });

        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: Some(resource_monitor),
            resource_integration: Some(resource_integration),
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:check_and_suspend_idle_agents (lines 696-721) ---[0m
    pub async fn check_and_suspend_idle_agents(&self) -> SessionResult<Vec<String>> {
        let mut suspended_agents = Vec::new();

        if let Some(ref integration) = self.resource_integration {
            let agents_to_check: Vec<(String, String)> = {
                let sessions = self.sessions.lock().map_lock_error()?;
                sessions
                    .values()
                    .filter(|s| s.is_runnable())
                    .map(|s| (s.id.clone(), s.agent_id.clone()))
                    .collect()
            };

            for (session_id, agent_id) in agents_to_check {
                if integration.check_agent_suspension(&agent_id).await {
                    if let Err(e) = self.pause_session(&session_id).await {
                        tracing::warn!("Failed to suspend idle agent {}: {}", agent_id, e);
                    } else {
                        suspended_agents.push(agent_id);
                    }
                }
            }
        }

        Ok(suspended_agents)
    }

  ./crates/ccswarm/src/session/mod.rs:497-524 method set_background_mode <-> ./crates/ccswarm/src/session/mod.rs:534-550 method enable_auto_accept
  Similarity: 75.88%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:set_background_mode (lines 497-524) ---[0m
    pub async fn set_background_mode(
        &self,
        session_id: &str,
        auto_accept: bool,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.background_mode = true;
        if auto_accept && session.auto_accept_config.is_none() {
            // Enable auto-accept with default configuration if not already configured
            session.enable_auto_accept(AutoAcceptConfig::default());
        } else {
            session.auto_accept = auto_accept;
        }

        if session.status == SessionStatus::Active {
            session.status = SessionStatus::Background;
        }
        session.touch();

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:enable_auto_accept (lines 534-550) ---[0m
    pub async fn enable_auto_accept(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.enable_auto_accept(config);

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:497-524 method set_background_mode <-> ./crates/ccswarm/src/session/mod.rs:581-597 method update_auto_accept_config
  Similarity: 75.88%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:set_background_mode (lines 497-524) ---[0m
    pub async fn set_background_mode(
        &self,
        session_id: &str,
        auto_accept: bool,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.background_mode = true;
        if auto_accept && session.auto_accept_config.is_none() {
            // Enable auto-accept with default configuration if not already configured
            session.enable_auto_accept(AutoAcceptConfig::default());
        } else {
            session.auto_accept = auto_accept;
        }

        if session.status == SessionStatus::Active {
            session.status = SessionStatus::Background;
        }
        session.touch();

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:update_auto_accept_config (lines 581-597) ---[0m
    pub async fn update_auto_accept_config(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.update_auto_accept_config(config);

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:534-550 method enable_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:581-597 method update_auto_accept_config
  Similarity: 99.68%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:enable_auto_accept (lines 534-550) ---[0m
    pub async fn enable_auto_accept(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.enable_auto_accept(config);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:update_auto_accept_config (lines 581-597) ---[0m
    pub async fn update_auto_accept_config(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.update_auto_accept_config(config);

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:497-524 method set_background_mode <-> ./crates/ccswarm/src/session/mod.rs:679-693 method cleanup_terminated_sessions
  Similarity: 78.35%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:set_background_mode (lines 497-524) ---[0m
    pub async fn set_background_mode(
        &self,
        session_id: &str,
        auto_accept: bool,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.background_mode = true;
        if auto_accept && session.auto_accept_config.is_none() {
            // Enable auto-accept with default configuration if not already configured
            session.enable_auto_accept(AutoAcceptConfig::default());
        } else {
            session.auto_accept = auto_accept;
        }

        if session.status == SessionStatus::Active {
            session.status = SessionStatus::Background;
        }
        session.touch();

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:cleanup_terminated_sessions (lines 679-693) ---[0m
    pub async fn cleanup_terminated_sessions(&self) -> SessionResult<usize> {
        let mut sessions = self.sessions.lock().map_lock_error()?;
        let terminated: Vec<String> = sessions
            .iter()
            .filter(|(_, s)| s.status == SessionStatus::Terminated)
            .map(|(id, _)| id.clone())
            .collect();

        let count = terminated.len();
        for id in terminated {
            sessions.remove(&id);
        }

        Ok(count)
    }

  ./crates/ccswarm/src/session/mod.rs:356-381 method resume_session <-> ./crates/ccswarm/src/session/mod.rs:679-693 method cleanup_terminated_sessions
  Similarity: 81.61%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:resume_session (lines 356-381) ---[0m
    pub async fn resume_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Paused => {
                // For native sessions, we just update the status
                session.status = if session.background_mode {
                    SessionStatus::Background
                } else {
                    SessionStatus::Active
                };
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "resume".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:cleanup_terminated_sessions (lines 679-693) ---[0m
    pub async fn cleanup_terminated_sessions(&self) -> SessionResult<usize> {
        let mut sessions = self.sessions.lock().map_lock_error()?;
        let terminated: Vec<String> = sessions
            .iter()
            .filter(|(_, s)| s.status == SessionStatus::Terminated)
            .map(|(id, _)| id.clone())
            .collect();

        let count = terminated.len();
        for id in terminated {
            sessions.remove(&id);
        }

        Ok(count)
    }

  ./crates/ccswarm/src/session/mod.rs:420-445 method attach_session <-> ./crates/ccswarm/src/session/mod.rs:679-693 method cleanup_terminated_sessions
  Similarity: 81.61%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:attach_session (lines 420-445) ---[0m
    pub async fn attach_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Detached => {
                // For native sessions, attaching just changes status
                session.status = if session.background_mode {
                    SessionStatus::Background
                } else {
                    SessionStatus::Active
                };
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "attach".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:cleanup_terminated_sessions (lines 679-693) ---[0m
    pub async fn cleanup_terminated_sessions(&self) -> SessionResult<usize> {
        let mut sessions = self.sessions.lock().map_lock_error()?;
        let terminated: Vec<String> = sessions
            .iter()
            .filter(|(_, s)| s.status == SessionStatus::Terminated)
            .map(|(id, _)| id.clone())
            .collect();

        let count = terminated.len();
        for id in terminated {
            sessions.remove(&id);
        }

        Ok(count)
    }

  ./crates/ccswarm/src/session/mod.rs:325-347 method pause_session <-> ./crates/ccswarm/src/session/mod.rs:559-571 method disable_auto_accept
  Similarity: 92.49%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:pause_session (lines 325-347) ---[0m
    pub async fn pause_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, we just update the status
                // The actual pause/resume will be handled by the session itself
                session.status = SessionStatus::Paused;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "pause".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:disable_auto_accept (lines 559-571) ---[0m
    pub async fn disable_auto_accept(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.disable_auto_accept();

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:679-693 method cleanup_terminated_sessions <-> ./crates/ccswarm/src/session/mod.rs:696-721 method check_and_suspend_idle_agents
  Similarity: 81.09%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:cleanup_terminated_sessions (lines 679-693) ---[0m
    pub async fn cleanup_terminated_sessions(&self) -> SessionResult<usize> {
        let mut sessions = self.sessions.lock().map_lock_error()?;
        let terminated: Vec<String> = sessions
            .iter()
            .filter(|(_, s)| s.status == SessionStatus::Terminated)
            .map(|(id, _)| id.clone())
            .collect();

        let count = terminated.len();
        for id in terminated {
            sessions.remove(&id);
        }

        Ok(count)
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:check_and_suspend_idle_agents (lines 696-721) ---[0m
    pub async fn check_and_suspend_idle_agents(&self) -> SessionResult<Vec<String>> {
        let mut suspended_agents = Vec::new();

        if let Some(ref integration) = self.resource_integration {
            let agents_to_check: Vec<(String, String)> = {
                let sessions = self.sessions.lock().map_lock_error()?;
                sessions
                    .values()
                    .filter(|s| s.is_runnable())
                    .map(|s| (s.id.clone(), s.agent_id.clone()))
                    .collect()
            };

            for (session_id, agent_id) in agents_to_check {
                if integration.check_agent_suspension(&agent_id).await {
                    if let Err(e) = self.pause_session(&session_id).await {
                        tracing::warn!("Failed to suspend idle agent {}: {}", agent_id, e);
                    } else {
                        suspended_agents.push(agent_id);
                    }
                }
            }
        }

        Ok(suspended_agents)
    }

  ./crates/ccswarm/src/session/mod.rs:246-264 method with_resource_monitoring <-> ./crates/ccswarm/src/session/mod.rs:356-381 method resume_session
  Similarity: 73.37%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:with_resource_monitoring (lines 246-264) ---[0m
    pub async fn with_resource_monitoring(
        resource_limits: crate::resource::ResourceLimits,
    ) -> SessionResult<Self> {
        let resource_monitor = Arc::new(ResourceMonitor::new(resource_limits));
        let resource_integration =
            Arc::new(SessionResourceIntegration::new(resource_monitor.clone()));

        // Start the monitoring loop
        let monitor_clone = resource_monitor.clone();
        tokio::spawn(async move {
            monitor_clone.start_monitoring_loop().await;
        });

        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: Some(resource_monitor),
            resource_integration: Some(resource_integration),
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:resume_session (lines 356-381) ---[0m
    pub async fn resume_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Paused => {
                // For native sessions, we just update the status
                session.status = if session.background_mode {
                    SessionStatus::Background
                } else {
                    SessionStatus::Active
                };
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "resume".to_string(),
            }),
        }
    }

  ./crates/ccswarm/src/session/mod.rs:246-264 method with_resource_monitoring <-> ./crates/ccswarm/src/session/mod.rs:420-445 method attach_session
  Similarity: 73.37%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:with_resource_monitoring (lines 246-264) ---[0m
    pub async fn with_resource_monitoring(
        resource_limits: crate::resource::ResourceLimits,
    ) -> SessionResult<Self> {
        let resource_monitor = Arc::new(ResourceMonitor::new(resource_limits));
        let resource_integration =
            Arc::new(SessionResourceIntegration::new(resource_monitor.clone()));

        // Start the monitoring loop
        let monitor_clone = resource_monitor.clone();
        tokio::spawn(async move {
            monitor_clone.start_monitoring_loop().await;
        });

        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: Some(resource_monitor),
            resource_integration: Some(resource_integration),
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:attach_session (lines 420-445) ---[0m
    pub async fn attach_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Detached => {
                // For native sessions, attaching just changes status
                session.status = if session.background_mode {
                    SessionStatus::Background
                } else {
                    SessionStatus::Active
                };
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "attach".to_string(),
            }),
        }
    }

  ./crates/ccswarm/src/session/mod.rs:390-411 method detach_session <-> ./crates/ccswarm/src/session/mod.rs:559-571 method disable_auto_accept
  Similarity: 92.49%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:detach_session (lines 390-411) ---[0m
    pub async fn detach_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, detaching just changes status
                session.status = SessionStatus::Detached;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "detach".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:disable_auto_accept (lines 559-571) ---[0m
    pub async fn disable_auto_accept(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.disable_auto_accept();

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:792-809 function test_agent_session_creation <-> ./crates/ccswarm/src/session/mod.rs:812-832 function test_session_is_runnable
  Similarity: 81.93%

[36m--- ./crates/ccswarm/src/session/mod.rs:test_agent_session_creation (lines 792-809) ---[0m
    fn test_agent_session_creation() {
        use crate::identity::default_frontend_role;

        let session = AgentSession::new(
            "agent-123".to_string(),
            default_frontend_role(),
            "/tmp/test".to_string(),
            Some("Test session".to_string()),
        );

        assert_eq!(session.agent_id, "agent-123");
        assert_eq!(session.agent_role.name(), "Frontend");
        assert_eq!(session.status, SessionStatus::Active);
        assert!(!session.background_mode);
        assert!(!session.auto_accept);
        assert!(session.auto_accept_config.is_none());
        assert_eq!(session.tasks_processed, 0);
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:test_session_is_runnable (lines 812-832) ---[0m
    fn test_session_is_runnable() {
        use crate::identity::default_backend_role;

        let mut session = AgentSession::new(
            "agent-123".to_string(),
            default_backend_role(),
            "/tmp/test".to_string(),
            None,
        );

        assert!(session.is_runnable());

        session.status = SessionStatus::Paused;
        assert!(!session.is_runnable());

        session.status = SessionStatus::Background;
        assert!(session.is_runnable());

        session.status = SessionStatus::Terminated;
        assert!(!session.is_runnable());
    }

  ./crates/ccswarm/src/session/mod.rs:497-524 method set_background_mode <-> ./crates/ccswarm/src/session/mod.rs:559-571 method disable_auto_accept
  Similarity: 76.43%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:set_background_mode (lines 497-524) ---[0m
    pub async fn set_background_mode(
        &self,
        session_id: &str,
        auto_accept: bool,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.background_mode = true;
        if auto_accept && session.auto_accept_config.is_none() {
            // Enable auto-accept with default configuration if not already configured
            session.enable_auto_accept(AutoAcceptConfig::default());
        } else {
            session.auto_accept = auto_accept;
        }

        if session.status == SessionStatus::Active {
            session.status = SessionStatus::Background;
        }
        session.touch();

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:disable_auto_accept (lines 559-571) ---[0m
    pub async fn disable_auto_accept(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.disable_auto_accept();

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:325-347 method pause_session <-> ./crates/ccswarm/src/session/mod.rs:679-693 method cleanup_terminated_sessions
  Similarity: 80.39%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:pause_session (lines 325-347) ---[0m
    pub async fn pause_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, we just update the status
                // The actual pause/resume will be handled by the session itself
                session.status = SessionStatus::Paused;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "pause".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:cleanup_terminated_sessions (lines 679-693) ---[0m
    pub async fn cleanup_terminated_sessions(&self) -> SessionResult<usize> {
        let mut sessions = self.sessions.lock().map_lock_error()?;
        let terminated: Vec<String> = sessions
            .iter()
            .filter(|(_, s)| s.status == SessionStatus::Terminated)
            .map(|(id, _)| id.clone())
            .collect();

        let count = terminated.len();
        for id in terminated {
            sessions.remove(&id);
        }

        Ok(count)
    }

  ./crates/ccswarm/src/session/mod.rs:246-264 method with_resource_monitoring <-> ./crates/ccswarm/src/session/mod.rs:325-347 method pause_session
  Similarity: 71.60%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:with_resource_monitoring (lines 246-264) ---[0m
    pub async fn with_resource_monitoring(
        resource_limits: crate::resource::ResourceLimits,
    ) -> SessionResult<Self> {
        let resource_monitor = Arc::new(ResourceMonitor::new(resource_limits));
        let resource_integration =
            Arc::new(SessionResourceIntegration::new(resource_monitor.clone()));

        // Start the monitoring loop
        let monitor_clone = resource_monitor.clone();
        tokio::spawn(async move {
            monitor_clone.start_monitoring_loop().await;
        });

        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: Some(resource_monitor),
            resource_integration: Some(resource_integration),
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:pause_session (lines 325-347) ---[0m
    pub async fn pause_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, we just update the status
                // The actual pause/resume will be handled by the session itself
                session.status = SessionStatus::Paused;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "pause".to_string(),
            }),
        }
    }

  ./crates/ccswarm/src/session/mod.rs:246-264 method with_resource_monitoring <-> ./crates/ccswarm/src/session/mod.rs:534-550 method enable_auto_accept
  Similarity: 82.66%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:with_resource_monitoring (lines 246-264) ---[0m
    pub async fn with_resource_monitoring(
        resource_limits: crate::resource::ResourceLimits,
    ) -> SessionResult<Self> {
        let resource_monitor = Arc::new(ResourceMonitor::new(resource_limits));
        let resource_integration =
            Arc::new(SessionResourceIntegration::new(resource_monitor.clone()));

        // Start the monitoring loop
        let monitor_clone = resource_monitor.clone();
        tokio::spawn(async move {
            monitor_clone.start_monitoring_loop().await;
        });

        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: Some(resource_monitor),
            resource_integration: Some(resource_integration),
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:enable_auto_accept (lines 534-550) ---[0m
    pub async fn enable_auto_accept(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.enable_auto_accept(config);

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:246-264 method with_resource_monitoring <-> ./crates/ccswarm/src/session/mod.rs:581-597 method update_auto_accept_config
  Similarity: 82.66%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:with_resource_monitoring (lines 246-264) ---[0m
    pub async fn with_resource_monitoring(
        resource_limits: crate::resource::ResourceLimits,
    ) -> SessionResult<Self> {
        let resource_monitor = Arc::new(ResourceMonitor::new(resource_limits));
        let resource_integration =
            Arc::new(SessionResourceIntegration::new(resource_monitor.clone()));

        // Start the monitoring loop
        let monitor_clone = resource_monitor.clone();
        tokio::spawn(async move {
            monitor_clone.start_monitoring_loop().await;
        });

        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: Some(resource_monitor),
            resource_integration: Some(resource_integration),
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:update_auto_accept_config (lines 581-597) ---[0m
    pub async fn update_auto_accept_config(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.update_auto_accept_config(config);

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:390-411 method detach_session <-> ./crates/ccswarm/src/session/mod.rs:679-693 method cleanup_terminated_sessions
  Similarity: 80.39%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:detach_session (lines 390-411) ---[0m
    pub async fn detach_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, detaching just changes status
                session.status = SessionStatus::Detached;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "detach".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:cleanup_terminated_sessions (lines 679-693) ---[0m
    pub async fn cleanup_terminated_sessions(&self) -> SessionResult<usize> {
        let mut sessions = self.sessions.lock().map_lock_error()?;
        let terminated: Vec<String> = sessions
            .iter()
            .filter(|(_, s)| s.status == SessionStatus::Terminated)
            .map(|(id, _)| id.clone())
            .collect();

        let count = terminated.len();
        for id in terminated {
            sessions.remove(&id);
        }

        Ok(count)
    }

  ./crates/ccswarm/src/session/mod.rs:534-550 method enable_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:559-571 method disable_auto_accept
  Similarity: 98.60%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:enable_auto_accept (lines 534-550) ---[0m
    pub async fn enable_auto_accept(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.enable_auto_accept(config);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:disable_auto_accept (lines 559-571) ---[0m
    pub async fn disable_auto_accept(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.disable_auto_accept();

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:559-571 method disable_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:581-597 method update_auto_accept_config
  Similarity: 98.60%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:disable_auto_accept (lines 559-571) ---[0m
    pub async fn disable_auto_accept(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.disable_auto_accept();

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:update_auto_accept_config (lines 581-597) ---[0m
    pub async fn update_auto_accept_config(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.update_auto_accept_config(config);

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:246-264 method with_resource_monitoring <-> ./crates/ccswarm/src/session/mod.rs:390-411 method detach_session
  Similarity: 71.60%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:with_resource_monitoring (lines 246-264) ---[0m
    pub async fn with_resource_monitoring(
        resource_limits: crate::resource::ResourceLimits,
    ) -> SessionResult<Self> {
        let resource_monitor = Arc::new(ResourceMonitor::new(resource_limits));
        let resource_integration =
            Arc::new(SessionResourceIntegration::new(resource_monitor.clone()));

        // Start the monitoring loop
        let monitor_clone = resource_monitor.clone();
        tokio::spawn(async move {
            monitor_clone.start_monitoring_loop().await;
        });

        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: Some(resource_monitor),
            resource_integration: Some(resource_integration),
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:detach_session (lines 390-411) ---[0m
    pub async fn detach_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, detaching just changes status
                session.status = SessionStatus::Detached;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "detach".to_string(),
            }),
        }
    }

  ./crates/ccswarm/src/session/mod.rs:246-264 method with_resource_monitoring <-> ./crates/ccswarm/src/session/mod.rs:679-693 method cleanup_terminated_sessions
  Similarity: 85.52%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:with_resource_monitoring (lines 246-264) ---[0m
    pub async fn with_resource_monitoring(
        resource_limits: crate::resource::ResourceLimits,
    ) -> SessionResult<Self> {
        let resource_monitor = Arc::new(ResourceMonitor::new(resource_limits));
        let resource_integration =
            Arc::new(SessionResourceIntegration::new(resource_monitor.clone()));

        // Start the monitoring loop
        let monitor_clone = resource_monitor.clone();
        tokio::spawn(async move {
            monitor_clone.start_monitoring_loop().await;
        });

        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: Some(resource_monitor),
            resource_integration: Some(resource_integration),
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:cleanup_terminated_sessions (lines 679-693) ---[0m
    pub async fn cleanup_terminated_sessions(&self) -> SessionResult<usize> {
        let mut sessions = self.sessions.lock().map_lock_error()?;
        let terminated: Vec<String> = sessions
            .iter()
            .filter(|(_, s)| s.status == SessionStatus::Terminated)
            .map(|(id, _)| id.clone())
            .collect();

        let count = terminated.len();
        for id in terminated {
            sessions.remove(&id);
        }

        Ok(count)
    }

  ./crates/ccswarm/src/session/mod.rs:534-550 method enable_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:756-769 method start_agent_in_session
  Similarity: 91.83%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:enable_auto_accept (lines 534-550) ---[0m
    pub async fn enable_auto_accept(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.enable_auto_accept(config);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:start_agent_in_session (lines 756-769) ---[0m
    async fn start_agent_in_session(&self, session: &AgentSession) -> SessionResult<()> {
        // Build the command to start the agent
        let _command = format!(
            "ccswarm agent start --id {} --role {} --session {} --working-dir {}",
            session.agent_id,
            session.agent_role.name(),
            session.id,
            session.working_directory
        );

        // TODO: Send command to native session when available

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:581-597 method update_auto_accept_config <-> ./crates/ccswarm/src/session/mod.rs:756-769 method start_agent_in_session
  Similarity: 91.83%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:update_auto_accept_config (lines 581-597) ---[0m
    pub async fn update_auto_accept_config(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.update_auto_accept_config(config);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:start_agent_in_session (lines 756-769) ---[0m
    async fn start_agent_in_session(&self, session: &AgentSession) -> SessionResult<()> {
        // Build the command to start the agent
        let _command = format!(
            "ccswarm agent start --id {} --role {} --session {} --working-dir {}",
            session.agent_id,
            session.agent_role.name(),
            session.id,
            session.working_directory
        );

        // TODO: Send command to native session when available

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:534-550 method enable_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:679-693 method cleanup_terminated_sessions
  Similarity: 84.97%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:enable_auto_accept (lines 534-550) ---[0m
    pub async fn enable_auto_accept(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.enable_auto_accept(config);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:cleanup_terminated_sessions (lines 679-693) ---[0m
    pub async fn cleanup_terminated_sessions(&self) -> SessionResult<usize> {
        let mut sessions = self.sessions.lock().map_lock_error()?;
        let terminated: Vec<String> = sessions
            .iter()
            .filter(|(_, s)| s.status == SessionStatus::Terminated)
            .map(|(id, _)| id.clone())
            .collect();

        let count = terminated.len();
        for id in terminated {
            sessions.remove(&id);
        }

        Ok(count)
    }

  ./crates/ccswarm/src/session/mod.rs:581-597 method update_auto_accept_config <-> ./crates/ccswarm/src/session/mod.rs:679-693 method cleanup_terminated_sessions
  Similarity: 84.97%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:update_auto_accept_config (lines 581-597) ---[0m
    pub async fn update_auto_accept_config(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.update_auto_accept_config(config);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:cleanup_terminated_sessions (lines 679-693) ---[0m
    pub async fn cleanup_terminated_sessions(&self) -> SessionResult<usize> {
        let mut sessions = self.sessions.lock().map_lock_error()?;
        let terminated: Vec<String> = sessions
            .iter()
            .filter(|(_, s)| s.status == SessionStatus::Terminated)
            .map(|(id, _)| id.clone())
            .collect();

        let count = terminated.len();
        for id in terminated {
            sessions.remove(&id);
        }

        Ok(count)
    }

  ./crates/ccswarm/src/session/mod.rs:603-619 method emergency_stop_all_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:724-737 method get_session_resource_usage
  Similarity: 85.18%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:emergency_stop_all_auto_accept (lines 603-619) ---[0m
    pub async fn emergency_stop_all_auto_accept(&self) -> usize {
        if let Ok(mut sessions) = self.sessions.lock() {
            let mut count = 0;

            for session in sessions.values_mut() {
                if session.auto_accept {
                    session.disable_auto_accept();
                    count += 1;
                }
            }

            count
        } else {
            // If we can't acquire the lock, return 0
            0
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_session_resource_usage (lines 724-737) ---[0m
    pub fn get_session_resource_usage(
        &self,
        session_id: &str,
    ) -> Option<crate::resource::ResourceUsage> {
        if let Ok(sessions) = self.sessions.lock() {
            let session = sessions.get(session_id)?;

            self.resource_monitor
                .as_ref()
                .and_then(|monitor| monitor.get_agent_usage(&session.agent_id))
        } else {
            None
        }
    }

  ./crates/ccswarm/src/session/mod.rs:246-264 method with_resource_monitoring <-> ./crates/ccswarm/src/session/mod.rs:559-571 method disable_auto_accept
  Similarity: 82.01%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:with_resource_monitoring (lines 246-264) ---[0m
    pub async fn with_resource_monitoring(
        resource_limits: crate::resource::ResourceLimits,
    ) -> SessionResult<Self> {
        let resource_monitor = Arc::new(ResourceMonitor::new(resource_limits));
        let resource_integration =
            Arc::new(SessionResourceIntegration::new(resource_monitor.clone()));

        // Start the monitoring loop
        let monitor_clone = resource_monitor.clone();
        tokio::spawn(async move {
            monitor_clone.start_monitoring_loop().await;
        });

        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: Some(resource_monitor),
            resource_integration: Some(resource_integration),
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:disable_auto_accept (lines 559-571) ---[0m
    pub async fn disable_auto_accept(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.disable_auto_accept();

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:356-381 method resume_session <-> ./crates/ccswarm/src/session/mod.rs:835-843 function test_session_status_display
  Similarity: 74.87%

[36m--- ./crates/ccswarm/src/session/mod.rs:resume_session (lines 356-381) ---[0m
    pub async fn resume_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Paused => {
                // For native sessions, we just update the status
                session.status = if session.background_mode {
                    SessionStatus::Background
                } else {
                    SessionStatus::Active
                };
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "resume".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:test_session_status_display (lines 835-843) ---[0m
    fn test_session_status_display() {
        assert_eq!(SessionStatus::Active.to_string(), "Active");
        assert_eq!(SessionStatus::Paused.to_string(), "Paused");
        assert_eq!(SessionStatus::Background.to_string(), "Background");
        assert_eq!(
            SessionStatus::Error("test error".to_string()).to_string(),
            "Error: test error"
        );
    }

  ./crates/ccswarm/src/session/mod.rs:420-445 method attach_session <-> ./crates/ccswarm/src/session/mod.rs:835-843 function test_session_status_display
  Similarity: 74.87%

[36m--- ./crates/ccswarm/src/session/mod.rs:attach_session (lines 420-445) ---[0m
    pub async fn attach_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Detached => {
                // For native sessions, attaching just changes status
                session.status = if session.background_mode {
                    SessionStatus::Background
                } else {
                    SessionStatus::Active
                };
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "attach".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:test_session_status_display (lines 835-843) ---[0m
    fn test_session_status_display() {
        assert_eq!(SessionStatus::Active.to_string(), "Active");
        assert_eq!(SessionStatus::Paused.to_string(), "Paused");
        assert_eq!(SessionStatus::Background.to_string(), "Background");
        assert_eq!(
            SessionStatus::Error("test error".to_string()).to_string(),
            "Error: test error"
        );
    }

  ./crates/ccswarm/src/session/mod.rs:46-55 method fmt <-> ./crates/ccswarm/src/session/mod.rs:603-619 method emergency_stop_all_auto_accept
  Similarity: 93.06%
  Classes: SessionStatus <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:fmt (lines 46-55) ---[0m
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            SessionStatus::Active => write!(f, "Active"),
            SessionStatus::Paused => write!(f, "Paused"),
            SessionStatus::Detached => write!(f, "Detached"),
            SessionStatus::Background => write!(f, "Background"),
            SessionStatus::Terminated => write!(f, "Terminated"),
            SessionStatus::Error(e) => write!(f, "Error: {}", e),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:emergency_stop_all_auto_accept (lines 603-619) ---[0m
    pub async fn emergency_stop_all_auto_accept(&self) -> usize {
        if let Ok(mut sessions) = self.sessions.lock() {
            let mut count = 0;

            for session in sessions.values_mut() {
                if session.auto_accept {
                    session.disable_auto_accept();
                    count += 1;
                }
            }

            count
        } else {
            // If we can't acquire the lock, return 0
            0
        }
    }

  ./crates/ccswarm/src/session/mod.rs:559-571 method disable_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:756-769 method start_agent_in_session
  Similarity: 91.74%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:disable_auto_accept (lines 559-571) ---[0m
    pub async fn disable_auto_accept(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.disable_auto_accept();

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:start_agent_in_session (lines 756-769) ---[0m
    async fn start_agent_in_session(&self, session: &AgentSession) -> SessionResult<()> {
        // Build the command to start the agent
        let _command = format!(
            "ccswarm agent start --id {} --role {} --session {} --working-dir {}",
            session.agent_id,
            session.agent_role.name(),
            session.id,
            session.working_directory
        );

        // TODO: Send command to native session when available

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:603-619 method emergency_stop_all_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:622-632 method get_auto_accept_sessions
  Similarity: 86.55%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:emergency_stop_all_auto_accept (lines 603-619) ---[0m
    pub async fn emergency_stop_all_auto_accept(&self) -> usize {
        if let Ok(mut sessions) = self.sessions.lock() {
            let mut count = 0;

            for session in sessions.values_mut() {
                if session.auto_accept {
                    session.disable_auto_accept();
                    count += 1;
                }
            }

            count
        } else {
            // If we can't acquire the lock, return 0
            0
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_auto_accept_sessions (lines 622-632) ---[0m
    pub fn get_auto_accept_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.is_auto_accept_ready())
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:603-619 method emergency_stop_all_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:653-663 method list_active_sessions
  Similarity: 86.55%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:emergency_stop_all_auto_accept (lines 603-619) ---[0m
    pub async fn emergency_stop_all_auto_accept(&self) -> usize {
        if let Ok(mut sessions) = self.sessions.lock() {
            let mut count = 0;

            for session in sessions.values_mut() {
                if session.auto_accept {
                    session.disable_auto_accept();
                    count += 1;
                }
            }

            count
        } else {
            // If we can't acquire the lock, return 0
            0
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:list_active_sessions (lines 653-663) ---[0m
    pub fn list_active_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.is_runnable())
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:603-619 method emergency_stop_all_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:666-676 method get_sessions_by_role
  Similarity: 86.55%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:emergency_stop_all_auto_accept (lines 603-619) ---[0m
    pub async fn emergency_stop_all_auto_accept(&self) -> usize {
        if let Ok(mut sessions) = self.sessions.lock() {
            let mut count = 0;

            for session in sessions.values_mut() {
                if session.auto_accept {
                    session.disable_auto_accept();
                    count += 1;
                }
            }

            count
        } else {
            // If we can't acquire the lock, return 0
            0
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_sessions_by_role (lines 666-676) ---[0m
    pub fn get_sessions_by_role(&self, role: AgentRole) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.agent_role == role)
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:559-571 method disable_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:679-693 method cleanup_terminated_sessions
  Similarity: 85.65%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:disable_auto_accept (lines 559-571) ---[0m
    pub async fn disable_auto_accept(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.disable_auto_accept();

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:cleanup_terminated_sessions (lines 679-693) ---[0m
    pub async fn cleanup_terminated_sessions(&self) -> SessionResult<usize> {
        let mut sessions = self.sessions.lock().map_lock_error()?;
        let terminated: Vec<String> = sessions
            .iter()
            .filter(|(_, s)| s.status == SessionStatus::Terminated)
            .map(|(id, _)| id.clone())
            .collect();

        let count = terminated.len();
        for id in terminated {
            sessions.remove(&id);
        }

        Ok(count)
    }

  ./crates/ccswarm/src/session/mod.rs:756-769 method start_agent_in_session <-> ./crates/ccswarm/src/session/mod.rs:773-784 method default
  Similarity: 91.41%
  Classes: SessionManager <-> Default

[36m--- ./crates/ccswarm/src/session/mod.rs:start_agent_in_session (lines 756-769) ---[0m
    async fn start_agent_in_session(&self, session: &AgentSession) -> SessionResult<()> {
        // Build the command to start the agent
        let _command = format!(
            "ccswarm agent start --id {} --role {} --session {} --working-dir {}",
            session.agent_id,
            session.agent_role.name(),
            session.id,
            session.working_directory
        );

        // TODO: Send command to native session when available

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:default (lines 773-784) ---[0m
    fn default() -> Self {
        // Note: This is a blocking call in an async context
        // Consider using lazy_static or once_cell for production
        tokio::task::block_in_place(|| {
            tokio::runtime::Handle::current().block_on(async {
                // Create with default resource monitoring enabled
                Self::with_resource_monitoring(crate::resource::ResourceLimits::default())
                    .await
                    .expect("Failed to create SessionManager")
            })
        })
    }

  ./crates/ccswarm/src/session/mod.rs:534-550 method enable_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:773-784 method default
  Similarity: 81.94%
  Classes: SessionManager <-> Default

[36m--- ./crates/ccswarm/src/session/mod.rs:enable_auto_accept (lines 534-550) ---[0m
    pub async fn enable_auto_accept(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.enable_auto_accept(config);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:default (lines 773-784) ---[0m
    fn default() -> Self {
        // Note: This is a blocking call in an async context
        // Consider using lazy_static or once_cell for production
        tokio::task::block_in_place(|| {
            tokio::runtime::Handle::current().block_on(async {
                // Create with default resource monitoring enabled
                Self::with_resource_monitoring(crate::resource::ResourceLimits::default())
                    .await
                    .expect("Failed to create SessionManager")
            })
        })
    }

  ./crates/ccswarm/src/session/mod.rs:581-597 method update_auto_accept_config <-> ./crates/ccswarm/src/session/mod.rs:773-784 method default
  Similarity: 81.94%
  Classes: SessionManager <-> Default

[36m--- ./crates/ccswarm/src/session/mod.rs:update_auto_accept_config (lines 581-597) ---[0m
    pub async fn update_auto_accept_config(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.update_auto_accept_config(config);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:default (lines 773-784) ---[0m
    fn default() -> Self {
        // Note: This is a blocking call in an async context
        // Consider using lazy_static or once_cell for production
        tokio::task::block_in_place(|| {
            tokio::runtime::Handle::current().block_on(async {
                // Create with default resource monitoring enabled
                Self::with_resource_monitoring(crate::resource::ResourceLimits::default())
                    .await
                    .expect("Failed to create SessionManager")
            })
        })
    }

  ./crates/ccswarm/src/session/mod.rs:325-347 method pause_session <-> ./crates/ccswarm/src/session/mod.rs:835-843 function test_session_status_display
  Similarity: 73.20%

[36m--- ./crates/ccswarm/src/session/mod.rs:pause_session (lines 325-347) ---[0m
    pub async fn pause_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, we just update the status
                // The actual pause/resume will be handled by the session itself
                session.status = SessionStatus::Paused;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "pause".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:test_session_status_display (lines 835-843) ---[0m
    fn test_session_status_display() {
        assert_eq!(SessionStatus::Active.to_string(), "Active");
        assert_eq!(SessionStatus::Paused.to_string(), "Paused");
        assert_eq!(SessionStatus::Background.to_string(), "Background");
        assert_eq!(
            SessionStatus::Error("test error".to_string()).to_string(),
            "Error: test error"
        );
    }

  ./crates/ccswarm/src/session/mod.rs:390-411 method detach_session <-> ./crates/ccswarm/src/session/mod.rs:835-843 function test_session_status_display
  Similarity: 73.20%

[36m--- ./crates/ccswarm/src/session/mod.rs:detach_session (lines 390-411) ---[0m
    pub async fn detach_session(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        match session.status {
            SessionStatus::Active | SessionStatus::Background => {
                // For native sessions, detaching just changes status
                session.status = SessionStatus::Detached;
                session.touch();
                Ok(())
            }
            _ => Err(SessionError::InvalidState {
                state: format!("{:?}", session.status),
                operation: "detach".to_string(),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:test_session_status_display (lines 835-843) ---[0m
    fn test_session_status_display() {
        assert_eq!(SessionStatus::Active.to_string(), "Active");
        assert_eq!(SessionStatus::Paused.to_string(), "Paused");
        assert_eq!(SessionStatus::Background.to_string(), "Background");
        assert_eq!(
            SessionStatus::Error("test error".to_string()).to_string(),
            "Error: test error"
        );
    }

  ./crates/ccswarm/src/session/mod.rs:622-632 method get_auto_accept_sessions <-> ./crates/ccswarm/src/session/mod.rs:724-737 method get_session_resource_usage
  Similarity: 89.53%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:get_auto_accept_sessions (lines 622-632) ---[0m
    pub fn get_auto_accept_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.is_auto_accept_ready())
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_session_resource_usage (lines 724-737) ---[0m
    pub fn get_session_resource_usage(
        &self,
        session_id: &str,
    ) -> Option<crate::resource::ResourceUsage> {
        if let Ok(sessions) = self.sessions.lock() {
            let session = sessions.get(session_id)?;

            self.resource_monitor
                .as_ref()
                .and_then(|monitor| monitor.get_agent_usage(&session.agent_id))
        } else {
            None
        }
    }

  ./crates/ccswarm/src/session/mod.rs:653-663 method list_active_sessions <-> ./crates/ccswarm/src/session/mod.rs:724-737 method get_session_resource_usage
  Similarity: 89.53%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:list_active_sessions (lines 653-663) ---[0m
    pub fn list_active_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.is_runnable())
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_session_resource_usage (lines 724-737) ---[0m
    pub fn get_session_resource_usage(
        &self,
        session_id: &str,
    ) -> Option<crate::resource::ResourceUsage> {
        if let Ok(sessions) = self.sessions.lock() {
            let session = sessions.get(session_id)?;

            self.resource_monitor
                .as_ref()
                .and_then(|monitor| monitor.get_agent_usage(&session.agent_id))
        } else {
            None
        }
    }

  ./crates/ccswarm/src/session/mod.rs:666-676 method get_sessions_by_role <-> ./crates/ccswarm/src/session/mod.rs:724-737 method get_session_resource_usage
  Similarity: 89.53%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:get_sessions_by_role (lines 666-676) ---[0m
    pub fn get_sessions_by_role(&self, role: AgentRole) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.agent_role == role)
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_session_resource_usage (lines 724-737) ---[0m
    pub fn get_session_resource_usage(
        &self,
        session_id: &str,
    ) -> Option<crate::resource::ResourceUsage> {
        if let Ok(sessions) = self.sessions.lock() {
            let session = sessions.get(session_id)?;

            self.resource_monitor
                .as_ref()
                .and_then(|monitor| monitor.get_agent_usage(&session.agent_id))
        } else {
            None
        }
    }

  ./crates/ccswarm/src/session/mod.rs:46-55 method fmt <-> ./crates/ccswarm/src/session/mod.rs:724-737 method get_session_resource_usage
  Similarity: 93.06%
  Classes: SessionStatus <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:fmt (lines 46-55) ---[0m
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            SessionStatus::Active => write!(f, "Active"),
            SessionStatus::Paused => write!(f, "Paused"),
            SessionStatus::Detached => write!(f, "Detached"),
            SessionStatus::Background => write!(f, "Background"),
            SessionStatus::Terminated => write!(f, "Terminated"),
            SessionStatus::Error(e) => write!(f, "Error: {}", e),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_session_resource_usage (lines 724-737) ---[0m
    pub fn get_session_resource_usage(
        &self,
        session_id: &str,
    ) -> Option<crate::resource::ResourceUsage> {
        if let Ok(sessions) = self.sessions.lock() {
            let session = sessions.get(session_id)?;

            self.resource_monitor
                .as_ref()
                .and_then(|monitor| monitor.get_agent_usage(&session.agent_id))
        } else {
            None
        }
    }

  ./crates/ccswarm/src/session/mod.rs:622-632 method get_auto_accept_sessions <-> ./crates/ccswarm/src/session/mod.rs:653-663 method list_active_sessions
  Similarity: 99.63%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:get_auto_accept_sessions (lines 622-632) ---[0m
    pub fn get_auto_accept_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.is_auto_accept_ready())
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:list_active_sessions (lines 653-663) ---[0m
    pub fn list_active_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.is_runnable())
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:246-264 method with_resource_monitoring <-> ./crates/ccswarm/src/session/mod.rs:773-784 method default
  Similarity: 70.13%
  Classes: SessionManager <-> Default

[36m--- ./crates/ccswarm/src/session/mod.rs:with_resource_monitoring (lines 246-264) ---[0m
    pub async fn with_resource_monitoring(
        resource_limits: crate::resource::ResourceLimits,
    ) -> SessionResult<Self> {
        let resource_monitor = Arc::new(ResourceMonitor::new(resource_limits));
        let resource_integration =
            Arc::new(SessionResourceIntegration::new(resource_monitor.clone()));

        // Start the monitoring loop
        let monitor_clone = resource_monitor.clone();
        tokio::spawn(async move {
            monitor_clone.start_monitoring_loop().await;
        });

        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: Some(resource_monitor),
            resource_integration: Some(resource_integration),
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:default (lines 773-784) ---[0m
    fn default() -> Self {
        // Note: This is a blocking call in an async context
        // Consider using lazy_static or once_cell for production
        tokio::task::block_in_place(|| {
            tokio::runtime::Handle::current().block_on(async {
                // Create with default resource monitoring enabled
                Self::with_resource_monitoring(crate::resource::ResourceLimits::default())
                    .await
                    .expect("Failed to create SessionManager")
            })
        })
    }

  ./crates/ccswarm/src/session/mod.rs:622-632 method get_auto_accept_sessions <-> ./crates/ccswarm/src/session/mod.rs:666-676 method get_sessions_by_role
  Similarity: 96.79%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:get_auto_accept_sessions (lines 622-632) ---[0m
    pub fn get_auto_accept_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.is_auto_accept_ready())
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_sessions_by_role (lines 666-676) ---[0m
    pub fn get_sessions_by_role(&self, role: AgentRole) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.agent_role == role)
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:653-663 method list_active_sessions <-> ./crates/ccswarm/src/session/mod.rs:666-676 method get_sessions_by_role
  Similarity: 96.79%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:list_active_sessions (lines 653-663) ---[0m
    pub fn list_active_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.is_runnable())
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_sessions_by_role (lines 666-676) ---[0m
    pub fn get_sessions_by_role(&self, role: AgentRole) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.agent_role == role)
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:603-619 method emergency_stop_all_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:635-641 method get_session
  Similarity: 87.38%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:emergency_stop_all_auto_accept (lines 603-619) ---[0m
    pub async fn emergency_stop_all_auto_accept(&self) -> usize {
        if let Ok(mut sessions) = self.sessions.lock() {
            let mut count = 0;

            for session in sessions.values_mut() {
                if session.auto_accept {
                    session.disable_auto_accept();
                    count += 1;
                }
            }

            count
        } else {
            // If we can't acquire the lock, return 0
            0
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_session (lines 635-641) ---[0m
    pub fn get_session(&self, session_id: &str) -> Option<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions.get(session_id).cloned()
        } else {
            None
        }
    }

  ./crates/ccswarm/src/session/mod.rs:603-619 method emergency_stop_all_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:644-650 method list_sessions
  Similarity: 86.55%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:emergency_stop_all_auto_accept (lines 603-619) ---[0m
    pub async fn emergency_stop_all_auto_accept(&self) -> usize {
        if let Ok(mut sessions) = self.sessions.lock() {
            let mut count = 0;

            for session in sessions.values_mut() {
                if session.auto_accept {
                    session.disable_auto_accept();
                    count += 1;
                }
            }

            count
        } else {
            // If we can't acquire the lock, return 0
            0
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:list_sessions (lines 644-650) ---[0m
    pub fn list_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions.values().cloned().collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:246-264 method with_resource_monitoring <-> ./crates/ccswarm/src/session/mod.rs:835-843 function test_session_status_display
  Similarity: 73.77%

[36m--- ./crates/ccswarm/src/session/mod.rs:with_resource_monitoring (lines 246-264) ---[0m
    pub async fn with_resource_monitoring(
        resource_limits: crate::resource::ResourceLimits,
    ) -> SessionResult<Self> {
        let resource_monitor = Arc::new(ResourceMonitor::new(resource_limits));
        let resource_integration =
            Arc::new(SessionResourceIntegration::new(resource_monitor.clone()));

        // Start the monitoring loop
        let monitor_clone = resource_monitor.clone();
        tokio::spawn(async move {
            monitor_clone.start_monitoring_loop().await;
        });

        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: Some(resource_monitor),
            resource_integration: Some(resource_integration),
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:test_session_status_display (lines 835-843) ---[0m
    fn test_session_status_display() {
        assert_eq!(SessionStatus::Active.to_string(), "Active");
        assert_eq!(SessionStatus::Paused.to_string(), "Paused");
        assert_eq!(SessionStatus::Background.to_string(), "Background");
        assert_eq!(
            SessionStatus::Error("test error".to_string()).to_string(),
            "Error: test error"
        );
    }

  ./crates/ccswarm/src/session/mod.rs:679-693 method cleanup_terminated_sessions <-> ./crates/ccswarm/src/session/mod.rs:773-784 method default
  Similarity: 76.26%
  Classes: SessionManager <-> Default

[36m--- ./crates/ccswarm/src/session/mod.rs:cleanup_terminated_sessions (lines 679-693) ---[0m
    pub async fn cleanup_terminated_sessions(&self) -> SessionResult<usize> {
        let mut sessions = self.sessions.lock().map_lock_error()?;
        let terminated: Vec<String> = sessions
            .iter()
            .filter(|(_, s)| s.status == SessionStatus::Terminated)
            .map(|(id, _)| id.clone())
            .collect();

        let count = terminated.len();
        for id in terminated {
            sessions.remove(&id);
        }

        Ok(count)
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:default (lines 773-784) ---[0m
    fn default() -> Self {
        // Note: This is a blocking call in an async context
        // Consider using lazy_static or once_cell for production
        tokio::task::block_in_place(|| {
            tokio::runtime::Handle::current().block_on(async {
                // Create with default resource monitoring enabled
                Self::with_resource_monitoring(crate::resource::ResourceLimits::default())
                    .await
                    .expect("Failed to create SessionManager")
            })
        })
    }

  ./crates/ccswarm/src/session/mod.rs:534-550 method enable_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:835-843 function test_session_status_display
  Similarity: 79.18%

[36m--- ./crates/ccswarm/src/session/mod.rs:enable_auto_accept (lines 534-550) ---[0m
    pub async fn enable_auto_accept(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.enable_auto_accept(config);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:test_session_status_display (lines 835-843) ---[0m
    fn test_session_status_display() {
        assert_eq!(SessionStatus::Active.to_string(), "Active");
        assert_eq!(SessionStatus::Paused.to_string(), "Paused");
        assert_eq!(SessionStatus::Background.to_string(), "Background");
        assert_eq!(
            SessionStatus::Error("test error".to_string()).to_string(),
            "Error: test error"
        );
    }

  ./crates/ccswarm/src/session/mod.rs:581-597 method update_auto_accept_config <-> ./crates/ccswarm/src/session/mod.rs:835-843 function test_session_status_display
  Similarity: 79.18%

[36m--- ./crates/ccswarm/src/session/mod.rs:update_auto_accept_config (lines 581-597) ---[0m
    pub async fn update_auto_accept_config(
        &self,
        session_id: &str,
        config: AutoAcceptConfig,
    ) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.update_auto_accept_config(config);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:test_session_status_display (lines 835-843) ---[0m
    fn test_session_status_display() {
        assert_eq!(SessionStatus::Active.to_string(), "Active");
        assert_eq!(SessionStatus::Paused.to_string(), "Paused");
        assert_eq!(SessionStatus::Background.to_string(), "Background");
        assert_eq!(
            SessionStatus::Error("test error".to_string()).to_string(),
            "Error: test error"
        );
    }

  ./crates/ccswarm/src/session/mod.rs:559-571 method disable_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:773-784 method default
  Similarity: 81.74%
  Classes: SessionManager <-> Default

[36m--- ./crates/ccswarm/src/session/mod.rs:disable_auto_accept (lines 559-571) ---[0m
    pub async fn disable_auto_accept(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.disable_auto_accept();

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:default (lines 773-784) ---[0m
    fn default() -> Self {
        // Note: This is a blocking call in an async context
        // Consider using lazy_static or once_cell for production
        tokio::task::block_in_place(|| {
            tokio::runtime::Handle::current().block_on(async {
                // Create with default resource monitoring enabled
                Self::with_resource_monitoring(crate::resource::ResourceLimits::default())
                    .await
                    .expect("Failed to create SessionManager")
            })
        })
    }

  ./crates/ccswarm/src/session/mod.rs:756-769 method start_agent_in_session <-> ./crates/ccswarm/src/session/mod.rs:835-843 function test_session_status_display
  Similarity: 88.04%

[36m--- ./crates/ccswarm/src/session/mod.rs:start_agent_in_session (lines 756-769) ---[0m
    async fn start_agent_in_session(&self, session: &AgentSession) -> SessionResult<()> {
        // Build the command to start the agent
        let _command = format!(
            "ccswarm agent start --id {} --role {} --session {} --working-dir {}",
            session.agent_id,
            session.agent_role.name(),
            session.id,
            session.working_directory
        );

        // TODO: Send command to native session when available

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:test_session_status_display (lines 835-843) ---[0m
    fn test_session_status_display() {
        assert_eq!(SessionStatus::Active.to_string(), "Active");
        assert_eq!(SessionStatus::Paused.to_string(), "Paused");
        assert_eq!(SessionStatus::Background.to_string(), "Background");
        assert_eq!(
            SessionStatus::Error("test error".to_string()).to_string(),
            "Error: test error"
        );
    }

  ./crates/ccswarm/src/session/mod.rs:46-55 method fmt <-> ./crates/ccswarm/src/session/mod.rs:773-784 method default
  Similarity: 89.63%
  Classes: SessionStatus <-> Default

[36m--- ./crates/ccswarm/src/session/mod.rs:fmt (lines 46-55) ---[0m
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            SessionStatus::Active => write!(f, "Active"),
            SessionStatus::Paused => write!(f, "Paused"),
            SessionStatus::Detached => write!(f, "Detached"),
            SessionStatus::Background => write!(f, "Background"),
            SessionStatus::Terminated => write!(f, "Terminated"),
            SessionStatus::Error(e) => write!(f, "Error: {}", e),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:default (lines 773-784) ---[0m
    fn default() -> Self {
        // Note: This is a blocking call in an async context
        // Consider using lazy_static or once_cell for production
        tokio::task::block_in_place(|| {
            tokio::runtime::Handle::current().block_on(async {
                // Create with default resource monitoring enabled
                Self::with_resource_monitoring(crate::resource::ResourceLimits::default())
                    .await
                    .expect("Failed to create SessionManager")
            })
        })
    }

  ./crates/ccswarm/src/session/mod.rs:237-243 method new <-> ./crates/ccswarm/src/session/mod.rs:603-619 method emergency_stop_all_auto_accept
  Similarity: 81.07%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:new (lines 237-243) ---[0m
    pub async fn new() -> SessionResult<Self> {
        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: None,
            resource_integration: None,
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:emergency_stop_all_auto_accept (lines 603-619) ---[0m
    pub async fn emergency_stop_all_auto_accept(&self) -> usize {
        if let Ok(mut sessions) = self.sessions.lock() {
            let mut count = 0;

            for session in sessions.values_mut() {
                if session.auto_accept {
                    session.disable_auto_accept();
                    count += 1;
                }
            }

            count
        } else {
            // If we can't acquire the lock, return 0
            0
        }
    }

  ./crates/ccswarm/src/session/mod.rs:635-641 method get_session <-> ./crates/ccswarm/src/session/mod.rs:724-737 method get_session_resource_usage
  Similarity: 92.24%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:get_session (lines 635-641) ---[0m
    pub fn get_session(&self, session_id: &str) -> Option<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions.get(session_id).cloned()
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_session_resource_usage (lines 724-737) ---[0m
    pub fn get_session_resource_usage(
        &self,
        session_id: &str,
    ) -> Option<crate::resource::ResourceUsage> {
        if let Ok(sessions) = self.sessions.lock() {
            let session = sessions.get(session_id)?;

            self.resource_monitor
                .as_ref()
                .and_then(|monitor| monitor.get_agent_usage(&session.agent_id))
        } else {
            None
        }
    }

  ./crates/ccswarm/src/session/mod.rs:196-206 method add_episode <-> ./crates/ccswarm/src/session/mod.rs:756-769 method start_agent_in_session
  Similarity: 76.33%
  Classes: AgentSession <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:add_episode (lines 196-206) ---[0m
    pub fn add_episode(
        &mut self,
        event_type: EpisodeType,
        description: String,
        context: HashMap<String, String>,
        outcome: EpisodeOutcome,
    ) {
        self.memory
            .add_episode(event_type, description, context, outcome);
        self.touch();
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:start_agent_in_session (lines 756-769) ---[0m
    async fn start_agent_in_session(&self, session: &AgentSession) -> SessionResult<()> {
        // Build the command to start the agent
        let _command = format!(
            "ccswarm agent start --id {} --role {} --session {} --working-dir {}",
            session.agent_id,
            session.agent_role.name(),
            session.id,
            session.working_directory
        );

        // TODO: Send command to native session when available

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:644-650 method list_sessions <-> ./crates/ccswarm/src/session/mod.rs:724-737 method get_session_resource_usage
  Similarity: 89.53%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:list_sessions (lines 644-650) ---[0m
    pub fn list_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions.values().cloned().collect()
        } else {
            Vec::new()
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_session_resource_usage (lines 724-737) ---[0m
    pub fn get_session_resource_usage(
        &self,
        session_id: &str,
    ) -> Option<crate::resource::ResourceUsage> {
        if let Ok(sessions) = self.sessions.lock() {
            let session = sessions.get(session_id)?;

            self.resource_monitor
                .as_ref()
                .and_then(|monitor| monitor.get_agent_usage(&session.agent_id))
        } else {
            None
        }
    }

  ./crates/ccswarm/src/session/mod.rs:46-55 method fmt <-> ./crates/ccswarm/src/session/mod.rs:622-632 method get_auto_accept_sessions
  Similarity: 87.84%
  Classes: SessionStatus <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:fmt (lines 46-55) ---[0m
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            SessionStatus::Active => write!(f, "Active"),
            SessionStatus::Paused => write!(f, "Paused"),
            SessionStatus::Detached => write!(f, "Detached"),
            SessionStatus::Background => write!(f, "Background"),
            SessionStatus::Terminated => write!(f, "Terminated"),
            SessionStatus::Error(e) => write!(f, "Error: {}", e),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_auto_accept_sessions (lines 622-632) ---[0m
    pub fn get_auto_accept_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.is_auto_accept_ready())
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:46-55 method fmt <-> ./crates/ccswarm/src/session/mod.rs:653-663 method list_active_sessions
  Similarity: 87.84%
  Classes: SessionStatus <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:fmt (lines 46-55) ---[0m
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            SessionStatus::Active => write!(f, "Active"),
            SessionStatus::Paused => write!(f, "Paused"),
            SessionStatus::Detached => write!(f, "Detached"),
            SessionStatus::Background => write!(f, "Background"),
            SessionStatus::Terminated => write!(f, "Terminated"),
            SessionStatus::Error(e) => write!(f, "Error: {}", e),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:list_active_sessions (lines 653-663) ---[0m
    pub fn list_active_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.is_runnable())
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:46-55 method fmt <-> ./crates/ccswarm/src/session/mod.rs:666-676 method get_sessions_by_role
  Similarity: 87.84%
  Classes: SessionStatus <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:fmt (lines 46-55) ---[0m
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            SessionStatus::Active => write!(f, "Active"),
            SessionStatus::Paused => write!(f, "Paused"),
            SessionStatus::Detached => write!(f, "Detached"),
            SessionStatus::Background => write!(f, "Background"),
            SessionStatus::Terminated => write!(f, "Terminated"),
            SessionStatus::Error(e) => write!(f, "Error: {}", e),
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_sessions_by_role (lines 666-676) ---[0m
    pub fn get_sessions_by_role(&self, role: AgentRole) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.agent_role == role)
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:622-632 method get_auto_accept_sessions <-> ./crates/ccswarm/src/session/mod.rs:773-784 method default
  Similarity: 77.41%
  Classes: SessionManager <-> Default

[36m--- ./crates/ccswarm/src/session/mod.rs:get_auto_accept_sessions (lines 622-632) ---[0m
    pub fn get_auto_accept_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.is_auto_accept_ready())
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:default (lines 773-784) ---[0m
    fn default() -> Self {
        // Note: This is a blocking call in an async context
        // Consider using lazy_static or once_cell for production
        tokio::task::block_in_place(|| {
            tokio::runtime::Handle::current().block_on(async {
                // Create with default resource monitoring enabled
                Self::with_resource_monitoring(crate::resource::ResourceLimits::default())
                    .await
                    .expect("Failed to create SessionManager")
            })
        })
    }

  ./crates/ccswarm/src/session/mod.rs:653-663 method list_active_sessions <-> ./crates/ccswarm/src/session/mod.rs:773-784 method default
  Similarity: 77.41%
  Classes: SessionManager <-> Default

[36m--- ./crates/ccswarm/src/session/mod.rs:list_active_sessions (lines 653-663) ---[0m
    pub fn list_active_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.is_runnable())
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:default (lines 773-784) ---[0m
    fn default() -> Self {
        // Note: This is a blocking call in an async context
        // Consider using lazy_static or once_cell for production
        tokio::task::block_in_place(|| {
            tokio::runtime::Handle::current().block_on(async {
                // Create with default resource monitoring enabled
                Self::with_resource_monitoring(crate::resource::ResourceLimits::default())
                    .await
                    .expect("Failed to create SessionManager")
            })
        })
    }

  ./crates/ccswarm/src/session/mod.rs:666-676 method get_sessions_by_role <-> ./crates/ccswarm/src/session/mod.rs:773-784 method default
  Similarity: 77.41%
  Classes: SessionManager <-> Default

[36m--- ./crates/ccswarm/src/session/mod.rs:get_sessions_by_role (lines 666-676) ---[0m
    pub fn get_sessions_by_role(&self, role: AgentRole) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.agent_role == role)
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:default (lines 773-784) ---[0m
    fn default() -> Self {
        // Note: This is a blocking call in an async context
        // Consider using lazy_static or once_cell for production
        tokio::task::block_in_place(|| {
            tokio::runtime::Handle::current().block_on(async {
                // Create with default resource monitoring enabled
                Self::with_resource_monitoring(crate::resource::ResourceLimits::default())
                    .await
                    .expect("Failed to create SessionManager")
            })
        })
    }

  ./crates/ccswarm/src/session/mod.rs:559-571 method disable_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:835-843 function test_session_status_display
  Similarity: 80.21%

[36m--- ./crates/ccswarm/src/session/mod.rs:disable_auto_accept (lines 559-571) ---[0m
    pub async fn disable_auto_accept(&self, session_id: &str) -> SessionResult<()> {
        let mut sessions = self.sessions.lock().map_lock_error()?;

        let session = sessions
            .get_mut(session_id)
            .ok_or_else(|| SessionError::NotFound {
                id: session_id.to_string(),
            })?;

        session.disable_auto_accept();

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:test_session_status_display (lines 835-843) ---[0m
    fn test_session_status_display() {
        assert_eq!(SessionStatus::Active.to_string(), "Active");
        assert_eq!(SessionStatus::Paused.to_string(), "Paused");
        assert_eq!(SessionStatus::Background.to_string(), "Background");
        assert_eq!(
            SessionStatus::Error("test error".to_string()).to_string(),
            "Error: test error"
        );
    }

  ./crates/ccswarm/src/session/mod.rs:622-632 method get_auto_accept_sessions <-> ./crates/ccswarm/src/session/mod.rs:644-650 method list_sessions
  Similarity: 95.93%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:get_auto_accept_sessions (lines 622-632) ---[0m
    pub fn get_auto_accept_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.is_auto_accept_ready())
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:list_sessions (lines 644-650) ---[0m
    pub fn list_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions.values().cloned().collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:644-650 method list_sessions <-> ./crates/ccswarm/src/session/mod.rs:653-663 method list_active_sessions
  Similarity: 95.93%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:list_sessions (lines 644-650) ---[0m
    pub fn list_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions.values().cloned().collect()
        } else {
            Vec::new()
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:list_active_sessions (lines 653-663) ---[0m
    pub fn list_active_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.is_runnable())
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:644-650 method list_sessions <-> ./crates/ccswarm/src/session/mod.rs:666-676 method get_sessions_by_role
  Similarity: 95.88%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:list_sessions (lines 644-650) ---[0m
    pub fn list_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions.values().cloned().collect()
        } else {
            Vec::new()
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_sessions_by_role (lines 666-676) ---[0m
    pub fn get_sessions_by_role(&self, role: AgentRole) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.agent_role == role)
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:622-632 method get_auto_accept_sessions <-> ./crates/ccswarm/src/session/mod.rs:635-641 method get_session
  Similarity: 95.56%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:get_auto_accept_sessions (lines 622-632) ---[0m
    pub fn get_auto_accept_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.is_auto_accept_ready())
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_session (lines 635-641) ---[0m
    pub fn get_session(&self, session_id: &str) -> Option<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions.get(session_id).cloned()
        } else {
            None
        }
    }

  ./crates/ccswarm/src/session/mod.rs:635-641 method get_session <-> ./crates/ccswarm/src/session/mod.rs:653-663 method list_active_sessions
  Similarity: 95.56%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:get_session (lines 635-641) ---[0m
    pub fn get_session(&self, session_id: &str) -> Option<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions.get(session_id).cloned()
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:list_active_sessions (lines 653-663) ---[0m
    pub fn list_active_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.is_runnable())
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:635-641 method get_session <-> ./crates/ccswarm/src/session/mod.rs:666-676 method get_sessions_by_role
  Similarity: 95.50%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:get_session (lines 635-641) ---[0m
    pub fn get_session(&self, session_id: &str) -> Option<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions.get(session_id).cloned()
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_sessions_by_role (lines 666-676) ---[0m
    pub fn get_sessions_by_role(&self, role: AgentRole) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.agent_role == role)
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:237-243 method new <-> ./crates/ccswarm/src/session/mod.rs:773-784 method default
  Similarity: 89.41%
  Classes: SessionManager <-> Default

[36m--- ./crates/ccswarm/src/session/mod.rs:new (lines 237-243) ---[0m
    pub async fn new() -> SessionResult<Self> {
        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: None,
            resource_integration: None,
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:default (lines 773-784) ---[0m
    fn default() -> Self {
        // Note: This is a blocking call in an async context
        // Consider using lazy_static or once_cell for production
        tokio::task::block_in_place(|| {
            tokio::runtime::Handle::current().block_on(async {
                // Create with default resource monitoring enabled
                Self::with_resource_monitoring(crate::resource::ResourceLimits::default())
                    .await
                    .expect("Failed to create SessionManager")
            })
        })
    }

  ./crates/ccswarm/src/session/mod.rs:237-243 method new <-> ./crates/ccswarm/src/session/mod.rs:622-632 method get_auto_accept_sessions
  Similarity: 90.25%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:new (lines 237-243) ---[0m
    pub async fn new() -> SessionResult<Self> {
        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: None,
            resource_integration: None,
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_auto_accept_sessions (lines 622-632) ---[0m
    pub fn get_auto_accept_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.is_auto_accept_ready())
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:237-243 method new <-> ./crates/ccswarm/src/session/mod.rs:653-663 method list_active_sessions
  Similarity: 90.25%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:new (lines 237-243) ---[0m
    pub async fn new() -> SessionResult<Self> {
        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: None,
            resource_integration: None,
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:list_active_sessions (lines 653-663) ---[0m
    pub fn list_active_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.is_runnable())
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:237-243 method new <-> ./crates/ccswarm/src/session/mod.rs:666-676 method get_sessions_by_role
  Similarity: 90.12%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:new (lines 237-243) ---[0m
    pub async fn new() -> SessionResult<Self> {
        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: None,
            resource_integration: None,
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_sessions_by_role (lines 666-676) ---[0m
    pub fn get_sessions_by_role(&self, role: AgentRole) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions
                .values()
                .filter(|s| s.agent_role == role)
                .cloned()
                .collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:237-243 method new <-> ./crates/ccswarm/src/session/mod.rs:724-737 method get_session_resource_usage
  Similarity: 74.47%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:new (lines 237-243) ---[0m
    pub async fn new() -> SessionResult<Self> {
        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: None,
            resource_integration: None,
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_session_resource_usage (lines 724-737) ---[0m
    pub fn get_session_resource_usage(
        &self,
        session_id: &str,
    ) -> Option<crate::resource::ResourceUsage> {
        if let Ok(sessions) = self.sessions.lock() {
            let session = sessions.get(session_id)?;

            self.resource_monitor
                .as_ref()
                .and_then(|monitor| monitor.get_agent_usage(&session.agent_id))
        } else {
            None
        }
    }

  ./crates/ccswarm/src/session/mod.rs:773-784 method default <-> ./crates/ccswarm/src/session/mod.rs:835-843 function test_session_status_display
  Similarity: 70.21%

[36m--- ./crates/ccswarm/src/session/mod.rs:default (lines 773-784) ---[0m
    fn default() -> Self {
        // Note: This is a blocking call in an async context
        // Consider using lazy_static or once_cell for production
        tokio::task::block_in_place(|| {
            tokio::runtime::Handle::current().block_on(async {
                // Create with default resource monitoring enabled
                Self::with_resource_monitoring(crate::resource::ResourceLimits::default())
                    .await
                    .expect("Failed to create SessionManager")
            })
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:test_session_status_display (lines 835-843) ---[0m
    fn test_session_status_display() {
        assert_eq!(SessionStatus::Active.to_string(), "Active");
        assert_eq!(SessionStatus::Paused.to_string(), "Paused");
        assert_eq!(SessionStatus::Background.to_string(), "Background");
        assert_eq!(
            SessionStatus::Error("test error".to_string()).to_string(),
            "Error: test error"
        );
    }

  ./crates/ccswarm/src/session/mod.rs:635-641 method get_session <-> ./crates/ccswarm/src/session/mod.rs:773-784 method default
  Similarity: 77.41%
  Classes: SessionManager <-> Default

[36m--- ./crates/ccswarm/src/session/mod.rs:get_session (lines 635-641) ---[0m
    pub fn get_session(&self, session_id: &str) -> Option<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions.get(session_id).cloned()
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:default (lines 773-784) ---[0m
    fn default() -> Self {
        // Note: This is a blocking call in an async context
        // Consider using lazy_static or once_cell for production
        tokio::task::block_in_place(|| {
            tokio::runtime::Handle::current().block_on(async {
                // Create with default resource monitoring enabled
                Self::with_resource_monitoring(crate::resource::ResourceLimits::default())
                    .await
                    .expect("Failed to create SessionManager")
            })
        })
    }

  ./crates/ccswarm/src/session/mod.rs:644-650 method list_sessions <-> ./crates/ccswarm/src/session/mod.rs:773-784 method default
  Similarity: 77.41%
  Classes: SessionManager <-> Default

[36m--- ./crates/ccswarm/src/session/mod.rs:list_sessions (lines 644-650) ---[0m
    pub fn list_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions.values().cloned().collect()
        } else {
            Vec::new()
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:default (lines 773-784) ---[0m
    fn default() -> Self {
        // Note: This is a blocking call in an async context
        // Consider using lazy_static or once_cell for production
        tokio::task::block_in_place(|| {
            tokio::runtime::Handle::current().block_on(async {
                // Create with default resource monitoring enabled
                Self::with_resource_monitoring(crate::resource::ResourceLimits::default())
                    .await
                    .expect("Failed to create SessionManager")
            })
        })
    }

  ./crates/ccswarm/src/session/mod.rs:151-155 method enable_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:756-769 method start_agent_in_session
  Similarity: 76.33%
  Classes: AgentSession <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:enable_auto_accept (lines 151-155) ---[0m
    pub fn enable_auto_accept(&mut self, config: AutoAcceptConfig) {
        self.auto_accept = true;
        self.auto_accept_config = Some(config);
        self.touch();
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:start_agent_in_session (lines 756-769) ---[0m
    async fn start_agent_in_session(&self, session: &AgentSession) -> SessionResult<()> {
        // Build the command to start the agent
        let _command = format!(
            "ccswarm agent start --id {} --role {} --session {} --working-dir {}",
            session.agent_id,
            session.agent_role.name(),
            session.id,
            session.working_directory
        );

        // TODO: Send command to native session when available

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:158-162 method disable_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:756-769 method start_agent_in_session
  Similarity: 76.33%
  Classes: AgentSession <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:disable_auto_accept (lines 158-162) ---[0m
    pub fn disable_auto_accept(&mut self) {
        self.auto_accept = false;
        self.auto_accept_config = None;
        self.touch();
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:start_agent_in_session (lines 756-769) ---[0m
    async fn start_agent_in_session(&self, session: &AgentSession) -> SessionResult<()> {
        // Build the command to start the agent
        let _command = format!(
            "ccswarm agent start --id {} --role {} --session {} --working-dir {}",
            session.agent_id,
            session.agent_role.name(),
            session.id,
            session.working_directory
        );

        // TODO: Send command to native session when available

        Ok(())
    }

  ./crates/ccswarm/src/session/mod.rs:196-206 method add_episode <-> ./crates/ccswarm/src/session/mod.rs:237-243 method new
  Similarity: 74.90%
  Classes: AgentSession <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:add_episode (lines 196-206) ---[0m
    pub fn add_episode(
        &mut self,
        event_type: EpisodeType,
        description: String,
        context: HashMap<String, String>,
        outcome: EpisodeOutcome,
    ) {
        self.memory
            .add_episode(event_type, description, context, outcome);
        self.touch();
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:new (lines 237-243) ---[0m
    pub async fn new() -> SessionResult<Self> {
        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: None,
            resource_integration: None,
        })
    }

  ./crates/ccswarm/src/session/mod.rs:635-641 method get_session <-> ./crates/ccswarm/src/session/mod.rs:644-650 method list_sessions
  Similarity: 94.10%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:get_session (lines 635-641) ---[0m
    pub fn get_session(&self, session_id: &str) -> Option<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions.get(session_id).cloned()
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:list_sessions (lines 644-650) ---[0m
    pub fn list_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions.values().cloned().collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:237-243 method new <-> ./crates/ccswarm/src/session/mod.rs:740-746 method get_resource_efficiency_stats
  Similarity: 89.80%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:new (lines 237-243) ---[0m
    pub async fn new() -> SessionResult<Self> {
        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: None,
            resource_integration: None,
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_resource_efficiency_stats (lines 740-746) ---[0m
    pub fn get_resource_efficiency_stats(
        &self,
    ) -> Option<crate::resource::ResourceEfficiencyStats> {
        self.resource_monitor
            .as_ref()
            .map(|monitor| monitor.get_efficiency_stats())
    }

  ./crates/ccswarm/src/session/mod.rs:237-243 method new <-> ./crates/ccswarm/src/session/mod.rs:644-650 method list_sessions
  Similarity: 87.05%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:new (lines 237-243) ---[0m
    pub async fn new() -> SessionResult<Self> {
        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: None,
            resource_integration: None,
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:list_sessions (lines 644-650) ---[0m
    pub fn list_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions.values().cloned().collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:237-243 method new <-> ./crates/ccswarm/src/session/mod.rs:635-641 method get_session
  Similarity: 83.88%
  Classes: SessionManager <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:new (lines 237-243) ---[0m
    pub async fn new() -> SessionResult<Self> {
        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: None,
            resource_integration: None,
        })
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_session (lines 635-641) ---[0m
    pub fn get_session(&self, session_id: &str) -> Option<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions.get(session_id).cloned()
        } else {
            None
        }
    }

  ./crates/ccswarm/src/session/mod.rs:165-170 method update_auto_accept_config <-> ./crates/ccswarm/src/session/mod.rs:237-243 method new
  Similarity: 88.78%
  Classes: AgentSession <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:update_auto_accept_config (lines 165-170) ---[0m
    pub fn update_auto_accept_config(&mut self, config: AutoAcceptConfig) {
        if self.auto_accept {
            self.auto_accept_config = Some(config);
            self.touch();
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:new (lines 237-243) ---[0m
    pub async fn new() -> SessionResult<Self> {
        Ok(Self {
            sessions: Arc::new(Mutex::new(HashMap::new())),
            resource_monitor: None,
            resource_integration: None,
        })
    }

  ./crates/ccswarm/src/session/mod.rs:165-170 method update_auto_accept_config <-> ./crates/ccswarm/src/session/mod.rs:644-650 method list_sessions
  Similarity: 83.61%
  Classes: AgentSession <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:update_auto_accept_config (lines 165-170) ---[0m
    pub fn update_auto_accept_config(&mut self, config: AutoAcceptConfig) {
        if self.auto_accept {
            self.auto_accept_config = Some(config);
            self.touch();
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:list_sessions (lines 644-650) ---[0m
    pub fn list_sessions(&self) -> Vec<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions.values().cloned().collect()
        } else {
            Vec::new()
        }
    }

  ./crates/ccswarm/src/session/mod.rs:165-170 method update_auto_accept_config <-> ./crates/ccswarm/src/session/mod.rs:635-641 method get_session
  Similarity: 79.58%
  Classes: AgentSession <-> SessionManager

[36m--- ./crates/ccswarm/src/session/mod.rs:update_auto_accept_config (lines 165-170) ---[0m
    pub fn update_auto_accept_config(&mut self, config: AutoAcceptConfig) {
        if self.auto_accept {
            self.auto_accept_config = Some(config);
            self.touch();
        }
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:get_session (lines 635-641) ---[0m
    pub fn get_session(&self, session_id: &str) -> Option<AgentSession> {
        if let Ok(sessions) = self.sessions.lock() {
            sessions.get(session_id).cloned()
        } else {
            None
        }
    }

  ./crates/ccswarm/src/session/mod.rs:151-155 method enable_auto_accept <-> ./crates/ccswarm/src/session/mod.rs:158-162 method disable_auto_accept
  Similarity: 96.29%
  Classes: AgentSession <-> AgentSession

[36m--- ./crates/ccswarm/src/session/mod.rs:enable_auto_accept (lines 151-155) ---[0m
    pub fn enable_auto_accept(&mut self, config: AutoAcceptConfig) {
        self.auto_accept = true;
        self.auto_accept_config = Some(config);
        self.touch();
    }

[36m--- ./crates/ccswarm/src/session/mod.rs:disable_auto_accept (lines 158-162) ---[0m
    pub fn disable_auto_accept(&mut self) {
        self.auto_accept = false;
        self.auto_accept_config = None;
        self.touch();
    }


Duplicates in ./crates/ccswarm/src/cli/tutorial.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/cli/tutorial.rs:107-183 method chapter_1_basics <-> ./crates/ccswarm/src/cli/tutorial.rs:185-256 method chapter_2_agents
  Similarity: 86.57%
  Classes: InteractiveTutorial <-> InteractiveTutorial

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:chapter_1_basics (lines 107-183) ---[0m
    async fn chapter_1_basics(&mut self) -> Result<bool> {
        clear_screen();
        self.show_chapter_header(1, "Setting Up Your First Project")
            .await;

        // Step 1: Check environment
        self.show_step(1, "Environment Check").await;
        println!("First, let's make sure everything is set up correctly.");
        println!();

        self.simulate_command("ccswarm doctor").await?;

        println!("{}", "✅ Git: Installed".bright_green());
        sleep(Duration::from_millis(300)).await;
        println!("{}", "✅ Rust: 1.70+".bright_green());
        sleep(Duration::from_millis(300)).await;

        if std::env::var("ANTHROPIC_API_KEY").is_err() {
            println!("{}", "⚠️  API Key: Not set".bright_yellow());
            println!();
            println!(
                "{}",
                "You'll need an API key from Anthropic to use AI features.".yellow()
            );
            println!(
                "Get one at: {}",
                "https://console.anthropic.com".bright_cyan().underline()
            );
        } else {
            println!("{}", "✅ API Key: Set".bright_green());
        }

        println!();
        self.wait_for_enter("Got it! Press ENTER to continue...")
            .await?;

        // Step 2: Create project
        clear_screen();
        self.show_step(2, "Creating Your Project").await;
        println!("Let's create a simple TODO app project:");
        println!();

        self.simulate_command("ccswarm init --name TodoApp --agents frontend,backend")
            .await?;

        self.animate_progress("Initializing git repository", 1000)
            .await;
        self.animate_progress("Creating project structure", 800)
            .await;
        self.animate_progress("Configuring AI agents", 1200).await;

        println!();
        println!("{}", "✅ Project created successfully!".bright_green());
        println!();
        println!("Your project structure:");
        println!(
            "{}",
            "
TodoApp/
├── ccswarm.json      # Configuration file
├── agents/           # Agent workspaces
│   ├── frontend/     # Frontend agent workspace
│   └── backend/      # Backend agent workspace
└── .ccswarm/         # Session data
        "
            .bright_white()
        );

        self.completed_steps.push("project_created".to_string());
        println!();

        if self.ask_continue().await? {
            Ok(true)
        } else {
            Ok(false)
        }
    }

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:chapter_2_agents (lines 185-256) ---[0m
    async fn chapter_2_agents(&mut self) -> Result<bool> {
        clear_screen();
        self.show_chapter_header(2, "Working with AI Agents").await;

        // Understanding agents
        self.show_step(1, "Understanding Agents").await;
        println!("ccswarm uses specialized AI agents for different tasks:");
        println!();

        let agents = vec![
            ("🎨 Frontend Agent", "React, Vue, UI/UX, CSS", "frontend"),
            (
                "⚙️  Backend Agent",
                "APIs, databases, business logic",
                "backend",
            ),
            ("🚀 DevOps Agent", "Docker, CI/CD, deployment", "devops"),
            ("🧪 QA Agent", "Testing, quality assurance", "qa"),
        ];

        for (icon_name, desc, _) in &agents {
            println!("  {} - {}", icon_name.bright_yellow(), desc.white());
            sleep(Duration::from_millis(400)).await;
        }

        println!();
        self.wait_for_enter("Press ENTER to see agents in action...")
            .await?;

        // Starting agents
        clear_screen();
        self.show_step(2, "Starting the System").await;

        self.simulate_command("ccswarm start").await?;

        self.animate_progress("Starting Master Claude", 1500).await;
        println!(
            "{}",
            "  🧠 Master Claude: Technical lead coordinating agents".dimmed()
        );
        sleep(Duration::from_millis(500)).await;

        self.animate_progress("Starting Frontend Agent", 1000).await;
        self.animate_progress("Starting Backend Agent", 1000).await;

        println!();
        println!("{}", "✅ All systems operational!".bright_green());
        println!();

        // Show agent status
        self.simulate_command("ccswarm agent list").await?;

        println!("{}", "AGENT      STATUS    TASKS  SESSION".bright_white());
        println!("{}", "─────────────────────────────────────".dimmed());
        println!(
            "frontend   {}      0     ai-session-a1b2c3",
            "Active".bright_green()
        );
        println!(
            "backend    {}       0     ai-session-d4e5f6",
            "Active".bright_green()
        );

        self.completed_steps.push("agents_started".to_string());
        println!();

        if self.ask_continue().await? {
            Ok(true)
        } else {
            Ok(false)
        }
    }

  ./crates/ccswarm/src/cli/tutorial.rs:107-183 method chapter_1_basics <-> ./crates/ccswarm/src/cli/tutorial.rs:258-342 method chapter_3_tasks
  Similarity: 77.17%
  Classes: InteractiveTutorial <-> InteractiveTutorial

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:chapter_1_basics (lines 107-183) ---[0m
    async fn chapter_1_basics(&mut self) -> Result<bool> {
        clear_screen();
        self.show_chapter_header(1, "Setting Up Your First Project")
            .await;

        // Step 1: Check environment
        self.show_step(1, "Environment Check").await;
        println!("First, let's make sure everything is set up correctly.");
        println!();

        self.simulate_command("ccswarm doctor").await?;

        println!("{}", "✅ Git: Installed".bright_green());
        sleep(Duration::from_millis(300)).await;
        println!("{}", "✅ Rust: 1.70+".bright_green());
        sleep(Duration::from_millis(300)).await;

        if std::env::var("ANTHROPIC_API_KEY").is_err() {
            println!("{}", "⚠️  API Key: Not set".bright_yellow());
            println!();
            println!(
                "{}",
                "You'll need an API key from Anthropic to use AI features.".yellow()
            );
            println!(
                "Get one at: {}",
                "https://console.anthropic.com".bright_cyan().underline()
            );
        } else {
            println!("{}", "✅ API Key: Set".bright_green());
        }

        println!();
        self.wait_for_enter("Got it! Press ENTER to continue...")
            .await?;

        // Step 2: Create project
        clear_screen();
        self.show_step(2, "Creating Your Project").await;
        println!("Let's create a simple TODO app project:");
        println!();

        self.simulate_command("ccswarm init --name TodoApp --agents frontend,backend")
            .await?;

        self.animate_progress("Initializing git repository", 1000)
            .await;
        self.animate_progress("Creating project structure", 800)
            .await;
        self.animate_progress("Configuring AI agents", 1200).await;

        println!();
        println!("{}", "✅ Project created successfully!".bright_green());
        println!();
        println!("Your project structure:");
        println!(
            "{}",
            "
TodoApp/
├── ccswarm.json      # Configuration file
├── agents/           # Agent workspaces
│   ├── frontend/     # Frontend agent workspace
│   └── backend/      # Backend agent workspace
└── .ccswarm/         # Session data
        "
            .bright_white()
        );

        self.completed_steps.push("project_created".to_string());
        println!();

        if self.ask_continue().await? {
            Ok(true)
        } else {
            Ok(false)
        }
    }

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:chapter_3_tasks (lines 258-342) ---[0m
    async fn chapter_3_tasks(&mut self) -> Result<bool> {
        clear_screen();
        self.show_chapter_header(3, "Creating and Managing Tasks")
            .await;

        // Creating first task
        self.show_step(1, "Your First Task").await;
        println!("Let's create a task for our TODO app:");
        println!();

        self.simulate_command("ccswarm task \"Create a React component for todo items\"")
            .await?;

        self.animate_progress("Analyzing task", 800).await;
        self.animate_progress("Determining best agent", 600).await;
        println!(
            "{}",
            "  → Frontend Agent selected (React expertise)".bright_cyan()
        );
        self.animate_progress("Delegating to Frontend Agent", 1000)
            .await;

        println!();
        println!("{}", "✅ Task delegated successfully!".bright_green());
        println!("   Task ID: task-7f8g9h0i");
        println!();

        // Monitoring progress
        sleep(Duration::from_secs(1)).await;
        println!(
            "{}",
            "The Frontend Agent is now working on your task...".dimmed()
        );
        println!();

        self.simulate_typing("📝 Frontend Agent: Creating TodoItem.jsx component...")
            .await;
        sleep(Duration::from_millis(800)).await;
        self.simulate_typing("📝 Frontend Agent: Adding prop types and styling...")
            .await;
        sleep(Duration::from_millis(800)).await;
        self.simulate_typing("✅ Frontend Agent: Component created with tests!")
            .await;

        println!();
        println!();

        // Task modifiers
        self.show_step(2, "Task Modifiers").await;
        println!("You can add modifiers to your tasks:");
        println!();

        println!("  {} Priority modifiers", "•".bright_cyan());
        println!(
            "    ccswarm task \"Fix login bug\" {}",
            "[high]".bright_red()
        );
        println!();

        println!("  {} Type modifiers", "•".bright_cyan());
        println!(
            "    ccswarm task \"Add user auth\" {}",
            "[feature]".bright_green()
        );
        println!(
            "    ccswarm task \"Fix memory leak\" {}",
            "[bug]".bright_yellow()
        );
        println!();

        println!("  {} Direct delegation", "•".bright_cyan());
        println!(
            "    ccswarm task \"Setup CI/CD\" {}",
            "--agent devops".bright_white()
        );

        self.completed_steps.push("task_created".to_string());
        println!();

        if self.ask_continue().await? {
            Ok(true)
        } else {
            Ok(false)
        }
    }

  ./crates/ccswarm/src/cli/tutorial.rs:185-256 method chapter_2_agents <-> ./crates/ccswarm/src/cli/tutorial.rs:258-342 method chapter_3_tasks
  Similarity: 76.73%
  Classes: InteractiveTutorial <-> InteractiveTutorial

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:chapter_2_agents (lines 185-256) ---[0m
    async fn chapter_2_agents(&mut self) -> Result<bool> {
        clear_screen();
        self.show_chapter_header(2, "Working with AI Agents").await;

        // Understanding agents
        self.show_step(1, "Understanding Agents").await;
        println!("ccswarm uses specialized AI agents for different tasks:");
        println!();

        let agents = vec![
            ("🎨 Frontend Agent", "React, Vue, UI/UX, CSS", "frontend"),
            (
                "⚙️  Backend Agent",
                "APIs, databases, business logic",
                "backend",
            ),
            ("🚀 DevOps Agent", "Docker, CI/CD, deployment", "devops"),
            ("🧪 QA Agent", "Testing, quality assurance", "qa"),
        ];

        for (icon_name, desc, _) in &agents {
            println!("  {} - {}", icon_name.bright_yellow(), desc.white());
            sleep(Duration::from_millis(400)).await;
        }

        println!();
        self.wait_for_enter("Press ENTER to see agents in action...")
            .await?;

        // Starting agents
        clear_screen();
        self.show_step(2, "Starting the System").await;

        self.simulate_command("ccswarm start").await?;

        self.animate_progress("Starting Master Claude", 1500).await;
        println!(
            "{}",
            "  🧠 Master Claude: Technical lead coordinating agents".dimmed()
        );
        sleep(Duration::from_millis(500)).await;

        self.animate_progress("Starting Frontend Agent", 1000).await;
        self.animate_progress("Starting Backend Agent", 1000).await;

        println!();
        println!("{}", "✅ All systems operational!".bright_green());
        println!();

        // Show agent status
        self.simulate_command("ccswarm agent list").await?;

        println!("{}", "AGENT      STATUS    TASKS  SESSION".bright_white());
        println!("{}", "─────────────────────────────────────".dimmed());
        println!(
            "frontend   {}      0     ai-session-a1b2c3",
            "Active".bright_green()
        );
        println!(
            "backend    {}       0     ai-session-d4e5f6",
            "Active".bright_green()
        );

        self.completed_steps.push("agents_started".to_string());
        println!();

        if self.ask_continue().await? {
            Ok(true)
        } else {
            Ok(false)
        }
    }

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:chapter_3_tasks (lines 258-342) ---[0m
    async fn chapter_3_tasks(&mut self) -> Result<bool> {
        clear_screen();
        self.show_chapter_header(3, "Creating and Managing Tasks")
            .await;

        // Creating first task
        self.show_step(1, "Your First Task").await;
        println!("Let's create a task for our TODO app:");
        println!();

        self.simulate_command("ccswarm task \"Create a React component for todo items\"")
            .await?;

        self.animate_progress("Analyzing task", 800).await;
        self.animate_progress("Determining best agent", 600).await;
        println!(
            "{}",
            "  → Frontend Agent selected (React expertise)".bright_cyan()
        );
        self.animate_progress("Delegating to Frontend Agent", 1000)
            .await;

        println!();
        println!("{}", "✅ Task delegated successfully!".bright_green());
        println!("   Task ID: task-7f8g9h0i");
        println!();

        // Monitoring progress
        sleep(Duration::from_secs(1)).await;
        println!(
            "{}",
            "The Frontend Agent is now working on your task...".dimmed()
        );
        println!();

        self.simulate_typing("📝 Frontend Agent: Creating TodoItem.jsx component...")
            .await;
        sleep(Duration::from_millis(800)).await;
        self.simulate_typing("📝 Frontend Agent: Adding prop types and styling...")
            .await;
        sleep(Duration::from_millis(800)).await;
        self.simulate_typing("✅ Frontend Agent: Component created with tests!")
            .await;

        println!();
        println!();

        // Task modifiers
        self.show_step(2, "Task Modifiers").await;
        println!("You can add modifiers to your tasks:");
        println!();

        println!("  {} Priority modifiers", "•".bright_cyan());
        println!(
            "    ccswarm task \"Fix login bug\" {}",
            "[high]".bright_red()
        );
        println!();

        println!("  {} Type modifiers", "•".bright_cyan());
        println!(
            "    ccswarm task \"Add user auth\" {}",
            "[feature]".bright_green()
        );
        println!(
            "    ccswarm task \"Fix memory leak\" {}",
            "[bug]".bright_yellow()
        );
        println!();

        println!("  {} Direct delegation", "•".bright_cyan());
        println!(
            "    ccswarm task \"Setup CI/CD\" {}",
            "--agent devops".bright_white()
        );

        self.completed_steps.push("task_created".to_string());
        println!();

        if self.ask_continue().await? {
            Ok(true)
        } else {
            Ok(false)
        }
    }

  ./crates/ccswarm/src/cli/tutorial.rs:107-183 method chapter_1_basics <-> ./crates/ccswarm/src/cli/tutorial.rs:344-399 method show_completion
  Similarity: 70.07%
  Classes: InteractiveTutorial <-> InteractiveTutorial

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:chapter_1_basics (lines 107-183) ---[0m
    async fn chapter_1_basics(&mut self) -> Result<bool> {
        clear_screen();
        self.show_chapter_header(1, "Setting Up Your First Project")
            .await;

        // Step 1: Check environment
        self.show_step(1, "Environment Check").await;
        println!("First, let's make sure everything is set up correctly.");
        println!();

        self.simulate_command("ccswarm doctor").await?;

        println!("{}", "✅ Git: Installed".bright_green());
        sleep(Duration::from_millis(300)).await;
        println!("{}", "✅ Rust: 1.70+".bright_green());
        sleep(Duration::from_millis(300)).await;

        if std::env::var("ANTHROPIC_API_KEY").is_err() {
            println!("{}", "⚠️  API Key: Not set".bright_yellow());
            println!();
            println!(
                "{}",
                "You'll need an API key from Anthropic to use AI features.".yellow()
            );
            println!(
                "Get one at: {}",
                "https://console.anthropic.com".bright_cyan().underline()
            );
        } else {
            println!("{}", "✅ API Key: Set".bright_green());
        }

        println!();
        self.wait_for_enter("Got it! Press ENTER to continue...")
            .await?;

        // Step 2: Create project
        clear_screen();
        self.show_step(2, "Creating Your Project").await;
        println!("Let's create a simple TODO app project:");
        println!();

        self.simulate_command("ccswarm init --name TodoApp --agents frontend,backend")
            .await?;

        self.animate_progress("Initializing git repository", 1000)
            .await;
        self.animate_progress("Creating project structure", 800)
            .await;
        self.animate_progress("Configuring AI agents", 1200).await;

        println!();
        println!("{}", "✅ Project created successfully!".bright_green());
        println!();
        println!("Your project structure:");
        println!(
            "{}",
            "
TodoApp/
├── ccswarm.json      # Configuration file
├── agents/           # Agent workspaces
│   ├── frontend/     # Frontend agent workspace
│   └── backend/      # Backend agent workspace
└── .ccswarm/         # Session data
        "
            .bright_white()
        );

        self.completed_steps.push("project_created".to_string());
        println!();

        if self.ask_continue().await? {
            Ok(true)
        } else {
            Ok(false)
        }
    }

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:show_completion (lines 344-399) ---[0m
    async fn show_completion(&self) -> Result<()> {
        clear_screen();

        println!(
            "{}",
            "
╔═══════════════════════════════════════════════════════════════╗
║                                                               ║
║              🎉 Congratulations! 🎉                           ║
║                                                               ║
║         You've completed the ccswarm tutorial!                ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝
        "
            .bright_green()
        );

        println!();
        println!("{}", "You've learned:".bright_white());
        println!();

        for step in &[
            "✓ Setting up a project",
            "✓ Working with AI agents",
            "✓ Creating and managing tasks",
        ] {
            println!("  {}", step.bright_green());
            sleep(Duration::from_millis(300)).await;
        }

        println!();
        println!("{}", "📚 What's Next?".bright_cyan());
        println!();
        println!(
            "  • Explore quality checks: {}",
            "ccswarm help quality".bright_white()
        );
        println!(
            "  • Learn about sessions: {}",
            "ccswarm help sessions".bright_white()
        );
        println!(
            "  • Try auto-create: {}",
            "ccswarm auto-create \"TODO app\"".bright_white()
        );
        println!(
            "  • Join our community: {}",
            "https://discord.gg/ccswarm".bright_cyan().underline()
        );

        println!();
        println!("{}", "Happy orchestrating! 🚀".bright_yellow());
        println!();

        Ok(())
    }

  ./crates/ccswarm/src/cli/tutorial.rs:63-105 method show_welcome <-> ./crates/ccswarm/src/cli/tutorial.rs:344-399 method show_completion
  Similarity: 84.55%
  Classes: InteractiveTutorial <-> InteractiveTutorial

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:show_welcome (lines 63-105) ---[0m
    async fn show_welcome(&self) -> Result<()> {
        clear_screen();

        println!(
            "{}",
            "
╔═══════════════════════════════════════════════════════════════╗
║                                                               ║
║              🚀 Welcome to ccswarm Tutorial! 🚀              ║
║                                                               ║
║         AI-Powered Multi-Agent Orchestration System           ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝
        "
            .bright_cyan()
        );

        println!();
        println!(
            "{}",
            "This interactive tutorial will teach you:".bright_white()
        );
        println!();
        println!(
            "  📚 Chapter 1: {}",
            "Setting Up Your First Project".bright_yellow()
        );
        println!(
            "  🤖 Chapter 2: {}",
            "Working with AI Agents".bright_yellow()
        );
        println!(
            "  📝 Chapter 3: {}",
            "Creating and Managing Tasks".bright_yellow()
        );
        println!("  🎯 Chapter 4: {}", "Advanced Features".bright_yellow());
        println!();
        println!("{}", "Each chapter takes ~3-5 minutes.".dimmed());
        println!();

        self.wait_for_enter("Press ENTER to begin...").await?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:show_completion (lines 344-399) ---[0m
    async fn show_completion(&self) -> Result<()> {
        clear_screen();

        println!(
            "{}",
            "
╔═══════════════════════════════════════════════════════════════╗
║                                                               ║
║              🎉 Congratulations! 🎉                           ║
║                                                               ║
║         You've completed the ccswarm tutorial!                ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝
        "
            .bright_green()
        );

        println!();
        println!("{}", "You've learned:".bright_white());
        println!();

        for step in &[
            "✓ Setting up a project",
            "✓ Working with AI agents",
            "✓ Creating and managing tasks",
        ] {
            println!("  {}", step.bright_green());
            sleep(Duration::from_millis(300)).await;
        }

        println!();
        println!("{}", "📚 What's Next?".bright_cyan());
        println!();
        println!(
            "  • Explore quality checks: {}",
            "ccswarm help quality".bright_white()
        );
        println!(
            "  • Learn about sessions: {}",
            "ccswarm help sessions".bright_white()
        );
        println!(
            "  • Try auto-create: {}",
            "ccswarm auto-create \"TODO app\"".bright_white()
        );
        println!(
            "  • Join our community: {}",
            "https://discord.gg/ccswarm".bright_cyan().underline()
        );

        println!();
        println!("{}", "Happy orchestrating! 🚀".bright_yellow());
        println!();

        Ok(())
    }

  ./crates/ccswarm/src/cli/tutorial.rs:26-61 method start <-> ./crates/ccswarm/src/cli/tutorial.rs:486-502 function show_contextual_tip
  Similarity: 72.93%

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:start (lines 26-61) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        self.show_welcome().await?;

        loop {
            match self.current_chapter {
                0 => {
                    if self.chapter_1_basics().await? {
                        self.current_chapter += 1;
                    } else {
                        break;
                    }
                }
                1 => {
                    if self.chapter_2_agents().await? {
                        self.current_chapter += 1;
                    } else {
                        break;
                    }
                }
                2 => {
                    if self.chapter_3_tasks().await? {
                        self.current_chapter += 1;
                    } else {
                        break;
                    }
                }
                3 => {
                    self.show_completion().await?;
                    break;
                }
                _ => break,
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:show_contextual_tip (lines 486-502) ---[0m
pub fn show_contextual_tip() {
    let tips = [
        "💡 Tip: Use 'ccswarm task list' to see all pending tasks",
        "💡 Tip: Sessions provide efficient context management through smart caching",
        "💡 Tip: Add [high] to task descriptions for priority handling",
        "💡 Tip: Quality checks run automatically every 30 seconds",
        "💡 Tip: Use 'ccswarm tui' for real-time monitoring",
        "💡 Tip: Agents work in isolated git worktrees for safety",
        "💡 Tip: Failed tasks are automatically retried with fixes",
    ];

    use rand::prelude::*;
    let mut rng = rand::rngs::ThreadRng::default();
    let tip = tips.choose(&mut rng).unwrap_or(&tips[0]);
    println!();
    println!("{}", tip.dimmed());
}

  ./crates/ccswarm/src/cli/tutorial.rs:439-452 method animate_progress <-> ./crates/ccswarm/src/cli/tutorial.rs:486-502 function show_contextual_tip
  Similarity: 80.40%

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:animate_progress (lines 439-452) ---[0m
    async fn animate_progress(&self, message: &str, duration_ms: u64) {
        print!("  {} ", "⏳".bright_yellow());
        print!("{}", message);
        io::stdout().flush().unwrap_or_else(|e| {
            eprintln!("Failed to flush stdout: {}", e);
        });

        sleep(Duration::from_millis(duration_ms)).await;

        print!("\r  {} {}\n", "✅".bright_green(), message);
        io::stdout().flush().unwrap_or_else(|e| {
            eprintln!("Failed to flush stdout: {}", e);
        });
    }

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:show_contextual_tip (lines 486-502) ---[0m
pub fn show_contextual_tip() {
    let tips = [
        "💡 Tip: Use 'ccswarm task list' to see all pending tasks",
        "💡 Tip: Sessions provide efficient context management through smart caching",
        "💡 Tip: Add [high] to task descriptions for priority handling",
        "💡 Tip: Quality checks run automatically every 30 seconds",
        "💡 Tip: Use 'ccswarm tui' for real-time monitoring",
        "💡 Tip: Agents work in isolated git worktrees for safety",
        "💡 Tip: Failed tasks are automatically retried with fixes",
    ];

    use rand::prelude::*;
    let mut rng = rand::rngs::ThreadRng::default();
    let tip = tips.choose(&mut rng).unwrap_or(&tips[0]);
    println!();
    println!("{}", tip.dimmed());
}

  ./crates/ccswarm/src/cli/tutorial.rs:464-474 method ask_continue <-> ./crates/ccswarm/src/cli/tutorial.rs:486-502 function show_contextual_tip
  Similarity: 77.46%

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:ask_continue (lines 464-474) ---[0m
    async fn ask_continue(&self) -> Result<bool> {
        println!();
        print!("{}", "Continue to next chapter? [Y/n] ".bright_cyan());
        io::stdout().flush()?;

        let mut input = String::new();
        io::stdin().read_line(&mut input)?;
        let input = input.trim().to_lowercase();

        Ok(input.is_empty() || input == "y" || input == "yes")
    }

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:show_contextual_tip (lines 486-502) ---[0m
pub fn show_contextual_tip() {
    let tips = [
        "💡 Tip: Use 'ccswarm task list' to see all pending tasks",
        "💡 Tip: Sessions provide efficient context management through smart caching",
        "💡 Tip: Add [high] to task descriptions for priority handling",
        "💡 Tip: Quality checks run automatically every 30 seconds",
        "💡 Tip: Use 'ccswarm tui' for real-time monitoring",
        "💡 Tip: Agents work in isolated git worktrees for safety",
        "💡 Tip: Failed tasks are automatically retried with fixes",
    ];

    use rand::prelude::*;
    let mut rng = rand::rngs::ThreadRng::default();
    let tip = tips.choose(&mut rng).unwrap_or(&tips[0]);
    println!();
    println!("{}", tip.dimmed());
}

  ./crates/ccswarm/src/cli/tutorial.rs:454-462 method wait_for_enter <-> ./crates/ccswarm/src/cli/tutorial.rs:486-502 function show_contextual_tip
  Similarity: 81.74%

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:wait_for_enter (lines 454-462) ---[0m
    async fn wait_for_enter(&self, prompt: &str) -> Result<()> {
        println!();
        print!("{}", prompt.bright_cyan());
        io::stdout().flush()?;

        let mut input = String::new();
        io::stdin().read_line(&mut input)?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:show_contextual_tip (lines 486-502) ---[0m
pub fn show_contextual_tip() {
    let tips = [
        "💡 Tip: Use 'ccswarm task list' to see all pending tasks",
        "💡 Tip: Sessions provide efficient context management through smart caching",
        "💡 Tip: Add [high] to task descriptions for priority handling",
        "💡 Tip: Quality checks run automatically every 30 seconds",
        "💡 Tip: Use 'ccswarm tui' for real-time monitoring",
        "💡 Tip: Agents work in isolated git worktrees for safety",
        "💡 Tip: Failed tasks are automatically retried with fixes",
    ];

    use rand::prelude::*;
    let mut rng = rand::rngs::ThreadRng::default();
    let tip = tips.choose(&mut rng).unwrap_or(&tips[0]);
    println!();
    println!("{}", tip.dimmed());
}

  ./crates/ccswarm/src/cli/tutorial.rs:439-452 method animate_progress <-> ./crates/ccswarm/src/cli/tutorial.rs:464-474 method ask_continue
  Similarity: 81.61%
  Classes: InteractiveTutorial <-> InteractiveTutorial

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:animate_progress (lines 439-452) ---[0m
    async fn animate_progress(&self, message: &str, duration_ms: u64) {
        print!("  {} ", "⏳".bright_yellow());
        print!("{}", message);
        io::stdout().flush().unwrap_or_else(|e| {
            eprintln!("Failed to flush stdout: {}", e);
        });

        sleep(Duration::from_millis(duration_ms)).await;

        print!("\r  {} {}\n", "✅".bright_green(), message);
        io::stdout().flush().unwrap_or_else(|e| {
            eprintln!("Failed to flush stdout: {}", e);
        });
    }

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:ask_continue (lines 464-474) ---[0m
    async fn ask_continue(&self) -> Result<bool> {
        println!();
        print!("{}", "Continue to next chapter? [Y/n] ".bright_cyan());
        io::stdout().flush()?;

        let mut input = String::new();
        io::stdin().read_line(&mut input)?;
        let input = input.trim().to_lowercase();

        Ok(input.is_empty() || input == "y" || input == "yes")
    }

  ./crates/ccswarm/src/cli/tutorial.rs:439-452 method animate_progress <-> ./crates/ccswarm/src/cli/tutorial.rs:454-462 method wait_for_enter
  Similarity: 82.36%
  Classes: InteractiveTutorial <-> InteractiveTutorial

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:animate_progress (lines 439-452) ---[0m
    async fn animate_progress(&self, message: &str, duration_ms: u64) {
        print!("  {} ", "⏳".bright_yellow());
        print!("{}", message);
        io::stdout().flush().unwrap_or_else(|e| {
            eprintln!("Failed to flush stdout: {}", e);
        });

        sleep(Duration::from_millis(duration_ms)).await;

        print!("\r  {} {}\n", "✅".bright_green(), message);
        io::stdout().flush().unwrap_or_else(|e| {
            eprintln!("Failed to flush stdout: {}", e);
        });
    }

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:wait_for_enter (lines 454-462) ---[0m
    async fn wait_for_enter(&self, prompt: &str) -> Result<()> {
        println!();
        print!("{}", prompt.bright_cyan());
        io::stdout().flush()?;

        let mut input = String::new();
        io::stdin().read_line(&mut input)?;
        Ok(())
    }

  ./crates/ccswarm/src/cli/tutorial.rs:421-427 method simulate_command <-> ./crates/ccswarm/src/cli/tutorial.rs:486-502 function show_contextual_tip
  Similarity: 76.89%

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:simulate_command (lines 421-427) ---[0m
    async fn simulate_command(&self, cmd: &str) -> Result<()> {
        print!("{} ", "$".bright_green());
        self.simulate_typing(cmd).await;
        println!();
        sleep(Duration::from_millis(500)).await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:show_contextual_tip (lines 486-502) ---[0m
pub fn show_contextual_tip() {
    let tips = [
        "💡 Tip: Use 'ccswarm task list' to see all pending tasks",
        "💡 Tip: Sessions provide efficient context management through smart caching",
        "💡 Tip: Add [high] to task descriptions for priority handling",
        "💡 Tip: Quality checks run automatically every 30 seconds",
        "💡 Tip: Use 'ccswarm tui' for real-time monitoring",
        "💡 Tip: Agents work in isolated git worktrees for safety",
        "💡 Tip: Failed tasks are automatically retried with fixes",
    ];

    use rand::prelude::*;
    let mut rng = rand::rngs::ThreadRng::default();
    let tip = tips.choose(&mut rng).unwrap_or(&tips[0]);
    println!();
    println!("{}", tip.dimmed());
}

  ./crates/ccswarm/src/cli/tutorial.rs:454-462 method wait_for_enter <-> ./crates/ccswarm/src/cli/tutorial.rs:464-474 method ask_continue
  Similarity: 82.61%
  Classes: InteractiveTutorial <-> InteractiveTutorial

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:wait_for_enter (lines 454-462) ---[0m
    async fn wait_for_enter(&self, prompt: &str) -> Result<()> {
        println!();
        print!("{}", prompt.bright_cyan());
        io::stdout().flush()?;

        let mut input = String::new();
        io::stdin().read_line(&mut input)?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:ask_continue (lines 464-474) ---[0m
    async fn ask_continue(&self) -> Result<bool> {
        println!();
        print!("{}", "Continue to next chapter? [Y/n] ".bright_cyan());
        io::stdout().flush()?;

        let mut input = String::new();
        io::stdin().read_line(&mut input)?;
        let input = input.trim().to_lowercase();

        Ok(input.is_empty() || input == "y" || input == "yes")
    }

  ./crates/ccswarm/src/cli/tutorial.rs:402-409 method show_chapter_header <-> ./crates/ccswarm/src/cli/tutorial.rs:411-419 method show_step
  Similarity: 80.00%
  Classes: InteractiveTutorial <-> InteractiveTutorial

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:show_chapter_header (lines 402-409) ---[0m
    async fn show_chapter_header(&self, num: u8, title: &str) {
        println!(
            "{}",
            format!("Chapter {} : {}", num, title).bright_cyan().bold()
        );
        println!("{}", "═".repeat(50).bright_cyan());
        println!();
    }

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:show_step (lines 411-419) ---[0m
    async fn show_step(&self, num: u8, title: &str) {
        println!();
        println!(
            "{} {}",
            format!("Step {}:", num).bright_yellow(),
            title.bright_white().bold()
        );
        println!();
    }

  ./crates/ccswarm/src/cli/tutorial.rs:421-427 method simulate_command <-> ./crates/ccswarm/src/cli/tutorial.rs:464-474 method ask_continue
  Similarity: 72.17%
  Classes: InteractiveTutorial <-> InteractiveTutorial

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:simulate_command (lines 421-427) ---[0m
    async fn simulate_command(&self, cmd: &str) -> Result<()> {
        print!("{} ", "$".bright_green());
        self.simulate_typing(cmd).await;
        println!();
        sleep(Duration::from_millis(500)).await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:ask_continue (lines 464-474) ---[0m
    async fn ask_continue(&self) -> Result<bool> {
        println!();
        print!("{}", "Continue to next chapter? [Y/n] ".bright_cyan());
        io::stdout().flush()?;

        let mut input = String::new();
        io::stdin().read_line(&mut input)?;
        let input = input.trim().to_lowercase();

        Ok(input.is_empty() || input == "y" || input == "yes")
    }

  ./crates/ccswarm/src/cli/tutorial.rs:421-427 method simulate_command <-> ./crates/ccswarm/src/cli/tutorial.rs:454-462 method wait_for_enter
  Similarity: 72.07%
  Classes: InteractiveTutorial <-> InteractiveTutorial

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:simulate_command (lines 421-427) ---[0m
    async fn simulate_command(&self, cmd: &str) -> Result<()> {
        print!("{} ", "$".bright_green());
        self.simulate_typing(cmd).await;
        println!();
        sleep(Duration::from_millis(500)).await;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/cli/tutorial.rs:wait_for_enter (lines 454-462) ---[0m
    async fn wait_for_enter(&self, prompt: &str) -> Result<()> {
        println!();
        print!("{}", prompt.bright_cyan());
        io::stdout().flush()?;

        let mut input = String::new();
        io::stdin().read_line(&mut input)?;
        Ok(())
    }


Duplicates in ./crates/ccswarm/src/resource/tests.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/resource/tests.rs:110-141 function test_is_exceeding_limits <-> ./crates/ccswarm/src/resource/tests.rs:168-196 function test_limit_violations
  Similarity: 72.89%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_is_exceeding_limits (lines 110-141) ---[0m
    fn test_is_exceeding_limits() {
        let mut limits = ResourceLimits::default();
        limits.max_cpu_percent = 50.0;
        limits.max_memory_bytes = 1024 * 1024 * 1024; // 1GB
        limits.max_memory_percent = 25.0;
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Not exceeding
        state.current_usage = ResourceUsage {
            cpu_percent: 30.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.0,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        assert!(!state.is_exceeding_limits());
        
        // Exceeding CPU
        state.current_usage.cpu_percent = 60.0;
        assert!(state.is_exceeding_limits());
        
        // Exceeding memory bytes
        state.current_usage.cpu_percent = 30.0;
        state.current_usage.memory_bytes = 2 * 1024 * 1024 * 1024;
        assert!(state.is_exceeding_limits());
        
        // Exceeding memory percent
        state.current_usage.memory_bytes = 512 * 1024 * 1024;
        state.current_usage.memory_percent = 30.0;
        assert!(state.is_exceeding_limits());
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_limit_violations (lines 168-196) ---[0m
    fn test_limit_violations() {
        let mut limits = ResourceLimits::default();
        limits.max_cpu_percent = 50.0;
        limits.enforce_limits = true;
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Normal usage
        let normal_usage = ResourceUsage {
            cpu_percent: 30.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(normal_usage);
        assert_eq!(state.limit_violations, 0);
        
        // Exceeding usage
        let high_usage = ResourceUsage {
            cpu_percent: 75.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(high_usage);
        assert_eq!(state.limit_violations, 1);
    }

  ./crates/ccswarm/src/resource/tests.rs:168-196 function test_limit_violations <-> ./crates/ccswarm/src/resource/tests.rs:218-241 function test_resource_monitor_events
  Similarity: 80.66%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_limit_violations (lines 168-196) ---[0m
    fn test_limit_violations() {
        let mut limits = ResourceLimits::default();
        limits.max_cpu_percent = 50.0;
        limits.enforce_limits = true;
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Normal usage
        let normal_usage = ResourceUsage {
            cpu_percent: 30.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(normal_usage);
        assert_eq!(state.limit_violations, 0);
        
        // Exceeding usage
        let high_usage = ResourceUsage {
            cpu_percent: 75.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(high_usage);
        assert_eq!(state.limit_violations, 1);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_events (lines 218-241) ---[0m
    async fn test_resource_monitor_events() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Subscribe to events
        let mut event_rx = monitor.subscribe();
        
        // Start monitoring should emit event
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check event
        let event = tokio::time::timeout(
            tokio::time::Duration::from_millis(100),
            event_rx.recv()
        ).await;
        
        assert!(event.is_ok());
        if let Ok(Ok(ResourceEvent::MonitoringStarted { agent_id, pid })) = event {
            assert_eq!(agent_id, "test-agent");
            assert_eq!(pid, Some(1234));
        } else {
            panic!("Expected MonitoringStarted event");
        }
    }

  ./crates/ccswarm/src/resource/tests.rs:144-165 function test_get_average_usage <-> ./crates/ccswarm/src/resource/tests.rs:168-196 function test_limit_violations
  Similarity: 81.33%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_get_average_usage (lines 144-165) ---[0m
    fn test_get_average_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add some usage entries
        for i in 1..=5 {
            let usage = ResourceUsage {
                cpu_percent: i as f32 * 10.0,
                memory_bytes: i as u64 * 100 * 1024 * 1024,
                memory_percent: i as f32 * 5.0,
                thread_count: i,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        let avg = state.get_average_usage();
        assert_eq!(avg.cpu_percent, 30.0); // (10+20+30+40+50)/5
        assert_eq!(avg.memory_bytes, 300 * 1024 * 1024); // Average of 100-500MB
        assert_eq!(avg.memory_percent, 15.0); // (5+10+15+20+25)/5
        assert_eq!(avg.thread_count, 3); // (1+2+3+4+5)/5
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_limit_violations (lines 168-196) ---[0m
    fn test_limit_violations() {
        let mut limits = ResourceLimits::default();
        limits.max_cpu_percent = 50.0;
        limits.enforce_limits = true;
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Normal usage
        let normal_usage = ResourceUsage {
            cpu_percent: 30.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(normal_usage);
        assert_eq!(state.limit_violations, 0);
        
        // Exceeding usage
        let high_usage = ResourceUsage {
            cpu_percent: 75.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(high_usage);
        assert_eq!(state.limit_violations, 1);
    }

  ./crates/ccswarm/src/resource/tests.rs:35-53 function test_update_usage <-> ./crates/ccswarm/src/resource/tests.rs:168-196 function test_limit_violations
  Similarity: 86.30%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_update_usage (lines 35-53) ---[0m
    fn test_update_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        let usage = ResourceUsage {
            cpu_percent: 25.0,
            memory_bytes: 512 * 1024 * 1024, // 512MB
            memory_percent: 12.5,
            thread_count: 8,
            timestamp: Utc::now(),
        };
        
        state.update_usage(usage.clone());
        
        assert_eq!(state.current_usage.cpu_percent, 25.0);
        assert_eq!(state.current_usage.memory_bytes, 512 * 1024 * 1024);
        assert_eq!(state.usage_history.len(), 1);
        assert_eq!(state.limit_violations, 0); // Not enforcing limits
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_limit_violations (lines 168-196) ---[0m
    fn test_limit_violations() {
        let mut limits = ResourceLimits::default();
        limits.max_cpu_percent = 50.0;
        limits.enforce_limits = true;
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Normal usage
        let normal_usage = ResourceUsage {
            cpu_percent: 30.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(normal_usage);
        assert_eq!(state.limit_violations, 0);
        
        // Exceeding usage
        let high_usage = ResourceUsage {
            cpu_percent: 75.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(high_usage);
        assert_eq!(state.limit_violations, 1);
    }

  ./crates/ccswarm/src/resource/tests.rs:78-94 function test_should_suspend_idle <-> ./crates/ccswarm/src/resource/tests.rs:168-196 function test_limit_violations
  Similarity: 87.58%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_should_suspend_idle (lines 78-94) ---[0m
    fn test_should_suspend_idle() {
        let mut limits = ResourceLimits::default();
        limits.idle_timeout = ChronoDuration::seconds(10); // Short timeout for testing
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Initially should not suspend
        assert!(!state.should_suspend());
        
        // Make agent idle by setting last_active to past
        state.last_active = Utc::now() - ChronoDuration::seconds(20);
        assert!(state.should_suspend());
        
        // Suspended agents should not be suspended again
        state.is_suspended = true;
        assert!(!state.should_suspend());
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_limit_violations (lines 168-196) ---[0m
    fn test_limit_violations() {
        let mut limits = ResourceLimits::default();
        limits.max_cpu_percent = 50.0;
        limits.enforce_limits = true;
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Normal usage
        let normal_usage = ResourceUsage {
            cpu_percent: 30.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(normal_usage);
        assert_eq!(state.limit_violations, 0);
        
        // Exceeding usage
        let high_usage = ResourceUsage {
            cpu_percent: 75.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(high_usage);
        assert_eq!(state.limit_violations, 1);
    }

  ./crates/ccswarm/src/resource/tests.rs:144-165 function test_get_average_usage <-> ./crates/ccswarm/src/resource/tests.rs:218-241 function test_resource_monitor_events
  Similarity: 82.48%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_get_average_usage (lines 144-165) ---[0m
    fn test_get_average_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add some usage entries
        for i in 1..=5 {
            let usage = ResourceUsage {
                cpu_percent: i as f32 * 10.0,
                memory_bytes: i as u64 * 100 * 1024 * 1024,
                memory_percent: i as f32 * 5.0,
                thread_count: i,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        let avg = state.get_average_usage();
        assert_eq!(avg.cpu_percent, 30.0); // (10+20+30+40+50)/5
        assert_eq!(avg.memory_bytes, 300 * 1024 * 1024); // Average of 100-500MB
        assert_eq!(avg.memory_percent, 15.0); // (5+10+15+20+25)/5
        assert_eq!(avg.thread_count, 3); // (1+2+3+4+5)/5
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_events (lines 218-241) ---[0m
    async fn test_resource_monitor_events() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Subscribe to events
        let mut event_rx = monitor.subscribe();
        
        // Start monitoring should emit event
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check event
        let event = tokio::time::timeout(
            tokio::time::Duration::from_millis(100),
            event_rx.recv()
        ).await;
        
        assert!(event.is_ok());
        if let Ok(Ok(ResourceEvent::MonitoringStarted { agent_id, pid })) = event {
            assert_eq!(agent_id, "test-agent");
            assert_eq!(pid, Some(1234));
        } else {
            panic!("Expected MonitoringStarted event");
        }
    }

  ./crates/ccswarm/src/resource/tests.rs:78-94 function test_should_suspend_idle <-> ./crates/ccswarm/src/resource/tests.rs:110-141 function test_is_exceeding_limits
  Similarity: 76.81%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_should_suspend_idle (lines 78-94) ---[0m
    fn test_should_suspend_idle() {
        let mut limits = ResourceLimits::default();
        limits.idle_timeout = ChronoDuration::seconds(10); // Short timeout for testing
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Initially should not suspend
        assert!(!state.should_suspend());
        
        // Make agent idle by setting last_active to past
        state.last_active = Utc::now() - ChronoDuration::seconds(20);
        assert!(state.should_suspend());
        
        // Suspended agents should not be suspended again
        state.is_suspended = true;
        assert!(!state.should_suspend());
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_is_exceeding_limits (lines 110-141) ---[0m
    fn test_is_exceeding_limits() {
        let mut limits = ResourceLimits::default();
        limits.max_cpu_percent = 50.0;
        limits.max_memory_bytes = 1024 * 1024 * 1024; // 1GB
        limits.max_memory_percent = 25.0;
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Not exceeding
        state.current_usage = ResourceUsage {
            cpu_percent: 30.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.0,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        assert!(!state.is_exceeding_limits());
        
        // Exceeding CPU
        state.current_usage.cpu_percent = 60.0;
        assert!(state.is_exceeding_limits());
        
        // Exceeding memory bytes
        state.current_usage.cpu_percent = 30.0;
        state.current_usage.memory_bytes = 2 * 1024 * 1024 * 1024;
        assert!(state.is_exceeding_limits());
        
        // Exceeding memory percent
        state.current_usage.memory_bytes = 512 * 1024 * 1024;
        state.current_usage.memory_percent = 30.0;
        assert!(state.is_exceeding_limits());
    }

  ./crates/ccswarm/src/resource/tests.rs:56-75 function test_usage_history_limit <-> ./crates/ccswarm/src/resource/tests.rs:168-196 function test_limit_violations
  Similarity: 76.11%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_usage_history_limit (lines 56-75) ---[0m
    fn test_usage_history_limit() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add 150 usage entries
        for i in 0..150 {
            let usage = ResourceUsage {
                cpu_percent: i as f32,
                memory_bytes: 1024 * 1024 * i as u64,
                memory_percent: (i as f32) / 10.0,
                thread_count: 1,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        // Should only keep last 100
        assert_eq!(state.usage_history.len(), 100);
        assert_eq!(state.usage_history[0].cpu_percent, 50.0); // First entry should be #50
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_limit_violations (lines 168-196) ---[0m
    fn test_limit_violations() {
        let mut limits = ResourceLimits::default();
        limits.max_cpu_percent = 50.0;
        limits.enforce_limits = true;
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Normal usage
        let normal_usage = ResourceUsage {
            cpu_percent: 30.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(normal_usage);
        assert_eq!(state.limit_violations, 0);
        
        // Exceeding usage
        let high_usage = ResourceUsage {
            cpu_percent: 75.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(high_usage);
        assert_eq!(state.limit_violations, 1);
    }

  ./crates/ccswarm/src/resource/tests.rs:168-196 function test_limit_violations <-> ./crates/ccswarm/src/resource/tests.rs:199-215 function test_resource_monitor_basic
  Similarity: 79.67%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_limit_violations (lines 168-196) ---[0m
    fn test_limit_violations() {
        let mut limits = ResourceLimits::default();
        limits.max_cpu_percent = 50.0;
        limits.enforce_limits = true;
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Normal usage
        let normal_usage = ResourceUsage {
            cpu_percent: 30.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(normal_usage);
        assert_eq!(state.limit_violations, 0);
        
        // Exceeding usage
        let high_usage = ResourceUsage {
            cpu_percent: 75.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(high_usage);
        assert_eq!(state.limit_violations, 1);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_basic (lines 199-215) ---[0m
    async fn test_resource_monitor_basic() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Start monitoring an agent
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check agent state exists
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_some());
        assert_eq!(state.unwrap().agent_id, "test-agent");
        
        // Stop monitoring
        monitor.stop_monitoring("test-agent").unwrap();
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_none());
    }

  ./crates/ccswarm/src/resource/tests.rs:56-75 function test_usage_history_limit <-> ./crates/ccswarm/src/resource/tests.rs:218-241 function test_resource_monitor_events
  Similarity: 80.88%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_usage_history_limit (lines 56-75) ---[0m
    fn test_usage_history_limit() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add 150 usage entries
        for i in 0..150 {
            let usage = ResourceUsage {
                cpu_percent: i as f32,
                memory_bytes: 1024 * 1024 * i as u64,
                memory_percent: (i as f32) / 10.0,
                thread_count: 1,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        // Should only keep last 100
        assert_eq!(state.usage_history.len(), 100);
        assert_eq!(state.usage_history[0].cpu_percent, 50.0); // First entry should be #50
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_events (lines 218-241) ---[0m
    async fn test_resource_monitor_events() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Subscribe to events
        let mut event_rx = monitor.subscribe();
        
        // Start monitoring should emit event
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check event
        let event = tokio::time::timeout(
            tokio::time::Duration::from_millis(100),
            event_rx.recv()
        ).await;
        
        assert!(event.is_ok());
        if let Ok(Ok(ResourceEvent::MonitoringStarted { agent_id, pid })) = event {
            assert_eq!(agent_id, "test-agent");
            assert_eq!(pid, Some(1234));
        } else {
            panic!("Expected MonitoringStarted event");
        }
    }

  ./crates/ccswarm/src/resource/tests.rs:110-141 function test_is_exceeding_limits <-> ./crates/ccswarm/src/resource/tests.rs:199-215 function test_resource_monitor_basic
  Similarity: 72.00%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_is_exceeding_limits (lines 110-141) ---[0m
    fn test_is_exceeding_limits() {
        let mut limits = ResourceLimits::default();
        limits.max_cpu_percent = 50.0;
        limits.max_memory_bytes = 1024 * 1024 * 1024; // 1GB
        limits.max_memory_percent = 25.0;
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Not exceeding
        state.current_usage = ResourceUsage {
            cpu_percent: 30.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.0,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        assert!(!state.is_exceeding_limits());
        
        // Exceeding CPU
        state.current_usage.cpu_percent = 60.0;
        assert!(state.is_exceeding_limits());
        
        // Exceeding memory bytes
        state.current_usage.cpu_percent = 30.0;
        state.current_usage.memory_bytes = 2 * 1024 * 1024 * 1024;
        assert!(state.is_exceeding_limits());
        
        // Exceeding memory percent
        state.current_usage.memory_bytes = 512 * 1024 * 1024;
        state.current_usage.memory_percent = 30.0;
        assert!(state.is_exceeding_limits());
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_basic (lines 199-215) ---[0m
    async fn test_resource_monitor_basic() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Start monitoring an agent
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check agent state exists
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_some());
        assert_eq!(state.unwrap().agent_id, "test-agent");
        
        // Stop monitoring
        monitor.stop_monitoring("test-agent").unwrap();
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_none());
    }

  ./crates/ccswarm/src/resource/tests.rs:35-53 function test_update_usage <-> ./crates/ccswarm/src/resource/tests.rs:218-241 function test_resource_monitor_events
  Similarity: 81.86%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_update_usage (lines 35-53) ---[0m
    fn test_update_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        let usage = ResourceUsage {
            cpu_percent: 25.0,
            memory_bytes: 512 * 1024 * 1024, // 512MB
            memory_percent: 12.5,
            thread_count: 8,
            timestamp: Utc::now(),
        };
        
        state.update_usage(usage.clone());
        
        assert_eq!(state.current_usage.cpu_percent, 25.0);
        assert_eq!(state.current_usage.memory_bytes, 512 * 1024 * 1024);
        assert_eq!(state.usage_history.len(), 1);
        assert_eq!(state.limit_violations, 0); // Not enforcing limits
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_events (lines 218-241) ---[0m
    async fn test_resource_monitor_events() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Subscribe to events
        let mut event_rx = monitor.subscribe();
        
        // Start monitoring should emit event
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check event
        let event = tokio::time::timeout(
            tokio::time::Duration::from_millis(100),
            event_rx.recv()
        ).await;
        
        assert!(event.is_ok());
        if let Ok(Ok(ResourceEvent::MonitoringStarted { agent_id, pid })) = event {
            assert_eq!(agent_id, "test-agent");
            assert_eq!(pid, Some(1234));
        } else {
            panic!("Expected MonitoringStarted event");
        }
    }

  ./crates/ccswarm/src/resource/tests.rs:199-215 function test_resource_monitor_basic <-> ./crates/ccswarm/src/resource/tests.rs:218-241 function test_resource_monitor_events
  Similarity: 83.14%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_basic (lines 199-215) ---[0m
    async fn test_resource_monitor_basic() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Start monitoring an agent
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check agent state exists
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_some());
        assert_eq!(state.unwrap().agent_id, "test-agent");
        
        // Stop monitoring
        monitor.stop_monitoring("test-agent").unwrap();
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_none());
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_events (lines 218-241) ---[0m
    async fn test_resource_monitor_events() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Subscribe to events
        let mut event_rx = monitor.subscribe();
        
        // Start monitoring should emit event
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check event
        let event = tokio::time::timeout(
            tokio::time::Duration::from_millis(100),
            event_rx.recv()
        ).await;
        
        assert!(event.is_ok());
        if let Ok(Ok(ResourceEvent::MonitoringStarted { agent_id, pid })) = event {
            assert_eq!(agent_id, "test-agent");
            assert_eq!(pid, Some(1234));
        } else {
            panic!("Expected MonitoringStarted event");
        }
    }

  ./crates/ccswarm/src/resource/tests.rs:56-75 function test_usage_history_limit <-> ./crates/ccswarm/src/resource/tests.rs:144-165 function test_get_average_usage
  Similarity: 80.43%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_usage_history_limit (lines 56-75) ---[0m
    fn test_usage_history_limit() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add 150 usage entries
        for i in 0..150 {
            let usage = ResourceUsage {
                cpu_percent: i as f32,
                memory_bytes: 1024 * 1024 * i as u64,
                memory_percent: (i as f32) / 10.0,
                thread_count: 1,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        // Should only keep last 100
        assert_eq!(state.usage_history.len(), 100);
        assert_eq!(state.usage_history[0].cpu_percent, 50.0); // First entry should be #50
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_get_average_usage (lines 144-165) ---[0m
    fn test_get_average_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add some usage entries
        for i in 1..=5 {
            let usage = ResourceUsage {
                cpu_percent: i as f32 * 10.0,
                memory_bytes: i as u64 * 100 * 1024 * 1024,
                memory_percent: i as f32 * 5.0,
                thread_count: i,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        let avg = state.get_average_usage();
        assert_eq!(avg.cpu_percent, 30.0); // (10+20+30+40+50)/5
        assert_eq!(avg.memory_bytes, 300 * 1024 * 1024); // Average of 100-500MB
        assert_eq!(avg.memory_percent, 15.0); // (5+10+15+20+25)/5
        assert_eq!(avg.thread_count, 3); // (1+2+3+4+5)/5
    }

  ./crates/ccswarm/src/resource/tests.rs:78-94 function test_should_suspend_idle <-> ./crates/ccswarm/src/resource/tests.rs:218-241 function test_resource_monitor_events
  Similarity: 81.95%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_should_suspend_idle (lines 78-94) ---[0m
    fn test_should_suspend_idle() {
        let mut limits = ResourceLimits::default();
        limits.idle_timeout = ChronoDuration::seconds(10); // Short timeout for testing
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Initially should not suspend
        assert!(!state.should_suspend());
        
        // Make agent idle by setting last_active to past
        state.last_active = Utc::now() - ChronoDuration::seconds(20);
        assert!(state.should_suspend());
        
        // Suspended agents should not be suspended again
        state.is_suspended = true;
        assert!(!state.should_suspend());
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_events (lines 218-241) ---[0m
    async fn test_resource_monitor_events() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Subscribe to events
        let mut event_rx = monitor.subscribe();
        
        // Start monitoring should emit event
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check event
        let event = tokio::time::timeout(
            tokio::time::Duration::from_millis(100),
            event_rx.recv()
        ).await;
        
        assert!(event.is_ok());
        if let Ok(Ok(ResourceEvent::MonitoringStarted { agent_id, pid })) = event {
            assert_eq!(agent_id, "test-agent");
            assert_eq!(pid, Some(1234));
        } else {
            panic!("Expected MonitoringStarted event");
        }
    }

  ./crates/ccswarm/src/resource/tests.rs:78-94 function test_should_suspend_idle <-> ./crates/ccswarm/src/resource/tests.rs:144-165 function test_get_average_usage
  Similarity: 85.12%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_should_suspend_idle (lines 78-94) ---[0m
    fn test_should_suspend_idle() {
        let mut limits = ResourceLimits::default();
        limits.idle_timeout = ChronoDuration::seconds(10); // Short timeout for testing
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Initially should not suspend
        assert!(!state.should_suspend());
        
        // Make agent idle by setting last_active to past
        state.last_active = Utc::now() - ChronoDuration::seconds(20);
        assert!(state.should_suspend());
        
        // Suspended agents should not be suspended again
        state.is_suspended = true;
        assert!(!state.should_suspend());
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_get_average_usage (lines 144-165) ---[0m
    fn test_get_average_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add some usage entries
        for i in 1..=5 {
            let usage = ResourceUsage {
                cpu_percent: i as f32 * 10.0,
                memory_bytes: i as u64 * 100 * 1024 * 1024,
                memory_percent: i as f32 * 5.0,
                thread_count: i,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        let avg = state.get_average_usage();
        assert_eq!(avg.cpu_percent, 30.0); // (10+20+30+40+50)/5
        assert_eq!(avg.memory_bytes, 300 * 1024 * 1024); // Average of 100-500MB
        assert_eq!(avg.memory_percent, 15.0); // (5+10+15+20+25)/5
        assert_eq!(avg.thread_count, 3); // (1+2+3+4+5)/5
    }

  ./crates/ccswarm/src/resource/tests.rs:168-196 function test_limit_violations <-> ./crates/ccswarm/src/resource/tests.rs:257-269 function test_efficiency_stats_with_agents
  Similarity: 77.87%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_limit_violations (lines 168-196) ---[0m
    fn test_limit_violations() {
        let mut limits = ResourceLimits::default();
        limits.max_cpu_percent = 50.0;
        limits.enforce_limits = true;
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Normal usage
        let normal_usage = ResourceUsage {
            cpu_percent: 30.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(normal_usage);
        assert_eq!(state.limit_violations, 0);
        
        // Exceeding usage
        let high_usage = ResourceUsage {
            cpu_percent: 75.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(high_usage);
        assert_eq!(state.limit_violations, 1);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_with_agents (lines 257-269) ---[0m
    fn test_efficiency_stats_with_agents() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        // Add some agents
        monitor.start_monitoring("agent1".to_string(), Some(1001), None).unwrap();
        monitor.start_monitoring("agent2".to_string(), Some(1002), None).unwrap();
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 2);
        assert_eq!(stats.active_agents, 2);
        assert_eq!(stats.suspended_agents, 0);
    }

  ./crates/ccswarm/src/resource/tests.rs:218-241 function test_resource_monitor_events <-> ./crates/ccswarm/src/resource/tests.rs:257-269 function test_efficiency_stats_with_agents
  Similarity: 87.17%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_events (lines 218-241) ---[0m
    async fn test_resource_monitor_events() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Subscribe to events
        let mut event_rx = monitor.subscribe();
        
        // Start monitoring should emit event
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check event
        let event = tokio::time::timeout(
            tokio::time::Duration::from_millis(100),
            event_rx.recv()
        ).await;
        
        assert!(event.is_ok());
        if let Ok(Ok(ResourceEvent::MonitoringStarted { agent_id, pid })) = event {
            assert_eq!(agent_id, "test-agent");
            assert_eq!(pid, Some(1234));
        } else {
            panic!("Expected MonitoringStarted event");
        }
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_with_agents (lines 257-269) ---[0m
    fn test_efficiency_stats_with_agents() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        // Add some agents
        monitor.start_monitoring("agent1".to_string(), Some(1001), None).unwrap();
        monitor.start_monitoring("agent2".to_string(), Some(1002), None).unwrap();
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 2);
        assert_eq!(stats.active_agents, 2);
        assert_eq!(stats.suspended_agents, 0);
    }

  ./crates/ccswarm/src/resource/tests.rs:35-53 function test_update_usage <-> ./crates/ccswarm/src/resource/tests.rs:144-165 function test_get_average_usage
  Similarity: 78.55%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_update_usage (lines 35-53) ---[0m
    fn test_update_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        let usage = ResourceUsage {
            cpu_percent: 25.0,
            memory_bytes: 512 * 1024 * 1024, // 512MB
            memory_percent: 12.5,
            thread_count: 8,
            timestamp: Utc::now(),
        };
        
        state.update_usage(usage.clone());
        
        assert_eq!(state.current_usage.cpu_percent, 25.0);
        assert_eq!(state.current_usage.memory_bytes, 512 * 1024 * 1024);
        assert_eq!(state.usage_history.len(), 1);
        assert_eq!(state.limit_violations, 0); // Not enforcing limits
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_get_average_usage (lines 144-165) ---[0m
    fn test_get_average_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add some usage entries
        for i in 1..=5 {
            let usage = ResourceUsage {
                cpu_percent: i as f32 * 10.0,
                memory_bytes: i as u64 * 100 * 1024 * 1024,
                memory_percent: i as f32 * 5.0,
                thread_count: i,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        let avg = state.get_average_usage();
        assert_eq!(avg.cpu_percent, 30.0); // (10+20+30+40+50)/5
        assert_eq!(avg.memory_bytes, 300 * 1024 * 1024); // Average of 100-500MB
        assert_eq!(avg.memory_percent, 15.0); // (5+10+15+20+25)/5
        assert_eq!(avg.thread_count, 3); // (1+2+3+4+5)/5
    }

  ./crates/ccswarm/src/resource/tests.rs:144-165 function test_get_average_usage <-> ./crates/ccswarm/src/resource/tests.rs:199-215 function test_resource_monitor_basic
  Similarity: 80.00%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_get_average_usage (lines 144-165) ---[0m
    fn test_get_average_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add some usage entries
        for i in 1..=5 {
            let usage = ResourceUsage {
                cpu_percent: i as f32 * 10.0,
                memory_bytes: i as u64 * 100 * 1024 * 1024,
                memory_percent: i as f32 * 5.0,
                thread_count: i,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        let avg = state.get_average_usage();
        assert_eq!(avg.cpu_percent, 30.0); // (10+20+30+40+50)/5
        assert_eq!(avg.memory_bytes, 300 * 1024 * 1024); // Average of 100-500MB
        assert_eq!(avg.memory_percent, 15.0); // (5+10+15+20+25)/5
        assert_eq!(avg.thread_count, 3); // (1+2+3+4+5)/5
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_basic (lines 199-215) ---[0m
    async fn test_resource_monitor_basic() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Start monitoring an agent
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check agent state exists
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_some());
        assert_eq!(state.unwrap().agent_id, "test-agent");
        
        // Stop monitoring
        monitor.stop_monitoring("test-agent").unwrap();
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_none());
    }

  ./crates/ccswarm/src/resource/tests.rs:10-19 function test_resource_limits_default <-> ./crates/ccswarm/src/resource/tests.rs:168-196 function test_limit_violations
  Similarity: 78.91%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_limits_default (lines 10-19) ---[0m
    fn test_resource_limits_default() {
        let limits = ResourceLimits::default();
        assert_eq!(limits.max_cpu_percent, 80.0);
        assert_eq!(limits.max_memory_bytes, 2 * 1024 * 1024 * 1024); // 2GB
        assert_eq!(limits.max_memory_percent, 50.0);
        assert_eq!(limits.idle_timeout, ChronoDuration::minutes(15));
        assert_eq!(limits.idle_cpu_threshold, 5.0);
        assert!(limits.auto_suspend_enabled);
        assert!(!limits.enforce_limits);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_limit_violations (lines 168-196) ---[0m
    fn test_limit_violations() {
        let mut limits = ResourceLimits::default();
        limits.max_cpu_percent = 50.0;
        limits.enforce_limits = true;
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Normal usage
        let normal_usage = ResourceUsage {
            cpu_percent: 30.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(normal_usage);
        assert_eq!(state.limit_violations, 0);
        
        // Exceeding usage
        let high_usage = ResourceUsage {
            cpu_percent: 75.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(high_usage);
        assert_eq!(state.limit_violations, 1);
    }

  ./crates/ccswarm/src/resource/tests.rs:168-196 function test_limit_violations <-> ./crates/ccswarm/src/resource/tests.rs:244-254 function test_efficiency_stats_empty
  Similarity: 75.69%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_limit_violations (lines 168-196) ---[0m
    fn test_limit_violations() {
        let mut limits = ResourceLimits::default();
        limits.max_cpu_percent = 50.0;
        limits.enforce_limits = true;
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Normal usage
        let normal_usage = ResourceUsage {
            cpu_percent: 30.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(normal_usage);
        assert_eq!(state.limit_violations, 0);
        
        // Exceeding usage
        let high_usage = ResourceUsage {
            cpu_percent: 75.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(high_usage);
        assert_eq!(state.limit_violations, 1);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_empty (lines 244-254) ---[0m
    fn test_efficiency_stats_empty() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 0);
        assert_eq!(stats.active_agents, 0);
        assert_eq!(stats.suspended_agents, 0);
        assert_eq!(stats.average_cpu_usage, 0.0);
        assert_eq!(stats.suspension_rate, 0.0);
    }

  ./crates/ccswarm/src/resource/tests.rs:97-107 function test_should_suspend_disabled <-> ./crates/ccswarm/src/resource/tests.rs:168-196 function test_limit_violations
  Similarity: 75.17%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_should_suspend_disabled (lines 97-107) ---[0m
    fn test_should_suspend_disabled() {
        let mut limits = ResourceLimits::default();
        limits.auto_suspend_enabled = false;
        limits.idle_timeout = ChronoDuration::seconds(1);
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        state.last_active = Utc::now() - ChronoDuration::seconds(10);
        
        // Should not suspend when disabled
        assert!(!state.should_suspend());
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_limit_violations (lines 168-196) ---[0m
    fn test_limit_violations() {
        let mut limits = ResourceLimits::default();
        limits.max_cpu_percent = 50.0;
        limits.enforce_limits = true;
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Normal usage
        let normal_usage = ResourceUsage {
            cpu_percent: 30.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(normal_usage);
        assert_eq!(state.limit_violations, 0);
        
        // Exceeding usage
        let high_usage = ResourceUsage {
            cpu_percent: 75.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(high_usage);
        assert_eq!(state.limit_violations, 1);
    }

  ./crates/ccswarm/src/resource/tests.rs:35-53 function test_update_usage <-> ./crates/ccswarm/src/resource/tests.rs:56-75 function test_usage_history_limit
  Similarity: 76.83%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_update_usage (lines 35-53) ---[0m
    fn test_update_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        let usage = ResourceUsage {
            cpu_percent: 25.0,
            memory_bytes: 512 * 1024 * 1024, // 512MB
            memory_percent: 12.5,
            thread_count: 8,
            timestamp: Utc::now(),
        };
        
        state.update_usage(usage.clone());
        
        assert_eq!(state.current_usage.cpu_percent, 25.0);
        assert_eq!(state.current_usage.memory_bytes, 512 * 1024 * 1024);
        assert_eq!(state.usage_history.len(), 1);
        assert_eq!(state.limit_violations, 0); // Not enforcing limits
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_usage_history_limit (lines 56-75) ---[0m
    fn test_usage_history_limit() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add 150 usage entries
        for i in 0..150 {
            let usage = ResourceUsage {
                cpu_percent: i as f32,
                memory_bytes: 1024 * 1024 * i as u64,
                memory_percent: (i as f32) / 10.0,
                thread_count: 1,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        // Should only keep last 100
        assert_eq!(state.usage_history.len(), 100);
        assert_eq!(state.usage_history[0].cpu_percent, 50.0); // First entry should be #50
    }

  ./crates/ccswarm/src/resource/tests.rs:22-32 function test_agent_resource_state_new <-> ./crates/ccswarm/src/resource/tests.rs:168-196 function test_limit_violations
  Similarity: 74.27%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_agent_resource_state_new (lines 22-32) ---[0m
    fn test_agent_resource_state_new() {
        let limits = ResourceLimits::default();
        let state = AgentResourceState::new("test-agent".to_string(), Some(1234), limits);
        
        assert_eq!(state.agent_id, "test-agent");
        assert_eq!(state.pid, Some(1234));
        assert!(!state.is_suspended);
        assert_eq!(state.limit_violations, 0);
        assert_eq!(state.current_usage.cpu_percent, 0.0);
        assert_eq!(state.current_usage.memory_bytes, 0);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_limit_violations (lines 168-196) ---[0m
    fn test_limit_violations() {
        let mut limits = ResourceLimits::default();
        limits.max_cpu_percent = 50.0;
        limits.enforce_limits = true;
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Normal usage
        let normal_usage = ResourceUsage {
            cpu_percent: 30.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(normal_usage);
        assert_eq!(state.limit_violations, 0);
        
        // Exceeding usage
        let high_usage = ResourceUsage {
            cpu_percent: 75.0,
            memory_bytes: 512 * 1024 * 1024,
            memory_percent: 12.5,
            thread_count: 4,
            timestamp: Utc::now(),
        };
        state.update_usage(high_usage);
        assert_eq!(state.limit_violations, 1);
    }

  ./crates/ccswarm/src/resource/tests.rs:56-75 function test_usage_history_limit <-> ./crates/ccswarm/src/resource/tests.rs:78-94 function test_should_suspend_idle
  Similarity: 79.83%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_usage_history_limit (lines 56-75) ---[0m
    fn test_usage_history_limit() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add 150 usage entries
        for i in 0..150 {
            let usage = ResourceUsage {
                cpu_percent: i as f32,
                memory_bytes: 1024 * 1024 * i as u64,
                memory_percent: (i as f32) / 10.0,
                thread_count: 1,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        // Should only keep last 100
        assert_eq!(state.usage_history.len(), 100);
        assert_eq!(state.usage_history[0].cpu_percent, 50.0); // First entry should be #50
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_should_suspend_idle (lines 78-94) ---[0m
    fn test_should_suspend_idle() {
        let mut limits = ResourceLimits::default();
        limits.idle_timeout = ChronoDuration::seconds(10); // Short timeout for testing
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Initially should not suspend
        assert!(!state.should_suspend());
        
        // Make agent idle by setting last_active to past
        state.last_active = Utc::now() - ChronoDuration::seconds(20);
        assert!(state.should_suspend());
        
        // Suspended agents should not be suspended again
        state.is_suspended = true;
        assert!(!state.should_suspend());
    }

  ./crates/ccswarm/src/resource/tests.rs:35-53 function test_update_usage <-> ./crates/ccswarm/src/resource/tests.rs:78-94 function test_should_suspend_idle
  Similarity: 81.78%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_update_usage (lines 35-53) ---[0m
    fn test_update_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        let usage = ResourceUsage {
            cpu_percent: 25.0,
            memory_bytes: 512 * 1024 * 1024, // 512MB
            memory_percent: 12.5,
            thread_count: 8,
            timestamp: Utc::now(),
        };
        
        state.update_usage(usage.clone());
        
        assert_eq!(state.current_usage.cpu_percent, 25.0);
        assert_eq!(state.current_usage.memory_bytes, 512 * 1024 * 1024);
        assert_eq!(state.usage_history.len(), 1);
        assert_eq!(state.limit_violations, 0); // Not enforcing limits
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_should_suspend_idle (lines 78-94) ---[0m
    fn test_should_suspend_idle() {
        let mut limits = ResourceLimits::default();
        limits.idle_timeout = ChronoDuration::seconds(10); // Short timeout for testing
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Initially should not suspend
        assert!(!state.should_suspend());
        
        // Make agent idle by setting last_active to past
        state.last_active = Utc::now() - ChronoDuration::seconds(20);
        assert!(state.should_suspend());
        
        // Suspended agents should not be suspended again
        state.is_suspended = true;
        assert!(!state.should_suspend());
    }

  ./crates/ccswarm/src/resource/tests.rs:144-165 function test_get_average_usage <-> ./crates/ccswarm/src/resource/tests.rs:257-269 function test_efficiency_stats_with_agents
  Similarity: 82.61%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_get_average_usage (lines 144-165) ---[0m
    fn test_get_average_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add some usage entries
        for i in 1..=5 {
            let usage = ResourceUsage {
                cpu_percent: i as f32 * 10.0,
                memory_bytes: i as u64 * 100 * 1024 * 1024,
                memory_percent: i as f32 * 5.0,
                thread_count: i,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        let avg = state.get_average_usage();
        assert_eq!(avg.cpu_percent, 30.0); // (10+20+30+40+50)/5
        assert_eq!(avg.memory_bytes, 300 * 1024 * 1024); // Average of 100-500MB
        assert_eq!(avg.memory_percent, 15.0); // (5+10+15+20+25)/5
        assert_eq!(avg.thread_count, 3); // (1+2+3+4+5)/5
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_with_agents (lines 257-269) ---[0m
    fn test_efficiency_stats_with_agents() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        // Add some agents
        monitor.start_monitoring("agent1".to_string(), Some(1001), None).unwrap();
        monitor.start_monitoring("agent2".to_string(), Some(1002), None).unwrap();
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 2);
        assert_eq!(stats.active_agents, 2);
        assert_eq!(stats.suspended_agents, 0);
    }

  ./crates/ccswarm/src/resource/tests.rs:10-19 function test_resource_limits_default <-> ./crates/ccswarm/src/resource/tests.rs:218-241 function test_resource_monitor_events
  Similarity: 84.03%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_limits_default (lines 10-19) ---[0m
    fn test_resource_limits_default() {
        let limits = ResourceLimits::default();
        assert_eq!(limits.max_cpu_percent, 80.0);
        assert_eq!(limits.max_memory_bytes, 2 * 1024 * 1024 * 1024); // 2GB
        assert_eq!(limits.max_memory_percent, 50.0);
        assert_eq!(limits.idle_timeout, ChronoDuration::minutes(15));
        assert_eq!(limits.idle_cpu_threshold, 5.0);
        assert!(limits.auto_suspend_enabled);
        assert!(!limits.enforce_limits);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_events (lines 218-241) ---[0m
    async fn test_resource_monitor_events() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Subscribe to events
        let mut event_rx = monitor.subscribe();
        
        // Start monitoring should emit event
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check event
        let event = tokio::time::timeout(
            tokio::time::Duration::from_millis(100),
            event_rx.recv()
        ).await;
        
        assert!(event.is_ok());
        if let Ok(Ok(ResourceEvent::MonitoringStarted { agent_id, pid })) = event {
            assert_eq!(agent_id, "test-agent");
            assert_eq!(pid, Some(1234));
        } else {
            panic!("Expected MonitoringStarted event");
        }
    }

  ./crates/ccswarm/src/resource/tests.rs:56-75 function test_usage_history_limit <-> ./crates/ccswarm/src/resource/tests.rs:199-215 function test_resource_monitor_basic
  Similarity: 76.39%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_usage_history_limit (lines 56-75) ---[0m
    fn test_usage_history_limit() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add 150 usage entries
        for i in 0..150 {
            let usage = ResourceUsage {
                cpu_percent: i as f32,
                memory_bytes: 1024 * 1024 * i as u64,
                memory_percent: (i as f32) / 10.0,
                thread_count: 1,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        // Should only keep last 100
        assert_eq!(state.usage_history.len(), 100);
        assert_eq!(state.usage_history[0].cpu_percent, 50.0); // First entry should be #50
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_basic (lines 199-215) ---[0m
    async fn test_resource_monitor_basic() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Start monitoring an agent
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check agent state exists
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_some());
        assert_eq!(state.unwrap().agent_id, "test-agent");
        
        // Stop monitoring
        monitor.stop_monitoring("test-agent").unwrap();
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_none());
    }

  ./crates/ccswarm/src/resource/tests.rs:97-107 function test_should_suspend_disabled <-> ./crates/ccswarm/src/resource/tests.rs:218-241 function test_resource_monitor_events
  Similarity: 80.62%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_should_suspend_disabled (lines 97-107) ---[0m
    fn test_should_suspend_disabled() {
        let mut limits = ResourceLimits::default();
        limits.auto_suspend_enabled = false;
        limits.idle_timeout = ChronoDuration::seconds(1);
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        state.last_active = Utc::now() - ChronoDuration::seconds(10);
        
        // Should not suspend when disabled
        assert!(!state.should_suspend());
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_events (lines 218-241) ---[0m
    async fn test_resource_monitor_events() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Subscribe to events
        let mut event_rx = monitor.subscribe();
        
        // Start monitoring should emit event
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check event
        let event = tokio::time::timeout(
            tokio::time::Duration::from_millis(100),
            event_rx.recv()
        ).await;
        
        assert!(event.is_ok());
        if let Ok(Ok(ResourceEvent::MonitoringStarted { agent_id, pid })) = event {
            assert_eq!(agent_id, "test-agent");
            assert_eq!(pid, Some(1234));
        } else {
            panic!("Expected MonitoringStarted event");
        }
    }

  ./crates/ccswarm/src/resource/tests.rs:22-32 function test_agent_resource_state_new <-> ./crates/ccswarm/src/resource/tests.rs:218-241 function test_resource_monitor_events
  Similarity: 80.09%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_agent_resource_state_new (lines 22-32) ---[0m
    fn test_agent_resource_state_new() {
        let limits = ResourceLimits::default();
        let state = AgentResourceState::new("test-agent".to_string(), Some(1234), limits);
        
        assert_eq!(state.agent_id, "test-agent");
        assert_eq!(state.pid, Some(1234));
        assert!(!state.is_suspended);
        assert_eq!(state.limit_violations, 0);
        assert_eq!(state.current_usage.cpu_percent, 0.0);
        assert_eq!(state.current_usage.memory_bytes, 0);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_events (lines 218-241) ---[0m
    async fn test_resource_monitor_events() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Subscribe to events
        let mut event_rx = monitor.subscribe();
        
        // Start monitoring should emit event
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check event
        let event = tokio::time::timeout(
            tokio::time::Duration::from_millis(100),
            event_rx.recv()
        ).await;
        
        assert!(event.is_ok());
        if let Ok(Ok(ResourceEvent::MonitoringStarted { agent_id, pid })) = event {
            assert_eq!(agent_id, "test-agent");
            assert_eq!(pid, Some(1234));
        } else {
            panic!("Expected MonitoringStarted event");
        }
    }

  ./crates/ccswarm/src/resource/tests.rs:78-94 function test_should_suspend_idle <-> ./crates/ccswarm/src/resource/tests.rs:199-215 function test_resource_monitor_basic
  Similarity: 81.69%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_should_suspend_idle (lines 78-94) ---[0m
    fn test_should_suspend_idle() {
        let mut limits = ResourceLimits::default();
        limits.idle_timeout = ChronoDuration::seconds(10); // Short timeout for testing
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Initially should not suspend
        assert!(!state.should_suspend());
        
        // Make agent idle by setting last_active to past
        state.last_active = Utc::now() - ChronoDuration::seconds(20);
        assert!(state.should_suspend());
        
        // Suspended agents should not be suspended again
        state.is_suspended = true;
        assert!(!state.should_suspend());
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_basic (lines 199-215) ---[0m
    async fn test_resource_monitor_basic() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Start monitoring an agent
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check agent state exists
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_some());
        assert_eq!(state.unwrap().agent_id, "test-agent");
        
        // Stop monitoring
        monitor.stop_monitoring("test-agent").unwrap();
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_none());
    }

  ./crates/ccswarm/src/resource/tests.rs:35-53 function test_update_usage <-> ./crates/ccswarm/src/resource/tests.rs:199-215 function test_resource_monitor_basic
  Similarity: 76.78%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_update_usage (lines 35-53) ---[0m
    fn test_update_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        let usage = ResourceUsage {
            cpu_percent: 25.0,
            memory_bytes: 512 * 1024 * 1024, // 512MB
            memory_percent: 12.5,
            thread_count: 8,
            timestamp: Utc::now(),
        };
        
        state.update_usage(usage.clone());
        
        assert_eq!(state.current_usage.cpu_percent, 25.0);
        assert_eq!(state.current_usage.memory_bytes, 512 * 1024 * 1024);
        assert_eq!(state.usage_history.len(), 1);
        assert_eq!(state.limit_violations, 0); // Not enforcing limits
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_basic (lines 199-215) ---[0m
    async fn test_resource_monitor_basic() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Start monitoring an agent
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check agent state exists
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_some());
        assert_eq!(state.unwrap().agent_id, "test-agent");
        
        // Stop monitoring
        monitor.stop_monitoring("test-agent").unwrap();
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_none());
    }

  ./crates/ccswarm/src/resource/tests.rs:144-165 function test_get_average_usage <-> ./crates/ccswarm/src/resource/tests.rs:244-254 function test_efficiency_stats_empty
  Similarity: 83.29%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_get_average_usage (lines 144-165) ---[0m
    fn test_get_average_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add some usage entries
        for i in 1..=5 {
            let usage = ResourceUsage {
                cpu_percent: i as f32 * 10.0,
                memory_bytes: i as u64 * 100 * 1024 * 1024,
                memory_percent: i as f32 * 5.0,
                thread_count: i,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        let avg = state.get_average_usage();
        assert_eq!(avg.cpu_percent, 30.0); // (10+20+30+40+50)/5
        assert_eq!(avg.memory_bytes, 300 * 1024 * 1024); // Average of 100-500MB
        assert_eq!(avg.memory_percent, 15.0); // (5+10+15+20+25)/5
        assert_eq!(avg.thread_count, 3); // (1+2+3+4+5)/5
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_empty (lines 244-254) ---[0m
    fn test_efficiency_stats_empty() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 0);
        assert_eq!(stats.active_agents, 0);
        assert_eq!(stats.suspended_agents, 0);
        assert_eq!(stats.average_cpu_usage, 0.0);
        assert_eq!(stats.suspension_rate, 0.0);
    }

  ./crates/ccswarm/src/resource/tests.rs:22-32 function test_agent_resource_state_new <-> ./crates/ccswarm/src/resource/tests.rs:144-165 function test_get_average_usage
  Similarity: 81.64%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_agent_resource_state_new (lines 22-32) ---[0m
    fn test_agent_resource_state_new() {
        let limits = ResourceLimits::default();
        let state = AgentResourceState::new("test-agent".to_string(), Some(1234), limits);
        
        assert_eq!(state.agent_id, "test-agent");
        assert_eq!(state.pid, Some(1234));
        assert!(!state.is_suspended);
        assert_eq!(state.limit_violations, 0);
        assert_eq!(state.current_usage.cpu_percent, 0.0);
        assert_eq!(state.current_usage.memory_bytes, 0);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_get_average_usage (lines 144-165) ---[0m
    fn test_get_average_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add some usage entries
        for i in 1..=5 {
            let usage = ResourceUsage {
                cpu_percent: i as f32 * 10.0,
                memory_bytes: i as u64 * 100 * 1024 * 1024,
                memory_percent: i as f32 * 5.0,
                thread_count: i,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        let avg = state.get_average_usage();
        assert_eq!(avg.cpu_percent, 30.0); // (10+20+30+40+50)/5
        assert_eq!(avg.memory_bytes, 300 * 1024 * 1024); // Average of 100-500MB
        assert_eq!(avg.memory_percent, 15.0); // (5+10+15+20+25)/5
        assert_eq!(avg.thread_count, 3); // (1+2+3+4+5)/5
    }

  ./crates/ccswarm/src/resource/tests.rs:56-75 function test_usage_history_limit <-> ./crates/ccswarm/src/resource/tests.rs:257-269 function test_efficiency_stats_with_agents
  Similarity: 80.58%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_usage_history_limit (lines 56-75) ---[0m
    fn test_usage_history_limit() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add 150 usage entries
        for i in 0..150 {
            let usage = ResourceUsage {
                cpu_percent: i as f32,
                memory_bytes: 1024 * 1024 * i as u64,
                memory_percent: (i as f32) / 10.0,
                thread_count: 1,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        // Should only keep last 100
        assert_eq!(state.usage_history.len(), 100);
        assert_eq!(state.usage_history[0].cpu_percent, 50.0); // First entry should be #50
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_with_agents (lines 257-269) ---[0m
    fn test_efficiency_stats_with_agents() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        // Add some agents
        monitor.start_monitoring("agent1".to_string(), Some(1001), None).unwrap();
        monitor.start_monitoring("agent2".to_string(), Some(1002), None).unwrap();
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 2);
        assert_eq!(stats.active_agents, 2);
        assert_eq!(stats.suspended_agents, 0);
    }

  ./crates/ccswarm/src/resource/tests.rs:10-19 function test_resource_limits_default <-> ./crates/ccswarm/src/resource/tests.rs:144-165 function test_get_average_usage
  Similarity: 80.97%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_limits_default (lines 10-19) ---[0m
    fn test_resource_limits_default() {
        let limits = ResourceLimits::default();
        assert_eq!(limits.max_cpu_percent, 80.0);
        assert_eq!(limits.max_memory_bytes, 2 * 1024 * 1024 * 1024); // 2GB
        assert_eq!(limits.max_memory_percent, 50.0);
        assert_eq!(limits.idle_timeout, ChronoDuration::minutes(15));
        assert_eq!(limits.idle_cpu_threshold, 5.0);
        assert!(limits.auto_suspend_enabled);
        assert!(!limits.enforce_limits);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_get_average_usage (lines 144-165) ---[0m
    fn test_get_average_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add some usage entries
        for i in 1..=5 {
            let usage = ResourceUsage {
                cpu_percent: i as f32 * 10.0,
                memory_bytes: i as u64 * 100 * 1024 * 1024,
                memory_percent: i as f32 * 5.0,
                thread_count: i,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        let avg = state.get_average_usage();
        assert_eq!(avg.cpu_percent, 30.0); // (10+20+30+40+50)/5
        assert_eq!(avg.memory_bytes, 300 * 1024 * 1024); // Average of 100-500MB
        assert_eq!(avg.memory_percent, 15.0); // (5+10+15+20+25)/5
        assert_eq!(avg.thread_count, 3); // (1+2+3+4+5)/5
    }

  ./crates/ccswarm/src/resource/tests.rs:35-53 function test_update_usage <-> ./crates/ccswarm/src/resource/tests.rs:257-269 function test_efficiency_stats_with_agents
  Similarity: 79.89%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_update_usage (lines 35-53) ---[0m
    fn test_update_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        let usage = ResourceUsage {
            cpu_percent: 25.0,
            memory_bytes: 512 * 1024 * 1024, // 512MB
            memory_percent: 12.5,
            thread_count: 8,
            timestamp: Utc::now(),
        };
        
        state.update_usage(usage.clone());
        
        assert_eq!(state.current_usage.cpu_percent, 25.0);
        assert_eq!(state.current_usage.memory_bytes, 512 * 1024 * 1024);
        assert_eq!(state.usage_history.len(), 1);
        assert_eq!(state.limit_violations, 0); // Not enforcing limits
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_with_agents (lines 257-269) ---[0m
    fn test_efficiency_stats_with_agents() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        // Add some agents
        monitor.start_monitoring("agent1".to_string(), Some(1001), None).unwrap();
        monitor.start_monitoring("agent2".to_string(), Some(1002), None).unwrap();
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 2);
        assert_eq!(stats.active_agents, 2);
        assert_eq!(stats.suspended_agents, 0);
    }

  ./crates/ccswarm/src/resource/tests.rs:56-75 function test_usage_history_limit <-> ./crates/ccswarm/src/resource/tests.rs:97-107 function test_should_suspend_disabled
  Similarity: 81.80%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_usage_history_limit (lines 56-75) ---[0m
    fn test_usage_history_limit() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add 150 usage entries
        for i in 0..150 {
            let usage = ResourceUsage {
                cpu_percent: i as f32,
                memory_bytes: 1024 * 1024 * i as u64,
                memory_percent: (i as f32) / 10.0,
                thread_count: 1,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        // Should only keep last 100
        assert_eq!(state.usage_history.len(), 100);
        assert_eq!(state.usage_history[0].cpu_percent, 50.0); // First entry should be #50
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_should_suspend_disabled (lines 97-107) ---[0m
    fn test_should_suspend_disabled() {
        let mut limits = ResourceLimits::default();
        limits.auto_suspend_enabled = false;
        limits.idle_timeout = ChronoDuration::seconds(1);
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        state.last_active = Utc::now() - ChronoDuration::seconds(10);
        
        // Should not suspend when disabled
        assert!(!state.should_suspend());
    }

  ./crates/ccswarm/src/resource/tests.rs:22-32 function test_agent_resource_state_new <-> ./crates/ccswarm/src/resource/tests.rs:56-75 function test_usage_history_limit
  Similarity: 81.28%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_agent_resource_state_new (lines 22-32) ---[0m
    fn test_agent_resource_state_new() {
        let limits = ResourceLimits::default();
        let state = AgentResourceState::new("test-agent".to_string(), Some(1234), limits);
        
        assert_eq!(state.agent_id, "test-agent");
        assert_eq!(state.pid, Some(1234));
        assert!(!state.is_suspended);
        assert_eq!(state.limit_violations, 0);
        assert_eq!(state.current_usage.cpu_percent, 0.0);
        assert_eq!(state.current_usage.memory_bytes, 0);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_usage_history_limit (lines 56-75) ---[0m
    fn test_usage_history_limit() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add 150 usage entries
        for i in 0..150 {
            let usage = ResourceUsage {
                cpu_percent: i as f32,
                memory_bytes: 1024 * 1024 * i as u64,
                memory_percent: (i as f32) / 10.0,
                thread_count: 1,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        // Should only keep last 100
        assert_eq!(state.usage_history.len(), 100);
        assert_eq!(state.usage_history[0].cpu_percent, 50.0); // First entry should be #50
    }

  ./crates/ccswarm/src/resource/tests.rs:56-75 function test_usage_history_limit <-> ./crates/ccswarm/src/resource/tests.rs:244-254 function test_efficiency_stats_empty
  Similarity: 78.14%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_usage_history_limit (lines 56-75) ---[0m
    fn test_usage_history_limit() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add 150 usage entries
        for i in 0..150 {
            let usage = ResourceUsage {
                cpu_percent: i as f32,
                memory_bytes: 1024 * 1024 * i as u64,
                memory_percent: (i as f32) / 10.0,
                thread_count: 1,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        // Should only keep last 100
        assert_eq!(state.usage_history.len(), 100);
        assert_eq!(state.usage_history[0].cpu_percent, 50.0); // First entry should be #50
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_empty (lines 244-254) ---[0m
    fn test_efficiency_stats_empty() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 0);
        assert_eq!(stats.active_agents, 0);
        assert_eq!(stats.suspended_agents, 0);
        assert_eq!(stats.average_cpu_usage, 0.0);
        assert_eq!(stats.suspension_rate, 0.0);
    }

  ./crates/ccswarm/src/resource/tests.rs:97-107 function test_should_suspend_disabled <-> ./crates/ccswarm/src/resource/tests.rs:144-165 function test_get_average_usage
  Similarity: 72.46%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_should_suspend_disabled (lines 97-107) ---[0m
    fn test_should_suspend_disabled() {
        let mut limits = ResourceLimits::default();
        limits.auto_suspend_enabled = false;
        limits.idle_timeout = ChronoDuration::seconds(1);
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        state.last_active = Utc::now() - ChronoDuration::seconds(10);
        
        // Should not suspend when disabled
        assert!(!state.should_suspend());
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_get_average_usage (lines 144-165) ---[0m
    fn test_get_average_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add some usage entries
        for i in 1..=5 {
            let usage = ResourceUsage {
                cpu_percent: i as f32 * 10.0,
                memory_bytes: i as u64 * 100 * 1024 * 1024,
                memory_percent: i as f32 * 5.0,
                thread_count: i,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        let avg = state.get_average_usage();
        assert_eq!(avg.cpu_percent, 30.0); // (10+20+30+40+50)/5
        assert_eq!(avg.memory_bytes, 300 * 1024 * 1024); // Average of 100-500MB
        assert_eq!(avg.memory_percent, 15.0); // (5+10+15+20+25)/5
        assert_eq!(avg.thread_count, 3); // (1+2+3+4+5)/5
    }

  ./crates/ccswarm/src/resource/tests.rs:35-53 function test_update_usage <-> ./crates/ccswarm/src/resource/tests.rs:244-254 function test_efficiency_stats_empty
  Similarity: 79.39%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_update_usage (lines 35-53) ---[0m
    fn test_update_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        let usage = ResourceUsage {
            cpu_percent: 25.0,
            memory_bytes: 512 * 1024 * 1024, // 512MB
            memory_percent: 12.5,
            thread_count: 8,
            timestamp: Utc::now(),
        };
        
        state.update_usage(usage.clone());
        
        assert_eq!(state.current_usage.cpu_percent, 25.0);
        assert_eq!(state.current_usage.memory_bytes, 512 * 1024 * 1024);
        assert_eq!(state.usage_history.len(), 1);
        assert_eq!(state.limit_violations, 0); // Not enforcing limits
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_empty (lines 244-254) ---[0m
    fn test_efficiency_stats_empty() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 0);
        assert_eq!(stats.active_agents, 0);
        assert_eq!(stats.suspended_agents, 0);
        assert_eq!(stats.average_cpu_usage, 0.0);
        assert_eq!(stats.suspension_rate, 0.0);
    }

  ./crates/ccswarm/src/resource/tests.rs:199-215 function test_resource_monitor_basic <-> ./crates/ccswarm/src/resource/tests.rs:257-269 function test_efficiency_stats_with_agents
  Similarity: 78.85%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_basic (lines 199-215) ---[0m
    async fn test_resource_monitor_basic() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Start monitoring an agent
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check agent state exists
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_some());
        assert_eq!(state.unwrap().agent_id, "test-agent");
        
        // Stop monitoring
        monitor.stop_monitoring("test-agent").unwrap();
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_none());
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_with_agents (lines 257-269) ---[0m
    fn test_efficiency_stats_with_agents() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        // Add some agents
        monitor.start_monitoring("agent1".to_string(), Some(1001), None).unwrap();
        monitor.start_monitoring("agent2".to_string(), Some(1002), None).unwrap();
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 2);
        assert_eq!(stats.active_agents, 2);
        assert_eq!(stats.suspended_agents, 0);
    }

  ./crates/ccswarm/src/resource/tests.rs:22-32 function test_agent_resource_state_new <-> ./crates/ccswarm/src/resource/tests.rs:35-53 function test_update_usage
  Similarity: 78.39%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_agent_resource_state_new (lines 22-32) ---[0m
    fn test_agent_resource_state_new() {
        let limits = ResourceLimits::default();
        let state = AgentResourceState::new("test-agent".to_string(), Some(1234), limits);
        
        assert_eq!(state.agent_id, "test-agent");
        assert_eq!(state.pid, Some(1234));
        assert!(!state.is_suspended);
        assert_eq!(state.limit_violations, 0);
        assert_eq!(state.current_usage.cpu_percent, 0.0);
        assert_eq!(state.current_usage.memory_bytes, 0);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_update_usage (lines 35-53) ---[0m
    fn test_update_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        let usage = ResourceUsage {
            cpu_percent: 25.0,
            memory_bytes: 512 * 1024 * 1024, // 512MB
            memory_percent: 12.5,
            thread_count: 8,
            timestamp: Utc::now(),
        };
        
        state.update_usage(usage.clone());
        
        assert_eq!(state.current_usage.cpu_percent, 25.0);
        assert_eq!(state.current_usage.memory_bytes, 512 * 1024 * 1024);
        assert_eq!(state.usage_history.len(), 1);
        assert_eq!(state.limit_violations, 0); // Not enforcing limits
    }

  ./crates/ccswarm/src/resource/tests.rs:10-19 function test_resource_limits_default <-> ./crates/ccswarm/src/resource/tests.rs:56-75 function test_usage_history_limit
  Similarity: 77.21%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_limits_default (lines 10-19) ---[0m
    fn test_resource_limits_default() {
        let limits = ResourceLimits::default();
        assert_eq!(limits.max_cpu_percent, 80.0);
        assert_eq!(limits.max_memory_bytes, 2 * 1024 * 1024 * 1024); // 2GB
        assert_eq!(limits.max_memory_percent, 50.0);
        assert_eq!(limits.idle_timeout, ChronoDuration::minutes(15));
        assert_eq!(limits.idle_cpu_threshold, 5.0);
        assert!(limits.auto_suspend_enabled);
        assert!(!limits.enforce_limits);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_usage_history_limit (lines 56-75) ---[0m
    fn test_usage_history_limit() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Add 150 usage entries
        for i in 0..150 {
            let usage = ResourceUsage {
                cpu_percent: i as f32,
                memory_bytes: 1024 * 1024 * i as u64,
                memory_percent: (i as f32) / 10.0,
                thread_count: 1,
                timestamp: Utc::now(),
            };
            state.update_usage(usage);
        }
        
        // Should only keep last 100
        assert_eq!(state.usage_history.len(), 100);
        assert_eq!(state.usage_history[0].cpu_percent, 50.0); // First entry should be #50
    }

  ./crates/ccswarm/src/resource/tests.rs:78-94 function test_should_suspend_idle <-> ./crates/ccswarm/src/resource/tests.rs:257-269 function test_efficiency_stats_with_agents
  Similarity: 76.99%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_should_suspend_idle (lines 78-94) ---[0m
    fn test_should_suspend_idle() {
        let mut limits = ResourceLimits::default();
        limits.idle_timeout = ChronoDuration::seconds(10); // Short timeout for testing
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Initially should not suspend
        assert!(!state.should_suspend());
        
        // Make agent idle by setting last_active to past
        state.last_active = Utc::now() - ChronoDuration::seconds(20);
        assert!(state.should_suspend());
        
        // Suspended agents should not be suspended again
        state.is_suspended = true;
        assert!(!state.should_suspend());
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_with_agents (lines 257-269) ---[0m
    fn test_efficiency_stats_with_agents() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        // Add some agents
        monitor.start_monitoring("agent1".to_string(), Some(1001), None).unwrap();
        monitor.start_monitoring("agent2".to_string(), Some(1002), None).unwrap();
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 2);
        assert_eq!(stats.active_agents, 2);
        assert_eq!(stats.suspended_agents, 0);
    }

  ./crates/ccswarm/src/resource/tests.rs:10-19 function test_resource_limits_default <-> ./crates/ccswarm/src/resource/tests.rs:35-53 function test_update_usage
  Similarity: 74.22%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_limits_default (lines 10-19) ---[0m
    fn test_resource_limits_default() {
        let limits = ResourceLimits::default();
        assert_eq!(limits.max_cpu_percent, 80.0);
        assert_eq!(limits.max_memory_bytes, 2 * 1024 * 1024 * 1024); // 2GB
        assert_eq!(limits.max_memory_percent, 50.0);
        assert_eq!(limits.idle_timeout, ChronoDuration::minutes(15));
        assert_eq!(limits.idle_cpu_threshold, 5.0);
        assert!(limits.auto_suspend_enabled);
        assert!(!limits.enforce_limits);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_update_usage (lines 35-53) ---[0m
    fn test_update_usage() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        let usage = ResourceUsage {
            cpu_percent: 25.0,
            memory_bytes: 512 * 1024 * 1024, // 512MB
            memory_percent: 12.5,
            thread_count: 8,
            timestamp: Utc::now(),
        };
        
        state.update_usage(usage.clone());
        
        assert_eq!(state.current_usage.cpu_percent, 25.0);
        assert_eq!(state.current_usage.memory_bytes, 512 * 1024 * 1024);
        assert_eq!(state.usage_history.len(), 1);
        assert_eq!(state.limit_violations, 0); // Not enforcing limits
    }

  ./crates/ccswarm/src/resource/tests.rs:10-19 function test_resource_limits_default <-> ./crates/ccswarm/src/resource/tests.rs:199-215 function test_resource_monitor_basic
  Similarity: 79.40%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_limits_default (lines 10-19) ---[0m
    fn test_resource_limits_default() {
        let limits = ResourceLimits::default();
        assert_eq!(limits.max_cpu_percent, 80.0);
        assert_eq!(limits.max_memory_bytes, 2 * 1024 * 1024 * 1024); // 2GB
        assert_eq!(limits.max_memory_percent, 50.0);
        assert_eq!(limits.idle_timeout, ChronoDuration::minutes(15));
        assert_eq!(limits.idle_cpu_threshold, 5.0);
        assert!(limits.auto_suspend_enabled);
        assert!(!limits.enforce_limits);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_basic (lines 199-215) ---[0m
    async fn test_resource_monitor_basic() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Start monitoring an agent
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check agent state exists
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_some());
        assert_eq!(state.unwrap().agent_id, "test-agent");
        
        // Stop monitoring
        monitor.stop_monitoring("test-agent").unwrap();
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_none());
    }

  ./crates/ccswarm/src/resource/tests.rs:78-94 function test_should_suspend_idle <-> ./crates/ccswarm/src/resource/tests.rs:97-107 function test_should_suspend_disabled
  Similarity: 76.53%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_should_suspend_idle (lines 78-94) ---[0m
    fn test_should_suspend_idle() {
        let mut limits = ResourceLimits::default();
        limits.idle_timeout = ChronoDuration::seconds(10); // Short timeout for testing
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Initially should not suspend
        assert!(!state.should_suspend());
        
        // Make agent idle by setting last_active to past
        state.last_active = Utc::now() - ChronoDuration::seconds(20);
        assert!(state.should_suspend());
        
        // Suspended agents should not be suspended again
        state.is_suspended = true;
        assert!(!state.should_suspend());
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_should_suspend_disabled (lines 97-107) ---[0m
    fn test_should_suspend_disabled() {
        let mut limits = ResourceLimits::default();
        limits.auto_suspend_enabled = false;
        limits.idle_timeout = ChronoDuration::seconds(1);
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        state.last_active = Utc::now() - ChronoDuration::seconds(10);
        
        // Should not suspend when disabled
        assert!(!state.should_suspend());
    }

  ./crates/ccswarm/src/resource/tests.rs:244-254 function test_efficiency_stats_empty <-> ./crates/ccswarm/src/resource/tests.rs:257-269 function test_efficiency_stats_with_agents
  Similarity: 87.82%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_empty (lines 244-254) ---[0m
    fn test_efficiency_stats_empty() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 0);
        assert_eq!(stats.active_agents, 0);
        assert_eq!(stats.suspended_agents, 0);
        assert_eq!(stats.average_cpu_usage, 0.0);
        assert_eq!(stats.suspension_rate, 0.0);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_with_agents (lines 257-269) ---[0m
    fn test_efficiency_stats_with_agents() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        // Add some agents
        monitor.start_monitoring("agent1".to_string(), Some(1001), None).unwrap();
        monitor.start_monitoring("agent2".to_string(), Some(1002), None).unwrap();
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 2);
        assert_eq!(stats.active_agents, 2);
        assert_eq!(stats.suspended_agents, 0);
    }

  ./crates/ccswarm/src/resource/tests.rs:78-94 function test_should_suspend_idle <-> ./crates/ccswarm/src/resource/tests.rs:244-254 function test_efficiency_stats_empty
  Similarity: 74.93%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_should_suspend_idle (lines 78-94) ---[0m
    fn test_should_suspend_idle() {
        let mut limits = ResourceLimits::default();
        limits.idle_timeout = ChronoDuration::seconds(10); // Short timeout for testing
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Initially should not suspend
        assert!(!state.should_suspend());
        
        // Make agent idle by setting last_active to past
        state.last_active = Utc::now() - ChronoDuration::seconds(20);
        assert!(state.should_suspend());
        
        // Suspended agents should not be suspended again
        state.is_suspended = true;
        assert!(!state.should_suspend());
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_empty (lines 244-254) ---[0m
    fn test_efficiency_stats_empty() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 0);
        assert_eq!(stats.active_agents, 0);
        assert_eq!(stats.suspended_agents, 0);
        assert_eq!(stats.average_cpu_usage, 0.0);
        assert_eq!(stats.suspension_rate, 0.0);
    }

  ./crates/ccswarm/src/resource/tests.rs:199-215 function test_resource_monitor_basic <-> ./crates/ccswarm/src/resource/tests.rs:244-254 function test_efficiency_stats_empty
  Similarity: 72.68%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_basic (lines 199-215) ---[0m
    async fn test_resource_monitor_basic() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Start monitoring an agent
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check agent state exists
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_some());
        assert_eq!(state.unwrap().agent_id, "test-agent");
        
        // Stop monitoring
        monitor.stop_monitoring("test-agent").unwrap();
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_none());
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_empty (lines 244-254) ---[0m
    fn test_efficiency_stats_empty() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 0);
        assert_eq!(stats.active_agents, 0);
        assert_eq!(stats.suspended_agents, 0);
        assert_eq!(stats.average_cpu_usage, 0.0);
        assert_eq!(stats.suspension_rate, 0.0);
    }

  ./crates/ccswarm/src/resource/tests.rs:22-32 function test_agent_resource_state_new <-> ./crates/ccswarm/src/resource/tests.rs:78-94 function test_should_suspend_idle
  Similarity: 72.20%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_agent_resource_state_new (lines 22-32) ---[0m
    fn test_agent_resource_state_new() {
        let limits = ResourceLimits::default();
        let state = AgentResourceState::new("test-agent".to_string(), Some(1234), limits);
        
        assert_eq!(state.agent_id, "test-agent");
        assert_eq!(state.pid, Some(1234));
        assert!(!state.is_suspended);
        assert_eq!(state.limit_violations, 0);
        assert_eq!(state.current_usage.cpu_percent, 0.0);
        assert_eq!(state.current_usage.memory_bytes, 0);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_should_suspend_idle (lines 78-94) ---[0m
    fn test_should_suspend_idle() {
        let mut limits = ResourceLimits::default();
        limits.idle_timeout = ChronoDuration::seconds(10); // Short timeout for testing
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Initially should not suspend
        assert!(!state.should_suspend());
        
        // Make agent idle by setting last_active to past
        state.last_active = Utc::now() - ChronoDuration::seconds(20);
        assert!(state.should_suspend());
        
        // Suspended agents should not be suspended again
        state.is_suspended = true;
        assert!(!state.should_suspend());
    }

  ./crates/ccswarm/src/resource/tests.rs:22-32 function test_agent_resource_state_new <-> ./crates/ccswarm/src/resource/tests.rs:199-215 function test_resource_monitor_basic
  Similarity: 71.37%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_agent_resource_state_new (lines 22-32) ---[0m
    fn test_agent_resource_state_new() {
        let limits = ResourceLimits::default();
        let state = AgentResourceState::new("test-agent".to_string(), Some(1234), limits);
        
        assert_eq!(state.agent_id, "test-agent");
        assert_eq!(state.pid, Some(1234));
        assert!(!state.is_suspended);
        assert_eq!(state.limit_violations, 0);
        assert_eq!(state.current_usage.cpu_percent, 0.0);
        assert_eq!(state.current_usage.memory_bytes, 0);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_monitor_basic (lines 199-215) ---[0m
    async fn test_resource_monitor_basic() {
        let limits = ResourceLimits::default();
        let monitor = Arc::new(ResourceMonitor::new(limits));
        
        // Start monitoring an agent
        monitor.start_monitoring("test-agent".to_string(), Some(1234), None).unwrap();
        
        // Check agent state exists
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_some());
        assert_eq!(state.unwrap().agent_id, "test-agent");
        
        // Stop monitoring
        monitor.stop_monitoring("test-agent").unwrap();
        let state = monitor.get_agent_state("test-agent");
        assert!(state.is_none());
    }

  ./crates/ccswarm/src/resource/tests.rs:22-32 function test_agent_resource_state_new <-> ./crates/ccswarm/src/resource/tests.rs:257-269 function test_efficiency_stats_with_agents
  Similarity: 81.67%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_agent_resource_state_new (lines 22-32) ---[0m
    fn test_agent_resource_state_new() {
        let limits = ResourceLimits::default();
        let state = AgentResourceState::new("test-agent".to_string(), Some(1234), limits);
        
        assert_eq!(state.agent_id, "test-agent");
        assert_eq!(state.pid, Some(1234));
        assert!(!state.is_suspended);
        assert_eq!(state.limit_violations, 0);
        assert_eq!(state.current_usage.cpu_percent, 0.0);
        assert_eq!(state.current_usage.memory_bytes, 0);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_with_agents (lines 257-269) ---[0m
    fn test_efficiency_stats_with_agents() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        // Add some agents
        monitor.start_monitoring("agent1".to_string(), Some(1001), None).unwrap();
        monitor.start_monitoring("agent2".to_string(), Some(1002), None).unwrap();
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 2);
        assert_eq!(stats.active_agents, 2);
        assert_eq!(stats.suspended_agents, 0);
    }

  ./crates/ccswarm/src/resource/tests.rs:10-19 function test_resource_limits_default <-> ./crates/ccswarm/src/resource/tests.rs:78-94 function test_should_suspend_idle
  Similarity: 71.27%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_limits_default (lines 10-19) ---[0m
    fn test_resource_limits_default() {
        let limits = ResourceLimits::default();
        assert_eq!(limits.max_cpu_percent, 80.0);
        assert_eq!(limits.max_memory_bytes, 2 * 1024 * 1024 * 1024); // 2GB
        assert_eq!(limits.max_memory_percent, 50.0);
        assert_eq!(limits.idle_timeout, ChronoDuration::minutes(15));
        assert_eq!(limits.idle_cpu_threshold, 5.0);
        assert!(limits.auto_suspend_enabled);
        assert!(!limits.enforce_limits);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_should_suspend_idle (lines 78-94) ---[0m
    fn test_should_suspend_idle() {
        let mut limits = ResourceLimits::default();
        limits.idle_timeout = ChronoDuration::seconds(10); // Short timeout for testing
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        
        // Initially should not suspend
        assert!(!state.should_suspend());
        
        // Make agent idle by setting last_active to past
        state.last_active = Utc::now() - ChronoDuration::seconds(20);
        assert!(state.should_suspend());
        
        // Suspended agents should not be suspended again
        state.is_suspended = true;
        assert!(!state.should_suspend());
    }

  ./crates/ccswarm/src/resource/tests.rs:10-19 function test_resource_limits_default <-> ./crates/ccswarm/src/resource/tests.rs:257-269 function test_efficiency_stats_with_agents
  Similarity: 83.21%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_limits_default (lines 10-19) ---[0m
    fn test_resource_limits_default() {
        let limits = ResourceLimits::default();
        assert_eq!(limits.max_cpu_percent, 80.0);
        assert_eq!(limits.max_memory_bytes, 2 * 1024 * 1024 * 1024); // 2GB
        assert_eq!(limits.max_memory_percent, 50.0);
        assert_eq!(limits.idle_timeout, ChronoDuration::minutes(15));
        assert_eq!(limits.idle_cpu_threshold, 5.0);
        assert!(limits.auto_suspend_enabled);
        assert!(!limits.enforce_limits);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_with_agents (lines 257-269) ---[0m
    fn test_efficiency_stats_with_agents() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        // Add some agents
        monitor.start_monitoring("agent1".to_string(), Some(1001), None).unwrap();
        monitor.start_monitoring("agent2".to_string(), Some(1002), None).unwrap();
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 2);
        assert_eq!(stats.active_agents, 2);
        assert_eq!(stats.suspended_agents, 0);
    }

  ./crates/ccswarm/src/resource/tests.rs:22-32 function test_agent_resource_state_new <-> ./crates/ccswarm/src/resource/tests.rs:244-254 function test_efficiency_stats_empty
  Similarity: 84.59%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_agent_resource_state_new (lines 22-32) ---[0m
    fn test_agent_resource_state_new() {
        let limits = ResourceLimits::default();
        let state = AgentResourceState::new("test-agent".to_string(), Some(1234), limits);
        
        assert_eq!(state.agent_id, "test-agent");
        assert_eq!(state.pid, Some(1234));
        assert!(!state.is_suspended);
        assert_eq!(state.limit_violations, 0);
        assert_eq!(state.current_usage.cpu_percent, 0.0);
        assert_eq!(state.current_usage.memory_bytes, 0);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_empty (lines 244-254) ---[0m
    fn test_efficiency_stats_empty() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 0);
        assert_eq!(stats.active_agents, 0);
        assert_eq!(stats.suspended_agents, 0);
        assert_eq!(stats.average_cpu_usage, 0.0);
        assert_eq!(stats.suspension_rate, 0.0);
    }

  ./crates/ccswarm/src/resource/tests.rs:97-107 function test_should_suspend_disabled <-> ./crates/ccswarm/src/resource/tests.rs:257-269 function test_efficiency_stats_with_agents
  Similarity: 72.88%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_should_suspend_disabled (lines 97-107) ---[0m
    fn test_should_suspend_disabled() {
        let mut limits = ResourceLimits::default();
        limits.auto_suspend_enabled = false;
        limits.idle_timeout = ChronoDuration::seconds(1);
        
        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);
        state.last_active = Utc::now() - ChronoDuration::seconds(10);
        
        // Should not suspend when disabled
        assert!(!state.should_suspend());
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_with_agents (lines 257-269) ---[0m
    fn test_efficiency_stats_with_agents() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        // Add some agents
        monitor.start_monitoring("agent1".to_string(), Some(1001), None).unwrap();
        monitor.start_monitoring("agent2".to_string(), Some(1002), None).unwrap();
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 2);
        assert_eq!(stats.active_agents, 2);
        assert_eq!(stats.suspended_agents, 0);
    }

  ./crates/ccswarm/src/resource/tests.rs:10-19 function test_resource_limits_default <-> ./crates/ccswarm/src/resource/tests.rs:244-254 function test_efficiency_stats_empty
  Similarity: 78.97%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_limits_default (lines 10-19) ---[0m
    fn test_resource_limits_default() {
        let limits = ResourceLimits::default();
        assert_eq!(limits.max_cpu_percent, 80.0);
        assert_eq!(limits.max_memory_bytes, 2 * 1024 * 1024 * 1024); // 2GB
        assert_eq!(limits.max_memory_percent, 50.0);
        assert_eq!(limits.idle_timeout, ChronoDuration::minutes(15));
        assert_eq!(limits.idle_cpu_threshold, 5.0);
        assert!(limits.auto_suspend_enabled);
        assert!(!limits.enforce_limits);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_efficiency_stats_empty (lines 244-254) ---[0m
    fn test_efficiency_stats_empty() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);
        
        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 0);
        assert_eq!(stats.active_agents, 0);
        assert_eq!(stats.suspended_agents, 0);
        assert_eq!(stats.average_cpu_usage, 0.0);
        assert_eq!(stats.suspension_rate, 0.0);
    }

  ./crates/ccswarm/src/resource/tests.rs:10-19 function test_resource_limits_default <-> ./crates/ccswarm/src/resource/tests.rs:22-32 function test_agent_resource_state_new
  Similarity: 75.04%

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_resource_limits_default (lines 10-19) ---[0m
    fn test_resource_limits_default() {
        let limits = ResourceLimits::default();
        assert_eq!(limits.max_cpu_percent, 80.0);
        assert_eq!(limits.max_memory_bytes, 2 * 1024 * 1024 * 1024); // 2GB
        assert_eq!(limits.max_memory_percent, 50.0);
        assert_eq!(limits.idle_timeout, ChronoDuration::minutes(15));
        assert_eq!(limits.idle_cpu_threshold, 5.0);
        assert!(limits.auto_suspend_enabled);
        assert!(!limits.enforce_limits);
    }

[36m--- ./crates/ccswarm/src/resource/tests.rs:test_agent_resource_state_new (lines 22-32) ---[0m
    fn test_agent_resource_state_new() {
        let limits = ResourceLimits::default();
        let state = AgentResourceState::new("test-agent".to_string(), Some(1234), limits);
        
        assert_eq!(state.agent_id, "test-agent");
        assert_eq!(state.pid, Some(1234));
        assert!(!state.is_suspended);
        assert_eq!(state.limit_violations, 0);
        assert_eq!(state.current_usage.cpu_percent, 0.0);
        assert_eq!(state.current_usage.memory_bytes, 0);
    }


Duplicates in ./crates/ccswarm/src/agent/isolation.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/agent/isolation.rs:127-131 function test_isolation_mode_requires_docker <-> ./crates/ccswarm/src/agent/isolation.rs:134-138 function test_isolation_mode_uses_worktree
  Similarity: 94.20%

[36m--- ./crates/ccswarm/src/agent/isolation.rs:test_isolation_mode_requires_docker (lines 127-131) ---[0m
    fn test_isolation_mode_requires_docker() {
        assert!(!IsolationMode::GitWorktree.requires_docker());
        assert!(IsolationMode::Container.requires_docker());
        assert!(IsolationMode::Hybrid.requires_docker());
    }

[36m--- ./crates/ccswarm/src/agent/isolation.rs:test_isolation_mode_uses_worktree (lines 134-138) ---[0m
    fn test_isolation_mode_uses_worktree() {
        assert!(IsolationMode::GitWorktree.uses_worktree());
        assert!(!IsolationMode::Container.uses_worktree());
        assert!(IsolationMode::Hybrid.uses_worktree());
    }


Duplicates in ./crates/ccswarm/src/tests/identity_tests.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/tests/identity_tests.rs:192-217 function test_frontend_boundary_checker_delegates_backend_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:244-268 function test_backend_boundary_checker_delegates_ui_tasks
  Similarity: 96.91%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_frontend_boundary_checker_delegates_backend_tasks (lines 192-217) ---[0m
    async fn test_frontend_boundary_checker_delegates_backend_tasks() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let backend_tasks = vec![
            create_test_task(
                "Create REST API endpoint for authentication",
                TaskType::Development,
            ),
            create_test_task("Design database schema for users", TaskType::Development),
            create_test_task("Implement JWT token validation", TaskType::Development),
            create_test_task("Set up GraphQL resolver", TaskType::Development),
        ];

        for task in backend_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "Frontend should delegate backend task: {}",
                task.description
            );

            if let TaskEvaluation::Delegate { target_agent, .. } = result {
                assert_eq!(target_agent, "backend-agent");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_backend_boundary_checker_delegates_ui_tasks (lines 244-268) ---[0m
    async fn test_backend_boundary_checker_delegates_ui_tasks() {
        let checker = TaskBoundaryChecker::new(default_backend_role());

        let ui_tasks = vec![
            create_test_task(
                "Create React component for login form",
                TaskType::Development,
            ),
            create_test_task("Style the user dashboard with CSS", TaskType::Development),
            create_test_task("Implement frontend routing", TaskType::Development),
        ];

        for task in ui_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "Backend should delegate UI task: {}",
                task.description
            );

            if let TaskEvaluation::Delegate { target_agent, .. } = result {
                assert_eq!(target_agent, "frontend-agent");
            }
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:296-322 function test_devops_boundary_checker_delegates_application_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:425-452 function test_boundary_checker_with_mixed_keywords
  Similarity: 86.48%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_delegates_application_tasks (lines 296-322) ---[0m
    async fn test_devops_boundary_checker_delegates_application_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        // Tasks that clearly match forbidden patterns for DevOps
        let app_tasks = vec![
            create_test_task("Implement new feature in the UI", TaskType::Feature),
            create_test_task("Fix React component rendering issue", TaskType::Bugfix),
            create_test_task(
                "Add API endpoint for user management",
                TaskType::Development,
            ),
        ];

        for task in app_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "DevOps should delegate application task: {}",
                task.description
            );
        }

        // Task that's ambiguous should trigger clarification
        let ambiguous = create_test_task("Fix authentication issue", TaskType::Bugfix);
        let result = checker.evaluate_task(&ambiguous).await;
        assert!(matches!(result, TaskEvaluation::Clarify { .. }));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_with_mixed_keywords (lines 425-452) ---[0m
    async fn test_boundary_checker_with_mixed_keywords() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Task that contains both frontend and backend keywords
        let mixed_task = create_test_task(
            "Create a React component that calls the backend API",
            TaskType::Development,
        );

        let result = checker.evaluate_task(&mixed_task).await;

        // Should delegate because it mentions "API" which is forbidden for frontend
        assert!(matches!(result, TaskEvaluation::Delegate { .. }));

        if let TaskEvaluation::Delegate { target_agent, .. } = result {
            assert_eq!(target_agent, "backend-agent");
        }

        // Task that is clearly frontend despite mentioning data
        let frontend_focused = create_test_task(
            "Create a React component to display user data",
            TaskType::Development,
        );

        let result2 = checker.evaluate_task(&frontend_focused).await;
        // This should be accepted as it's about displaying data, not API work
        assert!(matches!(result2, TaskEvaluation::Accept { .. }));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:192-217 function test_frontend_boundary_checker_delegates_backend_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:367-391 function test_ambiguous_task_triggers_clarification
  Similarity: 92.81%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_frontend_boundary_checker_delegates_backend_tasks (lines 192-217) ---[0m
    async fn test_frontend_boundary_checker_delegates_backend_tasks() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let backend_tasks = vec![
            create_test_task(
                "Create REST API endpoint for authentication",
                TaskType::Development,
            ),
            create_test_task("Design database schema for users", TaskType::Development),
            create_test_task("Implement JWT token validation", TaskType::Development),
            create_test_task("Set up GraphQL resolver", TaskType::Development),
        ];

        for task in backend_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "Frontend should delegate backend task: {}",
                task.description
            );

            if let TaskEvaluation::Delegate { target_agent, .. } = result {
                assert_eq!(target_agent, "backend-agent");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_ambiguous_task_triggers_clarification (lines 367-391) ---[0m
    async fn test_ambiguous_task_triggers_clarification() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Tasks that are truly ambiguous (could be frontend or backend)
        let ambiguous_tasks = vec![
            create_test_task("Update the user system", TaskType::Development),
            create_test_task("Optimize the application", TaskType::Development),
            create_test_task("Refactor the module", TaskType::Development),
        ];

        for task in ambiguous_tasks {
            let result = checker.evaluate_task(&task).await;
            // These tasks don't clearly match any patterns, so they should trigger clarification
            assert!(
                matches!(result, TaskEvaluation::Clarify { .. }),
                "Ambiguous task should trigger clarification: {}",
                task.description
            );

            if let TaskEvaluation::Clarify { questions, .. } = result {
                assert!(!questions.is_empty());
                assert!(questions.iter().any(|q| q.contains("Frontend")));
            }
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:192-217 function test_frontend_boundary_checker_delegates_backend_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:271-293 function test_devops_boundary_checker_accepts_infrastructure_tasks
  Similarity: 95.79%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_frontend_boundary_checker_delegates_backend_tasks (lines 192-217) ---[0m
    async fn test_frontend_boundary_checker_delegates_backend_tasks() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let backend_tasks = vec![
            create_test_task(
                "Create REST API endpoint for authentication",
                TaskType::Development,
            ),
            create_test_task("Design database schema for users", TaskType::Development),
            create_test_task("Implement JWT token validation", TaskType::Development),
            create_test_task("Set up GraphQL resolver", TaskType::Development),
        ];

        for task in backend_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "Frontend should delegate backend task: {}",
                task.description
            );

            if let TaskEvaluation::Delegate { target_agent, .. } = result {
                assert_eq!(target_agent, "backend-agent");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_accepts_infrastructure_tasks (lines 271-293) ---[0m
    async fn test_devops_boundary_checker_accepts_infrastructure_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        let infra_tasks = vec![
            create_test_task("Set up Kubernetes cluster", TaskType::Infrastructure),
            create_test_task("Configure Docker containers", TaskType::Infrastructure),
            create_test_task("Deploy application to AWS", TaskType::Infrastructure),
            create_test_task("Set up CI/CD pipeline", TaskType::Infrastructure),
            create_test_task(
                "Configure monitoring with Prometheus",
                TaskType::Infrastructure,
            ),
        ];

        for task in infra_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "DevOps should accept infrastructure task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:244-268 function test_backend_boundary_checker_delegates_ui_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:367-391 function test_ambiguous_task_triggers_clarification
  Similarity: 93.72%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_backend_boundary_checker_delegates_ui_tasks (lines 244-268) ---[0m
    async fn test_backend_boundary_checker_delegates_ui_tasks() {
        let checker = TaskBoundaryChecker::new(default_backend_role());

        let ui_tasks = vec![
            create_test_task(
                "Create React component for login form",
                TaskType::Development,
            ),
            create_test_task("Style the user dashboard with CSS", TaskType::Development),
            create_test_task("Implement frontend routing", TaskType::Development),
        ];

        for task in ui_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "Backend should delegate UI task: {}",
                task.description
            );

            if let TaskEvaluation::Delegate { target_agent, .. } = result {
                assert_eq!(target_agent, "frontend-agent");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_ambiguous_task_triggers_clarification (lines 367-391) ---[0m
    async fn test_ambiguous_task_triggers_clarification() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Tasks that are truly ambiguous (could be frontend or backend)
        let ambiguous_tasks = vec![
            create_test_task("Update the user system", TaskType::Development),
            create_test_task("Optimize the application", TaskType::Development),
            create_test_task("Refactor the module", TaskType::Development),
        ];

        for task in ambiguous_tasks {
            let result = checker.evaluate_task(&task).await;
            // These tasks don't clearly match any patterns, so they should trigger clarification
            assert!(
                matches!(result, TaskEvaluation::Clarify { .. }),
                "Ambiguous task should trigger clarification: {}",
                task.description
            );

            if let TaskEvaluation::Clarify { questions, .. } = result {
                assert!(!questions.is_empty());
                assert!(questions.iter().any(|q| q.contains("Frontend")));
            }
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:168-189 function test_frontend_boundary_checker_accepts_ui_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:192-217 function test_frontend_boundary_checker_delegates_backend_tasks
  Similarity: 97.36%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_frontend_boundary_checker_accepts_ui_tasks (lines 168-189) ---[0m
    async fn test_frontend_boundary_checker_accepts_ui_tasks() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let ui_tasks = vec![
            create_test_task(
                "Create a React component for user profile",
                TaskType::Development,
            ),
            create_test_task("Implement CSS styling with Tailwind", TaskType::Development),
            create_test_task("Add state management with Redux", TaskType::Development),
            create_test_task("Write Jest tests for components", TaskType::Testing),
        ];

        for task in ui_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Frontend should accept UI task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_frontend_boundary_checker_delegates_backend_tasks (lines 192-217) ---[0m
    async fn test_frontend_boundary_checker_delegates_backend_tasks() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let backend_tasks = vec![
            create_test_task(
                "Create REST API endpoint for authentication",
                TaskType::Development,
            ),
            create_test_task("Design database schema for users", TaskType::Development),
            create_test_task("Implement JWT token validation", TaskType::Development),
            create_test_task("Set up GraphQL resolver", TaskType::Development),
        ];

        for task in backend_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "Frontend should delegate backend task: {}",
                task.description
            );

            if let TaskEvaluation::Delegate { target_agent, .. } = result {
                assert_eq!(target_agent, "backend-agent");
            }
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:192-217 function test_frontend_boundary_checker_delegates_backend_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:220-241 function test_backend_boundary_checker_accepts_api_tasks
  Similarity: 96.24%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_frontend_boundary_checker_delegates_backend_tasks (lines 192-217) ---[0m
    async fn test_frontend_boundary_checker_delegates_backend_tasks() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let backend_tasks = vec![
            create_test_task(
                "Create REST API endpoint for authentication",
                TaskType::Development,
            ),
            create_test_task("Design database schema for users", TaskType::Development),
            create_test_task("Implement JWT token validation", TaskType::Development),
            create_test_task("Set up GraphQL resolver", TaskType::Development),
        ];

        for task in backend_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "Frontend should delegate backend task: {}",
                task.description
            );

            if let TaskEvaluation::Delegate { target_agent, .. } = result {
                assert_eq!(target_agent, "backend-agent");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_backend_boundary_checker_accepts_api_tasks (lines 220-241) ---[0m
    async fn test_backend_boundary_checker_accepts_api_tasks() {
        let checker = TaskBoundaryChecker::new(default_backend_role());

        let api_tasks = vec![
            create_test_task("Create REST API for user management", TaskType::Development),
            create_test_task(
                "Implement database query optimization",
                TaskType::Development,
            ),
            create_test_task("Add OAuth authentication flow", TaskType::Development),
            create_test_task("Set up Prisma ORM models", TaskType::Development),
        ];

        for task in api_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Backend should accept API task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:244-268 function test_backend_boundary_checker_delegates_ui_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:271-293 function test_devops_boundary_checker_accepts_infrastructure_tasks
  Similarity: 95.45%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_backend_boundary_checker_delegates_ui_tasks (lines 244-268) ---[0m
    async fn test_backend_boundary_checker_delegates_ui_tasks() {
        let checker = TaskBoundaryChecker::new(default_backend_role());

        let ui_tasks = vec![
            create_test_task(
                "Create React component for login form",
                TaskType::Development,
            ),
            create_test_task("Style the user dashboard with CSS", TaskType::Development),
            create_test_task("Implement frontend routing", TaskType::Development),
        ];

        for task in ui_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "Backend should delegate UI task: {}",
                task.description
            );

            if let TaskEvaluation::Delegate { target_agent, .. } = result {
                assert_eq!(target_agent, "frontend-agent");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_accepts_infrastructure_tasks (lines 271-293) ---[0m
    async fn test_devops_boundary_checker_accepts_infrastructure_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        let infra_tasks = vec![
            create_test_task("Set up Kubernetes cluster", TaskType::Infrastructure),
            create_test_task("Configure Docker containers", TaskType::Infrastructure),
            create_test_task("Deploy application to AWS", TaskType::Infrastructure),
            create_test_task("Set up CI/CD pipeline", TaskType::Infrastructure),
            create_test_task(
                "Configure monitoring with Prometheus",
                TaskType::Infrastructure,
            ),
        ];

        for task in infra_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "DevOps should accept infrastructure task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:271-293 function test_devops_boundary_checker_accepts_infrastructure_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:367-391 function test_ambiguous_task_triggers_clarification
  Similarity: 95.23%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_accepts_infrastructure_tasks (lines 271-293) ---[0m
    async fn test_devops_boundary_checker_accepts_infrastructure_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        let infra_tasks = vec![
            create_test_task("Set up Kubernetes cluster", TaskType::Infrastructure),
            create_test_task("Configure Docker containers", TaskType::Infrastructure),
            create_test_task("Deploy application to AWS", TaskType::Infrastructure),
            create_test_task("Set up CI/CD pipeline", TaskType::Infrastructure),
            create_test_task(
                "Configure monitoring with Prometheus",
                TaskType::Infrastructure,
            ),
        ];

        for task in infra_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "DevOps should accept infrastructure task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_ambiguous_task_triggers_clarification (lines 367-391) ---[0m
    async fn test_ambiguous_task_triggers_clarification() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Tasks that are truly ambiguous (could be frontend or backend)
        let ambiguous_tasks = vec![
            create_test_task("Update the user system", TaskType::Development),
            create_test_task("Optimize the application", TaskType::Development),
            create_test_task("Refactor the module", TaskType::Development),
        ];

        for task in ambiguous_tasks {
            let result = checker.evaluate_task(&task).await;
            // These tasks don't clearly match any patterns, so they should trigger clarification
            assert!(
                matches!(result, TaskEvaluation::Clarify { .. }),
                "Ambiguous task should trigger clarification: {}",
                task.description
            );

            if let TaskEvaluation::Clarify { questions, .. } = result {
                assert!(!questions.is_empty());
                assert!(questions.iter().any(|q| q.contains("Frontend")));
            }
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:168-189 function test_frontend_boundary_checker_accepts_ui_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:244-268 function test_backend_boundary_checker_delegates_ui_tasks
  Similarity: 95.82%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_frontend_boundary_checker_accepts_ui_tasks (lines 168-189) ---[0m
    async fn test_frontend_boundary_checker_accepts_ui_tasks() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let ui_tasks = vec![
            create_test_task(
                "Create a React component for user profile",
                TaskType::Development,
            ),
            create_test_task("Implement CSS styling with Tailwind", TaskType::Development),
            create_test_task("Add state management with Redux", TaskType::Development),
            create_test_task("Write Jest tests for components", TaskType::Testing),
        ];

        for task in ui_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Frontend should accept UI task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_backend_boundary_checker_delegates_ui_tasks (lines 244-268) ---[0m
    async fn test_backend_boundary_checker_delegates_ui_tasks() {
        let checker = TaskBoundaryChecker::new(default_backend_role());

        let ui_tasks = vec![
            create_test_task(
                "Create React component for login form",
                TaskType::Development,
            ),
            create_test_task("Style the user dashboard with CSS", TaskType::Development),
            create_test_task("Implement frontend routing", TaskType::Development),
        ];

        for task in ui_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "Backend should delegate UI task: {}",
                task.description
            );

            if let TaskEvaluation::Delegate { target_agent, .. } = result {
                assert_eq!(target_agent, "frontend-agent");
            }
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:220-241 function test_backend_boundary_checker_accepts_api_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:244-268 function test_backend_boundary_checker_delegates_ui_tasks
  Similarity: 95.64%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_backend_boundary_checker_accepts_api_tasks (lines 220-241) ---[0m
    async fn test_backend_boundary_checker_accepts_api_tasks() {
        let checker = TaskBoundaryChecker::new(default_backend_role());

        let api_tasks = vec![
            create_test_task("Create REST API for user management", TaskType::Development),
            create_test_task(
                "Implement database query optimization",
                TaskType::Development,
            ),
            create_test_task("Add OAuth authentication flow", TaskType::Development),
            create_test_task("Set up Prisma ORM models", TaskType::Development),
        ];

        for task in api_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Backend should accept API task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_backend_boundary_checker_delegates_ui_tasks (lines 244-268) ---[0m
    async fn test_backend_boundary_checker_delegates_ui_tasks() {
        let checker = TaskBoundaryChecker::new(default_backend_role());

        let ui_tasks = vec![
            create_test_task(
                "Create React component for login form",
                TaskType::Development,
            ),
            create_test_task("Style the user dashboard with CSS", TaskType::Development),
            create_test_task("Implement frontend routing", TaskType::Development),
        ];

        for task in ui_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "Backend should delegate UI task: {}",
                task.description
            );

            if let TaskEvaluation::Delegate { target_agent, .. } = result {
                assert_eq!(target_agent, "frontend-agent");
            }
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:168-189 function test_frontend_boundary_checker_accepts_ui_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:367-391 function test_ambiguous_task_triggers_clarification
  Similarity: 95.38%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_frontend_boundary_checker_accepts_ui_tasks (lines 168-189) ---[0m
    async fn test_frontend_boundary_checker_accepts_ui_tasks() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let ui_tasks = vec![
            create_test_task(
                "Create a React component for user profile",
                TaskType::Development,
            ),
            create_test_task("Implement CSS styling with Tailwind", TaskType::Development),
            create_test_task("Add state management with Redux", TaskType::Development),
            create_test_task("Write Jest tests for components", TaskType::Testing),
        ];

        for task in ui_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Frontend should accept UI task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_ambiguous_task_triggers_clarification (lines 367-391) ---[0m
    async fn test_ambiguous_task_triggers_clarification() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Tasks that are truly ambiguous (could be frontend or backend)
        let ambiguous_tasks = vec![
            create_test_task("Update the user system", TaskType::Development),
            create_test_task("Optimize the application", TaskType::Development),
            create_test_task("Refactor the module", TaskType::Development),
        ];

        for task in ambiguous_tasks {
            let result = checker.evaluate_task(&task).await;
            // These tasks don't clearly match any patterns, so they should trigger clarification
            assert!(
                matches!(result, TaskEvaluation::Clarify { .. }),
                "Ambiguous task should trigger clarification: {}",
                task.description
            );

            if let TaskEvaluation::Clarify { questions, .. } = result {
                assert!(!questions.is_empty());
                assert!(questions.iter().any(|q| q.contains("Frontend")));
            }
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:220-241 function test_backend_boundary_checker_accepts_api_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:367-391 function test_ambiguous_task_triggers_clarification
  Similarity: 95.23%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_backend_boundary_checker_accepts_api_tasks (lines 220-241) ---[0m
    async fn test_backend_boundary_checker_accepts_api_tasks() {
        let checker = TaskBoundaryChecker::new(default_backend_role());

        let api_tasks = vec![
            create_test_task("Create REST API for user management", TaskType::Development),
            create_test_task(
                "Implement database query optimization",
                TaskType::Development,
            ),
            create_test_task("Add OAuth authentication flow", TaskType::Development),
            create_test_task("Set up Prisma ORM models", TaskType::Development),
        ];

        for task in api_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Backend should accept API task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_ambiguous_task_triggers_clarification (lines 367-391) ---[0m
    async fn test_ambiguous_task_triggers_clarification() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Tasks that are truly ambiguous (could be frontend or backend)
        let ambiguous_tasks = vec![
            create_test_task("Update the user system", TaskType::Development),
            create_test_task("Optimize the application", TaskType::Development),
            create_test_task("Refactor the module", TaskType::Development),
        ];

        for task in ambiguous_tasks {
            let result = checker.evaluate_task(&task).await;
            // These tasks don't clearly match any patterns, so they should trigger clarification
            assert!(
                matches!(result, TaskEvaluation::Clarify { .. }),
                "Ambiguous task should trigger clarification: {}",
                task.description
            );

            if let TaskEvaluation::Clarify { questions, .. } = result {
                assert!(!questions.is_empty());
                assert!(questions.iter().any(|q| q.contains("Frontend")));
            }
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:168-189 function test_frontend_boundary_checker_accepts_ui_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:271-293 function test_devops_boundary_checker_accepts_infrastructure_tasks
  Similarity: 96.65%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_frontend_boundary_checker_accepts_ui_tasks (lines 168-189) ---[0m
    async fn test_frontend_boundary_checker_accepts_ui_tasks() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let ui_tasks = vec![
            create_test_task(
                "Create a React component for user profile",
                TaskType::Development,
            ),
            create_test_task("Implement CSS styling with Tailwind", TaskType::Development),
            create_test_task("Add state management with Redux", TaskType::Development),
            create_test_task("Write Jest tests for components", TaskType::Testing),
        ];

        for task in ui_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Frontend should accept UI task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_accepts_infrastructure_tasks (lines 271-293) ---[0m
    async fn test_devops_boundary_checker_accepts_infrastructure_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        let infra_tasks = vec![
            create_test_task("Set up Kubernetes cluster", TaskType::Infrastructure),
            create_test_task("Configure Docker containers", TaskType::Infrastructure),
            create_test_task("Deploy application to AWS", TaskType::Infrastructure),
            create_test_task("Set up CI/CD pipeline", TaskType::Infrastructure),
            create_test_task(
                "Configure monitoring with Prometheus",
                TaskType::Infrastructure,
            ),
        ];

        for task in infra_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "DevOps should accept infrastructure task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:220-241 function test_backend_boundary_checker_accepts_api_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:271-293 function test_devops_boundary_checker_accepts_infrastructure_tasks
  Similarity: 96.65%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_backend_boundary_checker_accepts_api_tasks (lines 220-241) ---[0m
    async fn test_backend_boundary_checker_accepts_api_tasks() {
        let checker = TaskBoundaryChecker::new(default_backend_role());

        let api_tasks = vec![
            create_test_task("Create REST API for user management", TaskType::Development),
            create_test_task(
                "Implement database query optimization",
                TaskType::Development,
            ),
            create_test_task("Add OAuth authentication flow", TaskType::Development),
            create_test_task("Set up Prisma ORM models", TaskType::Development),
        ];

        for task in api_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Backend should accept API task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_accepts_infrastructure_tasks (lines 271-293) ---[0m
    async fn test_devops_boundary_checker_accepts_infrastructure_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        let infra_tasks = vec![
            create_test_task("Set up Kubernetes cluster", TaskType::Infrastructure),
            create_test_task("Configure Docker containers", TaskType::Infrastructure),
            create_test_task("Deploy application to AWS", TaskType::Infrastructure),
            create_test_task("Set up CI/CD pipeline", TaskType::Infrastructure),
            create_test_task(
                "Configure monitoring with Prometheus",
                TaskType::Infrastructure,
            ),
        ];

        for task in infra_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "DevOps should accept infrastructure task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:192-217 function test_frontend_boundary_checker_delegates_backend_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:325-343 function test_qa_boundary_checker_accepts_testing_tasks
  Similarity: 96.12%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_frontend_boundary_checker_delegates_backend_tasks (lines 192-217) ---[0m
    async fn test_frontend_boundary_checker_delegates_backend_tasks() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let backend_tasks = vec![
            create_test_task(
                "Create REST API endpoint for authentication",
                TaskType::Development,
            ),
            create_test_task("Design database schema for users", TaskType::Development),
            create_test_task("Implement JWT token validation", TaskType::Development),
            create_test_task("Set up GraphQL resolver", TaskType::Development),
        ];

        for task in backend_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "Frontend should delegate backend task: {}",
                task.description
            );

            if let TaskEvaluation::Delegate { target_agent, .. } = result {
                assert_eq!(target_agent, "backend-agent");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_qa_boundary_checker_accepts_testing_tasks (lines 325-343) ---[0m
    async fn test_qa_boundary_checker_accepts_testing_tasks() {
        let checker = TaskBoundaryChecker::new(default_qa_role());

        let testing_tasks = vec![
            create_test_task("Write end-to-end tests with Cypress", TaskType::Testing),
            create_test_task("Perform load testing with K6", TaskType::Testing),
            create_test_task("Execute security penetration testing", TaskType::Testing),
            create_test_task("Create test automation framework", TaskType::Testing),
        ];

        for task in testing_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "QA should accept testing task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:168-189 function test_frontend_boundary_checker_accepts_ui_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:220-241 function test_backend_boundary_checker_accepts_api_tasks
  Similarity: 96.69%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_frontend_boundary_checker_accepts_ui_tasks (lines 168-189) ---[0m
    async fn test_frontend_boundary_checker_accepts_ui_tasks() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let ui_tasks = vec![
            create_test_task(
                "Create a React component for user profile",
                TaskType::Development,
            ),
            create_test_task("Implement CSS styling with Tailwind", TaskType::Development),
            create_test_task("Add state management with Redux", TaskType::Development),
            create_test_task("Write Jest tests for components", TaskType::Testing),
        ];

        for task in ui_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Frontend should accept UI task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_backend_boundary_checker_accepts_api_tasks (lines 220-241) ---[0m
    async fn test_backend_boundary_checker_accepts_api_tasks() {
        let checker = TaskBoundaryChecker::new(default_backend_role());

        let api_tasks = vec![
            create_test_task("Create REST API for user management", TaskType::Development),
            create_test_task(
                "Implement database query optimization",
                TaskType::Development,
            ),
            create_test_task("Add OAuth authentication flow", TaskType::Development),
            create_test_task("Set up Prisma ORM models", TaskType::Development),
        ];

        for task in api_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Backend should accept API task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:192-217 function test_frontend_boundary_checker_delegates_backend_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:347-364 function test_qa_boundary_checker_delegates_implementation_tasks
  Similarity: 95.96%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_frontend_boundary_checker_delegates_backend_tasks (lines 192-217) ---[0m
    async fn test_frontend_boundary_checker_delegates_backend_tasks() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let backend_tasks = vec![
            create_test_task(
                "Create REST API endpoint for authentication",
                TaskType::Development,
            ),
            create_test_task("Design database schema for users", TaskType::Development),
            create_test_task("Implement JWT token validation", TaskType::Development),
            create_test_task("Set up GraphQL resolver", TaskType::Development),
        ];

        for task in backend_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "Frontend should delegate backend task: {}",
                task.description
            );

            if let TaskEvaluation::Delegate { target_agent, .. } = result {
                assert_eq!(target_agent, "backend-agent");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_qa_boundary_checker_delegates_implementation_tasks (lines 347-364) ---[0m
    async fn test_qa_boundary_checker_delegates_implementation_tasks() {
        let checker = TaskBoundaryChecker::new(default_qa_role());

        let impl_tasks = vec![
            create_test_task("Implement new user registration feature", TaskType::Feature),
            create_test_task("Fix production bug in payment system", TaskType::Bugfix),
            create_test_task("Deploy application to production", TaskType::Infrastructure),
        ];

        for task in impl_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "QA should delegate implementation task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:192-217 function test_frontend_boundary_checker_delegates_backend_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:455-472 function test_boundary_checker_case_insensitive
  Similarity: 95.96%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_frontend_boundary_checker_delegates_backend_tasks (lines 192-217) ---[0m
    async fn test_frontend_boundary_checker_delegates_backend_tasks() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let backend_tasks = vec![
            create_test_task(
                "Create REST API endpoint for authentication",
                TaskType::Development,
            ),
            create_test_task("Design database schema for users", TaskType::Development),
            create_test_task("Implement JWT token validation", TaskType::Development),
            create_test_task("Set up GraphQL resolver", TaskType::Development),
        ];

        for task in backend_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "Frontend should delegate backend task: {}",
                task.description
            );

            if let TaskEvaluation::Delegate { target_agent, .. } = result {
                assert_eq!(target_agent, "backend-agent");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_case_insensitive (lines 455-472) ---[0m
    async fn test_boundary_checker_case_insensitive() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let tasks = vec![
            create_test_task("CREATE A REACT COMPONENT", TaskType::Development),
            create_test_task("create a react component", TaskType::Development),
            create_test_task("Create A React Component", TaskType::Development),
        ];

        for task in tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Should be case insensitive: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:244-268 function test_backend_boundary_checker_delegates_ui_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:325-343 function test_qa_boundary_checker_accepts_testing_tasks
  Similarity: 95.45%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_backend_boundary_checker_delegates_ui_tasks (lines 244-268) ---[0m
    async fn test_backend_boundary_checker_delegates_ui_tasks() {
        let checker = TaskBoundaryChecker::new(default_backend_role());

        let ui_tasks = vec![
            create_test_task(
                "Create React component for login form",
                TaskType::Development,
            ),
            create_test_task("Style the user dashboard with CSS", TaskType::Development),
            create_test_task("Implement frontend routing", TaskType::Development),
        ];

        for task in ui_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "Backend should delegate UI task: {}",
                task.description
            );

            if let TaskEvaluation::Delegate { target_agent, .. } = result {
                assert_eq!(target_agent, "frontend-agent");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_qa_boundary_checker_accepts_testing_tasks (lines 325-343) ---[0m
    async fn test_qa_boundary_checker_accepts_testing_tasks() {
        let checker = TaskBoundaryChecker::new(default_qa_role());

        let testing_tasks = vec![
            create_test_task("Write end-to-end tests with Cypress", TaskType::Testing),
            create_test_task("Perform load testing with K6", TaskType::Testing),
            create_test_task("Execute security penetration testing", TaskType::Testing),
            create_test_task("Create test automation framework", TaskType::Testing),
        ];

        for task in testing_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "QA should accept testing task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:325-343 function test_qa_boundary_checker_accepts_testing_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:367-391 function test_ambiguous_task_triggers_clarification
  Similarity: 95.23%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_qa_boundary_checker_accepts_testing_tasks (lines 325-343) ---[0m
    async fn test_qa_boundary_checker_accepts_testing_tasks() {
        let checker = TaskBoundaryChecker::new(default_qa_role());

        let testing_tasks = vec![
            create_test_task("Write end-to-end tests with Cypress", TaskType::Testing),
            create_test_task("Perform load testing with K6", TaskType::Testing),
            create_test_task("Execute security penetration testing", TaskType::Testing),
            create_test_task("Create test automation framework", TaskType::Testing),
        ];

        for task in testing_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "QA should accept testing task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_ambiguous_task_triggers_clarification (lines 367-391) ---[0m
    async fn test_ambiguous_task_triggers_clarification() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Tasks that are truly ambiguous (could be frontend or backend)
        let ambiguous_tasks = vec![
            create_test_task("Update the user system", TaskType::Development),
            create_test_task("Optimize the application", TaskType::Development),
            create_test_task("Refactor the module", TaskType::Development),
        ];

        for task in ambiguous_tasks {
            let result = checker.evaluate_task(&task).await;
            // These tasks don't clearly match any patterns, so they should trigger clarification
            assert!(
                matches!(result, TaskEvaluation::Clarify { .. }),
                "Ambiguous task should trigger clarification: {}",
                task.description
            );

            if let TaskEvaluation::Clarify { questions, .. } = result {
                assert!(!questions.is_empty());
                assert!(questions.iter().any(|q| q.contains("Frontend")));
            }
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:367-391 function test_ambiguous_task_triggers_clarification <-> ./crates/ccswarm/src/tests/identity_tests.rs:455-472 function test_boundary_checker_case_insensitive
  Similarity: 96.93%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_ambiguous_task_triggers_clarification (lines 367-391) ---[0m
    async fn test_ambiguous_task_triggers_clarification() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Tasks that are truly ambiguous (could be frontend or backend)
        let ambiguous_tasks = vec![
            create_test_task("Update the user system", TaskType::Development),
            create_test_task("Optimize the application", TaskType::Development),
            create_test_task("Refactor the module", TaskType::Development),
        ];

        for task in ambiguous_tasks {
            let result = checker.evaluate_task(&task).await;
            // These tasks don't clearly match any patterns, so they should trigger clarification
            assert!(
                matches!(result, TaskEvaluation::Clarify { .. }),
                "Ambiguous task should trigger clarification: {}",
                task.description
            );

            if let TaskEvaluation::Clarify { questions, .. } = result {
                assert!(!questions.is_empty());
                assert!(questions.iter().any(|q| q.contains("Frontend")));
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_case_insensitive (lines 455-472) ---[0m
    async fn test_boundary_checker_case_insensitive() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let tasks = vec![
            create_test_task("CREATE A REACT COMPONENT", TaskType::Development),
            create_test_task("create a react component", TaskType::Development),
            create_test_task("Create A React Component", TaskType::Development),
        ];

        for task in tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Should be case insensitive: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:244-268 function test_backend_boundary_checker_delegates_ui_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:455-472 function test_boundary_checker_case_insensitive
  Similarity: 96.73%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_backend_boundary_checker_delegates_ui_tasks (lines 244-268) ---[0m
    async fn test_backend_boundary_checker_delegates_ui_tasks() {
        let checker = TaskBoundaryChecker::new(default_backend_role());

        let ui_tasks = vec![
            create_test_task(
                "Create React component for login form",
                TaskType::Development,
            ),
            create_test_task("Style the user dashboard with CSS", TaskType::Development),
            create_test_task("Implement frontend routing", TaskType::Development),
        ];

        for task in ui_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "Backend should delegate UI task: {}",
                task.description
            );

            if let TaskEvaluation::Delegate { target_agent, .. } = result {
                assert_eq!(target_agent, "frontend-agent");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_case_insensitive (lines 455-472) ---[0m
    async fn test_boundary_checker_case_insensitive() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let tasks = vec![
            create_test_task("CREATE A REACT COMPONENT", TaskType::Development),
            create_test_task("create a react component", TaskType::Development),
            create_test_task("Create A React Component", TaskType::Development),
        ];

        for task in tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Should be case insensitive: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:244-268 function test_backend_boundary_checker_delegates_ui_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:347-364 function test_qa_boundary_checker_delegates_implementation_tasks
  Similarity: 96.36%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_backend_boundary_checker_delegates_ui_tasks (lines 244-268) ---[0m
    async fn test_backend_boundary_checker_delegates_ui_tasks() {
        let checker = TaskBoundaryChecker::new(default_backend_role());

        let ui_tasks = vec![
            create_test_task(
                "Create React component for login form",
                TaskType::Development,
            ),
            create_test_task("Style the user dashboard with CSS", TaskType::Development),
            create_test_task("Implement frontend routing", TaskType::Development),
        ];

        for task in ui_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "Backend should delegate UI task: {}",
                task.description
            );

            if let TaskEvaluation::Delegate { target_agent, .. } = result {
                assert_eq!(target_agent, "frontend-agent");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_qa_boundary_checker_delegates_implementation_tasks (lines 347-364) ---[0m
    async fn test_qa_boundary_checker_delegates_implementation_tasks() {
        let checker = TaskBoundaryChecker::new(default_qa_role());

        let impl_tasks = vec![
            create_test_task("Implement new user registration feature", TaskType::Feature),
            create_test_task("Fix production bug in payment system", TaskType::Bugfix),
            create_test_task("Deploy application to production", TaskType::Infrastructure),
        ];

        for task in impl_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "QA should delegate implementation task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:347-364 function test_qa_boundary_checker_delegates_implementation_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:367-391 function test_ambiguous_task_triggers_clarification
  Similarity: 96.33%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_qa_boundary_checker_delegates_implementation_tasks (lines 347-364) ---[0m
    async fn test_qa_boundary_checker_delegates_implementation_tasks() {
        let checker = TaskBoundaryChecker::new(default_qa_role());

        let impl_tasks = vec![
            create_test_task("Implement new user registration feature", TaskType::Feature),
            create_test_task("Fix production bug in payment system", TaskType::Bugfix),
            create_test_task("Deploy application to production", TaskType::Infrastructure),
        ];

        for task in impl_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "QA should delegate implementation task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_ambiguous_task_triggers_clarification (lines 367-391) ---[0m
    async fn test_ambiguous_task_triggers_clarification() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Tasks that are truly ambiguous (could be frontend or backend)
        let ambiguous_tasks = vec![
            create_test_task("Update the user system", TaskType::Development),
            create_test_task("Optimize the application", TaskType::Development),
            create_test_task("Refactor the module", TaskType::Development),
        ];

        for task in ambiguous_tasks {
            let result = checker.evaluate_task(&task).await;
            // These tasks don't clearly match any patterns, so they should trigger clarification
            assert!(
                matches!(result, TaskEvaluation::Clarify { .. }),
                "Ambiguous task should trigger clarification: {}",
                task.description
            );

            if let TaskEvaluation::Clarify { questions, .. } = result {
                assert!(!questions.is_empty());
                assert!(questions.iter().any(|q| q.contains("Frontend")));
            }
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:271-293 function test_devops_boundary_checker_accepts_infrastructure_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:325-343 function test_qa_boundary_checker_accepts_testing_tasks
  Similarity: 96.65%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_accepts_infrastructure_tasks (lines 271-293) ---[0m
    async fn test_devops_boundary_checker_accepts_infrastructure_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        let infra_tasks = vec![
            create_test_task("Set up Kubernetes cluster", TaskType::Infrastructure),
            create_test_task("Configure Docker containers", TaskType::Infrastructure),
            create_test_task("Deploy application to AWS", TaskType::Infrastructure),
            create_test_task("Set up CI/CD pipeline", TaskType::Infrastructure),
            create_test_task(
                "Configure monitoring with Prometheus",
                TaskType::Infrastructure,
            ),
        ];

        for task in infra_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "DevOps should accept infrastructure task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_qa_boundary_checker_accepts_testing_tasks (lines 325-343) ---[0m
    async fn test_qa_boundary_checker_accepts_testing_tasks() {
        let checker = TaskBoundaryChecker::new(default_qa_role());

        let testing_tasks = vec![
            create_test_task("Write end-to-end tests with Cypress", TaskType::Testing),
            create_test_task("Perform load testing with K6", TaskType::Testing),
            create_test_task("Execute security penetration testing", TaskType::Testing),
            create_test_task("Create test automation framework", TaskType::Testing),
        ];

        for task in testing_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "QA should accept testing task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:168-189 function test_frontend_boundary_checker_accepts_ui_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:325-343 function test_qa_boundary_checker_accepts_testing_tasks
  Similarity: 96.97%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_frontend_boundary_checker_accepts_ui_tasks (lines 168-189) ---[0m
    async fn test_frontend_boundary_checker_accepts_ui_tasks() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let ui_tasks = vec![
            create_test_task(
                "Create a React component for user profile",
                TaskType::Development,
            ),
            create_test_task("Implement CSS styling with Tailwind", TaskType::Development),
            create_test_task("Add state management with Redux", TaskType::Development),
            create_test_task("Write Jest tests for components", TaskType::Testing),
        ];

        for task in ui_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Frontend should accept UI task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_qa_boundary_checker_accepts_testing_tasks (lines 325-343) ---[0m
    async fn test_qa_boundary_checker_accepts_testing_tasks() {
        let checker = TaskBoundaryChecker::new(default_qa_role());

        let testing_tasks = vec![
            create_test_task("Write end-to-end tests with Cypress", TaskType::Testing),
            create_test_task("Perform load testing with K6", TaskType::Testing),
            create_test_task("Execute security penetration testing", TaskType::Testing),
            create_test_task("Create test automation framework", TaskType::Testing),
        ];

        for task in testing_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "QA should accept testing task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:220-241 function test_backend_boundary_checker_accepts_api_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:325-343 function test_qa_boundary_checker_accepts_testing_tasks
  Similarity: 96.76%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_backend_boundary_checker_accepts_api_tasks (lines 220-241) ---[0m
    async fn test_backend_boundary_checker_accepts_api_tasks() {
        let checker = TaskBoundaryChecker::new(default_backend_role());

        let api_tasks = vec![
            create_test_task("Create REST API for user management", TaskType::Development),
            create_test_task(
                "Implement database query optimization",
                TaskType::Development,
            ),
            create_test_task("Add OAuth authentication flow", TaskType::Development),
            create_test_task("Set up Prisma ORM models", TaskType::Development),
        ];

        for task in api_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Backend should accept API task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_qa_boundary_checker_accepts_testing_tasks (lines 325-343) ---[0m
    async fn test_qa_boundary_checker_accepts_testing_tasks() {
        let checker = TaskBoundaryChecker::new(default_qa_role());

        let testing_tasks = vec![
            create_test_task("Write end-to-end tests with Cypress", TaskType::Testing),
            create_test_task("Perform load testing with K6", TaskType::Testing),
            create_test_task("Execute security penetration testing", TaskType::Testing),
            create_test_task("Create test automation framework", TaskType::Testing),
        ];

        for task in testing_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "QA should accept testing task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:271-293 function test_devops_boundary_checker_accepts_infrastructure_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:455-472 function test_boundary_checker_case_insensitive
  Similarity: 96.65%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_accepts_infrastructure_tasks (lines 271-293) ---[0m
    async fn test_devops_boundary_checker_accepts_infrastructure_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        let infra_tasks = vec![
            create_test_task("Set up Kubernetes cluster", TaskType::Infrastructure),
            create_test_task("Configure Docker containers", TaskType::Infrastructure),
            create_test_task("Deploy application to AWS", TaskType::Infrastructure),
            create_test_task("Set up CI/CD pipeline", TaskType::Infrastructure),
            create_test_task(
                "Configure monitoring with Prometheus",
                TaskType::Infrastructure,
            ),
        ];

        for task in infra_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "DevOps should accept infrastructure task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_case_insensitive (lines 455-472) ---[0m
    async fn test_boundary_checker_case_insensitive() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let tasks = vec![
            create_test_task("CREATE A REACT COMPONENT", TaskType::Development),
            create_test_task("create a react component", TaskType::Development),
            create_test_task("Create A React Component", TaskType::Development),
        ];

        for task in tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Should be case insensitive: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:271-293 function test_devops_boundary_checker_accepts_infrastructure_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:347-364 function test_qa_boundary_checker_delegates_implementation_tasks
  Similarity: 96.45%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_accepts_infrastructure_tasks (lines 271-293) ---[0m
    async fn test_devops_boundary_checker_accepts_infrastructure_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        let infra_tasks = vec![
            create_test_task("Set up Kubernetes cluster", TaskType::Infrastructure),
            create_test_task("Configure Docker containers", TaskType::Infrastructure),
            create_test_task("Deploy application to AWS", TaskType::Infrastructure),
            create_test_task("Set up CI/CD pipeline", TaskType::Infrastructure),
            create_test_task(
                "Configure monitoring with Prometheus",
                TaskType::Infrastructure,
            ),
        ];

        for task in infra_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "DevOps should accept infrastructure task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_qa_boundary_checker_delegates_implementation_tasks (lines 347-364) ---[0m
    async fn test_qa_boundary_checker_delegates_implementation_tasks() {
        let checker = TaskBoundaryChecker::new(default_qa_role());

        let impl_tasks = vec![
            create_test_task("Implement new user registration feature", TaskType::Feature),
            create_test_task("Fix production bug in payment system", TaskType::Bugfix),
            create_test_task("Deploy application to production", TaskType::Infrastructure),
        ];

        for task in impl_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "QA should delegate implementation task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:111-129 function test_identity_monitor_response_analysis <-> ./crates/ccswarm/src/tests/identity_tests.rs:296-322 function test_devops_boundary_checker_delegates_application_tasks
  Similarity: 85.08%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_monitor_response_analysis (lines 111-129) ---[0m
    async fn test_identity_monitor_response_analysis() {
        let mut monitor = IdentityMonitor::new("Frontend");

        let healthy_response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: /test/workspace
🎯 SCOPE: Component development

I'm working on creating a React component for the user profile page.
This is within my frontend specialization.
"#;

        let result = monitor.monitor_response(healthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::Healthy));

        let unhealthy_response = "Working on backend API endpoints...";
        let result = monitor.monitor_response(unhealthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::DriftDetected(_)));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_delegates_application_tasks (lines 296-322) ---[0m
    async fn test_devops_boundary_checker_delegates_application_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        // Tasks that clearly match forbidden patterns for DevOps
        let app_tasks = vec![
            create_test_task("Implement new feature in the UI", TaskType::Feature),
            create_test_task("Fix React component rendering issue", TaskType::Bugfix),
            create_test_task(
                "Add API endpoint for user management",
                TaskType::Development,
            ),
        ];

        for task in app_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "DevOps should delegate application task: {}",
                task.description
            );
        }

        // Task that's ambiguous should trigger clarification
        let ambiguous = create_test_task("Fix authentication issue", TaskType::Bugfix);
        let result = checker.evaluate_task(&ambiguous).await;
        assert!(matches!(result, TaskEvaluation::Clarify { .. }));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:168-189 function test_frontend_boundary_checker_accepts_ui_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:455-472 function test_boundary_checker_case_insensitive
  Similarity: 96.55%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_frontend_boundary_checker_accepts_ui_tasks (lines 168-189) ---[0m
    async fn test_frontend_boundary_checker_accepts_ui_tasks() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let ui_tasks = vec![
            create_test_task(
                "Create a React component for user profile",
                TaskType::Development,
            ),
            create_test_task("Implement CSS styling with Tailwind", TaskType::Development),
            create_test_task("Add state management with Redux", TaskType::Development),
            create_test_task("Write Jest tests for components", TaskType::Testing),
        ];

        for task in ui_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Frontend should accept UI task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_case_insensitive (lines 455-472) ---[0m
    async fn test_boundary_checker_case_insensitive() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let tasks = vec![
            create_test_task("CREATE A REACT COMPONENT", TaskType::Development),
            create_test_task("create a react component", TaskType::Development),
            create_test_task("Create A React Component", TaskType::Development),
        ];

        for task in tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Should be case insensitive: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:220-241 function test_backend_boundary_checker_accepts_api_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:455-472 function test_boundary_checker_case_insensitive
  Similarity: 96.34%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_backend_boundary_checker_accepts_api_tasks (lines 220-241) ---[0m
    async fn test_backend_boundary_checker_accepts_api_tasks() {
        let checker = TaskBoundaryChecker::new(default_backend_role());

        let api_tasks = vec![
            create_test_task("Create REST API for user management", TaskType::Development),
            create_test_task(
                "Implement database query optimization",
                TaskType::Development,
            ),
            create_test_task("Add OAuth authentication flow", TaskType::Development),
            create_test_task("Set up Prisma ORM models", TaskType::Development),
        ];

        for task in api_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Backend should accept API task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_case_insensitive (lines 455-472) ---[0m
    async fn test_boundary_checker_case_insensitive() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let tasks = vec![
            create_test_task("CREATE A REACT COMPONENT", TaskType::Development),
            create_test_task("create a react component", TaskType::Development),
            create_test_task("Create A React Component", TaskType::Development),
        ];

        for task in tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Should be case insensitive: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:168-189 function test_frontend_boundary_checker_accepts_ui_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:347-364 function test_qa_boundary_checker_delegates_implementation_tasks
  Similarity: 96.13%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_frontend_boundary_checker_accepts_ui_tasks (lines 168-189) ---[0m
    async fn test_frontend_boundary_checker_accepts_ui_tasks() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let ui_tasks = vec![
            create_test_task(
                "Create a React component for user profile",
                TaskType::Development,
            ),
            create_test_task("Implement CSS styling with Tailwind", TaskType::Development),
            create_test_task("Add state management with Redux", TaskType::Development),
            create_test_task("Write Jest tests for components", TaskType::Testing),
        ];

        for task in ui_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Frontend should accept UI task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_qa_boundary_checker_delegates_implementation_tasks (lines 347-364) ---[0m
    async fn test_qa_boundary_checker_delegates_implementation_tasks() {
        let checker = TaskBoundaryChecker::new(default_qa_role());

        let impl_tasks = vec![
            create_test_task("Implement new user registration feature", TaskType::Feature),
            create_test_task("Fix production bug in payment system", TaskType::Bugfix),
            create_test_task("Deploy application to production", TaskType::Infrastructure),
        ];

        for task in impl_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "QA should delegate implementation task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:220-241 function test_backend_boundary_checker_accepts_api_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:347-364 function test_qa_boundary_checker_delegates_implementation_tasks
  Similarity: 96.13%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_backend_boundary_checker_accepts_api_tasks (lines 220-241) ---[0m
    async fn test_backend_boundary_checker_accepts_api_tasks() {
        let checker = TaskBoundaryChecker::new(default_backend_role());

        let api_tasks = vec![
            create_test_task("Create REST API for user management", TaskType::Development),
            create_test_task(
                "Implement database query optimization",
                TaskType::Development,
            ),
            create_test_task("Add OAuth authentication flow", TaskType::Development),
            create_test_task("Set up Prisma ORM models", TaskType::Development),
        ];

        for task in api_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Backend should accept API task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_qa_boundary_checker_delegates_implementation_tasks (lines 347-364) ---[0m
    async fn test_qa_boundary_checker_delegates_implementation_tasks() {
        let checker = TaskBoundaryChecker::new(default_qa_role());

        let impl_tasks = vec![
            create_test_task("Implement new user registration feature", TaskType::Feature),
            create_test_task("Fix production bug in payment system", TaskType::Bugfix),
            create_test_task("Deploy application to production", TaskType::Infrastructure),
        ];

        for task in impl_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "QA should delegate implementation task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:296-322 function test_devops_boundary_checker_delegates_application_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:506-526 function test_response_parser_performance
  Similarity: 79.95%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_delegates_application_tasks (lines 296-322) ---[0m
    async fn test_devops_boundary_checker_delegates_application_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        // Tasks that clearly match forbidden patterns for DevOps
        let app_tasks = vec![
            create_test_task("Implement new feature in the UI", TaskType::Feature),
            create_test_task("Fix React component rendering issue", TaskType::Bugfix),
            create_test_task(
                "Add API endpoint for user management",
                TaskType::Development,
            ),
        ];

        for task in app_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "DevOps should delegate application task: {}",
                task.description
            );
        }

        // Task that's ambiguous should trigger clarification
        let ambiguous = create_test_task("Fix authentication issue", TaskType::Bugfix);
        let result = checker.evaluate_task(&ambiguous).await;
        assert!(matches!(result, TaskEvaluation::Clarify { .. }));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_response_parser_performance (lines 506-526) ---[0m
    fn test_response_parser_performance() {
        let parser = ResponseParser::new();
        let response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: agents/frontend-agent/
🎯 SCOPE: Component development
"#;

        let start = std::time::Instant::now();

        // Run 1000 parses
        for _ in 0..1000 {
            let _ = parser.parse_identity(response);
        }

        let duration = start.elapsed();
        assert!(
            duration.as_millis() < 100,
            "1000 response parses should complete in under 100ms"
        );
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:14-32 function create_test_identity <-> ./crates/ccswarm/src/tests/identity_tests.rs:296-322 function test_devops_boundary_checker_delegates_application_tasks
  Similarity: 83.30%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:create_test_identity (lines 14-32) ---[0m
    fn create_test_identity(role: AgentRole) -> AgentIdentity {
        let agent_id = format!("{}-agent-test", role.as_str().to_lowercase());
        let session_id = Uuid::new_v4().to_string();

        let mut env_vars = HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.clone());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.clone());
        env_vars.insert("CCSWARM_ROLE".to_string(), role.as_str().to_string());

        AgentIdentity {
            agent_id,
            specialization: role,
            workspace_path: PathBuf::from("/test/workspace"),
            env_vars,
            session_id,
            parent_process_id: "1234".to_string(),
            initialized_at: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_delegates_application_tasks (lines 296-322) ---[0m
    async fn test_devops_boundary_checker_delegates_application_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        // Tasks that clearly match forbidden patterns for DevOps
        let app_tasks = vec![
            create_test_task("Implement new feature in the UI", TaskType::Feature),
            create_test_task("Fix React component rendering issue", TaskType::Bugfix),
            create_test_task(
                "Add API endpoint for user management",
                TaskType::Development,
            ),
        ];

        for task in app_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "DevOps should delegate application task: {}",
                task.description
            );
        }

        // Task that's ambiguous should trigger clarification
        let ambiguous = create_test_task("Fix authentication issue", TaskType::Bugfix);
        let result = checker.evaluate_task(&ambiguous).await;
        assert!(matches!(result, TaskEvaluation::Clarify { .. }));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:132-150 function test_response_parser <-> ./crates/ccswarm/src/tests/identity_tests.rs:296-322 function test_devops_boundary_checker_delegates_application_tasks
  Similarity: 81.61%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_response_parser (lines 132-150) ---[0m
    fn test_response_parser() {
        let parser = ResponseParser::new();

        let response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: agents/frontend-agent/
🎯 SCOPE: Component development

Working on React component...
"#;

        let parsed = parser.parse_identity(response);
        assert!(parsed.is_some());

        let (agent, workspace, scope) = parsed.unwrap();
        assert_eq!(agent, "Frontend");
        assert_eq!(workspace, "agents/frontend-agent/");
        assert_eq!(scope, "Component development");
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_delegates_application_tasks (lines 296-322) ---[0m
    async fn test_devops_boundary_checker_delegates_application_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        // Tasks that clearly match forbidden patterns for DevOps
        let app_tasks = vec![
            create_test_task("Implement new feature in the UI", TaskType::Feature),
            create_test_task("Fix React component rendering issue", TaskType::Bugfix),
            create_test_task(
                "Add API endpoint for user management",
                TaskType::Development,
            ),
        ];

        for task in app_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "DevOps should delegate application task: {}",
                task.description
            );
        }

        // Task that's ambiguous should trigger clarification
        let ambiguous = create_test_task("Fix authentication issue", TaskType::Bugfix);
        let result = checker.evaluate_task(&ambiguous).await;
        assert!(matches!(result, TaskEvaluation::Clarify { .. }));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:296-322 function test_devops_boundary_checker_delegates_application_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:367-391 function test_ambiguous_task_triggers_clarification
  Similarity: 70.00%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_delegates_application_tasks (lines 296-322) ---[0m
    async fn test_devops_boundary_checker_delegates_application_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        // Tasks that clearly match forbidden patterns for DevOps
        let app_tasks = vec![
            create_test_task("Implement new feature in the UI", TaskType::Feature),
            create_test_task("Fix React component rendering issue", TaskType::Bugfix),
            create_test_task(
                "Add API endpoint for user management",
                TaskType::Development,
            ),
        ];

        for task in app_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "DevOps should delegate application task: {}",
                task.description
            );
        }

        // Task that's ambiguous should trigger clarification
        let ambiguous = create_test_task("Fix authentication issue", TaskType::Bugfix);
        let result = checker.evaluate_task(&ambiguous).await;
        assert!(matches!(result, TaskEvaluation::Clarify { .. }));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_ambiguous_task_triggers_clarification (lines 367-391) ---[0m
    async fn test_ambiguous_task_triggers_clarification() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Tasks that are truly ambiguous (could be frontend or backend)
        let ambiguous_tasks = vec![
            create_test_task("Update the user system", TaskType::Development),
            create_test_task("Optimize the application", TaskType::Development),
            create_test_task("Refactor the module", TaskType::Development),
        ];

        for task in ambiguous_tasks {
            let result = checker.evaluate_task(&task).await;
            // These tasks don't clearly match any patterns, so they should trigger clarification
            assert!(
                matches!(result, TaskEvaluation::Clarify { .. }),
                "Ambiguous task should trigger clarification: {}",
                task.description
            );

            if let TaskEvaluation::Clarify { questions, .. } = result {
                assert!(!questions.is_empty());
                assert!(questions.iter().any(|q| q.contains("Frontend")));
            }
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:296-322 function test_devops_boundary_checker_delegates_application_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:487-503 function test_boundary_checker_performance
  Similarity: 81.14%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_delegates_application_tasks (lines 296-322) ---[0m
    async fn test_devops_boundary_checker_delegates_application_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        // Tasks that clearly match forbidden patterns for DevOps
        let app_tasks = vec![
            create_test_task("Implement new feature in the UI", TaskType::Feature),
            create_test_task("Fix React component rendering issue", TaskType::Bugfix),
            create_test_task(
                "Add API endpoint for user management",
                TaskType::Development,
            ),
        ];

        for task in app_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "DevOps should delegate application task: {}",
                task.description
            );
        }

        // Task that's ambiguous should trigger clarification
        let ambiguous = create_test_task("Fix authentication issue", TaskType::Bugfix);
        let result = checker.evaluate_task(&ambiguous).await;
        assert!(matches!(result, TaskEvaluation::Clarify { .. }));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_performance (lines 487-503) ---[0m
    async fn test_boundary_checker_performance() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());
        let task = create_test_task("Create React component", TaskType::Development);

        let start = std::time::Instant::now();

        // Run 100 evaluations
        for _ in 0..100 {
            let _ = checker.evaluate_task(&task).await;
        }

        let duration = start.elapsed();
        assert!(
            duration.as_millis() < 1000,
            "100 boundary checks should complete in under 1 second"
        );
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:325-343 function test_qa_boundary_checker_accepts_testing_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:347-364 function test_qa_boundary_checker_delegates_implementation_tasks
  Similarity: 96.31%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_qa_boundary_checker_accepts_testing_tasks (lines 325-343) ---[0m
    async fn test_qa_boundary_checker_accepts_testing_tasks() {
        let checker = TaskBoundaryChecker::new(default_qa_role());

        let testing_tasks = vec![
            create_test_task("Write end-to-end tests with Cypress", TaskType::Testing),
            create_test_task("Perform load testing with K6", TaskType::Testing),
            create_test_task("Execute security penetration testing", TaskType::Testing),
            create_test_task("Create test automation framework", TaskType::Testing),
        ];

        for task in testing_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "QA should accept testing task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_qa_boundary_checker_delegates_implementation_tasks (lines 347-364) ---[0m
    async fn test_qa_boundary_checker_delegates_implementation_tasks() {
        let checker = TaskBoundaryChecker::new(default_qa_role());

        let impl_tasks = vec![
            create_test_task("Implement new user registration feature", TaskType::Feature),
            create_test_task("Fix production bug in payment system", TaskType::Bugfix),
            create_test_task("Deploy application to production", TaskType::Infrastructure),
        ];

        for task in impl_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "QA should delegate implementation task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:325-343 function test_qa_boundary_checker_accepts_testing_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:455-472 function test_boundary_checker_case_insensitive
  Similarity: 96.31%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_qa_boundary_checker_accepts_testing_tasks (lines 325-343) ---[0m
    async fn test_qa_boundary_checker_accepts_testing_tasks() {
        let checker = TaskBoundaryChecker::new(default_qa_role());

        let testing_tasks = vec![
            create_test_task("Write end-to-end tests with Cypress", TaskType::Testing),
            create_test_task("Perform load testing with K6", TaskType::Testing),
            create_test_task("Execute security penetration testing", TaskType::Testing),
            create_test_task("Create test automation framework", TaskType::Testing),
        ];

        for task in testing_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "QA should accept testing task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_case_insensitive (lines 455-472) ---[0m
    async fn test_boundary_checker_case_insensitive() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let tasks = vec![
            create_test_task("CREATE A REACT COMPONENT", TaskType::Development),
            create_test_task("create a react component", TaskType::Development),
            create_test_task("Create A React Component", TaskType::Development),
        ];

        for task in tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Should be case insensitive: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:367-391 function test_ambiguous_task_triggers_clarification <-> ./crates/ccswarm/src/tests/identity_tests.rs:506-526 function test_response_parser_performance
  Similarity: 77.24%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_ambiguous_task_triggers_clarification (lines 367-391) ---[0m
    async fn test_ambiguous_task_triggers_clarification() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Tasks that are truly ambiguous (could be frontend or backend)
        let ambiguous_tasks = vec![
            create_test_task("Update the user system", TaskType::Development),
            create_test_task("Optimize the application", TaskType::Development),
            create_test_task("Refactor the module", TaskType::Development),
        ];

        for task in ambiguous_tasks {
            let result = checker.evaluate_task(&task).await;
            // These tasks don't clearly match any patterns, so they should trigger clarification
            assert!(
                matches!(result, TaskEvaluation::Clarify { .. }),
                "Ambiguous task should trigger clarification: {}",
                task.description
            );

            if let TaskEvaluation::Clarify { questions, .. } = result {
                assert!(!questions.is_empty());
                assert!(questions.iter().any(|q| q.contains("Frontend")));
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_response_parser_performance (lines 506-526) ---[0m
    fn test_response_parser_performance() {
        let parser = ResponseParser::new();
        let response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: agents/frontend-agent/
🎯 SCOPE: Component development
"#;

        let start = std::time::Instant::now();

        // Run 1000 parses
        for _ in 0..1000 {
            let _ = parser.parse_identity(response);
        }

        let duration = start.elapsed();
        assert!(
            duration.as_millis() < 100,
            "1000 response parses should complete in under 100ms"
        );
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:347-364 function test_qa_boundary_checker_delegates_implementation_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:455-472 function test_boundary_checker_case_insensitive
  Similarity: 97.42%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_qa_boundary_checker_delegates_implementation_tasks (lines 347-364) ---[0m
    async fn test_qa_boundary_checker_delegates_implementation_tasks() {
        let checker = TaskBoundaryChecker::new(default_qa_role());

        let impl_tasks = vec![
            create_test_task("Implement new user registration feature", TaskType::Feature),
            create_test_task("Fix production bug in payment system", TaskType::Bugfix),
            create_test_task("Deploy application to production", TaskType::Infrastructure),
        ];

        for task in impl_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "QA should delegate implementation task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_case_insensitive (lines 455-472) ---[0m
    async fn test_boundary_checker_case_insensitive() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let tasks = vec![
            create_test_task("CREATE A REACT COMPONENT", TaskType::Development),
            create_test_task("create a react component", TaskType::Development),
            create_test_task("Create A React Component", TaskType::Development),
        ];

        for task in tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Should be case insensitive: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:487-503 function test_boundary_checker_performance <-> ./crates/ccswarm/src/tests/identity_tests.rs:506-526 function test_response_parser_performance
  Similarity: 92.10%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_performance (lines 487-503) ---[0m
    async fn test_boundary_checker_performance() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());
        let task = create_test_task("Create React component", TaskType::Development);

        let start = std::time::Instant::now();

        // Run 100 evaluations
        for _ in 0..100 {
            let _ = checker.evaluate_task(&task).await;
        }

        let duration = start.elapsed();
        assert!(
            duration.as_millis() < 1000,
            "100 boundary checks should complete in under 1 second"
        );
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_response_parser_performance (lines 506-526) ---[0m
    fn test_response_parser_performance() {
        let parser = ResponseParser::new();
        let response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: agents/frontend-agent/
🎯 SCOPE: Component development
"#;

        let start = std::time::Instant::now();

        // Run 1000 parses
        for _ in 0..1000 {
            let _ = parser.parse_identity(response);
        }

        let duration = start.elapsed();
        assert!(
            duration.as_millis() < 100,
            "1000 response parses should complete in under 100ms"
        );
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:111-129 function test_identity_monitor_response_analysis <-> ./crates/ccswarm/src/tests/identity_tests.rs:425-452 function test_boundary_checker_with_mixed_keywords
  Similarity: 71.78%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_monitor_response_analysis (lines 111-129) ---[0m
    async fn test_identity_monitor_response_analysis() {
        let mut monitor = IdentityMonitor::new("Frontend");

        let healthy_response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: /test/workspace
🎯 SCOPE: Component development

I'm working on creating a React component for the user profile page.
This is within my frontend specialization.
"#;

        let result = monitor.monitor_response(healthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::Healthy));

        let unhealthy_response = "Working on backend API endpoints...";
        let result = monitor.monitor_response(unhealthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::DriftDetected(_)));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_with_mixed_keywords (lines 425-452) ---[0m
    async fn test_boundary_checker_with_mixed_keywords() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Task that contains both frontend and backend keywords
        let mixed_task = create_test_task(
            "Create a React component that calls the backend API",
            TaskType::Development,
        );

        let result = checker.evaluate_task(&mixed_task).await;

        // Should delegate because it mentions "API" which is forbidden for frontend
        assert!(matches!(result, TaskEvaluation::Delegate { .. }));

        if let TaskEvaluation::Delegate { target_agent, .. } = result {
            assert_eq!(target_agent, "backend-agent");
        }

        // Task that is clearly frontend despite mentioning data
        let frontend_focused = create_test_task(
            "Create a React component to display user data",
            TaskType::Development,
        );

        let result2 = checker.evaluate_task(&frontend_focused).await;
        // This should be accepted as it's about displaying data, not API work
        assert!(matches!(result2, TaskEvaluation::Accept { .. }));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:14-32 function create_test_identity <-> ./crates/ccswarm/src/tests/identity_tests.rs:425-452 function test_boundary_checker_with_mixed_keywords
  Similarity: 70.90%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:create_test_identity (lines 14-32) ---[0m
    fn create_test_identity(role: AgentRole) -> AgentIdentity {
        let agent_id = format!("{}-agent-test", role.as_str().to_lowercase());
        let session_id = Uuid::new_v4().to_string();

        let mut env_vars = HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.clone());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.clone());
        env_vars.insert("CCSWARM_ROLE".to_string(), role.as_str().to_string());

        AgentIdentity {
            agent_id,
            specialization: role,
            workspace_path: PathBuf::from("/test/workspace"),
            env_vars,
            session_id,
            parent_process_id: "1234".to_string(),
            initialized_at: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_with_mixed_keywords (lines 425-452) ---[0m
    async fn test_boundary_checker_with_mixed_keywords() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Task that contains both frontend and backend keywords
        let mixed_task = create_test_task(
            "Create a React component that calls the backend API",
            TaskType::Development,
        );

        let result = checker.evaluate_task(&mixed_task).await;

        // Should delegate because it mentions "API" which is forbidden for frontend
        assert!(matches!(result, TaskEvaluation::Delegate { .. }));

        if let TaskEvaluation::Delegate { target_agent, .. } = result {
            assert_eq!(target_agent, "backend-agent");
        }

        // Task that is clearly frontend despite mentioning data
        let frontend_focused = create_test_task(
            "Create a React component to display user data",
            TaskType::Development,
        );

        let result2 = checker.evaluate_task(&frontend_focused).await;
        // This should be accepted as it's about displaying data, not API work
        assert!(matches!(result2, TaskEvaluation::Accept { .. }));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:132-150 function test_response_parser <-> ./crates/ccswarm/src/tests/identity_tests.rs:425-452 function test_boundary_checker_with_mixed_keywords
  Similarity: 70.86%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_response_parser (lines 132-150) ---[0m
    fn test_response_parser() {
        let parser = ResponseParser::new();

        let response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: agents/frontend-agent/
🎯 SCOPE: Component development

Working on React component...
"#;

        let parsed = parser.parse_identity(response);
        assert!(parsed.is_some());

        let (agent, workspace, scope) = parsed.unwrap();
        assert_eq!(agent, "Frontend");
        assert_eq!(workspace, "agents/frontend-agent/");
        assert_eq!(scope, "Component development");
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_with_mixed_keywords (lines 425-452) ---[0m
    async fn test_boundary_checker_with_mixed_keywords() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Task that contains both frontend and backend keywords
        let mixed_task = create_test_task(
            "Create a React component that calls the backend API",
            TaskType::Development,
        );

        let result = checker.evaluate_task(&mixed_task).await;

        // Should delegate because it mentions "API" which is forbidden for frontend
        assert!(matches!(result, TaskEvaluation::Delegate { .. }));

        if let TaskEvaluation::Delegate { target_agent, .. } = result {
            assert_eq!(target_agent, "backend-agent");
        }

        // Task that is clearly frontend despite mentioning data
        let frontend_focused = create_test_task(
            "Create a React component to display user data",
            TaskType::Development,
        );

        let result2 = checker.evaluate_task(&frontend_focused).await;
        // This should be accepted as it's about displaying data, not API work
        assert!(matches!(result2, TaskEvaluation::Accept { .. }));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:14-32 function create_test_identity <-> ./crates/ccswarm/src/tests/identity_tests.rs:111-129 function test_identity_monitor_response_analysis
  Similarity: 86.61%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:create_test_identity (lines 14-32) ---[0m
    fn create_test_identity(role: AgentRole) -> AgentIdentity {
        let agent_id = format!("{}-agent-test", role.as_str().to_lowercase());
        let session_id = Uuid::new_v4().to_string();

        let mut env_vars = HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.clone());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.clone());
        env_vars.insert("CCSWARM_ROLE".to_string(), role.as_str().to_string());

        AgentIdentity {
            agent_id,
            specialization: role,
            workspace_path: PathBuf::from("/test/workspace"),
            env_vars,
            session_id,
            parent_process_id: "1234".to_string(),
            initialized_at: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_monitor_response_analysis (lines 111-129) ---[0m
    async fn test_identity_monitor_response_analysis() {
        let mut monitor = IdentityMonitor::new("Frontend");

        let healthy_response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: /test/workspace
🎯 SCOPE: Component development

I'm working on creating a React component for the user profile page.
This is within my frontend specialization.
"#;

        let result = monitor.monitor_response(healthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::Healthy));

        let unhealthy_response = "Working on backend API endpoints...";
        let result = monitor.monitor_response(unhealthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::DriftDetected(_)));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:111-129 function test_identity_monitor_response_analysis <-> ./crates/ccswarm/src/tests/identity_tests.rs:367-391 function test_ambiguous_task_triggers_clarification
  Similarity: 72.81%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_monitor_response_analysis (lines 111-129) ---[0m
    async fn test_identity_monitor_response_analysis() {
        let mut monitor = IdentityMonitor::new("Frontend");

        let healthy_response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: /test/workspace
🎯 SCOPE: Component development

I'm working on creating a React component for the user profile page.
This is within my frontend specialization.
"#;

        let result = monitor.monitor_response(healthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::Healthy));

        let unhealthy_response = "Working on backend API endpoints...";
        let result = monitor.monitor_response(unhealthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::DriftDetected(_)));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_ambiguous_task_triggers_clarification (lines 367-391) ---[0m
    async fn test_ambiguous_task_triggers_clarification() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Tasks that are truly ambiguous (could be frontend or backend)
        let ambiguous_tasks = vec![
            create_test_task("Update the user system", TaskType::Development),
            create_test_task("Optimize the application", TaskType::Development),
            create_test_task("Refactor the module", TaskType::Development),
        ];

        for task in ambiguous_tasks {
            let result = checker.evaluate_task(&task).await;
            // These tasks don't clearly match any patterns, so they should trigger clarification
            assert!(
                matches!(result, TaskEvaluation::Clarify { .. }),
                "Ambiguous task should trigger clarification: {}",
                task.description
            );

            if let TaskEvaluation::Clarify { questions, .. } = result {
                assert!(!questions.is_empty());
                assert!(questions.iter().any(|q| q.contains("Frontend")));
            }
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:52-64 function test_agent_role_technologies <-> ./crates/ccswarm/src/tests/identity_tests.rs:425-452 function test_boundary_checker_with_mixed_keywords
  Similarity: 75.97%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_technologies (lines 52-64) ---[0m
    fn test_agent_role_technologies() {
        let frontend = default_frontend_role();
        let technologies = frontend.technologies();
        assert!(technologies.contains(&"React".to_string()));
        assert!(technologies.contains(&"TypeScript".to_string()));
        assert!(technologies.contains(&"Tailwind CSS".to_string()));

        let backend = default_backend_role();
        let technologies = backend.technologies();
        assert!(technologies.contains(&"Node.js".to_string()));
        assert!(technologies.contains(&"Express".to_string()));
        assert!(technologies.contains(&"PostgreSQL".to_string()));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_with_mixed_keywords (lines 425-452) ---[0m
    async fn test_boundary_checker_with_mixed_keywords() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Task that contains both frontend and backend keywords
        let mixed_task = create_test_task(
            "Create a React component that calls the backend API",
            TaskType::Development,
        );

        let result = checker.evaluate_task(&mixed_task).await;

        // Should delegate because it mentions "API" which is forbidden for frontend
        assert!(matches!(result, TaskEvaluation::Delegate { .. }));

        if let TaskEvaluation::Delegate { target_agent, .. } = result {
            assert_eq!(target_agent, "backend-agent");
        }

        // Task that is clearly frontend despite mentioning data
        let frontend_focused = create_test_task(
            "Create a React component to display user data",
            TaskType::Development,
        );

        let result2 = checker.evaluate_task(&frontend_focused).await;
        // This should be accepted as it's about displaying data, not API work
        assert!(matches!(result2, TaskEvaluation::Accept { .. }));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:67-77 function test_agent_role_responsibilities <-> ./crates/ccswarm/src/tests/identity_tests.rs:296-322 function test_devops_boundary_checker_delegates_application_tasks
  Similarity: 81.71%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_responsibilities (lines 67-77) ---[0m
    fn test_agent_role_responsibilities() {
        let frontend = default_frontend_role();
        let responsibilities = frontend.responsibilities();
        assert!(responsibilities.contains(&"UI Component Development".to_string()));
        assert!(responsibilities.contains(&"State Management".to_string()));

        let backend = default_backend_role();
        let responsibilities = backend.responsibilities();
        assert!(responsibilities.contains(&"API Development".to_string()));
        assert!(responsibilities.contains(&"Database Design".to_string()));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_delegates_application_tasks (lines 296-322) ---[0m
    async fn test_devops_boundary_checker_delegates_application_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        // Tasks that clearly match forbidden patterns for DevOps
        let app_tasks = vec![
            create_test_task("Implement new feature in the UI", TaskType::Feature),
            create_test_task("Fix React component rendering issue", TaskType::Bugfix),
            create_test_task(
                "Add API endpoint for user management",
                TaskType::Development,
            ),
        ];

        for task in app_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "DevOps should delegate application task: {}",
                task.description
            );
        }

        // Task that's ambiguous should trigger clarification
        let ambiguous = create_test_task("Fix authentication issue", TaskType::Bugfix);
        let result = checker.evaluate_task(&ambiguous).await;
        assert!(matches!(result, TaskEvaluation::Clarify { .. }));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:80-90 function test_agent_role_boundaries <-> ./crates/ccswarm/src/tests/identity_tests.rs:296-322 function test_devops_boundary_checker_delegates_application_tasks
  Similarity: 81.71%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_boundaries (lines 80-90) ---[0m
    fn test_agent_role_boundaries() {
        let frontend = default_frontend_role();
        let boundaries = frontend.boundaries();
        assert!(boundaries.contains(&"No backend API development".to_string()));
        assert!(boundaries.contains(&"No database operations".to_string()));

        let devops = default_devops_role();
        let boundaries = devops.boundaries();
        assert!(boundaries.contains(&"No application code changes".to_string()));
        assert!(boundaries.contains(&"No business logic implementation".to_string()));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_delegates_application_tasks (lines 296-322) ---[0m
    async fn test_devops_boundary_checker_delegates_application_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        // Tasks that clearly match forbidden patterns for DevOps
        let app_tasks = vec![
            create_test_task("Implement new feature in the UI", TaskType::Feature),
            create_test_task("Fix React component rendering issue", TaskType::Bugfix),
            create_test_task(
                "Add API endpoint for user management",
                TaskType::Development,
            ),
        ];

        for task in app_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "DevOps should delegate application task: {}",
                task.description
            );
        }

        // Task that's ambiguous should trigger clarification
        let ambiguous = create_test_task("Fix authentication issue", TaskType::Bugfix);
        let result = checker.evaluate_task(&ambiguous).await;
        assert!(matches!(result, TaskEvaluation::Clarify { .. }));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:14-32 function create_test_identity <-> ./crates/ccswarm/src/tests/identity_tests.rs:487-503 function test_boundary_checker_performance
  Similarity: 85.70%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:create_test_identity (lines 14-32) ---[0m
    fn create_test_identity(role: AgentRole) -> AgentIdentity {
        let agent_id = format!("{}-agent-test", role.as_str().to_lowercase());
        let session_id = Uuid::new_v4().to_string();

        let mut env_vars = HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.clone());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.clone());
        env_vars.insert("CCSWARM_ROLE".to_string(), role.as_str().to_string());

        AgentIdentity {
            agent_id,
            specialization: role,
            workspace_path: PathBuf::from("/test/workspace"),
            env_vars,
            session_id,
            parent_process_id: "1234".to_string(),
            initialized_at: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_performance (lines 487-503) ---[0m
    async fn test_boundary_checker_performance() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());
        let task = create_test_task("Create React component", TaskType::Development);

        let start = std::time::Instant::now();

        // Run 100 evaluations
        for _ in 0..100 {
            let _ = checker.evaluate_task(&task).await;
        }

        let duration = start.elapsed();
        assert!(
            duration.as_millis() < 1000,
            "100 boundary checks should complete in under 1 second"
        );
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:367-391 function test_ambiguous_task_triggers_clarification <-> ./crates/ccswarm/src/tests/identity_tests.rs:487-503 function test_boundary_checker_performance
  Similarity: 73.42%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_ambiguous_task_triggers_clarification (lines 367-391) ---[0m
    async fn test_ambiguous_task_triggers_clarification() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Tasks that are truly ambiguous (could be frontend or backend)
        let ambiguous_tasks = vec![
            create_test_task("Update the user system", TaskType::Development),
            create_test_task("Optimize the application", TaskType::Development),
            create_test_task("Refactor the module", TaskType::Development),
        ];

        for task in ambiguous_tasks {
            let result = checker.evaluate_task(&task).await;
            // These tasks don't clearly match any patterns, so they should trigger clarification
            assert!(
                matches!(result, TaskEvaluation::Clarify { .. }),
                "Ambiguous task should trigger clarification: {}",
                task.description
            );

            if let TaskEvaluation::Clarify { questions, .. } = result {
                assert!(!questions.is_empty());
                assert!(questions.iter().any(|q| q.contains("Frontend")));
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_performance (lines 487-503) ---[0m
    async fn test_boundary_checker_performance() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());
        let task = create_test_task("Create React component", TaskType::Development);

        let start = std::time::Instant::now();

        // Run 100 evaluations
        for _ in 0..100 {
            let _ = checker.evaluate_task(&task).await;
        }

        let duration = start.elapsed();
        assert!(
            duration.as_millis() < 1000,
            "100 boundary checks should complete in under 1 second"
        );
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:111-129 function test_identity_monitor_response_analysis <-> ./crates/ccswarm/src/tests/identity_tests.rs:506-526 function test_response_parser_performance
  Similarity: 76.91%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_monitor_response_analysis (lines 111-129) ---[0m
    async fn test_identity_monitor_response_analysis() {
        let mut monitor = IdentityMonitor::new("Frontend");

        let healthy_response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: /test/workspace
🎯 SCOPE: Component development

I'm working on creating a React component for the user profile page.
This is within my frontend specialization.
"#;

        let result = monitor.monitor_response(healthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::Healthy));

        let unhealthy_response = "Working on backend API endpoints...";
        let result = monitor.monitor_response(unhealthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::DriftDetected(_)));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_response_parser_performance (lines 506-526) ---[0m
    fn test_response_parser_performance() {
        let parser = ResponseParser::new();
        let response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: agents/frontend-agent/
🎯 SCOPE: Component development
"#;

        let start = std::time::Instant::now();

        // Run 1000 parses
        for _ in 0..1000 {
            let _ = parser.parse_identity(response);
        }

        let duration = start.elapsed();
        assert!(
            duration.as_millis() < 100,
            "1000 response parses should complete in under 100ms"
        );
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:296-322 function test_devops_boundary_checker_delegates_application_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:411-420 function test_correction_prompt_generation
  Similarity: 81.76%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_delegates_application_tasks (lines 296-322) ---[0m
    async fn test_devops_boundary_checker_delegates_application_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        // Tasks that clearly match forbidden patterns for DevOps
        let app_tasks = vec![
            create_test_task("Implement new feature in the UI", TaskType::Feature),
            create_test_task("Fix React component rendering issue", TaskType::Bugfix),
            create_test_task(
                "Add API endpoint for user management",
                TaskType::Development,
            ),
        ];

        for task in app_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "DevOps should delegate application task: {}",
                task.description
            );
        }

        // Task that's ambiguous should trigger clarification
        let ambiguous = create_test_task("Fix authentication issue", TaskType::Bugfix);
        let result = checker.evaluate_task(&ambiguous).await;
        assert!(matches!(result, TaskEvaluation::Clarify { .. }));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_correction_prompt_generation (lines 411-420) ---[0m
    fn test_correction_prompt_generation() {
        let monitor = IdentityMonitor::new("frontend-agent");
        let prompt = monitor.generate_correction_prompt("/test/workspace", "Frontend");

        assert!(prompt.contains("IDENTITY DRIFT DETECTED"));
        assert!(prompt.contains("frontend-agent"));
        assert!(prompt.contains("Frontend"));
        assert!(prompt.contains("/test/workspace"));
        assert!(prompt.contains("🤖 AGENT:"));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:296-322 function test_devops_boundary_checker_delegates_application_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:396-408 function test_identity_serialization
  Similarity: 75.28%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_delegates_application_tasks (lines 296-322) ---[0m
    async fn test_devops_boundary_checker_delegates_application_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        // Tasks that clearly match forbidden patterns for DevOps
        let app_tasks = vec![
            create_test_task("Implement new feature in the UI", TaskType::Feature),
            create_test_task("Fix React component rendering issue", TaskType::Bugfix),
            create_test_task(
                "Add API endpoint for user management",
                TaskType::Development,
            ),
        ];

        for task in app_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "DevOps should delegate application task: {}",
                task.description
            );
        }

        // Task that's ambiguous should trigger clarification
        let ambiguous = create_test_task("Fix authentication issue", TaskType::Bugfix);
        let result = checker.evaluate_task(&ambiguous).await;
        assert!(matches!(result, TaskEvaluation::Clarify { .. }));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_serialization (lines 396-408) ---[0m
    fn test_identity_serialization() {
        let identity = create_test_identity(default_frontend_role());

        let serialized = serde_json::to_string(&identity).unwrap();
        let deserialized: AgentIdentity = serde_json::from_str(&serialized).unwrap();

        assert_eq!(identity.agent_id, deserialized.agent_id);
        assert_eq!(identity.session_id, deserialized.session_id);
        assert_eq!(
            identity.specialization.name(),
            deserialized.specialization.name()
        );
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:14-32 function create_test_identity <-> ./crates/ccswarm/src/tests/identity_tests.rs:132-150 function test_response_parser
  Similarity: 78.33%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:create_test_identity (lines 14-32) ---[0m
    fn create_test_identity(role: AgentRole) -> AgentIdentity {
        let agent_id = format!("{}-agent-test", role.as_str().to_lowercase());
        let session_id = Uuid::new_v4().to_string();

        let mut env_vars = HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.clone());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.clone());
        env_vars.insert("CCSWARM_ROLE".to_string(), role.as_str().to_string());

        AgentIdentity {
            agent_id,
            specialization: role,
            workspace_path: PathBuf::from("/test/workspace"),
            env_vars,
            session_id,
            parent_process_id: "1234".to_string(),
            initialized_at: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_response_parser (lines 132-150) ---[0m
    fn test_response_parser() {
        let parser = ResponseParser::new();

        let response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: agents/frontend-agent/
🎯 SCOPE: Component development

Working on React component...
"#;

        let parsed = parser.parse_identity(response);
        assert!(parsed.is_some());

        let (agent, workspace, scope) = parsed.unwrap();
        assert_eq!(agent, "Frontend");
        assert_eq!(workspace, "agents/frontend-agent/");
        assert_eq!(scope, "Component development");
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:67-77 function test_agent_role_responsibilities <-> ./crates/ccswarm/src/tests/identity_tests.rs:425-452 function test_boundary_checker_with_mixed_keywords
  Similarity: 74.65%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_responsibilities (lines 67-77) ---[0m
    fn test_agent_role_responsibilities() {
        let frontend = default_frontend_role();
        let responsibilities = frontend.responsibilities();
        assert!(responsibilities.contains(&"UI Component Development".to_string()));
        assert!(responsibilities.contains(&"State Management".to_string()));

        let backend = default_backend_role();
        let responsibilities = backend.responsibilities();
        assert!(responsibilities.contains(&"API Development".to_string()));
        assert!(responsibilities.contains(&"Database Design".to_string()));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_with_mixed_keywords (lines 425-452) ---[0m
    async fn test_boundary_checker_with_mixed_keywords() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Task that contains both frontend and backend keywords
        let mixed_task = create_test_task(
            "Create a React component that calls the backend API",
            TaskType::Development,
        );

        let result = checker.evaluate_task(&mixed_task).await;

        // Should delegate because it mentions "API" which is forbidden for frontend
        assert!(matches!(result, TaskEvaluation::Delegate { .. }));

        if let TaskEvaluation::Delegate { target_agent, .. } = result {
            assert_eq!(target_agent, "backend-agent");
        }

        // Task that is clearly frontend despite mentioning data
        let frontend_focused = create_test_task(
            "Create a React component to display user data",
            TaskType::Development,
        );

        let result2 = checker.evaluate_task(&frontend_focused).await;
        // This should be accepted as it's about displaying data, not API work
        assert!(matches!(result2, TaskEvaluation::Accept { .. }));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:80-90 function test_agent_role_boundaries <-> ./crates/ccswarm/src/tests/identity_tests.rs:425-452 function test_boundary_checker_with_mixed_keywords
  Similarity: 74.65%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_boundaries (lines 80-90) ---[0m
    fn test_agent_role_boundaries() {
        let frontend = default_frontend_role();
        let boundaries = frontend.boundaries();
        assert!(boundaries.contains(&"No backend API development".to_string()));
        assert!(boundaries.contains(&"No database operations".to_string()));

        let devops = default_devops_role();
        let boundaries = devops.boundaries();
        assert!(boundaries.contains(&"No application code changes".to_string()));
        assert!(boundaries.contains(&"No business logic implementation".to_string()));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_with_mixed_keywords (lines 425-452) ---[0m
    async fn test_boundary_checker_with_mixed_keywords() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Task that contains both frontend and backend keywords
        let mixed_task = create_test_task(
            "Create a React component that calls the backend API",
            TaskType::Development,
        );

        let result = checker.evaluate_task(&mixed_task).await;

        // Should delegate because it mentions "API" which is forbidden for frontend
        assert!(matches!(result, TaskEvaluation::Delegate { .. }));

        if let TaskEvaluation::Delegate { target_agent, .. } = result {
            assert_eq!(target_agent, "backend-agent");
        }

        // Task that is clearly frontend despite mentioning data
        let frontend_focused = create_test_task(
            "Create a React component to display user data",
            TaskType::Development,
        );

        let result2 = checker.evaluate_task(&frontend_focused).await;
        // This should be accepted as it's about displaying data, not API work
        assert!(matches!(result2, TaskEvaluation::Accept { .. }));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:52-64 function test_agent_role_technologies <-> ./crates/ccswarm/src/tests/identity_tests.rs:296-322 function test_devops_boundary_checker_delegates_application_tasks
  Similarity: 70.73%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_technologies (lines 52-64) ---[0m
    fn test_agent_role_technologies() {
        let frontend = default_frontend_role();
        let technologies = frontend.technologies();
        assert!(technologies.contains(&"React".to_string()));
        assert!(technologies.contains(&"TypeScript".to_string()));
        assert!(technologies.contains(&"Tailwind CSS".to_string()));

        let backend = default_backend_role();
        let technologies = backend.technologies();
        assert!(technologies.contains(&"Node.js".to_string()));
        assert!(technologies.contains(&"Express".to_string()));
        assert!(technologies.contains(&"PostgreSQL".to_string()));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_delegates_application_tasks (lines 296-322) ---[0m
    async fn test_devops_boundary_checker_delegates_application_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        // Tasks that clearly match forbidden patterns for DevOps
        let app_tasks = vec![
            create_test_task("Implement new feature in the UI", TaskType::Feature),
            create_test_task("Fix React component rendering issue", TaskType::Bugfix),
            create_test_task(
                "Add API endpoint for user management",
                TaskType::Development,
            ),
        ];

        for task in app_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "DevOps should delegate application task: {}",
                task.description
            );
        }

        // Task that's ambiguous should trigger clarification
        let ambiguous = create_test_task("Fix authentication issue", TaskType::Bugfix);
        let result = checker.evaluate_task(&ambiguous).await;
        assert!(matches!(result, TaskEvaluation::Clarify { .. }));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:111-129 function test_identity_monitor_response_analysis <-> ./crates/ccswarm/src/tests/identity_tests.rs:487-503 function test_boundary_checker_performance
  Similarity: 75.48%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_monitor_response_analysis (lines 111-129) ---[0m
    async fn test_identity_monitor_response_analysis() {
        let mut monitor = IdentityMonitor::new("Frontend");

        let healthy_response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: /test/workspace
🎯 SCOPE: Component development

I'm working on creating a React component for the user profile page.
This is within my frontend specialization.
"#;

        let result = monitor.monitor_response(healthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::Healthy));

        let unhealthy_response = "Working on backend API endpoints...";
        let result = monitor.monitor_response(unhealthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::DriftDetected(_)));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_performance (lines 487-503) ---[0m
    async fn test_boundary_checker_performance() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());
        let task = create_test_task("Create React component", TaskType::Development);

        let start = std::time::Instant::now();

        // Run 100 evaluations
        for _ in 0..100 {
            let _ = checker.evaluate_task(&task).await;
        }

        let duration = start.elapsed();
        assert!(
            duration.as_millis() < 1000,
            "100 boundary checks should complete in under 1 second"
        );
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:367-391 function test_ambiguous_task_triggers_clarification <-> ./crates/ccswarm/src/tests/identity_tests.rs:396-408 function test_identity_serialization
  Similarity: 71.36%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_ambiguous_task_triggers_clarification (lines 367-391) ---[0m
    async fn test_ambiguous_task_triggers_clarification() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        // Tasks that are truly ambiguous (could be frontend or backend)
        let ambiguous_tasks = vec![
            create_test_task("Update the user system", TaskType::Development),
            create_test_task("Optimize the application", TaskType::Development),
            create_test_task("Refactor the module", TaskType::Development),
        ];

        for task in ambiguous_tasks {
            let result = checker.evaluate_task(&task).await;
            // These tasks don't clearly match any patterns, so they should trigger clarification
            assert!(
                matches!(result, TaskEvaluation::Clarify { .. }),
                "Ambiguous task should trigger clarification: {}",
                task.description
            );

            if let TaskEvaluation::Clarify { questions, .. } = result {
                assert!(!questions.is_empty());
                assert!(questions.iter().any(|q| q.contains("Frontend")));
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_serialization (lines 396-408) ---[0m
    fn test_identity_serialization() {
        let identity = create_test_identity(default_frontend_role());

        let serialized = serde_json::to_string(&identity).unwrap();
        let deserialized: AgentIdentity = serde_json::from_str(&serialized).unwrap();

        assert_eq!(identity.agent_id, deserialized.agent_id);
        assert_eq!(identity.session_id, deserialized.session_id);
        assert_eq!(
            identity.specialization.name(),
            deserialized.specialization.name()
        );
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:14-32 function create_test_identity <-> ./crates/ccswarm/src/tests/identity_tests.rs:396-408 function test_identity_serialization
  Similarity: 78.14%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:create_test_identity (lines 14-32) ---[0m
    fn create_test_identity(role: AgentRole) -> AgentIdentity {
        let agent_id = format!("{}-agent-test", role.as_str().to_lowercase());
        let session_id = Uuid::new_v4().to_string();

        let mut env_vars = HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.clone());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.clone());
        env_vars.insert("CCSWARM_ROLE".to_string(), role.as_str().to_string());

        AgentIdentity {
            agent_id,
            specialization: role,
            workspace_path: PathBuf::from("/test/workspace"),
            env_vars,
            session_id,
            parent_process_id: "1234".to_string(),
            initialized_at: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_serialization (lines 396-408) ---[0m
    fn test_identity_serialization() {
        let identity = create_test_identity(default_frontend_role());

        let serialized = serde_json::to_string(&identity).unwrap();
        let deserialized: AgentIdentity = serde_json::from_str(&serialized).unwrap();

        assert_eq!(identity.agent_id, deserialized.agent_id);
        assert_eq!(identity.session_id, deserialized.session_id);
        assert_eq!(
            identity.specialization.name(),
            deserialized.specialization.name()
        );
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:67-77 function test_agent_role_responsibilities <-> ./crates/ccswarm/src/tests/identity_tests.rs:132-150 function test_response_parser
  Similarity: 80.70%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_responsibilities (lines 67-77) ---[0m
    fn test_agent_role_responsibilities() {
        let frontend = default_frontend_role();
        let responsibilities = frontend.responsibilities();
        assert!(responsibilities.contains(&"UI Component Development".to_string()));
        assert!(responsibilities.contains(&"State Management".to_string()));

        let backend = default_backend_role();
        let responsibilities = backend.responsibilities();
        assert!(responsibilities.contains(&"API Development".to_string()));
        assert!(responsibilities.contains(&"Database Design".to_string()));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_response_parser (lines 132-150) ---[0m
    fn test_response_parser() {
        let parser = ResponseParser::new();

        let response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: agents/frontend-agent/
🎯 SCOPE: Component development

Working on React component...
"#;

        let parsed = parser.parse_identity(response);
        assert!(parsed.is_some());

        let (agent, workspace, scope) = parsed.unwrap();
        assert_eq!(agent, "Frontend");
        assert_eq!(workspace, "agents/frontend-agent/");
        assert_eq!(scope, "Component development");
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:80-90 function test_agent_role_boundaries <-> ./crates/ccswarm/src/tests/identity_tests.rs:132-150 function test_response_parser
  Similarity: 80.70%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_boundaries (lines 80-90) ---[0m
    fn test_agent_role_boundaries() {
        let frontend = default_frontend_role();
        let boundaries = frontend.boundaries();
        assert!(boundaries.contains(&"No backend API development".to_string()));
        assert!(boundaries.contains(&"No database operations".to_string()));

        let devops = default_devops_role();
        let boundaries = devops.boundaries();
        assert!(boundaries.contains(&"No application code changes".to_string()));
        assert!(boundaries.contains(&"No business logic implementation".to_string()));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_response_parser (lines 132-150) ---[0m
    fn test_response_parser() {
        let parser = ResponseParser::new();

        let response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: agents/frontend-agent/
🎯 SCOPE: Component development

Working on React component...
"#;

        let parsed = parser.parse_identity(response);
        assert!(parsed.is_some());

        let (agent, workspace, scope) = parsed.unwrap();
        assert_eq!(agent, "Frontend");
        assert_eq!(workspace, "agents/frontend-agent/");
        assert_eq!(scope, "Component development");
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:14-32 function create_test_identity <-> ./crates/ccswarm/src/tests/identity_tests.rs:67-77 function test_agent_role_responsibilities
  Similarity: 80.27%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:create_test_identity (lines 14-32) ---[0m
    fn create_test_identity(role: AgentRole) -> AgentIdentity {
        let agent_id = format!("{}-agent-test", role.as_str().to_lowercase());
        let session_id = Uuid::new_v4().to_string();

        let mut env_vars = HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.clone());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.clone());
        env_vars.insert("CCSWARM_ROLE".to_string(), role.as_str().to_string());

        AgentIdentity {
            agent_id,
            specialization: role,
            workspace_path: PathBuf::from("/test/workspace"),
            env_vars,
            session_id,
            parent_process_id: "1234".to_string(),
            initialized_at: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_responsibilities (lines 67-77) ---[0m
    fn test_agent_role_responsibilities() {
        let frontend = default_frontend_role();
        let responsibilities = frontend.responsibilities();
        assert!(responsibilities.contains(&"UI Component Development".to_string()));
        assert!(responsibilities.contains(&"State Management".to_string()));

        let backend = default_backend_role();
        let responsibilities = backend.responsibilities();
        assert!(responsibilities.contains(&"API Development".to_string()));
        assert!(responsibilities.contains(&"Database Design".to_string()));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:14-32 function create_test_identity <-> ./crates/ccswarm/src/tests/identity_tests.rs:80-90 function test_agent_role_boundaries
  Similarity: 80.27%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:create_test_identity (lines 14-32) ---[0m
    fn create_test_identity(role: AgentRole) -> AgentIdentity {
        let agent_id = format!("{}-agent-test", role.as_str().to_lowercase());
        let session_id = Uuid::new_v4().to_string();

        let mut env_vars = HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.clone());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.clone());
        env_vars.insert("CCSWARM_ROLE".to_string(), role.as_str().to_string());

        AgentIdentity {
            agent_id,
            specialization: role,
            workspace_path: PathBuf::from("/test/workspace"),
            env_vars,
            session_id,
            parent_process_id: "1234".to_string(),
            initialized_at: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_boundaries (lines 80-90) ---[0m
    fn test_agent_role_boundaries() {
        let frontend = default_frontend_role();
        let boundaries = frontend.boundaries();
        assert!(boundaries.contains(&"No backend API development".to_string()));
        assert!(boundaries.contains(&"No database operations".to_string()));

        let devops = default_devops_role();
        let boundaries = devops.boundaries();
        assert!(boundaries.contains(&"No application code changes".to_string()));
        assert!(boundaries.contains(&"No business logic implementation".to_string()));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:14-32 function create_test_identity <-> ./crates/ccswarm/src/tests/identity_tests.rs:411-420 function test_correction_prompt_generation
  Similarity: 82.85%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:create_test_identity (lines 14-32) ---[0m
    fn create_test_identity(role: AgentRole) -> AgentIdentity {
        let agent_id = format!("{}-agent-test", role.as_str().to_lowercase());
        let session_id = Uuid::new_v4().to_string();

        let mut env_vars = HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.clone());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.clone());
        env_vars.insert("CCSWARM_ROLE".to_string(), role.as_str().to_string());

        AgentIdentity {
            agent_id,
            specialization: role,
            workspace_path: PathBuf::from("/test/workspace"),
            env_vars,
            session_id,
            parent_process_id: "1234".to_string(),
            initialized_at: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_correction_prompt_generation (lines 411-420) ---[0m
    fn test_correction_prompt_generation() {
        let monitor = IdentityMonitor::new("frontend-agent");
        let prompt = monitor.generate_correction_prompt("/test/workspace", "Frontend");

        assert!(prompt.contains("IDENTITY DRIFT DETECTED"));
        assert!(prompt.contains("frontend-agent"));
        assert!(prompt.contains("Frontend"));
        assert!(prompt.contains("/test/workspace"));
        assert!(prompt.contains("🤖 AGENT:"));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:52-64 function test_agent_role_technologies <-> ./crates/ccswarm/src/tests/identity_tests.rs:132-150 function test_response_parser
  Similarity: 74.35%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_technologies (lines 52-64) ---[0m
    fn test_agent_role_technologies() {
        let frontend = default_frontend_role();
        let technologies = frontend.technologies();
        assert!(technologies.contains(&"React".to_string()));
        assert!(technologies.contains(&"TypeScript".to_string()));
        assert!(technologies.contains(&"Tailwind CSS".to_string()));

        let backend = default_backend_role();
        let technologies = backend.technologies();
        assert!(technologies.contains(&"Node.js".to_string()));
        assert!(technologies.contains(&"Express".to_string()));
        assert!(technologies.contains(&"PostgreSQL".to_string()));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_response_parser (lines 132-150) ---[0m
    fn test_response_parser() {
        let parser = ResponseParser::new();

        let response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: agents/frontend-agent/
🎯 SCOPE: Component development

Working on React component...
"#;

        let parsed = parser.parse_identity(response);
        assert!(parsed.is_some());

        let (agent, workspace, scope) = parsed.unwrap();
        assert_eq!(agent, "Frontend");
        assert_eq!(workspace, "agents/frontend-agent/");
        assert_eq!(scope, "Component development");
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:44-49 function test_agent_role_names <-> ./crates/ccswarm/src/tests/identity_tests.rs:192-217 function test_frontend_boundary_checker_delegates_backend_tasks
  Similarity: 73.93%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_names (lines 44-49) ---[0m
    fn test_agent_role_names() {
        assert_eq!(default_frontend_role().name(), "Frontend");
        assert_eq!(default_backend_role().name(), "Backend");
        assert_eq!(default_devops_role().name(), "DevOps");
        assert_eq!(default_qa_role().name(), "QA");
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_frontend_boundary_checker_delegates_backend_tasks (lines 192-217) ---[0m
    async fn test_frontend_boundary_checker_delegates_backend_tasks() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let backend_tasks = vec![
            create_test_task(
                "Create REST API endpoint for authentication",
                TaskType::Development,
            ),
            create_test_task("Design database schema for users", TaskType::Development),
            create_test_task("Implement JWT token validation", TaskType::Development),
            create_test_task("Set up GraphQL resolver", TaskType::Development),
        ];

        for task in backend_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "Frontend should delegate backend task: {}",
                task.description
            );

            if let TaskEvaluation::Delegate { target_agent, .. } = result {
                assert_eq!(target_agent, "backend-agent");
            }
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:271-293 function test_devops_boundary_checker_accepts_infrastructure_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:475-482 function test_identity_env_vars
  Similarity: 74.77%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_accepts_infrastructure_tasks (lines 271-293) ---[0m
    async fn test_devops_boundary_checker_accepts_infrastructure_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        let infra_tasks = vec![
            create_test_task("Set up Kubernetes cluster", TaskType::Infrastructure),
            create_test_task("Configure Docker containers", TaskType::Infrastructure),
            create_test_task("Deploy application to AWS", TaskType::Infrastructure),
            create_test_task("Set up CI/CD pipeline", TaskType::Infrastructure),
            create_test_task(
                "Configure monitoring with Prometheus",
                TaskType::Infrastructure,
            ),
        ];

        for task in infra_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "DevOps should accept infrastructure task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_env_vars (lines 475-482) ---[0m
    fn test_identity_env_vars() {
        let identity = create_test_identity(default_frontend_role());

        // Check if environment variables are set up correctly
        assert!(identity.env_vars.contains_key("CCSWARM_AGENT_ID"));
        assert!(identity.env_vars.contains_key("CCSWARM_SESSION_ID"));
        assert!(identity.env_vars.contains_key("CCSWARM_ROLE"));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:111-129 function test_identity_monitor_response_analysis <-> ./crates/ccswarm/src/tests/identity_tests.rs:411-420 function test_correction_prompt_generation
  Similarity: 79.54%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_monitor_response_analysis (lines 111-129) ---[0m
    async fn test_identity_monitor_response_analysis() {
        let mut monitor = IdentityMonitor::new("Frontend");

        let healthy_response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: /test/workspace
🎯 SCOPE: Component development

I'm working on creating a React component for the user profile page.
This is within my frontend specialization.
"#;

        let result = monitor.monitor_response(healthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::Healthy));

        let unhealthy_response = "Working on backend API endpoints...";
        let result = monitor.monitor_response(unhealthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::DriftDetected(_)));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_correction_prompt_generation (lines 411-420) ---[0m
    fn test_correction_prompt_generation() {
        let monitor = IdentityMonitor::new("frontend-agent");
        let prompt = monitor.generate_correction_prompt("/test/workspace", "Frontend");

        assert!(prompt.contains("IDENTITY DRIFT DETECTED"));
        assert!(prompt.contains("frontend-agent"));
        assert!(prompt.contains("Frontend"));
        assert!(prompt.contains("/test/workspace"));
        assert!(prompt.contains("🤖 AGENT:"));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:411-420 function test_correction_prompt_generation <-> ./crates/ccswarm/src/tests/identity_tests.rs:506-526 function test_response_parser_performance
  Similarity: 73.15%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_correction_prompt_generation (lines 411-420) ---[0m
    fn test_correction_prompt_generation() {
        let monitor = IdentityMonitor::new("frontend-agent");
        let prompt = monitor.generate_correction_prompt("/test/workspace", "Frontend");

        assert!(prompt.contains("IDENTITY DRIFT DETECTED"));
        assert!(prompt.contains("frontend-agent"));
        assert!(prompt.contains("Frontend"));
        assert!(prompt.contains("/test/workspace"));
        assert!(prompt.contains("🤖 AGENT:"));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_response_parser_performance (lines 506-526) ---[0m
    fn test_response_parser_performance() {
        let parser = ResponseParser::new();
        let response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: agents/frontend-agent/
🎯 SCOPE: Component development
"#;

        let start = std::time::Instant::now();

        // Run 1000 parses
        for _ in 0..1000 {
            let _ = parser.parse_identity(response);
        }

        let duration = start.elapsed();
        assert!(
            duration.as_millis() < 100,
            "1000 response parses should complete in under 100ms"
        );
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:44-49 function test_agent_role_names <-> ./crates/ccswarm/src/tests/identity_tests.rs:244-268 function test_backend_boundary_checker_delegates_ui_tasks
  Similarity: 71.88%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_names (lines 44-49) ---[0m
    fn test_agent_role_names() {
        assert_eq!(default_frontend_role().name(), "Frontend");
        assert_eq!(default_backend_role().name(), "Backend");
        assert_eq!(default_devops_role().name(), "DevOps");
        assert_eq!(default_qa_role().name(), "QA");
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_backend_boundary_checker_delegates_ui_tasks (lines 244-268) ---[0m
    async fn test_backend_boundary_checker_delegates_ui_tasks() {
        let checker = TaskBoundaryChecker::new(default_backend_role());

        let ui_tasks = vec![
            create_test_task(
                "Create React component for login form",
                TaskType::Development,
            ),
            create_test_task("Style the user dashboard with CSS", TaskType::Development),
            create_test_task("Implement frontend routing", TaskType::Development),
        ];

        for task in ui_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Delegate { .. }),
                "Backend should delegate UI task: {}",
                task.description
            );

            if let TaskEvaluation::Delegate { target_agent, .. } = result {
                assert_eq!(target_agent, "frontend-agent");
            }
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:67-77 function test_agent_role_responsibilities <-> ./crates/ccswarm/src/tests/identity_tests.rs:111-129 function test_identity_monitor_response_analysis
  Similarity: 73.92%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_responsibilities (lines 67-77) ---[0m
    fn test_agent_role_responsibilities() {
        let frontend = default_frontend_role();
        let responsibilities = frontend.responsibilities();
        assert!(responsibilities.contains(&"UI Component Development".to_string()));
        assert!(responsibilities.contains(&"State Management".to_string()));

        let backend = default_backend_role();
        let responsibilities = backend.responsibilities();
        assert!(responsibilities.contains(&"API Development".to_string()));
        assert!(responsibilities.contains(&"Database Design".to_string()));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_monitor_response_analysis (lines 111-129) ---[0m
    async fn test_identity_monitor_response_analysis() {
        let mut monitor = IdentityMonitor::new("Frontend");

        let healthy_response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: /test/workspace
🎯 SCOPE: Component development

I'm working on creating a React component for the user profile page.
This is within my frontend specialization.
"#;

        let result = monitor.monitor_response(healthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::Healthy));

        let unhealthy_response = "Working on backend API endpoints...";
        let result = monitor.monitor_response(unhealthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::DriftDetected(_)));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:80-90 function test_agent_role_boundaries <-> ./crates/ccswarm/src/tests/identity_tests.rs:111-129 function test_identity_monitor_response_analysis
  Similarity: 73.92%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_boundaries (lines 80-90) ---[0m
    fn test_agent_role_boundaries() {
        let frontend = default_frontend_role();
        let boundaries = frontend.boundaries();
        assert!(boundaries.contains(&"No backend API development".to_string()));
        assert!(boundaries.contains(&"No database operations".to_string()));

        let devops = default_devops_role();
        let boundaries = devops.boundaries();
        assert!(boundaries.contains(&"No application code changes".to_string()));
        assert!(boundaries.contains(&"No business logic implementation".to_string()));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_monitor_response_analysis (lines 111-129) ---[0m
    async fn test_identity_monitor_response_analysis() {
        let mut monitor = IdentityMonitor::new("Frontend");

        let healthy_response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: /test/workspace
🎯 SCOPE: Component development

I'm working on creating a React component for the user profile page.
This is within my frontend specialization.
"#;

        let result = monitor.monitor_response(healthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::Healthy));

        let unhealthy_response = "Working on backend API endpoints...";
        let result = monitor.monitor_response(unhealthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::DriftDetected(_)));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:100-108 function test_identity_monitor_header_check <-> ./crates/ccswarm/src/tests/identity_tests.rs:168-189 function test_frontend_boundary_checker_accepts_ui_tasks
  Similarity: 71.13%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_monitor_header_check (lines 100-108) ---[0m
    fn test_identity_monitor_header_check() {
        let monitor = IdentityMonitor::new("Frontend");

        let valid_response = "🤖 AGENT: Frontend\n📁 WORKSPACE: /test\n🎯 SCOPE: UI work";
        assert!(monitor.check_identity_header(valid_response));

        let invalid_response = "Working on the task...";
        assert!(!monitor.check_identity_header(invalid_response));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_frontend_boundary_checker_accepts_ui_tasks (lines 168-189) ---[0m
    async fn test_frontend_boundary_checker_accepts_ui_tasks() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let ui_tasks = vec![
            create_test_task(
                "Create a React component for user profile",
                TaskType::Development,
            ),
            create_test_task("Implement CSS styling with Tailwind", TaskType::Development),
            create_test_task("Add state management with Redux", TaskType::Development),
            create_test_task("Write Jest tests for components", TaskType::Testing),
        ];

        for task in ui_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Frontend should accept UI task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:100-108 function test_identity_monitor_header_check <-> ./crates/ccswarm/src/tests/identity_tests.rs:220-241 function test_backend_boundary_checker_accepts_api_tasks
  Similarity: 71.13%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_monitor_header_check (lines 100-108) ---[0m
    fn test_identity_monitor_header_check() {
        let monitor = IdentityMonitor::new("Frontend");

        let valid_response = "🤖 AGENT: Frontend\n📁 WORKSPACE: /test\n🎯 SCOPE: UI work";
        assert!(monitor.check_identity_header(valid_response));

        let invalid_response = "Working on the task...";
        assert!(!monitor.check_identity_header(invalid_response));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_backend_boundary_checker_accepts_api_tasks (lines 220-241) ---[0m
    async fn test_backend_boundary_checker_accepts_api_tasks() {
        let checker = TaskBoundaryChecker::new(default_backend_role());

        let api_tasks = vec![
            create_test_task("Create REST API for user management", TaskType::Development),
            create_test_task(
                "Implement database query optimization",
                TaskType::Development,
            ),
            create_test_task("Add OAuth authentication flow", TaskType::Development),
            create_test_task("Set up Prisma ORM models", TaskType::Development),
        ];

        for task in api_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Backend should accept API task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:168-189 function test_frontend_boundary_checker_accepts_ui_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:475-482 function test_identity_env_vars
  Similarity: 72.46%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_frontend_boundary_checker_accepts_ui_tasks (lines 168-189) ---[0m
    async fn test_frontend_boundary_checker_accepts_ui_tasks() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());

        let ui_tasks = vec![
            create_test_task(
                "Create a React component for user profile",
                TaskType::Development,
            ),
            create_test_task("Implement CSS styling with Tailwind", TaskType::Development),
            create_test_task("Add state management with Redux", TaskType::Development),
            create_test_task("Write Jest tests for components", TaskType::Testing),
        ];

        for task in ui_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Frontend should accept UI task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_env_vars (lines 475-482) ---[0m
    fn test_identity_env_vars() {
        let identity = create_test_identity(default_frontend_role());

        // Check if environment variables are set up correctly
        assert!(identity.env_vars.contains_key("CCSWARM_AGENT_ID"));
        assert!(identity.env_vars.contains_key("CCSWARM_SESSION_ID"));
        assert!(identity.env_vars.contains_key("CCSWARM_ROLE"));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:220-241 function test_backend_boundary_checker_accepts_api_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:475-482 function test_identity_env_vars
  Similarity: 72.46%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_backend_boundary_checker_accepts_api_tasks (lines 220-241) ---[0m
    async fn test_backend_boundary_checker_accepts_api_tasks() {
        let checker = TaskBoundaryChecker::new(default_backend_role());

        let api_tasks = vec![
            create_test_task("Create REST API for user management", TaskType::Development),
            create_test_task(
                "Implement database query optimization",
                TaskType::Development,
            ),
            create_test_task("Add OAuth authentication flow", TaskType::Development),
            create_test_task("Set up Prisma ORM models", TaskType::Development),
        ];

        for task in api_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "Backend should accept API task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_env_vars (lines 475-482) ---[0m
    fn test_identity_env_vars() {
        let identity = create_test_identity(default_frontend_role());

        // Check if environment variables are set up correctly
        assert!(identity.env_vars.contains_key("CCSWARM_AGENT_ID"));
        assert!(identity.env_vars.contains_key("CCSWARM_SESSION_ID"));
        assert!(identity.env_vars.contains_key("CCSWARM_ROLE"));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:67-77 function test_agent_role_responsibilities <-> ./crates/ccswarm/src/tests/identity_tests.rs:80-90 function test_agent_role_boundaries
  Similarity: 96.85%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_responsibilities (lines 67-77) ---[0m
    fn test_agent_role_responsibilities() {
        let frontend = default_frontend_role();
        let responsibilities = frontend.responsibilities();
        assert!(responsibilities.contains(&"UI Component Development".to_string()));
        assert!(responsibilities.contains(&"State Management".to_string()));

        let backend = default_backend_role();
        let responsibilities = backend.responsibilities();
        assert!(responsibilities.contains(&"API Development".to_string()));
        assert!(responsibilities.contains(&"Database Design".to_string()));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_boundaries (lines 80-90) ---[0m
    fn test_agent_role_boundaries() {
        let frontend = default_frontend_role();
        let boundaries = frontend.boundaries();
        assert!(boundaries.contains(&"No backend API development".to_string()));
        assert!(boundaries.contains(&"No database operations".to_string()));

        let devops = default_devops_role();
        let boundaries = devops.boundaries();
        assert!(boundaries.contains(&"No application code changes".to_string()));
        assert!(boundaries.contains(&"No business logic implementation".to_string()));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:132-150 function test_response_parser <-> ./crates/ccswarm/src/tests/identity_tests.rs:411-420 function test_correction_prompt_generation
  Similarity: 70.69%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_response_parser (lines 132-150) ---[0m
    fn test_response_parser() {
        let parser = ResponseParser::new();

        let response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: agents/frontend-agent/
🎯 SCOPE: Component development

Working on React component...
"#;

        let parsed = parser.parse_identity(response);
        assert!(parsed.is_some());

        let (agent, workspace, scope) = parsed.unwrap();
        assert_eq!(agent, "Frontend");
        assert_eq!(workspace, "agents/frontend-agent/");
        assert_eq!(scope, "Component development");
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_correction_prompt_generation (lines 411-420) ---[0m
    fn test_correction_prompt_generation() {
        let monitor = IdentityMonitor::new("frontend-agent");
        let prompt = monitor.generate_correction_prompt("/test/workspace", "Frontend");

        assert!(prompt.contains("IDENTITY DRIFT DETECTED"));
        assert!(prompt.contains("frontend-agent"));
        assert!(prompt.contains("Frontend"));
        assert!(prompt.contains("/test/workspace"));
        assert!(prompt.contains("🤖 AGENT:"));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:100-108 function test_identity_monitor_header_check <-> ./crates/ccswarm/src/tests/identity_tests.rs:111-129 function test_identity_monitor_response_analysis
  Similarity: 73.09%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_monitor_header_check (lines 100-108) ---[0m
    fn test_identity_monitor_header_check() {
        let monitor = IdentityMonitor::new("Frontend");

        let valid_response = "🤖 AGENT: Frontend\n📁 WORKSPACE: /test\n🎯 SCOPE: UI work";
        assert!(monitor.check_identity_header(valid_response));

        let invalid_response = "Working on the task...";
        assert!(!monitor.check_identity_header(invalid_response));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_monitor_response_analysis (lines 111-129) ---[0m
    async fn test_identity_monitor_response_analysis() {
        let mut monitor = IdentityMonitor::new("Frontend");

        let healthy_response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: /test/workspace
🎯 SCOPE: Component development

I'm working on creating a React component for the user profile page.
This is within my frontend specialization.
"#;

        let result = monitor.monitor_response(healthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::Healthy));

        let unhealthy_response = "Working on backend API endpoints...";
        let result = monitor.monitor_response(unhealthy_response).await.unwrap();
        assert!(matches!(result, IdentityStatus::DriftDetected(_)));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:44-49 function test_agent_role_names <-> ./crates/ccswarm/src/tests/identity_tests.rs:271-293 function test_devops_boundary_checker_accepts_infrastructure_tasks
  Similarity: 70.06%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_names (lines 44-49) ---[0m
    fn test_agent_role_names() {
        assert_eq!(default_frontend_role().name(), "Frontend");
        assert_eq!(default_backend_role().name(), "Backend");
        assert_eq!(default_devops_role().name(), "DevOps");
        assert_eq!(default_qa_role().name(), "QA");
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_devops_boundary_checker_accepts_infrastructure_tasks (lines 271-293) ---[0m
    async fn test_devops_boundary_checker_accepts_infrastructure_tasks() {
        let checker = TaskBoundaryChecker::new(default_devops_role());

        let infra_tasks = vec![
            create_test_task("Set up Kubernetes cluster", TaskType::Infrastructure),
            create_test_task("Configure Docker containers", TaskType::Infrastructure),
            create_test_task("Deploy application to AWS", TaskType::Infrastructure),
            create_test_task("Set up CI/CD pipeline", TaskType::Infrastructure),
            create_test_task(
                "Configure monitoring with Prometheus",
                TaskType::Infrastructure,
            ),
        ];

        for task in infra_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "DevOps should accept infrastructure task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:100-108 function test_identity_monitor_header_check <-> ./crates/ccswarm/src/tests/identity_tests.rs:325-343 function test_qa_boundary_checker_accepts_testing_tasks
  Similarity: 70.92%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_monitor_header_check (lines 100-108) ---[0m
    fn test_identity_monitor_header_check() {
        let monitor = IdentityMonitor::new("Frontend");

        let valid_response = "🤖 AGENT: Frontend\n📁 WORKSPACE: /test\n🎯 SCOPE: UI work";
        assert!(monitor.check_identity_header(valid_response));

        let invalid_response = "Working on the task...";
        assert!(!monitor.check_identity_header(invalid_response));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_qa_boundary_checker_accepts_testing_tasks (lines 325-343) ---[0m
    async fn test_qa_boundary_checker_accepts_testing_tasks() {
        let checker = TaskBoundaryChecker::new(default_qa_role());

        let testing_tasks = vec![
            create_test_task("Write end-to-end tests with Cypress", TaskType::Testing),
            create_test_task("Perform load testing with K6", TaskType::Testing),
            create_test_task("Execute security penetration testing", TaskType::Testing),
            create_test_task("Create test automation framework", TaskType::Testing),
        ];

        for task in testing_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "QA should accept testing task: {}",
                task.description
            );
        }
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:52-64 function test_agent_role_technologies <-> ./crates/ccswarm/src/tests/identity_tests.rs:67-77 function test_agent_role_responsibilities
  Similarity: 81.36%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_technologies (lines 52-64) ---[0m
    fn test_agent_role_technologies() {
        let frontend = default_frontend_role();
        let technologies = frontend.technologies();
        assert!(technologies.contains(&"React".to_string()));
        assert!(technologies.contains(&"TypeScript".to_string()));
        assert!(technologies.contains(&"Tailwind CSS".to_string()));

        let backend = default_backend_role();
        let technologies = backend.technologies();
        assert!(technologies.contains(&"Node.js".to_string()));
        assert!(technologies.contains(&"Express".to_string()));
        assert!(technologies.contains(&"PostgreSQL".to_string()));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_responsibilities (lines 67-77) ---[0m
    fn test_agent_role_responsibilities() {
        let frontend = default_frontend_role();
        let responsibilities = frontend.responsibilities();
        assert!(responsibilities.contains(&"UI Component Development".to_string()));
        assert!(responsibilities.contains(&"State Management".to_string()));

        let backend = default_backend_role();
        let responsibilities = backend.responsibilities();
        assert!(responsibilities.contains(&"API Development".to_string()));
        assert!(responsibilities.contains(&"Database Design".to_string()));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:52-64 function test_agent_role_technologies <-> ./crates/ccswarm/src/tests/identity_tests.rs:80-90 function test_agent_role_boundaries
  Similarity: 81.36%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_technologies (lines 52-64) ---[0m
    fn test_agent_role_technologies() {
        let frontend = default_frontend_role();
        let technologies = frontend.technologies();
        assert!(technologies.contains(&"React".to_string()));
        assert!(technologies.contains(&"TypeScript".to_string()));
        assert!(technologies.contains(&"Tailwind CSS".to_string()));

        let backend = default_backend_role();
        let technologies = backend.technologies();
        assert!(technologies.contains(&"Node.js".to_string()));
        assert!(technologies.contains(&"Express".to_string()));
        assert!(technologies.contains(&"PostgreSQL".to_string()));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_boundaries (lines 80-90) ---[0m
    fn test_agent_role_boundaries() {
        let frontend = default_frontend_role();
        let boundaries = frontend.boundaries();
        assert!(boundaries.contains(&"No backend API development".to_string()));
        assert!(boundaries.contains(&"No database operations".to_string()));

        let devops = default_devops_role();
        let boundaries = devops.boundaries();
        assert!(boundaries.contains(&"No application code changes".to_string()));
        assert!(boundaries.contains(&"No business logic implementation".to_string()));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:411-420 function test_correction_prompt_generation <-> ./crates/ccswarm/src/tests/identity_tests.rs:487-503 function test_boundary_checker_performance
  Similarity: 72.31%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_correction_prompt_generation (lines 411-420) ---[0m
    fn test_correction_prompt_generation() {
        let monitor = IdentityMonitor::new("frontend-agent");
        let prompt = monitor.generate_correction_prompt("/test/workspace", "Frontend");

        assert!(prompt.contains("IDENTITY DRIFT DETECTED"));
        assert!(prompt.contains("frontend-agent"));
        assert!(prompt.contains("Frontend"));
        assert!(prompt.contains("/test/workspace"));
        assert!(prompt.contains("🤖 AGENT:"));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_boundary_checker_performance (lines 487-503) ---[0m
    async fn test_boundary_checker_performance() {
        let checker = TaskBoundaryChecker::new(default_frontend_role());
        let task = create_test_task("Create React component", TaskType::Development);

        let start = std::time::Instant::now();

        // Run 100 evaluations
        for _ in 0..100 {
            let _ = checker.evaluate_task(&task).await;
        }

        let duration = start.elapsed();
        assert!(
            duration.as_millis() < 1000,
            "100 boundary checks should complete in under 1 second"
        );
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:325-343 function test_qa_boundary_checker_accepts_testing_tasks <-> ./crates/ccswarm/src/tests/identity_tests.rs:475-482 function test_identity_env_vars
  Similarity: 72.27%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_qa_boundary_checker_accepts_testing_tasks (lines 325-343) ---[0m
    async fn test_qa_boundary_checker_accepts_testing_tasks() {
        let checker = TaskBoundaryChecker::new(default_qa_role());

        let testing_tasks = vec![
            create_test_task("Write end-to-end tests with Cypress", TaskType::Testing),
            create_test_task("Perform load testing with K6", TaskType::Testing),
            create_test_task("Execute security penetration testing", TaskType::Testing),
            create_test_task("Create test automation framework", TaskType::Testing),
        ];

        for task in testing_tasks {
            let result = checker.evaluate_task(&task).await;
            assert!(
                matches!(result, TaskEvaluation::Accept { .. }),
                "QA should accept testing task: {}",
                task.description
            );
        }
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_env_vars (lines 475-482) ---[0m
    fn test_identity_env_vars() {
        let identity = create_test_identity(default_frontend_role());

        // Check if environment variables are set up correctly
        assert!(identity.env_vars.contains_key("CCSWARM_AGENT_ID"));
        assert!(identity.env_vars.contains_key("CCSWARM_SESSION_ID"));
        assert!(identity.env_vars.contains_key("CCSWARM_ROLE"));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:67-77 function test_agent_role_responsibilities <-> ./crates/ccswarm/src/tests/identity_tests.rs:411-420 function test_correction_prompt_generation
  Similarity: 72.24%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_responsibilities (lines 67-77) ---[0m
    fn test_agent_role_responsibilities() {
        let frontend = default_frontend_role();
        let responsibilities = frontend.responsibilities();
        assert!(responsibilities.contains(&"UI Component Development".to_string()));
        assert!(responsibilities.contains(&"State Management".to_string()));

        let backend = default_backend_role();
        let responsibilities = backend.responsibilities();
        assert!(responsibilities.contains(&"API Development".to_string()));
        assert!(responsibilities.contains(&"Database Design".to_string()));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_correction_prompt_generation (lines 411-420) ---[0m
    fn test_correction_prompt_generation() {
        let monitor = IdentityMonitor::new("frontend-agent");
        let prompt = monitor.generate_correction_prompt("/test/workspace", "Frontend");

        assert!(prompt.contains("IDENTITY DRIFT DETECTED"));
        assert!(prompt.contains("frontend-agent"));
        assert!(prompt.contains("Frontend"));
        assert!(prompt.contains("/test/workspace"));
        assert!(prompt.contains("🤖 AGENT:"));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:80-90 function test_agent_role_boundaries <-> ./crates/ccswarm/src/tests/identity_tests.rs:411-420 function test_correction_prompt_generation
  Similarity: 72.24%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_boundaries (lines 80-90) ---[0m
    fn test_agent_role_boundaries() {
        let frontend = default_frontend_role();
        let boundaries = frontend.boundaries();
        assert!(boundaries.contains(&"No backend API development".to_string()));
        assert!(boundaries.contains(&"No database operations".to_string()));

        let devops = default_devops_role();
        let boundaries = devops.boundaries();
        assert!(boundaries.contains(&"No application code changes".to_string()));
        assert!(boundaries.contains(&"No business logic implementation".to_string()));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_correction_prompt_generation (lines 411-420) ---[0m
    fn test_correction_prompt_generation() {
        let monitor = IdentityMonitor::new("frontend-agent");
        let prompt = monitor.generate_correction_prompt("/test/workspace", "Frontend");

        assert!(prompt.contains("IDENTITY DRIFT DETECTED"));
        assert!(prompt.contains("frontend-agent"));
        assert!(prompt.contains("Frontend"));
        assert!(prompt.contains("/test/workspace"));
        assert!(prompt.contains("🤖 AGENT:"));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:153-163 function test_response_parser_missing_fields <-> ./crates/ccswarm/src/tests/identity_tests.rs:475-482 function test_identity_env_vars
  Similarity: 73.72%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_response_parser_missing_fields (lines 153-163) ---[0m
    fn test_response_parser_missing_fields() {
        let parser = ResponseParser::new();

        let incomplete_response = r#"
🤖 AGENT: Frontend
Working on React component...
"#;

        let parsed = parser.parse_identity(incomplete_response);
        assert!(parsed.is_none());
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_env_vars (lines 475-482) ---[0m
    fn test_identity_env_vars() {
        let identity = create_test_identity(default_frontend_role());

        // Check if environment variables are set up correctly
        assert!(identity.env_vars.contains_key("CCSWARM_AGENT_ID"));
        assert!(identity.env_vars.contains_key("CCSWARM_SESSION_ID"));
        assert!(identity.env_vars.contains_key("CCSWARM_ROLE"));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:100-108 function test_identity_monitor_header_check <-> ./crates/ccswarm/src/tests/identity_tests.rs:475-482 function test_identity_env_vars
  Similarity: 77.44%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_monitor_header_check (lines 100-108) ---[0m
    fn test_identity_monitor_header_check() {
        let monitor = IdentityMonitor::new("Frontend");

        let valid_response = "🤖 AGENT: Frontend\n📁 WORKSPACE: /test\n🎯 SCOPE: UI work";
        assert!(monitor.check_identity_header(valid_response));

        let invalid_response = "Working on the task...";
        assert!(!monitor.check_identity_header(invalid_response));
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_env_vars (lines 475-482) ---[0m
    fn test_identity_env_vars() {
        let identity = create_test_identity(default_frontend_role());

        // Check if environment variables are set up correctly
        assert!(identity.env_vars.contains_key("CCSWARM_AGENT_ID"));
        assert!(identity.env_vars.contains_key("CCSWARM_SESSION_ID"));
        assert!(identity.env_vars.contains_key("CCSWARM_ROLE"));
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:44-49 function test_agent_role_names <-> ./crates/ccswarm/src/tests/identity_tests.rs:153-163 function test_response_parser_missing_fields
  Similarity: 75.84%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_names (lines 44-49) ---[0m
    fn test_agent_role_names() {
        assert_eq!(default_frontend_role().name(), "Frontend");
        assert_eq!(default_backend_role().name(), "Backend");
        assert_eq!(default_devops_role().name(), "DevOps");
        assert_eq!(default_qa_role().name(), "QA");
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_response_parser_missing_fields (lines 153-163) ---[0m
    fn test_response_parser_missing_fields() {
        let parser = ResponseParser::new();

        let incomplete_response = r#"
🤖 AGENT: Frontend
Working on React component...
"#;

        let parsed = parser.parse_identity(incomplete_response);
        assert!(parsed.is_none());
    }

  ./crates/ccswarm/src/tests/identity_tests.rs:44-49 function test_agent_role_names <-> ./crates/ccswarm/src/tests/identity_tests.rs:475-482 function test_identity_env_vars
  Similarity: 72.25%

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_agent_role_names (lines 44-49) ---[0m
    fn test_agent_role_names() {
        assert_eq!(default_frontend_role().name(), "Frontend");
        assert_eq!(default_backend_role().name(), "Backend");
        assert_eq!(default_devops_role().name(), "DevOps");
        assert_eq!(default_qa_role().name(), "QA");
    }

[36m--- ./crates/ccswarm/src/tests/identity_tests.rs:test_identity_env_vars (lines 475-482) ---[0m
    fn test_identity_env_vars() {
        let identity = create_test_identity(default_frontend_role());

        // Check if environment variables are set up correctly
        assert!(identity.env_vars.contains_key("CCSWARM_AGENT_ID"));
        assert!(identity.env_vars.contains_key("CCSWARM_SESSION_ID"));
        assert!(identity.env_vars.contains_key("CCSWARM_ROLE"));
    }


Duplicates in ./crates/ccswarm/src/semantic.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/semantic.rs:61-67 method find_dependencies <-> ./crates/ccswarm/src/semantic.rs:69-75 method find_dependents
  Similarity: 99.10%
  Classes: SemanticAnalyzer <-> SemanticAnalyzer

[36m--- ./crates/ccswarm/src/semantic.rs:find_dependencies (lines 61-67) ---[0m
    pub fn find_dependencies(&self, symbol_name: &str) -> Vec<&Symbol> {
        self.relationships
            .iter()
            .filter(|r| r.from == symbol_name)
            .filter_map(|r| self.symbols.get(&r.to))
            .collect()
    }

[36m--- ./crates/ccswarm/src/semantic.rs:find_dependents (lines 69-75) ---[0m
    pub fn find_dependents(&self, symbol_name: &str) -> Vec<&Symbol> {
        self.relationships
            .iter()
            .filter(|r| r.to == symbol_name)
            .filter_map(|r| self.symbols.get(&r.from))
            .collect()
    }


Duplicates in ./crates/ccswarm/src/utils/filesystem.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/utils/filesystem.rs:28-36 method save_json <-> ./crates/ccswarm/src/utils/filesystem.rs:39-47 method load_json
  Similarity: 77.55%
  Classes: FsUtils <-> FsUtils

[36m--- ./crates/ccswarm/src/utils/filesystem.rs:save_json (lines 28-36) ---[0m
    pub async fn save_json<T: serde::Serialize>(
        data: &T,
        path: &Path,
        _name: &str,
    ) -> Result<()> {
        let content = serde_json::to_string_pretty(data)
            .map_err(CCSwarmError::SerdeJson)?;
        Self::write_file(path, &content, _name).await
    }

[36m--- ./crates/ccswarm/src/utils/filesystem.rs:load_json (lines 39-47) ---[0m
    pub async fn load_json<T: serde::de::DeserializeOwned>(
        path: &Path,
        _name: &str,
    ) -> Result<T> {
        let content = fs::read_to_string(path).await
            .map_err(CCSwarmError::Io)?;
        serde_json::from_str(&content)
            .map_err(CCSwarmError::SerdeJson)
    }

  ./crates/ccswarm/src/utils/filesystem.rs:19-25 method ensure_dir_exists <-> ./crates/ccswarm/src/utils/filesystem.rs:56-62 method remove_dir_all
  Similarity: 88.39%
  Classes: FsUtils <-> FsUtils

[36m--- ./crates/ccswarm/src/utils/filesystem.rs:ensure_dir_exists (lines 19-25) ---[0m
    pub async fn ensure_dir_exists(path: &Path, _context: &str) -> Result<()> {
        if !path.exists() {
            fs::create_dir_all(path).await
                .map_err(|e| CCSwarmError::Io(e))?;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/utils/filesystem.rs:remove_dir_all (lines 56-62) ---[0m
    pub async fn remove_dir_all(path: &Path, _name: &str) -> Result<()> {
        if path.exists() {
            fs::remove_dir_all(path).await
                .map_err(CCSwarmError::Io)?;
        }
        Ok(())
    }

  ./crates/ccswarm/src/utils/filesystem.rs:19-25 method ensure_dir_exists <-> ./crates/ccswarm/src/utils/filesystem.rs:39-47 method load_json
  Similarity: 76.94%
  Classes: FsUtils <-> FsUtils

[36m--- ./crates/ccswarm/src/utils/filesystem.rs:ensure_dir_exists (lines 19-25) ---[0m
    pub async fn ensure_dir_exists(path: &Path, _context: &str) -> Result<()> {
        if !path.exists() {
            fs::create_dir_all(path).await
                .map_err(|e| CCSwarmError::Io(e))?;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/utils/filesystem.rs:load_json (lines 39-47) ---[0m
    pub async fn load_json<T: serde::de::DeserializeOwned>(
        path: &Path,
        _name: &str,
    ) -> Result<T> {
        let content = fs::read_to_string(path).await
            .map_err(CCSwarmError::Io)?;
        serde_json::from_str(&content)
            .map_err(CCSwarmError::SerdeJson)
    }

  ./crates/ccswarm/src/utils/filesystem.rs:19-25 method ensure_dir_exists <-> ./crates/ccswarm/src/utils/filesystem.rs:28-36 method save_json
  Similarity: 76.29%
  Classes: FsUtils <-> FsUtils

[36m--- ./crates/ccswarm/src/utils/filesystem.rs:ensure_dir_exists (lines 19-25) ---[0m
    pub async fn ensure_dir_exists(path: &Path, _context: &str) -> Result<()> {
        if !path.exists() {
            fs::create_dir_all(path).await
                .map_err(|e| CCSwarmError::Io(e))?;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/utils/filesystem.rs:save_json (lines 28-36) ---[0m
    pub async fn save_json<T: serde::Serialize>(
        data: &T,
        path: &Path,
        _name: &str,
    ) -> Result<()> {
        let content = serde_json::to_string_pretty(data)
            .map_err(CCSwarmError::SerdeJson)?;
        Self::write_file(path, &content, _name).await
    }

  ./crates/ccswarm/src/utils/filesystem.rs:39-47 method load_json <-> ./crates/ccswarm/src/utils/filesystem.rs:56-62 method remove_dir_all
  Similarity: 75.85%
  Classes: FsUtils <-> FsUtils

[36m--- ./crates/ccswarm/src/utils/filesystem.rs:load_json (lines 39-47) ---[0m
    pub async fn load_json<T: serde::de::DeserializeOwned>(
        path: &Path,
        _name: &str,
    ) -> Result<T> {
        let content = fs::read_to_string(path).await
            .map_err(CCSwarmError::Io)?;
        serde_json::from_str(&content)
            .map_err(CCSwarmError::SerdeJson)
    }

[36m--- ./crates/ccswarm/src/utils/filesystem.rs:remove_dir_all (lines 56-62) ---[0m
    pub async fn remove_dir_all(path: &Path, _name: &str) -> Result<()> {
        if path.exists() {
            fs::remove_dir_all(path).await
                .map_err(CCSwarmError::Io)?;
        }
        Ok(())
    }

  ./crates/ccswarm/src/utils/filesystem.rs:28-36 method save_json <-> ./crates/ccswarm/src/utils/filesystem.rs:56-62 method remove_dir_all
  Similarity: 73.60%
  Classes: FsUtils <-> FsUtils

[36m--- ./crates/ccswarm/src/utils/filesystem.rs:save_json (lines 28-36) ---[0m
    pub async fn save_json<T: serde::Serialize>(
        data: &T,
        path: &Path,
        _name: &str,
    ) -> Result<()> {
        let content = serde_json::to_string_pretty(data)
            .map_err(CCSwarmError::SerdeJson)?;
        Self::write_file(path, &content, _name).await
    }

[36m--- ./crates/ccswarm/src/utils/filesystem.rs:remove_dir_all (lines 56-62) ---[0m
    pub async fn remove_dir_all(path: &Path, _name: &str) -> Result<()> {
        if path.exists() {
            fs::remove_dir_all(path).await
                .map_err(CCSwarmError::Io)?;
        }
        Ok(())
    }


Duplicates in ./crates/ccswarm/tests/simple_integration_test.rs:
------------------------------------------------------------
  ./crates/ccswarm/tests/simple_integration_test.rs:105-174 function test_dialogue_coordination_integration <-> ./crates/ccswarm/tests/simple_integration_test.rs:177-230 function test_session_with_memory
  Similarity: 88.23%

[36m--- ./crates/ccswarm/tests/simple_integration_test.rs:test_dialogue_coordination_integration (lines 105-174) ---[0m
async fn test_dialogue_coordination_integration() -> Result<()> {
    println!("💬 Testing Dialogue Coordination System Integration...");

    let mut dialogue_bus = DialogueCoordinationBus::new().await?;

    // Test conversation creation
    let participants = vec!["frontend-agent".to_string(), "backend-agent".to_string()];
    let context = ConversationContext {
        project_context: Some("User authentication project".to_string()),
        task_context: Some("API and UI coordination".to_string()),
        shared_resources: vec!["database".to_string(), "auth-service".to_string()],
        constraints: vec!["security-first".to_string()],
        goals: vec![
            "implement secure auth".to_string(),
            "maintain UX quality".to_string(),
        ],
        related_conversations: vec![],
    };

    let conversation_id = dialogue_bus
        .start_conversation(
            participants.clone(),
            "Authentication Implementation Coordination".to_string(),
            ConversationType::TaskCoordination,
            context,
        )
        .await?;

    assert!(!conversation_id.is_empty());
    assert!(dialogue_bus.conversations.contains_key(&conversation_id));

    // Test dialogue messages
    dialogue_bus
        .add_dialogue_message(
            &conversation_id,
            "frontend-agent".to_string(),
            "I need to know the API endpoints for user login and registration".to_string(),
            DialogueMessageType::Question,
            EmotionalTone::Curious,
            ResponseExpectation::ThoughtfulResponse,
        )
        .await?;

    dialogue_bus
        .add_dialogue_message(
            &conversation_id,
            "backend-agent".to_string(),
            "I'll provide POST /auth/login and POST /auth/register endpoints with JWT response"
                .to_string(),
            DialogueMessageType::Answer,
            EmotionalTone::Confident,
            ResponseExpectation::Acknowledgment,
        )
        .await?;

    // Test conversation summary
    let summary = dialogue_bus
        .get_conversation_summary(&conversation_id)
        .unwrap();
    assert_eq!(summary.participants.len(), 2);
    assert_eq!(summary.message_count, 2);
    assert_eq!(summary.topic, "Authentication Implementation Coordination");

    // Test active conversations
    let active_conversations = dialogue_bus.get_active_conversations();
    assert!(!active_conversations.is_empty());

    println!("✅ Dialogue Coordination System: PASSED");
    Ok(())
}

[36m--- ./crates/ccswarm/tests/simple_integration_test.rs:test_session_with_memory (lines 177-230) ---[0m
async fn test_session_with_memory() -> Result<()> {
    println!("📝 Testing Session with Memory Integration...");

    let session = AgentSession::new(
        "test-agent".to_string(),
        default_backend_role(),
        "/tmp/test".to_string(),
        Some("Integration test session".to_string()),
    );

    // Test session has memory
    assert_eq!(session.memory.session_id, session.id);
    assert_eq!(session.memory.agent_id, session.agent_id);

    // Test memory operations through session
    let mut test_session = session.clone();

    test_session.add_memory(
        "Working on API implementation".to_string(),
        WorkingMemoryType::TaskInstructions,
        0.9,
    );

    test_session.set_task_context(
        "task-api-123".to_string(),
        "Implement REST API for user management".to_string(),
    );

    let mut episode_context = HashMap::new();
    episode_context.insert("complexity".to_string(), "high".to_string());

    test_session.add_episode(
        EpisodeType::ProblemSolving,
        "Solved complex authentication issue".to_string(),
        episode_context,
        EpisodeOutcome::Success {
            metrics: HashMap::new(),
        },
    );

    // Test memory consolidation
    test_session.consolidate_memories();

    // Test memory retrieval
    let _memories = test_session.retrieve_memories("authentication");
    // Should find relevant memories

    // Test memory summary
    let memory_summary = test_session.get_memory_summary();
    assert_eq!(memory_summary.session_id, test_session.id);

    println!("✅ Session Memory Integration: PASSED");
    Ok(())
}

  ./crates/ccswarm/tests/simple_integration_test.rs:51-102 function test_session_memory_integration <-> ./crates/ccswarm/tests/simple_integration_test.rs:105-174 function test_dialogue_coordination_integration
  Similarity: 86.31%

[36m--- ./crates/ccswarm/tests/simple_integration_test.rs:test_session_memory_integration (lines 51-102) ---[0m
async fn test_session_memory_integration() -> Result<()> {
    println!("🧠 Testing Session Memory System Integration...");

    let mut memory = SessionMemory::new("session-123".to_string(), "agent-456".to_string());

    // Test working memory
    memory.add_to_working_memory(
        "Task: Create user authentication".to_string(),
        WorkingMemoryType::TaskInstructions,
        0.8,
    );
    assert!(!memory.working_memory.current_items.is_empty());

    // Test task context
    memory.set_task_context(
        "task-123".to_string(),
        "User authentication implementation".to_string(),
    );
    assert!(memory.working_memory.active_task_context.is_some());

    // Test episodic memory
    let mut context = HashMap::new();
    context.insert("task_type".to_string(), "development".to_string());

    memory.add_episode(
        EpisodeType::TaskCompletion,
        "Successfully implemented authentication".to_string(),
        context,
        EpisodeOutcome::Success {
            metrics: HashMap::new(),
        },
    );
    assert!(!memory.episodic_memory.episodes.is_empty());

    // Test memory consolidation
    memory.consolidate_memories();

    // Test memory retrieval
    let retrieval_result = memory.retrieve_relevant_memories("authentication");
    assert!(
        !retrieval_result.working_memory_items.is_empty()
            || !retrieval_result.relevant_episodes.is_empty()
    );

    // Test memory summary
    let summary = memory.generate_memory_summary();
    assert_eq!(summary.session_id, "session-123");
    assert_eq!(summary.agent_id, "agent-456");

    println!("✅ Session Memory System: PASSED");
    Ok(())
}

[36m--- ./crates/ccswarm/tests/simple_integration_test.rs:test_dialogue_coordination_integration (lines 105-174) ---[0m
async fn test_dialogue_coordination_integration() -> Result<()> {
    println!("💬 Testing Dialogue Coordination System Integration...");

    let mut dialogue_bus = DialogueCoordinationBus::new().await?;

    // Test conversation creation
    let participants = vec!["frontend-agent".to_string(), "backend-agent".to_string()];
    let context = ConversationContext {
        project_context: Some("User authentication project".to_string()),
        task_context: Some("API and UI coordination".to_string()),
        shared_resources: vec!["database".to_string(), "auth-service".to_string()],
        constraints: vec!["security-first".to_string()],
        goals: vec![
            "implement secure auth".to_string(),
            "maintain UX quality".to_string(),
        ],
        related_conversations: vec![],
    };

    let conversation_id = dialogue_bus
        .start_conversation(
            participants.clone(),
            "Authentication Implementation Coordination".to_string(),
            ConversationType::TaskCoordination,
            context,
        )
        .await?;

    assert!(!conversation_id.is_empty());
    assert!(dialogue_bus.conversations.contains_key(&conversation_id));

    // Test dialogue messages
    dialogue_bus
        .add_dialogue_message(
            &conversation_id,
            "frontend-agent".to_string(),
            "I need to know the API endpoints for user login and registration".to_string(),
            DialogueMessageType::Question,
            EmotionalTone::Curious,
            ResponseExpectation::ThoughtfulResponse,
        )
        .await?;

    dialogue_bus
        .add_dialogue_message(
            &conversation_id,
            "backend-agent".to_string(),
            "I'll provide POST /auth/login and POST /auth/register endpoints with JWT response"
                .to_string(),
            DialogueMessageType::Answer,
            EmotionalTone::Confident,
            ResponseExpectation::Acknowledgment,
        )
        .await?;

    // Test conversation summary
    let summary = dialogue_bus
        .get_conversation_summary(&conversation_id)
        .unwrap();
    assert_eq!(summary.participants.len(), 2);
    assert_eq!(summary.message_count, 2);
    assert_eq!(summary.topic, "Authentication Implementation Coordination");

    // Test active conversations
    let active_conversations = dialogue_bus.get_active_conversations();
    assert!(!active_conversations.is_empty());

    println!("✅ Dialogue Coordination System: PASSED");
    Ok(())
}

  ./crates/ccswarm/tests/simple_integration_test.rs:51-102 function test_session_memory_integration <-> ./crates/ccswarm/tests/simple_integration_test.rs:177-230 function test_session_with_memory
  Similarity: 86.10%

[36m--- ./crates/ccswarm/tests/simple_integration_test.rs:test_session_memory_integration (lines 51-102) ---[0m
async fn test_session_memory_integration() -> Result<()> {
    println!("🧠 Testing Session Memory System Integration...");

    let mut memory = SessionMemory::new("session-123".to_string(), "agent-456".to_string());

    // Test working memory
    memory.add_to_working_memory(
        "Task: Create user authentication".to_string(),
        WorkingMemoryType::TaskInstructions,
        0.8,
    );
    assert!(!memory.working_memory.current_items.is_empty());

    // Test task context
    memory.set_task_context(
        "task-123".to_string(),
        "User authentication implementation".to_string(),
    );
    assert!(memory.working_memory.active_task_context.is_some());

    // Test episodic memory
    let mut context = HashMap::new();
    context.insert("task_type".to_string(), "development".to_string());

    memory.add_episode(
        EpisodeType::TaskCompletion,
        "Successfully implemented authentication".to_string(),
        context,
        EpisodeOutcome::Success {
            metrics: HashMap::new(),
        },
    );
    assert!(!memory.episodic_memory.episodes.is_empty());

    // Test memory consolidation
    memory.consolidate_memories();

    // Test memory retrieval
    let retrieval_result = memory.retrieve_relevant_memories("authentication");
    assert!(
        !retrieval_result.working_memory_items.is_empty()
            || !retrieval_result.relevant_episodes.is_empty()
    );

    // Test memory summary
    let summary = memory.generate_memory_summary();
    assert_eq!(summary.session_id, "session-123");
    assert_eq!(summary.agent_id, "agent-456");

    println!("✅ Session Memory System: PASSED");
    Ok(())
}

[36m--- ./crates/ccswarm/tests/simple_integration_test.rs:test_session_with_memory (lines 177-230) ---[0m
async fn test_session_with_memory() -> Result<()> {
    println!("📝 Testing Session with Memory Integration...");

    let session = AgentSession::new(
        "test-agent".to_string(),
        default_backend_role(),
        "/tmp/test".to_string(),
        Some("Integration test session".to_string()),
    );

    // Test session has memory
    assert_eq!(session.memory.session_id, session.id);
    assert_eq!(session.memory.agent_id, session.agent_id);

    // Test memory operations through session
    let mut test_session = session.clone();

    test_session.add_memory(
        "Working on API implementation".to_string(),
        WorkingMemoryType::TaskInstructions,
        0.9,
    );

    test_session.set_task_context(
        "task-api-123".to_string(),
        "Implement REST API for user management".to_string(),
    );

    let mut episode_context = HashMap::new();
    episode_context.insert("complexity".to_string(), "high".to_string());

    test_session.add_episode(
        EpisodeType::ProblemSolving,
        "Solved complex authentication issue".to_string(),
        episode_context,
        EpisodeOutcome::Success {
            metrics: HashMap::new(),
        },
    );

    // Test memory consolidation
    test_session.consolidate_memories();

    // Test memory retrieval
    let _memories = test_session.retrieve_memories("authentication");
    // Should find relevant memories

    // Test memory summary
    let memory_summary = test_session.get_memory_summary();
    assert_eq!(memory_summary.session_id, test_session.id);

    println!("✅ Session Memory Integration: PASSED");
    Ok(())
}

  ./crates/ccswarm/tests/simple_integration_test.rs:24-48 function test_agent_personality_integration <-> ./crates/ccswarm/tests/simple_integration_test.rs:177-230 function test_session_with_memory
  Similarity: 81.77%

[36m--- ./crates/ccswarm/tests/simple_integration_test.rs:test_agent_personality_integration (lines 24-48) ---[0m
async fn test_agent_personality_integration() -> Result<()> {
    println!("🧠 Testing Agent Personality System Integration...");

    let frontend_role = default_frontend_role();
    let personality = AgentPersonality::new("test-agent".to_string());

    // Test personality creation
    assert_eq!(personality.style, "test-agent");
    assert!(!personality.skills.is_empty());
    assert!(personality.skills.contains_key("react"));

    // Test skill experience - simplified test
    let mut test_personality = personality.clone();
    assert!(test_personality.skills.contains_key("react"));

    // Test personality has expected fields
    assert!(!test_personality.approach.is_empty());
    assert!(!test_personality.working_style.is_empty());

    // Test relevant skills
    assert!(!test_personality.skills.is_empty());

    println!("✅ Agent Personality System: PASSED");
    Ok(())
}

[36m--- ./crates/ccswarm/tests/simple_integration_test.rs:test_session_with_memory (lines 177-230) ---[0m
async fn test_session_with_memory() -> Result<()> {
    println!("📝 Testing Session with Memory Integration...");

    let session = AgentSession::new(
        "test-agent".to_string(),
        default_backend_role(),
        "/tmp/test".to_string(),
        Some("Integration test session".to_string()),
    );

    // Test session has memory
    assert_eq!(session.memory.session_id, session.id);
    assert_eq!(session.memory.agent_id, session.agent_id);

    // Test memory operations through session
    let mut test_session = session.clone();

    test_session.add_memory(
        "Working on API implementation".to_string(),
        WorkingMemoryType::TaskInstructions,
        0.9,
    );

    test_session.set_task_context(
        "task-api-123".to_string(),
        "Implement REST API for user management".to_string(),
    );

    let mut episode_context = HashMap::new();
    episode_context.insert("complexity".to_string(), "high".to_string());

    test_session.add_episode(
        EpisodeType::ProblemSolving,
        "Solved complex authentication issue".to_string(),
        episode_context,
        EpisodeOutcome::Success {
            metrics: HashMap::new(),
        },
    );

    // Test memory consolidation
    test_session.consolidate_memories();

    // Test memory retrieval
    let _memories = test_session.retrieve_memories("authentication");
    // Should find relevant memories

    // Test memory summary
    let memory_summary = test_session.get_memory_summary();
    assert_eq!(memory_summary.session_id, test_session.id);

    println!("✅ Session Memory Integration: PASSED");
    Ok(())
}

  ./crates/ccswarm/tests/simple_integration_test.rs:24-48 function test_agent_personality_integration <-> ./crates/ccswarm/tests/simple_integration_test.rs:51-102 function test_session_memory_integration
  Similarity: 78.14%

[36m--- ./crates/ccswarm/tests/simple_integration_test.rs:test_agent_personality_integration (lines 24-48) ---[0m
async fn test_agent_personality_integration() -> Result<()> {
    println!("🧠 Testing Agent Personality System Integration...");

    let frontend_role = default_frontend_role();
    let personality = AgentPersonality::new("test-agent".to_string());

    // Test personality creation
    assert_eq!(personality.style, "test-agent");
    assert!(!personality.skills.is_empty());
    assert!(personality.skills.contains_key("react"));

    // Test skill experience - simplified test
    let mut test_personality = personality.clone();
    assert!(test_personality.skills.contains_key("react"));

    // Test personality has expected fields
    assert!(!test_personality.approach.is_empty());
    assert!(!test_personality.working_style.is_empty());

    // Test relevant skills
    assert!(!test_personality.skills.is_empty());

    println!("✅ Agent Personality System: PASSED");
    Ok(())
}

[36m--- ./crates/ccswarm/tests/simple_integration_test.rs:test_session_memory_integration (lines 51-102) ---[0m
async fn test_session_memory_integration() -> Result<()> {
    println!("🧠 Testing Session Memory System Integration...");

    let mut memory = SessionMemory::new("session-123".to_string(), "agent-456".to_string());

    // Test working memory
    memory.add_to_working_memory(
        "Task: Create user authentication".to_string(),
        WorkingMemoryType::TaskInstructions,
        0.8,
    );
    assert!(!memory.working_memory.current_items.is_empty());

    // Test task context
    memory.set_task_context(
        "task-123".to_string(),
        "User authentication implementation".to_string(),
    );
    assert!(memory.working_memory.active_task_context.is_some());

    // Test episodic memory
    let mut context = HashMap::new();
    context.insert("task_type".to_string(), "development".to_string());

    memory.add_episode(
        EpisodeType::TaskCompletion,
        "Successfully implemented authentication".to_string(),
        context,
        EpisodeOutcome::Success {
            metrics: HashMap::new(),
        },
    );
    assert!(!memory.episodic_memory.episodes.is_empty());

    // Test memory consolidation
    memory.consolidate_memories();

    // Test memory retrieval
    let retrieval_result = memory.retrieve_relevant_memories("authentication");
    assert!(
        !retrieval_result.working_memory_items.is_empty()
            || !retrieval_result.relevant_episodes.is_empty()
    );

    // Test memory summary
    let summary = memory.generate_memory_summary();
    assert_eq!(summary.session_id, "session-123");
    assert_eq!(summary.agent_id, "agent-456");

    println!("✅ Session Memory System: PASSED");
    Ok(())
}


Duplicates in ./crates/ccswarm/src/resource/mod.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/resource/mod.rs:365-433 method update_agent_resources <-> ./crates/ccswarm/src/resource/mod.rs:465-510 method find_agent_process_usage
  Similarity: 90.37%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_agent_resources (lines 365-433) ---[0m
    async fn update_agent_resources(&self, agent_id: &str) -> Result<()> {
        let (pid, limits) = {
            let agents = self
                .agents
                .read()
                .map_err(|e| anyhow::anyhow!("Failed to acquire read lock: {}", e))?;
            let state = agents.get(agent_id).context("Agent not found")?;
            (state.pid, state.limits.clone())
        };

        // Get resource usage
        let usage = if let Some(pid) = pid {
            self.get_process_usage(pid)?
        } else {
            // If no PID, try to find it by agent name pattern
            self.find_agent_process_usage(agent_id)?
        };

        // Update agent state
        let mut should_suspend = false;
        let mut should_emit_limit_event = false;
        {
            let mut agents = self
                .agents
                .write()
                .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
            if let Some(state) = agents.get_mut(agent_id) {
                state.update_usage(usage.clone());

                // Check for suspension
                if state.should_suspend() {
                    should_suspend = true;
                    state.is_suspended = true;
                }

                // Check for limit violations
                if limits.enforce_limits && state.is_exceeding_limits() {
                    should_emit_limit_event = true;
                }
            }
        }

        // Emit events outside of lock
        if should_suspend {
            let _ = self.event_tx.send(ResourceEvent::AgentSuspended {
                agent_id: agent_id.to_string(),
                reason: "Idle timeout exceeded".to_string(),
            });
        }

        if should_emit_limit_event && usage.cpu_percent > limits.max_cpu_percent {
            let _ = self.event_tx.send(ResourceEvent::LimitExceeded {
                agent_id: agent_id.to_string(),
                resource_type: "CPU".to_string(),
                current_value: usage.cpu_percent as f64,
                limit_value: limits.max_cpu_percent as f64,
            });
        }
        if should_emit_limit_event && usage.memory_bytes > limits.max_memory_bytes {
            let _ = self.event_tx.send(ResourceEvent::LimitExceeded {
                agent_id: agent_id.to_string(),
                resource_type: "Memory".to_string(),
                current_value: usage.memory_bytes as f64,
                limit_value: limits.max_memory_bytes as f64,
            });
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:find_agent_process_usage (lines 465-510) ---[0m
    fn find_agent_process_usage(&self, _agent_id: &str) -> Result<ResourceUsage> {
        let system = self
            .system
            .lock()
            .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;

        // Look for processes that might be the agent
        // Pattern: ccswarm processes with agent ID in command line
        for process in system.processes().values() {
            // Try to get process name and cmd
            if let Some(name) = process.name().to_str() {
                if name.contains("ccswarm")
                    || process
                        .exe()
                        .map(|exe| exe.to_string_lossy().contains("ccswarm"))
                        .unwrap_or(false)
                {
                    // Check if this might be our agent by looking at the process
                    let total_memory = system.total_memory();
                    let memory_bytes = process.memory();
                    let memory_percent = if total_memory > 0 {
                        (memory_bytes as f32 / total_memory as f32) * 100.0
                    } else {
                        0.0
                    };

                    return Ok(ResourceUsage {
                        cpu_percent: process.cpu_usage(),
                        memory_bytes,
                        memory_percent,
                        thread_count: 1,
                        timestamp: Utc::now(),
                    });
                }
            }
        }

        // If no process found, return minimal usage
        Ok(ResourceUsage {
            cpu_percent: 0.0,
            memory_bytes: 0,
            memory_percent: 0.0,
            thread_count: 0,
            timestamp: Utc::now(),
        })
    }

  ./crates/ccswarm/src/resource/mod.rs:465-510 method find_agent_process_usage <-> ./crates/ccswarm/src/resource/mod.rs:534-601 method get_efficiency_stats
  Similarity: 82.98%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:find_agent_process_usage (lines 465-510) ---[0m
    fn find_agent_process_usage(&self, _agent_id: &str) -> Result<ResourceUsage> {
        let system = self
            .system
            .lock()
            .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;

        // Look for processes that might be the agent
        // Pattern: ccswarm processes with agent ID in command line
        for process in system.processes().values() {
            // Try to get process name and cmd
            if let Some(name) = process.name().to_str() {
                if name.contains("ccswarm")
                    || process
                        .exe()
                        .map(|exe| exe.to_string_lossy().contains("ccswarm"))
                        .unwrap_or(false)
                {
                    // Check if this might be our agent by looking at the process
                    let total_memory = system.total_memory();
                    let memory_bytes = process.memory();
                    let memory_percent = if total_memory > 0 {
                        (memory_bytes as f32 / total_memory as f32) * 100.0
                    } else {
                        0.0
                    };

                    return Ok(ResourceUsage {
                        cpu_percent: process.cpu_usage(),
                        memory_bytes,
                        memory_percent,
                        thread_count: 1,
                        timestamp: Utc::now(),
                    });
                }
            }
        }

        // If no process found, return minimal usage
        Ok(ResourceUsage {
            cpu_percent: 0.0,
            memory_bytes: 0,
            memory_percent: 0.0,
            thread_count: 0,
            timestamp: Utc::now(),
        })
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_efficiency_stats (lines 534-601) ---[0m
    pub fn get_efficiency_stats(&self) -> ResourceEfficiencyStats {
        let agents_guard = self.agents.read().map_err(|e| {
            tracing::error!("Failed to acquire read lock: {}", e);
            e
        });

        if let Ok(agents) = agents_guard {
            let total_agents = agents.len();
            let suspended_agents = agents.values().filter(|s| s.is_suspended).count();
            let active_agents = total_agents - suspended_agents;

            let (total_cpu, total_memory, total_memory_pct) = agents
                .values()
                .filter(|s| !s.is_suspended)
                .fold((0.0, 0u64, 0.0), |(cpu, mem, mem_pct), state| {
                    (
                        cpu + state.current_usage.cpu_percent,
                        mem + state.current_usage.memory_bytes,
                        mem_pct + state.current_usage.memory_percent,
                    )
                });

            let avg_cpu = if active_agents > 0 {
                total_cpu / active_agents as f32
            } else {
                0.0
            };

            let avg_memory = if active_agents > 0 {
                total_memory / active_agents as u64
            } else {
                0
            };

            let avg_memory_pct = if active_agents > 0 {
                total_memory_pct / active_agents as f32
            } else {
                0.0
            };

            ResourceEfficiencyStats {
                total_agents,
                active_agents,
                suspended_agents,
                average_cpu_usage: avg_cpu,
                average_memory_usage: avg_memory,
                average_memory_percent: avg_memory_pct,
                total_memory_usage: total_memory,
                suspension_rate: if total_agents > 0 {
                    (suspended_agents as f32 / total_agents as f32) * 100.0
                } else {
                    0.0
                },
            }
        } else {
            // Return default stats if lock acquisition failed
            ResourceEfficiencyStats {
                total_agents: 0,
                active_agents: 0,
                suspended_agents: 0,
                average_cpu_usage: 0.0,
                average_memory_usage: 0,
                average_memory_percent: 0.0,
                total_memory_usage: 0,
                suspension_rate: 0.0,
            }
        }
    }

  ./crates/ccswarm/src/resource/mod.rs:436-462 method get_process_usage <-> ./crates/ccswarm/src/resource/mod.rs:534-601 method get_efficiency_stats
  Similarity: 83.56%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_process_usage (lines 436-462) ---[0m
    fn get_process_usage(&self, pid: u32) -> Result<ResourceUsage> {
        let system = self
            .system
            .lock()
            .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;
        let pid = Pid::from(pid as usize);

        if let Some(process) = system.process(pid) {
            let total_memory = system.total_memory();
            let memory_bytes = process.memory();
            let memory_percent = if total_memory > 0 {
                (memory_bytes as f32 / total_memory as f32) * 100.0
            } else {
                0.0
            };

            Ok(ResourceUsage {
                cpu_percent: process.cpu_usage(),
                memory_bytes,
                memory_percent,
                thread_count: 1, // sysinfo doesn't provide thread count directly
                timestamp: Utc::now(),
            })
        } else {
            Err(anyhow::anyhow!("Process not found: {}", pid))
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_efficiency_stats (lines 534-601) ---[0m
    pub fn get_efficiency_stats(&self) -> ResourceEfficiencyStats {
        let agents_guard = self.agents.read().map_err(|e| {
            tracing::error!("Failed to acquire read lock: {}", e);
            e
        });

        if let Ok(agents) = agents_guard {
            let total_agents = agents.len();
            let suspended_agents = agents.values().filter(|s| s.is_suspended).count();
            let active_agents = total_agents - suspended_agents;

            let (total_cpu, total_memory, total_memory_pct) = agents
                .values()
                .filter(|s| !s.is_suspended)
                .fold((0.0, 0u64, 0.0), |(cpu, mem, mem_pct), state| {
                    (
                        cpu + state.current_usage.cpu_percent,
                        mem + state.current_usage.memory_bytes,
                        mem_pct + state.current_usage.memory_percent,
                    )
                });

            let avg_cpu = if active_agents > 0 {
                total_cpu / active_agents as f32
            } else {
                0.0
            };

            let avg_memory = if active_agents > 0 {
                total_memory / active_agents as u64
            } else {
                0
            };

            let avg_memory_pct = if active_agents > 0 {
                total_memory_pct / active_agents as f32
            } else {
                0.0
            };

            ResourceEfficiencyStats {
                total_agents,
                active_agents,
                suspended_agents,
                average_cpu_usage: avg_cpu,
                average_memory_usage: avg_memory,
                average_memory_percent: avg_memory_pct,
                total_memory_usage: total_memory,
                suspension_rate: if total_agents > 0 {
                    (suspended_agents as f32 / total_agents as f32) * 100.0
                } else {
                    0.0
                },
            }
        } else {
            // Return default stats if lock acquisition failed
            ResourceEfficiencyStats {
                total_agents: 0,
                active_agents: 0,
                suspended_agents: 0,
                average_cpu_usage: 0.0,
                average_memory_usage: 0,
                average_memory_percent: 0.0,
                total_memory_usage: 0,
                suspension_rate: 0.0,
            }
        }
    }

  ./crates/ccswarm/src/resource/mod.rs:149-174 method get_average_usage <-> ./crates/ccswarm/src/resource/mod.rs:534-601 method get_efficiency_stats
  Similarity: 79.37%
  Classes: AgentResourceState <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_average_usage (lines 149-174) ---[0m
    pub fn get_average_usage(&self) -> ResourceUsage {
        if self.usage_history.is_empty() {
            return self.current_usage.clone();
        }

        let count = self.usage_history.len() as f32;
        let (cpu_sum, mem_sum, mem_pct_sum, thread_sum) = self.usage_history.iter().fold(
            (0.0, 0u64, 0.0, 0usize),
            |(cpu, mem, mem_pct, threads), usage| {
                (
                    cpu + usage.cpu_percent,
                    mem + usage.memory_bytes,
                    mem_pct + usage.memory_percent,
                    threads + usage.thread_count,
                )
            },
        );

        ResourceUsage {
            cpu_percent: cpu_sum / count,
            memory_bytes: (mem_sum as f32 / count) as u64,
            memory_percent: mem_pct_sum / count,
            thread_count: (thread_sum as f32 / count) as usize,
            timestamp: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_efficiency_stats (lines 534-601) ---[0m
    pub fn get_efficiency_stats(&self) -> ResourceEfficiencyStats {
        let agents_guard = self.agents.read().map_err(|e| {
            tracing::error!("Failed to acquire read lock: {}", e);
            e
        });

        if let Ok(agents) = agents_guard {
            let total_agents = agents.len();
            let suspended_agents = agents.values().filter(|s| s.is_suspended).count();
            let active_agents = total_agents - suspended_agents;

            let (total_cpu, total_memory, total_memory_pct) = agents
                .values()
                .filter(|s| !s.is_suspended)
                .fold((0.0, 0u64, 0.0), |(cpu, mem, mem_pct), state| {
                    (
                        cpu + state.current_usage.cpu_percent,
                        mem + state.current_usage.memory_bytes,
                        mem_pct + state.current_usage.memory_percent,
                    )
                });

            let avg_cpu = if active_agents > 0 {
                total_cpu / active_agents as f32
            } else {
                0.0
            };

            let avg_memory = if active_agents > 0 {
                total_memory / active_agents as u64
            } else {
                0
            };

            let avg_memory_pct = if active_agents > 0 {
                total_memory_pct / active_agents as f32
            } else {
                0.0
            };

            ResourceEfficiencyStats {
                total_agents,
                active_agents,
                suspended_agents,
                average_cpu_usage: avg_cpu,
                average_memory_usage: avg_memory,
                average_memory_percent: avg_memory_pct,
                total_memory_usage: total_memory,
                suspension_rate: if total_agents > 0 {
                    (suspended_agents as f32 / total_agents as f32) * 100.0
                } else {
                    0.0
                },
            }
        } else {
            // Return default stats if lock acquisition failed
            ResourceEfficiencyStats {
                total_agents: 0,
                active_agents: 0,
                suspended_agents: 0,
                average_cpu_usage: 0.0,
                average_memory_usage: 0,
                average_memory_percent: 0.0,
                total_memory_usage: 0,
                suspension_rate: 0.0,
            }
        }
    }

  ./crates/ccswarm/src/resource/mod.rs:335-362 method update_all_agents <-> ./crates/ccswarm/src/resource/mod.rs:465-510 method find_agent_process_usage
  Similarity: 96.46%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_all_agents (lines 335-362) ---[0m
    async fn update_all_agents(&self) -> Result<()> {
        // Refresh system information
        {
            let mut system = self
                .system
                .lock()
                .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;
            system.refresh_all();
        }

        // Get current agent states
        let agent_ids: Vec<String> = {
            let agents = self
                .agents
                .read()
                .map_err(|e| anyhow::anyhow!("Failed to acquire read lock: {}", e))?;
            agents.keys().cloned().collect()
        };

        // Update each agent
        for agent_id in agent_ids {
            if let Err(e) = self.update_agent_resources(&agent_id).await {
                tracing::warn!("Failed to update resources for agent {}: {}", agent_id, e);
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:find_agent_process_usage (lines 465-510) ---[0m
    fn find_agent_process_usage(&self, _agent_id: &str) -> Result<ResourceUsage> {
        let system = self
            .system
            .lock()
            .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;

        // Look for processes that might be the agent
        // Pattern: ccswarm processes with agent ID in command line
        for process in system.processes().values() {
            // Try to get process name and cmd
            if let Some(name) = process.name().to_str() {
                if name.contains("ccswarm")
                    || process
                        .exe()
                        .map(|exe| exe.to_string_lossy().contains("ccswarm"))
                        .unwrap_or(false)
                {
                    // Check if this might be our agent by looking at the process
                    let total_memory = system.total_memory();
                    let memory_bytes = process.memory();
                    let memory_percent = if total_memory > 0 {
                        (memory_bytes as f32 / total_memory as f32) * 100.0
                    } else {
                        0.0
                    };

                    return Ok(ResourceUsage {
                        cpu_percent: process.cpu_usage(),
                        memory_bytes,
                        memory_percent,
                        thread_count: 1,
                        timestamp: Utc::now(),
                    });
                }
            }
        }

        // If no process found, return minimal usage
        Ok(ResourceUsage {
            cpu_percent: 0.0,
            memory_bytes: 0,
            memory_percent: 0.0,
            thread_count: 0,
            timestamp: Utc::now(),
        })
    }

  ./crates/ccswarm/src/resource/mod.rs:106-129 method update_usage <-> ./crates/ccswarm/src/resource/mod.rs:465-510 method find_agent_process_usage
  Similarity: 91.17%
  Classes: AgentResourceState <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_usage (lines 106-129) ---[0m
    pub fn update_usage(&mut self, usage: ResourceUsage) {
        // Check if agent is active (not idle)
        if usage.cpu_percent > self.limits.idle_cpu_threshold {
            self.last_active = Utc::now();
        }

        // Check for limit violations
        if self.limits.enforce_limits
            && (usage.cpu_percent > self.limits.max_cpu_percent
                || usage.memory_bytes > self.limits.max_memory_bytes
                || usage.memory_percent > self.limits.max_memory_percent)
        {
            self.limit_violations += 1;
        }

        // Update current usage
        self.current_usage = usage.clone();

        // Add to history
        self.usage_history.push(usage);
        if self.usage_history.len() > 100 {
            self.usage_history.remove(0);
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:find_agent_process_usage (lines 465-510) ---[0m
    fn find_agent_process_usage(&self, _agent_id: &str) -> Result<ResourceUsage> {
        let system = self
            .system
            .lock()
            .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;

        // Look for processes that might be the agent
        // Pattern: ccswarm processes with agent ID in command line
        for process in system.processes().values() {
            // Try to get process name and cmd
            if let Some(name) = process.name().to_str() {
                if name.contains("ccswarm")
                    || process
                        .exe()
                        .map(|exe| exe.to_string_lossy().contains("ccswarm"))
                        .unwrap_or(false)
                {
                    // Check if this might be our agent by looking at the process
                    let total_memory = system.total_memory();
                    let memory_bytes = process.memory();
                    let memory_percent = if total_memory > 0 {
                        (memory_bytes as f32 / total_memory as f32) * 100.0
                    } else {
                        0.0
                    };

                    return Ok(ResourceUsage {
                        cpu_percent: process.cpu_usage(),
                        memory_bytes,
                        memory_percent,
                        thread_count: 1,
                        timestamp: Utc::now(),
                    });
                }
            }
        }

        // If no process found, return minimal usage
        Ok(ResourceUsage {
            cpu_percent: 0.0,
            memory_bytes: 0,
            memory_percent: 0.0,
            thread_count: 0,
            timestamp: Utc::now(),
        })
    }

  ./crates/ccswarm/src/resource/mod.rs:436-462 method get_process_usage <-> ./crates/ccswarm/src/resource/mod.rs:465-510 method find_agent_process_usage
  Similarity: 81.14%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_process_usage (lines 436-462) ---[0m
    fn get_process_usage(&self, pid: u32) -> Result<ResourceUsage> {
        let system = self
            .system
            .lock()
            .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;
        let pid = Pid::from(pid as usize);

        if let Some(process) = system.process(pid) {
            let total_memory = system.total_memory();
            let memory_bytes = process.memory();
            let memory_percent = if total_memory > 0 {
                (memory_bytes as f32 / total_memory as f32) * 100.0
            } else {
                0.0
            };

            Ok(ResourceUsage {
                cpu_percent: process.cpu_usage(),
                memory_bytes,
                memory_percent,
                thread_count: 1, // sysinfo doesn't provide thread count directly
                timestamp: Utc::now(),
            })
        } else {
            Err(anyhow::anyhow!("Process not found: {}", pid))
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:find_agent_process_usage (lines 465-510) ---[0m
    fn find_agent_process_usage(&self, _agent_id: &str) -> Result<ResourceUsage> {
        let system = self
            .system
            .lock()
            .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;

        // Look for processes that might be the agent
        // Pattern: ccswarm processes with agent ID in command line
        for process in system.processes().values() {
            // Try to get process name and cmd
            if let Some(name) = process.name().to_str() {
                if name.contains("ccswarm")
                    || process
                        .exe()
                        .map(|exe| exe.to_string_lossy().contains("ccswarm"))
                        .unwrap_or(false)
                {
                    // Check if this might be our agent by looking at the process
                    let total_memory = system.total_memory();
                    let memory_bytes = process.memory();
                    let memory_percent = if total_memory > 0 {
                        (memory_bytes as f32 / total_memory as f32) * 100.0
                    } else {
                        0.0
                    };

                    return Ok(ResourceUsage {
                        cpu_percent: process.cpu_usage(),
                        memory_bytes,
                        memory_percent,
                        thread_count: 1,
                        timestamp: Utc::now(),
                    });
                }
            }
        }

        // If no process found, return minimal usage
        Ok(ResourceUsage {
            cpu_percent: 0.0,
            memory_bytes: 0,
            memory_percent: 0.0,
            thread_count: 0,
            timestamp: Utc::now(),
        })
    }

  ./crates/ccswarm/src/resource/mod.rs:149-174 method get_average_usage <-> ./crates/ccswarm/src/resource/mod.rs:465-510 method find_agent_process_usage
  Similarity: 81.14%
  Classes: AgentResourceState <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_average_usage (lines 149-174) ---[0m
    pub fn get_average_usage(&self) -> ResourceUsage {
        if self.usage_history.is_empty() {
            return self.current_usage.clone();
        }

        let count = self.usage_history.len() as f32;
        let (cpu_sum, mem_sum, mem_pct_sum, thread_sum) = self.usage_history.iter().fold(
            (0.0, 0u64, 0.0, 0usize),
            |(cpu, mem, mem_pct, threads), usage| {
                (
                    cpu + usage.cpu_percent,
                    mem + usage.memory_bytes,
                    mem_pct + usage.memory_percent,
                    threads + usage.thread_count,
                )
            },
        );

        ResourceUsage {
            cpu_percent: cpu_sum / count,
            memory_bytes: (mem_sum as f32 / count) as u64,
            memory_percent: mem_pct_sum / count,
            thread_count: (thread_sum as f32 / count) as usize,
            timestamp: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:find_agent_process_usage (lines 465-510) ---[0m
    fn find_agent_process_usage(&self, _agent_id: &str) -> Result<ResourceUsage> {
        let system = self
            .system
            .lock()
            .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;

        // Look for processes that might be the agent
        // Pattern: ccswarm processes with agent ID in command line
        for process in system.processes().values() {
            // Try to get process name and cmd
            if let Some(name) = process.name().to_str() {
                if name.contains("ccswarm")
                    || process
                        .exe()
                        .map(|exe| exe.to_string_lossy().contains("ccswarm"))
                        .unwrap_or(false)
                {
                    // Check if this might be our agent by looking at the process
                    let total_memory = system.total_memory();
                    let memory_bytes = process.memory();
                    let memory_percent = if total_memory > 0 {
                        (memory_bytes as f32 / total_memory as f32) * 100.0
                    } else {
                        0.0
                    };

                    return Ok(ResourceUsage {
                        cpu_percent: process.cpu_usage(),
                        memory_bytes,
                        memory_percent,
                        thread_count: 1,
                        timestamp: Utc::now(),
                    });
                }
            }
        }

        // If no process found, return minimal usage
        Ok(ResourceUsage {
            cpu_percent: 0.0,
            memory_bytes: 0,
            memory_percent: 0.0,
            thread_count: 0,
            timestamp: Utc::now(),
        })
    }

  ./crates/ccswarm/src/resource/mod.rs:465-510 method find_agent_process_usage <-> ./crates/ccswarm/src/resource/mod.rs:692-713 function test_agent_resource_state
  Similarity: 79.75%

[36m--- ./crates/ccswarm/src/resource/mod.rs:find_agent_process_usage (lines 465-510) ---[0m
    fn find_agent_process_usage(&self, _agent_id: &str) -> Result<ResourceUsage> {
        let system = self
            .system
            .lock()
            .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;

        // Look for processes that might be the agent
        // Pattern: ccswarm processes with agent ID in command line
        for process in system.processes().values() {
            // Try to get process name and cmd
            if let Some(name) = process.name().to_str() {
                if name.contains("ccswarm")
                    || process
                        .exe()
                        .map(|exe| exe.to_string_lossy().contains("ccswarm"))
                        .unwrap_or(false)
                {
                    // Check if this might be our agent by looking at the process
                    let total_memory = system.total_memory();
                    let memory_bytes = process.memory();
                    let memory_percent = if total_memory > 0 {
                        (memory_bytes as f32 / total_memory as f32) * 100.0
                    } else {
                        0.0
                    };

                    return Ok(ResourceUsage {
                        cpu_percent: process.cpu_usage(),
                        memory_bytes,
                        memory_percent,
                        thread_count: 1,
                        timestamp: Utc::now(),
                    });
                }
            }
        }

        // If no process found, return minimal usage
        Ok(ResourceUsage {
            cpu_percent: 0.0,
            memory_bytes: 0,
            memory_percent: 0.0,
            thread_count: 0,
            timestamp: Utc::now(),
        })
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:test_agent_resource_state (lines 692-713) ---[0m
    fn test_agent_resource_state() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), Some(1234), limits);

        // Test initial state
        assert_eq!(state.agent_id, "test-agent");
        assert!(!state.is_suspended);
        assert_eq!(state.limit_violations, 0);

        // Test usage update
        let usage = ResourceUsage {
            cpu_percent: 10.0,
            memory_bytes: 1024 * 1024 * 100, // 100MB
            memory_percent: 5.0,
            thread_count: 4,
            timestamp: Utc::now(),
        };

        state.update_usage(usage.clone());
        assert_eq!(state.current_usage.cpu_percent, 10.0);
        assert_eq!(state.usage_history.len(), 1);
    }

  ./crates/ccswarm/src/resource/mod.rs:465-510 method find_agent_process_usage <-> ./crates/ccswarm/src/resource/mod.rs:513-531 method resume_agent
  Similarity: 79.32%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:find_agent_process_usage (lines 465-510) ---[0m
    fn find_agent_process_usage(&self, _agent_id: &str) -> Result<ResourceUsage> {
        let system = self
            .system
            .lock()
            .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;

        // Look for processes that might be the agent
        // Pattern: ccswarm processes with agent ID in command line
        for process in system.processes().values() {
            // Try to get process name and cmd
            if let Some(name) = process.name().to_str() {
                if name.contains("ccswarm")
                    || process
                        .exe()
                        .map(|exe| exe.to_string_lossy().contains("ccswarm"))
                        .unwrap_or(false)
                {
                    // Check if this might be our agent by looking at the process
                    let total_memory = system.total_memory();
                    let memory_bytes = process.memory();
                    let memory_percent = if total_memory > 0 {
                        (memory_bytes as f32 / total_memory as f32) * 100.0
                    } else {
                        0.0
                    };

                    return Ok(ResourceUsage {
                        cpu_percent: process.cpu_usage(),
                        memory_bytes,
                        memory_percent,
                        thread_count: 1,
                        timestamp: Utc::now(),
                    });
                }
            }
        }

        // If no process found, return minimal usage
        Ok(ResourceUsage {
            cpu_percent: 0.0,
            memory_bytes: 0,
            memory_percent: 0.0,
            thread_count: 0,
            timestamp: Utc::now(),
        })
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:resume_agent (lines 513-531) ---[0m
    pub async fn resume_agent(&self, agent_id: &str) -> Result<()> {
        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        if let Some(state) = agents.get_mut(agent_id) {
            if state.is_suspended {
                state.is_suspended = false;
                state.last_active = Utc::now();

                let _ = self.event_tx.send(ResourceEvent::AgentResumed {
                    agent_id: agent_id.to_string(),
                });
            }
            Ok(())
        } else {
            Err(anyhow::anyhow!("Agent not found: {}", agent_id))
        }
    }

  ./crates/ccswarm/src/resource/mod.rs:106-129 method update_usage <-> ./crates/ccswarm/src/resource/mod.rs:335-362 method update_all_agents
  Similarity: 88.45%
  Classes: AgentResourceState <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_usage (lines 106-129) ---[0m
    pub fn update_usage(&mut self, usage: ResourceUsage) {
        // Check if agent is active (not idle)
        if usage.cpu_percent > self.limits.idle_cpu_threshold {
            self.last_active = Utc::now();
        }

        // Check for limit violations
        if self.limits.enforce_limits
            && (usage.cpu_percent > self.limits.max_cpu_percent
                || usage.memory_bytes > self.limits.max_memory_bytes
                || usage.memory_percent > self.limits.max_memory_percent)
        {
            self.limit_violations += 1;
        }

        // Update current usage
        self.current_usage = usage.clone();

        // Add to history
        self.usage_history.push(usage);
        if self.usage_history.len() > 100 {
            self.usage_history.remove(0);
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_all_agents (lines 335-362) ---[0m
    async fn update_all_agents(&self) -> Result<()> {
        // Refresh system information
        {
            let mut system = self
                .system
                .lock()
                .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;
            system.refresh_all();
        }

        // Get current agent states
        let agent_ids: Vec<String> = {
            let agents = self
                .agents
                .read()
                .map_err(|e| anyhow::anyhow!("Failed to acquire read lock: {}", e))?;
            agents.keys().cloned().collect()
        };

        // Update each agent
        for agent_id in agent_ids {
            if let Err(e) = self.update_agent_resources(&agent_id).await {
                tracing::warn!("Failed to update resources for agent {}: {}", agent_id, e);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/resource/mod.rs:335-362 method update_all_agents <-> ./crates/ccswarm/src/resource/mod.rs:436-462 method get_process_usage
  Similarity: 80.55%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_all_agents (lines 335-362) ---[0m
    async fn update_all_agents(&self) -> Result<()> {
        // Refresh system information
        {
            let mut system = self
                .system
                .lock()
                .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;
            system.refresh_all();
        }

        // Get current agent states
        let agent_ids: Vec<String> = {
            let agents = self
                .agents
                .read()
                .map_err(|e| anyhow::anyhow!("Failed to acquire read lock: {}", e))?;
            agents.keys().cloned().collect()
        };

        // Update each agent
        for agent_id in agent_ids {
            if let Err(e) = self.update_agent_resources(&agent_id).await {
                tracing::warn!("Failed to update resources for agent {}: {}", agent_id, e);
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_process_usage (lines 436-462) ---[0m
    fn get_process_usage(&self, pid: u32) -> Result<ResourceUsage> {
        let system = self
            .system
            .lock()
            .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;
        let pid = Pid::from(pid as usize);

        if let Some(process) = system.process(pid) {
            let total_memory = system.total_memory();
            let memory_bytes = process.memory();
            let memory_percent = if total_memory > 0 {
                (memory_bytes as f32 / total_memory as f32) * 100.0
            } else {
                0.0
            };

            Ok(ResourceUsage {
                cpu_percent: process.cpu_usage(),
                memory_bytes,
                memory_percent,
                thread_count: 1, // sysinfo doesn't provide thread count directly
                timestamp: Utc::now(),
            })
        } else {
            Err(anyhow::anyhow!("Process not found: {}", pid))
        }
    }

  ./crates/ccswarm/src/resource/mod.rs:149-174 method get_average_usage <-> ./crates/ccswarm/src/resource/mod.rs:335-362 method update_all_agents
  Similarity: 80.65%
  Classes: AgentResourceState <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_average_usage (lines 149-174) ---[0m
    pub fn get_average_usage(&self) -> ResourceUsage {
        if self.usage_history.is_empty() {
            return self.current_usage.clone();
        }

        let count = self.usage_history.len() as f32;
        let (cpu_sum, mem_sum, mem_pct_sum, thread_sum) = self.usage_history.iter().fold(
            (0.0, 0u64, 0.0, 0usize),
            |(cpu, mem, mem_pct, threads), usage| {
                (
                    cpu + usage.cpu_percent,
                    mem + usage.memory_bytes,
                    mem_pct + usage.memory_percent,
                    threads + usage.thread_count,
                )
            },
        );

        ResourceUsage {
            cpu_percent: cpu_sum / count,
            memory_bytes: (mem_sum as f32 / count) as u64,
            memory_percent: mem_pct_sum / count,
            thread_count: (thread_sum as f32 / count) as usize,
            timestamp: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_all_agents (lines 335-362) ---[0m
    async fn update_all_agents(&self) -> Result<()> {
        // Refresh system information
        {
            let mut system = self
                .system
                .lock()
                .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;
            system.refresh_all();
        }

        // Get current agent states
        let agent_ids: Vec<String> = {
            let agents = self
                .agents
                .read()
                .map_err(|e| anyhow::anyhow!("Failed to acquire read lock: {}", e))?;
            agents.keys().cloned().collect()
        };

        // Update each agent
        for agent_id in agent_ids {
            if let Err(e) = self.update_agent_resources(&agent_id).await {
                tracing::warn!("Failed to update resources for agent {}: {}", agent_id, e);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/resource/mod.rs:225-245 method start_monitoring <-> ./crates/ccswarm/src/resource/mod.rs:335-362 method update_all_agents
  Similarity: 82.44%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:start_monitoring (lines 225-245) ---[0m
    pub fn start_monitoring(
        &self,
        agent_id: String,
        pid: Option<u32>,
        custom_limits: Option<ResourceLimits>,
    ) -> Result<()> {
        let limits = custom_limits.unwrap_or_else(|| self.global_limits.clone());
        let state = AgentResourceState::new(agent_id.clone(), pid, limits);

        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        agents.insert(agent_id.clone(), state);

        let _ = self
            .event_tx
            .send(ResourceEvent::MonitoringStarted { agent_id, pid });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_all_agents (lines 335-362) ---[0m
    async fn update_all_agents(&self) -> Result<()> {
        // Refresh system information
        {
            let mut system = self
                .system
                .lock()
                .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;
            system.refresh_all();
        }

        // Get current agent states
        let agent_ids: Vec<String> = {
            let agents = self
                .agents
                .read()
                .map_err(|e| anyhow::anyhow!("Failed to acquire read lock: {}", e))?;
            agents.keys().cloned().collect()
        };

        // Update each agent
        for agent_id in agent_ids {
            if let Err(e) = self.update_agent_resources(&agent_id).await {
                tracing::warn!("Failed to update resources for agent {}: {}", agent_id, e);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/resource/mod.rs:436-462 method get_process_usage <-> ./crates/ccswarm/src/resource/mod.rs:513-531 method resume_agent
  Similarity: 81.88%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_process_usage (lines 436-462) ---[0m
    fn get_process_usage(&self, pid: u32) -> Result<ResourceUsage> {
        let system = self
            .system
            .lock()
            .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;
        let pid = Pid::from(pid as usize);

        if let Some(process) = system.process(pid) {
            let total_memory = system.total_memory();
            let memory_bytes = process.memory();
            let memory_percent = if total_memory > 0 {
                (memory_bytes as f32 / total_memory as f32) * 100.0
            } else {
                0.0
            };

            Ok(ResourceUsage {
                cpu_percent: process.cpu_usage(),
                memory_bytes,
                memory_percent,
                thread_count: 1, // sysinfo doesn't provide thread count directly
                timestamp: Utc::now(),
            })
        } else {
            Err(anyhow::anyhow!("Process not found: {}", pid))
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:resume_agent (lines 513-531) ---[0m
    pub async fn resume_agent(&self, agent_id: &str) -> Result<()> {
        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        if let Some(state) = agents.get_mut(agent_id) {
            if state.is_suspended {
                state.is_suspended = false;
                state.last_active = Utc::now();

                let _ = self.event_tx.send(ResourceEvent::AgentResumed {
                    agent_id: agent_id.to_string(),
                });
            }
            Ok(())
        } else {
            Err(anyhow::anyhow!("Agent not found: {}", agent_id))
        }
    }

  ./crates/ccswarm/src/resource/mod.rs:335-362 method update_all_agents <-> ./crates/ccswarm/src/resource/mod.rs:692-713 function test_agent_resource_state
  Similarity: 71.97%

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_all_agents (lines 335-362) ---[0m
    async fn update_all_agents(&self) -> Result<()> {
        // Refresh system information
        {
            let mut system = self
                .system
                .lock()
                .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;
            system.refresh_all();
        }

        // Get current agent states
        let agent_ids: Vec<String> = {
            let agents = self
                .agents
                .read()
                .map_err(|e| anyhow::anyhow!("Failed to acquire read lock: {}", e))?;
            agents.keys().cloned().collect()
        };

        // Update each agent
        for agent_id in agent_ids {
            if let Err(e) = self.update_agent_resources(&agent_id).await {
                tracing::warn!("Failed to update resources for agent {}: {}", agent_id, e);
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:test_agent_resource_state (lines 692-713) ---[0m
    fn test_agent_resource_state() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), Some(1234), limits);

        // Test initial state
        assert_eq!(state.agent_id, "test-agent");
        assert!(!state.is_suspended);
        assert_eq!(state.limit_violations, 0);

        // Test usage update
        let usage = ResourceUsage {
            cpu_percent: 10.0,
            memory_bytes: 1024 * 1024 * 100, // 100MB
            memory_percent: 5.0,
            thread_count: 4,
            timestamp: Utc::now(),
        };

        state.update_usage(usage.clone());
        assert_eq!(state.current_usage.cpu_percent, 10.0);
        assert_eq!(state.usage_history.len(), 1);
    }

  ./crates/ccswarm/src/resource/mod.rs:149-174 method get_average_usage <-> ./crates/ccswarm/src/resource/mod.rs:225-245 method start_monitoring
  Similarity: 74.84%
  Classes: AgentResourceState <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_average_usage (lines 149-174) ---[0m
    pub fn get_average_usage(&self) -> ResourceUsage {
        if self.usage_history.is_empty() {
            return self.current_usage.clone();
        }

        let count = self.usage_history.len() as f32;
        let (cpu_sum, mem_sum, mem_pct_sum, thread_sum) = self.usage_history.iter().fold(
            (0.0, 0u64, 0.0, 0usize),
            |(cpu, mem, mem_pct, threads), usage| {
                (
                    cpu + usage.cpu_percent,
                    mem + usage.memory_bytes,
                    mem_pct + usage.memory_percent,
                    threads + usage.thread_count,
                )
            },
        );

        ResourceUsage {
            cpu_percent: cpu_sum / count,
            memory_bytes: (mem_sum as f32 / count) as u64,
            memory_percent: mem_pct_sum / count,
            thread_count: (thread_sum as f32 / count) as usize,
            timestamp: Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:start_monitoring (lines 225-245) ---[0m
    pub fn start_monitoring(
        &self,
        agent_id: String,
        pid: Option<u32>,
        custom_limits: Option<ResourceLimits>,
    ) -> Result<()> {
        let limits = custom_limits.unwrap_or_else(|| self.global_limits.clone());
        let state = AgentResourceState::new(agent_id.clone(), pid, limits);

        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        agents.insert(agent_id.clone(), state);

        let _ = self
            .event_tx
            .send(ResourceEvent::MonitoringStarted { agent_id, pid });

        Ok(())
    }

  ./crates/ccswarm/src/resource/mod.rs:335-362 method update_all_agents <-> ./crates/ccswarm/src/resource/mod.rs:735-751 function test_resource_efficiency_stats
  Similarity: 77.04%

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_all_agents (lines 335-362) ---[0m
    async fn update_all_agents(&self) -> Result<()> {
        // Refresh system information
        {
            let mut system = self
                .system
                .lock()
                .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;
            system.refresh_all();
        }

        // Get current agent states
        let agent_ids: Vec<String> = {
            let agents = self
                .agents
                .read()
                .map_err(|e| anyhow::anyhow!("Failed to acquire read lock: {}", e))?;
            agents.keys().cloned().collect()
        };

        // Update each agent
        for agent_id in agent_ids {
            if let Err(e) = self.update_agent_resources(&agent_id).await {
                tracing::warn!("Failed to update resources for agent {}: {}", agent_id, e);
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:test_resource_efficiency_stats (lines 735-751) ---[0m
    fn test_resource_efficiency_stats() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);

        // Add some test agents
        monitor
            .start_monitoring("agent1".to_string(), Some(1001), None)
            .unwrap();
        monitor
            .start_monitoring("agent2".to_string(), Some(1002), None)
            .unwrap();

        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 2);
        assert_eq!(stats.suspended_agents, 0);
        assert_eq!(stats.active_agents, 2);
    }

  ./crates/ccswarm/src/resource/mod.rs:335-362 method update_all_agents <-> ./crates/ccswarm/src/resource/mod.rs:716-732 function test_should_suspend
  Similarity: 73.71%

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_all_agents (lines 335-362) ---[0m
    async fn update_all_agents(&self) -> Result<()> {
        // Refresh system information
        {
            let mut system = self
                .system
                .lock()
                .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;
            system.refresh_all();
        }

        // Get current agent states
        let agent_ids: Vec<String> = {
            let agents = self
                .agents
                .read()
                .map_err(|e| anyhow::anyhow!("Failed to acquire read lock: {}", e))?;
            agents.keys().cloned().collect()
        };

        // Update each agent
        for agent_id in agent_ids {
            if let Err(e) = self.update_agent_resources(&agent_id).await {
                tracing::warn!("Failed to update resources for agent {}: {}", agent_id, e);
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:test_should_suspend (lines 716-732) ---[0m
    fn test_should_suspend() {
        let mut limits = ResourceLimits::default();
        limits.idle_timeout = Duration::seconds(10); // Short timeout for testing

        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);

        // Initially should not suspend
        assert!(!state.should_suspend());

        // Set last active to past
        state.last_active = Utc::now() - Duration::seconds(20);
        assert!(state.should_suspend());

        // Suspended agents should not be suspended again
        state.is_suspended = true;
        assert!(!state.should_suspend());
    }

  ./crates/ccswarm/src/resource/mod.rs:106-129 method update_usage <-> ./crates/ccswarm/src/resource/mod.rs:692-713 function test_agent_resource_state
  Similarity: 71.09%

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_usage (lines 106-129) ---[0m
    pub fn update_usage(&mut self, usage: ResourceUsage) {
        // Check if agent is active (not idle)
        if usage.cpu_percent > self.limits.idle_cpu_threshold {
            self.last_active = Utc::now();
        }

        // Check for limit violations
        if self.limits.enforce_limits
            && (usage.cpu_percent > self.limits.max_cpu_percent
                || usage.memory_bytes > self.limits.max_memory_bytes
                || usage.memory_percent > self.limits.max_memory_percent)
        {
            self.limit_violations += 1;
        }

        // Update current usage
        self.current_usage = usage.clone();

        // Add to history
        self.usage_history.push(usage);
        if self.usage_history.len() > 100 {
            self.usage_history.remove(0);
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:test_agent_resource_state (lines 692-713) ---[0m
    fn test_agent_resource_state() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), Some(1234), limits);

        // Test initial state
        assert_eq!(state.agent_id, "test-agent");
        assert!(!state.is_suspended);
        assert_eq!(state.limit_violations, 0);

        // Test usage update
        let usage = ResourceUsage {
            cpu_percent: 10.0,
            memory_bytes: 1024 * 1024 * 100, // 100MB
            memory_percent: 5.0,
            thread_count: 4,
            timestamp: Utc::now(),
        };

        state.update_usage(usage.clone());
        assert_eq!(state.current_usage.cpu_percent, 10.0);
        assert_eq!(state.usage_history.len(), 1);
    }

  ./crates/ccswarm/src/resource/mod.rs:263-274 method update_limits <-> ./crates/ccswarm/src/resource/mod.rs:436-462 method get_process_usage
  Similarity: 83.67%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_limits (lines 263-274) ---[0m
    pub fn update_limits(&self, agent_id: &str, limits: ResourceLimits) -> Result<()> {
        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        if let Some(state) = agents.get_mut(agent_id) {
            state.limits = limits;
            Ok(())
        } else {
            Err(anyhow::anyhow!("Agent not found: {}", agent_id))
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_process_usage (lines 436-462) ---[0m
    fn get_process_usage(&self, pid: u32) -> Result<ResourceUsage> {
        let system = self
            .system
            .lock()
            .map_err(|e| anyhow::anyhow!("Failed to acquire system lock: {}", e))?;
        let pid = Pid::from(pid as usize);

        if let Some(process) = system.process(pid) {
            let total_memory = system.total_memory();
            let memory_bytes = process.memory();
            let memory_percent = if total_memory > 0 {
                (memory_bytes as f32 / total_memory as f32) * 100.0
            } else {
                0.0
            };

            Ok(ResourceUsage {
                cpu_percent: process.cpu_usage(),
                memory_bytes,
                memory_percent,
                thread_count: 1, // sysinfo doesn't provide thread count directly
                timestamp: Utc::now(),
            })
        } else {
            Err(anyhow::anyhow!("Process not found: {}", pid))
        }
    }

  ./crates/ccswarm/src/resource/mod.rs:692-713 function test_agent_resource_state <-> ./crates/ccswarm/src/resource/mod.rs:716-732 function test_should_suspend
  Similarity: 82.08%

[36m--- ./crates/ccswarm/src/resource/mod.rs:test_agent_resource_state (lines 692-713) ---[0m
    fn test_agent_resource_state() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), Some(1234), limits);

        // Test initial state
        assert_eq!(state.agent_id, "test-agent");
        assert!(!state.is_suspended);
        assert_eq!(state.limit_violations, 0);

        // Test usage update
        let usage = ResourceUsage {
            cpu_percent: 10.0,
            memory_bytes: 1024 * 1024 * 100, // 100MB
            memory_percent: 5.0,
            thread_count: 4,
            timestamp: Utc::now(),
        };

        state.update_usage(usage.clone());
        assert_eq!(state.current_usage.cpu_percent, 10.0);
        assert_eq!(state.usage_history.len(), 1);
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:test_should_suspend (lines 716-732) ---[0m
    fn test_should_suspend() {
        let mut limits = ResourceLimits::default();
        limits.idle_timeout = Duration::seconds(10); // Short timeout for testing

        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);

        // Initially should not suspend
        assert!(!state.should_suspend());

        // Set last active to past
        state.last_active = Utc::now() - Duration::seconds(20);
        assert!(state.should_suspend());

        // Suspended agents should not be suspended again
        state.is_suspended = true;
        assert!(!state.should_suspend());
    }

  ./crates/ccswarm/src/resource/mod.rs:106-129 method update_usage <-> ./crates/ccswarm/src/resource/mod.rs:225-245 method start_monitoring
  Similarity: 70.79%
  Classes: AgentResourceState <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_usage (lines 106-129) ---[0m
    pub fn update_usage(&mut self, usage: ResourceUsage) {
        // Check if agent is active (not idle)
        if usage.cpu_percent > self.limits.idle_cpu_threshold {
            self.last_active = Utc::now();
        }

        // Check for limit violations
        if self.limits.enforce_limits
            && (usage.cpu_percent > self.limits.max_cpu_percent
                || usage.memory_bytes > self.limits.max_memory_bytes
                || usage.memory_percent > self.limits.max_memory_percent)
        {
            self.limit_violations += 1;
        }

        // Update current usage
        self.current_usage = usage.clone();

        // Add to history
        self.usage_history.push(usage);
        if self.usage_history.len() > 100 {
            self.usage_history.remove(0);
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:start_monitoring (lines 225-245) ---[0m
    pub fn start_monitoring(
        &self,
        agent_id: String,
        pid: Option<u32>,
        custom_limits: Option<ResourceLimits>,
    ) -> Result<()> {
        let limits = custom_limits.unwrap_or_else(|| self.global_limits.clone());
        let state = AgentResourceState::new(agent_id.clone(), pid, limits);

        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        agents.insert(agent_id.clone(), state);

        let _ = self
            .event_tx
            .send(ResourceEvent::MonitoringStarted { agent_id, pid });

        Ok(())
    }

  ./crates/ccswarm/src/resource/mod.rs:692-713 function test_agent_resource_state <-> ./crates/ccswarm/src/resource/mod.rs:735-751 function test_resource_efficiency_stats
  Similarity: 79.38%

[36m--- ./crates/ccswarm/src/resource/mod.rs:test_agent_resource_state (lines 692-713) ---[0m
    fn test_agent_resource_state() {
        let limits = ResourceLimits::default();
        let mut state = AgentResourceState::new("test-agent".to_string(), Some(1234), limits);

        // Test initial state
        assert_eq!(state.agent_id, "test-agent");
        assert!(!state.is_suspended);
        assert_eq!(state.limit_violations, 0);

        // Test usage update
        let usage = ResourceUsage {
            cpu_percent: 10.0,
            memory_bytes: 1024 * 1024 * 100, // 100MB
            memory_percent: 5.0,
            thread_count: 4,
            timestamp: Utc::now(),
        };

        state.update_usage(usage.clone());
        assert_eq!(state.current_usage.cpu_percent, 10.0);
        assert_eq!(state.usage_history.len(), 1);
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:test_resource_efficiency_stats (lines 735-751) ---[0m
    fn test_resource_efficiency_stats() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);

        // Add some test agents
        monitor
            .start_monitoring("agent1".to_string(), Some(1001), None)
            .unwrap();
        monitor
            .start_monitoring("agent2".to_string(), Some(1002), None)
            .unwrap();

        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 2);
        assert_eq!(stats.suspended_agents, 0);
        assert_eq!(stats.active_agents, 2);
    }

  ./crates/ccswarm/src/resource/mod.rs:106-129 method update_usage <-> ./crates/ccswarm/src/resource/mod.rs:716-732 function test_should_suspend
  Similarity: 74.89%

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_usage (lines 106-129) ---[0m
    pub fn update_usage(&mut self, usage: ResourceUsage) {
        // Check if agent is active (not idle)
        if usage.cpu_percent > self.limits.idle_cpu_threshold {
            self.last_active = Utc::now();
        }

        // Check for limit violations
        if self.limits.enforce_limits
            && (usage.cpu_percent > self.limits.max_cpu_percent
                || usage.memory_bytes > self.limits.max_memory_bytes
                || usage.memory_percent > self.limits.max_memory_percent)
        {
            self.limit_violations += 1;
        }

        // Update current usage
        self.current_usage = usage.clone();

        // Add to history
        self.usage_history.push(usage);
        if self.usage_history.len() > 100 {
            self.usage_history.remove(0);
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:test_should_suspend (lines 716-732) ---[0m
    fn test_should_suspend() {
        let mut limits = ResourceLimits::default();
        limits.idle_timeout = Duration::seconds(10); // Short timeout for testing

        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);

        // Initially should not suspend
        assert!(!state.should_suspend());

        // Set last active to past
        state.last_active = Utc::now() - Duration::seconds(20);
        assert!(state.should_suspend());

        // Suspended agents should not be suspended again
        state.is_suspended = true;
        assert!(!state.should_suspend());
    }

  ./crates/ccswarm/src/resource/mod.rs:106-129 method update_usage <-> ./crates/ccswarm/src/resource/mod.rs:735-751 function test_resource_efficiency_stats
  Similarity: 74.38%

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_usage (lines 106-129) ---[0m
    pub fn update_usage(&mut self, usage: ResourceUsage) {
        // Check if agent is active (not idle)
        if usage.cpu_percent > self.limits.idle_cpu_threshold {
            self.last_active = Utc::now();
        }

        // Check for limit violations
        if self.limits.enforce_limits
            && (usage.cpu_percent > self.limits.max_cpu_percent
                || usage.memory_bytes > self.limits.max_memory_bytes
                || usage.memory_percent > self.limits.max_memory_percent)
        {
            self.limit_violations += 1;
        }

        // Update current usage
        self.current_usage = usage.clone();

        // Add to history
        self.usage_history.push(usage);
        if self.usage_history.len() > 100 {
            self.usage_history.remove(0);
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:test_resource_efficiency_stats (lines 735-751) ---[0m
    fn test_resource_efficiency_stats() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);

        // Add some test agents
        monitor
            .start_monitoring("agent1".to_string(), Some(1001), None)
            .unwrap();
        monitor
            .start_monitoring("agent2".to_string(), Some(1002), None)
            .unwrap();

        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 2);
        assert_eq!(stats.suspended_agents, 0);
        assert_eq!(stats.active_agents, 2);
    }

  ./crates/ccswarm/src/resource/mod.rs:263-274 method update_limits <-> ./crates/ccswarm/src/resource/mod.rs:513-531 method resume_agent
  Similarity: 97.54%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_limits (lines 263-274) ---[0m
    pub fn update_limits(&self, agent_id: &str, limits: ResourceLimits) -> Result<()> {
        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        if let Some(state) = agents.get_mut(agent_id) {
            state.limits = limits;
            Ok(())
        } else {
            Err(anyhow::anyhow!("Agent not found: {}", agent_id))
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:resume_agent (lines 513-531) ---[0m
    pub async fn resume_agent(&self, agent_id: &str) -> Result<()> {
        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        if let Some(state) = agents.get_mut(agent_id) {
            if state.is_suspended {
                state.is_suspended = false;
                state.last_active = Utc::now();

                let _ = self.event_tx.send(ResourceEvent::AgentResumed {
                    agent_id: agent_id.to_string(),
                });
            }
            Ok(())
        } else {
            Err(anyhow::anyhow!("Agent not found: {}", agent_id))
        }
    }

  ./crates/ccswarm/src/resource/mod.rs:629-648 method on_session_created <-> ./crates/ccswarm/src/resource/mod.rs:651-661 method on_session_terminated
  Similarity: 91.79%
  Classes: SessionResourceIntegration <-> SessionResourceIntegration

[36m--- ./crates/ccswarm/src/resource/mod.rs:on_session_created (lines 629-648) ---[0m
    pub async fn on_session_created(
        &self,
        session_id: &str,
        agent_id: &str,
        pid: Option<u32>,
    ) -> Result<()> {
        self.monitor.start_monitoring(
            agent_id.to_string(),
            pid,
            None, // Use global limits
        )?;

        tracing::info!(
            "Started resource monitoring for agent {} in session {}",
            agent_id,
            session_id
        );

        Ok(())
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:on_session_terminated (lines 651-661) ---[0m
    pub async fn on_session_terminated(&self, session_id: &str, agent_id: &str) -> Result<()> {
        self.monitor.stop_monitoring(agent_id)?;

        tracing::info!(
            "Stopped resource monitoring for agent {} in session {}",
            agent_id,
            session_id
        );

        Ok(())
    }

  ./crates/ccswarm/src/resource/mod.rs:86-103 method new <-> ./crates/ccswarm/src/resource/mod.rs:305-315 method get_all_states
  Similarity: 95.38%
  Classes: AgentResourceState <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:new (lines 86-103) ---[0m
    pub fn new(agent_id: String, pid: Option<u32>, limits: ResourceLimits) -> Self {
        Self {
            agent_id,
            current_usage: ResourceUsage {
                cpu_percent: 0.0,
                memory_bytes: 0,
                memory_percent: 0.0,
                thread_count: 0,
                timestamp: Utc::now(),
            },
            usage_history: Vec::with_capacity(100),
            is_suspended: false,
            last_active: Utc::now(),
            pid,
            limit_violations: 0,
            limits,
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_all_states (lines 305-315) ---[0m
    pub fn get_all_states(&self) -> Vec<AgentResourceState> {
        self.agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()
            .map(|agents| agents.values().cloned().collect())
            .unwrap_or_default()
    }

  ./crates/ccswarm/src/resource/mod.rs:277-289 method get_agent_usage <-> ./crates/ccswarm/src/resource/mod.rs:513-531 method resume_agent
  Similarity: 84.11%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_agent_usage (lines 277-289) ---[0m
    pub fn get_agent_usage(&self, agent_id: &str) -> Option<ResourceUsage> {
        let agents = self
            .agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()?;
        agents
            .get(agent_id)
            .map(|state| state.current_usage.clone())
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:resume_agent (lines 513-531) ---[0m
    pub async fn resume_agent(&self, agent_id: &str) -> Result<()> {
        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        if let Some(state) = agents.get_mut(agent_id) {
            if state.is_suspended {
                state.is_suspended = false;
                state.last_active = Utc::now();

                let _ = self.event_tx.send(ResourceEvent::AgentResumed {
                    agent_id: agent_id.to_string(),
                });
            }
            Ok(())
        } else {
            Err(anyhow::anyhow!("Agent not found: {}", agent_id))
        }
    }

  ./crates/ccswarm/src/resource/mod.rs:716-732 function test_should_suspend <-> ./crates/ccswarm/src/resource/mod.rs:735-751 function test_resource_efficiency_stats
  Similarity: 76.99%

[36m--- ./crates/ccswarm/src/resource/mod.rs:test_should_suspend (lines 716-732) ---[0m
    fn test_should_suspend() {
        let mut limits = ResourceLimits::default();
        limits.idle_timeout = Duration::seconds(10); // Short timeout for testing

        let mut state = AgentResourceState::new("test-agent".to_string(), None, limits);

        // Initially should not suspend
        assert!(!state.should_suspend());

        // Set last active to past
        state.last_active = Utc::now() - Duration::seconds(20);
        assert!(state.should_suspend());

        // Suspended agents should not be suspended again
        state.is_suspended = true;
        assert!(!state.should_suspend());
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:test_resource_efficiency_stats (lines 735-751) ---[0m
    fn test_resource_efficiency_stats() {
        let limits = ResourceLimits::default();
        let monitor = ResourceMonitor::new(limits);

        // Add some test agents
        monitor
            .start_monitoring("agent1".to_string(), Some(1001), None)
            .unwrap();
        monitor
            .start_monitoring("agent2".to_string(), Some(1002), None)
            .unwrap();

        let stats = monitor.get_efficiency_stats();
        assert_eq!(stats.total_agents, 2);
        assert_eq!(stats.suspended_agents, 0);
        assert_eq!(stats.active_agents, 2);
    }

  ./crates/ccswarm/src/resource/mod.rs:106-129 method update_usage <-> ./crates/ccswarm/src/resource/mod.rs:248-260 method stop_monitoring
  Similarity: 70.56%
  Classes: AgentResourceState <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_usage (lines 106-129) ---[0m
    pub fn update_usage(&mut self, usage: ResourceUsage) {
        // Check if agent is active (not idle)
        if usage.cpu_percent > self.limits.idle_cpu_threshold {
            self.last_active = Utc::now();
        }

        // Check for limit violations
        if self.limits.enforce_limits
            && (usage.cpu_percent > self.limits.max_cpu_percent
                || usage.memory_bytes > self.limits.max_memory_bytes
                || usage.memory_percent > self.limits.max_memory_percent)
        {
            self.limit_violations += 1;
        }

        // Update current usage
        self.current_usage = usage.clone();

        // Add to history
        self.usage_history.push(usage);
        if self.usage_history.len() > 100 {
            self.usage_history.remove(0);
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:stop_monitoring (lines 248-260) ---[0m
    pub fn stop_monitoring(&self, agent_id: &str) -> Result<()> {
        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        agents.remove(agent_id);

        let _ = self.event_tx.send(ResourceEvent::MonitoringStopped {
            agent_id: agent_id.to_string(),
        });

        Ok(())
    }

  ./crates/ccswarm/src/resource/mod.rs:277-289 method get_agent_usage <-> ./crates/ccswarm/src/resource/mod.rs:629-648 method on_session_created
  Similarity: 78.57%
  Classes: ResourceMonitor <-> SessionResourceIntegration

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_agent_usage (lines 277-289) ---[0m
    pub fn get_agent_usage(&self, agent_id: &str) -> Option<ResourceUsage> {
        let agents = self
            .agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()?;
        agents
            .get(agent_id)
            .map(|state| state.current_usage.clone())
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:on_session_created (lines 629-648) ---[0m
    pub async fn on_session_created(
        &self,
        session_id: &str,
        agent_id: &str,
        pid: Option<u32>,
    ) -> Result<()> {
        self.monitor.start_monitoring(
            agent_id.to_string(),
            pid,
            None, // Use global limits
        )?;

        tracing::info!(
            "Started resource monitoring for agent {} in session {}",
            agent_id,
            session_id
        );

        Ok(())
    }

  ./crates/ccswarm/src/resource/mod.rs:248-260 method stop_monitoring <-> ./crates/ccswarm/src/resource/mod.rs:629-648 method on_session_created
  Similarity: 77.84%
  Classes: ResourceMonitor <-> SessionResourceIntegration

[36m--- ./crates/ccswarm/src/resource/mod.rs:stop_monitoring (lines 248-260) ---[0m
    pub fn stop_monitoring(&self, agent_id: &str) -> Result<()> {
        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        agents.remove(agent_id);

        let _ = self.event_tx.send(ResourceEvent::MonitoringStopped {
            agent_id: agent_id.to_string(),
        });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:on_session_created (lines 629-648) ---[0m
    pub async fn on_session_created(
        &self,
        session_id: &str,
        agent_id: &str,
        pid: Option<u32>,
    ) -> Result<()> {
        self.monitor.start_monitoring(
            agent_id.to_string(),
            pid,
            None, // Use global limits
        )?;

        tracing::info!(
            "Started resource monitoring for agent {} in session {}",
            agent_id,
            session_id
        );

        Ok(())
    }

  ./crates/ccswarm/src/resource/mod.rs:213-222 method new <-> ./crates/ccswarm/src/resource/mod.rs:513-531 method resume_agent
  Similarity: 84.57%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:new (lines 213-222) ---[0m
    pub fn new(global_limits: ResourceLimits) -> Self {
        let (event_tx, _) = broadcast::channel(1024);
        Self {
            system: Arc::new(Mutex::new(System::new_all())),
            agents: Arc::new(RwLock::new(HashMap::new())),
            event_tx,
            global_limits,
            monitor_interval: TokioDuration::from_secs(5),
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:resume_agent (lines 513-531) ---[0m
    pub async fn resume_agent(&self, agent_id: &str) -> Result<()> {
        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        if let Some(state) = agents.get_mut(agent_id) {
            if state.is_suspended {
                state.is_suspended = false;
                state.last_active = Utc::now();

                let _ = self.event_tx.send(ResourceEvent::AgentResumed {
                    agent_id: agent_id.to_string(),
                });
            }
            Ok(())
        } else {
            Err(anyhow::anyhow!("Agent not found: {}", agent_id))
        }
    }

  ./crates/ccswarm/src/resource/mod.rs:213-222 method new <-> ./crates/ccswarm/src/resource/mod.rs:629-648 method on_session_created
  Similarity: 80.80%
  Classes: ResourceMonitor <-> SessionResourceIntegration

[36m--- ./crates/ccswarm/src/resource/mod.rs:new (lines 213-222) ---[0m
    pub fn new(global_limits: ResourceLimits) -> Self {
        let (event_tx, _) = broadcast::channel(1024);
        Self {
            system: Arc::new(Mutex::new(System::new_all())),
            agents: Arc::new(RwLock::new(HashMap::new())),
            event_tx,
            global_limits,
            monitor_interval: TokioDuration::from_secs(5),
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:on_session_created (lines 629-648) ---[0m
    pub async fn on_session_created(
        &self,
        session_id: &str,
        agent_id: &str,
        pid: Option<u32>,
    ) -> Result<()> {
        self.monitor.start_monitoring(
            agent_id.to_string(),
            pid,
            None, // Use global limits
        )?;

        tracing::info!(
            "Started resource monitoring for agent {} in session {}",
            agent_id,
            session_id
        );

        Ok(())
    }

  ./crates/ccswarm/src/resource/mod.rs:50-60 method default <-> ./crates/ccswarm/src/resource/mod.rs:86-103 method new
  Similarity: 83.15%
  Classes: Default <-> AgentResourceState

[36m--- ./crates/ccswarm/src/resource/mod.rs:default (lines 50-60) ---[0m
    fn default() -> Self {
        Self {
            max_cpu_percent: 80.0,
            max_memory_bytes: 2 * 1024 * 1024 * 1024, // 2GB
            max_memory_percent: 50.0,
            idle_timeout: Duration::minutes(15),
            idle_cpu_threshold: 5.0,
            auto_suspend_enabled: true,
            enforce_limits: false,
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:new (lines 86-103) ---[0m
    pub fn new(agent_id: String, pid: Option<u32>, limits: ResourceLimits) -> Self {
        Self {
            agent_id,
            current_usage: ResourceUsage {
                cpu_percent: 0.0,
                memory_bytes: 0,
                memory_percent: 0.0,
                thread_count: 0,
                timestamp: Utc::now(),
            },
            usage_history: Vec::with_capacity(100),
            is_suspended: false,
            last_active: Utc::now(),
            pid,
            limit_violations: 0,
            limits,
        }
    }

  ./crates/ccswarm/src/resource/mod.rs:225-245 method start_monitoring <-> ./crates/ccswarm/src/resource/mod.rs:248-260 method stop_monitoring
  Similarity: 70.83%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:start_monitoring (lines 225-245) ---[0m
    pub fn start_monitoring(
        &self,
        agent_id: String,
        pid: Option<u32>,
        custom_limits: Option<ResourceLimits>,
    ) -> Result<()> {
        let limits = custom_limits.unwrap_or_else(|| self.global_limits.clone());
        let state = AgentResourceState::new(agent_id.clone(), pid, limits);

        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        agents.insert(agent_id.clone(), state);

        let _ = self
            .event_tx
            .send(ResourceEvent::MonitoringStarted { agent_id, pid });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:stop_monitoring (lines 248-260) ---[0m
    pub fn stop_monitoring(&self, agent_id: &str) -> Result<()> {
        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        agents.remove(agent_id);

        let _ = self.event_tx.send(ResourceEvent::MonitoringStopped {
            agent_id: agent_id.to_string(),
        });

        Ok(())
    }

  ./crates/ccswarm/src/resource/mod.rs:263-274 method update_limits <-> ./crates/ccswarm/src/resource/mod.rs:629-648 method on_session_created
  Similarity: 74.55%
  Classes: ResourceMonitor <-> SessionResourceIntegration

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_limits (lines 263-274) ---[0m
    pub fn update_limits(&self, agent_id: &str, limits: ResourceLimits) -> Result<()> {
        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        if let Some(state) = agents.get_mut(agent_id) {
            state.limits = limits;
            Ok(())
        } else {
            Err(anyhow::anyhow!("Agent not found: {}", agent_id))
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:on_session_created (lines 629-648) ---[0m
    pub async fn on_session_created(
        &self,
        session_id: &str,
        agent_id: &str,
        pid: Option<u32>,
    ) -> Result<()> {
        self.monitor.start_monitoring(
            agent_id.to_string(),
            pid,
            None, // Use global limits
        )?;

        tracing::info!(
            "Started resource monitoring for agent {} in session {}",
            agent_id,
            session_id
        );

        Ok(())
    }

  ./crates/ccswarm/src/resource/mod.rs:277-289 method get_agent_usage <-> ./crates/ccswarm/src/resource/mod.rs:292-302 method get_agent_state
  Similarity: 97.47%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_agent_usage (lines 277-289) ---[0m
    pub fn get_agent_usage(&self, agent_id: &str) -> Option<ResourceUsage> {
        let agents = self
            .agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()?;
        agents
            .get(agent_id)
            .map(|state| state.current_usage.clone())
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_agent_state (lines 292-302) ---[0m
    pub fn get_agent_state(&self, agent_id: &str) -> Option<AgentResourceState> {
        let agents = self
            .agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()?;
        agents.get(agent_id).cloned()
    }

  ./crates/ccswarm/src/resource/mod.rs:248-260 method stop_monitoring <-> ./crates/ccswarm/src/resource/mod.rs:513-531 method resume_agent
  Similarity: 71.89%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:stop_monitoring (lines 248-260) ---[0m
    pub fn stop_monitoring(&self, agent_id: &str) -> Result<()> {
        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        agents.remove(agent_id);

        let _ = self.event_tx.send(ResourceEvent::MonitoringStopped {
            agent_id: agent_id.to_string(),
        });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:resume_agent (lines 513-531) ---[0m
    pub async fn resume_agent(&self, agent_id: &str) -> Result<()> {
        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        if let Some(state) = agents.get_mut(agent_id) {
            if state.is_suspended {
                state.is_suspended = false;
                state.last_active = Utc::now();

                let _ = self.event_tx.send(ResourceEvent::AgentResumed {
                    agent_id: agent_id.to_string(),
                });
            }
            Ok(())
        } else {
            Err(anyhow::anyhow!("Agent not found: {}", agent_id))
        }
    }

  ./crates/ccswarm/src/resource/mod.rs:292-302 method get_agent_state <-> ./crates/ccswarm/src/resource/mod.rs:629-648 method on_session_created
  Similarity: 74.00%
  Classes: ResourceMonitor <-> SessionResourceIntegration

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_agent_state (lines 292-302) ---[0m
    pub fn get_agent_state(&self, agent_id: &str) -> Option<AgentResourceState> {
        let agents = self
            .agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()?;
        agents.get(agent_id).cloned()
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:on_session_created (lines 629-648) ---[0m
    pub async fn on_session_created(
        &self,
        session_id: &str,
        agent_id: &str,
        pid: Option<u32>,
    ) -> Result<()> {
        self.monitor.start_monitoring(
            agent_id.to_string(),
            pid,
            None, // Use global limits
        )?;

        tracing::info!(
            "Started resource monitoring for agent {} in session {}",
            agent_id,
            session_id
        );

        Ok(())
    }

  ./crates/ccswarm/src/resource/mod.rs:86-103 method new <-> ./crates/ccswarm/src/resource/mod.rs:292-302 method get_agent_state
  Similarity: 76.85%
  Classes: AgentResourceState <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:new (lines 86-103) ---[0m
    pub fn new(agent_id: String, pid: Option<u32>, limits: ResourceLimits) -> Self {
        Self {
            agent_id,
            current_usage: ResourceUsage {
                cpu_percent: 0.0,
                memory_bytes: 0,
                memory_percent: 0.0,
                thread_count: 0,
                timestamp: Utc::now(),
            },
            usage_history: Vec::with_capacity(100),
            is_suspended: false,
            last_active: Utc::now(),
            pid,
            limit_violations: 0,
            limits,
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_agent_state (lines 292-302) ---[0m
    pub fn get_agent_state(&self, agent_id: &str) -> Option<AgentResourceState> {
        let agents = self
            .agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()?;
        agents.get(agent_id).cloned()
    }

  ./crates/ccswarm/src/resource/mod.rs:50-60 method default <-> ./crates/ccswarm/src/resource/mod.rs:305-315 method get_all_states
  Similarity: 95.38%
  Classes: Default <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:default (lines 50-60) ---[0m
    fn default() -> Self {
        Self {
            max_cpu_percent: 80.0,
            max_memory_bytes: 2 * 1024 * 1024 * 1024, // 2GB
            max_memory_percent: 50.0,
            idle_timeout: Duration::minutes(15),
            idle_cpu_threshold: 5.0,
            auto_suspend_enabled: true,
            enforce_limits: false,
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_all_states (lines 305-315) ---[0m
    pub fn get_all_states(&self) -> Vec<AgentResourceState> {
        self.agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()
            .map(|agents| agents.values().cloned().collect())
            .unwrap_or_default()
    }

  ./crates/ccswarm/src/resource/mod.rs:263-274 method update_limits <-> ./crates/ccswarm/src/resource/mod.rs:292-302 method get_agent_state
  Similarity: 83.93%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_limits (lines 263-274) ---[0m
    pub fn update_limits(&self, agent_id: &str, limits: ResourceLimits) -> Result<()> {
        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        if let Some(state) = agents.get_mut(agent_id) {
            state.limits = limits;
            Ok(())
        } else {
            Err(anyhow::anyhow!("Agent not found: {}", agent_id))
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_agent_state (lines 292-302) ---[0m
    pub fn get_agent_state(&self, agent_id: &str) -> Option<AgentResourceState> {
        let agents = self
            .agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()?;
        agents.get(agent_id).cloned()
    }

  ./crates/ccswarm/src/resource/mod.rs:213-222 method new <-> ./crates/ccswarm/src/resource/mod.rs:292-302 method get_agent_state
  Similarity: 90.70%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:new (lines 213-222) ---[0m
    pub fn new(global_limits: ResourceLimits) -> Self {
        let (event_tx, _) = broadcast::channel(1024);
        Self {
            system: Arc::new(Mutex::new(System::new_all())),
            agents: Arc::new(RwLock::new(HashMap::new())),
            event_tx,
            global_limits,
            monitor_interval: TokioDuration::from_secs(5),
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_agent_state (lines 292-302) ---[0m
    pub fn get_agent_state(&self, agent_id: &str) -> Option<AgentResourceState> {
        let agents = self
            .agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()?;
        agents.get(agent_id).cloned()
    }

  ./crates/ccswarm/src/resource/mod.rs:263-274 method update_limits <-> ./crates/ccswarm/src/resource/mod.rs:277-289 method get_agent_usage
  Similarity: 75.80%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_limits (lines 263-274) ---[0m
    pub fn update_limits(&self, agent_id: &str, limits: ResourceLimits) -> Result<()> {
        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        if let Some(state) = agents.get_mut(agent_id) {
            state.limits = limits;
            Ok(())
        } else {
            Err(anyhow::anyhow!("Agent not found: {}", agent_id))
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_agent_usage (lines 277-289) ---[0m
    pub fn get_agent_usage(&self, agent_id: &str) -> Option<ResourceUsage> {
        let agents = self
            .agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()?;
        agents
            .get(agent_id)
            .map(|state| state.current_usage.clone())
    }

  ./crates/ccswarm/src/resource/mod.rs:86-103 method new <-> ./crates/ccswarm/src/resource/mod.rs:142-146 method is_exceeding_limits
  Similarity: 80.11%
  Classes: AgentResourceState <-> AgentResourceState

[36m--- ./crates/ccswarm/src/resource/mod.rs:new (lines 86-103) ---[0m
    pub fn new(agent_id: String, pid: Option<u32>, limits: ResourceLimits) -> Self {
        Self {
            agent_id,
            current_usage: ResourceUsage {
                cpu_percent: 0.0,
                memory_bytes: 0,
                memory_percent: 0.0,
                thread_count: 0,
                timestamp: Utc::now(),
            },
            usage_history: Vec::with_capacity(100),
            is_suspended: false,
            last_active: Utc::now(),
            pid,
            limit_violations: 0,
            limits,
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:is_exceeding_limits (lines 142-146) ---[0m
    pub fn is_exceeding_limits(&self) -> bool {
        self.current_usage.cpu_percent > self.limits.max_cpu_percent
            || self.current_usage.memory_bytes > self.limits.max_memory_bytes
            || self.current_usage.memory_percent > self.limits.max_memory_percent
    }

  ./crates/ccswarm/src/resource/mod.rs:292-302 method get_agent_state <-> ./crates/ccswarm/src/resource/mod.rs:323-332 method start_monitoring_loop
  Similarity: 86.75%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_agent_state (lines 292-302) ---[0m
    pub fn get_agent_state(&self, agent_id: &str) -> Option<AgentResourceState> {
        let agents = self
            .agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()?;
        agents.get(agent_id).cloned()
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:start_monitoring_loop (lines 323-332) ---[0m
    pub async fn start_monitoring_loop(self: Arc<Self>) {
        let mut interval = interval(self.monitor_interval);

        loop {
            interval.tick().await;
            if let Err(e) = self.update_all_agents().await {
                tracing::error!("Error updating agent resources: {}", e);
            }
        }
    }

  ./crates/ccswarm/src/resource/mod.rs:277-289 method get_agent_usage <-> ./crates/ccswarm/src/resource/mod.rs:323-332 method start_monitoring_loop
  Similarity: 78.90%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_agent_usage (lines 277-289) ---[0m
    pub fn get_agent_usage(&self, agent_id: &str) -> Option<ResourceUsage> {
        let agents = self
            .agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()?;
        agents
            .get(agent_id)
            .map(|state| state.current_usage.clone())
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:start_monitoring_loop (lines 323-332) ---[0m
    pub async fn start_monitoring_loop(self: Arc<Self>) {
        let mut interval = interval(self.monitor_interval);

        loop {
            interval.tick().await;
            if let Err(e) = self.update_all_agents().await {
                tracing::error!("Error updating agent resources: {}", e);
            }
        }
    }

  ./crates/ccswarm/src/resource/mod.rs:213-222 method new <-> ./crates/ccswarm/src/resource/mod.rs:277-289 method get_agent_usage
  Similarity: 74.70%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:new (lines 213-222) ---[0m
    pub fn new(global_limits: ResourceLimits) -> Self {
        let (event_tx, _) = broadcast::channel(1024);
        Self {
            system: Arc::new(Mutex::new(System::new_all())),
            agents: Arc::new(RwLock::new(HashMap::new())),
            event_tx,
            global_limits,
            monitor_interval: TokioDuration::from_secs(5),
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_agent_usage (lines 277-289) ---[0m
    pub fn get_agent_usage(&self, agent_id: &str) -> Option<ResourceUsage> {
        let agents = self
            .agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()?;
        agents
            .get(agent_id)
            .map(|state| state.current_usage.clone())
    }

  ./crates/ccswarm/src/resource/mod.rs:248-260 method stop_monitoring <-> ./crates/ccswarm/src/resource/mod.rs:292-302 method get_agent_state
  Similarity: 71.37%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:stop_monitoring (lines 248-260) ---[0m
    pub fn stop_monitoring(&self, agent_id: &str) -> Result<()> {
        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        agents.remove(agent_id);

        let _ = self.event_tx.send(ResourceEvent::MonitoringStopped {
            agent_id: agent_id.to_string(),
        });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_agent_state (lines 292-302) ---[0m
    pub fn get_agent_state(&self, agent_id: &str) -> Option<AgentResourceState> {
        let agents = self
            .agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()?;
        agents.get(agent_id).cloned()
    }

  ./crates/ccswarm/src/resource/mod.rs:292-302 method get_agent_state <-> ./crates/ccswarm/src/resource/mod.rs:305-315 method get_all_states
  Similarity: 76.45%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_agent_state (lines 292-302) ---[0m
    pub fn get_agent_state(&self, agent_id: &str) -> Option<AgentResourceState> {
        let agents = self
            .agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()?;
        agents.get(agent_id).cloned()
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_all_states (lines 305-315) ---[0m
    pub fn get_all_states(&self) -> Vec<AgentResourceState> {
        self.agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()
            .map(|agents| agents.values().cloned().collect())
            .unwrap_or_default()
    }

  ./crates/ccswarm/src/resource/mod.rs:213-222 method new <-> ./crates/ccswarm/src/resource/mod.rs:263-274 method update_limits
  Similarity: 75.89%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:new (lines 213-222) ---[0m
    pub fn new(global_limits: ResourceLimits) -> Self {
        let (event_tx, _) = broadcast::channel(1024);
        Self {
            system: Arc::new(Mutex::new(System::new_all())),
            agents: Arc::new(RwLock::new(HashMap::new())),
            event_tx,
            global_limits,
            monitor_interval: TokioDuration::from_secs(5),
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:update_limits (lines 263-274) ---[0m
    pub fn update_limits(&self, agent_id: &str, limits: ResourceLimits) -> Result<()> {
        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        if let Some(state) = agents.get_mut(agent_id) {
            state.limits = limits;
            Ok(())
        } else {
            Err(anyhow::anyhow!("Agent not found: {}", agent_id))
        }
    }

  ./crates/ccswarm/src/resource/mod.rs:213-222 method new <-> ./crates/ccswarm/src/resource/mod.rs:248-260 method stop_monitoring
  Similarity: 71.96%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:new (lines 213-222) ---[0m
    pub fn new(global_limits: ResourceLimits) -> Self {
        let (event_tx, _) = broadcast::channel(1024);
        Self {
            system: Arc::new(Mutex::new(System::new_all())),
            agents: Arc::new(RwLock::new(HashMap::new())),
            event_tx,
            global_limits,
            monitor_interval: TokioDuration::from_secs(5),
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:stop_monitoring (lines 248-260) ---[0m
    pub fn stop_monitoring(&self, agent_id: &str) -> Result<()> {
        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        agents.remove(agent_id);

        let _ = self.event_tx.send(ResourceEvent::MonitoringStopped {
            agent_id: agent_id.to_string(),
        });

        Ok(())
    }

  ./crates/ccswarm/src/resource/mod.rs:292-302 method get_agent_state <-> ./crates/ccswarm/src/resource/mod.rs:651-661 method on_session_terminated
  Similarity: 74.00%
  Classes: ResourceMonitor <-> SessionResourceIntegration

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_agent_state (lines 292-302) ---[0m
    pub fn get_agent_state(&self, agent_id: &str) -> Option<AgentResourceState> {
        let agents = self
            .agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()?;
        agents.get(agent_id).cloned()
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:on_session_terminated (lines 651-661) ---[0m
    pub async fn on_session_terminated(&self, session_id: &str, agent_id: &str) -> Result<()> {
        self.monitor.stop_monitoring(agent_id)?;

        tracing::info!(
            "Stopped resource monitoring for agent {} in session {}",
            agent_id,
            session_id
        );

        Ok(())
    }

  ./crates/ccswarm/src/resource/mod.rs:305-315 method get_all_states <-> ./crates/ccswarm/src/resource/mod.rs:323-332 method start_monitoring_loop
  Similarity: 76.77%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_all_states (lines 305-315) ---[0m
    pub fn get_all_states(&self) -> Vec<AgentResourceState> {
        self.agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()
            .map(|agents| agents.values().cloned().collect())
            .unwrap_or_default()
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:start_monitoring_loop (lines 323-332) ---[0m
    pub async fn start_monitoring_loop(self: Arc<Self>) {
        let mut interval = interval(self.monitor_interval);

        loop {
            interval.tick().await;
            if let Err(e) = self.update_all_agents().await {
                tracing::error!("Error updating agent resources: {}", e);
            }
        }
    }

  ./crates/ccswarm/src/resource/mod.rs:213-222 method new <-> ./crates/ccswarm/src/resource/mod.rs:305-315 method get_all_states
  Similarity: 76.70%
  Classes: ResourceMonitor <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:new (lines 213-222) ---[0m
    pub fn new(global_limits: ResourceLimits) -> Self {
        let (event_tx, _) = broadcast::channel(1024);
        Self {
            system: Arc::new(Mutex::new(System::new_all())),
            agents: Arc::new(RwLock::new(HashMap::new())),
            event_tx,
            global_limits,
            monitor_interval: TokioDuration::from_secs(5),
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_all_states (lines 305-315) ---[0m
    pub fn get_all_states(&self) -> Vec<AgentResourceState> {
        self.agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()
            .map(|agents| agents.values().cloned().collect())
            .unwrap_or_default()
    }

  ./crates/ccswarm/src/resource/mod.rs:50-60 method default <-> ./crates/ccswarm/src/resource/mod.rs:292-302 method get_agent_state
  Similarity: 71.60%
  Classes: Default <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:default (lines 50-60) ---[0m
    fn default() -> Self {
        Self {
            max_cpu_percent: 80.0,
            max_memory_bytes: 2 * 1024 * 1024 * 1024, // 2GB
            max_memory_percent: 50.0,
            idle_timeout: Duration::minutes(15),
            idle_cpu_threshold: 5.0,
            auto_suspend_enabled: true,
            enforce_limits: false,
        }
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_agent_state (lines 292-302) ---[0m
    pub fn get_agent_state(&self, agent_id: &str) -> Option<AgentResourceState> {
        let agents = self
            .agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()?;
        agents.get(agent_id).cloned()
    }

  ./crates/ccswarm/src/resource/mod.rs:248-260 method stop_monitoring <-> ./crates/ccswarm/src/resource/mod.rs:683-689 function test_resource_limits_default
  Similarity: 72.06%

[36m--- ./crates/ccswarm/src/resource/mod.rs:stop_monitoring (lines 248-260) ---[0m
    pub fn stop_monitoring(&self, agent_id: &str) -> Result<()> {
        let mut agents = self
            .agents
            .write()
            .map_err(|e| anyhow::anyhow!("Failed to acquire write lock: {}", e))?;
        agents.remove(agent_id);

        let _ = self.event_tx.send(ResourceEvent::MonitoringStopped {
            agent_id: agent_id.to_string(),
        });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:test_resource_limits_default (lines 683-689) ---[0m
    fn test_resource_limits_default() {
        let limits = ResourceLimits::default();
        assert_eq!(limits.max_cpu_percent, 80.0);
        assert_eq!(limits.max_memory_bytes, 2 * 1024 * 1024 * 1024);
        assert_eq!(limits.idle_cpu_threshold, 5.0);
        assert!(limits.auto_suspend_enabled);
    }

  ./crates/ccswarm/src/resource/mod.rs:142-146 method is_exceeding_limits <-> ./crates/ccswarm/src/resource/mod.rs:305-315 method get_all_states
  Similarity: 85.59%
  Classes: AgentResourceState <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:is_exceeding_limits (lines 142-146) ---[0m
    pub fn is_exceeding_limits(&self) -> bool {
        self.current_usage.cpu_percent > self.limits.max_cpu_percent
            || self.current_usage.memory_bytes > self.limits.max_memory_bytes
            || self.current_usage.memory_percent > self.limits.max_memory_percent
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_all_states (lines 305-315) ---[0m
    pub fn get_all_states(&self) -> Vec<AgentResourceState> {
        self.agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()
            .map(|agents| agents.values().cloned().collect())
            .unwrap_or_default()
    }

  ./crates/ccswarm/src/resource/mod.rs:142-146 method is_exceeding_limits <-> ./crates/ccswarm/src/resource/mod.rs:292-302 method get_agent_state
  Similarity: 71.60%
  Classes: AgentResourceState <-> ResourceMonitor

[36m--- ./crates/ccswarm/src/resource/mod.rs:is_exceeding_limits (lines 142-146) ---[0m
    pub fn is_exceeding_limits(&self) -> bool {
        self.current_usage.cpu_percent > self.limits.max_cpu_percent
            || self.current_usage.memory_bytes > self.limits.max_memory_bytes
            || self.current_usage.memory_percent > self.limits.max_memory_percent
    }

[36m--- ./crates/ccswarm/src/resource/mod.rs:get_agent_state (lines 292-302) ---[0m
    pub fn get_agent_state(&self, agent_id: &str) -> Option<AgentResourceState> {
        let agents = self
            .agents
            .read()
            .map_err(|e| {
                tracing::error!("Failed to acquire read lock: {}", e);
                e
            })
            .ok()?;
        agents.get(agent_id).cloned()
    }


Duplicates in ./crates/ccswarm/src/agent/personality.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/agent/personality.rs:15-23 method new <-> ./crates/ccswarm/src/agent/personality.rs:85-95 method new
  Similarity: 82.00%
  Classes: Personality <-> Skill

[36m--- ./crates/ccswarm/src/agent/personality.rs:new (lines 15-23) ---[0m
    pub fn new(style: String) -> Self {
        Self {
            traits: Vec::new(),
            style,
            approach: String::from("collaborative"),
            skills: HashMap::new(),
            working_style: String::from("balanced"),
        }
    }

[36m--- ./crates/ccswarm/src/agent/personality.rs:new (lines 85-95) ---[0m
    pub fn new(name: String, level: SkillLevel) -> Self {
        Self {
            name: name.clone(),
            level,
            experience: 0.0,
            experience_points: 0,
            relevance_score: 0.5,
            language: String::new(),
            framework: String::new(),
        }
    }


Duplicates in ./crates/ccswarm/src/coordination/ai_message_bus.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/coordination/ai_message_bus.rs:106-114 method send_task <-> ./crates/ccswarm/src/coordination/ai_message_bus.rs:117-125 method send_task_result
  Similarity: 93.75%
  Classes: AIMessageBus <-> AIMessageBus

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:send_task (lines 106-114) ---[0m
    pub async fn send_task(&self, agent_id: &AgentId, task: Task) -> Result<()> {
        let message = CCSwarmMessage::Base(CCSwarmAgentMessage::Coordination {
            from_agent: "orchestrator".to_string(),
            to_agent: agent_id.clone(),
            message_type: CoordinationType::TaskDelegation,
            payload: serde_json::to_value(&task)?,
        });
        self.send_to_agent(agent_id, message).await
    }

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:send_task_result (lines 117-125) ---[0m
    pub async fn send_task_result(&self, agent_id: &AgentId, result: TaskResult) -> Result<()> {
        let message = CCSwarmMessage::Base(CCSwarmAgentMessage::Coordination {
            from_agent: agent_id.clone(),
            to_agent: "orchestrator".to_string(),
            message_type: CoordinationType::TaskCompletion,
            payload: serde_json::to_value(&result)?,
        });
        self.broadcast(message).await
    }

  ./crates/ccswarm/src/coordination/ai_message_bus.rs:86-94 method send_to_agent <-> ./crates/ccswarm/src/coordination/ai_message_bus.rs:117-125 method send_task_result
  Similarity: 75.12%
  Classes: AIMessageBus <-> AIMessageBus

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:send_to_agent (lines 86-94) ---[0m
    pub async fn send_to_agent(&self, agent_id: &AgentId, message: CCSwarmMessage) -> Result<()> {
        let channels = self.channels.read().await;
        if let Some(tx) = channels.get(agent_id) {
            tx.send(message)
                .await
                .context("Failed to send message to agent")?;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:send_task_result (lines 117-125) ---[0m
    pub async fn send_task_result(&self, agent_id: &AgentId, result: TaskResult) -> Result<()> {
        let message = CCSwarmMessage::Base(CCSwarmAgentMessage::Coordination {
            from_agent: agent_id.clone(),
            to_agent: "orchestrator".to_string(),
            message_type: CoordinationType::TaskCompletion,
            payload: serde_json::to_value(&result)?,
        });
        self.broadcast(message).await
    }

  ./crates/ccswarm/src/coordination/ai_message_bus.rs:86-94 method send_to_agent <-> ./crates/ccswarm/src/coordination/ai_message_bus.rs:97-103 method broadcast
  Similarity: 82.82%
  Classes: AIMessageBus <-> AIMessageBus

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:send_to_agent (lines 86-94) ---[0m
    pub async fn send_to_agent(&self, agent_id: &AgentId, message: CCSwarmMessage) -> Result<()> {
        let channels = self.channels.read().await;
        if let Some(tx) = channels.get(agent_id) {
            tx.send(message)
                .await
                .context("Failed to send message to agent")?;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:broadcast (lines 97-103) ---[0m
    pub async fn broadcast(&self, message: CCSwarmMessage) -> Result<()> {
        let channels = self.channels.read().await;
        for tx in channels.values() {
            let _ = tx.send(message.clone()).await;
        }
        Ok(())
    }

  ./crates/ccswarm/src/coordination/ai_message_bus.rs:86-94 method send_to_agent <-> ./crates/ccswarm/src/coordination/ai_message_bus.rs:106-114 method send_task
  Similarity: 73.41%
  Classes: AIMessageBus <-> AIMessageBus

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:send_to_agent (lines 86-94) ---[0m
    pub async fn send_to_agent(&self, agent_id: &AgentId, message: CCSwarmMessage) -> Result<()> {
        let channels = self.channels.read().await;
        if let Some(tx) = channels.get(agent_id) {
            tx.send(message)
                .await
                .context("Failed to send message to agent")?;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:send_task (lines 106-114) ---[0m
    pub async fn send_task(&self, agent_id: &AgentId, task: Task) -> Result<()> {
        let message = CCSwarmMessage::Base(CCSwarmAgentMessage::Coordination {
            from_agent: "orchestrator".to_string(),
            to_agent: agent_id.clone(),
            message_type: CoordinationType::TaskDelegation,
            payload: serde_json::to_value(&task)?,
        });
        self.send_to_agent(agent_id, message).await
    }

  ./crates/ccswarm/src/coordination/ai_message_bus.rs:70-75 method new <-> ./crates/ccswarm/src/coordination/ai_message_bus.rs:117-125 method send_task_result
  Similarity: 79.88%
  Classes: AIMessageBus <-> AIMessageBus

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:new (lines 70-75) ---[0m
    pub fn new() -> Self {
        Self {
            channels: Arc::new(RwLock::new(HashMap::new())),
            mapping_registry: Arc::new(AgentMappingRegistry::new()),
        }
    }

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:send_task_result (lines 117-125) ---[0m
    pub async fn send_task_result(&self, agent_id: &AgentId, result: TaskResult) -> Result<()> {
        let message = CCSwarmMessage::Base(CCSwarmAgentMessage::Coordination {
            from_agent: agent_id.clone(),
            to_agent: "orchestrator".to_string(),
            message_type: CoordinationType::TaskCompletion,
            payload: serde_json::to_value(&result)?,
        });
        self.broadcast(message).await
    }

  ./crates/ccswarm/src/coordination/ai_message_bus.rs:78-83 method register_agent <-> ./crates/ccswarm/src/coordination/ai_message_bus.rs:86-94 method send_to_agent
  Similarity: 78.33%
  Classes: AIMessageBus <-> AIMessageBus

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:register_agent (lines 78-83) ---[0m
    pub async fn register_agent(&self, agent_id: AgentId) -> mpsc::Receiver<CCSwarmMessage> {
        let (tx, rx) = mpsc::channel(100);
        let mut channels = self.channels.write().await;
        channels.insert(agent_id, tx);
        rx
    }

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:send_to_agent (lines 86-94) ---[0m
    pub async fn send_to_agent(&self, agent_id: &AgentId, message: CCSwarmMessage) -> Result<()> {
        let channels = self.channels.read().await;
        if let Some(tx) = channels.get(agent_id) {
            tx.send(message)
                .await
                .context("Failed to send message to agent")?;
        }
        Ok(())
    }

  ./crates/ccswarm/src/coordination/ai_message_bus.rs:70-75 method new <-> ./crates/ccswarm/src/coordination/ai_message_bus.rs:106-114 method send_task
  Similarity: 78.07%
  Classes: AIMessageBus <-> AIMessageBus

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:new (lines 70-75) ---[0m
    pub fn new() -> Self {
        Self {
            channels: Arc::new(RwLock::new(HashMap::new())),
            mapping_registry: Arc::new(AgentMappingRegistry::new()),
        }
    }

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:send_task (lines 106-114) ---[0m
    pub async fn send_task(&self, agent_id: &AgentId, task: Task) -> Result<()> {
        let message = CCSwarmMessage::Base(CCSwarmAgentMessage::Coordination {
            from_agent: "orchestrator".to_string(),
            to_agent: agent_id.clone(),
            message_type: CoordinationType::TaskDelegation,
            payload: serde_json::to_value(&task)?,
        });
        self.send_to_agent(agent_id, message).await
    }

  ./crates/ccswarm/src/coordination/ai_message_bus.rs:97-103 method broadcast <-> ./crates/ccswarm/src/coordination/ai_message_bus.rs:117-125 method send_task_result
  Similarity: 73.14%
  Classes: AIMessageBus <-> AIMessageBus

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:broadcast (lines 97-103) ---[0m
    pub async fn broadcast(&self, message: CCSwarmMessage) -> Result<()> {
        let channels = self.channels.read().await;
        for tx in channels.values() {
            let _ = tx.send(message.clone()).await;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:send_task_result (lines 117-125) ---[0m
    pub async fn send_task_result(&self, agent_id: &AgentId, result: TaskResult) -> Result<()> {
        let message = CCSwarmMessage::Base(CCSwarmAgentMessage::Coordination {
            from_agent: agent_id.clone(),
            to_agent: "orchestrator".to_string(),
            message_type: CoordinationType::TaskCompletion,
            payload: serde_json::to_value(&result)?,
        });
        self.broadcast(message).await
    }

  ./crates/ccswarm/src/coordination/ai_message_bus.rs:97-103 method broadcast <-> ./crates/ccswarm/src/coordination/ai_message_bus.rs:106-114 method send_task
  Similarity: 71.48%
  Classes: AIMessageBus <-> AIMessageBus

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:broadcast (lines 97-103) ---[0m
    pub async fn broadcast(&self, message: CCSwarmMessage) -> Result<()> {
        let channels = self.channels.read().await;
        for tx in channels.values() {
            let _ = tx.send(message.clone()).await;
        }
        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:send_task (lines 106-114) ---[0m
    pub async fn send_task(&self, agent_id: &AgentId, task: Task) -> Result<()> {
        let message = CCSwarmMessage::Base(CCSwarmAgentMessage::Coordination {
            from_agent: "orchestrator".to_string(),
            to_agent: agent_id.clone(),
            message_type: CoordinationType::TaskDelegation,
            payload: serde_json::to_value(&task)?,
        });
        self.send_to_agent(agent_id, message).await
    }

  ./crates/ccswarm/src/coordination/ai_message_bus.rs:78-83 method register_agent <-> ./crates/ccswarm/src/coordination/ai_message_bus.rs:106-114 method send_task
  Similarity: 74.77%
  Classes: AIMessageBus <-> AIMessageBus

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:register_agent (lines 78-83) ---[0m
    pub async fn register_agent(&self, agent_id: AgentId) -> mpsc::Receiver<CCSwarmMessage> {
        let (tx, rx) = mpsc::channel(100);
        let mut channels = self.channels.write().await;
        channels.insert(agent_id, tx);
        rx
    }

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:send_task (lines 106-114) ---[0m
    pub async fn send_task(&self, agent_id: &AgentId, task: Task) -> Result<()> {
        let message = CCSwarmMessage::Base(CCSwarmAgentMessage::Coordination {
            from_agent: "orchestrator".to_string(),
            to_agent: agent_id.clone(),
            message_type: CoordinationType::TaskDelegation,
            payload: serde_json::to_value(&task)?,
        });
        self.send_to_agent(agent_id, message).await
    }

  ./crates/ccswarm/src/coordination/ai_message_bus.rs:78-83 method register_agent <-> ./crates/ccswarm/src/coordination/ai_message_bus.rs:117-125 method send_task_result
  Similarity: 74.19%
  Classes: AIMessageBus <-> AIMessageBus

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:register_agent (lines 78-83) ---[0m
    pub async fn register_agent(&self, agent_id: AgentId) -> mpsc::Receiver<CCSwarmMessage> {
        let (tx, rx) = mpsc::channel(100);
        let mut channels = self.channels.write().await;
        channels.insert(agent_id, tx);
        rx
    }

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:send_task_result (lines 117-125) ---[0m
    pub async fn send_task_result(&self, agent_id: &AgentId, result: TaskResult) -> Result<()> {
        let message = CCSwarmMessage::Base(CCSwarmAgentMessage::Coordination {
            from_agent: agent_id.clone(),
            to_agent: "orchestrator".to_string(),
            message_type: CoordinationType::TaskCompletion,
            payload: serde_json::to_value(&result)?,
        });
        self.broadcast(message).await
    }

  ./crates/ccswarm/src/coordination/ai_message_bus.rs:78-83 method register_agent <-> ./crates/ccswarm/src/coordination/ai_message_bus.rs:97-103 method broadcast
  Similarity: 72.21%
  Classes: AIMessageBus <-> AIMessageBus

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:register_agent (lines 78-83) ---[0m
    pub async fn register_agent(&self, agent_id: AgentId) -> mpsc::Receiver<CCSwarmMessage> {
        let (tx, rx) = mpsc::channel(100);
        let mut channels = self.channels.write().await;
        channels.insert(agent_id, tx);
        rx
    }

[36m--- ./crates/ccswarm/src/coordination/ai_message_bus.rs:broadcast (lines 97-103) ---[0m
    pub async fn broadcast(&self, message: CCSwarmMessage) -> Result<()> {
        let channels = self.channels.read().await;
        for tx in channels.values() {
            let _ = tx.send(message.clone()).await;
        }
        Ok(())
    }


Duplicates in ./crates/ccswarm/src/tests/provider_tests.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/tests/provider_tests.rs:241-299 function test_provider_env_vars <-> ./crates/ccswarm/src/tests/provider_tests.rs:302-336 function test_provider_factory
  Similarity: 84.77%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_provider_env_vars (lines 241-299) ---[0m
    fn test_provider_env_vars() {
        // Test Claude Code env vars
        let claude_config = ClaudeCodeConfig {
            api_key: Some("test-claude-key".to_string()),
            ..Default::default()
        };
        let env_vars = claude_config.get_env_vars();
        assert_eq!(
            env_vars.get("ANTHROPIC_API_KEY"),
            Some(&"test-claude-key".to_string())
        );

        // Test Aider env vars
        let aider_config = AiderConfig {
            openai_api_key: Some("test-openai-key".to_string()),
            anthropic_api_key: Some("test-anthropic-key".to_string()),
            ..Default::default()
        };
        let env_vars = aider_config.get_env_vars();
        assert_eq!(
            env_vars.get("OPENAI_API_KEY"),
            Some(&"test-openai-key".to_string())
        );
        assert_eq!(
            env_vars.get("ANTHROPIC_API_KEY"),
            Some(&"test-anthropic-key".to_string())
        );

        // Test Codex env vars
        let codex_config = CodexConfig {
            api_key: "test-codex-key".to_string(),
            organization: Some("test-org".to_string()),
            api_base: Some("https://custom.api.com".to_string()),
            ..Default::default()
        };
        let env_vars = codex_config.get_env_vars();
        assert_eq!(
            env_vars.get("OPENAI_API_KEY"),
            Some(&"test-codex-key".to_string())
        );
        assert_eq!(
            env_vars.get("OPENAI_ORGANIZATION"),
            Some(&"test-org".to_string())
        );
        assert_eq!(
            env_vars.get("OPENAI_API_BASE"),
            Some(&"https://custom.api.com".to_string())
        );

        // Test Custom env vars
        let mut custom_env = HashMap::new();
        custom_env.insert("CUSTOM_VAR".to_string(), "custom_value".to_string());
        let custom_config = CustomConfig {
            env_vars: custom_env.clone(),
            ..Default::default()
        };
        let env_vars = custom_config.get_env_vars();
        assert_eq!(env_vars, custom_env);
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_provider_factory (lines 302-336) ---[0m
    fn test_provider_factory() {
        // Test Claude Code executor creation
        let claude_config = ProviderConfiguration::claude_code(ClaudeCodeConfig::default());
        let executor_result = ProviderFactory::create_executor(&claude_config);
        assert!(executor_result.is_ok());

        // Test Aider executor creation
        let aider_config = ProviderConfiguration::aider(AiderConfig::default());
        let executor_result = ProviderFactory::create_executor(&aider_config);
        assert!(executor_result.is_ok());

        // Test Codex executor creation
        let codex_config = ProviderConfiguration::codex(CodexConfig::default());
        let executor_result = ProviderFactory::create_executor(&codex_config);
        assert!(executor_result.is_ok());

        // Test Custom executor creation
        let custom_config = ProviderConfiguration::custom(CustomConfig::default());
        let executor_result = ProviderFactory::create_executor(&custom_config);
        assert!(executor_result.is_ok());

        // Test missing configuration error
        let incomplete_config = ProviderConfiguration {
            provider_type: AIProvider::ClaudeCode,
            claude_code: None, // Missing configuration
            aider: None,
            codex: None,
            custom: None,
        };
        let executor_result = ProviderFactory::create_executor(&incomplete_config);
        assert!(executor_result.is_err());
        if let Err(e) = executor_result {
            assert!(e.to_string().contains("Claude Code configuration missing"));
        }
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:173-211 function test_codex_config_validation <-> ./crates/ccswarm/src/tests/provider_tests.rs:241-299 function test_provider_env_vars
  Similarity: 80.52%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_codex_config_validation (lines 173-211) ---[0m
    async fn test_codex_config_validation() {
        // Test empty API key
        let empty_key_config = CodexConfig {
            api_key: String::new(),
            ..Default::default()
        };
        let result = empty_key_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("OpenAI API key is required"));

        // Test empty model
        let empty_model_config = CodexConfig {
            api_key: "test-key".to_string(),
            model: String::new(),
            ..Default::default()
        };
        let result = empty_model_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Model name cannot be empty"));

        // Test invalid temperature
        let invalid_temp_config = CodexConfig {
            api_key: "test-key".to_string(),
            temperature: Some(2.0), // Invalid: > 1.0
            ..Default::default()
        };
        let result = invalid_temp_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Temperature must be between 0.0 and 1.0"));
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_provider_env_vars (lines 241-299) ---[0m
    fn test_provider_env_vars() {
        // Test Claude Code env vars
        let claude_config = ClaudeCodeConfig {
            api_key: Some("test-claude-key".to_string()),
            ..Default::default()
        };
        let env_vars = claude_config.get_env_vars();
        assert_eq!(
            env_vars.get("ANTHROPIC_API_KEY"),
            Some(&"test-claude-key".to_string())
        );

        // Test Aider env vars
        let aider_config = AiderConfig {
            openai_api_key: Some("test-openai-key".to_string()),
            anthropic_api_key: Some("test-anthropic-key".to_string()),
            ..Default::default()
        };
        let env_vars = aider_config.get_env_vars();
        assert_eq!(
            env_vars.get("OPENAI_API_KEY"),
            Some(&"test-openai-key".to_string())
        );
        assert_eq!(
            env_vars.get("ANTHROPIC_API_KEY"),
            Some(&"test-anthropic-key".to_string())
        );

        // Test Codex env vars
        let codex_config = CodexConfig {
            api_key: "test-codex-key".to_string(),
            organization: Some("test-org".to_string()),
            api_base: Some("https://custom.api.com".to_string()),
            ..Default::default()
        };
        let env_vars = codex_config.get_env_vars();
        assert_eq!(
            env_vars.get("OPENAI_API_KEY"),
            Some(&"test-codex-key".to_string())
        );
        assert_eq!(
            env_vars.get("OPENAI_ORGANIZATION"),
            Some(&"test-org".to_string())
        );
        assert_eq!(
            env_vars.get("OPENAI_API_BASE"),
            Some(&"https://custom.api.com".to_string())
        );

        // Test Custom env vars
        let mut custom_env = HashMap::new();
        custom_env.insert("CUSTOM_VAR".to_string(), "custom_value".to_string());
        let custom_config = CustomConfig {
            env_vars: custom_env.clone(),
            ..Default::default()
        };
        let env_vars = custom_config.get_env_vars();
        assert_eq!(env_vars, custom_env);
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:241-299 function test_provider_env_vars <-> ./crates/ccswarm/src/tests/provider_tests.rs:342-372 function test_provider_configuration_serialization
  Similarity: 83.91%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_provider_env_vars (lines 241-299) ---[0m
    fn test_provider_env_vars() {
        // Test Claude Code env vars
        let claude_config = ClaudeCodeConfig {
            api_key: Some("test-claude-key".to_string()),
            ..Default::default()
        };
        let env_vars = claude_config.get_env_vars();
        assert_eq!(
            env_vars.get("ANTHROPIC_API_KEY"),
            Some(&"test-claude-key".to_string())
        );

        // Test Aider env vars
        let aider_config = AiderConfig {
            openai_api_key: Some("test-openai-key".to_string()),
            anthropic_api_key: Some("test-anthropic-key".to_string()),
            ..Default::default()
        };
        let env_vars = aider_config.get_env_vars();
        assert_eq!(
            env_vars.get("OPENAI_API_KEY"),
            Some(&"test-openai-key".to_string())
        );
        assert_eq!(
            env_vars.get("ANTHROPIC_API_KEY"),
            Some(&"test-anthropic-key".to_string())
        );

        // Test Codex env vars
        let codex_config = CodexConfig {
            api_key: "test-codex-key".to_string(),
            organization: Some("test-org".to_string()),
            api_base: Some("https://custom.api.com".to_string()),
            ..Default::default()
        };
        let env_vars = codex_config.get_env_vars();
        assert_eq!(
            env_vars.get("OPENAI_API_KEY"),
            Some(&"test-codex-key".to_string())
        );
        assert_eq!(
            env_vars.get("OPENAI_ORGANIZATION"),
            Some(&"test-org".to_string())
        );
        assert_eq!(
            env_vars.get("OPENAI_API_BASE"),
            Some(&"https://custom.api.com".to_string())
        );

        // Test Custom env vars
        let mut custom_env = HashMap::new();
        custom_env.insert("CUSTOM_VAR".to_string(), "custom_value".to_string());
        let custom_config = CustomConfig {
            env_vars: custom_env.clone(),
            ..Default::default()
        };
        let env_vars = custom_config.get_env_vars();
        assert_eq!(env_vars, custom_env);
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_provider_configuration_serialization (lines 342-372) ---[0m
    async fn test_provider_configuration_serialization() {
        let temp_dir = TempDir::new().unwrap();
        let _config_path = temp_dir.path().join("test_config.json");

        // Create configuration with multiple providers
        let claude_config = ProviderConfiguration::claude_code(ClaudeCodeConfig {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: true,
            think_mode: Some("think_hard".to_string()),
            json_output: true,
            custom_commands: vec!["test".to_string()],
            mcp_servers: HashMap::new(),
            api_key: Some("test-key".to_string()),
        });

        // Test serialization
        let serialized = serde_json::to_string_pretty(&claude_config).unwrap();
        assert!(serialized.contains("claude_code"));
        assert!(serialized.contains("claude-3.5-sonnet"));
        assert!(serialized.contains("test-key"));

        // Test deserialization
        let deserialized: ProviderConfiguration = serde_json::from_str(&serialized).unwrap();
        assert_eq!(deserialized.provider_type, AIProvider::ClaudeCode);
        assert!(deserialized.claude_code.is_some());

        let claude_config = deserialized.claude_code.unwrap();
        assert_eq!(claude_config.model, "claude-3.5-sonnet");
        assert!(claude_config.dangerous_skip);
        assert_eq!(claude_config.api_key, Some("test-key".to_string()));
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:137-170 function test_aider_config_validation <-> ./crates/ccswarm/src/tests/provider_tests.rs:241-299 function test_provider_env_vars
  Similarity: 80.52%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_aider_config_validation (lines 137-170) ---[0m
    async fn test_aider_config_validation() {
        let config = AiderConfig::default();

        // This should fail in test environment as aider isn't available
        let result = config.validate().await;
        assert!(result.is_err());
        assert!(result.unwrap_err().to_string().contains("Aider not found"));

        // Test API key validation for GPT models
        let gpt_config = AiderConfig {
            model: "gpt-4".to_string(),
            openai_api_key: None,
            ..Default::default()
        };
        let result = gpt_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("OpenAI API key required"));

        // Test API key validation for Claude models
        let claude_config = AiderConfig {
            model: "claude-3.5-sonnet".to_string(),
            anthropic_api_key: None,
            ..Default::default()
        };
        let result = claude_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Anthropic API key required"));
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_provider_env_vars (lines 241-299) ---[0m
    fn test_provider_env_vars() {
        // Test Claude Code env vars
        let claude_config = ClaudeCodeConfig {
            api_key: Some("test-claude-key".to_string()),
            ..Default::default()
        };
        let env_vars = claude_config.get_env_vars();
        assert_eq!(
            env_vars.get("ANTHROPIC_API_KEY"),
            Some(&"test-claude-key".to_string())
        );

        // Test Aider env vars
        let aider_config = AiderConfig {
            openai_api_key: Some("test-openai-key".to_string()),
            anthropic_api_key: Some("test-anthropic-key".to_string()),
            ..Default::default()
        };
        let env_vars = aider_config.get_env_vars();
        assert_eq!(
            env_vars.get("OPENAI_API_KEY"),
            Some(&"test-openai-key".to_string())
        );
        assert_eq!(
            env_vars.get("ANTHROPIC_API_KEY"),
            Some(&"test-anthropic-key".to_string())
        );

        // Test Codex env vars
        let codex_config = CodexConfig {
            api_key: "test-codex-key".to_string(),
            organization: Some("test-org".to_string()),
            api_base: Some("https://custom.api.com".to_string()),
            ..Default::default()
        };
        let env_vars = codex_config.get_env_vars();
        assert_eq!(
            env_vars.get("OPENAI_API_KEY"),
            Some(&"test-codex-key".to_string())
        );
        assert_eq!(
            env_vars.get("OPENAI_ORGANIZATION"),
            Some(&"test-org".to_string())
        );
        assert_eq!(
            env_vars.get("OPENAI_API_BASE"),
            Some(&"https://custom.api.com".to_string())
        );

        // Test Custom env vars
        let mut custom_env = HashMap::new();
        custom_env.insert("CUSTOM_VAR".to_string(), "custom_value".to_string());
        let custom_config = CustomConfig {
            env_vars: custom_env.clone(),
            ..Default::default()
        };
        let env_vars = custom_config.get_env_vars();
        assert_eq!(env_vars, custom_env);
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:137-170 function test_aider_config_validation <-> ./crates/ccswarm/src/tests/provider_tests.rs:173-211 function test_codex_config_validation
  Similarity: 96.46%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_aider_config_validation (lines 137-170) ---[0m
    async fn test_aider_config_validation() {
        let config = AiderConfig::default();

        // This should fail in test environment as aider isn't available
        let result = config.validate().await;
        assert!(result.is_err());
        assert!(result.unwrap_err().to_string().contains("Aider not found"));

        // Test API key validation for GPT models
        let gpt_config = AiderConfig {
            model: "gpt-4".to_string(),
            openai_api_key: None,
            ..Default::default()
        };
        let result = gpt_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("OpenAI API key required"));

        // Test API key validation for Claude models
        let claude_config = AiderConfig {
            model: "claude-3.5-sonnet".to_string(),
            anthropic_api_key: None,
            ..Default::default()
        };
        let result = claude_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Anthropic API key required"));
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_codex_config_validation (lines 173-211) ---[0m
    async fn test_codex_config_validation() {
        // Test empty API key
        let empty_key_config = CodexConfig {
            api_key: String::new(),
            ..Default::default()
        };
        let result = empty_key_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("OpenAI API key is required"));

        // Test empty model
        let empty_model_config = CodexConfig {
            api_key: "test-key".to_string(),
            model: String::new(),
            ..Default::default()
        };
        let result = empty_model_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Model name cannot be empty"));

        // Test invalid temperature
        let invalid_temp_config = CodexConfig {
            api_key: "test-key".to_string(),
            temperature: Some(2.0), // Invalid: > 1.0
            ..Default::default()
        };
        let result = invalid_temp_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Temperature must be between 0.0 and 1.0"));
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:71-107 function test_provider_configuration_creation <-> ./crates/ccswarm/src/tests/provider_tests.rs:375-417 function test_provider_capabilities
  Similarity: 73.35%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_provider_configuration_creation (lines 71-107) ---[0m
    fn test_provider_configuration_creation() {
        // Test Claude Code configuration
        let claude_config = ClaudeCodeConfig::default();
        let provider_config = ProviderConfiguration::claude_code(claude_config);
        assert_eq!(provider_config.provider_type, AIProvider::ClaudeCode);
        assert!(provider_config.claude_code.is_some());
        assert!(provider_config.aider.is_none());
        assert!(provider_config.codex.is_none());
        assert!(provider_config.custom.is_none());

        // Test Aider configuration
        let aider_config = AiderConfig::default();
        let provider_config = ProviderConfiguration::aider(aider_config);
        assert_eq!(provider_config.provider_type, AIProvider::Aider);
        assert!(provider_config.claude_code.is_none());
        assert!(provider_config.aider.is_some());
        assert!(provider_config.codex.is_none());
        assert!(provider_config.custom.is_none());

        // Test Codex configuration
        let codex_config = CodexConfig::default();
        let provider_config = ProviderConfiguration::codex(codex_config);
        assert_eq!(provider_config.provider_type, AIProvider::Codex);
        assert!(provider_config.claude_code.is_none());
        assert!(provider_config.aider.is_none());
        assert!(provider_config.codex.is_some());
        assert!(provider_config.custom.is_none());

        // Test Custom configuration
        let custom_config = CustomConfig::default();
        let provider_config = ProviderConfiguration::custom(custom_config);
        assert_eq!(provider_config.provider_type, AIProvider::Custom);
        assert!(provider_config.claude_code.is_none());
        assert!(provider_config.aider.is_none());
        assert!(provider_config.codex.is_none());
        assert!(provider_config.custom.is_some());
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_provider_capabilities (lines 375-417) ---[0m
    fn test_provider_capabilities() {
        // Test Claude Code capabilities
        let claude_config = ClaudeCodeConfig::default();
        let executor = crate::providers::claude_code::ClaudeCodeExecutor::new(claude_config);
        let capabilities = executor.get_capabilities();

        assert!(capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert_eq!(capabilities.max_context_length, Some(200_000));
        assert!(capabilities
            .supported_languages
            .contains(&"rust".to_string()));

        // Test Aider capabilities
        let aider_config = AiderConfig::default();
        let executor = crate::providers::aider::AiderExecutor::new(aider_config);
        let capabilities = executor.get_capabilities();

        assert!(!capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(!capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert_eq!(capabilities.max_context_length, Some(128_000));

        // Test Custom capabilities
        let custom_config = CustomConfig {
            supports_json: true,
            ..Default::default()
        };
        let executor = crate::providers::custom::CustomExecutor::new(custom_config);
        let capabilities = executor.get_capabilities();

        assert!(capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert!(capabilities.max_context_length.is_none());
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:173-211 function test_codex_config_validation <-> ./crates/ccswarm/src/tests/provider_tests.rs:342-372 function test_provider_configuration_serialization
  Similarity: 80.49%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_codex_config_validation (lines 173-211) ---[0m
    async fn test_codex_config_validation() {
        // Test empty API key
        let empty_key_config = CodexConfig {
            api_key: String::new(),
            ..Default::default()
        };
        let result = empty_key_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("OpenAI API key is required"));

        // Test empty model
        let empty_model_config = CodexConfig {
            api_key: "test-key".to_string(),
            model: String::new(),
            ..Default::default()
        };
        let result = empty_model_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Model name cannot be empty"));

        // Test invalid temperature
        let invalid_temp_config = CodexConfig {
            api_key: "test-key".to_string(),
            temperature: Some(2.0), // Invalid: > 1.0
            ..Default::default()
        };
        let result = invalid_temp_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Temperature must be between 0.0 and 1.0"));
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_provider_configuration_serialization (lines 342-372) ---[0m
    async fn test_provider_configuration_serialization() {
        let temp_dir = TempDir::new().unwrap();
        let _config_path = temp_dir.path().join("test_config.json");

        // Create configuration with multiple providers
        let claude_config = ProviderConfiguration::claude_code(ClaudeCodeConfig {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: true,
            think_mode: Some("think_hard".to_string()),
            json_output: true,
            custom_commands: vec!["test".to_string()],
            mcp_servers: HashMap::new(),
            api_key: Some("test-key".to_string()),
        });

        // Test serialization
        let serialized = serde_json::to_string_pretty(&claude_config).unwrap();
        assert!(serialized.contains("claude_code"));
        assert!(serialized.contains("claude-3.5-sonnet"));
        assert!(serialized.contains("test-key"));

        // Test deserialization
        let deserialized: ProviderConfiguration = serde_json::from_str(&serialized).unwrap();
        assert_eq!(deserialized.provider_type, AIProvider::ClaudeCode);
        assert!(deserialized.claude_code.is_some());

        let claude_config = deserialized.claude_code.unwrap();
        assert_eq!(claude_config.model, "claude-3.5-sonnet");
        assert!(claude_config.dangerous_skip);
        assert_eq!(claude_config.api_key, Some("test-key".to_string()));
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:173-211 function test_codex_config_validation <-> ./crates/ccswarm/src/tests/provider_tests.rs:302-336 function test_provider_factory
  Similarity: 72.24%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_codex_config_validation (lines 173-211) ---[0m
    async fn test_codex_config_validation() {
        // Test empty API key
        let empty_key_config = CodexConfig {
            api_key: String::new(),
            ..Default::default()
        };
        let result = empty_key_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("OpenAI API key is required"));

        // Test empty model
        let empty_model_config = CodexConfig {
            api_key: "test-key".to_string(),
            model: String::new(),
            ..Default::default()
        };
        let result = empty_model_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Model name cannot be empty"));

        // Test invalid temperature
        let invalid_temp_config = CodexConfig {
            api_key: "test-key".to_string(),
            temperature: Some(2.0), // Invalid: > 1.0
            ..Default::default()
        };
        let result = invalid_temp_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Temperature must be between 0.0 and 1.0"));
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_provider_factory (lines 302-336) ---[0m
    fn test_provider_factory() {
        // Test Claude Code executor creation
        let claude_config = ProviderConfiguration::claude_code(ClaudeCodeConfig::default());
        let executor_result = ProviderFactory::create_executor(&claude_config);
        assert!(executor_result.is_ok());

        // Test Aider executor creation
        let aider_config = ProviderConfiguration::aider(AiderConfig::default());
        let executor_result = ProviderFactory::create_executor(&aider_config);
        assert!(executor_result.is_ok());

        // Test Codex executor creation
        let codex_config = ProviderConfiguration::codex(CodexConfig::default());
        let executor_result = ProviderFactory::create_executor(&codex_config);
        assert!(executor_result.is_ok());

        // Test Custom executor creation
        let custom_config = ProviderConfiguration::custom(CustomConfig::default());
        let executor_result = ProviderFactory::create_executor(&custom_config);
        assert!(executor_result.is_ok());

        // Test missing configuration error
        let incomplete_config = ProviderConfiguration {
            provider_type: AIProvider::ClaudeCode,
            claude_code: None, // Missing configuration
            aider: None,
            codex: None,
            custom: None,
        };
        let executor_result = ProviderFactory::create_executor(&incomplete_config);
        assert!(executor_result.is_err());
        if let Err(e) = executor_result {
            assert!(e.to_string().contains("Claude Code configuration missing"));
        }
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:137-170 function test_aider_config_validation <-> ./crates/ccswarm/src/tests/provider_tests.rs:342-372 function test_provider_configuration_serialization
  Similarity: 81.14%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_aider_config_validation (lines 137-170) ---[0m
    async fn test_aider_config_validation() {
        let config = AiderConfig::default();

        // This should fail in test environment as aider isn't available
        let result = config.validate().await;
        assert!(result.is_err());
        assert!(result.unwrap_err().to_string().contains("Aider not found"));

        // Test API key validation for GPT models
        let gpt_config = AiderConfig {
            model: "gpt-4".to_string(),
            openai_api_key: None,
            ..Default::default()
        };
        let result = gpt_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("OpenAI API key required"));

        // Test API key validation for Claude models
        let claude_config = AiderConfig {
            model: "claude-3.5-sonnet".to_string(),
            anthropic_api_key: None,
            ..Default::default()
        };
        let result = claude_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Anthropic API key required"));
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_provider_configuration_serialization (lines 342-372) ---[0m
    async fn test_provider_configuration_serialization() {
        let temp_dir = TempDir::new().unwrap();
        let _config_path = temp_dir.path().join("test_config.json");

        // Create configuration with multiple providers
        let claude_config = ProviderConfiguration::claude_code(ClaudeCodeConfig {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: true,
            think_mode: Some("think_hard".to_string()),
            json_output: true,
            custom_commands: vec!["test".to_string()],
            mcp_servers: HashMap::new(),
            api_key: Some("test-key".to_string()),
        });

        // Test serialization
        let serialized = serde_json::to_string_pretty(&claude_config).unwrap();
        assert!(serialized.contains("claude_code"));
        assert!(serialized.contains("claude-3.5-sonnet"));
        assert!(serialized.contains("test-key"));

        // Test deserialization
        let deserialized: ProviderConfiguration = serde_json::from_str(&serialized).unwrap();
        assert_eq!(deserialized.provider_type, AIProvider::ClaudeCode);
        assert!(deserialized.claude_code.is_some());

        let claude_config = deserialized.claude_code.unwrap();
        assert_eq!(claude_config.model, "claude-3.5-sonnet");
        assert!(claude_config.dangerous_skip);
        assert_eq!(claude_config.api_key, Some("test-key".to_string()));
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:302-336 function test_provider_factory <-> ./crates/ccswarm/src/tests/provider_tests.rs:342-372 function test_provider_configuration_serialization
  Similarity: 79.27%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_provider_factory (lines 302-336) ---[0m
    fn test_provider_factory() {
        // Test Claude Code executor creation
        let claude_config = ProviderConfiguration::claude_code(ClaudeCodeConfig::default());
        let executor_result = ProviderFactory::create_executor(&claude_config);
        assert!(executor_result.is_ok());

        // Test Aider executor creation
        let aider_config = ProviderConfiguration::aider(AiderConfig::default());
        let executor_result = ProviderFactory::create_executor(&aider_config);
        assert!(executor_result.is_ok());

        // Test Codex executor creation
        let codex_config = ProviderConfiguration::codex(CodexConfig::default());
        let executor_result = ProviderFactory::create_executor(&codex_config);
        assert!(executor_result.is_ok());

        // Test Custom executor creation
        let custom_config = ProviderConfiguration::custom(CustomConfig::default());
        let executor_result = ProviderFactory::create_executor(&custom_config);
        assert!(executor_result.is_ok());

        // Test missing configuration error
        let incomplete_config = ProviderConfiguration {
            provider_type: AIProvider::ClaudeCode,
            claude_code: None, // Missing configuration
            aider: None,
            codex: None,
            custom: None,
        };
        let executor_result = ProviderFactory::create_executor(&incomplete_config);
        assert!(executor_result.is_err());
        if let Err(e) = executor_result {
            assert!(e.to_string().contains("Claude Code configuration missing"));
        }
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_provider_configuration_serialization (lines 342-372) ---[0m
    async fn test_provider_configuration_serialization() {
        let temp_dir = TempDir::new().unwrap();
        let _config_path = temp_dir.path().join("test_config.json");

        // Create configuration with multiple providers
        let claude_config = ProviderConfiguration::claude_code(ClaudeCodeConfig {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: true,
            think_mode: Some("think_hard".to_string()),
            json_output: true,
            custom_commands: vec!["test".to_string()],
            mcp_servers: HashMap::new(),
            api_key: Some("test-key".to_string()),
        });

        // Test serialization
        let serialized = serde_json::to_string_pretty(&claude_config).unwrap();
        assert!(serialized.contains("claude_code"));
        assert!(serialized.contains("claude-3.5-sonnet"));
        assert!(serialized.contains("test-key"));

        // Test deserialization
        let deserialized: ProviderConfiguration = serde_json::from_str(&serialized).unwrap();
        assert_eq!(deserialized.provider_type, AIProvider::ClaudeCode);
        assert!(deserialized.claude_code.is_some());

        let claude_config = deserialized.claude_code.unwrap();
        assert_eq!(claude_config.model, "claude-3.5-sonnet");
        assert!(claude_config.dangerous_skip);
        assert_eq!(claude_config.api_key, Some("test-key".to_string()));
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:137-170 function test_aider_config_validation <-> ./crates/ccswarm/src/tests/provider_tests.rs:302-336 function test_provider_factory
  Similarity: 72.68%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_aider_config_validation (lines 137-170) ---[0m
    async fn test_aider_config_validation() {
        let config = AiderConfig::default();

        // This should fail in test environment as aider isn't available
        let result = config.validate().await;
        assert!(result.is_err());
        assert!(result.unwrap_err().to_string().contains("Aider not found"));

        // Test API key validation for GPT models
        let gpt_config = AiderConfig {
            model: "gpt-4".to_string(),
            openai_api_key: None,
            ..Default::default()
        };
        let result = gpt_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("OpenAI API key required"));

        // Test API key validation for Claude models
        let claude_config = AiderConfig {
            model: "claude-3.5-sonnet".to_string(),
            anthropic_api_key: None,
            ..Default::default()
        };
        let result = claude_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Anthropic API key required"));
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_provider_factory (lines 302-336) ---[0m
    fn test_provider_factory() {
        // Test Claude Code executor creation
        let claude_config = ProviderConfiguration::claude_code(ClaudeCodeConfig::default());
        let executor_result = ProviderFactory::create_executor(&claude_config);
        assert!(executor_result.is_ok());

        // Test Aider executor creation
        let aider_config = ProviderConfiguration::aider(AiderConfig::default());
        let executor_result = ProviderFactory::create_executor(&aider_config);
        assert!(executor_result.is_ok());

        // Test Codex executor creation
        let codex_config = ProviderConfiguration::codex(CodexConfig::default());
        let executor_result = ProviderFactory::create_executor(&codex_config);
        assert!(executor_result.is_ok());

        // Test Custom executor creation
        let custom_config = ProviderConfiguration::custom(CustomConfig::default());
        let executor_result = ProviderFactory::create_executor(&custom_config);
        assert!(executor_result.is_ok());

        // Test missing configuration error
        let incomplete_config = ProviderConfiguration {
            provider_type: AIProvider::ClaudeCode,
            claude_code: None, // Missing configuration
            aider: None,
            codex: None,
            custom: None,
        };
        let executor_result = ProviderFactory::create_executor(&incomplete_config);
        assert!(executor_result.is_err());
        if let Err(e) = executor_result {
            assert!(e.to_string().contains("Claude Code configuration missing"));
        }
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:111-133 function test_claude_code_config_validation <-> ./crates/ccswarm/src/tests/provider_tests.rs:173-211 function test_codex_config_validation
  Similarity: 79.31%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_claude_code_config_validation (lines 111-133) ---[0m
    async fn test_claude_code_config_validation() {
        let config = ClaudeCodeConfig::default();

        // This should fail in test environment as claude CLI isn't available
        let result = config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Claude Code CLI not found"));

        // Test empty model validation
        let invalid_config = ClaudeCodeConfig {
            model: String::new(),
            ..Default::default()
        };
        let result = invalid_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Model name cannot be empty"));
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_codex_config_validation (lines 173-211) ---[0m
    async fn test_codex_config_validation() {
        // Test empty API key
        let empty_key_config = CodexConfig {
            api_key: String::new(),
            ..Default::default()
        };
        let result = empty_key_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("OpenAI API key is required"));

        // Test empty model
        let empty_model_config = CodexConfig {
            api_key: "test-key".to_string(),
            model: String::new(),
            ..Default::default()
        };
        let result = empty_model_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Model name cannot be empty"));

        // Test invalid temperature
        let invalid_temp_config = CodexConfig {
            api_key: "test-key".to_string(),
            temperature: Some(2.0), // Invalid: > 1.0
            ..Default::default()
        };
        let result = invalid_temp_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Temperature must be between 0.0 and 1.0"));
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:173-211 function test_codex_config_validation <-> ./crates/ccswarm/src/tests/provider_tests.rs:214-238 function test_custom_config_validation
  Similarity: 74.46%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_codex_config_validation (lines 173-211) ---[0m
    async fn test_codex_config_validation() {
        // Test empty API key
        let empty_key_config = CodexConfig {
            api_key: String::new(),
            ..Default::default()
        };
        let result = empty_key_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("OpenAI API key is required"));

        // Test empty model
        let empty_model_config = CodexConfig {
            api_key: "test-key".to_string(),
            model: String::new(),
            ..Default::default()
        };
        let result = empty_model_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Model name cannot be empty"));

        // Test invalid temperature
        let invalid_temp_config = CodexConfig {
            api_key: "test-key".to_string(),
            temperature: Some(2.0), // Invalid: > 1.0
            ..Default::default()
        };
        let result = invalid_temp_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Temperature must be between 0.0 and 1.0"));
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_custom_config_validation (lines 214-238) ---[0m
    async fn test_custom_config_validation() {
        // Test empty command
        let empty_command_config = CustomConfig {
            command: String::new(),
            ..Default::default()
        };
        let result = empty_command_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Command cannot be empty"));

        // Test valid echo command (should exist on most systems)
        let echo_config = CustomConfig {
            command: "echo".to_string(),
            args: vec!["test".to_string()],
            ..Default::default()
        };
        let result = echo_config.validate().await;
        // Echo should be available on most systems
        if result.is_err() {
            println!("Echo validation failed: {:?}", result);
        }
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:111-133 function test_claude_code_config_validation <-> ./crates/ccswarm/src/tests/provider_tests.rs:137-170 function test_aider_config_validation
  Similarity: 78.43%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_claude_code_config_validation (lines 111-133) ---[0m
    async fn test_claude_code_config_validation() {
        let config = ClaudeCodeConfig::default();

        // This should fail in test environment as claude CLI isn't available
        let result = config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Claude Code CLI not found"));

        // Test empty model validation
        let invalid_config = ClaudeCodeConfig {
            model: String::new(),
            ..Default::default()
        };
        let result = invalid_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Model name cannot be empty"));
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_aider_config_validation (lines 137-170) ---[0m
    async fn test_aider_config_validation() {
        let config = AiderConfig::default();

        // This should fail in test environment as aider isn't available
        let result = config.validate().await;
        assert!(result.is_err());
        assert!(result.unwrap_err().to_string().contains("Aider not found"));

        // Test API key validation for GPT models
        let gpt_config = AiderConfig {
            model: "gpt-4".to_string(),
            openai_api_key: None,
            ..Default::default()
        };
        let result = gpt_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("OpenAI API key required"));

        // Test API key validation for Claude models
        let claude_config = AiderConfig {
            model: "claude-3.5-sonnet".to_string(),
            anthropic_api_key: None,
            ..Default::default()
        };
        let result = claude_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Anthropic API key required"));
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:137-170 function test_aider_config_validation <-> ./crates/ccswarm/src/tests/provider_tests.rs:214-238 function test_custom_config_validation
  Similarity: 72.92%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_aider_config_validation (lines 137-170) ---[0m
    async fn test_aider_config_validation() {
        let config = AiderConfig::default();

        // This should fail in test environment as aider isn't available
        let result = config.validate().await;
        assert!(result.is_err());
        assert!(result.unwrap_err().to_string().contains("Aider not found"));

        // Test API key validation for GPT models
        let gpt_config = AiderConfig {
            model: "gpt-4".to_string(),
            openai_api_key: None,
            ..Default::default()
        };
        let result = gpt_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("OpenAI API key required"));

        // Test API key validation for Claude models
        let claude_config = AiderConfig {
            model: "claude-3.5-sonnet".to_string(),
            anthropic_api_key: None,
            ..Default::default()
        };
        let result = claude_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Anthropic API key required"));
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_custom_config_validation (lines 214-238) ---[0m
    async fn test_custom_config_validation() {
        // Test empty command
        let empty_command_config = CustomConfig {
            command: String::new(),
            ..Default::default()
        };
        let result = empty_command_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Command cannot be empty"));

        // Test valid echo command (should exist on most systems)
        let echo_config = CustomConfig {
            command: "echo".to_string(),
            args: vec!["test".to_string()],
            ..Default::default()
        };
        let result = echo_config.validate().await;
        // Echo should be available on most systems
        if result.is_err() {
            println!("Echo validation failed: {:?}", result);
        }
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:8-23 function test_ai_provider_enum <-> ./crates/ccswarm/src/tests/provider_tests.rs:173-211 function test_codex_config_validation
  Similarity: 75.51%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_ai_provider_enum (lines 8-23) ---[0m
    fn test_ai_provider_enum() {
        assert_eq!(AIProvider::ClaudeCode.display_name(), "Claude Code");
        assert_eq!(AIProvider::Aider.display_name(), "Aider");
        assert_eq!(AIProvider::Codex.display_name(), "OpenAI Codex");
        assert_eq!(AIProvider::Custom.display_name(), "Custom");

        assert_eq!(AIProvider::ClaudeCode.icon(), "🤖");
        assert_eq!(AIProvider::Aider.icon(), "🔧");
        assert_eq!(AIProvider::Codex.icon(), "🧠");
        assert_eq!(AIProvider::Custom.icon(), "⚙️");

        assert_eq!(AIProvider::ClaudeCode.color(), "blue");
        assert_eq!(AIProvider::Aider.color(), "green");
        assert_eq!(AIProvider::Codex.color(), "purple");
        assert_eq!(AIProvider::Custom.color(), "gray");
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_codex_config_validation (lines 173-211) ---[0m
    async fn test_codex_config_validation() {
        // Test empty API key
        let empty_key_config = CodexConfig {
            api_key: String::new(),
            ..Default::default()
        };
        let result = empty_key_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("OpenAI API key is required"));

        // Test empty model
        let empty_model_config = CodexConfig {
            api_key: "test-key".to_string(),
            model: String::new(),
            ..Default::default()
        };
        let result = empty_model_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Model name cannot be empty"));

        // Test invalid temperature
        let invalid_temp_config = CodexConfig {
            api_key: "test-key".to_string(),
            temperature: Some(2.0), // Invalid: > 1.0
            ..Default::default()
        };
        let result = invalid_temp_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Temperature must be between 0.0 and 1.0"));
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:111-133 function test_claude_code_config_validation <-> ./crates/ccswarm/src/tests/provider_tests.rs:214-238 function test_custom_config_validation
  Similarity: 86.31%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_claude_code_config_validation (lines 111-133) ---[0m
    async fn test_claude_code_config_validation() {
        let config = ClaudeCodeConfig::default();

        // This should fail in test environment as claude CLI isn't available
        let result = config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Claude Code CLI not found"));

        // Test empty model validation
        let invalid_config = ClaudeCodeConfig {
            model: String::new(),
            ..Default::default()
        };
        let result = invalid_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Model name cannot be empty"));
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_custom_config_validation (lines 214-238) ---[0m
    async fn test_custom_config_validation() {
        // Test empty command
        let empty_command_config = CustomConfig {
            command: String::new(),
            ..Default::default()
        };
        let result = empty_command_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Command cannot be empty"));

        // Test valid echo command (should exist on most systems)
        let echo_config = CustomConfig {
            command: "echo".to_string(),
            args: vec!["test".to_string()],
            ..Default::default()
        };
        let result = echo_config.validate().await;
        // Echo should be available on most systems
        if result.is_err() {
            println!("Echo validation failed: {:?}", result);
        }
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:8-23 function test_ai_provider_enum <-> ./crates/ccswarm/src/tests/provider_tests.rs:137-170 function test_aider_config_validation
  Similarity: 72.74%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_ai_provider_enum (lines 8-23) ---[0m
    fn test_ai_provider_enum() {
        assert_eq!(AIProvider::ClaudeCode.display_name(), "Claude Code");
        assert_eq!(AIProvider::Aider.display_name(), "Aider");
        assert_eq!(AIProvider::Codex.display_name(), "OpenAI Codex");
        assert_eq!(AIProvider::Custom.display_name(), "Custom");

        assert_eq!(AIProvider::ClaudeCode.icon(), "🤖");
        assert_eq!(AIProvider::Aider.icon(), "🔧");
        assert_eq!(AIProvider::Codex.icon(), "🧠");
        assert_eq!(AIProvider::Custom.icon(), "⚙️");

        assert_eq!(AIProvider::ClaudeCode.color(), "blue");
        assert_eq!(AIProvider::Aider.color(), "green");
        assert_eq!(AIProvider::Codex.color(), "purple");
        assert_eq!(AIProvider::Custom.color(), "gray");
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_aider_config_validation (lines 137-170) ---[0m
    async fn test_aider_config_validation() {
        let config = AiderConfig::default();

        // This should fail in test environment as aider isn't available
        let result = config.validate().await;
        assert!(result.is_err());
        assert!(result.unwrap_err().to_string().contains("Aider not found"));

        // Test API key validation for GPT models
        let gpt_config = AiderConfig {
            model: "gpt-4".to_string(),
            openai_api_key: None,
            ..Default::default()
        };
        let result = gpt_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("OpenAI API key required"));

        // Test API key validation for Claude models
        let claude_config = AiderConfig {
            model: "claude-3.5-sonnet".to_string(),
            anthropic_api_key: None,
            ..Default::default()
        };
        let result = claude_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Anthropic API key required"));
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:8-23 function test_ai_provider_enum <-> ./crates/ccswarm/src/tests/provider_tests.rs:342-372 function test_provider_configuration_serialization
  Similarity: 71.85%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_ai_provider_enum (lines 8-23) ---[0m
    fn test_ai_provider_enum() {
        assert_eq!(AIProvider::ClaudeCode.display_name(), "Claude Code");
        assert_eq!(AIProvider::Aider.display_name(), "Aider");
        assert_eq!(AIProvider::Codex.display_name(), "OpenAI Codex");
        assert_eq!(AIProvider::Custom.display_name(), "Custom");

        assert_eq!(AIProvider::ClaudeCode.icon(), "🤖");
        assert_eq!(AIProvider::Aider.icon(), "🔧");
        assert_eq!(AIProvider::Codex.icon(), "🧠");
        assert_eq!(AIProvider::Custom.icon(), "⚙️");

        assert_eq!(AIProvider::ClaudeCode.color(), "blue");
        assert_eq!(AIProvider::Aider.color(), "green");
        assert_eq!(AIProvider::Codex.color(), "purple");
        assert_eq!(AIProvider::Custom.color(), "gray");
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_provider_configuration_serialization (lines 342-372) ---[0m
    async fn test_provider_configuration_serialization() {
        let temp_dir = TempDir::new().unwrap();
        let _config_path = temp_dir.path().join("test_config.json");

        // Create configuration with multiple providers
        let claude_config = ProviderConfiguration::claude_code(ClaudeCodeConfig {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: true,
            think_mode: Some("think_hard".to_string()),
            json_output: true,
            custom_commands: vec!["test".to_string()],
            mcp_servers: HashMap::new(),
            api_key: Some("test-key".to_string()),
        });

        // Test serialization
        let serialized = serde_json::to_string_pretty(&claude_config).unwrap();
        assert!(serialized.contains("claude_code"));
        assert!(serialized.contains("claude-3.5-sonnet"));
        assert!(serialized.contains("test-key"));

        // Test deserialization
        let deserialized: ProviderConfiguration = serde_json::from_str(&serialized).unwrap();
        assert_eq!(deserialized.provider_type, AIProvider::ClaudeCode);
        assert!(deserialized.claude_code.is_some());

        let claude_config = deserialized.claude_code.unwrap();
        assert_eq!(claude_config.model, "claude-3.5-sonnet");
        assert!(claude_config.dangerous_skip);
        assert_eq!(claude_config.api_key, Some("test-key".to_string()));
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:38-47 function test_aider_config_default <-> ./crates/ccswarm/src/tests/provider_tests.rs:214-238 function test_custom_config_validation
  Similarity: 76.67%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_aider_config_default (lines 38-47) ---[0m
    fn test_aider_config_default() {
        let config = AiderConfig::default();
        assert_eq!(config.model, "gpt-4");
        assert!(config.openai_api_key.is_none());
        assert!(config.anthropic_api_key.is_none());
        assert!(config.auto_commit);
        assert!(config.git);
        assert!(config.additional_args.is_empty());
        assert!(config.executable_path.is_none());
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_custom_config_validation (lines 214-238) ---[0m
    async fn test_custom_config_validation() {
        // Test empty command
        let empty_command_config = CustomConfig {
            command: String::new(),
            ..Default::default()
        };
        let result = empty_command_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Command cannot be empty"));

        // Test valid echo command (should exist on most systems)
        let echo_config = CustomConfig {
            command: "echo".to_string(),
            args: vec!["test".to_string()],
            ..Default::default()
        };
        let result = echo_config.validate().await;
        // Echo should be available on most systems
        if result.is_err() {
            println!("Echo validation failed: {:?}", result);
        }
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:26-35 function test_claude_code_config_default <-> ./crates/ccswarm/src/tests/provider_tests.rs:214-238 function test_custom_config_validation
  Similarity: 75.85%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_claude_code_config_default (lines 26-35) ---[0m
    fn test_claude_code_config_default() {
        let config = ClaudeCodeConfig::default();
        assert_eq!(config.model, "claude-3.5-sonnet");
        assert!(!config.dangerous_skip);
        assert_eq!(config.think_mode, Some("think".to_string()));
        assert!(config.json_output);
        assert!(config.custom_commands.is_empty());
        assert!(config.mcp_servers.is_empty());
        assert!(config.api_key.is_none());
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_custom_config_validation (lines 214-238) ---[0m
    async fn test_custom_config_validation() {
        // Test empty command
        let empty_command_config = CustomConfig {
            command: String::new(),
            ..Default::default()
        };
        let result = empty_command_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Command cannot be empty"));

        // Test valid echo command (should exist on most systems)
        let echo_config = CustomConfig {
            command: "echo".to_string(),
            args: vec!["test".to_string()],
            ..Default::default()
        };
        let result = echo_config.validate().await;
        // Echo should be available on most systems
        if result.is_err() {
            println!("Echo validation failed: {:?}", result);
        }
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:60-68 function test_custom_config_default <-> ./crates/ccswarm/src/tests/provider_tests.rs:214-238 function test_custom_config_validation
  Similarity: 76.31%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_custom_config_default (lines 60-68) ---[0m
    fn test_custom_config_default() {
        let config = CustomConfig::default();
        assert_eq!(config.command, "echo");
        assert_eq!(config.args, vec!["{prompt}"]);
        assert!(config.env_vars.is_empty());
        assert!(config.working_directory.is_none());
        assert_eq!(config.timeout_seconds, Some(300));
        assert!(!config.supports_json);
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_custom_config_validation (lines 214-238) ---[0m
    async fn test_custom_config_validation() {
        // Test empty command
        let empty_command_config = CustomConfig {
            command: String::new(),
            ..Default::default()
        };
        let result = empty_command_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Command cannot be empty"));

        // Test valid echo command (should exist on most systems)
        let echo_config = CustomConfig {
            command: "echo".to_string(),
            args: vec!["test".to_string()],
            ..Default::default()
        };
        let result = echo_config.validate().await;
        // Echo should be available on most systems
        if result.is_err() {
            println!("Echo validation failed: {:?}", result);
        }
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:38-47 function test_aider_config_default <-> ./crates/ccswarm/src/tests/provider_tests.rs:111-133 function test_claude_code_config_validation
  Similarity: 76.75%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_aider_config_default (lines 38-47) ---[0m
    fn test_aider_config_default() {
        let config = AiderConfig::default();
        assert_eq!(config.model, "gpt-4");
        assert!(config.openai_api_key.is_none());
        assert!(config.anthropic_api_key.is_none());
        assert!(config.auto_commit);
        assert!(config.git);
        assert!(config.additional_args.is_empty());
        assert!(config.executable_path.is_none());
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_claude_code_config_validation (lines 111-133) ---[0m
    async fn test_claude_code_config_validation() {
        let config = ClaudeCodeConfig::default();

        // This should fail in test environment as claude CLI isn't available
        let result = config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Claude Code CLI not found"));

        // Test empty model validation
        let invalid_config = ClaudeCodeConfig {
            model: String::new(),
            ..Default::default()
        };
        let result = invalid_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Model name cannot be empty"));
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:26-35 function test_claude_code_config_default <-> ./crates/ccswarm/src/tests/provider_tests.rs:111-133 function test_claude_code_config_validation
  Similarity: 75.96%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_claude_code_config_default (lines 26-35) ---[0m
    fn test_claude_code_config_default() {
        let config = ClaudeCodeConfig::default();
        assert_eq!(config.model, "claude-3.5-sonnet");
        assert!(!config.dangerous_skip);
        assert_eq!(config.think_mode, Some("think".to_string()));
        assert!(config.json_output);
        assert!(config.custom_commands.is_empty());
        assert!(config.mcp_servers.is_empty());
        assert!(config.api_key.is_none());
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_claude_code_config_validation (lines 111-133) ---[0m
    async fn test_claude_code_config_validation() {
        let config = ClaudeCodeConfig::default();

        // This should fail in test environment as claude CLI isn't available
        let result = config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Claude Code CLI not found"));

        // Test empty model validation
        let invalid_config = ClaudeCodeConfig {
            model: String::new(),
            ..Default::default()
        };
        let result = invalid_config.validate().await;
        assert!(result.is_err());
        assert!(result
            .unwrap_err()
            .to_string()
            .contains("Model name cannot be empty"));
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:26-35 function test_claude_code_config_default <-> ./crates/ccswarm/src/tests/provider_tests.rs:38-47 function test_aider_config_default
  Similarity: 89.44%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_claude_code_config_default (lines 26-35) ---[0m
    fn test_claude_code_config_default() {
        let config = ClaudeCodeConfig::default();
        assert_eq!(config.model, "claude-3.5-sonnet");
        assert!(!config.dangerous_skip);
        assert_eq!(config.think_mode, Some("think".to_string()));
        assert!(config.json_output);
        assert!(config.custom_commands.is_empty());
        assert!(config.mcp_servers.is_empty());
        assert!(config.api_key.is_none());
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_aider_config_default (lines 38-47) ---[0m
    fn test_aider_config_default() {
        let config = AiderConfig::default();
        assert_eq!(config.model, "gpt-4");
        assert!(config.openai_api_key.is_none());
        assert!(config.anthropic_api_key.is_none());
        assert!(config.auto_commit);
        assert!(config.git);
        assert!(config.additional_args.is_empty());
        assert!(config.executable_path.is_none());
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:26-35 function test_claude_code_config_default <-> ./crates/ccswarm/src/tests/provider_tests.rs:60-68 function test_custom_config_default
  Similarity: 78.65%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_claude_code_config_default (lines 26-35) ---[0m
    fn test_claude_code_config_default() {
        let config = ClaudeCodeConfig::default();
        assert_eq!(config.model, "claude-3.5-sonnet");
        assert!(!config.dangerous_skip);
        assert_eq!(config.think_mode, Some("think".to_string()));
        assert!(config.json_output);
        assert!(config.custom_commands.is_empty());
        assert!(config.mcp_servers.is_empty());
        assert!(config.api_key.is_none());
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_custom_config_default (lines 60-68) ---[0m
    fn test_custom_config_default() {
        let config = CustomConfig::default();
        assert_eq!(config.command, "echo");
        assert_eq!(config.args, vec!["{prompt}"]);
        assert!(config.env_vars.is_empty());
        assert!(config.working_directory.is_none());
        assert_eq!(config.timeout_seconds, Some(300));
        assert!(!config.supports_json);
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:38-47 function test_aider_config_default <-> ./crates/ccswarm/src/tests/provider_tests.rs:60-68 function test_custom_config_default
  Similarity: 77.16%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_aider_config_default (lines 38-47) ---[0m
    fn test_aider_config_default() {
        let config = AiderConfig::default();
        assert_eq!(config.model, "gpt-4");
        assert!(config.openai_api_key.is_none());
        assert!(config.anthropic_api_key.is_none());
        assert!(config.auto_commit);
        assert!(config.git);
        assert!(config.additional_args.is_empty());
        assert!(config.executable_path.is_none());
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_custom_config_default (lines 60-68) ---[0m
    fn test_custom_config_default() {
        let config = CustomConfig::default();
        assert_eq!(config.command, "echo");
        assert_eq!(config.args, vec!["{prompt}"]);
        assert!(config.env_vars.is_empty());
        assert!(config.working_directory.is_none());
        assert_eq!(config.timeout_seconds, Some(300));
        assert!(!config.supports_json);
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:50-57 function test_codex_config_default <-> ./crates/ccswarm/src/tests/provider_tests.rs:60-68 function test_custom_config_default
  Similarity: 81.43%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_codex_config_default (lines 50-57) ---[0m
    fn test_codex_config_default() {
        let config = CodexConfig::default();
        assert_eq!(config.model, "gpt-4");
        assert_eq!(config.max_tokens, Some(2048));
        assert_eq!(config.temperature, Some(0.1));
        assert!(config.api_base.is_none());
        assert!(config.organization.is_none());
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_custom_config_default (lines 60-68) ---[0m
    fn test_custom_config_default() {
        let config = CustomConfig::default();
        assert_eq!(config.command, "echo");
        assert_eq!(config.args, vec!["{prompt}"]);
        assert!(config.env_vars.is_empty());
        assert!(config.working_directory.is_none());
        assert_eq!(config.timeout_seconds, Some(300));
        assert!(!config.supports_json);
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:38-47 function test_aider_config_default <-> ./crates/ccswarm/src/tests/provider_tests.rs:50-57 function test_codex_config_default
  Similarity: 76.21%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_aider_config_default (lines 38-47) ---[0m
    fn test_aider_config_default() {
        let config = AiderConfig::default();
        assert_eq!(config.model, "gpt-4");
        assert!(config.openai_api_key.is_none());
        assert!(config.anthropic_api_key.is_none());
        assert!(config.auto_commit);
        assert!(config.git);
        assert!(config.additional_args.is_empty());
        assert!(config.executable_path.is_none());
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_codex_config_default (lines 50-57) ---[0m
    fn test_codex_config_default() {
        let config = CodexConfig::default();
        assert_eq!(config.model, "gpt-4");
        assert_eq!(config.max_tokens, Some(2048));
        assert_eq!(config.temperature, Some(0.1));
        assert!(config.api_base.is_none());
        assert!(config.organization.is_none());
    }

  ./crates/ccswarm/src/tests/provider_tests.rs:26-35 function test_claude_code_config_default <-> ./crates/ccswarm/src/tests/provider_tests.rs:50-57 function test_codex_config_default
  Similarity: 75.40%

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_claude_code_config_default (lines 26-35) ---[0m
    fn test_claude_code_config_default() {
        let config = ClaudeCodeConfig::default();
        assert_eq!(config.model, "claude-3.5-sonnet");
        assert!(!config.dangerous_skip);
        assert_eq!(config.think_mode, Some("think".to_string()));
        assert!(config.json_output);
        assert!(config.custom_commands.is_empty());
        assert!(config.mcp_servers.is_empty());
        assert!(config.api_key.is_none());
    }

[36m--- ./crates/ccswarm/src/tests/provider_tests.rs:test_codex_config_default (lines 50-57) ---[0m
    fn test_codex_config_default() {
        let config = CodexConfig::default();
        assert_eq!(config.model, "gpt-4");
        assert_eq!(config.max_tokens, Some(2048));
        assert_eq!(config.temperature, Some(0.1));
        assert!(config.api_base.is_none());
        assert!(config.organization.is_none());
    }


Duplicates in ./crates/ccswarm/src/agent/backend_status.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/agent/backend_status.rs:160-187 method check_api_endpoints <-> ./crates/ccswarm/src/agent/backend_status.rs:336-360 function test_backend_status_formatting
  Similarity: 88.60%

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:check_api_endpoints (lines 160-187) ---[0m
    async fn check_api_endpoints(&self) -> Result<HashMap<String, EndpointStatus>> {
        // In a real implementation, this would check actual endpoints
        // For now, return simulated data
        let mut endpoints = HashMap::new();

        let common_endpoints = vec![
            ("/api/health", "GET"),
            ("/api/users", "GET"),
            ("/api/users", "POST"),
            ("/api/auth/login", "POST"),
            ("/api/auth/logout", "POST"),
        ];

        for (path, method) in common_endpoints {
            endpoints.insert(
                format!("{}-{}", method, path),
                EndpointStatus {
                    path: path.to_string(),
                    method: method.to_string(),
                    is_healthy: true,
                    response_time_ms: Some(rand::random::<f64>() * 100.0),
                    last_checked: Utc::now(),
                },
            );
        }

        Ok(endpoints)
    }

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:test_backend_status_formatting (lines 336-360) ---[0m
    fn test_backend_status_formatting() {
        let status = BackendStatus {
            api_endpoints: HashMap::new(),
            database_status: DatabaseStatus {
                is_connected: true,
                database_type: "PostgreSQL".to_string(),
                connection_pool_size: 10,
                active_connections: 3,
                last_migration: None,
            },
            server_metrics: ServerMetrics {
                uptime_seconds: 3600,
                memory_usage_mb: 512.0,
                cpu_usage_percent: 25.0,
                request_count: 1000,
                error_rate: 0.01,
            },
            active_services: vec![],
            recent_api_calls: vec![],
        };

        let formatted = status.format_backend_status();
        assert!(formatted.contains("Backend Status Summary"));
        assert!(formatted.contains("PostgreSQL"));
    }

  ./crates/ccswarm/src/agent/backend_status.rs:160-187 method check_api_endpoints <-> ./crates/ccswarm/src/agent/backend_status.rs:239-257 method get_recent_api_calls
  Similarity: 93.18%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:check_api_endpoints (lines 160-187) ---[0m
    async fn check_api_endpoints(&self) -> Result<HashMap<String, EndpointStatus>> {
        // In a real implementation, this would check actual endpoints
        // For now, return simulated data
        let mut endpoints = HashMap::new();

        let common_endpoints = vec![
            ("/api/health", "GET"),
            ("/api/users", "GET"),
            ("/api/users", "POST"),
            ("/api/auth/login", "POST"),
            ("/api/auth/logout", "POST"),
        ];

        for (path, method) in common_endpoints {
            endpoints.insert(
                format!("{}-{}", method, path),
                EndpointStatus {
                    path: path.to_string(),
                    method: method.to_string(),
                    is_healthy: true,
                    response_time_ms: Some(rand::random::<f64>() * 100.0),
                    last_checked: Utc::now(),
                },
            );
        }

        Ok(endpoints)
    }

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:get_recent_api_calls (lines 239-257) ---[0m
    async fn get_recent_api_calls(&self) -> Result<Vec<ApiCallInfo>> {
        // Return last 10 API calls (simulated)
        let mut calls = Vec::new();
        for i in 0..10 {
            calls.push(ApiCallInfo {
                timestamp: Utc::now() - chrono::Duration::minutes(i as i64),
                endpoint: match i % 4 {
                    0 => "/api/users".to_string(),
                    1 => "/api/auth/login".to_string(),
                    2 => "/api/products".to_string(),
                    _ => "/api/health".to_string(),
                },
                method: if i % 3 == 0 { "POST" } else { "GET" }.to_string(),
                status_code: if i == 7 { 500 } else { 200 },
                response_time_ms: rand::random::<f64>() * 200.0,
            });
        }
        Ok(calls)
    }

  ./crates/ccswarm/src/agent/backend_status.rs:160-187 method check_api_endpoints <-> ./crates/ccswarm/src/agent/backend_status.rs:214-236 method list_active_services
  Similarity: 84.77%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:check_api_endpoints (lines 160-187) ---[0m
    async fn check_api_endpoints(&self) -> Result<HashMap<String, EndpointStatus>> {
        // In a real implementation, this would check actual endpoints
        // For now, return simulated data
        let mut endpoints = HashMap::new();

        let common_endpoints = vec![
            ("/api/health", "GET"),
            ("/api/users", "GET"),
            ("/api/users", "POST"),
            ("/api/auth/login", "POST"),
            ("/api/auth/logout", "POST"),
        ];

        for (path, method) in common_endpoints {
            endpoints.insert(
                format!("{}-{}", method, path),
                EndpointStatus {
                    path: path.to_string(),
                    method: method.to_string(),
                    is_healthy: true,
                    response_time_ms: Some(rand::random::<f64>() * 100.0),
                    last_checked: Utc::now(),
                },
            );
        }

        Ok(endpoints)
    }

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:list_active_services (lines 214-236) ---[0m
    async fn list_active_services(&self) -> Result<Vec<ServiceInfo>> {
        // Simulated service list
        Ok(vec![
            ServiceInfo {
                name: "express-server".to_string(),
                status: "running".to_string(),
                port: Some(3000),
                dependencies: vec!["postgresql".to_string(), "redis".to_string()],
            },
            ServiceInfo {
                name: "websocket-server".to_string(),
                status: "running".to_string(),
                port: Some(3001),
                dependencies: vec!["redis".to_string()],
            },
            ServiceInfo {
                name: "background-worker".to_string(),
                status: "running".to_string(),
                port: None,
                dependencies: vec!["postgresql".to_string(), "redis".to_string()],
            },
        ])
    }

  ./crates/ccswarm/src/agent/backend_status.rs:270-296 method format_backend_status <-> ./crates/ccswarm/src/agent/backend_status.rs:336-360 function test_backend_status_formatting
  Similarity: 79.95%

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:format_backend_status (lines 270-296) ---[0m
    fn format_backend_status(&self) -> String {
        let api_health = self.api_endpoints.values().filter(|e| e.is_healthy).count() as f64
            / self.api_endpoints.len().max(1) as f64
            * 100.0;

        format!(
            "Backend Status Summary:\n\
             ├─ API Health: {:.1}% ({}/{} endpoints)\n\
             ├─ Database: {} ({})\n\
             ├─ Server: {:.1}MB RAM, {:.1}% CPU\n\
             ├─ Active Services: {}\n\
             └─ Recent Activity: {} API calls",
            api_health,
            self.api_endpoints.values().filter(|e| e.is_healthy).count(),
            self.api_endpoints.len(),
            if self.database_status.is_connected {
                "Connected"
            } else {
                "Disconnected"
            },
            self.database_status.database_type,
            self.server_metrics.memory_usage_mb,
            self.server_metrics.cpu_usage_percent,
            self.active_services.len(),
            self.recent_api_calls.len()
        )
    }

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:test_backend_status_formatting (lines 336-360) ---[0m
    fn test_backend_status_formatting() {
        let status = BackendStatus {
            api_endpoints: HashMap::new(),
            database_status: DatabaseStatus {
                is_connected: true,
                database_type: "PostgreSQL".to_string(),
                connection_pool_size: 10,
                active_connections: 3,
                last_migration: None,
            },
            server_metrics: ServerMetrics {
                uptime_seconds: 3600,
                memory_usage_mb: 512.0,
                cpu_usage_percent: 25.0,
                request_count: 1000,
                error_rate: 0.01,
            },
            active_services: vec![],
            recent_api_calls: vec![],
        };

        let formatted = status.format_backend_status();
        assert!(formatted.contains("Backend Status Summary"));
        assert!(formatted.contains("PostgreSQL"));
    }

  ./crates/ccswarm/src/agent/backend_status.rs:239-257 method get_recent_api_calls <-> ./crates/ccswarm/src/agent/backend_status.rs:336-360 function test_backend_status_formatting
  Similarity: 92.70%

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:get_recent_api_calls (lines 239-257) ---[0m
    async fn get_recent_api_calls(&self) -> Result<Vec<ApiCallInfo>> {
        // Return last 10 API calls (simulated)
        let mut calls = Vec::new();
        for i in 0..10 {
            calls.push(ApiCallInfo {
                timestamp: Utc::now() - chrono::Duration::minutes(i as i64),
                endpoint: match i % 4 {
                    0 => "/api/users".to_string(),
                    1 => "/api/auth/login".to_string(),
                    2 => "/api/products".to_string(),
                    _ => "/api/health".to_string(),
                },
                method: if i % 3 == 0 { "POST" } else { "GET" }.to_string(),
                status_code: if i == 7 { 500 } else { 200 },
                response_time_ms: rand::random::<f64>() * 200.0,
            });
        }
        Ok(calls)
    }

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:test_backend_status_formatting (lines 336-360) ---[0m
    fn test_backend_status_formatting() {
        let status = BackendStatus {
            api_endpoints: HashMap::new(),
            database_status: DatabaseStatus {
                is_connected: true,
                database_type: "PostgreSQL".to_string(),
                connection_pool_size: 10,
                active_connections: 3,
                last_migration: None,
            },
            server_metrics: ServerMetrics {
                uptime_seconds: 3600,
                memory_usage_mb: 512.0,
                cpu_usage_percent: 25.0,
                request_count: 1000,
                error_rate: 0.01,
            },
            active_services: vec![],
            recent_api_calls: vec![],
        };

        let formatted = status.format_backend_status();
        assert!(formatted.contains("Backend Status Summary"));
        assert!(formatted.contains("PostgreSQL"));
    }

  ./crates/ccswarm/src/agent/backend_status.rs:82-104 method generate_backend_status <-> ./crates/ccswarm/src/agent/backend_status.rs:160-187 method check_api_endpoints
  Similarity: 78.50%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:generate_backend_status (lines 82-104) ---[0m
    pub async fn generate_backend_status(&self) -> Result<BackendStatus> {
        // Check if this is a backend agent
        if !matches!(self.identity.specialization, AgentRole::Backend { .. }) {
            return Err(anyhow::anyhow!(
                "This method is only available for backend agents"
            ));
        }

        // Simulate gathering backend-specific metrics
        let api_endpoints = self.check_api_endpoints().await?;
        let database_status = self.check_database_status().await?;
        let server_metrics = self.gather_server_metrics().await?;
        let active_services = self.list_active_services().await?;
        let recent_api_calls = self.get_recent_api_calls().await?;

        Ok(BackendStatus {
            api_endpoints,
            database_status,
            server_metrics,
            active_services,
            recent_api_calls,
        })
    }

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:check_api_endpoints (lines 160-187) ---[0m
    async fn check_api_endpoints(&self) -> Result<HashMap<String, EndpointStatus>> {
        // In a real implementation, this would check actual endpoints
        // For now, return simulated data
        let mut endpoints = HashMap::new();

        let common_endpoints = vec![
            ("/api/health", "GET"),
            ("/api/users", "GET"),
            ("/api/users", "POST"),
            ("/api/auth/login", "POST"),
            ("/api/auth/logout", "POST"),
        ];

        for (path, method) in common_endpoints {
            endpoints.insert(
                format!("{}-{}", method, path),
                EndpointStatus {
                    path: path.to_string(),
                    method: method.to_string(),
                    is_healthy: true,
                    response_time_ms: Some(rand::random::<f64>() * 100.0),
                    last_checked: Utc::now(),
                },
            );
        }

        Ok(endpoints)
    }

  ./crates/ccswarm/src/agent/backend_status.rs:239-257 method get_recent_api_calls <-> ./crates/ccswarm/src/agent/backend_status.rs:270-296 method format_backend_status
  Similarity: 86.61%
  Classes: ClaudeCodeAgent <-> BackendStatusExt

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:get_recent_api_calls (lines 239-257) ---[0m
    async fn get_recent_api_calls(&self) -> Result<Vec<ApiCallInfo>> {
        // Return last 10 API calls (simulated)
        let mut calls = Vec::new();
        for i in 0..10 {
            calls.push(ApiCallInfo {
                timestamp: Utc::now() - chrono::Duration::minutes(i as i64),
                endpoint: match i % 4 {
                    0 => "/api/users".to_string(),
                    1 => "/api/auth/login".to_string(),
                    2 => "/api/products".to_string(),
                    _ => "/api/health".to_string(),
                },
                method: if i % 3 == 0 { "POST" } else { "GET" }.to_string(),
                status_code: if i == 7 { 500 } else { 200 },
                response_time_ms: rand::random::<f64>() * 200.0,
            });
        }
        Ok(calls)
    }

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:format_backend_status (lines 270-296) ---[0m
    fn format_backend_status(&self) -> String {
        let api_health = self.api_endpoints.values().filter(|e| e.is_healthy).count() as f64
            / self.api_endpoints.len().max(1) as f64
            * 100.0;

        format!(
            "Backend Status Summary:\n\
             ├─ API Health: {:.1}% ({}/{} endpoints)\n\
             ├─ Database: {} ({})\n\
             ├─ Server: {:.1}MB RAM, {:.1}% CPU\n\
             ├─ Active Services: {}\n\
             └─ Recent Activity: {} API calls",
            api_health,
            self.api_endpoints.values().filter(|e| e.is_healthy).count(),
            self.api_endpoints.len(),
            if self.database_status.is_connected {
                "Connected"
            } else {
                "Disconnected"
            },
            self.database_status.database_type,
            self.server_metrics.memory_usage_mb,
            self.server_metrics.cpu_usage_percent,
            self.active_services.len(),
            self.recent_api_calls.len()
        )
    }

  ./crates/ccswarm/src/agent/backend_status.rs:214-236 method list_active_services <-> ./crates/ccswarm/src/agent/backend_status.rs:336-360 function test_backend_status_formatting
  Similarity: 81.61%

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:list_active_services (lines 214-236) ---[0m
    async fn list_active_services(&self) -> Result<Vec<ServiceInfo>> {
        // Simulated service list
        Ok(vec![
            ServiceInfo {
                name: "express-server".to_string(),
                status: "running".to_string(),
                port: Some(3000),
                dependencies: vec!["postgresql".to_string(), "redis".to_string()],
            },
            ServiceInfo {
                name: "websocket-server".to_string(),
                status: "running".to_string(),
                port: Some(3001),
                dependencies: vec!["redis".to_string()],
            },
            ServiceInfo {
                name: "background-worker".to_string(),
                status: "running".to_string(),
                port: None,
                dependencies: vec!["postgresql".to_string(), "redis".to_string()],
            },
        ])
    }

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:test_backend_status_formatting (lines 336-360) ---[0m
    fn test_backend_status_formatting() {
        let status = BackendStatus {
            api_endpoints: HashMap::new(),
            database_status: DatabaseStatus {
                is_connected: true,
                database_type: "PostgreSQL".to_string(),
                connection_pool_size: 10,
                active_connections: 3,
                last_migration: None,
            },
            server_metrics: ServerMetrics {
                uptime_seconds: 3600,
                memory_usage_mb: 512.0,
                cpu_usage_percent: 25.0,
                request_count: 1000,
                error_rate: 0.01,
            },
            active_services: vec![],
            recent_api_calls: vec![],
        };

        let formatted = status.format_backend_status();
        assert!(formatted.contains("Backend Status Summary"));
        assert!(formatted.contains("PostgreSQL"));
    }

  ./crates/ccswarm/src/agent/backend_status.rs:214-236 method list_active_services <-> ./crates/ccswarm/src/agent/backend_status.rs:239-257 method get_recent_api_calls
  Similarity: 92.79%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:list_active_services (lines 214-236) ---[0m
    async fn list_active_services(&self) -> Result<Vec<ServiceInfo>> {
        // Simulated service list
        Ok(vec![
            ServiceInfo {
                name: "express-server".to_string(),
                status: "running".to_string(),
                port: Some(3000),
                dependencies: vec!["postgresql".to_string(), "redis".to_string()],
            },
            ServiceInfo {
                name: "websocket-server".to_string(),
                status: "running".to_string(),
                port: Some(3001),
                dependencies: vec!["redis".to_string()],
            },
            ServiceInfo {
                name: "background-worker".to_string(),
                status: "running".to_string(),
                port: None,
                dependencies: vec!["postgresql".to_string(), "redis".to_string()],
            },
        ])
    }

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:get_recent_api_calls (lines 239-257) ---[0m
    async fn get_recent_api_calls(&self) -> Result<Vec<ApiCallInfo>> {
        // Return last 10 API calls (simulated)
        let mut calls = Vec::new();
        for i in 0..10 {
            calls.push(ApiCallInfo {
                timestamp: Utc::now() - chrono::Duration::minutes(i as i64),
                endpoint: match i % 4 {
                    0 => "/api/users".to_string(),
                    1 => "/api/auth/login".to_string(),
                    2 => "/api/products".to_string(),
                    _ => "/api/health".to_string(),
                },
                method: if i % 3 == 0 { "POST" } else { "GET" }.to_string(),
                status_code: if i == 7 { 500 } else { 200 },
                response_time_ms: rand::random::<f64>() * 200.0,
            });
        }
        Ok(calls)
    }

  ./crates/ccswarm/src/agent/backend_status.rs:160-187 method check_api_endpoints <-> ./crates/ccswarm/src/agent/backend_status.rs:320-333 function test_backend_status_generation
  Similarity: 81.36%

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:check_api_endpoints (lines 160-187) ---[0m
    async fn check_api_endpoints(&self) -> Result<HashMap<String, EndpointStatus>> {
        // In a real implementation, this would check actual endpoints
        // For now, return simulated data
        let mut endpoints = HashMap::new();

        let common_endpoints = vec![
            ("/api/health", "GET"),
            ("/api/users", "GET"),
            ("/api/users", "POST"),
            ("/api/auth/login", "POST"),
            ("/api/auth/logout", "POST"),
        ];

        for (path, method) in common_endpoints {
            endpoints.insert(
                format!("{}-{}", method, path),
                EndpointStatus {
                    path: path.to_string(),
                    method: method.to_string(),
                    is_healthy: true,
                    response_time_ms: Some(rand::random::<f64>() * 100.0),
                    last_checked: Utc::now(),
                },
            );
        }

        Ok(endpoints)
    }

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:test_backend_status_generation (lines 320-333) ---[0m
    async fn test_backend_status_generation() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::for_agent("backend");

        let agent = ClaudeCodeAgent::new(default_backend_role(), temp_dir.path(), "test", config)
            .await
            .unwrap();

        let status = agent.generate_backend_status().await.unwrap();

        assert!(!status.api_endpoints.is_empty());
        assert!(status.database_status.is_connected);
        assert!(!status.active_services.is_empty());
    }

  ./crates/ccswarm/src/agent/backend_status.rs:270-296 method format_backend_status <-> ./crates/ccswarm/src/agent/backend_status.rs:298-309 method is_backend_healthy
  Similarity: 87.35%
  Classes: BackendStatusExt <-> BackendStatusExt

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:format_backend_status (lines 270-296) ---[0m
    fn format_backend_status(&self) -> String {
        let api_health = self.api_endpoints.values().filter(|e| e.is_healthy).count() as f64
            / self.api_endpoints.len().max(1) as f64
            * 100.0;

        format!(
            "Backend Status Summary:\n\
             ├─ API Health: {:.1}% ({}/{} endpoints)\n\
             ├─ Database: {} ({})\n\
             ├─ Server: {:.1}MB RAM, {:.1}% CPU\n\
             ├─ Active Services: {}\n\
             └─ Recent Activity: {} API calls",
            api_health,
            self.api_endpoints.values().filter(|e| e.is_healthy).count(),
            self.api_endpoints.len(),
            if self.database_status.is_connected {
                "Connected"
            } else {
                "Disconnected"
            },
            self.database_status.database_type,
            self.server_metrics.memory_usage_mb,
            self.server_metrics.cpu_usage_percent,
            self.active_services.len(),
            self.recent_api_calls.len()
        )
    }

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:is_backend_healthy (lines 298-309) ---[0m
    fn is_backend_healthy(&self) -> bool {
        // Backend is healthy if:
        // - Database is connected
        // - At least 80% of API endpoints are healthy
        // - Error rate is below 5%
        let api_health_ratio = self.api_endpoints.values().filter(|e| e.is_healthy).count() as f64
            / self.api_endpoints.len().max(1) as f64;

        self.database_status.is_connected
            && api_health_ratio >= 0.8
            && self.server_metrics.error_rate < 0.05
    }

  ./crates/ccswarm/src/agent/backend_status.rs:298-309 method is_backend_healthy <-> ./crates/ccswarm/src/agent/backend_status.rs:336-360 function test_backend_status_formatting
  Similarity: 87.60%

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:is_backend_healthy (lines 298-309) ---[0m
    fn is_backend_healthy(&self) -> bool {
        // Backend is healthy if:
        // - Database is connected
        // - At least 80% of API endpoints are healthy
        // - Error rate is below 5%
        let api_health_ratio = self.api_endpoints.values().filter(|e| e.is_healthy).count() as f64
            / self.api_endpoints.len().max(1) as f64;

        self.database_status.is_connected
            && api_health_ratio >= 0.8
            && self.server_metrics.error_rate < 0.05
    }

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:test_backend_status_formatting (lines 336-360) ---[0m
    fn test_backend_status_formatting() {
        let status = BackendStatus {
            api_endpoints: HashMap::new(),
            database_status: DatabaseStatus {
                is_connected: true,
                database_type: "PostgreSQL".to_string(),
                connection_pool_size: 10,
                active_connections: 3,
                last_migration: None,
            },
            server_metrics: ServerMetrics {
                uptime_seconds: 3600,
                memory_usage_mb: 512.0,
                cpu_usage_percent: 25.0,
                request_count: 1000,
                error_rate: 0.01,
            },
            active_services: vec![],
            recent_api_calls: vec![],
        };

        let formatted = status.format_backend_status();
        assert!(formatted.contains("Backend Status Summary"));
        assert!(formatted.contains("PostgreSQL"));
    }

  ./crates/ccswarm/src/agent/backend_status.rs:214-236 method list_active_services <-> ./crates/ccswarm/src/agent/backend_status.rs:298-309 method is_backend_healthy
  Similarity: 92.24%
  Classes: ClaudeCodeAgent <-> BackendStatusExt

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:list_active_services (lines 214-236) ---[0m
    async fn list_active_services(&self) -> Result<Vec<ServiceInfo>> {
        // Simulated service list
        Ok(vec![
            ServiceInfo {
                name: "express-server".to_string(),
                status: "running".to_string(),
                port: Some(3000),
                dependencies: vec!["postgresql".to_string(), "redis".to_string()],
            },
            ServiceInfo {
                name: "websocket-server".to_string(),
                status: "running".to_string(),
                port: Some(3001),
                dependencies: vec!["redis".to_string()],
            },
            ServiceInfo {
                name: "background-worker".to_string(),
                status: "running".to_string(),
                port: None,
                dependencies: vec!["postgresql".to_string(), "redis".to_string()],
            },
        ])
    }

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:is_backend_healthy (lines 298-309) ---[0m
    fn is_backend_healthy(&self) -> bool {
        // Backend is healthy if:
        // - Database is connected
        // - At least 80% of API endpoints are healthy
        // - Error rate is below 5%
        let api_health_ratio = self.api_endpoints.values().filter(|e| e.is_healthy).count() as f64
            / self.api_endpoints.len().max(1) as f64;

        self.database_status.is_connected
            && api_health_ratio >= 0.8
            && self.server_metrics.error_rate < 0.05
    }

  ./crates/ccswarm/src/agent/backend_status.rs:82-104 method generate_backend_status <-> ./crates/ccswarm/src/agent/backend_status.rs:320-333 function test_backend_status_generation
  Similarity: 76.04%

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:generate_backend_status (lines 82-104) ---[0m
    pub async fn generate_backend_status(&self) -> Result<BackendStatus> {
        // Check if this is a backend agent
        if !matches!(self.identity.specialization, AgentRole::Backend { .. }) {
            return Err(anyhow::anyhow!(
                "This method is only available for backend agents"
            ));
        }

        // Simulate gathering backend-specific metrics
        let api_endpoints = self.check_api_endpoints().await?;
        let database_status = self.check_database_status().await?;
        let server_metrics = self.gather_server_metrics().await?;
        let active_services = self.list_active_services().await?;
        let recent_api_calls = self.get_recent_api_calls().await?;

        Ok(BackendStatus {
            api_endpoints,
            database_status,
            server_metrics,
            active_services,
            recent_api_calls,
        })
    }

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:test_backend_status_generation (lines 320-333) ---[0m
    async fn test_backend_status_generation() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::for_agent("backend");

        let agent = ClaudeCodeAgent::new(default_backend_role(), temp_dir.path(), "test", config)
            .await
            .unwrap();

        let status = agent.generate_backend_status().await.unwrap();

        assert!(!status.api_endpoints.is_empty());
        assert!(status.database_status.is_connected);
        assert!(!status.active_services.is_empty());
    }

  ./crates/ccswarm/src/agent/backend_status.rs:82-104 method generate_backend_status <-> ./crates/ccswarm/src/agent/backend_status.rs:298-309 method is_backend_healthy
  Similarity: 77.22%
  Classes: ClaudeCodeAgent <-> BackendStatusExt

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:generate_backend_status (lines 82-104) ---[0m
    pub async fn generate_backend_status(&self) -> Result<BackendStatus> {
        // Check if this is a backend agent
        if !matches!(self.identity.specialization, AgentRole::Backend { .. }) {
            return Err(anyhow::anyhow!(
                "This method is only available for backend agents"
            ));
        }

        // Simulate gathering backend-specific metrics
        let api_endpoints = self.check_api_endpoints().await?;
        let database_status = self.check_database_status().await?;
        let server_metrics = self.gather_server_metrics().await?;
        let active_services = self.list_active_services().await?;
        let recent_api_calls = self.get_recent_api_calls().await?;

        Ok(BackendStatus {
            api_endpoints,
            database_status,
            server_metrics,
            active_services,
            recent_api_calls,
        })
    }

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:is_backend_healthy (lines 298-309) ---[0m
    fn is_backend_healthy(&self) -> bool {
        // Backend is healthy if:
        // - Database is connected
        // - At least 80% of API endpoints are healthy
        // - Error rate is below 5%
        let api_health_ratio = self.api_endpoints.values().filter(|e| e.is_healthy).count() as f64
            / self.api_endpoints.len().max(1) as f64;

        self.database_status.is_connected
            && api_health_ratio >= 0.8
            && self.server_metrics.error_rate < 0.05
    }

  ./crates/ccswarm/src/agent/backend_status.rs:239-257 method get_recent_api_calls <-> ./crates/ccswarm/src/agent/backend_status.rs:320-333 function test_backend_status_generation
  Similarity: 71.42%

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:get_recent_api_calls (lines 239-257) ---[0m
    async fn get_recent_api_calls(&self) -> Result<Vec<ApiCallInfo>> {
        // Return last 10 API calls (simulated)
        let mut calls = Vec::new();
        for i in 0..10 {
            calls.push(ApiCallInfo {
                timestamp: Utc::now() - chrono::Duration::minutes(i as i64),
                endpoint: match i % 4 {
                    0 => "/api/users".to_string(),
                    1 => "/api/auth/login".to_string(),
                    2 => "/api/products".to_string(),
                    _ => "/api/health".to_string(),
                },
                method: if i % 3 == 0 { "POST" } else { "GET" }.to_string(),
                status_code: if i == 7 { 500 } else { 200 },
                response_time_ms: rand::random::<f64>() * 200.0,
            });
        }
        Ok(calls)
    }

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:test_backend_status_generation (lines 320-333) ---[0m
    async fn test_backend_status_generation() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::for_agent("backend");

        let agent = ClaudeCodeAgent::new(default_backend_role(), temp_dir.path(), "test", config)
            .await
            .unwrap();

        let status = agent.generate_backend_status().await.unwrap();

        assert!(!status.api_endpoints.is_empty());
        assert!(status.database_status.is_connected);
        assert!(!status.active_services.is_empty());
    }

  ./crates/ccswarm/src/agent/backend_status.rs:298-309 method is_backend_healthy <-> ./crates/ccswarm/src/agent/backend_status.rs:320-333 function test_backend_status_generation
  Similarity: 77.50%

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:is_backend_healthy (lines 298-309) ---[0m
    fn is_backend_healthy(&self) -> bool {
        // Backend is healthy if:
        // - Database is connected
        // - At least 80% of API endpoints are healthy
        // - Error rate is below 5%
        let api_health_ratio = self.api_endpoints.values().filter(|e| e.is_healthy).count() as f64
            / self.api_endpoints.len().max(1) as f64;

        self.database_status.is_connected
            && api_health_ratio >= 0.8
            && self.server_metrics.error_rate < 0.05
    }

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:test_backend_status_generation (lines 320-333) ---[0m
    async fn test_backend_status_generation() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::for_agent("backend");

        let agent = ClaudeCodeAgent::new(default_backend_role(), temp_dir.path(), "test", config)
            .await
            .unwrap();

        let status = agent.generate_backend_status().await.unwrap();

        assert!(!status.api_endpoints.is_empty());
        assert!(status.database_status.is_connected);
        assert!(!status.active_services.is_empty());
    }

  ./crates/ccswarm/src/agent/backend_status.rs:190-199 method check_database_status <-> ./crates/ccswarm/src/agent/backend_status.rs:202-211 method gather_server_metrics
  Similarity: 93.75%
  Classes: ClaudeCodeAgent <-> ClaudeCodeAgent

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:check_database_status (lines 190-199) ---[0m
    async fn check_database_status(&self) -> Result<DatabaseStatus> {
        // Simulated database status
        Ok(DatabaseStatus {
            is_connected: true,
            database_type: "PostgreSQL".to_string(),
            connection_pool_size: 10,
            active_connections: 3,
            last_migration: Some("20240315_add_user_table".to_string()),
        })
    }

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:gather_server_metrics (lines 202-211) ---[0m
    async fn gather_server_metrics(&self) -> Result<ServerMetrics> {
        // Simulated server metrics
        Ok(ServerMetrics {
            uptime_seconds: 86400, // 1 day
            memory_usage_mb: 256.5,
            cpu_usage_percent: 15.3,
            request_count: 1234,
            error_rate: 0.02, // 2% error rate
        })
    }

  ./crates/ccswarm/src/agent/backend_status.rs:190-199 method check_database_status <-> ./crates/ccswarm/src/agent/backend_status.rs:298-309 method is_backend_healthy
  Similarity: 85.10%
  Classes: ClaudeCodeAgent <-> BackendStatusExt

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:check_database_status (lines 190-199) ---[0m
    async fn check_database_status(&self) -> Result<DatabaseStatus> {
        // Simulated database status
        Ok(DatabaseStatus {
            is_connected: true,
            database_type: "PostgreSQL".to_string(),
            connection_pool_size: 10,
            active_connections: 3,
            last_migration: Some("20240315_add_user_table".to_string()),
        })
    }

[36m--- ./crates/ccswarm/src/agent/backend_status.rs:is_backend_healthy (lines 298-309) ---[0m
    fn is_backend_healthy(&self) -> bool {
        // Backend is healthy if:
        // - Database is connected
        // - At least 80% of API endpoints are healthy
        // - Error rate is below 5%
        let api_health_ratio = self.api_endpoints.values().filter(|e| e.is_healthy).count() as f64
            / self.api_endpoints.len().max(1) as f64;

        self.database_status.is_connected
            && api_health_ratio >= 0.8
            && self.server_metrics.error_rate < 0.05
    }


Duplicates in ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:15-61 function orchestrate_task <-> ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:67-118 function execute_step
  Similarity: 90.96%

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:orchestrate_task (lines 15-61) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!("Starting orchestration for task: {}", task.id);

        // Step 1: Initial Analysis
        let plan = self.analyze_task(task).await?;
        debug!("Created plan with {} steps", plan.steps.len());

        // Execute steps sequentially
        let mut all_results = Vec::new();
        let mut current_plan = plan;

        for (index, step) in current_plan.steps.clone().iter().enumerate() {
            info!(
                "Executing step {}/{}: {}",
                index + 1,
                current_plan.steps.len(),
                step.name
            );

            // Execute current step
            let step_result = self.execute_step(step, &current_plan.context).await?;

            // Update context with step outputs
            for (key, value) in &step_result.outputs {
                current_plan.update_context(key.clone(), value.clone());
            }

            all_results.push(step_result);

            // Review and adapt if this isn't the last step
            if index < current_plan.steps.len() - 1 && current_plan.adaptive {
                debug!("Reviewing progress and adapting plan...");
                if let Ok(adapted_plan) =
                    self.review_and_adapt(&all_results, &mut current_plan).await
                {
                    current_plan = adapted_plan;
                    info!(
                        "Plan adapted, now has {} remaining steps",
                        current_plan.steps.len() - index - 1
                    );
                }
            }
        }

        // Synthesize final result
        self.synthesize_results(task, all_results).await
    }

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:execute_step (lines 67-118) ---[0m
    async fn execute_step(
        &self,
        step: &TaskStep,
        context: &std::collections::HashMap<String, String>,
    ) -> Result<StepResult> {
        let start = Instant::now();
        let mut result = StepResult::new(step.id.clone());

        // Execute parallel tasks
        if !step.parallel_tasks.is_empty() {
            info!(
                "Executing {} parallel tasks for step: {}",
                step.parallel_tasks.len(),
                step.name
            );

            let futures: Vec<_> = step
                .parallel_tasks
                .iter()
                .map(|task| self.execute_parallel_task(task, context))
                .collect();

            let parallel_results = join_all(futures).await;

            // Collect results
            for (task, task_result) in step.parallel_tasks.iter().zip(parallel_results) {
                match task_result {
                    Ok(res) => {
                        if !res.success && task.critical {
                            result = result.failed(format!("Critical task '{}' failed", task.name));
                        }
                        result.parallel_results.push(res);
                    }
                    Err(e) => {
                        let error_msg = format!("Task '{}' error: {}", task.name, e);
                        if task.critical {
                            result = result.failed(error_msg);
                        } else {
                            result.errors.push(error_msg);
                        }
                    }
                }
            }
        }

        // Generate step summary
        let summary = self.generate_step_summary(step, &result).await?;
        result = result.with_summary(summary);

        result.duration_ms = start.elapsed().as_millis() as u64;
        Ok(result)
    }

  ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:204-229 method analyze_task <-> ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:244-264 method synthesize_results
  Similarity: 71.05%
  Classes: AgentOrchestrator <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:analyze_task (lines 204-229) ---[0m
        async fn analyze_task(&self, task: &Task) -> Result<TaskPlan> {
            let mut plan = TaskPlan::new(task.id.clone());

            // Add analysis step
            let analysis = OrchestrationBuilder::analysis_step(
                "step1",
                "Initial Analysis",
                vec![
                    OrchestrationBuilder::parallel_task(
                        "analyze_deps",
                        "Analyze Dependencies",
                        "cargo tree",
                        true,
                    ),
                    OrchestrationBuilder::parallel_task(
                        "check_files",
                        "Check Files",
                        "ls -la",
                        false,
                    ),
                ],
            );
            plan.add_step(analysis);

            Ok(plan)
        }

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:synthesize_results (lines 244-264) ---[0m
        async fn synthesize_results(
            &self,
            task: &Task,
            results: Vec<StepResult>,
        ) -> Result<TaskResult> {
            let all_success = results.iter().all(|r| r.is_success());
            if all_success {
                Ok(TaskResult::success(
                    serde_json::json!({
                        "task_id": task.id.clone(),
                        "message": "Orchestration complete"
                    }),
                    std::time::Duration::from_secs(0),
                ))
            } else {
                Ok(TaskResult::failure(
                    "Orchestration failed".to_string(),
                    std::time::Duration::from_secs(0),
                ))
            }
        }

  ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:142-150 function generate_step_summary <-> ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:204-229 method analyze_task
  Similarity: 85.44%

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:generate_step_summary (lines 142-150) ---[0m
    async fn generate_step_summary(&self, step: &TaskStep, result: &StepResult) -> Result<String> {
        let success_count = result.parallel_results.iter().filter(|r| r.success).count();
        let total_count = result.parallel_results.len();

        Ok(format!(
            "Step '{}' completed: {}/{} tasks successful. Duration: {}ms",
            step.name, success_count, total_count, result.duration_ms
        ))
    }

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:analyze_task (lines 204-229) ---[0m
        async fn analyze_task(&self, task: &Task) -> Result<TaskPlan> {
            let mut plan = TaskPlan::new(task.id.clone());

            // Add analysis step
            let analysis = OrchestrationBuilder::analysis_step(
                "step1",
                "Initial Analysis",
                vec![
                    OrchestrationBuilder::parallel_task(
                        "analyze_deps",
                        "Analyze Dependencies",
                        "cargo tree",
                        true,
                    ),
                    OrchestrationBuilder::parallel_task(
                        "check_files",
                        "Check Files",
                        "ls -la",
                        false,
                    ),
                ],
            );
            plan.add_step(analysis);

            Ok(plan)
        }

  ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:204-229 method analyze_task <-> ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:268-274 function test_orchestration
  Similarity: 87.04%

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:analyze_task (lines 204-229) ---[0m
        async fn analyze_task(&self, task: &Task) -> Result<TaskPlan> {
            let mut plan = TaskPlan::new(task.id.clone());

            // Add analysis step
            let analysis = OrchestrationBuilder::analysis_step(
                "step1",
                "Initial Analysis",
                vec![
                    OrchestrationBuilder::parallel_task(
                        "analyze_deps",
                        "Analyze Dependencies",
                        "cargo tree",
                        true,
                    ),
                    OrchestrationBuilder::parallel_task(
                        "check_files",
                        "Check Files",
                        "ls -la",
                        false,
                    ),
                ],
            );
            plan.add_step(analysis);

            Ok(plan)
        }

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:test_orchestration (lines 268-274) ---[0m
    async fn test_orchestration() {
        let orchestrator = TestOrchestrator;
        let task = TaskBuilder::new("Test orchestration".to_string()).build();

        let result = orchestrator.orchestrate_task(&task).await.unwrap();
        assert!(result.success);
    }

  ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:167-173 method execution_step <-> ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:204-229 method analyze_task
  Similarity: 81.12%
  Classes: OrchestrationBuilder <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:execution_step (lines 167-173) ---[0m
    pub fn execution_step(id: &str, name: &str, dependencies: Vec<&str>) -> TaskStep {
        let mut step = TaskStep::new(id.to_string(), name.to_string(), StepType::Execution);
        for dep in dependencies {
            step = step.depends_on(dep.to_string());
        }
        step
    }

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:analyze_task (lines 204-229) ---[0m
        async fn analyze_task(&self, task: &Task) -> Result<TaskPlan> {
            let mut plan = TaskPlan::new(task.id.clone());

            // Add analysis step
            let analysis = OrchestrationBuilder::analysis_step(
                "step1",
                "Initial Analysis",
                vec![
                    OrchestrationBuilder::parallel_task(
                        "analyze_deps",
                        "Analyze Dependencies",
                        "cargo tree",
                        true,
                    ),
                    OrchestrationBuilder::parallel_task(
                        "check_files",
                        "Check Files",
                        "ls -la",
                        false,
                    ),
                ],
            );
            plan.add_step(analysis);

            Ok(plan)
        }

  ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:142-150 function generate_step_summary <-> ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:244-264 method synthesize_results
  Similarity: 82.37%

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:generate_step_summary (lines 142-150) ---[0m
    async fn generate_step_summary(&self, step: &TaskStep, result: &StepResult) -> Result<String> {
        let success_count = result.parallel_results.iter().filter(|r| r.success).count();
        let total_count = result.parallel_results.len();

        Ok(format!(
            "Step '{}' completed: {}/{} tasks successful. Duration: {}ms",
            step.name, success_count, total_count, result.duration_ms
        ))
    }

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:synthesize_results (lines 244-264) ---[0m
        async fn synthesize_results(
            &self,
            task: &Task,
            results: Vec<StepResult>,
        ) -> Result<TaskResult> {
            let all_success = results.iter().all(|r| r.is_success());
            if all_success {
                Ok(TaskResult::success(
                    serde_json::json!({
                        "task_id": task.id.clone(),
                        "message": "Orchestration complete"
                    }),
                    std::time::Duration::from_secs(0),
                ))
            } else {
                Ok(TaskResult::failure(
                    "Orchestration failed".to_string(),
                    std::time::Duration::from_secs(0),
                ))
            }
        }

  ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:142-150 function generate_step_summary <-> ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:268-274 function test_orchestration
  Similarity: 82.17%

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:generate_step_summary (lines 142-150) ---[0m
    async fn generate_step_summary(&self, step: &TaskStep, result: &StepResult) -> Result<String> {
        let success_count = result.parallel_results.iter().filter(|r| r.success).count();
        let total_count = result.parallel_results.len();

        Ok(format!(
            "Step '{}' completed: {}/{} tasks successful. Duration: {}ms",
            step.name, success_count, total_count, result.duration_ms
        ))
    }

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:test_orchestration (lines 268-274) ---[0m
    async fn test_orchestration() {
        let orchestrator = TestOrchestrator;
        let task = TaskBuilder::new("Test orchestration".to_string()).build();

        let result = orchestrator.orchestrate_task(&task).await.unwrap();
        assert!(result.success);
    }

  ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:158-164 method analysis_step <-> ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:167-173 method execution_step
  Similarity: 92.79%
  Classes: OrchestrationBuilder <-> OrchestrationBuilder

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:analysis_step (lines 158-164) ---[0m
    pub fn analysis_step(id: &str, name: &str, tasks: Vec<ParallelTask>) -> TaskStep {
        let mut step = TaskStep::new(id.to_string(), name.to_string(), StepType::Analysis);
        for task in tasks {
            step.add_parallel_task(task);
        }
        step
    }

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:execution_step (lines 167-173) ---[0m
    pub fn execution_step(id: &str, name: &str, dependencies: Vec<&str>) -> TaskStep {
        let mut step = TaskStep::new(id.to_string(), name.to_string(), StepType::Execution);
        for dep in dependencies {
            step = step.depends_on(dep.to_string());
        }
        step
    }

  ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:142-150 function generate_step_summary <-> ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:158-164 method analysis_step
  Similarity: 80.96%

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:generate_step_summary (lines 142-150) ---[0m
    async fn generate_step_summary(&self, step: &TaskStep, result: &StepResult) -> Result<String> {
        let success_count = result.parallel_results.iter().filter(|r| r.success).count();
        let total_count = result.parallel_results.len();

        Ok(format!(
            "Step '{}' completed: {}/{} tasks successful. Duration: {}ms",
            step.name, success_count, total_count, result.duration_ms
        ))
    }

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:analysis_step (lines 158-164) ---[0m
    pub fn analysis_step(id: &str, name: &str, tasks: Vec<ParallelTask>) -> TaskStep {
        let mut step = TaskStep::new(id.to_string(), name.to_string(), StepType::Analysis);
        for task in tasks {
            step.add_parallel_task(task);
        }
        step
    }

  ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:142-150 function generate_step_summary <-> ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:167-173 method execution_step
  Similarity: 80.96%

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:generate_step_summary (lines 142-150) ---[0m
    async fn generate_step_summary(&self, step: &TaskStep, result: &StepResult) -> Result<String> {
        let success_count = result.parallel_results.iter().filter(|r| r.success).count();
        let total_count = result.parallel_results.len();

        Ok(format!(
            "Step '{}' completed: {}/{} tasks successful. Duration: {}ms",
            step.name, success_count, total_count, result.duration_ms
        ))
    }

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:execution_step (lines 167-173) ---[0m
    pub fn execution_step(id: &str, name: &str, dependencies: Vec<&str>) -> TaskStep {
        let mut step = TaskStep::new(id.to_string(), name.to_string(), StepType::Execution);
        for dep in dependencies {
            step = step.depends_on(dep.to_string());
        }
        step
    }

  ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:176-178 method validation_step <-> ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:231-242 method execute_parallel_task
  Similarity: 80.00%
  Classes: OrchestrationBuilder <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:validation_step (lines 176-178) ---[0m
    pub fn validation_step(id: &str, name: &str) -> TaskStep {
        TaskStep::new(id.to_string(), name.to_string(), StepType::Validation)
    }

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:execute_parallel_task (lines 231-242) ---[0m
        async fn execute_parallel_task(
            &self,
            task: &ParallelTask,
            _context: &HashMap<String, String>,
        ) -> Result<ParallelTaskResult> {
            Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: true,
                output: format!("Executed: {}", task.command),
                error: None,
            })
        }

  ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:158-164 method analysis_step <-> ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:268-274 function test_orchestration
  Similarity: 70.93%

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:analysis_step (lines 158-164) ---[0m
    pub fn analysis_step(id: &str, name: &str, tasks: Vec<ParallelTask>) -> TaskStep {
        let mut step = TaskStep::new(id.to_string(), name.to_string(), StepType::Analysis);
        for task in tasks {
            step.add_parallel_task(task);
        }
        step
    }

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:test_orchestration (lines 268-274) ---[0m
    async fn test_orchestration() {
        let orchestrator = TestOrchestrator;
        let task = TaskBuilder::new("Test orchestration".to_string()).build();

        let result = orchestrator.orchestrate_task(&task).await.unwrap();
        assert!(result.success);
    }

  ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:167-173 method execution_step <-> ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:268-274 function test_orchestration
  Similarity: 70.93%

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:execution_step (lines 167-173) ---[0m
    pub fn execution_step(id: &str, name: &str, dependencies: Vec<&str>) -> TaskStep {
        let mut step = TaskStep::new(id.to_string(), name.to_string(), StepType::Execution);
        for dep in dependencies {
            step = step.depends_on(dep.to_string());
        }
        step
    }

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:test_orchestration (lines 268-274) ---[0m
    async fn test_orchestration() {
        let orchestrator = TestOrchestrator;
        let task = TaskBuilder::new("Test orchestration".to_string()).build();

        let result = orchestrator.orchestrate_task(&task).await.unwrap();
        assert!(result.success);
    }

  ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:176-178 method validation_step <-> ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:181-190 method parallel_task
  Similarity: 70.00%
  Classes: OrchestrationBuilder <-> OrchestrationBuilder

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:validation_step (lines 176-178) ---[0m
    pub fn validation_step(id: &str, name: &str) -> TaskStep {
        TaskStep::new(id.to_string(), name.to_string(), StepType::Validation)
    }

[36m--- ./crates/ccswarm/src/agent/orchestrator/agent_orchestrator.rs:parallel_task (lines 181-190) ---[0m
    pub fn parallel_task(id: &str, name: &str, command: &str, critical: bool) -> ParallelTask {
        ParallelTask {
            id: id.to_string(),
            name: name.to_string(),
            command: command.to_string(),
            expected_duration_ms: 1000,
            critical,
            expect_failure: false,
        }
    }


Duplicates in ./crates/ccswarm/src/providers/mod.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/providers/mod.rs:499-530 method validate <-> ./crates/ccswarm/src/providers/mod.rs:598-631 method create_executor
  Similarity: 95.48%
  Classes: ProviderConfiguration <-> ProviderFactory

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 499-530) ---[0m
    pub async fn validate(&self) -> Result<()> {
        match self.provider_type {
            AIProvider::ClaudeCode => {
                if let Some(config) = &self.claude_code {
                    config.validate().await
                } else {
                    Err(anyhow::anyhow!("Claude Code configuration missing"))
                }
            }
            AIProvider::Aider => {
                if let Some(config) = &self.aider {
                    config.validate().await
                } else {
                    Err(anyhow::anyhow!("Aider configuration missing"))
                }
            }
            AIProvider::Codex => {
                if let Some(config) = &self.codex {
                    config.validate().await
                } else {
                    Err(anyhow::anyhow!("Codex configuration missing"))
                }
            }
            AIProvider::Custom => {
                if let Some(config) = &self.custom {
                    config.validate().await
                } else {
                    Err(anyhow::anyhow!("Custom configuration missing"))
                }
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:create_executor (lines 598-631) ---[0m
    pub fn create_executor(config: &ProviderConfiguration) -> Result<Box<dyn ProviderExecutor>> {
        match config.provider_type {
            AIProvider::ClaudeCode => {
                if let Some(claude_config) = &config.claude_code {
                    Ok(Box::new(claude_code::ClaudeCodeExecutor::new(
                        claude_config.clone(),
                    )))
                } else {
                    Err(anyhow::anyhow!("Claude Code configuration missing"))
                }
            }
            AIProvider::Aider => {
                if let Some(aider_config) = &config.aider {
                    Ok(Box::new(aider::AiderExecutor::new(aider_config.clone())))
                } else {
                    Err(anyhow::anyhow!("Aider configuration missing"))
                }
            }
            AIProvider::Codex => {
                if let Some(codex_config) = &config.codex {
                    Ok(Box::new(codex::CodexExecutor::new(codex_config.clone())))
                } else {
                    Err(anyhow::anyhow!("Codex configuration missing"))
                }
            }
            AIProvider::Custom => {
                if let Some(custom_config) = &config.custom {
                    Ok(Box::new(custom::CustomExecutor::new(custom_config.clone())))
                } else {
                    Err(anyhow::anyhow!("Custom configuration missing"))
                }
            }
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:559-590 method is_available <-> ./crates/ccswarm/src/providers/mod.rs:598-631 method create_executor
  Similarity: 95.37%
  Classes: ProviderConfiguration <-> ProviderFactory

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 559-590) ---[0m
    pub async fn is_available(&self) -> bool {
        match self.provider_type {
            AIProvider::ClaudeCode => {
                if let Some(config) = &self.claude_code {
                    config.is_available().await
                } else {
                    false
                }
            }
            AIProvider::Aider => {
                if let Some(config) = &self.aider {
                    config.is_available().await
                } else {
                    false
                }
            }
            AIProvider::Codex => {
                if let Some(config) = &self.codex {
                    config.is_available().await
                } else {
                    false
                }
            }
            AIProvider::Custom => {
                if let Some(config) = &self.custom {
                    config.is_available().await
                } else {
                    false
                }
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:create_executor (lines 598-631) ---[0m
    pub fn create_executor(config: &ProviderConfiguration) -> Result<Box<dyn ProviderExecutor>> {
        match config.provider_type {
            AIProvider::ClaudeCode => {
                if let Some(claude_config) = &config.claude_code {
                    Ok(Box::new(claude_code::ClaudeCodeExecutor::new(
                        claude_config.clone(),
                    )))
                } else {
                    Err(anyhow::anyhow!("Claude Code configuration missing"))
                }
            }
            AIProvider::Aider => {
                if let Some(aider_config) = &config.aider {
                    Ok(Box::new(aider::AiderExecutor::new(aider_config.clone())))
                } else {
                    Err(anyhow::anyhow!("Aider configuration missing"))
                }
            }
            AIProvider::Codex => {
                if let Some(codex_config) = &config.codex {
                    Ok(Box::new(codex::CodexExecutor::new(codex_config.clone())))
                } else {
                    Err(anyhow::anyhow!("Codex configuration missing"))
                }
            }
            AIProvider::Custom => {
                if let Some(custom_config) = &config.custom {
                    Ok(Box::new(custom::CustomExecutor::new(custom_config.clone())))
                } else {
                    Err(anyhow::anyhow!("Custom configuration missing"))
                }
            }
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:499-530 method validate <-> ./crates/ccswarm/src/providers/mod.rs:559-590 method is_available
  Similarity: 96.55%
  Classes: ProviderConfiguration <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 499-530) ---[0m
    pub async fn validate(&self) -> Result<()> {
        match self.provider_type {
            AIProvider::ClaudeCode => {
                if let Some(config) = &self.claude_code {
                    config.validate().await
                } else {
                    Err(anyhow::anyhow!("Claude Code configuration missing"))
                }
            }
            AIProvider::Aider => {
                if let Some(config) = &self.aider {
                    config.validate().await
                } else {
                    Err(anyhow::anyhow!("Aider configuration missing"))
                }
            }
            AIProvider::Codex => {
                if let Some(config) = &self.codex {
                    config.validate().await
                } else {
                    Err(anyhow::anyhow!("Codex configuration missing"))
                }
            }
            AIProvider::Custom => {
                if let Some(config) = &self.custom {
                    config.validate().await
                } else {
                    Err(anyhow::anyhow!("Custom configuration missing"))
                }
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 559-590) ---[0m
    pub async fn is_available(&self) -> bool {
        match self.provider_type {
            AIProvider::ClaudeCode => {
                if let Some(config) = &self.claude_code {
                    config.is_available().await
                } else {
                    false
                }
            }
            AIProvider::Aider => {
                if let Some(config) = &self.aider {
                    config.is_available().await
                } else {
                    false
                }
            }
            AIProvider::Codex => {
                if let Some(config) = &self.codex {
                    config.is_available().await
                } else {
                    false
                }
            }
            AIProvider::Custom => {
                if let Some(config) = &self.custom {
                    config.is_available().await
                } else {
                    false
                }
            }
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:533-556 method get_env_vars <-> ./crates/ccswarm/src/providers/mod.rs:598-631 method create_executor
  Similarity: 96.51%
  Classes: ProviderConfiguration <-> ProviderFactory

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 533-556) ---[0m
    pub fn get_env_vars(&self) -> HashMap<String, String> {
        match self.provider_type {
            AIProvider::ClaudeCode => self
                .claude_code
                .as_ref()
                .map(|c| c.get_env_vars())
                .unwrap_or_default(),
            AIProvider::Aider => self
                .aider
                .as_ref()
                .map(|c| c.get_env_vars())
                .unwrap_or_default(),
            AIProvider::Codex => self
                .codex
                .as_ref()
                .map(|c| c.get_env_vars())
                .unwrap_or_default(),
            AIProvider::Custom => self
                .custom
                .as_ref()
                .map(|c| c.get_env_vars())
                .unwrap_or_default(),
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:create_executor (lines 598-631) ---[0m
    pub fn create_executor(config: &ProviderConfiguration) -> Result<Box<dyn ProviderExecutor>> {
        match config.provider_type {
            AIProvider::ClaudeCode => {
                if let Some(claude_config) = &config.claude_code {
                    Ok(Box::new(claude_code::ClaudeCodeExecutor::new(
                        claude_config.clone(),
                    )))
                } else {
                    Err(anyhow::anyhow!("Claude Code configuration missing"))
                }
            }
            AIProvider::Aider => {
                if let Some(aider_config) = &config.aider {
                    Ok(Box::new(aider::AiderExecutor::new(aider_config.clone())))
                } else {
                    Err(anyhow::anyhow!("Aider configuration missing"))
                }
            }
            AIProvider::Codex => {
                if let Some(codex_config) = &config.codex {
                    Ok(Box::new(codex::CodexExecutor::new(codex_config.clone())))
                } else {
                    Err(anyhow::anyhow!("Codex configuration missing"))
                }
            }
            AIProvider::Custom => {
                if let Some(custom_config) = &config.custom {
                    Ok(Box::new(custom::CustomExecutor::new(custom_config.clone())))
                } else {
                    Err(anyhow::anyhow!("Custom configuration missing"))
                }
            }
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:499-530 method validate <-> ./crates/ccswarm/src/providers/mod.rs:533-556 method get_env_vars
  Similarity: 95.51%
  Classes: ProviderConfiguration <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 499-530) ---[0m
    pub async fn validate(&self) -> Result<()> {
        match self.provider_type {
            AIProvider::ClaudeCode => {
                if let Some(config) = &self.claude_code {
                    config.validate().await
                } else {
                    Err(anyhow::anyhow!("Claude Code configuration missing"))
                }
            }
            AIProvider::Aider => {
                if let Some(config) = &self.aider {
                    config.validate().await
                } else {
                    Err(anyhow::anyhow!("Aider configuration missing"))
                }
            }
            AIProvider::Codex => {
                if let Some(config) = &self.codex {
                    config.validate().await
                } else {
                    Err(anyhow::anyhow!("Codex configuration missing"))
                }
            }
            AIProvider::Custom => {
                if let Some(config) = &self.custom {
                    config.validate().await
                } else {
                    Err(anyhow::anyhow!("Custom configuration missing"))
                }
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 533-556) ---[0m
    pub fn get_env_vars(&self) -> HashMap<String, String> {
        match self.provider_type {
            AIProvider::ClaudeCode => self
                .claude_code
                .as_ref()
                .map(|c| c.get_env_vars())
                .unwrap_or_default(),
            AIProvider::Aider => self
                .aider
                .as_ref()
                .map(|c| c.get_env_vars())
                .unwrap_or_default(),
            AIProvider::Codex => self
                .codex
                .as_ref()
                .map(|c| c.get_env_vars())
                .unwrap_or_default(),
            AIProvider::Custom => self
                .custom
                .as_ref()
                .map(|c| c.get_env_vars())
                .unwrap_or_default(),
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:533-556 method get_env_vars <-> ./crates/ccswarm/src/providers/mod.rs:559-590 method is_available
  Similarity: 94.09%
  Classes: ProviderConfiguration <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 533-556) ---[0m
    pub fn get_env_vars(&self) -> HashMap<String, String> {
        match self.provider_type {
            AIProvider::ClaudeCode => self
                .claude_code
                .as_ref()
                .map(|c| c.get_env_vars())
                .unwrap_or_default(),
            AIProvider::Aider => self
                .aider
                .as_ref()
                .map(|c| c.get_env_vars())
                .unwrap_or_default(),
            AIProvider::Codex => self
                .codex
                .as_ref()
                .map(|c| c.get_env_vars())
                .unwrap_or_default(),
            AIProvider::Custom => self
                .custom
                .as_ref()
                .map(|c| c.get_env_vars())
                .unwrap_or_default(),
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 559-590) ---[0m
    pub async fn is_available(&self) -> bool {
        match self.provider_type {
            AIProvider::ClaudeCode => {
                if let Some(config) = &self.claude_code {
                    config.is_available().await
                } else {
                    false
                }
            }
            AIProvider::Aider => {
                if let Some(config) = &self.aider {
                    config.is_available().await
                } else {
                    false
                }
            }
            AIProvider::Codex => {
                if let Some(config) = &self.codex {
                    config.is_available().await
                } else {
                    false
                }
            }
            AIProvider::Custom => {
                if let Some(config) = &self.custom {
                    config.is_available().await
                } else {
                    false
                }
            }
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:238-256 method validate <-> ./crates/ccswarm/src/providers/mod.rs:324-341 method validate
  Similarity: 88.31%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 238-256) ---[0m
    async fn validate(&self) -> Result<()> {
        // Check if aider is available
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Aider not found in PATH"));
        }

        // Check API keys based on model
        if self.model.starts_with("gpt-") && self.openai_api_key.is_none() {
            return Err(anyhow::anyhow!("OpenAI API key required for GPT models"));
        }

        if self.model.starts_with("claude-") && self.anthropic_api_key.is_none() {
            return Err(anyhow::anyhow!(
                "Anthropic API key required for Claude models"
            ));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 324-341) ---[0m
    async fn validate(&self) -> Result<()> {
        if self.api_key.is_empty() {
            return Err(anyhow::anyhow!("OpenAI API key is required"));
        }

        if self.model.is_empty() {
            return Err(anyhow::anyhow!("Model name cannot be empty"));
        }

        // Validate temperature range
        if let Some(temp) = self.temperature {
            if !(0.0..=1.0).contains(&temp) {
                return Err(anyhow::anyhow!("Temperature must be between 0.0 and 1.0"));
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/providers/mod.rs:165-177 method validate <-> ./crates/ccswarm/src/providers/mod.rs:238-256 method validate
  Similarity: 93.31%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 165-177) ---[0m
    async fn validate(&self) -> Result<()> {
        // Check if claude command is available
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Claude Code CLI not found in PATH"));
        }

        // Validate model name
        if self.model.is_empty() {
            return Err(anyhow::anyhow!("Model name cannot be empty"));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 238-256) ---[0m
    async fn validate(&self) -> Result<()> {
        // Check if aider is available
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Aider not found in PATH"));
        }

        // Check API keys based on model
        if self.model.starts_with("gpt-") && self.openai_api_key.is_none() {
            return Err(anyhow::anyhow!("OpenAI API key required for GPT models"));
        }

        if self.model.starts_with("claude-") && self.anthropic_api_key.is_none() {
            return Err(anyhow::anyhow!(
                "Anthropic API key required for Claude models"
            ));
        }

        Ok(())
    }

  ./crates/ccswarm/src/providers/mod.rs:165-177 method validate <-> ./crates/ccswarm/src/providers/mod.rs:324-341 method validate
  Similarity: 89.22%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 165-177) ---[0m
    async fn validate(&self) -> Result<()> {
        // Check if claude command is available
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Claude Code CLI not found in PATH"));
        }

        // Validate model name
        if self.model.is_empty() {
            return Err(anyhow::anyhow!("Model name cannot be empty"));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 324-341) ---[0m
    async fn validate(&self) -> Result<()> {
        if self.api_key.is_empty() {
            return Err(anyhow::anyhow!("OpenAI API key is required"));
        }

        if self.model.is_empty() {
            return Err(anyhow::anyhow!("Model name cannot be empty"));
        }

        // Validate temperature range
        if let Some(temp) = self.temperature {
            if !(0.0..=1.0).contains(&temp) {
                return Err(anyhow::anyhow!("Temperature must be between 0.0 and 1.0"));
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/providers/mod.rs:324-341 method validate <-> ./crates/ccswarm/src/providers/mod.rs:401-412 method validate
  Similarity: 91.69%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 324-341) ---[0m
    async fn validate(&self) -> Result<()> {
        if self.api_key.is_empty() {
            return Err(anyhow::anyhow!("OpenAI API key is required"));
        }

        if self.model.is_empty() {
            return Err(anyhow::anyhow!("Model name cannot be empty"));
        }

        // Validate temperature range
        if let Some(temp) = self.temperature {
            if !(0.0..=1.0).contains(&temp) {
                return Err(anyhow::anyhow!("Temperature must be between 0.0 and 1.0"));
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 401-412) ---[0m
    async fn validate(&self) -> Result<()> {
        if self.command.is_empty() {
            return Err(anyhow::anyhow!("Command cannot be empty"));
        }

        // Check if command exists
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Command '{}' not found", self.command));
        }

        Ok(())
    }

  ./crates/ccswarm/src/providers/mod.rs:238-256 method validate <-> ./crates/ccswarm/src/providers/mod.rs:343-357 method get_env_vars
  Similarity: 80.12%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 238-256) ---[0m
    async fn validate(&self) -> Result<()> {
        // Check if aider is available
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Aider not found in PATH"));
        }

        // Check API keys based on model
        if self.model.starts_with("gpt-") && self.openai_api_key.is_none() {
            return Err(anyhow::anyhow!("OpenAI API key required for GPT models"));
        }

        if self.model.starts_with("claude-") && self.anthropic_api_key.is_none() {
            return Err(anyhow::anyhow!(
                "Anthropic API key required for Claude models"
            ));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 343-357) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        env_vars.insert("OPENAI_API_KEY".to_string(), self.api_key.clone());

        if let Some(org) = &self.organization {
            env_vars.insert("OPENAI_ORGANIZATION".to_string(), org.clone());
        }

        if let Some(base) = &self.api_base {
            env_vars.insert("OPENAI_API_BASE".to_string(), base.clone());
        }

        env_vars
    }

  ./crates/ccswarm/src/providers/mod.rs:238-256 method validate <-> ./crates/ccswarm/src/providers/mod.rs:401-412 method validate
  Similarity: 87.53%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 238-256) ---[0m
    async fn validate(&self) -> Result<()> {
        // Check if aider is available
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Aider not found in PATH"));
        }

        // Check API keys based on model
        if self.model.starts_with("gpt-") && self.openai_api_key.is_none() {
            return Err(anyhow::anyhow!("OpenAI API key required for GPT models"));
        }

        if self.model.starts_with("claude-") && self.anthropic_api_key.is_none() {
            return Err(anyhow::anyhow!(
                "Anthropic API key required for Claude models"
            ));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 401-412) ---[0m
    async fn validate(&self) -> Result<()> {
        if self.command.is_empty() {
            return Err(anyhow::anyhow!("Command cannot be empty"));
        }

        // Check if command exists
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Command '{}' not found", self.command));
        }

        Ok(())
    }

  ./crates/ccswarm/src/providers/mod.rs:324-341 method validate <-> ./crates/ccswarm/src/providers/mod.rs:343-357 method get_env_vars
  Similarity: 78.83%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 324-341) ---[0m
    async fn validate(&self) -> Result<()> {
        if self.api_key.is_empty() {
            return Err(anyhow::anyhow!("OpenAI API key is required"));
        }

        if self.model.is_empty() {
            return Err(anyhow::anyhow!("Model name cannot be empty"));
        }

        // Validate temperature range
        if let Some(temp) = self.temperature {
            if !(0.0..=1.0).contains(&temp) {
                return Err(anyhow::anyhow!("Temperature must be between 0.0 and 1.0"));
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 343-357) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        env_vars.insert("OPENAI_API_KEY".to_string(), self.api_key.clone());

        if let Some(org) = &self.organization {
            env_vars.insert("OPENAI_ORGANIZATION".to_string(), org.clone());
        }

        if let Some(base) = &self.api_base {
            env_vars.insert("OPENAI_API_BASE".to_string(), base.clone());
        }

        env_vars
    }

  ./crates/ccswarm/src/providers/mod.rs:238-256 method validate <-> ./crates/ccswarm/src/providers/mod.rs:258-270 method get_env_vars
  Similarity: 79.46%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 238-256) ---[0m
    async fn validate(&self) -> Result<()> {
        // Check if aider is available
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Aider not found in PATH"));
        }

        // Check API keys based on model
        if self.model.starts_with("gpt-") && self.openai_api_key.is_none() {
            return Err(anyhow::anyhow!("OpenAI API key required for GPT models"));
        }

        if self.model.starts_with("claude-") && self.anthropic_api_key.is_none() {
            return Err(anyhow::anyhow!(
                "Anthropic API key required for Claude models"
            ));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 258-270) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        if let Some(openai_key) = &self.openai_api_key {
            env_vars.insert("OPENAI_API_KEY".to_string(), openai_key.clone());
        }

        if let Some(anthropic_key) = &self.anthropic_api_key {
            env_vars.insert("ANTHROPIC_API_KEY".to_string(), anthropic_key.clone());
        }

        env_vars
    }

  ./crates/ccswarm/src/providers/mod.rs:258-270 method get_env_vars <-> ./crates/ccswarm/src/providers/mod.rs:343-357 method get_env_vars
  Similarity: 90.34%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 258-270) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        if let Some(openai_key) = &self.openai_api_key {
            env_vars.insert("OPENAI_API_KEY".to_string(), openai_key.clone());
        }

        if let Some(anthropic_key) = &self.anthropic_api_key {
            env_vars.insert("ANTHROPIC_API_KEY".to_string(), anthropic_key.clone());
        }

        env_vars
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 343-357) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        env_vars.insert("OPENAI_API_KEY".to_string(), self.api_key.clone());

        if let Some(org) = &self.organization {
            env_vars.insert("OPENAI_ORGANIZATION".to_string(), org.clone());
        }

        if let Some(base) = &self.api_base {
            env_vars.insert("OPENAI_API_BASE".to_string(), base.clone());
        }

        env_vars
    }

  ./crates/ccswarm/src/providers/mod.rs:258-270 method get_env_vars <-> ./crates/ccswarm/src/providers/mod.rs:324-341 method validate
  Similarity: 78.44%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 258-270) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        if let Some(openai_key) = &self.openai_api_key {
            env_vars.insert("OPENAI_API_KEY".to_string(), openai_key.clone());
        }

        if let Some(anthropic_key) = &self.anthropic_api_key {
            env_vars.insert("ANTHROPIC_API_KEY".to_string(), anthropic_key.clone());
        }

        env_vars
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 324-341) ---[0m
    async fn validate(&self) -> Result<()> {
        if self.api_key.is_empty() {
            return Err(anyhow::anyhow!("OpenAI API key is required"));
        }

        if self.model.is_empty() {
            return Err(anyhow::anyhow!("Model name cannot be empty"));
        }

        // Validate temperature range
        if let Some(temp) = self.temperature {
            if !(0.0..=1.0).contains(&temp) {
                return Err(anyhow::anyhow!("Temperature must be between 0.0 and 1.0"));
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/providers/mod.rs:165-177 method validate <-> ./crates/ccswarm/src/providers/mod.rs:343-357 method get_env_vars
  Similarity: 83.02%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 165-177) ---[0m
    async fn validate(&self) -> Result<()> {
        // Check if claude command is available
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Claude Code CLI not found in PATH"));
        }

        // Validate model name
        if self.model.is_empty() {
            return Err(anyhow::anyhow!("Model name cannot be empty"));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 343-357) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        env_vars.insert("OPENAI_API_KEY".to_string(), self.api_key.clone());

        if let Some(org) = &self.organization {
            env_vars.insert("OPENAI_ORGANIZATION".to_string(), org.clone());
        }

        if let Some(base) = &self.api_base {
            env_vars.insert("OPENAI_API_BASE".to_string(), base.clone());
        }

        env_vars
    }

  ./crates/ccswarm/src/providers/mod.rs:165-177 method validate <-> ./crates/ccswarm/src/providers/mod.rs:401-412 method validate
  Similarity: 86.84%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 165-177) ---[0m
    async fn validate(&self) -> Result<()> {
        // Check if claude command is available
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Claude Code CLI not found in PATH"));
        }

        // Validate model name
        if self.model.is_empty() {
            return Err(anyhow::anyhow!("Model name cannot be empty"));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 401-412) ---[0m
    async fn validate(&self) -> Result<()> {
        if self.command.is_empty() {
            return Err(anyhow::anyhow!("Command cannot be empty"));
        }

        // Check if command exists
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Command '{}' not found", self.command));
        }

        Ok(())
    }

  ./crates/ccswarm/src/providers/mod.rs:179-187 method get_env_vars <-> ./crates/ccswarm/src/providers/mod.rs:258-270 method get_env_vars
  Similarity: 97.24%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 179-187) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        if let Some(api_key) = &self.api_key {
            env_vars.insert("ANTHROPIC_API_KEY".to_string(), api_key.clone());
        }

        env_vars
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 258-270) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        if let Some(openai_key) = &self.openai_api_key {
            env_vars.insert("OPENAI_API_KEY".to_string(), openai_key.clone());
        }

        if let Some(anthropic_key) = &self.anthropic_api_key {
            env_vars.insert("ANTHROPIC_API_KEY".to_string(), anthropic_key.clone());
        }

        env_vars
    }

  ./crates/ccswarm/src/providers/mod.rs:343-357 method get_env_vars <-> ./crates/ccswarm/src/providers/mod.rs:401-412 method validate
  Similarity: 78.52%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 343-357) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        env_vars.insert("OPENAI_API_KEY".to_string(), self.api_key.clone());

        if let Some(org) = &self.organization {
            env_vars.insert("OPENAI_ORGANIZATION".to_string(), org.clone());
        }

        if let Some(base) = &self.api_base {
            env_vars.insert("OPENAI_API_BASE".to_string(), base.clone());
        }

        env_vars
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 401-412) ---[0m
    async fn validate(&self) -> Result<()> {
        if self.command.is_empty() {
            return Err(anyhow::anyhow!("Command cannot be empty"));
        }

        // Check if command exists
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Command '{}' not found", self.command));
        }

        Ok(())
    }

  ./crates/ccswarm/src/providers/mod.rs:165-177 method validate <-> ./crates/ccswarm/src/providers/mod.rs:258-270 method get_env_vars
  Similarity: 80.26%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 165-177) ---[0m
    async fn validate(&self) -> Result<()> {
        // Check if claude command is available
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Claude Code CLI not found in PATH"));
        }

        // Validate model name
        if self.model.is_empty() {
            return Err(anyhow::anyhow!("Model name cannot be empty"));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 258-270) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        if let Some(openai_key) = &self.openai_api_key {
            env_vars.insert("OPENAI_API_KEY".to_string(), openai_key.clone());
        }

        if let Some(anthropic_key) = &self.anthropic_api_key {
            env_vars.insert("ANTHROPIC_API_KEY".to_string(), anthropic_key.clone());
        }

        env_vars
    }

  ./crates/ccswarm/src/providers/mod.rs:179-187 method get_env_vars <-> ./crates/ccswarm/src/providers/mod.rs:276-289 method is_available
  Similarity: 89.00%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 179-187) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        if let Some(api_key) = &self.api_key {
            env_vars.insert("ANTHROPIC_API_KEY".to_string(), api_key.clone());
        }

        env_vars
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 276-289) ---[0m
    async fn is_available(&self) -> bool {
        let cmd = if let Some(path) = &self.executable_path {
            path.to_string_lossy().to_string()
        } else {
            "aider".to_string()
        };

        Command::new(&cmd)
            .arg("--version")
            .output()
            .await
            .map(|output| output.status.success())
            .unwrap_or(false)
    }

  ./crates/ccswarm/src/providers/mod.rs:150-160 method default <-> ./crates/ccswarm/src/providers/mod.rs:223-233 method default
  Similarity: 92.20%
  Classes: Default <-> Default

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 150-160) ---[0m
    fn default() -> Self {
        Self {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: false,
            think_mode: Some("think".to_string()),
            json_output: true,
            custom_commands: Vec::new(),
            mcp_servers: HashMap::new(),
            api_key: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 223-233) ---[0m
    fn default() -> Self {
        Self {
            model: "gpt-4".to_string(),
            openai_api_key: None,
            anthropic_api_key: None,
            auto_commit: true,
            git: true,
            additional_args: Vec::new(),
            executable_path: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:258-270 method get_env_vars <-> ./crates/ccswarm/src/providers/mod.rs:401-412 method validate
  Similarity: 78.10%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 258-270) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        if let Some(openai_key) = &self.openai_api_key {
            env_vars.insert("OPENAI_API_KEY".to_string(), openai_key.clone());
        }

        if let Some(anthropic_key) = &self.anthropic_api_key {
            env_vars.insert("ANTHROPIC_API_KEY".to_string(), anthropic_key.clone());
        }

        env_vars
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 401-412) ---[0m
    async fn validate(&self) -> Result<()> {
        if self.command.is_empty() {
            return Err(anyhow::anyhow!("Command cannot be empty"));
        }

        // Check if command exists
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Command '{}' not found", self.command));
        }

        Ok(())
    }

  ./crates/ccswarm/src/providers/mod.rs:165-177 method validate <-> ./crates/ccswarm/src/providers/mod.rs:276-289 method is_available
  Similarity: 72.08%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 165-177) ---[0m
    async fn validate(&self) -> Result<()> {
        // Check if claude command is available
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Claude Code CLI not found in PATH"));
        }

        // Validate model name
        if self.model.is_empty() {
            return Err(anyhow::anyhow!("Model name cannot be empty"));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 276-289) ---[0m
    async fn is_available(&self) -> bool {
        let cmd = if let Some(path) = &self.executable_path {
            path.to_string_lossy().to_string()
        } else {
            "aider".to_string()
        };

        Command::new(&cmd)
            .arg("--version")
            .output()
            .await
            .map(|output| output.status.success())
            .unwrap_or(false)
    }

  ./crates/ccswarm/src/providers/mod.rs:276-289 method is_available <-> ./crates/ccswarm/src/providers/mod.rs:401-412 method validate
  Similarity: 72.67%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 276-289) ---[0m
    async fn is_available(&self) -> bool {
        let cmd = if let Some(path) = &self.executable_path {
            path.to_string_lossy().to_string()
        } else {
            "aider".to_string()
        };

        Command::new(&cmd)
            .arg("--version")
            .output()
            .await
            .map(|output| output.status.success())
            .unwrap_or(false)
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 401-412) ---[0m
    async fn validate(&self) -> Result<()> {
        if self.command.is_empty() {
            return Err(anyhow::anyhow!("Command cannot be empty"));
        }

        // Check if command exists
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Command '{}' not found", self.command));
        }

        Ok(())
    }

  ./crates/ccswarm/src/providers/mod.rs:238-256 method validate <-> ./crates/ccswarm/src/providers/mod.rs:639-644 function test_ai_provider_display
  Similarity: 73.07%

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 238-256) ---[0m
    async fn validate(&self) -> Result<()> {
        // Check if aider is available
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Aider not found in PATH"));
        }

        // Check API keys based on model
        if self.model.starts_with("gpt-") && self.openai_api_key.is_none() {
            return Err(anyhow::anyhow!("OpenAI API key required for GPT models"));
        }

        if self.model.starts_with("claude-") && self.anthropic_api_key.is_none() {
            return Err(anyhow::anyhow!(
                "Anthropic API key required for Claude models"
            ));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_ai_provider_display (lines 639-644) ---[0m
    fn test_ai_provider_display() {
        assert_eq!(AIProvider::ClaudeCode.display_name(), "Claude Code");
        assert_eq!(AIProvider::Aider.display_name(), "Aider");
        assert_eq!(AIProvider::Codex.display_name(), "OpenAI Codex");
        assert_eq!(AIProvider::Custom.display_name(), "Custom");
    }

  ./crates/ccswarm/src/providers/mod.rs:258-270 method get_env_vars <-> ./crates/ccswarm/src/providers/mod.rs:657-664 function test_claude_code_config_validation
  Similarity: 86.38%

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 258-270) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        if let Some(openai_key) = &self.openai_api_key {
            env_vars.insert("OPENAI_API_KEY".to_string(), openai_key.clone());
        }

        if let Some(anthropic_key) = &self.anthropic_api_key {
            env_vars.insert("ANTHROPIC_API_KEY".to_string(), anthropic_key.clone());
        }

        env_vars
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_claude_code_config_validation (lines 657-664) ---[0m
    async fn test_claude_code_config_validation() {
        let config = ClaudeCodeConfig::default();
        // Test validation logic - result depends on whether claude CLI is installed
        let result = config.validate().await;
        // Just ensure the validation runs without panicking
        // The actual result depends on system state
        let _ = result.is_ok() || result.is_err();
    }

  ./crates/ccswarm/src/providers/mod.rs:223-233 method default <-> ./crates/ccswarm/src/providers/mod.rs:387-396 method default
  Similarity: 85.21%
  Classes: Default <-> Default

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 223-233) ---[0m
    fn default() -> Self {
        Self {
            model: "gpt-4".to_string(),
            openai_api_key: None,
            anthropic_api_key: None,
            auto_commit: true,
            git: true,
            additional_args: Vec::new(),
            executable_path: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 387-396) ---[0m
    fn default() -> Self {
        Self {
            command: "echo".to_string(),
            args: vec!["{prompt}".to_string()],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: Some(300), // 5 minutes
            supports_json: false,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:165-177 method validate <-> ./crates/ccswarm/src/providers/mod.rs:657-664 function test_claude_code_config_validation
  Similarity: 85.05%

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 165-177) ---[0m
    async fn validate(&self) -> Result<()> {
        // Check if claude command is available
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Claude Code CLI not found in PATH"));
        }

        // Validate model name
        if self.model.is_empty() {
            return Err(anyhow::anyhow!("Model name cannot be empty"));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_claude_code_config_validation (lines 657-664) ---[0m
    async fn test_claude_code_config_validation() {
        let config = ClaudeCodeConfig::default();
        // Test validation logic - result depends on whether claude CLI is installed
        let result = config.validate().await;
        // Just ensure the validation runs without panicking
        // The actual result depends on system state
        let _ = result.is_ok() || result.is_err();
    }

  ./crates/ccswarm/src/providers/mod.rs:150-160 method default <-> ./crates/ccswarm/src/providers/mod.rs:387-396 method default
  Similarity: 84.76%
  Classes: Default <-> Default

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 150-160) ---[0m
    fn default() -> Self {
        Self {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: false,
            think_mode: Some("think".to_string()),
            json_output: true,
            custom_commands: Vec::new(),
            mcp_servers: HashMap::new(),
            api_key: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 387-396) ---[0m
    fn default() -> Self {
        Self {
            command: "echo".to_string(),
            args: vec!["{prompt}".to_string()],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: Some(300), // 5 minutes
            supports_json: false,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:223-233 method default <-> ./crates/ccswarm/src/providers/mod.rs:310-319 method default
  Similarity: 84.68%
  Classes: Default <-> Default

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 223-233) ---[0m
    fn default() -> Self {
        Self {
            model: "gpt-4".to_string(),
            openai_api_key: None,
            anthropic_api_key: None,
            auto_commit: true,
            git: true,
            additional_args: Vec::new(),
            executable_path: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 310-319) ---[0m
    fn default() -> Self {
        Self {
            api_key: std::env::var("OPENAI_API_KEY").unwrap_or_default(),
            model: "gpt-4".to_string(), // Codex models are deprecated, using GPT-4
            max_tokens: Some(2048),
            temperature: Some(0.1),
            api_base: None,
            organization: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:150-160 method default <-> ./crates/ccswarm/src/providers/mod.rs:310-319 method default
  Similarity: 84.63%
  Classes: Default <-> Default

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 150-160) ---[0m
    fn default() -> Self {
        Self {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: false,
            think_mode: Some("think".to_string()),
            json_output: true,
            custom_commands: Vec::new(),
            mcp_servers: HashMap::new(),
            api_key: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 310-319) ---[0m
    fn default() -> Self {
        Self {
            api_key: std::env::var("OPENAI_API_KEY").unwrap_or_default(),
            model: "gpt-4".to_string(), // Codex models are deprecated, using GPT-4
            max_tokens: Some(2048),
            temperature: Some(0.1),
            api_base: None,
            organization: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:343-357 method get_env_vars <-> ./crates/ccswarm/src/providers/mod.rs:647-653 function test_provider_configuration_creation
  Similarity: 80.67%

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 343-357) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        env_vars.insert("OPENAI_API_KEY".to_string(), self.api_key.clone());

        if let Some(org) = &self.organization {
            env_vars.insert("OPENAI_ORGANIZATION".to_string(), org.clone());
        }

        if let Some(base) = &self.api_base {
            env_vars.insert("OPENAI_API_BASE".to_string(), base.clone());
        }

        env_vars
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_provider_configuration_creation (lines 647-653) ---[0m
    fn test_provider_configuration_creation() {
        let claude_config = ClaudeCodeConfig::default();
        let provider_config = ProviderConfiguration::claude_code(claude_config);
        assert_eq!(provider_config.provider_type, AIProvider::ClaudeCode);
        assert!(provider_config.claude_code.is_some());
        assert!(provider_config.aider.is_none());
    }

  ./crates/ccswarm/src/providers/mod.rs:442-450 method default <-> ./crates/ccswarm/src/providers/mod.rs:455-463 method claude_code
  Similarity: 97.83%
  Classes: Default <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 442-450) ---[0m
    fn default() -> Self {
        Self {
            provider_type: AIProvider::ClaudeCode,
            claude_code: Some(ClaudeCodeConfig::default()),
            aider: None,
            codex: None,
            custom: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:claude_code (lines 455-463) ---[0m
    pub fn claude_code(config: ClaudeCodeConfig) -> Self {
        Self {
            provider_type: AIProvider::ClaudeCode,
            claude_code: Some(config),
            aider: None,
            codex: None,
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:150-160 method default <-> ./crates/ccswarm/src/providers/mod.rs:193-200 method is_available
  Similarity: 92.32%
  Classes: Default <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 150-160) ---[0m
    fn default() -> Self {
        Self {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: false,
            think_mode: Some("think".to_string()),
            json_output: true,
            custom_commands: Vec::new(),
            mcp_servers: HashMap::new(),
            api_key: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 193-200) ---[0m
    async fn is_available(&self) -> bool {
        Command::new("claude")
            .arg("--version")
            .output()
            .await
            .map(|output| output.status.success())
            .unwrap_or(false)
    }

  ./crates/ccswarm/src/providers/mod.rs:324-341 method validate <-> ./crates/ccswarm/src/providers/mod.rs:639-644 function test_ai_provider_display
  Similarity: 72.21%

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 324-341) ---[0m
    async fn validate(&self) -> Result<()> {
        if self.api_key.is_empty() {
            return Err(anyhow::anyhow!("OpenAI API key is required"));
        }

        if self.model.is_empty() {
            return Err(anyhow::anyhow!("Model name cannot be empty"));
        }

        // Validate temperature range
        if let Some(temp) = self.temperature {
            if !(0.0..=1.0).contains(&temp) {
                return Err(anyhow::anyhow!("Temperature must be between 0.0 and 1.0"));
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_ai_provider_display (lines 639-644) ---[0m
    fn test_ai_provider_display() {
        assert_eq!(AIProvider::ClaudeCode.display_name(), "Claude Code");
        assert_eq!(AIProvider::Aider.display_name(), "Aider");
        assert_eq!(AIProvider::Codex.display_name(), "OpenAI Codex");
        assert_eq!(AIProvider::Custom.display_name(), "Custom");
    }

  ./crates/ccswarm/src/providers/mod.rs:193-200 method is_available <-> ./crates/ccswarm/src/providers/mod.rs:223-233 method default
  Similarity: 90.45%
  Classes: ProviderConfig <-> Default

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 193-200) ---[0m
    async fn is_available(&self) -> bool {
        Command::new("claude")
            .arg("--version")
            .output()
            .await
            .map(|output| output.status.success())
            .unwrap_or(false)
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 223-233) ---[0m
    fn default() -> Self {
        Self {
            model: "gpt-4".to_string(),
            openai_api_key: None,
            anthropic_api_key: None,
            auto_commit: true,
            git: true,
            additional_args: Vec::new(),
            executable_path: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:310-319 method default <-> ./crates/ccswarm/src/providers/mod.rs:455-463 method claude_code
  Similarity: 90.26%
  Classes: Default <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 310-319) ---[0m
    fn default() -> Self {
        Self {
            api_key: std::env::var("OPENAI_API_KEY").unwrap_or_default(),
            model: "gpt-4".to_string(), // Codex models are deprecated, using GPT-4
            max_tokens: Some(2048),
            temperature: Some(0.1),
            api_base: None,
            organization: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:claude_code (lines 455-463) ---[0m
    pub fn claude_code(config: ClaudeCodeConfig) -> Self {
        Self {
            provider_type: AIProvider::ClaudeCode,
            claude_code: Some(config),
            aider: None,
            codex: None,
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:310-319 method default <-> ./crates/ccswarm/src/providers/mod.rs:466-474 method aider
  Similarity: 90.26%
  Classes: Default <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 310-319) ---[0m
    fn default() -> Self {
        Self {
            api_key: std::env::var("OPENAI_API_KEY").unwrap_or_default(),
            model: "gpt-4".to_string(), // Codex models are deprecated, using GPT-4
            max_tokens: Some(2048),
            temperature: Some(0.1),
            api_base: None,
            organization: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:aider (lines 466-474) ---[0m
    pub fn aider(config: AiderConfig) -> Self {
        Self {
            provider_type: AIProvider::Aider,
            claude_code: None,
            aider: Some(config),
            codex: None,
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:442-450 method default <-> ./crates/ccswarm/src/providers/mod.rs:466-474 method aider
  Similarity: 95.00%
  Classes: Default <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 442-450) ---[0m
    fn default() -> Self {
        Self {
            provider_type: AIProvider::ClaudeCode,
            claude_code: Some(ClaudeCodeConfig::default()),
            aider: None,
            codex: None,
            custom: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:aider (lines 466-474) ---[0m
    pub fn aider(config: AiderConfig) -> Self {
        Self {
            provider_type: AIProvider::Aider,
            claude_code: None,
            aider: Some(config),
            codex: None,
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:442-450 method default <-> ./crates/ccswarm/src/providers/mod.rs:477-485 method codex
  Similarity: 95.00%
  Classes: Default <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 442-450) ---[0m
    fn default() -> Self {
        Self {
            provider_type: AIProvider::ClaudeCode,
            claude_code: Some(ClaudeCodeConfig::default()),
            aider: None,
            codex: None,
            custom: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:codex (lines 477-485) ---[0m
    pub fn codex(config: CodexConfig) -> Self {
        Self {
            provider_type: AIProvider::Codex,
            claude_code: None,
            aider: None,
            codex: Some(config),
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:442-450 method default <-> ./crates/ccswarm/src/providers/mod.rs:488-496 method custom
  Similarity: 95.00%
  Classes: Default <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 442-450) ---[0m
    fn default() -> Self {
        Self {
            provider_type: AIProvider::ClaudeCode,
            claude_code: Some(ClaudeCodeConfig::default()),
            aider: None,
            codex: None,
            custom: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:custom (lines 488-496) ---[0m
    pub fn custom(config: CustomConfig) -> Self {
        Self {
            provider_type: AIProvider::Custom,
            claude_code: None,
            aider: None,
            codex: None,
            custom: Some(config),
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:150-160 method default <-> ./crates/ccswarm/src/providers/mod.rs:422-428 method is_available
  Similarity: 94.76%
  Classes: Default <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 150-160) ---[0m
    fn default() -> Self {
        Self {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: false,
            think_mode: Some("think".to_string()),
            json_output: true,
            custom_commands: Vec::new(),
            mcp_servers: HashMap::new(),
            api_key: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 422-428) ---[0m
    async fn is_available(&self) -> bool {
        Command::new(&self.command)
            .arg("--help")
            .output()
            .await
            .is_ok()
    }

  ./crates/ccswarm/src/providers/mod.rs:310-319 method default <-> ./crates/ccswarm/src/providers/mod.rs:442-450 method default
  Similarity: 89.35%
  Classes: Default <-> Default

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 310-319) ---[0m
    fn default() -> Self {
        Self {
            api_key: std::env::var("OPENAI_API_KEY").unwrap_or_default(),
            model: "gpt-4".to_string(), // Codex models are deprecated, using GPT-4
            max_tokens: Some(2048),
            temperature: Some(0.1),
            api_base: None,
            organization: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 442-450) ---[0m
    fn default() -> Self {
        Self {
            provider_type: AIProvider::ClaudeCode,
            claude_code: Some(ClaudeCodeConfig::default()),
            aider: None,
            codex: None,
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:455-463 method claude_code <-> ./crates/ccswarm/src/providers/mod.rs:466-474 method aider
  Similarity: 94.10%
  Classes: ProviderConfiguration <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:claude_code (lines 455-463) ---[0m
    pub fn claude_code(config: ClaudeCodeConfig) -> Self {
        Self {
            provider_type: AIProvider::ClaudeCode,
            claude_code: Some(config),
            aider: None,
            codex: None,
            custom: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:aider (lines 466-474) ---[0m
    pub fn aider(config: AiderConfig) -> Self {
        Self {
            provider_type: AIProvider::Aider,
            claude_code: None,
            aider: Some(config),
            codex: None,
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:455-463 method claude_code <-> ./crates/ccswarm/src/providers/mod.rs:477-485 method codex
  Similarity: 94.10%
  Classes: ProviderConfiguration <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:claude_code (lines 455-463) ---[0m
    pub fn claude_code(config: ClaudeCodeConfig) -> Self {
        Self {
            provider_type: AIProvider::ClaudeCode,
            claude_code: Some(config),
            aider: None,
            codex: None,
            custom: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:codex (lines 477-485) ---[0m
    pub fn codex(config: CodexConfig) -> Self {
        Self {
            provider_type: AIProvider::Codex,
            claude_code: None,
            aider: None,
            codex: Some(config),
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:455-463 method claude_code <-> ./crates/ccswarm/src/providers/mod.rs:488-496 method custom
  Similarity: 94.10%
  Classes: ProviderConfiguration <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:claude_code (lines 455-463) ---[0m
    pub fn claude_code(config: ClaudeCodeConfig) -> Self {
        Self {
            provider_type: AIProvider::ClaudeCode,
            claude_code: Some(config),
            aider: None,
            codex: None,
            custom: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:custom (lines 488-496) ---[0m
    pub fn custom(config: CustomConfig) -> Self {
        Self {
            provider_type: AIProvider::Custom,
            claude_code: None,
            aider: None,
            codex: None,
            custom: Some(config),
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:466-474 method aider <-> ./crates/ccswarm/src/providers/mod.rs:477-485 method codex
  Similarity: 94.10%
  Classes: ProviderConfiguration <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:aider (lines 466-474) ---[0m
    pub fn aider(config: AiderConfig) -> Self {
        Self {
            provider_type: AIProvider::Aider,
            claude_code: None,
            aider: Some(config),
            codex: None,
            custom: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:codex (lines 477-485) ---[0m
    pub fn codex(config: CodexConfig) -> Self {
        Self {
            provider_type: AIProvider::Codex,
            claude_code: None,
            aider: None,
            codex: Some(config),
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:466-474 method aider <-> ./crates/ccswarm/src/providers/mod.rs:488-496 method custom
  Similarity: 94.10%
  Classes: ProviderConfiguration <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:aider (lines 466-474) ---[0m
    pub fn aider(config: AiderConfig) -> Self {
        Self {
            provider_type: AIProvider::Aider,
            claude_code: None,
            aider: Some(config),
            codex: None,
            custom: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:custom (lines 488-496) ---[0m
    pub fn custom(config: CustomConfig) -> Self {
        Self {
            provider_type: AIProvider::Custom,
            claude_code: None,
            aider: None,
            codex: None,
            custom: Some(config),
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:477-485 method codex <-> ./crates/ccswarm/src/providers/mod.rs:488-496 method custom
  Similarity: 94.10%
  Classes: ProviderConfiguration <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:codex (lines 477-485) ---[0m
    pub fn codex(config: CodexConfig) -> Self {
        Self {
            provider_type: AIProvider::Codex,
            claude_code: None,
            aider: None,
            codex: Some(config),
            custom: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:custom (lines 488-496) ---[0m
    pub fn custom(config: CustomConfig) -> Self {
        Self {
            provider_type: AIProvider::Custom,
            claude_code: None,
            aider: None,
            codex: None,
            custom: Some(config),
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:310-319 method default <-> ./crates/ccswarm/src/providers/mod.rs:387-396 method default
  Similarity: 84.55%
  Classes: Default <-> Default

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 310-319) ---[0m
    fn default() -> Self {
        Self {
            api_key: std::env::var("OPENAI_API_KEY").unwrap_or_default(),
            model: "gpt-4".to_string(), // Codex models are deprecated, using GPT-4
            max_tokens: Some(2048),
            temperature: Some(0.1),
            api_base: None,
            organization: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 387-396) ---[0m
    fn default() -> Self {
        Self {
            command: "echo".to_string(),
            args: vec!["{prompt}".to_string()],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: Some(300), // 5 minutes
            supports_json: false,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:150-160 method default <-> ./crates/ccswarm/src/providers/mod.rs:442-450 method default
  Similarity: 84.27%
  Classes: Default <-> Default

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 150-160) ---[0m
    fn default() -> Self {
        Self {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: false,
            think_mode: Some("think".to_string()),
            json_output: true,
            custom_commands: Vec::new(),
            mcp_servers: HashMap::new(),
            api_key: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 442-450) ---[0m
    fn default() -> Self {
        Self {
            provider_type: AIProvider::ClaudeCode,
            claude_code: Some(ClaudeCodeConfig::default()),
            aider: None,
            codex: None,
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:387-396 method default <-> ./crates/ccswarm/src/providers/mod.rs:477-485 method codex
  Similarity: 88.36%
  Classes: Default <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 387-396) ---[0m
    fn default() -> Self {
        Self {
            command: "echo".to_string(),
            args: vec!["{prompt}".to_string()],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: Some(300), // 5 minutes
            supports_json: false,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:codex (lines 477-485) ---[0m
    pub fn codex(config: CodexConfig) -> Self {
        Self {
            provider_type: AIProvider::Codex,
            claude_code: None,
            aider: None,
            codex: Some(config),
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:310-319 method default <-> ./crates/ccswarm/src/providers/mod.rs:477-485 method codex
  Similarity: 88.05%
  Classes: Default <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 310-319) ---[0m
    fn default() -> Self {
        Self {
            api_key: std::env::var("OPENAI_API_KEY").unwrap_or_default(),
            model: "gpt-4".to_string(), // Codex models are deprecated, using GPT-4
            max_tokens: Some(2048),
            temperature: Some(0.1),
            api_base: None,
            organization: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:codex (lines 477-485) ---[0m
    pub fn codex(config: CodexConfig) -> Self {
        Self {
            provider_type: AIProvider::Codex,
            claude_code: None,
            aider: None,
            codex: Some(config),
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:310-319 method default <-> ./crates/ccswarm/src/providers/mod.rs:488-496 method custom
  Similarity: 88.05%
  Classes: Default <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 310-319) ---[0m
    fn default() -> Self {
        Self {
            api_key: std::env::var("OPENAI_API_KEY").unwrap_or_default(),
            model: "gpt-4".to_string(), // Codex models are deprecated, using GPT-4
            max_tokens: Some(2048),
            temperature: Some(0.1),
            api_base: None,
            organization: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:custom (lines 488-496) ---[0m
    pub fn custom(config: CustomConfig) -> Self {
        Self {
            provider_type: AIProvider::Custom,
            claude_code: None,
            aider: None,
            codex: None,
            custom: Some(config),
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:223-233 method default <-> ./crates/ccswarm/src/providers/mod.rs:422-428 method is_available
  Similarity: 92.83%
  Classes: Default <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 223-233) ---[0m
    fn default() -> Self {
        Self {
            model: "gpt-4".to_string(),
            openai_api_key: None,
            anthropic_api_key: None,
            auto_commit: true,
            git: true,
            additional_args: Vec::new(),
            executable_path: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 422-428) ---[0m
    async fn is_available(&self) -> bool {
        Command::new(&self.command)
            .arg("--help")
            .output()
            .await
            .is_ok()
    }

  ./crates/ccswarm/src/providers/mod.rs:193-200 method is_available <-> ./crates/ccswarm/src/providers/mod.rs:310-319 method default
  Similarity: 91.82%
  Classes: ProviderConfig <-> Default

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 193-200) ---[0m
    async fn is_available(&self) -> bool {
        Command::new("claude")
            .arg("--version")
            .output()
            .await
            .map(|output| output.status.success())
            .unwrap_or(false)
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 310-319) ---[0m
    fn default() -> Self {
        Self {
            api_key: std::env::var("OPENAI_API_KEY").unwrap_or_default(),
            model: "gpt-4".to_string(), // Codex models are deprecated, using GPT-4
            max_tokens: Some(2048),
            temperature: Some(0.1),
            api_base: None,
            organization: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:193-200 method is_available <-> ./crates/ccswarm/src/providers/mod.rs:387-396 method default
  Similarity: 91.37%
  Classes: ProviderConfig <-> Default

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 193-200) ---[0m
    async fn is_available(&self) -> bool {
        Command::new("claude")
            .arg("--version")
            .output()
            .await
            .map(|output| output.status.success())
            .unwrap_or(false)
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 387-396) ---[0m
    fn default() -> Self {
        Self {
            command: "echo".to_string(),
            args: vec!["{prompt}".to_string()],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: Some(300), // 5 minutes
            supports_json: false,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:387-396 method default <-> ./crates/ccswarm/src/providers/mod.rs:442-450 method default
  Similarity: 86.44%
  Classes: Default <-> Default

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 387-396) ---[0m
    fn default() -> Self {
        Self {
            command: "echo".to_string(),
            args: vec!["{prompt}".to_string()],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: Some(300), // 5 minutes
            supports_json: false,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 442-450) ---[0m
    fn default() -> Self {
        Self {
            provider_type: AIProvider::ClaudeCode,
            claude_code: Some(ClaudeCodeConfig::default()),
            aider: None,
            codex: None,
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:387-396 method default <-> ./crates/ccswarm/src/providers/mod.rs:455-463 method claude_code
  Similarity: 86.44%
  Classes: Default <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 387-396) ---[0m
    fn default() -> Self {
        Self {
            command: "echo".to_string(),
            args: vec!["{prompt}".to_string()],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: Some(300), // 5 minutes
            supports_json: false,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:claude_code (lines 455-463) ---[0m
    pub fn claude_code(config: ClaudeCodeConfig) -> Self {
        Self {
            provider_type: AIProvider::ClaudeCode,
            claude_code: Some(config),
            aider: None,
            codex: None,
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:165-177 method validate <-> ./crates/ccswarm/src/providers/mod.rs:179-187 method get_env_vars
  Similarity: 74.62%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 165-177) ---[0m
    async fn validate(&self) -> Result<()> {
        // Check if claude command is available
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Claude Code CLI not found in PATH"));
        }

        // Validate model name
        if self.model.is_empty() {
            return Err(anyhow::anyhow!("Model name cannot be empty"));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 179-187) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        if let Some(api_key) = &self.api_key {
            env_vars.insert("ANTHROPIC_API_KEY".to_string(), api_key.clone());
        }

        env_vars
    }

  ./crates/ccswarm/src/providers/mod.rs:387-396 method default <-> ./crates/ccswarm/src/providers/mod.rs:488-496 method custom
  Similarity: 86.16%
  Classes: Default <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 387-396) ---[0m
    fn default() -> Self {
        Self {
            command: "echo".to_string(),
            args: vec!["{prompt}".to_string()],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: Some(300), // 5 minutes
            supports_json: false,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:custom (lines 488-496) ---[0m
    pub fn custom(config: CustomConfig) -> Self {
        Self {
            provider_type: AIProvider::Custom,
            claude_code: None,
            aider: None,
            codex: None,
            custom: Some(config),
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:387-396 method default <-> ./crates/ccswarm/src/providers/mod.rs:466-474 method aider
  Similarity: 86.03%
  Classes: Default <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 387-396) ---[0m
    fn default() -> Self {
        Self {
            command: "echo".to_string(),
            args: vec!["{prompt}".to_string()],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: Some(300), // 5 minutes
            supports_json: false,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:aider (lines 466-474) ---[0m
    pub fn aider(config: AiderConfig) -> Self {
        Self {
            provider_type: AIProvider::Aider,
            claude_code: None,
            aider: Some(config),
            codex: None,
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:223-233 method default <-> ./crates/ccswarm/src/providers/mod.rs:466-474 method aider
  Similarity: 81.33%
  Classes: Default <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 223-233) ---[0m
    fn default() -> Self {
        Self {
            model: "gpt-4".to_string(),
            openai_api_key: None,
            anthropic_api_key: None,
            auto_commit: true,
            git: true,
            additional_args: Vec::new(),
            executable_path: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:aider (lines 466-474) ---[0m
    pub fn aider(config: AiderConfig) -> Self {
        Self {
            provider_type: AIProvider::Aider,
            claude_code: None,
            aider: Some(config),
            codex: None,
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:223-233 method default <-> ./crates/ccswarm/src/providers/mod.rs:477-485 method codex
  Similarity: 81.33%
  Classes: Default <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 223-233) ---[0m
    fn default() -> Self {
        Self {
            model: "gpt-4".to_string(),
            openai_api_key: None,
            anthropic_api_key: None,
            auto_commit: true,
            git: true,
            additional_args: Vec::new(),
            executable_path: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:codex (lines 477-485) ---[0m
    pub fn codex(config: CodexConfig) -> Self {
        Self {
            provider_type: AIProvider::Codex,
            claude_code: None,
            aider: None,
            codex: Some(config),
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:223-233 method default <-> ./crates/ccswarm/src/providers/mod.rs:442-450 method default
  Similarity: 80.83%
  Classes: Default <-> Default

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 223-233) ---[0m
    fn default() -> Self {
        Self {
            model: "gpt-4".to_string(),
            openai_api_key: None,
            anthropic_api_key: None,
            auto_commit: true,
            git: true,
            additional_args: Vec::new(),
            executable_path: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 442-450) ---[0m
    fn default() -> Self {
        Self {
            provider_type: AIProvider::ClaudeCode,
            claude_code: Some(ClaudeCodeConfig::default()),
            aider: None,
            codex: None,
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:223-233 method default <-> ./crates/ccswarm/src/providers/mod.rs:455-463 method claude_code
  Similarity: 80.83%
  Classes: Default <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 223-233) ---[0m
    fn default() -> Self {
        Self {
            model: "gpt-4".to_string(),
            openai_api_key: None,
            anthropic_api_key: None,
            auto_commit: true,
            git: true,
            additional_args: Vec::new(),
            executable_path: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:claude_code (lines 455-463) ---[0m
    pub fn claude_code(config: ClaudeCodeConfig) -> Self {
        Self {
            provider_type: AIProvider::ClaudeCode,
            claude_code: Some(config),
            aider: None,
            codex: None,
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:223-233 method default <-> ./crates/ccswarm/src/providers/mod.rs:488-496 method custom
  Similarity: 80.83%
  Classes: Default <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 223-233) ---[0m
    fn default() -> Self {
        Self {
            model: "gpt-4".to_string(),
            openai_api_key: None,
            anthropic_api_key: None,
            auto_commit: true,
            git: true,
            additional_args: Vec::new(),
            executable_path: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:custom (lines 488-496) ---[0m
    pub fn custom(config: CustomConfig) -> Self {
        Self {
            provider_type: AIProvider::Custom,
            claude_code: None,
            aider: None,
            codex: None,
            custom: Some(config),
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:310-319 method default <-> ./crates/ccswarm/src/providers/mod.rs:422-428 method is_available
  Similarity: 94.42%
  Classes: Default <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 310-319) ---[0m
    fn default() -> Self {
        Self {
            api_key: std::env::var("OPENAI_API_KEY").unwrap_or_default(),
            model: "gpt-4".to_string(), // Codex models are deprecated, using GPT-4
            max_tokens: Some(2048),
            temperature: Some(0.1),
            api_base: None,
            organization: None,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 422-428) ---[0m
    async fn is_available(&self) -> bool {
        Command::new(&self.command)
            .arg("--help")
            .output()
            .await
            .is_ok()
    }

  ./crates/ccswarm/src/providers/mod.rs:387-396 method default <-> ./crates/ccswarm/src/providers/mod.rs:422-428 method is_available
  Similarity: 94.11%
  Classes: Default <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 387-396) ---[0m
    fn default() -> Self {
        Self {
            command: "echo".to_string(),
            args: vec!["{prompt}".to_string()],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: Some(300), // 5 minutes
            supports_json: false,
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 422-428) ---[0m
    async fn is_available(&self) -> bool {
        Command::new(&self.command)
            .arg("--help")
            .output()
            .await
            .is_ok()
    }

  ./crates/ccswarm/src/providers/mod.rs:179-187 method get_env_vars <-> ./crates/ccswarm/src/providers/mod.rs:401-412 method validate
  Similarity: 76.00%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 179-187) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        if let Some(api_key) = &self.api_key {
            env_vars.insert("ANTHROPIC_API_KEY".to_string(), api_key.clone());
        }

        env_vars
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 401-412) ---[0m
    async fn validate(&self) -> Result<()> {
        if self.command.is_empty() {
            return Err(anyhow::anyhow!("Command cannot be empty"));
        }

        // Check if command exists
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Command '{}' not found", self.command));
        }

        Ok(())
    }

  ./crates/ccswarm/src/providers/mod.rs:36-43 method display_name <-> ./crates/ccswarm/src/providers/mod.rs:46-53 method color
  Similarity: 97.86%
  Classes: AIProvider <-> AIProvider

[36m--- ./crates/ccswarm/src/providers/mod.rs:display_name (lines 36-43) ---[0m
    pub fn display_name(&self) -> &'static str {
        match self {
            AIProvider::ClaudeCode => "Claude Code",
            AIProvider::Aider => "Aider",
            AIProvider::Codex => "OpenAI Codex",
            AIProvider::Custom => "Custom",
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:color (lines 46-53) ---[0m
    pub fn color(&self) -> &'static str {
        match self {
            AIProvider::ClaudeCode => "blue",
            AIProvider::Aider => "green",
            AIProvider::Codex => "purple",
            AIProvider::Custom => "gray",
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:36-43 method display_name <-> ./crates/ccswarm/src/providers/mod.rs:56-63 method icon
  Similarity: 97.86%
  Classes: AIProvider <-> AIProvider

[36m--- ./crates/ccswarm/src/providers/mod.rs:display_name (lines 36-43) ---[0m
    pub fn display_name(&self) -> &'static str {
        match self {
            AIProvider::ClaudeCode => "Claude Code",
            AIProvider::Aider => "Aider",
            AIProvider::Codex => "OpenAI Codex",
            AIProvider::Custom => "Custom",
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:icon (lines 56-63) ---[0m
    pub fn icon(&self) -> &'static str {
        match self {
            AIProvider::ClaudeCode => "🤖",
            AIProvider::Aider => "🔧",
            AIProvider::Codex => "🧠",
            AIProvider::Custom => "⚙️",
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:46-53 method color <-> ./crates/ccswarm/src/providers/mod.rs:56-63 method icon
  Similarity: 97.86%
  Classes: AIProvider <-> AIProvider

[36m--- ./crates/ccswarm/src/providers/mod.rs:color (lines 46-53) ---[0m
    pub fn color(&self) -> &'static str {
        match self {
            AIProvider::ClaudeCode => "blue",
            AIProvider::Aider => "green",
            AIProvider::Codex => "purple",
            AIProvider::Custom => "gray",
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:icon (lines 56-63) ---[0m
    pub fn icon(&self) -> &'static str {
        match self {
            AIProvider::ClaudeCode => "🤖",
            AIProvider::Aider => "🔧",
            AIProvider::Codex => "🧠",
            AIProvider::Custom => "⚙️",
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:193-200 method is_available <-> ./crates/ccswarm/src/providers/mod.rs:442-450 method default
  Similarity: 90.45%
  Classes: ProviderConfig <-> Default

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 193-200) ---[0m
    async fn is_available(&self) -> bool {
        Command::new("claude")
            .arg("--version")
            .output()
            .await
            .map(|output| output.status.success())
            .unwrap_or(false)
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 442-450) ---[0m
    fn default() -> Self {
        Self {
            provider_type: AIProvider::ClaudeCode,
            claude_code: Some(ClaudeCodeConfig::default()),
            aider: None,
            codex: None,
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:193-200 method is_available <-> ./crates/ccswarm/src/providers/mod.rs:455-463 method claude_code
  Similarity: 90.45%
  Classes: ProviderConfig <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 193-200) ---[0m
    async fn is_available(&self) -> bool {
        Command::new("claude")
            .arg("--version")
            .output()
            .await
            .map(|output| output.status.success())
            .unwrap_or(false)
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:claude_code (lines 455-463) ---[0m
    pub fn claude_code(config: ClaudeCodeConfig) -> Self {
        Self {
            provider_type: AIProvider::ClaudeCode,
            claude_code: Some(config),
            aider: None,
            codex: None,
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:193-200 method is_available <-> ./crates/ccswarm/src/providers/mod.rs:466-474 method aider
  Similarity: 90.45%
  Classes: ProviderConfig <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 193-200) ---[0m
    async fn is_available(&self) -> bool {
        Command::new("claude")
            .arg("--version")
            .output()
            .await
            .map(|output| output.status.success())
            .unwrap_or(false)
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:aider (lines 466-474) ---[0m
    pub fn aider(config: AiderConfig) -> Self {
        Self {
            provider_type: AIProvider::Aider,
            claude_code: None,
            aider: Some(config),
            codex: None,
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:193-200 method is_available <-> ./crates/ccswarm/src/providers/mod.rs:477-485 method codex
  Similarity: 90.45%
  Classes: ProviderConfig <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 193-200) ---[0m
    async fn is_available(&self) -> bool {
        Command::new("claude")
            .arg("--version")
            .output()
            .await
            .map(|output| output.status.success())
            .unwrap_or(false)
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:codex (lines 477-485) ---[0m
    pub fn codex(config: CodexConfig) -> Self {
        Self {
            provider_type: AIProvider::Codex,
            claude_code: None,
            aider: None,
            codex: Some(config),
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:193-200 method is_available <-> ./crates/ccswarm/src/providers/mod.rs:488-496 method custom
  Similarity: 90.45%
  Classes: ProviderConfig <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 193-200) ---[0m
    async fn is_available(&self) -> bool {
        Command::new("claude")
            .arg("--version")
            .output()
            .await
            .map(|output| output.status.success())
            .unwrap_or(false)
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:custom (lines 488-496) ---[0m
    pub fn custom(config: CustomConfig) -> Self {
        Self {
            provider_type: AIProvider::Custom,
            claude_code: None,
            aider: None,
            codex: None,
            custom: Some(config),
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:258-270 method get_env_vars <-> ./crates/ccswarm/src/providers/mod.rs:667-672 function test_custom_config_default
  Similarity: 79.91%

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 258-270) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        if let Some(openai_key) = &self.openai_api_key {
            env_vars.insert("OPENAI_API_KEY".to_string(), openai_key.clone());
        }

        if let Some(anthropic_key) = &self.anthropic_api_key {
            env_vars.insert("ANTHROPIC_API_KEY".to_string(), anthropic_key.clone());
        }

        env_vars
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_custom_config_default (lines 667-672) ---[0m
    fn test_custom_config_default() {
        let config = CustomConfig::default();
        assert_eq!(config.command, "echo");
        assert_eq!(config.args, vec!["{prompt}"]);
        assert_eq!(config.timeout_seconds, Some(300));
    }

  ./crates/ccswarm/src/providers/mod.rs:401-412 method validate <-> ./crates/ccswarm/src/providers/mod.rs:657-664 function test_claude_code_config_validation
  Similarity: 74.63%

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 401-412) ---[0m
    async fn validate(&self) -> Result<()> {
        if self.command.is_empty() {
            return Err(anyhow::anyhow!("Command cannot be empty"));
        }

        // Check if command exists
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Command '{}' not found", self.command));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_claude_code_config_validation (lines 657-664) ---[0m
    async fn test_claude_code_config_validation() {
        let config = ClaudeCodeConfig::default();
        // Test validation logic - result depends on whether claude CLI is installed
        let result = config.validate().await;
        // Just ensure the validation runs without panicking
        // The actual result depends on system state
        let _ = result.is_ok() || result.is_err();
    }

  ./crates/ccswarm/src/providers/mod.rs:343-357 method get_env_vars <-> ./crates/ccswarm/src/providers/mod.rs:639-644 function test_ai_provider_display
  Similarity: 70.13%

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 343-357) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        env_vars.insert("OPENAI_API_KEY".to_string(), self.api_key.clone());

        if let Some(org) = &self.organization {
            env_vars.insert("OPENAI_ORGANIZATION".to_string(), org.clone());
        }

        if let Some(base) = &self.api_base {
            env_vars.insert("OPENAI_API_BASE".to_string(), base.clone());
        }

        env_vars
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_ai_provider_display (lines 639-644) ---[0m
    fn test_ai_provider_display() {
        assert_eq!(AIProvider::ClaudeCode.display_name(), "Claude Code");
        assert_eq!(AIProvider::Aider.display_name(), "Aider");
        assert_eq!(AIProvider::Codex.display_name(), "OpenAI Codex");
        assert_eq!(AIProvider::Custom.display_name(), "Custom");
    }

  ./crates/ccswarm/src/providers/mod.rs:422-428 method is_available <-> ./crates/ccswarm/src/providers/mod.rs:442-450 method default
  Similarity: 90.65%
  Classes: ProviderConfig <-> Default

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 422-428) ---[0m
    async fn is_available(&self) -> bool {
        Command::new(&self.command)
            .arg("--help")
            .output()
            .await
            .is_ok()
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:default (lines 442-450) ---[0m
    fn default() -> Self {
        Self {
            provider_type: AIProvider::ClaudeCode,
            claude_code: Some(ClaudeCodeConfig::default()),
            aider: None,
            codex: None,
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:422-428 method is_available <-> ./crates/ccswarm/src/providers/mod.rs:455-463 method claude_code
  Similarity: 90.00%
  Classes: ProviderConfig <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 422-428) ---[0m
    async fn is_available(&self) -> bool {
        Command::new(&self.command)
            .arg("--help")
            .output()
            .await
            .is_ok()
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:claude_code (lines 455-463) ---[0m
    pub fn claude_code(config: ClaudeCodeConfig) -> Self {
        Self {
            provider_type: AIProvider::ClaudeCode,
            claude_code: Some(config),
            aider: None,
            codex: None,
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:422-428 method is_available <-> ./crates/ccswarm/src/providers/mod.rs:466-474 method aider
  Similarity: 90.00%
  Classes: ProviderConfig <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 422-428) ---[0m
    async fn is_available(&self) -> bool {
        Command::new(&self.command)
            .arg("--help")
            .output()
            .await
            .is_ok()
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:aider (lines 466-474) ---[0m
    pub fn aider(config: AiderConfig) -> Self {
        Self {
            provider_type: AIProvider::Aider,
            claude_code: None,
            aider: Some(config),
            codex: None,
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:422-428 method is_available <-> ./crates/ccswarm/src/providers/mod.rs:477-485 method codex
  Similarity: 90.00%
  Classes: ProviderConfig <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 422-428) ---[0m
    async fn is_available(&self) -> bool {
        Command::new(&self.command)
            .arg("--help")
            .output()
            .await
            .is_ok()
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:codex (lines 477-485) ---[0m
    pub fn codex(config: CodexConfig) -> Self {
        Self {
            provider_type: AIProvider::Codex,
            claude_code: None,
            aider: None,
            codex: Some(config),
            custom: None,
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:422-428 method is_available <-> ./crates/ccswarm/src/providers/mod.rs:488-496 method custom
  Similarity: 90.00%
  Classes: ProviderConfig <-> ProviderConfiguration

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 422-428) ---[0m
    async fn is_available(&self) -> bool {
        Command::new(&self.command)
            .arg("--help")
            .output()
            .await
            .is_ok()
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:custom (lines 488-496) ---[0m
    pub fn custom(config: CustomConfig) -> Self {
        Self {
            provider_type: AIProvider::Custom,
            claude_code: None,
            aider: None,
            codex: None,
            custom: Some(config),
        }
    }

  ./crates/ccswarm/src/providers/mod.rs:165-177 method validate <-> ./crates/ccswarm/src/providers/mod.rs:647-653 function test_provider_configuration_creation
  Similarity: 71.51%

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 165-177) ---[0m
    async fn validate(&self) -> Result<()> {
        // Check if claude command is available
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Claude Code CLI not found in PATH"));
        }

        // Validate model name
        if self.model.is_empty() {
            return Err(anyhow::anyhow!("Model name cannot be empty"));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_provider_configuration_creation (lines 647-653) ---[0m
    fn test_provider_configuration_creation() {
        let claude_config = ClaudeCodeConfig::default();
        let provider_config = ProviderConfiguration::claude_code(claude_config);
        assert_eq!(provider_config.provider_type, AIProvider::ClaudeCode);
        assert!(provider_config.claude_code.is_some());
        assert!(provider_config.aider.is_none());
    }

  ./crates/ccswarm/src/providers/mod.rs:36-43 method display_name <-> ./crates/ccswarm/src/providers/mod.rs:193-200 method is_available
  Similarity: 88.03%
  Classes: AIProvider <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:display_name (lines 36-43) ---[0m
    pub fn display_name(&self) -> &'static str {
        match self {
            AIProvider::ClaudeCode => "Claude Code",
            AIProvider::Aider => "Aider",
            AIProvider::Codex => "OpenAI Codex",
            AIProvider::Custom => "Custom",
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 193-200) ---[0m
    async fn is_available(&self) -> bool {
        Command::new("claude")
            .arg("--version")
            .output()
            .await
            .map(|output| output.status.success())
            .unwrap_or(false)
    }

  ./crates/ccswarm/src/providers/mod.rs:46-53 method color <-> ./crates/ccswarm/src/providers/mod.rs:193-200 method is_available
  Similarity: 88.03%
  Classes: AIProvider <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:color (lines 46-53) ---[0m
    pub fn color(&self) -> &'static str {
        match self {
            AIProvider::ClaudeCode => "blue",
            AIProvider::Aider => "green",
            AIProvider::Codex => "purple",
            AIProvider::Custom => "gray",
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 193-200) ---[0m
    async fn is_available(&self) -> bool {
        Command::new("claude")
            .arg("--version")
            .output()
            .await
            .map(|output| output.status.success())
            .unwrap_or(false)
    }

  ./crates/ccswarm/src/providers/mod.rs:56-63 method icon <-> ./crates/ccswarm/src/providers/mod.rs:193-200 method is_available
  Similarity: 88.03%
  Classes: AIProvider <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:icon (lines 56-63) ---[0m
    pub fn icon(&self) -> &'static str {
        match self {
            AIProvider::ClaudeCode => "🤖",
            AIProvider::Aider => "🔧",
            AIProvider::Codex => "🧠",
            AIProvider::Custom => "⚙️",
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 193-200) ---[0m
    async fn is_available(&self) -> bool {
        Command::new("claude")
            .arg("--version")
            .output()
            .await
            .map(|output| output.status.success())
            .unwrap_or(false)
    }

  ./crates/ccswarm/src/providers/mod.rs:258-270 method get_env_vars <-> ./crates/ccswarm/src/providers/mod.rs:639-644 function test_ai_provider_display
  Similarity: 71.64%

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 258-270) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        if let Some(openai_key) = &self.openai_api_key {
            env_vars.insert("OPENAI_API_KEY".to_string(), openai_key.clone());
        }

        if let Some(anthropic_key) = &self.anthropic_api_key {
            env_vars.insert("ANTHROPIC_API_KEY".to_string(), anthropic_key.clone());
        }

        env_vars
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_ai_provider_display (lines 639-644) ---[0m
    fn test_ai_provider_display() {
        assert_eq!(AIProvider::ClaudeCode.display_name(), "Claude Code");
        assert_eq!(AIProvider::Aider.display_name(), "Aider");
        assert_eq!(AIProvider::Codex.display_name(), "OpenAI Codex");
        assert_eq!(AIProvider::Custom.display_name(), "Custom");
    }

  ./crates/ccswarm/src/providers/mod.rs:165-177 method validate <-> ./crates/ccswarm/src/providers/mod.rs:667-672 function test_custom_config_default
  Similarity: 71.29%

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 165-177) ---[0m
    async fn validate(&self) -> Result<()> {
        // Check if claude command is available
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Claude Code CLI not found in PATH"));
        }

        // Validate model name
        if self.model.is_empty() {
            return Err(anyhow::anyhow!("Model name cannot be empty"));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_custom_config_default (lines 667-672) ---[0m
    fn test_custom_config_default() {
        let config = CustomConfig::default();
        assert_eq!(config.command, "echo");
        assert_eq!(config.args, vec!["{prompt}"]);
        assert_eq!(config.timeout_seconds, Some(300));
    }

  ./crates/ccswarm/src/providers/mod.rs:36-43 method display_name <-> ./crates/ccswarm/src/providers/mod.rs:422-428 method is_available
  Similarity: 89.46%
  Classes: AIProvider <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:display_name (lines 36-43) ---[0m
    pub fn display_name(&self) -> &'static str {
        match self {
            AIProvider::ClaudeCode => "Claude Code",
            AIProvider::Aider => "Aider",
            AIProvider::Codex => "OpenAI Codex",
            AIProvider::Custom => "Custom",
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 422-428) ---[0m
    async fn is_available(&self) -> bool {
        Command::new(&self.command)
            .arg("--help")
            .output()
            .await
            .is_ok()
    }

  ./crates/ccswarm/src/providers/mod.rs:46-53 method color <-> ./crates/ccswarm/src/providers/mod.rs:422-428 method is_available
  Similarity: 89.46%
  Classes: AIProvider <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:color (lines 46-53) ---[0m
    pub fn color(&self) -> &'static str {
        match self {
            AIProvider::ClaudeCode => "blue",
            AIProvider::Aider => "green",
            AIProvider::Codex => "purple",
            AIProvider::Custom => "gray",
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 422-428) ---[0m
    async fn is_available(&self) -> bool {
        Command::new(&self.command)
            .arg("--help")
            .output()
            .await
            .is_ok()
    }

  ./crates/ccswarm/src/providers/mod.rs:56-63 method icon <-> ./crates/ccswarm/src/providers/mod.rs:422-428 method is_available
  Similarity: 89.46%
  Classes: AIProvider <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:icon (lines 56-63) ---[0m
    pub fn icon(&self) -> &'static str {
        match self {
            AIProvider::ClaudeCode => "🤖",
            AIProvider::Aider => "🔧",
            AIProvider::Codex => "🧠",
            AIProvider::Custom => "⚙️",
        }
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 422-428) ---[0m
    async fn is_available(&self) -> bool {
        Command::new(&self.command)
            .arg("--help")
            .output()
            .await
            .is_ok()
    }

  ./crates/ccswarm/src/providers/mod.rs:401-412 method validate <-> ./crates/ccswarm/src/providers/mod.rs:667-672 function test_custom_config_default
  Similarity: 71.58%

[36m--- ./crates/ccswarm/src/providers/mod.rs:validate (lines 401-412) ---[0m
    async fn validate(&self) -> Result<()> {
        if self.command.is_empty() {
            return Err(anyhow::anyhow!("Command cannot be empty"));
        }

        // Check if command exists
        if !self.is_available().await {
            return Err(anyhow::anyhow!("Command '{}' not found", self.command));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_custom_config_default (lines 667-672) ---[0m
    fn test_custom_config_default() {
        let config = CustomConfig::default();
        assert_eq!(config.command, "echo");
        assert_eq!(config.args, vec!["{prompt}"]);
        assert_eq!(config.timeout_seconds, Some(300));
    }

  ./crates/ccswarm/src/providers/mod.rs:179-187 method get_env_vars <-> ./crates/ccswarm/src/providers/mod.rs:657-664 function test_claude_code_config_validation
  Similarity: 75.00%

[36m--- ./crates/ccswarm/src/providers/mod.rs:get_env_vars (lines 179-187) ---[0m
    fn get_env_vars(&self) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();

        if let Some(api_key) = &self.api_key {
            env_vars.insert("ANTHROPIC_API_KEY".to_string(), api_key.clone());
        }

        env_vars
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_claude_code_config_validation (lines 657-664) ---[0m
    async fn test_claude_code_config_validation() {
        let config = ClaudeCodeConfig::default();
        // Test validation logic - result depends on whether claude CLI is installed
        let result = config.validate().await;
        // Just ensure the validation runs without panicking
        // The actual result depends on system state
        let _ = result.is_ok() || result.is_err();
    }

  ./crates/ccswarm/src/providers/mod.rs:647-653 function test_provider_configuration_creation <-> ./crates/ccswarm/src/providers/mod.rs:657-664 function test_claude_code_config_validation
  Similarity: 78.80%

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_provider_configuration_creation (lines 647-653) ---[0m
    fn test_provider_configuration_creation() {
        let claude_config = ClaudeCodeConfig::default();
        let provider_config = ProviderConfiguration::claude_code(claude_config);
        assert_eq!(provider_config.provider_type, AIProvider::ClaudeCode);
        assert!(provider_config.claude_code.is_some());
        assert!(provider_config.aider.is_none());
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_claude_code_config_validation (lines 657-664) ---[0m
    async fn test_claude_code_config_validation() {
        let config = ClaudeCodeConfig::default();
        // Test validation logic - result depends on whether claude CLI is installed
        let result = config.validate().await;
        // Just ensure the validation runs without panicking
        // The actual result depends on system state
        let _ = result.is_ok() || result.is_err();
    }

  ./crates/ccswarm/src/providers/mod.rs:193-200 method is_available <-> ./crates/ccswarm/src/providers/mod.rs:422-428 method is_available
  Similarity: 78.33%
  Classes: ProviderConfig <-> ProviderConfig

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 193-200) ---[0m
    async fn is_available(&self) -> bool {
        Command::new("claude")
            .arg("--version")
            .output()
            .await
            .map(|output| output.status.success())
            .unwrap_or(false)
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:is_available (lines 422-428) ---[0m
    async fn is_available(&self) -> bool {
        Command::new(&self.command)
            .arg("--help")
            .output()
            .await
            .is_ok()
    }

  ./crates/ccswarm/src/providers/mod.rs:657-664 function test_claude_code_config_validation <-> ./crates/ccswarm/src/providers/mod.rs:667-672 function test_custom_config_default
  Similarity: 75.15%

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_claude_code_config_validation (lines 657-664) ---[0m
    async fn test_claude_code_config_validation() {
        let config = ClaudeCodeConfig::default();
        // Test validation logic - result depends on whether claude CLI is installed
        let result = config.validate().await;
        // Just ensure the validation runs without panicking
        // The actual result depends on system state
        let _ = result.is_ok() || result.is_err();
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_custom_config_default (lines 667-672) ---[0m
    fn test_custom_config_default() {
        let config = CustomConfig::default();
        assert_eq!(config.command, "echo");
        assert_eq!(config.args, vec!["{prompt}"]);
        assert_eq!(config.timeout_seconds, Some(300));
    }

  ./crates/ccswarm/src/providers/mod.rs:639-644 function test_ai_provider_display <-> ./crates/ccswarm/src/providers/mod.rs:657-664 function test_claude_code_config_validation
  Similarity: 74.24%

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_ai_provider_display (lines 639-644) ---[0m
    fn test_ai_provider_display() {
        assert_eq!(AIProvider::ClaudeCode.display_name(), "Claude Code");
        assert_eq!(AIProvider::Aider.display_name(), "Aider");
        assert_eq!(AIProvider::Codex.display_name(), "OpenAI Codex");
        assert_eq!(AIProvider::Custom.display_name(), "Custom");
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_claude_code_config_validation (lines 657-664) ---[0m
    async fn test_claude_code_config_validation() {
        let config = ClaudeCodeConfig::default();
        // Test validation logic - result depends on whether claude CLI is installed
        let result = config.validate().await;
        // Just ensure the validation runs without panicking
        // The actual result depends on system state
        let _ = result.is_ok() || result.is_err();
    }

  ./crates/ccswarm/src/providers/mod.rs:639-644 function test_ai_provider_display <-> ./crates/ccswarm/src/providers/mod.rs:667-672 function test_custom_config_default
  Similarity: 76.00%

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_ai_provider_display (lines 639-644) ---[0m
    fn test_ai_provider_display() {
        assert_eq!(AIProvider::ClaudeCode.display_name(), "Claude Code");
        assert_eq!(AIProvider::Aider.display_name(), "Aider");
        assert_eq!(AIProvider::Codex.display_name(), "OpenAI Codex");
        assert_eq!(AIProvider::Custom.display_name(), "Custom");
    }

[36m--- ./crates/ccswarm/src/providers/mod.rs:test_custom_config_default (lines 667-672) ---[0m
    fn test_custom_config_default() {
        let config = CustomConfig::default();
        assert_eq!(config.command, "echo");
        assert_eq!(config.args, vec!["{prompt}"]);
        assert_eq!(config.timeout_seconds, Some(300));
    }


Duplicates in ./crates/ccswarm/src/subagent/parser.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/subagent/parser.rs:20-41 method parse_content <-> ./crates/ccswarm/src/subagent/parser.rs:68-101 method validate_definition
  Similarity: 83.72%
  Classes: SubagentParser <-> SubagentParser

[36m--- ./crates/ccswarm/src/subagent/parser.rs:parse_content (lines 20-41) ---[0m
    pub fn parse_content(content: &str) -> SubagentResult<(SubagentDefinition, String)> {
        // Split the content into frontmatter and body
        let parts: Vec<&str> = content.splitn(3, "---").collect();

        if parts.len() < 3 {
            return Err(SubagentError::Parse(
                "Invalid subagent file format: missing frontmatter".to_string(),
            ));
        }

        // Parse the YAML frontmatter
        let frontmatter = parts[1].trim();
        let body = parts[2].trim();

        let mut definition: SubagentDefinition = serde_yaml::from_str(frontmatter)
            .map_err(|e| SubagentError::Parse(format!("Failed to parse YAML: {}", e)))?;

        // Validate the definition
        Self::validate_definition(&mut definition)?;

        Ok((definition, body.to_string()))
    }

[36m--- ./crates/ccswarm/src/subagent/parser.rs:validate_definition (lines 68-101) ---[0m
    pub fn validate_definition(definition: &mut SubagentDefinition) -> SubagentResult<()> {
        // Validate that the definition has required fields
        if definition.name.is_empty() {
            return Err(SubagentError::Validation(
                "Subagent name cannot be empty".to_string(),
            ));
        }

        if definition.description.is_empty() {
            return Err(SubagentError::Validation(
                "Subagent description cannot be empty".to_string(),
            ));
        }

        // Validate tools
        if definition.tools.standard.is_empty()
            && definition.tools.semantic.is_empty()
            && definition.tools.memory.is_empty()
            && definition.tools.custom.is_empty()
        {
            return Err(SubagentError::Validation(
                "Subagent must have at least one tool".to_string(),
            ));
        }

        // Validate capabilities
        if definition.capabilities.is_empty() {
            return Err(SubagentError::Validation(
                "Subagent must have at least one capability".to_string(),
            ));
        }

        Ok(())
    }

  ./crates/ccswarm/src/subagent/parser.rs:44-66 method parse_directory <-> ./crates/ccswarm/src/subagent/parser.rs:68-101 method validate_definition
  Similarity: 76.28%
  Classes: SubagentParser <-> SubagentParser

[36m--- ./crates/ccswarm/src/subagent/parser.rs:parse_directory (lines 44-66) ---[0m
    pub fn parse_directory(dir: &Path) -> SubagentResult<Vec<(SubagentDefinition, String)>> {
        if !dir.exists() {
            return Ok(Vec::new());
        }

        let mut agents = Vec::new();

        for entry in fs::read_dir(dir)? {
            let entry = entry?;
            let path = entry.path();

            if path.extension().and_then(|s| s.to_str()) == Some("md") {
                match Self::parse_file(&path) {
                    Ok(agent) => agents.push(agent),
                    Err(e) => {
                        log::warn!("Failed to parse agent file {:?}: {}", path, e);
                    }
                }
            }
        }

        Ok(agents)
    }

[36m--- ./crates/ccswarm/src/subagent/parser.rs:validate_definition (lines 68-101) ---[0m
    pub fn validate_definition(definition: &mut SubagentDefinition) -> SubagentResult<()> {
        // Validate that the definition has required fields
        if definition.name.is_empty() {
            return Err(SubagentError::Validation(
                "Subagent name cannot be empty".to_string(),
            ));
        }

        if definition.description.is_empty() {
            return Err(SubagentError::Validation(
                "Subagent description cannot be empty".to_string(),
            ));
        }

        // Validate tools
        if definition.tools.standard.is_empty()
            && definition.tools.semantic.is_empty()
            && definition.tools.memory.is_empty()
            && definition.tools.custom.is_empty()
        {
            return Err(SubagentError::Validation(
                "Subagent must have at least one tool".to_string(),
            ));
        }

        // Validate capabilities
        if definition.capabilities.is_empty() {
            return Err(SubagentError::Validation(
                "Subagent must have at least one capability".to_string(),
            ));
        }

        Ok(())
    }

  ./crates/ccswarm/src/subagent/parser.rs:20-41 method parse_content <-> ./crates/ccswarm/src/subagent/parser.rs:109-133 function test_parse_valid_content
  Similarity: 79.76%

[36m--- ./crates/ccswarm/src/subagent/parser.rs:parse_content (lines 20-41) ---[0m
    pub fn parse_content(content: &str) -> SubagentResult<(SubagentDefinition, String)> {
        // Split the content into frontmatter and body
        let parts: Vec<&str> = content.splitn(3, "---").collect();

        if parts.len() < 3 {
            return Err(SubagentError::Parse(
                "Invalid subagent file format: missing frontmatter".to_string(),
            ));
        }

        // Parse the YAML frontmatter
        let frontmatter = parts[1].trim();
        let body = parts[2].trim();

        let mut definition: SubagentDefinition = serde_yaml::from_str(frontmatter)
            .map_err(|e| SubagentError::Parse(format!("Failed to parse YAML: {}", e)))?;

        // Validate the definition
        Self::validate_definition(&mut definition)?;

        Ok((definition, body.to_string()))
    }

[36m--- ./crates/ccswarm/src/subagent/parser.rs:test_parse_valid_content (lines 109-133) ---[0m
    fn test_parse_valid_content() {
        let content = r#"---
name: test-agent
description: A test agent
tools:
  standard:
    - read_file
    - write_file
capabilities:
  - Testing
  - Validation
---

# Test Agent

This is the body content."#;

        let result = SubagentParser::parse_content(content);
        assert!(result.is_ok());

        let (definition, body) = result.unwrap();
        assert_eq!(definition.name, "test-agent");
        assert_eq!(definition.tools.standard.len(), 2);
        assert!(body.contains("Test Agent"));
    }

  ./crates/ccswarm/src/subagent/parser.rs:136-140 function test_parse_invalid_frontmatter <-> ./crates/ccswarm/src/subagent/parser.rs:143-156 function test_validate_empty_name
  Similarity: 94.74%

[36m--- ./crates/ccswarm/src/subagent/parser.rs:test_parse_invalid_frontmatter (lines 136-140) ---[0m
    fn test_parse_invalid_frontmatter() {
        let content = "No frontmatter here";
        let result = SubagentParser::parse_content(content);
        assert!(result.is_err());
    }

[36m--- ./crates/ccswarm/src/subagent/parser.rs:test_validate_empty_name (lines 143-156) ---[0m
    fn test_validate_empty_name() {
        let content = r#"---
name: ""
description: Invalid agent
tools:
  standard:
    - read_file
capabilities: []
---
Body"#;

        let result = SubagentParser::parse_content(content);
        assert!(result.is_err());
    }


Duplicates in ./crates/ccswarm/src/cli/semantic_commands.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/cli/semantic_commands.rs:221-295 function refactor_codebase <-> ./crates/ccswarm/src/cli/semantic_commands.rs:350-436 function optimize_cross_codebase
  Similarity: 88.55%

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:refactor_codebase (lines 221-295) ---[0m
async fn refactor_codebase(
    manager: Arc<SemanticManager>,
    auto_apply: bool,
    priority: String,
    max: usize,
) -> Result<()> {
    println!("{}", "🔧 Scanning for refactoring opportunities...".bright_blue().bold());
    
    let mut refactoring_system = AutomaticRefactoringSystem::new(
        manager.analyzer(),
        manager.symbol_index(),
        manager.memory(),
    );
    
    let pb = ProgressBar::new_spinner();
    pb.set_style(
        ProgressStyle::default_spinner()
            .template("{spinner:.blue} {msg}")
            .unwrap()
    );
    pb.set_message("Analyzing code quality...");
    
    let proposals = refactoring_system.scan_codebase().await?;
    pb.finish_with_message("✓ Scan complete");
    
    // Filter by priority
    let priority_filter = parse_priority(&priority);
    let filtered: Vec<_> = proposals.iter()
        .filter(|p| p.priority >= priority_filter)
        .take(max)
        .collect();
    
    if filtered.is_empty() {
        println!("{}", "No refactoring opportunities found at this priority level.".yellow());
        return Ok(());
    }
    
    println!("\n{}", format!("Found {} refactoring opportunities:", filtered.len()).bright_green().bold());
    
    for (i, proposal) in filtered.iter().enumerate() {
        println!("\n{}. {} [{}]", 
            i + 1, 
            proposal.title.bright_white().bold(),
            format!("{:?}", proposal.priority).color(priority_color(&proposal.priority))
        );
        println!("   {}", proposal.description.dimmed());
        println!("   Effort: {:?} | Automated: {}", 
            proposal.estimated_effort,
            if proposal.automated { "✓".green() } else { "✗".red() }
        );
        
        if !proposal.benefits.is_empty() {
            println!("   Benefits:");
            for benefit in &proposal.benefits {
                println!("     • {}", benefit.green());
            }
        }
        
        if auto_apply && proposal.automated {
            print!("   Applying automatically... ");
            refactoring_system.apply_proposal(&proposal.id).await?;
            println!("{}", "✓".bright_green().bold());
        }
    }
    
    if auto_apply {
        let stats = refactoring_system.get_stats();
        println!("\n{}", "📈 Refactoring Statistics".bright_green().bold());
        println!("  Applied: {} proposals", stats.applied_proposals);
        println!("  Time saved: {:.1} hours", stats.time_saved_hours);
        println!("  Lines refactored: {}", stats.lines_refactored);
    }
    
    Ok(())
}

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:optimize_cross_codebase (lines 350-436) ---[0m
async fn optimize_cross_codebase(
    manager: Arc<SemanticManager>,
    repos: Vec<String>,
    detailed: bool,
    output: Option<PathBuf>,
) -> Result<()> {
    println!("{}", "🚀 Cross-codebase optimization analysis...".bright_blue().bold());
    
    let mut optimizer = CrossCodebaseOptimizer::new(manager.memory());
    
    let multi_pb = MultiProgress::new();
    
    // Parse and add repositories
    for repo_spec in repos {
        let parts: Vec<&str> = repo_spec.split(':').collect();
        if parts.len() != 3 {
            eprintln!("Invalid repo format: {} (expected name:path:language)", repo_spec);
            continue;
        }
        
        let name = parts[0].to_string();
        let path = PathBuf::from(parts[1]);
        let language = parse_language(parts[2]);
        
        let pb = multi_pb.add(ProgressBar::new_spinner());
        pb.set_style(
            ProgressStyle::default_spinner()
                .template("{spinner:.blue} Adding repository: {msg}")
                .unwrap()
        );
        pb.set_message(name.clone());
        
        optimizer.add_repository(name, path, language).await?;
        pb.finish_with_message("✓");
    }
    
    // Perform analysis
    let pb = multi_pb.add(ProgressBar::new_spinner());
    pb.set_style(
        ProgressStyle::default_spinner()
            .template("{spinner:.blue} {msg}")
            .unwrap()
    );
    pb.set_message("Performing comprehensive analysis...");
    
    let analysis = optimizer.analyze_all().await?;
    pb.finish_with_message("✓ Analysis complete");
    
    // Generate report
    let report = optimizer.generate_report().await?;
    
    // Save or display report
    if let Some(output_path) = output {
        tokio::fs::write(&output_path, &report).await?;
        println!("{}", format!("Report saved to: {}", output_path.display()).green());
    } else {
        println!("\n{}", report);
    }
    
    if detailed {
        // Show detailed findings
        println!("\n{}", "🔍 Detailed Findings".bright_green().bold());
        
        if !analysis.security_findings.is_empty() {
            println!("\n  Security Issues:");
            for finding in &analysis.security_findings {
                println!("    • [{:?}] {}", finding.severity, finding.description);
            }
        }
        
        if !analysis.performance_bottlenecks.is_empty() {
            println!("\n  Performance Bottlenecks:");
            for bottleneck in &analysis.performance_bottlenecks {
                println!("    • {:?} in {}", 
                    bottleneck.bottleneck_type,
                    bottleneck.location.function_name
                );
            }
        }
        
        println!("\n  Technical Debt:");
        println!("    Total: {:.0} hours", analysis.technical_debt_map.total_debt_hours);
        println!("    Trend: {:?}", analysis.technical_debt_map.debt_trends.trend_direction);
    }
    
    Ok(())
}

  ./crates/ccswarm/src/cli/semantic_commands.rs:165-218 function analyze_codebase <-> ./crates/ccswarm/src/cli/semantic_commands.rs:350-436 function optimize_cross_codebase
  Similarity: 89.29%

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:analyze_codebase (lines 165-218) ---[0m
async fn analyze_codebase(
    manager: Arc<SemanticManager>,
    path: PathBuf,
    format: String,
    show_symbols: bool,
) -> Result<()> {
    println!("{}", "🔍 Analyzing codebase...".bright_blue().bold());
    
    let pb = ProgressBar::new_spinner();
    pb.set_style(
        ProgressStyle::default_spinner()
            .template("{spinner:.blue} {msg}")
            .unwrap()
    );
    pb.set_message("Indexing symbols...");
    
    // Index the codebase
    manager.symbol_index().index_codebase().await?;
    
    pb.set_message("Analyzing code structure...");
    let all_symbols = manager.symbol_index().get_all_symbols().await?;
    
    pb.finish_with_message("✓ Analysis complete");
    
    // Display results based on format
    match format.as_str() {
        "json" => {
            let json = serde_json::to_string_pretty(&all_symbols)?;
            println!("{}", json);
        }
        "markdown" => {
            print_markdown_analysis(&all_symbols, show_symbols);
        }
        _ => {
            print_table_analysis(&all_symbols, show_symbols);
        }
    }
    
    // Show summary statistics
    println!("\n{}", "📊 Summary Statistics".bright_green().bold());
    println!("  Total symbols: {}", all_symbols.len());
    
    let functions = all_symbols.iter()
        .filter(|s| matches!(s.kind, crate::semantic::analyzer::SymbolKind::Function))
        .count();
    let structs = all_symbols.iter()
        .filter(|s| matches!(s.kind, crate::semantic::analyzer::SymbolKind::Struct))
        .count();
    
    println!("  Functions: {}", functions);
    println!("  Structs: {}", structs);
    
    Ok(())
}

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:optimize_cross_codebase (lines 350-436) ---[0m
async fn optimize_cross_codebase(
    manager: Arc<SemanticManager>,
    repos: Vec<String>,
    detailed: bool,
    output: Option<PathBuf>,
) -> Result<()> {
    println!("{}", "🚀 Cross-codebase optimization analysis...".bright_blue().bold());
    
    let mut optimizer = CrossCodebaseOptimizer::new(manager.memory());
    
    let multi_pb = MultiProgress::new();
    
    // Parse and add repositories
    for repo_spec in repos {
        let parts: Vec<&str> = repo_spec.split(':').collect();
        if parts.len() != 3 {
            eprintln!("Invalid repo format: {} (expected name:path:language)", repo_spec);
            continue;
        }
        
        let name = parts[0].to_string();
        let path = PathBuf::from(parts[1]);
        let language = parse_language(parts[2]);
        
        let pb = multi_pb.add(ProgressBar::new_spinner());
        pb.set_style(
            ProgressStyle::default_spinner()
                .template("{spinner:.blue} Adding repository: {msg}")
                .unwrap()
        );
        pb.set_message(name.clone());
        
        optimizer.add_repository(name, path, language).await?;
        pb.finish_with_message("✓");
    }
    
    // Perform analysis
    let pb = multi_pb.add(ProgressBar::new_spinner());
    pb.set_style(
        ProgressStyle::default_spinner()
            .template("{spinner:.blue} {msg}")
            .unwrap()
    );
    pb.set_message("Performing comprehensive analysis...");
    
    let analysis = optimizer.analyze_all().await?;
    pb.finish_with_message("✓ Analysis complete");
    
    // Generate report
    let report = optimizer.generate_report().await?;
    
    // Save or display report
    if let Some(output_path) = output {
        tokio::fs::write(&output_path, &report).await?;
        println!("{}", format!("Report saved to: {}", output_path.display()).green());
    } else {
        println!("\n{}", report);
    }
    
    if detailed {
        // Show detailed findings
        println!("\n{}", "🔍 Detailed Findings".bright_green().bold());
        
        if !analysis.security_findings.is_empty() {
            println!("\n  Security Issues:");
            for finding in &analysis.security_findings {
                println!("    • [{:?}] {}", finding.severity, finding.description);
            }
        }
        
        if !analysis.performance_bottlenecks.is_empty() {
            println!("\n  Performance Bottlenecks:");
            for bottleneck in &analysis.performance_bottlenecks {
                println!("    • {:?} in {}", 
                    bottleneck.bottleneck_type,
                    bottleneck.location.function_name
                );
            }
        }
        
        println!("\n  Technical Debt:");
        println!("    Total: {:.0} hours", analysis.technical_debt_map.total_debt_hours);
        println!("    Trend: {:?}", analysis.technical_debt_map.debt_trends.trend_direction);
    }
    
    Ok(())
}

  ./crates/ccswarm/src/cli/semantic_commands.rs:350-436 function optimize_cross_codebase <-> ./crates/ccswarm/src/cli/semantic_commands.rs:439-527 function handle_voting
  Similarity: 70.10%

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:optimize_cross_codebase (lines 350-436) ---[0m
async fn optimize_cross_codebase(
    manager: Arc<SemanticManager>,
    repos: Vec<String>,
    detailed: bool,
    output: Option<PathBuf>,
) -> Result<()> {
    println!("{}", "🚀 Cross-codebase optimization analysis...".bright_blue().bold());
    
    let mut optimizer = CrossCodebaseOptimizer::new(manager.memory());
    
    let multi_pb = MultiProgress::new();
    
    // Parse and add repositories
    for repo_spec in repos {
        let parts: Vec<&str> = repo_spec.split(':').collect();
        if parts.len() != 3 {
            eprintln!("Invalid repo format: {} (expected name:path:language)", repo_spec);
            continue;
        }
        
        let name = parts[0].to_string();
        let path = PathBuf::from(parts[1]);
        let language = parse_language(parts[2]);
        
        let pb = multi_pb.add(ProgressBar::new_spinner());
        pb.set_style(
            ProgressStyle::default_spinner()
                .template("{spinner:.blue} Adding repository: {msg}")
                .unwrap()
        );
        pb.set_message(name.clone());
        
        optimizer.add_repository(name, path, language).await?;
        pb.finish_with_message("✓");
    }
    
    // Perform analysis
    let pb = multi_pb.add(ProgressBar::new_spinner());
    pb.set_style(
        ProgressStyle::default_spinner()
            .template("{spinner:.blue} {msg}")
            .unwrap()
    );
    pb.set_message("Performing comprehensive analysis...");
    
    let analysis = optimizer.analyze_all().await?;
    pb.finish_with_message("✓ Analysis complete");
    
    // Generate report
    let report = optimizer.generate_report().await?;
    
    // Save or display report
    if let Some(output_path) = output {
        tokio::fs::write(&output_path, &report).await?;
        println!("{}", format!("Report saved to: {}", output_path.display()).green());
    } else {
        println!("\n{}", report);
    }
    
    if detailed {
        // Show detailed findings
        println!("\n{}", "🔍 Detailed Findings".bright_green().bold());
        
        if !analysis.security_findings.is_empty() {
            println!("\n  Security Issues:");
            for finding in &analysis.security_findings {
                println!("    • [{:?}] {}", finding.severity, finding.description);
            }
        }
        
        if !analysis.performance_bottlenecks.is_empty() {
            println!("\n  Performance Bottlenecks:");
            for bottleneck in &analysis.performance_bottlenecks {
                println!("    • {:?} in {}", 
                    bottleneck.bottleneck_type,
                    bottleneck.location.function_name
                );
            }
        }
        
        println!("\n  Technical Debt:");
        println!("    Total: {:.0} hours", analysis.technical_debt_map.total_debt_hours);
        println!("    Trend: {:?}", analysis.technical_debt_map.debt_trends.trend_direction);
    }
    
    Ok(())
}

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:handle_voting (lines 439-527) ---[0m
async fn handle_voting(
    manager: Arc<SemanticManager>,
    create: Option<String>,
    list: bool,
    submit: Option<String>,
    history: bool,
) -> Result<()> {
    let sangha = SanghaSemanticVoting::new(
        manager.analyzer(),
        manager.memory(),
        ConsensusAlgorithm::SimpleMajority,
    );
    
    if let Some(proposal_title) = create {
        println!("{}", "📝 Creating new proposal...".bright_blue().bold());
        
        // For demo, create a simple proposal
        let proposal = sangha.create_proposal(
            proposal_title.clone(),
            "Proposal created via CLI".to_string(),
            ProposalType::ArchitectureChange,
            vec![],
        ).await?;
        
        println!("{}", format!("✓ Proposal created: {}", proposal.id).green());
        println!("  Title: {}", proposal.title);
        println!("  Deadline: {}", proposal.voting_deadline);
        println!("  Quorum: {}", proposal.quorum_required);
    }
    
    if list {
        println!("{}", "📋 Active Proposals".bright_blue().bold());
        let proposals = sangha.get_active_proposals().await?;
        
        if proposals.is_empty() {
            println!("  No active proposals");
        } else {
            for proposal in proposals {
                println!("\n  ID: {}", proposal.id.bright_white().bold());
                println!("  Title: {}", proposal.title);
                println!("  Status: {:?}", proposal.status);
                println!("  Votes: {}", proposal.votes.len());
            }
        }
    }
    
    if let Some(vote_spec) = submit {
        let parts: Vec<&str> = vote_spec.split(':').collect();
        if parts.len() != 3 {
            eprintln!("Invalid vote format (expected proposal_id:decision:reason)");
            return Ok(());
        }
        
        let decision = match parts[1] {
            "approve" => VoteDecision::Approve,
            "reject" => VoteDecision::Reject,
            "abstain" => VoteDecision::Abstain,
            _ => VoteDecision::RequestChanges,
        };
        
        sangha.submit_vote(
            parts[0],
            "cli-user".to_string(),
            AgentRole::Custom("Human".to_string()),
            decision,
            parts[2].to_string(),
        ).await?;
        
        println!("{}", "✓ Vote submitted successfully".green());
    }
    
    if history {
        println!("{}", "📜 Voting History".bright_blue().bold());
        let history = sangha.get_voting_history().await?;
        
        for result in history {
            println!("\n  Proposal: {}", result.proposal_id);
            println!("  Result: {:?} ({}% approval)", 
                result.final_decision,
                result.approval_percentage
            );
            println!("  Consensus: {}", 
                if result.consensus_achieved { "✓".green() } else { "✗".red() }
            );
        }
    }
    
    Ok(())
}

  ./crates/ccswarm/src/cli/semantic_commands.rs:298-347 function generate_agents <-> ./crates/ccswarm/src/cli/semantic_commands.rs:350-436 function optimize_cross_codebase
  Similarity: 86.77%

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:generate_agents (lines 298-347) ---[0m
async fn generate_agents(
    manager: Arc<SemanticManager>,
    force: bool,
    deploy: bool,
) -> Result<()> {
    println!("{}", "🤖 Analyzing project for agent generation...".bright_blue().bold());
    
    let generator = DynamicAgentGenerator::new(
        manager.analyzer(),
        manager.symbol_index(),
        manager.memory(),
    );
    
    let pb = ProgressBar::new_spinner();
    pb.set_style(
        ProgressStyle::default_spinner()
            .template("{spinner:.blue} {msg}")
            .unwrap()
    );
    pb.set_message("Analyzing project characteristics...");
    
    let needs = generator.analyze_agent_needs().await?;
    pb.finish_with_message("✓ Analysis complete");
    
    if needs.is_empty() && !force {
        println!("{}", "All necessary agents already exist.".green());
        return Ok(());
    }
    
    println!("\n{}", format!("Need to generate {} agents:", needs.len()).bright_green().bold());
    
    for request in needs {
        println!("\n  Generating agent for: {}", 
            request.task_context.task.title.bright_white().bold());
        
        let generated = generator.generate_agent(&request).await?;
        
        println!("  ✓ Generated: {}", generated.template.name.green());
        println!("    Role: {:?}", generated.template.role);
        println!("    Capabilities: {}", generated.template.capabilities.len());
        
        if deploy {
            print!("  Deploying agent... ");
            generator.deploy_agent(&generated).await?;
            println!("{}", "✓".bright_green().bold());
        }
    }
    
    Ok(())
}

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:optimize_cross_codebase (lines 350-436) ---[0m
async fn optimize_cross_codebase(
    manager: Arc<SemanticManager>,
    repos: Vec<String>,
    detailed: bool,
    output: Option<PathBuf>,
) -> Result<()> {
    println!("{}", "🚀 Cross-codebase optimization analysis...".bright_blue().bold());
    
    let mut optimizer = CrossCodebaseOptimizer::new(manager.memory());
    
    let multi_pb = MultiProgress::new();
    
    // Parse and add repositories
    for repo_spec in repos {
        let parts: Vec<&str> = repo_spec.split(':').collect();
        if parts.len() != 3 {
            eprintln!("Invalid repo format: {} (expected name:path:language)", repo_spec);
            continue;
        }
        
        let name = parts[0].to_string();
        let path = PathBuf::from(parts[1]);
        let language = parse_language(parts[2]);
        
        let pb = multi_pb.add(ProgressBar::new_spinner());
        pb.set_style(
            ProgressStyle::default_spinner()
                .template("{spinner:.blue} Adding repository: {msg}")
                .unwrap()
        );
        pb.set_message(name.clone());
        
        optimizer.add_repository(name, path, language).await?;
        pb.finish_with_message("✓");
    }
    
    // Perform analysis
    let pb = multi_pb.add(ProgressBar::new_spinner());
    pb.set_style(
        ProgressStyle::default_spinner()
            .template("{spinner:.blue} {msg}")
            .unwrap()
    );
    pb.set_message("Performing comprehensive analysis...");
    
    let analysis = optimizer.analyze_all().await?;
    pb.finish_with_message("✓ Analysis complete");
    
    // Generate report
    let report = optimizer.generate_report().await?;
    
    // Save or display report
    if let Some(output_path) = output {
        tokio::fs::write(&output_path, &report).await?;
        println!("{}", format!("Report saved to: {}", output_path.display()).green());
    } else {
        println!("\n{}", report);
    }
    
    if detailed {
        // Show detailed findings
        println!("\n{}", "🔍 Detailed Findings".bright_green().bold());
        
        if !analysis.security_findings.is_empty() {
            println!("\n  Security Issues:");
            for finding in &analysis.security_findings {
                println!("    • [{:?}] {}", finding.severity, finding.description);
            }
        }
        
        if !analysis.performance_bottlenecks.is_empty() {
            println!("\n  Performance Bottlenecks:");
            for bottleneck in &analysis.performance_bottlenecks {
                println!("    • {:?} in {}", 
                    bottleneck.bottleneck_type,
                    bottleneck.location.function_name
                );
            }
        }
        
        println!("\n  Technical Debt:");
        println!("    Total: {:.0} hours", analysis.technical_debt_map.total_debt_hours);
        println!("    Trend: {:?}", analysis.technical_debt_map.debt_trends.trend_direction);
    }
    
    Ok(())
}

  ./crates/ccswarm/src/cli/semantic_commands.rs:165-218 function analyze_codebase <-> ./crates/ccswarm/src/cli/semantic_commands.rs:221-295 function refactor_codebase
  Similarity: 86.56%

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:analyze_codebase (lines 165-218) ---[0m
async fn analyze_codebase(
    manager: Arc<SemanticManager>,
    path: PathBuf,
    format: String,
    show_symbols: bool,
) -> Result<()> {
    println!("{}", "🔍 Analyzing codebase...".bright_blue().bold());
    
    let pb = ProgressBar::new_spinner();
    pb.set_style(
        ProgressStyle::default_spinner()
            .template("{spinner:.blue} {msg}")
            .unwrap()
    );
    pb.set_message("Indexing symbols...");
    
    // Index the codebase
    manager.symbol_index().index_codebase().await?;
    
    pb.set_message("Analyzing code structure...");
    let all_symbols = manager.symbol_index().get_all_symbols().await?;
    
    pb.finish_with_message("✓ Analysis complete");
    
    // Display results based on format
    match format.as_str() {
        "json" => {
            let json = serde_json::to_string_pretty(&all_symbols)?;
            println!("{}", json);
        }
        "markdown" => {
            print_markdown_analysis(&all_symbols, show_symbols);
        }
        _ => {
            print_table_analysis(&all_symbols, show_symbols);
        }
    }
    
    // Show summary statistics
    println!("\n{}", "📊 Summary Statistics".bright_green().bold());
    println!("  Total symbols: {}", all_symbols.len());
    
    let functions = all_symbols.iter()
        .filter(|s| matches!(s.kind, crate::semantic::analyzer::SymbolKind::Function))
        .count();
    let structs = all_symbols.iter()
        .filter(|s| matches!(s.kind, crate::semantic::analyzer::SymbolKind::Struct))
        .count();
    
    println!("  Functions: {}", functions);
    println!("  Structs: {}", structs);
    
    Ok(())
}

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:refactor_codebase (lines 221-295) ---[0m
async fn refactor_codebase(
    manager: Arc<SemanticManager>,
    auto_apply: bool,
    priority: String,
    max: usize,
) -> Result<()> {
    println!("{}", "🔧 Scanning for refactoring opportunities...".bright_blue().bold());
    
    let mut refactoring_system = AutomaticRefactoringSystem::new(
        manager.analyzer(),
        manager.symbol_index(),
        manager.memory(),
    );
    
    let pb = ProgressBar::new_spinner();
    pb.set_style(
        ProgressStyle::default_spinner()
            .template("{spinner:.blue} {msg}")
            .unwrap()
    );
    pb.set_message("Analyzing code quality...");
    
    let proposals = refactoring_system.scan_codebase().await?;
    pb.finish_with_message("✓ Scan complete");
    
    // Filter by priority
    let priority_filter = parse_priority(&priority);
    let filtered: Vec<_> = proposals.iter()
        .filter(|p| p.priority >= priority_filter)
        .take(max)
        .collect();
    
    if filtered.is_empty() {
        println!("{}", "No refactoring opportunities found at this priority level.".yellow());
        return Ok(());
    }
    
    println!("\n{}", format!("Found {} refactoring opportunities:", filtered.len()).bright_green().bold());
    
    for (i, proposal) in filtered.iter().enumerate() {
        println!("\n{}. {} [{}]", 
            i + 1, 
            proposal.title.bright_white().bold(),
            format!("{:?}", proposal.priority).color(priority_color(&proposal.priority))
        );
        println!("   {}", proposal.description.dimmed());
        println!("   Effort: {:?} | Automated: {}", 
            proposal.estimated_effort,
            if proposal.automated { "✓".green() } else { "✗".red() }
        );
        
        if !proposal.benefits.is_empty() {
            println!("   Benefits:");
            for benefit in &proposal.benefits {
                println!("     • {}", benefit.green());
            }
        }
        
        if auto_apply && proposal.automated {
            print!("   Applying automatically... ");
            refactoring_system.apply_proposal(&proposal.id).await?;
            println!("{}", "✓".bright_green().bold());
        }
    }
    
    if auto_apply {
        let stats = refactoring_system.get_stats();
        println!("\n{}", "📈 Refactoring Statistics".bright_green().bold());
        println!("  Applied: {} proposals", stats.applied_proposals);
        println!("  Time saved: {:.1} hours", stats.time_saved_hours);
        println!("  Lines refactored: {}", stats.lines_refactored);
    }
    
    Ok(())
}

  ./crates/ccswarm/src/cli/semantic_commands.rs:298-347 function generate_agents <-> ./crates/ccswarm/src/cli/semantic_commands.rs:439-527 function handle_voting
  Similarity: 79.28%

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:generate_agents (lines 298-347) ---[0m
async fn generate_agents(
    manager: Arc<SemanticManager>,
    force: bool,
    deploy: bool,
) -> Result<()> {
    println!("{}", "🤖 Analyzing project for agent generation...".bright_blue().bold());
    
    let generator = DynamicAgentGenerator::new(
        manager.analyzer(),
        manager.symbol_index(),
        manager.memory(),
    );
    
    let pb = ProgressBar::new_spinner();
    pb.set_style(
        ProgressStyle::default_spinner()
            .template("{spinner:.blue} {msg}")
            .unwrap()
    );
    pb.set_message("Analyzing project characteristics...");
    
    let needs = generator.analyze_agent_needs().await?;
    pb.finish_with_message("✓ Analysis complete");
    
    if needs.is_empty() && !force {
        println!("{}", "All necessary agents already exist.".green());
        return Ok(());
    }
    
    println!("\n{}", format!("Need to generate {} agents:", needs.len()).bright_green().bold());
    
    for request in needs {
        println!("\n  Generating agent for: {}", 
            request.task_context.task.title.bright_white().bold());
        
        let generated = generator.generate_agent(&request).await?;
        
        println!("  ✓ Generated: {}", generated.template.name.green());
        println!("    Role: {:?}", generated.template.role);
        println!("    Capabilities: {}", generated.template.capabilities.len());
        
        if deploy {
            print!("  Deploying agent... ");
            generator.deploy_agent(&generated).await?;
            println!("{}", "✓".bright_green().bold());
        }
    }
    
    Ok(())
}

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:handle_voting (lines 439-527) ---[0m
async fn handle_voting(
    manager: Arc<SemanticManager>,
    create: Option<String>,
    list: bool,
    submit: Option<String>,
    history: bool,
) -> Result<()> {
    let sangha = SanghaSemanticVoting::new(
        manager.analyzer(),
        manager.memory(),
        ConsensusAlgorithm::SimpleMajority,
    );
    
    if let Some(proposal_title) = create {
        println!("{}", "📝 Creating new proposal...".bright_blue().bold());
        
        // For demo, create a simple proposal
        let proposal = sangha.create_proposal(
            proposal_title.clone(),
            "Proposal created via CLI".to_string(),
            ProposalType::ArchitectureChange,
            vec![],
        ).await?;
        
        println!("{}", format!("✓ Proposal created: {}", proposal.id).green());
        println!("  Title: {}", proposal.title);
        println!("  Deadline: {}", proposal.voting_deadline);
        println!("  Quorum: {}", proposal.quorum_required);
    }
    
    if list {
        println!("{}", "📋 Active Proposals".bright_blue().bold());
        let proposals = sangha.get_active_proposals().await?;
        
        if proposals.is_empty() {
            println!("  No active proposals");
        } else {
            for proposal in proposals {
                println!("\n  ID: {}", proposal.id.bright_white().bold());
                println!("  Title: {}", proposal.title);
                println!("  Status: {:?}", proposal.status);
                println!("  Votes: {}", proposal.votes.len());
            }
        }
    }
    
    if let Some(vote_spec) = submit {
        let parts: Vec<&str> = vote_spec.split(':').collect();
        if parts.len() != 3 {
            eprintln!("Invalid vote format (expected proposal_id:decision:reason)");
            return Ok(());
        }
        
        let decision = match parts[1] {
            "approve" => VoteDecision::Approve,
            "reject" => VoteDecision::Reject,
            "abstain" => VoteDecision::Abstain,
            _ => VoteDecision::RequestChanges,
        };
        
        sangha.submit_vote(
            parts[0],
            "cli-user".to_string(),
            AgentRole::Custom("Human".to_string()),
            decision,
            parts[2].to_string(),
        ).await?;
        
        println!("{}", "✓ Vote submitted successfully".green());
    }
    
    if history {
        println!("{}", "📜 Voting History".bright_blue().bold());
        let history = sangha.get_voting_history().await?;
        
        for result in history {
            println!("\n  Proposal: {}", result.proposal_id);
            println!("  Result: {:?} ({}% approval)", 
                result.final_decision,
                result.approval_percentage
            );
            println!("  Consensus: {}", 
                if result.consensus_achieved { "✓".green() } else { "✗".red() }
            );
        }
    }
    
    Ok(())
}

  ./crates/ccswarm/src/cli/semantic_commands.rs:221-295 function refactor_codebase <-> ./crates/ccswarm/src/cli/semantic_commands.rs:298-347 function generate_agents
  Similarity: 86.10%

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:refactor_codebase (lines 221-295) ---[0m
async fn refactor_codebase(
    manager: Arc<SemanticManager>,
    auto_apply: bool,
    priority: String,
    max: usize,
) -> Result<()> {
    println!("{}", "🔧 Scanning for refactoring opportunities...".bright_blue().bold());
    
    let mut refactoring_system = AutomaticRefactoringSystem::new(
        manager.analyzer(),
        manager.symbol_index(),
        manager.memory(),
    );
    
    let pb = ProgressBar::new_spinner();
    pb.set_style(
        ProgressStyle::default_spinner()
            .template("{spinner:.blue} {msg}")
            .unwrap()
    );
    pb.set_message("Analyzing code quality...");
    
    let proposals = refactoring_system.scan_codebase().await?;
    pb.finish_with_message("✓ Scan complete");
    
    // Filter by priority
    let priority_filter = parse_priority(&priority);
    let filtered: Vec<_> = proposals.iter()
        .filter(|p| p.priority >= priority_filter)
        .take(max)
        .collect();
    
    if filtered.is_empty() {
        println!("{}", "No refactoring opportunities found at this priority level.".yellow());
        return Ok(());
    }
    
    println!("\n{}", format!("Found {} refactoring opportunities:", filtered.len()).bright_green().bold());
    
    for (i, proposal) in filtered.iter().enumerate() {
        println!("\n{}. {} [{}]", 
            i + 1, 
            proposal.title.bright_white().bold(),
            format!("{:?}", proposal.priority).color(priority_color(&proposal.priority))
        );
        println!("   {}", proposal.description.dimmed());
        println!("   Effort: {:?} | Automated: {}", 
            proposal.estimated_effort,
            if proposal.automated { "✓".green() } else { "✗".red() }
        );
        
        if !proposal.benefits.is_empty() {
            println!("   Benefits:");
            for benefit in &proposal.benefits {
                println!("     • {}", benefit.green());
            }
        }
        
        if auto_apply && proposal.automated {
            print!("   Applying automatically... ");
            refactoring_system.apply_proposal(&proposal.id).await?;
            println!("{}", "✓".bright_green().bold());
        }
    }
    
    if auto_apply {
        let stats = refactoring_system.get_stats();
        println!("\n{}", "📈 Refactoring Statistics".bright_green().bold());
        println!("  Applied: {} proposals", stats.applied_proposals);
        println!("  Time saved: {:.1} hours", stats.time_saved_hours);
        println!("  Lines refactored: {}", stats.lines_refactored);
    }
    
    Ok(())
}

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:generate_agents (lines 298-347) ---[0m
async fn generate_agents(
    manager: Arc<SemanticManager>,
    force: bool,
    deploy: bool,
) -> Result<()> {
    println!("{}", "🤖 Analyzing project for agent generation...".bright_blue().bold());
    
    let generator = DynamicAgentGenerator::new(
        manager.analyzer(),
        manager.symbol_index(),
        manager.memory(),
    );
    
    let pb = ProgressBar::new_spinner();
    pb.set_style(
        ProgressStyle::default_spinner()
            .template("{spinner:.blue} {msg}")
            .unwrap()
    );
    pb.set_message("Analyzing project characteristics...");
    
    let needs = generator.analyze_agent_needs().await?;
    pb.finish_with_message("✓ Analysis complete");
    
    if needs.is_empty() && !force {
        println!("{}", "All necessary agents already exist.".green());
        return Ok(());
    }
    
    println!("\n{}", format!("Need to generate {} agents:", needs.len()).bright_green().bold());
    
    for request in needs {
        println!("\n  Generating agent for: {}", 
            request.task_context.task.title.bright_white().bold());
        
        let generated = generator.generate_agent(&request).await?;
        
        println!("  ✓ Generated: {}", generated.template.name.green());
        println!("    Role: {:?}", generated.template.role);
        println!("    Capabilities: {}", generated.template.capabilities.len());
        
        if deploy {
            print!("  Deploying agent... ");
            generator.deploy_agent(&generated).await?;
            println!("{}", "✓".bright_green().bold());
        }
    }
    
    Ok(())
}

  ./crates/ccswarm/src/cli/semantic_commands.rs:165-218 function analyze_codebase <-> ./crates/ccswarm/src/cli/semantic_commands.rs:298-347 function generate_agents
  Similarity: 71.32%

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:analyze_codebase (lines 165-218) ---[0m
async fn analyze_codebase(
    manager: Arc<SemanticManager>,
    path: PathBuf,
    format: String,
    show_symbols: bool,
) -> Result<()> {
    println!("{}", "🔍 Analyzing codebase...".bright_blue().bold());
    
    let pb = ProgressBar::new_spinner();
    pb.set_style(
        ProgressStyle::default_spinner()
            .template("{spinner:.blue} {msg}")
            .unwrap()
    );
    pb.set_message("Indexing symbols...");
    
    // Index the codebase
    manager.symbol_index().index_codebase().await?;
    
    pb.set_message("Analyzing code structure...");
    let all_symbols = manager.symbol_index().get_all_symbols().await?;
    
    pb.finish_with_message("✓ Analysis complete");
    
    // Display results based on format
    match format.as_str() {
        "json" => {
            let json = serde_json::to_string_pretty(&all_symbols)?;
            println!("{}", json);
        }
        "markdown" => {
            print_markdown_analysis(&all_symbols, show_symbols);
        }
        _ => {
            print_table_analysis(&all_symbols, show_symbols);
        }
    }
    
    // Show summary statistics
    println!("\n{}", "📊 Summary Statistics".bright_green().bold());
    println!("  Total symbols: {}", all_symbols.len());
    
    let functions = all_symbols.iter()
        .filter(|s| matches!(s.kind, crate::semantic::analyzer::SymbolKind::Function))
        .count();
    let structs = all_symbols.iter()
        .filter(|s| matches!(s.kind, crate::semantic::analyzer::SymbolKind::Struct))
        .count();
    
    println!("  Functions: {}", functions);
    println!("  Structs: {}", structs);
    
    Ok(())
}

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:generate_agents (lines 298-347) ---[0m
async fn generate_agents(
    manager: Arc<SemanticManager>,
    force: bool,
    deploy: bool,
) -> Result<()> {
    println!("{}", "🤖 Analyzing project for agent generation...".bright_blue().bold());
    
    let generator = DynamicAgentGenerator::new(
        manager.analyzer(),
        manager.symbol_index(),
        manager.memory(),
    );
    
    let pb = ProgressBar::new_spinner();
    pb.set_style(
        ProgressStyle::default_spinner()
            .template("{spinner:.blue} {msg}")
            .unwrap()
    );
    pb.set_message("Analyzing project characteristics...");
    
    let needs = generator.analyze_agent_needs().await?;
    pb.finish_with_message("✓ Analysis complete");
    
    if needs.is_empty() && !force {
        println!("{}", "All necessary agents already exist.".green());
        return Ok(());
    }
    
    println!("\n{}", format!("Need to generate {} agents:", needs.len()).bright_green().bold());
    
    for request in needs {
        println!("\n  Generating agent for: {}", 
            request.task_context.task.title.bright_white().bold());
        
        let generated = generator.generate_agent(&request).await?;
        
        println!("  ✓ Generated: {}", generated.template.name.green());
        println!("    Role: {:?}", generated.template.role);
        println!("    Capabilities: {}", generated.template.capabilities.len());
        
        if deploy {
            print!("  Deploying agent... ");
            generator.deploy_agent(&generated).await?;
            println!("{}", "✓".bright_green().bold());
        }
    }
    
    Ok(())
}

  ./crates/ccswarm/src/cli/semantic_commands.rs:132-162 function execute <-> ./crates/ccswarm/src/cli/semantic_commands.rs:550-587 function monitor_operations
  Similarity: 87.22%

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:execute (lines 132-162) ---[0m
pub async fn execute(cmd: SemanticCommands) -> Result<()> {
    let config = SemanticConfig::default();
    let manager = Arc::new(SemanticManager::new(config).await?);
    manager.initialize().await?;
    
    match cmd.command {
        SemanticSubcommand::Analyze { path, format, symbols } => {
            analyze_codebase(manager, path, format, symbols).await?;
        }
        SemanticSubcommand::Refactor { auto_apply, priority, max } => {
            refactor_codebase(manager, auto_apply, priority, max).await?;
        }
        SemanticSubcommand::GenerateAgents { force, deploy } => {
            generate_agents(manager, force, deploy).await?;
        }
        SemanticSubcommand::Optimize { repos, detailed, output } => {
            optimize_cross_codebase(manager, repos, detailed, output).await?;
        }
        SemanticSubcommand::Vote { create, list, submit, history } => {
            handle_voting(manager, create, list, submit, history).await?;
        }
        SemanticSubcommand::Dashboard { port, realtime } => {
            launch_dashboard(port, realtime).await?;
        }
        SemanticSubcommand::Monitor { interval, filter } => {
            monitor_operations(manager, interval, filter).await?;
        }
    }
    
    Ok(())
}

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:monitor_operations (lines 550-587) ---[0m
async fn monitor_operations(
    manager: Arc<SemanticManager>,
    interval: u64,
    filter: Option<String>,
) -> Result<()> {
    println!("{}", "📊 Monitoring semantic operations...".bright_blue().bold());
    println!("  Refresh interval: {} seconds", interval);
    
    if let Some(f) = &filter {
        println!("  Filter: {}", f);
    }
    
    loop {
        // Clear screen (in real implementation)
        print!("\x1B[2J\x1B[1;1H");
        
        println!("{}", "═══════════════════════════════════════════".bright_blue());
        println!("{}", " SEMANTIC OPERATIONS MONITOR ".bright_white().bold());
        println!("{}", "═══════════════════════════════════════════".bright_blue());
        
        // Get current metrics
        let symbols = manager.symbol_index().get_all_symbols().await?;
        let memories = manager.memory().list_memories().await?;
        
        println!("\n📊 Metrics:");
        println!("  Indexed Symbols: {}", symbols.len());
        println!("  Project Memories: {}", memories.len());
        
        // Show recent operations (simulated)
        println!("\n🔄 Recent Operations:");
        println!("  [{}] Symbol analysis completed", chrono::Local::now().format("%H:%M:%S"));
        println!("  [{}] Memory stored: architecture_decision", chrono::Local::now().format("%H:%M:%S"));
        
        println!("\n  Press Ctrl+C to exit");
        
        sleep(Duration::from_secs(interval)).await;
    }
}

  ./crates/ccswarm/src/cli/semantic_commands.rs:132-162 function execute <-> ./crates/ccswarm/src/cli/semantic_commands.rs:613-642 function print_markdown_analysis
  Similarity: 73.86%

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:execute (lines 132-162) ---[0m
pub async fn execute(cmd: SemanticCommands) -> Result<()> {
    let config = SemanticConfig::default();
    let manager = Arc::new(SemanticManager::new(config).await?);
    manager.initialize().await?;
    
    match cmd.command {
        SemanticSubcommand::Analyze { path, format, symbols } => {
            analyze_codebase(manager, path, format, symbols).await?;
        }
        SemanticSubcommand::Refactor { auto_apply, priority, max } => {
            refactor_codebase(manager, auto_apply, priority, max).await?;
        }
        SemanticSubcommand::GenerateAgents { force, deploy } => {
            generate_agents(manager, force, deploy).await?;
        }
        SemanticSubcommand::Optimize { repos, detailed, output } => {
            optimize_cross_codebase(manager, repos, detailed, output).await?;
        }
        SemanticSubcommand::Vote { create, list, submit, history } => {
            handle_voting(manager, create, list, submit, history).await?;
        }
        SemanticSubcommand::Dashboard { port, realtime } => {
            launch_dashboard(port, realtime).await?;
        }
        SemanticSubcommand::Monitor { interval, filter } => {
            monitor_operations(manager, interval, filter).await?;
        }
    }
    
    Ok(())
}

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:print_markdown_analysis (lines 613-642) ---[0m
fn print_markdown_analysis(symbols: &[crate::semantic::analyzer::Symbol], show_symbols: bool) {
    println!("# Codebase Analysis\n");
    
    println!("## Symbol Summary\n");
    println!("| Type | Count |");
    println!("|------|-------|");
    
    use std::collections::HashMap;
    let mut type_counts = HashMap::new();
    
    for symbol in symbols {
        *type_counts.entry(format!("{:?}", symbol.kind)).or_insert(0) += 1;
    }
    
    for (kind, count) in type_counts {
        println!("| {} | {} |", kind, count);
    }
    
    if show_symbols {
        println!("\n## Symbols\n");
        for symbol in symbols.iter().take(50) {
            println!("- **{}** (`{:?}`) - {}:{}", 
                symbol.name, 
                symbol.kind,
                symbol.file_path,
                symbol.line
            );
        }
    }
}

  ./crates/ccswarm/src/cli/semantic_commands.rs:132-162 function execute <-> ./crates/ccswarm/src/cli/semantic_commands.rs:591-611 function print_table_analysis
  Similarity: 86.61%

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:execute (lines 132-162) ---[0m
pub async fn execute(cmd: SemanticCommands) -> Result<()> {
    let config = SemanticConfig::default();
    let manager = Arc::new(SemanticManager::new(config).await?);
    manager.initialize().await?;
    
    match cmd.command {
        SemanticSubcommand::Analyze { path, format, symbols } => {
            analyze_codebase(manager, path, format, symbols).await?;
        }
        SemanticSubcommand::Refactor { auto_apply, priority, max } => {
            refactor_codebase(manager, auto_apply, priority, max).await?;
        }
        SemanticSubcommand::GenerateAgents { force, deploy } => {
            generate_agents(manager, force, deploy).await?;
        }
        SemanticSubcommand::Optimize { repos, detailed, output } => {
            optimize_cross_codebase(manager, repos, detailed, output).await?;
        }
        SemanticSubcommand::Vote { create, list, submit, history } => {
            handle_voting(manager, create, list, submit, history).await?;
        }
        SemanticSubcommand::Dashboard { port, realtime } => {
            launch_dashboard(port, realtime).await?;
        }
        SemanticSubcommand::Monitor { interval, filter } => {
            monitor_operations(manager, interval, filter).await?;
        }
    }
    
    Ok(())
}

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:print_table_analysis (lines 591-611) ---[0m
fn print_table_analysis(symbols: &[crate::semantic::analyzer::Symbol], show_symbols: bool) {
    use prettytable::{Table, row, cell};
    
    let mut table = Table::new();
    table.add_row(row!["Type", "Name", "File", "Line"]);
    
    for symbol in symbols.iter().take(20) {
        table.add_row(row![
            format!("{:?}", symbol.kind),
            symbol.name,
            symbol.file_path,
            symbol.line
        ]);
    }
    
    table.printstd();
    
    if symbols.len() > 20 {
        println!("... and {} more", symbols.len() - 20);
    }
}

  ./crates/ccswarm/src/cli/semantic_commands.rs:530-547 function launch_dashboard <-> ./crates/ccswarm/src/cli/semantic_commands.rs:613-642 function print_markdown_analysis
  Similarity: 78.17%

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:launch_dashboard (lines 530-547) ---[0m
async fn launch_dashboard(port: u16, realtime: bool) -> Result<()> {
    println!("{}", format!("🌐 Launching semantic dashboard on port {}...", port).bright_blue().bold());
    
    // This would launch a web server with the dashboard
    // For now, we'll simulate it
    println!("  Dashboard URL: http://localhost:{}", port);
    
    if realtime {
        println!("  Real-time updates: {}", "enabled".green());
    }
    
    println!("\n  Press Ctrl+C to stop the dashboard");
    
    // Keep running until interrupted
    loop {
        sleep(Duration::from_secs(1)).await;
    }
}

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:print_markdown_analysis (lines 613-642) ---[0m
fn print_markdown_analysis(symbols: &[crate::semantic::analyzer::Symbol], show_symbols: bool) {
    println!("# Codebase Analysis\n");
    
    println!("## Symbol Summary\n");
    println!("| Type | Count |");
    println!("|------|-------|");
    
    use std::collections::HashMap;
    let mut type_counts = HashMap::new();
    
    for symbol in symbols {
        *type_counts.entry(format!("{:?}", symbol.kind)).or_insert(0) += 1;
    }
    
    for (kind, count) in type_counts {
        println!("| {} | {} |", kind, count);
    }
    
    if show_symbols {
        println!("\n## Symbols\n");
        for symbol in symbols.iter().take(50) {
            println!("- **{}** (`{:?}`) - {}:{}", 
                symbol.name, 
                symbol.kind,
                symbol.file_path,
                symbol.line
            );
        }
    }
}

  ./crates/ccswarm/src/cli/semantic_commands.rs:530-547 function launch_dashboard <-> ./crates/ccswarm/src/cli/semantic_commands.rs:591-611 function print_table_analysis
  Similarity: 81.41%

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:launch_dashboard (lines 530-547) ---[0m
async fn launch_dashboard(port: u16, realtime: bool) -> Result<()> {
    println!("{}", format!("🌐 Launching semantic dashboard on port {}...", port).bright_blue().bold());
    
    // This would launch a web server with the dashboard
    // For now, we'll simulate it
    println!("  Dashboard URL: http://localhost:{}", port);
    
    if realtime {
        println!("  Real-time updates: {}", "enabled".green());
    }
    
    println!("\n  Press Ctrl+C to stop the dashboard");
    
    // Keep running until interrupted
    loop {
        sleep(Duration::from_secs(1)).await;
    }
}

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:print_table_analysis (lines 591-611) ---[0m
fn print_table_analysis(symbols: &[crate::semantic::analyzer::Symbol], show_symbols: bool) {
    use prettytable::{Table, row, cell};
    
    let mut table = Table::new();
    table.add_row(row!["Type", "Name", "File", "Line"]);
    
    for symbol in symbols.iter().take(20) {
        table.add_row(row![
            format!("{:?}", symbol.kind),
            symbol.name,
            symbol.file_path,
            symbol.line
        ]);
    }
    
    table.printstd();
    
    if symbols.len() > 20 {
        println!("... and {} more", symbols.len() - 20);
    }
}

  ./crates/ccswarm/src/cli/semantic_commands.rs:644-654 function parse_priority <-> ./crates/ccswarm/src/cli/semantic_commands.rs:656-665 function priority_color
  Similarity: 86.09%

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:parse_priority (lines 644-654) ---[0m
fn parse_priority(priority: &str) -> crate::semantic::refactoring_system::RefactoringPriority {
    use crate::semantic::refactoring_system::RefactoringPriority;
    
    match priority.to_lowercase().as_str() {
        "low" => RefactoringPriority::Low,
        "medium" => RefactoringPriority::Medium,
        "high" => RefactoringPriority::High,
        "critical" => RefactoringPriority::Critical,
        _ => RefactoringPriority::Medium,
    }
}

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:priority_color (lines 656-665) ---[0m
fn priority_color(priority: &crate::semantic::refactoring_system::RefactoringPriority) -> colored::Color {
    use crate::semantic::refactoring_system::RefactoringPriority;
    
    match priority {
        RefactoringPriority::Low => colored::Color::Blue,
        RefactoringPriority::Medium => colored::Color::Yellow,
        RefactoringPriority::High => colored::Color::BrightRed,
        RefactoringPriority::Critical => colored::Color::Red,
    }
}

  ./crates/ccswarm/src/cli/semantic_commands.rs:644-654 function parse_priority <-> ./crates/ccswarm/src/cli/semantic_commands.rs:667-677 function parse_language
  Similarity: 77.89%

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:parse_priority (lines 644-654) ---[0m
fn parse_priority(priority: &str) -> crate::semantic::refactoring_system::RefactoringPriority {
    use crate::semantic::refactoring_system::RefactoringPriority;
    
    match priority.to_lowercase().as_str() {
        "low" => RefactoringPriority::Low,
        "medium" => RefactoringPriority::Medium,
        "high" => RefactoringPriority::High,
        "critical" => RefactoringPriority::Critical,
        _ => RefactoringPriority::Medium,
    }
}

[36m--- ./crates/ccswarm/src/cli/semantic_commands.rs:parse_language (lines 667-677) ---[0m
fn parse_language(lang: &str) -> ProgrammingLanguage {
    match lang.to_lowercase().as_str() {
        "rust" => ProgrammingLanguage::Rust,
        "typescript" | "ts" => ProgrammingLanguage::TypeScript,
        "javascript" | "js" => ProgrammingLanguage::JavaScript,
        "python" | "py" => ProgrammingLanguage::Python,
        "go" => ProgrammingLanguage::Go,
        "java" => ProgrammingLanguage::Java,
        _ => ProgrammingLanguage::Other(lang.to_string()),
    }
}


Duplicates in ./crates/ccswarm/src/mcp/jsonrpc.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/mcp/jsonrpc.rs:33-40 method response_success <-> ./crates/ccswarm/src/mcp/jsonrpc.rs:42-49 method response_error
  Similarity: 93.75%
  Classes: JsonRpcBuilder <-> JsonRpcBuilder

[36m--- ./crates/ccswarm/src/mcp/jsonrpc.rs:response_success (lines 33-40) ---[0m
    pub fn response_success(self, id: RequestId, result: Value) -> JsonRpcResponse {
        JsonRpcResponse {
            jsonrpc: self.jsonrpc,
            id,
            result: Some(result),
            error: None,
        }
    }

[36m--- ./crates/ccswarm/src/mcp/jsonrpc.rs:response_error (lines 42-49) ---[0m
    pub fn response_error(self, id: RequestId, error: JsonRpcError) -> JsonRpcResponse {
        JsonRpcResponse {
            jsonrpc: self.jsonrpc,
            id,
            result: None,
            error: Some(error),
        }
    }

  ./crates/ccswarm/src/mcp/jsonrpc.rs:33-40 method response_success <-> ./crates/ccswarm/src/mcp/jsonrpc.rs:75-80 method fmt
  Similarity: 74.67%
  Classes: JsonRpcBuilder <-> RequestId

[36m--- ./crates/ccswarm/src/mcp/jsonrpc.rs:response_success (lines 33-40) ---[0m
    pub fn response_success(self, id: RequestId, result: Value) -> JsonRpcResponse {
        JsonRpcResponse {
            jsonrpc: self.jsonrpc,
            id,
            result: Some(result),
            error: None,
        }
    }

[36m--- ./crates/ccswarm/src/mcp/jsonrpc.rs:fmt (lines 75-80) ---[0m
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            RequestId::Number(n) => write!(f, "{}", n),
            RequestId::String(s) => write!(f, "{}", s),
        }
    }

  ./crates/ccswarm/src/mcp/jsonrpc.rs:42-49 method response_error <-> ./crates/ccswarm/src/mcp/jsonrpc.rs:75-80 method fmt
  Similarity: 74.00%
  Classes: JsonRpcBuilder <-> RequestId

[36m--- ./crates/ccswarm/src/mcp/jsonrpc.rs:response_error (lines 42-49) ---[0m
    pub fn response_error(self, id: RequestId, error: JsonRpcError) -> JsonRpcResponse {
        JsonRpcResponse {
            jsonrpc: self.jsonrpc,
            id,
            result: None,
            error: Some(error),
        }
    }

[36m--- ./crates/ccswarm/src/mcp/jsonrpc.rs:fmt (lines 75-80) ---[0m
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            RequestId::Number(n) => write!(f, "{}", n),
            RequestId::String(s) => write!(f, "{}", s),
        }
    }


Duplicates in ./crates/ccswarm/src/session/session_optimization.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/session/session_optimization.rs:238-278 method is_session_reusable <-> ./crates/ccswarm/src/session/session_optimization.rs:415-457 method batch_execute
  Similarity: 89.81%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:is_session_reusable (lines 238-278) ---[0m
    async fn is_session_reusable(&self, session: &OptimizedSession) -> Result<bool> {
        // Validate session is still alive
        let session_guard = session.session.lock().await;
        let is_valid = self
            .session_factory
            .validate_session(session_guard.as_ref())
            .await?;
        drop(session_guard);

        if !is_valid {
            return Ok(false);
        }

        // Apply reuse strategy
        match &self.config.reuse_strategy {
            ReuseStrategy::Aggressive => Ok(true),

            ReuseStrategy::LoadBased { threshold } => {
                let metrics = session.metrics.read().await;
                let current_load = self.calculate_session_load(&metrics);
                Ok(current_load < *threshold)
            }

            ReuseStrategy::TimeBased { max_age } => {
                let age = Utc::now().signed_duration_since(session.metadata.created_at);
                Ok(age.to_std().unwrap_or(Duration::MAX) < *max_age)
            }

            ReuseStrategy::Hybrid {
                load_threshold,
                max_age,
            } => {
                let metrics = session.metrics.read().await;
                let current_load = self.calculate_session_load(&metrics);
                let age = Utc::now().signed_duration_since(session.metadata.created_at);

                Ok(current_load < *load_threshold
                    && age.to_std().unwrap_or(Duration::MAX) < *max_age)
            }
        }
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:batch_execute (lines 415-457) ---[0m
    pub async fn batch_execute<F, T>(
        &self,
        role: &AgentRole,
        operations: Vec<F>,
    ) -> Result<Vec<Result<T>>>
    where
        F: FnOnce(&dyn SimpleSession) -> futures::future::BoxFuture<'_, Result<T>> + Send,
        T: Send,
    {
        if !self.config.performance_settings.batch_operations {
            // Execute individually if batching is disabled
            let mut results = Vec::new();
            for op in operations {
                let session = self.get_or_create_session(role).await?;
                results.push(self.execute_on_session(&session, op).await);
            }
            return Ok(results);
        }

        // Get session for batch
        let session = self.get_or_create_session(role).await?;

        // Execute in batches
        let mut results = Vec::new();
        let batch_size = self.config.performance_settings.batch_size;

        // Convert operations into an iterator to handle chunks properly
        let mut ops_iter = operations.into_iter();

        loop {
            let batch: Vec<_> = ops_iter.by_ref().take(batch_size).collect();
            if batch.is_empty() {
                break;
            }

            for op in batch {
                let session_clone = session.clone();
                results.push(self.execute_on_session(&session_clone, op).await);
            }
        }

        Ok(results)
    }

  ./crates/ccswarm/src/session/session_optimization.rs:294-332 method create_optimized_session <-> ./crates/ccswarm/src/session/session_optimization.rs:415-457 method batch_execute
  Similarity: 91.12%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:create_optimized_session (lines 294-332) ---[0m
    async fn create_optimized_session(&self, role: &AgentRole) -> Result<Arc<OptimizedSession>> {
        // Create underlying session
        let role_clone = role.clone();
        let factory = self.session_factory.clone();
        let base_session = with_retry(
            move || {
                let factory = factory.clone();
                let role = role_clone.clone();
                Box::pin(async move {
                    factory.create_session(&role).await
                        .map_err(|e| crate::error::CCSwarmError::session("unknown", e.to_string()))
                })
            },
            3,
        )
        .await?;

        // Create optimized wrapper
        let session = Arc::new(OptimizedSession {
            id: uuid::Uuid::new_v4().to_string(),
            role: role.clone(),
            session: Arc::new(Mutex::new(base_session)),
            metadata: SessionMetadata {
                created_at: Utc::now(),
                last_used: Utc::now(),
                compression_enabled: self.config.enable_compression,
                compression_ratio: 0.0,
                context_size: 0,
            },
            metrics: Arc::new(RwLock::new(SessionMetrics::default())),
        });

        // Initialize compression if enabled
        if self.config.enable_compression {
            self.initialize_compression(&session).await?;
        }

        Ok(session)
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:batch_execute (lines 415-457) ---[0m
    pub async fn batch_execute<F, T>(
        &self,
        role: &AgentRole,
        operations: Vec<F>,
    ) -> Result<Vec<Result<T>>>
    where
        F: FnOnce(&dyn SimpleSession) -> futures::future::BoxFuture<'_, Result<T>> + Send,
        T: Send,
    {
        if !self.config.performance_settings.batch_operations {
            // Execute individually if batching is disabled
            let mut results = Vec::new();
            for op in operations {
                let session = self.get_or_create_session(role).await?;
                results.push(self.execute_on_session(&session, op).await);
            }
            return Ok(results);
        }

        // Get session for batch
        let session = self.get_or_create_session(role).await?;

        // Execute in batches
        let mut results = Vec::new();
        let batch_size = self.config.performance_settings.batch_size;

        // Convert operations into an iterator to handle chunks properly
        let mut ops_iter = operations.into_iter();

        loop {
            let batch: Vec<_> = ops_iter.by_ref().take(batch_size).collect();
            if batch.is_empty() {
                break;
            }

            for op in batch {
                let session_clone = session.clone();
                results.push(self.execute_on_session(&session_clone, op).await);
            }
        }

        Ok(results)
    }

  ./crates/ccswarm/src/session/session_optimization.rs:238-278 method is_session_reusable <-> ./crates/ccswarm/src/session/session_optimization.rs:294-332 method create_optimized_session
  Similarity: 92.69%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:is_session_reusable (lines 238-278) ---[0m
    async fn is_session_reusable(&self, session: &OptimizedSession) -> Result<bool> {
        // Validate session is still alive
        let session_guard = session.session.lock().await;
        let is_valid = self
            .session_factory
            .validate_session(session_guard.as_ref())
            .await?;
        drop(session_guard);

        if !is_valid {
            return Ok(false);
        }

        // Apply reuse strategy
        match &self.config.reuse_strategy {
            ReuseStrategy::Aggressive => Ok(true),

            ReuseStrategy::LoadBased { threshold } => {
                let metrics = session.metrics.read().await;
                let current_load = self.calculate_session_load(&metrics);
                Ok(current_load < *threshold)
            }

            ReuseStrategy::TimeBased { max_age } => {
                let age = Utc::now().signed_duration_since(session.metadata.created_at);
                Ok(age.to_std().unwrap_or(Duration::MAX) < *max_age)
            }

            ReuseStrategy::Hybrid {
                load_threshold,
                max_age,
            } => {
                let metrics = session.metrics.read().await;
                let current_load = self.calculate_session_load(&metrics);
                let age = Utc::now().signed_duration_since(session.metadata.created_at);

                Ok(current_load < *load_threshold
                    && age.to_std().unwrap_or(Duration::MAX) < *max_age)
            }
        }
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:create_optimized_session (lines 294-332) ---[0m
    async fn create_optimized_session(&self, role: &AgentRole) -> Result<Arc<OptimizedSession>> {
        // Create underlying session
        let role_clone = role.clone();
        let factory = self.session_factory.clone();
        let base_session = with_retry(
            move || {
                let factory = factory.clone();
                let role = role_clone.clone();
                Box::pin(async move {
                    factory.create_session(&role).await
                        .map_err(|e| crate::error::CCSwarmError::session("unknown", e.to_string()))
                })
            },
            3,
        )
        .await?;

        // Create optimized wrapper
        let session = Arc::new(OptimizedSession {
            id: uuid::Uuid::new_v4().to_string(),
            role: role.clone(),
            session: Arc::new(Mutex::new(base_session)),
            metadata: SessionMetadata {
                created_at: Utc::now(),
                last_used: Utc::now(),
                compression_enabled: self.config.enable_compression,
                compression_ratio: 0.0,
                context_size: 0,
            },
            metrics: Arc::new(RwLock::new(SessionMetrics::default())),
        });

        // Initialize compression if enabled
        if self.config.enable_compression {
            self.initialize_compression(&session).await?;
        }

        Ok(session)
    }

  ./crates/ccswarm/src/session/session_optimization.rs:415-457 method batch_execute <-> ./crates/ccswarm/src/session/session_optimization.rs:562-610 function test_session_optimization
  Similarity: 76.30%

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:batch_execute (lines 415-457) ---[0m
    pub async fn batch_execute<F, T>(
        &self,
        role: &AgentRole,
        operations: Vec<F>,
    ) -> Result<Vec<Result<T>>>
    where
        F: FnOnce(&dyn SimpleSession) -> futures::future::BoxFuture<'_, Result<T>> + Send,
        T: Send,
    {
        if !self.config.performance_settings.batch_operations {
            // Execute individually if batching is disabled
            let mut results = Vec::new();
            for op in operations {
                let session = self.get_or_create_session(role).await?;
                results.push(self.execute_on_session(&session, op).await);
            }
            return Ok(results);
        }

        // Get session for batch
        let session = self.get_or_create_session(role).await?;

        // Execute in batches
        let mut results = Vec::new();
        let batch_size = self.config.performance_settings.batch_size;

        // Convert operations into an iterator to handle chunks properly
        let mut ops_iter = operations.into_iter();

        loop {
            let batch: Vec<_> = ops_iter.by_ref().take(batch_size).collect();
            if batch.is_empty() {
                break;
            }

            for op in batch {
                let session_clone = session.clone();
                results.push(self.execute_on_session(&session_clone, op).await);
            }
        }

        Ok(results)
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:test_session_optimization (lines 562-610) ---[0m
    async fn test_session_optimization() {
        struct MockSessionFactory;

        #[async_trait]
        impl SessionFactory for MockSessionFactory {
            async fn create_session(
                &self,
                _role: &AgentRole,
            ) -> Result<Box<dyn SimpleSession + Send>> {
                // Return mock session
                Ok(Box::new(MockSession))
            }

            async fn validate_session(&self, _session: &dyn SimpleSession) -> Result<bool> {
                Ok(true)
            }
        }

        struct MockSession;

        impl SimpleSession for MockSession {
            fn get_stats(&self) -> SessionStats {
                SessionStats::default()
            }

            fn is_alive(&self) -> bool {
                true
            }
        }

        let config = OptimizedSessionConfig::default();
        let factory = Arc::new(MockSessionFactory);
        let manager = OptimizedSessionManager::new(config, factory);

        let role = default_frontend_role();

        // First call should create
        let session1 = manager.get_or_create_session(&role).await.unwrap();

        // Second call should reuse
        let session2 = manager.get_or_create_session(&role).await.unwrap();

        // Sessions should have same ID if reused properly
        assert_eq!(session1.id, session2.id);

        // Check statistics
        let stats = manager.get_statistics().await;
        assert_eq!(stats.total_sessions, 1);
    }

  ./crates/ccswarm/src/session/session_optimization.rs:294-332 method create_optimized_session <-> ./crates/ccswarm/src/session/session_optimization.rs:562-610 function test_session_optimization
  Similarity: 78.47%

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:create_optimized_session (lines 294-332) ---[0m
    async fn create_optimized_session(&self, role: &AgentRole) -> Result<Arc<OptimizedSession>> {
        // Create underlying session
        let role_clone = role.clone();
        let factory = self.session_factory.clone();
        let base_session = with_retry(
            move || {
                let factory = factory.clone();
                let role = role_clone.clone();
                Box::pin(async move {
                    factory.create_session(&role).await
                        .map_err(|e| crate::error::CCSwarmError::session("unknown", e.to_string()))
                })
            },
            3,
        )
        .await?;

        // Create optimized wrapper
        let session = Arc::new(OptimizedSession {
            id: uuid::Uuid::new_v4().to_string(),
            role: role.clone(),
            session: Arc::new(Mutex::new(base_session)),
            metadata: SessionMetadata {
                created_at: Utc::now(),
                last_used: Utc::now(),
                compression_enabled: self.config.enable_compression,
                compression_ratio: 0.0,
                context_size: 0,
            },
            metrics: Arc::new(RwLock::new(SessionMetrics::default())),
        });

        // Initialize compression if enabled
        if self.config.enable_compression {
            self.initialize_compression(&session).await?;
        }

        Ok(session)
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:test_session_optimization (lines 562-610) ---[0m
    async fn test_session_optimization() {
        struct MockSessionFactory;

        #[async_trait]
        impl SessionFactory for MockSessionFactory {
            async fn create_session(
                &self,
                _role: &AgentRole,
            ) -> Result<Box<dyn SimpleSession + Send>> {
                // Return mock session
                Ok(Box::new(MockSession))
            }

            async fn validate_session(&self, _session: &dyn SimpleSession) -> Result<bool> {
                Ok(true)
            }
        }

        struct MockSession;

        impl SimpleSession for MockSession {
            fn get_stats(&self) -> SessionStats {
                SessionStats::default()
            }

            fn is_alive(&self) -> bool {
                true
            }
        }

        let config = OptimizedSessionConfig::default();
        let factory = Arc::new(MockSessionFactory);
        let manager = OptimizedSessionManager::new(config, factory);

        let role = default_frontend_role();

        // First call should create
        let session1 = manager.get_or_create_session(&role).await.unwrap();

        // Second call should reuse
        let session2 = manager.get_or_create_session(&role).await.unwrap();

        // Sessions should have same ID if reused properly
        assert_eq!(session1.id, session2.id);

        // Check statistics
        let stats = manager.get_statistics().await;
        assert_eq!(stats.total_sessions, 1);
    }

  ./crates/ccswarm/src/session/session_optimization.rs:380-412 method execute_on_session <-> ./crates/ccswarm/src/session/session_optimization.rs:562-610 function test_session_optimization
  Similarity: 83.61%

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:execute_on_session (lines 380-412) ---[0m
    pub async fn execute_on_session<F, T>(
        &self,
        session: &OptimizedSession,
        operation: F,
    ) -> Result<T>
    where
        F: FnOnce(&dyn SimpleSession) -> futures::future::BoxFuture<'_, Result<T>> + Send,
        T: Send,
    {
        let start = Instant::now();

        // Lock session
        let session_guard = session.session.lock().await;

        // Execute operation
        let result = operation(session_guard.as_ref()).await;

        // Update metrics
        let execution_time = start.elapsed();
        let mut metrics = session.metrics.write().await;
        metrics.total_operations += 1;

        match &result {
            Ok(_) => metrics.successful_operations += 1,
            Err(_) => metrics.failed_operations += 1,
        }

        metrics.total_execution_time += execution_time;
        metrics.average_response_time =
            metrics.total_execution_time / metrics.total_operations as u32;

        result
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:test_session_optimization (lines 562-610) ---[0m
    async fn test_session_optimization() {
        struct MockSessionFactory;

        #[async_trait]
        impl SessionFactory for MockSessionFactory {
            async fn create_session(
                &self,
                _role: &AgentRole,
            ) -> Result<Box<dyn SimpleSession + Send>> {
                // Return mock session
                Ok(Box::new(MockSession))
            }

            async fn validate_session(&self, _session: &dyn SimpleSession) -> Result<bool> {
                Ok(true)
            }
        }

        struct MockSession;

        impl SimpleSession for MockSession {
            fn get_stats(&self) -> SessionStats {
                SessionStats::default()
            }

            fn is_alive(&self) -> bool {
                true
            }
        }

        let config = OptimizedSessionConfig::default();
        let factory = Arc::new(MockSessionFactory);
        let manager = OptimizedSessionManager::new(config, factory);

        let role = default_frontend_role();

        // First call should create
        let session1 = manager.get_or_create_session(&role).await.unwrap();

        // Second call should reuse
        let session2 = manager.get_or_create_session(&role).await.unwrap();

        // Sessions should have same ID if reused properly
        assert_eq!(session1.id, session2.id);

        // Check statistics
        let stats = manager.get_statistics().await;
        assert_eq!(stats.total_sessions, 1);
    }

  ./crates/ccswarm/src/session/session_optimization.rs:294-332 method create_optimized_session <-> ./crates/ccswarm/src/session/session_optimization.rs:380-412 method execute_on_session
  Similarity: 89.63%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:create_optimized_session (lines 294-332) ---[0m
    async fn create_optimized_session(&self, role: &AgentRole) -> Result<Arc<OptimizedSession>> {
        // Create underlying session
        let role_clone = role.clone();
        let factory = self.session_factory.clone();
        let base_session = with_retry(
            move || {
                let factory = factory.clone();
                let role = role_clone.clone();
                Box::pin(async move {
                    factory.create_session(&role).await
                        .map_err(|e| crate::error::CCSwarmError::session("unknown", e.to_string()))
                })
            },
            3,
        )
        .await?;

        // Create optimized wrapper
        let session = Arc::new(OptimizedSession {
            id: uuid::Uuid::new_v4().to_string(),
            role: role.clone(),
            session: Arc::new(Mutex::new(base_session)),
            metadata: SessionMetadata {
                created_at: Utc::now(),
                last_used: Utc::now(),
                compression_enabled: self.config.enable_compression,
                compression_ratio: 0.0,
                context_size: 0,
            },
            metrics: Arc::new(RwLock::new(SessionMetrics::default())),
        });

        // Initialize compression if enabled
        if self.config.enable_compression {
            self.initialize_compression(&session).await?;
        }

        Ok(session)
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:execute_on_session (lines 380-412) ---[0m
    pub async fn execute_on_session<F, T>(
        &self,
        session: &OptimizedSession,
        operation: F,
    ) -> Result<T>
    where
        F: FnOnce(&dyn SimpleSession) -> futures::future::BoxFuture<'_, Result<T>> + Send,
        T: Send,
    {
        let start = Instant::now();

        // Lock session
        let session_guard = session.session.lock().await;

        // Execute operation
        let result = operation(session_guard.as_ref()).await;

        // Update metrics
        let execution_time = start.elapsed();
        let mut metrics = session.metrics.write().await;
        metrics.total_operations += 1;

        match &result {
            Ok(_) => metrics.successful_operations += 1,
            Err(_) => metrics.failed_operations += 1,
        }

        metrics.total_execution_time += execution_time;
        metrics.average_response_time =
            metrics.total_execution_time / metrics.total_operations as u32;

        result
    }

  ./crates/ccswarm/src/session/session_optimization.rs:238-278 method is_session_reusable <-> ./crates/ccswarm/src/session/session_optimization.rs:460-488 method get_statistics
  Similarity: 91.50%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:is_session_reusable (lines 238-278) ---[0m
    async fn is_session_reusable(&self, session: &OptimizedSession) -> Result<bool> {
        // Validate session is still alive
        let session_guard = session.session.lock().await;
        let is_valid = self
            .session_factory
            .validate_session(session_guard.as_ref())
            .await?;
        drop(session_guard);

        if !is_valid {
            return Ok(false);
        }

        // Apply reuse strategy
        match &self.config.reuse_strategy {
            ReuseStrategy::Aggressive => Ok(true),

            ReuseStrategy::LoadBased { threshold } => {
                let metrics = session.metrics.read().await;
                let current_load = self.calculate_session_load(&metrics);
                Ok(current_load < *threshold)
            }

            ReuseStrategy::TimeBased { max_age } => {
                let age = Utc::now().signed_duration_since(session.metadata.created_at);
                Ok(age.to_std().unwrap_or(Duration::MAX) < *max_age)
            }

            ReuseStrategy::Hybrid {
                load_threshold,
                max_age,
            } => {
                let metrics = session.metrics.read().await;
                let current_load = self.calculate_session_load(&metrics);
                let age = Utc::now().signed_duration_since(session.metadata.created_at);

                Ok(current_load < *load_threshold
                    && age.to_std().unwrap_or(Duration::MAX) < *max_age)
            }
        }
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:get_statistics (lines 460-488) ---[0m
    pub async fn get_statistics(&self) -> SessionStatistics {
        let registry = self.registry.read().await;
        let mut stats = SessionStatistics::default();

        for (role, sessions) in registry.iter() {
            stats.sessions_by_role.insert(
                role.clone(),
                RoleSessionStats {
                    total_sessions: sessions.len(),
                    active_sessions: sessions
                        .iter()
                        .filter(|s| {
                            let age = Utc::now().signed_duration_since(s.metadata.last_used);
                            age.to_std().unwrap_or(Duration::MAX) < self.config.idle_timeout
                        })
                        .count(),
                    compression_ratio: sessions
                        .iter()
                        .map(|s| s.metadata.compression_ratio)
                        .sum::<f64>()
                        / sessions.len().max(1) as f64,
                },
            );

            stats.total_sessions += sessions.len();
        }

        stats
    }

  ./crates/ccswarm/src/session/session_optimization.rs:238-278 method is_session_reusable <-> ./crates/ccswarm/src/session/session_optimization.rs:562-610 function test_session_optimization
  Similarity: 70.53%

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:is_session_reusable (lines 238-278) ---[0m
    async fn is_session_reusable(&self, session: &OptimizedSession) -> Result<bool> {
        // Validate session is still alive
        let session_guard = session.session.lock().await;
        let is_valid = self
            .session_factory
            .validate_session(session_guard.as_ref())
            .await?;
        drop(session_guard);

        if !is_valid {
            return Ok(false);
        }

        // Apply reuse strategy
        match &self.config.reuse_strategy {
            ReuseStrategy::Aggressive => Ok(true),

            ReuseStrategy::LoadBased { threshold } => {
                let metrics = session.metrics.read().await;
                let current_load = self.calculate_session_load(&metrics);
                Ok(current_load < *threshold)
            }

            ReuseStrategy::TimeBased { max_age } => {
                let age = Utc::now().signed_duration_since(session.metadata.created_at);
                Ok(age.to_std().unwrap_or(Duration::MAX) < *max_age)
            }

            ReuseStrategy::Hybrid {
                load_threshold,
                max_age,
            } => {
                let metrics = session.metrics.read().await;
                let current_load = self.calculate_session_load(&metrics);
                let age = Utc::now().signed_duration_since(session.metadata.created_at);

                Ok(current_load < *load_threshold
                    && age.to_std().unwrap_or(Duration::MAX) < *max_age)
            }
        }
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:test_session_optimization (lines 562-610) ---[0m
    async fn test_session_optimization() {
        struct MockSessionFactory;

        #[async_trait]
        impl SessionFactory for MockSessionFactory {
            async fn create_session(
                &self,
                _role: &AgentRole,
            ) -> Result<Box<dyn SimpleSession + Send>> {
                // Return mock session
                Ok(Box::new(MockSession))
            }

            async fn validate_session(&self, _session: &dyn SimpleSession) -> Result<bool> {
                Ok(true)
            }
        }

        struct MockSession;

        impl SimpleSession for MockSession {
            fn get_stats(&self) -> SessionStats {
                SessionStats::default()
            }

            fn is_alive(&self) -> bool {
                true
            }
        }

        let config = OptimizedSessionConfig::default();
        let factory = Arc::new(MockSessionFactory);
        let manager = OptimizedSessionManager::new(config, factory);

        let role = default_frontend_role();

        // First call should create
        let session1 = manager.get_or_create_session(&role).await.unwrap();

        // Second call should reuse
        let session2 = manager.get_or_create_session(&role).await.unwrap();

        // Sessions should have same ID if reused properly
        assert_eq!(session1.id, session2.id);

        // Check statistics
        let stats = manager.get_statistics().await;
        assert_eq!(stats.total_sessions, 1);
    }

  ./crates/ccswarm/src/session/session_optimization.rs:380-412 method execute_on_session <-> ./crates/ccswarm/src/session/session_optimization.rs:415-457 method batch_execute
  Similarity: 82.59%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:execute_on_session (lines 380-412) ---[0m
    pub async fn execute_on_session<F, T>(
        &self,
        session: &OptimizedSession,
        operation: F,
    ) -> Result<T>
    where
        F: FnOnce(&dyn SimpleSession) -> futures::future::BoxFuture<'_, Result<T>> + Send,
        T: Send,
    {
        let start = Instant::now();

        // Lock session
        let session_guard = session.session.lock().await;

        // Execute operation
        let result = operation(session_guard.as_ref()).await;

        // Update metrics
        let execution_time = start.elapsed();
        let mut metrics = session.metrics.write().await;
        metrics.total_operations += 1;

        match &result {
            Ok(_) => metrics.successful_operations += 1,
            Err(_) => metrics.failed_operations += 1,
        }

        metrics.total_execution_time += execution_time;
        metrics.average_response_time =
            metrics.total_execution_time / metrics.total_operations as u32;

        result
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:batch_execute (lines 415-457) ---[0m
    pub async fn batch_execute<F, T>(
        &self,
        role: &AgentRole,
        operations: Vec<F>,
    ) -> Result<Vec<Result<T>>>
    where
        F: FnOnce(&dyn SimpleSession) -> futures::future::BoxFuture<'_, Result<T>> + Send,
        T: Send,
    {
        if !self.config.performance_settings.batch_operations {
            // Execute individually if batching is disabled
            let mut results = Vec::new();
            for op in operations {
                let session = self.get_or_create_session(role).await?;
                results.push(self.execute_on_session(&session, op).await);
            }
            return Ok(results);
        }

        // Get session for batch
        let session = self.get_or_create_session(role).await?;

        // Execute in batches
        let mut results = Vec::new();
        let batch_size = self.config.performance_settings.batch_size;

        // Convert operations into an iterator to handle chunks properly
        let mut ops_iter = operations.into_iter();

        loop {
            let batch: Vec<_> = ops_iter.by_ref().take(batch_size).collect();
            if batch.is_empty() {
                break;
            }

            for op in batch {
                let session_clone = session.clone();
                results.push(self.execute_on_session(&session_clone, op).await);
            }
        }

        Ok(results)
    }

  ./crates/ccswarm/src/session/session_optimization.rs:294-332 method create_optimized_session <-> ./crates/ccswarm/src/session/session_optimization.rs:460-488 method get_statistics
  Similarity: 85.82%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:create_optimized_session (lines 294-332) ---[0m
    async fn create_optimized_session(&self, role: &AgentRole) -> Result<Arc<OptimizedSession>> {
        // Create underlying session
        let role_clone = role.clone();
        let factory = self.session_factory.clone();
        let base_session = with_retry(
            move || {
                let factory = factory.clone();
                let role = role_clone.clone();
                Box::pin(async move {
                    factory.create_session(&role).await
                        .map_err(|e| crate::error::CCSwarmError::session("unknown", e.to_string()))
                })
            },
            3,
        )
        .await?;

        // Create optimized wrapper
        let session = Arc::new(OptimizedSession {
            id: uuid::Uuid::new_v4().to_string(),
            role: role.clone(),
            session: Arc::new(Mutex::new(base_session)),
            metadata: SessionMetadata {
                created_at: Utc::now(),
                last_used: Utc::now(),
                compression_enabled: self.config.enable_compression,
                compression_ratio: 0.0,
                context_size: 0,
            },
            metrics: Arc::new(RwLock::new(SessionMetrics::default())),
        });

        // Initialize compression if enabled
        if self.config.enable_compression {
            self.initialize_compression(&session).await?;
        }

        Ok(session)
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:get_statistics (lines 460-488) ---[0m
    pub async fn get_statistics(&self) -> SessionStatistics {
        let registry = self.registry.read().await;
        let mut stats = SessionStatistics::default();

        for (role, sessions) in registry.iter() {
            stats.sessions_by_role.insert(
                role.clone(),
                RoleSessionStats {
                    total_sessions: sessions.len(),
                    active_sessions: sessions
                        .iter()
                        .filter(|s| {
                            let age = Utc::now().signed_duration_since(s.metadata.last_used);
                            age.to_std().unwrap_or(Duration::MAX) < self.config.idle_timeout
                        })
                        .count(),
                    compression_ratio: sessions
                        .iter()
                        .map(|s| s.metadata.compression_ratio)
                        .sum::<f64>()
                        / sessions.len().max(1) as f64,
                },
            );

            stats.total_sessions += sessions.len();
        }

        stats
    }

  ./crates/ccswarm/src/session/session_optimization.rs:342-365 method register_session <-> ./crates/ccswarm/src/session/session_optimization.rs:415-457 method batch_execute
  Similarity: 83.99%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:register_session (lines 342-365) ---[0m
    async fn register_session(&self, role_key: &str, session: Arc<OptimizedSession>) -> Result<()> {
        let mut registry = self.registry.write().await;

        let sessions = registry
            .entry(role_key.to_string())
            .or_insert_with(Vec::new);

        // Enforce max sessions per role
        if sessions.len() >= self.config.max_sessions_per_role {
            // Remove oldest or least used session
            sessions.sort_by_key(|s| s.metadata.last_used);
            if let Some(old_session) = sessions.first() {
                tracing::info!(
                    "Evicting old session {} for role {}",
                    old_session.id,
                    role_key
                );
                sessions.remove(0);
            }
        }

        sessions.push(session);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:batch_execute (lines 415-457) ---[0m
    pub async fn batch_execute<F, T>(
        &self,
        role: &AgentRole,
        operations: Vec<F>,
    ) -> Result<Vec<Result<T>>>
    where
        F: FnOnce(&dyn SimpleSession) -> futures::future::BoxFuture<'_, Result<T>> + Send,
        T: Send,
    {
        if !self.config.performance_settings.batch_operations {
            // Execute individually if batching is disabled
            let mut results = Vec::new();
            for op in operations {
                let session = self.get_or_create_session(role).await?;
                results.push(self.execute_on_session(&session, op).await);
            }
            return Ok(results);
        }

        // Get session for batch
        let session = self.get_or_create_session(role).await?;

        // Execute in batches
        let mut results = Vec::new();
        let batch_size = self.config.performance_settings.batch_size;

        // Convert operations into an iterator to handle chunks properly
        let mut ops_iter = operations.into_iter();

        loop {
            let batch: Vec<_> = ops_iter.by_ref().take(batch_size).collect();
            if batch.is_empty() {
                break;
            }

            for op in batch {
                let session_clone = session.clone();
                results.push(self.execute_on_session(&session_clone, op).await);
            }
        }

        Ok(results)
    }

  ./crates/ccswarm/src/session/session_optimization.rs:196-216 method get_or_create_session <-> ./crates/ccswarm/src/session/session_optimization.rs:562-610 function test_session_optimization
  Similarity: 79.68%

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:get_or_create_session (lines 196-216) ---[0m
    pub async fn get_or_create_session(&self, role: &AgentRole) -> Result<Arc<OptimizedSession>> {
        let role_key = role.name().to_lowercase();

        // Try to reuse existing session
        if let Some(session) = self.find_reusable_session(&role_key).await? {
            self.performance_tracker.record_reuse().await;
            return Ok(session);
        }

        // Create new session with circuit breaker protection
        let _breaker = self.get_circuit_breaker(&role_key).await;

        // Create the session directly
        let session = self.create_optimized_session(role).await?;

        // Register the session
        self.register_session(&role_key, session.clone()).await?;
        self.performance_tracker.record_creation().await;

        Ok(session)
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:test_session_optimization (lines 562-610) ---[0m
    async fn test_session_optimization() {
        struct MockSessionFactory;

        #[async_trait]
        impl SessionFactory for MockSessionFactory {
            async fn create_session(
                &self,
                _role: &AgentRole,
            ) -> Result<Box<dyn SimpleSession + Send>> {
                // Return mock session
                Ok(Box::new(MockSession))
            }

            async fn validate_session(&self, _session: &dyn SimpleSession) -> Result<bool> {
                Ok(true)
            }
        }

        struct MockSession;

        impl SimpleSession for MockSession {
            fn get_stats(&self) -> SessionStats {
                SessionStats::default()
            }

            fn is_alive(&self) -> bool {
                true
            }
        }

        let config = OptimizedSessionConfig::default();
        let factory = Arc::new(MockSessionFactory);
        let manager = OptimizedSessionManager::new(config, factory);

        let role = default_frontend_role();

        // First call should create
        let session1 = manager.get_or_create_session(&role).await.unwrap();

        // Second call should reuse
        let session2 = manager.get_or_create_session(&role).await.unwrap();

        // Sessions should have same ID if reused properly
        assert_eq!(session1.id, session2.id);

        // Check statistics
        let stats = manager.get_statistics().await;
        assert_eq!(stats.total_sessions, 1);
    }

  ./crates/ccswarm/src/session/session_optimization.rs:196-216 method get_or_create_session <-> ./crates/ccswarm/src/session/session_optimization.rs:415-457 method batch_execute
  Similarity: 86.91%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:get_or_create_session (lines 196-216) ---[0m
    pub async fn get_or_create_session(&self, role: &AgentRole) -> Result<Arc<OptimizedSession>> {
        let role_key = role.name().to_lowercase();

        // Try to reuse existing session
        if let Some(session) = self.find_reusable_session(&role_key).await? {
            self.performance_tracker.record_reuse().await;
            return Ok(session);
        }

        // Create new session with circuit breaker protection
        let _breaker = self.get_circuit_breaker(&role_key).await;

        // Create the session directly
        let session = self.create_optimized_session(role).await?;

        // Register the session
        self.register_session(&role_key, session.clone()).await?;
        self.performance_tracker.record_creation().await;

        Ok(session)
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:batch_execute (lines 415-457) ---[0m
    pub async fn batch_execute<F, T>(
        &self,
        role: &AgentRole,
        operations: Vec<F>,
    ) -> Result<Vec<Result<T>>>
    where
        F: FnOnce(&dyn SimpleSession) -> futures::future::BoxFuture<'_, Result<T>> + Send,
        T: Send,
    {
        if !self.config.performance_settings.batch_operations {
            // Execute individually if batching is disabled
            let mut results = Vec::new();
            for op in operations {
                let session = self.get_or_create_session(role).await?;
                results.push(self.execute_on_session(&session, op).await);
            }
            return Ok(results);
        }

        // Get session for batch
        let session = self.get_or_create_session(role).await?;

        // Execute in batches
        let mut results = Vec::new();
        let batch_size = self.config.performance_settings.batch_size;

        // Convert operations into an iterator to handle chunks properly
        let mut ops_iter = operations.into_iter();

        loop {
            let batch: Vec<_> = ops_iter.by_ref().take(batch_size).collect();
            if batch.is_empty() {
                break;
            }

            for op in batch {
                let session_clone = session.clone();
                results.push(self.execute_on_session(&session_clone, op).await);
            }
        }

        Ok(results)
    }

  ./crates/ccswarm/src/session/session_optimization.rs:415-457 method batch_execute <-> ./crates/ccswarm/src/session/session_optimization.rs:460-488 method get_statistics
  Similarity: 76.73%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:batch_execute (lines 415-457) ---[0m
    pub async fn batch_execute<F, T>(
        &self,
        role: &AgentRole,
        operations: Vec<F>,
    ) -> Result<Vec<Result<T>>>
    where
        F: FnOnce(&dyn SimpleSession) -> futures::future::BoxFuture<'_, Result<T>> + Send,
        T: Send,
    {
        if !self.config.performance_settings.batch_operations {
            // Execute individually if batching is disabled
            let mut results = Vec::new();
            for op in operations {
                let session = self.get_or_create_session(role).await?;
                results.push(self.execute_on_session(&session, op).await);
            }
            return Ok(results);
        }

        // Get session for batch
        let session = self.get_or_create_session(role).await?;

        // Execute in batches
        let mut results = Vec::new();
        let batch_size = self.config.performance_settings.batch_size;

        // Convert operations into an iterator to handle chunks properly
        let mut ops_iter = operations.into_iter();

        loop {
            let batch: Vec<_> = ops_iter.by_ref().take(batch_size).collect();
            if batch.is_empty() {
                break;
            }

            for op in batch {
                let session_clone = session.clone();
                results.push(self.execute_on_session(&session_clone, op).await);
            }
        }

        Ok(results)
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:get_statistics (lines 460-488) ---[0m
    pub async fn get_statistics(&self) -> SessionStatistics {
        let registry = self.registry.read().await;
        let mut stats = SessionStatistics::default();

        for (role, sessions) in registry.iter() {
            stats.sessions_by_role.insert(
                role.clone(),
                RoleSessionStats {
                    total_sessions: sessions.len(),
                    active_sessions: sessions
                        .iter()
                        .filter(|s| {
                            let age = Utc::now().signed_duration_since(s.metadata.last_used);
                            age.to_std().unwrap_or(Duration::MAX) < self.config.idle_timeout
                        })
                        .count(),
                    compression_ratio: sessions
                        .iter()
                        .map(|s| s.metadata.compression_ratio)
                        .sum::<f64>()
                        / sessions.len().max(1) as f64,
                },
            );

            stats.total_sessions += sessions.len();
        }

        stats
    }

  ./crates/ccswarm/src/session/session_optimization.rs:415-457 method batch_execute <-> ./crates/ccswarm/src/session/session_optimization.rs:491-508 method cleanup_idle_sessions
  Similarity: 79.60%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:batch_execute (lines 415-457) ---[0m
    pub async fn batch_execute<F, T>(
        &self,
        role: &AgentRole,
        operations: Vec<F>,
    ) -> Result<Vec<Result<T>>>
    where
        F: FnOnce(&dyn SimpleSession) -> futures::future::BoxFuture<'_, Result<T>> + Send,
        T: Send,
    {
        if !self.config.performance_settings.batch_operations {
            // Execute individually if batching is disabled
            let mut results = Vec::new();
            for op in operations {
                let session = self.get_or_create_session(role).await?;
                results.push(self.execute_on_session(&session, op).await);
            }
            return Ok(results);
        }

        // Get session for batch
        let session = self.get_or_create_session(role).await?;

        // Execute in batches
        let mut results = Vec::new();
        let batch_size = self.config.performance_settings.batch_size;

        // Convert operations into an iterator to handle chunks properly
        let mut ops_iter = operations.into_iter();

        loop {
            let batch: Vec<_> = ops_iter.by_ref().take(batch_size).collect();
            if batch.is_empty() {
                break;
            }

            for op in batch {
                let session_clone = session.clone();
                results.push(self.execute_on_session(&session_clone, op).await);
            }
        }

        Ok(results)
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:cleanup_idle_sessions (lines 491-508) ---[0m
    pub async fn cleanup_idle_sessions(&self) -> Result<usize> {
        let mut registry = self.registry.write().await;
        let mut cleaned = 0;

        for sessions in registry.values_mut() {
            let before = sessions.len();

            sessions.retain(|session| {
                let age = Utc::now().signed_duration_since(session.metadata.last_used);
                age.to_std().unwrap_or(Duration::MAX) < self.config.idle_timeout
            });

            cleaned += before - sessions.len();
        }

        tracing::info!("Cleaned up {} idle sessions", cleaned);
        Ok(cleaned)
    }

  ./crates/ccswarm/src/session/session_optimization.rs:342-365 method register_session <-> ./crates/ccswarm/src/session/session_optimization.rs:460-488 method get_statistics
  Similarity: 90.15%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:register_session (lines 342-365) ---[0m
    async fn register_session(&self, role_key: &str, session: Arc<OptimizedSession>) -> Result<()> {
        let mut registry = self.registry.write().await;

        let sessions = registry
            .entry(role_key.to_string())
            .or_insert_with(Vec::new);

        // Enforce max sessions per role
        if sessions.len() >= self.config.max_sessions_per_role {
            // Remove oldest or least used session
            sessions.sort_by_key(|s| s.metadata.last_used);
            if let Some(old_session) = sessions.first() {
                tracing::info!(
                    "Evicting old session {} for role {}",
                    old_session.id,
                    role_key
                );
                sessions.remove(0);
            }
        }

        sessions.push(session);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:get_statistics (lines 460-488) ---[0m
    pub async fn get_statistics(&self) -> SessionStatistics {
        let registry = self.registry.read().await;
        let mut stats = SessionStatistics::default();

        for (role, sessions) in registry.iter() {
            stats.sessions_by_role.insert(
                role.clone(),
                RoleSessionStats {
                    total_sessions: sessions.len(),
                    active_sessions: sessions
                        .iter()
                        .filter(|s| {
                            let age = Utc::now().signed_duration_since(s.metadata.last_used);
                            age.to_std().unwrap_or(Duration::MAX) < self.config.idle_timeout
                        })
                        .count(),
                    compression_ratio: sessions
                        .iter()
                        .map(|s| s.metadata.compression_ratio)
                        .sum::<f64>()
                        / sessions.len().max(1) as f64,
                },
            );

            stats.total_sessions += sessions.len();
        }

        stats
    }

  ./crates/ccswarm/src/session/session_optimization.rs:196-216 method get_or_create_session <-> ./crates/ccswarm/src/session/session_optimization.rs:380-412 method execute_on_session
  Similarity: 81.32%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:get_or_create_session (lines 196-216) ---[0m
    pub async fn get_or_create_session(&self, role: &AgentRole) -> Result<Arc<OptimizedSession>> {
        let role_key = role.name().to_lowercase();

        // Try to reuse existing session
        if let Some(session) = self.find_reusable_session(&role_key).await? {
            self.performance_tracker.record_reuse().await;
            return Ok(session);
        }

        // Create new session with circuit breaker protection
        let _breaker = self.get_circuit_breaker(&role_key).await;

        // Create the session directly
        let session = self.create_optimized_session(role).await?;

        // Register the session
        self.register_session(&role_key, session.clone()).await?;
        self.performance_tracker.record_creation().await;

        Ok(session)
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:execute_on_session (lines 380-412) ---[0m
    pub async fn execute_on_session<F, T>(
        &self,
        session: &OptimizedSession,
        operation: F,
    ) -> Result<T>
    where
        F: FnOnce(&dyn SimpleSession) -> futures::future::BoxFuture<'_, Result<T>> + Send,
        T: Send,
    {
        let start = Instant::now();

        // Lock session
        let session_guard = session.session.lock().await;

        // Execute operation
        let result = operation(session_guard.as_ref()).await;

        // Update metrics
        let execution_time = start.elapsed();
        let mut metrics = session.metrics.write().await;
        metrics.total_operations += 1;

        match &result {
            Ok(_) => metrics.successful_operations += 1,
            Err(_) => metrics.failed_operations += 1,
        }

        metrics.total_execution_time += execution_time;
        metrics.average_response_time =
            metrics.total_execution_time / metrics.total_operations as u32;

        result
    }

  ./crates/ccswarm/src/session/session_optimization.rs:219-235 method find_reusable_session <-> ./crates/ccswarm/src/session/session_optimization.rs:460-488 method get_statistics
  Similarity: 92.82%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:find_reusable_session (lines 219-235) ---[0m
    async fn find_reusable_session(&self, role_key: &str) -> Result<Option<Arc<OptimizedSession>>> {
        let registry = self.registry.read().await;

        if let Some(sessions) = registry.get(role_key) {
            for session in sessions {
                if self.is_session_reusable(session).await? {
                    // Update last used time
                    let mut metadata = session.metadata.clone();
                    metadata.last_used = Utc::now();

                    return Ok(Some(session.clone()));
                }
            }
        }

        Ok(None)
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:get_statistics (lines 460-488) ---[0m
    pub async fn get_statistics(&self) -> SessionStatistics {
        let registry = self.registry.read().await;
        let mut stats = SessionStatistics::default();

        for (role, sessions) in registry.iter() {
            stats.sessions_by_role.insert(
                role.clone(),
                RoleSessionStats {
                    total_sessions: sessions.len(),
                    active_sessions: sessions
                        .iter()
                        .filter(|s| {
                            let age = Utc::now().signed_duration_since(s.metadata.last_used);
                            age.to_std().unwrap_or(Duration::MAX) < self.config.idle_timeout
                        })
                        .count(),
                    compression_ratio: sessions
                        .iter()
                        .map(|s| s.metadata.compression_ratio)
                        .sum::<f64>()
                        / sessions.len().max(1) as f64,
                },
            );

            stats.total_sessions += sessions.len();
        }

        stats
    }

  ./crates/ccswarm/src/session/session_optimization.rs:460-488 method get_statistics <-> ./crates/ccswarm/src/session/session_optimization.rs:491-508 method cleanup_idle_sessions
  Similarity: 90.71%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:get_statistics (lines 460-488) ---[0m
    pub async fn get_statistics(&self) -> SessionStatistics {
        let registry = self.registry.read().await;
        let mut stats = SessionStatistics::default();

        for (role, sessions) in registry.iter() {
            stats.sessions_by_role.insert(
                role.clone(),
                RoleSessionStats {
                    total_sessions: sessions.len(),
                    active_sessions: sessions
                        .iter()
                        .filter(|s| {
                            let age = Utc::now().signed_duration_since(s.metadata.last_used);
                            age.to_std().unwrap_or(Duration::MAX) < self.config.idle_timeout
                        })
                        .count(),
                    compression_ratio: sessions
                        .iter()
                        .map(|s| s.metadata.compression_ratio)
                        .sum::<f64>()
                        / sessions.len().max(1) as f64,
                },
            );

            stats.total_sessions += sessions.len();
        }

        stats
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:cleanup_idle_sessions (lines 491-508) ---[0m
    pub async fn cleanup_idle_sessions(&self) -> Result<usize> {
        let mut registry = self.registry.write().await;
        let mut cleaned = 0;

        for sessions in registry.values_mut() {
            let before = sessions.len();

            sessions.retain(|session| {
                let age = Utc::now().signed_duration_since(session.metadata.last_used);
                age.to_std().unwrap_or(Duration::MAX) < self.config.idle_timeout
            });

            cleaned += before - sessions.len();
        }

        tracing::info!("Cleaned up {} idle sessions", cleaned);
        Ok(cleaned)
    }

  ./crates/ccswarm/src/session/session_optimization.rs:342-365 method register_session <-> ./crates/ccswarm/src/session/session_optimization.rs:491-508 method cleanup_idle_sessions
  Similarity: 87.40%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:register_session (lines 342-365) ---[0m
    async fn register_session(&self, role_key: &str, session: Arc<OptimizedSession>) -> Result<()> {
        let mut registry = self.registry.write().await;

        let sessions = registry
            .entry(role_key.to_string())
            .or_insert_with(Vec::new);

        // Enforce max sessions per role
        if sessions.len() >= self.config.max_sessions_per_role {
            // Remove oldest or least used session
            sessions.sort_by_key(|s| s.metadata.last_used);
            if let Some(old_session) = sessions.first() {
                tracing::info!(
                    "Evicting old session {} for role {}",
                    old_session.id,
                    role_key
                );
                sessions.remove(0);
            }
        }

        sessions.push(session);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:cleanup_idle_sessions (lines 491-508) ---[0m
    pub async fn cleanup_idle_sessions(&self) -> Result<usize> {
        let mut registry = self.registry.write().await;
        let mut cleaned = 0;

        for sessions in registry.values_mut() {
            let before = sessions.len();

            sessions.retain(|session| {
                let age = Utc::now().signed_duration_since(session.metadata.last_used);
                age.to_std().unwrap_or(Duration::MAX) < self.config.idle_timeout
            });

            cleaned += before - sessions.len();
        }

        tracing::info!("Cleaned up {} idle sessions", cleaned);
        Ok(cleaned)
    }

  ./crates/ccswarm/src/session/session_optimization.rs:196-216 method get_or_create_session <-> ./crates/ccswarm/src/session/session_optimization.rs:342-365 method register_session
  Similarity: 71.27%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:get_or_create_session (lines 196-216) ---[0m
    pub async fn get_or_create_session(&self, role: &AgentRole) -> Result<Arc<OptimizedSession>> {
        let role_key = role.name().to_lowercase();

        // Try to reuse existing session
        if let Some(session) = self.find_reusable_session(&role_key).await? {
            self.performance_tracker.record_reuse().await;
            return Ok(session);
        }

        // Create new session with circuit breaker protection
        let _breaker = self.get_circuit_breaker(&role_key).await;

        // Create the session directly
        let session = self.create_optimized_session(role).await?;

        // Register the session
        self.register_session(&role_key, session.clone()).await?;
        self.performance_tracker.record_creation().await;

        Ok(session)
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:register_session (lines 342-365) ---[0m
    async fn register_session(&self, role_key: &str, session: Arc<OptimizedSession>) -> Result<()> {
        let mut registry = self.registry.write().await;

        let sessions = registry
            .entry(role_key.to_string())
            .or_insert_with(Vec::new);

        // Enforce max sessions per role
        if sessions.len() >= self.config.max_sessions_per_role {
            // Remove oldest or least used session
            sessions.sort_by_key(|s| s.metadata.last_used);
            if let Some(old_session) = sessions.first() {
                tracing::info!(
                    "Evicting old session {} for role {}",
                    old_session.id,
                    role_key
                );
                sessions.remove(0);
            }
        }

        sessions.push(session);
        Ok(())
    }

  ./crates/ccswarm/src/session/session_optimization.rs:219-235 method find_reusable_session <-> ./crates/ccswarm/src/session/session_optimization.rs:342-365 method register_session
  Similarity: 76.71%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:find_reusable_session (lines 219-235) ---[0m
    async fn find_reusable_session(&self, role_key: &str) -> Result<Option<Arc<OptimizedSession>>> {
        let registry = self.registry.read().await;

        if let Some(sessions) = registry.get(role_key) {
            for session in sessions {
                if self.is_session_reusable(session).await? {
                    // Update last used time
                    let mut metadata = session.metadata.clone();
                    metadata.last_used = Utc::now();

                    return Ok(Some(session.clone()));
                }
            }
        }

        Ok(None)
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:register_session (lines 342-365) ---[0m
    async fn register_session(&self, role_key: &str, session: Arc<OptimizedSession>) -> Result<()> {
        let mut registry = self.registry.write().await;

        let sessions = registry
            .entry(role_key.to_string())
            .or_insert_with(Vec::new);

        // Enforce max sessions per role
        if sessions.len() >= self.config.max_sessions_per_role {
            // Remove oldest or least used session
            sessions.sort_by_key(|s| s.metadata.last_used);
            if let Some(old_session) = sessions.first() {
                tracing::info!(
                    "Evicting old session {} for role {}",
                    old_session.id,
                    role_key
                );
                sessions.remove(0);
            }
        }

        sessions.push(session);
        Ok(())
    }

  ./crates/ccswarm/src/session/session_optimization.rs:219-235 method find_reusable_session <-> ./crates/ccswarm/src/session/session_optimization.rs:491-508 method cleanup_idle_sessions
  Similarity: 78.76%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:find_reusable_session (lines 219-235) ---[0m
    async fn find_reusable_session(&self, role_key: &str) -> Result<Option<Arc<OptimizedSession>>> {
        let registry = self.registry.read().await;

        if let Some(sessions) = registry.get(role_key) {
            for session in sessions {
                if self.is_session_reusable(session).await? {
                    // Update last used time
                    let mut metadata = session.metadata.clone();
                    metadata.last_used = Utc::now();

                    return Ok(Some(session.clone()));
                }
            }
        }

        Ok(None)
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:cleanup_idle_sessions (lines 491-508) ---[0m
    pub async fn cleanup_idle_sessions(&self) -> Result<usize> {
        let mut registry = self.registry.write().await;
        let mut cleaned = 0;

        for sessions in registry.values_mut() {
            let before = sessions.len();

            sessions.retain(|session| {
                let age = Utc::now().signed_duration_since(session.metadata.last_used);
                age.to_std().unwrap_or(Duration::MAX) < self.config.idle_timeout
            });

            cleaned += before - sessions.len();
        }

        tracing::info!("Cleaned up {} idle sessions", cleaned);
        Ok(cleaned)
    }

  ./crates/ccswarm/src/session/session_optimization.rs:79-97 method default <-> ./crates/ccswarm/src/session/session_optimization.rs:182-193 method new
  Similarity: 86.27%
  Classes: Default <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:default (lines 79-97) ---[0m
    fn default() -> Self {
        Self {
            max_sessions_per_role: 5,
            idle_timeout: Duration::from_secs(300),
            health_check_interval: Duration::from_secs(30),
            enable_compression: true,
            compression_threshold: 0.8,
            reuse_strategy: ReuseStrategy::Hybrid {
                load_threshold: 0.7,
                max_age: Duration::from_secs(3600),
            },
            performance_settings: PerformanceSettings {
                batch_operations: true,
                batch_size: 10,
                predictive_scaling: true,
                context_caching: true,
            },
        }
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:new (lines 182-193) ---[0m
    pub fn new(
        config: OptimizedSessionConfig,
        session_factory: Arc<dyn SessionFactory + Send + Sync>,
    ) -> Self {
        Self {
            config,
            registry: Arc::new(RwLock::new(HashMap::new())),
            circuit_breakers: Arc::new(RwLock::new(HashMap::new())),
            performance_tracker: Arc::new(PerformanceTracker::new()),
            session_factory,
        }
    }

  ./crates/ccswarm/src/session/session_optimization.rs:219-235 method find_reusable_session <-> ./crates/ccswarm/src/session/session_optimization.rs:368-377 method get_circuit_breaker
  Similarity: 89.10%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:find_reusable_session (lines 219-235) ---[0m
    async fn find_reusable_session(&self, role_key: &str) -> Result<Option<Arc<OptimizedSession>>> {
        let registry = self.registry.read().await;

        if let Some(sessions) = registry.get(role_key) {
            for session in sessions {
                if self.is_session_reusable(session).await? {
                    // Update last used time
                    let mut metadata = session.metadata.clone();
                    metadata.last_used = Utc::now();

                    return Ok(Some(session.clone()));
                }
            }
        }

        Ok(None)
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:get_circuit_breaker (lines 368-377) ---[0m
    async fn get_circuit_breaker(&self, role_key: &str) -> Arc<AsyncCircuitBreaker> {
        let mut breakers = self.circuit_breakers.write().await;

        breakers
            .entry(role_key.to_string())
            .or_insert_with(|| {
                Arc::new(AsyncCircuitBreaker::new(3))
            })
            .clone()
    }

  ./crates/ccswarm/src/session/session_optimization.rs:219-235 method find_reusable_session <-> ./crates/ccswarm/src/session/session_optimization.rs:281-291 method calculate_session_load
  Similarity: 83.83%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:find_reusable_session (lines 219-235) ---[0m
    async fn find_reusable_session(&self, role_key: &str) -> Result<Option<Arc<OptimizedSession>>> {
        let registry = self.registry.read().await;

        if let Some(sessions) = registry.get(role_key) {
            for session in sessions {
                if self.is_session_reusable(session).await? {
                    // Update last used time
                    let mut metadata = session.metadata.clone();
                    metadata.last_used = Utc::now();

                    return Ok(Some(session.clone()));
                }
            }
        }

        Ok(None)
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:calculate_session_load (lines 281-291) ---[0m
    fn calculate_session_load(&self, metrics: &SessionMetrics) -> f64 {
        // Simple load calculation based on recent operations
        let operation_rate = metrics.total_operations as f64 / 100.0;
        let failure_rate = if metrics.total_operations > 0 {
            metrics.failed_operations as f64 / metrics.total_operations as f64
        } else {
            0.0
        };

        (operation_rate + failure_rate * 2.0).min(1.0)
    }

  ./crates/ccswarm/src/session/session_optimization.rs:79-97 method default <-> ./crates/ccswarm/src/session/session_optimization.rs:512-520 method new
  Similarity: 81.64%
  Classes: Default <-> PerformanceTracker

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:default (lines 79-97) ---[0m
    fn default() -> Self {
        Self {
            max_sessions_per_role: 5,
            idle_timeout: Duration::from_secs(300),
            health_check_interval: Duration::from_secs(30),
            enable_compression: true,
            compression_threshold: 0.8,
            reuse_strategy: ReuseStrategy::Hybrid {
                load_threshold: 0.7,
                max_age: Duration::from_secs(3600),
            },
            performance_settings: PerformanceSettings {
                batch_operations: true,
                batch_size: 10,
                predictive_scaling: true,
                context_caching: true,
            },
        }
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:new (lines 512-520) ---[0m
    fn new() -> Self {
        Self {
            metrics: Arc::new(RwLock::new(GlobalMetrics::default())),
            predictions: Arc::new(RwLock::new(PredictionEngine {
                historical_load: Vec::new(),
                predicted_load: HashMap::new(),
            })),
        }
    }

  ./crates/ccswarm/src/session/session_optimization.rs:79-97 method default <-> ./crates/ccswarm/src/session/session_optimization.rs:368-377 method get_circuit_breaker
  Similarity: 77.91%
  Classes: Default <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:default (lines 79-97) ---[0m
    fn default() -> Self {
        Self {
            max_sessions_per_role: 5,
            idle_timeout: Duration::from_secs(300),
            health_check_interval: Duration::from_secs(30),
            enable_compression: true,
            compression_threshold: 0.8,
            reuse_strategy: ReuseStrategy::Hybrid {
                load_threshold: 0.7,
                max_age: Duration::from_secs(3600),
            },
            performance_settings: PerformanceSettings {
                batch_operations: true,
                batch_size: 10,
                predictive_scaling: true,
                context_caching: true,
            },
        }
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:get_circuit_breaker (lines 368-377) ---[0m
    async fn get_circuit_breaker(&self, role_key: &str) -> Arc<AsyncCircuitBreaker> {
        let mut breakers = self.circuit_breakers.write().await;

        breakers
            .entry(role_key.to_string())
            .or_insert_with(|| {
                Arc::new(AsyncCircuitBreaker::new(3))
            })
            .clone()
    }

  ./crates/ccswarm/src/session/session_optimization.rs:219-235 method find_reusable_session <-> ./crates/ccswarm/src/session/session_optimization.rs:512-520 method new
  Similarity: 74.29%
  Classes: OptimizedSessionManager <-> PerformanceTracker

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:find_reusable_session (lines 219-235) ---[0m
    async fn find_reusable_session(&self, role_key: &str) -> Result<Option<Arc<OptimizedSession>>> {
        let registry = self.registry.read().await;

        if let Some(sessions) = registry.get(role_key) {
            for session in sessions {
                if self.is_session_reusable(session).await? {
                    // Update last used time
                    let mut metadata = session.metadata.clone();
                    metadata.last_used = Utc::now();

                    return Ok(Some(session.clone()));
                }
            }
        }

        Ok(None)
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:new (lines 512-520) ---[0m
    fn new() -> Self {
        Self {
            metrics: Arc::new(RwLock::new(GlobalMetrics::default())),
            predictions: Arc::new(RwLock::new(PredictionEngine {
                historical_load: Vec::new(),
                predicted_load: HashMap::new(),
            })),
        }
    }

  ./crates/ccswarm/src/session/session_optimization.rs:182-193 method new <-> ./crates/ccswarm/src/session/session_optimization.rs:512-520 method new
  Similarity: 86.48%
  Classes: OptimizedSessionManager <-> PerformanceTracker

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:new (lines 182-193) ---[0m
    pub fn new(
        config: OptimizedSessionConfig,
        session_factory: Arc<dyn SessionFactory + Send + Sync>,
    ) -> Self {
        Self {
            config,
            registry: Arc::new(RwLock::new(HashMap::new())),
            circuit_breakers: Arc::new(RwLock::new(HashMap::new())),
            performance_tracker: Arc::new(PerformanceTracker::new()),
            session_factory,
        }
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:new (lines 512-520) ---[0m
    fn new() -> Self {
        Self {
            metrics: Arc::new(RwLock::new(GlobalMetrics::default())),
            predictions: Arc::new(RwLock::new(PredictionEngine {
                historical_load: Vec::new(),
                predicted_load: HashMap::new(),
            })),
        }
    }

  ./crates/ccswarm/src/session/session_optimization.rs:182-193 method new <-> ./crates/ccswarm/src/session/session_optimization.rs:368-377 method get_circuit_breaker
  Similarity: 72.39%
  Classes: OptimizedSessionManager <-> OptimizedSessionManager

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:new (lines 182-193) ---[0m
    pub fn new(
        config: OptimizedSessionConfig,
        session_factory: Arc<dyn SessionFactory + Send + Sync>,
    ) -> Self {
        Self {
            config,
            registry: Arc::new(RwLock::new(HashMap::new())),
            circuit_breakers: Arc::new(RwLock::new(HashMap::new())),
            performance_tracker: Arc::new(PerformanceTracker::new()),
            session_factory,
        }
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:get_circuit_breaker (lines 368-377) ---[0m
    async fn get_circuit_breaker(&self, role_key: &str) -> Arc<AsyncCircuitBreaker> {
        let mut breakers = self.circuit_breakers.write().await;

        breakers
            .entry(role_key.to_string())
            .or_insert_with(|| {
                Arc::new(AsyncCircuitBreaker::new(3))
            })
            .clone()
    }

  ./crates/ccswarm/src/session/session_optimization.rs:281-291 method calculate_session_load <-> ./crates/ccswarm/src/session/session_optimization.rs:532-540 method get_reuse_ratio
  Similarity: 73.20%
  Classes: OptimizedSessionManager <-> PerformanceTracker

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:calculate_session_load (lines 281-291) ---[0m
    fn calculate_session_load(&self, metrics: &SessionMetrics) -> f64 {
        // Simple load calculation based on recent operations
        let operation_rate = metrics.total_operations as f64 / 100.0;
        let failure_rate = if metrics.total_operations > 0 {
            metrics.failed_operations as f64 / metrics.total_operations as f64
        } else {
            0.0
        };

        (operation_rate + failure_rate * 2.0).min(1.0)
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:get_reuse_ratio (lines 532-540) ---[0m
    pub async fn get_reuse_ratio(&self) -> f64 {
        let metrics = self.metrics.read().await;
        let total = metrics.total_sessions_created + metrics.total_sessions_reused;
        if total > 0 {
            metrics.total_sessions_reused as f64 / total as f64
        } else {
            0.0
        }
    }

  ./crates/ccswarm/src/session/session_optimization.rs:368-377 method get_circuit_breaker <-> ./crates/ccswarm/src/session/session_optimization.rs:512-520 method new
  Similarity: 70.72%
  Classes: OptimizedSessionManager <-> PerformanceTracker

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:get_circuit_breaker (lines 368-377) ---[0m
    async fn get_circuit_breaker(&self, role_key: &str) -> Arc<AsyncCircuitBreaker> {
        let mut breakers = self.circuit_breakers.write().await;

        breakers
            .entry(role_key.to_string())
            .or_insert_with(|| {
                Arc::new(AsyncCircuitBreaker::new(3))
            })
            .clone()
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:new (lines 512-520) ---[0m
    fn new() -> Self {
        Self {
            metrics: Arc::new(RwLock::new(GlobalMetrics::default())),
            predictions: Arc::new(RwLock::new(PredictionEngine {
                historical_load: Vec::new(),
                predicted_load: HashMap::new(),
            })),
        }
    }

  ./crates/ccswarm/src/session/session_optimization.rs:522-525 method record_creation <-> ./crates/ccswarm/src/session/session_optimization.rs:527-530 method record_reuse
  Similarity: 99.00%
  Classes: PerformanceTracker <-> PerformanceTracker

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:record_creation (lines 522-525) ---[0m
    async fn record_creation(&self) {
        let mut metrics = self.metrics.write().await;
        metrics.total_sessions_created += 1;
    }

[36m--- ./crates/ccswarm/src/session/session_optimization.rs:record_reuse (lines 527-530) ---[0m
    async fn record_reuse(&self) {
        let mut metrics = self.metrics.write().await;
        metrics.total_sessions_reused += 1;
    }


Duplicates in ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:479-584 method evaluate_with_heuristics <-> ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:601-663 method generate_fix_instructions
  Similarity: 75.76%
  Classes: LLMQualityJudge <-> LLMQualityJudge

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:evaluate_with_heuristics (lines 479-584) ---[0m
    fn evaluate_with_heuristics(
        &self,
        content: &str,
        _task: &Task,
        agent_role: &AgentRole,
    ) -> Result<QualityEvaluation> {
        let mut dimension_scores = HashMap::new();
        let mut issues = Vec::new();

        // Basic heuristics
        let lines = content.lines().count();
        let has_tests = content.contains("test") || content.contains("spec");
        let has_error_handling = content.contains("try")
            || content.contains("catch")
            || content.contains("Result")
            || content.contains("Option");
        let has_comments =
            content.contains("//") || content.contains("/*") || content.contains("#");

        // Score dimensions based on heuristics
        dimension_scores.insert("correctness".to_string(), 0.8); // Assume mostly correct
        dimension_scores.insert(
            "maintainability".to_string(),
            if lines < 200 { 0.9 } else { 0.7 },
        );
        dimension_scores.insert(
            "test_quality".to_string(),
            if has_tests { 0.8 } else { 0.3 },
        );
        dimension_scores.insert("security".to_string(), 0.7); // Conservative estimate
        dimension_scores.insert("performance".to_string(), 0.7);
        dimension_scores.insert(
            "documentation".to_string(),
            if has_comments { 0.7 } else { 0.4 },
        );
        dimension_scores.insert(
            "error_handling".to_string(),
            if has_error_handling { 0.8 } else { 0.5 },
        );
        dimension_scores.insert("architecture".to_string(), 0.7);

        // Generate issues based on low scores
        if !has_tests {
            issues.push(QualityIssue {
                severity: IssueSeverity::High,
                category: IssueCategory::TestCoverage,
                description: "No tests found for the implementation".to_string(),
                suggested_fix: "Add unit tests covering main functionality and edge cases"
                    .to_string(),
                affected_areas: vec![],
                fix_effort: 60,
            });
        }

        if !has_error_handling {
            issues.push(QualityIssue {
                severity: IssueSeverity::Medium,
                category: IssueCategory::ErrorHandling,
                description: "Limited error handling detected".to_string(),
                suggested_fix: "Add proper error handling for edge cases and failures".to_string(),
                affected_areas: vec![],
                fix_effort: 30,
            });
        }

        // Calculate overall score
        let weights = self.rubric.get_role_weights(agent_role.name());
        let overall_score = dimension_scores
            .iter()
            .map(|(dim, score)| weights.get(dim).unwrap_or(&0.1) * score)
            .sum::<f64>()
            / weights.values().sum::<f64>();

        let passes_standards = overall_score >= 0.85
            && issues
                .iter()
                .filter(|i| matches!(i.severity, IssueSeverity::Critical | IssueSeverity::High))
                .count()
                == 0;

        Ok(QualityEvaluation {
            overall_score,
            dimension_scores,
            issues: issues.clone(),
            feedback: format!(
                "Heuristic evaluation for {} agent: {} issues found. {}",
                agent_role.name(),
                issues.len(),
                if passes_standards {
                    "Meets standards."
                } else {
                    "Needs improvement."
                }
            ),
            passes_standards,
            confidence: 0.6, // Lower confidence for heuristic evaluation
            evaluated_at: Utc::now(),
            metadata: EvaluationMetadata {
                files_analyzed: 1,
                lines_of_code: lines,
                duration_ms: 0,
                tools_used: vec!["Heuristics".to_string()],
                agent_role: agent_role.name().to_string(),
            },
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:generate_fix_instructions (lines 601-663) ---[0m
    pub fn generate_fix_instructions(&self, issues: &[QualityIssue], agent_role: &str) -> String {
        if issues.is_empty() {
            return "No issues found. Great work!".to_string();
        }

        let mut instructions = format!(
            "## Quality Review - Fix Instructions for {} Agent\n\n",
            agent_role
        );

        // Group issues by severity
        let mut critical_issues = Vec::new();
        let mut high_issues = Vec::new();
        let mut medium_issues = Vec::new();
        let mut low_issues = Vec::new();

        for issue in issues {
            match issue.severity {
                IssueSeverity::Critical => critical_issues.push(issue),
                IssueSeverity::High => high_issues.push(issue),
                IssueSeverity::Medium => medium_issues.push(issue),
                IssueSeverity::Low => low_issues.push(issue),
            }
        }

        // Add instructions by priority
        if !critical_issues.is_empty() {
            instructions.push_str("### 🚨 CRITICAL Issues (Fix Immediately)\n");
            for issue in critical_issues {
                instructions.push_str(&format!(
                    "- **{:?}**: {}\n  - Fix: {}\n  - Effort: {} minutes\n\n",
                    issue.category, issue.description, issue.suggested_fix, issue.fix_effort
                ));
            }
        }

        if !high_issues.is_empty() {
            instructions.push_str("### ⚠️  HIGH Priority Issues\n");
            for issue in high_issues {
                instructions.push_str(&format!(
                    "- **{:?}**: {}\n  - Fix: {}\n  - Effort: {} minutes\n\n",
                    issue.category, issue.description, issue.suggested_fix, issue.fix_effort
                ));
            }
        }

        if !medium_issues.is_empty() {
            instructions.push_str("### 📝 MEDIUM Priority Issues\n");
            for issue in medium_issues {
                instructions.push_str(&format!(
                    "- **{:?}**: {}\n  - Fix: {}\n\n",
                    issue.category, issue.description, issue.suggested_fix
                ));
            }
        }

        instructions.push_str(&format!(
            "\n### Estimated Total Fix Time: {} minutes\n",
            issues.iter().map(|i| i.fix_effort).sum::<u32>()
        ));

        instructions
    }

  ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:50-85 method default_role_weights <-> ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:479-584 method evaluate_with_heuristics
  Similarity: 89.84%
  Classes: QualityRubric <-> LLMQualityJudge

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:default_role_weights (lines 50-85) ---[0m
    fn default_role_weights() -> HashMap<String, HashMap<String, f64>> {
        let mut role_weights = HashMap::new();

        // Frontend weights
        let mut frontend_weights = HashMap::new();
        frontend_weights.insert("correctness".to_string(), 0.25);
        frontend_weights.insert("maintainability".to_string(), 0.2);
        frontend_weights.insert("test_quality".to_string(), 0.15);
        frontend_weights.insert("security".to_string(), 0.1);
        frontend_weights.insert("performance".to_string(), 0.15);
        frontend_weights.insert("documentation".to_string(), 0.05);
        frontend_weights.insert("accessibility".to_string(), 0.1);
        role_weights.insert("Frontend".to_string(), frontend_weights);

        // Backend weights
        let mut backend_weights = HashMap::new();
        backend_weights.insert("correctness".to_string(), 0.3);
        backend_weights.insert("maintainability".to_string(), 0.15);
        backend_weights.insert("test_quality".to_string(), 0.25);
        backend_weights.insert("security".to_string(), 0.2);
        backend_weights.insert("performance".to_string(), 0.1);
        backend_weights.insert("documentation".to_string(), 0.0);
        role_weights.insert("Backend".to_string(), backend_weights);

        // DevOps weights
        let mut devops_weights = HashMap::new();
        devops_weights.insert("correctness".to_string(), 0.25);
        devops_weights.insert("maintainability".to_string(), 0.15);
        devops_weights.insert("test_quality".to_string(), 0.1);
        devops_weights.insert("security".to_string(), 0.3);
        devops_weights.insert("performance".to_string(), 0.15);
        devops_weights.insert("documentation".to_string(), 0.05);
        role_weights.insert("DevOps".to_string(), devops_weights);

        role_weights
    }

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:evaluate_with_heuristics (lines 479-584) ---[0m
    fn evaluate_with_heuristics(
        &self,
        content: &str,
        _task: &Task,
        agent_role: &AgentRole,
    ) -> Result<QualityEvaluation> {
        let mut dimension_scores = HashMap::new();
        let mut issues = Vec::new();

        // Basic heuristics
        let lines = content.lines().count();
        let has_tests = content.contains("test") || content.contains("spec");
        let has_error_handling = content.contains("try")
            || content.contains("catch")
            || content.contains("Result")
            || content.contains("Option");
        let has_comments =
            content.contains("//") || content.contains("/*") || content.contains("#");

        // Score dimensions based on heuristics
        dimension_scores.insert("correctness".to_string(), 0.8); // Assume mostly correct
        dimension_scores.insert(
            "maintainability".to_string(),
            if lines < 200 { 0.9 } else { 0.7 },
        );
        dimension_scores.insert(
            "test_quality".to_string(),
            if has_tests { 0.8 } else { 0.3 },
        );
        dimension_scores.insert("security".to_string(), 0.7); // Conservative estimate
        dimension_scores.insert("performance".to_string(), 0.7);
        dimension_scores.insert(
            "documentation".to_string(),
            if has_comments { 0.7 } else { 0.4 },
        );
        dimension_scores.insert(
            "error_handling".to_string(),
            if has_error_handling { 0.8 } else { 0.5 },
        );
        dimension_scores.insert("architecture".to_string(), 0.7);

        // Generate issues based on low scores
        if !has_tests {
            issues.push(QualityIssue {
                severity: IssueSeverity::High,
                category: IssueCategory::TestCoverage,
                description: "No tests found for the implementation".to_string(),
                suggested_fix: "Add unit tests covering main functionality and edge cases"
                    .to_string(),
                affected_areas: vec![],
                fix_effort: 60,
            });
        }

        if !has_error_handling {
            issues.push(QualityIssue {
                severity: IssueSeverity::Medium,
                category: IssueCategory::ErrorHandling,
                description: "Limited error handling detected".to_string(),
                suggested_fix: "Add proper error handling for edge cases and failures".to_string(),
                affected_areas: vec![],
                fix_effort: 30,
            });
        }

        // Calculate overall score
        let weights = self.rubric.get_role_weights(agent_role.name());
        let overall_score = dimension_scores
            .iter()
            .map(|(dim, score)| weights.get(dim).unwrap_or(&0.1) * score)
            .sum::<f64>()
            / weights.values().sum::<f64>();

        let passes_standards = overall_score >= 0.85
            && issues
                .iter()
                .filter(|i| matches!(i.severity, IssueSeverity::Critical | IssueSeverity::High))
                .count()
                == 0;

        Ok(QualityEvaluation {
            overall_score,
            dimension_scores,
            issues: issues.clone(),
            feedback: format!(
                "Heuristic evaluation for {} agent: {} issues found. {}",
                agent_role.name(),
                issues.len(),
                if passes_standards {
                    "Meets standards."
                } else {
                    "Needs improvement."
                }
            ),
            passes_standards,
            confidence: 0.6, // Lower confidence for heuristic evaluation
            evaluated_at: Utc::now(),
            metadata: EvaluationMetadata {
                files_analyzed: 1,
                lines_of_code: lines,
                duration_ms: 0,
                tools_used: vec!["Heuristics".to_string()],
                agent_role: agent_role.name().to_string(),
            },
        })
    }

  ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:381-435 method parse_claude_evaluation <-> ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:601-663 method generate_fix_instructions
  Similarity: 84.53%
  Classes: LLMQualityJudge <-> LLMQualityJudge

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:parse_claude_evaluation (lines 381-435) ---[0m
    fn parse_claude_evaluation(
        &self,
        response: &str,
        agent_role: &AgentRole,
    ) -> Result<QualityEvaluation> {
        // Try to parse JSON response
        let json_result: serde_json::Value = serde_json::from_str(response)
            .context("Failed to parse Claude's evaluation response")?;

        let overall_score = json_result["overall_score"].as_f64().unwrap_or(0.5);

        let mut dimension_scores = HashMap::new();
        if let Some(dimensions) = json_result["dimensions"].as_object() {
            for (key, value) in dimensions {
                dimension_scores.insert(key.clone(), value.as_f64().unwrap_or(0.5));
            }
        }

        let mut issues = Vec::new();
        if let Some(issues_array) = json_result["issues"].as_array() {
            for issue_json in issues_array {
                if let Ok(issue) = self.parse_quality_issue(issue_json) {
                    issues.push(issue);
                }
            }
        }

        let feedback = json_result["feedback"]
            .as_str()
            .unwrap_or("No detailed feedback provided")
            .to_string();

        let passes_standards = json_result["passes_standards"]
            .as_bool()
            .unwrap_or(overall_score >= 0.85);

        let confidence = json_result["confidence"].as_f64().unwrap_or(0.8);

        Ok(QualityEvaluation {
            overall_score,
            dimension_scores,
            issues,
            feedback,
            passes_standards,
            confidence,
            evaluated_at: Utc::now(),
            metadata: EvaluationMetadata {
                files_analyzed: 1,
                lines_of_code: response.lines().count(),
                duration_ms: 0, // Will be set by caller
                tools_used: vec!["Claude".to_string()],
                agent_role: agent_role.name().to_string(),
            },
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:generate_fix_instructions (lines 601-663) ---[0m
    pub fn generate_fix_instructions(&self, issues: &[QualityIssue], agent_role: &str) -> String {
        if issues.is_empty() {
            return "No issues found. Great work!".to_string();
        }

        let mut instructions = format!(
            "## Quality Review - Fix Instructions for {} Agent\n\n",
            agent_role
        );

        // Group issues by severity
        let mut critical_issues = Vec::new();
        let mut high_issues = Vec::new();
        let mut medium_issues = Vec::new();
        let mut low_issues = Vec::new();

        for issue in issues {
            match issue.severity {
                IssueSeverity::Critical => critical_issues.push(issue),
                IssueSeverity::High => high_issues.push(issue),
                IssueSeverity::Medium => medium_issues.push(issue),
                IssueSeverity::Low => low_issues.push(issue),
            }
        }

        // Add instructions by priority
        if !critical_issues.is_empty() {
            instructions.push_str("### 🚨 CRITICAL Issues (Fix Immediately)\n");
            for issue in critical_issues {
                instructions.push_str(&format!(
                    "- **{:?}**: {}\n  - Fix: {}\n  - Effort: {} minutes\n\n",
                    issue.category, issue.description, issue.suggested_fix, issue.fix_effort
                ));
            }
        }

        if !high_issues.is_empty() {
            instructions.push_str("### ⚠️  HIGH Priority Issues\n");
            for issue in high_issues {
                instructions.push_str(&format!(
                    "- **{:?}**: {}\n  - Fix: {}\n  - Effort: {} minutes\n\n",
                    issue.category, issue.description, issue.suggested_fix, issue.fix_effort
                ));
            }
        }

        if !medium_issues.is_empty() {
            instructions.push_str("### 📝 MEDIUM Priority Issues\n");
            for issue in medium_issues {
                instructions.push_str(&format!(
                    "- **{:?}**: {}\n  - Fix: {}\n\n",
                    issue.category, issue.description, issue.suggested_fix
                ));
            }
        }

        instructions.push_str(&format!(
            "\n### Estimated Total Fix Time: {} minutes\n",
            issues.iter().map(|i| i.fix_effort).sum::<u32>()
        ));

        instructions
    }

  ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:230-273 method evaluate_task <-> ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:601-663 method generate_fix_instructions
  Similarity: 84.57%
  Classes: LLMQualityJudge <-> LLMQualityJudge

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:evaluate_task (lines 230-273) ---[0m
    pub async fn evaluate_task(
        &mut self,
        task: &Task,
        result: &TaskResult,
        agent_role: &AgentRole,
        workspace_path: &str,
    ) -> Result<QualityEvaluation> {
        let start_time = std::time::Instant::now();

        // Check cache first
        let cache_key = format!("{}-{}", task.id, result.output);
        if let Some(cached) = self.evaluation_cache.get(&cache_key) {
            debug!("Using cached evaluation for task {}", task.id);
            return Ok(cached.clone());
        }

        info!(
            "Evaluating quality for task {} by {} agent",
            task.id,
            agent_role.name()
        );

        // Extract code or content from result
        let content = self.extract_content_from_result(result)?;

        // Perform LLM-based evaluation
        let evaluation = if self.use_claude {
            self.evaluate_with_claude(&content, task, agent_role, workspace_path)
                .await?
        } else {
            self.evaluate_with_heuristics(&content, task, agent_role)?
        };

        // Cache the evaluation
        self.evaluation_cache.insert(cache_key, evaluation.clone());

        let duration_ms = start_time.elapsed().as_millis() as u64;
        info!(
            "Quality evaluation completed in {}ms: score={:.2}, passed={}",
            duration_ms, evaluation.overall_score, evaluation.passes_standards
        );

        Ok(evaluation)
    }

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:generate_fix_instructions (lines 601-663) ---[0m
    pub fn generate_fix_instructions(&self, issues: &[QualityIssue], agent_role: &str) -> String {
        if issues.is_empty() {
            return "No issues found. Great work!".to_string();
        }

        let mut instructions = format!(
            "## Quality Review - Fix Instructions for {} Agent\n\n",
            agent_role
        );

        // Group issues by severity
        let mut critical_issues = Vec::new();
        let mut high_issues = Vec::new();
        let mut medium_issues = Vec::new();
        let mut low_issues = Vec::new();

        for issue in issues {
            match issue.severity {
                IssueSeverity::Critical => critical_issues.push(issue),
                IssueSeverity::High => high_issues.push(issue),
                IssueSeverity::Medium => medium_issues.push(issue),
                IssueSeverity::Low => low_issues.push(issue),
            }
        }

        // Add instructions by priority
        if !critical_issues.is_empty() {
            instructions.push_str("### 🚨 CRITICAL Issues (Fix Immediately)\n");
            for issue in critical_issues {
                instructions.push_str(&format!(
                    "- **{:?}**: {}\n  - Fix: {}\n  - Effort: {} minutes\n\n",
                    issue.category, issue.description, issue.suggested_fix, issue.fix_effort
                ));
            }
        }

        if !high_issues.is_empty() {
            instructions.push_str("### ⚠️  HIGH Priority Issues\n");
            for issue in high_issues {
                instructions.push_str(&format!(
                    "- **{:?}**: {}\n  - Fix: {}\n  - Effort: {} minutes\n\n",
                    issue.category, issue.description, issue.suggested_fix, issue.fix_effort
                ));
            }
        }

        if !medium_issues.is_empty() {
            instructions.push_str("### 📝 MEDIUM Priority Issues\n");
            for issue in medium_issues {
                instructions.push_str(&format!(
                    "- **{:?}**: {}\n  - Fix: {}\n\n",
                    issue.category, issue.description, issue.suggested_fix
                ));
            }
        }

        instructions.push_str(&format!(
            "\n### Estimated Total Fix Time: {} minutes\n",
            issues.iter().map(|i| i.fix_effort).sum::<u32>()
        ));

        instructions
    }

  ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:230-273 method evaluate_task <-> ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:381-435 method parse_claude_evaluation
  Similarity: 86.89%
  Classes: LLMQualityJudge <-> LLMQualityJudge

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:evaluate_task (lines 230-273) ---[0m
    pub async fn evaluate_task(
        &mut self,
        task: &Task,
        result: &TaskResult,
        agent_role: &AgentRole,
        workspace_path: &str,
    ) -> Result<QualityEvaluation> {
        let start_time = std::time::Instant::now();

        // Check cache first
        let cache_key = format!("{}-{}", task.id, result.output);
        if let Some(cached) = self.evaluation_cache.get(&cache_key) {
            debug!("Using cached evaluation for task {}", task.id);
            return Ok(cached.clone());
        }

        info!(
            "Evaluating quality for task {} by {} agent",
            task.id,
            agent_role.name()
        );

        // Extract code or content from result
        let content = self.extract_content_from_result(result)?;

        // Perform LLM-based evaluation
        let evaluation = if self.use_claude {
            self.evaluate_with_claude(&content, task, agent_role, workspace_path)
                .await?
        } else {
            self.evaluate_with_heuristics(&content, task, agent_role)?
        };

        // Cache the evaluation
        self.evaluation_cache.insert(cache_key, evaluation.clone());

        let duration_ms = start_time.elapsed().as_millis() as u64;
        info!(
            "Quality evaluation completed in {}ms: score={:.2}, passed={}",
            duration_ms, evaluation.overall_score, evaluation.passes_standards
        );

        Ok(evaluation)
    }

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:parse_claude_evaluation (lines 381-435) ---[0m
    fn parse_claude_evaluation(
        &self,
        response: &str,
        agent_role: &AgentRole,
    ) -> Result<QualityEvaluation> {
        // Try to parse JSON response
        let json_result: serde_json::Value = serde_json::from_str(response)
            .context("Failed to parse Claude's evaluation response")?;

        let overall_score = json_result["overall_score"].as_f64().unwrap_or(0.5);

        let mut dimension_scores = HashMap::new();
        if let Some(dimensions) = json_result["dimensions"].as_object() {
            for (key, value) in dimensions {
                dimension_scores.insert(key.clone(), value.as_f64().unwrap_or(0.5));
            }
        }

        let mut issues = Vec::new();
        if let Some(issues_array) = json_result["issues"].as_array() {
            for issue_json in issues_array {
                if let Ok(issue) = self.parse_quality_issue(issue_json) {
                    issues.push(issue);
                }
            }
        }

        let feedback = json_result["feedback"]
            .as_str()
            .unwrap_or("No detailed feedback provided")
            .to_string();

        let passes_standards = json_result["passes_standards"]
            .as_bool()
            .unwrap_or(overall_score >= 0.85);

        let confidence = json_result["confidence"].as_f64().unwrap_or(0.8);

        Ok(QualityEvaluation {
            overall_score,
            dimension_scores,
            issues,
            feedback,
            passes_standards,
            confidence,
            evaluated_at: Utc::now(),
            metadata: EvaluationMetadata {
                files_analyzed: 1,
                lines_of_code: response.lines().count(),
                duration_ms: 0, // Will be set by caller
                tools_used: vec!["Claude".to_string()],
                agent_role: agent_role.name().to_string(),
            },
        })
    }

  ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:601-663 method generate_fix_instructions <-> ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:696-730 function test_heuristic_evaluation
  Similarity: 83.43%

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:generate_fix_instructions (lines 601-663) ---[0m
    pub fn generate_fix_instructions(&self, issues: &[QualityIssue], agent_role: &str) -> String {
        if issues.is_empty() {
            return "No issues found. Great work!".to_string();
        }

        let mut instructions = format!(
            "## Quality Review - Fix Instructions for {} Agent\n\n",
            agent_role
        );

        // Group issues by severity
        let mut critical_issues = Vec::new();
        let mut high_issues = Vec::new();
        let mut medium_issues = Vec::new();
        let mut low_issues = Vec::new();

        for issue in issues {
            match issue.severity {
                IssueSeverity::Critical => critical_issues.push(issue),
                IssueSeverity::High => high_issues.push(issue),
                IssueSeverity::Medium => medium_issues.push(issue),
                IssueSeverity::Low => low_issues.push(issue),
            }
        }

        // Add instructions by priority
        if !critical_issues.is_empty() {
            instructions.push_str("### 🚨 CRITICAL Issues (Fix Immediately)\n");
            for issue in critical_issues {
                instructions.push_str(&format!(
                    "- **{:?}**: {}\n  - Fix: {}\n  - Effort: {} minutes\n\n",
                    issue.category, issue.description, issue.suggested_fix, issue.fix_effort
                ));
            }
        }

        if !high_issues.is_empty() {
            instructions.push_str("### ⚠️  HIGH Priority Issues\n");
            for issue in high_issues {
                instructions.push_str(&format!(
                    "- **{:?}**: {}\n  - Fix: {}\n  - Effort: {} minutes\n\n",
                    issue.category, issue.description, issue.suggested_fix, issue.fix_effort
                ));
            }
        }

        if !medium_issues.is_empty() {
            instructions.push_str("### 📝 MEDIUM Priority Issues\n");
            for issue in medium_issues {
                instructions.push_str(&format!(
                    "- **{:?}**: {}\n  - Fix: {}\n\n",
                    issue.category, issue.description, issue.suggested_fix
                ));
            }
        }

        instructions.push_str(&format!(
            "\n### Estimated Total Fix Time: {} minutes\n",
            issues.iter().map(|i| i.fix_effort).sum::<u32>()
        ));

        instructions
    }

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:test_heuristic_evaluation (lines 696-730) ---[0m
    async fn test_heuristic_evaluation() {
        let mut judge = LLMQualityJudge::new();
        judge.use_claude = false; // Force heuristics

        let task = Task::new(
            "test-123".to_string(),
            "Test task".to_string(),
            crate::agent::Priority::Medium,
            crate::agent::TaskType::Development,
        );

        let result = TaskResult {
            success: true,
            output: serde_json::json!({
                "response": "function add(a, b) { return a + b; }"
            }),
            error: None,
            duration: std::time::Duration::from_secs(1),
        };

        let role = AgentRole::Frontend {
            technologies: vec!["React".to_string()],
            responsibilities: vec![],
            boundaries: vec![],
        };

        let evaluation = judge
            .evaluate_task(&task, &result, &role, "/tmp")
            .await
            .unwrap();

        assert!(evaluation.overall_score > 0.0);
        assert!(evaluation.overall_score <= 1.0);
        assert!(!evaluation.issues.is_empty()); // Should find missing tests
    }

  ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:381-435 method parse_claude_evaluation <-> ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:696-730 function test_heuristic_evaluation
  Similarity: 84.98%

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:parse_claude_evaluation (lines 381-435) ---[0m
    fn parse_claude_evaluation(
        &self,
        response: &str,
        agent_role: &AgentRole,
    ) -> Result<QualityEvaluation> {
        // Try to parse JSON response
        let json_result: serde_json::Value = serde_json::from_str(response)
            .context("Failed to parse Claude's evaluation response")?;

        let overall_score = json_result["overall_score"].as_f64().unwrap_or(0.5);

        let mut dimension_scores = HashMap::new();
        if let Some(dimensions) = json_result["dimensions"].as_object() {
            for (key, value) in dimensions {
                dimension_scores.insert(key.clone(), value.as_f64().unwrap_or(0.5));
            }
        }

        let mut issues = Vec::new();
        if let Some(issues_array) = json_result["issues"].as_array() {
            for issue_json in issues_array {
                if let Ok(issue) = self.parse_quality_issue(issue_json) {
                    issues.push(issue);
                }
            }
        }

        let feedback = json_result["feedback"]
            .as_str()
            .unwrap_or("No detailed feedback provided")
            .to_string();

        let passes_standards = json_result["passes_standards"]
            .as_bool()
            .unwrap_or(overall_score >= 0.85);

        let confidence = json_result["confidence"].as_f64().unwrap_or(0.8);

        Ok(QualityEvaluation {
            overall_score,
            dimension_scores,
            issues,
            feedback,
            passes_standards,
            confidence,
            evaluated_at: Utc::now(),
            metadata: EvaluationMetadata {
                files_analyzed: 1,
                lines_of_code: response.lines().count(),
                duration_ms: 0, // Will be set by caller
                tools_used: vec!["Claude".to_string()],
                agent_role: agent_role.name().to_string(),
            },
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:test_heuristic_evaluation (lines 696-730) ---[0m
    async fn test_heuristic_evaluation() {
        let mut judge = LLMQualityJudge::new();
        judge.use_claude = false; // Force heuristics

        let task = Task::new(
            "test-123".to_string(),
            "Test task".to_string(),
            crate::agent::Priority::Medium,
            crate::agent::TaskType::Development,
        );

        let result = TaskResult {
            success: true,
            output: serde_json::json!({
                "response": "function add(a, b) { return a + b; }"
            }),
            error: None,
            duration: std::time::Duration::from_secs(1),
        };

        let role = AgentRole::Frontend {
            technologies: vec!["React".to_string()],
            responsibilities: vec![],
            boundaries: vec![],
        };

        let evaluation = judge
            .evaluate_task(&task, &result, &role, "/tmp")
            .await
            .unwrap();

        assert!(evaluation.overall_score > 0.0);
        assert!(evaluation.overall_score <= 1.0);
        assert!(!evaluation.issues.is_empty()); // Should find missing tests
    }

  ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:23-45 method default <-> ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:601-663 method generate_fix_instructions
  Similarity: 80.20%
  Classes: Default <-> LLMQualityJudge

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:default (lines 23-45) ---[0m
    fn default() -> Self {
        let mut dimensions = HashMap::new();
        dimensions.insert("correctness".to_string(), 0.3);
        dimensions.insert("maintainability".to_string(), 0.2);
        dimensions.insert("test_quality".to_string(), 0.2);
        dimensions.insert("security".to_string(), 0.15);
        dimensions.insert("performance".to_string(), 0.1);
        dimensions.insert("documentation".to_string(), 0.05);

        let mut thresholds = HashMap::new();
        thresholds.insert("correctness".to_string(), 0.9);
        thresholds.insert("maintainability".to_string(), 0.8);
        thresholds.insert("test_quality".to_string(), 0.85);
        thresholds.insert("security".to_string(), 0.9);
        thresholds.insert("performance".to_string(), 0.7);
        thresholds.insert("documentation".to_string(), 0.7);

        Self {
            dimensions,
            thresholds,
            role_weights: Self::default_role_weights(),
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:generate_fix_instructions (lines 601-663) ---[0m
    pub fn generate_fix_instructions(&self, issues: &[QualityIssue], agent_role: &str) -> String {
        if issues.is_empty() {
            return "No issues found. Great work!".to_string();
        }

        let mut instructions = format!(
            "## Quality Review - Fix Instructions for {} Agent\n\n",
            agent_role
        );

        // Group issues by severity
        let mut critical_issues = Vec::new();
        let mut high_issues = Vec::new();
        let mut medium_issues = Vec::new();
        let mut low_issues = Vec::new();

        for issue in issues {
            match issue.severity {
                IssueSeverity::Critical => critical_issues.push(issue),
                IssueSeverity::High => high_issues.push(issue),
                IssueSeverity::Medium => medium_issues.push(issue),
                IssueSeverity::Low => low_issues.push(issue),
            }
        }

        // Add instructions by priority
        if !critical_issues.is_empty() {
            instructions.push_str("### 🚨 CRITICAL Issues (Fix Immediately)\n");
            for issue in critical_issues {
                instructions.push_str(&format!(
                    "- **{:?}**: {}\n  - Fix: {}\n  - Effort: {} minutes\n\n",
                    issue.category, issue.description, issue.suggested_fix, issue.fix_effort
                ));
            }
        }

        if !high_issues.is_empty() {
            instructions.push_str("### ⚠️  HIGH Priority Issues\n");
            for issue in high_issues {
                instructions.push_str(&format!(
                    "- **{:?}**: {}\n  - Fix: {}\n  - Effort: {} minutes\n\n",
                    issue.category, issue.description, issue.suggested_fix, issue.fix_effort
                ));
            }
        }

        if !medium_issues.is_empty() {
            instructions.push_str("### 📝 MEDIUM Priority Issues\n");
            for issue in medium_issues {
                instructions.push_str(&format!(
                    "- **{:?}**: {}\n  - Fix: {}\n\n",
                    issue.category, issue.description, issue.suggested_fix
                ));
            }
        }

        instructions.push_str(&format!(
            "\n### Estimated Total Fix Time: {} minutes\n",
            issues.iter().map(|i| i.fix_effort).sum::<u32>()
        ));

        instructions
    }

  ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:230-273 method evaluate_task <-> ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:696-730 function test_heuristic_evaluation
  Similarity: 84.78%

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:evaluate_task (lines 230-273) ---[0m
    pub async fn evaluate_task(
        &mut self,
        task: &Task,
        result: &TaskResult,
        agent_role: &AgentRole,
        workspace_path: &str,
    ) -> Result<QualityEvaluation> {
        let start_time = std::time::Instant::now();

        // Check cache first
        let cache_key = format!("{}-{}", task.id, result.output);
        if let Some(cached) = self.evaluation_cache.get(&cache_key) {
            debug!("Using cached evaluation for task {}", task.id);
            return Ok(cached.clone());
        }

        info!(
            "Evaluating quality for task {} by {} agent",
            task.id,
            agent_role.name()
        );

        // Extract code or content from result
        let content = self.extract_content_from_result(result)?;

        // Perform LLM-based evaluation
        let evaluation = if self.use_claude {
            self.evaluate_with_claude(&content, task, agent_role, workspace_path)
                .await?
        } else {
            self.evaluate_with_heuristics(&content, task, agent_role)?
        };

        // Cache the evaluation
        self.evaluation_cache.insert(cache_key, evaluation.clone());

        let duration_ms = start_time.elapsed().as_millis() as u64;
        info!(
            "Quality evaluation completed in {}ms: score={:.2}, passed={}",
            duration_ms, evaluation.overall_score, evaluation.passes_standards
        );

        Ok(evaluation)
    }

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:test_heuristic_evaluation (lines 696-730) ---[0m
    async fn test_heuristic_evaluation() {
        let mut judge = LLMQualityJudge::new();
        judge.use_claude = false; // Force heuristics

        let task = Task::new(
            "test-123".to_string(),
            "Test task".to_string(),
            crate::agent::Priority::Medium,
            crate::agent::TaskType::Development,
        );

        let result = TaskResult {
            success: true,
            output: serde_json::json!({
                "response": "function add(a, b) { return a + b; }"
            }),
            error: None,
            duration: std::time::Duration::from_secs(1),
        };

        let role = AgentRole::Frontend {
            technologies: vec!["React".to_string()],
            responsibilities: vec![],
            boundaries: vec![],
        };

        let evaluation = judge
            .evaluate_task(&task, &result, &role, "/tmp")
            .await
            .unwrap();

        assert!(evaluation.overall_score > 0.0);
        assert!(evaluation.overall_score <= 1.0);
        assert!(!evaluation.issues.is_empty()); // Should find missing tests
    }

  ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:438-476 method parse_quality_issue <-> ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:696-730 function test_heuristic_evaluation
  Similarity: 79.39%

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:parse_quality_issue (lines 438-476) ---[0m
    pub fn parse_quality_issue(&self, json: &serde_json::Value) -> Result<QualityIssue> {
        let severity = match json["severity"].as_str().unwrap_or("medium") {
            "critical" => IssueSeverity::Critical,
            "high" => IssueSeverity::High,
            "low" => IssueSeverity::Low,
            _ => IssueSeverity::Medium,
        };

        let category = match json["category"].as_str().unwrap_or("BestPractices") {
            "Security" => IssueCategory::Security,
            "Performance" => IssueCategory::Performance,
            "TestCoverage" => IssueCategory::TestCoverage,
            "CodeComplexity" => IssueCategory::CodeComplexity,
            "Documentation" => IssueCategory::Documentation,
            "ErrorHandling" => IssueCategory::ErrorHandling,
            "Architecture" => IssueCategory::Architecture,
            "Accessibility" => IssueCategory::Accessibility,
            "TypeSafety" => IssueCategory::TypeSafety,
            _ => IssueCategory::BestPractices,
        };

        let affected_areas = json["affected_areas"]
            .as_array()
            .map(|arr| {
                arr.iter()
                    .filter_map(|v| v.as_str().map(String::from))
                    .collect()
            })
            .unwrap_or_default();

        Ok(QualityIssue {
            severity,
            category,
            description: json["description"].as_str().unwrap_or("").to_string(),
            suggested_fix: json["suggested_fix"].as_str().unwrap_or("").to_string(),
            affected_areas,
            fix_effort: json["fix_effort"].as_u64().unwrap_or(30) as u32,
        })
    }

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:test_heuristic_evaluation (lines 696-730) ---[0m
    async fn test_heuristic_evaluation() {
        let mut judge = LLMQualityJudge::new();
        judge.use_claude = false; // Force heuristics

        let task = Task::new(
            "test-123".to_string(),
            "Test task".to_string(),
            crate::agent::Priority::Medium,
            crate::agent::TaskType::Development,
        );

        let result = TaskResult {
            success: true,
            output: serde_json::json!({
                "response": "function add(a, b) { return a + b; }"
            }),
            error: None,
            duration: std::time::Duration::from_secs(1),
        };

        let role = AgentRole::Frontend {
            technologies: vec!["React".to_string()],
            responsibilities: vec![],
            boundaries: vec![],
        };

        let evaluation = judge
            .evaluate_task(&task, &result, &role, "/tmp")
            .await
            .unwrap();

        assert!(evaluation.overall_score > 0.0);
        assert!(evaluation.overall_score <= 1.0);
        assert!(!evaluation.issues.is_empty()); // Should find missing tests
    }

  ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:230-273 method evaluate_task <-> ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:286-315 method evaluate_with_claude
  Similarity: 77.63%
  Classes: LLMQualityJudge <-> LLMQualityJudge

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:evaluate_task (lines 230-273) ---[0m
    pub async fn evaluate_task(
        &mut self,
        task: &Task,
        result: &TaskResult,
        agent_role: &AgentRole,
        workspace_path: &str,
    ) -> Result<QualityEvaluation> {
        let start_time = std::time::Instant::now();

        // Check cache first
        let cache_key = format!("{}-{}", task.id, result.output);
        if let Some(cached) = self.evaluation_cache.get(&cache_key) {
            debug!("Using cached evaluation for task {}", task.id);
            return Ok(cached.clone());
        }

        info!(
            "Evaluating quality for task {} by {} agent",
            task.id,
            agent_role.name()
        );

        // Extract code or content from result
        let content = self.extract_content_from_result(result)?;

        // Perform LLM-based evaluation
        let evaluation = if self.use_claude {
            self.evaluate_with_claude(&content, task, agent_role, workspace_path)
                .await?
        } else {
            self.evaluate_with_heuristics(&content, task, agent_role)?
        };

        // Cache the evaluation
        self.evaluation_cache.insert(cache_key, evaluation.clone());

        let duration_ms = start_time.elapsed().as_millis() as u64;
        info!(
            "Quality evaluation completed in {}ms: score={:.2}, passed={}",
            duration_ms, evaluation.overall_score, evaluation.passes_standards
        );

        Ok(evaluation)
    }

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:evaluate_with_claude (lines 286-315) ---[0m
    async fn evaluate_with_claude(
        &self,
        content: &str,
        task: &Task,
        agent_role: &AgentRole,
        workspace_path: &str,
    ) -> Result<QualityEvaluation> {
        let prompt = self.generate_evaluation_prompt(content, task, agent_role);

        // Execute Claude for evaluation
        let mut cmd = Command::new("claude");
        cmd.current_dir(workspace_path)
            .arg("-p")
            .arg(&prompt)
            .arg("--json");

        let output = cmd
            .output()
            .await
            .context("Failed to execute Claude for quality evaluation")?;

        if !output.status.success() {
            warn!("Claude evaluation failed, falling back to heuristics");
            return self.evaluate_with_heuristics(content, task, agent_role);
        }

        // Parse Claude's response
        let response = String::from_utf8_lossy(&output.stdout);
        self.parse_claude_evaluation(&response, agent_role)
    }

  ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:286-315 method evaluate_with_claude <-> ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:696-730 function test_heuristic_evaluation
  Similarity: 83.56%

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:evaluate_with_claude (lines 286-315) ---[0m
    async fn evaluate_with_claude(
        &self,
        content: &str,
        task: &Task,
        agent_role: &AgentRole,
        workspace_path: &str,
    ) -> Result<QualityEvaluation> {
        let prompt = self.generate_evaluation_prompt(content, task, agent_role);

        // Execute Claude for evaluation
        let mut cmd = Command::new("claude");
        cmd.current_dir(workspace_path)
            .arg("-p")
            .arg(&prompt)
            .arg("--json");

        let output = cmd
            .output()
            .await
            .context("Failed to execute Claude for quality evaluation")?;

        if !output.status.success() {
            warn!("Claude evaluation failed, falling back to heuristics");
            return self.evaluate_with_heuristics(content, task, agent_role);
        }

        // Parse Claude's response
        let response = String::from_utf8_lossy(&output.stdout);
        self.parse_claude_evaluation(&response, agent_role)
    }

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:test_heuristic_evaluation (lines 696-730) ---[0m
    async fn test_heuristic_evaluation() {
        let mut judge = LLMQualityJudge::new();
        judge.use_claude = false; // Force heuristics

        let task = Task::new(
            "test-123".to_string(),
            "Test task".to_string(),
            crate::agent::Priority::Medium,
            crate::agent::TaskType::Development,
        );

        let result = TaskResult {
            success: true,
            output: serde_json::json!({
                "response": "function add(a, b) { return a + b; }"
            }),
            error: None,
            duration: std::time::Duration::from_secs(1),
        };

        let role = AgentRole::Frontend {
            technologies: vec!["React".to_string()],
            responsibilities: vec![],
            boundaries: vec![],
        };

        let evaluation = judge
            .evaluate_task(&task, &result, &role, "/tmp")
            .await
            .unwrap();

        assert!(evaluation.overall_score > 0.0);
        assert!(evaluation.overall_score <= 1.0);
        assert!(!evaluation.issues.is_empty()); // Should find missing tests
    }

  ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:23-45 method default <-> ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:230-273 method evaluate_task
  Similarity: 72.74%
  Classes: Default <-> LLMQualityJudge

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:default (lines 23-45) ---[0m
    fn default() -> Self {
        let mut dimensions = HashMap::new();
        dimensions.insert("correctness".to_string(), 0.3);
        dimensions.insert("maintainability".to_string(), 0.2);
        dimensions.insert("test_quality".to_string(), 0.2);
        dimensions.insert("security".to_string(), 0.15);
        dimensions.insert("performance".to_string(), 0.1);
        dimensions.insert("documentation".to_string(), 0.05);

        let mut thresholds = HashMap::new();
        thresholds.insert("correctness".to_string(), 0.9);
        thresholds.insert("maintainability".to_string(), 0.8);
        thresholds.insert("test_quality".to_string(), 0.85);
        thresholds.insert("security".to_string(), 0.9);
        thresholds.insert("performance".to_string(), 0.7);
        thresholds.insert("documentation".to_string(), 0.7);

        Self {
            dimensions,
            thresholds,
            role_weights: Self::default_role_weights(),
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:evaluate_task (lines 230-273) ---[0m
    pub async fn evaluate_task(
        &mut self,
        task: &Task,
        result: &TaskResult,
        agent_role: &AgentRole,
        workspace_path: &str,
    ) -> Result<QualityEvaluation> {
        let start_time = std::time::Instant::now();

        // Check cache first
        let cache_key = format!("{}-{}", task.id, result.output);
        if let Some(cached) = self.evaluation_cache.get(&cache_key) {
            debug!("Using cached evaluation for task {}", task.id);
            return Ok(cached.clone());
        }

        info!(
            "Evaluating quality for task {} by {} agent",
            task.id,
            agent_role.name()
        );

        // Extract code or content from result
        let content = self.extract_content_from_result(result)?;

        // Perform LLM-based evaluation
        let evaluation = if self.use_claude {
            self.evaluate_with_claude(&content, task, agent_role, workspace_path)
                .await?
        } else {
            self.evaluate_with_heuristics(&content, task, agent_role)?
        };

        // Cache the evaluation
        self.evaluation_cache.insert(cache_key, evaluation.clone());

        let duration_ms = start_time.elapsed().as_millis() as u64;
        info!(
            "Quality evaluation completed in {}ms: score={:.2}, passed={}",
            duration_ms, evaluation.overall_score, evaluation.passes_standards
        );

        Ok(evaluation)
    }

  ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:23-45 method default <-> ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:696-730 function test_heuristic_evaluation
  Similarity: 75.91%

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:default (lines 23-45) ---[0m
    fn default() -> Self {
        let mut dimensions = HashMap::new();
        dimensions.insert("correctness".to_string(), 0.3);
        dimensions.insert("maintainability".to_string(), 0.2);
        dimensions.insert("test_quality".to_string(), 0.2);
        dimensions.insert("security".to_string(), 0.15);
        dimensions.insert("performance".to_string(), 0.1);
        dimensions.insert("documentation".to_string(), 0.05);

        let mut thresholds = HashMap::new();
        thresholds.insert("correctness".to_string(), 0.9);
        thresholds.insert("maintainability".to_string(), 0.8);
        thresholds.insert("test_quality".to_string(), 0.85);
        thresholds.insert("security".to_string(), 0.9);
        thresholds.insert("performance".to_string(), 0.7);
        thresholds.insert("documentation".to_string(), 0.7);

        Self {
            dimensions,
            thresholds,
            role_weights: Self::default_role_weights(),
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:test_heuristic_evaluation (lines 696-730) ---[0m
    async fn test_heuristic_evaluation() {
        let mut judge = LLMQualityJudge::new();
        judge.use_claude = false; // Force heuristics

        let task = Task::new(
            "test-123".to_string(),
            "Test task".to_string(),
            crate::agent::Priority::Medium,
            crate::agent::TaskType::Development,
        );

        let result = TaskResult {
            success: true,
            output: serde_json::json!({
                "response": "function add(a, b) { return a + b; }"
            }),
            error: None,
            duration: std::time::Duration::from_secs(1),
        };

        let role = AgentRole::Frontend {
            technologies: vec!["React".to_string()],
            responsibilities: vec![],
            boundaries: vec![],
        };

        let evaluation = judge
            .evaluate_task(&task, &result, &role, "/tmp")
            .await
            .unwrap();

        assert!(evaluation.overall_score > 0.0);
        assert!(evaluation.overall_score <= 1.0);
        assert!(!evaluation.issues.is_empty()); // Should find missing tests
    }

  ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:205-211 method default <-> ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:587-598 method evaluation_to_issues
  Similarity: 92.32%
  Classes: Default <-> LLMQualityJudge

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:default (lines 205-211) ---[0m
    fn default() -> Self {
        Self {
            rubric: QualityRubric::default(),
            evaluation_cache: HashMap::new(),
            use_claude: true,
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:evaluation_to_issues (lines 587-598) ---[0m
    pub fn evaluation_to_issues(&self, evaluation: &QualityEvaluation) -> Vec<String> {
        evaluation
            .issues
            .iter()
            .map(|issue| {
                format!(
                    "[{:?}] {:?}: {}",
                    issue.severity, issue.category, issue.description
                )
            })
            .collect()
    }

  ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:276-283 method extract_content_from_result <-> ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:587-598 method evaluation_to_issues
  Similarity: 76.39%
  Classes: LLMQualityJudge <-> LLMQualityJudge

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:extract_content_from_result (lines 276-283) ---[0m
    fn extract_content_from_result(&self, result: &TaskResult) -> Result<String> {
        // Extract the response field from the JSON output
        if let Some(response) = result.output.get("response") {
            Ok(response.as_str().unwrap_or("").to_string())
        } else {
            Ok(result.output.to_string())
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/llm_quality_judge.rs:evaluation_to_issues (lines 587-598) ---[0m
    pub fn evaluation_to_issues(&self, evaluation: &QualityEvaluation) -> Vec<String> {
        evaluation
            .issues
            .iter()
            .map(|issue| {
                format!(
                    "[{:?}] {:?}: {}",
                    issue.severity, issue.category, issue.description
                )
            })
            .collect()
    }


Duplicates in ./crates/ccswarm/src/providers/claude_code.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/providers/claude_code.rs:105-175 method execute_claude_command <-> ./crates/ccswarm/src/providers/claude_code.rs:178-208 method build_command_args
  Similarity: 85.98%
  Classes: ClaudeCodeExecutor <-> ClaudeCodeExecutor

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:execute_claude_command (lines 105-175) ---[0m
    async fn execute_claude_command(
        &self,
        args: Vec<String>,
        working_dir: &Path,
        identity: &AgentIdentity,
    ) -> Result<String> {
        let mut cmd = Command::new("claude");

        // Set working directory - always use current directory to avoid Node.js module issues
        // This is a workaround for the yoga.wasm module resolution problem
        cmd.current_dir(".");

        // Add identity environment variables
        for (key, value) in &identity.env_vars {
            cmd.env(key, value);
        }

        // Add provider environment variables
        for (key, value) in self.config.get_env_vars() {
            cmd.env(key, value);
        }

        // Add command arguments
        cmd.args(&args);

        tracing::info!(
            "Executing Claude Code with args: {:?} in dir: {}",
            args,
            working_dir.display()
        );

        // Execute command with timeout
        let start = Instant::now();
        let output = tokio::time::timeout(
            std::time::Duration::from_secs(300), // 5 minute timeout
            cmd.output(),
        )
        .await
        .context("Claude Code command timed out")?
        .context("Failed to execute Claude Code")?;

        let duration = start.elapsed();

        tracing::debug!(
            "Claude Code execution completed in {:?} for agent {}",
            duration,
            identity.agent_id
        );

        if output.status.success() {
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            if stdout.trim().is_empty() {
                // Sometimes Claude outputs to stderr even on success
                let stderr = String::from_utf8_lossy(&output.stderr).to_string();
                if !stderr.trim().is_empty() {
                    return Ok(stderr);
                }
            }
            Ok(stdout)
        } else {
            let stderr = String::from_utf8_lossy(&output.stderr);
            let stdout = String::from_utf8_lossy(&output.stdout);

            Err(anyhow::anyhow!(
                "Claude Code execution failed (exit code: {:?})\nStderr: {}\nStdout: {}",
                output.status.code(),
                stderr,
                stdout
            ))
        }
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:build_command_args (lines 178-208) ---[0m
    fn build_command_args(&self, prompt: &str) -> Vec<String> {
        let mut args = Vec::new();

        // Add prompt
        args.push("-p".to_string());
        args.push(prompt.to_string());

        // Add JSON output if enabled
        if self.config.json_output {
            args.push("--json".to_string());
        }

        // Add dangerous skip if enabled
        if self.config.dangerous_skip {
            args.push("--dangerously-skip-permissions".to_string());
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.config.think_mode {
            args.push("--think".to_string());
            args.push(think_mode.clone());
        }

        // Add model if not default
        if self.config.model != "claude-3.5-sonnet" {
            args.push("--model".to_string());
            args.push(self.config.model.clone());
        }

        args
    }

  ./crates/ccswarm/src/providers/claude_code.rs:256-305 method execute_task <-> ./crates/ccswarm/src/providers/claude_code.rs:307-350 method health_check
  Similarity: 92.77%
  Classes: ProviderExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:execute_task (lines 256-305) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate enhanced prompt with identity and boundaries
        let prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with Claude Code for agent {}",
            task.description,
            identity.agent_id
        );

        // Execute the prompt
        match self.execute_prompt(&prompt, identity, working_dir).await {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_task_result(output, task, duration);

                tracing::info!(
                    "Task completed successfully in {:?} for agent {}",
                    duration,
                    identity.agent_id
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({}),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:health_check (lines 307-350) ---[0m
    async fn health_check(&self, working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        // Try to execute a simple version check
        let result = Command::new("claude")
            .arg("--version")
            .current_dir(working_dir)
            .output()
            .await;

        let duration = start.elapsed();
        let response_time_ms = duration.as_millis() as u64;

        match result {
            Ok(output) if output.status.success() => {
                let version = String::from_utf8_lossy(&output.stdout).trim().to_string();

                Ok(ProviderHealthStatus {
                    is_healthy: true,
                    version: Some(version),
                    last_check: chrono::Utc::now(),
                    error_message: None,
                    response_time_ms: Some(response_time_ms),
                })
            }
            Ok(output) => {
                let error = String::from_utf8_lossy(&output.stderr).to_string();
                Ok(ProviderHealthStatus {
                    is_healthy: false,
                    version: None,
                    last_check: chrono::Utc::now(),
                    error_message: Some(format!("Command failed: {}", error)),
                    response_time_ms: Some(response_time_ms),
                })
            }
            Err(e) => Ok(ProviderHealthStatus {
                is_healthy: false,
                version: None,
                last_check: chrono::Utc::now(),
                error_message: Some(format!("Failed to execute: {}", e)),
                response_time_ms: Some(response_time_ms),
            }),
        }
    }

  ./crates/ccswarm/src/providers/claude_code.rs:51-102 method generate_boundary_reminder <-> ./crates/ccswarm/src/providers/claude_code.rs:352-379 method get_capabilities
  Similarity: 98.29%
  Classes: ClaudeCodeExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:generate_boundary_reminder (lines 51-102) ---[0m
    fn generate_boundary_reminder(&self, specialization: &crate::identity::AgentRole) -> String {
        match specialization {
            crate::identity::AgentRole::Frontend { .. } => {
                "REMEMBER: You are a FRONTEND specialist. Focus ONLY on:\n\
                 - UI components and interfaces\n\
                 - Client-side logic and state management\n\
                 - Styling and responsive design\n\
                 - Frontend testing and optimization\n\
                 DELEGATE backend/API work to backend agents."
            }
            crate::identity::AgentRole::Backend { .. } => {
                "REMEMBER: You are a BACKEND specialist. Focus ONLY on:\n\
                 - API endpoints and server logic\n\
                 - Database operations and migrations\n\
                 - Authentication and authorization\n\
                 - Backend testing and performance\n\
                 DELEGATE frontend/UI work to frontend agents."
            }
            crate::identity::AgentRole::DevOps { .. } => {
                "REMEMBER: You are a DEVOPS specialist. Focus ONLY on:\n\
                 - Infrastructure and deployment\n\
                 - CI/CD pipelines and automation\n\
                 - Monitoring and observability\n\
                 - Security and compliance\n\
                 DELEGATE application code to dev agents."
            }
            crate::identity::AgentRole::QA { .. } => {
                "REMEMBER: You are a QA specialist. Focus ONLY on:\n\
                 - Test strategy and implementation\n\
                 - Quality assurance and validation\n\
                 - Bug reporting and regression testing\n\
                 - Performance and security testing\n\
                 DELEGATE implementation to dev agents."
            }
            crate::identity::AgentRole::Master { .. } => {
                "REMEMBER: You are the MASTER orchestrator. Focus ONLY on:\n\
                 - Coordinating between agents\n\
                 - Quality reviews and approvals\n\
                 - Strategic decisions and planning\n\
                 - NEVER write code directly - delegate to specialists."
            }
            crate::identity::AgentRole::Search { .. } => {
                "REMEMBER: You are a SEARCH specialist. Focus ONLY on:\n\
                 - Web search and information retrieval\n\
                 - Query optimization and filtering\n\
                 - Source evaluation and ranking\n\
                 - Result presentation and summarization\n\
                 - NEVER modify code - only gather information."
            }
        }
        .to_string()
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:get_capabilities (lines 352-379) ---[0m
    fn get_capabilities(&self) -> ProviderCapabilities {
        ProviderCapabilities {
            supports_json_output: true,
            supports_streaming: false, // Claude Code doesn't support streaming yet
            supports_file_operations: true,
            supports_git_operations: true,
            supports_code_execution: true,
            max_context_length: Some(200_000), // Claude 3.5 Sonnet context length
            supported_languages: vec![
                "rust".to_string(),
                "python".to_string(),
                "javascript".to_string(),
                "typescript".to_string(),
                "go".to_string(),
                "java".to_string(),
                "c++".to_string(),
                "c".to_string(),
                "c#".to_string(),
                "html".to_string(),
                "css".to_string(),
                "sql".to_string(),
                "bash".to_string(),
                "yaml".to_string(),
                "json".to_string(),
                "markdown".to_string(),
            ],
        }
    }

  ./crates/ccswarm/src/providers/claude_code.rs:51-102 method generate_boundary_reminder <-> ./crates/ccswarm/src/providers/claude_code.rs:256-305 method execute_task
  Similarity: 75.06%
  Classes: ClaudeCodeExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:generate_boundary_reminder (lines 51-102) ---[0m
    fn generate_boundary_reminder(&self, specialization: &crate::identity::AgentRole) -> String {
        match specialization {
            crate::identity::AgentRole::Frontend { .. } => {
                "REMEMBER: You are a FRONTEND specialist. Focus ONLY on:\n\
                 - UI components and interfaces\n\
                 - Client-side logic and state management\n\
                 - Styling and responsive design\n\
                 - Frontend testing and optimization\n\
                 DELEGATE backend/API work to backend agents."
            }
            crate::identity::AgentRole::Backend { .. } => {
                "REMEMBER: You are a BACKEND specialist. Focus ONLY on:\n\
                 - API endpoints and server logic\n\
                 - Database operations and migrations\n\
                 - Authentication and authorization\n\
                 - Backend testing and performance\n\
                 DELEGATE frontend/UI work to frontend agents."
            }
            crate::identity::AgentRole::DevOps { .. } => {
                "REMEMBER: You are a DEVOPS specialist. Focus ONLY on:\n\
                 - Infrastructure and deployment\n\
                 - CI/CD pipelines and automation\n\
                 - Monitoring and observability\n\
                 - Security and compliance\n\
                 DELEGATE application code to dev agents."
            }
            crate::identity::AgentRole::QA { .. } => {
                "REMEMBER: You are a QA specialist. Focus ONLY on:\n\
                 - Test strategy and implementation\n\
                 - Quality assurance and validation\n\
                 - Bug reporting and regression testing\n\
                 - Performance and security testing\n\
                 DELEGATE implementation to dev agents."
            }
            crate::identity::AgentRole::Master { .. } => {
                "REMEMBER: You are the MASTER orchestrator. Focus ONLY on:\n\
                 - Coordinating between agents\n\
                 - Quality reviews and approvals\n\
                 - Strategic decisions and planning\n\
                 - NEVER write code directly - delegate to specialists."
            }
            crate::identity::AgentRole::Search { .. } => {
                "REMEMBER: You are a SEARCH specialist. Focus ONLY on:\n\
                 - Web search and information retrieval\n\
                 - Query optimization and filtering\n\
                 - Source evaluation and ranking\n\
                 - Result presentation and summarization\n\
                 - NEVER modify code - only gather information."
            }
        }
        .to_string()
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:execute_task (lines 256-305) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate enhanced prompt with identity and boundaries
        let prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with Claude Code for agent {}",
            task.description,
            identity.agent_id
        );

        // Execute the prompt
        match self.execute_prompt(&prompt, identity, working_dir).await {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_task_result(output, task, duration);

                tracing::info!(
                    "Task completed successfully in {:?} for agent {}",
                    duration,
                    identity.agent_id
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({}),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/claude_code.rs:51-102 method generate_boundary_reminder <-> ./crates/ccswarm/src/providers/claude_code.rs:307-350 method health_check
  Similarity: 75.41%
  Classes: ClaudeCodeExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:generate_boundary_reminder (lines 51-102) ---[0m
    fn generate_boundary_reminder(&self, specialization: &crate::identity::AgentRole) -> String {
        match specialization {
            crate::identity::AgentRole::Frontend { .. } => {
                "REMEMBER: You are a FRONTEND specialist. Focus ONLY on:\n\
                 - UI components and interfaces\n\
                 - Client-side logic and state management\n\
                 - Styling and responsive design\n\
                 - Frontend testing and optimization\n\
                 DELEGATE backend/API work to backend agents."
            }
            crate::identity::AgentRole::Backend { .. } => {
                "REMEMBER: You are a BACKEND specialist. Focus ONLY on:\n\
                 - API endpoints and server logic\n\
                 - Database operations and migrations\n\
                 - Authentication and authorization\n\
                 - Backend testing and performance\n\
                 DELEGATE frontend/UI work to frontend agents."
            }
            crate::identity::AgentRole::DevOps { .. } => {
                "REMEMBER: You are a DEVOPS specialist. Focus ONLY on:\n\
                 - Infrastructure and deployment\n\
                 - CI/CD pipelines and automation\n\
                 - Monitoring and observability\n\
                 - Security and compliance\n\
                 DELEGATE application code to dev agents."
            }
            crate::identity::AgentRole::QA { .. } => {
                "REMEMBER: You are a QA specialist. Focus ONLY on:\n\
                 - Test strategy and implementation\n\
                 - Quality assurance and validation\n\
                 - Bug reporting and regression testing\n\
                 - Performance and security testing\n\
                 DELEGATE implementation to dev agents."
            }
            crate::identity::AgentRole::Master { .. } => {
                "REMEMBER: You are the MASTER orchestrator. Focus ONLY on:\n\
                 - Coordinating between agents\n\
                 - Quality reviews and approvals\n\
                 - Strategic decisions and planning\n\
                 - NEVER write code directly - delegate to specialists."
            }
            crate::identity::AgentRole::Search { .. } => {
                "REMEMBER: You are a SEARCH specialist. Focus ONLY on:\n\
                 - Web search and information retrieval\n\
                 - Query optimization and filtering\n\
                 - Source evaluation and ranking\n\
                 - Result presentation and summarization\n\
                 - NEVER modify code - only gather information."
            }
        }
        .to_string()
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:health_check (lines 307-350) ---[0m
    async fn health_check(&self, working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        // Try to execute a simple version check
        let result = Command::new("claude")
            .arg("--version")
            .current_dir(working_dir)
            .output()
            .await;

        let duration = start.elapsed();
        let response_time_ms = duration.as_millis() as u64;

        match result {
            Ok(output) if output.status.success() => {
                let version = String::from_utf8_lossy(&output.stdout).trim().to_string();

                Ok(ProviderHealthStatus {
                    is_healthy: true,
                    version: Some(version),
                    last_check: chrono::Utc::now(),
                    error_message: None,
                    response_time_ms: Some(response_time_ms),
                })
            }
            Ok(output) => {
                let error = String::from_utf8_lossy(&output.stderr).to_string();
                Ok(ProviderHealthStatus {
                    is_healthy: false,
                    version: None,
                    last_check: chrono::Utc::now(),
                    error_message: Some(format!("Command failed: {}", error)),
                    response_time_ms: Some(response_time_ms),
                })
            }
            Err(e) => Ok(ProviderHealthStatus {
                is_healthy: false,
                version: None,
                last_check: chrono::Utc::now(),
                error_message: Some(format!("Failed to execute: {}", e)),
                response_time_ms: Some(response_time_ms),
            }),
        }
    }

  ./crates/ccswarm/src/providers/claude_code.rs:211-240 method parse_task_result <-> ./crates/ccswarm/src/providers/claude_code.rs:256-305 method execute_task
  Similarity: 85.70%
  Classes: ClaudeCodeExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:parse_task_result (lines 211-240) ---[0m
    fn parse_task_result(
        &self,
        output: String,
        task: &Task,
        duration: std::time::Duration,
    ) -> TaskResult {
        // Try to parse as JSON if JSON output is enabled
        if self.config.json_output {
            if let Ok(json_value) = serde_json::from_str::<serde_json::Value>(&output) {
                return TaskResult {
                    success: true,
                    output: json_value,
                    error: None,
                    duration,
                };
            }
        }

        // Fallback to text output
        TaskResult {
            success: true,
            output: serde_json::json!({
                "response": output,
                "task_id": task.id,
                "format": "text"
            }),
            error: None,
            duration,
        }
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:execute_task (lines 256-305) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate enhanced prompt with identity and boundaries
        let prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with Claude Code for agent {}",
            task.description,
            identity.agent_id
        );

        // Execute the prompt
        match self.execute_prompt(&prompt, identity, working_dir).await {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_task_result(output, task, duration);

                tracing::info!(
                    "Task completed successfully in {:?} for agent {}",
                    duration,
                    identity.agent_id
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({}),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/claude_code.rs:51-102 method generate_boundary_reminder <-> ./crates/ccswarm/src/providers/claude_code.rs:391-406 function create_test_identity
  Similarity: 90.32%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:generate_boundary_reminder (lines 51-102) ---[0m
    fn generate_boundary_reminder(&self, specialization: &crate::identity::AgentRole) -> String {
        match specialization {
            crate::identity::AgentRole::Frontend { .. } => {
                "REMEMBER: You are a FRONTEND specialist. Focus ONLY on:\n\
                 - UI components and interfaces\n\
                 - Client-side logic and state management\n\
                 - Styling and responsive design\n\
                 - Frontend testing and optimization\n\
                 DELEGATE backend/API work to backend agents."
            }
            crate::identity::AgentRole::Backend { .. } => {
                "REMEMBER: You are a BACKEND specialist. Focus ONLY on:\n\
                 - API endpoints and server logic\n\
                 - Database operations and migrations\n\
                 - Authentication and authorization\n\
                 - Backend testing and performance\n\
                 DELEGATE frontend/UI work to frontend agents."
            }
            crate::identity::AgentRole::DevOps { .. } => {
                "REMEMBER: You are a DEVOPS specialist. Focus ONLY on:\n\
                 - Infrastructure and deployment\n\
                 - CI/CD pipelines and automation\n\
                 - Monitoring and observability\n\
                 - Security and compliance\n\
                 DELEGATE application code to dev agents."
            }
            crate::identity::AgentRole::QA { .. } => {
                "REMEMBER: You are a QA specialist. Focus ONLY on:\n\
                 - Test strategy and implementation\n\
                 - Quality assurance and validation\n\
                 - Bug reporting and regression testing\n\
                 - Performance and security testing\n\
                 DELEGATE implementation to dev agents."
            }
            crate::identity::AgentRole::Master { .. } => {
                "REMEMBER: You are the MASTER orchestrator. Focus ONLY on:\n\
                 - Coordinating between agents\n\
                 - Quality reviews and approvals\n\
                 - Strategic decisions and planning\n\
                 - NEVER write code directly - delegate to specialists."
            }
            crate::identity::AgentRole::Search { .. } => {
                "REMEMBER: You are a SEARCH specialist. Focus ONLY on:\n\
                 - Web search and information retrieval\n\
                 - Query optimization and filtering\n\
                 - Source evaluation and ranking\n\
                 - Result presentation and summarization\n\
                 - NEVER modify code - only gather information."
            }
        }
        .to_string()
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:create_test_identity (lines 391-406) ---[0m
    fn create_test_identity() -> AgentIdentity {
        let temp_dir = TempDir::new().unwrap();
        AgentIdentity {
            agent_id: "test-agent".to_string(),
            specialization: AgentRole::Frontend {
                technologies: vec!["React".to_string(), "TypeScript".to_string()],
                responsibilities: vec!["UI components".to_string()],
                boundaries: vec!["No backend work".to_string()],
            },
            workspace_path: temp_dir.path().to_path_buf(),
            env_vars: HashMap::new(),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: "12345".to_string(),
            initialized_at: chrono::Utc::now(),
        }
    }

  ./crates/ccswarm/src/providers/claude_code.rs:178-208 method build_command_args <-> ./crates/ccswarm/src/providers/claude_code.rs:307-350 method health_check
  Similarity: 80.26%
  Classes: ClaudeCodeExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:build_command_args (lines 178-208) ---[0m
    fn build_command_args(&self, prompt: &str) -> Vec<String> {
        let mut args = Vec::new();

        // Add prompt
        args.push("-p".to_string());
        args.push(prompt.to_string());

        // Add JSON output if enabled
        if self.config.json_output {
            args.push("--json".to_string());
        }

        // Add dangerous skip if enabled
        if self.config.dangerous_skip {
            args.push("--dangerously-skip-permissions".to_string());
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.config.think_mode {
            args.push("--think".to_string());
            args.push(think_mode.clone());
        }

        // Add model if not default
        if self.config.model != "claude-3.5-sonnet" {
            args.push("--model".to_string());
            args.push(self.config.model.clone());
        }

        args
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:health_check (lines 307-350) ---[0m
    async fn health_check(&self, working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        // Try to execute a simple version check
        let result = Command::new("claude")
            .arg("--version")
            .current_dir(working_dir)
            .output()
            .await;

        let duration = start.elapsed();
        let response_time_ms = duration.as_millis() as u64;

        match result {
            Ok(output) if output.status.success() => {
                let version = String::from_utf8_lossy(&output.stdout).trim().to_string();

                Ok(ProviderHealthStatus {
                    is_healthy: true,
                    version: Some(version),
                    last_check: chrono::Utc::now(),
                    error_message: None,
                    response_time_ms: Some(response_time_ms),
                })
            }
            Ok(output) => {
                let error = String::from_utf8_lossy(&output.stderr).to_string();
                Ok(ProviderHealthStatus {
                    is_healthy: false,
                    version: None,
                    last_check: chrono::Utc::now(),
                    error_message: Some(format!("Command failed: {}", error)),
                    response_time_ms: Some(response_time_ms),
                })
            }
            Err(e) => Ok(ProviderHealthStatus {
                is_healthy: false,
                version: None,
                last_check: chrono::Utc::now(),
                error_message: Some(format!("Failed to execute: {}", e)),
                response_time_ms: Some(response_time_ms),
            }),
        }
    }

  ./crates/ccswarm/src/providers/claude_code.rs:25-48 method generate_task_prompt <-> ./crates/ccswarm/src/providers/claude_code.rs:256-305 method execute_task
  Similarity: 81.08%
  Classes: ClaudeCodeExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:generate_task_prompt (lines 25-48) ---[0m
    fn generate_task_prompt(&self, identity: &AgentIdentity, task: &Task) -> String {
        let agent_header = format!(
            "🤖 AGENT: {}\n📁 WORKSPACE: {}\n🎯 SCOPE: {:?}\n",
            identity.specialization.name(),
            identity.workspace_path.display(),
            task.task_type
        );

        let task_context = format!(
            "TASK ID: {}\nTASK TYPE: {:?}\nPRIORITY: {:?}\n",
            task.id, task.task_type, task.priority
        );

        let boundaries = self.generate_boundary_reminder(&identity.specialization);

        format!(
            "{}\n{}\n{}\nTASK DESCRIPTION:\n{}\n\n{}",
            agent_header,
            task_context,
            boundaries,
            task.description,
            task.details.as_deref().unwrap_or("")
        )
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:execute_task (lines 256-305) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate enhanced prompt with identity and boundaries
        let prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with Claude Code for agent {}",
            task.description,
            identity.agent_id
        );

        // Execute the prompt
        match self.execute_prompt(&prompt, identity, working_dir).await {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_task_result(output, task, duration);

                tracing::info!(
                    "Task completed successfully in {:?} for agent {}",
                    duration,
                    identity.agent_id
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({}),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/claude_code.rs:51-102 method generate_boundary_reminder <-> ./crates/ccswarm/src/providers/claude_code.rs:211-240 method parse_task_result
  Similarity: 71.87%
  Classes: ClaudeCodeExecutor <-> ClaudeCodeExecutor

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:generate_boundary_reminder (lines 51-102) ---[0m
    fn generate_boundary_reminder(&self, specialization: &crate::identity::AgentRole) -> String {
        match specialization {
            crate::identity::AgentRole::Frontend { .. } => {
                "REMEMBER: You are a FRONTEND specialist. Focus ONLY on:\n\
                 - UI components and interfaces\n\
                 - Client-side logic and state management\n\
                 - Styling and responsive design\n\
                 - Frontend testing and optimization\n\
                 DELEGATE backend/API work to backend agents."
            }
            crate::identity::AgentRole::Backend { .. } => {
                "REMEMBER: You are a BACKEND specialist. Focus ONLY on:\n\
                 - API endpoints and server logic\n\
                 - Database operations and migrations\n\
                 - Authentication and authorization\n\
                 - Backend testing and performance\n\
                 DELEGATE frontend/UI work to frontend agents."
            }
            crate::identity::AgentRole::DevOps { .. } => {
                "REMEMBER: You are a DEVOPS specialist. Focus ONLY on:\n\
                 - Infrastructure and deployment\n\
                 - CI/CD pipelines and automation\n\
                 - Monitoring and observability\n\
                 - Security and compliance\n\
                 DELEGATE application code to dev agents."
            }
            crate::identity::AgentRole::QA { .. } => {
                "REMEMBER: You are a QA specialist. Focus ONLY on:\n\
                 - Test strategy and implementation\n\
                 - Quality assurance and validation\n\
                 - Bug reporting and regression testing\n\
                 - Performance and security testing\n\
                 DELEGATE implementation to dev agents."
            }
            crate::identity::AgentRole::Master { .. } => {
                "REMEMBER: You are the MASTER orchestrator. Focus ONLY on:\n\
                 - Coordinating between agents\n\
                 - Quality reviews and approvals\n\
                 - Strategic decisions and planning\n\
                 - NEVER write code directly - delegate to specialists."
            }
            crate::identity::AgentRole::Search { .. } => {
                "REMEMBER: You are a SEARCH specialist. Focus ONLY on:\n\
                 - Web search and information retrieval\n\
                 - Query optimization and filtering\n\
                 - Source evaluation and ranking\n\
                 - Result presentation and summarization\n\
                 - NEVER modify code - only gather information."
            }
        }
        .to_string()
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:parse_task_result (lines 211-240) ---[0m
    fn parse_task_result(
        &self,
        output: String,
        task: &Task,
        duration: std::time::Duration,
    ) -> TaskResult {
        // Try to parse as JSON if JSON output is enabled
        if self.config.json_output {
            if let Ok(json_value) = serde_json::from_str::<serde_json::Value>(&output) {
                return TaskResult {
                    success: true,
                    output: json_value,
                    error: None,
                    duration,
                };
            }
        }

        // Fallback to text output
        TaskResult {
            success: true,
            output: serde_json::json!({
                "response": output,
                "task_id": task.id,
                "format": "text"
            }),
            error: None,
            duration,
        }
    }

  ./crates/ccswarm/src/providers/claude_code.rs:178-208 method build_command_args <-> ./crates/ccswarm/src/providers/claude_code.rs:256-305 method execute_task
  Similarity: 70.81%
  Classes: ClaudeCodeExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:build_command_args (lines 178-208) ---[0m
    fn build_command_args(&self, prompt: &str) -> Vec<String> {
        let mut args = Vec::new();

        // Add prompt
        args.push("-p".to_string());
        args.push(prompt.to_string());

        // Add JSON output if enabled
        if self.config.json_output {
            args.push("--json".to_string());
        }

        // Add dangerous skip if enabled
        if self.config.dangerous_skip {
            args.push("--dangerously-skip-permissions".to_string());
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.config.think_mode {
            args.push("--think".to_string());
            args.push(think_mode.clone());
        }

        // Add model if not default
        if self.config.model != "claude-3.5-sonnet" {
            args.push("--model".to_string());
            args.push(self.config.model.clone());
        }

        args
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:execute_task (lines 256-305) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate enhanced prompt with identity and boundaries
        let prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with Claude Code for agent {}",
            task.description,
            identity.agent_id
        );

        // Execute the prompt
        match self.execute_prompt(&prompt, identity, working_dir).await {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_task_result(output, task, duration);

                tracing::info!(
                    "Task completed successfully in {:?} for agent {}",
                    duration,
                    identity.agent_id
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({}),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/claude_code.rs:256-305 method execute_task <-> ./crates/ccswarm/src/providers/claude_code.rs:526-540 function test_boundary_reminder_generation
  Similarity: 84.22%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:execute_task (lines 256-305) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate enhanced prompt with identity and boundaries
        let prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with Claude Code for agent {}",
            task.description,
            identity.agent_id
        );

        // Execute the prompt
        match self.execute_prompt(&prompt, identity, working_dir).await {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_task_result(output, task, duration);

                tracing::info!(
                    "Task completed successfully in {:?} for agent {}",
                    duration,
                    identity.agent_id
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({}),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_boundary_reminder_generation (lines 526-540) ---[0m
    fn test_boundary_reminder_generation() {
        let config = ClaudeCodeConfig::default();
        let executor = ClaudeCodeExecutor::new(config);

        let frontend_role = AgentRole::Frontend {
            technologies: vec!["React".to_string()],
            responsibilities: vec!["UI".to_string()],
            boundaries: vec!["No backend".to_string()],
        };

        let reminder = executor.generate_boundary_reminder(&frontend_role);
        assert!(reminder.contains("FRONTEND specialist"));
        assert!(reminder.contains("DELEGATE backend"));
        assert!(reminder.contains("UI components"));
    }

  ./crates/ccswarm/src/providers/claude_code.rs:307-350 method health_check <-> ./crates/ccswarm/src/providers/claude_code.rs:484-503 function test_parse_task_result_text
  Similarity: 77.18%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:health_check (lines 307-350) ---[0m
    async fn health_check(&self, working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        // Try to execute a simple version check
        let result = Command::new("claude")
            .arg("--version")
            .current_dir(working_dir)
            .output()
            .await;

        let duration = start.elapsed();
        let response_time_ms = duration.as_millis() as u64;

        match result {
            Ok(output) if output.status.success() => {
                let version = String::from_utf8_lossy(&output.stdout).trim().to_string();

                Ok(ProviderHealthStatus {
                    is_healthy: true,
                    version: Some(version),
                    last_check: chrono::Utc::now(),
                    error_message: None,
                    response_time_ms: Some(response_time_ms),
                })
            }
            Ok(output) => {
                let error = String::from_utf8_lossy(&output.stderr).to_string();
                Ok(ProviderHealthStatus {
                    is_healthy: false,
                    version: None,
                    last_check: chrono::Utc::now(),
                    error_message: Some(format!("Command failed: {}", error)),
                    response_time_ms: Some(response_time_ms),
                })
            }
            Err(e) => Ok(ProviderHealthStatus {
                is_healthy: false,
                version: None,
                last_check: chrono::Utc::now(),
                error_message: Some(format!("Failed to execute: {}", e)),
                response_time_ms: Some(response_time_ms),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_parse_task_result_text (lines 484-503) ---[0m
    fn test_parse_task_result_text() {
        let config = ClaudeCodeConfig {
            json_output: false,
            ..Default::default()
        };
        let executor = ClaudeCodeExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let text_output = "Task completed successfully!";
        let result = executor.parse_task_result(text_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        // Should wrap in JSON with text format
        let output = result.output.as_object().unwrap();
        assert_eq!(output["response"], "Task completed successfully!");
        assert_eq!(output["format"], "text");
    }

  ./crates/ccswarm/src/providers/claude_code.rs:307-350 method health_check <-> ./crates/ccswarm/src/providers/claude_code.rs:441-458 function test_build_command_args
  Similarity: 78.28%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:health_check (lines 307-350) ---[0m
    async fn health_check(&self, working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        // Try to execute a simple version check
        let result = Command::new("claude")
            .arg("--version")
            .current_dir(working_dir)
            .output()
            .await;

        let duration = start.elapsed();
        let response_time_ms = duration.as_millis() as u64;

        match result {
            Ok(output) if output.status.success() => {
                let version = String::from_utf8_lossy(&output.stdout).trim().to_string();

                Ok(ProviderHealthStatus {
                    is_healthy: true,
                    version: Some(version),
                    last_check: chrono::Utc::now(),
                    error_message: None,
                    response_time_ms: Some(response_time_ms),
                })
            }
            Ok(output) => {
                let error = String::from_utf8_lossy(&output.stderr).to_string();
                Ok(ProviderHealthStatus {
                    is_healthy: false,
                    version: None,
                    last_check: chrono::Utc::now(),
                    error_message: Some(format!("Command failed: {}", error)),
                    response_time_ms: Some(response_time_ms),
                })
            }
            Err(e) => Ok(ProviderHealthStatus {
                is_healthy: false,
                version: None,
                last_check: chrono::Utc::now(),
                error_message: Some(format!("Failed to execute: {}", e)),
                response_time_ms: Some(response_time_ms),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_build_command_args (lines 441-458) ---[0m
    fn test_build_command_args() {
        let config = ClaudeCodeConfig {
            json_output: true,
            dangerous_skip: true,
            think_mode: Some("think_hard".to_string()),
            ..Default::default()
        };

        let executor = ClaudeCodeExecutor::new(config);
        let args = executor.build_command_args("test prompt");

        assert!(args.contains(&"-p".to_string()));
        assert!(args.contains(&"test prompt".to_string()));
        assert!(args.contains(&"--json".to_string()));
        assert!(args.contains(&"--dangerously-skip-permissions".to_string()));
        assert!(args.contains(&"--think".to_string()));
        assert!(args.contains(&"think_hard".to_string()));
    }

  ./crates/ccswarm/src/providers/claude_code.rs:307-350 method health_check <-> ./crates/ccswarm/src/providers/claude_code.rs:461-481 function test_parse_task_result_json
  Similarity: 74.54%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:health_check (lines 307-350) ---[0m
    async fn health_check(&self, working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        // Try to execute a simple version check
        let result = Command::new("claude")
            .arg("--version")
            .current_dir(working_dir)
            .output()
            .await;

        let duration = start.elapsed();
        let response_time_ms = duration.as_millis() as u64;

        match result {
            Ok(output) if output.status.success() => {
                let version = String::from_utf8_lossy(&output.stdout).trim().to_string();

                Ok(ProviderHealthStatus {
                    is_healthy: true,
                    version: Some(version),
                    last_check: chrono::Utc::now(),
                    error_message: None,
                    response_time_ms: Some(response_time_ms),
                })
            }
            Ok(output) => {
                let error = String::from_utf8_lossy(&output.stderr).to_string();
                Ok(ProviderHealthStatus {
                    is_healthy: false,
                    version: None,
                    last_check: chrono::Utc::now(),
                    error_message: Some(format!("Command failed: {}", error)),
                    response_time_ms: Some(response_time_ms),
                })
            }
            Err(e) => Ok(ProviderHealthStatus {
                is_healthy: false,
                version: None,
                last_check: chrono::Utc::now(),
                error_message: Some(format!("Failed to execute: {}", e)),
                response_time_ms: Some(response_time_ms),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_parse_task_result_json (lines 461-481) ---[0m
    fn test_parse_task_result_json() {
        let config = ClaudeCodeConfig {
            json_output: true,
            ..Default::default()
        };
        let executor = ClaudeCodeExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let json_output = r#"{"status": "completed", "files_changed": 2}"#;
        let result = executor.parse_task_result(json_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());
        assert_eq!(result.duration, duration);

        // Should parse as JSON
        let output = result.output.as_object().unwrap();
        assert_eq!(output["status"], "completed");
        assert_eq!(output["files_changed"], 2);
    }

  ./crates/ccswarm/src/providers/claude_code.rs:256-305 method execute_task <-> ./crates/ccswarm/src/providers/claude_code.rs:426-438 function test_generate_task_prompt
  Similarity: 75.62%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:execute_task (lines 256-305) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate enhanced prompt with identity and boundaries
        let prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with Claude Code for agent {}",
            task.description,
            identity.agent_id
        );

        // Execute the prompt
        match self.execute_prompt(&prompt, identity, working_dir).await {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_task_result(output, task, duration);

                tracing::info!(
                    "Task completed successfully in {:?} for agent {}",
                    duration,
                    identity.agent_id
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({}),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_generate_task_prompt (lines 426-438) ---[0m
    fn test_generate_task_prompt() {
        let config = ClaudeCodeConfig::default();
        let executor = ClaudeCodeExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let prompt = executor.generate_task_prompt(&identity, &task);

        assert!(prompt.contains("🤖 AGENT: Frontend"));
        assert!(prompt.contains("FRONTEND specialist"));
        assert!(prompt.contains("Create a React component"));
        assert!(prompt.contains("DELEGATE backend"));
    }

  ./crates/ccswarm/src/providers/claude_code.rs:178-208 method build_command_args <-> ./crates/ccswarm/src/providers/claude_code.rs:484-503 function test_parse_task_result_text
  Similarity: 80.81%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:build_command_args (lines 178-208) ---[0m
    fn build_command_args(&self, prompt: &str) -> Vec<String> {
        let mut args = Vec::new();

        // Add prompt
        args.push("-p".to_string());
        args.push(prompt.to_string());

        // Add JSON output if enabled
        if self.config.json_output {
            args.push("--json".to_string());
        }

        // Add dangerous skip if enabled
        if self.config.dangerous_skip {
            args.push("--dangerously-skip-permissions".to_string());
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.config.think_mode {
            args.push("--think".to_string());
            args.push(think_mode.clone());
        }

        // Add model if not default
        if self.config.model != "claude-3.5-sonnet" {
            args.push("--model".to_string());
            args.push(self.config.model.clone());
        }

        args
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_parse_task_result_text (lines 484-503) ---[0m
    fn test_parse_task_result_text() {
        let config = ClaudeCodeConfig {
            json_output: false,
            ..Default::default()
        };
        let executor = ClaudeCodeExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let text_output = "Task completed successfully!";
        let result = executor.parse_task_result(text_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        // Should wrap in JSON with text format
        let output = result.output.as_object().unwrap();
        assert_eq!(output["response"], "Task completed successfully!");
        assert_eq!(output["format"], "text");
    }

  ./crates/ccswarm/src/providers/claude_code.rs:178-208 method build_command_args <-> ./crates/ccswarm/src/providers/claude_code.rs:461-481 function test_parse_task_result_json
  Similarity: 77.49%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:build_command_args (lines 178-208) ---[0m
    fn build_command_args(&self, prompt: &str) -> Vec<String> {
        let mut args = Vec::new();

        // Add prompt
        args.push("-p".to_string());
        args.push(prompt.to_string());

        // Add JSON output if enabled
        if self.config.json_output {
            args.push("--json".to_string());
        }

        // Add dangerous skip if enabled
        if self.config.dangerous_skip {
            args.push("--dangerously-skip-permissions".to_string());
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.config.think_mode {
            args.push("--think".to_string());
            args.push(think_mode.clone());
        }

        // Add model if not default
        if self.config.model != "claude-3.5-sonnet" {
            args.push("--model".to_string());
            args.push(self.config.model.clone());
        }

        args
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_parse_task_result_json (lines 461-481) ---[0m
    fn test_parse_task_result_json() {
        let config = ClaudeCodeConfig {
            json_output: true,
            ..Default::default()
        };
        let executor = ClaudeCodeExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let json_output = r#"{"status": "completed", "files_changed": 2}"#;
        let result = executor.parse_task_result(json_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());
        assert_eq!(result.duration, duration);

        // Should parse as JSON
        let output = result.output.as_object().unwrap();
        assert_eq!(output["status"], "completed");
        assert_eq!(output["files_changed"], 2);
    }

  ./crates/ccswarm/src/providers/claude_code.rs:461-481 function test_parse_task_result_json <-> ./crates/ccswarm/src/providers/claude_code.rs:484-503 function test_parse_task_result_text
  Similarity: 95.79%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_parse_task_result_json (lines 461-481) ---[0m
    fn test_parse_task_result_json() {
        let config = ClaudeCodeConfig {
            json_output: true,
            ..Default::default()
        };
        let executor = ClaudeCodeExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let json_output = r#"{"status": "completed", "files_changed": 2}"#;
        let result = executor.parse_task_result(json_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());
        assert_eq!(result.duration, duration);

        // Should parse as JSON
        let output = result.output.as_object().unwrap();
        assert_eq!(output["status"], "completed");
        assert_eq!(output["files_changed"], 2);
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_parse_task_result_text (lines 484-503) ---[0m
    fn test_parse_task_result_text() {
        let config = ClaudeCodeConfig {
            json_output: false,
            ..Default::default()
        };
        let executor = ClaudeCodeExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let text_output = "Task completed successfully!";
        let result = executor.parse_task_result(text_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        // Should wrap in JSON with text format
        let output = result.output.as_object().unwrap();
        assert_eq!(output["response"], "Task completed successfully!");
        assert_eq!(output["format"], "text");
    }

  ./crates/ccswarm/src/providers/claude_code.rs:178-208 method build_command_args <-> ./crates/ccswarm/src/providers/claude_code.rs:506-523 function test_get_capabilities
  Similarity: 79.07%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:build_command_args (lines 178-208) ---[0m
    fn build_command_args(&self, prompt: &str) -> Vec<String> {
        let mut args = Vec::new();

        // Add prompt
        args.push("-p".to_string());
        args.push(prompt.to_string());

        // Add JSON output if enabled
        if self.config.json_output {
            args.push("--json".to_string());
        }

        // Add dangerous skip if enabled
        if self.config.dangerous_skip {
            args.push("--dangerously-skip-permissions".to_string());
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.config.think_mode {
            args.push("--think".to_string());
            args.push(think_mode.clone());
        }

        // Add model if not default
        if self.config.model != "claude-3.5-sonnet" {
            args.push("--model".to_string());
            args.push(self.config.model.clone());
        }

        args
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_get_capabilities (lines 506-523) ---[0m
    fn test_get_capabilities() {
        let config = ClaudeCodeConfig::default();
        let executor = ClaudeCodeExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert_eq!(capabilities.max_context_length, Some(200_000));
        assert!(capabilities
            .supported_languages
            .contains(&"rust".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"typescript".to_string()));
    }

  ./crates/ccswarm/src/providers/claude_code.rs:211-240 method parse_task_result <-> ./crates/ccswarm/src/providers/claude_code.rs:391-406 function create_test_identity
  Similarity: 83.80%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:parse_task_result (lines 211-240) ---[0m
    fn parse_task_result(
        &self,
        output: String,
        task: &Task,
        duration: std::time::Duration,
    ) -> TaskResult {
        // Try to parse as JSON if JSON output is enabled
        if self.config.json_output {
            if let Ok(json_value) = serde_json::from_str::<serde_json::Value>(&output) {
                return TaskResult {
                    success: true,
                    output: json_value,
                    error: None,
                    duration,
                };
            }
        }

        // Fallback to text output
        TaskResult {
            success: true,
            output: serde_json::json!({
                "response": output,
                "task_id": task.id,
                "format": "text"
            }),
            error: None,
            duration,
        }
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:create_test_identity (lines 391-406) ---[0m
    fn create_test_identity() -> AgentIdentity {
        let temp_dir = TempDir::new().unwrap();
        AgentIdentity {
            agent_id: "test-agent".to_string(),
            specialization: AgentRole::Frontend {
                technologies: vec!["React".to_string(), "TypeScript".to_string()],
                responsibilities: vec!["UI components".to_string()],
                boundaries: vec!["No backend work".to_string()],
            },
            workspace_path: temp_dir.path().to_path_buf(),
            env_vars: HashMap::new(),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: "12345".to_string(),
            initialized_at: chrono::Utc::now(),
        }
    }

  ./crates/ccswarm/src/providers/claude_code.rs:178-208 method build_command_args <-> ./crates/ccswarm/src/providers/claude_code.rs:441-458 function test_build_command_args
  Similarity: 77.61%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:build_command_args (lines 178-208) ---[0m
    fn build_command_args(&self, prompt: &str) -> Vec<String> {
        let mut args = Vec::new();

        // Add prompt
        args.push("-p".to_string());
        args.push(prompt.to_string());

        // Add JSON output if enabled
        if self.config.json_output {
            args.push("--json".to_string());
        }

        // Add dangerous skip if enabled
        if self.config.dangerous_skip {
            args.push("--dangerously-skip-permissions".to_string());
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.config.think_mode {
            args.push("--think".to_string());
            args.push(think_mode.clone());
        }

        // Add model if not default
        if self.config.model != "claude-3.5-sonnet" {
            args.push("--model".to_string());
            args.push(self.config.model.clone());
        }

        args
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_build_command_args (lines 441-458) ---[0m
    fn test_build_command_args() {
        let config = ClaudeCodeConfig {
            json_output: true,
            dangerous_skip: true,
            think_mode: Some("think_hard".to_string()),
            ..Default::default()
        };

        let executor = ClaudeCodeExecutor::new(config);
        let args = executor.build_command_args("test prompt");

        assert!(args.contains(&"-p".to_string()));
        assert!(args.contains(&"test prompt".to_string()));
        assert!(args.contains(&"--json".to_string()));
        assert!(args.contains(&"--dangerously-skip-permissions".to_string()));
        assert!(args.contains(&"--think".to_string()));
        assert!(args.contains(&"think_hard".to_string()));
    }

  ./crates/ccswarm/src/providers/claude_code.rs:352-379 method get_capabilities <-> ./crates/ccswarm/src/providers/claude_code.rs:391-406 function create_test_identity
  Similarity: 83.19%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:get_capabilities (lines 352-379) ---[0m
    fn get_capabilities(&self) -> ProviderCapabilities {
        ProviderCapabilities {
            supports_json_output: true,
            supports_streaming: false, // Claude Code doesn't support streaming yet
            supports_file_operations: true,
            supports_git_operations: true,
            supports_code_execution: true,
            max_context_length: Some(200_000), // Claude 3.5 Sonnet context length
            supported_languages: vec![
                "rust".to_string(),
                "python".to_string(),
                "javascript".to_string(),
                "typescript".to_string(),
                "go".to_string(),
                "java".to_string(),
                "c++".to_string(),
                "c".to_string(),
                "c#".to_string(),
                "html".to_string(),
                "css".to_string(),
                "sql".to_string(),
                "bash".to_string(),
                "yaml".to_string(),
                "json".to_string(),
                "markdown".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:create_test_identity (lines 391-406) ---[0m
    fn create_test_identity() -> AgentIdentity {
        let temp_dir = TempDir::new().unwrap();
        AgentIdentity {
            agent_id: "test-agent".to_string(),
            specialization: AgentRole::Frontend {
                technologies: vec!["React".to_string(), "TypeScript".to_string()],
                responsibilities: vec!["UI components".to_string()],
                boundaries: vec!["No backend work".to_string()],
            },
            workspace_path: temp_dir.path().to_path_buf(),
            env_vars: HashMap::new(),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: "12345".to_string(),
            initialized_at: chrono::Utc::now(),
        }
    }

  ./crates/ccswarm/src/providers/claude_code.rs:178-208 method build_command_args <-> ./crates/ccswarm/src/providers/claude_code.rs:526-540 function test_boundary_reminder_generation
  Similarity: 77.80%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:build_command_args (lines 178-208) ---[0m
    fn build_command_args(&self, prompt: &str) -> Vec<String> {
        let mut args = Vec::new();

        // Add prompt
        args.push("-p".to_string());
        args.push(prompt.to_string());

        // Add JSON output if enabled
        if self.config.json_output {
            args.push("--json".to_string());
        }

        // Add dangerous skip if enabled
        if self.config.dangerous_skip {
            args.push("--dangerously-skip-permissions".to_string());
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.config.think_mode {
            args.push("--think".to_string());
            args.push(think_mode.clone());
        }

        // Add model if not default
        if self.config.model != "claude-3.5-sonnet" {
            args.push("--model".to_string());
            args.push(self.config.model.clone());
        }

        args
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_boundary_reminder_generation (lines 526-540) ---[0m
    fn test_boundary_reminder_generation() {
        let config = ClaudeCodeConfig::default();
        let executor = ClaudeCodeExecutor::new(config);

        let frontend_role = AgentRole::Frontend {
            technologies: vec!["React".to_string()],
            responsibilities: vec!["UI".to_string()],
            boundaries: vec!["No backend".to_string()],
        };

        let reminder = executor.generate_boundary_reminder(&frontend_role);
        assert!(reminder.contains("FRONTEND specialist"));
        assert!(reminder.contains("DELEGATE backend"));
        assert!(reminder.contains("UI components"));
    }

  ./crates/ccswarm/src/providers/claude_code.rs:178-208 method build_command_args <-> ./crates/ccswarm/src/providers/claude_code.rs:426-438 function test_generate_task_prompt
  Similarity: 78.26%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:build_command_args (lines 178-208) ---[0m
    fn build_command_args(&self, prompt: &str) -> Vec<String> {
        let mut args = Vec::new();

        // Add prompt
        args.push("-p".to_string());
        args.push(prompt.to_string());

        // Add JSON output if enabled
        if self.config.json_output {
            args.push("--json".to_string());
        }

        // Add dangerous skip if enabled
        if self.config.dangerous_skip {
            args.push("--dangerously-skip-permissions".to_string());
        }

        // Add think mode if specified
        if let Some(think_mode) = &self.config.think_mode {
            args.push("--think".to_string());
            args.push(think_mode.clone());
        }

        // Add model if not default
        if self.config.model != "claude-3.5-sonnet" {
            args.push("--model".to_string());
            args.push(self.config.model.clone());
        }

        args
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_generate_task_prompt (lines 426-438) ---[0m
    fn test_generate_task_prompt() {
        let config = ClaudeCodeConfig::default();
        let executor = ClaudeCodeExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let prompt = executor.generate_task_prompt(&identity, &task);

        assert!(prompt.contains("🤖 AGENT: Frontend"));
        assert!(prompt.contains("FRONTEND specialist"));
        assert!(prompt.contains("Create a React component"));
        assert!(prompt.contains("DELEGATE backend"));
    }

  ./crates/ccswarm/src/providers/claude_code.rs:461-481 function test_parse_task_result_json <-> ./crates/ccswarm/src/providers/claude_code.rs:506-523 function test_get_capabilities
  Similarity: 76.84%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_parse_task_result_json (lines 461-481) ---[0m
    fn test_parse_task_result_json() {
        let config = ClaudeCodeConfig {
            json_output: true,
            ..Default::default()
        };
        let executor = ClaudeCodeExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let json_output = r#"{"status": "completed", "files_changed": 2}"#;
        let result = executor.parse_task_result(json_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());
        assert_eq!(result.duration, duration);

        // Should parse as JSON
        let output = result.output.as_object().unwrap();
        assert_eq!(output["status"], "completed");
        assert_eq!(output["files_changed"], 2);
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_get_capabilities (lines 506-523) ---[0m
    fn test_get_capabilities() {
        let config = ClaudeCodeConfig::default();
        let executor = ClaudeCodeExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert_eq!(capabilities.max_context_length, Some(200_000));
        assert!(capabilities
            .supported_languages
            .contains(&"rust".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"typescript".to_string()));
    }

  ./crates/ccswarm/src/providers/claude_code.rs:484-503 function test_parse_task_result_text <-> ./crates/ccswarm/src/providers/claude_code.rs:506-523 function test_get_capabilities
  Similarity: 77.91%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_parse_task_result_text (lines 484-503) ---[0m
    fn test_parse_task_result_text() {
        let config = ClaudeCodeConfig {
            json_output: false,
            ..Default::default()
        };
        let executor = ClaudeCodeExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let text_output = "Task completed successfully!";
        let result = executor.parse_task_result(text_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        // Should wrap in JSON with text format
        let output = result.output.as_object().unwrap();
        assert_eq!(output["response"], "Task completed successfully!");
        assert_eq!(output["format"], "text");
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_get_capabilities (lines 506-523) ---[0m
    fn test_get_capabilities() {
        let config = ClaudeCodeConfig::default();
        let executor = ClaudeCodeExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert_eq!(capabilities.max_context_length, Some(200_000));
        assert!(capabilities
            .supported_languages
            .contains(&"rust".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"typescript".to_string()));
    }

  ./crates/ccswarm/src/providers/claude_code.rs:441-458 function test_build_command_args <-> ./crates/ccswarm/src/providers/claude_code.rs:506-523 function test_get_capabilities
  Similarity: 80.18%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_build_command_args (lines 441-458) ---[0m
    fn test_build_command_args() {
        let config = ClaudeCodeConfig {
            json_output: true,
            dangerous_skip: true,
            think_mode: Some("think_hard".to_string()),
            ..Default::default()
        };

        let executor = ClaudeCodeExecutor::new(config);
        let args = executor.build_command_args("test prompt");

        assert!(args.contains(&"-p".to_string()));
        assert!(args.contains(&"test prompt".to_string()));
        assert!(args.contains(&"--json".to_string()));
        assert!(args.contains(&"--dangerously-skip-permissions".to_string()));
        assert!(args.contains(&"--think".to_string()));
        assert!(args.contains(&"think_hard".to_string()));
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_get_capabilities (lines 506-523) ---[0m
    fn test_get_capabilities() {
        let config = ClaudeCodeConfig::default();
        let executor = ClaudeCodeExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert_eq!(capabilities.max_context_length, Some(200_000));
        assert!(capabilities
            .supported_languages
            .contains(&"rust".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"typescript".to_string()));
    }

  ./crates/ccswarm/src/providers/claude_code.rs:441-458 function test_build_command_args <-> ./crates/ccswarm/src/providers/claude_code.rs:484-503 function test_parse_task_result_text
  Similarity: 75.04%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_build_command_args (lines 441-458) ---[0m
    fn test_build_command_args() {
        let config = ClaudeCodeConfig {
            json_output: true,
            dangerous_skip: true,
            think_mode: Some("think_hard".to_string()),
            ..Default::default()
        };

        let executor = ClaudeCodeExecutor::new(config);
        let args = executor.build_command_args("test prompt");

        assert!(args.contains(&"-p".to_string()));
        assert!(args.contains(&"test prompt".to_string()));
        assert!(args.contains(&"--json".to_string()));
        assert!(args.contains(&"--dangerously-skip-permissions".to_string()));
        assert!(args.contains(&"--think".to_string()));
        assert!(args.contains(&"think_hard".to_string()));
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_parse_task_result_text (lines 484-503) ---[0m
    fn test_parse_task_result_text() {
        let config = ClaudeCodeConfig {
            json_output: false,
            ..Default::default()
        };
        let executor = ClaudeCodeExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let text_output = "Task completed successfully!";
        let result = executor.parse_task_result(text_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        // Should wrap in JSON with text format
        let output = result.output.as_object().unwrap();
        assert_eq!(output["response"], "Task completed successfully!");
        assert_eq!(output["format"], "text");
    }

  ./crates/ccswarm/src/providers/claude_code.rs:441-458 function test_build_command_args <-> ./crates/ccswarm/src/providers/claude_code.rs:461-481 function test_parse_task_result_json
  Similarity: 72.02%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_build_command_args (lines 441-458) ---[0m
    fn test_build_command_args() {
        let config = ClaudeCodeConfig {
            json_output: true,
            dangerous_skip: true,
            think_mode: Some("think_hard".to_string()),
            ..Default::default()
        };

        let executor = ClaudeCodeExecutor::new(config);
        let args = executor.build_command_args("test prompt");

        assert!(args.contains(&"-p".to_string()));
        assert!(args.contains(&"test prompt".to_string()));
        assert!(args.contains(&"--json".to_string()));
        assert!(args.contains(&"--dangerously-skip-permissions".to_string()));
        assert!(args.contains(&"--think".to_string()));
        assert!(args.contains(&"think_hard".to_string()));
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_parse_task_result_json (lines 461-481) ---[0m
    fn test_parse_task_result_json() {
        let config = ClaudeCodeConfig {
            json_output: true,
            ..Default::default()
        };
        let executor = ClaudeCodeExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let json_output = r#"{"status": "completed", "files_changed": 2}"#;
        let result = executor.parse_task_result(json_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());
        assert_eq!(result.duration, duration);

        // Should parse as JSON
        let output = result.output.as_object().unwrap();
        assert_eq!(output["status"], "completed");
        assert_eq!(output["files_changed"], 2);
    }

  ./crates/ccswarm/src/providers/claude_code.rs:426-438 function test_generate_task_prompt <-> ./crates/ccswarm/src/providers/claude_code.rs:441-458 function test_build_command_args
  Similarity: 84.61%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_generate_task_prompt (lines 426-438) ---[0m
    fn test_generate_task_prompt() {
        let config = ClaudeCodeConfig::default();
        let executor = ClaudeCodeExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let prompt = executor.generate_task_prompt(&identity, &task);

        assert!(prompt.contains("🤖 AGENT: Frontend"));
        assert!(prompt.contains("FRONTEND specialist"));
        assert!(prompt.contains("Create a React component"));
        assert!(prompt.contains("DELEGATE backend"));
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_build_command_args (lines 441-458) ---[0m
    fn test_build_command_args() {
        let config = ClaudeCodeConfig {
            json_output: true,
            dangerous_skip: true,
            think_mode: Some("think_hard".to_string()),
            ..Default::default()
        };

        let executor = ClaudeCodeExecutor::new(config);
        let args = executor.build_command_args("test prompt");

        assert!(args.contains(&"-p".to_string()));
        assert!(args.contains(&"test prompt".to_string()));
        assert!(args.contains(&"--json".to_string()));
        assert!(args.contains(&"--dangerously-skip-permissions".to_string()));
        assert!(args.contains(&"--think".to_string()));
        assert!(args.contains(&"think_hard".to_string()));
    }

  ./crates/ccswarm/src/providers/claude_code.rs:426-438 function test_generate_task_prompt <-> ./crates/ccswarm/src/providers/claude_code.rs:484-503 function test_parse_task_result_text
  Similarity: 74.78%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_generate_task_prompt (lines 426-438) ---[0m
    fn test_generate_task_prompt() {
        let config = ClaudeCodeConfig::default();
        let executor = ClaudeCodeExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let prompt = executor.generate_task_prompt(&identity, &task);

        assert!(prompt.contains("🤖 AGENT: Frontend"));
        assert!(prompt.contains("FRONTEND specialist"));
        assert!(prompt.contains("Create a React component"));
        assert!(prompt.contains("DELEGATE backend"));
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_parse_task_result_text (lines 484-503) ---[0m
    fn test_parse_task_result_text() {
        let config = ClaudeCodeConfig {
            json_output: false,
            ..Default::default()
        };
        let executor = ClaudeCodeExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let text_output = "Task completed successfully!";
        let result = executor.parse_task_result(text_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        // Should wrap in JSON with text format
        let output = result.output.as_object().unwrap();
        assert_eq!(output["response"], "Task completed successfully!");
        assert_eq!(output["format"], "text");
    }

  ./crates/ccswarm/src/providers/claude_code.rs:426-438 function test_generate_task_prompt <-> ./crates/ccswarm/src/providers/claude_code.rs:461-481 function test_parse_task_result_json
  Similarity: 70.91%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_generate_task_prompt (lines 426-438) ---[0m
    fn test_generate_task_prompt() {
        let config = ClaudeCodeConfig::default();
        let executor = ClaudeCodeExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let prompt = executor.generate_task_prompt(&identity, &task);

        assert!(prompt.contains("🤖 AGENT: Frontend"));
        assert!(prompt.contains("FRONTEND specialist"));
        assert!(prompt.contains("Create a React component"));
        assert!(prompt.contains("DELEGATE backend"));
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_parse_task_result_json (lines 461-481) ---[0m
    fn test_parse_task_result_json() {
        let config = ClaudeCodeConfig {
            json_output: true,
            ..Default::default()
        };
        let executor = ClaudeCodeExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let json_output = r#"{"status": "completed", "files_changed": 2}"#;
        let result = executor.parse_task_result(json_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());
        assert_eq!(result.duration, duration);

        // Should parse as JSON
        let output = result.output.as_object().unwrap();
        assert_eq!(output["status"], "completed");
        assert_eq!(output["files_changed"], 2);
    }

  ./crates/ccswarm/src/providers/claude_code.rs:441-458 function test_build_command_args <-> ./crates/ccswarm/src/providers/claude_code.rs:526-540 function test_boundary_reminder_generation
  Similarity: 72.02%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_build_command_args (lines 441-458) ---[0m
    fn test_build_command_args() {
        let config = ClaudeCodeConfig {
            json_output: true,
            dangerous_skip: true,
            think_mode: Some("think_hard".to_string()),
            ..Default::default()
        };

        let executor = ClaudeCodeExecutor::new(config);
        let args = executor.build_command_args("test prompt");

        assert!(args.contains(&"-p".to_string()));
        assert!(args.contains(&"test prompt".to_string()));
        assert!(args.contains(&"--json".to_string()));
        assert!(args.contains(&"--dangerously-skip-permissions".to_string()));
        assert!(args.contains(&"--think".to_string()));
        assert!(args.contains(&"think_hard".to_string()));
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_boundary_reminder_generation (lines 526-540) ---[0m
    fn test_boundary_reminder_generation() {
        let config = ClaudeCodeConfig::default();
        let executor = ClaudeCodeExecutor::new(config);

        let frontend_role = AgentRole::Frontend {
            technologies: vec!["React".to_string()],
            responsibilities: vec!["UI".to_string()],
            boundaries: vec!["No backend".to_string()],
        };

        let reminder = executor.generate_boundary_reminder(&frontend_role);
        assert!(reminder.contains("FRONTEND specialist"));
        assert!(reminder.contains("DELEGATE backend"));
        assert!(reminder.contains("UI components"));
    }

  ./crates/ccswarm/src/providers/claude_code.rs:426-438 function test_generate_task_prompt <-> ./crates/ccswarm/src/providers/claude_code.rs:526-540 function test_boundary_reminder_generation
  Similarity: 81.49%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_generate_task_prompt (lines 426-438) ---[0m
    fn test_generate_task_prompt() {
        let config = ClaudeCodeConfig::default();
        let executor = ClaudeCodeExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let prompt = executor.generate_task_prompt(&identity, &task);

        assert!(prompt.contains("🤖 AGENT: Frontend"));
        assert!(prompt.contains("FRONTEND specialist"));
        assert!(prompt.contains("Create a React component"));
        assert!(prompt.contains("DELEGATE backend"));
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_boundary_reminder_generation (lines 526-540) ---[0m
    fn test_boundary_reminder_generation() {
        let config = ClaudeCodeConfig::default();
        let executor = ClaudeCodeExecutor::new(config);

        let frontend_role = AgentRole::Frontend {
            technologies: vec!["React".to_string()],
            responsibilities: vec!["UI".to_string()],
            boundaries: vec!["No backend".to_string()],
        };

        let reminder = executor.generate_boundary_reminder(&frontend_role);
        assert!(reminder.contains("FRONTEND specialist"));
        assert!(reminder.contains("DELEGATE backend"));
        assert!(reminder.contains("UI components"));
    }

  ./crates/ccswarm/src/providers/claude_code.rs:426-438 function test_generate_task_prompt <-> ./crates/ccswarm/src/providers/claude_code.rs:506-523 function test_get_capabilities
  Similarity: 70.54%

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_generate_task_prompt (lines 426-438) ---[0m
    fn test_generate_task_prompt() {
        let config = ClaudeCodeConfig::default();
        let executor = ClaudeCodeExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let prompt = executor.generate_task_prompt(&identity, &task);

        assert!(prompt.contains("🤖 AGENT: Frontend"));
        assert!(prompt.contains("FRONTEND specialist"));
        assert!(prompt.contains("Create a React component"));
        assert!(prompt.contains("DELEGATE backend"));
    }

[36m--- ./crates/ccswarm/src/providers/claude_code.rs:test_get_capabilities (lines 506-523) ---[0m
    fn test_get_capabilities() {
        let config = ClaudeCodeConfig::default();
        let executor = ClaudeCodeExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert_eq!(capabilities.max_context_length, Some(200_000));
        assert!(capabilities
            .supported_languages
            .contains(&"rust".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"typescript".to_string()));
    }


Duplicates in ./crates/ccswarm/src/orchestrator/auto_create.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/orchestrator/auto_create.rs:541-769 method create_frontend_files <-> ./crates/ccswarm/src/orchestrator/auto_create.rs:846-901 method create_devops_files
  Similarity: 97.95%
  Classes: AutoCreateEngine <-> AutoCreateEngine

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:create_frontend_files (lines 541-769) ---[0m
    async fn create_frontend_files(&self, output_path: &Path) -> Result<()> {
        // Create index.html
        let html_content = r#"<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TODO App - ccswarm Generated</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <div id="root"></div>
    <script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <script type="text/babel" src="app.js"></script>
</body>
</html>"#;

        tokio::fs::write(output_path.join("index.html"), html_content).await?;

        // Create app.js
        let app_content = r#"const { useState, useEffect } = React;

function TodoApp() {
    const [todos, setTodos] = useState([]);
    const [inputValue, setInputValue] = useState('');

    useEffect(() => {
        fetchTodos();
    }, []);

    const fetchTodos = async () => {
        try {
            const response = await fetch('http://localhost:3001/api/todos');
            const data = await response.json();
            setTodos(data);
        } catch (error) {
            console.error('Error fetching todos:', error);
        }
    };

    const addTodo = async () => {
        if (!inputValue.trim()) return;
        
        try {
            const response = await fetch('http://localhost:3001/api/todos', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ text: inputValue })
            });
            const newTodo = await response.json();
            setTodos([...todos, newTodo]);
            setInputValue('');
        } catch (error) {
            console.error('Error adding todo:', error);
        }
    };

    const toggleTodo = async (id) => {
        try {
            const todo = todos.find(t => t.id === id);
            const response = await fetch(`http://localhost:3001/api/todos/${id}`, {
                method: 'PUT',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ completed: !todo.completed })
            });
            const updatedTodo = await response.json();
            setTodos(todos.map(t => t.id === id ? updatedTodo : t));
        } catch (error) {
            console.error('Error updating todo:', error);
        }
    };

    const deleteTodo = async (id) => {
        try {
            await fetch(`http://localhost:3001/api/todos/${id}`, {
                method: 'DELETE'
            });
            setTodos(todos.filter(t => t.id !== id));
        } catch (error) {
            console.error('Error deleting todo:', error);
        }
    };

    return (
        <div className="todo-app">
            <h1>📝 TODO App</h1>
            <p className="subtitle">Generated by ccswarm 🤖</p>
            
            <div className="input-group">
                <input
                    type="text"
                    value={inputValue}
                    onChange={(e) => setInputValue(e.target.value)}
                    onKeyPress={(e) => e.key === 'Enter' && addTodo()}
                    placeholder="Add a new task..."
                />
                <button onClick={addTodo}>Add</button>
            </div>

            <ul className="todo-list">
                {todos.map(todo => (
                    <li key={todo.id} className={todo.completed ? 'completed' : ''}>
                        <input
                            type="checkbox"
                            checked={todo.completed}
                            onChange={() => toggleTodo(todo.id)}
                        />
                        <span>{todo.text}</span>
                        <button onClick={() => deleteTodo(todo.id)}>Delete</button>
                    </li>
                ))}
            </ul>
        </div>
    );
}

ReactDOM.render(<TodoApp />, document.getElementById('root'));"#;

        tokio::fs::write(output_path.join("app.js"), app_content).await?;

        // Create styles.css
        let styles_content = r#"* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
}

.todo-app {
    background: white;
    padding: 2rem;
    border-radius: 10px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
    width: 90%;
    max-width: 500px;
}

h1 {
    color: #333;
    text-align: center;
    margin-bottom: 0.5rem;
}

.subtitle {
    text-align: center;
    color: #666;
    font-size: 0.9rem;
    margin-bottom: 2rem;
}

.input-group {
    display: flex;
    margin-bottom: 1.5rem;
}

input[type="text"] {
    flex: 1;
    padding: 0.75rem;
    border: 2px solid #ddd;
    border-radius: 5px 0 0 5px;
    font-size: 1rem;
}

.input-group button {
    padding: 0.75rem 1.5rem;
    background: #667eea;
    color: white;
    border: none;
    border-radius: 0 5px 5px 0;
    cursor: pointer;
    font-size: 1rem;
}

.input-group button:hover {
    background: #5a67d8;
}

.todo-list {
    list-style: none;
}

.todo-list li {
    display: flex;
    align-items: center;
    padding: 1rem;
    border-bottom: 1px solid #eee;
}

.todo-list li.completed span {
    text-decoration: line-through;
    color: #999;
}

.todo-list input[type="checkbox"] {
    margin-right: 1rem;
}

.todo-list span {
    flex: 1;
}

.todo-list button {
    padding: 0.5rem 1rem;
    background: #f56565;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 0.9rem;
}

.todo-list button:hover {
    background: #e53e3e;
}"#;

        tokio::fs::write(output_path.join("styles.css"), styles_content).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:create_devops_files (lines 846-901) ---[0m
    async fn create_devops_files(&self, output_path: &Path) -> Result<()> {
        // Create package.json
        let package_content = r#"{
  "name": "todo-app-ccswarm",
  "version": "1.0.0",
  "description": "TODO application generated by ccswarm",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.5.0"
  }
}"#;

        tokio::fs::write(output_path.join("package.json"), package_content).await?;

        // Create Dockerfile
        let dockerfile_content = r#"FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 3001

CMD ["npm", "start"]"#;

        tokio::fs::write(output_path.join("Dockerfile"), dockerfile_content).await?;

        // Create docker-compose.yml
        let compose_content = r#"version: '3.8'

services:
  todo-app:
    build: .
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
    restart: unless-stopped"#;

        tokio::fs::write(output_path.join("docker-compose.yml"), compose_content).await?;

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/auto_create.rs:541-769 method create_frontend_files <-> ./crates/ccswarm/src/orchestrator/auto_create.rs:935-1013 method create_project_structure
  Similarity: 88.79%
  Classes: AutoCreateEngine <-> AutoCreateEngine

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:create_frontend_files (lines 541-769) ---[0m
    async fn create_frontend_files(&self, output_path: &Path) -> Result<()> {
        // Create index.html
        let html_content = r#"<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TODO App - ccswarm Generated</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <div id="root"></div>
    <script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <script type="text/babel" src="app.js"></script>
</body>
</html>"#;

        tokio::fs::write(output_path.join("index.html"), html_content).await?;

        // Create app.js
        let app_content = r#"const { useState, useEffect } = React;

function TodoApp() {
    const [todos, setTodos] = useState([]);
    const [inputValue, setInputValue] = useState('');

    useEffect(() => {
        fetchTodos();
    }, []);

    const fetchTodos = async () => {
        try {
            const response = await fetch('http://localhost:3001/api/todos');
            const data = await response.json();
            setTodos(data);
        } catch (error) {
            console.error('Error fetching todos:', error);
        }
    };

    const addTodo = async () => {
        if (!inputValue.trim()) return;
        
        try {
            const response = await fetch('http://localhost:3001/api/todos', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ text: inputValue })
            });
            const newTodo = await response.json();
            setTodos([...todos, newTodo]);
            setInputValue('');
        } catch (error) {
            console.error('Error adding todo:', error);
        }
    };

    const toggleTodo = async (id) => {
        try {
            const todo = todos.find(t => t.id === id);
            const response = await fetch(`http://localhost:3001/api/todos/${id}`, {
                method: 'PUT',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ completed: !todo.completed })
            });
            const updatedTodo = await response.json();
            setTodos(todos.map(t => t.id === id ? updatedTodo : t));
        } catch (error) {
            console.error('Error updating todo:', error);
        }
    };

    const deleteTodo = async (id) => {
        try {
            await fetch(`http://localhost:3001/api/todos/${id}`, {
                method: 'DELETE'
            });
            setTodos(todos.filter(t => t.id !== id));
        } catch (error) {
            console.error('Error deleting todo:', error);
        }
    };

    return (
        <div className="todo-app">
            <h1>📝 TODO App</h1>
            <p className="subtitle">Generated by ccswarm 🤖</p>
            
            <div className="input-group">
                <input
                    type="text"
                    value={inputValue}
                    onChange={(e) => setInputValue(e.target.value)}
                    onKeyPress={(e) => e.key === 'Enter' && addTodo()}
                    placeholder="Add a new task..."
                />
                <button onClick={addTodo}>Add</button>
            </div>

            <ul className="todo-list">
                {todos.map(todo => (
                    <li key={todo.id} className={todo.completed ? 'completed' : ''}>
                        <input
                            type="checkbox"
                            checked={todo.completed}
                            onChange={() => toggleTodo(todo.id)}
                        />
                        <span>{todo.text}</span>
                        <button onClick={() => deleteTodo(todo.id)}>Delete</button>
                    </li>
                ))}
            </ul>
        </div>
    );
}

ReactDOM.render(<TodoApp />, document.getElementById('root'));"#;

        tokio::fs::write(output_path.join("app.js"), app_content).await?;

        // Create styles.css
        let styles_content = r#"* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
}

.todo-app {
    background: white;
    padding: 2rem;
    border-radius: 10px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
    width: 90%;
    max-width: 500px;
}

h1 {
    color: #333;
    text-align: center;
    margin-bottom: 0.5rem;
}

.subtitle {
    text-align: center;
    color: #666;
    font-size: 0.9rem;
    margin-bottom: 2rem;
}

.input-group {
    display: flex;
    margin-bottom: 1.5rem;
}

input[type="text"] {
    flex: 1;
    padding: 0.75rem;
    border: 2px solid #ddd;
    border-radius: 5px 0 0 5px;
    font-size: 1rem;
}

.input-group button {
    padding: 0.75rem 1.5rem;
    background: #667eea;
    color: white;
    border: none;
    border-radius: 0 5px 5px 0;
    cursor: pointer;
    font-size: 1rem;
}

.input-group button:hover {
    background: #5a67d8;
}

.todo-list {
    list-style: none;
}

.todo-list li {
    display: flex;
    align-items: center;
    padding: 1rem;
    border-bottom: 1px solid #eee;
}

.todo-list li.completed span {
    text-decoration: line-through;
    color: #999;
}

.todo-list input[type="checkbox"] {
    margin-right: 1rem;
}

.todo-list span {
    flex: 1;
}

.todo-list button {
    padding: 0.5rem 1rem;
    background: #f56565;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 0.9rem;
}

.todo-list button:hover {
    background: #e53e3e;
}"#;

        tokio::fs::write(output_path.join("styles.css"), styles_content).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:create_project_structure (lines 935-1013) ---[0m
    async fn create_project_structure(&self, output_path: &Path) -> Result<()> {
        // Create README.md
        let readme_content = r#"# TODO App - Generated by ccswarm 🤖

This TODO application was automatically generated by ccswarm's multi-agent orchestration system.

## 🚀 Quick Start

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start the server:
   ```bash
   npm start
   ```

3. Open your browser to http://localhost:3001

## 🏗️ Architecture

- **Frontend**: React with vanilla JavaScript
- **Backend**: Express.js REST API
- **Storage**: In-memory (for demo purposes)
- **Deployment**: Docker-ready

## 🤖 Generated by ccswarm

This application was created by the following agents:
- **Frontend Agent**: Created React UI components
- **Backend Agent**: Implemented REST API
- **DevOps Agent**: Set up deployment configuration
- **QA Agent**: Added test structure

## 📋 Features

- ✅ Add new tasks
- ✅ Mark tasks as complete
- ✅ Delete tasks
- ✅ Real-time updates
- ✅ Responsive design

## 🔧 Development

Run in development mode with auto-reload:
```bash
npm run dev
```

Run tests:
```bash
npm test
```

## 🐳 Docker Deployment

Build and run with Docker:
```bash
docker-compose up
```

---
Generated with ❤️ by ccswarm"#;

        tokio::fs::write(output_path.join("README.md"), readme_content).await?;

        // Create .gitignore
        let gitignore_content = r#"node_modules/
.env
.DS_Store
*.log
dist/
build/"#;

        tokio::fs::write(output_path.join(".gitignore"), gitignore_content).await?;

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/auto_create.rs:137-157 method analyze_and_decompose <-> ./crates/ccswarm/src/orchestrator/auto_create.rs:541-769 method create_frontend_files
  Similarity: 80.91%
  Classes: AutoCreateEngine <-> AutoCreateEngine

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:analyze_and_decompose (lines 137-157) ---[0m
    pub async fn analyze_and_decompose(&mut self, description: &str) -> Result<Vec<Task>> {
        info!("🤖 Analyzing request: {}", description);

        // Detect application type from description
        let app_type = self.detect_app_type(description);
        info!("📱 Detected app type: {:?}", app_type);

        // Get base tasks from template
        let mut tasks = self.get_template_tasks(&app_type);

        // Customize tasks based on specific requirements
        self.customize_tasks(&mut tasks, description);

        // Convert templates to actual tasks
        let tasks = tasks
            .into_iter()
            .map(|template| self.template_to_task(template))
            .collect();

        Ok(tasks)
    }

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:create_frontend_files (lines 541-769) ---[0m
    async fn create_frontend_files(&self, output_path: &Path) -> Result<()> {
        // Create index.html
        let html_content = r#"<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TODO App - ccswarm Generated</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <div id="root"></div>
    <script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <script type="text/babel" src="app.js"></script>
</body>
</html>"#;

        tokio::fs::write(output_path.join("index.html"), html_content).await?;

        // Create app.js
        let app_content = r#"const { useState, useEffect } = React;

function TodoApp() {
    const [todos, setTodos] = useState([]);
    const [inputValue, setInputValue] = useState('');

    useEffect(() => {
        fetchTodos();
    }, []);

    const fetchTodos = async () => {
        try {
            const response = await fetch('http://localhost:3001/api/todos');
            const data = await response.json();
            setTodos(data);
        } catch (error) {
            console.error('Error fetching todos:', error);
        }
    };

    const addTodo = async () => {
        if (!inputValue.trim()) return;
        
        try {
            const response = await fetch('http://localhost:3001/api/todos', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ text: inputValue })
            });
            const newTodo = await response.json();
            setTodos([...todos, newTodo]);
            setInputValue('');
        } catch (error) {
            console.error('Error adding todo:', error);
        }
    };

    const toggleTodo = async (id) => {
        try {
            const todo = todos.find(t => t.id === id);
            const response = await fetch(`http://localhost:3001/api/todos/${id}`, {
                method: 'PUT',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ completed: !todo.completed })
            });
            const updatedTodo = await response.json();
            setTodos(todos.map(t => t.id === id ? updatedTodo : t));
        } catch (error) {
            console.error('Error updating todo:', error);
        }
    };

    const deleteTodo = async (id) => {
        try {
            await fetch(`http://localhost:3001/api/todos/${id}`, {
                method: 'DELETE'
            });
            setTodos(todos.filter(t => t.id !== id));
        } catch (error) {
            console.error('Error deleting todo:', error);
        }
    };

    return (
        <div className="todo-app">
            <h1>📝 TODO App</h1>
            <p className="subtitle">Generated by ccswarm 🤖</p>
            
            <div className="input-group">
                <input
                    type="text"
                    value={inputValue}
                    onChange={(e) => setInputValue(e.target.value)}
                    onKeyPress={(e) => e.key === 'Enter' && addTodo()}
                    placeholder="Add a new task..."
                />
                <button onClick={addTodo}>Add</button>
            </div>

            <ul className="todo-list">
                {todos.map(todo => (
                    <li key={todo.id} className={todo.completed ? 'completed' : ''}>
                        <input
                            type="checkbox"
                            checked={todo.completed}
                            onChange={() => toggleTodo(todo.id)}
                        />
                        <span>{todo.text}</span>
                        <button onClick={() => deleteTodo(todo.id)}>Delete</button>
                    </li>
                ))}
            </ul>
        </div>
    );
}

ReactDOM.render(<TodoApp />, document.getElementById('root'));"#;

        tokio::fs::write(output_path.join("app.js"), app_content).await?;

        // Create styles.css
        let styles_content = r#"* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
}

.todo-app {
    background: white;
    padding: 2rem;
    border-radius: 10px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
    width: 90%;
    max-width: 500px;
}

h1 {
    color: #333;
    text-align: center;
    margin-bottom: 0.5rem;
}

.subtitle {
    text-align: center;
    color: #666;
    font-size: 0.9rem;
    margin-bottom: 2rem;
}

.input-group {
    display: flex;
    margin-bottom: 1.5rem;
}

input[type="text"] {
    flex: 1;
    padding: 0.75rem;
    border: 2px solid #ddd;
    border-radius: 5px 0 0 5px;
    font-size: 1rem;
}

.input-group button {
    padding: 0.75rem 1.5rem;
    background: #667eea;
    color: white;
    border: none;
    border-radius: 0 5px 5px 0;
    cursor: pointer;
    font-size: 1rem;
}

.input-group button:hover {
    background: #5a67d8;
}

.todo-list {
    list-style: none;
}

.todo-list li {
    display: flex;
    align-items: center;
    padding: 1rem;
    border-bottom: 1px solid #eee;
}

.todo-list li.completed span {
    text-decoration: line-through;
    color: #999;
}

.todo-list input[type="checkbox"] {
    margin-right: 1rem;
}

.todo-list span {
    flex: 1;
}

.todo-list button {
    padding: 0.5rem 1rem;
    background: #f56565;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 0.9rem;
}

.todo-list button:hover {
    background: #e53e3e;
}"#;

        tokio::fs::write(output_path.join("styles.css"), styles_content).await?;

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/auto_create.rs:50-134 method new <-> ./crates/ccswarm/src/orchestrator/auto_create.rs:350-450 method execute_with_real_agents
  Similarity: 88.77%
  Classes: AutoCreateEngine <-> AutoCreateEngine

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:new (lines 50-134) ---[0m
    pub fn new() -> Self {
        let mut templates = HashMap::new();

        // TODOアプリテンプレート
        templates.insert(AppType::Todo, vec![
            TaskTemplate {
                id: "todo-frontend".to_string(),
                description: "Create React TODO app UI with add, delete, and complete task features".to_string(),
                target_agent: "frontend".to_string(),
                priority: Priority::High,
                task_type: TaskType::Feature,
                dependencies: vec![],
                estimated_duration: Some(1800),
            },
            TaskTemplate {
                id: "todo-backend".to_string(),
                description: "Create REST API for TODO app with CRUD operations using Node.js and Express".to_string(),
                target_agent: "backend".to_string(),
                priority: Priority::High,
                task_type: TaskType::Feature,
                dependencies: vec![],
                estimated_duration: Some(2400),
            },
            TaskTemplate {
                id: "todo-database".to_string(),
                description: "Setup SQLite database schema and migrations for TODO app".to_string(),
                target_agent: "backend".to_string(),
                priority: Priority::Medium,
                task_type: TaskType::Infrastructure,
                dependencies: vec!["todo-backend".to_string()],
                estimated_duration: Some(900),
            },
            TaskTemplate {
                id: "todo-tests".to_string(),
                description: "Write unit tests and integration tests for TODO app".to_string(),
                target_agent: "qa".to_string(),
                priority: Priority::Medium,
                task_type: TaskType::Testing,
                dependencies: vec!["todo-frontend".to_string(), "todo-backend".to_string()],
                estimated_duration: Some(1800),
            },
            TaskTemplate {
                id: "todo-deploy".to_string(),
                description: "Create Docker configuration and deployment scripts".to_string(),
                target_agent: "devops".to_string(),
                priority: Priority::Low,
                task_type: TaskType::Infrastructure,
                dependencies: vec!["todo-tests".to_string()],
                estimated_duration: Some(1200),
            },
        ]);

        // ブログアプリテンプレート
        templates.insert(
            AppType::Blog,
            vec![
                TaskTemplate {
                    id: "blog-frontend".to_string(),
                    description:
                        "Create blog frontend with article list, detail view, and comments"
                            .to_string(),
                    target_agent: "frontend".to_string(),
                    priority: Priority::High,
                    task_type: TaskType::Feature,
                    dependencies: vec![],
                    estimated_duration: Some(2400),
                },
                TaskTemplate {
                    id: "blog-backend".to_string(),
                    description: "Create blog API with authentication and content management"
                        .to_string(),
                    target_agent: "backend".to_string(),
                    priority: Priority::High,
                    task_type: TaskType::Feature,
                    dependencies: vec![],
                    estimated_duration: Some(3600),
                },
            ],
        );

        Self {
            templates,
            delegation_engine: MasterDelegationEngine::new(DelegationStrategy::Hybrid),
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:execute_with_real_agents (lines 350-450) ---[0m
    async fn execute_with_real_agents(
        &mut self,
        tasks: Vec<Task>,
        _config: &CcswarmConfig,
        output_path: &Path,
    ) -> Result<()> {
        use crate::identity::AgentIdentity;
        use crate::providers::claude_code::ClaudeCodeExecutor;
        use crate::providers::{ClaudeCodeConfig, ProviderExecutor};
        use std::collections::HashMap;

        // Create output directory as workspace
        let workspace_path = output_path.to_path_buf();

        // Store outputs for Master review
        let mut task_outputs: Vec<(Task, crate::identity::AgentRole, String)> = Vec::new();

        for task in tasks {
            let decision = self.delegation_engine.delegate_task(task.clone())?;
            info!(
                "   {} → {}: {}",
                "Master",
                decision.target_agent.name(),
                task.description
            );

            // Create agent identity
            let agent_identity = AgentIdentity {
                agent_id: format!(
                    "{}-{}",
                    decision.target_agent.name().to_lowercase(),
                    Uuid::new_v4()
                ),
                specialization: decision.target_agent.clone(),
                workspace_path: workspace_path.clone(),
                env_vars: HashMap::new(),
                session_id: Uuid::new_v4().to_string(),
                parent_process_id: std::process::id().to_string(),
                initialized_at: chrono::Utc::now(),
            };

            // Create Claude config with proper settings
            let claude_config = ClaudeCodeConfig {
                model: "claude-3.5-sonnet".to_string(), // Use default model name
                dangerous_skip: true,
                think_mode: None, // Disable think mode for now
                json_output: false,
                api_key: None,
                custom_commands: vec![],
                mcp_servers: HashMap::new(),
            };

            // Create executor
            let executor = ClaudeCodeExecutor::new(claude_config);

            // Create a simple test prompt first
            let test_prompt = format!(
                "You are a {} agent working on: {}. Please create a simple file for this task in the current directory.",
                decision.target_agent.name(),
                task.description
            );

            // Execute with a simple prompt
            match executor
                .execute_prompt(&test_prompt, &agent_identity, &workspace_path)
                .await
            {
                Ok(output) => {
                    info!("      ✅ Task completed successfully");
                    info!(
                        "      📝 Output: {}",
                        output.chars().take(200).collect::<String>()
                    );

                    // Store output for Master review
                    task_outputs.push((task.clone(), decision.target_agent.clone(), output));
                }
                Err(e) => {
                    info!("      ❌ Execution error: {}", e);
                    // Continue with simulation fallback
                    self.simulate_agent_execution(&decision, &task, output_path)
                        .await?;
                    // Add simulated output for review
                    task_outputs.push((
                        task.clone(),
                        decision.target_agent.clone(),
                        format!("Simulated output for {} task", decision.target_agent.name()),
                    ));
                }
            }
        }

        // Master reviews all outputs
        if !task_outputs.is_empty() {
            info!("\n👑 Master reviewing agent outputs...");
            self.master_review_outputs(&task_outputs, output_path)
                .await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/auto_create.rs:350-450 method execute_with_real_agents <-> ./crates/ccswarm/src/orchestrator/auto_create.rs:453-538 method master_review_outputs
  Similarity: 85.83%
  Classes: AutoCreateEngine <-> AutoCreateEngine

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:execute_with_real_agents (lines 350-450) ---[0m
    async fn execute_with_real_agents(
        &mut self,
        tasks: Vec<Task>,
        _config: &CcswarmConfig,
        output_path: &Path,
    ) -> Result<()> {
        use crate::identity::AgentIdentity;
        use crate::providers::claude_code::ClaudeCodeExecutor;
        use crate::providers::{ClaudeCodeConfig, ProviderExecutor};
        use std::collections::HashMap;

        // Create output directory as workspace
        let workspace_path = output_path.to_path_buf();

        // Store outputs for Master review
        let mut task_outputs: Vec<(Task, crate::identity::AgentRole, String)> = Vec::new();

        for task in tasks {
            let decision = self.delegation_engine.delegate_task(task.clone())?;
            info!(
                "   {} → {}: {}",
                "Master",
                decision.target_agent.name(),
                task.description
            );

            // Create agent identity
            let agent_identity = AgentIdentity {
                agent_id: format!(
                    "{}-{}",
                    decision.target_agent.name().to_lowercase(),
                    Uuid::new_v4()
                ),
                specialization: decision.target_agent.clone(),
                workspace_path: workspace_path.clone(),
                env_vars: HashMap::new(),
                session_id: Uuid::new_v4().to_string(),
                parent_process_id: std::process::id().to_string(),
                initialized_at: chrono::Utc::now(),
            };

            // Create Claude config with proper settings
            let claude_config = ClaudeCodeConfig {
                model: "claude-3.5-sonnet".to_string(), // Use default model name
                dangerous_skip: true,
                think_mode: None, // Disable think mode for now
                json_output: false,
                api_key: None,
                custom_commands: vec![],
                mcp_servers: HashMap::new(),
            };

            // Create executor
            let executor = ClaudeCodeExecutor::new(claude_config);

            // Create a simple test prompt first
            let test_prompt = format!(
                "You are a {} agent working on: {}. Please create a simple file for this task in the current directory.",
                decision.target_agent.name(),
                task.description
            );

            // Execute with a simple prompt
            match executor
                .execute_prompt(&test_prompt, &agent_identity, &workspace_path)
                .await
            {
                Ok(output) => {
                    info!("      ✅ Task completed successfully");
                    info!(
                        "      📝 Output: {}",
                        output.chars().take(200).collect::<String>()
                    );

                    // Store output for Master review
                    task_outputs.push((task.clone(), decision.target_agent.clone(), output));
                }
                Err(e) => {
                    info!("      ❌ Execution error: {}", e);
                    // Continue with simulation fallback
                    self.simulate_agent_execution(&decision, &task, output_path)
                        .await?;
                    // Add simulated output for review
                    task_outputs.push((
                        task.clone(),
                        decision.target_agent.clone(),
                        format!("Simulated output for {} task", decision.target_agent.name()),
                    ));
                }
            }
        }

        // Master reviews all outputs
        if !task_outputs.is_empty() {
            info!("\n👑 Master reviewing agent outputs...");
            self.master_review_outputs(&task_outputs, output_path)
                .await?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:master_review_outputs (lines 453-538) ---[0m
    async fn master_review_outputs(
        &self,
        task_outputs: &[(Task, crate::identity::AgentRole, String)],
        output_path: &Path,
    ) -> Result<()> {
        use crate::identity::{AgentIdentity, AgentRole};
        use crate::providers::claude_code::ClaudeCodeExecutor;
        use crate::providers::{ClaudeCodeConfig, ProviderExecutor};
        use std::collections::HashMap;

        // Create Master identity
        let master_identity = AgentIdentity {
            agent_id: format!("master-{}", Uuid::new_v4()),
            specialization: AgentRole::Master {
                oversight_roles: vec![
                    "technical_lead".to_string(),
                    "quality_assurance".to_string(),
                    "code_review".to_string(),
                ],
                quality_standards: crate::identity::QualityStandards {
                    min_test_coverage: 90.0,
                    max_complexity: 15,
                    security_scan_required: true,
                    performance_threshold_secs: 5,
                },
            },
            workspace_path: output_path.to_path_buf(),
            env_vars: HashMap::new(),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: std::process::id().to_string(),
            initialized_at: chrono::Utc::now(),
        };

        // Create Master Claude config
        let claude_config = ClaudeCodeConfig {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: true,
            think_mode: None,
            json_output: false,
            api_key: None,
            custom_commands: vec![],
            mcp_servers: HashMap::new(),
        };

        let executor = ClaudeCodeExecutor::new(claude_config);

        // Build review prompt
        let mut review_prompt = String::from("You are the Master orchestrator reviewing the work of specialized agents. Please review the following outputs and provide:\n\n");
        review_prompt.push_str("1. Quality assessment (1-10 scale)\n");
        review_prompt.push_str("2. Completeness check\n");
        review_prompt.push_str("3. Suggestions for improvement\n");
        review_prompt.push_str("4. Overall verdict (APPROVED/NEEDS_REVISION)\n\n");

        for (task, agent, output) in task_outputs {
            review_prompt.push_str(&format!("\n📋 Task: {}\n", task.description));
            review_prompt.push_str(&format!("🤖 Agent: {}\n", agent.name()));
            review_prompt.push_str(&format!(
                "📝 Output: {}\n",
                output.chars().take(500).collect::<String>()
            ));
            review_prompt.push_str("---\n");
        }

        review_prompt.push_str("\nProvide your review in a structured format.");

        // Execute Master review
        match executor
            .execute_prompt(&review_prompt, &master_identity, output_path)
            .await
        {
            Ok(review) => {
                info!("👑 Master Review Complete:");
                info!("{}", review.chars().take(500).collect::<String>());

                // Save review to file
                let review_path = output_path.join("MASTER_REVIEW.md");
                tokio::fs::write(&review_path, format!("# Master Review\n\n{}", review)).await?;
                info!("📄 Review saved to: {}", review_path.display());
            }
            Err(e) => {
                info!("❌ Master review failed: {}", e);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/auto_create.rs:258-318 method execute_auto_create <-> ./crates/ccswarm/src/orchestrator/auto_create.rs:350-450 method execute_with_real_agents
  Similarity: 84.46%
  Classes: AutoCreateEngine <-> AutoCreateEngine

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:execute_auto_create (lines 258-318) ---[0m
    pub async fn execute_auto_create(
        &mut self,
        description: &str,
        config: &CcswarmConfig,
        output_path: &Path,
    ) -> Result<()> {
        info!("🚀 Starting auto-create workflow");

        // Step 1: Create output directory
        tokio::fs::create_dir_all(output_path).await?;
        info!("📂 Created output directory: {}", output_path.display());

        // Step 2: Analyze and decompose tasks
        let tasks = self.analyze_and_decompose(description).await?;
        info!("📋 Generated {} tasks", tasks.len());

        // Step 3: Check if we should use real Claude API or simulation
        let use_real_api = std::env::var("CCSWARM_USE_REAL_API")
            .unwrap_or_default()
            .to_lowercase()
            == "true";

        info!(
            "📋 API mode check: CCSWARM_USE_REAL_API={}",
            std::env::var("CCSWARM_USE_REAL_API").unwrap_or_else(|_| "not set".to_string())
        );

        if use_real_api {
            info!("\n🤖 Executing with real Claude API...");
            self.execute_with_real_agents(tasks, config, output_path)
                .await?;
        } else {
            info!("\n🤖 Simulating agent execution...");
            for task in &tasks {
                let decision = self.delegation_engine.delegate_task(task.clone())?;
                info!(
                    "   {} → {}: {}",
                    "Master",
                    decision.target_agent.name(),
                    task.description
                );

                // Simulate agent execution by creating files
                self.simulate_agent_execution(&decision, task, output_path)
                    .await?;
            }
        }

        // Step 4: Create project structure
        self.create_project_structure(output_path).await?;

        // Step 5: Summary
        info!("\n📊 Auto-create completed!");
        info!("   📂 Project created at: {}", output_path.display());
        info!("   🚀 To run the app:");
        info!("      cd {}", output_path.display());
        info!("      npm install");
        info!("      npm start");

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:execute_with_real_agents (lines 350-450) ---[0m
    async fn execute_with_real_agents(
        &mut self,
        tasks: Vec<Task>,
        _config: &CcswarmConfig,
        output_path: &Path,
    ) -> Result<()> {
        use crate::identity::AgentIdentity;
        use crate::providers::claude_code::ClaudeCodeExecutor;
        use crate::providers::{ClaudeCodeConfig, ProviderExecutor};
        use std::collections::HashMap;

        // Create output directory as workspace
        let workspace_path = output_path.to_path_buf();

        // Store outputs for Master review
        let mut task_outputs: Vec<(Task, crate::identity::AgentRole, String)> = Vec::new();

        for task in tasks {
            let decision = self.delegation_engine.delegate_task(task.clone())?;
            info!(
                "   {} → {}: {}",
                "Master",
                decision.target_agent.name(),
                task.description
            );

            // Create agent identity
            let agent_identity = AgentIdentity {
                agent_id: format!(
                    "{}-{}",
                    decision.target_agent.name().to_lowercase(),
                    Uuid::new_v4()
                ),
                specialization: decision.target_agent.clone(),
                workspace_path: workspace_path.clone(),
                env_vars: HashMap::new(),
                session_id: Uuid::new_v4().to_string(),
                parent_process_id: std::process::id().to_string(),
                initialized_at: chrono::Utc::now(),
            };

            // Create Claude config with proper settings
            let claude_config = ClaudeCodeConfig {
                model: "claude-3.5-sonnet".to_string(), // Use default model name
                dangerous_skip: true,
                think_mode: None, // Disable think mode for now
                json_output: false,
                api_key: None,
                custom_commands: vec![],
                mcp_servers: HashMap::new(),
            };

            // Create executor
            let executor = ClaudeCodeExecutor::new(claude_config);

            // Create a simple test prompt first
            let test_prompt = format!(
                "You are a {} agent working on: {}. Please create a simple file for this task in the current directory.",
                decision.target_agent.name(),
                task.description
            );

            // Execute with a simple prompt
            match executor
                .execute_prompt(&test_prompt, &agent_identity, &workspace_path)
                .await
            {
                Ok(output) => {
                    info!("      ✅ Task completed successfully");
                    info!(
                        "      📝 Output: {}",
                        output.chars().take(200).collect::<String>()
                    );

                    // Store output for Master review
                    task_outputs.push((task.clone(), decision.target_agent.clone(), output));
                }
                Err(e) => {
                    info!("      ❌ Execution error: {}", e);
                    // Continue with simulation fallback
                    self.simulate_agent_execution(&decision, &task, output_path)
                        .await?;
                    // Add simulated output for review
                    task_outputs.push((
                        task.clone(),
                        decision.target_agent.clone(),
                        format!("Simulated output for {} task", decision.target_agent.name()),
                    ));
                }
            }
        }

        // Master reviews all outputs
        if !task_outputs.is_empty() {
            info!("\n👑 Master reviewing agent outputs...");
            self.master_review_outputs(&task_outputs, output_path)
                .await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/auto_create.rs:50-134 method new <-> ./crates/ccswarm/src/orchestrator/auto_create.rs:453-538 method master_review_outputs
  Similarity: 77.40%
  Classes: AutoCreateEngine <-> AutoCreateEngine

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:new (lines 50-134) ---[0m
    pub fn new() -> Self {
        let mut templates = HashMap::new();

        // TODOアプリテンプレート
        templates.insert(AppType::Todo, vec![
            TaskTemplate {
                id: "todo-frontend".to_string(),
                description: "Create React TODO app UI with add, delete, and complete task features".to_string(),
                target_agent: "frontend".to_string(),
                priority: Priority::High,
                task_type: TaskType::Feature,
                dependencies: vec![],
                estimated_duration: Some(1800),
            },
            TaskTemplate {
                id: "todo-backend".to_string(),
                description: "Create REST API for TODO app with CRUD operations using Node.js and Express".to_string(),
                target_agent: "backend".to_string(),
                priority: Priority::High,
                task_type: TaskType::Feature,
                dependencies: vec![],
                estimated_duration: Some(2400),
            },
            TaskTemplate {
                id: "todo-database".to_string(),
                description: "Setup SQLite database schema and migrations for TODO app".to_string(),
                target_agent: "backend".to_string(),
                priority: Priority::Medium,
                task_type: TaskType::Infrastructure,
                dependencies: vec!["todo-backend".to_string()],
                estimated_duration: Some(900),
            },
            TaskTemplate {
                id: "todo-tests".to_string(),
                description: "Write unit tests and integration tests for TODO app".to_string(),
                target_agent: "qa".to_string(),
                priority: Priority::Medium,
                task_type: TaskType::Testing,
                dependencies: vec!["todo-frontend".to_string(), "todo-backend".to_string()],
                estimated_duration: Some(1800),
            },
            TaskTemplate {
                id: "todo-deploy".to_string(),
                description: "Create Docker configuration and deployment scripts".to_string(),
                target_agent: "devops".to_string(),
                priority: Priority::Low,
                task_type: TaskType::Infrastructure,
                dependencies: vec!["todo-tests".to_string()],
                estimated_duration: Some(1200),
            },
        ]);

        // ブログアプリテンプレート
        templates.insert(
            AppType::Blog,
            vec![
                TaskTemplate {
                    id: "blog-frontend".to_string(),
                    description:
                        "Create blog frontend with article list, detail view, and comments"
                            .to_string(),
                    target_agent: "frontend".to_string(),
                    priority: Priority::High,
                    task_type: TaskType::Feature,
                    dependencies: vec![],
                    estimated_duration: Some(2400),
                },
                TaskTemplate {
                    id: "blog-backend".to_string(),
                    description: "Create blog API with authentication and content management"
                        .to_string(),
                    target_agent: "backend".to_string(),
                    priority: Priority::High,
                    task_type: TaskType::Feature,
                    dependencies: vec![],
                    estimated_duration: Some(3600),
                },
            ],
        );

        Self {
            templates,
            delegation_engine: MasterDelegationEngine::new(DelegationStrategy::Hybrid),
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:master_review_outputs (lines 453-538) ---[0m
    async fn master_review_outputs(
        &self,
        task_outputs: &[(Task, crate::identity::AgentRole, String)],
        output_path: &Path,
    ) -> Result<()> {
        use crate::identity::{AgentIdentity, AgentRole};
        use crate::providers::claude_code::ClaudeCodeExecutor;
        use crate::providers::{ClaudeCodeConfig, ProviderExecutor};
        use std::collections::HashMap;

        // Create Master identity
        let master_identity = AgentIdentity {
            agent_id: format!("master-{}", Uuid::new_v4()),
            specialization: AgentRole::Master {
                oversight_roles: vec![
                    "technical_lead".to_string(),
                    "quality_assurance".to_string(),
                    "code_review".to_string(),
                ],
                quality_standards: crate::identity::QualityStandards {
                    min_test_coverage: 90.0,
                    max_complexity: 15,
                    security_scan_required: true,
                    performance_threshold_secs: 5,
                },
            },
            workspace_path: output_path.to_path_buf(),
            env_vars: HashMap::new(),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: std::process::id().to_string(),
            initialized_at: chrono::Utc::now(),
        };

        // Create Master Claude config
        let claude_config = ClaudeCodeConfig {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: true,
            think_mode: None,
            json_output: false,
            api_key: None,
            custom_commands: vec![],
            mcp_servers: HashMap::new(),
        };

        let executor = ClaudeCodeExecutor::new(claude_config);

        // Build review prompt
        let mut review_prompt = String::from("You are the Master orchestrator reviewing the work of specialized agents. Please review the following outputs and provide:\n\n");
        review_prompt.push_str("1. Quality assessment (1-10 scale)\n");
        review_prompt.push_str("2. Completeness check\n");
        review_prompt.push_str("3. Suggestions for improvement\n");
        review_prompt.push_str("4. Overall verdict (APPROVED/NEEDS_REVISION)\n\n");

        for (task, agent, output) in task_outputs {
            review_prompt.push_str(&format!("\n📋 Task: {}\n", task.description));
            review_prompt.push_str(&format!("🤖 Agent: {}\n", agent.name()));
            review_prompt.push_str(&format!(
                "📝 Output: {}\n",
                output.chars().take(500).collect::<String>()
            ));
            review_prompt.push_str("---\n");
        }

        review_prompt.push_str("\nProvide your review in a structured format.");

        // Execute Master review
        match executor
            .execute_prompt(&review_prompt, &master_identity, output_path)
            .await
        {
            Ok(review) => {
                info!("👑 Master Review Complete:");
                info!("{}", review.chars().take(500).collect::<String>());

                // Save review to file
                let review_path = output_path.join("MASTER_REVIEW.md");
                tokio::fs::write(&review_path, format!("# Master Review\n\n{}", review)).await?;
                info!("📄 Review saved to: {}", review_path.display());
            }
            Err(e) => {
                info!("❌ Master review failed: {}", e);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/auto_create.rs:258-318 method execute_auto_create <-> ./crates/ccswarm/src/orchestrator/auto_create.rs:453-538 method master_review_outputs
  Similarity: 89.37%
  Classes: AutoCreateEngine <-> AutoCreateEngine

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:execute_auto_create (lines 258-318) ---[0m
    pub async fn execute_auto_create(
        &mut self,
        description: &str,
        config: &CcswarmConfig,
        output_path: &Path,
    ) -> Result<()> {
        info!("🚀 Starting auto-create workflow");

        // Step 1: Create output directory
        tokio::fs::create_dir_all(output_path).await?;
        info!("📂 Created output directory: {}", output_path.display());

        // Step 2: Analyze and decompose tasks
        let tasks = self.analyze_and_decompose(description).await?;
        info!("📋 Generated {} tasks", tasks.len());

        // Step 3: Check if we should use real Claude API or simulation
        let use_real_api = std::env::var("CCSWARM_USE_REAL_API")
            .unwrap_or_default()
            .to_lowercase()
            == "true";

        info!(
            "📋 API mode check: CCSWARM_USE_REAL_API={}",
            std::env::var("CCSWARM_USE_REAL_API").unwrap_or_else(|_| "not set".to_string())
        );

        if use_real_api {
            info!("\n🤖 Executing with real Claude API...");
            self.execute_with_real_agents(tasks, config, output_path)
                .await?;
        } else {
            info!("\n🤖 Simulating agent execution...");
            for task in &tasks {
                let decision = self.delegation_engine.delegate_task(task.clone())?;
                info!(
                    "   {} → {}: {}",
                    "Master",
                    decision.target_agent.name(),
                    task.description
                );

                // Simulate agent execution by creating files
                self.simulate_agent_execution(&decision, task, output_path)
                    .await?;
            }
        }

        // Step 4: Create project structure
        self.create_project_structure(output_path).await?;

        // Step 5: Summary
        info!("\n📊 Auto-create completed!");
        info!("   📂 Project created at: {}", output_path.display());
        info!("   🚀 To run the app:");
        info!("      cd {}", output_path.display());
        info!("      npm install");
        info!("      npm start");

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:master_review_outputs (lines 453-538) ---[0m
    async fn master_review_outputs(
        &self,
        task_outputs: &[(Task, crate::identity::AgentRole, String)],
        output_path: &Path,
    ) -> Result<()> {
        use crate::identity::{AgentIdentity, AgentRole};
        use crate::providers::claude_code::ClaudeCodeExecutor;
        use crate::providers::{ClaudeCodeConfig, ProviderExecutor};
        use std::collections::HashMap;

        // Create Master identity
        let master_identity = AgentIdentity {
            agent_id: format!("master-{}", Uuid::new_v4()),
            specialization: AgentRole::Master {
                oversight_roles: vec![
                    "technical_lead".to_string(),
                    "quality_assurance".to_string(),
                    "code_review".to_string(),
                ],
                quality_standards: crate::identity::QualityStandards {
                    min_test_coverage: 90.0,
                    max_complexity: 15,
                    security_scan_required: true,
                    performance_threshold_secs: 5,
                },
            },
            workspace_path: output_path.to_path_buf(),
            env_vars: HashMap::new(),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: std::process::id().to_string(),
            initialized_at: chrono::Utc::now(),
        };

        // Create Master Claude config
        let claude_config = ClaudeCodeConfig {
            model: "claude-3.5-sonnet".to_string(),
            dangerous_skip: true,
            think_mode: None,
            json_output: false,
            api_key: None,
            custom_commands: vec![],
            mcp_servers: HashMap::new(),
        };

        let executor = ClaudeCodeExecutor::new(claude_config);

        // Build review prompt
        let mut review_prompt = String::from("You are the Master orchestrator reviewing the work of specialized agents. Please review the following outputs and provide:\n\n");
        review_prompt.push_str("1. Quality assessment (1-10 scale)\n");
        review_prompt.push_str("2. Completeness check\n");
        review_prompt.push_str("3. Suggestions for improvement\n");
        review_prompt.push_str("4. Overall verdict (APPROVED/NEEDS_REVISION)\n\n");

        for (task, agent, output) in task_outputs {
            review_prompt.push_str(&format!("\n📋 Task: {}\n", task.description));
            review_prompt.push_str(&format!("🤖 Agent: {}\n", agent.name()));
            review_prompt.push_str(&format!(
                "📝 Output: {}\n",
                output.chars().take(500).collect::<String>()
            ));
            review_prompt.push_str("---\n");
        }

        review_prompt.push_str("\nProvide your review in a structured format.");

        // Execute Master review
        match executor
            .execute_prompt(&review_prompt, &master_identity, output_path)
            .await
        {
            Ok(review) => {
                info!("👑 Master Review Complete:");
                info!("{}", review.chars().take(500).collect::<String>());

                // Save review to file
                let review_path = output_path.join("MASTER_REVIEW.md");
                tokio::fs::write(&review_path, format!("# Master Review\n\n{}", review)).await?;
                info!("📄 Review saved to: {}", review_path.display());
            }
            Err(e) => {
                info!("❌ Master review failed: {}", e);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/auto_create.rs:772-843 method create_backend_files <-> ./crates/ccswarm/src/orchestrator/auto_create.rs:935-1013 method create_project_structure
  Similarity: 84.73%
  Classes: AutoCreateEngine <-> AutoCreateEngine

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:create_backend_files (lines 772-843) ---[0m
    async fn create_backend_files(&self, output_path: &Path) -> Result<()> {
        // Create server.js
        let server_content = r#"const express = require('express');
const cors = require('cors');
const app = express();
const PORT = 3001;

// Middleware
app.use(cors());
app.use(express.json());
app.use(express.static('.'));

// In-memory todo storage
let todos = [];
let nextId = 1;

// Routes
app.get('/api/todos', (req, res) => {
    res.json(todos);
});

app.post('/api/todos', (req, res) => {
    const { text } = req.body;
    const newTodo = {
        id: nextId++,
        text,
        completed: false,
        createdAt: new Date()
    };
    todos.push(newTodo);
    res.status(201).json(newTodo);
});

app.put('/api/todos/:id', (req, res) => {
    const id = parseInt(req.params.id);
    const { completed } = req.body;
    const todo = todos.find(t => t.id === id);
    
    if (!todo) {
        return res.status(404).json({ error: 'Todo not found' });
    }
    
    todo.completed = completed;
    res.json(todo);
});

app.delete('/api/todos/:id', (req, res) => {
    const id = parseInt(req.params.id);
    const index = todos.findIndex(t => t.id === id);
    
    if (index === -1) {
        return res.status(404).json({ error: 'Todo not found' });
    }
    
    todos.splice(index, 1);
    res.status(204).send();
});

// Start server
app.listen(PORT, () => {
    console.log(`🚀 Server running on http://localhost:${PORT}`);
    console.log('📝 TODO API available at:');
    console.log(`   GET    http://localhost:${PORT}/api/todos`);
    console.log(`   POST   http://localhost:${PORT}/api/todos`);
    console.log(`   PUT    http://localhost:${PORT}/api/todos/:id`);
    console.log(`   DELETE http://localhost:${PORT}/api/todos/:id`);
});"#;

        tokio::fs::write(output_path.join("server.js"), server_content).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:create_project_structure (lines 935-1013) ---[0m
    async fn create_project_structure(&self, output_path: &Path) -> Result<()> {
        // Create README.md
        let readme_content = r#"# TODO App - Generated by ccswarm 🤖

This TODO application was automatically generated by ccswarm's multi-agent orchestration system.

## 🚀 Quick Start

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start the server:
   ```bash
   npm start
   ```

3. Open your browser to http://localhost:3001

## 🏗️ Architecture

- **Frontend**: React with vanilla JavaScript
- **Backend**: Express.js REST API
- **Storage**: In-memory (for demo purposes)
- **Deployment**: Docker-ready

## 🤖 Generated by ccswarm

This application was created by the following agents:
- **Frontend Agent**: Created React UI components
- **Backend Agent**: Implemented REST API
- **DevOps Agent**: Set up deployment configuration
- **QA Agent**: Added test structure

## 📋 Features

- ✅ Add new tasks
- ✅ Mark tasks as complete
- ✅ Delete tasks
- ✅ Real-time updates
- ✅ Responsive design

## 🔧 Development

Run in development mode with auto-reload:
```bash
npm run dev
```

Run tests:
```bash
npm test
```

## 🐳 Docker Deployment

Build and run with Docker:
```bash
docker-compose up
```

---
Generated with ❤️ by ccswarm"#;

        tokio::fs::write(output_path.join("README.md"), readme_content).await?;

        // Create .gitignore
        let gitignore_content = r#"node_modules/
.env
.DS_Store
*.log
dist/
build/"#;

        tokio::fs::write(output_path.join(".gitignore"), gitignore_content).await?;

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/auto_create.rs:209-244 method customize_tasks <-> ./crates/ccswarm/src/orchestrator/auto_create.rs:350-450 method execute_with_real_agents
  Similarity: 89.37%
  Classes: AutoCreateEngine <-> AutoCreateEngine

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:customize_tasks (lines 209-244) ---[0m
    fn customize_tasks(&self, tasks: &mut Vec<TaskTemplate>, description: &str) {
        let desc_lower = description.to_lowercase();

        // Add authentication if mentioned
        if desc_lower.contains("auth") || desc_lower.contains("login") {
            tasks.push(TaskTemplate {
                id: "auth-system".to_string(),
                description: "Implement user authentication system with JWT".to_string(),
                target_agent: "backend".to_string(),
                priority: Priority::High,
                task_type: TaskType::Feature,
                dependencies: vec![],
                estimated_duration: Some(1800),
            });
        }

        // Add real-time features if mentioned
        if desc_lower.contains("real-time") || desc_lower.contains("websocket") {
            tasks.push(TaskTemplate {
                id: "realtime-features".to_string(),
                description: "Implement real-time updates using WebSockets".to_string(),
                target_agent: "backend".to_string(),
                priority: Priority::Medium,
                task_type: TaskType::Feature,
                dependencies: vec![],
                estimated_duration: Some(1200),
            });
        }

        // Add mobile responsiveness if mentioned
        if desc_lower.contains("mobile") || desc_lower.contains("responsive") {
            if let Some(frontend_task) = tasks.iter_mut().find(|t| t.target_agent == "frontend") {
                frontend_task.description += " with mobile-responsive design";
            }
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:execute_with_real_agents (lines 350-450) ---[0m
    async fn execute_with_real_agents(
        &mut self,
        tasks: Vec<Task>,
        _config: &CcswarmConfig,
        output_path: &Path,
    ) -> Result<()> {
        use crate::identity::AgentIdentity;
        use crate::providers::claude_code::ClaudeCodeExecutor;
        use crate::providers::{ClaudeCodeConfig, ProviderExecutor};
        use std::collections::HashMap;

        // Create output directory as workspace
        let workspace_path = output_path.to_path_buf();

        // Store outputs for Master review
        let mut task_outputs: Vec<(Task, crate::identity::AgentRole, String)> = Vec::new();

        for task in tasks {
            let decision = self.delegation_engine.delegate_task(task.clone())?;
            info!(
                "   {} → {}: {}",
                "Master",
                decision.target_agent.name(),
                task.description
            );

            // Create agent identity
            let agent_identity = AgentIdentity {
                agent_id: format!(
                    "{}-{}",
                    decision.target_agent.name().to_lowercase(),
                    Uuid::new_v4()
                ),
                specialization: decision.target_agent.clone(),
                workspace_path: workspace_path.clone(),
                env_vars: HashMap::new(),
                session_id: Uuid::new_v4().to_string(),
                parent_process_id: std::process::id().to_string(),
                initialized_at: chrono::Utc::now(),
            };

            // Create Claude config with proper settings
            let claude_config = ClaudeCodeConfig {
                model: "claude-3.5-sonnet".to_string(), // Use default model name
                dangerous_skip: true,
                think_mode: None, // Disable think mode for now
                json_output: false,
                api_key: None,
                custom_commands: vec![],
                mcp_servers: HashMap::new(),
            };

            // Create executor
            let executor = ClaudeCodeExecutor::new(claude_config);

            // Create a simple test prompt first
            let test_prompt = format!(
                "You are a {} agent working on: {}. Please create a simple file for this task in the current directory.",
                decision.target_agent.name(),
                task.description
            );

            // Execute with a simple prompt
            match executor
                .execute_prompt(&test_prompt, &agent_identity, &workspace_path)
                .await
            {
                Ok(output) => {
                    info!("      ✅ Task completed successfully");
                    info!(
                        "      📝 Output: {}",
                        output.chars().take(200).collect::<String>()
                    );

                    // Store output for Master review
                    task_outputs.push((task.clone(), decision.target_agent.clone(), output));
                }
                Err(e) => {
                    info!("      ❌ Execution error: {}", e);
                    // Continue with simulation fallback
                    self.simulate_agent_execution(&decision, &task, output_path)
                        .await?;
                    // Add simulated output for review
                    task_outputs.push((
                        task.clone(),
                        decision.target_agent.clone(),
                        format!("Simulated output for {} task", decision.target_agent.name()),
                    ));
                }
            }
        }

        // Master reviews all outputs
        if !task_outputs.is_empty() {
            info!("\n👑 Master reviewing agent outputs...");
            self.master_review_outputs(&task_outputs, output_path)
                .await?;
        }

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/auto_create.rs:846-901 method create_devops_files <-> ./crates/ccswarm/src/orchestrator/auto_create.rs:935-1013 method create_project_structure
  Similarity: 88.79%
  Classes: AutoCreateEngine <-> AutoCreateEngine

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:create_devops_files (lines 846-901) ---[0m
    async fn create_devops_files(&self, output_path: &Path) -> Result<()> {
        // Create package.json
        let package_content = r#"{
  "name": "todo-app-ccswarm",
  "version": "1.0.0",
  "description": "TODO application generated by ccswarm",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.5.0"
  }
}"#;

        tokio::fs::write(output_path.join("package.json"), package_content).await?;

        // Create Dockerfile
        let dockerfile_content = r#"FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 3001

CMD ["npm", "start"]"#;

        tokio::fs::write(output_path.join("Dockerfile"), dockerfile_content).await?;

        // Create docker-compose.yml
        let compose_content = r#"version: '3.8'

services:
  todo-app:
    build: .
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
    restart: unless-stopped"#;

        tokio::fs::write(output_path.join("docker-compose.yml"), compose_content).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:create_project_structure (lines 935-1013) ---[0m
    async fn create_project_structure(&self, output_path: &Path) -> Result<()> {
        // Create README.md
        let readme_content = r#"# TODO App - Generated by ccswarm 🤖

This TODO application was automatically generated by ccswarm's multi-agent orchestration system.

## 🚀 Quick Start

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start the server:
   ```bash
   npm start
   ```

3. Open your browser to http://localhost:3001

## 🏗️ Architecture

- **Frontend**: React with vanilla JavaScript
- **Backend**: Express.js REST API
- **Storage**: In-memory (for demo purposes)
- **Deployment**: Docker-ready

## 🤖 Generated by ccswarm

This application was created by the following agents:
- **Frontend Agent**: Created React UI components
- **Backend Agent**: Implemented REST API
- **DevOps Agent**: Set up deployment configuration
- **QA Agent**: Added test structure

## 📋 Features

- ✅ Add new tasks
- ✅ Mark tasks as complete
- ✅ Delete tasks
- ✅ Real-time updates
- ✅ Responsive design

## 🔧 Development

Run in development mode with auto-reload:
```bash
npm run dev
```

Run tests:
```bash
npm test
```

## 🐳 Docker Deployment

Build and run with Docker:
```bash
docker-compose up
```

---
Generated with ❤️ by ccswarm"#;

        tokio::fs::write(output_path.join("README.md"), readme_content).await?;

        // Create .gitignore
        let gitignore_content = r#"node_modules/
.env
.DS_Store
*.log
dist/
build/"#;

        tokio::fs::write(output_path.join(".gitignore"), gitignore_content).await?;

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/auto_create.rs:50-134 method new <-> ./crates/ccswarm/src/orchestrator/auto_create.rs:209-244 method customize_tasks
  Similarity: 96.75%
  Classes: AutoCreateEngine <-> AutoCreateEngine

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:new (lines 50-134) ---[0m
    pub fn new() -> Self {
        let mut templates = HashMap::new();

        // TODOアプリテンプレート
        templates.insert(AppType::Todo, vec![
            TaskTemplate {
                id: "todo-frontend".to_string(),
                description: "Create React TODO app UI with add, delete, and complete task features".to_string(),
                target_agent: "frontend".to_string(),
                priority: Priority::High,
                task_type: TaskType::Feature,
                dependencies: vec![],
                estimated_duration: Some(1800),
            },
            TaskTemplate {
                id: "todo-backend".to_string(),
                description: "Create REST API for TODO app with CRUD operations using Node.js and Express".to_string(),
                target_agent: "backend".to_string(),
                priority: Priority::High,
                task_type: TaskType::Feature,
                dependencies: vec![],
                estimated_duration: Some(2400),
            },
            TaskTemplate {
                id: "todo-database".to_string(),
                description: "Setup SQLite database schema and migrations for TODO app".to_string(),
                target_agent: "backend".to_string(),
                priority: Priority::Medium,
                task_type: TaskType::Infrastructure,
                dependencies: vec!["todo-backend".to_string()],
                estimated_duration: Some(900),
            },
            TaskTemplate {
                id: "todo-tests".to_string(),
                description: "Write unit tests and integration tests for TODO app".to_string(),
                target_agent: "qa".to_string(),
                priority: Priority::Medium,
                task_type: TaskType::Testing,
                dependencies: vec!["todo-frontend".to_string(), "todo-backend".to_string()],
                estimated_duration: Some(1800),
            },
            TaskTemplate {
                id: "todo-deploy".to_string(),
                description: "Create Docker configuration and deployment scripts".to_string(),
                target_agent: "devops".to_string(),
                priority: Priority::Low,
                task_type: TaskType::Infrastructure,
                dependencies: vec!["todo-tests".to_string()],
                estimated_duration: Some(1200),
            },
        ]);

        // ブログアプリテンプレート
        templates.insert(
            AppType::Blog,
            vec![
                TaskTemplate {
                    id: "blog-frontend".to_string(),
                    description:
                        "Create blog frontend with article list, detail view, and comments"
                            .to_string(),
                    target_agent: "frontend".to_string(),
                    priority: Priority::High,
                    task_type: TaskType::Feature,
                    dependencies: vec![],
                    estimated_duration: Some(2400),
                },
                TaskTemplate {
                    id: "blog-backend".to_string(),
                    description: "Create blog API with authentication and content management"
                        .to_string(),
                    target_agent: "backend".to_string(),
                    priority: Priority::High,
                    task_type: TaskType::Feature,
                    dependencies: vec![],
                    estimated_duration: Some(3600),
                },
            ],
        );

        Self {
            templates,
            delegation_engine: MasterDelegationEngine::new(DelegationStrategy::Hybrid),
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:customize_tasks (lines 209-244) ---[0m
    fn customize_tasks(&self, tasks: &mut Vec<TaskTemplate>, description: &str) {
        let desc_lower = description.to_lowercase();

        // Add authentication if mentioned
        if desc_lower.contains("auth") || desc_lower.contains("login") {
            tasks.push(TaskTemplate {
                id: "auth-system".to_string(),
                description: "Implement user authentication system with JWT".to_string(),
                target_agent: "backend".to_string(),
                priority: Priority::High,
                task_type: TaskType::Feature,
                dependencies: vec![],
                estimated_duration: Some(1800),
            });
        }

        // Add real-time features if mentioned
        if desc_lower.contains("real-time") || desc_lower.contains("websocket") {
            tasks.push(TaskTemplate {
                id: "realtime-features".to_string(),
                description: "Implement real-time updates using WebSockets".to_string(),
                target_agent: "backend".to_string(),
                priority: Priority::Medium,
                task_type: TaskType::Feature,
                dependencies: vec![],
                estimated_duration: Some(1200),
            });
        }

        // Add mobile responsiveness if mentioned
        if desc_lower.contains("mobile") || desc_lower.contains("responsive") {
            if let Some(frontend_task) = tasks.iter_mut().find(|t| t.target_agent == "frontend") {
                frontend_task.description += " with mobile-responsive design";
            }
        }
    }

  ./crates/ccswarm/src/orchestrator/auto_create.rs:772-843 method create_backend_files <-> ./crates/ccswarm/src/orchestrator/auto_create.rs:904-932 method create_test_files
  Similarity: 98.20%
  Classes: AutoCreateEngine <-> AutoCreateEngine

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:create_backend_files (lines 772-843) ---[0m
    async fn create_backend_files(&self, output_path: &Path) -> Result<()> {
        // Create server.js
        let server_content = r#"const express = require('express');
const cors = require('cors');
const app = express();
const PORT = 3001;

// Middleware
app.use(cors());
app.use(express.json());
app.use(express.static('.'));

// In-memory todo storage
let todos = [];
let nextId = 1;

// Routes
app.get('/api/todos', (req, res) => {
    res.json(todos);
});

app.post('/api/todos', (req, res) => {
    const { text } = req.body;
    const newTodo = {
        id: nextId++,
        text,
        completed: false,
        createdAt: new Date()
    };
    todos.push(newTodo);
    res.status(201).json(newTodo);
});

app.put('/api/todos/:id', (req, res) => {
    const id = parseInt(req.params.id);
    const { completed } = req.body;
    const todo = todos.find(t => t.id === id);
    
    if (!todo) {
        return res.status(404).json({ error: 'Todo not found' });
    }
    
    todo.completed = completed;
    res.json(todo);
});

app.delete('/api/todos/:id', (req, res) => {
    const id = parseInt(req.params.id);
    const index = todos.findIndex(t => t.id === id);
    
    if (index === -1) {
        return res.status(404).json({ error: 'Todo not found' });
    }
    
    todos.splice(index, 1);
    res.status(204).send();
});

// Start server
app.listen(PORT, () => {
    console.log(`🚀 Server running on http://localhost:${PORT}`);
    console.log('📝 TODO API available at:');
    console.log(`   GET    http://localhost:${PORT}/api/todos`);
    console.log(`   POST   http://localhost:${PORT}/api/todos`);
    console.log(`   PUT    http://localhost:${PORT}/api/todos/:id`);
    console.log(`   DELETE http://localhost:${PORT}/api/todos/:id`);
});"#;

        tokio::fs::write(output_path.join("server.js"), server_content).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:create_test_files (lines 904-932) ---[0m
    async fn create_test_files(&self, output_path: &Path) -> Result<()> {
        // Create basic test file
        let test_content = r#"// Basic tests for TODO app
describe('TODO API', () => {
    test('GET /api/todos returns array', async () => {
        // Test implementation would go here
        expect(true).toBe(true);
    });
    
    test('POST /api/todos creates new todo', async () => {
        // Test implementation would go here
        expect(true).toBe(true);
    });
    
    test('PUT /api/todos/:id updates todo', async () => {
        // Test implementation would go here
        expect(true).toBe(true);
    });
    
    test('DELETE /api/todos/:id removes todo', async () => {
        // Test implementation would go here
        expect(true).toBe(true);
    });
});"#;

        tokio::fs::write(output_path.join("app.test.js"), test_content).await?;

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/auto_create.rs:904-932 method create_test_files <-> ./crates/ccswarm/src/orchestrator/auto_create.rs:935-1013 method create_project_structure
  Similarity: 84.73%
  Classes: AutoCreateEngine <-> AutoCreateEngine

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:create_test_files (lines 904-932) ---[0m
    async fn create_test_files(&self, output_path: &Path) -> Result<()> {
        // Create basic test file
        let test_content = r#"// Basic tests for TODO app
describe('TODO API', () => {
    test('GET /api/todos returns array', async () => {
        // Test implementation would go here
        expect(true).toBe(true);
    });
    
    test('POST /api/todos creates new todo', async () => {
        // Test implementation would go here
        expect(true).toBe(true);
    });
    
    test('PUT /api/todos/:id updates todo', async () => {
        // Test implementation would go here
        expect(true).toBe(true);
    });
    
    test('DELETE /api/todos/:id removes todo', async () => {
        // Test implementation would go here
        expect(true).toBe(true);
    });
});"#;

        tokio::fs::write(output_path.join("app.test.js"), test_content).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:create_project_structure (lines 935-1013) ---[0m
    async fn create_project_structure(&self, output_path: &Path) -> Result<()> {
        // Create README.md
        let readme_content = r#"# TODO App - Generated by ccswarm 🤖

This TODO application was automatically generated by ccswarm's multi-agent orchestration system.

## 🚀 Quick Start

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start the server:
   ```bash
   npm start
   ```

3. Open your browser to http://localhost:3001

## 🏗️ Architecture

- **Frontend**: React with vanilla JavaScript
- **Backend**: Express.js REST API
- **Storage**: In-memory (for demo purposes)
- **Deployment**: Docker-ready

## 🤖 Generated by ccswarm

This application was created by the following agents:
- **Frontend Agent**: Created React UI components
- **Backend Agent**: Implemented REST API
- **DevOps Agent**: Set up deployment configuration
- **QA Agent**: Added test structure

## 📋 Features

- ✅ Add new tasks
- ✅ Mark tasks as complete
- ✅ Delete tasks
- ✅ Real-time updates
- ✅ Responsive design

## 🔧 Development

Run in development mode with auto-reload:
```bash
npm run dev
```

Run tests:
```bash
npm test
```

## 🐳 Docker Deployment

Build and run with Docker:
```bash
docker-compose up
```

---
Generated with ❤️ by ccswarm"#;

        tokio::fs::write(output_path.join("README.md"), readme_content).await?;

        // Create .gitignore
        let gitignore_content = r#"node_modules/
.env
.DS_Store
*.log
dist/
build/"#;

        tokio::fs::write(output_path.join(".gitignore"), gitignore_content).await?;

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/auto_create.rs:137-157 method analyze_and_decompose <-> ./crates/ccswarm/src/orchestrator/auto_create.rs:935-1013 method create_project_structure
  Similarity: 77.71%
  Classes: AutoCreateEngine <-> AutoCreateEngine

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:analyze_and_decompose (lines 137-157) ---[0m
    pub async fn analyze_and_decompose(&mut self, description: &str) -> Result<Vec<Task>> {
        info!("🤖 Analyzing request: {}", description);

        // Detect application type from description
        let app_type = self.detect_app_type(description);
        info!("📱 Detected app type: {:?}", app_type);

        // Get base tasks from template
        let mut tasks = self.get_template_tasks(&app_type);

        // Customize tasks based on specific requirements
        self.customize_tasks(&mut tasks, description);

        // Convert templates to actual tasks
        let tasks = tasks
            .into_iter()
            .map(|template| self.template_to_task(template))
            .collect();

        Ok(tasks)
    }

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:create_project_structure (lines 935-1013) ---[0m
    async fn create_project_structure(&self, output_path: &Path) -> Result<()> {
        // Create README.md
        let readme_content = r#"# TODO App - Generated by ccswarm 🤖

This TODO application was automatically generated by ccswarm's multi-agent orchestration system.

## 🚀 Quick Start

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start the server:
   ```bash
   npm start
   ```

3. Open your browser to http://localhost:3001

## 🏗️ Architecture

- **Frontend**: React with vanilla JavaScript
- **Backend**: Express.js REST API
- **Storage**: In-memory (for demo purposes)
- **Deployment**: Docker-ready

## 🤖 Generated by ccswarm

This application was created by the following agents:
- **Frontend Agent**: Created React UI components
- **Backend Agent**: Implemented REST API
- **DevOps Agent**: Set up deployment configuration
- **QA Agent**: Added test structure

## 📋 Features

- ✅ Add new tasks
- ✅ Mark tasks as complete
- ✅ Delete tasks
- ✅ Real-time updates
- ✅ Responsive design

## 🔧 Development

Run in development mode with auto-reload:
```bash
npm run dev
```

Run tests:
```bash
npm test
```

## 🐳 Docker Deployment

Build and run with Docker:
```bash
docker-compose up
```

---
Generated with ❤️ by ccswarm"#;

        tokio::fs::write(output_path.join("README.md"), readme_content).await?;

        // Create .gitignore
        let gitignore_content = r#"node_modules/
.env
.DS_Store
*.log
dist/
build/"#;

        tokio::fs::write(output_path.join(".gitignore"), gitignore_content).await?;

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/auto_create.rs:137-157 method analyze_and_decompose <-> ./crates/ccswarm/src/orchestrator/auto_create.rs:846-901 method create_devops_files
  Similarity: 80.91%
  Classes: AutoCreateEngine <-> AutoCreateEngine

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:analyze_and_decompose (lines 137-157) ---[0m
    pub async fn analyze_and_decompose(&mut self, description: &str) -> Result<Vec<Task>> {
        info!("🤖 Analyzing request: {}", description);

        // Detect application type from description
        let app_type = self.detect_app_type(description);
        info!("📱 Detected app type: {:?}", app_type);

        // Get base tasks from template
        let mut tasks = self.get_template_tasks(&app_type);

        // Customize tasks based on specific requirements
        self.customize_tasks(&mut tasks, description);

        // Convert templates to actual tasks
        let tasks = tasks
            .into_iter()
            .map(|template| self.template_to_task(template))
            .collect();

        Ok(tasks)
    }

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:create_devops_files (lines 846-901) ---[0m
    async fn create_devops_files(&self, output_path: &Path) -> Result<()> {
        // Create package.json
        let package_content = r#"{
  "name": "todo-app-ccswarm",
  "version": "1.0.0",
  "description": "TODO application generated by ccswarm",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.5.0"
  }
}"#;

        tokio::fs::write(output_path.join("package.json"), package_content).await?;

        // Create Dockerfile
        let dockerfile_content = r#"FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 3001

CMD ["npm", "start"]"#;

        tokio::fs::write(output_path.join("Dockerfile"), dockerfile_content).await?;

        // Create docker-compose.yml
        let compose_content = r#"version: '3.8'

services:
  todo-app:
    build: .
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
    restart: unless-stopped"#;

        tokio::fs::write(output_path.join("docker-compose.yml"), compose_content).await?;

        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/auto_create.rs:179-206 method get_template_tasks <-> ./crates/ccswarm/src/orchestrator/auto_create.rs:321-347 method simulate_agent_execution
  Similarity: 82.67%
  Classes: AutoCreateEngine <-> AutoCreateEngine

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:get_template_tasks (lines 179-206) ---[0m
    fn get_template_tasks(&self, app_type: &AppType) -> Vec<TaskTemplate> {
        match self.templates.get(app_type) {
            Some(tasks) => tasks.clone(),
            None => {
                // Return generic tasks for unknown types
                vec![
                    TaskTemplate {
                        id: "generic-frontend".to_string(),
                        description: "Create frontend application".to_string(),
                        target_agent: "frontend".to_string(),
                        priority: Priority::High,
                        task_type: TaskType::Feature,
                        dependencies: vec![],
                        estimated_duration: Some(2400),
                    },
                    TaskTemplate {
                        id: "generic-backend".to_string(),
                        description: "Create backend API".to_string(),
                        target_agent: "backend".to_string(),
                        priority: Priority::High,
                        task_type: TaskType::Feature,
                        dependencies: vec![],
                        estimated_duration: Some(2400),
                    },
                ]
            }
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:simulate_agent_execution (lines 321-347) ---[0m
    async fn simulate_agent_execution(
        &self,
        decision: &DelegationDecision,
        _task: &Task,
        output_path: &Path,
    ) -> Result<()> {
        match decision.target_agent.name() {
            "Frontend" => {
                self.create_frontend_files(output_path).await?;
                info!("      ✅ Created frontend files");
            }
            "Backend" => {
                self.create_backend_files(output_path).await?;
                info!("      ✅ Created backend files");
            }
            "DevOps" => {
                self.create_devops_files(output_path).await?;
                info!("      ✅ Created deployment files");
            }
            "QA" => {
                self.create_test_files(output_path).await?;
                info!("      ✅ Created test files");
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/auto_create.rs:160-176 method detect_app_type <-> ./crates/ccswarm/src/orchestrator/auto_create.rs:321-347 method simulate_agent_execution
  Similarity: 91.40%
  Classes: AutoCreateEngine <-> AutoCreateEngine

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:detect_app_type (lines 160-176) ---[0m
    fn detect_app_type(&self, description: &str) -> AppType {
        let desc_lower = description.to_lowercase();

        if desc_lower.contains("todo") || desc_lower.contains("task") {
            AppType::Todo
        } else if desc_lower.contains("blog") || desc_lower.contains("article") {
            AppType::Blog
        } else if desc_lower.contains("shop") || desc_lower.contains("ecommerce") {
            AppType::Ecommerce
        } else if desc_lower.contains("api") || desc_lower.contains("rest") {
            AppType::Api
        } else if desc_lower.contains("dashboard") || desc_lower.contains("admin") {
            AppType::Dashboard
        } else {
            AppType::Custom("generic".to_string())
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:simulate_agent_execution (lines 321-347) ---[0m
    async fn simulate_agent_execution(
        &self,
        decision: &DelegationDecision,
        _task: &Task,
        output_path: &Path,
    ) -> Result<()> {
        match decision.target_agent.name() {
            "Frontend" => {
                self.create_frontend_files(output_path).await?;
                info!("      ✅ Created frontend files");
            }
            "Backend" => {
                self.create_backend_files(output_path).await?;
                info!("      ✅ Created backend files");
            }
            "DevOps" => {
                self.create_devops_files(output_path).await?;
                info!("      ✅ Created deployment files");
            }
            "QA" => {
                self.create_test_files(output_path).await?;
                info!("      ✅ Created test files");
            }
            _ => {}
        }
        Ok(())
    }

  ./crates/ccswarm/src/orchestrator/auto_create.rs:160-176 method detect_app_type <-> ./crates/ccswarm/src/orchestrator/auto_create.rs:179-206 method get_template_tasks
  Similarity: 86.62%
  Classes: AutoCreateEngine <-> AutoCreateEngine

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:detect_app_type (lines 160-176) ---[0m
    fn detect_app_type(&self, description: &str) -> AppType {
        let desc_lower = description.to_lowercase();

        if desc_lower.contains("todo") || desc_lower.contains("task") {
            AppType::Todo
        } else if desc_lower.contains("blog") || desc_lower.contains("article") {
            AppType::Blog
        } else if desc_lower.contains("shop") || desc_lower.contains("ecommerce") {
            AppType::Ecommerce
        } else if desc_lower.contains("api") || desc_lower.contains("rest") {
            AppType::Api
        } else if desc_lower.contains("dashboard") || desc_lower.contains("admin") {
            AppType::Dashboard
        } else {
            AppType::Custom("generic".to_string())
        }
    }

[36m--- ./crates/ccswarm/src/orchestrator/auto_create.rs:get_template_tasks (lines 179-206) ---[0m
    fn get_template_tasks(&self, app_type: &AppType) -> Vec<TaskTemplate> {
        match self.templates.get(app_type) {
            Some(tasks) => tasks.clone(),
            None => {
                // Return generic tasks for unknown types
                vec![
                    TaskTemplate {
                        id: "generic-frontend".to_string(),
                        description: "Create frontend application".to_string(),
                        target_agent: "frontend".to_string(),
                        priority: Priority::High,
                        task_type: TaskType::Feature,
                        dependencies: vec![],
                        estimated_duration: Some(2400),
                    },
                    TaskTemplate {
                        id: "generic-backend".to_string(),
                        description: "Create backend API".to_string(),
                        target_agent: "backend".to_string(),
                        priority: Priority::High,
                        task_type: TaskType::Feature,
                        dependencies: vec![],
                        estimated_duration: Some(2400),
                    },
                ]
            }
        }
    }


Duplicates in ./crates/ccswarm/src/mcp/client.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/mcp/client.rs:99-152 method start <-> ./crates/ccswarm/src/mcp/client.rs:223-282 method execute_tool
  Similarity: 92.71%
  Classes: McpClient <-> McpClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:start (lines 99-152) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        info!(
            "Starting MCP client: {}/{}",
            self.config.client_name, self.config.client_version
        );

        // Initialize with the server
        self.initialize().await?;

        // Discover available tools
        self.discover_tools().await?;

        // Start message handling loop
        let (shutdown_tx, mut shutdown_rx) = mpsc::channel(1);
        self.shutdown_tx = Some(shutdown_tx);

        let transport = self.transport.clone();
        let pending_requests = self.pending_requests.clone();

        tokio::spawn(async move {
            loop {
                tokio::select! {
                    // Handle shutdown signal
                    _ = shutdown_rx.recv() => {
                        info!("MCP client shutting down");
                        break;
                    }

                    // Handle incoming messages
                    message_result = async {
                        let mut transport = transport.lock().await;
                        transport.receive().await
                    } => {
                        match message_result {
                            Ok(Some(message)) => {
                                if let Err(e) = Self::handle_message(message, &pending_requests).await {
                                    error!("Error handling message: {}", e);
                                }
                            }
                            Ok(None) => {
                                warn!("Transport closed");
                                break;
                            }
                            Err(e) => {
                                error!("Error receiving message: {}", e);
                            }
                        }
                    }
                }
            }
        });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:execute_tool (lines 223-282) ---[0m
    pub async fn execute_tool(&self, tool_name: &str, arguments: Value) -> Result<McpToolResult> {
        // Verify tool exists
        {
            let tools = self.tools.read().await;
            if !tools.contains_key(tool_name) {
                return Err(anyhow::anyhow!("Tool '{}' not found", tool_name));
            }
        }

        let request = JsonRpcRequest::new(
            self.next_request_id(),
            "tools/call".to_string(),
            Some(json!({
                "name": tool_name,
                "arguments": arguments
            })),
        );

        let response = self.send_request(request).await?;

        if let Some(error) = response.error {
            return Err(anyhow::anyhow!("Tool execution failed: {:?}", error));
        }

        let result = response
            .result
            .ok_or_else(|| anyhow::anyhow!("No result from tool execution"))?;

        // Parse tool result
        let content = result
            .get("content")
            .and_then(|v| v.as_array())
            .unwrap_or(&vec![])
            .iter()
            .filter_map(|v| {
                if let Some(text) = v.get("text").and_then(|t| t.as_str()) {
                    Some(McpContent::Text {
                        text: text.to_string(),
                    })
                } else if let (Some(data), Some(mime_type)) = (
                    v.get("data").and_then(|d| d.as_str()),
                    v.get("mimeType").and_then(|m| m.as_str()),
                ) {
                    Some(McpContent::Image {
                        data: data.to_string(),
                        mime_type: mime_type.to_string(),
                    })
                } else {
                    None
                }
            })
            .collect();

        let is_error = result
            .get("isError")
            .and_then(|v| v.as_bool())
            .unwrap_or(false);

        Ok(McpToolResult { content, is_error })
    }

  ./crates/ccswarm/src/mcp/client.rs:223-282 method execute_tool <-> ./crates/ccswarm/src/mcp/client.rs:375-407 method create_session
  Similarity: 86.56%
  Classes: McpClient <-> AiSessionClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:execute_tool (lines 223-282) ---[0m
    pub async fn execute_tool(&self, tool_name: &str, arguments: Value) -> Result<McpToolResult> {
        // Verify tool exists
        {
            let tools = self.tools.read().await;
            if !tools.contains_key(tool_name) {
                return Err(anyhow::anyhow!("Tool '{}' not found", tool_name));
            }
        }

        let request = JsonRpcRequest::new(
            self.next_request_id(),
            "tools/call".to_string(),
            Some(json!({
                "name": tool_name,
                "arguments": arguments
            })),
        );

        let response = self.send_request(request).await?;

        if let Some(error) = response.error {
            return Err(anyhow::anyhow!("Tool execution failed: {:?}", error));
        }

        let result = response
            .result
            .ok_or_else(|| anyhow::anyhow!("No result from tool execution"))?;

        // Parse tool result
        let content = result
            .get("content")
            .and_then(|v| v.as_array())
            .unwrap_or(&vec![])
            .iter()
            .filter_map(|v| {
                if let Some(text) = v.get("text").and_then(|t| t.as_str()) {
                    Some(McpContent::Text {
                        text: text.to_string(),
                    })
                } else if let (Some(data), Some(mime_type)) = (
                    v.get("data").and_then(|d| d.as_str()),
                    v.get("mimeType").and_then(|m| m.as_str()),
                ) {
                    Some(McpContent::Image {
                        data: data.to_string(),
                        mime_type: mime_type.to_string(),
                    })
                } else {
                    None
                }
            })
            .collect();

        let is_error = result
            .get("isError")
            .and_then(|v| v.as_bool())
            .unwrap_or(false);

        Ok(McpToolResult { content, is_error })
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:create_session (lines 375-407) ---[0m
    pub async fn create_session(
        &self,
        name: &str,
        working_directory: Option<&str>,
    ) -> Result<String> {
        let mut args = json!({ "name": name });
        if let Some(wd) = working_directory {
            args["working_directory"] = json!(wd);
        }

        let result = self.mcp_client.execute_tool("create_session", args).await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Failed to create session"));
        }

        // Extract session ID from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            // Parse session ID from response text
            if let Some(start) = text.find("ID: ") {
                let id_start = start + 4;
                if let Some(end) = text[id_start..].find(char::is_whitespace) {
                    return Ok(text[id_start..id_start + end].to_string());
                } else {
                    return Ok(text[id_start..].to_string());
                }
            }
        }

        Err(anyhow::anyhow!(
            "Could not extract session ID from response"
        ))
    }

  ./crates/ccswarm/src/mcp/client.rs:99-152 method start <-> ./crates/ccswarm/src/mcp/client.rs:375-407 method create_session
  Similarity: 77.98%
  Classes: McpClient <-> AiSessionClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:start (lines 99-152) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        info!(
            "Starting MCP client: {}/{}",
            self.config.client_name, self.config.client_version
        );

        // Initialize with the server
        self.initialize().await?;

        // Discover available tools
        self.discover_tools().await?;

        // Start message handling loop
        let (shutdown_tx, mut shutdown_rx) = mpsc::channel(1);
        self.shutdown_tx = Some(shutdown_tx);

        let transport = self.transport.clone();
        let pending_requests = self.pending_requests.clone();

        tokio::spawn(async move {
            loop {
                tokio::select! {
                    // Handle shutdown signal
                    _ = shutdown_rx.recv() => {
                        info!("MCP client shutting down");
                        break;
                    }

                    // Handle incoming messages
                    message_result = async {
                        let mut transport = transport.lock().await;
                        transport.receive().await
                    } => {
                        match message_result {
                            Ok(Some(message)) => {
                                if let Err(e) = Self::handle_message(message, &pending_requests).await {
                                    error!("Error handling message: {}", e);
                                }
                            }
                            Ok(None) => {
                                warn!("Transport closed");
                                break;
                            }
                            Err(e) => {
                                error!("Error receiving message: {}", e);
                            }
                        }
                    }
                }
            }
        });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:create_session (lines 375-407) ---[0m
    pub async fn create_session(
        &self,
        name: &str,
        working_directory: Option<&str>,
    ) -> Result<String> {
        let mut args = json!({ "name": name });
        if let Some(wd) = working_directory {
            args["working_directory"] = json!(wd);
        }

        let result = self.mcp_client.execute_tool("create_session", args).await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Failed to create session"));
        }

        // Extract session ID from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            // Parse session ID from response text
            if let Some(start) = text.find("ID: ") {
                let id_start = start + 4;
                if let Some(end) = text[id_start..].find(char::is_whitespace) {
                    return Ok(text[id_start..id_start + end].to_string());
                } else {
                    return Ok(text[id_start..].to_string());
                }
            }
        }

        Err(anyhow::anyhow!(
            "Could not extract session ID from response"
        ))
    }

  ./crates/ccswarm/src/mcp/client.rs:195-220 method discover_tools <-> ./crates/ccswarm/src/mcp/client.rs:223-282 method execute_tool
  Similarity: 78.56%
  Classes: McpClient <-> McpClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:discover_tools (lines 195-220) ---[0m
    async fn discover_tools(&self) -> Result<()> {
        let request = JsonRpcRequest::new(self.next_request_id(), "tools/list".to_string(), None);

        let response = self.send_request(request).await?;

        if let Some(error) = response.error {
            return Err(anyhow::anyhow!("Tools discovery failed: {:?}", error));
        }

        if let Some(result) = response.result {
            if let Some(tools_array) = result.get("tools").and_then(|v| v.as_array()) {
                let mut tools = self.tools.write().await;

                for tool_value in tools_array {
                    if let Ok(tool) = serde_json::from_value::<McpTool>(tool_value.clone()) {
                        debug!("Discovered tool: {}", tool.name);
                        tools.insert(tool.name.clone(), tool);
                    }
                }

                info!("Discovered {} tools", tools.len());
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:execute_tool (lines 223-282) ---[0m
    pub async fn execute_tool(&self, tool_name: &str, arguments: Value) -> Result<McpToolResult> {
        // Verify tool exists
        {
            let tools = self.tools.read().await;
            if !tools.contains_key(tool_name) {
                return Err(anyhow::anyhow!("Tool '{}' not found", tool_name));
            }
        }

        let request = JsonRpcRequest::new(
            self.next_request_id(),
            "tools/call".to_string(),
            Some(json!({
                "name": tool_name,
                "arguments": arguments
            })),
        );

        let response = self.send_request(request).await?;

        if let Some(error) = response.error {
            return Err(anyhow::anyhow!("Tool execution failed: {:?}", error));
        }

        let result = response
            .result
            .ok_or_else(|| anyhow::anyhow!("No result from tool execution"))?;

        // Parse tool result
        let content = result
            .get("content")
            .and_then(|v| v.as_array())
            .unwrap_or(&vec![])
            .iter()
            .filter_map(|v| {
                if let Some(text) = v.get("text").and_then(|t| t.as_str()) {
                    Some(McpContent::Text {
                        text: text.to_string(),
                    })
                } else if let (Some(data), Some(mime_type)) = (
                    v.get("data").and_then(|d| d.as_str()),
                    v.get("mimeType").and_then(|m| m.as_str()),
                ) {
                    Some(McpContent::Image {
                        data: data.to_string(),
                        mime_type: mime_type.to_string(),
                    })
                } else {
                    None
                }
            })
            .collect();

        let is_error = result
            .get("isError")
            .and_then(|v| v.as_bool())
            .unwrap_or(false);

        Ok(McpToolResult { content, is_error })
    }

  ./crates/ccswarm/src/mcp/client.rs:99-152 method start <-> ./crates/ccswarm/src/mcp/client.rs:291-313 method send_request
  Similarity: 83.82%
  Classes: McpClient <-> McpClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:start (lines 99-152) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        info!(
            "Starting MCP client: {}/{}",
            self.config.client_name, self.config.client_version
        );

        // Initialize with the server
        self.initialize().await?;

        // Discover available tools
        self.discover_tools().await?;

        // Start message handling loop
        let (shutdown_tx, mut shutdown_rx) = mpsc::channel(1);
        self.shutdown_tx = Some(shutdown_tx);

        let transport = self.transport.clone();
        let pending_requests = self.pending_requests.clone();

        tokio::spawn(async move {
            loop {
                tokio::select! {
                    // Handle shutdown signal
                    _ = shutdown_rx.recv() => {
                        info!("MCP client shutting down");
                        break;
                    }

                    // Handle incoming messages
                    message_result = async {
                        let mut transport = transport.lock().await;
                        transport.receive().await
                    } => {
                        match message_result {
                            Ok(Some(message)) => {
                                if let Err(e) = Self::handle_message(message, &pending_requests).await {
                                    error!("Error handling message: {}", e);
                                }
                            }
                            Ok(None) => {
                                warn!("Transport closed");
                                break;
                            }
                            Err(e) => {
                                error!("Error receiving message: {}", e);
                            }
                        }
                    }
                }
            }
        });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:send_request (lines 291-313) ---[0m
    async fn send_request(&self, request: JsonRpcRequest) -> Result<JsonRpcResponse> {
        let (response_tx, response_rx) = tokio::sync::oneshot::channel();

        // Store the pending request
        {
            let mut pending = self.pending_requests.write().await;
            pending.insert(request.id.clone(), response_tx);
        }

        // Send the request
        {
            let mut transport = self.transport.lock().await;
            transport.send(&JsonRpcMessage::Request(request)).await?;
        }

        // Wait for response with timeout
        let response = timeout(self.config.request_timeout, response_rx)
            .await
            .map_err(|_| anyhow::anyhow!("Request timeout"))?
            .map_err(|_| anyhow::anyhow!("Response channel closed"))?;

        Ok(response)
    }

  ./crates/ccswarm/src/mcp/client.rs:99-152 method start <-> ./crates/ccswarm/src/mcp/client.rs:168-192 method initialize
  Similarity: 73.60%
  Classes: McpClient <-> McpClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:start (lines 99-152) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        info!(
            "Starting MCP client: {}/{}",
            self.config.client_name, self.config.client_version
        );

        // Initialize with the server
        self.initialize().await?;

        // Discover available tools
        self.discover_tools().await?;

        // Start message handling loop
        let (shutdown_tx, mut shutdown_rx) = mpsc::channel(1);
        self.shutdown_tx = Some(shutdown_tx);

        let transport = self.transport.clone();
        let pending_requests = self.pending_requests.clone();

        tokio::spawn(async move {
            loop {
                tokio::select! {
                    // Handle shutdown signal
                    _ = shutdown_rx.recv() => {
                        info!("MCP client shutting down");
                        break;
                    }

                    // Handle incoming messages
                    message_result = async {
                        let mut transport = transport.lock().await;
                        transport.receive().await
                    } => {
                        match message_result {
                            Ok(Some(message)) => {
                                if let Err(e) = Self::handle_message(message, &pending_requests).await {
                                    error!("Error handling message: {}", e);
                                }
                            }
                            Ok(None) => {
                                warn!("Transport closed");
                                break;
                            }
                            Err(e) => {
                                error!("Error receiving message: {}", e);
                            }
                        }
                    }
                }
            }
        });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:initialize (lines 168-192) ---[0m
    async fn initialize(&self) -> Result<()> {
        let request = JsonRpcRequest::new(
            self.next_request_id(),
            "initialize".to_string(),
            Some(json!({
                "protocolVersion": "2024-11-05",
                "capabilities": {
                    "tools": {}
                },
                "clientInfo": {
                    "name": self.config.client_name,
                    "version": self.config.client_version
                }
            })),
        );

        let response = self.send_request(request).await?;

        if response.error.is_some() {
            return Err(anyhow::anyhow!("Initialize failed: {:?}", response.error));
        }

        info!("MCP client initialized successfully");
        Ok(())
    }

  ./crates/ccswarm/src/mcp/client.rs:99-152 method start <-> ./crates/ccswarm/src/mcp/client.rs:195-220 method discover_tools
  Similarity: 71.32%
  Classes: McpClient <-> McpClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:start (lines 99-152) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        info!(
            "Starting MCP client: {}/{}",
            self.config.client_name, self.config.client_version
        );

        // Initialize with the server
        self.initialize().await?;

        // Discover available tools
        self.discover_tools().await?;

        // Start message handling loop
        let (shutdown_tx, mut shutdown_rx) = mpsc::channel(1);
        self.shutdown_tx = Some(shutdown_tx);

        let transport = self.transport.clone();
        let pending_requests = self.pending_requests.clone();

        tokio::spawn(async move {
            loop {
                tokio::select! {
                    // Handle shutdown signal
                    _ = shutdown_rx.recv() => {
                        info!("MCP client shutting down");
                        break;
                    }

                    // Handle incoming messages
                    message_result = async {
                        let mut transport = transport.lock().await;
                        transport.receive().await
                    } => {
                        match message_result {
                            Ok(Some(message)) => {
                                if let Err(e) = Self::handle_message(message, &pending_requests).await {
                                    error!("Error handling message: {}", e);
                                }
                            }
                            Ok(None) => {
                                warn!("Transport closed");
                                break;
                            }
                            Err(e) => {
                                error!("Error receiving message: {}", e);
                            }
                        }
                    }
                }
            }
        });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:discover_tools (lines 195-220) ---[0m
    async fn discover_tools(&self) -> Result<()> {
        let request = JsonRpcRequest::new(self.next_request_id(), "tools/list".to_string(), None);

        let response = self.send_request(request).await?;

        if let Some(error) = response.error {
            return Err(anyhow::anyhow!("Tools discovery failed: {:?}", error));
        }

        if let Some(result) = response.result {
            if let Some(tools_array) = result.get("tools").and_then(|v| v.as_array()) {
                let mut tools = self.tools.write().await;

                for tool_value in tools_array {
                    if let Ok(tool) = serde_json::from_value::<McpTool>(tool_value.clone()) {
                        debug!("Discovered tool: {}", tool.name);
                        tools.insert(tool.name.clone(), tool);
                    }
                }

                info!("Discovered {} tools", tools.len());
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/mcp/client.rs:99-152 method start <-> ./crates/ccswarm/src/mcp/client.rs:434-453 method get_session_info
  Similarity: 70.57%
  Classes: McpClient <-> AiSessionClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:start (lines 99-152) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        info!(
            "Starting MCP client: {}/{}",
            self.config.client_name, self.config.client_version
        );

        // Initialize with the server
        self.initialize().await?;

        // Discover available tools
        self.discover_tools().await?;

        // Start message handling loop
        let (shutdown_tx, mut shutdown_rx) = mpsc::channel(1);
        self.shutdown_tx = Some(shutdown_tx);

        let transport = self.transport.clone();
        let pending_requests = self.pending_requests.clone();

        tokio::spawn(async move {
            loop {
                tokio::select! {
                    // Handle shutdown signal
                    _ = shutdown_rx.recv() => {
                        info!("MCP client shutting down");
                        break;
                    }

                    // Handle incoming messages
                    message_result = async {
                        let mut transport = transport.lock().await;
                        transport.receive().await
                    } => {
                        match message_result {
                            Ok(Some(message)) => {
                                if let Err(e) = Self::handle_message(message, &pending_requests).await {
                                    error!("Error handling message: {}", e);
                                }
                            }
                            Ok(None) => {
                                warn!("Transport closed");
                                break;
                            }
                            Err(e) => {
                                error!("Error receiving message: {}", e);
                            }
                        }
                    }
                }
            }
        });

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:get_session_info (lines 434-453) ---[0m
    pub async fn get_session_info(&self, session_id: &str) -> Result<Value> {
        let args = json!({ "session_id": session_id });

        let result = self
            .mcp_client
            .execute_tool("get_session_info", args)
            .await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Failed to get session info"));
        }

        // Parse JSON from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            serde_json::from_str(text)
                .map_err(|e| anyhow::anyhow!("Failed to parse session info: {}", e))
        } else {
            Ok(json!({}))
        }
    }

  ./crates/ccswarm/src/mcp/client.rs:291-313 method send_request <-> ./crates/ccswarm/src/mcp/client.rs:375-407 method create_session
  Similarity: 88.28%
  Classes: McpClient <-> AiSessionClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:send_request (lines 291-313) ---[0m
    async fn send_request(&self, request: JsonRpcRequest) -> Result<JsonRpcResponse> {
        let (response_tx, response_rx) = tokio::sync::oneshot::channel();

        // Store the pending request
        {
            let mut pending = self.pending_requests.write().await;
            pending.insert(request.id.clone(), response_tx);
        }

        // Send the request
        {
            let mut transport = self.transport.lock().await;
            transport.send(&JsonRpcMessage::Request(request)).await?;
        }

        // Wait for response with timeout
        let response = timeout(self.config.request_timeout, response_rx)
            .await
            .map_err(|_| anyhow::anyhow!("Request timeout"))?
            .map_err(|_| anyhow::anyhow!("Response channel closed"))?;

        Ok(response)
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:create_session (lines 375-407) ---[0m
    pub async fn create_session(
        &self,
        name: &str,
        working_directory: Option<&str>,
    ) -> Result<String> {
        let mut args = json!({ "name": name });
        if let Some(wd) = working_directory {
            args["working_directory"] = json!(wd);
        }

        let result = self.mcp_client.execute_tool("create_session", args).await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Failed to create session"));
        }

        // Extract session ID from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            // Parse session ID from response text
            if let Some(start) = text.find("ID: ") {
                let id_start = start + 4;
                if let Some(end) = text[id_start..].find(char::is_whitespace) {
                    return Ok(text[id_start..id_start + end].to_string());
                } else {
                    return Ok(text[id_start..].to_string());
                }
            }
        }

        Err(anyhow::anyhow!(
            "Could not extract session ID from response"
        ))
    }

  ./crates/ccswarm/src/mcp/client.rs:168-192 method initialize <-> ./crates/ccswarm/src/mcp/client.rs:195-220 method discover_tools
  Similarity: 93.66%
  Classes: McpClient <-> McpClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:initialize (lines 168-192) ---[0m
    async fn initialize(&self) -> Result<()> {
        let request = JsonRpcRequest::new(
            self.next_request_id(),
            "initialize".to_string(),
            Some(json!({
                "protocolVersion": "2024-11-05",
                "capabilities": {
                    "tools": {}
                },
                "clientInfo": {
                    "name": self.config.client_name,
                    "version": self.config.client_version
                }
            })),
        );

        let response = self.send_request(request).await?;

        if response.error.is_some() {
            return Err(anyhow::anyhow!("Initialize failed: {:?}", response.error));
        }

        info!("MCP client initialized successfully");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:discover_tools (lines 195-220) ---[0m
    async fn discover_tools(&self) -> Result<()> {
        let request = JsonRpcRequest::new(self.next_request_id(), "tools/list".to_string(), None);

        let response = self.send_request(request).await?;

        if let Some(error) = response.error {
            return Err(anyhow::anyhow!("Tools discovery failed: {:?}", error));
        }

        if let Some(result) = response.result {
            if let Some(tools_array) = result.get("tools").and_then(|v| v.as_array()) {
                let mut tools = self.tools.write().await;

                for tool_value in tools_array {
                    if let Ok(tool) = serde_json::from_value::<McpTool>(tool_value.clone()) {
                        debug!("Discovered tool: {}", tool.name);
                        tools.insert(tool.name.clone(), tool);
                    }
                }

                info!("Discovered {} tools", tools.len());
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/mcp/client.rs:195-220 method discover_tools <-> ./crates/ccswarm/src/mcp/client.rs:375-407 method create_session
  Similarity: 80.07%
  Classes: McpClient <-> AiSessionClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:discover_tools (lines 195-220) ---[0m
    async fn discover_tools(&self) -> Result<()> {
        let request = JsonRpcRequest::new(self.next_request_id(), "tools/list".to_string(), None);

        let response = self.send_request(request).await?;

        if let Some(error) = response.error {
            return Err(anyhow::anyhow!("Tools discovery failed: {:?}", error));
        }

        if let Some(result) = response.result {
            if let Some(tools_array) = result.get("tools").and_then(|v| v.as_array()) {
                let mut tools = self.tools.write().await;

                for tool_value in tools_array {
                    if let Ok(tool) = serde_json::from_value::<McpTool>(tool_value.clone()) {
                        debug!("Discovered tool: {}", tool.name);
                        tools.insert(tool.name.clone(), tool);
                    }
                }

                info!("Discovered {} tools", tools.len());
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:create_session (lines 375-407) ---[0m
    pub async fn create_session(
        &self,
        name: &str,
        working_directory: Option<&str>,
    ) -> Result<String> {
        let mut args = json!({ "name": name });
        if let Some(wd) = working_directory {
            args["working_directory"] = json!(wd);
        }

        let result = self.mcp_client.execute_tool("create_session", args).await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Failed to create session"));
        }

        // Extract session ID from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            // Parse session ID from response text
            if let Some(start) = text.find("ID: ") {
                let id_start = start + 4;
                if let Some(end) = text[id_start..].find(char::is_whitespace) {
                    return Ok(text[id_start..id_start + end].to_string());
                } else {
                    return Ok(text[id_start..].to_string());
                }
            }
        }

        Err(anyhow::anyhow!(
            "Could not extract session ID from response"
        ))
    }

  ./crates/ccswarm/src/mcp/client.rs:375-407 method create_session <-> ./crates/ccswarm/src/mcp/client.rs:410-431 method execute_command
  Similarity: 85.17%
  Classes: AiSessionClient <-> AiSessionClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:create_session (lines 375-407) ---[0m
    pub async fn create_session(
        &self,
        name: &str,
        working_directory: Option<&str>,
    ) -> Result<String> {
        let mut args = json!({ "name": name });
        if let Some(wd) = working_directory {
            args["working_directory"] = json!(wd);
        }

        let result = self.mcp_client.execute_tool("create_session", args).await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Failed to create session"));
        }

        // Extract session ID from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            // Parse session ID from response text
            if let Some(start) = text.find("ID: ") {
                let id_start = start + 4;
                if let Some(end) = text[id_start..].find(char::is_whitespace) {
                    return Ok(text[id_start..id_start + end].to_string());
                } else {
                    return Ok(text[id_start..].to_string());
                }
            }
        }

        Err(anyhow::anyhow!(
            "Could not extract session ID from response"
        ))
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:execute_command (lines 410-431) ---[0m
    pub async fn execute_command(&self, session_id: &str, command: &str) -> Result<String> {
        let args = json!({
            "session_id": session_id,
            "command": command
        });

        let result = self
            .mcp_client
            .execute_tool("execute_command", args)
            .await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Command execution failed"));
        }

        // Extract output from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            Ok(text.clone())
        } else {
            Ok(String::new())
        }
    }

  ./crates/ccswarm/src/mcp/client.rs:168-192 method initialize <-> ./crates/ccswarm/src/mcp/client.rs:375-407 method create_session
  Similarity: 79.90%
  Classes: McpClient <-> AiSessionClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:initialize (lines 168-192) ---[0m
    async fn initialize(&self) -> Result<()> {
        let request = JsonRpcRequest::new(
            self.next_request_id(),
            "initialize".to_string(),
            Some(json!({
                "protocolVersion": "2024-11-05",
                "capabilities": {
                    "tools": {}
                },
                "clientInfo": {
                    "name": self.config.client_name,
                    "version": self.config.client_version
                }
            })),
        );

        let response = self.send_request(request).await?;

        if response.error.is_some() {
            return Err(anyhow::anyhow!("Initialize failed: {:?}", response.error));
        }

        info!("MCP client initialized successfully");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:create_session (lines 375-407) ---[0m
    pub async fn create_session(
        &self,
        name: &str,
        working_directory: Option<&str>,
    ) -> Result<String> {
        let mut args = json!({ "name": name });
        if let Some(wd) = working_directory {
            args["working_directory"] = json!(wd);
        }

        let result = self.mcp_client.execute_tool("create_session", args).await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Failed to create session"));
        }

        // Extract session ID from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            // Parse session ID from response text
            if let Some(start) = text.find("ID: ") {
                let id_start = start + 4;
                if let Some(end) = text[id_start..].find(char::is_whitespace) {
                    return Ok(text[id_start..id_start + end].to_string());
                } else {
                    return Ok(text[id_start..].to_string());
                }
            }
        }

        Err(anyhow::anyhow!(
            "Could not extract session ID from response"
        ))
    }

  ./crates/ccswarm/src/mcp/client.rs:375-407 method create_session <-> ./crates/ccswarm/src/mcp/client.rs:434-453 method get_session_info
  Similarity: 85.17%
  Classes: AiSessionClient <-> AiSessionClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:create_session (lines 375-407) ---[0m
    pub async fn create_session(
        &self,
        name: &str,
        working_directory: Option<&str>,
    ) -> Result<String> {
        let mut args = json!({ "name": name });
        if let Some(wd) = working_directory {
            args["working_directory"] = json!(wd);
        }

        let result = self.mcp_client.execute_tool("create_session", args).await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Failed to create session"));
        }

        // Extract session ID from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            // Parse session ID from response text
            if let Some(start) = text.find("ID: ") {
                let id_start = start + 4;
                if let Some(end) = text[id_start..].find(char::is_whitespace) {
                    return Ok(text[id_start..id_start + end].to_string());
                } else {
                    return Ok(text[id_start..].to_string());
                }
            }
        }

        Err(anyhow::anyhow!(
            "Could not extract session ID from response"
        ))
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:get_session_info (lines 434-453) ---[0m
    pub async fn get_session_info(&self, session_id: &str) -> Result<Value> {
        let args = json!({ "session_id": session_id });

        let result = self
            .mcp_client
            .execute_tool("get_session_info", args)
            .await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Failed to get session info"));
        }

        // Parse JSON from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            serde_json::from_str(text)
                .map_err(|e| anyhow::anyhow!("Failed to parse session info: {}", e))
        } else {
            Ok(json!({}))
        }
    }

  ./crates/ccswarm/src/mcp/client.rs:195-220 method discover_tools <-> ./crates/ccswarm/src/mcp/client.rs:410-431 method execute_command
  Similarity: 92.12%
  Classes: McpClient <-> AiSessionClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:discover_tools (lines 195-220) ---[0m
    async fn discover_tools(&self) -> Result<()> {
        let request = JsonRpcRequest::new(self.next_request_id(), "tools/list".to_string(), None);

        let response = self.send_request(request).await?;

        if let Some(error) = response.error {
            return Err(anyhow::anyhow!("Tools discovery failed: {:?}", error));
        }

        if let Some(result) = response.result {
            if let Some(tools_array) = result.get("tools").and_then(|v| v.as_array()) {
                let mut tools = self.tools.write().await;

                for tool_value in tools_array {
                    if let Ok(tool) = serde_json::from_value::<McpTool>(tool_value.clone()) {
                        debug!("Discovered tool: {}", tool.name);
                        tools.insert(tool.name.clone(), tool);
                    }
                }

                info!("Discovered {} tools", tools.len());
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:execute_command (lines 410-431) ---[0m
    pub async fn execute_command(&self, session_id: &str, command: &str) -> Result<String> {
        let args = json!({
            "session_id": session_id,
            "command": command
        });

        let result = self
            .mcp_client
            .execute_tool("execute_command", args)
            .await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Command execution failed"));
        }

        // Extract output from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            Ok(text.clone())
        } else {
            Ok(String::new())
        }
    }

  ./crates/ccswarm/src/mcp/client.rs:195-220 method discover_tools <-> ./crates/ccswarm/src/mcp/client.rs:434-453 method get_session_info
  Similarity: 92.12%
  Classes: McpClient <-> AiSessionClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:discover_tools (lines 195-220) ---[0m
    async fn discover_tools(&self) -> Result<()> {
        let request = JsonRpcRequest::new(self.next_request_id(), "tools/list".to_string(), None);

        let response = self.send_request(request).await?;

        if let Some(error) = response.error {
            return Err(anyhow::anyhow!("Tools discovery failed: {:?}", error));
        }

        if let Some(result) = response.result {
            if let Some(tools_array) = result.get("tools").and_then(|v| v.as_array()) {
                let mut tools = self.tools.write().await;

                for tool_value in tools_array {
                    if let Ok(tool) = serde_json::from_value::<McpTool>(tool_value.clone()) {
                        debug!("Discovered tool: {}", tool.name);
                        tools.insert(tool.name.clone(), tool);
                    }
                }

                info!("Discovered {} tools", tools.len());
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:get_session_info (lines 434-453) ---[0m
    pub async fn get_session_info(&self, session_id: &str) -> Result<Value> {
        let args = json!({ "session_id": session_id });

        let result = self
            .mcp_client
            .execute_tool("get_session_info", args)
            .await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Failed to get session info"));
        }

        // Parse JSON from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            serde_json::from_str(text)
                .map_err(|e| anyhow::anyhow!("Failed to parse session info: {}", e))
        } else {
            Ok(json!({}))
        }
    }

  ./crates/ccswarm/src/mcp/client.rs:195-220 method discover_tools <-> ./crates/ccswarm/src/mcp/client.rs:291-313 method send_request
  Similarity: 85.65%
  Classes: McpClient <-> McpClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:discover_tools (lines 195-220) ---[0m
    async fn discover_tools(&self) -> Result<()> {
        let request = JsonRpcRequest::new(self.next_request_id(), "tools/list".to_string(), None);

        let response = self.send_request(request).await?;

        if let Some(error) = response.error {
            return Err(anyhow::anyhow!("Tools discovery failed: {:?}", error));
        }

        if let Some(result) = response.result {
            if let Some(tools_array) = result.get("tools").and_then(|v| v.as_array()) {
                let mut tools = self.tools.write().await;

                for tool_value in tools_array {
                    if let Ok(tool) = serde_json::from_value::<McpTool>(tool_value.clone()) {
                        debug!("Discovered tool: {}", tool.name);
                        tools.insert(tool.name.clone(), tool);
                    }
                }

                info!("Discovered {} tools", tools.len());
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:send_request (lines 291-313) ---[0m
    async fn send_request(&self, request: JsonRpcRequest) -> Result<JsonRpcResponse> {
        let (response_tx, response_rx) = tokio::sync::oneshot::channel();

        // Store the pending request
        {
            let mut pending = self.pending_requests.write().await;
            pending.insert(request.id.clone(), response_tx);
        }

        // Send the request
        {
            let mut transport = self.transport.lock().await;
            transport.send(&JsonRpcMessage::Request(request)).await?;
        }

        // Wait for response with timeout
        let response = timeout(self.config.request_timeout, response_rx)
            .await
            .map_err(|_| anyhow::anyhow!("Request timeout"))?
            .map_err(|_| anyhow::anyhow!("Response channel closed"))?;

        Ok(response)
    }

  ./crates/ccswarm/src/mcp/client.rs:168-192 method initialize <-> ./crates/ccswarm/src/mcp/client.rs:410-431 method execute_command
  Similarity: 86.65%
  Classes: McpClient <-> AiSessionClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:initialize (lines 168-192) ---[0m
    async fn initialize(&self) -> Result<()> {
        let request = JsonRpcRequest::new(
            self.next_request_id(),
            "initialize".to_string(),
            Some(json!({
                "protocolVersion": "2024-11-05",
                "capabilities": {
                    "tools": {}
                },
                "clientInfo": {
                    "name": self.config.client_name,
                    "version": self.config.client_version
                }
            })),
        );

        let response = self.send_request(request).await?;

        if response.error.is_some() {
            return Err(anyhow::anyhow!("Initialize failed: {:?}", response.error));
        }

        info!("MCP client initialized successfully");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:execute_command (lines 410-431) ---[0m
    pub async fn execute_command(&self, session_id: &str, command: &str) -> Result<String> {
        let args = json!({
            "session_id": session_id,
            "command": command
        });

        let result = self
            .mcp_client
            .execute_tool("execute_command", args)
            .await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Command execution failed"));
        }

        // Extract output from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            Ok(text.clone())
        } else {
            Ok(String::new())
        }
    }

  ./crates/ccswarm/src/mcp/client.rs:168-192 method initialize <-> ./crates/ccswarm/src/mcp/client.rs:291-313 method send_request
  Similarity: 83.29%
  Classes: McpClient <-> McpClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:initialize (lines 168-192) ---[0m
    async fn initialize(&self) -> Result<()> {
        let request = JsonRpcRequest::new(
            self.next_request_id(),
            "initialize".to_string(),
            Some(json!({
                "protocolVersion": "2024-11-05",
                "capabilities": {
                    "tools": {}
                },
                "clientInfo": {
                    "name": self.config.client_name,
                    "version": self.config.client_version
                }
            })),
        );

        let response = self.send_request(request).await?;

        if response.error.is_some() {
            return Err(anyhow::anyhow!("Initialize failed: {:?}", response.error));
        }

        info!("MCP client initialized successfully");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:send_request (lines 291-313) ---[0m
    async fn send_request(&self, request: JsonRpcRequest) -> Result<JsonRpcResponse> {
        let (response_tx, response_rx) = tokio::sync::oneshot::channel();

        // Store the pending request
        {
            let mut pending = self.pending_requests.write().await;
            pending.insert(request.id.clone(), response_tx);
        }

        // Send the request
        {
            let mut transport = self.transport.lock().await;
            transport.send(&JsonRpcMessage::Request(request)).await?;
        }

        // Wait for response with timeout
        let response = timeout(self.config.request_timeout, response_rx)
            .await
            .map_err(|_| anyhow::anyhow!("Request timeout"))?
            .map_err(|_| anyhow::anyhow!("Response channel closed"))?;

        Ok(response)
    }

  ./crates/ccswarm/src/mcp/client.rs:168-192 method initialize <-> ./crates/ccswarm/src/mcp/client.rs:434-453 method get_session_info
  Similarity: 86.65%
  Classes: McpClient <-> AiSessionClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:initialize (lines 168-192) ---[0m
    async fn initialize(&self) -> Result<()> {
        let request = JsonRpcRequest::new(
            self.next_request_id(),
            "initialize".to_string(),
            Some(json!({
                "protocolVersion": "2024-11-05",
                "capabilities": {
                    "tools": {}
                },
                "clientInfo": {
                    "name": self.config.client_name,
                    "version": self.config.client_version
                }
            })),
        );

        let response = self.send_request(request).await?;

        if response.error.is_some() {
            return Err(anyhow::anyhow!("Initialize failed: {:?}", response.error));
        }

        info!("MCP client initialized successfully");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:get_session_info (lines 434-453) ---[0m
    pub async fn get_session_info(&self, session_id: &str) -> Result<Value> {
        let args = json!({ "session_id": session_id });

        let result = self
            .mcp_client
            .execute_tool("get_session_info", args)
            .await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Failed to get session info"));
        }

        // Parse JSON from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            serde_json::from_str(text)
                .map_err(|e| anyhow::anyhow!("Failed to parse session info: {}", e))
        } else {
            Ok(json!({}))
        }
    }

  ./crates/ccswarm/src/mcp/client.rs:195-220 method discover_tools <-> ./crates/ccswarm/src/mcp/client.rs:316-343 method handle_message
  Similarity: 72.02%
  Classes: McpClient <-> McpClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:discover_tools (lines 195-220) ---[0m
    async fn discover_tools(&self) -> Result<()> {
        let request = JsonRpcRequest::new(self.next_request_id(), "tools/list".to_string(), None);

        let response = self.send_request(request).await?;

        if let Some(error) = response.error {
            return Err(anyhow::anyhow!("Tools discovery failed: {:?}", error));
        }

        if let Some(result) = response.result {
            if let Some(tools_array) = result.get("tools").and_then(|v| v.as_array()) {
                let mut tools = self.tools.write().await;

                for tool_value in tools_array {
                    if let Ok(tool) = serde_json::from_value::<McpTool>(tool_value.clone()) {
                        debug!("Discovered tool: {}", tool.name);
                        tools.insert(tool.name.clone(), tool);
                    }
                }

                info!("Discovered {} tools", tools.len());
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:handle_message (lines 316-343) ---[0m
    async fn handle_message(
        message: JsonRpcMessage,
        pending_requests: &Arc<
            RwLock<HashMap<RequestId, tokio::sync::oneshot::Sender<JsonRpcResponse>>>,
        >,
    ) -> Result<()> {
        match message {
            JsonRpcMessage::Response(response) => {
                // Find and complete pending request
                let mut pending = pending_requests.write().await;
                if let Some(sender) = pending.remove(&response.id) {
                    let _ = sender.send(response);
                } else {
                    warn!("Received response for unknown request: {}", response.id);
                }
            }
            JsonRpcMessage::Notification(notification) => {
                debug!("Received notification: {}", notification.method);
                // Handle notifications (currently just log them)
            }
            JsonRpcMessage::Request(request) => {
                warn!("Client received unexpected request: {}", request.method);
                // Clients don't typically handle requests
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/mcp/client.rs:410-431 method execute_command <-> ./crates/ccswarm/src/mcp/client.rs:434-453 method get_session_info
  Similarity: 92.59%
  Classes: AiSessionClient <-> AiSessionClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:execute_command (lines 410-431) ---[0m
    pub async fn execute_command(&self, session_id: &str, command: &str) -> Result<String> {
        let args = json!({
            "session_id": session_id,
            "command": command
        });

        let result = self
            .mcp_client
            .execute_tool("execute_command", args)
            .await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Command execution failed"));
        }

        // Extract output from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            Ok(text.clone())
        } else {
            Ok(String::new())
        }
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:get_session_info (lines 434-453) ---[0m
    pub async fn get_session_info(&self, session_id: &str) -> Result<Value> {
        let args = json!({ "session_id": session_id });

        let result = self
            .mcp_client
            .execute_tool("get_session_info", args)
            .await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Failed to get session info"));
        }

        // Parse JSON from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            serde_json::from_str(text)
                .map_err(|e| anyhow::anyhow!("Failed to parse session info: {}", e))
        } else {
            Ok(json!({}))
        }
    }

  ./crates/ccswarm/src/mcp/client.rs:291-313 method send_request <-> ./crates/ccswarm/src/mcp/client.rs:410-431 method execute_command
  Similarity: 82.19%
  Classes: McpClient <-> AiSessionClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:send_request (lines 291-313) ---[0m
    async fn send_request(&self, request: JsonRpcRequest) -> Result<JsonRpcResponse> {
        let (response_tx, response_rx) = tokio::sync::oneshot::channel();

        // Store the pending request
        {
            let mut pending = self.pending_requests.write().await;
            pending.insert(request.id.clone(), response_tx);
        }

        // Send the request
        {
            let mut transport = self.transport.lock().await;
            transport.send(&JsonRpcMessage::Request(request)).await?;
        }

        // Wait for response with timeout
        let response = timeout(self.config.request_timeout, response_rx)
            .await
            .map_err(|_| anyhow::anyhow!("Request timeout"))?
            .map_err(|_| anyhow::anyhow!("Response channel closed"))?;

        Ok(response)
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:execute_command (lines 410-431) ---[0m
    pub async fn execute_command(&self, session_id: &str, command: &str) -> Result<String> {
        let args = json!({
            "session_id": session_id,
            "command": command
        });

        let result = self
            .mcp_client
            .execute_tool("execute_command", args)
            .await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Command execution failed"));
        }

        // Extract output from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            Ok(text.clone())
        } else {
            Ok(String::new())
        }
    }

  ./crates/ccswarm/src/mcp/client.rs:291-313 method send_request <-> ./crates/ccswarm/src/mcp/client.rs:434-453 method get_session_info
  Similarity: 82.19%
  Classes: McpClient <-> AiSessionClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:send_request (lines 291-313) ---[0m
    async fn send_request(&self, request: JsonRpcRequest) -> Result<JsonRpcResponse> {
        let (response_tx, response_rx) = tokio::sync::oneshot::channel();

        // Store the pending request
        {
            let mut pending = self.pending_requests.write().await;
            pending.insert(request.id.clone(), response_tx);
        }

        // Send the request
        {
            let mut transport = self.transport.lock().await;
            transport.send(&JsonRpcMessage::Request(request)).await?;
        }

        // Wait for response with timeout
        let response = timeout(self.config.request_timeout, response_rx)
            .await
            .map_err(|_| anyhow::anyhow!("Request timeout"))?
            .map_err(|_| anyhow::anyhow!("Response channel closed"))?;

        Ok(response)
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:get_session_info (lines 434-453) ---[0m
    pub async fn get_session_info(&self, session_id: &str) -> Result<Value> {
        let args = json!({ "session_id": session_id });

        let result = self
            .mcp_client
            .execute_tool("get_session_info", args)
            .await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Failed to get session info"));
        }

        // Parse JSON from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            serde_json::from_str(text)
                .map_err(|e| anyhow::anyhow!("Failed to parse session info: {}", e))
        } else {
            Ok(json!({}))
        }
    }

  ./crates/ccswarm/src/mcp/client.rs:155-165 method shutdown <-> ./crates/ccswarm/src/mcp/client.rs:316-343 method handle_message
  Similarity: 75.00%
  Classes: McpClient <-> McpClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:shutdown (lines 155-165) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        if let Some(shutdown_tx) = self.shutdown_tx.take() {
            let _ = shutdown_tx.send(()).await;
        }

        // Close transport
        let mut transport = self.transport.lock().await;
        transport.close().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:handle_message (lines 316-343) ---[0m
    async fn handle_message(
        message: JsonRpcMessage,
        pending_requests: &Arc<
            RwLock<HashMap<RequestId, tokio::sync::oneshot::Sender<JsonRpcResponse>>>,
        >,
    ) -> Result<()> {
        match message {
            JsonRpcMessage::Response(response) => {
                // Find and complete pending request
                let mut pending = pending_requests.write().await;
                if let Some(sender) = pending.remove(&response.id) {
                    let _ = sender.send(response);
                } else {
                    warn!("Received response for unknown request: {}", response.id);
                }
            }
            JsonRpcMessage::Notification(notification) => {
                debug!("Received notification: {}", notification.method);
                // Handle notifications (currently just log them)
            }
            JsonRpcMessage::Request(request) => {
                warn!("Client received unexpected request: {}", request.method);
                // Clients don't typically handle requests
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/mcp/client.rs:87-96 method with_config <-> ./crates/ccswarm/src/mcp/client.rs:316-343 method handle_message
  Similarity: 70.24%
  Classes: McpClient <-> McpClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:with_config (lines 87-96) ---[0m
    pub fn with_config(transport: Box<dyn Transport>, config: McpClientConfig) -> Self {
        Self {
            transport: Arc::new(Mutex::new(transport)),
            request_id: AtomicI64::new(1),
            pending_requests: Arc::new(RwLock::new(HashMap::new())),
            tools: Arc::new(RwLock::new(HashMap::new())),
            config,
            shutdown_tx: None,
        }
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:handle_message (lines 316-343) ---[0m
    async fn handle_message(
        message: JsonRpcMessage,
        pending_requests: &Arc<
            RwLock<HashMap<RequestId, tokio::sync::oneshot::Sender<JsonRpcResponse>>>,
        >,
    ) -> Result<()> {
        match message {
            JsonRpcMessage::Response(response) => {
                // Find and complete pending request
                let mut pending = pending_requests.write().await;
                if let Some(sender) = pending.remove(&response.id) {
                    let _ = sender.send(response);
                } else {
                    warn!("Received response for unknown request: {}", response.id);
                }
            }
            JsonRpcMessage::Notification(notification) => {
                debug!("Received notification: {}", notification.method);
                // Handle notifications (currently just log them)
            }
            JsonRpcMessage::Request(request) => {
                warn!("Client received unexpected request: {}", request.method);
                // Clients don't typically handle requests
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/mcp/client.rs:155-165 method shutdown <-> ./crates/ccswarm/src/mcp/client.rs:410-431 method execute_command
  Similarity: 80.46%
  Classes: McpClient <-> AiSessionClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:shutdown (lines 155-165) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        if let Some(shutdown_tx) = self.shutdown_tx.take() {
            let _ = shutdown_tx.send(()).await;
        }

        // Close transport
        let mut transport = self.transport.lock().await;
        transport.close().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:execute_command (lines 410-431) ---[0m
    pub async fn execute_command(&self, session_id: &str, command: &str) -> Result<String> {
        let args = json!({
            "session_id": session_id,
            "command": command
        });

        let result = self
            .mcp_client
            .execute_tool("execute_command", args)
            .await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Command execution failed"));
        }

        // Extract output from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            Ok(text.clone())
        } else {
            Ok(String::new())
        }
    }

  ./crates/ccswarm/src/mcp/client.rs:155-165 method shutdown <-> ./crates/ccswarm/src/mcp/client.rs:434-453 method get_session_info
  Similarity: 82.53%
  Classes: McpClient <-> AiSessionClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:shutdown (lines 155-165) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        if let Some(shutdown_tx) = self.shutdown_tx.take() {
            let _ = shutdown_tx.send(()).await;
        }

        // Close transport
        let mut transport = self.transport.lock().await;
        transport.close().await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:get_session_info (lines 434-453) ---[0m
    pub async fn get_session_info(&self, session_id: &str) -> Result<Value> {
        let args = json!({ "session_id": session_id });

        let result = self
            .mcp_client
            .execute_tool("get_session_info", args)
            .await?;

        if result.is_error {
            return Err(anyhow::anyhow!("Failed to get session info"));
        }

        // Parse JSON from result
        if let Some(McpContent::Text { text }) = result.content.first() {
            serde_json::from_str(text)
                .map_err(|e| anyhow::anyhow!("Failed to parse session info: {}", e))
        } else {
            Ok(json!({}))
        }
    }

  ./crates/ccswarm/src/mcp/client.rs:70-77 method default <-> ./crates/ccswarm/src/mcp/client.rs:87-96 method with_config
  Similarity: 88.65%
  Classes: Default <-> McpClient

[36m--- ./crates/ccswarm/src/mcp/client.rs:default (lines 70-77) ---[0m
    fn default() -> Self {
        Self {
            request_timeout: Duration::from_secs(30),
            max_concurrent_requests: 100,
            client_name: "ccswarm".to_string(),
            client_version: env!("CARGO_PKG_VERSION").to_string(),
        }
    }

[36m--- ./crates/ccswarm/src/mcp/client.rs:with_config (lines 87-96) ---[0m
    pub fn with_config(transport: Box<dyn Transport>, config: McpClientConfig) -> Self {
        Self {
            transport: Arc::new(Mutex::new(transport)),
            request_id: AtomicI64::new(1),
            pending_requests: Arc::new(RwLock::new(HashMap::new())),
            tools: Arc::new(RwLock::new(HashMap::new())),
            config,
            shutdown_tx: None,
        }
    }


Duplicates in ./crates/ccswarm/src/subagent/manager.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/subagent/manager.rs:211-242 method create_dynamic_definition <-> ./crates/ccswarm/src/subagent/manager.rs:269-297 function test_dynamic_definition_creation
  Similarity: 81.91%

[36m--- ./crates/ccswarm/src/subagent/manager.rs:create_dynamic_definition (lines 211-242) ---[0m
    pub async fn create_dynamic_definition(
        &self,
        name: String,
        description: String,
        tools: super::SubagentTools,
        capabilities: Vec<String>,
        instructions: String,
    ) -> SubagentResult<()> {
        if !self.config.enable_dynamic_generation {
            return Err(SubagentError::Validation(
                "Dynamic subagent generation is disabled".to_string(),
            ));
        }

        let definition = SubagentDefinition {
            name: name.clone(),
            description,
            tools,
            capabilities,
            metadata: HashMap::new(),
        };

        // Validate the definition
        SubagentParser::validate_definition(&mut definition.clone())?;

        let mut definitions = self.definitions.write().await;
        definitions.insert(name.clone(), (definition, instructions));

        log::info!("Created dynamic subagent definition: {}", name);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:test_dynamic_definition_creation (lines 269-297) ---[0m
    async fn test_dynamic_definition_creation() {
        let config = SubagentConfig {
            enable_dynamic_generation: true,
            ..Default::default()
        };
        let manager = SubagentManager::new(config);

        let tools = super::super::SubagentTools {
            standard: vec!["read_file".to_string()],
            semantic: vec![],
            memory: vec![],
            custom: vec![],
        };

        let result = manager
            .create_dynamic_definition(
                "test-agent".to_string(),
                "Test agent".to_string(),
                tools,
                vec!["testing".to_string()],
                "Test instructions".to_string(),
            )
            .await;

        assert!(result.is_ok());

        let definitions = manager.list_definitions().await;
        assert!(definitions.contains(&"test-agent".to_string()));
    }

  ./crates/ccswarm/src/subagent/manager.rs:161-184 method delegate_task <-> ./crates/ccswarm/src/subagent/manager.rs:211-242 method create_dynamic_definition
  Similarity: 83.06%
  Classes: SubagentManager <-> SubagentManager

[36m--- ./crates/ccswarm/src/subagent/manager.rs:delegate_task (lines 161-184) ---[0m
    pub async fn delegate_task(&self, instance_id: &str, task: &str) -> SubagentResult<String> {
        let mut agents = self.agents.write().await;

        let agent = agents
            .get_mut(instance_id)
            .ok_or_else(|| SubagentError::NotFound(instance_id.to_string()))?;

        if agent.status != SubagentStatus::Available {
            return Err(SubagentError::Delegation(format!(
                "Subagent {} is not available",
                instance_id
            )));
        }

        agent.status = SubagentStatus::Busy;

        // Here we would actually delegate to the ai-session
        // For now, return a placeholder task ID
        let task_id = format!("task-{}", uuid::Uuid::new_v4());

        log::info!("Delegated task to subagent {}: {}", instance_id, task);

        Ok(task_id)
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:create_dynamic_definition (lines 211-242) ---[0m
    pub async fn create_dynamic_definition(
        &self,
        name: String,
        description: String,
        tools: super::SubagentTools,
        capabilities: Vec<String>,
        instructions: String,
    ) -> SubagentResult<()> {
        if !self.config.enable_dynamic_generation {
            return Err(SubagentError::Validation(
                "Dynamic subagent generation is disabled".to_string(),
            ));
        }

        let definition = SubagentDefinition {
            name: name.clone(),
            description,
            tools,
            capabilities,
            metadata: HashMap::new(),
        };

        // Validate the definition
        SubagentParser::validate_definition(&mut definition.clone())?;

        let mut definitions = self.definitions.write().await;
        definitions.insert(name.clone(), (definition, instructions));

        log::info!("Created dynamic subagent definition: {}", name);

        Ok(())
    }

  ./crates/ccswarm/src/subagent/manager.rs:161-184 method delegate_task <-> ./crates/ccswarm/src/subagent/manager.rs:269-297 function test_dynamic_definition_creation
  Similarity: 77.13%

[36m--- ./crates/ccswarm/src/subagent/manager.rs:delegate_task (lines 161-184) ---[0m
    pub async fn delegate_task(&self, instance_id: &str, task: &str) -> SubagentResult<String> {
        let mut agents = self.agents.write().await;

        let agent = agents
            .get_mut(instance_id)
            .ok_or_else(|| SubagentError::NotFound(instance_id.to_string()))?;

        if agent.status != SubagentStatus::Available {
            return Err(SubagentError::Delegation(format!(
                "Subagent {} is not available",
                instance_id
            )));
        }

        agent.status = SubagentStatus::Busy;

        // Here we would actually delegate to the ai-session
        // For now, return a placeholder task ID
        let task_id = format!("task-{}", uuid::Uuid::new_v4());

        log::info!("Delegated task to subagent {}: {}", instance_id, task);

        Ok(task_id)
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:test_dynamic_definition_creation (lines 269-297) ---[0m
    async fn test_dynamic_definition_creation() {
        let config = SubagentConfig {
            enable_dynamic_generation: true,
            ..Default::default()
        };
        let manager = SubagentManager::new(config);

        let tools = super::super::SubagentTools {
            standard: vec!["read_file".to_string()],
            semantic: vec![],
            memory: vec![],
            custom: vec![],
        };

        let result = manager
            .create_dynamic_definition(
                "test-agent".to_string(),
                "Test agent".to_string(),
                tools,
                vec!["testing".to_string()],
                "Test instructions".to_string(),
            )
            .await;

        assert!(result.is_ok());

        let definitions = manager.list_definitions().await;
        assert!(definitions.contains(&"test-agent".to_string()));
    }

  ./crates/ccswarm/src/subagent/manager.rs:122-137 method initialize_subagent <-> ./crates/ccswarm/src/subagent/manager.rs:211-242 method create_dynamic_definition
  Similarity: 79.13%
  Classes: SubagentManager <-> SubagentManager

[36m--- ./crates/ccswarm/src/subagent/manager.rs:initialize_subagent (lines 122-137) ---[0m
    async fn initialize_subagent(&self, instance_id: &str) -> SubagentResult<()> {
        // Here we would integrate with ai-session to create the actual session
        // For now, we'll simulate the initialization

        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        let mut agents = self.agents.write().await;
        if let Some(agent) = agents.get_mut(instance_id) {
            // In a real implementation, we would create an ai-session here
            agent.session_id = Some(format!("session-{}", instance_id));
            agent.status = SubagentStatus::Available;
            log::info!("Subagent {} initialized successfully", instance_id);
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:create_dynamic_definition (lines 211-242) ---[0m
    pub async fn create_dynamic_definition(
        &self,
        name: String,
        description: String,
        tools: super::SubagentTools,
        capabilities: Vec<String>,
        instructions: String,
    ) -> SubagentResult<()> {
        if !self.config.enable_dynamic_generation {
            return Err(SubagentError::Validation(
                "Dynamic subagent generation is disabled".to_string(),
            ));
        }

        let definition = SubagentDefinition {
            name: name.clone(),
            description,
            tools,
            capabilities,
            metadata: HashMap::new(),
        };

        // Validate the definition
        SubagentParser::validate_definition(&mut definition.clone())?;

        let mut definitions = self.definitions.write().await;
        definitions.insert(name.clone(), (definition, instructions));

        log::info!("Created dynamic subagent definition: {}", name);

        Ok(())
    }

  ./crates/ccswarm/src/subagent/manager.rs:122-137 method initialize_subagent <-> ./crates/ccswarm/src/subagent/manager.rs:269-297 function test_dynamic_definition_creation
  Similarity: 84.00%

[36m--- ./crates/ccswarm/src/subagent/manager.rs:initialize_subagent (lines 122-137) ---[0m
    async fn initialize_subagent(&self, instance_id: &str) -> SubagentResult<()> {
        // Here we would integrate with ai-session to create the actual session
        // For now, we'll simulate the initialization

        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        let mut agents = self.agents.write().await;
        if let Some(agent) = agents.get_mut(instance_id) {
            // In a real implementation, we would create an ai-session here
            agent.session_id = Some(format!("session-{}", instance_id));
            agent.status = SubagentStatus::Available;
            log::info!("Subagent {} initialized successfully", instance_id);
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:test_dynamic_definition_creation (lines 269-297) ---[0m
    async fn test_dynamic_definition_creation() {
        let config = SubagentConfig {
            enable_dynamic_generation: true,
            ..Default::default()
        };
        let manager = SubagentManager::new(config);

        let tools = super::super::SubagentTools {
            standard: vec!["read_file".to_string()],
            semantic: vec![],
            memory: vec![],
            custom: vec![],
        };

        let result = manager
            .create_dynamic_definition(
                "test-agent".to_string(),
                "Test agent".to_string(),
                tools,
                vec!["testing".to_string()],
                "Test instructions".to_string(),
            )
            .await;

        assert!(result.is_ok());

        let definitions = manager.list_definitions().await;
        assert!(definitions.contains(&"test-agent".to_string()));
    }

  ./crates/ccswarm/src/subagent/manager.rs:52-67 method initialize <-> ./crates/ccswarm/src/subagent/manager.rs:269-297 function test_dynamic_definition_creation
  Similarity: 82.13%

[36m--- ./crates/ccswarm/src/subagent/manager.rs:initialize (lines 52-67) ---[0m
    pub async fn initialize(&self) -> SubagentResult<()> {
        // Load definitions from the agents directory
        let definitions = SubagentParser::parse_directory(&self.config.agents_dir)?;

        let mut defs = self.definitions.write().await;
        for (definition, instructions) in definitions {
            log::info!("Loaded subagent definition: {}", definition.name);
            defs.insert(definition.name.clone(), (definition, instructions));
        }

        log::info!(
            "Initialized SubagentManager with {} definitions",
            defs.len()
        );
        Ok(())
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:test_dynamic_definition_creation (lines 269-297) ---[0m
    async fn test_dynamic_definition_creation() {
        let config = SubagentConfig {
            enable_dynamic_generation: true,
            ..Default::default()
        };
        let manager = SubagentManager::new(config);

        let tools = super::super::SubagentTools {
            standard: vec!["read_file".to_string()],
            semantic: vec![],
            memory: vec![],
            custom: vec![],
        };

        let result = manager
            .create_dynamic_definition(
                "test-agent".to_string(),
                "Test agent".to_string(),
                tools,
                vec!["testing".to_string()],
                "Test instructions".to_string(),
            )
            .await;

        assert!(result.is_ok());

        let definitions = manager.list_definitions().await;
        assert!(definitions.contains(&"test-agent".to_string()));
    }

  ./crates/ccswarm/src/subagent/manager.rs:52-67 method initialize <-> ./crates/ccswarm/src/subagent/manager.rs:211-242 method create_dynamic_definition
  Similarity: 76.39%
  Classes: SubagentManager <-> SubagentManager

[36m--- ./crates/ccswarm/src/subagent/manager.rs:initialize (lines 52-67) ---[0m
    pub async fn initialize(&self) -> SubagentResult<()> {
        // Load definitions from the agents directory
        let definitions = SubagentParser::parse_directory(&self.config.agents_dir)?;

        let mut defs = self.definitions.write().await;
        for (definition, instructions) in definitions {
            log::info!("Loaded subagent definition: {}", definition.name);
            defs.insert(definition.name.clone(), (definition, instructions));
        }

        log::info!(
            "Initialized SubagentManager with {} definitions",
            defs.len()
        );
        Ok(())
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:create_dynamic_definition (lines 211-242) ---[0m
    pub async fn create_dynamic_definition(
        &self,
        name: String,
        description: String,
        tools: super::SubagentTools,
        capabilities: Vec<String>,
        instructions: String,
    ) -> SubagentResult<()> {
        if !self.config.enable_dynamic_generation {
            return Err(SubagentError::Validation(
                "Dynamic subagent generation is disabled".to_string(),
            ));
        }

        let definition = SubagentDefinition {
            name: name.clone(),
            description,
            tools,
            capabilities,
            metadata: HashMap::new(),
        };

        // Validate the definition
        SubagentParser::validate_definition(&mut definition.clone())?;

        let mut definitions = self.definitions.write().await;
        definitions.insert(name.clone(), (definition, instructions));

        log::info!("Created dynamic subagent definition: {}", name);

        Ok(())
    }

  ./crates/ccswarm/src/subagent/manager.rs:122-137 method initialize_subagent <-> ./crates/ccswarm/src/subagent/manager.rs:161-184 method delegate_task
  Similarity: 77.70%
  Classes: SubagentManager <-> SubagentManager

[36m--- ./crates/ccswarm/src/subagent/manager.rs:initialize_subagent (lines 122-137) ---[0m
    async fn initialize_subagent(&self, instance_id: &str) -> SubagentResult<()> {
        // Here we would integrate with ai-session to create the actual session
        // For now, we'll simulate the initialization

        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        let mut agents = self.agents.write().await;
        if let Some(agent) = agents.get_mut(instance_id) {
            // In a real implementation, we would create an ai-session here
            agent.session_id = Some(format!("session-{}", instance_id));
            agent.status = SubagentStatus::Available;
            log::info!("Subagent {} initialized successfully", instance_id);
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:delegate_task (lines 161-184) ---[0m
    pub async fn delegate_task(&self, instance_id: &str, task: &str) -> SubagentResult<String> {
        let mut agents = self.agents.write().await;

        let agent = agents
            .get_mut(instance_id)
            .ok_or_else(|| SubagentError::NotFound(instance_id.to_string()))?;

        if agent.status != SubagentStatus::Available {
            return Err(SubagentError::Delegation(format!(
                "Subagent {} is not available",
                instance_id
            )));
        }

        agent.status = SubagentStatus::Busy;

        // Here we would actually delegate to the ai-session
        // For now, return a placeholder task ID
        let task_id = format!("task-{}", uuid::Uuid::new_v4());

        log::info!("Delegated task to subagent {}: {}", instance_id, task);

        Ok(task_id)
    }

  ./crates/ccswarm/src/subagent/manager.rs:52-67 method initialize <-> ./crates/ccswarm/src/subagent/manager.rs:161-184 method delegate_task
  Similarity: 76.21%
  Classes: SubagentManager <-> SubagentManager

[36m--- ./crates/ccswarm/src/subagent/manager.rs:initialize (lines 52-67) ---[0m
    pub async fn initialize(&self) -> SubagentResult<()> {
        // Load definitions from the agents directory
        let definitions = SubagentParser::parse_directory(&self.config.agents_dir)?;

        let mut defs = self.definitions.write().await;
        for (definition, instructions) in definitions {
            log::info!("Loaded subagent definition: {}", definition.name);
            defs.insert(definition.name.clone(), (definition, instructions));
        }

        log::info!(
            "Initialized SubagentManager with {} definitions",
            defs.len()
        );
        Ok(())
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:delegate_task (lines 161-184) ---[0m
    pub async fn delegate_task(&self, instance_id: &str, task: &str) -> SubagentResult<String> {
        let mut agents = self.agents.write().await;

        let agent = agents
            .get_mut(instance_id)
            .ok_or_else(|| SubagentError::NotFound(instance_id.to_string()))?;

        if agent.status != SubagentStatus::Available {
            return Err(SubagentError::Delegation(format!(
                "Subagent {} is not available",
                instance_id
            )));
        }

        agent.status = SubagentStatus::Busy;

        // Here we would actually delegate to the ai-session
        // For now, return a placeholder task ID
        let task_id = format!("task-{}", uuid::Uuid::new_v4());

        log::info!("Delegated task to subagent {}: {}", instance_id, task);

        Ok(task_id)
    }

  ./crates/ccswarm/src/subagent/manager.rs:52-67 method initialize <-> ./crates/ccswarm/src/subagent/manager.rs:122-137 method initialize_subagent
  Similarity: 78.44%
  Classes: SubagentManager <-> SubagentManager

[36m--- ./crates/ccswarm/src/subagent/manager.rs:initialize (lines 52-67) ---[0m
    pub async fn initialize(&self) -> SubagentResult<()> {
        // Load definitions from the agents directory
        let definitions = SubagentParser::parse_directory(&self.config.agents_dir)?;

        let mut defs = self.definitions.write().await;
        for (definition, instructions) in definitions {
            log::info!("Loaded subagent definition: {}", definition.name);
            defs.insert(definition.name.clone(), (definition, instructions));
        }

        log::info!(
            "Initialized SubagentManager with {} definitions",
            defs.len()
        );
        Ok(())
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:initialize_subagent (lines 122-137) ---[0m
    async fn initialize_subagent(&self, instance_id: &str) -> SubagentResult<()> {
        // Here we would integrate with ai-session to create the actual session
        // For now, we'll simulate the initialization

        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        let mut agents = self.agents.write().await;
        if let Some(agent) = agents.get_mut(instance_id) {
            // In a real implementation, we would create an ai-session here
            agent.session_id = Some(format!("session-{}", instance_id));
            agent.status = SubagentStatus::Available;
            log::info!("Subagent {} initialized successfully", instance_id);
        }

        Ok(())
    }

  ./crates/ccswarm/src/subagent/manager.rs:122-137 method initialize_subagent <-> ./crates/ccswarm/src/subagent/manager.rs:187-196 method complete_task
  Similarity: 76.88%
  Classes: SubagentManager <-> SubagentManager

[36m--- ./crates/ccswarm/src/subagent/manager.rs:initialize_subagent (lines 122-137) ---[0m
    async fn initialize_subagent(&self, instance_id: &str) -> SubagentResult<()> {
        // Here we would integrate with ai-session to create the actual session
        // For now, we'll simulate the initialization

        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        let mut agents = self.agents.write().await;
        if let Some(agent) = agents.get_mut(instance_id) {
            // In a real implementation, we would create an ai-session here
            agent.session_id = Some(format!("session-{}", instance_id));
            agent.status = SubagentStatus::Available;
            log::info!("Subagent {} initialized successfully", instance_id);
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:complete_task (lines 187-196) ---[0m
    pub async fn complete_task(&self, instance_id: &str) -> SubagentResult<()> {
        let mut agents = self.agents.write().await;

        if let Some(agent) = agents.get_mut(instance_id) {
            agent.status = SubagentStatus::Available;
            log::info!("Subagent {} completed task", instance_id);
        }

        Ok(())
    }

  ./crates/ccswarm/src/subagent/manager.rs:187-196 method complete_task <-> ./crates/ccswarm/src/subagent/manager.rs:199-208 method remove_subagent
  Similarity: 75.74%
  Classes: SubagentManager <-> SubagentManager

[36m--- ./crates/ccswarm/src/subagent/manager.rs:complete_task (lines 187-196) ---[0m
    pub async fn complete_task(&self, instance_id: &str) -> SubagentResult<()> {
        let mut agents = self.agents.write().await;

        if let Some(agent) = agents.get_mut(instance_id) {
            agent.status = SubagentStatus::Available;
            log::info!("Subagent {} completed task", instance_id);
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:remove_subagent (lines 199-208) ---[0m
    pub async fn remove_subagent(&self, instance_id: &str) -> SubagentResult<()> {
        let mut agents = self.agents.write().await;

        if agents.remove(instance_id).is_some() {
            log::info!("Removed subagent instance: {}", instance_id);
            Ok(())
        } else {
            Err(SubagentError::NotFound(instance_id.to_string()))
        }
    }

  ./crates/ccswarm/src/subagent/manager.rs:152-158 method list_instances <-> ./crates/ccswarm/src/subagent/manager.rs:199-208 method remove_subagent
  Similarity: 87.77%
  Classes: SubagentManager <-> SubagentManager

[36m--- ./crates/ccswarm/src/subagent/manager.rs:list_instances (lines 152-158) ---[0m
    pub async fn list_instances(&self) -> HashMap<String, SubagentStatus> {
        let agents = self.agents.read().await;
        agents
            .iter()
            .map(|(id, agent)| (id.clone(), agent.status.clone()))
            .collect()
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:remove_subagent (lines 199-208) ---[0m
    pub async fn remove_subagent(&self, instance_id: &str) -> SubagentResult<()> {
        let mut agents = self.agents.write().await;

        if agents.remove(instance_id).is_some() {
            log::info!("Removed subagent instance: {}", instance_id);
            Ok(())
        } else {
            Err(SubagentError::NotFound(instance_id.to_string()))
        }
    }

  ./crates/ccswarm/src/subagent/manager.rs:152-158 method list_instances <-> ./crates/ccswarm/src/subagent/manager.rs:187-196 method complete_task
  Similarity: 80.37%
  Classes: SubagentManager <-> SubagentManager

[36m--- ./crates/ccswarm/src/subagent/manager.rs:list_instances (lines 152-158) ---[0m
    pub async fn list_instances(&self) -> HashMap<String, SubagentStatus> {
        let agents = self.agents.read().await;
        agents
            .iter()
            .map(|(id, agent)| (id.clone(), agent.status.clone()))
            .collect()
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:complete_task (lines 187-196) ---[0m
    pub async fn complete_task(&self, instance_id: &str) -> SubagentResult<()> {
        let mut agents = self.agents.write().await;

        if let Some(agent) = agents.get_mut(instance_id) {
            agent.status = SubagentStatus::Available;
            log::info!("Subagent {} completed task", instance_id);
        }

        Ok(())
    }

  ./crates/ccswarm/src/subagent/manager.rs:187-196 method complete_task <-> ./crates/ccswarm/src/subagent/manager.rs:260-266 function test_manager_initialization
  Similarity: 75.06%

[36m--- ./crates/ccswarm/src/subagent/manager.rs:complete_task (lines 187-196) ---[0m
    pub async fn complete_task(&self, instance_id: &str) -> SubagentResult<()> {
        let mut agents = self.agents.write().await;

        if let Some(agent) = agents.get_mut(instance_id) {
            agent.status = SubagentStatus::Available;
            log::info!("Subagent {} completed task", instance_id);
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:test_manager_initialization (lines 260-266) ---[0m
    async fn test_manager_initialization() {
        let config = SubagentConfig::default();
        let manager = SubagentManager::new(config);

        // Should initialize without error even with no definitions
        assert!(manager.initialize().await.is_ok());
    }

  ./crates/ccswarm/src/subagent/manager.rs:43-49 method new <-> ./crates/ccswarm/src/subagent/manager.rs:246-252 method clone
  Similarity: 87.69%
  Classes: SubagentManager <-> Clone

[36m--- ./crates/ccswarm/src/subagent/manager.rs:new (lines 43-49) ---[0m
    pub fn new(config: SubagentConfig) -> Self {
        Self {
            config,
            agents: Arc::new(RwLock::new(HashMap::new())),
            definitions: Arc::new(RwLock::new(HashMap::new())),
        }
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:clone (lines 246-252) ---[0m
    fn clone(&self) -> Self {
        Self {
            config: self.config.clone(),
            agents: Arc::clone(&self.agents),
            definitions: Arc::clone(&self.definitions),
        }
    }

  ./crates/ccswarm/src/subagent/manager.rs:146-149 method list_definitions <-> ./crates/ccswarm/src/subagent/manager.rs:187-196 method complete_task
  Similarity: 79.63%
  Classes: SubagentManager <-> SubagentManager

[36m--- ./crates/ccswarm/src/subagent/manager.rs:list_definitions (lines 146-149) ---[0m
    pub async fn list_definitions(&self) -> Vec<String> {
        let definitions = self.definitions.read().await;
        definitions.keys().cloned().collect()
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:complete_task (lines 187-196) ---[0m
    pub async fn complete_task(&self, instance_id: &str) -> SubagentResult<()> {
        let mut agents = self.agents.write().await;

        if let Some(agent) = agents.get_mut(instance_id) {
            agent.status = SubagentStatus::Available;
            log::info!("Subagent {} completed task", instance_id);
        }

        Ok(())
    }

  ./crates/ccswarm/src/subagent/manager.rs:146-149 method list_definitions <-> ./crates/ccswarm/src/subagent/manager.rs:152-158 method list_instances
  Similarity: 95.33%
  Classes: SubagentManager <-> SubagentManager

[36m--- ./crates/ccswarm/src/subagent/manager.rs:list_definitions (lines 146-149) ---[0m
    pub async fn list_definitions(&self) -> Vec<String> {
        let definitions = self.definitions.read().await;
        definitions.keys().cloned().collect()
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:list_instances (lines 152-158) ---[0m
    pub async fn list_instances(&self) -> HashMap<String, SubagentStatus> {
        let agents = self.agents.read().await;
        agents
            .iter()
            .map(|(id, agent)| (id.clone(), agent.status.clone()))
            .collect()
    }

  ./crates/ccswarm/src/subagent/manager.rs:140-143 method get_status <-> ./crates/ccswarm/src/subagent/manager.rs:152-158 method list_instances
  Similarity: 93.87%
  Classes: SubagentManager <-> SubagentManager

[36m--- ./crates/ccswarm/src/subagent/manager.rs:get_status (lines 140-143) ---[0m
    pub async fn get_status(&self, instance_id: &str) -> Option<SubagentStatus> {
        let agents = self.agents.read().await;
        agents.get(instance_id).map(|a| a.status.clone())
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:list_instances (lines 152-158) ---[0m
    pub async fn list_instances(&self) -> HashMap<String, SubagentStatus> {
        let agents = self.agents.read().await;
        agents
            .iter()
            .map(|(id, agent)| (id.clone(), agent.status.clone()))
            .collect()
    }

  ./crates/ccswarm/src/subagent/manager.rs:140-143 method get_status <-> ./crates/ccswarm/src/subagent/manager.rs:199-208 method remove_subagent
  Similarity: 73.62%
  Classes: SubagentManager <-> SubagentManager

[36m--- ./crates/ccswarm/src/subagent/manager.rs:get_status (lines 140-143) ---[0m
    pub async fn get_status(&self, instance_id: &str) -> Option<SubagentStatus> {
        let agents = self.agents.read().await;
        agents.get(instance_id).map(|a| a.status.clone())
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:remove_subagent (lines 199-208) ---[0m
    pub async fn remove_subagent(&self, instance_id: &str) -> SubagentResult<()> {
        let mut agents = self.agents.write().await;

        if agents.remove(instance_id).is_some() {
            log::info!("Removed subagent instance: {}", instance_id);
            Ok(())
        } else {
            Err(SubagentError::NotFound(instance_id.to_string()))
        }
    }

  ./crates/ccswarm/src/subagent/manager.rs:140-143 method get_status <-> ./crates/ccswarm/src/subagent/manager.rs:146-149 method list_definitions
  Similarity: 90.00%
  Classes: SubagentManager <-> SubagentManager

[36m--- ./crates/ccswarm/src/subagent/manager.rs:get_status (lines 140-143) ---[0m
    pub async fn get_status(&self, instance_id: &str) -> Option<SubagentStatus> {
        let agents = self.agents.read().await;
        agents.get(instance_id).map(|a| a.status.clone())
    }

[36m--- ./crates/ccswarm/src/subagent/manager.rs:list_definitions (lines 146-149) ---[0m
    pub async fn list_definitions(&self) -> Vec<String> {
        let definitions = self.definitions.read().await;
        definitions.keys().cloned().collect()
    }


Duplicates in ./crates/ccswarm/src/tests/monitoring_tests.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/tests/monitoring_tests.rs:42-90 function test_output_filtering <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:337-390 function test_output_entry_filter_matching
  Similarity: 85.35%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_filtering (lines 42-90) ---[0m
async fn test_output_filtering() {
    let monitoring = MonitoringSystem::new();
    let agent_id = "test-agent-456".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add various types of output
    let outputs = vec![
        (OutputType::Info, "Info message"),
        (OutputType::Warning, "Warning message"),
        (OutputType::Error, "Error message"),
        (OutputType::Debug, "Debug message"),
    ];
    
    for (output_type, content) in outputs {
        monitoring.add_output(
            agent_id.clone(),
            "Backend".to_string(),
            output_type,
            content.to_string(),
            None,
            "session-1".to_string(),
        ).unwrap();
    }
    
    // Test filtering by output type
    let error_filter = OutputFilter {
        agent_ids: None,
        output_types: Some(vec![OutputType::Error]),
        content_pattern: None,
        task_id: None,
    };
    
    let filtered = monitoring.get_all_recent(10, Some(&error_filter));
    assert_eq!(filtered.len(), 1);
    assert_eq!(filtered[0].content, "Error message");
    
    // Test filtering by content pattern
    let warning_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: Some("Warning".to_string()),
        task_id: None,
    };
    
    let warning_filtered = monitoring.get_all_recent(10, Some(&warning_filter));
    assert_eq!(warning_filtered.len(), 1);
    assert_eq!(warning_filtered[0].content, "Warning message");
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_entry_filter_matching (lines 337-390) ---[0m
fn test_output_entry_filter_matching() {
    let entry = OutputEntry::new(
        "test-agent-filter".to_string(),
        "Frontend".to_string(),
        OutputType::Warning,
        "This is a test warning message".to_string(),
        Some("task-123".to_string()),
        "session-abc".to_string(),
    );
    
    // Test agent filter
    let agent_filter = OutputFilter {
        agent_ids: Some(vec!["test-agent-filter".to_string()]),
        output_types: None,
        content_pattern: None,
        task_id: None,
    };
    assert!(entry.matches_filter(&agent_filter));
    
    let wrong_agent_filter = OutputFilter {
        agent_ids: Some(vec!["other-agent".to_string()]),
        output_types: None,
        content_pattern: None,
        task_id: None,
    };
    assert!(!entry.matches_filter(&wrong_agent_filter));
    
    // Test output type filter
    let type_filter = OutputFilter {
        agent_ids: None,
        output_types: Some(vec![OutputType::Warning]),
        content_pattern: None,
        task_id: None,
    };
    assert!(entry.matches_filter(&type_filter));
    
    // Test content pattern filter
    let content_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: Some("warning".to_string()),
        task_id: None,
    };
    assert!(entry.matches_filter(&content_filter));
    
    // Test task ID filter
    let task_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: None,
        task_id: Some("task-123".to_string()),
    };
    assert!(entry.matches_filter(&task_filter));
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:291-334 function test_concurrent_access <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:337-390 function test_output_entry_filter_matching
  Similarity: 86.86%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_concurrent_access (lines 291-334) ---[0m
async fn test_concurrent_access() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let agent_id = "concurrent-test-agent".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Spawn multiple tasks adding output concurrently
    let mut handles = Vec::new();
    
    for i in 0..10 {
        let monitoring_clone = Arc::clone(&monitoring);
        let agent_id_clone = agent_id.clone();
        
        let handle = tokio::spawn(async move {
            for j in 0..20 {
                monitoring_clone.add_output(
                    agent_id_clone.clone(),
                    "Concurrent".to_string(),
                    OutputType::Info,
                    format!("Concurrent message {} from task {}", j, i),
                    None,
                    format!("session-{}", i),
                ).unwrap();
                
                // Small delay to increase chance of concurrent access
                sleep(Duration::from_millis(1)).await;
            }
        });
        
        handles.push(handle);
    }
    
    // Wait for all tasks to complete
    for handle in handles {
        handle.await.unwrap();
    }
    
    // Verify all messages were added
    let all_recent = monitoring.get_all_recent(300, None);
    assert_eq!(all_recent.len(), 200); // 10 tasks * 20 messages each
    
    let stats = monitoring.get_stats();
    assert_eq!(stats.total_entries, 200);
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_entry_filter_matching (lines 337-390) ---[0m
fn test_output_entry_filter_matching() {
    let entry = OutputEntry::new(
        "test-agent-filter".to_string(),
        "Frontend".to_string(),
        OutputType::Warning,
        "This is a test warning message".to_string(),
        Some("task-123".to_string()),
        "session-abc".to_string(),
    );
    
    // Test agent filter
    let agent_filter = OutputFilter {
        agent_ids: Some(vec!["test-agent-filter".to_string()]),
        output_types: None,
        content_pattern: None,
        task_id: None,
    };
    assert!(entry.matches_filter(&agent_filter));
    
    let wrong_agent_filter = OutputFilter {
        agent_ids: Some(vec!["other-agent".to_string()]),
        output_types: None,
        content_pattern: None,
        task_id: None,
    };
    assert!(!entry.matches_filter(&wrong_agent_filter));
    
    // Test output type filter
    let type_filter = OutputFilter {
        agent_ids: None,
        output_types: Some(vec![OutputType::Warning]),
        content_pattern: None,
        task_id: None,
    };
    assert!(entry.matches_filter(&type_filter));
    
    // Test content pattern filter
    let content_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: Some("warning".to_string()),
        task_id: None,
    };
    assert!(entry.matches_filter(&content_filter));
    
    // Test task ID filter
    let task_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: None,
        task_id: Some("task-123".to_string()),
    };
    assert!(entry.matches_filter(&task_filter));
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:42-90 function test_output_filtering <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:291-334 function test_concurrent_access
  Similarity: 84.71%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_filtering (lines 42-90) ---[0m
async fn test_output_filtering() {
    let monitoring = MonitoringSystem::new();
    let agent_id = "test-agent-456".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add various types of output
    let outputs = vec![
        (OutputType::Info, "Info message"),
        (OutputType::Warning, "Warning message"),
        (OutputType::Error, "Error message"),
        (OutputType::Debug, "Debug message"),
    ];
    
    for (output_type, content) in outputs {
        monitoring.add_output(
            agent_id.clone(),
            "Backend".to_string(),
            output_type,
            content.to_string(),
            None,
            "session-1".to_string(),
        ).unwrap();
    }
    
    // Test filtering by output type
    let error_filter = OutputFilter {
        agent_ids: None,
        output_types: Some(vec![OutputType::Error]),
        content_pattern: None,
        task_id: None,
    };
    
    let filtered = monitoring.get_all_recent(10, Some(&error_filter));
    assert_eq!(filtered.len(), 1);
    assert_eq!(filtered[0].content, "Error message");
    
    // Test filtering by content pattern
    let warning_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: Some("Warning".to_string()),
        task_id: None,
    };
    
    let warning_filtered = monitoring.get_all_recent(10, Some(&warning_filter));
    assert_eq!(warning_filtered.len(), 1);
    assert_eq!(warning_filtered[0].content, "Warning message");
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_concurrent_access (lines 291-334) ---[0m
async fn test_concurrent_access() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let agent_id = "concurrent-test-agent".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Spawn multiple tasks adding output concurrently
    let mut handles = Vec::new();
    
    for i in 0..10 {
        let monitoring_clone = Arc::clone(&monitoring);
        let agent_id_clone = agent_id.clone();
        
        let handle = tokio::spawn(async move {
            for j in 0..20 {
                monitoring_clone.add_output(
                    agent_id_clone.clone(),
                    "Concurrent".to_string(),
                    OutputType::Info,
                    format!("Concurrent message {} from task {}", j, i),
                    None,
                    format!("session-{}", i),
                ).unwrap();
                
                // Small delay to increase chance of concurrent access
                sleep(Duration::from_millis(1)).await;
            }
        });
        
        handles.push(handle);
    }
    
    // Wait for all tasks to complete
    for handle in handles {
        handle.await.unwrap();
    }
    
    // Verify all messages were added
    let all_recent = monitoring.get_all_recent(300, None);
    assert_eq!(all_recent.len(), 200); // 10 tasks * 20 messages each
    
    let stats = monitoring.get_stats();
    assert_eq!(stats.total_entries, 200);
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:160-201 function test_streaming_manager <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:337-390 function test_output_entry_filter_matching
  Similarity: 80.19%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_streaming_manager (lines 160-201) ---[0m
async fn test_streaming_manager() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let config = StreamConfig::default();
    let streaming = Arc::new(StreamingManager::new(Arc::clone(&monitoring), config));
    
    // Start streaming manager
    streaming.start().await.unwrap();
    
    // Subscribe to streaming
    let subscription_id = "test-subscription".to_string();
    let mut receiver = streaming.subscribe(subscription_id.clone()).unwrap();
    
    // Register agent and add output
    let agent_id = "streaming-test-agent".to_string();
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add output in background
    let monitoring_clone = Arc::clone(&monitoring);
    let agent_id_clone = agent_id.clone();
    tokio::spawn(async move {
        sleep(Duration::from_millis(100)).await;
        
        monitoring_clone.add_output(
            agent_id_clone,
            "Testing".to_string(),
            OutputType::Info,
            "Streaming test message".to_string(),
            None,
            "streaming-session".to_string(),
        ).unwrap();
    });
    
    // Receive the streamed message
    let received = tokio::time::timeout(Duration::from_secs(2), receiver.recv()).await;
    assert!(received.is_ok());
    
    let entry = received.unwrap().unwrap();
    assert_eq!(entry.content, "Streaming test message");
    
    // Test unsubscribe
    streaming.unsubscribe(&subscription_id).unwrap();
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_entry_filter_matching (lines 337-390) ---[0m
fn test_output_entry_filter_matching() {
    let entry = OutputEntry::new(
        "test-agent-filter".to_string(),
        "Frontend".to_string(),
        OutputType::Warning,
        "This is a test warning message".to_string(),
        Some("task-123".to_string()),
        "session-abc".to_string(),
    );
    
    // Test agent filter
    let agent_filter = OutputFilter {
        agent_ids: Some(vec!["test-agent-filter".to_string()]),
        output_types: None,
        content_pattern: None,
        task_id: None,
    };
    assert!(entry.matches_filter(&agent_filter));
    
    let wrong_agent_filter = OutputFilter {
        agent_ids: Some(vec!["other-agent".to_string()]),
        output_types: None,
        content_pattern: None,
        task_id: None,
    };
    assert!(!entry.matches_filter(&wrong_agent_filter));
    
    // Test output type filter
    let type_filter = OutputFilter {
        agent_ids: None,
        output_types: Some(vec![OutputType::Warning]),
        content_pattern: None,
        task_id: None,
    };
    assert!(entry.matches_filter(&type_filter));
    
    // Test content pattern filter
    let content_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: Some("warning".to_string()),
        task_id: None,
    };
    assert!(entry.matches_filter(&content_filter));
    
    // Test task ID filter
    let task_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: None,
        task_id: Some("task-123".to_string()),
    };
    assert!(entry.matches_filter(&task_filter));
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:125-157 function test_output_subscription <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:337-390 function test_output_entry_filter_matching
  Similarity: 87.33%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_subscription (lines 125-157) ---[0m
async fn test_output_subscription() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let agent_id = "subscription-test-agent".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Subscribe to global output
    let mut receiver = monitoring.subscribe_global();
    
    // Add output in a background task
    let monitoring_clone = Arc::clone(&monitoring);
    let agent_id_clone = agent_id.clone();
    tokio::spawn(async move {
        sleep(Duration::from_millis(100)).await;
        
        monitoring_clone.add_output(
            agent_id_clone,
            "QA".to_string(),
            OutputType::System,
            "Subscription test message".to_string(),
            Some("task-2".to_string()),
            "session-2".to_string(),
        ).unwrap();
    });
    
    // Wait for the message
    let received = tokio::time::timeout(Duration::from_secs(1), receiver.recv()).await;
    assert!(received.is_ok());
    
    let entry = received.unwrap().unwrap();
    assert_eq!(entry.content, "Subscription test message");
    assert_eq!(entry.output_type, OutputType::System);
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_entry_filter_matching (lines 337-390) ---[0m
fn test_output_entry_filter_matching() {
    let entry = OutputEntry::new(
        "test-agent-filter".to_string(),
        "Frontend".to_string(),
        OutputType::Warning,
        "This is a test warning message".to_string(),
        Some("task-123".to_string()),
        "session-abc".to_string(),
    );
    
    // Test agent filter
    let agent_filter = OutputFilter {
        agent_ids: Some(vec!["test-agent-filter".to_string()]),
        output_types: None,
        content_pattern: None,
        task_id: None,
    };
    assert!(entry.matches_filter(&agent_filter));
    
    let wrong_agent_filter = OutputFilter {
        agent_ids: Some(vec!["other-agent".to_string()]),
        output_types: None,
        content_pattern: None,
        task_id: None,
    };
    assert!(!entry.matches_filter(&wrong_agent_filter));
    
    // Test output type filter
    let type_filter = OutputFilter {
        agent_ids: None,
        output_types: Some(vec![OutputType::Warning]),
        content_pattern: None,
        task_id: None,
    };
    assert!(entry.matches_filter(&type_filter));
    
    // Test content pattern filter
    let content_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: Some("warning".to_string()),
        task_id: None,
    };
    assert!(entry.matches_filter(&content_filter));
    
    // Test task ID filter
    let task_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: None,
        task_id: Some("task-123".to_string()),
    };
    assert!(entry.matches_filter(&task_filter));
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:228-260 function test_monitoring_stats <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:337-390 function test_output_entry_filter_matching
  Similarity: 84.73%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_monitoring_stats (lines 228-260) ---[0m
async fn test_monitoring_stats() {
    let monitoring = MonitoringSystem::new();
    
    // Register multiple agents
    let agents = vec![
        "stats-agent-1", "stats-agent-2", "stats-agent-3"
    ];
    
    for agent in &agents {
        monitoring.register_agent(agent.to_string()).unwrap();
    }
    
    // Add various outputs
    for (i, agent) in agents.iter().enumerate() {
        for j in 0..=i {
            monitoring.add_output(
                agent.to_string(),
                "Stats".to_string(),
                if j % 2 == 0 { OutputType::Info } else { OutputType::Error },
                format!("Stats message {} from {}", j, agent),
                None,
                "stats-session".to_string(),
            ).unwrap();
        }
    }
    
    let stats = monitoring.get_stats();
    assert_eq!(stats.active_streams, 3);
    assert_eq!(stats.total_entries, 6); // 1 + 2 + 3 = 6 total messages
    assert!(stats.entries_per_agent.contains_key("stats-agent-1"));
    assert!(stats.entries_per_agent.contains_key("stats-agent-2"));
    assert!(stats.entries_per_agent.contains_key("stats-agent-3"));
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_entry_filter_matching (lines 337-390) ---[0m
fn test_output_entry_filter_matching() {
    let entry = OutputEntry::new(
        "test-agent-filter".to_string(),
        "Frontend".to_string(),
        OutputType::Warning,
        "This is a test warning message".to_string(),
        Some("task-123".to_string()),
        "session-abc".to_string(),
    );
    
    // Test agent filter
    let agent_filter = OutputFilter {
        agent_ids: Some(vec!["test-agent-filter".to_string()]),
        output_types: None,
        content_pattern: None,
        task_id: None,
    };
    assert!(entry.matches_filter(&agent_filter));
    
    let wrong_agent_filter = OutputFilter {
        agent_ids: Some(vec!["other-agent".to_string()]),
        output_types: None,
        content_pattern: None,
        task_id: None,
    };
    assert!(!entry.matches_filter(&wrong_agent_filter));
    
    // Test output type filter
    let type_filter = OutputFilter {
        agent_ids: None,
        output_types: Some(vec![OutputType::Warning]),
        content_pattern: None,
        task_id: None,
    };
    assert!(entry.matches_filter(&type_filter));
    
    // Test content pattern filter
    let content_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: Some("warning".to_string()),
        task_id: None,
    };
    assert!(entry.matches_filter(&content_filter));
    
    // Test task ID filter
    let task_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: None,
        task_id: Some("task-123".to_string()),
    };
    assert!(entry.matches_filter(&task_filter));
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:42-90 function test_output_filtering <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:125-157 function test_output_subscription
  Similarity: 88.71%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_filtering (lines 42-90) ---[0m
async fn test_output_filtering() {
    let monitoring = MonitoringSystem::new();
    let agent_id = "test-agent-456".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add various types of output
    let outputs = vec![
        (OutputType::Info, "Info message"),
        (OutputType::Warning, "Warning message"),
        (OutputType::Error, "Error message"),
        (OutputType::Debug, "Debug message"),
    ];
    
    for (output_type, content) in outputs {
        monitoring.add_output(
            agent_id.clone(),
            "Backend".to_string(),
            output_type,
            content.to_string(),
            None,
            "session-1".to_string(),
        ).unwrap();
    }
    
    // Test filtering by output type
    let error_filter = OutputFilter {
        agent_ids: None,
        output_types: Some(vec![OutputType::Error]),
        content_pattern: None,
        task_id: None,
    };
    
    let filtered = monitoring.get_all_recent(10, Some(&error_filter));
    assert_eq!(filtered.len(), 1);
    assert_eq!(filtered[0].content, "Error message");
    
    // Test filtering by content pattern
    let warning_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: Some("Warning".to_string()),
        task_id: None,
    };
    
    let warning_filtered = monitoring.get_all_recent(10, Some(&warning_filter));
    assert_eq!(warning_filtered.len(), 1);
    assert_eq!(warning_filtered[0].content, "Warning message");
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_subscription (lines 125-157) ---[0m
async fn test_output_subscription() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let agent_id = "subscription-test-agent".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Subscribe to global output
    let mut receiver = monitoring.subscribe_global();
    
    // Add output in a background task
    let monitoring_clone = Arc::clone(&monitoring);
    let agent_id_clone = agent_id.clone();
    tokio::spawn(async move {
        sleep(Duration::from_millis(100)).await;
        
        monitoring_clone.add_output(
            agent_id_clone,
            "QA".to_string(),
            OutputType::System,
            "Subscription test message".to_string(),
            Some("task-2".to_string()),
            "session-2".to_string(),
        ).unwrap();
    });
    
    // Wait for the message
    let received = tokio::time::timeout(Duration::from_secs(1), receiver.recv()).await;
    assert!(received.is_ok());
    
    let entry = received.unwrap().unwrap();
    assert_eq!(entry.content, "Subscription test message");
    assert_eq!(entry.output_type, OutputType::System);
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:93-122 function test_agent_output_stream <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:337-390 function test_output_entry_filter_matching
  Similarity: 86.39%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_agent_output_stream (lines 93-122) ---[0m
async fn test_agent_output_stream() {
    let agent_id = "stream-test-agent".to_string();
    let stream = AgentOutputStream::new(agent_id.clone());
    
    // Add some entries
    for i in 0..5 {
        let entry = OutputEntry::new(
            agent_id.clone(),
            "DevOps".to_string(),
            OutputType::Info,
            format!("Message {}", i),
            None,
            "session-1".to_string(),
        );
        stream.add_output(entry).unwrap();
    }
    
    // Test recent entries
    let recent = stream.get_recent(3, None);
    assert_eq!(recent.len(), 3);
    assert_eq!(recent[2].content, "Message 4"); // Most recent should be last
    assert_eq!(recent[0].content, "Message 2"); // Oldest of the 3
    
    // Test total length
    assert_eq!(stream.len(), 5);
    
    // Test clearing
    stream.clear();
    assert_eq!(stream.len(), 0);
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_entry_filter_matching (lines 337-390) ---[0m
fn test_output_entry_filter_matching() {
    let entry = OutputEntry::new(
        "test-agent-filter".to_string(),
        "Frontend".to_string(),
        OutputType::Warning,
        "This is a test warning message".to_string(),
        Some("task-123".to_string()),
        "session-abc".to_string(),
    );
    
    // Test agent filter
    let agent_filter = OutputFilter {
        agent_ids: Some(vec!["test-agent-filter".to_string()]),
        output_types: None,
        content_pattern: None,
        task_id: None,
    };
    assert!(entry.matches_filter(&agent_filter));
    
    let wrong_agent_filter = OutputFilter {
        agent_ids: Some(vec!["other-agent".to_string()]),
        output_types: None,
        content_pattern: None,
        task_id: None,
    };
    assert!(!entry.matches_filter(&wrong_agent_filter));
    
    // Test output type filter
    let type_filter = OutputFilter {
        agent_ids: None,
        output_types: Some(vec![OutputType::Warning]),
        content_pattern: None,
        task_id: None,
    };
    assert!(entry.matches_filter(&type_filter));
    
    // Test content pattern filter
    let content_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: Some("warning".to_string()),
        task_id: None,
    };
    assert!(entry.matches_filter(&content_filter));
    
    // Test task ID filter
    let task_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: None,
        task_id: Some("task-123".to_string()),
    };
    assert!(entry.matches_filter(&task_filter));
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:12-39 function test_monitoring_system_basic_operations <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:42-90 function test_output_filtering
  Similarity: 93.08%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_monitoring_system_basic_operations (lines 12-39) ---[0m
async fn test_monitoring_system_basic_operations() {
    let monitoring = MonitoringSystem::new();
    
    // Register an agent
    let agent_id = "test-agent-123".to_string();
    let stream = monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add some output
    monitoring.add_output(
        agent_id.clone(),
        "Frontend".to_string(),
        OutputType::Info,
        "Test message".to_string(),
        Some("task-1".to_string()),
        "session-1".to_string(),
    ).unwrap();
    
    // Verify output was added
    let recent = monitoring.get_all_recent(10, None);
    assert_eq!(recent.len(), 1);
    assert_eq!(recent[0].content, "Test message");
    assert_eq!(recent[0].agent_id, agent_id);
    
    // Test agent-specific stream
    let agent_recent = stream.get_recent(10, None);
    assert_eq!(agent_recent.len(), 1);
    assert_eq!(agent_recent[0].content, "Test message");
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_filtering (lines 42-90) ---[0m
async fn test_output_filtering() {
    let monitoring = MonitoringSystem::new();
    let agent_id = "test-agent-456".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add various types of output
    let outputs = vec![
        (OutputType::Info, "Info message"),
        (OutputType::Warning, "Warning message"),
        (OutputType::Error, "Error message"),
        (OutputType::Debug, "Debug message"),
    ];
    
    for (output_type, content) in outputs {
        monitoring.add_output(
            agent_id.clone(),
            "Backend".to_string(),
            output_type,
            content.to_string(),
            None,
            "session-1".to_string(),
        ).unwrap();
    }
    
    // Test filtering by output type
    let error_filter = OutputFilter {
        agent_ids: None,
        output_types: Some(vec![OutputType::Error]),
        content_pattern: None,
        task_id: None,
    };
    
    let filtered = monitoring.get_all_recent(10, Some(&error_filter));
    assert_eq!(filtered.len(), 1);
    assert_eq!(filtered[0].content, "Error message");
    
    // Test filtering by content pattern
    let warning_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: Some("Warning".to_string()),
        task_id: None,
    };
    
    let warning_filtered = monitoring.get_all_recent(10, Some(&warning_filter));
    assert_eq!(warning_filtered.len(), 1);
    assert_eq!(warning_filtered[0].content, "Warning message");
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:42-90 function test_output_filtering <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:160-201 function test_streaming_manager
  Similarity: 78.67%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_filtering (lines 42-90) ---[0m
async fn test_output_filtering() {
    let monitoring = MonitoringSystem::new();
    let agent_id = "test-agent-456".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add various types of output
    let outputs = vec![
        (OutputType::Info, "Info message"),
        (OutputType::Warning, "Warning message"),
        (OutputType::Error, "Error message"),
        (OutputType::Debug, "Debug message"),
    ];
    
    for (output_type, content) in outputs {
        monitoring.add_output(
            agent_id.clone(),
            "Backend".to_string(),
            output_type,
            content.to_string(),
            None,
            "session-1".to_string(),
        ).unwrap();
    }
    
    // Test filtering by output type
    let error_filter = OutputFilter {
        agent_ids: None,
        output_types: Some(vec![OutputType::Error]),
        content_pattern: None,
        task_id: None,
    };
    
    let filtered = monitoring.get_all_recent(10, Some(&error_filter));
    assert_eq!(filtered.len(), 1);
    assert_eq!(filtered[0].content, "Error message");
    
    // Test filtering by content pattern
    let warning_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: Some("Warning".to_string()),
        task_id: None,
    };
    
    let warning_filtered = monitoring.get_all_recent(10, Some(&warning_filter));
    assert_eq!(warning_filtered.len(), 1);
    assert_eq!(warning_filtered[0].content, "Warning message");
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_streaming_manager (lines 160-201) ---[0m
async fn test_streaming_manager() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let config = StreamConfig::default();
    let streaming = Arc::new(StreamingManager::new(Arc::clone(&monitoring), config));
    
    // Start streaming manager
    streaming.start().await.unwrap();
    
    // Subscribe to streaming
    let subscription_id = "test-subscription".to_string();
    let mut receiver = streaming.subscribe(subscription_id.clone()).unwrap();
    
    // Register agent and add output
    let agent_id = "streaming-test-agent".to_string();
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add output in background
    let monitoring_clone = Arc::clone(&monitoring);
    let agent_id_clone = agent_id.clone();
    tokio::spawn(async move {
        sleep(Duration::from_millis(100)).await;
        
        monitoring_clone.add_output(
            agent_id_clone,
            "Testing".to_string(),
            OutputType::Info,
            "Streaming test message".to_string(),
            None,
            "streaming-session".to_string(),
        ).unwrap();
    });
    
    // Receive the streamed message
    let received = tokio::time::timeout(Duration::from_secs(2), receiver.recv()).await;
    assert!(received.is_ok());
    
    let entry = received.unwrap().unwrap();
    assert_eq!(entry.content, "Streaming test message");
    
    // Test unsubscribe
    streaming.unsubscribe(&subscription_id).unwrap();
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:12-39 function test_monitoring_system_basic_operations <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:337-390 function test_output_entry_filter_matching
  Similarity: 87.23%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_monitoring_system_basic_operations (lines 12-39) ---[0m
async fn test_monitoring_system_basic_operations() {
    let monitoring = MonitoringSystem::new();
    
    // Register an agent
    let agent_id = "test-agent-123".to_string();
    let stream = monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add some output
    monitoring.add_output(
        agent_id.clone(),
        "Frontend".to_string(),
        OutputType::Info,
        "Test message".to_string(),
        Some("task-1".to_string()),
        "session-1".to_string(),
    ).unwrap();
    
    // Verify output was added
    let recent = monitoring.get_all_recent(10, None);
    assert_eq!(recent.len(), 1);
    assert_eq!(recent[0].content, "Test message");
    assert_eq!(recent[0].agent_id, agent_id);
    
    // Test agent-specific stream
    let agent_recent = stream.get_recent(10, None);
    assert_eq!(agent_recent.len(), 1);
    assert_eq!(agent_recent[0].content, "Test message");
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_entry_filter_matching (lines 337-390) ---[0m
fn test_output_entry_filter_matching() {
    let entry = OutputEntry::new(
        "test-agent-filter".to_string(),
        "Frontend".to_string(),
        OutputType::Warning,
        "This is a test warning message".to_string(),
        Some("task-123".to_string()),
        "session-abc".to_string(),
    );
    
    // Test agent filter
    let agent_filter = OutputFilter {
        agent_ids: Some(vec!["test-agent-filter".to_string()]),
        output_types: None,
        content_pattern: None,
        task_id: None,
    };
    assert!(entry.matches_filter(&agent_filter));
    
    let wrong_agent_filter = OutputFilter {
        agent_ids: Some(vec!["other-agent".to_string()]),
        output_types: None,
        content_pattern: None,
        task_id: None,
    };
    assert!(!entry.matches_filter(&wrong_agent_filter));
    
    // Test output type filter
    let type_filter = OutputFilter {
        agent_ids: None,
        output_types: Some(vec![OutputType::Warning]),
        content_pattern: None,
        task_id: None,
    };
    assert!(entry.matches_filter(&type_filter));
    
    // Test content pattern filter
    let content_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: Some("warning".to_string()),
        task_id: None,
    };
    assert!(entry.matches_filter(&content_filter));
    
    // Test task ID filter
    let task_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: None,
        task_id: Some("task-123".to_string()),
    };
    assert!(entry.matches_filter(&task_filter));
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:42-90 function test_output_filtering <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:228-260 function test_monitoring_stats
  Similarity: 84.12%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_filtering (lines 42-90) ---[0m
async fn test_output_filtering() {
    let monitoring = MonitoringSystem::new();
    let agent_id = "test-agent-456".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add various types of output
    let outputs = vec![
        (OutputType::Info, "Info message"),
        (OutputType::Warning, "Warning message"),
        (OutputType::Error, "Error message"),
        (OutputType::Debug, "Debug message"),
    ];
    
    for (output_type, content) in outputs {
        monitoring.add_output(
            agent_id.clone(),
            "Backend".to_string(),
            output_type,
            content.to_string(),
            None,
            "session-1".to_string(),
        ).unwrap();
    }
    
    // Test filtering by output type
    let error_filter = OutputFilter {
        agent_ids: None,
        output_types: Some(vec![OutputType::Error]),
        content_pattern: None,
        task_id: None,
    };
    
    let filtered = monitoring.get_all_recent(10, Some(&error_filter));
    assert_eq!(filtered.len(), 1);
    assert_eq!(filtered[0].content, "Error message");
    
    // Test filtering by content pattern
    let warning_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: Some("Warning".to_string()),
        task_id: None,
    };
    
    let warning_filtered = monitoring.get_all_recent(10, Some(&warning_filter));
    assert_eq!(warning_filtered.len(), 1);
    assert_eq!(warning_filtered[0].content, "Warning message");
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_monitoring_stats (lines 228-260) ---[0m
async fn test_monitoring_stats() {
    let monitoring = MonitoringSystem::new();
    
    // Register multiple agents
    let agents = vec![
        "stats-agent-1", "stats-agent-2", "stats-agent-3"
    ];
    
    for agent in &agents {
        monitoring.register_agent(agent.to_string()).unwrap();
    }
    
    // Add various outputs
    for (i, agent) in agents.iter().enumerate() {
        for j in 0..=i {
            monitoring.add_output(
                agent.to_string(),
                "Stats".to_string(),
                if j % 2 == 0 { OutputType::Info } else { OutputType::Error },
                format!("Stats message {} from {}", j, agent),
                None,
                "stats-session".to_string(),
            ).unwrap();
        }
    }
    
    let stats = monitoring.get_stats();
    assert_eq!(stats.active_streams, 3);
    assert_eq!(stats.total_entries, 6); // 1 + 2 + 3 = 6 total messages
    assert!(stats.entries_per_agent.contains_key("stats-agent-1"));
    assert!(stats.entries_per_agent.contains_key("stats-agent-2"));
    assert!(stats.entries_per_agent.contains_key("stats-agent-3"));
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:125-157 function test_output_subscription <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:291-334 function test_concurrent_access
  Similarity: 88.91%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_subscription (lines 125-157) ---[0m
async fn test_output_subscription() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let agent_id = "subscription-test-agent".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Subscribe to global output
    let mut receiver = monitoring.subscribe_global();
    
    // Add output in a background task
    let monitoring_clone = Arc::clone(&monitoring);
    let agent_id_clone = agent_id.clone();
    tokio::spawn(async move {
        sleep(Duration::from_millis(100)).await;
        
        monitoring_clone.add_output(
            agent_id_clone,
            "QA".to_string(),
            OutputType::System,
            "Subscription test message".to_string(),
            Some("task-2".to_string()),
            "session-2".to_string(),
        ).unwrap();
    });
    
    // Wait for the message
    let received = tokio::time::timeout(Duration::from_secs(1), receiver.recv()).await;
    assert!(received.is_ok());
    
    let entry = received.unwrap().unwrap();
    assert_eq!(entry.content, "Subscription test message");
    assert_eq!(entry.output_type, OutputType::System);
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_concurrent_access (lines 291-334) ---[0m
async fn test_concurrent_access() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let agent_id = "concurrent-test-agent".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Spawn multiple tasks adding output concurrently
    let mut handles = Vec::new();
    
    for i in 0..10 {
        let monitoring_clone = Arc::clone(&monitoring);
        let agent_id_clone = agent_id.clone();
        
        let handle = tokio::spawn(async move {
            for j in 0..20 {
                monitoring_clone.add_output(
                    agent_id_clone.clone(),
                    "Concurrent".to_string(),
                    OutputType::Info,
                    format!("Concurrent message {} from task {}", j, i),
                    None,
                    format!("session-{}", i),
                ).unwrap();
                
                // Small delay to increase chance of concurrent access
                sleep(Duration::from_millis(1)).await;
            }
        });
        
        handles.push(handle);
    }
    
    // Wait for all tasks to complete
    for handle in handles {
        handle.await.unwrap();
    }
    
    // Verify all messages were added
    let all_recent = monitoring.get_all_recent(300, None);
    assert_eq!(all_recent.len(), 200); // 10 tasks * 20 messages each
    
    let stats = monitoring.get_stats();
    assert_eq!(stats.total_entries, 200);
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:42-90 function test_output_filtering <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:93-122 function test_agent_output_stream
  Similarity: 85.73%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_filtering (lines 42-90) ---[0m
async fn test_output_filtering() {
    let monitoring = MonitoringSystem::new();
    let agent_id = "test-agent-456".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add various types of output
    let outputs = vec![
        (OutputType::Info, "Info message"),
        (OutputType::Warning, "Warning message"),
        (OutputType::Error, "Error message"),
        (OutputType::Debug, "Debug message"),
    ];
    
    for (output_type, content) in outputs {
        monitoring.add_output(
            agent_id.clone(),
            "Backend".to_string(),
            output_type,
            content.to_string(),
            None,
            "session-1".to_string(),
        ).unwrap();
    }
    
    // Test filtering by output type
    let error_filter = OutputFilter {
        agent_ids: None,
        output_types: Some(vec![OutputType::Error]),
        content_pattern: None,
        task_id: None,
    };
    
    let filtered = monitoring.get_all_recent(10, Some(&error_filter));
    assert_eq!(filtered.len(), 1);
    assert_eq!(filtered[0].content, "Error message");
    
    // Test filtering by content pattern
    let warning_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: Some("Warning".to_string()),
        task_id: None,
    };
    
    let warning_filtered = monitoring.get_all_recent(10, Some(&warning_filter));
    assert_eq!(warning_filtered.len(), 1);
    assert_eq!(warning_filtered[0].content, "Warning message");
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_agent_output_stream (lines 93-122) ---[0m
async fn test_agent_output_stream() {
    let agent_id = "stream-test-agent".to_string();
    let stream = AgentOutputStream::new(agent_id.clone());
    
    // Add some entries
    for i in 0..5 {
        let entry = OutputEntry::new(
            agent_id.clone(),
            "DevOps".to_string(),
            OutputType::Info,
            format!("Message {}", i),
            None,
            "session-1".to_string(),
        );
        stream.add_output(entry).unwrap();
    }
    
    // Test recent entries
    let recent = stream.get_recent(3, None);
    assert_eq!(recent.len(), 3);
    assert_eq!(recent[2].content, "Message 4"); // Most recent should be last
    assert_eq!(recent[0].content, "Message 2"); // Oldest of the 3
    
    // Test total length
    assert_eq!(stream.len(), 5);
    
    // Test clearing
    stream.clear();
    assert_eq!(stream.len(), 0);
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:263-288 function test_buffer_size_limits <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:337-390 function test_output_entry_filter_matching
  Similarity: 83.59%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_buffer_size_limits (lines 263-288) ---[0m
async fn test_buffer_size_limits() {
    let agent_id = "buffer-test-agent".to_string();
    let stream = AgentOutputStream::new(agent_id.clone());
    
    // Add more entries than the typical buffer size
    for i in 0..15000 {
        let entry = OutputEntry::new(
            agent_id.clone(),
            "Buffer".to_string(),
            OutputType::Debug,
            format!("Buffer test message {}", i),
            None,
            "buffer-session".to_string(),
        );
        stream.add_output(entry).unwrap();
    }
    
    // Should be limited by MAX_BUFFER_SIZE (10000)
    assert!(stream.len() <= 10000);
    
    // Recent entries should be the newest ones
    let recent = stream.get_recent(5, None);
    assert_eq!(recent.len(), 5);
    // Should contain the latest messages
    assert!(recent[4].content.contains("14999") || recent[4].content.contains("9999"));
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_entry_filter_matching (lines 337-390) ---[0m
fn test_output_entry_filter_matching() {
    let entry = OutputEntry::new(
        "test-agent-filter".to_string(),
        "Frontend".to_string(),
        OutputType::Warning,
        "This is a test warning message".to_string(),
        Some("task-123".to_string()),
        "session-abc".to_string(),
    );
    
    // Test agent filter
    let agent_filter = OutputFilter {
        agent_ids: Some(vec!["test-agent-filter".to_string()]),
        output_types: None,
        content_pattern: None,
        task_id: None,
    };
    assert!(entry.matches_filter(&agent_filter));
    
    let wrong_agent_filter = OutputFilter {
        agent_ids: Some(vec!["other-agent".to_string()]),
        output_types: None,
        content_pattern: None,
        task_id: None,
    };
    assert!(!entry.matches_filter(&wrong_agent_filter));
    
    // Test output type filter
    let type_filter = OutputFilter {
        agent_ids: None,
        output_types: Some(vec![OutputType::Warning]),
        content_pattern: None,
        task_id: None,
    };
    assert!(entry.matches_filter(&type_filter));
    
    // Test content pattern filter
    let content_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: Some("warning".to_string()),
        task_id: None,
    };
    assert!(entry.matches_filter(&content_filter));
    
    // Test task ID filter
    let task_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: None,
        task_id: Some("task-123".to_string()),
    };
    assert!(entry.matches_filter(&task_filter));
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:160-201 function test_streaming_manager <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:291-334 function test_concurrent_access
  Similarity: 76.52%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_streaming_manager (lines 160-201) ---[0m
async fn test_streaming_manager() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let config = StreamConfig::default();
    let streaming = Arc::new(StreamingManager::new(Arc::clone(&monitoring), config));
    
    // Start streaming manager
    streaming.start().await.unwrap();
    
    // Subscribe to streaming
    let subscription_id = "test-subscription".to_string();
    let mut receiver = streaming.subscribe(subscription_id.clone()).unwrap();
    
    // Register agent and add output
    let agent_id = "streaming-test-agent".to_string();
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add output in background
    let monitoring_clone = Arc::clone(&monitoring);
    let agent_id_clone = agent_id.clone();
    tokio::spawn(async move {
        sleep(Duration::from_millis(100)).await;
        
        monitoring_clone.add_output(
            agent_id_clone,
            "Testing".to_string(),
            OutputType::Info,
            "Streaming test message".to_string(),
            None,
            "streaming-session".to_string(),
        ).unwrap();
    });
    
    // Receive the streamed message
    let received = tokio::time::timeout(Duration::from_secs(2), receiver.recv()).await;
    assert!(received.is_ok());
    
    let entry = received.unwrap().unwrap();
    assert_eq!(entry.content, "Streaming test message");
    
    // Test unsubscribe
    streaming.unsubscribe(&subscription_id).unwrap();
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_concurrent_access (lines 291-334) ---[0m
async fn test_concurrent_access() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let agent_id = "concurrent-test-agent".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Spawn multiple tasks adding output concurrently
    let mut handles = Vec::new();
    
    for i in 0..10 {
        let monitoring_clone = Arc::clone(&monitoring);
        let agent_id_clone = agent_id.clone();
        
        let handle = tokio::spawn(async move {
            for j in 0..20 {
                monitoring_clone.add_output(
                    agent_id_clone.clone(),
                    "Concurrent".to_string(),
                    OutputType::Info,
                    format!("Concurrent message {} from task {}", j, i),
                    None,
                    format!("session-{}", i),
                ).unwrap();
                
                // Small delay to increase chance of concurrent access
                sleep(Duration::from_millis(1)).await;
            }
        });
        
        handles.push(handle);
    }
    
    // Wait for all tasks to complete
    for handle in handles {
        handle.await.unwrap();
    }
    
    // Verify all messages were added
    let all_recent = monitoring.get_all_recent(300, None);
    assert_eq!(all_recent.len(), 200); // 10 tasks * 20 messages each
    
    let stats = monitoring.get_stats();
    assert_eq!(stats.total_entries, 200);
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:228-260 function test_monitoring_stats <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:291-334 function test_concurrent_access
  Similarity: 83.50%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_monitoring_stats (lines 228-260) ---[0m
async fn test_monitoring_stats() {
    let monitoring = MonitoringSystem::new();
    
    // Register multiple agents
    let agents = vec![
        "stats-agent-1", "stats-agent-2", "stats-agent-3"
    ];
    
    for agent in &agents {
        monitoring.register_agent(agent.to_string()).unwrap();
    }
    
    // Add various outputs
    for (i, agent) in agents.iter().enumerate() {
        for j in 0..=i {
            monitoring.add_output(
                agent.to_string(),
                "Stats".to_string(),
                if j % 2 == 0 { OutputType::Info } else { OutputType::Error },
                format!("Stats message {} from {}", j, agent),
                None,
                "stats-session".to_string(),
            ).unwrap();
        }
    }
    
    let stats = monitoring.get_stats();
    assert_eq!(stats.active_streams, 3);
    assert_eq!(stats.total_entries, 6); // 1 + 2 + 3 = 6 total messages
    assert!(stats.entries_per_agent.contains_key("stats-agent-1"));
    assert!(stats.entries_per_agent.contains_key("stats-agent-2"));
    assert!(stats.entries_per_agent.contains_key("stats-agent-3"));
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_concurrent_access (lines 291-334) ---[0m
async fn test_concurrent_access() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let agent_id = "concurrent-test-agent".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Spawn multiple tasks adding output concurrently
    let mut handles = Vec::new();
    
    for i in 0..10 {
        let monitoring_clone = Arc::clone(&monitoring);
        let agent_id_clone = agent_id.clone();
        
        let handle = tokio::spawn(async move {
            for j in 0..20 {
                monitoring_clone.add_output(
                    agent_id_clone.clone(),
                    "Concurrent".to_string(),
                    OutputType::Info,
                    format!("Concurrent message {} from task {}", j, i),
                    None,
                    format!("session-{}", i),
                ).unwrap();
                
                // Small delay to increase chance of concurrent access
                sleep(Duration::from_millis(1)).await;
            }
        });
        
        handles.push(handle);
    }
    
    // Wait for all tasks to complete
    for handle in handles {
        handle.await.unwrap();
    }
    
    // Verify all messages were added
    let all_recent = monitoring.get_all_recent(300, None);
    assert_eq!(all_recent.len(), 200); // 10 tasks * 20 messages each
    
    let stats = monitoring.get_stats();
    assert_eq!(stats.total_entries, 200);
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:125-157 function test_output_subscription <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:160-201 function test_streaming_manager
  Similarity: 85.45%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_subscription (lines 125-157) ---[0m
async fn test_output_subscription() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let agent_id = "subscription-test-agent".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Subscribe to global output
    let mut receiver = monitoring.subscribe_global();
    
    // Add output in a background task
    let monitoring_clone = Arc::clone(&monitoring);
    let agent_id_clone = agent_id.clone();
    tokio::spawn(async move {
        sleep(Duration::from_millis(100)).await;
        
        monitoring_clone.add_output(
            agent_id_clone,
            "QA".to_string(),
            OutputType::System,
            "Subscription test message".to_string(),
            Some("task-2".to_string()),
            "session-2".to_string(),
        ).unwrap();
    });
    
    // Wait for the message
    let received = tokio::time::timeout(Duration::from_secs(1), receiver.recv()).await;
    assert!(received.is_ok());
    
    let entry = received.unwrap().unwrap();
    assert_eq!(entry.content, "Subscription test message");
    assert_eq!(entry.output_type, OutputType::System);
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_streaming_manager (lines 160-201) ---[0m
async fn test_streaming_manager() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let config = StreamConfig::default();
    let streaming = Arc::new(StreamingManager::new(Arc::clone(&monitoring), config));
    
    // Start streaming manager
    streaming.start().await.unwrap();
    
    // Subscribe to streaming
    let subscription_id = "test-subscription".to_string();
    let mut receiver = streaming.subscribe(subscription_id.clone()).unwrap();
    
    // Register agent and add output
    let agent_id = "streaming-test-agent".to_string();
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add output in background
    let monitoring_clone = Arc::clone(&monitoring);
    let agent_id_clone = agent_id.clone();
    tokio::spawn(async move {
        sleep(Duration::from_millis(100)).await;
        
        monitoring_clone.add_output(
            agent_id_clone,
            "Testing".to_string(),
            OutputType::Info,
            "Streaming test message".to_string(),
            None,
            "streaming-session".to_string(),
        ).unwrap();
    });
    
    // Receive the streamed message
    let received = tokio::time::timeout(Duration::from_secs(2), receiver.recv()).await;
    assert!(received.is_ok());
    
    let entry = received.unwrap().unwrap();
    assert_eq!(entry.content, "Streaming test message");
    
    // Test unsubscribe
    streaming.unsubscribe(&subscription_id).unwrap();
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:93-122 function test_agent_output_stream <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:291-334 function test_concurrent_access
  Similarity: 86.53%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_agent_output_stream (lines 93-122) ---[0m
async fn test_agent_output_stream() {
    let agent_id = "stream-test-agent".to_string();
    let stream = AgentOutputStream::new(agent_id.clone());
    
    // Add some entries
    for i in 0..5 {
        let entry = OutputEntry::new(
            agent_id.clone(),
            "DevOps".to_string(),
            OutputType::Info,
            format!("Message {}", i),
            None,
            "session-1".to_string(),
        );
        stream.add_output(entry).unwrap();
    }
    
    // Test recent entries
    let recent = stream.get_recent(3, None);
    assert_eq!(recent.len(), 3);
    assert_eq!(recent[2].content, "Message 4"); // Most recent should be last
    assert_eq!(recent[0].content, "Message 2"); // Oldest of the 3
    
    // Test total length
    assert_eq!(stream.len(), 5);
    
    // Test clearing
    stream.clear();
    assert_eq!(stream.len(), 0);
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_concurrent_access (lines 291-334) ---[0m
async fn test_concurrent_access() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let agent_id = "concurrent-test-agent".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Spawn multiple tasks adding output concurrently
    let mut handles = Vec::new();
    
    for i in 0..10 {
        let monitoring_clone = Arc::clone(&monitoring);
        let agent_id_clone = agent_id.clone();
        
        let handle = tokio::spawn(async move {
            for j in 0..20 {
                monitoring_clone.add_output(
                    agent_id_clone.clone(),
                    "Concurrent".to_string(),
                    OutputType::Info,
                    format!("Concurrent message {} from task {}", j, i),
                    None,
                    format!("session-{}", i),
                ).unwrap();
                
                // Small delay to increase chance of concurrent access
                sleep(Duration::from_millis(1)).await;
            }
        });
        
        handles.push(handle);
    }
    
    // Wait for all tasks to complete
    for handle in handles {
        handle.await.unwrap();
    }
    
    // Verify all messages were added
    let all_recent = monitoring.get_all_recent(300, None);
    assert_eq!(all_recent.len(), 200); // 10 tasks * 20 messages each
    
    let stats = monitoring.get_stats();
    assert_eq!(stats.total_entries, 200);
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:12-39 function test_monitoring_system_basic_operations <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:291-334 function test_concurrent_access
  Similarity: 85.68%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_monitoring_system_basic_operations (lines 12-39) ---[0m
async fn test_monitoring_system_basic_operations() {
    let monitoring = MonitoringSystem::new();
    
    // Register an agent
    let agent_id = "test-agent-123".to_string();
    let stream = monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add some output
    monitoring.add_output(
        agent_id.clone(),
        "Frontend".to_string(),
        OutputType::Info,
        "Test message".to_string(),
        Some("task-1".to_string()),
        "session-1".to_string(),
    ).unwrap();
    
    // Verify output was added
    let recent = monitoring.get_all_recent(10, None);
    assert_eq!(recent.len(), 1);
    assert_eq!(recent[0].content, "Test message");
    assert_eq!(recent[0].agent_id, agent_id);
    
    // Test agent-specific stream
    let agent_recent = stream.get_recent(10, None);
    assert_eq!(agent_recent.len(), 1);
    assert_eq!(agent_recent[0].content, "Test message");
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_concurrent_access (lines 291-334) ---[0m
async fn test_concurrent_access() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let agent_id = "concurrent-test-agent".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Spawn multiple tasks adding output concurrently
    let mut handles = Vec::new();
    
    for i in 0..10 {
        let monitoring_clone = Arc::clone(&monitoring);
        let agent_id_clone = agent_id.clone();
        
        let handle = tokio::spawn(async move {
            for j in 0..20 {
                monitoring_clone.add_output(
                    agent_id_clone.clone(),
                    "Concurrent".to_string(),
                    OutputType::Info,
                    format!("Concurrent message {} from task {}", j, i),
                    None,
                    format!("session-{}", i),
                ).unwrap();
                
                // Small delay to increase chance of concurrent access
                sleep(Duration::from_millis(1)).await;
            }
        });
        
        handles.push(handle);
    }
    
    // Wait for all tasks to complete
    for handle in handles {
        handle.await.unwrap();
    }
    
    // Verify all messages were added
    let all_recent = monitoring.get_all_recent(300, None);
    assert_eq!(all_recent.len(), 200); // 10 tasks * 20 messages each
    
    let stats = monitoring.get_stats();
    assert_eq!(stats.total_entries, 200);
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:263-288 function test_buffer_size_limits <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:291-334 function test_concurrent_access
  Similarity: 86.71%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_buffer_size_limits (lines 263-288) ---[0m
async fn test_buffer_size_limits() {
    let agent_id = "buffer-test-agent".to_string();
    let stream = AgentOutputStream::new(agent_id.clone());
    
    // Add more entries than the typical buffer size
    for i in 0..15000 {
        let entry = OutputEntry::new(
            agent_id.clone(),
            "Buffer".to_string(),
            OutputType::Debug,
            format!("Buffer test message {}", i),
            None,
            "buffer-session".to_string(),
        );
        stream.add_output(entry).unwrap();
    }
    
    // Should be limited by MAX_BUFFER_SIZE (10000)
    assert!(stream.len() <= 10000);
    
    // Recent entries should be the newest ones
    let recent = stream.get_recent(5, None);
    assert_eq!(recent.len(), 5);
    // Should contain the latest messages
    assert!(recent[4].content.contains("14999") || recent[4].content.contains("9999"));
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_concurrent_access (lines 291-334) ---[0m
async fn test_concurrent_access() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let agent_id = "concurrent-test-agent".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Spawn multiple tasks adding output concurrently
    let mut handles = Vec::new();
    
    for i in 0..10 {
        let monitoring_clone = Arc::clone(&monitoring);
        let agent_id_clone = agent_id.clone();
        
        let handle = tokio::spawn(async move {
            for j in 0..20 {
                monitoring_clone.add_output(
                    agent_id_clone.clone(),
                    "Concurrent".to_string(),
                    OutputType::Info,
                    format!("Concurrent message {} from task {}", j, i),
                    None,
                    format!("session-{}", i),
                ).unwrap();
                
                // Small delay to increase chance of concurrent access
                sleep(Duration::from_millis(1)).await;
            }
        });
        
        handles.push(handle);
    }
    
    // Wait for all tasks to complete
    for handle in handles {
        handle.await.unwrap();
    }
    
    // Verify all messages were added
    let all_recent = monitoring.get_all_recent(300, None);
    assert_eq!(all_recent.len(), 200); // 10 tasks * 20 messages each
    
    let stats = monitoring.get_stats();
    assert_eq!(stats.total_entries, 200);
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:93-122 function test_agent_output_stream <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:160-201 function test_streaming_manager
  Similarity: 82.55%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_agent_output_stream (lines 93-122) ---[0m
async fn test_agent_output_stream() {
    let agent_id = "stream-test-agent".to_string();
    let stream = AgentOutputStream::new(agent_id.clone());
    
    // Add some entries
    for i in 0..5 {
        let entry = OutputEntry::new(
            agent_id.clone(),
            "DevOps".to_string(),
            OutputType::Info,
            format!("Message {}", i),
            None,
            "session-1".to_string(),
        );
        stream.add_output(entry).unwrap();
    }
    
    // Test recent entries
    let recent = stream.get_recent(3, None);
    assert_eq!(recent.len(), 3);
    assert_eq!(recent[2].content, "Message 4"); // Most recent should be last
    assert_eq!(recent[0].content, "Message 2"); // Oldest of the 3
    
    // Test total length
    assert_eq!(stream.len(), 5);
    
    // Test clearing
    stream.clear();
    assert_eq!(stream.len(), 0);
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_streaming_manager (lines 160-201) ---[0m
async fn test_streaming_manager() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let config = StreamConfig::default();
    let streaming = Arc::new(StreamingManager::new(Arc::clone(&monitoring), config));
    
    // Start streaming manager
    streaming.start().await.unwrap();
    
    // Subscribe to streaming
    let subscription_id = "test-subscription".to_string();
    let mut receiver = streaming.subscribe(subscription_id.clone()).unwrap();
    
    // Register agent and add output
    let agent_id = "streaming-test-agent".to_string();
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add output in background
    let monitoring_clone = Arc::clone(&monitoring);
    let agent_id_clone = agent_id.clone();
    tokio::spawn(async move {
        sleep(Duration::from_millis(100)).await;
        
        monitoring_clone.add_output(
            agent_id_clone,
            "Testing".to_string(),
            OutputType::Info,
            "Streaming test message".to_string(),
            None,
            "streaming-session".to_string(),
        ).unwrap();
    });
    
    // Receive the streamed message
    let received = tokio::time::timeout(Duration::from_secs(2), receiver.recv()).await;
    assert!(received.is_ok());
    
    let entry = received.unwrap().unwrap();
    assert_eq!(entry.content, "Streaming test message");
    
    // Test unsubscribe
    streaming.unsubscribe(&subscription_id).unwrap();
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:42-90 function test_output_filtering <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:263-288 function test_buffer_size_limits
  Similarity: 78.93%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_filtering (lines 42-90) ---[0m
async fn test_output_filtering() {
    let monitoring = MonitoringSystem::new();
    let agent_id = "test-agent-456".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add various types of output
    let outputs = vec![
        (OutputType::Info, "Info message"),
        (OutputType::Warning, "Warning message"),
        (OutputType::Error, "Error message"),
        (OutputType::Debug, "Debug message"),
    ];
    
    for (output_type, content) in outputs {
        monitoring.add_output(
            agent_id.clone(),
            "Backend".to_string(),
            output_type,
            content.to_string(),
            None,
            "session-1".to_string(),
        ).unwrap();
    }
    
    // Test filtering by output type
    let error_filter = OutputFilter {
        agent_ids: None,
        output_types: Some(vec![OutputType::Error]),
        content_pattern: None,
        task_id: None,
    };
    
    let filtered = monitoring.get_all_recent(10, Some(&error_filter));
    assert_eq!(filtered.len(), 1);
    assert_eq!(filtered[0].content, "Error message");
    
    // Test filtering by content pattern
    let warning_filter = OutputFilter {
        agent_ids: None,
        output_types: None,
        content_pattern: Some("Warning".to_string()),
        task_id: None,
    };
    
    let warning_filtered = monitoring.get_all_recent(10, Some(&warning_filter));
    assert_eq!(warning_filtered.len(), 1);
    assert_eq!(warning_filtered[0].content, "Warning message");
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_buffer_size_limits (lines 263-288) ---[0m
async fn test_buffer_size_limits() {
    let agent_id = "buffer-test-agent".to_string();
    let stream = AgentOutputStream::new(agent_id.clone());
    
    // Add more entries than the typical buffer size
    for i in 0..15000 {
        let entry = OutputEntry::new(
            agent_id.clone(),
            "Buffer".to_string(),
            OutputType::Debug,
            format!("Buffer test message {}", i),
            None,
            "buffer-session".to_string(),
        );
        stream.add_output(entry).unwrap();
    }
    
    // Should be limited by MAX_BUFFER_SIZE (10000)
    assert!(stream.len() <= 10000);
    
    // Recent entries should be the newest ones
    let recent = stream.get_recent(5, None);
    assert_eq!(recent.len(), 5);
    // Should contain the latest messages
    assert!(recent[4].content.contains("14999") || recent[4].content.contains("9999"));
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:12-39 function test_monitoring_system_basic_operations <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:160-201 function test_streaming_manager
  Similarity: 79.02%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_monitoring_system_basic_operations (lines 12-39) ---[0m
async fn test_monitoring_system_basic_operations() {
    let monitoring = MonitoringSystem::new();
    
    // Register an agent
    let agent_id = "test-agent-123".to_string();
    let stream = monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add some output
    monitoring.add_output(
        agent_id.clone(),
        "Frontend".to_string(),
        OutputType::Info,
        "Test message".to_string(),
        Some("task-1".to_string()),
        "session-1".to_string(),
    ).unwrap();
    
    // Verify output was added
    let recent = monitoring.get_all_recent(10, None);
    assert_eq!(recent.len(), 1);
    assert_eq!(recent[0].content, "Test message");
    assert_eq!(recent[0].agent_id, agent_id);
    
    // Test agent-specific stream
    let agent_recent = stream.get_recent(10, None);
    assert_eq!(agent_recent.len(), 1);
    assert_eq!(agent_recent[0].content, "Test message");
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_streaming_manager (lines 160-201) ---[0m
async fn test_streaming_manager() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let config = StreamConfig::default();
    let streaming = Arc::new(StreamingManager::new(Arc::clone(&monitoring), config));
    
    // Start streaming manager
    streaming.start().await.unwrap();
    
    // Subscribe to streaming
    let subscription_id = "test-subscription".to_string();
    let mut receiver = streaming.subscribe(subscription_id.clone()).unwrap();
    
    // Register agent and add output
    let agent_id = "streaming-test-agent".to_string();
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add output in background
    let monitoring_clone = Arc::clone(&monitoring);
    let agent_id_clone = agent_id.clone();
    tokio::spawn(async move {
        sleep(Duration::from_millis(100)).await;
        
        monitoring_clone.add_output(
            agent_id_clone,
            "Testing".to_string(),
            OutputType::Info,
            "Streaming test message".to_string(),
            None,
            "streaming-session".to_string(),
        ).unwrap();
    });
    
    // Receive the streamed message
    let received = tokio::time::timeout(Duration::from_secs(2), receiver.recv()).await;
    assert!(received.is_ok());
    
    let entry = received.unwrap().unwrap();
    assert_eq!(entry.content, "Streaming test message");
    
    // Test unsubscribe
    streaming.unsubscribe(&subscription_id).unwrap();
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:93-122 function test_agent_output_stream <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:228-260 function test_monitoring_stats
  Similarity: 86.07%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_agent_output_stream (lines 93-122) ---[0m
async fn test_agent_output_stream() {
    let agent_id = "stream-test-agent".to_string();
    let stream = AgentOutputStream::new(agent_id.clone());
    
    // Add some entries
    for i in 0..5 {
        let entry = OutputEntry::new(
            agent_id.clone(),
            "DevOps".to_string(),
            OutputType::Info,
            format!("Message {}", i),
            None,
            "session-1".to_string(),
        );
        stream.add_output(entry).unwrap();
    }
    
    // Test recent entries
    let recent = stream.get_recent(3, None);
    assert_eq!(recent.len(), 3);
    assert_eq!(recent[2].content, "Message 4"); // Most recent should be last
    assert_eq!(recent[0].content, "Message 2"); // Oldest of the 3
    
    // Test total length
    assert_eq!(stream.len(), 5);
    
    // Test clearing
    stream.clear();
    assert_eq!(stream.len(), 0);
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_monitoring_stats (lines 228-260) ---[0m
async fn test_monitoring_stats() {
    let monitoring = MonitoringSystem::new();
    
    // Register multiple agents
    let agents = vec![
        "stats-agent-1", "stats-agent-2", "stats-agent-3"
    ];
    
    for agent in &agents {
        monitoring.register_agent(agent.to_string()).unwrap();
    }
    
    // Add various outputs
    for (i, agent) in agents.iter().enumerate() {
        for j in 0..=i {
            monitoring.add_output(
                agent.to_string(),
                "Stats".to_string(),
                if j % 2 == 0 { OutputType::Info } else { OutputType::Error },
                format!("Stats message {} from {}", j, agent),
                None,
                "stats-session".to_string(),
            ).unwrap();
        }
    }
    
    let stats = monitoring.get_stats();
    assert_eq!(stats.active_streams, 3);
    assert_eq!(stats.total_entries, 6); // 1 + 2 + 3 = 6 total messages
    assert!(stats.entries_per_agent.contains_key("stats-agent-1"));
    assert!(stats.entries_per_agent.contains_key("stats-agent-2"));
    assert!(stats.entries_per_agent.contains_key("stats-agent-3"));
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:93-122 function test_agent_output_stream <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:125-157 function test_output_subscription
  Similarity: 85.37%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_agent_output_stream (lines 93-122) ---[0m
async fn test_agent_output_stream() {
    let agent_id = "stream-test-agent".to_string();
    let stream = AgentOutputStream::new(agent_id.clone());
    
    // Add some entries
    for i in 0..5 {
        let entry = OutputEntry::new(
            agent_id.clone(),
            "DevOps".to_string(),
            OutputType::Info,
            format!("Message {}", i),
            None,
            "session-1".to_string(),
        );
        stream.add_output(entry).unwrap();
    }
    
    // Test recent entries
    let recent = stream.get_recent(3, None);
    assert_eq!(recent.len(), 3);
    assert_eq!(recent[2].content, "Message 4"); // Most recent should be last
    assert_eq!(recent[0].content, "Message 2"); // Oldest of the 3
    
    // Test total length
    assert_eq!(stream.len(), 5);
    
    // Test clearing
    stream.clear();
    assert_eq!(stream.len(), 0);
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_subscription (lines 125-157) ---[0m
async fn test_output_subscription() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let agent_id = "subscription-test-agent".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Subscribe to global output
    let mut receiver = monitoring.subscribe_global();
    
    // Add output in a background task
    let monitoring_clone = Arc::clone(&monitoring);
    let agent_id_clone = agent_id.clone();
    tokio::spawn(async move {
        sleep(Duration::from_millis(100)).await;
        
        monitoring_clone.add_output(
            agent_id_clone,
            "QA".to_string(),
            OutputType::System,
            "Subscription test message".to_string(),
            Some("task-2".to_string()),
            "session-2".to_string(),
        ).unwrap();
    });
    
    // Wait for the message
    let received = tokio::time::timeout(Duration::from_secs(1), receiver.recv()).await;
    assert!(received.is_ok());
    
    let entry = received.unwrap().unwrap();
    assert_eq!(entry.content, "Subscription test message");
    assert_eq!(entry.output_type, OutputType::System);
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:125-157 function test_output_subscription <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:228-260 function test_monitoring_stats
  Similarity: 81.44%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_subscription (lines 125-157) ---[0m
async fn test_output_subscription() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let agent_id = "subscription-test-agent".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Subscribe to global output
    let mut receiver = monitoring.subscribe_global();
    
    // Add output in a background task
    let monitoring_clone = Arc::clone(&monitoring);
    let agent_id_clone = agent_id.clone();
    tokio::spawn(async move {
        sleep(Duration::from_millis(100)).await;
        
        monitoring_clone.add_output(
            agent_id_clone,
            "QA".to_string(),
            OutputType::System,
            "Subscription test message".to_string(),
            Some("task-2".to_string()),
            "session-2".to_string(),
        ).unwrap();
    });
    
    // Wait for the message
    let received = tokio::time::timeout(Duration::from_secs(1), receiver.recv()).await;
    assert!(received.is_ok());
    
    let entry = received.unwrap().unwrap();
    assert_eq!(entry.content, "Subscription test message");
    assert_eq!(entry.output_type, OutputType::System);
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_monitoring_stats (lines 228-260) ---[0m
async fn test_monitoring_stats() {
    let monitoring = MonitoringSystem::new();
    
    // Register multiple agents
    let agents = vec![
        "stats-agent-1", "stats-agent-2", "stats-agent-3"
    ];
    
    for agent in &agents {
        monitoring.register_agent(agent.to_string()).unwrap();
    }
    
    // Add various outputs
    for (i, agent) in agents.iter().enumerate() {
        for j in 0..=i {
            monitoring.add_output(
                agent.to_string(),
                "Stats".to_string(),
                if j % 2 == 0 { OutputType::Info } else { OutputType::Error },
                format!("Stats message {} from {}", j, agent),
                None,
                "stats-session".to_string(),
            ).unwrap();
        }
    }
    
    let stats = monitoring.get_stats();
    assert_eq!(stats.active_streams, 3);
    assert_eq!(stats.total_entries, 6); // 1 + 2 + 3 = 6 total messages
    assert!(stats.entries_per_agent.contains_key("stats-agent-1"));
    assert!(stats.entries_per_agent.contains_key("stats-agent-2"));
    assert!(stats.entries_per_agent.contains_key("stats-agent-3"));
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:160-201 function test_streaming_manager <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:228-260 function test_monitoring_stats
  Similarity: 71.31%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_streaming_manager (lines 160-201) ---[0m
async fn test_streaming_manager() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let config = StreamConfig::default();
    let streaming = Arc::new(StreamingManager::new(Arc::clone(&monitoring), config));
    
    // Start streaming manager
    streaming.start().await.unwrap();
    
    // Subscribe to streaming
    let subscription_id = "test-subscription".to_string();
    let mut receiver = streaming.subscribe(subscription_id.clone()).unwrap();
    
    // Register agent and add output
    let agent_id = "streaming-test-agent".to_string();
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add output in background
    let monitoring_clone = Arc::clone(&monitoring);
    let agent_id_clone = agent_id.clone();
    tokio::spawn(async move {
        sleep(Duration::from_millis(100)).await;
        
        monitoring_clone.add_output(
            agent_id_clone,
            "Testing".to_string(),
            OutputType::Info,
            "Streaming test message".to_string(),
            None,
            "streaming-session".to_string(),
        ).unwrap();
    });
    
    // Receive the streamed message
    let received = tokio::time::timeout(Duration::from_secs(2), receiver.recv()).await;
    assert!(received.is_ok());
    
    let entry = received.unwrap().unwrap();
    assert_eq!(entry.content, "Streaming test message");
    
    // Test unsubscribe
    streaming.unsubscribe(&subscription_id).unwrap();
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_monitoring_stats (lines 228-260) ---[0m
async fn test_monitoring_stats() {
    let monitoring = MonitoringSystem::new();
    
    // Register multiple agents
    let agents = vec![
        "stats-agent-1", "stats-agent-2", "stats-agent-3"
    ];
    
    for agent in &agents {
        monitoring.register_agent(agent.to_string()).unwrap();
    }
    
    // Add various outputs
    for (i, agent) in agents.iter().enumerate() {
        for j in 0..=i {
            monitoring.add_output(
                agent.to_string(),
                "Stats".to_string(),
                if j % 2 == 0 { OutputType::Info } else { OutputType::Error },
                format!("Stats message {} from {}", j, agent),
                None,
                "stats-session".to_string(),
            ).unwrap();
        }
    }
    
    let stats = monitoring.get_stats();
    assert_eq!(stats.active_streams, 3);
    assert_eq!(stats.total_entries, 6); // 1 + 2 + 3 = 6 total messages
    assert!(stats.entries_per_agent.contains_key("stats-agent-1"));
    assert!(stats.entries_per_agent.contains_key("stats-agent-2"));
    assert!(stats.entries_per_agent.contains_key("stats-agent-3"));
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:12-39 function test_monitoring_system_basic_operations <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:228-260 function test_monitoring_stats
  Similarity: 87.44%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_monitoring_system_basic_operations (lines 12-39) ---[0m
async fn test_monitoring_system_basic_operations() {
    let monitoring = MonitoringSystem::new();
    
    // Register an agent
    let agent_id = "test-agent-123".to_string();
    let stream = monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add some output
    monitoring.add_output(
        agent_id.clone(),
        "Frontend".to_string(),
        OutputType::Info,
        "Test message".to_string(),
        Some("task-1".to_string()),
        "session-1".to_string(),
    ).unwrap();
    
    // Verify output was added
    let recent = monitoring.get_all_recent(10, None);
    assert_eq!(recent.len(), 1);
    assert_eq!(recent[0].content, "Test message");
    assert_eq!(recent[0].agent_id, agent_id);
    
    // Test agent-specific stream
    let agent_recent = stream.get_recent(10, None);
    assert_eq!(agent_recent.len(), 1);
    assert_eq!(agent_recent[0].content, "Test message");
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_monitoring_stats (lines 228-260) ---[0m
async fn test_monitoring_stats() {
    let monitoring = MonitoringSystem::new();
    
    // Register multiple agents
    let agents = vec![
        "stats-agent-1", "stats-agent-2", "stats-agent-3"
    ];
    
    for agent in &agents {
        monitoring.register_agent(agent.to_string()).unwrap();
    }
    
    // Add various outputs
    for (i, agent) in agents.iter().enumerate() {
        for j in 0..=i {
            monitoring.add_output(
                agent.to_string(),
                "Stats".to_string(),
                if j % 2 == 0 { OutputType::Info } else { OutputType::Error },
                format!("Stats message {} from {}", j, agent),
                None,
                "stats-session".to_string(),
            ).unwrap();
        }
    }
    
    let stats = monitoring.get_stats();
    assert_eq!(stats.active_streams, 3);
    assert_eq!(stats.total_entries, 6); // 1 + 2 + 3 = 6 total messages
    assert!(stats.entries_per_agent.contains_key("stats-agent-1"));
    assert!(stats.entries_per_agent.contains_key("stats-agent-2"));
    assert!(stats.entries_per_agent.contains_key("stats-agent-3"));
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:12-39 function test_monitoring_system_basic_operations <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:125-157 function test_output_subscription
  Similarity: 84.51%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_monitoring_system_basic_operations (lines 12-39) ---[0m
async fn test_monitoring_system_basic_operations() {
    let monitoring = MonitoringSystem::new();
    
    // Register an agent
    let agent_id = "test-agent-123".to_string();
    let stream = monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add some output
    monitoring.add_output(
        agent_id.clone(),
        "Frontend".to_string(),
        OutputType::Info,
        "Test message".to_string(),
        Some("task-1".to_string()),
        "session-1".to_string(),
    ).unwrap();
    
    // Verify output was added
    let recent = monitoring.get_all_recent(10, None);
    assert_eq!(recent.len(), 1);
    assert_eq!(recent[0].content, "Test message");
    assert_eq!(recent[0].agent_id, agent_id);
    
    // Test agent-specific stream
    let agent_recent = stream.get_recent(10, None);
    assert_eq!(agent_recent.len(), 1);
    assert_eq!(agent_recent[0].content, "Test message");
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_subscription (lines 125-157) ---[0m
async fn test_output_subscription() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let agent_id = "subscription-test-agent".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Subscribe to global output
    let mut receiver = monitoring.subscribe_global();
    
    // Add output in a background task
    let monitoring_clone = Arc::clone(&monitoring);
    let agent_id_clone = agent_id.clone();
    tokio::spawn(async move {
        sleep(Duration::from_millis(100)).await;
        
        monitoring_clone.add_output(
            agent_id_clone,
            "QA".to_string(),
            OutputType::System,
            "Subscription test message".to_string(),
            Some("task-2".to_string()),
            "session-2".to_string(),
        ).unwrap();
    });
    
    // Wait for the message
    let received = tokio::time::timeout(Duration::from_secs(1), receiver.recv()).await;
    assert!(received.is_ok());
    
    let entry = received.unwrap().unwrap();
    assert_eq!(entry.content, "Subscription test message");
    assert_eq!(entry.output_type, OutputType::System);
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:228-260 function test_monitoring_stats <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:263-288 function test_buffer_size_limits
  Similarity: 85.69%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_monitoring_stats (lines 228-260) ---[0m
async fn test_monitoring_stats() {
    let monitoring = MonitoringSystem::new();
    
    // Register multiple agents
    let agents = vec![
        "stats-agent-1", "stats-agent-2", "stats-agent-3"
    ];
    
    for agent in &agents {
        monitoring.register_agent(agent.to_string()).unwrap();
    }
    
    // Add various outputs
    for (i, agent) in agents.iter().enumerate() {
        for j in 0..=i {
            monitoring.add_output(
                agent.to_string(),
                "Stats".to_string(),
                if j % 2 == 0 { OutputType::Info } else { OutputType::Error },
                format!("Stats message {} from {}", j, agent),
                None,
                "stats-session".to_string(),
            ).unwrap();
        }
    }
    
    let stats = monitoring.get_stats();
    assert_eq!(stats.active_streams, 3);
    assert_eq!(stats.total_entries, 6); // 1 + 2 + 3 = 6 total messages
    assert!(stats.entries_per_agent.contains_key("stats-agent-1"));
    assert!(stats.entries_per_agent.contains_key("stats-agent-2"));
    assert!(stats.entries_per_agent.contains_key("stats-agent-3"));
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_buffer_size_limits (lines 263-288) ---[0m
async fn test_buffer_size_limits() {
    let agent_id = "buffer-test-agent".to_string();
    let stream = AgentOutputStream::new(agent_id.clone());
    
    // Add more entries than the typical buffer size
    for i in 0..15000 {
        let entry = OutputEntry::new(
            agent_id.clone(),
            "Buffer".to_string(),
            OutputType::Debug,
            format!("Buffer test message {}", i),
            None,
            "buffer-session".to_string(),
        );
        stream.add_output(entry).unwrap();
    }
    
    // Should be limited by MAX_BUFFER_SIZE (10000)
    assert!(stream.len() <= 10000);
    
    // Recent entries should be the newest ones
    let recent = stream.get_recent(5, None);
    assert_eq!(recent.len(), 5);
    // Should contain the latest messages
    assert!(recent[4].content.contains("14999") || recent[4].content.contains("9999"));
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:12-39 function test_monitoring_system_basic_operations <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:93-122 function test_agent_output_stream
  Similarity: 86.09%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_monitoring_system_basic_operations (lines 12-39) ---[0m
async fn test_monitoring_system_basic_operations() {
    let monitoring = MonitoringSystem::new();
    
    // Register an agent
    let agent_id = "test-agent-123".to_string();
    let stream = monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add some output
    monitoring.add_output(
        agent_id.clone(),
        "Frontend".to_string(),
        OutputType::Info,
        "Test message".to_string(),
        Some("task-1".to_string()),
        "session-1".to_string(),
    ).unwrap();
    
    // Verify output was added
    let recent = monitoring.get_all_recent(10, None);
    assert_eq!(recent.len(), 1);
    assert_eq!(recent[0].content, "Test message");
    assert_eq!(recent[0].agent_id, agent_id);
    
    // Test agent-specific stream
    let agent_recent = stream.get_recent(10, None);
    assert_eq!(agent_recent.len(), 1);
    assert_eq!(agent_recent[0].content, "Test message");
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_agent_output_stream (lines 93-122) ---[0m
async fn test_agent_output_stream() {
    let agent_id = "stream-test-agent".to_string();
    let stream = AgentOutputStream::new(agent_id.clone());
    
    // Add some entries
    for i in 0..5 {
        let entry = OutputEntry::new(
            agent_id.clone(),
            "DevOps".to_string(),
            OutputType::Info,
            format!("Message {}", i),
            None,
            "session-1".to_string(),
        );
        stream.add_output(entry).unwrap();
    }
    
    // Test recent entries
    let recent = stream.get_recent(3, None);
    assert_eq!(recent.len(), 3);
    assert_eq!(recent[2].content, "Message 4"); // Most recent should be last
    assert_eq!(recent[0].content, "Message 2"); // Oldest of the 3
    
    // Test total length
    assert_eq!(stream.len(), 5);
    
    // Test clearing
    stream.clear();
    assert_eq!(stream.len(), 0);
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:93-122 function test_agent_output_stream <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:263-288 function test_buffer_size_limits
  Similarity: 86.19%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_agent_output_stream (lines 93-122) ---[0m
async fn test_agent_output_stream() {
    let agent_id = "stream-test-agent".to_string();
    let stream = AgentOutputStream::new(agent_id.clone());
    
    // Add some entries
    for i in 0..5 {
        let entry = OutputEntry::new(
            agent_id.clone(),
            "DevOps".to_string(),
            OutputType::Info,
            format!("Message {}", i),
            None,
            "session-1".to_string(),
        );
        stream.add_output(entry).unwrap();
    }
    
    // Test recent entries
    let recent = stream.get_recent(3, None);
    assert_eq!(recent.len(), 3);
    assert_eq!(recent[2].content, "Message 4"); // Most recent should be last
    assert_eq!(recent[0].content, "Message 2"); // Oldest of the 3
    
    // Test total length
    assert_eq!(stream.len(), 5);
    
    // Test clearing
    stream.clear();
    assert_eq!(stream.len(), 0);
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_buffer_size_limits (lines 263-288) ---[0m
async fn test_buffer_size_limits() {
    let agent_id = "buffer-test-agent".to_string();
    let stream = AgentOutputStream::new(agent_id.clone());
    
    // Add more entries than the typical buffer size
    for i in 0..15000 {
        let entry = OutputEntry::new(
            agent_id.clone(),
            "Buffer".to_string(),
            OutputType::Debug,
            format!("Buffer test message {}", i),
            None,
            "buffer-session".to_string(),
        );
        stream.add_output(entry).unwrap();
    }
    
    // Should be limited by MAX_BUFFER_SIZE (10000)
    assert!(stream.len() <= 10000);
    
    // Recent entries should be the newest ones
    let recent = stream.get_recent(5, None);
    assert_eq!(recent.len(), 5);
    // Should contain the latest messages
    assert!(recent[4].content.contains("14999") || recent[4].content.contains("9999"));
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:125-157 function test_output_subscription <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:263-288 function test_buffer_size_limits
  Similarity: 77.45%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_subscription (lines 125-157) ---[0m
async fn test_output_subscription() {
    let monitoring = Arc::new(MonitoringSystem::new());
    let agent_id = "subscription-test-agent".to_string();
    
    monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Subscribe to global output
    let mut receiver = monitoring.subscribe_global();
    
    // Add output in a background task
    let monitoring_clone = Arc::clone(&monitoring);
    let agent_id_clone = agent_id.clone();
    tokio::spawn(async move {
        sleep(Duration::from_millis(100)).await;
        
        monitoring_clone.add_output(
            agent_id_clone,
            "QA".to_string(),
            OutputType::System,
            "Subscription test message".to_string(),
            Some("task-2".to_string()),
            "session-2".to_string(),
        ).unwrap();
    });
    
    // Wait for the message
    let received = tokio::time::timeout(Duration::from_secs(1), receiver.recv()).await;
    assert!(received.is_ok());
    
    let entry = received.unwrap().unwrap();
    assert_eq!(entry.content, "Subscription test message");
    assert_eq!(entry.output_type, OutputType::System);
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_buffer_size_limits (lines 263-288) ---[0m
async fn test_buffer_size_limits() {
    let agent_id = "buffer-test-agent".to_string();
    let stream = AgentOutputStream::new(agent_id.clone());
    
    // Add more entries than the typical buffer size
    for i in 0..15000 {
        let entry = OutputEntry::new(
            agent_id.clone(),
            "Buffer".to_string(),
            OutputType::Debug,
            format!("Buffer test message {}", i),
            None,
            "buffer-session".to_string(),
        );
        stream.add_output(entry).unwrap();
    }
    
    // Should be limited by MAX_BUFFER_SIZE (10000)
    assert!(stream.len() <= 10000);
    
    // Recent entries should be the newest ones
    let recent = stream.get_recent(5, None);
    assert_eq!(recent.len(), 5);
    // Should contain the latest messages
    assert!(recent[4].content.contains("14999") || recent[4].content.contains("9999"));
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:12-39 function test_monitoring_system_basic_operations <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:263-288 function test_buffer_size_limits
  Similarity: 79.15%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_monitoring_system_basic_operations (lines 12-39) ---[0m
async fn test_monitoring_system_basic_operations() {
    let monitoring = MonitoringSystem::new();
    
    // Register an agent
    let agent_id = "test-agent-123".to_string();
    let stream = monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add some output
    monitoring.add_output(
        agent_id.clone(),
        "Frontend".to_string(),
        OutputType::Info,
        "Test message".to_string(),
        Some("task-1".to_string()),
        "session-1".to_string(),
    ).unwrap();
    
    // Verify output was added
    let recent = monitoring.get_all_recent(10, None);
    assert_eq!(recent.len(), 1);
    assert_eq!(recent[0].content, "Test message");
    assert_eq!(recent[0].agent_id, agent_id);
    
    // Test agent-specific stream
    let agent_recent = stream.get_recent(10, None);
    assert_eq!(agent_recent.len(), 1);
    assert_eq!(agent_recent[0].content, "Test message");
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_buffer_size_limits (lines 263-288) ---[0m
async fn test_buffer_size_limits() {
    let agent_id = "buffer-test-agent".to_string();
    let stream = AgentOutputStream::new(agent_id.clone());
    
    // Add more entries than the typical buffer size
    for i in 0..15000 {
        let entry = OutputEntry::new(
            agent_id.clone(),
            "Buffer".to_string(),
            OutputType::Debug,
            format!("Buffer test message {}", i),
            None,
            "buffer-session".to_string(),
        );
        stream.add_output(entry).unwrap();
    }
    
    // Should be limited by MAX_BUFFER_SIZE (10000)
    assert!(stream.len() <= 10000);
    
    // Recent entries should be the newest ones
    let recent = stream.get_recent(5, None);
    assert_eq!(recent.len(), 5);
    // Should contain the latest messages
    assert!(recent[4].content.contains("14999") || recent[4].content.contains("9999"));
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:204-225 function test_output_subscriber <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:263-288 function test_buffer_size_limits
  Similarity: 87.75%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_subscriber (lines 204-225) ---[0m
async fn test_output_subscriber() {
    let monitoring = MonitoringSystem::new();
    
    // Create a console subscriber
    let subscriber = Arc::new(ConsoleOutputSubscriber::new("test-console".to_string()));
    monitoring.add_subscriber(subscriber).unwrap();
    
    // Add output (should trigger subscriber)
    monitoring.add_output(
        "subscriber-test-agent".to_string(),
        "System".to_string(),
        OutputType::Warning,
        "Subscriber test message".to_string(),
        None,
        "subscriber-session".to_string(),
    ).unwrap();
    
    // The console subscriber will print to stdout, 
    // which we can't easily test here, but we can verify
    // the subscriber was called without error
    assert!(true);
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_buffer_size_limits (lines 263-288) ---[0m
async fn test_buffer_size_limits() {
    let agent_id = "buffer-test-agent".to_string();
    let stream = AgentOutputStream::new(agent_id.clone());
    
    // Add more entries than the typical buffer size
    for i in 0..15000 {
        let entry = OutputEntry::new(
            agent_id.clone(),
            "Buffer".to_string(),
            OutputType::Debug,
            format!("Buffer test message {}", i),
            None,
            "buffer-session".to_string(),
        );
        stream.add_output(entry).unwrap();
    }
    
    // Should be limited by MAX_BUFFER_SIZE (10000)
    assert!(stream.len() <= 10000);
    
    // Recent entries should be the newest ones
    let recent = stream.get_recent(5, None);
    assert_eq!(recent.len(), 5);
    // Should contain the latest messages
    assert!(recent[4].content.contains("14999") || recent[4].content.contains("9999"));
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:93-122 function test_agent_output_stream <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:204-225 function test_output_subscriber
  Similarity: 79.23%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_agent_output_stream (lines 93-122) ---[0m
async fn test_agent_output_stream() {
    let agent_id = "stream-test-agent".to_string();
    let stream = AgentOutputStream::new(agent_id.clone());
    
    // Add some entries
    for i in 0..5 {
        let entry = OutputEntry::new(
            agent_id.clone(),
            "DevOps".to_string(),
            OutputType::Info,
            format!("Message {}", i),
            None,
            "session-1".to_string(),
        );
        stream.add_output(entry).unwrap();
    }
    
    // Test recent entries
    let recent = stream.get_recent(3, None);
    assert_eq!(recent.len(), 3);
    assert_eq!(recent[2].content, "Message 4"); // Most recent should be last
    assert_eq!(recent[0].content, "Message 2"); // Oldest of the 3
    
    // Test total length
    assert_eq!(stream.len(), 5);
    
    // Test clearing
    stream.clear();
    assert_eq!(stream.len(), 0);
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_subscriber (lines 204-225) ---[0m
async fn test_output_subscriber() {
    let monitoring = MonitoringSystem::new();
    
    // Create a console subscriber
    let subscriber = Arc::new(ConsoleOutputSubscriber::new("test-console".to_string()));
    monitoring.add_subscriber(subscriber).unwrap();
    
    // Add output (should trigger subscriber)
    monitoring.add_output(
        "subscriber-test-agent".to_string(),
        "System".to_string(),
        OutputType::Warning,
        "Subscriber test message".to_string(),
        None,
        "subscriber-session".to_string(),
    ).unwrap();
    
    // The console subscriber will print to stdout, 
    // which we can't easily test here, but we can verify
    // the subscriber was called without error
    assert!(true);
}

  ./crates/ccswarm/src/tests/monitoring_tests.rs:12-39 function test_monitoring_system_basic_operations <-> ./crates/ccswarm/src/tests/monitoring_tests.rs:204-225 function test_output_subscriber
  Similarity: 80.41%

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_monitoring_system_basic_operations (lines 12-39) ---[0m
async fn test_monitoring_system_basic_operations() {
    let monitoring = MonitoringSystem::new();
    
    // Register an agent
    let agent_id = "test-agent-123".to_string();
    let stream = monitoring.register_agent(agent_id.clone()).unwrap();
    
    // Add some output
    monitoring.add_output(
        agent_id.clone(),
        "Frontend".to_string(),
        OutputType::Info,
        "Test message".to_string(),
        Some("task-1".to_string()),
        "session-1".to_string(),
    ).unwrap();
    
    // Verify output was added
    let recent = monitoring.get_all_recent(10, None);
    assert_eq!(recent.len(), 1);
    assert_eq!(recent[0].content, "Test message");
    assert_eq!(recent[0].agent_id, agent_id);
    
    // Test agent-specific stream
    let agent_recent = stream.get_recent(10, None);
    assert_eq!(agent_recent.len(), 1);
    assert_eq!(agent_recent[0].content, "Test message");
}

[36m--- ./crates/ccswarm/src/tests/monitoring_tests.rs:test_output_subscriber (lines 204-225) ---[0m
async fn test_output_subscriber() {
    let monitoring = MonitoringSystem::new();
    
    // Create a console subscriber
    let subscriber = Arc::new(ConsoleOutputSubscriber::new("test-console".to_string()));
    monitoring.add_subscriber(subscriber).unwrap();
    
    // Add output (should trigger subscriber)
    monitoring.add_output(
        "subscriber-test-agent".to_string(),
        "System".to_string(),
        OutputType::Warning,
        "Subscriber test message".to_string(),
        None,
        "subscriber-session".to_string(),
    ).unwrap();
    
    // The console subscriber will print to stdout, 
    // which we can't easily test here, but we can verify
    // the subscriber was called without error
    assert!(true);
}


Duplicates in ./crates/ccswarm/src/orchestrator/agent_access.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/orchestrator/agent_access.rs:87-106 function test_agent_attribute_access <-> ./crates/ccswarm/src/orchestrator/agent_access.rs:109-125 function test_role_conversions
  Similarity: 79.43%

[36m--- ./crates/ccswarm/src/orchestrator/agent_access.rs:test_agent_attribute_access (lines 87-106) ---[0m
    async fn test_agent_attribute_access() {
        let agent = ClaudeCodeAgent::new(
            crate::identity::default_frontend_role(),
            &std::path::PathBuf::from("/tmp/test"),
            "test",
            crate::config::ClaudeConfig::default(),
        )
        .await
        .unwrap();

        assert_eq!(agent.role().name(), "Frontend");
        assert_eq!(agent.specialization(), "Frontend");
        assert!(agent.has_capability("Frontend"));
        assert!(agent.has_capability("frontend"));
        assert!(!agent.has_capability("Backend"));

        let capabilities = agent.capabilities();
        assert_eq!(capabilities.len(), 2);
        assert!(capabilities.contains(&"Frontend".to_string()));
    }

[36m--- ./crates/ccswarm/src/orchestrator/agent_access.rs:test_role_conversions (lines 109-125) ---[0m
    fn test_role_conversions() {
        // Test role from specialization
        assert_eq!(role_from_specialization("frontend").name(), "Frontend");
        assert_eq!(
            role_from_specialization("react_typescript").name(),
            "Frontend"
        );
        assert_eq!(role_from_specialization("Backend").name(), "Backend");
        assert_eq!(role_from_specialization("unknown").name(), "Frontend"); // default

        // Test specialization from role
        let frontend_role = crate::identity::default_frontend_role();
        assert_eq!(specialization_from_role(&frontend_role), "frontend");

        let backend_role = crate::identity::default_backend_role();
        assert_eq!(specialization_from_role(&backend_role), "backend");
    }

  ./crates/ccswarm/src/orchestrator/agent_access.rs:55-69 function role_from_specialization <-> ./crates/ccswarm/src/orchestrator/agent_access.rs:72-80 function specialization_from_role
  Similarity: 86.35%

[36m--- ./crates/ccswarm/src/orchestrator/agent_access.rs:role_from_specialization (lines 55-69) ---[0m
pub fn role_from_specialization(specialization: &str) -> AgentRole {
    match specialization {
        "react_typescript" | "frontend" | "Frontend" => crate::identity::default_frontend_role(),
        "node_microservices" | "backend" | "Backend" => crate::identity::default_backend_role(),
        "aws_kubernetes" | "devops" | "DevOps" => crate::identity::default_devops_role(),
        "qa" | "testing" | "QA" => crate::identity::default_qa_role(),
        _ => {
            tracing::warn!(
                "Unknown specialization: {}, defaulting to frontend role",
                specialization
            );
            crate::identity::default_frontend_role()
        }
    }
}

[36m--- ./crates/ccswarm/src/orchestrator/agent_access.rs:specialization_from_role (lines 72-80) ---[0m
pub fn specialization_from_role(role: &AgentRole) -> String {
    match role.name() {
        "Frontend" => "frontend".to_string(),
        "Backend" => "backend".to_string(),
        "DevOps" => "devops".to_string(),
        "QA" => "qa".to_string(),
        _ => role.name().to_lowercase(),
    }
}


Duplicates in ./crates/ccswarm/src/error.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/error.rs:172-180 method with_context <-> ./crates/ccswarm/src/error.rs:182-191 method with_context_and_source
  Similarity: 98.78%

[36m--- ./crates/ccswarm/src/error.rs:with_context (lines 172-180) ---[0m
    fn with_context<F>(self, f: F) -> Result<T>
    where
        F: FnOnce() -> String,
    {
        self.map_err(|e| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(e)),
        })
    }

[36m--- ./crates/ccswarm/src/error.rs:with_context_and_source (lines 182-191) ---[0m
    fn with_context_and_source<F, S>(self, f: F, source: S) -> Result<T>
    where
        F: FnOnce() -> String,
        S: std::error::Error + Send + Sync + 'static,
    {
        self.map_err(|_| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(source)),
        })
    }

  ./crates/ccswarm/src/error.rs:357-367 method severity <-> ./crates/ccswarm/src/error.rs:386-394 method fmt
  Similarity: 86.39%
  Classes: CCSwarmError <-> ErrorSeverity

[36m--- ./crates/ccswarm/src/error.rs:severity (lines 357-367) ---[0m
    pub fn severity(&self) -> ErrorSeverity {
        match self {
            Self::Auth { .. } | Self::Configuration { .. } => ErrorSeverity::Critical,
            Self::Agent { .. } | Self::Session { .. } | Self::Extension { .. } => ErrorSeverity::High,
            Self::Task { .. } | Self::Git { .. } | Self::Template { .. } => ErrorSeverity::Medium,
            Self::Network { .. } | Self::Resource { .. } => ErrorSeverity::Low,
            Self::Io(_) | Self::SerdeJson(_) => ErrorSeverity::Medium,
            Self::UserError { .. } => ErrorSeverity::Info,
            Self::Other { .. } => ErrorSeverity::Medium,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:fmt (lines 386-394) ---[0m
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Info => write!(f, "INFO"),
            Self::Low => write!(f, "LOW"),
            Self::Medium => write!(f, "MEDIUM"),
            Self::High => write!(f, "HIGH"),
            Self::Critical => write!(f, "CRITICAL"),
        }
    }

  ./crates/ccswarm/src/error.rs:248-254 method template <-> ./crates/ccswarm/src/error.rs:275-281 method resource
  Similarity: 98.06%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:template (lines 248-254) ---[0m
    pub fn template<S: Into<String>>(message: S) -> Self {
        Self::Template {
            message: message.into(),
            template_name: None,
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:resource (lines 275-281) ---[0m
    pub fn resource<S: Into<String>>(message: S) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: None,
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:257-263 method template_with_name <-> ./crates/ccswarm/src/error.rs:284-290 method resource_with_type
  Similarity: 97.91%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:template_with_name (lines 257-263) ---[0m
    pub fn template_with_name<S: Into<String>, N: Into<String>>(message: S, template_name: N) -> Self {
        Self::Template {
            message: message.into(),
            template_name: Some(template_name.into()),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:resource_with_type (lines 284-290) ---[0m
    pub fn resource_with_type<S: Into<String>, T: Into<String>>(message: S, resource_type: T) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: Some(resource_type.into()),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:248-254 method template <-> ./crates/ccswarm/src/error.rs:257-263 method template_with_name
  Similarity: 97.67%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:template (lines 248-254) ---[0m
    pub fn template<S: Into<String>>(message: S) -> Self {
        Self::Template {
            message: message.into(),
            template_name: None,
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:template_with_name (lines 257-263) ---[0m
    pub fn template_with_name<S: Into<String>, N: Into<String>>(message: S, template_name: N) -> Self {
        Self::Template {
            message: message.into(),
            template_name: Some(template_name.into()),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:275-281 method resource <-> ./crates/ccswarm/src/error.rs:284-290 method resource_with_type
  Similarity: 97.67%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:resource (lines 275-281) ---[0m
    pub fn resource<S: Into<String>>(message: S) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: None,
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:resource_with_type (lines 284-290) ---[0m
    pub fn resource_with_type<S: Into<String>, T: Into<String>>(message: S, resource_type: T) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: Some(resource_type.into()),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:205-211 method agent <-> ./crates/ccswarm/src/error.rs:214-220 method session
  Similarity: 97.63%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:agent (lines 205-211) ---[0m
    pub fn agent<S: Into<String>, I: Into<String>>(agent_id: I, message: S) -> Self {
        Self::Agent {
            agent_id: agent_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:session (lines 214-220) ---[0m
    pub fn session<S: Into<String>, I: Into<String>>(session_id: I, message: S) -> Self {
        Self::Session {
            session_id: session_id.into(),
            message: message.into(),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:205-211 method agent <-> ./crates/ccswarm/src/error.rs:223-229 method task
  Similarity: 97.63%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:agent (lines 205-211) ---[0m
    pub fn agent<S: Into<String>, I: Into<String>>(agent_id: I, message: S) -> Self {
        Self::Agent {
            agent_id: agent_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:task (lines 223-229) ---[0m
    pub fn task<S: Into<String>, I: Into<String>>(task_id: I, message: S) -> Self {
        Self::Task {
            task_id: task_id.into(),
            message: message.into(),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:205-211 method agent <-> ./crates/ccswarm/src/error.rs:266-272 method extension
  Similarity: 97.63%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:agent (lines 205-211) ---[0m
    pub fn agent<S: Into<String>, I: Into<String>>(agent_id: I, message: S) -> Self {
        Self::Agent {
            agent_id: agent_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:extension (lines 266-272) ---[0m
    pub fn extension<S: Into<String>, I: Into<String>>(extension_id: I, message: S) -> Self {
        Self::Extension {
            extension_id: extension_id.into(),
            message: message.into(),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:214-220 method session <-> ./crates/ccswarm/src/error.rs:223-229 method task
  Similarity: 97.63%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:session (lines 214-220) ---[0m
    pub fn session<S: Into<String>, I: Into<String>>(session_id: I, message: S) -> Self {
        Self::Session {
            session_id: session_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:task (lines 223-229) ---[0m
    pub fn task<S: Into<String>, I: Into<String>>(task_id: I, message: S) -> Self {
        Self::Task {
            task_id: task_id.into(),
            message: message.into(),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:214-220 method session <-> ./crates/ccswarm/src/error.rs:266-272 method extension
  Similarity: 97.63%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:session (lines 214-220) ---[0m
    pub fn session<S: Into<String>, I: Into<String>>(session_id: I, message: S) -> Self {
        Self::Session {
            session_id: session_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:extension (lines 266-272) ---[0m
    pub fn extension<S: Into<String>, I: Into<String>>(extension_id: I, message: S) -> Self {
        Self::Extension {
            extension_id: extension_id.into(),
            message: message.into(),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:223-229 method task <-> ./crates/ccswarm/src/error.rs:266-272 method extension
  Similarity: 97.63%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:task (lines 223-229) ---[0m
    pub fn task<S: Into<String>, I: Into<String>>(task_id: I, message: S) -> Self {
        Self::Task {
            task_id: task_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:extension (lines 266-272) ---[0m
    pub fn extension<S: Into<String>, I: Into<String>>(extension_id: I, message: S) -> Self {
        Self::Extension {
            extension_id: extension_id.into(),
            message: message.into(),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:248-254 method template <-> ./crates/ccswarm/src/error.rs:284-290 method resource_with_type
  Similarity: 96.28%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:template (lines 248-254) ---[0m
    pub fn template<S: Into<String>>(message: S) -> Self {
        Self::Template {
            message: message.into(),
            template_name: None,
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:resource_with_type (lines 284-290) ---[0m
    pub fn resource_with_type<S: Into<String>, T: Into<String>>(message: S, resource_type: T) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: Some(resource_type.into()),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:257-263 method template_with_name <-> ./crates/ccswarm/src/error.rs:275-281 method resource
  Similarity: 96.28%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:template_with_name (lines 257-263) ---[0m
    pub fn template_with_name<S: Into<String>, N: Into<String>>(message: S, template_name: N) -> Self {
        Self::Template {
            message: message.into(),
            template_name: Some(template_name.into()),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:resource (lines 275-281) ---[0m
    pub fn resource<S: Into<String>>(message: S) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: None,
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:182-191 method with_context_and_source <-> ./crates/ccswarm/src/error.rs:309-314 method user_error_with_suggestion
  Similarity: 84.08%

[36m--- ./crates/ccswarm/src/error.rs:with_context_and_source (lines 182-191) ---[0m
    fn with_context_and_source<F, S>(self, f: F, source: S) -> Result<T>
    where
        F: FnOnce() -> String,
        S: std::error::Error + Send + Sync + 'static,
    {
        self.map_err(|_| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(source)),
        })
    }

[36m--- ./crates/ccswarm/src/error.rs:user_error_with_suggestion (lines 309-314) ---[0m
    pub fn user_error_with_suggestion<S: Into<String>, T: Into<String>>(message: S, suggestion: T) -> Self {
        Self::UserError {
            message: message.into(),
            suggestion: Some(suggestion.into()),
        }
    }

  ./crates/ccswarm/src/error.rs:205-211 method agent <-> ./crates/ccswarm/src/error.rs:248-254 method template
  Similarity: 94.21%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:agent (lines 205-211) ---[0m
    pub fn agent<S: Into<String>, I: Into<String>>(agent_id: I, message: S) -> Self {
        Self::Agent {
            agent_id: agent_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:template (lines 248-254) ---[0m
    pub fn template<S: Into<String>>(message: S) -> Self {
        Self::Template {
            message: message.into(),
            template_name: None,
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:205-211 method agent <-> ./crates/ccswarm/src/error.rs:275-281 method resource
  Similarity: 94.21%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:agent (lines 205-211) ---[0m
    pub fn agent<S: Into<String>, I: Into<String>>(agent_id: I, message: S) -> Self {
        Self::Agent {
            agent_id: agent_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:resource (lines 275-281) ---[0m
    pub fn resource<S: Into<String>>(message: S) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: None,
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:214-220 method session <-> ./crates/ccswarm/src/error.rs:248-254 method template
  Similarity: 94.21%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:session (lines 214-220) ---[0m
    pub fn session<S: Into<String>, I: Into<String>>(session_id: I, message: S) -> Self {
        Self::Session {
            session_id: session_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:template (lines 248-254) ---[0m
    pub fn template<S: Into<String>>(message: S) -> Self {
        Self::Template {
            message: message.into(),
            template_name: None,
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:214-220 method session <-> ./crates/ccswarm/src/error.rs:275-281 method resource
  Similarity: 94.21%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:session (lines 214-220) ---[0m
    pub fn session<S: Into<String>, I: Into<String>>(session_id: I, message: S) -> Self {
        Self::Session {
            session_id: session_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:resource (lines 275-281) ---[0m
    pub fn resource<S: Into<String>>(message: S) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: None,
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:223-229 method task <-> ./crates/ccswarm/src/error.rs:248-254 method template
  Similarity: 94.21%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:task (lines 223-229) ---[0m
    pub fn task<S: Into<String>, I: Into<String>>(task_id: I, message: S) -> Self {
        Self::Task {
            task_id: task_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:template (lines 248-254) ---[0m
    pub fn template<S: Into<String>>(message: S) -> Self {
        Self::Template {
            message: message.into(),
            template_name: None,
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:223-229 method task <-> ./crates/ccswarm/src/error.rs:275-281 method resource
  Similarity: 94.21%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:task (lines 223-229) ---[0m
    pub fn task<S: Into<String>, I: Into<String>>(task_id: I, message: S) -> Self {
        Self::Task {
            task_id: task_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:resource (lines 275-281) ---[0m
    pub fn resource<S: Into<String>>(message: S) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: None,
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:248-254 method template <-> ./crates/ccswarm/src/error.rs:266-272 method extension
  Similarity: 94.21%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:template (lines 248-254) ---[0m
    pub fn template<S: Into<String>>(message: S) -> Self {
        Self::Template {
            message: message.into(),
            template_name: None,
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:extension (lines 266-272) ---[0m
    pub fn extension<S: Into<String>, I: Into<String>>(extension_id: I, message: S) -> Self {
        Self::Extension {
            extension_id: extension_id.into(),
            message: message.into(),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:266-272 method extension <-> ./crates/ccswarm/src/error.rs:275-281 method resource
  Similarity: 94.21%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:extension (lines 266-272) ---[0m
    pub fn extension<S: Into<String>, I: Into<String>>(extension_id: I, message: S) -> Self {
        Self::Extension {
            extension_id: extension_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:resource (lines 275-281) ---[0m
    pub fn resource<S: Into<String>>(message: S) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: None,
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:182-191 method with_context_and_source <-> ./crates/ccswarm/src/error.rs:248-254 method template
  Similarity: 75.31%

[36m--- ./crates/ccswarm/src/error.rs:with_context_and_source (lines 182-191) ---[0m
    fn with_context_and_source<F, S>(self, f: F, source: S) -> Result<T>
    where
        F: FnOnce() -> String,
        S: std::error::Error + Send + Sync + 'static,
    {
        self.map_err(|_| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(source)),
        })
    }

[36m--- ./crates/ccswarm/src/error.rs:template (lines 248-254) ---[0m
    pub fn template<S: Into<String>>(message: S) -> Self {
        Self::Template {
            message: message.into(),
            template_name: None,
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:182-191 method with_context_and_source <-> ./crates/ccswarm/src/error.rs:275-281 method resource
  Similarity: 75.31%

[36m--- ./crates/ccswarm/src/error.rs:with_context_and_source (lines 182-191) ---[0m
    fn with_context_and_source<F, S>(self, f: F, source: S) -> Result<T>
    where
        F: FnOnce() -> String,
        S: std::error::Error + Send + Sync + 'static,
    {
        self.map_err(|_| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(source)),
        })
    }

[36m--- ./crates/ccswarm/src/error.rs:resource (lines 275-281) ---[0m
    pub fn resource<S: Into<String>>(message: S) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: None,
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:205-211 method agent <-> ./crates/ccswarm/src/error.rs:257-263 method template_with_name
  Similarity: 90.23%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:agent (lines 205-211) ---[0m
    pub fn agent<S: Into<String>, I: Into<String>>(agent_id: I, message: S) -> Self {
        Self::Agent {
            agent_id: agent_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:template_with_name (lines 257-263) ---[0m
    pub fn template_with_name<S: Into<String>, N: Into<String>>(message: S, template_name: N) -> Self {
        Self::Template {
            message: message.into(),
            template_name: Some(template_name.into()),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:205-211 method agent <-> ./crates/ccswarm/src/error.rs:284-290 method resource_with_type
  Similarity: 90.23%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:agent (lines 205-211) ---[0m
    pub fn agent<S: Into<String>, I: Into<String>>(agent_id: I, message: S) -> Self {
        Self::Agent {
            agent_id: agent_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:resource_with_type (lines 284-290) ---[0m
    pub fn resource_with_type<S: Into<String>, T: Into<String>>(message: S, resource_type: T) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: Some(resource_type.into()),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:214-220 method session <-> ./crates/ccswarm/src/error.rs:257-263 method template_with_name
  Similarity: 90.23%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:session (lines 214-220) ---[0m
    pub fn session<S: Into<String>, I: Into<String>>(session_id: I, message: S) -> Self {
        Self::Session {
            session_id: session_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:template_with_name (lines 257-263) ---[0m
    pub fn template_with_name<S: Into<String>, N: Into<String>>(message: S, template_name: N) -> Self {
        Self::Template {
            message: message.into(),
            template_name: Some(template_name.into()),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:214-220 method session <-> ./crates/ccswarm/src/error.rs:284-290 method resource_with_type
  Similarity: 90.23%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:session (lines 214-220) ---[0m
    pub fn session<S: Into<String>, I: Into<String>>(session_id: I, message: S) -> Self {
        Self::Session {
            session_id: session_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:resource_with_type (lines 284-290) ---[0m
    pub fn resource_with_type<S: Into<String>, T: Into<String>>(message: S, resource_type: T) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: Some(resource_type.into()),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:223-229 method task <-> ./crates/ccswarm/src/error.rs:257-263 method template_with_name
  Similarity: 90.23%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:task (lines 223-229) ---[0m
    pub fn task<S: Into<String>, I: Into<String>>(task_id: I, message: S) -> Self {
        Self::Task {
            task_id: task_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:template_with_name (lines 257-263) ---[0m
    pub fn template_with_name<S: Into<String>, N: Into<String>>(message: S, template_name: N) -> Self {
        Self::Template {
            message: message.into(),
            template_name: Some(template_name.into()),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:223-229 method task <-> ./crates/ccswarm/src/error.rs:284-290 method resource_with_type
  Similarity: 90.23%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:task (lines 223-229) ---[0m
    pub fn task<S: Into<String>, I: Into<String>>(task_id: I, message: S) -> Self {
        Self::Task {
            task_id: task_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:resource_with_type (lines 284-290) ---[0m
    pub fn resource_with_type<S: Into<String>, T: Into<String>>(message: S, resource_type: T) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: Some(resource_type.into()),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:257-263 method template_with_name <-> ./crates/ccswarm/src/error.rs:266-272 method extension
  Similarity: 90.23%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:template_with_name (lines 257-263) ---[0m
    pub fn template_with_name<S: Into<String>, N: Into<String>>(message: S, template_name: N) -> Self {
        Self::Template {
            message: message.into(),
            template_name: Some(template_name.into()),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:extension (lines 266-272) ---[0m
    pub fn extension<S: Into<String>, I: Into<String>>(extension_id: I, message: S) -> Self {
        Self::Extension {
            extension_id: extension_id.into(),
            message: message.into(),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:266-272 method extension <-> ./crates/ccswarm/src/error.rs:284-290 method resource_with_type
  Similarity: 90.23%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:extension (lines 266-272) ---[0m
    pub fn extension<S: Into<String>, I: Into<String>>(extension_id: I, message: S) -> Self {
        Self::Extension {
            extension_id: extension_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:resource_with_type (lines 284-290) ---[0m
    pub fn resource_with_type<S: Into<String>, T: Into<String>>(message: S, resource_type: T) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: Some(resource_type.into()),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:172-180 method with_context <-> ./crates/ccswarm/src/error.rs:309-314 method user_error_with_suggestion
  Similarity: 84.08%

[36m--- ./crates/ccswarm/src/error.rs:with_context (lines 172-180) ---[0m
    fn with_context<F>(self, f: F) -> Result<T>
    where
        F: FnOnce() -> String,
    {
        self.map_err(|e| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(e)),
        })
    }

[36m--- ./crates/ccswarm/src/error.rs:user_error_with_suggestion (lines 309-314) ---[0m
    pub fn user_error_with_suggestion<S: Into<String>, T: Into<String>>(message: S, suggestion: T) -> Self {
        Self::UserError {
            message: message.into(),
            suggestion: Some(suggestion.into()),
        }
    }

  ./crates/ccswarm/src/error.rs:182-191 method with_context_and_source <-> ./crates/ccswarm/src/error.rs:205-211 method agent
  Similarity: 73.88%

[36m--- ./crates/ccswarm/src/error.rs:with_context_and_source (lines 182-191) ---[0m
    fn with_context_and_source<F, S>(self, f: F, source: S) -> Result<T>
    where
        F: FnOnce() -> String,
        S: std::error::Error + Send + Sync + 'static,
    {
        self.map_err(|_| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(source)),
        })
    }

[36m--- ./crates/ccswarm/src/error.rs:agent (lines 205-211) ---[0m
    pub fn agent<S: Into<String>, I: Into<String>>(agent_id: I, message: S) -> Self {
        Self::Agent {
            agent_id: agent_id.into(),
            message: message.into(),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:182-191 method with_context_and_source <-> ./crates/ccswarm/src/error.rs:214-220 method session
  Similarity: 73.88%

[36m--- ./crates/ccswarm/src/error.rs:with_context_and_source (lines 182-191) ---[0m
    fn with_context_and_source<F, S>(self, f: F, source: S) -> Result<T>
    where
        F: FnOnce() -> String,
        S: std::error::Error + Send + Sync + 'static,
    {
        self.map_err(|_| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(source)),
        })
    }

[36m--- ./crates/ccswarm/src/error.rs:session (lines 214-220) ---[0m
    pub fn session<S: Into<String>, I: Into<String>>(session_id: I, message: S) -> Self {
        Self::Session {
            session_id: session_id.into(),
            message: message.into(),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:182-191 method with_context_and_source <-> ./crates/ccswarm/src/error.rs:223-229 method task
  Similarity: 73.88%

[36m--- ./crates/ccswarm/src/error.rs:with_context_and_source (lines 182-191) ---[0m
    fn with_context_and_source<F, S>(self, f: F, source: S) -> Result<T>
    where
        F: FnOnce() -> String,
        S: std::error::Error + Send + Sync + 'static,
    {
        self.map_err(|_| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(source)),
        })
    }

[36m--- ./crates/ccswarm/src/error.rs:task (lines 223-229) ---[0m
    pub fn task<S: Into<String>, I: Into<String>>(task_id: I, message: S) -> Self {
        Self::Task {
            task_id: task_id.into(),
            message: message.into(),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:182-191 method with_context_and_source <-> ./crates/ccswarm/src/error.rs:257-263 method template_with_name
  Similarity: 73.88%

[36m--- ./crates/ccswarm/src/error.rs:with_context_and_source (lines 182-191) ---[0m
    fn with_context_and_source<F, S>(self, f: F, source: S) -> Result<T>
    where
        F: FnOnce() -> String,
        S: std::error::Error + Send + Sync + 'static,
    {
        self.map_err(|_| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(source)),
        })
    }

[36m--- ./crates/ccswarm/src/error.rs:template_with_name (lines 257-263) ---[0m
    pub fn template_with_name<S: Into<String>, N: Into<String>>(message: S, template_name: N) -> Self {
        Self::Template {
            message: message.into(),
            template_name: Some(template_name.into()),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:182-191 method with_context_and_source <-> ./crates/ccswarm/src/error.rs:266-272 method extension
  Similarity: 73.88%

[36m--- ./crates/ccswarm/src/error.rs:with_context_and_source (lines 182-191) ---[0m
    fn with_context_and_source<F, S>(self, f: F, source: S) -> Result<T>
    where
        F: FnOnce() -> String,
        S: std::error::Error + Send + Sync + 'static,
    {
        self.map_err(|_| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(source)),
        })
    }

[36m--- ./crates/ccswarm/src/error.rs:extension (lines 266-272) ---[0m
    pub fn extension<S: Into<String>, I: Into<String>>(extension_id: I, message: S) -> Self {
        Self::Extension {
            extension_id: extension_id.into(),
            message: message.into(),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:182-191 method with_context_and_source <-> ./crates/ccswarm/src/error.rs:284-290 method resource_with_type
  Similarity: 73.88%

[36m--- ./crates/ccswarm/src/error.rs:with_context_and_source (lines 182-191) ---[0m
    fn with_context_and_source<F, S>(self, f: F, source: S) -> Result<T>
    where
        F: FnOnce() -> String,
        S: std::error::Error + Send + Sync + 'static,
    {
        self.map_err(|_| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(source)),
        })
    }

[36m--- ./crates/ccswarm/src/error.rs:resource_with_type (lines 284-290) ---[0m
    pub fn resource_with_type<S: Into<String>, T: Into<String>>(message: S, resource_type: T) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: Some(resource_type.into()),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:172-180 method with_context <-> ./crates/ccswarm/src/error.rs:248-254 method template
  Similarity: 75.31%

[36m--- ./crates/ccswarm/src/error.rs:with_context (lines 172-180) ---[0m
    fn with_context<F>(self, f: F) -> Result<T>
    where
        F: FnOnce() -> String,
    {
        self.map_err(|e| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(e)),
        })
    }

[36m--- ./crates/ccswarm/src/error.rs:template (lines 248-254) ---[0m
    pub fn template<S: Into<String>>(message: S) -> Self {
        Self::Template {
            message: message.into(),
            template_name: None,
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:172-180 method with_context <-> ./crates/ccswarm/src/error.rs:275-281 method resource
  Similarity: 75.31%

[36m--- ./crates/ccswarm/src/error.rs:with_context (lines 172-180) ---[0m
    fn with_context<F>(self, f: F) -> Result<T>
    where
        F: FnOnce() -> String,
    {
        self.map_err(|e| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(e)),
        })
    }

[36m--- ./crates/ccswarm/src/error.rs:resource (lines 275-281) ---[0m
    pub fn resource<S: Into<String>>(message: S) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: None,
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:257-263 method template_with_name <-> ./crates/ccswarm/src/error.rs:309-314 method user_error_with_suggestion
  Similarity: 90.93%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:template_with_name (lines 257-263) ---[0m
    pub fn template_with_name<S: Into<String>, N: Into<String>>(message: S, template_name: N) -> Self {
        Self::Template {
            message: message.into(),
            template_name: Some(template_name.into()),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:user_error_with_suggestion (lines 309-314) ---[0m
    pub fn user_error_with_suggestion<S: Into<String>, T: Into<String>>(message: S, suggestion: T) -> Self {
        Self::UserError {
            message: message.into(),
            suggestion: Some(suggestion.into()),
        }
    }

  ./crates/ccswarm/src/error.rs:284-290 method resource_with_type <-> ./crates/ccswarm/src/error.rs:309-314 method user_error_with_suggestion
  Similarity: 90.93%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:resource_with_type (lines 284-290) ---[0m
    pub fn resource_with_type<S: Into<String>, T: Into<String>>(message: S, resource_type: T) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: Some(resource_type.into()),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:user_error_with_suggestion (lines 309-314) ---[0m
    pub fn user_error_with_suggestion<S: Into<String>, T: Into<String>>(message: S, suggestion: T) -> Self {
        Self::UserError {
            message: message.into(),
            suggestion: Some(suggestion.into()),
        }
    }

  ./crates/ccswarm/src/error.rs:172-180 method with_context <-> ./crates/ccswarm/src/error.rs:205-211 method agent
  Similarity: 73.88%

[36m--- ./crates/ccswarm/src/error.rs:with_context (lines 172-180) ---[0m
    fn with_context<F>(self, f: F) -> Result<T>
    where
        F: FnOnce() -> String,
    {
        self.map_err(|e| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(e)),
        })
    }

[36m--- ./crates/ccswarm/src/error.rs:agent (lines 205-211) ---[0m
    pub fn agent<S: Into<String>, I: Into<String>>(agent_id: I, message: S) -> Self {
        Self::Agent {
            agent_id: agent_id.into(),
            message: message.into(),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:172-180 method with_context <-> ./crates/ccswarm/src/error.rs:214-220 method session
  Similarity: 73.88%

[36m--- ./crates/ccswarm/src/error.rs:with_context (lines 172-180) ---[0m
    fn with_context<F>(self, f: F) -> Result<T>
    where
        F: FnOnce() -> String,
    {
        self.map_err(|e| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(e)),
        })
    }

[36m--- ./crates/ccswarm/src/error.rs:session (lines 214-220) ---[0m
    pub fn session<S: Into<String>, I: Into<String>>(session_id: I, message: S) -> Self {
        Self::Session {
            session_id: session_id.into(),
            message: message.into(),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:172-180 method with_context <-> ./crates/ccswarm/src/error.rs:223-229 method task
  Similarity: 73.88%

[36m--- ./crates/ccswarm/src/error.rs:with_context (lines 172-180) ---[0m
    fn with_context<F>(self, f: F) -> Result<T>
    where
        F: FnOnce() -> String,
    {
        self.map_err(|e| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(e)),
        })
    }

[36m--- ./crates/ccswarm/src/error.rs:task (lines 223-229) ---[0m
    pub fn task<S: Into<String>, I: Into<String>>(task_id: I, message: S) -> Self {
        Self::Task {
            task_id: task_id.into(),
            message: message.into(),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:172-180 method with_context <-> ./crates/ccswarm/src/error.rs:257-263 method template_with_name
  Similarity: 73.88%

[36m--- ./crates/ccswarm/src/error.rs:with_context (lines 172-180) ---[0m
    fn with_context<F>(self, f: F) -> Result<T>
    where
        F: FnOnce() -> String,
    {
        self.map_err(|e| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(e)),
        })
    }

[36m--- ./crates/ccswarm/src/error.rs:template_with_name (lines 257-263) ---[0m
    pub fn template_with_name<S: Into<String>, N: Into<String>>(message: S, template_name: N) -> Self {
        Self::Template {
            message: message.into(),
            template_name: Some(template_name.into()),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:172-180 method with_context <-> ./crates/ccswarm/src/error.rs:266-272 method extension
  Similarity: 73.88%

[36m--- ./crates/ccswarm/src/error.rs:with_context (lines 172-180) ---[0m
    fn with_context<F>(self, f: F) -> Result<T>
    where
        F: FnOnce() -> String,
    {
        self.map_err(|e| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(e)),
        })
    }

[36m--- ./crates/ccswarm/src/error.rs:extension (lines 266-272) ---[0m
    pub fn extension<S: Into<String>, I: Into<String>>(extension_id: I, message: S) -> Self {
        Self::Extension {
            extension_id: extension_id.into(),
            message: message.into(),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:172-180 method with_context <-> ./crates/ccswarm/src/error.rs:284-290 method resource_with_type
  Similarity: 73.88%

[36m--- ./crates/ccswarm/src/error.rs:with_context (lines 172-180) ---[0m
    fn with_context<F>(self, f: F) -> Result<T>
    where
        F: FnOnce() -> String,
    {
        self.map_err(|e| CCSwarmError::Other {
            message: f(),
            source: Some(Box::new(e)),
        })
    }

[36m--- ./crates/ccswarm/src/error.rs:resource_with_type (lines 284-290) ---[0m
    pub fn resource_with_type<S: Into<String>, T: Into<String>>(message: S, resource_type: T) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: Some(resource_type.into()),
            source: None,
        }
    }

  ./crates/ccswarm/src/error.rs:205-211 method agent <-> ./crates/ccswarm/src/error.rs:309-314 method user_error_with_suggestion
  Similarity: 87.89%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:agent (lines 205-211) ---[0m
    pub fn agent<S: Into<String>, I: Into<String>>(agent_id: I, message: S) -> Self {
        Self::Agent {
            agent_id: agent_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:user_error_with_suggestion (lines 309-314) ---[0m
    pub fn user_error_with_suggestion<S: Into<String>, T: Into<String>>(message: S, suggestion: T) -> Self {
        Self::UserError {
            message: message.into(),
            suggestion: Some(suggestion.into()),
        }
    }

  ./crates/ccswarm/src/error.rs:214-220 method session <-> ./crates/ccswarm/src/error.rs:309-314 method user_error_with_suggestion
  Similarity: 87.89%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:session (lines 214-220) ---[0m
    pub fn session<S: Into<String>, I: Into<String>>(session_id: I, message: S) -> Self {
        Self::Session {
            session_id: session_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:user_error_with_suggestion (lines 309-314) ---[0m
    pub fn user_error_with_suggestion<S: Into<String>, T: Into<String>>(message: S, suggestion: T) -> Self {
        Self::UserError {
            message: message.into(),
            suggestion: Some(suggestion.into()),
        }
    }

  ./crates/ccswarm/src/error.rs:223-229 method task <-> ./crates/ccswarm/src/error.rs:309-314 method user_error_with_suggestion
  Similarity: 87.89%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:task (lines 223-229) ---[0m
    pub fn task<S: Into<String>, I: Into<String>>(task_id: I, message: S) -> Self {
        Self::Task {
            task_id: task_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:user_error_with_suggestion (lines 309-314) ---[0m
    pub fn user_error_with_suggestion<S: Into<String>, T: Into<String>>(message: S, suggestion: T) -> Self {
        Self::UserError {
            message: message.into(),
            suggestion: Some(suggestion.into()),
        }
    }

  ./crates/ccswarm/src/error.rs:248-254 method template <-> ./crates/ccswarm/src/error.rs:309-314 method user_error_with_suggestion
  Similarity: 87.89%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:template (lines 248-254) ---[0m
    pub fn template<S: Into<String>>(message: S) -> Self {
        Self::Template {
            message: message.into(),
            template_name: None,
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:user_error_with_suggestion (lines 309-314) ---[0m
    pub fn user_error_with_suggestion<S: Into<String>, T: Into<String>>(message: S, suggestion: T) -> Self {
        Self::UserError {
            message: message.into(),
            suggestion: Some(suggestion.into()),
        }
    }

  ./crates/ccswarm/src/error.rs:266-272 method extension <-> ./crates/ccswarm/src/error.rs:309-314 method user_error_with_suggestion
  Similarity: 87.89%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:extension (lines 266-272) ---[0m
    pub fn extension<S: Into<String>, I: Into<String>>(extension_id: I, message: S) -> Self {
        Self::Extension {
            extension_id: extension_id.into(),
            message: message.into(),
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:user_error_with_suggestion (lines 309-314) ---[0m
    pub fn user_error_with_suggestion<S: Into<String>, T: Into<String>>(message: S, suggestion: T) -> Self {
        Self::UserError {
            message: message.into(),
            suggestion: Some(suggestion.into()),
        }
    }

  ./crates/ccswarm/src/error.rs:275-281 method resource <-> ./crates/ccswarm/src/error.rs:309-314 method user_error_with_suggestion
  Similarity: 87.89%
  Classes: CCSwarmError <-> CCSwarmError

[36m--- ./crates/ccswarm/src/error.rs:resource (lines 275-281) ---[0m
    pub fn resource<S: Into<String>>(message: S) -> Self {
        Self::Resource {
            message: message.into(),
            resource_type: None,
            source: None,
        }
    }

[36m--- ./crates/ccswarm/src/error.rs:user_error_with_suggestion (lines 309-314) ---[0m
    pub fn user_error_with_suggestion<S: Into<String>, T: Into<String>>(message: S, suggestion: T) -> Self {
        Self::UserError {
            message: message.into(),
            suggestion: Some(suggestion.into()),
        }
    }


Duplicates in ./crates/ccswarm/src/utils/test_helpers.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/utils/test_helpers.rs:20-24 method cleanup_test_workspace <-> ./crates/ccswarm/src/utils/test_helpers.rs:34-39 method new
  Similarity: 82.45%
  Classes: TestSetup <-> TestContext

[36m--- ./crates/ccswarm/src/utils/test_helpers.rs:cleanup_test_workspace (lines 20-24) ---[0m
    pub async fn cleanup_test_workspace(path: &str) {
        if std::path::Path::new(path).exists() {
            let _ = tokio::fs::remove_dir_all(path).await;
        }
    }

[36m--- ./crates/ccswarm/src/utils/test_helpers.rs:new (lines 34-39) ---[0m
    pub fn new(workspace: String) -> Self {
        Self {
            tasks: Arc::new(Mutex::new(Vec::new())),
            workspace,
        }
    }

  ./crates/ccswarm/src/utils/test_helpers.rs:10-12 method create_test_task <-> ./crates/ccswarm/src/utils/test_helpers.rs:34-39 method new
  Similarity: 74.05%
  Classes: TestSetup <-> TestContext

[36m--- ./crates/ccswarm/src/utils/test_helpers.rs:create_test_task (lines 10-12) ---[0m
    pub fn create_test_task(id: &str, description: &str, priority: Priority) -> Task {
        Task::new(id.to_string(), description.to_string(), priority, TaskType::Development)
    }

[36m--- ./crates/ccswarm/src/utils/test_helpers.rs:new (lines 34-39) ---[0m
    pub fn new(workspace: String) -> Self {
        Self {
            tasks: Arc::new(Mutex::new(Vec::new())),
            workspace,
        }
    }


Duplicates in ./crates/ccswarm/src/subagent/converter.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/subagent/converter.rs:243-271 method save_subagent_definition <-> ./crates/ccswarm/src/subagent/converter.rs:274-299 method batch_convert_project
  Similarity: 82.26%
  Classes: AgentConverter <-> AgentConverter

[36m--- ./crates/ccswarm/src/subagent/converter.rs:save_subagent_definition (lines 243-271) ---[0m
    pub fn save_subagent_definition(
        definition: &SubagentDefinition,
        instructions: &str,
        output_dir: &Path,
    ) -> SubagentResult<()> {
        // Create the output directory if it doesn't exist
        fs::create_dir_all(output_dir)?;

        // Generate the filename
        let filename = format!("{}.md", definition.name);
        let filepath = output_dir.join(filename);

        // Serialize the definition to YAML frontmatter
        let frontmatter = serde_yaml::to_string(definition).map_err(|e| {
            SubagentError::Serialization(serde_json::Error::io(std::io::Error::other(
                e.to_string(),
            )))
        })?;

        // Combine frontmatter and instructions
        let content = format!("---\n{}---\n\n{}", frontmatter, instructions);

        // Write to file
        fs::write(&filepath, content)?;

        log::info!("Saved subagent definition to {:?}", filepath);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/subagent/converter.rs:batch_convert_project (lines 274-299) ---[0m
    pub fn batch_convert_project(
        project_config_path: &Path,
        output_dir: &Path,
    ) -> SubagentResult<Vec<String>> {
        // Read the project configuration
        let config_content = fs::read_to_string(project_config_path)?;
        let project_config: serde_json::Value = serde_json::from_str(&config_content)?;

        let mut converted = Vec::new();

        // Extract agents array
        if let Some(agents) = project_config.get("agents").and_then(|a| a.as_array()) {
            for agent_value in agents {
                if let Ok(agent_config) = serde_json::from_value::<AgentConfig>(agent_value.clone())
                {
                    let (definition, instructions) = Self::convert_agent_config(&agent_config)?;
                    Self::save_subagent_definition(&definition, &instructions, output_dir)?;
                    converted.push(definition.name);
                }
            }
        }

        log::info!("Converted {} agents to subagent format", converted.len());

        Ok(converted)
    }

  ./crates/ccswarm/src/subagent/converter.rs:17-35 method convert_agent_config <-> ./crates/ccswarm/src/subagent/converter.rs:274-299 method batch_convert_project
  Similarity: 83.66%
  Classes: AgentConverter <-> AgentConverter

[36m--- ./crates/ccswarm/src/subagent/converter.rs:convert_agent_config (lines 17-35) ---[0m
    pub fn convert_agent_config(
        config: &AgentConfig,
    ) -> SubagentResult<(SubagentDefinition, String)> {
        // Determine role from specialization
        let role = Self::role_from_specialization(&config.specialization);
        let (tools, capabilities) = Self::extract_tools_and_capabilities(&role);

        let definition = SubagentDefinition {
            name: config.specialization.clone(),
            description: Self::generate_description(&role),
            tools,
            capabilities,
            metadata: Self::extract_metadata(config),
        };

        let instructions = Self::generate_instructions(&role, &config.specialization);

        Ok((definition, instructions))
    }

[36m--- ./crates/ccswarm/src/subagent/converter.rs:batch_convert_project (lines 274-299) ---[0m
    pub fn batch_convert_project(
        project_config_path: &Path,
        output_dir: &Path,
    ) -> SubagentResult<Vec<String>> {
        // Read the project configuration
        let config_content = fs::read_to_string(project_config_path)?;
        let project_config: serde_json::Value = serde_json::from_str(&config_content)?;

        let mut converted = Vec::new();

        // Extract agents array
        if let Some(agents) = project_config.get("agents").and_then(|a| a.as_array()) {
            for agent_value in agents {
                if let Ok(agent_config) = serde_json::from_value::<AgentConfig>(agent_value.clone())
                {
                    let (definition, instructions) = Self::convert_agent_config(&agent_config)?;
                    Self::save_subagent_definition(&definition, &instructions, output_dir)?;
                    converted.push(definition.name);
                }
            }
        }

        log::info!("Converted {} agents to subagent format", converted.len());

        Ok(converted)
    }

  ./crates/ccswarm/src/subagent/converter.rs:274-299 method batch_convert_project <-> ./crates/ccswarm/src/subagent/converter.rs:327-345 function test_convert_backend_agent
  Similarity: 82.00%

[36m--- ./crates/ccswarm/src/subagent/converter.rs:batch_convert_project (lines 274-299) ---[0m
    pub fn batch_convert_project(
        project_config_path: &Path,
        output_dir: &Path,
    ) -> SubagentResult<Vec<String>> {
        // Read the project configuration
        let config_content = fs::read_to_string(project_config_path)?;
        let project_config: serde_json::Value = serde_json::from_str(&config_content)?;

        let mut converted = Vec::new();

        // Extract agents array
        if let Some(agents) = project_config.get("agents").and_then(|a| a.as_array()) {
            for agent_value in agents {
                if let Ok(agent_config) = serde_json::from_value::<AgentConfig>(agent_value.clone())
                {
                    let (definition, instructions) = Self::convert_agent_config(&agent_config)?;
                    Self::save_subagent_definition(&definition, &instructions, output_dir)?;
                    converted.push(definition.name);
                }
            }
        }

        log::info!("Converted {} agents to subagent format", converted.len());

        Ok(converted)
    }

[36m--- ./crates/ccswarm/src/subagent/converter.rs:test_convert_backend_agent (lines 327-345) ---[0m
    fn test_convert_backend_agent() {
        let config = AgentConfig {
            specialization: "backend-specialist".to_string(),
            worktree: "agents/backend".to_string(),
            branch: "feature/backend".to_string(),
            claude_config: ClaudeConfig::default(),
            claude_md_template: "backend.md".to_string(),
        };

        let result = AgentConverter::convert_agent_config(&config);
        assert!(result.is_ok());

        let (definition, _instructions) = result.unwrap();
        assert!(definition
            .tools
            .standard
            .contains(&"database_query".to_string()));
        assert!(!definition.tools.semantic.is_empty());
    }

  ./crates/ccswarm/src/subagent/converter.rs:274-299 method batch_convert_project <-> ./crates/ccswarm/src/subagent/converter.rs:308-324 function test_convert_frontend_agent
  Similarity: 84.85%

[36m--- ./crates/ccswarm/src/subagent/converter.rs:batch_convert_project (lines 274-299) ---[0m
    pub fn batch_convert_project(
        project_config_path: &Path,
        output_dir: &Path,
    ) -> SubagentResult<Vec<String>> {
        // Read the project configuration
        let config_content = fs::read_to_string(project_config_path)?;
        let project_config: serde_json::Value = serde_json::from_str(&config_content)?;

        let mut converted = Vec::new();

        // Extract agents array
        if let Some(agents) = project_config.get("agents").and_then(|a| a.as_array()) {
            for agent_value in agents {
                if let Ok(agent_config) = serde_json::from_value::<AgentConfig>(agent_value.clone())
                {
                    let (definition, instructions) = Self::convert_agent_config(&agent_config)?;
                    Self::save_subagent_definition(&definition, &instructions, output_dir)?;
                    converted.push(definition.name);
                }
            }
        }

        log::info!("Converted {} agents to subagent format", converted.len());

        Ok(converted)
    }

[36m--- ./crates/ccswarm/src/subagent/converter.rs:test_convert_frontend_agent (lines 308-324) ---[0m
    fn test_convert_frontend_agent() {
        let config = AgentConfig {
            specialization: "frontend-specialist".to_string(),
            worktree: "agents/frontend".to_string(),
            branch: "feature/frontend".to_string(),
            claude_config: ClaudeConfig::default(),
            claude_md_template: "frontend.md".to_string(),
        };

        let result = AgentConverter::convert_agent_config(&config);
        assert!(result.is_ok());

        let (definition, _instructions) = result.unwrap();
        assert_eq!(definition.name, "frontend-specialist");
        assert!(!definition.tools.standard.is_empty());
        assert!(!definition.capabilities.is_empty());
    }

  ./crates/ccswarm/src/subagent/converter.rs:17-35 method convert_agent_config <-> ./crates/ccswarm/src/subagent/converter.rs:243-271 method save_subagent_definition
  Similarity: 73.27%
  Classes: AgentConverter <-> AgentConverter

[36m--- ./crates/ccswarm/src/subagent/converter.rs:convert_agent_config (lines 17-35) ---[0m
    pub fn convert_agent_config(
        config: &AgentConfig,
    ) -> SubagentResult<(SubagentDefinition, String)> {
        // Determine role from specialization
        let role = Self::role_from_specialization(&config.specialization);
        let (tools, capabilities) = Self::extract_tools_and_capabilities(&role);

        let definition = SubagentDefinition {
            name: config.specialization.clone(),
            description: Self::generate_description(&role),
            tools,
            capabilities,
            metadata: Self::extract_metadata(config),
        };

        let instructions = Self::generate_instructions(&role, &config.specialization);

        Ok((definition, instructions))
    }

[36m--- ./crates/ccswarm/src/subagent/converter.rs:save_subagent_definition (lines 243-271) ---[0m
    pub fn save_subagent_definition(
        definition: &SubagentDefinition,
        instructions: &str,
        output_dir: &Path,
    ) -> SubagentResult<()> {
        // Create the output directory if it doesn't exist
        fs::create_dir_all(output_dir)?;

        // Generate the filename
        let filename = format!("{}.md", definition.name);
        let filepath = output_dir.join(filename);

        // Serialize the definition to YAML frontmatter
        let frontmatter = serde_yaml::to_string(definition).map_err(|e| {
            SubagentError::Serialization(serde_json::Error::io(std::io::Error::other(
                e.to_string(),
            )))
        })?;

        // Combine frontmatter and instructions
        let content = format!("---\n{}---\n\n{}", frontmatter, instructions);

        // Write to file
        fs::write(&filepath, content)?;

        log::info!("Saved subagent definition to {:?}", filepath);

        Ok(())
    }

  ./crates/ccswarm/src/subagent/converter.rs:243-271 method save_subagent_definition <-> ./crates/ccswarm/src/subagent/converter.rs:308-324 function test_convert_frontend_agent
  Similarity: 72.55%

[36m--- ./crates/ccswarm/src/subagent/converter.rs:save_subagent_definition (lines 243-271) ---[0m
    pub fn save_subagent_definition(
        definition: &SubagentDefinition,
        instructions: &str,
        output_dir: &Path,
    ) -> SubagentResult<()> {
        // Create the output directory if it doesn't exist
        fs::create_dir_all(output_dir)?;

        // Generate the filename
        let filename = format!("{}.md", definition.name);
        let filepath = output_dir.join(filename);

        // Serialize the definition to YAML frontmatter
        let frontmatter = serde_yaml::to_string(definition).map_err(|e| {
            SubagentError::Serialization(serde_json::Error::io(std::io::Error::other(
                e.to_string(),
            )))
        })?;

        // Combine frontmatter and instructions
        let content = format!("---\n{}---\n\n{}", frontmatter, instructions);

        // Write to file
        fs::write(&filepath, content)?;

        log::info!("Saved subagent definition to {:?}", filepath);

        Ok(())
    }

[36m--- ./crates/ccswarm/src/subagent/converter.rs:test_convert_frontend_agent (lines 308-324) ---[0m
    fn test_convert_frontend_agent() {
        let config = AgentConfig {
            specialization: "frontend-specialist".to_string(),
            worktree: "agents/frontend".to_string(),
            branch: "feature/frontend".to_string(),
            claude_config: ClaudeConfig::default(),
            claude_md_template: "frontend.md".to_string(),
        };

        let result = AgentConverter::convert_agent_config(&config);
        assert!(result.is_ok());

        let (definition, _instructions) = result.unwrap();
        assert_eq!(definition.name, "frontend-specialist");
        assert!(!definition.tools.standard.is_empty());
        assert!(!definition.capabilities.is_empty());
    }

  ./crates/ccswarm/src/subagent/converter.rs:190-203 method extract_metadata <-> ./crates/ccswarm/src/subagent/converter.rs:274-299 method batch_convert_project
  Similarity: 82.26%
  Classes: AgentConverter <-> AgentConverter

[36m--- ./crates/ccswarm/src/subagent/converter.rs:extract_metadata (lines 190-203) ---[0m
    fn extract_metadata(config: &AgentConfig) -> HashMap<String, serde_json::Value> {
        let mut metadata = HashMap::new();

        metadata.insert(
            "specialization".to_string(),
            serde_json::json!(config.specialization),
        );

        metadata.insert("worktree".to_string(), serde_json::json!(config.worktree));

        metadata.insert("branch".to_string(), serde_json::json!(config.branch));

        metadata
    }

[36m--- ./crates/ccswarm/src/subagent/converter.rs:batch_convert_project (lines 274-299) ---[0m
    pub fn batch_convert_project(
        project_config_path: &Path,
        output_dir: &Path,
    ) -> SubagentResult<Vec<String>> {
        // Read the project configuration
        let config_content = fs::read_to_string(project_config_path)?;
        let project_config: serde_json::Value = serde_json::from_str(&config_content)?;

        let mut converted = Vec::new();

        // Extract agents array
        if let Some(agents) = project_config.get("agents").and_then(|a| a.as_array()) {
            for agent_value in agents {
                if let Ok(agent_config) = serde_json::from_value::<AgentConfig>(agent_value.clone())
                {
                    let (definition, instructions) = Self::convert_agent_config(&agent_config)?;
                    Self::save_subagent_definition(&definition, &instructions, output_dir)?;
                    converted.push(definition.name);
                }
            }
        }

        log::info!("Converted {} agents to subagent format", converted.len());

        Ok(converted)
    }

  ./crates/ccswarm/src/subagent/converter.rs:17-35 method convert_agent_config <-> ./crates/ccswarm/src/subagent/converter.rs:327-345 function test_convert_backend_agent
  Similarity: 86.12%

[36m--- ./crates/ccswarm/src/subagent/converter.rs:convert_agent_config (lines 17-35) ---[0m
    pub fn convert_agent_config(
        config: &AgentConfig,
    ) -> SubagentResult<(SubagentDefinition, String)> {
        // Determine role from specialization
        let role = Self::role_from_specialization(&config.specialization);
        let (tools, capabilities) = Self::extract_tools_and_capabilities(&role);

        let definition = SubagentDefinition {
            name: config.specialization.clone(),
            description: Self::generate_description(&role),
            tools,
            capabilities,
            metadata: Self::extract_metadata(config),
        };

        let instructions = Self::generate_instructions(&role, &config.specialization);

        Ok((definition, instructions))
    }

[36m--- ./crates/ccswarm/src/subagent/converter.rs:test_convert_backend_agent (lines 327-345) ---[0m
    fn test_convert_backend_agent() {
        let config = AgentConfig {
            specialization: "backend-specialist".to_string(),
            worktree: "agents/backend".to_string(),
            branch: "feature/backend".to_string(),
            claude_config: ClaudeConfig::default(),
            claude_md_template: "backend.md".to_string(),
        };

        let result = AgentConverter::convert_agent_config(&config);
        assert!(result.is_ok());

        let (definition, _instructions) = result.unwrap();
        assert!(definition
            .tools
            .standard
            .contains(&"database_query".to_string()));
        assert!(!definition.tools.semantic.is_empty());
    }

  ./crates/ccswarm/src/subagent/converter.rs:308-324 function test_convert_frontend_agent <-> ./crates/ccswarm/src/subagent/converter.rs:327-345 function test_convert_backend_agent
  Similarity: 86.99%

[36m--- ./crates/ccswarm/src/subagent/converter.rs:test_convert_frontend_agent (lines 308-324) ---[0m
    fn test_convert_frontend_agent() {
        let config = AgentConfig {
            specialization: "frontend-specialist".to_string(),
            worktree: "agents/frontend".to_string(),
            branch: "feature/frontend".to_string(),
            claude_config: ClaudeConfig::default(),
            claude_md_template: "frontend.md".to_string(),
        };

        let result = AgentConverter::convert_agent_config(&config);
        assert!(result.is_ok());

        let (definition, _instructions) = result.unwrap();
        assert_eq!(definition.name, "frontend-specialist");
        assert!(!definition.tools.standard.is_empty());
        assert!(!definition.capabilities.is_empty());
    }

[36m--- ./crates/ccswarm/src/subagent/converter.rs:test_convert_backend_agent (lines 327-345) ---[0m
    fn test_convert_backend_agent() {
        let config = AgentConfig {
            specialization: "backend-specialist".to_string(),
            worktree: "agents/backend".to_string(),
            branch: "feature/backend".to_string(),
            claude_config: ClaudeConfig::default(),
            claude_md_template: "backend.md".to_string(),
        };

        let result = AgentConverter::convert_agent_config(&config);
        assert!(result.is_ok());

        let (definition, _instructions) = result.unwrap();
        assert!(definition
            .tools
            .standard
            .contains(&"database_query".to_string()));
        assert!(!definition.tools.semantic.is_empty());
    }

  ./crates/ccswarm/src/subagent/converter.rs:17-35 method convert_agent_config <-> ./crates/ccswarm/src/subagent/converter.rs:308-324 function test_convert_frontend_agent
  Similarity: 81.09%

[36m--- ./crates/ccswarm/src/subagent/converter.rs:convert_agent_config (lines 17-35) ---[0m
    pub fn convert_agent_config(
        config: &AgentConfig,
    ) -> SubagentResult<(SubagentDefinition, String)> {
        // Determine role from specialization
        let role = Self::role_from_specialization(&config.specialization);
        let (tools, capabilities) = Self::extract_tools_and_capabilities(&role);

        let definition = SubagentDefinition {
            name: config.specialization.clone(),
            description: Self::generate_description(&role),
            tools,
            capabilities,
            metadata: Self::extract_metadata(config),
        };

        let instructions = Self::generate_instructions(&role, &config.specialization);

        Ok((definition, instructions))
    }

[36m--- ./crates/ccswarm/src/subagent/converter.rs:test_convert_frontend_agent (lines 308-324) ---[0m
    fn test_convert_frontend_agent() {
        let config = AgentConfig {
            specialization: "frontend-specialist".to_string(),
            worktree: "agents/frontend".to_string(),
            branch: "feature/frontend".to_string(),
            claude_config: ClaudeConfig::default(),
            claude_md_template: "frontend.md".to_string(),
        };

        let result = AgentConverter::convert_agent_config(&config);
        assert!(result.is_ok());

        let (definition, _instructions) = result.unwrap();
        assert_eq!(definition.name, "frontend-specialist");
        assert!(!definition.tools.standard.is_empty());
        assert!(!definition.capabilities.is_empty());
    }

  ./crates/ccswarm/src/subagent/converter.rs:17-35 method convert_agent_config <-> ./crates/ccswarm/src/subagent/converter.rs:190-203 method extract_metadata
  Similarity: 80.29%
  Classes: AgentConverter <-> AgentConverter

[36m--- ./crates/ccswarm/src/subagent/converter.rs:convert_agent_config (lines 17-35) ---[0m
    pub fn convert_agent_config(
        config: &AgentConfig,
    ) -> SubagentResult<(SubagentDefinition, String)> {
        // Determine role from specialization
        let role = Self::role_from_specialization(&config.specialization);
        let (tools, capabilities) = Self::extract_tools_and_capabilities(&role);

        let definition = SubagentDefinition {
            name: config.specialization.clone(),
            description: Self::generate_description(&role),
            tools,
            capabilities,
            metadata: Self::extract_metadata(config),
        };

        let instructions = Self::generate_instructions(&role, &config.specialization);

        Ok((definition, instructions))
    }

[36m--- ./crates/ccswarm/src/subagent/converter.rs:extract_metadata (lines 190-203) ---[0m
    fn extract_metadata(config: &AgentConfig) -> HashMap<String, serde_json::Value> {
        let mut metadata = HashMap::new();

        metadata.insert(
            "specialization".to_string(),
            serde_json::json!(config.specialization),
        );

        metadata.insert("worktree".to_string(), serde_json::json!(config.worktree));

        metadata.insert("branch".to_string(), serde_json::json!(config.branch));

        metadata
    }

  ./crates/ccswarm/src/subagent/converter.rs:190-203 method extract_metadata <-> ./crates/ccswarm/src/subagent/converter.rs:327-345 function test_convert_backend_agent
  Similarity: 75.06%

[36m--- ./crates/ccswarm/src/subagent/converter.rs:extract_metadata (lines 190-203) ---[0m
    fn extract_metadata(config: &AgentConfig) -> HashMap<String, serde_json::Value> {
        let mut metadata = HashMap::new();

        metadata.insert(
            "specialization".to_string(),
            serde_json::json!(config.specialization),
        );

        metadata.insert("worktree".to_string(), serde_json::json!(config.worktree));

        metadata.insert("branch".to_string(), serde_json::json!(config.branch));

        metadata
    }

[36m--- ./crates/ccswarm/src/subagent/converter.rs:test_convert_backend_agent (lines 327-345) ---[0m
    fn test_convert_backend_agent() {
        let config = AgentConfig {
            specialization: "backend-specialist".to_string(),
            worktree: "agents/backend".to_string(),
            branch: "feature/backend".to_string(),
            claude_config: ClaudeConfig::default(),
            claude_md_template: "backend.md".to_string(),
        };

        let result = AgentConverter::convert_agent_config(&config);
        assert!(result.is_ok());

        let (definition, _instructions) = result.unwrap();
        assert!(definition
            .tools
            .standard
            .contains(&"database_query".to_string()));
        assert!(!definition.tools.semantic.is_empty());
    }

  ./crates/ccswarm/src/subagent/converter.rs:158-174 method generate_description <-> ./crates/ccswarm/src/subagent/converter.rs:177-187 method generate_instructions
  Similarity: 72.57%
  Classes: AgentConverter <-> AgentConverter

[36m--- ./crates/ccswarm/src/subagent/converter.rs:generate_description (lines 158-174) ---[0m
    fn generate_description(role: &AgentRole) -> String {
        match role {
            AgentRole::Frontend { .. } => {
                "Frontend development specialist with expertise in modern web frameworks and UI/UX implementation.".to_string()
            }
            AgentRole::Backend { .. } => {
                "Backend development specialist focused on API design, database optimization, and business logic.".to_string()
            }
            AgentRole::DevOps { .. } => {
                "DevOps specialist handling containerization, CI/CD, and infrastructure automation.".to_string()
            }
            AgentRole::QA { .. } => {
                "Quality assurance specialist ensuring code quality through comprehensive testing and analysis.".to_string()
            }
            _ => "General purpose agent for various development tasks.".to_string(),
        }
    }

[36m--- ./crates/ccswarm/src/subagent/converter.rs:generate_instructions (lines 177-187) ---[0m
    fn generate_instructions(role: &AgentRole, name: &str) -> String {
        let role_specific = match role {
            AgentRole::Frontend { .. } => include_str!("../../templates/frontend_instructions.md"),
            AgentRole::Backend { .. } => include_str!("../../templates/backend_instructions.md"),
            AgentRole::DevOps { .. } => include_str!("../../templates/devops_instructions.md"),
            AgentRole::QA { .. } => include_str!("../../templates/qa_instructions.md"),
            _ => include_str!("../../templates/general_instructions.md"),
        };

        format!("# {} Agent Instructions\n\n{}", name, role_specific)
    }


Duplicates in ./crates/ccswarm/src/agent/phronesis.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/agent/phronesis.rs:34-39 method get_relevant_wisdom <-> ./crates/ccswarm/src/agent/phronesis.rs:124-134 method new
  Similarity: 92.83%
  Classes: Phronesis <-> LearningOutcome

[36m--- ./crates/ccswarm/src/agent/phronesis.rs:get_relevant_wisdom (lines 34-39) ---[0m
    pub fn get_relevant_wisdom(&self, context: &str) -> Vec<&WisdomPoint> {
        self.wisdom_points
            .iter()
            .filter(|w| w.context.contains(context))
            .collect()
    }

[36m--- ./crates/ccswarm/src/agent/phronesis.rs:new (lines 124-134) ---[0m
    pub fn new(success: bool) -> Self {
        Self {
            success,
            insights: Vec::new(),
            improvements: Vec::new(),
            confidence: 0.5,
            lesson_learned: None,
            actionable_insight: None,
            applicable_situations: Vec::new(),
        }
    }

  ./crates/ccswarm/src/agent/phronesis.rs:25-32 method add_wisdom <-> ./crates/ccswarm/src/agent/phronesis.rs:124-134 method new
  Similarity: 81.17%
  Classes: Phronesis <-> LearningOutcome

[36m--- ./crates/ccswarm/src/agent/phronesis.rs:add_wisdom (lines 25-32) ---[0m
    pub fn add_wisdom(&mut self, context: String, lesson: String, confidence: f32) {
        self.wisdom_points.push(WisdomPoint {
            context,
            lesson,
            confidence,
        });
        self.experience_level += 1;
    }

[36m--- ./crates/ccswarm/src/agent/phronesis.rs:new (lines 124-134) ---[0m
    pub fn new(success: bool) -> Self {
        Self {
            success,
            insights: Vec::new(),
            improvements: Vec::new(),
            confidence: 0.5,
            lesson_learned: None,
            actionable_insight: None,
            applicable_situations: Vec::new(),
        }
    }


Duplicates in ./crates/ccswarm/src/agent/pool.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/agent/pool.rs:347-466 method analyze_task <-> ./crates/ccswarm/src/agent/pool.rs:637-688 method execute_parallel_step
  Similarity: 87.89%
  Classes: AgentOrchestrator <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:analyze_task (lines 347-466) ---[0m
    async fn analyze_task(&self, task: &Task) -> Result<TaskPlan> {
        let mut plan = TaskPlan::new(task.id.clone());

        // Pool-specific orchestration strategies
        match task.task_type {
            TaskType::Development => {
                // Multi-agent development workflow
                plan.add_step(
                    TaskStep::new(
                        "pool_agent_selection".to_string(),
                        "Select optimal agents for task".to_string(),
                        StepType::Analysis,
                    )
                    .with_description(
                        "Analyze available agents and select best ones for development task"
                            .to_string(),
                    ),
                );
                plan.add_step(
                    TaskStep::new(
                        "parallel_implementation".to_string(),
                        "Parallel implementation".to_string(),
                        StepType::Execution,
                    )
                    .with_description("Execute implementation across multiple agents".to_string()),
                );
                plan.add_step(
                    TaskStep::new(
                        "integration_test".to_string(),
                        "Test agent integration".to_string(),
                        StepType::Validation,
                    )
                    .with_description("Verify integration between agents".to_string()),
                );
            }
            TaskType::Testing => {
                // Distributed testing across agents
                plan.add_step(
                    TaskStep::new(
                        "test_planning".to_string(),
                        "Plan distributed test execution".to_string(),
                        StepType::Analysis,
                    )
                    .with_description("Plan test distribution across agents".to_string()),
                );
                plan.add_step(
                    TaskStep::new(
                        "parallel_testing".to_string(),
                        "Execute parallel tests".to_string(),
                        StepType::Execution,
                    )
                    .with_description("Run tests in parallel across agents".to_string()),
                );
                plan.add_step(
                    TaskStep::new(
                        "test_aggregation".to_string(),
                        "Aggregate test results".to_string(),
                        StepType::Validation,
                    )
                    .with_description("Collect and analyze test results".to_string()),
                );
            }
            TaskType::Infrastructure => {
                // DevOps coordination with other agents
                plan.add_step(
                    TaskStep::new(
                        "infra_planning".to_string(),
                        "Plan infrastructure changes".to_string(),
                        StepType::Analysis,
                    )
                    .with_description("Analyze infrastructure requirements".to_string()),
                );
                plan.add_step(
                    TaskStep::new(
                        "infra_implementation".to_string(),
                        "Deploy infrastructure".to_string(),
                        StepType::Execution,
                    )
                    .with_description("Execute infrastructure deployment".to_string()),
                );
                plan.add_step(
                    TaskStep::new(
                        "infra_validation".to_string(),
                        "Validate deployment".to_string(),
                        StepType::Validation,
                    )
                    .with_description("Verify infrastructure is working".to_string()),
                );
            }
            _ => {
                // Default pool workflow
                plan.add_step(
                    TaskStep::new(
                        "pool_analysis".to_string(),
                        "Analyze task for pool execution".to_string(),
                        StepType::Analysis,
                    )
                    .with_description("Determine best pool execution strategy".to_string()),
                );
                plan.add_step(
                    TaskStep::new(
                        "pool_execution".to_string(),
                        "Execute with best agent".to_string(),
                        StepType::Execution,
                    )
                    .with_description("Execute task with selected agent".to_string()),
                );
                plan.add_step(
                    TaskStep::new(
                        "pool_validation".to_string(),
                        "Validate pool execution".to_string(),
                        StepType::Validation,
                    )
                    .with_description("Verify task completion".to_string()),
                );
            }
        }

        Ok(plan)
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_parallel_step (lines 637-688) ---[0m
    async fn execute_parallel_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let mut results = Vec::new();

        // Get available agents
        let available_agents: Vec<_> = self
            .agents
            .iter()
            .map(|entry| {
                let agent_type = entry.key().clone();
                agent_type
            })
            .collect();

        if available_agents.is_empty() {
            return Ok(StepResult::new(step.id.clone())
                .failed("No available agents for parallel execution".to_string()));
        }

        // Execute on each available agent
        for agent_type in available_agents {
            let task = Task::new(
                format!("parallel_{}_{}", step.id, agent_type),
                format!("{} ({})", step.description, agent_type),
                crate::agent::Priority::Medium,
                TaskType::Development,
            );

            match self.execute_task_with_agent(&agent_type, &task).await {
                Ok(result) => results.push(result),
                Err(e) => {
                    warn!("Parallel execution failed for agent {}: {}", agent_type, e);
                    results.push(TaskResult::failure(e.to_string(), Duration::from_secs(0)));
                }
            }
        }

        let success_count = results.iter().filter(|r| r.success).count();
        let _success = success_count > 0; // At least one agent succeeded

        Ok(StepResult::new(step.id.clone())
            .with_summary(format!(
                "Parallel execution: {}/{} agents succeeded",
                success_count,
                results.len()
            ))
            .add_output("parallel_results".to_string(), results.len().to_string())
            .add_output("success_count".to_string(), success_count.to_string()))
    }

  ./crates/ccswarm/src/agent/pool.rs:66-116 method spawn_agent <-> ./crates/ccswarm/src/agent/pool.rs:142-214 method execute_task_with_agent
  Similarity: 90.88%
  Classes: AgentPool <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:spawn_agent (lines 66-116) ---[0m
    pub async fn spawn_agent(&mut self, agent_type: &str, config: &CcswarmConfig) -> Result<()> {
        info!("🚀 Spawning {} agent", agent_type);

        // Get role for agent type
        let role = match agent_type.to_lowercase().as_str() {
            "frontend" => default_frontend_role(),
            "backend" => default_backend_role(),
            "devops" => default_devops_role(),
            "qa" => default_qa_role(),
            _ => return Err(anyhow::anyhow!("Unknown agent type: {}", agent_type)),
        };

        // Get agent config
        let agent_config = config
            .agents
            .get(agent_type)
            .ok_or_else(|| anyhow::anyhow!("No configuration for agent: {}", agent_type))?;

        // Create agent
        let mut agent = ClaudeCodeAgent::new(
            role,
            &PathBuf::from(&config.project.name),
            &agent_config.branch,
            agent_config.claude_config.clone(),
        )
        .await?;

        // Initialize agent
        agent.initialize().await?;

        // Create persistent session for agent
        let mut session = PersistentClaudeSession::new(
            agent.identity.clone(),
            agent.worktree_path.clone(),
            agent_config.claude_config.clone(),
        )
        .await?;

        // Initialize session
        session.initialize().await?;

        // Store agent and session
        self.agents
            .insert(agent_type.to_string(), Arc::new(RwLock::new(agent)));

        self.sessions
            .insert(agent_type.to_string(), Arc::new(RwLock::new(session)));

        info!("✅ {} agent spawned and initialized", agent_type);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_task_with_agent (lines 142-214) ---[0m
    pub async fn execute_task_with_agent(
        &self,
        agent_type: &str,
        task: &Task,
    ) -> Result<TaskResult> {
        info!(
            "📋 Executing task with {} agent: {}",
            agent_type, task.description
        );

        // Get agent and session
        let agent = self.get_agent(agent_type)?;
        let session = self
            .sessions
            .get(agent_type)
            .ok_or_else(|| anyhow::anyhow!("No session for agent: {}", agent_type))?;

        // Record execution start
        let record = TaskExecutionRecord {
            task_id: task.id.clone(),
            agent_id: agent_type.to_string(),
            started_at: chrono::Utc::now(),
            completed_at: None,
            result: None,
        };

        self.execution_history.write().await.push(record.clone());

        // Update agent status
        {
            let mut agent_guard = agent.write().await;
            agent_guard.status = AgentStatus::Working;
            agent_guard.current_task = Some(task.clone());
            agent_guard.last_activity = chrono::Utc::now();
        }

        // Execute task with session
        let result = {
            let mut session_guard = session.write().await;
            session_guard.execute_task(task.clone()).await?
        };

        // Update execution record
        let mut history = self.execution_history.write().await;
        if let Some(record) = history.iter_mut().find(|r| r.task_id == task.id) {
            record.completed_at = Some(chrono::Utc::now());
            record.result = Some(result.clone());
        }

        // Update agent status
        {
            let mut agent_guard = agent.write().await;
            agent_guard.status = AgentStatus::Available;
            agent_guard.current_task = None;
            agent_guard.last_activity = chrono::Utc::now();

            // Add to task history
            agent_guard
                .task_history
                .push((task.clone(), result.clone()));
        }

        // Send completion message
        self.coordination_bus
            .send_message(AgentMessage::TaskCompleted {
                agent_id: agent_type.to_string(),
                task_id: task.id.clone(),
                result: result.clone(),
            })
            .await?;

        Ok(result)
    }

  ./crates/ccswarm/src/agent/pool.rs:142-214 method execute_task_with_agent <-> ./crates/ccswarm/src/agent/pool.rs:637-688 method execute_parallel_step
  Similarity: 85.57%
  Classes: AgentPool <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_task_with_agent (lines 142-214) ---[0m
    pub async fn execute_task_with_agent(
        &self,
        agent_type: &str,
        task: &Task,
    ) -> Result<TaskResult> {
        info!(
            "📋 Executing task with {} agent: {}",
            agent_type, task.description
        );

        // Get agent and session
        let agent = self.get_agent(agent_type)?;
        let session = self
            .sessions
            .get(agent_type)
            .ok_or_else(|| anyhow::anyhow!("No session for agent: {}", agent_type))?;

        // Record execution start
        let record = TaskExecutionRecord {
            task_id: task.id.clone(),
            agent_id: agent_type.to_string(),
            started_at: chrono::Utc::now(),
            completed_at: None,
            result: None,
        };

        self.execution_history.write().await.push(record.clone());

        // Update agent status
        {
            let mut agent_guard = agent.write().await;
            agent_guard.status = AgentStatus::Working;
            agent_guard.current_task = Some(task.clone());
            agent_guard.last_activity = chrono::Utc::now();
        }

        // Execute task with session
        let result = {
            let mut session_guard = session.write().await;
            session_guard.execute_task(task.clone()).await?
        };

        // Update execution record
        let mut history = self.execution_history.write().await;
        if let Some(record) = history.iter_mut().find(|r| r.task_id == task.id) {
            record.completed_at = Some(chrono::Utc::now());
            record.result = Some(result.clone());
        }

        // Update agent status
        {
            let mut agent_guard = agent.write().await;
            agent_guard.status = AgentStatus::Available;
            agent_guard.current_task = None;
            agent_guard.last_activity = chrono::Utc::now();

            // Add to task history
            agent_guard
                .task_history
                .push((task.clone(), result.clone()));
        }

        // Send completion message
        self.coordination_bus
            .send_message(AgentMessage::TaskCompleted {
                agent_id: agent_type.to_string(),
                task_id: task.id.clone(),
                result: result.clone(),
            })
            .await?;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_parallel_step (lines 637-688) ---[0m
    async fn execute_parallel_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let mut results = Vec::new();

        // Get available agents
        let available_agents: Vec<_> = self
            .agents
            .iter()
            .map(|entry| {
                let agent_type = entry.key().clone();
                agent_type
            })
            .collect();

        if available_agents.is_empty() {
            return Ok(StepResult::new(step.id.clone())
                .failed("No available agents for parallel execution".to_string()));
        }

        // Execute on each available agent
        for agent_type in available_agents {
            let task = Task::new(
                format!("parallel_{}_{}", step.id, agent_type),
                format!("{} ({})", step.description, agent_type),
                crate::agent::Priority::Medium,
                TaskType::Development,
            );

            match self.execute_task_with_agent(&agent_type, &task).await {
                Ok(result) => results.push(result),
                Err(e) => {
                    warn!("Parallel execution failed for agent {}: {}", agent_type, e);
                    results.push(TaskResult::failure(e.to_string(), Duration::from_secs(0)));
                }
            }
        }

        let success_count = results.iter().filter(|r| r.success).count();
        let _success = success_count > 0; // At least one agent succeeded

        Ok(StepResult::new(step.id.clone())
            .with_summary(format!(
                "Parallel execution: {}/{} agents succeeded",
                success_count,
                results.len()
            ))
            .add_output("parallel_results".to_string(), results.len().to_string())
            .add_output("success_count".to_string(), success_count.to_string()))
    }

  ./crates/ccswarm/src/agent/pool.rs:537-584 method execute_parallel_task <-> ./crates/ccswarm/src/agent/pool.rs:637-688 method execute_parallel_step
  Similarity: 92.50%
  Classes: AgentOrchestrator <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_parallel_task (lines 537-584) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task across available agents
        let available_agents: Vec<_> = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        if available_agents.is_empty() {
            return Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: false,
                output: "No available agents".to_string(),
                error: Some("No agents available for parallel task execution".to_string()),
            });
        }

        // Use first available agent for the parallel task
        let agent_type = &available_agents[0];
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        match self
            .execute_task_with_agent(agent_type, &execution_task)
            .await
        {
            Ok(result) => Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: result.success,
                output: serde_json::to_string(&result.output).unwrap_or_default(),
                error: result.error,
            }),
            Err(e) => Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: false,
                output: String::new(),
                error: Some(e.to_string()),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_parallel_step (lines 637-688) ---[0m
    async fn execute_parallel_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let mut results = Vec::new();

        // Get available agents
        let available_agents: Vec<_> = self
            .agents
            .iter()
            .map(|entry| {
                let agent_type = entry.key().clone();
                agent_type
            })
            .collect();

        if available_agents.is_empty() {
            return Ok(StepResult::new(step.id.clone())
                .failed("No available agents for parallel execution".to_string()));
        }

        // Execute on each available agent
        for agent_type in available_agents {
            let task = Task::new(
                format!("parallel_{}_{}", step.id, agent_type),
                format!("{} ({})", step.description, agent_type),
                crate::agent::Priority::Medium,
                TaskType::Development,
            );

            match self.execute_task_with_agent(&agent_type, &task).await {
                Ok(result) => results.push(result),
                Err(e) => {
                    warn!("Parallel execution failed for agent {}: {}", agent_type, e);
                    results.push(TaskResult::failure(e.to_string(), Duration::from_secs(0)));
                }
            }
        }

        let success_count = results.iter().filter(|r| r.success).count();
        let _success = success_count > 0; // At least one agent succeeded

        Ok(StepResult::new(step.id.clone())
            .with_summary(format!(
                "Parallel execution: {}/{} agents succeeded",
                success_count,
                results.len()
            ))
            .add_output("parallel_results".to_string(), results.len().to_string())
            .add_output("success_count".to_string(), success_count.to_string()))
    }

  ./crates/ccswarm/src/agent/pool.rs:66-116 method spawn_agent <-> ./crates/ccswarm/src/agent/pool.rs:637-688 method execute_parallel_step
  Similarity: 85.35%
  Classes: AgentPool <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:spawn_agent (lines 66-116) ---[0m
    pub async fn spawn_agent(&mut self, agent_type: &str, config: &CcswarmConfig) -> Result<()> {
        info!("🚀 Spawning {} agent", agent_type);

        // Get role for agent type
        let role = match agent_type.to_lowercase().as_str() {
            "frontend" => default_frontend_role(),
            "backend" => default_backend_role(),
            "devops" => default_devops_role(),
            "qa" => default_qa_role(),
            _ => return Err(anyhow::anyhow!("Unknown agent type: {}", agent_type)),
        };

        // Get agent config
        let agent_config = config
            .agents
            .get(agent_type)
            .ok_or_else(|| anyhow::anyhow!("No configuration for agent: {}", agent_type))?;

        // Create agent
        let mut agent = ClaudeCodeAgent::new(
            role,
            &PathBuf::from(&config.project.name),
            &agent_config.branch,
            agent_config.claude_config.clone(),
        )
        .await?;

        // Initialize agent
        agent.initialize().await?;

        // Create persistent session for agent
        let mut session = PersistentClaudeSession::new(
            agent.identity.clone(),
            agent.worktree_path.clone(),
            agent_config.claude_config.clone(),
        )
        .await?;

        // Initialize session
        session.initialize().await?;

        // Store agent and session
        self.agents
            .insert(agent_type.to_string(), Arc::new(RwLock::new(agent)));

        self.sessions
            .insert(agent_type.to_string(), Arc::new(RwLock::new(session)));

        info!("✅ {} agent spawned and initialized", agent_type);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_parallel_step (lines 637-688) ---[0m
    async fn execute_parallel_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let mut results = Vec::new();

        // Get available agents
        let available_agents: Vec<_> = self
            .agents
            .iter()
            .map(|entry| {
                let agent_type = entry.key().clone();
                agent_type
            })
            .collect();

        if available_agents.is_empty() {
            return Ok(StepResult::new(step.id.clone())
                .failed("No available agents for parallel execution".to_string()));
        }

        // Execute on each available agent
        for agent_type in available_agents {
            let task = Task::new(
                format!("parallel_{}_{}", step.id, agent_type),
                format!("{} ({})", step.description, agent_type),
                crate::agent::Priority::Medium,
                TaskType::Development,
            );

            match self.execute_task_with_agent(&agent_type, &task).await {
                Ok(result) => results.push(result),
                Err(e) => {
                    warn!("Parallel execution failed for agent {}: {}", agent_type, e);
                    results.push(TaskResult::failure(e.to_string(), Duration::from_secs(0)));
                }
            }
        }

        let success_count = results.iter().filter(|r| r.success).count();
        let _success = success_count > 0; // At least one agent succeeded

        Ok(StepResult::new(step.id.clone())
            .with_summary(format!(
                "Parallel execution: {}/{} agents succeeded",
                success_count,
                results.len()
            ))
            .add_output("parallel_results".to_string(), results.len().to_string())
            .add_output("success_count".to_string(), success_count.to_string()))
    }

  ./crates/ccswarm/src/agent/pool.rs:142-214 method execute_task_with_agent <-> ./crates/ccswarm/src/agent/pool.rs:312-345 method orchestrate_task
  Similarity: 80.98%
  Classes: AgentPool <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_task_with_agent (lines 142-214) ---[0m
    pub async fn execute_task_with_agent(
        &self,
        agent_type: &str,
        task: &Task,
    ) -> Result<TaskResult> {
        info!(
            "📋 Executing task with {} agent: {}",
            agent_type, task.description
        );

        // Get agent and session
        let agent = self.get_agent(agent_type)?;
        let session = self
            .sessions
            .get(agent_type)
            .ok_or_else(|| anyhow::anyhow!("No session for agent: {}", agent_type))?;

        // Record execution start
        let record = TaskExecutionRecord {
            task_id: task.id.clone(),
            agent_id: agent_type.to_string(),
            started_at: chrono::Utc::now(),
            completed_at: None,
            result: None,
        };

        self.execution_history.write().await.push(record.clone());

        // Update agent status
        {
            let mut agent_guard = agent.write().await;
            agent_guard.status = AgentStatus::Working;
            agent_guard.current_task = Some(task.clone());
            agent_guard.last_activity = chrono::Utc::now();
        }

        // Execute task with session
        let result = {
            let mut session_guard = session.write().await;
            session_guard.execute_task(task.clone()).await?
        };

        // Update execution record
        let mut history = self.execution_history.write().await;
        if let Some(record) = history.iter_mut().find(|r| r.task_id == task.id) {
            record.completed_at = Some(chrono::Utc::now());
            record.result = Some(result.clone());
        }

        // Update agent status
        {
            let mut agent_guard = agent.write().await;
            agent_guard.status = AgentStatus::Available;
            agent_guard.current_task = None;
            agent_guard.last_activity = chrono::Utc::now();

            // Add to task history
            agent_guard
                .task_history
                .push((task.clone(), result.clone()));
        }

        // Send completion message
        self.coordination_bus
            .send_message(AgentMessage::TaskCompleted {
                agent_id: agent_type.to_string(),
                task_id: task.id.clone(),
                result: result.clone(),
            })
            .await?;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:orchestrate_task (lines 312-345) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!("🎭 Pool orchestrating task: {}", task.description);

        // Analyze task to create execution plan
        let plan = self.analyze_task(task).await?;

        // Execute plan with parallel agent coordination
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("pool_mode".to_string(), "true".to_string());

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Pool step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Pool orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({"orchestration": "pool_success", "steps_completed": plan.steps.len()}),
            Duration::from_secs(10),
        ))
    }

  ./crates/ccswarm/src/agent/pool.rs:586-632 method synthesize_results <-> ./crates/ccswarm/src/agent/pool.rs:637-688 method execute_parallel_step
  Similarity: 86.88%
  Classes: AgentOrchestrator <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:synthesize_results (lines 586-632) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Pool orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Pool orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let output = serde_json::json!({
            "orchestration_type": "pool",
            "task_id": task.id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_parallel_step (lines 637-688) ---[0m
    async fn execute_parallel_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let mut results = Vec::new();

        // Get available agents
        let available_agents: Vec<_> = self
            .agents
            .iter()
            .map(|entry| {
                let agent_type = entry.key().clone();
                agent_type
            })
            .collect();

        if available_agents.is_empty() {
            return Ok(StepResult::new(step.id.clone())
                .failed("No available agents for parallel execution".to_string()));
        }

        // Execute on each available agent
        for agent_type in available_agents {
            let task = Task::new(
                format!("parallel_{}_{}", step.id, agent_type),
                format!("{} ({})", step.description, agent_type),
                crate::agent::Priority::Medium,
                TaskType::Development,
            );

            match self.execute_task_with_agent(&agent_type, &task).await {
                Ok(result) => results.push(result),
                Err(e) => {
                    warn!("Parallel execution failed for agent {}: {}", agent_type, e);
                    results.push(TaskResult::failure(e.to_string(), Duration::from_secs(0)));
                }
            }
        }

        let success_count = results.iter().filter(|r| r.success).count();
        let _success = success_count > 0; // At least one agent succeeded

        Ok(StepResult::new(step.id.clone())
            .with_summary(format!(
                "Parallel execution: {}/{} agents succeeded",
                success_count,
                results.len()
            ))
            .add_output("parallel_results".to_string(), results.len().to_string())
            .add_output("success_count".to_string(), success_count.to_string()))
    }

  ./crates/ccswarm/src/agent/pool.rs:537-584 method execute_parallel_task <-> ./crates/ccswarm/src/agent/pool.rs:586-632 method synthesize_results
  Similarity: 90.29%
  Classes: AgentOrchestrator <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_parallel_task (lines 537-584) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task across available agents
        let available_agents: Vec<_> = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        if available_agents.is_empty() {
            return Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: false,
                output: "No available agents".to_string(),
                error: Some("No agents available for parallel task execution".to_string()),
            });
        }

        // Use first available agent for the parallel task
        let agent_type = &available_agents[0];
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        match self
            .execute_task_with_agent(agent_type, &execution_task)
            .await
        {
            Ok(result) => Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: result.success,
                output: serde_json::to_string(&result.output).unwrap_or_default(),
                error: result.error,
            }),
            Err(e) => Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: false,
                output: String::new(),
                error: Some(e.to_string()),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:synthesize_results (lines 586-632) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Pool orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Pool orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let output = serde_json::json!({
            "orchestration_type": "pool",
            "task_id": task.id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:142-214 method execute_task_with_agent <-> ./crates/ccswarm/src/agent/pool.rs:754-792 method execute_default_step
  Similarity: 74.97%
  Classes: AgentPool <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_task_with_agent (lines 142-214) ---[0m
    pub async fn execute_task_with_agent(
        &self,
        agent_type: &str,
        task: &Task,
    ) -> Result<TaskResult> {
        info!(
            "📋 Executing task with {} agent: {}",
            agent_type, task.description
        );

        // Get agent and session
        let agent = self.get_agent(agent_type)?;
        let session = self
            .sessions
            .get(agent_type)
            .ok_or_else(|| anyhow::anyhow!("No session for agent: {}", agent_type))?;

        // Record execution start
        let record = TaskExecutionRecord {
            task_id: task.id.clone(),
            agent_id: agent_type.to_string(),
            started_at: chrono::Utc::now(),
            completed_at: None,
            result: None,
        };

        self.execution_history.write().await.push(record.clone());

        // Update agent status
        {
            let mut agent_guard = agent.write().await;
            agent_guard.status = AgentStatus::Working;
            agent_guard.current_task = Some(task.clone());
            agent_guard.last_activity = chrono::Utc::now();
        }

        // Execute task with session
        let result = {
            let mut session_guard = session.write().await;
            session_guard.execute_task(task.clone()).await?
        };

        // Update execution record
        let mut history = self.execution_history.write().await;
        if let Some(record) = history.iter_mut().find(|r| r.task_id == task.id) {
            record.completed_at = Some(chrono::Utc::now());
            record.result = Some(result.clone());
        }

        // Update agent status
        {
            let mut agent_guard = agent.write().await;
            agent_guard.status = AgentStatus::Available;
            agent_guard.current_task = None;
            agent_guard.last_activity = chrono::Utc::now();

            // Add to task history
            agent_guard
                .task_history
                .push((task.clone(), result.clone()));
        }

        // Send completion message
        self.coordination_bus
            .send_message(AgentMessage::TaskCompleted {
                agent_id: agent_type.to_string(),
                task_id: task.id.clone(),
                result: result.clone(),
            })
            .await?;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_default_step (lines 754-792) ---[0m
    async fn execute_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("pool_step_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Get best agent for this task
        let best_agent = self.get_best_agent_for_task(&task).await?;
        let agent_type = {
            let agent_guard = best_agent.read().await;
            agent_guard.identity.specialization.name().to_lowercase()
        };

        // Execute with selected agent
        match self.execute_task_with_agent(&agent_type, &task).await {
            Ok(result) => Ok(StepResult::new(step.id.clone())
                .with_summary(format!(
                    "Task executed successfully by {} agent",
                    agent_type
                ))
                .add_output("agent_used".to_string(), agent_type)
                .add_output("task_success".to_string(), result.success.to_string())),
            Err(e) => {
                Ok(StepResult::new(step.id.clone())
                    .failed(format!("Default execution failed: {}", e)))
            }
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:637-688 method execute_parallel_step <-> ./crates/ccswarm/src/agent/pool.rs:754-792 method execute_default_step
  Similarity: 91.15%
  Classes: AgentPool <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_parallel_step (lines 637-688) ---[0m
    async fn execute_parallel_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let mut results = Vec::new();

        // Get available agents
        let available_agents: Vec<_> = self
            .agents
            .iter()
            .map(|entry| {
                let agent_type = entry.key().clone();
                agent_type
            })
            .collect();

        if available_agents.is_empty() {
            return Ok(StepResult::new(step.id.clone())
                .failed("No available agents for parallel execution".to_string()));
        }

        // Execute on each available agent
        for agent_type in available_agents {
            let task = Task::new(
                format!("parallel_{}_{}", step.id, agent_type),
                format!("{} ({})", step.description, agent_type),
                crate::agent::Priority::Medium,
                TaskType::Development,
            );

            match self.execute_task_with_agent(&agent_type, &task).await {
                Ok(result) => results.push(result),
                Err(e) => {
                    warn!("Parallel execution failed for agent {}: {}", agent_type, e);
                    results.push(TaskResult::failure(e.to_string(), Duration::from_secs(0)));
                }
            }
        }

        let success_count = results.iter().filter(|r| r.success).count();
        let _success = success_count > 0; // At least one agent succeeded

        Ok(StepResult::new(step.id.clone())
            .with_summary(format!(
                "Parallel execution: {}/{} agents succeeded",
                success_count,
                results.len()
            ))
            .add_output("parallel_results".to_string(), results.len().to_string())
            .add_output("success_count".to_string(), success_count.to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_default_step (lines 754-792) ---[0m
    async fn execute_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("pool_step_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Get best agent for this task
        let best_agent = self.get_best_agent_for_task(&task).await?;
        let agent_type = {
            let agent_guard = best_agent.read().await;
            agent_guard.identity.specialization.name().to_lowercase()
        };

        // Execute with selected agent
        match self.execute_task_with_agent(&agent_type, &task).await {
            Ok(result) => Ok(StepResult::new(step.id.clone())
                .with_summary(format!(
                    "Task executed successfully by {} agent",
                    agent_type
                ))
                .add_output("agent_used".to_string(), agent_type)
                .add_output("task_success".to_string(), result.success.to_string())),
            Err(e) => {
                Ok(StepResult::new(step.id.clone())
                    .failed(format!("Default execution failed: {}", e)))
            }
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:537-584 method execute_parallel_task <-> ./crates/ccswarm/src/agent/pool.rs:754-792 method execute_default_step
  Similarity: 93.40%
  Classes: AgentOrchestrator <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_parallel_task (lines 537-584) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task across available agents
        let available_agents: Vec<_> = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        if available_agents.is_empty() {
            return Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: false,
                output: "No available agents".to_string(),
                error: Some("No agents available for parallel task execution".to_string()),
            });
        }

        // Use first available agent for the parallel task
        let agent_type = &available_agents[0];
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        match self
            .execute_task_with_agent(agent_type, &execution_task)
            .await
        {
            Ok(result) => Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: result.success,
                output: serde_json::to_string(&result.output).unwrap_or_default(),
                error: result.error,
            }),
            Err(e) => Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: false,
                output: String::new(),
                error: Some(e.to_string()),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_default_step (lines 754-792) ---[0m
    async fn execute_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("pool_step_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Get best agent for this task
        let best_agent = self.get_best_agent_for_task(&task).await?;
        let agent_type = {
            let agent_guard = best_agent.read().await;
            agent_guard.identity.specialization.name().to_lowercase()
        };

        // Execute with selected agent
        match self.execute_task_with_agent(&agent_type, &task).await {
            Ok(result) => Ok(StepResult::new(step.id.clone())
                .with_summary(format!(
                    "Task executed successfully by {} agent",
                    agent_type
                ))
                .add_output("agent_used".to_string(), agent_type)
                .add_output("task_success".to_string(), result.success.to_string())),
            Err(e) => {
                Ok(StepResult::new(step.id.clone())
                    .failed(format!("Default execution failed: {}", e)))
            }
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:637-688 method execute_parallel_step <-> ./crates/ccswarm/src/agent/pool.rs:715-751 method execute_integration_test_step
  Similarity: 89.19%
  Classes: AgentPool <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_parallel_step (lines 637-688) ---[0m
    async fn execute_parallel_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let mut results = Vec::new();

        // Get available agents
        let available_agents: Vec<_> = self
            .agents
            .iter()
            .map(|entry| {
                let agent_type = entry.key().clone();
                agent_type
            })
            .collect();

        if available_agents.is_empty() {
            return Ok(StepResult::new(step.id.clone())
                .failed("No available agents for parallel execution".to_string()));
        }

        // Execute on each available agent
        for agent_type in available_agents {
            let task = Task::new(
                format!("parallel_{}_{}", step.id, agent_type),
                format!("{} ({})", step.description, agent_type),
                crate::agent::Priority::Medium,
                TaskType::Development,
            );

            match self.execute_task_with_agent(&agent_type, &task).await {
                Ok(result) => results.push(result),
                Err(e) => {
                    warn!("Parallel execution failed for agent {}: {}", agent_type, e);
                    results.push(TaskResult::failure(e.to_string(), Duration::from_secs(0)));
                }
            }
        }

        let success_count = results.iter().filter(|r| r.success).count();
        let _success = success_count > 0; // At least one agent succeeded

        Ok(StepResult::new(step.id.clone())
            .with_summary(format!(
                "Parallel execution: {}/{} agents succeeded",
                success_count,
                results.len()
            ))
            .add_output("parallel_results".to_string(), results.len().to_string())
            .add_output("success_count".to_string(), success_count.to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_integration_test_step (lines 715-751) ---[0m
    async fn execute_integration_test_step(
        &self,
        _step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        // Test communication between agents
        let agent_types: Vec<_> = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        let mut test_results = Vec::new();

        // Test inter-agent communication
        for i in 0..agent_types.len() {
            for j in (i + 1)..agent_types.len() {
                let from = &agent_types[i];
                let to = &agent_types[j];

                match self.send_message(from, to, "integration_test").await {
                    Ok(_) => test_results.push(format!("{} -> {} ✅", from, to)),
                    Err(e) => test_results.push(format!("{} -> {} ❌: {}", from, to, e)),
                }
            }
        }

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!(
                "Integration test: {} agent pairs tested",
                test_results.len()
            ))
            .add_output(
                "agent_pairs_tested".to_string(),
                test_results.len().to_string(),
            ))
    }

  ./crates/ccswarm/src/agent/pool.rs:586-632 method synthesize_results <-> ./crates/ccswarm/src/agent/pool.rs:754-792 method execute_default_step
  Similarity: 92.10%
  Classes: AgentOrchestrator <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:synthesize_results (lines 586-632) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Pool orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Pool orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let output = serde_json::json!({
            "orchestration_type": "pool",
            "task_id": task.id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_default_step (lines 754-792) ---[0m
    async fn execute_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("pool_step_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Get best agent for this task
        let best_agent = self.get_best_agent_for_task(&task).await?;
        let agent_type = {
            let agent_guard = best_agent.read().await;
            agent_guard.identity.specialization.name().to_lowercase()
        };

        // Execute with selected agent
        match self.execute_task_with_agent(&agent_type, &task).await {
            Ok(result) => Ok(StepResult::new(step.id.clone())
                .with_summary(format!(
                    "Task executed successfully by {} agent",
                    agent_type
                ))
                .add_output("agent_used".to_string(), agent_type)
                .add_output("task_success".to_string(), result.success.to_string())),
            Err(e) => {
                Ok(StepResult::new(step.id.clone())
                    .failed(format!("Default execution failed: {}", e)))
            }
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:312-345 method orchestrate_task <-> ./crates/ccswarm/src/agent/pool.rs:637-688 method execute_parallel_step
  Similarity: 90.96%
  Classes: AgentOrchestrator <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:orchestrate_task (lines 312-345) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!("🎭 Pool orchestrating task: {}", task.description);

        // Analyze task to create execution plan
        let plan = self.analyze_task(task).await?;

        // Execute plan with parallel agent coordination
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("pool_mode".to_string(), "true".to_string());

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Pool step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Pool orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({"orchestration": "pool_success", "steps_completed": plan.steps.len()}),
            Duration::from_secs(10),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_parallel_step (lines 637-688) ---[0m
    async fn execute_parallel_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let mut results = Vec::new();

        // Get available agents
        let available_agents: Vec<_> = self
            .agents
            .iter()
            .map(|entry| {
                let agent_type = entry.key().clone();
                agent_type
            })
            .collect();

        if available_agents.is_empty() {
            return Ok(StepResult::new(step.id.clone())
                .failed("No available agents for parallel execution".to_string()));
        }

        // Execute on each available agent
        for agent_type in available_agents {
            let task = Task::new(
                format!("parallel_{}_{}", step.id, agent_type),
                format!("{} ({})", step.description, agent_type),
                crate::agent::Priority::Medium,
                TaskType::Development,
            );

            match self.execute_task_with_agent(&agent_type, &task).await {
                Ok(result) => results.push(result),
                Err(e) => {
                    warn!("Parallel execution failed for agent {}: {}", agent_type, e);
                    results.push(TaskResult::failure(e.to_string(), Duration::from_secs(0)));
                }
            }
        }

        let success_count = results.iter().filter(|r| r.success).count();
        let _success = success_count > 0; // At least one agent succeeded

        Ok(StepResult::new(step.id.clone())
            .with_summary(format!(
                "Parallel execution: {}/{} agents succeeded",
                success_count,
                results.len()
            ))
            .add_output("parallel_results".to_string(), results.len().to_string())
            .add_output("success_count".to_string(), success_count.to_string()))
    }

  ./crates/ccswarm/src/agent/pool.rs:496-535 method review_and_adapt <-> ./crates/ccswarm/src/agent/pool.rs:637-688 method execute_parallel_step
  Similarity: 84.02%
  Classes: AgentOrchestrator <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:review_and_adapt (lines 496-535) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Pool reviewing and adapting plan based on {} results",
            results.len()
        );

        // Check if any agents failed and need to be reassigned
        let failed_agents: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_agents.is_empty() {
            warn!(
                "Pool detected {} failed agent executions",
                failed_agents.len()
            );

            // Add recovery steps for failed agents
            let recovery_step = TaskStep::new(
                "agent_recovery".to_string(),
                "Recover failed agents".to_string(),
                StepType::Execution,
            )
            .with_description("Restart and recover failed agents".to_string());

            let validation_step = TaskStep::new(
                "recovery_validation".to_string(),
                "Validate agent recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Verify all agents are operational".to_string());

            current_plan.steps.push(recovery_step);
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_parallel_step (lines 637-688) ---[0m
    async fn execute_parallel_step(
        &self,
        step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let mut results = Vec::new();

        // Get available agents
        let available_agents: Vec<_> = self
            .agents
            .iter()
            .map(|entry| {
                let agent_type = entry.key().clone();
                agent_type
            })
            .collect();

        if available_agents.is_empty() {
            return Ok(StepResult::new(step.id.clone())
                .failed("No available agents for parallel execution".to_string()));
        }

        // Execute on each available agent
        for agent_type in available_agents {
            let task = Task::new(
                format!("parallel_{}_{}", step.id, agent_type),
                format!("{} ({})", step.description, agent_type),
                crate::agent::Priority::Medium,
                TaskType::Development,
            );

            match self.execute_task_with_agent(&agent_type, &task).await {
                Ok(result) => results.push(result),
                Err(e) => {
                    warn!("Parallel execution failed for agent {}: {}", agent_type, e);
                    results.push(TaskResult::failure(e.to_string(), Duration::from_secs(0)));
                }
            }
        }

        let success_count = results.iter().filter(|r| r.success).count();
        let _success = success_count > 0; // At least one agent succeeded

        Ok(StepResult::new(step.id.clone())
            .with_summary(format!(
                "Parallel execution: {}/{} agents succeeded",
                success_count,
                results.len()
            ))
            .add_output("parallel_results".to_string(), results.len().to_string())
            .add_output("success_count".to_string(), success_count.to_string()))
    }

  ./crates/ccswarm/src/agent/pool.rs:586-632 method synthesize_results <-> ./crates/ccswarm/src/agent/pool.rs:715-751 method execute_integration_test_step
  Similarity: 89.86%
  Classes: AgentOrchestrator <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:synthesize_results (lines 586-632) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Pool orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Pool orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let output = serde_json::json!({
            "orchestration_type": "pool",
            "task_id": task.id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_integration_test_step (lines 715-751) ---[0m
    async fn execute_integration_test_step(
        &self,
        _step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        // Test communication between agents
        let agent_types: Vec<_> = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        let mut test_results = Vec::new();

        // Test inter-agent communication
        for i in 0..agent_types.len() {
            for j in (i + 1)..agent_types.len() {
                let from = &agent_types[i];
                let to = &agent_types[j];

                match self.send_message(from, to, "integration_test").await {
                    Ok(_) => test_results.push(format!("{} -> {} ✅", from, to)),
                    Err(e) => test_results.push(format!("{} -> {} ❌: {}", from, to, e)),
                }
            }
        }

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!(
                "Integration test: {} agent pairs tested",
                test_results.len()
            ))
            .add_output(
                "agent_pairs_tested".to_string(),
                test_results.len().to_string(),
            ))
    }

  ./crates/ccswarm/src/agent/pool.rs:496-535 method review_and_adapt <-> ./crates/ccswarm/src/agent/pool.rs:537-584 method execute_parallel_task
  Similarity: 84.91%
  Classes: AgentOrchestrator <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/pool.rs:review_and_adapt (lines 496-535) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Pool reviewing and adapting plan based on {} results",
            results.len()
        );

        // Check if any agents failed and need to be reassigned
        let failed_agents: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_agents.is_empty() {
            warn!(
                "Pool detected {} failed agent executions",
                failed_agents.len()
            );

            // Add recovery steps for failed agents
            let recovery_step = TaskStep::new(
                "agent_recovery".to_string(),
                "Recover failed agents".to_string(),
                StepType::Execution,
            )
            .with_description("Restart and recover failed agents".to_string());

            let validation_step = TaskStep::new(
                "recovery_validation".to_string(),
                "Validate agent recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Verify all agents are operational".to_string());

            current_plan.steps.push(recovery_step);
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_parallel_task (lines 537-584) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task across available agents
        let available_agents: Vec<_> = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        if available_agents.is_empty() {
            return Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: false,
                output: "No available agents".to_string(),
                error: Some("No agents available for parallel task execution".to_string()),
            });
        }

        // Use first available agent for the parallel task
        let agent_type = &available_agents[0];
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        match self
            .execute_task_with_agent(agent_type, &execution_task)
            .await
        {
            Ok(result) => Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: result.success,
                output: serde_json::to_string(&result.output).unwrap_or_default(),
                error: result.error,
            }),
            Err(e) => Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: false,
                output: String::new(),
                error: Some(e.to_string()),
            }),
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:66-116 method spawn_agent <-> ./crates/ccswarm/src/agent/pool.rs:537-584 method execute_parallel_task
  Similarity: 74.87%
  Classes: AgentPool <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/pool.rs:spawn_agent (lines 66-116) ---[0m
    pub async fn spawn_agent(&mut self, agent_type: &str, config: &CcswarmConfig) -> Result<()> {
        info!("🚀 Spawning {} agent", agent_type);

        // Get role for agent type
        let role = match agent_type.to_lowercase().as_str() {
            "frontend" => default_frontend_role(),
            "backend" => default_backend_role(),
            "devops" => default_devops_role(),
            "qa" => default_qa_role(),
            _ => return Err(anyhow::anyhow!("Unknown agent type: {}", agent_type)),
        };

        // Get agent config
        let agent_config = config
            .agents
            .get(agent_type)
            .ok_or_else(|| anyhow::anyhow!("No configuration for agent: {}", agent_type))?;

        // Create agent
        let mut agent = ClaudeCodeAgent::new(
            role,
            &PathBuf::from(&config.project.name),
            &agent_config.branch,
            agent_config.claude_config.clone(),
        )
        .await?;

        // Initialize agent
        agent.initialize().await?;

        // Create persistent session for agent
        let mut session = PersistentClaudeSession::new(
            agent.identity.clone(),
            agent.worktree_path.clone(),
            agent_config.claude_config.clone(),
        )
        .await?;

        // Initialize session
        session.initialize().await?;

        // Store agent and session
        self.agents
            .insert(agent_type.to_string(), Arc::new(RwLock::new(agent)));

        self.sessions
            .insert(agent_type.to_string(), Arc::new(RwLock::new(session)));

        info!("✅ {} agent spawned and initialized", agent_type);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_parallel_task (lines 537-584) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task across available agents
        let available_agents: Vec<_> = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        if available_agents.is_empty() {
            return Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: false,
                output: "No available agents".to_string(),
                error: Some("No agents available for parallel task execution".to_string()),
            });
        }

        // Use first available agent for the parallel task
        let agent_type = &available_agents[0];
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        match self
            .execute_task_with_agent(agent_type, &execution_task)
            .await
        {
            Ok(result) => Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: result.success,
                output: serde_json::to_string(&result.output).unwrap_or_default(),
                error: result.error,
            }),
            Err(e) => Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: false,
                output: String::new(),
                error: Some(e.to_string()),
            }),
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:537-584 method execute_parallel_task <-> ./crates/ccswarm/src/agent/pool.rs:715-751 method execute_integration_test_step
  Similarity: 84.62%
  Classes: AgentOrchestrator <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_parallel_task (lines 537-584) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task across available agents
        let available_agents: Vec<_> = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        if available_agents.is_empty() {
            return Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: false,
                output: "No available agents".to_string(),
                error: Some("No agents available for parallel task execution".to_string()),
            });
        }

        // Use first available agent for the parallel task
        let agent_type = &available_agents[0];
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        match self
            .execute_task_with_agent(agent_type, &execution_task)
            .await
        {
            Ok(result) => Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: result.success,
                output: serde_json::to_string(&result.output).unwrap_or_default(),
                error: result.error,
            }),
            Err(e) => Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: false,
                output: String::new(),
                error: Some(e.to_string()),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_integration_test_step (lines 715-751) ---[0m
    async fn execute_integration_test_step(
        &self,
        _step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        // Test communication between agents
        let agent_types: Vec<_> = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        let mut test_results = Vec::new();

        // Test inter-agent communication
        for i in 0..agent_types.len() {
            for j in (i + 1)..agent_types.len() {
                let from = &agent_types[i];
                let to = &agent_types[j];

                match self.send_message(from, to, "integration_test").await {
                    Ok(_) => test_results.push(format!("{} -> {} ✅", from, to)),
                    Err(e) => test_results.push(format!("{} -> {} ❌: {}", from, to, e)),
                }
            }
        }

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!(
                "Integration test: {} agent pairs tested",
                test_results.len()
            ))
            .add_output(
                "agent_pairs_tested".to_string(),
                test_results.len().to_string(),
            ))
    }

  ./crates/ccswarm/src/agent/pool.rs:66-116 method spawn_agent <-> ./crates/ccswarm/src/agent/pool.rs:586-632 method synthesize_results
  Similarity: 72.53%
  Classes: AgentPool <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/pool.rs:spawn_agent (lines 66-116) ---[0m
    pub async fn spawn_agent(&mut self, agent_type: &str, config: &CcswarmConfig) -> Result<()> {
        info!("🚀 Spawning {} agent", agent_type);

        // Get role for agent type
        let role = match agent_type.to_lowercase().as_str() {
            "frontend" => default_frontend_role(),
            "backend" => default_backend_role(),
            "devops" => default_devops_role(),
            "qa" => default_qa_role(),
            _ => return Err(anyhow::anyhow!("Unknown agent type: {}", agent_type)),
        };

        // Get agent config
        let agent_config = config
            .agents
            .get(agent_type)
            .ok_or_else(|| anyhow::anyhow!("No configuration for agent: {}", agent_type))?;

        // Create agent
        let mut agent = ClaudeCodeAgent::new(
            role,
            &PathBuf::from(&config.project.name),
            &agent_config.branch,
            agent_config.claude_config.clone(),
        )
        .await?;

        // Initialize agent
        agent.initialize().await?;

        // Create persistent session for agent
        let mut session = PersistentClaudeSession::new(
            agent.identity.clone(),
            agent.worktree_path.clone(),
            agent_config.claude_config.clone(),
        )
        .await?;

        // Initialize session
        session.initialize().await?;

        // Store agent and session
        self.agents
            .insert(agent_type.to_string(), Arc::new(RwLock::new(agent)));

        self.sessions
            .insert(agent_type.to_string(), Arc::new(RwLock::new(session)));

        info!("✅ {} agent spawned and initialized", agent_type);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:synthesize_results (lines 586-632) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Pool orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Pool orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let output = serde_json::json!({
            "orchestration_type": "pool",
            "task_id": task.id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:496-535 method review_and_adapt <-> ./crates/ccswarm/src/agent/pool.rs:754-792 method execute_default_step
  Similarity: 89.72%
  Classes: AgentOrchestrator <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:review_and_adapt (lines 496-535) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Pool reviewing and adapting plan based on {} results",
            results.len()
        );

        // Check if any agents failed and need to be reassigned
        let failed_agents: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_agents.is_empty() {
            warn!(
                "Pool detected {} failed agent executions",
                failed_agents.len()
            );

            // Add recovery steps for failed agents
            let recovery_step = TaskStep::new(
                "agent_recovery".to_string(),
                "Recover failed agents".to_string(),
                StepType::Execution,
            )
            .with_description("Restart and recover failed agents".to_string());

            let validation_step = TaskStep::new(
                "recovery_validation".to_string(),
                "Validate agent recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Verify all agents are operational".to_string());

            current_plan.steps.push(recovery_step);
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_default_step (lines 754-792) ---[0m
    async fn execute_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("pool_step_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Get best agent for this task
        let best_agent = self.get_best_agent_for_task(&task).await?;
        let agent_type = {
            let agent_guard = best_agent.read().await;
            agent_guard.identity.specialization.name().to_lowercase()
        };

        // Execute with selected agent
        match self.execute_task_with_agent(&agent_type, &task).await {
            Ok(result) => Ok(StepResult::new(step.id.clone())
                .with_summary(format!(
                    "Task executed successfully by {} agent",
                    agent_type
                ))
                .add_output("agent_used".to_string(), agent_type)
                .add_output("task_success".to_string(), result.success.to_string())),
            Err(e) => {
                Ok(StepResult::new(step.id.clone())
                    .failed(format!("Default execution failed: {}", e)))
            }
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:715-751 method execute_integration_test_step <-> ./crates/ccswarm/src/agent/pool.rs:754-792 method execute_default_step
  Similarity: 92.35%
  Classes: AgentPool <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_integration_test_step (lines 715-751) ---[0m
    async fn execute_integration_test_step(
        &self,
        _step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        // Test communication between agents
        let agent_types: Vec<_> = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        let mut test_results = Vec::new();

        // Test inter-agent communication
        for i in 0..agent_types.len() {
            for j in (i + 1)..agent_types.len() {
                let from = &agent_types[i];
                let to = &agent_types[j];

                match self.send_message(from, to, "integration_test").await {
                    Ok(_) => test_results.push(format!("{} -> {} ✅", from, to)),
                    Err(e) => test_results.push(format!("{} -> {} ❌: {}", from, to, e)),
                }
            }
        }

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!(
                "Integration test: {} agent pairs tested",
                test_results.len()
            ))
            .add_output(
                "agent_pairs_tested".to_string(),
                test_results.len().to_string(),
            ))
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_default_step (lines 754-792) ---[0m
    async fn execute_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("pool_step_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Get best agent for this task
        let best_agent = self.get_best_agent_for_task(&task).await?;
        let agent_type = {
            let agent_guard = best_agent.read().await;
            agent_guard.identity.specialization.name().to_lowercase()
        };

        // Execute with selected agent
        match self.execute_task_with_agent(&agent_type, &task).await {
            Ok(result) => Ok(StepResult::new(step.id.clone())
                .with_summary(format!(
                    "Task executed successfully by {} agent",
                    agent_type
                ))
                .add_output("agent_used".to_string(), agent_type)
                .add_output("task_success".to_string(), result.success.to_string())),
            Err(e) => {
                Ok(StepResult::new(step.id.clone())
                    .failed(format!("Default execution failed: {}", e)))
            }
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:250-280 method execute_command_with_agent <-> ./crates/ccswarm/src/agent/pool.rs:586-632 method synthesize_results
  Similarity: 89.86%
  Classes: AgentPool <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_command_with_agent (lines 250-280) ---[0m
    pub async fn execute_command_with_agent(
        &self,
        agent_type: &str,
        command: &str,
    ) -> Result<CommandResult> {
        info!("🔧 {} executing: {}", agent_type, command);

        // Get agent session
        let session = self
            .sessions
            .get(agent_type)
            .ok_or_else(|| anyhow::anyhow!("No session for agent: {}", agent_type))?;

        // Execute command through session
        let mut session_guard = session.write().await;
        let output = session_guard.execute_command(command).await?;

        // Parse output for test results if applicable
        let (passed_tests, total_tests) = if command.contains("npm test") {
            parse_test_results(&output)
        } else {
            (0, 0)
        };

        Ok(CommandResult {
            success: true,
            output,
            passed_tests,
            total_tests,
        })
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:synthesize_results (lines 586-632) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Pool orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Pool orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let output = serde_json::json!({
            "orchestration_type": "pool",
            "task_id": task.id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:66-116 method spawn_agent <-> ./crates/ccswarm/src/agent/pool.rs:754-792 method execute_default_step
  Similarity: 77.61%
  Classes: AgentPool <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:spawn_agent (lines 66-116) ---[0m
    pub async fn spawn_agent(&mut self, agent_type: &str, config: &CcswarmConfig) -> Result<()> {
        info!("🚀 Spawning {} agent", agent_type);

        // Get role for agent type
        let role = match agent_type.to_lowercase().as_str() {
            "frontend" => default_frontend_role(),
            "backend" => default_backend_role(),
            "devops" => default_devops_role(),
            "qa" => default_qa_role(),
            _ => return Err(anyhow::anyhow!("Unknown agent type: {}", agent_type)),
        };

        // Get agent config
        let agent_config = config
            .agents
            .get(agent_type)
            .ok_or_else(|| anyhow::anyhow!("No configuration for agent: {}", agent_type))?;

        // Create agent
        let mut agent = ClaudeCodeAgent::new(
            role,
            &PathBuf::from(&config.project.name),
            &agent_config.branch,
            agent_config.claude_config.clone(),
        )
        .await?;

        // Initialize agent
        agent.initialize().await?;

        // Create persistent session for agent
        let mut session = PersistentClaudeSession::new(
            agent.identity.clone(),
            agent.worktree_path.clone(),
            agent_config.claude_config.clone(),
        )
        .await?;

        // Initialize session
        session.initialize().await?;

        // Store agent and session
        self.agents
            .insert(agent_type.to_string(), Arc::new(RwLock::new(agent)));

        self.sessions
            .insert(agent_type.to_string(), Arc::new(RwLock::new(session)));

        info!("✅ {} agent spawned and initialized", agent_type);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_default_step (lines 754-792) ---[0m
    async fn execute_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("pool_step_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Get best agent for this task
        let best_agent = self.get_best_agent_for_task(&task).await?;
        let agent_type = {
            let agent_guard = best_agent.read().await;
            agent_guard.identity.specialization.name().to_lowercase()
        };

        // Execute with selected agent
        match self.execute_task_with_agent(&agent_type, &task).await {
            Ok(result) => Ok(StepResult::new(step.id.clone())
                .with_summary(format!(
                    "Task executed successfully by {} agent",
                    agent_type
                ))
                .add_output("agent_used".to_string(), agent_type)
                .add_output("task_success".to_string(), result.success.to_string())),
            Err(e) => {
                Ok(StepResult::new(step.id.clone())
                    .failed(format!("Default execution failed: {}", e)))
            }
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:66-116 method spawn_agent <-> ./crates/ccswarm/src/agent/pool.rs:312-345 method orchestrate_task
  Similarity: 81.09%
  Classes: AgentPool <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/pool.rs:spawn_agent (lines 66-116) ---[0m
    pub async fn spawn_agent(&mut self, agent_type: &str, config: &CcswarmConfig) -> Result<()> {
        info!("🚀 Spawning {} agent", agent_type);

        // Get role for agent type
        let role = match agent_type.to_lowercase().as_str() {
            "frontend" => default_frontend_role(),
            "backend" => default_backend_role(),
            "devops" => default_devops_role(),
            "qa" => default_qa_role(),
            _ => return Err(anyhow::anyhow!("Unknown agent type: {}", agent_type)),
        };

        // Get agent config
        let agent_config = config
            .agents
            .get(agent_type)
            .ok_or_else(|| anyhow::anyhow!("No configuration for agent: {}", agent_type))?;

        // Create agent
        let mut agent = ClaudeCodeAgent::new(
            role,
            &PathBuf::from(&config.project.name),
            &agent_config.branch,
            agent_config.claude_config.clone(),
        )
        .await?;

        // Initialize agent
        agent.initialize().await?;

        // Create persistent session for agent
        let mut session = PersistentClaudeSession::new(
            agent.identity.clone(),
            agent.worktree_path.clone(),
            agent_config.claude_config.clone(),
        )
        .await?;

        // Initialize session
        session.initialize().await?;

        // Store agent and session
        self.agents
            .insert(agent_type.to_string(), Arc::new(RwLock::new(agent)));

        self.sessions
            .insert(agent_type.to_string(), Arc::new(RwLock::new(session)));

        info!("✅ {} agent spawned and initialized", agent_type);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:orchestrate_task (lines 312-345) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!("🎭 Pool orchestrating task: {}", task.description);

        // Analyze task to create execution plan
        let plan = self.analyze_task(task).await?;

        // Execute plan with parallel agent coordination
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("pool_mode".to_string(), "true".to_string());

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Pool step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Pool orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({"orchestration": "pool_success", "steps_completed": plan.steps.len()}),
            Duration::from_secs(10),
        ))
    }

  ./crates/ccswarm/src/agent/pool.rs:312-345 method orchestrate_task <-> ./crates/ccswarm/src/agent/pool.rs:537-584 method execute_parallel_task
  Similarity: 82.41%
  Classes: AgentOrchestrator <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/pool.rs:orchestrate_task (lines 312-345) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!("🎭 Pool orchestrating task: {}", task.description);

        // Analyze task to create execution plan
        let plan = self.analyze_task(task).await?;

        // Execute plan with parallel agent coordination
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("pool_mode".to_string(), "true".to_string());

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Pool step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Pool orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({"orchestration": "pool_success", "steps_completed": plan.steps.len()}),
            Duration::from_secs(10),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_parallel_task (lines 537-584) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task across available agents
        let available_agents: Vec<_> = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        if available_agents.is_empty() {
            return Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: false,
                output: "No available agents".to_string(),
                error: Some("No agents available for parallel task execution".to_string()),
            });
        }

        // Use first available agent for the parallel task
        let agent_type = &available_agents[0];
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        match self
            .execute_task_with_agent(agent_type, &execution_task)
            .await
        {
            Ok(result) => Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: result.success,
                output: serde_json::to_string(&result.output).unwrap_or_default(),
                error: result.error,
            }),
            Err(e) => Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: false,
                output: String::new(),
                error: Some(e.to_string()),
            }),
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:312-345 method orchestrate_task <-> ./crates/ccswarm/src/agent/pool.rs:754-792 method execute_default_step
  Similarity: 90.03%
  Classes: AgentOrchestrator <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:orchestrate_task (lines 312-345) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!("🎭 Pool orchestrating task: {}", task.description);

        // Analyze task to create execution plan
        let plan = self.analyze_task(task).await?;

        // Execute plan with parallel agent coordination
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("pool_mode".to_string(), "true".to_string());

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Pool step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Pool orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({"orchestration": "pool_success", "steps_completed": plan.steps.len()}),
            Duration::from_secs(10),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_default_step (lines 754-792) ---[0m
    async fn execute_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("pool_step_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Get best agent for this task
        let best_agent = self.get_best_agent_for_task(&task).await?;
        let agent_type = {
            let agent_guard = best_agent.read().await;
            agent_guard.identity.specialization.name().to_lowercase()
        };

        // Execute with selected agent
        match self.execute_task_with_agent(&agent_type, &task).await {
            Ok(result) => Ok(StepResult::new(step.id.clone())
                .with_summary(format!(
                    "Task executed successfully by {} agent",
                    agent_type
                ))
                .add_output("agent_used".to_string(), agent_type)
                .add_output("task_success".to_string(), result.success.to_string())),
            Err(e) => {
                Ok(StepResult::new(step.id.clone())
                    .failed(format!("Default execution failed: {}", e)))
            }
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:66-116 method spawn_agent <-> ./crates/ccswarm/src/agent/pool.rs:715-751 method execute_integration_test_step
  Similarity: 74.63%
  Classes: AgentPool <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:spawn_agent (lines 66-116) ---[0m
    pub async fn spawn_agent(&mut self, agent_type: &str, config: &CcswarmConfig) -> Result<()> {
        info!("🚀 Spawning {} agent", agent_type);

        // Get role for agent type
        let role = match agent_type.to_lowercase().as_str() {
            "frontend" => default_frontend_role(),
            "backend" => default_backend_role(),
            "devops" => default_devops_role(),
            "qa" => default_qa_role(),
            _ => return Err(anyhow::anyhow!("Unknown agent type: {}", agent_type)),
        };

        // Get agent config
        let agent_config = config
            .agents
            .get(agent_type)
            .ok_or_else(|| anyhow::anyhow!("No configuration for agent: {}", agent_type))?;

        // Create agent
        let mut agent = ClaudeCodeAgent::new(
            role,
            &PathBuf::from(&config.project.name),
            &agent_config.branch,
            agent_config.claude_config.clone(),
        )
        .await?;

        // Initialize agent
        agent.initialize().await?;

        // Create persistent session for agent
        let mut session = PersistentClaudeSession::new(
            agent.identity.clone(),
            agent.worktree_path.clone(),
            agent_config.claude_config.clone(),
        )
        .await?;

        // Initialize session
        session.initialize().await?;

        // Store agent and session
        self.agents
            .insert(agent_type.to_string(), Arc::new(RwLock::new(agent)));

        self.sessions
            .insert(agent_type.to_string(), Arc::new(RwLock::new(session)));

        info!("✅ {} agent spawned and initialized", agent_type);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_integration_test_step (lines 715-751) ---[0m
    async fn execute_integration_test_step(
        &self,
        _step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        // Test communication between agents
        let agent_types: Vec<_> = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        let mut test_results = Vec::new();

        // Test inter-agent communication
        for i in 0..agent_types.len() {
            for j in (i + 1)..agent_types.len() {
                let from = &agent_types[i];
                let to = &agent_types[j];

                match self.send_message(from, to, "integration_test").await {
                    Ok(_) => test_results.push(format!("{} -> {} ✅", from, to)),
                    Err(e) => test_results.push(format!("{} -> {} ❌: {}", from, to, e)),
                }
            }
        }

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!(
                "Integration test: {} agent pairs tested",
                test_results.len()
            ))
            .add_output(
                "agent_pairs_tested".to_string(),
                test_results.len().to_string(),
            ))
    }

  ./crates/ccswarm/src/agent/pool.rs:496-535 method review_and_adapt <-> ./crates/ccswarm/src/agent/pool.rs:715-751 method execute_integration_test_step
  Similarity: 80.09%
  Classes: AgentOrchestrator <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:review_and_adapt (lines 496-535) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Pool reviewing and adapting plan based on {} results",
            results.len()
        );

        // Check if any agents failed and need to be reassigned
        let failed_agents: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_agents.is_empty() {
            warn!(
                "Pool detected {} failed agent executions",
                failed_agents.len()
            );

            // Add recovery steps for failed agents
            let recovery_step = TaskStep::new(
                "agent_recovery".to_string(),
                "Recover failed agents".to_string(),
                StepType::Execution,
            )
            .with_description("Restart and recover failed agents".to_string());

            let validation_step = TaskStep::new(
                "recovery_validation".to_string(),
                "Validate agent recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Verify all agents are operational".to_string());

            current_plan.steps.push(recovery_step);
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_integration_test_step (lines 715-751) ---[0m
    async fn execute_integration_test_step(
        &self,
        _step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        // Test communication between agents
        let agent_types: Vec<_> = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        let mut test_results = Vec::new();

        // Test inter-agent communication
        for i in 0..agent_types.len() {
            for j in (i + 1)..agent_types.len() {
                let from = &agent_types[i];
                let to = &agent_types[j];

                match self.send_message(from, to, "integration_test").await {
                    Ok(_) => test_results.push(format!("{} -> {} ✅", from, to)),
                    Err(e) => test_results.push(format!("{} -> {} ❌: {}", from, to, e)),
                }
            }
        }

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!(
                "Integration test: {} agent pairs tested",
                test_results.len()
            ))
            .add_output(
                "agent_pairs_tested".to_string(),
                test_results.len().to_string(),
            ))
    }

  ./crates/ccswarm/src/agent/pool.rs:537-584 method execute_parallel_task <-> ./crates/ccswarm/src/agent/pool.rs:691-712 method execute_agent_selection_step
  Similarity: 85.94%
  Classes: AgentOrchestrator <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_parallel_task (lines 537-584) ---[0m
    async fn execute_parallel_task(
        &self,
        task: &ParallelTask,
        _context: &HashMap<String, String>,
    ) -> Result<ParallelTaskResult> {
        // Execute parallel task across available agents
        let available_agents: Vec<_> = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        if available_agents.is_empty() {
            return Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: false,
                output: "No available agents".to_string(),
                error: Some("No agents available for parallel task execution".to_string()),
            });
        }

        // Use first available agent for the parallel task
        let agent_type = &available_agents[0];
        let execution_task = Task::new(
            task.id.clone(),
            task.name.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        match self
            .execute_task_with_agent(agent_type, &execution_task)
            .await
        {
            Ok(result) => Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: result.success,
                output: serde_json::to_string(&result.output).unwrap_or_default(),
                error: result.error,
            }),
            Err(e) => Ok(ParallelTaskResult {
                task_id: task.id.clone(),
                success: false,
                output: String::new(),
                error: Some(e.to_string()),
            }),
        }
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_agent_selection_step (lines 691-712) ---[0m
    async fn execute_agent_selection_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Count available agents by type
        let mut agent_counts = HashMap::new();
        for entry in self.agents.iter() {
            let agent_type = entry.key().clone();
            *agent_counts.entry(agent_type).or_insert(0) += 1;
        }

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Selected agents for task {}", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("total_agents".to_string(), self.agents.len().to_string()))
    }

  ./crates/ccswarm/src/agent/pool.rs:250-280 method execute_command_with_agent <-> ./crates/ccswarm/src/agent/pool.rs:715-751 method execute_integration_test_step
  Similarity: 86.90%
  Classes: AgentPool <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_command_with_agent (lines 250-280) ---[0m
    pub async fn execute_command_with_agent(
        &self,
        agent_type: &str,
        command: &str,
    ) -> Result<CommandResult> {
        info!("🔧 {} executing: {}", agent_type, command);

        // Get agent session
        let session = self
            .sessions
            .get(agent_type)
            .ok_or_else(|| anyhow::anyhow!("No session for agent: {}", agent_type))?;

        // Execute command through session
        let mut session_guard = session.write().await;
        let output = session_guard.execute_command(command).await?;

        // Parse output for test results if applicable
        let (passed_tests, total_tests) = if command.contains("npm test") {
            parse_test_results(&output)
        } else {
            (0, 0)
        };

        Ok(CommandResult {
            success: true,
            output,
            passed_tests,
            total_tests,
        })
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_integration_test_step (lines 715-751) ---[0m
    async fn execute_integration_test_step(
        &self,
        _step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        // Test communication between agents
        let agent_types: Vec<_> = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        let mut test_results = Vec::new();

        // Test inter-agent communication
        for i in 0..agent_types.len() {
            for j in (i + 1)..agent_types.len() {
                let from = &agent_types[i];
                let to = &agent_types[j];

                match self.send_message(from, to, "integration_test").await {
                    Ok(_) => test_results.push(format!("{} -> {} ✅", from, to)),
                    Err(e) => test_results.push(format!("{} -> {} ❌: {}", from, to, e)),
                }
            }
        }

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!(
                "Integration test: {} agent pairs tested",
                test_results.len()
            ))
            .add_output(
                "agent_pairs_tested".to_string(),
                test_results.len().to_string(),
            ))
    }

  ./crates/ccswarm/src/agent/pool.rs:586-632 method synthesize_results <-> ./crates/ccswarm/src/agent/pool.rs:691-712 method execute_agent_selection_step
  Similarity: 83.65%
  Classes: AgentOrchestrator <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:synthesize_results (lines 586-632) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Pool orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Pool orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let output = serde_json::json!({
            "orchestration_type": "pool",
            "task_id": task.id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_agent_selection_step (lines 691-712) ---[0m
    async fn execute_agent_selection_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Count available agents by type
        let mut agent_counts = HashMap::new();
        for entry in self.agents.iter() {
            let agent_type = entry.key().clone();
            *agent_counts.entry(agent_type).or_insert(0) += 1;
        }

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Selected agents for task {}", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("total_agents".to_string(), self.agents.len().to_string()))
    }

  ./crates/ccswarm/src/agent/pool.rs:312-345 method orchestrate_task <-> ./crates/ccswarm/src/agent/pool.rs:586-632 method synthesize_results
  Similarity: 71.06%
  Classes: AgentOrchestrator <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/pool.rs:orchestrate_task (lines 312-345) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!("🎭 Pool orchestrating task: {}", task.description);

        // Analyze task to create execution plan
        let plan = self.analyze_task(task).await?;

        // Execute plan with parallel agent coordination
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("pool_mode".to_string(), "true".to_string());

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Pool step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Pool orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({"orchestration": "pool_success", "steps_completed": plan.steps.len()}),
            Duration::from_secs(10),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:synthesize_results (lines 586-632) ---[0m
    async fn synthesize_results(
        &self,
        task: &Task,
        results: Vec<StepResult>,
    ) -> Result<TaskResult> {
        let total_steps = results.len();
        let successful_steps = results.iter().filter(|r| r.is_success()).count();
        let overall_success = successful_steps == total_steps;

        let summary = if overall_success {
            format!(
                "Pool orchestration completed successfully: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        } else {
            format!(
                "Pool orchestration partially failed: {}/{} steps succeeded",
                successful_steps, total_steps
            )
        };

        let output = serde_json::json!({
            "orchestration_type": "pool",
            "task_id": task.id,
            "total_steps": total_steps,
            "successful_steps": successful_steps,
            "step_results": results.iter().map(|r| {
                serde_json::json!({
                    "step_id": r.step_id,
                    "success": r.success,
                    "summary": r.summary
                })
            }).collect::<Vec<_>>()
        });

        if overall_success {
            Ok(TaskResult::success(
                output,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        } else {
            Ok(TaskResult::failure(
                summary,
                Duration::from_millis(results.iter().map(|r| r.duration_ms).sum()),
            ))
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:312-345 method orchestrate_task <-> ./crates/ccswarm/src/agent/pool.rs:715-751 method execute_integration_test_step
  Similarity: 80.61%
  Classes: AgentOrchestrator <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:orchestrate_task (lines 312-345) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!("🎭 Pool orchestrating task: {}", task.description);

        // Analyze task to create execution plan
        let plan = self.analyze_task(task).await?;

        // Execute plan with parallel agent coordination
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("pool_mode".to_string(), "true".to_string());

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Pool step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Pool orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({"orchestration": "pool_success", "steps_completed": plan.steps.len()}),
            Duration::from_secs(10),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_integration_test_step (lines 715-751) ---[0m
    async fn execute_integration_test_step(
        &self,
        _step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        // Test communication between agents
        let agent_types: Vec<_> = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        let mut test_results = Vec::new();

        // Test inter-agent communication
        for i in 0..agent_types.len() {
            for j in (i + 1)..agent_types.len() {
                let from = &agent_types[i];
                let to = &agent_types[j];

                match self.send_message(from, to, "integration_test").await {
                    Ok(_) => test_results.push(format!("{} -> {} ✅", from, to)),
                    Err(e) => test_results.push(format!("{} -> {} ❌: {}", from, to, e)),
                }
            }
        }

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!(
                "Integration test: {} agent pairs tested",
                test_results.len()
            ))
            .add_output(
                "agent_pairs_tested".to_string(),
                test_results.len().to_string(),
            ))
    }

  ./crates/ccswarm/src/agent/pool.rs:468-494 method execute_step <-> ./crates/ccswarm/src/agent/pool.rs:496-535 method review_and_adapt
  Similarity: 85.40%
  Classes: AgentOrchestrator <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_step (lines 468-494) ---[0m
    async fn execute_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        info!("🎭 Pool executing step: {}", step.name);

        // Match on step name for custom pool behavior
        match step.name.as_str() {
            "Parallel implementation" | "Execute parallel tests" => {
                // Execute parallel tasks across multiple agents
                self.execute_parallel_step(step, context).await
            }
            "Select optimal agents for task" => {
                // Select best agents for the task
                self.execute_agent_selection_step(step, context).await
            }
            "Test agent integration" => {
                // Test integration between agents
                self.execute_integration_test_step(step, context).await
            }
            _ => {
                // Default execution - delegate to best agent
                self.execute_default_step(step, context).await
            }
        }
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:review_and_adapt (lines 496-535) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Pool reviewing and adapting plan based on {} results",
            results.len()
        );

        // Check if any agents failed and need to be reassigned
        let failed_agents: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_agents.is_empty() {
            warn!(
                "Pool detected {} failed agent executions",
                failed_agents.len()
            );

            // Add recovery steps for failed agents
            let recovery_step = TaskStep::new(
                "agent_recovery".to_string(),
                "Recover failed agents".to_string(),
                StepType::Execution,
            )
            .with_description("Restart and recover failed agents".to_string());

            let validation_step = TaskStep::new(
                "recovery_validation".to_string(),
                "Validate agent recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Verify all agents are operational".to_string());

            current_plan.steps.push(recovery_step);
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

  ./crates/ccswarm/src/agent/pool.rs:250-280 method execute_command_with_agent <-> ./crates/ccswarm/src/agent/pool.rs:312-345 method orchestrate_task
  Similarity: 85.58%
  Classes: AgentPool <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_command_with_agent (lines 250-280) ---[0m
    pub async fn execute_command_with_agent(
        &self,
        agent_type: &str,
        command: &str,
    ) -> Result<CommandResult> {
        info!("🔧 {} executing: {}", agent_type, command);

        // Get agent session
        let session = self
            .sessions
            .get(agent_type)
            .ok_or_else(|| anyhow::anyhow!("No session for agent: {}", agent_type))?;

        // Execute command through session
        let mut session_guard = session.write().await;
        let output = session_guard.execute_command(command).await?;

        // Parse output for test results if applicable
        let (passed_tests, total_tests) = if command.contains("npm test") {
            parse_test_results(&output)
        } else {
            (0, 0)
        };

        Ok(CommandResult {
            success: true,
            output,
            passed_tests,
            total_tests,
        })
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:orchestrate_task (lines 312-345) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!("🎭 Pool orchestrating task: {}", task.description);

        // Analyze task to create execution plan
        let plan = self.analyze_task(task).await?;

        // Execute plan with parallel agent coordination
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("pool_mode".to_string(), "true".to_string());

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Pool step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Pool orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({"orchestration": "pool_success", "steps_completed": plan.steps.len()}),
            Duration::from_secs(10),
        ))
    }

  ./crates/ccswarm/src/agent/pool.rs:691-712 method execute_agent_selection_step <-> ./crates/ccswarm/src/agent/pool.rs:754-792 method execute_default_step
  Similarity: 90.96%
  Classes: AgentPool <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_agent_selection_step (lines 691-712) ---[0m
    async fn execute_agent_selection_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Count available agents by type
        let mut agent_counts = HashMap::new();
        for entry in self.agents.iter() {
            let agent_type = entry.key().clone();
            *agent_counts.entry(agent_type).or_insert(0) += 1;
        }

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Selected agents for task {}", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("total_agents".to_string(), self.agents.len().to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_default_step (lines 754-792) ---[0m
    async fn execute_default_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        let task = Task::new(
            format!("pool_step_{}_{}", step.id, task_id),
            step.description.clone(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        // Get best agent for this task
        let best_agent = self.get_best_agent_for_task(&task).await?;
        let agent_type = {
            let agent_guard = best_agent.read().await;
            agent_guard.identity.specialization.name().to_lowercase()
        };

        // Execute with selected agent
        match self.execute_task_with_agent(&agent_type, &task).await {
            Ok(result) => Ok(StepResult::new(step.id.clone())
                .with_summary(format!(
                    "Task executed successfully by {} agent",
                    agent_type
                ))
                .add_output("agent_used".to_string(), agent_type)
                .add_output("task_success".to_string(), result.success.to_string())),
            Err(e) => {
                Ok(StepResult::new(step.id.clone())
                    .failed(format!("Default execution failed: {}", e)))
            }
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:250-280 method execute_command_with_agent <-> ./crates/ccswarm/src/agent/pool.rs:496-535 method review_and_adapt
  Similarity: 76.92%
  Classes: AgentPool <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_command_with_agent (lines 250-280) ---[0m
    pub async fn execute_command_with_agent(
        &self,
        agent_type: &str,
        command: &str,
    ) -> Result<CommandResult> {
        info!("🔧 {} executing: {}", agent_type, command);

        // Get agent session
        let session = self
            .sessions
            .get(agent_type)
            .ok_or_else(|| anyhow::anyhow!("No session for agent: {}", agent_type))?;

        // Execute command through session
        let mut session_guard = session.write().await;
        let output = session_guard.execute_command(command).await?;

        // Parse output for test results if applicable
        let (passed_tests, total_tests) = if command.contains("npm test") {
            parse_test_results(&output)
        } else {
            (0, 0)
        };

        Ok(CommandResult {
            success: true,
            output,
            passed_tests,
            total_tests,
        })
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:review_and_adapt (lines 496-535) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Pool reviewing and adapting plan based on {} results",
            results.len()
        );

        // Check if any agents failed and need to be reassigned
        let failed_agents: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_agents.is_empty() {
            warn!(
                "Pool detected {} failed agent executions",
                failed_agents.len()
            );

            // Add recovery steps for failed agents
            let recovery_step = TaskStep::new(
                "agent_recovery".to_string(),
                "Recover failed agents".to_string(),
                StepType::Execution,
            )
            .with_description("Restart and recover failed agents".to_string());

            let validation_step = TaskStep::new(
                "recovery_validation".to_string(),
                "Validate agent recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Verify all agents are operational".to_string());

            current_plan.steps.push(recovery_step);
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

  ./crates/ccswarm/src/agent/pool.rs:691-712 method execute_agent_selection_step <-> ./crates/ccswarm/src/agent/pool.rs:715-751 method execute_integration_test_step
  Similarity: 92.47%
  Classes: AgentPool <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_agent_selection_step (lines 691-712) ---[0m
    async fn execute_agent_selection_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Count available agents by type
        let mut agent_counts = HashMap::new();
        for entry in self.agents.iter() {
            let agent_type = entry.key().clone();
            *agent_counts.entry(agent_type).or_insert(0) += 1;
        }

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Selected agents for task {}", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("total_agents".to_string(), self.agents.len().to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_integration_test_step (lines 715-751) ---[0m
    async fn execute_integration_test_step(
        &self,
        _step: &TaskStep,
        _context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        // Test communication between agents
        let agent_types: Vec<_> = self
            .agents
            .iter()
            .map(|entry| entry.key().clone())
            .collect();

        let mut test_results = Vec::new();

        // Test inter-agent communication
        for i in 0..agent_types.len() {
            for j in (i + 1)..agent_types.len() {
                let from = &agent_types[i];
                let to = &agent_types[j];

                match self.send_message(from, to, "integration_test").await {
                    Ok(_) => test_results.push(format!("{} -> {} ✅", from, to)),
                    Err(e) => test_results.push(format!("{} -> {} ❌: {}", from, to, e)),
                }
            }
        }

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!(
                "Integration test: {} agent pairs tested",
                test_results.len()
            ))
            .add_output(
                "agent_pairs_tested".to_string(),
                test_results.len().to_string(),
            ))
    }

  ./crates/ccswarm/src/agent/pool.rs:312-345 method orchestrate_task <-> ./crates/ccswarm/src/agent/pool.rs:496-535 method review_and_adapt
  Similarity: 73.72%
  Classes: AgentOrchestrator <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/pool.rs:orchestrate_task (lines 312-345) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!("🎭 Pool orchestrating task: {}", task.description);

        // Analyze task to create execution plan
        let plan = self.analyze_task(task).await?;

        // Execute plan with parallel agent coordination
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("pool_mode".to_string(), "true".to_string());

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Pool step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Pool orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({"orchestration": "pool_success", "steps_completed": plan.steps.len()}),
            Duration::from_secs(10),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:review_and_adapt (lines 496-535) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Pool reviewing and adapting plan based on {} results",
            results.len()
        );

        // Check if any agents failed and need to be reassigned
        let failed_agents: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_agents.is_empty() {
            warn!(
                "Pool detected {} failed agent executions",
                failed_agents.len()
            );

            // Add recovery steps for failed agents
            let recovery_step = TaskStep::new(
                "agent_recovery".to_string(),
                "Recover failed agents".to_string(),
                StepType::Execution,
            )
            .with_description("Restart and recover failed agents".to_string());

            let validation_step = TaskStep::new(
                "recovery_validation".to_string(),
                "Validate agent recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Verify all agents are operational".to_string());

            current_plan.steps.push(recovery_step);
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

  ./crates/ccswarm/src/agent/pool.rs:496-535 method review_and_adapt <-> ./crates/ccswarm/src/agent/pool.rs:691-712 method execute_agent_selection_step
  Similarity: 82.02%
  Classes: AgentOrchestrator <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:review_and_adapt (lines 496-535) ---[0m
    async fn review_and_adapt(
        &self,
        results: &[StepResult],
        current_plan: &mut TaskPlan,
    ) -> Result<TaskPlan> {
        info!(
            "🎭 Pool reviewing and adapting plan based on {} results",
            results.len()
        );

        // Check if any agents failed and need to be reassigned
        let failed_agents: Vec<_> = results.iter().filter(|r| !r.success).collect();

        if !failed_agents.is_empty() {
            warn!(
                "Pool detected {} failed agent executions",
                failed_agents.len()
            );

            // Add recovery steps for failed agents
            let recovery_step = TaskStep::new(
                "agent_recovery".to_string(),
                "Recover failed agents".to_string(),
                StepType::Execution,
            )
            .with_description("Restart and recover failed agents".to_string());

            let validation_step = TaskStep::new(
                "recovery_validation".to_string(),
                "Validate agent recovery".to_string(),
                StepType::Validation,
            )
            .with_description("Verify all agents are operational".to_string());

            current_plan.steps.push(recovery_step);
            current_plan.steps.push(validation_step);
        }

        Ok(current_plan.clone())
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_agent_selection_step (lines 691-712) ---[0m
    async fn execute_agent_selection_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Count available agents by type
        let mut agent_counts = HashMap::new();
        for entry in self.agents.iter() {
            let agent_type = entry.key().clone();
            *agent_counts.entry(agent_type).or_insert(0) += 1;
        }

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Selected agents for task {}", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("total_agents".to_string(), self.agents.len().to_string()))
    }

  ./crates/ccswarm/src/agent/pool.rs:312-345 method orchestrate_task <-> ./crates/ccswarm/src/agent/pool.rs:691-712 method execute_agent_selection_step
  Similarity: 80.09%
  Classes: AgentOrchestrator <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:orchestrate_task (lines 312-345) ---[0m
    async fn orchestrate_task(&self, task: &Task) -> Result<TaskResult> {
        info!("🎭 Pool orchestrating task: {}", task.description);

        // Analyze task to create execution plan
        let plan = self.analyze_task(task).await?;

        // Execute plan with parallel agent coordination
        let mut context = HashMap::new();
        context.insert("task_id".to_string(), task.id.clone());
        context.insert("pool_mode".to_string(), "true".to_string());

        for step in &plan.steps {
            let step_result = self.execute_step(step, &context).await?;

            // Update context with step results
            context.insert(
                format!("step_{}_result", step.id),
                serde_json::to_string(&step_result.outputs)?,
            );

            if !step_result.success {
                warn!("Pool step failed: {}", step.name);
                return Ok(TaskResult::failure(
                    format!("Pool orchestration failed at step: {}", step.name),
                    Duration::from_secs(0),
                ));
            }
        }

        Ok(TaskResult::success(
            serde_json::json!({"orchestration": "pool_success", "steps_completed": plan.steps.len()}),
            Duration::from_secs(10),
        ))
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_agent_selection_step (lines 691-712) ---[0m
    async fn execute_agent_selection_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Count available agents by type
        let mut agent_counts = HashMap::new();
        for entry in self.agents.iter() {
            let agent_type = entry.key().clone();
            *agent_counts.entry(agent_type).or_insert(0) += 1;
        }

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Selected agents for task {}", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("total_agents".to_string(), self.agents.len().to_string()))
    }

  ./crates/ccswarm/src/agent/pool.rs:468-494 method execute_step <-> ./crates/ccswarm/src/agent/pool.rs:691-712 method execute_agent_selection_step
  Similarity: 85.67%
  Classes: AgentOrchestrator <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_step (lines 468-494) ---[0m
    async fn execute_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        info!("🎭 Pool executing step: {}", step.name);

        // Match on step name for custom pool behavior
        match step.name.as_str() {
            "Parallel implementation" | "Execute parallel tests" => {
                // Execute parallel tasks across multiple agents
                self.execute_parallel_step(step, context).await
            }
            "Select optimal agents for task" => {
                // Select best agents for the task
                self.execute_agent_selection_step(step, context).await
            }
            "Test agent integration" => {
                // Test integration between agents
                self.execute_integration_test_step(step, context).await
            }
            _ => {
                // Default execution - delegate to best agent
                self.execute_default_step(step, context).await
            }
        }
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_agent_selection_step (lines 691-712) ---[0m
    async fn execute_agent_selection_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Count available agents by type
        let mut agent_counts = HashMap::new();
        for entry in self.agents.iter() {
            let agent_type = entry.key().clone();
            *agent_counts.entry(agent_type).or_insert(0) += 1;
        }

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Selected agents for task {}", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("total_agents".to_string(), self.agents.len().to_string()))
    }

  ./crates/ccswarm/src/agent/pool.rs:217-233 method send_message <-> ./crates/ccswarm/src/agent/pool.rs:250-280 method execute_command_with_agent
  Similarity: 87.14%
  Classes: AgentPool <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:send_message (lines 217-233) ---[0m
    pub async fn send_message(&self, from: &str, to: &str, message: &str) -> Result<String> {
        info!("💬 {} → {}: {}", from, to, message);

        // Create inter-agent message
        let msg = AgentMessage::InterAgentMessage {
            from_agent: from.to_string(),
            to_agent: to.to_string(),
            message: message.to_string(),
            timestamp: chrono::Utc::now(),
        };

        // Send through coordination bus
        self.coordination_bus.send_message(msg).await?;

        // Simulate response (in real implementation, would wait for actual response)
        Ok(format!("Acknowledged: {}", message))
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_command_with_agent (lines 250-280) ---[0m
    pub async fn execute_command_with_agent(
        &self,
        agent_type: &str,
        command: &str,
    ) -> Result<CommandResult> {
        info!("🔧 {} executing: {}", agent_type, command);

        // Get agent session
        let session = self
            .sessions
            .get(agent_type)
            .ok_or_else(|| anyhow::anyhow!("No session for agent: {}", agent_type))?;

        // Execute command through session
        let mut session_guard = session.write().await;
        let output = session_guard.execute_command(command).await?;

        // Parse output for test results if applicable
        let (passed_tests, total_tests) = if command.contains("npm test") {
            parse_test_results(&output)
        } else {
            (0, 0)
        };

        Ok(CommandResult {
            success: true,
            output,
            passed_tests,
            total_tests,
        })
    }

  ./crates/ccswarm/src/agent/pool.rs:250-280 method execute_command_with_agent <-> ./crates/ccswarm/src/agent/pool.rs:691-712 method execute_agent_selection_step
  Similarity: 73.97%
  Classes: AgentPool <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_command_with_agent (lines 250-280) ---[0m
    pub async fn execute_command_with_agent(
        &self,
        agent_type: &str,
        command: &str,
    ) -> Result<CommandResult> {
        info!("🔧 {} executing: {}", agent_type, command);

        // Get agent session
        let session = self
            .sessions
            .get(agent_type)
            .ok_or_else(|| anyhow::anyhow!("No session for agent: {}", agent_type))?;

        // Execute command through session
        let mut session_guard = session.write().await;
        let output = session_guard.execute_command(command).await?;

        // Parse output for test results if applicable
        let (passed_tests, total_tests) = if command.contains("npm test") {
            parse_test_results(&output)
        } else {
            (0, 0)
        };

        Ok(CommandResult {
            success: true,
            output,
            passed_tests,
            total_tests,
        })
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_agent_selection_step (lines 691-712) ---[0m
    async fn execute_agent_selection_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Count available agents by type
        let mut agent_counts = HashMap::new();
        for entry in self.agents.iter() {
            let agent_type = entry.key().clone();
            *agent_counts.entry(agent_type).or_insert(0) += 1;
        }

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Selected agents for task {}", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("total_agents".to_string(), self.agents.len().to_string()))
    }

  ./crates/ccswarm/src/agent/pool.rs:236-247 method broadcast_message <-> ./crates/ccswarm/src/agent/pool.rs:468-494 method execute_step
  Similarity: 94.30%
  Classes: AgentPool <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/pool.rs:broadcast_message (lines 236-247) ---[0m
    pub async fn broadcast_message(&self, from: &str, message: &str) -> Result<()> {
        info!("📢 {} → all: {}", from, message);

        for entry in self.agents.iter() {
            let to_agent = entry.key();
            if to_agent != from {
                self.send_message(from, to_agent, message).await?;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_step (lines 468-494) ---[0m
    async fn execute_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        info!("🎭 Pool executing step: {}", step.name);

        // Match on step name for custom pool behavior
        match step.name.as_str() {
            "Parallel implementation" | "Execute parallel tests" => {
                // Execute parallel tasks across multiple agents
                self.execute_parallel_step(step, context).await
            }
            "Select optimal agents for task" => {
                // Select best agents for the task
                self.execute_agent_selection_step(step, context).await
            }
            "Test agent integration" => {
                // Test integration between agents
                self.execute_integration_test_step(step, context).await
            }
            _ => {
                // Default execution - delegate to best agent
                self.execute_default_step(step, context).await
            }
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:217-233 method send_message <-> ./crates/ccswarm/src/agent/pool.rs:691-712 method execute_agent_selection_step
  Similarity: 89.78%
  Classes: AgentPool <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:send_message (lines 217-233) ---[0m
    pub async fn send_message(&self, from: &str, to: &str, message: &str) -> Result<String> {
        info!("💬 {} → {}: {}", from, to, message);

        // Create inter-agent message
        let msg = AgentMessage::InterAgentMessage {
            from_agent: from.to_string(),
            to_agent: to.to_string(),
            message: message.to_string(),
            timestamp: chrono::Utc::now(),
        };

        // Send through coordination bus
        self.coordination_bus.send_message(msg).await?;

        // Simulate response (in real implementation, would wait for actual response)
        Ok(format!("Acknowledged: {}", message))
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_agent_selection_step (lines 691-712) ---[0m
    async fn execute_agent_selection_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Count available agents by type
        let mut agent_counts = HashMap::new();
        for entry in self.agents.iter() {
            let agent_type = entry.key().clone();
            *agent_counts.entry(agent_type).or_insert(0) += 1;
        }

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Selected agents for task {}", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("total_agents".to_string(), self.agents.len().to_string()))
    }

  ./crates/ccswarm/src/agent/pool.rs:49-58 method new <-> ./crates/ccswarm/src/agent/pool.rs:468-494 method execute_step
  Similarity: 89.26%
  Classes: AgentPool <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/pool.rs:new (lines 49-58) ---[0m
    pub async fn new() -> Result<Self> {
        let coordination_bus = Arc::new(CoordinationBus::new().await?);

        Ok(Self {
            agents: Arc::new(DashMap::new()),
            sessions: Arc::new(DashMap::new()),
            coordination_bus,
            execution_history: Arc::new(RwLock::new(Vec::new())),
        })
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_step (lines 468-494) ---[0m
    async fn execute_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        info!("🎭 Pool executing step: {}", step.name);

        // Match on step name for custom pool behavior
        match step.name.as_str() {
            "Parallel implementation" | "Execute parallel tests" => {
                // Execute parallel tasks across multiple agents
                self.execute_parallel_step(step, context).await
            }
            "Select optimal agents for task" => {
                // Select best agents for the task
                self.execute_agent_selection_step(step, context).await
            }
            "Test agent integration" => {
                // Test integration between agents
                self.execute_integration_test_step(step, context).await
            }
            _ => {
                // Default execution - delegate to best agent
                self.execute_default_step(step, context).await
            }
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:217-233 method send_message <-> ./crates/ccswarm/src/agent/pool.rs:468-494 method execute_step
  Similarity: 72.30%
  Classes: AgentPool <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/pool.rs:send_message (lines 217-233) ---[0m
    pub async fn send_message(&self, from: &str, to: &str, message: &str) -> Result<String> {
        info!("💬 {} → {}: {}", from, to, message);

        // Create inter-agent message
        let msg = AgentMessage::InterAgentMessage {
            from_agent: from.to_string(),
            to_agent: to.to_string(),
            message: message.to_string(),
            timestamp: chrono::Utc::now(),
        };

        // Send through coordination bus
        self.coordination_bus.send_message(msg).await?;

        // Simulate response (in real implementation, would wait for actual response)
        Ok(format!("Acknowledged: {}", message))
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_step (lines 468-494) ---[0m
    async fn execute_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        info!("🎭 Pool executing step: {}", step.name);

        // Match on step name for custom pool behavior
        match step.name.as_str() {
            "Parallel implementation" | "Execute parallel tests" => {
                // Execute parallel tasks across multiple agents
                self.execute_parallel_step(step, context).await
            }
            "Select optimal agents for task" => {
                // Select best agents for the task
                self.execute_agent_selection_step(step, context).await
            }
            "Test agent integration" => {
                // Test integration between agents
                self.execute_integration_test_step(step, context).await
            }
            _ => {
                // Default execution - delegate to best agent
                self.execute_default_step(step, context).await
            }
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:691-712 method execute_agent_selection_step <-> ./crates/ccswarm/src/agent/pool.rs:806-818 function test_agent_pool_orchestration
  Similarity: 87.05%

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_agent_selection_step (lines 691-712) ---[0m
    async fn execute_agent_selection_step(
        &self,
        _step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        let task_id = context
            .get("task_id")
            .unwrap_or(&"unknown".to_string())
            .clone();

        // Count available agents by type
        let mut agent_counts = HashMap::new();
        for entry in self.agents.iter() {
            let agent_type = entry.key().clone();
            *agent_counts.entry(agent_type).or_insert(0) += 1;
        }

        Ok(StepResult::new(_step.id.clone())
            .with_summary(format!("Selected agents for task {}", task_id))
            .add_output("task_id".to_string(), task_id)
            .add_output("total_agents".to_string(), self.agents.len().to_string()))
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:test_agent_pool_orchestration (lines 806-818) ---[0m
    async fn test_agent_pool_orchestration() {
        let pool = AgentPool::new().await.unwrap();

        let task = Task::new(
            "test_orchestration".to_string(),
            "Test pool orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = pool.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
    }

  ./crates/ccswarm/src/agent/pool.rs:127-139 method get_best_agent_for_task <-> ./crates/ccswarm/src/agent/pool.rs:468-494 method execute_step
  Similarity: 75.19%
  Classes: AgentPool <-> AgentOrchestrator

[36m--- ./crates/ccswarm/src/agent/pool.rs:get_best_agent_for_task (lines 127-139) ---[0m
    pub async fn get_best_agent_for_task(
        &self,
        task: &Task,
    ) -> Result<Arc<RwLock<ClaudeCodeAgent>>> {
        let mut engine = MasterDelegationEngine::new(
            crate::orchestrator::master_delegation::DelegationStrategy::Hybrid,
        );

        let decision = engine.delegate_task(task.clone())?;
        let agent_type = decision.target_agent.name().to_lowercase();

        self.get_agent(&agent_type)
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_step (lines 468-494) ---[0m
    async fn execute_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        info!("🎭 Pool executing step: {}", step.name);

        // Match on step name for custom pool behavior
        match step.name.as_str() {
            "Parallel implementation" | "Execute parallel tests" => {
                // Execute parallel tasks across multiple agents
                self.execute_parallel_step(step, context).await
            }
            "Select optimal agents for task" => {
                // Select best agents for the task
                self.execute_agent_selection_step(step, context).await
            }
            "Test agent integration" => {
                // Test integration between agents
                self.execute_integration_test_step(step, context).await
            }
            _ => {
                // Default execution - delegate to best agent
                self.execute_default_step(step, context).await
            }
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:298-308 function parse_test_results <-> ./crates/ccswarm/src/agent/pool.rs:468-494 method execute_step
  Similarity: 79.04%

[36m--- ./crates/ccswarm/src/agent/pool.rs:parse_test_results (lines 298-308) ---[0m
fn parse_test_results(output: &str) -> (usize, usize) {
    // Simple parsing - in real implementation would be more robust
    if output.contains("Tests:") {
        // Jest format: "Tests: 5 passed, 5 total"
        let passed = output.matches("passed").count();
        let total = output.matches("total").count();
        (passed, total)
    } else {
        (0, 0)
    }
}

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_step (lines 468-494) ---[0m
    async fn execute_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        info!("🎭 Pool executing step: {}", step.name);

        // Match on step name for custom pool behavior
        match step.name.as_str() {
            "Parallel implementation" | "Execute parallel tests" => {
                // Execute parallel tasks across multiple agents
                self.execute_parallel_step(step, context).await
            }
            "Select optimal agents for task" => {
                // Select best agents for the task
                self.execute_agent_selection_step(step, context).await
            }
            "Test agent integration" => {
                // Test integration between agents
                self.execute_integration_test_step(step, context).await
            }
            _ => {
                // Default execution - delegate to best agent
                self.execute_default_step(step, context).await
            }
        }
    }

  ./crates/ccswarm/src/agent/pool.rs:468-494 method execute_step <-> ./crates/ccswarm/src/agent/pool.rs:806-818 function test_agent_pool_orchestration
  Similarity: 71.04%

[36m--- ./crates/ccswarm/src/agent/pool.rs:execute_step (lines 468-494) ---[0m
    async fn execute_step(
        &self,
        step: &TaskStep,
        context: &HashMap<String, String>,
    ) -> Result<StepResult> {
        info!("🎭 Pool executing step: {}", step.name);

        // Match on step name for custom pool behavior
        match step.name.as_str() {
            "Parallel implementation" | "Execute parallel tests" => {
                // Execute parallel tasks across multiple agents
                self.execute_parallel_step(step, context).await
            }
            "Select optimal agents for task" => {
                // Select best agents for the task
                self.execute_agent_selection_step(step, context).await
            }
            "Test agent integration" => {
                // Test integration between agents
                self.execute_integration_test_step(step, context).await
            }
            _ => {
                // Default execution - delegate to best agent
                self.execute_default_step(step, context).await
            }
        }
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:test_agent_pool_orchestration (lines 806-818) ---[0m
    async fn test_agent_pool_orchestration() {
        let pool = AgentPool::new().await.unwrap();

        let task = Task::new(
            "test_orchestration".to_string(),
            "Test pool orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = pool.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
    }

  ./crates/ccswarm/src/agent/pool.rs:217-233 method send_message <-> ./crates/ccswarm/src/agent/pool.rs:806-818 function test_agent_pool_orchestration
  Similarity: 81.44%

[36m--- ./crates/ccswarm/src/agent/pool.rs:send_message (lines 217-233) ---[0m
    pub async fn send_message(&self, from: &str, to: &str, message: &str) -> Result<String> {
        info!("💬 {} → {}: {}", from, to, message);

        // Create inter-agent message
        let msg = AgentMessage::InterAgentMessage {
            from_agent: from.to_string(),
            to_agent: to.to_string(),
            message: message.to_string(),
            timestamp: chrono::Utc::now(),
        };

        // Send through coordination bus
        self.coordination_bus.send_message(msg).await?;

        // Simulate response (in real implementation, would wait for actual response)
        Ok(format!("Acknowledged: {}", message))
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:test_agent_pool_orchestration (lines 806-818) ---[0m
    async fn test_agent_pool_orchestration() {
        let pool = AgentPool::new().await.unwrap();

        let task = Task::new(
            "test_orchestration".to_string(),
            "Test pool orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = pool.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
    }

  ./crates/ccswarm/src/agent/pool.rs:127-139 method get_best_agent_for_task <-> ./crates/ccswarm/src/agent/pool.rs:217-233 method send_message
  Similarity: 79.60%
  Classes: AgentPool <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:get_best_agent_for_task (lines 127-139) ---[0m
    pub async fn get_best_agent_for_task(
        &self,
        task: &Task,
    ) -> Result<Arc<RwLock<ClaudeCodeAgent>>> {
        let mut engine = MasterDelegationEngine::new(
            crate::orchestrator::master_delegation::DelegationStrategy::Hybrid,
        );

        let decision = engine.delegate_task(task.clone())?;
        let agent_type = decision.target_agent.name().to_lowercase();

        self.get_agent(&agent_type)
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:send_message (lines 217-233) ---[0m
    pub async fn send_message(&self, from: &str, to: &str, message: &str) -> Result<String> {
        info!("💬 {} → {}: {}", from, to, message);

        // Create inter-agent message
        let msg = AgentMessage::InterAgentMessage {
            from_agent: from.to_string(),
            to_agent: to.to_string(),
            message: message.to_string(),
            timestamp: chrono::Utc::now(),
        };

        // Send through coordination bus
        self.coordination_bus.send_message(msg).await?;

        // Simulate response (in real implementation, would wait for actual response)
        Ok(format!("Acknowledged: {}", message))
    }

  ./crates/ccswarm/src/agent/pool.rs:127-139 method get_best_agent_for_task <-> ./crates/ccswarm/src/agent/pool.rs:806-818 function test_agent_pool_orchestration
  Similarity: 87.17%

[36m--- ./crates/ccswarm/src/agent/pool.rs:get_best_agent_for_task (lines 127-139) ---[0m
    pub async fn get_best_agent_for_task(
        &self,
        task: &Task,
    ) -> Result<Arc<RwLock<ClaudeCodeAgent>>> {
        let mut engine = MasterDelegationEngine::new(
            crate::orchestrator::master_delegation::DelegationStrategy::Hybrid,
        );

        let decision = engine.delegate_task(task.clone())?;
        let agent_type = decision.target_agent.name().to_lowercase();

        self.get_agent(&agent_type)
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:test_agent_pool_orchestration (lines 806-818) ---[0m
    async fn test_agent_pool_orchestration() {
        let pool = AgentPool::new().await.unwrap();

        let task = Task::new(
            "test_orchestration".to_string(),
            "Test pool orchestration".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let plan = pool.analyze_task(&task).await.unwrap();
        assert!(!plan.steps.is_empty());
    }

  ./crates/ccswarm/src/agent/pool.rs:49-58 method new <-> ./crates/ccswarm/src/agent/pool.rs:236-247 method broadcast_message
  Similarity: 79.17%
  Classes: AgentPool <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:new (lines 49-58) ---[0m
    pub async fn new() -> Result<Self> {
        let coordination_bus = Arc::new(CoordinationBus::new().await?);

        Ok(Self {
            agents: Arc::new(DashMap::new()),
            sessions: Arc::new(DashMap::new()),
            coordination_bus,
            execution_history: Arc::new(RwLock::new(Vec::new())),
        })
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:broadcast_message (lines 236-247) ---[0m
    pub async fn broadcast_message(&self, from: &str, message: &str) -> Result<()> {
        info!("📢 {} → all: {}", from, message);

        for entry in self.agents.iter() {
            let to_agent = entry.key();
            if to_agent != from {
                self.send_message(from, to_agent, message).await?;
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/agent/pool.rs:236-247 method broadcast_message <-> ./crates/ccswarm/src/agent/pool.rs:298-308 function parse_test_results
  Similarity: 71.72%

[36m--- ./crates/ccswarm/src/agent/pool.rs:broadcast_message (lines 236-247) ---[0m
    pub async fn broadcast_message(&self, from: &str, message: &str) -> Result<()> {
        info!("📢 {} → all: {}", from, message);

        for entry in self.agents.iter() {
            let to_agent = entry.key();
            if to_agent != from {
                self.send_message(from, to_agent, message).await?;
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:parse_test_results (lines 298-308) ---[0m
fn parse_test_results(output: &str) -> (usize, usize) {
    // Simple parsing - in real implementation would be more robust
    if output.contains("Tests:") {
        // Jest format: "Tests: 5 passed, 5 total"
        let passed = output.matches("passed").count();
        let total = output.matches("total").count();
        (passed, total)
    } else {
        (0, 0)
    }
}

  ./crates/ccswarm/src/agent/pool.rs:49-58 method new <-> ./crates/ccswarm/src/agent/pool.rs:298-308 function parse_test_results
  Similarity: 77.59%

[36m--- ./crates/ccswarm/src/agent/pool.rs:new (lines 49-58) ---[0m
    pub async fn new() -> Result<Self> {
        let coordination_bus = Arc::new(CoordinationBus::new().await?);

        Ok(Self {
            agents: Arc::new(DashMap::new()),
            sessions: Arc::new(DashMap::new()),
            coordination_bus,
            execution_history: Arc::new(RwLock::new(Vec::new())),
        })
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:parse_test_results (lines 298-308) ---[0m
fn parse_test_results(output: &str) -> (usize, usize) {
    // Simple parsing - in real implementation would be more robust
    if output.contains("Tests:") {
        // Jest format: "Tests: 5 passed, 5 total"
        let passed = output.matches("passed").count();
        let total = output.matches("total").count();
        (passed, total)
    } else {
        (0, 0)
    }
}

  ./crates/ccswarm/src/agent/pool.rs:298-308 function parse_test_results <-> ./crates/ccswarm/src/agent/pool.rs:800-803 function test_agent_pool_creation
  Similarity: 84.19%

[36m--- ./crates/ccswarm/src/agent/pool.rs:parse_test_results (lines 298-308) ---[0m
fn parse_test_results(output: &str) -> (usize, usize) {
    // Simple parsing - in real implementation would be more robust
    if output.contains("Tests:") {
        // Jest format: "Tests: 5 passed, 5 total"
        let passed = output.matches("passed").count();
        let total = output.matches("total").count();
        (passed, total)
    } else {
        (0, 0)
    }
}

[36m--- ./crates/ccswarm/src/agent/pool.rs:test_agent_pool_creation (lines 800-803) ---[0m
    async fn test_agent_pool_creation() {
        let pool = AgentPool::new().await.unwrap();
        assert!(!pool.has_agent("frontend"));
    }

  ./crates/ccswarm/src/agent/pool.rs:49-58 method new <-> ./crates/ccswarm/src/agent/pool.rs:119-124 method get_agent
  Similarity: 71.30%
  Classes: AgentPool <-> AgentPool

[36m--- ./crates/ccswarm/src/agent/pool.rs:new (lines 49-58) ---[0m
    pub async fn new() -> Result<Self> {
        let coordination_bus = Arc::new(CoordinationBus::new().await?);

        Ok(Self {
            agents: Arc::new(DashMap::new()),
            sessions: Arc::new(DashMap::new()),
            coordination_bus,
            execution_history: Arc::new(RwLock::new(Vec::new())),
        })
    }

[36m--- ./crates/ccswarm/src/agent/pool.rs:get_agent (lines 119-124) ---[0m
    pub fn get_agent(&self, agent_type: &str) -> Result<Arc<RwLock<ClaudeCodeAgent>>> {
        self.agents
            .get(agent_type)
            .map(|entry| entry.value().clone())
            .ok_or_else(|| anyhow::anyhow!("Agent not found: {}", agent_type))
    }


Duplicates in ./crates/ccswarm/src/extension_stub/mod.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/extension_stub/mod.rs:140-164 method load_from_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:723-769 function test_extension_manager_proposals
  Similarity: 80.46%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:load_from_registry (lines 140-164) ---[0m
    pub async fn load_from_registry(&mut self) -> Result<usize> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        if !registry_path.exists() {
            return Ok(0);
        }

        let content = tokio::fs::read_to_string(registry_path).await
            .map_err(|e| CCSwarmError::config(format!("Failed to read registry: {}", e)))?;

        let extensions: Vec<Extension> = serde_json::from_str(&content)
            .map_err(|e| CCSwarmError::config(format!("Failed to parse registry: {}", e)))?;

        let mut loaded_count = 0;
        for extension in extensions {
            if let Err(e) = self.register(extension) {
                tracing::warn!("Failed to load extension from registry: {}", e);
            } else {
                loaded_count += 1;
            }
        }

        Ok(loaded_count)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_manager_proposals (lines 723-769) ---[0m
    fn test_extension_manager_proposals() {
        let mut manager = ExtensionManager::new();

        let proposal = ExtensionProposal {
            id: "test-proposal".to_string(),
            agent_id: "test-agent".to_string(),
            extension_type: ExtensionType::Capability,
            status: ExtensionStatus::Proposed,
            specification: "Test specification".to_string(),
            proposer: "Test Proposer".to_string(),
            title: "Test Proposal".to_string(),
            description: "A test proposal".to_string(),
            current_state: CurrentState::new(),
            proposed_state: ProposedState::new(),
            implementation_plan: ImplementationPlan {
                phases: vec![],
                total_duration: std::time::Duration::from_secs(3600),
                resources_required: vec![],
                timeline: "1 week".to_string(),
                dependencies: vec![],
            },
            risk_assessment: RiskAssessment {
                risk_level: RiskLevel::Low,
                potential_issues: vec![],
                mitigation_strategies: vec![],
                rollback_plan: "Rollback plan".to_string(),
                overall_risk_score: 0.2,
                categories: vec![RiskCategory::Stability],
            },
            success_criteria: vec![],
            created_at: chrono::Utc::now(),
        };

        tokio::runtime::Runtime::new().unwrap().block_on(async {
            let proposal_id = manager.propose_extension(proposal).await.unwrap();
            assert!(manager.get_proposal(&proposal_id).is_some());

            // Set proposal to UnderReview before approving
            if let Some(p) = manager.proposals.get_mut(&proposal_id) {
                p.status = ExtensionStatus::UnderReview;
            }

            manager.approve_proposal(&proposal_id).await.unwrap();
            let approved_proposal = manager.get_proposal(&proposal_id).unwrap();
            assert_eq!(approved_proposal.status, ExtensionStatus::Approved);
        });
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:40-82 method register <-> ./crates/ccswarm/src/extension_stub/mod.rs:665-695 function test_extension_stub_find_by_capability
  Similarity: 76.68%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:register (lines 40-82) ---[0m
    pub fn register(&mut self, mut extension: Extension) -> Result<()> {
        // Validate extension before registration
        let validation_issues = extension.validate()?;
        if !validation_issues.is_empty() {
            // Try to auto-fix issues
            let fixes = extension.auto_fix()?;
            tracing::info!("Auto-fixed {} issues for extension {}: {:?}",
                         fixes.len(), extension.id(), fixes);

            // Re-validate after fixes
            let remaining_issues = extension.validate()?;
            if !remaining_issues.is_empty() {
                return Err(CCSwarmError::extension(
                    extension.id(),
                    format!("Extension validation failed: {:?}", remaining_issues)
                ));
            }
        }

        // Check for duplicate registration
        if self.extensions.contains_key(extension.id()) {
            return Err(CCSwarmError::extension(
                extension.id(),
                "Extension with this ID is already registered"
            ));
        }

        // Check dependencies
        let available_extensions: Vec<Extension> = self.extensions.values().cloned().collect();
        let missing_deps = extension.check_dependencies(&available_extensions)?;
        if !missing_deps.is_empty() {
            return Err(CCSwarmError::extension(
                extension.id(),
                format!("Missing dependencies: {:?}", missing_deps)
            ));
        }

        let extension_id = extension.id().to_string();
        self.extensions.insert(extension_id.clone(), extension);

        tracing::info!("Successfully registered extension: {}", extension_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_find_by_capability (lines 665-695) ---[0m
    fn test_extension_stub_find_by_capability() {
        let mut stub = ExtensionStub::new();

        let cmd_extension = Extension::new(
            "Command Extension",
            Version::new(1, 0, 0),
            "A command extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let security_extension = Extension::new(
            "Security Extension",
            Version::new(1, 0, 0),
            "A security extension",
            "Test Author",
            vec![ExtensionCapability::Security],
        );

        stub.register(cmd_extension).unwrap();
        stub.register(security_extension).unwrap();

        let cmd_extensions = stub.find_by_capability(&ExtensionCapability::Command);
        assert_eq!(cmd_extensions.len(), 1);

        let security_extensions = stub.find_by_capability(&ExtensionCapability::Security);
        assert_eq!(security_extensions.len(), 1);

        let monitoring_extensions = stub.find_by_capability(&ExtensionCapability::Monitoring);
        assert_eq!(monitoring_extensions.len(), 0);
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:665-695 function test_extension_stub_find_by_capability <-> ./crates/ccswarm/src/extension_stub/mod.rs:723-769 function test_extension_manager_proposals
  Similarity: 70.40%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_find_by_capability (lines 665-695) ---[0m
    fn test_extension_stub_find_by_capability() {
        let mut stub = ExtensionStub::new();

        let cmd_extension = Extension::new(
            "Command Extension",
            Version::new(1, 0, 0),
            "A command extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let security_extension = Extension::new(
            "Security Extension",
            Version::new(1, 0, 0),
            "A security extension",
            "Test Author",
            vec![ExtensionCapability::Security],
        );

        stub.register(cmd_extension).unwrap();
        stub.register(security_extension).unwrap();

        let cmd_extensions = stub.find_by_capability(&ExtensionCapability::Command);
        assert_eq!(cmd_extensions.len(), 1);

        let security_extensions = stub.find_by_capability(&ExtensionCapability::Security);
        assert_eq!(security_extensions.len(), 1);

        let monitoring_extensions = stub.find_by_capability(&ExtensionCapability::Monitoring);
        assert_eq!(monitoring_extensions.len(), 0);
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_manager_proposals (lines 723-769) ---[0m
    fn test_extension_manager_proposals() {
        let mut manager = ExtensionManager::new();

        let proposal = ExtensionProposal {
            id: "test-proposal".to_string(),
            agent_id: "test-agent".to_string(),
            extension_type: ExtensionType::Capability,
            status: ExtensionStatus::Proposed,
            specification: "Test specification".to_string(),
            proposer: "Test Proposer".to_string(),
            title: "Test Proposal".to_string(),
            description: "A test proposal".to_string(),
            current_state: CurrentState::new(),
            proposed_state: ProposedState::new(),
            implementation_plan: ImplementationPlan {
                phases: vec![],
                total_duration: std::time::Duration::from_secs(3600),
                resources_required: vec![],
                timeline: "1 week".to_string(),
                dependencies: vec![],
            },
            risk_assessment: RiskAssessment {
                risk_level: RiskLevel::Low,
                potential_issues: vec![],
                mitigation_strategies: vec![],
                rollback_plan: "Rollback plan".to_string(),
                overall_risk_score: 0.2,
                categories: vec![RiskCategory::Stability],
            },
            success_criteria: vec![],
            created_at: chrono::Utc::now(),
        };

        tokio::runtime::Runtime::new().unwrap().block_on(async {
            let proposal_id = manager.propose_extension(proposal).await.unwrap();
            assert!(manager.get_proposal(&proposal_id).is_some());

            // Set proposal to UnderReview before approving
            if let Some(p) = manager.proposals.get_mut(&proposal_id) {
                p.status = ExtensionStatus::UnderReview;
            }

            manager.approve_proposal(&proposal_id).await.unwrap();
            let approved_proposal = manager.get_proposal(&proposal_id).unwrap();
            assert_eq!(approved_proposal.status, ExtensionStatus::Approved);
        });
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:40-82 method register <-> ./crates/ccswarm/src/extension_stub/mod.rs:698-720 function test_extension_stub_registry_persistence
  Similarity: 81.58%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:register (lines 40-82) ---[0m
    pub fn register(&mut self, mut extension: Extension) -> Result<()> {
        // Validate extension before registration
        let validation_issues = extension.validate()?;
        if !validation_issues.is_empty() {
            // Try to auto-fix issues
            let fixes = extension.auto_fix()?;
            tracing::info!("Auto-fixed {} issues for extension {}: {:?}",
                         fixes.len(), extension.id(), fixes);

            // Re-validate after fixes
            let remaining_issues = extension.validate()?;
            if !remaining_issues.is_empty() {
                return Err(CCSwarmError::extension(
                    extension.id(),
                    format!("Extension validation failed: {:?}", remaining_issues)
                ));
            }
        }

        // Check for duplicate registration
        if self.extensions.contains_key(extension.id()) {
            return Err(CCSwarmError::extension(
                extension.id(),
                "Extension with this ID is already registered"
            ));
        }

        // Check dependencies
        let available_extensions: Vec<Extension> = self.extensions.values().cloned().collect();
        let missing_deps = extension.check_dependencies(&available_extensions)?;
        if !missing_deps.is_empty() {
            return Err(CCSwarmError::extension(
                extension.id(),
                format!("Missing dependencies: {:?}", missing_deps)
            ));
        }

        let extension_id = extension.id().to_string();
        self.extensions.insert(extension_id.clone(), extension);

        tracing::info!("Successfully registered extension: {}", extension_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_registry_persistence (lines 698-720) ---[0m
    async fn test_extension_stub_registry_persistence() {
        let temp_dir = TempDir::new().unwrap();
        let registry_path = temp_dir.path().join("extensions.json");

        let mut stub = ExtensionStub::with_registry(&registry_path);

        let extension = Extension::new(
            "Persistent Extension",
            Version::new(1, 0, 0),
            "A persistent extension",
            "Test Author",
            vec![ExtensionCapability::Integration],
        );

        stub.register(extension).unwrap();
        stub.save_to_registry().await.unwrap();

        let mut new_stub = ExtensionStub::with_registry(&registry_path);
        let loaded_count = new_stub.load_from_registry().await.unwrap();

        assert_eq!(loaded_count, 1);
        assert_eq!(new_stub.count(), 1);
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:40-82 method register <-> ./crates/ccswarm/src/extension_stub/mod.rs:140-164 method load_from_registry
  Similarity: 75.04%
  Classes: ExtensionStub <-> ExtensionStub

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:register (lines 40-82) ---[0m
    pub fn register(&mut self, mut extension: Extension) -> Result<()> {
        // Validate extension before registration
        let validation_issues = extension.validate()?;
        if !validation_issues.is_empty() {
            // Try to auto-fix issues
            let fixes = extension.auto_fix()?;
            tracing::info!("Auto-fixed {} issues for extension {}: {:?}",
                         fixes.len(), extension.id(), fixes);

            // Re-validate after fixes
            let remaining_issues = extension.validate()?;
            if !remaining_issues.is_empty() {
                return Err(CCSwarmError::extension(
                    extension.id(),
                    format!("Extension validation failed: {:?}", remaining_issues)
                ));
            }
        }

        // Check for duplicate registration
        if self.extensions.contains_key(extension.id()) {
            return Err(CCSwarmError::extension(
                extension.id(),
                "Extension with this ID is already registered"
            ));
        }

        // Check dependencies
        let available_extensions: Vec<Extension> = self.extensions.values().cloned().collect();
        let missing_deps = extension.check_dependencies(&available_extensions)?;
        if !missing_deps.is_empty() {
            return Err(CCSwarmError::extension(
                extension.id(),
                format!("Missing dependencies: {:?}", missing_deps)
            ));
        }

        let extension_id = extension.id().to_string();
        self.extensions.insert(extension_id.clone(), extension);

        tracing::info!("Successfully registered extension: {}", extension_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:load_from_registry (lines 140-164) ---[0m
    pub async fn load_from_registry(&mut self) -> Result<usize> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        if !registry_path.exists() {
            return Ok(0);
        }

        let content = tokio::fs::read_to_string(registry_path).await
            .map_err(|e| CCSwarmError::config(format!("Failed to read registry: {}", e)))?;

        let extensions: Vec<Extension> = serde_json::from_str(&content)
            .map_err(|e| CCSwarmError::config(format!("Failed to parse registry: {}", e)))?;

        let mut loaded_count = 0;
        for extension in extensions {
            if let Err(e) = self.register(extension) {
                tracing::warn!("Failed to load extension from registry: {}", e);
            } else {
                loaded_count += 1;
            }
        }

        Ok(loaded_count)
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:167-185 method save_to_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:723-769 function test_extension_manager_proposals
  Similarity: 77.28%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:save_to_registry (lines 167-185) ---[0m
    pub async fn save_to_registry(&self) -> Result<()> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        // Create parent directory if it doesn't exist
        if let Some(parent) = registry_path.parent() {
            tokio::fs::create_dir_all(parent).await
                .map_err(|e| CCSwarmError::config(format!("Failed to create registry directory: {}", e)))?;
        }

        let extensions: Vec<Extension> = self.extensions.values().cloned().collect();
        let content = serde_json::to_string_pretty(&extensions)
            .map_err(|e| CCSwarmError::config(format!("Failed to serialize extensions: {}", e)))?;

        tokio::fs::write(registry_path, content).await
            .map_err(|e| CCSwarmError::config(format!("Failed to write registry: {}", e)))?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_manager_proposals (lines 723-769) ---[0m
    fn test_extension_manager_proposals() {
        let mut manager = ExtensionManager::new();

        let proposal = ExtensionProposal {
            id: "test-proposal".to_string(),
            agent_id: "test-agent".to_string(),
            extension_type: ExtensionType::Capability,
            status: ExtensionStatus::Proposed,
            specification: "Test specification".to_string(),
            proposer: "Test Proposer".to_string(),
            title: "Test Proposal".to_string(),
            description: "A test proposal".to_string(),
            current_state: CurrentState::new(),
            proposed_state: ProposedState::new(),
            implementation_plan: ImplementationPlan {
                phases: vec![],
                total_duration: std::time::Duration::from_secs(3600),
                resources_required: vec![],
                timeline: "1 week".to_string(),
                dependencies: vec![],
            },
            risk_assessment: RiskAssessment {
                risk_level: RiskLevel::Low,
                potential_issues: vec![],
                mitigation_strategies: vec![],
                rollback_plan: "Rollback plan".to_string(),
                overall_risk_score: 0.2,
                categories: vec![RiskCategory::Stability],
            },
            success_criteria: vec![],
            created_at: chrono::Utc::now(),
        };

        tokio::runtime::Runtime::new().unwrap().block_on(async {
            let proposal_id = manager.propose_extension(proposal).await.unwrap();
            assert!(manager.get_proposal(&proposal_id).is_some());

            // Set proposal to UnderReview before approving
            if let Some(p) = manager.proposals.get_mut(&proposal_id) {
                p.status = ExtensionStatus::UnderReview;
            }

            manager.approve_proposal(&proposal_id).await.unwrap();
            let approved_proposal = manager.get_proposal(&proposal_id).unwrap();
            assert_eq!(approved_proposal.status, ExtensionStatus::Approved);
        });
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:40-82 method register <-> ./crates/ccswarm/src/extension_stub/mod.rs:780-805 function test_extension_stats
  Similarity: 73.87%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:register (lines 40-82) ---[0m
    pub fn register(&mut self, mut extension: Extension) -> Result<()> {
        // Validate extension before registration
        let validation_issues = extension.validate()?;
        if !validation_issues.is_empty() {
            // Try to auto-fix issues
            let fixes = extension.auto_fix()?;
            tracing::info!("Auto-fixed {} issues for extension {}: {:?}",
                         fixes.len(), extension.id(), fixes);

            // Re-validate after fixes
            let remaining_issues = extension.validate()?;
            if !remaining_issues.is_empty() {
                return Err(CCSwarmError::extension(
                    extension.id(),
                    format!("Extension validation failed: {:?}", remaining_issues)
                ));
            }
        }

        // Check for duplicate registration
        if self.extensions.contains_key(extension.id()) {
            return Err(CCSwarmError::extension(
                extension.id(),
                "Extension with this ID is already registered"
            ));
        }

        // Check dependencies
        let available_extensions: Vec<Extension> = self.extensions.values().cloned().collect();
        let missing_deps = extension.check_dependencies(&available_extensions)?;
        if !missing_deps.is_empty() {
            return Err(CCSwarmError::extension(
                extension.id(),
                format!("Missing dependencies: {:?}", missing_deps)
            ));
        }

        let extension_id = extension.id().to_string();
        self.extensions.insert(extension_id.clone(), extension);

        tracing::info!("Successfully registered extension: {}", extension_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stats (lines 780-805) ---[0m
    fn test_extension_stats() {
        let mut stub = ExtensionStub::new();

        let extension1 = Extension::new(
            "Extension 1",
            Version::new(1, 0, 0),
            "First extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let extension2 = Extension::new(
            "Extension 2",
            Version::new(1, 0, 0),
            "Second extension",
            "Test Author",
            vec![ExtensionCapability::Security],
        );

        stub.register(extension1).unwrap();
        stub.register(extension2).unwrap();

        let stats = stub.get_stats();
        assert_eq!(stats.total_extensions, 2);
        assert_eq!(stats.error_extensions, 0);
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:40-82 method register <-> ./crates/ccswarm/src/extension_stub/mod.rs:167-185 method save_to_registry
  Similarity: 78.26%
  Classes: ExtensionStub <-> ExtensionStub

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:register (lines 40-82) ---[0m
    pub fn register(&mut self, mut extension: Extension) -> Result<()> {
        // Validate extension before registration
        let validation_issues = extension.validate()?;
        if !validation_issues.is_empty() {
            // Try to auto-fix issues
            let fixes = extension.auto_fix()?;
            tracing::info!("Auto-fixed {} issues for extension {}: {:?}",
                         fixes.len(), extension.id(), fixes);

            // Re-validate after fixes
            let remaining_issues = extension.validate()?;
            if !remaining_issues.is_empty() {
                return Err(CCSwarmError::extension(
                    extension.id(),
                    format!("Extension validation failed: {:?}", remaining_issues)
                ));
            }
        }

        // Check for duplicate registration
        if self.extensions.contains_key(extension.id()) {
            return Err(CCSwarmError::extension(
                extension.id(),
                "Extension with this ID is already registered"
            ));
        }

        // Check dependencies
        let available_extensions: Vec<Extension> = self.extensions.values().cloned().collect();
        let missing_deps = extension.check_dependencies(&available_extensions)?;
        if !missing_deps.is_empty() {
            return Err(CCSwarmError::extension(
                extension.id(),
                format!("Missing dependencies: {:?}", missing_deps)
            ));
        }

        let extension_id = extension.id().to_string();
        self.extensions.insert(extension_id.clone(), extension);

        tracing::info!("Successfully registered extension: {}", extension_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:save_to_registry (lines 167-185) ---[0m
    pub async fn save_to_registry(&self) -> Result<()> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        // Create parent directory if it doesn't exist
        if let Some(parent) = registry_path.parent() {
            tokio::fs::create_dir_all(parent).await
                .map_err(|e| CCSwarmError::config(format!("Failed to create registry directory: {}", e)))?;
        }

        let extensions: Vec<Extension> = self.extensions.values().cloned().collect();
        let content = serde_json::to_string_pretty(&extensions)
            .map_err(|e| CCSwarmError::config(format!("Failed to serialize extensions: {}", e)))?;

        tokio::fs::write(registry_path, content).await
            .map_err(|e| CCSwarmError::config(format!("Failed to write registry: {}", e)))?;

        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:665-695 function test_extension_stub_find_by_capability <-> ./crates/ccswarm/src/extension_stub/mod.rs:780-805 function test_extension_stats
  Similarity: 75.54%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_find_by_capability (lines 665-695) ---[0m
    fn test_extension_stub_find_by_capability() {
        let mut stub = ExtensionStub::new();

        let cmd_extension = Extension::new(
            "Command Extension",
            Version::new(1, 0, 0),
            "A command extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let security_extension = Extension::new(
            "Security Extension",
            Version::new(1, 0, 0),
            "A security extension",
            "Test Author",
            vec![ExtensionCapability::Security],
        );

        stub.register(cmd_extension).unwrap();
        stub.register(security_extension).unwrap();

        let cmd_extensions = stub.find_by_capability(&ExtensionCapability::Command);
        assert_eq!(cmd_extensions.len(), 1);

        let security_extensions = stub.find_by_capability(&ExtensionCapability::Security);
        assert_eq!(security_extensions.len(), 1);

        let monitoring_extensions = stub.find_by_capability(&ExtensionCapability::Monitoring);
        assert_eq!(monitoring_extensions.len(), 0);
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stats (lines 780-805) ---[0m
    fn test_extension_stats() {
        let mut stub = ExtensionStub::new();

        let extension1 = Extension::new(
            "Extension 1",
            Version::new(1, 0, 0),
            "First extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let extension2 = Extension::new(
            "Extension 2",
            Version::new(1, 0, 0),
            "Second extension",
            "Test Author",
            vec![ExtensionCapability::Security],
        );

        stub.register(extension1).unwrap();
        stub.register(extension2).unwrap();

        let stats = stub.get_stats();
        assert_eq!(stats.total_extensions, 2);
        assert_eq!(stats.error_extensions, 0);
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:665-695 function test_extension_stub_find_by_capability <-> ./crates/ccswarm/src/extension_stub/mod.rs:698-720 function test_extension_stub_registry_persistence
  Similarity: 79.62%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_find_by_capability (lines 665-695) ---[0m
    fn test_extension_stub_find_by_capability() {
        let mut stub = ExtensionStub::new();

        let cmd_extension = Extension::new(
            "Command Extension",
            Version::new(1, 0, 0),
            "A command extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let security_extension = Extension::new(
            "Security Extension",
            Version::new(1, 0, 0),
            "A security extension",
            "Test Author",
            vec![ExtensionCapability::Security],
        );

        stub.register(cmd_extension).unwrap();
        stub.register(security_extension).unwrap();

        let cmd_extensions = stub.find_by_capability(&ExtensionCapability::Command);
        assert_eq!(cmd_extensions.len(), 1);

        let security_extensions = stub.find_by_capability(&ExtensionCapability::Security);
        assert_eq!(security_extensions.len(), 1);

        let monitoring_extensions = stub.find_by_capability(&ExtensionCapability::Monitoring);
        assert_eq!(monitoring_extensions.len(), 0);
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_registry_persistence (lines 698-720) ---[0m
    async fn test_extension_stub_registry_persistence() {
        let temp_dir = TempDir::new().unwrap();
        let registry_path = temp_dir.path().join("extensions.json");

        let mut stub = ExtensionStub::with_registry(&registry_path);

        let extension = Extension::new(
            "Persistent Extension",
            Version::new(1, 0, 0),
            "A persistent extension",
            "Test Author",
            vec![ExtensionCapability::Integration],
        );

        stub.register(extension).unwrap();
        stub.save_to_registry().await.unwrap();

        let mut new_stub = ExtensionStub::with_registry(&registry_path);
        let loaded_count = new_stub.load_from_registry().await.unwrap();

        assert_eq!(loaded_count, 1);
        assert_eq!(new_stub.count(), 1);
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:625-643 function test_extension_stub_basic_operations <-> ./crates/ccswarm/src/extension_stub/mod.rs:780-805 function test_extension_stats
  Similarity: 87.06%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_basic_operations (lines 625-643) ---[0m
    fn test_extension_stub_basic_operations() {
        let mut stub = ExtensionStub::new();
        assert_eq!(stub.count(), 0);

        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let ext_id = extension.id().to_string();
        stub.register(extension).unwrap();

        assert_eq!(stub.count(), 1);
        assert!(stub.contains(&ext_id));
        assert!(stub.get(&ext_id).is_some());
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stats (lines 780-805) ---[0m
    fn test_extension_stats() {
        let mut stub = ExtensionStub::new();

        let extension1 = Extension::new(
            "Extension 1",
            Version::new(1, 0, 0),
            "First extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let extension2 = Extension::new(
            "Extension 2",
            Version::new(1, 0, 0),
            "Second extension",
            "Test Author",
            vec![ExtensionCapability::Security],
        );

        stub.register(extension1).unwrap();
        stub.register(extension2).unwrap();

        let stats = stub.get_stats();
        assert_eq!(stats.total_extensions, 2);
        assert_eq!(stats.error_extensions, 0);
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:140-164 method load_from_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:167-185 method save_to_registry
  Similarity: 88.19%
  Classes: ExtensionStub <-> ExtensionStub

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:load_from_registry (lines 140-164) ---[0m
    pub async fn load_from_registry(&mut self) -> Result<usize> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        if !registry_path.exists() {
            return Ok(0);
        }

        let content = tokio::fs::read_to_string(registry_path).await
            .map_err(|e| CCSwarmError::config(format!("Failed to read registry: {}", e)))?;

        let extensions: Vec<Extension> = serde_json::from_str(&content)
            .map_err(|e| CCSwarmError::config(format!("Failed to parse registry: {}", e)))?;

        let mut loaded_count = 0;
        for extension in extensions {
            if let Err(e) = self.register(extension) {
                tracing::warn!("Failed to load extension from registry: {}", e);
            } else {
                loaded_count += 1;
            }
        }

        Ok(loaded_count)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:save_to_registry (lines 167-185) ---[0m
    pub async fn save_to_registry(&self) -> Result<()> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        // Create parent directory if it doesn't exist
        if let Some(parent) = registry_path.parent() {
            tokio::fs::create_dir_all(parent).await
                .map_err(|e| CCSwarmError::config(format!("Failed to create registry directory: {}", e)))?;
        }

        let extensions: Vec<Extension> = self.extensions.values().cloned().collect();
        let content = serde_json::to_string_pretty(&extensions)
            .map_err(|e| CCSwarmError::config(format!("Failed to serialize extensions: {}", e)))?;

        tokio::fs::write(registry_path, content).await
            .map_err(|e| CCSwarmError::config(format!("Failed to write registry: {}", e)))?;

        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:140-164 method load_from_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:780-805 function test_extension_stats
  Similarity: 72.74%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:load_from_registry (lines 140-164) ---[0m
    pub async fn load_from_registry(&mut self) -> Result<usize> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        if !registry_path.exists() {
            return Ok(0);
        }

        let content = tokio::fs::read_to_string(registry_path).await
            .map_err(|e| CCSwarmError::config(format!("Failed to read registry: {}", e)))?;

        let extensions: Vec<Extension> = serde_json::from_str(&content)
            .map_err(|e| CCSwarmError::config(format!("Failed to parse registry: {}", e)))?;

        let mut loaded_count = 0;
        for extension in extensions {
            if let Err(e) = self.register(extension) {
                tracing::warn!("Failed to load extension from registry: {}", e);
            } else {
                loaded_count += 1;
            }
        }

        Ok(loaded_count)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stats (lines 780-805) ---[0m
    fn test_extension_stats() {
        let mut stub = ExtensionStub::new();

        let extension1 = Extension::new(
            "Extension 1",
            Version::new(1, 0, 0),
            "First extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let extension2 = Extension::new(
            "Extension 2",
            Version::new(1, 0, 0),
            "Second extension",
            "Test Author",
            vec![ExtensionCapability::Security],
        );

        stub.register(extension1).unwrap();
        stub.register(extension2).unwrap();

        let stats = stub.get_stats();
        assert_eq!(stats.total_extensions, 2);
        assert_eq!(stats.error_extensions, 0);
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:698-720 function test_extension_stub_registry_persistence <-> ./crates/ccswarm/src/extension_stub/mod.rs:780-805 function test_extension_stats
  Similarity: 75.30%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_registry_persistence (lines 698-720) ---[0m
    async fn test_extension_stub_registry_persistence() {
        let temp_dir = TempDir::new().unwrap();
        let registry_path = temp_dir.path().join("extensions.json");

        let mut stub = ExtensionStub::with_registry(&registry_path);

        let extension = Extension::new(
            "Persistent Extension",
            Version::new(1, 0, 0),
            "A persistent extension",
            "Test Author",
            vec![ExtensionCapability::Integration],
        );

        stub.register(extension).unwrap();
        stub.save_to_registry().await.unwrap();

        let mut new_stub = ExtensionStub::with_registry(&registry_path);
        let loaded_count = new_stub.load_from_registry().await.unwrap();

        assert_eq!(loaded_count, 1);
        assert_eq!(new_stub.count(), 1);
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stats (lines 780-805) ---[0m
    fn test_extension_stats() {
        let mut stub = ExtensionStub::new();

        let extension1 = Extension::new(
            "Extension 1",
            Version::new(1, 0, 0),
            "First extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let extension2 = Extension::new(
            "Extension 2",
            Version::new(1, 0, 0),
            "Second extension",
            "Test Author",
            vec![ExtensionCapability::Security],
        );

        stub.register(extension1).unwrap();
        stub.register(extension2).unwrap();

        let stats = stub.get_stats();
        assert_eq!(stats.total_extensions, 2);
        assert_eq!(stats.error_extensions, 0);
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:167-185 method save_to_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:780-805 function test_extension_stats
  Similarity: 81.55%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:save_to_registry (lines 167-185) ---[0m
    pub async fn save_to_registry(&self) -> Result<()> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        // Create parent directory if it doesn't exist
        if let Some(parent) = registry_path.parent() {
            tokio::fs::create_dir_all(parent).await
                .map_err(|e| CCSwarmError::config(format!("Failed to create registry directory: {}", e)))?;
        }

        let extensions: Vec<Extension> = self.extensions.values().cloned().collect();
        let content = serde_json::to_string_pretty(&extensions)
            .map_err(|e| CCSwarmError::config(format!("Failed to serialize extensions: {}", e)))?;

        tokio::fs::write(registry_path, content).await
            .map_err(|e| CCSwarmError::config(format!("Failed to write registry: {}", e)))?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stats (lines 780-805) ---[0m
    fn test_extension_stats() {
        let mut stub = ExtensionStub::new();

        let extension1 = Extension::new(
            "Extension 1",
            Version::new(1, 0, 0),
            "First extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let extension2 = Extension::new(
            "Extension 2",
            Version::new(1, 0, 0),
            "Second extension",
            "Test Author",
            vec![ExtensionCapability::Security],
        );

        stub.register(extension1).unwrap();
        stub.register(extension2).unwrap();

        let stats = stub.get_stats();
        assert_eq!(stats.total_extensions, 2);
        assert_eq!(stats.error_extensions, 0);
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:140-164 method load_from_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:445-464 method validate_proposal
  Similarity: 81.07%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:load_from_registry (lines 140-164) ---[0m
    pub async fn load_from_registry(&mut self) -> Result<usize> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        if !registry_path.exists() {
            return Ok(0);
        }

        let content = tokio::fs::read_to_string(registry_path).await
            .map_err(|e| CCSwarmError::config(format!("Failed to read registry: {}", e)))?;

        let extensions: Vec<Extension> = serde_json::from_str(&content)
            .map_err(|e| CCSwarmError::config(format!("Failed to parse registry: {}", e)))?;

        let mut loaded_count = 0;
        for extension in extensions {
            if let Err(e) = self.register(extension) {
                tracing::warn!("Failed to load extension from registry: {}", e);
            } else {
                loaded_count += 1;
            }
        }

        Ok(loaded_count)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:validate_proposal (lines 445-464) ---[0m
    fn validate_proposal(&self, proposal: &ExtensionProposal) -> Result<()> {
        if proposal.title.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposal title cannot be empty"));
        }

        if proposal.description.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposal description cannot be empty"));
        }

        if proposal.proposer.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposer cannot be empty"));
        }

        // Validate risk assessment
        if proposal.risk_assessment.overall_risk_score < 0.0 || proposal.risk_assessment.overall_risk_score > 1.0 {
            return Err(CCSwarmError::extension(&proposal.id, "Risk score must be between 0.0 and 1.0"));
        }

        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:625-643 function test_extension_stub_basic_operations <-> ./crates/ccswarm/src/extension_stub/mod.rs:665-695 function test_extension_stub_find_by_capability
  Similarity: 71.46%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_basic_operations (lines 625-643) ---[0m
    fn test_extension_stub_basic_operations() {
        let mut stub = ExtensionStub::new();
        assert_eq!(stub.count(), 0);

        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let ext_id = extension.id().to_string();
        stub.register(extension).unwrap();

        assert_eq!(stub.count(), 1);
        assert!(stub.contains(&ext_id));
        assert!(stub.get(&ext_id).is_some());
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_find_by_capability (lines 665-695) ---[0m
    fn test_extension_stub_find_by_capability() {
        let mut stub = ExtensionStub::new();

        let cmd_extension = Extension::new(
            "Command Extension",
            Version::new(1, 0, 0),
            "A command extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let security_extension = Extension::new(
            "Security Extension",
            Version::new(1, 0, 0),
            "A security extension",
            "Test Author",
            vec![ExtensionCapability::Security],
        );

        stub.register(cmd_extension).unwrap();
        stub.register(security_extension).unwrap();

        let cmd_extensions = stub.find_by_capability(&ExtensionCapability::Command);
        assert_eq!(cmd_extensions.len(), 1);

        let security_extensions = stub.find_by_capability(&ExtensionCapability::Security);
        assert_eq!(security_extensions.len(), 1);

        let monitoring_extensions = stub.find_by_capability(&ExtensionCapability::Monitoring);
        assert_eq!(monitoring_extensions.len(), 0);
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:140-164 method load_from_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:376-392 method approve_proposal
  Similarity: 82.86%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:load_from_registry (lines 140-164) ---[0m
    pub async fn load_from_registry(&mut self) -> Result<usize> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        if !registry_path.exists() {
            return Ok(0);
        }

        let content = tokio::fs::read_to_string(registry_path).await
            .map_err(|e| CCSwarmError::config(format!("Failed to read registry: {}", e)))?;

        let extensions: Vec<Extension> = serde_json::from_str(&content)
            .map_err(|e| CCSwarmError::config(format!("Failed to parse registry: {}", e)))?;

        let mut loaded_count = 0;
        for extension in extensions {
            if let Err(e) = self.register(extension) {
                tracing::warn!("Failed to load extension from registry: {}", e);
            } else {
                loaded_count += 1;
            }
        }

        Ok(loaded_count)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:approve_proposal (lines 376-392) ---[0m
    pub async fn approve_proposal(&mut self, proposal_id: &str) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        if proposal.status != ExtensionStatus::UnderReview {
            return Err(CCSwarmError::extension(
                proposal_id,
                format!("Cannot approve proposal in status: {:?}", proposal.status)
            ));
        }

        proposal.status = ExtensionStatus::Approved;
        self.update_stats();

        tracing::info!("Extension proposal approved: {}", proposal_id);
        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:140-164 method load_from_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:407-423 method mark_implemented
  Similarity: 82.86%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:load_from_registry (lines 140-164) ---[0m
    pub async fn load_from_registry(&mut self) -> Result<usize> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        if !registry_path.exists() {
            return Ok(0);
        }

        let content = tokio::fs::read_to_string(registry_path).await
            .map_err(|e| CCSwarmError::config(format!("Failed to read registry: {}", e)))?;

        let extensions: Vec<Extension> = serde_json::from_str(&content)
            .map_err(|e| CCSwarmError::config(format!("Failed to parse registry: {}", e)))?;

        let mut loaded_count = 0;
        for extension in extensions {
            if let Err(e) = self.register(extension) {
                tracing::warn!("Failed to load extension from registry: {}", e);
            } else {
                loaded_count += 1;
            }
        }

        Ok(loaded_count)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:mark_implemented (lines 407-423) ---[0m
    pub async fn mark_implemented(&mut self, proposal_id: &str) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        if proposal.status != ExtensionStatus::Approved {
            return Err(CCSwarmError::extension(
                proposal_id,
                format!("Cannot implement proposal in status: {:?}", proposal.status)
            ));
        }

        proposal.status = ExtensionStatus::Implemented;
        self.update_stats();

        tracing::info!("Extension proposal implemented: {}", proposal_id);
        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:646-662 function test_extension_stub_duplicate_registration <-> ./crates/ccswarm/src/extension_stub/mod.rs:780-805 function test_extension_stats
  Similarity: 80.00%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_duplicate_registration (lines 646-662) ---[0m
    fn test_extension_stub_duplicate_registration() {
        let mut stub = ExtensionStub::new();

        let extension1 = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let extension2 = extension1.clone();

        stub.register(extension1).unwrap();
        let result = stub.register(extension2);
        assert!(result.is_err());
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stats (lines 780-805) ---[0m
    fn test_extension_stats() {
        let mut stub = ExtensionStub::new();

        let extension1 = Extension::new(
            "Extension 1",
            Version::new(1, 0, 0),
            "First extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let extension2 = Extension::new(
            "Extension 2",
            Version::new(1, 0, 0),
            "Second extension",
            "Test Author",
            vec![ExtensionCapability::Security],
        );

        stub.register(extension1).unwrap();
        stub.register(extension2).unwrap();

        let stats = stub.get_stats();
        assert_eq!(stats.total_extensions, 2);
        assert_eq!(stats.error_extensions, 0);
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:140-164 method load_from_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:625-643 function test_extension_stub_basic_operations
  Similarity: 77.62%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:load_from_registry (lines 140-164) ---[0m
    pub async fn load_from_registry(&mut self) -> Result<usize> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        if !registry_path.exists() {
            return Ok(0);
        }

        let content = tokio::fs::read_to_string(registry_path).await
            .map_err(|e| CCSwarmError::config(format!("Failed to read registry: {}", e)))?;

        let extensions: Vec<Extension> = serde_json::from_str(&content)
            .map_err(|e| CCSwarmError::config(format!("Failed to parse registry: {}", e)))?;

        let mut loaded_count = 0;
        for extension in extensions {
            if let Err(e) = self.register(extension) {
                tracing::warn!("Failed to load extension from registry: {}", e);
            } else {
                loaded_count += 1;
            }
        }

        Ok(loaded_count)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_basic_operations (lines 625-643) ---[0m
    fn test_extension_stub_basic_operations() {
        let mut stub = ExtensionStub::new();
        assert_eq!(stub.count(), 0);

        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let ext_id = extension.id().to_string();
        stub.register(extension).unwrap();

        assert_eq!(stub.count(), 1);
        assert!(stub.contains(&ext_id));
        assert!(stub.get(&ext_id).is_some());
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:140-164 method load_from_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:467-482 method cleanup_old_proposals
  Similarity: 82.58%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:load_from_registry (lines 140-164) ---[0m
    pub async fn load_from_registry(&mut self) -> Result<usize> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        if !registry_path.exists() {
            return Ok(0);
        }

        let content = tokio::fs::read_to_string(registry_path).await
            .map_err(|e| CCSwarmError::config(format!("Failed to read registry: {}", e)))?;

        let extensions: Vec<Extension> = serde_json::from_str(&content)
            .map_err(|e| CCSwarmError::config(format!("Failed to parse registry: {}", e)))?;

        let mut loaded_count = 0;
        for extension in extensions {
            if let Err(e) = self.register(extension) {
                tracing::warn!("Failed to load extension from registry: {}", e);
            } else {
                loaded_count += 1;
            }
        }

        Ok(loaded_count)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:cleanup_old_proposals (lines 467-482) ---[0m
    pub async fn cleanup_old_proposals(&mut self, max_age: chrono::Duration) -> Result<usize> {
        let cutoff = chrono::Utc::now() - max_age;
        let initial_count = self.proposals.len();

        self.proposals.retain(|_, proposal| {
            proposal.status != ExtensionStatus::Rejected || proposal.created_at > cutoff
        });

        let removed = initial_count - self.proposals.len();
        if removed > 0 {
            self.update_stats();
            tracing::info!("Cleaned up {} old proposals", removed);
        }

        Ok(removed)
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:376-392 method approve_proposal <-> ./crates/ccswarm/src/extension_stub/mod.rs:407-423 method mark_implemented
  Similarity: 99.15%
  Classes: ExtensionManager <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:approve_proposal (lines 376-392) ---[0m
    pub async fn approve_proposal(&mut self, proposal_id: &str) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        if proposal.status != ExtensionStatus::UnderReview {
            return Err(CCSwarmError::extension(
                proposal_id,
                format!("Cannot approve proposal in status: {:?}", proposal.status)
            ));
        }

        proposal.status = ExtensionStatus::Approved;
        self.update_stats();

        tracing::info!("Extension proposal approved: {}", proposal_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:mark_implemented (lines 407-423) ---[0m
    pub async fn mark_implemented(&mut self, proposal_id: &str) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        if proposal.status != ExtensionStatus::Approved {
            return Err(CCSwarmError::extension(
                proposal_id,
                format!("Cannot implement proposal in status: {:?}", proposal.status)
            ));
        }

        proposal.status = ExtensionStatus::Implemented;
        self.update_stats();

        tracing::info!("Extension proposal implemented: {}", proposal_id);
        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:335-350 method propose_extension <-> ./crates/ccswarm/src/extension_stub/mod.rs:780-805 function test_extension_stats
  Similarity: 78.51%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:propose_extension (lines 335-350) ---[0m
    pub async fn propose_extension(&mut self, mut proposal: ExtensionProposal) -> Result<String> {
        // Validate proposal
        self.validate_proposal(&proposal)?;

        // Auto-approve if enabled
        if self.auto_approve {
            proposal.status = ExtensionStatus::Approved;
        }

        let id = proposal.id.clone();
        self.proposals.insert(id.clone(), proposal);
        self.update_stats();

        tracing::info!("Extension proposal submitted: {}", id);
        Ok(id)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stats (lines 780-805) ---[0m
    fn test_extension_stats() {
        let mut stub = ExtensionStub::new();

        let extension1 = Extension::new(
            "Extension 1",
            Version::new(1, 0, 0),
            "First extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let extension2 = Extension::new(
            "Extension 2",
            Version::new(1, 0, 0),
            "Second extension",
            "Test Author",
            vec![ExtensionCapability::Security],
        );

        stub.register(extension1).unwrap();
        stub.register(extension2).unwrap();

        let stats = stub.get_stats();
        assert_eq!(stats.total_extensions, 2);
        assert_eq!(stats.error_extensions, 0);
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:140-164 method load_from_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:646-662 function test_extension_stub_duplicate_registration
  Similarity: 77.62%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:load_from_registry (lines 140-164) ---[0m
    pub async fn load_from_registry(&mut self) -> Result<usize> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        if !registry_path.exists() {
            return Ok(0);
        }

        let content = tokio::fs::read_to_string(registry_path).await
            .map_err(|e| CCSwarmError::config(format!("Failed to read registry: {}", e)))?;

        let extensions: Vec<Extension> = serde_json::from_str(&content)
            .map_err(|e| CCSwarmError::config(format!("Failed to parse registry: {}", e)))?;

        let mut loaded_count = 0;
        for extension in extensions {
            if let Err(e) = self.register(extension) {
                tracing::warn!("Failed to load extension from registry: {}", e);
            } else {
                loaded_count += 1;
            }
        }

        Ok(loaded_count)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_duplicate_registration (lines 646-662) ---[0m
    fn test_extension_stub_duplicate_registration() {
        let mut stub = ExtensionStub::new();

        let extension1 = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let extension2 = extension1.clone();

        stub.register(extension1).unwrap();
        let result = stub.register(extension2);
        assert!(result.is_err());
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:140-164 method load_from_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:202-219 method get_stats
  Similarity: 75.12%
  Classes: ExtensionStub <-> ExtensionStub

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:load_from_registry (lines 140-164) ---[0m
    pub async fn load_from_registry(&mut self) -> Result<usize> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        if !registry_path.exists() {
            return Ok(0);
        }

        let content = tokio::fs::read_to_string(registry_path).await
            .map_err(|e| CCSwarmError::config(format!("Failed to read registry: {}", e)))?;

        let extensions: Vec<Extension> = serde_json::from_str(&content)
            .map_err(|e| CCSwarmError::config(format!("Failed to parse registry: {}", e)))?;

        let mut loaded_count = 0;
        for extension in extensions {
            if let Err(e) = self.register(extension) {
                tracing::warn!("Failed to load extension from registry: {}", e);
            } else {
                loaded_count += 1;
            }
        }

        Ok(loaded_count)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:get_stats (lines 202-219) ---[0m
    pub fn get_stats(&self) -> ExtensionStats {
        let total = self.extensions.len();
        let active = self.list_by_state(&ExtensionState::Active).len();
        let loaded = self.list_by_state(&ExtensionState::Loaded).len();
        let error_count = self.extensions.values()
            .filter(|ext| matches!(ext.state(), ExtensionState::Error(_)))
            .count();

        ExtensionStats {
            total_extensions: total,
            active_extensions: active,
            loaded_extensions: loaded,
            error_extensions: error_count,
            pending_proposals: 0, // Managed by ExtensionManager
            successful_extensions: active + loaded,
            failed_extensions: error_count,
        }
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:167-185 method save_to_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:445-464 method validate_proposal
  Similarity: 81.85%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:save_to_registry (lines 167-185) ---[0m
    pub async fn save_to_registry(&self) -> Result<()> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        // Create parent directory if it doesn't exist
        if let Some(parent) = registry_path.parent() {
            tokio::fs::create_dir_all(parent).await
                .map_err(|e| CCSwarmError::config(format!("Failed to create registry directory: {}", e)))?;
        }

        let extensions: Vec<Extension> = self.extensions.values().cloned().collect();
        let content = serde_json::to_string_pretty(&extensions)
            .map_err(|e| CCSwarmError::config(format!("Failed to serialize extensions: {}", e)))?;

        tokio::fs::write(registry_path, content).await
            .map_err(|e| CCSwarmError::config(format!("Failed to write registry: {}", e)))?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:validate_proposal (lines 445-464) ---[0m
    fn validate_proposal(&self, proposal: &ExtensionProposal) -> Result<()> {
        if proposal.title.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposal title cannot be empty"));
        }

        if proposal.description.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposal description cannot be empty"));
        }

        if proposal.proposer.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposer cannot be empty"));
        }

        // Validate risk assessment
        if proposal.risk_assessment.overall_risk_score < 0.0 || proposal.risk_assessment.overall_risk_score > 1.0 {
            return Err(CCSwarmError::extension(&proposal.id, "Risk score must be between 0.0 and 1.0"));
        }

        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:167-185 method save_to_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:202-219 method get_stats
  Similarity: 85.40%
  Classes: ExtensionStub <-> ExtensionStub

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:save_to_registry (lines 167-185) ---[0m
    pub async fn save_to_registry(&self) -> Result<()> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        // Create parent directory if it doesn't exist
        if let Some(parent) = registry_path.parent() {
            tokio::fs::create_dir_all(parent).await
                .map_err(|e| CCSwarmError::config(format!("Failed to create registry directory: {}", e)))?;
        }

        let extensions: Vec<Extension> = self.extensions.values().cloned().collect();
        let content = serde_json::to_string_pretty(&extensions)
            .map_err(|e| CCSwarmError::config(format!("Failed to serialize extensions: {}", e)))?;

        tokio::fs::write(registry_path, content).await
            .map_err(|e| CCSwarmError::config(format!("Failed to write registry: {}", e)))?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:get_stats (lines 202-219) ---[0m
    pub fn get_stats(&self) -> ExtensionStats {
        let total = self.extensions.len();
        let active = self.list_by_state(&ExtensionState::Active).len();
        let loaded = self.list_by_state(&ExtensionState::Loaded).len();
        let error_count = self.extensions.values()
            .filter(|ext| matches!(ext.state(), ExtensionState::Error(_)))
            .count();

        ExtensionStats {
            total_extensions: total,
            active_extensions: active,
            loaded_extensions: loaded,
            error_extensions: error_count,
            pending_proposals: 0, // Managed by ExtensionManager
            successful_extensions: active + loaded,
            failed_extensions: error_count,
        }
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:376-392 method approve_proposal <-> ./crates/ccswarm/src/extension_stub/mod.rs:445-464 method validate_proposal
  Similarity: 85.07%
  Classes: ExtensionManager <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:approve_proposal (lines 376-392) ---[0m
    pub async fn approve_proposal(&mut self, proposal_id: &str) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        if proposal.status != ExtensionStatus::UnderReview {
            return Err(CCSwarmError::extension(
                proposal_id,
                format!("Cannot approve proposal in status: {:?}", proposal.status)
            ));
        }

        proposal.status = ExtensionStatus::Approved;
        self.update_stats();

        tracing::info!("Extension proposal approved: {}", proposal_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:validate_proposal (lines 445-464) ---[0m
    fn validate_proposal(&self, proposal: &ExtensionProposal) -> Result<()> {
        if proposal.title.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposal title cannot be empty"));
        }

        if proposal.description.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposal description cannot be empty"));
        }

        if proposal.proposer.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposer cannot be empty"));
        }

        // Validate risk assessment
        if proposal.risk_assessment.overall_risk_score < 0.0 || proposal.risk_assessment.overall_risk_score > 1.0 {
            return Err(CCSwarmError::extension(&proposal.id, "Risk score must be between 0.0 and 1.0"));
        }

        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:407-423 method mark_implemented <-> ./crates/ccswarm/src/extension_stub/mod.rs:445-464 method validate_proposal
  Similarity: 85.07%
  Classes: ExtensionManager <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:mark_implemented (lines 407-423) ---[0m
    pub async fn mark_implemented(&mut self, proposal_id: &str) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        if proposal.status != ExtensionStatus::Approved {
            return Err(CCSwarmError::extension(
                proposal_id,
                format!("Cannot implement proposal in status: {:?}", proposal.status)
            ));
        }

        proposal.status = ExtensionStatus::Implemented;
        self.update_stats();

        tracing::info!("Extension proposal implemented: {}", proposal_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:validate_proposal (lines 445-464) ---[0m
    fn validate_proposal(&self, proposal: &ExtensionProposal) -> Result<()> {
        if proposal.title.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposal title cannot be empty"));
        }

        if proposal.description.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposal description cannot be empty"));
        }

        if proposal.proposer.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposer cannot be empty"));
        }

        // Validate risk assessment
        if proposal.risk_assessment.overall_risk_score < 0.0 || proposal.risk_assessment.overall_risk_score > 1.0 {
            return Err(CCSwarmError::extension(&proposal.id, "Risk score must be between 0.0 and 1.0"));
        }

        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:431-442 method update_stats <-> ./crates/ccswarm/src/extension_stub/mod.rs:780-805 function test_extension_stats
  Similarity: 82.78%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:update_stats (lines 431-442) ---[0m
    fn update_stats(&mut self) {
        let total = self.proposals.len();
        let pending = self.list_proposals_by_status(&ExtensionStatus::Proposed).len() +
                     self.list_proposals_by_status(&ExtensionStatus::UnderReview).len();
        let successful = self.list_proposals_by_status(&ExtensionStatus::Implemented).len();
        let failed = self.list_proposals_by_status(&ExtensionStatus::Rejected).len();

        self.stats.total_extensions = total;
        self.stats.pending_proposals = pending;
        self.stats.successful_extensions = successful;
        self.stats.failed_extensions = failed;
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stats (lines 780-805) ---[0m
    fn test_extension_stats() {
        let mut stub = ExtensionStub::new();

        let extension1 = Extension::new(
            "Extension 1",
            Version::new(1, 0, 0),
            "First extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let extension2 = Extension::new(
            "Extension 2",
            Version::new(1, 0, 0),
            "Second extension",
            "Test Author",
            vec![ExtensionCapability::Security],
        );

        stub.register(extension1).unwrap();
        stub.register(extension2).unwrap();

        let stats = stub.get_stats();
        assert_eq!(stats.total_extensions, 2);
        assert_eq!(stats.error_extensions, 0);
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:167-185 method save_to_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:376-392 method approve_proposal
  Similarity: 86.45%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:save_to_registry (lines 167-185) ---[0m
    pub async fn save_to_registry(&self) -> Result<()> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        // Create parent directory if it doesn't exist
        if let Some(parent) = registry_path.parent() {
            tokio::fs::create_dir_all(parent).await
                .map_err(|e| CCSwarmError::config(format!("Failed to create registry directory: {}", e)))?;
        }

        let extensions: Vec<Extension> = self.extensions.values().cloned().collect();
        let content = serde_json::to_string_pretty(&extensions)
            .map_err(|e| CCSwarmError::config(format!("Failed to serialize extensions: {}", e)))?;

        tokio::fs::write(registry_path, content).await
            .map_err(|e| CCSwarmError::config(format!("Failed to write registry: {}", e)))?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:approve_proposal (lines 376-392) ---[0m
    pub async fn approve_proposal(&mut self, proposal_id: &str) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        if proposal.status != ExtensionStatus::UnderReview {
            return Err(CCSwarmError::extension(
                proposal_id,
                format!("Cannot approve proposal in status: {:?}", proposal.status)
            ));
        }

        proposal.status = ExtensionStatus::Approved;
        self.update_stats();

        tracing::info!("Extension proposal approved: {}", proposal_id);
        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:167-185 method save_to_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:407-423 method mark_implemented
  Similarity: 86.45%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:save_to_registry (lines 167-185) ---[0m
    pub async fn save_to_registry(&self) -> Result<()> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        // Create parent directory if it doesn't exist
        if let Some(parent) = registry_path.parent() {
            tokio::fs::create_dir_all(parent).await
                .map_err(|e| CCSwarmError::config(format!("Failed to create registry directory: {}", e)))?;
        }

        let extensions: Vec<Extension> = self.extensions.values().cloned().collect();
        let content = serde_json::to_string_pretty(&extensions)
            .map_err(|e| CCSwarmError::config(format!("Failed to serialize extensions: {}", e)))?;

        tokio::fs::write(registry_path, content).await
            .map_err(|e| CCSwarmError::config(format!("Failed to write registry: {}", e)))?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:mark_implemented (lines 407-423) ---[0m
    pub async fn mark_implemented(&mut self, proposal_id: &str) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        if proposal.status != ExtensionStatus::Approved {
            return Err(CCSwarmError::extension(
                proposal_id,
                format!("Cannot implement proposal in status: {:?}", proposal.status)
            ));
        }

        proposal.status = ExtensionStatus::Implemented;
        self.update_stats();

        tracing::info!("Extension proposal implemented: {}", proposal_id);
        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:625-643 function test_extension_stub_basic_operations <-> ./crates/ccswarm/src/extension_stub/mod.rs:698-720 function test_extension_stub_registry_persistence
  Similarity: 72.65%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_basic_operations (lines 625-643) ---[0m
    fn test_extension_stub_basic_operations() {
        let mut stub = ExtensionStub::new();
        assert_eq!(stub.count(), 0);

        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let ext_id = extension.id().to_string();
        stub.register(extension).unwrap();

        assert_eq!(stub.count(), 1);
        assert!(stub.contains(&ext_id));
        assert!(stub.get(&ext_id).is_some());
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_registry_persistence (lines 698-720) ---[0m
    async fn test_extension_stub_registry_persistence() {
        let temp_dir = TempDir::new().unwrap();
        let registry_path = temp_dir.path().join("extensions.json");

        let mut stub = ExtensionStub::with_registry(&registry_path);

        let extension = Extension::new(
            "Persistent Extension",
            Version::new(1, 0, 0),
            "A persistent extension",
            "Test Author",
            vec![ExtensionCapability::Integration],
        );

        stub.register(extension).unwrap();
        stub.save_to_registry().await.unwrap();

        let mut new_stub = ExtensionStub::with_registry(&registry_path);
        let loaded_count = new_stub.load_from_registry().await.unwrap();

        assert_eq!(loaded_count, 1);
        assert_eq!(new_stub.count(), 1);
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:467-482 method cleanup_old_proposals <-> ./crates/ccswarm/src/extension_stub/mod.rs:780-805 function test_extension_stats
  Similarity: 72.16%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:cleanup_old_proposals (lines 467-482) ---[0m
    pub async fn cleanup_old_proposals(&mut self, max_age: chrono::Duration) -> Result<usize> {
        let cutoff = chrono::Utc::now() - max_age;
        let initial_count = self.proposals.len();

        self.proposals.retain(|_, proposal| {
            proposal.status != ExtensionStatus::Rejected || proposal.created_at > cutoff
        });

        let removed = initial_count - self.proposals.len();
        if removed > 0 {
            self.update_stats();
            tracing::info!("Cleaned up {} old proposals", removed);
        }

        Ok(removed)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stats (lines 780-805) ---[0m
    fn test_extension_stats() {
        let mut stub = ExtensionStub::new();

        let extension1 = Extension::new(
            "Extension 1",
            Version::new(1, 0, 0),
            "First extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let extension2 = Extension::new(
            "Extension 2",
            Version::new(1, 0, 0),
            "Second extension",
            "Test Author",
            vec![ExtensionCapability::Security],
        );

        stub.register(extension1).unwrap();
        stub.register(extension2).unwrap();

        let stats = stub.get_stats();
        assert_eq!(stats.total_extensions, 2);
        assert_eq!(stats.error_extensions, 0);
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:167-185 method save_to_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:625-643 function test_extension_stub_basic_operations
  Similarity: 79.55%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:save_to_registry (lines 167-185) ---[0m
    pub async fn save_to_registry(&self) -> Result<()> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        // Create parent directory if it doesn't exist
        if let Some(parent) = registry_path.parent() {
            tokio::fs::create_dir_all(parent).await
                .map_err(|e| CCSwarmError::config(format!("Failed to create registry directory: {}", e)))?;
        }

        let extensions: Vec<Extension> = self.extensions.values().cloned().collect();
        let content = serde_json::to_string_pretty(&extensions)
            .map_err(|e| CCSwarmError::config(format!("Failed to serialize extensions: {}", e)))?;

        tokio::fs::write(registry_path, content).await
            .map_err(|e| CCSwarmError::config(format!("Failed to write registry: {}", e)))?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_basic_operations (lines 625-643) ---[0m
    fn test_extension_stub_basic_operations() {
        let mut stub = ExtensionStub::new();
        assert_eq!(stub.count(), 0);

        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let ext_id = extension.id().to_string();
        stub.register(extension).unwrap();

        assert_eq!(stub.count(), 1);
        assert!(stub.contains(&ext_id));
        assert!(stub.get(&ext_id).is_some());
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:167-185 method save_to_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:467-482 method cleanup_old_proposals
  Similarity: 85.09%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:save_to_registry (lines 167-185) ---[0m
    pub async fn save_to_registry(&self) -> Result<()> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        // Create parent directory if it doesn't exist
        if let Some(parent) = registry_path.parent() {
            tokio::fs::create_dir_all(parent).await
                .map_err(|e| CCSwarmError::config(format!("Failed to create registry directory: {}", e)))?;
        }

        let extensions: Vec<Extension> = self.extensions.values().cloned().collect();
        let content = serde_json::to_string_pretty(&extensions)
            .map_err(|e| CCSwarmError::config(format!("Failed to serialize extensions: {}", e)))?;

        tokio::fs::write(registry_path, content).await
            .map_err(|e| CCSwarmError::config(format!("Failed to write registry: {}", e)))?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:cleanup_old_proposals (lines 467-482) ---[0m
    pub async fn cleanup_old_proposals(&mut self, max_age: chrono::Duration) -> Result<usize> {
        let cutoff = chrono::Utc::now() - max_age;
        let initial_count = self.proposals.len();

        self.proposals.retain(|_, proposal| {
            proposal.status != ExtensionStatus::Rejected || proposal.created_at > cutoff
        });

        let removed = initial_count - self.proposals.len();
        if removed > 0 {
            self.update_stats();
            tracing::info!("Cleaned up {} old proposals", removed);
        }

        Ok(removed)
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:140-164 method load_from_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:431-442 method update_stats
  Similarity: 80.32%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:load_from_registry (lines 140-164) ---[0m
    pub async fn load_from_registry(&mut self) -> Result<usize> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        if !registry_path.exists() {
            return Ok(0);
        }

        let content = tokio::fs::read_to_string(registry_path).await
            .map_err(|e| CCSwarmError::config(format!("Failed to read registry: {}", e)))?;

        let extensions: Vec<Extension> = serde_json::from_str(&content)
            .map_err(|e| CCSwarmError::config(format!("Failed to parse registry: {}", e)))?;

        let mut loaded_count = 0;
        for extension in extensions {
            if let Err(e) = self.register(extension) {
                tracing::warn!("Failed to load extension from registry: {}", e);
            } else {
                loaded_count += 1;
            }
        }

        Ok(loaded_count)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:update_stats (lines 431-442) ---[0m
    fn update_stats(&mut self) {
        let total = self.proposals.len();
        let pending = self.list_proposals_by_status(&ExtensionStatus::Proposed).len() +
                     self.list_proposals_by_status(&ExtensionStatus::UnderReview).len();
        let successful = self.list_proposals_by_status(&ExtensionStatus::Implemented).len();
        let failed = self.list_proposals_by_status(&ExtensionStatus::Rejected).len();

        self.stats.total_extensions = total;
        self.stats.pending_proposals = pending;
        self.stats.successful_extensions = successful;
        self.stats.failed_extensions = failed;
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:445-464 method validate_proposal <-> ./crates/ccswarm/src/extension_stub/mod.rs:467-482 method cleanup_old_proposals
  Similarity: 82.15%
  Classes: ExtensionManager <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:validate_proposal (lines 445-464) ---[0m
    fn validate_proposal(&self, proposal: &ExtensionProposal) -> Result<()> {
        if proposal.title.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposal title cannot be empty"));
        }

        if proposal.description.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposal description cannot be empty"));
        }

        if proposal.proposer.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposer cannot be empty"));
        }

        // Validate risk assessment
        if proposal.risk_assessment.overall_risk_score < 0.0 || proposal.risk_assessment.overall_risk_score > 1.0 {
            return Err(CCSwarmError::extension(&proposal.id, "Risk score must be between 0.0 and 1.0"));
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:cleanup_old_proposals (lines 467-482) ---[0m
    pub async fn cleanup_old_proposals(&mut self, max_age: chrono::Duration) -> Result<usize> {
        let cutoff = chrono::Utc::now() - max_age;
        let initial_count = self.proposals.len();

        self.proposals.retain(|_, proposal| {
            proposal.status != ExtensionStatus::Rejected || proposal.created_at > cutoff
        });

        let removed = initial_count - self.proposals.len();
        if removed > 0 {
            self.update_stats();
            tracing::info!("Cleaned up {} old proposals", removed);
        }

        Ok(removed)
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:335-350 method propose_extension <-> ./crates/ccswarm/src/extension_stub/mod.rs:698-720 function test_extension_stub_registry_persistence
  Similarity: 75.71%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:propose_extension (lines 335-350) ---[0m
    pub async fn propose_extension(&mut self, mut proposal: ExtensionProposal) -> Result<String> {
        // Validate proposal
        self.validate_proposal(&proposal)?;

        // Auto-approve if enabled
        if self.auto_approve {
            proposal.status = ExtensionStatus::Approved;
        }

        let id = proposal.id.clone();
        self.proposals.insert(id.clone(), proposal);
        self.update_stats();

        tracing::info!("Extension proposal submitted: {}", id);
        Ok(id)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_registry_persistence (lines 698-720) ---[0m
    async fn test_extension_stub_registry_persistence() {
        let temp_dir = TempDir::new().unwrap();
        let registry_path = temp_dir.path().join("extensions.json");

        let mut stub = ExtensionStub::with_registry(&registry_path);

        let extension = Extension::new(
            "Persistent Extension",
            Version::new(1, 0, 0),
            "A persistent extension",
            "Test Author",
            vec![ExtensionCapability::Integration],
        );

        stub.register(extension).unwrap();
        stub.save_to_registry().await.unwrap();

        let mut new_stub = ExtensionStub::with_registry(&registry_path);
        let loaded_count = new_stub.load_from_registry().await.unwrap();

        assert_eq!(loaded_count, 1);
        assert_eq!(new_stub.count(), 1);
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:202-219 method get_stats <-> ./crates/ccswarm/src/extension_stub/mod.rs:646-662 function test_extension_stub_duplicate_registration
  Similarity: 82.04%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:get_stats (lines 202-219) ---[0m
    pub fn get_stats(&self) -> ExtensionStats {
        let total = self.extensions.len();
        let active = self.list_by_state(&ExtensionState::Active).len();
        let loaded = self.list_by_state(&ExtensionState::Loaded).len();
        let error_count = self.extensions.values()
            .filter(|ext| matches!(ext.state(), ExtensionState::Error(_)))
            .count();

        ExtensionStats {
            total_extensions: total,
            active_extensions: active,
            loaded_extensions: loaded,
            error_extensions: error_count,
            pending_proposals: 0, // Managed by ExtensionManager
            successful_extensions: active + loaded,
            failed_extensions: error_count,
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_duplicate_registration (lines 646-662) ---[0m
    fn test_extension_stub_duplicate_registration() {
        let mut stub = ExtensionStub::new();

        let extension1 = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let extension2 = extension1.clone();

        stub.register(extension1).unwrap();
        let result = stub.register(extension2);
        assert!(result.is_err());
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:335-350 method propose_extension <-> ./crates/ccswarm/src/extension_stub/mod.rs:445-464 method validate_proposal
  Similarity: 78.70%
  Classes: ExtensionManager <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:propose_extension (lines 335-350) ---[0m
    pub async fn propose_extension(&mut self, mut proposal: ExtensionProposal) -> Result<String> {
        // Validate proposal
        self.validate_proposal(&proposal)?;

        // Auto-approve if enabled
        if self.auto_approve {
            proposal.status = ExtensionStatus::Approved;
        }

        let id = proposal.id.clone();
        self.proposals.insert(id.clone(), proposal);
        self.update_stats();

        tracing::info!("Extension proposal submitted: {}", id);
        Ok(id)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:validate_proposal (lines 445-464) ---[0m
    fn validate_proposal(&self, proposal: &ExtensionProposal) -> Result<()> {
        if proposal.title.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposal title cannot be empty"));
        }

        if proposal.description.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposal description cannot be empty"));
        }

        if proposal.proposer.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposer cannot be empty"));
        }

        // Validate risk assessment
        if proposal.risk_assessment.overall_risk_score < 0.0 || proposal.risk_assessment.overall_risk_score > 1.0 {
            return Err(CCSwarmError::extension(&proposal.id, "Risk score must be between 0.0 and 1.0"));
        }

        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:85-104 method unregister <-> ./crates/ccswarm/src/extension_stub/mod.rs:646-662 function test_extension_stub_duplicate_registration
  Similarity: 76.45%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:unregister (lines 85-104) ---[0m
    pub fn unregister(&mut self, extension_id: &str) -> Result<Extension> {
        // Check if other extensions depend on this one
        let dependents: Vec<String> = self.extensions
            .values()
            .filter(|ext| {
                ext.dependencies.iter().any(|dep| dep.name == extension_id && !dep.optional)
            })
            .map(|ext| ext.id().to_string())
            .collect();

        if !dependents.is_empty() {
            return Err(CCSwarmError::extension(
                extension_id,
                format!("Cannot unregister extension: required by {:?}", dependents)
            ));
        }

        self.extensions.remove(extension_id)
            .ok_or_else(|| CCSwarmError::extension(extension_id, "Extension not found"))
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_duplicate_registration (lines 646-662) ---[0m
    fn test_extension_stub_duplicate_registration() {
        let mut stub = ExtensionStub::new();

        let extension1 = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let extension2 = extension1.clone();

        stub.register(extension1).unwrap();
        let result = stub.register(extension2);
        assert!(result.is_err());
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:85-104 method unregister <-> ./crates/ccswarm/src/extension_stub/mod.rs:445-464 method validate_proposal
  Similarity: 70.40%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:unregister (lines 85-104) ---[0m
    pub fn unregister(&mut self, extension_id: &str) -> Result<Extension> {
        // Check if other extensions depend on this one
        let dependents: Vec<String> = self.extensions
            .values()
            .filter(|ext| {
                ext.dependencies.iter().any(|dep| dep.name == extension_id && !dep.optional)
            })
            .map(|ext| ext.id().to_string())
            .collect();

        if !dependents.is_empty() {
            return Err(CCSwarmError::extension(
                extension_id,
                format!("Cannot unregister extension: required by {:?}", dependents)
            ));
        }

        self.extensions.remove(extension_id)
            .ok_or_else(|| CCSwarmError::extension(extension_id, "Extension not found"))
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:validate_proposal (lines 445-464) ---[0m
    fn validate_proposal(&self, proposal: &ExtensionProposal) -> Result<()> {
        if proposal.title.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposal title cannot be empty"));
        }

        if proposal.description.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposal description cannot be empty"));
        }

        if proposal.proposer.trim().is_empty() {
            return Err(CCSwarmError::extension(&proposal.id, "Proposer cannot be empty"));
        }

        // Validate risk assessment
        if proposal.risk_assessment.overall_risk_score < 0.0 || proposal.risk_assessment.overall_risk_score > 1.0 {
            return Err(CCSwarmError::extension(&proposal.id, "Risk score must be between 0.0 and 1.0"));
        }

        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:85-104 method unregister <-> ./crates/ccswarm/src/extension_stub/mod.rs:167-185 method save_to_registry
  Similarity: 71.62%
  Classes: ExtensionStub <-> ExtensionStub

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:unregister (lines 85-104) ---[0m
    pub fn unregister(&mut self, extension_id: &str) -> Result<Extension> {
        // Check if other extensions depend on this one
        let dependents: Vec<String> = self.extensions
            .values()
            .filter(|ext| {
                ext.dependencies.iter().any(|dep| dep.name == extension_id && !dep.optional)
            })
            .map(|ext| ext.id().to_string())
            .collect();

        if !dependents.is_empty() {
            return Err(CCSwarmError::extension(
                extension_id,
                format!("Cannot unregister extension: required by {:?}", dependents)
            ));
        }

        self.extensions.remove(extension_id)
            .ok_or_else(|| CCSwarmError::extension(extension_id, "Extension not found"))
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:save_to_registry (lines 167-185) ---[0m
    pub async fn save_to_registry(&self) -> Result<()> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        // Create parent directory if it doesn't exist
        if let Some(parent) = registry_path.parent() {
            tokio::fs::create_dir_all(parent).await
                .map_err(|e| CCSwarmError::config(format!("Failed to create registry directory: {}", e)))?;
        }

        let extensions: Vec<Extension> = self.extensions.values().cloned().collect();
        let content = serde_json::to_string_pretty(&extensions)
            .map_err(|e| CCSwarmError::config(format!("Failed to serialize extensions: {}", e)))?;

        tokio::fs::write(registry_path, content).await
            .map_err(|e| CCSwarmError::config(format!("Failed to write registry: {}", e)))?;

        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:85-104 method unregister <-> ./crates/ccswarm/src/extension_stub/mod.rs:376-392 method approve_proposal
  Similarity: 73.86%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:unregister (lines 85-104) ---[0m
    pub fn unregister(&mut self, extension_id: &str) -> Result<Extension> {
        // Check if other extensions depend on this one
        let dependents: Vec<String> = self.extensions
            .values()
            .filter(|ext| {
                ext.dependencies.iter().any(|dep| dep.name == extension_id && !dep.optional)
            })
            .map(|ext| ext.id().to_string())
            .collect();

        if !dependents.is_empty() {
            return Err(CCSwarmError::extension(
                extension_id,
                format!("Cannot unregister extension: required by {:?}", dependents)
            ));
        }

        self.extensions.remove(extension_id)
            .ok_or_else(|| CCSwarmError::extension(extension_id, "Extension not found"))
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:approve_proposal (lines 376-392) ---[0m
    pub async fn approve_proposal(&mut self, proposal_id: &str) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        if proposal.status != ExtensionStatus::UnderReview {
            return Err(CCSwarmError::extension(
                proposal_id,
                format!("Cannot approve proposal in status: {:?}", proposal.status)
            ));
        }

        proposal.status = ExtensionStatus::Approved;
        self.update_stats();

        tracing::info!("Extension proposal approved: {}", proposal_id);
        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:85-104 method unregister <-> ./crates/ccswarm/src/extension_stub/mod.rs:407-423 method mark_implemented
  Similarity: 73.86%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:unregister (lines 85-104) ---[0m
    pub fn unregister(&mut self, extension_id: &str) -> Result<Extension> {
        // Check if other extensions depend on this one
        let dependents: Vec<String> = self.extensions
            .values()
            .filter(|ext| {
                ext.dependencies.iter().any(|dep| dep.name == extension_id && !dep.optional)
            })
            .map(|ext| ext.id().to_string())
            .collect();

        if !dependents.is_empty() {
            return Err(CCSwarmError::extension(
                extension_id,
                format!("Cannot unregister extension: required by {:?}", dependents)
            ));
        }

        self.extensions.remove(extension_id)
            .ok_or_else(|| CCSwarmError::extension(extension_id, "Extension not found"))
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:mark_implemented (lines 407-423) ---[0m
    pub async fn mark_implemented(&mut self, proposal_id: &str) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        if proposal.status != ExtensionStatus::Approved {
            return Err(CCSwarmError::extension(
                proposal_id,
                format!("Cannot implement proposal in status: {:?}", proposal.status)
            ));
        }

        proposal.status = ExtensionStatus::Implemented;
        self.update_stats();

        tracing::info!("Extension proposal implemented: {}", proposal_id);
        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:202-219 method get_stats <-> ./crates/ccswarm/src/extension_stub/mod.rs:376-392 method approve_proposal
  Similarity: 77.49%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:get_stats (lines 202-219) ---[0m
    pub fn get_stats(&self) -> ExtensionStats {
        let total = self.extensions.len();
        let active = self.list_by_state(&ExtensionState::Active).len();
        let loaded = self.list_by_state(&ExtensionState::Loaded).len();
        let error_count = self.extensions.values()
            .filter(|ext| matches!(ext.state(), ExtensionState::Error(_)))
            .count();

        ExtensionStats {
            total_extensions: total,
            active_extensions: active,
            loaded_extensions: loaded,
            error_extensions: error_count,
            pending_proposals: 0, // Managed by ExtensionManager
            successful_extensions: active + loaded,
            failed_extensions: error_count,
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:approve_proposal (lines 376-392) ---[0m
    pub async fn approve_proposal(&mut self, proposal_id: &str) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        if proposal.status != ExtensionStatus::UnderReview {
            return Err(CCSwarmError::extension(
                proposal_id,
                format!("Cannot approve proposal in status: {:?}", proposal.status)
            ));
        }

        proposal.status = ExtensionStatus::Approved;
        self.update_stats();

        tracing::info!("Extension proposal approved: {}", proposal_id);
        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:202-219 method get_stats <-> ./crates/ccswarm/src/extension_stub/mod.rs:407-423 method mark_implemented
  Similarity: 77.49%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:get_stats (lines 202-219) ---[0m
    pub fn get_stats(&self) -> ExtensionStats {
        let total = self.extensions.len();
        let active = self.list_by_state(&ExtensionState::Active).len();
        let loaded = self.list_by_state(&ExtensionState::Loaded).len();
        let error_count = self.extensions.values()
            .filter(|ext| matches!(ext.state(), ExtensionState::Error(_)))
            .count();

        ExtensionStats {
            total_extensions: total,
            active_extensions: active,
            loaded_extensions: loaded,
            error_extensions: error_count,
            pending_proposals: 0, // Managed by ExtensionManager
            successful_extensions: active + loaded,
            failed_extensions: error_count,
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:mark_implemented (lines 407-423) ---[0m
    pub async fn mark_implemented(&mut self, proposal_id: &str) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        if proposal.status != ExtensionStatus::Approved {
            return Err(CCSwarmError::extension(
                proposal_id,
                format!("Cannot implement proposal in status: {:?}", proposal.status)
            ));
        }

        proposal.status = ExtensionStatus::Implemented;
        self.update_stats();

        tracing::info!("Extension proposal implemented: {}", proposal_id);
        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:85-104 method unregister <-> ./crates/ccswarm/src/extension_stub/mod.rs:202-219 method get_stats
  Similarity: 71.02%
  Classes: ExtensionStub <-> ExtensionStub

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:unregister (lines 85-104) ---[0m
    pub fn unregister(&mut self, extension_id: &str) -> Result<Extension> {
        // Check if other extensions depend on this one
        let dependents: Vec<String> = self.extensions
            .values()
            .filter(|ext| {
                ext.dependencies.iter().any(|dep| dep.name == extension_id && !dep.optional)
            })
            .map(|ext| ext.id().to_string())
            .collect();

        if !dependents.is_empty() {
            return Err(CCSwarmError::extension(
                extension_id,
                format!("Cannot unregister extension: required by {:?}", dependents)
            ));
        }

        self.extensions.remove(extension_id)
            .ok_or_else(|| CCSwarmError::extension(extension_id, "Extension not found"))
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:get_stats (lines 202-219) ---[0m
    pub fn get_stats(&self) -> ExtensionStats {
        let total = self.extensions.len();
        let active = self.list_by_state(&ExtensionState::Active).len();
        let loaded = self.list_by_state(&ExtensionState::Loaded).len();
        let error_count = self.extensions.values()
            .filter(|ext| matches!(ext.state(), ExtensionState::Error(_)))
            .count();

        ExtensionStats {
            total_extensions: total,
            active_extensions: active,
            loaded_extensions: loaded,
            error_extensions: error_count,
            pending_proposals: 0, // Managed by ExtensionManager
            successful_extensions: active + loaded,
            failed_extensions: error_count,
        }
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:625-643 function test_extension_stub_basic_operations <-> ./crates/ccswarm/src/extension_stub/mod.rs:646-662 function test_extension_stub_duplicate_registration
  Similarity: 74.83%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_basic_operations (lines 625-643) ---[0m
    fn test_extension_stub_basic_operations() {
        let mut stub = ExtensionStub::new();
        assert_eq!(stub.count(), 0);

        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let ext_id = extension.id().to_string();
        stub.register(extension).unwrap();

        assert_eq!(stub.count(), 1);
        assert!(stub.contains(&ext_id));
        assert!(stub.get(&ext_id).is_some());
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_duplicate_registration (lines 646-662) ---[0m
    fn test_extension_stub_duplicate_registration() {
        let mut stub = ExtensionStub::new();

        let extension1 = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let extension2 = extension1.clone();

        stub.register(extension1).unwrap();
        let result = stub.register(extension2);
        assert!(result.is_err());
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:85-104 method unregister <-> ./crates/ccswarm/src/extension_stub/mod.rs:467-482 method cleanup_old_proposals
  Similarity: 73.71%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:unregister (lines 85-104) ---[0m
    pub fn unregister(&mut self, extension_id: &str) -> Result<Extension> {
        // Check if other extensions depend on this one
        let dependents: Vec<String> = self.extensions
            .values()
            .filter(|ext| {
                ext.dependencies.iter().any(|dep| dep.name == extension_id && !dep.optional)
            })
            .map(|ext| ext.id().to_string())
            .collect();

        if !dependents.is_empty() {
            return Err(CCSwarmError::extension(
                extension_id,
                format!("Cannot unregister extension: required by {:?}", dependents)
            ));
        }

        self.extensions.remove(extension_id)
            .ok_or_else(|| CCSwarmError::extension(extension_id, "Extension not found"))
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:cleanup_old_proposals (lines 467-482) ---[0m
    pub async fn cleanup_old_proposals(&mut self, max_age: chrono::Duration) -> Result<usize> {
        let cutoff = chrono::Utc::now() - max_age;
        let initial_count = self.proposals.len();

        self.proposals.retain(|_, proposal| {
            proposal.status != ExtensionStatus::Rejected || proposal.created_at > cutoff
        });

        let removed = initial_count - self.proposals.len();
        if removed > 0 {
            self.update_stats();
            tracing::info!("Cleaned up {} old proposals", removed);
        }

        Ok(removed)
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:431-442 method update_stats <-> ./crates/ccswarm/src/extension_stub/mod.rs:625-643 function test_extension_stub_basic_operations
  Similarity: 82.93%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:update_stats (lines 431-442) ---[0m
    fn update_stats(&mut self) {
        let total = self.proposals.len();
        let pending = self.list_proposals_by_status(&ExtensionStatus::Proposed).len() +
                     self.list_proposals_by_status(&ExtensionStatus::UnderReview).len();
        let successful = self.list_proposals_by_status(&ExtensionStatus::Implemented).len();
        let failed = self.list_proposals_by_status(&ExtensionStatus::Rejected).len();

        self.stats.total_extensions = total;
        self.stats.pending_proposals = pending;
        self.stats.successful_extensions = successful;
        self.stats.failed_extensions = failed;
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_basic_operations (lines 625-643) ---[0m
    fn test_extension_stub_basic_operations() {
        let mut stub = ExtensionStub::new();
        assert_eq!(stub.count(), 0);

        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let ext_id = extension.id().to_string();
        stub.register(extension).unwrap();

        assert_eq!(stub.count(), 1);
        assert!(stub.contains(&ext_id));
        assert!(stub.get(&ext_id).is_some());
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:467-482 method cleanup_old_proposals <-> ./crates/ccswarm/src/extension_stub/mod.rs:646-662 function test_extension_stub_duplicate_registration
  Similarity: 77.71%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:cleanup_old_proposals (lines 467-482) ---[0m
    pub async fn cleanup_old_proposals(&mut self, max_age: chrono::Duration) -> Result<usize> {
        let cutoff = chrono::Utc::now() - max_age;
        let initial_count = self.proposals.len();

        self.proposals.retain(|_, proposal| {
            proposal.status != ExtensionStatus::Rejected || proposal.created_at > cutoff
        });

        let removed = initial_count - self.proposals.len();
        if removed > 0 {
            self.update_stats();
            tracing::info!("Cleaned up {} old proposals", removed);
        }

        Ok(removed)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_duplicate_registration (lines 646-662) ---[0m
    fn test_extension_stub_duplicate_registration() {
        let mut stub = ExtensionStub::new();

        let extension1 = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let extension2 = extension1.clone();

        stub.register(extension1).unwrap();
        let result = stub.register(extension2);
        assert!(result.is_err());
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:335-350 method propose_extension <-> ./crates/ccswarm/src/extension_stub/mod.rs:625-643 function test_extension_stub_basic_operations
  Similarity: 73.22%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:propose_extension (lines 335-350) ---[0m
    pub async fn propose_extension(&mut self, mut proposal: ExtensionProposal) -> Result<String> {
        // Validate proposal
        self.validate_proposal(&proposal)?;

        // Auto-approve if enabled
        if self.auto_approve {
            proposal.status = ExtensionStatus::Approved;
        }

        let id = proposal.id.clone();
        self.proposals.insert(id.clone(), proposal);
        self.update_stats();

        tracing::info!("Extension proposal submitted: {}", id);
        Ok(id)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_basic_operations (lines 625-643) ---[0m
    fn test_extension_stub_basic_operations() {
        let mut stub = ExtensionStub::new();
        assert_eq!(stub.count(), 0);

        let extension = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let ext_id = extension.id().to_string();
        stub.register(extension).unwrap();

        assert_eq!(stub.count(), 1);
        assert!(stub.contains(&ext_id));
        assert!(stub.get(&ext_id).is_some());
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:85-104 method unregister <-> ./crates/ccswarm/src/extension_stub/mod.rs:335-350 method propose_extension
  Similarity: 70.56%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:unregister (lines 85-104) ---[0m
    pub fn unregister(&mut self, extension_id: &str) -> Result<Extension> {
        // Check if other extensions depend on this one
        let dependents: Vec<String> = self.extensions
            .values()
            .filter(|ext| {
                ext.dependencies.iter().any(|dep| dep.name == extension_id && !dep.optional)
            })
            .map(|ext| ext.id().to_string())
            .collect();

        if !dependents.is_empty() {
            return Err(CCSwarmError::extension(
                extension_id,
                format!("Cannot unregister extension: required by {:?}", dependents)
            ));
        }

        self.extensions.remove(extension_id)
            .ok_or_else(|| CCSwarmError::extension(extension_id, "Extension not found"))
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:propose_extension (lines 335-350) ---[0m
    pub async fn propose_extension(&mut self, mut proposal: ExtensionProposal) -> Result<String> {
        // Validate proposal
        self.validate_proposal(&proposal)?;

        // Auto-approve if enabled
        if self.auto_approve {
            proposal.status = ExtensionStatus::Approved;
        }

        let id = proposal.id.clone();
        self.proposals.insert(id.clone(), proposal);
        self.update_stats();

        tracing::info!("Extension proposal submitted: {}", id);
        Ok(id)
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:167-185 method save_to_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:431-442 method update_stats
  Similarity: 81.55%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:save_to_registry (lines 167-185) ---[0m
    pub async fn save_to_registry(&self) -> Result<()> {
        let registry_path = self.registry_path.as_ref()
            .ok_or_else(|| CCSwarmError::config("No registry path configured"))?;

        // Create parent directory if it doesn't exist
        if let Some(parent) = registry_path.parent() {
            tokio::fs::create_dir_all(parent).await
                .map_err(|e| CCSwarmError::config(format!("Failed to create registry directory: {}", e)))?;
        }

        let extensions: Vec<Extension> = self.extensions.values().cloned().collect();
        let content = serde_json::to_string_pretty(&extensions)
            .map_err(|e| CCSwarmError::config(format!("Failed to serialize extensions: {}", e)))?;

        tokio::fs::write(registry_path, content).await
            .map_err(|e| CCSwarmError::config(format!("Failed to write registry: {}", e)))?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:update_stats (lines 431-442) ---[0m
    fn update_stats(&mut self) {
        let total = self.proposals.len();
        let pending = self.list_proposals_by_status(&ExtensionStatus::Proposed).len() +
                     self.list_proposals_by_status(&ExtensionStatus::UnderReview).len();
        let successful = self.list_proposals_by_status(&ExtensionStatus::Implemented).len();
        let failed = self.list_proposals_by_status(&ExtensionStatus::Rejected).len();

        self.stats.total_extensions = total;
        self.stats.pending_proposals = pending;
        self.stats.successful_extensions = successful;
        self.stats.failed_extensions = failed;
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:376-392 method approve_proposal <-> ./crates/ccswarm/src/extension_stub/mod.rs:467-482 method cleanup_old_proposals
  Similarity: 76.31%
  Classes: ExtensionManager <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:approve_proposal (lines 376-392) ---[0m
    pub async fn approve_proposal(&mut self, proposal_id: &str) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        if proposal.status != ExtensionStatus::UnderReview {
            return Err(CCSwarmError::extension(
                proposal_id,
                format!("Cannot approve proposal in status: {:?}", proposal.status)
            ));
        }

        proposal.status = ExtensionStatus::Approved;
        self.update_stats();

        tracing::info!("Extension proposal approved: {}", proposal_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:cleanup_old_proposals (lines 467-482) ---[0m
    pub async fn cleanup_old_proposals(&mut self, max_age: chrono::Duration) -> Result<usize> {
        let cutoff = chrono::Utc::now() - max_age;
        let initial_count = self.proposals.len();

        self.proposals.retain(|_, proposal| {
            proposal.status != ExtensionStatus::Rejected || proposal.created_at > cutoff
        });

        let removed = initial_count - self.proposals.len();
        if removed > 0 {
            self.update_stats();
            tracing::info!("Cleaned up {} old proposals", removed);
        }

        Ok(removed)
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:407-423 method mark_implemented <-> ./crates/ccswarm/src/extension_stub/mod.rs:467-482 method cleanup_old_proposals
  Similarity: 76.31%
  Classes: ExtensionManager <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:mark_implemented (lines 407-423) ---[0m
    pub async fn mark_implemented(&mut self, proposal_id: &str) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        if proposal.status != ExtensionStatus::Approved {
            return Err(CCSwarmError::extension(
                proposal_id,
                format!("Cannot implement proposal in status: {:?}", proposal.status)
            ));
        }

        proposal.status = ExtensionStatus::Implemented;
        self.update_stats();

        tracing::info!("Extension proposal implemented: {}", proposal_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:cleanup_old_proposals (lines 467-482) ---[0m
    pub async fn cleanup_old_proposals(&mut self, max_age: chrono::Duration) -> Result<usize> {
        let cutoff = chrono::Utc::now() - max_age;
        let initial_count = self.proposals.len();

        self.proposals.retain(|_, proposal| {
            proposal.status != ExtensionStatus::Rejected || proposal.created_at > cutoff
        });

        let removed = initial_count - self.proposals.len();
        if removed > 0 {
            self.update_stats();
            tracing::info!("Cleaned up {} old proposals", removed);
        }

        Ok(removed)
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:431-442 method update_stats <-> ./crates/ccswarm/src/extension_stub/mod.rs:698-720 function test_extension_stub_registry_persistence
  Similarity: 71.64%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:update_stats (lines 431-442) ---[0m
    fn update_stats(&mut self) {
        let total = self.proposals.len();
        let pending = self.list_proposals_by_status(&ExtensionStatus::Proposed).len() +
                     self.list_proposals_by_status(&ExtensionStatus::UnderReview).len();
        let successful = self.list_proposals_by_status(&ExtensionStatus::Implemented).len();
        let failed = self.list_proposals_by_status(&ExtensionStatus::Rejected).len();

        self.stats.total_extensions = total;
        self.stats.pending_proposals = pending;
        self.stats.successful_extensions = successful;
        self.stats.failed_extensions = failed;
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_registry_persistence (lines 698-720) ---[0m
    async fn test_extension_stub_registry_persistence() {
        let temp_dir = TempDir::new().unwrap();
        let registry_path = temp_dir.path().join("extensions.json");

        let mut stub = ExtensionStub::with_registry(&registry_path);

        let extension = Extension::new(
            "Persistent Extension",
            Version::new(1, 0, 0),
            "A persistent extension",
            "Test Author",
            vec![ExtensionCapability::Integration],
        );

        stub.register(extension).unwrap();
        stub.save_to_registry().await.unwrap();

        let mut new_stub = ExtensionStub::with_registry(&registry_path);
        let loaded_count = new_stub.load_from_registry().await.unwrap();

        assert_eq!(loaded_count, 1);
        assert_eq!(new_stub.count(), 1);
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:202-219 method get_stats <-> ./crates/ccswarm/src/extension_stub/mod.rs:467-482 method cleanup_old_proposals
  Similarity: 73.47%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:get_stats (lines 202-219) ---[0m
    pub fn get_stats(&self) -> ExtensionStats {
        let total = self.extensions.len();
        let active = self.list_by_state(&ExtensionState::Active).len();
        let loaded = self.list_by_state(&ExtensionState::Loaded).len();
        let error_count = self.extensions.values()
            .filter(|ext| matches!(ext.state(), ExtensionState::Error(_)))
            .count();

        ExtensionStats {
            total_extensions: total,
            active_extensions: active,
            loaded_extensions: loaded,
            error_extensions: error_count,
            pending_proposals: 0, // Managed by ExtensionManager
            successful_extensions: active + loaded,
            failed_extensions: error_count,
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:cleanup_old_proposals (lines 467-482) ---[0m
    pub async fn cleanup_old_proposals(&mut self, max_age: chrono::Duration) -> Result<usize> {
        let cutoff = chrono::Utc::now() - max_age;
        let initial_count = self.proposals.len();

        self.proposals.retain(|_, proposal| {
            proposal.status != ExtensionStatus::Rejected || proposal.created_at > cutoff
        });

        let removed = initial_count - self.proposals.len();
        if removed > 0 {
            self.update_stats();
            tracing::info!("Cleaned up {} old proposals", removed);
        }

        Ok(removed)
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:202-219 method get_stats <-> ./crates/ccswarm/src/extension_stub/mod.rs:335-350 method propose_extension
  Similarity: 72.28%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:get_stats (lines 202-219) ---[0m
    pub fn get_stats(&self) -> ExtensionStats {
        let total = self.extensions.len();
        let active = self.list_by_state(&ExtensionState::Active).len();
        let loaded = self.list_by_state(&ExtensionState::Loaded).len();
        let error_count = self.extensions.values()
            .filter(|ext| matches!(ext.state(), ExtensionState::Error(_)))
            .count();

        ExtensionStats {
            total_extensions: total,
            active_extensions: active,
            loaded_extensions: loaded,
            error_extensions: error_count,
            pending_proposals: 0, // Managed by ExtensionManager
            successful_extensions: active + loaded,
            failed_extensions: error_count,
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:propose_extension (lines 335-350) ---[0m
    pub async fn propose_extension(&mut self, mut proposal: ExtensionProposal) -> Result<String> {
        // Validate proposal
        self.validate_proposal(&proposal)?;

        // Auto-approve if enabled
        if self.auto_approve {
            proposal.status = ExtensionStatus::Approved;
        }

        let id = proposal.id.clone();
        self.proposals.insert(id.clone(), proposal);
        self.update_stats();

        tracing::info!("Extension proposal submitted: {}", id);
        Ok(id)
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:335-350 method propose_extension <-> ./crates/ccswarm/src/extension_stub/mod.rs:376-392 method approve_proposal
  Similarity: 73.69%
  Classes: ExtensionManager <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:propose_extension (lines 335-350) ---[0m
    pub async fn propose_extension(&mut self, mut proposal: ExtensionProposal) -> Result<String> {
        // Validate proposal
        self.validate_proposal(&proposal)?;

        // Auto-approve if enabled
        if self.auto_approve {
            proposal.status = ExtensionStatus::Approved;
        }

        let id = proposal.id.clone();
        self.proposals.insert(id.clone(), proposal);
        self.update_stats();

        tracing::info!("Extension proposal submitted: {}", id);
        Ok(id)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:approve_proposal (lines 376-392) ---[0m
    pub async fn approve_proposal(&mut self, proposal_id: &str) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        if proposal.status != ExtensionStatus::UnderReview {
            return Err(CCSwarmError::extension(
                proposal_id,
                format!("Cannot approve proposal in status: {:?}", proposal.status)
            ));
        }

        proposal.status = ExtensionStatus::Approved;
        self.update_stats();

        tracing::info!("Extension proposal approved: {}", proposal_id);
        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:335-350 method propose_extension <-> ./crates/ccswarm/src/extension_stub/mod.rs:407-423 method mark_implemented
  Similarity: 73.69%
  Classes: ExtensionManager <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:propose_extension (lines 335-350) ---[0m
    pub async fn propose_extension(&mut self, mut proposal: ExtensionProposal) -> Result<String> {
        // Validate proposal
        self.validate_proposal(&proposal)?;

        // Auto-approve if enabled
        if self.auto_approve {
            proposal.status = ExtensionStatus::Approved;
        }

        let id = proposal.id.clone();
        self.proposals.insert(id.clone(), proposal);
        self.update_stats();

        tracing::info!("Extension proposal submitted: {}", id);
        Ok(id)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:mark_implemented (lines 407-423) ---[0m
    pub async fn mark_implemented(&mut self, proposal_id: &str) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        if proposal.status != ExtensionStatus::Approved {
            return Err(CCSwarmError::extension(
                proposal_id,
                format!("Cannot implement proposal in status: {:?}", proposal.status)
            ));
        }

        proposal.status = ExtensionStatus::Implemented;
        self.update_stats();

        tracing::info!("Extension proposal implemented: {}", proposal_id);
        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:202-219 method get_stats <-> ./crates/ccswarm/src/extension_stub/mod.rs:395-404 method reject_proposal
  Similarity: 84.37%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:get_stats (lines 202-219) ---[0m
    pub fn get_stats(&self) -> ExtensionStats {
        let total = self.extensions.len();
        let active = self.list_by_state(&ExtensionState::Active).len();
        let loaded = self.list_by_state(&ExtensionState::Loaded).len();
        let error_count = self.extensions.values()
            .filter(|ext| matches!(ext.state(), ExtensionState::Error(_)))
            .count();

        ExtensionStats {
            total_extensions: total,
            active_extensions: active,
            loaded_extensions: loaded,
            error_extensions: error_count,
            pending_proposals: 0, // Managed by ExtensionManager
            successful_extensions: active + loaded,
            failed_extensions: error_count,
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:reject_proposal (lines 395-404) ---[0m
    pub async fn reject_proposal(&mut self, proposal_id: &str, reason: String) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        proposal.status = ExtensionStatus::Rejected;
        self.update_stats();

        tracing::info!("Extension proposal rejected: {} - {}", proposal_id, reason);
        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:376-392 method approve_proposal <-> ./crates/ccswarm/src/extension_stub/mod.rs:395-404 method reject_proposal
  Similarity: 86.17%
  Classes: ExtensionManager <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:approve_proposal (lines 376-392) ---[0m
    pub async fn approve_proposal(&mut self, proposal_id: &str) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        if proposal.status != ExtensionStatus::UnderReview {
            return Err(CCSwarmError::extension(
                proposal_id,
                format!("Cannot approve proposal in status: {:?}", proposal.status)
            ));
        }

        proposal.status = ExtensionStatus::Approved;
        self.update_stats();

        tracing::info!("Extension proposal approved: {}", proposal_id);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:reject_proposal (lines 395-404) ---[0m
    pub async fn reject_proposal(&mut self, proposal_id: &str, reason: String) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        proposal.status = ExtensionStatus::Rejected;
        self.update_stats();

        tracing::info!("Extension proposal rejected: {} - {}", proposal_id, reason);
        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:395-404 method reject_proposal <-> ./crates/ccswarm/src/extension_stub/mod.rs:407-423 method mark_implemented
  Similarity: 86.17%
  Classes: ExtensionManager <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:reject_proposal (lines 395-404) ---[0m
    pub async fn reject_proposal(&mut self, proposal_id: &str, reason: String) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        proposal.status = ExtensionStatus::Rejected;
        self.update_stats();

        tracing::info!("Extension proposal rejected: {} - {}", proposal_id, reason);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:mark_implemented (lines 407-423) ---[0m
    pub async fn mark_implemented(&mut self, proposal_id: &str) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        if proposal.status != ExtensionStatus::Approved {
            return Err(CCSwarmError::extension(
                proposal_id,
                format!("Cannot implement proposal in status: {:?}", proposal.status)
            ));
        }

        proposal.status = ExtensionStatus::Implemented;
        self.update_stats();

        tracing::info!("Extension proposal implemented: {}", proposal_id);
        Ok(())
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:335-350 method propose_extension <-> ./crates/ccswarm/src/extension_stub/mod.rs:431-442 method update_stats
  Similarity: 78.92%
  Classes: ExtensionManager <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:propose_extension (lines 335-350) ---[0m
    pub async fn propose_extension(&mut self, mut proposal: ExtensionProposal) -> Result<String> {
        // Validate proposal
        self.validate_proposal(&proposal)?;

        // Auto-approve if enabled
        if self.auto_approve {
            proposal.status = ExtensionStatus::Approved;
        }

        let id = proposal.id.clone();
        self.proposals.insert(id.clone(), proposal);
        self.update_stats();

        tracing::info!("Extension proposal submitted: {}", id);
        Ok(id)
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:update_stats (lines 431-442) ---[0m
    fn update_stats(&mut self) {
        let total = self.proposals.len();
        let pending = self.list_proposals_by_status(&ExtensionStatus::Proposed).len() +
                     self.list_proposals_by_status(&ExtensionStatus::UnderReview).len();
        let successful = self.list_proposals_by_status(&ExtensionStatus::Implemented).len();
        let failed = self.list_proposals_by_status(&ExtensionStatus::Rejected).len();

        self.stats.total_extensions = total;
        self.stats.pending_proposals = pending;
        self.stats.successful_extensions = successful;
        self.stats.failed_extensions = failed;
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:202-219 method get_stats <-> ./crates/ccswarm/src/extension_stub/mod.rs:431-442 method update_stats
  Similarity: 71.02%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:get_stats (lines 202-219) ---[0m
    pub fn get_stats(&self) -> ExtensionStats {
        let total = self.extensions.len();
        let active = self.list_by_state(&ExtensionState::Active).len();
        let loaded = self.list_by_state(&ExtensionState::Loaded).len();
        let error_count = self.extensions.values()
            .filter(|ext| matches!(ext.state(), ExtensionState::Error(_)))
            .count();

        ExtensionStats {
            total_extensions: total,
            active_extensions: active,
            loaded_extensions: loaded,
            error_extensions: error_count,
            pending_proposals: 0, // Managed by ExtensionManager
            successful_extensions: active + loaded,
            failed_extensions: error_count,
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:update_stats (lines 431-442) ---[0m
    fn update_stats(&mut self) {
        let total = self.proposals.len();
        let pending = self.list_proposals_by_status(&ExtensionStatus::Proposed).len() +
                     self.list_proposals_by_status(&ExtensionStatus::UnderReview).len();
        let successful = self.list_proposals_by_status(&ExtensionStatus::Implemented).len();
        let failed = self.list_proposals_by_status(&ExtensionStatus::Rejected).len();

        self.stats.total_extensions = total;
        self.stats.pending_proposals = pending;
        self.stats.successful_extensions = successful;
        self.stats.failed_extensions = failed;
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:431-442 method update_stats <-> ./crates/ccswarm/src/extension_stub/mod.rs:646-662 function test_extension_stub_duplicate_registration
  Similarity: 72.17%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:update_stats (lines 431-442) ---[0m
    fn update_stats(&mut self) {
        let total = self.proposals.len();
        let pending = self.list_proposals_by_status(&ExtensionStatus::Proposed).len() +
                     self.list_proposals_by_status(&ExtensionStatus::UnderReview).len();
        let successful = self.list_proposals_by_status(&ExtensionStatus::Implemented).len();
        let failed = self.list_proposals_by_status(&ExtensionStatus::Rejected).len();

        self.stats.total_extensions = total;
        self.stats.pending_proposals = pending;
        self.stats.successful_extensions = successful;
        self.stats.failed_extensions = failed;
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_duplicate_registration (lines 646-662) ---[0m
    fn test_extension_stub_duplicate_registration() {
        let mut stub = ExtensionStub::new();

        let extension1 = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let extension2 = extension1.clone();

        stub.register(extension1).unwrap();
        let result = stub.register(extension2);
        assert!(result.is_err());
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:395-404 method reject_proposal <-> ./crates/ccswarm/src/extension_stub/mod.rs:467-482 method cleanup_old_proposals
  Similarity: 79.29%
  Classes: ExtensionManager <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:reject_proposal (lines 395-404) ---[0m
    pub async fn reject_proposal(&mut self, proposal_id: &str, reason: String) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        proposal.status = ExtensionStatus::Rejected;
        self.update_stats();

        tracing::info!("Extension proposal rejected: {} - {}", proposal_id, reason);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:cleanup_old_proposals (lines 467-482) ---[0m
    pub async fn cleanup_old_proposals(&mut self, max_age: chrono::Duration) -> Result<usize> {
        let cutoff = chrono::Utc::now() - max_age;
        let initial_count = self.proposals.len();

        self.proposals.retain(|_, proposal| {
            proposal.status != ExtensionStatus::Rejected || proposal.created_at > cutoff
        });

        let removed = initial_count - self.proposals.len();
        if removed > 0 {
            self.update_stats();
            tracing::info!("Cleaned up {} old proposals", removed);
        }

        Ok(removed)
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:310-324 method new <-> ./crates/ccswarm/src/extension_stub/mod.rs:525-532 method new
  Similarity: 88.82%
  Classes: ExtensionManager <-> ProposedState

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:new (lines 310-324) ---[0m
    pub fn new() -> Self {
        Self {
            proposals: HashMap::new(),
            stats: ExtensionStats {
                total_extensions: 0,
                active_extensions: 0,
                loaded_extensions: 0,
                error_extensions: 0,
                pending_proposals: 0,
                successful_extensions: 0,
                failed_extensions: 0,
            },
            auto_approve: false,
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:new (lines 525-532) ---[0m
    pub fn new() -> Self {
        Self {
            new_capabilities: Vec::new(),
            removed_limitations: Vec::new(),
            expected_improvements: HashMap::new(),
            performance_targets: HashMap::new(),
        }
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:395-404 method reject_proposal <-> ./crates/ccswarm/src/extension_stub/mod.rs:646-662 function test_extension_stub_duplicate_registration
  Similarity: 75.56%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:reject_proposal (lines 395-404) ---[0m
    pub async fn reject_proposal(&mut self, proposal_id: &str, reason: String) -> Result<()> {
        let proposal = self.proposals.get_mut(proposal_id)
            .ok_or_else(|| CCSwarmError::extension(proposal_id, "Proposal not found"))?;

        proposal.status = ExtensionStatus::Rejected;
        self.update_stats();

        tracing::info!("Extension proposal rejected: {} - {}", proposal_id, reason);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_extension_stub_duplicate_registration (lines 646-662) ---[0m
    fn test_extension_stub_duplicate_registration() {
        let mut stub = ExtensionStub::new();

        let extension1 = Extension::new(
            "Test Extension",
            Version::new(1, 0, 0),
            "A test extension",
            "Test Author",
            vec![ExtensionCapability::Command],
        );

        let extension2 = extension1.clone();

        stub.register(extension1).unwrap();
        let result = stub.register(extension2);
        assert!(result.is_err());
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:310-324 method new <-> ./crates/ccswarm/src/extension_stub/mod.rs:500-506 method new
  Similarity: 90.29%
  Classes: ExtensionManager <-> CurrentState

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:new (lines 310-324) ---[0m
    pub fn new() -> Self {
        Self {
            proposals: HashMap::new(),
            stats: ExtensionStats {
                total_extensions: 0,
                active_extensions: 0,
                loaded_extensions: 0,
                error_extensions: 0,
                pending_proposals: 0,
                successful_extensions: 0,
                failed_extensions: 0,
            },
            auto_approve: false,
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:new (lines 500-506) ---[0m
    pub fn new() -> Self {
        Self {
            capabilities: Vec::new(),
            limitations: Vec::new(),
            performance_metrics: HashMap::new(),
        }
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:431-442 method update_stats <-> ./crates/ccswarm/src/extension_stub/mod.rs:467-482 method cleanup_old_proposals
  Similarity: 70.60%
  Classes: ExtensionManager <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:update_stats (lines 431-442) ---[0m
    fn update_stats(&mut self) {
        let total = self.proposals.len();
        let pending = self.list_proposals_by_status(&ExtensionStatus::Proposed).len() +
                     self.list_proposals_by_status(&ExtensionStatus::UnderReview).len();
        let successful = self.list_proposals_by_status(&ExtensionStatus::Implemented).len();
        let failed = self.list_proposals_by_status(&ExtensionStatus::Rejected).len();

        self.stats.total_extensions = total;
        self.stats.pending_proposals = pending;
        self.stats.successful_extensions = successful;
        self.stats.failed_extensions = failed;
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:cleanup_old_proposals (lines 467-482) ---[0m
    pub async fn cleanup_old_proposals(&mut self, max_age: chrono::Duration) -> Result<usize> {
        let cutoff = chrono::Utc::now() - max_age;
        let initial_count = self.proposals.len();

        self.proposals.retain(|_, proposal| {
            proposal.status != ExtensionStatus::Rejected || proposal.created_at > cutoff
        });

        let removed = initial_count - self.proposals.len();
        if removed > 0 {
            self.update_stats();
            tracing::info!("Cleaned up {} old proposals", removed);
        }

        Ok(removed)
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:122-127 method list_by_state <-> ./crates/ccswarm/src/extension_stub/mod.rs:310-324 method new
  Similarity: 93.68%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:list_by_state (lines 122-127) ---[0m
    pub fn list_by_state(&self, state: &ExtensionState) -> Vec<&Extension> {
        self.extensions
            .values()
            .filter(|ext| ext.state() == state)
            .collect()
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:new (lines 310-324) ---[0m
    pub fn new() -> Self {
        Self {
            proposals: HashMap::new(),
            stats: ExtensionStats {
                total_extensions: 0,
                active_extensions: 0,
                loaded_extensions: 0,
                error_extensions: 0,
                pending_proposals: 0,
                successful_extensions: 0,
                failed_extensions: 0,
            },
            auto_approve: false,
        }
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:222-227 method find_by_capability <-> ./crates/ccswarm/src/extension_stub/mod.rs:310-324 method new
  Similarity: 93.68%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:find_by_capability (lines 222-227) ---[0m
    pub fn find_by_capability(&self, capability: &crate::extension::ExtensionCapability) -> Vec<&Extension> {
        self.extensions
            .values()
            .filter(|ext| ext.has_capability(capability))
            .collect()
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:new (lines 310-324) ---[0m
    pub fn new() -> Self {
        Self {
            proposals: HashMap::new(),
            stats: ExtensionStats {
                total_extensions: 0,
                active_extensions: 0,
                loaded_extensions: 0,
                error_extensions: 0,
                pending_proposals: 0,
                successful_extensions: 0,
                failed_extensions: 0,
            },
            auto_approve: false,
        }
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:310-324 method new <-> ./crates/ccswarm/src/extension_stub/mod.rs:368-373 method list_proposals_by_status
  Similarity: 93.68%
  Classes: ExtensionManager <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:new (lines 310-324) ---[0m
    pub fn new() -> Self {
        Self {
            proposals: HashMap::new(),
            stats: ExtensionStats {
                total_extensions: 0,
                active_extensions: 0,
                loaded_extensions: 0,
                error_extensions: 0,
                pending_proposals: 0,
                successful_extensions: 0,
                failed_extensions: 0,
            },
            auto_approve: false,
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:list_proposals_by_status (lines 368-373) ---[0m
    pub fn list_proposals_by_status(&self, status: &ExtensionStatus) -> Vec<&ExtensionProposal> {
        self.proposals
            .values()
            .filter(|p| &p.status == status)
            .collect()
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:310-324 method new <-> ./crates/ccswarm/src/extension_stub/mod.rs:567-574 method fmt
  Similarity: 84.55%
  Classes: ExtensionManager <-> ComplexityLevel

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:new (lines 310-324) ---[0m
    pub fn new() -> Self {
        Self {
            proposals: HashMap::new(),
            stats: ExtensionStats {
                total_extensions: 0,
                active_extensions: 0,
                loaded_extensions: 0,
                error_extensions: 0,
                pending_proposals: 0,
                successful_extensions: 0,
                failed_extensions: 0,
            },
            auto_approve: false,
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:fmt (lines 567-574) ---[0m
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Low => write!(f, "Low"),
            Self::Medium => write!(f, "Medium"),
            Self::High => write!(f, "High"),
            Self::Critical => write!(f, "Critical"),
        }
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:32-37 method with_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:310-324 method new
  Similarity: 92.21%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:with_registry (lines 32-37) ---[0m
    pub fn with_registry<P: Into<std::path::PathBuf>>(path: P) -> Self {
        Self {
            extensions: HashMap::new(),
            registry_path: Some(path.into()),
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:new (lines 310-324) ---[0m
    pub fn new() -> Self {
        Self {
            proposals: HashMap::new(),
            stats: ExtensionStats {
                total_extensions: 0,
                active_extensions: 0,
                loaded_extensions: 0,
                error_extensions: 0,
                pending_proposals: 0,
                successful_extensions: 0,
                failed_extensions: 0,
            },
            auto_approve: false,
        }
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:202-219 method get_stats <-> ./crates/ccswarm/src/extension_stub/mod.rs:772-777 function test_complexity_level_display
  Similarity: 79.52%

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:get_stats (lines 202-219) ---[0m
    pub fn get_stats(&self) -> ExtensionStats {
        let total = self.extensions.len();
        let active = self.list_by_state(&ExtensionState::Active).len();
        let loaded = self.list_by_state(&ExtensionState::Loaded).len();
        let error_count = self.extensions.values()
            .filter(|ext| matches!(ext.state(), ExtensionState::Error(_)))
            .count();

        ExtensionStats {
            total_extensions: total,
            active_extensions: active,
            loaded_extensions: loaded,
            error_extensions: error_count,
            pending_proposals: 0, // Managed by ExtensionManager
            successful_extensions: active + loaded,
            failed_extensions: error_count,
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:test_complexity_level_display (lines 772-777) ---[0m
    fn test_complexity_level_display() {
        assert_eq!(ComplexityLevel::Low.to_string(), "Low");
        assert_eq!(ComplexityLevel::Medium.to_string(), "Medium");
        assert_eq!(ComplexityLevel::High.to_string(), "High");
        assert_eq!(ComplexityLevel::Critical.to_string(), "Critical");
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:500-506 method new <-> ./crates/ccswarm/src/extension_stub/mod.rs:525-532 method new
  Similarity: 92.78%
  Classes: CurrentState <-> ProposedState

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:new (lines 500-506) ---[0m
    pub fn new() -> Self {
        Self {
            capabilities: Vec::new(),
            limitations: Vec::new(),
            performance_metrics: HashMap::new(),
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:new (lines 525-532) ---[0m
    pub fn new() -> Self {
        Self {
            new_capabilities: Vec::new(),
            removed_limitations: Vec::new(),
            expected_improvements: HashMap::new(),
            performance_targets: HashMap::new(),
        }
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:122-127 method list_by_state <-> ./crates/ccswarm/src/extension_stub/mod.rs:525-532 method new
  Similarity: 92.04%
  Classes: ExtensionStub <-> ProposedState

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:list_by_state (lines 122-127) ---[0m
    pub fn list_by_state(&self, state: &ExtensionState) -> Vec<&Extension> {
        self.extensions
            .values()
            .filter(|ext| ext.state() == state)
            .collect()
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:new (lines 525-532) ---[0m
    pub fn new() -> Self {
        Self {
            new_capabilities: Vec::new(),
            removed_limitations: Vec::new(),
            expected_improvements: HashMap::new(),
            performance_targets: HashMap::new(),
        }
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:222-227 method find_by_capability <-> ./crates/ccswarm/src/extension_stub/mod.rs:525-532 method new
  Similarity: 92.04%
  Classes: ExtensionStub <-> ProposedState

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:find_by_capability (lines 222-227) ---[0m
    pub fn find_by_capability(&self, capability: &crate::extension::ExtensionCapability) -> Vec<&Extension> {
        self.extensions
            .values()
            .filter(|ext| ext.has_capability(capability))
            .collect()
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:new (lines 525-532) ---[0m
    pub fn new() -> Self {
        Self {
            new_capabilities: Vec::new(),
            removed_limitations: Vec::new(),
            expected_improvements: HashMap::new(),
            performance_targets: HashMap::new(),
        }
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:368-373 method list_proposals_by_status <-> ./crates/ccswarm/src/extension_stub/mod.rs:525-532 method new
  Similarity: 92.04%
  Classes: ExtensionManager <-> ProposedState

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:list_proposals_by_status (lines 368-373) ---[0m
    pub fn list_proposals_by_status(&self, status: &ExtensionStatus) -> Vec<&ExtensionProposal> {
        self.proposals
            .values()
            .filter(|p| &p.status == status)
            .collect()
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:new (lines 525-532) ---[0m
    pub fn new() -> Self {
        Self {
            new_capabilities: Vec::new(),
            removed_limitations: Vec::new(),
            expected_improvements: HashMap::new(),
            performance_targets: HashMap::new(),
        }
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:32-37 method with_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:525-532 method new
  Similarity: 84.07%
  Classes: ExtensionStub <-> ProposedState

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:with_registry (lines 32-37) ---[0m
    pub fn with_registry<P: Into<std::path::PathBuf>>(path: P) -> Self {
        Self {
            extensions: HashMap::new(),
            registry_path: Some(path.into()),
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:new (lines 525-532) ---[0m
    pub fn new() -> Self {
        Self {
            new_capabilities: Vec::new(),
            removed_limitations: Vec::new(),
            expected_improvements: HashMap::new(),
            performance_targets: HashMap::new(),
        }
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:122-127 method list_by_state <-> ./crates/ccswarm/src/extension_stub/mod.rs:500-506 method new
  Similarity: 89.76%
  Classes: ExtensionStub <-> CurrentState

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:list_by_state (lines 122-127) ---[0m
    pub fn list_by_state(&self, state: &ExtensionState) -> Vec<&Extension> {
        self.extensions
            .values()
            .filter(|ext| ext.state() == state)
            .collect()
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:new (lines 500-506) ---[0m
    pub fn new() -> Self {
        Self {
            capabilities: Vec::new(),
            limitations: Vec::new(),
            performance_metrics: HashMap::new(),
        }
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:222-227 method find_by_capability <-> ./crates/ccswarm/src/extension_stub/mod.rs:500-506 method new
  Similarity: 89.76%
  Classes: ExtensionStub <-> CurrentState

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:find_by_capability (lines 222-227) ---[0m
    pub fn find_by_capability(&self, capability: &crate::extension::ExtensionCapability) -> Vec<&Extension> {
        self.extensions
            .values()
            .filter(|ext| ext.has_capability(capability))
            .collect()
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:new (lines 500-506) ---[0m
    pub fn new() -> Self {
        Self {
            capabilities: Vec::new(),
            limitations: Vec::new(),
            performance_metrics: HashMap::new(),
        }
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:368-373 method list_proposals_by_status <-> ./crates/ccswarm/src/extension_stub/mod.rs:500-506 method new
  Similarity: 89.76%
  Classes: ExtensionManager <-> CurrentState

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:list_proposals_by_status (lines 368-373) ---[0m
    pub fn list_proposals_by_status(&self, status: &ExtensionStatus) -> Vec<&ExtensionProposal> {
        self.proposals
            .values()
            .filter(|p| &p.status == status)
            .collect()
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:new (lines 500-506) ---[0m
    pub fn new() -> Self {
        Self {
            capabilities: Vec::new(),
            limitations: Vec::new(),
            performance_metrics: HashMap::new(),
        }
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:32-37 method with_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:500-506 method new
  Similarity: 86.67%
  Classes: ExtensionStub <-> CurrentState

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:with_registry (lines 32-37) ---[0m
    pub fn with_registry<P: Into<std::path::PathBuf>>(path: P) -> Self {
        Self {
            extensions: HashMap::new(),
            registry_path: Some(path.into()),
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:new (lines 500-506) ---[0m
    pub fn new() -> Self {
        Self {
            capabilities: Vec::new(),
            limitations: Vec::new(),
            performance_metrics: HashMap::new(),
        }
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:122-127 method list_by_state <-> ./crates/ccswarm/src/extension_stub/mod.rs:222-227 method find_by_capability
  Similarity: 90.48%
  Classes: ExtensionStub <-> ExtensionStub

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:list_by_state (lines 122-127) ---[0m
    pub fn list_by_state(&self, state: &ExtensionState) -> Vec<&Extension> {
        self.extensions
            .values()
            .filter(|ext| ext.state() == state)
            .collect()
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:find_by_capability (lines 222-227) ---[0m
    pub fn find_by_capability(&self, capability: &crate::extension::ExtensionCapability) -> Vec<&Extension> {
        self.extensions
            .values()
            .filter(|ext| ext.has_capability(capability))
            .collect()
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:122-127 method list_by_state <-> ./crates/ccswarm/src/extension_stub/mod.rs:368-373 method list_proposals_by_status
  Similarity: 90.24%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:list_by_state (lines 122-127) ---[0m
    pub fn list_by_state(&self, state: &ExtensionState) -> Vec<&Extension> {
        self.extensions
            .values()
            .filter(|ext| ext.state() == state)
            .collect()
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:list_proposals_by_status (lines 368-373) ---[0m
    pub fn list_proposals_by_status(&self, status: &ExtensionStatus) -> Vec<&ExtensionProposal> {
        self.proposals
            .values()
            .filter(|p| &p.status == status)
            .collect()
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:32-37 method with_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:122-127 method list_by_state
  Similarity: 89.76%
  Classes: ExtensionStub <-> ExtensionStub

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:with_registry (lines 32-37) ---[0m
    pub fn with_registry<P: Into<std::path::PathBuf>>(path: P) -> Self {
        Self {
            extensions: HashMap::new(),
            registry_path: Some(path.into()),
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:list_by_state (lines 122-127) ---[0m
    pub fn list_by_state(&self, state: &ExtensionState) -> Vec<&Extension> {
        self.extensions
            .values()
            .filter(|ext| ext.state() == state)
            .collect()
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:32-37 method with_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:222-227 method find_by_capability
  Similarity: 89.25%
  Classes: ExtensionStub <-> ExtensionStub

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:with_registry (lines 32-37) ---[0m
    pub fn with_registry<P: Into<std::path::PathBuf>>(path: P) -> Self {
        Self {
            extensions: HashMap::new(),
            registry_path: Some(path.into()),
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:find_by_capability (lines 222-227) ---[0m
    pub fn find_by_capability(&self, capability: &crate::extension::ExtensionCapability) -> Vec<&Extension> {
        self.extensions
            .values()
            .filter(|ext| ext.has_capability(capability))
            .collect()
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:32-37 method with_registry <-> ./crates/ccswarm/src/extension_stub/mod.rs:368-373 method list_proposals_by_status
  Similarity: 89.25%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:with_registry (lines 32-37) ---[0m
    pub fn with_registry<P: Into<std::path::PathBuf>>(path: P) -> Self {
        Self {
            extensions: HashMap::new(),
            registry_path: Some(path.into()),
        }
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:list_proposals_by_status (lines 368-373) ---[0m
    pub fn list_proposals_by_status(&self, status: &ExtensionStatus) -> Vec<&ExtensionProposal> {
        self.proposals
            .values()
            .filter(|p| &p.status == status)
            .collect()
    }

  ./crates/ccswarm/src/extension_stub/mod.rs:222-227 method find_by_capability <-> ./crates/ccswarm/src/extension_stub/mod.rs:368-373 method list_proposals_by_status
  Similarity: 89.25%
  Classes: ExtensionStub <-> ExtensionManager

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:find_by_capability (lines 222-227) ---[0m
    pub fn find_by_capability(&self, capability: &crate::extension::ExtensionCapability) -> Vec<&Extension> {
        self.extensions
            .values()
            .filter(|ext| ext.has_capability(capability))
            .collect()
    }

[36m--- ./crates/ccswarm/src/extension_stub/mod.rs:list_proposals_by_status (lines 368-373) ---[0m
    pub fn list_proposals_by_status(&self, status: &ExtensionStatus) -> Vec<&ExtensionProposal> {
        self.proposals
            .values()
            .filter(|p| &p.status == status)
            .collect()
    }


Duplicates in ./crates/ccswarm/src/session/worktree_session.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/session/worktree_session.rs:290-351 method setup_worktree_environment <-> ./crates/ccswarm/src/session/worktree_session.rs:395-442 method execute_task_batch
  Similarity: 80.46%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:setup_worktree_environment (lines 290-351) ---[0m
    async fn setup_worktree_environment(
        &self,
        session_info: &WorktreeSessionInfo,
        agent: &Arc<Mutex<PersistentClaudeAgent>>,
    ) -> Result<()> {
        // Create minimal CLAUDE.md in worktree
        let agent_guard = agent.lock().await;
        let compact_prompt = format!(
            r#"# CLAUDE.md - {} Agent Workspace

🤖 **AGENT**: {}
📁 **WORKSPACE**: {}
🎯 **SPECIALIZATION**: {}

## Quick Identity
You are a specialized {} agent working in an isolated git worktree.
Maintain strict role boundaries and provide focused responses.

## Response Format
Always include:
🤖 AGENT: {}
📁 WORKSPACE: {}
🎯 SCOPE: [Task assessment]
"#,
            agent_guard.identity.specialization.name(),
            agent_guard.identity.agent_id,
            session_info
                .worktree_path
                .file_name()
                .unwrap_or_default()
                .to_string_lossy(),
            agent_guard.identity.specialization.name(),
            agent_guard.identity.specialization.name(),
            agent_guard.identity.specialization.name(),
            session_info
                .worktree_path
                .file_name()
                .unwrap_or_default()
                .to_string_lossy(),
        );

        let claude_md_path = session_info.worktree_path.join("CLAUDE.md");
        tokio::fs::write(&claude_md_path, compact_prompt)
            .await
            .context("Failed to write CLAUDE.md")?;

        // Create .claude.json configuration
        let claude_config_path = session_info.worktree_path.join(".claude.json");
        let config_json = serde_json::json!({
            "dangerous_skip": true,
            "json_output": false,
            "think_mode": null
        });
        tokio::fs::write(
            &claude_config_path,
            serde_json::to_string_pretty(&config_json)?,
        )
        .await
        .context("Failed to write .claude.json")?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:execute_task_batch (lines 395-442) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks in worktree for role: {}",
            tasks.len(),
            role.name()
        );

        let session = self
            .get_or_create_worktree_session(role, claude_config)
            .await?;

        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, WorktreeSessionStatus::Active)
            .await;

        // Execute batch
        let results = {
            let mut agent = session.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Auto-commit batch results if enabled
        if self.config.auto_commit {
            if let Err(e) = self.auto_commit_changes(&agent_id).await {
                tracing::warn!("Auto-commit failed for agent {}: {}", agent_id, e);
            }
        }

        self.update_session_activity(&agent_id).await;
        self.update_session_status(&agent_id, WorktreeSessionStatus::Idle)
            .await;

        tracing::info!("Batch execution completed in worktree session");
        Ok(results)
    }

  ./crates/ccswarm/src/session/worktree_session.rs:290-351 method setup_worktree_environment <-> ./crates/ccswarm/src/session/worktree_session.rs:354-392 method execute_task
  Similarity: 86.74%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:setup_worktree_environment (lines 290-351) ---[0m
    async fn setup_worktree_environment(
        &self,
        session_info: &WorktreeSessionInfo,
        agent: &Arc<Mutex<PersistentClaudeAgent>>,
    ) -> Result<()> {
        // Create minimal CLAUDE.md in worktree
        let agent_guard = agent.lock().await;
        let compact_prompt = format!(
            r#"# CLAUDE.md - {} Agent Workspace

🤖 **AGENT**: {}
📁 **WORKSPACE**: {}
🎯 **SPECIALIZATION**: {}

## Quick Identity
You are a specialized {} agent working in an isolated git worktree.
Maintain strict role boundaries and provide focused responses.

## Response Format
Always include:
🤖 AGENT: {}
📁 WORKSPACE: {}
🎯 SCOPE: [Task assessment]
"#,
            agent_guard.identity.specialization.name(),
            agent_guard.identity.agent_id,
            session_info
                .worktree_path
                .file_name()
                .unwrap_or_default()
                .to_string_lossy(),
            agent_guard.identity.specialization.name(),
            agent_guard.identity.specialization.name(),
            agent_guard.identity.specialization.name(),
            session_info
                .worktree_path
                .file_name()
                .unwrap_or_default()
                .to_string_lossy(),
        );

        let claude_md_path = session_info.worktree_path.join("CLAUDE.md");
        tokio::fs::write(&claude_md_path, compact_prompt)
            .await
            .context("Failed to write CLAUDE.md")?;

        // Create .claude.json configuration
        let claude_config_path = session_info.worktree_path.join(".claude.json");
        let config_json = serde_json::json!({
            "dangerous_skip": true,
            "json_output": false,
            "think_mode": null
        });
        tokio::fs::write(
            &claude_config_path,
            serde_json::to_string_pretty(&config_json)?,
        )
        .await
        .context("Failed to write .claude.json")?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:execute_task (lines 354-392) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let session = self
            .get_or_create_worktree_session(role, claude_config)
            .await?;

        // Update session status
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, WorktreeSessionStatus::Active)
            .await;

        // Execute task
        let result = {
            let mut agent = session.lock().await;
            agent.execute_task(task).await?
        };

        // Auto-commit if enabled
        if self.config.auto_commit && result.success {
            if let Err(e) = self.auto_commit_changes(&agent_id).await {
                tracing::warn!("Auto-commit failed for agent {}: {}", agent_id, e);
            }
        }

        // Update session info
        self.update_session_activity(&agent_id).await;
        self.update_session_status(&agent_id, WorktreeSessionStatus::Idle)
            .await;

        Ok(result)
    }

  ./crates/ccswarm/src/session/worktree_session.rs:354-392 method execute_task <-> ./crates/ccswarm/src/session/worktree_session.rs:395-442 method execute_task_batch
  Similarity: 86.52%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:execute_task (lines 354-392) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let session = self
            .get_or_create_worktree_session(role, claude_config)
            .await?;

        // Update session status
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, WorktreeSessionStatus::Active)
            .await;

        // Execute task
        let result = {
            let mut agent = session.lock().await;
            agent.execute_task(task).await?
        };

        // Auto-commit if enabled
        if self.config.auto_commit && result.success {
            if let Err(e) = self.auto_commit_changes(&agent_id).await {
                tracing::warn!("Auto-commit failed for agent {}: {}", agent_id, e);
            }
        }

        // Update session info
        self.update_session_activity(&agent_id).await;
        self.update_session_status(&agent_id, WorktreeSessionStatus::Idle)
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:execute_task_batch (lines 395-442) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks in worktree for role: {}",
            tasks.len(),
            role.name()
        );

        let session = self
            .get_or_create_worktree_session(role, claude_config)
            .await?;

        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, WorktreeSessionStatus::Active)
            .await;

        // Execute batch
        let results = {
            let mut agent = session.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Auto-commit batch results if enabled
        if self.config.auto_commit {
            if let Err(e) = self.auto_commit_changes(&agent_id).await {
                tracing::warn!("Auto-commit failed for agent {}: {}", agent_id, e);
            }
        }

        self.update_session_activity(&agent_id).await;
        self.update_session_status(&agent_id, WorktreeSessionStatus::Idle)
            .await;

        tracing::info!("Batch execution completed in worktree session");
        Ok(results)
    }

  ./crates/ccswarm/src/session/worktree_session.rs:290-351 method setup_worktree_environment <-> ./crates/ccswarm/src/session/worktree_session.rs:565-586 method shutdown
  Similarity: 89.42%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:setup_worktree_environment (lines 290-351) ---[0m
    async fn setup_worktree_environment(
        &self,
        session_info: &WorktreeSessionInfo,
        agent: &Arc<Mutex<PersistentClaudeAgent>>,
    ) -> Result<()> {
        // Create minimal CLAUDE.md in worktree
        let agent_guard = agent.lock().await;
        let compact_prompt = format!(
            r#"# CLAUDE.md - {} Agent Workspace

🤖 **AGENT**: {}
📁 **WORKSPACE**: {}
🎯 **SPECIALIZATION**: {}

## Quick Identity
You are a specialized {} agent working in an isolated git worktree.
Maintain strict role boundaries and provide focused responses.

## Response Format
Always include:
🤖 AGENT: {}
📁 WORKSPACE: {}
🎯 SCOPE: [Task assessment]
"#,
            agent_guard.identity.specialization.name(),
            agent_guard.identity.agent_id,
            session_info
                .worktree_path
                .file_name()
                .unwrap_or_default()
                .to_string_lossy(),
            agent_guard.identity.specialization.name(),
            agent_guard.identity.specialization.name(),
            agent_guard.identity.specialization.name(),
            session_info
                .worktree_path
                .file_name()
                .unwrap_or_default()
                .to_string_lossy(),
        );

        let claude_md_path = session_info.worktree_path.join("CLAUDE.md");
        tokio::fs::write(&claude_md_path, compact_prompt)
            .await
            .context("Failed to write CLAUDE.md")?;

        // Create .claude.json configuration
        let claude_config_path = session_info.worktree_path.join(".claude.json");
        let config_json = serde_json::json!({
            "dangerous_skip": true,
            "json_output": false,
            "think_mode": null
        });
        tokio::fs::write(
            &claude_config_path,
            serde_json::to_string_pretty(&config_json)?,
        )
        .await
        .context("Failed to write .claude.json")?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:shutdown (lines 565-586) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down worktree session manager");

        // Get all session IDs
        let session_ids: Vec<String> = {
            let sessions = self.worktree_sessions.read().await;
            sessions.keys().cloned().collect()
        };

        // Cleanup all sessions
        for agent_id in session_ids {
            if let Err(e) = self.cleanup_worktree_session(&agent_id).await {
                tracing::error!("Failed to cleanup session {}: {}", agent_id, e);
            }
        }

        // Shutdown persistent manager
        self.persistent_manager.shutdown().await?;

        tracing::info!("Worktree session manager shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/worktree_session.rs:144-170 method get_or_create_worktree_session <-> ./crates/ccswarm/src/session/worktree_session.rs:290-351 method setup_worktree_environment
  Similarity: 79.52%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_or_create_worktree_session (lines 144-170) ---[0m
    pub async fn get_or_create_worktree_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        // Check if we can reuse an existing worktree session
        if let Some(existing) = self.find_reusable_worktree_session(&role).await {
            tracing::info!(
                "Reusing existing worktree session for role: {}",
                role.name()
            );
            return Ok(existing);
        }

        // Check limits
        let current_count = self.count_sessions_by_role(&role).await;
        if current_count >= self.config.max_worktrees_per_role {
            return Err(anyhow::anyhow!(
                "Maximum worktrees per role reached for {}: {}",
                role.name(),
                self.config.max_worktrees_per_role
            ));
        }

        // Create new worktree session
        self.create_new_worktree_session(role, claude_config).await
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:setup_worktree_environment (lines 290-351) ---[0m
    async fn setup_worktree_environment(
        &self,
        session_info: &WorktreeSessionInfo,
        agent: &Arc<Mutex<PersistentClaudeAgent>>,
    ) -> Result<()> {
        // Create minimal CLAUDE.md in worktree
        let agent_guard = agent.lock().await;
        let compact_prompt = format!(
            r#"# CLAUDE.md - {} Agent Workspace

🤖 **AGENT**: {}
📁 **WORKSPACE**: {}
🎯 **SPECIALIZATION**: {}

## Quick Identity
You are a specialized {} agent working in an isolated git worktree.
Maintain strict role boundaries and provide focused responses.

## Response Format
Always include:
🤖 AGENT: {}
📁 WORKSPACE: {}
🎯 SCOPE: [Task assessment]
"#,
            agent_guard.identity.specialization.name(),
            agent_guard.identity.agent_id,
            session_info
                .worktree_path
                .file_name()
                .unwrap_or_default()
                .to_string_lossy(),
            agent_guard.identity.specialization.name(),
            agent_guard.identity.specialization.name(),
            agent_guard.identity.specialization.name(),
            session_info
                .worktree_path
                .file_name()
                .unwrap_or_default()
                .to_string_lossy(),
        );

        let claude_md_path = session_info.worktree_path.join("CLAUDE.md");
        tokio::fs::write(&claude_md_path, compact_prompt)
            .await
            .context("Failed to write CLAUDE.md")?;

        // Create .claude.json configuration
        let claude_config_path = session_info.worktree_path.join(".claude.json");
        let config_json = serde_json::json!({
            "dangerous_skip": true,
            "json_output": false,
            "think_mode": null
        });
        tokio::fs::write(
            &claude_config_path,
            serde_json::to_string_pretty(&config_json)?,
        )
        .await
        .context("Failed to write .claude.json")?;

        Ok(())
    }

  ./crates/ccswarm/src/session/worktree_session.rs:290-351 method setup_worktree_environment <-> ./crates/ccswarm/src/session/worktree_session.rs:514-541 method get_combined_efficiency_stats
  Similarity: 76.94%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:setup_worktree_environment (lines 290-351) ---[0m
    async fn setup_worktree_environment(
        &self,
        session_info: &WorktreeSessionInfo,
        agent: &Arc<Mutex<PersistentClaudeAgent>>,
    ) -> Result<()> {
        // Create minimal CLAUDE.md in worktree
        let agent_guard = agent.lock().await;
        let compact_prompt = format!(
            r#"# CLAUDE.md - {} Agent Workspace

🤖 **AGENT**: {}
📁 **WORKSPACE**: {}
🎯 **SPECIALIZATION**: {}

## Quick Identity
You are a specialized {} agent working in an isolated git worktree.
Maintain strict role boundaries and provide focused responses.

## Response Format
Always include:
🤖 AGENT: {}
📁 WORKSPACE: {}
🎯 SCOPE: [Task assessment]
"#,
            agent_guard.identity.specialization.name(),
            agent_guard.identity.agent_id,
            session_info
                .worktree_path
                .file_name()
                .unwrap_or_default()
                .to_string_lossy(),
            agent_guard.identity.specialization.name(),
            agent_guard.identity.specialization.name(),
            agent_guard.identity.specialization.name(),
            session_info
                .worktree_path
                .file_name()
                .unwrap_or_default()
                .to_string_lossy(),
        );

        let claude_md_path = session_info.worktree_path.join("CLAUDE.md");
        tokio::fs::write(&claude_md_path, compact_prompt)
            .await
            .context("Failed to write CLAUDE.md")?;

        // Create .claude.json configuration
        let claude_config_path = session_info.worktree_path.join(".claude.json");
        let config_json = serde_json::json!({
            "dangerous_skip": true,
            "json_output": false,
            "think_mode": null
        });
        tokio::fs::write(
            &claude_config_path,
            serde_json::to_string_pretty(&config_json)?,
        )
        .await
        .context("Failed to write .claude.json")?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_combined_efficiency_stats (lines 514-541) ---[0m
    pub async fn get_combined_efficiency_stats(&self) -> CombinedEfficiencyStats {
        let persistent_stats = self.persistent_manager.get_efficiency_stats().await;
        let worktree_sessions = self.worktree_sessions.read().await;

        let active_worktrees = worktree_sessions
            .values()
            .filter(|info| info.status == WorktreeSessionStatus::Active)
            .count();

        let idle_worktrees = worktree_sessions
            .values()
            .filter(|info| info.status == WorktreeSessionStatus::Idle)
            .count();

        CombinedEfficiencyStats {
            persistent_stats: persistent_stats.clone(),
            total_worktrees: worktree_sessions.len(),
            active_worktrees,
            idle_worktrees,
            worktree_reuse_rate: if persistent_stats.total_tasks_completed > worktree_sessions.len()
            {
                (persistent_stats.total_tasks_completed - worktree_sessions.len()) as f64
                    / persistent_stats.total_tasks_completed as f64
            } else {
                0.0
            },
        }
    }

  ./crates/ccswarm/src/session/worktree_session.rs:290-351 method setup_worktree_environment <-> ./crates/ccswarm/src/session/worktree_session.rs:589-602 method create_env_vars
  Similarity: 81.75%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:setup_worktree_environment (lines 290-351) ---[0m
    async fn setup_worktree_environment(
        &self,
        session_info: &WorktreeSessionInfo,
        agent: &Arc<Mutex<PersistentClaudeAgent>>,
    ) -> Result<()> {
        // Create minimal CLAUDE.md in worktree
        let agent_guard = agent.lock().await;
        let compact_prompt = format!(
            r#"# CLAUDE.md - {} Agent Workspace

🤖 **AGENT**: {}
📁 **WORKSPACE**: {}
🎯 **SPECIALIZATION**: {}

## Quick Identity
You are a specialized {} agent working in an isolated git worktree.
Maintain strict role boundaries and provide focused responses.

## Response Format
Always include:
🤖 AGENT: {}
📁 WORKSPACE: {}
🎯 SCOPE: [Task assessment]
"#,
            agent_guard.identity.specialization.name(),
            agent_guard.identity.agent_id,
            session_info
                .worktree_path
                .file_name()
                .unwrap_or_default()
                .to_string_lossy(),
            agent_guard.identity.specialization.name(),
            agent_guard.identity.specialization.name(),
            agent_guard.identity.specialization.name(),
            session_info
                .worktree_path
                .file_name()
                .unwrap_or_default()
                .to_string_lossy(),
        );

        let claude_md_path = session_info.worktree_path.join("CLAUDE.md");
        tokio::fs::write(&claude_md_path, compact_prompt)
            .await
            .context("Failed to write CLAUDE.md")?;

        // Create .claude.json configuration
        let claude_config_path = session_info.worktree_path.join(".claude.json");
        let config_json = serde_json::json!({
            "dangerous_skip": true,
            "json_output": false,
            "think_mode": null
        });
        tokio::fs::write(
            &claude_config_path,
            serde_json::to_string_pretty(&config_json)?,
        )
        .await
        .context("Failed to write .claude.json")?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:create_env_vars (lines 589-602) ---[0m
    fn create_env_vars(
        agent_id: &str,
        session_id: &str,
    ) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert("CCSWARM_WORKTREE_SESSION".to_string(), "true".to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

  ./crates/ccswarm/src/session/worktree_session.rs:395-442 method execute_task_batch <-> ./crates/ccswarm/src/session/worktree_session.rs:565-586 method shutdown
  Similarity: 79.11%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:execute_task_batch (lines 395-442) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks in worktree for role: {}",
            tasks.len(),
            role.name()
        );

        let session = self
            .get_or_create_worktree_session(role, claude_config)
            .await?;

        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, WorktreeSessionStatus::Active)
            .await;

        // Execute batch
        let results = {
            let mut agent = session.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Auto-commit batch results if enabled
        if self.config.auto_commit {
            if let Err(e) = self.auto_commit_changes(&agent_id).await {
                tracing::warn!("Auto-commit failed for agent {}: {}", agent_id, e);
            }
        }

        self.update_session_activity(&agent_id).await;
        self.update_session_status(&agent_id, WorktreeSessionStatus::Idle)
            .await;

        tracing::info!("Batch execution completed in worktree session");
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:shutdown (lines 565-586) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down worktree session manager");

        // Get all session IDs
        let session_ids: Vec<String> = {
            let sessions = self.worktree_sessions.read().await;
            sessions.keys().cloned().collect()
        };

        // Cleanup all sessions
        for agent_id in session_ids {
            if let Err(e) = self.cleanup_worktree_session(&agent_id).await {
                tracing::error!("Failed to cleanup session {}: {}", agent_id, e);
            }
        }

        // Shutdown persistent manager
        self.persistent_manager.shutdown().await?;

        tracing::info!("Worktree session manager shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/worktree_session.rs:144-170 method get_or_create_worktree_session <-> ./crates/ccswarm/src/session/worktree_session.rs:354-392 method execute_task
  Similarity: 77.43%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_or_create_worktree_session (lines 144-170) ---[0m
    pub async fn get_or_create_worktree_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        // Check if we can reuse an existing worktree session
        if let Some(existing) = self.find_reusable_worktree_session(&role).await {
            tracing::info!(
                "Reusing existing worktree session for role: {}",
                role.name()
            );
            return Ok(existing);
        }

        // Check limits
        let current_count = self.count_sessions_by_role(&role).await;
        if current_count >= self.config.max_worktrees_per_role {
            return Err(anyhow::anyhow!(
                "Maximum worktrees per role reached for {}: {}",
                role.name(),
                self.config.max_worktrees_per_role
            ));
        }

        // Create new worktree session
        self.create_new_worktree_session(role, claude_config).await
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:execute_task (lines 354-392) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let session = self
            .get_or_create_worktree_session(role, claude_config)
            .await?;

        // Update session status
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, WorktreeSessionStatus::Active)
            .await;

        // Execute task
        let result = {
            let mut agent = session.lock().await;
            agent.execute_task(task).await?
        };

        // Auto-commit if enabled
        if self.config.auto_commit && result.success {
            if let Err(e) = self.auto_commit_changes(&agent_id).await {
                tracing::warn!("Auto-commit failed for agent {}: {}", agent_id, e);
            }
        }

        // Update session info
        self.update_session_activity(&agent_id).await;
        self.update_session_status(&agent_id, WorktreeSessionStatus::Idle)
            .await;

        Ok(result)
    }

  ./crates/ccswarm/src/session/worktree_session.rs:354-392 method execute_task <-> ./crates/ccswarm/src/session/worktree_session.rs:565-586 method shutdown
  Similarity: 82.87%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:execute_task (lines 354-392) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let session = self
            .get_or_create_worktree_session(role, claude_config)
            .await?;

        // Update session status
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, WorktreeSessionStatus::Active)
            .await;

        // Execute task
        let result = {
            let mut agent = session.lock().await;
            agent.execute_task(task).await?
        };

        // Auto-commit if enabled
        if self.config.auto_commit && result.success {
            if let Err(e) = self.auto_commit_changes(&agent_id).await {
                tracing::warn!("Auto-commit failed for agent {}: {}", agent_id, e);
            }
        }

        // Update session info
        self.update_session_activity(&agent_id).await;
        self.update_session_status(&agent_id, WorktreeSessionStatus::Idle)
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:shutdown (lines 565-586) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down worktree session manager");

        // Get all session IDs
        let session_ids: Vec<String> = {
            let sessions = self.worktree_sessions.read().await;
            sessions.keys().cloned().collect()
        };

        // Cleanup all sessions
        for agent_id in session_ids {
            if let Err(e) = self.cleanup_worktree_session(&agent_id).await {
                tracing::error!("Failed to cleanup session {}: {}", agent_id, e);
            }
        }

        // Shutdown persistent manager
        self.persistent_manager.shutdown().await?;

        tracing::info!("Worktree session manager shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/worktree_session.rs:144-170 method get_or_create_worktree_session <-> ./crates/ccswarm/src/session/worktree_session.rs:514-541 method get_combined_efficiency_stats
  Similarity: 85.51%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_or_create_worktree_session (lines 144-170) ---[0m
    pub async fn get_or_create_worktree_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        // Check if we can reuse an existing worktree session
        if let Some(existing) = self.find_reusable_worktree_session(&role).await {
            tracing::info!(
                "Reusing existing worktree session for role: {}",
                role.name()
            );
            return Ok(existing);
        }

        // Check limits
        let current_count = self.count_sessions_by_role(&role).await;
        if current_count >= self.config.max_worktrees_per_role {
            return Err(anyhow::anyhow!(
                "Maximum worktrees per role reached for {}: {}",
                role.name(),
                self.config.max_worktrees_per_role
            ));
        }

        // Create new worktree session
        self.create_new_worktree_session(role, claude_config).await
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_combined_efficiency_stats (lines 514-541) ---[0m
    pub async fn get_combined_efficiency_stats(&self) -> CombinedEfficiencyStats {
        let persistent_stats = self.persistent_manager.get_efficiency_stats().await;
        let worktree_sessions = self.worktree_sessions.read().await;

        let active_worktrees = worktree_sessions
            .values()
            .filter(|info| info.status == WorktreeSessionStatus::Active)
            .count();

        let idle_worktrees = worktree_sessions
            .values()
            .filter(|info| info.status == WorktreeSessionStatus::Idle)
            .count();

        CombinedEfficiencyStats {
            persistent_stats: persistent_stats.clone(),
            total_worktrees: worktree_sessions.len(),
            active_worktrees,
            idle_worktrees,
            worktree_reuse_rate: if persistent_stats.total_tasks_completed > worktree_sessions.len()
            {
                (persistent_stats.total_tasks_completed - worktree_sessions.len()) as f64
                    / persistent_stats.total_tasks_completed as f64
            } else {
                0.0
            },
        }
    }

  ./crates/ccswarm/src/session/worktree_session.rs:354-392 method execute_task <-> ./crates/ccswarm/src/session/worktree_session.rs:634-647 function test_combined_efficiency_stats
  Similarity: 80.63%

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:execute_task (lines 354-392) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let session = self
            .get_or_create_worktree_session(role, claude_config)
            .await?;

        // Update session status
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, WorktreeSessionStatus::Active)
            .await;

        // Execute task
        let result = {
            let mut agent = session.lock().await;
            agent.execute_task(task).await?
        };

        // Auto-commit if enabled
        if self.config.auto_commit && result.success {
            if let Err(e) = self.auto_commit_changes(&agent_id).await {
                tracing::warn!("Auto-commit failed for agent {}: {}", agent_id, e);
            }
        }

        // Update session info
        self.update_session_activity(&agent_id).await;
        self.update_session_status(&agent_id, WorktreeSessionStatus::Idle)
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:test_combined_efficiency_stats (lines 634-647) ---[0m
    async fn test_combined_efficiency_stats() {
        let temp_dir = TempDir::new().unwrap();
        let config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let manager = WorktreeSessionManager::new(config).unwrap();
        let stats = manager.get_combined_efficiency_stats().await;

        assert_eq!(stats.total_worktrees, 0);
        assert_eq!(stats.active_worktrees, 0);
        assert_eq!(stats.idle_worktrees, 0);
    }

  ./crates/ccswarm/src/session/worktree_session.rs:144-170 method get_or_create_worktree_session <-> ./crates/ccswarm/src/session/worktree_session.rs:565-586 method shutdown
  Similarity: 83.20%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_or_create_worktree_session (lines 144-170) ---[0m
    pub async fn get_or_create_worktree_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        // Check if we can reuse an existing worktree session
        if let Some(existing) = self.find_reusable_worktree_session(&role).await {
            tracing::info!(
                "Reusing existing worktree session for role: {}",
                role.name()
            );
            return Ok(existing);
        }

        // Check limits
        let current_count = self.count_sessions_by_role(&role).await;
        if current_count >= self.config.max_worktrees_per_role {
            return Err(anyhow::anyhow!(
                "Maximum worktrees per role reached for {}: {}",
                role.name(),
                self.config.max_worktrees_per_role
            ));
        }

        // Create new worktree session
        self.create_new_worktree_session(role, claude_config).await
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:shutdown (lines 565-586) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down worktree session manager");

        // Get all session IDs
        let session_ids: Vec<String> = {
            let sessions = self.worktree_sessions.read().await;
            sessions.keys().cloned().collect()
        };

        // Cleanup all sessions
        for agent_id in session_ids {
            if let Err(e) = self.cleanup_worktree_session(&agent_id).await {
                tracing::error!("Failed to cleanup session {}: {}", agent_id, e);
            }
        }

        // Shutdown persistent manager
        self.persistent_manager.shutdown().await?;

        tracing::info!("Worktree session manager shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/worktree_session.rs:514-541 method get_combined_efficiency_stats <-> ./crates/ccswarm/src/session/worktree_session.rs:565-586 method shutdown
  Similarity: 79.91%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_combined_efficiency_stats (lines 514-541) ---[0m
    pub async fn get_combined_efficiency_stats(&self) -> CombinedEfficiencyStats {
        let persistent_stats = self.persistent_manager.get_efficiency_stats().await;
        let worktree_sessions = self.worktree_sessions.read().await;

        let active_worktrees = worktree_sessions
            .values()
            .filter(|info| info.status == WorktreeSessionStatus::Active)
            .count();

        let idle_worktrees = worktree_sessions
            .values()
            .filter(|info| info.status == WorktreeSessionStatus::Idle)
            .count();

        CombinedEfficiencyStats {
            persistent_stats: persistent_stats.clone(),
            total_worktrees: worktree_sessions.len(),
            active_worktrees,
            idle_worktrees,
            worktree_reuse_rate: if persistent_stats.total_tasks_completed > worktree_sessions.len()
            {
                (persistent_stats.total_tasks_completed - worktree_sessions.len()) as f64
                    / persistent_stats.total_tasks_completed as f64
            } else {
                0.0
            },
        }
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:shutdown (lines 565-586) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down worktree session manager");

        // Get all session IDs
        let session_ids: Vec<String> = {
            let sessions = self.worktree_sessions.read().await;
            sessions.keys().cloned().collect()
        };

        // Cleanup all sessions
        for agent_id in session_ids {
            if let Err(e) = self.cleanup_worktree_session(&agent_id).await {
                tracing::error!("Failed to cleanup session {}: {}", agent_id, e);
            }
        }

        // Shutdown persistent manager
        self.persistent_manager.shutdown().await?;

        tracing::info!("Worktree session manager shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/worktree_session.rs:445-468 method auto_commit_changes <-> ./crates/ccswarm/src/session/worktree_session.rs:544-562 method cleanup_worktree_session
  Similarity: 91.86%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:auto_commit_changes (lines 445-468) ---[0m
    async fn auto_commit_changes(&self, agent_id: &str) -> Result<()> {
        let session_info = {
            let sessions = self.worktree_sessions.read().await;
            sessions.get(agent_id).cloned()
        };

        if let Some(info) = session_info {
            let commit_message = format!(
                "Auto-commit from {} agent\n\nSession: {}\nTimestamp: {}",
                info.agent_role.name(),
                info.session_id,
                Utc::now().format("%Y-%m-%d %H:%M:%S UTC")
            );

            // Use git manager to commit changes
            self.git_manager
                .commit_worktree_changes(&info.worktree_path, &commit_message)
                .await?;

            tracing::info!("Auto-committed changes for agent: {}", agent_id);
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:cleanup_worktree_session (lines 544-562) ---[0m
    pub async fn cleanup_worktree_session(&self, agent_id: &str) -> Result<()> {
        let session_info = {
            let mut sessions = self.worktree_sessions.write().await;
            sessions.remove(agent_id)
        };

        if let Some(info) = session_info {
            if self.config.cleanup_worktrees {
                // Remove git worktree
                if let Err(e) = self.git_manager.remove_worktree(&info.worktree_path).await {
                    tracing::warn!("Failed to remove worktree for {}: {}", agent_id, e);
                }
            }

            tracing::info!("Cleaned up worktree session: {}", agent_id);
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/worktree_session.rs:354-392 method execute_task <-> ./crates/ccswarm/src/session/worktree_session.rs:589-602 method create_env_vars
  Similarity: 73.84%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:execute_task (lines 354-392) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let session = self
            .get_or_create_worktree_session(role, claude_config)
            .await?;

        // Update session status
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, WorktreeSessionStatus::Active)
            .await;

        // Execute task
        let result = {
            let mut agent = session.lock().await;
            agent.execute_task(task).await?
        };

        // Auto-commit if enabled
        if self.config.auto_commit && result.success {
            if let Err(e) = self.auto_commit_changes(&agent_id).await {
                tracing::warn!("Auto-commit failed for agent {}: {}", agent_id, e);
            }
        }

        // Update session info
        self.update_session_activity(&agent_id).await;
        self.update_session_status(&agent_id, WorktreeSessionStatus::Idle)
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:create_env_vars (lines 589-602) ---[0m
    fn create_env_vars(
        agent_id: &str,
        session_id: &str,
    ) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert("CCSWARM_WORKTREE_SESSION".to_string(), "true".to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

  ./crates/ccswarm/src/session/worktree_session.rs:514-541 method get_combined_efficiency_stats <-> ./crates/ccswarm/src/session/worktree_session.rs:589-602 method create_env_vars
  Similarity: 89.44%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_combined_efficiency_stats (lines 514-541) ---[0m
    pub async fn get_combined_efficiency_stats(&self) -> CombinedEfficiencyStats {
        let persistent_stats = self.persistent_manager.get_efficiency_stats().await;
        let worktree_sessions = self.worktree_sessions.read().await;

        let active_worktrees = worktree_sessions
            .values()
            .filter(|info| info.status == WorktreeSessionStatus::Active)
            .count();

        let idle_worktrees = worktree_sessions
            .values()
            .filter(|info| info.status == WorktreeSessionStatus::Idle)
            .count();

        CombinedEfficiencyStats {
            persistent_stats: persistent_stats.clone(),
            total_worktrees: worktree_sessions.len(),
            active_worktrees,
            idle_worktrees,
            worktree_reuse_rate: if persistent_stats.total_tasks_completed > worktree_sessions.len()
            {
                (persistent_stats.total_tasks_completed - worktree_sessions.len()) as f64
                    / persistent_stats.total_tasks_completed as f64
            } else {
                0.0
            },
        }
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:create_env_vars (lines 589-602) ---[0m
    fn create_env_vars(
        agent_id: &str,
        session_id: &str,
    ) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert("CCSWARM_WORKTREE_SESSION".to_string(), "true".to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

  ./crates/ccswarm/src/session/worktree_session.rs:445-468 method auto_commit_changes <-> ./crates/ccswarm/src/session/worktree_session.rs:514-541 method get_combined_efficiency_stats
  Similarity: 71.71%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:auto_commit_changes (lines 445-468) ---[0m
    async fn auto_commit_changes(&self, agent_id: &str) -> Result<()> {
        let session_info = {
            let sessions = self.worktree_sessions.read().await;
            sessions.get(agent_id).cloned()
        };

        if let Some(info) = session_info {
            let commit_message = format!(
                "Auto-commit from {} agent\n\nSession: {}\nTimestamp: {}",
                info.agent_role.name(),
                info.session_id,
                Utc::now().format("%Y-%m-%d %H:%M:%S UTC")
            );

            // Use git manager to commit changes
            self.git_manager
                .commit_worktree_changes(&info.worktree_path, &commit_message)
                .await?;

            tracing::info!("Auto-committed changes for agent: {}", agent_id);
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_combined_efficiency_stats (lines 514-541) ---[0m
    pub async fn get_combined_efficiency_stats(&self) -> CombinedEfficiencyStats {
        let persistent_stats = self.persistent_manager.get_efficiency_stats().await;
        let worktree_sessions = self.worktree_sessions.read().await;

        let active_worktrees = worktree_sessions
            .values()
            .filter(|info| info.status == WorktreeSessionStatus::Active)
            .count();

        let idle_worktrees = worktree_sessions
            .values()
            .filter(|info| info.status == WorktreeSessionStatus::Idle)
            .count();

        CombinedEfficiencyStats {
            persistent_stats: persistent_stats.clone(),
            total_worktrees: worktree_sessions.len(),
            active_worktrees,
            idle_worktrees,
            worktree_reuse_rate: if persistent_stats.total_tasks_completed > worktree_sessions.len()
            {
                (persistent_stats.total_tasks_completed - worktree_sessions.len()) as f64
                    / persistent_stats.total_tasks_completed as f64
            } else {
                0.0
            },
        }
    }

  ./crates/ccswarm/src/session/worktree_session.rs:173-194 method find_reusable_worktree_session <-> ./crates/ccswarm/src/session/worktree_session.rs:445-468 method auto_commit_changes
  Similarity: 78.01%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:find_reusable_worktree_session (lines 173-194) ---[0m
    async fn find_reusable_worktree_session(
        &self,
        role: &AgentRole,
    ) -> Option<Arc<Mutex<PersistentClaudeAgent>>> {
        let sessions = self.worktree_sessions.read().await;

        for (_agent_id, info) in sessions.iter() {
            if info.agent_role.name() == role.name()
                && info.status == WorktreeSessionStatus::Idle
                && info.git_status == GitWorktreeStatus::Ready
            {
                // Get the persistent session
                return self
                    .persistent_manager
                    .get_or_create_session(role.clone(), ClaudeConfig::default())
                    .await
                    .ok();
            }
        }

        None
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:auto_commit_changes (lines 445-468) ---[0m
    async fn auto_commit_changes(&self, agent_id: &str) -> Result<()> {
        let session_info = {
            let sessions = self.worktree_sessions.read().await;
            sessions.get(agent_id).cloned()
        };

        if let Some(info) = session_info {
            let commit_message = format!(
                "Auto-commit from {} agent\n\nSession: {}\nTimestamp: {}",
                info.agent_role.name(),
                info.session_id,
                Utc::now().format("%Y-%m-%d %H:%M:%S UTC")
            );

            // Use git manager to commit changes
            self.git_manager
                .commit_worktree_changes(&info.worktree_path, &commit_message)
                .await?;

            tracing::info!("Auto-committed changes for agent: {}", agent_id);
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/worktree_session.rs:144-170 method get_or_create_worktree_session <-> ./crates/ccswarm/src/session/worktree_session.rs:173-194 method find_reusable_worktree_session
  Similarity: 71.85%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_or_create_worktree_session (lines 144-170) ---[0m
    pub async fn get_or_create_worktree_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        // Check if we can reuse an existing worktree session
        if let Some(existing) = self.find_reusable_worktree_session(&role).await {
            tracing::info!(
                "Reusing existing worktree session for role: {}",
                role.name()
            );
            return Ok(existing);
        }

        // Check limits
        let current_count = self.count_sessions_by_role(&role).await;
        if current_count >= self.config.max_worktrees_per_role {
            return Err(anyhow::anyhow!(
                "Maximum worktrees per role reached for {}: {}",
                role.name(),
                self.config.max_worktrees_per_role
            ));
        }

        // Create new worktree session
        self.create_new_worktree_session(role, claude_config).await
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:find_reusable_worktree_session (lines 173-194) ---[0m
    async fn find_reusable_worktree_session(
        &self,
        role: &AgentRole,
    ) -> Option<Arc<Mutex<PersistentClaudeAgent>>> {
        let sessions = self.worktree_sessions.read().await;

        for (_agent_id, info) in sessions.iter() {
            if info.agent_role.name() == role.name()
                && info.status == WorktreeSessionStatus::Idle
                && info.git_status == GitWorktreeStatus::Ready
            {
                // Get the persistent session
                return self
                    .persistent_manager
                    .get_or_create_session(role.clone(), ClaudeConfig::default())
                    .await
                    .ok();
            }
        }

        None
    }

  ./crates/ccswarm/src/session/worktree_session.rs:173-194 method find_reusable_worktree_session <-> ./crates/ccswarm/src/session/worktree_session.rs:514-541 method get_combined_efficiency_stats
  Similarity: 70.32%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:find_reusable_worktree_session (lines 173-194) ---[0m
    async fn find_reusable_worktree_session(
        &self,
        role: &AgentRole,
    ) -> Option<Arc<Mutex<PersistentClaudeAgent>>> {
        let sessions = self.worktree_sessions.read().await;

        for (_agent_id, info) in sessions.iter() {
            if info.agent_role.name() == role.name()
                && info.status == WorktreeSessionStatus::Idle
                && info.git_status == GitWorktreeStatus::Ready
            {
                // Get the persistent session
                return self
                    .persistent_manager
                    .get_or_create_session(role.clone(), ClaudeConfig::default())
                    .await
                    .ok();
            }
        }

        None
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_combined_efficiency_stats (lines 514-541) ---[0m
    pub async fn get_combined_efficiency_stats(&self) -> CombinedEfficiencyStats {
        let persistent_stats = self.persistent_manager.get_efficiency_stats().await;
        let worktree_sessions = self.worktree_sessions.read().await;

        let active_worktrees = worktree_sessions
            .values()
            .filter(|info| info.status == WorktreeSessionStatus::Active)
            .count();

        let idle_worktrees = worktree_sessions
            .values()
            .filter(|info| info.status == WorktreeSessionStatus::Idle)
            .count();

        CombinedEfficiencyStats {
            persistent_stats: persistent_stats.clone(),
            total_worktrees: worktree_sessions.len(),
            active_worktrees,
            idle_worktrees,
            worktree_reuse_rate: if persistent_stats.total_tasks_completed > worktree_sessions.len()
            {
                (persistent_stats.total_tasks_completed - worktree_sessions.len()) as f64
                    / persistent_stats.total_tasks_completed as f64
            } else {
                0.0
            },
        }
    }

  ./crates/ccswarm/src/session/worktree_session.rs:113-127 method new <-> ./crates/ccswarm/src/session/worktree_session.rs:445-468 method auto_commit_changes
  Similarity: 88.82%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:new (lines 113-127) ---[0m
    pub fn new(config: WorktreeSessionConfig) -> Result<Self> {
        let persistent_manager = PersistentSessionManager::new(
            config.repo_path.join("agents"),
            config.persistent_config.clone(),
        );

        let git_manager = ShellWorktreeManager::new(config.repo_path.clone())?;

        Ok(Self {
            persistent_manager,
            git_manager,
            worktree_sessions: Arc::new(RwLock::new(HashMap::new())),
            config,
        })
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:auto_commit_changes (lines 445-468) ---[0m
    async fn auto_commit_changes(&self, agent_id: &str) -> Result<()> {
        let session_info = {
            let sessions = self.worktree_sessions.read().await;
            sessions.get(agent_id).cloned()
        };

        if let Some(info) = session_info {
            let commit_message = format!(
                "Auto-commit from {} agent\n\nSession: {}\nTimestamp: {}",
                info.agent_role.name(),
                info.session_id,
                Utc::now().format("%Y-%m-%d %H:%M:%S UTC")
            );

            // Use git manager to commit changes
            self.git_manager
                .commit_worktree_changes(&info.worktree_path, &commit_message)
                .await?;

            tracing::info!("Auto-committed changes for agent: {}", agent_id);
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/worktree_session.rs:130-141 method start <-> ./crates/ccswarm/src/session/worktree_session.rs:144-170 method get_or_create_worktree_session
  Similarity: 87.75%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:start (lines 130-141) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting worktree session manager");

        // Start the underlying persistent session manager
        self.persistent_manager.start().await?;

        // Initialize git repository if needed
        self.git_manager.init_repo_if_needed().await?;

        tracing::info!("Worktree session manager started successfully");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_or_create_worktree_session (lines 144-170) ---[0m
    pub async fn get_or_create_worktree_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        // Check if we can reuse an existing worktree session
        if let Some(existing) = self.find_reusable_worktree_session(&role).await {
            tracing::info!(
                "Reusing existing worktree session for role: {}",
                role.name()
            );
            return Ok(existing);
        }

        // Check limits
        let current_count = self.count_sessions_by_role(&role).await;
        if current_count >= self.config.max_worktrees_per_role {
            return Err(anyhow::anyhow!(
                "Maximum worktrees per role reached for {}: {}",
                role.name(),
                self.config.max_worktrees_per_role
            ));
        }

        // Create new worktree session
        self.create_new_worktree_session(role, claude_config).await
    }

  ./crates/ccswarm/src/session/worktree_session.rs:173-194 method find_reusable_worktree_session <-> ./crates/ccswarm/src/session/worktree_session.rs:544-562 method cleanup_worktree_session
  Similarity: 83.33%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:find_reusable_worktree_session (lines 173-194) ---[0m
    async fn find_reusable_worktree_session(
        &self,
        role: &AgentRole,
    ) -> Option<Arc<Mutex<PersistentClaudeAgent>>> {
        let sessions = self.worktree_sessions.read().await;

        for (_agent_id, info) in sessions.iter() {
            if info.agent_role.name() == role.name()
                && info.status == WorktreeSessionStatus::Idle
                && info.git_status == GitWorktreeStatus::Ready
            {
                // Get the persistent session
                return self
                    .persistent_manager
                    .get_or_create_session(role.clone(), ClaudeConfig::default())
                    .await
                    .ok();
            }
        }

        None
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:cleanup_worktree_session (lines 544-562) ---[0m
    pub async fn cleanup_worktree_session(&self, agent_id: &str) -> Result<()> {
        let session_info = {
            let mut sessions = self.worktree_sessions.write().await;
            sessions.remove(agent_id)
        };

        if let Some(info) = session_info {
            if self.config.cleanup_worktrees {
                // Remove git worktree
                if let Err(e) = self.git_manager.remove_worktree(&info.worktree_path).await {
                    tracing::warn!("Failed to remove worktree for {}: {}", agent_id, e);
                }
            }

            tracing::info!("Cleaned up worktree session: {}", agent_id);
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/worktree_session.rs:514-541 method get_combined_efficiency_stats <-> ./crates/ccswarm/src/session/worktree_session.rs:544-562 method cleanup_worktree_session
  Similarity: 71.71%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_combined_efficiency_stats (lines 514-541) ---[0m
    pub async fn get_combined_efficiency_stats(&self) -> CombinedEfficiencyStats {
        let persistent_stats = self.persistent_manager.get_efficiency_stats().await;
        let worktree_sessions = self.worktree_sessions.read().await;

        let active_worktrees = worktree_sessions
            .values()
            .filter(|info| info.status == WorktreeSessionStatus::Active)
            .count();

        let idle_worktrees = worktree_sessions
            .values()
            .filter(|info| info.status == WorktreeSessionStatus::Idle)
            .count();

        CombinedEfficiencyStats {
            persistent_stats: persistent_stats.clone(),
            total_worktrees: worktree_sessions.len(),
            active_worktrees,
            idle_worktrees,
            worktree_reuse_rate: if persistent_stats.total_tasks_completed > worktree_sessions.len()
            {
                (persistent_stats.total_tasks_completed - worktree_sessions.len()) as f64
                    / persistent_stats.total_tasks_completed as f64
            } else {
                0.0
            },
        }
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:cleanup_worktree_session (lines 544-562) ---[0m
    pub async fn cleanup_worktree_session(&self, agent_id: &str) -> Result<()> {
        let session_info = {
            let mut sessions = self.worktree_sessions.write().await;
            sessions.remove(agent_id)
        };

        if let Some(info) = session_info {
            if self.config.cleanup_worktrees {
                // Remove git worktree
                if let Err(e) = self.git_manager.remove_worktree(&info.worktree_path).await {
                    tracing::warn!("Failed to remove worktree for {}: {}", agent_id, e);
                }
            }

            tracing::info!("Cleaned up worktree session: {}", agent_id);
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/worktree_session.rs:514-541 method get_combined_efficiency_stats <-> ./crates/ccswarm/src/session/worktree_session.rs:634-647 function test_combined_efficiency_stats
  Similarity: 79.63%

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_combined_efficiency_stats (lines 514-541) ---[0m
    pub async fn get_combined_efficiency_stats(&self) -> CombinedEfficiencyStats {
        let persistent_stats = self.persistent_manager.get_efficiency_stats().await;
        let worktree_sessions = self.worktree_sessions.read().await;

        let active_worktrees = worktree_sessions
            .values()
            .filter(|info| info.status == WorktreeSessionStatus::Active)
            .count();

        let idle_worktrees = worktree_sessions
            .values()
            .filter(|info| info.status == WorktreeSessionStatus::Idle)
            .count();

        CombinedEfficiencyStats {
            persistent_stats: persistent_stats.clone(),
            total_worktrees: worktree_sessions.len(),
            active_worktrees,
            idle_worktrees,
            worktree_reuse_rate: if persistent_stats.total_tasks_completed > worktree_sessions.len()
            {
                (persistent_stats.total_tasks_completed - worktree_sessions.len()) as f64
                    / persistent_stats.total_tasks_completed as f64
            } else {
                0.0
            },
        }
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:test_combined_efficiency_stats (lines 634-647) ---[0m
    async fn test_combined_efficiency_stats() {
        let temp_dir = TempDir::new().unwrap();
        let config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let manager = WorktreeSessionManager::new(config).unwrap();
        let stats = manager.get_combined_efficiency_stats().await;

        assert_eq!(stats.total_worktrees, 0);
        assert_eq!(stats.active_worktrees, 0);
        assert_eq!(stats.idle_worktrees, 0);
    }

  ./crates/ccswarm/src/session/worktree_session.rs:144-170 method get_or_create_worktree_session <-> ./crates/ccswarm/src/session/worktree_session.rs:589-602 method create_env_vars
  Similarity: 79.53%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_or_create_worktree_session (lines 144-170) ---[0m
    pub async fn get_or_create_worktree_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        // Check if we can reuse an existing worktree session
        if let Some(existing) = self.find_reusable_worktree_session(&role).await {
            tracing::info!(
                "Reusing existing worktree session for role: {}",
                role.name()
            );
            return Ok(existing);
        }

        // Check limits
        let current_count = self.count_sessions_by_role(&role).await;
        if current_count >= self.config.max_worktrees_per_role {
            return Err(anyhow::anyhow!(
                "Maximum worktrees per role reached for {}: {}",
                role.name(),
                self.config.max_worktrees_per_role
            ));
        }

        // Create new worktree session
        self.create_new_worktree_session(role, claude_config).await
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:create_env_vars (lines 589-602) ---[0m
    fn create_env_vars(
        agent_id: &str,
        session_id: &str,
    ) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert("CCSWARM_WORKTREE_SESSION".to_string(), "true".to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

  ./crates/ccswarm/src/session/worktree_session.rs:144-170 method get_or_create_worktree_session <-> ./crates/ccswarm/src/session/worktree_session.rs:634-647 function test_combined_efficiency_stats
  Similarity: 75.89%

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_or_create_worktree_session (lines 144-170) ---[0m
    pub async fn get_or_create_worktree_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        // Check if we can reuse an existing worktree session
        if let Some(existing) = self.find_reusable_worktree_session(&role).await {
            tracing::info!(
                "Reusing existing worktree session for role: {}",
                role.name()
            );
            return Ok(existing);
        }

        // Check limits
        let current_count = self.count_sessions_by_role(&role).await;
        if current_count >= self.config.max_worktrees_per_role {
            return Err(anyhow::anyhow!(
                "Maximum worktrees per role reached for {}: {}",
                role.name(),
                self.config.max_worktrees_per_role
            ));
        }

        // Create new worktree session
        self.create_new_worktree_session(role, claude_config).await
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:test_combined_efficiency_stats (lines 634-647) ---[0m
    async fn test_combined_efficiency_stats() {
        let temp_dir = TempDir::new().unwrap();
        let config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let manager = WorktreeSessionManager::new(config).unwrap();
        let stats = manager.get_combined_efficiency_stats().await;

        assert_eq!(stats.total_worktrees, 0);
        assert_eq!(stats.active_worktrees, 0);
        assert_eq!(stats.idle_worktrees, 0);
    }

  ./crates/ccswarm/src/session/worktree_session.rs:113-127 method new <-> ./crates/ccswarm/src/session/worktree_session.rs:173-194 method find_reusable_worktree_session
  Similarity: 83.63%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:new (lines 113-127) ---[0m
    pub fn new(config: WorktreeSessionConfig) -> Result<Self> {
        let persistent_manager = PersistentSessionManager::new(
            config.repo_path.join("agents"),
            config.persistent_config.clone(),
        );

        let git_manager = ShellWorktreeManager::new(config.repo_path.clone())?;

        Ok(Self {
            persistent_manager,
            git_manager,
            worktree_sessions: Arc::new(RwLock::new(HashMap::new())),
            config,
        })
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:find_reusable_worktree_session (lines 173-194) ---[0m
    async fn find_reusable_worktree_session(
        &self,
        role: &AgentRole,
    ) -> Option<Arc<Mutex<PersistentClaudeAgent>>> {
        let sessions = self.worktree_sessions.read().await;

        for (_agent_id, info) in sessions.iter() {
            if info.agent_role.name() == role.name()
                && info.status == WorktreeSessionStatus::Idle
                && info.git_status == GitWorktreeStatus::Ready
            {
                // Get the persistent session
                return self
                    .persistent_manager
                    .get_or_create_session(role.clone(), ClaudeConfig::default())
                    .await
                    .ok();
            }
        }

        None
    }

  ./crates/ccswarm/src/session/worktree_session.rs:113-127 method new <-> ./crates/ccswarm/src/session/worktree_session.rs:544-562 method cleanup_worktree_session
  Similarity: 87.50%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:new (lines 113-127) ---[0m
    pub fn new(config: WorktreeSessionConfig) -> Result<Self> {
        let persistent_manager = PersistentSessionManager::new(
            config.repo_path.join("agents"),
            config.persistent_config.clone(),
        );

        let git_manager = ShellWorktreeManager::new(config.repo_path.clone())?;

        Ok(Self {
            persistent_manager,
            git_manager,
            worktree_sessions: Arc::new(RwLock::new(HashMap::new())),
            config,
        })
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:cleanup_worktree_session (lines 544-562) ---[0m
    pub async fn cleanup_worktree_session(&self, agent_id: &str) -> Result<()> {
        let session_info = {
            let mut sessions = self.worktree_sessions.write().await;
            sessions.remove(agent_id)
        };

        if let Some(info) = session_info {
            if self.config.cleanup_worktrees {
                // Remove git worktree
                if let Err(e) = self.git_manager.remove_worktree(&info.worktree_path).await {
                    tracing::warn!("Failed to remove worktree for {}: {}", agent_id, e);
                }
            }

            tracing::info!("Cleaned up worktree session: {}", agent_id);
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/worktree_session.rs:173-194 method find_reusable_worktree_session <-> ./crates/ccswarm/src/session/worktree_session.rs:501-511 method get_worktree_sessions_by_role
  Similarity: 89.70%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:find_reusable_worktree_session (lines 173-194) ---[0m
    async fn find_reusable_worktree_session(
        &self,
        role: &AgentRole,
    ) -> Option<Arc<Mutex<PersistentClaudeAgent>>> {
        let sessions = self.worktree_sessions.read().await;

        for (_agent_id, info) in sessions.iter() {
            if info.agent_role.name() == role.name()
                && info.status == WorktreeSessionStatus::Idle
                && info.git_status == GitWorktreeStatus::Ready
            {
                // Get the persistent session
                return self
                    .persistent_manager
                    .get_or_create_session(role.clone(), ClaudeConfig::default())
                    .await
                    .ok();
            }
        }

        None
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_worktree_sessions_by_role (lines 501-511) ---[0m
    pub async fn get_worktree_sessions_by_role(
        &self,
        role: &AgentRole,
    ) -> Vec<WorktreeSessionInfo> {
        let sessions = self.worktree_sessions.read().await;
        sessions
            .values()
            .filter(|info| info.agent_role.name() == role.name())
            .cloned()
            .collect()
    }

  ./crates/ccswarm/src/session/worktree_session.rs:565-586 method shutdown <-> ./crates/ccswarm/src/session/worktree_session.rs:634-647 function test_combined_efficiency_stats
  Similarity: 78.80%

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:shutdown (lines 565-586) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down worktree session manager");

        // Get all session IDs
        let session_ids: Vec<String> = {
            let sessions = self.worktree_sessions.read().await;
            sessions.keys().cloned().collect()
        };

        // Cleanup all sessions
        for agent_id in session_ids {
            if let Err(e) = self.cleanup_worktree_session(&agent_id).await {
                tracing::error!("Failed to cleanup session {}: {}", agent_id, e);
            }
        }

        // Shutdown persistent manager
        self.persistent_manager.shutdown().await?;

        tracing::info!("Worktree session manager shutdown complete");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:test_combined_efficiency_stats (lines 634-647) ---[0m
    async fn test_combined_efficiency_stats() {
        let temp_dir = TempDir::new().unwrap();
        let config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let manager = WorktreeSessionManager::new(config).unwrap();
        let stats = manager.get_combined_efficiency_stats().await;

        assert_eq!(stats.total_worktrees, 0);
        assert_eq!(stats.active_worktrees, 0);
        assert_eq!(stats.idle_worktrees, 0);
    }

  ./crates/ccswarm/src/session/worktree_session.rs:565-586 method shutdown <-> ./crates/ccswarm/src/session/worktree_session.rs:589-602 method create_env_vars
  Similarity: 73.49%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:shutdown (lines 565-586) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down worktree session manager");

        // Get all session IDs
        let session_ids: Vec<String> = {
            let sessions = self.worktree_sessions.read().await;
            sessions.keys().cloned().collect()
        };

        // Cleanup all sessions
        for agent_id in session_ids {
            if let Err(e) = self.cleanup_worktree_session(&agent_id).await {
                tracing::error!("Failed to cleanup session {}: {}", agent_id, e);
            }
        }

        // Shutdown persistent manager
        self.persistent_manager.shutdown().await?;

        tracing::info!("Worktree session manager shutdown complete");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:create_env_vars (lines 589-602) ---[0m
    fn create_env_vars(
        agent_id: &str,
        session_id: &str,
    ) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert("CCSWARM_WORKTREE_SESSION".to_string(), "true".to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

  ./crates/ccswarm/src/session/worktree_session.rs:144-170 method get_or_create_worktree_session <-> ./crates/ccswarm/src/session/worktree_session.rs:622-631 function test_worktree_session_manager_creation
  Similarity: 71.39%

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_or_create_worktree_session (lines 144-170) ---[0m
    pub async fn get_or_create_worktree_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        // Check if we can reuse an existing worktree session
        if let Some(existing) = self.find_reusable_worktree_session(&role).await {
            tracing::info!(
                "Reusing existing worktree session for role: {}",
                role.name()
            );
            return Ok(existing);
        }

        // Check limits
        let current_count = self.count_sessions_by_role(&role).await;
        if current_count >= self.config.max_worktrees_per_role {
            return Err(anyhow::anyhow!(
                "Maximum worktrees per role reached for {}: {}",
                role.name(),
                self.config.max_worktrees_per_role
            ));
        }

        // Create new worktree session
        self.create_new_worktree_session(role, claude_config).await
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:test_worktree_session_manager_creation (lines 622-631) ---[0m
    async fn test_worktree_session_manager_creation() {
        let temp_dir = TempDir::new().unwrap();
        let config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let manager = WorktreeSessionManager::new(config).unwrap();
        assert_eq!(manager.worktree_sessions.read().await.len(), 0);
    }

  ./crates/ccswarm/src/session/worktree_session.rs:501-511 method get_worktree_sessions_by_role <-> ./crates/ccswarm/src/session/worktree_session.rs:544-562 method cleanup_worktree_session
  Similarity: 86.88%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_worktree_sessions_by_role (lines 501-511) ---[0m
    pub async fn get_worktree_sessions_by_role(
        &self,
        role: &AgentRole,
    ) -> Vec<WorktreeSessionInfo> {
        let sessions = self.worktree_sessions.read().await;
        sessions
            .values()
            .filter(|info| info.agent_role.name() == role.name())
            .cloned()
            .collect()
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:cleanup_worktree_session (lines 544-562) ---[0m
    pub async fn cleanup_worktree_session(&self, agent_id: &str) -> Result<()> {
        let session_info = {
            let mut sessions = self.worktree_sessions.write().await;
            sessions.remove(agent_id)
        };

        if let Some(info) = session_info {
            if self.config.cleanup_worktrees {
                // Remove git worktree
                if let Err(e) = self.git_manager.remove_worktree(&info.worktree_path).await {
                    tracing::warn!("Failed to remove worktree for {}: {}", agent_id, e);
                }
            }

            tracing::info!("Cleaned up worktree session: {}", agent_id);
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/worktree_session.rs:173-194 method find_reusable_worktree_session <-> ./crates/ccswarm/src/session/worktree_session.rs:197-203 method count_sessions_by_role
  Similarity: 89.70%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:find_reusable_worktree_session (lines 173-194) ---[0m
    async fn find_reusable_worktree_session(
        &self,
        role: &AgentRole,
    ) -> Option<Arc<Mutex<PersistentClaudeAgent>>> {
        let sessions = self.worktree_sessions.read().await;

        for (_agent_id, info) in sessions.iter() {
            if info.agent_role.name() == role.name()
                && info.status == WorktreeSessionStatus::Idle
                && info.git_status == GitWorktreeStatus::Ready
            {
                // Get the persistent session
                return self
                    .persistent_manager
                    .get_or_create_session(role.clone(), ClaudeConfig::default())
                    .await
                    .ok();
            }
        }

        None
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:count_sessions_by_role (lines 197-203) ---[0m
    async fn count_sessions_by_role(&self, role: &AgentRole) -> usize {
        let sessions = self.worktree_sessions.read().await;
        sessions
            .values()
            .filter(|info| info.agent_role.name() == role.name())
            .count()
    }

  ./crates/ccswarm/src/session/worktree_session.rs:445-468 method auto_commit_changes <-> ./crates/ccswarm/src/session/worktree_session.rs:622-631 function test_worktree_session_manager_creation
  Similarity: 75.90%

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:auto_commit_changes (lines 445-468) ---[0m
    async fn auto_commit_changes(&self, agent_id: &str) -> Result<()> {
        let session_info = {
            let sessions = self.worktree_sessions.read().await;
            sessions.get(agent_id).cloned()
        };

        if let Some(info) = session_info {
            let commit_message = format!(
                "Auto-commit from {} agent\n\nSession: {}\nTimestamp: {}",
                info.agent_role.name(),
                info.session_id,
                Utc::now().format("%Y-%m-%d %H:%M:%S UTC")
            );

            // Use git manager to commit changes
            self.git_manager
                .commit_worktree_changes(&info.worktree_path, &commit_message)
                .await?;

            tracing::info!("Auto-committed changes for agent: {}", agent_id);
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:test_worktree_session_manager_creation (lines 622-631) ---[0m
    async fn test_worktree_session_manager_creation() {
        let temp_dir = TempDir::new().unwrap();
        let config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let manager = WorktreeSessionManager::new(config).unwrap();
        assert_eq!(manager.worktree_sessions.read().await.len(), 0);
    }

  ./crates/ccswarm/src/session/worktree_session.rs:173-194 method find_reusable_worktree_session <-> ./crates/ccswarm/src/session/worktree_session.rs:477-483 method update_session_status
  Similarity: 86.15%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:find_reusable_worktree_session (lines 173-194) ---[0m
    async fn find_reusable_worktree_session(
        &self,
        role: &AgentRole,
    ) -> Option<Arc<Mutex<PersistentClaudeAgent>>> {
        let sessions = self.worktree_sessions.read().await;

        for (_agent_id, info) in sessions.iter() {
            if info.agent_role.name() == role.name()
                && info.status == WorktreeSessionStatus::Idle
                && info.git_status == GitWorktreeStatus::Ready
            {
                // Get the persistent session
                return self
                    .persistent_manager
                    .get_or_create_session(role.clone(), ClaudeConfig::default())
                    .await
                    .ok();
            }
        }

        None
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:update_session_status (lines 477-483) ---[0m
    async fn update_session_status(&self, agent_id: &str, status: WorktreeSessionStatus) {
        let mut sessions = self.worktree_sessions.write().await;
        if let Some(info) = sessions.get_mut(agent_id) {
            info.status = status;
            info.last_activity = Utc::now();
        }
    }

  ./crates/ccswarm/src/session/worktree_session.rs:173-194 method find_reusable_worktree_session <-> ./crates/ccswarm/src/session/worktree_session.rs:486-492 method update_session_activity
  Similarity: 86.15%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:find_reusable_worktree_session (lines 173-194) ---[0m
    async fn find_reusable_worktree_session(
        &self,
        role: &AgentRole,
    ) -> Option<Arc<Mutex<PersistentClaudeAgent>>> {
        let sessions = self.worktree_sessions.read().await;

        for (_agent_id, info) in sessions.iter() {
            if info.agent_role.name() == role.name()
                && info.status == WorktreeSessionStatus::Idle
                && info.git_status == GitWorktreeStatus::Ready
            {
                // Get the persistent session
                return self
                    .persistent_manager
                    .get_or_create_session(role.clone(), ClaudeConfig::default())
                    .await
                    .ok();
            }
        }

        None
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:update_session_activity (lines 486-492) ---[0m
    async fn update_session_activity(&self, agent_id: &str) {
        let mut sessions = self.worktree_sessions.write().await;
        if let Some(info) = sessions.get_mut(agent_id) {
            info.last_activity = Utc::now();
            info.tasks_completed += 1;
        }
    }

  ./crates/ccswarm/src/session/worktree_session.rs:130-141 method start <-> ./crates/ccswarm/src/session/worktree_session.rs:544-562 method cleanup_worktree_session
  Similarity: 71.18%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:start (lines 130-141) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting worktree session manager");

        // Start the underlying persistent session manager
        self.persistent_manager.start().await?;

        // Initialize git repository if needed
        self.git_manager.init_repo_if_needed().await?;

        tracing::info!("Worktree session manager started successfully");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:cleanup_worktree_session (lines 544-562) ---[0m
    pub async fn cleanup_worktree_session(&self, agent_id: &str) -> Result<()> {
        let session_info = {
            let mut sessions = self.worktree_sessions.write().await;
            sessions.remove(agent_id)
        };

        if let Some(info) = session_info {
            if self.config.cleanup_worktrees {
                // Remove git worktree
                if let Err(e) = self.git_manager.remove_worktree(&info.worktree_path).await {
                    tracing::warn!("Failed to remove worktree for {}: {}", agent_id, e);
                }
            }

            tracing::info!("Cleaned up worktree session: {}", agent_id);
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/worktree_session.rs:589-602 method create_env_vars <-> ./crates/ccswarm/src/session/worktree_session.rs:634-647 function test_combined_efficiency_stats
  Similarity: 77.09%

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:create_env_vars (lines 589-602) ---[0m
    fn create_env_vars(
        agent_id: &str,
        session_id: &str,
    ) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert("CCSWARM_WORKTREE_SESSION".to_string(), "true".to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:test_combined_efficiency_stats (lines 634-647) ---[0m
    async fn test_combined_efficiency_stats() {
        let temp_dir = TempDir::new().unwrap();
        let config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let manager = WorktreeSessionManager::new(config).unwrap();
        let stats = manager.get_combined_efficiency_stats().await;

        assert_eq!(stats.total_worktrees, 0);
        assert_eq!(stats.active_worktrees, 0);
        assert_eq!(stats.idle_worktrees, 0);
    }

  ./crates/ccswarm/src/session/worktree_session.rs:544-562 method cleanup_worktree_session <-> ./crates/ccswarm/src/session/worktree_session.rs:622-631 function test_worktree_session_manager_creation
  Similarity: 73.06%

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:cleanup_worktree_session (lines 544-562) ---[0m
    pub async fn cleanup_worktree_session(&self, agent_id: &str) -> Result<()> {
        let session_info = {
            let mut sessions = self.worktree_sessions.write().await;
            sessions.remove(agent_id)
        };

        if let Some(info) = session_info {
            if self.config.cleanup_worktrees {
                // Remove git worktree
                if let Err(e) = self.git_manager.remove_worktree(&info.worktree_path).await {
                    tracing::warn!("Failed to remove worktree for {}: {}", agent_id, e);
                }
            }

            tracing::info!("Cleaned up worktree session: {}", agent_id);
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:test_worktree_session_manager_creation (lines 622-631) ---[0m
    async fn test_worktree_session_manager_creation() {
        let temp_dir = TempDir::new().unwrap();
        let config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let manager = WorktreeSessionManager::new(config).unwrap();
        assert_eq!(manager.worktree_sessions.read().await.len(), 0);
    }

  ./crates/ccswarm/src/session/worktree_session.rs:130-141 method start <-> ./crates/ccswarm/src/session/worktree_session.rs:634-647 function test_combined_efficiency_stats
  Similarity: 80.74%

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:start (lines 130-141) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting worktree session manager");

        // Start the underlying persistent session manager
        self.persistent_manager.start().await?;

        // Initialize git repository if needed
        self.git_manager.init_repo_if_needed().await?;

        tracing::info!("Worktree session manager started successfully");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:test_combined_efficiency_stats (lines 634-647) ---[0m
    async fn test_combined_efficiency_stats() {
        let temp_dir = TempDir::new().unwrap();
        let config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let manager = WorktreeSessionManager::new(config).unwrap();
        let stats = manager.get_combined_efficiency_stats().await;

        assert_eq!(stats.total_worktrees, 0);
        assert_eq!(stats.active_worktrees, 0);
        assert_eq!(stats.idle_worktrees, 0);
    }

  ./crates/ccswarm/src/session/worktree_session.rs:113-127 method new <-> ./crates/ccswarm/src/session/worktree_session.rs:501-511 method get_worktree_sessions_by_role
  Similarity: 71.04%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:new (lines 113-127) ---[0m
    pub fn new(config: WorktreeSessionConfig) -> Result<Self> {
        let persistent_manager = PersistentSessionManager::new(
            config.repo_path.join("agents"),
            config.persistent_config.clone(),
        );

        let git_manager = ShellWorktreeManager::new(config.repo_path.clone())?;

        Ok(Self {
            persistent_manager,
            git_manager,
            worktree_sessions: Arc::new(RwLock::new(HashMap::new())),
            config,
        })
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_worktree_sessions_by_role (lines 501-511) ---[0m
    pub async fn get_worktree_sessions_by_role(
        &self,
        role: &AgentRole,
    ) -> Vec<WorktreeSessionInfo> {
        let sessions = self.worktree_sessions.read().await;
        sessions
            .values()
            .filter(|info| info.agent_role.name() == role.name())
            .cloned()
            .collect()
    }

  ./crates/ccswarm/src/session/worktree_session.rs:113-127 method new <-> ./crates/ccswarm/src/session/worktree_session.rs:622-631 function test_worktree_session_manager_creation
  Similarity: 73.74%

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:new (lines 113-127) ---[0m
    pub fn new(config: WorktreeSessionConfig) -> Result<Self> {
        let persistent_manager = PersistentSessionManager::new(
            config.repo_path.join("agents"),
            config.persistent_config.clone(),
        );

        let git_manager = ShellWorktreeManager::new(config.repo_path.clone())?;

        Ok(Self {
            persistent_manager,
            git_manager,
            worktree_sessions: Arc::new(RwLock::new(HashMap::new())),
            config,
        })
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:test_worktree_session_manager_creation (lines 622-631) ---[0m
    async fn test_worktree_session_manager_creation() {
        let temp_dir = TempDir::new().unwrap();
        let config = WorktreeSessionConfig {
            repo_path: temp_dir.path().to_path_buf(),
            ..Default::default()
        };

        let manager = WorktreeSessionManager::new(config).unwrap();
        assert_eq!(manager.worktree_sessions.read().await.len(), 0);
    }

  ./crates/ccswarm/src/session/worktree_session.rs:197-203 method count_sessions_by_role <-> ./crates/ccswarm/src/session/worktree_session.rs:501-511 method get_worktree_sessions_by_role
  Similarity: 92.13%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:count_sessions_by_role (lines 197-203) ---[0m
    async fn count_sessions_by_role(&self, role: &AgentRole) -> usize {
        let sessions = self.worktree_sessions.read().await;
        sessions
            .values()
            .filter(|info| info.agent_role.name() == role.name())
            .count()
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_worktree_sessions_by_role (lines 501-511) ---[0m
    pub async fn get_worktree_sessions_by_role(
        &self,
        role: &AgentRole,
    ) -> Vec<WorktreeSessionInfo> {
        let sessions = self.worktree_sessions.read().await;
        sessions
            .values()
            .filter(|info| info.agent_role.name() == role.name())
            .cloned()
            .collect()
    }

  ./crates/ccswarm/src/session/worktree_session.rs:477-483 method update_session_status <-> ./crates/ccswarm/src/session/worktree_session.rs:501-511 method get_worktree_sessions_by_role
  Similarity: 89.47%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:update_session_status (lines 477-483) ---[0m
    async fn update_session_status(&self, agent_id: &str, status: WorktreeSessionStatus) {
        let mut sessions = self.worktree_sessions.write().await;
        if let Some(info) = sessions.get_mut(agent_id) {
            info.status = status;
            info.last_activity = Utc::now();
        }
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_worktree_sessions_by_role (lines 501-511) ---[0m
    pub async fn get_worktree_sessions_by_role(
        &self,
        role: &AgentRole,
    ) -> Vec<WorktreeSessionInfo> {
        let sessions = self.worktree_sessions.read().await;
        sessions
            .values()
            .filter(|info| info.agent_role.name() == role.name())
            .cloned()
            .collect()
    }

  ./crates/ccswarm/src/session/worktree_session.rs:486-492 method update_session_activity <-> ./crates/ccswarm/src/session/worktree_session.rs:501-511 method get_worktree_sessions_by_role
  Similarity: 89.47%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:update_session_activity (lines 486-492) ---[0m
    async fn update_session_activity(&self, agent_id: &str) {
        let mut sessions = self.worktree_sessions.write().await;
        if let Some(info) = sessions.get_mut(agent_id) {
            info.last_activity = Utc::now();
            info.tasks_completed += 1;
        }
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_worktree_sessions_by_role (lines 501-511) ---[0m
    pub async fn get_worktree_sessions_by_role(
        &self,
        role: &AgentRole,
    ) -> Vec<WorktreeSessionInfo> {
        let sessions = self.worktree_sessions.read().await;
        sessions
            .values()
            .filter(|info| info.agent_role.name() == role.name())
            .cloned()
            .collect()
    }

  ./crates/ccswarm/src/session/worktree_session.rs:113-127 method new <-> ./crates/ccswarm/src/session/worktree_session.rs:197-203 method count_sessions_by_role
  Similarity: 71.04%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:new (lines 113-127) ---[0m
    pub fn new(config: WorktreeSessionConfig) -> Result<Self> {
        let persistent_manager = PersistentSessionManager::new(
            config.repo_path.join("agents"),
            config.persistent_config.clone(),
        );

        let git_manager = ShellWorktreeManager::new(config.repo_path.clone())?;

        Ok(Self {
            persistent_manager,
            git_manager,
            worktree_sessions: Arc::new(RwLock::new(HashMap::new())),
            config,
        })
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:count_sessions_by_role (lines 197-203) ---[0m
    async fn count_sessions_by_role(&self, role: &AgentRole) -> usize {
        let sessions = self.worktree_sessions.read().await;
        sessions
            .values()
            .filter(|info| info.agent_role.name() == role.name())
            .count()
    }

  ./crates/ccswarm/src/session/worktree_session.rs:83-92 method default <-> ./crates/ccswarm/src/session/worktree_session.rs:501-511 method get_worktree_sessions_by_role
  Similarity: 73.82%
  Classes: Default <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:default (lines 83-92) ---[0m
    fn default() -> Self {
        Self {
            persistent_config: PersistentSessionManagerConfig::default(),
            repo_path: std::env::current_dir().unwrap_or_else(|_| PathBuf::from(".")),
            branch_prefix: "feature".to_string(),
            auto_commit: false,
            cleanup_worktrees: true,
            max_worktrees_per_role: 3,
        }
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_worktree_sessions_by_role (lines 501-511) ---[0m
    pub async fn get_worktree_sessions_by_role(
        &self,
        role: &AgentRole,
    ) -> Vec<WorktreeSessionInfo> {
        let sessions = self.worktree_sessions.read().await;
        sessions
            .values()
            .filter(|info| info.agent_role.name() == role.name())
            .cloned()
            .collect()
    }

  ./crates/ccswarm/src/session/worktree_session.rs:495-498 method list_worktree_sessions <-> ./crates/ccswarm/src/session/worktree_session.rs:501-511 method get_worktree_sessions_by_role
  Similarity: 95.60%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:list_worktree_sessions (lines 495-498) ---[0m
    pub async fn list_worktree_sessions(&self) -> Vec<WorktreeSessionInfo> {
        let sessions = self.worktree_sessions.read().await;
        sessions.values().cloned().collect()
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_worktree_sessions_by_role (lines 501-511) ---[0m
    pub async fn get_worktree_sessions_by_role(
        &self,
        role: &AgentRole,
    ) -> Vec<WorktreeSessionInfo> {
        let sessions = self.worktree_sessions.read().await;
        sessions
            .values()
            .filter(|info| info.agent_role.name() == role.name())
            .cloned()
            .collect()
    }

  ./crates/ccswarm/src/session/worktree_session.rs:471-474 method update_session_info <-> ./crates/ccswarm/src/session/worktree_session.rs:501-511 method get_worktree_sessions_by_role
  Similarity: 89.07%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:update_session_info (lines 471-474) ---[0m
    async fn update_session_info(&self, agent_id: &str, info: WorktreeSessionInfo) {
        let mut sessions = self.worktree_sessions.write().await;
        sessions.insert(agent_id.to_string(), info);
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:get_worktree_sessions_by_role (lines 501-511) ---[0m
    pub async fn get_worktree_sessions_by_role(
        &self,
        role: &AgentRole,
    ) -> Vec<WorktreeSessionInfo> {
        let sessions = self.worktree_sessions.read().await;
        sessions
            .values()
            .filter(|info| info.agent_role.name() == role.name())
            .cloned()
            .collect()
    }

  ./crates/ccswarm/src/session/worktree_session.rs:477-483 method update_session_status <-> ./crates/ccswarm/src/session/worktree_session.rs:486-492 method update_session_activity
  Similarity: 95.36%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:update_session_status (lines 477-483) ---[0m
    async fn update_session_status(&self, agent_id: &str, status: WorktreeSessionStatus) {
        let mut sessions = self.worktree_sessions.write().await;
        if let Some(info) = sessions.get_mut(agent_id) {
            info.status = status;
            info.last_activity = Utc::now();
        }
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:update_session_activity (lines 486-492) ---[0m
    async fn update_session_activity(&self, agent_id: &str) {
        let mut sessions = self.worktree_sessions.write().await;
        if let Some(info) = sessions.get_mut(agent_id) {
            info.last_activity = Utc::now();
            info.tasks_completed += 1;
        }
    }

  ./crates/ccswarm/src/session/worktree_session.rs:83-92 method default <-> ./crates/ccswarm/src/session/worktree_session.rs:197-203 method count_sessions_by_role
  Similarity: 73.82%
  Classes: Default <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:default (lines 83-92) ---[0m
    fn default() -> Self {
        Self {
            persistent_config: PersistentSessionManagerConfig::default(),
            repo_path: std::env::current_dir().unwrap_or_else(|_| PathBuf::from(".")),
            branch_prefix: "feature".to_string(),
            auto_commit: false,
            cleanup_worktrees: true,
            max_worktrees_per_role: 3,
        }
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:count_sessions_by_role (lines 197-203) ---[0m
    async fn count_sessions_by_role(&self, role: &AgentRole) -> usize {
        let sessions = self.worktree_sessions.read().await;
        sessions
            .values()
            .filter(|info| info.agent_role.name() == role.name())
            .count()
    }

  ./crates/ccswarm/src/session/worktree_session.rs:197-203 method count_sessions_by_role <-> ./crates/ccswarm/src/session/worktree_session.rs:477-483 method update_session_status
  Similarity: 88.55%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:count_sessions_by_role (lines 197-203) ---[0m
    async fn count_sessions_by_role(&self, role: &AgentRole) -> usize {
        let sessions = self.worktree_sessions.read().await;
        sessions
            .values()
            .filter(|info| info.agent_role.name() == role.name())
            .count()
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:update_session_status (lines 477-483) ---[0m
    async fn update_session_status(&self, agent_id: &str, status: WorktreeSessionStatus) {
        let mut sessions = self.worktree_sessions.write().await;
        if let Some(info) = sessions.get_mut(agent_id) {
            info.status = status;
            info.last_activity = Utc::now();
        }
    }

  ./crates/ccswarm/src/session/worktree_session.rs:197-203 method count_sessions_by_role <-> ./crates/ccswarm/src/session/worktree_session.rs:486-492 method update_session_activity
  Similarity: 88.55%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:count_sessions_by_role (lines 197-203) ---[0m
    async fn count_sessions_by_role(&self, role: &AgentRole) -> usize {
        let sessions = self.worktree_sessions.read().await;
        sessions
            .values()
            .filter(|info| info.agent_role.name() == role.name())
            .count()
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:update_session_activity (lines 486-492) ---[0m
    async fn update_session_activity(&self, agent_id: &str) {
        let mut sessions = self.worktree_sessions.write().await;
        if let Some(info) = sessions.get_mut(agent_id) {
            info.last_activity = Utc::now();
            info.tasks_completed += 1;
        }
    }

  ./crates/ccswarm/src/session/worktree_session.rs:197-203 method count_sessions_by_role <-> ./crates/ccswarm/src/session/worktree_session.rs:495-498 method list_worktree_sessions
  Similarity: 96.18%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:count_sessions_by_role (lines 197-203) ---[0m
    async fn count_sessions_by_role(&self, role: &AgentRole) -> usize {
        let sessions = self.worktree_sessions.read().await;
        sessions
            .values()
            .filter(|info| info.agent_role.name() == role.name())
            .count()
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:list_worktree_sessions (lines 495-498) ---[0m
    pub async fn list_worktree_sessions(&self) -> Vec<WorktreeSessionInfo> {
        let sessions = self.worktree_sessions.read().await;
        sessions.values().cloned().collect()
    }

  ./crates/ccswarm/src/session/worktree_session.rs:477-483 method update_session_status <-> ./crates/ccswarm/src/session/worktree_session.rs:495-498 method list_worktree_sessions
  Similarity: 88.55%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:update_session_status (lines 477-483) ---[0m
    async fn update_session_status(&self, agent_id: &str, status: WorktreeSessionStatus) {
        let mut sessions = self.worktree_sessions.write().await;
        if let Some(info) = sessions.get_mut(agent_id) {
            info.status = status;
            info.last_activity = Utc::now();
        }
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:list_worktree_sessions (lines 495-498) ---[0m
    pub async fn list_worktree_sessions(&self) -> Vec<WorktreeSessionInfo> {
        let sessions = self.worktree_sessions.read().await;
        sessions.values().cloned().collect()
    }

  ./crates/ccswarm/src/session/worktree_session.rs:486-492 method update_session_activity <-> ./crates/ccswarm/src/session/worktree_session.rs:495-498 method list_worktree_sessions
  Similarity: 88.55%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:update_session_activity (lines 486-492) ---[0m
    async fn update_session_activity(&self, agent_id: &str) {
        let mut sessions = self.worktree_sessions.write().await;
        if let Some(info) = sessions.get_mut(agent_id) {
            info.last_activity = Utc::now();
            info.tasks_completed += 1;
        }
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:list_worktree_sessions (lines 495-498) ---[0m
    pub async fn list_worktree_sessions(&self) -> Vec<WorktreeSessionInfo> {
        let sessions = self.worktree_sessions.read().await;
        sessions.values().cloned().collect()
    }

  ./crates/ccswarm/src/session/worktree_session.rs:197-203 method count_sessions_by_role <-> ./crates/ccswarm/src/session/worktree_session.rs:471-474 method update_session_info
  Similarity: 87.94%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:count_sessions_by_role (lines 197-203) ---[0m
    async fn count_sessions_by_role(&self, role: &AgentRole) -> usize {
        let sessions = self.worktree_sessions.read().await;
        sessions
            .values()
            .filter(|info| info.agent_role.name() == role.name())
            .count()
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:update_session_info (lines 471-474) ---[0m
    async fn update_session_info(&self, agent_id: &str, info: WorktreeSessionInfo) {
        let mut sessions = self.worktree_sessions.write().await;
        sessions.insert(agent_id.to_string(), info);
    }

  ./crates/ccswarm/src/session/worktree_session.rs:471-474 method update_session_info <-> ./crates/ccswarm/src/session/worktree_session.rs:477-483 method update_session_status
  Similarity: 84.78%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:update_session_info (lines 471-474) ---[0m
    async fn update_session_info(&self, agent_id: &str, info: WorktreeSessionInfo) {
        let mut sessions = self.worktree_sessions.write().await;
        sessions.insert(agent_id.to_string(), info);
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:update_session_status (lines 477-483) ---[0m
    async fn update_session_status(&self, agent_id: &str, status: WorktreeSessionStatus) {
        let mut sessions = self.worktree_sessions.write().await;
        if let Some(info) = sessions.get_mut(agent_id) {
            info.status = status;
            info.last_activity = Utc::now();
        }
    }

  ./crates/ccswarm/src/session/worktree_session.rs:471-474 method update_session_info <-> ./crates/ccswarm/src/session/worktree_session.rs:486-492 method update_session_activity
  Similarity: 84.78%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:update_session_info (lines 471-474) ---[0m
    async fn update_session_info(&self, agent_id: &str, info: WorktreeSessionInfo) {
        let mut sessions = self.worktree_sessions.write().await;
        sessions.insert(agent_id.to_string(), info);
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:update_session_activity (lines 486-492) ---[0m
    async fn update_session_activity(&self, agent_id: &str) {
        let mut sessions = self.worktree_sessions.write().await;
        if let Some(info) = sessions.get_mut(agent_id) {
            info.last_activity = Utc::now();
            info.tasks_completed += 1;
        }
    }

  ./crates/ccswarm/src/session/worktree_session.rs:471-474 method update_session_info <-> ./crates/ccswarm/src/session/worktree_session.rs:495-498 method list_worktree_sessions
  Similarity: 80.48%
  Classes: WorktreeSessionManager <-> WorktreeSessionManager

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:update_session_info (lines 471-474) ---[0m
    async fn update_session_info(&self, agent_id: &str, info: WorktreeSessionInfo) {
        let mut sessions = self.worktree_sessions.write().await;
        sessions.insert(agent_id.to_string(), info);
    }

[36m--- ./crates/ccswarm/src/session/worktree_session.rs:list_worktree_sessions (lines 495-498) ---[0m
    pub async fn list_worktree_sessions(&self) -> Vec<WorktreeSessionInfo> {
        let sessions = self.worktree_sessions.read().await;
        sessions.values().cloned().collect()
    }


Duplicates in ./crates/ccswarm/src/git/shell.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/git/shell.rs:143-196 method create_worktree_full <-> ./crates/ccswarm/src/git/shell.rs:232-289 method commit_worktree_changes
  Similarity: 85.71%
  Classes: ShellWorktreeManager <-> ShellWorktreeManager

[36m--- ./crates/ccswarm/src/git/shell.rs:create_worktree_full (lines 143-196) ---[0m
    pub async fn create_worktree_full(
        &self,
        worktree_path: &Path,
        branch_name: &str,
        create_new_branch: bool,
    ) -> Result<ShellWorktreeInfo> {
        // ブランチが存在するかチェック
        let branch_exists = self.branch_exists(branch_name).await?;

        let mut args = vec!["worktree", "add"];

        if create_new_branch || !branch_exists {
            args.extend(["-b", branch_name]);
        }

        args.push(worktree_path.to_str().unwrap());

        if !create_new_branch && branch_exists {
            args.push(branch_name);
        }

        let output = Command::new("git")
            .args(&args)
            .current_dir(&self.repo_path)
            .output()
            .await
            .context("Failed to execute git worktree add")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to create worktree: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        // ワークツリー情報を取得
        let head_commit = self.get_head_commit(worktree_path).await?;
        let is_locked = self.is_worktree_locked(worktree_path).await?;

        let info = ShellWorktreeInfo {
            path: worktree_path.to_path_buf(),
            branch: branch_name.to_string(),
            head_commit,
            is_locked,
            is_bare: false,
        };

        info!(
            "Created worktree: {} on branch {}",
            worktree_path.display(),
            branch_name
        );
        Ok(info)
    }

[36m--- ./crates/ccswarm/src/git/shell.rs:commit_worktree_changes (lines 232-289) ---[0m
    pub async fn commit_worktree_changes(&self, worktree_path: &Path, message: &str) -> Result<()> {
        // まず変更があるかチェック
        let status_output = Command::new("git")
            .args(["status", "--porcelain"])
            .current_dir(worktree_path)
            .output()
            .await
            .context("Failed to check git status")?;

        if !status_output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to check git status: {}",
                String::from_utf8_lossy(&status_output.stderr)
            ));
        }

        // 変更がない場合は何もしない
        if status_output.stdout.is_empty() {
            info!(
                "No changes to commit in worktree: {}",
                worktree_path.display()
            );
            return Ok(());
        }

        // 変更をステージング
        let add_output = Command::new("git")
            .args(["add", "."])
            .current_dir(worktree_path)
            .output()
            .await
            .context("Failed to stage changes")?;

        if !add_output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to stage changes: {}",
                String::from_utf8_lossy(&add_output.stderr)
            ));
        }

        // コミット
        let commit_output = Command::new("git")
            .args(["commit", "-m", message])
            .current_dir(worktree_path)
            .output()
            .await
            .context("Failed to commit changes")?;

        if !commit_output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to commit changes: {}",
                String::from_utf8_lossy(&commit_output.stderr)
            ));
        }

        info!("Committed changes in worktree: {}", worktree_path.display());
        Ok(())
    }

  ./crates/ccswarm/src/git/shell.rs:39-85 method init_if_needed <-> ./crates/ccswarm/src/git/shell.rs:325-367 method parse_worktree_list
  Similarity: 83.48%
  Classes: ShellWorktreeManager <-> ShellWorktreeManager

[36m--- ./crates/ccswarm/src/git/shell.rs:init_if_needed (lines 39-85) ---[0m
    pub async fn init_if_needed(path: &Path) -> Result<()> {
        if !path.join(".git").exists() {
            info!("Initializing new Git repository at: {}", path.display());

            let output = Command::new("git")
                .args(["init"])
                .current_dir(path)
                .output()
                .await
                .context("Failed to execute git init")?;

            if !output.status.success() {
                return Err(anyhow::anyhow!(
                    "Failed to initialize git repository: {}",
                    String::from_utf8_lossy(&output.stderr)
                ));
            }

            // 初期コミットを作成
            let readme_content =
                "# ccswarm Project\n\nThis repository is managed by ccswarm multi-agent system.\n";
            tokio::fs::write(path.join("README.md"), readme_content).await?;

            // git add . && git commit
            let add_output = Command::new("git")
                .args(["add", "."])
                .current_dir(path)
                .output()
                .await?;

            if add_output.status.success() {
                let commit_output = Command::new("git")
                    .args(["commit", "-m", "Initial commit by ccswarm"])
                    .current_dir(path)
                    .output()
                    .await?;

                if !commit_output.status.success() {
                    warn!("Failed to create initial commit, but continuing...");
                }
            }

            info!("Git repository initialized successfully");
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/git/shell.rs:parse_worktree_list (lines 325-367) ---[0m
    async fn parse_worktree_list(&self, output: &str) -> Result<Vec<ShellWorktreeInfo>> {
        let mut worktrees = Vec::new();
        let mut current_worktree: Option<ShellWorktreeInfo> = None;

        for line in output.lines() {
            if line.starts_with("worktree ") {
                // 前のワークツリーを保存
                if let Some(wt) = current_worktree.take() {
                    worktrees.push(wt);
                }

                // 新しいワークツリーを開始
                let path = PathBuf::from(line.strip_prefix("worktree ").unwrap());
                current_worktree = Some(ShellWorktreeInfo {
                    path,
                    branch: String::new(),
                    head_commit: String::new(),
                    is_locked: false,
                    is_bare: false,
                });
            } else if let Some(ref mut wt) = current_worktree {
                if line.starts_with("HEAD ") {
                    wt.head_commit = line.strip_prefix("HEAD ").unwrap().to_string();
                } else if line.starts_with("branch ") {
                    wt.branch = line
                        .strip_prefix("branch refs/heads/")
                        .unwrap_or(line.strip_prefix("branch ").unwrap())
                        .to_string();
                } else if line == "bare" {
                    wt.is_bare = true;
                } else if line == "locked" {
                    wt.is_locked = true;
                }
            }
        }

        // 最後のワークツリーを追加
        if let Some(wt) = current_worktree {
            worktrees.push(wt);
        }

        Ok(worktrees)
    }

  ./crates/ccswarm/src/git/shell.rs:232-289 method commit_worktree_changes <-> ./crates/ccswarm/src/git/shell.rs:325-367 method parse_worktree_list
  Similarity: 71.24%
  Classes: ShellWorktreeManager <-> ShellWorktreeManager

[36m--- ./crates/ccswarm/src/git/shell.rs:commit_worktree_changes (lines 232-289) ---[0m
    pub async fn commit_worktree_changes(&self, worktree_path: &Path, message: &str) -> Result<()> {
        // まず変更があるかチェック
        let status_output = Command::new("git")
            .args(["status", "--porcelain"])
            .current_dir(worktree_path)
            .output()
            .await
            .context("Failed to check git status")?;

        if !status_output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to check git status: {}",
                String::from_utf8_lossy(&status_output.stderr)
            ));
        }

        // 変更がない場合は何もしない
        if status_output.stdout.is_empty() {
            info!(
                "No changes to commit in worktree: {}",
                worktree_path.display()
            );
            return Ok(());
        }

        // 変更をステージング
        let add_output = Command::new("git")
            .args(["add", "."])
            .current_dir(worktree_path)
            .output()
            .await
            .context("Failed to stage changes")?;

        if !add_output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to stage changes: {}",
                String::from_utf8_lossy(&add_output.stderr)
            ));
        }

        // コミット
        let commit_output = Command::new("git")
            .args(["commit", "-m", message])
            .current_dir(worktree_path)
            .output()
            .await
            .context("Failed to commit changes")?;

        if !commit_output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to commit changes: {}",
                String::from_utf8_lossy(&commit_output.stderr)
            ));
        }

        info!("Committed changes in worktree: {}", worktree_path.display());
        Ok(())
    }

[36m--- ./crates/ccswarm/src/git/shell.rs:parse_worktree_list (lines 325-367) ---[0m
    async fn parse_worktree_list(&self, output: &str) -> Result<Vec<ShellWorktreeInfo>> {
        let mut worktrees = Vec::new();
        let mut current_worktree: Option<ShellWorktreeInfo> = None;

        for line in output.lines() {
            if line.starts_with("worktree ") {
                // 前のワークツリーを保存
                if let Some(wt) = current_worktree.take() {
                    worktrees.push(wt);
                }

                // 新しいワークツリーを開始
                let path = PathBuf::from(line.strip_prefix("worktree ").unwrap());
                current_worktree = Some(ShellWorktreeInfo {
                    path,
                    branch: String::new(),
                    head_commit: String::new(),
                    is_locked: false,
                    is_bare: false,
                });
            } else if let Some(ref mut wt) = current_worktree {
                if line.starts_with("HEAD ") {
                    wt.head_commit = line.strip_prefix("HEAD ").unwrap().to_string();
                } else if line.starts_with("branch ") {
                    wt.branch = line
                        .strip_prefix("branch refs/heads/")
                        .unwrap_or(line.strip_prefix("branch ").unwrap())
                        .to_string();
                } else if line == "bare" {
                    wt.is_bare = true;
                } else if line == "locked" {
                    wt.is_locked = true;
                }
            }
        }

        // 最後のワークツリーを追加
        if let Some(wt) = current_worktree {
            worktrees.push(wt);
        }

        Ok(worktrees)
    }

  ./crates/ccswarm/src/git/shell.rs:204-229 method remove_worktree_full <-> ./crates/ccswarm/src/git/shell.rs:325-367 method parse_worktree_list
  Similarity: 88.36%
  Classes: ShellWorktreeManager <-> ShellWorktreeManager

[36m--- ./crates/ccswarm/src/git/shell.rs:remove_worktree_full (lines 204-229) ---[0m
    pub async fn remove_worktree_full(&self, worktree_path: &Path, force: bool) -> Result<()> {
        let mut args = vec!["worktree", "remove"];

        if force {
            args.push("--force");
        }

        args.push(worktree_path.to_str().unwrap());

        let output = Command::new("git")
            .args(&args)
            .current_dir(&self.repo_path)
            .output()
            .await
            .context("Failed to execute git worktree remove")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to remove worktree: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        info!("Removed worktree: {}", worktree_path.display());
        Ok(())
    }

[36m--- ./crates/ccswarm/src/git/shell.rs:parse_worktree_list (lines 325-367) ---[0m
    async fn parse_worktree_list(&self, output: &str) -> Result<Vec<ShellWorktreeInfo>> {
        let mut worktrees = Vec::new();
        let mut current_worktree: Option<ShellWorktreeInfo> = None;

        for line in output.lines() {
            if line.starts_with("worktree ") {
                // 前のワークツリーを保存
                if let Some(wt) = current_worktree.take() {
                    worktrees.push(wt);
                }

                // 新しいワークツリーを開始
                let path = PathBuf::from(line.strip_prefix("worktree ").unwrap());
                current_worktree = Some(ShellWorktreeInfo {
                    path,
                    branch: String::new(),
                    head_commit: String::new(),
                    is_locked: false,
                    is_bare: false,
                });
            } else if let Some(ref mut wt) = current_worktree {
                if line.starts_with("HEAD ") {
                    wt.head_commit = line.strip_prefix("HEAD ").unwrap().to_string();
                } else if line.starts_with("branch ") {
                    wt.branch = line
                        .strip_prefix("branch refs/heads/")
                        .unwrap_or(line.strip_prefix("branch ").unwrap())
                        .to_string();
                } else if line == "bare" {
                    wt.is_bare = true;
                } else if line == "locked" {
                    wt.is_locked = true;
                }
            }
        }

        // 最後のワークツリーを追加
        if let Some(wt) = current_worktree {
            worktrees.push(wt);
        }

        Ok(worktrees)
    }

  ./crates/ccswarm/src/git/shell.rs:143-196 method create_worktree_full <-> ./crates/ccswarm/src/git/shell.rs:204-229 method remove_worktree_full
  Similarity: 70.43%
  Classes: ShellWorktreeManager <-> ShellWorktreeManager

[36m--- ./crates/ccswarm/src/git/shell.rs:create_worktree_full (lines 143-196) ---[0m
    pub async fn create_worktree_full(
        &self,
        worktree_path: &Path,
        branch_name: &str,
        create_new_branch: bool,
    ) -> Result<ShellWorktreeInfo> {
        // ブランチが存在するかチェック
        let branch_exists = self.branch_exists(branch_name).await?;

        let mut args = vec!["worktree", "add"];

        if create_new_branch || !branch_exists {
            args.extend(["-b", branch_name]);
        }

        args.push(worktree_path.to_str().unwrap());

        if !create_new_branch && branch_exists {
            args.push(branch_name);
        }

        let output = Command::new("git")
            .args(&args)
            .current_dir(&self.repo_path)
            .output()
            .await
            .context("Failed to execute git worktree add")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to create worktree: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        // ワークツリー情報を取得
        let head_commit = self.get_head_commit(worktree_path).await?;
        let is_locked = self.is_worktree_locked(worktree_path).await?;

        let info = ShellWorktreeInfo {
            path: worktree_path.to_path_buf(),
            branch: branch_name.to_string(),
            head_commit,
            is_locked,
            is_bare: false,
        };

        info!(
            "Created worktree: {} on branch {}",
            worktree_path.display(),
            branch_name
        );
        Ok(info)
    }

[36m--- ./crates/ccswarm/src/git/shell.rs:remove_worktree_full (lines 204-229) ---[0m
    pub async fn remove_worktree_full(&self, worktree_path: &Path, force: bool) -> Result<()> {
        let mut args = vec!["worktree", "remove"];

        if force {
            args.push("--force");
        }

        args.push(worktree_path.to_str().unwrap());

        let output = Command::new("git")
            .args(&args)
            .current_dir(&self.repo_path)
            .output()
            .await
            .context("Failed to execute git worktree remove")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to remove worktree: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        info!("Removed worktree: {}", worktree_path.display());
        Ok(())
    }

  ./crates/ccswarm/src/git/shell.rs:39-85 method init_if_needed <-> ./crates/ccswarm/src/git/shell.rs:204-229 method remove_worktree_full
  Similarity: 71.71%
  Classes: ShellWorktreeManager <-> ShellWorktreeManager

[36m--- ./crates/ccswarm/src/git/shell.rs:init_if_needed (lines 39-85) ---[0m
    pub async fn init_if_needed(path: &Path) -> Result<()> {
        if !path.join(".git").exists() {
            info!("Initializing new Git repository at: {}", path.display());

            let output = Command::new("git")
                .args(["init"])
                .current_dir(path)
                .output()
                .await
                .context("Failed to execute git init")?;

            if !output.status.success() {
                return Err(anyhow::anyhow!(
                    "Failed to initialize git repository: {}",
                    String::from_utf8_lossy(&output.stderr)
                ));
            }

            // 初期コミットを作成
            let readme_content =
                "# ccswarm Project\n\nThis repository is managed by ccswarm multi-agent system.\n";
            tokio::fs::write(path.join("README.md"), readme_content).await?;

            // git add . && git commit
            let add_output = Command::new("git")
                .args(["add", "."])
                .current_dir(path)
                .output()
                .await?;

            if add_output.status.success() {
                let commit_output = Command::new("git")
                    .args(["commit", "-m", "Initial commit by ccswarm"])
                    .current_dir(path)
                    .output()
                    .await?;

                if !commit_output.status.success() {
                    warn!("Failed to create initial commit, but continuing...");
                }
            }

            info!("Git repository initialized successfully");
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/git/shell.rs:remove_worktree_full (lines 204-229) ---[0m
    pub async fn remove_worktree_full(&self, worktree_path: &Path, force: bool) -> Result<()> {
        let mut args = vec!["worktree", "remove"];

        if force {
            args.push("--force");
        }

        args.push(worktree_path.to_str().unwrap());

        let output = Command::new("git")
            .args(&args)
            .current_dir(&self.repo_path)
            .output()
            .await
            .context("Failed to execute git worktree remove")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to remove worktree: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        info!("Removed worktree: {}", worktree_path.display());
        Ok(())
    }

  ./crates/ccswarm/src/git/shell.rs:88-105 method list_worktrees <-> ./crates/ccswarm/src/git/shell.rs:108-125 method prune_worktrees
  Similarity: 92.33%
  Classes: ShellWorktreeManager <-> ShellWorktreeManager

[36m--- ./crates/ccswarm/src/git/shell.rs:list_worktrees (lines 88-105) ---[0m
    pub async fn list_worktrees(&self) -> Result<Vec<ShellWorktreeInfo>> {
        let output = Command::new("git")
            .args(["worktree", "list", "--porcelain"])
            .current_dir(&self.repo_path)
            .output()
            .await
            .context("Failed to execute git worktree list")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to list worktrees: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        let output_str = String::from_utf8_lossy(&output.stdout);
        self.parse_worktree_list(&output_str).await
    }

[36m--- ./crates/ccswarm/src/git/shell.rs:prune_worktrees (lines 108-125) ---[0m
    pub async fn prune_worktrees(&self) -> Result<()> {
        let output = Command::new("git")
            .args(["worktree", "prune"])
            .current_dir(&self.repo_path)
            .output()
            .await
            .context("Failed to execute git worktree prune")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to prune worktrees: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        info!("Pruned stale worktrees");
        Ok(())
    }

  ./crates/ccswarm/src/git/shell.rs:204-229 method remove_worktree_full <-> ./crates/ccswarm/src/git/shell.rs:389-408 function test_worktree_creation
  Similarity: 72.19%

[36m--- ./crates/ccswarm/src/git/shell.rs:remove_worktree_full (lines 204-229) ---[0m
    pub async fn remove_worktree_full(&self, worktree_path: &Path, force: bool) -> Result<()> {
        let mut args = vec!["worktree", "remove"];

        if force {
            args.push("--force");
        }

        args.push(worktree_path.to_str().unwrap());

        let output = Command::new("git")
            .args(&args)
            .current_dir(&self.repo_path)
            .output()
            .await
            .context("Failed to execute git worktree remove")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to remove worktree: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        info!("Removed worktree: {}", worktree_path.display());
        Ok(())
    }

[36m--- ./crates/ccswarm/src/git/shell.rs:test_worktree_creation (lines 389-408) ---[0m
    async fn test_worktree_creation() {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // リポジトリを初期化
        ShellWorktreeManager::init_if_needed(&repo_path)
            .await
            .unwrap();

        let manager = ShellWorktreeManager::new(repo_path).unwrap();
        let worktree_path = temp_dir.path().join("test-worktree");

        let info = manager
            .create_worktree(&worktree_path, "test-branch")
            .await
            .unwrap();

        assert_eq!(info.branch, "test-branch");
        assert!(info.path.exists());
    }

  ./crates/ccswarm/src/git/shell.rs:204-229 method remove_worktree_full <-> ./crates/ccswarm/src/git/shell.rs:376-386 function test_git_initialization
  Similarity: 71.95%

[36m--- ./crates/ccswarm/src/git/shell.rs:remove_worktree_full (lines 204-229) ---[0m
    pub async fn remove_worktree_full(&self, worktree_path: &Path, force: bool) -> Result<()> {
        let mut args = vec!["worktree", "remove"];

        if force {
            args.push("--force");
        }

        args.push(worktree_path.to_str().unwrap());

        let output = Command::new("git")
            .args(&args)
            .current_dir(&self.repo_path)
            .output()
            .await
            .context("Failed to execute git worktree remove")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to remove worktree: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        info!("Removed worktree: {}", worktree_path.display());
        Ok(())
    }

[36m--- ./crates/ccswarm/src/git/shell.rs:test_git_initialization (lines 376-386) ---[0m
    async fn test_git_initialization() {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        ShellWorktreeManager::init_if_needed(&repo_path)
            .await
            .unwrap();

        assert!(repo_path.join(".git").exists());
        assert!(repo_path.join("README.md").exists());
    }

  ./crates/ccswarm/src/git/shell.rs:108-125 method prune_worktrees <-> ./crates/ccswarm/src/git/shell.rs:303-315 method get_head_commit
  Similarity: 77.52%
  Classes: ShellWorktreeManager <-> ShellWorktreeManager

[36m--- ./crates/ccswarm/src/git/shell.rs:prune_worktrees (lines 108-125) ---[0m
    pub async fn prune_worktrees(&self) -> Result<()> {
        let output = Command::new("git")
            .args(["worktree", "prune"])
            .current_dir(&self.repo_path)
            .output()
            .await
            .context("Failed to execute git worktree prune")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to prune worktrees: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        info!("Pruned stale worktrees");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/git/shell.rs:get_head_commit (lines 303-315) ---[0m
    async fn get_head_commit(&self, worktree_path: &Path) -> Result<String> {
        let output = Command::new("git")
            .args(["rev-parse", "HEAD"])
            .current_dir(worktree_path)
            .output()
            .await?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).trim().to_string())
        } else {
            Ok("unknown".to_string())
        }
    }

  ./crates/ccswarm/src/git/shell.rs:88-105 method list_worktrees <-> ./crates/ccswarm/src/git/shell.rs:303-315 method get_head_commit
  Similarity: 72.02%
  Classes: ShellWorktreeManager <-> ShellWorktreeManager

[36m--- ./crates/ccswarm/src/git/shell.rs:list_worktrees (lines 88-105) ---[0m
    pub async fn list_worktrees(&self) -> Result<Vec<ShellWorktreeInfo>> {
        let output = Command::new("git")
            .args(["worktree", "list", "--porcelain"])
            .current_dir(&self.repo_path)
            .output()
            .await
            .context("Failed to execute git worktree list")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to list worktrees: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        let output_str = String::from_utf8_lossy(&output.stdout);
        self.parse_worktree_list(&output_str).await
    }

[36m--- ./crates/ccswarm/src/git/shell.rs:get_head_commit (lines 303-315) ---[0m
    async fn get_head_commit(&self, worktree_path: &Path) -> Result<String> {
        let output = Command::new("git")
            .args(["rev-parse", "HEAD"])
            .current_dir(worktree_path)
            .output()
            .await?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).trim().to_string())
        } else {
            Ok("unknown".to_string())
        }
    }

  ./crates/ccswarm/src/git/shell.rs:108-125 method prune_worktrees <-> ./crates/ccswarm/src/git/shell.rs:292-300 method branch_exists
  Similarity: 78.00%
  Classes: ShellWorktreeManager <-> ShellWorktreeManager

[36m--- ./crates/ccswarm/src/git/shell.rs:prune_worktrees (lines 108-125) ---[0m
    pub async fn prune_worktrees(&self) -> Result<()> {
        let output = Command::new("git")
            .args(["worktree", "prune"])
            .current_dir(&self.repo_path)
            .output()
            .await
            .context("Failed to execute git worktree prune")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to prune worktrees: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        info!("Pruned stale worktrees");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/git/shell.rs:branch_exists (lines 292-300) ---[0m
    async fn branch_exists(&self, branch_name: &str) -> Result<bool> {
        let output = Command::new("git")
            .args(["branch", "--list", branch_name])
            .current_dir(&self.repo_path)
            .output()
            .await?;

        Ok(output.status.success() && !output.stdout.is_empty())
    }

  ./crates/ccswarm/src/git/shell.rs:108-125 method prune_worktrees <-> ./crates/ccswarm/src/git/shell.rs:376-386 function test_git_initialization
  Similarity: 70.97%

[36m--- ./crates/ccswarm/src/git/shell.rs:prune_worktrees (lines 108-125) ---[0m
    pub async fn prune_worktrees(&self) -> Result<()> {
        let output = Command::new("git")
            .args(["worktree", "prune"])
            .current_dir(&self.repo_path)
            .output()
            .await
            .context("Failed to execute git worktree prune")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to prune worktrees: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        info!("Pruned stale worktrees");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/git/shell.rs:test_git_initialization (lines 376-386) ---[0m
    async fn test_git_initialization() {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        ShellWorktreeManager::init_if_needed(&repo_path)
            .await
            .unwrap();

        assert!(repo_path.join(".git").exists());
        assert!(repo_path.join("README.md").exists());
    }

  ./crates/ccswarm/src/git/shell.rs:88-105 method list_worktrees <-> ./crates/ccswarm/src/git/shell.rs:376-386 function test_git_initialization
  Similarity: 70.74%

[36m--- ./crates/ccswarm/src/git/shell.rs:list_worktrees (lines 88-105) ---[0m
    pub async fn list_worktrees(&self) -> Result<Vec<ShellWorktreeInfo>> {
        let output = Command::new("git")
            .args(["worktree", "list", "--porcelain"])
            .current_dir(&self.repo_path)
            .output()
            .await
            .context("Failed to execute git worktree list")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to list worktrees: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        let output_str = String::from_utf8_lossy(&output.stdout);
        self.parse_worktree_list(&output_str).await
    }

[36m--- ./crates/ccswarm/src/git/shell.rs:test_git_initialization (lines 376-386) ---[0m
    async fn test_git_initialization() {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        ShellWorktreeManager::init_if_needed(&repo_path)
            .await
            .unwrap();

        assert!(repo_path.join(".git").exists());
        assert!(repo_path.join("README.md").exists());
    }

  ./crates/ccswarm/src/git/shell.rs:292-300 method branch_exists <-> ./crates/ccswarm/src/git/shell.rs:303-315 method get_head_commit
  Similarity: 91.02%
  Classes: ShellWorktreeManager <-> ShellWorktreeManager

[36m--- ./crates/ccswarm/src/git/shell.rs:branch_exists (lines 292-300) ---[0m
    async fn branch_exists(&self, branch_name: &str) -> Result<bool> {
        let output = Command::new("git")
            .args(["branch", "--list", branch_name])
            .current_dir(&self.repo_path)
            .output()
            .await?;

        Ok(output.status.success() && !output.stdout.is_empty())
    }

[36m--- ./crates/ccswarm/src/git/shell.rs:get_head_commit (lines 303-315) ---[0m
    async fn get_head_commit(&self, worktree_path: &Path) -> Result<String> {
        let output = Command::new("git")
            .args(["rev-parse", "HEAD"])
            .current_dir(worktree_path)
            .output()
            .await?;

        if output.status.success() {
            Ok(String::from_utf8_lossy(&output.stdout).trim().to_string())
        } else {
            Ok("unknown".to_string())
        }
    }

  ./crates/ccswarm/src/git/shell.rs:88-105 method list_worktrees <-> ./crates/ccswarm/src/git/shell.rs:292-300 method branch_exists
  Similarity: 72.45%
  Classes: ShellWorktreeManager <-> ShellWorktreeManager

[36m--- ./crates/ccswarm/src/git/shell.rs:list_worktrees (lines 88-105) ---[0m
    pub async fn list_worktrees(&self) -> Result<Vec<ShellWorktreeInfo>> {
        let output = Command::new("git")
            .args(["worktree", "list", "--porcelain"])
            .current_dir(&self.repo_path)
            .output()
            .await
            .context("Failed to execute git worktree list")?;

        if !output.status.success() {
            return Err(anyhow::anyhow!(
                "Failed to list worktrees: {}",
                String::from_utf8_lossy(&output.stderr)
            ));
        }

        let output_str = String::from_utf8_lossy(&output.stdout);
        self.parse_worktree_list(&output_str).await
    }

[36m--- ./crates/ccswarm/src/git/shell.rs:branch_exists (lines 292-300) ---[0m
    async fn branch_exists(&self, branch_name: &str) -> Result<bool> {
        let output = Command::new("git")
            .args(["branch", "--list", branch_name])
            .current_dir(&self.repo_path)
            .output()
            .await?;

        Ok(output.status.success() && !output.stdout.is_empty())
    }


Duplicates in ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:15-100 function create_comprehensive_test_config <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:200-258 function test_assistance_request_handling
  Similarity: 92.41%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:create_comprehensive_test_config (lines 15-100) ---[0m
    async fn create_comprehensive_test_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Initialize git repo
        WorktreeManager::init_if_needed(&repo_path).await.unwrap();

        let mut agents = HashMap::new();

        // Frontend agent
        agents.insert(
            "frontend".to_string(),
            AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend".to_string(),
                branch: "feature/frontend".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        // Backend agent
        agents.insert(
            "backend".to_string(),
            AgentConfig {
                specialization: "backend".to_string(),
                worktree: "agents/backend".to_string(),
                branch: "feature/backend".to_string(),
                claude_config: ClaudeConfig::for_agent("backend"),
                claude_md_template: "backend_specialist".to_string(),
            },
        );

        // DevOps agent
        agents.insert(
            "devops".to_string(),
            AgentConfig {
                specialization: "devops".to_string(),
                worktree: "agents/devops".to_string(),
                branch: "feature/devops".to_string(),
                claude_config: ClaudeConfig::for_agent("devops"),
                claude_md_template: "devops_specialist".to_string(),
            },
        );

        // QA agent
        agents.insert(
            "qa".to_string(),
            AgentConfig {
                specialization: "qa".to_string(),
                worktree: "agents/qa".to_string(),
                branch: "feature/qa".to_string(),
                claude_config: ClaudeConfig::for_agent("qa"),
                claude_md_template: "qa_specialist".to_string(),
            },
        );

        let config = CcswarmConfig {
            project: ProjectConfig {
                name: "Test Orchestrator Project".to_string(),
                repository: RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };

        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_assistance_request_handling (lines 200-258) ---[0m
    async fn test_assistance_request_handling() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create two backend agents
        let backend_agent1 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend1",
            ClaudeConfig::for_agent("backend1"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        let backend_agent2 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend2",
            ClaudeConfig::for_agent("backend2"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("backend-agent-1".to_string(), backend_agent1);
        master
            .agents
            .insert("backend-agent-2".to_string(), backend_agent2);

        // Simulate assistance request
        let assistance_message = AgentMessage::RequestAssistance {
            agent_id: "backend-agent-1".to_string(),
            task_id: "complex-task-1".to_string(),
            reason: "Need help with database optimization".to_string(),
        };

        master
            .coordination_bus
            .send_message(assistance_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify assistance task was created
        let state = master.state.read().await;
        let assistance_task = state
            .pending_tasks
            .iter()
            .find(|t| t.task_type == TaskType::Assistance);
        assert!(assistance_task.is_some());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:15-100 function create_comprehensive_test_config <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:289-346 function test_remediation_task_completion_triggers_rereview
  Similarity: 92.63%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:create_comprehensive_test_config (lines 15-100) ---[0m
    async fn create_comprehensive_test_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Initialize git repo
        WorktreeManager::init_if_needed(&repo_path).await.unwrap();

        let mut agents = HashMap::new();

        // Frontend agent
        agents.insert(
            "frontend".to_string(),
            AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend".to_string(),
                branch: "feature/frontend".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        // Backend agent
        agents.insert(
            "backend".to_string(),
            AgentConfig {
                specialization: "backend".to_string(),
                worktree: "agents/backend".to_string(),
                branch: "feature/backend".to_string(),
                claude_config: ClaudeConfig::for_agent("backend"),
                claude_md_template: "backend_specialist".to_string(),
            },
        );

        // DevOps agent
        agents.insert(
            "devops".to_string(),
            AgentConfig {
                specialization: "devops".to_string(),
                worktree: "agents/devops".to_string(),
                branch: "feature/devops".to_string(),
                claude_config: ClaudeConfig::for_agent("devops"),
                claude_md_template: "devops_specialist".to_string(),
            },
        );

        // QA agent
        agents.insert(
            "qa".to_string(),
            AgentConfig {
                specialization: "qa".to_string(),
                worktree: "agents/qa".to_string(),
                branch: "feature/qa".to_string(),
                claude_config: ClaudeConfig::for_agent("qa"),
                claude_md_template: "qa_specialist".to_string(),
            },
        );

        let config = CcswarmConfig {
            project: ProjectConfig {
                name: "Test Orchestrator Project".to_string(),
                repository: RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };

        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_remediation_task_completion_triggers_rereview (lines 289-346) ---[0m
    async fn test_remediation_task_completion_triggers_rereview() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Setup initial review history
        let original_task_id = "original-task-1";
        let remediation_task_id = format!("remediate-{}-123", original_task_id);

        let review_entry = ReviewHistoryEntry {
            task_id: original_task_id.to_string(),
            agent_id: "backend-agent".to_string(),
            review_date: chrono::Utc::now(),
            issues_found: vec!["Low test coverage".to_string()],
            remediation_task_id: Some(remediation_task_id.clone()),
            review_passed: false,
            iteration: 1,
        };

        {
            let mut state = master.state.write().await;
            state
                .review_history
                .entry(original_task_id.to_string())
                .or_insert_with(Vec::new)
                .push(review_entry);
        }

        // Simulate remediation task completion
        let completion_message = AgentMessage::TaskCompleted {
            agent_id: "backend-agent".to_string(),
            task_id: remediation_task_id.clone(),
            result: TaskResult {
                success: true,
                output: serde_json::json!({"fixed": true}),
                error: None,
                duration: std::time::Duration::from_secs(300),
            },
        };

        master
            .coordination_bus
            .send_message(completion_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify re-review task was created
        let state = master.state.read().await;
        let review_task = state.pending_tasks.iter().find(|t| {
            t.task_type == TaskType::Review
                && t.parent_task_id == Some(original_task_id.to_string())
        });
        assert!(review_task.is_some());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:15-100 function create_comprehensive_test_config <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:418-461 function test_task_prioritization
  Similarity: 92.54%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:create_comprehensive_test_config (lines 15-100) ---[0m
    async fn create_comprehensive_test_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Initialize git repo
        WorktreeManager::init_if_needed(&repo_path).await.unwrap();

        let mut agents = HashMap::new();

        // Frontend agent
        agents.insert(
            "frontend".to_string(),
            AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend".to_string(),
                branch: "feature/frontend".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        // Backend agent
        agents.insert(
            "backend".to_string(),
            AgentConfig {
                specialization: "backend".to_string(),
                worktree: "agents/backend".to_string(),
                branch: "feature/backend".to_string(),
                claude_config: ClaudeConfig::for_agent("backend"),
                claude_md_template: "backend_specialist".to_string(),
            },
        );

        // DevOps agent
        agents.insert(
            "devops".to_string(),
            AgentConfig {
                specialization: "devops".to_string(),
                worktree: "agents/devops".to_string(),
                branch: "feature/devops".to_string(),
                claude_config: ClaudeConfig::for_agent("devops"),
                claude_md_template: "devops_specialist".to_string(),
            },
        );

        // QA agent
        agents.insert(
            "qa".to_string(),
            AgentConfig {
                specialization: "qa".to_string(),
                worktree: "agents/qa".to_string(),
                branch: "feature/qa".to_string(),
                claude_config: ClaudeConfig::for_agent("qa"),
                claude_md_template: "qa_specialist".to_string(),
            },
        );

        let config = CcswarmConfig {
            project: ProjectConfig {
                name: "Test Orchestrator Project".to_string(),
                repository: RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };

        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_prioritization (lines 418-461) ---[0m
    async fn test_task_prioritization() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add tasks with different priorities
        let critical_task = Task::new(
            "critical-1".to_string(),
            "Fix production bug".to_string(),
            Priority::Critical,
            TaskType::Bug,
        );

        let medium_task = Task::new(
            "medium-1".to_string(),
            "Add new feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let low_task = Task::new(
            "low-1".to_string(),
            "Update documentation".to_string(),
            Priority::Low,
            TaskType::Documentation,
        );

        // Add in random order
        master.add_task(medium_task).await.unwrap();
        master.add_task(low_task).await.unwrap();
        master.add_task(critical_task).await.unwrap();

        // Verify tasks are properly tracked
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);

        // Find the critical task
        let critical_exists = state
            .pending_tasks
            .iter()
            .any(|t| t.priority == Priority::Critical);
        assert!(critical_exists);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:15-100 function create_comprehensive_test_config <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:464-504 function test_status_report_generation
  Similarity: 92.33%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:create_comprehensive_test_config (lines 15-100) ---[0m
    async fn create_comprehensive_test_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Initialize git repo
        WorktreeManager::init_if_needed(&repo_path).await.unwrap();

        let mut agents = HashMap::new();

        // Frontend agent
        agents.insert(
            "frontend".to_string(),
            AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend".to_string(),
                branch: "feature/frontend".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        // Backend agent
        agents.insert(
            "backend".to_string(),
            AgentConfig {
                specialization: "backend".to_string(),
                worktree: "agents/backend".to_string(),
                branch: "feature/backend".to_string(),
                claude_config: ClaudeConfig::for_agent("backend"),
                claude_md_template: "backend_specialist".to_string(),
            },
        );

        // DevOps agent
        agents.insert(
            "devops".to_string(),
            AgentConfig {
                specialization: "devops".to_string(),
                worktree: "agents/devops".to_string(),
                branch: "feature/devops".to_string(),
                claude_config: ClaudeConfig::for_agent("devops"),
                claude_md_template: "devops_specialist".to_string(),
            },
        );

        // QA agent
        agents.insert(
            "qa".to_string(),
            AgentConfig {
                specialization: "qa".to_string(),
                worktree: "agents/qa".to_string(),
                branch: "feature/qa".to_string(),
                claude_config: ClaudeConfig::for_agent("qa"),
                claude_md_template: "qa_specialist".to_string(),
            },
        );

        let config = CcswarmConfig {
            project: ProjectConfig {
                name: "Test Orchestrator Project".to_string(),
                repository: RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };

        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_status_report_generation (lines 464-504) ---[0m
    async fn test_status_report_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Add some agents
        for i in 0..3 {
            let agent = ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/agent-{}", i),
                ClaudeConfig::for_agent(&format!("agent-{}", i)),
                IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            master.agents.insert(format!("agent-{}", i), agent);
        }

        // Update orchestrator state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
            state.total_tasks_processed = 10;
            state.successful_tasks = 8;
            state.failed_tasks = 2;
        }

        // Generate report
        let report = master.generate_status_report().await.unwrap();

        assert_eq!(report.orchestrator_id, master.id);
        assert_eq!(report.status, OrchestratorStatus::Running);
        assert_eq!(report.total_agents, 3);
        assert_eq!(report.total_tasks_processed, 10);
        assert_eq!(report.successful_tasks, 8);
        assert_eq!(report.failed_tasks, 2);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:200-258 function test_assistance_request_handling <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:289-346 function test_remediation_task_completion_triggers_rereview
  Similarity: 92.13%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_assistance_request_handling (lines 200-258) ---[0m
    async fn test_assistance_request_handling() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create two backend agents
        let backend_agent1 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend1",
            ClaudeConfig::for_agent("backend1"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        let backend_agent2 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend2",
            ClaudeConfig::for_agent("backend2"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("backend-agent-1".to_string(), backend_agent1);
        master
            .agents
            .insert("backend-agent-2".to_string(), backend_agent2);

        // Simulate assistance request
        let assistance_message = AgentMessage::RequestAssistance {
            agent_id: "backend-agent-1".to_string(),
            task_id: "complex-task-1".to_string(),
            reason: "Need help with database optimization".to_string(),
        };

        master
            .coordination_bus
            .send_message(assistance_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify assistance task was created
        let state = master.state.read().await;
        let assistance_task = state
            .pending_tasks
            .iter()
            .find(|t| t.task_type == TaskType::Assistance);
        assert!(assistance_task.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_remediation_task_completion_triggers_rereview (lines 289-346) ---[0m
    async fn test_remediation_task_completion_triggers_rereview() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Setup initial review history
        let original_task_id = "original-task-1";
        let remediation_task_id = format!("remediate-{}-123", original_task_id);

        let review_entry = ReviewHistoryEntry {
            task_id: original_task_id.to_string(),
            agent_id: "backend-agent".to_string(),
            review_date: chrono::Utc::now(),
            issues_found: vec!["Low test coverage".to_string()],
            remediation_task_id: Some(remediation_task_id.clone()),
            review_passed: false,
            iteration: 1,
        };

        {
            let mut state = master.state.write().await;
            state
                .review_history
                .entry(original_task_id.to_string())
                .or_insert_with(Vec::new)
                .push(review_entry);
        }

        // Simulate remediation task completion
        let completion_message = AgentMessage::TaskCompleted {
            agent_id: "backend-agent".to_string(),
            task_id: remediation_task_id.clone(),
            result: TaskResult {
                success: true,
                output: serde_json::json!({"fixed": true}),
                error: None,
                duration: std::time::Duration::from_secs(300),
            },
        };

        master
            .coordination_bus
            .send_message(completion_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify re-review task was created
        let state = master.state.read().await;
        let review_task = state.pending_tasks.iter().find(|t| {
            t.task_type == TaskType::Review
                && t.parent_task_id == Some(original_task_id.to_string())
        });
        assert!(review_task.is_some());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:289-346 function test_remediation_task_completion_triggers_rereview <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:418-461 function test_task_prioritization
  Similarity: 92.46%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_remediation_task_completion_triggers_rereview (lines 289-346) ---[0m
    async fn test_remediation_task_completion_triggers_rereview() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Setup initial review history
        let original_task_id = "original-task-1";
        let remediation_task_id = format!("remediate-{}-123", original_task_id);

        let review_entry = ReviewHistoryEntry {
            task_id: original_task_id.to_string(),
            agent_id: "backend-agent".to_string(),
            review_date: chrono::Utc::now(),
            issues_found: vec!["Low test coverage".to_string()],
            remediation_task_id: Some(remediation_task_id.clone()),
            review_passed: false,
            iteration: 1,
        };

        {
            let mut state = master.state.write().await;
            state
                .review_history
                .entry(original_task_id.to_string())
                .or_insert_with(Vec::new)
                .push(review_entry);
        }

        // Simulate remediation task completion
        let completion_message = AgentMessage::TaskCompleted {
            agent_id: "backend-agent".to_string(),
            task_id: remediation_task_id.clone(),
            result: TaskResult {
                success: true,
                output: serde_json::json!({"fixed": true}),
                error: None,
                duration: std::time::Duration::from_secs(300),
            },
        };

        master
            .coordination_bus
            .send_message(completion_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify re-review task was created
        let state = master.state.read().await;
        let review_task = state.pending_tasks.iter().find(|t| {
            t.task_type == TaskType::Review
                && t.parent_task_id == Some(original_task_id.to_string())
        });
        assert!(review_task.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_prioritization (lines 418-461) ---[0m
    async fn test_task_prioritization() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add tasks with different priorities
        let critical_task = Task::new(
            "critical-1".to_string(),
            "Fix production bug".to_string(),
            Priority::Critical,
            TaskType::Bug,
        );

        let medium_task = Task::new(
            "medium-1".to_string(),
            "Add new feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let low_task = Task::new(
            "low-1".to_string(),
            "Update documentation".to_string(),
            Priority::Low,
            TaskType::Documentation,
        );

        // Add in random order
        master.add_task(medium_task).await.unwrap();
        master.add_task(low_task).await.unwrap();
        master.add_task(critical_task).await.unwrap();

        // Verify tasks are properly tracked
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);

        // Find the critical task
        let critical_exists = state
            .pending_tasks
            .iter()
            .any(|t| t.priority == Priority::Critical);
        assert!(critical_exists);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:200-258 function test_assistance_request_handling <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:418-461 function test_task_prioritization
  Similarity: 90.36%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_assistance_request_handling (lines 200-258) ---[0m
    async fn test_assistance_request_handling() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create two backend agents
        let backend_agent1 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend1",
            ClaudeConfig::for_agent("backend1"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        let backend_agent2 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend2",
            ClaudeConfig::for_agent("backend2"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("backend-agent-1".to_string(), backend_agent1);
        master
            .agents
            .insert("backend-agent-2".to_string(), backend_agent2);

        // Simulate assistance request
        let assistance_message = AgentMessage::RequestAssistance {
            agent_id: "backend-agent-1".to_string(),
            task_id: "complex-task-1".to_string(),
            reason: "Need help with database optimization".to_string(),
        };

        master
            .coordination_bus
            .send_message(assistance_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify assistance task was created
        let state = master.state.read().await;
        let assistance_task = state
            .pending_tasks
            .iter()
            .find(|t| t.task_type == TaskType::Assistance);
        assert!(assistance_task.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_prioritization (lines 418-461) ---[0m
    async fn test_task_prioritization() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add tasks with different priorities
        let critical_task = Task::new(
            "critical-1".to_string(),
            "Fix production bug".to_string(),
            Priority::Critical,
            TaskType::Bug,
        );

        let medium_task = Task::new(
            "medium-1".to_string(),
            "Add new feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let low_task = Task::new(
            "low-1".to_string(),
            "Update documentation".to_string(),
            Priority::Low,
            TaskType::Documentation,
        );

        // Add in random order
        master.add_task(medium_task).await.unwrap();
        master.add_task(low_task).await.unwrap();
        master.add_task(critical_task).await.unwrap();

        // Verify tasks are properly tracked
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);

        // Find the critical task
        let critical_exists = state
            .pending_tasks
            .iter()
            .any(|t| t.priority == Priority::Critical);
        assert!(critical_exists);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:289-346 function test_remediation_task_completion_triggers_rereview <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:464-504 function test_status_report_generation
  Similarity: 89.10%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_remediation_task_completion_triggers_rereview (lines 289-346) ---[0m
    async fn test_remediation_task_completion_triggers_rereview() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Setup initial review history
        let original_task_id = "original-task-1";
        let remediation_task_id = format!("remediate-{}-123", original_task_id);

        let review_entry = ReviewHistoryEntry {
            task_id: original_task_id.to_string(),
            agent_id: "backend-agent".to_string(),
            review_date: chrono::Utc::now(),
            issues_found: vec!["Low test coverage".to_string()],
            remediation_task_id: Some(remediation_task_id.clone()),
            review_passed: false,
            iteration: 1,
        };

        {
            let mut state = master.state.write().await;
            state
                .review_history
                .entry(original_task_id.to_string())
                .or_insert_with(Vec::new)
                .push(review_entry);
        }

        // Simulate remediation task completion
        let completion_message = AgentMessage::TaskCompleted {
            agent_id: "backend-agent".to_string(),
            task_id: remediation_task_id.clone(),
            result: TaskResult {
                success: true,
                output: serde_json::json!({"fixed": true}),
                error: None,
                duration: std::time::Duration::from_secs(300),
            },
        };

        master
            .coordination_bus
            .send_message(completion_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify re-review task was created
        let state = master.state.read().await;
        let review_task = state.pending_tasks.iter().find(|t| {
            t.task_type == TaskType::Review
                && t.parent_task_id == Some(original_task_id.to_string())
        });
        assert!(review_task.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_status_report_generation (lines 464-504) ---[0m
    async fn test_status_report_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Add some agents
        for i in 0..3 {
            let agent = ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/agent-{}", i),
                ClaudeConfig::for_agent(&format!("agent-{}", i)),
                IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            master.agents.insert(format!("agent-{}", i), agent);
        }

        // Update orchestrator state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
            state.total_tasks_processed = 10;
            state.successful_tasks = 8;
            state.failed_tasks = 2;
        }

        // Generate report
        let report = master.generate_status_report().await.unwrap();

        assert_eq!(report.orchestrator_id, master.id);
        assert_eq!(report.status, OrchestratorStatus::Running);
        assert_eq!(report.total_agents, 3);
        assert_eq!(report.total_tasks_processed, 10);
        assert_eq!(report.successful_tasks, 8);
        assert_eq!(report.failed_tasks, 2);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:200-258 function test_assistance_request_handling <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:464-504 function test_status_report_generation
  Similarity: 86.09%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_assistance_request_handling (lines 200-258) ---[0m
    async fn test_assistance_request_handling() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create two backend agents
        let backend_agent1 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend1",
            ClaudeConfig::for_agent("backend1"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        let backend_agent2 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend2",
            ClaudeConfig::for_agent("backend2"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("backend-agent-1".to_string(), backend_agent1);
        master
            .agents
            .insert("backend-agent-2".to_string(), backend_agent2);

        // Simulate assistance request
        let assistance_message = AgentMessage::RequestAssistance {
            agent_id: "backend-agent-1".to_string(),
            task_id: "complex-task-1".to_string(),
            reason: "Need help with database optimization".to_string(),
        };

        master
            .coordination_bus
            .send_message(assistance_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify assistance task was created
        let state = master.state.read().await;
        let assistance_task = state
            .pending_tasks
            .iter()
            .find(|t| t.task_type == TaskType::Assistance);
        assert!(assistance_task.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_status_report_generation (lines 464-504) ---[0m
    async fn test_status_report_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Add some agents
        for i in 0..3 {
            let agent = ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/agent-{}", i),
                ClaudeConfig::for_agent(&format!("agent-{}", i)),
                IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            master.agents.insert(format!("agent-{}", i), agent);
        }

        // Update orchestrator state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
            state.total_tasks_processed = 10;
            state.successful_tasks = 8;
            state.failed_tasks = 2;
        }

        // Generate report
        let report = master.generate_status_report().await.unwrap();

        assert_eq!(report.orchestrator_id, master.id);
        assert_eq!(report.status, OrchestratorStatus::Running);
        assert_eq!(report.total_agents, 3);
        assert_eq!(report.total_tasks_processed, 10);
        assert_eq!(report.successful_tasks, 8);
        assert_eq!(report.failed_tasks, 2);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:165-196 function test_quality_review_triggers_remediation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:289-346 function test_remediation_task_completion_triggers_rereview
  Similarity: 91.35%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_quality_review_triggers_remediation (lines 165-196) ---[0m
    async fn test_quality_review_triggers_remediation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Simulate a quality issue
        let quality_message = AgentMessage::QualityIssue {
            agent_id: "backend-agent".to_string(),
            task_id: "api-task-1".to_string(),
            issues: vec![
                "Low test coverage".to_string(),
                "High complexity".to_string(),
                "Missing error handling".to_string(),
            ],
        };

        bus.send_message(quality_message).await.unwrap();

        // Allow time for message processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Check that remediation task was created in review history
        let state = master.state.read().await;
        assert!(state.review_history.contains_key("api-task-1"));

        let history_entries = &state.review_history["api-task-1"];
        assert_eq!(history_entries.len(), 1);
        assert_eq!(history_entries[0].issues_found.len(), 3);
        assert!(history_entries[0].remediation_task_id.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_remediation_task_completion_triggers_rereview (lines 289-346) ---[0m
    async fn test_remediation_task_completion_triggers_rereview() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Setup initial review history
        let original_task_id = "original-task-1";
        let remediation_task_id = format!("remediate-{}-123", original_task_id);

        let review_entry = ReviewHistoryEntry {
            task_id: original_task_id.to_string(),
            agent_id: "backend-agent".to_string(),
            review_date: chrono::Utc::now(),
            issues_found: vec!["Low test coverage".to_string()],
            remediation_task_id: Some(remediation_task_id.clone()),
            review_passed: false,
            iteration: 1,
        };

        {
            let mut state = master.state.write().await;
            state
                .review_history
                .entry(original_task_id.to_string())
                .or_insert_with(Vec::new)
                .push(review_entry);
        }

        // Simulate remediation task completion
        let completion_message = AgentMessage::TaskCompleted {
            agent_id: "backend-agent".to_string(),
            task_id: remediation_task_id.clone(),
            result: TaskResult {
                success: true,
                output: serde_json::json!({"fixed": true}),
                error: None,
                duration: std::time::Duration::from_secs(300),
            },
        };

        master
            .coordination_bus
            .send_message(completion_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify re-review task was created
        let state = master.state.read().await;
        let review_task = state.pending_tasks.iter().find(|t| {
            t.task_type == TaskType::Review
                && t.parent_task_id == Some(original_task_id.to_string())
        });
        assert!(review_task.is_some());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:128-161 function test_agent_task_delegation_with_metadata <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:289-346 function test_remediation_task_completion_triggers_rereview
  Similarity: 88.04%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_task_delegation_with_metadata (lines 128-161) ---[0m
    async fn test_agent_task_delegation_with_metadata() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create mock agents
        let frontend_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/frontend",
            ClaudeConfig::for_agent("frontend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("frontend-agent".to_string(), frontend_agent);

        // Create a frontend task
        let task = Task::new(
            "ui-task-1".to_string(),
            "Create login component with React".to_string(),
            Priority::High,
            TaskType::Development,
        );

        // Test task assignment logic
        let selected_agent = master.select_optimal_agent(&task).await.unwrap();
        assert_eq!(selected_agent, "frontend-agent");
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_remediation_task_completion_triggers_rereview (lines 289-346) ---[0m
    async fn test_remediation_task_completion_triggers_rereview() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Setup initial review history
        let original_task_id = "original-task-1";
        let remediation_task_id = format!("remediate-{}-123", original_task_id);

        let review_entry = ReviewHistoryEntry {
            task_id: original_task_id.to_string(),
            agent_id: "backend-agent".to_string(),
            review_date: chrono::Utc::now(),
            issues_found: vec!["Low test coverage".to_string()],
            remediation_task_id: Some(remediation_task_id.clone()),
            review_passed: false,
            iteration: 1,
        };

        {
            let mut state = master.state.write().await;
            state
                .review_history
                .entry(original_task_id.to_string())
                .or_insert_with(Vec::new)
                .push(review_entry);
        }

        // Simulate remediation task completion
        let completion_message = AgentMessage::TaskCompleted {
            agent_id: "backend-agent".to_string(),
            task_id: remediation_task_id.clone(),
            result: TaskResult {
                success: true,
                output: serde_json::json!({"fixed": true}),
                error: None,
                duration: std::time::Duration::from_secs(300),
            },
        };

        master
            .coordination_bus
            .send_message(completion_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify re-review task was created
        let state = master.state.read().await;
        let review_task = state.pending_tasks.iter().find(|t| {
            t.task_type == TaskType::Review
                && t.parent_task_id == Some(original_task_id.to_string())
        });
        assert!(review_task.is_some());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:165-196 function test_quality_review_triggers_remediation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:200-258 function test_assistance_request_handling
  Similarity: 88.63%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_quality_review_triggers_remediation (lines 165-196) ---[0m
    async fn test_quality_review_triggers_remediation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Simulate a quality issue
        let quality_message = AgentMessage::QualityIssue {
            agent_id: "backend-agent".to_string(),
            task_id: "api-task-1".to_string(),
            issues: vec![
                "Low test coverage".to_string(),
                "High complexity".to_string(),
                "Missing error handling".to_string(),
            ],
        };

        bus.send_message(quality_message).await.unwrap();

        // Allow time for message processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Check that remediation task was created in review history
        let state = master.state.read().await;
        assert!(state.review_history.contains_key("api-task-1"));

        let history_entries = &state.review_history["api-task-1"];
        assert_eq!(history_entries.len(), 1);
        assert_eq!(history_entries[0].issues_found.len(), 3);
        assert!(history_entries[0].remediation_task_id.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_assistance_request_handling (lines 200-258) ---[0m
    async fn test_assistance_request_handling() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create two backend agents
        let backend_agent1 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend1",
            ClaudeConfig::for_agent("backend1"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        let backend_agent2 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend2",
            ClaudeConfig::for_agent("backend2"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("backend-agent-1".to_string(), backend_agent1);
        master
            .agents
            .insert("backend-agent-2".to_string(), backend_agent2);

        // Simulate assistance request
        let assistance_message = AgentMessage::RequestAssistance {
            agent_id: "backend-agent-1".to_string(),
            task_id: "complex-task-1".to_string(),
            reason: "Need help with database optimization".to_string(),
        };

        master
            .coordination_bus
            .send_message(assistance_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify assistance task was created
        let state = master.state.read().await;
        let assistance_task = state
            .pending_tasks
            .iter()
            .find(|t| t.task_type == TaskType::Assistance);
        assert!(assistance_task.is_some());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:289-346 function test_remediation_task_completion_triggers_rereview <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:383-415 function test_agent_health_monitoring
  Similarity: 88.46%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_remediation_task_completion_triggers_rereview (lines 289-346) ---[0m
    async fn test_remediation_task_completion_triggers_rereview() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Setup initial review history
        let original_task_id = "original-task-1";
        let remediation_task_id = format!("remediate-{}-123", original_task_id);

        let review_entry = ReviewHistoryEntry {
            task_id: original_task_id.to_string(),
            agent_id: "backend-agent".to_string(),
            review_date: chrono::Utc::now(),
            issues_found: vec!["Low test coverage".to_string()],
            remediation_task_id: Some(remediation_task_id.clone()),
            review_passed: false,
            iteration: 1,
        };

        {
            let mut state = master.state.write().await;
            state
                .review_history
                .entry(original_task_id.to_string())
                .or_insert_with(Vec::new)
                .push(review_entry);
        }

        // Simulate remediation task completion
        let completion_message = AgentMessage::TaskCompleted {
            agent_id: "backend-agent".to_string(),
            task_id: remediation_task_id.clone(),
            result: TaskResult {
                success: true,
                output: serde_json::json!({"fixed": true}),
                error: None,
                duration: std::time::Duration::from_secs(300),
            },
        };

        master
            .coordination_bus
            .send_message(completion_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify re-review task was created
        let state = master.state.read().await;
        let review_task = state.pending_tasks.iter().find(|t| {
            t.task_type == TaskType::Review
                && t.parent_task_id == Some(original_task_id.to_string())
        });
        assert!(review_task.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_health_monitoring (lines 383-415) ---[0m
    async fn test_agent_health_monitoring() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create an agent with old last_activity
        let mut unhealthy_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend",
            ClaudeConfig::for_agent("backend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Set last activity to 10 minutes ago
        unhealthy_agent.last_activity = chrono::Utc::now() - chrono::Duration::seconds(600);

        master
            .agents
            .insert("unhealthy-agent".to_string(), unhealthy_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was restarted (last_activity updated)
        let agent = master.agents.get("unhealthy-agent").unwrap();
        let time_diff = chrono::Utc::now() - agent.last_activity;
        assert!(time_diff.num_seconds() < 5); // Should be recent
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:128-161 function test_agent_task_delegation_with_metadata <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:200-258 function test_assistance_request_handling
  Similarity: 86.33%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_task_delegation_with_metadata (lines 128-161) ---[0m
    async fn test_agent_task_delegation_with_metadata() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create mock agents
        let frontend_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/frontend",
            ClaudeConfig::for_agent("frontend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("frontend-agent".to_string(), frontend_agent);

        // Create a frontend task
        let task = Task::new(
            "ui-task-1".to_string(),
            "Create login component with React".to_string(),
            Priority::High,
            TaskType::Development,
        );

        // Test task assignment logic
        let selected_agent = master.select_optimal_agent(&task).await.unwrap();
        assert_eq!(selected_agent, "frontend-agent");
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_assistance_request_handling (lines 200-258) ---[0m
    async fn test_assistance_request_handling() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create two backend agents
        let backend_agent1 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend1",
            ClaudeConfig::for_agent("backend1"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        let backend_agent2 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend2",
            ClaudeConfig::for_agent("backend2"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("backend-agent-1".to_string(), backend_agent1);
        master
            .agents
            .insert("backend-agent-2".to_string(), backend_agent2);

        // Simulate assistance request
        let assistance_message = AgentMessage::RequestAssistance {
            agent_id: "backend-agent-1".to_string(),
            task_id: "complex-task-1".to_string(),
            reason: "Need help with database optimization".to_string(),
        };

        master
            .coordination_bus
            .send_message(assistance_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify assistance task was created
        let state = master.state.read().await;
        let assistance_task = state
            .pending_tasks
            .iter()
            .find(|t| t.task_type == TaskType::Assistance);
        assert!(assistance_task.is_some());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:200-258 function test_assistance_request_handling <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:383-415 function test_agent_health_monitoring
  Similarity: 86.67%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_assistance_request_handling (lines 200-258) ---[0m
    async fn test_assistance_request_handling() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create two backend agents
        let backend_agent1 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend1",
            ClaudeConfig::for_agent("backend1"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        let backend_agent2 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend2",
            ClaudeConfig::for_agent("backend2"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("backend-agent-1".to_string(), backend_agent1);
        master
            .agents
            .insert("backend-agent-2".to_string(), backend_agent2);

        // Simulate assistance request
        let assistance_message = AgentMessage::RequestAssistance {
            agent_id: "backend-agent-1".to_string(),
            task_id: "complex-task-1".to_string(),
            reason: "Need help with database optimization".to_string(),
        };

        master
            .coordination_bus
            .send_message(assistance_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify assistance task was created
        let state = master.state.read().await;
        let assistance_task = state
            .pending_tasks
            .iter()
            .find(|t| t.task_type == TaskType::Assistance);
        assert!(assistance_task.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_health_monitoring (lines 383-415) ---[0m
    async fn test_agent_health_monitoring() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create an agent with old last_activity
        let mut unhealthy_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend",
            ClaudeConfig::for_agent("backend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Set last activity to 10 minutes ago
        unhealthy_agent.last_activity = chrono::Utc::now() - chrono::Duration::seconds(600);

        master
            .agents
            .insert("unhealthy-agent".to_string(), unhealthy_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was restarted (last_activity updated)
        let agent = master.agents.get("unhealthy-agent").unwrap();
        let time_diff = chrono::Utc::now() - agent.last_activity;
        assert!(time_diff.num_seconds() < 5); // Should be recent
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:289-346 function test_remediation_task_completion_triggers_rereview <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:349-379 function test_concurrent_task_processing
  Similarity: 88.57%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_remediation_task_completion_triggers_rereview (lines 289-346) ---[0m
    async fn test_remediation_task_completion_triggers_rereview() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Setup initial review history
        let original_task_id = "original-task-1";
        let remediation_task_id = format!("remediate-{}-123", original_task_id);

        let review_entry = ReviewHistoryEntry {
            task_id: original_task_id.to_string(),
            agent_id: "backend-agent".to_string(),
            review_date: chrono::Utc::now(),
            issues_found: vec!["Low test coverage".to_string()],
            remediation_task_id: Some(remediation_task_id.clone()),
            review_passed: false,
            iteration: 1,
        };

        {
            let mut state = master.state.write().await;
            state
                .review_history
                .entry(original_task_id.to_string())
                .or_insert_with(Vec::new)
                .push(review_entry);
        }

        // Simulate remediation task completion
        let completion_message = AgentMessage::TaskCompleted {
            agent_id: "backend-agent".to_string(),
            task_id: remediation_task_id.clone(),
            result: TaskResult {
                success: true,
                output: serde_json::json!({"fixed": true}),
                error: None,
                duration: std::time::Duration::from_secs(300),
            },
        };

        master
            .coordination_bus
            .send_message(completion_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify re-review task was created
        let state = master.state.read().await;
        let review_task = state.pending_tasks.iter().find(|t| {
            t.task_type == TaskType::Review
                && t.parent_task_id == Some(original_task_id.to_string())
        });
        assert!(review_task.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_concurrent_task_processing (lines 349-379) ---[0m
    async fn test_concurrent_task_processing() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create multiple tasks concurrently
        let mut handles = vec![];
        for i in 0..10 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("concurrent-task-{}", i),
                    format!("Task {} description", i),
                    Priority::Medium,
                    TaskType::Development,
                );
                master_clone.add_task(task).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all tasks to be added
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify all tasks were added
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 10);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:200-258 function test_assistance_request_handling <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:528-556 function test_error_recovery_mechanism
  Similarity: 88.48%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_assistance_request_handling (lines 200-258) ---[0m
    async fn test_assistance_request_handling() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create two backend agents
        let backend_agent1 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend1",
            ClaudeConfig::for_agent("backend1"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        let backend_agent2 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend2",
            ClaudeConfig::for_agent("backend2"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("backend-agent-1".to_string(), backend_agent1);
        master
            .agents
            .insert("backend-agent-2".to_string(), backend_agent2);

        // Simulate assistance request
        let assistance_message = AgentMessage::RequestAssistance {
            agent_id: "backend-agent-1".to_string(),
            task_id: "complex-task-1".to_string(),
            reason: "Need help with database optimization".to_string(),
        };

        master
            .coordination_bus
            .send_message(assistance_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify assistance task was created
        let state = master.state.read().await;
        let assistance_task = state
            .pending_tasks
            .iter()
            .find(|t| t.task_type == TaskType::Assistance);
        assert!(assistance_task.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_error_recovery_mechanism (lines 528-556) ---[0m
    async fn test_error_recovery_mechanism() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create agent in error state
        let mut error_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/error",
            ClaudeConfig::for_agent("error-agent"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        error_agent.status = crate::agent::AgentStatus::Error("Test error".to_string());

        master.agents.insert("error-agent".to_string(), error_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was recovered
        let agent = master.agents.get("error-agent").unwrap();
        assert!(matches!(agent.status, crate::agent::AgentStatus::Available));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:289-346 function test_remediation_task_completion_triggers_rereview <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:528-556 function test_error_recovery_mechanism
  Similarity: 89.49%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_remediation_task_completion_triggers_rereview (lines 289-346) ---[0m
    async fn test_remediation_task_completion_triggers_rereview() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Setup initial review history
        let original_task_id = "original-task-1";
        let remediation_task_id = format!("remediate-{}-123", original_task_id);

        let review_entry = ReviewHistoryEntry {
            task_id: original_task_id.to_string(),
            agent_id: "backend-agent".to_string(),
            review_date: chrono::Utc::now(),
            issues_found: vec!["Low test coverage".to_string()],
            remediation_task_id: Some(remediation_task_id.clone()),
            review_passed: false,
            iteration: 1,
        };

        {
            let mut state = master.state.write().await;
            state
                .review_history
                .entry(original_task_id.to_string())
                .or_insert_with(Vec::new)
                .push(review_entry);
        }

        // Simulate remediation task completion
        let completion_message = AgentMessage::TaskCompleted {
            agent_id: "backend-agent".to_string(),
            task_id: remediation_task_id.clone(),
            result: TaskResult {
                success: true,
                output: serde_json::json!({"fixed": true}),
                error: None,
                duration: std::time::Duration::from_secs(300),
            },
        };

        master
            .coordination_bus
            .send_message(completion_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify re-review task was created
        let state = master.state.read().await;
        let review_task = state.pending_tasks.iter().find(|t| {
            t.task_type == TaskType::Review
                && t.parent_task_id == Some(original_task_id.to_string())
        });
        assert!(review_task.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_error_recovery_mechanism (lines 528-556) ---[0m
    async fn test_error_recovery_mechanism() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create agent in error state
        let mut error_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/error",
            ClaudeConfig::for_agent("error-agent"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        error_agent.status = crate::agent::AgentStatus::Error("Test error".to_string());

        master.agents.insert("error-agent".to_string(), error_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was recovered
        let agent = master.agents.get("error-agent").unwrap();
        assert!(matches!(agent.status, crate::agent::AgentStatus::Available));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:289-346 function test_remediation_task_completion_triggers_rereview <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:632-664 function test_similar_task_matching
  Similarity: 78.00%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_remediation_task_completion_triggers_rereview (lines 289-346) ---[0m
    async fn test_remediation_task_completion_triggers_rereview() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Setup initial review history
        let original_task_id = "original-task-1";
        let remediation_task_id = format!("remediate-{}-123", original_task_id);

        let review_entry = ReviewHistoryEntry {
            task_id: original_task_id.to_string(),
            agent_id: "backend-agent".to_string(),
            review_date: chrono::Utc::now(),
            issues_found: vec!["Low test coverage".to_string()],
            remediation_task_id: Some(remediation_task_id.clone()),
            review_passed: false,
            iteration: 1,
        };

        {
            let mut state = master.state.write().await;
            state
                .review_history
                .entry(original_task_id.to_string())
                .or_insert_with(Vec::new)
                .push(review_entry);
        }

        // Simulate remediation task completion
        let completion_message = AgentMessage::TaskCompleted {
            agent_id: "backend-agent".to_string(),
            task_id: remediation_task_id.clone(),
            result: TaskResult {
                success: true,
                output: serde_json::json!({"fixed": true}),
                error: None,
                duration: std::time::Duration::from_secs(300),
            },
        };

        master
            .coordination_bus
            .send_message(completion_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify re-review task was created
        let state = master.state.read().await;
        let review_task = state.pending_tasks.iter().find(|t| {
            t.task_type == TaskType::Review
                && t.parent_task_id == Some(original_task_id.to_string())
        });
        assert!(review_task.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_similar_task_matching (lines 632-664) ---[0m
    async fn test_similar_task_matching() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add some review history
        {
            let mut state = master.state.write().await;
            state.review_history.insert(
                "feature-auth-1".to_string(),
                vec![ReviewHistoryEntry {
                    task_id: "feature-auth-1".to_string(),
                    agent_id: "backend".to_string(),
                    review_date: chrono::Utc::now(),
                    issues_found: vec![],
                    remediation_task_id: None,
                    review_passed: true,
                    iteration: 1,
                }],
            );
        }

        let similar_task = Task::new(
            "new-feature-1".to_string(),
            "Another feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let similar = master.find_similar_completed_tasks(&similar_task).await;
        assert!(!similar.is_empty());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:200-258 function test_assistance_request_handling <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:349-379 function test_concurrent_task_processing
  Similarity: 78.69%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_assistance_request_handling (lines 200-258) ---[0m
    async fn test_assistance_request_handling() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create two backend agents
        let backend_agent1 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend1",
            ClaudeConfig::for_agent("backend1"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        let backend_agent2 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend2",
            ClaudeConfig::for_agent("backend2"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("backend-agent-1".to_string(), backend_agent1);
        master
            .agents
            .insert("backend-agent-2".to_string(), backend_agent2);

        // Simulate assistance request
        let assistance_message = AgentMessage::RequestAssistance {
            agent_id: "backend-agent-1".to_string(),
            task_id: "complex-task-1".to_string(),
            reason: "Need help with database optimization".to_string(),
        };

        master
            .coordination_bus
            .send_message(assistance_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify assistance task was created
        let state = master.state.read().await;
        let assistance_task = state
            .pending_tasks
            .iter()
            .find(|t| t.task_type == TaskType::Assistance);
        assert!(assistance_task.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_concurrent_task_processing (lines 349-379) ---[0m
    async fn test_concurrent_task_processing() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create multiple tasks concurrently
        let mut handles = vec![];
        for i in 0..10 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("concurrent-task-{}", i),
                    format!("Task {} description", i),
                    Priority::Medium,
                    TaskType::Development,
                );
                master_clone.add_task(task).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all tasks to be added
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify all tasks were added
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 10);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:418-461 function test_task_prioritization <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:464-504 function test_status_report_generation
  Similarity: 82.01%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_prioritization (lines 418-461) ---[0m
    async fn test_task_prioritization() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add tasks with different priorities
        let critical_task = Task::new(
            "critical-1".to_string(),
            "Fix production bug".to_string(),
            Priority::Critical,
            TaskType::Bug,
        );

        let medium_task = Task::new(
            "medium-1".to_string(),
            "Add new feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let low_task = Task::new(
            "low-1".to_string(),
            "Update documentation".to_string(),
            Priority::Low,
            TaskType::Documentation,
        );

        // Add in random order
        master.add_task(medium_task).await.unwrap();
        master.add_task(low_task).await.unwrap();
        master.add_task(critical_task).await.unwrap();

        // Verify tasks are properly tracked
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);

        // Find the critical task
        let critical_exists = state
            .pending_tasks
            .iter()
            .any(|t| t.priority == Priority::Critical);
        assert!(critical_exists);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_status_report_generation (lines 464-504) ---[0m
    async fn test_status_report_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Add some agents
        for i in 0..3 {
            let agent = ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/agent-{}", i),
                ClaudeConfig::for_agent(&format!("agent-{}", i)),
                IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            master.agents.insert(format!("agent-{}", i), agent);
        }

        // Update orchestrator state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
            state.total_tasks_processed = 10;
            state.successful_tasks = 8;
            state.failed_tasks = 2;
        }

        // Generate report
        let report = master.generate_status_report().await.unwrap();

        assert_eq!(report.orchestrator_id, master.id);
        assert_eq!(report.status, OrchestratorStatus::Running);
        assert_eq!(report.total_agents, 3);
        assert_eq!(report.total_tasks_processed, 10);
        assert_eq!(report.successful_tasks, 8);
        assert_eq!(report.failed_tasks, 2);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:128-161 function test_agent_task_delegation_with_metadata <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:418-461 function test_task_prioritization
  Similarity: 85.11%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_task_delegation_with_metadata (lines 128-161) ---[0m
    async fn test_agent_task_delegation_with_metadata() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create mock agents
        let frontend_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/frontend",
            ClaudeConfig::for_agent("frontend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("frontend-agent".to_string(), frontend_agent);

        // Create a frontend task
        let task = Task::new(
            "ui-task-1".to_string(),
            "Create login component with React".to_string(),
            Priority::High,
            TaskType::Development,
        );

        // Test task assignment logic
        let selected_agent = master.select_optimal_agent(&task).await.unwrap();
        assert_eq!(selected_agent, "frontend-agent");
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_prioritization (lines 418-461) ---[0m
    async fn test_task_prioritization() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add tasks with different priorities
        let critical_task = Task::new(
            "critical-1".to_string(),
            "Fix production bug".to_string(),
            Priority::Critical,
            TaskType::Bug,
        );

        let medium_task = Task::new(
            "medium-1".to_string(),
            "Add new feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let low_task = Task::new(
            "low-1".to_string(),
            "Update documentation".to_string(),
            Priority::Low,
            TaskType::Documentation,
        );

        // Add in random order
        master.add_task(medium_task).await.unwrap();
        master.add_task(low_task).await.unwrap();
        master.add_task(critical_task).await.unwrap();

        // Verify tasks are properly tracked
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);

        // Find the critical task
        let critical_exists = state
            .pending_tasks
            .iter()
            .any(|t| t.priority == Priority::Critical);
        assert!(critical_exists);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:165-196 function test_quality_review_triggers_remediation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:418-461 function test_task_prioritization
  Similarity: 86.77%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_quality_review_triggers_remediation (lines 165-196) ---[0m
    async fn test_quality_review_triggers_remediation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Simulate a quality issue
        let quality_message = AgentMessage::QualityIssue {
            agent_id: "backend-agent".to_string(),
            task_id: "api-task-1".to_string(),
            issues: vec![
                "Low test coverage".to_string(),
                "High complexity".to_string(),
                "Missing error handling".to_string(),
            ],
        };

        bus.send_message(quality_message).await.unwrap();

        // Allow time for message processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Check that remediation task was created in review history
        let state = master.state.read().await;
        assert!(state.review_history.contains_key("api-task-1"));

        let history_entries = &state.review_history["api-task-1"];
        assert_eq!(history_entries.len(), 1);
        assert_eq!(history_entries[0].issues_found.len(), 3);
        assert!(history_entries[0].remediation_task_id.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_prioritization (lines 418-461) ---[0m
    async fn test_task_prioritization() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add tasks with different priorities
        let critical_task = Task::new(
            "critical-1".to_string(),
            "Fix production bug".to_string(),
            Priority::Critical,
            TaskType::Bug,
        );

        let medium_task = Task::new(
            "medium-1".to_string(),
            "Add new feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let low_task = Task::new(
            "low-1".to_string(),
            "Update documentation".to_string(),
            Priority::Low,
            TaskType::Documentation,
        );

        // Add in random order
        master.add_task(medium_task).await.unwrap();
        master.add_task(low_task).await.unwrap();
        master.add_task(critical_task).await.unwrap();

        // Verify tasks are properly tracked
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);

        // Find the critical task
        let critical_exists = state
            .pending_tasks
            .iter()
            .any(|t| t.priority == Priority::Critical);
        assert!(critical_exists);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:200-258 function test_assistance_request_handling <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:603-629 function test_dependency_identification
  Similarity: 75.67%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_assistance_request_handling (lines 200-258) ---[0m
    async fn test_assistance_request_handling() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create two backend agents
        let backend_agent1 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend1",
            ClaudeConfig::for_agent("backend1"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        let backend_agent2 = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend2",
            ClaudeConfig::for_agent("backend2"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("backend-agent-1".to_string(), backend_agent1);
        master
            .agents
            .insert("backend-agent-2".to_string(), backend_agent2);

        // Simulate assistance request
        let assistance_message = AgentMessage::RequestAssistance {
            agent_id: "backend-agent-1".to_string(),
            task_id: "complex-task-1".to_string(),
            reason: "Need help with database optimization".to_string(),
        };

        master
            .coordination_bus
            .send_message(assistance_message)
            .await
            .unwrap();

        // Allow time for processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Verify assistance task was created
        let state = master.state.read().await;
        let assistance_task = state
            .pending_tasks
            .iter()
            .find(|t| t.task_type == TaskType::Assistance);
        assert!(assistance_task.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_dependency_identification (lines 603-629) ---[0m
    async fn test_dependency_identification() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test various task descriptions
        let api_task = Task::new(
            "api-1".to_string(),
            "Create REST API endpoint for users".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let dependencies = master.identify_task_dependencies(&api_task).await;
        assert!(dependencies.contains(&"backend_api".to_string()));

        let ui_task = Task::new(
            "ui-1".to_string(),
            "Build UI component for dashboard".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let ui_deps = master.identify_task_dependencies(&ui_task).await;
        assert!(ui_deps.contains(&"frontend_components".to_string()));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:383-415 function test_agent_health_monitoring <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:418-461 function test_task_prioritization
  Similarity: 84.49%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_health_monitoring (lines 383-415) ---[0m
    async fn test_agent_health_monitoring() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create an agent with old last_activity
        let mut unhealthy_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend",
            ClaudeConfig::for_agent("backend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Set last activity to 10 minutes ago
        unhealthy_agent.last_activity = chrono::Utc::now() - chrono::Duration::seconds(600);

        master
            .agents
            .insert("unhealthy-agent".to_string(), unhealthy_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was restarted (last_activity updated)
        let agent = master.agents.get("unhealthy-agent").unwrap();
        let time_diff = chrono::Utc::now() - agent.last_activity;
        assert!(time_diff.num_seconds() < 5); // Should be recent
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_prioritization (lines 418-461) ---[0m
    async fn test_task_prioritization() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add tasks with different priorities
        let critical_task = Task::new(
            "critical-1".to_string(),
            "Fix production bug".to_string(),
            Priority::Critical,
            TaskType::Bug,
        );

        let medium_task = Task::new(
            "medium-1".to_string(),
            "Add new feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let low_task = Task::new(
            "low-1".to_string(),
            "Update documentation".to_string(),
            Priority::Low,
            TaskType::Documentation,
        );

        // Add in random order
        master.add_task(medium_task).await.unwrap();
        master.add_task(low_task).await.unwrap();
        master.add_task(critical_task).await.unwrap();

        // Verify tasks are properly tracked
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);

        // Find the critical task
        let critical_exists = state
            .pending_tasks
            .iter()
            .any(|t| t.priority == Priority::Critical);
        assert!(critical_exists);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:418-461 function test_task_prioritization <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:528-556 function test_error_recovery_mechanism
  Similarity: 87.28%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_prioritization (lines 418-461) ---[0m
    async fn test_task_prioritization() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add tasks with different priorities
        let critical_task = Task::new(
            "critical-1".to_string(),
            "Fix production bug".to_string(),
            Priority::Critical,
            TaskType::Bug,
        );

        let medium_task = Task::new(
            "medium-1".to_string(),
            "Add new feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let low_task = Task::new(
            "low-1".to_string(),
            "Update documentation".to_string(),
            Priority::Low,
            TaskType::Documentation,
        );

        // Add in random order
        master.add_task(medium_task).await.unwrap();
        master.add_task(low_task).await.unwrap();
        master.add_task(critical_task).await.unwrap();

        // Verify tasks are properly tracked
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);

        // Find the critical task
        let critical_exists = state
            .pending_tasks
            .iter()
            .any(|t| t.priority == Priority::Critical);
        assert!(critical_exists);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_error_recovery_mechanism (lines 528-556) ---[0m
    async fn test_error_recovery_mechanism() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create agent in error state
        let mut error_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/error",
            ClaudeConfig::for_agent("error-agent"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        error_agent.status = crate::agent::AgentStatus::Error("Test error".to_string());

        master.agents.insert("error-agent".to_string(), error_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was recovered
        let agent = master.agents.get("error-agent").unwrap();
        assert!(matches!(agent.status, crate::agent::AgentStatus::Available));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:128-161 function test_agent_task_delegation_with_metadata <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:464-504 function test_status_report_generation
  Similarity: 84.20%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_task_delegation_with_metadata (lines 128-161) ---[0m
    async fn test_agent_task_delegation_with_metadata() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create mock agents
        let frontend_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/frontend",
            ClaudeConfig::for_agent("frontend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("frontend-agent".to_string(), frontend_agent);

        // Create a frontend task
        let task = Task::new(
            "ui-task-1".to_string(),
            "Create login component with React".to_string(),
            Priority::High,
            TaskType::Development,
        );

        // Test task assignment logic
        let selected_agent = master.select_optimal_agent(&task).await.unwrap();
        assert_eq!(selected_agent, "frontend-agent");
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_status_report_generation (lines 464-504) ---[0m
    async fn test_status_report_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Add some agents
        for i in 0..3 {
            let agent = ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/agent-{}", i),
                ClaudeConfig::for_agent(&format!("agent-{}", i)),
                IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            master.agents.insert(format!("agent-{}", i), agent);
        }

        // Update orchestrator state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
            state.total_tasks_processed = 10;
            state.successful_tasks = 8;
            state.failed_tasks = 2;
        }

        // Generate report
        let report = master.generate_status_report().await.unwrap();

        assert_eq!(report.orchestrator_id, master.id);
        assert_eq!(report.status, OrchestratorStatus::Running);
        assert_eq!(report.total_agents, 3);
        assert_eq!(report.total_tasks_processed, 10);
        assert_eq!(report.successful_tasks, 8);
        assert_eq!(report.failed_tasks, 2);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:165-196 function test_quality_review_triggers_remediation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:464-504 function test_status_report_generation
  Similarity: 85.83%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_quality_review_triggers_remediation (lines 165-196) ---[0m
    async fn test_quality_review_triggers_remediation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Simulate a quality issue
        let quality_message = AgentMessage::QualityIssue {
            agent_id: "backend-agent".to_string(),
            task_id: "api-task-1".to_string(),
            issues: vec![
                "Low test coverage".to_string(),
                "High complexity".to_string(),
                "Missing error handling".to_string(),
            ],
        };

        bus.send_message(quality_message).await.unwrap();

        // Allow time for message processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Check that remediation task was created in review history
        let state = master.state.read().await;
        assert!(state.review_history.contains_key("api-task-1"));

        let history_entries = &state.review_history["api-task-1"];
        assert_eq!(history_entries.len(), 1);
        assert_eq!(history_entries[0].issues_found.len(), 3);
        assert!(history_entries[0].remediation_task_id.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_status_report_generation (lines 464-504) ---[0m
    async fn test_status_report_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Add some agents
        for i in 0..3 {
            let agent = ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/agent-{}", i),
                ClaudeConfig::for_agent(&format!("agent-{}", i)),
                IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            master.agents.insert(format!("agent-{}", i), agent);
        }

        // Update orchestrator state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
            state.total_tasks_processed = 10;
            state.successful_tasks = 8;
            state.failed_tasks = 2;
        }

        // Generate report
        let report = master.generate_status_report().await.unwrap();

        assert_eq!(report.orchestrator_id, master.id);
        assert_eq!(report.status, OrchestratorStatus::Running);
        assert_eq!(report.total_agents, 3);
        assert_eq!(report.total_tasks_processed, 10);
        assert_eq!(report.successful_tasks, 8);
        assert_eq!(report.failed_tasks, 2);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:128-161 function test_agent_task_delegation_with_metadata <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:383-415 function test_agent_health_monitoring
  Similarity: 89.58%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_task_delegation_with_metadata (lines 128-161) ---[0m
    async fn test_agent_task_delegation_with_metadata() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create mock agents
        let frontend_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/frontend",
            ClaudeConfig::for_agent("frontend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("frontend-agent".to_string(), frontend_agent);

        // Create a frontend task
        let task = Task::new(
            "ui-task-1".to_string(),
            "Create login component with React".to_string(),
            Priority::High,
            TaskType::Development,
        );

        // Test task assignment logic
        let selected_agent = master.select_optimal_agent(&task).await.unwrap();
        assert_eq!(selected_agent, "frontend-agent");
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_health_monitoring (lines 383-415) ---[0m
    async fn test_agent_health_monitoring() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create an agent with old last_activity
        let mut unhealthy_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend",
            ClaudeConfig::for_agent("backend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Set last activity to 10 minutes ago
        unhealthy_agent.last_activity = chrono::Utc::now() - chrono::Duration::seconds(600);

        master
            .agents
            .insert("unhealthy-agent".to_string(), unhealthy_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was restarted (last_activity updated)
        let agent = master.agents.get("unhealthy-agent").unwrap();
        let time_diff = chrono::Utc::now() - agent.last_activity;
        assert!(time_diff.num_seconds() < 5); // Should be recent
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:383-415 function test_agent_health_monitoring <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:464-504 function test_status_report_generation
  Similarity: 79.52%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_health_monitoring (lines 383-415) ---[0m
    async fn test_agent_health_monitoring() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create an agent with old last_activity
        let mut unhealthy_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend",
            ClaudeConfig::for_agent("backend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Set last activity to 10 minutes ago
        unhealthy_agent.last_activity = chrono::Utc::now() - chrono::Duration::seconds(600);

        master
            .agents
            .insert("unhealthy-agent".to_string(), unhealthy_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was restarted (last_activity updated)
        let agent = master.agents.get("unhealthy-agent").unwrap();
        let time_diff = chrono::Utc::now() - agent.last_activity;
        assert!(time_diff.num_seconds() < 5); // Should be recent
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_status_report_generation (lines 464-504) ---[0m
    async fn test_status_report_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Add some agents
        for i in 0..3 {
            let agent = ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/agent-{}", i),
                ClaudeConfig::for_agent(&format!("agent-{}", i)),
                IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            master.agents.insert(format!("agent-{}", i), agent);
        }

        // Update orchestrator state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
            state.total_tasks_processed = 10;
            state.successful_tasks = 8;
            state.failed_tasks = 2;
        }

        // Generate report
        let report = master.generate_status_report().await.unwrap();

        assert_eq!(report.orchestrator_id, master.id);
        assert_eq!(report.status, OrchestratorStatus::Running);
        assert_eq!(report.total_agents, 3);
        assert_eq!(report.total_tasks_processed, 10);
        assert_eq!(report.successful_tasks, 8);
        assert_eq!(report.failed_tasks, 2);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:464-504 function test_status_report_generation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:528-556 function test_error_recovery_mechanism
  Similarity: 83.79%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_status_report_generation (lines 464-504) ---[0m
    async fn test_status_report_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Add some agents
        for i in 0..3 {
            let agent = ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/agent-{}", i),
                ClaudeConfig::for_agent(&format!("agent-{}", i)),
                IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            master.agents.insert(format!("agent-{}", i), agent);
        }

        // Update orchestrator state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
            state.total_tasks_processed = 10;
            state.successful_tasks = 8;
            state.failed_tasks = 2;
        }

        // Generate report
        let report = master.generate_status_report().await.unwrap();

        assert_eq!(report.orchestrator_id, master.id);
        assert_eq!(report.status, OrchestratorStatus::Running);
        assert_eq!(report.total_agents, 3);
        assert_eq!(report.total_tasks_processed, 10);
        assert_eq!(report.successful_tasks, 8);
        assert_eq!(report.failed_tasks, 2);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_error_recovery_mechanism (lines 528-556) ---[0m
    async fn test_error_recovery_mechanism() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create agent in error state
        let mut error_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/error",
            ClaudeConfig::for_agent("error-agent"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        error_agent.status = crate::agent::AgentStatus::Error("Test error".to_string());

        master.agents.insert("error-agent".to_string(), error_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was recovered
        let agent = master.agents.get("error-agent").unwrap();
        assert!(matches!(agent.status, crate::agent::AgentStatus::Available));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:349-379 function test_concurrent_task_processing <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:418-461 function test_task_prioritization
  Similarity: 77.39%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_concurrent_task_processing (lines 349-379) ---[0m
    async fn test_concurrent_task_processing() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create multiple tasks concurrently
        let mut handles = vec![];
        for i in 0..10 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("concurrent-task-{}", i),
                    format!("Task {} description", i),
                    Priority::Medium,
                    TaskType::Development,
                );
                master_clone.add_task(task).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all tasks to be added
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify all tasks were added
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 10);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_prioritization (lines 418-461) ---[0m
    async fn test_task_prioritization() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add tasks with different priorities
        let critical_task = Task::new(
            "critical-1".to_string(),
            "Fix production bug".to_string(),
            Priority::Critical,
            TaskType::Bug,
        );

        let medium_task = Task::new(
            "medium-1".to_string(),
            "Add new feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let low_task = Task::new(
            "low-1".to_string(),
            "Update documentation".to_string(),
            Priority::Low,
            TaskType::Documentation,
        );

        // Add in random order
        master.add_task(medium_task).await.unwrap();
        master.add_task(low_task).await.unwrap();
        master.add_task(critical_task).await.unwrap();

        // Verify tasks are properly tracked
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);

        // Find the critical task
        let critical_exists = state
            .pending_tasks
            .iter()
            .any(|t| t.priority == Priority::Critical);
        assert!(critical_exists);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:128-161 function test_agent_task_delegation_with_metadata <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:528-556 function test_error_recovery_mechanism
  Similarity: 91.89%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_task_delegation_with_metadata (lines 128-161) ---[0m
    async fn test_agent_task_delegation_with_metadata() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create mock agents
        let frontend_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/frontend",
            ClaudeConfig::for_agent("frontend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("frontend-agent".to_string(), frontend_agent);

        // Create a frontend task
        let task = Task::new(
            "ui-task-1".to_string(),
            "Create login component with React".to_string(),
            Priority::High,
            TaskType::Development,
        );

        // Test task assignment logic
        let selected_agent = master.select_optimal_agent(&task).await.unwrap();
        assert_eq!(selected_agent, "frontend-agent");
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_error_recovery_mechanism (lines 528-556) ---[0m
    async fn test_error_recovery_mechanism() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create agent in error state
        let mut error_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/error",
            ClaudeConfig::for_agent("error-agent"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        error_agent.status = crate::agent::AgentStatus::Error("Test error".to_string());

        master.agents.insert("error-agent".to_string(), error_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was recovered
        let agent = master.agents.get("error-agent").unwrap();
        assert!(matches!(agent.status, crate::agent::AgentStatus::Available));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:349-379 function test_concurrent_task_processing <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:464-504 function test_status_report_generation
  Similarity: 79.06%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_concurrent_task_processing (lines 349-379) ---[0m
    async fn test_concurrent_task_processing() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create multiple tasks concurrently
        let mut handles = vec![];
        for i in 0..10 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("concurrent-task-{}", i),
                    format!("Task {} description", i),
                    Priority::Medium,
                    TaskType::Development,
                );
                master_clone.add_task(task).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all tasks to be added
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify all tasks were added
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 10);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_status_report_generation (lines 464-504) ---[0m
    async fn test_status_report_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Add some agents
        for i in 0..3 {
            let agent = ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/agent-{}", i),
                ClaudeConfig::for_agent(&format!("agent-{}", i)),
                IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            master.agents.insert(format!("agent-{}", i), agent);
        }

        // Update orchestrator state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
            state.total_tasks_processed = 10;
            state.successful_tasks = 8;
            state.failed_tasks = 2;
        }

        // Generate report
        let report = master.generate_status_report().await.unwrap();

        assert_eq!(report.orchestrator_id, master.id);
        assert_eq!(report.status, OrchestratorStatus::Running);
        assert_eq!(report.total_agents, 3);
        assert_eq!(report.total_tasks_processed, 10);
        assert_eq!(report.successful_tasks, 8);
        assert_eq!(report.failed_tasks, 2);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:103-124 function test_master_claude_with_orchestration_context <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:418-461 function test_task_prioritization
  Similarity: 86.08%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_master_claude_with_orchestration_context (lines 103-124) ---[0m
    async fn test_master_claude_with_orchestration_context() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test that orchestration context is properly set
        let task = Task::new(
            "test-task-1".to_string(),
            "Create user authentication flow".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        // Add task to queue
        master.add_task(task.clone()).await.unwrap();

        // Verify task was added to pending tasks
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
        assert_eq!(state.pending_tasks[0].id, task.id);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_prioritization (lines 418-461) ---[0m
    async fn test_task_prioritization() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add tasks with different priorities
        let critical_task = Task::new(
            "critical-1".to_string(),
            "Fix production bug".to_string(),
            Priority::Critical,
            TaskType::Bug,
        );

        let medium_task = Task::new(
            "medium-1".to_string(),
            "Add new feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let low_task = Task::new(
            "low-1".to_string(),
            "Update documentation".to_string(),
            Priority::Low,
            TaskType::Documentation,
        );

        // Add in random order
        master.add_task(medium_task).await.unwrap();
        master.add_task(low_task).await.unwrap();
        master.add_task(critical_task).await.unwrap();

        // Verify tasks are properly tracked
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);

        // Find the critical task
        let critical_exists = state
            .pending_tasks
            .iter()
            .any(|t| t.priority == Priority::Critical);
        assert!(critical_exists);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:349-379 function test_concurrent_task_processing <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:632-664 function test_similar_task_matching
  Similarity: 88.60%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_concurrent_task_processing (lines 349-379) ---[0m
    async fn test_concurrent_task_processing() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create multiple tasks concurrently
        let mut handles = vec![];
        for i in 0..10 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("concurrent-task-{}", i),
                    format!("Task {} description", i),
                    Priority::Medium,
                    TaskType::Development,
                );
                master_clone.add_task(task).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all tasks to be added
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify all tasks were added
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 10);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_similar_task_matching (lines 632-664) ---[0m
    async fn test_similar_task_matching() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add some review history
        {
            let mut state = master.state.write().await;
            state.review_history.insert(
                "feature-auth-1".to_string(),
                vec![ReviewHistoryEntry {
                    task_id: "feature-auth-1".to_string(),
                    agent_id: "backend".to_string(),
                    review_date: chrono::Utc::now(),
                    issues_found: vec![],
                    remediation_task_id: None,
                    review_passed: true,
                    iteration: 1,
                }],
            );
        }

        let similar_task = Task::new(
            "new-feature-1".to_string(),
            "Another feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let similar = master.find_similar_completed_tasks(&similar_task).await;
        assert!(!similar.is_empty());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:128-161 function test_agent_task_delegation_with_metadata <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:349-379 function test_concurrent_task_processing
  Similarity: 86.70%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_task_delegation_with_metadata (lines 128-161) ---[0m
    async fn test_agent_task_delegation_with_metadata() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create mock agents
        let frontend_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/frontend",
            ClaudeConfig::for_agent("frontend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("frontend-agent".to_string(), frontend_agent);

        // Create a frontend task
        let task = Task::new(
            "ui-task-1".to_string(),
            "Create login component with React".to_string(),
            Priority::High,
            TaskType::Development,
        );

        // Test task assignment logic
        let selected_agent = master.select_optimal_agent(&task).await.unwrap();
        assert_eq!(selected_agent, "frontend-agent");
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_concurrent_task_processing (lines 349-379) ---[0m
    async fn test_concurrent_task_processing() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create multiple tasks concurrently
        let mut handles = vec![];
        for i in 0..10 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("concurrent-task-{}", i),
                    format!("Task {} description", i),
                    Priority::Medium,
                    TaskType::Development,
                );
                master_clone.add_task(task).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all tasks to be added
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify all tasks were added
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 10);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:418-461 function test_task_prioritization <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:559-580 function test_graceful_shutdown
  Similarity: 85.38%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_prioritization (lines 418-461) ---[0m
    async fn test_task_prioritization() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add tasks with different priorities
        let critical_task = Task::new(
            "critical-1".to_string(),
            "Fix production bug".to_string(),
            Priority::Critical,
            TaskType::Bug,
        );

        let medium_task = Task::new(
            "medium-1".to_string(),
            "Add new feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let low_task = Task::new(
            "low-1".to_string(),
            "Update documentation".to_string(),
            Priority::Low,
            TaskType::Documentation,
        );

        // Add in random order
        master.add_task(medium_task).await.unwrap();
        master.add_task(low_task).await.unwrap();
        master.add_task(critical_task).await.unwrap();

        // Verify tasks are properly tracked
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);

        // Find the critical task
        let critical_exists = state
            .pending_tasks
            .iter()
            .any(|t| t.priority == Priority::Critical);
        assert!(critical_exists);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_graceful_shutdown (lines 559-580) ---[0m
    async fn test_graceful_shutdown() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add a task
        let task = Task::new(
            "shutdown-test".to_string(),
            "Test task".to_string(),
            Priority::Low,
            TaskType::Testing,
        );
        master.add_task(task).await.unwrap();

        // Initiate shutdown
        master.shutdown().await.unwrap();

        // Verify shutdown state
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::ShuttingDown);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:128-161 function test_agent_task_delegation_with_metadata <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:632-664 function test_similar_task_matching
  Similarity: 82.51%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_task_delegation_with_metadata (lines 128-161) ---[0m
    async fn test_agent_task_delegation_with_metadata() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create mock agents
        let frontend_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/frontend",
            ClaudeConfig::for_agent("frontend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("frontend-agent".to_string(), frontend_agent);

        // Create a frontend task
        let task = Task::new(
            "ui-task-1".to_string(),
            "Create login component with React".to_string(),
            Priority::High,
            TaskType::Development,
        );

        // Test task assignment logic
        let selected_agent = master.select_optimal_agent(&task).await.unwrap();
        assert_eq!(selected_agent, "frontend-agent");
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_similar_task_matching (lines 632-664) ---[0m
    async fn test_similar_task_matching() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add some review history
        {
            let mut state = master.state.write().await;
            state.review_history.insert(
                "feature-auth-1".to_string(),
                vec![ReviewHistoryEntry {
                    task_id: "feature-auth-1".to_string(),
                    agent_id: "backend".to_string(),
                    review_date: chrono::Utc::now(),
                    issues_found: vec![],
                    remediation_task_id: None,
                    review_passed: true,
                    iteration: 1,
                }],
            );
        }

        let similar_task = Task::new(
            "new-feature-1".to_string(),
            "Another feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let similar = master.find_similar_completed_tasks(&similar_task).await;
        assert!(!similar.is_empty());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:383-415 function test_agent_health_monitoring <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:528-556 function test_error_recovery_mechanism
  Similarity: 89.04%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_health_monitoring (lines 383-415) ---[0m
    async fn test_agent_health_monitoring() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create an agent with old last_activity
        let mut unhealthy_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend",
            ClaudeConfig::for_agent("backend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Set last activity to 10 minutes ago
        unhealthy_agent.last_activity = chrono::Utc::now() - chrono::Duration::seconds(600);

        master
            .agents
            .insert("unhealthy-agent".to_string(), unhealthy_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was restarted (last_activity updated)
        let agent = master.agents.get("unhealthy-agent").unwrap();
        let time_diff = chrono::Utc::now() - agent.last_activity;
        assert!(time_diff.num_seconds() < 5); // Should be recent
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_error_recovery_mechanism (lines 528-556) ---[0m
    async fn test_error_recovery_mechanism() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create agent in error state
        let mut error_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/error",
            ClaudeConfig::for_agent("error-agent"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        error_agent.status = crate::agent::AgentStatus::Error("Test error".to_string());

        master.agents.insert("error-agent".to_string(), error_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was recovered
        let agent = master.agents.get("error-agent").unwrap();
        assert!(matches!(agent.status, crate::agent::AgentStatus::Available));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:418-461 function test_task_prioritization <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:603-629 function test_dependency_identification
  Similarity: 77.69%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_prioritization (lines 418-461) ---[0m
    async fn test_task_prioritization() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add tasks with different priorities
        let critical_task = Task::new(
            "critical-1".to_string(),
            "Fix production bug".to_string(),
            Priority::Critical,
            TaskType::Bug,
        );

        let medium_task = Task::new(
            "medium-1".to_string(),
            "Add new feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let low_task = Task::new(
            "low-1".to_string(),
            "Update documentation".to_string(),
            Priority::Low,
            TaskType::Documentation,
        );

        // Add in random order
        master.add_task(medium_task).await.unwrap();
        master.add_task(low_task).await.unwrap();
        master.add_task(critical_task).await.unwrap();

        // Verify tasks are properly tracked
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);

        // Find the critical task
        let critical_exists = state
            .pending_tasks
            .iter()
            .any(|t| t.priority == Priority::Critical);
        assert!(critical_exists);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_dependency_identification (lines 603-629) ---[0m
    async fn test_dependency_identification() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test various task descriptions
        let api_task = Task::new(
            "api-1".to_string(),
            "Create REST API endpoint for users".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let dependencies = master.identify_task_dependencies(&api_task).await;
        assert!(dependencies.contains(&"backend_api".to_string()));

        let ui_task = Task::new(
            "ui-1".to_string(),
            "Build UI component for dashboard".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let ui_deps = master.identify_task_dependencies(&ui_task).await;
        assert!(ui_deps.contains(&"frontend_components".to_string()));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:261-285 function test_proactive_task_generation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:418-461 function test_task_prioritization
  Similarity: 79.54%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_proactive_task_generation (lines 261-285) ---[0m
    async fn test_proactive_task_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add an objective for proactive tracking
        let obj_id = master
            .set_objective(
                "Complete user authentication".to_string(),
                "Implement full authentication flow".to_string(),
                None,
            )
            .await
            .unwrap();

        assert!(obj_id.starts_with("obj-"));

        // Trigger proactive analysis
        let decisions = master.trigger_proactive_analysis().await.unwrap();

        // In a real scenario, this would generate tasks based on objectives
        // For testing, we verify the mechanism works
        assert!(decisions.is_empty() || !decisions.is_empty()); // Either case is valid
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_prioritization (lines 418-461) ---[0m
    async fn test_task_prioritization() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add tasks with different priorities
        let critical_task = Task::new(
            "critical-1".to_string(),
            "Fix production bug".to_string(),
            Priority::Critical,
            TaskType::Bug,
        );

        let medium_task = Task::new(
            "medium-1".to_string(),
            "Add new feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let low_task = Task::new(
            "low-1".to_string(),
            "Update documentation".to_string(),
            Priority::Low,
            TaskType::Documentation,
        );

        // Add in random order
        master.add_task(medium_task).await.unwrap();
        master.add_task(low_task).await.unwrap();
        master.add_task(critical_task).await.unwrap();

        // Verify tasks are properly tracked
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 3);

        // Find the critical task
        let critical_exists = state
            .pending_tasks
            .iter()
            .any(|t| t.priority == Priority::Critical);
        assert!(critical_exists);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:128-161 function test_agent_task_delegation_with_metadata <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:603-629 function test_dependency_identification
  Similarity: 89.61%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_task_delegation_with_metadata (lines 128-161) ---[0m
    async fn test_agent_task_delegation_with_metadata() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create mock agents
        let frontend_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/frontend",
            ClaudeConfig::for_agent("frontend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("frontend-agent".to_string(), frontend_agent);

        // Create a frontend task
        let task = Task::new(
            "ui-task-1".to_string(),
            "Create login component with React".to_string(),
            Priority::High,
            TaskType::Development,
        );

        // Test task assignment logic
        let selected_agent = master.select_optimal_agent(&task).await.unwrap();
        assert_eq!(selected_agent, "frontend-agent");
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_dependency_identification (lines 603-629) ---[0m
    async fn test_dependency_identification() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test various task descriptions
        let api_task = Task::new(
            "api-1".to_string(),
            "Create REST API endpoint for users".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let dependencies = master.identify_task_dependencies(&api_task).await;
        assert!(dependencies.contains(&"backend_api".to_string()));

        let ui_task = Task::new(
            "ui-1".to_string(),
            "Build UI component for dashboard".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let ui_deps = master.identify_task_dependencies(&ui_task).await;
        assert!(ui_deps.contains(&"frontend_components".to_string()));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:128-161 function test_agent_task_delegation_with_metadata <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:165-196 function test_quality_review_triggers_remediation
  Similarity: 81.90%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_task_delegation_with_metadata (lines 128-161) ---[0m
    async fn test_agent_task_delegation_with_metadata() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create mock agents
        let frontend_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/frontend",
            ClaudeConfig::for_agent("frontend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("frontend-agent".to_string(), frontend_agent);

        // Create a frontend task
        let task = Task::new(
            "ui-task-1".to_string(),
            "Create login component with React".to_string(),
            Priority::High,
            TaskType::Development,
        );

        // Test task assignment logic
        let selected_agent = master.select_optimal_agent(&task).await.unwrap();
        assert_eq!(selected_agent, "frontend-agent");
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_quality_review_triggers_remediation (lines 165-196) ---[0m
    async fn test_quality_review_triggers_remediation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Simulate a quality issue
        let quality_message = AgentMessage::QualityIssue {
            agent_id: "backend-agent".to_string(),
            task_id: "api-task-1".to_string(),
            issues: vec![
                "Low test coverage".to_string(),
                "High complexity".to_string(),
                "Missing error handling".to_string(),
            ],
        };

        bus.send_message(quality_message).await.unwrap();

        // Allow time for message processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Check that remediation task was created in review history
        let state = master.state.read().await;
        assert!(state.review_history.contains_key("api-task-1"));

        let history_entries = &state.review_history["api-task-1"];
        assert_eq!(history_entries.len(), 1);
        assert_eq!(history_entries[0].issues_found.len(), 3);
        assert!(history_entries[0].remediation_task_id.is_some());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:165-196 function test_quality_review_triggers_remediation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:383-415 function test_agent_health_monitoring
  Similarity: 82.21%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_quality_review_triggers_remediation (lines 165-196) ---[0m
    async fn test_quality_review_triggers_remediation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Simulate a quality issue
        let quality_message = AgentMessage::QualityIssue {
            agent_id: "backend-agent".to_string(),
            task_id: "api-task-1".to_string(),
            issues: vec![
                "Low test coverage".to_string(),
                "High complexity".to_string(),
                "Missing error handling".to_string(),
            ],
        };

        bus.send_message(quality_message).await.unwrap();

        // Allow time for message processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Check that remediation task was created in review history
        let state = master.state.read().await;
        assert!(state.review_history.contains_key("api-task-1"));

        let history_entries = &state.review_history["api-task-1"];
        assert_eq!(history_entries.len(), 1);
        assert_eq!(history_entries[0].issues_found.len(), 3);
        assert!(history_entries[0].remediation_task_id.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_health_monitoring (lines 383-415) ---[0m
    async fn test_agent_health_monitoring() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create an agent with old last_activity
        let mut unhealthy_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend",
            ClaudeConfig::for_agent("backend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Set last activity to 10 minutes ago
        unhealthy_agent.last_activity = chrono::Utc::now() - chrono::Duration::seconds(600);

        master
            .agents
            .insert("unhealthy-agent".to_string(), unhealthy_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was restarted (last_activity updated)
        let agent = master.agents.get("unhealthy-agent").unwrap();
        let time_diff = chrono::Utc::now() - agent.last_activity;
        assert!(time_diff.num_seconds() < 5); // Should be recent
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:349-379 function test_concurrent_task_processing <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:383-415 function test_agent_health_monitoring
  Similarity: 82.89%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_concurrent_task_processing (lines 349-379) ---[0m
    async fn test_concurrent_task_processing() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create multiple tasks concurrently
        let mut handles = vec![];
        for i in 0..10 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("concurrent-task-{}", i),
                    format!("Task {} description", i),
                    Priority::Medium,
                    TaskType::Development,
                );
                master_clone.add_task(task).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all tasks to be added
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify all tasks were added
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 10);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_health_monitoring (lines 383-415) ---[0m
    async fn test_agent_health_monitoring() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create an agent with old last_activity
        let mut unhealthy_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend",
            ClaudeConfig::for_agent("backend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Set last activity to 10 minutes ago
        unhealthy_agent.last_activity = chrono::Utc::now() - chrono::Duration::seconds(600);

        master
            .agents
            .insert("unhealthy-agent".to_string(), unhealthy_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was restarted (last_activity updated)
        let agent = master.agents.get("unhealthy-agent").unwrap();
        let time_diff = chrono::Utc::now() - agent.last_activity;
        assert!(time_diff.num_seconds() < 5); // Should be recent
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:464-504 function test_status_report_generation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:632-664 function test_similar_task_matching
  Similarity: 71.42%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_status_report_generation (lines 464-504) ---[0m
    async fn test_status_report_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Add some agents
        for i in 0..3 {
            let agent = ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/agent-{}", i),
                ClaudeConfig::for_agent(&format!("agent-{}", i)),
                IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            master.agents.insert(format!("agent-{}", i), agent);
        }

        // Update orchestrator state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
            state.total_tasks_processed = 10;
            state.successful_tasks = 8;
            state.failed_tasks = 2;
        }

        // Generate report
        let report = master.generate_status_report().await.unwrap();

        assert_eq!(report.orchestrator_id, master.id);
        assert_eq!(report.status, OrchestratorStatus::Running);
        assert_eq!(report.total_agents, 3);
        assert_eq!(report.total_tasks_processed, 10);
        assert_eq!(report.successful_tasks, 8);
        assert_eq!(report.failed_tasks, 2);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_similar_task_matching (lines 632-664) ---[0m
    async fn test_similar_task_matching() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add some review history
        {
            let mut state = master.state.write().await;
            state.review_history.insert(
                "feature-auth-1".to_string(),
                vec![ReviewHistoryEntry {
                    task_id: "feature-auth-1".to_string(),
                    agent_id: "backend".to_string(),
                    review_date: chrono::Utc::now(),
                    issues_found: vec![],
                    remediation_task_id: None,
                    review_passed: true,
                    iteration: 1,
                }],
            );
        }

        let similar_task = Task::new(
            "new-feature-1".to_string(),
            "Another feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let similar = master.find_similar_completed_tasks(&similar_task).await;
        assert!(!similar.is_empty());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:603-629 function test_dependency_identification <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:632-664 function test_similar_task_matching
  Similarity: 87.53%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_dependency_identification (lines 603-629) ---[0m
    async fn test_dependency_identification() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test various task descriptions
        let api_task = Task::new(
            "api-1".to_string(),
            "Create REST API endpoint for users".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let dependencies = master.identify_task_dependencies(&api_task).await;
        assert!(dependencies.contains(&"backend_api".to_string()));

        let ui_task = Task::new(
            "ui-1".to_string(),
            "Build UI component for dashboard".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let ui_deps = master.identify_task_dependencies(&ui_task).await;
        assert!(ui_deps.contains(&"frontend_components".to_string()));
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_similar_task_matching (lines 632-664) ---[0m
    async fn test_similar_task_matching() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add some review history
        {
            let mut state = master.state.write().await;
            state.review_history.insert(
                "feature-auth-1".to_string(),
                vec![ReviewHistoryEntry {
                    task_id: "feature-auth-1".to_string(),
                    agent_id: "backend".to_string(),
                    review_date: chrono::Utc::now(),
                    issues_found: vec![],
                    remediation_task_id: None,
                    review_passed: true,
                    iteration: 1,
                }],
            );
        }

        let similar_task = Task::new(
            "new-feature-1".to_string(),
            "Another feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let similar = master.find_similar_completed_tasks(&similar_task).await;
        assert!(!similar.is_empty());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:165-196 function test_quality_review_triggers_remediation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:528-556 function test_error_recovery_mechanism
  Similarity: 84.57%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_quality_review_triggers_remediation (lines 165-196) ---[0m
    async fn test_quality_review_triggers_remediation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Simulate a quality issue
        let quality_message = AgentMessage::QualityIssue {
            agent_id: "backend-agent".to_string(),
            task_id: "api-task-1".to_string(),
            issues: vec![
                "Low test coverage".to_string(),
                "High complexity".to_string(),
                "Missing error handling".to_string(),
            ],
        };

        bus.send_message(quality_message).await.unwrap();

        // Allow time for message processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Check that remediation task was created in review history
        let state = master.state.read().await;
        assert!(state.review_history.contains_key("api-task-1"));

        let history_entries = &state.review_history["api-task-1"];
        assert_eq!(history_entries.len(), 1);
        assert_eq!(history_entries[0].issues_found.len(), 3);
        assert!(history_entries[0].remediation_task_id.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_error_recovery_mechanism (lines 528-556) ---[0m
    async fn test_error_recovery_mechanism() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create agent in error state
        let mut error_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/error",
            ClaudeConfig::for_agent("error-agent"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        error_agent.status = crate::agent::AgentStatus::Error("Test error".to_string());

        master.agents.insert("error-agent".to_string(), error_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was recovered
        let agent = master.agents.get("error-agent").unwrap();
        assert!(matches!(agent.status, crate::agent::AgentStatus::Available));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:464-504 function test_status_report_generation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:559-580 function test_graceful_shutdown
  Similarity: 81.35%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_status_report_generation (lines 464-504) ---[0m
    async fn test_status_report_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Add some agents
        for i in 0..3 {
            let agent = ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/agent-{}", i),
                ClaudeConfig::for_agent(&format!("agent-{}", i)),
                IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            master.agents.insert(format!("agent-{}", i), agent);
        }

        // Update orchestrator state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
            state.total_tasks_processed = 10;
            state.successful_tasks = 8;
            state.failed_tasks = 2;
        }

        // Generate report
        let report = master.generate_status_report().await.unwrap();

        assert_eq!(report.orchestrator_id, master.id);
        assert_eq!(report.status, OrchestratorStatus::Running);
        assert_eq!(report.total_agents, 3);
        assert_eq!(report.total_tasks_processed, 10);
        assert_eq!(report.successful_tasks, 8);
        assert_eq!(report.failed_tasks, 2);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_graceful_shutdown (lines 559-580) ---[0m
    async fn test_graceful_shutdown() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add a task
        let task = Task::new(
            "shutdown-test".to_string(),
            "Test task".to_string(),
            Priority::Low,
            TaskType::Testing,
        );
        master.add_task(task).await.unwrap();

        // Initiate shutdown
        master.shutdown().await.unwrap();

        // Verify shutdown state
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::ShuttingDown);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:103-124 function test_master_claude_with_orchestration_context <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:464-504 function test_status_report_generation
  Similarity: 81.07%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_master_claude_with_orchestration_context (lines 103-124) ---[0m
    async fn test_master_claude_with_orchestration_context() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test that orchestration context is properly set
        let task = Task::new(
            "test-task-1".to_string(),
            "Create user authentication flow".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        // Add task to queue
        master.add_task(task.clone()).await.unwrap();

        // Verify task was added to pending tasks
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
        assert_eq!(state.pending_tasks[0].id, task.id);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_status_report_generation (lines 464-504) ---[0m
    async fn test_status_report_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Add some agents
        for i in 0..3 {
            let agent = ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/agent-{}", i),
                ClaudeConfig::for_agent(&format!("agent-{}", i)),
                IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            master.agents.insert(format!("agent-{}", i), agent);
        }

        // Update orchestrator state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
            state.total_tasks_processed = 10;
            state.successful_tasks = 8;
            state.failed_tasks = 2;
        }

        // Generate report
        let report = master.generate_status_report().await.unwrap();

        assert_eq!(report.orchestrator_id, master.id);
        assert_eq!(report.status, OrchestratorStatus::Running);
        assert_eq!(report.total_agents, 3);
        assert_eq!(report.total_tasks_processed, 10);
        assert_eq!(report.successful_tasks, 8);
        assert_eq!(report.failed_tasks, 2);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:128-161 function test_agent_task_delegation_with_metadata <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:261-285 function test_proactive_task_generation
  Similarity: 86.34%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_task_delegation_with_metadata (lines 128-161) ---[0m
    async fn test_agent_task_delegation_with_metadata() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create mock agents
        let frontend_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/frontend",
            ClaudeConfig::for_agent("frontend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("frontend-agent".to_string(), frontend_agent);

        // Create a frontend task
        let task = Task::new(
            "ui-task-1".to_string(),
            "Create login component with React".to_string(),
            Priority::High,
            TaskType::Development,
        );

        // Test task assignment logic
        let selected_agent = master.select_optimal_agent(&task).await.unwrap();
        assert_eq!(selected_agent, "frontend-agent");
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_proactive_task_generation (lines 261-285) ---[0m
    async fn test_proactive_task_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add an objective for proactive tracking
        let obj_id = master
            .set_objective(
                "Complete user authentication".to_string(),
                "Implement full authentication flow".to_string(),
                None,
            )
            .await
            .unwrap();

        assert!(obj_id.starts_with("obj-"));

        // Trigger proactive analysis
        let decisions = master.trigger_proactive_analysis().await.unwrap();

        // In a real scenario, this would generate tasks based on objectives
        // For testing, we verify the mechanism works
        assert!(decisions.is_empty() || !decisions.is_empty()); // Either case is valid
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:464-504 function test_status_report_generation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:603-629 function test_dependency_identification
  Similarity: 74.71%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_status_report_generation (lines 464-504) ---[0m
    async fn test_status_report_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Add some agents
        for i in 0..3 {
            let agent = ClaudeCodeAgent::new_with_isolation(
                crate::identity::default_backend_role(),
                &repo_path,
                &format!("feature/agent-{}", i),
                ClaudeConfig::for_agent(&format!("agent-{}", i)),
                IsolationMode::GitWorktree,
            )
            .await
            .unwrap();

            master.agents.insert(format!("agent-{}", i), agent);
        }

        // Update orchestrator state
        {
            let mut state = master.state.write().await;
            state.status = OrchestratorStatus::Running;
            state.total_tasks_processed = 10;
            state.successful_tasks = 8;
            state.failed_tasks = 2;
        }

        // Generate report
        let report = master.generate_status_report().await.unwrap();

        assert_eq!(report.orchestrator_id, master.id);
        assert_eq!(report.status, OrchestratorStatus::Running);
        assert_eq!(report.total_agents, 3);
        assert_eq!(report.total_tasks_processed, 10);
        assert_eq!(report.successful_tasks, 8);
        assert_eq!(report.failed_tasks, 2);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_dependency_identification (lines 603-629) ---[0m
    async fn test_dependency_identification() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test various task descriptions
        let api_task = Task::new(
            "api-1".to_string(),
            "Create REST API endpoint for users".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let dependencies = master.identify_task_dependencies(&api_task).await;
        assert!(dependencies.contains(&"backend_api".to_string()));

        let ui_task = Task::new(
            "ui-1".to_string(),
            "Build UI component for dashboard".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let ui_deps = master.identify_task_dependencies(&ui_task).await;
        assert!(ui_deps.contains(&"frontend_components".to_string()));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:349-379 function test_concurrent_task_processing <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:603-629 function test_dependency_identification
  Similarity: 86.73%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_concurrent_task_processing (lines 349-379) ---[0m
    async fn test_concurrent_task_processing() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create multiple tasks concurrently
        let mut handles = vec![];
        for i in 0..10 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("concurrent-task-{}", i),
                    format!("Task {} description", i),
                    Priority::Medium,
                    TaskType::Development,
                );
                master_clone.add_task(task).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all tasks to be added
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify all tasks were added
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 10);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_dependency_identification (lines 603-629) ---[0m
    async fn test_dependency_identification() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test various task descriptions
        let api_task = Task::new(
            "api-1".to_string(),
            "Create REST API endpoint for users".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let dependencies = master.identify_task_dependencies(&api_task).await;
        assert!(dependencies.contains(&"backend_api".to_string()));

        let ui_task = Task::new(
            "ui-1".to_string(),
            "Build UI component for dashboard".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let ui_deps = master.identify_task_dependencies(&ui_task).await;
        assert!(ui_deps.contains(&"frontend_components".to_string()));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:128-161 function test_agent_task_delegation_with_metadata <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:559-580 function test_graceful_shutdown
  Similarity: 89.64%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_task_delegation_with_metadata (lines 128-161) ---[0m
    async fn test_agent_task_delegation_with_metadata() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create mock agents
        let frontend_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/frontend",
            ClaudeConfig::for_agent("frontend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("frontend-agent".to_string(), frontend_agent);

        // Create a frontend task
        let task = Task::new(
            "ui-task-1".to_string(),
            "Create login component with React".to_string(),
            Priority::High,
            TaskType::Development,
        );

        // Test task assignment logic
        let selected_agent = master.select_optimal_agent(&task).await.unwrap();
        assert_eq!(selected_agent, "frontend-agent");
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_graceful_shutdown (lines 559-580) ---[0m
    async fn test_graceful_shutdown() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add a task
        let task = Task::new(
            "shutdown-test".to_string(),
            "Test task".to_string(),
            Priority::Low,
            TaskType::Testing,
        );
        master.add_task(task).await.unwrap();

        // Initiate shutdown
        master.shutdown().await.unwrap();

        // Verify shutdown state
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::ShuttingDown);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:349-379 function test_concurrent_task_processing <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:528-556 function test_error_recovery_mechanism
  Similarity: 83.40%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_concurrent_task_processing (lines 349-379) ---[0m
    async fn test_concurrent_task_processing() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create multiple tasks concurrently
        let mut handles = vec![];
        for i in 0..10 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("concurrent-task-{}", i),
                    format!("Task {} description", i),
                    Priority::Medium,
                    TaskType::Development,
                );
                master_clone.add_task(task).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all tasks to be added
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify all tasks were added
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 10);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_error_recovery_mechanism (lines 528-556) ---[0m
    async fn test_error_recovery_mechanism() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create agent in error state
        let mut error_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/error",
            ClaudeConfig::for_agent("error-agent"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        error_agent.status = crate::agent::AgentStatus::Error("Test error".to_string());

        master.agents.insert("error-agent".to_string(), error_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was recovered
        let agent = master.agents.get("error-agent").unwrap();
        assert!(matches!(agent.status, crate::agent::AgentStatus::Available));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:261-285 function test_proactive_task_generation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:632-664 function test_similar_task_matching
  Similarity: 86.18%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_proactive_task_generation (lines 261-285) ---[0m
    async fn test_proactive_task_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add an objective for proactive tracking
        let obj_id = master
            .set_objective(
                "Complete user authentication".to_string(),
                "Implement full authentication flow".to_string(),
                None,
            )
            .await
            .unwrap();

        assert!(obj_id.starts_with("obj-"));

        // Trigger proactive analysis
        let decisions = master.trigger_proactive_analysis().await.unwrap();

        // In a real scenario, this would generate tasks based on objectives
        // For testing, we verify the mechanism works
        assert!(decisions.is_empty() || !decisions.is_empty()); // Either case is valid
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_similar_task_matching (lines 632-664) ---[0m
    async fn test_similar_task_matching() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add some review history
        {
            let mut state = master.state.write().await;
            state.review_history.insert(
                "feature-auth-1".to_string(),
                vec![ReviewHistoryEntry {
                    task_id: "feature-auth-1".to_string(),
                    agent_id: "backend".to_string(),
                    review_date: chrono::Utc::now(),
                    issues_found: vec![],
                    remediation_task_id: None,
                    review_passed: true,
                    iteration: 1,
                }],
            );
        }

        let similar_task = Task::new(
            "new-feature-1".to_string(),
            "Another feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let similar = master.find_similar_completed_tasks(&similar_task).await;
        assert!(!similar.is_empty());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:383-415 function test_agent_health_monitoring <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:603-629 function test_dependency_identification
  Similarity: 82.93%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_health_monitoring (lines 383-415) ---[0m
    async fn test_agent_health_monitoring() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create an agent with old last_activity
        let mut unhealthy_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend",
            ClaudeConfig::for_agent("backend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Set last activity to 10 minutes ago
        unhealthy_agent.last_activity = chrono::Utc::now() - chrono::Duration::seconds(600);

        master
            .agents
            .insert("unhealthy-agent".to_string(), unhealthy_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was restarted (last_activity updated)
        let agent = master.agents.get("unhealthy-agent").unwrap();
        let time_diff = chrono::Utc::now() - agent.last_activity;
        assert!(time_diff.num_seconds() < 5); // Should be recent
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_dependency_identification (lines 603-629) ---[0m
    async fn test_dependency_identification() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test various task descriptions
        let api_task = Task::new(
            "api-1".to_string(),
            "Create REST API endpoint for users".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let dependencies = master.identify_task_dependencies(&api_task).await;
        assert!(dependencies.contains(&"backend_api".to_string()));

        let ui_task = Task::new(
            "ui-1".to_string(),
            "Build UI component for dashboard".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let ui_deps = master.identify_task_dependencies(&ui_task).await;
        assert!(ui_deps.contains(&"frontend_components".to_string()));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:103-124 function test_master_claude_with_orchestration_context <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:128-161 function test_agent_task_delegation_with_metadata
  Similarity: 88.39%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_master_claude_with_orchestration_context (lines 103-124) ---[0m
    async fn test_master_claude_with_orchestration_context() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test that orchestration context is properly set
        let task = Task::new(
            "test-task-1".to_string(),
            "Create user authentication flow".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        // Add task to queue
        master.add_task(task.clone()).await.unwrap();

        // Verify task was added to pending tasks
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
        assert_eq!(state.pending_tasks[0].id, task.id);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_task_delegation_with_metadata (lines 128-161) ---[0m
    async fn test_agent_task_delegation_with_metadata() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create mock agents
        let frontend_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/frontend",
            ClaudeConfig::for_agent("frontend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("frontend-agent".to_string(), frontend_agent);

        // Create a frontend task
        let task = Task::new(
            "ui-task-1".to_string(),
            "Create login component with React".to_string(),
            Priority::High,
            TaskType::Development,
        );

        // Test task assignment logic
        let selected_agent = master.select_optimal_agent(&task).await.unwrap();
        assert_eq!(selected_agent, "frontend-agent");
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:383-415 function test_agent_health_monitoring <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:559-580 function test_graceful_shutdown
  Similarity: 89.94%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_health_monitoring (lines 383-415) ---[0m
    async fn test_agent_health_monitoring() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create an agent with old last_activity
        let mut unhealthy_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend",
            ClaudeConfig::for_agent("backend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Set last activity to 10 minutes ago
        unhealthy_agent.last_activity = chrono::Utc::now() - chrono::Duration::seconds(600);

        master
            .agents
            .insert("unhealthy-agent".to_string(), unhealthy_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was restarted (last_activity updated)
        let agent = master.agents.get("unhealthy-agent").unwrap();
        let time_diff = chrono::Utc::now() - agent.last_activity;
        assert!(time_diff.num_seconds() < 5); // Should be recent
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_graceful_shutdown (lines 559-580) ---[0m
    async fn test_graceful_shutdown() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add a task
        let task = Task::new(
            "shutdown-test".to_string(),
            "Test task".to_string(),
            Priority::Low,
            TaskType::Testing,
        );
        master.add_task(task).await.unwrap();

        // Initiate shutdown
        master.shutdown().await.unwrap();

        // Verify shutdown state
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::ShuttingDown);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:165-196 function test_quality_review_triggers_remediation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:349-379 function test_concurrent_task_processing
  Similarity: 78.19%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_quality_review_triggers_remediation (lines 165-196) ---[0m
    async fn test_quality_review_triggers_remediation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Simulate a quality issue
        let quality_message = AgentMessage::QualityIssue {
            agent_id: "backend-agent".to_string(),
            task_id: "api-task-1".to_string(),
            issues: vec![
                "Low test coverage".to_string(),
                "High complexity".to_string(),
                "Missing error handling".to_string(),
            ],
        };

        bus.send_message(quality_message).await.unwrap();

        // Allow time for message processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Check that remediation task was created in review history
        let state = master.state.read().await;
        assert!(state.review_history.contains_key("api-task-1"));

        let history_entries = &state.review_history["api-task-1"];
        assert_eq!(history_entries.len(), 1);
        assert_eq!(history_entries[0].issues_found.len(), 3);
        assert!(history_entries[0].remediation_task_id.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_concurrent_task_processing (lines 349-379) ---[0m
    async fn test_concurrent_task_processing() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create multiple tasks concurrently
        let mut handles = vec![];
        for i in 0..10 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("concurrent-task-{}", i),
                    format!("Task {} description", i),
                    Priority::Medium,
                    TaskType::Development,
                );
                master_clone.add_task(task).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all tasks to be added
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify all tasks were added
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 10);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:103-124 function test_master_claude_with_orchestration_context <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:349-379 function test_concurrent_task_processing
  Similarity: 92.77%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_master_claude_with_orchestration_context (lines 103-124) ---[0m
    async fn test_master_claude_with_orchestration_context() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test that orchestration context is properly set
        let task = Task::new(
            "test-task-1".to_string(),
            "Create user authentication flow".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        // Add task to queue
        master.add_task(task.clone()).await.unwrap();

        // Verify task was added to pending tasks
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
        assert_eq!(state.pending_tasks[0].id, task.id);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_concurrent_task_processing (lines 349-379) ---[0m
    async fn test_concurrent_task_processing() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create multiple tasks concurrently
        let mut handles = vec![];
        for i in 0..10 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("concurrent-task-{}", i),
                    format!("Task {} description", i),
                    Priority::Medium,
                    TaskType::Development,
                );
                master_clone.add_task(task).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all tasks to be added
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify all tasks were added
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 10);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:349-379 function test_concurrent_task_processing <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:559-580 function test_graceful_shutdown
  Similarity: 92.77%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_concurrent_task_processing (lines 349-379) ---[0m
    async fn test_concurrent_task_processing() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create multiple tasks concurrently
        let mut handles = vec![];
        for i in 0..10 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("concurrent-task-{}", i),
                    format!("Task {} description", i),
                    Priority::Medium,
                    TaskType::Development,
                );
                master_clone.add_task(task).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all tasks to be added
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify all tasks were added
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 10);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_graceful_shutdown (lines 559-580) ---[0m
    async fn test_graceful_shutdown() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add a task
        let task = Task::new(
            "shutdown-test".to_string(),
            "Test task".to_string(),
            Priority::Low,
            TaskType::Testing,
        );
        master.add_task(task).await.unwrap();

        // Initiate shutdown
        master.shutdown().await.unwrap();

        // Verify shutdown state
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::ShuttingDown);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:261-285 function test_proactive_task_generation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:383-415 function test_agent_health_monitoring
  Similarity: 84.66%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_proactive_task_generation (lines 261-285) ---[0m
    async fn test_proactive_task_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add an objective for proactive tracking
        let obj_id = master
            .set_objective(
                "Complete user authentication".to_string(),
                "Implement full authentication flow".to_string(),
                None,
            )
            .await
            .unwrap();

        assert!(obj_id.starts_with("obj-"));

        // Trigger proactive analysis
        let decisions = master.trigger_proactive_analysis().await.unwrap();

        // In a real scenario, this would generate tasks based on objectives
        // For testing, we verify the mechanism works
        assert!(decisions.is_empty() || !decisions.is_empty()); // Either case is valid
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_health_monitoring (lines 383-415) ---[0m
    async fn test_agent_health_monitoring() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create an agent with old last_activity
        let mut unhealthy_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend",
            ClaudeConfig::for_agent("backend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Set last activity to 10 minutes ago
        unhealthy_agent.last_activity = chrono::Utc::now() - chrono::Duration::seconds(600);

        master
            .agents
            .insert("unhealthy-agent".to_string(), unhealthy_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was restarted (last_activity updated)
        let agent = master.agents.get("unhealthy-agent").unwrap();
        let time_diff = chrono::Utc::now() - agent.last_activity;
        assert!(time_diff.num_seconds() < 5); // Should be recent
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:528-556 function test_error_recovery_mechanism <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:603-629 function test_dependency_identification
  Similarity: 87.21%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_error_recovery_mechanism (lines 528-556) ---[0m
    async fn test_error_recovery_mechanism() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create agent in error state
        let mut error_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/error",
            ClaudeConfig::for_agent("error-agent"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        error_agent.status = crate::agent::AgentStatus::Error("Test error".to_string());

        master.agents.insert("error-agent".to_string(), error_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was recovered
        let agent = master.agents.get("error-agent").unwrap();
        assert!(matches!(agent.status, crate::agent::AgentStatus::Available));
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_dependency_identification (lines 603-629) ---[0m
    async fn test_dependency_identification() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test various task descriptions
        let api_task = Task::new(
            "api-1".to_string(),
            "Create REST API endpoint for users".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let dependencies = master.identify_task_dependencies(&api_task).await;
        assert!(dependencies.contains(&"backend_api".to_string()));

        let ui_task = Task::new(
            "ui-1".to_string(),
            "Build UI component for dashboard".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let ui_deps = master.identify_task_dependencies(&ui_task).await;
        assert!(ui_deps.contains(&"frontend_components".to_string()));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:559-580 function test_graceful_shutdown <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:632-664 function test_similar_task_matching
  Similarity: 88.40%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_graceful_shutdown (lines 559-580) ---[0m
    async fn test_graceful_shutdown() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add a task
        let task = Task::new(
            "shutdown-test".to_string(),
            "Test task".to_string(),
            Priority::Low,
            TaskType::Testing,
        );
        master.add_task(task).await.unwrap();

        // Initiate shutdown
        master.shutdown().await.unwrap();

        // Verify shutdown state
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::ShuttingDown);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_similar_task_matching (lines 632-664) ---[0m
    async fn test_similar_task_matching() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add some review history
        {
            let mut state = master.state.write().await;
            state.review_history.insert(
                "feature-auth-1".to_string(),
                vec![ReviewHistoryEntry {
                    task_id: "feature-auth-1".to_string(),
                    agent_id: "backend".to_string(),
                    review_date: chrono::Utc::now(),
                    issues_found: vec![],
                    remediation_task_id: None,
                    review_passed: true,
                    iteration: 1,
                }],
            );
        }

        let similar_task = Task::new(
            "new-feature-1".to_string(),
            "Another feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let similar = master.find_similar_completed_tasks(&similar_task).await;
        assert!(!similar.is_empty());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:383-415 function test_agent_health_monitoring <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:632-664 function test_similar_task_matching
  Similarity: 73.15%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_health_monitoring (lines 383-415) ---[0m
    async fn test_agent_health_monitoring() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create an agent with old last_activity
        let mut unhealthy_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend",
            ClaudeConfig::for_agent("backend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Set last activity to 10 minutes ago
        unhealthy_agent.last_activity = chrono::Utc::now() - chrono::Duration::seconds(600);

        master
            .agents
            .insert("unhealthy-agent".to_string(), unhealthy_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was restarted (last_activity updated)
        let agent = master.agents.get("unhealthy-agent").unwrap();
        let time_diff = chrono::Utc::now() - agent.last_activity;
        assert!(time_diff.num_seconds() < 5); // Should be recent
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_similar_task_matching (lines 632-664) ---[0m
    async fn test_similar_task_matching() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add some review history
        {
            let mut state = master.state.write().await;
            state.review_history.insert(
                "feature-auth-1".to_string(),
                vec![ReviewHistoryEntry {
                    task_id: "feature-auth-1".to_string(),
                    agent_id: "backend".to_string(),
                    review_date: chrono::Utc::now(),
                    issues_found: vec![],
                    remediation_task_id: None,
                    review_passed: true,
                    iteration: 1,
                }],
            );
        }

        let similar_task = Task::new(
            "new-feature-1".to_string(),
            "Another feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let similar = master.find_similar_completed_tasks(&similar_task).await;
        assert!(!similar.is_empty());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:528-556 function test_error_recovery_mechanism <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:632-664 function test_similar_task_matching
  Similarity: 77.61%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_error_recovery_mechanism (lines 528-556) ---[0m
    async fn test_error_recovery_mechanism() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create agent in error state
        let mut error_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/error",
            ClaudeConfig::for_agent("error-agent"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        error_agent.status = crate::agent::AgentStatus::Error("Test error".to_string());

        master.agents.insert("error-agent".to_string(), error_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was recovered
        let agent = master.agents.get("error-agent").unwrap();
        assert!(matches!(agent.status, crate::agent::AgentStatus::Available));
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_similar_task_matching (lines 632-664) ---[0m
    async fn test_similar_task_matching() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add some review history
        {
            let mut state = master.state.write().await;
            state.review_history.insert(
                "feature-auth-1".to_string(),
                vec![ReviewHistoryEntry {
                    task_id: "feature-auth-1".to_string(),
                    agent_id: "backend".to_string(),
                    review_date: chrono::Utc::now(),
                    issues_found: vec![],
                    remediation_task_id: None,
                    review_passed: true,
                    iteration: 1,
                }],
            );
        }

        let similar_task = Task::new(
            "new-feature-1".to_string(),
            "Another feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let similar = master.find_similar_completed_tasks(&similar_task).await;
        assert!(!similar.is_empty());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:103-124 function test_master_claude_with_orchestration_context <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:383-415 function test_agent_health_monitoring
  Similarity: 87.30%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_master_claude_with_orchestration_context (lines 103-124) ---[0m
    async fn test_master_claude_with_orchestration_context() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test that orchestration context is properly set
        let task = Task::new(
            "test-task-1".to_string(),
            "Create user authentication flow".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        // Add task to queue
        master.add_task(task.clone()).await.unwrap();

        // Verify task was added to pending tasks
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
        assert_eq!(state.pending_tasks[0].id, task.id);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_health_monitoring (lines 383-415) ---[0m
    async fn test_agent_health_monitoring() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create an agent with old last_activity
        let mut unhealthy_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend",
            ClaudeConfig::for_agent("backend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Set last activity to 10 minutes ago
        unhealthy_agent.last_activity = chrono::Utc::now() - chrono::Duration::seconds(600);

        master
            .agents
            .insert("unhealthy-agent".to_string(), unhealthy_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was restarted (last_activity updated)
        let agent = master.agents.get("unhealthy-agent").unwrap();
        let time_diff = chrono::Utc::now() - agent.last_activity;
        assert!(time_diff.num_seconds() < 5); // Should be recent
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:103-124 function test_master_claude_with_orchestration_context <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:632-664 function test_similar_task_matching
  Similarity: 86.91%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_master_claude_with_orchestration_context (lines 103-124) ---[0m
    async fn test_master_claude_with_orchestration_context() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test that orchestration context is properly set
        let task = Task::new(
            "test-task-1".to_string(),
            "Create user authentication flow".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        // Add task to queue
        master.add_task(task.clone()).await.unwrap();

        // Verify task was added to pending tasks
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
        assert_eq!(state.pending_tasks[0].id, task.id);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_similar_task_matching (lines 632-664) ---[0m
    async fn test_similar_task_matching() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add some review history
        {
            let mut state = master.state.write().await;
            state.review_history.insert(
                "feature-auth-1".to_string(),
                vec![ReviewHistoryEntry {
                    task_id: "feature-auth-1".to_string(),
                    agent_id: "backend".to_string(),
                    review_date: chrono::Utc::now(),
                    issues_found: vec![],
                    remediation_task_id: None,
                    review_passed: true,
                    iteration: 1,
                }],
            );
        }

        let similar_task = Task::new(
            "new-feature-1".to_string(),
            "Another feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let similar = master.find_similar_completed_tasks(&similar_task).await;
        assert!(!similar.is_empty());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:261-285 function test_proactive_task_generation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:349-379 function test_concurrent_task_processing
  Similarity: 82.48%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_proactive_task_generation (lines 261-285) ---[0m
    async fn test_proactive_task_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add an objective for proactive tracking
        let obj_id = master
            .set_objective(
                "Complete user authentication".to_string(),
                "Implement full authentication flow".to_string(),
                None,
            )
            .await
            .unwrap();

        assert!(obj_id.starts_with("obj-"));

        // Trigger proactive analysis
        let decisions = master.trigger_proactive_analysis().await.unwrap();

        // In a real scenario, this would generate tasks based on objectives
        // For testing, we verify the mechanism works
        assert!(decisions.is_empty() || !decisions.is_empty()); // Either case is valid
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_concurrent_task_processing (lines 349-379) ---[0m
    async fn test_concurrent_task_processing() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create multiple tasks concurrently
        let mut handles = vec![];
        for i in 0..10 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("concurrent-task-{}", i),
                    format!("Task {} description", i),
                    Priority::Medium,
                    TaskType::Development,
                );
                master_clone.add_task(task).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all tasks to be added
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify all tasks were added
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 10);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:528-556 function test_error_recovery_mechanism <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:559-580 function test_graceful_shutdown
  Similarity: 90.24%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_error_recovery_mechanism (lines 528-556) ---[0m
    async fn test_error_recovery_mechanism() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create agent in error state
        let mut error_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/error",
            ClaudeConfig::for_agent("error-agent"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        error_agent.status = crate::agent::AgentStatus::Error("Test error".to_string());

        master.agents.insert("error-agent".to_string(), error_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was recovered
        let agent = master.agents.get("error-agent").unwrap();
        assert!(matches!(agent.status, crate::agent::AgentStatus::Available));
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_graceful_shutdown (lines 559-580) ---[0m
    async fn test_graceful_shutdown() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add a task
        let task = Task::new(
            "shutdown-test".to_string(),
            "Test task".to_string(),
            Priority::Low,
            TaskType::Testing,
        );
        master.add_task(task).await.unwrap();

        // Initiate shutdown
        master.shutdown().await.unwrap();

        // Verify shutdown state
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::ShuttingDown);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:507-524 function test_milestone_tracking <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:632-664 function test_similar_task_matching
  Similarity: 89.85%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_milestone_tracking (lines 507-524) ---[0m
    async fn test_milestone_tracking() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add milestone
        let milestone_id = master
            .add_milestone(
                "MVP Release".to_string(),
                "Complete minimum viable product".to_string(),
                Some(chrono::Utc::now() + chrono::Duration::days(30)),
            )
            .await
            .unwrap();

        assert!(milestone_id.starts_with("milestone-"));
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_similar_task_matching (lines 632-664) ---[0m
    async fn test_similar_task_matching() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add some review history
        {
            let mut state = master.state.write().await;
            state.review_history.insert(
                "feature-auth-1".to_string(),
                vec![ReviewHistoryEntry {
                    task_id: "feature-auth-1".to_string(),
                    agent_id: "backend".to_string(),
                    review_date: chrono::Utc::now(),
                    issues_found: vec![],
                    remediation_task_id: None,
                    review_passed: true,
                    iteration: 1,
                }],
            );
        }

        let similar_task = Task::new(
            "new-feature-1".to_string(),
            "Another feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let similar = master.find_similar_completed_tasks(&similar_task).await;
        assert!(!similar.is_empty());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:261-285 function test_proactive_task_generation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:528-556 function test_error_recovery_mechanism
  Similarity: 84.48%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_proactive_task_generation (lines 261-285) ---[0m
    async fn test_proactive_task_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add an objective for proactive tracking
        let obj_id = master
            .set_objective(
                "Complete user authentication".to_string(),
                "Implement full authentication flow".to_string(),
                None,
            )
            .await
            .unwrap();

        assert!(obj_id.starts_with("obj-"));

        // Trigger proactive analysis
        let decisions = master.trigger_proactive_analysis().await.unwrap();

        // In a real scenario, this would generate tasks based on objectives
        // For testing, we verify the mechanism works
        assert!(decisions.is_empty() || !decisions.is_empty()); // Either case is valid
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_error_recovery_mechanism (lines 528-556) ---[0m
    async fn test_error_recovery_mechanism() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create agent in error state
        let mut error_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/error",
            ClaudeConfig::for_agent("error-agent"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        error_agent.status = crate::agent::AgentStatus::Error("Test error".to_string());

        master.agents.insert("error-agent".to_string(), error_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was recovered
        let agent = master.agents.get("error-agent").unwrap();
        assert!(matches!(agent.status, crate::agent::AgentStatus::Available));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:583-600 function test_task_metadata_enhancement <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:632-664 function test_similar_task_matching
  Similarity: 88.65%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_metadata_enhancement (lines 583-600) ---[0m
    async fn test_task_metadata_enhancement() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test task complexity detection
        let feature_task = Task::new(
            "feature-1".to_string(),
            "Implement complex feature".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        let insights = master.get_proactive_insights_for_task(&feature_task).await;
        assert_eq!(insights["task_complexity"], "high");
        assert_eq!(insights["recommended_approach"], "careful_planning");
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_similar_task_matching (lines 632-664) ---[0m
    async fn test_similar_task_matching() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add some review history
        {
            let mut state = master.state.write().await;
            state.review_history.insert(
                "feature-auth-1".to_string(),
                vec![ReviewHistoryEntry {
                    task_id: "feature-auth-1".to_string(),
                    agent_id: "backend".to_string(),
                    review_date: chrono::Utc::now(),
                    issues_found: vec![],
                    remediation_task_id: None,
                    review_passed: true,
                    iteration: 1,
                }],
            );
        }

        let similar_task = Task::new(
            "new-feature-1".to_string(),
            "Another feature".to_string(),
            Priority::Medium,
            TaskType::Feature,
        );

        let similar = master.find_similar_completed_tasks(&similar_task).await;
        assert!(!similar.is_empty());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:103-124 function test_master_claude_with_orchestration_context <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:528-556 function test_error_recovery_mechanism
  Similarity: 87.41%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_master_claude_with_orchestration_context (lines 103-124) ---[0m
    async fn test_master_claude_with_orchestration_context() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test that orchestration context is properly set
        let task = Task::new(
            "test-task-1".to_string(),
            "Create user authentication flow".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        // Add task to queue
        master.add_task(task.clone()).await.unwrap();

        // Verify task was added to pending tasks
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
        assert_eq!(state.pending_tasks[0].id, task.id);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_error_recovery_mechanism (lines 528-556) ---[0m
    async fn test_error_recovery_mechanism() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create agent in error state
        let mut error_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/error",
            ClaudeConfig::for_agent("error-agent"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        error_agent.status = crate::agent::AgentStatus::Error("Test error".to_string());

        master.agents.insert("error-agent".to_string(), error_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was recovered
        let agent = master.agents.get("error-agent").unwrap();
        assert!(matches!(agent.status, crate::agent::AgentStatus::Available));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:559-580 function test_graceful_shutdown <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:603-629 function test_dependency_identification
  Similarity: 90.47%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_graceful_shutdown (lines 559-580) ---[0m
    async fn test_graceful_shutdown() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add a task
        let task = Task::new(
            "shutdown-test".to_string(),
            "Test task".to_string(),
            Priority::Low,
            TaskType::Testing,
        );
        master.add_task(task).await.unwrap();

        // Initiate shutdown
        master.shutdown().await.unwrap();

        // Verify shutdown state
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::ShuttingDown);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_dependency_identification (lines 603-629) ---[0m
    async fn test_dependency_identification() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test various task descriptions
        let api_task = Task::new(
            "api-1".to_string(),
            "Create REST API endpoint for users".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let dependencies = master.identify_task_dependencies(&api_task).await;
        assert!(dependencies.contains(&"backend_api".to_string()));

        let ui_task = Task::new(
            "ui-1".to_string(),
            "Build UI component for dashboard".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let ui_deps = master.identify_task_dependencies(&ui_task).await;
        assert!(ui_deps.contains(&"frontend_components".to_string()));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:128-161 function test_agent_task_delegation_with_metadata <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:583-600 function test_task_metadata_enhancement
  Similarity: 85.23%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_task_delegation_with_metadata (lines 128-161) ---[0m
    async fn test_agent_task_delegation_with_metadata() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create mock agents
        let frontend_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/frontend",
            ClaudeConfig::for_agent("frontend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("frontend-agent".to_string(), frontend_agent);

        // Create a frontend task
        let task = Task::new(
            "ui-task-1".to_string(),
            "Create login component with React".to_string(),
            Priority::High,
            TaskType::Development,
        );

        // Test task assignment logic
        let selected_agent = master.select_optimal_agent(&task).await.unwrap();
        assert_eq!(selected_agent, "frontend-agent");
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_metadata_enhancement (lines 583-600) ---[0m
    async fn test_task_metadata_enhancement() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test task complexity detection
        let feature_task = Task::new(
            "feature-1".to_string(),
            "Implement complex feature".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        let insights = master.get_proactive_insights_for_task(&feature_task).await;
        assert_eq!(insights["task_complexity"], "high");
        assert_eq!(insights["recommended_approach"], "careful_planning");
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:165-196 function test_quality_review_triggers_remediation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:261-285 function test_proactive_task_generation
  Similarity: 76.95%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_quality_review_triggers_remediation (lines 165-196) ---[0m
    async fn test_quality_review_triggers_remediation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Simulate a quality issue
        let quality_message = AgentMessage::QualityIssue {
            agent_id: "backend-agent".to_string(),
            task_id: "api-task-1".to_string(),
            issues: vec![
                "Low test coverage".to_string(),
                "High complexity".to_string(),
                "Missing error handling".to_string(),
            ],
        };

        bus.send_message(quality_message).await.unwrap();

        // Allow time for message processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Check that remediation task was created in review history
        let state = master.state.read().await;
        assert!(state.review_history.contains_key("api-task-1"));

        let history_entries = &state.review_history["api-task-1"];
        assert_eq!(history_entries.len(), 1);
        assert_eq!(history_entries[0].issues_found.len(), 3);
        assert!(history_entries[0].remediation_task_id.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_proactive_task_generation (lines 261-285) ---[0m
    async fn test_proactive_task_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add an objective for proactive tracking
        let obj_id = master
            .set_objective(
                "Complete user authentication".to_string(),
                "Implement full authentication flow".to_string(),
                None,
            )
            .await
            .unwrap();

        assert!(obj_id.starts_with("obj-"));

        // Trigger proactive analysis
        let decisions = master.trigger_proactive_analysis().await.unwrap();

        // In a real scenario, this would generate tasks based on objectives
        // For testing, we verify the mechanism works
        assert!(decisions.is_empty() || !decisions.is_empty()); // Either case is valid
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:165-196 function test_quality_review_triggers_remediation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:559-580 function test_graceful_shutdown
  Similarity: 81.15%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_quality_review_triggers_remediation (lines 165-196) ---[0m
    async fn test_quality_review_triggers_remediation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Simulate a quality issue
        let quality_message = AgentMessage::QualityIssue {
            agent_id: "backend-agent".to_string(),
            task_id: "api-task-1".to_string(),
            issues: vec![
                "Low test coverage".to_string(),
                "High complexity".to_string(),
                "Missing error handling".to_string(),
            ],
        };

        bus.send_message(quality_message).await.unwrap();

        // Allow time for message processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Check that remediation task was created in review history
        let state = master.state.read().await;
        assert!(state.review_history.contains_key("api-task-1"));

        let history_entries = &state.review_history["api-task-1"];
        assert_eq!(history_entries.len(), 1);
        assert_eq!(history_entries[0].issues_found.len(), 3);
        assert!(history_entries[0].remediation_task_id.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_graceful_shutdown (lines 559-580) ---[0m
    async fn test_graceful_shutdown() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add a task
        let task = Task::new(
            "shutdown-test".to_string(),
            "Test task".to_string(),
            Priority::Low,
            TaskType::Testing,
        );
        master.add_task(task).await.unwrap();

        // Initiate shutdown
        master.shutdown().await.unwrap();

        // Verify shutdown state
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::ShuttingDown);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:165-196 function test_quality_review_triggers_remediation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:603-629 function test_dependency_identification
  Similarity: 74.11%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_quality_review_triggers_remediation (lines 165-196) ---[0m
    async fn test_quality_review_triggers_remediation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Simulate a quality issue
        let quality_message = AgentMessage::QualityIssue {
            agent_id: "backend-agent".to_string(),
            task_id: "api-task-1".to_string(),
            issues: vec![
                "Low test coverage".to_string(),
                "High complexity".to_string(),
                "Missing error handling".to_string(),
            ],
        };

        bus.send_message(quality_message).await.unwrap();

        // Allow time for message processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Check that remediation task was created in review history
        let state = master.state.read().await;
        assert!(state.review_history.contains_key("api-task-1"));

        let history_entries = &state.review_history["api-task-1"];
        assert_eq!(history_entries.len(), 1);
        assert_eq!(history_entries[0].issues_found.len(), 3);
        assert!(history_entries[0].remediation_task_id.is_some());
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_dependency_identification (lines 603-629) ---[0m
    async fn test_dependency_identification() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test various task descriptions
        let api_task = Task::new(
            "api-1".to_string(),
            "Create REST API endpoint for users".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let dependencies = master.identify_task_dependencies(&api_task).await;
        assert!(dependencies.contains(&"backend_api".to_string()));

        let ui_task = Task::new(
            "ui-1".to_string(),
            "Build UI component for dashboard".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let ui_deps = master.identify_task_dependencies(&ui_task).await;
        assert!(ui_deps.contains(&"frontend_components".to_string()));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:261-285 function test_proactive_task_generation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:603-629 function test_dependency_identification
  Similarity: 84.07%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_proactive_task_generation (lines 261-285) ---[0m
    async fn test_proactive_task_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add an objective for proactive tracking
        let obj_id = master
            .set_objective(
                "Complete user authentication".to_string(),
                "Implement full authentication flow".to_string(),
                None,
            )
            .await
            .unwrap();

        assert!(obj_id.starts_with("obj-"));

        // Trigger proactive analysis
        let decisions = master.trigger_proactive_analysis().await.unwrap();

        // In a real scenario, this would generate tasks based on objectives
        // For testing, we verify the mechanism works
        assert!(decisions.is_empty() || !decisions.is_empty()); // Either case is valid
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_dependency_identification (lines 603-629) ---[0m
    async fn test_dependency_identification() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test various task descriptions
        let api_task = Task::new(
            "api-1".to_string(),
            "Create REST API endpoint for users".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let dependencies = master.identify_task_dependencies(&api_task).await;
        assert!(dependencies.contains(&"backend_api".to_string()));

        let ui_task = Task::new(
            "ui-1".to_string(),
            "Build UI component for dashboard".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let ui_deps = master.identify_task_dependencies(&ui_task).await;
        assert!(ui_deps.contains(&"frontend_components".to_string()));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:103-124 function test_master_claude_with_orchestration_context <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:165-196 function test_quality_review_triggers_remediation
  Similarity: 80.95%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_master_claude_with_orchestration_context (lines 103-124) ---[0m
    async fn test_master_claude_with_orchestration_context() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test that orchestration context is properly set
        let task = Task::new(
            "test-task-1".to_string(),
            "Create user authentication flow".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        // Add task to queue
        master.add_task(task.clone()).await.unwrap();

        // Verify task was added to pending tasks
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
        assert_eq!(state.pending_tasks[0].id, task.id);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_quality_review_triggers_remediation (lines 165-196) ---[0m
    async fn test_quality_review_triggers_remediation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();
        let bus = master.coordination_bus.clone();

        // Simulate a quality issue
        let quality_message = AgentMessage::QualityIssue {
            agent_id: "backend-agent".to_string(),
            task_id: "api-task-1".to_string(),
            issues: vec![
                "Low test coverage".to_string(),
                "High complexity".to_string(),
                "Missing error handling".to_string(),
            ],
        };

        bus.send_message(quality_message).await.unwrap();

        // Allow time for message processing
        tokio::time::sleep(tokio::time::Duration::from_millis(100)).await;

        // Check that remediation task was created in review history
        let state = master.state.read().await;
        assert!(state.review_history.contains_key("api-task-1"));

        let history_entries = &state.review_history["api-task-1"];
        assert_eq!(history_entries.len(), 1);
        assert_eq!(history_entries[0].issues_found.len(), 3);
        assert!(history_entries[0].remediation_task_id.is_some());
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:103-124 function test_master_claude_with_orchestration_context <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:603-629 function test_dependency_identification
  Similarity: 88.88%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_master_claude_with_orchestration_context (lines 103-124) ---[0m
    async fn test_master_claude_with_orchestration_context() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test that orchestration context is properly set
        let task = Task::new(
            "test-task-1".to_string(),
            "Create user authentication flow".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        // Add task to queue
        master.add_task(task.clone()).await.unwrap();

        // Verify task was added to pending tasks
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
        assert_eq!(state.pending_tasks[0].id, task.id);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_dependency_identification (lines 603-629) ---[0m
    async fn test_dependency_identification() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test various task descriptions
        let api_task = Task::new(
            "api-1".to_string(),
            "Create REST API endpoint for users".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let dependencies = master.identify_task_dependencies(&api_task).await;
        assert!(dependencies.contains(&"backend_api".to_string()));

        let ui_task = Task::new(
            "ui-1".to_string(),
            "Build UI component for dashboard".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let ui_deps = master.identify_task_dependencies(&ui_task).await;
        assert!(ui_deps.contains(&"frontend_components".to_string()));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:349-379 function test_concurrent_task_processing <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:583-600 function test_task_metadata_enhancement
  Similarity: 85.32%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_concurrent_task_processing (lines 349-379) ---[0m
    async fn test_concurrent_task_processing() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create multiple tasks concurrently
        let mut handles = vec![];
        for i in 0..10 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("concurrent-task-{}", i),
                    format!("Task {} description", i),
                    Priority::Medium,
                    TaskType::Development,
                );
                master_clone.add_task(task).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all tasks to be added
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify all tasks were added
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 10);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_metadata_enhancement (lines 583-600) ---[0m
    async fn test_task_metadata_enhancement() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test task complexity detection
        let feature_task = Task::new(
            "feature-1".to_string(),
            "Implement complex feature".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        let insights = master.get_proactive_insights_for_task(&feature_task).await;
        assert_eq!(insights["task_complexity"], "high");
        assert_eq!(insights["recommended_approach"], "careful_planning");
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:103-124 function test_master_claude_with_orchestration_context <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:559-580 function test_graceful_shutdown
  Similarity: 90.98%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_master_claude_with_orchestration_context (lines 103-124) ---[0m
    async fn test_master_claude_with_orchestration_context() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test that orchestration context is properly set
        let task = Task::new(
            "test-task-1".to_string(),
            "Create user authentication flow".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        // Add task to queue
        master.add_task(task.clone()).await.unwrap();

        // Verify task was added to pending tasks
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
        assert_eq!(state.pending_tasks[0].id, task.id);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_graceful_shutdown (lines 559-580) ---[0m
    async fn test_graceful_shutdown() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add a task
        let task = Task::new(
            "shutdown-test".to_string(),
            "Test task".to_string(),
            Priority::Low,
            TaskType::Testing,
        );
        master.add_task(task).await.unwrap();

        // Initiate shutdown
        master.shutdown().await.unwrap();

        // Verify shutdown state
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::ShuttingDown);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:349-379 function test_concurrent_task_processing <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:507-524 function test_milestone_tracking
  Similarity: 81.22%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_concurrent_task_processing (lines 349-379) ---[0m
    async fn test_concurrent_task_processing() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Create multiple tasks concurrently
        let mut handles = vec![];
        for i in 0..10 {
            let master_clone = master.clone();
            let handle = tokio::spawn(async move {
                let task = Task::new(
                    format!("concurrent-task-{}", i),
                    format!("Task {} description", i),
                    Priority::Medium,
                    TaskType::Development,
                );
                master_clone.add_task(task).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all tasks to be added
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify all tasks were added
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 10);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_milestone_tracking (lines 507-524) ---[0m
    async fn test_milestone_tracking() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add milestone
        let milestone_id = master
            .add_milestone(
                "MVP Release".to_string(),
                "Complete minimum viable product".to_string(),
                Some(chrono::Utc::now() + chrono::Duration::days(30)),
            )
            .await
            .unwrap();

        assert!(milestone_id.starts_with("milestone-"));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:103-124 function test_master_claude_with_orchestration_context <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:261-285 function test_proactive_task_generation
  Similarity: 83.16%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_master_claude_with_orchestration_context (lines 103-124) ---[0m
    async fn test_master_claude_with_orchestration_context() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test that orchestration context is properly set
        let task = Task::new(
            "test-task-1".to_string(),
            "Create user authentication flow".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        // Add task to queue
        master.add_task(task.clone()).await.unwrap();

        // Verify task was added to pending tasks
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
        assert_eq!(state.pending_tasks[0].id, task.id);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_proactive_task_generation (lines 261-285) ---[0m
    async fn test_proactive_task_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add an objective for proactive tracking
        let obj_id = master
            .set_objective(
                "Complete user authentication".to_string(),
                "Implement full authentication flow".to_string(),
                None,
            )
            .await
            .unwrap();

        assert!(obj_id.starts_with("obj-"));

        // Trigger proactive analysis
        let decisions = master.trigger_proactive_analysis().await.unwrap();

        // In a real scenario, this would generate tasks based on objectives
        // For testing, we verify the mechanism works
        assert!(decisions.is_empty() || !decisions.is_empty()); // Either case is valid
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:261-285 function test_proactive_task_generation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:559-580 function test_graceful_shutdown
  Similarity: 82.01%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_proactive_task_generation (lines 261-285) ---[0m
    async fn test_proactive_task_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add an objective for proactive tracking
        let obj_id = master
            .set_objective(
                "Complete user authentication".to_string(),
                "Implement full authentication flow".to_string(),
                None,
            )
            .await
            .unwrap();

        assert!(obj_id.starts_with("obj-"));

        // Trigger proactive analysis
        let decisions = master.trigger_proactive_analysis().await.unwrap();

        // In a real scenario, this would generate tasks based on objectives
        // For testing, we verify the mechanism works
        assert!(decisions.is_empty() || !decisions.is_empty()); // Either case is valid
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_graceful_shutdown (lines 559-580) ---[0m
    async fn test_graceful_shutdown() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add a task
        let task = Task::new(
            "shutdown-test".to_string(),
            "Test task".to_string(),
            Priority::Low,
            TaskType::Testing,
        );
        master.add_task(task).await.unwrap();

        // Initiate shutdown
        master.shutdown().await.unwrap();

        // Verify shutdown state
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::ShuttingDown);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:128-161 function test_agent_task_delegation_with_metadata <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:507-524 function test_milestone_tracking
  Similarity: 73.62%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_task_delegation_with_metadata (lines 128-161) ---[0m
    async fn test_agent_task_delegation_with_metadata() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create mock agents
        let frontend_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_frontend_role(),
            &repo_path,
            "feature/frontend",
            ClaudeConfig::for_agent("frontend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        master
            .agents
            .insert("frontend-agent".to_string(), frontend_agent);

        // Create a frontend task
        let task = Task::new(
            "ui-task-1".to_string(),
            "Create login component with React".to_string(),
            Priority::High,
            TaskType::Development,
        );

        // Test task assignment logic
        let selected_agent = master.select_optimal_agent(&task).await.unwrap();
        assert_eq!(selected_agent, "frontend-agent");
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_milestone_tracking (lines 507-524) ---[0m
    async fn test_milestone_tracking() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add milestone
        let milestone_id = master
            .add_milestone(
                "MVP Release".to_string(),
                "Complete minimum viable product".to_string(),
                Some(chrono::Utc::now() + chrono::Duration::days(30)),
            )
            .await
            .unwrap();

        assert!(milestone_id.starts_with("milestone-"));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:383-415 function test_agent_health_monitoring <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:583-600 function test_task_metadata_enhancement
  Similarity: 73.79%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_agent_health_monitoring (lines 383-415) ---[0m
    async fn test_agent_health_monitoring() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create an agent with old last_activity
        let mut unhealthy_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/backend",
            ClaudeConfig::for_agent("backend"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        // Set last activity to 10 minutes ago
        unhealthy_agent.last_activity = chrono::Utc::now() - chrono::Duration::seconds(600);

        master
            .agents
            .insert("unhealthy-agent".to_string(), unhealthy_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was restarted (last_activity updated)
        let agent = master.agents.get("unhealthy-agent").unwrap();
        let time_diff = chrono::Utc::now() - agent.last_activity;
        assert!(time_diff.num_seconds() < 5); // Should be recent
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_metadata_enhancement (lines 583-600) ---[0m
    async fn test_task_metadata_enhancement() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test task complexity detection
        let feature_task = Task::new(
            "feature-1".to_string(),
            "Implement complex feature".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        let insights = master.get_proactive_insights_for_task(&feature_task).await;
        assert_eq!(insights["task_complexity"], "high");
        assert_eq!(insights["recommended_approach"], "careful_planning");
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:583-600 function test_task_metadata_enhancement <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:603-629 function test_dependency_identification
  Similarity: 83.14%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_metadata_enhancement (lines 583-600) ---[0m
    async fn test_task_metadata_enhancement() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test task complexity detection
        let feature_task = Task::new(
            "feature-1".to_string(),
            "Implement complex feature".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        let insights = master.get_proactive_insights_for_task(&feature_task).await;
        assert_eq!(insights["task_complexity"], "high");
        assert_eq!(insights["recommended_approach"], "careful_planning");
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_dependency_identification (lines 603-629) ---[0m
    async fn test_dependency_identification() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test various task descriptions
        let api_task = Task::new(
            "api-1".to_string(),
            "Create REST API endpoint for users".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let dependencies = master.identify_task_dependencies(&api_task).await;
        assert!(dependencies.contains(&"backend_api".to_string()));

        let ui_task = Task::new(
            "ui-1".to_string(),
            "Build UI component for dashboard".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let ui_deps = master.identify_task_dependencies(&ui_task).await;
        assert!(ui_deps.contains(&"frontend_components".to_string()));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:528-556 function test_error_recovery_mechanism <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:583-600 function test_task_metadata_enhancement
  Similarity: 78.28%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_error_recovery_mechanism (lines 528-556) ---[0m
    async fn test_error_recovery_mechanism() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let mut master = MasterClaude::new(config, repo_path.clone()).await.unwrap();
        master.set_isolation_mode(IsolationMode::GitWorktree);

        // Create agent in error state
        let mut error_agent = ClaudeCodeAgent::new_with_isolation(
            crate::identity::default_backend_role(),
            &repo_path,
            "feature/error",
            ClaudeConfig::for_agent("error-agent"),
            IsolationMode::GitWorktree,
        )
        .await
        .unwrap();

        error_agent.status = crate::agent::AgentStatus::Error("Test error".to_string());

        master.agents.insert("error-agent".to_string(), error_agent);

        // Run health check
        master.check_agent_health().await.unwrap();

        // Verify agent was recovered
        let agent = master.agents.get("error-agent").unwrap();
        assert!(matches!(agent.status, crate::agent::AgentStatus::Available));
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_metadata_enhancement (lines 583-600) ---[0m
    async fn test_task_metadata_enhancement() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test task complexity detection
        let feature_task = Task::new(
            "feature-1".to_string(),
            "Implement complex feature".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        let insights = master.get_proactive_insights_for_task(&feature_task).await;
        assert_eq!(insights["task_complexity"], "high");
        assert_eq!(insights["recommended_approach"], "careful_planning");
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:261-285 function test_proactive_task_generation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:583-600 function test_task_metadata_enhancement
  Similarity: 82.63%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_proactive_task_generation (lines 261-285) ---[0m
    async fn test_proactive_task_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add an objective for proactive tracking
        let obj_id = master
            .set_objective(
                "Complete user authentication".to_string(),
                "Implement full authentication flow".to_string(),
                None,
            )
            .await
            .unwrap();

        assert!(obj_id.starts_with("obj-"));

        // Trigger proactive analysis
        let decisions = master.trigger_proactive_analysis().await.unwrap();

        // In a real scenario, this would generate tasks based on objectives
        // For testing, we verify the mechanism works
        assert!(decisions.is_empty() || !decisions.is_empty()); // Either case is valid
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_metadata_enhancement (lines 583-600) ---[0m
    async fn test_task_metadata_enhancement() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test task complexity detection
        let feature_task = Task::new(
            "feature-1".to_string(),
            "Implement complex feature".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        let insights = master.get_proactive_insights_for_task(&feature_task).await;
        assert_eq!(insights["task_complexity"], "high");
        assert_eq!(insights["recommended_approach"], "careful_planning");
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:261-285 function test_proactive_task_generation <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:507-524 function test_milestone_tracking
  Similarity: 79.79%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_proactive_task_generation (lines 261-285) ---[0m
    async fn test_proactive_task_generation() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add an objective for proactive tracking
        let obj_id = master
            .set_objective(
                "Complete user authentication".to_string(),
                "Implement full authentication flow".to_string(),
                None,
            )
            .await
            .unwrap();

        assert!(obj_id.starts_with("obj-"));

        // Trigger proactive analysis
        let decisions = master.trigger_proactive_analysis().await.unwrap();

        // In a real scenario, this would generate tasks based on objectives
        // For testing, we verify the mechanism works
        assert!(decisions.is_empty() || !decisions.is_empty()); // Either case is valid
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_milestone_tracking (lines 507-524) ---[0m
    async fn test_milestone_tracking() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add milestone
        let milestone_id = master
            .add_milestone(
                "MVP Release".to_string(),
                "Complete minimum viable product".to_string(),
                Some(chrono::Utc::now() + chrono::Duration::days(30)),
            )
            .await
            .unwrap();

        assert!(milestone_id.starts_with("milestone-"));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:559-580 function test_graceful_shutdown <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:583-600 function test_task_metadata_enhancement
  Similarity: 85.69%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_graceful_shutdown (lines 559-580) ---[0m
    async fn test_graceful_shutdown() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add a task
        let task = Task::new(
            "shutdown-test".to_string(),
            "Test task".to_string(),
            Priority::Low,
            TaskType::Testing,
        );
        master.add_task(task).await.unwrap();

        // Initiate shutdown
        master.shutdown().await.unwrap();

        // Verify shutdown state
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::ShuttingDown);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_metadata_enhancement (lines 583-600) ---[0m
    async fn test_task_metadata_enhancement() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test task complexity detection
        let feature_task = Task::new(
            "feature-1".to_string(),
            "Implement complex feature".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        let insights = master.get_proactive_insights_for_task(&feature_task).await;
        assert_eq!(insights["task_complexity"], "high");
        assert_eq!(insights["recommended_approach"], "careful_planning");
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:103-124 function test_master_claude_with_orchestration_context <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:583-600 function test_task_metadata_enhancement
  Similarity: 84.88%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_master_claude_with_orchestration_context (lines 103-124) ---[0m
    async fn test_master_claude_with_orchestration_context() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test that orchestration context is properly set
        let task = Task::new(
            "test-task-1".to_string(),
            "Create user authentication flow".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        // Add task to queue
        master.add_task(task.clone()).await.unwrap();

        // Verify task was added to pending tasks
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
        assert_eq!(state.pending_tasks[0].id, task.id);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_metadata_enhancement (lines 583-600) ---[0m
    async fn test_task_metadata_enhancement() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test task complexity detection
        let feature_task = Task::new(
            "feature-1".to_string(),
            "Implement complex feature".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        let insights = master.get_proactive_insights_for_task(&feature_task).await;
        assert_eq!(insights["task_complexity"], "high");
        assert_eq!(insights["recommended_approach"], "careful_planning");
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:507-524 function test_milestone_tracking <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:603-629 function test_dependency_identification
  Similarity: 71.09%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_milestone_tracking (lines 507-524) ---[0m
    async fn test_milestone_tracking() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add milestone
        let milestone_id = master
            .add_milestone(
                "MVP Release".to_string(),
                "Complete minimum viable product".to_string(),
                Some(chrono::Utc::now() + chrono::Duration::days(30)),
            )
            .await
            .unwrap();

        assert!(milestone_id.starts_with("milestone-"));
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_dependency_identification (lines 603-629) ---[0m
    async fn test_dependency_identification() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test various task descriptions
        let api_task = Task::new(
            "api-1".to_string(),
            "Create REST API endpoint for users".to_string(),
            Priority::High,
            TaskType::Development,
        );

        let dependencies = master.identify_task_dependencies(&api_task).await;
        assert!(dependencies.contains(&"backend_api".to_string()));

        let ui_task = Task::new(
            "ui-1".to_string(),
            "Build UI component for dashboard".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let ui_deps = master.identify_task_dependencies(&ui_task).await;
        assert!(ui_deps.contains(&"frontend_components".to_string()));
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:507-524 function test_milestone_tracking <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:583-600 function test_task_metadata_enhancement
  Similarity: 82.74%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_milestone_tracking (lines 507-524) ---[0m
    async fn test_milestone_tracking() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add milestone
        let milestone_id = master
            .add_milestone(
                "MVP Release".to_string(),
                "Complete minimum viable product".to_string(),
                Some(chrono::Utc::now() + chrono::Duration::days(30)),
            )
            .await
            .unwrap();

        assert!(milestone_id.starts_with("milestone-"));
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_task_metadata_enhancement (lines 583-600) ---[0m
    async fn test_task_metadata_enhancement() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test task complexity detection
        let feature_task = Task::new(
            "feature-1".to_string(),
            "Implement complex feature".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        let insights = master.get_proactive_insights_for_task(&feature_task).await;
        assert_eq!(insights["task_complexity"], "high");
        assert_eq!(insights["recommended_approach"], "careful_planning");
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:507-524 function test_milestone_tracking <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:559-580 function test_graceful_shutdown
  Similarity: 74.22%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_milestone_tracking (lines 507-524) ---[0m
    async fn test_milestone_tracking() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add milestone
        let milestone_id = master
            .add_milestone(
                "MVP Release".to_string(),
                "Complete minimum viable product".to_string(),
                Some(chrono::Utc::now() + chrono::Duration::days(30)),
            )
            .await
            .unwrap();

        assert!(milestone_id.starts_with("milestone-"));
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_graceful_shutdown (lines 559-580) ---[0m
    async fn test_graceful_shutdown() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add a task
        let task = Task::new(
            "shutdown-test".to_string(),
            "Test task".to_string(),
            Priority::Low,
            TaskType::Testing,
        );
        master.add_task(task).await.unwrap();

        // Initiate shutdown
        master.shutdown().await.unwrap();

        // Verify shutdown state
        let state = master.state.read().await;
        assert_eq!(state.status, OrchestratorStatus::ShuttingDown);
    }

  ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:103-124 function test_master_claude_with_orchestration_context <-> ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:507-524 function test_milestone_tracking
  Similarity: 73.21%

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_master_claude_with_orchestration_context (lines 103-124) ---[0m
    async fn test_master_claude_with_orchestration_context() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Test that orchestration context is properly set
        let task = Task::new(
            "test-task-1".to_string(),
            "Create user authentication flow".to_string(),
            Priority::High,
            TaskType::Feature,
        );

        // Add task to queue
        master.add_task(task.clone()).await.unwrap();

        // Verify task was added to pending tasks
        let state = master.state.read().await;
        assert_eq!(state.pending_tasks.len(), 1);
        assert_eq!(state.pending_tasks[0].id, task.id);
    }

[36m--- ./crates/ccswarm/src/orchestrator/orchestrator_integration_tests.rs:test_milestone_tracking (lines 507-524) ---[0m
    async fn test_milestone_tracking() {
        let (config, _temp_dir) = create_comprehensive_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let master = MasterClaude::new(config, repo_path).await.unwrap();

        // Add milestone
        let milestone_id = master
            .add_milestone(
                "MVP Release".to_string(),
                "Complete minimum viable product".to_string(),
                Some(chrono::Utc::now() + chrono::Duration::days(30)),
            )
            .await
            .unwrap();

        assert!(milestone_id.starts_with("milestone-"));
    }


Duplicates in ./crates/ccswarm/src/extension_stub/agent_extension.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/extension_stub/agent_extension.rs:77-90 method search <-> ./crates/ccswarm/src/extension_stub/agent_extension.rs:114-127 method search
  Similarity: 98.22%
  Classes: SearchStrategy <-> SearchStrategy

[36m--- ./crates/ccswarm/src/extension_stub/agent_extension.rs:search (lines 77-90) ---[0m
    async fn search(&self, query: &SearchQuery) -> Result<Vec<SearchResult>, CCSwarmError> {
        // Stub implementation - returns mock results
        let keywords = query.keywords.join(" ");
        Ok(vec![
            SearchResult {
                title: format!("MDN: {} Documentation", keywords),
                url: format!("https://developer.mozilla.org/search?q={}", keywords),
                snippet: format!("Documentation for {} from MDN Web Docs", keywords),
                source: "MDN".to_string(),
                score: 0.95,
                relevance_score: 0.95,
            },
        ])
    }

[36m--- ./crates/ccswarm/src/extension_stub/agent_extension.rs:search (lines 114-127) ---[0m
    async fn search(&self, query: &SearchQuery) -> Result<Vec<SearchResult>, CCSwarmError> {
        // Stub implementation
        let keywords = query.keywords.join(" ");
        Ok(vec![
            SearchResult {
                title: format!("GitHub: {} Examples", keywords),
                url: format!("https://github.com/search?q={}", keywords),
                snippet: format!("Code examples for {} from GitHub repositories", keywords),
                source: "GitHub".to_string(),
                score: 0.85,
                relevance_score: 0.85,
            },
        ])
    }

  ./crates/ccswarm/src/extension_stub/agent_extension.rs:77-90 method search <-> ./crates/ccswarm/src/extension_stub/agent_extension.rs:139-152 method search
  Similarity: 98.22%
  Classes: SearchStrategy <-> SearchStrategy

[36m--- ./crates/ccswarm/src/extension_stub/agent_extension.rs:search (lines 77-90) ---[0m
    async fn search(&self, query: &SearchQuery) -> Result<Vec<SearchResult>, CCSwarmError> {
        // Stub implementation - returns mock results
        let keywords = query.keywords.join(" ");
        Ok(vec![
            SearchResult {
                title: format!("MDN: {} Documentation", keywords),
                url: format!("https://developer.mozilla.org/search?q={}", keywords),
                snippet: format!("Documentation for {} from MDN Web Docs", keywords),
                source: "MDN".to_string(),
                score: 0.95,
                relevance_score: 0.95,
            },
        ])
    }

[36m--- ./crates/ccswarm/src/extension_stub/agent_extension.rs:search (lines 139-152) ---[0m
    async fn search(&self, query: &SearchQuery) -> Result<Vec<SearchResult>, CCSwarmError> {
        // Stub implementation
        let keywords = query.keywords.join(" ");
        Ok(vec![
            SearchResult {
                title: format!("StackOverflow: {} Questions", keywords),
                url: format!("https://stackoverflow.com/search?q={}", keywords),
                snippet: format!("Questions and answers about {} from StackOverflow", keywords),
                source: "StackOverflow".to_string(),
                score: 0.90,
                relevance_score: 0.90,
            },
        ])
    }

  ./crates/ccswarm/src/extension_stub/agent_extension.rs:114-127 method search <-> ./crates/ccswarm/src/extension_stub/agent_extension.rs:139-152 method search
  Similarity: 98.22%
  Classes: SearchStrategy <-> SearchStrategy

[36m--- ./crates/ccswarm/src/extension_stub/agent_extension.rs:search (lines 114-127) ---[0m
    async fn search(&self, query: &SearchQuery) -> Result<Vec<SearchResult>, CCSwarmError> {
        // Stub implementation
        let keywords = query.keywords.join(" ");
        Ok(vec![
            SearchResult {
                title: format!("GitHub: {} Examples", keywords),
                url: format!("https://github.com/search?q={}", keywords),
                snippet: format!("Code examples for {} from GitHub repositories", keywords),
                source: "GitHub".to_string(),
                score: 0.85,
                relevance_score: 0.85,
            },
        ])
    }

[36m--- ./crates/ccswarm/src/extension_stub/agent_extension.rs:search (lines 139-152) ---[0m
    async fn search(&self, query: &SearchQuery) -> Result<Vec<SearchResult>, CCSwarmError> {
        // Stub implementation
        let keywords = query.keywords.join(" ");
        Ok(vec![
            SearchResult {
                title: format!("StackOverflow: {} Questions", keywords),
                url: format!("https://stackoverflow.com/search?q={}", keywords),
                snippet: format!("Questions and answers about {} from StackOverflow", keywords),
                source: "StackOverflow".to_string(),
                score: 0.90,
                relevance_score: 0.90,
            },
        ])
    }


Duplicates in ./crates/ccswarm/src/providers/codex.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/providers/codex.rs:114-252 method generate_system_prompt <-> ./crates/ccswarm/src/providers/codex.rs:255-385 method generate_task_prompt
  Similarity: 90.12%
  Classes: CodexExecutor <-> CodexExecutor

[36m--- ./crates/ccswarm/src/providers/codex.rs:generate_system_prompt (lines 114-252) ---[0m
    fn generate_system_prompt(&self, identity: &AgentIdentity) -> String {
        let agent_context = format!(
            "You are a specialized AI coding assistant working as part of the ccswarm multi-agent system.\n\
             \n\
             Agent ID: {}\n\
             Specialization: {}\n\
             Workspace: {}\n\
             \n",
            identity.agent_id,
            identity.specialization.name(),
            identity.workspace_path.display()
        );

        let specialization_prompt = match &identity.specialization {
            crate::identity::AgentRole::Frontend {
                technologies,
                responsibilities,
                boundaries,
            } => {
                format!(
                    "FRONTEND SPECIALIST\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on frontend development. Do not write backend code, \
                     database schemas, or infrastructure configurations. If asked about \
                     backend concerns, explain that you need to delegate to a backend specialist.\n",
                    technologies.join(", "),
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
            crate::identity::AgentRole::Backend {
                technologies,
                responsibilities,
                boundaries,
            } => {
                format!(
                    "BACKEND SPECIALIST\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on backend development. Do not write frontend UI code, \
                     styling, or client-side logic. If asked about frontend concerns, \
                     explain that you need to delegate to a frontend specialist.\n",
                    technologies.join(", "),
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
            crate::identity::AgentRole::DevOps {
                technologies,
                responsibilities,
                boundaries,
            } => {
                format!(
                    "DEVOPS SPECIALIST\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on infrastructure, deployment, and operational concerns. \
                     Do not write application business logic or feature code. If asked about \
                     application development, explain that you need to delegate to development specialists.\n",
                    technologies.join(", "),
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
            crate::identity::AgentRole::QA {
                responsibilities,
                boundaries,
                ..
            } => {
                format!(
                    "QA SPECIALIST\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on testing, quality assurance, and validation. \
                     Do not write production application code. Focus on test strategies, \
                     test implementation, and quality verification.\n",
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
            crate::identity::AgentRole::Master {
                oversight_roles,
                quality_standards,
                ..
            } => {
                format!(
                    "MASTER ORCHESTRATOR\n\
                     Oversight: {}\n\
                     Quality Standards: Test coverage ≥{:.0}%, Max complexity ≤{}, Security scan: {}\n\
                     \n\
                     You are the master orchestrator. Do not write code directly. \
                     Instead, focus on coordination, planning, and quality review. \
                     Delegate specific implementation tasks to specialized agents.\n",
                    oversight_roles.join(", "),
                    quality_standards.min_test_coverage,
                    quality_standards.max_complexity,
                    if quality_standards.security_scan_required { "Required" } else { "Optional" }
                )
            }
            crate::identity::AgentRole::Search {
                technologies,
                responsibilities,
                boundaries,
            } => {
                format!(
                    "SEARCH SPECIALIST\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on information retrieval and search operations. \
                     Do not write or modify any code. Your role is to find and present \
                     relevant information to help other agents complete their tasks.\n",
                    technologies.join(", "),
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
        };

        let guidelines = "\nGUIDELINES:\n\
                         1. Always identify yourself and your role at the start of responses\n\
                         2. Stay strictly within your specialization boundaries\n\
                         3. Provide clear, actionable code and explanations\n\
                         4. If a request is outside your scope, clearly state what needs to be delegated\n\
                         5. Focus on clean, maintainable, and well-documented code\n\
                         6. Consider security, performance, and best practices\n\
                         7. Respond in a structured format that can be easily parsed\n";

        format!("{}{}\n{}", agent_context, specialization_prompt, guidelines)
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:generate_task_prompt (lines 255-385) ---[0m
    fn generate_task_prompt(&self, _identity: &AgentIdentity, task: &Task) -> String {
        let task_header = format!(
            "TASK REQUEST\n\
             Task ID: {}\n\
             Type: {:?}\n\
             Priority: {:?}\n\
             \n",
            task.id, task.task_type, task.priority
        );

        let task_instructions = match task.task_type {
            TaskType::Development => {
                "DEVELOPMENT TASK\n\
                 Please implement the requested feature with:\n\
                 - Clean, readable code\n\
                 - Proper error handling\n\
                 - Relevant comments\n\
                 - Consider testing implications\n"
            }
            TaskType::Testing => {
                "TESTING TASK\n\
                 Please create comprehensive tests with:\n\
                 - Good test coverage\n\
                 - Edge case handling\n\
                 - Clear test descriptions\n\
                 - Mock/stub strategies where appropriate\n"
            }
            TaskType::Documentation => {
                "DOCUMENTATION TASK\n\
                 Please create clear documentation with:\n\
                 - Comprehensive explanations\n\
                 - Code examples\n\
                 - Usage instructions\n\
                 - API references where applicable\n"
            }
            TaskType::Bugfix => {
                "BUG FIX TASK\n\
                 Please analyze and fix the issue with:\n\
                 - Root cause identification\n\
                 - Minimal, targeted changes\n\
                 - Prevention of regression\n\
                 - Explanation of the fix\n"
            }
            TaskType::Infrastructure => {
                "INFRASTRUCTURE TASK\n\
                 Please handle infrastructure concerns with:\n\
                 - Security best practices\n\
                 - Scalability considerations\n\
                 - Monitoring and observability\n\
                 - Documentation of configurations\n"
            }
            TaskType::Coordination => {
                "COORDINATION TASK\n\
                 Please provide coordination guidance with:\n\
                 - Clear delegation instructions\n\
                 - Task breakdown\n\
                 - Quality requirements\n\
                 - No direct implementation\n"
            }
            TaskType::Review => {
                "REVIEW TASK\n\
                 Please review the code/documentation with:\n\
                 - Quality assessment\n\
                 - Security considerations\n\
                 - Best practice validation\n\
                 - Improvement suggestions\n"
            }
            TaskType::Feature => {
                "FEATURE TASK\n\
                 Please implement the new feature with:\n\
                 - Complete functionality\n\
                 - Proper integration\n\
                 - Testing considerations\n\
                 - Documentation updates\n"
            }
            TaskType::Remediation => {
                "REMEDIATION TASK\n\
                 Please fix the quality issues identified with:\n\
                 - Address all specific issues listed\n\
                 - Follow provided instructions exactly\n\
                 - Add tests to prevent regression\n\
                 - Improve overall code quality\n\
                 - Verify all issues are resolved\n"
            }
            TaskType::Bug => {
                "BUG FIX TASK\n\
                 Please analyze and fix the bug with:\n\
                 - Root cause identification\n\
                 - Minimal, targeted changes\n\
                 - Prevention of regression\n\
                 - Explanation of the fix\n"
            }
            TaskType::Assistance => {
                "ASSISTANCE TASK\n\
                 Please help with the requested task by:\n\
                 - Understanding the blocker\n\
                 - Providing expert guidance\n\
                 - Collaborative problem solving\n\
                 - Knowledge sharing\n"
            }
            TaskType::Research => {
                "RESEARCH TASK\n\
                 Please research and analyze information with:\n\
                 - Review search results thoroughly\n\
                 - Extract key insights\n\
                 - Apply findings to current work\n\
                 - Document recommendations\n"
            }
        };

        let task_description = format!("DESCRIPTION:\n{}\n\n", task.description);

        let task_details = if let Some(details) = &task.details {
            format!("ADDITIONAL DETAILS:\n{}\n\n", details)
        } else {
            String::new()
        };

        let response_format = "RESPONSE FORMAT:\n\
                              Please structure your response as:\n\
                              1. Agent identification and scope confirmation\n\
                              2. Task analysis and approach\n\
                              3. Implementation (code/configuration/guidance)\n\
                              4. Verification steps or testing approach\n\
                              5. Any delegation needs or dependencies\n";

        format!(
            "{}{}\n{}{}\n{}",
            task_header, task_instructions, task_description, task_details, response_format
        )
    }

  ./crates/ccswarm/src/providers/codex.rs:114-252 method generate_system_prompt <-> ./crates/ccswarm/src/providers/codex.rs:504-569 method execute_task
  Similarity: 95.48%
  Classes: CodexExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/codex.rs:generate_system_prompt (lines 114-252) ---[0m
    fn generate_system_prompt(&self, identity: &AgentIdentity) -> String {
        let agent_context = format!(
            "You are a specialized AI coding assistant working as part of the ccswarm multi-agent system.\n\
             \n\
             Agent ID: {}\n\
             Specialization: {}\n\
             Workspace: {}\n\
             \n",
            identity.agent_id,
            identity.specialization.name(),
            identity.workspace_path.display()
        );

        let specialization_prompt = match &identity.specialization {
            crate::identity::AgentRole::Frontend {
                technologies,
                responsibilities,
                boundaries,
            } => {
                format!(
                    "FRONTEND SPECIALIST\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on frontend development. Do not write backend code, \
                     database schemas, or infrastructure configurations. If asked about \
                     backend concerns, explain that you need to delegate to a backend specialist.\n",
                    technologies.join(", "),
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
            crate::identity::AgentRole::Backend {
                technologies,
                responsibilities,
                boundaries,
            } => {
                format!(
                    "BACKEND SPECIALIST\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on backend development. Do not write frontend UI code, \
                     styling, or client-side logic. If asked about frontend concerns, \
                     explain that you need to delegate to a frontend specialist.\n",
                    technologies.join(", "),
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
            crate::identity::AgentRole::DevOps {
                technologies,
                responsibilities,
                boundaries,
            } => {
                format!(
                    "DEVOPS SPECIALIST\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on infrastructure, deployment, and operational concerns. \
                     Do not write application business logic or feature code. If asked about \
                     application development, explain that you need to delegate to development specialists.\n",
                    technologies.join(", "),
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
            crate::identity::AgentRole::QA {
                responsibilities,
                boundaries,
                ..
            } => {
                format!(
                    "QA SPECIALIST\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on testing, quality assurance, and validation. \
                     Do not write production application code. Focus on test strategies, \
                     test implementation, and quality verification.\n",
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
            crate::identity::AgentRole::Master {
                oversight_roles,
                quality_standards,
                ..
            } => {
                format!(
                    "MASTER ORCHESTRATOR\n\
                     Oversight: {}\n\
                     Quality Standards: Test coverage ≥{:.0}%, Max complexity ≤{}, Security scan: {}\n\
                     \n\
                     You are the master orchestrator. Do not write code directly. \
                     Instead, focus on coordination, planning, and quality review. \
                     Delegate specific implementation tasks to specialized agents.\n",
                    oversight_roles.join(", "),
                    quality_standards.min_test_coverage,
                    quality_standards.max_complexity,
                    if quality_standards.security_scan_required { "Required" } else { "Optional" }
                )
            }
            crate::identity::AgentRole::Search {
                technologies,
                responsibilities,
                boundaries,
            } => {
                format!(
                    "SEARCH SPECIALIST\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on information retrieval and search operations. \
                     Do not write or modify any code. Your role is to find and present \
                     relevant information to help other agents complete their tasks.\n",
                    technologies.join(", "),
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
        };

        let guidelines = "\nGUIDELINES:\n\
                         1. Always identify yourself and your role at the start of responses\n\
                         2. Stay strictly within your specialization boundaries\n\
                         3. Provide clear, actionable code and explanations\n\
                         4. If a request is outside your scope, clearly state what needs to be delegated\n\
                         5. Focus on clean, maintainable, and well-documented code\n\
                         6. Consider security, performance, and best practices\n\
                         7. Respond in a structured format that can be easily parsed\n";

        format!("{}{}\n{}", agent_context, specialization_prompt, guidelines)
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:execute_task (lines 504-569) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate comprehensive prompt
        let task_prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with OpenAI (model: {}) for agent {}",
            task.description,
            self.config.model,
            identity.agent_id
        );

        // Execute the prompt
        match self
            .execute_prompt(&task_prompt, identity, working_dir)
            .await
        {
            Ok(response_content) => {
                let duration = start.elapsed();

                tracing::info!(
                    "OpenAI task completed in {:?} for agent {}",
                    duration,
                    identity.agent_id
                );

                Ok(TaskResult {
                    success: true,
                    output: serde_json::json!({
                        "response": response_content,
                        "task_id": task.id,
                        "provider": "codex",
                        "model": self.config.model,
                    }),
                    error: None,
                    duration,
                })
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "OpenAI task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "codex",
                        "model": self.config.model,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/codex.rs:255-385 method generate_task_prompt <-> ./crates/ccswarm/src/providers/codex.rs:504-569 method execute_task
  Similarity: 93.86%
  Classes: CodexExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/codex.rs:generate_task_prompt (lines 255-385) ---[0m
    fn generate_task_prompt(&self, _identity: &AgentIdentity, task: &Task) -> String {
        let task_header = format!(
            "TASK REQUEST\n\
             Task ID: {}\n\
             Type: {:?}\n\
             Priority: {:?}\n\
             \n",
            task.id, task.task_type, task.priority
        );

        let task_instructions = match task.task_type {
            TaskType::Development => {
                "DEVELOPMENT TASK\n\
                 Please implement the requested feature with:\n\
                 - Clean, readable code\n\
                 - Proper error handling\n\
                 - Relevant comments\n\
                 - Consider testing implications\n"
            }
            TaskType::Testing => {
                "TESTING TASK\n\
                 Please create comprehensive tests with:\n\
                 - Good test coverage\n\
                 - Edge case handling\n\
                 - Clear test descriptions\n\
                 - Mock/stub strategies where appropriate\n"
            }
            TaskType::Documentation => {
                "DOCUMENTATION TASK\n\
                 Please create clear documentation with:\n\
                 - Comprehensive explanations\n\
                 - Code examples\n\
                 - Usage instructions\n\
                 - API references where applicable\n"
            }
            TaskType::Bugfix => {
                "BUG FIX TASK\n\
                 Please analyze and fix the issue with:\n\
                 - Root cause identification\n\
                 - Minimal, targeted changes\n\
                 - Prevention of regression\n\
                 - Explanation of the fix\n"
            }
            TaskType::Infrastructure => {
                "INFRASTRUCTURE TASK\n\
                 Please handle infrastructure concerns with:\n\
                 - Security best practices\n\
                 - Scalability considerations\n\
                 - Monitoring and observability\n\
                 - Documentation of configurations\n"
            }
            TaskType::Coordination => {
                "COORDINATION TASK\n\
                 Please provide coordination guidance with:\n\
                 - Clear delegation instructions\n\
                 - Task breakdown\n\
                 - Quality requirements\n\
                 - No direct implementation\n"
            }
            TaskType::Review => {
                "REVIEW TASK\n\
                 Please review the code/documentation with:\n\
                 - Quality assessment\n\
                 - Security considerations\n\
                 - Best practice validation\n\
                 - Improvement suggestions\n"
            }
            TaskType::Feature => {
                "FEATURE TASK\n\
                 Please implement the new feature with:\n\
                 - Complete functionality\n\
                 - Proper integration\n\
                 - Testing considerations\n\
                 - Documentation updates\n"
            }
            TaskType::Remediation => {
                "REMEDIATION TASK\n\
                 Please fix the quality issues identified with:\n\
                 - Address all specific issues listed\n\
                 - Follow provided instructions exactly\n\
                 - Add tests to prevent regression\n\
                 - Improve overall code quality\n\
                 - Verify all issues are resolved\n"
            }
            TaskType::Bug => {
                "BUG FIX TASK\n\
                 Please analyze and fix the bug with:\n\
                 - Root cause identification\n\
                 - Minimal, targeted changes\n\
                 - Prevention of regression\n\
                 - Explanation of the fix\n"
            }
            TaskType::Assistance => {
                "ASSISTANCE TASK\n\
                 Please help with the requested task by:\n\
                 - Understanding the blocker\n\
                 - Providing expert guidance\n\
                 - Collaborative problem solving\n\
                 - Knowledge sharing\n"
            }
            TaskType::Research => {
                "RESEARCH TASK\n\
                 Please research and analyze information with:\n\
                 - Review search results thoroughly\n\
                 - Extract key insights\n\
                 - Apply findings to current work\n\
                 - Document recommendations\n"
            }
        };

        let task_description = format!("DESCRIPTION:\n{}\n\n", task.description);

        let task_details = if let Some(details) = &task.details {
            format!("ADDITIONAL DETAILS:\n{}\n\n", details)
        } else {
            String::new()
        };

        let response_format = "RESPONSE FORMAT:\n\
                              Please structure your response as:\n\
                              1. Agent identification and scope confirmation\n\
                              2. Task analysis and approach\n\
                              3. Implementation (code/configuration/guidance)\n\
                              4. Verification steps or testing approach\n\
                              5. Any delegation needs or dependencies\n";

        format!(
            "{}{}\n{}{}\n{}",
            task_header, task_instructions, task_description, task_details, response_format
        )
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:execute_task (lines 504-569) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate comprehensive prompt
        let task_prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with OpenAI (model: {}) for agent {}",
            task.description,
            self.config.model,
            identity.agent_id
        );

        // Execute the prompt
        match self
            .execute_prompt(&task_prompt, identity, working_dir)
            .await
        {
            Ok(response_content) => {
                let duration = start.elapsed();

                tracing::info!(
                    "OpenAI task completed in {:?} for agent {}",
                    duration,
                    identity.agent_id
                );

                Ok(TaskResult {
                    success: true,
                    output: serde_json::json!({
                        "response": response_content,
                        "task_id": task.id,
                        "provider": "codex",
                        "model": self.config.model,
                    }),
                    error: None,
                    duration,
                })
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "OpenAI task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "codex",
                        "model": self.config.model,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/codex.rs:114-252 method generate_system_prompt <-> ./crates/ccswarm/src/providers/codex.rs:571-612 method health_check
  Similarity: 96.60%
  Classes: CodexExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/codex.rs:generate_system_prompt (lines 114-252) ---[0m
    fn generate_system_prompt(&self, identity: &AgentIdentity) -> String {
        let agent_context = format!(
            "You are a specialized AI coding assistant working as part of the ccswarm multi-agent system.\n\
             \n\
             Agent ID: {}\n\
             Specialization: {}\n\
             Workspace: {}\n\
             \n",
            identity.agent_id,
            identity.specialization.name(),
            identity.workspace_path.display()
        );

        let specialization_prompt = match &identity.specialization {
            crate::identity::AgentRole::Frontend {
                technologies,
                responsibilities,
                boundaries,
            } => {
                format!(
                    "FRONTEND SPECIALIST\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on frontend development. Do not write backend code, \
                     database schemas, or infrastructure configurations. If asked about \
                     backend concerns, explain that you need to delegate to a backend specialist.\n",
                    technologies.join(", "),
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
            crate::identity::AgentRole::Backend {
                technologies,
                responsibilities,
                boundaries,
            } => {
                format!(
                    "BACKEND SPECIALIST\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on backend development. Do not write frontend UI code, \
                     styling, or client-side logic. If asked about frontend concerns, \
                     explain that you need to delegate to a frontend specialist.\n",
                    technologies.join(", "),
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
            crate::identity::AgentRole::DevOps {
                technologies,
                responsibilities,
                boundaries,
            } => {
                format!(
                    "DEVOPS SPECIALIST\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on infrastructure, deployment, and operational concerns. \
                     Do not write application business logic or feature code. If asked about \
                     application development, explain that you need to delegate to development specialists.\n",
                    technologies.join(", "),
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
            crate::identity::AgentRole::QA {
                responsibilities,
                boundaries,
                ..
            } => {
                format!(
                    "QA SPECIALIST\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on testing, quality assurance, and validation. \
                     Do not write production application code. Focus on test strategies, \
                     test implementation, and quality verification.\n",
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
            crate::identity::AgentRole::Master {
                oversight_roles,
                quality_standards,
                ..
            } => {
                format!(
                    "MASTER ORCHESTRATOR\n\
                     Oversight: {}\n\
                     Quality Standards: Test coverage ≥{:.0}%, Max complexity ≤{}, Security scan: {}\n\
                     \n\
                     You are the master orchestrator. Do not write code directly. \
                     Instead, focus on coordination, planning, and quality review. \
                     Delegate specific implementation tasks to specialized agents.\n",
                    oversight_roles.join(", "),
                    quality_standards.min_test_coverage,
                    quality_standards.max_complexity,
                    if quality_standards.security_scan_required { "Required" } else { "Optional" }
                )
            }
            crate::identity::AgentRole::Search {
                technologies,
                responsibilities,
                boundaries,
            } => {
                format!(
                    "SEARCH SPECIALIST\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on information retrieval and search operations. \
                     Do not write or modify any code. Your role is to find and present \
                     relevant information to help other agents complete their tasks.\n",
                    technologies.join(", "),
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
        };

        let guidelines = "\nGUIDELINES:\n\
                         1. Always identify yourself and your role at the start of responses\n\
                         2. Stay strictly within your specialization boundaries\n\
                         3. Provide clear, actionable code and explanations\n\
                         4. If a request is outside your scope, clearly state what needs to be delegated\n\
                         5. Focus on clean, maintainable, and well-documented code\n\
                         6. Consider security, performance, and best practices\n\
                         7. Respond in a structured format that can be easily parsed\n";

        format!("{}{}\n{}", agent_context, specialization_prompt, guidelines)
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:health_check (lines 571-612) ---[0m
    async fn health_check(&self, _working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        // Make a simple API call to check health
        let request = OpenAIRequest {
            model: self.config.model.clone(),
            messages: vec![OpenAIMessage {
                role: "user".to_string(),
                content: "Hello, please respond with 'OK' to confirm you're working.".to_string(),
            }],
            max_tokens: Some(10),
            temperature: Some(0.1),
            stream: false,
        };

        match self.make_api_request(request).await {
            Ok(response) => {
                let duration = start.elapsed();
                let response_time_ms = duration.as_millis() as u64;

                Ok(ProviderHealthStatus {
                    is_healthy: true,
                    version: Some(format!("OpenAI API - Model: {}", response.model)),
                    last_check: chrono::Utc::now(),
                    error_message: None,
                    response_time_ms: Some(response_time_ms),
                })
            }
            Err(e) => {
                let duration = start.elapsed();
                let response_time_ms = duration.as_millis() as u64;

                Ok(ProviderHealthStatus {
                    is_healthy: false,
                    version: None,
                    last_check: chrono::Utc::now(),
                    error_message: Some(e.to_string()),
                    response_time_ms: Some(response_time_ms),
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/codex.rs:255-385 method generate_task_prompt <-> ./crates/ccswarm/src/providers/codex.rs:571-612 method health_check
  Similarity: 85.86%
  Classes: CodexExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/codex.rs:generate_task_prompt (lines 255-385) ---[0m
    fn generate_task_prompt(&self, _identity: &AgentIdentity, task: &Task) -> String {
        let task_header = format!(
            "TASK REQUEST\n\
             Task ID: {}\n\
             Type: {:?}\n\
             Priority: {:?}\n\
             \n",
            task.id, task.task_type, task.priority
        );

        let task_instructions = match task.task_type {
            TaskType::Development => {
                "DEVELOPMENT TASK\n\
                 Please implement the requested feature with:\n\
                 - Clean, readable code\n\
                 - Proper error handling\n\
                 - Relevant comments\n\
                 - Consider testing implications\n"
            }
            TaskType::Testing => {
                "TESTING TASK\n\
                 Please create comprehensive tests with:\n\
                 - Good test coverage\n\
                 - Edge case handling\n\
                 - Clear test descriptions\n\
                 - Mock/stub strategies where appropriate\n"
            }
            TaskType::Documentation => {
                "DOCUMENTATION TASK\n\
                 Please create clear documentation with:\n\
                 - Comprehensive explanations\n\
                 - Code examples\n\
                 - Usage instructions\n\
                 - API references where applicable\n"
            }
            TaskType::Bugfix => {
                "BUG FIX TASK\n\
                 Please analyze and fix the issue with:\n\
                 - Root cause identification\n\
                 - Minimal, targeted changes\n\
                 - Prevention of regression\n\
                 - Explanation of the fix\n"
            }
            TaskType::Infrastructure => {
                "INFRASTRUCTURE TASK\n\
                 Please handle infrastructure concerns with:\n\
                 - Security best practices\n\
                 - Scalability considerations\n\
                 - Monitoring and observability\n\
                 - Documentation of configurations\n"
            }
            TaskType::Coordination => {
                "COORDINATION TASK\n\
                 Please provide coordination guidance with:\n\
                 - Clear delegation instructions\n\
                 - Task breakdown\n\
                 - Quality requirements\n\
                 - No direct implementation\n"
            }
            TaskType::Review => {
                "REVIEW TASK\n\
                 Please review the code/documentation with:\n\
                 - Quality assessment\n\
                 - Security considerations\n\
                 - Best practice validation\n\
                 - Improvement suggestions\n"
            }
            TaskType::Feature => {
                "FEATURE TASK\n\
                 Please implement the new feature with:\n\
                 - Complete functionality\n\
                 - Proper integration\n\
                 - Testing considerations\n\
                 - Documentation updates\n"
            }
            TaskType::Remediation => {
                "REMEDIATION TASK\n\
                 Please fix the quality issues identified with:\n\
                 - Address all specific issues listed\n\
                 - Follow provided instructions exactly\n\
                 - Add tests to prevent regression\n\
                 - Improve overall code quality\n\
                 - Verify all issues are resolved\n"
            }
            TaskType::Bug => {
                "BUG FIX TASK\n\
                 Please analyze and fix the bug with:\n\
                 - Root cause identification\n\
                 - Minimal, targeted changes\n\
                 - Prevention of regression\n\
                 - Explanation of the fix\n"
            }
            TaskType::Assistance => {
                "ASSISTANCE TASK\n\
                 Please help with the requested task by:\n\
                 - Understanding the blocker\n\
                 - Providing expert guidance\n\
                 - Collaborative problem solving\n\
                 - Knowledge sharing\n"
            }
            TaskType::Research => {
                "RESEARCH TASK\n\
                 Please research and analyze information with:\n\
                 - Review search results thoroughly\n\
                 - Extract key insights\n\
                 - Apply findings to current work\n\
                 - Document recommendations\n"
            }
        };

        let task_description = format!("DESCRIPTION:\n{}\n\n", task.description);

        let task_details = if let Some(details) = &task.details {
            format!("ADDITIONAL DETAILS:\n{}\n\n", details)
        } else {
            String::new()
        };

        let response_format = "RESPONSE FORMAT:\n\
                              Please structure your response as:\n\
                              1. Agent identification and scope confirmation\n\
                              2. Task analysis and approach\n\
                              3. Implementation (code/configuration/guidance)\n\
                              4. Verification steps or testing approach\n\
                              5. Any delegation needs or dependencies\n";

        format!(
            "{}{}\n{}{}\n{}",
            task_header, task_instructions, task_description, task_details, response_format
        )
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:health_check (lines 571-612) ---[0m
    async fn health_check(&self, _working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        // Make a simple API call to check health
        let request = OpenAIRequest {
            model: self.config.model.clone(),
            messages: vec![OpenAIMessage {
                role: "user".to_string(),
                content: "Hello, please respond with 'OK' to confirm you're working.".to_string(),
            }],
            max_tokens: Some(10),
            temperature: Some(0.1),
            stream: false,
        };

        match self.make_api_request(request).await {
            Ok(response) => {
                let duration = start.elapsed();
                let response_time_ms = duration.as_millis() as u64;

                Ok(ProviderHealthStatus {
                    is_healthy: true,
                    version: Some(format!("OpenAI API - Model: {}", response.model)),
                    last_check: chrono::Utc::now(),
                    error_message: None,
                    response_time_ms: Some(response_time_ms),
                })
            }
            Err(e) => {
                let duration = start.elapsed();
                let response_time_ms = duration.as_millis() as u64;

                Ok(ProviderHealthStatus {
                    is_healthy: false,
                    version: None,
                    last_check: chrono::Utc::now(),
                    error_message: Some(e.to_string()),
                    response_time_ms: Some(response_time_ms),
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/codex.rs:114-252 method generate_system_prompt <-> ./crates/ccswarm/src/providers/codex.rs:770-810 function test_parse_response
  Similarity: 73.02%

[36m--- ./crates/ccswarm/src/providers/codex.rs:generate_system_prompt (lines 114-252) ---[0m
    fn generate_system_prompt(&self, identity: &AgentIdentity) -> String {
        let agent_context = format!(
            "You are a specialized AI coding assistant working as part of the ccswarm multi-agent system.\n\
             \n\
             Agent ID: {}\n\
             Specialization: {}\n\
             Workspace: {}\n\
             \n",
            identity.agent_id,
            identity.specialization.name(),
            identity.workspace_path.display()
        );

        let specialization_prompt = match &identity.specialization {
            crate::identity::AgentRole::Frontend {
                technologies,
                responsibilities,
                boundaries,
            } => {
                format!(
                    "FRONTEND SPECIALIST\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on frontend development. Do not write backend code, \
                     database schemas, or infrastructure configurations. If asked about \
                     backend concerns, explain that you need to delegate to a backend specialist.\n",
                    technologies.join(", "),
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
            crate::identity::AgentRole::Backend {
                technologies,
                responsibilities,
                boundaries,
            } => {
                format!(
                    "BACKEND SPECIALIST\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on backend development. Do not write frontend UI code, \
                     styling, or client-side logic. If asked about frontend concerns, \
                     explain that you need to delegate to a frontend specialist.\n",
                    technologies.join(", "),
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
            crate::identity::AgentRole::DevOps {
                technologies,
                responsibilities,
                boundaries,
            } => {
                format!(
                    "DEVOPS SPECIALIST\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on infrastructure, deployment, and operational concerns. \
                     Do not write application business logic or feature code. If asked about \
                     application development, explain that you need to delegate to development specialists.\n",
                    technologies.join(", "),
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
            crate::identity::AgentRole::QA {
                responsibilities,
                boundaries,
                ..
            } => {
                format!(
                    "QA SPECIALIST\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on testing, quality assurance, and validation. \
                     Do not write production application code. Focus on test strategies, \
                     test implementation, and quality verification.\n",
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
            crate::identity::AgentRole::Master {
                oversight_roles,
                quality_standards,
                ..
            } => {
                format!(
                    "MASTER ORCHESTRATOR\n\
                     Oversight: {}\n\
                     Quality Standards: Test coverage ≥{:.0}%, Max complexity ≤{}, Security scan: {}\n\
                     \n\
                     You are the master orchestrator. Do not write code directly. \
                     Instead, focus on coordination, planning, and quality review. \
                     Delegate specific implementation tasks to specialized agents.\n",
                    oversight_roles.join(", "),
                    quality_standards.min_test_coverage,
                    quality_standards.max_complexity,
                    if quality_standards.security_scan_required { "Required" } else { "Optional" }
                )
            }
            crate::identity::AgentRole::Search {
                technologies,
                responsibilities,
                boundaries,
            } => {
                format!(
                    "SEARCH SPECIALIST\n\
                     Technologies: {}\n\
                     Responsibilities: {}\n\
                     Boundaries: {}\n\
                     \n\
                     Focus exclusively on information retrieval and search operations. \
                     Do not write or modify any code. Your role is to find and present \
                     relevant information to help other agents complete their tasks.\n",
                    technologies.join(", "),
                    responsibilities.join(", "),
                    boundaries.join(", ")
                )
            }
        };

        let guidelines = "\nGUIDELINES:\n\
                         1. Always identify yourself and your role at the start of responses\n\
                         2. Stay strictly within your specialization boundaries\n\
                         3. Provide clear, actionable code and explanations\n\
                         4. If a request is outside your scope, clearly state what needs to be delegated\n\
                         5. Focus on clean, maintainable, and well-documented code\n\
                         6. Consider security, performance, and best practices\n\
                         7. Respond in a structured format that can be easily parsed\n";

        format!("{}{}\n{}", agent_context, specialization_prompt, guidelines)
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:test_parse_response (lines 770-810) ---[0m
    fn test_parse_response() {
        let config = CodexConfig::default();
        let executor = CodexExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(500);

        let response = OpenAIResponse {
            id: "test-response-id".to_string(),
            object: "chat.completion".to_string(),
            created: 1234567890,
            model: "gpt-4".to_string(),
            choices: vec![OpenAIChoice {
                index: 0,
                message: OpenAIMessage {
                    role: "assistant".to_string(),
                    content: "Here's your modal component implementation...".to_string(),
                },
                finish_reason: Some("stop".to_string()),
            }],
            usage: Some(OpenAIUsage {
                prompt_tokens: 100,
                completion_tokens: 200,
                total_tokens: 300,
            }),
        };

        let result = executor.parse_response(response, &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());
        assert_eq!(result.duration, duration);

        let output = result.output.as_object().unwrap();
        assert_eq!(output["provider"], "codex");
        assert_eq!(output["model"], "gpt-4");
        assert_eq!(output["response_id"], "test-response-id");
        assert!(output["response"]
            .as_str()
            .unwrap()
            .contains("modal component"));
    }

  ./crates/ccswarm/src/providers/codex.rs:255-385 method generate_task_prompt <-> ./crates/ccswarm/src/providers/codex.rs:770-810 function test_parse_response
  Similarity: 73.48%

[36m--- ./crates/ccswarm/src/providers/codex.rs:generate_task_prompt (lines 255-385) ---[0m
    fn generate_task_prompt(&self, _identity: &AgentIdentity, task: &Task) -> String {
        let task_header = format!(
            "TASK REQUEST\n\
             Task ID: {}\n\
             Type: {:?}\n\
             Priority: {:?}\n\
             \n",
            task.id, task.task_type, task.priority
        );

        let task_instructions = match task.task_type {
            TaskType::Development => {
                "DEVELOPMENT TASK\n\
                 Please implement the requested feature with:\n\
                 - Clean, readable code\n\
                 - Proper error handling\n\
                 - Relevant comments\n\
                 - Consider testing implications\n"
            }
            TaskType::Testing => {
                "TESTING TASK\n\
                 Please create comprehensive tests with:\n\
                 - Good test coverage\n\
                 - Edge case handling\n\
                 - Clear test descriptions\n\
                 - Mock/stub strategies where appropriate\n"
            }
            TaskType::Documentation => {
                "DOCUMENTATION TASK\n\
                 Please create clear documentation with:\n\
                 - Comprehensive explanations\n\
                 - Code examples\n\
                 - Usage instructions\n\
                 - API references where applicable\n"
            }
            TaskType::Bugfix => {
                "BUG FIX TASK\n\
                 Please analyze and fix the issue with:\n\
                 - Root cause identification\n\
                 - Minimal, targeted changes\n\
                 - Prevention of regression\n\
                 - Explanation of the fix\n"
            }
            TaskType::Infrastructure => {
                "INFRASTRUCTURE TASK\n\
                 Please handle infrastructure concerns with:\n\
                 - Security best practices\n\
                 - Scalability considerations\n\
                 - Monitoring and observability\n\
                 - Documentation of configurations\n"
            }
            TaskType::Coordination => {
                "COORDINATION TASK\n\
                 Please provide coordination guidance with:\n\
                 - Clear delegation instructions\n\
                 - Task breakdown\n\
                 - Quality requirements\n\
                 - No direct implementation\n"
            }
            TaskType::Review => {
                "REVIEW TASK\n\
                 Please review the code/documentation with:\n\
                 - Quality assessment\n\
                 - Security considerations\n\
                 - Best practice validation\n\
                 - Improvement suggestions\n"
            }
            TaskType::Feature => {
                "FEATURE TASK\n\
                 Please implement the new feature with:\n\
                 - Complete functionality\n\
                 - Proper integration\n\
                 - Testing considerations\n\
                 - Documentation updates\n"
            }
            TaskType::Remediation => {
                "REMEDIATION TASK\n\
                 Please fix the quality issues identified with:\n\
                 - Address all specific issues listed\n\
                 - Follow provided instructions exactly\n\
                 - Add tests to prevent regression\n\
                 - Improve overall code quality\n\
                 - Verify all issues are resolved\n"
            }
            TaskType::Bug => {
                "BUG FIX TASK\n\
                 Please analyze and fix the bug with:\n\
                 - Root cause identification\n\
                 - Minimal, targeted changes\n\
                 - Prevention of regression\n\
                 - Explanation of the fix\n"
            }
            TaskType::Assistance => {
                "ASSISTANCE TASK\n\
                 Please help with the requested task by:\n\
                 - Understanding the blocker\n\
                 - Providing expert guidance\n\
                 - Collaborative problem solving\n\
                 - Knowledge sharing\n"
            }
            TaskType::Research => {
                "RESEARCH TASK\n\
                 Please research and analyze information with:\n\
                 - Review search results thoroughly\n\
                 - Extract key insights\n\
                 - Apply findings to current work\n\
                 - Document recommendations\n"
            }
        };

        let task_description = format!("DESCRIPTION:\n{}\n\n", task.description);

        let task_details = if let Some(details) = &task.details {
            format!("ADDITIONAL DETAILS:\n{}\n\n", details)
        } else {
            String::new()
        };

        let response_format = "RESPONSE FORMAT:\n\
                              Please structure your response as:\n\
                              1. Agent identification and scope confirmation\n\
                              2. Task analysis and approach\n\
                              3. Implementation (code/configuration/guidance)\n\
                              4. Verification steps or testing approach\n\
                              5. Any delegation needs or dependencies\n";

        format!(
            "{}{}\n{}{}\n{}",
            task_header, task_instructions, task_description, task_details, response_format
        )
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:test_parse_response (lines 770-810) ---[0m
    fn test_parse_response() {
        let config = CodexConfig::default();
        let executor = CodexExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(500);

        let response = OpenAIResponse {
            id: "test-response-id".to_string(),
            object: "chat.completion".to_string(),
            created: 1234567890,
            model: "gpt-4".to_string(),
            choices: vec![OpenAIChoice {
                index: 0,
                message: OpenAIMessage {
                    role: "assistant".to_string(),
                    content: "Here's your modal component implementation...".to_string(),
                },
                finish_reason: Some("stop".to_string()),
            }],
            usage: Some(OpenAIUsage {
                prompt_tokens: 100,
                completion_tokens: 200,
                total_tokens: 300,
            }),
        };

        let result = executor.parse_response(response, &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());
        assert_eq!(result.duration, duration);

        let output = result.output.as_object().unwrap();
        assert_eq!(output["provider"], "codex");
        assert_eq!(output["model"], "gpt-4");
        assert_eq!(output["response_id"], "test-response-id");
        assert!(output["response"]
            .as_str()
            .unwrap()
            .contains("modal component"));
    }

  ./crates/ccswarm/src/providers/codex.rs:504-569 method execute_task <-> ./crates/ccswarm/src/providers/codex.rs:571-612 method health_check
  Similarity: 91.27%
  Classes: ProviderExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/codex.rs:execute_task (lines 504-569) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate comprehensive prompt
        let task_prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with OpenAI (model: {}) for agent {}",
            task.description,
            self.config.model,
            identity.agent_id
        );

        // Execute the prompt
        match self
            .execute_prompt(&task_prompt, identity, working_dir)
            .await
        {
            Ok(response_content) => {
                let duration = start.elapsed();

                tracing::info!(
                    "OpenAI task completed in {:?} for agent {}",
                    duration,
                    identity.agent_id
                );

                Ok(TaskResult {
                    success: true,
                    output: serde_json::json!({
                        "response": response_content,
                        "task_id": task.id,
                        "provider": "codex",
                        "model": self.config.model,
                    }),
                    error: None,
                    duration,
                })
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "OpenAI task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "codex",
                        "model": self.config.model,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:health_check (lines 571-612) ---[0m
    async fn health_check(&self, _working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        // Make a simple API call to check health
        let request = OpenAIRequest {
            model: self.config.model.clone(),
            messages: vec![OpenAIMessage {
                role: "user".to_string(),
                content: "Hello, please respond with 'OK' to confirm you're working.".to_string(),
            }],
            max_tokens: Some(10),
            temperature: Some(0.1),
            stream: false,
        };

        match self.make_api_request(request).await {
            Ok(response) => {
                let duration = start.elapsed();
                let response_time_ms = duration.as_millis() as u64;

                Ok(ProviderHealthStatus {
                    is_healthy: true,
                    version: Some(format!("OpenAI API - Model: {}", response.model)),
                    last_check: chrono::Utc::now(),
                    error_message: None,
                    response_time_ms: Some(response_time_ms),
                })
            }
            Err(e) => {
                let duration = start.elapsed();
                let response_time_ms = duration.as_millis() as u64;

                Ok(ProviderHealthStatus {
                    is_healthy: false,
                    version: None,
                    last_check: chrono::Utc::now(),
                    error_message: Some(e.to_string()),
                    response_time_ms: Some(response_time_ms),
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/codex.rs:470-502 method execute_prompt <-> ./crates/ccswarm/src/providers/codex.rs:504-569 method execute_task
  Similarity: 91.61%
  Classes: ProviderExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/codex.rs:execute_prompt (lines 470-502) ---[0m
    async fn execute_prompt(
        &self,
        prompt: &str,
        identity: &AgentIdentity,
        _working_dir: &Path,
    ) -> Result<String> {
        let system_prompt = self.generate_system_prompt(identity);

        let request = OpenAIRequest {
            model: self.config.model.clone(),
            messages: vec![
                OpenAIMessage {
                    role: "system".to_string(),
                    content: system_prompt,
                },
                OpenAIMessage {
                    role: "user".to_string(),
                    content: prompt.to_string(),
                },
            ],
            max_tokens: self.config.max_tokens,
            temperature: self.config.temperature,
            stream: false,
        };

        let response = self.make_api_request(request).await?;

        if let Some(choice) = response.choices.first() {
            Ok(choice.message.content.clone())
        } else {
            Err(anyhow::anyhow!("No response choices returned from API"))
        }
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:execute_task (lines 504-569) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate comprehensive prompt
        let task_prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with OpenAI (model: {}) for agent {}",
            task.description,
            self.config.model,
            identity.agent_id
        );

        // Execute the prompt
        match self
            .execute_prompt(&task_prompt, identity, working_dir)
            .await
        {
            Ok(response_content) => {
                let duration = start.elapsed();

                tracing::info!(
                    "OpenAI task completed in {:?} for agent {}",
                    duration,
                    identity.agent_id
                );

                Ok(TaskResult {
                    success: true,
                    output: serde_json::json!({
                        "response": response_content,
                        "task_id": task.id,
                        "provider": "codex",
                        "model": self.config.model,
                    }),
                    error: None,
                    duration,
                })
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "OpenAI task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "codex",
                        "model": self.config.model,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/codex.rs:430-465 method make_api_request <-> ./crates/ccswarm/src/providers/codex.rs:504-569 method execute_task
  Similarity: 82.13%
  Classes: CodexExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/codex.rs:make_api_request (lines 430-465) ---[0m
    async fn make_api_request(&self, request: OpenAIRequest) -> Result<OpenAIResponse> {
        let url = format!("{}/chat/completions", self.get_api_base());

        let response = self
            .client
            .post(&url)
            .json(&request)
            .send()
            .await
            .context("Failed to send request to OpenAI API")?;

        if response.status().is_success() {
            let response_text = response.text().await?;
            serde_json::from_str::<OpenAIResponse>(&response_text)
                .context("Failed to parse OpenAI API response")
        } else {
            let status = response.status();
            let error_text = response.text().await?;

            // Try to parse as OpenAI error format
            if let Ok(error_response) = serde_json::from_str::<OpenAIError>(&error_text) {
                Err(anyhow::anyhow!(
                    "OpenAI API error ({}): {} ({})",
                    status,
                    error_response.error.message,
                    error_response.error.error_type
                ))
            } else {
                Err(anyhow::anyhow!(
                    "OpenAI API error ({}): {}",
                    status,
                    error_text
                ))
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:execute_task (lines 504-569) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate comprehensive prompt
        let task_prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with OpenAI (model: {}) for agent {}",
            task.description,
            self.config.model,
            identity.agent_id
        );

        // Execute the prompt
        match self
            .execute_prompt(&task_prompt, identity, working_dir)
            .await
        {
            Ok(response_content) => {
                let duration = start.elapsed();

                tracing::info!(
                    "OpenAI task completed in {:?} for agent {}",
                    duration,
                    identity.agent_id
                );

                Ok(TaskResult {
                    success: true,
                    output: serde_json::json!({
                        "response": response_content,
                        "task_id": task.id,
                        "provider": "codex",
                        "model": self.config.model,
                    }),
                    error: None,
                    duration,
                })
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "OpenAI task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "codex",
                        "model": self.config.model,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/codex.rs:86-111 method new <-> ./crates/ccswarm/src/providers/codex.rs:504-569 method execute_task
  Similarity: 88.02%
  Classes: CodexExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/codex.rs:new (lines 86-111) ---[0m
    pub fn new(config: CodexConfig) -> Self {
        let mut headers = header::HeaderMap::new();
        headers.insert(
            header::AUTHORIZATION,
            header::HeaderValue::from_str(&format!("Bearer {}", config.api_key)).unwrap(),
        );
        headers.insert(
            header::CONTENT_TYPE,
            header::HeaderValue::from_static("application/json"),
        );

        if let Some(org) = &config.organization {
            headers.insert(
                header::HeaderName::from_static("openai-organization"),
                header::HeaderValue::from_str(org).unwrap(),
            );
        }

        let client = Client::builder()
            .default_headers(headers)
            .timeout(std::time::Duration::from_secs(120))
            .build()
            .unwrap();

        Self { config, client }
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:execute_task (lines 504-569) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate comprehensive prompt
        let task_prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with OpenAI (model: {}) for agent {}",
            task.description,
            self.config.model,
            identity.agent_id
        );

        // Execute the prompt
        match self
            .execute_prompt(&task_prompt, identity, working_dir)
            .await
        {
            Ok(response_content) => {
                let duration = start.elapsed();

                tracing::info!(
                    "OpenAI task completed in {:?} for agent {}",
                    duration,
                    identity.agent_id
                );

                Ok(TaskResult {
                    success: true,
                    output: serde_json::json!({
                        "response": response_content,
                        "task_id": task.id,
                        "provider": "codex",
                        "model": self.config.model,
                    }),
                    error: None,
                    duration,
                })
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "OpenAI task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "codex",
                        "model": self.config.model,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/codex.rs:397-427 method parse_response <-> ./crates/ccswarm/src/providers/codex.rs:504-569 method execute_task
  Similarity: 77.66%
  Classes: CodexExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/codex.rs:parse_response (lines 397-427) ---[0m
    fn parse_response(
        &self,
        response: OpenAIResponse,
        task: &Task,
        duration: std::time::Duration,
    ) -> TaskResult {
        if let Some(choice) = response.choices.first() {
            TaskResult {
                success: true,
                output: serde_json::json!({
                    "response": choice.message.content,
                    "response_id": response.id,
                    "provider": "codex",
                    "model": response.model,
                    "task_id": task.id,
                }),
                error: None,
                duration,
            }
        } else {
            TaskResult {
                success: false,
                output: serde_json::json!({
                    "provider": "codex",
                    "model": response.model,
                }),
                error: Some("No response choices returned".to_string()),
                duration,
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:execute_task (lines 504-569) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate comprehensive prompt
        let task_prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with OpenAI (model: {}) for agent {}",
            task.description,
            self.config.model,
            identity.agent_id
        );

        // Execute the prompt
        match self
            .execute_prompt(&task_prompt, identity, working_dir)
            .await
        {
            Ok(response_content) => {
                let duration = start.elapsed();

                tracing::info!(
                    "OpenAI task completed in {:?} for agent {}",
                    duration,
                    identity.agent_id
                );

                Ok(TaskResult {
                    success: true,
                    output: serde_json::json!({
                        "response": response_content,
                        "task_id": task.id,
                        "provider": "codex",
                        "model": self.config.model,
                    }),
                    error: None,
                    duration,
                })
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "OpenAI task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "codex",
                        "model": self.config.model,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/codex.rs:470-502 method execute_prompt <-> ./crates/ccswarm/src/providers/codex.rs:571-612 method health_check
  Similarity: 94.50%
  Classes: ProviderExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/codex.rs:execute_prompt (lines 470-502) ---[0m
    async fn execute_prompt(
        &self,
        prompt: &str,
        identity: &AgentIdentity,
        _working_dir: &Path,
    ) -> Result<String> {
        let system_prompt = self.generate_system_prompt(identity);

        let request = OpenAIRequest {
            model: self.config.model.clone(),
            messages: vec![
                OpenAIMessage {
                    role: "system".to_string(),
                    content: system_prompt,
                },
                OpenAIMessage {
                    role: "user".to_string(),
                    content: prompt.to_string(),
                },
            ],
            max_tokens: self.config.max_tokens,
            temperature: self.config.temperature,
            stream: false,
        };

        let response = self.make_api_request(request).await?;

        if let Some(choice) = response.choices.first() {
            Ok(choice.message.content.clone())
        } else {
            Err(anyhow::anyhow!("No response choices returned from API"))
        }
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:health_check (lines 571-612) ---[0m
    async fn health_check(&self, _working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        // Make a simple API call to check health
        let request = OpenAIRequest {
            model: self.config.model.clone(),
            messages: vec![OpenAIMessage {
                role: "user".to_string(),
                content: "Hello, please respond with 'OK' to confirm you're working.".to_string(),
            }],
            max_tokens: Some(10),
            temperature: Some(0.1),
            stream: false,
        };

        match self.make_api_request(request).await {
            Ok(response) => {
                let duration = start.elapsed();
                let response_time_ms = duration.as_millis() as u64;

                Ok(ProviderHealthStatus {
                    is_healthy: true,
                    version: Some(format!("OpenAI API - Model: {}", response.model)),
                    last_check: chrono::Utc::now(),
                    error_message: None,
                    response_time_ms: Some(response_time_ms),
                })
            }
            Err(e) => {
                let duration = start.elapsed();
                let response_time_ms = duration.as_millis() as u64;

                Ok(ProviderHealthStatus {
                    is_healthy: false,
                    version: None,
                    last_check: chrono::Utc::now(),
                    error_message: Some(e.to_string()),
                    response_time_ms: Some(response_time_ms),
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/codex.rs:430-465 method make_api_request <-> ./crates/ccswarm/src/providers/codex.rs:571-612 method health_check
  Similarity: 87.84%
  Classes: CodexExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/codex.rs:make_api_request (lines 430-465) ---[0m
    async fn make_api_request(&self, request: OpenAIRequest) -> Result<OpenAIResponse> {
        let url = format!("{}/chat/completions", self.get_api_base());

        let response = self
            .client
            .post(&url)
            .json(&request)
            .send()
            .await
            .context("Failed to send request to OpenAI API")?;

        if response.status().is_success() {
            let response_text = response.text().await?;
            serde_json::from_str::<OpenAIResponse>(&response_text)
                .context("Failed to parse OpenAI API response")
        } else {
            let status = response.status();
            let error_text = response.text().await?;

            // Try to parse as OpenAI error format
            if let Ok(error_response) = serde_json::from_str::<OpenAIError>(&error_text) {
                Err(anyhow::anyhow!(
                    "OpenAI API error ({}): {} ({})",
                    status,
                    error_response.error.message,
                    error_response.error.error_type
                ))
            } else {
                Err(anyhow::anyhow!(
                    "OpenAI API error ({}): {}",
                    status,
                    error_text
                ))
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:health_check (lines 571-612) ---[0m
    async fn health_check(&self, _working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        // Make a simple API call to check health
        let request = OpenAIRequest {
            model: self.config.model.clone(),
            messages: vec![OpenAIMessage {
                role: "user".to_string(),
                content: "Hello, please respond with 'OK' to confirm you're working.".to_string(),
            }],
            max_tokens: Some(10),
            temperature: Some(0.1),
            stream: false,
        };

        match self.make_api_request(request).await {
            Ok(response) => {
                let duration = start.elapsed();
                let response_time_ms = duration.as_millis() as u64;

                Ok(ProviderHealthStatus {
                    is_healthy: true,
                    version: Some(format!("OpenAI API - Model: {}", response.model)),
                    last_check: chrono::Utc::now(),
                    error_message: None,
                    response_time_ms: Some(response_time_ms),
                })
            }
            Err(e) => {
                let duration = start.elapsed();
                let response_time_ms = duration.as_millis() as u64;

                Ok(ProviderHealthStatus {
                    is_healthy: false,
                    version: None,
                    last_check: chrono::Utc::now(),
                    error_message: Some(e.to_string()),
                    response_time_ms: Some(response_time_ms),
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/codex.rs:504-569 method execute_task <-> ./crates/ccswarm/src/providers/codex.rs:748-767 function test_get_capabilities
  Similarity: 71.85%

[36m--- ./crates/ccswarm/src/providers/codex.rs:execute_task (lines 504-569) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate comprehensive prompt
        let task_prompt = self.generate_task_prompt(identity, task);

        tracing::info!(
            "Executing task '{}' with OpenAI (model: {}) for agent {}",
            task.description,
            self.config.model,
            identity.agent_id
        );

        // Execute the prompt
        match self
            .execute_prompt(&task_prompt, identity, working_dir)
            .await
        {
            Ok(response_content) => {
                let duration = start.elapsed();

                tracing::info!(
                    "OpenAI task completed in {:?} for agent {}",
                    duration,
                    identity.agent_id
                );

                Ok(TaskResult {
                    success: true,
                    output: serde_json::json!({
                        "response": response_content,
                        "task_id": task.id,
                        "provider": "codex",
                        "model": self.config.model,
                    }),
                    error: None,
                    duration,
                })
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "OpenAI task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "codex",
                        "model": self.config.model,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:test_get_capabilities (lines 748-767) ---[0m
    fn test_get_capabilities() {
        let config = CodexConfig::default();
        let executor = CodexExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(capabilities.supports_json_output);
        assert!(!capabilities.supports_file_operations);
        assert!(!capabilities.supports_git_operations);
        assert!(!capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);

        // Default model should be gpt-4 with 8192 context length
        assert_eq!(capabilities.max_context_length, Some(8192));
        assert!(capabilities
            .supported_languages
            .contains(&"typescript".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"rust".to_string()));
    }

  ./crates/ccswarm/src/providers/codex.rs:397-427 method parse_response <-> ./crates/ccswarm/src/providers/codex.rs:614-651 method get_capabilities
  Similarity: 87.97%
  Classes: CodexExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/codex.rs:parse_response (lines 397-427) ---[0m
    fn parse_response(
        &self,
        response: OpenAIResponse,
        task: &Task,
        duration: std::time::Duration,
    ) -> TaskResult {
        if let Some(choice) = response.choices.first() {
            TaskResult {
                success: true,
                output: serde_json::json!({
                    "response": choice.message.content,
                    "response_id": response.id,
                    "provider": "codex",
                    "model": response.model,
                    "task_id": task.id,
                }),
                error: None,
                duration,
            }
        } else {
            TaskResult {
                success: false,
                output: serde_json::json!({
                    "provider": "codex",
                    "model": response.model,
                }),
                error: Some("No response choices returned".to_string()),
                duration,
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:get_capabilities (lines 614-651) ---[0m
    fn get_capabilities(&self) -> ProviderCapabilities {
        ProviderCapabilities {
            supports_json_output: true,      // Can be instructed to return JSON
            supports_streaming: false,       // Not implemented in this executor
            supports_file_operations: false, // API-only, no direct file access
            supports_git_operations: false,  // API-only, no direct git access
            supports_code_execution: false,  // API-only, no code execution
            max_context_length: Some(match self.config.model.as_str() {
                "gpt-4" => 8192,
                "gpt-4-32k" => 32768,
                "gpt-3.5-turbo" => 4096,
                "gpt-3.5-turbo-16k" => 16384,
                _ => 4096, // Default assumption
            }),
            supported_languages: vec![
                "python".to_string(),
                "javascript".to_string(),
                "typescript".to_string(),
                "java".to_string(),
                "c++".to_string(),
                "c".to_string(),
                "c#".to_string(),
                "go".to_string(),
                "rust".to_string(),
                "php".to_string(),
                "ruby".to_string(),
                "swift".to_string(),
                "kotlin".to_string(),
                "html".to_string(),
                "css".to_string(),
                "sql".to_string(),
                "bash".to_string(),
                "yaml".to_string(),
                "json".to_string(),
                "markdown".to_string(),
            ],
        }
    }

  ./crates/ccswarm/src/providers/codex.rs:430-465 method make_api_request <-> ./crates/ccswarm/src/providers/codex.rs:470-502 method execute_prompt
  Similarity: 83.06%
  Classes: CodexExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/codex.rs:make_api_request (lines 430-465) ---[0m
    async fn make_api_request(&self, request: OpenAIRequest) -> Result<OpenAIResponse> {
        let url = format!("{}/chat/completions", self.get_api_base());

        let response = self
            .client
            .post(&url)
            .json(&request)
            .send()
            .await
            .context("Failed to send request to OpenAI API")?;

        if response.status().is_success() {
            let response_text = response.text().await?;
            serde_json::from_str::<OpenAIResponse>(&response_text)
                .context("Failed to parse OpenAI API response")
        } else {
            let status = response.status();
            let error_text = response.text().await?;

            // Try to parse as OpenAI error format
            if let Ok(error_response) = serde_json::from_str::<OpenAIError>(&error_text) {
                Err(anyhow::anyhow!(
                    "OpenAI API error ({}): {} ({})",
                    status,
                    error_response.error.message,
                    error_response.error.error_type
                ))
            } else {
                Err(anyhow::anyhow!(
                    "OpenAI API error ({}): {}",
                    status,
                    error_text
                ))
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:execute_prompt (lines 470-502) ---[0m
    async fn execute_prompt(
        &self,
        prompt: &str,
        identity: &AgentIdentity,
        _working_dir: &Path,
    ) -> Result<String> {
        let system_prompt = self.generate_system_prompt(identity);

        let request = OpenAIRequest {
            model: self.config.model.clone(),
            messages: vec![
                OpenAIMessage {
                    role: "system".to_string(),
                    content: system_prompt,
                },
                OpenAIMessage {
                    role: "user".to_string(),
                    content: prompt.to_string(),
                },
            ],
            max_tokens: self.config.max_tokens,
            temperature: self.config.temperature,
            stream: false,
        };

        let response = self.make_api_request(request).await?;

        if let Some(choice) = response.choices.first() {
            Ok(choice.message.content.clone())
        } else {
            Err(anyhow::anyhow!("No response choices returned from API"))
        }
    }

  ./crates/ccswarm/src/providers/codex.rs:86-111 method new <-> ./crates/ccswarm/src/providers/codex.rs:571-612 method health_check
  Similarity: 80.33%
  Classes: CodexExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/codex.rs:new (lines 86-111) ---[0m
    pub fn new(config: CodexConfig) -> Self {
        let mut headers = header::HeaderMap::new();
        headers.insert(
            header::AUTHORIZATION,
            header::HeaderValue::from_str(&format!("Bearer {}", config.api_key)).unwrap(),
        );
        headers.insert(
            header::CONTENT_TYPE,
            header::HeaderValue::from_static("application/json"),
        );

        if let Some(org) = &config.organization {
            headers.insert(
                header::HeaderName::from_static("openai-organization"),
                header::HeaderValue::from_str(org).unwrap(),
            );
        }

        let client = Client::builder()
            .default_headers(headers)
            .timeout(std::time::Duration::from_secs(120))
            .build()
            .unwrap();

        Self { config, client }
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:health_check (lines 571-612) ---[0m
    async fn health_check(&self, _working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        // Make a simple API call to check health
        let request = OpenAIRequest {
            model: self.config.model.clone(),
            messages: vec![OpenAIMessage {
                role: "user".to_string(),
                content: "Hello, please respond with 'OK' to confirm you're working.".to_string(),
            }],
            max_tokens: Some(10),
            temperature: Some(0.1),
            stream: false,
        };

        match self.make_api_request(request).await {
            Ok(response) => {
                let duration = start.elapsed();
                let response_time_ms = duration.as_millis() as u64;

                Ok(ProviderHealthStatus {
                    is_healthy: true,
                    version: Some(format!("OpenAI API - Model: {}", response.model)),
                    last_check: chrono::Utc::now(),
                    error_message: None,
                    response_time_ms: Some(response_time_ms),
                })
            }
            Err(e) => {
                let duration = start.elapsed();
                let response_time_ms = duration.as_millis() as u64;

                Ok(ProviderHealthStatus {
                    is_healthy: false,
                    version: None,
                    last_check: chrono::Utc::now(),
                    error_message: Some(e.to_string()),
                    response_time_ms: Some(response_time_ms),
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/codex.rs:614-651 method get_capabilities <-> ./crates/ccswarm/src/providers/codex.rs:662-677 function create_test_identity
  Similarity: 88.51%

[36m--- ./crates/ccswarm/src/providers/codex.rs:get_capabilities (lines 614-651) ---[0m
    fn get_capabilities(&self) -> ProviderCapabilities {
        ProviderCapabilities {
            supports_json_output: true,      // Can be instructed to return JSON
            supports_streaming: false,       // Not implemented in this executor
            supports_file_operations: false, // API-only, no direct file access
            supports_git_operations: false,  // API-only, no direct git access
            supports_code_execution: false,  // API-only, no code execution
            max_context_length: Some(match self.config.model.as_str() {
                "gpt-4" => 8192,
                "gpt-4-32k" => 32768,
                "gpt-3.5-turbo" => 4096,
                "gpt-3.5-turbo-16k" => 16384,
                _ => 4096, // Default assumption
            }),
            supported_languages: vec![
                "python".to_string(),
                "javascript".to_string(),
                "typescript".to_string(),
                "java".to_string(),
                "c++".to_string(),
                "c".to_string(),
                "c#".to_string(),
                "go".to_string(),
                "rust".to_string(),
                "php".to_string(),
                "ruby".to_string(),
                "swift".to_string(),
                "kotlin".to_string(),
                "html".to_string(),
                "css".to_string(),
                "sql".to_string(),
                "bash".to_string(),
                "yaml".to_string(),
                "json".to_string(),
                "markdown".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:create_test_identity (lines 662-677) ---[0m
    fn create_test_identity() -> AgentIdentity {
        let temp_dir = TempDir::new().unwrap();
        AgentIdentity {
            agent_id: "test-codex-agent".to_string(),
            specialization: AgentRole::Frontend {
                technologies: vec!["React".to_string(), "TypeScript".to_string()],
                responsibilities: vec!["UI development".to_string()],
                boundaries: vec!["No backend work".to_string()],
            },
            workspace_path: temp_dir.path().to_path_buf(),
            env_vars: HashMap::new(),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: "12345".to_string(),
            initialized_at: chrono::Utc::now(),
        }
    }

  ./crates/ccswarm/src/providers/codex.rs:571-612 method health_check <-> ./crates/ccswarm/src/providers/codex.rs:662-677 function create_test_identity
  Similarity: 73.14%

[36m--- ./crates/ccswarm/src/providers/codex.rs:health_check (lines 571-612) ---[0m
    async fn health_check(&self, _working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        // Make a simple API call to check health
        let request = OpenAIRequest {
            model: self.config.model.clone(),
            messages: vec![OpenAIMessage {
                role: "user".to_string(),
                content: "Hello, please respond with 'OK' to confirm you're working.".to_string(),
            }],
            max_tokens: Some(10),
            temperature: Some(0.1),
            stream: false,
        };

        match self.make_api_request(request).await {
            Ok(response) => {
                let duration = start.elapsed();
                let response_time_ms = duration.as_millis() as u64;

                Ok(ProviderHealthStatus {
                    is_healthy: true,
                    version: Some(format!("OpenAI API - Model: {}", response.model)),
                    last_check: chrono::Utc::now(),
                    error_message: None,
                    response_time_ms: Some(response_time_ms),
                })
            }
            Err(e) => {
                let duration = start.elapsed();
                let response_time_ms = duration.as_millis() as u64;

                Ok(ProviderHealthStatus {
                    is_healthy: false,
                    version: None,
                    last_check: chrono::Utc::now(),
                    error_message: Some(e.to_string()),
                    response_time_ms: Some(response_time_ms),
                })
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:create_test_identity (lines 662-677) ---[0m
    fn create_test_identity() -> AgentIdentity {
        let temp_dir = TempDir::new().unwrap();
        AgentIdentity {
            agent_id: "test-codex-agent".to_string(),
            specialization: AgentRole::Frontend {
                technologies: vec!["React".to_string(), "TypeScript".to_string()],
                responsibilities: vec!["UI development".to_string()],
                boundaries: vec!["No backend work".to_string()],
            },
            workspace_path: temp_dir.path().to_path_buf(),
            env_vars: HashMap::new(),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: "12345".to_string(),
            initialized_at: chrono::Utc::now(),
        }
    }

  ./crates/ccswarm/src/providers/codex.rs:470-502 method execute_prompt <-> ./crates/ccswarm/src/providers/codex.rs:690-703 function test_codex_executor_creation
  Similarity: 86.68%

[36m--- ./crates/ccswarm/src/providers/codex.rs:execute_prompt (lines 470-502) ---[0m
    async fn execute_prompt(
        &self,
        prompt: &str,
        identity: &AgentIdentity,
        _working_dir: &Path,
    ) -> Result<String> {
        let system_prompt = self.generate_system_prompt(identity);

        let request = OpenAIRequest {
            model: self.config.model.clone(),
            messages: vec![
                OpenAIMessage {
                    role: "system".to_string(),
                    content: system_prompt,
                },
                OpenAIMessage {
                    role: "user".to_string(),
                    content: prompt.to_string(),
                },
            ],
            max_tokens: self.config.max_tokens,
            temperature: self.config.temperature,
            stream: false,
        };

        let response = self.make_api_request(request).await?;

        if let Some(choice) = response.choices.first() {
            Ok(choice.message.content.clone())
        } else {
            Err(anyhow::anyhow!("No response choices returned from API"))
        }
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:test_codex_executor_creation (lines 690-703) ---[0m
    fn test_codex_executor_creation() {
        let config = CodexConfig {
            api_key: "test-key".to_string(),
            model: "gpt-4".to_string(),
            max_tokens: Some(1000),
            temperature: Some(0.7),
            api_base: None,
            organization: None,
        };

        let executor = CodexExecutor::new(config);
        assert_eq!(executor.config.model, "gpt-4");
        assert_eq!(executor.config.max_tokens, Some(1000));
    }

  ./crates/ccswarm/src/providers/codex.rs:470-502 method execute_prompt <-> ./crates/ccswarm/src/providers/codex.rs:737-745 function test_get_api_base
  Similarity: 74.49%

[36m--- ./crates/ccswarm/src/providers/codex.rs:execute_prompt (lines 470-502) ---[0m
    async fn execute_prompt(
        &self,
        prompt: &str,
        identity: &AgentIdentity,
        _working_dir: &Path,
    ) -> Result<String> {
        let system_prompt = self.generate_system_prompt(identity);

        let request = OpenAIRequest {
            model: self.config.model.clone(),
            messages: vec![
                OpenAIMessage {
                    role: "system".to_string(),
                    content: system_prompt,
                },
                OpenAIMessage {
                    role: "user".to_string(),
                    content: prompt.to_string(),
                },
            ],
            max_tokens: self.config.max_tokens,
            temperature: self.config.temperature,
            stream: false,
        };

        let response = self.make_api_request(request).await?;

        if let Some(choice) = response.choices.first() {
            Ok(choice.message.content.clone())
        } else {
            Err(anyhow::anyhow!("No response choices returned from API"))
        }
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:test_get_api_base (lines 737-745) ---[0m
    fn test_get_api_base() {
        let mut config = CodexConfig::default();
        let executor1 = CodexExecutor::new(config.clone());
        assert_eq!(executor1.get_api_base(), "https://api.openai.com/v1");

        config.api_base = Some("https://custom.api.com/v1".to_string());
        let executor2 = CodexExecutor::new(config);
        assert_eq!(executor2.get_api_base(), "https://custom.api.com/v1");
    }

  ./crates/ccswarm/src/providers/codex.rs:86-111 method new <-> ./crates/ccswarm/src/providers/codex.rs:690-703 function test_codex_executor_creation
  Similarity: 76.68%

[36m--- ./crates/ccswarm/src/providers/codex.rs:new (lines 86-111) ---[0m
    pub fn new(config: CodexConfig) -> Self {
        let mut headers = header::HeaderMap::new();
        headers.insert(
            header::AUTHORIZATION,
            header::HeaderValue::from_str(&format!("Bearer {}", config.api_key)).unwrap(),
        );
        headers.insert(
            header::CONTENT_TYPE,
            header::HeaderValue::from_static("application/json"),
        );

        if let Some(org) = &config.organization {
            headers.insert(
                header::HeaderName::from_static("openai-organization"),
                header::HeaderValue::from_str(org).unwrap(),
            );
        }

        let client = Client::builder()
            .default_headers(headers)
            .timeout(std::time::Duration::from_secs(120))
            .build()
            .unwrap();

        Self { config, client }
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:test_codex_executor_creation (lines 690-703) ---[0m
    fn test_codex_executor_creation() {
        let config = CodexConfig {
            api_key: "test-key".to_string(),
            model: "gpt-4".to_string(),
            max_tokens: Some(1000),
            temperature: Some(0.7),
            api_base: None,
            organization: None,
        };

        let executor = CodexExecutor::new(config);
        assert_eq!(executor.config.model, "gpt-4");
        assert_eq!(executor.config.max_tokens, Some(1000));
    }

  ./crates/ccswarm/src/providers/codex.rs:86-111 method new <-> ./crates/ccswarm/src/providers/codex.rs:706-718 function test_generate_system_prompt
  Similarity: 70.60%

[36m--- ./crates/ccswarm/src/providers/codex.rs:new (lines 86-111) ---[0m
    pub fn new(config: CodexConfig) -> Self {
        let mut headers = header::HeaderMap::new();
        headers.insert(
            header::AUTHORIZATION,
            header::HeaderValue::from_str(&format!("Bearer {}", config.api_key)).unwrap(),
        );
        headers.insert(
            header::CONTENT_TYPE,
            header::HeaderValue::from_static("application/json"),
        );

        if let Some(org) = &config.organization {
            headers.insert(
                header::HeaderName::from_static("openai-organization"),
                header::HeaderValue::from_str(org).unwrap(),
            );
        }

        let client = Client::builder()
            .default_headers(headers)
            .timeout(std::time::Duration::from_secs(120))
            .build()
            .unwrap();

        Self { config, client }
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:test_generate_system_prompt (lines 706-718) ---[0m
    fn test_generate_system_prompt() {
        let config = CodexConfig::default();
        let executor = CodexExecutor::new(config);
        let identity = create_test_identity();

        let prompt = executor.generate_system_prompt(&identity);

        assert!(prompt.contains("ccswarm multi-agent system"));
        assert!(prompt.contains("FRONTEND SPECIALIST"));
        assert!(prompt.contains("test-codex-agent"));
        assert!(prompt.contains("React, TypeScript"));
        assert!(prompt.contains("Do not write backend code"));
    }

  ./crates/ccswarm/src/providers/codex.rs:721-734 function test_generate_task_prompt <-> ./crates/ccswarm/src/providers/codex.rs:748-767 function test_get_capabilities
  Similarity: 75.56%

[36m--- ./crates/ccswarm/src/providers/codex.rs:test_generate_task_prompt (lines 721-734) ---[0m
    fn test_generate_task_prompt() {
        let config = CodexConfig::default();
        let executor = CodexExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let prompt = executor.generate_task_prompt(&identity, &task);

        assert!(prompt.contains("TASK REQUEST"));
        assert!(prompt.contains("Development"));
        assert!(prompt.contains("Create a modal component"));
        assert!(prompt.contains("DEVELOPMENT TASK"));
        assert!(prompt.contains("RESPONSE FORMAT"));
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:test_get_capabilities (lines 748-767) ---[0m
    fn test_get_capabilities() {
        let config = CodexConfig::default();
        let executor = CodexExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(capabilities.supports_json_output);
        assert!(!capabilities.supports_file_operations);
        assert!(!capabilities.supports_git_operations);
        assert!(!capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);

        // Default model should be gpt-4 with 8192 context length
        assert_eq!(capabilities.max_context_length, Some(8192));
        assert!(capabilities
            .supported_languages
            .contains(&"typescript".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"rust".to_string()));
    }

  ./crates/ccswarm/src/providers/codex.rs:706-718 function test_generate_system_prompt <-> ./crates/ccswarm/src/providers/codex.rs:721-734 function test_generate_task_prompt
  Similarity: 90.63%

[36m--- ./crates/ccswarm/src/providers/codex.rs:test_generate_system_prompt (lines 706-718) ---[0m
    fn test_generate_system_prompt() {
        let config = CodexConfig::default();
        let executor = CodexExecutor::new(config);
        let identity = create_test_identity();

        let prompt = executor.generate_system_prompt(&identity);

        assert!(prompt.contains("ccswarm multi-agent system"));
        assert!(prompt.contains("FRONTEND SPECIALIST"));
        assert!(prompt.contains("test-codex-agent"));
        assert!(prompt.contains("React, TypeScript"));
        assert!(prompt.contains("Do not write backend code"));
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:test_generate_task_prompt (lines 721-734) ---[0m
    fn test_generate_task_prompt() {
        let config = CodexConfig::default();
        let executor = CodexExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let prompt = executor.generate_task_prompt(&identity, &task);

        assert!(prompt.contains("TASK REQUEST"));
        assert!(prompt.contains("Development"));
        assert!(prompt.contains("Create a modal component"));
        assert!(prompt.contains("DEVELOPMENT TASK"));
        assert!(prompt.contains("RESPONSE FORMAT"));
    }

  ./crates/ccswarm/src/providers/codex.rs:662-677 function create_test_identity <-> ./crates/ccswarm/src/providers/codex.rs:690-703 function test_codex_executor_creation
  Similarity: 80.70%

[36m--- ./crates/ccswarm/src/providers/codex.rs:create_test_identity (lines 662-677) ---[0m
    fn create_test_identity() -> AgentIdentity {
        let temp_dir = TempDir::new().unwrap();
        AgentIdentity {
            agent_id: "test-codex-agent".to_string(),
            specialization: AgentRole::Frontend {
                technologies: vec!["React".to_string(), "TypeScript".to_string()],
                responsibilities: vec!["UI development".to_string()],
                boundaries: vec!["No backend work".to_string()],
            },
            workspace_path: temp_dir.path().to_path_buf(),
            env_vars: HashMap::new(),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: "12345".to_string(),
            initialized_at: chrono::Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:test_codex_executor_creation (lines 690-703) ---[0m
    fn test_codex_executor_creation() {
        let config = CodexConfig {
            api_key: "test-key".to_string(),
            model: "gpt-4".to_string(),
            max_tokens: Some(1000),
            temperature: Some(0.7),
            api_base: None,
            organization: None,
        };

        let executor = CodexExecutor::new(config);
        assert_eq!(executor.config.model, "gpt-4");
        assert_eq!(executor.config.max_tokens, Some(1000));
    }

  ./crates/ccswarm/src/providers/codex.rs:706-718 function test_generate_system_prompt <-> ./crates/ccswarm/src/providers/codex.rs:748-767 function test_get_capabilities
  Similarity: 71.64%

[36m--- ./crates/ccswarm/src/providers/codex.rs:test_generate_system_prompt (lines 706-718) ---[0m
    fn test_generate_system_prompt() {
        let config = CodexConfig::default();
        let executor = CodexExecutor::new(config);
        let identity = create_test_identity();

        let prompt = executor.generate_system_prompt(&identity);

        assert!(prompt.contains("ccswarm multi-agent system"));
        assert!(prompt.contains("FRONTEND SPECIALIST"));
        assert!(prompt.contains("test-codex-agent"));
        assert!(prompt.contains("React, TypeScript"));
        assert!(prompt.contains("Do not write backend code"));
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:test_get_capabilities (lines 748-767) ---[0m
    fn test_get_capabilities() {
        let config = CodexConfig::default();
        let executor = CodexExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(capabilities.supports_json_output);
        assert!(!capabilities.supports_file_operations);
        assert!(!capabilities.supports_git_operations);
        assert!(!capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);

        // Default model should be gpt-4 with 8192 context length
        assert_eq!(capabilities.max_context_length, Some(8192));
        assert!(capabilities
            .supported_languages
            .contains(&"typescript".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"rust".to_string()));
    }

  ./crates/ccswarm/src/providers/codex.rs:388-393 method get_api_base <-> ./crates/ccswarm/src/providers/codex.rs:679-687 function create_test_task
  Similarity: 87.04%

[36m--- ./crates/ccswarm/src/providers/codex.rs:get_api_base (lines 388-393) ---[0m
    fn get_api_base(&self) -> String {
        self.config
            .api_base
            .clone()
            .unwrap_or_else(|| "https://api.openai.com/v1".to_string())
    }

[36m--- ./crates/ccswarm/src/providers/codex.rs:create_test_task (lines 679-687) ---[0m
    fn create_test_task() -> Task {
        Task::new(
            Uuid::new_v4().to_string(),
            "Create a modal component".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        )
        .with_details("Implement a reusable modal with TypeScript".to_string())
    }


Duplicates in ./crates/ccswarm/src/tests/auto_create_tests.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/tests/auto_create_tests.rs:20-43 function test_app_type_detection <-> ./crates/ccswarm/src/tests/auto_create_tests.rs:46-62 function test_task_decomposition
  Similarity: 83.73%

[36m--- ./crates/ccswarm/src/tests/auto_create_tests.rs:test_app_type_detection (lines 20-43) ---[0m
async fn test_app_type_detection() {
    let mut engine = AutoCreateEngine::new();

    // Test TODO detection
    let tasks = engine
        .analyze_and_decompose("Create a TODO application")
        .await
        .unwrap();
    assert!(!tasks.is_empty());

    // Test Blog detection
    let tasks = engine
        .analyze_and_decompose("Create a blog website")
        .await
        .unwrap();
    assert!(!tasks.is_empty());

    // Test multiple app types are detected properly
    let tasks = engine
        .analyze_and_decompose("Create an online shop")
        .await
        .unwrap();
    assert!(!tasks.is_empty());
}

[36m--- ./crates/ccswarm/src/tests/auto_create_tests.rs:test_task_decomposition (lines 46-62) ---[0m
async fn test_task_decomposition() {
    let mut engine = AutoCreateEngine::new();

    let tasks = engine
        .analyze_and_decompose("Create a TODO app with authentication")
        .await
        .unwrap();

    // Should have standard tasks
    assert!(tasks.iter().any(|t| t.description.contains("React")));
    assert!(tasks.iter().any(|t| t.description.contains("REST API")));

    // Should have authentication task
    assert!(tasks
        .iter()
        .any(|t| t.description.contains("authentication") || t.description.contains("JWT")));
}

  ./crates/ccswarm/src/tests/auto_create_tests.rs:46-62 function test_task_decomposition <-> ./crates/ccswarm/src/tests/auto_create_tests.rs:93-108 function test_responsive_design_customization
  Similarity: 89.75%

[36m--- ./crates/ccswarm/src/tests/auto_create_tests.rs:test_task_decomposition (lines 46-62) ---[0m
async fn test_task_decomposition() {
    let mut engine = AutoCreateEngine::new();

    let tasks = engine
        .analyze_and_decompose("Create a TODO app with authentication")
        .await
        .unwrap();

    // Should have standard tasks
    assert!(tasks.iter().any(|t| t.description.contains("React")));
    assert!(tasks.iter().any(|t| t.description.contains("REST API")));

    // Should have authentication task
    assert!(tasks
        .iter()
        .any(|t| t.description.contains("authentication") || t.description.contains("JWT")));
}

[36m--- ./crates/ccswarm/src/tests/auto_create_tests.rs:test_responsive_design_customization (lines 93-108) ---[0m
async fn test_responsive_design_customization() {
    let mut engine = AutoCreateEngine::new();

    let tasks = engine
        .analyze_and_decompose("Create a mobile-responsive TODO app")
        .await
        .unwrap();

    // Should have mobile/responsive mentioned in frontend task
    let frontend_task = tasks
        .iter()
        .find(|t| t.description.contains("React"))
        .expect("Should have frontend task");

    assert!(frontend_task.description.contains("mobile-responsive"));
}

  ./crates/ccswarm/src/tests/auto_create_tests.rs:7-17 function test_auto_create_engine_initialization <-> ./crates/ccswarm/src/tests/auto_create_tests.rs:20-43 function test_app_type_detection
  Similarity: 82.45%

[36m--- ./crates/ccswarm/src/tests/auto_create_tests.rs:test_auto_create_engine_initialization (lines 7-17) ---[0m
async fn test_auto_create_engine_initialization() {
    let mut engine = AutoCreateEngine::new();

    // Should have templates for TODO app
    let tasks = engine
        .analyze_and_decompose("Create a TODO app")
        .await
        .unwrap();
    assert!(!tasks.is_empty());
    assert!(tasks.len() >= 4); // Frontend, Backend, DB, Tests
}

[36m--- ./crates/ccswarm/src/tests/auto_create_tests.rs:test_app_type_detection (lines 20-43) ---[0m
async fn test_app_type_detection() {
    let mut engine = AutoCreateEngine::new();

    // Test TODO detection
    let tasks = engine
        .analyze_and_decompose("Create a TODO application")
        .await
        .unwrap();
    assert!(!tasks.is_empty());

    // Test Blog detection
    let tasks = engine
        .analyze_and_decompose("Create a blog website")
        .await
        .unwrap();
    assert!(!tasks.is_empty());

    // Test multiple app types are detected properly
    let tasks = engine
        .analyze_and_decompose("Create an online shop")
        .await
        .unwrap();
    assert!(!tasks.is_empty());
}

  ./crates/ccswarm/src/tests/auto_create_tests.rs:93-108 function test_responsive_design_customization <-> ./crates/ccswarm/src/tests/auto_create_tests.rs:111-123 function test_realtime_features_customization
  Similarity: 92.00%

[36m--- ./crates/ccswarm/src/tests/auto_create_tests.rs:test_responsive_design_customization (lines 93-108) ---[0m
async fn test_responsive_design_customization() {
    let mut engine = AutoCreateEngine::new();

    let tasks = engine
        .analyze_and_decompose("Create a mobile-responsive TODO app")
        .await
        .unwrap();

    // Should have mobile/responsive mentioned in frontend task
    let frontend_task = tasks
        .iter()
        .find(|t| t.description.contains("React"))
        .expect("Should have frontend task");

    assert!(frontend_task.description.contains("mobile-responsive"));
}

[36m--- ./crates/ccswarm/src/tests/auto_create_tests.rs:test_realtime_features_customization (lines 111-123) ---[0m
async fn test_realtime_features_customization() {
    let mut engine = AutoCreateEngine::new();

    let tasks = engine
        .analyze_and_decompose("Create a real-time chat application")
        .await
        .unwrap();

    // Should have WebSocket task
    assert!(tasks
        .iter()
        .any(|t| t.description.contains("real-time") || t.description.contains("WebSocket")));
}

  ./crates/ccswarm/src/tests/auto_create_tests.rs:46-62 function test_task_decomposition <-> ./crates/ccswarm/src/tests/auto_create_tests.rs:111-123 function test_realtime_features_customization
  Similarity: 81.93%

[36m--- ./crates/ccswarm/src/tests/auto_create_tests.rs:test_task_decomposition (lines 46-62) ---[0m
async fn test_task_decomposition() {
    let mut engine = AutoCreateEngine::new();

    let tasks = engine
        .analyze_and_decompose("Create a TODO app with authentication")
        .await
        .unwrap();

    // Should have standard tasks
    assert!(tasks.iter().any(|t| t.description.contains("React")));
    assert!(tasks.iter().any(|t| t.description.contains("REST API")));

    // Should have authentication task
    assert!(tasks
        .iter()
        .any(|t| t.description.contains("authentication") || t.description.contains("JWT")));
}

[36m--- ./crates/ccswarm/src/tests/auto_create_tests.rs:test_realtime_features_customization (lines 111-123) ---[0m
async fn test_realtime_features_customization() {
    let mut engine = AutoCreateEngine::new();

    let tasks = engine
        .analyze_and_decompose("Create a real-time chat application")
        .await
        .unwrap();

    // Should have WebSocket task
    assert!(tasks
        .iter()
        .any(|t| t.description.contains("real-time") || t.description.contains("WebSocket")));
}

  ./crates/ccswarm/src/tests/auto_create_tests.rs:7-17 function test_auto_create_engine_initialization <-> ./crates/ccswarm/src/tests/auto_create_tests.rs:93-108 function test_responsive_design_customization
  Similarity: 85.39%

[36m--- ./crates/ccswarm/src/tests/auto_create_tests.rs:test_auto_create_engine_initialization (lines 7-17) ---[0m
async fn test_auto_create_engine_initialization() {
    let mut engine = AutoCreateEngine::new();

    // Should have templates for TODO app
    let tasks = engine
        .analyze_and_decompose("Create a TODO app")
        .await
        .unwrap();
    assert!(!tasks.is_empty());
    assert!(tasks.len() >= 4); // Frontend, Backend, DB, Tests
}

[36m--- ./crates/ccswarm/src/tests/auto_create_tests.rs:test_responsive_design_customization (lines 93-108) ---[0m
async fn test_responsive_design_customization() {
    let mut engine = AutoCreateEngine::new();

    let tasks = engine
        .analyze_and_decompose("Create a mobile-responsive TODO app")
        .await
        .unwrap();

    // Should have mobile/responsive mentioned in frontend task
    let frontend_task = tasks
        .iter()
        .find(|t| t.description.contains("React"))
        .expect("Should have frontend task");

    assert!(frontend_task.description.contains("mobile-responsive"));
}

  ./crates/ccswarm/src/tests/auto_create_tests.rs:7-17 function test_auto_create_engine_initialization <-> ./crates/ccswarm/src/tests/auto_create_tests.rs:111-123 function test_realtime_features_customization
  Similarity: 87.69%

[36m--- ./crates/ccswarm/src/tests/auto_create_tests.rs:test_auto_create_engine_initialization (lines 7-17) ---[0m
async fn test_auto_create_engine_initialization() {
    let mut engine = AutoCreateEngine::new();

    // Should have templates for TODO app
    let tasks = engine
        .analyze_and_decompose("Create a TODO app")
        .await
        .unwrap();
    assert!(!tasks.is_empty());
    assert!(tasks.len() >= 4); // Frontend, Backend, DB, Tests
}

[36m--- ./crates/ccswarm/src/tests/auto_create_tests.rs:test_realtime_features_customization (lines 111-123) ---[0m
async fn test_realtime_features_customization() {
    let mut engine = AutoCreateEngine::new();

    let tasks = engine
        .analyze_and_decompose("Create a real-time chat application")
        .await
        .unwrap();

    // Should have WebSocket task
    assert!(tasks
        .iter()
        .any(|t| t.description.contains("real-time") || t.description.contains("WebSocket")));
}


Duplicates in ./crates/ccswarm/src/tests/integration_tests.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/tests/integration_tests.rs:330-435 function test_multi_agent_workflow_simulation <-> ./crates/ccswarm/src/tests/integration_tests.rs:484-543 function test_concurrent_agent_operations
  Similarity: 92.76%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_multi_agent_workflow_simulation (lines 330-435) ---[0m
    async fn test_multi_agent_workflow_simulation() {
        let bus = CoordinationBus::new().await.unwrap();
        let queue = TaskQueue::new().await.unwrap();
        let tracker = StatusTracker::new().await.unwrap();

        // Simulate a multi-agent workflow
        let workflow_tasks = vec![
            ("frontend", "Create user registration form"),
            ("backend", "Implement user registration API"),
            ("qa", "Test user registration flow"),
            ("devops", "Deploy user registration feature"),
        ];

        // 1. Add all tasks to queue
        for (_agent_type, description) in &workflow_tasks {
            let task = Task::new(
                Uuid::new_v4().to_string(),
                description.to_string(),
                Priority::High,
                TaskType::Development,
            );
            queue.add_task(&task).await.unwrap();
        }

        // 2. Simulate agents picking up tasks
        let mut completed_tasks = Vec::new();

        for (agent_type, description) in &workflow_tasks {
            let agent_id = format!("{}-agent", agent_type);

            // Update agent status to working
            tracker
                .update_status(
                    &agent_id,
                    &crate::agent::AgentStatus::Working,
                    serde_json::json!({"current_task": description}),
                )
                .await
                .unwrap();

            // Send status update message
            bus.send_message(AgentMessage::StatusUpdate {
                agent_id: agent_id.clone(),
                status: crate::agent::AgentStatus::Working,
                metrics: serde_json::Value::Object(serde_json::Map::new()),
            })
            .await
            .unwrap();

            // Simulate task completion
            let task_id = Uuid::new_v4().to_string();
            let result = crate::agent::TaskResult {
                success: true,
                output: serde_json::json!({
                    "task": description,
                    "agent": agent_type,
                    "completed_at": chrono::Utc::now(),
                }),
                error: None,
                duration: std::time::Duration::from_secs(300),
            };

            // Send completion message
            bus.send_message(AgentMessage::TaskCompleted {
                agent_id: agent_id.clone(),
                task_id: task_id.clone(),
                result: result.clone(),
            })
            .await
            .unwrap();

            completed_tasks.push((agent_id.clone(), task_id, result));

            // Update agent status to available
            tracker
                .update_status(
                    &agent_id,
                    &crate::agent::AgentStatus::Available,
                    serde_json::json!({"last_task": description}),
                )
                .await
                .unwrap();
        }

        // 3. Verify workflow completion
        assert_eq!(completed_tasks.len(), workflow_tasks.len());

        // Check all agents are available
        let all_statuses = tracker.get_all_statuses().await.unwrap();
        assert_eq!(all_statuses.len(), workflow_tasks.len());

        for status in &all_statuses {
            let agent_status: crate::agent::AgentStatus =
                serde_json::from_value(status["status"].clone()).unwrap();
            assert!(matches!(agent_status, crate::agent::AgentStatus::Available));
        }

        // 4. Verify message flow
        let mut received_messages = Vec::new();
        while let Some(message) = bus.try_receive_message() {
            received_messages.push(message);
        }

        // Should have received 2 messages per agent (status update + completion)
        assert_eq!(received_messages.len(), workflow_tasks.len() * 2);
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_concurrent_agent_operations (lines 484-543) ---[0m
    async fn test_concurrent_agent_operations() {
        use tokio::task;

        let bus = CoordinationBus::new().await.unwrap();
        let queue = TaskQueue::new().await.unwrap();

        // Create multiple tasks
        let tasks: Vec<_> = (0..10)
            .map(|i| {
                Task::new(
                    format!("task-{}", i),
                    format!("Test task {}", i),
                    Priority::Medium,
                    TaskType::Development,
                )
            })
            .collect();

        // Simulate concurrent operations
        let mut handles = Vec::new();

        // Task creation tasks
        for task in tasks {
            let queue_clone = queue.clone();
            let handle = task::spawn(async move {
                queue_clone.add_task(&task).await.unwrap();
            });
            handles.push(handle);
        }

        // Message sending tasks
        for i in 0..5 {
            let bus_clone = bus.clone();
            let agent_id = format!("agent-{}", i);
            let handle = task::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id,
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all operations to complete
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify results
        let pending_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(pending_tasks.len(), 10);

        // Verify at least some messages were received
        let mut message_count = 0;
        while bus.try_receive_message().is_some() {
            message_count += 1;
        }
        assert!(message_count > 0);
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:108-185 function test_coordination_bus_message_flow <-> ./crates/ccswarm/src/tests/integration_tests.rs:330-435 function test_multi_agent_workflow_simulation
  Similarity: 78.63%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_coordination_bus_message_flow (lines 108-185) ---[0m
    async fn test_coordination_bus_message_flow() {
        let bus = CoordinationBus::new().await.unwrap();

        // Test various message types
        let messages = vec![
            AgentMessage::StatusUpdate {
                agent_id: "frontend-agent".to_string(),
                status: crate::agent::AgentStatus::Available,
                metrics: serde_json::Value::Object(serde_json::Map::new()),
            },
            AgentMessage::TaskCompleted {
                agent_id: "backend-agent".to_string(),
                task_id: "task-1".to_string(),
                result: crate::agent::TaskResult {
                    success: true,
                    output: serde_json::json!({"result": "completed"}),
                    error: None,
                    duration: std::time::Duration::from_secs(120),
                },
            },
            AgentMessage::RequestAssistance {
                agent_id: "frontend-agent".to_string(),
                task_id: "task-2".to_string(),
                reason: "Need backend API specification".to_string(),
            },
        ];

        // Send all messages
        for message in &messages {
            bus.send_message(message.clone()).await.unwrap();
        }

        // Receive and verify all messages
        for expected_message in &messages {
            let received = bus.receive_message().await.unwrap();

            match (expected_message, &received) {
                (
                    AgentMessage::StatusUpdate { agent_id: e_id, .. },
                    AgentMessage::StatusUpdate { agent_id: r_id, .. },
                ) => {
                    assert_eq!(e_id, r_id);
                }
                (
                    AgentMessage::TaskCompleted {
                        agent_id: e_id,
                        task_id: e_task,
                        ..
                    },
                    AgentMessage::TaskCompleted {
                        agent_id: r_id,
                        task_id: r_task,
                        ..
                    },
                ) => {
                    assert_eq!(e_id, r_id);
                    assert_eq!(e_task, r_task);
                }
                (
                    AgentMessage::RequestAssistance {
                        agent_id: e_id,
                        task_id: e_task,
                        reason: e_reason,
                    },
                    AgentMessage::RequestAssistance {
                        agent_id: r_id,
                        task_id: r_task,
                        reason: r_reason,
                    },
                ) => {
                    assert_eq!(e_id, r_id);
                    assert_eq!(e_task, r_task);
                    assert_eq!(e_reason, r_reason);
                }
                _ => panic!("Message type mismatch"),
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_multi_agent_workflow_simulation (lines 330-435) ---[0m
    async fn test_multi_agent_workflow_simulation() {
        let bus = CoordinationBus::new().await.unwrap();
        let queue = TaskQueue::new().await.unwrap();
        let tracker = StatusTracker::new().await.unwrap();

        // Simulate a multi-agent workflow
        let workflow_tasks = vec![
            ("frontend", "Create user registration form"),
            ("backend", "Implement user registration API"),
            ("qa", "Test user registration flow"),
            ("devops", "Deploy user registration feature"),
        ];

        // 1. Add all tasks to queue
        for (_agent_type, description) in &workflow_tasks {
            let task = Task::new(
                Uuid::new_v4().to_string(),
                description.to_string(),
                Priority::High,
                TaskType::Development,
            );
            queue.add_task(&task).await.unwrap();
        }

        // 2. Simulate agents picking up tasks
        let mut completed_tasks = Vec::new();

        for (agent_type, description) in &workflow_tasks {
            let agent_id = format!("{}-agent", agent_type);

            // Update agent status to working
            tracker
                .update_status(
                    &agent_id,
                    &crate::agent::AgentStatus::Working,
                    serde_json::json!({"current_task": description}),
                )
                .await
                .unwrap();

            // Send status update message
            bus.send_message(AgentMessage::StatusUpdate {
                agent_id: agent_id.clone(),
                status: crate::agent::AgentStatus::Working,
                metrics: serde_json::Value::Object(serde_json::Map::new()),
            })
            .await
            .unwrap();

            // Simulate task completion
            let task_id = Uuid::new_v4().to_string();
            let result = crate::agent::TaskResult {
                success: true,
                output: serde_json::json!({
                    "task": description,
                    "agent": agent_type,
                    "completed_at": chrono::Utc::now(),
                }),
                error: None,
                duration: std::time::Duration::from_secs(300),
            };

            // Send completion message
            bus.send_message(AgentMessage::TaskCompleted {
                agent_id: agent_id.clone(),
                task_id: task_id.clone(),
                result: result.clone(),
            })
            .await
            .unwrap();

            completed_tasks.push((agent_id.clone(), task_id, result));

            // Update agent status to available
            tracker
                .update_status(
                    &agent_id,
                    &crate::agent::AgentStatus::Available,
                    serde_json::json!({"last_task": description}),
                )
                .await
                .unwrap();
        }

        // 3. Verify workflow completion
        assert_eq!(completed_tasks.len(), workflow_tasks.len());

        // Check all agents are available
        let all_statuses = tracker.get_all_statuses().await.unwrap();
        assert_eq!(all_statuses.len(), workflow_tasks.len());

        for status in &all_statuses {
            let agent_status: crate::agent::AgentStatus =
                serde_json::from_value(status["status"].clone()).unwrap();
            assert!(matches!(agent_status, crate::agent::AgentStatus::Available));
        }

        // 4. Verify message flow
        let mut received_messages = Vec::new();
        while let Some(message) = bus.try_receive_message() {
            received_messages.push(message);
        }

        // Should have received 2 messages per agent (status update + completion)
        assert_eq!(received_messages.len(), workflow_tasks.len() * 2);
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:15-76 function create_test_config <-> ./crates/ccswarm/src/tests/integration_tests.rs:330-435 function test_multi_agent_workflow_simulation
  Similarity: 85.07%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:create_test_config (lines 15-76) ---[0m
    async fn create_test_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Initialize git repo
        WorktreeManager::init_if_needed(&repo_path).await.unwrap();

        let mut agents = HashMap::new();

        // Frontend agent config
        agents.insert(
            "frontend".to_string(),
            AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend-agent".to_string(),
                branch: "feature/frontend-ui".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        // Backend agent config
        agents.insert(
            "backend".to_string(),
            AgentConfig {
                specialization: "backend".to_string(),
                worktree: "agents/backend-agent".to_string(),
                branch: "feature/backend-api".to_string(),
                claude_config: ClaudeConfig::for_agent("backend"),
                claude_md_template: "backend_specialist".to_string(),
            },
        );

        let config = CcswarmConfig {
            project: ProjectConfig {
                name: "Test Integration Project".to_string(),
                repository: RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };

        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_multi_agent_workflow_simulation (lines 330-435) ---[0m
    async fn test_multi_agent_workflow_simulation() {
        let bus = CoordinationBus::new().await.unwrap();
        let queue = TaskQueue::new().await.unwrap();
        let tracker = StatusTracker::new().await.unwrap();

        // Simulate a multi-agent workflow
        let workflow_tasks = vec![
            ("frontend", "Create user registration form"),
            ("backend", "Implement user registration API"),
            ("qa", "Test user registration flow"),
            ("devops", "Deploy user registration feature"),
        ];

        // 1. Add all tasks to queue
        for (_agent_type, description) in &workflow_tasks {
            let task = Task::new(
                Uuid::new_v4().to_string(),
                description.to_string(),
                Priority::High,
                TaskType::Development,
            );
            queue.add_task(&task).await.unwrap();
        }

        // 2. Simulate agents picking up tasks
        let mut completed_tasks = Vec::new();

        for (agent_type, description) in &workflow_tasks {
            let agent_id = format!("{}-agent", agent_type);

            // Update agent status to working
            tracker
                .update_status(
                    &agent_id,
                    &crate::agent::AgentStatus::Working,
                    serde_json::json!({"current_task": description}),
                )
                .await
                .unwrap();

            // Send status update message
            bus.send_message(AgentMessage::StatusUpdate {
                agent_id: agent_id.clone(),
                status: crate::agent::AgentStatus::Working,
                metrics: serde_json::Value::Object(serde_json::Map::new()),
            })
            .await
            .unwrap();

            // Simulate task completion
            let task_id = Uuid::new_v4().to_string();
            let result = crate::agent::TaskResult {
                success: true,
                output: serde_json::json!({
                    "task": description,
                    "agent": agent_type,
                    "completed_at": chrono::Utc::now(),
                }),
                error: None,
                duration: std::time::Duration::from_secs(300),
            };

            // Send completion message
            bus.send_message(AgentMessage::TaskCompleted {
                agent_id: agent_id.clone(),
                task_id: task_id.clone(),
                result: result.clone(),
            })
            .await
            .unwrap();

            completed_tasks.push((agent_id.clone(), task_id, result));

            // Update agent status to available
            tracker
                .update_status(
                    &agent_id,
                    &crate::agent::AgentStatus::Available,
                    serde_json::json!({"last_task": description}),
                )
                .await
                .unwrap();
        }

        // 3. Verify workflow completion
        assert_eq!(completed_tasks.len(), workflow_tasks.len());

        // Check all agents are available
        let all_statuses = tracker.get_all_statuses().await.unwrap();
        assert_eq!(all_statuses.len(), workflow_tasks.len());

        for status in &all_statuses {
            let agent_status: crate::agent::AgentStatus =
                serde_json::from_value(status["status"].clone()).unwrap();
            assert!(matches!(agent_status, crate::agent::AgentStatus::Available));
        }

        // 4. Verify message flow
        let mut received_messages = Vec::new();
        while let Some(message) = bus.try_receive_message() {
            received_messages.push(message);
        }

        // Should have received 2 messages per agent (status update + completion)
        assert_eq!(received_messages.len(), workflow_tasks.len() * 2);
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:15-76 function create_test_config <-> ./crates/ccswarm/src/tests/integration_tests.rs:108-185 function test_coordination_bus_message_flow
  Similarity: 89.92%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:create_test_config (lines 15-76) ---[0m
    async fn create_test_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Initialize git repo
        WorktreeManager::init_if_needed(&repo_path).await.unwrap();

        let mut agents = HashMap::new();

        // Frontend agent config
        agents.insert(
            "frontend".to_string(),
            AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend-agent".to_string(),
                branch: "feature/frontend-ui".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        // Backend agent config
        agents.insert(
            "backend".to_string(),
            AgentConfig {
                specialization: "backend".to_string(),
                worktree: "agents/backend-agent".to_string(),
                branch: "feature/backend-api".to_string(),
                claude_config: ClaudeConfig::for_agent("backend"),
                claude_md_template: "backend_specialist".to_string(),
            },
        );

        let config = CcswarmConfig {
            project: ProjectConfig {
                name: "Test Integration Project".to_string(),
                repository: RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };

        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_coordination_bus_message_flow (lines 108-185) ---[0m
    async fn test_coordination_bus_message_flow() {
        let bus = CoordinationBus::new().await.unwrap();

        // Test various message types
        let messages = vec![
            AgentMessage::StatusUpdate {
                agent_id: "frontend-agent".to_string(),
                status: crate::agent::AgentStatus::Available,
                metrics: serde_json::Value::Object(serde_json::Map::new()),
            },
            AgentMessage::TaskCompleted {
                agent_id: "backend-agent".to_string(),
                task_id: "task-1".to_string(),
                result: crate::agent::TaskResult {
                    success: true,
                    output: serde_json::json!({"result": "completed"}),
                    error: None,
                    duration: std::time::Duration::from_secs(120),
                },
            },
            AgentMessage::RequestAssistance {
                agent_id: "frontend-agent".to_string(),
                task_id: "task-2".to_string(),
                reason: "Need backend API specification".to_string(),
            },
        ];

        // Send all messages
        for message in &messages {
            bus.send_message(message.clone()).await.unwrap();
        }

        // Receive and verify all messages
        for expected_message in &messages {
            let received = bus.receive_message().await.unwrap();

            match (expected_message, &received) {
                (
                    AgentMessage::StatusUpdate { agent_id: e_id, .. },
                    AgentMessage::StatusUpdate { agent_id: r_id, .. },
                ) => {
                    assert_eq!(e_id, r_id);
                }
                (
                    AgentMessage::TaskCompleted {
                        agent_id: e_id,
                        task_id: e_task,
                        ..
                    },
                    AgentMessage::TaskCompleted {
                        agent_id: r_id,
                        task_id: r_task,
                        ..
                    },
                ) => {
                    assert_eq!(e_id, r_id);
                    assert_eq!(e_task, r_task);
                }
                (
                    AgentMessage::RequestAssistance {
                        agent_id: e_id,
                        task_id: e_task,
                        reason: e_reason,
                    },
                    AgentMessage::RequestAssistance {
                        agent_id: r_id,
                        task_id: r_task,
                        reason: r_reason,
                    },
                ) => {
                    assert_eq!(e_id, r_id);
                    assert_eq!(e_task, r_task);
                    assert_eq!(e_reason, r_reason);
                }
                _ => panic!("Message type mismatch"),
            }
        }
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:108-185 function test_coordination_bus_message_flow <-> ./crates/ccswarm/src/tests/integration_tests.rs:484-543 function test_concurrent_agent_operations
  Similarity: 75.15%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_coordination_bus_message_flow (lines 108-185) ---[0m
    async fn test_coordination_bus_message_flow() {
        let bus = CoordinationBus::new().await.unwrap();

        // Test various message types
        let messages = vec![
            AgentMessage::StatusUpdate {
                agent_id: "frontend-agent".to_string(),
                status: crate::agent::AgentStatus::Available,
                metrics: serde_json::Value::Object(serde_json::Map::new()),
            },
            AgentMessage::TaskCompleted {
                agent_id: "backend-agent".to_string(),
                task_id: "task-1".to_string(),
                result: crate::agent::TaskResult {
                    success: true,
                    output: serde_json::json!({"result": "completed"}),
                    error: None,
                    duration: std::time::Duration::from_secs(120),
                },
            },
            AgentMessage::RequestAssistance {
                agent_id: "frontend-agent".to_string(),
                task_id: "task-2".to_string(),
                reason: "Need backend API specification".to_string(),
            },
        ];

        // Send all messages
        for message in &messages {
            bus.send_message(message.clone()).await.unwrap();
        }

        // Receive and verify all messages
        for expected_message in &messages {
            let received = bus.receive_message().await.unwrap();

            match (expected_message, &received) {
                (
                    AgentMessage::StatusUpdate { agent_id: e_id, .. },
                    AgentMessage::StatusUpdate { agent_id: r_id, .. },
                ) => {
                    assert_eq!(e_id, r_id);
                }
                (
                    AgentMessage::TaskCompleted {
                        agent_id: e_id,
                        task_id: e_task,
                        ..
                    },
                    AgentMessage::TaskCompleted {
                        agent_id: r_id,
                        task_id: r_task,
                        ..
                    },
                ) => {
                    assert_eq!(e_id, r_id);
                    assert_eq!(e_task, r_task);
                }
                (
                    AgentMessage::RequestAssistance {
                        agent_id: e_id,
                        task_id: e_task,
                        reason: e_reason,
                    },
                    AgentMessage::RequestAssistance {
                        agent_id: r_id,
                        task_id: r_task,
                        reason: r_reason,
                    },
                ) => {
                    assert_eq!(e_id, r_id);
                    assert_eq!(e_task, r_task);
                    assert_eq!(e_reason, r_reason);
                }
                _ => panic!("Message type mismatch"),
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_concurrent_agent_operations (lines 484-543) ---[0m
    async fn test_concurrent_agent_operations() {
        use tokio::task;

        let bus = CoordinationBus::new().await.unwrap();
        let queue = TaskQueue::new().await.unwrap();

        // Create multiple tasks
        let tasks: Vec<_> = (0..10)
            .map(|i| {
                Task::new(
                    format!("task-{}", i),
                    format!("Test task {}", i),
                    Priority::Medium,
                    TaskType::Development,
                )
            })
            .collect();

        // Simulate concurrent operations
        let mut handles = Vec::new();

        // Task creation tasks
        for task in tasks {
            let queue_clone = queue.clone();
            let handle = task::spawn(async move {
                queue_clone.add_task(&task).await.unwrap();
            });
            handles.push(handle);
        }

        // Message sending tasks
        for i in 0..5 {
            let bus_clone = bus.clone();
            let agent_id = format!("agent-{}", i);
            let handle = task::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id,
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all operations to complete
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify results
        let pending_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(pending_tasks.len(), 10);

        // Verify at least some messages were received
        let mut message_count = 0;
        while bus.try_receive_message().is_some() {
            message_count += 1;
        }
        assert!(message_count > 0);
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:15-76 function create_test_config <-> ./crates/ccswarm/src/tests/integration_tests.rs:484-543 function test_concurrent_agent_operations
  Similarity: 83.40%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:create_test_config (lines 15-76) ---[0m
    async fn create_test_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Initialize git repo
        WorktreeManager::init_if_needed(&repo_path).await.unwrap();

        let mut agents = HashMap::new();

        // Frontend agent config
        agents.insert(
            "frontend".to_string(),
            AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend-agent".to_string(),
                branch: "feature/frontend-ui".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        // Backend agent config
        agents.insert(
            "backend".to_string(),
            AgentConfig {
                specialization: "backend".to_string(),
                worktree: "agents/backend-agent".to_string(),
                branch: "feature/backend-api".to_string(),
                claude_config: ClaudeConfig::for_agent("backend"),
                claude_md_template: "backend_specialist".to_string(),
            },
        );

        let config = CcswarmConfig {
            project: ProjectConfig {
                name: "Test Integration Project".to_string(),
                repository: RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };

        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_concurrent_agent_operations (lines 484-543) ---[0m
    async fn test_concurrent_agent_operations() {
        use tokio::task;

        let bus = CoordinationBus::new().await.unwrap();
        let queue = TaskQueue::new().await.unwrap();

        // Create multiple tasks
        let tasks: Vec<_> = (0..10)
            .map(|i| {
                Task::new(
                    format!("task-{}", i),
                    format!("Test task {}", i),
                    Priority::Medium,
                    TaskType::Development,
                )
            })
            .collect();

        // Simulate concurrent operations
        let mut handles = Vec::new();

        // Task creation tasks
        for task in tasks {
            let queue_clone = queue.clone();
            let handle = task::spawn(async move {
                queue_clone.add_task(&task).await.unwrap();
            });
            handles.push(handle);
        }

        // Message sending tasks
        for i in 0..5 {
            let bus_clone = bus.clone();
            let agent_id = format!("agent-{}", i);
            let handle = task::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id,
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all operations to complete
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify results
        let pending_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(pending_tasks.len(), 10);

        // Verify at least some messages were received
        let mut message_count = 0;
        while bus.try_receive_message().is_some() {
            message_count += 1;
        }
        assert!(message_count > 0);
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:108-185 function test_coordination_bus_message_flow <-> ./crates/ccswarm/src/tests/integration_tests.rs:266-295 function test_task_assignment_logic
  Similarity: 93.21%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_coordination_bus_message_flow (lines 108-185) ---[0m
    async fn test_coordination_bus_message_flow() {
        let bus = CoordinationBus::new().await.unwrap();

        // Test various message types
        let messages = vec![
            AgentMessage::StatusUpdate {
                agent_id: "frontend-agent".to_string(),
                status: crate::agent::AgentStatus::Available,
                metrics: serde_json::Value::Object(serde_json::Map::new()),
            },
            AgentMessage::TaskCompleted {
                agent_id: "backend-agent".to_string(),
                task_id: "task-1".to_string(),
                result: crate::agent::TaskResult {
                    success: true,
                    output: serde_json::json!({"result": "completed"}),
                    error: None,
                    duration: std::time::Duration::from_secs(120),
                },
            },
            AgentMessage::RequestAssistance {
                agent_id: "frontend-agent".to_string(),
                task_id: "task-2".to_string(),
                reason: "Need backend API specification".to_string(),
            },
        ];

        // Send all messages
        for message in &messages {
            bus.send_message(message.clone()).await.unwrap();
        }

        // Receive and verify all messages
        for expected_message in &messages {
            let received = bus.receive_message().await.unwrap();

            match (expected_message, &received) {
                (
                    AgentMessage::StatusUpdate { agent_id: e_id, .. },
                    AgentMessage::StatusUpdate { agent_id: r_id, .. },
                ) => {
                    assert_eq!(e_id, r_id);
                }
                (
                    AgentMessage::TaskCompleted {
                        agent_id: e_id,
                        task_id: e_task,
                        ..
                    },
                    AgentMessage::TaskCompleted {
                        agent_id: r_id,
                        task_id: r_task,
                        ..
                    },
                ) => {
                    assert_eq!(e_id, r_id);
                    assert_eq!(e_task, r_task);
                }
                (
                    AgentMessage::RequestAssistance {
                        agent_id: e_id,
                        task_id: e_task,
                        reason: e_reason,
                    },
                    AgentMessage::RequestAssistance {
                        agent_id: r_id,
                        task_id: r_task,
                        reason: r_reason,
                    },
                ) => {
                    assert_eq!(e_id, r_id);
                    assert_eq!(e_task, r_task);
                    assert_eq!(e_reason, r_reason);
                }
                _ => panic!("Message type mismatch"),
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_task_assignment_logic (lines 266-295) ---[0m
    async fn test_task_assignment_logic() {
        let (config, _temp_dir) = create_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let _master = MasterClaude::new(config, repo_path).await.unwrap();
        let tasks = create_test_tasks();

        // Test task categorization
        for task in &tasks {
            // This would normally call select_optimal_agent, but we test the logic
            let expected_agent = match task.description.as_str() {
                desc if desc.contains("React") || desc.contains("component") => "Frontend",
                desc if desc.contains("API") || desc.contains("authentication") => "Backend",
                desc if desc.contains("CI/CD") || desc.contains("deployment") => "DevOps",
                desc if desc.contains("test") => "QA",
                _ => "Backend", // Default
            };

            // Verify our logic matches expectations
            match task.description.as_str() {
                "Create user login component with React" => {
                    assert_eq!(expected_agent, "Frontend")
                }
                "Implement authentication API endpoint" => assert_eq!(expected_agent, "Backend"),
                "Set up CI/CD pipeline for deployment" => assert_eq!(expected_agent, "DevOps"),
                "Write integration tests for user flow" => assert_eq!(expected_agent, "QA"),
                _ => {}
            }
        }
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:108-185 function test_coordination_bus_message_flow <-> ./crates/ccswarm/src/tests/integration_tests.rs:299-326 function test_coordination_message_persistence
  Similarity: 92.72%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_coordination_bus_message_flow (lines 108-185) ---[0m
    async fn test_coordination_bus_message_flow() {
        let bus = CoordinationBus::new().await.unwrap();

        // Test various message types
        let messages = vec![
            AgentMessage::StatusUpdate {
                agent_id: "frontend-agent".to_string(),
                status: crate::agent::AgentStatus::Available,
                metrics: serde_json::Value::Object(serde_json::Map::new()),
            },
            AgentMessage::TaskCompleted {
                agent_id: "backend-agent".to_string(),
                task_id: "task-1".to_string(),
                result: crate::agent::TaskResult {
                    success: true,
                    output: serde_json::json!({"result": "completed"}),
                    error: None,
                    duration: std::time::Duration::from_secs(120),
                },
            },
            AgentMessage::RequestAssistance {
                agent_id: "frontend-agent".to_string(),
                task_id: "task-2".to_string(),
                reason: "Need backend API specification".to_string(),
            },
        ];

        // Send all messages
        for message in &messages {
            bus.send_message(message.clone()).await.unwrap();
        }

        // Receive and verify all messages
        for expected_message in &messages {
            let received = bus.receive_message().await.unwrap();

            match (expected_message, &received) {
                (
                    AgentMessage::StatusUpdate { agent_id: e_id, .. },
                    AgentMessage::StatusUpdate { agent_id: r_id, .. },
                ) => {
                    assert_eq!(e_id, r_id);
                }
                (
                    AgentMessage::TaskCompleted {
                        agent_id: e_id,
                        task_id: e_task,
                        ..
                    },
                    AgentMessage::TaskCompleted {
                        agent_id: r_id,
                        task_id: r_task,
                        ..
                    },
                ) => {
                    assert_eq!(e_id, r_id);
                    assert_eq!(e_task, r_task);
                }
                (
                    AgentMessage::RequestAssistance {
                        agent_id: e_id,
                        task_id: e_task,
                        reason: e_reason,
                    },
                    AgentMessage::RequestAssistance {
                        agent_id: r_id,
                        task_id: r_task,
                        reason: r_reason,
                    },
                ) => {
                    assert_eq!(e_id, r_id);
                    assert_eq!(e_task, r_task);
                    assert_eq!(e_reason, r_reason);
                }
                _ => panic!("Message type mismatch"),
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_coordination_message_persistence (lines 299-326) ---[0m
    async fn test_coordination_message_persistence() {
        let bus = CoordinationBus::new().await.unwrap();

        let test_message = AgentMessage::TaskCompleted {
            agent_id: "test-agent".to_string(),
            task_id: "test-task".to_string(),
            result: crate::agent::TaskResult {
                success: true,
                output: serde_json::json!({"test": "data"}),
                error: None,
                duration: std::time::Duration::from_secs(30),
            },
        };

        // Send message (should persist automatically)
        bus.send_message(test_message.clone()).await.unwrap();

        // Load persisted messages
        let persisted = bus.load_persisted_messages().await.unwrap();
        assert!(!persisted.is_empty());

        // Find our message
        let found = persisted.iter().find(
            |m| matches!(m, AgentMessage::TaskCompleted { task_id, .. } if task_id == "test-task"),
        );

        assert!(found.is_some());
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:108-185 function test_coordination_bus_message_flow <-> ./crates/ccswarm/src/tests/integration_tests.rs:438-480 function test_error_handling_and_recovery
  Similarity: 78.19%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_coordination_bus_message_flow (lines 108-185) ---[0m
    async fn test_coordination_bus_message_flow() {
        let bus = CoordinationBus::new().await.unwrap();

        // Test various message types
        let messages = vec![
            AgentMessage::StatusUpdate {
                agent_id: "frontend-agent".to_string(),
                status: crate::agent::AgentStatus::Available,
                metrics: serde_json::Value::Object(serde_json::Map::new()),
            },
            AgentMessage::TaskCompleted {
                agent_id: "backend-agent".to_string(),
                task_id: "task-1".to_string(),
                result: crate::agent::TaskResult {
                    success: true,
                    output: serde_json::json!({"result": "completed"}),
                    error: None,
                    duration: std::time::Duration::from_secs(120),
                },
            },
            AgentMessage::RequestAssistance {
                agent_id: "frontend-agent".to_string(),
                task_id: "task-2".to_string(),
                reason: "Need backend API specification".to_string(),
            },
        ];

        // Send all messages
        for message in &messages {
            bus.send_message(message.clone()).await.unwrap();
        }

        // Receive and verify all messages
        for expected_message in &messages {
            let received = bus.receive_message().await.unwrap();

            match (expected_message, &received) {
                (
                    AgentMessage::StatusUpdate { agent_id: e_id, .. },
                    AgentMessage::StatusUpdate { agent_id: r_id, .. },
                ) => {
                    assert_eq!(e_id, r_id);
                }
                (
                    AgentMessage::TaskCompleted {
                        agent_id: e_id,
                        task_id: e_task,
                        ..
                    },
                    AgentMessage::TaskCompleted {
                        agent_id: r_id,
                        task_id: r_task,
                        ..
                    },
                ) => {
                    assert_eq!(e_id, r_id);
                    assert_eq!(e_task, r_task);
                }
                (
                    AgentMessage::RequestAssistance {
                        agent_id: e_id,
                        task_id: e_task,
                        reason: e_reason,
                    },
                    AgentMessage::RequestAssistance {
                        agent_id: r_id,
                        task_id: r_task,
                        reason: r_reason,
                    },
                ) => {
                    assert_eq!(e_id, r_id);
                    assert_eq!(e_task, r_task);
                    assert_eq!(e_reason, r_reason);
                }
                _ => panic!("Message type mismatch"),
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_error_handling_and_recovery (lines 438-480) ---[0m
    async fn test_error_handling_and_recovery() {
        let bus = CoordinationBus::new().await.unwrap();
        let tracker = StatusTracker::new().await.unwrap();

        let agent_id = "error-prone-agent";

        // 1. Agent encounters an error
        let error_status = crate::agent::AgentStatus::Error("Connection timeout".to_string());
        tracker
            .update_status(
                agent_id,
                &error_status,
                serde_json::json!({"error_details": "Failed to connect to service"}),
            )
            .await
            .unwrap();

        // 2. Send error notification
        bus.send_message(AgentMessage::RequestAssistance {
            agent_id: agent_id.to_string(),
            task_id: "failed-task".to_string(),
            reason: "Connection timeout during task execution".to_string(),
        })
        .await
        .unwrap();

        // 3. Simulate recovery
        let recovered_status = crate::agent::AgentStatus::Available;
        tracker
            .update_status(
                agent_id,
                &recovered_status,
                serde_json::json!({"recovery_time": chrono::Utc::now()}),
            )
            .await
            .unwrap();

        // 4. Verify recovery
        let final_status = tracker.get_status(agent_id).await.unwrap().unwrap();
        let agent_status: crate::agent::AgentStatus =
            serde_json::from_value(final_status["status"].clone()).unwrap();
        assert!(matches!(agent_status, crate::agent::AgentStatus::Available));
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:15-76 function create_test_config <-> ./crates/ccswarm/src/tests/integration_tests.rs:438-480 function test_error_handling_and_recovery
  Similarity: 89.44%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:create_test_config (lines 15-76) ---[0m
    async fn create_test_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Initialize git repo
        WorktreeManager::init_if_needed(&repo_path).await.unwrap();

        let mut agents = HashMap::new();

        // Frontend agent config
        agents.insert(
            "frontend".to_string(),
            AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend-agent".to_string(),
                branch: "feature/frontend-ui".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        // Backend agent config
        agents.insert(
            "backend".to_string(),
            AgentConfig {
                specialization: "backend".to_string(),
                worktree: "agents/backend-agent".to_string(),
                branch: "feature/backend-api".to_string(),
                claude_config: ClaudeConfig::for_agent("backend"),
                claude_md_template: "backend_specialist".to_string(),
            },
        );

        let config = CcswarmConfig {
            project: ProjectConfig {
                name: "Test Integration Project".to_string(),
                repository: RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };

        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_error_handling_and_recovery (lines 438-480) ---[0m
    async fn test_error_handling_and_recovery() {
        let bus = CoordinationBus::new().await.unwrap();
        let tracker = StatusTracker::new().await.unwrap();

        let agent_id = "error-prone-agent";

        // 1. Agent encounters an error
        let error_status = crate::agent::AgentStatus::Error("Connection timeout".to_string());
        tracker
            .update_status(
                agent_id,
                &error_status,
                serde_json::json!({"error_details": "Failed to connect to service"}),
            )
            .await
            .unwrap();

        // 2. Send error notification
        bus.send_message(AgentMessage::RequestAssistance {
            agent_id: agent_id.to_string(),
            task_id: "failed-task".to_string(),
            reason: "Connection timeout during task execution".to_string(),
        })
        .await
        .unwrap();

        // 3. Simulate recovery
        let recovered_status = crate::agent::AgentStatus::Available;
        tracker
            .update_status(
                agent_id,
                &recovered_status,
                serde_json::json!({"recovery_time": chrono::Utc::now()}),
            )
            .await
            .unwrap();

        // 4. Verify recovery
        let final_status = tracker.get_status(agent_id).await.unwrap().unwrap();
        let agent_status: crate::agent::AgentStatus =
            serde_json::from_value(final_status["status"].clone()).unwrap();
        assert!(matches!(agent_status, crate::agent::AgentStatus::Available));
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:438-480 function test_error_handling_and_recovery <-> ./crates/ccswarm/src/tests/integration_tests.rs:484-543 function test_concurrent_agent_operations
  Similarity: 85.91%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_error_handling_and_recovery (lines 438-480) ---[0m
    async fn test_error_handling_and_recovery() {
        let bus = CoordinationBus::new().await.unwrap();
        let tracker = StatusTracker::new().await.unwrap();

        let agent_id = "error-prone-agent";

        // 1. Agent encounters an error
        let error_status = crate::agent::AgentStatus::Error("Connection timeout".to_string());
        tracker
            .update_status(
                agent_id,
                &error_status,
                serde_json::json!({"error_details": "Failed to connect to service"}),
            )
            .await
            .unwrap();

        // 2. Send error notification
        bus.send_message(AgentMessage::RequestAssistance {
            agent_id: agent_id.to_string(),
            task_id: "failed-task".to_string(),
            reason: "Connection timeout during task execution".to_string(),
        })
        .await
        .unwrap();

        // 3. Simulate recovery
        let recovered_status = crate::agent::AgentStatus::Available;
        tracker
            .update_status(
                agent_id,
                &recovered_status,
                serde_json::json!({"recovery_time": chrono::Utc::now()}),
            )
            .await
            .unwrap();

        // 4. Verify recovery
        let final_status = tracker.get_status(agent_id).await.unwrap().unwrap();
        let agent_status: crate::agent::AgentStatus =
            serde_json::from_value(final_status["status"].clone()).unwrap();
        assert!(matches!(agent_status, crate::agent::AgentStatus::Available));
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_concurrent_agent_operations (lines 484-543) ---[0m
    async fn test_concurrent_agent_operations() {
        use tokio::task;

        let bus = CoordinationBus::new().await.unwrap();
        let queue = TaskQueue::new().await.unwrap();

        // Create multiple tasks
        let tasks: Vec<_> = (0..10)
            .map(|i| {
                Task::new(
                    format!("task-{}", i),
                    format!("Test task {}", i),
                    Priority::Medium,
                    TaskType::Development,
                )
            })
            .collect();

        // Simulate concurrent operations
        let mut handles = Vec::new();

        // Task creation tasks
        for task in tasks {
            let queue_clone = queue.clone();
            let handle = task::spawn(async move {
                queue_clone.add_task(&task).await.unwrap();
            });
            handles.push(handle);
        }

        // Message sending tasks
        for i in 0..5 {
            let bus_clone = bus.clone();
            let agent_id = format!("agent-{}", i);
            let handle = task::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id,
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all operations to complete
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify results
        let pending_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(pending_tasks.len(), 10);

        // Verify at least some messages were received
        let mut message_count = 0;
        while bus.try_receive_message().is_some() {
            message_count += 1;
        }
        assert!(message_count > 0);
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:15-76 function create_test_config <-> ./crates/ccswarm/src/tests/integration_tests.rs:299-326 function test_coordination_message_persistence
  Similarity: 93.37%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:create_test_config (lines 15-76) ---[0m
    async fn create_test_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Initialize git repo
        WorktreeManager::init_if_needed(&repo_path).await.unwrap();

        let mut agents = HashMap::new();

        // Frontend agent config
        agents.insert(
            "frontend".to_string(),
            AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend-agent".to_string(),
                branch: "feature/frontend-ui".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        // Backend agent config
        agents.insert(
            "backend".to_string(),
            AgentConfig {
                specialization: "backend".to_string(),
                worktree: "agents/backend-agent".to_string(),
                branch: "feature/backend-api".to_string(),
                claude_config: ClaudeConfig::for_agent("backend"),
                claude_md_template: "backend_specialist".to_string(),
            },
        );

        let config = CcswarmConfig {
            project: ProjectConfig {
                name: "Test Integration Project".to_string(),
                repository: RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };

        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_coordination_message_persistence (lines 299-326) ---[0m
    async fn test_coordination_message_persistence() {
        let bus = CoordinationBus::new().await.unwrap();

        let test_message = AgentMessage::TaskCompleted {
            agent_id: "test-agent".to_string(),
            task_id: "test-task".to_string(),
            result: crate::agent::TaskResult {
                success: true,
                output: serde_json::json!({"test": "data"}),
                error: None,
                duration: std::time::Duration::from_secs(30),
            },
        };

        // Send message (should persist automatically)
        bus.send_message(test_message.clone()).await.unwrap();

        // Load persisted messages
        let persisted = bus.load_persisted_messages().await.unwrap();
        assert!(!persisted.is_empty());

        // Find our message
        let found = persisted.iter().find(
            |m| matches!(m, AgentMessage::TaskCompleted { task_id, .. } if task_id == "test-task"),
        );

        assert!(found.is_some());
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:108-185 function test_coordination_bus_message_flow <-> ./crates/ccswarm/src/tests/integration_tests.rs:221-249 function test_status_tracker
  Similarity: 76.11%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_coordination_bus_message_flow (lines 108-185) ---[0m
    async fn test_coordination_bus_message_flow() {
        let bus = CoordinationBus::new().await.unwrap();

        // Test various message types
        let messages = vec![
            AgentMessage::StatusUpdate {
                agent_id: "frontend-agent".to_string(),
                status: crate::agent::AgentStatus::Available,
                metrics: serde_json::Value::Object(serde_json::Map::new()),
            },
            AgentMessage::TaskCompleted {
                agent_id: "backend-agent".to_string(),
                task_id: "task-1".to_string(),
                result: crate::agent::TaskResult {
                    success: true,
                    output: serde_json::json!({"result": "completed"}),
                    error: None,
                    duration: std::time::Duration::from_secs(120),
                },
            },
            AgentMessage::RequestAssistance {
                agent_id: "frontend-agent".to_string(),
                task_id: "task-2".to_string(),
                reason: "Need backend API specification".to_string(),
            },
        ];

        // Send all messages
        for message in &messages {
            bus.send_message(message.clone()).await.unwrap();
        }

        // Receive and verify all messages
        for expected_message in &messages {
            let received = bus.receive_message().await.unwrap();

            match (expected_message, &received) {
                (
                    AgentMessage::StatusUpdate { agent_id: e_id, .. },
                    AgentMessage::StatusUpdate { agent_id: r_id, .. },
                ) => {
                    assert_eq!(e_id, r_id);
                }
                (
                    AgentMessage::TaskCompleted {
                        agent_id: e_id,
                        task_id: e_task,
                        ..
                    },
                    AgentMessage::TaskCompleted {
                        agent_id: r_id,
                        task_id: r_task,
                        ..
                    },
                ) => {
                    assert_eq!(e_id, r_id);
                    assert_eq!(e_task, r_task);
                }
                (
                    AgentMessage::RequestAssistance {
                        agent_id: e_id,
                        task_id: e_task,
                        reason: e_reason,
                    },
                    AgentMessage::RequestAssistance {
                        agent_id: r_id,
                        task_id: r_task,
                        reason: r_reason,
                    },
                ) => {
                    assert_eq!(e_id, r_id);
                    assert_eq!(e_task, r_task);
                    assert_eq!(e_reason, r_reason);
                }
                _ => panic!("Message type mismatch"),
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_status_tracker (lines 221-249) ---[0m
    async fn test_status_tracker() {
        let tracker = StatusTracker::new().await.unwrap();

        let agent_id = "test-agent";
        let status = crate::agent::AgentStatus::Working;
        let additional_info = serde_json::json!({
            "current_task": "creating component",
            "progress": 0.75
        });

        // Update status
        tracker
            .update_status(agent_id, &status, additional_info.clone())
            .await
            .unwrap();

        // Retrieve status
        let retrieved = tracker.get_status(agent_id).await.unwrap();
        assert!(retrieved.is_some());

        let status_info = retrieved.unwrap();
        assert_eq!(status_info["agent_id"], agent_id);
        assert_eq!(status_info["additional_info"], additional_info);

        // Get all statuses
        let all_statuses = tracker.get_all_statuses().await.unwrap();
        assert_eq!(all_statuses.len(), 1);
        assert_eq!(all_statuses[0]["agent_id"], agent_id);
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:15-76 function create_test_config <-> ./crates/ccswarm/src/tests/integration_tests.rs:266-295 function test_task_assignment_logic
  Similarity: 87.47%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:create_test_config (lines 15-76) ---[0m
    async fn create_test_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Initialize git repo
        WorktreeManager::init_if_needed(&repo_path).await.unwrap();

        let mut agents = HashMap::new();

        // Frontend agent config
        agents.insert(
            "frontend".to_string(),
            AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend-agent".to_string(),
                branch: "feature/frontend-ui".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        // Backend agent config
        agents.insert(
            "backend".to_string(),
            AgentConfig {
                specialization: "backend".to_string(),
                worktree: "agents/backend-agent".to_string(),
                branch: "feature/backend-api".to_string(),
                claude_config: ClaudeConfig::for_agent("backend"),
                claude_md_template: "backend_specialist".to_string(),
            },
        );

        let config = CcswarmConfig {
            project: ProjectConfig {
                name: "Test Integration Project".to_string(),
                repository: RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };

        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_task_assignment_logic (lines 266-295) ---[0m
    async fn test_task_assignment_logic() {
        let (config, _temp_dir) = create_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let _master = MasterClaude::new(config, repo_path).await.unwrap();
        let tasks = create_test_tasks();

        // Test task categorization
        for task in &tasks {
            // This would normally call select_optimal_agent, but we test the logic
            let expected_agent = match task.description.as_str() {
                desc if desc.contains("React") || desc.contains("component") => "Frontend",
                desc if desc.contains("API") || desc.contains("authentication") => "Backend",
                desc if desc.contains("CI/CD") || desc.contains("deployment") => "DevOps",
                desc if desc.contains("test") => "QA",
                _ => "Backend", // Default
            };

            // Verify our logic matches expectations
            match task.description.as_str() {
                "Create user login component with React" => {
                    assert_eq!(expected_agent, "Frontend")
                }
                "Implement authentication API endpoint" => assert_eq!(expected_agent, "Backend"),
                "Set up CI/CD pipeline for deployment" => assert_eq!(expected_agent, "DevOps"),
                "Write integration tests for user flow" => assert_eq!(expected_agent, "QA"),
                _ => {}
            }
        }
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:15-76 function create_test_config <-> ./crates/ccswarm/src/tests/integration_tests.rs:188-217 function test_task_queue_operations
  Similarity: 85.69%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:create_test_config (lines 15-76) ---[0m
    async fn create_test_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Initialize git repo
        WorktreeManager::init_if_needed(&repo_path).await.unwrap();

        let mut agents = HashMap::new();

        // Frontend agent config
        agents.insert(
            "frontend".to_string(),
            AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend-agent".to_string(),
                branch: "feature/frontend-ui".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        // Backend agent config
        agents.insert(
            "backend".to_string(),
            AgentConfig {
                specialization: "backend".to_string(),
                worktree: "agents/backend-agent".to_string(),
                branch: "feature/backend-api".to_string(),
                claude_config: ClaudeConfig::for_agent("backend"),
                claude_md_template: "backend_specialist".to_string(),
            },
        );

        let config = CcswarmConfig {
            project: ProjectConfig {
                name: "Test Integration Project".to_string(),
                repository: RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };

        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_task_queue_operations (lines 188-217) ---[0m
    async fn test_task_queue_operations() {
        use tempfile::TempDir;
        let temp_dir = TempDir::new().unwrap();
        let queue_dir = temp_dir.path().join("task-queue");
        let queue = TaskQueue::with_dir(queue_dir.to_str().unwrap())
            .await
            .unwrap();
        let tasks = create_test_tasks();

        // Add all tasks
        for task in &tasks {
            queue.add_task(task).await.unwrap();
        }

        // Retrieve tasks
        let pending_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(pending_tasks.len(), tasks.len());

        // Verify tasks are sorted by priority
        for i in 0..pending_tasks.len() - 1 {
            assert!(pending_tasks[i].priority >= pending_tasks[i + 1].priority);
        }

        // Remove a task
        queue.remove_task(&tasks[0].id).await.unwrap();

        let remaining_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(remaining_tasks.len(), tasks.len() - 1);
        assert!(!remaining_tasks.iter().any(|t| t.id == tasks[0].id));
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:15-76 function create_test_config <-> ./crates/ccswarm/src/tests/integration_tests.rs:221-249 function test_status_tracker
  Similarity: 85.80%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:create_test_config (lines 15-76) ---[0m
    async fn create_test_config() -> (CcswarmConfig, TempDir) {
        let temp_dir = TempDir::new().unwrap();
        let repo_path = temp_dir.path().to_path_buf();

        // Initialize git repo
        WorktreeManager::init_if_needed(&repo_path).await.unwrap();

        let mut agents = HashMap::new();

        // Frontend agent config
        agents.insert(
            "frontend".to_string(),
            AgentConfig {
                specialization: "frontend".to_string(),
                worktree: "agents/frontend-agent".to_string(),
                branch: "feature/frontend-ui".to_string(),
                claude_config: ClaudeConfig::for_agent("frontend"),
                claude_md_template: "frontend_specialist".to_string(),
            },
        );

        // Backend agent config
        agents.insert(
            "backend".to_string(),
            AgentConfig {
                specialization: "backend".to_string(),
                worktree: "agents/backend-agent".to_string(),
                branch: "feature/backend-api".to_string(),
                claude_config: ClaudeConfig::for_agent("backend"),
                claude_md_template: "backend_specialist".to_string(),
            },
        );

        let config = CcswarmConfig {
            project: ProjectConfig {
                name: "Test Integration Project".to_string(),
                repository: RepositoryConfig {
                    url: repo_path.to_string_lossy().to_string(),
                    main_branch: "main".to_string(),
                },
                master_claude: MasterClaudeConfig {
                    role: "technical_lead".to_string(),
                    quality_threshold: 0.85,
                    think_mode: ThinkMode::UltraThink,
                    permission_level: "supervised".to_string(),
                    claude_config: ClaudeConfig::for_master(),
                    enable_proactive_mode: true,
                    proactive_frequency: 300,
                    high_frequency: 60,
                },
            },
            agents,
            coordination: CoordinationConfig {
                communication_method: "json_files".to_string(),
                sync_interval: 30,
                quality_gate_frequency: "on_commit".to_string(),
                master_review_trigger: "all_tasks_complete".to_string(),
            },
        };

        (config, temp_dir)
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_status_tracker (lines 221-249) ---[0m
    async fn test_status_tracker() {
        let tracker = StatusTracker::new().await.unwrap();

        let agent_id = "test-agent";
        let status = crate::agent::AgentStatus::Working;
        let additional_info = serde_json::json!({
            "current_task": "creating component",
            "progress": 0.75
        });

        // Update status
        tracker
            .update_status(agent_id, &status, additional_info.clone())
            .await
            .unwrap();

        // Retrieve status
        let retrieved = tracker.get_status(agent_id).await.unwrap();
        assert!(retrieved.is_some());

        let status_info = retrieved.unwrap();
        assert_eq!(status_info["agent_id"], agent_id);
        assert_eq!(status_info["additional_info"], additional_info);

        // Get all statuses
        let all_statuses = tracker.get_all_statuses().await.unwrap();
        assert_eq!(all_statuses.len(), 1);
        assert_eq!(all_statuses[0]["agent_id"], agent_id);
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:108-185 function test_coordination_bus_message_flow <-> ./crates/ccswarm/src/tests/integration_tests.rs:188-217 function test_task_queue_operations
  Similarity: 71.85%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_coordination_bus_message_flow (lines 108-185) ---[0m
    async fn test_coordination_bus_message_flow() {
        let bus = CoordinationBus::new().await.unwrap();

        // Test various message types
        let messages = vec![
            AgentMessage::StatusUpdate {
                agent_id: "frontend-agent".to_string(),
                status: crate::agent::AgentStatus::Available,
                metrics: serde_json::Value::Object(serde_json::Map::new()),
            },
            AgentMessage::TaskCompleted {
                agent_id: "backend-agent".to_string(),
                task_id: "task-1".to_string(),
                result: crate::agent::TaskResult {
                    success: true,
                    output: serde_json::json!({"result": "completed"}),
                    error: None,
                    duration: std::time::Duration::from_secs(120),
                },
            },
            AgentMessage::RequestAssistance {
                agent_id: "frontend-agent".to_string(),
                task_id: "task-2".to_string(),
                reason: "Need backend API specification".to_string(),
            },
        ];

        // Send all messages
        for message in &messages {
            bus.send_message(message.clone()).await.unwrap();
        }

        // Receive and verify all messages
        for expected_message in &messages {
            let received = bus.receive_message().await.unwrap();

            match (expected_message, &received) {
                (
                    AgentMessage::StatusUpdate { agent_id: e_id, .. },
                    AgentMessage::StatusUpdate { agent_id: r_id, .. },
                ) => {
                    assert_eq!(e_id, r_id);
                }
                (
                    AgentMessage::TaskCompleted {
                        agent_id: e_id,
                        task_id: e_task,
                        ..
                    },
                    AgentMessage::TaskCompleted {
                        agent_id: r_id,
                        task_id: r_task,
                        ..
                    },
                ) => {
                    assert_eq!(e_id, r_id);
                    assert_eq!(e_task, r_task);
                }
                (
                    AgentMessage::RequestAssistance {
                        agent_id: e_id,
                        task_id: e_task,
                        reason: e_reason,
                    },
                    AgentMessage::RequestAssistance {
                        agent_id: r_id,
                        task_id: r_task,
                        reason: r_reason,
                    },
                ) => {
                    assert_eq!(e_id, r_id);
                    assert_eq!(e_task, r_task);
                    assert_eq!(e_reason, r_reason);
                }
                _ => panic!("Message type mismatch"),
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_task_queue_operations (lines 188-217) ---[0m
    async fn test_task_queue_operations() {
        use tempfile::TempDir;
        let temp_dir = TempDir::new().unwrap();
        let queue_dir = temp_dir.path().join("task-queue");
        let queue = TaskQueue::with_dir(queue_dir.to_str().unwrap())
            .await
            .unwrap();
        let tasks = create_test_tasks();

        // Add all tasks
        for task in &tasks {
            queue.add_task(task).await.unwrap();
        }

        // Retrieve tasks
        let pending_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(pending_tasks.len(), tasks.len());

        // Verify tasks are sorted by priority
        for i in 0..pending_tasks.len() - 1 {
            assert!(pending_tasks[i].priority >= pending_tasks[i + 1].priority);
        }

        // Remove a task
        queue.remove_task(&tasks[0].id).await.unwrap();

        let remaining_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(remaining_tasks.len(), tasks.len() - 1);
        assert!(!remaining_tasks.iter().any(|t| t.id == tasks[0].id));
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:188-217 function test_task_queue_operations <-> ./crates/ccswarm/src/tests/integration_tests.rs:484-543 function test_concurrent_agent_operations
  Similarity: 85.15%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_task_queue_operations (lines 188-217) ---[0m
    async fn test_task_queue_operations() {
        use tempfile::TempDir;
        let temp_dir = TempDir::new().unwrap();
        let queue_dir = temp_dir.path().join("task-queue");
        let queue = TaskQueue::with_dir(queue_dir.to_str().unwrap())
            .await
            .unwrap();
        let tasks = create_test_tasks();

        // Add all tasks
        for task in &tasks {
            queue.add_task(task).await.unwrap();
        }

        // Retrieve tasks
        let pending_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(pending_tasks.len(), tasks.len());

        // Verify tasks are sorted by priority
        for i in 0..pending_tasks.len() - 1 {
            assert!(pending_tasks[i].priority >= pending_tasks[i + 1].priority);
        }

        // Remove a task
        queue.remove_task(&tasks[0].id).await.unwrap();

        let remaining_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(remaining_tasks.len(), tasks.len() - 1);
        assert!(!remaining_tasks.iter().any(|t| t.id == tasks[0].id));
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_concurrent_agent_operations (lines 484-543) ---[0m
    async fn test_concurrent_agent_operations() {
        use tokio::task;

        let bus = CoordinationBus::new().await.unwrap();
        let queue = TaskQueue::new().await.unwrap();

        // Create multiple tasks
        let tasks: Vec<_> = (0..10)
            .map(|i| {
                Task::new(
                    format!("task-{}", i),
                    format!("Test task {}", i),
                    Priority::Medium,
                    TaskType::Development,
                )
            })
            .collect();

        // Simulate concurrent operations
        let mut handles = Vec::new();

        // Task creation tasks
        for task in tasks {
            let queue_clone = queue.clone();
            let handle = task::spawn(async move {
                queue_clone.add_task(&task).await.unwrap();
            });
            handles.push(handle);
        }

        // Message sending tasks
        for i in 0..5 {
            let bus_clone = bus.clone();
            let agent_id = format!("agent-{}", i);
            let handle = task::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id,
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all operations to complete
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify results
        let pending_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(pending_tasks.len(), 10);

        // Verify at least some messages were received
        let mut message_count = 0;
        while bus.try_receive_message().is_some() {
            message_count += 1;
        }
        assert!(message_count > 0);
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:221-249 function test_status_tracker <-> ./crates/ccswarm/src/tests/integration_tests.rs:484-543 function test_concurrent_agent_operations
  Similarity: 84.96%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_status_tracker (lines 221-249) ---[0m
    async fn test_status_tracker() {
        let tracker = StatusTracker::new().await.unwrap();

        let agent_id = "test-agent";
        let status = crate::agent::AgentStatus::Working;
        let additional_info = serde_json::json!({
            "current_task": "creating component",
            "progress": 0.75
        });

        // Update status
        tracker
            .update_status(agent_id, &status, additional_info.clone())
            .await
            .unwrap();

        // Retrieve status
        let retrieved = tracker.get_status(agent_id).await.unwrap();
        assert!(retrieved.is_some());

        let status_info = retrieved.unwrap();
        assert_eq!(status_info["agent_id"], agent_id);
        assert_eq!(status_info["additional_info"], additional_info);

        // Get all statuses
        let all_statuses = tracker.get_all_statuses().await.unwrap();
        assert_eq!(all_statuses.len(), 1);
        assert_eq!(all_statuses[0]["agent_id"], agent_id);
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_concurrent_agent_operations (lines 484-543) ---[0m
    async fn test_concurrent_agent_operations() {
        use tokio::task;

        let bus = CoordinationBus::new().await.unwrap();
        let queue = TaskQueue::new().await.unwrap();

        // Create multiple tasks
        let tasks: Vec<_> = (0..10)
            .map(|i| {
                Task::new(
                    format!("task-{}", i),
                    format!("Test task {}", i),
                    Priority::Medium,
                    TaskType::Development,
                )
            })
            .collect();

        // Simulate concurrent operations
        let mut handles = Vec::new();

        // Task creation tasks
        for task in tasks {
            let queue_clone = queue.clone();
            let handle = task::spawn(async move {
                queue_clone.add_task(&task).await.unwrap();
            });
            handles.push(handle);
        }

        // Message sending tasks
        for i in 0..5 {
            let bus_clone = bus.clone();
            let agent_id = format!("agent-{}", i);
            let handle = task::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id,
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all operations to complete
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify results
        let pending_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(pending_tasks.len(), 10);

        // Verify at least some messages were received
        let mut message_count = 0;
        while bus.try_receive_message().is_some() {
            message_count += 1;
        }
        assert!(message_count > 0);
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:299-326 function test_coordination_message_persistence <-> ./crates/ccswarm/src/tests/integration_tests.rs:484-543 function test_concurrent_agent_operations
  Similarity: 80.76%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_coordination_message_persistence (lines 299-326) ---[0m
    async fn test_coordination_message_persistence() {
        let bus = CoordinationBus::new().await.unwrap();

        let test_message = AgentMessage::TaskCompleted {
            agent_id: "test-agent".to_string(),
            task_id: "test-task".to_string(),
            result: crate::agent::TaskResult {
                success: true,
                output: serde_json::json!({"test": "data"}),
                error: None,
                duration: std::time::Duration::from_secs(30),
            },
        };

        // Send message (should persist automatically)
        bus.send_message(test_message.clone()).await.unwrap();

        // Load persisted messages
        let persisted = bus.load_persisted_messages().await.unwrap();
        assert!(!persisted.is_empty());

        // Find our message
        let found = persisted.iter().find(
            |m| matches!(m, AgentMessage::TaskCompleted { task_id, .. } if task_id == "test-task"),
        );

        assert!(found.is_some());
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_concurrent_agent_operations (lines 484-543) ---[0m
    async fn test_concurrent_agent_operations() {
        use tokio::task;

        let bus = CoordinationBus::new().await.unwrap();
        let queue = TaskQueue::new().await.unwrap();

        // Create multiple tasks
        let tasks: Vec<_> = (0..10)
            .map(|i| {
                Task::new(
                    format!("task-{}", i),
                    format!("Test task {}", i),
                    Priority::Medium,
                    TaskType::Development,
                )
            })
            .collect();

        // Simulate concurrent operations
        let mut handles = Vec::new();

        // Task creation tasks
        for task in tasks {
            let queue_clone = queue.clone();
            let handle = task::spawn(async move {
                queue_clone.add_task(&task).await.unwrap();
            });
            handles.push(handle);
        }

        // Message sending tasks
        for i in 0..5 {
            let bus_clone = bus.clone();
            let agent_id = format!("agent-{}", i);
            let handle = task::spawn(async move {
                let message = AgentMessage::Heartbeat {
                    agent_id,
                    timestamp: chrono::Utc::now(),
                };
                bus_clone.send_message(message).await.unwrap();
            });
            handles.push(handle);
        }

        // Wait for all operations to complete
        for handle in handles {
            handle.await.unwrap();
        }

        // Verify results
        let pending_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(pending_tasks.len(), 10);

        // Verify at least some messages were received
        let mut message_count = 0;
        while bus.try_receive_message().is_some() {
            message_count += 1;
        }
        assert!(message_count > 0);
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:221-249 function test_status_tracker <-> ./crates/ccswarm/src/tests/integration_tests.rs:438-480 function test_error_handling_and_recovery
  Similarity: 85.95%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_status_tracker (lines 221-249) ---[0m
    async fn test_status_tracker() {
        let tracker = StatusTracker::new().await.unwrap();

        let agent_id = "test-agent";
        let status = crate::agent::AgentStatus::Working;
        let additional_info = serde_json::json!({
            "current_task": "creating component",
            "progress": 0.75
        });

        // Update status
        tracker
            .update_status(agent_id, &status, additional_info.clone())
            .await
            .unwrap();

        // Retrieve status
        let retrieved = tracker.get_status(agent_id).await.unwrap();
        assert!(retrieved.is_some());

        let status_info = retrieved.unwrap();
        assert_eq!(status_info["agent_id"], agent_id);
        assert_eq!(status_info["additional_info"], additional_info);

        // Get all statuses
        let all_statuses = tracker.get_all_statuses().await.unwrap();
        assert_eq!(all_statuses.len(), 1);
        assert_eq!(all_statuses[0]["agent_id"], agent_id);
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_error_handling_and_recovery (lines 438-480) ---[0m
    async fn test_error_handling_and_recovery() {
        let bus = CoordinationBus::new().await.unwrap();
        let tracker = StatusTracker::new().await.unwrap();

        let agent_id = "error-prone-agent";

        // 1. Agent encounters an error
        let error_status = crate::agent::AgentStatus::Error("Connection timeout".to_string());
        tracker
            .update_status(
                agent_id,
                &error_status,
                serde_json::json!({"error_details": "Failed to connect to service"}),
            )
            .await
            .unwrap();

        // 2. Send error notification
        bus.send_message(AgentMessage::RequestAssistance {
            agent_id: agent_id.to_string(),
            task_id: "failed-task".to_string(),
            reason: "Connection timeout during task execution".to_string(),
        })
        .await
        .unwrap();

        // 3. Simulate recovery
        let recovered_status = crate::agent::AgentStatus::Available;
        tracker
            .update_status(
                agent_id,
                &recovered_status,
                serde_json::json!({"recovery_time": chrono::Utc::now()}),
            )
            .await
            .unwrap();

        // 4. Verify recovery
        let final_status = tracker.get_status(agent_id).await.unwrap().unwrap();
        let agent_status: crate::agent::AgentStatus =
            serde_json::from_value(final_status["status"].clone()).unwrap();
        assert!(matches!(agent_status, crate::agent::AgentStatus::Available));
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:299-326 function test_coordination_message_persistence <-> ./crates/ccswarm/src/tests/integration_tests.rs:438-480 function test_error_handling_and_recovery
  Similarity: 85.49%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_coordination_message_persistence (lines 299-326) ---[0m
    async fn test_coordination_message_persistence() {
        let bus = CoordinationBus::new().await.unwrap();

        let test_message = AgentMessage::TaskCompleted {
            agent_id: "test-agent".to_string(),
            task_id: "test-task".to_string(),
            result: crate::agent::TaskResult {
                success: true,
                output: serde_json::json!({"test": "data"}),
                error: None,
                duration: std::time::Duration::from_secs(30),
            },
        };

        // Send message (should persist automatically)
        bus.send_message(test_message.clone()).await.unwrap();

        // Load persisted messages
        let persisted = bus.load_persisted_messages().await.unwrap();
        assert!(!persisted.is_empty());

        // Find our message
        let found = persisted.iter().find(
            |m| matches!(m, AgentMessage::TaskCompleted { task_id, .. } if task_id == "test-task"),
        );

        assert!(found.is_some());
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_error_handling_and_recovery (lines 438-480) ---[0m
    async fn test_error_handling_and_recovery() {
        let bus = CoordinationBus::new().await.unwrap();
        let tracker = StatusTracker::new().await.unwrap();

        let agent_id = "error-prone-agent";

        // 1. Agent encounters an error
        let error_status = crate::agent::AgentStatus::Error("Connection timeout".to_string());
        tracker
            .update_status(
                agent_id,
                &error_status,
                serde_json::json!({"error_details": "Failed to connect to service"}),
            )
            .await
            .unwrap();

        // 2. Send error notification
        bus.send_message(AgentMessage::RequestAssistance {
            agent_id: agent_id.to_string(),
            task_id: "failed-task".to_string(),
            reason: "Connection timeout during task execution".to_string(),
        })
        .await
        .unwrap();

        // 3. Simulate recovery
        let recovered_status = crate::agent::AgentStatus::Available;
        tracker
            .update_status(
                agent_id,
                &recovered_status,
                serde_json::json!({"recovery_time": chrono::Utc::now()}),
            )
            .await
            .unwrap();

        // 4. Verify recovery
        let final_status = tracker.get_status(agent_id).await.unwrap().unwrap();
        let agent_status: crate::agent::AgentStatus =
            serde_json::from_value(final_status["status"].clone()).unwrap();
        assert!(matches!(agent_status, crate::agent::AgentStatus::Available));
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:188-217 function test_task_queue_operations <-> ./crates/ccswarm/src/tests/integration_tests.rs:438-480 function test_error_handling_and_recovery
  Similarity: 82.03%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_task_queue_operations (lines 188-217) ---[0m
    async fn test_task_queue_operations() {
        use tempfile::TempDir;
        let temp_dir = TempDir::new().unwrap();
        let queue_dir = temp_dir.path().join("task-queue");
        let queue = TaskQueue::with_dir(queue_dir.to_str().unwrap())
            .await
            .unwrap();
        let tasks = create_test_tasks();

        // Add all tasks
        for task in &tasks {
            queue.add_task(task).await.unwrap();
        }

        // Retrieve tasks
        let pending_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(pending_tasks.len(), tasks.len());

        // Verify tasks are sorted by priority
        for i in 0..pending_tasks.len() - 1 {
            assert!(pending_tasks[i].priority >= pending_tasks[i + 1].priority);
        }

        // Remove a task
        queue.remove_task(&tasks[0].id).await.unwrap();

        let remaining_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(remaining_tasks.len(), tasks.len() - 1);
        assert!(!remaining_tasks.iter().any(|t| t.id == tasks[0].id));
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_error_handling_and_recovery (lines 438-480) ---[0m
    async fn test_error_handling_and_recovery() {
        let bus = CoordinationBus::new().await.unwrap();
        let tracker = StatusTracker::new().await.unwrap();

        let agent_id = "error-prone-agent";

        // 1. Agent encounters an error
        let error_status = crate::agent::AgentStatus::Error("Connection timeout".to_string());
        tracker
            .update_status(
                agent_id,
                &error_status,
                serde_json::json!({"error_details": "Failed to connect to service"}),
            )
            .await
            .unwrap();

        // 2. Send error notification
        bus.send_message(AgentMessage::RequestAssistance {
            agent_id: agent_id.to_string(),
            task_id: "failed-task".to_string(),
            reason: "Connection timeout during task execution".to_string(),
        })
        .await
        .unwrap();

        // 3. Simulate recovery
        let recovered_status = crate::agent::AgentStatus::Available;
        tracker
            .update_status(
                agent_id,
                &recovered_status,
                serde_json::json!({"recovery_time": chrono::Utc::now()}),
            )
            .await
            .unwrap();

        // 4. Verify recovery
        let final_status = tracker.get_status(agent_id).await.unwrap().unwrap();
        let agent_status: crate::agent::AgentStatus =
            serde_json::from_value(final_status["status"].clone()).unwrap();
        assert!(matches!(agent_status, crate::agent::AgentStatus::Available));
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:438-480 function test_error_handling_and_recovery <-> ./crates/ccswarm/src/tests/integration_tests.rs:547-568 function test_config_validation
  Similarity: 82.27%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_error_handling_and_recovery (lines 438-480) ---[0m
    async fn test_error_handling_and_recovery() {
        let bus = CoordinationBus::new().await.unwrap();
        let tracker = StatusTracker::new().await.unwrap();

        let agent_id = "error-prone-agent";

        // 1. Agent encounters an error
        let error_status = crate::agent::AgentStatus::Error("Connection timeout".to_string());
        tracker
            .update_status(
                agent_id,
                &error_status,
                serde_json::json!({"error_details": "Failed to connect to service"}),
            )
            .await
            .unwrap();

        // 2. Send error notification
        bus.send_message(AgentMessage::RequestAssistance {
            agent_id: agent_id.to_string(),
            task_id: "failed-task".to_string(),
            reason: "Connection timeout during task execution".to_string(),
        })
        .await
        .unwrap();

        // 3. Simulate recovery
        let recovered_status = crate::agent::AgentStatus::Available;
        tracker
            .update_status(
                agent_id,
                &recovered_status,
                serde_json::json!({"recovery_time": chrono::Utc::now()}),
            )
            .await
            .unwrap();

        // 4. Verify recovery
        let final_status = tracker.get_status(agent_id).await.unwrap().unwrap();
        let agent_status: crate::agent::AgentStatus =
            serde_json::from_value(final_status["status"].clone()).unwrap();
        assert!(matches!(agent_status, crate::agent::AgentStatus::Available));
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_config_validation (lines 547-568) ---[0m
    async fn test_config_validation() {
        let (config, _temp_dir) = create_test_config().await;

        // Verify config structure
        assert_eq!(config.project.name, "Test Integration Project");
        assert_eq!(config.agents.len(), 2);
        assert!(config.agents.contains_key("frontend"));
        assert!(config.agents.contains_key("backend"));

        // Verify agent configurations
        let frontend_config = &config.agents["frontend"];
        assert_eq!(frontend_config.specialization, "frontend");
        assert!(frontend_config.claude_config.dangerous_skip);

        let backend_config = &config.agents["backend"];
        assert_eq!(backend_config.specialization, "backend");
        assert!(backend_config.claude_config.dangerous_skip);

        // Verify master claude config
        assert_eq!(config.project.master_claude.quality_threshold, 0.85);
        assert!(!config.project.master_claude.claude_config.dangerous_skip);
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:266-295 function test_task_assignment_logic <-> ./crates/ccswarm/src/tests/integration_tests.rs:299-326 function test_coordination_message_persistence
  Similarity: 86.55%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_task_assignment_logic (lines 266-295) ---[0m
    async fn test_task_assignment_logic() {
        let (config, _temp_dir) = create_test_config().await;
        let repo_path = std::path::PathBuf::from(&config.project.repository.url);

        let _master = MasterClaude::new(config, repo_path).await.unwrap();
        let tasks = create_test_tasks();

        // Test task categorization
        for task in &tasks {
            // This would normally call select_optimal_agent, but we test the logic
            let expected_agent = match task.description.as_str() {
                desc if desc.contains("React") || desc.contains("component") => "Frontend",
                desc if desc.contains("API") || desc.contains("authentication") => "Backend",
                desc if desc.contains("CI/CD") || desc.contains("deployment") => "DevOps",
                desc if desc.contains("test") => "QA",
                _ => "Backend", // Default
            };

            // Verify our logic matches expectations
            match task.description.as_str() {
                "Create user login component with React" => {
                    assert_eq!(expected_agent, "Frontend")
                }
                "Implement authentication API endpoint" => assert_eq!(expected_agent, "Backend"),
                "Set up CI/CD pipeline for deployment" => assert_eq!(expected_agent, "DevOps"),
                "Write integration tests for user flow" => assert_eq!(expected_agent, "QA"),
                _ => {}
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_coordination_message_persistence (lines 299-326) ---[0m
    async fn test_coordination_message_persistence() {
        let bus = CoordinationBus::new().await.unwrap();

        let test_message = AgentMessage::TaskCompleted {
            agent_id: "test-agent".to_string(),
            task_id: "test-task".to_string(),
            result: crate::agent::TaskResult {
                success: true,
                output: serde_json::json!({"test": "data"}),
                error: None,
                duration: std::time::Duration::from_secs(30),
            },
        };

        // Send message (should persist automatically)
        bus.send_message(test_message.clone()).await.unwrap();

        // Load persisted messages
        let persisted = bus.load_persisted_messages().await.unwrap();
        assert!(!persisted.is_empty());

        // Find our message
        let found = persisted.iter().find(
            |m| matches!(m, AgentMessage::TaskCompleted { task_id, .. } if task_id == "test-task"),
        );

        assert!(found.is_some());
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:188-217 function test_task_queue_operations <-> ./crates/ccswarm/src/tests/integration_tests.rs:221-249 function test_status_tracker
  Similarity: 81.38%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_task_queue_operations (lines 188-217) ---[0m
    async fn test_task_queue_operations() {
        use tempfile::TempDir;
        let temp_dir = TempDir::new().unwrap();
        let queue_dir = temp_dir.path().join("task-queue");
        let queue = TaskQueue::with_dir(queue_dir.to_str().unwrap())
            .await
            .unwrap();
        let tasks = create_test_tasks();

        // Add all tasks
        for task in &tasks {
            queue.add_task(task).await.unwrap();
        }

        // Retrieve tasks
        let pending_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(pending_tasks.len(), tasks.len());

        // Verify tasks are sorted by priority
        for i in 0..pending_tasks.len() - 1 {
            assert!(pending_tasks[i].priority >= pending_tasks[i + 1].priority);
        }

        // Remove a task
        queue.remove_task(&tasks[0].id).await.unwrap();

        let remaining_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(remaining_tasks.len(), tasks.len() - 1);
        assert!(!remaining_tasks.iter().any(|t| t.id == tasks[0].id));
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_status_tracker (lines 221-249) ---[0m
    async fn test_status_tracker() {
        let tracker = StatusTracker::new().await.unwrap();

        let agent_id = "test-agent";
        let status = crate::agent::AgentStatus::Working;
        let additional_info = serde_json::json!({
            "current_task": "creating component",
            "progress": 0.75
        });

        // Update status
        tracker
            .update_status(agent_id, &status, additional_info.clone())
            .await
            .unwrap();

        // Retrieve status
        let retrieved = tracker.get_status(agent_id).await.unwrap();
        assert!(retrieved.is_some());

        let status_info = retrieved.unwrap();
        assert_eq!(status_info["agent_id"], agent_id);
        assert_eq!(status_info["additional_info"], additional_info);

        // Get all statuses
        let all_statuses = tracker.get_all_statuses().await.unwrap();
        assert_eq!(all_statuses.len(), 1);
        assert_eq!(all_statuses[0]["agent_id"], agent_id);
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:188-217 function test_task_queue_operations <-> ./crates/ccswarm/src/tests/integration_tests.rs:299-326 function test_coordination_message_persistence
  Similarity: 72.67%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_task_queue_operations (lines 188-217) ---[0m
    async fn test_task_queue_operations() {
        use tempfile::TempDir;
        let temp_dir = TempDir::new().unwrap();
        let queue_dir = temp_dir.path().join("task-queue");
        let queue = TaskQueue::with_dir(queue_dir.to_str().unwrap())
            .await
            .unwrap();
        let tasks = create_test_tasks();

        // Add all tasks
        for task in &tasks {
            queue.add_task(task).await.unwrap();
        }

        // Retrieve tasks
        let pending_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(pending_tasks.len(), tasks.len());

        // Verify tasks are sorted by priority
        for i in 0..pending_tasks.len() - 1 {
            assert!(pending_tasks[i].priority >= pending_tasks[i + 1].priority);
        }

        // Remove a task
        queue.remove_task(&tasks[0].id).await.unwrap();

        let remaining_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(remaining_tasks.len(), tasks.len() - 1);
        assert!(!remaining_tasks.iter().any(|t| t.id == tasks[0].id));
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_coordination_message_persistence (lines 299-326) ---[0m
    async fn test_coordination_message_persistence() {
        let bus = CoordinationBus::new().await.unwrap();

        let test_message = AgentMessage::TaskCompleted {
            agent_id: "test-agent".to_string(),
            task_id: "test-task".to_string(),
            result: crate::agent::TaskResult {
                success: true,
                output: serde_json::json!({"test": "data"}),
                error: None,
                duration: std::time::Duration::from_secs(30),
            },
        };

        // Send message (should persist automatically)
        bus.send_message(test_message.clone()).await.unwrap();

        // Load persisted messages
        let persisted = bus.load_persisted_messages().await.unwrap();
        assert!(!persisted.is_empty());

        // Find our message
        let found = persisted.iter().find(
            |m| matches!(m, AgentMessage::TaskCompleted { task_id, .. } if task_id == "test-task"),
        );

        assert!(found.is_some());
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:221-249 function test_status_tracker <-> ./crates/ccswarm/src/tests/integration_tests.rs:299-326 function test_coordination_message_persistence
  Similarity: 72.23%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_status_tracker (lines 221-249) ---[0m
    async fn test_status_tracker() {
        let tracker = StatusTracker::new().await.unwrap();

        let agent_id = "test-agent";
        let status = crate::agent::AgentStatus::Working;
        let additional_info = serde_json::json!({
            "current_task": "creating component",
            "progress": 0.75
        });

        // Update status
        tracker
            .update_status(agent_id, &status, additional_info.clone())
            .await
            .unwrap();

        // Retrieve status
        let retrieved = tracker.get_status(agent_id).await.unwrap();
        assert!(retrieved.is_some());

        let status_info = retrieved.unwrap();
        assert_eq!(status_info["agent_id"], agent_id);
        assert_eq!(status_info["additional_info"], additional_info);

        // Get all statuses
        let all_statuses = tracker.get_all_statuses().await.unwrap();
        assert_eq!(all_statuses.len(), 1);
        assert_eq!(all_statuses[0]["agent_id"], agent_id);
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_coordination_message_persistence (lines 299-326) ---[0m
    async fn test_coordination_message_persistence() {
        let bus = CoordinationBus::new().await.unwrap();

        let test_message = AgentMessage::TaskCompleted {
            agent_id: "test-agent".to_string(),
            task_id: "test-task".to_string(),
            result: crate::agent::TaskResult {
                success: true,
                output: serde_json::json!({"test": "data"}),
                error: None,
                duration: std::time::Duration::from_secs(30),
            },
        };

        // Send message (should persist automatically)
        bus.send_message(test_message.clone()).await.unwrap();

        // Load persisted messages
        let persisted = bus.load_persisted_messages().await.unwrap();
        assert!(!persisted.is_empty());

        // Find our message
        let found = persisted.iter().find(
            |m| matches!(m, AgentMessage::TaskCompleted { task_id, .. } if task_id == "test-task"),
        );

        assert!(found.is_some());
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:188-217 function test_task_queue_operations <-> ./crates/ccswarm/src/tests/integration_tests.rs:547-568 function test_config_validation
  Similarity: 78.53%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_task_queue_operations (lines 188-217) ---[0m
    async fn test_task_queue_operations() {
        use tempfile::TempDir;
        let temp_dir = TempDir::new().unwrap();
        let queue_dir = temp_dir.path().join("task-queue");
        let queue = TaskQueue::with_dir(queue_dir.to_str().unwrap())
            .await
            .unwrap();
        let tasks = create_test_tasks();

        // Add all tasks
        for task in &tasks {
            queue.add_task(task).await.unwrap();
        }

        // Retrieve tasks
        let pending_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(pending_tasks.len(), tasks.len());

        // Verify tasks are sorted by priority
        for i in 0..pending_tasks.len() - 1 {
            assert!(pending_tasks[i].priority >= pending_tasks[i + 1].priority);
        }

        // Remove a task
        queue.remove_task(&tasks[0].id).await.unwrap();

        let remaining_tasks = queue.get_pending_tasks().await.unwrap();
        assert_eq!(remaining_tasks.len(), tasks.len() - 1);
        assert!(!remaining_tasks.iter().any(|t| t.id == tasks[0].id));
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_config_validation (lines 547-568) ---[0m
    async fn test_config_validation() {
        let (config, _temp_dir) = create_test_config().await;

        // Verify config structure
        assert_eq!(config.project.name, "Test Integration Project");
        assert_eq!(config.agents.len(), 2);
        assert!(config.agents.contains_key("frontend"));
        assert!(config.agents.contains_key("backend"));

        // Verify agent configurations
        let frontend_config = &config.agents["frontend"];
        assert_eq!(frontend_config.specialization, "frontend");
        assert!(frontend_config.claude_config.dangerous_skip);

        let backend_config = &config.agents["backend"];
        assert_eq!(backend_config.specialization, "backend");
        assert!(backend_config.claude_config.dangerous_skip);

        // Verify master claude config
        assert_eq!(config.project.master_claude.quality_threshold, 0.85);
        assert!(!config.project.master_claude.claude_config.dangerous_skip);
    }

  ./crates/ccswarm/src/tests/integration_tests.rs:221-249 function test_status_tracker <-> ./crates/ccswarm/src/tests/integration_tests.rs:547-568 function test_config_validation
  Similarity: 76.21%

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_status_tracker (lines 221-249) ---[0m
    async fn test_status_tracker() {
        let tracker = StatusTracker::new().await.unwrap();

        let agent_id = "test-agent";
        let status = crate::agent::AgentStatus::Working;
        let additional_info = serde_json::json!({
            "current_task": "creating component",
            "progress": 0.75
        });

        // Update status
        tracker
            .update_status(agent_id, &status, additional_info.clone())
            .await
            .unwrap();

        // Retrieve status
        let retrieved = tracker.get_status(agent_id).await.unwrap();
        assert!(retrieved.is_some());

        let status_info = retrieved.unwrap();
        assert_eq!(status_info["agent_id"], agent_id);
        assert_eq!(status_info["additional_info"], additional_info);

        // Get all statuses
        let all_statuses = tracker.get_all_statuses().await.unwrap();
        assert_eq!(all_statuses.len(), 1);
        assert_eq!(all_statuses[0]["agent_id"], agent_id);
    }

[36m--- ./crates/ccswarm/src/tests/integration_tests.rs:test_config_validation (lines 547-568) ---[0m
    async fn test_config_validation() {
        let (config, _temp_dir) = create_test_config().await;

        // Verify config structure
        assert_eq!(config.project.name, "Test Integration Project");
        assert_eq!(config.agents.len(), 2);
        assert!(config.agents.contains_key("frontend"));
        assert!(config.agents.contains_key("backend"));

        // Verify agent configurations
        let frontend_config = &config.agents["frontend"];
        assert_eq!(frontend_config.specialization, "frontend");
        assert!(frontend_config.claude_config.dangerous_skip);

        let backend_config = &config.agents["backend"];
        assert_eq!(backend_config.specialization, "backend");
        assert!(backend_config.claude_config.dangerous_skip);

        // Verify master claude config
        assert_eq!(config.project.master_claude.quality_threshold, 0.85);
        assert!(!config.project.master_claude.claude_config.dangerous_skip);
    }


Duplicates in ./crates/ccswarm/src/template/types.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/template/types.rs:233-263 method new <-> ./crates/ccswarm/src/template/types.rs:350-372 method update_success_rate
  Similarity: 77.76%
  Classes: Template <-> Template

[36m--- ./crates/ccswarm/src/template/types.rs:new (lines 233-263) ---[0m
    pub fn new(
        id: impl Into<String>,
        name: impl Into<String>,
        description: impl Into<String>,
        category: TemplateCategory,
    ) -> Self {
        let now = chrono::Utc::now();
        Self {
            id: id.into(),
            name: name.into(),
            description: description.into(),
            category,
            author: None,
            version: "1.0.0".to_string(),
            created_at: now,
            updated_at: now,
            tags: Vec::new(),
            task_description: String::new(),
            task_details: None,
            default_priority: crate::agent::Priority::Medium,
            default_task_type: crate::agent::TaskType::Development,
            estimated_duration: None,
            variables: Vec::new(),
            target_files: Vec::new(),
            preconditions: Vec::new(),
            post_actions: Vec::new(),
            usage_count: 0,
            success_rate: None,
            metadata: HashMap::new(),
        }
    }

[36m--- ./crates/ccswarm/src/template/types.rs:update_success_rate (lines 350-372) ---[0m
    pub fn update_success_rate(&mut self, success: bool) {
        let current_rate = self.success_rate.unwrap_or(0.0);
        let current_count = self.usage_count as f64;

        let new_rate = if current_count == 0.0 {
            if success {
                1.0
            } else {
                0.0
            }
        } else {
            let total_successes = current_rate * current_count;
            let new_successes = if success {
                total_successes + 1.0
            } else {
                total_successes
            };
            new_successes / (current_count + 1.0)
        };

        self.success_rate = Some(new_rate);
        self.updated_at = chrono::Utc::now();
    }

  ./crates/ccswarm/src/template/types.rs:113-127 method text_with_default <-> ./crates/ccswarm/src/template/types.rs:130-144 method choice
  Similarity: 93.52%
  Classes: TemplateVariable <-> TemplateVariable

[36m--- ./crates/ccswarm/src/template/types.rs:text_with_default (lines 113-127) ---[0m
    pub fn text_with_default(
        name: impl Into<String>,
        description: impl Into<String>,
        default: impl Into<String>,
    ) -> Self {
        Self {
            name: name.into(),
            description: description.into(),
            variable_type: VariableType::Text,
            default_value: Some(default.into()),
            required: false,
            example: None,
            validation_pattern: None,
        }
    }

[36m--- ./crates/ccswarm/src/template/types.rs:choice (lines 130-144) ---[0m
    pub fn choice(
        name: impl Into<String>,
        description: impl Into<String>,
        choices: Vec<String>,
    ) -> Self {
        Self {
            name: name.into(),
            description: description.into(),
            variable_type: VariableType::Choice(choices),
            default_value: None,
            required: true,
            example: None,
            validation_pattern: None,
        }
    }

  ./crates/ccswarm/src/template/types.rs:100-110 method text <-> ./crates/ccswarm/src/template/types.rs:113-127 method text_with_default
  Similarity: 98.17%
  Classes: TemplateVariable <-> TemplateVariable

[36m--- ./crates/ccswarm/src/template/types.rs:text (lines 100-110) ---[0m
    pub fn text(name: impl Into<String>, description: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            description: description.into(),
            variable_type: VariableType::Text,
            default_value: None,
            required: true,
            example: None,
            validation_pattern: None,
        }
    }

[36m--- ./crates/ccswarm/src/template/types.rs:text_with_default (lines 113-127) ---[0m
    pub fn text_with_default(
        name: impl Into<String>,
        description: impl Into<String>,
        default: impl Into<String>,
    ) -> Self {
        Self {
            name: name.into(),
            description: description.into(),
            variable_type: VariableType::Text,
            default_value: Some(default.into()),
            required: false,
            example: None,
            validation_pattern: None,
        }
    }

  ./crates/ccswarm/src/template/types.rs:113-127 method text_with_default <-> ./crates/ccswarm/src/template/types.rs:147-157 method boolean
  Similarity: 97.08%
  Classes: TemplateVariable <-> TemplateVariable

[36m--- ./crates/ccswarm/src/template/types.rs:text_with_default (lines 113-127) ---[0m
    pub fn text_with_default(
        name: impl Into<String>,
        description: impl Into<String>,
        default: impl Into<String>,
    ) -> Self {
        Self {
            name: name.into(),
            description: description.into(),
            variable_type: VariableType::Text,
            default_value: Some(default.into()),
            required: false,
            example: None,
            validation_pattern: None,
        }
    }

[36m--- ./crates/ccswarm/src/template/types.rs:boolean (lines 147-157) ---[0m
    pub fn boolean(name: impl Into<String>, description: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            description: description.into(),
            variable_type: VariableType::Boolean,
            default_value: Some("false".to_string()),
            required: false,
            example: Some("true".to_string()),
            validation_pattern: None,
        }
    }

  ./crates/ccswarm/src/template/types.rs:100-110 method text <-> ./crates/ccswarm/src/template/types.rs:130-144 method choice
  Similarity: 94.84%
  Classes: TemplateVariable <-> TemplateVariable

[36m--- ./crates/ccswarm/src/template/types.rs:text (lines 100-110) ---[0m
    pub fn text(name: impl Into<String>, description: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            description: description.into(),
            variable_type: VariableType::Text,
            default_value: None,
            required: true,
            example: None,
            validation_pattern: None,
        }
    }

[36m--- ./crates/ccswarm/src/template/types.rs:choice (lines 130-144) ---[0m
    pub fn choice(
        name: impl Into<String>,
        description: impl Into<String>,
        choices: Vec<String>,
    ) -> Self {
        Self {
            name: name.into(),
            description: description.into(),
            variable_type: VariableType::Choice(choices),
            default_value: None,
            required: true,
            example: None,
            validation_pattern: None,
        }
    }

  ./crates/ccswarm/src/template/types.rs:130-144 method choice <-> ./crates/ccswarm/src/template/types.rs:147-157 method boolean
  Similarity: 93.71%
  Classes: TemplateVariable <-> TemplateVariable

[36m--- ./crates/ccswarm/src/template/types.rs:choice (lines 130-144) ---[0m
    pub fn choice(
        name: impl Into<String>,
        description: impl Into<String>,
        choices: Vec<String>,
    ) -> Self {
        Self {
            name: name.into(),
            description: description.into(),
            variable_type: VariableType::Choice(choices),
            default_value: None,
            required: true,
            example: None,
            validation_pattern: None,
        }
    }

[36m--- ./crates/ccswarm/src/template/types.rs:boolean (lines 147-157) ---[0m
    pub fn boolean(name: impl Into<String>, description: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            description: description.into(),
            variable_type: VariableType::Boolean,
            default_value: Some("false".to_string()),
            required: false,
            example: Some("true".to_string()),
            validation_pattern: None,
        }
    }

  ./crates/ccswarm/src/template/types.rs:100-110 method text <-> ./crates/ccswarm/src/template/types.rs:147-157 method boolean
  Similarity: 97.08%
  Classes: TemplateVariable <-> TemplateVariable

[36m--- ./crates/ccswarm/src/template/types.rs:text (lines 100-110) ---[0m
    pub fn text(name: impl Into<String>, description: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            description: description.into(),
            variable_type: VariableType::Text,
            default_value: None,
            required: true,
            example: None,
            validation_pattern: None,
        }
    }

[36m--- ./crates/ccswarm/src/template/types.rs:boolean (lines 147-157) ---[0m
    pub fn boolean(name: impl Into<String>, description: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            description: description.into(),
            variable_type: VariableType::Boolean,
            default_value: Some("false".to_string()),
            required: false,
            example: Some("true".to_string()),
            validation_pattern: None,
        }
    }

  ./crates/ccswarm/src/template/types.rs:31-42 method from_str <-> ./crates/ccswarm/src/template/types.rs:46-57 method fmt
  Similarity: 80.24%
  Classes: FromStr <-> TemplateCategory

[36m--- ./crates/ccswarm/src/template/types.rs:from_str (lines 31-42) ---[0m
    fn from_str(s: &str) -> Result<Self, Self::Err> {
        match s.to_lowercase().as_str() {
            "frontend" | "front" | "ui" => Ok(TemplateCategory::Frontend),
            "backend" | "back" | "api" => Ok(TemplateCategory::Backend),
            "devops" | "ops" | "infra" | "infrastructure" => Ok(TemplateCategory::DevOps),
            "testing" | "test" | "qa" => Ok(TemplateCategory::Testing),
            "documentation" | "docs" | "doc" => Ok(TemplateCategory::Documentation),
            "bugfix" | "bug" | "fix" => Ok(TemplateCategory::BugFix),
            "general" | "misc" => Ok(TemplateCategory::General),
            _ => Ok(TemplateCategory::Custom(s.to_string())),
        }
    }

[36m--- ./crates/ccswarm/src/template/types.rs:fmt (lines 46-57) ---[0m
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            TemplateCategory::Frontend => write!(f, "Frontend"),
            TemplateCategory::Backend => write!(f, "Backend"),
            TemplateCategory::DevOps => write!(f, "DevOps"),
            TemplateCategory::Testing => write!(f, "Testing"),
            TemplateCategory::Documentation => write!(f, "Documentation"),
            TemplateCategory::BugFix => write!(f, "BugFix"),
            TemplateCategory::General => write!(f, "General"),
            TemplateCategory::Custom(name) => write!(f, "{}", name),
        }
    }

  ./crates/ccswarm/src/template/types.rs:113-127 method text_with_default <-> ./crates/ccswarm/src/template/types.rs:375-377 method get_variable_names
  Similarity: 93.94%
  Classes: TemplateVariable <-> Template

[36m--- ./crates/ccswarm/src/template/types.rs:text_with_default (lines 113-127) ---[0m
    pub fn text_with_default(
        name: impl Into<String>,
        description: impl Into<String>,
        default: impl Into<String>,
    ) -> Self {
        Self {
            name: name.into(),
            description: description.into(),
            variable_type: VariableType::Text,
            default_value: Some(default.into()),
            required: false,
            example: None,
            validation_pattern: None,
        }
    }

[36m--- ./crates/ccswarm/src/template/types.rs:get_variable_names (lines 375-377) ---[0m
    pub fn get_variable_names(&self) -> Vec<String> {
        self.variables.iter().map(|v| v.name.clone()).collect()
    }

  ./crates/ccswarm/src/template/types.rs:130-144 method choice <-> ./crates/ccswarm/src/template/types.rs:375-377 method get_variable_names
  Similarity: 93.28%
  Classes: TemplateVariable <-> Template

[36m--- ./crates/ccswarm/src/template/types.rs:choice (lines 130-144) ---[0m
    pub fn choice(
        name: impl Into<String>,
        description: impl Into<String>,
        choices: Vec<String>,
    ) -> Self {
        Self {
            name: name.into(),
            description: description.into(),
            variable_type: VariableType::Choice(choices),
            default_value: None,
            required: true,
            example: None,
            validation_pattern: None,
        }
    }

[36m--- ./crates/ccswarm/src/template/types.rs:get_variable_names (lines 375-377) ---[0m
    pub fn get_variable_names(&self) -> Vec<String> {
        self.variables.iter().map(|v| v.name.clone()).collect()
    }

  ./crates/ccswarm/src/template/types.rs:46-57 method fmt <-> ./crates/ccswarm/src/template/types.rs:147-157 method boolean
  Similarity: 70.92%
  Classes: TemplateCategory <-> TemplateVariable

[36m--- ./crates/ccswarm/src/template/types.rs:fmt (lines 46-57) ---[0m
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            TemplateCategory::Frontend => write!(f, "Frontend"),
            TemplateCategory::Backend => write!(f, "Backend"),
            TemplateCategory::DevOps => write!(f, "DevOps"),
            TemplateCategory::Testing => write!(f, "Testing"),
            TemplateCategory::Documentation => write!(f, "Documentation"),
            TemplateCategory::BugFix => write!(f, "BugFix"),
            TemplateCategory::General => write!(f, "General"),
            TemplateCategory::Custom(name) => write!(f, "{}", name),
        }
    }

[36m--- ./crates/ccswarm/src/template/types.rs:boolean (lines 147-157) ---[0m
    pub fn boolean(name: impl Into<String>, description: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            description: description.into(),
            variable_type: VariableType::Boolean,
            default_value: Some("false".to_string()),
            required: false,
            example: Some("true".to_string()),
            validation_pattern: None,
        }
    }

  ./crates/ccswarm/src/template/types.rs:113-127 method text_with_default <-> ./crates/ccswarm/src/template/types.rs:380-382 method is_valid
  Similarity: 78.45%
  Classes: TemplateVariable <-> Template

[36m--- ./crates/ccswarm/src/template/types.rs:text_with_default (lines 113-127) ---[0m
    pub fn text_with_default(
        name: impl Into<String>,
        description: impl Into<String>,
        default: impl Into<String>,
    ) -> Self {
        Self {
            name: name.into(),
            description: description.into(),
            variable_type: VariableType::Text,
            default_value: Some(default.into()),
            required: false,
            example: None,
            validation_pattern: None,
        }
    }

[36m--- ./crates/ccswarm/src/template/types.rs:is_valid (lines 380-382) ---[0m
    pub fn is_valid(&self) -> bool {
        !self.task_description.is_empty() && !self.name.is_empty() && !self.id.is_empty()
    }

  ./crates/ccswarm/src/template/types.rs:130-144 method choice <-> ./crates/ccswarm/src/template/types.rs:380-382 method is_valid
  Similarity: 76.09%
  Classes: TemplateVariable <-> Template

[36m--- ./crates/ccswarm/src/template/types.rs:choice (lines 130-144) ---[0m
    pub fn choice(
        name: impl Into<String>,
        description: impl Into<String>,
        choices: Vec<String>,
    ) -> Self {
        Self {
            name: name.into(),
            description: description.into(),
            variable_type: VariableType::Choice(choices),
            default_value: None,
            required: true,
            example: None,
            validation_pattern: None,
        }
    }

[36m--- ./crates/ccswarm/src/template/types.rs:is_valid (lines 380-382) ---[0m
    pub fn is_valid(&self) -> bool {
        !self.task_description.is_empty() && !self.name.is_empty() && !self.id.is_empty()
    }

  ./crates/ccswarm/src/template/types.rs:100-110 method text <-> ./crates/ccswarm/src/template/types.rs:375-377 method get_variable_names
  Similarity: 92.71%
  Classes: TemplateVariable <-> Template

[36m--- ./crates/ccswarm/src/template/types.rs:text (lines 100-110) ---[0m
    pub fn text(name: impl Into<String>, description: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            description: description.into(),
            variable_type: VariableType::Text,
            default_value: None,
            required: true,
            example: None,
            validation_pattern: None,
        }
    }

[36m--- ./crates/ccswarm/src/template/types.rs:get_variable_names (lines 375-377) ---[0m
    pub fn get_variable_names(&self) -> Vec<String> {
        self.variables.iter().map(|v| v.name.clone()).collect()
    }

  ./crates/ccswarm/src/template/types.rs:100-110 method text <-> ./crates/ccswarm/src/template/types.rs:380-382 method is_valid
  Similarity: 74.07%
  Classes: TemplateVariable <-> Template

[36m--- ./crates/ccswarm/src/template/types.rs:text (lines 100-110) ---[0m
    pub fn text(name: impl Into<String>, description: impl Into<String>) -> Self {
        Self {
            name: name.into(),
            description: description.into(),
            variable_type: VariableType::Text,
            default_value: None,
            required: true,
            example: None,
            validation_pattern: None,
        }
    }

[36m--- ./crates/ccswarm/src/template/types.rs:is_valid (lines 380-382) ---[0m
    pub fn is_valid(&self) -> bool {
        !self.task_description.is_empty() && !self.name.is_empty() && !self.id.is_empty()
    }

  ./crates/ccswarm/src/template/types.rs:375-377 method get_variable_names <-> ./crates/ccswarm/src/template/types.rs:380-382 method is_valid
  Similarity: 76.36%
  Classes: Template <-> Template

[36m--- ./crates/ccswarm/src/template/types.rs:get_variable_names (lines 375-377) ---[0m
    pub fn get_variable_names(&self) -> Vec<String> {
        self.variables.iter().map(|v| v.name.clone()).collect()
    }

[36m--- ./crates/ccswarm/src/template/types.rs:is_valid (lines 380-382) ---[0m
    pub fn is_valid(&self) -> bool {
        !self.task_description.is_empty() && !self.name.is_empty() && !self.id.is_empty()
    }


Duplicates in ./crates/ccswarm/src/session/claude_session.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/session/claude_session.rs:32-54 method new <-> ./crates/ccswarm/src/session/claude_session.rs:77-99 method execute_task
  Similarity: 85.93%
  Classes: PersistentClaudeSession <-> PersistentClaudeSession

[36m--- ./crates/ccswarm/src/session/claude_session.rs:new (lines 32-54) ---[0m
    pub async fn new(
        identity: AgentIdentity,
        working_dir: PathBuf,
        claude_config: ClaudeConfig,
    ) -> Result<Self> {
        let session_id = format!("session-{}-{}", identity.agent_id, uuid::Uuid::new_v4());

        let mut env_vars = HashMap::new();
        env_vars.insert("AGENT_ID".to_string(), identity.agent_id.clone());
        env_vars.insert(
            "AGENT_TYPE".to_string(),
            identity.specialization.name().to_string(),
        );
        env_vars.insert("SESSION_ID".to_string(), session_id.clone());

        Ok(Self {
            identity,
            working_dir,
            claude_config,
            session_id,
            env_vars,
        })
    }

[36m--- ./crates/ccswarm/src/session/claude_session.rs:execute_task (lines 77-99) ---[0m
    pub async fn execute_task(&mut self, task: Task) -> Result<TaskResult> {
        info!("📋 Executing task {}: {}", task.id, task.description);

        // Generate task prompt
        let prompt = self.generate_task_prompt(&task);

        // Execute with Claude
        let response = self.execute_prompt(&prompt).await?;

        // Create task result
        Ok(TaskResult {
            success: true,
            output: serde_json::json!({
                "task_id": task.id,
                "agent_id": self.identity.agent_id.clone(),
                "response": response,
                "files_created": [],
                "files_modified": [],
            }),
            error: None,
            duration: std::time::Duration::from_secs(10),
        })
    }

  ./crates/ccswarm/src/session/claude_session.rs:77-99 method execute_task <-> ./crates/ccswarm/src/session/claude_session.rs:102-121 method execute_command
  Similarity: 87.88%
  Classes: PersistentClaudeSession <-> PersistentClaudeSession

[36m--- ./crates/ccswarm/src/session/claude_session.rs:execute_task (lines 77-99) ---[0m
    pub async fn execute_task(&mut self, task: Task) -> Result<TaskResult> {
        info!("📋 Executing task {}: {}", task.id, task.description);

        // Generate task prompt
        let prompt = self.generate_task_prompt(&task);

        // Execute with Claude
        let response = self.execute_prompt(&prompt).await?;

        // Create task result
        Ok(TaskResult {
            success: true,
            output: serde_json::json!({
                "task_id": task.id,
                "agent_id": self.identity.agent_id.clone(),
                "response": response,
                "files_created": [],
                "files_modified": [],
            }),
            error: None,
            duration: std::time::Duration::from_secs(10),
        })
    }

[36m--- ./crates/ccswarm/src/session/claude_session.rs:execute_command (lines 102-121) ---[0m
    pub async fn execute_command(&mut self, command: &str) -> Result<String> {
        info!("🔧 Executing command: {}", command);

        // Execute shell command in working directory
        let output = Command::new("sh")
            .arg("-c")
            .arg(command)
            .current_dir(&self.working_dir)
            .output()
            .await?;

        let stdout = String::from_utf8_lossy(&output.stdout);
        let stderr = String::from_utf8_lossy(&output.stderr);

        if !output.status.success() {
            warn!("Command failed: {}", stderr);
        }

        Ok(format!("{}\n{}", stdout, stderr))
    }

  ./crates/ccswarm/src/session/claude_session.rs:57-74 method initialize <-> ./crates/ccswarm/src/session/claude_session.rs:77-99 method execute_task
  Similarity: 89.76%
  Classes: PersistentClaudeSession <-> PersistentClaudeSession

[36m--- ./crates/ccswarm/src/session/claude_session.rs:initialize (lines 57-74) ---[0m
    pub async fn initialize(&mut self) -> Result<()> {
        info!("🚀 Initializing session: {}", self.session_id);

        // Create working directory if needed
        tokio::fs::create_dir_all(&self.working_dir).await?;

        // Establish identity
        let identity_prompt = format!(
            "You are a {} agent. Your workspace is {}. Your specialization is in {}.",
            self.identity.specialization.name(),
            self.working_dir.display(),
            self.identity.specialization.name()
        );

        self.execute_prompt(&identity_prompt).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/claude_session.rs:execute_task (lines 77-99) ---[0m
    pub async fn execute_task(&mut self, task: Task) -> Result<TaskResult> {
        info!("📋 Executing task {}: {}", task.id, task.description);

        // Generate task prompt
        let prompt = self.generate_task_prompt(&task);

        // Execute with Claude
        let response = self.execute_prompt(&prompt).await?;

        // Create task result
        Ok(TaskResult {
            success: true,
            output: serde_json::json!({
                "task_id": task.id,
                "agent_id": self.identity.agent_id.clone(),
                "response": response,
                "files_created": [],
                "files_modified": [],
            }),
            error: None,
            duration: std::time::Duration::from_secs(10),
        })
    }

  ./crates/ccswarm/src/session/claude_session.rs:32-54 method new <-> ./crates/ccswarm/src/session/claude_session.rs:57-74 method initialize
  Similarity: 87.21%
  Classes: PersistentClaudeSession <-> PersistentClaudeSession

[36m--- ./crates/ccswarm/src/session/claude_session.rs:new (lines 32-54) ---[0m
    pub async fn new(
        identity: AgentIdentity,
        working_dir: PathBuf,
        claude_config: ClaudeConfig,
    ) -> Result<Self> {
        let session_id = format!("session-{}-{}", identity.agent_id, uuid::Uuid::new_v4());

        let mut env_vars = HashMap::new();
        env_vars.insert("AGENT_ID".to_string(), identity.agent_id.clone());
        env_vars.insert(
            "AGENT_TYPE".to_string(),
            identity.specialization.name().to_string(),
        );
        env_vars.insert("SESSION_ID".to_string(), session_id.clone());

        Ok(Self {
            identity,
            working_dir,
            claude_config,
            session_id,
            env_vars,
        })
    }

[36m--- ./crates/ccswarm/src/session/claude_session.rs:initialize (lines 57-74) ---[0m
    pub async fn initialize(&mut self) -> Result<()> {
        info!("🚀 Initializing session: {}", self.session_id);

        // Create working directory if needed
        tokio::fs::create_dir_all(&self.working_dir).await?;

        // Establish identity
        let identity_prompt = format!(
            "You are a {} agent. Your workspace is {}. Your specialization is in {}.",
            self.identity.specialization.name(),
            self.working_dir.display(),
            self.identity.specialization.name()
        );

        self.execute_prompt(&identity_prompt).await?;

        Ok(())
    }

  ./crates/ccswarm/src/session/claude_session.rs:77-99 method execute_task <-> ./crates/ccswarm/src/session/claude_session.rs:124-140 method execute_prompt
  Similarity: 87.88%
  Classes: PersistentClaudeSession <-> PersistentClaudeSession

[36m--- ./crates/ccswarm/src/session/claude_session.rs:execute_task (lines 77-99) ---[0m
    pub async fn execute_task(&mut self, task: Task) -> Result<TaskResult> {
        info!("📋 Executing task {}: {}", task.id, task.description);

        // Generate task prompt
        let prompt = self.generate_task_prompt(&task);

        // Execute with Claude
        let response = self.execute_prompt(&prompt).await?;

        // Create task result
        Ok(TaskResult {
            success: true,
            output: serde_json::json!({
                "task_id": task.id,
                "agent_id": self.identity.agent_id.clone(),
                "response": response,
                "files_created": [],
                "files_modified": [],
            }),
            error: None,
            duration: std::time::Duration::from_secs(10),
        })
    }

[36m--- ./crates/ccswarm/src/session/claude_session.rs:execute_prompt (lines 124-140) ---[0m
    async fn execute_prompt(&self, prompt: &str) -> Result<String> {
        info!("🤖 Claude prompt: {}", prompt);

        // In real implementation, this would call Claude Code CLI
        // For now, simulate response based on task
        let response = if prompt.contains("React") || prompt.contains("frontend") {
            "Created React components with TypeScript. Files: App.tsx, TodoList.tsx, TodoItem.tsx"
        } else if prompt.contains("API") || prompt.contains("backend") {
            "Created Express API with endpoints: GET /todos, POST /todos, PUT /todos/:id, DELETE /todos/:id"
        } else if prompt.contains("test") {
            "Created test suites with Jest. Coverage: 85%"
        } else {
            "Task completed successfully"
        };

        Ok(response.to_string())
    }

  ./crates/ccswarm/src/session/claude_session.rs:32-54 method new <-> ./crates/ccswarm/src/session/claude_session.rs:102-121 method execute_command
  Similarity: 79.24%
  Classes: PersistentClaudeSession <-> PersistentClaudeSession

[36m--- ./crates/ccswarm/src/session/claude_session.rs:new (lines 32-54) ---[0m
    pub async fn new(
        identity: AgentIdentity,
        working_dir: PathBuf,
        claude_config: ClaudeConfig,
    ) -> Result<Self> {
        let session_id = format!("session-{}-{}", identity.agent_id, uuid::Uuid::new_v4());

        let mut env_vars = HashMap::new();
        env_vars.insert("AGENT_ID".to_string(), identity.agent_id.clone());
        env_vars.insert(
            "AGENT_TYPE".to_string(),
            identity.specialization.name().to_string(),
        );
        env_vars.insert("SESSION_ID".to_string(), session_id.clone());

        Ok(Self {
            identity,
            working_dir,
            claude_config,
            session_id,
            env_vars,
        })
    }

[36m--- ./crates/ccswarm/src/session/claude_session.rs:execute_command (lines 102-121) ---[0m
    pub async fn execute_command(&mut self, command: &str) -> Result<String> {
        info!("🔧 Executing command: {}", command);

        // Execute shell command in working directory
        let output = Command::new("sh")
            .arg("-c")
            .arg(command)
            .current_dir(&self.working_dir)
            .output()
            .await?;

        let stdout = String::from_utf8_lossy(&output.stdout);
        let stderr = String::from_utf8_lossy(&output.stderr);

        if !output.status.success() {
            warn!("Command failed: {}", stderr);
        }

        Ok(format!("{}\n{}", stdout, stderr))
    }

  ./crates/ccswarm/src/session/claude_session.rs:57-74 method initialize <-> ./crates/ccswarm/src/session/claude_session.rs:102-121 method execute_command
  Similarity: 86.24%
  Classes: PersistentClaudeSession <-> PersistentClaudeSession

[36m--- ./crates/ccswarm/src/session/claude_session.rs:initialize (lines 57-74) ---[0m
    pub async fn initialize(&mut self) -> Result<()> {
        info!("🚀 Initializing session: {}", self.session_id);

        // Create working directory if needed
        tokio::fs::create_dir_all(&self.working_dir).await?;

        // Establish identity
        let identity_prompt = format!(
            "You are a {} agent. Your workspace is {}. Your specialization is in {}.",
            self.identity.specialization.name(),
            self.working_dir.display(),
            self.identity.specialization.name()
        );

        self.execute_prompt(&identity_prompt).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/claude_session.rs:execute_command (lines 102-121) ---[0m
    pub async fn execute_command(&mut self, command: &str) -> Result<String> {
        info!("🔧 Executing command: {}", command);

        // Execute shell command in working directory
        let output = Command::new("sh")
            .arg("-c")
            .arg(command)
            .current_dir(&self.working_dir)
            .output()
            .await?;

        let stdout = String::from_utf8_lossy(&output.stdout);
        let stderr = String::from_utf8_lossy(&output.stderr);

        if !output.status.success() {
            warn!("Command failed: {}", stderr);
        }

        Ok(format!("{}\n{}", stdout, stderr))
    }

  ./crates/ccswarm/src/session/claude_session.rs:32-54 method new <-> ./crates/ccswarm/src/session/claude_session.rs:124-140 method execute_prompt
  Similarity: 81.05%
  Classes: PersistentClaudeSession <-> PersistentClaudeSession

[36m--- ./crates/ccswarm/src/session/claude_session.rs:new (lines 32-54) ---[0m
    pub async fn new(
        identity: AgentIdentity,
        working_dir: PathBuf,
        claude_config: ClaudeConfig,
    ) -> Result<Self> {
        let session_id = format!("session-{}-{}", identity.agent_id, uuid::Uuid::new_v4());

        let mut env_vars = HashMap::new();
        env_vars.insert("AGENT_ID".to_string(), identity.agent_id.clone());
        env_vars.insert(
            "AGENT_TYPE".to_string(),
            identity.specialization.name().to_string(),
        );
        env_vars.insert("SESSION_ID".to_string(), session_id.clone());

        Ok(Self {
            identity,
            working_dir,
            claude_config,
            session_id,
            env_vars,
        })
    }

[36m--- ./crates/ccswarm/src/session/claude_session.rs:execute_prompt (lines 124-140) ---[0m
    async fn execute_prompt(&self, prompt: &str) -> Result<String> {
        info!("🤖 Claude prompt: {}", prompt);

        // In real implementation, this would call Claude Code CLI
        // For now, simulate response based on task
        let response = if prompt.contains("React") || prompt.contains("frontend") {
            "Created React components with TypeScript. Files: App.tsx, TodoList.tsx, TodoItem.tsx"
        } else if prompt.contains("API") || prompt.contains("backend") {
            "Created Express API with endpoints: GET /todos, POST /todos, PUT /todos/:id, DELETE /todos/:id"
        } else if prompt.contains("test") {
            "Created test suites with Jest. Coverage: 85%"
        } else {
            "Task completed successfully"
        };

        Ok(response.to_string())
    }

  ./crates/ccswarm/src/session/claude_session.rs:57-74 method initialize <-> ./crates/ccswarm/src/session/claude_session.rs:124-140 method execute_prompt
  Similarity: 87.09%
  Classes: PersistentClaudeSession <-> PersistentClaudeSession

[36m--- ./crates/ccswarm/src/session/claude_session.rs:initialize (lines 57-74) ---[0m
    pub async fn initialize(&mut self) -> Result<()> {
        info!("🚀 Initializing session: {}", self.session_id);

        // Create working directory if needed
        tokio::fs::create_dir_all(&self.working_dir).await?;

        // Establish identity
        let identity_prompt = format!(
            "You are a {} agent. Your workspace is {}. Your specialization is in {}.",
            self.identity.specialization.name(),
            self.working_dir.display(),
            self.identity.specialization.name()
        );

        self.execute_prompt(&identity_prompt).await?;

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/claude_session.rs:execute_prompt (lines 124-140) ---[0m
    async fn execute_prompt(&self, prompt: &str) -> Result<String> {
        info!("🤖 Claude prompt: {}", prompt);

        // In real implementation, this would call Claude Code CLI
        // For now, simulate response based on task
        let response = if prompt.contains("React") || prompt.contains("frontend") {
            "Created React components with TypeScript. Files: App.tsx, TodoList.tsx, TodoItem.tsx"
        } else if prompt.contains("API") || prompt.contains("backend") {
            "Created Express API with endpoints: GET /todos, POST /todos, PUT /todos/:id, DELETE /todos/:id"
        } else if prompt.contains("test") {
            "Created test suites with Jest. Coverage: 85%"
        } else {
            "Task completed successfully"
        };

        Ok(response.to_string())
    }

  ./crates/ccswarm/src/session/claude_session.rs:102-121 method execute_command <-> ./crates/ccswarm/src/session/claude_session.rs:124-140 method execute_prompt
  Similarity: 77.41%
  Classes: PersistentClaudeSession <-> PersistentClaudeSession

[36m--- ./crates/ccswarm/src/session/claude_session.rs:execute_command (lines 102-121) ---[0m
    pub async fn execute_command(&mut self, command: &str) -> Result<String> {
        info!("🔧 Executing command: {}", command);

        // Execute shell command in working directory
        let output = Command::new("sh")
            .arg("-c")
            .arg(command)
            .current_dir(&self.working_dir)
            .output()
            .await?;

        let stdout = String::from_utf8_lossy(&output.stdout);
        let stderr = String::from_utf8_lossy(&output.stderr);

        if !output.status.success() {
            warn!("Command failed: {}", stderr);
        }

        Ok(format!("{}\n{}", stdout, stderr))
    }

[36m--- ./crates/ccswarm/src/session/claude_session.rs:execute_prompt (lines 124-140) ---[0m
    async fn execute_prompt(&self, prompt: &str) -> Result<String> {
        info!("🤖 Claude prompt: {}", prompt);

        // In real implementation, this would call Claude Code CLI
        // For now, simulate response based on task
        let response = if prompt.contains("React") || prompt.contains("frontend") {
            "Created React components with TypeScript. Files: App.tsx, TodoList.tsx, TodoItem.tsx"
        } else if prompt.contains("API") || prompt.contains("backend") {
            "Created Express API with endpoints: GET /todos, POST /todos, PUT /todos/:id, DELETE /todos/:id"
        } else if prompt.contains("test") {
            "Created test suites with Jest. Coverage: 85%"
        } else {
            "Task completed successfully"
        };

        Ok(response.to_string())
    }

  ./crates/ccswarm/src/session/claude_session.rs:124-140 method execute_prompt <-> ./crates/ccswarm/src/session/claude_session.rs:143-158 method generate_task_prompt
  Similarity: 74.75%
  Classes: PersistentClaudeSession <-> PersistentClaudeSession

[36m--- ./crates/ccswarm/src/session/claude_session.rs:execute_prompt (lines 124-140) ---[0m
    async fn execute_prompt(&self, prompt: &str) -> Result<String> {
        info!("🤖 Claude prompt: {}", prompt);

        // In real implementation, this would call Claude Code CLI
        // For now, simulate response based on task
        let response = if prompt.contains("React") || prompt.contains("frontend") {
            "Created React components with TypeScript. Files: App.tsx, TodoList.tsx, TodoItem.tsx"
        } else if prompt.contains("API") || prompt.contains("backend") {
            "Created Express API with endpoints: GET /todos, POST /todos, PUT /todos/:id, DELETE /todos/:id"
        } else if prompt.contains("test") {
            "Created test suites with Jest. Coverage: 85%"
        } else {
            "Task completed successfully"
        };

        Ok(response.to_string())
    }

[36m--- ./crates/ccswarm/src/session/claude_session.rs:generate_task_prompt (lines 143-158) ---[0m
    fn generate_task_prompt(&self, task: &Task) -> String {
        format!(
            "As a {} agent, please complete the following task:\n\n\
            Task: {}\n\
            Priority: {:?}\n\
            Type: {:?}\n\
            {}\n\n\
            Please implement this task according to best practices for {}.",
            self.identity.specialization.name(),
            task.description,
            task.priority,
            task.task_type,
            task.details.as_deref().unwrap_or(""),
            self.identity.specialization.name()
        )
    }


Duplicates in ./crates/ccswarm/src/agent/whiteboard.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/agent/whiteboard.rs:326-356 method summarize <-> ./crates/ccswarm/src/agent/whiteboard.rs:376-395 function test_whiteboard_calculation
  Similarity: 75.56%

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:summarize (lines 326-356) ---[0m
    pub fn summarize(&self) -> WhiteboardSummary {
        let mut type_counts = HashMap::new();
        let mut total_annotations = 0;
        let mut total_revisions = 0;

        for entry in self.entries.values() {
            let type_name = match &entry.entry_type {
                EntryType::Calculation { .. } => "calculations",
                EntryType::Diagram { .. } => "diagrams",
                EntryType::Note { .. } => "notes",
                EntryType::Hypothesis { .. } => "hypotheses",
                EntryType::TodoList { .. } => "todo_lists",
                EntryType::ComparisonTable { .. } => "comparisons",
                EntryType::ThoughtTrace { .. } => "thought_traces",
            };

            *type_counts.entry(type_name.to_string()).or_insert(0) += 1;
            total_annotations += entry.annotations.len();
            total_revisions += entry.revisions.len();
        }

        WhiteboardSummary {
            agent_id: self.agent_id.clone(),
            created_at: self.created_at,
            total_entries: self.entries.len(),
            type_counts,
            total_annotations,
            total_revisions,
            section_count: self.sections.len(),
        }
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:test_whiteboard_calculation (lines 376-395) ---[0m
    fn test_whiteboard_calculation() {
        let mut whiteboard = Whiteboard::new("test-agent".to_string());

        // 計算を追加
        let entry_id = whiteboard.add_calculation("317 × 456");
        assert_eq!(whiteboard.entries.len(), 1);

        // 結果を更新
        whiteboard.update_calculation_result(&entry_id, "144,552");

        let entry = &whiteboard.entries[&entry_id];
        if let EntryType::Calculation { expression, result } = &entry.entry_type {
            assert_eq!(expression, "317 × 456");
            assert_eq!(result.as_ref().unwrap(), "144,552");
        } else {
            panic!("Wrong entry type");
        }

        assert_eq!(entry.revisions.len(), 1);
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:159-177 method update_calculation_result <-> ./crates/ccswarm/src/agent/whiteboard.rs:238-256 method set_conclusion
  Similarity: 92.83%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:update_calculation_result (lines 159-177) ---[0m
    pub fn update_calculation_result(&mut self, entry_id: &str, result: &str) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        if let EntryType::Calculation {
            expression,
            result: ref mut res,
        } = &mut entry.entry_type
        {
            entry.revisions.push(Revision {
                timestamp: Utc::now(),
                description: format!("計算結果を追加: {}", result),
                previous_content: Some(expression.clone()),
            });
            *res = Some(result.to_string());
            Some(())
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:set_conclusion (lines 238-256) ---[0m
    pub fn set_conclusion(&mut self, entry_id: &str, conclusion: &str) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        if let EntryType::ThoughtTrace {
            conclusion: ref mut conc,
            ..
        } = &mut entry.entry_type
        {
            *conc = Some(conclusion.to_string());
            entry.revisions.push(Revision {
                timestamp: Utc::now(),
                description: "結論を設定".to_string(),
                previous_content: None,
            });
            Some(())
        } else {
            None
        }
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:159-177 method update_calculation_result <-> ./crates/ccswarm/src/agent/whiteboard.rs:197-215 method add_evidence
  Similarity: 92.17%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:update_calculation_result (lines 159-177) ---[0m
    pub fn update_calculation_result(&mut self, entry_id: &str, result: &str) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        if let EntryType::Calculation {
            expression,
            result: ref mut res,
        } = &mut entry.entry_type
        {
            entry.revisions.push(Revision {
                timestamp: Utc::now(),
                description: format!("計算結果を追加: {}", result),
                previous_content: Some(expression.clone()),
            });
            *res = Some(result.to_string());
            Some(())
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_evidence (lines 197-215) ---[0m
    pub fn add_evidence(&mut self, entry_id: &str, evidence: &str) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        if let EntryType::Hypothesis {
            evidence: ref mut ev,
            ..
        } = &mut entry.entry_type
        {
            ev.push(evidence.to_string());
            entry.revisions.push(Revision {
                timestamp: Utc::now(),
                description: format!("証拠を追加: {}", evidence),
                previous_content: None,
            });
            Some(())
        } else {
            None
        }
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:197-215 method add_evidence <-> ./crates/ccswarm/src/agent/whiteboard.rs:238-256 method set_conclusion
  Similarity: 91.74%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_evidence (lines 197-215) ---[0m
    pub fn add_evidence(&mut self, entry_id: &str, evidence: &str) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        if let EntryType::Hypothesis {
            evidence: ref mut ev,
            ..
        } = &mut entry.entry_type
        {
            ev.push(evidence.to_string());
            entry.revisions.push(Revision {
                timestamp: Utc::now(),
                description: format!("証拠を追加: {}", evidence),
                previous_content: None,
            });
            Some(())
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:set_conclusion (lines 238-256) ---[0m
    pub fn set_conclusion(&mut self, entry_id: &str, conclusion: &str) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        if let EntryType::ThoughtTrace {
            conclusion: ref mut conc,
            ..
        } = &mut entry.entry_type
        {
            *conc = Some(conclusion.to_string());
            entry.revisions.push(Revision {
                timestamp: Utc::now(),
                description: "結論を設定".to_string(),
                previous_content: None,
            });
            Some(())
        } else {
            None
        }
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:376-395 function test_whiteboard_calculation <-> ./crates/ccswarm/src/agent/whiteboard.rs:398-416 function test_thought_trace
  Similarity: 74.65%

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:test_whiteboard_calculation (lines 376-395) ---[0m
    fn test_whiteboard_calculation() {
        let mut whiteboard = Whiteboard::new("test-agent".to_string());

        // 計算を追加
        let entry_id = whiteboard.add_calculation("317 × 456");
        assert_eq!(whiteboard.entries.len(), 1);

        // 結果を更新
        whiteboard.update_calculation_result(&entry_id, "144,552");

        let entry = &whiteboard.entries[&entry_id];
        if let EntryType::Calculation { expression, result } = &entry.entry_type {
            assert_eq!(expression, "317 × 456");
            assert_eq!(result.as_ref().unwrap(), "144,552");
        } else {
            panic!("Wrong entry type");
        }

        assert_eq!(entry.revisions.len(), 1);
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:test_thought_trace (lines 398-416) ---[0m
    fn test_thought_trace() {
        let mut whiteboard = Whiteboard::new("test-agent".to_string());

        let trace_id = whiteboard.start_thought_trace();
        whiteboard.add_thought(&trace_id, "最初は単純な解法を考えた");
        whiteboard.add_thought(&trace_id, "でも、エッジケースで問題が発生");
        whiteboard.add_thought(&trace_id, "別のアプローチを試してみる");
        whiteboard.set_conclusion(&trace_id, "再帰的な解法が最適");

        let entry = &whiteboard.entries[&trace_id];
        if let EntryType::ThoughtTrace {
            thoughts,
            conclusion,
        } = &entry.entry_type
        {
            assert_eq!(thoughts.len(), 3);
            assert_eq!(conclusion.as_ref().unwrap(), "再帰的な解法が最適");
        }
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:134-148 method add_entry <-> ./crates/ccswarm/src/agent/whiteboard.rs:259-276 method annotate
  Similarity: 87.56%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_entry (lines 134-148) ---[0m
    pub fn add_entry(&mut self, entry_type: EntryType) -> String {
        let entry_id = Uuid::new_v4().to_string();
        let entry = WhiteboardEntry {
            id: entry_id.clone(),
            timestamp: Utc::now(),
            entry_type,
            annotations: Vec::new(),
            revisions: Vec::new(),
        };

        self.entries.insert(entry_id.clone(), entry);
        self.entry_order.push_back(entry_id.clone());

        entry_id
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:annotate (lines 259-276) ---[0m
    pub fn annotate(
        &mut self,
        entry_id: &str,
        content: &str,
        marker: AnnotationMarker,
    ) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        let annotation = Annotation {
            id: Uuid::new_v4().to_string(),
            timestamp: Utc::now(),
            content: content.to_string(),
            marker,
        };

        entry.annotations.push(annotation);
        Some(())
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:197-215 method add_evidence <-> ./crates/ccswarm/src/agent/whiteboard.rs:226-235 method add_thought
  Similarity: 94.74%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_evidence (lines 197-215) ---[0m
    pub fn add_evidence(&mut self, entry_id: &str, evidence: &str) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        if let EntryType::Hypothesis {
            evidence: ref mut ev,
            ..
        } = &mut entry.entry_type
        {
            ev.push(evidence.to_string());
            entry.revisions.push(Revision {
                timestamp: Utc::now(),
                description: format!("証拠を追加: {}", evidence),
                previous_content: None,
            });
            Some(())
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_thought (lines 226-235) ---[0m
    pub fn add_thought(&mut self, entry_id: &str, thought: &str) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        if let EntryType::ThoughtTrace { thoughts, .. } = &mut entry.entry_type {
            thoughts.push(thought.to_string());
            Some(())
        } else {
            None
        }
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:226-235 method add_thought <-> ./crates/ccswarm/src/agent/whiteboard.rs:238-256 method set_conclusion
  Similarity: 93.48%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_thought (lines 226-235) ---[0m
    pub fn add_thought(&mut self, entry_id: &str, thought: &str) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        if let EntryType::ThoughtTrace { thoughts, .. } = &mut entry.entry_type {
            thoughts.push(thought.to_string());
            Some(())
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:set_conclusion (lines 238-256) ---[0m
    pub fn set_conclusion(&mut self, entry_id: &str, conclusion: &str) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        if let EntryType::ThoughtTrace {
            conclusion: ref mut conc,
            ..
        } = &mut entry.entry_type
        {
            *conc = Some(conclusion.to_string());
            entry.revisions.push(Revision {
                timestamp: Utc::now(),
                description: "結論を設定".to_string(),
                previous_content: None,
            });
            Some(())
        } else {
            None
        }
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:159-177 method update_calculation_result <-> ./crates/ccswarm/src/agent/whiteboard.rs:226-235 method add_thought
  Similarity: 92.24%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:update_calculation_result (lines 159-177) ---[0m
    pub fn update_calculation_result(&mut self, entry_id: &str, result: &str) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        if let EntryType::Calculation {
            expression,
            result: ref mut res,
        } = &mut entry.entry_type
        {
            entry.revisions.push(Revision {
                timestamp: Utc::now(),
                description: format!("計算結果を追加: {}", result),
                previous_content: Some(expression.clone()),
            });
            *res = Some(result.to_string());
            Some(())
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_thought (lines 226-235) ---[0m
    pub fn add_thought(&mut self, entry_id: &str, thought: &str) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        if let EntryType::ThoughtTrace { thoughts, .. } = &mut entry.entry_type {
            thoughts.push(thought.to_string());
            Some(())
        } else {
            None
        }
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:159-177 method update_calculation_result <-> ./crates/ccswarm/src/agent/whiteboard.rs:259-276 method annotate
  Similarity: 71.97%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:update_calculation_result (lines 159-177) ---[0m
    pub fn update_calculation_result(&mut self, entry_id: &str, result: &str) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        if let EntryType::Calculation {
            expression,
            result: ref mut res,
        } = &mut entry.entry_type
        {
            entry.revisions.push(Revision {
                timestamp: Utc::now(),
                description: format!("計算結果を追加: {}", result),
                previous_content: Some(expression.clone()),
            });
            *res = Some(result.to_string());
            Some(())
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:annotate (lines 259-276) ---[0m
    pub fn annotate(
        &mut self,
        entry_id: &str,
        content: &str,
        marker: AnnotationMarker,
    ) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        let annotation = Annotation {
            id: Uuid::new_v4().to_string(),
            timestamp: Utc::now(),
            content: content.to_string(),
            marker,
        };

        entry.annotations.push(annotation);
        Some(())
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:259-276 method annotate <-> ./crates/ccswarm/src/agent/whiteboard.rs:279-290 method create_section
  Similarity: 86.89%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:annotate (lines 259-276) ---[0m
    pub fn annotate(
        &mut self,
        entry_id: &str,
        content: &str,
        marker: AnnotationMarker,
    ) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        let annotation = Annotation {
            id: Uuid::new_v4().to_string(),
            timestamp: Utc::now(),
            content: content.to_string(),
            marker,
        };

        entry.annotations.push(annotation);
        Some(())
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:create_section (lines 279-290) ---[0m
    pub fn create_section(&mut self, name: &str) -> String {
        let section_id = Uuid::new_v4().to_string();
        let section = Section {
            id: section_id.clone(),
            name: name.to_string(),
            entry_ids: Vec::new(),
            created_at: Utc::now(),
        };

        self.sections.insert(section_id.clone(), section);
        section_id
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:197-215 method add_evidence <-> ./crates/ccswarm/src/agent/whiteboard.rs:293-302 method add_to_section
  Similarity: 89.27%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_evidence (lines 197-215) ---[0m
    pub fn add_evidence(&mut self, entry_id: &str, evidence: &str) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        if let EntryType::Hypothesis {
            evidence: ref mut ev,
            ..
        } = &mut entry.entry_type
        {
            ev.push(evidence.to_string());
            entry.revisions.push(Revision {
                timestamp: Utc::now(),
                description: format!("証拠を追加: {}", evidence),
                previous_content: None,
            });
            Some(())
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_to_section (lines 293-302) ---[0m
    pub fn add_to_section(&mut self, section_id: &str, entry_id: &str) -> Option<()> {
        let section = self.sections.get_mut(section_id)?;
        if self.entries.contains_key(entry_id) && !section.entry_ids.contains(&entry_id.to_string())
        {
            section.entry_ids.push(entry_id.to_string());
            Some(())
        } else {
            None
        }
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:238-256 method set_conclusion <-> ./crates/ccswarm/src/agent/whiteboard.rs:293-302 method add_to_section
  Similarity: 88.62%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:set_conclusion (lines 238-256) ---[0m
    pub fn set_conclusion(&mut self, entry_id: &str, conclusion: &str) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        if let EntryType::ThoughtTrace {
            conclusion: ref mut conc,
            ..
        } = &mut entry.entry_type
        {
            *conc = Some(conclusion.to_string());
            entry.revisions.push(Revision {
                timestamp: Utc::now(),
                description: "結論を設定".to_string(),
                previous_content: None,
            });
            Some(())
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_to_section (lines 293-302) ---[0m
    pub fn add_to_section(&mut self, section_id: &str, entry_id: &str) -> Option<()> {
        let section = self.sections.get_mut(section_id)?;
        if self.entries.contains_key(entry_id) && !section.entry_ids.contains(&entry_id.to_string())
        {
            section.entry_ids.push(entry_id.to_string());
            Some(())
        } else {
            None
        }
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:159-177 method update_calculation_result <-> ./crates/ccswarm/src/agent/whiteboard.rs:293-302 method add_to_section
  Similarity: 88.16%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:update_calculation_result (lines 159-177) ---[0m
    pub fn update_calculation_result(&mut self, entry_id: &str, result: &str) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        if let EntryType::Calculation {
            expression,
            result: ref mut res,
        } = &mut entry.entry_type
        {
            entry.revisions.push(Revision {
                timestamp: Utc::now(),
                description: format!("計算結果を追加: {}", result),
                previous_content: Some(expression.clone()),
            });
            *res = Some(result.to_string());
            Some(())
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_to_section (lines 293-302) ---[0m
    pub fn add_to_section(&mut self, section_id: &str, entry_id: &str) -> Option<()> {
        let section = self.sections.get_mut(section_id)?;
        if self.entries.contains_key(entry_id) && !section.entry_ids.contains(&entry_id.to_string())
        {
            section.entry_ids.push(entry_id.to_string());
            Some(())
        } else {
            None
        }
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:134-148 method add_entry <-> ./crates/ccswarm/src/agent/whiteboard.rs:279-290 method create_section
  Similarity: 94.09%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_entry (lines 134-148) ---[0m
    pub fn add_entry(&mut self, entry_type: EntryType) -> String {
        let entry_id = Uuid::new_v4().to_string();
        let entry = WhiteboardEntry {
            id: entry_id.clone(),
            timestamp: Utc::now(),
            entry_type,
            annotations: Vec::new(),
            revisions: Vec::new(),
        };

        self.entries.insert(entry_id.clone(), entry);
        self.entry_order.push_back(entry_id.clone());

        entry_id
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:create_section (lines 279-290) ---[0m
    pub fn create_section(&mut self, name: &str) -> String {
        let section_id = Uuid::new_v4().to_string();
        let section = Section {
            id: section_id.clone(),
            name: name.to_string(),
            entry_ids: Vec::new(),
            created_at: Utc::now(),
        };

        self.sections.insert(section_id.clone(), section);
        section_id
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:376-395 function test_whiteboard_calculation <-> ./crates/ccswarm/src/agent/whiteboard.rs:419-432 function test_annotations
  Similarity: 74.00%

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:test_whiteboard_calculation (lines 376-395) ---[0m
    fn test_whiteboard_calculation() {
        let mut whiteboard = Whiteboard::new("test-agent".to_string());

        // 計算を追加
        let entry_id = whiteboard.add_calculation("317 × 456");
        assert_eq!(whiteboard.entries.len(), 1);

        // 結果を更新
        whiteboard.update_calculation_result(&entry_id, "144,552");

        let entry = &whiteboard.entries[&entry_id];
        if let EntryType::Calculation { expression, result } = &entry.entry_type {
            assert_eq!(expression, "317 × 456");
            assert_eq!(result.as_ref().unwrap(), "144,552");
        } else {
            panic!("Wrong entry type");
        }

        assert_eq!(entry.revisions.len(), 1);
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:test_annotations (lines 419-432) ---[0m
    fn test_annotations() {
        let mut whiteboard = Whiteboard::new("test-agent".to_string());

        let note_id = whiteboard.add_note("APIのレスポンスが遅い", vec!["performance".to_string()]);
        whiteboard.annotate(&note_id, "キャッシュを検討", AnnotationMarker::Important);
        whiteboard.annotate(
            &note_id,
            "本当に遅いのか測定が必要",
            AnnotationMarker::Verification,
        );

        let entry = &whiteboard.entries[&note_id];
        assert_eq!(entry.annotations.len(), 2);
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:134-148 method add_entry <-> ./crates/ccswarm/src/agent/whiteboard.rs:376-395 function test_whiteboard_calculation
  Similarity: 70.60%

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_entry (lines 134-148) ---[0m
    pub fn add_entry(&mut self, entry_type: EntryType) -> String {
        let entry_id = Uuid::new_v4().to_string();
        let entry = WhiteboardEntry {
            id: entry_id.clone(),
            timestamp: Utc::now(),
            entry_type,
            annotations: Vec::new(),
            revisions: Vec::new(),
        };

        self.entries.insert(entry_id.clone(), entry);
        self.entry_order.push_back(entry_id.clone());

        entry_id
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:test_whiteboard_calculation (lines 376-395) ---[0m
    fn test_whiteboard_calculation() {
        let mut whiteboard = Whiteboard::new("test-agent".to_string());

        // 計算を追加
        let entry_id = whiteboard.add_calculation("317 × 456");
        assert_eq!(whiteboard.entries.len(), 1);

        // 結果を更新
        whiteboard.update_calculation_result(&entry_id, "144,552");

        let entry = &whiteboard.entries[&entry_id];
        if let EntryType::Calculation { expression, result } = &entry.entry_type {
            assert_eq!(expression, "317 × 456");
            assert_eq!(result.as_ref().unwrap(), "144,552");
        } else {
            panic!("Wrong entry type");
        }

        assert_eq!(entry.revisions.len(), 1);
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:398-416 function test_thought_trace <-> ./crates/ccswarm/src/agent/whiteboard.rs:419-432 function test_annotations
  Similarity: 70.66%

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:test_thought_trace (lines 398-416) ---[0m
    fn test_thought_trace() {
        let mut whiteboard = Whiteboard::new("test-agent".to_string());

        let trace_id = whiteboard.start_thought_trace();
        whiteboard.add_thought(&trace_id, "最初は単純な解法を考えた");
        whiteboard.add_thought(&trace_id, "でも、エッジケースで問題が発生");
        whiteboard.add_thought(&trace_id, "別のアプローチを試してみる");
        whiteboard.set_conclusion(&trace_id, "再帰的な解法が最適");

        let entry = &whiteboard.entries[&trace_id];
        if let EntryType::ThoughtTrace {
            thoughts,
            conclusion,
        } = &entry.entry_type
        {
            assert_eq!(thoughts.len(), 3);
            assert_eq!(conclusion.as_ref().unwrap(), "再帰的な解法が最適");
        }
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:test_annotations (lines 419-432) ---[0m
    fn test_annotations() {
        let mut whiteboard = Whiteboard::new("test-agent".to_string());

        let note_id = whiteboard.add_note("APIのレスポンスが遅い", vec!["performance".to_string()]);
        whiteboard.annotate(&note_id, "キャッシュを検討", AnnotationMarker::Important);
        whiteboard.annotate(
            &note_id,
            "本当に遅いのか測定が必要",
            AnnotationMarker::Verification,
        );

        let entry = &whiteboard.entries[&note_id];
        assert_eq!(entry.annotations.len(), 2);
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:159-177 method update_calculation_result <-> ./crates/ccswarm/src/agent/whiteboard.rs:279-290 method create_section
  Similarity: 72.04%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:update_calculation_result (lines 159-177) ---[0m
    pub fn update_calculation_result(&mut self, entry_id: &str, result: &str) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        if let EntryType::Calculation {
            expression,
            result: ref mut res,
        } = &mut entry.entry_type
        {
            entry.revisions.push(Revision {
                timestamp: Utc::now(),
                description: format!("計算結果を追加: {}", result),
                previous_content: Some(expression.clone()),
            });
            *res = Some(result.to_string());
            Some(())
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:create_section (lines 279-290) ---[0m
    pub fn create_section(&mut self, name: &str) -> String {
        let section_id = Uuid::new_v4().to_string();
        let section = Section {
            id: section_id.clone(),
            name: name.to_string(),
            entry_ids: Vec::new(),
            created_at: Utc::now(),
        };

        self.sections.insert(section_id.clone(), section);
        section_id
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:226-235 method add_thought <-> ./crates/ccswarm/src/agent/whiteboard.rs:293-302 method add_to_section
  Similarity: 89.39%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_thought (lines 226-235) ---[0m
    pub fn add_thought(&mut self, entry_id: &str, thought: &str) -> Option<()> {
        let entry = self.entries.get_mut(entry_id)?;

        if let EntryType::ThoughtTrace { thoughts, .. } = &mut entry.entry_type {
            thoughts.push(thought.to_string());
            Some(())
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_to_section (lines 293-302) ---[0m
    pub fn add_to_section(&mut self, section_id: &str, entry_id: &str) -> Option<()> {
        let section = self.sections.get_mut(section_id)?;
        if self.entries.contains_key(entry_id) && !section.entry_ids.contains(&entry_id.to_string())
        {
            section.entry_ids.push(entry_id.to_string());
            Some(())
        } else {
            None
        }
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:123-131 method new <-> ./crates/ccswarm/src/agent/whiteboard.rs:315-323 method find_entries_by_type
  Similarity: 92.46%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:new (lines 123-131) ---[0m
    pub fn new(agent_id: String) -> Self {
        Self {
            agent_id,
            created_at: Utc::now(),
            entries: HashMap::new(),
            entry_order: VecDeque::new(),
            sections: HashMap::new(),
        }
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:find_entries_by_type (lines 315-323) ---[0m
    pub fn find_entries_by_type(
        &self,
        entry_type_filter: impl Fn(&EntryType) -> bool,
    ) -> Vec<&WhiteboardEntry> {
        self.entries
            .values()
            .filter(|entry| entry_type_filter(&entry.entry_type))
            .collect()
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:123-131 method new <-> ./crates/ccswarm/src/agent/whiteboard.rs:305-312 method recent_entries
  Similarity: 92.59%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:new (lines 123-131) ---[0m
    pub fn new(agent_id: String) -> Self {
        Self {
            agent_id,
            created_at: Utc::now(),
            entries: HashMap::new(),
            entry_order: VecDeque::new(),
            sections: HashMap::new(),
        }
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:recent_entries (lines 305-312) ---[0m
    pub fn recent_entries(&self, count: usize) -> Vec<&WhiteboardEntry> {
        self.entry_order
            .iter()
            .rev()
            .take(count)
            .filter_map(|id| self.entries.get(id))
            .collect()
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:188-194 method add_hypothesis <-> ./crates/ccswarm/src/agent/whiteboard.rs:315-323 method find_entries_by_type
  Similarity: 94.11%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_hypothesis (lines 188-194) ---[0m
    pub fn add_hypothesis(&mut self, statement: &str, confidence: f32) -> String {
        self.add_entry(EntryType::Hypothesis {
            statement: statement.to_string(),
            confidence: confidence.clamp(0.0, 1.0),
            evidence: Vec::new(),
        })
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:find_entries_by_type (lines 315-323) ---[0m
    pub fn find_entries_by_type(
        &self,
        entry_type_filter: impl Fn(&EntryType) -> bool,
    ) -> Vec<&WhiteboardEntry> {
        self.entries
            .values()
            .filter(|entry| entry_type_filter(&entry.entry_type))
            .collect()
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:305-312 method recent_entries <-> ./crates/ccswarm/src/agent/whiteboard.rs:315-323 method find_entries_by_type
  Similarity: 87.59%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:recent_entries (lines 305-312) ---[0m
    pub fn recent_entries(&self, count: usize) -> Vec<&WhiteboardEntry> {
        self.entry_order
            .iter()
            .rev()
            .take(count)
            .filter_map(|id| self.entries.get(id))
            .collect()
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:find_entries_by_type (lines 315-323) ---[0m
    pub fn find_entries_by_type(
        &self,
        entry_type_filter: impl Fn(&EntryType) -> bool,
    ) -> Vec<&WhiteboardEntry> {
        self.entries
            .values()
            .filter(|entry| entry_type_filter(&entry.entry_type))
            .collect()
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:188-194 method add_hypothesis <-> ./crates/ccswarm/src/agent/whiteboard.rs:305-312 method recent_entries
  Similarity: 94.31%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_hypothesis (lines 188-194) ---[0m
    pub fn add_hypothesis(&mut self, statement: &str, confidence: f32) -> String {
        self.add_entry(EntryType::Hypothesis {
            statement: statement.to_string(),
            confidence: confidence.clamp(0.0, 1.0),
            evidence: Vec::new(),
        })
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:recent_entries (lines 305-312) ---[0m
    pub fn recent_entries(&self, count: usize) -> Vec<&WhiteboardEntry> {
        self.entry_order
            .iter()
            .rev()
            .take(count)
            .filter_map(|id| self.entries.get(id))
            .collect()
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:151-156 method add_calculation <-> ./crates/ccswarm/src/agent/whiteboard.rs:315-323 method find_entries_by_type
  Similarity: 92.14%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_calculation (lines 151-156) ---[0m
    pub fn add_calculation(&mut self, expression: &str) -> String {
        self.add_entry(EntryType::Calculation {
            expression: expression.to_string(),
            result: None,
        })
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:find_entries_by_type (lines 315-323) ---[0m
    pub fn find_entries_by_type(
        &self,
        entry_type_filter: impl Fn(&EntryType) -> bool,
    ) -> Vec<&WhiteboardEntry> {
        self.entries
            .values()
            .filter(|entry| entry_type_filter(&entry.entry_type))
            .collect()
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:180-185 method add_note <-> ./crates/ccswarm/src/agent/whiteboard.rs:315-323 method find_entries_by_type
  Similarity: 92.14%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_note (lines 180-185) ---[0m
    pub fn add_note(&mut self, content: &str, tags: Vec<String>) -> String {
        self.add_entry(EntryType::Note {
            content: content.to_string(),
            tags,
        })
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:find_entries_by_type (lines 315-323) ---[0m
    pub fn find_entries_by_type(
        &self,
        entry_type_filter: impl Fn(&EntryType) -> bool,
    ) -> Vec<&WhiteboardEntry> {
        self.entries
            .values()
            .filter(|entry| entry_type_filter(&entry.entry_type))
            .collect()
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:218-223 method start_thought_trace <-> ./crates/ccswarm/src/agent/whiteboard.rs:315-323 method find_entries_by_type
  Similarity: 92.14%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:start_thought_trace (lines 218-223) ---[0m
    pub fn start_thought_trace(&mut self) -> String {
        self.add_entry(EntryType::ThoughtTrace {
            thoughts: Vec::new(),
            conclusion: None,
        })
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:find_entries_by_type (lines 315-323) ---[0m
    pub fn find_entries_by_type(
        &self,
        entry_type_filter: impl Fn(&EntryType) -> bool,
    ) -> Vec<&WhiteboardEntry> {
        self.entries
            .values()
            .filter(|entry| entry_type_filter(&entry.entry_type))
            .collect()
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:151-156 method add_calculation <-> ./crates/ccswarm/src/agent/whiteboard.rs:305-312 method recent_entries
  Similarity: 94.31%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_calculation (lines 151-156) ---[0m
    pub fn add_calculation(&mut self, expression: &str) -> String {
        self.add_entry(EntryType::Calculation {
            expression: expression.to_string(),
            result: None,
        })
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:recent_entries (lines 305-312) ---[0m
    pub fn recent_entries(&self, count: usize) -> Vec<&WhiteboardEntry> {
        self.entry_order
            .iter()
            .rev()
            .take(count)
            .filter_map(|id| self.entries.get(id))
            .collect()
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:180-185 method add_note <-> ./crates/ccswarm/src/agent/whiteboard.rs:305-312 method recent_entries
  Similarity: 94.31%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_note (lines 180-185) ---[0m
    pub fn add_note(&mut self, content: &str, tags: Vec<String>) -> String {
        self.add_entry(EntryType::Note {
            content: content.to_string(),
            tags,
        })
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:recent_entries (lines 305-312) ---[0m
    pub fn recent_entries(&self, count: usize) -> Vec<&WhiteboardEntry> {
        self.entry_order
            .iter()
            .rev()
            .take(count)
            .filter_map(|id| self.entries.get(id))
            .collect()
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:218-223 method start_thought_trace <-> ./crates/ccswarm/src/agent/whiteboard.rs:305-312 method recent_entries
  Similarity: 94.31%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:start_thought_trace (lines 218-223) ---[0m
    pub fn start_thought_trace(&mut self) -> String {
        self.add_entry(EntryType::ThoughtTrace {
            thoughts: Vec::new(),
            conclusion: None,
        })
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:recent_entries (lines 305-312) ---[0m
    pub fn recent_entries(&self, count: usize) -> Vec<&WhiteboardEntry> {
        self.entry_order
            .iter()
            .rev()
            .take(count)
            .filter_map(|id| self.entries.get(id))
            .collect()
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:151-156 method add_calculation <-> ./crates/ccswarm/src/agent/whiteboard.rs:188-194 method add_hypothesis
  Similarity: 92.86%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_calculation (lines 151-156) ---[0m
    pub fn add_calculation(&mut self, expression: &str) -> String {
        self.add_entry(EntryType::Calculation {
            expression: expression.to_string(),
            result: None,
        })
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_hypothesis (lines 188-194) ---[0m
    pub fn add_hypothesis(&mut self, statement: &str, confidence: f32) -> String {
        self.add_entry(EntryType::Hypothesis {
            statement: statement.to_string(),
            confidence: confidence.clamp(0.0, 1.0),
            evidence: Vec::new(),
        })
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:180-185 method add_note <-> ./crates/ccswarm/src/agent/whiteboard.rs:188-194 method add_hypothesis
  Similarity: 92.86%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_note (lines 180-185) ---[0m
    pub fn add_note(&mut self, content: &str, tags: Vec<String>) -> String {
        self.add_entry(EntryType::Note {
            content: content.to_string(),
            tags,
        })
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_hypothesis (lines 188-194) ---[0m
    pub fn add_hypothesis(&mut self, statement: &str, confidence: f32) -> String {
        self.add_entry(EntryType::Hypothesis {
            statement: statement.to_string(),
            confidence: confidence.clamp(0.0, 1.0),
            evidence: Vec::new(),
        })
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:188-194 method add_hypothesis <-> ./crates/ccswarm/src/agent/whiteboard.rs:218-223 method start_thought_trace
  Similarity: 92.86%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_hypothesis (lines 188-194) ---[0m
    pub fn add_hypothesis(&mut self, statement: &str, confidence: f32) -> String {
        self.add_entry(EntryType::Hypothesis {
            statement: statement.to_string(),
            confidence: confidence.clamp(0.0, 1.0),
            evidence: Vec::new(),
        })
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:start_thought_trace (lines 218-223) ---[0m
    pub fn start_thought_trace(&mut self) -> String {
        self.add_entry(EntryType::ThoughtTrace {
            thoughts: Vec::new(),
            conclusion: None,
        })
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:151-156 method add_calculation <-> ./crates/ccswarm/src/agent/whiteboard.rs:218-223 method start_thought_trace
  Similarity: 93.82%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_calculation (lines 151-156) ---[0m
    pub fn add_calculation(&mut self, expression: &str) -> String {
        self.add_entry(EntryType::Calculation {
            expression: expression.to_string(),
            result: None,
        })
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:start_thought_trace (lines 218-223) ---[0m
    pub fn start_thought_trace(&mut self) -> String {
        self.add_entry(EntryType::ThoughtTrace {
            thoughts: Vec::new(),
            conclusion: None,
        })
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:151-156 method add_calculation <-> ./crates/ccswarm/src/agent/whiteboard.rs:180-185 method add_note
  Similarity: 91.47%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_calculation (lines 151-156) ---[0m
    pub fn add_calculation(&mut self, expression: &str) -> String {
        self.add_entry(EntryType::Calculation {
            expression: expression.to_string(),
            result: None,
        })
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_note (lines 180-185) ---[0m
    pub fn add_note(&mut self, content: &str, tags: Vec<String>) -> String {
        self.add_entry(EntryType::Note {
            content: content.to_string(),
            tags,
        })
    }

  ./crates/ccswarm/src/agent/whiteboard.rs:180-185 method add_note <-> ./crates/ccswarm/src/agent/whiteboard.rs:218-223 method start_thought_trace
  Similarity: 88.82%
  Classes: Whiteboard <-> Whiteboard

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:add_note (lines 180-185) ---[0m
    pub fn add_note(&mut self, content: &str, tags: Vec<String>) -> String {
        self.add_entry(EntryType::Note {
            content: content.to_string(),
            tags,
        })
    }

[36m--- ./crates/ccswarm/src/agent/whiteboard.rs:start_thought_trace (lines 218-223) ---[0m
    pub fn start_thought_trace(&mut self) -> String {
        self.add_entry(EntryType::ThoughtTrace {
            thoughts: Vec::new(),
            conclusion: None,
        })
    }


Duplicates in ./crates/ccswarm/src/tui/ui.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/tui/ui.rs:432-585 function draw_agents <-> ./crates/ccswarm/src/tui/ui.rs:1017-1157 function draw_delegation_interface
  Similarity: 95.83%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_agents (lines 432-585) ---[0m
fn draw_agents(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(65), Constraint::Percentage(35)])
        .split(area);

    // Create table rows for better formatting
    let header = Row::new(vec![
        Cell::from("#").style(Style::default().fg(Color::DarkGray)),
        Cell::from("Name").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Provider").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Type").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Status").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = app
        .agents
        .iter()
        .enumerate()
        .map(|(i, agent)| {
            let (status_color, status_text, status_icon) = match agent.status {
                crate::agent::AgentStatus::Available => (Color::Green, "Available", "✅"),
                crate::agent::AgentStatus::Working => (Color::Cyan, "Working", "⚙️"),
                crate::agent::AgentStatus::Error(_) => (Color::Red, "Error", "❌"),
                crate::agent::AgentStatus::Initializing => (Color::Yellow, "Initializing", "🔄"),
                crate::agent::AgentStatus::WaitingForReview => (Color::Magenta, "Waiting", "⏳"),
                crate::agent::AgentStatus::ShuttingDown => (Color::DarkGray, "Shutting Down", "⏹️"),
            };

            // Special styling for Master Claude Code
            let is_master = agent.specialization.contains("Master");
            let (name_color, name_icon, type_color) = if is_master {
                (Color::Yellow, "👑", Color::Yellow)
            } else {
                let type_icon = match agent.specialization.as_str() {
                    s if s.contains("Frontend") => "🎨",
                    s if s.contains("Backend") => "⚙️",
                    s if s.contains("DevOps") => "🔧",
                    s if s.contains("QA") => "🧪",
                    s if s.contains("Test") => "🔬",
                    s if s.contains("Error") => "🚨",
                    _ => "🤖",
                };
                (Color::Cyan, type_icon, Color::White)
            };

            let provider_display = if is_master {
                format!("{} claude_code", "👑")
            } else {
                format!("{} claude_code", "🤖")
            };

            Row::new(vec![
                Cell::from(format!("{}", i + 1)).style(Style::default().fg(Color::DarkGray)),
                Cell::from(format!("{} {}", name_icon, agent.name)).style(
                    Style::default().fg(name_color).add_modifier(if is_master {
                        Modifier::BOLD
                    } else {
                        Modifier::empty()
                    }),
                ),
                Cell::from(provider_display).style(Style::default().fg(if is_master {
                    Color::Yellow
                } else {
                    Color::Blue
                })),
                Cell::from(agent.specialization.clone()).style(Style::default().fg(type_color)),
                Cell::from(format!("{} {}", status_icon, status_text)).style(
                    Style::default()
                        .fg(status_color)
                        .add_modifier(Modifier::BOLD),
                ),
            ])
        })
        .collect();

    let agents_table = Table::new(
        rows,
        [
            Constraint::Length(3),  // #
            Constraint::Length(18), // Name
            Constraint::Length(16), // Provider
            Constraint::Length(20), // Type
            Constraint::Length(15), // Status
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 🤖 Agent Management Dashboard ")
            .title_alignment(Alignment::Center)
            .style(Style::default().fg(Color::White)),
    )
    .row_highlight_style(
        Style::default()
            .bg(Color::Blue)
            .fg(Color::White)
            .add_modifier(Modifier::BOLD),
    )
    .highlight_symbol("► ");

    // Create table state
    let mut table_state = ratatui::widgets::TableState::default();
    table_state.select(Some(app.selected_agent));

    f.render_stateful_widget(agents_table, chunks[0], &mut table_state);

    // Agent details
    if let Some(agent) = app.agents.get(app.selected_agent) {
        let details = vec![
            format!("ID: {}", agent.id),
            format!("Name: {}", agent.name),
            format!("Type: {}", agent.specialization),
            format!("Provider: {} {}", agent.provider_icon, agent.provider_type),
            format!("Status: {:?}", agent.status),
            format!("Tasks Completed: {}", agent.tasks_completed),
            format!("Last Activity: {}", agent.last_activity.format("%H:%M:%S")),
            format!("Workspace: {}", agent.workspace),
            format!(
                "Current Task: {}",
                agent.current_task.as_deref().unwrap_or("None")
            ),
        ];

        let details_text = details.join("\n");
        let details_paragraph = Paragraph::new(details_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .title(" Agent Details "),
            )
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_interface (lines 1017-1157) ---[0m
fn draw_delegation_interface(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(40), Constraint::Percentage(60)])
        .split(area);

    // Available agents
    let agent_items: Vec<ListItem> = vec![
        ListItem::new(Line::from(vec![
            Span::styled("🎨 ", Style::default().fg(Color::Cyan)),
            Span::styled(
                "Frontend",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - UI/UX Development", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("⚙️ ", Style::default().fg(Color::Green)),
            Span::styled(
                "Backend",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - API/Database Work", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("🔧 ", Style::default().fg(Color::Blue)),
            Span::styled(
                "DevOps",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - Infrastructure/Deploy", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("🧪 ", Style::default().fg(Color::Magenta)),
            Span::styled(
                "QA",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - Testing/Quality", Style::default().fg(Color::Gray)),
        ])),
    ];

    let agents_list = List::new(agent_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🤖 Available Agents ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Green)
                .add_modifier(Modifier::BOLD),
        );

    f.render_widget(agents_list, chunks[0]);

    // Delegation form and recent delegations
    let right_chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(8), Constraint::Min(0)])
        .split(chunks[1]);

    // Delegation form
    let form_text = "🎯 Manual Task Delegation\n\n\
                     Format: <agent> <task_description>\n\n\
                     Examples:\n\
                     • frontend Create user dashboard\n\
                     • backend Add payment processing\n\
                     • qa Write end-to-end tests\n\
                     • devops Setup CI/CD pipeline";

    let form_paragraph = Paragraph::new(form_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📝 Delegation Form ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(form_paragraph, right_chunks[0]);

    // Recent delegations
    let delegation_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(6)
        .filter(|d| d.reasoning.contains("Manual delegation"))
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "frontend" | "Frontend" => "🎨",
                "backend" | "Backend" => "⚙️",
                "devops" | "DevOps" => "🔧",
                "qa" | "QA" => "🧪",
                _ => "🤖",
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::Green)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("{} ", decision.created_at.format("%H:%M")),
                    Style::default().fg(Color::DarkGray),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::White),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let delegations_list = List::new(delegation_items).block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 📋 Recent Delegations ")
            .title_alignment(Alignment::Center),
    );

    f.render_widget(delegations_list, right_chunks[1]);
}

  ./crates/ccswarm/src/tui/ui.rs:298-429 function draw_enhanced_agent_summary <-> ./crates/ccswarm/src/tui/ui.rs:432-585 function draw_agents
  Similarity: 97.38%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_agent_summary (lines 298-429) ---[0m
fn draw_enhanced_agent_summary(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(60), Constraint::Percentage(40)])
        .split(area);

    // Enhanced agents table
    let header = Row::new(vec![
        Cell::from("Agent").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Type").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Status").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Tasks").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = app
        .agents
        .iter()
        .take(6)
        .map(|agent| {
            let (status_color, status_text, status_icon) = match agent.status {
                crate::agent::AgentStatus::Available => (Color::Green, "Available", "✅"),
                crate::agent::AgentStatus::Working => (Color::Cyan, "Working", "⚙️"),
                crate::agent::AgentStatus::Error(_) => (Color::Red, "Error", "❌"),
                crate::agent::AgentStatus::Initializing => (Color::Yellow, "Init", "🔄"),
                crate::agent::AgentStatus::WaitingForReview => (Color::Magenta, "Review", "⏳"),
                crate::agent::AgentStatus::ShuttingDown => (Color::DarkGray, "Shutdown", "⏹️"),
            };

            let is_master = agent.specialization.contains("Master");
            let (name_icon, name_color) = if is_master {
                ("👑", Color::Yellow)
            } else {
                match agent.specialization.as_str() {
                    s if s.contains("Frontend") => ("🎨", Color::Cyan),
                    s if s.contains("Backend") => ("⚙️", Color::Green),
                    s if s.contains("DevOps") => ("🔧", Color::Blue),
                    s if s.contains("QA") => ("🧪", Color::Magenta),
                    _ => ("🤖", Color::White),
                }
            };

            Row::new(vec![
                Cell::from(format!("{} {}", name_icon, agent.name)).style(
                    Style::default().fg(name_color).add_modifier(if is_master {
                        Modifier::BOLD
                    } else {
                        Modifier::empty()
                    }),
                ),
                Cell::from(agent.specialization.clone()).style(Style::default().fg(Color::White)),
                Cell::from(format!("{} {}", status_icon, status_text))
                    .style(Style::default().fg(status_color)),
                Cell::from(format!("{}", agent.tasks_completed))
                    .style(Style::default().fg(Color::Yellow)),
            ])
        })
        .collect();

    let agents_table = Table::new(
        rows,
        [
            Constraint::Length(15), // Agent
            Constraint::Length(12), // Type
            Constraint::Length(12), // Status
            Constraint::Length(6),  // Tasks
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 👥 Agents Overview ")
            .title_alignment(Alignment::Center),
    )
    .row_highlight_style(
        Style::default()
            .bg(Color::DarkGray)
            .add_modifier(Modifier::BOLD),
    );

    f.render_widget(agents_table, chunks[0]);

    // System activity summary
    let activity_text = format!(
        "🔄 System Activity\n\n\
         ⏱️  Uptime: Active\n\
         📊 Total Agents: {}\n\
         🎯 Active: {}\n\
         📋 Tasks Queued: {}\n\
         ✅ Completed: {}\n\n\
         📈 Efficiency: {:.1}%",
        app.total_agents,
        app.active_agents,
        app.pending_tasks,
        app.completed_tasks,
        if app.total_agents > 0 {
            (app.active_agents as f32 / app.total_agents as f32) * 100.0
        } else {
            0.0
        }
    );

    let activity_paragraph = Paragraph::new(activity_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📊 System Metrics ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::Cyan))
        .wrap(Wrap { trim: true });

    f.render_widget(activity_paragraph, chunks[1]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_agents (lines 432-585) ---[0m
fn draw_agents(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(65), Constraint::Percentage(35)])
        .split(area);

    // Create table rows for better formatting
    let header = Row::new(vec![
        Cell::from("#").style(Style::default().fg(Color::DarkGray)),
        Cell::from("Name").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Provider").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Type").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Status").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = app
        .agents
        .iter()
        .enumerate()
        .map(|(i, agent)| {
            let (status_color, status_text, status_icon) = match agent.status {
                crate::agent::AgentStatus::Available => (Color::Green, "Available", "✅"),
                crate::agent::AgentStatus::Working => (Color::Cyan, "Working", "⚙️"),
                crate::agent::AgentStatus::Error(_) => (Color::Red, "Error", "❌"),
                crate::agent::AgentStatus::Initializing => (Color::Yellow, "Initializing", "🔄"),
                crate::agent::AgentStatus::WaitingForReview => (Color::Magenta, "Waiting", "⏳"),
                crate::agent::AgentStatus::ShuttingDown => (Color::DarkGray, "Shutting Down", "⏹️"),
            };

            // Special styling for Master Claude Code
            let is_master = agent.specialization.contains("Master");
            let (name_color, name_icon, type_color) = if is_master {
                (Color::Yellow, "👑", Color::Yellow)
            } else {
                let type_icon = match agent.specialization.as_str() {
                    s if s.contains("Frontend") => "🎨",
                    s if s.contains("Backend") => "⚙️",
                    s if s.contains("DevOps") => "🔧",
                    s if s.contains("QA") => "🧪",
                    s if s.contains("Test") => "🔬",
                    s if s.contains("Error") => "🚨",
                    _ => "🤖",
                };
                (Color::Cyan, type_icon, Color::White)
            };

            let provider_display = if is_master {
                format!("{} claude_code", "👑")
            } else {
                format!("{} claude_code", "🤖")
            };

            Row::new(vec![
                Cell::from(format!("{}", i + 1)).style(Style::default().fg(Color::DarkGray)),
                Cell::from(format!("{} {}", name_icon, agent.name)).style(
                    Style::default().fg(name_color).add_modifier(if is_master {
                        Modifier::BOLD
                    } else {
                        Modifier::empty()
                    }),
                ),
                Cell::from(provider_display).style(Style::default().fg(if is_master {
                    Color::Yellow
                } else {
                    Color::Blue
                })),
                Cell::from(agent.specialization.clone()).style(Style::default().fg(type_color)),
                Cell::from(format!("{} {}", status_icon, status_text)).style(
                    Style::default()
                        .fg(status_color)
                        .add_modifier(Modifier::BOLD),
                ),
            ])
        })
        .collect();

    let agents_table = Table::new(
        rows,
        [
            Constraint::Length(3),  // #
            Constraint::Length(18), // Name
            Constraint::Length(16), // Provider
            Constraint::Length(20), // Type
            Constraint::Length(15), // Status
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 🤖 Agent Management Dashboard ")
            .title_alignment(Alignment::Center)
            .style(Style::default().fg(Color::White)),
    )
    .row_highlight_style(
        Style::default()
            .bg(Color::Blue)
            .fg(Color::White)
            .add_modifier(Modifier::BOLD),
    )
    .highlight_symbol("► ");

    // Create table state
    let mut table_state = ratatui::widgets::TableState::default();
    table_state.select(Some(app.selected_agent));

    f.render_stateful_widget(agents_table, chunks[0], &mut table_state);

    // Agent details
    if let Some(agent) = app.agents.get(app.selected_agent) {
        let details = vec![
            format!("ID: {}", agent.id),
            format!("Name: {}", agent.name),
            format!("Type: {}", agent.specialization),
            format!("Provider: {} {}", agent.provider_icon, agent.provider_type),
            format!("Status: {:?}", agent.status),
            format!("Tasks Completed: {}", agent.tasks_completed),
            format!("Last Activity: {}", agent.last_activity.format("%H:%M:%S")),
            format!("Workspace: {}", agent.workspace),
            format!(
                "Current Task: {}",
                agent.current_task.as_deref().unwrap_or("None")
            ),
        ];

        let details_text = details.join("\n");
        let details_paragraph = Paragraph::new(details_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .title(" Agent Details "),
            )
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    }
}

  ./crates/ccswarm/src/tui/ui.rs:432-585 function draw_agents <-> ./crates/ccswarm/src/tui/ui.rs:1160-1303 function draw_delegation_stats
  Similarity: 92.77%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_agents (lines 432-585) ---[0m
fn draw_agents(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(65), Constraint::Percentage(35)])
        .split(area);

    // Create table rows for better formatting
    let header = Row::new(vec![
        Cell::from("#").style(Style::default().fg(Color::DarkGray)),
        Cell::from("Name").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Provider").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Type").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Status").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = app
        .agents
        .iter()
        .enumerate()
        .map(|(i, agent)| {
            let (status_color, status_text, status_icon) = match agent.status {
                crate::agent::AgentStatus::Available => (Color::Green, "Available", "✅"),
                crate::agent::AgentStatus::Working => (Color::Cyan, "Working", "⚙️"),
                crate::agent::AgentStatus::Error(_) => (Color::Red, "Error", "❌"),
                crate::agent::AgentStatus::Initializing => (Color::Yellow, "Initializing", "🔄"),
                crate::agent::AgentStatus::WaitingForReview => (Color::Magenta, "Waiting", "⏳"),
                crate::agent::AgentStatus::ShuttingDown => (Color::DarkGray, "Shutting Down", "⏹️"),
            };

            // Special styling for Master Claude Code
            let is_master = agent.specialization.contains("Master");
            let (name_color, name_icon, type_color) = if is_master {
                (Color::Yellow, "👑", Color::Yellow)
            } else {
                let type_icon = match agent.specialization.as_str() {
                    s if s.contains("Frontend") => "🎨",
                    s if s.contains("Backend") => "⚙️",
                    s if s.contains("DevOps") => "🔧",
                    s if s.contains("QA") => "🧪",
                    s if s.contains("Test") => "🔬",
                    s if s.contains("Error") => "🚨",
                    _ => "🤖",
                };
                (Color::Cyan, type_icon, Color::White)
            };

            let provider_display = if is_master {
                format!("{} claude_code", "👑")
            } else {
                format!("{} claude_code", "🤖")
            };

            Row::new(vec![
                Cell::from(format!("{}", i + 1)).style(Style::default().fg(Color::DarkGray)),
                Cell::from(format!("{} {}", name_icon, agent.name)).style(
                    Style::default().fg(name_color).add_modifier(if is_master {
                        Modifier::BOLD
                    } else {
                        Modifier::empty()
                    }),
                ),
                Cell::from(provider_display).style(Style::default().fg(if is_master {
                    Color::Yellow
                } else {
                    Color::Blue
                })),
                Cell::from(agent.specialization.clone()).style(Style::default().fg(type_color)),
                Cell::from(format!("{} {}", status_icon, status_text)).style(
                    Style::default()
                        .fg(status_color)
                        .add_modifier(Modifier::BOLD),
                ),
            ])
        })
        .collect();

    let agents_table = Table::new(
        rows,
        [
            Constraint::Length(3),  // #
            Constraint::Length(18), // Name
            Constraint::Length(16), // Provider
            Constraint::Length(20), // Type
            Constraint::Length(15), // Status
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 🤖 Agent Management Dashboard ")
            .title_alignment(Alignment::Center)
            .style(Style::default().fg(Color::White)),
    )
    .row_highlight_style(
        Style::default()
            .bg(Color::Blue)
            .fg(Color::White)
            .add_modifier(Modifier::BOLD),
    )
    .highlight_symbol("► ");

    // Create table state
    let mut table_state = ratatui::widgets::TableState::default();
    table_state.select(Some(app.selected_agent));

    f.render_stateful_widget(agents_table, chunks[0], &mut table_state);

    // Agent details
    if let Some(agent) = app.agents.get(app.selected_agent) {
        let details = vec![
            format!("ID: {}", agent.id),
            format!("Name: {}", agent.name),
            format!("Type: {}", agent.specialization),
            format!("Provider: {} {}", agent.provider_icon, agent.provider_type),
            format!("Status: {:?}", agent.status),
            format!("Tasks Completed: {}", agent.tasks_completed),
            format!("Last Activity: {}", agent.last_activity.format("%H:%M:%S")),
            format!("Workspace: {}", agent.workspace),
            format!(
                "Current Task: {}",
                agent.current_task.as_deref().unwrap_or("None")
            ),
        ];

        let details_text = details.join("\n");
        let details_paragraph = Paragraph::new(details_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .title(" Agent Details "),
            )
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_stats (lines 1160-1303) ---[0m
fn draw_delegation_stats(f: &mut Frame, area: Rect, app: &App) {
    if app.delegation_decisions.is_empty() {
        let empty_text = "📊 Delegation Statistics\n\n\
                         No delegation data available yet.\n\n\
                         Start analyzing or delegating tasks\n\
                         to see statistics here.";

        let empty_paragraph = Paragraph::new(empty_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📊 Statistics ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::Gray))
            .alignment(Alignment::Center)
            .wrap(Wrap { trim: true });

        f.render_widget(empty_paragraph, area);
        return;
    }

    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Agent distribution chart
    let mut agent_counts = std::collections::HashMap::new();
    let mut total_confidence = 0.0;

    for decision in &app.delegation_decisions {
        *agent_counts
            .entry(decision.recommended_agent.clone())
            .or_insert(0) += 1;
        total_confidence += decision.confidence;
    }

    let total = app.delegation_decisions.len();
    let avg_confidence = total_confidence / total as f64;

    // Create agent distribution table
    let header = Row::new(vec![
        Cell::from("Agent").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Count").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Percentage").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = agent_counts
        .iter()
        .map(|(agent, count)| {
            let percentage = (*count as f64 / total as f64) * 100.0;
            let agent_icon = match agent.as_str() {
                "Frontend" | "frontend" => "🎨",
                "Backend" | "backend" => "⚙️",
                "DevOps" | "devops" => "🔧",
                "QA" | "qa" => "🧪",
                _ => "🤖",
            };

            Row::new(vec![
                Cell::from(format!("{} {}", agent_icon, agent)).style(
                    Style::default()
                        .fg(Color::Cyan)
                        .add_modifier(Modifier::BOLD),
                ),
                Cell::from(format!("{}", count)).style(Style::default().fg(Color::Yellow)),
                Cell::from(format!("{:.1}%", percentage)).style(Style::default().fg(Color::Green)),
            ])
        })
        .collect();

    let distribution_table = Table::new(
        rows,
        [
            Constraint::Length(15), // Agent
            Constraint::Length(8),  // Count
            Constraint::Length(12), // Percentage
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 📊 Agent Distribution ")
            .title_alignment(Alignment::Center),
    );

    f.render_widget(distribution_table, chunks[0]);

    // Statistics summary
    let stats_text = format!(
        "📈 Delegation Summary\n\n\
         Total Delegations: {}\n\
         Average Confidence: {:.1}%\n\n\
         📊 Distribution:\n\
         {}\n\n\
         🎯 Most Delegated: {}\n\
         📅 Latest: {}",
        total,
        avg_confidence * 100.0,
        agent_counts
            .iter()
            .map(|(agent, count)| format!("• {}: {}", agent, count))
            .collect::<Vec<_>>()
            .join("\n         "),
        agent_counts
            .iter()
            .max_by_key(|(_, count)| *count)
            .map(|(agent, _)| agent.as_str())
            .unwrap_or("None"),
        app.delegation_decisions
            .last()
            .map(|d| d.created_at.format("%H:%M:%S").to_string())
            .unwrap_or("None".to_string())
    );

    let stats_paragraph = Paragraph::new(stats_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📋 Statistics Summary ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(stats_paragraph, chunks[1]);
}

  ./crates/ccswarm/src/tui/ui.rs:1017-1157 function draw_delegation_interface <-> ./crates/ccswarm/src/tui/ui.rs:1160-1303 function draw_delegation_stats
  Similarity: 94.95%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_interface (lines 1017-1157) ---[0m
fn draw_delegation_interface(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(40), Constraint::Percentage(60)])
        .split(area);

    // Available agents
    let agent_items: Vec<ListItem> = vec![
        ListItem::new(Line::from(vec![
            Span::styled("🎨 ", Style::default().fg(Color::Cyan)),
            Span::styled(
                "Frontend",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - UI/UX Development", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("⚙️ ", Style::default().fg(Color::Green)),
            Span::styled(
                "Backend",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - API/Database Work", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("🔧 ", Style::default().fg(Color::Blue)),
            Span::styled(
                "DevOps",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - Infrastructure/Deploy", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("🧪 ", Style::default().fg(Color::Magenta)),
            Span::styled(
                "QA",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - Testing/Quality", Style::default().fg(Color::Gray)),
        ])),
    ];

    let agents_list = List::new(agent_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🤖 Available Agents ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Green)
                .add_modifier(Modifier::BOLD),
        );

    f.render_widget(agents_list, chunks[0]);

    // Delegation form and recent delegations
    let right_chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(8), Constraint::Min(0)])
        .split(chunks[1]);

    // Delegation form
    let form_text = "🎯 Manual Task Delegation\n\n\
                     Format: <agent> <task_description>\n\n\
                     Examples:\n\
                     • frontend Create user dashboard\n\
                     • backend Add payment processing\n\
                     • qa Write end-to-end tests\n\
                     • devops Setup CI/CD pipeline";

    let form_paragraph = Paragraph::new(form_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📝 Delegation Form ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(form_paragraph, right_chunks[0]);

    // Recent delegations
    let delegation_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(6)
        .filter(|d| d.reasoning.contains("Manual delegation"))
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "frontend" | "Frontend" => "🎨",
                "backend" | "Backend" => "⚙️",
                "devops" | "DevOps" => "🔧",
                "qa" | "QA" => "🧪",
                _ => "🤖",
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::Green)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("{} ", decision.created_at.format("%H:%M")),
                    Style::default().fg(Color::DarkGray),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::White),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let delegations_list = List::new(delegation_items).block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 📋 Recent Delegations ")
            .title_alignment(Alignment::Center),
    );

    f.render_widget(delegations_list, right_chunks[1]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_stats (lines 1160-1303) ---[0m
fn draw_delegation_stats(f: &mut Frame, area: Rect, app: &App) {
    if app.delegation_decisions.is_empty() {
        let empty_text = "📊 Delegation Statistics\n\n\
                         No delegation data available yet.\n\n\
                         Start analyzing or delegating tasks\n\
                         to see statistics here.";

        let empty_paragraph = Paragraph::new(empty_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📊 Statistics ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::Gray))
            .alignment(Alignment::Center)
            .wrap(Wrap { trim: true });

        f.render_widget(empty_paragraph, area);
        return;
    }

    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Agent distribution chart
    let mut agent_counts = std::collections::HashMap::new();
    let mut total_confidence = 0.0;

    for decision in &app.delegation_decisions {
        *agent_counts
            .entry(decision.recommended_agent.clone())
            .or_insert(0) += 1;
        total_confidence += decision.confidence;
    }

    let total = app.delegation_decisions.len();
    let avg_confidence = total_confidence / total as f64;

    // Create agent distribution table
    let header = Row::new(vec![
        Cell::from("Agent").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Count").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Percentage").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = agent_counts
        .iter()
        .map(|(agent, count)| {
            let percentage = (*count as f64 / total as f64) * 100.0;
            let agent_icon = match agent.as_str() {
                "Frontend" | "frontend" => "🎨",
                "Backend" | "backend" => "⚙️",
                "DevOps" | "devops" => "🔧",
                "QA" | "qa" => "🧪",
                _ => "🤖",
            };

            Row::new(vec![
                Cell::from(format!("{} {}", agent_icon, agent)).style(
                    Style::default()
                        .fg(Color::Cyan)
                        .add_modifier(Modifier::BOLD),
                ),
                Cell::from(format!("{}", count)).style(Style::default().fg(Color::Yellow)),
                Cell::from(format!("{:.1}%", percentage)).style(Style::default().fg(Color::Green)),
            ])
        })
        .collect();

    let distribution_table = Table::new(
        rows,
        [
            Constraint::Length(15), // Agent
            Constraint::Length(8),  // Count
            Constraint::Length(12), // Percentage
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 📊 Agent Distribution ")
            .title_alignment(Alignment::Center),
    );

    f.render_widget(distribution_table, chunks[0]);

    // Statistics summary
    let stats_text = format!(
        "📈 Delegation Summary\n\n\
         Total Delegations: {}\n\
         Average Confidence: {:.1}%\n\n\
         📊 Distribution:\n\
         {}\n\n\
         🎯 Most Delegated: {}\n\
         📅 Latest: {}",
        total,
        avg_confidence * 100.0,
        agent_counts
            .iter()
            .map(|(agent, count)| format!("• {}: {}", agent, count))
            .collect::<Vec<_>>()
            .join("\n         "),
        agent_counts
            .iter()
            .max_by_key(|(_, count)| *count)
            .map(|(agent, _)| agent.as_str())
            .unwrap_or("None"),
        app.delegation_decisions
            .last()
            .map(|d| d.created_at.format("%H:%M:%S").to_string())
            .unwrap_or("None".to_string())
    );

    let stats_paragraph = Paragraph::new(stats_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📋 Statistics Summary ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(stats_paragraph, chunks[1]);
}

  ./crates/ccswarm/src/tui/ui.rs:432-585 function draw_agents <-> ./crates/ccswarm/src/tui/ui.rs:588-706 function draw_tasks
  Similarity: 98.78%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_agents (lines 432-585) ---[0m
fn draw_agents(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(65), Constraint::Percentage(35)])
        .split(area);

    // Create table rows for better formatting
    let header = Row::new(vec![
        Cell::from("#").style(Style::default().fg(Color::DarkGray)),
        Cell::from("Name").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Provider").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Type").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Status").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = app
        .agents
        .iter()
        .enumerate()
        .map(|(i, agent)| {
            let (status_color, status_text, status_icon) = match agent.status {
                crate::agent::AgentStatus::Available => (Color::Green, "Available", "✅"),
                crate::agent::AgentStatus::Working => (Color::Cyan, "Working", "⚙️"),
                crate::agent::AgentStatus::Error(_) => (Color::Red, "Error", "❌"),
                crate::agent::AgentStatus::Initializing => (Color::Yellow, "Initializing", "🔄"),
                crate::agent::AgentStatus::WaitingForReview => (Color::Magenta, "Waiting", "⏳"),
                crate::agent::AgentStatus::ShuttingDown => (Color::DarkGray, "Shutting Down", "⏹️"),
            };

            // Special styling for Master Claude Code
            let is_master = agent.specialization.contains("Master");
            let (name_color, name_icon, type_color) = if is_master {
                (Color::Yellow, "👑", Color::Yellow)
            } else {
                let type_icon = match agent.specialization.as_str() {
                    s if s.contains("Frontend") => "🎨",
                    s if s.contains("Backend") => "⚙️",
                    s if s.contains("DevOps") => "🔧",
                    s if s.contains("QA") => "🧪",
                    s if s.contains("Test") => "🔬",
                    s if s.contains("Error") => "🚨",
                    _ => "🤖",
                };
                (Color::Cyan, type_icon, Color::White)
            };

            let provider_display = if is_master {
                format!("{} claude_code", "👑")
            } else {
                format!("{} claude_code", "🤖")
            };

            Row::new(vec![
                Cell::from(format!("{}", i + 1)).style(Style::default().fg(Color::DarkGray)),
                Cell::from(format!("{} {}", name_icon, agent.name)).style(
                    Style::default().fg(name_color).add_modifier(if is_master {
                        Modifier::BOLD
                    } else {
                        Modifier::empty()
                    }),
                ),
                Cell::from(provider_display).style(Style::default().fg(if is_master {
                    Color::Yellow
                } else {
                    Color::Blue
                })),
                Cell::from(agent.specialization.clone()).style(Style::default().fg(type_color)),
                Cell::from(format!("{} {}", status_icon, status_text)).style(
                    Style::default()
                        .fg(status_color)
                        .add_modifier(Modifier::BOLD),
                ),
            ])
        })
        .collect();

    let agents_table = Table::new(
        rows,
        [
            Constraint::Length(3),  // #
            Constraint::Length(18), // Name
            Constraint::Length(16), // Provider
            Constraint::Length(20), // Type
            Constraint::Length(15), // Status
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 🤖 Agent Management Dashboard ")
            .title_alignment(Alignment::Center)
            .style(Style::default().fg(Color::White)),
    )
    .row_highlight_style(
        Style::default()
            .bg(Color::Blue)
            .fg(Color::White)
            .add_modifier(Modifier::BOLD),
    )
    .highlight_symbol("► ");

    // Create table state
    let mut table_state = ratatui::widgets::TableState::default();
    table_state.select(Some(app.selected_agent));

    f.render_stateful_widget(agents_table, chunks[0], &mut table_state);

    // Agent details
    if let Some(agent) = app.agents.get(app.selected_agent) {
        let details = vec![
            format!("ID: {}", agent.id),
            format!("Name: {}", agent.name),
            format!("Type: {}", agent.specialization),
            format!("Provider: {} {}", agent.provider_icon, agent.provider_type),
            format!("Status: {:?}", agent.status),
            format!("Tasks Completed: {}", agent.tasks_completed),
            format!("Last Activity: {}", agent.last_activity.format("%H:%M:%S")),
            format!("Workspace: {}", agent.workspace),
            format!(
                "Current Task: {}",
                agent.current_task.as_deref().unwrap_or("None")
            ),
        ];

        let details_text = details.join("\n");
        let details_paragraph = Paragraph::new(details_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .title(" Agent Details "),
            )
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_tasks (lines 588-706) ---[0m
fn draw_tasks(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Percentage(70), Constraint::Percentage(30)])
        .split(area);

    // Task list with enhanced headers
    let header = Row::new(vec![
        "#",
        "Description",
        "Priority",
        "Type",
        "Status",
        "Agent",
        "Created",
    ])
    .style(Style::default().fg(Color::Yellow))
    .height(1)
    .bottom_margin(1);

    let rows: Vec<Row> = app
        .tasks
        .iter()
        .enumerate()
        .map(|(i, task)| {
            let priority_color = match task.priority.as_str() {
                "High" => Color::Red,
                "Medium" => Color::Yellow,
                "Low" => Color::Green,
                _ => Color::White,
            };

            let status_color = if task.status.contains("✅") {
                Color::Green
            } else if task.status.contains("❌") {
                Color::Red
            } else if task.status.contains("🏃") {
                Color::Cyan
            } else if task.status.contains("📋") {
                Color::Yellow
            } else {
                Color::White
            };

            Row::new(vec![
                Cell::from(format!("{}", i + 1)),
                Cell::from(task.description.clone()),
                Cell::from(Span::styled(
                    task.priority.clone(),
                    Style::default().fg(priority_color),
                )),
                Cell::from(task.task_type.clone()),
                Cell::from(Span::styled(
                    task.status.clone(),
                    Style::default().fg(status_color),
                )),
                Cell::from(task.assigned_agent.as_deref().unwrap_or("-")),
                Cell::from(task.created_at.format("%H:%M:%S").to_string()),
            ])
        })
        .collect();

    let selected_style = Style::default()
        .bg(Color::Yellow)
        .fg(Color::Black)
        .add_modifier(Modifier::BOLD);

    let table = Table::new(
        rows,
        &[
            Constraint::Length(3),  // #
            Constraint::Min(25),    // Description
            Constraint::Length(8),  // Priority
            Constraint::Length(12), // Type
            Constraint::Length(15), // Status
            Constraint::Length(12), // Agent
            Constraint::Length(8),  // Created
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .title(" Tasks (t to add new task) "),
    )
    .row_highlight_style(selected_style);

    let mut table_state = ratatui::widgets::TableState::default();
    table_state.select(Some(app.selected_task));

    f.render_stateful_widget(table, chunks[0], &mut table_state);

    // Task details
    if let Some(task) = app.tasks.get(app.selected_task) {
        let details = [
            format!("ID: {}", task.id),
            format!("Description: {}", task.description),
            format!("Priority: {}", task.priority),
            format!("Type: {}", task.task_type),
            format!("Status: {}", task.status),
            format!("Created: {}", task.created_at.format("%Y-%m-%d %H:%M:%S")),
            format!(
                "Assigned Agent: {}",
                task.assigned_agent.as_deref().unwrap_or("None")
            ),
        ];

        let details_text = details.join("\n");
        let details_paragraph = Paragraph::new(details_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .title(" Task Details "),
            )
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    }
}

  ./crates/ccswarm/src/tui/ui.rs:432-585 function draw_agents <-> ./crates/ccswarm/src/tui/ui.rs:894-1014 function draw_delegation_analysis
  Similarity: 95.10%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_agents (lines 432-585) ---[0m
fn draw_agents(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(65), Constraint::Percentage(35)])
        .split(area);

    // Create table rows for better formatting
    let header = Row::new(vec![
        Cell::from("#").style(Style::default().fg(Color::DarkGray)),
        Cell::from("Name").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Provider").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Type").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Status").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = app
        .agents
        .iter()
        .enumerate()
        .map(|(i, agent)| {
            let (status_color, status_text, status_icon) = match agent.status {
                crate::agent::AgentStatus::Available => (Color::Green, "Available", "✅"),
                crate::agent::AgentStatus::Working => (Color::Cyan, "Working", "⚙️"),
                crate::agent::AgentStatus::Error(_) => (Color::Red, "Error", "❌"),
                crate::agent::AgentStatus::Initializing => (Color::Yellow, "Initializing", "🔄"),
                crate::agent::AgentStatus::WaitingForReview => (Color::Magenta, "Waiting", "⏳"),
                crate::agent::AgentStatus::ShuttingDown => (Color::DarkGray, "Shutting Down", "⏹️"),
            };

            // Special styling for Master Claude Code
            let is_master = agent.specialization.contains("Master");
            let (name_color, name_icon, type_color) = if is_master {
                (Color::Yellow, "👑", Color::Yellow)
            } else {
                let type_icon = match agent.specialization.as_str() {
                    s if s.contains("Frontend") => "🎨",
                    s if s.contains("Backend") => "⚙️",
                    s if s.contains("DevOps") => "🔧",
                    s if s.contains("QA") => "🧪",
                    s if s.contains("Test") => "🔬",
                    s if s.contains("Error") => "🚨",
                    _ => "🤖",
                };
                (Color::Cyan, type_icon, Color::White)
            };

            let provider_display = if is_master {
                format!("{} claude_code", "👑")
            } else {
                format!("{} claude_code", "🤖")
            };

            Row::new(vec![
                Cell::from(format!("{}", i + 1)).style(Style::default().fg(Color::DarkGray)),
                Cell::from(format!("{} {}", name_icon, agent.name)).style(
                    Style::default().fg(name_color).add_modifier(if is_master {
                        Modifier::BOLD
                    } else {
                        Modifier::empty()
                    }),
                ),
                Cell::from(provider_display).style(Style::default().fg(if is_master {
                    Color::Yellow
                } else {
                    Color::Blue
                })),
                Cell::from(agent.specialization.clone()).style(Style::default().fg(type_color)),
                Cell::from(format!("{} {}", status_icon, status_text)).style(
                    Style::default()
                        .fg(status_color)
                        .add_modifier(Modifier::BOLD),
                ),
            ])
        })
        .collect();

    let agents_table = Table::new(
        rows,
        [
            Constraint::Length(3),  // #
            Constraint::Length(18), // Name
            Constraint::Length(16), // Provider
            Constraint::Length(20), // Type
            Constraint::Length(15), // Status
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 🤖 Agent Management Dashboard ")
            .title_alignment(Alignment::Center)
            .style(Style::default().fg(Color::White)),
    )
    .row_highlight_style(
        Style::default()
            .bg(Color::Blue)
            .fg(Color::White)
            .add_modifier(Modifier::BOLD),
    )
    .highlight_symbol("► ");

    // Create table state
    let mut table_state = ratatui::widgets::TableState::default();
    table_state.select(Some(app.selected_agent));

    f.render_stateful_widget(agents_table, chunks[0], &mut table_state);

    // Agent details
    if let Some(agent) = app.agents.get(app.selected_agent) {
        let details = vec![
            format!("ID: {}", agent.id),
            format!("Name: {}", agent.name),
            format!("Type: {}", agent.specialization),
            format!("Provider: {} {}", agent.provider_icon, agent.provider_type),
            format!("Status: {:?}", agent.status),
            format!("Tasks Completed: {}", agent.tasks_completed),
            format!("Last Activity: {}", agent.last_activity.format("%H:%M:%S")),
            format!("Workspace: {}", agent.workspace),
            format!(
                "Current Task: {}",
                agent.current_task.as_deref().unwrap_or("None")
            ),
        ];

        let details_text = details.join("\n");
        let details_paragraph = Paragraph::new(details_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .title(" Agent Details "),
            )
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_analysis (lines 894-1014) ---[0m
fn draw_delegation_analysis(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Recent analysis results
    let analysis_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(10)
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "Frontend" => "🎨",
                "Backend" => "⚙️",
                "DevOps" => "🔧",
                "QA" => "🧪",
                _ => "🤖",
            };

            let confidence_color = if decision.confidence >= 0.8 {
                Color::Green
            } else if decision.confidence >= 0.6 {
                Color::Yellow
            } else {
                Color::Red
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::White)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("({:.0}%) ", decision.confidence * 100.0),
                    Style::default().fg(confidence_color),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::Gray),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut analysis_state = ListState::default();
    analysis_state.select(Some(app.selected_delegation));

    let analysis_list = List::new(analysis_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🔍 Analysis Results ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Blue)
                .add_modifier(Modifier::BOLD),
        )
        .highlight_symbol("► ");

    f.render_stateful_widget(analysis_list, chunks[0], &mut analysis_state);

    // Analysis details
    if let Some(decision) = app.delegation_decisions.get(app.selected_delegation) {
        let details = format!(
            "📋 Task Analysis Details\n\n\
             Task: {}\n\n\
             🎯 Recommended Agent: {}\n\
             📊 Confidence: {:.1}%\n\
             🧠 Reasoning: {}\n\
             📅 Analyzed: {}\n\n\
             💡 Press Enter to delegate this task",
            decision.task_description,
            decision.recommended_agent,
            decision.confidence * 100.0,
            decision.reasoning,
            decision.created_at.format("%H:%M:%S")
        );

        let details_paragraph = Paragraph::new(details)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Analysis Details ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::White))
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    } else {
        let empty_text = "🔍 Task Analysis\n\n\
                         No analysis results yet.\n\n\
                         Press Enter to analyze a new task\n\
                         and get Master's recommendation\n\
                         for optimal agent assignment.";

        let empty_paragraph = Paragraph::new(empty_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Ready for Analysis ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::Gray))
            .alignment(Alignment::Center)
            .wrap(Wrap { trim: true });

        f.render_widget(empty_paragraph, chunks[1]);
    }
}

  ./crates/ccswarm/src/tui/ui.rs:432-585 function draw_agents <-> ./crates/ccswarm/src/tui/ui.rs:1420-1558 function draw_input_popup
  Similarity: 89.22%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_agents (lines 432-585) ---[0m
fn draw_agents(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(65), Constraint::Percentage(35)])
        .split(area);

    // Create table rows for better formatting
    let header = Row::new(vec![
        Cell::from("#").style(Style::default().fg(Color::DarkGray)),
        Cell::from("Name").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Provider").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Type").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Status").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = app
        .agents
        .iter()
        .enumerate()
        .map(|(i, agent)| {
            let (status_color, status_text, status_icon) = match agent.status {
                crate::agent::AgentStatus::Available => (Color::Green, "Available", "✅"),
                crate::agent::AgentStatus::Working => (Color::Cyan, "Working", "⚙️"),
                crate::agent::AgentStatus::Error(_) => (Color::Red, "Error", "❌"),
                crate::agent::AgentStatus::Initializing => (Color::Yellow, "Initializing", "🔄"),
                crate::agent::AgentStatus::WaitingForReview => (Color::Magenta, "Waiting", "⏳"),
                crate::agent::AgentStatus::ShuttingDown => (Color::DarkGray, "Shutting Down", "⏹️"),
            };

            // Special styling for Master Claude Code
            let is_master = agent.specialization.contains("Master");
            let (name_color, name_icon, type_color) = if is_master {
                (Color::Yellow, "👑", Color::Yellow)
            } else {
                let type_icon = match agent.specialization.as_str() {
                    s if s.contains("Frontend") => "🎨",
                    s if s.contains("Backend") => "⚙️",
                    s if s.contains("DevOps") => "🔧",
                    s if s.contains("QA") => "🧪",
                    s if s.contains("Test") => "🔬",
                    s if s.contains("Error") => "🚨",
                    _ => "🤖",
                };
                (Color::Cyan, type_icon, Color::White)
            };

            let provider_display = if is_master {
                format!("{} claude_code", "👑")
            } else {
                format!("{} claude_code", "🤖")
            };

            Row::new(vec![
                Cell::from(format!("{}", i + 1)).style(Style::default().fg(Color::DarkGray)),
                Cell::from(format!("{} {}", name_icon, agent.name)).style(
                    Style::default().fg(name_color).add_modifier(if is_master {
                        Modifier::BOLD
                    } else {
                        Modifier::empty()
                    }),
                ),
                Cell::from(provider_display).style(Style::default().fg(if is_master {
                    Color::Yellow
                } else {
                    Color::Blue
                })),
                Cell::from(agent.specialization.clone()).style(Style::default().fg(type_color)),
                Cell::from(format!("{} {}", status_icon, status_text)).style(
                    Style::default()
                        .fg(status_color)
                        .add_modifier(Modifier::BOLD),
                ),
            ])
        })
        .collect();

    let agents_table = Table::new(
        rows,
        [
            Constraint::Length(3),  // #
            Constraint::Length(18), // Name
            Constraint::Length(16), // Provider
            Constraint::Length(20), // Type
            Constraint::Length(15), // Status
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 🤖 Agent Management Dashboard ")
            .title_alignment(Alignment::Center)
            .style(Style::default().fg(Color::White)),
    )
    .row_highlight_style(
        Style::default()
            .bg(Color::Blue)
            .fg(Color::White)
            .add_modifier(Modifier::BOLD),
    )
    .highlight_symbol("► ");

    // Create table state
    let mut table_state = ratatui::widgets::TableState::default();
    table_state.select(Some(app.selected_agent));

    f.render_stateful_widget(agents_table, chunks[0], &mut table_state);

    // Agent details
    if let Some(agent) = app.agents.get(app.selected_agent) {
        let details = vec![
            format!("ID: {}", agent.id),
            format!("Name: {}", agent.name),
            format!("Type: {}", agent.specialization),
            format!("Provider: {} {}", agent.provider_icon, agent.provider_type),
            format!("Status: {:?}", agent.status),
            format!("Tasks Completed: {}", agent.tasks_completed),
            format!("Last Activity: {}", agent.last_activity.format("%H:%M:%S")),
            format!("Workspace: {}", agent.workspace),
            format!(
                "Current Task: {}",
                agent.current_task.as_deref().unwrap_or("None")
            ),
        ];

        let details_text = details.join("\n");
        let details_paragraph = Paragraph::new(details_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .title(" Agent Details "),
            )
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_input_popup (lines 1420-1558) ---[0m
fn draw_input_popup(f: &mut Frame, app: &App) {
    let area = centered_rect(70, 25, f.area());

    let (title, _title_icon, border_color) = match app.input_mode {
        InputMode::AddingTask => (" 📋 Add New Task ", "📋", Color::Green),
        InputMode::CreatingAgent => (" 🤖 Create New Agent ", "🤖", Color::Blue),
        InputMode::Command => (" 💻 Command Prompt ", "💻", Color::Yellow),
        InputMode::DelegationInput => (" 🎯 Master Delegation ", "🎯", Color::Magenta),
        _ => (" ✏️  Input ", "✏️", Color::White),
    };

    let prompt = match app.input_mode {
        InputMode::AddingTask => {
            "Enter task description:\n\n\
             💡 Tips:\n\
             • Use [high]/[medium]/[low] for priority\n\
             • Use [test]/[docs]/[bug]/[feature] for type\n\
             • Example: \"Fix login bug [high] [bug]\""
        }
        InputMode::CreatingAgent => {
            "Select agent specialization:\n\n\
             🎨 frontend  - UI/UX development\n\
             ⚙️  backend   - API/Database work\n\
             🔧 devops    - Infrastructure/Deploy\n\
             🧪 qa        - Testing/Quality assurance"
        }
        InputMode::Command => {
            "Enter command:\n\n\
             💡 Quick commands:\n\
             • help - Show all commands\n\
             • start_agent master - Start master agent\n\
             • task <description> - Add new task\n\
             • status - Show system status"
        }
        InputMode::DelegationInput => match app.delegation_mode {
            DelegationMode::Analyze => {
                "🔍 Analyze Task for Delegation:\n\n\
                     Enter task description to get Master's recommendation\n\
                     💡 Examples:\n\
                     • \"Create login form with validation\"\n\
                     • \"Fix API endpoint error handling\"\n\
                     • \"Write unit tests for user service\""
            }
            DelegationMode::Delegate => {
                "🎯 Delegate Task to Agent:\n\n\
                     Format: <agent_type> <task_description>\n\
                     💡 Examples:\n\
                     • \"frontend Create responsive navigation\"\n\
                     • \"backend Add user authentication API\"\n\
                     • \"qa Write integration tests\""
            }
            _ => "Enter delegation input:",
        },
        _ => "Enter your input:",
    };

    // Create a shadow effect
    let shadow_area = Rect {
        x: area.x + 1,
        y: area.y + 1,
        width: area.width,
        height: area.height,
    };
    f.render_widget(
        Block::default().style(Style::default().bg(Color::Black)),
        shadow_area,
    );

    f.render_widget(Clear, area); // Clear background

    let input_block = Block::default()
        .title(title)
        .title_alignment(Alignment::Center)
        .borders(Borders::ALL)
        .border_type(BorderType::Rounded)
        .border_style(
            Style::default()
                .fg(border_color)
                .add_modifier(Modifier::BOLD),
        );

    let input_area = input_block.inner(area);
    f.render_widget(input_block, area);

    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(8), // Prompt area
            Constraint::Length(3), // Input area
            Constraint::Length(2), // Help area
        ])
        .split(input_area);

    // Prompt text
    let prompt_paragraph = Paragraph::new(prompt)
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });
    f.render_widget(prompt_paragraph, chunks[0]);

    // Input box with cursor
    let input_text = format!("{}_", app.input_buffer);
    let input_paragraph = Paragraph::new(input_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Input ")
                .border_style(Style::default().fg(Color::Cyan)),
        )
        .style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        );
    f.render_widget(input_paragraph, chunks[1]);

    // Help text
    let help_text = vec![Line::from(vec![
        Span::styled(
            " Enter ",
            Style::default()
                .fg(Color::Black)
                .bg(Color::Green)
                .add_modifier(Modifier::BOLD),
        ),
        Span::styled(" Confirm ", Style::default().fg(Color::Gray)),
        Span::raw("  "),
        Span::styled(
            " Esc ",
            Style::default()
                .fg(Color::Black)
                .bg(Color::Red)
                .add_modifier(Modifier::BOLD),
        ),
        Span::styled(" Cancel ", Style::default().fg(Color::Gray)),
    ])];
    let help_paragraph = Paragraph::new(help_text).alignment(Alignment::Center);
    f.render_widget(help_paragraph, chunks[2]);
}

  ./crates/ccswarm/src/tui/ui.rs:298-429 function draw_enhanced_agent_summary <-> ./crates/ccswarm/src/tui/ui.rs:1017-1157 function draw_delegation_interface
  Similarity: 95.56%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_agent_summary (lines 298-429) ---[0m
fn draw_enhanced_agent_summary(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(60), Constraint::Percentage(40)])
        .split(area);

    // Enhanced agents table
    let header = Row::new(vec![
        Cell::from("Agent").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Type").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Status").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Tasks").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = app
        .agents
        .iter()
        .take(6)
        .map(|agent| {
            let (status_color, status_text, status_icon) = match agent.status {
                crate::agent::AgentStatus::Available => (Color::Green, "Available", "✅"),
                crate::agent::AgentStatus::Working => (Color::Cyan, "Working", "⚙️"),
                crate::agent::AgentStatus::Error(_) => (Color::Red, "Error", "❌"),
                crate::agent::AgentStatus::Initializing => (Color::Yellow, "Init", "🔄"),
                crate::agent::AgentStatus::WaitingForReview => (Color::Magenta, "Review", "⏳"),
                crate::agent::AgentStatus::ShuttingDown => (Color::DarkGray, "Shutdown", "⏹️"),
            };

            let is_master = agent.specialization.contains("Master");
            let (name_icon, name_color) = if is_master {
                ("👑", Color::Yellow)
            } else {
                match agent.specialization.as_str() {
                    s if s.contains("Frontend") => ("🎨", Color::Cyan),
                    s if s.contains("Backend") => ("⚙️", Color::Green),
                    s if s.contains("DevOps") => ("🔧", Color::Blue),
                    s if s.contains("QA") => ("🧪", Color::Magenta),
                    _ => ("🤖", Color::White),
                }
            };

            Row::new(vec![
                Cell::from(format!("{} {}", name_icon, agent.name)).style(
                    Style::default().fg(name_color).add_modifier(if is_master {
                        Modifier::BOLD
                    } else {
                        Modifier::empty()
                    }),
                ),
                Cell::from(agent.specialization.clone()).style(Style::default().fg(Color::White)),
                Cell::from(format!("{} {}", status_icon, status_text))
                    .style(Style::default().fg(status_color)),
                Cell::from(format!("{}", agent.tasks_completed))
                    .style(Style::default().fg(Color::Yellow)),
            ])
        })
        .collect();

    let agents_table = Table::new(
        rows,
        [
            Constraint::Length(15), // Agent
            Constraint::Length(12), // Type
            Constraint::Length(12), // Status
            Constraint::Length(6),  // Tasks
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 👥 Agents Overview ")
            .title_alignment(Alignment::Center),
    )
    .row_highlight_style(
        Style::default()
            .bg(Color::DarkGray)
            .add_modifier(Modifier::BOLD),
    );

    f.render_widget(agents_table, chunks[0]);

    // System activity summary
    let activity_text = format!(
        "🔄 System Activity\n\n\
         ⏱️  Uptime: Active\n\
         📊 Total Agents: {}\n\
         🎯 Active: {}\n\
         📋 Tasks Queued: {}\n\
         ✅ Completed: {}\n\n\
         📈 Efficiency: {:.1}%",
        app.total_agents,
        app.active_agents,
        app.pending_tasks,
        app.completed_tasks,
        if app.total_agents > 0 {
            (app.active_agents as f32 / app.total_agents as f32) * 100.0
        } else {
            0.0
        }
    );

    let activity_paragraph = Paragraph::new(activity_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📊 System Metrics ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::Cyan))
        .wrap(Wrap { trim: true });

    f.render_widget(activity_paragraph, chunks[1]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_interface (lines 1017-1157) ---[0m
fn draw_delegation_interface(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(40), Constraint::Percentage(60)])
        .split(area);

    // Available agents
    let agent_items: Vec<ListItem> = vec![
        ListItem::new(Line::from(vec![
            Span::styled("🎨 ", Style::default().fg(Color::Cyan)),
            Span::styled(
                "Frontend",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - UI/UX Development", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("⚙️ ", Style::default().fg(Color::Green)),
            Span::styled(
                "Backend",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - API/Database Work", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("🔧 ", Style::default().fg(Color::Blue)),
            Span::styled(
                "DevOps",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - Infrastructure/Deploy", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("🧪 ", Style::default().fg(Color::Magenta)),
            Span::styled(
                "QA",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - Testing/Quality", Style::default().fg(Color::Gray)),
        ])),
    ];

    let agents_list = List::new(agent_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🤖 Available Agents ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Green)
                .add_modifier(Modifier::BOLD),
        );

    f.render_widget(agents_list, chunks[0]);

    // Delegation form and recent delegations
    let right_chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(8), Constraint::Min(0)])
        .split(chunks[1]);

    // Delegation form
    let form_text = "🎯 Manual Task Delegation\n\n\
                     Format: <agent> <task_description>\n\n\
                     Examples:\n\
                     • frontend Create user dashboard\n\
                     • backend Add payment processing\n\
                     • qa Write end-to-end tests\n\
                     • devops Setup CI/CD pipeline";

    let form_paragraph = Paragraph::new(form_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📝 Delegation Form ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(form_paragraph, right_chunks[0]);

    // Recent delegations
    let delegation_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(6)
        .filter(|d| d.reasoning.contains("Manual delegation"))
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "frontend" | "Frontend" => "🎨",
                "backend" | "Backend" => "⚙️",
                "devops" | "DevOps" => "🔧",
                "qa" | "QA" => "🧪",
                _ => "🤖",
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::Green)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("{} ", decision.created_at.format("%H:%M")),
                    Style::default().fg(Color::DarkGray),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::White),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let delegations_list = List::new(delegation_items).block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 📋 Recent Delegations ")
            .title_alignment(Alignment::Center),
    );

    f.render_widget(delegations_list, right_chunks[1]);
}

  ./crates/ccswarm/src/tui/ui.rs:1160-1303 function draw_delegation_stats <-> ./crates/ccswarm/src/tui/ui.rs:1420-1558 function draw_input_popup
  Similarity: 91.60%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_stats (lines 1160-1303) ---[0m
fn draw_delegation_stats(f: &mut Frame, area: Rect, app: &App) {
    if app.delegation_decisions.is_empty() {
        let empty_text = "📊 Delegation Statistics\n\n\
                         No delegation data available yet.\n\n\
                         Start analyzing or delegating tasks\n\
                         to see statistics here.";

        let empty_paragraph = Paragraph::new(empty_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📊 Statistics ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::Gray))
            .alignment(Alignment::Center)
            .wrap(Wrap { trim: true });

        f.render_widget(empty_paragraph, area);
        return;
    }

    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Agent distribution chart
    let mut agent_counts = std::collections::HashMap::new();
    let mut total_confidence = 0.0;

    for decision in &app.delegation_decisions {
        *agent_counts
            .entry(decision.recommended_agent.clone())
            .or_insert(0) += 1;
        total_confidence += decision.confidence;
    }

    let total = app.delegation_decisions.len();
    let avg_confidence = total_confidence / total as f64;

    // Create agent distribution table
    let header = Row::new(vec![
        Cell::from("Agent").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Count").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Percentage").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = agent_counts
        .iter()
        .map(|(agent, count)| {
            let percentage = (*count as f64 / total as f64) * 100.0;
            let agent_icon = match agent.as_str() {
                "Frontend" | "frontend" => "🎨",
                "Backend" | "backend" => "⚙️",
                "DevOps" | "devops" => "🔧",
                "QA" | "qa" => "🧪",
                _ => "🤖",
            };

            Row::new(vec![
                Cell::from(format!("{} {}", agent_icon, agent)).style(
                    Style::default()
                        .fg(Color::Cyan)
                        .add_modifier(Modifier::BOLD),
                ),
                Cell::from(format!("{}", count)).style(Style::default().fg(Color::Yellow)),
                Cell::from(format!("{:.1}%", percentage)).style(Style::default().fg(Color::Green)),
            ])
        })
        .collect();

    let distribution_table = Table::new(
        rows,
        [
            Constraint::Length(15), // Agent
            Constraint::Length(8),  // Count
            Constraint::Length(12), // Percentage
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 📊 Agent Distribution ")
            .title_alignment(Alignment::Center),
    );

    f.render_widget(distribution_table, chunks[0]);

    // Statistics summary
    let stats_text = format!(
        "📈 Delegation Summary\n\n\
         Total Delegations: {}\n\
         Average Confidence: {:.1}%\n\n\
         📊 Distribution:\n\
         {}\n\n\
         🎯 Most Delegated: {}\n\
         📅 Latest: {}",
        total,
        avg_confidence * 100.0,
        agent_counts
            .iter()
            .map(|(agent, count)| format!("• {}: {}", agent, count))
            .collect::<Vec<_>>()
            .join("\n         "),
        agent_counts
            .iter()
            .max_by_key(|(_, count)| *count)
            .map(|(agent, _)| agent.as_str())
            .unwrap_or("None"),
        app.delegation_decisions
            .last()
            .map(|d| d.created_at.format("%H:%M:%S").to_string())
            .unwrap_or("None".to_string())
    );

    let stats_paragraph = Paragraph::new(stats_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📋 Statistics Summary ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(stats_paragraph, chunks[1]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_input_popup (lines 1420-1558) ---[0m
fn draw_input_popup(f: &mut Frame, app: &App) {
    let area = centered_rect(70, 25, f.area());

    let (title, _title_icon, border_color) = match app.input_mode {
        InputMode::AddingTask => (" 📋 Add New Task ", "📋", Color::Green),
        InputMode::CreatingAgent => (" 🤖 Create New Agent ", "🤖", Color::Blue),
        InputMode::Command => (" 💻 Command Prompt ", "💻", Color::Yellow),
        InputMode::DelegationInput => (" 🎯 Master Delegation ", "🎯", Color::Magenta),
        _ => (" ✏️  Input ", "✏️", Color::White),
    };

    let prompt = match app.input_mode {
        InputMode::AddingTask => {
            "Enter task description:\n\n\
             💡 Tips:\n\
             • Use [high]/[medium]/[low] for priority\n\
             • Use [test]/[docs]/[bug]/[feature] for type\n\
             • Example: \"Fix login bug [high] [bug]\""
        }
        InputMode::CreatingAgent => {
            "Select agent specialization:\n\n\
             🎨 frontend  - UI/UX development\n\
             ⚙️  backend   - API/Database work\n\
             🔧 devops    - Infrastructure/Deploy\n\
             🧪 qa        - Testing/Quality assurance"
        }
        InputMode::Command => {
            "Enter command:\n\n\
             💡 Quick commands:\n\
             • help - Show all commands\n\
             • start_agent master - Start master agent\n\
             • task <description> - Add new task\n\
             • status - Show system status"
        }
        InputMode::DelegationInput => match app.delegation_mode {
            DelegationMode::Analyze => {
                "🔍 Analyze Task for Delegation:\n\n\
                     Enter task description to get Master's recommendation\n\
                     💡 Examples:\n\
                     • \"Create login form with validation\"\n\
                     • \"Fix API endpoint error handling\"\n\
                     • \"Write unit tests for user service\""
            }
            DelegationMode::Delegate => {
                "🎯 Delegate Task to Agent:\n\n\
                     Format: <agent_type> <task_description>\n\
                     💡 Examples:\n\
                     • \"frontend Create responsive navigation\"\n\
                     • \"backend Add user authentication API\"\n\
                     • \"qa Write integration tests\""
            }
            _ => "Enter delegation input:",
        },
        _ => "Enter your input:",
    };

    // Create a shadow effect
    let shadow_area = Rect {
        x: area.x + 1,
        y: area.y + 1,
        width: area.width,
        height: area.height,
    };
    f.render_widget(
        Block::default().style(Style::default().bg(Color::Black)),
        shadow_area,
    );

    f.render_widget(Clear, area); // Clear background

    let input_block = Block::default()
        .title(title)
        .title_alignment(Alignment::Center)
        .borders(Borders::ALL)
        .border_type(BorderType::Rounded)
        .border_style(
            Style::default()
                .fg(border_color)
                .add_modifier(Modifier::BOLD),
        );

    let input_area = input_block.inner(area);
    f.render_widget(input_block, area);

    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(8), // Prompt area
            Constraint::Length(3), // Input area
            Constraint::Length(2), // Help area
        ])
        .split(input_area);

    // Prompt text
    let prompt_paragraph = Paragraph::new(prompt)
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });
    f.render_widget(prompt_paragraph, chunks[0]);

    // Input box with cursor
    let input_text = format!("{}_", app.input_buffer);
    let input_paragraph = Paragraph::new(input_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Input ")
                .border_style(Style::default().fg(Color::Cyan)),
        )
        .style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        );
    f.render_widget(input_paragraph, chunks[1]);

    // Help text
    let help_text = vec![Line::from(vec![
        Span::styled(
            " Enter ",
            Style::default()
                .fg(Color::Black)
                .bg(Color::Green)
                .add_modifier(Modifier::BOLD),
        ),
        Span::styled(" Confirm ", Style::default().fg(Color::Gray)),
        Span::raw("  "),
        Span::styled(
            " Esc ",
            Style::default()
                .fg(Color::Black)
                .bg(Color::Red)
                .add_modifier(Modifier::BOLD),
        ),
        Span::styled(" Cancel ", Style::default().fg(Color::Gray)),
    ])];
    let help_paragraph = Paragraph::new(help_text).alignment(Alignment::Center);
    f.render_widget(help_paragraph, chunks[2]);
}

  ./crates/ccswarm/src/tui/ui.rs:1017-1157 function draw_delegation_interface <-> ./crates/ccswarm/src/tui/ui.rs:1420-1558 function draw_input_popup
  Similarity: 92.14%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_interface (lines 1017-1157) ---[0m
fn draw_delegation_interface(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(40), Constraint::Percentage(60)])
        .split(area);

    // Available agents
    let agent_items: Vec<ListItem> = vec![
        ListItem::new(Line::from(vec![
            Span::styled("🎨 ", Style::default().fg(Color::Cyan)),
            Span::styled(
                "Frontend",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - UI/UX Development", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("⚙️ ", Style::default().fg(Color::Green)),
            Span::styled(
                "Backend",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - API/Database Work", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("🔧 ", Style::default().fg(Color::Blue)),
            Span::styled(
                "DevOps",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - Infrastructure/Deploy", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("🧪 ", Style::default().fg(Color::Magenta)),
            Span::styled(
                "QA",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - Testing/Quality", Style::default().fg(Color::Gray)),
        ])),
    ];

    let agents_list = List::new(agent_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🤖 Available Agents ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Green)
                .add_modifier(Modifier::BOLD),
        );

    f.render_widget(agents_list, chunks[0]);

    // Delegation form and recent delegations
    let right_chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(8), Constraint::Min(0)])
        .split(chunks[1]);

    // Delegation form
    let form_text = "🎯 Manual Task Delegation\n\n\
                     Format: <agent> <task_description>\n\n\
                     Examples:\n\
                     • frontend Create user dashboard\n\
                     • backend Add payment processing\n\
                     • qa Write end-to-end tests\n\
                     • devops Setup CI/CD pipeline";

    let form_paragraph = Paragraph::new(form_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📝 Delegation Form ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(form_paragraph, right_chunks[0]);

    // Recent delegations
    let delegation_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(6)
        .filter(|d| d.reasoning.contains("Manual delegation"))
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "frontend" | "Frontend" => "🎨",
                "backend" | "Backend" => "⚙️",
                "devops" | "DevOps" => "🔧",
                "qa" | "QA" => "🧪",
                _ => "🤖",
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::Green)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("{} ", decision.created_at.format("%H:%M")),
                    Style::default().fg(Color::DarkGray),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::White),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let delegations_list = List::new(delegation_items).block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 📋 Recent Delegations ")
            .title_alignment(Alignment::Center),
    );

    f.render_widget(delegations_list, right_chunks[1]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_input_popup (lines 1420-1558) ---[0m
fn draw_input_popup(f: &mut Frame, app: &App) {
    let area = centered_rect(70, 25, f.area());

    let (title, _title_icon, border_color) = match app.input_mode {
        InputMode::AddingTask => (" 📋 Add New Task ", "📋", Color::Green),
        InputMode::CreatingAgent => (" 🤖 Create New Agent ", "🤖", Color::Blue),
        InputMode::Command => (" 💻 Command Prompt ", "💻", Color::Yellow),
        InputMode::DelegationInput => (" 🎯 Master Delegation ", "🎯", Color::Magenta),
        _ => (" ✏️  Input ", "✏️", Color::White),
    };

    let prompt = match app.input_mode {
        InputMode::AddingTask => {
            "Enter task description:\n\n\
             💡 Tips:\n\
             • Use [high]/[medium]/[low] for priority\n\
             • Use [test]/[docs]/[bug]/[feature] for type\n\
             • Example: \"Fix login bug [high] [bug]\""
        }
        InputMode::CreatingAgent => {
            "Select agent specialization:\n\n\
             🎨 frontend  - UI/UX development\n\
             ⚙️  backend   - API/Database work\n\
             🔧 devops    - Infrastructure/Deploy\n\
             🧪 qa        - Testing/Quality assurance"
        }
        InputMode::Command => {
            "Enter command:\n\n\
             💡 Quick commands:\n\
             • help - Show all commands\n\
             • start_agent master - Start master agent\n\
             • task <description> - Add new task\n\
             • status - Show system status"
        }
        InputMode::DelegationInput => match app.delegation_mode {
            DelegationMode::Analyze => {
                "🔍 Analyze Task for Delegation:\n\n\
                     Enter task description to get Master's recommendation\n\
                     💡 Examples:\n\
                     • \"Create login form with validation\"\n\
                     • \"Fix API endpoint error handling\"\n\
                     • \"Write unit tests for user service\""
            }
            DelegationMode::Delegate => {
                "🎯 Delegate Task to Agent:\n\n\
                     Format: <agent_type> <task_description>\n\
                     💡 Examples:\n\
                     • \"frontend Create responsive navigation\"\n\
                     • \"backend Add user authentication API\"\n\
                     • \"qa Write integration tests\""
            }
            _ => "Enter delegation input:",
        },
        _ => "Enter your input:",
    };

    // Create a shadow effect
    let shadow_area = Rect {
        x: area.x + 1,
        y: area.y + 1,
        width: area.width,
        height: area.height,
    };
    f.render_widget(
        Block::default().style(Style::default().bg(Color::Black)),
        shadow_area,
    );

    f.render_widget(Clear, area); // Clear background

    let input_block = Block::default()
        .title(title)
        .title_alignment(Alignment::Center)
        .borders(Borders::ALL)
        .border_type(BorderType::Rounded)
        .border_style(
            Style::default()
                .fg(border_color)
                .add_modifier(Modifier::BOLD),
        );

    let input_area = input_block.inner(area);
    f.render_widget(input_block, area);

    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(8), // Prompt area
            Constraint::Length(3), // Input area
            Constraint::Length(2), // Help area
        ])
        .split(input_area);

    // Prompt text
    let prompt_paragraph = Paragraph::new(prompt)
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });
    f.render_widget(prompt_paragraph, chunks[0]);

    // Input box with cursor
    let input_text = format!("{}_", app.input_buffer);
    let input_paragraph = Paragraph::new(input_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Input ")
                .border_style(Style::default().fg(Color::Cyan)),
        )
        .style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        );
    f.render_widget(input_paragraph, chunks[1]);

    // Help text
    let help_text = vec![Line::from(vec![
        Span::styled(
            " Enter ",
            Style::default()
                .fg(Color::Black)
                .bg(Color::Green)
                .add_modifier(Modifier::BOLD),
        ),
        Span::styled(" Confirm ", Style::default().fg(Color::Gray)),
        Span::raw("  "),
        Span::styled(
            " Esc ",
            Style::default()
                .fg(Color::Black)
                .bg(Color::Red)
                .add_modifier(Modifier::BOLD),
        ),
        Span::styled(" Cancel ", Style::default().fg(Color::Gray)),
    ])];
    let help_paragraph = Paragraph::new(help_text).alignment(Alignment::Center);
    f.render_widget(help_paragraph, chunks[2]);
}

  ./crates/ccswarm/src/tui/ui.rs:298-429 function draw_enhanced_agent_summary <-> ./crates/ccswarm/src/tui/ui.rs:1160-1303 function draw_delegation_stats
  Similarity: 90.88%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_agent_summary (lines 298-429) ---[0m
fn draw_enhanced_agent_summary(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(60), Constraint::Percentage(40)])
        .split(area);

    // Enhanced agents table
    let header = Row::new(vec![
        Cell::from("Agent").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Type").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Status").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Tasks").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = app
        .agents
        .iter()
        .take(6)
        .map(|agent| {
            let (status_color, status_text, status_icon) = match agent.status {
                crate::agent::AgentStatus::Available => (Color::Green, "Available", "✅"),
                crate::agent::AgentStatus::Working => (Color::Cyan, "Working", "⚙️"),
                crate::agent::AgentStatus::Error(_) => (Color::Red, "Error", "❌"),
                crate::agent::AgentStatus::Initializing => (Color::Yellow, "Init", "🔄"),
                crate::agent::AgentStatus::WaitingForReview => (Color::Magenta, "Review", "⏳"),
                crate::agent::AgentStatus::ShuttingDown => (Color::DarkGray, "Shutdown", "⏹️"),
            };

            let is_master = agent.specialization.contains("Master");
            let (name_icon, name_color) = if is_master {
                ("👑", Color::Yellow)
            } else {
                match agent.specialization.as_str() {
                    s if s.contains("Frontend") => ("🎨", Color::Cyan),
                    s if s.contains("Backend") => ("⚙️", Color::Green),
                    s if s.contains("DevOps") => ("🔧", Color::Blue),
                    s if s.contains("QA") => ("🧪", Color::Magenta),
                    _ => ("🤖", Color::White),
                }
            };

            Row::new(vec![
                Cell::from(format!("{} {}", name_icon, agent.name)).style(
                    Style::default().fg(name_color).add_modifier(if is_master {
                        Modifier::BOLD
                    } else {
                        Modifier::empty()
                    }),
                ),
                Cell::from(agent.specialization.clone()).style(Style::default().fg(Color::White)),
                Cell::from(format!("{} {}", status_icon, status_text))
                    .style(Style::default().fg(status_color)),
                Cell::from(format!("{}", agent.tasks_completed))
                    .style(Style::default().fg(Color::Yellow)),
            ])
        })
        .collect();

    let agents_table = Table::new(
        rows,
        [
            Constraint::Length(15), // Agent
            Constraint::Length(12), // Type
            Constraint::Length(12), // Status
            Constraint::Length(6),  // Tasks
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 👥 Agents Overview ")
            .title_alignment(Alignment::Center),
    )
    .row_highlight_style(
        Style::default()
            .bg(Color::DarkGray)
            .add_modifier(Modifier::BOLD),
    );

    f.render_widget(agents_table, chunks[0]);

    // System activity summary
    let activity_text = format!(
        "🔄 System Activity\n\n\
         ⏱️  Uptime: Active\n\
         📊 Total Agents: {}\n\
         🎯 Active: {}\n\
         📋 Tasks Queued: {}\n\
         ✅ Completed: {}\n\n\
         📈 Efficiency: {:.1}%",
        app.total_agents,
        app.active_agents,
        app.pending_tasks,
        app.completed_tasks,
        if app.total_agents > 0 {
            (app.active_agents as f32 / app.total_agents as f32) * 100.0
        } else {
            0.0
        }
    );

    let activity_paragraph = Paragraph::new(activity_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📊 System Metrics ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::Cyan))
        .wrap(Wrap { trim: true });

    f.render_widget(activity_paragraph, chunks[1]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_stats (lines 1160-1303) ---[0m
fn draw_delegation_stats(f: &mut Frame, area: Rect, app: &App) {
    if app.delegation_decisions.is_empty() {
        let empty_text = "📊 Delegation Statistics\n\n\
                         No delegation data available yet.\n\n\
                         Start analyzing or delegating tasks\n\
                         to see statistics here.";

        let empty_paragraph = Paragraph::new(empty_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📊 Statistics ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::Gray))
            .alignment(Alignment::Center)
            .wrap(Wrap { trim: true });

        f.render_widget(empty_paragraph, area);
        return;
    }

    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Agent distribution chart
    let mut agent_counts = std::collections::HashMap::new();
    let mut total_confidence = 0.0;

    for decision in &app.delegation_decisions {
        *agent_counts
            .entry(decision.recommended_agent.clone())
            .or_insert(0) += 1;
        total_confidence += decision.confidence;
    }

    let total = app.delegation_decisions.len();
    let avg_confidence = total_confidence / total as f64;

    // Create agent distribution table
    let header = Row::new(vec![
        Cell::from("Agent").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Count").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Percentage").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = agent_counts
        .iter()
        .map(|(agent, count)| {
            let percentage = (*count as f64 / total as f64) * 100.0;
            let agent_icon = match agent.as_str() {
                "Frontend" | "frontend" => "🎨",
                "Backend" | "backend" => "⚙️",
                "DevOps" | "devops" => "🔧",
                "QA" | "qa" => "🧪",
                _ => "🤖",
            };

            Row::new(vec![
                Cell::from(format!("{} {}", agent_icon, agent)).style(
                    Style::default()
                        .fg(Color::Cyan)
                        .add_modifier(Modifier::BOLD),
                ),
                Cell::from(format!("{}", count)).style(Style::default().fg(Color::Yellow)),
                Cell::from(format!("{:.1}%", percentage)).style(Style::default().fg(Color::Green)),
            ])
        })
        .collect();

    let distribution_table = Table::new(
        rows,
        [
            Constraint::Length(15), // Agent
            Constraint::Length(8),  // Count
            Constraint::Length(12), // Percentage
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 📊 Agent Distribution ")
            .title_alignment(Alignment::Center),
    );

    f.render_widget(distribution_table, chunks[0]);

    // Statistics summary
    let stats_text = format!(
        "📈 Delegation Summary\n\n\
         Total Delegations: {}\n\
         Average Confidence: {:.1}%\n\n\
         📊 Distribution:\n\
         {}\n\n\
         🎯 Most Delegated: {}\n\
         📅 Latest: {}",
        total,
        avg_confidence * 100.0,
        agent_counts
            .iter()
            .map(|(agent, count)| format!("• {}: {}", agent, count))
            .collect::<Vec<_>>()
            .join("\n         "),
        agent_counts
            .iter()
            .max_by_key(|(_, count)| *count)
            .map(|(agent, _)| agent.as_str())
            .unwrap_or("None"),
        app.delegation_decisions
            .last()
            .map(|d| d.created_at.format("%H:%M:%S").to_string())
            .unwrap_or("None".to_string())
    );

    let stats_paragraph = Paragraph::new(stats_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📋 Statistics Summary ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(stats_paragraph, chunks[1]);
}

  ./crates/ccswarm/src/tui/ui.rs:432-585 function draw_agents <-> ./crates/ccswarm/src/tui/ui.rs:709-810 function draw_logs
  Similarity: 97.05%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_agents (lines 432-585) ---[0m
fn draw_agents(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(65), Constraint::Percentage(35)])
        .split(area);

    // Create table rows for better formatting
    let header = Row::new(vec![
        Cell::from("#").style(Style::default().fg(Color::DarkGray)),
        Cell::from("Name").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Provider").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Type").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Status").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = app
        .agents
        .iter()
        .enumerate()
        .map(|(i, agent)| {
            let (status_color, status_text, status_icon) = match agent.status {
                crate::agent::AgentStatus::Available => (Color::Green, "Available", "✅"),
                crate::agent::AgentStatus::Working => (Color::Cyan, "Working", "⚙️"),
                crate::agent::AgentStatus::Error(_) => (Color::Red, "Error", "❌"),
                crate::agent::AgentStatus::Initializing => (Color::Yellow, "Initializing", "🔄"),
                crate::agent::AgentStatus::WaitingForReview => (Color::Magenta, "Waiting", "⏳"),
                crate::agent::AgentStatus::ShuttingDown => (Color::DarkGray, "Shutting Down", "⏹️"),
            };

            // Special styling for Master Claude Code
            let is_master = agent.specialization.contains("Master");
            let (name_color, name_icon, type_color) = if is_master {
                (Color::Yellow, "👑", Color::Yellow)
            } else {
                let type_icon = match agent.specialization.as_str() {
                    s if s.contains("Frontend") => "🎨",
                    s if s.contains("Backend") => "⚙️",
                    s if s.contains("DevOps") => "🔧",
                    s if s.contains("QA") => "🧪",
                    s if s.contains("Test") => "🔬",
                    s if s.contains("Error") => "🚨",
                    _ => "🤖",
                };
                (Color::Cyan, type_icon, Color::White)
            };

            let provider_display = if is_master {
                format!("{} claude_code", "👑")
            } else {
                format!("{} claude_code", "🤖")
            };

            Row::new(vec![
                Cell::from(format!("{}", i + 1)).style(Style::default().fg(Color::DarkGray)),
                Cell::from(format!("{} {}", name_icon, agent.name)).style(
                    Style::default().fg(name_color).add_modifier(if is_master {
                        Modifier::BOLD
                    } else {
                        Modifier::empty()
                    }),
                ),
                Cell::from(provider_display).style(Style::default().fg(if is_master {
                    Color::Yellow
                } else {
                    Color::Blue
                })),
                Cell::from(agent.specialization.clone()).style(Style::default().fg(type_color)),
                Cell::from(format!("{} {}", status_icon, status_text)).style(
                    Style::default()
                        .fg(status_color)
                        .add_modifier(Modifier::BOLD),
                ),
            ])
        })
        .collect();

    let agents_table = Table::new(
        rows,
        [
            Constraint::Length(3),  // #
            Constraint::Length(18), // Name
            Constraint::Length(16), // Provider
            Constraint::Length(20), // Type
            Constraint::Length(15), // Status
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 🤖 Agent Management Dashboard ")
            .title_alignment(Alignment::Center)
            .style(Style::default().fg(Color::White)),
    )
    .row_highlight_style(
        Style::default()
            .bg(Color::Blue)
            .fg(Color::White)
            .add_modifier(Modifier::BOLD),
    )
    .highlight_symbol("► ");

    // Create table state
    let mut table_state = ratatui::widgets::TableState::default();
    table_state.select(Some(app.selected_agent));

    f.render_stateful_widget(agents_table, chunks[0], &mut table_state);

    // Agent details
    if let Some(agent) = app.agents.get(app.selected_agent) {
        let details = vec![
            format!("ID: {}", agent.id),
            format!("Name: {}", agent.name),
            format!("Type: {}", agent.specialization),
            format!("Provider: {} {}", agent.provider_icon, agent.provider_type),
            format!("Status: {:?}", agent.status),
            format!("Tasks Completed: {}", agent.tasks_completed),
            format!("Last Activity: {}", agent.last_activity.format("%H:%M:%S")),
            format!("Workspace: {}", agent.workspace),
            format!(
                "Current Task: {}",
                agent.current_task.as_deref().unwrap_or("None")
            ),
        ];

        let details_text = details.join("\n");
        let details_paragraph = Paragraph::new(details_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .title(" Agent Details "),
            )
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_logs (lines 709-810) ---[0m
fn draw_logs(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(3), Constraint::Min(0)])
        .split(area);

    // Log level summary bar
    let log_counts = app
        .logs
        .iter()
        .fold(std::collections::HashMap::new(), |mut acc, log| {
            *acc.entry(log.level.as_str()).or_insert(0) += 1;
            acc
        });

    let summary_text = format!(
        "📊 Logs: 🔴 {} errors | 🟡 {} warnings | 🟢 {} info | 🔵 {} debug | Total: {}",
        log_counts.get("ERROR").unwrap_or(&0),
        log_counts.get("WARN").unwrap_or(&0),
        log_counts.get("INFO").unwrap_or(&0),
        log_counts.get("DEBUG").unwrap_or(&0),
        app.logs.len()
    );

    let summary_paragraph = Paragraph::new(summary_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📋 Log Summary ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .alignment(Alignment::Center);

    f.render_widget(summary_paragraph, chunks[0]);

    // Enhanced logs with better visual hierarchy
    let items: Vec<ListItem> = app
        .logs
        .iter()
        .rev() // Show newest logs first
        .take(100) // Limit to prevent performance issues
        .map(|log| {
            let (level_color, level_icon) = match log.level.as_str() {
                "ERROR" => (Color::Red, "🔴"),
                "WARN" => (Color::Yellow, "🟡"),
                "INFO" => (Color::Green, "🟢"),
                "DEBUG" => (Color::Cyan, "🔵"),
                _ => (Color::White, "⚪"),
            };

            let agent_name = log.agent.as_deref().unwrap_or("System");
            let (agent_icon, agent_color) = if agent_name == "System" {
                ("🖥️", Color::Gray)
            } else if agent_name.contains("master") || agent_name.contains("Master") {
                ("👑", Color::Yellow)
            } else {
                ("🤖", Color::Cyan)
            };

            let content = vec![Line::from(vec![
                Span::styled(
                    format!("{} ", log.timestamp.format("%H:%M:%S")),
                    Style::default().fg(Color::DarkGray),
                ),
                Span::styled(format!("{} ", level_icon), Style::default().fg(level_color)),
                Span::styled(
                    format!("{:<5} ", log.level),
                    Style::default()
                        .fg(level_color)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("{} {:<10} ", agent_icon, agent_name),
                    Style::default().fg(agent_color),
                ),
                Span::styled(log.message.clone(), Style::default().fg(Color::White)),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut logs_state = ListState::default();
    logs_state.select(Some(app.selected_log));

    let logs_list = List::new(items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📜 Live Logs (r: refresh, ↑↓: navigate) ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::DarkGray)
                .add_modifier(Modifier::BOLD),
        );

    f.render_stateful_widget(logs_list, chunks[1], &mut logs_state);
}

  ./crates/ccswarm/src/tui/ui.rs:104-227 function draw_enhanced_system_stats <-> ./crates/ccswarm/src/tui/ui.rs:1420-1558 function draw_input_popup
  Similarity: 94.13%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_system_stats (lines 104-227) ---[0m
fn draw_enhanced_system_stats(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
        ])
        .split(area);

    // Enhanced total agents with progress bar
    let agent_percentage = (app.active_agents * 100 / app.total_agents.max(1)) as u16;
    let total_agents = Gauge::default()
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🤖 Active Agents ")
                .title_alignment(Alignment::Center),
        )
        .gauge_style(
            Style::default()
                .fg(Color::Cyan)
                .add_modifier(Modifier::BOLD),
        )
        .percent(agent_percentage)
        .label(format!(
            "{}/{} ({}%)",
            app.active_agents, app.total_agents, agent_percentage
        ));
    f.render_widget(total_agents, chunks[0]);

    // Enhanced pending tasks with icon
    let pending_text = format!("📋 {}\nqueued", app.pending_tasks);
    let pending_tasks = Paragraph::new(pending_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Pending Tasks ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(pending_tasks, chunks[1]);

    // Enhanced completed tasks with icon
    let completed_text = format!("✅ {}\ncompleted", app.completed_tasks);
    let completed_tasks = Paragraph::new(completed_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Completed Tasks ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(Color::Green)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(completed_tasks, chunks[2]);

    // Enhanced system status with icon and color
    let (status_icon, status_color) = match app.system_status.as_str() {
        "Running" => ("🟢", Color::Green),
        "Stopped" => ("🔴", Color::Red),
        _ => ("🟡", Color::Yellow),
    };
    let status_text = format!(
        "{} {}\nsystem",
        status_icon,
        app.system_status.to_lowercase()
    );
    let system_status = Paragraph::new(status_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" System Status ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(status_color)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(system_status, chunks[3]);

    // Add execution engine success rate gauge
    let success_percentage = app.success_rate as u16;
    let success_color = if success_percentage >= 90 {
        Color::Green
    } else if success_percentage >= 70 {
        Color::Yellow
    } else {
        Color::Red
    };

    let success_rate = Gauge::default()
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📊 Success Rate ")
                .title_alignment(Alignment::Center),
        )
        .gauge_style(
            Style::default()
                .fg(success_color)
                .add_modifier(Modifier::BOLD),
        )
        .percent(success_percentage)
        .label(format!("{:.1}%", app.success_rate));
    f.render_widget(success_rate, chunks[4]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_input_popup (lines 1420-1558) ---[0m
fn draw_input_popup(f: &mut Frame, app: &App) {
    let area = centered_rect(70, 25, f.area());

    let (title, _title_icon, border_color) = match app.input_mode {
        InputMode::AddingTask => (" 📋 Add New Task ", "📋", Color::Green),
        InputMode::CreatingAgent => (" 🤖 Create New Agent ", "🤖", Color::Blue),
        InputMode::Command => (" 💻 Command Prompt ", "💻", Color::Yellow),
        InputMode::DelegationInput => (" 🎯 Master Delegation ", "🎯", Color::Magenta),
        _ => (" ✏️  Input ", "✏️", Color::White),
    };

    let prompt = match app.input_mode {
        InputMode::AddingTask => {
            "Enter task description:\n\n\
             💡 Tips:\n\
             • Use [high]/[medium]/[low] for priority\n\
             • Use [test]/[docs]/[bug]/[feature] for type\n\
             • Example: \"Fix login bug [high] [bug]\""
        }
        InputMode::CreatingAgent => {
            "Select agent specialization:\n\n\
             🎨 frontend  - UI/UX development\n\
             ⚙️  backend   - API/Database work\n\
             🔧 devops    - Infrastructure/Deploy\n\
             🧪 qa        - Testing/Quality assurance"
        }
        InputMode::Command => {
            "Enter command:\n\n\
             💡 Quick commands:\n\
             • help - Show all commands\n\
             • start_agent master - Start master agent\n\
             • task <description> - Add new task\n\
             • status - Show system status"
        }
        InputMode::DelegationInput => match app.delegation_mode {
            DelegationMode::Analyze => {
                "🔍 Analyze Task for Delegation:\n\n\
                     Enter task description to get Master's recommendation\n\
                     💡 Examples:\n\
                     • \"Create login form with validation\"\n\
                     • \"Fix API endpoint error handling\"\n\
                     • \"Write unit tests for user service\""
            }
            DelegationMode::Delegate => {
                "🎯 Delegate Task to Agent:\n\n\
                     Format: <agent_type> <task_description>\n\
                     💡 Examples:\n\
                     • \"frontend Create responsive navigation\"\n\
                     • \"backend Add user authentication API\"\n\
                     • \"qa Write integration tests\""
            }
            _ => "Enter delegation input:",
        },
        _ => "Enter your input:",
    };

    // Create a shadow effect
    let shadow_area = Rect {
        x: area.x + 1,
        y: area.y + 1,
        width: area.width,
        height: area.height,
    };
    f.render_widget(
        Block::default().style(Style::default().bg(Color::Black)),
        shadow_area,
    );

    f.render_widget(Clear, area); // Clear background

    let input_block = Block::default()
        .title(title)
        .title_alignment(Alignment::Center)
        .borders(Borders::ALL)
        .border_type(BorderType::Rounded)
        .border_style(
            Style::default()
                .fg(border_color)
                .add_modifier(Modifier::BOLD),
        );

    let input_area = input_block.inner(area);
    f.render_widget(input_block, area);

    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(8), // Prompt area
            Constraint::Length(3), // Input area
            Constraint::Length(2), // Help area
        ])
        .split(input_area);

    // Prompt text
    let prompt_paragraph = Paragraph::new(prompt)
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });
    f.render_widget(prompt_paragraph, chunks[0]);

    // Input box with cursor
    let input_text = format!("{}_", app.input_buffer);
    let input_paragraph = Paragraph::new(input_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Input ")
                .border_style(Style::default().fg(Color::Cyan)),
        )
        .style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        );
    f.render_widget(input_paragraph, chunks[1]);

    // Help text
    let help_text = vec![Line::from(vec![
        Span::styled(
            " Enter ",
            Style::default()
                .fg(Color::Black)
                .bg(Color::Green)
                .add_modifier(Modifier::BOLD),
        ),
        Span::styled(" Confirm ", Style::default().fg(Color::Gray)),
        Span::raw("  "),
        Span::styled(
            " Esc ",
            Style::default()
                .fg(Color::Black)
                .bg(Color::Red)
                .add_modifier(Modifier::BOLD),
        ),
        Span::styled(" Cancel ", Style::default().fg(Color::Gray)),
    ])];
    let help_paragraph = Paragraph::new(help_text).alignment(Alignment::Center);
    f.render_widget(help_paragraph, chunks[2]);
}

  ./crates/ccswarm/src/tui/ui.rs:104-227 function draw_enhanced_system_stats <-> ./crates/ccswarm/src/tui/ui.rs:432-585 function draw_agents
  Similarity: 88.77%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_system_stats (lines 104-227) ---[0m
fn draw_enhanced_system_stats(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
        ])
        .split(area);

    // Enhanced total agents with progress bar
    let agent_percentage = (app.active_agents * 100 / app.total_agents.max(1)) as u16;
    let total_agents = Gauge::default()
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🤖 Active Agents ")
                .title_alignment(Alignment::Center),
        )
        .gauge_style(
            Style::default()
                .fg(Color::Cyan)
                .add_modifier(Modifier::BOLD),
        )
        .percent(agent_percentage)
        .label(format!(
            "{}/{} ({}%)",
            app.active_agents, app.total_agents, agent_percentage
        ));
    f.render_widget(total_agents, chunks[0]);

    // Enhanced pending tasks with icon
    let pending_text = format!("📋 {}\nqueued", app.pending_tasks);
    let pending_tasks = Paragraph::new(pending_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Pending Tasks ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(pending_tasks, chunks[1]);

    // Enhanced completed tasks with icon
    let completed_text = format!("✅ {}\ncompleted", app.completed_tasks);
    let completed_tasks = Paragraph::new(completed_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Completed Tasks ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(Color::Green)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(completed_tasks, chunks[2]);

    // Enhanced system status with icon and color
    let (status_icon, status_color) = match app.system_status.as_str() {
        "Running" => ("🟢", Color::Green),
        "Stopped" => ("🔴", Color::Red),
        _ => ("🟡", Color::Yellow),
    };
    let status_text = format!(
        "{} {}\nsystem",
        status_icon,
        app.system_status.to_lowercase()
    );
    let system_status = Paragraph::new(status_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" System Status ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(status_color)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(system_status, chunks[3]);

    // Add execution engine success rate gauge
    let success_percentage = app.success_rate as u16;
    let success_color = if success_percentage >= 90 {
        Color::Green
    } else if success_percentage >= 70 {
        Color::Yellow
    } else {
        Color::Red
    };

    let success_rate = Gauge::default()
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📊 Success Rate ")
                .title_alignment(Alignment::Center),
        )
        .gauge_style(
            Style::default()
                .fg(success_color)
                .add_modifier(Modifier::BOLD),
        )
        .percent(success_percentage)
        .label(format!("{:.1}%", app.success_rate));
    f.render_widget(success_rate, chunks[4]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_agents (lines 432-585) ---[0m
fn draw_agents(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(65), Constraint::Percentage(35)])
        .split(area);

    // Create table rows for better formatting
    let header = Row::new(vec![
        Cell::from("#").style(Style::default().fg(Color::DarkGray)),
        Cell::from("Name").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Provider").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Type").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Status").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = app
        .agents
        .iter()
        .enumerate()
        .map(|(i, agent)| {
            let (status_color, status_text, status_icon) = match agent.status {
                crate::agent::AgentStatus::Available => (Color::Green, "Available", "✅"),
                crate::agent::AgentStatus::Working => (Color::Cyan, "Working", "⚙️"),
                crate::agent::AgentStatus::Error(_) => (Color::Red, "Error", "❌"),
                crate::agent::AgentStatus::Initializing => (Color::Yellow, "Initializing", "🔄"),
                crate::agent::AgentStatus::WaitingForReview => (Color::Magenta, "Waiting", "⏳"),
                crate::agent::AgentStatus::ShuttingDown => (Color::DarkGray, "Shutting Down", "⏹️"),
            };

            // Special styling for Master Claude Code
            let is_master = agent.specialization.contains("Master");
            let (name_color, name_icon, type_color) = if is_master {
                (Color::Yellow, "👑", Color::Yellow)
            } else {
                let type_icon = match agent.specialization.as_str() {
                    s if s.contains("Frontend") => "🎨",
                    s if s.contains("Backend") => "⚙️",
                    s if s.contains("DevOps") => "🔧",
                    s if s.contains("QA") => "🧪",
                    s if s.contains("Test") => "🔬",
                    s if s.contains("Error") => "🚨",
                    _ => "🤖",
                };
                (Color::Cyan, type_icon, Color::White)
            };

            let provider_display = if is_master {
                format!("{} claude_code", "👑")
            } else {
                format!("{} claude_code", "🤖")
            };

            Row::new(vec![
                Cell::from(format!("{}", i + 1)).style(Style::default().fg(Color::DarkGray)),
                Cell::from(format!("{} {}", name_icon, agent.name)).style(
                    Style::default().fg(name_color).add_modifier(if is_master {
                        Modifier::BOLD
                    } else {
                        Modifier::empty()
                    }),
                ),
                Cell::from(provider_display).style(Style::default().fg(if is_master {
                    Color::Yellow
                } else {
                    Color::Blue
                })),
                Cell::from(agent.specialization.clone()).style(Style::default().fg(type_color)),
                Cell::from(format!("{} {}", status_icon, status_text)).style(
                    Style::default()
                        .fg(status_color)
                        .add_modifier(Modifier::BOLD),
                ),
            ])
        })
        .collect();

    let agents_table = Table::new(
        rows,
        [
            Constraint::Length(3),  // #
            Constraint::Length(18), // Name
            Constraint::Length(16), // Provider
            Constraint::Length(20), // Type
            Constraint::Length(15), // Status
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 🤖 Agent Management Dashboard ")
            .title_alignment(Alignment::Center)
            .style(Style::default().fg(Color::White)),
    )
    .row_highlight_style(
        Style::default()
            .bg(Color::Blue)
            .fg(Color::White)
            .add_modifier(Modifier::BOLD),
    )
    .highlight_symbol("► ");

    // Create table state
    let mut table_state = ratatui::widgets::TableState::default();
    table_state.select(Some(app.selected_agent));

    f.render_stateful_widget(agents_table, chunks[0], &mut table_state);

    // Agent details
    if let Some(agent) = app.agents.get(app.selected_agent) {
        let details = vec![
            format!("ID: {}", agent.id),
            format!("Name: {}", agent.name),
            format!("Type: {}", agent.specialization),
            format!("Provider: {} {}", agent.provider_icon, agent.provider_type),
            format!("Status: {:?}", agent.status),
            format!("Tasks Completed: {}", agent.tasks_completed),
            format!("Last Activity: {}", agent.last_activity.format("%H:%M:%S")),
            format!("Workspace: {}", agent.workspace),
            format!(
                "Current Task: {}",
                agent.current_task.as_deref().unwrap_or("None")
            ),
        ];

        let details_text = details.join("\n");
        let details_paragraph = Paragraph::new(details_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .title(" Agent Details "),
            )
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    }
}

  ./crates/ccswarm/src/tui/ui.rs:588-706 function draw_tasks <-> ./crates/ccswarm/src/tui/ui.rs:1017-1157 function draw_delegation_interface
  Similarity: 94.67%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_tasks (lines 588-706) ---[0m
fn draw_tasks(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Percentage(70), Constraint::Percentage(30)])
        .split(area);

    // Task list with enhanced headers
    let header = Row::new(vec![
        "#",
        "Description",
        "Priority",
        "Type",
        "Status",
        "Agent",
        "Created",
    ])
    .style(Style::default().fg(Color::Yellow))
    .height(1)
    .bottom_margin(1);

    let rows: Vec<Row> = app
        .tasks
        .iter()
        .enumerate()
        .map(|(i, task)| {
            let priority_color = match task.priority.as_str() {
                "High" => Color::Red,
                "Medium" => Color::Yellow,
                "Low" => Color::Green,
                _ => Color::White,
            };

            let status_color = if task.status.contains("✅") {
                Color::Green
            } else if task.status.contains("❌") {
                Color::Red
            } else if task.status.contains("🏃") {
                Color::Cyan
            } else if task.status.contains("📋") {
                Color::Yellow
            } else {
                Color::White
            };

            Row::new(vec![
                Cell::from(format!("{}", i + 1)),
                Cell::from(task.description.clone()),
                Cell::from(Span::styled(
                    task.priority.clone(),
                    Style::default().fg(priority_color),
                )),
                Cell::from(task.task_type.clone()),
                Cell::from(Span::styled(
                    task.status.clone(),
                    Style::default().fg(status_color),
                )),
                Cell::from(task.assigned_agent.as_deref().unwrap_or("-")),
                Cell::from(task.created_at.format("%H:%M:%S").to_string()),
            ])
        })
        .collect();

    let selected_style = Style::default()
        .bg(Color::Yellow)
        .fg(Color::Black)
        .add_modifier(Modifier::BOLD);

    let table = Table::new(
        rows,
        &[
            Constraint::Length(3),  // #
            Constraint::Min(25),    // Description
            Constraint::Length(8),  // Priority
            Constraint::Length(12), // Type
            Constraint::Length(15), // Status
            Constraint::Length(12), // Agent
            Constraint::Length(8),  // Created
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .title(" Tasks (t to add new task) "),
    )
    .row_highlight_style(selected_style);

    let mut table_state = ratatui::widgets::TableState::default();
    table_state.select(Some(app.selected_task));

    f.render_stateful_widget(table, chunks[0], &mut table_state);

    // Task details
    if let Some(task) = app.tasks.get(app.selected_task) {
        let details = [
            format!("ID: {}", task.id),
            format!("Description: {}", task.description),
            format!("Priority: {}", task.priority),
            format!("Type: {}", task.task_type),
            format!("Status: {}", task.status),
            format!("Created: {}", task.created_at.format("%Y-%m-%d %H:%M:%S")),
            format!(
                "Assigned Agent: {}",
                task.assigned_agent.as_deref().unwrap_or("None")
            ),
        ];

        let details_text = details.join("\n");
        let details_paragraph = Paragraph::new(details_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .title(" Task Details "),
            )
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_interface (lines 1017-1157) ---[0m
fn draw_delegation_interface(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(40), Constraint::Percentage(60)])
        .split(area);

    // Available agents
    let agent_items: Vec<ListItem> = vec![
        ListItem::new(Line::from(vec![
            Span::styled("🎨 ", Style::default().fg(Color::Cyan)),
            Span::styled(
                "Frontend",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - UI/UX Development", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("⚙️ ", Style::default().fg(Color::Green)),
            Span::styled(
                "Backend",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - API/Database Work", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("🔧 ", Style::default().fg(Color::Blue)),
            Span::styled(
                "DevOps",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - Infrastructure/Deploy", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("🧪 ", Style::default().fg(Color::Magenta)),
            Span::styled(
                "QA",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - Testing/Quality", Style::default().fg(Color::Gray)),
        ])),
    ];

    let agents_list = List::new(agent_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🤖 Available Agents ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Green)
                .add_modifier(Modifier::BOLD),
        );

    f.render_widget(agents_list, chunks[0]);

    // Delegation form and recent delegations
    let right_chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(8), Constraint::Min(0)])
        .split(chunks[1]);

    // Delegation form
    let form_text = "🎯 Manual Task Delegation\n\n\
                     Format: <agent> <task_description>\n\n\
                     Examples:\n\
                     • frontend Create user dashboard\n\
                     • backend Add payment processing\n\
                     • qa Write end-to-end tests\n\
                     • devops Setup CI/CD pipeline";

    let form_paragraph = Paragraph::new(form_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📝 Delegation Form ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(form_paragraph, right_chunks[0]);

    // Recent delegations
    let delegation_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(6)
        .filter(|d| d.reasoning.contains("Manual delegation"))
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "frontend" | "Frontend" => "🎨",
                "backend" | "Backend" => "⚙️",
                "devops" | "DevOps" => "🔧",
                "qa" | "QA" => "🧪",
                _ => "🤖",
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::Green)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("{} ", decision.created_at.format("%H:%M")),
                    Style::default().fg(Color::DarkGray),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::White),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let delegations_list = List::new(delegation_items).block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 📋 Recent Delegations ")
            .title_alignment(Alignment::Center),
    );

    f.render_widget(delegations_list, right_chunks[1]);
}

  ./crates/ccswarm/src/tui/ui.rs:104-227 function draw_enhanced_system_stats <-> ./crates/ccswarm/src/tui/ui.rs:1160-1303 function draw_delegation_stats
  Similarity: 91.84%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_system_stats (lines 104-227) ---[0m
fn draw_enhanced_system_stats(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
        ])
        .split(area);

    // Enhanced total agents with progress bar
    let agent_percentage = (app.active_agents * 100 / app.total_agents.max(1)) as u16;
    let total_agents = Gauge::default()
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🤖 Active Agents ")
                .title_alignment(Alignment::Center),
        )
        .gauge_style(
            Style::default()
                .fg(Color::Cyan)
                .add_modifier(Modifier::BOLD),
        )
        .percent(agent_percentage)
        .label(format!(
            "{}/{} ({}%)",
            app.active_agents, app.total_agents, agent_percentage
        ));
    f.render_widget(total_agents, chunks[0]);

    // Enhanced pending tasks with icon
    let pending_text = format!("📋 {}\nqueued", app.pending_tasks);
    let pending_tasks = Paragraph::new(pending_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Pending Tasks ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(pending_tasks, chunks[1]);

    // Enhanced completed tasks with icon
    let completed_text = format!("✅ {}\ncompleted", app.completed_tasks);
    let completed_tasks = Paragraph::new(completed_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Completed Tasks ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(Color::Green)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(completed_tasks, chunks[2]);

    // Enhanced system status with icon and color
    let (status_icon, status_color) = match app.system_status.as_str() {
        "Running" => ("🟢", Color::Green),
        "Stopped" => ("🔴", Color::Red),
        _ => ("🟡", Color::Yellow),
    };
    let status_text = format!(
        "{} {}\nsystem",
        status_icon,
        app.system_status.to_lowercase()
    );
    let system_status = Paragraph::new(status_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" System Status ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(status_color)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(system_status, chunks[3]);

    // Add execution engine success rate gauge
    let success_percentage = app.success_rate as u16;
    let success_color = if success_percentage >= 90 {
        Color::Green
    } else if success_percentage >= 70 {
        Color::Yellow
    } else {
        Color::Red
    };

    let success_rate = Gauge::default()
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📊 Success Rate ")
                .title_alignment(Alignment::Center),
        )
        .gauge_style(
            Style::default()
                .fg(success_color)
                .add_modifier(Modifier::BOLD),
        )
        .percent(success_percentage)
        .label(format!("{:.1}%", app.success_rate));
    f.render_widget(success_rate, chunks[4]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_stats (lines 1160-1303) ---[0m
fn draw_delegation_stats(f: &mut Frame, area: Rect, app: &App) {
    if app.delegation_decisions.is_empty() {
        let empty_text = "📊 Delegation Statistics\n\n\
                         No delegation data available yet.\n\n\
                         Start analyzing or delegating tasks\n\
                         to see statistics here.";

        let empty_paragraph = Paragraph::new(empty_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📊 Statistics ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::Gray))
            .alignment(Alignment::Center)
            .wrap(Wrap { trim: true });

        f.render_widget(empty_paragraph, area);
        return;
    }

    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Agent distribution chart
    let mut agent_counts = std::collections::HashMap::new();
    let mut total_confidence = 0.0;

    for decision in &app.delegation_decisions {
        *agent_counts
            .entry(decision.recommended_agent.clone())
            .or_insert(0) += 1;
        total_confidence += decision.confidence;
    }

    let total = app.delegation_decisions.len();
    let avg_confidence = total_confidence / total as f64;

    // Create agent distribution table
    let header = Row::new(vec![
        Cell::from("Agent").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Count").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Percentage").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = agent_counts
        .iter()
        .map(|(agent, count)| {
            let percentage = (*count as f64 / total as f64) * 100.0;
            let agent_icon = match agent.as_str() {
                "Frontend" | "frontend" => "🎨",
                "Backend" | "backend" => "⚙️",
                "DevOps" | "devops" => "🔧",
                "QA" | "qa" => "🧪",
                _ => "🤖",
            };

            Row::new(vec![
                Cell::from(format!("{} {}", agent_icon, agent)).style(
                    Style::default()
                        .fg(Color::Cyan)
                        .add_modifier(Modifier::BOLD),
                ),
                Cell::from(format!("{}", count)).style(Style::default().fg(Color::Yellow)),
                Cell::from(format!("{:.1}%", percentage)).style(Style::default().fg(Color::Green)),
            ])
        })
        .collect();

    let distribution_table = Table::new(
        rows,
        [
            Constraint::Length(15), // Agent
            Constraint::Length(8),  // Count
            Constraint::Length(12), // Percentage
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 📊 Agent Distribution ")
            .title_alignment(Alignment::Center),
    );

    f.render_widget(distribution_table, chunks[0]);

    // Statistics summary
    let stats_text = format!(
        "📈 Delegation Summary\n\n\
         Total Delegations: {}\n\
         Average Confidence: {:.1}%\n\n\
         📊 Distribution:\n\
         {}\n\n\
         🎯 Most Delegated: {}\n\
         📅 Latest: {}",
        total,
        avg_confidence * 100.0,
        agent_counts
            .iter()
            .map(|(agent, count)| format!("• {}: {}", agent, count))
            .collect::<Vec<_>>()
            .join("\n         "),
        agent_counts
            .iter()
            .max_by_key(|(_, count)| *count)
            .map(|(agent, _)| agent.as_str())
            .unwrap_or("None"),
        app.delegation_decisions
            .last()
            .map(|d| d.created_at.format("%H:%M:%S").to_string())
            .unwrap_or("None".to_string())
    );

    let stats_paragraph = Paragraph::new(stats_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📋 Statistics Summary ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(stats_paragraph, chunks[1]);
}

  ./crates/ccswarm/src/tui/ui.rs:298-429 function draw_enhanced_agent_summary <-> ./crates/ccswarm/src/tui/ui.rs:894-1014 function draw_delegation_analysis
  Similarity: 96.71%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_agent_summary (lines 298-429) ---[0m
fn draw_enhanced_agent_summary(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(60), Constraint::Percentage(40)])
        .split(area);

    // Enhanced agents table
    let header = Row::new(vec![
        Cell::from("Agent").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Type").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Status").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Tasks").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = app
        .agents
        .iter()
        .take(6)
        .map(|agent| {
            let (status_color, status_text, status_icon) = match agent.status {
                crate::agent::AgentStatus::Available => (Color::Green, "Available", "✅"),
                crate::agent::AgentStatus::Working => (Color::Cyan, "Working", "⚙️"),
                crate::agent::AgentStatus::Error(_) => (Color::Red, "Error", "❌"),
                crate::agent::AgentStatus::Initializing => (Color::Yellow, "Init", "🔄"),
                crate::agent::AgentStatus::WaitingForReview => (Color::Magenta, "Review", "⏳"),
                crate::agent::AgentStatus::ShuttingDown => (Color::DarkGray, "Shutdown", "⏹️"),
            };

            let is_master = agent.specialization.contains("Master");
            let (name_icon, name_color) = if is_master {
                ("👑", Color::Yellow)
            } else {
                match agent.specialization.as_str() {
                    s if s.contains("Frontend") => ("🎨", Color::Cyan),
                    s if s.contains("Backend") => ("⚙️", Color::Green),
                    s if s.contains("DevOps") => ("🔧", Color::Blue),
                    s if s.contains("QA") => ("🧪", Color::Magenta),
                    _ => ("🤖", Color::White),
                }
            };

            Row::new(vec![
                Cell::from(format!("{} {}", name_icon, agent.name)).style(
                    Style::default().fg(name_color).add_modifier(if is_master {
                        Modifier::BOLD
                    } else {
                        Modifier::empty()
                    }),
                ),
                Cell::from(agent.specialization.clone()).style(Style::default().fg(Color::White)),
                Cell::from(format!("{} {}", status_icon, status_text))
                    .style(Style::default().fg(status_color)),
                Cell::from(format!("{}", agent.tasks_completed))
                    .style(Style::default().fg(Color::Yellow)),
            ])
        })
        .collect();

    let agents_table = Table::new(
        rows,
        [
            Constraint::Length(15), // Agent
            Constraint::Length(12), // Type
            Constraint::Length(12), // Status
            Constraint::Length(6),  // Tasks
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 👥 Agents Overview ")
            .title_alignment(Alignment::Center),
    )
    .row_highlight_style(
        Style::default()
            .bg(Color::DarkGray)
            .add_modifier(Modifier::BOLD),
    );

    f.render_widget(agents_table, chunks[0]);

    // System activity summary
    let activity_text = format!(
        "🔄 System Activity\n\n\
         ⏱️  Uptime: Active\n\
         📊 Total Agents: {}\n\
         🎯 Active: {}\n\
         📋 Tasks Queued: {}\n\
         ✅ Completed: {}\n\n\
         📈 Efficiency: {:.1}%",
        app.total_agents,
        app.active_agents,
        app.pending_tasks,
        app.completed_tasks,
        if app.total_agents > 0 {
            (app.active_agents as f32 / app.total_agents as f32) * 100.0
        } else {
            0.0
        }
    );

    let activity_paragraph = Paragraph::new(activity_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📊 System Metrics ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::Cyan))
        .wrap(Wrap { trim: true });

    f.render_widget(activity_paragraph, chunks[1]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_analysis (lines 894-1014) ---[0m
fn draw_delegation_analysis(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Recent analysis results
    let analysis_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(10)
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "Frontend" => "🎨",
                "Backend" => "⚙️",
                "DevOps" => "🔧",
                "QA" => "🧪",
                _ => "🤖",
            };

            let confidence_color = if decision.confidence >= 0.8 {
                Color::Green
            } else if decision.confidence >= 0.6 {
                Color::Yellow
            } else {
                Color::Red
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::White)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("({:.0}%) ", decision.confidence * 100.0),
                    Style::default().fg(confidence_color),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::Gray),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut analysis_state = ListState::default();
    analysis_state.select(Some(app.selected_delegation));

    let analysis_list = List::new(analysis_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🔍 Analysis Results ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Blue)
                .add_modifier(Modifier::BOLD),
        )
        .highlight_symbol("► ");

    f.render_stateful_widget(analysis_list, chunks[0], &mut analysis_state);

    // Analysis details
    if let Some(decision) = app.delegation_decisions.get(app.selected_delegation) {
        let details = format!(
            "📋 Task Analysis Details\n\n\
             Task: {}\n\n\
             🎯 Recommended Agent: {}\n\
             📊 Confidence: {:.1}%\n\
             🧠 Reasoning: {}\n\
             📅 Analyzed: {}\n\n\
             💡 Press Enter to delegate this task",
            decision.task_description,
            decision.recommended_agent,
            decision.confidence * 100.0,
            decision.reasoning,
            decision.created_at.format("%H:%M:%S")
        );

        let details_paragraph = Paragraph::new(details)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Analysis Details ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::White))
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    } else {
        let empty_text = "🔍 Task Analysis\n\n\
                         No analysis results yet.\n\n\
                         Press Enter to analyze a new task\n\
                         and get Master's recommendation\n\
                         for optimal agent assignment.";

        let empty_paragraph = Paragraph::new(empty_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Ready for Analysis ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::Gray))
            .alignment(Alignment::Center)
            .wrap(Wrap { trim: true });

        f.render_widget(empty_paragraph, chunks[1]);
    }
}

  ./crates/ccswarm/src/tui/ui.rs:894-1014 function draw_delegation_analysis <-> ./crates/ccswarm/src/tui/ui.rs:1017-1157 function draw_delegation_interface
  Similarity: 93.02%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_analysis (lines 894-1014) ---[0m
fn draw_delegation_analysis(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Recent analysis results
    let analysis_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(10)
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "Frontend" => "🎨",
                "Backend" => "⚙️",
                "DevOps" => "🔧",
                "QA" => "🧪",
                _ => "🤖",
            };

            let confidence_color = if decision.confidence >= 0.8 {
                Color::Green
            } else if decision.confidence >= 0.6 {
                Color::Yellow
            } else {
                Color::Red
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::White)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("({:.0}%) ", decision.confidence * 100.0),
                    Style::default().fg(confidence_color),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::Gray),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut analysis_state = ListState::default();
    analysis_state.select(Some(app.selected_delegation));

    let analysis_list = List::new(analysis_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🔍 Analysis Results ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Blue)
                .add_modifier(Modifier::BOLD),
        )
        .highlight_symbol("► ");

    f.render_stateful_widget(analysis_list, chunks[0], &mut analysis_state);

    // Analysis details
    if let Some(decision) = app.delegation_decisions.get(app.selected_delegation) {
        let details = format!(
            "📋 Task Analysis Details\n\n\
             Task: {}\n\n\
             🎯 Recommended Agent: {}\n\
             📊 Confidence: {:.1}%\n\
             🧠 Reasoning: {}\n\
             📅 Analyzed: {}\n\n\
             💡 Press Enter to delegate this task",
            decision.task_description,
            decision.recommended_agent,
            decision.confidence * 100.0,
            decision.reasoning,
            decision.created_at.format("%H:%M:%S")
        );

        let details_paragraph = Paragraph::new(details)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Analysis Details ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::White))
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    } else {
        let empty_text = "🔍 Task Analysis\n\n\
                         No analysis results yet.\n\n\
                         Press Enter to analyze a new task\n\
                         and get Master's recommendation\n\
                         for optimal agent assignment.";

        let empty_paragraph = Paragraph::new(empty_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Ready for Analysis ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::Gray))
            .alignment(Alignment::Center)
            .wrap(Wrap { trim: true });

        f.render_widget(empty_paragraph, chunks[1]);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_interface (lines 1017-1157) ---[0m
fn draw_delegation_interface(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(40), Constraint::Percentage(60)])
        .split(area);

    // Available agents
    let agent_items: Vec<ListItem> = vec![
        ListItem::new(Line::from(vec![
            Span::styled("🎨 ", Style::default().fg(Color::Cyan)),
            Span::styled(
                "Frontend",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - UI/UX Development", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("⚙️ ", Style::default().fg(Color::Green)),
            Span::styled(
                "Backend",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - API/Database Work", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("🔧 ", Style::default().fg(Color::Blue)),
            Span::styled(
                "DevOps",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - Infrastructure/Deploy", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("🧪 ", Style::default().fg(Color::Magenta)),
            Span::styled(
                "QA",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - Testing/Quality", Style::default().fg(Color::Gray)),
        ])),
    ];

    let agents_list = List::new(agent_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🤖 Available Agents ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Green)
                .add_modifier(Modifier::BOLD),
        );

    f.render_widget(agents_list, chunks[0]);

    // Delegation form and recent delegations
    let right_chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(8), Constraint::Min(0)])
        .split(chunks[1]);

    // Delegation form
    let form_text = "🎯 Manual Task Delegation\n\n\
                     Format: <agent> <task_description>\n\n\
                     Examples:\n\
                     • frontend Create user dashboard\n\
                     • backend Add payment processing\n\
                     • qa Write end-to-end tests\n\
                     • devops Setup CI/CD pipeline";

    let form_paragraph = Paragraph::new(form_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📝 Delegation Form ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(form_paragraph, right_chunks[0]);

    // Recent delegations
    let delegation_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(6)
        .filter(|d| d.reasoning.contains("Manual delegation"))
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "frontend" | "Frontend" => "🎨",
                "backend" | "Backend" => "⚙️",
                "devops" | "DevOps" => "🔧",
                "qa" | "QA" => "🧪",
                _ => "🤖",
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::Green)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("{} ", decision.created_at.format("%H:%M")),
                    Style::default().fg(Color::DarkGray),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::White),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let delegations_list = List::new(delegation_items).block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 📋 Recent Delegations ")
            .title_alignment(Alignment::Center),
    );

    f.render_widget(delegations_list, right_chunks[1]);
}

  ./crates/ccswarm/src/tui/ui.rs:104-227 function draw_enhanced_system_stats <-> ./crates/ccswarm/src/tui/ui.rs:1017-1157 function draw_delegation_interface
  Similarity: 91.61%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_system_stats (lines 104-227) ---[0m
fn draw_enhanced_system_stats(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
        ])
        .split(area);

    // Enhanced total agents with progress bar
    let agent_percentage = (app.active_agents * 100 / app.total_agents.max(1)) as u16;
    let total_agents = Gauge::default()
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🤖 Active Agents ")
                .title_alignment(Alignment::Center),
        )
        .gauge_style(
            Style::default()
                .fg(Color::Cyan)
                .add_modifier(Modifier::BOLD),
        )
        .percent(agent_percentage)
        .label(format!(
            "{}/{} ({}%)",
            app.active_agents, app.total_agents, agent_percentage
        ));
    f.render_widget(total_agents, chunks[0]);

    // Enhanced pending tasks with icon
    let pending_text = format!("📋 {}\nqueued", app.pending_tasks);
    let pending_tasks = Paragraph::new(pending_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Pending Tasks ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(pending_tasks, chunks[1]);

    // Enhanced completed tasks with icon
    let completed_text = format!("✅ {}\ncompleted", app.completed_tasks);
    let completed_tasks = Paragraph::new(completed_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Completed Tasks ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(Color::Green)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(completed_tasks, chunks[2]);

    // Enhanced system status with icon and color
    let (status_icon, status_color) = match app.system_status.as_str() {
        "Running" => ("🟢", Color::Green),
        "Stopped" => ("🔴", Color::Red),
        _ => ("🟡", Color::Yellow),
    };
    let status_text = format!(
        "{} {}\nsystem",
        status_icon,
        app.system_status.to_lowercase()
    );
    let system_status = Paragraph::new(status_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" System Status ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(status_color)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(system_status, chunks[3]);

    // Add execution engine success rate gauge
    let success_percentage = app.success_rate as u16;
    let success_color = if success_percentage >= 90 {
        Color::Green
    } else if success_percentage >= 70 {
        Color::Yellow
    } else {
        Color::Red
    };

    let success_rate = Gauge::default()
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📊 Success Rate ")
                .title_alignment(Alignment::Center),
        )
        .gauge_style(
            Style::default()
                .fg(success_color)
                .add_modifier(Modifier::BOLD),
        )
        .percent(success_percentage)
        .label(format!("{:.1}%", app.success_rate));
    f.render_widget(success_rate, chunks[4]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_interface (lines 1017-1157) ---[0m
fn draw_delegation_interface(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(40), Constraint::Percentage(60)])
        .split(area);

    // Available agents
    let agent_items: Vec<ListItem> = vec![
        ListItem::new(Line::from(vec![
            Span::styled("🎨 ", Style::default().fg(Color::Cyan)),
            Span::styled(
                "Frontend",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - UI/UX Development", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("⚙️ ", Style::default().fg(Color::Green)),
            Span::styled(
                "Backend",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - API/Database Work", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("🔧 ", Style::default().fg(Color::Blue)),
            Span::styled(
                "DevOps",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - Infrastructure/Deploy", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("🧪 ", Style::default().fg(Color::Magenta)),
            Span::styled(
                "QA",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - Testing/Quality", Style::default().fg(Color::Gray)),
        ])),
    ];

    let agents_list = List::new(agent_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🤖 Available Agents ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Green)
                .add_modifier(Modifier::BOLD),
        );

    f.render_widget(agents_list, chunks[0]);

    // Delegation form and recent delegations
    let right_chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(8), Constraint::Min(0)])
        .split(chunks[1]);

    // Delegation form
    let form_text = "🎯 Manual Task Delegation\n\n\
                     Format: <agent> <task_description>\n\n\
                     Examples:\n\
                     • frontend Create user dashboard\n\
                     • backend Add payment processing\n\
                     • qa Write end-to-end tests\n\
                     • devops Setup CI/CD pipeline";

    let form_paragraph = Paragraph::new(form_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📝 Delegation Form ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(form_paragraph, right_chunks[0]);

    // Recent delegations
    let delegation_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(6)
        .filter(|d| d.reasoning.contains("Manual delegation"))
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "frontend" | "Frontend" => "🎨",
                "backend" | "Backend" => "⚙️",
                "devops" | "DevOps" => "🔧",
                "qa" | "QA" => "🧪",
                _ => "🤖",
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::Green)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("{} ", decision.created_at.format("%H:%M")),
                    Style::default().fg(Color::DarkGray),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::White),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let delegations_list = List::new(delegation_items).block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 📋 Recent Delegations ")
            .title_alignment(Alignment::Center),
    );

    f.render_widget(delegations_list, right_chunks[1]);
}

  ./crates/ccswarm/src/tui/ui.rs:298-429 function draw_enhanced_agent_summary <-> ./crates/ccswarm/src/tui/ui.rs:588-706 function draw_tasks
  Similarity: 96.48%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_agent_summary (lines 298-429) ---[0m
fn draw_enhanced_agent_summary(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(60), Constraint::Percentage(40)])
        .split(area);

    // Enhanced agents table
    let header = Row::new(vec![
        Cell::from("Agent").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Type").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Status").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Tasks").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = app
        .agents
        .iter()
        .take(6)
        .map(|agent| {
            let (status_color, status_text, status_icon) = match agent.status {
                crate::agent::AgentStatus::Available => (Color::Green, "Available", "✅"),
                crate::agent::AgentStatus::Working => (Color::Cyan, "Working", "⚙️"),
                crate::agent::AgentStatus::Error(_) => (Color::Red, "Error", "❌"),
                crate::agent::AgentStatus::Initializing => (Color::Yellow, "Init", "🔄"),
                crate::agent::AgentStatus::WaitingForReview => (Color::Magenta, "Review", "⏳"),
                crate::agent::AgentStatus::ShuttingDown => (Color::DarkGray, "Shutdown", "⏹️"),
            };

            let is_master = agent.specialization.contains("Master");
            let (name_icon, name_color) = if is_master {
                ("👑", Color::Yellow)
            } else {
                match agent.specialization.as_str() {
                    s if s.contains("Frontend") => ("🎨", Color::Cyan),
                    s if s.contains("Backend") => ("⚙️", Color::Green),
                    s if s.contains("DevOps") => ("🔧", Color::Blue),
                    s if s.contains("QA") => ("🧪", Color::Magenta),
                    _ => ("🤖", Color::White),
                }
            };

            Row::new(vec![
                Cell::from(format!("{} {}", name_icon, agent.name)).style(
                    Style::default().fg(name_color).add_modifier(if is_master {
                        Modifier::BOLD
                    } else {
                        Modifier::empty()
                    }),
                ),
                Cell::from(agent.specialization.clone()).style(Style::default().fg(Color::White)),
                Cell::from(format!("{} {}", status_icon, status_text))
                    .style(Style::default().fg(status_color)),
                Cell::from(format!("{}", agent.tasks_completed))
                    .style(Style::default().fg(Color::Yellow)),
            ])
        })
        .collect();

    let agents_table = Table::new(
        rows,
        [
            Constraint::Length(15), // Agent
            Constraint::Length(12), // Type
            Constraint::Length(12), // Status
            Constraint::Length(6),  // Tasks
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 👥 Agents Overview ")
            .title_alignment(Alignment::Center),
    )
    .row_highlight_style(
        Style::default()
            .bg(Color::DarkGray)
            .add_modifier(Modifier::BOLD),
    );

    f.render_widget(agents_table, chunks[0]);

    // System activity summary
    let activity_text = format!(
        "🔄 System Activity\n\n\
         ⏱️  Uptime: Active\n\
         📊 Total Agents: {}\n\
         🎯 Active: {}\n\
         📋 Tasks Queued: {}\n\
         ✅ Completed: {}\n\n\
         📈 Efficiency: {:.1}%",
        app.total_agents,
        app.active_agents,
        app.pending_tasks,
        app.completed_tasks,
        if app.total_agents > 0 {
            (app.active_agents as f32 / app.total_agents as f32) * 100.0
        } else {
            0.0
        }
    );

    let activity_paragraph = Paragraph::new(activity_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📊 System Metrics ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::Cyan))
        .wrap(Wrap { trim: true });

    f.render_widget(activity_paragraph, chunks[1]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_tasks (lines 588-706) ---[0m
fn draw_tasks(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Percentage(70), Constraint::Percentage(30)])
        .split(area);

    // Task list with enhanced headers
    let header = Row::new(vec![
        "#",
        "Description",
        "Priority",
        "Type",
        "Status",
        "Agent",
        "Created",
    ])
    .style(Style::default().fg(Color::Yellow))
    .height(1)
    .bottom_margin(1);

    let rows: Vec<Row> = app
        .tasks
        .iter()
        .enumerate()
        .map(|(i, task)| {
            let priority_color = match task.priority.as_str() {
                "High" => Color::Red,
                "Medium" => Color::Yellow,
                "Low" => Color::Green,
                _ => Color::White,
            };

            let status_color = if task.status.contains("✅") {
                Color::Green
            } else if task.status.contains("❌") {
                Color::Red
            } else if task.status.contains("🏃") {
                Color::Cyan
            } else if task.status.contains("📋") {
                Color::Yellow
            } else {
                Color::White
            };

            Row::new(vec![
                Cell::from(format!("{}", i + 1)),
                Cell::from(task.description.clone()),
                Cell::from(Span::styled(
                    task.priority.clone(),
                    Style::default().fg(priority_color),
                )),
                Cell::from(task.task_type.clone()),
                Cell::from(Span::styled(
                    task.status.clone(),
                    Style::default().fg(status_color),
                )),
                Cell::from(task.assigned_agent.as_deref().unwrap_or("-")),
                Cell::from(task.created_at.format("%H:%M:%S").to_string()),
            ])
        })
        .collect();

    let selected_style = Style::default()
        .bg(Color::Yellow)
        .fg(Color::Black)
        .add_modifier(Modifier::BOLD);

    let table = Table::new(
        rows,
        &[
            Constraint::Length(3),  // #
            Constraint::Min(25),    // Description
            Constraint::Length(8),  // Priority
            Constraint::Length(12), // Type
            Constraint::Length(15), // Status
            Constraint::Length(12), // Agent
            Constraint::Length(8),  // Created
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .title(" Tasks (t to add new task) "),
    )
    .row_highlight_style(selected_style);

    let mut table_state = ratatui::widgets::TableState::default();
    table_state.select(Some(app.selected_task));

    f.render_stateful_widget(table, chunks[0], &mut table_state);

    // Task details
    if let Some(task) = app.tasks.get(app.selected_task) {
        let details = [
            format!("ID: {}", task.id),
            format!("Description: {}", task.description),
            format!("Priority: {}", task.priority),
            format!("Type: {}", task.task_type),
            format!("Status: {}", task.status),
            format!("Created: {}", task.created_at.format("%Y-%m-%d %H:%M:%S")),
            format!(
                "Assigned Agent: {}",
                task.assigned_agent.as_deref().unwrap_or("None")
            ),
        ];

        let details_text = details.join("\n");
        let details_paragraph = Paragraph::new(details_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .title(" Task Details "),
            )
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    }
}

  ./crates/ccswarm/src/tui/ui.rs:588-706 function draw_tasks <-> ./crates/ccswarm/src/tui/ui.rs:1160-1303 function draw_delegation_stats
  Similarity: 90.44%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_tasks (lines 588-706) ---[0m
fn draw_tasks(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Percentage(70), Constraint::Percentage(30)])
        .split(area);

    // Task list with enhanced headers
    let header = Row::new(vec![
        "#",
        "Description",
        "Priority",
        "Type",
        "Status",
        "Agent",
        "Created",
    ])
    .style(Style::default().fg(Color::Yellow))
    .height(1)
    .bottom_margin(1);

    let rows: Vec<Row> = app
        .tasks
        .iter()
        .enumerate()
        .map(|(i, task)| {
            let priority_color = match task.priority.as_str() {
                "High" => Color::Red,
                "Medium" => Color::Yellow,
                "Low" => Color::Green,
                _ => Color::White,
            };

            let status_color = if task.status.contains("✅") {
                Color::Green
            } else if task.status.contains("❌") {
                Color::Red
            } else if task.status.contains("🏃") {
                Color::Cyan
            } else if task.status.contains("📋") {
                Color::Yellow
            } else {
                Color::White
            };

            Row::new(vec![
                Cell::from(format!("{}", i + 1)),
                Cell::from(task.description.clone()),
                Cell::from(Span::styled(
                    task.priority.clone(),
                    Style::default().fg(priority_color),
                )),
                Cell::from(task.task_type.clone()),
                Cell::from(Span::styled(
                    task.status.clone(),
                    Style::default().fg(status_color),
                )),
                Cell::from(task.assigned_agent.as_deref().unwrap_or("-")),
                Cell::from(task.created_at.format("%H:%M:%S").to_string()),
            ])
        })
        .collect();

    let selected_style = Style::default()
        .bg(Color::Yellow)
        .fg(Color::Black)
        .add_modifier(Modifier::BOLD);

    let table = Table::new(
        rows,
        &[
            Constraint::Length(3),  // #
            Constraint::Min(25),    // Description
            Constraint::Length(8),  // Priority
            Constraint::Length(12), // Type
            Constraint::Length(15), // Status
            Constraint::Length(12), // Agent
            Constraint::Length(8),  // Created
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .title(" Tasks (t to add new task) "),
    )
    .row_highlight_style(selected_style);

    let mut table_state = ratatui::widgets::TableState::default();
    table_state.select(Some(app.selected_task));

    f.render_stateful_widget(table, chunks[0], &mut table_state);

    // Task details
    if let Some(task) = app.tasks.get(app.selected_task) {
        let details = [
            format!("ID: {}", task.id),
            format!("Description: {}", task.description),
            format!("Priority: {}", task.priority),
            format!("Type: {}", task.task_type),
            format!("Status: {}", task.status),
            format!("Created: {}", task.created_at.format("%Y-%m-%d %H:%M:%S")),
            format!(
                "Assigned Agent: {}",
                task.assigned_agent.as_deref().unwrap_or("None")
            ),
        ];

        let details_text = details.join("\n");
        let details_paragraph = Paragraph::new(details_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .title(" Task Details "),
            )
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_stats (lines 1160-1303) ---[0m
fn draw_delegation_stats(f: &mut Frame, area: Rect, app: &App) {
    if app.delegation_decisions.is_empty() {
        let empty_text = "📊 Delegation Statistics\n\n\
                         No delegation data available yet.\n\n\
                         Start analyzing or delegating tasks\n\
                         to see statistics here.";

        let empty_paragraph = Paragraph::new(empty_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📊 Statistics ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::Gray))
            .alignment(Alignment::Center)
            .wrap(Wrap { trim: true });

        f.render_widget(empty_paragraph, area);
        return;
    }

    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Agent distribution chart
    let mut agent_counts = std::collections::HashMap::new();
    let mut total_confidence = 0.0;

    for decision in &app.delegation_decisions {
        *agent_counts
            .entry(decision.recommended_agent.clone())
            .or_insert(0) += 1;
        total_confidence += decision.confidence;
    }

    let total = app.delegation_decisions.len();
    let avg_confidence = total_confidence / total as f64;

    // Create agent distribution table
    let header = Row::new(vec![
        Cell::from("Agent").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Count").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Percentage").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = agent_counts
        .iter()
        .map(|(agent, count)| {
            let percentage = (*count as f64 / total as f64) * 100.0;
            let agent_icon = match agent.as_str() {
                "Frontend" | "frontend" => "🎨",
                "Backend" | "backend" => "⚙️",
                "DevOps" | "devops" => "🔧",
                "QA" | "qa" => "🧪",
                _ => "🤖",
            };

            Row::new(vec![
                Cell::from(format!("{} {}", agent_icon, agent)).style(
                    Style::default()
                        .fg(Color::Cyan)
                        .add_modifier(Modifier::BOLD),
                ),
                Cell::from(format!("{}", count)).style(Style::default().fg(Color::Yellow)),
                Cell::from(format!("{:.1}%", percentage)).style(Style::default().fg(Color::Green)),
            ])
        })
        .collect();

    let distribution_table = Table::new(
        rows,
        [
            Constraint::Length(15), // Agent
            Constraint::Length(8),  // Count
            Constraint::Length(12), // Percentage
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 📊 Agent Distribution ")
            .title_alignment(Alignment::Center),
    );

    f.render_widget(distribution_table, chunks[0]);

    // Statistics summary
    let stats_text = format!(
        "📈 Delegation Summary\n\n\
         Total Delegations: {}\n\
         Average Confidence: {:.1}%\n\n\
         📊 Distribution:\n\
         {}\n\n\
         🎯 Most Delegated: {}\n\
         📅 Latest: {}",
        total,
        avg_confidence * 100.0,
        agent_counts
            .iter()
            .map(|(agent, count)| format!("• {}: {}", agent, count))
            .collect::<Vec<_>>()
            .join("\n         "),
        agent_counts
            .iter()
            .max_by_key(|(_, count)| *count)
            .map(|(agent, _)| agent.as_str())
            .unwrap_or("None"),
        app.delegation_decisions
            .last()
            .map(|d| d.created_at.format("%H:%M:%S").to_string())
            .unwrap_or("None".to_string())
    );

    let stats_paragraph = Paragraph::new(stats_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📋 Statistics Summary ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(stats_paragraph, chunks[1]);
}

  ./crates/ccswarm/src/tui/ui.rs:709-810 function draw_logs <-> ./crates/ccswarm/src/tui/ui.rs:1017-1157 function draw_delegation_interface
  Similarity: 95.73%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_logs (lines 709-810) ---[0m
fn draw_logs(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(3), Constraint::Min(0)])
        .split(area);

    // Log level summary bar
    let log_counts = app
        .logs
        .iter()
        .fold(std::collections::HashMap::new(), |mut acc, log| {
            *acc.entry(log.level.as_str()).or_insert(0) += 1;
            acc
        });

    let summary_text = format!(
        "📊 Logs: 🔴 {} errors | 🟡 {} warnings | 🟢 {} info | 🔵 {} debug | Total: {}",
        log_counts.get("ERROR").unwrap_or(&0),
        log_counts.get("WARN").unwrap_or(&0),
        log_counts.get("INFO").unwrap_or(&0),
        log_counts.get("DEBUG").unwrap_or(&0),
        app.logs.len()
    );

    let summary_paragraph = Paragraph::new(summary_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📋 Log Summary ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .alignment(Alignment::Center);

    f.render_widget(summary_paragraph, chunks[0]);

    // Enhanced logs with better visual hierarchy
    let items: Vec<ListItem> = app
        .logs
        .iter()
        .rev() // Show newest logs first
        .take(100) // Limit to prevent performance issues
        .map(|log| {
            let (level_color, level_icon) = match log.level.as_str() {
                "ERROR" => (Color::Red, "🔴"),
                "WARN" => (Color::Yellow, "🟡"),
                "INFO" => (Color::Green, "🟢"),
                "DEBUG" => (Color::Cyan, "🔵"),
                _ => (Color::White, "⚪"),
            };

            let agent_name = log.agent.as_deref().unwrap_or("System");
            let (agent_icon, agent_color) = if agent_name == "System" {
                ("🖥️", Color::Gray)
            } else if agent_name.contains("master") || agent_name.contains("Master") {
                ("👑", Color::Yellow)
            } else {
                ("🤖", Color::Cyan)
            };

            let content = vec![Line::from(vec![
                Span::styled(
                    format!("{} ", log.timestamp.format("%H:%M:%S")),
                    Style::default().fg(Color::DarkGray),
                ),
                Span::styled(format!("{} ", level_icon), Style::default().fg(level_color)),
                Span::styled(
                    format!("{:<5} ", log.level),
                    Style::default()
                        .fg(level_color)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("{} {:<10} ", agent_icon, agent_name),
                    Style::default().fg(agent_color),
                ),
                Span::styled(log.message.clone(), Style::default().fg(Color::White)),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut logs_state = ListState::default();
    logs_state.select(Some(app.selected_log));

    let logs_list = List::new(items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📜 Live Logs (r: refresh, ↑↓: navigate) ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::DarkGray)
                .add_modifier(Modifier::BOLD),
        );

    f.render_stateful_widget(logs_list, chunks[1], &mut logs_state);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_interface (lines 1017-1157) ---[0m
fn draw_delegation_interface(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(40), Constraint::Percentage(60)])
        .split(area);

    // Available agents
    let agent_items: Vec<ListItem> = vec![
        ListItem::new(Line::from(vec![
            Span::styled("🎨 ", Style::default().fg(Color::Cyan)),
            Span::styled(
                "Frontend",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - UI/UX Development", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("⚙️ ", Style::default().fg(Color::Green)),
            Span::styled(
                "Backend",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - API/Database Work", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("🔧 ", Style::default().fg(Color::Blue)),
            Span::styled(
                "DevOps",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - Infrastructure/Deploy", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("🧪 ", Style::default().fg(Color::Magenta)),
            Span::styled(
                "QA",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - Testing/Quality", Style::default().fg(Color::Gray)),
        ])),
    ];

    let agents_list = List::new(agent_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🤖 Available Agents ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Green)
                .add_modifier(Modifier::BOLD),
        );

    f.render_widget(agents_list, chunks[0]);

    // Delegation form and recent delegations
    let right_chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(8), Constraint::Min(0)])
        .split(chunks[1]);

    // Delegation form
    let form_text = "🎯 Manual Task Delegation\n\n\
                     Format: <agent> <task_description>\n\n\
                     Examples:\n\
                     • frontend Create user dashboard\n\
                     • backend Add payment processing\n\
                     • qa Write end-to-end tests\n\
                     • devops Setup CI/CD pipeline";

    let form_paragraph = Paragraph::new(form_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📝 Delegation Form ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(form_paragraph, right_chunks[0]);

    // Recent delegations
    let delegation_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(6)
        .filter(|d| d.reasoning.contains("Manual delegation"))
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "frontend" | "Frontend" => "🎨",
                "backend" | "Backend" => "⚙️",
                "devops" | "DevOps" => "🔧",
                "qa" | "QA" => "🧪",
                _ => "🤖",
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::Green)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("{} ", decision.created_at.format("%H:%M")),
                    Style::default().fg(Color::DarkGray),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::White),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let delegations_list = List::new(delegation_items).block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 📋 Recent Delegations ")
            .title_alignment(Alignment::Center),
    );

    f.render_widget(delegations_list, right_chunks[1]);
}

  ./crates/ccswarm/src/tui/ui.rs:894-1014 function draw_delegation_analysis <-> ./crates/ccswarm/src/tui/ui.rs:1160-1303 function draw_delegation_stats
  Similarity: 87.58%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_analysis (lines 894-1014) ---[0m
fn draw_delegation_analysis(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Recent analysis results
    let analysis_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(10)
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "Frontend" => "🎨",
                "Backend" => "⚙️",
                "DevOps" => "🔧",
                "QA" => "🧪",
                _ => "🤖",
            };

            let confidence_color = if decision.confidence >= 0.8 {
                Color::Green
            } else if decision.confidence >= 0.6 {
                Color::Yellow
            } else {
                Color::Red
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::White)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("({:.0}%) ", decision.confidence * 100.0),
                    Style::default().fg(confidence_color),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::Gray),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut analysis_state = ListState::default();
    analysis_state.select(Some(app.selected_delegation));

    let analysis_list = List::new(analysis_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🔍 Analysis Results ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Blue)
                .add_modifier(Modifier::BOLD),
        )
        .highlight_symbol("► ");

    f.render_stateful_widget(analysis_list, chunks[0], &mut analysis_state);

    // Analysis details
    if let Some(decision) = app.delegation_decisions.get(app.selected_delegation) {
        let details = format!(
            "📋 Task Analysis Details\n\n\
             Task: {}\n\n\
             🎯 Recommended Agent: {}\n\
             📊 Confidence: {:.1}%\n\
             🧠 Reasoning: {}\n\
             📅 Analyzed: {}\n\n\
             💡 Press Enter to delegate this task",
            decision.task_description,
            decision.recommended_agent,
            decision.confidence * 100.0,
            decision.reasoning,
            decision.created_at.format("%H:%M:%S")
        );

        let details_paragraph = Paragraph::new(details)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Analysis Details ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::White))
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    } else {
        let empty_text = "🔍 Task Analysis\n\n\
                         No analysis results yet.\n\n\
                         Press Enter to analyze a new task\n\
                         and get Master's recommendation\n\
                         for optimal agent assignment.";

        let empty_paragraph = Paragraph::new(empty_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Ready for Analysis ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::Gray))
            .alignment(Alignment::Center)
            .wrap(Wrap { trim: true });

        f.render_widget(empty_paragraph, chunks[1]);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_stats (lines 1160-1303) ---[0m
fn draw_delegation_stats(f: &mut Frame, area: Rect, app: &App) {
    if app.delegation_decisions.is_empty() {
        let empty_text = "📊 Delegation Statistics\n\n\
                         No delegation data available yet.\n\n\
                         Start analyzing or delegating tasks\n\
                         to see statistics here.";

        let empty_paragraph = Paragraph::new(empty_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📊 Statistics ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::Gray))
            .alignment(Alignment::Center)
            .wrap(Wrap { trim: true });

        f.render_widget(empty_paragraph, area);
        return;
    }

    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Agent distribution chart
    let mut agent_counts = std::collections::HashMap::new();
    let mut total_confidence = 0.0;

    for decision in &app.delegation_decisions {
        *agent_counts
            .entry(decision.recommended_agent.clone())
            .or_insert(0) += 1;
        total_confidence += decision.confidence;
    }

    let total = app.delegation_decisions.len();
    let avg_confidence = total_confidence / total as f64;

    // Create agent distribution table
    let header = Row::new(vec![
        Cell::from("Agent").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Count").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Percentage").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = agent_counts
        .iter()
        .map(|(agent, count)| {
            let percentage = (*count as f64 / total as f64) * 100.0;
            let agent_icon = match agent.as_str() {
                "Frontend" | "frontend" => "🎨",
                "Backend" | "backend" => "⚙️",
                "DevOps" | "devops" => "🔧",
                "QA" | "qa" => "🧪",
                _ => "🤖",
            };

            Row::new(vec![
                Cell::from(format!("{} {}", agent_icon, agent)).style(
                    Style::default()
                        .fg(Color::Cyan)
                        .add_modifier(Modifier::BOLD),
                ),
                Cell::from(format!("{}", count)).style(Style::default().fg(Color::Yellow)),
                Cell::from(format!("{:.1}%", percentage)).style(Style::default().fg(Color::Green)),
            ])
        })
        .collect();

    let distribution_table = Table::new(
        rows,
        [
            Constraint::Length(15), // Agent
            Constraint::Length(8),  // Count
            Constraint::Length(12), // Percentage
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 📊 Agent Distribution ")
            .title_alignment(Alignment::Center),
    );

    f.render_widget(distribution_table, chunks[0]);

    // Statistics summary
    let stats_text = format!(
        "📈 Delegation Summary\n\n\
         Total Delegations: {}\n\
         Average Confidence: {:.1}%\n\n\
         📊 Distribution:\n\
         {}\n\n\
         🎯 Most Delegated: {}\n\
         📅 Latest: {}",
        total,
        avg_confidence * 100.0,
        agent_counts
            .iter()
            .map(|(agent, count)| format!("• {}: {}", agent, count))
            .collect::<Vec<_>>()
            .join("\n         "),
        agent_counts
            .iter()
            .max_by_key(|(_, count)| *count)
            .map(|(agent, _)| agent.as_str())
            .unwrap_or("None"),
        app.delegation_decisions
            .last()
            .map(|d| d.created_at.format("%H:%M:%S").to_string())
            .unwrap_or("None".to_string())
    );

    let stats_paragraph = Paragraph::new(stats_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📋 Statistics Summary ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(stats_paragraph, chunks[1]);
}

  ./crates/ccswarm/src/tui/ui.rs:298-429 function draw_enhanced_agent_summary <-> ./crates/ccswarm/src/tui/ui.rs:1420-1558 function draw_input_popup
  Similarity: 85.55%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_agent_summary (lines 298-429) ---[0m
fn draw_enhanced_agent_summary(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(60), Constraint::Percentage(40)])
        .split(area);

    // Enhanced agents table
    let header = Row::new(vec![
        Cell::from("Agent").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Type").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Status").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Tasks").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = app
        .agents
        .iter()
        .take(6)
        .map(|agent| {
            let (status_color, status_text, status_icon) = match agent.status {
                crate::agent::AgentStatus::Available => (Color::Green, "Available", "✅"),
                crate::agent::AgentStatus::Working => (Color::Cyan, "Working", "⚙️"),
                crate::agent::AgentStatus::Error(_) => (Color::Red, "Error", "❌"),
                crate::agent::AgentStatus::Initializing => (Color::Yellow, "Init", "🔄"),
                crate::agent::AgentStatus::WaitingForReview => (Color::Magenta, "Review", "⏳"),
                crate::agent::AgentStatus::ShuttingDown => (Color::DarkGray, "Shutdown", "⏹️"),
            };

            let is_master = agent.specialization.contains("Master");
            let (name_icon, name_color) = if is_master {
                ("👑", Color::Yellow)
            } else {
                match agent.specialization.as_str() {
                    s if s.contains("Frontend") => ("🎨", Color::Cyan),
                    s if s.contains("Backend") => ("⚙️", Color::Green),
                    s if s.contains("DevOps") => ("🔧", Color::Blue),
                    s if s.contains("QA") => ("🧪", Color::Magenta),
                    _ => ("🤖", Color::White),
                }
            };

            Row::new(vec![
                Cell::from(format!("{} {}", name_icon, agent.name)).style(
                    Style::default().fg(name_color).add_modifier(if is_master {
                        Modifier::BOLD
                    } else {
                        Modifier::empty()
                    }),
                ),
                Cell::from(agent.specialization.clone()).style(Style::default().fg(Color::White)),
                Cell::from(format!("{} {}", status_icon, status_text))
                    .style(Style::default().fg(status_color)),
                Cell::from(format!("{}", agent.tasks_completed))
                    .style(Style::default().fg(Color::Yellow)),
            ])
        })
        .collect();

    let agents_table = Table::new(
        rows,
        [
            Constraint::Length(15), // Agent
            Constraint::Length(12), // Type
            Constraint::Length(12), // Status
            Constraint::Length(6),  // Tasks
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 👥 Agents Overview ")
            .title_alignment(Alignment::Center),
    )
    .row_highlight_style(
        Style::default()
            .bg(Color::DarkGray)
            .add_modifier(Modifier::BOLD),
    );

    f.render_widget(agents_table, chunks[0]);

    // System activity summary
    let activity_text = format!(
        "🔄 System Activity\n\n\
         ⏱️  Uptime: Active\n\
         📊 Total Agents: {}\n\
         🎯 Active: {}\n\
         📋 Tasks Queued: {}\n\
         ✅ Completed: {}\n\n\
         📈 Efficiency: {:.1}%",
        app.total_agents,
        app.active_agents,
        app.pending_tasks,
        app.completed_tasks,
        if app.total_agents > 0 {
            (app.active_agents as f32 / app.total_agents as f32) * 100.0
        } else {
            0.0
        }
    );

    let activity_paragraph = Paragraph::new(activity_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📊 System Metrics ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::Cyan))
        .wrap(Wrap { trim: true });

    f.render_widget(activity_paragraph, chunks[1]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_input_popup (lines 1420-1558) ---[0m
fn draw_input_popup(f: &mut Frame, app: &App) {
    let area = centered_rect(70, 25, f.area());

    let (title, _title_icon, border_color) = match app.input_mode {
        InputMode::AddingTask => (" 📋 Add New Task ", "📋", Color::Green),
        InputMode::CreatingAgent => (" 🤖 Create New Agent ", "🤖", Color::Blue),
        InputMode::Command => (" 💻 Command Prompt ", "💻", Color::Yellow),
        InputMode::DelegationInput => (" 🎯 Master Delegation ", "🎯", Color::Magenta),
        _ => (" ✏️  Input ", "✏️", Color::White),
    };

    let prompt = match app.input_mode {
        InputMode::AddingTask => {
            "Enter task description:\n\n\
             💡 Tips:\n\
             • Use [high]/[medium]/[low] for priority\n\
             • Use [test]/[docs]/[bug]/[feature] for type\n\
             • Example: \"Fix login bug [high] [bug]\""
        }
        InputMode::CreatingAgent => {
            "Select agent specialization:\n\n\
             🎨 frontend  - UI/UX development\n\
             ⚙️  backend   - API/Database work\n\
             🔧 devops    - Infrastructure/Deploy\n\
             🧪 qa        - Testing/Quality assurance"
        }
        InputMode::Command => {
            "Enter command:\n\n\
             💡 Quick commands:\n\
             • help - Show all commands\n\
             • start_agent master - Start master agent\n\
             • task <description> - Add new task\n\
             • status - Show system status"
        }
        InputMode::DelegationInput => match app.delegation_mode {
            DelegationMode::Analyze => {
                "🔍 Analyze Task for Delegation:\n\n\
                     Enter task description to get Master's recommendation\n\
                     💡 Examples:\n\
                     • \"Create login form with validation\"\n\
                     • \"Fix API endpoint error handling\"\n\
                     • \"Write unit tests for user service\""
            }
            DelegationMode::Delegate => {
                "🎯 Delegate Task to Agent:\n\n\
                     Format: <agent_type> <task_description>\n\
                     💡 Examples:\n\
                     • \"frontend Create responsive navigation\"\n\
                     • \"backend Add user authentication API\"\n\
                     • \"qa Write integration tests\""
            }
            _ => "Enter delegation input:",
        },
        _ => "Enter your input:",
    };

    // Create a shadow effect
    let shadow_area = Rect {
        x: area.x + 1,
        y: area.y + 1,
        width: area.width,
        height: area.height,
    };
    f.render_widget(
        Block::default().style(Style::default().bg(Color::Black)),
        shadow_area,
    );

    f.render_widget(Clear, area); // Clear background

    let input_block = Block::default()
        .title(title)
        .title_alignment(Alignment::Center)
        .borders(Borders::ALL)
        .border_type(BorderType::Rounded)
        .border_style(
            Style::default()
                .fg(border_color)
                .add_modifier(Modifier::BOLD),
        );

    let input_area = input_block.inner(area);
    f.render_widget(input_block, area);

    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(8), // Prompt area
            Constraint::Length(3), // Input area
            Constraint::Length(2), // Help area
        ])
        .split(input_area);

    // Prompt text
    let prompt_paragraph = Paragraph::new(prompt)
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });
    f.render_widget(prompt_paragraph, chunks[0]);

    // Input box with cursor
    let input_text = format!("{}_", app.input_buffer);
    let input_paragraph = Paragraph::new(input_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Input ")
                .border_style(Style::default().fg(Color::Cyan)),
        )
        .style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        );
    f.render_widget(input_paragraph, chunks[1]);

    // Help text
    let help_text = vec![Line::from(vec![
        Span::styled(
            " Enter ",
            Style::default()
                .fg(Color::Black)
                .bg(Color::Green)
                .add_modifier(Modifier::BOLD),
        ),
        Span::styled(" Confirm ", Style::default().fg(Color::Gray)),
        Span::raw("  "),
        Span::styled(
            " Esc ",
            Style::default()
                .fg(Color::Black)
                .bg(Color::Red)
                .add_modifier(Modifier::BOLD),
        ),
        Span::styled(" Cancel ", Style::default().fg(Color::Gray)),
    ])];
    let help_paragraph = Paragraph::new(help_text).alignment(Alignment::Center);
    f.render_widget(help_paragraph, chunks[2]);
}

  ./crates/ccswarm/src/tui/ui.rs:709-810 function draw_logs <-> ./crates/ccswarm/src/tui/ui.rs:1160-1303 function draw_delegation_stats
  Similarity: 91.84%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_logs (lines 709-810) ---[0m
fn draw_logs(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(3), Constraint::Min(0)])
        .split(area);

    // Log level summary bar
    let log_counts = app
        .logs
        .iter()
        .fold(std::collections::HashMap::new(), |mut acc, log| {
            *acc.entry(log.level.as_str()).or_insert(0) += 1;
            acc
        });

    let summary_text = format!(
        "📊 Logs: 🔴 {} errors | 🟡 {} warnings | 🟢 {} info | 🔵 {} debug | Total: {}",
        log_counts.get("ERROR").unwrap_or(&0),
        log_counts.get("WARN").unwrap_or(&0),
        log_counts.get("INFO").unwrap_or(&0),
        log_counts.get("DEBUG").unwrap_or(&0),
        app.logs.len()
    );

    let summary_paragraph = Paragraph::new(summary_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📋 Log Summary ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .alignment(Alignment::Center);

    f.render_widget(summary_paragraph, chunks[0]);

    // Enhanced logs with better visual hierarchy
    let items: Vec<ListItem> = app
        .logs
        .iter()
        .rev() // Show newest logs first
        .take(100) // Limit to prevent performance issues
        .map(|log| {
            let (level_color, level_icon) = match log.level.as_str() {
                "ERROR" => (Color::Red, "🔴"),
                "WARN" => (Color::Yellow, "🟡"),
                "INFO" => (Color::Green, "🟢"),
                "DEBUG" => (Color::Cyan, "🔵"),
                _ => (Color::White, "⚪"),
            };

            let agent_name = log.agent.as_deref().unwrap_or("System");
            let (agent_icon, agent_color) = if agent_name == "System" {
                ("🖥️", Color::Gray)
            } else if agent_name.contains("master") || agent_name.contains("Master") {
                ("👑", Color::Yellow)
            } else {
                ("🤖", Color::Cyan)
            };

            let content = vec![Line::from(vec![
                Span::styled(
                    format!("{} ", log.timestamp.format("%H:%M:%S")),
                    Style::default().fg(Color::DarkGray),
                ),
                Span::styled(format!("{} ", level_icon), Style::default().fg(level_color)),
                Span::styled(
                    format!("{:<5} ", log.level),
                    Style::default()
                        .fg(level_color)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("{} {:<10} ", agent_icon, agent_name),
                    Style::default().fg(agent_color),
                ),
                Span::styled(log.message.clone(), Style::default().fg(Color::White)),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut logs_state = ListState::default();
    logs_state.select(Some(app.selected_log));

    let logs_list = List::new(items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📜 Live Logs (r: refresh, ↑↓: navigate) ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::DarkGray)
                .add_modifier(Modifier::BOLD),
        );

    f.render_stateful_widget(logs_list, chunks[1], &mut logs_state);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_stats (lines 1160-1303) ---[0m
fn draw_delegation_stats(f: &mut Frame, area: Rect, app: &App) {
    if app.delegation_decisions.is_empty() {
        let empty_text = "📊 Delegation Statistics\n\n\
                         No delegation data available yet.\n\n\
                         Start analyzing or delegating tasks\n\
                         to see statistics here.";

        let empty_paragraph = Paragraph::new(empty_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📊 Statistics ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::Gray))
            .alignment(Alignment::Center)
            .wrap(Wrap { trim: true });

        f.render_widget(empty_paragraph, area);
        return;
    }

    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Agent distribution chart
    let mut agent_counts = std::collections::HashMap::new();
    let mut total_confidence = 0.0;

    for decision in &app.delegation_decisions {
        *agent_counts
            .entry(decision.recommended_agent.clone())
            .or_insert(0) += 1;
        total_confidence += decision.confidence;
    }

    let total = app.delegation_decisions.len();
    let avg_confidence = total_confidence / total as f64;

    // Create agent distribution table
    let header = Row::new(vec![
        Cell::from("Agent").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Count").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Percentage").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = agent_counts
        .iter()
        .map(|(agent, count)| {
            let percentage = (*count as f64 / total as f64) * 100.0;
            let agent_icon = match agent.as_str() {
                "Frontend" | "frontend" => "🎨",
                "Backend" | "backend" => "⚙️",
                "DevOps" | "devops" => "🔧",
                "QA" | "qa" => "🧪",
                _ => "🤖",
            };

            Row::new(vec![
                Cell::from(format!("{} {}", agent_icon, agent)).style(
                    Style::default()
                        .fg(Color::Cyan)
                        .add_modifier(Modifier::BOLD),
                ),
                Cell::from(format!("{}", count)).style(Style::default().fg(Color::Yellow)),
                Cell::from(format!("{:.1}%", percentage)).style(Style::default().fg(Color::Green)),
            ])
        })
        .collect();

    let distribution_table = Table::new(
        rows,
        [
            Constraint::Length(15), // Agent
            Constraint::Length(8),  // Count
            Constraint::Length(12), // Percentage
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 📊 Agent Distribution ")
            .title_alignment(Alignment::Center),
    );

    f.render_widget(distribution_table, chunks[0]);

    // Statistics summary
    let stats_text = format!(
        "📈 Delegation Summary\n\n\
         Total Delegations: {}\n\
         Average Confidence: {:.1}%\n\n\
         📊 Distribution:\n\
         {}\n\n\
         🎯 Most Delegated: {}\n\
         📅 Latest: {}",
        total,
        avg_confidence * 100.0,
        agent_counts
            .iter()
            .map(|(agent, count)| format!("• {}: {}", agent, count))
            .collect::<Vec<_>>()
            .join("\n         "),
        agent_counts
            .iter()
            .max_by_key(|(_, count)| *count)
            .map(|(agent, _)| agent.as_str())
            .unwrap_or("None"),
        app.delegation_decisions
            .last()
            .map(|d| d.created_at.format("%H:%M:%S").to_string())
            .unwrap_or("None".to_string())
    );

    let stats_paragraph = Paragraph::new(stats_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📋 Statistics Summary ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(stats_paragraph, chunks[1]);
}

  ./crates/ccswarm/src/tui/ui.rs:298-429 function draw_enhanced_agent_summary <-> ./crates/ccswarm/src/tui/ui.rs:709-810 function draw_logs
  Similarity: 96.38%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_agent_summary (lines 298-429) ---[0m
fn draw_enhanced_agent_summary(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(60), Constraint::Percentage(40)])
        .split(area);

    // Enhanced agents table
    let header = Row::new(vec![
        Cell::from("Agent").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Type").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Status").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Tasks").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = app
        .agents
        .iter()
        .take(6)
        .map(|agent| {
            let (status_color, status_text, status_icon) = match agent.status {
                crate::agent::AgentStatus::Available => (Color::Green, "Available", "✅"),
                crate::agent::AgentStatus::Working => (Color::Cyan, "Working", "⚙️"),
                crate::agent::AgentStatus::Error(_) => (Color::Red, "Error", "❌"),
                crate::agent::AgentStatus::Initializing => (Color::Yellow, "Init", "🔄"),
                crate::agent::AgentStatus::WaitingForReview => (Color::Magenta, "Review", "⏳"),
                crate::agent::AgentStatus::ShuttingDown => (Color::DarkGray, "Shutdown", "⏹️"),
            };

            let is_master = agent.specialization.contains("Master");
            let (name_icon, name_color) = if is_master {
                ("👑", Color::Yellow)
            } else {
                match agent.specialization.as_str() {
                    s if s.contains("Frontend") => ("🎨", Color::Cyan),
                    s if s.contains("Backend") => ("⚙️", Color::Green),
                    s if s.contains("DevOps") => ("🔧", Color::Blue),
                    s if s.contains("QA") => ("🧪", Color::Magenta),
                    _ => ("🤖", Color::White),
                }
            };

            Row::new(vec![
                Cell::from(format!("{} {}", name_icon, agent.name)).style(
                    Style::default().fg(name_color).add_modifier(if is_master {
                        Modifier::BOLD
                    } else {
                        Modifier::empty()
                    }),
                ),
                Cell::from(agent.specialization.clone()).style(Style::default().fg(Color::White)),
                Cell::from(format!("{} {}", status_icon, status_text))
                    .style(Style::default().fg(status_color)),
                Cell::from(format!("{}", agent.tasks_completed))
                    .style(Style::default().fg(Color::Yellow)),
            ])
        })
        .collect();

    let agents_table = Table::new(
        rows,
        [
            Constraint::Length(15), // Agent
            Constraint::Length(12), // Type
            Constraint::Length(12), // Status
            Constraint::Length(6),  // Tasks
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 👥 Agents Overview ")
            .title_alignment(Alignment::Center),
    )
    .row_highlight_style(
        Style::default()
            .bg(Color::DarkGray)
            .add_modifier(Modifier::BOLD),
    );

    f.render_widget(agents_table, chunks[0]);

    // System activity summary
    let activity_text = format!(
        "🔄 System Activity\n\n\
         ⏱️  Uptime: Active\n\
         📊 Total Agents: {}\n\
         🎯 Active: {}\n\
         📋 Tasks Queued: {}\n\
         ✅ Completed: {}\n\n\
         📈 Efficiency: {:.1}%",
        app.total_agents,
        app.active_agents,
        app.pending_tasks,
        app.completed_tasks,
        if app.total_agents > 0 {
            (app.active_agents as f32 / app.total_agents as f32) * 100.0
        } else {
            0.0
        }
    );

    let activity_paragraph = Paragraph::new(activity_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📊 System Metrics ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::Cyan))
        .wrap(Wrap { trim: true });

    f.render_widget(activity_paragraph, chunks[1]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_logs (lines 709-810) ---[0m
fn draw_logs(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(3), Constraint::Min(0)])
        .split(area);

    // Log level summary bar
    let log_counts = app
        .logs
        .iter()
        .fold(std::collections::HashMap::new(), |mut acc, log| {
            *acc.entry(log.level.as_str()).or_insert(0) += 1;
            acc
        });

    let summary_text = format!(
        "📊 Logs: 🔴 {} errors | 🟡 {} warnings | 🟢 {} info | 🔵 {} debug | Total: {}",
        log_counts.get("ERROR").unwrap_or(&0),
        log_counts.get("WARN").unwrap_or(&0),
        log_counts.get("INFO").unwrap_or(&0),
        log_counts.get("DEBUG").unwrap_or(&0),
        app.logs.len()
    );

    let summary_paragraph = Paragraph::new(summary_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📋 Log Summary ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .alignment(Alignment::Center);

    f.render_widget(summary_paragraph, chunks[0]);

    // Enhanced logs with better visual hierarchy
    let items: Vec<ListItem> = app
        .logs
        .iter()
        .rev() // Show newest logs first
        .take(100) // Limit to prevent performance issues
        .map(|log| {
            let (level_color, level_icon) = match log.level.as_str() {
                "ERROR" => (Color::Red, "🔴"),
                "WARN" => (Color::Yellow, "🟡"),
                "INFO" => (Color::Green, "🟢"),
                "DEBUG" => (Color::Cyan, "🔵"),
                _ => (Color::White, "⚪"),
            };

            let agent_name = log.agent.as_deref().unwrap_or("System");
            let (agent_icon, agent_color) = if agent_name == "System" {
                ("🖥️", Color::Gray)
            } else if agent_name.contains("master") || agent_name.contains("Master") {
                ("👑", Color::Yellow)
            } else {
                ("🤖", Color::Cyan)
            };

            let content = vec![Line::from(vec![
                Span::styled(
                    format!("{} ", log.timestamp.format("%H:%M:%S")),
                    Style::default().fg(Color::DarkGray),
                ),
                Span::styled(format!("{} ", level_icon), Style::default().fg(level_color)),
                Span::styled(
                    format!("{:<5} ", log.level),
                    Style::default()
                        .fg(level_color)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("{} {:<10} ", agent_icon, agent_name),
                    Style::default().fg(agent_color),
                ),
                Span::styled(log.message.clone(), Style::default().fg(Color::White)),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut logs_state = ListState::default();
    logs_state.select(Some(app.selected_log));

    let logs_list = List::new(items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📜 Live Logs (r: refresh, ↑↓: navigate) ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::DarkGray)
                .add_modifier(Modifier::BOLD),
        );

    f.render_stateful_widget(logs_list, chunks[1], &mut logs_state);
}

  ./crates/ccswarm/src/tui/ui.rs:588-706 function draw_tasks <-> ./crates/ccswarm/src/tui/ui.rs:894-1014 function draw_delegation_analysis
  Similarity: 92.14%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_tasks (lines 588-706) ---[0m
fn draw_tasks(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Percentage(70), Constraint::Percentage(30)])
        .split(area);

    // Task list with enhanced headers
    let header = Row::new(vec![
        "#",
        "Description",
        "Priority",
        "Type",
        "Status",
        "Agent",
        "Created",
    ])
    .style(Style::default().fg(Color::Yellow))
    .height(1)
    .bottom_margin(1);

    let rows: Vec<Row> = app
        .tasks
        .iter()
        .enumerate()
        .map(|(i, task)| {
            let priority_color = match task.priority.as_str() {
                "High" => Color::Red,
                "Medium" => Color::Yellow,
                "Low" => Color::Green,
                _ => Color::White,
            };

            let status_color = if task.status.contains("✅") {
                Color::Green
            } else if task.status.contains("❌") {
                Color::Red
            } else if task.status.contains("🏃") {
                Color::Cyan
            } else if task.status.contains("📋") {
                Color::Yellow
            } else {
                Color::White
            };

            Row::new(vec![
                Cell::from(format!("{}", i + 1)),
                Cell::from(task.description.clone()),
                Cell::from(Span::styled(
                    task.priority.clone(),
                    Style::default().fg(priority_color),
                )),
                Cell::from(task.task_type.clone()),
                Cell::from(Span::styled(
                    task.status.clone(),
                    Style::default().fg(status_color),
                )),
                Cell::from(task.assigned_agent.as_deref().unwrap_or("-")),
                Cell::from(task.created_at.format("%H:%M:%S").to_string()),
            ])
        })
        .collect();

    let selected_style = Style::default()
        .bg(Color::Yellow)
        .fg(Color::Black)
        .add_modifier(Modifier::BOLD);

    let table = Table::new(
        rows,
        &[
            Constraint::Length(3),  // #
            Constraint::Min(25),    // Description
            Constraint::Length(8),  // Priority
            Constraint::Length(12), // Type
            Constraint::Length(15), // Status
            Constraint::Length(12), // Agent
            Constraint::Length(8),  // Created
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .title(" Tasks (t to add new task) "),
    )
    .row_highlight_style(selected_style);

    let mut table_state = ratatui::widgets::TableState::default();
    table_state.select(Some(app.selected_task));

    f.render_stateful_widget(table, chunks[0], &mut table_state);

    // Task details
    if let Some(task) = app.tasks.get(app.selected_task) {
        let details = [
            format!("ID: {}", task.id),
            format!("Description: {}", task.description),
            format!("Priority: {}", task.priority),
            format!("Type: {}", task.task_type),
            format!("Status: {}", task.status),
            format!("Created: {}", task.created_at.format("%Y-%m-%d %H:%M:%S")),
            format!(
                "Assigned Agent: {}",
                task.assigned_agent.as_deref().unwrap_or("None")
            ),
        ];

        let details_text = details.join("\n");
        let details_paragraph = Paragraph::new(details_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .title(" Task Details "),
            )
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_analysis (lines 894-1014) ---[0m
fn draw_delegation_analysis(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Recent analysis results
    let analysis_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(10)
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "Frontend" => "🎨",
                "Backend" => "⚙️",
                "DevOps" => "🔧",
                "QA" => "🧪",
                _ => "🤖",
            };

            let confidence_color = if decision.confidence >= 0.8 {
                Color::Green
            } else if decision.confidence >= 0.6 {
                Color::Yellow
            } else {
                Color::Red
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::White)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("({:.0}%) ", decision.confidence * 100.0),
                    Style::default().fg(confidence_color),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::Gray),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut analysis_state = ListState::default();
    analysis_state.select(Some(app.selected_delegation));

    let analysis_list = List::new(analysis_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🔍 Analysis Results ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Blue)
                .add_modifier(Modifier::BOLD),
        )
        .highlight_symbol("► ");

    f.render_stateful_widget(analysis_list, chunks[0], &mut analysis_state);

    // Analysis details
    if let Some(decision) = app.delegation_decisions.get(app.selected_delegation) {
        let details = format!(
            "📋 Task Analysis Details\n\n\
             Task: {}\n\n\
             🎯 Recommended Agent: {}\n\
             📊 Confidence: {:.1}%\n\
             🧠 Reasoning: {}\n\
             📅 Analyzed: {}\n\n\
             💡 Press Enter to delegate this task",
            decision.task_description,
            decision.recommended_agent,
            decision.confidence * 100.0,
            decision.reasoning,
            decision.created_at.format("%H:%M:%S")
        );

        let details_paragraph = Paragraph::new(details)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Analysis Details ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::White))
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    } else {
        let empty_text = "🔍 Task Analysis\n\n\
                         No analysis results yet.\n\n\
                         Press Enter to analyze a new task\n\
                         and get Master's recommendation\n\
                         for optimal agent assignment.";

        let empty_paragraph = Paragraph::new(empty_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Ready for Analysis ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::Gray))
            .alignment(Alignment::Center)
            .wrap(Wrap { trim: true });

        f.render_widget(empty_paragraph, chunks[1]);
    }
}

  ./crates/ccswarm/src/tui/ui.rs:104-227 function draw_enhanced_system_stats <-> ./crates/ccswarm/src/tui/ui.rs:298-429 function draw_enhanced_agent_summary
  Similarity: 85.01%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_system_stats (lines 104-227) ---[0m
fn draw_enhanced_system_stats(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
        ])
        .split(area);

    // Enhanced total agents with progress bar
    let agent_percentage = (app.active_agents * 100 / app.total_agents.max(1)) as u16;
    let total_agents = Gauge::default()
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🤖 Active Agents ")
                .title_alignment(Alignment::Center),
        )
        .gauge_style(
            Style::default()
                .fg(Color::Cyan)
                .add_modifier(Modifier::BOLD),
        )
        .percent(agent_percentage)
        .label(format!(
            "{}/{} ({}%)",
            app.active_agents, app.total_agents, agent_percentage
        ));
    f.render_widget(total_agents, chunks[0]);

    // Enhanced pending tasks with icon
    let pending_text = format!("📋 {}\nqueued", app.pending_tasks);
    let pending_tasks = Paragraph::new(pending_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Pending Tasks ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(pending_tasks, chunks[1]);

    // Enhanced completed tasks with icon
    let completed_text = format!("✅ {}\ncompleted", app.completed_tasks);
    let completed_tasks = Paragraph::new(completed_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Completed Tasks ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(Color::Green)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(completed_tasks, chunks[2]);

    // Enhanced system status with icon and color
    let (status_icon, status_color) = match app.system_status.as_str() {
        "Running" => ("🟢", Color::Green),
        "Stopped" => ("🔴", Color::Red),
        _ => ("🟡", Color::Yellow),
    };
    let status_text = format!(
        "{} {}\nsystem",
        status_icon,
        app.system_status.to_lowercase()
    );
    let system_status = Paragraph::new(status_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" System Status ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(status_color)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(system_status, chunks[3]);

    // Add execution engine success rate gauge
    let success_percentage = app.success_rate as u16;
    let success_color = if success_percentage >= 90 {
        Color::Green
    } else if success_percentage >= 70 {
        Color::Yellow
    } else {
        Color::Red
    };

    let success_rate = Gauge::default()
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📊 Success Rate ")
                .title_alignment(Alignment::Center),
        )
        .gauge_style(
            Style::default()
                .fg(success_color)
                .add_modifier(Modifier::BOLD),
        )
        .percent(success_percentage)
        .label(format!("{:.1}%", app.success_rate));
    f.render_widget(success_rate, chunks[4]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_agent_summary (lines 298-429) ---[0m
fn draw_enhanced_agent_summary(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(60), Constraint::Percentage(40)])
        .split(area);

    // Enhanced agents table
    let header = Row::new(vec![
        Cell::from("Agent").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Type").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Status").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Tasks").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = app
        .agents
        .iter()
        .take(6)
        .map(|agent| {
            let (status_color, status_text, status_icon) = match agent.status {
                crate::agent::AgentStatus::Available => (Color::Green, "Available", "✅"),
                crate::agent::AgentStatus::Working => (Color::Cyan, "Working", "⚙️"),
                crate::agent::AgentStatus::Error(_) => (Color::Red, "Error", "❌"),
                crate::agent::AgentStatus::Initializing => (Color::Yellow, "Init", "🔄"),
                crate::agent::AgentStatus::WaitingForReview => (Color::Magenta, "Review", "⏳"),
                crate::agent::AgentStatus::ShuttingDown => (Color::DarkGray, "Shutdown", "⏹️"),
            };

            let is_master = agent.specialization.contains("Master");
            let (name_icon, name_color) = if is_master {
                ("👑", Color::Yellow)
            } else {
                match agent.specialization.as_str() {
                    s if s.contains("Frontend") => ("🎨", Color::Cyan),
                    s if s.contains("Backend") => ("⚙️", Color::Green),
                    s if s.contains("DevOps") => ("🔧", Color::Blue),
                    s if s.contains("QA") => ("🧪", Color::Magenta),
                    _ => ("🤖", Color::White),
                }
            };

            Row::new(vec![
                Cell::from(format!("{} {}", name_icon, agent.name)).style(
                    Style::default().fg(name_color).add_modifier(if is_master {
                        Modifier::BOLD
                    } else {
                        Modifier::empty()
                    }),
                ),
                Cell::from(agent.specialization.clone()).style(Style::default().fg(Color::White)),
                Cell::from(format!("{} {}", status_icon, status_text))
                    .style(Style::default().fg(status_color)),
                Cell::from(format!("{}", agent.tasks_completed))
                    .style(Style::default().fg(Color::Yellow)),
            ])
        })
        .collect();

    let agents_table = Table::new(
        rows,
        [
            Constraint::Length(15), // Agent
            Constraint::Length(12), // Type
            Constraint::Length(12), // Status
            Constraint::Length(6),  // Tasks
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 👥 Agents Overview ")
            .title_alignment(Alignment::Center),
    )
    .row_highlight_style(
        Style::default()
            .bg(Color::DarkGray)
            .add_modifier(Modifier::BOLD),
    );

    f.render_widget(agents_table, chunks[0]);

    // System activity summary
    let activity_text = format!(
        "🔄 System Activity\n\n\
         ⏱️  Uptime: Active\n\
         📊 Total Agents: {}\n\
         🎯 Active: {}\n\
         📋 Tasks Queued: {}\n\
         ✅ Completed: {}\n\n\
         📈 Efficiency: {:.1}%",
        app.total_agents,
        app.active_agents,
        app.pending_tasks,
        app.completed_tasks,
        if app.total_agents > 0 {
            (app.active_agents as f32 / app.total_agents as f32) * 100.0
        } else {
            0.0
        }
    );

    let activity_paragraph = Paragraph::new(activity_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📊 System Metrics ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::Cyan))
        .wrap(Wrap { trim: true });

    f.render_widget(activity_paragraph, chunks[1]);
}

  ./crates/ccswarm/src/tui/ui.rs:588-706 function draw_tasks <-> ./crates/ccswarm/src/tui/ui.rs:1420-1558 function draw_input_popup
  Similarity: 84.28%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_tasks (lines 588-706) ---[0m
fn draw_tasks(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Percentage(70), Constraint::Percentage(30)])
        .split(area);

    // Task list with enhanced headers
    let header = Row::new(vec![
        "#",
        "Description",
        "Priority",
        "Type",
        "Status",
        "Agent",
        "Created",
    ])
    .style(Style::default().fg(Color::Yellow))
    .height(1)
    .bottom_margin(1);

    let rows: Vec<Row> = app
        .tasks
        .iter()
        .enumerate()
        .map(|(i, task)| {
            let priority_color = match task.priority.as_str() {
                "High" => Color::Red,
                "Medium" => Color::Yellow,
                "Low" => Color::Green,
                _ => Color::White,
            };

            let status_color = if task.status.contains("✅") {
                Color::Green
            } else if task.status.contains("❌") {
                Color::Red
            } else if task.status.contains("🏃") {
                Color::Cyan
            } else if task.status.contains("📋") {
                Color::Yellow
            } else {
                Color::White
            };

            Row::new(vec![
                Cell::from(format!("{}", i + 1)),
                Cell::from(task.description.clone()),
                Cell::from(Span::styled(
                    task.priority.clone(),
                    Style::default().fg(priority_color),
                )),
                Cell::from(task.task_type.clone()),
                Cell::from(Span::styled(
                    task.status.clone(),
                    Style::default().fg(status_color),
                )),
                Cell::from(task.assigned_agent.as_deref().unwrap_or("-")),
                Cell::from(task.created_at.format("%H:%M:%S").to_string()),
            ])
        })
        .collect();

    let selected_style = Style::default()
        .bg(Color::Yellow)
        .fg(Color::Black)
        .add_modifier(Modifier::BOLD);

    let table = Table::new(
        rows,
        &[
            Constraint::Length(3),  // #
            Constraint::Min(25),    // Description
            Constraint::Length(8),  // Priority
            Constraint::Length(12), // Type
            Constraint::Length(15), // Status
            Constraint::Length(12), // Agent
            Constraint::Length(8),  // Created
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .title(" Tasks (t to add new task) "),
    )
    .row_highlight_style(selected_style);

    let mut table_state = ratatui::widgets::TableState::default();
    table_state.select(Some(app.selected_task));

    f.render_stateful_widget(table, chunks[0], &mut table_state);

    // Task details
    if let Some(task) = app.tasks.get(app.selected_task) {
        let details = [
            format!("ID: {}", task.id),
            format!("Description: {}", task.description),
            format!("Priority: {}", task.priority),
            format!("Type: {}", task.task_type),
            format!("Status: {}", task.status),
            format!("Created: {}", task.created_at.format("%Y-%m-%d %H:%M:%S")),
            format!(
                "Assigned Agent: {}",
                task.assigned_agent.as_deref().unwrap_or("None")
            ),
        ];

        let details_text = details.join("\n");
        let details_paragraph = Paragraph::new(details_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .title(" Task Details "),
            )
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_input_popup (lines 1420-1558) ---[0m
fn draw_input_popup(f: &mut Frame, app: &App) {
    let area = centered_rect(70, 25, f.area());

    let (title, _title_icon, border_color) = match app.input_mode {
        InputMode::AddingTask => (" 📋 Add New Task ", "📋", Color::Green),
        InputMode::CreatingAgent => (" 🤖 Create New Agent ", "🤖", Color::Blue),
        InputMode::Command => (" 💻 Command Prompt ", "💻", Color::Yellow),
        InputMode::DelegationInput => (" 🎯 Master Delegation ", "🎯", Color::Magenta),
        _ => (" ✏️  Input ", "✏️", Color::White),
    };

    let prompt = match app.input_mode {
        InputMode::AddingTask => {
            "Enter task description:\n\n\
             💡 Tips:\n\
             • Use [high]/[medium]/[low] for priority\n\
             • Use [test]/[docs]/[bug]/[feature] for type\n\
             • Example: \"Fix login bug [high] [bug]\""
        }
        InputMode::CreatingAgent => {
            "Select agent specialization:\n\n\
             🎨 frontend  - UI/UX development\n\
             ⚙️  backend   - API/Database work\n\
             🔧 devops    - Infrastructure/Deploy\n\
             🧪 qa        - Testing/Quality assurance"
        }
        InputMode::Command => {
            "Enter command:\n\n\
             💡 Quick commands:\n\
             • help - Show all commands\n\
             • start_agent master - Start master agent\n\
             • task <description> - Add new task\n\
             • status - Show system status"
        }
        InputMode::DelegationInput => match app.delegation_mode {
            DelegationMode::Analyze => {
                "🔍 Analyze Task for Delegation:\n\n\
                     Enter task description to get Master's recommendation\n\
                     💡 Examples:\n\
                     • \"Create login form with validation\"\n\
                     • \"Fix API endpoint error handling\"\n\
                     • \"Write unit tests for user service\""
            }
            DelegationMode::Delegate => {
                "🎯 Delegate Task to Agent:\n\n\
                     Format: <agent_type> <task_description>\n\
                     💡 Examples:\n\
                     • \"frontend Create responsive navigation\"\n\
                     • \"backend Add user authentication API\"\n\
                     • \"qa Write integration tests\""
            }
            _ => "Enter delegation input:",
        },
        _ => "Enter your input:",
    };

    // Create a shadow effect
    let shadow_area = Rect {
        x: area.x + 1,
        y: area.y + 1,
        width: area.width,
        height: area.height,
    };
    f.render_widget(
        Block::default().style(Style::default().bg(Color::Black)),
        shadow_area,
    );

    f.render_widget(Clear, area); // Clear background

    let input_block = Block::default()
        .title(title)
        .title_alignment(Alignment::Center)
        .borders(Borders::ALL)
        .border_type(BorderType::Rounded)
        .border_style(
            Style::default()
                .fg(border_color)
                .add_modifier(Modifier::BOLD),
        );

    let input_area = input_block.inner(area);
    f.render_widget(input_block, area);

    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(8), // Prompt area
            Constraint::Length(3), // Input area
            Constraint::Length(2), // Help area
        ])
        .split(input_area);

    // Prompt text
    let prompt_paragraph = Paragraph::new(prompt)
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });
    f.render_widget(prompt_paragraph, chunks[0]);

    // Input box with cursor
    let input_text = format!("{}_", app.input_buffer);
    let input_paragraph = Paragraph::new(input_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Input ")
                .border_style(Style::default().fg(Color::Cyan)),
        )
        .style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        );
    f.render_widget(input_paragraph, chunks[1]);

    // Help text
    let help_text = vec![Line::from(vec![
        Span::styled(
            " Enter ",
            Style::default()
                .fg(Color::Black)
                .bg(Color::Green)
                .add_modifier(Modifier::BOLD),
        ),
        Span::styled(" Confirm ", Style::default().fg(Color::Gray)),
        Span::raw("  "),
        Span::styled(
            " Esc ",
            Style::default()
                .fg(Color::Black)
                .bg(Color::Red)
                .add_modifier(Modifier::BOLD),
        ),
        Span::styled(" Cancel ", Style::default().fg(Color::Gray)),
    ])];
    let help_paragraph = Paragraph::new(help_text).alignment(Alignment::Center);
    f.render_widget(help_paragraph, chunks[2]);
}

  ./crates/ccswarm/src/tui/ui.rs:588-706 function draw_tasks <-> ./crates/ccswarm/src/tui/ui.rs:709-810 function draw_logs
  Similarity: 95.35%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_tasks (lines 588-706) ---[0m
fn draw_tasks(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Percentage(70), Constraint::Percentage(30)])
        .split(area);

    // Task list with enhanced headers
    let header = Row::new(vec![
        "#",
        "Description",
        "Priority",
        "Type",
        "Status",
        "Agent",
        "Created",
    ])
    .style(Style::default().fg(Color::Yellow))
    .height(1)
    .bottom_margin(1);

    let rows: Vec<Row> = app
        .tasks
        .iter()
        .enumerate()
        .map(|(i, task)| {
            let priority_color = match task.priority.as_str() {
                "High" => Color::Red,
                "Medium" => Color::Yellow,
                "Low" => Color::Green,
                _ => Color::White,
            };

            let status_color = if task.status.contains("✅") {
                Color::Green
            } else if task.status.contains("❌") {
                Color::Red
            } else if task.status.contains("🏃") {
                Color::Cyan
            } else if task.status.contains("📋") {
                Color::Yellow
            } else {
                Color::White
            };

            Row::new(vec![
                Cell::from(format!("{}", i + 1)),
                Cell::from(task.description.clone()),
                Cell::from(Span::styled(
                    task.priority.clone(),
                    Style::default().fg(priority_color),
                )),
                Cell::from(task.task_type.clone()),
                Cell::from(Span::styled(
                    task.status.clone(),
                    Style::default().fg(status_color),
                )),
                Cell::from(task.assigned_agent.as_deref().unwrap_or("-")),
                Cell::from(task.created_at.format("%H:%M:%S").to_string()),
            ])
        })
        .collect();

    let selected_style = Style::default()
        .bg(Color::Yellow)
        .fg(Color::Black)
        .add_modifier(Modifier::BOLD);

    let table = Table::new(
        rows,
        &[
            Constraint::Length(3),  // #
            Constraint::Min(25),    // Description
            Constraint::Length(8),  // Priority
            Constraint::Length(12), // Type
            Constraint::Length(15), // Status
            Constraint::Length(12), // Agent
            Constraint::Length(8),  // Created
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .title(" Tasks (t to add new task) "),
    )
    .row_highlight_style(selected_style);

    let mut table_state = ratatui::widgets::TableState::default();
    table_state.select(Some(app.selected_task));

    f.render_stateful_widget(table, chunks[0], &mut table_state);

    // Task details
    if let Some(task) = app.tasks.get(app.selected_task) {
        let details = [
            format!("ID: {}", task.id),
            format!("Description: {}", task.description),
            format!("Priority: {}", task.priority),
            format!("Type: {}", task.task_type),
            format!("Status: {}", task.status),
            format!("Created: {}", task.created_at.format("%Y-%m-%d %H:%M:%S")),
            format!(
                "Assigned Agent: {}",
                task.assigned_agent.as_deref().unwrap_or("None")
            ),
        ];

        let details_text = details.join("\n");
        let details_paragraph = Paragraph::new(details_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .title(" Task Details "),
            )
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_logs (lines 709-810) ---[0m
fn draw_logs(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(3), Constraint::Min(0)])
        .split(area);

    // Log level summary bar
    let log_counts = app
        .logs
        .iter()
        .fold(std::collections::HashMap::new(), |mut acc, log| {
            *acc.entry(log.level.as_str()).or_insert(0) += 1;
            acc
        });

    let summary_text = format!(
        "📊 Logs: 🔴 {} errors | 🟡 {} warnings | 🟢 {} info | 🔵 {} debug | Total: {}",
        log_counts.get("ERROR").unwrap_or(&0),
        log_counts.get("WARN").unwrap_or(&0),
        log_counts.get("INFO").unwrap_or(&0),
        log_counts.get("DEBUG").unwrap_or(&0),
        app.logs.len()
    );

    let summary_paragraph = Paragraph::new(summary_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📋 Log Summary ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .alignment(Alignment::Center);

    f.render_widget(summary_paragraph, chunks[0]);

    // Enhanced logs with better visual hierarchy
    let items: Vec<ListItem> = app
        .logs
        .iter()
        .rev() // Show newest logs first
        .take(100) // Limit to prevent performance issues
        .map(|log| {
            let (level_color, level_icon) = match log.level.as_str() {
                "ERROR" => (Color::Red, "🔴"),
                "WARN" => (Color::Yellow, "🟡"),
                "INFO" => (Color::Green, "🟢"),
                "DEBUG" => (Color::Cyan, "🔵"),
                _ => (Color::White, "⚪"),
            };

            let agent_name = log.agent.as_deref().unwrap_or("System");
            let (agent_icon, agent_color) = if agent_name == "System" {
                ("🖥️", Color::Gray)
            } else if agent_name.contains("master") || agent_name.contains("Master") {
                ("👑", Color::Yellow)
            } else {
                ("🤖", Color::Cyan)
            };

            let content = vec![Line::from(vec![
                Span::styled(
                    format!("{} ", log.timestamp.format("%H:%M:%S")),
                    Style::default().fg(Color::DarkGray),
                ),
                Span::styled(format!("{} ", level_icon), Style::default().fg(level_color)),
                Span::styled(
                    format!("{:<5} ", log.level),
                    Style::default()
                        .fg(level_color)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("{} {:<10} ", agent_icon, agent_name),
                    Style::default().fg(agent_color),
                ),
                Span::styled(log.message.clone(), Style::default().fg(Color::White)),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut logs_state = ListState::default();
    logs_state.select(Some(app.selected_log));

    let logs_list = List::new(items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📜 Live Logs (r: refresh, ↑↓: navigate) ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::DarkGray)
                .add_modifier(Modifier::BOLD),
        );

    f.render_stateful_widget(logs_list, chunks[1], &mut logs_state);
}

  ./crates/ccswarm/src/tui/ui.rs:894-1014 function draw_delegation_analysis <-> ./crates/ccswarm/src/tui/ui.rs:1420-1558 function draw_input_popup
  Similarity: 80.95%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_analysis (lines 894-1014) ---[0m
fn draw_delegation_analysis(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Recent analysis results
    let analysis_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(10)
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "Frontend" => "🎨",
                "Backend" => "⚙️",
                "DevOps" => "🔧",
                "QA" => "🧪",
                _ => "🤖",
            };

            let confidence_color = if decision.confidence >= 0.8 {
                Color::Green
            } else if decision.confidence >= 0.6 {
                Color::Yellow
            } else {
                Color::Red
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::White)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("({:.0}%) ", decision.confidence * 100.0),
                    Style::default().fg(confidence_color),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::Gray),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut analysis_state = ListState::default();
    analysis_state.select(Some(app.selected_delegation));

    let analysis_list = List::new(analysis_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🔍 Analysis Results ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Blue)
                .add_modifier(Modifier::BOLD),
        )
        .highlight_symbol("► ");

    f.render_stateful_widget(analysis_list, chunks[0], &mut analysis_state);

    // Analysis details
    if let Some(decision) = app.delegation_decisions.get(app.selected_delegation) {
        let details = format!(
            "📋 Task Analysis Details\n\n\
             Task: {}\n\n\
             🎯 Recommended Agent: {}\n\
             📊 Confidence: {:.1}%\n\
             🧠 Reasoning: {}\n\
             📅 Analyzed: {}\n\n\
             💡 Press Enter to delegate this task",
            decision.task_description,
            decision.recommended_agent,
            decision.confidence * 100.0,
            decision.reasoning,
            decision.created_at.format("%H:%M:%S")
        );

        let details_paragraph = Paragraph::new(details)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Analysis Details ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::White))
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    } else {
        let empty_text = "🔍 Task Analysis\n\n\
                         No analysis results yet.\n\n\
                         Press Enter to analyze a new task\n\
                         and get Master's recommendation\n\
                         for optimal agent assignment.";

        let empty_paragraph = Paragraph::new(empty_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Ready for Analysis ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::Gray))
            .alignment(Alignment::Center)
            .wrap(Wrap { trim: true });

        f.render_widget(empty_paragraph, chunks[1]);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_input_popup (lines 1420-1558) ---[0m
fn draw_input_popup(f: &mut Frame, app: &App) {
    let area = centered_rect(70, 25, f.area());

    let (title, _title_icon, border_color) = match app.input_mode {
        InputMode::AddingTask => (" 📋 Add New Task ", "📋", Color::Green),
        InputMode::CreatingAgent => (" 🤖 Create New Agent ", "🤖", Color::Blue),
        InputMode::Command => (" 💻 Command Prompt ", "💻", Color::Yellow),
        InputMode::DelegationInput => (" 🎯 Master Delegation ", "🎯", Color::Magenta),
        _ => (" ✏️  Input ", "✏️", Color::White),
    };

    let prompt = match app.input_mode {
        InputMode::AddingTask => {
            "Enter task description:\n\n\
             💡 Tips:\n\
             • Use [high]/[medium]/[low] for priority\n\
             • Use [test]/[docs]/[bug]/[feature] for type\n\
             • Example: \"Fix login bug [high] [bug]\""
        }
        InputMode::CreatingAgent => {
            "Select agent specialization:\n\n\
             🎨 frontend  - UI/UX development\n\
             ⚙️  backend   - API/Database work\n\
             🔧 devops    - Infrastructure/Deploy\n\
             🧪 qa        - Testing/Quality assurance"
        }
        InputMode::Command => {
            "Enter command:\n\n\
             💡 Quick commands:\n\
             • help - Show all commands\n\
             • start_agent master - Start master agent\n\
             • task <description> - Add new task\n\
             • status - Show system status"
        }
        InputMode::DelegationInput => match app.delegation_mode {
            DelegationMode::Analyze => {
                "🔍 Analyze Task for Delegation:\n\n\
                     Enter task description to get Master's recommendation\n\
                     💡 Examples:\n\
                     • \"Create login form with validation\"\n\
                     • \"Fix API endpoint error handling\"\n\
                     • \"Write unit tests for user service\""
            }
            DelegationMode::Delegate => {
                "🎯 Delegate Task to Agent:\n\n\
                     Format: <agent_type> <task_description>\n\
                     💡 Examples:\n\
                     • \"frontend Create responsive navigation\"\n\
                     • \"backend Add user authentication API\"\n\
                     • \"qa Write integration tests\""
            }
            _ => "Enter delegation input:",
        },
        _ => "Enter your input:",
    };

    // Create a shadow effect
    let shadow_area = Rect {
        x: area.x + 1,
        y: area.y + 1,
        width: area.width,
        height: area.height,
    };
    f.render_widget(
        Block::default().style(Style::default().bg(Color::Black)),
        shadow_area,
    );

    f.render_widget(Clear, area); // Clear background

    let input_block = Block::default()
        .title(title)
        .title_alignment(Alignment::Center)
        .borders(Borders::ALL)
        .border_type(BorderType::Rounded)
        .border_style(
            Style::default()
                .fg(border_color)
                .add_modifier(Modifier::BOLD),
        );

    let input_area = input_block.inner(area);
    f.render_widget(input_block, area);

    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(8), // Prompt area
            Constraint::Length(3), // Input area
            Constraint::Length(2), // Help area
        ])
        .split(input_area);

    // Prompt text
    let prompt_paragraph = Paragraph::new(prompt)
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });
    f.render_widget(prompt_paragraph, chunks[0]);

    // Input box with cursor
    let input_text = format!("{}_", app.input_buffer);
    let input_paragraph = Paragraph::new(input_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Input ")
                .border_style(Style::default().fg(Color::Cyan)),
        )
        .style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        );
    f.render_widget(input_paragraph, chunks[1]);

    // Help text
    let help_text = vec![Line::from(vec![
        Span::styled(
            " Enter ",
            Style::default()
                .fg(Color::Black)
                .bg(Color::Green)
                .add_modifier(Modifier::BOLD),
        ),
        Span::styled(" Confirm ", Style::default().fg(Color::Gray)),
        Span::raw("  "),
        Span::styled(
            " Esc ",
            Style::default()
                .fg(Color::Black)
                .bg(Color::Red)
                .add_modifier(Modifier::BOLD),
        ),
        Span::styled(" Cancel ", Style::default().fg(Color::Gray)),
    ])];
    let help_paragraph = Paragraph::new(help_text).alignment(Alignment::Center);
    f.render_widget(help_paragraph, chunks[2]);
}

  ./crates/ccswarm/src/tui/ui.rs:709-810 function draw_logs <-> ./crates/ccswarm/src/tui/ui.rs:1420-1558 function draw_input_popup
  Similarity: 86.59%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_logs (lines 709-810) ---[0m
fn draw_logs(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(3), Constraint::Min(0)])
        .split(area);

    // Log level summary bar
    let log_counts = app
        .logs
        .iter()
        .fold(std::collections::HashMap::new(), |mut acc, log| {
            *acc.entry(log.level.as_str()).or_insert(0) += 1;
            acc
        });

    let summary_text = format!(
        "📊 Logs: 🔴 {} errors | 🟡 {} warnings | 🟢 {} info | 🔵 {} debug | Total: {}",
        log_counts.get("ERROR").unwrap_or(&0),
        log_counts.get("WARN").unwrap_or(&0),
        log_counts.get("INFO").unwrap_or(&0),
        log_counts.get("DEBUG").unwrap_or(&0),
        app.logs.len()
    );

    let summary_paragraph = Paragraph::new(summary_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📋 Log Summary ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .alignment(Alignment::Center);

    f.render_widget(summary_paragraph, chunks[0]);

    // Enhanced logs with better visual hierarchy
    let items: Vec<ListItem> = app
        .logs
        .iter()
        .rev() // Show newest logs first
        .take(100) // Limit to prevent performance issues
        .map(|log| {
            let (level_color, level_icon) = match log.level.as_str() {
                "ERROR" => (Color::Red, "🔴"),
                "WARN" => (Color::Yellow, "🟡"),
                "INFO" => (Color::Green, "🟢"),
                "DEBUG" => (Color::Cyan, "🔵"),
                _ => (Color::White, "⚪"),
            };

            let agent_name = log.agent.as_deref().unwrap_or("System");
            let (agent_icon, agent_color) = if agent_name == "System" {
                ("🖥️", Color::Gray)
            } else if agent_name.contains("master") || agent_name.contains("Master") {
                ("👑", Color::Yellow)
            } else {
                ("🤖", Color::Cyan)
            };

            let content = vec![Line::from(vec![
                Span::styled(
                    format!("{} ", log.timestamp.format("%H:%M:%S")),
                    Style::default().fg(Color::DarkGray),
                ),
                Span::styled(format!("{} ", level_icon), Style::default().fg(level_color)),
                Span::styled(
                    format!("{:<5} ", log.level),
                    Style::default()
                        .fg(level_color)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("{} {:<10} ", agent_icon, agent_name),
                    Style::default().fg(agent_color),
                ),
                Span::styled(log.message.clone(), Style::default().fg(Color::White)),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut logs_state = ListState::default();
    logs_state.select(Some(app.selected_log));

    let logs_list = List::new(items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📜 Live Logs (r: refresh, ↑↓: navigate) ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::DarkGray)
                .add_modifier(Modifier::BOLD),
        );

    f.render_stateful_widget(logs_list, chunks[1], &mut logs_state);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_input_popup (lines 1420-1558) ---[0m
fn draw_input_popup(f: &mut Frame, app: &App) {
    let area = centered_rect(70, 25, f.area());

    let (title, _title_icon, border_color) = match app.input_mode {
        InputMode::AddingTask => (" 📋 Add New Task ", "📋", Color::Green),
        InputMode::CreatingAgent => (" 🤖 Create New Agent ", "🤖", Color::Blue),
        InputMode::Command => (" 💻 Command Prompt ", "💻", Color::Yellow),
        InputMode::DelegationInput => (" 🎯 Master Delegation ", "🎯", Color::Magenta),
        _ => (" ✏️  Input ", "✏️", Color::White),
    };

    let prompt = match app.input_mode {
        InputMode::AddingTask => {
            "Enter task description:\n\n\
             💡 Tips:\n\
             • Use [high]/[medium]/[low] for priority\n\
             • Use [test]/[docs]/[bug]/[feature] for type\n\
             • Example: \"Fix login bug [high] [bug]\""
        }
        InputMode::CreatingAgent => {
            "Select agent specialization:\n\n\
             🎨 frontend  - UI/UX development\n\
             ⚙️  backend   - API/Database work\n\
             🔧 devops    - Infrastructure/Deploy\n\
             🧪 qa        - Testing/Quality assurance"
        }
        InputMode::Command => {
            "Enter command:\n\n\
             💡 Quick commands:\n\
             • help - Show all commands\n\
             • start_agent master - Start master agent\n\
             • task <description> - Add new task\n\
             • status - Show system status"
        }
        InputMode::DelegationInput => match app.delegation_mode {
            DelegationMode::Analyze => {
                "🔍 Analyze Task for Delegation:\n\n\
                     Enter task description to get Master's recommendation\n\
                     💡 Examples:\n\
                     • \"Create login form with validation\"\n\
                     • \"Fix API endpoint error handling\"\n\
                     • \"Write unit tests for user service\""
            }
            DelegationMode::Delegate => {
                "🎯 Delegate Task to Agent:\n\n\
                     Format: <agent_type> <task_description>\n\
                     💡 Examples:\n\
                     • \"frontend Create responsive navigation\"\n\
                     • \"backend Add user authentication API\"\n\
                     • \"qa Write integration tests\""
            }
            _ => "Enter delegation input:",
        },
        _ => "Enter your input:",
    };

    // Create a shadow effect
    let shadow_area = Rect {
        x: area.x + 1,
        y: area.y + 1,
        width: area.width,
        height: area.height,
    };
    f.render_widget(
        Block::default().style(Style::default().bg(Color::Black)),
        shadow_area,
    );

    f.render_widget(Clear, area); // Clear background

    let input_block = Block::default()
        .title(title)
        .title_alignment(Alignment::Center)
        .borders(Borders::ALL)
        .border_type(BorderType::Rounded)
        .border_style(
            Style::default()
                .fg(border_color)
                .add_modifier(Modifier::BOLD),
        );

    let input_area = input_block.inner(area);
    f.render_widget(input_block, area);

    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(8), // Prompt area
            Constraint::Length(3), // Input area
            Constraint::Length(2), // Help area
        ])
        .split(input_area);

    // Prompt text
    let prompt_paragraph = Paragraph::new(prompt)
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });
    f.render_widget(prompt_paragraph, chunks[0]);

    // Input box with cursor
    let input_text = format!("{}_", app.input_buffer);
    let input_paragraph = Paragraph::new(input_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Input ")
                .border_style(Style::default().fg(Color::Cyan)),
        )
        .style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        );
    f.render_widget(input_paragraph, chunks[1]);

    // Help text
    let help_text = vec![Line::from(vec![
        Span::styled(
            " Enter ",
            Style::default()
                .fg(Color::Black)
                .bg(Color::Green)
                .add_modifier(Modifier::BOLD),
        ),
        Span::styled(" Confirm ", Style::default().fg(Color::Gray)),
        Span::raw("  "),
        Span::styled(
            " Esc ",
            Style::default()
                .fg(Color::Black)
                .bg(Color::Red)
                .add_modifier(Modifier::BOLD),
        ),
        Span::styled(" Cancel ", Style::default().fg(Color::Gray)),
    ])];
    let help_paragraph = Paragraph::new(help_text).alignment(Alignment::Center);
    f.render_widget(help_paragraph, chunks[2]);
}

  ./crates/ccswarm/src/tui/ui.rs:709-810 function draw_logs <-> ./crates/ccswarm/src/tui/ui.rs:894-1014 function draw_delegation_analysis
  Similarity: 93.32%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_logs (lines 709-810) ---[0m
fn draw_logs(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(3), Constraint::Min(0)])
        .split(area);

    // Log level summary bar
    let log_counts = app
        .logs
        .iter()
        .fold(std::collections::HashMap::new(), |mut acc, log| {
            *acc.entry(log.level.as_str()).or_insert(0) += 1;
            acc
        });

    let summary_text = format!(
        "📊 Logs: 🔴 {} errors | 🟡 {} warnings | 🟢 {} info | 🔵 {} debug | Total: {}",
        log_counts.get("ERROR").unwrap_or(&0),
        log_counts.get("WARN").unwrap_or(&0),
        log_counts.get("INFO").unwrap_or(&0),
        log_counts.get("DEBUG").unwrap_or(&0),
        app.logs.len()
    );

    let summary_paragraph = Paragraph::new(summary_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📋 Log Summary ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .alignment(Alignment::Center);

    f.render_widget(summary_paragraph, chunks[0]);

    // Enhanced logs with better visual hierarchy
    let items: Vec<ListItem> = app
        .logs
        .iter()
        .rev() // Show newest logs first
        .take(100) // Limit to prevent performance issues
        .map(|log| {
            let (level_color, level_icon) = match log.level.as_str() {
                "ERROR" => (Color::Red, "🔴"),
                "WARN" => (Color::Yellow, "🟡"),
                "INFO" => (Color::Green, "🟢"),
                "DEBUG" => (Color::Cyan, "🔵"),
                _ => (Color::White, "⚪"),
            };

            let agent_name = log.agent.as_deref().unwrap_or("System");
            let (agent_icon, agent_color) = if agent_name == "System" {
                ("🖥️", Color::Gray)
            } else if agent_name.contains("master") || agent_name.contains("Master") {
                ("👑", Color::Yellow)
            } else {
                ("🤖", Color::Cyan)
            };

            let content = vec![Line::from(vec![
                Span::styled(
                    format!("{} ", log.timestamp.format("%H:%M:%S")),
                    Style::default().fg(Color::DarkGray),
                ),
                Span::styled(format!("{} ", level_icon), Style::default().fg(level_color)),
                Span::styled(
                    format!("{:<5} ", log.level),
                    Style::default()
                        .fg(level_color)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("{} {:<10} ", agent_icon, agent_name),
                    Style::default().fg(agent_color),
                ),
                Span::styled(log.message.clone(), Style::default().fg(Color::White)),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut logs_state = ListState::default();
    logs_state.select(Some(app.selected_log));

    let logs_list = List::new(items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📜 Live Logs (r: refresh, ↑↓: navigate) ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::DarkGray)
                .add_modifier(Modifier::BOLD),
        );

    f.render_stateful_widget(logs_list, chunks[1], &mut logs_state);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_analysis (lines 894-1014) ---[0m
fn draw_delegation_analysis(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Recent analysis results
    let analysis_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(10)
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "Frontend" => "🎨",
                "Backend" => "⚙️",
                "DevOps" => "🔧",
                "QA" => "🧪",
                _ => "🤖",
            };

            let confidence_color = if decision.confidence >= 0.8 {
                Color::Green
            } else if decision.confidence >= 0.6 {
                Color::Yellow
            } else {
                Color::Red
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::White)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("({:.0}%) ", decision.confidence * 100.0),
                    Style::default().fg(confidence_color),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::Gray),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut analysis_state = ListState::default();
    analysis_state.select(Some(app.selected_delegation));

    let analysis_list = List::new(analysis_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🔍 Analysis Results ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Blue)
                .add_modifier(Modifier::BOLD),
        )
        .highlight_symbol("► ");

    f.render_stateful_widget(analysis_list, chunks[0], &mut analysis_state);

    // Analysis details
    if let Some(decision) = app.delegation_decisions.get(app.selected_delegation) {
        let details = format!(
            "📋 Task Analysis Details\n\n\
             Task: {}\n\n\
             🎯 Recommended Agent: {}\n\
             📊 Confidence: {:.1}%\n\
             🧠 Reasoning: {}\n\
             📅 Analyzed: {}\n\n\
             💡 Press Enter to delegate this task",
            decision.task_description,
            decision.recommended_agent,
            decision.confidence * 100.0,
            decision.reasoning,
            decision.created_at.format("%H:%M:%S")
        );

        let details_paragraph = Paragraph::new(details)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Analysis Details ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::White))
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    } else {
        let empty_text = "🔍 Task Analysis\n\n\
                         No analysis results yet.\n\n\
                         Press Enter to analyze a new task\n\
                         and get Master's recommendation\n\
                         for optimal agent assignment.";

        let empty_paragraph = Paragraph::new(empty_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Ready for Analysis ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::Gray))
            .alignment(Alignment::Center)
            .wrap(Wrap { trim: true });

        f.render_widget(empty_paragraph, chunks[1]);
    }
}

  ./crates/ccswarm/src/tui/ui.rs:104-227 function draw_enhanced_system_stats <-> ./crates/ccswarm/src/tui/ui.rs:588-706 function draw_tasks
  Similarity: 83.06%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_system_stats (lines 104-227) ---[0m
fn draw_enhanced_system_stats(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
        ])
        .split(area);

    // Enhanced total agents with progress bar
    let agent_percentage = (app.active_agents * 100 / app.total_agents.max(1)) as u16;
    let total_agents = Gauge::default()
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🤖 Active Agents ")
                .title_alignment(Alignment::Center),
        )
        .gauge_style(
            Style::default()
                .fg(Color::Cyan)
                .add_modifier(Modifier::BOLD),
        )
        .percent(agent_percentage)
        .label(format!(
            "{}/{} ({}%)",
            app.active_agents, app.total_agents, agent_percentage
        ));
    f.render_widget(total_agents, chunks[0]);

    // Enhanced pending tasks with icon
    let pending_text = format!("📋 {}\nqueued", app.pending_tasks);
    let pending_tasks = Paragraph::new(pending_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Pending Tasks ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(pending_tasks, chunks[1]);

    // Enhanced completed tasks with icon
    let completed_text = format!("✅ {}\ncompleted", app.completed_tasks);
    let completed_tasks = Paragraph::new(completed_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Completed Tasks ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(Color::Green)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(completed_tasks, chunks[2]);

    // Enhanced system status with icon and color
    let (status_icon, status_color) = match app.system_status.as_str() {
        "Running" => ("🟢", Color::Green),
        "Stopped" => ("🔴", Color::Red),
        _ => ("🟡", Color::Yellow),
    };
    let status_text = format!(
        "{} {}\nsystem",
        status_icon,
        app.system_status.to_lowercase()
    );
    let system_status = Paragraph::new(status_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" System Status ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(status_color)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(system_status, chunks[3]);

    // Add execution engine success rate gauge
    let success_percentage = app.success_rate as u16;
    let success_color = if success_percentage >= 90 {
        Color::Green
    } else if success_percentage >= 70 {
        Color::Yellow
    } else {
        Color::Red
    };

    let success_rate = Gauge::default()
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📊 Success Rate ")
                .title_alignment(Alignment::Center),
        )
        .gauge_style(
            Style::default()
                .fg(success_color)
                .add_modifier(Modifier::BOLD),
        )
        .percent(success_percentage)
        .label(format!("{:.1}%", app.success_rate));
    f.render_widget(success_rate, chunks[4]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_tasks (lines 588-706) ---[0m
fn draw_tasks(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Percentage(70), Constraint::Percentage(30)])
        .split(area);

    // Task list with enhanced headers
    let header = Row::new(vec![
        "#",
        "Description",
        "Priority",
        "Type",
        "Status",
        "Agent",
        "Created",
    ])
    .style(Style::default().fg(Color::Yellow))
    .height(1)
    .bottom_margin(1);

    let rows: Vec<Row> = app
        .tasks
        .iter()
        .enumerate()
        .map(|(i, task)| {
            let priority_color = match task.priority.as_str() {
                "High" => Color::Red,
                "Medium" => Color::Yellow,
                "Low" => Color::Green,
                _ => Color::White,
            };

            let status_color = if task.status.contains("✅") {
                Color::Green
            } else if task.status.contains("❌") {
                Color::Red
            } else if task.status.contains("🏃") {
                Color::Cyan
            } else if task.status.contains("📋") {
                Color::Yellow
            } else {
                Color::White
            };

            Row::new(vec![
                Cell::from(format!("{}", i + 1)),
                Cell::from(task.description.clone()),
                Cell::from(Span::styled(
                    task.priority.clone(),
                    Style::default().fg(priority_color),
                )),
                Cell::from(task.task_type.clone()),
                Cell::from(Span::styled(
                    task.status.clone(),
                    Style::default().fg(status_color),
                )),
                Cell::from(task.assigned_agent.as_deref().unwrap_or("-")),
                Cell::from(task.created_at.format("%H:%M:%S").to_string()),
            ])
        })
        .collect();

    let selected_style = Style::default()
        .bg(Color::Yellow)
        .fg(Color::Black)
        .add_modifier(Modifier::BOLD);

    let table = Table::new(
        rows,
        &[
            Constraint::Length(3),  // #
            Constraint::Min(25),    // Description
            Constraint::Length(8),  // Priority
            Constraint::Length(12), // Type
            Constraint::Length(15), // Status
            Constraint::Length(12), // Agent
            Constraint::Length(8),  // Created
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .title(" Tasks (t to add new task) "),
    )
    .row_highlight_style(selected_style);

    let mut table_state = ratatui::widgets::TableState::default();
    table_state.select(Some(app.selected_task));

    f.render_stateful_widget(table, chunks[0], &mut table_state);

    // Task details
    if let Some(task) = app.tasks.get(app.selected_task) {
        let details = [
            format!("ID: {}", task.id),
            format!("Description: {}", task.description),
            format!("Priority: {}", task.priority),
            format!("Type: {}", task.task_type),
            format!("Status: {}", task.status),
            format!("Created: {}", task.created_at.format("%Y-%m-%d %H:%M:%S")),
            format!(
                "Assigned Agent: {}",
                task.assigned_agent.as_deref().unwrap_or("None")
            ),
        ];

        let details_text = details.join("\n");
        let details_paragraph = Paragraph::new(details_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .title(" Task Details "),
            )
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    }
}

  ./crates/ccswarm/src/tui/ui.rs:104-227 function draw_enhanced_system_stats <-> ./crates/ccswarm/src/tui/ui.rs:709-810 function draw_logs
  Similarity: 85.45%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_system_stats (lines 104-227) ---[0m
fn draw_enhanced_system_stats(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
        ])
        .split(area);

    // Enhanced total agents with progress bar
    let agent_percentage = (app.active_agents * 100 / app.total_agents.max(1)) as u16;
    let total_agents = Gauge::default()
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🤖 Active Agents ")
                .title_alignment(Alignment::Center),
        )
        .gauge_style(
            Style::default()
                .fg(Color::Cyan)
                .add_modifier(Modifier::BOLD),
        )
        .percent(agent_percentage)
        .label(format!(
            "{}/{} ({}%)",
            app.active_agents, app.total_agents, agent_percentage
        ));
    f.render_widget(total_agents, chunks[0]);

    // Enhanced pending tasks with icon
    let pending_text = format!("📋 {}\nqueued", app.pending_tasks);
    let pending_tasks = Paragraph::new(pending_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Pending Tasks ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(pending_tasks, chunks[1]);

    // Enhanced completed tasks with icon
    let completed_text = format!("✅ {}\ncompleted", app.completed_tasks);
    let completed_tasks = Paragraph::new(completed_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Completed Tasks ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(Color::Green)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(completed_tasks, chunks[2]);

    // Enhanced system status with icon and color
    let (status_icon, status_color) = match app.system_status.as_str() {
        "Running" => ("🟢", Color::Green),
        "Stopped" => ("🔴", Color::Red),
        _ => ("🟡", Color::Yellow),
    };
    let status_text = format!(
        "{} {}\nsystem",
        status_icon,
        app.system_status.to_lowercase()
    );
    let system_status = Paragraph::new(status_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" System Status ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(status_color)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(system_status, chunks[3]);

    // Add execution engine success rate gauge
    let success_percentage = app.success_rate as u16;
    let success_color = if success_percentage >= 90 {
        Color::Green
    } else if success_percentage >= 70 {
        Color::Yellow
    } else {
        Color::Red
    };

    let success_rate = Gauge::default()
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📊 Success Rate ")
                .title_alignment(Alignment::Center),
        )
        .gauge_style(
            Style::default()
                .fg(success_color)
                .add_modifier(Modifier::BOLD),
        )
        .percent(success_percentage)
        .label(format!("{:.1}%", app.success_rate));
    f.render_widget(success_rate, chunks[4]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_logs (lines 709-810) ---[0m
fn draw_logs(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(3), Constraint::Min(0)])
        .split(area);

    // Log level summary bar
    let log_counts = app
        .logs
        .iter()
        .fold(std::collections::HashMap::new(), |mut acc, log| {
            *acc.entry(log.level.as_str()).or_insert(0) += 1;
            acc
        });

    let summary_text = format!(
        "📊 Logs: 🔴 {} errors | 🟡 {} warnings | 🟢 {} info | 🔵 {} debug | Total: {}",
        log_counts.get("ERROR").unwrap_or(&0),
        log_counts.get("WARN").unwrap_or(&0),
        log_counts.get("INFO").unwrap_or(&0),
        log_counts.get("DEBUG").unwrap_or(&0),
        app.logs.len()
    );

    let summary_paragraph = Paragraph::new(summary_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📋 Log Summary ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .alignment(Alignment::Center);

    f.render_widget(summary_paragraph, chunks[0]);

    // Enhanced logs with better visual hierarchy
    let items: Vec<ListItem> = app
        .logs
        .iter()
        .rev() // Show newest logs first
        .take(100) // Limit to prevent performance issues
        .map(|log| {
            let (level_color, level_icon) = match log.level.as_str() {
                "ERROR" => (Color::Red, "🔴"),
                "WARN" => (Color::Yellow, "🟡"),
                "INFO" => (Color::Green, "🟢"),
                "DEBUG" => (Color::Cyan, "🔵"),
                _ => (Color::White, "⚪"),
            };

            let agent_name = log.agent.as_deref().unwrap_or("System");
            let (agent_icon, agent_color) = if agent_name == "System" {
                ("🖥️", Color::Gray)
            } else if agent_name.contains("master") || agent_name.contains("Master") {
                ("👑", Color::Yellow)
            } else {
                ("🤖", Color::Cyan)
            };

            let content = vec![Line::from(vec![
                Span::styled(
                    format!("{} ", log.timestamp.format("%H:%M:%S")),
                    Style::default().fg(Color::DarkGray),
                ),
                Span::styled(format!("{} ", level_icon), Style::default().fg(level_color)),
                Span::styled(
                    format!("{:<5} ", log.level),
                    Style::default()
                        .fg(level_color)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("{} {:<10} ", agent_icon, agent_name),
                    Style::default().fg(agent_color),
                ),
                Span::styled(log.message.clone(), Style::default().fg(Color::White)),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut logs_state = ListState::default();
    logs_state.select(Some(app.selected_log));

    let logs_list = List::new(items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📜 Live Logs (r: refresh, ↑↓: navigate) ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::DarkGray)
                .add_modifier(Modifier::BOLD),
        );

    f.render_stateful_widget(logs_list, chunks[1], &mut logs_state);
}

  ./crates/ccswarm/src/tui/ui.rs:104-227 function draw_enhanced_system_stats <-> ./crates/ccswarm/src/tui/ui.rs:894-1014 function draw_delegation_analysis
  Similarity: 77.31%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_system_stats (lines 104-227) ---[0m
fn draw_enhanced_system_stats(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
        ])
        .split(area);

    // Enhanced total agents with progress bar
    let agent_percentage = (app.active_agents * 100 / app.total_agents.max(1)) as u16;
    let total_agents = Gauge::default()
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🤖 Active Agents ")
                .title_alignment(Alignment::Center),
        )
        .gauge_style(
            Style::default()
                .fg(Color::Cyan)
                .add_modifier(Modifier::BOLD),
        )
        .percent(agent_percentage)
        .label(format!(
            "{}/{} ({}%)",
            app.active_agents, app.total_agents, agent_percentage
        ));
    f.render_widget(total_agents, chunks[0]);

    // Enhanced pending tasks with icon
    let pending_text = format!("📋 {}\nqueued", app.pending_tasks);
    let pending_tasks = Paragraph::new(pending_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Pending Tasks ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(pending_tasks, chunks[1]);

    // Enhanced completed tasks with icon
    let completed_text = format!("✅ {}\ncompleted", app.completed_tasks);
    let completed_tasks = Paragraph::new(completed_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Completed Tasks ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(Color::Green)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(completed_tasks, chunks[2]);

    // Enhanced system status with icon and color
    let (status_icon, status_color) = match app.system_status.as_str() {
        "Running" => ("🟢", Color::Green),
        "Stopped" => ("🔴", Color::Red),
        _ => ("🟡", Color::Yellow),
    };
    let status_text = format!(
        "{} {}\nsystem",
        status_icon,
        app.system_status.to_lowercase()
    );
    let system_status = Paragraph::new(status_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" System Status ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(status_color)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(system_status, chunks[3]);

    // Add execution engine success rate gauge
    let success_percentage = app.success_rate as u16;
    let success_color = if success_percentage >= 90 {
        Color::Green
    } else if success_percentage >= 70 {
        Color::Yellow
    } else {
        Color::Red
    };

    let success_rate = Gauge::default()
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📊 Success Rate ")
                .title_alignment(Alignment::Center),
        )
        .gauge_style(
            Style::default()
                .fg(success_color)
                .add_modifier(Modifier::BOLD),
        )
        .percent(success_percentage)
        .label(format!("{:.1}%", app.success_rate));
    f.render_widget(success_rate, chunks[4]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_analysis (lines 894-1014) ---[0m
fn draw_delegation_analysis(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Recent analysis results
    let analysis_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(10)
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "Frontend" => "🎨",
                "Backend" => "⚙️",
                "DevOps" => "🔧",
                "QA" => "🧪",
                _ => "🤖",
            };

            let confidence_color = if decision.confidence >= 0.8 {
                Color::Green
            } else if decision.confidence >= 0.6 {
                Color::Yellow
            } else {
                Color::Red
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::White)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("({:.0}%) ", decision.confidence * 100.0),
                    Style::default().fg(confidence_color),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::Gray),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut analysis_state = ListState::default();
    analysis_state.select(Some(app.selected_delegation));

    let analysis_list = List::new(analysis_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🔍 Analysis Results ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Blue)
                .add_modifier(Modifier::BOLD),
        )
        .highlight_symbol("► ");

    f.render_stateful_widget(analysis_list, chunks[0], &mut analysis_state);

    // Analysis details
    if let Some(decision) = app.delegation_decisions.get(app.selected_delegation) {
        let details = format!(
            "📋 Task Analysis Details\n\n\
             Task: {}\n\n\
             🎯 Recommended Agent: {}\n\
             📊 Confidence: {:.1}%\n\
             🧠 Reasoning: {}\n\
             📅 Analyzed: {}\n\n\
             💡 Press Enter to delegate this task",
            decision.task_description,
            decision.recommended_agent,
            decision.confidence * 100.0,
            decision.reasoning,
            decision.created_at.format("%H:%M:%S")
        );

        let details_paragraph = Paragraph::new(details)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Analysis Details ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::White))
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    } else {
        let empty_text = "🔍 Task Analysis\n\n\
                         No analysis results yet.\n\n\
                         Press Enter to analyze a new task\n\
                         and get Master's recommendation\n\
                         for optimal agent assignment.";

        let empty_paragraph = Paragraph::new(empty_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Ready for Analysis ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::Gray))
            .alignment(Alignment::Center)
            .wrap(Wrap { trim: true });

        f.render_widget(empty_paragraph, chunks[1]);
    }
}

  ./crates/ccswarm/src/tui/ui.rs:230-295 function draw_enhanced_provider_stats <-> ./crates/ccswarm/src/tui/ui.rs:894-1014 function draw_delegation_analysis
  Similarity: 95.72%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_provider_stats (lines 230-295) ---[0m
fn draw_enhanced_provider_stats(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Execution Statistics
    let exec_stats = [
        format!("📊 Total Executed: {}", app.tasks_executed),
        format!("✅ Completed: {}", app.completed_tasks),
        format!("❌ Failed: {}", app.tasks_failed),
        format!("🎯 Orchestration: {:.1}%", app.orchestration_usage),
    ];

    let exec_paragraph = Paragraph::new(exec_stats.join("\n"))
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🚀 Execution Statistics ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(exec_paragraph, chunks[0]);

    // Performance Metrics
    let perf_stats = [
        format!("💯 Success Rate: {:.1}%", app.success_rate),
        format!("⏳ Pending: {}", app.pending_tasks),
        format!(
            "🤖 Active Agents: {}/{}",
            app.active_agents, app.total_agents
        ),
        format!(
            "📈 Engine Status: {}",
            if app.tasks_executed > 0 {
                "Active"
            } else {
                "Idle"
            }
        ),
    ];

    let perf_color = if app.success_rate >= 90.0 {
        Color::Green
    } else if app.success_rate >= 70.0 {
        Color::Yellow
    } else {
        Color::Red
    };

    let perf_paragraph = Paragraph::new(perf_stats.join("\n"))
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📈 Performance Metrics ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(perf_color))
        .wrap(Wrap { trim: true });

    f.render_widget(perf_paragraph, chunks[1]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_analysis (lines 894-1014) ---[0m
fn draw_delegation_analysis(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Recent analysis results
    let analysis_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(10)
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "Frontend" => "🎨",
                "Backend" => "⚙️",
                "DevOps" => "🔧",
                "QA" => "🧪",
                _ => "🤖",
            };

            let confidence_color = if decision.confidence >= 0.8 {
                Color::Green
            } else if decision.confidence >= 0.6 {
                Color::Yellow
            } else {
                Color::Red
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::White)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("({:.0}%) ", decision.confidence * 100.0),
                    Style::default().fg(confidence_color),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::Gray),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut analysis_state = ListState::default();
    analysis_state.select(Some(app.selected_delegation));

    let analysis_list = List::new(analysis_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🔍 Analysis Results ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Blue)
                .add_modifier(Modifier::BOLD),
        )
        .highlight_symbol("► ");

    f.render_stateful_widget(analysis_list, chunks[0], &mut analysis_state);

    // Analysis details
    if let Some(decision) = app.delegation_decisions.get(app.selected_delegation) {
        let details = format!(
            "📋 Task Analysis Details\n\n\
             Task: {}\n\n\
             🎯 Recommended Agent: {}\n\
             📊 Confidence: {:.1}%\n\
             🧠 Reasoning: {}\n\
             📅 Analyzed: {}\n\n\
             💡 Press Enter to delegate this task",
            decision.task_description,
            decision.recommended_agent,
            decision.confidence * 100.0,
            decision.reasoning,
            decision.created_at.format("%H:%M:%S")
        );

        let details_paragraph = Paragraph::new(details)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Analysis Details ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::White))
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    } else {
        let empty_text = "🔍 Task Analysis\n\n\
                         No analysis results yet.\n\n\
                         Press Enter to analyze a new task\n\
                         and get Master's recommendation\n\
                         for optimal agent assignment.";

        let empty_paragraph = Paragraph::new(empty_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Ready for Analysis ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::Gray))
            .alignment(Alignment::Center)
            .wrap(Wrap { trim: true });

        f.render_widget(empty_paragraph, chunks[1]);
    }
}

  ./crates/ccswarm/src/tui/ui.rs:230-295 function draw_enhanced_provider_stats <-> ./crates/ccswarm/src/tui/ui.rs:588-706 function draw_tasks
  Similarity: 95.68%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_provider_stats (lines 230-295) ---[0m
fn draw_enhanced_provider_stats(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Execution Statistics
    let exec_stats = [
        format!("📊 Total Executed: {}", app.tasks_executed),
        format!("✅ Completed: {}", app.completed_tasks),
        format!("❌ Failed: {}", app.tasks_failed),
        format!("🎯 Orchestration: {:.1}%", app.orchestration_usage),
    ];

    let exec_paragraph = Paragraph::new(exec_stats.join("\n"))
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🚀 Execution Statistics ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(exec_paragraph, chunks[0]);

    // Performance Metrics
    let perf_stats = [
        format!("💯 Success Rate: {:.1}%", app.success_rate),
        format!("⏳ Pending: {}", app.pending_tasks),
        format!(
            "🤖 Active Agents: {}/{}",
            app.active_agents, app.total_agents
        ),
        format!(
            "📈 Engine Status: {}",
            if app.tasks_executed > 0 {
                "Active"
            } else {
                "Idle"
            }
        ),
    ];

    let perf_color = if app.success_rate >= 90.0 {
        Color::Green
    } else if app.success_rate >= 70.0 {
        Color::Yellow
    } else {
        Color::Red
    };

    let perf_paragraph = Paragraph::new(perf_stats.join("\n"))
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📈 Performance Metrics ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(perf_color))
        .wrap(Wrap { trim: true });

    f.render_widget(perf_paragraph, chunks[1]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_tasks (lines 588-706) ---[0m
fn draw_tasks(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Percentage(70), Constraint::Percentage(30)])
        .split(area);

    // Task list with enhanced headers
    let header = Row::new(vec![
        "#",
        "Description",
        "Priority",
        "Type",
        "Status",
        "Agent",
        "Created",
    ])
    .style(Style::default().fg(Color::Yellow))
    .height(1)
    .bottom_margin(1);

    let rows: Vec<Row> = app
        .tasks
        .iter()
        .enumerate()
        .map(|(i, task)| {
            let priority_color = match task.priority.as_str() {
                "High" => Color::Red,
                "Medium" => Color::Yellow,
                "Low" => Color::Green,
                _ => Color::White,
            };

            let status_color = if task.status.contains("✅") {
                Color::Green
            } else if task.status.contains("❌") {
                Color::Red
            } else if task.status.contains("🏃") {
                Color::Cyan
            } else if task.status.contains("📋") {
                Color::Yellow
            } else {
                Color::White
            };

            Row::new(vec![
                Cell::from(format!("{}", i + 1)),
                Cell::from(task.description.clone()),
                Cell::from(Span::styled(
                    task.priority.clone(),
                    Style::default().fg(priority_color),
                )),
                Cell::from(task.task_type.clone()),
                Cell::from(Span::styled(
                    task.status.clone(),
                    Style::default().fg(status_color),
                )),
                Cell::from(task.assigned_agent.as_deref().unwrap_or("-")),
                Cell::from(task.created_at.format("%H:%M:%S").to_string()),
            ])
        })
        .collect();

    let selected_style = Style::default()
        .bg(Color::Yellow)
        .fg(Color::Black)
        .add_modifier(Modifier::BOLD);

    let table = Table::new(
        rows,
        &[
            Constraint::Length(3),  // #
            Constraint::Min(25),    // Description
            Constraint::Length(8),  // Priority
            Constraint::Length(12), // Type
            Constraint::Length(15), // Status
            Constraint::Length(12), // Agent
            Constraint::Length(8),  // Created
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .title(" Tasks (t to add new task) "),
    )
    .row_highlight_style(selected_style);

    let mut table_state = ratatui::widgets::TableState::default();
    table_state.select(Some(app.selected_task));

    f.render_stateful_widget(table, chunks[0], &mut table_state);

    // Task details
    if let Some(task) = app.tasks.get(app.selected_task) {
        let details = [
            format!("ID: {}", task.id),
            format!("Description: {}", task.description),
            format!("Priority: {}", task.priority),
            format!("Type: {}", task.task_type),
            format!("Status: {}", task.status),
            format!("Created: {}", task.created_at.format("%Y-%m-%d %H:%M:%S")),
            format!(
                "Assigned Agent: {}",
                task.assigned_agent.as_deref().unwrap_or("None")
            ),
        ];

        let details_text = details.join("\n");
        let details_paragraph = Paragraph::new(details_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .title(" Task Details "),
            )
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    }
}

  ./crates/ccswarm/src/tui/ui.rs:894-1014 function draw_delegation_analysis <-> ./crates/ccswarm/src/tui/ui.rs:1346-1417 function draw_footer
  Similarity: 91.65%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_analysis (lines 894-1014) ---[0m
fn draw_delegation_analysis(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Recent analysis results
    let analysis_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(10)
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "Frontend" => "🎨",
                "Backend" => "⚙️",
                "DevOps" => "🔧",
                "QA" => "🧪",
                _ => "🤖",
            };

            let confidence_color = if decision.confidence >= 0.8 {
                Color::Green
            } else if decision.confidence >= 0.6 {
                Color::Yellow
            } else {
                Color::Red
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::White)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("({:.0}%) ", decision.confidence * 100.0),
                    Style::default().fg(confidence_color),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::Gray),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut analysis_state = ListState::default();
    analysis_state.select(Some(app.selected_delegation));

    let analysis_list = List::new(analysis_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🔍 Analysis Results ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Blue)
                .add_modifier(Modifier::BOLD),
        )
        .highlight_symbol("► ");

    f.render_stateful_widget(analysis_list, chunks[0], &mut analysis_state);

    // Analysis details
    if let Some(decision) = app.delegation_decisions.get(app.selected_delegation) {
        let details = format!(
            "📋 Task Analysis Details\n\n\
             Task: {}\n\n\
             🎯 Recommended Agent: {}\n\
             📊 Confidence: {:.1}%\n\
             🧠 Reasoning: {}\n\
             📅 Analyzed: {}\n\n\
             💡 Press Enter to delegate this task",
            decision.task_description,
            decision.recommended_agent,
            decision.confidence * 100.0,
            decision.reasoning,
            decision.created_at.format("%H:%M:%S")
        );

        let details_paragraph = Paragraph::new(details)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Analysis Details ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::White))
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    } else {
        let empty_text = "🔍 Task Analysis\n\n\
                         No analysis results yet.\n\n\
                         Press Enter to analyze a new task\n\
                         and get Master's recommendation\n\
                         for optimal agent assignment.";

        let empty_paragraph = Paragraph::new(empty_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Ready for Analysis ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::Gray))
            .alignment(Alignment::Center)
            .wrap(Wrap { trim: true });

        f.render_widget(empty_paragraph, chunks[1]);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_footer (lines 1346-1417) ---[0m
fn draw_footer(f: &mut Frame, area: Rect, app: &App) {
    let key_bindings = match app.current_tab {
        Tab::Overview => vec![
            ("Tab/Shift+Tab", "Switch tabs", Color::Cyan),
            ("c", "Command prompt", Color::Yellow),
            ("r", "Refresh data", Color::Green),
            ("q", "Quit", Color::Red),
        ],
        Tab::Agents => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("Enter", "Start/Details", Color::Green),
            ("n", "New Agent", Color::Blue),
            ("d", "Delete", Color::Red),
            ("c", "Command", Color::Yellow),
        ],
        Tab::Tasks => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("t", "Add Task", Color::Green),
            ("c", "Command", Color::Yellow),
            ("r", "Refresh", Color::Blue),
        ],
        Tab::Logs => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("r", "Refresh", Color::Green),
            ("c", "Command", Color::Yellow),
        ],
        Tab::Delegation => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("Space", "Switch mode", Color::Blue),
            ("Enter", "Analyze/Delegate", Color::Green),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("c", "Command", Color::Yellow),
        ],
    };

    let help_text: Vec<Line> = vec![Line::from(
        key_bindings
            .iter()
            .flat_map(|(key, desc, color)| {
                vec![
                    Span::styled(
                        format!(" {} ", key),
                        Style::default()
                            .fg(*color)
                            .bg(Color::DarkGray)
                            .add_modifier(Modifier::BOLD),
                    ),
                    Span::styled(format!(" {} ", desc), Style::default().fg(Color::Gray)),
                    Span::raw(" │ "),
                ]
                .into_iter()
            })
            .collect::<Vec<_>>(),
    )];

    let footer = Paragraph::new(help_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" ⌨️  Keyboard Shortcuts ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .alignment(Alignment::Center)
        .wrap(Wrap { trim: true });

    f.render_widget(footer, area);
}

  ./crates/ccswarm/src/tui/ui.rs:298-429 function draw_enhanced_agent_summary <-> ./crates/ccswarm/src/tui/ui.rs:1346-1417 function draw_footer
  Similarity: 84.62%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_agent_summary (lines 298-429) ---[0m
fn draw_enhanced_agent_summary(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(60), Constraint::Percentage(40)])
        .split(area);

    // Enhanced agents table
    let header = Row::new(vec![
        Cell::from("Agent").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Type").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Status").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
        Cell::from("Tasks").style(
            Style::default()
                .fg(Color::White)
                .add_modifier(Modifier::BOLD),
        ),
    ]);

    let rows: Vec<Row> = app
        .agents
        .iter()
        .take(6)
        .map(|agent| {
            let (status_color, status_text, status_icon) = match agent.status {
                crate::agent::AgentStatus::Available => (Color::Green, "Available", "✅"),
                crate::agent::AgentStatus::Working => (Color::Cyan, "Working", "⚙️"),
                crate::agent::AgentStatus::Error(_) => (Color::Red, "Error", "❌"),
                crate::agent::AgentStatus::Initializing => (Color::Yellow, "Init", "🔄"),
                crate::agent::AgentStatus::WaitingForReview => (Color::Magenta, "Review", "⏳"),
                crate::agent::AgentStatus::ShuttingDown => (Color::DarkGray, "Shutdown", "⏹️"),
            };

            let is_master = agent.specialization.contains("Master");
            let (name_icon, name_color) = if is_master {
                ("👑", Color::Yellow)
            } else {
                match agent.specialization.as_str() {
                    s if s.contains("Frontend") => ("🎨", Color::Cyan),
                    s if s.contains("Backend") => ("⚙️", Color::Green),
                    s if s.contains("DevOps") => ("🔧", Color::Blue),
                    s if s.contains("QA") => ("🧪", Color::Magenta),
                    _ => ("🤖", Color::White),
                }
            };

            Row::new(vec![
                Cell::from(format!("{} {}", name_icon, agent.name)).style(
                    Style::default().fg(name_color).add_modifier(if is_master {
                        Modifier::BOLD
                    } else {
                        Modifier::empty()
                    }),
                ),
                Cell::from(agent.specialization.clone()).style(Style::default().fg(Color::White)),
                Cell::from(format!("{} {}", status_icon, status_text))
                    .style(Style::default().fg(status_color)),
                Cell::from(format!("{}", agent.tasks_completed))
                    .style(Style::default().fg(Color::Yellow)),
            ])
        })
        .collect();

    let agents_table = Table::new(
        rows,
        [
            Constraint::Length(15), // Agent
            Constraint::Length(12), // Type
            Constraint::Length(12), // Status
            Constraint::Length(6),  // Tasks
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 👥 Agents Overview ")
            .title_alignment(Alignment::Center),
    )
    .row_highlight_style(
        Style::default()
            .bg(Color::DarkGray)
            .add_modifier(Modifier::BOLD),
    );

    f.render_widget(agents_table, chunks[0]);

    // System activity summary
    let activity_text = format!(
        "🔄 System Activity\n\n\
         ⏱️  Uptime: Active\n\
         📊 Total Agents: {}\n\
         🎯 Active: {}\n\
         📋 Tasks Queued: {}\n\
         ✅ Completed: {}\n\n\
         📈 Efficiency: {:.1}%",
        app.total_agents,
        app.active_agents,
        app.pending_tasks,
        app.completed_tasks,
        if app.total_agents > 0 {
            (app.active_agents as f32 / app.total_agents as f32) * 100.0
        } else {
            0.0
        }
    );

    let activity_paragraph = Paragraph::new(activity_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📊 System Metrics ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::Cyan))
        .wrap(Wrap { trim: true });

    f.render_widget(activity_paragraph, chunks[1]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_footer (lines 1346-1417) ---[0m
fn draw_footer(f: &mut Frame, area: Rect, app: &App) {
    let key_bindings = match app.current_tab {
        Tab::Overview => vec![
            ("Tab/Shift+Tab", "Switch tabs", Color::Cyan),
            ("c", "Command prompt", Color::Yellow),
            ("r", "Refresh data", Color::Green),
            ("q", "Quit", Color::Red),
        ],
        Tab::Agents => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("Enter", "Start/Details", Color::Green),
            ("n", "New Agent", Color::Blue),
            ("d", "Delete", Color::Red),
            ("c", "Command", Color::Yellow),
        ],
        Tab::Tasks => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("t", "Add Task", Color::Green),
            ("c", "Command", Color::Yellow),
            ("r", "Refresh", Color::Blue),
        ],
        Tab::Logs => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("r", "Refresh", Color::Green),
            ("c", "Command", Color::Yellow),
        ],
        Tab::Delegation => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("Space", "Switch mode", Color::Blue),
            ("Enter", "Analyze/Delegate", Color::Green),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("c", "Command", Color::Yellow),
        ],
    };

    let help_text: Vec<Line> = vec![Line::from(
        key_bindings
            .iter()
            .flat_map(|(key, desc, color)| {
                vec![
                    Span::styled(
                        format!(" {} ", key),
                        Style::default()
                            .fg(*color)
                            .bg(Color::DarkGray)
                            .add_modifier(Modifier::BOLD),
                    ),
                    Span::styled(format!(" {} ", desc), Style::default().fg(Color::Gray)),
                    Span::raw(" │ "),
                ]
                .into_iter()
            })
            .collect::<Vec<_>>(),
    )];

    let footer = Paragraph::new(help_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" ⌨️  Keyboard Shortcuts ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .alignment(Alignment::Center)
        .wrap(Wrap { trim: true });

    f.render_widget(footer, area);
}

  ./crates/ccswarm/src/tui/ui.rs:1017-1157 function draw_delegation_interface <-> ./crates/ccswarm/src/tui/ui.rs:1346-1417 function draw_footer
  Similarity: 76.40%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_interface (lines 1017-1157) ---[0m
fn draw_delegation_interface(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(40), Constraint::Percentage(60)])
        .split(area);

    // Available agents
    let agent_items: Vec<ListItem> = vec![
        ListItem::new(Line::from(vec![
            Span::styled("🎨 ", Style::default().fg(Color::Cyan)),
            Span::styled(
                "Frontend",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - UI/UX Development", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("⚙️ ", Style::default().fg(Color::Green)),
            Span::styled(
                "Backend",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - API/Database Work", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("🔧 ", Style::default().fg(Color::Blue)),
            Span::styled(
                "DevOps",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - Infrastructure/Deploy", Style::default().fg(Color::Gray)),
        ])),
        ListItem::new(Line::from(vec![
            Span::styled("🧪 ", Style::default().fg(Color::Magenta)),
            Span::styled(
                "QA",
                Style::default()
                    .fg(Color::White)
                    .add_modifier(Modifier::BOLD),
            ),
            Span::styled(" - Testing/Quality", Style::default().fg(Color::Gray)),
        ])),
    ];

    let agents_list = List::new(agent_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🤖 Available Agents ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Green)
                .add_modifier(Modifier::BOLD),
        );

    f.render_widget(agents_list, chunks[0]);

    // Delegation form and recent delegations
    let right_chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(8), Constraint::Min(0)])
        .split(chunks[1]);

    // Delegation form
    let form_text = "🎯 Manual Task Delegation\n\n\
                     Format: <agent> <task_description>\n\n\
                     Examples:\n\
                     • frontend Create user dashboard\n\
                     • backend Add payment processing\n\
                     • qa Write end-to-end tests\n\
                     • devops Setup CI/CD pipeline";

    let form_paragraph = Paragraph::new(form_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📝 Delegation Form ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(form_paragraph, right_chunks[0]);

    // Recent delegations
    let delegation_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(6)
        .filter(|d| d.reasoning.contains("Manual delegation"))
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "frontend" | "Frontend" => "🎨",
                "backend" | "Backend" => "⚙️",
                "devops" | "DevOps" => "🔧",
                "qa" | "QA" => "🧪",
                _ => "🤖",
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::Green)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("{} ", decision.created_at.format("%H:%M")),
                    Style::default().fg(Color::DarkGray),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::White),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let delegations_list = List::new(delegation_items).block(
        Block::default()
            .borders(Borders::ALL)
            .border_type(BorderType::Rounded)
            .title(" 📋 Recent Delegations ")
            .title_alignment(Alignment::Center),
    );

    f.render_widget(delegations_list, right_chunks[1]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_footer (lines 1346-1417) ---[0m
fn draw_footer(f: &mut Frame, area: Rect, app: &App) {
    let key_bindings = match app.current_tab {
        Tab::Overview => vec![
            ("Tab/Shift+Tab", "Switch tabs", Color::Cyan),
            ("c", "Command prompt", Color::Yellow),
            ("r", "Refresh data", Color::Green),
            ("q", "Quit", Color::Red),
        ],
        Tab::Agents => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("Enter", "Start/Details", Color::Green),
            ("n", "New Agent", Color::Blue),
            ("d", "Delete", Color::Red),
            ("c", "Command", Color::Yellow),
        ],
        Tab::Tasks => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("t", "Add Task", Color::Green),
            ("c", "Command", Color::Yellow),
            ("r", "Refresh", Color::Blue),
        ],
        Tab::Logs => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("r", "Refresh", Color::Green),
            ("c", "Command", Color::Yellow),
        ],
        Tab::Delegation => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("Space", "Switch mode", Color::Blue),
            ("Enter", "Analyze/Delegate", Color::Green),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("c", "Command", Color::Yellow),
        ],
    };

    let help_text: Vec<Line> = vec![Line::from(
        key_bindings
            .iter()
            .flat_map(|(key, desc, color)| {
                vec![
                    Span::styled(
                        format!(" {} ", key),
                        Style::default()
                            .fg(*color)
                            .bg(Color::DarkGray)
                            .add_modifier(Modifier::BOLD),
                    ),
                    Span::styled(format!(" {} ", desc), Style::default().fg(Color::Gray)),
                    Span::raw(" │ "),
                ]
                .into_iter()
            })
            .collect::<Vec<_>>(),
    )];

    let footer = Paragraph::new(help_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" ⌨️  Keyboard Shortcuts ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .alignment(Alignment::Center)
        .wrap(Wrap { trim: true });

    f.render_widget(footer, area);
}

  ./crates/ccswarm/src/tui/ui.rs:230-295 function draw_enhanced_provider_stats <-> ./crates/ccswarm/src/tui/ui.rs:709-810 function draw_logs
  Similarity: 96.68%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_provider_stats (lines 230-295) ---[0m
fn draw_enhanced_provider_stats(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Execution Statistics
    let exec_stats = [
        format!("📊 Total Executed: {}", app.tasks_executed),
        format!("✅ Completed: {}", app.completed_tasks),
        format!("❌ Failed: {}", app.tasks_failed),
        format!("🎯 Orchestration: {:.1}%", app.orchestration_usage),
    ];

    let exec_paragraph = Paragraph::new(exec_stats.join("\n"))
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🚀 Execution Statistics ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(exec_paragraph, chunks[0]);

    // Performance Metrics
    let perf_stats = [
        format!("💯 Success Rate: {:.1}%", app.success_rate),
        format!("⏳ Pending: {}", app.pending_tasks),
        format!(
            "🤖 Active Agents: {}/{}",
            app.active_agents, app.total_agents
        ),
        format!(
            "📈 Engine Status: {}",
            if app.tasks_executed > 0 {
                "Active"
            } else {
                "Idle"
            }
        ),
    ];

    let perf_color = if app.success_rate >= 90.0 {
        Color::Green
    } else if app.success_rate >= 70.0 {
        Color::Yellow
    } else {
        Color::Red
    };

    let perf_paragraph = Paragraph::new(perf_stats.join("\n"))
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📈 Performance Metrics ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(perf_color))
        .wrap(Wrap { trim: true });

    f.render_widget(perf_paragraph, chunks[1]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_logs (lines 709-810) ---[0m
fn draw_logs(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(3), Constraint::Min(0)])
        .split(area);

    // Log level summary bar
    let log_counts = app
        .logs
        .iter()
        .fold(std::collections::HashMap::new(), |mut acc, log| {
            *acc.entry(log.level.as_str()).or_insert(0) += 1;
            acc
        });

    let summary_text = format!(
        "📊 Logs: 🔴 {} errors | 🟡 {} warnings | 🟢 {} info | 🔵 {} debug | Total: {}",
        log_counts.get("ERROR").unwrap_or(&0),
        log_counts.get("WARN").unwrap_or(&0),
        log_counts.get("INFO").unwrap_or(&0),
        log_counts.get("DEBUG").unwrap_or(&0),
        app.logs.len()
    );

    let summary_paragraph = Paragraph::new(summary_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📋 Log Summary ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .alignment(Alignment::Center);

    f.render_widget(summary_paragraph, chunks[0]);

    // Enhanced logs with better visual hierarchy
    let items: Vec<ListItem> = app
        .logs
        .iter()
        .rev() // Show newest logs first
        .take(100) // Limit to prevent performance issues
        .map(|log| {
            let (level_color, level_icon) = match log.level.as_str() {
                "ERROR" => (Color::Red, "🔴"),
                "WARN" => (Color::Yellow, "🟡"),
                "INFO" => (Color::Green, "🟢"),
                "DEBUG" => (Color::Cyan, "🔵"),
                _ => (Color::White, "⚪"),
            };

            let agent_name = log.agent.as_deref().unwrap_or("System");
            let (agent_icon, agent_color) = if agent_name == "System" {
                ("🖥️", Color::Gray)
            } else if agent_name.contains("master") || agent_name.contains("Master") {
                ("👑", Color::Yellow)
            } else {
                ("🤖", Color::Cyan)
            };

            let content = vec![Line::from(vec![
                Span::styled(
                    format!("{} ", log.timestamp.format("%H:%M:%S")),
                    Style::default().fg(Color::DarkGray),
                ),
                Span::styled(format!("{} ", level_icon), Style::default().fg(level_color)),
                Span::styled(
                    format!("{:<5} ", log.level),
                    Style::default()
                        .fg(level_color)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("{} {:<10} ", agent_icon, agent_name),
                    Style::default().fg(agent_color),
                ),
                Span::styled(log.message.clone(), Style::default().fg(Color::White)),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut logs_state = ListState::default();
    logs_state.select(Some(app.selected_log));

    let logs_list = List::new(items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📜 Live Logs (r: refresh, ↑↓: navigate) ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::DarkGray)
                .add_modifier(Modifier::BOLD),
        );

    f.render_stateful_widget(logs_list, chunks[1], &mut logs_state);
}

  ./crates/ccswarm/src/tui/ui.rs:588-706 function draw_tasks <-> ./crates/ccswarm/src/tui/ui.rs:1346-1417 function draw_footer
  Similarity: 82.17%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_tasks (lines 588-706) ---[0m
fn draw_tasks(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Percentage(70), Constraint::Percentage(30)])
        .split(area);

    // Task list with enhanced headers
    let header = Row::new(vec![
        "#",
        "Description",
        "Priority",
        "Type",
        "Status",
        "Agent",
        "Created",
    ])
    .style(Style::default().fg(Color::Yellow))
    .height(1)
    .bottom_margin(1);

    let rows: Vec<Row> = app
        .tasks
        .iter()
        .enumerate()
        .map(|(i, task)| {
            let priority_color = match task.priority.as_str() {
                "High" => Color::Red,
                "Medium" => Color::Yellow,
                "Low" => Color::Green,
                _ => Color::White,
            };

            let status_color = if task.status.contains("✅") {
                Color::Green
            } else if task.status.contains("❌") {
                Color::Red
            } else if task.status.contains("🏃") {
                Color::Cyan
            } else if task.status.contains("📋") {
                Color::Yellow
            } else {
                Color::White
            };

            Row::new(vec![
                Cell::from(format!("{}", i + 1)),
                Cell::from(task.description.clone()),
                Cell::from(Span::styled(
                    task.priority.clone(),
                    Style::default().fg(priority_color),
                )),
                Cell::from(task.task_type.clone()),
                Cell::from(Span::styled(
                    task.status.clone(),
                    Style::default().fg(status_color),
                )),
                Cell::from(task.assigned_agent.as_deref().unwrap_or("-")),
                Cell::from(task.created_at.format("%H:%M:%S").to_string()),
            ])
        })
        .collect();

    let selected_style = Style::default()
        .bg(Color::Yellow)
        .fg(Color::Black)
        .add_modifier(Modifier::BOLD);

    let table = Table::new(
        rows,
        &[
            Constraint::Length(3),  // #
            Constraint::Min(25),    // Description
            Constraint::Length(8),  // Priority
            Constraint::Length(12), // Type
            Constraint::Length(15), // Status
            Constraint::Length(12), // Agent
            Constraint::Length(8),  // Created
        ],
    )
    .header(header)
    .block(
        Block::default()
            .borders(Borders::ALL)
            .title(" Tasks (t to add new task) "),
    )
    .row_highlight_style(selected_style);

    let mut table_state = ratatui::widgets::TableState::default();
    table_state.select(Some(app.selected_task));

    f.render_stateful_widget(table, chunks[0], &mut table_state);

    // Task details
    if let Some(task) = app.tasks.get(app.selected_task) {
        let details = [
            format!("ID: {}", task.id),
            format!("Description: {}", task.description),
            format!("Priority: {}", task.priority),
            format!("Type: {}", task.task_type),
            format!("Status: {}", task.status),
            format!("Created: {}", task.created_at.format("%Y-%m-%d %H:%M:%S")),
            format!(
                "Assigned Agent: {}",
                task.assigned_agent.as_deref().unwrap_or("None")
            ),
        ];

        let details_text = details.join("\n");
        let details_paragraph = Paragraph::new(details_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .title(" Task Details "),
            )
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_footer (lines 1346-1417) ---[0m
fn draw_footer(f: &mut Frame, area: Rect, app: &App) {
    let key_bindings = match app.current_tab {
        Tab::Overview => vec![
            ("Tab/Shift+Tab", "Switch tabs", Color::Cyan),
            ("c", "Command prompt", Color::Yellow),
            ("r", "Refresh data", Color::Green),
            ("q", "Quit", Color::Red),
        ],
        Tab::Agents => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("Enter", "Start/Details", Color::Green),
            ("n", "New Agent", Color::Blue),
            ("d", "Delete", Color::Red),
            ("c", "Command", Color::Yellow),
        ],
        Tab::Tasks => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("t", "Add Task", Color::Green),
            ("c", "Command", Color::Yellow),
            ("r", "Refresh", Color::Blue),
        ],
        Tab::Logs => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("r", "Refresh", Color::Green),
            ("c", "Command", Color::Yellow),
        ],
        Tab::Delegation => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("Space", "Switch mode", Color::Blue),
            ("Enter", "Analyze/Delegate", Color::Green),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("c", "Command", Color::Yellow),
        ],
    };

    let help_text: Vec<Line> = vec![Line::from(
        key_bindings
            .iter()
            .flat_map(|(key, desc, color)| {
                vec![
                    Span::styled(
                        format!(" {} ", key),
                        Style::default()
                            .fg(*color)
                            .bg(Color::DarkGray)
                            .add_modifier(Modifier::BOLD),
                    ),
                    Span::styled(format!(" {} ", desc), Style::default().fg(Color::Gray)),
                    Span::raw(" │ "),
                ]
                .into_iter()
            })
            .collect::<Vec<_>>(),
    )];

    let footer = Paragraph::new(help_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" ⌨️  Keyboard Shortcuts ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .alignment(Alignment::Center)
        .wrap(Wrap { trim: true });

    f.render_widget(footer, area);
}

  ./crates/ccswarm/src/tui/ui.rs:104-227 function draw_enhanced_system_stats <-> ./crates/ccswarm/src/tui/ui.rs:230-295 function draw_enhanced_provider_stats
  Similarity: 82.43%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_system_stats (lines 104-227) ---[0m
fn draw_enhanced_system_stats(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
            Constraint::Percentage(20),
        ])
        .split(area);

    // Enhanced total agents with progress bar
    let agent_percentage = (app.active_agents * 100 / app.total_agents.max(1)) as u16;
    let total_agents = Gauge::default()
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🤖 Active Agents ")
                .title_alignment(Alignment::Center),
        )
        .gauge_style(
            Style::default()
                .fg(Color::Cyan)
                .add_modifier(Modifier::BOLD),
        )
        .percent(agent_percentage)
        .label(format!(
            "{}/{} ({}%)",
            app.active_agents, app.total_agents, agent_percentage
        ));
    f.render_widget(total_agents, chunks[0]);

    // Enhanced pending tasks with icon
    let pending_text = format!("📋 {}\nqueued", app.pending_tasks);
    let pending_tasks = Paragraph::new(pending_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Pending Tasks ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(pending_tasks, chunks[1]);

    // Enhanced completed tasks with icon
    let completed_text = format!("✅ {}\ncompleted", app.completed_tasks);
    let completed_tasks = Paragraph::new(completed_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" Completed Tasks ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(Color::Green)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(completed_tasks, chunks[2]);

    // Enhanced system status with icon and color
    let (status_icon, status_color) = match app.system_status.as_str() {
        "Running" => ("🟢", Color::Green),
        "Stopped" => ("🔴", Color::Red),
        _ => ("🟡", Color::Yellow),
    };
    let status_text = format!(
        "{} {}\nsystem",
        status_icon,
        app.system_status.to_lowercase()
    );
    let system_status = Paragraph::new(status_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" System Status ")
                .title_alignment(Alignment::Center),
        )
        .style(
            Style::default()
                .fg(status_color)
                .add_modifier(Modifier::BOLD),
        )
        .alignment(Alignment::Center);
    f.render_widget(system_status, chunks[3]);

    // Add execution engine success rate gauge
    let success_percentage = app.success_rate as u16;
    let success_color = if success_percentage >= 90 {
        Color::Green
    } else if success_percentage >= 70 {
        Color::Yellow
    } else {
        Color::Red
    };

    let success_rate = Gauge::default()
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📊 Success Rate ")
                .title_alignment(Alignment::Center),
        )
        .gauge_style(
            Style::default()
                .fg(success_color)
                .add_modifier(Modifier::BOLD),
        )
        .percent(success_percentage)
        .label(format!("{:.1}%", app.success_rate));
    f.render_widget(success_rate, chunks[4]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_provider_stats (lines 230-295) ---[0m
fn draw_enhanced_provider_stats(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Execution Statistics
    let exec_stats = [
        format!("📊 Total Executed: {}", app.tasks_executed),
        format!("✅ Completed: {}", app.completed_tasks),
        format!("❌ Failed: {}", app.tasks_failed),
        format!("🎯 Orchestration: {:.1}%", app.orchestration_usage),
    ];

    let exec_paragraph = Paragraph::new(exec_stats.join("\n"))
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🚀 Execution Statistics ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(exec_paragraph, chunks[0]);

    // Performance Metrics
    let perf_stats = [
        format!("💯 Success Rate: {:.1}%", app.success_rate),
        format!("⏳ Pending: {}", app.pending_tasks),
        format!(
            "🤖 Active Agents: {}/{}",
            app.active_agents, app.total_agents
        ),
        format!(
            "📈 Engine Status: {}",
            if app.tasks_executed > 0 {
                "Active"
            } else {
                "Idle"
            }
        ),
    ];

    let perf_color = if app.success_rate >= 90.0 {
        Color::Green
    } else if app.success_rate >= 70.0 {
        Color::Yellow
    } else {
        Color::Red
    };

    let perf_paragraph = Paragraph::new(perf_stats.join("\n"))
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📈 Performance Metrics ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(perf_color))
        .wrap(Wrap { trim: true });

    f.render_widget(perf_paragraph, chunks[1]);
}

  ./crates/ccswarm/src/tui/ui.rs:709-810 function draw_logs <-> ./crates/ccswarm/src/tui/ui.rs:1346-1417 function draw_footer
  Similarity: 77.17%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_logs (lines 709-810) ---[0m
fn draw_logs(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([Constraint::Length(3), Constraint::Min(0)])
        .split(area);

    // Log level summary bar
    let log_counts = app
        .logs
        .iter()
        .fold(std::collections::HashMap::new(), |mut acc, log| {
            *acc.entry(log.level.as_str()).or_insert(0) += 1;
            acc
        });

    let summary_text = format!(
        "📊 Logs: 🔴 {} errors | 🟡 {} warnings | 🟢 {} info | 🔵 {} debug | Total: {}",
        log_counts.get("ERROR").unwrap_or(&0),
        log_counts.get("WARN").unwrap_or(&0),
        log_counts.get("INFO").unwrap_or(&0),
        log_counts.get("DEBUG").unwrap_or(&0),
        app.logs.len()
    );

    let summary_paragraph = Paragraph::new(summary_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📋 Log Summary ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .alignment(Alignment::Center);

    f.render_widget(summary_paragraph, chunks[0]);

    // Enhanced logs with better visual hierarchy
    let items: Vec<ListItem> = app
        .logs
        .iter()
        .rev() // Show newest logs first
        .take(100) // Limit to prevent performance issues
        .map(|log| {
            let (level_color, level_icon) = match log.level.as_str() {
                "ERROR" => (Color::Red, "🔴"),
                "WARN" => (Color::Yellow, "🟡"),
                "INFO" => (Color::Green, "🟢"),
                "DEBUG" => (Color::Cyan, "🔵"),
                _ => (Color::White, "⚪"),
            };

            let agent_name = log.agent.as_deref().unwrap_or("System");
            let (agent_icon, agent_color) = if agent_name == "System" {
                ("🖥️", Color::Gray)
            } else if agent_name.contains("master") || agent_name.contains("Master") {
                ("👑", Color::Yellow)
            } else {
                ("🤖", Color::Cyan)
            };

            let content = vec![Line::from(vec![
                Span::styled(
                    format!("{} ", log.timestamp.format("%H:%M:%S")),
                    Style::default().fg(Color::DarkGray),
                ),
                Span::styled(format!("{} ", level_icon), Style::default().fg(level_color)),
                Span::styled(
                    format!("{:<5} ", log.level),
                    Style::default()
                        .fg(level_color)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("{} {:<10} ", agent_icon, agent_name),
                    Style::default().fg(agent_color),
                ),
                Span::styled(log.message.clone(), Style::default().fg(Color::White)),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut logs_state = ListState::default();
    logs_state.select(Some(app.selected_log));

    let logs_list = List::new(items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📜 Live Logs (r: refresh, ↑↓: navigate) ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::DarkGray)
                .add_modifier(Modifier::BOLD),
        );

    f.render_stateful_widget(logs_list, chunks[1], &mut logs_state);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_footer (lines 1346-1417) ---[0m
fn draw_footer(f: &mut Frame, area: Rect, app: &App) {
    let key_bindings = match app.current_tab {
        Tab::Overview => vec![
            ("Tab/Shift+Tab", "Switch tabs", Color::Cyan),
            ("c", "Command prompt", Color::Yellow),
            ("r", "Refresh data", Color::Green),
            ("q", "Quit", Color::Red),
        ],
        Tab::Agents => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("Enter", "Start/Details", Color::Green),
            ("n", "New Agent", Color::Blue),
            ("d", "Delete", Color::Red),
            ("c", "Command", Color::Yellow),
        ],
        Tab::Tasks => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("t", "Add Task", Color::Green),
            ("c", "Command", Color::Yellow),
            ("r", "Refresh", Color::Blue),
        ],
        Tab::Logs => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("r", "Refresh", Color::Green),
            ("c", "Command", Color::Yellow),
        ],
        Tab::Delegation => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("Space", "Switch mode", Color::Blue),
            ("Enter", "Analyze/Delegate", Color::Green),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("c", "Command", Color::Yellow),
        ],
    };

    let help_text: Vec<Line> = vec![Line::from(
        key_bindings
            .iter()
            .flat_map(|(key, desc, color)| {
                vec![
                    Span::styled(
                        format!(" {} ", key),
                        Style::default()
                            .fg(*color)
                            .bg(Color::DarkGray)
                            .add_modifier(Modifier::BOLD),
                    ),
                    Span::styled(format!(" {} ", desc), Style::default().fg(Color::Gray)),
                    Span::raw(" │ "),
                ]
                .into_iter()
            })
            .collect::<Vec<_>>(),
    )];

    let footer = Paragraph::new(help_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" ⌨️  Keyboard Shortcuts ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .alignment(Alignment::Center)
        .wrap(Wrap { trim: true });

    f.render_widget(footer, area);
}

  ./crates/ccswarm/src/tui/ui.rs:838-891 function draw_delegation_mode_selector <-> ./crates/ccswarm/src/tui/ui.rs:894-1014 function draw_delegation_analysis
  Similarity: 76.04%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_mode_selector (lines 838-891) ---[0m
fn draw_delegation_mode_selector(f: &mut Frame, area: Rect, app: &App) {
    let mode_chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([
            Constraint::Percentage(33),
            Constraint::Percentage(33),
            Constraint::Percentage(34),
        ])
        .split(area);

    let modes = [
        ("🔍 Analyze", DelegationMode::Analyze, Color::Blue),
        ("🎯 Delegate", DelegationMode::Delegate, Color::Green),
        ("📊 Stats", DelegationMode::ViewStats, Color::Magenta),
    ];

    for (i, (title, mode, color)) in modes.iter().enumerate() {
        let is_selected = *mode == app.delegation_mode;

        let block = if is_selected {
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Double)
                .border_style(Style::default().fg(*color).add_modifier(Modifier::BOLD))
                .title(*title)
                .title_alignment(Alignment::Center)
                .style(Style::default().bg(Color::DarkGray))
        } else {
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .border_style(Style::default().fg(Color::Gray))
                .title(*title)
                .title_alignment(Alignment::Center)
        };

        let description = match mode {
            DelegationMode::Analyze => "Analyze tasks and\nget agent recommendations",
            DelegationMode::Delegate => "Manually delegate\ntasks to specific agents",
            DelegationMode::ViewStats => "View delegation\nstatistics and history",
        };

        let paragraph = Paragraph::new(description)
            .block(block)
            .style(Style::default().fg(if is_selected {
                Color::White
            } else {
                Color::Gray
            }))
            .alignment(Alignment::Center);

        f.render_widget(paragraph, mode_chunks[i]);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_analysis (lines 894-1014) ---[0m
fn draw_delegation_analysis(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Recent analysis results
    let analysis_items: Vec<ListItem> = app
        .delegation_decisions
        .iter()
        .rev()
        .take(10)
        .map(|decision| {
            let agent_icon = match decision.recommended_agent.as_str() {
                "Frontend" => "🎨",
                "Backend" => "⚙️",
                "DevOps" => "🔧",
                "QA" => "🧪",
                _ => "🤖",
            };

            let confidence_color = if decision.confidence >= 0.8 {
                Color::Green
            } else if decision.confidence >= 0.6 {
                Color::Yellow
            } else {
                Color::Red
            };

            let content = vec![Line::from(vec![
                Span::styled(format!("{} ", agent_icon), Style::default().fg(Color::Cyan)),
                Span::styled(
                    format!("{:<8} ", decision.recommended_agent),
                    Style::default()
                        .fg(Color::White)
                        .add_modifier(Modifier::BOLD),
                ),
                Span::styled(
                    format!("({:.0}%) ", decision.confidence * 100.0),
                    Style::default().fg(confidence_color),
                ),
                Span::styled(
                    decision.task_description.clone(),
                    Style::default().fg(Color::Gray),
                ),
            ])];
            ListItem::new(content)
        })
        .collect();

    let mut analysis_state = ListState::default();
    analysis_state.select(Some(app.selected_delegation));

    let analysis_list = List::new(analysis_items)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🔍 Analysis Results ")
                .title_alignment(Alignment::Center),
        )
        .highlight_style(
            Style::default()
                .bg(Color::Blue)
                .add_modifier(Modifier::BOLD),
        )
        .highlight_symbol("► ");

    f.render_stateful_widget(analysis_list, chunks[0], &mut analysis_state);

    // Analysis details
    if let Some(decision) = app.delegation_decisions.get(app.selected_delegation) {
        let details = format!(
            "📋 Task Analysis Details\n\n\
             Task: {}\n\n\
             🎯 Recommended Agent: {}\n\
             📊 Confidence: {:.1}%\n\
             🧠 Reasoning: {}\n\
             📅 Analyzed: {}\n\n\
             💡 Press Enter to delegate this task",
            decision.task_description,
            decision.recommended_agent,
            decision.confidence * 100.0,
            decision.reasoning,
            decision.created_at.format("%H:%M:%S")
        );

        let details_paragraph = Paragraph::new(details)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Analysis Details ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::White))
            .wrap(Wrap { trim: true });

        f.render_widget(details_paragraph, chunks[1]);
    } else {
        let empty_text = "🔍 Task Analysis\n\n\
                         No analysis results yet.\n\n\
                         Press Enter to analyze a new task\n\
                         and get Master's recommendation\n\
                         for optimal agent assignment.";

        let empty_paragraph = Paragraph::new(empty_text)
            .block(
                Block::default()
                    .borders(Borders::ALL)
                    .border_type(BorderType::Rounded)
                    .title(" 📋 Ready for Analysis ")
                    .title_alignment(Alignment::Center),
            )
            .style(Style::default().fg(Color::Gray))
            .alignment(Alignment::Center)
            .wrap(Wrap { trim: true });

        f.render_widget(empty_paragraph, chunks[1]);
    }
}

  ./crates/ccswarm/src/tui/ui.rs:838-891 function draw_delegation_mode_selector <-> ./crates/ccswarm/src/tui/ui.rs:1346-1417 function draw_footer
  Similarity: 92.25%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_mode_selector (lines 838-891) ---[0m
fn draw_delegation_mode_selector(f: &mut Frame, area: Rect, app: &App) {
    let mode_chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([
            Constraint::Percentage(33),
            Constraint::Percentage(33),
            Constraint::Percentage(34),
        ])
        .split(area);

    let modes = [
        ("🔍 Analyze", DelegationMode::Analyze, Color::Blue),
        ("🎯 Delegate", DelegationMode::Delegate, Color::Green),
        ("📊 Stats", DelegationMode::ViewStats, Color::Magenta),
    ];

    for (i, (title, mode, color)) in modes.iter().enumerate() {
        let is_selected = *mode == app.delegation_mode;

        let block = if is_selected {
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Double)
                .border_style(Style::default().fg(*color).add_modifier(Modifier::BOLD))
                .title(*title)
                .title_alignment(Alignment::Center)
                .style(Style::default().bg(Color::DarkGray))
        } else {
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .border_style(Style::default().fg(Color::Gray))
                .title(*title)
                .title_alignment(Alignment::Center)
        };

        let description = match mode {
            DelegationMode::Analyze => "Analyze tasks and\nget agent recommendations",
            DelegationMode::Delegate => "Manually delegate\ntasks to specific agents",
            DelegationMode::ViewStats => "View delegation\nstatistics and history",
        };

        let paragraph = Paragraph::new(description)
            .block(block)
            .style(Style::default().fg(if is_selected {
                Color::White
            } else {
                Color::Gray
            }))
            .alignment(Alignment::Center);

        f.render_widget(paragraph, mode_chunks[i]);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_footer (lines 1346-1417) ---[0m
fn draw_footer(f: &mut Frame, area: Rect, app: &App) {
    let key_bindings = match app.current_tab {
        Tab::Overview => vec![
            ("Tab/Shift+Tab", "Switch tabs", Color::Cyan),
            ("c", "Command prompt", Color::Yellow),
            ("r", "Refresh data", Color::Green),
            ("q", "Quit", Color::Red),
        ],
        Tab::Agents => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("Enter", "Start/Details", Color::Green),
            ("n", "New Agent", Color::Blue),
            ("d", "Delete", Color::Red),
            ("c", "Command", Color::Yellow),
        ],
        Tab::Tasks => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("t", "Add Task", Color::Green),
            ("c", "Command", Color::Yellow),
            ("r", "Refresh", Color::Blue),
        ],
        Tab::Logs => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("r", "Refresh", Color::Green),
            ("c", "Command", Color::Yellow),
        ],
        Tab::Delegation => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("Space", "Switch mode", Color::Blue),
            ("Enter", "Analyze/Delegate", Color::Green),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("c", "Command", Color::Yellow),
        ],
    };

    let help_text: Vec<Line> = vec![Line::from(
        key_bindings
            .iter()
            .flat_map(|(key, desc, color)| {
                vec![
                    Span::styled(
                        format!(" {} ", key),
                        Style::default()
                            .fg(*color)
                            .bg(Color::DarkGray)
                            .add_modifier(Modifier::BOLD),
                    ),
                    Span::styled(format!(" {} ", desc), Style::default().fg(Color::Gray)),
                    Span::raw(" │ "),
                ]
                .into_iter()
            })
            .collect::<Vec<_>>(),
    )];

    let footer = Paragraph::new(help_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" ⌨️  Keyboard Shortcuts ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .alignment(Alignment::Center)
        .wrap(Wrap { trim: true });

    f.render_widget(footer, area);
}

  ./crates/ccswarm/src/tui/ui.rs:230-295 function draw_enhanced_provider_stats <-> ./crates/ccswarm/src/tui/ui.rs:1346-1417 function draw_footer
  Similarity: 79.15%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_enhanced_provider_stats (lines 230-295) ---[0m
fn draw_enhanced_provider_stats(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Horizontal)
        .constraints([Constraint::Percentage(50), Constraint::Percentage(50)])
        .split(area);

    // Execution Statistics
    let exec_stats = [
        format!("📊 Total Executed: {}", app.tasks_executed),
        format!("✅ Completed: {}", app.completed_tasks),
        format!("❌ Failed: {}", app.tasks_failed),
        format!("🎯 Orchestration: {:.1}%", app.orchestration_usage),
    ];

    let exec_paragraph = Paragraph::new(exec_stats.join("\n"))
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 🚀 Execution Statistics ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .wrap(Wrap { trim: true });

    f.render_widget(exec_paragraph, chunks[0]);

    // Performance Metrics
    let perf_stats = [
        format!("💯 Success Rate: {:.1}%", app.success_rate),
        format!("⏳ Pending: {}", app.pending_tasks),
        format!(
            "🤖 Active Agents: {}/{}",
            app.active_agents, app.total_agents
        ),
        format!(
            "📈 Engine Status: {}",
            if app.tasks_executed > 0 {
                "Active"
            } else {
                "Idle"
            }
        ),
    ];

    let perf_color = if app.success_rate >= 90.0 {
        Color::Green
    } else if app.success_rate >= 70.0 {
        Color::Yellow
    } else {
        Color::Red
    };

    let perf_paragraph = Paragraph::new(perf_stats.join("\n"))
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" 📈 Performance Metrics ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(perf_color))
        .wrap(Wrap { trim: true });

    f.render_widget(perf_paragraph, chunks[1]);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_footer (lines 1346-1417) ---[0m
fn draw_footer(f: &mut Frame, area: Rect, app: &App) {
    let key_bindings = match app.current_tab {
        Tab::Overview => vec![
            ("Tab/Shift+Tab", "Switch tabs", Color::Cyan),
            ("c", "Command prompt", Color::Yellow),
            ("r", "Refresh data", Color::Green),
            ("q", "Quit", Color::Red),
        ],
        Tab::Agents => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("Enter", "Start/Details", Color::Green),
            ("n", "New Agent", Color::Blue),
            ("d", "Delete", Color::Red),
            ("c", "Command", Color::Yellow),
        ],
        Tab::Tasks => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("t", "Add Task", Color::Green),
            ("c", "Command", Color::Yellow),
            ("r", "Refresh", Color::Blue),
        ],
        Tab::Logs => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("r", "Refresh", Color::Green),
            ("c", "Command", Color::Yellow),
        ],
        Tab::Delegation => vec![
            ("Tab", "Switch tabs", Color::Cyan),
            ("Space", "Switch mode", Color::Blue),
            ("Enter", "Analyze/Delegate", Color::Green),
            ("↑/↓ j/k", "Navigate", Color::White),
            ("c", "Command", Color::Yellow),
        ],
    };

    let help_text: Vec<Line> = vec![Line::from(
        key_bindings
            .iter()
            .flat_map(|(key, desc, color)| {
                vec![
                    Span::styled(
                        format!(" {} ", key),
                        Style::default()
                            .fg(*color)
                            .bg(Color::DarkGray)
                            .add_modifier(Modifier::BOLD),
                    ),
                    Span::styled(format!(" {} ", desc), Style::default().fg(Color::Gray)),
                    Span::raw(" │ "),
                ]
                .into_iter()
            })
            .collect::<Vec<_>>(),
    )];

    let footer = Paragraph::new(help_text)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" ⌨️  Keyboard Shortcuts ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .alignment(Alignment::Center)
        .wrap(Wrap { trim: true });

    f.render_widget(footer, area);
}

  ./crates/ccswarm/src/tui/ui.rs:47-80 function draw_tabs <-> ./crates/ccswarm/src/tui/ui.rs:1306-1343 function draw_delegation_instructions
  Similarity: 89.81%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_tabs (lines 47-80) ---[0m
fn draw_tabs(f: &mut Frame, area: Rect, app: &App) {
    let tab_titles = vec![
        Tab::Overview.title(),
        Tab::Agents.title(),
        Tab::Tasks.title(),
        Tab::Logs.title(),
        Tab::Delegation.title(),
    ];

    let current_tab_index = match app.current_tab {
        Tab::Overview => 0,
        Tab::Agents => 1,
        Tab::Tasks => 2,
        Tab::Logs => 3,
        Tab::Delegation => 4,
    };

    let tabs = Tabs::new(tab_titles)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .title(" ccswarm TUI ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .highlight_style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        )
        .select(current_tab_index);

    f.render_widget(tabs, area);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_instructions (lines 1306-1343) ---[0m
fn draw_delegation_instructions(f: &mut Frame, area: Rect, app: &App) {
    let instructions = match app.delegation_mode {
        DelegationMode::Analyze => {
            "🔍 Analysis Mode Instructions:\n\
             • Press Enter to analyze a new task description\n\
             • Master will recommend the optimal agent based on task content\n\
             • Use ↑/↓ to navigate through analysis results\n\
             • View detailed reasoning and confidence scores"
        }
        DelegationMode::Delegate => {
            "🎯 Delegation Mode Instructions:\n\
             • Press Enter to manually delegate a task to a specific agent\n\
             • Format: <agent_type> <task_description>\n\
             • Valid agents: frontend, backend, devops, qa\n\
             • Tasks are automatically added to the queue"
        }
        DelegationMode::ViewStats => {
            "📊 Statistics Mode Instructions:\n\
             • View delegation patterns and agent utilization\n\
             • Analyze confidence scores and distribution\n\
             • Track delegation history and trends\n\
             • Use Space to switch between modes"
        }
    };

    let instructions_paragraph = Paragraph::new(instructions)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" ℹ️  Instructions ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::Cyan))
        .wrap(Wrap { trim: true });

    f.render_widget(instructions_paragraph, area);
}

  ./crates/ccswarm/src/tui/ui.rs:1306-1343 function draw_delegation_instructions <-> ./crates/ccswarm/src/tui/ui.rs:1561-1579 function centered_rect
  Similarity: 90.49%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_instructions (lines 1306-1343) ---[0m
fn draw_delegation_instructions(f: &mut Frame, area: Rect, app: &App) {
    let instructions = match app.delegation_mode {
        DelegationMode::Analyze => {
            "🔍 Analysis Mode Instructions:\n\
             • Press Enter to analyze a new task description\n\
             • Master will recommend the optimal agent based on task content\n\
             • Use ↑/↓ to navigate through analysis results\n\
             • View detailed reasoning and confidence scores"
        }
        DelegationMode::Delegate => {
            "🎯 Delegation Mode Instructions:\n\
             • Press Enter to manually delegate a task to a specific agent\n\
             • Format: <agent_type> <task_description>\n\
             • Valid agents: frontend, backend, devops, qa\n\
             • Tasks are automatically added to the queue"
        }
        DelegationMode::ViewStats => {
            "📊 Statistics Mode Instructions:\n\
             • View delegation patterns and agent utilization\n\
             • Analyze confidence scores and distribution\n\
             • Track delegation history and trends\n\
             • Use Space to switch between modes"
        }
    };

    let instructions_paragraph = Paragraph::new(instructions)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" ℹ️  Instructions ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::Cyan))
        .wrap(Wrap { trim: true });

    f.render_widget(instructions_paragraph, area);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:centered_rect (lines 1561-1579) ---[0m
fn centered_rect(percent_x: u16, percent_y: u16, r: Rect) -> Rect {
    let popup_layout = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Percentage((100 - percent_y) / 2),
            Constraint::Percentage(percent_y),
            Constraint::Percentage((100 - percent_y) / 2),
        ])
        .split(r);

    Layout::default()
        .direction(Direction::Horizontal)
        .constraints([
            Constraint::Percentage((100 - percent_x) / 2),
            Constraint::Percentage(percent_x),
            Constraint::Percentage((100 - percent_x) / 2),
        ])
        .split(popup_layout[1])[1]
}

  ./crates/ccswarm/src/tui/ui.rs:15-44 function draw <-> ./crates/ccswarm/src/tui/ui.rs:47-80 function draw_tabs
  Similarity: 80.56%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw (lines 15-44) ---[0m
pub fn draw(f: &mut Frame, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(3), // Header/Tabs
            Constraint::Min(0),    // Main content
            Constraint::Length(3), // Footer/Status
        ])
        .split(f.area());

    // Draw header with tabs
    draw_tabs(f, chunks[0], app);

    // Draw main content based on current tab
    match app.current_tab {
        Tab::Overview => draw_overview(f, chunks[1], app),
        Tab::Agents => draw_agents(f, chunks[1], app),
        Tab::Tasks => draw_tasks(f, chunks[1], app),
        Tab::Logs => draw_logs(f, chunks[1], app),
        Tab::Delegation => draw_delegation(f, chunks[1], app),
    }

    // Draw footer
    draw_footer(f, chunks[2], app);

    // Draw input popup if needed
    if app.input_mode != InputMode::Normal {
        draw_input_popup(f, app);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_tabs (lines 47-80) ---[0m
fn draw_tabs(f: &mut Frame, area: Rect, app: &App) {
    let tab_titles = vec![
        Tab::Overview.title(),
        Tab::Agents.title(),
        Tab::Tasks.title(),
        Tab::Logs.title(),
        Tab::Delegation.title(),
    ];

    let current_tab_index = match app.current_tab {
        Tab::Overview => 0,
        Tab::Agents => 1,
        Tab::Tasks => 2,
        Tab::Logs => 3,
        Tab::Delegation => 4,
    };

    let tabs = Tabs::new(tab_titles)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .title(" ccswarm TUI ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .highlight_style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        )
        .select(current_tab_index);

    f.render_widget(tabs, area);
}

  ./crates/ccswarm/src/tui/ui.rs:813-835 function draw_delegation <-> ./crates/ccswarm/src/tui/ui.rs:1306-1343 function draw_delegation_instructions
  Similarity: 82.78%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation (lines 813-835) ---[0m
fn draw_delegation(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(5), // Mode selector
            Constraint::Min(10),   // Main content
            Constraint::Length(8), // Instructions/Help
        ])
        .split(area);

    // Mode selector
    draw_delegation_mode_selector(f, chunks[0], app);

    // Main content based on delegation mode
    match app.delegation_mode {
        DelegationMode::Analyze => draw_delegation_analysis(f, chunks[1], app),
        DelegationMode::Delegate => draw_delegation_interface(f, chunks[1], app),
        DelegationMode::ViewStats => draw_delegation_stats(f, chunks[1], app),
    }

    // Instructions
    draw_delegation_instructions(f, chunks[2], app);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_instructions (lines 1306-1343) ---[0m
fn draw_delegation_instructions(f: &mut Frame, area: Rect, app: &App) {
    let instructions = match app.delegation_mode {
        DelegationMode::Analyze => {
            "🔍 Analysis Mode Instructions:\n\
             • Press Enter to analyze a new task description\n\
             • Master will recommend the optimal agent based on task content\n\
             • Use ↑/↓ to navigate through analysis results\n\
             • View detailed reasoning and confidence scores"
        }
        DelegationMode::Delegate => {
            "🎯 Delegation Mode Instructions:\n\
             • Press Enter to manually delegate a task to a specific agent\n\
             • Format: <agent_type> <task_description>\n\
             • Valid agents: frontend, backend, devops, qa\n\
             • Tasks are automatically added to the queue"
        }
        DelegationMode::ViewStats => {
            "📊 Statistics Mode Instructions:\n\
             • View delegation patterns and agent utilization\n\
             • Analyze confidence scores and distribution\n\
             • Track delegation history and trends\n\
             • Use Space to switch between modes"
        }
    };

    let instructions_paragraph = Paragraph::new(instructions)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" ℹ️  Instructions ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::Cyan))
        .wrap(Wrap { trim: true });

    f.render_widget(instructions_paragraph, area);
}

  ./crates/ccswarm/src/tui/ui.rs:15-44 function draw <-> ./crates/ccswarm/src/tui/ui.rs:1306-1343 function draw_delegation_instructions
  Similarity: 74.25%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw (lines 15-44) ---[0m
pub fn draw(f: &mut Frame, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(3), // Header/Tabs
            Constraint::Min(0),    // Main content
            Constraint::Length(3), // Footer/Status
        ])
        .split(f.area());

    // Draw header with tabs
    draw_tabs(f, chunks[0], app);

    // Draw main content based on current tab
    match app.current_tab {
        Tab::Overview => draw_overview(f, chunks[1], app),
        Tab::Agents => draw_agents(f, chunks[1], app),
        Tab::Tasks => draw_tasks(f, chunks[1], app),
        Tab::Logs => draw_logs(f, chunks[1], app),
        Tab::Delegation => draw_delegation(f, chunks[1], app),
    }

    // Draw footer
    draw_footer(f, chunks[2], app);

    // Draw input popup if needed
    if app.input_mode != InputMode::Normal {
        draw_input_popup(f, app);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_instructions (lines 1306-1343) ---[0m
fn draw_delegation_instructions(f: &mut Frame, area: Rect, app: &App) {
    let instructions = match app.delegation_mode {
        DelegationMode::Analyze => {
            "🔍 Analysis Mode Instructions:\n\
             • Press Enter to analyze a new task description\n\
             • Master will recommend the optimal agent based on task content\n\
             • Use ↑/↓ to navigate through analysis results\n\
             • View detailed reasoning and confidence scores"
        }
        DelegationMode::Delegate => {
            "🎯 Delegation Mode Instructions:\n\
             • Press Enter to manually delegate a task to a specific agent\n\
             • Format: <agent_type> <task_description>\n\
             • Valid agents: frontend, backend, devops, qa\n\
             • Tasks are automatically added to the queue"
        }
        DelegationMode::ViewStats => {
            "📊 Statistics Mode Instructions:\n\
             • View delegation patterns and agent utilization\n\
             • Analyze confidence scores and distribution\n\
             • Track delegation history and trends\n\
             • Use Space to switch between modes"
        }
    };

    let instructions_paragraph = Paragraph::new(instructions)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" ℹ️  Instructions ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::Cyan))
        .wrap(Wrap { trim: true });

    f.render_widget(instructions_paragraph, area);
}

  ./crates/ccswarm/src/tui/ui.rs:47-80 function draw_tabs <-> ./crates/ccswarm/src/tui/ui.rs:813-835 function draw_delegation
  Similarity: 88.33%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_tabs (lines 47-80) ---[0m
fn draw_tabs(f: &mut Frame, area: Rect, app: &App) {
    let tab_titles = vec![
        Tab::Overview.title(),
        Tab::Agents.title(),
        Tab::Tasks.title(),
        Tab::Logs.title(),
        Tab::Delegation.title(),
    ];

    let current_tab_index = match app.current_tab {
        Tab::Overview => 0,
        Tab::Agents => 1,
        Tab::Tasks => 2,
        Tab::Logs => 3,
        Tab::Delegation => 4,
    };

    let tabs = Tabs::new(tab_titles)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .title(" ccswarm TUI ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .highlight_style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        )
        .select(current_tab_index);

    f.render_widget(tabs, area);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation (lines 813-835) ---[0m
fn draw_delegation(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(5), // Mode selector
            Constraint::Min(10),   // Main content
            Constraint::Length(8), // Instructions/Help
        ])
        .split(area);

    // Mode selector
    draw_delegation_mode_selector(f, chunks[0], app);

    // Main content based on delegation mode
    match app.delegation_mode {
        DelegationMode::Analyze => draw_delegation_analysis(f, chunks[1], app),
        DelegationMode::Delegate => draw_delegation_interface(f, chunks[1], app),
        DelegationMode::ViewStats => draw_delegation_stats(f, chunks[1], app),
    }

    // Instructions
    draw_delegation_instructions(f, chunks[2], app);
}

  ./crates/ccswarm/src/tui/ui.rs:15-44 function draw <-> ./crates/ccswarm/src/tui/ui.rs:813-835 function draw_delegation
  Similarity: 92.22%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw (lines 15-44) ---[0m
pub fn draw(f: &mut Frame, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(3), // Header/Tabs
            Constraint::Min(0),    // Main content
            Constraint::Length(3), // Footer/Status
        ])
        .split(f.area());

    // Draw header with tabs
    draw_tabs(f, chunks[0], app);

    // Draw main content based on current tab
    match app.current_tab {
        Tab::Overview => draw_overview(f, chunks[1], app),
        Tab::Agents => draw_agents(f, chunks[1], app),
        Tab::Tasks => draw_tasks(f, chunks[1], app),
        Tab::Logs => draw_logs(f, chunks[1], app),
        Tab::Delegation => draw_delegation(f, chunks[1], app),
    }

    // Draw footer
    draw_footer(f, chunks[2], app);

    // Draw input popup if needed
    if app.input_mode != InputMode::Normal {
        draw_input_popup(f, app);
    }
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation (lines 813-835) ---[0m
fn draw_delegation(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(5), // Mode selector
            Constraint::Min(10),   // Main content
            Constraint::Length(8), // Instructions/Help
        ])
        .split(area);

    // Mode selector
    draw_delegation_mode_selector(f, chunks[0], app);

    // Main content based on delegation mode
    match app.delegation_mode {
        DelegationMode::Analyze => draw_delegation_analysis(f, chunks[1], app),
        DelegationMode::Delegate => draw_delegation_interface(f, chunks[1], app),
        DelegationMode::ViewStats => draw_delegation_stats(f, chunks[1], app),
    }

    // Instructions
    draw_delegation_instructions(f, chunks[2], app);
}

  ./crates/ccswarm/src/tui/ui.rs:83-101 function draw_overview <-> ./crates/ccswarm/src/tui/ui.rs:1306-1343 function draw_delegation_instructions
  Similarity: 83.90%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_overview (lines 83-101) ---[0m
fn draw_overview(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(9),  // System stats with better spacing
            Constraint::Length(10), // Provider stats with more detail
            Constraint::Min(8),     // Agent summary with minimum height
        ])
        .split(area);

    // System stats with enhanced design
    draw_enhanced_system_stats(f, chunks[0], app);

    // Provider statistics with better layout
    draw_enhanced_provider_stats(f, chunks[1], app);

    // Agent summary with improved formatting
    draw_enhanced_agent_summary(f, chunks[2], app);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation_instructions (lines 1306-1343) ---[0m
fn draw_delegation_instructions(f: &mut Frame, area: Rect, app: &App) {
    let instructions = match app.delegation_mode {
        DelegationMode::Analyze => {
            "🔍 Analysis Mode Instructions:\n\
             • Press Enter to analyze a new task description\n\
             • Master will recommend the optimal agent based on task content\n\
             • Use ↑/↓ to navigate through analysis results\n\
             • View detailed reasoning and confidence scores"
        }
        DelegationMode::Delegate => {
            "🎯 Delegation Mode Instructions:\n\
             • Press Enter to manually delegate a task to a specific agent\n\
             • Format: <agent_type> <task_description>\n\
             • Valid agents: frontend, backend, devops, qa\n\
             • Tasks are automatically added to the queue"
        }
        DelegationMode::ViewStats => {
            "📊 Statistics Mode Instructions:\n\
             • View delegation patterns and agent utilization\n\
             • Analyze confidence scores and distribution\n\
             • Track delegation history and trends\n\
             • Use Space to switch between modes"
        }
    };

    let instructions_paragraph = Paragraph::new(instructions)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .border_type(BorderType::Rounded)
                .title(" ℹ️  Instructions ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::Cyan))
        .wrap(Wrap { trim: true });

    f.render_widget(instructions_paragraph, area);
}

  ./crates/ccswarm/src/tui/ui.rs:83-101 function draw_overview <-> ./crates/ccswarm/src/tui/ui.rs:813-835 function draw_delegation
  Similarity: 95.29%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_overview (lines 83-101) ---[0m
fn draw_overview(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(9),  // System stats with better spacing
            Constraint::Length(10), // Provider stats with more detail
            Constraint::Min(8),     // Agent summary with minimum height
        ])
        .split(area);

    // System stats with enhanced design
    draw_enhanced_system_stats(f, chunks[0], app);

    // Provider statistics with better layout
    draw_enhanced_provider_stats(f, chunks[1], app);

    // Agent summary with improved formatting
    draw_enhanced_agent_summary(f, chunks[2], app);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation (lines 813-835) ---[0m
fn draw_delegation(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(5), // Mode selector
            Constraint::Min(10),   // Main content
            Constraint::Length(8), // Instructions/Help
        ])
        .split(area);

    // Mode selector
    draw_delegation_mode_selector(f, chunks[0], app);

    // Main content based on delegation mode
    match app.delegation_mode {
        DelegationMode::Analyze => draw_delegation_analysis(f, chunks[1], app),
        DelegationMode::Delegate => draw_delegation_interface(f, chunks[1], app),
        DelegationMode::ViewStats => draw_delegation_stats(f, chunks[1], app),
    }

    // Instructions
    draw_delegation_instructions(f, chunks[2], app);
}

  ./crates/ccswarm/src/tui/ui.rs:47-80 function draw_tabs <-> ./crates/ccswarm/src/tui/ui.rs:1561-1579 function centered_rect
  Similarity: 70.76%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_tabs (lines 47-80) ---[0m
fn draw_tabs(f: &mut Frame, area: Rect, app: &App) {
    let tab_titles = vec![
        Tab::Overview.title(),
        Tab::Agents.title(),
        Tab::Tasks.title(),
        Tab::Logs.title(),
        Tab::Delegation.title(),
    ];

    let current_tab_index = match app.current_tab {
        Tab::Overview => 0,
        Tab::Agents => 1,
        Tab::Tasks => 2,
        Tab::Logs => 3,
        Tab::Delegation => 4,
    };

    let tabs = Tabs::new(tab_titles)
        .block(
            Block::default()
                .borders(Borders::ALL)
                .title(" ccswarm TUI ")
                .title_alignment(Alignment::Center),
        )
        .style(Style::default().fg(Color::White))
        .highlight_style(
            Style::default()
                .fg(Color::Yellow)
                .add_modifier(Modifier::BOLD),
        )
        .select(current_tab_index);

    f.render_widget(tabs, area);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:centered_rect (lines 1561-1579) ---[0m
fn centered_rect(percent_x: u16, percent_y: u16, r: Rect) -> Rect {
    let popup_layout = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Percentage((100 - percent_y) / 2),
            Constraint::Percentage(percent_y),
            Constraint::Percentage((100 - percent_y) / 2),
        ])
        .split(r);

    Layout::default()
        .direction(Direction::Horizontal)
        .constraints([
            Constraint::Percentage((100 - percent_x) / 2),
            Constraint::Percentage(percent_x),
            Constraint::Percentage((100 - percent_x) / 2),
        ])
        .split(popup_layout[1])[1]
}

  ./crates/ccswarm/src/tui/ui.rs:813-835 function draw_delegation <-> ./crates/ccswarm/src/tui/ui.rs:1561-1579 function centered_rect
  Similarity: 70.64%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_delegation (lines 813-835) ---[0m
fn draw_delegation(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(5), // Mode selector
            Constraint::Min(10),   // Main content
            Constraint::Length(8), // Instructions/Help
        ])
        .split(area);

    // Mode selector
    draw_delegation_mode_selector(f, chunks[0], app);

    // Main content based on delegation mode
    match app.delegation_mode {
        DelegationMode::Analyze => draw_delegation_analysis(f, chunks[1], app),
        DelegationMode::Delegate => draw_delegation_interface(f, chunks[1], app),
        DelegationMode::ViewStats => draw_delegation_stats(f, chunks[1], app),
    }

    // Instructions
    draw_delegation_instructions(f, chunks[2], app);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:centered_rect (lines 1561-1579) ---[0m
fn centered_rect(percent_x: u16, percent_y: u16, r: Rect) -> Rect {
    let popup_layout = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Percentage((100 - percent_y) / 2),
            Constraint::Percentage(percent_y),
            Constraint::Percentage((100 - percent_y) / 2),
        ])
        .split(r);

    Layout::default()
        .direction(Direction::Horizontal)
        .constraints([
            Constraint::Percentage((100 - percent_x) / 2),
            Constraint::Percentage(percent_x),
            Constraint::Percentage((100 - percent_x) / 2),
        ])
        .split(popup_layout[1])[1]
}

  ./crates/ccswarm/src/tui/ui.rs:83-101 function draw_overview <-> ./crates/ccswarm/src/tui/ui.rs:1561-1579 function centered_rect
  Similarity: 72.99%

[36m--- ./crates/ccswarm/src/tui/ui.rs:draw_overview (lines 83-101) ---[0m
fn draw_overview(f: &mut Frame, area: Rect, app: &App) {
    let chunks = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Length(9),  // System stats with better spacing
            Constraint::Length(10), // Provider stats with more detail
            Constraint::Min(8),     // Agent summary with minimum height
        ])
        .split(area);

    // System stats with enhanced design
    draw_enhanced_system_stats(f, chunks[0], app);

    // Provider statistics with better layout
    draw_enhanced_provider_stats(f, chunks[1], app);

    // Agent summary with improved formatting
    draw_enhanced_agent_summary(f, chunks[2], app);
}

[36m--- ./crates/ccswarm/src/tui/ui.rs:centered_rect (lines 1561-1579) ---[0m
fn centered_rect(percent_x: u16, percent_y: u16, r: Rect) -> Rect {
    let popup_layout = Layout::default()
        .direction(Direction::Vertical)
        .constraints([
            Constraint::Percentage((100 - percent_y) / 2),
            Constraint::Percentage(percent_y),
            Constraint::Percentage((100 - percent_y) / 2),
        ])
        .split(r);

    Layout::default()
        .direction(Direction::Horizontal)
        .constraints([
            Constraint::Percentage((100 - percent_x) / 2),
            Constraint::Percentage(percent_x),
            Constraint::Percentage((100 - percent_x) / 2),
        ])
        .split(popup_layout[1])[1]
}


Duplicates in ./crates/ccswarm/src/coordination/conversion.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/coordination/conversion.rs:45-49 method register <-> ./crates/ccswarm/src/coordination/conversion.rs:52-55 method get_agent_info
  Similarity: 71.07%
  Classes: AgentMappingRegistry <-> AgentMappingRegistry

[36m--- ./crates/ccswarm/src/coordination/conversion.rs:register (lines 45-49) ---[0m
    pub async fn register(&self, info: UnifiedAgentInfo) -> Result<()> {
        let mut mappings = self.mappings.write().unwrap();
        mappings.insert(info.ccswarm_id.clone(), info);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/coordination/conversion.rs:get_agent_info (lines 52-55) ---[0m
    pub fn get_agent_info(&self, agent_id: &str) -> Option<UnifiedAgentInfo> {
        let mappings = self.mappings.read().unwrap();
        mappings.get(agent_id).cloned()
    }


Duplicates in ./crates/ccswarm/src/workspace/mod.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/workspace/mod.rs:49-73 method create_workspace <-> ./crates/ccswarm/src/workspace/mod.rs:76-104 method list_workspaces
  Similarity: 90.77%
  Classes: SimpleWorkspaceManager <-> SimpleWorkspaceManager

[36m--- ./crates/ccswarm/src/workspace/mod.rs:create_workspace (lines 49-73) ---[0m
    pub async fn create_workspace(&self, agent_id: &str) -> Result<WorkspaceInfo> {
        let workspace_path = self.base_path.join("agents").join(agent_id);

        if workspace_path.exists() {
            warn!("Workspace already exists: {}", workspace_path.display());
        } else {
            fs::create_dir_all(&workspace_path)
                .await
                .context("Failed to create agent workspace")?;

            info!("Created workspace: {}", workspace_path.display());
        }

        let workspace_info = WorkspaceInfo {
            path: workspace_path,
            agent_id: agent_id.to_string(),
            created_at: chrono::Utc::now(),
            is_active: true,
        };

        // ワークスペース情報を保存
        self.save_workspace_info(&workspace_info).await?;

        Ok(workspace_info)
    }

[36m--- ./crates/ccswarm/src/workspace/mod.rs:list_workspaces (lines 76-104) ---[0m
    pub async fn list_workspaces(&self) -> Result<Vec<WorkspaceInfo>> {
        let agents_dir = self.base_path.join("agents");
        if !agents_dir.exists() {
            return Ok(Vec::new());
        }

        let mut workspaces = Vec::new();
        let mut entries = fs::read_dir(&agents_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if entry.metadata().await?.is_dir() {
                let agent_id = entry.file_name().to_string_lossy().to_string();
                if let Ok(info) = self.load_workspace_info(&agent_id).await {
                    workspaces.push(info);
                } else {
                    // 情報ファイルがない場合は新規作成
                    let workspace_info = WorkspaceInfo {
                        path: entry.path(),
                        agent_id: agent_id.clone(),
                        created_at: chrono::Utc::now(),
                        is_active: true,
                    };
                    workspaces.push(workspace_info);
                }
            }
        }

        Ok(workspaces)
    }

  ./crates/ccswarm/src/workspace/mod.rs:29-46 method init_if_needed <-> ./crates/ccswarm/src/workspace/mod.rs:76-104 method list_workspaces
  Similarity: 85.38%
  Classes: SimpleWorkspaceManager <-> SimpleWorkspaceManager

[36m--- ./crates/ccswarm/src/workspace/mod.rs:init_if_needed (lines 29-46) ---[0m
    pub async fn init_if_needed(&self) -> Result<()> {
        if !self.base_path.exists() {
            info!("Creating workspace directory: {}", self.base_path.display());
            fs::create_dir_all(&self.base_path)
                .await
                .context("Failed to create workspace directory")?;
        }

        // agents ディレクトリを作成
        let agents_dir = self.base_path.join("agents");
        if !agents_dir.exists() {
            fs::create_dir_all(&agents_dir)
                .await
                .context("Failed to create agents directory")?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/workspace/mod.rs:list_workspaces (lines 76-104) ---[0m
    pub async fn list_workspaces(&self) -> Result<Vec<WorkspaceInfo>> {
        let agents_dir = self.base_path.join("agents");
        if !agents_dir.exists() {
            return Ok(Vec::new());
        }

        let mut workspaces = Vec::new();
        let mut entries = fs::read_dir(&agents_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if entry.metadata().await?.is_dir() {
                let agent_id = entry.file_name().to_string_lossy().to_string();
                if let Ok(info) = self.load_workspace_info(&agent_id).await {
                    workspaces.push(info);
                } else {
                    // 情報ファイルがない場合は新規作成
                    let workspace_info = WorkspaceInfo {
                        path: entry.path(),
                        agent_id: agent_id.clone(),
                        created_at: chrono::Utc::now(),
                        is_active: true,
                    };
                    workspaces.push(workspace_info);
                }
            }
        }

        Ok(workspaces)
    }

  ./crates/ccswarm/src/workspace/mod.rs:29-46 method init_if_needed <-> ./crates/ccswarm/src/workspace/mod.rs:49-73 method create_workspace
  Similarity: 85.34%
  Classes: SimpleWorkspaceManager <-> SimpleWorkspaceManager

[36m--- ./crates/ccswarm/src/workspace/mod.rs:init_if_needed (lines 29-46) ---[0m
    pub async fn init_if_needed(&self) -> Result<()> {
        if !self.base_path.exists() {
            info!("Creating workspace directory: {}", self.base_path.display());
            fs::create_dir_all(&self.base_path)
                .await
                .context("Failed to create workspace directory")?;
        }

        // agents ディレクトリを作成
        let agents_dir = self.base_path.join("agents");
        if !agents_dir.exists() {
            fs::create_dir_all(&agents_dir)
                .await
                .context("Failed to create agents directory")?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/workspace/mod.rs:create_workspace (lines 49-73) ---[0m
    pub async fn create_workspace(&self, agent_id: &str) -> Result<WorkspaceInfo> {
        let workspace_path = self.base_path.join("agents").join(agent_id);

        if workspace_path.exists() {
            warn!("Workspace already exists: {}", workspace_path.display());
        } else {
            fs::create_dir_all(&workspace_path)
                .await
                .context("Failed to create agent workspace")?;

            info!("Created workspace: {}", workspace_path.display());
        }

        let workspace_info = WorkspaceInfo {
            path: workspace_path,
            agent_id: agent_id.to_string(),
            created_at: chrono::Utc::now(),
            is_active: true,
        };

        // ワークスペース情報を保存
        self.save_workspace_info(&workspace_info).await?;

        Ok(workspace_info)
    }

  ./crates/ccswarm/src/workspace/mod.rs:76-104 method list_workspaces <-> ./crates/ccswarm/src/workspace/mod.rs:176-186 function test_workspace_listing
  Similarity: 79.09%

[36m--- ./crates/ccswarm/src/workspace/mod.rs:list_workspaces (lines 76-104) ---[0m
    pub async fn list_workspaces(&self) -> Result<Vec<WorkspaceInfo>> {
        let agents_dir = self.base_path.join("agents");
        if !agents_dir.exists() {
            return Ok(Vec::new());
        }

        let mut workspaces = Vec::new();
        let mut entries = fs::read_dir(&agents_dir).await?;

        while let Some(entry) = entries.next_entry().await? {
            if entry.metadata().await?.is_dir() {
                let agent_id = entry.file_name().to_string_lossy().to_string();
                if let Ok(info) = self.load_workspace_info(&agent_id).await {
                    workspaces.push(info);
                } else {
                    // 情報ファイルがない場合は新規作成
                    let workspace_info = WorkspaceInfo {
                        path: entry.path(),
                        agent_id: agent_id.clone(),
                        created_at: chrono::Utc::now(),
                        is_active: true,
                    };
                    workspaces.push(workspace_info);
                }
            }
        }

        Ok(workspaces)
    }

[36m--- ./crates/ccswarm/src/workspace/mod.rs:test_workspace_listing (lines 176-186) ---[0m
    async fn test_workspace_listing() {
        let temp_dir = TempDir::new().unwrap();
        let manager = SimpleWorkspaceManager::new(temp_dir.path().to_path_buf());

        manager.init_if_needed().await.unwrap();
        manager.create_workspace("agent1").await.unwrap();
        manager.create_workspace("agent2").await.unwrap();

        let workspaces = manager.list_workspaces().await.unwrap();
        assert_eq!(workspaces.len(), 2);
    }

  ./crates/ccswarm/src/workspace/mod.rs:49-73 method create_workspace <-> ./crates/ccswarm/src/workspace/mod.rs:131-142 method load_workspace_info
  Similarity: 84.92%
  Classes: SimpleWorkspaceManager <-> SimpleWorkspaceManager

[36m--- ./crates/ccswarm/src/workspace/mod.rs:create_workspace (lines 49-73) ---[0m
    pub async fn create_workspace(&self, agent_id: &str) -> Result<WorkspaceInfo> {
        let workspace_path = self.base_path.join("agents").join(agent_id);

        if workspace_path.exists() {
            warn!("Workspace already exists: {}", workspace_path.display());
        } else {
            fs::create_dir_all(&workspace_path)
                .await
                .context("Failed to create agent workspace")?;

            info!("Created workspace: {}", workspace_path.display());
        }

        let workspace_info = WorkspaceInfo {
            path: workspace_path,
            agent_id: agent_id.to_string(),
            created_at: chrono::Utc::now(),
            is_active: true,
        };

        // ワークスペース情報を保存
        self.save_workspace_info(&workspace_info).await?;

        Ok(workspace_info)
    }

[36m--- ./crates/ccswarm/src/workspace/mod.rs:load_workspace_info (lines 131-142) ---[0m
    async fn load_workspace_info(&self, agent_id: &str) -> Result<WorkspaceInfo> {
        let workspace_path = self.base_path.join("agents").join(agent_id);
        let info_file = workspace_path.join(".workspace_info.json");

        let content = fs::read_to_string(&info_file)
            .await
            .context("Failed to read workspace info")?;
        let info: WorkspaceInfo =
            serde_json::from_str(&content).context("Failed to parse workspace info")?;

        Ok(info)
    }

  ./crates/ccswarm/src/workspace/mod.rs:49-73 method create_workspace <-> ./crates/ccswarm/src/workspace/mod.rs:145-155 method setup_claude_config
  Similarity: 85.24%
  Classes: SimpleWorkspaceManager <-> SimpleWorkspaceManager

[36m--- ./crates/ccswarm/src/workspace/mod.rs:create_workspace (lines 49-73) ---[0m
    pub async fn create_workspace(&self, agent_id: &str) -> Result<WorkspaceInfo> {
        let workspace_path = self.base_path.join("agents").join(agent_id);

        if workspace_path.exists() {
            warn!("Workspace already exists: {}", workspace_path.display());
        } else {
            fs::create_dir_all(&workspace_path)
                .await
                .context("Failed to create agent workspace")?;

            info!("Created workspace: {}", workspace_path.display());
        }

        let workspace_info = WorkspaceInfo {
            path: workspace_path,
            agent_id: agent_id.to_string(),
            created_at: chrono::Utc::now(),
            is_active: true,
        };

        // ワークスペース情報を保存
        self.save_workspace_info(&workspace_info).await?;

        Ok(workspace_info)
    }

[36m--- ./crates/ccswarm/src/workspace/mod.rs:setup_claude_config (lines 145-155) ---[0m
    pub async fn setup_claude_config(&self, agent_id: &str, claude_md_content: &str) -> Result<()> {
        let workspace_path = self.base_path.join("agents").join(agent_id);
        let claude_md_path = workspace_path.join("CLAUDE.md");

        fs::write(&claude_md_path, claude_md_content)
            .await
            .context("Failed to write CLAUDE.md")?;

        info!("CLAUDE.md created for agent: {}", agent_id);
        Ok(())
    }

  ./crates/ccswarm/src/workspace/mod.rs:49-73 method create_workspace <-> ./crates/ccswarm/src/workspace/mod.rs:164-173 function test_workspace_creation
  Similarity: 80.95%

[36m--- ./crates/ccswarm/src/workspace/mod.rs:create_workspace (lines 49-73) ---[0m
    pub async fn create_workspace(&self, agent_id: &str) -> Result<WorkspaceInfo> {
        let workspace_path = self.base_path.join("agents").join(agent_id);

        if workspace_path.exists() {
            warn!("Workspace already exists: {}", workspace_path.display());
        } else {
            fs::create_dir_all(&workspace_path)
                .await
                .context("Failed to create agent workspace")?;

            info!("Created workspace: {}", workspace_path.display());
        }

        let workspace_info = WorkspaceInfo {
            path: workspace_path,
            agent_id: agent_id.to_string(),
            created_at: chrono::Utc::now(),
            is_active: true,
        };

        // ワークスペース情報を保存
        self.save_workspace_info(&workspace_info).await?;

        Ok(workspace_info)
    }

[36m--- ./crates/ccswarm/src/workspace/mod.rs:test_workspace_creation (lines 164-173) ---[0m
    async fn test_workspace_creation() {
        let temp_dir = TempDir::new().unwrap();
        let manager = SimpleWorkspaceManager::new(temp_dir.path().to_path_buf());

        manager.init_if_needed().await.unwrap();

        let workspace = manager.create_workspace("test-agent").await.unwrap();
        assert_eq!(workspace.agent_id, "test-agent");
        assert!(workspace.path.exists());
    }

  ./crates/ccswarm/src/workspace/mod.rs:49-73 method create_workspace <-> ./crates/ccswarm/src/workspace/mod.rs:107-118 method remove_workspace
  Similarity: 75.08%
  Classes: SimpleWorkspaceManager <-> SimpleWorkspaceManager

[36m--- ./crates/ccswarm/src/workspace/mod.rs:create_workspace (lines 49-73) ---[0m
    pub async fn create_workspace(&self, agent_id: &str) -> Result<WorkspaceInfo> {
        let workspace_path = self.base_path.join("agents").join(agent_id);

        if workspace_path.exists() {
            warn!("Workspace already exists: {}", workspace_path.display());
        } else {
            fs::create_dir_all(&workspace_path)
                .await
                .context("Failed to create agent workspace")?;

            info!("Created workspace: {}", workspace_path.display());
        }

        let workspace_info = WorkspaceInfo {
            path: workspace_path,
            agent_id: agent_id.to_string(),
            created_at: chrono::Utc::now(),
            is_active: true,
        };

        // ワークスペース情報を保存
        self.save_workspace_info(&workspace_info).await?;

        Ok(workspace_info)
    }

[36m--- ./crates/ccswarm/src/workspace/mod.rs:remove_workspace (lines 107-118) ---[0m
    pub async fn remove_workspace(&self, agent_id: &str) -> Result<()> {
        let workspace_path = self.base_path.join("agents").join(agent_id);

        if workspace_path.exists() {
            fs::remove_dir_all(&workspace_path)
                .await
                .context("Failed to remove workspace")?;
            info!("Removed workspace: {}", workspace_path.display());
        }

        Ok(())
    }

  ./crates/ccswarm/src/workspace/mod.rs:49-73 method create_workspace <-> ./crates/ccswarm/src/workspace/mod.rs:176-186 function test_workspace_listing
  Similarity: 72.17%

[36m--- ./crates/ccswarm/src/workspace/mod.rs:create_workspace (lines 49-73) ---[0m
    pub async fn create_workspace(&self, agent_id: &str) -> Result<WorkspaceInfo> {
        let workspace_path = self.base_path.join("agents").join(agent_id);

        if workspace_path.exists() {
            warn!("Workspace already exists: {}", workspace_path.display());
        } else {
            fs::create_dir_all(&workspace_path)
                .await
                .context("Failed to create agent workspace")?;

            info!("Created workspace: {}", workspace_path.display());
        }

        let workspace_info = WorkspaceInfo {
            path: workspace_path,
            agent_id: agent_id.to_string(),
            created_at: chrono::Utc::now(),
            is_active: true,
        };

        // ワークスペース情報を保存
        self.save_workspace_info(&workspace_info).await?;

        Ok(workspace_info)
    }

[36m--- ./crates/ccswarm/src/workspace/mod.rs:test_workspace_listing (lines 176-186) ---[0m
    async fn test_workspace_listing() {
        let temp_dir = TempDir::new().unwrap();
        let manager = SimpleWorkspaceManager::new(temp_dir.path().to_path_buf());

        manager.init_if_needed().await.unwrap();
        manager.create_workspace("agent1").await.unwrap();
        manager.create_workspace("agent2").await.unwrap();

        let workspaces = manager.list_workspaces().await.unwrap();
        assert_eq!(workspaces.len(), 2);
    }

  ./crates/ccswarm/src/workspace/mod.rs:29-46 method init_if_needed <-> ./crates/ccswarm/src/workspace/mod.rs:145-155 method setup_claude_config
  Similarity: 81.76%
  Classes: SimpleWorkspaceManager <-> SimpleWorkspaceManager

[36m--- ./crates/ccswarm/src/workspace/mod.rs:init_if_needed (lines 29-46) ---[0m
    pub async fn init_if_needed(&self) -> Result<()> {
        if !self.base_path.exists() {
            info!("Creating workspace directory: {}", self.base_path.display());
            fs::create_dir_all(&self.base_path)
                .await
                .context("Failed to create workspace directory")?;
        }

        // agents ディレクトリを作成
        let agents_dir = self.base_path.join("agents");
        if !agents_dir.exists() {
            fs::create_dir_all(&agents_dir)
                .await
                .context("Failed to create agents directory")?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/workspace/mod.rs:setup_claude_config (lines 145-155) ---[0m
    pub async fn setup_claude_config(&self, agent_id: &str, claude_md_content: &str) -> Result<()> {
        let workspace_path = self.base_path.join("agents").join(agent_id);
        let claude_md_path = workspace_path.join("CLAUDE.md");

        fs::write(&claude_md_path, claude_md_content)
            .await
            .context("Failed to write CLAUDE.md")?;

        info!("CLAUDE.md created for agent: {}", agent_id);
        Ok(())
    }

  ./crates/ccswarm/src/workspace/mod.rs:29-46 method init_if_needed <-> ./crates/ccswarm/src/workspace/mod.rs:131-142 method load_workspace_info
  Similarity: 77.57%
  Classes: SimpleWorkspaceManager <-> SimpleWorkspaceManager

[36m--- ./crates/ccswarm/src/workspace/mod.rs:init_if_needed (lines 29-46) ---[0m
    pub async fn init_if_needed(&self) -> Result<()> {
        if !self.base_path.exists() {
            info!("Creating workspace directory: {}", self.base_path.display());
            fs::create_dir_all(&self.base_path)
                .await
                .context("Failed to create workspace directory")?;
        }

        // agents ディレクトリを作成
        let agents_dir = self.base_path.join("agents");
        if !agents_dir.exists() {
            fs::create_dir_all(&agents_dir)
                .await
                .context("Failed to create agents directory")?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/workspace/mod.rs:load_workspace_info (lines 131-142) ---[0m
    async fn load_workspace_info(&self, agent_id: &str) -> Result<WorkspaceInfo> {
        let workspace_path = self.base_path.join("agents").join(agent_id);
        let info_file = workspace_path.join(".workspace_info.json");

        let content = fs::read_to_string(&info_file)
            .await
            .context("Failed to read workspace info")?;
        let info: WorkspaceInfo =
            serde_json::from_str(&content).context("Failed to parse workspace info")?;

        Ok(info)
    }

  ./crates/ccswarm/src/workspace/mod.rs:29-46 method init_if_needed <-> ./crates/ccswarm/src/workspace/mod.rs:107-118 method remove_workspace
  Similarity: 72.50%
  Classes: SimpleWorkspaceManager <-> SimpleWorkspaceManager

[36m--- ./crates/ccswarm/src/workspace/mod.rs:init_if_needed (lines 29-46) ---[0m
    pub async fn init_if_needed(&self) -> Result<()> {
        if !self.base_path.exists() {
            info!("Creating workspace directory: {}", self.base_path.display());
            fs::create_dir_all(&self.base_path)
                .await
                .context("Failed to create workspace directory")?;
        }

        // agents ディレクトリを作成
        let agents_dir = self.base_path.join("agents");
        if !agents_dir.exists() {
            fs::create_dir_all(&agents_dir)
                .await
                .context("Failed to create agents directory")?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/workspace/mod.rs:remove_workspace (lines 107-118) ---[0m
    pub async fn remove_workspace(&self, agent_id: &str) -> Result<()> {
        let workspace_path = self.base_path.join("agents").join(agent_id);

        if workspace_path.exists() {
            fs::remove_dir_all(&workspace_path)
                .await
                .context("Failed to remove workspace")?;
            info!("Removed workspace: {}", workspace_path.display());
        }

        Ok(())
    }

  ./crates/ccswarm/src/workspace/mod.rs:29-46 method init_if_needed <-> ./crates/ccswarm/src/workspace/mod.rs:121-128 method save_workspace_info
  Similarity: 80.74%
  Classes: SimpleWorkspaceManager <-> SimpleWorkspaceManager

[36m--- ./crates/ccswarm/src/workspace/mod.rs:init_if_needed (lines 29-46) ---[0m
    pub async fn init_if_needed(&self) -> Result<()> {
        if !self.base_path.exists() {
            info!("Creating workspace directory: {}", self.base_path.display());
            fs::create_dir_all(&self.base_path)
                .await
                .context("Failed to create workspace directory")?;
        }

        // agents ディレクトリを作成
        let agents_dir = self.base_path.join("agents");
        if !agents_dir.exists() {
            fs::create_dir_all(&agents_dir)
                .await
                .context("Failed to create agents directory")?;
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/workspace/mod.rs:save_workspace_info (lines 121-128) ---[0m
    async fn save_workspace_info(&self, info: &WorkspaceInfo) -> Result<()> {
        let info_file = info.path.join(".workspace_info.json");
        let content = serde_json::to_string_pretty(info)?;
        fs::write(&info_file, content)
            .await
            .context("Failed to save workspace info")?;
        Ok(())
    }

  ./crates/ccswarm/src/workspace/mod.rs:131-142 method load_workspace_info <-> ./crates/ccswarm/src/workspace/mod.rs:145-155 method setup_claude_config
  Similarity: 87.65%
  Classes: SimpleWorkspaceManager <-> SimpleWorkspaceManager

[36m--- ./crates/ccswarm/src/workspace/mod.rs:load_workspace_info (lines 131-142) ---[0m
    async fn load_workspace_info(&self, agent_id: &str) -> Result<WorkspaceInfo> {
        let workspace_path = self.base_path.join("agents").join(agent_id);
        let info_file = workspace_path.join(".workspace_info.json");

        let content = fs::read_to_string(&info_file)
            .await
            .context("Failed to read workspace info")?;
        let info: WorkspaceInfo =
            serde_json::from_str(&content).context("Failed to parse workspace info")?;

        Ok(info)
    }

[36m--- ./crates/ccswarm/src/workspace/mod.rs:setup_claude_config (lines 145-155) ---[0m
    pub async fn setup_claude_config(&self, agent_id: &str, claude_md_content: &str) -> Result<()> {
        let workspace_path = self.base_path.join("agents").join(agent_id);
        let claude_md_path = workspace_path.join("CLAUDE.md");

        fs::write(&claude_md_path, claude_md_content)
            .await
            .context("Failed to write CLAUDE.md")?;

        info!("CLAUDE.md created for agent: {}", agent_id);
        Ok(())
    }

  ./crates/ccswarm/src/workspace/mod.rs:164-173 function test_workspace_creation <-> ./crates/ccswarm/src/workspace/mod.rs:176-186 function test_workspace_listing
  Similarity: 80.64%

[36m--- ./crates/ccswarm/src/workspace/mod.rs:test_workspace_creation (lines 164-173) ---[0m
    async fn test_workspace_creation() {
        let temp_dir = TempDir::new().unwrap();
        let manager = SimpleWorkspaceManager::new(temp_dir.path().to_path_buf());

        manager.init_if_needed().await.unwrap();

        let workspace = manager.create_workspace("test-agent").await.unwrap();
        assert_eq!(workspace.agent_id, "test-agent");
        assert!(workspace.path.exists());
    }

[36m--- ./crates/ccswarm/src/workspace/mod.rs:test_workspace_listing (lines 176-186) ---[0m
    async fn test_workspace_listing() {
        let temp_dir = TempDir::new().unwrap();
        let manager = SimpleWorkspaceManager::new(temp_dir.path().to_path_buf());

        manager.init_if_needed().await.unwrap();
        manager.create_workspace("agent1").await.unwrap();
        manager.create_workspace("agent2").await.unwrap();

        let workspaces = manager.list_workspaces().await.unwrap();
        assert_eq!(workspaces.len(), 2);
    }

  ./crates/ccswarm/src/workspace/mod.rs:131-142 method load_workspace_info <-> ./crates/ccswarm/src/workspace/mod.rs:164-173 function test_workspace_creation
  Similarity: 71.25%

[36m--- ./crates/ccswarm/src/workspace/mod.rs:load_workspace_info (lines 131-142) ---[0m
    async fn load_workspace_info(&self, agent_id: &str) -> Result<WorkspaceInfo> {
        let workspace_path = self.base_path.join("agents").join(agent_id);
        let info_file = workspace_path.join(".workspace_info.json");

        let content = fs::read_to_string(&info_file)
            .await
            .context("Failed to read workspace info")?;
        let info: WorkspaceInfo =
            serde_json::from_str(&content).context("Failed to parse workspace info")?;

        Ok(info)
    }

[36m--- ./crates/ccswarm/src/workspace/mod.rs:test_workspace_creation (lines 164-173) ---[0m
    async fn test_workspace_creation() {
        let temp_dir = TempDir::new().unwrap();
        let manager = SimpleWorkspaceManager::new(temp_dir.path().to_path_buf());

        manager.init_if_needed().await.unwrap();

        let workspace = manager.create_workspace("test-agent").await.unwrap();
        assert_eq!(workspace.agent_id, "test-agent");
        assert!(workspace.path.exists());
    }

  ./crates/ccswarm/src/workspace/mod.rs:107-118 method remove_workspace <-> ./crates/ccswarm/src/workspace/mod.rs:121-128 method save_workspace_info
  Similarity: 76.36%
  Classes: SimpleWorkspaceManager <-> SimpleWorkspaceManager

[36m--- ./crates/ccswarm/src/workspace/mod.rs:remove_workspace (lines 107-118) ---[0m
    pub async fn remove_workspace(&self, agent_id: &str) -> Result<()> {
        let workspace_path = self.base_path.join("agents").join(agent_id);

        if workspace_path.exists() {
            fs::remove_dir_all(&workspace_path)
                .await
                .context("Failed to remove workspace")?;
            info!("Removed workspace: {}", workspace_path.display());
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/workspace/mod.rs:save_workspace_info (lines 121-128) ---[0m
    async fn save_workspace_info(&self, info: &WorkspaceInfo) -> Result<()> {
        let info_file = info.path.join(".workspace_info.json");
        let content = serde_json::to_string_pretty(info)?;
        fs::write(&info_file, content)
            .await
            .context("Failed to save workspace info")?;
        Ok(())
    }

  ./crates/ccswarm/src/workspace/mod.rs:121-128 method save_workspace_info <-> ./crates/ccswarm/src/workspace/mod.rs:131-142 method load_workspace_info
  Similarity: 76.35%
  Classes: SimpleWorkspaceManager <-> SimpleWorkspaceManager

[36m--- ./crates/ccswarm/src/workspace/mod.rs:save_workspace_info (lines 121-128) ---[0m
    async fn save_workspace_info(&self, info: &WorkspaceInfo) -> Result<()> {
        let info_file = info.path.join(".workspace_info.json");
        let content = serde_json::to_string_pretty(info)?;
        fs::write(&info_file, content)
            .await
            .context("Failed to save workspace info")?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/workspace/mod.rs:load_workspace_info (lines 131-142) ---[0m
    async fn load_workspace_info(&self, agent_id: &str) -> Result<WorkspaceInfo> {
        let workspace_path = self.base_path.join("agents").join(agent_id);
        let info_file = workspace_path.join(".workspace_info.json");

        let content = fs::read_to_string(&info_file)
            .await
            .context("Failed to read workspace info")?;
        let info: WorkspaceInfo =
            serde_json::from_str(&content).context("Failed to parse workspace info")?;

        Ok(info)
    }

  ./crates/ccswarm/src/workspace/mod.rs:121-128 method save_workspace_info <-> ./crates/ccswarm/src/workspace/mod.rs:145-155 method setup_claude_config
  Similarity: 80.00%
  Classes: SimpleWorkspaceManager <-> SimpleWorkspaceManager

[36m--- ./crates/ccswarm/src/workspace/mod.rs:save_workspace_info (lines 121-128) ---[0m
    async fn save_workspace_info(&self, info: &WorkspaceInfo) -> Result<()> {
        let info_file = info.path.join(".workspace_info.json");
        let content = serde_json::to_string_pretty(info)?;
        fs::write(&info_file, content)
            .await
            .context("Failed to save workspace info")?;
        Ok(())
    }

[36m--- ./crates/ccswarm/src/workspace/mod.rs:setup_claude_config (lines 145-155) ---[0m
    pub async fn setup_claude_config(&self, agent_id: &str, claude_md_content: &str) -> Result<()> {
        let workspace_path = self.base_path.join("agents").join(agent_id);
        let claude_md_path = workspace_path.join("CLAUDE.md");

        fs::write(&claude_md_path, claude_md_content)
            .await
            .context("Failed to write CLAUDE.md")?;

        info!("CLAUDE.md created for agent: {}", agent_id);
        Ok(())
    }


Duplicates in ./crates/ccswarm/src/identity/mod.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/identity/mod.rs:334-358 function default_frontend_role <-> ./crates/ccswarm/src/identity/mod.rs:360-383 function default_backend_role
  Similarity: 96.30%

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_frontend_role (lines 334-358) ---[0m
pub fn default_frontend_role() -> AgentRole {
    AgentRole::Frontend {
        technologies: vec![
            "React".to_string(),
            "TypeScript".to_string(),
            "Tailwind CSS".to_string(),
            "Jest".to_string(),
            "Vite".to_string(),
        ],
        responsibilities: vec![
            "UI Component Development".to_string(),
            "State Management".to_string(),
            "Frontend Testing".to_string(),
            "User Experience".to_string(),
            "Accessibility".to_string(),
        ],
        boundaries: vec![
            "No backend API development".to_string(),
            "No database operations".to_string(),
            "No server-side logic".to_string(),
            "No infrastructure changes".to_string(),
            "No deployment scripts".to_string(),
        ],
    }
}

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_backend_role (lines 360-383) ---[0m
pub fn default_backend_role() -> AgentRole {
    AgentRole::Backend {
        technologies: vec![
            "Node.js".to_string(),
            "TypeScript".to_string(),
            "Express".to_string(),
            "PostgreSQL".to_string(),
            "Prisma".to_string(),
        ],
        responsibilities: vec![
            "API Development".to_string(),
            "Database Design".to_string(),
            "Authentication".to_string(),
            "Business Logic".to_string(),
            "Data Validation".to_string(),
        ],
        boundaries: vec![
            "No frontend UI code".to_string(),
            "No CSS styling".to_string(),
            "No infrastructure provisioning".to_string(),
            "No deployment automation".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:334-358 function default_frontend_role <-> ./crates/ccswarm/src/identity/mod.rs:385-408 function default_devops_role
  Similarity: 96.14%

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_frontend_role (lines 334-358) ---[0m
pub fn default_frontend_role() -> AgentRole {
    AgentRole::Frontend {
        technologies: vec![
            "React".to_string(),
            "TypeScript".to_string(),
            "Tailwind CSS".to_string(),
            "Jest".to_string(),
            "Vite".to_string(),
        ],
        responsibilities: vec![
            "UI Component Development".to_string(),
            "State Management".to_string(),
            "Frontend Testing".to_string(),
            "User Experience".to_string(),
            "Accessibility".to_string(),
        ],
        boundaries: vec![
            "No backend API development".to_string(),
            "No database operations".to_string(),
            "No server-side logic".to_string(),
            "No infrastructure changes".to_string(),
            "No deployment scripts".to_string(),
        ],
    }
}

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_devops_role (lines 385-408) ---[0m
pub fn default_devops_role() -> AgentRole {
    AgentRole::DevOps {
        technologies: vec![
            "Docker".to_string(),
            "Kubernetes".to_string(),
            "Terraform".to_string(),
            "AWS".to_string(),
            "GitHub Actions".to_string(),
        ],
        responsibilities: vec![
            "Infrastructure Provisioning".to_string(),
            "CI/CD Pipelines".to_string(),
            "Monitoring Setup".to_string(),
            "Security Configuration".to_string(),
            "Deployment Automation".to_string(),
        ],
        boundaries: vec![
            "No application code changes".to_string(),
            "No business logic implementation".to_string(),
            "No UI development".to_string(),
            "No database schema design".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:334-358 function default_frontend_role <-> ./crates/ccswarm/src/identity/mod.rs:410-433 function default_qa_role
  Similarity: 96.14%

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_frontend_role (lines 334-358) ---[0m
pub fn default_frontend_role() -> AgentRole {
    AgentRole::Frontend {
        technologies: vec![
            "React".to_string(),
            "TypeScript".to_string(),
            "Tailwind CSS".to_string(),
            "Jest".to_string(),
            "Vite".to_string(),
        ],
        responsibilities: vec![
            "UI Component Development".to_string(),
            "State Management".to_string(),
            "Frontend Testing".to_string(),
            "User Experience".to_string(),
            "Accessibility".to_string(),
        ],
        boundaries: vec![
            "No backend API development".to_string(),
            "No database operations".to_string(),
            "No server-side logic".to_string(),
            "No infrastructure changes".to_string(),
            "No deployment scripts".to_string(),
        ],
    }
}

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_qa_role (lines 410-433) ---[0m
pub fn default_qa_role() -> AgentRole {
    AgentRole::QA {
        technologies: vec![
            "Jest".to_string(),
            "Cypress".to_string(),
            "Playwright".to_string(),
            "Postman".to_string(),
            "K6".to_string(),
        ],
        responsibilities: vec![
            "Test Strategy".to_string(),
            "Test Implementation".to_string(),
            "Quality Assurance".to_string(),
            "Performance Testing".to_string(),
            "Security Testing".to_string(),
        ],
        boundaries: vec![
            "No production code changes".to_string(),
            "No feature implementation".to_string(),
            "No infrastructure changes".to_string(),
            "No deployment execution".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:334-358 function default_frontend_role <-> ./crates/ccswarm/src/identity/mod.rs:435-458 function default_search_role
  Similarity: 96.14%

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_frontend_role (lines 334-358) ---[0m
pub fn default_frontend_role() -> AgentRole {
    AgentRole::Frontend {
        technologies: vec![
            "React".to_string(),
            "TypeScript".to_string(),
            "Tailwind CSS".to_string(),
            "Jest".to_string(),
            "Vite".to_string(),
        ],
        responsibilities: vec![
            "UI Component Development".to_string(),
            "State Management".to_string(),
            "Frontend Testing".to_string(),
            "User Experience".to_string(),
            "Accessibility".to_string(),
        ],
        boundaries: vec![
            "No backend API development".to_string(),
            "No database operations".to_string(),
            "No server-side logic".to_string(),
            "No infrastructure changes".to_string(),
            "No deployment scripts".to_string(),
        ],
    }
}

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_search_role (lines 435-458) ---[0m
pub fn default_search_role() -> AgentRole {
    AgentRole::Search {
        technologies: vec![
            "Gemini CLI".to_string(),
            "Web Search".to_string(),
            "Information Retrieval".to_string(),
            "Search APIs".to_string(),
        ],
        responsibilities: vec![
            "Web Search".to_string(),
            "Information Gathering".to_string(),
            "Result Filtering".to_string(),
            "Query Optimization".to_string(),
            "Knowledge Discovery".to_string(),
        ],
        boundaries: vec![
            "No code implementation".to_string(),
            "No direct file modifications".to_string(),
            "Read-only information gathering".to_string(),
            "No execution of found code".to_string(),
            "No decision making beyond search".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:360-383 function default_backend_role <-> ./crates/ccswarm/src/identity/mod.rs:385-408 function default_devops_role
  Similarity: 97.49%

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_backend_role (lines 360-383) ---[0m
pub fn default_backend_role() -> AgentRole {
    AgentRole::Backend {
        technologies: vec![
            "Node.js".to_string(),
            "TypeScript".to_string(),
            "Express".to_string(),
            "PostgreSQL".to_string(),
            "Prisma".to_string(),
        ],
        responsibilities: vec![
            "API Development".to_string(),
            "Database Design".to_string(),
            "Authentication".to_string(),
            "Business Logic".to_string(),
            "Data Validation".to_string(),
        ],
        boundaries: vec![
            "No frontend UI code".to_string(),
            "No CSS styling".to_string(),
            "No infrastructure provisioning".to_string(),
            "No deployment automation".to_string(),
        ],
    }
}

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_devops_role (lines 385-408) ---[0m
pub fn default_devops_role() -> AgentRole {
    AgentRole::DevOps {
        technologies: vec![
            "Docker".to_string(),
            "Kubernetes".to_string(),
            "Terraform".to_string(),
            "AWS".to_string(),
            "GitHub Actions".to_string(),
        ],
        responsibilities: vec![
            "Infrastructure Provisioning".to_string(),
            "CI/CD Pipelines".to_string(),
            "Monitoring Setup".to_string(),
            "Security Configuration".to_string(),
            "Deployment Automation".to_string(),
        ],
        boundaries: vec![
            "No application code changes".to_string(),
            "No business logic implementation".to_string(),
            "No UI development".to_string(),
            "No database schema design".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:360-383 function default_backend_role <-> ./crates/ccswarm/src/identity/mod.rs:410-433 function default_qa_role
  Similarity: 97.49%

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_backend_role (lines 360-383) ---[0m
pub fn default_backend_role() -> AgentRole {
    AgentRole::Backend {
        technologies: vec![
            "Node.js".to_string(),
            "TypeScript".to_string(),
            "Express".to_string(),
            "PostgreSQL".to_string(),
            "Prisma".to_string(),
        ],
        responsibilities: vec![
            "API Development".to_string(),
            "Database Design".to_string(),
            "Authentication".to_string(),
            "Business Logic".to_string(),
            "Data Validation".to_string(),
        ],
        boundaries: vec![
            "No frontend UI code".to_string(),
            "No CSS styling".to_string(),
            "No infrastructure provisioning".to_string(),
            "No deployment automation".to_string(),
        ],
    }
}

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_qa_role (lines 410-433) ---[0m
pub fn default_qa_role() -> AgentRole {
    AgentRole::QA {
        technologies: vec![
            "Jest".to_string(),
            "Cypress".to_string(),
            "Playwright".to_string(),
            "Postman".to_string(),
            "K6".to_string(),
        ],
        responsibilities: vec![
            "Test Strategy".to_string(),
            "Test Implementation".to_string(),
            "Quality Assurance".to_string(),
            "Performance Testing".to_string(),
            "Security Testing".to_string(),
        ],
        boundaries: vec![
            "No production code changes".to_string(),
            "No feature implementation".to_string(),
            "No infrastructure changes".to_string(),
            "No deployment execution".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:385-408 function default_devops_role <-> ./crates/ccswarm/src/identity/mod.rs:410-433 function default_qa_role
  Similarity: 97.49%

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_devops_role (lines 385-408) ---[0m
pub fn default_devops_role() -> AgentRole {
    AgentRole::DevOps {
        technologies: vec![
            "Docker".to_string(),
            "Kubernetes".to_string(),
            "Terraform".to_string(),
            "AWS".to_string(),
            "GitHub Actions".to_string(),
        ],
        responsibilities: vec![
            "Infrastructure Provisioning".to_string(),
            "CI/CD Pipelines".to_string(),
            "Monitoring Setup".to_string(),
            "Security Configuration".to_string(),
            "Deployment Automation".to_string(),
        ],
        boundaries: vec![
            "No application code changes".to_string(),
            "No business logic implementation".to_string(),
            "No UI development".to_string(),
            "No database schema design".to_string(),
        ],
    }
}

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_qa_role (lines 410-433) ---[0m
pub fn default_qa_role() -> AgentRole {
    AgentRole::QA {
        technologies: vec![
            "Jest".to_string(),
            "Cypress".to_string(),
            "Playwright".to_string(),
            "Postman".to_string(),
            "K6".to_string(),
        ],
        responsibilities: vec![
            "Test Strategy".to_string(),
            "Test Implementation".to_string(),
            "Quality Assurance".to_string(),
            "Performance Testing".to_string(),
            "Security Testing".to_string(),
        ],
        boundaries: vec![
            "No production code changes".to_string(),
            "No feature implementation".to_string(),
            "No infrastructure changes".to_string(),
            "No deployment execution".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:360-383 function default_backend_role <-> ./crates/ccswarm/src/identity/mod.rs:435-458 function default_search_role
  Similarity: 95.36%

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_backend_role (lines 360-383) ---[0m
pub fn default_backend_role() -> AgentRole {
    AgentRole::Backend {
        technologies: vec![
            "Node.js".to_string(),
            "TypeScript".to_string(),
            "Express".to_string(),
            "PostgreSQL".to_string(),
            "Prisma".to_string(),
        ],
        responsibilities: vec![
            "API Development".to_string(),
            "Database Design".to_string(),
            "Authentication".to_string(),
            "Business Logic".to_string(),
            "Data Validation".to_string(),
        ],
        boundaries: vec![
            "No frontend UI code".to_string(),
            "No CSS styling".to_string(),
            "No infrastructure provisioning".to_string(),
            "No deployment automation".to_string(),
        ],
    }
}

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_search_role (lines 435-458) ---[0m
pub fn default_search_role() -> AgentRole {
    AgentRole::Search {
        technologies: vec![
            "Gemini CLI".to_string(),
            "Web Search".to_string(),
            "Information Retrieval".to_string(),
            "Search APIs".to_string(),
        ],
        responsibilities: vec![
            "Web Search".to_string(),
            "Information Gathering".to_string(),
            "Result Filtering".to_string(),
            "Query Optimization".to_string(),
            "Knowledge Discovery".to_string(),
        ],
        boundaries: vec![
            "No code implementation".to_string(),
            "No direct file modifications".to_string(),
            "Read-only information gathering".to_string(),
            "No execution of found code".to_string(),
            "No decision making beyond search".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:385-408 function default_devops_role <-> ./crates/ccswarm/src/identity/mod.rs:435-458 function default_search_role
  Similarity: 95.36%

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_devops_role (lines 385-408) ---[0m
pub fn default_devops_role() -> AgentRole {
    AgentRole::DevOps {
        technologies: vec![
            "Docker".to_string(),
            "Kubernetes".to_string(),
            "Terraform".to_string(),
            "AWS".to_string(),
            "GitHub Actions".to_string(),
        ],
        responsibilities: vec![
            "Infrastructure Provisioning".to_string(),
            "CI/CD Pipelines".to_string(),
            "Monitoring Setup".to_string(),
            "Security Configuration".to_string(),
            "Deployment Automation".to_string(),
        ],
        boundaries: vec![
            "No application code changes".to_string(),
            "No business logic implementation".to_string(),
            "No UI development".to_string(),
            "No database schema design".to_string(),
        ],
    }
}

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_search_role (lines 435-458) ---[0m
pub fn default_search_role() -> AgentRole {
    AgentRole::Search {
        technologies: vec![
            "Gemini CLI".to_string(),
            "Web Search".to_string(),
            "Information Retrieval".to_string(),
            "Search APIs".to_string(),
        ],
        responsibilities: vec![
            "Web Search".to_string(),
            "Information Gathering".to_string(),
            "Result Filtering".to_string(),
            "Query Optimization".to_string(),
            "Knowledge Discovery".to_string(),
        ],
        boundaries: vec![
            "No code implementation".to_string(),
            "No direct file modifications".to_string(),
            "Read-only information gathering".to_string(),
            "No execution of found code".to_string(),
            "No decision making beyond search".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:410-433 function default_qa_role <-> ./crates/ccswarm/src/identity/mod.rs:435-458 function default_search_role
  Similarity: 95.36%

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_qa_role (lines 410-433) ---[0m
pub fn default_qa_role() -> AgentRole {
    AgentRole::QA {
        technologies: vec![
            "Jest".to_string(),
            "Cypress".to_string(),
            "Playwright".to_string(),
            "Postman".to_string(),
            "K6".to_string(),
        ],
        responsibilities: vec![
            "Test Strategy".to_string(),
            "Test Implementation".to_string(),
            "Quality Assurance".to_string(),
            "Performance Testing".to_string(),
            "Security Testing".to_string(),
        ],
        boundaries: vec![
            "No production code changes".to_string(),
            "No feature implementation".to_string(),
            "No infrastructure changes".to_string(),
            "No deployment execution".to_string(),
        ],
    }
}

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_search_role (lines 435-458) ---[0m
pub fn default_search_role() -> AgentRole {
    AgentRole::Search {
        technologies: vec![
            "Gemini CLI".to_string(),
            "Web Search".to_string(),
            "Information Retrieval".to_string(),
            "Search APIs".to_string(),
        ],
        responsibilities: vec![
            "Web Search".to_string(),
            "Information Gathering".to_string(),
            "Result Filtering".to_string(),
            "Query Optimization".to_string(),
            "Knowledge Discovery".to_string(),
        ],
        boundaries: vec![
            "No code implementation".to_string(),
            "No direct file modifications".to_string(),
            "Read-only information gathering".to_string(),
            "No execution of found code".to_string(),
            "No decision making beyond search".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:102-123 method responsibilities <-> ./crates/ccswarm/src/identity/mod.rs:334-358 function default_frontend_role
  Similarity: 91.59%

[36m--- ./crates/ccswarm/src/identity/mod.rs:responsibilities (lines 102-123) ---[0m
    pub fn responsibilities(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend {
                responsibilities, ..
            }
            | AgentRole::Backend {
                responsibilities, ..
            }
            | AgentRole::DevOps {
                responsibilities, ..
            }
            | AgentRole::QA {
                responsibilities, ..
            }
            | AgentRole::Search {
                responsibilities, ..
            } => responsibilities.clone(),
            AgentRole::Master {
                oversight_roles, ..
            } => oversight_roles.clone(),
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_frontend_role (lines 334-358) ---[0m
pub fn default_frontend_role() -> AgentRole {
    AgentRole::Frontend {
        technologies: vec![
            "React".to_string(),
            "TypeScript".to_string(),
            "Tailwind CSS".to_string(),
            "Jest".to_string(),
            "Vite".to_string(),
        ],
        responsibilities: vec![
            "UI Component Development".to_string(),
            "State Management".to_string(),
            "Frontend Testing".to_string(),
            "User Experience".to_string(),
            "Accessibility".to_string(),
        ],
        boundaries: vec![
            "No backend API development".to_string(),
            "No database operations".to_string(),
            "No server-side logic".to_string(),
            "No infrastructure changes".to_string(),
            "No deployment scripts".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:102-123 method responsibilities <-> ./crates/ccswarm/src/identity/mod.rs:360-383 function default_backend_role
  Similarity: 91.12%

[36m--- ./crates/ccswarm/src/identity/mod.rs:responsibilities (lines 102-123) ---[0m
    pub fn responsibilities(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend {
                responsibilities, ..
            }
            | AgentRole::Backend {
                responsibilities, ..
            }
            | AgentRole::DevOps {
                responsibilities, ..
            }
            | AgentRole::QA {
                responsibilities, ..
            }
            | AgentRole::Search {
                responsibilities, ..
            } => responsibilities.clone(),
            AgentRole::Master {
                oversight_roles, ..
            } => oversight_roles.clone(),
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_backend_role (lines 360-383) ---[0m
pub fn default_backend_role() -> AgentRole {
    AgentRole::Backend {
        technologies: vec![
            "Node.js".to_string(),
            "TypeScript".to_string(),
            "Express".to_string(),
            "PostgreSQL".to_string(),
            "Prisma".to_string(),
        ],
        responsibilities: vec![
            "API Development".to_string(),
            "Database Design".to_string(),
            "Authentication".to_string(),
            "Business Logic".to_string(),
            "Data Validation".to_string(),
        ],
        boundaries: vec![
            "No frontend UI code".to_string(),
            "No CSS styling".to_string(),
            "No infrastructure provisioning".to_string(),
            "No deployment automation".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:102-123 method responsibilities <-> ./crates/ccswarm/src/identity/mod.rs:385-408 function default_devops_role
  Similarity: 91.12%

[36m--- ./crates/ccswarm/src/identity/mod.rs:responsibilities (lines 102-123) ---[0m
    pub fn responsibilities(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend {
                responsibilities, ..
            }
            | AgentRole::Backend {
                responsibilities, ..
            }
            | AgentRole::DevOps {
                responsibilities, ..
            }
            | AgentRole::QA {
                responsibilities, ..
            }
            | AgentRole::Search {
                responsibilities, ..
            } => responsibilities.clone(),
            AgentRole::Master {
                oversight_roles, ..
            } => oversight_roles.clone(),
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_devops_role (lines 385-408) ---[0m
pub fn default_devops_role() -> AgentRole {
    AgentRole::DevOps {
        technologies: vec![
            "Docker".to_string(),
            "Kubernetes".to_string(),
            "Terraform".to_string(),
            "AWS".to_string(),
            "GitHub Actions".to_string(),
        ],
        responsibilities: vec![
            "Infrastructure Provisioning".to_string(),
            "CI/CD Pipelines".to_string(),
            "Monitoring Setup".to_string(),
            "Security Configuration".to_string(),
            "Deployment Automation".to_string(),
        ],
        boundaries: vec![
            "No application code changes".to_string(),
            "No business logic implementation".to_string(),
            "No UI development".to_string(),
            "No database schema design".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:102-123 method responsibilities <-> ./crates/ccswarm/src/identity/mod.rs:410-433 function default_qa_role
  Similarity: 91.12%

[36m--- ./crates/ccswarm/src/identity/mod.rs:responsibilities (lines 102-123) ---[0m
    pub fn responsibilities(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend {
                responsibilities, ..
            }
            | AgentRole::Backend {
                responsibilities, ..
            }
            | AgentRole::DevOps {
                responsibilities, ..
            }
            | AgentRole::QA {
                responsibilities, ..
            }
            | AgentRole::Search {
                responsibilities, ..
            } => responsibilities.clone(),
            AgentRole::Master {
                oversight_roles, ..
            } => oversight_roles.clone(),
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_qa_role (lines 410-433) ---[0m
pub fn default_qa_role() -> AgentRole {
    AgentRole::QA {
        technologies: vec![
            "Jest".to_string(),
            "Cypress".to_string(),
            "Playwright".to_string(),
            "Postman".to_string(),
            "K6".to_string(),
        ],
        responsibilities: vec![
            "Test Strategy".to_string(),
            "Test Implementation".to_string(),
            "Quality Assurance".to_string(),
            "Performance Testing".to_string(),
            "Security Testing".to_string(),
        ],
        boundaries: vec![
            "No production code changes".to_string(),
            "No feature implementation".to_string(),
            "No infrastructure changes".to_string(),
            "No deployment execution".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:102-123 method responsibilities <-> ./crates/ccswarm/src/identity/mod.rs:435-458 function default_search_role
  Similarity: 91.12%

[36m--- ./crates/ccswarm/src/identity/mod.rs:responsibilities (lines 102-123) ---[0m
    pub fn responsibilities(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend {
                responsibilities, ..
            }
            | AgentRole::Backend {
                responsibilities, ..
            }
            | AgentRole::DevOps {
                responsibilities, ..
            }
            | AgentRole::QA {
                responsibilities, ..
            }
            | AgentRole::Search {
                responsibilities, ..
            } => responsibilities.clone(),
            AgentRole::Master {
                oversight_roles, ..
            } => oversight_roles.clone(),
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_search_role (lines 435-458) ---[0m
pub fn default_search_role() -> AgentRole {
    AgentRole::Search {
        technologies: vec![
            "Gemini CLI".to_string(),
            "Web Search".to_string(),
            "Information Retrieval".to_string(),
            "Search APIs".to_string(),
        ],
        responsibilities: vec![
            "Web Search".to_string(),
            "Information Gathering".to_string(),
            "Result Filtering".to_string(),
            "Query Optimization".to_string(),
            "Knowledge Discovery".to_string(),
        ],
        boundaries: vec![
            "No code implementation".to_string(),
            "No direct file modifications".to_string(),
            "Read-only information gathering".to_string(),
            "No execution of found code".to_string(),
            "No decision making beyond search".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:200-224 method monitor_response <-> ./crates/ccswarm/src/identity/mod.rs:307-330 method parse_identity
  Similarity: 71.07%
  Classes: IdentityMonitor <-> ResponseParser

[36m--- ./crates/ccswarm/src/identity/mod.rs:monitor_response (lines 200-224) ---[0m
    pub async fn monitor_response(&mut self, response: &str) -> Result<IdentityStatus> {
        // Check for identity header
        let has_identity_header = self.check_identity_header(response);

        // Check boundary compliance
        let boundary_compliance = self.check_boundary_compliance(response);

        // Check delegation behavior
        let _delegation_behavior = self.check_delegation_behavior(response);

        if !has_identity_header {
            return Ok(IdentityStatus::DriftDetected(
                "Missing identity header".to_string(),
            ));
        }

        if !boundary_compliance {
            return Ok(IdentityStatus::BoundaryViolation(
                "Response indicates work outside specialization".to_string(),
            ));
        }

        self.last_identity_check = Instant::now();
        Ok(IdentityStatus::Healthy)
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:parse_identity (lines 307-330) ---[0m
    pub fn parse_identity(&self, response: &str) -> Option<(String, String, String)> {
        let agent = self
            .identity_regex
            .captures(response)
            .and_then(|cap| cap.get(1))
            .map(|m| m.as_str().to_string());

        let workspace = self
            .workspace_regex
            .captures(response)
            .and_then(|cap| cap.get(1))
            .map(|m| m.as_str().to_string());

        let scope = self
            .scope_regex
            .captures(response)
            .and_then(|cap| cap.get(1))
            .map(|m| m.as_str().to_string());

        match (agent, workspace, scope) {
            (Some(a), Some(w), Some(s)) => Some((a, w, s)),
            _ => None,
        }
    }

  ./crates/ccswarm/src/identity/mod.rs:90-99 method technologies <-> ./crates/ccswarm/src/identity/mod.rs:334-358 function default_frontend_role
  Similarity: 91.59%

[36m--- ./crates/ccswarm/src/identity/mod.rs:technologies (lines 90-99) ---[0m
    pub fn technologies(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend { technologies, .. }
            | AgentRole::Backend { technologies, .. }
            | AgentRole::DevOps { technologies, .. }
            | AgentRole::QA { technologies, .. }
            | AgentRole::Search { technologies, .. } => technologies.clone(),
            AgentRole::Master { .. } => vec!["Orchestration".to_string()],
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_frontend_role (lines 334-358) ---[0m
pub fn default_frontend_role() -> AgentRole {
    AgentRole::Frontend {
        technologies: vec![
            "React".to_string(),
            "TypeScript".to_string(),
            "Tailwind CSS".to_string(),
            "Jest".to_string(),
            "Vite".to_string(),
        ],
        responsibilities: vec![
            "UI Component Development".to_string(),
            "State Management".to_string(),
            "Frontend Testing".to_string(),
            "User Experience".to_string(),
            "Accessibility".to_string(),
        ],
        boundaries: vec![
            "No backend API development".to_string(),
            "No database operations".to_string(),
            "No server-side logic".to_string(),
            "No infrastructure changes".to_string(),
            "No deployment scripts".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:126-135 method boundaries <-> ./crates/ccswarm/src/identity/mod.rs:334-358 function default_frontend_role
  Similarity: 91.59%

[36m--- ./crates/ccswarm/src/identity/mod.rs:boundaries (lines 126-135) ---[0m
    pub fn boundaries(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend { boundaries, .. }
            | AgentRole::Backend { boundaries, .. }
            | AgentRole::DevOps { boundaries, .. }
            | AgentRole::QA { boundaries, .. }
            | AgentRole::Search { boundaries, .. } => boundaries.clone(),
            AgentRole::Master { .. } => vec!["No direct code implementation".to_string()],
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_frontend_role (lines 334-358) ---[0m
pub fn default_frontend_role() -> AgentRole {
    AgentRole::Frontend {
        technologies: vec![
            "React".to_string(),
            "TypeScript".to_string(),
            "Tailwind CSS".to_string(),
            "Jest".to_string(),
            "Vite".to_string(),
        ],
        responsibilities: vec![
            "UI Component Development".to_string(),
            "State Management".to_string(),
            "Frontend Testing".to_string(),
            "User Experience".to_string(),
            "Accessibility".to_string(),
        ],
        boundaries: vec![
            "No backend API development".to_string(),
            "No database operations".to_string(),
            "No server-side logic".to_string(),
            "No infrastructure changes".to_string(),
            "No deployment scripts".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:90-99 method technologies <-> ./crates/ccswarm/src/identity/mod.rs:360-383 function default_backend_role
  Similarity: 91.12%

[36m--- ./crates/ccswarm/src/identity/mod.rs:technologies (lines 90-99) ---[0m
    pub fn technologies(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend { technologies, .. }
            | AgentRole::Backend { technologies, .. }
            | AgentRole::DevOps { technologies, .. }
            | AgentRole::QA { technologies, .. }
            | AgentRole::Search { technologies, .. } => technologies.clone(),
            AgentRole::Master { .. } => vec!["Orchestration".to_string()],
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_backend_role (lines 360-383) ---[0m
pub fn default_backend_role() -> AgentRole {
    AgentRole::Backend {
        technologies: vec![
            "Node.js".to_string(),
            "TypeScript".to_string(),
            "Express".to_string(),
            "PostgreSQL".to_string(),
            "Prisma".to_string(),
        ],
        responsibilities: vec![
            "API Development".to_string(),
            "Database Design".to_string(),
            "Authentication".to_string(),
            "Business Logic".to_string(),
            "Data Validation".to_string(),
        ],
        boundaries: vec![
            "No frontend UI code".to_string(),
            "No CSS styling".to_string(),
            "No infrastructure provisioning".to_string(),
            "No deployment automation".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:90-99 method technologies <-> ./crates/ccswarm/src/identity/mod.rs:385-408 function default_devops_role
  Similarity: 91.12%

[36m--- ./crates/ccswarm/src/identity/mod.rs:technologies (lines 90-99) ---[0m
    pub fn technologies(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend { technologies, .. }
            | AgentRole::Backend { technologies, .. }
            | AgentRole::DevOps { technologies, .. }
            | AgentRole::QA { technologies, .. }
            | AgentRole::Search { technologies, .. } => technologies.clone(),
            AgentRole::Master { .. } => vec!["Orchestration".to_string()],
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_devops_role (lines 385-408) ---[0m
pub fn default_devops_role() -> AgentRole {
    AgentRole::DevOps {
        technologies: vec![
            "Docker".to_string(),
            "Kubernetes".to_string(),
            "Terraform".to_string(),
            "AWS".to_string(),
            "GitHub Actions".to_string(),
        ],
        responsibilities: vec![
            "Infrastructure Provisioning".to_string(),
            "CI/CD Pipelines".to_string(),
            "Monitoring Setup".to_string(),
            "Security Configuration".to_string(),
            "Deployment Automation".to_string(),
        ],
        boundaries: vec![
            "No application code changes".to_string(),
            "No business logic implementation".to_string(),
            "No UI development".to_string(),
            "No database schema design".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:90-99 method technologies <-> ./crates/ccswarm/src/identity/mod.rs:410-433 function default_qa_role
  Similarity: 91.12%

[36m--- ./crates/ccswarm/src/identity/mod.rs:technologies (lines 90-99) ---[0m
    pub fn technologies(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend { technologies, .. }
            | AgentRole::Backend { technologies, .. }
            | AgentRole::DevOps { technologies, .. }
            | AgentRole::QA { technologies, .. }
            | AgentRole::Search { technologies, .. } => technologies.clone(),
            AgentRole::Master { .. } => vec!["Orchestration".to_string()],
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_qa_role (lines 410-433) ---[0m
pub fn default_qa_role() -> AgentRole {
    AgentRole::QA {
        technologies: vec![
            "Jest".to_string(),
            "Cypress".to_string(),
            "Playwright".to_string(),
            "Postman".to_string(),
            "K6".to_string(),
        ],
        responsibilities: vec![
            "Test Strategy".to_string(),
            "Test Implementation".to_string(),
            "Quality Assurance".to_string(),
            "Performance Testing".to_string(),
            "Security Testing".to_string(),
        ],
        boundaries: vec![
            "No production code changes".to_string(),
            "No feature implementation".to_string(),
            "No infrastructure changes".to_string(),
            "No deployment execution".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:90-99 method technologies <-> ./crates/ccswarm/src/identity/mod.rs:435-458 function default_search_role
  Similarity: 91.12%

[36m--- ./crates/ccswarm/src/identity/mod.rs:technologies (lines 90-99) ---[0m
    pub fn technologies(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend { technologies, .. }
            | AgentRole::Backend { technologies, .. }
            | AgentRole::DevOps { technologies, .. }
            | AgentRole::QA { technologies, .. }
            | AgentRole::Search { technologies, .. } => technologies.clone(),
            AgentRole::Master { .. } => vec!["Orchestration".to_string()],
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_search_role (lines 435-458) ---[0m
pub fn default_search_role() -> AgentRole {
    AgentRole::Search {
        technologies: vec![
            "Gemini CLI".to_string(),
            "Web Search".to_string(),
            "Information Retrieval".to_string(),
            "Search APIs".to_string(),
        ],
        responsibilities: vec![
            "Web Search".to_string(),
            "Information Gathering".to_string(),
            "Result Filtering".to_string(),
            "Query Optimization".to_string(),
            "Knowledge Discovery".to_string(),
        ],
        boundaries: vec![
            "No code implementation".to_string(),
            "No direct file modifications".to_string(),
            "Read-only information gathering".to_string(),
            "No execution of found code".to_string(),
            "No decision making beyond search".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:126-135 method boundaries <-> ./crates/ccswarm/src/identity/mod.rs:360-383 function default_backend_role
  Similarity: 91.12%

[36m--- ./crates/ccswarm/src/identity/mod.rs:boundaries (lines 126-135) ---[0m
    pub fn boundaries(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend { boundaries, .. }
            | AgentRole::Backend { boundaries, .. }
            | AgentRole::DevOps { boundaries, .. }
            | AgentRole::QA { boundaries, .. }
            | AgentRole::Search { boundaries, .. } => boundaries.clone(),
            AgentRole::Master { .. } => vec!["No direct code implementation".to_string()],
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_backend_role (lines 360-383) ---[0m
pub fn default_backend_role() -> AgentRole {
    AgentRole::Backend {
        technologies: vec![
            "Node.js".to_string(),
            "TypeScript".to_string(),
            "Express".to_string(),
            "PostgreSQL".to_string(),
            "Prisma".to_string(),
        ],
        responsibilities: vec![
            "API Development".to_string(),
            "Database Design".to_string(),
            "Authentication".to_string(),
            "Business Logic".to_string(),
            "Data Validation".to_string(),
        ],
        boundaries: vec![
            "No frontend UI code".to_string(),
            "No CSS styling".to_string(),
            "No infrastructure provisioning".to_string(),
            "No deployment automation".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:126-135 method boundaries <-> ./crates/ccswarm/src/identity/mod.rs:385-408 function default_devops_role
  Similarity: 91.12%

[36m--- ./crates/ccswarm/src/identity/mod.rs:boundaries (lines 126-135) ---[0m
    pub fn boundaries(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend { boundaries, .. }
            | AgentRole::Backend { boundaries, .. }
            | AgentRole::DevOps { boundaries, .. }
            | AgentRole::QA { boundaries, .. }
            | AgentRole::Search { boundaries, .. } => boundaries.clone(),
            AgentRole::Master { .. } => vec!["No direct code implementation".to_string()],
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_devops_role (lines 385-408) ---[0m
pub fn default_devops_role() -> AgentRole {
    AgentRole::DevOps {
        technologies: vec![
            "Docker".to_string(),
            "Kubernetes".to_string(),
            "Terraform".to_string(),
            "AWS".to_string(),
            "GitHub Actions".to_string(),
        ],
        responsibilities: vec![
            "Infrastructure Provisioning".to_string(),
            "CI/CD Pipelines".to_string(),
            "Monitoring Setup".to_string(),
            "Security Configuration".to_string(),
            "Deployment Automation".to_string(),
        ],
        boundaries: vec![
            "No application code changes".to_string(),
            "No business logic implementation".to_string(),
            "No UI development".to_string(),
            "No database schema design".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:126-135 method boundaries <-> ./crates/ccswarm/src/identity/mod.rs:410-433 function default_qa_role
  Similarity: 91.12%

[36m--- ./crates/ccswarm/src/identity/mod.rs:boundaries (lines 126-135) ---[0m
    pub fn boundaries(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend { boundaries, .. }
            | AgentRole::Backend { boundaries, .. }
            | AgentRole::DevOps { boundaries, .. }
            | AgentRole::QA { boundaries, .. }
            | AgentRole::Search { boundaries, .. } => boundaries.clone(),
            AgentRole::Master { .. } => vec!["No direct code implementation".to_string()],
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_qa_role (lines 410-433) ---[0m
pub fn default_qa_role() -> AgentRole {
    AgentRole::QA {
        technologies: vec![
            "Jest".to_string(),
            "Cypress".to_string(),
            "Playwright".to_string(),
            "Postman".to_string(),
            "K6".to_string(),
        ],
        responsibilities: vec![
            "Test Strategy".to_string(),
            "Test Implementation".to_string(),
            "Quality Assurance".to_string(),
            "Performance Testing".to_string(),
            "Security Testing".to_string(),
        ],
        boundaries: vec![
            "No production code changes".to_string(),
            "No feature implementation".to_string(),
            "No infrastructure changes".to_string(),
            "No deployment execution".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:126-135 method boundaries <-> ./crates/ccswarm/src/identity/mod.rs:435-458 function default_search_role
  Similarity: 91.12%

[36m--- ./crates/ccswarm/src/identity/mod.rs:boundaries (lines 126-135) ---[0m
    pub fn boundaries(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend { boundaries, .. }
            | AgentRole::Backend { boundaries, .. }
            | AgentRole::DevOps { boundaries, .. }
            | AgentRole::QA { boundaries, .. }
            | AgentRole::Search { boundaries, .. } => boundaries.clone(),
            AgentRole::Master { .. } => vec!["No direct code implementation".to_string()],
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:default_search_role (lines 435-458) ---[0m
pub fn default_search_role() -> AgentRole {
    AgentRole::Search {
        technologies: vec![
            "Gemini CLI".to_string(),
            "Web Search".to_string(),
            "Information Retrieval".to_string(),
            "Search APIs".to_string(),
        ],
        responsibilities: vec![
            "Web Search".to_string(),
            "Information Gathering".to_string(),
            "Result Filtering".to_string(),
            "Query Optimization".to_string(),
            "Knowledge Discovery".to_string(),
        ],
        boundaries: vec![
            "No code implementation".to_string(),
            "No direct file modifications".to_string(),
            "Read-only information gathering".to_string(),
            "No execution of found code".to_string(),
            "No decision making beyond search".to_string(),
        ],
    }
}

  ./crates/ccswarm/src/identity/mod.rs:200-224 method monitor_response <-> ./crates/ccswarm/src/identity/mod.rs:511-528 function test_response_parser
  Similarity: 71.80%

[36m--- ./crates/ccswarm/src/identity/mod.rs:monitor_response (lines 200-224) ---[0m
    pub async fn monitor_response(&mut self, response: &str) -> Result<IdentityStatus> {
        // Check for identity header
        let has_identity_header = self.check_identity_header(response);

        // Check boundary compliance
        let boundary_compliance = self.check_boundary_compliance(response);

        // Check delegation behavior
        let _delegation_behavior = self.check_delegation_behavior(response);

        if !has_identity_header {
            return Ok(IdentityStatus::DriftDetected(
                "Missing identity header".to_string(),
            ));
        }

        if !boundary_compliance {
            return Ok(IdentityStatus::BoundaryViolation(
                "Response indicates work outside specialization".to_string(),
            ));
        }

        self.last_identity_check = Instant::now();
        Ok(IdentityStatus::Healthy)
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:test_response_parser (lines 511-528) ---[0m
    fn test_response_parser() {
        let parser = ResponseParser::new();
        let response = r#"
🤖 AGENT: Frontend
📁 WORKSPACE: agents/frontend-agent/
🎯 SCOPE: Component development

Working on React component...
"#;

        let parsed = parser.parse_identity(response);
        assert!(parsed.is_some());

        let (agent, workspace, scope) = parsed.unwrap();
        assert_eq!(agent, "Frontend");
        assert_eq!(workspace, "agents/frontend-agent/");
        assert_eq!(scope, "Component development");
    }

  ./crates/ccswarm/src/identity/mod.rs:90-99 method technologies <-> ./crates/ccswarm/src/identity/mod.rs:102-123 method responsibilities
  Similarity: 93.71%
  Classes: AgentRole <-> AgentRole

[36m--- ./crates/ccswarm/src/identity/mod.rs:technologies (lines 90-99) ---[0m
    pub fn technologies(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend { technologies, .. }
            | AgentRole::Backend { technologies, .. }
            | AgentRole::DevOps { technologies, .. }
            | AgentRole::QA { technologies, .. }
            | AgentRole::Search { technologies, .. } => technologies.clone(),
            AgentRole::Master { .. } => vec!["Orchestration".to_string()],
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:responsibilities (lines 102-123) ---[0m
    pub fn responsibilities(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend {
                responsibilities, ..
            }
            | AgentRole::Backend {
                responsibilities, ..
            }
            | AgentRole::DevOps {
                responsibilities, ..
            }
            | AgentRole::QA {
                responsibilities, ..
            }
            | AgentRole::Search {
                responsibilities, ..
            } => responsibilities.clone(),
            AgentRole::Master {
                oversight_roles, ..
            } => oversight_roles.clone(),
        }
    }

  ./crates/ccswarm/src/identity/mod.rs:102-123 method responsibilities <-> ./crates/ccswarm/src/identity/mod.rs:126-135 method boundaries
  Similarity: 93.71%
  Classes: AgentRole <-> AgentRole

[36m--- ./crates/ccswarm/src/identity/mod.rs:responsibilities (lines 102-123) ---[0m
    pub fn responsibilities(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend {
                responsibilities, ..
            }
            | AgentRole::Backend {
                responsibilities, ..
            }
            | AgentRole::DevOps {
                responsibilities, ..
            }
            | AgentRole::QA {
                responsibilities, ..
            }
            | AgentRole::Search {
                responsibilities, ..
            } => responsibilities.clone(),
            AgentRole::Master {
                oversight_roles, ..
            } => oversight_roles.clone(),
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:boundaries (lines 126-135) ---[0m
    pub fn boundaries(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend { boundaries, .. }
            | AgentRole::Backend { boundaries, .. }
            | AgentRole::DevOps { boundaries, .. }
            | AgentRole::QA { boundaries, .. }
            | AgentRole::Search { boundaries, .. } => boundaries.clone(),
            AgentRole::Master { .. } => vec!["No direct code implementation".to_string()],
        }
    }

  ./crates/ccswarm/src/identity/mod.rs:102-123 method responsibilities <-> ./crates/ccswarm/src/identity/mod.rs:298-304 method new
  Similarity: 88.48%
  Classes: AgentRole <-> ResponseParser

[36m--- ./crates/ccswarm/src/identity/mod.rs:responsibilities (lines 102-123) ---[0m
    pub fn responsibilities(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend {
                responsibilities, ..
            }
            | AgentRole::Backend {
                responsibilities, ..
            }
            | AgentRole::DevOps {
                responsibilities, ..
            }
            | AgentRole::QA {
                responsibilities, ..
            }
            | AgentRole::Search {
                responsibilities, ..
            } => responsibilities.clone(),
            AgentRole::Master {
                oversight_roles, ..
            } => oversight_roles.clone(),
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:new (lines 298-304) ---[0m
    pub fn new() -> Self {
        Self {
            identity_regex: Regex::new(r"🤖 AGENT: (.+)").unwrap(),
            workspace_regex: Regex::new(r"📁 WORKSPACE: (.+)").unwrap(),
            scope_regex: Regex::new(r"🎯 SCOPE: (.+)").unwrap(),
        }
    }

  ./crates/ccswarm/src/identity/mod.rs:90-99 method technologies <-> ./crates/ccswarm/src/identity/mod.rs:126-135 method boundaries
  Similarity: 99.48%
  Classes: AgentRole <-> AgentRole

[36m--- ./crates/ccswarm/src/identity/mod.rs:technologies (lines 90-99) ---[0m
    pub fn technologies(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend { technologies, .. }
            | AgentRole::Backend { technologies, .. }
            | AgentRole::DevOps { technologies, .. }
            | AgentRole::QA { technologies, .. }
            | AgentRole::Search { technologies, .. } => technologies.clone(),
            AgentRole::Master { .. } => vec!["Orchestration".to_string()],
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:boundaries (lines 126-135) ---[0m
    pub fn boundaries(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend { boundaries, .. }
            | AgentRole::Backend { boundaries, .. }
            | AgentRole::DevOps { boundaries, .. }
            | AgentRole::QA { boundaries, .. }
            | AgentRole::Search { boundaries, .. } => boundaries.clone(),
            AgentRole::Master { .. } => vec!["No direct code implementation".to_string()],
        }
    }

  ./crates/ccswarm/src/identity/mod.rs:231-246 method check_boundary_compliance <-> ./crates/ccswarm/src/identity/mod.rs:465-471 function test_agent_role_names
  Similarity: 75.53%

[36m--- ./crates/ccswarm/src/identity/mod.rs:check_boundary_compliance (lines 231-246) ---[0m
    fn check_boundary_compliance(&self, response: &str) -> bool {
        // Check for indicators of boundary violations
        let violation_patterns = vec![
            r"working on backend code",
            r"modifying infrastructure",
            r"changing database schema",
        ];

        for pattern in violation_patterns {
            let re = Regex::new(pattern).unwrap();
            if re.is_match(response) {
                return false;
            }
        }
        true
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:test_agent_role_names (lines 465-471) ---[0m
    fn test_agent_role_names() {
        let frontend = default_frontend_role();
        assert_eq!(frontend.name(), "Frontend");

        let backend = default_backend_role();
        assert_eq!(backend.name(), "Backend");
    }

  ./crates/ccswarm/src/identity/mod.rs:149-155 method hash <-> ./crates/ccswarm/src/identity/mod.rs:231-246 method check_boundary_compliance
  Similarity: 73.42%
  Classes: QualityStandards <-> IdentityMonitor

[36m--- ./crates/ccswarm/src/identity/mod.rs:hash (lines 149-155) ---[0m
    fn hash<H: std::hash::Hasher>(&self, state: &mut H) {
        // Convert f64 to bits for hashing
        self.min_test_coverage.to_bits().hash(state);
        self.max_complexity.hash(state);
        self.security_scan_required.hash(state);
        self.performance_threshold_secs.hash(state);
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:check_boundary_compliance (lines 231-246) ---[0m
    fn check_boundary_compliance(&self, response: &str) -> bool {
        // Check for indicators of boundary violations
        let violation_patterns = vec![
            r"working on backend code",
            r"modifying infrastructure",
            r"changing database schema",
        ];

        for pattern in violation_patterns {
            let re = Regex::new(pattern).unwrap();
            if re.is_match(response) {
                return false;
            }
        }
        true
    }

  ./crates/ccswarm/src/identity/mod.rs:90-99 method technologies <-> ./crates/ccswarm/src/identity/mod.rs:298-304 method new
  Similarity: 88.88%
  Classes: AgentRole <-> ResponseParser

[36m--- ./crates/ccswarm/src/identity/mod.rs:technologies (lines 90-99) ---[0m
    pub fn technologies(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend { technologies, .. }
            | AgentRole::Backend { technologies, .. }
            | AgentRole::DevOps { technologies, .. }
            | AgentRole::QA { technologies, .. }
            | AgentRole::Search { technologies, .. } => technologies.clone(),
            AgentRole::Master { .. } => vec!["Orchestration".to_string()],
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:new (lines 298-304) ---[0m
    pub fn new() -> Self {
        Self {
            identity_regex: Regex::new(r"🤖 AGENT: (.+)").unwrap(),
            workspace_regex: Regex::new(r"📁 WORKSPACE: (.+)").unwrap(),
            scope_regex: Regex::new(r"🎯 SCOPE: (.+)").unwrap(),
        }
    }

  ./crates/ccswarm/src/identity/mod.rs:126-135 method boundaries <-> ./crates/ccswarm/src/identity/mod.rs:298-304 method new
  Similarity: 88.88%
  Classes: AgentRole <-> ResponseParser

[36m--- ./crates/ccswarm/src/identity/mod.rs:boundaries (lines 126-135) ---[0m
    pub fn boundaries(&self) -> Vec<String> {
        match self {
            AgentRole::Frontend { boundaries, .. }
            | AgentRole::Backend { boundaries, .. }
            | AgentRole::DevOps { boundaries, .. }
            | AgentRole::QA { boundaries, .. }
            | AgentRole::Search { boundaries, .. } => boundaries.clone(),
            AgentRole::Master { .. } => vec!["No direct code implementation".to_string()],
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:new (lines 298-304) ---[0m
    pub fn new() -> Self {
        Self {
            identity_regex: Regex::new(r"🤖 AGENT: (.+)").unwrap(),
            workspace_regex: Regex::new(r"📁 WORKSPACE: (.+)").unwrap(),
            scope_regex: Regex::new(r"🎯 SCOPE: (.+)").unwrap(),
        }
    }

  ./crates/ccswarm/src/identity/mod.rs:190-197 method new <-> ./crates/ccswarm/src/identity/mod.rs:298-304 method new
  Similarity: 87.54%
  Classes: IdentityMonitor <-> ResponseParser

[36m--- ./crates/ccswarm/src/identity/mod.rs:new (lines 190-197) ---[0m
    pub fn new(agent_id: &str) -> Self {
        Self {
            agent_id: agent_id.to_string(),
            last_identity_check: Instant::now(),
            identity_drift_threshold: Duration::from_secs(300), // 5 minutes
            response_parser: ResponseParser::new(),
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:new (lines 298-304) ---[0m
    pub fn new() -> Self {
        Self {
            identity_regex: Regex::new(r"🤖 AGENT: (.+)").unwrap(),
            workspace_regex: Regex::new(r"📁 WORKSPACE: (.+)").unwrap(),
            scope_regex: Regex::new(r"🎯 SCOPE: (.+)").unwrap(),
        }
    }

  ./crates/ccswarm/src/identity/mod.rs:72-81 method name <-> ./crates/ccswarm/src/identity/mod.rs:190-197 method new
  Similarity: 71.27%
  Classes: AgentRole <-> IdentityMonitor

[36m--- ./crates/ccswarm/src/identity/mod.rs:name (lines 72-81) ---[0m
    pub fn name(&self) -> &str {
        match self {
            AgentRole::Frontend { .. } => "Frontend",
            AgentRole::Backend { .. } => "Backend",
            AgentRole::DevOps { .. } => "DevOps",
            AgentRole::QA { .. } => "QA",
            AgentRole::Master { .. } => "Master",
            AgentRole::Search { .. } => "Search",
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:new (lines 190-197) ---[0m
    pub fn new(agent_id: &str) -> Self {
        Self {
            agent_id: agent_id.to_string(),
            last_identity_check: Instant::now(),
            identity_drift_threshold: Duration::from_secs(300), // 5 minutes
            response_parser: ResponseParser::new(),
        }
    }

  ./crates/ccswarm/src/identity/mod.rs:72-81 method name <-> ./crates/ccswarm/src/identity/mod.rs:298-304 method new
  Similarity: 74.00%
  Classes: AgentRole <-> ResponseParser

[36m--- ./crates/ccswarm/src/identity/mod.rs:name (lines 72-81) ---[0m
    pub fn name(&self) -> &str {
        match self {
            AgentRole::Frontend { .. } => "Frontend",
            AgentRole::Backend { .. } => "Backend",
            AgentRole::DevOps { .. } => "DevOps",
            AgentRole::QA { .. } => "QA",
            AgentRole::Master { .. } => "Master",
            AgentRole::Search { .. } => "Search",
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:new (lines 298-304) ---[0m
    pub fn new() -> Self {
        Self {
            identity_regex: Regex::new(r"🤖 AGENT: (.+)").unwrap(),
            workspace_regex: Regex::new(r"📁 WORKSPACE: (.+)").unwrap(),
            scope_regex: Regex::new(r"🎯 SCOPE: (.+)").unwrap(),
        }
    }

  ./crates/ccswarm/src/identity/mod.rs:248-253 method check_delegation_behavior <-> ./crates/ccswarm/src/identity/mod.rs:298-304 method new
  Similarity: 73.48%
  Classes: IdentityMonitor <-> ResponseParser

[36m--- ./crates/ccswarm/src/identity/mod.rs:check_delegation_behavior (lines 248-253) ---[0m
    fn check_delegation_behavior(&self, response: &str) -> bool {
        // Check for proper delegation patterns
        response.contains("DELEGATING TO:")
            || response.contains("outside my specialization")
            || !response.contains("I'll handle this")
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:new (lines 298-304) ---[0m
    pub fn new() -> Self {
        Self {
            identity_regex: Regex::new(r"🤖 AGENT: (.+)").unwrap(),
            workspace_regex: Regex::new(r"📁 WORKSPACE: (.+)").unwrap(),
            scope_regex: Regex::new(r"🎯 SCOPE: (.+)").unwrap(),
        }
    }

  ./crates/ccswarm/src/identity/mod.rs:190-197 method new <-> ./crates/ccswarm/src/identity/mod.rs:226-229 method check_identity_header
  Similarity: 70.36%
  Classes: IdentityMonitor <-> IdentityMonitor

[36m--- ./crates/ccswarm/src/identity/mod.rs:new (lines 190-197) ---[0m
    pub fn new(agent_id: &str) -> Self {
        Self {
            agent_id: agent_id.to_string(),
            last_identity_check: Instant::now(),
            identity_drift_threshold: Duration::from_secs(300), // 5 minutes
            response_parser: ResponseParser::new(),
        }
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:check_identity_header (lines 226-229) ---[0m
    pub fn check_identity_header(&self, response: &str) -> bool {
        let required_pattern = format!("🤖 AGENT: {}", self.agent_id);
        response.contains(&required_pattern)
    }

  ./crates/ccswarm/src/identity/mod.rs:226-229 method check_identity_header <-> ./crates/ccswarm/src/identity/mod.rs:248-253 method check_delegation_behavior
  Similarity: 79.56%
  Classes: IdentityMonitor <-> IdentityMonitor

[36m--- ./crates/ccswarm/src/identity/mod.rs:check_identity_header (lines 226-229) ---[0m
    pub fn check_identity_header(&self, response: &str) -> bool {
        let required_pattern = format!("🤖 AGENT: {}", self.agent_id);
        response.contains(&required_pattern)
    }

[36m--- ./crates/ccswarm/src/identity/mod.rs:check_delegation_behavior (lines 248-253) ---[0m
    fn check_delegation_behavior(&self, response: &str) -> bool {
        // Check for proper delegation patterns
        response.contains("DELEGATING TO:")
            || response.contains("outside my specialization")
            || !response.contains("I'll handle this")
    }


Duplicates in ./crates/ccswarm/src/cli/resource_commands.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/cli/resource_commands.rs:141-187 function show_resource_stats <-> ./crates/ccswarm/src/cli/resource_commands.rs:232-261 function check_idle_agents
  Similarity: 85.06%

[36m--- ./crates/ccswarm/src/cli/resource_commands.rs:show_resource_stats (lines 141-187) ---[0m
async fn show_resource_stats(session_manager: Arc<SessionManager>) -> Result<()> {
    if let Some(stats) = session_manager.get_resource_efficiency_stats() {
        println!("{}", "Resource Efficiency Statistics".bold().cyan());
        println!("{}", "=".repeat(50));

        println!("Total Agents:      {}", stats.total_agents);
        println!(
            "Active Agents:     {} ({})",
            stats.active_agents,
            format!(
                "{:.1}%",
                (stats.active_agents as f32 / stats.total_agents.max(1) as f32) * 100.0
            )
            .green()
        );
        println!(
            "Suspended Agents:  {} ({})",
            stats.suspended_agents,
            format!("{:.1}%", stats.suspension_rate).yellow()
        );
        println!();
        println!("Average CPU Usage:    {:.1}%", stats.average_cpu_usage);
        println!(
            "Average Memory Usage: {} MB ({:.1}%)",
            stats.average_memory_usage / (1024 * 1024),
            stats.average_memory_percent
        );
        println!(
            "Total Memory Usage:   {} MB",
            stats.total_memory_usage / (1024 * 1024)
        );

        println!();
        println!(
            "{}",
            format!(
                "💡 Efficiency Tip: {:.1}% of agents are suspended, saving resources",
                stats.suspension_rate
            )
            .italic()
        );
    } else {
        println!("{}", "Resource monitoring not enabled".yellow());
    }

    Ok(())
}

[36m--- ./crates/ccswarm/src/cli/resource_commands.rs:check_idle_agents (lines 232-261) ---[0m
async fn check_idle_agents(session_manager: Arc<SessionManager>) -> Result<()> {
    println!("{}", "Checking for idle agents...".cyan());

    let suspended = session_manager.check_and_suspend_idle_agents().await?;

    if suspended.is_empty() {
        println!("{}", "No idle agents found".green());
    } else {
        println!(
            "{}",
            format!("Suspended {} idle agents:", suspended.len()).yellow()
        );
        for agent_id in &suspended {
            println!("  - {}", agent_id);
        }

        let stats = session_manager.get_resource_efficiency_stats();
        if let Some(stats) = stats {
            let saved_memory_mb =
                (stats.average_memory_usage * suspended.len() as u64) / (1024 * 1024);
            println!();
            println!(
                "{}",
                format!("💡 Estimated memory saved: {} MB", saved_memory_mb).green()
            );
        }
    }

    Ok(())
}

  ./crates/ccswarm/src/cli/resource_commands.rs:190-229 function update_resource_limits <-> ./crates/ccswarm/src/cli/resource_commands.rs:264-278 function resume_agent
  Similarity: 83.52%

[36m--- ./crates/ccswarm/src/cli/resource_commands.rs:update_resource_limits (lines 190-229) ---[0m
async fn update_resource_limits(
    _session_manager: Arc<SessionManager>,
    max_cpu: Option<f32>,
    max_memory_gb: Option<f32>,
    idle_timeout_min: Option<u64>,
    auto_suspend: Option<bool>,
) -> Result<()> {
    let mut limits = ResourceLimits::default();

    if let Some(cpu) = max_cpu {
        limits.max_cpu_percent = cpu;
        println!("✓ Max CPU set to {}%", cpu);
    }

    if let Some(mem_gb) = max_memory_gb {
        limits.max_memory_bytes = (mem_gb * 1024.0 * 1024.0 * 1024.0) as u64;
        println!("✓ Max memory set to {} GB", mem_gb);
    }

    if let Some(timeout_min) = idle_timeout_min {
        limits.idle_timeout = chrono::Duration::minutes(timeout_min as i64);
        println!("✓ Idle timeout set to {} minutes", timeout_min);
    }

    if let Some(suspend) = auto_suspend {
        limits.auto_suspend_enabled = suspend;
        println!(
            "✓ Auto-suspend {}",
            if suspend { "enabled" } else { "disabled" }
        );
    }

    // TODO: Apply these limits to the session manager
    println!(
        "{}",
        "Note: Resource limits will be applied to new sessions".italic()
    );

    Ok(())
}

[36m--- ./crates/ccswarm/src/cli/resource_commands.rs:resume_agent (lines 264-278) ---[0m
async fn resume_agent(session_manager: Arc<SessionManager>, agent_id: String) -> Result<()> {
    // Find the session for this agent
    let sessions = session_manager.list_sessions();
    let session = sessions
        .iter()
        .find(|s| s.agent_id == agent_id)
        .ok_or_else(|| anyhow::anyhow!("Agent not found: {}", agent_id))?;

    // Resume the session
    session_manager.resume_session(&session.id).await?;

    println!("{}", format!("✓ Agent {} resumed", agent_id).green());

    Ok(())
}

  ./crates/ccswarm/src/cli/resource_commands.rs:232-261 function check_idle_agents <-> ./crates/ccswarm/src/cli/resource_commands.rs:264-278 function resume_agent
  Similarity: 84.76%

[36m--- ./crates/ccswarm/src/cli/resource_commands.rs:check_idle_agents (lines 232-261) ---[0m
async fn check_idle_agents(session_manager: Arc<SessionManager>) -> Result<()> {
    println!("{}", "Checking for idle agents...".cyan());

    let suspended = session_manager.check_and_suspend_idle_agents().await?;

    if suspended.is_empty() {
        println!("{}", "No idle agents found".green());
    } else {
        println!(
            "{}",
            format!("Suspended {} idle agents:", suspended.len()).yellow()
        );
        for agent_id in &suspended {
            println!("  - {}", agent_id);
        }

        let stats = session_manager.get_resource_efficiency_stats();
        if let Some(stats) = stats {
            let saved_memory_mb =
                (stats.average_memory_usage * suspended.len() as u64) / (1024 * 1024);
            println!();
            println!(
                "{}",
                format!("💡 Estimated memory saved: {} MB", saved_memory_mb).green()
            );
        }
    }

    Ok(())
}

[36m--- ./crates/ccswarm/src/cli/resource_commands.rs:resume_agent (lines 264-278) ---[0m
async fn resume_agent(session_manager: Arc<SessionManager>, agent_id: String) -> Result<()> {
    // Find the session for this agent
    let sessions = session_manager.list_sessions();
    let session = sessions
        .iter()
        .find(|s| s.agent_id == agent_id)
        .ok_or_else(|| anyhow::anyhow!("Agent not found: {}", agent_id))?;

    // Resume the session
    session_manager.resume_session(&session.id).await?;

    println!("{}", format!("✓ Agent {} resumed", agent_id).green());

    Ok(())
}

  ./crates/ccswarm/src/cli/resource_commands.rs:281-291 function format_duration <-> ./crates/ccswarm/src/cli/resource_commands.rs:299-305 method trunc
  Similarity: 72.47%

[36m--- ./crates/ccswarm/src/cli/resource_commands.rs:format_duration (lines 281-291) ---[0m
fn format_duration(duration: chrono::Duration) -> String {
    let total_seconds = duration.num_seconds();

    if total_seconds < 60 {
        format!("{}s", total_seconds)
    } else if total_seconds < 3600 {
        format!("{}m", total_seconds / 60)
    } else {
        format!("{}h {}m", total_seconds / 3600, (total_seconds % 3600) / 60)
    }
}

[36m--- ./crates/ccswarm/src/cli/resource_commands.rs:trunc (lines 299-305) ---[0m
    fn trunc(&self, max_len: usize) -> String {
        if self.len() <= max_len {
            self.clone()
        } else {
            format!("{}...", &self[..max_len - 3])
        }
    }


Duplicates in ./crates/ccswarm/src/providers/custom.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/providers/custom.rs:326-390 method execute_task <-> ./crates/ccswarm/src/providers/custom.rs:392-448 method health_check
  Similarity: 92.05%
  Classes: ProviderExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/custom.rs:execute_task (lines 326-390) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate context and prompt
        let context = self.generate_context_info(identity, task);
        let prompt = format!(
            "{}\n\nAdditional Details: {}",
            task.description,
            task.details.as_deref().unwrap_or("None")
        );

        tracing::info!(
            "Executing task '{}' with custom command '{}' for agent {}",
            task.description,
            self.config.command,
            identity.agent_id
        );

        // Prepare and execute command
        let args = self.prepare_command_args(&prompt, &context, identity, task);

        match self
            .execute_custom_command(args, working_dir, identity)
            .await
        {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_command_output(output, task, duration);

                tracing::info!(
                    "Custom command task completed in {:?} for agent {} (success: {})",
                    duration,
                    identity.agent_id,
                    result.success
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Custom command task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "custom",
                        "command": self.config.command,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:health_check (lines 392-448) ---[0m
    async fn health_check(&self, working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        // Try to execute a simple version or help command
        let test_commands = vec!["--version", "--help", "-h", "-V"];
        let mut last_error = None;

        for test_arg in test_commands {
            let result = Command::new(&self.config.command)
                .arg(test_arg)
                .current_dir(working_dir)
                .output()
                .await;

            match result {
                Ok(output) if output.status.success() => {
                    let duration = start.elapsed();
                    let response_time_ms = duration.as_millis() as u64;

                    let version_info = String::from_utf8_lossy(&output.stdout)
                        .lines()
                        .next()
                        .unwrap_or("Custom command available")
                        .to_string();

                    return Ok(ProviderHealthStatus {
                        is_healthy: true,
                        version: Some(format!("{}: {}", self.config.command, version_info)),
                        last_check: chrono::Utc::now(),
                        error_message: None,
                        response_time_ms: Some(response_time_ms),
                    });
                }
                Ok(output) => {
                    last_error = Some(format!(
                        "Command failed with {}: {}",
                        test_arg,
                        String::from_utf8_lossy(&output.stderr)
                    ));
                }
                Err(e) => {
                    last_error = Some(format!("Failed to execute {}: {}", test_arg, e));
                }
            }
        }

        let duration = start.elapsed();
        let response_time_ms = duration.as_millis() as u64;

        Ok(ProviderHealthStatus {
            is_healthy: false,
            version: None,
            last_check: chrono::Utc::now(),
            error_message: last_error.or_else(|| Some("Command not available".to_string())),
            response_time_ms: Some(response_time_ms),
        })
    }

  ./crates/ccswarm/src/providers/custom.rs:95-168 method execute_custom_command <-> ./crates/ccswarm/src/providers/custom.rs:326-390 method execute_task
  Similarity: 75.15%
  Classes: CustomExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/custom.rs:execute_custom_command (lines 95-168) ---[0m
    async fn execute_custom_command(
        &self,
        args: Vec<String>,
        working_dir: &Path,
        identity: &AgentIdentity,
    ) -> Result<String> {
        let mut cmd = Command::new(&self.config.command);

        // Set working directory
        if let Some(ref dir) = self.config.working_directory {
            cmd.current_dir(dir);
        } else {
            cmd.current_dir(working_dir);
        }

        // Add identity environment variables
        for (key, value) in &identity.env_vars {
            cmd.env(key, value);
        }

        // Add provider environment variables
        for (key, value) in &self.config.env_vars {
            cmd.env(key, value);
        }

        // Add command arguments
        cmd.args(&args);

        // Execute command with timeout
        let start = Instant::now();
        let timeout_duration =
            std::time::Duration::from_secs(self.config.timeout_seconds.unwrap_or(300));

        let output = tokio::time::timeout(timeout_duration, cmd.output())
            .await
            .context("Custom command timed out")?
            .context("Failed to execute custom command")?;

        let duration = start.elapsed();

        tracing::debug!(
            "Custom command '{}' execution completed in {:?} for agent {}",
            self.config.command,
            duration,
            identity.agent_id
        );

        if output.status.success() {
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let stderr = String::from_utf8_lossy(&output.stderr).to_string();

            // Combine stdout and stderr if both have content
            if !stdout.trim().is_empty() && !stderr.trim().is_empty() {
                Ok(format!("{}\n\nSTDERR:\n{}", stdout, stderr))
            } else if !stdout.trim().is_empty() {
                Ok(stdout)
            } else if !stderr.trim().is_empty() {
                Ok(stderr)
            } else {
                Ok("Command executed successfully with no output".to_string())
            }
        } else {
            let stderr = String::from_utf8_lossy(&output.stderr);
            let stdout = String::from_utf8_lossy(&output.stdout);

            Err(anyhow::anyhow!(
                "Custom command '{}' failed (exit code: {:?})\nStderr: {}\nStdout: {}",
                self.config.command,
                output.status.code(),
                stderr,
                stdout
            ))
        }
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:execute_task (lines 326-390) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate context and prompt
        let context = self.generate_context_info(identity, task);
        let prompt = format!(
            "{}\n\nAdditional Details: {}",
            task.description,
            task.details.as_deref().unwrap_or("None")
        );

        tracing::info!(
            "Executing task '{}' with custom command '{}' for agent {}",
            task.description,
            self.config.command,
            identity.agent_id
        );

        // Prepare and execute command
        let args = self.prepare_command_args(&prompt, &context, identity, task);

        match self
            .execute_custom_command(args, working_dir, identity)
            .await
        {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_command_output(output, task, duration);

                tracing::info!(
                    "Custom command task completed in {:?} for agent {} (success: {})",
                    duration,
                    identity.agent_id,
                    result.success
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Custom command task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "custom",
                        "command": self.config.command,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/custom.rs:95-168 method execute_custom_command <-> ./crates/ccswarm/src/providers/custom.rs:392-448 method health_check
  Similarity: 76.29%
  Classes: CustomExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/custom.rs:execute_custom_command (lines 95-168) ---[0m
    async fn execute_custom_command(
        &self,
        args: Vec<String>,
        working_dir: &Path,
        identity: &AgentIdentity,
    ) -> Result<String> {
        let mut cmd = Command::new(&self.config.command);

        // Set working directory
        if let Some(ref dir) = self.config.working_directory {
            cmd.current_dir(dir);
        } else {
            cmd.current_dir(working_dir);
        }

        // Add identity environment variables
        for (key, value) in &identity.env_vars {
            cmd.env(key, value);
        }

        // Add provider environment variables
        for (key, value) in &self.config.env_vars {
            cmd.env(key, value);
        }

        // Add command arguments
        cmd.args(&args);

        // Execute command with timeout
        let start = Instant::now();
        let timeout_duration =
            std::time::Duration::from_secs(self.config.timeout_seconds.unwrap_or(300));

        let output = tokio::time::timeout(timeout_duration, cmd.output())
            .await
            .context("Custom command timed out")?
            .context("Failed to execute custom command")?;

        let duration = start.elapsed();

        tracing::debug!(
            "Custom command '{}' execution completed in {:?} for agent {}",
            self.config.command,
            duration,
            identity.agent_id
        );

        if output.status.success() {
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let stderr = String::from_utf8_lossy(&output.stderr).to_string();

            // Combine stdout and stderr if both have content
            if !stdout.trim().is_empty() && !stderr.trim().is_empty() {
                Ok(format!("{}\n\nSTDERR:\n{}", stdout, stderr))
            } else if !stdout.trim().is_empty() {
                Ok(stdout)
            } else if !stderr.trim().is_empty() {
                Ok(stderr)
            } else {
                Ok("Command executed successfully with no output".to_string())
            }
        } else {
            let stderr = String::from_utf8_lossy(&output.stderr);
            let stdout = String::from_utf8_lossy(&output.stdout);

            Err(anyhow::anyhow!(
                "Custom command '{}' failed (exit code: {:?})\nStderr: {}\nStdout: {}",
                self.config.command,
                output.status.code(),
                stderr,
                stdout
            ))
        }
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:health_check (lines 392-448) ---[0m
    async fn health_check(&self, working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        // Try to execute a simple version or help command
        let test_commands = vec!["--version", "--help", "-h", "-V"];
        let mut last_error = None;

        for test_arg in test_commands {
            let result = Command::new(&self.config.command)
                .arg(test_arg)
                .current_dir(working_dir)
                .output()
                .await;

            match result {
                Ok(output) if output.status.success() => {
                    let duration = start.elapsed();
                    let response_time_ms = duration.as_millis() as u64;

                    let version_info = String::from_utf8_lossy(&output.stdout)
                        .lines()
                        .next()
                        .unwrap_or("Custom command available")
                        .to_string();

                    return Ok(ProviderHealthStatus {
                        is_healthy: true,
                        version: Some(format!("{}: {}", self.config.command, version_info)),
                        last_check: chrono::Utc::now(),
                        error_message: None,
                        response_time_ms: Some(response_time_ms),
                    });
                }
                Ok(output) => {
                    last_error = Some(format!(
                        "Command failed with {}: {}",
                        test_arg,
                        String::from_utf8_lossy(&output.stderr)
                    ));
                }
                Err(e) => {
                    last_error = Some(format!("Failed to execute {}: {}", test_arg, e));
                }
            }
        }

        let duration = start.elapsed();
        let response_time_ms = duration.as_millis() as u64;

        Ok(ProviderHealthStatus {
            is_healthy: false,
            version: None,
            last_check: chrono::Utc::now(),
            error_message: last_error.or_else(|| Some("Command not available".to_string())),
            response_time_ms: Some(response_time_ms),
        })
    }

  ./crates/ccswarm/src/providers/custom.rs:219-263 method determine_success_from_output <-> ./crates/ccswarm/src/providers/custom.rs:326-390 method execute_task
  Similarity: 88.51%
  Classes: CustomExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/custom.rs:determine_success_from_output (lines 219-263) ---[0m
    fn determine_success_from_output(&self, output: &str) -> bool {
        let output_lower = output.to_lowercase();

        // Check for explicit failure indicators
        let failure_indicators = [
            "error:",
            "failed:",
            "exception",
            "panic:",
            "fatal:",
            "command not found",
            "permission denied",
            "file not found",
            "syntax error",
            "compilation failed",
            "test failed",
        ];

        for indicator in &failure_indicators {
            if output_lower.contains(indicator) {
                return false;
            }
        }

        // Check for success indicators
        let success_indicators = [
            "success",
            "completed",
            "finished",
            "done",
            "ok",
            "passed",
            "test passed",
            "build successful",
        ];

        for indicator in &success_indicators {
            if output_lower.contains(indicator) {
                return true;
            }
        }

        // If no clear indicators, assume success if there's meaningful output
        !output.trim().is_empty()
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:execute_task (lines 326-390) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate context and prompt
        let context = self.generate_context_info(identity, task);
        let prompt = format!(
            "{}\n\nAdditional Details: {}",
            task.description,
            task.details.as_deref().unwrap_or("None")
        );

        tracing::info!(
            "Executing task '{}' with custom command '{}' for agent {}",
            task.description,
            self.config.command,
            identity.agent_id
        );

        // Prepare and execute command
        let args = self.prepare_command_args(&prompt, &context, identity, task);

        match self
            .execute_custom_command(args, working_dir, identity)
            .await
        {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_command_output(output, task, duration);

                tracing::info!(
                    "Custom command task completed in {:?} for agent {} (success: {})",
                    duration,
                    identity.agent_id,
                    result.success
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Custom command task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "custom",
                        "command": self.config.command,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/custom.rs:171-216 method parse_command_output <-> ./crates/ccswarm/src/providers/custom.rs:326-390 method execute_task
  Similarity: 84.21%
  Classes: CustomExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/custom.rs:parse_command_output (lines 171-216) ---[0m
    fn parse_command_output(
        &self,
        output: String,
        task: &Task,
        duration: std::time::Duration,
    ) -> TaskResult {
        if self.config.supports_json {
            // Try to parse as JSON
            if let Ok(json_value) = serde_json::from_str::<serde_json::Value>(&output) {
                return TaskResult {
                    success: true,
                    output: serde_json::json!({
                        "result": json_value,
                        "task_id": task.id,
                        "provider": "custom",
                        "command": self.config.command,
                        "format": "json"
                    }),
                    error: None,
                    duration,
                };
            }
        }

        // Fallback to text parsing
        let success = self.determine_success_from_output(&output);
        let error = if success {
            None
        } else {
            Some("Command output indicates failure".to_string())
        };

        TaskResult {
            success,
            output: serde_json::json!({
                "response": output,
                "task_id": task.id,
                "provider": "custom",
                "command": self.config.command,
                "format": "text",
                "working_directory": self.config.working_directory,
            }),
            error,
            duration,
        }
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:execute_task (lines 326-390) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate context and prompt
        let context = self.generate_context_info(identity, task);
        let prompt = format!(
            "{}\n\nAdditional Details: {}",
            task.description,
            task.details.as_deref().unwrap_or("None")
        );

        tracing::info!(
            "Executing task '{}' with custom command '{}' for agent {}",
            task.description,
            self.config.command,
            identity.agent_id
        );

        // Prepare and execute command
        let args = self.prepare_command_args(&prompt, &context, identity, task);

        match self
            .execute_custom_command(args, working_dir, identity)
            .await
        {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_command_output(output, task, duration);

                tracing::info!(
                    "Custom command task completed in {:?} for agent {} (success: {})",
                    duration,
                    identity.agent_id,
                    result.success
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Custom command task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "custom",
                        "command": self.config.command,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

  ./crates/ccswarm/src/providers/custom.rs:95-168 method execute_custom_command <-> ./crates/ccswarm/src/providers/custom.rs:266-300 method generate_help_text
  Similarity: 83.42%
  Classes: CustomExecutor <-> CustomExecutor

[36m--- ./crates/ccswarm/src/providers/custom.rs:execute_custom_command (lines 95-168) ---[0m
    async fn execute_custom_command(
        &self,
        args: Vec<String>,
        working_dir: &Path,
        identity: &AgentIdentity,
    ) -> Result<String> {
        let mut cmd = Command::new(&self.config.command);

        // Set working directory
        if let Some(ref dir) = self.config.working_directory {
            cmd.current_dir(dir);
        } else {
            cmd.current_dir(working_dir);
        }

        // Add identity environment variables
        for (key, value) in &identity.env_vars {
            cmd.env(key, value);
        }

        // Add provider environment variables
        for (key, value) in &self.config.env_vars {
            cmd.env(key, value);
        }

        // Add command arguments
        cmd.args(&args);

        // Execute command with timeout
        let start = Instant::now();
        let timeout_duration =
            std::time::Duration::from_secs(self.config.timeout_seconds.unwrap_or(300));

        let output = tokio::time::timeout(timeout_duration, cmd.output())
            .await
            .context("Custom command timed out")?
            .context("Failed to execute custom command")?;

        let duration = start.elapsed();

        tracing::debug!(
            "Custom command '{}' execution completed in {:?} for agent {}",
            self.config.command,
            duration,
            identity.agent_id
        );

        if output.status.success() {
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let stderr = String::from_utf8_lossy(&output.stderr).to_string();

            // Combine stdout and stderr if both have content
            if !stdout.trim().is_empty() && !stderr.trim().is_empty() {
                Ok(format!("{}\n\nSTDERR:\n{}", stdout, stderr))
            } else if !stdout.trim().is_empty() {
                Ok(stdout)
            } else if !stderr.trim().is_empty() {
                Ok(stderr)
            } else {
                Ok("Command executed successfully with no output".to_string())
            }
        } else {
            let stderr = String::from_utf8_lossy(&output.stderr);
            let stdout = String::from_utf8_lossy(&output.stdout);

            Err(anyhow::anyhow!(
                "Custom command '{}' failed (exit code: {:?})\nStderr: {}\nStdout: {}",
                self.config.command,
                output.status.code(),
                stderr,
                stdout
            ))
        }
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:generate_help_text (lines 266-300) ---[0m
    pub fn generate_help_text(&self, identity: &AgentIdentity, task: &Task) -> String {
        let mut help = String::new();

        help.push_str("Custom Command Configuration:\n");
        help.push_str(&format!("Command: {}\n", self.config.command));
        help.push_str(&format!("Arguments: {:?}\n", self.config.args));
        help.push_str(&format!(
            "Working Directory: {:?}\n",
            self.config.working_directory
        ));
        help.push_str(&format!(
            "Timeout: {:?} seconds\n",
            self.config.timeout_seconds
        ));
        help.push_str(&format!("Supports JSON: {}\n", self.config.supports_json));

        help.push_str("\nAvailable Placeholders:\n");
        help.push_str("{prompt} -> Task prompt will be substituted here\n");
        help.push_str(&format!("{{agent_id}} -> {} \n", identity.agent_id));
        help.push_str(&format!("{{task_id}} -> {}\n", task.id));
        help.push_str(&format!("{{task_description}} -> {}\n", task.description));
        help.push_str(&format!(
            "{{workspace}} -> {}\n",
            identity.workspace_path.display()
        ));

        if !self.config.env_vars.is_empty() {
            help.push_str("\nEnvironment Variables:\n");
            for (key, value) in &self.config.env_vars {
                help.push_str(&format!("{} = {}\n", key, value));
            }
        }

        help
    }

  ./crates/ccswarm/src/providers/custom.rs:171-216 method parse_command_output <-> ./crates/ccswarm/src/providers/custom.rs:392-448 method health_check
  Similarity: 86.77%
  Classes: CustomExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/custom.rs:parse_command_output (lines 171-216) ---[0m
    fn parse_command_output(
        &self,
        output: String,
        task: &Task,
        duration: std::time::Duration,
    ) -> TaskResult {
        if self.config.supports_json {
            // Try to parse as JSON
            if let Ok(json_value) = serde_json::from_str::<serde_json::Value>(&output) {
                return TaskResult {
                    success: true,
                    output: serde_json::json!({
                        "result": json_value,
                        "task_id": task.id,
                        "provider": "custom",
                        "command": self.config.command,
                        "format": "json"
                    }),
                    error: None,
                    duration,
                };
            }
        }

        // Fallback to text parsing
        let success = self.determine_success_from_output(&output);
        let error = if success {
            None
        } else {
            Some("Command output indicates failure".to_string())
        };

        TaskResult {
            success,
            output: serde_json::json!({
                "response": output,
                "task_id": task.id,
                "provider": "custom",
                "command": self.config.command,
                "format": "text",
                "working_directory": self.config.working_directory,
            }),
            error,
            duration,
        }
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:health_check (lines 392-448) ---[0m
    async fn health_check(&self, working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        // Try to execute a simple version or help command
        let test_commands = vec!["--version", "--help", "-h", "-V"];
        let mut last_error = None;

        for test_arg in test_commands {
            let result = Command::new(&self.config.command)
                .arg(test_arg)
                .current_dir(working_dir)
                .output()
                .await;

            match result {
                Ok(output) if output.status.success() => {
                    let duration = start.elapsed();
                    let response_time_ms = duration.as_millis() as u64;

                    let version_info = String::from_utf8_lossy(&output.stdout)
                        .lines()
                        .next()
                        .unwrap_or("Custom command available")
                        .to_string();

                    return Ok(ProviderHealthStatus {
                        is_healthy: true,
                        version: Some(format!("{}: {}", self.config.command, version_info)),
                        last_check: chrono::Utc::now(),
                        error_message: None,
                        response_time_ms: Some(response_time_ms),
                    });
                }
                Ok(output) => {
                    last_error = Some(format!(
                        "Command failed with {}: {}",
                        test_arg,
                        String::from_utf8_lossy(&output.stderr)
                    ));
                }
                Err(e) => {
                    last_error = Some(format!("Failed to execute {}: {}", test_arg, e));
                }
            }
        }

        let duration = start.elapsed();
        let response_time_ms = duration.as_millis() as u64;

        Ok(ProviderHealthStatus {
            is_healthy: false,
            version: None,
            last_check: chrono::Utc::now(),
            error_message: last_error.or_else(|| Some("Command not available".to_string())),
            response_time_ms: Some(response_time_ms),
        })
    }

  ./crates/ccswarm/src/providers/custom.rs:95-168 method execute_custom_command <-> ./crates/ccswarm/src/providers/custom.rs:669-698 function test_generate_help_text
  Similarity: 84.36%

[36m--- ./crates/ccswarm/src/providers/custom.rs:execute_custom_command (lines 95-168) ---[0m
    async fn execute_custom_command(
        &self,
        args: Vec<String>,
        working_dir: &Path,
        identity: &AgentIdentity,
    ) -> Result<String> {
        let mut cmd = Command::new(&self.config.command);

        // Set working directory
        if let Some(ref dir) = self.config.working_directory {
            cmd.current_dir(dir);
        } else {
            cmd.current_dir(working_dir);
        }

        // Add identity environment variables
        for (key, value) in &identity.env_vars {
            cmd.env(key, value);
        }

        // Add provider environment variables
        for (key, value) in &self.config.env_vars {
            cmd.env(key, value);
        }

        // Add command arguments
        cmd.args(&args);

        // Execute command with timeout
        let start = Instant::now();
        let timeout_duration =
            std::time::Duration::from_secs(self.config.timeout_seconds.unwrap_or(300));

        let output = tokio::time::timeout(timeout_duration, cmd.output())
            .await
            .context("Custom command timed out")?
            .context("Failed to execute custom command")?;

        let duration = start.elapsed();

        tracing::debug!(
            "Custom command '{}' execution completed in {:?} for agent {}",
            self.config.command,
            duration,
            identity.agent_id
        );

        if output.status.success() {
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let stderr = String::from_utf8_lossy(&output.stderr).to_string();

            // Combine stdout and stderr if both have content
            if !stdout.trim().is_empty() && !stderr.trim().is_empty() {
                Ok(format!("{}\n\nSTDERR:\n{}", stdout, stderr))
            } else if !stdout.trim().is_empty() {
                Ok(stdout)
            } else if !stderr.trim().is_empty() {
                Ok(stderr)
            } else {
                Ok("Command executed successfully with no output".to_string())
            }
        } else {
            let stderr = String::from_utf8_lossy(&output.stderr);
            let stdout = String::from_utf8_lossy(&output.stdout);

            Err(anyhow::anyhow!(
                "Custom command '{}' failed (exit code: {:?})\nStderr: {}\nStdout: {}",
                self.config.command,
                output.status.code(),
                stderr,
                stdout
            ))
        }
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_generate_help_text (lines 669-698) ---[0m
    fn test_generate_help_text() {
        let config = CustomConfig {
            command: "my-tool".to_string(),
            args: vec!["--input".to_string(), "{prompt}".to_string()],
            env_vars: {
                let mut env = HashMap::new();
                env.insert("TOOL_MODE".to_string(), "production".to_string());
                env
            },
            working_directory: Some(PathBuf::from("/tmp")),
            timeout_seconds: Some(120),
            supports_json: true,
        };

        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let help = executor.generate_help_text(&identity, &task);

        assert!(help.contains("Custom Command Configuration:"));
        assert!(help.contains("Command: my-tool"));
        assert!(help.contains("Timeout: Some(120) seconds"));
        assert!(help.contains("Supports JSON: true"));
        assert!(help.contains("Available Placeholders:"));
        assert!(help.contains("{prompt} ->"));
        assert!(help.contains("{agent_id} ->"));
        assert!(help.contains("Environment Variables:"));
        assert!(help.contains("TOOL_MODE = production"));
    }

  ./crates/ccswarm/src/providers/custom.rs:95-168 method execute_custom_command <-> ./crates/ccswarm/src/providers/custom.rs:540-572 function test_prepare_command_args
  Similarity: 81.25%

[36m--- ./crates/ccswarm/src/providers/custom.rs:execute_custom_command (lines 95-168) ---[0m
    async fn execute_custom_command(
        &self,
        args: Vec<String>,
        working_dir: &Path,
        identity: &AgentIdentity,
    ) -> Result<String> {
        let mut cmd = Command::new(&self.config.command);

        // Set working directory
        if let Some(ref dir) = self.config.working_directory {
            cmd.current_dir(dir);
        } else {
            cmd.current_dir(working_dir);
        }

        // Add identity environment variables
        for (key, value) in &identity.env_vars {
            cmd.env(key, value);
        }

        // Add provider environment variables
        for (key, value) in &self.config.env_vars {
            cmd.env(key, value);
        }

        // Add command arguments
        cmd.args(&args);

        // Execute command with timeout
        let start = Instant::now();
        let timeout_duration =
            std::time::Duration::from_secs(self.config.timeout_seconds.unwrap_or(300));

        let output = tokio::time::timeout(timeout_duration, cmd.output())
            .await
            .context("Custom command timed out")?
            .context("Failed to execute custom command")?;

        let duration = start.elapsed();

        tracing::debug!(
            "Custom command '{}' execution completed in {:?} for agent {}",
            self.config.command,
            duration,
            identity.agent_id
        );

        if output.status.success() {
            let stdout = String::from_utf8_lossy(&output.stdout).to_string();
            let stderr = String::from_utf8_lossy(&output.stderr).to_string();

            // Combine stdout and stderr if both have content
            if !stdout.trim().is_empty() && !stderr.trim().is_empty() {
                Ok(format!("{}\n\nSTDERR:\n{}", stdout, stderr))
            } else if !stdout.trim().is_empty() {
                Ok(stdout)
            } else if !stderr.trim().is_empty() {
                Ok(stderr)
            } else {
                Ok("Command executed successfully with no output".to_string())
            }
        } else {
            let stderr = String::from_utf8_lossy(&output.stderr);
            let stdout = String::from_utf8_lossy(&output.stdout);

            Err(anyhow::anyhow!(
                "Custom command '{}' failed (exit code: {:?})\nStderr: {}\nStdout: {}",
                self.config.command,
                output.status.code(),
                stderr,
                stdout
            ))
        }
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_prepare_command_args (lines 540-572) ---[0m
    fn test_prepare_command_args() {
        let config = CustomConfig {
            command: "my-tool".to_string(),
            args: vec![
                "--prompt".to_string(),
                "{prompt}".to_string(),
                "--agent".to_string(),
                "{agent_id}".to_string(),
                "--workspace".to_string(),
                "{workspace}".to_string(),
            ],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: None,
            supports_json: false,
        };

        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();
        let prompt = "Test prompt";
        let context = "{}";

        let args = executor.prepare_command_args(prompt, context, &identity, &task);

        assert_eq!(args.len(), 6);
        assert_eq!(args[0], "--prompt");
        assert_eq!(args[1], "Test prompt");
        assert_eq!(args[2], "--agent");
        assert_eq!(args[3], "test-custom-agent");
        assert_eq!(args[4], "--workspace");
        assert!(args[5].contains("/tmp") || args[5].contains("/var")); // Workspace path is a temp directory
    }

  ./crates/ccswarm/src/providers/custom.rs:171-216 method parse_command_output <-> ./crates/ccswarm/src/providers/custom.rs:219-263 method determine_success_from_output
  Similarity: 76.58%
  Classes: CustomExecutor <-> CustomExecutor

[36m--- ./crates/ccswarm/src/providers/custom.rs:parse_command_output (lines 171-216) ---[0m
    fn parse_command_output(
        &self,
        output: String,
        task: &Task,
        duration: std::time::Duration,
    ) -> TaskResult {
        if self.config.supports_json {
            // Try to parse as JSON
            if let Ok(json_value) = serde_json::from_str::<serde_json::Value>(&output) {
                return TaskResult {
                    success: true,
                    output: serde_json::json!({
                        "result": json_value,
                        "task_id": task.id,
                        "provider": "custom",
                        "command": self.config.command,
                        "format": "json"
                    }),
                    error: None,
                    duration,
                };
            }
        }

        // Fallback to text parsing
        let success = self.determine_success_from_output(&output);
        let error = if success {
            None
        } else {
            Some("Command output indicates failure".to_string())
        };

        TaskResult {
            success,
            output: serde_json::json!({
                "response": output,
                "task_id": task.id,
                "provider": "custom",
                "command": self.config.command,
                "format": "text",
                "working_directory": self.config.working_directory,
            }),
            error,
            duration,
        }
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:determine_success_from_output (lines 219-263) ---[0m
    fn determine_success_from_output(&self, output: &str) -> bool {
        let output_lower = output.to_lowercase();

        // Check for explicit failure indicators
        let failure_indicators = [
            "error:",
            "failed:",
            "exception",
            "panic:",
            "fatal:",
            "command not found",
            "permission denied",
            "file not found",
            "syntax error",
            "compilation failed",
            "test failed",
        ];

        for indicator in &failure_indicators {
            if output_lower.contains(indicator) {
                return false;
            }
        }

        // Check for success indicators
        let success_indicators = [
            "success",
            "completed",
            "finished",
            "done",
            "ok",
            "passed",
            "test passed",
            "build successful",
        ];

        for indicator in &success_indicators {
            if output_lower.contains(indicator) {
                return true;
            }
        }

        // If no clear indicators, assume success if there's meaningful output
        !output.trim().is_empty()
    }

  ./crates/ccswarm/src/providers/custom.rs:392-448 method health_check <-> ./crates/ccswarm/src/providers/custom.rs:540-572 function test_prepare_command_args
  Similarity: 76.00%

[36m--- ./crates/ccswarm/src/providers/custom.rs:health_check (lines 392-448) ---[0m
    async fn health_check(&self, working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        // Try to execute a simple version or help command
        let test_commands = vec!["--version", "--help", "-h", "-V"];
        let mut last_error = None;

        for test_arg in test_commands {
            let result = Command::new(&self.config.command)
                .arg(test_arg)
                .current_dir(working_dir)
                .output()
                .await;

            match result {
                Ok(output) if output.status.success() => {
                    let duration = start.elapsed();
                    let response_time_ms = duration.as_millis() as u64;

                    let version_info = String::from_utf8_lossy(&output.stdout)
                        .lines()
                        .next()
                        .unwrap_or("Custom command available")
                        .to_string();

                    return Ok(ProviderHealthStatus {
                        is_healthy: true,
                        version: Some(format!("{}: {}", self.config.command, version_info)),
                        last_check: chrono::Utc::now(),
                        error_message: None,
                        response_time_ms: Some(response_time_ms),
                    });
                }
                Ok(output) => {
                    last_error = Some(format!(
                        "Command failed with {}: {}",
                        test_arg,
                        String::from_utf8_lossy(&output.stderr)
                    ));
                }
                Err(e) => {
                    last_error = Some(format!("Failed to execute {}: {}", test_arg, e));
                }
            }
        }

        let duration = start.elapsed();
        let response_time_ms = duration.as_millis() as u64;

        Ok(ProviderHealthStatus {
            is_healthy: false,
            version: None,
            last_check: chrono::Utc::now(),
            error_message: last_error.or_else(|| Some("Command not available".to_string())),
            response_time_ms: Some(response_time_ms),
        })
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_prepare_command_args (lines 540-572) ---[0m
    fn test_prepare_command_args() {
        let config = CustomConfig {
            command: "my-tool".to_string(),
            args: vec![
                "--prompt".to_string(),
                "{prompt}".to_string(),
                "--agent".to_string(),
                "{agent_id}".to_string(),
                "--workspace".to_string(),
                "{workspace}".to_string(),
            ],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: None,
            supports_json: false,
        };

        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();
        let prompt = "Test prompt";
        let context = "{}";

        let args = executor.prepare_command_args(prompt, context, &identity, &task);

        assert_eq!(args.len(), 6);
        assert_eq!(args[0], "--prompt");
        assert_eq!(args[1], "Test prompt");
        assert_eq!(args[2], "--agent");
        assert_eq!(args[3], "test-custom-agent");
        assert_eq!(args[4], "--workspace");
        assert!(args[5].contains("/tmp") || args[5].contains("/var")); // Workspace path is a temp directory
    }

  ./crates/ccswarm/src/providers/custom.rs:326-390 method execute_task <-> ./crates/ccswarm/src/providers/custom.rs:646-666 function test_get_capabilities
  Similarity: 78.36%

[36m--- ./crates/ccswarm/src/providers/custom.rs:execute_task (lines 326-390) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate context and prompt
        let context = self.generate_context_info(identity, task);
        let prompt = format!(
            "{}\n\nAdditional Details: {}",
            task.description,
            task.details.as_deref().unwrap_or("None")
        );

        tracing::info!(
            "Executing task '{}' with custom command '{}' for agent {}",
            task.description,
            self.config.command,
            identity.agent_id
        );

        // Prepare and execute command
        let args = self.prepare_command_args(&prompt, &context, identity, task);

        match self
            .execute_custom_command(args, working_dir, identity)
            .await
        {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_command_output(output, task, duration);

                tracing::info!(
                    "Custom command task completed in {:?} for agent {} (success: {})",
                    duration,
                    identity.agent_id,
                    result.success
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Custom command task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "custom",
                        "command": self.config.command,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_get_capabilities (lines 646-666) ---[0m
    fn test_get_capabilities() {
        let config = CustomConfig {
            supports_json: true,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert!(capabilities.max_context_length.is_none());
        assert!(capabilities
            .supported_languages
            .contains(&"text".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"json".to_string()));
    }

  ./crates/ccswarm/src/providers/custom.rs:326-390 method execute_task <-> ./crates/ccswarm/src/providers/custom.rs:523-537 function test_generate_context_info
  Similarity: 81.08%

[36m--- ./crates/ccswarm/src/providers/custom.rs:execute_task (lines 326-390) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate context and prompt
        let context = self.generate_context_info(identity, task);
        let prompt = format!(
            "{}\n\nAdditional Details: {}",
            task.description,
            task.details.as_deref().unwrap_or("None")
        );

        tracing::info!(
            "Executing task '{}' with custom command '{}' for agent {}",
            task.description,
            self.config.command,
            identity.agent_id
        );

        // Prepare and execute command
        let args = self.prepare_command_args(&prompt, &context, identity, task);

        match self
            .execute_custom_command(args, working_dir, identity)
            .await
        {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_command_output(output, task, duration);

                tracing::info!(
                    "Custom command task completed in {:?} for agent {} (success: {})",
                    duration,
                    identity.agent_id,
                    result.success
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Custom command task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "custom",
                        "command": self.config.command,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_generate_context_info (lines 523-537) ---[0m
    fn test_generate_context_info() {
        let config = CustomConfig::default();
        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let context = executor.generate_context_info(&identity, &task);

        // Should be valid JSON
        let parsed: serde_json::Value = serde_json::from_str(&context).unwrap();
        assert_eq!(parsed["agent"]["id"], "test-custom-agent");
        assert_eq!(parsed["agent"]["specialization"], "DevOps");
        assert_eq!(parsed["task"]["description"], "Deploy application");
        assert!(parsed["boundaries"].is_array());
    }

  ./crates/ccswarm/src/providers/custom.rs:326-390 method execute_task <-> ./crates/ccswarm/src/providers/custom.rs:624-643 function test_parse_command_output_text
  Similarity: 75.94%

[36m--- ./crates/ccswarm/src/providers/custom.rs:execute_task (lines 326-390) ---[0m
    async fn execute_task(
        &self,
        task: &Task,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<TaskResult> {
        let start = Instant::now();

        // Generate context and prompt
        let context = self.generate_context_info(identity, task);
        let prompt = format!(
            "{}\n\nAdditional Details: {}",
            task.description,
            task.details.as_deref().unwrap_or("None")
        );

        tracing::info!(
            "Executing task '{}' with custom command '{}' for agent {}",
            task.description,
            self.config.command,
            identity.agent_id
        );

        // Prepare and execute command
        let args = self.prepare_command_args(&prompt, &context, identity, task);

        match self
            .execute_custom_command(args, working_dir, identity)
            .await
        {
            Ok(output) => {
                let duration = start.elapsed();
                let result = self.parse_command_output(output, task, duration);

                tracing::info!(
                    "Custom command task completed in {:?} for agent {} (success: {})",
                    duration,
                    identity.agent_id,
                    result.success
                );

                Ok(result)
            }
            Err(e) => {
                let duration = start.elapsed();

                tracing::error!(
                    "Custom command task failed after {:?} for agent {}: {}",
                    duration,
                    identity.agent_id,
                    e
                );

                Ok(TaskResult {
                    success: false,
                    output: serde_json::json!({
                        "provider": "custom",
                        "command": self.config.command,
                    }),
                    error: Some(e.to_string()),
                    duration,
                })
            }
        }
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_parse_command_output_text (lines 624-643) ---[0m
    fn test_parse_command_output_text() {
        let config = CustomConfig {
            supports_json: false,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(200);

        let text_output = "Deployment completed successfully!";
        let result = executor.parse_command_output(text_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        let output = result.output.as_object().unwrap();
        assert_eq!(output["format"], "text");
        assert_eq!(output["provider"], "custom");
        assert_eq!(output["response"], "Deployment completed successfully!");
    }

  ./crates/ccswarm/src/providers/custom.rs:392-448 method health_check <-> ./crates/ccswarm/src/providers/custom.rs:669-698 function test_generate_help_text
  Similarity: 73.64%

[36m--- ./crates/ccswarm/src/providers/custom.rs:health_check (lines 392-448) ---[0m
    async fn health_check(&self, working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        // Try to execute a simple version or help command
        let test_commands = vec!["--version", "--help", "-h", "-V"];
        let mut last_error = None;

        for test_arg in test_commands {
            let result = Command::new(&self.config.command)
                .arg(test_arg)
                .current_dir(working_dir)
                .output()
                .await;

            match result {
                Ok(output) if output.status.success() => {
                    let duration = start.elapsed();
                    let response_time_ms = duration.as_millis() as u64;

                    let version_info = String::from_utf8_lossy(&output.stdout)
                        .lines()
                        .next()
                        .unwrap_or("Custom command available")
                        .to_string();

                    return Ok(ProviderHealthStatus {
                        is_healthy: true,
                        version: Some(format!("{}: {}", self.config.command, version_info)),
                        last_check: chrono::Utc::now(),
                        error_message: None,
                        response_time_ms: Some(response_time_ms),
                    });
                }
                Ok(output) => {
                    last_error = Some(format!(
                        "Command failed with {}: {}",
                        test_arg,
                        String::from_utf8_lossy(&output.stderr)
                    ));
                }
                Err(e) => {
                    last_error = Some(format!("Failed to execute {}: {}", test_arg, e));
                }
            }
        }

        let duration = start.elapsed();
        let response_time_ms = duration.as_millis() as u64;

        Ok(ProviderHealthStatus {
            is_healthy: false,
            version: None,
            last_check: chrono::Utc::now(),
            error_message: last_error.or_else(|| Some("Command not available".to_string())),
            response_time_ms: Some(response_time_ms),
        })
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_generate_help_text (lines 669-698) ---[0m
    fn test_generate_help_text() {
        let config = CustomConfig {
            command: "my-tool".to_string(),
            args: vec!["--input".to_string(), "{prompt}".to_string()],
            env_vars: {
                let mut env = HashMap::new();
                env.insert("TOOL_MODE".to_string(), "production".to_string());
                env
            },
            working_directory: Some(PathBuf::from("/tmp")),
            timeout_seconds: Some(120),
            supports_json: true,
        };

        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let help = executor.generate_help_text(&identity, &task);

        assert!(help.contains("Custom Command Configuration:"));
        assert!(help.contains("Command: my-tool"));
        assert!(help.contains("Timeout: Some(120) seconds"));
        assert!(help.contains("Supports JSON: true"));
        assert!(help.contains("Available Placeholders:"));
        assert!(help.contains("{prompt} ->"));
        assert!(help.contains("{agent_id} ->"));
        assert!(help.contains("Environment Variables:"));
        assert!(help.contains("TOOL_MODE = production"));
    }

  ./crates/ccswarm/src/providers/custom.rs:392-448 method health_check <-> ./crates/ccswarm/src/providers/custom.rs:599-621 function test_parse_command_output_json
  Similarity: 77.10%

[36m--- ./crates/ccswarm/src/providers/custom.rs:health_check (lines 392-448) ---[0m
    async fn health_check(&self, working_dir: &Path) -> Result<ProviderHealthStatus> {
        let start = Instant::now();

        // Try to execute a simple version or help command
        let test_commands = vec!["--version", "--help", "-h", "-V"];
        let mut last_error = None;

        for test_arg in test_commands {
            let result = Command::new(&self.config.command)
                .arg(test_arg)
                .current_dir(working_dir)
                .output()
                .await;

            match result {
                Ok(output) if output.status.success() => {
                    let duration = start.elapsed();
                    let response_time_ms = duration.as_millis() as u64;

                    let version_info = String::from_utf8_lossy(&output.stdout)
                        .lines()
                        .next()
                        .unwrap_or("Custom command available")
                        .to_string();

                    return Ok(ProviderHealthStatus {
                        is_healthy: true,
                        version: Some(format!("{}: {}", self.config.command, version_info)),
                        last_check: chrono::Utc::now(),
                        error_message: None,
                        response_time_ms: Some(response_time_ms),
                    });
                }
                Ok(output) => {
                    last_error = Some(format!(
                        "Command failed with {}: {}",
                        test_arg,
                        String::from_utf8_lossy(&output.stderr)
                    ));
                }
                Err(e) => {
                    last_error = Some(format!("Failed to execute {}: {}", test_arg, e));
                }
            }
        }

        let duration = start.elapsed();
        let response_time_ms = duration.as_millis() as u64;

        Ok(ProviderHealthStatus {
            is_healthy: false,
            version: None,
            last_check: chrono::Utc::now(),
            error_message: last_error.or_else(|| Some("Command not available".to_string())),
            response_time_ms: Some(response_time_ms),
        })
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_parse_command_output_json (lines 599-621) ---[0m
    fn test_parse_command_output_json() {
        let config = CustomConfig {
            supports_json: true,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let json_output = r#"{"status": "success", "files_processed": 5}"#;
        let result = executor.parse_command_output(json_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        let output = result.output.as_object().unwrap();
        assert_eq!(output["format"], "json");
        assert_eq!(output["provider"], "custom");

        let json_result = &output["result"];
        assert_eq!(json_result["status"], "success");
        assert_eq!(json_result["files_processed"], 5);
    }

  ./crates/ccswarm/src/providers/custom.rs:219-263 method determine_success_from_output <-> ./crates/ccswarm/src/providers/custom.rs:540-572 function test_prepare_command_args
  Similarity: 70.40%

[36m--- ./crates/ccswarm/src/providers/custom.rs:determine_success_from_output (lines 219-263) ---[0m
    fn determine_success_from_output(&self, output: &str) -> bool {
        let output_lower = output.to_lowercase();

        // Check for explicit failure indicators
        let failure_indicators = [
            "error:",
            "failed:",
            "exception",
            "panic:",
            "fatal:",
            "command not found",
            "permission denied",
            "file not found",
            "syntax error",
            "compilation failed",
            "test failed",
        ];

        for indicator in &failure_indicators {
            if output_lower.contains(indicator) {
                return false;
            }
        }

        // Check for success indicators
        let success_indicators = [
            "success",
            "completed",
            "finished",
            "done",
            "ok",
            "passed",
            "test passed",
            "build successful",
        ];

        for indicator in &success_indicators {
            if output_lower.contains(indicator) {
                return true;
            }
        }

        // If no clear indicators, assume success if there's meaningful output
        !output.trim().is_empty()
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_prepare_command_args (lines 540-572) ---[0m
    fn test_prepare_command_args() {
        let config = CustomConfig {
            command: "my-tool".to_string(),
            args: vec![
                "--prompt".to_string(),
                "{prompt}".to_string(),
                "--agent".to_string(),
                "{agent_id}".to_string(),
                "--workspace".to_string(),
                "{workspace}".to_string(),
            ],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: None,
            supports_json: false,
        };

        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();
        let prompt = "Test prompt";
        let context = "{}";

        let args = executor.prepare_command_args(prompt, context, &identity, &task);

        assert_eq!(args.len(), 6);
        assert_eq!(args[0], "--prompt");
        assert_eq!(args[1], "Test prompt");
        assert_eq!(args[2], "--agent");
        assert_eq!(args[3], "test-custom-agent");
        assert_eq!(args[4], "--workspace");
        assert!(args[5].contains("/tmp") || args[5].contains("/var")); // Workspace path is a temp directory
    }

  ./crates/ccswarm/src/providers/custom.rs:24-51 method generate_context_info <-> ./crates/ccswarm/src/providers/custom.rs:171-216 method parse_command_output
  Similarity: 72.24%
  Classes: CustomExecutor <-> CustomExecutor

[36m--- ./crates/ccswarm/src/providers/custom.rs:generate_context_info (lines 24-51) ---[0m
    fn generate_context_info(&self, identity: &AgentIdentity, task: &Task) -> String {
        let context_json = serde_json::json!({
            "agent": {
                "id": identity.agent_id,
                "specialization": identity.specialization.name(),
                "workspace": identity.workspace_path.to_string_lossy(),
                "session_id": identity.session_id,
            },
            "task": {
                "id": task.id,
                "description": task.description,
                "details": task.details,
                "priority": format!("{:?}", task.priority),
                "type": format!("{:?}", task.task_type),
            },
            "boundaries": self.get_agent_boundaries(&identity.specialization),
        });

        serde_json::to_string_pretty(&context_json).unwrap_or_else(|_| {
            format!(
                "Agent: {} ({})\nTask: {}\nDescription: {}",
                identity.agent_id,
                identity.specialization.name(),
                task.id,
                task.description
            )
        })
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:parse_command_output (lines 171-216) ---[0m
    fn parse_command_output(
        &self,
        output: String,
        task: &Task,
        duration: std::time::Duration,
    ) -> TaskResult {
        if self.config.supports_json {
            // Try to parse as JSON
            if let Ok(json_value) = serde_json::from_str::<serde_json::Value>(&output) {
                return TaskResult {
                    success: true,
                    output: serde_json::json!({
                        "result": json_value,
                        "task_id": task.id,
                        "provider": "custom",
                        "command": self.config.command,
                        "format": "json"
                    }),
                    error: None,
                    duration,
                };
            }
        }

        // Fallback to text parsing
        let success = self.determine_success_from_output(&output);
        let error = if success {
            None
        } else {
            Some("Command output indicates failure".to_string())
        };

        TaskResult {
            success,
            output: serde_json::json!({
                "response": output,
                "task_id": task.id,
                "provider": "custom",
                "command": self.config.command,
                "format": "text",
                "working_directory": self.config.working_directory,
            }),
            error,
            duration,
        }
    }

  ./crates/ccswarm/src/providers/custom.rs:540-572 function test_prepare_command_args <-> ./crates/ccswarm/src/providers/custom.rs:669-698 function test_generate_help_text
  Similarity: 83.94%

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_prepare_command_args (lines 540-572) ---[0m
    fn test_prepare_command_args() {
        let config = CustomConfig {
            command: "my-tool".to_string(),
            args: vec![
                "--prompt".to_string(),
                "{prompt}".to_string(),
                "--agent".to_string(),
                "{agent_id}".to_string(),
                "--workspace".to_string(),
                "{workspace}".to_string(),
            ],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: None,
            supports_json: false,
        };

        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();
        let prompt = "Test prompt";
        let context = "{}";

        let args = executor.prepare_command_args(prompt, context, &identity, &task);

        assert_eq!(args.len(), 6);
        assert_eq!(args[0], "--prompt");
        assert_eq!(args[1], "Test prompt");
        assert_eq!(args[2], "--agent");
        assert_eq!(args[3], "test-custom-agent");
        assert_eq!(args[4], "--workspace");
        assert!(args[5].contains("/tmp") || args[5].contains("/var")); // Workspace path is a temp directory
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_generate_help_text (lines 669-698) ---[0m
    fn test_generate_help_text() {
        let config = CustomConfig {
            command: "my-tool".to_string(),
            args: vec!["--input".to_string(), "{prompt}".to_string()],
            env_vars: {
                let mut env = HashMap::new();
                env.insert("TOOL_MODE".to_string(), "production".to_string());
                env
            },
            working_directory: Some(PathBuf::from("/tmp")),
            timeout_seconds: Some(120),
            supports_json: true,
        };

        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let help = executor.generate_help_text(&identity, &task);

        assert!(help.contains("Custom Command Configuration:"));
        assert!(help.contains("Command: my-tool"));
        assert!(help.contains("Timeout: Some(120) seconds"));
        assert!(help.contains("Supports JSON: true"));
        assert!(help.contains("Available Placeholders:"));
        assert!(help.contains("{prompt} ->"));
        assert!(help.contains("{agent_id} ->"));
        assert!(help.contains("Environment Variables:"));
        assert!(help.contains("TOOL_MODE = production"));
    }

  ./crates/ccswarm/src/providers/custom.rs:266-300 method generate_help_text <-> ./crates/ccswarm/src/providers/custom.rs:540-572 function test_prepare_command_args
  Similarity: 75.87%

[36m--- ./crates/ccswarm/src/providers/custom.rs:generate_help_text (lines 266-300) ---[0m
    pub fn generate_help_text(&self, identity: &AgentIdentity, task: &Task) -> String {
        let mut help = String::new();

        help.push_str("Custom Command Configuration:\n");
        help.push_str(&format!("Command: {}\n", self.config.command));
        help.push_str(&format!("Arguments: {:?}\n", self.config.args));
        help.push_str(&format!(
            "Working Directory: {:?}\n",
            self.config.working_directory
        ));
        help.push_str(&format!(
            "Timeout: {:?} seconds\n",
            self.config.timeout_seconds
        ));
        help.push_str(&format!("Supports JSON: {}\n", self.config.supports_json));

        help.push_str("\nAvailable Placeholders:\n");
        help.push_str("{prompt} -> Task prompt will be substituted here\n");
        help.push_str(&format!("{{agent_id}} -> {} \n", identity.agent_id));
        help.push_str(&format!("{{task_id}} -> {}\n", task.id));
        help.push_str(&format!("{{task_description}} -> {}\n", task.description));
        help.push_str(&format!(
            "{{workspace}} -> {}\n",
            identity.workspace_path.display()
        ));

        if !self.config.env_vars.is_empty() {
            help.push_str("\nEnvironment Variables:\n");
            for (key, value) in &self.config.env_vars {
                help.push_str(&format!("{} = {}\n", key, value));
            }
        }

        help
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_prepare_command_args (lines 540-572) ---[0m
    fn test_prepare_command_args() {
        let config = CustomConfig {
            command: "my-tool".to_string(),
            args: vec![
                "--prompt".to_string(),
                "{prompt}".to_string(),
                "--agent".to_string(),
                "{agent_id}".to_string(),
                "--workspace".to_string(),
                "{workspace}".to_string(),
            ],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: None,
            supports_json: false,
        };

        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();
        let prompt = "Test prompt";
        let context = "{}";

        let args = executor.prepare_command_args(prompt, context, &identity, &task);

        assert_eq!(args.len(), 6);
        assert_eq!(args[0], "--prompt");
        assert_eq!(args[1], "Test prompt");
        assert_eq!(args[2], "--agent");
        assert_eq!(args[3], "test-custom-agent");
        assert_eq!(args[4], "--workspace");
        assert!(args[5].contains("/tmp") || args[5].contains("/var")); // Workspace path is a temp directory
    }

  ./crates/ccswarm/src/providers/custom.rs:266-300 method generate_help_text <-> ./crates/ccswarm/src/providers/custom.rs:669-698 function test_generate_help_text
  Similarity: 76.71%

[36m--- ./crates/ccswarm/src/providers/custom.rs:generate_help_text (lines 266-300) ---[0m
    pub fn generate_help_text(&self, identity: &AgentIdentity, task: &Task) -> String {
        let mut help = String::new();

        help.push_str("Custom Command Configuration:\n");
        help.push_str(&format!("Command: {}\n", self.config.command));
        help.push_str(&format!("Arguments: {:?}\n", self.config.args));
        help.push_str(&format!(
            "Working Directory: {:?}\n",
            self.config.working_directory
        ));
        help.push_str(&format!(
            "Timeout: {:?} seconds\n",
            self.config.timeout_seconds
        ));
        help.push_str(&format!("Supports JSON: {}\n", self.config.supports_json));

        help.push_str("\nAvailable Placeholders:\n");
        help.push_str("{prompt} -> Task prompt will be substituted here\n");
        help.push_str(&format!("{{agent_id}} -> {} \n", identity.agent_id));
        help.push_str(&format!("{{task_id}} -> {}\n", task.id));
        help.push_str(&format!("{{task_description}} -> {}\n", task.description));
        help.push_str(&format!(
            "{{workspace}} -> {}\n",
            identity.workspace_path.display()
        ));

        if !self.config.env_vars.is_empty() {
            help.push_str("\nEnvironment Variables:\n");
            for (key, value) in &self.config.env_vars {
                help.push_str(&format!("{} = {}\n", key, value));
            }
        }

        help
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_generate_help_text (lines 669-698) ---[0m
    fn test_generate_help_text() {
        let config = CustomConfig {
            command: "my-tool".to_string(),
            args: vec!["--input".to_string(), "{prompt}".to_string()],
            env_vars: {
                let mut env = HashMap::new();
                env.insert("TOOL_MODE".to_string(), "production".to_string());
                env
            },
            working_directory: Some(PathBuf::from("/tmp")),
            timeout_seconds: Some(120),
            supports_json: true,
        };

        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let help = executor.generate_help_text(&identity, &task);

        assert!(help.contains("Custom Command Configuration:"));
        assert!(help.contains("Command: my-tool"));
        assert!(help.contains("Timeout: Some(120) seconds"));
        assert!(help.contains("Supports JSON: true"));
        assert!(help.contains("Available Placeholders:"));
        assert!(help.contains("{prompt} ->"));
        assert!(help.contains("{agent_id} ->"));
        assert!(help.contains("Environment Variables:"));
        assert!(help.contains("TOOL_MODE = production"));
    }

  ./crates/ccswarm/src/providers/custom.rs:540-572 function test_prepare_command_args <-> ./crates/ccswarm/src/providers/custom.rs:599-621 function test_parse_command_output_json
  Similarity: 84.47%

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_prepare_command_args (lines 540-572) ---[0m
    fn test_prepare_command_args() {
        let config = CustomConfig {
            command: "my-tool".to_string(),
            args: vec![
                "--prompt".to_string(),
                "{prompt}".to_string(),
                "--agent".to_string(),
                "{agent_id}".to_string(),
                "--workspace".to_string(),
                "{workspace}".to_string(),
            ],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: None,
            supports_json: false,
        };

        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();
        let prompt = "Test prompt";
        let context = "{}";

        let args = executor.prepare_command_args(prompt, context, &identity, &task);

        assert_eq!(args.len(), 6);
        assert_eq!(args[0], "--prompt");
        assert_eq!(args[1], "Test prompt");
        assert_eq!(args[2], "--agent");
        assert_eq!(args[3], "test-custom-agent");
        assert_eq!(args[4], "--workspace");
        assert!(args[5].contains("/tmp") || args[5].contains("/var")); // Workspace path is a temp directory
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_parse_command_output_json (lines 599-621) ---[0m
    fn test_parse_command_output_json() {
        let config = CustomConfig {
            supports_json: true,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let json_output = r#"{"status": "success", "files_processed": 5}"#;
        let result = executor.parse_command_output(json_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        let output = result.output.as_object().unwrap();
        assert_eq!(output["format"], "json");
        assert_eq!(output["provider"], "custom");

        let json_result = &output["result"];
        assert_eq!(json_result["status"], "success");
        assert_eq!(json_result["files_processed"], 5);
    }

  ./crates/ccswarm/src/providers/custom.rs:575-596 function test_determine_success_from_output <-> ./crates/ccswarm/src/providers/custom.rs:669-698 function test_generate_help_text
  Similarity: 90.14%

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_determine_success_from_output (lines 575-596) ---[0m
    fn test_determine_success_from_output() {
        let config = CustomConfig::default();
        let executor = CustomExecutor::new(config);

        // Test success cases
        assert!(executor.determine_success_from_output("Task completed successfully"));
        assert!(executor.determine_success_from_output("Build finished with success"));
        assert!(executor.determine_success_from_output("All tests passed"));
        assert!(executor.determine_success_from_output("Done processing files"));

        // Test failure cases
        assert!(!executor.determine_success_from_output("Error: File not found"));
        assert!(!executor.determine_success_from_output("failed: to compile"));
        assert!(!executor.determine_success_from_output("Exception occurred"));
        assert!(!executor.determine_success_from_output("Fatal: cannot proceed"));

        // Test neutral case with content
        assert!(executor.determine_success_from_output("Processing files..."));

        // Test empty case
        assert!(!executor.determine_success_from_output(""));
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_generate_help_text (lines 669-698) ---[0m
    fn test_generate_help_text() {
        let config = CustomConfig {
            command: "my-tool".to_string(),
            args: vec!["--input".to_string(), "{prompt}".to_string()],
            env_vars: {
                let mut env = HashMap::new();
                env.insert("TOOL_MODE".to_string(), "production".to_string());
                env
            },
            working_directory: Some(PathBuf::from("/tmp")),
            timeout_seconds: Some(120),
            supports_json: true,
        };

        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let help = executor.generate_help_text(&identity, &task);

        assert!(help.contains("Custom Command Configuration:"));
        assert!(help.contains("Command: my-tool"));
        assert!(help.contains("Timeout: Some(120) seconds"));
        assert!(help.contains("Supports JSON: true"));
        assert!(help.contains("Available Placeholders:"));
        assert!(help.contains("{prompt} ->"));
        assert!(help.contains("{agent_id} ->"));
        assert!(help.contains("Environment Variables:"));
        assert!(help.contains("TOOL_MODE = production"));
    }

  ./crates/ccswarm/src/providers/custom.rs:219-263 method determine_success_from_output <-> ./crates/ccswarm/src/providers/custom.rs:305-324 method execute_prompt
  Similarity: 71.18%
  Classes: CustomExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/custom.rs:determine_success_from_output (lines 219-263) ---[0m
    fn determine_success_from_output(&self, output: &str) -> bool {
        let output_lower = output.to_lowercase();

        // Check for explicit failure indicators
        let failure_indicators = [
            "error:",
            "failed:",
            "exception",
            "panic:",
            "fatal:",
            "command not found",
            "permission denied",
            "file not found",
            "syntax error",
            "compilation failed",
            "test failed",
        ];

        for indicator in &failure_indicators {
            if output_lower.contains(indicator) {
                return false;
            }
        }

        // Check for success indicators
        let success_indicators = [
            "success",
            "completed",
            "finished",
            "done",
            "ok",
            "passed",
            "test passed",
            "build successful",
        ];

        for indicator in &success_indicators {
            if output_lower.contains(indicator) {
                return true;
            }
        }

        // If no clear indicators, assume success if there's meaningful output
        !output.trim().is_empty()
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:execute_prompt (lines 305-324) ---[0m
    async fn execute_prompt(
        &self,
        prompt: &str,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<String> {
        // Create a mock task for prompt execution
        let mock_task = Task::new(
            format!("prompt-{}", uuid::Uuid::new_v4()),
            "Direct prompt execution".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let context = self.generate_context_info(identity, &mock_task);
        let args = self.prepare_command_args(prompt, &context, identity, &mock_task);

        self.execute_custom_command(args, working_dir, identity)
            .await
    }

  ./crates/ccswarm/src/providers/custom.rs:599-621 function test_parse_command_output_json <-> ./crates/ccswarm/src/providers/custom.rs:669-698 function test_generate_help_text
  Similarity: 87.25%

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_parse_command_output_json (lines 599-621) ---[0m
    fn test_parse_command_output_json() {
        let config = CustomConfig {
            supports_json: true,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let json_output = r#"{"status": "success", "files_processed": 5}"#;
        let result = executor.parse_command_output(json_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        let output = result.output.as_object().unwrap();
        assert_eq!(output["format"], "json");
        assert_eq!(output["provider"], "custom");

        let json_result = &output["result"];
        assert_eq!(json_result["status"], "success");
        assert_eq!(json_result["files_processed"], 5);
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_generate_help_text (lines 669-698) ---[0m
    fn test_generate_help_text() {
        let config = CustomConfig {
            command: "my-tool".to_string(),
            args: vec!["--input".to_string(), "{prompt}".to_string()],
            env_vars: {
                let mut env = HashMap::new();
                env.insert("TOOL_MODE".to_string(), "production".to_string());
                env
            },
            working_directory: Some(PathBuf::from("/tmp")),
            timeout_seconds: Some(120),
            supports_json: true,
        };

        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let help = executor.generate_help_text(&identity, &task);

        assert!(help.contains("Custom Command Configuration:"));
        assert!(help.contains("Command: my-tool"));
        assert!(help.contains("Timeout: Some(120) seconds"));
        assert!(help.contains("Supports JSON: true"));
        assert!(help.contains("Available Placeholders:"));
        assert!(help.contains("{prompt} ->"));
        assert!(help.contains("{agent_id} ->"));
        assert!(help.contains("Environment Variables:"));
        assert!(help.contains("TOOL_MODE = production"));
    }

  ./crates/ccswarm/src/providers/custom.rs:266-300 method generate_help_text <-> ./crates/ccswarm/src/providers/custom.rs:599-621 function test_parse_command_output_json
  Similarity: 77.67%

[36m--- ./crates/ccswarm/src/providers/custom.rs:generate_help_text (lines 266-300) ---[0m
    pub fn generate_help_text(&self, identity: &AgentIdentity, task: &Task) -> String {
        let mut help = String::new();

        help.push_str("Custom Command Configuration:\n");
        help.push_str(&format!("Command: {}\n", self.config.command));
        help.push_str(&format!("Arguments: {:?}\n", self.config.args));
        help.push_str(&format!(
            "Working Directory: {:?}\n",
            self.config.working_directory
        ));
        help.push_str(&format!(
            "Timeout: {:?} seconds\n",
            self.config.timeout_seconds
        ));
        help.push_str(&format!("Supports JSON: {}\n", self.config.supports_json));

        help.push_str("\nAvailable Placeholders:\n");
        help.push_str("{prompt} -> Task prompt will be substituted here\n");
        help.push_str(&format!("{{agent_id}} -> {} \n", identity.agent_id));
        help.push_str(&format!("{{task_id}} -> {}\n", task.id));
        help.push_str(&format!("{{task_description}} -> {}\n", task.description));
        help.push_str(&format!(
            "{{workspace}} -> {}\n",
            identity.workspace_path.display()
        ));

        if !self.config.env_vars.is_empty() {
            help.push_str("\nEnvironment Variables:\n");
            for (key, value) in &self.config.env_vars {
                help.push_str(&format!("{} = {}\n", key, value));
            }
        }

        help
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_parse_command_output_json (lines 599-621) ---[0m
    fn test_parse_command_output_json() {
        let config = CustomConfig {
            supports_json: true,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let json_output = r#"{"status": "success", "files_processed": 5}"#;
        let result = executor.parse_command_output(json_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        let output = result.output.as_object().unwrap();
        assert_eq!(output["format"], "json");
        assert_eq!(output["provider"], "custom");

        let json_result = &output["result"];
        assert_eq!(json_result["status"], "success");
        assert_eq!(json_result["files_processed"], 5);
    }

  ./crates/ccswarm/src/providers/custom.rs:266-300 method generate_help_text <-> ./crates/ccswarm/src/providers/custom.rs:575-596 function test_determine_success_from_output
  Similarity: 78.40%

[36m--- ./crates/ccswarm/src/providers/custom.rs:generate_help_text (lines 266-300) ---[0m
    pub fn generate_help_text(&self, identity: &AgentIdentity, task: &Task) -> String {
        let mut help = String::new();

        help.push_str("Custom Command Configuration:\n");
        help.push_str(&format!("Command: {}\n", self.config.command));
        help.push_str(&format!("Arguments: {:?}\n", self.config.args));
        help.push_str(&format!(
            "Working Directory: {:?}\n",
            self.config.working_directory
        ));
        help.push_str(&format!(
            "Timeout: {:?} seconds\n",
            self.config.timeout_seconds
        ));
        help.push_str(&format!("Supports JSON: {}\n", self.config.supports_json));

        help.push_str("\nAvailable Placeholders:\n");
        help.push_str("{prompt} -> Task prompt will be substituted here\n");
        help.push_str(&format!("{{agent_id}} -> {} \n", identity.agent_id));
        help.push_str(&format!("{{task_id}} -> {}\n", task.id));
        help.push_str(&format!("{{task_description}} -> {}\n", task.description));
        help.push_str(&format!(
            "{{workspace}} -> {}\n",
            identity.workspace_path.display()
        ));

        if !self.config.env_vars.is_empty() {
            help.push_str("\nEnvironment Variables:\n");
            for (key, value) in &self.config.env_vars {
                help.push_str(&format!("{} = {}\n", key, value));
            }
        }

        help
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_determine_success_from_output (lines 575-596) ---[0m
    fn test_determine_success_from_output() {
        let config = CustomConfig::default();
        let executor = CustomExecutor::new(config);

        // Test success cases
        assert!(executor.determine_success_from_output("Task completed successfully"));
        assert!(executor.determine_success_from_output("Build finished with success"));
        assert!(executor.determine_success_from_output("All tests passed"));
        assert!(executor.determine_success_from_output("Done processing files"));

        // Test failure cases
        assert!(!executor.determine_success_from_output("Error: File not found"));
        assert!(!executor.determine_success_from_output("failed: to compile"));
        assert!(!executor.determine_success_from_output("Exception occurred"));
        assert!(!executor.determine_success_from_output("Fatal: cannot proceed"));

        // Test neutral case with content
        assert!(executor.determine_success_from_output("Processing files..."));

        // Test empty case
        assert!(!executor.determine_success_from_output(""));
    }

  ./crates/ccswarm/src/providers/custom.rs:540-572 function test_prepare_command_args <-> ./crates/ccswarm/src/providers/custom.rs:575-596 function test_determine_success_from_output
  Similarity: 80.26%

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_prepare_command_args (lines 540-572) ---[0m
    fn test_prepare_command_args() {
        let config = CustomConfig {
            command: "my-tool".to_string(),
            args: vec![
                "--prompt".to_string(),
                "{prompt}".to_string(),
                "--agent".to_string(),
                "{agent_id}".to_string(),
                "--workspace".to_string(),
                "{workspace}".to_string(),
            ],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: None,
            supports_json: false,
        };

        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();
        let prompt = "Test prompt";
        let context = "{}";

        let args = executor.prepare_command_args(prompt, context, &identity, &task);

        assert_eq!(args.len(), 6);
        assert_eq!(args[0], "--prompt");
        assert_eq!(args[1], "Test prompt");
        assert_eq!(args[2], "--agent");
        assert_eq!(args[3], "test-custom-agent");
        assert_eq!(args[4], "--workspace");
        assert!(args[5].contains("/tmp") || args[5].contains("/var")); // Workspace path is a temp directory
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_determine_success_from_output (lines 575-596) ---[0m
    fn test_determine_success_from_output() {
        let config = CustomConfig::default();
        let executor = CustomExecutor::new(config);

        // Test success cases
        assert!(executor.determine_success_from_output("Task completed successfully"));
        assert!(executor.determine_success_from_output("Build finished with success"));
        assert!(executor.determine_success_from_output("All tests passed"));
        assert!(executor.determine_success_from_output("Done processing files"));

        // Test failure cases
        assert!(!executor.determine_success_from_output("Error: File not found"));
        assert!(!executor.determine_success_from_output("failed: to compile"));
        assert!(!executor.determine_success_from_output("Exception occurred"));
        assert!(!executor.determine_success_from_output("Fatal: cannot proceed"));

        // Test neutral case with content
        assert!(executor.determine_success_from_output("Processing files..."));

        // Test empty case
        assert!(!executor.determine_success_from_output(""));
    }

  ./crates/ccswarm/src/providers/custom.rs:219-263 method determine_success_from_output <-> ./crates/ccswarm/src/providers/custom.rs:523-537 function test_generate_context_info
  Similarity: 73.35%

[36m--- ./crates/ccswarm/src/providers/custom.rs:determine_success_from_output (lines 219-263) ---[0m
    fn determine_success_from_output(&self, output: &str) -> bool {
        let output_lower = output.to_lowercase();

        // Check for explicit failure indicators
        let failure_indicators = [
            "error:",
            "failed:",
            "exception",
            "panic:",
            "fatal:",
            "command not found",
            "permission denied",
            "file not found",
            "syntax error",
            "compilation failed",
            "test failed",
        ];

        for indicator in &failure_indicators {
            if output_lower.contains(indicator) {
                return false;
            }
        }

        // Check for success indicators
        let success_indicators = [
            "success",
            "completed",
            "finished",
            "done",
            "ok",
            "passed",
            "test passed",
            "build successful",
        ];

        for indicator in &success_indicators {
            if output_lower.contains(indicator) {
                return true;
            }
        }

        // If no clear indicators, assume success if there's meaningful output
        !output.trim().is_empty()
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_generate_context_info (lines 523-537) ---[0m
    fn test_generate_context_info() {
        let config = CustomConfig::default();
        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let context = executor.generate_context_info(&identity, &task);

        // Should be valid JSON
        let parsed: serde_json::Value = serde_json::from_str(&context).unwrap();
        assert_eq!(parsed["agent"]["id"], "test-custom-agent");
        assert_eq!(parsed["agent"]["specialization"], "DevOps");
        assert_eq!(parsed["task"]["description"], "Deploy application");
        assert!(parsed["boundaries"].is_array());
    }

  ./crates/ccswarm/src/providers/custom.rs:171-216 method parse_command_output <-> ./crates/ccswarm/src/providers/custom.rs:479-494 function create_test_identity
  Similarity: 70.55%

[36m--- ./crates/ccswarm/src/providers/custom.rs:parse_command_output (lines 171-216) ---[0m
    fn parse_command_output(
        &self,
        output: String,
        task: &Task,
        duration: std::time::Duration,
    ) -> TaskResult {
        if self.config.supports_json {
            // Try to parse as JSON
            if let Ok(json_value) = serde_json::from_str::<serde_json::Value>(&output) {
                return TaskResult {
                    success: true,
                    output: serde_json::json!({
                        "result": json_value,
                        "task_id": task.id,
                        "provider": "custom",
                        "command": self.config.command,
                        "format": "json"
                    }),
                    error: None,
                    duration,
                };
            }
        }

        // Fallback to text parsing
        let success = self.determine_success_from_output(&output);
        let error = if success {
            None
        } else {
            Some("Command output indicates failure".to_string())
        };

        TaskResult {
            success,
            output: serde_json::json!({
                "response": output,
                "task_id": task.id,
                "provider": "custom",
                "command": self.config.command,
                "format": "text",
                "working_directory": self.config.working_directory,
            }),
            error,
            duration,
        }
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:create_test_identity (lines 479-494) ---[0m
    fn create_test_identity() -> AgentIdentity {
        let temp_dir = TempDir::new().unwrap();
        AgentIdentity {
            agent_id: "test-custom-agent".to_string(),
            specialization: AgentRole::DevOps {
                technologies: vec!["Docker".to_string(), "Kubernetes".to_string()],
                responsibilities: vec!["Infrastructure".to_string()],
                boundaries: vec!["No app development".to_string()],
            },
            workspace_path: temp_dir.path().to_path_buf(),
            env_vars: HashMap::new(),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: "12345".to_string(),
            initialized_at: chrono::Utc::now(),
        }
    }

  ./crates/ccswarm/src/providers/custom.rs:540-572 function test_prepare_command_args <-> ./crates/ccswarm/src/providers/custom.rs:624-643 function test_parse_command_output_text
  Similarity: 80.03%

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_prepare_command_args (lines 540-572) ---[0m
    fn test_prepare_command_args() {
        let config = CustomConfig {
            command: "my-tool".to_string(),
            args: vec![
                "--prompt".to_string(),
                "{prompt}".to_string(),
                "--agent".to_string(),
                "{agent_id}".to_string(),
                "--workspace".to_string(),
                "{workspace}".to_string(),
            ],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: None,
            supports_json: false,
        };

        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();
        let prompt = "Test prompt";
        let context = "{}";

        let args = executor.prepare_command_args(prompt, context, &identity, &task);

        assert_eq!(args.len(), 6);
        assert_eq!(args[0], "--prompt");
        assert_eq!(args[1], "Test prompt");
        assert_eq!(args[2], "--agent");
        assert_eq!(args[3], "test-custom-agent");
        assert_eq!(args[4], "--workspace");
        assert!(args[5].contains("/tmp") || args[5].contains("/var")); // Workspace path is a temp directory
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_parse_command_output_text (lines 624-643) ---[0m
    fn test_parse_command_output_text() {
        let config = CustomConfig {
            supports_json: false,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(200);

        let text_output = "Deployment completed successfully!";
        let result = executor.parse_command_output(text_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        let output = result.output.as_object().unwrap();
        assert_eq!(output["format"], "text");
        assert_eq!(output["provider"], "custom");
        assert_eq!(output["response"], "Deployment completed successfully!");
    }

  ./crates/ccswarm/src/providers/custom.rs:540-572 function test_prepare_command_args <-> ./crates/ccswarm/src/providers/custom.rs:646-666 function test_get_capabilities
  Similarity: 76.62%

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_prepare_command_args (lines 540-572) ---[0m
    fn test_prepare_command_args() {
        let config = CustomConfig {
            command: "my-tool".to_string(),
            args: vec![
                "--prompt".to_string(),
                "{prompt}".to_string(),
                "--agent".to_string(),
                "{agent_id}".to_string(),
                "--workspace".to_string(),
                "{workspace}".to_string(),
            ],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: None,
            supports_json: false,
        };

        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();
        let prompt = "Test prompt";
        let context = "{}";

        let args = executor.prepare_command_args(prompt, context, &identity, &task);

        assert_eq!(args.len(), 6);
        assert_eq!(args[0], "--prompt");
        assert_eq!(args[1], "Test prompt");
        assert_eq!(args[2], "--agent");
        assert_eq!(args[3], "test-custom-agent");
        assert_eq!(args[4], "--workspace");
        assert!(args[5].contains("/tmp") || args[5].contains("/var")); // Workspace path is a temp directory
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_get_capabilities (lines 646-666) ---[0m
    fn test_get_capabilities() {
        let config = CustomConfig {
            supports_json: true,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert!(capabilities.max_context_length.is_none());
        assert!(capabilities
            .supported_languages
            .contains(&"text".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"json".to_string()));
    }

  ./crates/ccswarm/src/providers/custom.rs:624-643 function test_parse_command_output_text <-> ./crates/ccswarm/src/providers/custom.rs:669-698 function test_generate_help_text
  Similarity: 81.35%

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_parse_command_output_text (lines 624-643) ---[0m
    fn test_parse_command_output_text() {
        let config = CustomConfig {
            supports_json: false,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(200);

        let text_output = "Deployment completed successfully!";
        let result = executor.parse_command_output(text_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        let output = result.output.as_object().unwrap();
        assert_eq!(output["format"], "text");
        assert_eq!(output["provider"], "custom");
        assert_eq!(output["response"], "Deployment completed successfully!");
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_generate_help_text (lines 669-698) ---[0m
    fn test_generate_help_text() {
        let config = CustomConfig {
            command: "my-tool".to_string(),
            args: vec!["--input".to_string(), "{prompt}".to_string()],
            env_vars: {
                let mut env = HashMap::new();
                env.insert("TOOL_MODE".to_string(), "production".to_string());
                env
            },
            working_directory: Some(PathBuf::from("/tmp")),
            timeout_seconds: Some(120),
            supports_json: true,
        };

        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let help = executor.generate_help_text(&identity, &task);

        assert!(help.contains("Custom Command Configuration:"));
        assert!(help.contains("Command: my-tool"));
        assert!(help.contains("Timeout: Some(120) seconds"));
        assert!(help.contains("Supports JSON: true"));
        assert!(help.contains("Available Placeholders:"));
        assert!(help.contains("{prompt} ->"));
        assert!(help.contains("{agent_id} ->"));
        assert!(help.contains("Environment Variables:"));
        assert!(help.contains("TOOL_MODE = production"));
    }

  ./crates/ccswarm/src/providers/custom.rs:266-300 method generate_help_text <-> ./crates/ccswarm/src/providers/custom.rs:624-643 function test_parse_command_output_text
  Similarity: 73.24%

[36m--- ./crates/ccswarm/src/providers/custom.rs:generate_help_text (lines 266-300) ---[0m
    pub fn generate_help_text(&self, identity: &AgentIdentity, task: &Task) -> String {
        let mut help = String::new();

        help.push_str("Custom Command Configuration:\n");
        help.push_str(&format!("Command: {}\n", self.config.command));
        help.push_str(&format!("Arguments: {:?}\n", self.config.args));
        help.push_str(&format!(
            "Working Directory: {:?}\n",
            self.config.working_directory
        ));
        help.push_str(&format!(
            "Timeout: {:?} seconds\n",
            self.config.timeout_seconds
        ));
        help.push_str(&format!("Supports JSON: {}\n", self.config.supports_json));

        help.push_str("\nAvailable Placeholders:\n");
        help.push_str("{prompt} -> Task prompt will be substituted here\n");
        help.push_str(&format!("{{agent_id}} -> {} \n", identity.agent_id));
        help.push_str(&format!("{{task_id}} -> {}\n", task.id));
        help.push_str(&format!("{{task_description}} -> {}\n", task.description));
        help.push_str(&format!(
            "{{workspace}} -> {}\n",
            identity.workspace_path.display()
        ));

        if !self.config.env_vars.is_empty() {
            help.push_str("\nEnvironment Variables:\n");
            for (key, value) in &self.config.env_vars {
                help.push_str(&format!("{} = {}\n", key, value));
            }
        }

        help
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_parse_command_output_text (lines 624-643) ---[0m
    fn test_parse_command_output_text() {
        let config = CustomConfig {
            supports_json: false,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(200);

        let text_output = "Deployment completed successfully!";
        let result = executor.parse_command_output(text_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        let output = result.output.as_object().unwrap();
        assert_eq!(output["format"], "text");
        assert_eq!(output["provider"], "custom");
        assert_eq!(output["response"], "Deployment completed successfully!");
    }

  ./crates/ccswarm/src/providers/custom.rs:71-92 method prepare_command_args <-> ./crates/ccswarm/src/providers/custom.rs:450-467 method get_capabilities
  Similarity: 97.89%
  Classes: CustomExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/custom.rs:prepare_command_args (lines 71-92) ---[0m
    fn prepare_command_args(
        &self,
        prompt: &str,
        context: &str,
        identity: &AgentIdentity,
        task: &Task,
    ) -> Vec<String> {
        self.config
            .args
            .iter()
            .map(|arg| {
                arg.replace("{prompt}", prompt)
                    .replace("{context}", context)
                    .replace("{agent_id}", &identity.agent_id)
                    .replace("{task_id}", &task.id)
                    .replace("{workspace}", &identity.workspace_path.to_string_lossy())
                    .replace("{specialization}", identity.specialization.name())
                    .replace("{task_type}", &format!("{:?}", task.task_type))
                    .replace("{priority}", &format!("{:?}", task.priority))
            })
            .collect()
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:get_capabilities (lines 450-467) ---[0m
    fn get_capabilities(&self) -> ProviderCapabilities {
        ProviderCapabilities {
            supports_json_output: self.config.supports_json,
            supports_streaming: false, // Custom commands typically don't support streaming
            supports_file_operations: true, // Depends on the command, but assume yes
            supports_git_operations: true, // Depends on the command, but assume yes
            supports_code_execution: true, // Depends on the command, but assume yes
            max_context_length: None,  // Unknown for custom commands
            supported_languages: vec![
                // Generic list - actual support depends on the custom command
                "text".to_string(),
                "json".to_string(),
                "yaml".to_string(),
                "markdown".to_string(),
                "bash".to_string(),
            ],
        }
    }

  ./crates/ccswarm/src/providers/custom.rs:24-51 method generate_context_info <-> ./crates/ccswarm/src/providers/custom.rs:305-324 method execute_prompt
  Similarity: 77.59%
  Classes: CustomExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/custom.rs:generate_context_info (lines 24-51) ---[0m
    fn generate_context_info(&self, identity: &AgentIdentity, task: &Task) -> String {
        let context_json = serde_json::json!({
            "agent": {
                "id": identity.agent_id,
                "specialization": identity.specialization.name(),
                "workspace": identity.workspace_path.to_string_lossy(),
                "session_id": identity.session_id,
            },
            "task": {
                "id": task.id,
                "description": task.description,
                "details": task.details,
                "priority": format!("{:?}", task.priority),
                "type": format!("{:?}", task.task_type),
            },
            "boundaries": self.get_agent_boundaries(&identity.specialization),
        });

        serde_json::to_string_pretty(&context_json).unwrap_or_else(|_| {
            format!(
                "Agent: {} ({})\nTask: {}\nDescription: {}",
                identity.agent_id,
                identity.specialization.name(),
                task.id,
                task.description
            )
        })
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:execute_prompt (lines 305-324) ---[0m
    async fn execute_prompt(
        &self,
        prompt: &str,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<String> {
        // Create a mock task for prompt execution
        let mock_task = Task::new(
            format!("prompt-{}", uuid::Uuid::new_v4()),
            "Direct prompt execution".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let context = self.generate_context_info(identity, &mock_task);
        let args = self.prepare_command_args(prompt, &context, identity, &mock_task);

        self.execute_custom_command(args, working_dir, identity)
            .await
    }

  ./crates/ccswarm/src/providers/custom.rs:523-537 function test_generate_context_info <-> ./crates/ccswarm/src/providers/custom.rs:540-572 function test_prepare_command_args
  Similarity: 77.39%

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_generate_context_info (lines 523-537) ---[0m
    fn test_generate_context_info() {
        let config = CustomConfig::default();
        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let context = executor.generate_context_info(&identity, &task);

        // Should be valid JSON
        let parsed: serde_json::Value = serde_json::from_str(&context).unwrap();
        assert_eq!(parsed["agent"]["id"], "test-custom-agent");
        assert_eq!(parsed["agent"]["specialization"], "DevOps");
        assert_eq!(parsed["task"]["description"], "Deploy application");
        assert!(parsed["boundaries"].is_array());
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_prepare_command_args (lines 540-572) ---[0m
    fn test_prepare_command_args() {
        let config = CustomConfig {
            command: "my-tool".to_string(),
            args: vec![
                "--prompt".to_string(),
                "{prompt}".to_string(),
                "--agent".to_string(),
                "{agent_id}".to_string(),
                "--workspace".to_string(),
                "{workspace}".to_string(),
            ],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: None,
            supports_json: false,
        };

        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();
        let prompt = "Test prompt";
        let context = "{}";

        let args = executor.prepare_command_args(prompt, context, &identity, &task);

        assert_eq!(args.len(), 6);
        assert_eq!(args[0], "--prompt");
        assert_eq!(args[1], "Test prompt");
        assert_eq!(args[2], "--agent");
        assert_eq!(args[3], "test-custom-agent");
        assert_eq!(args[4], "--workspace");
        assert!(args[5].contains("/tmp") || args[5].contains("/var")); // Workspace path is a temp directory
    }

  ./crates/ccswarm/src/providers/custom.rs:599-621 function test_parse_command_output_json <-> ./crates/ccswarm/src/providers/custom.rs:624-643 function test_parse_command_output_text
  Similarity: 85.10%

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_parse_command_output_json (lines 599-621) ---[0m
    fn test_parse_command_output_json() {
        let config = CustomConfig {
            supports_json: true,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let json_output = r#"{"status": "success", "files_processed": 5}"#;
        let result = executor.parse_command_output(json_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        let output = result.output.as_object().unwrap();
        assert_eq!(output["format"], "json");
        assert_eq!(output["provider"], "custom");

        let json_result = &output["result"];
        assert_eq!(json_result["status"], "success");
        assert_eq!(json_result["files_processed"], 5);
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_parse_command_output_text (lines 624-643) ---[0m
    fn test_parse_command_output_text() {
        let config = CustomConfig {
            supports_json: false,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(200);

        let text_output = "Deployment completed successfully!";
        let result = executor.parse_command_output(text_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        let output = result.output.as_object().unwrap();
        assert_eq!(output["format"], "text");
        assert_eq!(output["provider"], "custom");
        assert_eq!(output["response"], "Deployment completed successfully!");
    }

  ./crates/ccswarm/src/providers/custom.rs:54-68 method get_agent_boundaries <-> ./crates/ccswarm/src/providers/custom.rs:71-92 method prepare_command_args
  Similarity: 97.12%
  Classes: CustomExecutor <-> CustomExecutor

[36m--- ./crates/ccswarm/src/providers/custom.rs:get_agent_boundaries (lines 54-68) ---[0m
    fn get_agent_boundaries(&self, specialization: &crate::identity::AgentRole) -> Vec<String> {
        match specialization {
            crate::identity::AgentRole::Frontend { boundaries, .. } => boundaries.clone(),
            crate::identity::AgentRole::Backend { boundaries, .. } => boundaries.clone(),
            crate::identity::AgentRole::DevOps { boundaries, .. } => boundaries.clone(),
            crate::identity::AgentRole::QA { boundaries, .. } => boundaries.clone(),
            crate::identity::AgentRole::Master { .. } => {
                vec![
                    "No direct code implementation".to_string(),
                    "Coordination only".to_string(),
                ]
            }
            crate::identity::AgentRole::Search { boundaries, .. } => boundaries.clone(),
        }
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:prepare_command_args (lines 71-92) ---[0m
    fn prepare_command_args(
        &self,
        prompt: &str,
        context: &str,
        identity: &AgentIdentity,
        task: &Task,
    ) -> Vec<String> {
        self.config
            .args
            .iter()
            .map(|arg| {
                arg.replace("{prompt}", prompt)
                    .replace("{context}", context)
                    .replace("{agent_id}", &identity.agent_id)
                    .replace("{task_id}", &task.id)
                    .replace("{workspace}", &identity.workspace_path.to_string_lossy())
                    .replace("{specialization}", identity.specialization.name())
                    .replace("{task_type}", &format!("{:?}", task.task_type))
                    .replace("{priority}", &format!("{:?}", task.priority))
            })
            .collect()
    }

  ./crates/ccswarm/src/providers/custom.rs:575-596 function test_determine_success_from_output <-> ./crates/ccswarm/src/providers/custom.rs:599-621 function test_parse_command_output_json
  Similarity: 77.41%

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_determine_success_from_output (lines 575-596) ---[0m
    fn test_determine_success_from_output() {
        let config = CustomConfig::default();
        let executor = CustomExecutor::new(config);

        // Test success cases
        assert!(executor.determine_success_from_output("Task completed successfully"));
        assert!(executor.determine_success_from_output("Build finished with success"));
        assert!(executor.determine_success_from_output("All tests passed"));
        assert!(executor.determine_success_from_output("Done processing files"));

        // Test failure cases
        assert!(!executor.determine_success_from_output("Error: File not found"));
        assert!(!executor.determine_success_from_output("failed: to compile"));
        assert!(!executor.determine_success_from_output("Exception occurred"));
        assert!(!executor.determine_success_from_output("Fatal: cannot proceed"));

        // Test neutral case with content
        assert!(executor.determine_success_from_output("Processing files..."));

        // Test empty case
        assert!(!executor.determine_success_from_output(""));
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_parse_command_output_json (lines 599-621) ---[0m
    fn test_parse_command_output_json() {
        let config = CustomConfig {
            supports_json: true,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let json_output = r#"{"status": "success", "files_processed": 5}"#;
        let result = executor.parse_command_output(json_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        let output = result.output.as_object().unwrap();
        assert_eq!(output["format"], "json");
        assert_eq!(output["provider"], "custom");

        let json_result = &output["result"];
        assert_eq!(json_result["status"], "success");
        assert_eq!(json_result["files_processed"], 5);
    }

  ./crates/ccswarm/src/providers/custom.rs:24-51 method generate_context_info <-> ./crates/ccswarm/src/providers/custom.rs:507-520 function test_custom_executor_creation
  Similarity: 82.05%

[36m--- ./crates/ccswarm/src/providers/custom.rs:generate_context_info (lines 24-51) ---[0m
    fn generate_context_info(&self, identity: &AgentIdentity, task: &Task) -> String {
        let context_json = serde_json::json!({
            "agent": {
                "id": identity.agent_id,
                "specialization": identity.specialization.name(),
                "workspace": identity.workspace_path.to_string_lossy(),
                "session_id": identity.session_id,
            },
            "task": {
                "id": task.id,
                "description": task.description,
                "details": task.details,
                "priority": format!("{:?}", task.priority),
                "type": format!("{:?}", task.task_type),
            },
            "boundaries": self.get_agent_boundaries(&identity.specialization),
        });

        serde_json::to_string_pretty(&context_json).unwrap_or_else(|_| {
            format!(
                "Agent: {} ({})\nTask: {}\nDescription: {}",
                identity.agent_id,
                identity.specialization.name(),
                task.id,
                task.description
            )
        })
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_custom_executor_creation (lines 507-520) ---[0m
    fn test_custom_executor_creation() {
        let config = CustomConfig {
            command: "echo".to_string(),
            args: vec!["{prompt}".to_string()],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: Some(60),
            supports_json: false,
        };

        let executor = CustomExecutor::new(config);
        assert_eq!(executor.config.command, "echo");
        assert_eq!(executor.config.timeout_seconds, Some(60));
    }

  ./crates/ccswarm/src/providers/custom.rs:523-537 function test_generate_context_info <-> ./crates/ccswarm/src/providers/custom.rs:669-698 function test_generate_help_text
  Similarity: 75.79%

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_generate_context_info (lines 523-537) ---[0m
    fn test_generate_context_info() {
        let config = CustomConfig::default();
        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let context = executor.generate_context_info(&identity, &task);

        // Should be valid JSON
        let parsed: serde_json::Value = serde_json::from_str(&context).unwrap();
        assert_eq!(parsed["agent"]["id"], "test-custom-agent");
        assert_eq!(parsed["agent"]["specialization"], "DevOps");
        assert_eq!(parsed["task"]["description"], "Deploy application");
        assert!(parsed["boundaries"].is_array());
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_generate_help_text (lines 669-698) ---[0m
    fn test_generate_help_text() {
        let config = CustomConfig {
            command: "my-tool".to_string(),
            args: vec!["--input".to_string(), "{prompt}".to_string()],
            env_vars: {
                let mut env = HashMap::new();
                env.insert("TOOL_MODE".to_string(), "production".to_string());
                env
            },
            working_directory: Some(PathBuf::from("/tmp")),
            timeout_seconds: Some(120),
            supports_json: true,
        };

        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let help = executor.generate_help_text(&identity, &task);

        assert!(help.contains("Custom Command Configuration:"));
        assert!(help.contains("Command: my-tool"));
        assert!(help.contains("Timeout: Some(120) seconds"));
        assert!(help.contains("Supports JSON: true"));
        assert!(help.contains("Available Placeholders:"));
        assert!(help.contains("{prompt} ->"));
        assert!(help.contains("{agent_id} ->"));
        assert!(help.contains("Environment Variables:"));
        assert!(help.contains("TOOL_MODE = production"));
    }

  ./crates/ccswarm/src/providers/custom.rs:24-51 method generate_context_info <-> ./crates/ccswarm/src/providers/custom.rs:479-494 function create_test_identity
  Similarity: 76.96%

[36m--- ./crates/ccswarm/src/providers/custom.rs:generate_context_info (lines 24-51) ---[0m
    fn generate_context_info(&self, identity: &AgentIdentity, task: &Task) -> String {
        let context_json = serde_json::json!({
            "agent": {
                "id": identity.agent_id,
                "specialization": identity.specialization.name(),
                "workspace": identity.workspace_path.to_string_lossy(),
                "session_id": identity.session_id,
            },
            "task": {
                "id": task.id,
                "description": task.description,
                "details": task.details,
                "priority": format!("{:?}", task.priority),
                "type": format!("{:?}", task.task_type),
            },
            "boundaries": self.get_agent_boundaries(&identity.specialization),
        });

        serde_json::to_string_pretty(&context_json).unwrap_or_else(|_| {
            format!(
                "Agent: {} ({})\nTask: {}\nDescription: {}",
                identity.agent_id,
                identity.specialization.name(),
                task.id,
                task.description
            )
        })
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:create_test_identity (lines 479-494) ---[0m
    fn create_test_identity() -> AgentIdentity {
        let temp_dir = TempDir::new().unwrap();
        AgentIdentity {
            agent_id: "test-custom-agent".to_string(),
            specialization: AgentRole::DevOps {
                technologies: vec!["Docker".to_string(), "Kubernetes".to_string()],
                responsibilities: vec!["Infrastructure".to_string()],
                boundaries: vec!["No app development".to_string()],
            },
            workspace_path: temp_dir.path().to_path_buf(),
            env_vars: HashMap::new(),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: "12345".to_string(),
            initialized_at: chrono::Utc::now(),
        }
    }

  ./crates/ccswarm/src/providers/custom.rs:71-92 method prepare_command_args <-> ./crates/ccswarm/src/providers/custom.rs:479-494 function create_test_identity
  Similarity: 88.09%

[36m--- ./crates/ccswarm/src/providers/custom.rs:prepare_command_args (lines 71-92) ---[0m
    fn prepare_command_args(
        &self,
        prompt: &str,
        context: &str,
        identity: &AgentIdentity,
        task: &Task,
    ) -> Vec<String> {
        self.config
            .args
            .iter()
            .map(|arg| {
                arg.replace("{prompt}", prompt)
                    .replace("{context}", context)
                    .replace("{agent_id}", &identity.agent_id)
                    .replace("{task_id}", &task.id)
                    .replace("{workspace}", &identity.workspace_path.to_string_lossy())
                    .replace("{specialization}", identity.specialization.name())
                    .replace("{task_type}", &format!("{:?}", task.task_type))
                    .replace("{priority}", &format!("{:?}", task.priority))
            })
            .collect()
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:create_test_identity (lines 479-494) ---[0m
    fn create_test_identity() -> AgentIdentity {
        let temp_dir = TempDir::new().unwrap();
        AgentIdentity {
            agent_id: "test-custom-agent".to_string(),
            specialization: AgentRole::DevOps {
                technologies: vec!["Docker".to_string(), "Kubernetes".to_string()],
                responsibilities: vec!["Infrastructure".to_string()],
                boundaries: vec!["No app development".to_string()],
            },
            workspace_path: temp_dir.path().to_path_buf(),
            env_vars: HashMap::new(),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: "12345".to_string(),
            initialized_at: chrono::Utc::now(),
        }
    }

  ./crates/ccswarm/src/providers/custom.rs:599-621 function test_parse_command_output_json <-> ./crates/ccswarm/src/providers/custom.rs:646-666 function test_get_capabilities
  Similarity: 72.63%

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_parse_command_output_json (lines 599-621) ---[0m
    fn test_parse_command_output_json() {
        let config = CustomConfig {
            supports_json: true,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let json_output = r#"{"status": "success", "files_processed": 5}"#;
        let result = executor.parse_command_output(json_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        let output = result.output.as_object().unwrap();
        assert_eq!(output["format"], "json");
        assert_eq!(output["provider"], "custom");

        let json_result = &output["result"];
        assert_eq!(json_result["status"], "success");
        assert_eq!(json_result["files_processed"], 5);
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_get_capabilities (lines 646-666) ---[0m
    fn test_get_capabilities() {
        let config = CustomConfig {
            supports_json: true,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert!(capabilities.max_context_length.is_none());
        assert!(capabilities
            .supported_languages
            .contains(&"text".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"json".to_string()));
    }

  ./crates/ccswarm/src/providers/custom.rs:624-643 function test_parse_command_output_text <-> ./crates/ccswarm/src/providers/custom.rs:646-666 function test_get_capabilities
  Similarity: 77.73%

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_parse_command_output_text (lines 624-643) ---[0m
    fn test_parse_command_output_text() {
        let config = CustomConfig {
            supports_json: false,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(200);

        let text_output = "Deployment completed successfully!";
        let result = executor.parse_command_output(text_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        let output = result.output.as_object().unwrap();
        assert_eq!(output["format"], "text");
        assert_eq!(output["provider"], "custom");
        assert_eq!(output["response"], "Deployment completed successfully!");
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_get_capabilities (lines 646-666) ---[0m
    fn test_get_capabilities() {
        let config = CustomConfig {
            supports_json: true,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert!(capabilities.max_context_length.is_none());
        assert!(capabilities
            .supported_languages
            .contains(&"text".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"json".to_string()));
    }

  ./crates/ccswarm/src/providers/custom.rs:575-596 function test_determine_success_from_output <-> ./crates/ccswarm/src/providers/custom.rs:646-666 function test_get_capabilities
  Similarity: 72.68%

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_determine_success_from_output (lines 575-596) ---[0m
    fn test_determine_success_from_output() {
        let config = CustomConfig::default();
        let executor = CustomExecutor::new(config);

        // Test success cases
        assert!(executor.determine_success_from_output("Task completed successfully"));
        assert!(executor.determine_success_from_output("Build finished with success"));
        assert!(executor.determine_success_from_output("All tests passed"));
        assert!(executor.determine_success_from_output("Done processing files"));

        // Test failure cases
        assert!(!executor.determine_success_from_output("Error: File not found"));
        assert!(!executor.determine_success_from_output("failed: to compile"));
        assert!(!executor.determine_success_from_output("Exception occurred"));
        assert!(!executor.determine_success_from_output("Fatal: cannot proceed"));

        // Test neutral case with content
        assert!(executor.determine_success_from_output("Processing files..."));

        // Test empty case
        assert!(!executor.determine_success_from_output(""));
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_get_capabilities (lines 646-666) ---[0m
    fn test_get_capabilities() {
        let config = CustomConfig {
            supports_json: true,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert!(capabilities.max_context_length.is_none());
        assert!(capabilities
            .supported_languages
            .contains(&"text".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"json".to_string()));
    }

  ./crates/ccswarm/src/providers/custom.rs:575-596 function test_determine_success_from_output <-> ./crates/ccswarm/src/providers/custom.rs:624-643 function test_parse_command_output_text
  Similarity: 73.18%

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_determine_success_from_output (lines 575-596) ---[0m
    fn test_determine_success_from_output() {
        let config = CustomConfig::default();
        let executor = CustomExecutor::new(config);

        // Test success cases
        assert!(executor.determine_success_from_output("Task completed successfully"));
        assert!(executor.determine_success_from_output("Build finished with success"));
        assert!(executor.determine_success_from_output("All tests passed"));
        assert!(executor.determine_success_from_output("Done processing files"));

        // Test failure cases
        assert!(!executor.determine_success_from_output("Error: File not found"));
        assert!(!executor.determine_success_from_output("failed: to compile"));
        assert!(!executor.determine_success_from_output("Exception occurred"));
        assert!(!executor.determine_success_from_output("Fatal: cannot proceed"));

        // Test neutral case with content
        assert!(executor.determine_success_from_output("Processing files..."));

        // Test empty case
        assert!(!executor.determine_success_from_output(""));
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_parse_command_output_text (lines 624-643) ---[0m
    fn test_parse_command_output_text() {
        let config = CustomConfig {
            supports_json: false,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(200);

        let text_output = "Deployment completed successfully!";
        let result = executor.parse_command_output(text_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        let output = result.output.as_object().unwrap();
        assert_eq!(output["format"], "text");
        assert_eq!(output["provider"], "custom");
        assert_eq!(output["response"], "Deployment completed successfully!");
    }

  ./crates/ccswarm/src/providers/custom.rs:523-537 function test_generate_context_info <-> ./crates/ccswarm/src/providers/custom.rs:599-621 function test_parse_command_output_json
  Similarity: 75.10%

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_generate_context_info (lines 523-537) ---[0m
    fn test_generate_context_info() {
        let config = CustomConfig::default();
        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let context = executor.generate_context_info(&identity, &task);

        // Should be valid JSON
        let parsed: serde_json::Value = serde_json::from_str(&context).unwrap();
        assert_eq!(parsed["agent"]["id"], "test-custom-agent");
        assert_eq!(parsed["agent"]["specialization"], "DevOps");
        assert_eq!(parsed["task"]["description"], "Deploy application");
        assert!(parsed["boundaries"].is_array());
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_parse_command_output_json (lines 599-621) ---[0m
    fn test_parse_command_output_json() {
        let config = CustomConfig {
            supports_json: true,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(100);

        let json_output = r#"{"status": "success", "files_processed": 5}"#;
        let result = executor.parse_command_output(json_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        let output = result.output.as_object().unwrap();
        assert_eq!(output["format"], "json");
        assert_eq!(output["provider"], "custom");

        let json_result = &output["result"];
        assert_eq!(json_result["status"], "success");
        assert_eq!(json_result["files_processed"], 5);
    }

  ./crates/ccswarm/src/providers/custom.rs:523-537 function test_generate_context_info <-> ./crates/ccswarm/src/providers/custom.rs:624-643 function test_parse_command_output_text
  Similarity: 79.55%

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_generate_context_info (lines 523-537) ---[0m
    fn test_generate_context_info() {
        let config = CustomConfig::default();
        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let context = executor.generate_context_info(&identity, &task);

        // Should be valid JSON
        let parsed: serde_json::Value = serde_json::from_str(&context).unwrap();
        assert_eq!(parsed["agent"]["id"], "test-custom-agent");
        assert_eq!(parsed["agent"]["specialization"], "DevOps");
        assert_eq!(parsed["task"]["description"], "Deploy application");
        assert!(parsed["boundaries"].is_array());
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_parse_command_output_text (lines 624-643) ---[0m
    fn test_parse_command_output_text() {
        let config = CustomConfig {
            supports_json: false,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let task = create_test_task();
        let duration = std::time::Duration::from_millis(200);

        let text_output = "Deployment completed successfully!";
        let result = executor.parse_command_output(text_output.to_string(), &task, duration);

        assert!(result.success);
        assert!(result.error.is_none());

        let output = result.output.as_object().unwrap();
        assert_eq!(output["format"], "text");
        assert_eq!(output["provider"], "custom");
        assert_eq!(output["response"], "Deployment completed successfully!");
    }

  ./crates/ccswarm/src/providers/custom.rs:450-467 method get_capabilities <-> ./crates/ccswarm/src/providers/custom.rs:479-494 function create_test_identity
  Similarity: 81.85%

[36m--- ./crates/ccswarm/src/providers/custom.rs:get_capabilities (lines 450-467) ---[0m
    fn get_capabilities(&self) -> ProviderCapabilities {
        ProviderCapabilities {
            supports_json_output: self.config.supports_json,
            supports_streaming: false, // Custom commands typically don't support streaming
            supports_file_operations: true, // Depends on the command, but assume yes
            supports_git_operations: true, // Depends on the command, but assume yes
            supports_code_execution: true, // Depends on the command, but assume yes
            max_context_length: None,  // Unknown for custom commands
            supported_languages: vec![
                // Generic list - actual support depends on the custom command
                "text".to_string(),
                "json".to_string(),
                "yaml".to_string(),
                "markdown".to_string(),
                "bash".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:create_test_identity (lines 479-494) ---[0m
    fn create_test_identity() -> AgentIdentity {
        let temp_dir = TempDir::new().unwrap();
        AgentIdentity {
            agent_id: "test-custom-agent".to_string(),
            specialization: AgentRole::DevOps {
                technologies: vec!["Docker".to_string(), "Kubernetes".to_string()],
                responsibilities: vec!["Infrastructure".to_string()],
                boundaries: vec!["No app development".to_string()],
            },
            workspace_path: temp_dir.path().to_path_buf(),
            env_vars: HashMap::new(),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: "12345".to_string(),
            initialized_at: chrono::Utc::now(),
        }
    }

  ./crates/ccswarm/src/providers/custom.rs:305-324 method execute_prompt <-> ./crates/ccswarm/src/providers/custom.rs:479-494 function create_test_identity
  Similarity: 77.20%

[36m--- ./crates/ccswarm/src/providers/custom.rs:execute_prompt (lines 305-324) ---[0m
    async fn execute_prompt(
        &self,
        prompt: &str,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<String> {
        // Create a mock task for prompt execution
        let mock_task = Task::new(
            format!("prompt-{}", uuid::Uuid::new_v4()),
            "Direct prompt execution".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let context = self.generate_context_info(identity, &mock_task);
        let args = self.prepare_command_args(prompt, &context, identity, &mock_task);

        self.execute_custom_command(args, working_dir, identity)
            .await
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:create_test_identity (lines 479-494) ---[0m
    fn create_test_identity() -> AgentIdentity {
        let temp_dir = TempDir::new().unwrap();
        AgentIdentity {
            agent_id: "test-custom-agent".to_string(),
            specialization: AgentRole::DevOps {
                technologies: vec!["Docker".to_string(), "Kubernetes".to_string()],
                responsibilities: vec!["Infrastructure".to_string()],
                boundaries: vec!["No app development".to_string()],
            },
            workspace_path: temp_dir.path().to_path_buf(),
            env_vars: HashMap::new(),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: "12345".to_string(),
            initialized_at: chrono::Utc::now(),
        }
    }

  ./crates/ccswarm/src/providers/custom.rs:523-537 function test_generate_context_info <-> ./crates/ccswarm/src/providers/custom.rs:646-666 function test_get_capabilities
  Similarity: 76.70%

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_generate_context_info (lines 523-537) ---[0m
    fn test_generate_context_info() {
        let config = CustomConfig::default();
        let executor = CustomExecutor::new(config);
        let identity = create_test_identity();
        let task = create_test_task();

        let context = executor.generate_context_info(&identity, &task);

        // Should be valid JSON
        let parsed: serde_json::Value = serde_json::from_str(&context).unwrap();
        assert_eq!(parsed["agent"]["id"], "test-custom-agent");
        assert_eq!(parsed["agent"]["specialization"], "DevOps");
        assert_eq!(parsed["task"]["description"], "Deploy application");
        assert!(parsed["boundaries"].is_array());
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_get_capabilities (lines 646-666) ---[0m
    fn test_get_capabilities() {
        let config = CustomConfig {
            supports_json: true,
            ..Default::default()
        };
        let executor = CustomExecutor::new(config);
        let capabilities = executor.get_capabilities();

        assert!(capabilities.supports_json_output);
        assert!(capabilities.supports_file_operations);
        assert!(capabilities.supports_git_operations);
        assert!(capabilities.supports_code_execution);
        assert!(!capabilities.supports_streaming);
        assert!(capabilities.max_context_length.is_none());
        assert!(capabilities
            .supported_languages
            .contains(&"text".to_string()));
        assert!(capabilities
            .supported_languages
            .contains(&"json".to_string()));
    }

  ./crates/ccswarm/src/providers/custom.rs:305-324 method execute_prompt <-> ./crates/ccswarm/src/providers/custom.rs:507-520 function test_custom_executor_creation
  Similarity: 80.62%

[36m--- ./crates/ccswarm/src/providers/custom.rs:execute_prompt (lines 305-324) ---[0m
    async fn execute_prompt(
        &self,
        prompt: &str,
        identity: &AgentIdentity,
        working_dir: &Path,
    ) -> Result<String> {
        // Create a mock task for prompt execution
        let mock_task = Task::new(
            format!("prompt-{}", uuid::Uuid::new_v4()),
            "Direct prompt execution".to_string(),
            crate::agent::Priority::Medium,
            TaskType::Development,
        );

        let context = self.generate_context_info(identity, &mock_task);
        let args = self.prepare_command_args(prompt, &context, identity, &mock_task);

        self.execute_custom_command(args, working_dir, identity)
            .await
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_custom_executor_creation (lines 507-520) ---[0m
    fn test_custom_executor_creation() {
        let config = CustomConfig {
            command: "echo".to_string(),
            args: vec!["{prompt}".to_string()],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: Some(60),
            supports_json: false,
        };

        let executor = CustomExecutor::new(config);
        assert_eq!(executor.config.command, "echo");
        assert_eq!(executor.config.timeout_seconds, Some(60));
    }

  ./crates/ccswarm/src/providers/custom.rs:54-68 method get_agent_boundaries <-> ./crates/ccswarm/src/providers/custom.rs:450-467 method get_capabilities
  Similarity: 82.15%
  Classes: CustomExecutor <-> ProviderExecutor

[36m--- ./crates/ccswarm/src/providers/custom.rs:get_agent_boundaries (lines 54-68) ---[0m
    fn get_agent_boundaries(&self, specialization: &crate::identity::AgentRole) -> Vec<String> {
        match specialization {
            crate::identity::AgentRole::Frontend { boundaries, .. } => boundaries.clone(),
            crate::identity::AgentRole::Backend { boundaries, .. } => boundaries.clone(),
            crate::identity::AgentRole::DevOps { boundaries, .. } => boundaries.clone(),
            crate::identity::AgentRole::QA { boundaries, .. } => boundaries.clone(),
            crate::identity::AgentRole::Master { .. } => {
                vec![
                    "No direct code implementation".to_string(),
                    "Coordination only".to_string(),
                ]
            }
            crate::identity::AgentRole::Search { boundaries, .. } => boundaries.clone(),
        }
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:get_capabilities (lines 450-467) ---[0m
    fn get_capabilities(&self) -> ProviderCapabilities {
        ProviderCapabilities {
            supports_json_output: self.config.supports_json,
            supports_streaming: false, // Custom commands typically don't support streaming
            supports_file_operations: true, // Depends on the command, but assume yes
            supports_git_operations: true, // Depends on the command, but assume yes
            supports_code_execution: true, // Depends on the command, but assume yes
            max_context_length: None,  // Unknown for custom commands
            supported_languages: vec![
                // Generic list - actual support depends on the custom command
                "text".to_string(),
                "json".to_string(),
                "yaml".to_string(),
                "markdown".to_string(),
                "bash".to_string(),
            ],
        }
    }

  ./crates/ccswarm/src/providers/custom.rs:71-92 method prepare_command_args <-> ./crates/ccswarm/src/providers/custom.rs:507-520 function test_custom_executor_creation
  Similarity: 72.11%

[36m--- ./crates/ccswarm/src/providers/custom.rs:prepare_command_args (lines 71-92) ---[0m
    fn prepare_command_args(
        &self,
        prompt: &str,
        context: &str,
        identity: &AgentIdentity,
        task: &Task,
    ) -> Vec<String> {
        self.config
            .args
            .iter()
            .map(|arg| {
                arg.replace("{prompt}", prompt)
                    .replace("{context}", context)
                    .replace("{agent_id}", &identity.agent_id)
                    .replace("{task_id}", &task.id)
                    .replace("{workspace}", &identity.workspace_path.to_string_lossy())
                    .replace("{specialization}", identity.specialization.name())
                    .replace("{task_type}", &format!("{:?}", task.task_type))
                    .replace("{priority}", &format!("{:?}", task.priority))
            })
            .collect()
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_custom_executor_creation (lines 507-520) ---[0m
    fn test_custom_executor_creation() {
        let config = CustomConfig {
            command: "echo".to_string(),
            args: vec!["{prompt}".to_string()],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: Some(60),
            supports_json: false,
        };

        let executor = CustomExecutor::new(config);
        assert_eq!(executor.config.command, "echo");
        assert_eq!(executor.config.timeout_seconds, Some(60));
    }

  ./crates/ccswarm/src/providers/custom.rs:479-494 function create_test_identity <-> ./crates/ccswarm/src/providers/custom.rs:507-520 function test_custom_executor_creation
  Similarity: 80.70%

[36m--- ./crates/ccswarm/src/providers/custom.rs:create_test_identity (lines 479-494) ---[0m
    fn create_test_identity() -> AgentIdentity {
        let temp_dir = TempDir::new().unwrap();
        AgentIdentity {
            agent_id: "test-custom-agent".to_string(),
            specialization: AgentRole::DevOps {
                technologies: vec!["Docker".to_string(), "Kubernetes".to_string()],
                responsibilities: vec!["Infrastructure".to_string()],
                boundaries: vec!["No app development".to_string()],
            },
            workspace_path: temp_dir.path().to_path_buf(),
            env_vars: HashMap::new(),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: "12345".to_string(),
            initialized_at: chrono::Utc::now(),
        }
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:test_custom_executor_creation (lines 507-520) ---[0m
    fn test_custom_executor_creation() {
        let config = CustomConfig {
            command: "echo".to_string(),
            args: vec!["{prompt}".to_string()],
            env_vars: HashMap::new(),
            working_directory: None,
            timeout_seconds: Some(60),
            supports_json: false,
        };

        let executor = CustomExecutor::new(config);
        assert_eq!(executor.config.command, "echo");
        assert_eq!(executor.config.timeout_seconds, Some(60));
    }

  ./crates/ccswarm/src/providers/custom.rs:450-467 method get_capabilities <-> ./crates/ccswarm/src/providers/custom.rs:496-504 function create_test_task
  Similarity: 85.57%

[36m--- ./crates/ccswarm/src/providers/custom.rs:get_capabilities (lines 450-467) ---[0m
    fn get_capabilities(&self) -> ProviderCapabilities {
        ProviderCapabilities {
            supports_json_output: self.config.supports_json,
            supports_streaming: false, // Custom commands typically don't support streaming
            supports_file_operations: true, // Depends on the command, but assume yes
            supports_git_operations: true, // Depends on the command, but assume yes
            supports_code_execution: true, // Depends on the command, but assume yes
            max_context_length: None,  // Unknown for custom commands
            supported_languages: vec![
                // Generic list - actual support depends on the custom command
                "text".to_string(),
                "json".to_string(),
                "yaml".to_string(),
                "markdown".to_string(),
                "bash".to_string(),
            ],
        }
    }

[36m--- ./crates/ccswarm/src/providers/custom.rs:create_test_task (lines 496-504) ---[0m
    fn create_test_task() -> Task {
        Task::new(
            Uuid::new_v4().to_string(),
            "Deploy application".to_string(),
            crate::agent::Priority::High,
            TaskType::Infrastructure,
        )
        .with_details("Deploy to staging environment".to_string())
    }


Duplicates in ./crates/ccswarm/src/template/predefined.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/template/predefined.rs:32-108 method react_component <-> ./crates/ccswarm/src/template/predefined.rs:169-250 method api_endpoint
  Similarity: 94.04%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_component (lines 32-108) ---[0m
    pub fn react_component() -> Template {
        Template::new(
            "react-component",
            "React Component",
            "Create a new React functional component with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec!["react".to_string(), "frontend".to_string(), "typescript".to_string(), "component".to_string()])
        .with_task_description("Create a {{component_type}} React component named {{component_name}}{{#if props}} with props: {{props}}{{/if}}{{#if styling}} using {{styling}}{{/if}}")
        .with_task_details(
            "## Component Specification\n\n\
            - **Name**: {{component_name}}\n\
            - **Type**: {{component_type}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if props}}- **Props**: {{props}}\n{{/if}}\
            {{#if state}}- **State**: {{state}}\n{{/if}}\
            {{#if styling}}- **Styling**: {{styling}}\n{{/if}}\
            {{#if accessibility}}- **Accessibility**: Include ARIA attributes and keyboard navigation\n{{/if}}\
            {{#if testing}}- **Testing**: Create unit tests with React Testing Library\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React best practices and hooks conventions\n\
            - Implement proper error boundaries if needed\n\
            - Add PropTypes or TypeScript interfaces for props\n\
            - Include JSDoc comments for component documentation\n\
            {{#if responsive}}- Make component responsive for mobile and desktop\n{{/if}}\
            {{#if performance}}- Optimize for performance with React.memo if appropriate\n{{/if}}"
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("component_name", "Name of the React component"),
            TemplateVariable::choice(
                "component_type",
                "Type of component",
                vec!["functional".to_string(), "class".to_string(), "custom hook".to_string()],
            ),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the component",
                "src/components/{{component_name}}.tsx"
            ),
            TemplateVariable::text("props", "Component props (optional)").optional(),
            TemplateVariable::text("state", "Component state description (optional)").optional(),
            TemplateVariable::choice(
                "styling",
                "Styling approach",
                vec![
                    "CSS Modules".to_string(),
                    "Styled Components".to_string(),
                    "Tailwind CSS".to_string(),
                    "SCSS".to_string(),
                    "Emotion".to_string(),
                ],
            ).optional(),
            TemplateVariable::boolean("accessibility", "Include accessibility features"),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("responsive", "Make responsive design"),
            TemplateVariable::boolean("performance", "Optimize for performance"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.tsx".to_string(),
            "{{file_path}}.module.css".to_string(),
        ])
        .with_preconditions(vec![
            "React and TypeScript are configured in the project".to_string(),
            "Component directory structure exists".to_string(),
        ])
        .with_post_actions(vec![
            "Export component from index file".to_string(),
            "Add to Storybook if available".to_string(),
            "Update documentation".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:api_endpoint (lines 169-250) ---[0m
    pub fn api_endpoint() -> Template {
        Template::new(
            "api-endpoint",
            "API Endpoint",
            "Create a new REST API endpoint with proper error handling",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "api".to_string(),
            "backend".to_string(),
            "rest".to_string(),
            "endpoint".to_string(),
        ])
        .with_task_description(
            "Create a {{http_method}} API endpoint for {{endpoint_path}} to {{endpoint_purpose}}",
        )
        .with_task_details(
            "## API Endpoint Specification\n\n\
            - **Method**: {{http_method}}\n\
            - **Path**: {{endpoint_path}}\n\
            - **Purpose**: {{endpoint_purpose}}\n\
            {{#if request_body}}- **Request Body**: {{request_body}}\n{{/if}}\
            {{#if response_format}}- **Response**: {{response_format}}\n{{/if}}\
            {{#if authentication}}- **Authentication**: {{authentication}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Implement proper HTTP status codes\n\
            - Add comprehensive error handling\n\
            - Include request validation\n\
            - Add API documentation (OpenAPI/Swagger)\n\
            - Implement proper logging\n\
            {{#if database}}- Database operations with proper transactions\n{{/if}}\
            {{#if caching}}- Implement caching strategy\n{{/if}}\
            {{#if rate_limiting}}- Add rate limiting\n{{/if}}\
            {{#if testing}}- Create unit and integration tests\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "http_method",
                "HTTP method",
                vec![
                    "GET".to_string(),
                    "POST".to_string(),
                    "PUT".to_string(),
                    "PATCH".to_string(),
                    "DELETE".to_string(),
                ],
            ),
            TemplateVariable::text("endpoint_path", "API endpoint path (e.g., /api/users)"),
            TemplateVariable::text("endpoint_purpose", "What this endpoint does"),
            TemplateVariable::text("request_body", "Request body format (optional)").optional(),
            TemplateVariable::text("response_format", "Response format (optional)").optional(),
            TemplateVariable::choice(
                "authentication",
                "Authentication method",
                vec![
                    "None".to_string(),
                    "JWT Bearer".to_string(),
                    "API Key".to_string(),
                    "OAuth2".to_string(),
                    "Basic Auth".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("database", "Includes database operations"),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("rate_limiting", "Add rate limiting"),
            TemplateVariable::boolean("testing", "Create tests"),
        ])
        .with_target_files(vec![
            "src/routes/{{endpoint_path}}.rs".to_string(),
            "src/handlers/{{endpoint_path}}_handler.rs".to_string(),
            "tests/{{endpoint_path}}_test.rs".to_string(),
        ])
        .with_preconditions(vec![
            "API framework is set up (Express, Axum, etc.)".to_string(),
            "Database connection is configured if needed".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:169-250 method api_endpoint <-> ./crates/ccswarm/src/template/predefined.rs:562-630 method ci_pipeline
  Similarity: 94.94%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:api_endpoint (lines 169-250) ---[0m
    pub fn api_endpoint() -> Template {
        Template::new(
            "api-endpoint",
            "API Endpoint",
            "Create a new REST API endpoint with proper error handling",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "api".to_string(),
            "backend".to_string(),
            "rest".to_string(),
            "endpoint".to_string(),
        ])
        .with_task_description(
            "Create a {{http_method}} API endpoint for {{endpoint_path}} to {{endpoint_purpose}}",
        )
        .with_task_details(
            "## API Endpoint Specification\n\n\
            - **Method**: {{http_method}}\n\
            - **Path**: {{endpoint_path}}\n\
            - **Purpose**: {{endpoint_purpose}}\n\
            {{#if request_body}}- **Request Body**: {{request_body}}\n{{/if}}\
            {{#if response_format}}- **Response**: {{response_format}}\n{{/if}}\
            {{#if authentication}}- **Authentication**: {{authentication}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Implement proper HTTP status codes\n\
            - Add comprehensive error handling\n\
            - Include request validation\n\
            - Add API documentation (OpenAPI/Swagger)\n\
            - Implement proper logging\n\
            {{#if database}}- Database operations with proper transactions\n{{/if}}\
            {{#if caching}}- Implement caching strategy\n{{/if}}\
            {{#if rate_limiting}}- Add rate limiting\n{{/if}}\
            {{#if testing}}- Create unit and integration tests\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "http_method",
                "HTTP method",
                vec![
                    "GET".to_string(),
                    "POST".to_string(),
                    "PUT".to_string(),
                    "PATCH".to_string(),
                    "DELETE".to_string(),
                ],
            ),
            TemplateVariable::text("endpoint_path", "API endpoint path (e.g., /api/users)"),
            TemplateVariable::text("endpoint_purpose", "What this endpoint does"),
            TemplateVariable::text("request_body", "Request body format (optional)").optional(),
            TemplateVariable::text("response_format", "Response format (optional)").optional(),
            TemplateVariable::choice(
                "authentication",
                "Authentication method",
                vec![
                    "None".to_string(),
                    "JWT Bearer".to_string(),
                    "API Key".to_string(),
                    "OAuth2".to_string(),
                    "Basic Auth".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("database", "Includes database operations"),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("rate_limiting", "Add rate limiting"),
            TemplateVariable::boolean("testing", "Create tests"),
        ])
        .with_target_files(vec![
            "src/routes/{{endpoint_path}}.rs".to_string(),
            "src/handlers/{{endpoint_path}}_handler.rs".to_string(),
            "tests/{{endpoint_path}}_test.rs".to_string(),
        ])
        .with_preconditions(vec![
            "API framework is set up (Express, Axum, etc.)".to_string(),
            "Database connection is configured if needed".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:ci_pipeline (lines 562-630) ---[0m
    pub fn ci_pipeline() -> Template {
        Template::new(
            "ci-pipeline",
            "CI/CD Pipeline",
            "Create a continuous integration pipeline",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "ci".to_string(),
            "cd".to_string(),
            "pipeline".to_string(),
            "automation".to_string(),
        ])
        .with_task_description("Create {{ci_platform}} pipeline for {{project_type}} project")
        .with_task_details(
            "## CI/CD Pipeline Configuration\n\n\
            - **Platform**: {{ci_platform}}\n\
            - **Project Type**: {{project_type}}\n\
            {{#if triggers}}- **Triggers**: {{triggers}}\n{{/if}}\
            {{#if environments}}- **Environments**: {{environments}}\n{{/if}}\n\n\
            ## Pipeline Stages\n\n\
            - Code checkout and setup\n\
            - Dependency installation\n\
            - Linting and code quality checks\n\
            - Unit and integration tests\n\
            - Build and package\n\
            {{#if security_scan}}- Security vulnerability scanning\n{{/if}}\
            {{#if deployment}}- Automated deployment to {{environments}}\n{{/if}}\
            {{#if notifications}}- Notification on success/failure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "ci_platform",
                "CI/CD platform",
                vec![
                    "GitHub Actions".to_string(),
                    "GitLab CI".to_string(),
                    "Jenkins".to_string(),
                    "CircleCI".to_string(),
                    "Azure DevOps".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "project_type",
                "Project type",
                vec![
                    "Node.js".to_string(),
                    "Python".to_string(),
                    "Rust".to_string(),
                    "Go".to_string(),
                    "Java".to_string(),
                    "Docker".to_string(),
                ],
            ),
            TemplateVariable::text("triggers", "Pipeline triggers (e.g., push, PR)").optional(),
            TemplateVariable::text("environments", "Deployment environments").optional(),
            TemplateVariable::boolean("security_scan", "Include security scanning"),
            TemplateVariable::boolean("deployment", "Include deployment stages"),
            TemplateVariable::boolean("notifications", "Set up notifications"),
        ])
        .with_target_files(vec![
            ".github/workflows/ci.yml".to_string(),
            ".github/workflows/cd.yml".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:169-250 method api_endpoint <-> ./crates/ccswarm/src/template/predefined.rs:633-701 method documentation_page
  Similarity: 94.77%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:api_endpoint (lines 169-250) ---[0m
    pub fn api_endpoint() -> Template {
        Template::new(
            "api-endpoint",
            "API Endpoint",
            "Create a new REST API endpoint with proper error handling",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "api".to_string(),
            "backend".to_string(),
            "rest".to_string(),
            "endpoint".to_string(),
        ])
        .with_task_description(
            "Create a {{http_method}} API endpoint for {{endpoint_path}} to {{endpoint_purpose}}",
        )
        .with_task_details(
            "## API Endpoint Specification\n\n\
            - **Method**: {{http_method}}\n\
            - **Path**: {{endpoint_path}}\n\
            - **Purpose**: {{endpoint_purpose}}\n\
            {{#if request_body}}- **Request Body**: {{request_body}}\n{{/if}}\
            {{#if response_format}}- **Response**: {{response_format}}\n{{/if}}\
            {{#if authentication}}- **Authentication**: {{authentication}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Implement proper HTTP status codes\n\
            - Add comprehensive error handling\n\
            - Include request validation\n\
            - Add API documentation (OpenAPI/Swagger)\n\
            - Implement proper logging\n\
            {{#if database}}- Database operations with proper transactions\n{{/if}}\
            {{#if caching}}- Implement caching strategy\n{{/if}}\
            {{#if rate_limiting}}- Add rate limiting\n{{/if}}\
            {{#if testing}}- Create unit and integration tests\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "http_method",
                "HTTP method",
                vec![
                    "GET".to_string(),
                    "POST".to_string(),
                    "PUT".to_string(),
                    "PATCH".to_string(),
                    "DELETE".to_string(),
                ],
            ),
            TemplateVariable::text("endpoint_path", "API endpoint path (e.g., /api/users)"),
            TemplateVariable::text("endpoint_purpose", "What this endpoint does"),
            TemplateVariable::text("request_body", "Request body format (optional)").optional(),
            TemplateVariable::text("response_format", "Response format (optional)").optional(),
            TemplateVariable::choice(
                "authentication",
                "Authentication method",
                vec![
                    "None".to_string(),
                    "JWT Bearer".to_string(),
                    "API Key".to_string(),
                    "OAuth2".to_string(),
                    "Basic Auth".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("database", "Includes database operations"),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("rate_limiting", "Add rate limiting"),
            TemplateVariable::boolean("testing", "Create tests"),
        ])
        .with_target_files(vec![
            "src/routes/{{endpoint_path}}.rs".to_string(),
            "src/handlers/{{endpoint_path}}_handler.rs".to_string(),
            "tests/{{endpoint_path}}_test.rs".to_string(),
        ])
        .with_preconditions(vec![
            "API framework is set up (Express, Axum, etc.)".to_string(),
            "Database connection is configured if needed".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:documentation_page (lines 633-701) ---[0m
    pub fn documentation_page() -> Template {
        Template::new(
            "documentation-page",
            "Documentation Page",
            "Create comprehensive documentation for a feature or API",
            TemplateCategory::Documentation,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "documentation".to_string(),
            "docs".to_string(),
            "guide".to_string(),
        ])
        .with_task_description(
            "Create documentation for {{doc_subject}} explaining {{doc_purpose}}",
        )
        .with_task_details(
            "## Documentation Specification\n\n\
            - **Subject**: {{doc_subject}}\n\
            - **Purpose**: {{doc_purpose}}\n\
            - **Audience**: {{target_audience}}\n\
            {{#if doc_type}}- **Type**: {{doc_type}}\n{{/if}}\n\n\
            ## Content Requirements\n\n\
            - Clear overview and introduction\n\
            - Step-by-step instructions or usage guide\n\
            - Code examples with explanations\n\
            - Common pitfalls and troubleshooting\n\
            - Links to related documentation\n\
            {{#if api_reference}}- Complete API reference with parameters\n{{/if}}\
            {{#if diagrams}}- Architecture or flow diagrams\n{{/if}}\
            {{#if examples}}- Real-world usage examples\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Documentation)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("doc_subject", "What is being documented"),
            TemplateVariable::text("doc_purpose", "Purpose of the documentation"),
            TemplateVariable::choice(
                "target_audience",
                "Target audience",
                vec![
                    "Developers".to_string(),
                    "Users".to_string(),
                    "System Administrators".to_string(),
                    "Product Managers".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "doc_type",
                "Documentation type",
                vec![
                    "API Reference".to_string(),
                    "User Guide".to_string(),
                    "Tutorial".to_string(),
                    "Architecture Guide".to_string(),
                    "Troubleshooting".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("api_reference", "Include API reference"),
            TemplateVariable::boolean("diagrams", "Include diagrams"),
            TemplateVariable::boolean("examples", "Include code examples"),
        ])
        .with_target_files(vec![
            "docs/{{doc_subject}}.md".to_string(),
            "examples/{{doc_subject}}_example.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:169-250 method api_endpoint <-> ./crates/ccswarm/src/template/predefined.rs:313-378 method unit_test
  Similarity: 94.36%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:api_endpoint (lines 169-250) ---[0m
    pub fn api_endpoint() -> Template {
        Template::new(
            "api-endpoint",
            "API Endpoint",
            "Create a new REST API endpoint with proper error handling",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "api".to_string(),
            "backend".to_string(),
            "rest".to_string(),
            "endpoint".to_string(),
        ])
        .with_task_description(
            "Create a {{http_method}} API endpoint for {{endpoint_path}} to {{endpoint_purpose}}",
        )
        .with_task_details(
            "## API Endpoint Specification\n\n\
            - **Method**: {{http_method}}\n\
            - **Path**: {{endpoint_path}}\n\
            - **Purpose**: {{endpoint_purpose}}\n\
            {{#if request_body}}- **Request Body**: {{request_body}}\n{{/if}}\
            {{#if response_format}}- **Response**: {{response_format}}\n{{/if}}\
            {{#if authentication}}- **Authentication**: {{authentication}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Implement proper HTTP status codes\n\
            - Add comprehensive error handling\n\
            - Include request validation\n\
            - Add API documentation (OpenAPI/Swagger)\n\
            - Implement proper logging\n\
            {{#if database}}- Database operations with proper transactions\n{{/if}}\
            {{#if caching}}- Implement caching strategy\n{{/if}}\
            {{#if rate_limiting}}- Add rate limiting\n{{/if}}\
            {{#if testing}}- Create unit and integration tests\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "http_method",
                "HTTP method",
                vec![
                    "GET".to_string(),
                    "POST".to_string(),
                    "PUT".to_string(),
                    "PATCH".to_string(),
                    "DELETE".to_string(),
                ],
            ),
            TemplateVariable::text("endpoint_path", "API endpoint path (e.g., /api/users)"),
            TemplateVariable::text("endpoint_purpose", "What this endpoint does"),
            TemplateVariable::text("request_body", "Request body format (optional)").optional(),
            TemplateVariable::text("response_format", "Response format (optional)").optional(),
            TemplateVariable::choice(
                "authentication",
                "Authentication method",
                vec![
                    "None".to_string(),
                    "JWT Bearer".to_string(),
                    "API Key".to_string(),
                    "OAuth2".to_string(),
                    "Basic Auth".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("database", "Includes database operations"),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("rate_limiting", "Add rate limiting"),
            TemplateVariable::boolean("testing", "Create tests"),
        ])
        .with_target_files(vec![
            "src/routes/{{endpoint_path}}.rs".to_string(),
            "src/handlers/{{endpoint_path}}_handler.rs".to_string(),
            "tests/{{endpoint_path}}_test.rs".to_string(),
        ])
        .with_preconditions(vec![
            "API framework is set up (Express, Axum, etc.)".to_string(),
            "Database connection is configured if needed".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:unit_test (lines 313-378) ---[0m
    pub fn unit_test() -> Template {
        Template::new(
            "unit-test",
            "Unit Test",
            "Create comprehensive unit tests for a function or module",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "unit-test".to_string(),
            "quality".to_string(),
        ])
        .with_task_description("Create unit tests for {{test_target}} covering {{test_scenarios}}")
        .with_task_details(
            "## Test Specification\n\n\
            - **Target**: {{test_target}}\n\
            - **Test Framework**: {{test_framework}}\n\
            - **Scenarios**: {{test_scenarios}}\n\
            {{#if mocking}}- **Mocking**: {{mocking_strategy}}\n{{/if}}\n\n\
            ## Test Coverage Requirements\n\n\
            - Happy path scenarios\n\
            - Error conditions and edge cases\n\
            - Boundary value testing\n\
            - Input validation testing\n\
            {{#if async_code}}- Async operation testing\n{{/if}}\
            {{#if performance}}- Performance benchmarks\n{{/if}}\
            - Achieve >90% code coverage for the target",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Testing)
        .with_duration(35)
        .with_variables(vec![
            TemplateVariable::text("test_target", "Function, class, or module to test"),
            TemplateVariable::text("test_scenarios", "Specific scenarios to test"),
            TemplateVariable::choice(
                "test_framework",
                "Testing framework",
                vec![
                    "Jest".to_string(),
                    "Vitest".to_string(),
                    "Mocha".to_string(),
                    "pytest".to_string(),
                    "Rust test".to_string(),
                    "Go test".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "mocking_strategy",
                "Mocking approach",
                vec![
                    "Manual mocks".to_string(),
                    "Jest mocks".to_string(),
                    "Sinon".to_string(),
                    "unittest.mock".to_string(),
                    "mockall".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("async_code", "Testing async operations"),
            TemplateVariable::boolean("performance", "Include performance tests"),
        ])
        .with_target_files(vec![
            "tests/{{test_target}}.test.{{file_extension}}".to_string()
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:32-108 method react_component <-> ./crates/ccswarm/src/template/predefined.rs:562-630 method ci_pipeline
  Similarity: 93.61%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_component (lines 32-108) ---[0m
    pub fn react_component() -> Template {
        Template::new(
            "react-component",
            "React Component",
            "Create a new React functional component with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec!["react".to_string(), "frontend".to_string(), "typescript".to_string(), "component".to_string()])
        .with_task_description("Create a {{component_type}} React component named {{component_name}}{{#if props}} with props: {{props}}{{/if}}{{#if styling}} using {{styling}}{{/if}}")
        .with_task_details(
            "## Component Specification\n\n\
            - **Name**: {{component_name}}\n\
            - **Type**: {{component_type}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if props}}- **Props**: {{props}}\n{{/if}}\
            {{#if state}}- **State**: {{state}}\n{{/if}}\
            {{#if styling}}- **Styling**: {{styling}}\n{{/if}}\
            {{#if accessibility}}- **Accessibility**: Include ARIA attributes and keyboard navigation\n{{/if}}\
            {{#if testing}}- **Testing**: Create unit tests with React Testing Library\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React best practices and hooks conventions\n\
            - Implement proper error boundaries if needed\n\
            - Add PropTypes or TypeScript interfaces for props\n\
            - Include JSDoc comments for component documentation\n\
            {{#if responsive}}- Make component responsive for mobile and desktop\n{{/if}}\
            {{#if performance}}- Optimize for performance with React.memo if appropriate\n{{/if}}"
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("component_name", "Name of the React component"),
            TemplateVariable::choice(
                "component_type",
                "Type of component",
                vec!["functional".to_string(), "class".to_string(), "custom hook".to_string()],
            ),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the component",
                "src/components/{{component_name}}.tsx"
            ),
            TemplateVariable::text("props", "Component props (optional)").optional(),
            TemplateVariable::text("state", "Component state description (optional)").optional(),
            TemplateVariable::choice(
                "styling",
                "Styling approach",
                vec![
                    "CSS Modules".to_string(),
                    "Styled Components".to_string(),
                    "Tailwind CSS".to_string(),
                    "SCSS".to_string(),
                    "Emotion".to_string(),
                ],
            ).optional(),
            TemplateVariable::boolean("accessibility", "Include accessibility features"),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("responsive", "Make responsive design"),
            TemplateVariable::boolean("performance", "Optimize for performance"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.tsx".to_string(),
            "{{file_path}}.module.css".to_string(),
        ])
        .with_preconditions(vec![
            "React and TypeScript are configured in the project".to_string(),
            "Component directory structure exists".to_string(),
        ])
        .with_post_actions(vec![
            "Export component from index file".to_string(),
            "Add to Storybook if available".to_string(),
            "Update documentation".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:ci_pipeline (lines 562-630) ---[0m
    pub fn ci_pipeline() -> Template {
        Template::new(
            "ci-pipeline",
            "CI/CD Pipeline",
            "Create a continuous integration pipeline",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "ci".to_string(),
            "cd".to_string(),
            "pipeline".to_string(),
            "automation".to_string(),
        ])
        .with_task_description("Create {{ci_platform}} pipeline for {{project_type}} project")
        .with_task_details(
            "## CI/CD Pipeline Configuration\n\n\
            - **Platform**: {{ci_platform}}\n\
            - **Project Type**: {{project_type}}\n\
            {{#if triggers}}- **Triggers**: {{triggers}}\n{{/if}}\
            {{#if environments}}- **Environments**: {{environments}}\n{{/if}}\n\n\
            ## Pipeline Stages\n\n\
            - Code checkout and setup\n\
            - Dependency installation\n\
            - Linting and code quality checks\n\
            - Unit and integration tests\n\
            - Build and package\n\
            {{#if security_scan}}- Security vulnerability scanning\n{{/if}}\
            {{#if deployment}}- Automated deployment to {{environments}}\n{{/if}}\
            {{#if notifications}}- Notification on success/failure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "ci_platform",
                "CI/CD platform",
                vec![
                    "GitHub Actions".to_string(),
                    "GitLab CI".to_string(),
                    "Jenkins".to_string(),
                    "CircleCI".to_string(),
                    "Azure DevOps".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "project_type",
                "Project type",
                vec![
                    "Node.js".to_string(),
                    "Python".to_string(),
                    "Rust".to_string(),
                    "Go".to_string(),
                    "Java".to_string(),
                    "Docker".to_string(),
                ],
            ),
            TemplateVariable::text("triggers", "Pipeline triggers (e.g., push, PR)").optional(),
            TemplateVariable::text("environments", "Deployment environments").optional(),
            TemplateVariable::boolean("security_scan", "Include security scanning"),
            TemplateVariable::boolean("deployment", "Include deployment stages"),
            TemplateVariable::boolean("notifications", "Set up notifications"),
        ])
        .with_target_files(vec![
            ".github/workflows/ci.yml".to_string(),
            ".github/workflows/cd.yml".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:32-108 method react_component <-> ./crates/ccswarm/src/template/predefined.rs:633-701 method documentation_page
  Similarity: 93.45%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_component (lines 32-108) ---[0m
    pub fn react_component() -> Template {
        Template::new(
            "react-component",
            "React Component",
            "Create a new React functional component with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec!["react".to_string(), "frontend".to_string(), "typescript".to_string(), "component".to_string()])
        .with_task_description("Create a {{component_type}} React component named {{component_name}}{{#if props}} with props: {{props}}{{/if}}{{#if styling}} using {{styling}}{{/if}}")
        .with_task_details(
            "## Component Specification\n\n\
            - **Name**: {{component_name}}\n\
            - **Type**: {{component_type}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if props}}- **Props**: {{props}}\n{{/if}}\
            {{#if state}}- **State**: {{state}}\n{{/if}}\
            {{#if styling}}- **Styling**: {{styling}}\n{{/if}}\
            {{#if accessibility}}- **Accessibility**: Include ARIA attributes and keyboard navigation\n{{/if}}\
            {{#if testing}}- **Testing**: Create unit tests with React Testing Library\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React best practices and hooks conventions\n\
            - Implement proper error boundaries if needed\n\
            - Add PropTypes or TypeScript interfaces for props\n\
            - Include JSDoc comments for component documentation\n\
            {{#if responsive}}- Make component responsive for mobile and desktop\n{{/if}}\
            {{#if performance}}- Optimize for performance with React.memo if appropriate\n{{/if}}"
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("component_name", "Name of the React component"),
            TemplateVariable::choice(
                "component_type",
                "Type of component",
                vec!["functional".to_string(), "class".to_string(), "custom hook".to_string()],
            ),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the component",
                "src/components/{{component_name}}.tsx"
            ),
            TemplateVariable::text("props", "Component props (optional)").optional(),
            TemplateVariable::text("state", "Component state description (optional)").optional(),
            TemplateVariable::choice(
                "styling",
                "Styling approach",
                vec![
                    "CSS Modules".to_string(),
                    "Styled Components".to_string(),
                    "Tailwind CSS".to_string(),
                    "SCSS".to_string(),
                    "Emotion".to_string(),
                ],
            ).optional(),
            TemplateVariable::boolean("accessibility", "Include accessibility features"),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("responsive", "Make responsive design"),
            TemplateVariable::boolean("performance", "Optimize for performance"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.tsx".to_string(),
            "{{file_path}}.module.css".to_string(),
        ])
        .with_preconditions(vec![
            "React and TypeScript are configured in the project".to_string(),
            "Component directory structure exists".to_string(),
        ])
        .with_post_actions(vec![
            "Export component from index file".to_string(),
            "Add to Storybook if available".to_string(),
            "Update documentation".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:documentation_page (lines 633-701) ---[0m
    pub fn documentation_page() -> Template {
        Template::new(
            "documentation-page",
            "Documentation Page",
            "Create comprehensive documentation for a feature or API",
            TemplateCategory::Documentation,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "documentation".to_string(),
            "docs".to_string(),
            "guide".to_string(),
        ])
        .with_task_description(
            "Create documentation for {{doc_subject}} explaining {{doc_purpose}}",
        )
        .with_task_details(
            "## Documentation Specification\n\n\
            - **Subject**: {{doc_subject}}\n\
            - **Purpose**: {{doc_purpose}}\n\
            - **Audience**: {{target_audience}}\n\
            {{#if doc_type}}- **Type**: {{doc_type}}\n{{/if}}\n\n\
            ## Content Requirements\n\n\
            - Clear overview and introduction\n\
            - Step-by-step instructions or usage guide\n\
            - Code examples with explanations\n\
            - Common pitfalls and troubleshooting\n\
            - Links to related documentation\n\
            {{#if api_reference}}- Complete API reference with parameters\n{{/if}}\
            {{#if diagrams}}- Architecture or flow diagrams\n{{/if}}\
            {{#if examples}}- Real-world usage examples\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Documentation)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("doc_subject", "What is being documented"),
            TemplateVariable::text("doc_purpose", "Purpose of the documentation"),
            TemplateVariable::choice(
                "target_audience",
                "Target audience",
                vec![
                    "Developers".to_string(),
                    "Users".to_string(),
                    "System Administrators".to_string(),
                    "Product Managers".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "doc_type",
                "Documentation type",
                vec![
                    "API Reference".to_string(),
                    "User Guide".to_string(),
                    "Tutorial".to_string(),
                    "Architecture Guide".to_string(),
                    "Troubleshooting".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("api_reference", "Include API reference"),
            TemplateVariable::boolean("diagrams", "Include diagrams"),
            TemplateVariable::boolean("examples", "Include code examples"),
        ])
        .with_target_files(vec![
            "docs/{{doc_subject}}.md".to_string(),
            "examples/{{doc_subject}}_example.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:562-630 method ci_pipeline <-> ./crates/ccswarm/src/template/predefined.rs:633-701 method documentation_page
  Similarity: 97.27%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:ci_pipeline (lines 562-630) ---[0m
    pub fn ci_pipeline() -> Template {
        Template::new(
            "ci-pipeline",
            "CI/CD Pipeline",
            "Create a continuous integration pipeline",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "ci".to_string(),
            "cd".to_string(),
            "pipeline".to_string(),
            "automation".to_string(),
        ])
        .with_task_description("Create {{ci_platform}} pipeline for {{project_type}} project")
        .with_task_details(
            "## CI/CD Pipeline Configuration\n\n\
            - **Platform**: {{ci_platform}}\n\
            - **Project Type**: {{project_type}}\n\
            {{#if triggers}}- **Triggers**: {{triggers}}\n{{/if}}\
            {{#if environments}}- **Environments**: {{environments}}\n{{/if}}\n\n\
            ## Pipeline Stages\n\n\
            - Code checkout and setup\n\
            - Dependency installation\n\
            - Linting and code quality checks\n\
            - Unit and integration tests\n\
            - Build and package\n\
            {{#if security_scan}}- Security vulnerability scanning\n{{/if}}\
            {{#if deployment}}- Automated deployment to {{environments}}\n{{/if}}\
            {{#if notifications}}- Notification on success/failure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "ci_platform",
                "CI/CD platform",
                vec![
                    "GitHub Actions".to_string(),
                    "GitLab CI".to_string(),
                    "Jenkins".to_string(),
                    "CircleCI".to_string(),
                    "Azure DevOps".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "project_type",
                "Project type",
                vec![
                    "Node.js".to_string(),
                    "Python".to_string(),
                    "Rust".to_string(),
                    "Go".to_string(),
                    "Java".to_string(),
                    "Docker".to_string(),
                ],
            ),
            TemplateVariable::text("triggers", "Pipeline triggers (e.g., push, PR)").optional(),
            TemplateVariable::text("environments", "Deployment environments").optional(),
            TemplateVariable::boolean("security_scan", "Include security scanning"),
            TemplateVariable::boolean("deployment", "Include deployment stages"),
            TemplateVariable::boolean("notifications", "Set up notifications"),
        ])
        .with_target_files(vec![
            ".github/workflows/ci.yml".to_string(),
            ".github/workflows/cd.yml".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:documentation_page (lines 633-701) ---[0m
    pub fn documentation_page() -> Template {
        Template::new(
            "documentation-page",
            "Documentation Page",
            "Create comprehensive documentation for a feature or API",
            TemplateCategory::Documentation,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "documentation".to_string(),
            "docs".to_string(),
            "guide".to_string(),
        ])
        .with_task_description(
            "Create documentation for {{doc_subject}} explaining {{doc_purpose}}",
        )
        .with_task_details(
            "## Documentation Specification\n\n\
            - **Subject**: {{doc_subject}}\n\
            - **Purpose**: {{doc_purpose}}\n\
            - **Audience**: {{target_audience}}\n\
            {{#if doc_type}}- **Type**: {{doc_type}}\n{{/if}}\n\n\
            ## Content Requirements\n\n\
            - Clear overview and introduction\n\
            - Step-by-step instructions or usage guide\n\
            - Code examples with explanations\n\
            - Common pitfalls and troubleshooting\n\
            - Links to related documentation\n\
            {{#if api_reference}}- Complete API reference with parameters\n{{/if}}\
            {{#if diagrams}}- Architecture or flow diagrams\n{{/if}}\
            {{#if examples}}- Real-world usage examples\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Documentation)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("doc_subject", "What is being documented"),
            TemplateVariable::text("doc_purpose", "Purpose of the documentation"),
            TemplateVariable::choice(
                "target_audience",
                "Target audience",
                vec![
                    "Developers".to_string(),
                    "Users".to_string(),
                    "System Administrators".to_string(),
                    "Product Managers".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "doc_type",
                "Documentation type",
                vec![
                    "API Reference".to_string(),
                    "User Guide".to_string(),
                    "Tutorial".to_string(),
                    "Architecture Guide".to_string(),
                    "Troubleshooting".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("api_reference", "Include API reference"),
            TemplateVariable::boolean("diagrams", "Include diagrams"),
            TemplateVariable::boolean("examples", "Include code examples"),
        ])
        .with_target_files(vec![
            "docs/{{doc_subject}}.md".to_string(),
            "examples/{{doc_subject}}_example.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:32-108 method react_component <-> ./crates/ccswarm/src/template/predefined.rs:313-378 method unit_test
  Similarity: 93.61%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_component (lines 32-108) ---[0m
    pub fn react_component() -> Template {
        Template::new(
            "react-component",
            "React Component",
            "Create a new React functional component with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec!["react".to_string(), "frontend".to_string(), "typescript".to_string(), "component".to_string()])
        .with_task_description("Create a {{component_type}} React component named {{component_name}}{{#if props}} with props: {{props}}{{/if}}{{#if styling}} using {{styling}}{{/if}}")
        .with_task_details(
            "## Component Specification\n\n\
            - **Name**: {{component_name}}\n\
            - **Type**: {{component_type}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if props}}- **Props**: {{props}}\n{{/if}}\
            {{#if state}}- **State**: {{state}}\n{{/if}}\
            {{#if styling}}- **Styling**: {{styling}}\n{{/if}}\
            {{#if accessibility}}- **Accessibility**: Include ARIA attributes and keyboard navigation\n{{/if}}\
            {{#if testing}}- **Testing**: Create unit tests with React Testing Library\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React best practices and hooks conventions\n\
            - Implement proper error boundaries if needed\n\
            - Add PropTypes or TypeScript interfaces for props\n\
            - Include JSDoc comments for component documentation\n\
            {{#if responsive}}- Make component responsive for mobile and desktop\n{{/if}}\
            {{#if performance}}- Optimize for performance with React.memo if appropriate\n{{/if}}"
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("component_name", "Name of the React component"),
            TemplateVariable::choice(
                "component_type",
                "Type of component",
                vec!["functional".to_string(), "class".to_string(), "custom hook".to_string()],
            ),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the component",
                "src/components/{{component_name}}.tsx"
            ),
            TemplateVariable::text("props", "Component props (optional)").optional(),
            TemplateVariable::text("state", "Component state description (optional)").optional(),
            TemplateVariable::choice(
                "styling",
                "Styling approach",
                vec![
                    "CSS Modules".to_string(),
                    "Styled Components".to_string(),
                    "Tailwind CSS".to_string(),
                    "SCSS".to_string(),
                    "Emotion".to_string(),
                ],
            ).optional(),
            TemplateVariable::boolean("accessibility", "Include accessibility features"),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("responsive", "Make responsive design"),
            TemplateVariable::boolean("performance", "Optimize for performance"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.tsx".to_string(),
            "{{file_path}}.module.css".to_string(),
        ])
        .with_preconditions(vec![
            "React and TypeScript are configured in the project".to_string(),
            "Component directory structure exists".to_string(),
        ])
        .with_post_actions(vec![
            "Export component from index file".to_string(),
            "Add to Storybook if available".to_string(),
            "Update documentation".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:unit_test (lines 313-378) ---[0m
    pub fn unit_test() -> Template {
        Template::new(
            "unit-test",
            "Unit Test",
            "Create comprehensive unit tests for a function or module",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "unit-test".to_string(),
            "quality".to_string(),
        ])
        .with_task_description("Create unit tests for {{test_target}} covering {{test_scenarios}}")
        .with_task_details(
            "## Test Specification\n\n\
            - **Target**: {{test_target}}\n\
            - **Test Framework**: {{test_framework}}\n\
            - **Scenarios**: {{test_scenarios}}\n\
            {{#if mocking}}- **Mocking**: {{mocking_strategy}}\n{{/if}}\n\n\
            ## Test Coverage Requirements\n\n\
            - Happy path scenarios\n\
            - Error conditions and edge cases\n\
            - Boundary value testing\n\
            - Input validation testing\n\
            {{#if async_code}}- Async operation testing\n{{/if}}\
            {{#if performance}}- Performance benchmarks\n{{/if}}\
            - Achieve >90% code coverage for the target",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Testing)
        .with_duration(35)
        .with_variables(vec![
            TemplateVariable::text("test_target", "Function, class, or module to test"),
            TemplateVariable::text("test_scenarios", "Specific scenarios to test"),
            TemplateVariable::choice(
                "test_framework",
                "Testing framework",
                vec![
                    "Jest".to_string(),
                    "Vitest".to_string(),
                    "Mocha".to_string(),
                    "pytest".to_string(),
                    "Rust test".to_string(),
                    "Go test".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "mocking_strategy",
                "Mocking approach",
                vec![
                    "Manual mocks".to_string(),
                    "Jest mocks".to_string(),
                    "Sinon".to_string(),
                    "unittest.mock".to_string(),
                    "mockall".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("async_code", "Testing async operations"),
            TemplateVariable::boolean("performance", "Include performance tests"),
        ])
        .with_target_files(vec![
            "tests/{{test_target}}.test.{{file_extension}}".to_string()
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:169-250 method api_endpoint <-> ./crates/ccswarm/src/template/predefined.rs:381-439 method integration_test
  Similarity: 94.77%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:api_endpoint (lines 169-250) ---[0m
    pub fn api_endpoint() -> Template {
        Template::new(
            "api-endpoint",
            "API Endpoint",
            "Create a new REST API endpoint with proper error handling",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "api".to_string(),
            "backend".to_string(),
            "rest".to_string(),
            "endpoint".to_string(),
        ])
        .with_task_description(
            "Create a {{http_method}} API endpoint for {{endpoint_path}} to {{endpoint_purpose}}",
        )
        .with_task_details(
            "## API Endpoint Specification\n\n\
            - **Method**: {{http_method}}\n\
            - **Path**: {{endpoint_path}}\n\
            - **Purpose**: {{endpoint_purpose}}\n\
            {{#if request_body}}- **Request Body**: {{request_body}}\n{{/if}}\
            {{#if response_format}}- **Response**: {{response_format}}\n{{/if}}\
            {{#if authentication}}- **Authentication**: {{authentication}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Implement proper HTTP status codes\n\
            - Add comprehensive error handling\n\
            - Include request validation\n\
            - Add API documentation (OpenAPI/Swagger)\n\
            - Implement proper logging\n\
            {{#if database}}- Database operations with proper transactions\n{{/if}}\
            {{#if caching}}- Implement caching strategy\n{{/if}}\
            {{#if rate_limiting}}- Add rate limiting\n{{/if}}\
            {{#if testing}}- Create unit and integration tests\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "http_method",
                "HTTP method",
                vec![
                    "GET".to_string(),
                    "POST".to_string(),
                    "PUT".to_string(),
                    "PATCH".to_string(),
                    "DELETE".to_string(),
                ],
            ),
            TemplateVariable::text("endpoint_path", "API endpoint path (e.g., /api/users)"),
            TemplateVariable::text("endpoint_purpose", "What this endpoint does"),
            TemplateVariable::text("request_body", "Request body format (optional)").optional(),
            TemplateVariable::text("response_format", "Response format (optional)").optional(),
            TemplateVariable::choice(
                "authentication",
                "Authentication method",
                vec![
                    "None".to_string(),
                    "JWT Bearer".to_string(),
                    "API Key".to_string(),
                    "OAuth2".to_string(),
                    "Basic Auth".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("database", "Includes database operations"),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("rate_limiting", "Add rate limiting"),
            TemplateVariable::boolean("testing", "Create tests"),
        ])
        .with_target_files(vec![
            "src/routes/{{endpoint_path}}.rs".to_string(),
            "src/handlers/{{endpoint_path}}_handler.rs".to_string(),
            "tests/{{endpoint_path}}_test.rs".to_string(),
        ])
        .with_preconditions(vec![
            "API framework is set up (Express, Axum, etc.)".to_string(),
            "Database connection is configured if needed".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:integration_test (lines 381-439) ---[0m
    pub fn integration_test() -> Template {
        Template::new(
            "integration-test",
            "Integration Test",
            "Create integration tests for system components",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "integration".to_string(),
            "e2e".to_string(),
        ])
        .with_task_description(
            "Create integration tests for {{system_component}} testing {{integration_points}}",
        )
        .with_task_details(
            "## Integration Test Specification\n\n\
            - **Component**: {{system_component}}\n\
            - **Integration Points**: {{integration_points}}\n\
            - **Test Environment**: {{test_environment}}\n\
            {{#if database}}- **Database**: Test database setup required\n{{/if}}\
            {{#if external_services}}- **External Services**: {{external_services}}\n{{/if}}\n\n\
            ## Test Requirements\n\n\
            - Set up test environment and data\n\
            - Test end-to-end workflows\n\
            - Verify data consistency across components\n\
            - Test error propagation and recovery\n\
            - Clean up test data after execution\n\
            {{#if performance}}- Measure integration performance\n{{/if}}\
            {{#if security}}- Verify security boundaries\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Testing)
        .with_duration(75)
        .with_variables(vec![
            TemplateVariable::text("system_component", "System component being tested"),
            TemplateVariable::text("integration_points", "What integrations to test"),
            TemplateVariable::choice(
                "test_environment",
                "Test environment",
                vec![
                    "Docker Compose".to_string(),
                    "Testcontainers".to_string(),
                    "In-memory".to_string(),
                    "Staging".to_string(),
                ],
            ),
            TemplateVariable::boolean("database", "Includes database testing"),
            TemplateVariable::text("external_services", "External services involved").optional(),
            TemplateVariable::boolean("performance", "Include performance testing"),
            TemplateVariable::boolean("security", "Include security testing"),
        ])
        .with_target_files(vec![
            "tests/integration/{{system_component}}_integration.test.{{file_extension}}"
                .to_string(),
            "tests/fixtures/{{system_component}}_data.json".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:169-250 method api_endpoint <-> ./crates/ccswarm/src/template/predefined.rs:501-559 method docker_service
  Similarity: 94.36%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:api_endpoint (lines 169-250) ---[0m
    pub fn api_endpoint() -> Template {
        Template::new(
            "api-endpoint",
            "API Endpoint",
            "Create a new REST API endpoint with proper error handling",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "api".to_string(),
            "backend".to_string(),
            "rest".to_string(),
            "endpoint".to_string(),
        ])
        .with_task_description(
            "Create a {{http_method}} API endpoint for {{endpoint_path}} to {{endpoint_purpose}}",
        )
        .with_task_details(
            "## API Endpoint Specification\n\n\
            - **Method**: {{http_method}}\n\
            - **Path**: {{endpoint_path}}\n\
            - **Purpose**: {{endpoint_purpose}}\n\
            {{#if request_body}}- **Request Body**: {{request_body}}\n{{/if}}\
            {{#if response_format}}- **Response**: {{response_format}}\n{{/if}}\
            {{#if authentication}}- **Authentication**: {{authentication}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Implement proper HTTP status codes\n\
            - Add comprehensive error handling\n\
            - Include request validation\n\
            - Add API documentation (OpenAPI/Swagger)\n\
            - Implement proper logging\n\
            {{#if database}}- Database operations with proper transactions\n{{/if}}\
            {{#if caching}}- Implement caching strategy\n{{/if}}\
            {{#if rate_limiting}}- Add rate limiting\n{{/if}}\
            {{#if testing}}- Create unit and integration tests\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "http_method",
                "HTTP method",
                vec![
                    "GET".to_string(),
                    "POST".to_string(),
                    "PUT".to_string(),
                    "PATCH".to_string(),
                    "DELETE".to_string(),
                ],
            ),
            TemplateVariable::text("endpoint_path", "API endpoint path (e.g., /api/users)"),
            TemplateVariable::text("endpoint_purpose", "What this endpoint does"),
            TemplateVariable::text("request_body", "Request body format (optional)").optional(),
            TemplateVariable::text("response_format", "Response format (optional)").optional(),
            TemplateVariable::choice(
                "authentication",
                "Authentication method",
                vec![
                    "None".to_string(),
                    "JWT Bearer".to_string(),
                    "API Key".to_string(),
                    "OAuth2".to_string(),
                    "Basic Auth".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("database", "Includes database operations"),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("rate_limiting", "Add rate limiting"),
            TemplateVariable::boolean("testing", "Create tests"),
        ])
        .with_target_files(vec![
            "src/routes/{{endpoint_path}}.rs".to_string(),
            "src/handlers/{{endpoint_path}}_handler.rs".to_string(),
            "tests/{{endpoint_path}}_test.rs".to_string(),
        ])
        .with_preconditions(vec![
            "API framework is set up (Express, Axum, etc.)".to_string(),
            "Database connection is configured if needed".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:docker_service (lines 501-559) ---[0m
    pub fn docker_service() -> Template {
        Template::new(
            "docker-service",
            "Docker Service",
            "Create a Docker container configuration for a service",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "docker".to_string(),
            "container".to_string(),
            "devops".to_string(),
        ])
        .with_task_description("Create Docker configuration for {{service_name}} service")
        .with_task_details(
            "## Docker Service Configuration\n\n\
            - **Service**: {{service_name}}\n\
            - **Base Image**: {{base_image}}\n\
            {{#if ports}}- **Ports**: {{ports}}\n{{/if}}\
            {{#if volumes}}- **Volumes**: {{volumes}}\n{{/if}}\
            {{#if environment}}- **Environment**: {{environment}}\n{{/if}}\n\n\
            ## Container Requirements\n\n\
            - Multi-stage build for optimization\n\
            - Non-root user for security\n\
            - Health check configuration\n\
            - Proper signal handling\n\
            - Minimal attack surface\n\
            {{#if secrets}}- Secure secret management\n{{/if}}\
            {{#if monitoring}}- Monitoring and logging setup\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("service_name", "Name of the service"),
            TemplateVariable::choice(
                "base_image",
                "Base Docker image",
                vec![
                    "alpine".to_string(),
                    "ubuntu".to_string(),
                    "node:alpine".to_string(),
                    "python:alpine".to_string(),
                    "rust:alpine".to_string(),
                    "nginx:alpine".to_string(),
                ],
            ),
            TemplateVariable::text("ports", "Port mappings (e.g., 8080:8080)").optional(),
            TemplateVariable::text("volumes", "Volume mounts").optional(),
            TemplateVariable::text("environment", "Environment variables").optional(),
            TemplateVariable::boolean("secrets", "Include secret management"),
            TemplateVariable::boolean("monitoring", "Add monitoring setup"),
        ])
        .with_target_files(vec![
            "Dockerfile".to_string(),
            "docker-compose.yml".to_string(),
            ".dockerignore".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:169-250 method api_endpoint <-> ./crates/ccswarm/src/template/predefined.rs:253-310 method database_migration
  Similarity: 94.36%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:api_endpoint (lines 169-250) ---[0m
    pub fn api_endpoint() -> Template {
        Template::new(
            "api-endpoint",
            "API Endpoint",
            "Create a new REST API endpoint with proper error handling",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "api".to_string(),
            "backend".to_string(),
            "rest".to_string(),
            "endpoint".to_string(),
        ])
        .with_task_description(
            "Create a {{http_method}} API endpoint for {{endpoint_path}} to {{endpoint_purpose}}",
        )
        .with_task_details(
            "## API Endpoint Specification\n\n\
            - **Method**: {{http_method}}\n\
            - **Path**: {{endpoint_path}}\n\
            - **Purpose**: {{endpoint_purpose}}\n\
            {{#if request_body}}- **Request Body**: {{request_body}}\n{{/if}}\
            {{#if response_format}}- **Response**: {{response_format}}\n{{/if}}\
            {{#if authentication}}- **Authentication**: {{authentication}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Implement proper HTTP status codes\n\
            - Add comprehensive error handling\n\
            - Include request validation\n\
            - Add API documentation (OpenAPI/Swagger)\n\
            - Implement proper logging\n\
            {{#if database}}- Database operations with proper transactions\n{{/if}}\
            {{#if caching}}- Implement caching strategy\n{{/if}}\
            {{#if rate_limiting}}- Add rate limiting\n{{/if}}\
            {{#if testing}}- Create unit and integration tests\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "http_method",
                "HTTP method",
                vec![
                    "GET".to_string(),
                    "POST".to_string(),
                    "PUT".to_string(),
                    "PATCH".to_string(),
                    "DELETE".to_string(),
                ],
            ),
            TemplateVariable::text("endpoint_path", "API endpoint path (e.g., /api/users)"),
            TemplateVariable::text("endpoint_purpose", "What this endpoint does"),
            TemplateVariable::text("request_body", "Request body format (optional)").optional(),
            TemplateVariable::text("response_format", "Response format (optional)").optional(),
            TemplateVariable::choice(
                "authentication",
                "Authentication method",
                vec![
                    "None".to_string(),
                    "JWT Bearer".to_string(),
                    "API Key".to_string(),
                    "OAuth2".to_string(),
                    "Basic Auth".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("database", "Includes database operations"),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("rate_limiting", "Add rate limiting"),
            TemplateVariable::boolean("testing", "Create tests"),
        ])
        .with_target_files(vec![
            "src/routes/{{endpoint_path}}.rs".to_string(),
            "src/handlers/{{endpoint_path}}_handler.rs".to_string(),
            "tests/{{endpoint_path}}_test.rs".to_string(),
        ])
        .with_preconditions(vec![
            "API framework is set up (Express, Axum, etc.)".to_string(),
            "Database connection is configured if needed".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:database_migration (lines 253-310) ---[0m
    pub fn database_migration() -> Template {
        Template::new(
            "database-migration",
            "Database Migration",
            "Create a database migration script",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "database".to_string(),
            "migration".to_string(),
            "schema".to_string(),
        ])
        .with_task_description("Create database migration to {{migration_purpose}}")
        .with_task_details(
            "## Migration Details\n\n\
            - **Purpose**: {{migration_purpose}}\n\
            - **Type**: {{migration_type}}\n\
            {{#if table_name}}- **Table**: {{table_name}}\n{{/if}}\
            {{#if columns}}- **Columns**: {{columns}}\n{{/if}}\
            {{#if indexes}}- **Indexes**: {{indexes}}\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Write both up and down migrations\n\
            - Include proper constraints and indexes\n\
            - Handle existing data appropriately\n\
            - Add comments for complex operations\n\
            - Test migration on sample data\n\
            {{#if backup}}- Create backup before running\n{{/if}}\
            {{#if rollback_plan}}- Document rollback procedure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(40)
        .with_variables(vec![
            TemplateVariable::text("migration_purpose", "What this migration accomplishes"),
            TemplateVariable::choice(
                "migration_type",
                "Type of migration",
                vec![
                    "Create Table".to_string(),
                    "Alter Table".to_string(),
                    "Drop Table".to_string(),
                    "Add Column".to_string(),
                    "Drop Column".to_string(),
                    "Create Index".to_string(),
                    "Data Migration".to_string(),
                ],
            ),
            TemplateVariable::text("table_name", "Table name (if applicable)").optional(),
            TemplateVariable::text("columns", "Column definitions (optional)").optional(),
            TemplateVariable::text("indexes", "Index definitions (optional)").optional(),
            TemplateVariable::boolean("backup", "Create backup before migration"),
            TemplateVariable::boolean("rollback_plan", "Document rollback procedure"),
        ])
        .with_target_files(vec![
            "migrations/{{current_datetime}}_{{migration_purpose}}.sql".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:169-250 method api_endpoint <-> ./crates/ccswarm/src/template/predefined.rs:704-761 method rust_module
  Similarity: 94.36%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:api_endpoint (lines 169-250) ---[0m
    pub fn api_endpoint() -> Template {
        Template::new(
            "api-endpoint",
            "API Endpoint",
            "Create a new REST API endpoint with proper error handling",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "api".to_string(),
            "backend".to_string(),
            "rest".to_string(),
            "endpoint".to_string(),
        ])
        .with_task_description(
            "Create a {{http_method}} API endpoint for {{endpoint_path}} to {{endpoint_purpose}}",
        )
        .with_task_details(
            "## API Endpoint Specification\n\n\
            - **Method**: {{http_method}}\n\
            - **Path**: {{endpoint_path}}\n\
            - **Purpose**: {{endpoint_purpose}}\n\
            {{#if request_body}}- **Request Body**: {{request_body}}\n{{/if}}\
            {{#if response_format}}- **Response**: {{response_format}}\n{{/if}}\
            {{#if authentication}}- **Authentication**: {{authentication}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Implement proper HTTP status codes\n\
            - Add comprehensive error handling\n\
            - Include request validation\n\
            - Add API documentation (OpenAPI/Swagger)\n\
            - Implement proper logging\n\
            {{#if database}}- Database operations with proper transactions\n{{/if}}\
            {{#if caching}}- Implement caching strategy\n{{/if}}\
            {{#if rate_limiting}}- Add rate limiting\n{{/if}}\
            {{#if testing}}- Create unit and integration tests\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "http_method",
                "HTTP method",
                vec![
                    "GET".to_string(),
                    "POST".to_string(),
                    "PUT".to_string(),
                    "PATCH".to_string(),
                    "DELETE".to_string(),
                ],
            ),
            TemplateVariable::text("endpoint_path", "API endpoint path (e.g., /api/users)"),
            TemplateVariable::text("endpoint_purpose", "What this endpoint does"),
            TemplateVariable::text("request_body", "Request body format (optional)").optional(),
            TemplateVariable::text("response_format", "Response format (optional)").optional(),
            TemplateVariable::choice(
                "authentication",
                "Authentication method",
                vec![
                    "None".to_string(),
                    "JWT Bearer".to_string(),
                    "API Key".to_string(),
                    "OAuth2".to_string(),
                    "Basic Auth".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("database", "Includes database operations"),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("rate_limiting", "Add rate limiting"),
            TemplateVariable::boolean("testing", "Create tests"),
        ])
        .with_target_files(vec![
            "src/routes/{{endpoint_path}}.rs".to_string(),
            "src/handlers/{{endpoint_path}}_handler.rs".to_string(),
            "tests/{{endpoint_path}}_test.rs".to_string(),
        ])
        .with_preconditions(vec![
            "API framework is set up (Express, Axum, etc.)".to_string(),
            "Database connection is configured if needed".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_module (lines 704-761) ---[0m
    pub fn rust_module() -> Template {
        Template::new(
            "rust-module",
            "Rust Module",
            "Create a new Rust module with proper structure and documentation",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "module".to_string(),
            "backend".to_string(),
        ])
        .with_task_description("Create Rust module {{module_name}} for {{module_purpose}}")
        .with_task_details(
            "## Rust Module Specification\n\n\
            - **Module**: {{module_name}}\n\
            - **Purpose**: {{module_purpose}}\n\
            - **Visibility**: {{visibility}}\n\
            {{#if traits}}- **Traits**: {{traits}}\n{{/if}}\
            {{#if structs}}- **Structs**: {{structs}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Follow Rust naming conventions\n\
            - Add comprehensive rustdoc comments\n\
            - Implement proper error handling with Result<T, E>\n\
            - Use appropriate visibility modifiers\n\
            - Include unit tests in #[cfg(test)] module\n\
            {{#if async_support}}- Implement async/await patterns\n{{/if}}\
            {{#if serde_support}}- Add Serde serialization support\n{{/if}}\
            {{#if benchmark}}- Include criterion benchmarks\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("module_name", "Name of the Rust module"),
            TemplateVariable::text("module_purpose", "What this module does"),
            TemplateVariable::choice(
                "visibility",
                "Module visibility",
                vec![
                    "public".to_string(),
                    "private".to_string(),
                    "pub(crate)".to_string(),
                ],
            ),
            TemplateVariable::text("traits", "Traits to implement").optional(),
            TemplateVariable::text("structs", "Main structs in the module").optional(),
            TemplateVariable::boolean("async_support", "Include async support"),
            TemplateVariable::boolean("serde_support", "Add Serde support"),
            TemplateVariable::boolean("benchmark", "Include benchmarks"),
        ])
        .with_target_files(vec![
            "src/{{module_name}}/mod.rs".to_string(),
            "src/{{module_name}}/types.rs".to_string(),
            "benches/{{module_name}}_bench.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:169-250 method api_endpoint <-> ./crates/ccswarm/src/template/predefined.rs:442-498 method bug_fix
  Similarity: 94.94%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:api_endpoint (lines 169-250) ---[0m
    pub fn api_endpoint() -> Template {
        Template::new(
            "api-endpoint",
            "API Endpoint",
            "Create a new REST API endpoint with proper error handling",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "api".to_string(),
            "backend".to_string(),
            "rest".to_string(),
            "endpoint".to_string(),
        ])
        .with_task_description(
            "Create a {{http_method}} API endpoint for {{endpoint_path}} to {{endpoint_purpose}}",
        )
        .with_task_details(
            "## API Endpoint Specification\n\n\
            - **Method**: {{http_method}}\n\
            - **Path**: {{endpoint_path}}\n\
            - **Purpose**: {{endpoint_purpose}}\n\
            {{#if request_body}}- **Request Body**: {{request_body}}\n{{/if}}\
            {{#if response_format}}- **Response**: {{response_format}}\n{{/if}}\
            {{#if authentication}}- **Authentication**: {{authentication}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Implement proper HTTP status codes\n\
            - Add comprehensive error handling\n\
            - Include request validation\n\
            - Add API documentation (OpenAPI/Swagger)\n\
            - Implement proper logging\n\
            {{#if database}}- Database operations with proper transactions\n{{/if}}\
            {{#if caching}}- Implement caching strategy\n{{/if}}\
            {{#if rate_limiting}}- Add rate limiting\n{{/if}}\
            {{#if testing}}- Create unit and integration tests\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "http_method",
                "HTTP method",
                vec![
                    "GET".to_string(),
                    "POST".to_string(),
                    "PUT".to_string(),
                    "PATCH".to_string(),
                    "DELETE".to_string(),
                ],
            ),
            TemplateVariable::text("endpoint_path", "API endpoint path (e.g., /api/users)"),
            TemplateVariable::text("endpoint_purpose", "What this endpoint does"),
            TemplateVariable::text("request_body", "Request body format (optional)").optional(),
            TemplateVariable::text("response_format", "Response format (optional)").optional(),
            TemplateVariable::choice(
                "authentication",
                "Authentication method",
                vec![
                    "None".to_string(),
                    "JWT Bearer".to_string(),
                    "API Key".to_string(),
                    "OAuth2".to_string(),
                    "Basic Auth".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("database", "Includes database operations"),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("rate_limiting", "Add rate limiting"),
            TemplateVariable::boolean("testing", "Create tests"),
        ])
        .with_target_files(vec![
            "src/routes/{{endpoint_path}}.rs".to_string(),
            "src/handlers/{{endpoint_path}}_handler.rs".to_string(),
            "tests/{{endpoint_path}}_test.rs".to_string(),
        ])
        .with_preconditions(vec![
            "API framework is set up (Express, Axum, etc.)".to_string(),
            "Database connection is configured if needed".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:bug_fix (lines 442-498) ---[0m
    pub fn bug_fix() -> Template {
        Template::new(
            "bug-fix",
            "Bug Fix",
            "Fix a reported bug with proper testing and documentation",
            TemplateCategory::BugFix,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "bug".to_string(),
            "fix".to_string(),
            "maintenance".to_string(),
        ])
        .with_task_description("Fix bug: {{bug_description}} ({{bug_severity}})")
        .with_task_details(
            "## Bug Report\n\n\
            - **Description**: {{bug_description}}\n\
            - **Severity**: {{bug_severity}}\n\
            {{#if reproduction_steps}}- **Reproduction**: {{reproduction_steps}}\n{{/if}}\
            {{#if affected_components}}- **Affected Components**: {{affected_components}}\n{{/if}}\
            {{#if user_impact}}- **User Impact**: {{user_impact}}\n{{/if}}\n\n\
            ## Fix Requirements\n\n\
            - Reproduce the bug in a test environment\n\
            - Identify root cause through debugging\n\
            - Implement minimal fix that addresses root cause\n\
            - Add regression tests to prevent recurrence\n\
            - Verify fix doesn't introduce new issues\n\
            {{#if documentation}}- Update relevant documentation\n{{/if}}\
            {{#if release_notes}}- Add to release notes\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Bugfix)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("bug_description", "Clear description of the bug"),
            TemplateVariable::choice(
                "bug_severity",
                "Bug severity level",
                vec![
                    "Critical".to_string(),
                    "High".to_string(),
                    "Medium".to_string(),
                    "Low".to_string(),
                ],
            ),
            TemplateVariable::text("reproduction_steps", "Steps to reproduce the bug").optional(),
            TemplateVariable::text("affected_components", "Components affected by the bug")
                .optional(),
            TemplateVariable::text("user_impact", "How this affects users").optional(),
            TemplateVariable::boolean("documentation", "Update documentation"),
            TemplateVariable::boolean("release_notes", "Add to release notes"),
        ])
        .with_target_files(vec![
            "src/{{affected_components}}/fix.{{file_extension}}".to_string(),
            "tests/regression/{{bug_description}}.test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:169-250 method api_endpoint <-> ./crates/ccswarm/src/template/predefined.rs:931-987 method code_refactor
  Similarity: 94.94%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:api_endpoint (lines 169-250) ---[0m
    pub fn api_endpoint() -> Template {
        Template::new(
            "api-endpoint",
            "API Endpoint",
            "Create a new REST API endpoint with proper error handling",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "api".to_string(),
            "backend".to_string(),
            "rest".to_string(),
            "endpoint".to_string(),
        ])
        .with_task_description(
            "Create a {{http_method}} API endpoint for {{endpoint_path}} to {{endpoint_purpose}}",
        )
        .with_task_details(
            "## API Endpoint Specification\n\n\
            - **Method**: {{http_method}}\n\
            - **Path**: {{endpoint_path}}\n\
            - **Purpose**: {{endpoint_purpose}}\n\
            {{#if request_body}}- **Request Body**: {{request_body}}\n{{/if}}\
            {{#if response_format}}- **Response**: {{response_format}}\n{{/if}}\
            {{#if authentication}}- **Authentication**: {{authentication}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Implement proper HTTP status codes\n\
            - Add comprehensive error handling\n\
            - Include request validation\n\
            - Add API documentation (OpenAPI/Swagger)\n\
            - Implement proper logging\n\
            {{#if database}}- Database operations with proper transactions\n{{/if}}\
            {{#if caching}}- Implement caching strategy\n{{/if}}\
            {{#if rate_limiting}}- Add rate limiting\n{{/if}}\
            {{#if testing}}- Create unit and integration tests\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "http_method",
                "HTTP method",
                vec![
                    "GET".to_string(),
                    "POST".to_string(),
                    "PUT".to_string(),
                    "PATCH".to_string(),
                    "DELETE".to_string(),
                ],
            ),
            TemplateVariable::text("endpoint_path", "API endpoint path (e.g., /api/users)"),
            TemplateVariable::text("endpoint_purpose", "What this endpoint does"),
            TemplateVariable::text("request_body", "Request body format (optional)").optional(),
            TemplateVariable::text("response_format", "Response format (optional)").optional(),
            TemplateVariable::choice(
                "authentication",
                "Authentication method",
                vec![
                    "None".to_string(),
                    "JWT Bearer".to_string(),
                    "API Key".to_string(),
                    "OAuth2".to_string(),
                    "Basic Auth".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("database", "Includes database operations"),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("rate_limiting", "Add rate limiting"),
            TemplateVariable::boolean("testing", "Create tests"),
        ])
        .with_target_files(vec![
            "src/routes/{{endpoint_path}}.rs".to_string(),
            "src/handlers/{{endpoint_path}}_handler.rs".to_string(),
            "tests/{{endpoint_path}}_test.rs".to_string(),
        ])
        .with_preconditions(vec![
            "API framework is set up (Express, Axum, etc.)".to_string(),
            "Database connection is configured if needed".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:code_refactor (lines 931-987) ---[0m
    pub fn code_refactor() -> Template {
        Template::new(
            "code-refactor",
            "Code Refactoring",
            "Refactor code to improve maintainability and design",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "refactor".to_string(),
            "maintainability".to_string(),
            "design".to_string(),
        ])
        .with_task_description("Refactor {{refactor_target}} to {{refactor_goal}}")
        .with_task_details(
            "## Refactoring Plan\n\n\
            - **Target**: {{refactor_target}}\n\
            - **Goal**: {{refactor_goal}}\n\
            - **Refactor Type**: {{refactor_type}}\n\
            {{#if design_patterns}}- **Design Patterns**: {{design_patterns}}\n{{/if}}\n\n\
            ## Refactoring Steps\n\n\
            - Analyze current code structure and identify issues\n\
            - Create comprehensive test coverage before refactoring\n\
            - Apply refactoring techniques incrementally\n\
            - Verify tests pass after each step\n\
            - Update documentation to reflect changes\n\
            {{#if breaking_changes}}- Document any breaking changes\n{{/if}}\
            {{#if performance_impact}}- Measure performance impact\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(80)
        .with_variables(vec![
            TemplateVariable::text("refactor_target", "Code to be refactored"),
            TemplateVariable::text("refactor_goal", "What the refactoring should achieve"),
            TemplateVariable::choice(
                "refactor_type",
                "Type of refactoring",
                vec![
                    "Extract Method".to_string(),
                    "Extract Class".to_string(),
                    "Move Method".to_string(),
                    "Rename".to_string(),
                    "Simplify Conditional".to_string(),
                    "Remove Duplication".to_string(),
                    "Improve Abstraction".to_string(),
                ],
            ),
            TemplateVariable::text("design_patterns", "Design patterns to apply").optional(),
            TemplateVariable::boolean("breaking_changes", "May introduce breaking changes"),
            TemplateVariable::boolean("performance_impact", "Measure performance impact"),
        ])
        .with_target_files(vec![
            "refactor-plan-{{refactor_target}}.md".to_string(),
            "tests/refactor/{{refactor_target}}_test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:169-250 method api_endpoint <-> ./crates/ccswarm/src/template/predefined.rs:816-872 method security_review
  Similarity: 94.77%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:api_endpoint (lines 169-250) ---[0m
    pub fn api_endpoint() -> Template {
        Template::new(
            "api-endpoint",
            "API Endpoint",
            "Create a new REST API endpoint with proper error handling",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "api".to_string(),
            "backend".to_string(),
            "rest".to_string(),
            "endpoint".to_string(),
        ])
        .with_task_description(
            "Create a {{http_method}} API endpoint for {{endpoint_path}} to {{endpoint_purpose}}",
        )
        .with_task_details(
            "## API Endpoint Specification\n\n\
            - **Method**: {{http_method}}\n\
            - **Path**: {{endpoint_path}}\n\
            - **Purpose**: {{endpoint_purpose}}\n\
            {{#if request_body}}- **Request Body**: {{request_body}}\n{{/if}}\
            {{#if response_format}}- **Response**: {{response_format}}\n{{/if}}\
            {{#if authentication}}- **Authentication**: {{authentication}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Implement proper HTTP status codes\n\
            - Add comprehensive error handling\n\
            - Include request validation\n\
            - Add API documentation (OpenAPI/Swagger)\n\
            - Implement proper logging\n\
            {{#if database}}- Database operations with proper transactions\n{{/if}}\
            {{#if caching}}- Implement caching strategy\n{{/if}}\
            {{#if rate_limiting}}- Add rate limiting\n{{/if}}\
            {{#if testing}}- Create unit and integration tests\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "http_method",
                "HTTP method",
                vec![
                    "GET".to_string(),
                    "POST".to_string(),
                    "PUT".to_string(),
                    "PATCH".to_string(),
                    "DELETE".to_string(),
                ],
            ),
            TemplateVariable::text("endpoint_path", "API endpoint path (e.g., /api/users)"),
            TemplateVariable::text("endpoint_purpose", "What this endpoint does"),
            TemplateVariable::text("request_body", "Request body format (optional)").optional(),
            TemplateVariable::text("response_format", "Response format (optional)").optional(),
            TemplateVariable::choice(
                "authentication",
                "Authentication method",
                vec![
                    "None".to_string(),
                    "JWT Bearer".to_string(),
                    "API Key".to_string(),
                    "OAuth2".to_string(),
                    "Basic Auth".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("database", "Includes database operations"),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("rate_limiting", "Add rate limiting"),
            TemplateVariable::boolean("testing", "Create tests"),
        ])
        .with_target_files(vec![
            "src/routes/{{endpoint_path}}.rs".to_string(),
            "src/handlers/{{endpoint_path}}_handler.rs".to_string(),
            "tests/{{endpoint_path}}_test.rs".to_string(),
        ])
        .with_preconditions(vec![
            "API framework is set up (Express, Axum, etc.)".to_string(),
            "Database connection is configured if needed".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:security_review (lines 816-872) ---[0m
    pub fn security_review() -> Template {
        Template::new(
            "security-review",
            "Security Review",
            "Conduct a comprehensive security review of code or system",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "security".to_string(),
            "review".to_string(),
            "audit".to_string(),
        ])
        .with_task_description(
            "Conduct security review of {{review_target}} focusing on {{security_aspects}}",
        )
        .with_task_details(
            "## Security Review Scope\n\n\
            - **Target**: {{review_target}}\n\
            - **Focus Areas**: {{security_aspects}}\n\
            - **Review Type**: {{review_type}}\n\n\
            ## Security Checklist\n\n\
            - Authentication and authorization mechanisms\n\
            - Input validation and sanitization\n\
            - SQL injection and XSS vulnerabilities\n\
            - Secure data storage and transmission\n\
            - Error handling and information disclosure\n\
            - Dependencies and supply chain security\n\
            {{#if crypto}}- Cryptographic implementations\n{{/if}}\
            {{#if api_security}}- API security and rate limiting\n{{/if}}\
            {{#if infrastructure}}- Infrastructure security configuration\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Review)
        .with_duration(120)
        .with_variables(vec![
            TemplateVariable::text("review_target", "What is being reviewed"),
            TemplateVariable::text("security_aspects", "Specific security concerns"),
            TemplateVariable::choice(
                "review_type",
                "Type of security review",
                vec![
                    "Code Review".to_string(),
                    "Architecture Review".to_string(),
                    "Penetration Test".to_string(),
                    "Compliance Audit".to_string(),
                ],
            ),
            TemplateVariable::boolean("crypto", "Review cryptographic code"),
            TemplateVariable::boolean("api_security", "Review API security"),
            TemplateVariable::boolean("infrastructure", "Review infrastructure"),
        ])
        .with_target_files(vec![
            "security-review-{{review_target}}.md".to_string(),
            "security-checklist-{{review_target}}.md".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:111-166 method react_hook <-> ./crates/ccswarm/src/template/predefined.rs:169-250 method api_endpoint
  Similarity: 94.77%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_hook (lines 111-166) ---[0m
    pub fn react_hook() -> Template {
        Template::new(
            "react-hook",
            "React Custom Hook",
            "Create a custom React hook with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "react".to_string(),
            "hook".to_string(),
            "typescript".to_string(),
        ])
        .with_task_description(
            "Create a custom React hook named {{hook_name}} for {{hook_purpose}}",
        )
        .with_task_details(
            "## Hook Specification\n\n\
            - **Name**: {{hook_name}}\n\
            - **Purpose**: {{hook_purpose}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if parameters}}- **Parameters**: {{parameters}}\n{{/if}}\
            {{#if return_type}}- **Returns**: {{return_type}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React hooks rules and conventions\n\
            - Include proper dependency arrays for useEffect\n\
            - Add comprehensive JSDoc comments\n\
            - Handle cleanup and memory leaks\n\
            {{#if testing}}- Create unit tests with React Hooks Testing Library\n{{/if}}\
            {{#if memoization}}- Use useMemo/useCallback for performance optimization\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(30)
        .with_variables(vec![
            TemplateVariable::text(
                "hook_name",
                "Name of the custom hook (e.g., useLocalStorage)",
            ),
            TemplateVariable::text("hook_purpose", "What the hook does"),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the hook",
                "src/hooks/{{hook_name}}.ts",
            ),
            TemplateVariable::text("parameters", "Hook parameters (optional)").optional(),
            TemplateVariable::text("return_type", "Hook return type (optional)").optional(),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("memoization", "Include performance optimizations"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.ts".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:api_endpoint (lines 169-250) ---[0m
    pub fn api_endpoint() -> Template {
        Template::new(
            "api-endpoint",
            "API Endpoint",
            "Create a new REST API endpoint with proper error handling",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "api".to_string(),
            "backend".to_string(),
            "rest".to_string(),
            "endpoint".to_string(),
        ])
        .with_task_description(
            "Create a {{http_method}} API endpoint for {{endpoint_path}} to {{endpoint_purpose}}",
        )
        .with_task_details(
            "## API Endpoint Specification\n\n\
            - **Method**: {{http_method}}\n\
            - **Path**: {{endpoint_path}}\n\
            - **Purpose**: {{endpoint_purpose}}\n\
            {{#if request_body}}- **Request Body**: {{request_body}}\n{{/if}}\
            {{#if response_format}}- **Response**: {{response_format}}\n{{/if}}\
            {{#if authentication}}- **Authentication**: {{authentication}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Implement proper HTTP status codes\n\
            - Add comprehensive error handling\n\
            - Include request validation\n\
            - Add API documentation (OpenAPI/Swagger)\n\
            - Implement proper logging\n\
            {{#if database}}- Database operations with proper transactions\n{{/if}}\
            {{#if caching}}- Implement caching strategy\n{{/if}}\
            {{#if rate_limiting}}- Add rate limiting\n{{/if}}\
            {{#if testing}}- Create unit and integration tests\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "http_method",
                "HTTP method",
                vec![
                    "GET".to_string(),
                    "POST".to_string(),
                    "PUT".to_string(),
                    "PATCH".to_string(),
                    "DELETE".to_string(),
                ],
            ),
            TemplateVariable::text("endpoint_path", "API endpoint path (e.g., /api/users)"),
            TemplateVariable::text("endpoint_purpose", "What this endpoint does"),
            TemplateVariable::text("request_body", "Request body format (optional)").optional(),
            TemplateVariable::text("response_format", "Response format (optional)").optional(),
            TemplateVariable::choice(
                "authentication",
                "Authentication method",
                vec![
                    "None".to_string(),
                    "JWT Bearer".to_string(),
                    "API Key".to_string(),
                    "OAuth2".to_string(),
                    "Basic Auth".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("database", "Includes database operations"),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("rate_limiting", "Add rate limiting"),
            TemplateVariable::boolean("testing", "Create tests"),
        ])
        .with_target_files(vec![
            "src/routes/{{endpoint_path}}.rs".to_string(),
            "src/handlers/{{endpoint_path}}_handler.rs".to_string(),
            "tests/{{endpoint_path}}_test.rs".to_string(),
        ])
        .with_preconditions(vec![
            "API framework is set up (Express, Axum, etc.)".to_string(),
            "Database connection is configured if needed".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:313-378 method unit_test <-> ./crates/ccswarm/src/template/predefined.rs:633-701 method documentation_page
  Similarity: 96.46%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:unit_test (lines 313-378) ---[0m
    pub fn unit_test() -> Template {
        Template::new(
            "unit-test",
            "Unit Test",
            "Create comprehensive unit tests for a function or module",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "unit-test".to_string(),
            "quality".to_string(),
        ])
        .with_task_description("Create unit tests for {{test_target}} covering {{test_scenarios}}")
        .with_task_details(
            "## Test Specification\n\n\
            - **Target**: {{test_target}}\n\
            - **Test Framework**: {{test_framework}}\n\
            - **Scenarios**: {{test_scenarios}}\n\
            {{#if mocking}}- **Mocking**: {{mocking_strategy}}\n{{/if}}\n\n\
            ## Test Coverage Requirements\n\n\
            - Happy path scenarios\n\
            - Error conditions and edge cases\n\
            - Boundary value testing\n\
            - Input validation testing\n\
            {{#if async_code}}- Async operation testing\n{{/if}}\
            {{#if performance}}- Performance benchmarks\n{{/if}}\
            - Achieve >90% code coverage for the target",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Testing)
        .with_duration(35)
        .with_variables(vec![
            TemplateVariable::text("test_target", "Function, class, or module to test"),
            TemplateVariable::text("test_scenarios", "Specific scenarios to test"),
            TemplateVariable::choice(
                "test_framework",
                "Testing framework",
                vec![
                    "Jest".to_string(),
                    "Vitest".to_string(),
                    "Mocha".to_string(),
                    "pytest".to_string(),
                    "Rust test".to_string(),
                    "Go test".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "mocking_strategy",
                "Mocking approach",
                vec![
                    "Manual mocks".to_string(),
                    "Jest mocks".to_string(),
                    "Sinon".to_string(),
                    "unittest.mock".to_string(),
                    "mockall".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("async_code", "Testing async operations"),
            TemplateVariable::boolean("performance", "Include performance tests"),
        ])
        .with_target_files(vec![
            "tests/{{test_target}}.test.{{file_extension}}".to_string()
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:documentation_page (lines 633-701) ---[0m
    pub fn documentation_page() -> Template {
        Template::new(
            "documentation-page",
            "Documentation Page",
            "Create comprehensive documentation for a feature or API",
            TemplateCategory::Documentation,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "documentation".to_string(),
            "docs".to_string(),
            "guide".to_string(),
        ])
        .with_task_description(
            "Create documentation for {{doc_subject}} explaining {{doc_purpose}}",
        )
        .with_task_details(
            "## Documentation Specification\n\n\
            - **Subject**: {{doc_subject}}\n\
            - **Purpose**: {{doc_purpose}}\n\
            - **Audience**: {{target_audience}}\n\
            {{#if doc_type}}- **Type**: {{doc_type}}\n{{/if}}\n\n\
            ## Content Requirements\n\n\
            - Clear overview and introduction\n\
            - Step-by-step instructions or usage guide\n\
            - Code examples with explanations\n\
            - Common pitfalls and troubleshooting\n\
            - Links to related documentation\n\
            {{#if api_reference}}- Complete API reference with parameters\n{{/if}}\
            {{#if diagrams}}- Architecture or flow diagrams\n{{/if}}\
            {{#if examples}}- Real-world usage examples\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Documentation)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("doc_subject", "What is being documented"),
            TemplateVariable::text("doc_purpose", "Purpose of the documentation"),
            TemplateVariable::choice(
                "target_audience",
                "Target audience",
                vec![
                    "Developers".to_string(),
                    "Users".to_string(),
                    "System Administrators".to_string(),
                    "Product Managers".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "doc_type",
                "Documentation type",
                vec![
                    "API Reference".to_string(),
                    "User Guide".to_string(),
                    "Tutorial".to_string(),
                    "Architecture Guide".to_string(),
                    "Troubleshooting".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("api_reference", "Include API reference"),
            TemplateVariable::boolean("diagrams", "Include diagrams"),
            TemplateVariable::boolean("examples", "Include code examples"),
        ])
        .with_target_files(vec![
            "docs/{{doc_subject}}.md".to_string(),
            "examples/{{doc_subject}}_example.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:313-378 method unit_test <-> ./crates/ccswarm/src/template/predefined.rs:562-630 method ci_pipeline
  Similarity: 96.22%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:unit_test (lines 313-378) ---[0m
    pub fn unit_test() -> Template {
        Template::new(
            "unit-test",
            "Unit Test",
            "Create comprehensive unit tests for a function or module",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "unit-test".to_string(),
            "quality".to_string(),
        ])
        .with_task_description("Create unit tests for {{test_target}} covering {{test_scenarios}}")
        .with_task_details(
            "## Test Specification\n\n\
            - **Target**: {{test_target}}\n\
            - **Test Framework**: {{test_framework}}\n\
            - **Scenarios**: {{test_scenarios}}\n\
            {{#if mocking}}- **Mocking**: {{mocking_strategy}}\n{{/if}}\n\n\
            ## Test Coverage Requirements\n\n\
            - Happy path scenarios\n\
            - Error conditions and edge cases\n\
            - Boundary value testing\n\
            - Input validation testing\n\
            {{#if async_code}}- Async operation testing\n{{/if}}\
            {{#if performance}}- Performance benchmarks\n{{/if}}\
            - Achieve >90% code coverage for the target",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Testing)
        .with_duration(35)
        .with_variables(vec![
            TemplateVariable::text("test_target", "Function, class, or module to test"),
            TemplateVariable::text("test_scenarios", "Specific scenarios to test"),
            TemplateVariable::choice(
                "test_framework",
                "Testing framework",
                vec![
                    "Jest".to_string(),
                    "Vitest".to_string(),
                    "Mocha".to_string(),
                    "pytest".to_string(),
                    "Rust test".to_string(),
                    "Go test".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "mocking_strategy",
                "Mocking approach",
                vec![
                    "Manual mocks".to_string(),
                    "Jest mocks".to_string(),
                    "Sinon".to_string(),
                    "unittest.mock".to_string(),
                    "mockall".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("async_code", "Testing async operations"),
            TemplateVariable::boolean("performance", "Include performance tests"),
        ])
        .with_target_files(vec![
            "tests/{{test_target}}.test.{{file_extension}}".to_string()
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:ci_pipeline (lines 562-630) ---[0m
    pub fn ci_pipeline() -> Template {
        Template::new(
            "ci-pipeline",
            "CI/CD Pipeline",
            "Create a continuous integration pipeline",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "ci".to_string(),
            "cd".to_string(),
            "pipeline".to_string(),
            "automation".to_string(),
        ])
        .with_task_description("Create {{ci_platform}} pipeline for {{project_type}} project")
        .with_task_details(
            "## CI/CD Pipeline Configuration\n\n\
            - **Platform**: {{ci_platform}}\n\
            - **Project Type**: {{project_type}}\n\
            {{#if triggers}}- **Triggers**: {{triggers}}\n{{/if}}\
            {{#if environments}}- **Environments**: {{environments}}\n{{/if}}\n\n\
            ## Pipeline Stages\n\n\
            - Code checkout and setup\n\
            - Dependency installation\n\
            - Linting and code quality checks\n\
            - Unit and integration tests\n\
            - Build and package\n\
            {{#if security_scan}}- Security vulnerability scanning\n{{/if}}\
            {{#if deployment}}- Automated deployment to {{environments}}\n{{/if}}\
            {{#if notifications}}- Notification on success/failure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "ci_platform",
                "CI/CD platform",
                vec![
                    "GitHub Actions".to_string(),
                    "GitLab CI".to_string(),
                    "Jenkins".to_string(),
                    "CircleCI".to_string(),
                    "Azure DevOps".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "project_type",
                "Project type",
                vec![
                    "Node.js".to_string(),
                    "Python".to_string(),
                    "Rust".to_string(),
                    "Go".to_string(),
                    "Java".to_string(),
                    "Docker".to_string(),
                ],
            ),
            TemplateVariable::text("triggers", "Pipeline triggers (e.g., push, PR)").optional(),
            TemplateVariable::text("environments", "Deployment environments").optional(),
            TemplateVariable::boolean("security_scan", "Include security scanning"),
            TemplateVariable::boolean("deployment", "Include deployment stages"),
            TemplateVariable::boolean("notifications", "Set up notifications"),
        ])
        .with_target_files(vec![
            ".github/workflows/ci.yml".to_string(),
            ".github/workflows/cd.yml".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:169-250 method api_endpoint <-> ./crates/ccswarm/src/template/predefined.rs:875-928 method performance_optimization
  Similarity: 94.77%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:api_endpoint (lines 169-250) ---[0m
    pub fn api_endpoint() -> Template {
        Template::new(
            "api-endpoint",
            "API Endpoint",
            "Create a new REST API endpoint with proper error handling",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "api".to_string(),
            "backend".to_string(),
            "rest".to_string(),
            "endpoint".to_string(),
        ])
        .with_task_description(
            "Create a {{http_method}} API endpoint for {{endpoint_path}} to {{endpoint_purpose}}",
        )
        .with_task_details(
            "## API Endpoint Specification\n\n\
            - **Method**: {{http_method}}\n\
            - **Path**: {{endpoint_path}}\n\
            - **Purpose**: {{endpoint_purpose}}\n\
            {{#if request_body}}- **Request Body**: {{request_body}}\n{{/if}}\
            {{#if response_format}}- **Response**: {{response_format}}\n{{/if}}\
            {{#if authentication}}- **Authentication**: {{authentication}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Implement proper HTTP status codes\n\
            - Add comprehensive error handling\n\
            - Include request validation\n\
            - Add API documentation (OpenAPI/Swagger)\n\
            - Implement proper logging\n\
            {{#if database}}- Database operations with proper transactions\n{{/if}}\
            {{#if caching}}- Implement caching strategy\n{{/if}}\
            {{#if rate_limiting}}- Add rate limiting\n{{/if}}\
            {{#if testing}}- Create unit and integration tests\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "http_method",
                "HTTP method",
                vec![
                    "GET".to_string(),
                    "POST".to_string(),
                    "PUT".to_string(),
                    "PATCH".to_string(),
                    "DELETE".to_string(),
                ],
            ),
            TemplateVariable::text("endpoint_path", "API endpoint path (e.g., /api/users)"),
            TemplateVariable::text("endpoint_purpose", "What this endpoint does"),
            TemplateVariable::text("request_body", "Request body format (optional)").optional(),
            TemplateVariable::text("response_format", "Response format (optional)").optional(),
            TemplateVariable::choice(
                "authentication",
                "Authentication method",
                vec![
                    "None".to_string(),
                    "JWT Bearer".to_string(),
                    "API Key".to_string(),
                    "OAuth2".to_string(),
                    "Basic Auth".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("database", "Includes database operations"),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("rate_limiting", "Add rate limiting"),
            TemplateVariable::boolean("testing", "Create tests"),
        ])
        .with_target_files(vec![
            "src/routes/{{endpoint_path}}.rs".to_string(),
            "src/handlers/{{endpoint_path}}_handler.rs".to_string(),
            "tests/{{endpoint_path}}_test.rs".to_string(),
        ])
        .with_preconditions(vec![
            "API framework is set up (Express, Axum, etc.)".to_string(),
            "Database connection is configured if needed".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:performance_optimization (lines 875-928) ---[0m
    pub fn performance_optimization() -> Template {
        Template::new(
            "performance-optimization",
            "Performance Optimization",
            "Optimize performance of a system component",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "performance".to_string(),
            "optimization".to_string(),
            "profiling".to_string(),
        ])
        .with_task_description(
            "Optimize performance of {{optimization_target}} to improve {{performance_metric}}",
        )
        .with_task_details(
            "## Performance Optimization Plan\n\n\
            - **Target**: {{optimization_target}}\n\
            - **Metric**: {{performance_metric}}\n\
            - **Current Performance**: {{current_baseline}}\n\
            - **Target Performance**: {{target_goal}}\n\n\
            ## Optimization Approach\n\n\
            - Profile current performance with appropriate tools\n\
            - Identify bottlenecks and hot paths\n\
            - Implement targeted optimizations\n\
            - Measure impact of each optimization\n\
            - Verify no functional regressions\n\
            {{#if caching}}- Implement caching strategies\n{{/if}}\
            {{#if database}}- Optimize database queries and indexes\n{{/if}}\
            {{#if memory}}- Reduce memory allocation and fragmentation\n{{/if}}\
            {{#if concurrency}}- Improve concurrency and parallelization\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("optimization_target", "Component to optimize"),
            TemplateVariable::text(
                "performance_metric",
                "Metric to improve (latency, throughput, etc.)",
            ),
            TemplateVariable::text("current_baseline", "Current performance baseline").optional(),
            TemplateVariable::text("target_goal", "Performance improvement goal").optional(),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("database", "Optimize database operations"),
            TemplateVariable::boolean("memory", "Optimize memory usage"),
            TemplateVariable::boolean("concurrency", "Improve concurrency"),
        ])
        .with_target_files(vec![
            "performance-analysis-{{optimization_target}}.md".to_string(),
            "benchmarks/{{optimization_target}}_bench.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:32-108 method react_component <-> ./crates/ccswarm/src/template/predefined.rs:501-559 method docker_service
  Similarity: 94.12%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_component (lines 32-108) ---[0m
    pub fn react_component() -> Template {
        Template::new(
            "react-component",
            "React Component",
            "Create a new React functional component with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec!["react".to_string(), "frontend".to_string(), "typescript".to_string(), "component".to_string()])
        .with_task_description("Create a {{component_type}} React component named {{component_name}}{{#if props}} with props: {{props}}{{/if}}{{#if styling}} using {{styling}}{{/if}}")
        .with_task_details(
            "## Component Specification\n\n\
            - **Name**: {{component_name}}\n\
            - **Type**: {{component_type}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if props}}- **Props**: {{props}}\n{{/if}}\
            {{#if state}}- **State**: {{state}}\n{{/if}}\
            {{#if styling}}- **Styling**: {{styling}}\n{{/if}}\
            {{#if accessibility}}- **Accessibility**: Include ARIA attributes and keyboard navigation\n{{/if}}\
            {{#if testing}}- **Testing**: Create unit tests with React Testing Library\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React best practices and hooks conventions\n\
            - Implement proper error boundaries if needed\n\
            - Add PropTypes or TypeScript interfaces for props\n\
            - Include JSDoc comments for component documentation\n\
            {{#if responsive}}- Make component responsive for mobile and desktop\n{{/if}}\
            {{#if performance}}- Optimize for performance with React.memo if appropriate\n{{/if}}"
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("component_name", "Name of the React component"),
            TemplateVariable::choice(
                "component_type",
                "Type of component",
                vec!["functional".to_string(), "class".to_string(), "custom hook".to_string()],
            ),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the component",
                "src/components/{{component_name}}.tsx"
            ),
            TemplateVariable::text("props", "Component props (optional)").optional(),
            TemplateVariable::text("state", "Component state description (optional)").optional(),
            TemplateVariable::choice(
                "styling",
                "Styling approach",
                vec![
                    "CSS Modules".to_string(),
                    "Styled Components".to_string(),
                    "Tailwind CSS".to_string(),
                    "SCSS".to_string(),
                    "Emotion".to_string(),
                ],
            ).optional(),
            TemplateVariable::boolean("accessibility", "Include accessibility features"),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("responsive", "Make responsive design"),
            TemplateVariable::boolean("performance", "Optimize for performance"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.tsx".to_string(),
            "{{file_path}}.module.css".to_string(),
        ])
        .with_preconditions(vec![
            "React and TypeScript are configured in the project".to_string(),
            "Component directory structure exists".to_string(),
        ])
        .with_post_actions(vec![
            "Export component from index file".to_string(),
            "Add to Storybook if available".to_string(),
            "Update documentation".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:docker_service (lines 501-559) ---[0m
    pub fn docker_service() -> Template {
        Template::new(
            "docker-service",
            "Docker Service",
            "Create a Docker container configuration for a service",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "docker".to_string(),
            "container".to_string(),
            "devops".to_string(),
        ])
        .with_task_description("Create Docker configuration for {{service_name}} service")
        .with_task_details(
            "## Docker Service Configuration\n\n\
            - **Service**: {{service_name}}\n\
            - **Base Image**: {{base_image}}\n\
            {{#if ports}}- **Ports**: {{ports}}\n{{/if}}\
            {{#if volumes}}- **Volumes**: {{volumes}}\n{{/if}}\
            {{#if environment}}- **Environment**: {{environment}}\n{{/if}}\n\n\
            ## Container Requirements\n\n\
            - Multi-stage build for optimization\n\
            - Non-root user for security\n\
            - Health check configuration\n\
            - Proper signal handling\n\
            - Minimal attack surface\n\
            {{#if secrets}}- Secure secret management\n{{/if}}\
            {{#if monitoring}}- Monitoring and logging setup\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("service_name", "Name of the service"),
            TemplateVariable::choice(
                "base_image",
                "Base Docker image",
                vec![
                    "alpine".to_string(),
                    "ubuntu".to_string(),
                    "node:alpine".to_string(),
                    "python:alpine".to_string(),
                    "rust:alpine".to_string(),
                    "nginx:alpine".to_string(),
                ],
            ),
            TemplateVariable::text("ports", "Port mappings (e.g., 8080:8080)").optional(),
            TemplateVariable::text("volumes", "Volume mounts").optional(),
            TemplateVariable::text("environment", "Environment variables").optional(),
            TemplateVariable::boolean("secrets", "Include secret management"),
            TemplateVariable::boolean("monitoring", "Add monitoring setup"),
        ])
        .with_target_files(vec![
            "Dockerfile".to_string(),
            "docker-compose.yml".to_string(),
            ".dockerignore".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:32-108 method react_component <-> ./crates/ccswarm/src/template/predefined.rs:381-439 method integration_test
  Similarity: 93.45%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_component (lines 32-108) ---[0m
    pub fn react_component() -> Template {
        Template::new(
            "react-component",
            "React Component",
            "Create a new React functional component with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec!["react".to_string(), "frontend".to_string(), "typescript".to_string(), "component".to_string()])
        .with_task_description("Create a {{component_type}} React component named {{component_name}}{{#if props}} with props: {{props}}{{/if}}{{#if styling}} using {{styling}}{{/if}}")
        .with_task_details(
            "## Component Specification\n\n\
            - **Name**: {{component_name}}\n\
            - **Type**: {{component_type}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if props}}- **Props**: {{props}}\n{{/if}}\
            {{#if state}}- **State**: {{state}}\n{{/if}}\
            {{#if styling}}- **Styling**: {{styling}}\n{{/if}}\
            {{#if accessibility}}- **Accessibility**: Include ARIA attributes and keyboard navigation\n{{/if}}\
            {{#if testing}}- **Testing**: Create unit tests with React Testing Library\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React best practices and hooks conventions\n\
            - Implement proper error boundaries if needed\n\
            - Add PropTypes or TypeScript interfaces for props\n\
            - Include JSDoc comments for component documentation\n\
            {{#if responsive}}- Make component responsive for mobile and desktop\n{{/if}}\
            {{#if performance}}- Optimize for performance with React.memo if appropriate\n{{/if}}"
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("component_name", "Name of the React component"),
            TemplateVariable::choice(
                "component_type",
                "Type of component",
                vec!["functional".to_string(), "class".to_string(), "custom hook".to_string()],
            ),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the component",
                "src/components/{{component_name}}.tsx"
            ),
            TemplateVariable::text("props", "Component props (optional)").optional(),
            TemplateVariable::text("state", "Component state description (optional)").optional(),
            TemplateVariable::choice(
                "styling",
                "Styling approach",
                vec![
                    "CSS Modules".to_string(),
                    "Styled Components".to_string(),
                    "Tailwind CSS".to_string(),
                    "SCSS".to_string(),
                    "Emotion".to_string(),
                ],
            ).optional(),
            TemplateVariable::boolean("accessibility", "Include accessibility features"),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("responsive", "Make responsive design"),
            TemplateVariable::boolean("performance", "Optimize for performance"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.tsx".to_string(),
            "{{file_path}}.module.css".to_string(),
        ])
        .with_preconditions(vec![
            "React and TypeScript are configured in the project".to_string(),
            "Component directory structure exists".to_string(),
        ])
        .with_post_actions(vec![
            "Export component from index file".to_string(),
            "Add to Storybook if available".to_string(),
            "Update documentation".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:integration_test (lines 381-439) ---[0m
    pub fn integration_test() -> Template {
        Template::new(
            "integration-test",
            "Integration Test",
            "Create integration tests for system components",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "integration".to_string(),
            "e2e".to_string(),
        ])
        .with_task_description(
            "Create integration tests for {{system_component}} testing {{integration_points}}",
        )
        .with_task_details(
            "## Integration Test Specification\n\n\
            - **Component**: {{system_component}}\n\
            - **Integration Points**: {{integration_points}}\n\
            - **Test Environment**: {{test_environment}}\n\
            {{#if database}}- **Database**: Test database setup required\n{{/if}}\
            {{#if external_services}}- **External Services**: {{external_services}}\n{{/if}}\n\n\
            ## Test Requirements\n\n\
            - Set up test environment and data\n\
            - Test end-to-end workflows\n\
            - Verify data consistency across components\n\
            - Test error propagation and recovery\n\
            - Clean up test data after execution\n\
            {{#if performance}}- Measure integration performance\n{{/if}}\
            {{#if security}}- Verify security boundaries\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Testing)
        .with_duration(75)
        .with_variables(vec![
            TemplateVariable::text("system_component", "System component being tested"),
            TemplateVariable::text("integration_points", "What integrations to test"),
            TemplateVariable::choice(
                "test_environment",
                "Test environment",
                vec![
                    "Docker Compose".to_string(),
                    "Testcontainers".to_string(),
                    "In-memory".to_string(),
                    "Staging".to_string(),
                ],
            ),
            TemplateVariable::boolean("database", "Includes database testing"),
            TemplateVariable::text("external_services", "External services involved").optional(),
            TemplateVariable::boolean("performance", "Include performance testing"),
            TemplateVariable::boolean("security", "Include security testing"),
        ])
        .with_target_files(vec![
            "tests/integration/{{system_component}}_integration.test.{{file_extension}}"
                .to_string(),
            "tests/fixtures/{{system_component}}_data.json".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:32-108 method react_component <-> ./crates/ccswarm/src/template/predefined.rs:704-761 method rust_module
  Similarity: 94.12%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_component (lines 32-108) ---[0m
    pub fn react_component() -> Template {
        Template::new(
            "react-component",
            "React Component",
            "Create a new React functional component with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec!["react".to_string(), "frontend".to_string(), "typescript".to_string(), "component".to_string()])
        .with_task_description("Create a {{component_type}} React component named {{component_name}}{{#if props}} with props: {{props}}{{/if}}{{#if styling}} using {{styling}}{{/if}}")
        .with_task_details(
            "## Component Specification\n\n\
            - **Name**: {{component_name}}\n\
            - **Type**: {{component_type}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if props}}- **Props**: {{props}}\n{{/if}}\
            {{#if state}}- **State**: {{state}}\n{{/if}}\
            {{#if styling}}- **Styling**: {{styling}}\n{{/if}}\
            {{#if accessibility}}- **Accessibility**: Include ARIA attributes and keyboard navigation\n{{/if}}\
            {{#if testing}}- **Testing**: Create unit tests with React Testing Library\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React best practices and hooks conventions\n\
            - Implement proper error boundaries if needed\n\
            - Add PropTypes or TypeScript interfaces for props\n\
            - Include JSDoc comments for component documentation\n\
            {{#if responsive}}- Make component responsive for mobile and desktop\n{{/if}}\
            {{#if performance}}- Optimize for performance with React.memo if appropriate\n{{/if}}"
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("component_name", "Name of the React component"),
            TemplateVariable::choice(
                "component_type",
                "Type of component",
                vec!["functional".to_string(), "class".to_string(), "custom hook".to_string()],
            ),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the component",
                "src/components/{{component_name}}.tsx"
            ),
            TemplateVariable::text("props", "Component props (optional)").optional(),
            TemplateVariable::text("state", "Component state description (optional)").optional(),
            TemplateVariable::choice(
                "styling",
                "Styling approach",
                vec![
                    "CSS Modules".to_string(),
                    "Styled Components".to_string(),
                    "Tailwind CSS".to_string(),
                    "SCSS".to_string(),
                    "Emotion".to_string(),
                ],
            ).optional(),
            TemplateVariable::boolean("accessibility", "Include accessibility features"),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("responsive", "Make responsive design"),
            TemplateVariable::boolean("performance", "Optimize for performance"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.tsx".to_string(),
            "{{file_path}}.module.css".to_string(),
        ])
        .with_preconditions(vec![
            "React and TypeScript are configured in the project".to_string(),
            "Component directory structure exists".to_string(),
        ])
        .with_post_actions(vec![
            "Export component from index file".to_string(),
            "Add to Storybook if available".to_string(),
            "Update documentation".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_module (lines 704-761) ---[0m
    pub fn rust_module() -> Template {
        Template::new(
            "rust-module",
            "Rust Module",
            "Create a new Rust module with proper structure and documentation",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "module".to_string(),
            "backend".to_string(),
        ])
        .with_task_description("Create Rust module {{module_name}} for {{module_purpose}}")
        .with_task_details(
            "## Rust Module Specification\n\n\
            - **Module**: {{module_name}}\n\
            - **Purpose**: {{module_purpose}}\n\
            - **Visibility**: {{visibility}}\n\
            {{#if traits}}- **Traits**: {{traits}}\n{{/if}}\
            {{#if structs}}- **Structs**: {{structs}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Follow Rust naming conventions\n\
            - Add comprehensive rustdoc comments\n\
            - Implement proper error handling with Result<T, E>\n\
            - Use appropriate visibility modifiers\n\
            - Include unit tests in #[cfg(test)] module\n\
            {{#if async_support}}- Implement async/await patterns\n{{/if}}\
            {{#if serde_support}}- Add Serde serialization support\n{{/if}}\
            {{#if benchmark}}- Include criterion benchmarks\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("module_name", "Name of the Rust module"),
            TemplateVariable::text("module_purpose", "What this module does"),
            TemplateVariable::choice(
                "visibility",
                "Module visibility",
                vec![
                    "public".to_string(),
                    "private".to_string(),
                    "pub(crate)".to_string(),
                ],
            ),
            TemplateVariable::text("traits", "Traits to implement").optional(),
            TemplateVariable::text("structs", "Main structs in the module").optional(),
            TemplateVariable::boolean("async_support", "Include async support"),
            TemplateVariable::boolean("serde_support", "Add Serde support"),
            TemplateVariable::boolean("benchmark", "Include benchmarks"),
        ])
        .with_target_files(vec![
            "src/{{module_name}}/mod.rs".to_string(),
            "src/{{module_name}}/types.rs".to_string(),
            "benches/{{module_name}}_bench.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:32-108 method react_component <-> ./crates/ccswarm/src/template/predefined.rs:253-310 method database_migration
  Similarity: 93.61%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_component (lines 32-108) ---[0m
    pub fn react_component() -> Template {
        Template::new(
            "react-component",
            "React Component",
            "Create a new React functional component with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec!["react".to_string(), "frontend".to_string(), "typescript".to_string(), "component".to_string()])
        .with_task_description("Create a {{component_type}} React component named {{component_name}}{{#if props}} with props: {{props}}{{/if}}{{#if styling}} using {{styling}}{{/if}}")
        .with_task_details(
            "## Component Specification\n\n\
            - **Name**: {{component_name}}\n\
            - **Type**: {{component_type}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if props}}- **Props**: {{props}}\n{{/if}}\
            {{#if state}}- **State**: {{state}}\n{{/if}}\
            {{#if styling}}- **Styling**: {{styling}}\n{{/if}}\
            {{#if accessibility}}- **Accessibility**: Include ARIA attributes and keyboard navigation\n{{/if}}\
            {{#if testing}}- **Testing**: Create unit tests with React Testing Library\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React best practices and hooks conventions\n\
            - Implement proper error boundaries if needed\n\
            - Add PropTypes or TypeScript interfaces for props\n\
            - Include JSDoc comments for component documentation\n\
            {{#if responsive}}- Make component responsive for mobile and desktop\n{{/if}}\
            {{#if performance}}- Optimize for performance with React.memo if appropriate\n{{/if}}"
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("component_name", "Name of the React component"),
            TemplateVariable::choice(
                "component_type",
                "Type of component",
                vec!["functional".to_string(), "class".to_string(), "custom hook".to_string()],
            ),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the component",
                "src/components/{{component_name}}.tsx"
            ),
            TemplateVariable::text("props", "Component props (optional)").optional(),
            TemplateVariable::text("state", "Component state description (optional)").optional(),
            TemplateVariable::choice(
                "styling",
                "Styling approach",
                vec![
                    "CSS Modules".to_string(),
                    "Styled Components".to_string(),
                    "Tailwind CSS".to_string(),
                    "SCSS".to_string(),
                    "Emotion".to_string(),
                ],
            ).optional(),
            TemplateVariable::boolean("accessibility", "Include accessibility features"),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("responsive", "Make responsive design"),
            TemplateVariable::boolean("performance", "Optimize for performance"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.tsx".to_string(),
            "{{file_path}}.module.css".to_string(),
        ])
        .with_preconditions(vec![
            "React and TypeScript are configured in the project".to_string(),
            "Component directory structure exists".to_string(),
        ])
        .with_post_actions(vec![
            "Export component from index file".to_string(),
            "Add to Storybook if available".to_string(),
            "Update documentation".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:database_migration (lines 253-310) ---[0m
    pub fn database_migration() -> Template {
        Template::new(
            "database-migration",
            "Database Migration",
            "Create a database migration script",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "database".to_string(),
            "migration".to_string(),
            "schema".to_string(),
        ])
        .with_task_description("Create database migration to {{migration_purpose}}")
        .with_task_details(
            "## Migration Details\n\n\
            - **Purpose**: {{migration_purpose}}\n\
            - **Type**: {{migration_type}}\n\
            {{#if table_name}}- **Table**: {{table_name}}\n{{/if}}\
            {{#if columns}}- **Columns**: {{columns}}\n{{/if}}\
            {{#if indexes}}- **Indexes**: {{indexes}}\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Write both up and down migrations\n\
            - Include proper constraints and indexes\n\
            - Handle existing data appropriately\n\
            - Add comments for complex operations\n\
            - Test migration on sample data\n\
            {{#if backup}}- Create backup before running\n{{/if}}\
            {{#if rollback_plan}}- Document rollback procedure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(40)
        .with_variables(vec![
            TemplateVariable::text("migration_purpose", "What this migration accomplishes"),
            TemplateVariable::choice(
                "migration_type",
                "Type of migration",
                vec![
                    "Create Table".to_string(),
                    "Alter Table".to_string(),
                    "Drop Table".to_string(),
                    "Add Column".to_string(),
                    "Drop Column".to_string(),
                    "Create Index".to_string(),
                    "Data Migration".to_string(),
                ],
            ),
            TemplateVariable::text("table_name", "Table name (if applicable)").optional(),
            TemplateVariable::text("columns", "Column definitions (optional)").optional(),
            TemplateVariable::text("indexes", "Index definitions (optional)").optional(),
            TemplateVariable::boolean("backup", "Create backup before migration"),
            TemplateVariable::boolean("rollback_plan", "Document rollback procedure"),
        ])
        .with_target_files(vec![
            "migrations/{{current_datetime}}_{{migration_purpose}}.sql".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:32-108 method react_component <-> ./crates/ccswarm/src/template/predefined.rs:442-498 method bug_fix
  Similarity: 93.61%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_component (lines 32-108) ---[0m
    pub fn react_component() -> Template {
        Template::new(
            "react-component",
            "React Component",
            "Create a new React functional component with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec!["react".to_string(), "frontend".to_string(), "typescript".to_string(), "component".to_string()])
        .with_task_description("Create a {{component_type}} React component named {{component_name}}{{#if props}} with props: {{props}}{{/if}}{{#if styling}} using {{styling}}{{/if}}")
        .with_task_details(
            "## Component Specification\n\n\
            - **Name**: {{component_name}}\n\
            - **Type**: {{component_type}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if props}}- **Props**: {{props}}\n{{/if}}\
            {{#if state}}- **State**: {{state}}\n{{/if}}\
            {{#if styling}}- **Styling**: {{styling}}\n{{/if}}\
            {{#if accessibility}}- **Accessibility**: Include ARIA attributes and keyboard navigation\n{{/if}}\
            {{#if testing}}- **Testing**: Create unit tests with React Testing Library\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React best practices and hooks conventions\n\
            - Implement proper error boundaries if needed\n\
            - Add PropTypes or TypeScript interfaces for props\n\
            - Include JSDoc comments for component documentation\n\
            {{#if responsive}}- Make component responsive for mobile and desktop\n{{/if}}\
            {{#if performance}}- Optimize for performance with React.memo if appropriate\n{{/if}}"
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("component_name", "Name of the React component"),
            TemplateVariable::choice(
                "component_type",
                "Type of component",
                vec!["functional".to_string(), "class".to_string(), "custom hook".to_string()],
            ),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the component",
                "src/components/{{component_name}}.tsx"
            ),
            TemplateVariable::text("props", "Component props (optional)").optional(),
            TemplateVariable::text("state", "Component state description (optional)").optional(),
            TemplateVariable::choice(
                "styling",
                "Styling approach",
                vec![
                    "CSS Modules".to_string(),
                    "Styled Components".to_string(),
                    "Tailwind CSS".to_string(),
                    "SCSS".to_string(),
                    "Emotion".to_string(),
                ],
            ).optional(),
            TemplateVariable::boolean("accessibility", "Include accessibility features"),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("responsive", "Make responsive design"),
            TemplateVariable::boolean("performance", "Optimize for performance"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.tsx".to_string(),
            "{{file_path}}.module.css".to_string(),
        ])
        .with_preconditions(vec![
            "React and TypeScript are configured in the project".to_string(),
            "Component directory structure exists".to_string(),
        ])
        .with_post_actions(vec![
            "Export component from index file".to_string(),
            "Add to Storybook if available".to_string(),
            "Update documentation".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:bug_fix (lines 442-498) ---[0m
    pub fn bug_fix() -> Template {
        Template::new(
            "bug-fix",
            "Bug Fix",
            "Fix a reported bug with proper testing and documentation",
            TemplateCategory::BugFix,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "bug".to_string(),
            "fix".to_string(),
            "maintenance".to_string(),
        ])
        .with_task_description("Fix bug: {{bug_description}} ({{bug_severity}})")
        .with_task_details(
            "## Bug Report\n\n\
            - **Description**: {{bug_description}}\n\
            - **Severity**: {{bug_severity}}\n\
            {{#if reproduction_steps}}- **Reproduction**: {{reproduction_steps}}\n{{/if}}\
            {{#if affected_components}}- **Affected Components**: {{affected_components}}\n{{/if}}\
            {{#if user_impact}}- **User Impact**: {{user_impact}}\n{{/if}}\n\n\
            ## Fix Requirements\n\n\
            - Reproduce the bug in a test environment\n\
            - Identify root cause through debugging\n\
            - Implement minimal fix that addresses root cause\n\
            - Add regression tests to prevent recurrence\n\
            - Verify fix doesn't introduce new issues\n\
            {{#if documentation}}- Update relevant documentation\n{{/if}}\
            {{#if release_notes}}- Add to release notes\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Bugfix)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("bug_description", "Clear description of the bug"),
            TemplateVariable::choice(
                "bug_severity",
                "Bug severity level",
                vec![
                    "Critical".to_string(),
                    "High".to_string(),
                    "Medium".to_string(),
                    "Low".to_string(),
                ],
            ),
            TemplateVariable::text("reproduction_steps", "Steps to reproduce the bug").optional(),
            TemplateVariable::text("affected_components", "Components affected by the bug")
                .optional(),
            TemplateVariable::text("user_impact", "How this affects users").optional(),
            TemplateVariable::boolean("documentation", "Update documentation"),
            TemplateVariable::boolean("release_notes", "Add to release notes"),
        ])
        .with_target_files(vec![
            "src/{{affected_components}}/fix.{{file_extension}}".to_string(),
            "tests/regression/{{bug_description}}.test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:32-108 method react_component <-> ./crates/ccswarm/src/template/predefined.rs:931-987 method code_refactor
  Similarity: 93.61%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_component (lines 32-108) ---[0m
    pub fn react_component() -> Template {
        Template::new(
            "react-component",
            "React Component",
            "Create a new React functional component with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec!["react".to_string(), "frontend".to_string(), "typescript".to_string(), "component".to_string()])
        .with_task_description("Create a {{component_type}} React component named {{component_name}}{{#if props}} with props: {{props}}{{/if}}{{#if styling}} using {{styling}}{{/if}}")
        .with_task_details(
            "## Component Specification\n\n\
            - **Name**: {{component_name}}\n\
            - **Type**: {{component_type}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if props}}- **Props**: {{props}}\n{{/if}}\
            {{#if state}}- **State**: {{state}}\n{{/if}}\
            {{#if styling}}- **Styling**: {{styling}}\n{{/if}}\
            {{#if accessibility}}- **Accessibility**: Include ARIA attributes and keyboard navigation\n{{/if}}\
            {{#if testing}}- **Testing**: Create unit tests with React Testing Library\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React best practices and hooks conventions\n\
            - Implement proper error boundaries if needed\n\
            - Add PropTypes or TypeScript interfaces for props\n\
            - Include JSDoc comments for component documentation\n\
            {{#if responsive}}- Make component responsive for mobile and desktop\n{{/if}}\
            {{#if performance}}- Optimize for performance with React.memo if appropriate\n{{/if}}"
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("component_name", "Name of the React component"),
            TemplateVariable::choice(
                "component_type",
                "Type of component",
                vec!["functional".to_string(), "class".to_string(), "custom hook".to_string()],
            ),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the component",
                "src/components/{{component_name}}.tsx"
            ),
            TemplateVariable::text("props", "Component props (optional)").optional(),
            TemplateVariable::text("state", "Component state description (optional)").optional(),
            TemplateVariable::choice(
                "styling",
                "Styling approach",
                vec![
                    "CSS Modules".to_string(),
                    "Styled Components".to_string(),
                    "Tailwind CSS".to_string(),
                    "SCSS".to_string(),
                    "Emotion".to_string(),
                ],
            ).optional(),
            TemplateVariable::boolean("accessibility", "Include accessibility features"),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("responsive", "Make responsive design"),
            TemplateVariable::boolean("performance", "Optimize for performance"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.tsx".to_string(),
            "{{file_path}}.module.css".to_string(),
        ])
        .with_preconditions(vec![
            "React and TypeScript are configured in the project".to_string(),
            "Component directory structure exists".to_string(),
        ])
        .with_post_actions(vec![
            "Export component from index file".to_string(),
            "Add to Storybook if available".to_string(),
            "Update documentation".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:code_refactor (lines 931-987) ---[0m
    pub fn code_refactor() -> Template {
        Template::new(
            "code-refactor",
            "Code Refactoring",
            "Refactor code to improve maintainability and design",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "refactor".to_string(),
            "maintainability".to_string(),
            "design".to_string(),
        ])
        .with_task_description("Refactor {{refactor_target}} to {{refactor_goal}}")
        .with_task_details(
            "## Refactoring Plan\n\n\
            - **Target**: {{refactor_target}}\n\
            - **Goal**: {{refactor_goal}}\n\
            - **Refactor Type**: {{refactor_type}}\n\
            {{#if design_patterns}}- **Design Patterns**: {{design_patterns}}\n{{/if}}\n\n\
            ## Refactoring Steps\n\n\
            - Analyze current code structure and identify issues\n\
            - Create comprehensive test coverage before refactoring\n\
            - Apply refactoring techniques incrementally\n\
            - Verify tests pass after each step\n\
            - Update documentation to reflect changes\n\
            {{#if breaking_changes}}- Document any breaking changes\n{{/if}}\
            {{#if performance_impact}}- Measure performance impact\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(80)
        .with_variables(vec![
            TemplateVariable::text("refactor_target", "Code to be refactored"),
            TemplateVariable::text("refactor_goal", "What the refactoring should achieve"),
            TemplateVariable::choice(
                "refactor_type",
                "Type of refactoring",
                vec![
                    "Extract Method".to_string(),
                    "Extract Class".to_string(),
                    "Move Method".to_string(),
                    "Rename".to_string(),
                    "Simplify Conditional".to_string(),
                    "Remove Duplication".to_string(),
                    "Improve Abstraction".to_string(),
                ],
            ),
            TemplateVariable::text("design_patterns", "Design patterns to apply").optional(),
            TemplateVariable::boolean("breaking_changes", "May introduce breaking changes"),
            TemplateVariable::boolean("performance_impact", "Measure performance impact"),
        ])
        .with_target_files(vec![
            "refactor-plan-{{refactor_target}}.md".to_string(),
            "tests/refactor/{{refactor_target}}_test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:169-250 method api_endpoint <-> ./crates/ccswarm/src/template/predefined.rs:764-813 method rust_cli_command
  Similarity: 94.94%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:api_endpoint (lines 169-250) ---[0m
    pub fn api_endpoint() -> Template {
        Template::new(
            "api-endpoint",
            "API Endpoint",
            "Create a new REST API endpoint with proper error handling",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "api".to_string(),
            "backend".to_string(),
            "rest".to_string(),
            "endpoint".to_string(),
        ])
        .with_task_description(
            "Create a {{http_method}} API endpoint for {{endpoint_path}} to {{endpoint_purpose}}",
        )
        .with_task_details(
            "## API Endpoint Specification\n\n\
            - **Method**: {{http_method}}\n\
            - **Path**: {{endpoint_path}}\n\
            - **Purpose**: {{endpoint_purpose}}\n\
            {{#if request_body}}- **Request Body**: {{request_body}}\n{{/if}}\
            {{#if response_format}}- **Response**: {{response_format}}\n{{/if}}\
            {{#if authentication}}- **Authentication**: {{authentication}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Implement proper HTTP status codes\n\
            - Add comprehensive error handling\n\
            - Include request validation\n\
            - Add API documentation (OpenAPI/Swagger)\n\
            - Implement proper logging\n\
            {{#if database}}- Database operations with proper transactions\n{{/if}}\
            {{#if caching}}- Implement caching strategy\n{{/if}}\
            {{#if rate_limiting}}- Add rate limiting\n{{/if}}\
            {{#if testing}}- Create unit and integration tests\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "http_method",
                "HTTP method",
                vec![
                    "GET".to_string(),
                    "POST".to_string(),
                    "PUT".to_string(),
                    "PATCH".to_string(),
                    "DELETE".to_string(),
                ],
            ),
            TemplateVariable::text("endpoint_path", "API endpoint path (e.g., /api/users)"),
            TemplateVariable::text("endpoint_purpose", "What this endpoint does"),
            TemplateVariable::text("request_body", "Request body format (optional)").optional(),
            TemplateVariable::text("response_format", "Response format (optional)").optional(),
            TemplateVariable::choice(
                "authentication",
                "Authentication method",
                vec![
                    "None".to_string(),
                    "JWT Bearer".to_string(),
                    "API Key".to_string(),
                    "OAuth2".to_string(),
                    "Basic Auth".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("database", "Includes database operations"),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("rate_limiting", "Add rate limiting"),
            TemplateVariable::boolean("testing", "Create tests"),
        ])
        .with_target_files(vec![
            "src/routes/{{endpoint_path}}.rs".to_string(),
            "src/handlers/{{endpoint_path}}_handler.rs".to_string(),
            "tests/{{endpoint_path}}_test.rs".to_string(),
        ])
        .with_preconditions(vec![
            "API framework is set up (Express, Axum, etc.)".to_string(),
            "Database connection is configured if needed".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_cli_command (lines 764-813) ---[0m
    pub fn rust_cli_command() -> Template {
        Template::new(
            "rust-cli-command",
            "Rust CLI Command",
            "Add a new command to a Rust CLI application using clap",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "cli".to_string(),
            "clap".to_string(),
            "command".to_string(),
        ])
        .with_task_description("Add CLI command {{command_name}} to {{command_description}}")
        .with_task_details(
            "## CLI Command Specification\n\n\
            - **Command**: {{command_name}}\n\
            - **Description**: {{command_description}}\n\
            {{#if arguments}}- **Arguments**: {{arguments}}\n{{/if}}\
            {{#if options}}- **Options**: {{options}}\n{{/if}}\
            {{#if subcommands}}- **Subcommands**: {{subcommands}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use clap derive macros for argument parsing\n\
            - Implement proper error handling and exit codes\n\
            - Add comprehensive help text and examples\n\
            - Include input validation\n\
            - Support common CLI patterns (--help, --version)\n\
            {{#if config_file}}- Support configuration file loading\n{{/if}}\
            {{#if logging}}- Integrate with logging framework\n{{/if}}\
            {{#if testing}}- Add integration tests for CLI\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(50)
        .with_variables(vec![
            TemplateVariable::text("command_name", "Name of the CLI command"),
            TemplateVariable::text("command_description", "What the command does"),
            TemplateVariable::text("arguments", "Required arguments").optional(),
            TemplateVariable::text("options", "Optional flags and options").optional(),
            TemplateVariable::text("subcommands", "Subcommands if any").optional(),
            TemplateVariable::boolean("config_file", "Support config files"),
            TemplateVariable::boolean("logging", "Add logging support"),
            TemplateVariable::boolean("testing", "Create CLI tests"),
        ])
        .with_target_files(vec![
            "src/cli/{{command_name}}.rs".to_string(),
            "tests/cli/{{command_name}}_test.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:32-108 method react_component <-> ./crates/ccswarm/src/template/predefined.rs:816-872 method security_review
  Similarity: 93.45%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_component (lines 32-108) ---[0m
    pub fn react_component() -> Template {
        Template::new(
            "react-component",
            "React Component",
            "Create a new React functional component with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec!["react".to_string(), "frontend".to_string(), "typescript".to_string(), "component".to_string()])
        .with_task_description("Create a {{component_type}} React component named {{component_name}}{{#if props}} with props: {{props}}{{/if}}{{#if styling}} using {{styling}}{{/if}}")
        .with_task_details(
            "## Component Specification\n\n\
            - **Name**: {{component_name}}\n\
            - **Type**: {{component_type}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if props}}- **Props**: {{props}}\n{{/if}}\
            {{#if state}}- **State**: {{state}}\n{{/if}}\
            {{#if styling}}- **Styling**: {{styling}}\n{{/if}}\
            {{#if accessibility}}- **Accessibility**: Include ARIA attributes and keyboard navigation\n{{/if}}\
            {{#if testing}}- **Testing**: Create unit tests with React Testing Library\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React best practices and hooks conventions\n\
            - Implement proper error boundaries if needed\n\
            - Add PropTypes or TypeScript interfaces for props\n\
            - Include JSDoc comments for component documentation\n\
            {{#if responsive}}- Make component responsive for mobile and desktop\n{{/if}}\
            {{#if performance}}- Optimize for performance with React.memo if appropriate\n{{/if}}"
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("component_name", "Name of the React component"),
            TemplateVariable::choice(
                "component_type",
                "Type of component",
                vec!["functional".to_string(), "class".to_string(), "custom hook".to_string()],
            ),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the component",
                "src/components/{{component_name}}.tsx"
            ),
            TemplateVariable::text("props", "Component props (optional)").optional(),
            TemplateVariable::text("state", "Component state description (optional)").optional(),
            TemplateVariable::choice(
                "styling",
                "Styling approach",
                vec![
                    "CSS Modules".to_string(),
                    "Styled Components".to_string(),
                    "Tailwind CSS".to_string(),
                    "SCSS".to_string(),
                    "Emotion".to_string(),
                ],
            ).optional(),
            TemplateVariable::boolean("accessibility", "Include accessibility features"),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("responsive", "Make responsive design"),
            TemplateVariable::boolean("performance", "Optimize for performance"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.tsx".to_string(),
            "{{file_path}}.module.css".to_string(),
        ])
        .with_preconditions(vec![
            "React and TypeScript are configured in the project".to_string(),
            "Component directory structure exists".to_string(),
        ])
        .with_post_actions(vec![
            "Export component from index file".to_string(),
            "Add to Storybook if available".to_string(),
            "Update documentation".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:security_review (lines 816-872) ---[0m
    pub fn security_review() -> Template {
        Template::new(
            "security-review",
            "Security Review",
            "Conduct a comprehensive security review of code or system",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "security".to_string(),
            "review".to_string(),
            "audit".to_string(),
        ])
        .with_task_description(
            "Conduct security review of {{review_target}} focusing on {{security_aspects}}",
        )
        .with_task_details(
            "## Security Review Scope\n\n\
            - **Target**: {{review_target}}\n\
            - **Focus Areas**: {{security_aspects}}\n\
            - **Review Type**: {{review_type}}\n\n\
            ## Security Checklist\n\n\
            - Authentication and authorization mechanisms\n\
            - Input validation and sanitization\n\
            - SQL injection and XSS vulnerabilities\n\
            - Secure data storage and transmission\n\
            - Error handling and information disclosure\n\
            - Dependencies and supply chain security\n\
            {{#if crypto}}- Cryptographic implementations\n{{/if}}\
            {{#if api_security}}- API security and rate limiting\n{{/if}}\
            {{#if infrastructure}}- Infrastructure security configuration\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Review)
        .with_duration(120)
        .with_variables(vec![
            TemplateVariable::text("review_target", "What is being reviewed"),
            TemplateVariable::text("security_aspects", "Specific security concerns"),
            TemplateVariable::choice(
                "review_type",
                "Type of security review",
                vec![
                    "Code Review".to_string(),
                    "Architecture Review".to_string(),
                    "Penetration Test".to_string(),
                    "Compliance Audit".to_string(),
                ],
            ),
            TemplateVariable::boolean("crypto", "Review cryptographic code"),
            TemplateVariable::boolean("api_security", "Review API security"),
            TemplateVariable::boolean("infrastructure", "Review infrastructure"),
        ])
        .with_target_files(vec![
            "security-review-{{review_target}}.md".to_string(),
            "security-checklist-{{review_target}}.md".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:381-439 method integration_test <-> ./crates/ccswarm/src/template/predefined.rs:633-701 method documentation_page
  Similarity: 97.68%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:integration_test (lines 381-439) ---[0m
    pub fn integration_test() -> Template {
        Template::new(
            "integration-test",
            "Integration Test",
            "Create integration tests for system components",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "integration".to_string(),
            "e2e".to_string(),
        ])
        .with_task_description(
            "Create integration tests for {{system_component}} testing {{integration_points}}",
        )
        .with_task_details(
            "## Integration Test Specification\n\n\
            - **Component**: {{system_component}}\n\
            - **Integration Points**: {{integration_points}}\n\
            - **Test Environment**: {{test_environment}}\n\
            {{#if database}}- **Database**: Test database setup required\n{{/if}}\
            {{#if external_services}}- **External Services**: {{external_services}}\n{{/if}}\n\n\
            ## Test Requirements\n\n\
            - Set up test environment and data\n\
            - Test end-to-end workflows\n\
            - Verify data consistency across components\n\
            - Test error propagation and recovery\n\
            - Clean up test data after execution\n\
            {{#if performance}}- Measure integration performance\n{{/if}}\
            {{#if security}}- Verify security boundaries\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Testing)
        .with_duration(75)
        .with_variables(vec![
            TemplateVariable::text("system_component", "System component being tested"),
            TemplateVariable::text("integration_points", "What integrations to test"),
            TemplateVariable::choice(
                "test_environment",
                "Test environment",
                vec![
                    "Docker Compose".to_string(),
                    "Testcontainers".to_string(),
                    "In-memory".to_string(),
                    "Staging".to_string(),
                ],
            ),
            TemplateVariable::boolean("database", "Includes database testing"),
            TemplateVariable::text("external_services", "External services involved").optional(),
            TemplateVariable::boolean("performance", "Include performance testing"),
            TemplateVariable::boolean("security", "Include security testing"),
        ])
        .with_target_files(vec![
            "tests/integration/{{system_component}}_integration.test.{{file_extension}}"
                .to_string(),
            "tests/fixtures/{{system_component}}_data.json".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:documentation_page (lines 633-701) ---[0m
    pub fn documentation_page() -> Template {
        Template::new(
            "documentation-page",
            "Documentation Page",
            "Create comprehensive documentation for a feature or API",
            TemplateCategory::Documentation,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "documentation".to_string(),
            "docs".to_string(),
            "guide".to_string(),
        ])
        .with_task_description(
            "Create documentation for {{doc_subject}} explaining {{doc_purpose}}",
        )
        .with_task_details(
            "## Documentation Specification\n\n\
            - **Subject**: {{doc_subject}}\n\
            - **Purpose**: {{doc_purpose}}\n\
            - **Audience**: {{target_audience}}\n\
            {{#if doc_type}}- **Type**: {{doc_type}}\n{{/if}}\n\n\
            ## Content Requirements\n\n\
            - Clear overview and introduction\n\
            - Step-by-step instructions or usage guide\n\
            - Code examples with explanations\n\
            - Common pitfalls and troubleshooting\n\
            - Links to related documentation\n\
            {{#if api_reference}}- Complete API reference with parameters\n{{/if}}\
            {{#if diagrams}}- Architecture or flow diagrams\n{{/if}}\
            {{#if examples}}- Real-world usage examples\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Documentation)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("doc_subject", "What is being documented"),
            TemplateVariable::text("doc_purpose", "Purpose of the documentation"),
            TemplateVariable::choice(
                "target_audience",
                "Target audience",
                vec![
                    "Developers".to_string(),
                    "Users".to_string(),
                    "System Administrators".to_string(),
                    "Product Managers".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "doc_type",
                "Documentation type",
                vec![
                    "API Reference".to_string(),
                    "User Guide".to_string(),
                    "Tutorial".to_string(),
                    "Architecture Guide".to_string(),
                    "Troubleshooting".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("api_reference", "Include API reference"),
            TemplateVariable::boolean("diagrams", "Include diagrams"),
            TemplateVariable::boolean("examples", "Include code examples"),
        ])
        .with_target_files(vec![
            "docs/{{doc_subject}}.md".to_string(),
            "examples/{{doc_subject}}_example.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:32-108 method react_component <-> ./crates/ccswarm/src/template/predefined.rs:111-166 method react_hook
  Similarity: 93.45%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_component (lines 32-108) ---[0m
    pub fn react_component() -> Template {
        Template::new(
            "react-component",
            "React Component",
            "Create a new React functional component with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec!["react".to_string(), "frontend".to_string(), "typescript".to_string(), "component".to_string()])
        .with_task_description("Create a {{component_type}} React component named {{component_name}}{{#if props}} with props: {{props}}{{/if}}{{#if styling}} using {{styling}}{{/if}}")
        .with_task_details(
            "## Component Specification\n\n\
            - **Name**: {{component_name}}\n\
            - **Type**: {{component_type}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if props}}- **Props**: {{props}}\n{{/if}}\
            {{#if state}}- **State**: {{state}}\n{{/if}}\
            {{#if styling}}- **Styling**: {{styling}}\n{{/if}}\
            {{#if accessibility}}- **Accessibility**: Include ARIA attributes and keyboard navigation\n{{/if}}\
            {{#if testing}}- **Testing**: Create unit tests with React Testing Library\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React best practices and hooks conventions\n\
            - Implement proper error boundaries if needed\n\
            - Add PropTypes or TypeScript interfaces for props\n\
            - Include JSDoc comments for component documentation\n\
            {{#if responsive}}- Make component responsive for mobile and desktop\n{{/if}}\
            {{#if performance}}- Optimize for performance with React.memo if appropriate\n{{/if}}"
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("component_name", "Name of the React component"),
            TemplateVariable::choice(
                "component_type",
                "Type of component",
                vec!["functional".to_string(), "class".to_string(), "custom hook".to_string()],
            ),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the component",
                "src/components/{{component_name}}.tsx"
            ),
            TemplateVariable::text("props", "Component props (optional)").optional(),
            TemplateVariable::text("state", "Component state description (optional)").optional(),
            TemplateVariable::choice(
                "styling",
                "Styling approach",
                vec![
                    "CSS Modules".to_string(),
                    "Styled Components".to_string(),
                    "Tailwind CSS".to_string(),
                    "SCSS".to_string(),
                    "Emotion".to_string(),
                ],
            ).optional(),
            TemplateVariable::boolean("accessibility", "Include accessibility features"),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("responsive", "Make responsive design"),
            TemplateVariable::boolean("performance", "Optimize for performance"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.tsx".to_string(),
            "{{file_path}}.module.css".to_string(),
        ])
        .with_preconditions(vec![
            "React and TypeScript are configured in the project".to_string(),
            "Component directory structure exists".to_string(),
        ])
        .with_post_actions(vec![
            "Export component from index file".to_string(),
            "Add to Storybook if available".to_string(),
            "Update documentation".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_hook (lines 111-166) ---[0m
    pub fn react_hook() -> Template {
        Template::new(
            "react-hook",
            "React Custom Hook",
            "Create a custom React hook with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "react".to_string(),
            "hook".to_string(),
            "typescript".to_string(),
        ])
        .with_task_description(
            "Create a custom React hook named {{hook_name}} for {{hook_purpose}}",
        )
        .with_task_details(
            "## Hook Specification\n\n\
            - **Name**: {{hook_name}}\n\
            - **Purpose**: {{hook_purpose}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if parameters}}- **Parameters**: {{parameters}}\n{{/if}}\
            {{#if return_type}}- **Returns**: {{return_type}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React hooks rules and conventions\n\
            - Include proper dependency arrays for useEffect\n\
            - Add comprehensive JSDoc comments\n\
            - Handle cleanup and memory leaks\n\
            {{#if testing}}- Create unit tests with React Hooks Testing Library\n{{/if}}\
            {{#if memoization}}- Use useMemo/useCallback for performance optimization\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(30)
        .with_variables(vec![
            TemplateVariable::text(
                "hook_name",
                "Name of the custom hook (e.g., useLocalStorage)",
            ),
            TemplateVariable::text("hook_purpose", "What the hook does"),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the hook",
                "src/hooks/{{hook_name}}.ts",
            ),
            TemplateVariable::text("parameters", "Hook parameters (optional)").optional(),
            TemplateVariable::text("return_type", "Hook return type (optional)").optional(),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("memoization", "Include performance optimizations"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.ts".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:381-439 method integration_test <-> ./crates/ccswarm/src/template/predefined.rs:562-630 method ci_pipeline
  Similarity: 96.86%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:integration_test (lines 381-439) ---[0m
    pub fn integration_test() -> Template {
        Template::new(
            "integration-test",
            "Integration Test",
            "Create integration tests for system components",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "integration".to_string(),
            "e2e".to_string(),
        ])
        .with_task_description(
            "Create integration tests for {{system_component}} testing {{integration_points}}",
        )
        .with_task_details(
            "## Integration Test Specification\n\n\
            - **Component**: {{system_component}}\n\
            - **Integration Points**: {{integration_points}}\n\
            - **Test Environment**: {{test_environment}}\n\
            {{#if database}}- **Database**: Test database setup required\n{{/if}}\
            {{#if external_services}}- **External Services**: {{external_services}}\n{{/if}}\n\n\
            ## Test Requirements\n\n\
            - Set up test environment and data\n\
            - Test end-to-end workflows\n\
            - Verify data consistency across components\n\
            - Test error propagation and recovery\n\
            - Clean up test data after execution\n\
            {{#if performance}}- Measure integration performance\n{{/if}}\
            {{#if security}}- Verify security boundaries\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Testing)
        .with_duration(75)
        .with_variables(vec![
            TemplateVariable::text("system_component", "System component being tested"),
            TemplateVariable::text("integration_points", "What integrations to test"),
            TemplateVariable::choice(
                "test_environment",
                "Test environment",
                vec![
                    "Docker Compose".to_string(),
                    "Testcontainers".to_string(),
                    "In-memory".to_string(),
                    "Staging".to_string(),
                ],
            ),
            TemplateVariable::boolean("database", "Includes database testing"),
            TemplateVariable::text("external_services", "External services involved").optional(),
            TemplateVariable::boolean("performance", "Include performance testing"),
            TemplateVariable::boolean("security", "Include security testing"),
        ])
        .with_target_files(vec![
            "tests/integration/{{system_component}}_integration.test.{{file_extension}}"
                .to_string(),
            "tests/fixtures/{{system_component}}_data.json".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:ci_pipeline (lines 562-630) ---[0m
    pub fn ci_pipeline() -> Template {
        Template::new(
            "ci-pipeline",
            "CI/CD Pipeline",
            "Create a continuous integration pipeline",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "ci".to_string(),
            "cd".to_string(),
            "pipeline".to_string(),
            "automation".to_string(),
        ])
        .with_task_description("Create {{ci_platform}} pipeline for {{project_type}} project")
        .with_task_details(
            "## CI/CD Pipeline Configuration\n\n\
            - **Platform**: {{ci_platform}}\n\
            - **Project Type**: {{project_type}}\n\
            {{#if triggers}}- **Triggers**: {{triggers}}\n{{/if}}\
            {{#if environments}}- **Environments**: {{environments}}\n{{/if}}\n\n\
            ## Pipeline Stages\n\n\
            - Code checkout and setup\n\
            - Dependency installation\n\
            - Linting and code quality checks\n\
            - Unit and integration tests\n\
            - Build and package\n\
            {{#if security_scan}}- Security vulnerability scanning\n{{/if}}\
            {{#if deployment}}- Automated deployment to {{environments}}\n{{/if}}\
            {{#if notifications}}- Notification on success/failure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "ci_platform",
                "CI/CD platform",
                vec![
                    "GitHub Actions".to_string(),
                    "GitLab CI".to_string(),
                    "Jenkins".to_string(),
                    "CircleCI".to_string(),
                    "Azure DevOps".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "project_type",
                "Project type",
                vec![
                    "Node.js".to_string(),
                    "Python".to_string(),
                    "Rust".to_string(),
                    "Go".to_string(),
                    "Java".to_string(),
                    "Docker".to_string(),
                ],
            ),
            TemplateVariable::text("triggers", "Pipeline triggers (e.g., push, PR)").optional(),
            TemplateVariable::text("environments", "Deployment environments").optional(),
            TemplateVariable::boolean("security_scan", "Include security scanning"),
            TemplateVariable::boolean("deployment", "Include deployment stages"),
            TemplateVariable::boolean("notifications", "Set up notifications"),
        ])
        .with_target_files(vec![
            ".github/workflows/ci.yml".to_string(),
            ".github/workflows/cd.yml".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:501-559 method docker_service <-> ./crates/ccswarm/src/template/predefined.rs:562-630 method ci_pipeline
  Similarity: 96.63%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:docker_service (lines 501-559) ---[0m
    pub fn docker_service() -> Template {
        Template::new(
            "docker-service",
            "Docker Service",
            "Create a Docker container configuration for a service",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "docker".to_string(),
            "container".to_string(),
            "devops".to_string(),
        ])
        .with_task_description("Create Docker configuration for {{service_name}} service")
        .with_task_details(
            "## Docker Service Configuration\n\n\
            - **Service**: {{service_name}}\n\
            - **Base Image**: {{base_image}}\n\
            {{#if ports}}- **Ports**: {{ports}}\n{{/if}}\
            {{#if volumes}}- **Volumes**: {{volumes}}\n{{/if}}\
            {{#if environment}}- **Environment**: {{environment}}\n{{/if}}\n\n\
            ## Container Requirements\n\n\
            - Multi-stage build for optimization\n\
            - Non-root user for security\n\
            - Health check configuration\n\
            - Proper signal handling\n\
            - Minimal attack surface\n\
            {{#if secrets}}- Secure secret management\n{{/if}}\
            {{#if monitoring}}- Monitoring and logging setup\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("service_name", "Name of the service"),
            TemplateVariable::choice(
                "base_image",
                "Base Docker image",
                vec![
                    "alpine".to_string(),
                    "ubuntu".to_string(),
                    "node:alpine".to_string(),
                    "python:alpine".to_string(),
                    "rust:alpine".to_string(),
                    "nginx:alpine".to_string(),
                ],
            ),
            TemplateVariable::text("ports", "Port mappings (e.g., 8080:8080)").optional(),
            TemplateVariable::text("volumes", "Volume mounts").optional(),
            TemplateVariable::text("environment", "Environment variables").optional(),
            TemplateVariable::boolean("secrets", "Include secret management"),
            TemplateVariable::boolean("monitoring", "Add monitoring setup"),
        ])
        .with_target_files(vec![
            "Dockerfile".to_string(),
            "docker-compose.yml".to_string(),
            ".dockerignore".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:ci_pipeline (lines 562-630) ---[0m
    pub fn ci_pipeline() -> Template {
        Template::new(
            "ci-pipeline",
            "CI/CD Pipeline",
            "Create a continuous integration pipeline",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "ci".to_string(),
            "cd".to_string(),
            "pipeline".to_string(),
            "automation".to_string(),
        ])
        .with_task_description("Create {{ci_platform}} pipeline for {{project_type}} project")
        .with_task_details(
            "## CI/CD Pipeline Configuration\n\n\
            - **Platform**: {{ci_platform}}\n\
            - **Project Type**: {{project_type}}\n\
            {{#if triggers}}- **Triggers**: {{triggers}}\n{{/if}}\
            {{#if environments}}- **Environments**: {{environments}}\n{{/if}}\n\n\
            ## Pipeline Stages\n\n\
            - Code checkout and setup\n\
            - Dependency installation\n\
            - Linting and code quality checks\n\
            - Unit and integration tests\n\
            - Build and package\n\
            {{#if security_scan}}- Security vulnerability scanning\n{{/if}}\
            {{#if deployment}}- Automated deployment to {{environments}}\n{{/if}}\
            {{#if notifications}}- Notification on success/failure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "ci_platform",
                "CI/CD platform",
                vec![
                    "GitHub Actions".to_string(),
                    "GitLab CI".to_string(),
                    "Jenkins".to_string(),
                    "CircleCI".to_string(),
                    "Azure DevOps".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "project_type",
                "Project type",
                vec![
                    "Node.js".to_string(),
                    "Python".to_string(),
                    "Rust".to_string(),
                    "Go".to_string(),
                    "Java".to_string(),
                    "Docker".to_string(),
                ],
            ),
            TemplateVariable::text("triggers", "Pipeline triggers (e.g., push, PR)").optional(),
            TemplateVariable::text("environments", "Deployment environments").optional(),
            TemplateVariable::boolean("security_scan", "Include security scanning"),
            TemplateVariable::boolean("deployment", "Include deployment stages"),
            TemplateVariable::boolean("notifications", "Set up notifications"),
        ])
        .with_target_files(vec![
            ".github/workflows/ci.yml".to_string(),
            ".github/workflows/cd.yml".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:501-559 method docker_service <-> ./crates/ccswarm/src/template/predefined.rs:633-701 method documentation_page
  Similarity: 96.55%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:docker_service (lines 501-559) ---[0m
    pub fn docker_service() -> Template {
        Template::new(
            "docker-service",
            "Docker Service",
            "Create a Docker container configuration for a service",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "docker".to_string(),
            "container".to_string(),
            "devops".to_string(),
        ])
        .with_task_description("Create Docker configuration for {{service_name}} service")
        .with_task_details(
            "## Docker Service Configuration\n\n\
            - **Service**: {{service_name}}\n\
            - **Base Image**: {{base_image}}\n\
            {{#if ports}}- **Ports**: {{ports}}\n{{/if}}\
            {{#if volumes}}- **Volumes**: {{volumes}}\n{{/if}}\
            {{#if environment}}- **Environment**: {{environment}}\n{{/if}}\n\n\
            ## Container Requirements\n\n\
            - Multi-stage build for optimization\n\
            - Non-root user for security\n\
            - Health check configuration\n\
            - Proper signal handling\n\
            - Minimal attack surface\n\
            {{#if secrets}}- Secure secret management\n{{/if}}\
            {{#if monitoring}}- Monitoring and logging setup\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("service_name", "Name of the service"),
            TemplateVariable::choice(
                "base_image",
                "Base Docker image",
                vec![
                    "alpine".to_string(),
                    "ubuntu".to_string(),
                    "node:alpine".to_string(),
                    "python:alpine".to_string(),
                    "rust:alpine".to_string(),
                    "nginx:alpine".to_string(),
                ],
            ),
            TemplateVariable::text("ports", "Port mappings (e.g., 8080:8080)").optional(),
            TemplateVariable::text("volumes", "Volume mounts").optional(),
            TemplateVariable::text("environment", "Environment variables").optional(),
            TemplateVariable::boolean("secrets", "Include secret management"),
            TemplateVariable::boolean("monitoring", "Add monitoring setup"),
        ])
        .with_target_files(vec![
            "Dockerfile".to_string(),
            "docker-compose.yml".to_string(),
            ".dockerignore".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:documentation_page (lines 633-701) ---[0m
    pub fn documentation_page() -> Template {
        Template::new(
            "documentation-page",
            "Documentation Page",
            "Create comprehensive documentation for a feature or API",
            TemplateCategory::Documentation,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "documentation".to_string(),
            "docs".to_string(),
            "guide".to_string(),
        ])
        .with_task_description(
            "Create documentation for {{doc_subject}} explaining {{doc_purpose}}",
        )
        .with_task_details(
            "## Documentation Specification\n\n\
            - **Subject**: {{doc_subject}}\n\
            - **Purpose**: {{doc_purpose}}\n\
            - **Audience**: {{target_audience}}\n\
            {{#if doc_type}}- **Type**: {{doc_type}}\n{{/if}}\n\n\
            ## Content Requirements\n\n\
            - Clear overview and introduction\n\
            - Step-by-step instructions or usage guide\n\
            - Code examples with explanations\n\
            - Common pitfalls and troubleshooting\n\
            - Links to related documentation\n\
            {{#if api_reference}}- Complete API reference with parameters\n{{/if}}\
            {{#if diagrams}}- Architecture or flow diagrams\n{{/if}}\
            {{#if examples}}- Real-world usage examples\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Documentation)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("doc_subject", "What is being documented"),
            TemplateVariable::text("doc_purpose", "Purpose of the documentation"),
            TemplateVariable::choice(
                "target_audience",
                "Target audience",
                vec![
                    "Developers".to_string(),
                    "Users".to_string(),
                    "System Administrators".to_string(),
                    "Product Managers".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "doc_type",
                "Documentation type",
                vec![
                    "API Reference".to_string(),
                    "User Guide".to_string(),
                    "Tutorial".to_string(),
                    "Architecture Guide".to_string(),
                    "Troubleshooting".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("api_reference", "Include API reference"),
            TemplateVariable::boolean("diagrams", "Include diagrams"),
            TemplateVariable::boolean("examples", "Include code examples"),
        ])
        .with_target_files(vec![
            "docs/{{doc_subject}}.md".to_string(),
            "examples/{{doc_subject}}_example.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:633-701 method documentation_page <-> ./crates/ccswarm/src/template/predefined.rs:816-872 method security_review
  Similarity: 97.74%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:documentation_page (lines 633-701) ---[0m
    pub fn documentation_page() -> Template {
        Template::new(
            "documentation-page",
            "Documentation Page",
            "Create comprehensive documentation for a feature or API",
            TemplateCategory::Documentation,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "documentation".to_string(),
            "docs".to_string(),
            "guide".to_string(),
        ])
        .with_task_description(
            "Create documentation for {{doc_subject}} explaining {{doc_purpose}}",
        )
        .with_task_details(
            "## Documentation Specification\n\n\
            - **Subject**: {{doc_subject}}\n\
            - **Purpose**: {{doc_purpose}}\n\
            - **Audience**: {{target_audience}}\n\
            {{#if doc_type}}- **Type**: {{doc_type}}\n{{/if}}\n\n\
            ## Content Requirements\n\n\
            - Clear overview and introduction\n\
            - Step-by-step instructions or usage guide\n\
            - Code examples with explanations\n\
            - Common pitfalls and troubleshooting\n\
            - Links to related documentation\n\
            {{#if api_reference}}- Complete API reference with parameters\n{{/if}}\
            {{#if diagrams}}- Architecture or flow diagrams\n{{/if}}\
            {{#if examples}}- Real-world usage examples\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Documentation)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("doc_subject", "What is being documented"),
            TemplateVariable::text("doc_purpose", "Purpose of the documentation"),
            TemplateVariable::choice(
                "target_audience",
                "Target audience",
                vec![
                    "Developers".to_string(),
                    "Users".to_string(),
                    "System Administrators".to_string(),
                    "Product Managers".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "doc_type",
                "Documentation type",
                vec![
                    "API Reference".to_string(),
                    "User Guide".to_string(),
                    "Tutorial".to_string(),
                    "Architecture Guide".to_string(),
                    "Troubleshooting".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("api_reference", "Include API reference"),
            TemplateVariable::boolean("diagrams", "Include diagrams"),
            TemplateVariable::boolean("examples", "Include code examples"),
        ])
        .with_target_files(vec![
            "docs/{{doc_subject}}.md".to_string(),
            "examples/{{doc_subject}}_example.{{file_extension}}".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:security_review (lines 816-872) ---[0m
    pub fn security_review() -> Template {
        Template::new(
            "security-review",
            "Security Review",
            "Conduct a comprehensive security review of code or system",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "security".to_string(),
            "review".to_string(),
            "audit".to_string(),
        ])
        .with_task_description(
            "Conduct security review of {{review_target}} focusing on {{security_aspects}}",
        )
        .with_task_details(
            "## Security Review Scope\n\n\
            - **Target**: {{review_target}}\n\
            - **Focus Areas**: {{security_aspects}}\n\
            - **Review Type**: {{review_type}}\n\n\
            ## Security Checklist\n\n\
            - Authentication and authorization mechanisms\n\
            - Input validation and sanitization\n\
            - SQL injection and XSS vulnerabilities\n\
            - Secure data storage and transmission\n\
            - Error handling and information disclosure\n\
            - Dependencies and supply chain security\n\
            {{#if crypto}}- Cryptographic implementations\n{{/if}}\
            {{#if api_security}}- API security and rate limiting\n{{/if}}\
            {{#if infrastructure}}- Infrastructure security configuration\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Review)
        .with_duration(120)
        .with_variables(vec![
            TemplateVariable::text("review_target", "What is being reviewed"),
            TemplateVariable::text("security_aspects", "Specific security concerns"),
            TemplateVariable::choice(
                "review_type",
                "Type of security review",
                vec![
                    "Code Review".to_string(),
                    "Architecture Review".to_string(),
                    "Penetration Test".to_string(),
                    "Compliance Audit".to_string(),
                ],
            ),
            TemplateVariable::boolean("crypto", "Review cryptographic code"),
            TemplateVariable::boolean("api_security", "Review API security"),
            TemplateVariable::boolean("infrastructure", "Review infrastructure"),
        ])
        .with_target_files(vec![
            "security-review-{{review_target}}.md".to_string(),
            "security-checklist-{{review_target}}.md".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:253-310 method database_migration <-> ./crates/ccswarm/src/template/predefined.rs:562-630 method ci_pipeline
  Similarity: 96.63%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:database_migration (lines 253-310) ---[0m
    pub fn database_migration() -> Template {
        Template::new(
            "database-migration",
            "Database Migration",
            "Create a database migration script",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "database".to_string(),
            "migration".to_string(),
            "schema".to_string(),
        ])
        .with_task_description("Create database migration to {{migration_purpose}}")
        .with_task_details(
            "## Migration Details\n\n\
            - **Purpose**: {{migration_purpose}}\n\
            - **Type**: {{migration_type}}\n\
            {{#if table_name}}- **Table**: {{table_name}}\n{{/if}}\
            {{#if columns}}- **Columns**: {{columns}}\n{{/if}}\
            {{#if indexes}}- **Indexes**: {{indexes}}\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Write both up and down migrations\n\
            - Include proper constraints and indexes\n\
            - Handle existing data appropriately\n\
            - Add comments for complex operations\n\
            - Test migration on sample data\n\
            {{#if backup}}- Create backup before running\n{{/if}}\
            {{#if rollback_plan}}- Document rollback procedure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(40)
        .with_variables(vec![
            TemplateVariable::text("migration_purpose", "What this migration accomplishes"),
            TemplateVariable::choice(
                "migration_type",
                "Type of migration",
                vec![
                    "Create Table".to_string(),
                    "Alter Table".to_string(),
                    "Drop Table".to_string(),
                    "Add Column".to_string(),
                    "Drop Column".to_string(),
                    "Create Index".to_string(),
                    "Data Migration".to_string(),
                ],
            ),
            TemplateVariable::text("table_name", "Table name (if applicable)").optional(),
            TemplateVariable::text("columns", "Column definitions (optional)").optional(),
            TemplateVariable::text("indexes", "Index definitions (optional)").optional(),
            TemplateVariable::boolean("backup", "Create backup before migration"),
            TemplateVariable::boolean("rollback_plan", "Document rollback procedure"),
        ])
        .with_target_files(vec![
            "migrations/{{current_datetime}}_{{migration_purpose}}.sql".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:ci_pipeline (lines 562-630) ---[0m
    pub fn ci_pipeline() -> Template {
        Template::new(
            "ci-pipeline",
            "CI/CD Pipeline",
            "Create a continuous integration pipeline",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "ci".to_string(),
            "cd".to_string(),
            "pipeline".to_string(),
            "automation".to_string(),
        ])
        .with_task_description("Create {{ci_platform}} pipeline for {{project_type}} project")
        .with_task_details(
            "## CI/CD Pipeline Configuration\n\n\
            - **Platform**: {{ci_platform}}\n\
            - **Project Type**: {{project_type}}\n\
            {{#if triggers}}- **Triggers**: {{triggers}}\n{{/if}}\
            {{#if environments}}- **Environments**: {{environments}}\n{{/if}}\n\n\
            ## Pipeline Stages\n\n\
            - Code checkout and setup\n\
            - Dependency installation\n\
            - Linting and code quality checks\n\
            - Unit and integration tests\n\
            - Build and package\n\
            {{#if security_scan}}- Security vulnerability scanning\n{{/if}}\
            {{#if deployment}}- Automated deployment to {{environments}}\n{{/if}}\
            {{#if notifications}}- Notification on success/failure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "ci_platform",
                "CI/CD platform",
                vec![
                    "GitHub Actions".to_string(),
                    "GitLab CI".to_string(),
                    "Jenkins".to_string(),
                    "CircleCI".to_string(),
                    "Azure DevOps".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "project_type",
                "Project type",
                vec![
                    "Node.js".to_string(),
                    "Python".to_string(),
                    "Rust".to_string(),
                    "Go".to_string(),
                    "Java".to_string(),
                    "Docker".to_string(),
                ],
            ),
            TemplateVariable::text("triggers", "Pipeline triggers (e.g., push, PR)").optional(),
            TemplateVariable::text("environments", "Deployment environments").optional(),
            TemplateVariable::boolean("security_scan", "Include security scanning"),
            TemplateVariable::boolean("deployment", "Include deployment stages"),
            TemplateVariable::boolean("notifications", "Set up notifications"),
        ])
        .with_target_files(vec![
            ".github/workflows/ci.yml".to_string(),
            ".github/workflows/cd.yml".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:442-498 method bug_fix <-> ./crates/ccswarm/src/template/predefined.rs:633-701 method documentation_page
  Similarity: 97.30%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:bug_fix (lines 442-498) ---[0m
    pub fn bug_fix() -> Template {
        Template::new(
            "bug-fix",
            "Bug Fix",
            "Fix a reported bug with proper testing and documentation",
            TemplateCategory::BugFix,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "bug".to_string(),
            "fix".to_string(),
            "maintenance".to_string(),
        ])
        .with_task_description("Fix bug: {{bug_description}} ({{bug_severity}})")
        .with_task_details(
            "## Bug Report\n\n\
            - **Description**: {{bug_description}}\n\
            - **Severity**: {{bug_severity}}\n\
            {{#if reproduction_steps}}- **Reproduction**: {{reproduction_steps}}\n{{/if}}\
            {{#if affected_components}}- **Affected Components**: {{affected_components}}\n{{/if}}\
            {{#if user_impact}}- **User Impact**: {{user_impact}}\n{{/if}}\n\n\
            ## Fix Requirements\n\n\
            - Reproduce the bug in a test environment\n\
            - Identify root cause through debugging\n\
            - Implement minimal fix that addresses root cause\n\
            - Add regression tests to prevent recurrence\n\
            - Verify fix doesn't introduce new issues\n\
            {{#if documentation}}- Update relevant documentation\n{{/if}}\
            {{#if release_notes}}- Add to release notes\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Bugfix)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("bug_description", "Clear description of the bug"),
            TemplateVariable::choice(
                "bug_severity",
                "Bug severity level",
                vec![
                    "Critical".to_string(),
                    "High".to_string(),
                    "Medium".to_string(),
                    "Low".to_string(),
                ],
            ),
            TemplateVariable::text("reproduction_steps", "Steps to reproduce the bug").optional(),
            TemplateVariable::text("affected_components", "Components affected by the bug")
                .optional(),
            TemplateVariable::text("user_impact", "How this affects users").optional(),
            TemplateVariable::boolean("documentation", "Update documentation"),
            TemplateVariable::boolean("release_notes", "Add to release notes"),
        ])
        .with_target_files(vec![
            "src/{{affected_components}}/fix.{{file_extension}}".to_string(),
            "tests/regression/{{bug_description}}.test.{{file_extension}}".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:documentation_page (lines 633-701) ---[0m
    pub fn documentation_page() -> Template {
        Template::new(
            "documentation-page",
            "Documentation Page",
            "Create comprehensive documentation for a feature or API",
            TemplateCategory::Documentation,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "documentation".to_string(),
            "docs".to_string(),
            "guide".to_string(),
        ])
        .with_task_description(
            "Create documentation for {{doc_subject}} explaining {{doc_purpose}}",
        )
        .with_task_details(
            "## Documentation Specification\n\n\
            - **Subject**: {{doc_subject}}\n\
            - **Purpose**: {{doc_purpose}}\n\
            - **Audience**: {{target_audience}}\n\
            {{#if doc_type}}- **Type**: {{doc_type}}\n{{/if}}\n\n\
            ## Content Requirements\n\n\
            - Clear overview and introduction\n\
            - Step-by-step instructions or usage guide\n\
            - Code examples with explanations\n\
            - Common pitfalls and troubleshooting\n\
            - Links to related documentation\n\
            {{#if api_reference}}- Complete API reference with parameters\n{{/if}}\
            {{#if diagrams}}- Architecture or flow diagrams\n{{/if}}\
            {{#if examples}}- Real-world usage examples\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Documentation)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("doc_subject", "What is being documented"),
            TemplateVariable::text("doc_purpose", "Purpose of the documentation"),
            TemplateVariable::choice(
                "target_audience",
                "Target audience",
                vec![
                    "Developers".to_string(),
                    "Users".to_string(),
                    "System Administrators".to_string(),
                    "Product Managers".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "doc_type",
                "Documentation type",
                vec![
                    "API Reference".to_string(),
                    "User Guide".to_string(),
                    "Tutorial".to_string(),
                    "Architecture Guide".to_string(),
                    "Troubleshooting".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("api_reference", "Include API reference"),
            TemplateVariable::boolean("diagrams", "Include diagrams"),
            TemplateVariable::boolean("examples", "Include code examples"),
        ])
        .with_target_files(vec![
            "docs/{{doc_subject}}.md".to_string(),
            "examples/{{doc_subject}}_example.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:442-498 method bug_fix <-> ./crates/ccswarm/src/template/predefined.rs:562-630 method ci_pipeline
  Similarity: 97.27%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:bug_fix (lines 442-498) ---[0m
    pub fn bug_fix() -> Template {
        Template::new(
            "bug-fix",
            "Bug Fix",
            "Fix a reported bug with proper testing and documentation",
            TemplateCategory::BugFix,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "bug".to_string(),
            "fix".to_string(),
            "maintenance".to_string(),
        ])
        .with_task_description("Fix bug: {{bug_description}} ({{bug_severity}})")
        .with_task_details(
            "## Bug Report\n\n\
            - **Description**: {{bug_description}}\n\
            - **Severity**: {{bug_severity}}\n\
            {{#if reproduction_steps}}- **Reproduction**: {{reproduction_steps}}\n{{/if}}\
            {{#if affected_components}}- **Affected Components**: {{affected_components}}\n{{/if}}\
            {{#if user_impact}}- **User Impact**: {{user_impact}}\n{{/if}}\n\n\
            ## Fix Requirements\n\n\
            - Reproduce the bug in a test environment\n\
            - Identify root cause through debugging\n\
            - Implement minimal fix that addresses root cause\n\
            - Add regression tests to prevent recurrence\n\
            - Verify fix doesn't introduce new issues\n\
            {{#if documentation}}- Update relevant documentation\n{{/if}}\
            {{#if release_notes}}- Add to release notes\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Bugfix)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("bug_description", "Clear description of the bug"),
            TemplateVariable::choice(
                "bug_severity",
                "Bug severity level",
                vec![
                    "Critical".to_string(),
                    "High".to_string(),
                    "Medium".to_string(),
                    "Low".to_string(),
                ],
            ),
            TemplateVariable::text("reproduction_steps", "Steps to reproduce the bug").optional(),
            TemplateVariable::text("affected_components", "Components affected by the bug")
                .optional(),
            TemplateVariable::text("user_impact", "How this affects users").optional(),
            TemplateVariable::boolean("documentation", "Update documentation"),
            TemplateVariable::boolean("release_notes", "Add to release notes"),
        ])
        .with_target_files(vec![
            "src/{{affected_components}}/fix.{{file_extension}}".to_string(),
            "tests/regression/{{bug_description}}.test.{{file_extension}}".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:ci_pipeline (lines 562-630) ---[0m
    pub fn ci_pipeline() -> Template {
        Template::new(
            "ci-pipeline",
            "CI/CD Pipeline",
            "Create a continuous integration pipeline",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "ci".to_string(),
            "cd".to_string(),
            "pipeline".to_string(),
            "automation".to_string(),
        ])
        .with_task_description("Create {{ci_platform}} pipeline for {{project_type}} project")
        .with_task_details(
            "## CI/CD Pipeline Configuration\n\n\
            - **Platform**: {{ci_platform}}\n\
            - **Project Type**: {{project_type}}\n\
            {{#if triggers}}- **Triggers**: {{triggers}}\n{{/if}}\
            {{#if environments}}- **Environments**: {{environments}}\n{{/if}}\n\n\
            ## Pipeline Stages\n\n\
            - Code checkout and setup\n\
            - Dependency installation\n\
            - Linting and code quality checks\n\
            - Unit and integration tests\n\
            - Build and package\n\
            {{#if security_scan}}- Security vulnerability scanning\n{{/if}}\
            {{#if deployment}}- Automated deployment to {{environments}}\n{{/if}}\
            {{#if notifications}}- Notification on success/failure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "ci_platform",
                "CI/CD platform",
                vec![
                    "GitHub Actions".to_string(),
                    "GitLab CI".to_string(),
                    "Jenkins".to_string(),
                    "CircleCI".to_string(),
                    "Azure DevOps".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "project_type",
                "Project type",
                vec![
                    "Node.js".to_string(),
                    "Python".to_string(),
                    "Rust".to_string(),
                    "Go".to_string(),
                    "Java".to_string(),
                    "Docker".to_string(),
                ],
            ),
            TemplateVariable::text("triggers", "Pipeline triggers (e.g., push, PR)").optional(),
            TemplateVariable::text("environments", "Deployment environments").optional(),
            TemplateVariable::boolean("security_scan", "Include security scanning"),
            TemplateVariable::boolean("deployment", "Include deployment stages"),
            TemplateVariable::boolean("notifications", "Set up notifications"),
        ])
        .with_target_files(vec![
            ".github/workflows/ci.yml".to_string(),
            ".github/workflows/cd.yml".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:253-310 method database_migration <-> ./crates/ccswarm/src/template/predefined.rs:633-701 method documentation_page
  Similarity: 96.48%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:database_migration (lines 253-310) ---[0m
    pub fn database_migration() -> Template {
        Template::new(
            "database-migration",
            "Database Migration",
            "Create a database migration script",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "database".to_string(),
            "migration".to_string(),
            "schema".to_string(),
        ])
        .with_task_description("Create database migration to {{migration_purpose}}")
        .with_task_details(
            "## Migration Details\n\n\
            - **Purpose**: {{migration_purpose}}\n\
            - **Type**: {{migration_type}}\n\
            {{#if table_name}}- **Table**: {{table_name}}\n{{/if}}\
            {{#if columns}}- **Columns**: {{columns}}\n{{/if}}\
            {{#if indexes}}- **Indexes**: {{indexes}}\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Write both up and down migrations\n\
            - Include proper constraints and indexes\n\
            - Handle existing data appropriately\n\
            - Add comments for complex operations\n\
            - Test migration on sample data\n\
            {{#if backup}}- Create backup before running\n{{/if}}\
            {{#if rollback_plan}}- Document rollback procedure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(40)
        .with_variables(vec![
            TemplateVariable::text("migration_purpose", "What this migration accomplishes"),
            TemplateVariable::choice(
                "migration_type",
                "Type of migration",
                vec![
                    "Create Table".to_string(),
                    "Alter Table".to_string(),
                    "Drop Table".to_string(),
                    "Add Column".to_string(),
                    "Drop Column".to_string(),
                    "Create Index".to_string(),
                    "Data Migration".to_string(),
                ],
            ),
            TemplateVariable::text("table_name", "Table name (if applicable)").optional(),
            TemplateVariable::text("columns", "Column definitions (optional)").optional(),
            TemplateVariable::text("indexes", "Index definitions (optional)").optional(),
            TemplateVariable::boolean("backup", "Create backup before migration"),
            TemplateVariable::boolean("rollback_plan", "Document rollback procedure"),
        ])
        .with_target_files(vec![
            "migrations/{{current_datetime}}_{{migration_purpose}}.sql".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:documentation_page (lines 633-701) ---[0m
    pub fn documentation_page() -> Template {
        Template::new(
            "documentation-page",
            "Documentation Page",
            "Create comprehensive documentation for a feature or API",
            TemplateCategory::Documentation,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "documentation".to_string(),
            "docs".to_string(),
            "guide".to_string(),
        ])
        .with_task_description(
            "Create documentation for {{doc_subject}} explaining {{doc_purpose}}",
        )
        .with_task_details(
            "## Documentation Specification\n\n\
            - **Subject**: {{doc_subject}}\n\
            - **Purpose**: {{doc_purpose}}\n\
            - **Audience**: {{target_audience}}\n\
            {{#if doc_type}}- **Type**: {{doc_type}}\n{{/if}}\n\n\
            ## Content Requirements\n\n\
            - Clear overview and introduction\n\
            - Step-by-step instructions or usage guide\n\
            - Code examples with explanations\n\
            - Common pitfalls and troubleshooting\n\
            - Links to related documentation\n\
            {{#if api_reference}}- Complete API reference with parameters\n{{/if}}\
            {{#if diagrams}}- Architecture or flow diagrams\n{{/if}}\
            {{#if examples}}- Real-world usage examples\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Documentation)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("doc_subject", "What is being documented"),
            TemplateVariable::text("doc_purpose", "Purpose of the documentation"),
            TemplateVariable::choice(
                "target_audience",
                "Target audience",
                vec![
                    "Developers".to_string(),
                    "Users".to_string(),
                    "System Administrators".to_string(),
                    "Product Managers".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "doc_type",
                "Documentation type",
                vec![
                    "API Reference".to_string(),
                    "User Guide".to_string(),
                    "Tutorial".to_string(),
                    "Architecture Guide".to_string(),
                    "Troubleshooting".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("api_reference", "Include API reference"),
            TemplateVariable::boolean("diagrams", "Include diagrams"),
            TemplateVariable::boolean("examples", "Include code examples"),
        ])
        .with_target_files(vec![
            "docs/{{doc_subject}}.md".to_string(),
            "examples/{{doc_subject}}_example.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:633-701 method documentation_page <-> ./crates/ccswarm/src/template/predefined.rs:704-761 method rust_module
  Similarity: 96.46%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:documentation_page (lines 633-701) ---[0m
    pub fn documentation_page() -> Template {
        Template::new(
            "documentation-page",
            "Documentation Page",
            "Create comprehensive documentation for a feature or API",
            TemplateCategory::Documentation,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "documentation".to_string(),
            "docs".to_string(),
            "guide".to_string(),
        ])
        .with_task_description(
            "Create documentation for {{doc_subject}} explaining {{doc_purpose}}",
        )
        .with_task_details(
            "## Documentation Specification\n\n\
            - **Subject**: {{doc_subject}}\n\
            - **Purpose**: {{doc_purpose}}\n\
            - **Audience**: {{target_audience}}\n\
            {{#if doc_type}}- **Type**: {{doc_type}}\n{{/if}}\n\n\
            ## Content Requirements\n\n\
            - Clear overview and introduction\n\
            - Step-by-step instructions or usage guide\n\
            - Code examples with explanations\n\
            - Common pitfalls and troubleshooting\n\
            - Links to related documentation\n\
            {{#if api_reference}}- Complete API reference with parameters\n{{/if}}\
            {{#if diagrams}}- Architecture or flow diagrams\n{{/if}}\
            {{#if examples}}- Real-world usage examples\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Documentation)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("doc_subject", "What is being documented"),
            TemplateVariable::text("doc_purpose", "Purpose of the documentation"),
            TemplateVariable::choice(
                "target_audience",
                "Target audience",
                vec![
                    "Developers".to_string(),
                    "Users".to_string(),
                    "System Administrators".to_string(),
                    "Product Managers".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "doc_type",
                "Documentation type",
                vec![
                    "API Reference".to_string(),
                    "User Guide".to_string(),
                    "Tutorial".to_string(),
                    "Architecture Guide".to_string(),
                    "Troubleshooting".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("api_reference", "Include API reference"),
            TemplateVariable::boolean("diagrams", "Include diagrams"),
            TemplateVariable::boolean("examples", "Include code examples"),
        ])
        .with_target_files(vec![
            "docs/{{doc_subject}}.md".to_string(),
            "examples/{{doc_subject}}_example.{{file_extension}}".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_module (lines 704-761) ---[0m
    pub fn rust_module() -> Template {
        Template::new(
            "rust-module",
            "Rust Module",
            "Create a new Rust module with proper structure and documentation",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "module".to_string(),
            "backend".to_string(),
        ])
        .with_task_description("Create Rust module {{module_name}} for {{module_purpose}}")
        .with_task_details(
            "## Rust Module Specification\n\n\
            - **Module**: {{module_name}}\n\
            - **Purpose**: {{module_purpose}}\n\
            - **Visibility**: {{visibility}}\n\
            {{#if traits}}- **Traits**: {{traits}}\n{{/if}}\
            {{#if structs}}- **Structs**: {{structs}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Follow Rust naming conventions\n\
            - Add comprehensive rustdoc comments\n\
            - Implement proper error handling with Result<T, E>\n\
            - Use appropriate visibility modifiers\n\
            - Include unit tests in #[cfg(test)] module\n\
            {{#if async_support}}- Implement async/await patterns\n{{/if}}\
            {{#if serde_support}}- Add Serde serialization support\n{{/if}}\
            {{#if benchmark}}- Include criterion benchmarks\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("module_name", "Name of the Rust module"),
            TemplateVariable::text("module_purpose", "What this module does"),
            TemplateVariable::choice(
                "visibility",
                "Module visibility",
                vec![
                    "public".to_string(),
                    "private".to_string(),
                    "pub(crate)".to_string(),
                ],
            ),
            TemplateVariable::text("traits", "Traits to implement").optional(),
            TemplateVariable::text("structs", "Main structs in the module").optional(),
            TemplateVariable::boolean("async_support", "Include async support"),
            TemplateVariable::boolean("serde_support", "Add Serde support"),
            TemplateVariable::boolean("benchmark", "Include benchmarks"),
        ])
        .with_target_files(vec![
            "src/{{module_name}}/mod.rs".to_string(),
            "src/{{module_name}}/types.rs".to_string(),
            "benches/{{module_name}}_bench.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:633-701 method documentation_page <-> ./crates/ccswarm/src/template/predefined.rs:931-987 method code_refactor
  Similarity: 97.21%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:documentation_page (lines 633-701) ---[0m
    pub fn documentation_page() -> Template {
        Template::new(
            "documentation-page",
            "Documentation Page",
            "Create comprehensive documentation for a feature or API",
            TemplateCategory::Documentation,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "documentation".to_string(),
            "docs".to_string(),
            "guide".to_string(),
        ])
        .with_task_description(
            "Create documentation for {{doc_subject}} explaining {{doc_purpose}}",
        )
        .with_task_details(
            "## Documentation Specification\n\n\
            - **Subject**: {{doc_subject}}\n\
            - **Purpose**: {{doc_purpose}}\n\
            - **Audience**: {{target_audience}}\n\
            {{#if doc_type}}- **Type**: {{doc_type}}\n{{/if}}\n\n\
            ## Content Requirements\n\n\
            - Clear overview and introduction\n\
            - Step-by-step instructions or usage guide\n\
            - Code examples with explanations\n\
            - Common pitfalls and troubleshooting\n\
            - Links to related documentation\n\
            {{#if api_reference}}- Complete API reference with parameters\n{{/if}}\
            {{#if diagrams}}- Architecture or flow diagrams\n{{/if}}\
            {{#if examples}}- Real-world usage examples\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Documentation)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("doc_subject", "What is being documented"),
            TemplateVariable::text("doc_purpose", "Purpose of the documentation"),
            TemplateVariable::choice(
                "target_audience",
                "Target audience",
                vec![
                    "Developers".to_string(),
                    "Users".to_string(),
                    "System Administrators".to_string(),
                    "Product Managers".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "doc_type",
                "Documentation type",
                vec![
                    "API Reference".to_string(),
                    "User Guide".to_string(),
                    "Tutorial".to_string(),
                    "Architecture Guide".to_string(),
                    "Troubleshooting".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("api_reference", "Include API reference"),
            TemplateVariable::boolean("diagrams", "Include diagrams"),
            TemplateVariable::boolean("examples", "Include code examples"),
        ])
        .with_target_files(vec![
            "docs/{{doc_subject}}.md".to_string(),
            "examples/{{doc_subject}}_example.{{file_extension}}".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:code_refactor (lines 931-987) ---[0m
    pub fn code_refactor() -> Template {
        Template::new(
            "code-refactor",
            "Code Refactoring",
            "Refactor code to improve maintainability and design",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "refactor".to_string(),
            "maintainability".to_string(),
            "design".to_string(),
        ])
        .with_task_description("Refactor {{refactor_target}} to {{refactor_goal}}")
        .with_task_details(
            "## Refactoring Plan\n\n\
            - **Target**: {{refactor_target}}\n\
            - **Goal**: {{refactor_goal}}\n\
            - **Refactor Type**: {{refactor_type}}\n\
            {{#if design_patterns}}- **Design Patterns**: {{design_patterns}}\n{{/if}}\n\n\
            ## Refactoring Steps\n\n\
            - Analyze current code structure and identify issues\n\
            - Create comprehensive test coverage before refactoring\n\
            - Apply refactoring techniques incrementally\n\
            - Verify tests pass after each step\n\
            - Update documentation to reflect changes\n\
            {{#if breaking_changes}}- Document any breaking changes\n{{/if}}\
            {{#if performance_impact}}- Measure performance impact\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(80)
        .with_variables(vec![
            TemplateVariable::text("refactor_target", "Code to be refactored"),
            TemplateVariable::text("refactor_goal", "What the refactoring should achieve"),
            TemplateVariable::choice(
                "refactor_type",
                "Type of refactoring",
                vec![
                    "Extract Method".to_string(),
                    "Extract Class".to_string(),
                    "Move Method".to_string(),
                    "Rename".to_string(),
                    "Simplify Conditional".to_string(),
                    "Remove Duplication".to_string(),
                    "Improve Abstraction".to_string(),
                ],
            ),
            TemplateVariable::text("design_patterns", "Design patterns to apply").optional(),
            TemplateVariable::boolean("breaking_changes", "May introduce breaking changes"),
            TemplateVariable::boolean("performance_impact", "Measure performance impact"),
        ])
        .with_target_files(vec![
            "refactor-plan-{{refactor_target}}.md".to_string(),
            "tests/refactor/{{refactor_target}}_test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:32-108 method react_component <-> ./crates/ccswarm/src/template/predefined.rs:875-928 method performance_optimization
  Similarity: 93.45%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_component (lines 32-108) ---[0m
    pub fn react_component() -> Template {
        Template::new(
            "react-component",
            "React Component",
            "Create a new React functional component with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec!["react".to_string(), "frontend".to_string(), "typescript".to_string(), "component".to_string()])
        .with_task_description("Create a {{component_type}} React component named {{component_name}}{{#if props}} with props: {{props}}{{/if}}{{#if styling}} using {{styling}}{{/if}}")
        .with_task_details(
            "## Component Specification\n\n\
            - **Name**: {{component_name}}\n\
            - **Type**: {{component_type}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if props}}- **Props**: {{props}}\n{{/if}}\
            {{#if state}}- **State**: {{state}}\n{{/if}}\
            {{#if styling}}- **Styling**: {{styling}}\n{{/if}}\
            {{#if accessibility}}- **Accessibility**: Include ARIA attributes and keyboard navigation\n{{/if}}\
            {{#if testing}}- **Testing**: Create unit tests with React Testing Library\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React best practices and hooks conventions\n\
            - Implement proper error boundaries if needed\n\
            - Add PropTypes or TypeScript interfaces for props\n\
            - Include JSDoc comments for component documentation\n\
            {{#if responsive}}- Make component responsive for mobile and desktop\n{{/if}}\
            {{#if performance}}- Optimize for performance with React.memo if appropriate\n{{/if}}"
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("component_name", "Name of the React component"),
            TemplateVariable::choice(
                "component_type",
                "Type of component",
                vec!["functional".to_string(), "class".to_string(), "custom hook".to_string()],
            ),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the component",
                "src/components/{{component_name}}.tsx"
            ),
            TemplateVariable::text("props", "Component props (optional)").optional(),
            TemplateVariable::text("state", "Component state description (optional)").optional(),
            TemplateVariable::choice(
                "styling",
                "Styling approach",
                vec![
                    "CSS Modules".to_string(),
                    "Styled Components".to_string(),
                    "Tailwind CSS".to_string(),
                    "SCSS".to_string(),
                    "Emotion".to_string(),
                ],
            ).optional(),
            TemplateVariable::boolean("accessibility", "Include accessibility features"),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("responsive", "Make responsive design"),
            TemplateVariable::boolean("performance", "Optimize for performance"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.tsx".to_string(),
            "{{file_path}}.module.css".to_string(),
        ])
        .with_preconditions(vec![
            "React and TypeScript are configured in the project".to_string(),
            "Component directory structure exists".to_string(),
        ])
        .with_post_actions(vec![
            "Export component from index file".to_string(),
            "Add to Storybook if available".to_string(),
            "Update documentation".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:performance_optimization (lines 875-928) ---[0m
    pub fn performance_optimization() -> Template {
        Template::new(
            "performance-optimization",
            "Performance Optimization",
            "Optimize performance of a system component",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "performance".to_string(),
            "optimization".to_string(),
            "profiling".to_string(),
        ])
        .with_task_description(
            "Optimize performance of {{optimization_target}} to improve {{performance_metric}}",
        )
        .with_task_details(
            "## Performance Optimization Plan\n\n\
            - **Target**: {{optimization_target}}\n\
            - **Metric**: {{performance_metric}}\n\
            - **Current Performance**: {{current_baseline}}\n\
            - **Target Performance**: {{target_goal}}\n\n\
            ## Optimization Approach\n\n\
            - Profile current performance with appropriate tools\n\
            - Identify bottlenecks and hot paths\n\
            - Implement targeted optimizations\n\
            - Measure impact of each optimization\n\
            - Verify no functional regressions\n\
            {{#if caching}}- Implement caching strategies\n{{/if}}\
            {{#if database}}- Optimize database queries and indexes\n{{/if}}\
            {{#if memory}}- Reduce memory allocation and fragmentation\n{{/if}}\
            {{#if concurrency}}- Improve concurrency and parallelization\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("optimization_target", "Component to optimize"),
            TemplateVariable::text(
                "performance_metric",
                "Metric to improve (latency, throughput, etc.)",
            ),
            TemplateVariable::text("current_baseline", "Current performance baseline").optional(),
            TemplateVariable::text("target_goal", "Performance improvement goal").optional(),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("database", "Optimize database operations"),
            TemplateVariable::boolean("memory", "Optimize memory usage"),
            TemplateVariable::boolean("concurrency", "Improve concurrency"),
        ])
        .with_target_files(vec![
            "performance-analysis-{{optimization_target}}.md".to_string(),
            "benchmarks/{{optimization_target}}_bench.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:562-630 method ci_pipeline <-> ./crates/ccswarm/src/template/predefined.rs:704-761 method rust_module
  Similarity: 96.37%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:ci_pipeline (lines 562-630) ---[0m
    pub fn ci_pipeline() -> Template {
        Template::new(
            "ci-pipeline",
            "CI/CD Pipeline",
            "Create a continuous integration pipeline",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "ci".to_string(),
            "cd".to_string(),
            "pipeline".to_string(),
            "automation".to_string(),
        ])
        .with_task_description("Create {{ci_platform}} pipeline for {{project_type}} project")
        .with_task_details(
            "## CI/CD Pipeline Configuration\n\n\
            - **Platform**: {{ci_platform}}\n\
            - **Project Type**: {{project_type}}\n\
            {{#if triggers}}- **Triggers**: {{triggers}}\n{{/if}}\
            {{#if environments}}- **Environments**: {{environments}}\n{{/if}}\n\n\
            ## Pipeline Stages\n\n\
            - Code checkout and setup\n\
            - Dependency installation\n\
            - Linting and code quality checks\n\
            - Unit and integration tests\n\
            - Build and package\n\
            {{#if security_scan}}- Security vulnerability scanning\n{{/if}}\
            {{#if deployment}}- Automated deployment to {{environments}}\n{{/if}}\
            {{#if notifications}}- Notification on success/failure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "ci_platform",
                "CI/CD platform",
                vec![
                    "GitHub Actions".to_string(),
                    "GitLab CI".to_string(),
                    "Jenkins".to_string(),
                    "CircleCI".to_string(),
                    "Azure DevOps".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "project_type",
                "Project type",
                vec![
                    "Node.js".to_string(),
                    "Python".to_string(),
                    "Rust".to_string(),
                    "Go".to_string(),
                    "Java".to_string(),
                    "Docker".to_string(),
                ],
            ),
            TemplateVariable::text("triggers", "Pipeline triggers (e.g., push, PR)").optional(),
            TemplateVariable::text("environments", "Deployment environments").optional(),
            TemplateVariable::boolean("security_scan", "Include security scanning"),
            TemplateVariable::boolean("deployment", "Include deployment stages"),
            TemplateVariable::boolean("notifications", "Set up notifications"),
        ])
        .with_target_files(vec![
            ".github/workflows/ci.yml".to_string(),
            ".github/workflows/cd.yml".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_module (lines 704-761) ---[0m
    pub fn rust_module() -> Template {
        Template::new(
            "rust-module",
            "Rust Module",
            "Create a new Rust module with proper structure and documentation",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "module".to_string(),
            "backend".to_string(),
        ])
        .with_task_description("Create Rust module {{module_name}} for {{module_purpose}}")
        .with_task_details(
            "## Rust Module Specification\n\n\
            - **Module**: {{module_name}}\n\
            - **Purpose**: {{module_purpose}}\n\
            - **Visibility**: {{visibility}}\n\
            {{#if traits}}- **Traits**: {{traits}}\n{{/if}}\
            {{#if structs}}- **Structs**: {{structs}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Follow Rust naming conventions\n\
            - Add comprehensive rustdoc comments\n\
            - Implement proper error handling with Result<T, E>\n\
            - Use appropriate visibility modifiers\n\
            - Include unit tests in #[cfg(test)] module\n\
            {{#if async_support}}- Implement async/await patterns\n{{/if}}\
            {{#if serde_support}}- Add Serde serialization support\n{{/if}}\
            {{#if benchmark}}- Include criterion benchmarks\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("module_name", "Name of the Rust module"),
            TemplateVariable::text("module_purpose", "What this module does"),
            TemplateVariable::choice(
                "visibility",
                "Module visibility",
                vec![
                    "public".to_string(),
                    "private".to_string(),
                    "pub(crate)".to_string(),
                ],
            ),
            TemplateVariable::text("traits", "Traits to implement").optional(),
            TemplateVariable::text("structs", "Main structs in the module").optional(),
            TemplateVariable::boolean("async_support", "Include async support"),
            TemplateVariable::boolean("serde_support", "Add Serde support"),
            TemplateVariable::boolean("benchmark", "Include benchmarks"),
        ])
        .with_target_files(vec![
            "src/{{module_name}}/mod.rs".to_string(),
            "src/{{module_name}}/types.rs".to_string(),
            "benches/{{module_name}}_bench.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:111-166 method react_hook <-> ./crates/ccswarm/src/template/predefined.rs:633-701 method documentation_page
  Similarity: 97.74%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_hook (lines 111-166) ---[0m
    pub fn react_hook() -> Template {
        Template::new(
            "react-hook",
            "React Custom Hook",
            "Create a custom React hook with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "react".to_string(),
            "hook".to_string(),
            "typescript".to_string(),
        ])
        .with_task_description(
            "Create a custom React hook named {{hook_name}} for {{hook_purpose}}",
        )
        .with_task_details(
            "## Hook Specification\n\n\
            - **Name**: {{hook_name}}\n\
            - **Purpose**: {{hook_purpose}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if parameters}}- **Parameters**: {{parameters}}\n{{/if}}\
            {{#if return_type}}- **Returns**: {{return_type}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React hooks rules and conventions\n\
            - Include proper dependency arrays for useEffect\n\
            - Add comprehensive JSDoc comments\n\
            - Handle cleanup and memory leaks\n\
            {{#if testing}}- Create unit tests with React Hooks Testing Library\n{{/if}}\
            {{#if memoization}}- Use useMemo/useCallback for performance optimization\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(30)
        .with_variables(vec![
            TemplateVariable::text(
                "hook_name",
                "Name of the custom hook (e.g., useLocalStorage)",
            ),
            TemplateVariable::text("hook_purpose", "What the hook does"),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the hook",
                "src/hooks/{{hook_name}}.ts",
            ),
            TemplateVariable::text("parameters", "Hook parameters (optional)").optional(),
            TemplateVariable::text("return_type", "Hook return type (optional)").optional(),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("memoization", "Include performance optimizations"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.ts".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:documentation_page (lines 633-701) ---[0m
    pub fn documentation_page() -> Template {
        Template::new(
            "documentation-page",
            "Documentation Page",
            "Create comprehensive documentation for a feature or API",
            TemplateCategory::Documentation,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "documentation".to_string(),
            "docs".to_string(),
            "guide".to_string(),
        ])
        .with_task_description(
            "Create documentation for {{doc_subject}} explaining {{doc_purpose}}",
        )
        .with_task_details(
            "## Documentation Specification\n\n\
            - **Subject**: {{doc_subject}}\n\
            - **Purpose**: {{doc_purpose}}\n\
            - **Audience**: {{target_audience}}\n\
            {{#if doc_type}}- **Type**: {{doc_type}}\n{{/if}}\n\n\
            ## Content Requirements\n\n\
            - Clear overview and introduction\n\
            - Step-by-step instructions or usage guide\n\
            - Code examples with explanations\n\
            - Common pitfalls and troubleshooting\n\
            - Links to related documentation\n\
            {{#if api_reference}}- Complete API reference with parameters\n{{/if}}\
            {{#if diagrams}}- Architecture or flow diagrams\n{{/if}}\
            {{#if examples}}- Real-world usage examples\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Documentation)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("doc_subject", "What is being documented"),
            TemplateVariable::text("doc_purpose", "Purpose of the documentation"),
            TemplateVariable::choice(
                "target_audience",
                "Target audience",
                vec![
                    "Developers".to_string(),
                    "Users".to_string(),
                    "System Administrators".to_string(),
                    "Product Managers".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "doc_type",
                "Documentation type",
                vec![
                    "API Reference".to_string(),
                    "User Guide".to_string(),
                    "Tutorial".to_string(),
                    "Architecture Guide".to_string(),
                    "Troubleshooting".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("api_reference", "Include API reference"),
            TemplateVariable::boolean("diagrams", "Include diagrams"),
            TemplateVariable::boolean("examples", "Include code examples"),
        ])
        .with_target_files(vec![
            "docs/{{doc_subject}}.md".to_string(),
            "examples/{{doc_subject}}_example.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:562-630 method ci_pipeline <-> ./crates/ccswarm/src/template/predefined.rs:816-872 method security_review
  Similarity: 96.92%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:ci_pipeline (lines 562-630) ---[0m
    pub fn ci_pipeline() -> Template {
        Template::new(
            "ci-pipeline",
            "CI/CD Pipeline",
            "Create a continuous integration pipeline",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "ci".to_string(),
            "cd".to_string(),
            "pipeline".to_string(),
            "automation".to_string(),
        ])
        .with_task_description("Create {{ci_platform}} pipeline for {{project_type}} project")
        .with_task_details(
            "## CI/CD Pipeline Configuration\n\n\
            - **Platform**: {{ci_platform}}\n\
            - **Project Type**: {{project_type}}\n\
            {{#if triggers}}- **Triggers**: {{triggers}}\n{{/if}}\
            {{#if environments}}- **Environments**: {{environments}}\n{{/if}}\n\n\
            ## Pipeline Stages\n\n\
            - Code checkout and setup\n\
            - Dependency installation\n\
            - Linting and code quality checks\n\
            - Unit and integration tests\n\
            - Build and package\n\
            {{#if security_scan}}- Security vulnerability scanning\n{{/if}}\
            {{#if deployment}}- Automated deployment to {{environments}}\n{{/if}}\
            {{#if notifications}}- Notification on success/failure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "ci_platform",
                "CI/CD platform",
                vec![
                    "GitHub Actions".to_string(),
                    "GitLab CI".to_string(),
                    "Jenkins".to_string(),
                    "CircleCI".to_string(),
                    "Azure DevOps".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "project_type",
                "Project type",
                vec![
                    "Node.js".to_string(),
                    "Python".to_string(),
                    "Rust".to_string(),
                    "Go".to_string(),
                    "Java".to_string(),
                    "Docker".to_string(),
                ],
            ),
            TemplateVariable::text("triggers", "Pipeline triggers (e.g., push, PR)").optional(),
            TemplateVariable::text("environments", "Deployment environments").optional(),
            TemplateVariable::boolean("security_scan", "Include security scanning"),
            TemplateVariable::boolean("deployment", "Include deployment stages"),
            TemplateVariable::boolean("notifications", "Set up notifications"),
        ])
        .with_target_files(vec![
            ".github/workflows/ci.yml".to_string(),
            ".github/workflows/cd.yml".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:security_review (lines 816-872) ---[0m
    pub fn security_review() -> Template {
        Template::new(
            "security-review",
            "Security Review",
            "Conduct a comprehensive security review of code or system",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "security".to_string(),
            "review".to_string(),
            "audit".to_string(),
        ])
        .with_task_description(
            "Conduct security review of {{review_target}} focusing on {{security_aspects}}",
        )
        .with_task_details(
            "## Security Review Scope\n\n\
            - **Target**: {{review_target}}\n\
            - **Focus Areas**: {{security_aspects}}\n\
            - **Review Type**: {{review_type}}\n\n\
            ## Security Checklist\n\n\
            - Authentication and authorization mechanisms\n\
            - Input validation and sanitization\n\
            - SQL injection and XSS vulnerabilities\n\
            - Secure data storage and transmission\n\
            - Error handling and information disclosure\n\
            - Dependencies and supply chain security\n\
            {{#if crypto}}- Cryptographic implementations\n{{/if}}\
            {{#if api_security}}- API security and rate limiting\n{{/if}}\
            {{#if infrastructure}}- Infrastructure security configuration\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Review)
        .with_duration(120)
        .with_variables(vec![
            TemplateVariable::text("review_target", "What is being reviewed"),
            TemplateVariable::text("security_aspects", "Specific security concerns"),
            TemplateVariable::choice(
                "review_type",
                "Type of security review",
                vec![
                    "Code Review".to_string(),
                    "Architecture Review".to_string(),
                    "Penetration Test".to_string(),
                    "Compliance Audit".to_string(),
                ],
            ),
            TemplateVariable::boolean("crypto", "Review cryptographic code"),
            TemplateVariable::boolean("api_security", "Review API security"),
            TemplateVariable::boolean("infrastructure", "Review infrastructure"),
        ])
        .with_target_files(vec![
            "security-review-{{review_target}}.md".to_string(),
            "security-checklist-{{review_target}}.md".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:562-630 method ci_pipeline <-> ./crates/ccswarm/src/template/predefined.rs:931-987 method code_refactor
  Similarity: 96.92%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:ci_pipeline (lines 562-630) ---[0m
    pub fn ci_pipeline() -> Template {
        Template::new(
            "ci-pipeline",
            "CI/CD Pipeline",
            "Create a continuous integration pipeline",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "ci".to_string(),
            "cd".to_string(),
            "pipeline".to_string(),
            "automation".to_string(),
        ])
        .with_task_description("Create {{ci_platform}} pipeline for {{project_type}} project")
        .with_task_details(
            "## CI/CD Pipeline Configuration\n\n\
            - **Platform**: {{ci_platform}}\n\
            - **Project Type**: {{project_type}}\n\
            {{#if triggers}}- **Triggers**: {{triggers}}\n{{/if}}\
            {{#if environments}}- **Environments**: {{environments}}\n{{/if}}\n\n\
            ## Pipeline Stages\n\n\
            - Code checkout and setup\n\
            - Dependency installation\n\
            - Linting and code quality checks\n\
            - Unit and integration tests\n\
            - Build and package\n\
            {{#if security_scan}}- Security vulnerability scanning\n{{/if}}\
            {{#if deployment}}- Automated deployment to {{environments}}\n{{/if}}\
            {{#if notifications}}- Notification on success/failure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "ci_platform",
                "CI/CD platform",
                vec![
                    "GitHub Actions".to_string(),
                    "GitLab CI".to_string(),
                    "Jenkins".to_string(),
                    "CircleCI".to_string(),
                    "Azure DevOps".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "project_type",
                "Project type",
                vec![
                    "Node.js".to_string(),
                    "Python".to_string(),
                    "Rust".to_string(),
                    "Go".to_string(),
                    "Java".to_string(),
                    "Docker".to_string(),
                ],
            ),
            TemplateVariable::text("triggers", "Pipeline triggers (e.g., push, PR)").optional(),
            TemplateVariable::text("environments", "Deployment environments").optional(),
            TemplateVariable::boolean("security_scan", "Include security scanning"),
            TemplateVariable::boolean("deployment", "Include deployment stages"),
            TemplateVariable::boolean("notifications", "Set up notifications"),
        ])
        .with_target_files(vec![
            ".github/workflows/ci.yml".to_string(),
            ".github/workflows/cd.yml".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:code_refactor (lines 931-987) ---[0m
    pub fn code_refactor() -> Template {
        Template::new(
            "code-refactor",
            "Code Refactoring",
            "Refactor code to improve maintainability and design",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "refactor".to_string(),
            "maintainability".to_string(),
            "design".to_string(),
        ])
        .with_task_description("Refactor {{refactor_target}} to {{refactor_goal}}")
        .with_task_details(
            "## Refactoring Plan\n\n\
            - **Target**: {{refactor_target}}\n\
            - **Goal**: {{refactor_goal}}\n\
            - **Refactor Type**: {{refactor_type}}\n\
            {{#if design_patterns}}- **Design Patterns**: {{design_patterns}}\n{{/if}}\n\n\
            ## Refactoring Steps\n\n\
            - Analyze current code structure and identify issues\n\
            - Create comprehensive test coverage before refactoring\n\
            - Apply refactoring techniques incrementally\n\
            - Verify tests pass after each step\n\
            - Update documentation to reflect changes\n\
            {{#if breaking_changes}}- Document any breaking changes\n{{/if}}\
            {{#if performance_impact}}- Measure performance impact\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(80)
        .with_variables(vec![
            TemplateVariable::text("refactor_target", "Code to be refactored"),
            TemplateVariable::text("refactor_goal", "What the refactoring should achieve"),
            TemplateVariable::choice(
                "refactor_type",
                "Type of refactoring",
                vec![
                    "Extract Method".to_string(),
                    "Extract Class".to_string(),
                    "Move Method".to_string(),
                    "Rename".to_string(),
                    "Simplify Conditional".to_string(),
                    "Remove Duplication".to_string(),
                    "Improve Abstraction".to_string(),
                ],
            ),
            TemplateVariable::text("design_patterns", "Design patterns to apply").optional(),
            TemplateVariable::boolean("breaking_changes", "May introduce breaking changes"),
            TemplateVariable::boolean("performance_impact", "Measure performance impact"),
        ])
        .with_target_files(vec![
            "refactor-plan-{{refactor_target}}.md".to_string(),
            "tests/refactor/{{refactor_target}}_test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:111-166 method react_hook <-> ./crates/ccswarm/src/template/predefined.rs:562-630 method ci_pipeline
  Similarity: 96.80%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_hook (lines 111-166) ---[0m
    pub fn react_hook() -> Template {
        Template::new(
            "react-hook",
            "React Custom Hook",
            "Create a custom React hook with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "react".to_string(),
            "hook".to_string(),
            "typescript".to_string(),
        ])
        .with_task_description(
            "Create a custom React hook named {{hook_name}} for {{hook_purpose}}",
        )
        .with_task_details(
            "## Hook Specification\n\n\
            - **Name**: {{hook_name}}\n\
            - **Purpose**: {{hook_purpose}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if parameters}}- **Parameters**: {{parameters}}\n{{/if}}\
            {{#if return_type}}- **Returns**: {{return_type}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React hooks rules and conventions\n\
            - Include proper dependency arrays for useEffect\n\
            - Add comprehensive JSDoc comments\n\
            - Handle cleanup and memory leaks\n\
            {{#if testing}}- Create unit tests with React Hooks Testing Library\n{{/if}}\
            {{#if memoization}}- Use useMemo/useCallback for performance optimization\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(30)
        .with_variables(vec![
            TemplateVariable::text(
                "hook_name",
                "Name of the custom hook (e.g., useLocalStorage)",
            ),
            TemplateVariable::text("hook_purpose", "What the hook does"),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the hook",
                "src/hooks/{{hook_name}}.ts",
            ),
            TemplateVariable::text("parameters", "Hook parameters (optional)").optional(),
            TemplateVariable::text("return_type", "Hook return type (optional)").optional(),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("memoization", "Include performance optimizations"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.ts".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:ci_pipeline (lines 562-630) ---[0m
    pub fn ci_pipeline() -> Template {
        Template::new(
            "ci-pipeline",
            "CI/CD Pipeline",
            "Create a continuous integration pipeline",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "ci".to_string(),
            "cd".to_string(),
            "pipeline".to_string(),
            "automation".to_string(),
        ])
        .with_task_description("Create {{ci_platform}} pipeline for {{project_type}} project")
        .with_task_details(
            "## CI/CD Pipeline Configuration\n\n\
            - **Platform**: {{ci_platform}}\n\
            - **Project Type**: {{project_type}}\n\
            {{#if triggers}}- **Triggers**: {{triggers}}\n{{/if}}\
            {{#if environments}}- **Environments**: {{environments}}\n{{/if}}\n\n\
            ## Pipeline Stages\n\n\
            - Code checkout and setup\n\
            - Dependency installation\n\
            - Linting and code quality checks\n\
            - Unit and integration tests\n\
            - Build and package\n\
            {{#if security_scan}}- Security vulnerability scanning\n{{/if}}\
            {{#if deployment}}- Automated deployment to {{environments}}\n{{/if}}\
            {{#if notifications}}- Notification on success/failure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "ci_platform",
                "CI/CD platform",
                vec![
                    "GitHub Actions".to_string(),
                    "GitLab CI".to_string(),
                    "Jenkins".to_string(),
                    "CircleCI".to_string(),
                    "Azure DevOps".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "project_type",
                "Project type",
                vec![
                    "Node.js".to_string(),
                    "Python".to_string(),
                    "Rust".to_string(),
                    "Go".to_string(),
                    "Java".to_string(),
                    "Docker".to_string(),
                ],
            ),
            TemplateVariable::text("triggers", "Pipeline triggers (e.g., push, PR)").optional(),
            TemplateVariable::text("environments", "Deployment environments").optional(),
            TemplateVariable::boolean("security_scan", "Include security scanning"),
            TemplateVariable::boolean("deployment", "Include deployment stages"),
            TemplateVariable::boolean("notifications", "Set up notifications"),
        ])
        .with_target_files(vec![
            ".github/workflows/ci.yml".to_string(),
            ".github/workflows/cd.yml".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:313-378 method unit_test <-> ./crates/ccswarm/src/template/predefined.rs:501-559 method docker_service
  Similarity: 96.58%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:unit_test (lines 313-378) ---[0m
    pub fn unit_test() -> Template {
        Template::new(
            "unit-test",
            "Unit Test",
            "Create comprehensive unit tests for a function or module",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "unit-test".to_string(),
            "quality".to_string(),
        ])
        .with_task_description("Create unit tests for {{test_target}} covering {{test_scenarios}}")
        .with_task_details(
            "## Test Specification\n\n\
            - **Target**: {{test_target}}\n\
            - **Test Framework**: {{test_framework}}\n\
            - **Scenarios**: {{test_scenarios}}\n\
            {{#if mocking}}- **Mocking**: {{mocking_strategy}}\n{{/if}}\n\n\
            ## Test Coverage Requirements\n\n\
            - Happy path scenarios\n\
            - Error conditions and edge cases\n\
            - Boundary value testing\n\
            - Input validation testing\n\
            {{#if async_code}}- Async operation testing\n{{/if}}\
            {{#if performance}}- Performance benchmarks\n{{/if}}\
            - Achieve >90% code coverage for the target",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Testing)
        .with_duration(35)
        .with_variables(vec![
            TemplateVariable::text("test_target", "Function, class, or module to test"),
            TemplateVariable::text("test_scenarios", "Specific scenarios to test"),
            TemplateVariable::choice(
                "test_framework",
                "Testing framework",
                vec![
                    "Jest".to_string(),
                    "Vitest".to_string(),
                    "Mocha".to_string(),
                    "pytest".to_string(),
                    "Rust test".to_string(),
                    "Go test".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "mocking_strategy",
                "Mocking approach",
                vec![
                    "Manual mocks".to_string(),
                    "Jest mocks".to_string(),
                    "Sinon".to_string(),
                    "unittest.mock".to_string(),
                    "mockall".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("async_code", "Testing async operations"),
            TemplateVariable::boolean("performance", "Include performance tests"),
        ])
        .with_target_files(vec![
            "tests/{{test_target}}.test.{{file_extension}}".to_string()
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:docker_service (lines 501-559) ---[0m
    pub fn docker_service() -> Template {
        Template::new(
            "docker-service",
            "Docker Service",
            "Create a Docker container configuration for a service",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "docker".to_string(),
            "container".to_string(),
            "devops".to_string(),
        ])
        .with_task_description("Create Docker configuration for {{service_name}} service")
        .with_task_details(
            "## Docker Service Configuration\n\n\
            - **Service**: {{service_name}}\n\
            - **Base Image**: {{base_image}}\n\
            {{#if ports}}- **Ports**: {{ports}}\n{{/if}}\
            {{#if volumes}}- **Volumes**: {{volumes}}\n{{/if}}\
            {{#if environment}}- **Environment**: {{environment}}\n{{/if}}\n\n\
            ## Container Requirements\n\n\
            - Multi-stage build for optimization\n\
            - Non-root user for security\n\
            - Health check configuration\n\
            - Proper signal handling\n\
            - Minimal attack surface\n\
            {{#if secrets}}- Secure secret management\n{{/if}}\
            {{#if monitoring}}- Monitoring and logging setup\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("service_name", "Name of the service"),
            TemplateVariable::choice(
                "base_image",
                "Base Docker image",
                vec![
                    "alpine".to_string(),
                    "ubuntu".to_string(),
                    "node:alpine".to_string(),
                    "python:alpine".to_string(),
                    "rust:alpine".to_string(),
                    "nginx:alpine".to_string(),
                ],
            ),
            TemplateVariable::text("ports", "Port mappings (e.g., 8080:8080)").optional(),
            TemplateVariable::text("volumes", "Volume mounts").optional(),
            TemplateVariable::text("environment", "Environment variables").optional(),
            TemplateVariable::boolean("secrets", "Include secret management"),
            TemplateVariable::boolean("monitoring", "Add monitoring setup"),
        ])
        .with_target_files(vec![
            "Dockerfile".to_string(),
            "docker-compose.yml".to_string(),
            ".dockerignore".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:313-378 method unit_test <-> ./crates/ccswarm/src/template/predefined.rs:381-439 method integration_test
  Similarity: 96.49%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:unit_test (lines 313-378) ---[0m
    pub fn unit_test() -> Template {
        Template::new(
            "unit-test",
            "Unit Test",
            "Create comprehensive unit tests for a function or module",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "unit-test".to_string(),
            "quality".to_string(),
        ])
        .with_task_description("Create unit tests for {{test_target}} covering {{test_scenarios}}")
        .with_task_details(
            "## Test Specification\n\n\
            - **Target**: {{test_target}}\n\
            - **Test Framework**: {{test_framework}}\n\
            - **Scenarios**: {{test_scenarios}}\n\
            {{#if mocking}}- **Mocking**: {{mocking_strategy}}\n{{/if}}\n\n\
            ## Test Coverage Requirements\n\n\
            - Happy path scenarios\n\
            - Error conditions and edge cases\n\
            - Boundary value testing\n\
            - Input validation testing\n\
            {{#if async_code}}- Async operation testing\n{{/if}}\
            {{#if performance}}- Performance benchmarks\n{{/if}}\
            - Achieve >90% code coverage for the target",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Testing)
        .with_duration(35)
        .with_variables(vec![
            TemplateVariable::text("test_target", "Function, class, or module to test"),
            TemplateVariable::text("test_scenarios", "Specific scenarios to test"),
            TemplateVariable::choice(
                "test_framework",
                "Testing framework",
                vec![
                    "Jest".to_string(),
                    "Vitest".to_string(),
                    "Mocha".to_string(),
                    "pytest".to_string(),
                    "Rust test".to_string(),
                    "Go test".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "mocking_strategy",
                "Mocking approach",
                vec![
                    "Manual mocks".to_string(),
                    "Jest mocks".to_string(),
                    "Sinon".to_string(),
                    "unittest.mock".to_string(),
                    "mockall".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("async_code", "Testing async operations"),
            TemplateVariable::boolean("performance", "Include performance tests"),
        ])
        .with_target_files(vec![
            "tests/{{test_target}}.test.{{file_extension}}".to_string()
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:integration_test (lines 381-439) ---[0m
    pub fn integration_test() -> Template {
        Template::new(
            "integration-test",
            "Integration Test",
            "Create integration tests for system components",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "integration".to_string(),
            "e2e".to_string(),
        ])
        .with_task_description(
            "Create integration tests for {{system_component}} testing {{integration_points}}",
        )
        .with_task_details(
            "## Integration Test Specification\n\n\
            - **Component**: {{system_component}}\n\
            - **Integration Points**: {{integration_points}}\n\
            - **Test Environment**: {{test_environment}}\n\
            {{#if database}}- **Database**: Test database setup required\n{{/if}}\
            {{#if external_services}}- **External Services**: {{external_services}}\n{{/if}}\n\n\
            ## Test Requirements\n\n\
            - Set up test environment and data\n\
            - Test end-to-end workflows\n\
            - Verify data consistency across components\n\
            - Test error propagation and recovery\n\
            - Clean up test data after execution\n\
            {{#if performance}}- Measure integration performance\n{{/if}}\
            {{#if security}}- Verify security boundaries\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Testing)
        .with_duration(75)
        .with_variables(vec![
            TemplateVariable::text("system_component", "System component being tested"),
            TemplateVariable::text("integration_points", "What integrations to test"),
            TemplateVariable::choice(
                "test_environment",
                "Test environment",
                vec![
                    "Docker Compose".to_string(),
                    "Testcontainers".to_string(),
                    "In-memory".to_string(),
                    "Staging".to_string(),
                ],
            ),
            TemplateVariable::boolean("database", "Includes database testing"),
            TemplateVariable::text("external_services", "External services involved").optional(),
            TemplateVariable::boolean("performance", "Include performance testing"),
            TemplateVariable::boolean("security", "Include security testing"),
        ])
        .with_target_files(vec![
            "tests/integration/{{system_component}}_integration.test.{{file_extension}}"
                .to_string(),
            "tests/fixtures/{{system_component}}_data.json".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:253-310 method database_migration <-> ./crates/ccswarm/src/template/predefined.rs:313-378 method unit_test
  Similarity: 97.13%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:database_migration (lines 253-310) ---[0m
    pub fn database_migration() -> Template {
        Template::new(
            "database-migration",
            "Database Migration",
            "Create a database migration script",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "database".to_string(),
            "migration".to_string(),
            "schema".to_string(),
        ])
        .with_task_description("Create database migration to {{migration_purpose}}")
        .with_task_details(
            "## Migration Details\n\n\
            - **Purpose**: {{migration_purpose}}\n\
            - **Type**: {{migration_type}}\n\
            {{#if table_name}}- **Table**: {{table_name}}\n{{/if}}\
            {{#if columns}}- **Columns**: {{columns}}\n{{/if}}\
            {{#if indexes}}- **Indexes**: {{indexes}}\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Write both up and down migrations\n\
            - Include proper constraints and indexes\n\
            - Handle existing data appropriately\n\
            - Add comments for complex operations\n\
            - Test migration on sample data\n\
            {{#if backup}}- Create backup before running\n{{/if}}\
            {{#if rollback_plan}}- Document rollback procedure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(40)
        .with_variables(vec![
            TemplateVariable::text("migration_purpose", "What this migration accomplishes"),
            TemplateVariable::choice(
                "migration_type",
                "Type of migration",
                vec![
                    "Create Table".to_string(),
                    "Alter Table".to_string(),
                    "Drop Table".to_string(),
                    "Add Column".to_string(),
                    "Drop Column".to_string(),
                    "Create Index".to_string(),
                    "Data Migration".to_string(),
                ],
            ),
            TemplateVariable::text("table_name", "Table name (if applicable)").optional(),
            TemplateVariable::text("columns", "Column definitions (optional)").optional(),
            TemplateVariable::text("indexes", "Index definitions (optional)").optional(),
            TemplateVariable::boolean("backup", "Create backup before migration"),
            TemplateVariable::boolean("rollback_plan", "Document rollback procedure"),
        ])
        .with_target_files(vec![
            "migrations/{{current_datetime}}_{{migration_purpose}}.sql".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:unit_test (lines 313-378) ---[0m
    pub fn unit_test() -> Template {
        Template::new(
            "unit-test",
            "Unit Test",
            "Create comprehensive unit tests for a function or module",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "unit-test".to_string(),
            "quality".to_string(),
        ])
        .with_task_description("Create unit tests for {{test_target}} covering {{test_scenarios}}")
        .with_task_details(
            "## Test Specification\n\n\
            - **Target**: {{test_target}}\n\
            - **Test Framework**: {{test_framework}}\n\
            - **Scenarios**: {{test_scenarios}}\n\
            {{#if mocking}}- **Mocking**: {{mocking_strategy}}\n{{/if}}\n\n\
            ## Test Coverage Requirements\n\n\
            - Happy path scenarios\n\
            - Error conditions and edge cases\n\
            - Boundary value testing\n\
            - Input validation testing\n\
            {{#if async_code}}- Async operation testing\n{{/if}}\
            {{#if performance}}- Performance benchmarks\n{{/if}}\
            - Achieve >90% code coverage for the target",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Testing)
        .with_duration(35)
        .with_variables(vec![
            TemplateVariable::text("test_target", "Function, class, or module to test"),
            TemplateVariable::text("test_scenarios", "Specific scenarios to test"),
            TemplateVariable::choice(
                "test_framework",
                "Testing framework",
                vec![
                    "Jest".to_string(),
                    "Vitest".to_string(),
                    "Mocha".to_string(),
                    "pytest".to_string(),
                    "Rust test".to_string(),
                    "Go test".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "mocking_strategy",
                "Mocking approach",
                vec![
                    "Manual mocks".to_string(),
                    "Jest mocks".to_string(),
                    "Sinon".to_string(),
                    "unittest.mock".to_string(),
                    "mockall".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("async_code", "Testing async operations"),
            TemplateVariable::boolean("performance", "Include performance tests"),
        ])
        .with_target_files(vec![
            "tests/{{test_target}}.test.{{file_extension}}".to_string()
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:633-701 method documentation_page <-> ./crates/ccswarm/src/template/predefined.rs:875-928 method performance_optimization
  Similarity: 97.61%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:documentation_page (lines 633-701) ---[0m
    pub fn documentation_page() -> Template {
        Template::new(
            "documentation-page",
            "Documentation Page",
            "Create comprehensive documentation for a feature or API",
            TemplateCategory::Documentation,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "documentation".to_string(),
            "docs".to_string(),
            "guide".to_string(),
        ])
        .with_task_description(
            "Create documentation for {{doc_subject}} explaining {{doc_purpose}}",
        )
        .with_task_details(
            "## Documentation Specification\n\n\
            - **Subject**: {{doc_subject}}\n\
            - **Purpose**: {{doc_purpose}}\n\
            - **Audience**: {{target_audience}}\n\
            {{#if doc_type}}- **Type**: {{doc_type}}\n{{/if}}\n\n\
            ## Content Requirements\n\n\
            - Clear overview and introduction\n\
            - Step-by-step instructions or usage guide\n\
            - Code examples with explanations\n\
            - Common pitfalls and troubleshooting\n\
            - Links to related documentation\n\
            {{#if api_reference}}- Complete API reference with parameters\n{{/if}}\
            {{#if diagrams}}- Architecture or flow diagrams\n{{/if}}\
            {{#if examples}}- Real-world usage examples\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Documentation)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("doc_subject", "What is being documented"),
            TemplateVariable::text("doc_purpose", "Purpose of the documentation"),
            TemplateVariable::choice(
                "target_audience",
                "Target audience",
                vec![
                    "Developers".to_string(),
                    "Users".to_string(),
                    "System Administrators".to_string(),
                    "Product Managers".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "doc_type",
                "Documentation type",
                vec![
                    "API Reference".to_string(),
                    "User Guide".to_string(),
                    "Tutorial".to_string(),
                    "Architecture Guide".to_string(),
                    "Troubleshooting".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("api_reference", "Include API reference"),
            TemplateVariable::boolean("diagrams", "Include diagrams"),
            TemplateVariable::boolean("examples", "Include code examples"),
        ])
        .with_target_files(vec![
            "docs/{{doc_subject}}.md".to_string(),
            "examples/{{doc_subject}}_example.{{file_extension}}".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:performance_optimization (lines 875-928) ---[0m
    pub fn performance_optimization() -> Template {
        Template::new(
            "performance-optimization",
            "Performance Optimization",
            "Optimize performance of a system component",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "performance".to_string(),
            "optimization".to_string(),
            "profiling".to_string(),
        ])
        .with_task_description(
            "Optimize performance of {{optimization_target}} to improve {{performance_metric}}",
        )
        .with_task_details(
            "## Performance Optimization Plan\n\n\
            - **Target**: {{optimization_target}}\n\
            - **Metric**: {{performance_metric}}\n\
            - **Current Performance**: {{current_baseline}}\n\
            - **Target Performance**: {{target_goal}}\n\n\
            ## Optimization Approach\n\n\
            - Profile current performance with appropriate tools\n\
            - Identify bottlenecks and hot paths\n\
            - Implement targeted optimizations\n\
            - Measure impact of each optimization\n\
            - Verify no functional regressions\n\
            {{#if caching}}- Implement caching strategies\n{{/if}}\
            {{#if database}}- Optimize database queries and indexes\n{{/if}}\
            {{#if memory}}- Reduce memory allocation and fragmentation\n{{/if}}\
            {{#if concurrency}}- Improve concurrency and parallelization\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("optimization_target", "Component to optimize"),
            TemplateVariable::text(
                "performance_metric",
                "Metric to improve (latency, throughput, etc.)",
            ),
            TemplateVariable::text("current_baseline", "Current performance baseline").optional(),
            TemplateVariable::text("target_goal", "Performance improvement goal").optional(),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("database", "Optimize database operations"),
            TemplateVariable::boolean("memory", "Optimize memory usage"),
            TemplateVariable::boolean("concurrency", "Improve concurrency"),
        ])
        .with_target_files(vec![
            "performance-analysis-{{optimization_target}}.md".to_string(),
            "benchmarks/{{optimization_target}}_bench.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:313-378 method unit_test <-> ./crates/ccswarm/src/template/predefined.rs:704-761 method rust_module
  Similarity: 96.58%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:unit_test (lines 313-378) ---[0m
    pub fn unit_test() -> Template {
        Template::new(
            "unit-test",
            "Unit Test",
            "Create comprehensive unit tests for a function or module",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "unit-test".to_string(),
            "quality".to_string(),
        ])
        .with_task_description("Create unit tests for {{test_target}} covering {{test_scenarios}}")
        .with_task_details(
            "## Test Specification\n\n\
            - **Target**: {{test_target}}\n\
            - **Test Framework**: {{test_framework}}\n\
            - **Scenarios**: {{test_scenarios}}\n\
            {{#if mocking}}- **Mocking**: {{mocking_strategy}}\n{{/if}}\n\n\
            ## Test Coverage Requirements\n\n\
            - Happy path scenarios\n\
            - Error conditions and edge cases\n\
            - Boundary value testing\n\
            - Input validation testing\n\
            {{#if async_code}}- Async operation testing\n{{/if}}\
            {{#if performance}}- Performance benchmarks\n{{/if}}\
            - Achieve >90% code coverage for the target",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Testing)
        .with_duration(35)
        .with_variables(vec![
            TemplateVariable::text("test_target", "Function, class, or module to test"),
            TemplateVariable::text("test_scenarios", "Specific scenarios to test"),
            TemplateVariable::choice(
                "test_framework",
                "Testing framework",
                vec![
                    "Jest".to_string(),
                    "Vitest".to_string(),
                    "Mocha".to_string(),
                    "pytest".to_string(),
                    "Rust test".to_string(),
                    "Go test".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "mocking_strategy",
                "Mocking approach",
                vec![
                    "Manual mocks".to_string(),
                    "Jest mocks".to_string(),
                    "Sinon".to_string(),
                    "unittest.mock".to_string(),
                    "mockall".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("async_code", "Testing async operations"),
            TemplateVariable::boolean("performance", "Include performance tests"),
        ])
        .with_target_files(vec![
            "tests/{{test_target}}.test.{{file_extension}}".to_string()
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_module (lines 704-761) ---[0m
    pub fn rust_module() -> Template {
        Template::new(
            "rust-module",
            "Rust Module",
            "Create a new Rust module with proper structure and documentation",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "module".to_string(),
            "backend".to_string(),
        ])
        .with_task_description("Create Rust module {{module_name}} for {{module_purpose}}")
        .with_task_details(
            "## Rust Module Specification\n\n\
            - **Module**: {{module_name}}\n\
            - **Purpose**: {{module_purpose}}\n\
            - **Visibility**: {{visibility}}\n\
            {{#if traits}}- **Traits**: {{traits}}\n{{/if}}\
            {{#if structs}}- **Structs**: {{structs}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Follow Rust naming conventions\n\
            - Add comprehensive rustdoc comments\n\
            - Implement proper error handling with Result<T, E>\n\
            - Use appropriate visibility modifiers\n\
            - Include unit tests in #[cfg(test)] module\n\
            {{#if async_support}}- Implement async/await patterns\n{{/if}}\
            {{#if serde_support}}- Add Serde serialization support\n{{/if}}\
            {{#if benchmark}}- Include criterion benchmarks\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("module_name", "Name of the Rust module"),
            TemplateVariable::text("module_purpose", "What this module does"),
            TemplateVariable::choice(
                "visibility",
                "Module visibility",
                vec![
                    "public".to_string(),
                    "private".to_string(),
                    "pub(crate)".to_string(),
                ],
            ),
            TemplateVariable::text("traits", "Traits to implement").optional(),
            TemplateVariable::text("structs", "Main structs in the module").optional(),
            TemplateVariable::boolean("async_support", "Include async support"),
            TemplateVariable::boolean("serde_support", "Add Serde support"),
            TemplateVariable::boolean("benchmark", "Include benchmarks"),
        ])
        .with_target_files(vec![
            "src/{{module_name}}/mod.rs".to_string(),
            "src/{{module_name}}/types.rs".to_string(),
            "benches/{{module_name}}_bench.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:313-378 method unit_test <-> ./crates/ccswarm/src/template/predefined.rs:931-987 method code_refactor
  Similarity: 97.20%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:unit_test (lines 313-378) ---[0m
    pub fn unit_test() -> Template {
        Template::new(
            "unit-test",
            "Unit Test",
            "Create comprehensive unit tests for a function or module",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "unit-test".to_string(),
            "quality".to_string(),
        ])
        .with_task_description("Create unit tests for {{test_target}} covering {{test_scenarios}}")
        .with_task_details(
            "## Test Specification\n\n\
            - **Target**: {{test_target}}\n\
            - **Test Framework**: {{test_framework}}\n\
            - **Scenarios**: {{test_scenarios}}\n\
            {{#if mocking}}- **Mocking**: {{mocking_strategy}}\n{{/if}}\n\n\
            ## Test Coverage Requirements\n\n\
            - Happy path scenarios\n\
            - Error conditions and edge cases\n\
            - Boundary value testing\n\
            - Input validation testing\n\
            {{#if async_code}}- Async operation testing\n{{/if}}\
            {{#if performance}}- Performance benchmarks\n{{/if}}\
            - Achieve >90% code coverage for the target",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Testing)
        .with_duration(35)
        .with_variables(vec![
            TemplateVariable::text("test_target", "Function, class, or module to test"),
            TemplateVariable::text("test_scenarios", "Specific scenarios to test"),
            TemplateVariable::choice(
                "test_framework",
                "Testing framework",
                vec![
                    "Jest".to_string(),
                    "Vitest".to_string(),
                    "Mocha".to_string(),
                    "pytest".to_string(),
                    "Rust test".to_string(),
                    "Go test".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "mocking_strategy",
                "Mocking approach",
                vec![
                    "Manual mocks".to_string(),
                    "Jest mocks".to_string(),
                    "Sinon".to_string(),
                    "unittest.mock".to_string(),
                    "mockall".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("async_code", "Testing async operations"),
            TemplateVariable::boolean("performance", "Include performance tests"),
        ])
        .with_target_files(vec![
            "tests/{{test_target}}.test.{{file_extension}}".to_string()
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:code_refactor (lines 931-987) ---[0m
    pub fn code_refactor() -> Template {
        Template::new(
            "code-refactor",
            "Code Refactoring",
            "Refactor code to improve maintainability and design",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "refactor".to_string(),
            "maintainability".to_string(),
            "design".to_string(),
        ])
        .with_task_description("Refactor {{refactor_target}} to {{refactor_goal}}")
        .with_task_details(
            "## Refactoring Plan\n\n\
            - **Target**: {{refactor_target}}\n\
            - **Goal**: {{refactor_goal}}\n\
            - **Refactor Type**: {{refactor_type}}\n\
            {{#if design_patterns}}- **Design Patterns**: {{design_patterns}}\n{{/if}}\n\n\
            ## Refactoring Steps\n\n\
            - Analyze current code structure and identify issues\n\
            - Create comprehensive test coverage before refactoring\n\
            - Apply refactoring techniques incrementally\n\
            - Verify tests pass after each step\n\
            - Update documentation to reflect changes\n\
            {{#if breaking_changes}}- Document any breaking changes\n{{/if}}\
            {{#if performance_impact}}- Measure performance impact\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(80)
        .with_variables(vec![
            TemplateVariable::text("refactor_target", "Code to be refactored"),
            TemplateVariable::text("refactor_goal", "What the refactoring should achieve"),
            TemplateVariable::choice(
                "refactor_type",
                "Type of refactoring",
                vec![
                    "Extract Method".to_string(),
                    "Extract Class".to_string(),
                    "Move Method".to_string(),
                    "Rename".to_string(),
                    "Simplify Conditional".to_string(),
                    "Remove Duplication".to_string(),
                    "Improve Abstraction".to_string(),
                ],
            ),
            TemplateVariable::text("design_patterns", "Design patterns to apply").optional(),
            TemplateVariable::boolean("breaking_changes", "May introduce breaking changes"),
            TemplateVariable::boolean("performance_impact", "Measure performance impact"),
        ])
        .with_target_files(vec![
            "refactor-plan-{{refactor_target}}.md".to_string(),
            "tests/refactor/{{refactor_target}}_test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:562-630 method ci_pipeline <-> ./crates/ccswarm/src/template/predefined.rs:875-928 method performance_optimization
  Similarity: 96.90%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:ci_pipeline (lines 562-630) ---[0m
    pub fn ci_pipeline() -> Template {
        Template::new(
            "ci-pipeline",
            "CI/CD Pipeline",
            "Create a continuous integration pipeline",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "ci".to_string(),
            "cd".to_string(),
            "pipeline".to_string(),
            "automation".to_string(),
        ])
        .with_task_description("Create {{ci_platform}} pipeline for {{project_type}} project")
        .with_task_details(
            "## CI/CD Pipeline Configuration\n\n\
            - **Platform**: {{ci_platform}}\n\
            - **Project Type**: {{project_type}}\n\
            {{#if triggers}}- **Triggers**: {{triggers}}\n{{/if}}\
            {{#if environments}}- **Environments**: {{environments}}\n{{/if}}\n\n\
            ## Pipeline Stages\n\n\
            - Code checkout and setup\n\
            - Dependency installation\n\
            - Linting and code quality checks\n\
            - Unit and integration tests\n\
            - Build and package\n\
            {{#if security_scan}}- Security vulnerability scanning\n{{/if}}\
            {{#if deployment}}- Automated deployment to {{environments}}\n{{/if}}\
            {{#if notifications}}- Notification on success/failure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "ci_platform",
                "CI/CD platform",
                vec![
                    "GitHub Actions".to_string(),
                    "GitLab CI".to_string(),
                    "Jenkins".to_string(),
                    "CircleCI".to_string(),
                    "Azure DevOps".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "project_type",
                "Project type",
                vec![
                    "Node.js".to_string(),
                    "Python".to_string(),
                    "Rust".to_string(),
                    "Go".to_string(),
                    "Java".to_string(),
                    "Docker".to_string(),
                ],
            ),
            TemplateVariable::text("triggers", "Pipeline triggers (e.g., push, PR)").optional(),
            TemplateVariable::text("environments", "Deployment environments").optional(),
            TemplateVariable::boolean("security_scan", "Include security scanning"),
            TemplateVariable::boolean("deployment", "Include deployment stages"),
            TemplateVariable::boolean("notifications", "Set up notifications"),
        ])
        .with_target_files(vec![
            ".github/workflows/ci.yml".to_string(),
            ".github/workflows/cd.yml".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:performance_optimization (lines 875-928) ---[0m
    pub fn performance_optimization() -> Template {
        Template::new(
            "performance-optimization",
            "Performance Optimization",
            "Optimize performance of a system component",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "performance".to_string(),
            "optimization".to_string(),
            "profiling".to_string(),
        ])
        .with_task_description(
            "Optimize performance of {{optimization_target}} to improve {{performance_metric}}",
        )
        .with_task_details(
            "## Performance Optimization Plan\n\n\
            - **Target**: {{optimization_target}}\n\
            - **Metric**: {{performance_metric}}\n\
            - **Current Performance**: {{current_baseline}}\n\
            - **Target Performance**: {{target_goal}}\n\n\
            ## Optimization Approach\n\n\
            - Profile current performance with appropriate tools\n\
            - Identify bottlenecks and hot paths\n\
            - Implement targeted optimizations\n\
            - Measure impact of each optimization\n\
            - Verify no functional regressions\n\
            {{#if caching}}- Implement caching strategies\n{{/if}}\
            {{#if database}}- Optimize database queries and indexes\n{{/if}}\
            {{#if memory}}- Reduce memory allocation and fragmentation\n{{/if}}\
            {{#if concurrency}}- Improve concurrency and parallelization\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("optimization_target", "Component to optimize"),
            TemplateVariable::text(
                "performance_metric",
                "Metric to improve (latency, throughput, etc.)",
            ),
            TemplateVariable::text("current_baseline", "Current performance baseline").optional(),
            TemplateVariable::text("target_goal", "Performance improvement goal").optional(),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("database", "Optimize database operations"),
            TemplateVariable::boolean("memory", "Optimize memory usage"),
            TemplateVariable::boolean("concurrency", "Improve concurrency"),
        ])
        .with_target_files(vec![
            "performance-analysis-{{optimization_target}}.md".to_string(),
            "benchmarks/{{optimization_target}}_bench.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:32-108 method react_component <-> ./crates/ccswarm/src/template/predefined.rs:764-813 method rust_cli_command
  Similarity: 93.61%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_component (lines 32-108) ---[0m
    pub fn react_component() -> Template {
        Template::new(
            "react-component",
            "React Component",
            "Create a new React functional component with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec!["react".to_string(), "frontend".to_string(), "typescript".to_string(), "component".to_string()])
        .with_task_description("Create a {{component_type}} React component named {{component_name}}{{#if props}} with props: {{props}}{{/if}}{{#if styling}} using {{styling}}{{/if}}")
        .with_task_details(
            "## Component Specification\n\n\
            - **Name**: {{component_name}}\n\
            - **Type**: {{component_type}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if props}}- **Props**: {{props}}\n{{/if}}\
            {{#if state}}- **State**: {{state}}\n{{/if}}\
            {{#if styling}}- **Styling**: {{styling}}\n{{/if}}\
            {{#if accessibility}}- **Accessibility**: Include ARIA attributes and keyboard navigation\n{{/if}}\
            {{#if testing}}- **Testing**: Create unit tests with React Testing Library\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React best practices and hooks conventions\n\
            - Implement proper error boundaries if needed\n\
            - Add PropTypes or TypeScript interfaces for props\n\
            - Include JSDoc comments for component documentation\n\
            {{#if responsive}}- Make component responsive for mobile and desktop\n{{/if}}\
            {{#if performance}}- Optimize for performance with React.memo if appropriate\n{{/if}}"
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("component_name", "Name of the React component"),
            TemplateVariable::choice(
                "component_type",
                "Type of component",
                vec!["functional".to_string(), "class".to_string(), "custom hook".to_string()],
            ),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the component",
                "src/components/{{component_name}}.tsx"
            ),
            TemplateVariable::text("props", "Component props (optional)").optional(),
            TemplateVariable::text("state", "Component state description (optional)").optional(),
            TemplateVariable::choice(
                "styling",
                "Styling approach",
                vec![
                    "CSS Modules".to_string(),
                    "Styled Components".to_string(),
                    "Tailwind CSS".to_string(),
                    "SCSS".to_string(),
                    "Emotion".to_string(),
                ],
            ).optional(),
            TemplateVariable::boolean("accessibility", "Include accessibility features"),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("responsive", "Make responsive design"),
            TemplateVariable::boolean("performance", "Optimize for performance"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.tsx".to_string(),
            "{{file_path}}.module.css".to_string(),
        ])
        .with_preconditions(vec![
            "React and TypeScript are configured in the project".to_string(),
            "Component directory structure exists".to_string(),
        ])
        .with_post_actions(vec![
            "Export component from index file".to_string(),
            "Add to Storybook if available".to_string(),
            "Update documentation".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_cli_command (lines 764-813) ---[0m
    pub fn rust_cli_command() -> Template {
        Template::new(
            "rust-cli-command",
            "Rust CLI Command",
            "Add a new command to a Rust CLI application using clap",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "cli".to_string(),
            "clap".to_string(),
            "command".to_string(),
        ])
        .with_task_description("Add CLI command {{command_name}} to {{command_description}}")
        .with_task_details(
            "## CLI Command Specification\n\n\
            - **Command**: {{command_name}}\n\
            - **Description**: {{command_description}}\n\
            {{#if arguments}}- **Arguments**: {{arguments}}\n{{/if}}\
            {{#if options}}- **Options**: {{options}}\n{{/if}}\
            {{#if subcommands}}- **Subcommands**: {{subcommands}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use clap derive macros for argument parsing\n\
            - Implement proper error handling and exit codes\n\
            - Add comprehensive help text and examples\n\
            - Include input validation\n\
            - Support common CLI patterns (--help, --version)\n\
            {{#if config_file}}- Support configuration file loading\n{{/if}}\
            {{#if logging}}- Integrate with logging framework\n{{/if}}\
            {{#if testing}}- Add integration tests for CLI\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(50)
        .with_variables(vec![
            TemplateVariable::text("command_name", "Name of the CLI command"),
            TemplateVariable::text("command_description", "What the command does"),
            TemplateVariable::text("arguments", "Required arguments").optional(),
            TemplateVariable::text("options", "Optional flags and options").optional(),
            TemplateVariable::text("subcommands", "Subcommands if any").optional(),
            TemplateVariable::boolean("config_file", "Support config files"),
            TemplateVariable::boolean("logging", "Add logging support"),
            TemplateVariable::boolean("testing", "Create CLI tests"),
        ])
        .with_target_files(vec![
            "src/cli/{{command_name}}.rs".to_string(),
            "tests/cli/{{command_name}}_test.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:313-378 method unit_test <-> ./crates/ccswarm/src/template/predefined.rs:442-498 method bug_fix
  Similarity: 96.51%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:unit_test (lines 313-378) ---[0m
    pub fn unit_test() -> Template {
        Template::new(
            "unit-test",
            "Unit Test",
            "Create comprehensive unit tests for a function or module",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "unit-test".to_string(),
            "quality".to_string(),
        ])
        .with_task_description("Create unit tests for {{test_target}} covering {{test_scenarios}}")
        .with_task_details(
            "## Test Specification\n\n\
            - **Target**: {{test_target}}\n\
            - **Test Framework**: {{test_framework}}\n\
            - **Scenarios**: {{test_scenarios}}\n\
            {{#if mocking}}- **Mocking**: {{mocking_strategy}}\n{{/if}}\n\n\
            ## Test Coverage Requirements\n\n\
            - Happy path scenarios\n\
            - Error conditions and edge cases\n\
            - Boundary value testing\n\
            - Input validation testing\n\
            {{#if async_code}}- Async operation testing\n{{/if}}\
            {{#if performance}}- Performance benchmarks\n{{/if}}\
            - Achieve >90% code coverage for the target",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Testing)
        .with_duration(35)
        .with_variables(vec![
            TemplateVariable::text("test_target", "Function, class, or module to test"),
            TemplateVariable::text("test_scenarios", "Specific scenarios to test"),
            TemplateVariable::choice(
                "test_framework",
                "Testing framework",
                vec![
                    "Jest".to_string(),
                    "Vitest".to_string(),
                    "Mocha".to_string(),
                    "pytest".to_string(),
                    "Rust test".to_string(),
                    "Go test".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "mocking_strategy",
                "Mocking approach",
                vec![
                    "Manual mocks".to_string(),
                    "Jest mocks".to_string(),
                    "Sinon".to_string(),
                    "unittest.mock".to_string(),
                    "mockall".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("async_code", "Testing async operations"),
            TemplateVariable::boolean("performance", "Include performance tests"),
        ])
        .with_target_files(vec![
            "tests/{{test_target}}.test.{{file_extension}}".to_string()
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:bug_fix (lines 442-498) ---[0m
    pub fn bug_fix() -> Template {
        Template::new(
            "bug-fix",
            "Bug Fix",
            "Fix a reported bug with proper testing and documentation",
            TemplateCategory::BugFix,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "bug".to_string(),
            "fix".to_string(),
            "maintenance".to_string(),
        ])
        .with_task_description("Fix bug: {{bug_description}} ({{bug_severity}})")
        .with_task_details(
            "## Bug Report\n\n\
            - **Description**: {{bug_description}}\n\
            - **Severity**: {{bug_severity}}\n\
            {{#if reproduction_steps}}- **Reproduction**: {{reproduction_steps}}\n{{/if}}\
            {{#if affected_components}}- **Affected Components**: {{affected_components}}\n{{/if}}\
            {{#if user_impact}}- **User Impact**: {{user_impact}}\n{{/if}}\n\n\
            ## Fix Requirements\n\n\
            - Reproduce the bug in a test environment\n\
            - Identify root cause through debugging\n\
            - Implement minimal fix that addresses root cause\n\
            - Add regression tests to prevent recurrence\n\
            - Verify fix doesn't introduce new issues\n\
            {{#if documentation}}- Update relevant documentation\n{{/if}}\
            {{#if release_notes}}- Add to release notes\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Bugfix)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("bug_description", "Clear description of the bug"),
            TemplateVariable::choice(
                "bug_severity",
                "Bug severity level",
                vec![
                    "Critical".to_string(),
                    "High".to_string(),
                    "Medium".to_string(),
                    "Low".to_string(),
                ],
            ),
            TemplateVariable::text("reproduction_steps", "Steps to reproduce the bug").optional(),
            TemplateVariable::text("affected_components", "Components affected by the bug")
                .optional(),
            TemplateVariable::text("user_impact", "How this affects users").optional(),
            TemplateVariable::boolean("documentation", "Update documentation"),
            TemplateVariable::boolean("release_notes", "Add to release notes"),
        ])
        .with_target_files(vec![
            "src/{{affected_components}}/fix.{{file_extension}}".to_string(),
            "tests/regression/{{bug_description}}.test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:313-378 method unit_test <-> ./crates/ccswarm/src/template/predefined.rs:816-872 method security_review
  Similarity: 96.36%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:unit_test (lines 313-378) ---[0m
    pub fn unit_test() -> Template {
        Template::new(
            "unit-test",
            "Unit Test",
            "Create comprehensive unit tests for a function or module",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "unit-test".to_string(),
            "quality".to_string(),
        ])
        .with_task_description("Create unit tests for {{test_target}} covering {{test_scenarios}}")
        .with_task_details(
            "## Test Specification\n\n\
            - **Target**: {{test_target}}\n\
            - **Test Framework**: {{test_framework}}\n\
            - **Scenarios**: {{test_scenarios}}\n\
            {{#if mocking}}- **Mocking**: {{mocking_strategy}}\n{{/if}}\n\n\
            ## Test Coverage Requirements\n\n\
            - Happy path scenarios\n\
            - Error conditions and edge cases\n\
            - Boundary value testing\n\
            - Input validation testing\n\
            {{#if async_code}}- Async operation testing\n{{/if}}\
            {{#if performance}}- Performance benchmarks\n{{/if}}\
            - Achieve >90% code coverage for the target",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Testing)
        .with_duration(35)
        .with_variables(vec![
            TemplateVariable::text("test_target", "Function, class, or module to test"),
            TemplateVariable::text("test_scenarios", "Specific scenarios to test"),
            TemplateVariable::choice(
                "test_framework",
                "Testing framework",
                vec![
                    "Jest".to_string(),
                    "Vitest".to_string(),
                    "Mocha".to_string(),
                    "pytest".to_string(),
                    "Rust test".to_string(),
                    "Go test".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "mocking_strategy",
                "Mocking approach",
                vec![
                    "Manual mocks".to_string(),
                    "Jest mocks".to_string(),
                    "Sinon".to_string(),
                    "unittest.mock".to_string(),
                    "mockall".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("async_code", "Testing async operations"),
            TemplateVariable::boolean("performance", "Include performance tests"),
        ])
        .with_target_files(vec![
            "tests/{{test_target}}.test.{{file_extension}}".to_string()
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:security_review (lines 816-872) ---[0m
    pub fn security_review() -> Template {
        Template::new(
            "security-review",
            "Security Review",
            "Conduct a comprehensive security review of code or system",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "security".to_string(),
            "review".to_string(),
            "audit".to_string(),
        ])
        .with_task_description(
            "Conduct security review of {{review_target}} focusing on {{security_aspects}}",
        )
        .with_task_details(
            "## Security Review Scope\n\n\
            - **Target**: {{review_target}}\n\
            - **Focus Areas**: {{security_aspects}}\n\
            - **Review Type**: {{review_type}}\n\n\
            ## Security Checklist\n\n\
            - Authentication and authorization mechanisms\n\
            - Input validation and sanitization\n\
            - SQL injection and XSS vulnerabilities\n\
            - Secure data storage and transmission\n\
            - Error handling and information disclosure\n\
            - Dependencies and supply chain security\n\
            {{#if crypto}}- Cryptographic implementations\n{{/if}}\
            {{#if api_security}}- API security and rate limiting\n{{/if}}\
            {{#if infrastructure}}- Infrastructure security configuration\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Review)
        .with_duration(120)
        .with_variables(vec![
            TemplateVariable::text("review_target", "What is being reviewed"),
            TemplateVariable::text("security_aspects", "Specific security concerns"),
            TemplateVariable::choice(
                "review_type",
                "Type of security review",
                vec![
                    "Code Review".to_string(),
                    "Architecture Review".to_string(),
                    "Penetration Test".to_string(),
                    "Compliance Audit".to_string(),
                ],
            ),
            TemplateVariable::boolean("crypto", "Review cryptographic code"),
            TemplateVariable::boolean("api_security", "Review API security"),
            TemplateVariable::boolean("infrastructure", "Review infrastructure"),
        ])
        .with_target_files(vec![
            "security-review-{{review_target}}.md".to_string(),
            "security-checklist-{{review_target}}.md".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:111-166 method react_hook <-> ./crates/ccswarm/src/template/predefined.rs:313-378 method unit_test
  Similarity: 96.36%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_hook (lines 111-166) ---[0m
    pub fn react_hook() -> Template {
        Template::new(
            "react-hook",
            "React Custom Hook",
            "Create a custom React hook with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "react".to_string(),
            "hook".to_string(),
            "typescript".to_string(),
        ])
        .with_task_description(
            "Create a custom React hook named {{hook_name}} for {{hook_purpose}}",
        )
        .with_task_details(
            "## Hook Specification\n\n\
            - **Name**: {{hook_name}}\n\
            - **Purpose**: {{hook_purpose}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if parameters}}- **Parameters**: {{parameters}}\n{{/if}}\
            {{#if return_type}}- **Returns**: {{return_type}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React hooks rules and conventions\n\
            - Include proper dependency arrays for useEffect\n\
            - Add comprehensive JSDoc comments\n\
            - Handle cleanup and memory leaks\n\
            {{#if testing}}- Create unit tests with React Hooks Testing Library\n{{/if}}\
            {{#if memoization}}- Use useMemo/useCallback for performance optimization\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(30)
        .with_variables(vec![
            TemplateVariable::text(
                "hook_name",
                "Name of the custom hook (e.g., useLocalStorage)",
            ),
            TemplateVariable::text("hook_purpose", "What the hook does"),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the hook",
                "src/hooks/{{hook_name}}.ts",
            ),
            TemplateVariable::text("parameters", "Hook parameters (optional)").optional(),
            TemplateVariable::text("return_type", "Hook return type (optional)").optional(),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("memoization", "Include performance optimizations"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.ts".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:unit_test (lines 313-378) ---[0m
    pub fn unit_test() -> Template {
        Template::new(
            "unit-test",
            "Unit Test",
            "Create comprehensive unit tests for a function or module",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "unit-test".to_string(),
            "quality".to_string(),
        ])
        .with_task_description("Create unit tests for {{test_target}} covering {{test_scenarios}}")
        .with_task_details(
            "## Test Specification\n\n\
            - **Target**: {{test_target}}\n\
            - **Test Framework**: {{test_framework}}\n\
            - **Scenarios**: {{test_scenarios}}\n\
            {{#if mocking}}- **Mocking**: {{mocking_strategy}}\n{{/if}}\n\n\
            ## Test Coverage Requirements\n\n\
            - Happy path scenarios\n\
            - Error conditions and edge cases\n\
            - Boundary value testing\n\
            - Input validation testing\n\
            {{#if async_code}}- Async operation testing\n{{/if}}\
            {{#if performance}}- Performance benchmarks\n{{/if}}\
            - Achieve >90% code coverage for the target",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Testing)
        .with_duration(35)
        .with_variables(vec![
            TemplateVariable::text("test_target", "Function, class, or module to test"),
            TemplateVariable::text("test_scenarios", "Specific scenarios to test"),
            TemplateVariable::choice(
                "test_framework",
                "Testing framework",
                vec![
                    "Jest".to_string(),
                    "Vitest".to_string(),
                    "Mocha".to_string(),
                    "pytest".to_string(),
                    "Rust test".to_string(),
                    "Go test".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "mocking_strategy",
                "Mocking approach",
                vec![
                    "Manual mocks".to_string(),
                    "Jest mocks".to_string(),
                    "Sinon".to_string(),
                    "unittest.mock".to_string(),
                    "mockall".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("async_code", "Testing async operations"),
            TemplateVariable::boolean("performance", "Include performance tests"),
        ])
        .with_target_files(vec![
            "tests/{{test_target}}.test.{{file_extension}}".to_string()
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:562-630 method ci_pipeline <-> ./crates/ccswarm/src/template/predefined.rs:764-813 method rust_cli_command
  Similarity: 97.49%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:ci_pipeline (lines 562-630) ---[0m
    pub fn ci_pipeline() -> Template {
        Template::new(
            "ci-pipeline",
            "CI/CD Pipeline",
            "Create a continuous integration pipeline",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "ci".to_string(),
            "cd".to_string(),
            "pipeline".to_string(),
            "automation".to_string(),
        ])
        .with_task_description("Create {{ci_platform}} pipeline for {{project_type}} project")
        .with_task_details(
            "## CI/CD Pipeline Configuration\n\n\
            - **Platform**: {{ci_platform}}\n\
            - **Project Type**: {{project_type}}\n\
            {{#if triggers}}- **Triggers**: {{triggers}}\n{{/if}}\
            {{#if environments}}- **Environments**: {{environments}}\n{{/if}}\n\n\
            ## Pipeline Stages\n\n\
            - Code checkout and setup\n\
            - Dependency installation\n\
            - Linting and code quality checks\n\
            - Unit and integration tests\n\
            - Build and package\n\
            {{#if security_scan}}- Security vulnerability scanning\n{{/if}}\
            {{#if deployment}}- Automated deployment to {{environments}}\n{{/if}}\
            {{#if notifications}}- Notification on success/failure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(60)
        .with_variables(vec![
            TemplateVariable::choice(
                "ci_platform",
                "CI/CD platform",
                vec![
                    "GitHub Actions".to_string(),
                    "GitLab CI".to_string(),
                    "Jenkins".to_string(),
                    "CircleCI".to_string(),
                    "Azure DevOps".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "project_type",
                "Project type",
                vec![
                    "Node.js".to_string(),
                    "Python".to_string(),
                    "Rust".to_string(),
                    "Go".to_string(),
                    "Java".to_string(),
                    "Docker".to_string(),
                ],
            ),
            TemplateVariable::text("triggers", "Pipeline triggers (e.g., push, PR)").optional(),
            TemplateVariable::text("environments", "Deployment environments").optional(),
            TemplateVariable::boolean("security_scan", "Include security scanning"),
            TemplateVariable::boolean("deployment", "Include deployment stages"),
            TemplateVariable::boolean("notifications", "Set up notifications"),
        ])
        .with_target_files(vec![
            ".github/workflows/ci.yml".to_string(),
            ".github/workflows/cd.yml".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_cli_command (lines 764-813) ---[0m
    pub fn rust_cli_command() -> Template {
        Template::new(
            "rust-cli-command",
            "Rust CLI Command",
            "Add a new command to a Rust CLI application using clap",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "cli".to_string(),
            "clap".to_string(),
            "command".to_string(),
        ])
        .with_task_description("Add CLI command {{command_name}} to {{command_description}}")
        .with_task_details(
            "## CLI Command Specification\n\n\
            - **Command**: {{command_name}}\n\
            - **Description**: {{command_description}}\n\
            {{#if arguments}}- **Arguments**: {{arguments}}\n{{/if}}\
            {{#if options}}- **Options**: {{options}}\n{{/if}}\
            {{#if subcommands}}- **Subcommands**: {{subcommands}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use clap derive macros for argument parsing\n\
            - Implement proper error handling and exit codes\n\
            - Add comprehensive help text and examples\n\
            - Include input validation\n\
            - Support common CLI patterns (--help, --version)\n\
            {{#if config_file}}- Support configuration file loading\n{{/if}}\
            {{#if logging}}- Integrate with logging framework\n{{/if}}\
            {{#if testing}}- Add integration tests for CLI\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(50)
        .with_variables(vec![
            TemplateVariable::text("command_name", "Name of the CLI command"),
            TemplateVariable::text("command_description", "What the command does"),
            TemplateVariable::text("arguments", "Required arguments").optional(),
            TemplateVariable::text("options", "Optional flags and options").optional(),
            TemplateVariable::text("subcommands", "Subcommands if any").optional(),
            TemplateVariable::boolean("config_file", "Support config files"),
            TemplateVariable::boolean("logging", "Add logging support"),
            TemplateVariable::boolean("testing", "Create CLI tests"),
        ])
        .with_target_files(vec![
            "src/cli/{{command_name}}.rs".to_string(),
            "tests/cli/{{command_name}}_test.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:313-378 method unit_test <-> ./crates/ccswarm/src/template/predefined.rs:875-928 method performance_optimization
  Similarity: 96.29%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:unit_test (lines 313-378) ---[0m
    pub fn unit_test() -> Template {
        Template::new(
            "unit-test",
            "Unit Test",
            "Create comprehensive unit tests for a function or module",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "unit-test".to_string(),
            "quality".to_string(),
        ])
        .with_task_description("Create unit tests for {{test_target}} covering {{test_scenarios}}")
        .with_task_details(
            "## Test Specification\n\n\
            - **Target**: {{test_target}}\n\
            - **Test Framework**: {{test_framework}}\n\
            - **Scenarios**: {{test_scenarios}}\n\
            {{#if mocking}}- **Mocking**: {{mocking_strategy}}\n{{/if}}\n\n\
            ## Test Coverage Requirements\n\n\
            - Happy path scenarios\n\
            - Error conditions and edge cases\n\
            - Boundary value testing\n\
            - Input validation testing\n\
            {{#if async_code}}- Async operation testing\n{{/if}}\
            {{#if performance}}- Performance benchmarks\n{{/if}}\
            - Achieve >90% code coverage for the target",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Testing)
        .with_duration(35)
        .with_variables(vec![
            TemplateVariable::text("test_target", "Function, class, or module to test"),
            TemplateVariable::text("test_scenarios", "Specific scenarios to test"),
            TemplateVariable::choice(
                "test_framework",
                "Testing framework",
                vec![
                    "Jest".to_string(),
                    "Vitest".to_string(),
                    "Mocha".to_string(),
                    "pytest".to_string(),
                    "Rust test".to_string(),
                    "Go test".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "mocking_strategy",
                "Mocking approach",
                vec![
                    "Manual mocks".to_string(),
                    "Jest mocks".to_string(),
                    "Sinon".to_string(),
                    "unittest.mock".to_string(),
                    "mockall".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("async_code", "Testing async operations"),
            TemplateVariable::boolean("performance", "Include performance tests"),
        ])
        .with_target_files(vec![
            "tests/{{test_target}}.test.{{file_extension}}".to_string()
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:performance_optimization (lines 875-928) ---[0m
    pub fn performance_optimization() -> Template {
        Template::new(
            "performance-optimization",
            "Performance Optimization",
            "Optimize performance of a system component",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "performance".to_string(),
            "optimization".to_string(),
            "profiling".to_string(),
        ])
        .with_task_description(
            "Optimize performance of {{optimization_target}} to improve {{performance_metric}}",
        )
        .with_task_details(
            "## Performance Optimization Plan\n\n\
            - **Target**: {{optimization_target}}\n\
            - **Metric**: {{performance_metric}}\n\
            - **Current Performance**: {{current_baseline}}\n\
            - **Target Performance**: {{target_goal}}\n\n\
            ## Optimization Approach\n\n\
            - Profile current performance with appropriate tools\n\
            - Identify bottlenecks and hot paths\n\
            - Implement targeted optimizations\n\
            - Measure impact of each optimization\n\
            - Verify no functional regressions\n\
            {{#if caching}}- Implement caching strategies\n{{/if}}\
            {{#if database}}- Optimize database queries and indexes\n{{/if}}\
            {{#if memory}}- Reduce memory allocation and fragmentation\n{{/if}}\
            {{#if concurrency}}- Improve concurrency and parallelization\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("optimization_target", "Component to optimize"),
            TemplateVariable::text(
                "performance_metric",
                "Metric to improve (latency, throughput, etc.)",
            ),
            TemplateVariable::text("current_baseline", "Current performance baseline").optional(),
            TemplateVariable::text("target_goal", "Performance improvement goal").optional(),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("database", "Optimize database operations"),
            TemplateVariable::boolean("memory", "Optimize memory usage"),
            TemplateVariable::boolean("concurrency", "Improve concurrency"),
        ])
        .with_target_files(vec![
            "performance-analysis-{{optimization_target}}.md".to_string(),
            "benchmarks/{{optimization_target}}_bench.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:633-701 method documentation_page <-> ./crates/ccswarm/src/template/predefined.rs:764-813 method rust_cli_command
  Similarity: 96.53%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:documentation_page (lines 633-701) ---[0m
    pub fn documentation_page() -> Template {
        Template::new(
            "documentation-page",
            "Documentation Page",
            "Create comprehensive documentation for a feature or API",
            TemplateCategory::Documentation,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "documentation".to_string(),
            "docs".to_string(),
            "guide".to_string(),
        ])
        .with_task_description(
            "Create documentation for {{doc_subject}} explaining {{doc_purpose}}",
        )
        .with_task_details(
            "## Documentation Specification\n\n\
            - **Subject**: {{doc_subject}}\n\
            - **Purpose**: {{doc_purpose}}\n\
            - **Audience**: {{target_audience}}\n\
            {{#if doc_type}}- **Type**: {{doc_type}}\n{{/if}}\n\n\
            ## Content Requirements\n\n\
            - Clear overview and introduction\n\
            - Step-by-step instructions or usage guide\n\
            - Code examples with explanations\n\
            - Common pitfalls and troubleshooting\n\
            - Links to related documentation\n\
            {{#if api_reference}}- Complete API reference with parameters\n{{/if}}\
            {{#if diagrams}}- Architecture or flow diagrams\n{{/if}}\
            {{#if examples}}- Real-world usage examples\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Documentation)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("doc_subject", "What is being documented"),
            TemplateVariable::text("doc_purpose", "Purpose of the documentation"),
            TemplateVariable::choice(
                "target_audience",
                "Target audience",
                vec![
                    "Developers".to_string(),
                    "Users".to_string(),
                    "System Administrators".to_string(),
                    "Product Managers".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "doc_type",
                "Documentation type",
                vec![
                    "API Reference".to_string(),
                    "User Guide".to_string(),
                    "Tutorial".to_string(),
                    "Architecture Guide".to_string(),
                    "Troubleshooting".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("api_reference", "Include API reference"),
            TemplateVariable::boolean("diagrams", "Include diagrams"),
            TemplateVariable::boolean("examples", "Include code examples"),
        ])
        .with_target_files(vec![
            "docs/{{doc_subject}}.md".to_string(),
            "examples/{{doc_subject}}_example.{{file_extension}}".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_cli_command (lines 764-813) ---[0m
    pub fn rust_cli_command() -> Template {
        Template::new(
            "rust-cli-command",
            "Rust CLI Command",
            "Add a new command to a Rust CLI application using clap",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "cli".to_string(),
            "clap".to_string(),
            "command".to_string(),
        ])
        .with_task_description("Add CLI command {{command_name}} to {{command_description}}")
        .with_task_details(
            "## CLI Command Specification\n\n\
            - **Command**: {{command_name}}\n\
            - **Description**: {{command_description}}\n\
            {{#if arguments}}- **Arguments**: {{arguments}}\n{{/if}}\
            {{#if options}}- **Options**: {{options}}\n{{/if}}\
            {{#if subcommands}}- **Subcommands**: {{subcommands}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use clap derive macros for argument parsing\n\
            - Implement proper error handling and exit codes\n\
            - Add comprehensive help text and examples\n\
            - Include input validation\n\
            - Support common CLI patterns (--help, --version)\n\
            {{#if config_file}}- Support configuration file loading\n{{/if}}\
            {{#if logging}}- Integrate with logging framework\n{{/if}}\
            {{#if testing}}- Add integration tests for CLI\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(50)
        .with_variables(vec![
            TemplateVariable::text("command_name", "Name of the CLI command"),
            TemplateVariable::text("command_description", "What the command does"),
            TemplateVariable::text("arguments", "Required arguments").optional(),
            TemplateVariable::text("options", "Optional flags and options").optional(),
            TemplateVariable::text("subcommands", "Subcommands if any").optional(),
            TemplateVariable::boolean("config_file", "Support config files"),
            TemplateVariable::boolean("logging", "Add logging support"),
            TemplateVariable::boolean("testing", "Create CLI tests"),
        ])
        .with_target_files(vec![
            "src/cli/{{command_name}}.rs".to_string(),
            "tests/cli/{{command_name}}_test.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:381-439 method integration_test <-> ./crates/ccswarm/src/template/predefined.rs:501-559 method docker_service
  Similarity: 96.87%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:integration_test (lines 381-439) ---[0m
    pub fn integration_test() -> Template {
        Template::new(
            "integration-test",
            "Integration Test",
            "Create integration tests for system components",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "integration".to_string(),
            "e2e".to_string(),
        ])
        .with_task_description(
            "Create integration tests for {{system_component}} testing {{integration_points}}",
        )
        .with_task_details(
            "## Integration Test Specification\n\n\
            - **Component**: {{system_component}}\n\
            - **Integration Points**: {{integration_points}}\n\
            - **Test Environment**: {{test_environment}}\n\
            {{#if database}}- **Database**: Test database setup required\n{{/if}}\
            {{#if external_services}}- **External Services**: {{external_services}}\n{{/if}}\n\n\
            ## Test Requirements\n\n\
            - Set up test environment and data\n\
            - Test end-to-end workflows\n\
            - Verify data consistency across components\n\
            - Test error propagation and recovery\n\
            - Clean up test data after execution\n\
            {{#if performance}}- Measure integration performance\n{{/if}}\
            {{#if security}}- Verify security boundaries\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Testing)
        .with_duration(75)
        .with_variables(vec![
            TemplateVariable::text("system_component", "System component being tested"),
            TemplateVariable::text("integration_points", "What integrations to test"),
            TemplateVariable::choice(
                "test_environment",
                "Test environment",
                vec![
                    "Docker Compose".to_string(),
                    "Testcontainers".to_string(),
                    "In-memory".to_string(),
                    "Staging".to_string(),
                ],
            ),
            TemplateVariable::boolean("database", "Includes database testing"),
            TemplateVariable::text("external_services", "External services involved").optional(),
            TemplateVariable::boolean("performance", "Include performance testing"),
            TemplateVariable::boolean("security", "Include security testing"),
        ])
        .with_target_files(vec![
            "tests/integration/{{system_component}}_integration.test.{{file_extension}}"
                .to_string(),
            "tests/fixtures/{{system_component}}_data.json".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:docker_service (lines 501-559) ---[0m
    pub fn docker_service() -> Template {
        Template::new(
            "docker-service",
            "Docker Service",
            "Create a Docker container configuration for a service",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "docker".to_string(),
            "container".to_string(),
            "devops".to_string(),
        ])
        .with_task_description("Create Docker configuration for {{service_name}} service")
        .with_task_details(
            "## Docker Service Configuration\n\n\
            - **Service**: {{service_name}}\n\
            - **Base Image**: {{base_image}}\n\
            {{#if ports}}- **Ports**: {{ports}}\n{{/if}}\
            {{#if volumes}}- **Volumes**: {{volumes}}\n{{/if}}\
            {{#if environment}}- **Environment**: {{environment}}\n{{/if}}\n\n\
            ## Container Requirements\n\n\
            - Multi-stage build for optimization\n\
            - Non-root user for security\n\
            - Health check configuration\n\
            - Proper signal handling\n\
            - Minimal attack surface\n\
            {{#if secrets}}- Secure secret management\n{{/if}}\
            {{#if monitoring}}- Monitoring and logging setup\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("service_name", "Name of the service"),
            TemplateVariable::choice(
                "base_image",
                "Base Docker image",
                vec![
                    "alpine".to_string(),
                    "ubuntu".to_string(),
                    "node:alpine".to_string(),
                    "python:alpine".to_string(),
                    "rust:alpine".to_string(),
                    "nginx:alpine".to_string(),
                ],
            ),
            TemplateVariable::text("ports", "Port mappings (e.g., 8080:8080)").optional(),
            TemplateVariable::text("volumes", "Volume mounts").optional(),
            TemplateVariable::text("environment", "Environment variables").optional(),
            TemplateVariable::boolean("secrets", "Include secret management"),
            TemplateVariable::boolean("monitoring", "Add monitoring setup"),
        ])
        .with_target_files(vec![
            "Dockerfile".to_string(),
            "docker-compose.yml".to_string(),
            ".dockerignore".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:253-310 method database_migration <-> ./crates/ccswarm/src/template/predefined.rs:501-559 method docker_service
  Similarity: 97.40%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:database_migration (lines 253-310) ---[0m
    pub fn database_migration() -> Template {
        Template::new(
            "database-migration",
            "Database Migration",
            "Create a database migration script",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "database".to_string(),
            "migration".to_string(),
            "schema".to_string(),
        ])
        .with_task_description("Create database migration to {{migration_purpose}}")
        .with_task_details(
            "## Migration Details\n\n\
            - **Purpose**: {{migration_purpose}}\n\
            - **Type**: {{migration_type}}\n\
            {{#if table_name}}- **Table**: {{table_name}}\n{{/if}}\
            {{#if columns}}- **Columns**: {{columns}}\n{{/if}}\
            {{#if indexes}}- **Indexes**: {{indexes}}\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Write both up and down migrations\n\
            - Include proper constraints and indexes\n\
            - Handle existing data appropriately\n\
            - Add comments for complex operations\n\
            - Test migration on sample data\n\
            {{#if backup}}- Create backup before running\n{{/if}}\
            {{#if rollback_plan}}- Document rollback procedure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(40)
        .with_variables(vec![
            TemplateVariable::text("migration_purpose", "What this migration accomplishes"),
            TemplateVariable::choice(
                "migration_type",
                "Type of migration",
                vec![
                    "Create Table".to_string(),
                    "Alter Table".to_string(),
                    "Drop Table".to_string(),
                    "Add Column".to_string(),
                    "Drop Column".to_string(),
                    "Create Index".to_string(),
                    "Data Migration".to_string(),
                ],
            ),
            TemplateVariable::text("table_name", "Table name (if applicable)").optional(),
            TemplateVariable::text("columns", "Column definitions (optional)").optional(),
            TemplateVariable::text("indexes", "Index definitions (optional)").optional(),
            TemplateVariable::boolean("backup", "Create backup before migration"),
            TemplateVariable::boolean("rollback_plan", "Document rollback procedure"),
        ])
        .with_target_files(vec![
            "migrations/{{current_datetime}}_{{migration_purpose}}.sql".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:docker_service (lines 501-559) ---[0m
    pub fn docker_service() -> Template {
        Template::new(
            "docker-service",
            "Docker Service",
            "Create a Docker container configuration for a service",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "docker".to_string(),
            "container".to_string(),
            "devops".to_string(),
        ])
        .with_task_description("Create Docker configuration for {{service_name}} service")
        .with_task_details(
            "## Docker Service Configuration\n\n\
            - **Service**: {{service_name}}\n\
            - **Base Image**: {{base_image}}\n\
            {{#if ports}}- **Ports**: {{ports}}\n{{/if}}\
            {{#if volumes}}- **Volumes**: {{volumes}}\n{{/if}}\
            {{#if environment}}- **Environment**: {{environment}}\n{{/if}}\n\n\
            ## Container Requirements\n\n\
            - Multi-stage build for optimization\n\
            - Non-root user for security\n\
            - Health check configuration\n\
            - Proper signal handling\n\
            - Minimal attack surface\n\
            {{#if secrets}}- Secure secret management\n{{/if}}\
            {{#if monitoring}}- Monitoring and logging setup\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("service_name", "Name of the service"),
            TemplateVariable::choice(
                "base_image",
                "Base Docker image",
                vec![
                    "alpine".to_string(),
                    "ubuntu".to_string(),
                    "node:alpine".to_string(),
                    "python:alpine".to_string(),
                    "rust:alpine".to_string(),
                    "nginx:alpine".to_string(),
                ],
            ),
            TemplateVariable::text("ports", "Port mappings (e.g., 8080:8080)").optional(),
            TemplateVariable::text("volumes", "Volume mounts").optional(),
            TemplateVariable::text("environment", "Environment variables").optional(),
            TemplateVariable::boolean("secrets", "Include secret management"),
            TemplateVariable::boolean("monitoring", "Add monitoring setup"),
        ])
        .with_target_files(vec![
            "Dockerfile".to_string(),
            "docker-compose.yml".to_string(),
            ".dockerignore".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:501-559 method docker_service <-> ./crates/ccswarm/src/template/predefined.rs:704-761 method rust_module
  Similarity: 97.33%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:docker_service (lines 501-559) ---[0m
    pub fn docker_service() -> Template {
        Template::new(
            "docker-service",
            "Docker Service",
            "Create a Docker container configuration for a service",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "docker".to_string(),
            "container".to_string(),
            "devops".to_string(),
        ])
        .with_task_description("Create Docker configuration for {{service_name}} service")
        .with_task_details(
            "## Docker Service Configuration\n\n\
            - **Service**: {{service_name}}\n\
            - **Base Image**: {{base_image}}\n\
            {{#if ports}}- **Ports**: {{ports}}\n{{/if}}\
            {{#if volumes}}- **Volumes**: {{volumes}}\n{{/if}}\
            {{#if environment}}- **Environment**: {{environment}}\n{{/if}}\n\n\
            ## Container Requirements\n\n\
            - Multi-stage build for optimization\n\
            - Non-root user for security\n\
            - Health check configuration\n\
            - Proper signal handling\n\
            - Minimal attack surface\n\
            {{#if secrets}}- Secure secret management\n{{/if}}\
            {{#if monitoring}}- Monitoring and logging setup\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("service_name", "Name of the service"),
            TemplateVariable::choice(
                "base_image",
                "Base Docker image",
                vec![
                    "alpine".to_string(),
                    "ubuntu".to_string(),
                    "node:alpine".to_string(),
                    "python:alpine".to_string(),
                    "rust:alpine".to_string(),
                    "nginx:alpine".to_string(),
                ],
            ),
            TemplateVariable::text("ports", "Port mappings (e.g., 8080:8080)").optional(),
            TemplateVariable::text("volumes", "Volume mounts").optional(),
            TemplateVariable::text("environment", "Environment variables").optional(),
            TemplateVariable::boolean("secrets", "Include secret management"),
            TemplateVariable::boolean("monitoring", "Add monitoring setup"),
        ])
        .with_target_files(vec![
            "Dockerfile".to_string(),
            "docker-compose.yml".to_string(),
            ".dockerignore".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_module (lines 704-761) ---[0m
    pub fn rust_module() -> Template {
        Template::new(
            "rust-module",
            "Rust Module",
            "Create a new Rust module with proper structure and documentation",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "module".to_string(),
            "backend".to_string(),
        ])
        .with_task_description("Create Rust module {{module_name}} for {{module_purpose}}")
        .with_task_details(
            "## Rust Module Specification\n\n\
            - **Module**: {{module_name}}\n\
            - **Purpose**: {{module_purpose}}\n\
            - **Visibility**: {{visibility}}\n\
            {{#if traits}}- **Traits**: {{traits}}\n{{/if}}\
            {{#if structs}}- **Structs**: {{structs}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Follow Rust naming conventions\n\
            - Add comprehensive rustdoc comments\n\
            - Implement proper error handling with Result<T, E>\n\
            - Use appropriate visibility modifiers\n\
            - Include unit tests in #[cfg(test)] module\n\
            {{#if async_support}}- Implement async/await patterns\n{{/if}}\
            {{#if serde_support}}- Add Serde serialization support\n{{/if}}\
            {{#if benchmark}}- Include criterion benchmarks\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("module_name", "Name of the Rust module"),
            TemplateVariable::text("module_purpose", "What this module does"),
            TemplateVariable::choice(
                "visibility",
                "Module visibility",
                vec![
                    "public".to_string(),
                    "private".to_string(),
                    "pub(crate)".to_string(),
                ],
            ),
            TemplateVariable::text("traits", "Traits to implement").optional(),
            TemplateVariable::text("structs", "Main structs in the module").optional(),
            TemplateVariable::boolean("async_support", "Include async support"),
            TemplateVariable::boolean("serde_support", "Add Serde support"),
            TemplateVariable::boolean("benchmark", "Include benchmarks"),
        ])
        .with_target_files(vec![
            "src/{{module_name}}/mod.rs".to_string(),
            "src/{{module_name}}/types.rs".to_string(),
            "benches/{{module_name}}_bench.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:253-310 method database_migration <-> ./crates/ccswarm/src/template/predefined.rs:381-439 method integration_test
  Similarity: 96.87%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:database_migration (lines 253-310) ---[0m
    pub fn database_migration() -> Template {
        Template::new(
            "database-migration",
            "Database Migration",
            "Create a database migration script",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "database".to_string(),
            "migration".to_string(),
            "schema".to_string(),
        ])
        .with_task_description("Create database migration to {{migration_purpose}}")
        .with_task_details(
            "## Migration Details\n\n\
            - **Purpose**: {{migration_purpose}}\n\
            - **Type**: {{migration_type}}\n\
            {{#if table_name}}- **Table**: {{table_name}}\n{{/if}}\
            {{#if columns}}- **Columns**: {{columns}}\n{{/if}}\
            {{#if indexes}}- **Indexes**: {{indexes}}\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Write both up and down migrations\n\
            - Include proper constraints and indexes\n\
            - Handle existing data appropriately\n\
            - Add comments for complex operations\n\
            - Test migration on sample data\n\
            {{#if backup}}- Create backup before running\n{{/if}}\
            {{#if rollback_plan}}- Document rollback procedure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(40)
        .with_variables(vec![
            TemplateVariable::text("migration_purpose", "What this migration accomplishes"),
            TemplateVariable::choice(
                "migration_type",
                "Type of migration",
                vec![
                    "Create Table".to_string(),
                    "Alter Table".to_string(),
                    "Drop Table".to_string(),
                    "Add Column".to_string(),
                    "Drop Column".to_string(),
                    "Create Index".to_string(),
                    "Data Migration".to_string(),
                ],
            ),
            TemplateVariable::text("table_name", "Table name (if applicable)").optional(),
            TemplateVariable::text("columns", "Column definitions (optional)").optional(),
            TemplateVariable::text("indexes", "Index definitions (optional)").optional(),
            TemplateVariable::boolean("backup", "Create backup before migration"),
            TemplateVariable::boolean("rollback_plan", "Document rollback procedure"),
        ])
        .with_target_files(vec![
            "migrations/{{current_datetime}}_{{migration_purpose}}.sql".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:integration_test (lines 381-439) ---[0m
    pub fn integration_test() -> Template {
        Template::new(
            "integration-test",
            "Integration Test",
            "Create integration tests for system components",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "integration".to_string(),
            "e2e".to_string(),
        ])
        .with_task_description(
            "Create integration tests for {{system_component}} testing {{integration_points}}",
        )
        .with_task_details(
            "## Integration Test Specification\n\n\
            - **Component**: {{system_component}}\n\
            - **Integration Points**: {{integration_points}}\n\
            - **Test Environment**: {{test_environment}}\n\
            {{#if database}}- **Database**: Test database setup required\n{{/if}}\
            {{#if external_services}}- **External Services**: {{external_services}}\n{{/if}}\n\n\
            ## Test Requirements\n\n\
            - Set up test environment and data\n\
            - Test end-to-end workflows\n\
            - Verify data consistency across components\n\
            - Test error propagation and recovery\n\
            - Clean up test data after execution\n\
            {{#if performance}}- Measure integration performance\n{{/if}}\
            {{#if security}}- Verify security boundaries\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Testing)
        .with_duration(75)
        .with_variables(vec![
            TemplateVariable::text("system_component", "System component being tested"),
            TemplateVariable::text("integration_points", "What integrations to test"),
            TemplateVariable::choice(
                "test_environment",
                "Test environment",
                vec![
                    "Docker Compose".to_string(),
                    "Testcontainers".to_string(),
                    "In-memory".to_string(),
                    "Staging".to_string(),
                ],
            ),
            TemplateVariable::boolean("database", "Includes database testing"),
            TemplateVariable::text("external_services", "External services involved").optional(),
            TemplateVariable::boolean("performance", "Include performance testing"),
            TemplateVariable::boolean("security", "Include security testing"),
        ])
        .with_target_files(vec![
            "tests/integration/{{system_component}}_integration.test.{{file_extension}}"
                .to_string(),
            "tests/fixtures/{{system_component}}_data.json".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:381-439 method integration_test <-> ./crates/ccswarm/src/template/predefined.rs:442-498 method bug_fix
  Similarity: 97.56%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:integration_test (lines 381-439) ---[0m
    pub fn integration_test() -> Template {
        Template::new(
            "integration-test",
            "Integration Test",
            "Create integration tests for system components",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "integration".to_string(),
            "e2e".to_string(),
        ])
        .with_task_description(
            "Create integration tests for {{system_component}} testing {{integration_points}}",
        )
        .with_task_details(
            "## Integration Test Specification\n\n\
            - **Component**: {{system_component}}\n\
            - **Integration Points**: {{integration_points}}\n\
            - **Test Environment**: {{test_environment}}\n\
            {{#if database}}- **Database**: Test database setup required\n{{/if}}\
            {{#if external_services}}- **External Services**: {{external_services}}\n{{/if}}\n\n\
            ## Test Requirements\n\n\
            - Set up test environment and data\n\
            - Test end-to-end workflows\n\
            - Verify data consistency across components\n\
            - Test error propagation and recovery\n\
            - Clean up test data after execution\n\
            {{#if performance}}- Measure integration performance\n{{/if}}\
            {{#if security}}- Verify security boundaries\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Testing)
        .with_duration(75)
        .with_variables(vec![
            TemplateVariable::text("system_component", "System component being tested"),
            TemplateVariable::text("integration_points", "What integrations to test"),
            TemplateVariable::choice(
                "test_environment",
                "Test environment",
                vec![
                    "Docker Compose".to_string(),
                    "Testcontainers".to_string(),
                    "In-memory".to_string(),
                    "Staging".to_string(),
                ],
            ),
            TemplateVariable::boolean("database", "Includes database testing"),
            TemplateVariable::text("external_services", "External services involved").optional(),
            TemplateVariable::boolean("performance", "Include performance testing"),
            TemplateVariable::boolean("security", "Include security testing"),
        ])
        .with_target_files(vec![
            "tests/integration/{{system_component}}_integration.test.{{file_extension}}"
                .to_string(),
            "tests/fixtures/{{system_component}}_data.json".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:bug_fix (lines 442-498) ---[0m
    pub fn bug_fix() -> Template {
        Template::new(
            "bug-fix",
            "Bug Fix",
            "Fix a reported bug with proper testing and documentation",
            TemplateCategory::BugFix,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "bug".to_string(),
            "fix".to_string(),
            "maintenance".to_string(),
        ])
        .with_task_description("Fix bug: {{bug_description}} ({{bug_severity}})")
        .with_task_details(
            "## Bug Report\n\n\
            - **Description**: {{bug_description}}\n\
            - **Severity**: {{bug_severity}}\n\
            {{#if reproduction_steps}}- **Reproduction**: {{reproduction_steps}}\n{{/if}}\
            {{#if affected_components}}- **Affected Components**: {{affected_components}}\n{{/if}}\
            {{#if user_impact}}- **User Impact**: {{user_impact}}\n{{/if}}\n\n\
            ## Fix Requirements\n\n\
            - Reproduce the bug in a test environment\n\
            - Identify root cause through debugging\n\
            - Implement minimal fix that addresses root cause\n\
            - Add regression tests to prevent recurrence\n\
            - Verify fix doesn't introduce new issues\n\
            {{#if documentation}}- Update relevant documentation\n{{/if}}\
            {{#if release_notes}}- Add to release notes\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Bugfix)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("bug_description", "Clear description of the bug"),
            TemplateVariable::choice(
                "bug_severity",
                "Bug severity level",
                vec![
                    "Critical".to_string(),
                    "High".to_string(),
                    "Medium".to_string(),
                    "Low".to_string(),
                ],
            ),
            TemplateVariable::text("reproduction_steps", "Steps to reproduce the bug").optional(),
            TemplateVariable::text("affected_components", "Components affected by the bug")
                .optional(),
            TemplateVariable::text("user_impact", "How this affects users").optional(),
            TemplateVariable::boolean("documentation", "Update documentation"),
            TemplateVariable::boolean("release_notes", "Add to release notes"),
        ])
        .with_target_files(vec![
            "src/{{affected_components}}/fix.{{file_extension}}".to_string(),
            "tests/regression/{{bug_description}}.test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:442-498 method bug_fix <-> ./crates/ccswarm/src/template/predefined.rs:501-559 method docker_service
  Similarity: 97.33%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:bug_fix (lines 442-498) ---[0m
    pub fn bug_fix() -> Template {
        Template::new(
            "bug-fix",
            "Bug Fix",
            "Fix a reported bug with proper testing and documentation",
            TemplateCategory::BugFix,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "bug".to_string(),
            "fix".to_string(),
            "maintenance".to_string(),
        ])
        .with_task_description("Fix bug: {{bug_description}} ({{bug_severity}})")
        .with_task_details(
            "## Bug Report\n\n\
            - **Description**: {{bug_description}}\n\
            - **Severity**: {{bug_severity}}\n\
            {{#if reproduction_steps}}- **Reproduction**: {{reproduction_steps}}\n{{/if}}\
            {{#if affected_components}}- **Affected Components**: {{affected_components}}\n{{/if}}\
            {{#if user_impact}}- **User Impact**: {{user_impact}}\n{{/if}}\n\n\
            ## Fix Requirements\n\n\
            - Reproduce the bug in a test environment\n\
            - Identify root cause through debugging\n\
            - Implement minimal fix that addresses root cause\n\
            - Add regression tests to prevent recurrence\n\
            - Verify fix doesn't introduce new issues\n\
            {{#if documentation}}- Update relevant documentation\n{{/if}}\
            {{#if release_notes}}- Add to release notes\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Bugfix)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("bug_description", "Clear description of the bug"),
            TemplateVariable::choice(
                "bug_severity",
                "Bug severity level",
                vec![
                    "Critical".to_string(),
                    "High".to_string(),
                    "Medium".to_string(),
                    "Low".to_string(),
                ],
            ),
            TemplateVariable::text("reproduction_steps", "Steps to reproduce the bug").optional(),
            TemplateVariable::text("affected_components", "Components affected by the bug")
                .optional(),
            TemplateVariable::text("user_impact", "How this affects users").optional(),
            TemplateVariable::boolean("documentation", "Update documentation"),
            TemplateVariable::boolean("release_notes", "Add to release notes"),
        ])
        .with_target_files(vec![
            "src/{{affected_components}}/fix.{{file_extension}}".to_string(),
            "tests/regression/{{bug_description}}.test.{{file_extension}}".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:docker_service (lines 501-559) ---[0m
    pub fn docker_service() -> Template {
        Template::new(
            "docker-service",
            "Docker Service",
            "Create a Docker container configuration for a service",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "docker".to_string(),
            "container".to_string(),
            "devops".to_string(),
        ])
        .with_task_description("Create Docker configuration for {{service_name}} service")
        .with_task_details(
            "## Docker Service Configuration\n\n\
            - **Service**: {{service_name}}\n\
            - **Base Image**: {{base_image}}\n\
            {{#if ports}}- **Ports**: {{ports}}\n{{/if}}\
            {{#if volumes}}- **Volumes**: {{volumes}}\n{{/if}}\
            {{#if environment}}- **Environment**: {{environment}}\n{{/if}}\n\n\
            ## Container Requirements\n\n\
            - Multi-stage build for optimization\n\
            - Non-root user for security\n\
            - Health check configuration\n\
            - Proper signal handling\n\
            - Minimal attack surface\n\
            {{#if secrets}}- Secure secret management\n{{/if}}\
            {{#if monitoring}}- Monitoring and logging setup\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("service_name", "Name of the service"),
            TemplateVariable::choice(
                "base_image",
                "Base Docker image",
                vec![
                    "alpine".to_string(),
                    "ubuntu".to_string(),
                    "node:alpine".to_string(),
                    "python:alpine".to_string(),
                    "rust:alpine".to_string(),
                    "nginx:alpine".to_string(),
                ],
            ),
            TemplateVariable::text("ports", "Port mappings (e.g., 8080:8080)").optional(),
            TemplateVariable::text("volumes", "Volume mounts").optional(),
            TemplateVariable::text("environment", "Environment variables").optional(),
            TemplateVariable::boolean("secrets", "Include secret management"),
            TemplateVariable::boolean("monitoring", "Add monitoring setup"),
        ])
        .with_target_files(vec![
            "Dockerfile".to_string(),
            "docker-compose.yml".to_string(),
            ".dockerignore".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:381-439 method integration_test <-> ./crates/ccswarm/src/template/predefined.rs:816-872 method security_review
  Similarity: 97.14%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:integration_test (lines 381-439) ---[0m
    pub fn integration_test() -> Template {
        Template::new(
            "integration-test",
            "Integration Test",
            "Create integration tests for system components",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "integration".to_string(),
            "e2e".to_string(),
        ])
        .with_task_description(
            "Create integration tests for {{system_component}} testing {{integration_points}}",
        )
        .with_task_details(
            "## Integration Test Specification\n\n\
            - **Component**: {{system_component}}\n\
            - **Integration Points**: {{integration_points}}\n\
            - **Test Environment**: {{test_environment}}\n\
            {{#if database}}- **Database**: Test database setup required\n{{/if}}\
            {{#if external_services}}- **External Services**: {{external_services}}\n{{/if}}\n\n\
            ## Test Requirements\n\n\
            - Set up test environment and data\n\
            - Test end-to-end workflows\n\
            - Verify data consistency across components\n\
            - Test error propagation and recovery\n\
            - Clean up test data after execution\n\
            {{#if performance}}- Measure integration performance\n{{/if}}\
            {{#if security}}- Verify security boundaries\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Testing)
        .with_duration(75)
        .with_variables(vec![
            TemplateVariable::text("system_component", "System component being tested"),
            TemplateVariable::text("integration_points", "What integrations to test"),
            TemplateVariable::choice(
                "test_environment",
                "Test environment",
                vec![
                    "Docker Compose".to_string(),
                    "Testcontainers".to_string(),
                    "In-memory".to_string(),
                    "Staging".to_string(),
                ],
            ),
            TemplateVariable::boolean("database", "Includes database testing"),
            TemplateVariable::text("external_services", "External services involved").optional(),
            TemplateVariable::boolean("performance", "Include performance testing"),
            TemplateVariable::boolean("security", "Include security testing"),
        ])
        .with_target_files(vec![
            "tests/integration/{{system_component}}_integration.test.{{file_extension}}"
                .to_string(),
            "tests/fixtures/{{system_component}}_data.json".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:security_review (lines 816-872) ---[0m
    pub fn security_review() -> Template {
        Template::new(
            "security-review",
            "Security Review",
            "Conduct a comprehensive security review of code or system",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "security".to_string(),
            "review".to_string(),
            "audit".to_string(),
        ])
        .with_task_description(
            "Conduct security review of {{review_target}} focusing on {{security_aspects}}",
        )
        .with_task_details(
            "## Security Review Scope\n\n\
            - **Target**: {{review_target}}\n\
            - **Focus Areas**: {{security_aspects}}\n\
            - **Review Type**: {{review_type}}\n\n\
            ## Security Checklist\n\n\
            - Authentication and authorization mechanisms\n\
            - Input validation and sanitization\n\
            - SQL injection and XSS vulnerabilities\n\
            - Secure data storage and transmission\n\
            - Error handling and information disclosure\n\
            - Dependencies and supply chain security\n\
            {{#if crypto}}- Cryptographic implementations\n{{/if}}\
            {{#if api_security}}- API security and rate limiting\n{{/if}}\
            {{#if infrastructure}}- Infrastructure security configuration\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Review)
        .with_duration(120)
        .with_variables(vec![
            TemplateVariable::text("review_target", "What is being reviewed"),
            TemplateVariable::text("security_aspects", "Specific security concerns"),
            TemplateVariable::choice(
                "review_type",
                "Type of security review",
                vec![
                    "Code Review".to_string(),
                    "Architecture Review".to_string(),
                    "Penetration Test".to_string(),
                    "Compliance Audit".to_string(),
                ],
            ),
            TemplateVariable::boolean("crypto", "Review cryptographic code"),
            TemplateVariable::boolean("api_security", "Review API security"),
            TemplateVariable::boolean("infrastructure", "Review infrastructure"),
        ])
        .with_target_files(vec![
            "security-review-{{review_target}}.md".to_string(),
            "security-checklist-{{review_target}}.md".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:111-166 method react_hook <-> ./crates/ccswarm/src/template/predefined.rs:381-439 method integration_test
  Similarity: 97.92%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_hook (lines 111-166) ---[0m
    pub fn react_hook() -> Template {
        Template::new(
            "react-hook",
            "React Custom Hook",
            "Create a custom React hook with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "react".to_string(),
            "hook".to_string(),
            "typescript".to_string(),
        ])
        .with_task_description(
            "Create a custom React hook named {{hook_name}} for {{hook_purpose}}",
        )
        .with_task_details(
            "## Hook Specification\n\n\
            - **Name**: {{hook_name}}\n\
            - **Purpose**: {{hook_purpose}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if parameters}}- **Parameters**: {{parameters}}\n{{/if}}\
            {{#if return_type}}- **Returns**: {{return_type}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React hooks rules and conventions\n\
            - Include proper dependency arrays for useEffect\n\
            - Add comprehensive JSDoc comments\n\
            - Handle cleanup and memory leaks\n\
            {{#if testing}}- Create unit tests with React Hooks Testing Library\n{{/if}}\
            {{#if memoization}}- Use useMemo/useCallback for performance optimization\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(30)
        .with_variables(vec![
            TemplateVariable::text(
                "hook_name",
                "Name of the custom hook (e.g., useLocalStorage)",
            ),
            TemplateVariable::text("hook_purpose", "What the hook does"),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the hook",
                "src/hooks/{{hook_name}}.ts",
            ),
            TemplateVariable::text("parameters", "Hook parameters (optional)").optional(),
            TemplateVariable::text("return_type", "Hook return type (optional)").optional(),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("memoization", "Include performance optimizations"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.ts".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:integration_test (lines 381-439) ---[0m
    pub fn integration_test() -> Template {
        Template::new(
            "integration-test",
            "Integration Test",
            "Create integration tests for system components",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "integration".to_string(),
            "e2e".to_string(),
        ])
        .with_task_description(
            "Create integration tests for {{system_component}} testing {{integration_points}}",
        )
        .with_task_details(
            "## Integration Test Specification\n\n\
            - **Component**: {{system_component}}\n\
            - **Integration Points**: {{integration_points}}\n\
            - **Test Environment**: {{test_environment}}\n\
            {{#if database}}- **Database**: Test database setup required\n{{/if}}\
            {{#if external_services}}- **External Services**: {{external_services}}\n{{/if}}\n\n\
            ## Test Requirements\n\n\
            - Set up test environment and data\n\
            - Test end-to-end workflows\n\
            - Verify data consistency across components\n\
            - Test error propagation and recovery\n\
            - Clean up test data after execution\n\
            {{#if performance}}- Measure integration performance\n{{/if}}\
            {{#if security}}- Verify security boundaries\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Testing)
        .with_duration(75)
        .with_variables(vec![
            TemplateVariable::text("system_component", "System component being tested"),
            TemplateVariable::text("integration_points", "What integrations to test"),
            TemplateVariable::choice(
                "test_environment",
                "Test environment",
                vec![
                    "Docker Compose".to_string(),
                    "Testcontainers".to_string(),
                    "In-memory".to_string(),
                    "Staging".to_string(),
                ],
            ),
            TemplateVariable::boolean("database", "Includes database testing"),
            TemplateVariable::text("external_services", "External services involved").optional(),
            TemplateVariable::boolean("performance", "Include performance testing"),
            TemplateVariable::boolean("security", "Include security testing"),
        ])
        .with_target_files(vec![
            "tests/integration/{{system_component}}_integration.test.{{file_extension}}"
                .to_string(),
            "tests/fixtures/{{system_component}}_data.json".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:381-439 method integration_test <-> ./crates/ccswarm/src/template/predefined.rs:704-761 method rust_module
  Similarity: 96.11%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:integration_test (lines 381-439) ---[0m
    pub fn integration_test() -> Template {
        Template::new(
            "integration-test",
            "Integration Test",
            "Create integration tests for system components",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "integration".to_string(),
            "e2e".to_string(),
        ])
        .with_task_description(
            "Create integration tests for {{system_component}} testing {{integration_points}}",
        )
        .with_task_details(
            "## Integration Test Specification\n\n\
            - **Component**: {{system_component}}\n\
            - **Integration Points**: {{integration_points}}\n\
            - **Test Environment**: {{test_environment}}\n\
            {{#if database}}- **Database**: Test database setup required\n{{/if}}\
            {{#if external_services}}- **External Services**: {{external_services}}\n{{/if}}\n\n\
            ## Test Requirements\n\n\
            - Set up test environment and data\n\
            - Test end-to-end workflows\n\
            - Verify data consistency across components\n\
            - Test error propagation and recovery\n\
            - Clean up test data after execution\n\
            {{#if performance}}- Measure integration performance\n{{/if}}\
            {{#if security}}- Verify security boundaries\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Testing)
        .with_duration(75)
        .with_variables(vec![
            TemplateVariable::text("system_component", "System component being tested"),
            TemplateVariable::text("integration_points", "What integrations to test"),
            TemplateVariable::choice(
                "test_environment",
                "Test environment",
                vec![
                    "Docker Compose".to_string(),
                    "Testcontainers".to_string(),
                    "In-memory".to_string(),
                    "Staging".to_string(),
                ],
            ),
            TemplateVariable::boolean("database", "Includes database testing"),
            TemplateVariable::text("external_services", "External services involved").optional(),
            TemplateVariable::boolean("performance", "Include performance testing"),
            TemplateVariable::boolean("security", "Include security testing"),
        ])
        .with_target_files(vec![
            "tests/integration/{{system_component}}_integration.test.{{file_extension}}"
                .to_string(),
            "tests/fixtures/{{system_component}}_data.json".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_module (lines 704-761) ---[0m
    pub fn rust_module() -> Template {
        Template::new(
            "rust-module",
            "Rust Module",
            "Create a new Rust module with proper structure and documentation",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "module".to_string(),
            "backend".to_string(),
        ])
        .with_task_description("Create Rust module {{module_name}} for {{module_purpose}}")
        .with_task_details(
            "## Rust Module Specification\n\n\
            - **Module**: {{module_name}}\n\
            - **Purpose**: {{module_purpose}}\n\
            - **Visibility**: {{visibility}}\n\
            {{#if traits}}- **Traits**: {{traits}}\n{{/if}}\
            {{#if structs}}- **Structs**: {{structs}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Follow Rust naming conventions\n\
            - Add comprehensive rustdoc comments\n\
            - Implement proper error handling with Result<T, E>\n\
            - Use appropriate visibility modifiers\n\
            - Include unit tests in #[cfg(test)] module\n\
            {{#if async_support}}- Implement async/await patterns\n{{/if}}\
            {{#if serde_support}}- Add Serde serialization support\n{{/if}}\
            {{#if benchmark}}- Include criterion benchmarks\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("module_name", "Name of the Rust module"),
            TemplateVariable::text("module_purpose", "What this module does"),
            TemplateVariable::choice(
                "visibility",
                "Module visibility",
                vec![
                    "public".to_string(),
                    "private".to_string(),
                    "pub(crate)".to_string(),
                ],
            ),
            TemplateVariable::text("traits", "Traits to implement").optional(),
            TemplateVariable::text("structs", "Main structs in the module").optional(),
            TemplateVariable::boolean("async_support", "Include async support"),
            TemplateVariable::boolean("serde_support", "Add Serde support"),
            TemplateVariable::boolean("benchmark", "Include benchmarks"),
        ])
        .with_target_files(vec![
            "src/{{module_name}}/mod.rs".to_string(),
            "src/{{module_name}}/types.rs".to_string(),
            "benches/{{module_name}}_bench.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:381-439 method integration_test <-> ./crates/ccswarm/src/template/predefined.rs:931-987 method code_refactor
  Similarity: 96.81%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:integration_test (lines 381-439) ---[0m
    pub fn integration_test() -> Template {
        Template::new(
            "integration-test",
            "Integration Test",
            "Create integration tests for system components",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "integration".to_string(),
            "e2e".to_string(),
        ])
        .with_task_description(
            "Create integration tests for {{system_component}} testing {{integration_points}}",
        )
        .with_task_details(
            "## Integration Test Specification\n\n\
            - **Component**: {{system_component}}\n\
            - **Integration Points**: {{integration_points}}\n\
            - **Test Environment**: {{test_environment}}\n\
            {{#if database}}- **Database**: Test database setup required\n{{/if}}\
            {{#if external_services}}- **External Services**: {{external_services}}\n{{/if}}\n\n\
            ## Test Requirements\n\n\
            - Set up test environment and data\n\
            - Test end-to-end workflows\n\
            - Verify data consistency across components\n\
            - Test error propagation and recovery\n\
            - Clean up test data after execution\n\
            {{#if performance}}- Measure integration performance\n{{/if}}\
            {{#if security}}- Verify security boundaries\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Testing)
        .with_duration(75)
        .with_variables(vec![
            TemplateVariable::text("system_component", "System component being tested"),
            TemplateVariable::text("integration_points", "What integrations to test"),
            TemplateVariable::choice(
                "test_environment",
                "Test environment",
                vec![
                    "Docker Compose".to_string(),
                    "Testcontainers".to_string(),
                    "In-memory".to_string(),
                    "Staging".to_string(),
                ],
            ),
            TemplateVariable::boolean("database", "Includes database testing"),
            TemplateVariable::text("external_services", "External services involved").optional(),
            TemplateVariable::boolean("performance", "Include performance testing"),
            TemplateVariable::boolean("security", "Include security testing"),
        ])
        .with_target_files(vec![
            "tests/integration/{{system_component}}_integration.test.{{file_extension}}"
                .to_string(),
            "tests/fixtures/{{system_component}}_data.json".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:code_refactor (lines 931-987) ---[0m
    pub fn code_refactor() -> Template {
        Template::new(
            "code-refactor",
            "Code Refactoring",
            "Refactor code to improve maintainability and design",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "refactor".to_string(),
            "maintainability".to_string(),
            "design".to_string(),
        ])
        .with_task_description("Refactor {{refactor_target}} to {{refactor_goal}}")
        .with_task_details(
            "## Refactoring Plan\n\n\
            - **Target**: {{refactor_target}}\n\
            - **Goal**: {{refactor_goal}}\n\
            - **Refactor Type**: {{refactor_type}}\n\
            {{#if design_patterns}}- **Design Patterns**: {{design_patterns}}\n{{/if}}\n\n\
            ## Refactoring Steps\n\n\
            - Analyze current code structure and identify issues\n\
            - Create comprehensive test coverage before refactoring\n\
            - Apply refactoring techniques incrementally\n\
            - Verify tests pass after each step\n\
            - Update documentation to reflect changes\n\
            {{#if breaking_changes}}- Document any breaking changes\n{{/if}}\
            {{#if performance_impact}}- Measure performance impact\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(80)
        .with_variables(vec![
            TemplateVariable::text("refactor_target", "Code to be refactored"),
            TemplateVariable::text("refactor_goal", "What the refactoring should achieve"),
            TemplateVariable::choice(
                "refactor_type",
                "Type of refactoring",
                vec![
                    "Extract Method".to_string(),
                    "Extract Class".to_string(),
                    "Move Method".to_string(),
                    "Rename".to_string(),
                    "Simplify Conditional".to_string(),
                    "Remove Duplication".to_string(),
                    "Improve Abstraction".to_string(),
                ],
            ),
            TemplateVariable::text("design_patterns", "Design patterns to apply").optional(),
            TemplateVariable::boolean("breaking_changes", "May introduce breaking changes"),
            TemplateVariable::boolean("performance_impact", "Measure performance impact"),
        ])
        .with_target_files(vec![
            "refactor-plan-{{refactor_target}}.md".to_string(),
            "tests/refactor/{{refactor_target}}_test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:501-559 method docker_service <-> ./crates/ccswarm/src/template/predefined.rs:931-987 method code_refactor
  Similarity: 96.55%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:docker_service (lines 501-559) ---[0m
    pub fn docker_service() -> Template {
        Template::new(
            "docker-service",
            "Docker Service",
            "Create a Docker container configuration for a service",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "docker".to_string(),
            "container".to_string(),
            "devops".to_string(),
        ])
        .with_task_description("Create Docker configuration for {{service_name}} service")
        .with_task_details(
            "## Docker Service Configuration\n\n\
            - **Service**: {{service_name}}\n\
            - **Base Image**: {{base_image}}\n\
            {{#if ports}}- **Ports**: {{ports}}\n{{/if}}\
            {{#if volumes}}- **Volumes**: {{volumes}}\n{{/if}}\
            {{#if environment}}- **Environment**: {{environment}}\n{{/if}}\n\n\
            ## Container Requirements\n\n\
            - Multi-stage build for optimization\n\
            - Non-root user for security\n\
            - Health check configuration\n\
            - Proper signal handling\n\
            - Minimal attack surface\n\
            {{#if secrets}}- Secure secret management\n{{/if}}\
            {{#if monitoring}}- Monitoring and logging setup\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("service_name", "Name of the service"),
            TemplateVariable::choice(
                "base_image",
                "Base Docker image",
                vec![
                    "alpine".to_string(),
                    "ubuntu".to_string(),
                    "node:alpine".to_string(),
                    "python:alpine".to_string(),
                    "rust:alpine".to_string(),
                    "nginx:alpine".to_string(),
                ],
            ),
            TemplateVariable::text("ports", "Port mappings (e.g., 8080:8080)").optional(),
            TemplateVariable::text("volumes", "Volume mounts").optional(),
            TemplateVariable::text("environment", "Environment variables").optional(),
            TemplateVariable::boolean("secrets", "Include secret management"),
            TemplateVariable::boolean("monitoring", "Add monitoring setup"),
        ])
        .with_target_files(vec![
            "Dockerfile".to_string(),
            "docker-compose.yml".to_string(),
            ".dockerignore".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:code_refactor (lines 931-987) ---[0m
    pub fn code_refactor() -> Template {
        Template::new(
            "code-refactor",
            "Code Refactoring",
            "Refactor code to improve maintainability and design",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "refactor".to_string(),
            "maintainability".to_string(),
            "design".to_string(),
        ])
        .with_task_description("Refactor {{refactor_target}} to {{refactor_goal}}")
        .with_task_details(
            "## Refactoring Plan\n\n\
            - **Target**: {{refactor_target}}\n\
            - **Goal**: {{refactor_goal}}\n\
            - **Refactor Type**: {{refactor_type}}\n\
            {{#if design_patterns}}- **Design Patterns**: {{design_patterns}}\n{{/if}}\n\n\
            ## Refactoring Steps\n\n\
            - Analyze current code structure and identify issues\n\
            - Create comprehensive test coverage before refactoring\n\
            - Apply refactoring techniques incrementally\n\
            - Verify tests pass after each step\n\
            - Update documentation to reflect changes\n\
            {{#if breaking_changes}}- Document any breaking changes\n{{/if}}\
            {{#if performance_impact}}- Measure performance impact\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(80)
        .with_variables(vec![
            TemplateVariable::text("refactor_target", "Code to be refactored"),
            TemplateVariable::text("refactor_goal", "What the refactoring should achieve"),
            TemplateVariable::choice(
                "refactor_type",
                "Type of refactoring",
                vec![
                    "Extract Method".to_string(),
                    "Extract Class".to_string(),
                    "Move Method".to_string(),
                    "Rename".to_string(),
                    "Simplify Conditional".to_string(),
                    "Remove Duplication".to_string(),
                    "Improve Abstraction".to_string(),
                ],
            ),
            TemplateVariable::text("design_patterns", "Design patterns to apply").optional(),
            TemplateVariable::boolean("breaking_changes", "May introduce breaking changes"),
            TemplateVariable::boolean("performance_impact", "Measure performance impact"),
        ])
        .with_target_files(vec![
            "refactor-plan-{{refactor_target}}.md".to_string(),
            "tests/refactor/{{refactor_target}}_test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:253-310 method database_migration <-> ./crates/ccswarm/src/template/predefined.rs:442-498 method bug_fix
  Similarity: 97.34%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:database_migration (lines 253-310) ---[0m
    pub fn database_migration() -> Template {
        Template::new(
            "database-migration",
            "Database Migration",
            "Create a database migration script",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "database".to_string(),
            "migration".to_string(),
            "schema".to_string(),
        ])
        .with_task_description("Create database migration to {{migration_purpose}}")
        .with_task_details(
            "## Migration Details\n\n\
            - **Purpose**: {{migration_purpose}}\n\
            - **Type**: {{migration_type}}\n\
            {{#if table_name}}- **Table**: {{table_name}}\n{{/if}}\
            {{#if columns}}- **Columns**: {{columns}}\n{{/if}}\
            {{#if indexes}}- **Indexes**: {{indexes}}\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Write both up and down migrations\n\
            - Include proper constraints and indexes\n\
            - Handle existing data appropriately\n\
            - Add comments for complex operations\n\
            - Test migration on sample data\n\
            {{#if backup}}- Create backup before running\n{{/if}}\
            {{#if rollback_plan}}- Document rollback procedure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(40)
        .with_variables(vec![
            TemplateVariable::text("migration_purpose", "What this migration accomplishes"),
            TemplateVariable::choice(
                "migration_type",
                "Type of migration",
                vec![
                    "Create Table".to_string(),
                    "Alter Table".to_string(),
                    "Drop Table".to_string(),
                    "Add Column".to_string(),
                    "Drop Column".to_string(),
                    "Create Index".to_string(),
                    "Data Migration".to_string(),
                ],
            ),
            TemplateVariable::text("table_name", "Table name (if applicable)").optional(),
            TemplateVariable::text("columns", "Column definitions (optional)").optional(),
            TemplateVariable::text("indexes", "Index definitions (optional)").optional(),
            TemplateVariable::boolean("backup", "Create backup before migration"),
            TemplateVariable::boolean("rollback_plan", "Document rollback procedure"),
        ])
        .with_target_files(vec![
            "migrations/{{current_datetime}}_{{migration_purpose}}.sql".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:bug_fix (lines 442-498) ---[0m
    pub fn bug_fix() -> Template {
        Template::new(
            "bug-fix",
            "Bug Fix",
            "Fix a reported bug with proper testing and documentation",
            TemplateCategory::BugFix,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "bug".to_string(),
            "fix".to_string(),
            "maintenance".to_string(),
        ])
        .with_task_description("Fix bug: {{bug_description}} ({{bug_severity}})")
        .with_task_details(
            "## Bug Report\n\n\
            - **Description**: {{bug_description}}\n\
            - **Severity**: {{bug_severity}}\n\
            {{#if reproduction_steps}}- **Reproduction**: {{reproduction_steps}}\n{{/if}}\
            {{#if affected_components}}- **Affected Components**: {{affected_components}}\n{{/if}}\
            {{#if user_impact}}- **User Impact**: {{user_impact}}\n{{/if}}\n\n\
            ## Fix Requirements\n\n\
            - Reproduce the bug in a test environment\n\
            - Identify root cause through debugging\n\
            - Implement minimal fix that addresses root cause\n\
            - Add regression tests to prevent recurrence\n\
            - Verify fix doesn't introduce new issues\n\
            {{#if documentation}}- Update relevant documentation\n{{/if}}\
            {{#if release_notes}}- Add to release notes\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Bugfix)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("bug_description", "Clear description of the bug"),
            TemplateVariable::choice(
                "bug_severity",
                "Bug severity level",
                vec![
                    "Critical".to_string(),
                    "High".to_string(),
                    "Medium".to_string(),
                    "Low".to_string(),
                ],
            ),
            TemplateVariable::text("reproduction_steps", "Steps to reproduce the bug").optional(),
            TemplateVariable::text("affected_components", "Components affected by the bug")
                .optional(),
            TemplateVariable::text("user_impact", "How this affects users").optional(),
            TemplateVariable::boolean("documentation", "Update documentation"),
            TemplateVariable::boolean("release_notes", "Add to release notes"),
        ])
        .with_target_files(vec![
            "src/{{affected_components}}/fix.{{file_extension}}".to_string(),
            "tests/regression/{{bug_description}}.test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:253-310 method database_migration <-> ./crates/ccswarm/src/template/predefined.rs:704-761 method rust_module
  Similarity: 96.47%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:database_migration (lines 253-310) ---[0m
    pub fn database_migration() -> Template {
        Template::new(
            "database-migration",
            "Database Migration",
            "Create a database migration script",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "database".to_string(),
            "migration".to_string(),
            "schema".to_string(),
        ])
        .with_task_description("Create database migration to {{migration_purpose}}")
        .with_task_details(
            "## Migration Details\n\n\
            - **Purpose**: {{migration_purpose}}\n\
            - **Type**: {{migration_type}}\n\
            {{#if table_name}}- **Table**: {{table_name}}\n{{/if}}\
            {{#if columns}}- **Columns**: {{columns}}\n{{/if}}\
            {{#if indexes}}- **Indexes**: {{indexes}}\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Write both up and down migrations\n\
            - Include proper constraints and indexes\n\
            - Handle existing data appropriately\n\
            - Add comments for complex operations\n\
            - Test migration on sample data\n\
            {{#if backup}}- Create backup before running\n{{/if}}\
            {{#if rollback_plan}}- Document rollback procedure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(40)
        .with_variables(vec![
            TemplateVariable::text("migration_purpose", "What this migration accomplishes"),
            TemplateVariable::choice(
                "migration_type",
                "Type of migration",
                vec![
                    "Create Table".to_string(),
                    "Alter Table".to_string(),
                    "Drop Table".to_string(),
                    "Add Column".to_string(),
                    "Drop Column".to_string(),
                    "Create Index".to_string(),
                    "Data Migration".to_string(),
                ],
            ),
            TemplateVariable::text("table_name", "Table name (if applicable)").optional(),
            TemplateVariable::text("columns", "Column definitions (optional)").optional(),
            TemplateVariable::text("indexes", "Index definitions (optional)").optional(),
            TemplateVariable::boolean("backup", "Create backup before migration"),
            TemplateVariable::boolean("rollback_plan", "Document rollback procedure"),
        ])
        .with_target_files(vec![
            "migrations/{{current_datetime}}_{{migration_purpose}}.sql".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_module (lines 704-761) ---[0m
    pub fn rust_module() -> Template {
        Template::new(
            "rust-module",
            "Rust Module",
            "Create a new Rust module with proper structure and documentation",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "module".to_string(),
            "backend".to_string(),
        ])
        .with_task_description("Create Rust module {{module_name}} for {{module_purpose}}")
        .with_task_details(
            "## Rust Module Specification\n\n\
            - **Module**: {{module_name}}\n\
            - **Purpose**: {{module_purpose}}\n\
            - **Visibility**: {{visibility}}\n\
            {{#if traits}}- **Traits**: {{traits}}\n{{/if}}\
            {{#if structs}}- **Structs**: {{structs}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Follow Rust naming conventions\n\
            - Add comprehensive rustdoc comments\n\
            - Implement proper error handling with Result<T, E>\n\
            - Use appropriate visibility modifiers\n\
            - Include unit tests in #[cfg(test)] module\n\
            {{#if async_support}}- Implement async/await patterns\n{{/if}}\
            {{#if serde_support}}- Add Serde serialization support\n{{/if}}\
            {{#if benchmark}}- Include criterion benchmarks\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("module_name", "Name of the Rust module"),
            TemplateVariable::text("module_purpose", "What this module does"),
            TemplateVariable::choice(
                "visibility",
                "Module visibility",
                vec![
                    "public".to_string(),
                    "private".to_string(),
                    "pub(crate)".to_string(),
                ],
            ),
            TemplateVariable::text("traits", "Traits to implement").optional(),
            TemplateVariable::text("structs", "Main structs in the module").optional(),
            TemplateVariable::boolean("async_support", "Include async support"),
            TemplateVariable::boolean("serde_support", "Add Serde support"),
            TemplateVariable::boolean("benchmark", "Include benchmarks"),
        ])
        .with_target_files(vec![
            "src/{{module_name}}/mod.rs".to_string(),
            "src/{{module_name}}/types.rs".to_string(),
            "benches/{{module_name}}_bench.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:501-559 method docker_service <-> ./crates/ccswarm/src/template/predefined.rs:816-872 method security_review
  Similarity: 96.25%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:docker_service (lines 501-559) ---[0m
    pub fn docker_service() -> Template {
        Template::new(
            "docker-service",
            "Docker Service",
            "Create a Docker container configuration for a service",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "docker".to_string(),
            "container".to_string(),
            "devops".to_string(),
        ])
        .with_task_description("Create Docker configuration for {{service_name}} service")
        .with_task_details(
            "## Docker Service Configuration\n\n\
            - **Service**: {{service_name}}\n\
            - **Base Image**: {{base_image}}\n\
            {{#if ports}}- **Ports**: {{ports}}\n{{/if}}\
            {{#if volumes}}- **Volumes**: {{volumes}}\n{{/if}}\
            {{#if environment}}- **Environment**: {{environment}}\n{{/if}}\n\n\
            ## Container Requirements\n\n\
            - Multi-stage build for optimization\n\
            - Non-root user for security\n\
            - Health check configuration\n\
            - Proper signal handling\n\
            - Minimal attack surface\n\
            {{#if secrets}}- Secure secret management\n{{/if}}\
            {{#if monitoring}}- Monitoring and logging setup\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("service_name", "Name of the service"),
            TemplateVariable::choice(
                "base_image",
                "Base Docker image",
                vec![
                    "alpine".to_string(),
                    "ubuntu".to_string(),
                    "node:alpine".to_string(),
                    "python:alpine".to_string(),
                    "rust:alpine".to_string(),
                    "nginx:alpine".to_string(),
                ],
            ),
            TemplateVariable::text("ports", "Port mappings (e.g., 8080:8080)").optional(),
            TemplateVariable::text("volumes", "Volume mounts").optional(),
            TemplateVariable::text("environment", "Environment variables").optional(),
            TemplateVariable::boolean("secrets", "Include secret management"),
            TemplateVariable::boolean("monitoring", "Add monitoring setup"),
        ])
        .with_target_files(vec![
            "Dockerfile".to_string(),
            "docker-compose.yml".to_string(),
            ".dockerignore".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:security_review (lines 816-872) ---[0m
    pub fn security_review() -> Template {
        Template::new(
            "security-review",
            "Security Review",
            "Conduct a comprehensive security review of code or system",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "security".to_string(),
            "review".to_string(),
            "audit".to_string(),
        ])
        .with_task_description(
            "Conduct security review of {{review_target}} focusing on {{security_aspects}}",
        )
        .with_task_details(
            "## Security Review Scope\n\n\
            - **Target**: {{review_target}}\n\
            - **Focus Areas**: {{security_aspects}}\n\
            - **Review Type**: {{review_type}}\n\n\
            ## Security Checklist\n\n\
            - Authentication and authorization mechanisms\n\
            - Input validation and sanitization\n\
            - SQL injection and XSS vulnerabilities\n\
            - Secure data storage and transmission\n\
            - Error handling and information disclosure\n\
            - Dependencies and supply chain security\n\
            {{#if crypto}}- Cryptographic implementations\n{{/if}}\
            {{#if api_security}}- API security and rate limiting\n{{/if}}\
            {{#if infrastructure}}- Infrastructure security configuration\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Review)
        .with_duration(120)
        .with_variables(vec![
            TemplateVariable::text("review_target", "What is being reviewed"),
            TemplateVariable::text("security_aspects", "Specific security concerns"),
            TemplateVariable::choice(
                "review_type",
                "Type of security review",
                vec![
                    "Code Review".to_string(),
                    "Architecture Review".to_string(),
                    "Penetration Test".to_string(),
                    "Compliance Audit".to_string(),
                ],
            ),
            TemplateVariable::boolean("crypto", "Review cryptographic code"),
            TemplateVariable::boolean("api_security", "Review API security"),
            TemplateVariable::boolean("infrastructure", "Review infrastructure"),
        ])
        .with_target_files(vec![
            "security-review-{{review_target}}.md".to_string(),
            "security-checklist-{{review_target}}.md".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:111-166 method react_hook <-> ./crates/ccswarm/src/template/predefined.rs:501-559 method docker_service
  Similarity: 96.94%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_hook (lines 111-166) ---[0m
    pub fn react_hook() -> Template {
        Template::new(
            "react-hook",
            "React Custom Hook",
            "Create a custom React hook with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "react".to_string(),
            "hook".to_string(),
            "typescript".to_string(),
        ])
        .with_task_description(
            "Create a custom React hook named {{hook_name}} for {{hook_purpose}}",
        )
        .with_task_details(
            "## Hook Specification\n\n\
            - **Name**: {{hook_name}}\n\
            - **Purpose**: {{hook_purpose}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if parameters}}- **Parameters**: {{parameters}}\n{{/if}}\
            {{#if return_type}}- **Returns**: {{return_type}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React hooks rules and conventions\n\
            - Include proper dependency arrays for useEffect\n\
            - Add comprehensive JSDoc comments\n\
            - Handle cleanup and memory leaks\n\
            {{#if testing}}- Create unit tests with React Hooks Testing Library\n{{/if}}\
            {{#if memoization}}- Use useMemo/useCallback for performance optimization\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(30)
        .with_variables(vec![
            TemplateVariable::text(
                "hook_name",
                "Name of the custom hook (e.g., useLocalStorage)",
            ),
            TemplateVariable::text("hook_purpose", "What the hook does"),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the hook",
                "src/hooks/{{hook_name}}.ts",
            ),
            TemplateVariable::text("parameters", "Hook parameters (optional)").optional(),
            TemplateVariable::text("return_type", "Hook return type (optional)").optional(),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("memoization", "Include performance optimizations"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.ts".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:docker_service (lines 501-559) ---[0m
    pub fn docker_service() -> Template {
        Template::new(
            "docker-service",
            "Docker Service",
            "Create a Docker container configuration for a service",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "docker".to_string(),
            "container".to_string(),
            "devops".to_string(),
        ])
        .with_task_description("Create Docker configuration for {{service_name}} service")
        .with_task_details(
            "## Docker Service Configuration\n\n\
            - **Service**: {{service_name}}\n\
            - **Base Image**: {{base_image}}\n\
            {{#if ports}}- **Ports**: {{ports}}\n{{/if}}\
            {{#if volumes}}- **Volumes**: {{volumes}}\n{{/if}}\
            {{#if environment}}- **Environment**: {{environment}}\n{{/if}}\n\n\
            ## Container Requirements\n\n\
            - Multi-stage build for optimization\n\
            - Non-root user for security\n\
            - Health check configuration\n\
            - Proper signal handling\n\
            - Minimal attack surface\n\
            {{#if secrets}}- Secure secret management\n{{/if}}\
            {{#if monitoring}}- Monitoring and logging setup\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("service_name", "Name of the service"),
            TemplateVariable::choice(
                "base_image",
                "Base Docker image",
                vec![
                    "alpine".to_string(),
                    "ubuntu".to_string(),
                    "node:alpine".to_string(),
                    "python:alpine".to_string(),
                    "rust:alpine".to_string(),
                    "nginx:alpine".to_string(),
                ],
            ),
            TemplateVariable::text("ports", "Port mappings (e.g., 8080:8080)").optional(),
            TemplateVariable::text("volumes", "Volume mounts").optional(),
            TemplateVariable::text("environment", "Environment variables").optional(),
            TemplateVariable::boolean("secrets", "Include secret management"),
            TemplateVariable::boolean("monitoring", "Add monitoring setup"),
        ])
        .with_target_files(vec![
            "Dockerfile".to_string(),
            "docker-compose.yml".to_string(),
            ".dockerignore".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:313-378 method unit_test <-> ./crates/ccswarm/src/template/predefined.rs:764-813 method rust_cli_command
  Similarity: 95.88%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:unit_test (lines 313-378) ---[0m
    pub fn unit_test() -> Template {
        Template::new(
            "unit-test",
            "Unit Test",
            "Create comprehensive unit tests for a function or module",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "unit-test".to_string(),
            "quality".to_string(),
        ])
        .with_task_description("Create unit tests for {{test_target}} covering {{test_scenarios}}")
        .with_task_details(
            "## Test Specification\n\n\
            - **Target**: {{test_target}}\n\
            - **Test Framework**: {{test_framework}}\n\
            - **Scenarios**: {{test_scenarios}}\n\
            {{#if mocking}}- **Mocking**: {{mocking_strategy}}\n{{/if}}\n\n\
            ## Test Coverage Requirements\n\n\
            - Happy path scenarios\n\
            - Error conditions and edge cases\n\
            - Boundary value testing\n\
            - Input validation testing\n\
            {{#if async_code}}- Async operation testing\n{{/if}}\
            {{#if performance}}- Performance benchmarks\n{{/if}}\
            - Achieve >90% code coverage for the target",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Testing)
        .with_duration(35)
        .with_variables(vec![
            TemplateVariable::text("test_target", "Function, class, or module to test"),
            TemplateVariable::text("test_scenarios", "Specific scenarios to test"),
            TemplateVariable::choice(
                "test_framework",
                "Testing framework",
                vec![
                    "Jest".to_string(),
                    "Vitest".to_string(),
                    "Mocha".to_string(),
                    "pytest".to_string(),
                    "Rust test".to_string(),
                    "Go test".to_string(),
                ],
            ),
            TemplateVariable::choice(
                "mocking_strategy",
                "Mocking approach",
                vec![
                    "Manual mocks".to_string(),
                    "Jest mocks".to_string(),
                    "Sinon".to_string(),
                    "unittest.mock".to_string(),
                    "mockall".to_string(),
                ],
            )
            .optional(),
            TemplateVariable::boolean("async_code", "Testing async operations"),
            TemplateVariable::boolean("performance", "Include performance tests"),
        ])
        .with_target_files(vec![
            "tests/{{test_target}}.test.{{file_extension}}".to_string()
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_cli_command (lines 764-813) ---[0m
    pub fn rust_cli_command() -> Template {
        Template::new(
            "rust-cli-command",
            "Rust CLI Command",
            "Add a new command to a Rust CLI application using clap",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "cli".to_string(),
            "clap".to_string(),
            "command".to_string(),
        ])
        .with_task_description("Add CLI command {{command_name}} to {{command_description}}")
        .with_task_details(
            "## CLI Command Specification\n\n\
            - **Command**: {{command_name}}\n\
            - **Description**: {{command_description}}\n\
            {{#if arguments}}- **Arguments**: {{arguments}}\n{{/if}}\
            {{#if options}}- **Options**: {{options}}\n{{/if}}\
            {{#if subcommands}}- **Subcommands**: {{subcommands}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use clap derive macros for argument parsing\n\
            - Implement proper error handling and exit codes\n\
            - Add comprehensive help text and examples\n\
            - Include input validation\n\
            - Support common CLI patterns (--help, --version)\n\
            {{#if config_file}}- Support configuration file loading\n{{/if}}\
            {{#if logging}}- Integrate with logging framework\n{{/if}}\
            {{#if testing}}- Add integration tests for CLI\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(50)
        .with_variables(vec![
            TemplateVariable::text("command_name", "Name of the CLI command"),
            TemplateVariable::text("command_description", "What the command does"),
            TemplateVariable::text("arguments", "Required arguments").optional(),
            TemplateVariable::text("options", "Optional flags and options").optional(),
            TemplateVariable::text("subcommands", "Subcommands if any").optional(),
            TemplateVariable::boolean("config_file", "Support config files"),
            TemplateVariable::boolean("logging", "Add logging support"),
            TemplateVariable::boolean("testing", "Create CLI tests"),
        ])
        .with_target_files(vec![
            "src/cli/{{command_name}}.rs".to_string(),
            "tests/cli/{{command_name}}_test.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:704-761 method rust_module <-> ./crates/ccswarm/src/template/predefined.rs:931-987 method code_refactor
  Similarity: 96.49%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_module (lines 704-761) ---[0m
    pub fn rust_module() -> Template {
        Template::new(
            "rust-module",
            "Rust Module",
            "Create a new Rust module with proper structure and documentation",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "module".to_string(),
            "backend".to_string(),
        ])
        .with_task_description("Create Rust module {{module_name}} for {{module_purpose}}")
        .with_task_details(
            "## Rust Module Specification\n\n\
            - **Module**: {{module_name}}\n\
            - **Purpose**: {{module_purpose}}\n\
            - **Visibility**: {{visibility}}\n\
            {{#if traits}}- **Traits**: {{traits}}\n{{/if}}\
            {{#if structs}}- **Structs**: {{structs}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Follow Rust naming conventions\n\
            - Add comprehensive rustdoc comments\n\
            - Implement proper error handling with Result<T, E>\n\
            - Use appropriate visibility modifiers\n\
            - Include unit tests in #[cfg(test)] module\n\
            {{#if async_support}}- Implement async/await patterns\n{{/if}}\
            {{#if serde_support}}- Add Serde serialization support\n{{/if}}\
            {{#if benchmark}}- Include criterion benchmarks\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("module_name", "Name of the Rust module"),
            TemplateVariable::text("module_purpose", "What this module does"),
            TemplateVariable::choice(
                "visibility",
                "Module visibility",
                vec![
                    "public".to_string(),
                    "private".to_string(),
                    "pub(crate)".to_string(),
                ],
            ),
            TemplateVariable::text("traits", "Traits to implement").optional(),
            TemplateVariable::text("structs", "Main structs in the module").optional(),
            TemplateVariable::boolean("async_support", "Include async support"),
            TemplateVariable::boolean("serde_support", "Add Serde support"),
            TemplateVariable::boolean("benchmark", "Include benchmarks"),
        ])
        .with_target_files(vec![
            "src/{{module_name}}/mod.rs".to_string(),
            "src/{{module_name}}/types.rs".to_string(),
            "benches/{{module_name}}_bench.rs".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:code_refactor (lines 931-987) ---[0m
    pub fn code_refactor() -> Template {
        Template::new(
            "code-refactor",
            "Code Refactoring",
            "Refactor code to improve maintainability and design",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "refactor".to_string(),
            "maintainability".to_string(),
            "design".to_string(),
        ])
        .with_task_description("Refactor {{refactor_target}} to {{refactor_goal}}")
        .with_task_details(
            "## Refactoring Plan\n\n\
            - **Target**: {{refactor_target}}\n\
            - **Goal**: {{refactor_goal}}\n\
            - **Refactor Type**: {{refactor_type}}\n\
            {{#if design_patterns}}- **Design Patterns**: {{design_patterns}}\n{{/if}}\n\n\
            ## Refactoring Steps\n\n\
            - Analyze current code structure and identify issues\n\
            - Create comprehensive test coverage before refactoring\n\
            - Apply refactoring techniques incrementally\n\
            - Verify tests pass after each step\n\
            - Update documentation to reflect changes\n\
            {{#if breaking_changes}}- Document any breaking changes\n{{/if}}\
            {{#if performance_impact}}- Measure performance impact\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(80)
        .with_variables(vec![
            TemplateVariable::text("refactor_target", "Code to be refactored"),
            TemplateVariable::text("refactor_goal", "What the refactoring should achieve"),
            TemplateVariable::choice(
                "refactor_type",
                "Type of refactoring",
                vec![
                    "Extract Method".to_string(),
                    "Extract Class".to_string(),
                    "Move Method".to_string(),
                    "Rename".to_string(),
                    "Simplify Conditional".to_string(),
                    "Remove Duplication".to_string(),
                    "Improve Abstraction".to_string(),
                ],
            ),
            TemplateVariable::text("design_patterns", "Design patterns to apply").optional(),
            TemplateVariable::boolean("breaking_changes", "May introduce breaking changes"),
            TemplateVariable::boolean("performance_impact", "Measure performance impact"),
        ])
        .with_target_files(vec![
            "refactor-plan-{{refactor_target}}.md".to_string(),
            "tests/refactor/{{refactor_target}}_test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:253-310 method database_migration <-> ./crates/ccswarm/src/template/predefined.rs:931-987 method code_refactor
  Similarity: 96.40%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:database_migration (lines 253-310) ---[0m
    pub fn database_migration() -> Template {
        Template::new(
            "database-migration",
            "Database Migration",
            "Create a database migration script",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "database".to_string(),
            "migration".to_string(),
            "schema".to_string(),
        ])
        .with_task_description("Create database migration to {{migration_purpose}}")
        .with_task_details(
            "## Migration Details\n\n\
            - **Purpose**: {{migration_purpose}}\n\
            - **Type**: {{migration_type}}\n\
            {{#if table_name}}- **Table**: {{table_name}}\n{{/if}}\
            {{#if columns}}- **Columns**: {{columns}}\n{{/if}}\
            {{#if indexes}}- **Indexes**: {{indexes}}\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Write both up and down migrations\n\
            - Include proper constraints and indexes\n\
            - Handle existing data appropriately\n\
            - Add comments for complex operations\n\
            - Test migration on sample data\n\
            {{#if backup}}- Create backup before running\n{{/if}}\
            {{#if rollback_plan}}- Document rollback procedure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(40)
        .with_variables(vec![
            TemplateVariable::text("migration_purpose", "What this migration accomplishes"),
            TemplateVariable::choice(
                "migration_type",
                "Type of migration",
                vec![
                    "Create Table".to_string(),
                    "Alter Table".to_string(),
                    "Drop Table".to_string(),
                    "Add Column".to_string(),
                    "Drop Column".to_string(),
                    "Create Index".to_string(),
                    "Data Migration".to_string(),
                ],
            ),
            TemplateVariable::text("table_name", "Table name (if applicable)").optional(),
            TemplateVariable::text("columns", "Column definitions (optional)").optional(),
            TemplateVariable::text("indexes", "Index definitions (optional)").optional(),
            TemplateVariable::boolean("backup", "Create backup before migration"),
            TemplateVariable::boolean("rollback_plan", "Document rollback procedure"),
        ])
        .with_target_files(vec![
            "migrations/{{current_datetime}}_{{migration_purpose}}.sql".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:code_refactor (lines 931-987) ---[0m
    pub fn code_refactor() -> Template {
        Template::new(
            "code-refactor",
            "Code Refactoring",
            "Refactor code to improve maintainability and design",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "refactor".to_string(),
            "maintainability".to_string(),
            "design".to_string(),
        ])
        .with_task_description("Refactor {{refactor_target}} to {{refactor_goal}}")
        .with_task_details(
            "## Refactoring Plan\n\n\
            - **Target**: {{refactor_target}}\n\
            - **Goal**: {{refactor_goal}}\n\
            - **Refactor Type**: {{refactor_type}}\n\
            {{#if design_patterns}}- **Design Patterns**: {{design_patterns}}\n{{/if}}\n\n\
            ## Refactoring Steps\n\n\
            - Analyze current code structure and identify issues\n\
            - Create comprehensive test coverage before refactoring\n\
            - Apply refactoring techniques incrementally\n\
            - Verify tests pass after each step\n\
            - Update documentation to reflect changes\n\
            {{#if breaking_changes}}- Document any breaking changes\n{{/if}}\
            {{#if performance_impact}}- Measure performance impact\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(80)
        .with_variables(vec![
            TemplateVariable::text("refactor_target", "Code to be refactored"),
            TemplateVariable::text("refactor_goal", "What the refactoring should achieve"),
            TemplateVariable::choice(
                "refactor_type",
                "Type of refactoring",
                vec![
                    "Extract Method".to_string(),
                    "Extract Class".to_string(),
                    "Move Method".to_string(),
                    "Rename".to_string(),
                    "Simplify Conditional".to_string(),
                    "Remove Duplication".to_string(),
                    "Improve Abstraction".to_string(),
                ],
            ),
            TemplateVariable::text("design_patterns", "Design patterns to apply").optional(),
            TemplateVariable::boolean("breaking_changes", "May introduce breaking changes"),
            TemplateVariable::boolean("performance_impact", "Measure performance impact"),
        ])
        .with_target_files(vec![
            "refactor-plan-{{refactor_target}}.md".to_string(),
            "tests/refactor/{{refactor_target}}_test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:442-498 method bug_fix <-> ./crates/ccswarm/src/template/predefined.rs:704-761 method rust_module
  Similarity: 96.35%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:bug_fix (lines 442-498) ---[0m
    pub fn bug_fix() -> Template {
        Template::new(
            "bug-fix",
            "Bug Fix",
            "Fix a reported bug with proper testing and documentation",
            TemplateCategory::BugFix,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "bug".to_string(),
            "fix".to_string(),
            "maintenance".to_string(),
        ])
        .with_task_description("Fix bug: {{bug_description}} ({{bug_severity}})")
        .with_task_details(
            "## Bug Report\n\n\
            - **Description**: {{bug_description}}\n\
            - **Severity**: {{bug_severity}}\n\
            {{#if reproduction_steps}}- **Reproduction**: {{reproduction_steps}}\n{{/if}}\
            {{#if affected_components}}- **Affected Components**: {{affected_components}}\n{{/if}}\
            {{#if user_impact}}- **User Impact**: {{user_impact}}\n{{/if}}\n\n\
            ## Fix Requirements\n\n\
            - Reproduce the bug in a test environment\n\
            - Identify root cause through debugging\n\
            - Implement minimal fix that addresses root cause\n\
            - Add regression tests to prevent recurrence\n\
            - Verify fix doesn't introduce new issues\n\
            {{#if documentation}}- Update relevant documentation\n{{/if}}\
            {{#if release_notes}}- Add to release notes\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Bugfix)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("bug_description", "Clear description of the bug"),
            TemplateVariable::choice(
                "bug_severity",
                "Bug severity level",
                vec![
                    "Critical".to_string(),
                    "High".to_string(),
                    "Medium".to_string(),
                    "Low".to_string(),
                ],
            ),
            TemplateVariable::text("reproduction_steps", "Steps to reproduce the bug").optional(),
            TemplateVariable::text("affected_components", "Components affected by the bug")
                .optional(),
            TemplateVariable::text("user_impact", "How this affects users").optional(),
            TemplateVariable::boolean("documentation", "Update documentation"),
            TemplateVariable::boolean("release_notes", "Add to release notes"),
        ])
        .with_target_files(vec![
            "src/{{affected_components}}/fix.{{file_extension}}".to_string(),
            "tests/regression/{{bug_description}}.test.{{file_extension}}".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_module (lines 704-761) ---[0m
    pub fn rust_module() -> Template {
        Template::new(
            "rust-module",
            "Rust Module",
            "Create a new Rust module with proper structure and documentation",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "module".to_string(),
            "backend".to_string(),
        ])
        .with_task_description("Create Rust module {{module_name}} for {{module_purpose}}")
        .with_task_details(
            "## Rust Module Specification\n\n\
            - **Module**: {{module_name}}\n\
            - **Purpose**: {{module_purpose}}\n\
            - **Visibility**: {{visibility}}\n\
            {{#if traits}}- **Traits**: {{traits}}\n{{/if}}\
            {{#if structs}}- **Structs**: {{structs}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Follow Rust naming conventions\n\
            - Add comprehensive rustdoc comments\n\
            - Implement proper error handling with Result<T, E>\n\
            - Use appropriate visibility modifiers\n\
            - Include unit tests in #[cfg(test)] module\n\
            {{#if async_support}}- Implement async/await patterns\n{{/if}}\
            {{#if serde_support}}- Add Serde serialization support\n{{/if}}\
            {{#if benchmark}}- Include criterion benchmarks\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("module_name", "Name of the Rust module"),
            TemplateVariable::text("module_purpose", "What this module does"),
            TemplateVariable::choice(
                "visibility",
                "Module visibility",
                vec![
                    "public".to_string(),
                    "private".to_string(),
                    "pub(crate)".to_string(),
                ],
            ),
            TemplateVariable::text("traits", "Traits to implement").optional(),
            TemplateVariable::text("structs", "Main structs in the module").optional(),
            TemplateVariable::boolean("async_support", "Include async support"),
            TemplateVariable::boolean("serde_support", "Add Serde support"),
            TemplateVariable::boolean("benchmark", "Include benchmarks"),
        ])
        .with_target_files(vec![
            "src/{{module_name}}/mod.rs".to_string(),
            "src/{{module_name}}/types.rs".to_string(),
            "benches/{{module_name}}_bench.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:253-310 method database_migration <-> ./crates/ccswarm/src/template/predefined.rs:816-872 method security_review
  Similarity: 96.24%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:database_migration (lines 253-310) ---[0m
    pub fn database_migration() -> Template {
        Template::new(
            "database-migration",
            "Database Migration",
            "Create a database migration script",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "database".to_string(),
            "migration".to_string(),
            "schema".to_string(),
        ])
        .with_task_description("Create database migration to {{migration_purpose}}")
        .with_task_details(
            "## Migration Details\n\n\
            - **Purpose**: {{migration_purpose}}\n\
            - **Type**: {{migration_type}}\n\
            {{#if table_name}}- **Table**: {{table_name}}\n{{/if}}\
            {{#if columns}}- **Columns**: {{columns}}\n{{/if}}\
            {{#if indexes}}- **Indexes**: {{indexes}}\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Write both up and down migrations\n\
            - Include proper constraints and indexes\n\
            - Handle existing data appropriately\n\
            - Add comments for complex operations\n\
            - Test migration on sample data\n\
            {{#if backup}}- Create backup before running\n{{/if}}\
            {{#if rollback_plan}}- Document rollback procedure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(40)
        .with_variables(vec![
            TemplateVariable::text("migration_purpose", "What this migration accomplishes"),
            TemplateVariable::choice(
                "migration_type",
                "Type of migration",
                vec![
                    "Create Table".to_string(),
                    "Alter Table".to_string(),
                    "Drop Table".to_string(),
                    "Add Column".to_string(),
                    "Drop Column".to_string(),
                    "Create Index".to_string(),
                    "Data Migration".to_string(),
                ],
            ),
            TemplateVariable::text("table_name", "Table name (if applicable)").optional(),
            TemplateVariable::text("columns", "Column definitions (optional)").optional(),
            TemplateVariable::text("indexes", "Index definitions (optional)").optional(),
            TemplateVariable::boolean("backup", "Create backup before migration"),
            TemplateVariable::boolean("rollback_plan", "Document rollback procedure"),
        ])
        .with_target_files(vec![
            "migrations/{{current_datetime}}_{{migration_purpose}}.sql".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:security_review (lines 816-872) ---[0m
    pub fn security_review() -> Template {
        Template::new(
            "security-review",
            "Security Review",
            "Conduct a comprehensive security review of code or system",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "security".to_string(),
            "review".to_string(),
            "audit".to_string(),
        ])
        .with_task_description(
            "Conduct security review of {{review_target}} focusing on {{security_aspects}}",
        )
        .with_task_details(
            "## Security Review Scope\n\n\
            - **Target**: {{review_target}}\n\
            - **Focus Areas**: {{security_aspects}}\n\
            - **Review Type**: {{review_type}}\n\n\
            ## Security Checklist\n\n\
            - Authentication and authorization mechanisms\n\
            - Input validation and sanitization\n\
            - SQL injection and XSS vulnerabilities\n\
            - Secure data storage and transmission\n\
            - Error handling and information disclosure\n\
            - Dependencies and supply chain security\n\
            {{#if crypto}}- Cryptographic implementations\n{{/if}}\
            {{#if api_security}}- API security and rate limiting\n{{/if}}\
            {{#if infrastructure}}- Infrastructure security configuration\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Review)
        .with_duration(120)
        .with_variables(vec![
            TemplateVariable::text("review_target", "What is being reviewed"),
            TemplateVariable::text("security_aspects", "Specific security concerns"),
            TemplateVariable::choice(
                "review_type",
                "Type of security review",
                vec![
                    "Code Review".to_string(),
                    "Architecture Review".to_string(),
                    "Penetration Test".to_string(),
                    "Compliance Audit".to_string(),
                ],
            ),
            TemplateVariable::boolean("crypto", "Review cryptographic code"),
            TemplateVariable::boolean("api_security", "Review API security"),
            TemplateVariable::boolean("infrastructure", "Review infrastructure"),
        ])
        .with_target_files(vec![
            "security-review-{{review_target}}.md".to_string(),
            "security-checklist-{{review_target}}.md".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:442-498 method bug_fix <-> ./crates/ccswarm/src/template/predefined.rs:931-987 method code_refactor
  Similarity: 97.06%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:bug_fix (lines 442-498) ---[0m
    pub fn bug_fix() -> Template {
        Template::new(
            "bug-fix",
            "Bug Fix",
            "Fix a reported bug with proper testing and documentation",
            TemplateCategory::BugFix,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "bug".to_string(),
            "fix".to_string(),
            "maintenance".to_string(),
        ])
        .with_task_description("Fix bug: {{bug_description}} ({{bug_severity}})")
        .with_task_details(
            "## Bug Report\n\n\
            - **Description**: {{bug_description}}\n\
            - **Severity**: {{bug_severity}}\n\
            {{#if reproduction_steps}}- **Reproduction**: {{reproduction_steps}}\n{{/if}}\
            {{#if affected_components}}- **Affected Components**: {{affected_components}}\n{{/if}}\
            {{#if user_impact}}- **User Impact**: {{user_impact}}\n{{/if}}\n\n\
            ## Fix Requirements\n\n\
            - Reproduce the bug in a test environment\n\
            - Identify root cause through debugging\n\
            - Implement minimal fix that addresses root cause\n\
            - Add regression tests to prevent recurrence\n\
            - Verify fix doesn't introduce new issues\n\
            {{#if documentation}}- Update relevant documentation\n{{/if}}\
            {{#if release_notes}}- Add to release notes\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Bugfix)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("bug_description", "Clear description of the bug"),
            TemplateVariable::choice(
                "bug_severity",
                "Bug severity level",
                vec![
                    "Critical".to_string(),
                    "High".to_string(),
                    "Medium".to_string(),
                    "Low".to_string(),
                ],
            ),
            TemplateVariable::text("reproduction_steps", "Steps to reproduce the bug").optional(),
            TemplateVariable::text("affected_components", "Components affected by the bug")
                .optional(),
            TemplateVariable::text("user_impact", "How this affects users").optional(),
            TemplateVariable::boolean("documentation", "Update documentation"),
            TemplateVariable::boolean("release_notes", "Add to release notes"),
        ])
        .with_target_files(vec![
            "src/{{affected_components}}/fix.{{file_extension}}".to_string(),
            "tests/regression/{{bug_description}}.test.{{file_extension}}".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:code_refactor (lines 931-987) ---[0m
    pub fn code_refactor() -> Template {
        Template::new(
            "code-refactor",
            "Code Refactoring",
            "Refactor code to improve maintainability and design",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "refactor".to_string(),
            "maintainability".to_string(),
            "design".to_string(),
        ])
        .with_task_description("Refactor {{refactor_target}} to {{refactor_goal}}")
        .with_task_details(
            "## Refactoring Plan\n\n\
            - **Target**: {{refactor_target}}\n\
            - **Goal**: {{refactor_goal}}\n\
            - **Refactor Type**: {{refactor_type}}\n\
            {{#if design_patterns}}- **Design Patterns**: {{design_patterns}}\n{{/if}}\n\n\
            ## Refactoring Steps\n\n\
            - Analyze current code structure and identify issues\n\
            - Create comprehensive test coverage before refactoring\n\
            - Apply refactoring techniques incrementally\n\
            - Verify tests pass after each step\n\
            - Update documentation to reflect changes\n\
            {{#if breaking_changes}}- Document any breaking changes\n{{/if}}\
            {{#if performance_impact}}- Measure performance impact\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(80)
        .with_variables(vec![
            TemplateVariable::text("refactor_target", "Code to be refactored"),
            TemplateVariable::text("refactor_goal", "What the refactoring should achieve"),
            TemplateVariable::choice(
                "refactor_type",
                "Type of refactoring",
                vec![
                    "Extract Method".to_string(),
                    "Extract Class".to_string(),
                    "Move Method".to_string(),
                    "Rename".to_string(),
                    "Simplify Conditional".to_string(),
                    "Remove Duplication".to_string(),
                    "Improve Abstraction".to_string(),
                ],
            ),
            TemplateVariable::text("design_patterns", "Design patterns to apply").optional(),
            TemplateVariable::boolean("breaking_changes", "May introduce breaking changes"),
            TemplateVariable::boolean("performance_impact", "Measure performance impact"),
        ])
        .with_target_files(vec![
            "refactor-plan-{{refactor_target}}.md".to_string(),
            "tests/refactor/{{refactor_target}}_test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:704-761 method rust_module <-> ./crates/ccswarm/src/template/predefined.rs:816-872 method security_review
  Similarity: 96.11%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_module (lines 704-761) ---[0m
    pub fn rust_module() -> Template {
        Template::new(
            "rust-module",
            "Rust Module",
            "Create a new Rust module with proper structure and documentation",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "module".to_string(),
            "backend".to_string(),
        ])
        .with_task_description("Create Rust module {{module_name}} for {{module_purpose}}")
        .with_task_details(
            "## Rust Module Specification\n\n\
            - **Module**: {{module_name}}\n\
            - **Purpose**: {{module_purpose}}\n\
            - **Visibility**: {{visibility}}\n\
            {{#if traits}}- **Traits**: {{traits}}\n{{/if}}\
            {{#if structs}}- **Structs**: {{structs}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Follow Rust naming conventions\n\
            - Add comprehensive rustdoc comments\n\
            - Implement proper error handling with Result<T, E>\n\
            - Use appropriate visibility modifiers\n\
            - Include unit tests in #[cfg(test)] module\n\
            {{#if async_support}}- Implement async/await patterns\n{{/if}}\
            {{#if serde_support}}- Add Serde serialization support\n{{/if}}\
            {{#if benchmark}}- Include criterion benchmarks\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("module_name", "Name of the Rust module"),
            TemplateVariable::text("module_purpose", "What this module does"),
            TemplateVariable::choice(
                "visibility",
                "Module visibility",
                vec![
                    "public".to_string(),
                    "private".to_string(),
                    "pub(crate)".to_string(),
                ],
            ),
            TemplateVariable::text("traits", "Traits to implement").optional(),
            TemplateVariable::text("structs", "Main structs in the module").optional(),
            TemplateVariable::boolean("async_support", "Include async support"),
            TemplateVariable::boolean("serde_support", "Add Serde support"),
            TemplateVariable::boolean("benchmark", "Include benchmarks"),
        ])
        .with_target_files(vec![
            "src/{{module_name}}/mod.rs".to_string(),
            "src/{{module_name}}/types.rs".to_string(),
            "benches/{{module_name}}_bench.rs".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:security_review (lines 816-872) ---[0m
    pub fn security_review() -> Template {
        Template::new(
            "security-review",
            "Security Review",
            "Conduct a comprehensive security review of code or system",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "security".to_string(),
            "review".to_string(),
            "audit".to_string(),
        ])
        .with_task_description(
            "Conduct security review of {{review_target}} focusing on {{security_aspects}}",
        )
        .with_task_details(
            "## Security Review Scope\n\n\
            - **Target**: {{review_target}}\n\
            - **Focus Areas**: {{security_aspects}}\n\
            - **Review Type**: {{review_type}}\n\n\
            ## Security Checklist\n\n\
            - Authentication and authorization mechanisms\n\
            - Input validation and sanitization\n\
            - SQL injection and XSS vulnerabilities\n\
            - Secure data storage and transmission\n\
            - Error handling and information disclosure\n\
            - Dependencies and supply chain security\n\
            {{#if crypto}}- Cryptographic implementations\n{{/if}}\
            {{#if api_security}}- API security and rate limiting\n{{/if}}\
            {{#if infrastructure}}- Infrastructure security configuration\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Review)
        .with_duration(120)
        .with_variables(vec![
            TemplateVariable::text("review_target", "What is being reviewed"),
            TemplateVariable::text("security_aspects", "Specific security concerns"),
            TemplateVariable::choice(
                "review_type",
                "Type of security review",
                vec![
                    "Code Review".to_string(),
                    "Architecture Review".to_string(),
                    "Penetration Test".to_string(),
                    "Compliance Audit".to_string(),
                ],
            ),
            TemplateVariable::boolean("crypto", "Review cryptographic code"),
            TemplateVariable::boolean("api_security", "Review API security"),
            TemplateVariable::boolean("infrastructure", "Review infrastructure"),
        ])
        .with_target_files(vec![
            "security-review-{{review_target}}.md".to_string(),
            "security-checklist-{{review_target}}.md".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:816-872 method security_review <-> ./crates/ccswarm/src/template/predefined.rs:931-987 method code_refactor
  Similarity: 96.93%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:security_review (lines 816-872) ---[0m
    pub fn security_review() -> Template {
        Template::new(
            "security-review",
            "Security Review",
            "Conduct a comprehensive security review of code or system",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "security".to_string(),
            "review".to_string(),
            "audit".to_string(),
        ])
        .with_task_description(
            "Conduct security review of {{review_target}} focusing on {{security_aspects}}",
        )
        .with_task_details(
            "## Security Review Scope\n\n\
            - **Target**: {{review_target}}\n\
            - **Focus Areas**: {{security_aspects}}\n\
            - **Review Type**: {{review_type}}\n\n\
            ## Security Checklist\n\n\
            - Authentication and authorization mechanisms\n\
            - Input validation and sanitization\n\
            - SQL injection and XSS vulnerabilities\n\
            - Secure data storage and transmission\n\
            - Error handling and information disclosure\n\
            - Dependencies and supply chain security\n\
            {{#if crypto}}- Cryptographic implementations\n{{/if}}\
            {{#if api_security}}- API security and rate limiting\n{{/if}}\
            {{#if infrastructure}}- Infrastructure security configuration\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Review)
        .with_duration(120)
        .with_variables(vec![
            TemplateVariable::text("review_target", "What is being reviewed"),
            TemplateVariable::text("security_aspects", "Specific security concerns"),
            TemplateVariable::choice(
                "review_type",
                "Type of security review",
                vec![
                    "Code Review".to_string(),
                    "Architecture Review".to_string(),
                    "Penetration Test".to_string(),
                    "Compliance Audit".to_string(),
                ],
            ),
            TemplateVariable::boolean("crypto", "Review cryptographic code"),
            TemplateVariable::boolean("api_security", "Review API security"),
            TemplateVariable::boolean("infrastructure", "Review infrastructure"),
        ])
        .with_target_files(vec![
            "security-review-{{review_target}}.md".to_string(),
            "security-checklist-{{review_target}}.md".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:code_refactor (lines 931-987) ---[0m
    pub fn code_refactor() -> Template {
        Template::new(
            "code-refactor",
            "Code Refactoring",
            "Refactor code to improve maintainability and design",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "refactor".to_string(),
            "maintainability".to_string(),
            "design".to_string(),
        ])
        .with_task_description("Refactor {{refactor_target}} to {{refactor_goal}}")
        .with_task_details(
            "## Refactoring Plan\n\n\
            - **Target**: {{refactor_target}}\n\
            - **Goal**: {{refactor_goal}}\n\
            - **Refactor Type**: {{refactor_type}}\n\
            {{#if design_patterns}}- **Design Patterns**: {{design_patterns}}\n{{/if}}\n\n\
            ## Refactoring Steps\n\n\
            - Analyze current code structure and identify issues\n\
            - Create comprehensive test coverage before refactoring\n\
            - Apply refactoring techniques incrementally\n\
            - Verify tests pass after each step\n\
            - Update documentation to reflect changes\n\
            {{#if breaking_changes}}- Document any breaking changes\n{{/if}}\
            {{#if performance_impact}}- Measure performance impact\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(80)
        .with_variables(vec![
            TemplateVariable::text("refactor_target", "Code to be refactored"),
            TemplateVariable::text("refactor_goal", "What the refactoring should achieve"),
            TemplateVariable::choice(
                "refactor_type",
                "Type of refactoring",
                vec![
                    "Extract Method".to_string(),
                    "Extract Class".to_string(),
                    "Move Method".to_string(),
                    "Rename".to_string(),
                    "Simplify Conditional".to_string(),
                    "Remove Duplication".to_string(),
                    "Improve Abstraction".to_string(),
                ],
            ),
            TemplateVariable::text("design_patterns", "Design patterns to apply").optional(),
            TemplateVariable::boolean("breaking_changes", "May introduce breaking changes"),
            TemplateVariable::boolean("performance_impact", "Measure performance impact"),
        ])
        .with_target_files(vec![
            "refactor-plan-{{refactor_target}}.md".to_string(),
            "tests/refactor/{{refactor_target}}_test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:442-498 method bug_fix <-> ./crates/ccswarm/src/template/predefined.rs:816-872 method security_review
  Similarity: 96.89%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:bug_fix (lines 442-498) ---[0m
    pub fn bug_fix() -> Template {
        Template::new(
            "bug-fix",
            "Bug Fix",
            "Fix a reported bug with proper testing and documentation",
            TemplateCategory::BugFix,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "bug".to_string(),
            "fix".to_string(),
            "maintenance".to_string(),
        ])
        .with_task_description("Fix bug: {{bug_description}} ({{bug_severity}})")
        .with_task_details(
            "## Bug Report\n\n\
            - **Description**: {{bug_description}}\n\
            - **Severity**: {{bug_severity}}\n\
            {{#if reproduction_steps}}- **Reproduction**: {{reproduction_steps}}\n{{/if}}\
            {{#if affected_components}}- **Affected Components**: {{affected_components}}\n{{/if}}\
            {{#if user_impact}}- **User Impact**: {{user_impact}}\n{{/if}}\n\n\
            ## Fix Requirements\n\n\
            - Reproduce the bug in a test environment\n\
            - Identify root cause through debugging\n\
            - Implement minimal fix that addresses root cause\n\
            - Add regression tests to prevent recurrence\n\
            - Verify fix doesn't introduce new issues\n\
            {{#if documentation}}- Update relevant documentation\n{{/if}}\
            {{#if release_notes}}- Add to release notes\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Bugfix)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("bug_description", "Clear description of the bug"),
            TemplateVariable::choice(
                "bug_severity",
                "Bug severity level",
                vec![
                    "Critical".to_string(),
                    "High".to_string(),
                    "Medium".to_string(),
                    "Low".to_string(),
                ],
            ),
            TemplateVariable::text("reproduction_steps", "Steps to reproduce the bug").optional(),
            TemplateVariable::text("affected_components", "Components affected by the bug")
                .optional(),
            TemplateVariable::text("user_impact", "How this affects users").optional(),
            TemplateVariable::boolean("documentation", "Update documentation"),
            TemplateVariable::boolean("release_notes", "Add to release notes"),
        ])
        .with_target_files(vec![
            "src/{{affected_components}}/fix.{{file_extension}}".to_string(),
            "tests/regression/{{bug_description}}.test.{{file_extension}}".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:security_review (lines 816-872) ---[0m
    pub fn security_review() -> Template {
        Template::new(
            "security-review",
            "Security Review",
            "Conduct a comprehensive security review of code or system",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "security".to_string(),
            "review".to_string(),
            "audit".to_string(),
        ])
        .with_task_description(
            "Conduct security review of {{review_target}} focusing on {{security_aspects}}",
        )
        .with_task_details(
            "## Security Review Scope\n\n\
            - **Target**: {{review_target}}\n\
            - **Focus Areas**: {{security_aspects}}\n\
            - **Review Type**: {{review_type}}\n\n\
            ## Security Checklist\n\n\
            - Authentication and authorization mechanisms\n\
            - Input validation and sanitization\n\
            - SQL injection and XSS vulnerabilities\n\
            - Secure data storage and transmission\n\
            - Error handling and information disclosure\n\
            - Dependencies and supply chain security\n\
            {{#if crypto}}- Cryptographic implementations\n{{/if}}\
            {{#if api_security}}- API security and rate limiting\n{{/if}}\
            {{#if infrastructure}}- Infrastructure security configuration\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Review)
        .with_duration(120)
        .with_variables(vec![
            TemplateVariable::text("review_target", "What is being reviewed"),
            TemplateVariable::text("security_aspects", "Specific security concerns"),
            TemplateVariable::choice(
                "review_type",
                "Type of security review",
                vec![
                    "Code Review".to_string(),
                    "Architecture Review".to_string(),
                    "Penetration Test".to_string(),
                    "Compliance Audit".to_string(),
                ],
            ),
            TemplateVariable::boolean("crypto", "Review cryptographic code"),
            TemplateVariable::boolean("api_security", "Review API security"),
            TemplateVariable::boolean("infrastructure", "Review infrastructure"),
        ])
        .with_target_files(vec![
            "security-review-{{review_target}}.md".to_string(),
            "security-checklist-{{review_target}}.md".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:111-166 method react_hook <-> ./crates/ccswarm/src/template/predefined.rs:253-310 method database_migration
  Similarity: 96.80%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_hook (lines 111-166) ---[0m
    pub fn react_hook() -> Template {
        Template::new(
            "react-hook",
            "React Custom Hook",
            "Create a custom React hook with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "react".to_string(),
            "hook".to_string(),
            "typescript".to_string(),
        ])
        .with_task_description(
            "Create a custom React hook named {{hook_name}} for {{hook_purpose}}",
        )
        .with_task_details(
            "## Hook Specification\n\n\
            - **Name**: {{hook_name}}\n\
            - **Purpose**: {{hook_purpose}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if parameters}}- **Parameters**: {{parameters}}\n{{/if}}\
            {{#if return_type}}- **Returns**: {{return_type}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React hooks rules and conventions\n\
            - Include proper dependency arrays for useEffect\n\
            - Add comprehensive JSDoc comments\n\
            - Handle cleanup and memory leaks\n\
            {{#if testing}}- Create unit tests with React Hooks Testing Library\n{{/if}}\
            {{#if memoization}}- Use useMemo/useCallback for performance optimization\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(30)
        .with_variables(vec![
            TemplateVariable::text(
                "hook_name",
                "Name of the custom hook (e.g., useLocalStorage)",
            ),
            TemplateVariable::text("hook_purpose", "What the hook does"),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the hook",
                "src/hooks/{{hook_name}}.ts",
            ),
            TemplateVariable::text("parameters", "Hook parameters (optional)").optional(),
            TemplateVariable::text("return_type", "Hook return type (optional)").optional(),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("memoization", "Include performance optimizations"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.ts".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:database_migration (lines 253-310) ---[0m
    pub fn database_migration() -> Template {
        Template::new(
            "database-migration",
            "Database Migration",
            "Create a database migration script",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "database".to_string(),
            "migration".to_string(),
            "schema".to_string(),
        ])
        .with_task_description("Create database migration to {{migration_purpose}}")
        .with_task_details(
            "## Migration Details\n\n\
            - **Purpose**: {{migration_purpose}}\n\
            - **Type**: {{migration_type}}\n\
            {{#if table_name}}- **Table**: {{table_name}}\n{{/if}}\
            {{#if columns}}- **Columns**: {{columns}}\n{{/if}}\
            {{#if indexes}}- **Indexes**: {{indexes}}\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Write both up and down migrations\n\
            - Include proper constraints and indexes\n\
            - Handle existing data appropriately\n\
            - Add comments for complex operations\n\
            - Test migration on sample data\n\
            {{#if backup}}- Create backup before running\n{{/if}}\
            {{#if rollback_plan}}- Document rollback procedure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(40)
        .with_variables(vec![
            TemplateVariable::text("migration_purpose", "What this migration accomplishes"),
            TemplateVariable::choice(
                "migration_type",
                "Type of migration",
                vec![
                    "Create Table".to_string(),
                    "Alter Table".to_string(),
                    "Drop Table".to_string(),
                    "Add Column".to_string(),
                    "Drop Column".to_string(),
                    "Create Index".to_string(),
                    "Data Migration".to_string(),
                ],
            ),
            TemplateVariable::text("table_name", "Table name (if applicable)").optional(),
            TemplateVariable::text("columns", "Column definitions (optional)").optional(),
            TemplateVariable::text("indexes", "Index definitions (optional)").optional(),
            TemplateVariable::boolean("backup", "Create backup before migration"),
            TemplateVariable::boolean("rollback_plan", "Document rollback procedure"),
        ])
        .with_target_files(vec![
            "migrations/{{current_datetime}}_{{migration_purpose}}.sql".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:111-166 method react_hook <-> ./crates/ccswarm/src/template/predefined.rs:442-498 method bug_fix
  Similarity: 97.48%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_hook (lines 111-166) ---[0m
    pub fn react_hook() -> Template {
        Template::new(
            "react-hook",
            "React Custom Hook",
            "Create a custom React hook with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "react".to_string(),
            "hook".to_string(),
            "typescript".to_string(),
        ])
        .with_task_description(
            "Create a custom React hook named {{hook_name}} for {{hook_purpose}}",
        )
        .with_task_details(
            "## Hook Specification\n\n\
            - **Name**: {{hook_name}}\n\
            - **Purpose**: {{hook_purpose}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if parameters}}- **Parameters**: {{parameters}}\n{{/if}}\
            {{#if return_type}}- **Returns**: {{return_type}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React hooks rules and conventions\n\
            - Include proper dependency arrays for useEffect\n\
            - Add comprehensive JSDoc comments\n\
            - Handle cleanup and memory leaks\n\
            {{#if testing}}- Create unit tests with React Hooks Testing Library\n{{/if}}\
            {{#if memoization}}- Use useMemo/useCallback for performance optimization\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(30)
        .with_variables(vec![
            TemplateVariable::text(
                "hook_name",
                "Name of the custom hook (e.g., useLocalStorage)",
            ),
            TemplateVariable::text("hook_purpose", "What the hook does"),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the hook",
                "src/hooks/{{hook_name}}.ts",
            ),
            TemplateVariable::text("parameters", "Hook parameters (optional)").optional(),
            TemplateVariable::text("return_type", "Hook return type (optional)").optional(),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("memoization", "Include performance optimizations"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.ts".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:bug_fix (lines 442-498) ---[0m
    pub fn bug_fix() -> Template {
        Template::new(
            "bug-fix",
            "Bug Fix",
            "Fix a reported bug with proper testing and documentation",
            TemplateCategory::BugFix,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "bug".to_string(),
            "fix".to_string(),
            "maintenance".to_string(),
        ])
        .with_task_description("Fix bug: {{bug_description}} ({{bug_severity}})")
        .with_task_details(
            "## Bug Report\n\n\
            - **Description**: {{bug_description}}\n\
            - **Severity**: {{bug_severity}}\n\
            {{#if reproduction_steps}}- **Reproduction**: {{reproduction_steps}}\n{{/if}}\
            {{#if affected_components}}- **Affected Components**: {{affected_components}}\n{{/if}}\
            {{#if user_impact}}- **User Impact**: {{user_impact}}\n{{/if}}\n\n\
            ## Fix Requirements\n\n\
            - Reproduce the bug in a test environment\n\
            - Identify root cause through debugging\n\
            - Implement minimal fix that addresses root cause\n\
            - Add regression tests to prevent recurrence\n\
            - Verify fix doesn't introduce new issues\n\
            {{#if documentation}}- Update relevant documentation\n{{/if}}\
            {{#if release_notes}}- Add to release notes\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Bugfix)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("bug_description", "Clear description of the bug"),
            TemplateVariable::choice(
                "bug_severity",
                "Bug severity level",
                vec![
                    "Critical".to_string(),
                    "High".to_string(),
                    "Medium".to_string(),
                    "Low".to_string(),
                ],
            ),
            TemplateVariable::text("reproduction_steps", "Steps to reproduce the bug").optional(),
            TemplateVariable::text("affected_components", "Components affected by the bug")
                .optional(),
            TemplateVariable::text("user_impact", "How this affects users").optional(),
            TemplateVariable::boolean("documentation", "Update documentation"),
            TemplateVariable::boolean("release_notes", "Add to release notes"),
        ])
        .with_target_files(vec![
            "src/{{affected_components}}/fix.{{file_extension}}".to_string(),
            "tests/regression/{{bug_description}}.test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:381-439 method integration_test <-> ./crates/ccswarm/src/template/predefined.rs:875-928 method performance_optimization
  Similarity: 97.11%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:integration_test (lines 381-439) ---[0m
    pub fn integration_test() -> Template {
        Template::new(
            "integration-test",
            "Integration Test",
            "Create integration tests for system components",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "integration".to_string(),
            "e2e".to_string(),
        ])
        .with_task_description(
            "Create integration tests for {{system_component}} testing {{integration_points}}",
        )
        .with_task_details(
            "## Integration Test Specification\n\n\
            - **Component**: {{system_component}}\n\
            - **Integration Points**: {{integration_points}}\n\
            - **Test Environment**: {{test_environment}}\n\
            {{#if database}}- **Database**: Test database setup required\n{{/if}}\
            {{#if external_services}}- **External Services**: {{external_services}}\n{{/if}}\n\n\
            ## Test Requirements\n\n\
            - Set up test environment and data\n\
            - Test end-to-end workflows\n\
            - Verify data consistency across components\n\
            - Test error propagation and recovery\n\
            - Clean up test data after execution\n\
            {{#if performance}}- Measure integration performance\n{{/if}}\
            {{#if security}}- Verify security boundaries\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Testing)
        .with_duration(75)
        .with_variables(vec![
            TemplateVariable::text("system_component", "System component being tested"),
            TemplateVariable::text("integration_points", "What integrations to test"),
            TemplateVariable::choice(
                "test_environment",
                "Test environment",
                vec![
                    "Docker Compose".to_string(),
                    "Testcontainers".to_string(),
                    "In-memory".to_string(),
                    "Staging".to_string(),
                ],
            ),
            TemplateVariable::boolean("database", "Includes database testing"),
            TemplateVariable::text("external_services", "External services involved").optional(),
            TemplateVariable::boolean("performance", "Include performance testing"),
            TemplateVariable::boolean("security", "Include security testing"),
        ])
        .with_target_files(vec![
            "tests/integration/{{system_component}}_integration.test.{{file_extension}}"
                .to_string(),
            "tests/fixtures/{{system_component}}_data.json".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:performance_optimization (lines 875-928) ---[0m
    pub fn performance_optimization() -> Template {
        Template::new(
            "performance-optimization",
            "Performance Optimization",
            "Optimize performance of a system component",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "performance".to_string(),
            "optimization".to_string(),
            "profiling".to_string(),
        ])
        .with_task_description(
            "Optimize performance of {{optimization_target}} to improve {{performance_metric}}",
        )
        .with_task_details(
            "## Performance Optimization Plan\n\n\
            - **Target**: {{optimization_target}}\n\
            - **Metric**: {{performance_metric}}\n\
            - **Current Performance**: {{current_baseline}}\n\
            - **Target Performance**: {{target_goal}}\n\n\
            ## Optimization Approach\n\n\
            - Profile current performance with appropriate tools\n\
            - Identify bottlenecks and hot paths\n\
            - Implement targeted optimizations\n\
            - Measure impact of each optimization\n\
            - Verify no functional regressions\n\
            {{#if caching}}- Implement caching strategies\n{{/if}}\
            {{#if database}}- Optimize database queries and indexes\n{{/if}}\
            {{#if memory}}- Reduce memory allocation and fragmentation\n{{/if}}\
            {{#if concurrency}}- Improve concurrency and parallelization\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("optimization_target", "Component to optimize"),
            TemplateVariable::text(
                "performance_metric",
                "Metric to improve (latency, throughput, etc.)",
            ),
            TemplateVariable::text("current_baseline", "Current performance baseline").optional(),
            TemplateVariable::text("target_goal", "Performance improvement goal").optional(),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("database", "Optimize database operations"),
            TemplateVariable::boolean("memory", "Optimize memory usage"),
            TemplateVariable::boolean("concurrency", "Improve concurrency"),
        ])
        .with_target_files(vec![
            "performance-analysis-{{optimization_target}}.md".to_string(),
            "benchmarks/{{optimization_target}}_bench.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:111-166 method react_hook <-> ./crates/ccswarm/src/template/predefined.rs:704-761 method rust_module
  Similarity: 96.25%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_hook (lines 111-166) ---[0m
    pub fn react_hook() -> Template {
        Template::new(
            "react-hook",
            "React Custom Hook",
            "Create a custom React hook with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "react".to_string(),
            "hook".to_string(),
            "typescript".to_string(),
        ])
        .with_task_description(
            "Create a custom React hook named {{hook_name}} for {{hook_purpose}}",
        )
        .with_task_details(
            "## Hook Specification\n\n\
            - **Name**: {{hook_name}}\n\
            - **Purpose**: {{hook_purpose}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if parameters}}- **Parameters**: {{parameters}}\n{{/if}}\
            {{#if return_type}}- **Returns**: {{return_type}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React hooks rules and conventions\n\
            - Include proper dependency arrays for useEffect\n\
            - Add comprehensive JSDoc comments\n\
            - Handle cleanup and memory leaks\n\
            {{#if testing}}- Create unit tests with React Hooks Testing Library\n{{/if}}\
            {{#if memoization}}- Use useMemo/useCallback for performance optimization\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(30)
        .with_variables(vec![
            TemplateVariable::text(
                "hook_name",
                "Name of the custom hook (e.g., useLocalStorage)",
            ),
            TemplateVariable::text("hook_purpose", "What the hook does"),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the hook",
                "src/hooks/{{hook_name}}.ts",
            ),
            TemplateVariable::text("parameters", "Hook parameters (optional)").optional(),
            TemplateVariable::text("return_type", "Hook return type (optional)").optional(),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("memoization", "Include performance optimizations"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.ts".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_module (lines 704-761) ---[0m
    pub fn rust_module() -> Template {
        Template::new(
            "rust-module",
            "Rust Module",
            "Create a new Rust module with proper structure and documentation",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "module".to_string(),
            "backend".to_string(),
        ])
        .with_task_description("Create Rust module {{module_name}} for {{module_purpose}}")
        .with_task_details(
            "## Rust Module Specification\n\n\
            - **Module**: {{module_name}}\n\
            - **Purpose**: {{module_purpose}}\n\
            - **Visibility**: {{visibility}}\n\
            {{#if traits}}- **Traits**: {{traits}}\n{{/if}}\
            {{#if structs}}- **Structs**: {{structs}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Follow Rust naming conventions\n\
            - Add comprehensive rustdoc comments\n\
            - Implement proper error handling with Result<T, E>\n\
            - Use appropriate visibility modifiers\n\
            - Include unit tests in #[cfg(test)] module\n\
            {{#if async_support}}- Implement async/await patterns\n{{/if}}\
            {{#if serde_support}}- Add Serde serialization support\n{{/if}}\
            {{#if benchmark}}- Include criterion benchmarks\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("module_name", "Name of the Rust module"),
            TemplateVariable::text("module_purpose", "What this module does"),
            TemplateVariable::choice(
                "visibility",
                "Module visibility",
                vec![
                    "public".to_string(),
                    "private".to_string(),
                    "pub(crate)".to_string(),
                ],
            ),
            TemplateVariable::text("traits", "Traits to implement").optional(),
            TemplateVariable::text("structs", "Main structs in the module").optional(),
            TemplateVariable::boolean("async_support", "Include async support"),
            TemplateVariable::boolean("serde_support", "Add Serde support"),
            TemplateVariable::boolean("benchmark", "Include benchmarks"),
        ])
        .with_target_files(vec![
            "src/{{module_name}}/mod.rs".to_string(),
            "src/{{module_name}}/types.rs".to_string(),
            "benches/{{module_name}}_bench.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:111-166 method react_hook <-> ./crates/ccswarm/src/template/predefined.rs:931-987 method code_refactor
  Similarity: 96.96%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_hook (lines 111-166) ---[0m
    pub fn react_hook() -> Template {
        Template::new(
            "react-hook",
            "React Custom Hook",
            "Create a custom React hook with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "react".to_string(),
            "hook".to_string(),
            "typescript".to_string(),
        ])
        .with_task_description(
            "Create a custom React hook named {{hook_name}} for {{hook_purpose}}",
        )
        .with_task_details(
            "## Hook Specification\n\n\
            - **Name**: {{hook_name}}\n\
            - **Purpose**: {{hook_purpose}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if parameters}}- **Parameters**: {{parameters}}\n{{/if}}\
            {{#if return_type}}- **Returns**: {{return_type}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React hooks rules and conventions\n\
            - Include proper dependency arrays for useEffect\n\
            - Add comprehensive JSDoc comments\n\
            - Handle cleanup and memory leaks\n\
            {{#if testing}}- Create unit tests with React Hooks Testing Library\n{{/if}}\
            {{#if memoization}}- Use useMemo/useCallback for performance optimization\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(30)
        .with_variables(vec![
            TemplateVariable::text(
                "hook_name",
                "Name of the custom hook (e.g., useLocalStorage)",
            ),
            TemplateVariable::text("hook_purpose", "What the hook does"),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the hook",
                "src/hooks/{{hook_name}}.ts",
            ),
            TemplateVariable::text("parameters", "Hook parameters (optional)").optional(),
            TemplateVariable::text("return_type", "Hook return type (optional)").optional(),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("memoization", "Include performance optimizations"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.ts".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:code_refactor (lines 931-987) ---[0m
    pub fn code_refactor() -> Template {
        Template::new(
            "code-refactor",
            "Code Refactoring",
            "Refactor code to improve maintainability and design",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "refactor".to_string(),
            "maintainability".to_string(),
            "design".to_string(),
        ])
        .with_task_description("Refactor {{refactor_target}} to {{refactor_goal}}")
        .with_task_details(
            "## Refactoring Plan\n\n\
            - **Target**: {{refactor_target}}\n\
            - **Goal**: {{refactor_goal}}\n\
            - **Refactor Type**: {{refactor_type}}\n\
            {{#if design_patterns}}- **Design Patterns**: {{design_patterns}}\n{{/if}}\n\n\
            ## Refactoring Steps\n\n\
            - Analyze current code structure and identify issues\n\
            - Create comprehensive test coverage before refactoring\n\
            - Apply refactoring techniques incrementally\n\
            - Verify tests pass after each step\n\
            - Update documentation to reflect changes\n\
            {{#if breaking_changes}}- Document any breaking changes\n{{/if}}\
            {{#if performance_impact}}- Measure performance impact\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(80)
        .with_variables(vec![
            TemplateVariable::text("refactor_target", "Code to be refactored"),
            TemplateVariable::text("refactor_goal", "What the refactoring should achieve"),
            TemplateVariable::choice(
                "refactor_type",
                "Type of refactoring",
                vec![
                    "Extract Method".to_string(),
                    "Extract Class".to_string(),
                    "Move Method".to_string(),
                    "Rename".to_string(),
                    "Simplify Conditional".to_string(),
                    "Remove Duplication".to_string(),
                    "Improve Abstraction".to_string(),
                ],
            ),
            TemplateVariable::text("design_patterns", "Design patterns to apply").optional(),
            TemplateVariable::boolean("breaking_changes", "May introduce breaking changes"),
            TemplateVariable::boolean("performance_impact", "Measure performance impact"),
        ])
        .with_target_files(vec![
            "refactor-plan-{{refactor_target}}.md".to_string(),
            "tests/refactor/{{refactor_target}}_test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:111-166 method react_hook <-> ./crates/ccswarm/src/template/predefined.rs:816-872 method security_review
  Similarity: 96.80%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_hook (lines 111-166) ---[0m
    pub fn react_hook() -> Template {
        Template::new(
            "react-hook",
            "React Custom Hook",
            "Create a custom React hook with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "react".to_string(),
            "hook".to_string(),
            "typescript".to_string(),
        ])
        .with_task_description(
            "Create a custom React hook named {{hook_name}} for {{hook_purpose}}",
        )
        .with_task_details(
            "## Hook Specification\n\n\
            - **Name**: {{hook_name}}\n\
            - **Purpose**: {{hook_purpose}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if parameters}}- **Parameters**: {{parameters}}\n{{/if}}\
            {{#if return_type}}- **Returns**: {{return_type}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React hooks rules and conventions\n\
            - Include proper dependency arrays for useEffect\n\
            - Add comprehensive JSDoc comments\n\
            - Handle cleanup and memory leaks\n\
            {{#if testing}}- Create unit tests with React Hooks Testing Library\n{{/if}}\
            {{#if memoization}}- Use useMemo/useCallback for performance optimization\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(30)
        .with_variables(vec![
            TemplateVariable::text(
                "hook_name",
                "Name of the custom hook (e.g., useLocalStorage)",
            ),
            TemplateVariable::text("hook_purpose", "What the hook does"),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the hook",
                "src/hooks/{{hook_name}}.ts",
            ),
            TemplateVariable::text("parameters", "Hook parameters (optional)").optional(),
            TemplateVariable::text("return_type", "Hook return type (optional)").optional(),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("memoization", "Include performance optimizations"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.ts".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:security_review (lines 816-872) ---[0m
    pub fn security_review() -> Template {
        Template::new(
            "security-review",
            "Security Review",
            "Conduct a comprehensive security review of code or system",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "security".to_string(),
            "review".to_string(),
            "audit".to_string(),
        ])
        .with_task_description(
            "Conduct security review of {{review_target}} focusing on {{security_aspects}}",
        )
        .with_task_details(
            "## Security Review Scope\n\n\
            - **Target**: {{review_target}}\n\
            - **Focus Areas**: {{security_aspects}}\n\
            - **Review Type**: {{review_type}}\n\n\
            ## Security Checklist\n\n\
            - Authentication and authorization mechanisms\n\
            - Input validation and sanitization\n\
            - SQL injection and XSS vulnerabilities\n\
            - Secure data storage and transmission\n\
            - Error handling and information disclosure\n\
            - Dependencies and supply chain security\n\
            {{#if crypto}}- Cryptographic implementations\n{{/if}}\
            {{#if api_security}}- API security and rate limiting\n{{/if}}\
            {{#if infrastructure}}- Infrastructure security configuration\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Review)
        .with_duration(120)
        .with_variables(vec![
            TemplateVariable::text("review_target", "What is being reviewed"),
            TemplateVariable::text("security_aspects", "Specific security concerns"),
            TemplateVariable::choice(
                "review_type",
                "Type of security review",
                vec![
                    "Code Review".to_string(),
                    "Architecture Review".to_string(),
                    "Penetration Test".to_string(),
                    "Compliance Audit".to_string(),
                ],
            ),
            TemplateVariable::boolean("crypto", "Review cryptographic code"),
            TemplateVariable::boolean("api_security", "Review API security"),
            TemplateVariable::boolean("infrastructure", "Review infrastructure"),
        ])
        .with_target_files(vec![
            "security-review-{{review_target}}.md".to_string(),
            "security-checklist-{{review_target}}.md".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:501-559 method docker_service <-> ./crates/ccswarm/src/template/predefined.rs:875-928 method performance_optimization
  Similarity: 96.25%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:docker_service (lines 501-559) ---[0m
    pub fn docker_service() -> Template {
        Template::new(
            "docker-service",
            "Docker Service",
            "Create a Docker container configuration for a service",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "docker".to_string(),
            "container".to_string(),
            "devops".to_string(),
        ])
        .with_task_description("Create Docker configuration for {{service_name}} service")
        .with_task_details(
            "## Docker Service Configuration\n\n\
            - **Service**: {{service_name}}\n\
            - **Base Image**: {{base_image}}\n\
            {{#if ports}}- **Ports**: {{ports}}\n{{/if}}\
            {{#if volumes}}- **Volumes**: {{volumes}}\n{{/if}}\
            {{#if environment}}- **Environment**: {{environment}}\n{{/if}}\n\n\
            ## Container Requirements\n\n\
            - Multi-stage build for optimization\n\
            - Non-root user for security\n\
            - Health check configuration\n\
            - Proper signal handling\n\
            - Minimal attack surface\n\
            {{#if secrets}}- Secure secret management\n{{/if}}\
            {{#if monitoring}}- Monitoring and logging setup\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("service_name", "Name of the service"),
            TemplateVariable::choice(
                "base_image",
                "Base Docker image",
                vec![
                    "alpine".to_string(),
                    "ubuntu".to_string(),
                    "node:alpine".to_string(),
                    "python:alpine".to_string(),
                    "rust:alpine".to_string(),
                    "nginx:alpine".to_string(),
                ],
            ),
            TemplateVariable::text("ports", "Port mappings (e.g., 8080:8080)").optional(),
            TemplateVariable::text("volumes", "Volume mounts").optional(),
            TemplateVariable::text("environment", "Environment variables").optional(),
            TemplateVariable::boolean("secrets", "Include secret management"),
            TemplateVariable::boolean("monitoring", "Add monitoring setup"),
        ])
        .with_target_files(vec![
            "Dockerfile".to_string(),
            "docker-compose.yml".to_string(),
            ".dockerignore".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:performance_optimization (lines 875-928) ---[0m
    pub fn performance_optimization() -> Template {
        Template::new(
            "performance-optimization",
            "Performance Optimization",
            "Optimize performance of a system component",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "performance".to_string(),
            "optimization".to_string(),
            "profiling".to_string(),
        ])
        .with_task_description(
            "Optimize performance of {{optimization_target}} to improve {{performance_metric}}",
        )
        .with_task_details(
            "## Performance Optimization Plan\n\n\
            - **Target**: {{optimization_target}}\n\
            - **Metric**: {{performance_metric}}\n\
            - **Current Performance**: {{current_baseline}}\n\
            - **Target Performance**: {{target_goal}}\n\n\
            ## Optimization Approach\n\n\
            - Profile current performance with appropriate tools\n\
            - Identify bottlenecks and hot paths\n\
            - Implement targeted optimizations\n\
            - Measure impact of each optimization\n\
            - Verify no functional regressions\n\
            {{#if caching}}- Implement caching strategies\n{{/if}}\
            {{#if database}}- Optimize database queries and indexes\n{{/if}}\
            {{#if memory}}- Reduce memory allocation and fragmentation\n{{/if}}\
            {{#if concurrency}}- Improve concurrency and parallelization\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("optimization_target", "Component to optimize"),
            TemplateVariable::text(
                "performance_metric",
                "Metric to improve (latency, throughput, etc.)",
            ),
            TemplateVariable::text("current_baseline", "Current performance baseline").optional(),
            TemplateVariable::text("target_goal", "Performance improvement goal").optional(),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("database", "Optimize database operations"),
            TemplateVariable::boolean("memory", "Optimize memory usage"),
            TemplateVariable::boolean("concurrency", "Improve concurrency"),
        ])
        .with_target_files(vec![
            "performance-analysis-{{optimization_target}}.md".to_string(),
            "benchmarks/{{optimization_target}}_bench.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:253-310 method database_migration <-> ./crates/ccswarm/src/template/predefined.rs:875-928 method performance_optimization
  Similarity: 96.24%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:database_migration (lines 253-310) ---[0m
    pub fn database_migration() -> Template {
        Template::new(
            "database-migration",
            "Database Migration",
            "Create a database migration script",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "database".to_string(),
            "migration".to_string(),
            "schema".to_string(),
        ])
        .with_task_description("Create database migration to {{migration_purpose}}")
        .with_task_details(
            "## Migration Details\n\n\
            - **Purpose**: {{migration_purpose}}\n\
            - **Type**: {{migration_type}}\n\
            {{#if table_name}}- **Table**: {{table_name}}\n{{/if}}\
            {{#if columns}}- **Columns**: {{columns}}\n{{/if}}\
            {{#if indexes}}- **Indexes**: {{indexes}}\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Write both up and down migrations\n\
            - Include proper constraints and indexes\n\
            - Handle existing data appropriately\n\
            - Add comments for complex operations\n\
            - Test migration on sample data\n\
            {{#if backup}}- Create backup before running\n{{/if}}\
            {{#if rollback_plan}}- Document rollback procedure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(40)
        .with_variables(vec![
            TemplateVariable::text("migration_purpose", "What this migration accomplishes"),
            TemplateVariable::choice(
                "migration_type",
                "Type of migration",
                vec![
                    "Create Table".to_string(),
                    "Alter Table".to_string(),
                    "Drop Table".to_string(),
                    "Add Column".to_string(),
                    "Drop Column".to_string(),
                    "Create Index".to_string(),
                    "Data Migration".to_string(),
                ],
            ),
            TemplateVariable::text("table_name", "Table name (if applicable)").optional(),
            TemplateVariable::text("columns", "Column definitions (optional)").optional(),
            TemplateVariable::text("indexes", "Index definitions (optional)").optional(),
            TemplateVariable::boolean("backup", "Create backup before migration"),
            TemplateVariable::boolean("rollback_plan", "Document rollback procedure"),
        ])
        .with_target_files(vec![
            "migrations/{{current_datetime}}_{{migration_purpose}}.sql".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:performance_optimization (lines 875-928) ---[0m
    pub fn performance_optimization() -> Template {
        Template::new(
            "performance-optimization",
            "Performance Optimization",
            "Optimize performance of a system component",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "performance".to_string(),
            "optimization".to_string(),
            "profiling".to_string(),
        ])
        .with_task_description(
            "Optimize performance of {{optimization_target}} to improve {{performance_metric}}",
        )
        .with_task_details(
            "## Performance Optimization Plan\n\n\
            - **Target**: {{optimization_target}}\n\
            - **Metric**: {{performance_metric}}\n\
            - **Current Performance**: {{current_baseline}}\n\
            - **Target Performance**: {{target_goal}}\n\n\
            ## Optimization Approach\n\n\
            - Profile current performance with appropriate tools\n\
            - Identify bottlenecks and hot paths\n\
            - Implement targeted optimizations\n\
            - Measure impact of each optimization\n\
            - Verify no functional regressions\n\
            {{#if caching}}- Implement caching strategies\n{{/if}}\
            {{#if database}}- Optimize database queries and indexes\n{{/if}}\
            {{#if memory}}- Reduce memory allocation and fragmentation\n{{/if}}\
            {{#if concurrency}}- Improve concurrency and parallelization\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("optimization_target", "Component to optimize"),
            TemplateVariable::text(
                "performance_metric",
                "Metric to improve (latency, throughput, etc.)",
            ),
            TemplateVariable::text("current_baseline", "Current performance baseline").optional(),
            TemplateVariable::text("target_goal", "Performance improvement goal").optional(),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("database", "Optimize database operations"),
            TemplateVariable::boolean("memory", "Optimize memory usage"),
            TemplateVariable::boolean("concurrency", "Improve concurrency"),
        ])
        .with_target_files(vec![
            "performance-analysis-{{optimization_target}}.md".to_string(),
            "benchmarks/{{optimization_target}}_bench.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:704-761 method rust_module <-> ./crates/ccswarm/src/template/predefined.rs:875-928 method performance_optimization
  Similarity: 96.23%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_module (lines 704-761) ---[0m
    pub fn rust_module() -> Template {
        Template::new(
            "rust-module",
            "Rust Module",
            "Create a new Rust module with proper structure and documentation",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "module".to_string(),
            "backend".to_string(),
        ])
        .with_task_description("Create Rust module {{module_name}} for {{module_purpose}}")
        .with_task_details(
            "## Rust Module Specification\n\n\
            - **Module**: {{module_name}}\n\
            - **Purpose**: {{module_purpose}}\n\
            - **Visibility**: {{visibility}}\n\
            {{#if traits}}- **Traits**: {{traits}}\n{{/if}}\
            {{#if structs}}- **Structs**: {{structs}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Follow Rust naming conventions\n\
            - Add comprehensive rustdoc comments\n\
            - Implement proper error handling with Result<T, E>\n\
            - Use appropriate visibility modifiers\n\
            - Include unit tests in #[cfg(test)] module\n\
            {{#if async_support}}- Implement async/await patterns\n{{/if}}\
            {{#if serde_support}}- Add Serde serialization support\n{{/if}}\
            {{#if benchmark}}- Include criterion benchmarks\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("module_name", "Name of the Rust module"),
            TemplateVariable::text("module_purpose", "What this module does"),
            TemplateVariable::choice(
                "visibility",
                "Module visibility",
                vec![
                    "public".to_string(),
                    "private".to_string(),
                    "pub(crate)".to_string(),
                ],
            ),
            TemplateVariable::text("traits", "Traits to implement").optional(),
            TemplateVariable::text("structs", "Main structs in the module").optional(),
            TemplateVariable::boolean("async_support", "Include async support"),
            TemplateVariable::boolean("serde_support", "Add Serde support"),
            TemplateVariable::boolean("benchmark", "Include benchmarks"),
        ])
        .with_target_files(vec![
            "src/{{module_name}}/mod.rs".to_string(),
            "src/{{module_name}}/types.rs".to_string(),
            "benches/{{module_name}}_bench.rs".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:performance_optimization (lines 875-928) ---[0m
    pub fn performance_optimization() -> Template {
        Template::new(
            "performance-optimization",
            "Performance Optimization",
            "Optimize performance of a system component",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "performance".to_string(),
            "optimization".to_string(),
            "profiling".to_string(),
        ])
        .with_task_description(
            "Optimize performance of {{optimization_target}} to improve {{performance_metric}}",
        )
        .with_task_details(
            "## Performance Optimization Plan\n\n\
            - **Target**: {{optimization_target}}\n\
            - **Metric**: {{performance_metric}}\n\
            - **Current Performance**: {{current_baseline}}\n\
            - **Target Performance**: {{target_goal}}\n\n\
            ## Optimization Approach\n\n\
            - Profile current performance with appropriate tools\n\
            - Identify bottlenecks and hot paths\n\
            - Implement targeted optimizations\n\
            - Measure impact of each optimization\n\
            - Verify no functional regressions\n\
            {{#if caching}}- Implement caching strategies\n{{/if}}\
            {{#if database}}- Optimize database queries and indexes\n{{/if}}\
            {{#if memory}}- Reduce memory allocation and fragmentation\n{{/if}}\
            {{#if concurrency}}- Improve concurrency and parallelization\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("optimization_target", "Component to optimize"),
            TemplateVariable::text(
                "performance_metric",
                "Metric to improve (latency, throughput, etc.)",
            ),
            TemplateVariable::text("current_baseline", "Current performance baseline").optional(),
            TemplateVariable::text("target_goal", "Performance improvement goal").optional(),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("database", "Optimize database operations"),
            TemplateVariable::boolean("memory", "Optimize memory usage"),
            TemplateVariable::boolean("concurrency", "Improve concurrency"),
        ])
        .with_target_files(vec![
            "performance-analysis-{{optimization_target}}.md".to_string(),
            "benchmarks/{{optimization_target}}_bench.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:442-498 method bug_fix <-> ./crates/ccswarm/src/template/predefined.rs:875-928 method performance_optimization
  Similarity: 96.96%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:bug_fix (lines 442-498) ---[0m
    pub fn bug_fix() -> Template {
        Template::new(
            "bug-fix",
            "Bug Fix",
            "Fix a reported bug with proper testing and documentation",
            TemplateCategory::BugFix,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "bug".to_string(),
            "fix".to_string(),
            "maintenance".to_string(),
        ])
        .with_task_description("Fix bug: {{bug_description}} ({{bug_severity}})")
        .with_task_details(
            "## Bug Report\n\n\
            - **Description**: {{bug_description}}\n\
            - **Severity**: {{bug_severity}}\n\
            {{#if reproduction_steps}}- **Reproduction**: {{reproduction_steps}}\n{{/if}}\
            {{#if affected_components}}- **Affected Components**: {{affected_components}}\n{{/if}}\
            {{#if user_impact}}- **User Impact**: {{user_impact}}\n{{/if}}\n\n\
            ## Fix Requirements\n\n\
            - Reproduce the bug in a test environment\n\
            - Identify root cause through debugging\n\
            - Implement minimal fix that addresses root cause\n\
            - Add regression tests to prevent recurrence\n\
            - Verify fix doesn't introduce new issues\n\
            {{#if documentation}}- Update relevant documentation\n{{/if}}\
            {{#if release_notes}}- Add to release notes\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Bugfix)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("bug_description", "Clear description of the bug"),
            TemplateVariable::choice(
                "bug_severity",
                "Bug severity level",
                vec![
                    "Critical".to_string(),
                    "High".to_string(),
                    "Medium".to_string(),
                    "Low".to_string(),
                ],
            ),
            TemplateVariable::text("reproduction_steps", "Steps to reproduce the bug").optional(),
            TemplateVariable::text("affected_components", "Components affected by the bug")
                .optional(),
            TemplateVariable::text("user_impact", "How this affects users").optional(),
            TemplateVariable::boolean("documentation", "Update documentation"),
            TemplateVariable::boolean("release_notes", "Add to release notes"),
        ])
        .with_target_files(vec![
            "src/{{affected_components}}/fix.{{file_extension}}".to_string(),
            "tests/regression/{{bug_description}}.test.{{file_extension}}".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:performance_optimization (lines 875-928) ---[0m
    pub fn performance_optimization() -> Template {
        Template::new(
            "performance-optimization",
            "Performance Optimization",
            "Optimize performance of a system component",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "performance".to_string(),
            "optimization".to_string(),
            "profiling".to_string(),
        ])
        .with_task_description(
            "Optimize performance of {{optimization_target}} to improve {{performance_metric}}",
        )
        .with_task_details(
            "## Performance Optimization Plan\n\n\
            - **Target**: {{optimization_target}}\n\
            - **Metric**: {{performance_metric}}\n\
            - **Current Performance**: {{current_baseline}}\n\
            - **Target Performance**: {{target_goal}}\n\n\
            ## Optimization Approach\n\n\
            - Profile current performance with appropriate tools\n\
            - Identify bottlenecks and hot paths\n\
            - Implement targeted optimizations\n\
            - Measure impact of each optimization\n\
            - Verify no functional regressions\n\
            {{#if caching}}- Implement caching strategies\n{{/if}}\
            {{#if database}}- Optimize database queries and indexes\n{{/if}}\
            {{#if memory}}- Reduce memory allocation and fragmentation\n{{/if}}\
            {{#if concurrency}}- Improve concurrency and parallelization\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("optimization_target", "Component to optimize"),
            TemplateVariable::text(
                "performance_metric",
                "Metric to improve (latency, throughput, etc.)",
            ),
            TemplateVariable::text("current_baseline", "Current performance baseline").optional(),
            TemplateVariable::text("target_goal", "Performance improvement goal").optional(),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("database", "Optimize database operations"),
            TemplateVariable::boolean("memory", "Optimize memory usage"),
            TemplateVariable::boolean("concurrency", "Improve concurrency"),
        ])
        .with_target_files(vec![
            "performance-analysis-{{optimization_target}}.md".to_string(),
            "benchmarks/{{optimization_target}}_bench.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:875-928 method performance_optimization <-> ./crates/ccswarm/src/template/predefined.rs:931-987 method code_refactor
  Similarity: 96.96%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:performance_optimization (lines 875-928) ---[0m
    pub fn performance_optimization() -> Template {
        Template::new(
            "performance-optimization",
            "Performance Optimization",
            "Optimize performance of a system component",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "performance".to_string(),
            "optimization".to_string(),
            "profiling".to_string(),
        ])
        .with_task_description(
            "Optimize performance of {{optimization_target}} to improve {{performance_metric}}",
        )
        .with_task_details(
            "## Performance Optimization Plan\n\n\
            - **Target**: {{optimization_target}}\n\
            - **Metric**: {{performance_metric}}\n\
            - **Current Performance**: {{current_baseline}}\n\
            - **Target Performance**: {{target_goal}}\n\n\
            ## Optimization Approach\n\n\
            - Profile current performance with appropriate tools\n\
            - Identify bottlenecks and hot paths\n\
            - Implement targeted optimizations\n\
            - Measure impact of each optimization\n\
            - Verify no functional regressions\n\
            {{#if caching}}- Implement caching strategies\n{{/if}}\
            {{#if database}}- Optimize database queries and indexes\n{{/if}}\
            {{#if memory}}- Reduce memory allocation and fragmentation\n{{/if}}\
            {{#if concurrency}}- Improve concurrency and parallelization\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("optimization_target", "Component to optimize"),
            TemplateVariable::text(
                "performance_metric",
                "Metric to improve (latency, throughput, etc.)",
            ),
            TemplateVariable::text("current_baseline", "Current performance baseline").optional(),
            TemplateVariable::text("target_goal", "Performance improvement goal").optional(),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("database", "Optimize database operations"),
            TemplateVariable::boolean("memory", "Optimize memory usage"),
            TemplateVariable::boolean("concurrency", "Improve concurrency"),
        ])
        .with_target_files(vec![
            "performance-analysis-{{optimization_target}}.md".to_string(),
            "benchmarks/{{optimization_target}}_bench.{{file_extension}}".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:code_refactor (lines 931-987) ---[0m
    pub fn code_refactor() -> Template {
        Template::new(
            "code-refactor",
            "Code Refactoring",
            "Refactor code to improve maintainability and design",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "refactor".to_string(),
            "maintainability".to_string(),
            "design".to_string(),
        ])
        .with_task_description("Refactor {{refactor_target}} to {{refactor_goal}}")
        .with_task_details(
            "## Refactoring Plan\n\n\
            - **Target**: {{refactor_target}}\n\
            - **Goal**: {{refactor_goal}}\n\
            - **Refactor Type**: {{refactor_type}}\n\
            {{#if design_patterns}}- **Design Patterns**: {{design_patterns}}\n{{/if}}\n\n\
            ## Refactoring Steps\n\n\
            - Analyze current code structure and identify issues\n\
            - Create comprehensive test coverage before refactoring\n\
            - Apply refactoring techniques incrementally\n\
            - Verify tests pass after each step\n\
            - Update documentation to reflect changes\n\
            {{#if breaking_changes}}- Document any breaking changes\n{{/if}}\
            {{#if performance_impact}}- Measure performance impact\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(80)
        .with_variables(vec![
            TemplateVariable::text("refactor_target", "Code to be refactored"),
            TemplateVariable::text("refactor_goal", "What the refactoring should achieve"),
            TemplateVariable::choice(
                "refactor_type",
                "Type of refactoring",
                vec![
                    "Extract Method".to_string(),
                    "Extract Class".to_string(),
                    "Move Method".to_string(),
                    "Rename".to_string(),
                    "Simplify Conditional".to_string(),
                    "Remove Duplication".to_string(),
                    "Improve Abstraction".to_string(),
                ],
            ),
            TemplateVariable::text("design_patterns", "Design patterns to apply").optional(),
            TemplateVariable::boolean("breaking_changes", "May introduce breaking changes"),
            TemplateVariable::boolean("performance_impact", "Measure performance impact"),
        ])
        .with_target_files(vec![
            "refactor-plan-{{refactor_target}}.md".to_string(),
            "tests/refactor/{{refactor_target}}_test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:816-872 method security_review <-> ./crates/ccswarm/src/template/predefined.rs:875-928 method performance_optimization
  Similarity: 96.88%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:security_review (lines 816-872) ---[0m
    pub fn security_review() -> Template {
        Template::new(
            "security-review",
            "Security Review",
            "Conduct a comprehensive security review of code or system",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "security".to_string(),
            "review".to_string(),
            "audit".to_string(),
        ])
        .with_task_description(
            "Conduct security review of {{review_target}} focusing on {{security_aspects}}",
        )
        .with_task_details(
            "## Security Review Scope\n\n\
            - **Target**: {{review_target}}\n\
            - **Focus Areas**: {{security_aspects}}\n\
            - **Review Type**: {{review_type}}\n\n\
            ## Security Checklist\n\n\
            - Authentication and authorization mechanisms\n\
            - Input validation and sanitization\n\
            - SQL injection and XSS vulnerabilities\n\
            - Secure data storage and transmission\n\
            - Error handling and information disclosure\n\
            - Dependencies and supply chain security\n\
            {{#if crypto}}- Cryptographic implementations\n{{/if}}\
            {{#if api_security}}- API security and rate limiting\n{{/if}}\
            {{#if infrastructure}}- Infrastructure security configuration\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Review)
        .with_duration(120)
        .with_variables(vec![
            TemplateVariable::text("review_target", "What is being reviewed"),
            TemplateVariable::text("security_aspects", "Specific security concerns"),
            TemplateVariable::choice(
                "review_type",
                "Type of security review",
                vec![
                    "Code Review".to_string(),
                    "Architecture Review".to_string(),
                    "Penetration Test".to_string(),
                    "Compliance Audit".to_string(),
                ],
            ),
            TemplateVariable::boolean("crypto", "Review cryptographic code"),
            TemplateVariable::boolean("api_security", "Review API security"),
            TemplateVariable::boolean("infrastructure", "Review infrastructure"),
        ])
        .with_target_files(vec![
            "security-review-{{review_target}}.md".to_string(),
            "security-checklist-{{review_target}}.md".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:performance_optimization (lines 875-928) ---[0m
    pub fn performance_optimization() -> Template {
        Template::new(
            "performance-optimization",
            "Performance Optimization",
            "Optimize performance of a system component",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "performance".to_string(),
            "optimization".to_string(),
            "profiling".to_string(),
        ])
        .with_task_description(
            "Optimize performance of {{optimization_target}} to improve {{performance_metric}}",
        )
        .with_task_details(
            "## Performance Optimization Plan\n\n\
            - **Target**: {{optimization_target}}\n\
            - **Metric**: {{performance_metric}}\n\
            - **Current Performance**: {{current_baseline}}\n\
            - **Target Performance**: {{target_goal}}\n\n\
            ## Optimization Approach\n\n\
            - Profile current performance with appropriate tools\n\
            - Identify bottlenecks and hot paths\n\
            - Implement targeted optimizations\n\
            - Measure impact of each optimization\n\
            - Verify no functional regressions\n\
            {{#if caching}}- Implement caching strategies\n{{/if}}\
            {{#if database}}- Optimize database queries and indexes\n{{/if}}\
            {{#if memory}}- Reduce memory allocation and fragmentation\n{{/if}}\
            {{#if concurrency}}- Improve concurrency and parallelization\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("optimization_target", "Component to optimize"),
            TemplateVariable::text(
                "performance_metric",
                "Metric to improve (latency, throughput, etc.)",
            ),
            TemplateVariable::text("current_baseline", "Current performance baseline").optional(),
            TemplateVariable::text("target_goal", "Performance improvement goal").optional(),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("database", "Optimize database operations"),
            TemplateVariable::boolean("memory", "Optimize memory usage"),
            TemplateVariable::boolean("concurrency", "Improve concurrency"),
        ])
        .with_target_files(vec![
            "performance-analysis-{{optimization_target}}.md".to_string(),
            "benchmarks/{{optimization_target}}_bench.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:111-166 method react_hook <-> ./crates/ccswarm/src/template/predefined.rs:875-928 method performance_optimization
  Similarity: 96.94%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_hook (lines 111-166) ---[0m
    pub fn react_hook() -> Template {
        Template::new(
            "react-hook",
            "React Custom Hook",
            "Create a custom React hook with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "react".to_string(),
            "hook".to_string(),
            "typescript".to_string(),
        ])
        .with_task_description(
            "Create a custom React hook named {{hook_name}} for {{hook_purpose}}",
        )
        .with_task_details(
            "## Hook Specification\n\n\
            - **Name**: {{hook_name}}\n\
            - **Purpose**: {{hook_purpose}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if parameters}}- **Parameters**: {{parameters}}\n{{/if}}\
            {{#if return_type}}- **Returns**: {{return_type}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React hooks rules and conventions\n\
            - Include proper dependency arrays for useEffect\n\
            - Add comprehensive JSDoc comments\n\
            - Handle cleanup and memory leaks\n\
            {{#if testing}}- Create unit tests with React Hooks Testing Library\n{{/if}}\
            {{#if memoization}}- Use useMemo/useCallback for performance optimization\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(30)
        .with_variables(vec![
            TemplateVariable::text(
                "hook_name",
                "Name of the custom hook (e.g., useLocalStorage)",
            ),
            TemplateVariable::text("hook_purpose", "What the hook does"),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the hook",
                "src/hooks/{{hook_name}}.ts",
            ),
            TemplateVariable::text("parameters", "Hook parameters (optional)").optional(),
            TemplateVariable::text("return_type", "Hook return type (optional)").optional(),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("memoization", "Include performance optimizations"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.ts".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:performance_optimization (lines 875-928) ---[0m
    pub fn performance_optimization() -> Template {
        Template::new(
            "performance-optimization",
            "Performance Optimization",
            "Optimize performance of a system component",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "performance".to_string(),
            "optimization".to_string(),
            "profiling".to_string(),
        ])
        .with_task_description(
            "Optimize performance of {{optimization_target}} to improve {{performance_metric}}",
        )
        .with_task_details(
            "## Performance Optimization Plan\n\n\
            - **Target**: {{optimization_target}}\n\
            - **Metric**: {{performance_metric}}\n\
            - **Current Performance**: {{current_baseline}}\n\
            - **Target Performance**: {{target_goal}}\n\n\
            ## Optimization Approach\n\n\
            - Profile current performance with appropriate tools\n\
            - Identify bottlenecks and hot paths\n\
            - Implement targeted optimizations\n\
            - Measure impact of each optimization\n\
            - Verify no functional regressions\n\
            {{#if caching}}- Implement caching strategies\n{{/if}}\
            {{#if database}}- Optimize database queries and indexes\n{{/if}}\
            {{#if memory}}- Reduce memory allocation and fragmentation\n{{/if}}\
            {{#if concurrency}}- Improve concurrency and parallelization\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("optimization_target", "Component to optimize"),
            TemplateVariable::text(
                "performance_metric",
                "Metric to improve (latency, throughput, etc.)",
            ),
            TemplateVariable::text("current_baseline", "Current performance baseline").optional(),
            TemplateVariable::text("target_goal", "Performance improvement goal").optional(),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("database", "Optimize database operations"),
            TemplateVariable::boolean("memory", "Optimize memory usage"),
            TemplateVariable::boolean("concurrency", "Improve concurrency"),
        ])
        .with_target_files(vec![
            "performance-analysis-{{optimization_target}}.md".to_string(),
            "benchmarks/{{optimization_target}}_bench.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:381-439 method integration_test <-> ./crates/ccswarm/src/template/predefined.rs:764-813 method rust_cli_command
  Similarity: 95.95%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:integration_test (lines 381-439) ---[0m
    pub fn integration_test() -> Template {
        Template::new(
            "integration-test",
            "Integration Test",
            "Create integration tests for system components",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "testing".to_string(),
            "integration".to_string(),
            "e2e".to_string(),
        ])
        .with_task_description(
            "Create integration tests for {{system_component}} testing {{integration_points}}",
        )
        .with_task_details(
            "## Integration Test Specification\n\n\
            - **Component**: {{system_component}}\n\
            - **Integration Points**: {{integration_points}}\n\
            - **Test Environment**: {{test_environment}}\n\
            {{#if database}}- **Database**: Test database setup required\n{{/if}}\
            {{#if external_services}}- **External Services**: {{external_services}}\n{{/if}}\n\n\
            ## Test Requirements\n\n\
            - Set up test environment and data\n\
            - Test end-to-end workflows\n\
            - Verify data consistency across components\n\
            - Test error propagation and recovery\n\
            - Clean up test data after execution\n\
            {{#if performance}}- Measure integration performance\n{{/if}}\
            {{#if security}}- Verify security boundaries\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Testing)
        .with_duration(75)
        .with_variables(vec![
            TemplateVariable::text("system_component", "System component being tested"),
            TemplateVariable::text("integration_points", "What integrations to test"),
            TemplateVariable::choice(
                "test_environment",
                "Test environment",
                vec![
                    "Docker Compose".to_string(),
                    "Testcontainers".to_string(),
                    "In-memory".to_string(),
                    "Staging".to_string(),
                ],
            ),
            TemplateVariable::boolean("database", "Includes database testing"),
            TemplateVariable::text("external_services", "External services involved").optional(),
            TemplateVariable::boolean("performance", "Include performance testing"),
            TemplateVariable::boolean("security", "Include security testing"),
        ])
        .with_target_files(vec![
            "tests/integration/{{system_component}}_integration.test.{{file_extension}}"
                .to_string(),
            "tests/fixtures/{{system_component}}_data.json".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_cli_command (lines 764-813) ---[0m
    pub fn rust_cli_command() -> Template {
        Template::new(
            "rust-cli-command",
            "Rust CLI Command",
            "Add a new command to a Rust CLI application using clap",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "cli".to_string(),
            "clap".to_string(),
            "command".to_string(),
        ])
        .with_task_description("Add CLI command {{command_name}} to {{command_description}}")
        .with_task_details(
            "## CLI Command Specification\n\n\
            - **Command**: {{command_name}}\n\
            - **Description**: {{command_description}}\n\
            {{#if arguments}}- **Arguments**: {{arguments}}\n{{/if}}\
            {{#if options}}- **Options**: {{options}}\n{{/if}}\
            {{#if subcommands}}- **Subcommands**: {{subcommands}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use clap derive macros for argument parsing\n\
            - Implement proper error handling and exit codes\n\
            - Add comprehensive help text and examples\n\
            - Include input validation\n\
            - Support common CLI patterns (--help, --version)\n\
            {{#if config_file}}- Support configuration file loading\n{{/if}}\
            {{#if logging}}- Integrate with logging framework\n{{/if}}\
            {{#if testing}}- Add integration tests for CLI\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(50)
        .with_variables(vec![
            TemplateVariable::text("command_name", "Name of the CLI command"),
            TemplateVariable::text("command_description", "What the command does"),
            TemplateVariable::text("arguments", "Required arguments").optional(),
            TemplateVariable::text("options", "Optional flags and options").optional(),
            TemplateVariable::text("subcommands", "Subcommands if any").optional(),
            TemplateVariable::boolean("config_file", "Support config files"),
            TemplateVariable::boolean("logging", "Add logging support"),
            TemplateVariable::boolean("testing", "Create CLI tests"),
        ])
        .with_target_files(vec![
            "src/cli/{{command_name}}.rs".to_string(),
            "tests/cli/{{command_name}}_test.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:501-559 method docker_service <-> ./crates/ccswarm/src/template/predefined.rs:764-813 method rust_cli_command
  Similarity: 95.84%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:docker_service (lines 501-559) ---[0m
    pub fn docker_service() -> Template {
        Template::new(
            "docker-service",
            "Docker Service",
            "Create a Docker container configuration for a service",
            TemplateCategory::DevOps,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "docker".to_string(),
            "container".to_string(),
            "devops".to_string(),
        ])
        .with_task_description("Create Docker configuration for {{service_name}} service")
        .with_task_details(
            "## Docker Service Configuration\n\n\
            - **Service**: {{service_name}}\n\
            - **Base Image**: {{base_image}}\n\
            {{#if ports}}- **Ports**: {{ports}}\n{{/if}}\
            {{#if volumes}}- **Volumes**: {{volumes}}\n{{/if}}\
            {{#if environment}}- **Environment**: {{environment}}\n{{/if}}\n\n\
            ## Container Requirements\n\n\
            - Multi-stage build for optimization\n\
            - Non-root user for security\n\
            - Health check configuration\n\
            - Proper signal handling\n\
            - Minimal attack surface\n\
            {{#if secrets}}- Secure secret management\n{{/if}}\
            {{#if monitoring}}- Monitoring and logging setup\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("service_name", "Name of the service"),
            TemplateVariable::choice(
                "base_image",
                "Base Docker image",
                vec![
                    "alpine".to_string(),
                    "ubuntu".to_string(),
                    "node:alpine".to_string(),
                    "python:alpine".to_string(),
                    "rust:alpine".to_string(),
                    "nginx:alpine".to_string(),
                ],
            ),
            TemplateVariable::text("ports", "Port mappings (e.g., 8080:8080)").optional(),
            TemplateVariable::text("volumes", "Volume mounts").optional(),
            TemplateVariable::text("environment", "Environment variables").optional(),
            TemplateVariable::boolean("secrets", "Include secret management"),
            TemplateVariable::boolean("monitoring", "Add monitoring setup"),
        ])
        .with_target_files(vec![
            "Dockerfile".to_string(),
            "docker-compose.yml".to_string(),
            ".dockerignore".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_cli_command (lines 764-813) ---[0m
    pub fn rust_cli_command() -> Template {
        Template::new(
            "rust-cli-command",
            "Rust CLI Command",
            "Add a new command to a Rust CLI application using clap",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "cli".to_string(),
            "clap".to_string(),
            "command".to_string(),
        ])
        .with_task_description("Add CLI command {{command_name}} to {{command_description}}")
        .with_task_details(
            "## CLI Command Specification\n\n\
            - **Command**: {{command_name}}\n\
            - **Description**: {{command_description}}\n\
            {{#if arguments}}- **Arguments**: {{arguments}}\n{{/if}}\
            {{#if options}}- **Options**: {{options}}\n{{/if}}\
            {{#if subcommands}}- **Subcommands**: {{subcommands}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use clap derive macros for argument parsing\n\
            - Implement proper error handling and exit codes\n\
            - Add comprehensive help text and examples\n\
            - Include input validation\n\
            - Support common CLI patterns (--help, --version)\n\
            {{#if config_file}}- Support configuration file loading\n{{/if}}\
            {{#if logging}}- Integrate with logging framework\n{{/if}}\
            {{#if testing}}- Add integration tests for CLI\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(50)
        .with_variables(vec![
            TemplateVariable::text("command_name", "Name of the CLI command"),
            TemplateVariable::text("command_description", "What the command does"),
            TemplateVariable::text("arguments", "Required arguments").optional(),
            TemplateVariable::text("options", "Optional flags and options").optional(),
            TemplateVariable::text("subcommands", "Subcommands if any").optional(),
            TemplateVariable::boolean("config_file", "Support config files"),
            TemplateVariable::boolean("logging", "Add logging support"),
            TemplateVariable::boolean("testing", "Create CLI tests"),
        ])
        .with_target_files(vec![
            "src/cli/{{command_name}}.rs".to_string(),
            "tests/cli/{{command_name}}_test.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:704-761 method rust_module <-> ./crates/ccswarm/src/template/predefined.rs:764-813 method rust_cli_command
  Similarity: 96.47%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_module (lines 704-761) ---[0m
    pub fn rust_module() -> Template {
        Template::new(
            "rust-module",
            "Rust Module",
            "Create a new Rust module with proper structure and documentation",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "module".to_string(),
            "backend".to_string(),
        ])
        .with_task_description("Create Rust module {{module_name}} for {{module_purpose}}")
        .with_task_details(
            "## Rust Module Specification\n\n\
            - **Module**: {{module_name}}\n\
            - **Purpose**: {{module_purpose}}\n\
            - **Visibility**: {{visibility}}\n\
            {{#if traits}}- **Traits**: {{traits}}\n{{/if}}\
            {{#if structs}}- **Structs**: {{structs}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Follow Rust naming conventions\n\
            - Add comprehensive rustdoc comments\n\
            - Implement proper error handling with Result<T, E>\n\
            - Use appropriate visibility modifiers\n\
            - Include unit tests in #[cfg(test)] module\n\
            {{#if async_support}}- Implement async/await patterns\n{{/if}}\
            {{#if serde_support}}- Add Serde serialization support\n{{/if}}\
            {{#if benchmark}}- Include criterion benchmarks\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(45)
        .with_variables(vec![
            TemplateVariable::text("module_name", "Name of the Rust module"),
            TemplateVariable::text("module_purpose", "What this module does"),
            TemplateVariable::choice(
                "visibility",
                "Module visibility",
                vec![
                    "public".to_string(),
                    "private".to_string(),
                    "pub(crate)".to_string(),
                ],
            ),
            TemplateVariable::text("traits", "Traits to implement").optional(),
            TemplateVariable::text("structs", "Main structs in the module").optional(),
            TemplateVariable::boolean("async_support", "Include async support"),
            TemplateVariable::boolean("serde_support", "Add Serde support"),
            TemplateVariable::boolean("benchmark", "Include benchmarks"),
        ])
        .with_target_files(vec![
            "src/{{module_name}}/mod.rs".to_string(),
            "src/{{module_name}}/types.rs".to_string(),
            "benches/{{module_name}}_bench.rs".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_cli_command (lines 764-813) ---[0m
    pub fn rust_cli_command() -> Template {
        Template::new(
            "rust-cli-command",
            "Rust CLI Command",
            "Add a new command to a Rust CLI application using clap",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "cli".to_string(),
            "clap".to_string(),
            "command".to_string(),
        ])
        .with_task_description("Add CLI command {{command_name}} to {{command_description}}")
        .with_task_details(
            "## CLI Command Specification\n\n\
            - **Command**: {{command_name}}\n\
            - **Description**: {{command_description}}\n\
            {{#if arguments}}- **Arguments**: {{arguments}}\n{{/if}}\
            {{#if options}}- **Options**: {{options}}\n{{/if}}\
            {{#if subcommands}}- **Subcommands**: {{subcommands}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use clap derive macros for argument parsing\n\
            - Implement proper error handling and exit codes\n\
            - Add comprehensive help text and examples\n\
            - Include input validation\n\
            - Support common CLI patterns (--help, --version)\n\
            {{#if config_file}}- Support configuration file loading\n{{/if}}\
            {{#if logging}}- Integrate with logging framework\n{{/if}}\
            {{#if testing}}- Add integration tests for CLI\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(50)
        .with_variables(vec![
            TemplateVariable::text("command_name", "Name of the CLI command"),
            TemplateVariable::text("command_description", "What the command does"),
            TemplateVariable::text("arguments", "Required arguments").optional(),
            TemplateVariable::text("options", "Optional flags and options").optional(),
            TemplateVariable::text("subcommands", "Subcommands if any").optional(),
            TemplateVariable::boolean("config_file", "Support config files"),
            TemplateVariable::boolean("logging", "Add logging support"),
            TemplateVariable::boolean("testing", "Create CLI tests"),
        ])
        .with_target_files(vec![
            "src/cli/{{command_name}}.rs".to_string(),
            "tests/cli/{{command_name}}_test.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:253-310 method database_migration <-> ./crates/ccswarm/src/template/predefined.rs:764-813 method rust_cli_command
  Similarity: 95.74%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:database_migration (lines 253-310) ---[0m
    pub fn database_migration() -> Template {
        Template::new(
            "database-migration",
            "Database Migration",
            "Create a database migration script",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "database".to_string(),
            "migration".to_string(),
            "schema".to_string(),
        ])
        .with_task_description("Create database migration to {{migration_purpose}}")
        .with_task_details(
            "## Migration Details\n\n\
            - **Purpose**: {{migration_purpose}}\n\
            - **Type**: {{migration_type}}\n\
            {{#if table_name}}- **Table**: {{table_name}}\n{{/if}}\
            {{#if columns}}- **Columns**: {{columns}}\n{{/if}}\
            {{#if indexes}}- **Indexes**: {{indexes}}\n{{/if}}\n\n\
            ## Requirements\n\n\
            - Write both up and down migrations\n\
            - Include proper constraints and indexes\n\
            - Handle existing data appropriately\n\
            - Add comments for complex operations\n\
            - Test migration on sample data\n\
            {{#if backup}}- Create backup before running\n{{/if}}\
            {{#if rollback_plan}}- Document rollback procedure\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Infrastructure)
        .with_duration(40)
        .with_variables(vec![
            TemplateVariable::text("migration_purpose", "What this migration accomplishes"),
            TemplateVariable::choice(
                "migration_type",
                "Type of migration",
                vec![
                    "Create Table".to_string(),
                    "Alter Table".to_string(),
                    "Drop Table".to_string(),
                    "Add Column".to_string(),
                    "Drop Column".to_string(),
                    "Create Index".to_string(),
                    "Data Migration".to_string(),
                ],
            ),
            TemplateVariable::text("table_name", "Table name (if applicable)").optional(),
            TemplateVariable::text("columns", "Column definitions (optional)").optional(),
            TemplateVariable::text("indexes", "Index definitions (optional)").optional(),
            TemplateVariable::boolean("backup", "Create backup before migration"),
            TemplateVariable::boolean("rollback_plan", "Document rollback procedure"),
        ])
        .with_target_files(vec![
            "migrations/{{current_datetime}}_{{migration_purpose}}.sql".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_cli_command (lines 764-813) ---[0m
    pub fn rust_cli_command() -> Template {
        Template::new(
            "rust-cli-command",
            "Rust CLI Command",
            "Add a new command to a Rust CLI application using clap",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "cli".to_string(),
            "clap".to_string(),
            "command".to_string(),
        ])
        .with_task_description("Add CLI command {{command_name}} to {{command_description}}")
        .with_task_details(
            "## CLI Command Specification\n\n\
            - **Command**: {{command_name}}\n\
            - **Description**: {{command_description}}\n\
            {{#if arguments}}- **Arguments**: {{arguments}}\n{{/if}}\
            {{#if options}}- **Options**: {{options}}\n{{/if}}\
            {{#if subcommands}}- **Subcommands**: {{subcommands}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use clap derive macros for argument parsing\n\
            - Implement proper error handling and exit codes\n\
            - Add comprehensive help text and examples\n\
            - Include input validation\n\
            - Support common CLI patterns (--help, --version)\n\
            {{#if config_file}}- Support configuration file loading\n{{/if}}\
            {{#if logging}}- Integrate with logging framework\n{{/if}}\
            {{#if testing}}- Add integration tests for CLI\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(50)
        .with_variables(vec![
            TemplateVariable::text("command_name", "Name of the CLI command"),
            TemplateVariable::text("command_description", "What the command does"),
            TemplateVariable::text("arguments", "Required arguments").optional(),
            TemplateVariable::text("options", "Optional flags and options").optional(),
            TemplateVariable::text("subcommands", "Subcommands if any").optional(),
            TemplateVariable::boolean("config_file", "Support config files"),
            TemplateVariable::boolean("logging", "Add logging support"),
            TemplateVariable::boolean("testing", "Create CLI tests"),
        ])
        .with_target_files(vec![
            "src/cli/{{command_name}}.rs".to_string(),
            "tests/cli/{{command_name}}_test.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:764-813 method rust_cli_command <-> ./crates/ccswarm/src/template/predefined.rs:931-987 method code_refactor
  Similarity: 96.44%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_cli_command (lines 764-813) ---[0m
    pub fn rust_cli_command() -> Template {
        Template::new(
            "rust-cli-command",
            "Rust CLI Command",
            "Add a new command to a Rust CLI application using clap",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "cli".to_string(),
            "clap".to_string(),
            "command".to_string(),
        ])
        .with_task_description("Add CLI command {{command_name}} to {{command_description}}")
        .with_task_details(
            "## CLI Command Specification\n\n\
            - **Command**: {{command_name}}\n\
            - **Description**: {{command_description}}\n\
            {{#if arguments}}- **Arguments**: {{arguments}}\n{{/if}}\
            {{#if options}}- **Options**: {{options}}\n{{/if}}\
            {{#if subcommands}}- **Subcommands**: {{subcommands}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use clap derive macros for argument parsing\n\
            - Implement proper error handling and exit codes\n\
            - Add comprehensive help text and examples\n\
            - Include input validation\n\
            - Support common CLI patterns (--help, --version)\n\
            {{#if config_file}}- Support configuration file loading\n{{/if}}\
            {{#if logging}}- Integrate with logging framework\n{{/if}}\
            {{#if testing}}- Add integration tests for CLI\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(50)
        .with_variables(vec![
            TemplateVariable::text("command_name", "Name of the CLI command"),
            TemplateVariable::text("command_description", "What the command does"),
            TemplateVariable::text("arguments", "Required arguments").optional(),
            TemplateVariable::text("options", "Optional flags and options").optional(),
            TemplateVariable::text("subcommands", "Subcommands if any").optional(),
            TemplateVariable::boolean("config_file", "Support config files"),
            TemplateVariable::boolean("logging", "Add logging support"),
            TemplateVariable::boolean("testing", "Create CLI tests"),
        ])
        .with_target_files(vec![
            "src/cli/{{command_name}}.rs".to_string(),
            "tests/cli/{{command_name}}_test.rs".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:code_refactor (lines 931-987) ---[0m
    pub fn code_refactor() -> Template {
        Template::new(
            "code-refactor",
            "Code Refactoring",
            "Refactor code to improve maintainability and design",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "refactor".to_string(),
            "maintainability".to_string(),
            "design".to_string(),
        ])
        .with_task_description("Refactor {{refactor_target}} to {{refactor_goal}}")
        .with_task_details(
            "## Refactoring Plan\n\n\
            - **Target**: {{refactor_target}}\n\
            - **Goal**: {{refactor_goal}}\n\
            - **Refactor Type**: {{refactor_type}}\n\
            {{#if design_patterns}}- **Design Patterns**: {{design_patterns}}\n{{/if}}\n\n\
            ## Refactoring Steps\n\n\
            - Analyze current code structure and identify issues\n\
            - Create comprehensive test coverage before refactoring\n\
            - Apply refactoring techniques incrementally\n\
            - Verify tests pass after each step\n\
            - Update documentation to reflect changes\n\
            {{#if breaking_changes}}- Document any breaking changes\n{{/if}}\
            {{#if performance_impact}}- Measure performance impact\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(80)
        .with_variables(vec![
            TemplateVariable::text("refactor_target", "Code to be refactored"),
            TemplateVariable::text("refactor_goal", "What the refactoring should achieve"),
            TemplateVariable::choice(
                "refactor_type",
                "Type of refactoring",
                vec![
                    "Extract Method".to_string(),
                    "Extract Class".to_string(),
                    "Move Method".to_string(),
                    "Rename".to_string(),
                    "Simplify Conditional".to_string(),
                    "Remove Duplication".to_string(),
                    "Improve Abstraction".to_string(),
                ],
            ),
            TemplateVariable::text("design_patterns", "Design patterns to apply").optional(),
            TemplateVariable::boolean("breaking_changes", "May introduce breaking changes"),
            TemplateVariable::boolean("performance_impact", "Measure performance impact"),
        ])
        .with_target_files(vec![
            "refactor-plan-{{refactor_target}}.md".to_string(),
            "tests/refactor/{{refactor_target}}_test.{{file_extension}}".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:442-498 method bug_fix <-> ./crates/ccswarm/src/template/predefined.rs:764-813 method rust_cli_command
  Similarity: 96.30%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:bug_fix (lines 442-498) ---[0m
    pub fn bug_fix() -> Template {
        Template::new(
            "bug-fix",
            "Bug Fix",
            "Fix a reported bug with proper testing and documentation",
            TemplateCategory::BugFix,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "bug".to_string(),
            "fix".to_string(),
            "maintenance".to_string(),
        ])
        .with_task_description("Fix bug: {{bug_description}} ({{bug_severity}})")
        .with_task_details(
            "## Bug Report\n\n\
            - **Description**: {{bug_description}}\n\
            - **Severity**: {{bug_severity}}\n\
            {{#if reproduction_steps}}- **Reproduction**: {{reproduction_steps}}\n{{/if}}\
            {{#if affected_components}}- **Affected Components**: {{affected_components}}\n{{/if}}\
            {{#if user_impact}}- **User Impact**: {{user_impact}}\n{{/if}}\n\n\
            ## Fix Requirements\n\n\
            - Reproduce the bug in a test environment\n\
            - Identify root cause through debugging\n\
            - Implement minimal fix that addresses root cause\n\
            - Add regression tests to prevent recurrence\n\
            - Verify fix doesn't introduce new issues\n\
            {{#if documentation}}- Update relevant documentation\n{{/if}}\
            {{#if release_notes}}- Add to release notes\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Bugfix)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("bug_description", "Clear description of the bug"),
            TemplateVariable::choice(
                "bug_severity",
                "Bug severity level",
                vec![
                    "Critical".to_string(),
                    "High".to_string(),
                    "Medium".to_string(),
                    "Low".to_string(),
                ],
            ),
            TemplateVariable::text("reproduction_steps", "Steps to reproduce the bug").optional(),
            TemplateVariable::text("affected_components", "Components affected by the bug")
                .optional(),
            TemplateVariable::text("user_impact", "How this affects users").optional(),
            TemplateVariable::boolean("documentation", "Update documentation"),
            TemplateVariable::boolean("release_notes", "Add to release notes"),
        ])
        .with_target_files(vec![
            "src/{{affected_components}}/fix.{{file_extension}}".to_string(),
            "tests/regression/{{bug_description}}.test.{{file_extension}}".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_cli_command (lines 764-813) ---[0m
    pub fn rust_cli_command() -> Template {
        Template::new(
            "rust-cli-command",
            "Rust CLI Command",
            "Add a new command to a Rust CLI application using clap",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "cli".to_string(),
            "clap".to_string(),
            "command".to_string(),
        ])
        .with_task_description("Add CLI command {{command_name}} to {{command_description}}")
        .with_task_details(
            "## CLI Command Specification\n\n\
            - **Command**: {{command_name}}\n\
            - **Description**: {{command_description}}\n\
            {{#if arguments}}- **Arguments**: {{arguments}}\n{{/if}}\
            {{#if options}}- **Options**: {{options}}\n{{/if}}\
            {{#if subcommands}}- **Subcommands**: {{subcommands}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use clap derive macros for argument parsing\n\
            - Implement proper error handling and exit codes\n\
            - Add comprehensive help text and examples\n\
            - Include input validation\n\
            - Support common CLI patterns (--help, --version)\n\
            {{#if config_file}}- Support configuration file loading\n{{/if}}\
            {{#if logging}}- Integrate with logging framework\n{{/if}}\
            {{#if testing}}- Add integration tests for CLI\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(50)
        .with_variables(vec![
            TemplateVariable::text("command_name", "Name of the CLI command"),
            TemplateVariable::text("command_description", "What the command does"),
            TemplateVariable::text("arguments", "Required arguments").optional(),
            TemplateVariable::text("options", "Optional flags and options").optional(),
            TemplateVariable::text("subcommands", "Subcommands if any").optional(),
            TemplateVariable::boolean("config_file", "Support config files"),
            TemplateVariable::boolean("logging", "Add logging support"),
            TemplateVariable::boolean("testing", "Create CLI tests"),
        ])
        .with_target_files(vec![
            "src/cli/{{command_name}}.rs".to_string(),
            "tests/cli/{{command_name}}_test.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:764-813 method rust_cli_command <-> ./crates/ccswarm/src/template/predefined.rs:816-872 method security_review
  Similarity: 95.88%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_cli_command (lines 764-813) ---[0m
    pub fn rust_cli_command() -> Template {
        Template::new(
            "rust-cli-command",
            "Rust CLI Command",
            "Add a new command to a Rust CLI application using clap",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "cli".to_string(),
            "clap".to_string(),
            "command".to_string(),
        ])
        .with_task_description("Add CLI command {{command_name}} to {{command_description}}")
        .with_task_details(
            "## CLI Command Specification\n\n\
            - **Command**: {{command_name}}\n\
            - **Description**: {{command_description}}\n\
            {{#if arguments}}- **Arguments**: {{arguments}}\n{{/if}}\
            {{#if options}}- **Options**: {{options}}\n{{/if}}\
            {{#if subcommands}}- **Subcommands**: {{subcommands}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use clap derive macros for argument parsing\n\
            - Implement proper error handling and exit codes\n\
            - Add comprehensive help text and examples\n\
            - Include input validation\n\
            - Support common CLI patterns (--help, --version)\n\
            {{#if config_file}}- Support configuration file loading\n{{/if}}\
            {{#if logging}}- Integrate with logging framework\n{{/if}}\
            {{#if testing}}- Add integration tests for CLI\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(50)
        .with_variables(vec![
            TemplateVariable::text("command_name", "Name of the CLI command"),
            TemplateVariable::text("command_description", "What the command does"),
            TemplateVariable::text("arguments", "Required arguments").optional(),
            TemplateVariable::text("options", "Optional flags and options").optional(),
            TemplateVariable::text("subcommands", "Subcommands if any").optional(),
            TemplateVariable::boolean("config_file", "Support config files"),
            TemplateVariable::boolean("logging", "Add logging support"),
            TemplateVariable::boolean("testing", "Create CLI tests"),
        ])
        .with_target_files(vec![
            "src/cli/{{command_name}}.rs".to_string(),
            "tests/cli/{{command_name}}_test.rs".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:security_review (lines 816-872) ---[0m
    pub fn security_review() -> Template {
        Template::new(
            "security-review",
            "Security Review",
            "Conduct a comprehensive security review of code or system",
            TemplateCategory::Testing,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "security".to_string(),
            "review".to_string(),
            "audit".to_string(),
        ])
        .with_task_description(
            "Conduct security review of {{review_target}} focusing on {{security_aspects}}",
        )
        .with_task_details(
            "## Security Review Scope\n\n\
            - **Target**: {{review_target}}\n\
            - **Focus Areas**: {{security_aspects}}\n\
            - **Review Type**: {{review_type}}\n\n\
            ## Security Checklist\n\n\
            - Authentication and authorization mechanisms\n\
            - Input validation and sanitization\n\
            - SQL injection and XSS vulnerabilities\n\
            - Secure data storage and transmission\n\
            - Error handling and information disclosure\n\
            - Dependencies and supply chain security\n\
            {{#if crypto}}- Cryptographic implementations\n{{/if}}\
            {{#if api_security}}- API security and rate limiting\n{{/if}}\
            {{#if infrastructure}}- Infrastructure security configuration\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Review)
        .with_duration(120)
        .with_variables(vec![
            TemplateVariable::text("review_target", "What is being reviewed"),
            TemplateVariable::text("security_aspects", "Specific security concerns"),
            TemplateVariable::choice(
                "review_type",
                "Type of security review",
                vec![
                    "Code Review".to_string(),
                    "Architecture Review".to_string(),
                    "Penetration Test".to_string(),
                    "Compliance Audit".to_string(),
                ],
            ),
            TemplateVariable::boolean("crypto", "Review cryptographic code"),
            TemplateVariable::boolean("api_security", "Review API security"),
            TemplateVariable::boolean("infrastructure", "Review infrastructure"),
        ])
        .with_target_files(vec![
            "security-review-{{review_target}}.md".to_string(),
            "security-checklist-{{review_target}}.md".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:111-166 method react_hook <-> ./crates/ccswarm/src/template/predefined.rs:764-813 method rust_cli_command
  Similarity: 96.03%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:react_hook (lines 111-166) ---[0m
    pub fn react_hook() -> Template {
        Template::new(
            "react-hook",
            "React Custom Hook",
            "Create a custom React hook with TypeScript",
            TemplateCategory::Frontend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "react".to_string(),
            "hook".to_string(),
            "typescript".to_string(),
        ])
        .with_task_description(
            "Create a custom React hook named {{hook_name}} for {{hook_purpose}}",
        )
        .with_task_details(
            "## Hook Specification\n\n\
            - **Name**: {{hook_name}}\n\
            - **Purpose**: {{hook_purpose}}\n\
            - **File**: `{{file_path}}`\n\
            {{#if parameters}}- **Parameters**: {{parameters}}\n{{/if}}\
            {{#if return_type}}- **Returns**: {{return_type}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use TypeScript with proper type definitions\n\
            - Follow React hooks rules and conventions\n\
            - Include proper dependency arrays for useEffect\n\
            - Add comprehensive JSDoc comments\n\
            - Handle cleanup and memory leaks\n\
            {{#if testing}}- Create unit tests with React Hooks Testing Library\n{{/if}}\
            {{#if memoization}}- Use useMemo/useCallback for performance optimization\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(30)
        .with_variables(vec![
            TemplateVariable::text(
                "hook_name",
                "Name of the custom hook (e.g., useLocalStorage)",
            ),
            TemplateVariable::text("hook_purpose", "What the hook does"),
            TemplateVariable::text_with_default(
                "file_path",
                "File path for the hook",
                "src/hooks/{{hook_name}}.ts",
            ),
            TemplateVariable::text("parameters", "Hook parameters (optional)").optional(),
            TemplateVariable::text("return_type", "Hook return type (optional)").optional(),
            TemplateVariable::boolean("testing", "Create unit tests"),
            TemplateVariable::boolean("memoization", "Include performance optimizations"),
        ])
        .with_target_files(vec![
            "{{file_path}}".to_string(),
            "{{file_path}}.test.ts".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_cli_command (lines 764-813) ---[0m
    pub fn rust_cli_command() -> Template {
        Template::new(
            "rust-cli-command",
            "Rust CLI Command",
            "Add a new command to a Rust CLI application using clap",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "cli".to_string(),
            "clap".to_string(),
            "command".to_string(),
        ])
        .with_task_description("Add CLI command {{command_name}} to {{command_description}}")
        .with_task_details(
            "## CLI Command Specification\n\n\
            - **Command**: {{command_name}}\n\
            - **Description**: {{command_description}}\n\
            {{#if arguments}}- **Arguments**: {{arguments}}\n{{/if}}\
            {{#if options}}- **Options**: {{options}}\n{{/if}}\
            {{#if subcommands}}- **Subcommands**: {{subcommands}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use clap derive macros for argument parsing\n\
            - Implement proper error handling and exit codes\n\
            - Add comprehensive help text and examples\n\
            - Include input validation\n\
            - Support common CLI patterns (--help, --version)\n\
            {{#if config_file}}- Support configuration file loading\n{{/if}}\
            {{#if logging}}- Integrate with logging framework\n{{/if}}\
            {{#if testing}}- Add integration tests for CLI\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(50)
        .with_variables(vec![
            TemplateVariable::text("command_name", "Name of the CLI command"),
            TemplateVariable::text("command_description", "What the command does"),
            TemplateVariable::text("arguments", "Required arguments").optional(),
            TemplateVariable::text("options", "Optional flags and options").optional(),
            TemplateVariable::text("subcommands", "Subcommands if any").optional(),
            TemplateVariable::boolean("config_file", "Support config files"),
            TemplateVariable::boolean("logging", "Add logging support"),
            TemplateVariable::boolean("testing", "Create CLI tests"),
        ])
        .with_target_files(vec![
            "src/cli/{{command_name}}.rs".to_string(),
            "tests/cli/{{command_name}}_test.rs".to_string(),
        ])
    }

  ./crates/ccswarm/src/template/predefined.rs:764-813 method rust_cli_command <-> ./crates/ccswarm/src/template/predefined.rs:875-928 method performance_optimization
  Similarity: 95.95%
  Classes: PredefinedTemplates <-> PredefinedTemplates

[36m--- ./crates/ccswarm/src/template/predefined.rs:rust_cli_command (lines 764-813) ---[0m
    pub fn rust_cli_command() -> Template {
        Template::new(
            "rust-cli-command",
            "Rust CLI Command",
            "Add a new command to a Rust CLI application using clap",
            TemplateCategory::Backend,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "rust".to_string(),
            "cli".to_string(),
            "clap".to_string(),
            "command".to_string(),
        ])
        .with_task_description("Add CLI command {{command_name}} to {{command_description}}")
        .with_task_details(
            "## CLI Command Specification\n\n\
            - **Command**: {{command_name}}\n\
            - **Description**: {{command_description}}\n\
            {{#if arguments}}- **Arguments**: {{arguments}}\n{{/if}}\
            {{#if options}}- **Options**: {{options}}\n{{/if}}\
            {{#if subcommands}}- **Subcommands**: {{subcommands}}\n{{/if}}\n\n\
            ## Implementation Requirements\n\n\
            - Use clap derive macros for argument parsing\n\
            - Implement proper error handling and exit codes\n\
            - Add comprehensive help text and examples\n\
            - Include input validation\n\
            - Support common CLI patterns (--help, --version)\n\
            {{#if config_file}}- Support configuration file loading\n{{/if}}\
            {{#if logging}}- Integrate with logging framework\n{{/if}}\
            {{#if testing}}- Add integration tests for CLI\n{{/if}}",
        )
        .with_priority(Priority::Medium)
        .with_task_type(TaskType::Development)
        .with_duration(50)
        .with_variables(vec![
            TemplateVariable::text("command_name", "Name of the CLI command"),
            TemplateVariable::text("command_description", "What the command does"),
            TemplateVariable::text("arguments", "Required arguments").optional(),
            TemplateVariable::text("options", "Optional flags and options").optional(),
            TemplateVariable::text("subcommands", "Subcommands if any").optional(),
            TemplateVariable::boolean("config_file", "Support config files"),
            TemplateVariable::boolean("logging", "Add logging support"),
            TemplateVariable::boolean("testing", "Create CLI tests"),
        ])
        .with_target_files(vec![
            "src/cli/{{command_name}}.rs".to_string(),
            "tests/cli/{{command_name}}_test.rs".to_string(),
        ])
    }

[36m--- ./crates/ccswarm/src/template/predefined.rs:performance_optimization (lines 875-928) ---[0m
    pub fn performance_optimization() -> Template {
        Template::new(
            "performance-optimization",
            "Performance Optimization",
            "Optimize performance of a system component",
            TemplateCategory::General,
        )
        .with_author("ccswarm")
        .with_tags(vec![
            "performance".to_string(),
            "optimization".to_string(),
            "profiling".to_string(),
        ])
        .with_task_description(
            "Optimize performance of {{optimization_target}} to improve {{performance_metric}}",
        )
        .with_task_details(
            "## Performance Optimization Plan\n\n\
            - **Target**: {{optimization_target}}\n\
            - **Metric**: {{performance_metric}}\n\
            - **Current Performance**: {{current_baseline}}\n\
            - **Target Performance**: {{target_goal}}\n\n\
            ## Optimization Approach\n\n\
            - Profile current performance with appropriate tools\n\
            - Identify bottlenecks and hot paths\n\
            - Implement targeted optimizations\n\
            - Measure impact of each optimization\n\
            - Verify no functional regressions\n\
            {{#if caching}}- Implement caching strategies\n{{/if}}\
            {{#if database}}- Optimize database queries and indexes\n{{/if}}\
            {{#if memory}}- Reduce memory allocation and fragmentation\n{{/if}}\
            {{#if concurrency}}- Improve concurrency and parallelization\n{{/if}}",
        )
        .with_priority(Priority::High)
        .with_task_type(TaskType::Development)
        .with_duration(90)
        .with_variables(vec![
            TemplateVariable::text("optimization_target", "Component to optimize"),
            TemplateVariable::text(
                "performance_metric",
                "Metric to improve (latency, throughput, etc.)",
            ),
            TemplateVariable::text("current_baseline", "Current performance baseline").optional(),
            TemplateVariable::text("target_goal", "Performance improvement goal").optional(),
            TemplateVariable::boolean("caching", "Implement caching"),
            TemplateVariable::boolean("database", "Optimize database operations"),
            TemplateVariable::boolean("memory", "Optimize memory usage"),
            TemplateVariable::boolean("concurrency", "Improve concurrency"),
        ])
        .with_target_files(vec![
            "performance-analysis-{{optimization_target}}.md".to_string(),
            "benchmarks/{{optimization_target}}_bench.{{file_extension}}".to_string(),
        ])
    }


Duplicates in ./crates/ccswarm/src/tests/session_tests.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/tests/session_tests.rs:361-424 function test_tmux_session_operations <-> ./crates/ccswarm/src/tests/session_tests.rs:548-609 function test_concurrent_session_operations
  Similarity: 96.17%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_tmux_session_operations (lines 361-424) ---[0m
    async fn test_tmux_session_operations() {
        match TmuxClient::new() {
            Ok(client) => {
                let temp_dir = create_temp_workspace();
                let workspace_path = temp_dir.path().to_string_lossy();
                let session_name = "test-ccswarm-session";

                // Ensure server is running
                if client.ensure_server_running().is_err() {
                    println!("Skipping test: cannot start tmux server");
                    return;
                }

                // Test session creation
                match client.create_session(session_name, &workspace_path) {
                    Ok(_) => {
                        // Test session listing
                        match client.list_sessions() {
                            Ok(sessions) => {
                                let our_session = sessions
                                    .iter()
                                    .find(|s| s.name == session_name);
                                assert!(our_session.is_some());
                            }
                            Err(e) => {
                                println!("Warning: Could not list sessions: {}", e);
                            }
                        }

                        // Test session info
                        match client.get_session_info(session_name) {
                            Ok(session_info) => {
                                assert_eq!(session_info.name, session_name);
                            }
                            Err(e) => {
                                println!("Warning: Could not get session info: {}", e);
                            }
                        }

                        // Test environment variable setting
                        if let Err(e) = client.set_environment(session_name, "TEST_VAR", "test_value") {
                            println!("Warning: Could not set environment: {}", e);
                        }

                        // Test option setting
                        if let Err(e) = client.set_option(session_name, "status-left", "[TEST]") {
                            println!("Warning: Could not set option: {}", e);
                        }

                        // Clean up - kill the session
                        if let Err(e) = client.kill_session(session_name) {
                            println!("Warning: Could not kill session: {}", e);
                        }
                    }
                    Err(e) => {
                        println!("Skipping tmux operations test: {}", e);
                    }
                }
            }
            Err(_) => {
                println!("Skipping test: tmux not available");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_concurrent_session_operations (lines 548-609) ---[0m
    async fn test_concurrent_session_operations() {
        match SessionManager::new() {
            Ok(manager) => {
                let temp_dir = TempDir::new().expect("Failed to create temp dir");
                let workspace_path = temp_dir.path().to_string_lossy().to_string();

                // Create multiple sessions concurrently
                let manager = std::sync::Arc::new(manager);
                let mut handles = Vec::new();
                for i in 0..3 {
                    let manager_clone = manager.clone();
                    let workspace_clone = workspace_path.clone();
                    
                    let handle = tokio::spawn(async move {
                        let agent_id = format!("concurrent-agent-{:03}", i);
                        let role = match i % 4 {
                            0 => default_frontend_role(),
                            1 => default_backend_role(),
                            2 => default_devops_role(),
                            _ => default_qa_role(),
                        };

                        manager_clone.create_session(
                            agent_id,
                            role,
                            workspace_clone,
                            Some(format!("Concurrent test session {}", i)),
                            false,
                        )
                    });
                    handles.push(handle);
                }

                // Wait for all sessions to be created
                let mut created_sessions = Vec::new();
                for handle in handles {
                    match handle.await {
                        Ok(Ok(session)) => created_sessions.push(session),
                        Ok(Err(e)) => println!("Session creation failed: {}", e),
                        Err(e) => println!("Task failed: {}", e),
                    }
                }

                if !created_sessions.is_empty() {
                    println!("Created {} concurrent sessions", created_sessions.len());

                    // Verify all sessions exist
                    let all_sessions = manager.list_sessions();
                    assert!(all_sessions.len() >= created_sessions.len());

                    // Clean up all sessions
                    for session in created_sessions {
                        let _ = manager.terminate_session(&session.id);
                    }
                    let _ = manager.cleanup_terminated_sessions();
                }
            }
            Err(_) => {
                println!("Skipping concurrent test: SessionManager creation failed");
            }
        }
    }

  ./crates/ccswarm/src/tests/session_tests.rs:361-424 function test_tmux_session_operations <-> ./crates/ccswarm/src/tests/session_tests.rs:427-487 function test_tmux_error_handling
  Similarity: 95.84%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_tmux_session_operations (lines 361-424) ---[0m
    async fn test_tmux_session_operations() {
        match TmuxClient::new() {
            Ok(client) => {
                let temp_dir = create_temp_workspace();
                let workspace_path = temp_dir.path().to_string_lossy();
                let session_name = "test-ccswarm-session";

                // Ensure server is running
                if client.ensure_server_running().is_err() {
                    println!("Skipping test: cannot start tmux server");
                    return;
                }

                // Test session creation
                match client.create_session(session_name, &workspace_path) {
                    Ok(_) => {
                        // Test session listing
                        match client.list_sessions() {
                            Ok(sessions) => {
                                let our_session = sessions
                                    .iter()
                                    .find(|s| s.name == session_name);
                                assert!(our_session.is_some());
                            }
                            Err(e) => {
                                println!("Warning: Could not list sessions: {}", e);
                            }
                        }

                        // Test session info
                        match client.get_session_info(session_name) {
                            Ok(session_info) => {
                                assert_eq!(session_info.name, session_name);
                            }
                            Err(e) => {
                                println!("Warning: Could not get session info: {}", e);
                            }
                        }

                        // Test environment variable setting
                        if let Err(e) = client.set_environment(session_name, "TEST_VAR", "test_value") {
                            println!("Warning: Could not set environment: {}", e);
                        }

                        // Test option setting
                        if let Err(e) = client.set_option(session_name, "status-left", "[TEST]") {
                            println!("Warning: Could not set option: {}", e);
                        }

                        // Clean up - kill the session
                        if let Err(e) = client.kill_session(session_name) {
                            println!("Warning: Could not kill session: {}", e);
                        }
                    }
                    Err(e) => {
                        println!("Skipping tmux operations test: {}", e);
                    }
                }
            }
            Err(_) => {
                println!("Skipping test: tmux not available");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_tmux_error_handling (lines 427-487) ---[0m
    fn test_tmux_error_handling() {
        match TmuxClient::new() {
            Ok(client) => {
                // Test operations on non-existent session
                let non_existent = "non-existent-session-12345";
                
                assert!(matches!(
                    client.kill_session(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.attach_session(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.send_keys(non_existent, "test"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.send_command(non_existent, "echo test"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.capture_pane(non_existent, None),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.list_windows(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.new_window(non_existent, "test-window", None),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.set_environment(non_existent, "VAR", "value"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.set_option(non_existent, "option", "value"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.get_session_info(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));
            }
            Err(_) => {
                println!("Skipping error handling test: tmux not available");
            }
        }
    }

  ./crates/ccswarm/src/tests/session_tests.rs:427-487 function test_tmux_error_handling <-> ./crates/ccswarm/src/tests/session_tests.rs:548-609 function test_concurrent_session_operations
  Similarity: 96.17%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_tmux_error_handling (lines 427-487) ---[0m
    fn test_tmux_error_handling() {
        match TmuxClient::new() {
            Ok(client) => {
                // Test operations on non-existent session
                let non_existent = "non-existent-session-12345";
                
                assert!(matches!(
                    client.kill_session(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.attach_session(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.send_keys(non_existent, "test"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.send_command(non_existent, "echo test"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.capture_pane(non_existent, None),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.list_windows(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.new_window(non_existent, "test-window", None),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.set_environment(non_existent, "VAR", "value"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.set_option(non_existent, "option", "value"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.get_session_info(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));
            }
            Err(_) => {
                println!("Skipping error handling test: tmux not available");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_concurrent_session_operations (lines 548-609) ---[0m
    async fn test_concurrent_session_operations() {
        match SessionManager::new() {
            Ok(manager) => {
                let temp_dir = TempDir::new().expect("Failed to create temp dir");
                let workspace_path = temp_dir.path().to_string_lossy().to_string();

                // Create multiple sessions concurrently
                let manager = std::sync::Arc::new(manager);
                let mut handles = Vec::new();
                for i in 0..3 {
                    let manager_clone = manager.clone();
                    let workspace_clone = workspace_path.clone();
                    
                    let handle = tokio::spawn(async move {
                        let agent_id = format!("concurrent-agent-{:03}", i);
                        let role = match i % 4 {
                            0 => default_frontend_role(),
                            1 => default_backend_role(),
                            2 => default_devops_role(),
                            _ => default_qa_role(),
                        };

                        manager_clone.create_session(
                            agent_id,
                            role,
                            workspace_clone,
                            Some(format!("Concurrent test session {}", i)),
                            false,
                        )
                    });
                    handles.push(handle);
                }

                // Wait for all sessions to be created
                let mut created_sessions = Vec::new();
                for handle in handles {
                    match handle.await {
                        Ok(Ok(session)) => created_sessions.push(session),
                        Ok(Err(e)) => println!("Session creation failed: {}", e),
                        Err(e) => println!("Task failed: {}", e),
                    }
                }

                if !created_sessions.is_empty() {
                    println!("Created {} concurrent sessions", created_sessions.len());

                    // Verify all sessions exist
                    let all_sessions = manager.list_sessions();
                    assert!(all_sessions.len() >= created_sessions.len());

                    // Clean up all sessions
                    for session in created_sessions {
                        let _ = manager.terminate_session(&session.id);
                    }
                    let _ = manager.cleanup_terminated_sessions();
                }
            }
            Err(_) => {
                println!("Skipping concurrent test: SessionManager creation failed");
            }
        }
    }

  ./crates/ccswarm/src/tests/session_tests.rs:178-240 function test_session_lifecycle <-> ./crates/ccswarm/src/tests/session_tests.rs:243-299 function test_sessions_by_role
  Similarity: 97.87%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_session_lifecycle (lines 178-240) ---[0m
    async fn test_session_lifecycle() {
        let temp_dir = create_temp_workspace();
        let workspace_path = temp_dir.path().to_string_lossy().to_string();

        match SessionManager::new() {
            Ok(manager) => {
                // Create session
                let session_result = manager.create_session(
                    "test-agent-001".to_string(),
                    default_frontend_role(),
                    workspace_path.clone(),
                    Some("Test session for frontend".to_string()),
                    false, // Don't auto-start to avoid command execution
                );

                match session_result {
                    Ok(session) => {
                        assert_eq!(session.agent_id, "test-agent-001");
                        assert_eq!(session.working_directory, workspace_path);
                        assert_eq!(session.status, SessionStatus::Active);

                        // Test session retrieval
                        let retrieved = manager.get_session(&session.id);
                        assert!(retrieved.is_some());
                        assert_eq!(retrieved.unwrap().id, session.id);

                        // Test session listing
                        let all_sessions = manager.list_sessions();
                        assert_eq!(all_sessions.len(), 1);
                        assert_eq!(all_sessions[0].id, session.id);

                        // Test active sessions
                        let active_sessions = manager.list_active_sessions();
                        assert_eq!(active_sessions.len(), 1);

                        // Test background mode
                        assert!(manager.set_background_mode(&session.id, true).is_ok());
                        let updated_session = manager.get_session(&session.id).unwrap();
                        assert!(updated_session.background_mode);
                        assert!(updated_session.auto_accept);

                        // Test session termination
                        assert!(manager.terminate_session(&session.id).is_ok());
                        let terminated_session = manager.get_session(&session.id).unwrap();
                        assert_eq!(terminated_session.status, SessionStatus::Terminated);

                        // Test cleanup
                        let cleaned_count = manager.cleanup_terminated_sessions().unwrap();
                        assert_eq!(cleaned_count, 1);
                        
                        let final_sessions = manager.list_sessions();
                        assert!(final_sessions.is_empty());
                    }
                    Err(e) => {
                        println!("Skipping session lifecycle test: {}", e);
                    }
                }
            }
            Err(_) => {
                println!("Skipping test: tmux not available");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_sessions_by_role (lines 243-299) ---[0m
    async fn test_sessions_by_role() {
        let temp_dir = create_temp_workspace();
        let workspace_path = temp_dir.path().to_string_lossy().to_string();

        match SessionManager::new() {
            Ok(manager) => {
                // Create sessions with different roles
                let roles_and_ids = vec![
                    ("frontend-001", default_frontend_role()),
                    ("backend-001", default_backend_role()),
                    ("frontend-002", default_frontend_role()),
                    ("devops-001", default_devops_role()),
                ];

                let mut created_sessions = Vec::new();
                for (agent_id, role) in roles_and_ids {
                    match manager.create_session(
                        agent_id.to_string(),
                        role.clone(),
                        workspace_path.clone(),
                        None,
                        false,
                    ) {
                        Ok(session) => created_sessions.push(session),
                        Err(_) => {
                            println!("Skipping multi-role test: session creation failed");
                            return;
                        }
                    }
                }

                if !created_sessions.is_empty() {
                    // Test filtering by role
                    let frontend_sessions = manager.get_sessions_by_role(default_frontend_role());
                    assert_eq!(frontend_sessions.len(), 2);

                    let backend_sessions = manager.get_sessions_by_role(default_backend_role());
                    assert_eq!(backend_sessions.len(), 1);

                    let devops_sessions = manager.get_sessions_by_role(default_devops_role());
                    assert_eq!(devops_sessions.len(), 1);

                    let qa_sessions = manager.get_sessions_by_role(default_qa_role());
                    assert_eq!(qa_sessions.len(), 0);

                    // Cleanup
                    for session in &created_sessions {
                        let _ = manager.terminate_session(&session.id);
                    }
                    let _ = manager.cleanup_terminated_sessions();
                }
            }
            Err(_) => {
                println!("Skipping test: tmux not available");
            }
        }
    }

  ./crates/ccswarm/src/tests/session_tests.rs:178-240 function test_session_lifecycle <-> ./crates/ccswarm/src/tests/session_tests.rs:361-424 function test_tmux_session_operations
  Similarity: 91.20%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_session_lifecycle (lines 178-240) ---[0m
    async fn test_session_lifecycle() {
        let temp_dir = create_temp_workspace();
        let workspace_path = temp_dir.path().to_string_lossy().to_string();

        match SessionManager::new() {
            Ok(manager) => {
                // Create session
                let session_result = manager.create_session(
                    "test-agent-001".to_string(),
                    default_frontend_role(),
                    workspace_path.clone(),
                    Some("Test session for frontend".to_string()),
                    false, // Don't auto-start to avoid command execution
                );

                match session_result {
                    Ok(session) => {
                        assert_eq!(session.agent_id, "test-agent-001");
                        assert_eq!(session.working_directory, workspace_path);
                        assert_eq!(session.status, SessionStatus::Active);

                        // Test session retrieval
                        let retrieved = manager.get_session(&session.id);
                        assert!(retrieved.is_some());
                        assert_eq!(retrieved.unwrap().id, session.id);

                        // Test session listing
                        let all_sessions = manager.list_sessions();
                        assert_eq!(all_sessions.len(), 1);
                        assert_eq!(all_sessions[0].id, session.id);

                        // Test active sessions
                        let active_sessions = manager.list_active_sessions();
                        assert_eq!(active_sessions.len(), 1);

                        // Test background mode
                        assert!(manager.set_background_mode(&session.id, true).is_ok());
                        let updated_session = manager.get_session(&session.id).unwrap();
                        assert!(updated_session.background_mode);
                        assert!(updated_session.auto_accept);

                        // Test session termination
                        assert!(manager.terminate_session(&session.id).is_ok());
                        let terminated_session = manager.get_session(&session.id).unwrap();
                        assert_eq!(terminated_session.status, SessionStatus::Terminated);

                        // Test cleanup
                        let cleaned_count = manager.cleanup_terminated_sessions().unwrap();
                        assert_eq!(cleaned_count, 1);
                        
                        let final_sessions = manager.list_sessions();
                        assert!(final_sessions.is_empty());
                    }
                    Err(e) => {
                        println!("Skipping session lifecycle test: {}", e);
                    }
                }
            }
            Err(_) => {
                println!("Skipping test: tmux not available");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_tmux_session_operations (lines 361-424) ---[0m
    async fn test_tmux_session_operations() {
        match TmuxClient::new() {
            Ok(client) => {
                let temp_dir = create_temp_workspace();
                let workspace_path = temp_dir.path().to_string_lossy();
                let session_name = "test-ccswarm-session";

                // Ensure server is running
                if client.ensure_server_running().is_err() {
                    println!("Skipping test: cannot start tmux server");
                    return;
                }

                // Test session creation
                match client.create_session(session_name, &workspace_path) {
                    Ok(_) => {
                        // Test session listing
                        match client.list_sessions() {
                            Ok(sessions) => {
                                let our_session = sessions
                                    .iter()
                                    .find(|s| s.name == session_name);
                                assert!(our_session.is_some());
                            }
                            Err(e) => {
                                println!("Warning: Could not list sessions: {}", e);
                            }
                        }

                        // Test session info
                        match client.get_session_info(session_name) {
                            Ok(session_info) => {
                                assert_eq!(session_info.name, session_name);
                            }
                            Err(e) => {
                                println!("Warning: Could not get session info: {}", e);
                            }
                        }

                        // Test environment variable setting
                        if let Err(e) = client.set_environment(session_name, "TEST_VAR", "test_value") {
                            println!("Warning: Could not set environment: {}", e);
                        }

                        // Test option setting
                        if let Err(e) = client.set_option(session_name, "status-left", "[TEST]") {
                            println!("Warning: Could not set option: {}", e);
                        }

                        // Clean up - kill the session
                        if let Err(e) = client.kill_session(session_name) {
                            println!("Warning: Could not kill session: {}", e);
                        }
                    }
                    Err(e) => {
                        println!("Skipping tmux operations test: {}", e);
                    }
                }
            }
            Err(_) => {
                println!("Skipping test: tmux not available");
            }
        }
    }

  ./crates/ccswarm/src/tests/session_tests.rs:178-240 function test_session_lifecycle <-> ./crates/ccswarm/src/tests/session_tests.rs:548-609 function test_concurrent_session_operations
  Similarity: 91.25%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_session_lifecycle (lines 178-240) ---[0m
    async fn test_session_lifecycle() {
        let temp_dir = create_temp_workspace();
        let workspace_path = temp_dir.path().to_string_lossy().to_string();

        match SessionManager::new() {
            Ok(manager) => {
                // Create session
                let session_result = manager.create_session(
                    "test-agent-001".to_string(),
                    default_frontend_role(),
                    workspace_path.clone(),
                    Some("Test session for frontend".to_string()),
                    false, // Don't auto-start to avoid command execution
                );

                match session_result {
                    Ok(session) => {
                        assert_eq!(session.agent_id, "test-agent-001");
                        assert_eq!(session.working_directory, workspace_path);
                        assert_eq!(session.status, SessionStatus::Active);

                        // Test session retrieval
                        let retrieved = manager.get_session(&session.id);
                        assert!(retrieved.is_some());
                        assert_eq!(retrieved.unwrap().id, session.id);

                        // Test session listing
                        let all_sessions = manager.list_sessions();
                        assert_eq!(all_sessions.len(), 1);
                        assert_eq!(all_sessions[0].id, session.id);

                        // Test active sessions
                        let active_sessions = manager.list_active_sessions();
                        assert_eq!(active_sessions.len(), 1);

                        // Test background mode
                        assert!(manager.set_background_mode(&session.id, true).is_ok());
                        let updated_session = manager.get_session(&session.id).unwrap();
                        assert!(updated_session.background_mode);
                        assert!(updated_session.auto_accept);

                        // Test session termination
                        assert!(manager.terminate_session(&session.id).is_ok());
                        let terminated_session = manager.get_session(&session.id).unwrap();
                        assert_eq!(terminated_session.status, SessionStatus::Terminated);

                        // Test cleanup
                        let cleaned_count = manager.cleanup_terminated_sessions().unwrap();
                        assert_eq!(cleaned_count, 1);
                        
                        let final_sessions = manager.list_sessions();
                        assert!(final_sessions.is_empty());
                    }
                    Err(e) => {
                        println!("Skipping session lifecycle test: {}", e);
                    }
                }
            }
            Err(_) => {
                println!("Skipping test: tmux not available");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_concurrent_session_operations (lines 548-609) ---[0m
    async fn test_concurrent_session_operations() {
        match SessionManager::new() {
            Ok(manager) => {
                let temp_dir = TempDir::new().expect("Failed to create temp dir");
                let workspace_path = temp_dir.path().to_string_lossy().to_string();

                // Create multiple sessions concurrently
                let manager = std::sync::Arc::new(manager);
                let mut handles = Vec::new();
                for i in 0..3 {
                    let manager_clone = manager.clone();
                    let workspace_clone = workspace_path.clone();
                    
                    let handle = tokio::spawn(async move {
                        let agent_id = format!("concurrent-agent-{:03}", i);
                        let role = match i % 4 {
                            0 => default_frontend_role(),
                            1 => default_backend_role(),
                            2 => default_devops_role(),
                            _ => default_qa_role(),
                        };

                        manager_clone.create_session(
                            agent_id,
                            role,
                            workspace_clone,
                            Some(format!("Concurrent test session {}", i)),
                            false,
                        )
                    });
                    handles.push(handle);
                }

                // Wait for all sessions to be created
                let mut created_sessions = Vec::new();
                for handle in handles {
                    match handle.await {
                        Ok(Ok(session)) => created_sessions.push(session),
                        Ok(Err(e)) => println!("Session creation failed: {}", e),
                        Err(e) => println!("Task failed: {}", e),
                    }
                }

                if !created_sessions.is_empty() {
                    println!("Created {} concurrent sessions", created_sessions.len());

                    // Verify all sessions exist
                    let all_sessions = manager.list_sessions();
                    assert!(all_sessions.len() >= created_sessions.len());

                    // Clean up all sessions
                    for session in created_sessions {
                        let _ = manager.terminate_session(&session.id);
                    }
                    let _ = manager.cleanup_terminated_sessions();
                }
            }
            Err(_) => {
                println!("Skipping concurrent test: SessionManager creation failed");
            }
        }
    }

  ./crates/ccswarm/src/tests/session_tests.rs:178-240 function test_session_lifecycle <-> ./crates/ccswarm/src/tests/session_tests.rs:427-487 function test_tmux_error_handling
  Similarity: 91.20%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_session_lifecycle (lines 178-240) ---[0m
    async fn test_session_lifecycle() {
        let temp_dir = create_temp_workspace();
        let workspace_path = temp_dir.path().to_string_lossy().to_string();

        match SessionManager::new() {
            Ok(manager) => {
                // Create session
                let session_result = manager.create_session(
                    "test-agent-001".to_string(),
                    default_frontend_role(),
                    workspace_path.clone(),
                    Some("Test session for frontend".to_string()),
                    false, // Don't auto-start to avoid command execution
                );

                match session_result {
                    Ok(session) => {
                        assert_eq!(session.agent_id, "test-agent-001");
                        assert_eq!(session.working_directory, workspace_path);
                        assert_eq!(session.status, SessionStatus::Active);

                        // Test session retrieval
                        let retrieved = manager.get_session(&session.id);
                        assert!(retrieved.is_some());
                        assert_eq!(retrieved.unwrap().id, session.id);

                        // Test session listing
                        let all_sessions = manager.list_sessions();
                        assert_eq!(all_sessions.len(), 1);
                        assert_eq!(all_sessions[0].id, session.id);

                        // Test active sessions
                        let active_sessions = manager.list_active_sessions();
                        assert_eq!(active_sessions.len(), 1);

                        // Test background mode
                        assert!(manager.set_background_mode(&session.id, true).is_ok());
                        let updated_session = manager.get_session(&session.id).unwrap();
                        assert!(updated_session.background_mode);
                        assert!(updated_session.auto_accept);

                        // Test session termination
                        assert!(manager.terminate_session(&session.id).is_ok());
                        let terminated_session = manager.get_session(&session.id).unwrap();
                        assert_eq!(terminated_session.status, SessionStatus::Terminated);

                        // Test cleanup
                        let cleaned_count = manager.cleanup_terminated_sessions().unwrap();
                        assert_eq!(cleaned_count, 1);
                        
                        let final_sessions = manager.list_sessions();
                        assert!(final_sessions.is_empty());
                    }
                    Err(e) => {
                        println!("Skipping session lifecycle test: {}", e);
                    }
                }
            }
            Err(_) => {
                println!("Skipping test: tmux not available");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_tmux_error_handling (lines 427-487) ---[0m
    fn test_tmux_error_handling() {
        match TmuxClient::new() {
            Ok(client) => {
                // Test operations on non-existent session
                let non_existent = "non-existent-session-12345";
                
                assert!(matches!(
                    client.kill_session(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.attach_session(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.send_keys(non_existent, "test"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.send_command(non_existent, "echo test"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.capture_pane(non_existent, None),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.list_windows(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.new_window(non_existent, "test-window", None),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.set_environment(non_existent, "VAR", "value"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.set_option(non_existent, "option", "value"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.get_session_info(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));
            }
            Err(_) => {
                println!("Skipping error handling test: tmux not available");
            }
        }
    }

  ./crates/ccswarm/src/tests/session_tests.rs:243-299 function test_sessions_by_role <-> ./crates/ccswarm/src/tests/session_tests.rs:361-424 function test_tmux_session_operations
  Similarity: 87.92%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_sessions_by_role (lines 243-299) ---[0m
    async fn test_sessions_by_role() {
        let temp_dir = create_temp_workspace();
        let workspace_path = temp_dir.path().to_string_lossy().to_string();

        match SessionManager::new() {
            Ok(manager) => {
                // Create sessions with different roles
                let roles_and_ids = vec![
                    ("frontend-001", default_frontend_role()),
                    ("backend-001", default_backend_role()),
                    ("frontend-002", default_frontend_role()),
                    ("devops-001", default_devops_role()),
                ];

                let mut created_sessions = Vec::new();
                for (agent_id, role) in roles_and_ids {
                    match manager.create_session(
                        agent_id.to_string(),
                        role.clone(),
                        workspace_path.clone(),
                        None,
                        false,
                    ) {
                        Ok(session) => created_sessions.push(session),
                        Err(_) => {
                            println!("Skipping multi-role test: session creation failed");
                            return;
                        }
                    }
                }

                if !created_sessions.is_empty() {
                    // Test filtering by role
                    let frontend_sessions = manager.get_sessions_by_role(default_frontend_role());
                    assert_eq!(frontend_sessions.len(), 2);

                    let backend_sessions = manager.get_sessions_by_role(default_backend_role());
                    assert_eq!(backend_sessions.len(), 1);

                    let devops_sessions = manager.get_sessions_by_role(default_devops_role());
                    assert_eq!(devops_sessions.len(), 1);

                    let qa_sessions = manager.get_sessions_by_role(default_qa_role());
                    assert_eq!(qa_sessions.len(), 0);

                    // Cleanup
                    for session in &created_sessions {
                        let _ = manager.terminate_session(&session.id);
                    }
                    let _ = manager.cleanup_terminated_sessions();
                }
            }
            Err(_) => {
                println!("Skipping test: tmux not available");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_tmux_session_operations (lines 361-424) ---[0m
    async fn test_tmux_session_operations() {
        match TmuxClient::new() {
            Ok(client) => {
                let temp_dir = create_temp_workspace();
                let workspace_path = temp_dir.path().to_string_lossy();
                let session_name = "test-ccswarm-session";

                // Ensure server is running
                if client.ensure_server_running().is_err() {
                    println!("Skipping test: cannot start tmux server");
                    return;
                }

                // Test session creation
                match client.create_session(session_name, &workspace_path) {
                    Ok(_) => {
                        // Test session listing
                        match client.list_sessions() {
                            Ok(sessions) => {
                                let our_session = sessions
                                    .iter()
                                    .find(|s| s.name == session_name);
                                assert!(our_session.is_some());
                            }
                            Err(e) => {
                                println!("Warning: Could not list sessions: {}", e);
                            }
                        }

                        // Test session info
                        match client.get_session_info(session_name) {
                            Ok(session_info) => {
                                assert_eq!(session_info.name, session_name);
                            }
                            Err(e) => {
                                println!("Warning: Could not get session info: {}", e);
                            }
                        }

                        // Test environment variable setting
                        if let Err(e) = client.set_environment(session_name, "TEST_VAR", "test_value") {
                            println!("Warning: Could not set environment: {}", e);
                        }

                        // Test option setting
                        if let Err(e) = client.set_option(session_name, "status-left", "[TEST]") {
                            println!("Warning: Could not set option: {}", e);
                        }

                        // Clean up - kill the session
                        if let Err(e) = client.kill_session(session_name) {
                            println!("Warning: Could not kill session: {}", e);
                        }
                    }
                    Err(e) => {
                        println!("Skipping tmux operations test: {}", e);
                    }
                }
            }
            Err(_) => {
                println!("Skipping test: tmux not available");
            }
        }
    }

  ./crates/ccswarm/src/tests/session_tests.rs:243-299 function test_sessions_by_role <-> ./crates/ccswarm/src/tests/session_tests.rs:548-609 function test_concurrent_session_operations
  Similarity: 89.09%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_sessions_by_role (lines 243-299) ---[0m
    async fn test_sessions_by_role() {
        let temp_dir = create_temp_workspace();
        let workspace_path = temp_dir.path().to_string_lossy().to_string();

        match SessionManager::new() {
            Ok(manager) => {
                // Create sessions with different roles
                let roles_and_ids = vec![
                    ("frontend-001", default_frontend_role()),
                    ("backend-001", default_backend_role()),
                    ("frontend-002", default_frontend_role()),
                    ("devops-001", default_devops_role()),
                ];

                let mut created_sessions = Vec::new();
                for (agent_id, role) in roles_and_ids {
                    match manager.create_session(
                        agent_id.to_string(),
                        role.clone(),
                        workspace_path.clone(),
                        None,
                        false,
                    ) {
                        Ok(session) => created_sessions.push(session),
                        Err(_) => {
                            println!("Skipping multi-role test: session creation failed");
                            return;
                        }
                    }
                }

                if !created_sessions.is_empty() {
                    // Test filtering by role
                    let frontend_sessions = manager.get_sessions_by_role(default_frontend_role());
                    assert_eq!(frontend_sessions.len(), 2);

                    let backend_sessions = manager.get_sessions_by_role(default_backend_role());
                    assert_eq!(backend_sessions.len(), 1);

                    let devops_sessions = manager.get_sessions_by_role(default_devops_role());
                    assert_eq!(devops_sessions.len(), 1);

                    let qa_sessions = manager.get_sessions_by_role(default_qa_role());
                    assert_eq!(qa_sessions.len(), 0);

                    // Cleanup
                    for session in &created_sessions {
                        let _ = manager.terminate_session(&session.id);
                    }
                    let _ = manager.cleanup_terminated_sessions();
                }
            }
            Err(_) => {
                println!("Skipping test: tmux not available");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_concurrent_session_operations (lines 548-609) ---[0m
    async fn test_concurrent_session_operations() {
        match SessionManager::new() {
            Ok(manager) => {
                let temp_dir = TempDir::new().expect("Failed to create temp dir");
                let workspace_path = temp_dir.path().to_string_lossy().to_string();

                // Create multiple sessions concurrently
                let manager = std::sync::Arc::new(manager);
                let mut handles = Vec::new();
                for i in 0..3 {
                    let manager_clone = manager.clone();
                    let workspace_clone = workspace_path.clone();
                    
                    let handle = tokio::spawn(async move {
                        let agent_id = format!("concurrent-agent-{:03}", i);
                        let role = match i % 4 {
                            0 => default_frontend_role(),
                            1 => default_backend_role(),
                            2 => default_devops_role(),
                            _ => default_qa_role(),
                        };

                        manager_clone.create_session(
                            agent_id,
                            role,
                            workspace_clone,
                            Some(format!("Concurrent test session {}", i)),
                            false,
                        )
                    });
                    handles.push(handle);
                }

                // Wait for all sessions to be created
                let mut created_sessions = Vec::new();
                for handle in handles {
                    match handle.await {
                        Ok(Ok(session)) => created_sessions.push(session),
                        Ok(Err(e)) => println!("Session creation failed: {}", e),
                        Err(e) => println!("Task failed: {}", e),
                    }
                }

                if !created_sessions.is_empty() {
                    println!("Created {} concurrent sessions", created_sessions.len());

                    // Verify all sessions exist
                    let all_sessions = manager.list_sessions();
                    assert!(all_sessions.len() >= created_sessions.len());

                    // Clean up all sessions
                    for session in created_sessions {
                        let _ = manager.terminate_session(&session.id);
                    }
                    let _ = manager.cleanup_terminated_sessions();
                }
            }
            Err(_) => {
                println!("Skipping concurrent test: SessionManager creation failed");
            }
        }
    }

  ./crates/ccswarm/src/tests/session_tests.rs:178-240 function test_session_lifecycle <-> ./crates/ccswarm/src/tests/session_tests.rs:501-545 function test_session_manager_tmux_integration
  Similarity: 97.92%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_session_lifecycle (lines 178-240) ---[0m
    async fn test_session_lifecycle() {
        let temp_dir = create_temp_workspace();
        let workspace_path = temp_dir.path().to_string_lossy().to_string();

        match SessionManager::new() {
            Ok(manager) => {
                // Create session
                let session_result = manager.create_session(
                    "test-agent-001".to_string(),
                    default_frontend_role(),
                    workspace_path.clone(),
                    Some("Test session for frontend".to_string()),
                    false, // Don't auto-start to avoid command execution
                );

                match session_result {
                    Ok(session) => {
                        assert_eq!(session.agent_id, "test-agent-001");
                        assert_eq!(session.working_directory, workspace_path);
                        assert_eq!(session.status, SessionStatus::Active);

                        // Test session retrieval
                        let retrieved = manager.get_session(&session.id);
                        assert!(retrieved.is_some());
                        assert_eq!(retrieved.unwrap().id, session.id);

                        // Test session listing
                        let all_sessions = manager.list_sessions();
                        assert_eq!(all_sessions.len(), 1);
                        assert_eq!(all_sessions[0].id, session.id);

                        // Test active sessions
                        let active_sessions = manager.list_active_sessions();
                        assert_eq!(active_sessions.len(), 1);

                        // Test background mode
                        assert!(manager.set_background_mode(&session.id, true).is_ok());
                        let updated_session = manager.get_session(&session.id).unwrap();
                        assert!(updated_session.background_mode);
                        assert!(updated_session.auto_accept);

                        // Test session termination
                        assert!(manager.terminate_session(&session.id).is_ok());
                        let terminated_session = manager.get_session(&session.id).unwrap();
                        assert_eq!(terminated_session.status, SessionStatus::Terminated);

                        // Test cleanup
                        let cleaned_count = manager.cleanup_terminated_sessions().unwrap();
                        assert_eq!(cleaned_count, 1);
                        
                        let final_sessions = manager.list_sessions();
                        assert!(final_sessions.is_empty());
                    }
                    Err(e) => {
                        println!("Skipping session lifecycle test: {}", e);
                    }
                }
            }
            Err(_) => {
                println!("Skipping test: tmux not available");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_session_manager_tmux_integration (lines 501-545) ---[0m
    async fn test_session_manager_tmux_integration() {
        let temp_dir = TempDir::new().expect("Failed to create temp dir");
        let workspace_path = temp_dir.path().to_string_lossy().to_string();

        match SessionManager::new() {
            Ok(manager) => {
                // Test creating a session that actually creates a tmux session
                match manager.create_session(
                    "integration-test-001".to_string(),
                    default_frontend_role(),
                    workspace_path,
                    Some("Integration test session".to_string()),
                    false, // Don't auto-start
                ) {
                    Ok(session) => {
                        // Verify the tmux session exists
                        let tmux_client = TmuxClient::new().unwrap();
                        match tmux_client.list_sessions() {
                            Ok(tmux_sessions) => {
                                let found_session = tmux_sessions
                                    .iter()
                                    .find(|s| s.name == session.tmux_session);
                                assert!(found_session.is_some(), "Tmux session should exist");

                                // Clean up
                                let _ = manager.terminate_session(&session.id);
                                let _ = manager.cleanup_terminated_sessions();
                            }
                            Err(e) => {
                                println!("Could not verify tmux session: {}", e);
                                // Clean up anyway
                                let _ = manager.terminate_session(&session.id);
                            }
                        }
                    }
                    Err(e) => {
                        println!("Skipping integration test: {}", e);
                    }
                }
            }
            Err(_) => {
                println!("Skipping integration test: SessionManager creation failed");
            }
        }
    }

  ./crates/ccswarm/src/tests/session_tests.rs:243-299 function test_sessions_by_role <-> ./crates/ccswarm/src/tests/session_tests.rs:427-487 function test_tmux_error_handling
  Similarity: 87.71%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_sessions_by_role (lines 243-299) ---[0m
    async fn test_sessions_by_role() {
        let temp_dir = create_temp_workspace();
        let workspace_path = temp_dir.path().to_string_lossy().to_string();

        match SessionManager::new() {
            Ok(manager) => {
                // Create sessions with different roles
                let roles_and_ids = vec![
                    ("frontend-001", default_frontend_role()),
                    ("backend-001", default_backend_role()),
                    ("frontend-002", default_frontend_role()),
                    ("devops-001", default_devops_role()),
                ];

                let mut created_sessions = Vec::new();
                for (agent_id, role) in roles_and_ids {
                    match manager.create_session(
                        agent_id.to_string(),
                        role.clone(),
                        workspace_path.clone(),
                        None,
                        false,
                    ) {
                        Ok(session) => created_sessions.push(session),
                        Err(_) => {
                            println!("Skipping multi-role test: session creation failed");
                            return;
                        }
                    }
                }

                if !created_sessions.is_empty() {
                    // Test filtering by role
                    let frontend_sessions = manager.get_sessions_by_role(default_frontend_role());
                    assert_eq!(frontend_sessions.len(), 2);

                    let backend_sessions = manager.get_sessions_by_role(default_backend_role());
                    assert_eq!(backend_sessions.len(), 1);

                    let devops_sessions = manager.get_sessions_by_role(default_devops_role());
                    assert_eq!(devops_sessions.len(), 1);

                    let qa_sessions = manager.get_sessions_by_role(default_qa_role());
                    assert_eq!(qa_sessions.len(), 0);

                    // Cleanup
                    for session in &created_sessions {
                        let _ = manager.terminate_session(&session.id);
                    }
                    let _ = manager.cleanup_terminated_sessions();
                }
            }
            Err(_) => {
                println!("Skipping test: tmux not available");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_tmux_error_handling (lines 427-487) ---[0m
    fn test_tmux_error_handling() {
        match TmuxClient::new() {
            Ok(client) => {
                // Test operations on non-existent session
                let non_existent = "non-existent-session-12345";
                
                assert!(matches!(
                    client.kill_session(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.attach_session(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.send_keys(non_existent, "test"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.send_command(non_existent, "echo test"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.capture_pane(non_existent, None),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.list_windows(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.new_window(non_existent, "test-window", None),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.set_environment(non_existent, "VAR", "value"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.set_option(non_existent, "option", "value"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.get_session_info(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));
            }
            Err(_) => {
                println!("Skipping error handling test: tmux not available");
            }
        }
    }

  ./crates/ccswarm/src/tests/session_tests.rs:243-299 function test_sessions_by_role <-> ./crates/ccswarm/src/tests/session_tests.rs:501-545 function test_session_manager_tmux_integration
  Similarity: 97.49%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_sessions_by_role (lines 243-299) ---[0m
    async fn test_sessions_by_role() {
        let temp_dir = create_temp_workspace();
        let workspace_path = temp_dir.path().to_string_lossy().to_string();

        match SessionManager::new() {
            Ok(manager) => {
                // Create sessions with different roles
                let roles_and_ids = vec![
                    ("frontend-001", default_frontend_role()),
                    ("backend-001", default_backend_role()),
                    ("frontend-002", default_frontend_role()),
                    ("devops-001", default_devops_role()),
                ];

                let mut created_sessions = Vec::new();
                for (agent_id, role) in roles_and_ids {
                    match manager.create_session(
                        agent_id.to_string(),
                        role.clone(),
                        workspace_path.clone(),
                        None,
                        false,
                    ) {
                        Ok(session) => created_sessions.push(session),
                        Err(_) => {
                            println!("Skipping multi-role test: session creation failed");
                            return;
                        }
                    }
                }

                if !created_sessions.is_empty() {
                    // Test filtering by role
                    let frontend_sessions = manager.get_sessions_by_role(default_frontend_role());
                    assert_eq!(frontend_sessions.len(), 2);

                    let backend_sessions = manager.get_sessions_by_role(default_backend_role());
                    assert_eq!(backend_sessions.len(), 1);

                    let devops_sessions = manager.get_sessions_by_role(default_devops_role());
                    assert_eq!(devops_sessions.len(), 1);

                    let qa_sessions = manager.get_sessions_by_role(default_qa_role());
                    assert_eq!(qa_sessions.len(), 0);

                    // Cleanup
                    for session in &created_sessions {
                        let _ = manager.terminate_session(&session.id);
                    }
                    let _ = manager.cleanup_terminated_sessions();
                }
            }
            Err(_) => {
                println!("Skipping test: tmux not available");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_session_manager_tmux_integration (lines 501-545) ---[0m
    async fn test_session_manager_tmux_integration() {
        let temp_dir = TempDir::new().expect("Failed to create temp dir");
        let workspace_path = temp_dir.path().to_string_lossy().to_string();

        match SessionManager::new() {
            Ok(manager) => {
                // Test creating a session that actually creates a tmux session
                match manager.create_session(
                    "integration-test-001".to_string(),
                    default_frontend_role(),
                    workspace_path,
                    Some("Integration test session".to_string()),
                    false, // Don't auto-start
                ) {
                    Ok(session) => {
                        // Verify the tmux session exists
                        let tmux_client = TmuxClient::new().unwrap();
                        match tmux_client.list_sessions() {
                            Ok(tmux_sessions) => {
                                let found_session = tmux_sessions
                                    .iter()
                                    .find(|s| s.name == session.tmux_session);
                                assert!(found_session.is_some(), "Tmux session should exist");

                                // Clean up
                                let _ = manager.terminate_session(&session.id);
                                let _ = manager.cleanup_terminated_sessions();
                            }
                            Err(e) => {
                                println!("Could not verify tmux session: {}", e);
                                // Clean up anyway
                                let _ = manager.terminate_session(&session.id);
                            }
                        }
                    }
                    Err(e) => {
                        println!("Skipping integration test: {}", e);
                    }
                }
            }
            Err(_) => {
                println!("Skipping integration test: SessionManager creation failed");
            }
        }
    }

  ./crates/ccswarm/src/tests/session_tests.rs:361-424 function test_tmux_session_operations <-> ./crates/ccswarm/src/tests/session_tests.rs:501-545 function test_session_manager_tmux_integration
  Similarity: 87.28%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_tmux_session_operations (lines 361-424) ---[0m
    async fn test_tmux_session_operations() {
        match TmuxClient::new() {
            Ok(client) => {
                let temp_dir = create_temp_workspace();
                let workspace_path = temp_dir.path().to_string_lossy();
                let session_name = "test-ccswarm-session";

                // Ensure server is running
                if client.ensure_server_running().is_err() {
                    println!("Skipping test: cannot start tmux server");
                    return;
                }

                // Test session creation
                match client.create_session(session_name, &workspace_path) {
                    Ok(_) => {
                        // Test session listing
                        match client.list_sessions() {
                            Ok(sessions) => {
                                let our_session = sessions
                                    .iter()
                                    .find(|s| s.name == session_name);
                                assert!(our_session.is_some());
                            }
                            Err(e) => {
                                println!("Warning: Could not list sessions: {}", e);
                            }
                        }

                        // Test session info
                        match client.get_session_info(session_name) {
                            Ok(session_info) => {
                                assert_eq!(session_info.name, session_name);
                            }
                            Err(e) => {
                                println!("Warning: Could not get session info: {}", e);
                            }
                        }

                        // Test environment variable setting
                        if let Err(e) = client.set_environment(session_name, "TEST_VAR", "test_value") {
                            println!("Warning: Could not set environment: {}", e);
                        }

                        // Test option setting
                        if let Err(e) = client.set_option(session_name, "status-left", "[TEST]") {
                            println!("Warning: Could not set option: {}", e);
                        }

                        // Clean up - kill the session
                        if let Err(e) = client.kill_session(session_name) {
                            println!("Warning: Could not kill session: {}", e);
                        }
                    }
                    Err(e) => {
                        println!("Skipping tmux operations test: {}", e);
                    }
                }
            }
            Err(_) => {
                println!("Skipping test: tmux not available");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_session_manager_tmux_integration (lines 501-545) ---[0m
    async fn test_session_manager_tmux_integration() {
        let temp_dir = TempDir::new().expect("Failed to create temp dir");
        let workspace_path = temp_dir.path().to_string_lossy().to_string();

        match SessionManager::new() {
            Ok(manager) => {
                // Test creating a session that actually creates a tmux session
                match manager.create_session(
                    "integration-test-001".to_string(),
                    default_frontend_role(),
                    workspace_path,
                    Some("Integration test session".to_string()),
                    false, // Don't auto-start
                ) {
                    Ok(session) => {
                        // Verify the tmux session exists
                        let tmux_client = TmuxClient::new().unwrap();
                        match tmux_client.list_sessions() {
                            Ok(tmux_sessions) => {
                                let found_session = tmux_sessions
                                    .iter()
                                    .find(|s| s.name == session.tmux_session);
                                assert!(found_session.is_some(), "Tmux session should exist");

                                // Clean up
                                let _ = manager.terminate_session(&session.id);
                                let _ = manager.cleanup_terminated_sessions();
                            }
                            Err(e) => {
                                println!("Could not verify tmux session: {}", e);
                                // Clean up anyway
                                let _ = manager.terminate_session(&session.id);
                            }
                        }
                    }
                    Err(e) => {
                        println!("Skipping integration test: {}", e);
                    }
                }
            }
            Err(_) => {
                println!("Skipping integration test: SessionManager creation failed");
            }
        }
    }

  ./crates/ccswarm/src/tests/session_tests.rs:501-545 function test_session_manager_tmux_integration <-> ./crates/ccswarm/src/tests/session_tests.rs:548-609 function test_concurrent_session_operations
  Similarity: 88.52%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_session_manager_tmux_integration (lines 501-545) ---[0m
    async fn test_session_manager_tmux_integration() {
        let temp_dir = TempDir::new().expect("Failed to create temp dir");
        let workspace_path = temp_dir.path().to_string_lossy().to_string();

        match SessionManager::new() {
            Ok(manager) => {
                // Test creating a session that actually creates a tmux session
                match manager.create_session(
                    "integration-test-001".to_string(),
                    default_frontend_role(),
                    workspace_path,
                    Some("Integration test session".to_string()),
                    false, // Don't auto-start
                ) {
                    Ok(session) => {
                        // Verify the tmux session exists
                        let tmux_client = TmuxClient::new().unwrap();
                        match tmux_client.list_sessions() {
                            Ok(tmux_sessions) => {
                                let found_session = tmux_sessions
                                    .iter()
                                    .find(|s| s.name == session.tmux_session);
                                assert!(found_session.is_some(), "Tmux session should exist");

                                // Clean up
                                let _ = manager.terminate_session(&session.id);
                                let _ = manager.cleanup_terminated_sessions();
                            }
                            Err(e) => {
                                println!("Could not verify tmux session: {}", e);
                                // Clean up anyway
                                let _ = manager.terminate_session(&session.id);
                            }
                        }
                    }
                    Err(e) => {
                        println!("Skipping integration test: {}", e);
                    }
                }
            }
            Err(_) => {
                println!("Skipping integration test: SessionManager creation failed");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_concurrent_session_operations (lines 548-609) ---[0m
    async fn test_concurrent_session_operations() {
        match SessionManager::new() {
            Ok(manager) => {
                let temp_dir = TempDir::new().expect("Failed to create temp dir");
                let workspace_path = temp_dir.path().to_string_lossy().to_string();

                // Create multiple sessions concurrently
                let manager = std::sync::Arc::new(manager);
                let mut handles = Vec::new();
                for i in 0..3 {
                    let manager_clone = manager.clone();
                    let workspace_clone = workspace_path.clone();
                    
                    let handle = tokio::spawn(async move {
                        let agent_id = format!("concurrent-agent-{:03}", i);
                        let role = match i % 4 {
                            0 => default_frontend_role(),
                            1 => default_backend_role(),
                            2 => default_devops_role(),
                            _ => default_qa_role(),
                        };

                        manager_clone.create_session(
                            agent_id,
                            role,
                            workspace_clone,
                            Some(format!("Concurrent test session {}", i)),
                            false,
                        )
                    });
                    handles.push(handle);
                }

                // Wait for all sessions to be created
                let mut created_sessions = Vec::new();
                for handle in handles {
                    match handle.await {
                        Ok(Ok(session)) => created_sessions.push(session),
                        Ok(Err(e)) => println!("Session creation failed: {}", e),
                        Err(e) => println!("Task failed: {}", e),
                    }
                }

                if !created_sessions.is_empty() {
                    println!("Created {} concurrent sessions", created_sessions.len());

                    // Verify all sessions exist
                    let all_sessions = manager.list_sessions();
                    assert!(all_sessions.len() >= created_sessions.len());

                    // Clean up all sessions
                    for session in created_sessions {
                        let _ = manager.terminate_session(&session.id);
                    }
                    let _ = manager.cleanup_terminated_sessions();
                }
            }
            Err(_) => {
                println!("Skipping concurrent test: SessionManager creation failed");
            }
        }
    }

  ./crates/ccswarm/src/tests/session_tests.rs:427-487 function test_tmux_error_handling <-> ./crates/ccswarm/src/tests/session_tests.rs:501-545 function test_session_manager_tmux_integration
  Similarity: 85.64%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_tmux_error_handling (lines 427-487) ---[0m
    fn test_tmux_error_handling() {
        match TmuxClient::new() {
            Ok(client) => {
                // Test operations on non-existent session
                let non_existent = "non-existent-session-12345";
                
                assert!(matches!(
                    client.kill_session(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.attach_session(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.send_keys(non_existent, "test"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.send_command(non_existent, "echo test"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.capture_pane(non_existent, None),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.list_windows(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.new_window(non_existent, "test-window", None),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.set_environment(non_existent, "VAR", "value"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.set_option(non_existent, "option", "value"),
                    Err(TmuxError::SessionNotFound(_))
                ));

                assert!(matches!(
                    client.get_session_info(non_existent),
                    Err(TmuxError::SessionNotFound(_))
                ));
            }
            Err(_) => {
                println!("Skipping error handling test: tmux not available");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_session_manager_tmux_integration (lines 501-545) ---[0m
    async fn test_session_manager_tmux_integration() {
        let temp_dir = TempDir::new().expect("Failed to create temp dir");
        let workspace_path = temp_dir.path().to_string_lossy().to_string();

        match SessionManager::new() {
            Ok(manager) => {
                // Test creating a session that actually creates a tmux session
                match manager.create_session(
                    "integration-test-001".to_string(),
                    default_frontend_role(),
                    workspace_path,
                    Some("Integration test session".to_string()),
                    false, // Don't auto-start
                ) {
                    Ok(session) => {
                        // Verify the tmux session exists
                        let tmux_client = TmuxClient::new().unwrap();
                        match tmux_client.list_sessions() {
                            Ok(tmux_sessions) => {
                                let found_session = tmux_sessions
                                    .iter()
                                    .find(|s| s.name == session.tmux_session);
                                assert!(found_session.is_some(), "Tmux session should exist");

                                // Clean up
                                let _ = manager.terminate_session(&session.id);
                                let _ = manager.cleanup_terminated_sessions();
                            }
                            Err(e) => {
                                println!("Could not verify tmux session: {}", e);
                                // Clean up anyway
                                let _ = manager.terminate_session(&session.id);
                            }
                        }
                    }
                    Err(e) => {
                        println!("Skipping integration test: {}", e);
                    }
                }
            }
            Err(_) => {
                println!("Skipping integration test: SessionManager creation failed");
            }
        }
    }

  ./crates/ccswarm/src/tests/session_tests.rs:302-319 function test_session_state_transitions <-> ./crates/ccswarm/src/tests/session_tests.rs:345-358 function test_tmux_client_with_timeout
  Similarity: 87.73%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_session_state_transitions (lines 302-319) ---[0m
    fn test_session_state_transitions() {
        match SessionManager::new() {
            Ok(manager) => {
                // Test invalid state transitions
                let non_existent_id = "non-existent-session";
                
                assert!(manager.pause_session(non_existent_id).is_err());
                assert!(manager.resume_session(non_existent_id).is_err());
                assert!(manager.detach_session(non_existent_id).is_err());
                assert!(manager.attach_session(non_existent_id).is_err());
                assert!(manager.terminate_session(non_existent_id).is_err());
                assert!(manager.set_background_mode(non_existent_id, true).is_err());
            }
            Err(_) => {
                println!("Skipping test: tmux not available");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_tmux_client_with_timeout (lines 345-358) ---[0m
    fn test_tmux_client_with_timeout() {
        let custom_timeout = Duration::from_secs(60);
        match TmuxClient::with_timeout(custom_timeout) {
            Ok(_client) => {
                // Test passes if client is created with custom timeout
            }
            Err(TmuxError::TmuxNotFound) => {
                println!("Tmux not installed, skipping timeout test");
            }
            Err(e) => {
                panic!("Unexpected error creating tmux client with timeout: {}", e);
            }
        }
    }

  ./crates/ccswarm/src/tests/session_tests.rs:33-46 function test_session_touch <-> ./crates/ccswarm/src/tests/session_tests.rs:98-116 function test_session_tmux_naming
  Similarity: 83.42%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_session_touch (lines 33-46) ---[0m
    fn test_session_touch() {
        let mut session = AgentSession::new(
            "backend-001".to_string(),
            default_backend_role(),
            "/tmp/backend-workspace".to_string(),
            None,
        );

        let initial_time = session.last_activity;
        std::thread::sleep(Duration::from_millis(10));
        session.touch();
        
        assert!(session.last_activity > initial_time);
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_session_tmux_naming (lines 98-116) ---[0m
    fn test_session_tmux_naming() {
        let frontend_session = AgentSession::new(
            "frontend-001".to_string(),
            default_frontend_role(),
            "/tmp/frontend".to_string(),
            None,
        );

        let backend_session = AgentSession::new(
            "backend-001".to_string(),
            default_backend_role(),
            "/tmp/backend".to_string(),
            None,
        );

        assert!(frontend_session.tmux_session.starts_with("ccswarm-frontend-"));
        assert!(backend_session.tmux_session.starts_with("ccswarm-backend-"));
        assert_ne!(frontend_session.tmux_session, backend_session.tmux_session);
    }

  ./crates/ccswarm/src/tests/session_tests.rs:79-95 function test_increment_tasks_processed <-> ./crates/ccswarm/src/tests/session_tests.rs:98-116 function test_session_tmux_naming
  Similarity: 71.58%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_increment_tasks_processed (lines 79-95) ---[0m
    fn test_increment_tasks_processed() {
        let mut session = AgentSession::new(
            "qa-001".to_string(),
            default_qa_role(),
            "/tmp/qa-workspace".to_string(),
            None,
        );

        let initial_time = session.last_activity;
        assert_eq!(session.tasks_processed, 0);

        std::thread::sleep(Duration::from_millis(10));
        session.increment_tasks_processed();

        assert_eq!(session.tasks_processed, 1);
        assert!(session.last_activity > initial_time);
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_session_tmux_naming (lines 98-116) ---[0m
    fn test_session_tmux_naming() {
        let frontend_session = AgentSession::new(
            "frontend-001".to_string(),
            default_frontend_role(),
            "/tmp/frontend".to_string(),
            None,
        );

        let backend_session = AgentSession::new(
            "backend-001".to_string(),
            default_backend_role(),
            "/tmp/backend".to_string(),
            None,
        );

        assert!(frontend_session.tmux_session.starts_with("ccswarm-frontend-"));
        assert!(backend_session.tmux_session.starts_with("ccswarm-backend-"));
        assert_ne!(frontend_session.tmux_session, backend_session.tmux_session);
    }

  ./crates/ccswarm/src/tests/session_tests.rs:163-175 function test_session_manager_creation <-> ./crates/ccswarm/src/tests/session_tests.rs:328-342 function test_tmux_client_creation
  Similarity: 90.22%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_session_manager_creation (lines 163-175) ---[0m
    async fn test_session_manager_creation() {
        // Note: This test may fail if tmux is not installed
        match SessionManager::new() {
            Ok(manager) => {
                let sessions = manager.list_sessions();
                assert!(sessions.is_empty());
            }
            Err(_) => {
                // Skip test if tmux is not available
                println!("Skipping test: tmux not available");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_tmux_client_creation (lines 328-342) ---[0m
    fn test_tmux_client_creation() {
        match TmuxClient::new() {
            Ok(client) => {
                // Test server running check
                let _is_running = client.is_server_running();
                // This test passes if tmux client can be created
            }
            Err(TmuxError::TmuxNotFound) => {
                println!("Tmux not installed, skipping tmux client tests");
            }
            Err(e) => {
                panic!("Unexpected error creating tmux client: {}", e);
            }
        }
    }

  ./crates/ccswarm/src/tests/session_tests.rs:33-46 function test_session_touch <-> ./crates/ccswarm/src/tests/session_tests.rs:79-95 function test_increment_tasks_processed
  Similarity: 78.58%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_session_touch (lines 33-46) ---[0m
    fn test_session_touch() {
        let mut session = AgentSession::new(
            "backend-001".to_string(),
            default_backend_role(),
            "/tmp/backend-workspace".to_string(),
            None,
        );

        let initial_time = session.last_activity;
        std::thread::sleep(Duration::from_millis(10));
        session.touch();
        
        assert!(session.last_activity > initial_time);
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_increment_tasks_processed (lines 79-95) ---[0m
    fn test_increment_tasks_processed() {
        let mut session = AgentSession::new(
            "qa-001".to_string(),
            default_qa_role(),
            "/tmp/qa-workspace".to_string(),
            None,
        );

        let initial_time = session.last_activity;
        assert_eq!(session.tasks_processed, 0);

        std::thread::sleep(Duration::from_millis(10));
        session.increment_tasks_processed();

        assert_eq!(session.tasks_processed, 1);
        assert!(session.last_activity > initial_time);
    }

  ./crates/ccswarm/src/tests/session_tests.rs:163-175 function test_session_manager_creation <-> ./crates/ccswarm/src/tests/session_tests.rs:345-358 function test_tmux_client_with_timeout
  Similarity: 88.91%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_session_manager_creation (lines 163-175) ---[0m
    async fn test_session_manager_creation() {
        // Note: This test may fail if tmux is not installed
        match SessionManager::new() {
            Ok(manager) => {
                let sessions = manager.list_sessions();
                assert!(sessions.is_empty());
            }
            Err(_) => {
                // Skip test if tmux is not available
                println!("Skipping test: tmux not available");
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_tmux_client_with_timeout (lines 345-358) ---[0m
    fn test_tmux_client_with_timeout() {
        let custom_timeout = Duration::from_secs(60);
        match TmuxClient::with_timeout(custom_timeout) {
            Ok(_client) => {
                // Test passes if client is created with custom timeout
            }
            Err(TmuxError::TmuxNotFound) => {
                println!("Tmux not installed, skipping timeout test");
            }
            Err(e) => {
                panic!("Unexpected error creating tmux client with timeout: {}", e);
            }
        }
    }

  ./crates/ccswarm/src/tests/session_tests.rs:328-342 function test_tmux_client_creation <-> ./crates/ccswarm/src/tests/session_tests.rs:345-358 function test_tmux_client_with_timeout
  Similarity: 80.22%

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_tmux_client_creation (lines 328-342) ---[0m
    fn test_tmux_client_creation() {
        match TmuxClient::new() {
            Ok(client) => {
                // Test server running check
                let _is_running = client.is_server_running();
                // This test passes if tmux client can be created
            }
            Err(TmuxError::TmuxNotFound) => {
                println!("Tmux not installed, skipping tmux client tests");
            }
            Err(e) => {
                panic!("Unexpected error creating tmux client: {}", e);
            }
        }
    }

[36m--- ./crates/ccswarm/src/tests/session_tests.rs:test_tmux_client_with_timeout (lines 345-358) ---[0m
    fn test_tmux_client_with_timeout() {
        let custom_timeout = Duration::from_secs(60);
        match TmuxClient::with_timeout(custom_timeout) {
            Ok(_client) => {
                // Test passes if client is created with custom timeout
            }
            Err(TmuxError::TmuxNotFound) => {
                println!("Tmux not installed, skipping timeout test");
            }
            Err(e) => {
                panic!("Unexpected error creating tmux client with timeout: {}", e);
            }
        }
    }


Duplicates in ./crates/ccswarm/src/cli/setup_wizard.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/cli/setup_wizard.rs:230-253 method prompt <-> ./crates/ccswarm/src/cli/setup_wizard.rs:270-290 method confirm
  Similarity: 81.06%
  Classes: SetupWizard <-> SetupWizard

[36m--- ./crates/ccswarm/src/cli/setup_wizard.rs:prompt (lines 230-253) ---[0m
    fn prompt(question: &str, default: &str, hint: Option<&str>) -> Result<String> {
        print!("{} ", question.bright_cyan());
        if !default.is_empty() {
            print!("[{}] ", default.dimmed());
        }

        if let Some(hint_text) = hint {
            println!();
            println!("  {}", hint_text.dimmed());
            print!("> ");
        }

        io::stdout().flush()?;

        let mut input = String::new();
        io::stdin().read_line(&mut input)?;
        let input = input.trim();

        Ok(if input.is_empty() {
            default.to_string()
        } else {
            input.to_string()
        })
    }

[36m--- ./crates/ccswarm/src/cli/setup_wizard.rs:confirm (lines 270-290) ---[0m
    fn confirm(question: &str, default: bool, hint: Option<&str>) -> Result<bool> {
        print!("{} ", question.bright_cyan());
        print!("[{}] ", if default { "Y/n" } else { "y/N" }.dimmed());

        if let Some(hint_text) = hint {
            print!("- {}", hint_text.dimmed());
        }

        print!(": ");
        io::stdout().flush()?;

        let mut input = String::new();
        io::stdin().read_line(&mut input)?;
        let input = input.trim().to_lowercase();

        Ok(if input.is_empty() {
            default
        } else {
            input == "y" || input == "yes"
        })
    }


Duplicates in ./crates/ccswarm/src/session/traits.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/session/traits.rs:149-155 function create_env_vars <-> ./crates/ccswarm/src/session/traits.rs:198-226 function get_or_create_session
  Similarity: 85.68%

[36m--- ./crates/ccswarm/src/session/traits.rs:create_env_vars (lines 149-155) ---[0m
    fn create_env_vars(agent_id: &str, session_id: &str) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert("CCSWARM_SESSION_TYPE".to_string(), "generic".to_string());
        env_vars
    }

[36m--- ./crates/ccswarm/src/session/traits.rs:get_or_create_session (lines 198-226) ---[0m
    async fn get_or_create_session(&self, agent_id: &str) -> Result<Arc<Mutex<Self::SessionType>>> {
        // Default implementation
        let sessions = self.list_sessions().await;

        // Look for an existing active session for this agent
        for session_info in sessions {
            if session_info.id.contains(agent_id)
                && matches!(
                    session_info.status,
                    SessionStatus::Active | SessionStatus::Idle
                )
            {
                if let Some(session) = self.get_session(&session_info.id).await {
                    return Ok(session);
                }
            }
        }

        // Create a new session
        let config = SessionConfig {
            agent_id: agent_id.to_string(),
            working_directory: None,
            env_vars: HashMap::new(),
            auto_cleanup: true,
            idle_timeout: Some(Duration::from_secs(300)),
        };

        self.create_session(config).await
    }

  ./crates/ccswarm/src/session/traits.rs:105-112 function execute_task_batch <-> ./crates/ccswarm/src/session/traits.rs:149-155 function create_env_vars
  Similarity: 79.63%

[36m--- ./crates/ccswarm/src/session/traits.rs:execute_task_batch (lines 105-112) ---[0m
    async fn execute_task_batch(&mut self, tasks: Vec<Task>) -> Result<Vec<TaskResult>> {
        // Default implementation: execute tasks sequentially
        let mut results = Vec::new();
        for task in tasks {
            results.push(self.execute_task(task).await?);
        }
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/session/traits.rs:create_env_vars (lines 149-155) ---[0m
    fn create_env_vars(agent_id: &str, session_id: &str) -> HashMap<String, String> {
        let mut env_vars = HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert("CCSWARM_SESSION_TYPE".to_string(), "generic".to_string());
        env_vars
    }


Duplicates in ./crates/ccswarm/src/session/persistent_session.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/session/persistent_session.rs:196-249 method create_new_session <-> ./crates/ccswarm/src/session/persistent_session.rs:286-326 method execute_task_batch
  Similarity: 89.29%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:create_new_session (lines 196-249) ---[0m
    async fn create_new_session(
        &self,
        agent_id: String,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        tracing::info!("Creating new persistent session for agent: {}", agent_id);

        // Create workspace path
        let workspace_path = self.workspace_root.join(format!("agents/{}", &agent_id));
        tokio::fs::create_dir_all(&workspace_path)
            .await
            .context("Failed to create agent workspace")?;

        // Create agent identity
        let identity = AgentIdentity {
            agent_id: agent_id.clone(),
            specialization: role,
            workspace_path,
            env_vars: Self::create_env_vars(&agent_id),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: std::process::id().to_string(),
            initialized_at: Utc::now(),
        };

        // Create persistent agent
        let agent = PersistentClaudeAgent::new(identity, claude_config).await?;
        let agent = Arc::new(Mutex::new(agent));

        // Create session info
        let session_info = PersistentSessionInfo {
            session_id: Uuid::new_v4().to_string(),
            agent_id: agent_id.clone(),
            created_at: Utc::now(),
            last_activity: Utc::now(),
            status: PersistentSessionStatus::Creating,
            tasks_completed: 0,
            workspace_path: agent.lock().await.identity.workspace_path.clone(),
        };

        // Store session
        {
            let mut sessions = self.sessions.write().await;
            sessions.insert(agent_id.clone(), Arc::clone(&agent));
        }

        {
            let mut info_map = self.session_info.write().await;
            info_map.insert(agent_id, session_info);
        }

        tracing::info!("Persistent session created successfully");
        Ok(agent)
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:execute_task_batch (lines 286-326) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks for role: {}",
            tasks.len(),
            role.name()
        );

        let session = self.get_or_create_session(role, claude_config).await?;

        // Update session status to active
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, PersistentSessionStatus::Active)
            .await;

        // Execute batch - this amortizes the identity establishment cost
        let results = {
            let mut agent = session.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Update session info
        self.update_session_activity(&agent_id).await;
        self.update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        tracing::info!("Batch execution completed successfully");
        Ok(results)
    }

  ./crates/ccswarm/src/session/persistent_session.rs:196-249 method create_new_session <-> ./crates/ccswarm/src/session/persistent_session.rs:584-620 function test_session_reuse
  Similarity: 89.49%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:create_new_session (lines 196-249) ---[0m
    async fn create_new_session(
        &self,
        agent_id: String,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        tracing::info!("Creating new persistent session for agent: {}", agent_id);

        // Create workspace path
        let workspace_path = self.workspace_root.join(format!("agents/{}", &agent_id));
        tokio::fs::create_dir_all(&workspace_path)
            .await
            .context("Failed to create agent workspace")?;

        // Create agent identity
        let identity = AgentIdentity {
            agent_id: agent_id.clone(),
            specialization: role,
            workspace_path,
            env_vars: Self::create_env_vars(&agent_id),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: std::process::id().to_string(),
            initialized_at: Utc::now(),
        };

        // Create persistent agent
        let agent = PersistentClaudeAgent::new(identity, claude_config).await?;
        let agent = Arc::new(Mutex::new(agent));

        // Create session info
        let session_info = PersistentSessionInfo {
            session_id: Uuid::new_v4().to_string(),
            agent_id: agent_id.clone(),
            created_at: Utc::now(),
            last_activity: Utc::now(),
            status: PersistentSessionStatus::Creating,
            tasks_completed: 0,
            workspace_path: agent.lock().await.identity.workspace_path.clone(),
        };

        // Store session
        {
            let mut sessions = self.sessions.write().await;
            sessions.insert(agent_id.clone(), Arc::clone(&agent));
        }

        {
            let mut info_map = self.session_info.write().await;
            info_map.insert(agent_id, session_info);
        }

        tracing::info!("Persistent session created successfully");
        Ok(agent)
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_session_reuse (lines 584-620) ---[0m
    async fn test_session_reuse() {
        let temp_dir = TempDir::new().unwrap();
        let mut config = test_config();
        config.enable_session_reuse = true;

        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        let claude_config = ClaudeConfig::default();

        // Create first session
        let session1 = manager
            .get_or_create_session(default_frontend_role(), claude_config.clone())
            .await
            .unwrap();

        // Mark as idle - extract agent_id first then update
        let agent_id = {
            let agent = session1.lock().await;
            agent.identity.agent_id.clone()
        };

        manager
            .update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        // Request another session of same type
        let session2 = manager
            .get_or_create_session(default_frontend_role(), claude_config)
            .await
            .unwrap();

        // Should still have only 1 session (reused)
        assert_eq!(manager.sessions.read().await.len(), 1);

        // Sessions should be the same - check by Arc pointer equality
        assert!(Arc::ptr_eq(&session1, &session2));
    }

  ./crates/ccswarm/src/session/persistent_session.rs:196-249 method create_new_session <-> ./crates/ccswarm/src/session/persistent_session.rs:490-525 method get_efficiency_stats
  Similarity: 82.92%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:create_new_session (lines 196-249) ---[0m
    async fn create_new_session(
        &self,
        agent_id: String,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        tracing::info!("Creating new persistent session for agent: {}", agent_id);

        // Create workspace path
        let workspace_path = self.workspace_root.join(format!("agents/{}", &agent_id));
        tokio::fs::create_dir_all(&workspace_path)
            .await
            .context("Failed to create agent workspace")?;

        // Create agent identity
        let identity = AgentIdentity {
            agent_id: agent_id.clone(),
            specialization: role,
            workspace_path,
            env_vars: Self::create_env_vars(&agent_id),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: std::process::id().to_string(),
            initialized_at: Utc::now(),
        };

        // Create persistent agent
        let agent = PersistentClaudeAgent::new(identity, claude_config).await?;
        let agent = Arc::new(Mutex::new(agent));

        // Create session info
        let session_info = PersistentSessionInfo {
            session_id: Uuid::new_v4().to_string(),
            agent_id: agent_id.clone(),
            created_at: Utc::now(),
            last_activity: Utc::now(),
            status: PersistentSessionStatus::Creating,
            tasks_completed: 0,
            workspace_path: agent.lock().await.identity.workspace_path.clone(),
        };

        // Store session
        {
            let mut sessions = self.sessions.write().await;
            sessions.insert(agent_id.clone(), Arc::clone(&agent));
        }

        {
            let mut info_map = self.session_info.write().await;
            info_map.insert(agent_id, session_info);
        }

        tracing::info!("Persistent session created successfully");
        Ok(agent)
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_efficiency_stats (lines 490-525) ---[0m
    pub async fn get_efficiency_stats(&self) -> EfficiencyStats {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        let total_sessions = sessions.len();
        let active_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Active)
            .count();
        let idle_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Idle)
            .count();
        let total_tasks = session_info.values().map(|info| info.tasks_completed).sum();

        // Estimate token savings based on task reuse
        let estimated_token_savings = if total_tasks > total_sessions {
            // Each reused session saves ~3400 tokens
            (total_tasks - total_sessions) * 3400
        } else {
            0
        };

        EfficiencyStats {
            total_sessions,
            active_sessions,
            idle_sessions,
            total_tasks_completed: total_tasks,
            estimated_token_savings,
            session_reuse_rate: if total_tasks > 0 {
                (total_tasks - total_sessions) as f64 / total_tasks as f64
            } else {
                0.0
            },
        }
    }

  ./crates/ccswarm/src/session/persistent_session.rs:196-249 method create_new_session <-> ./crates/ccswarm/src/session/persistent_session.rs:380-419 method cleanup_sessions
  Similarity: 70.49%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:create_new_session (lines 196-249) ---[0m
    async fn create_new_session(
        &self,
        agent_id: String,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        tracing::info!("Creating new persistent session for agent: {}", agent_id);

        // Create workspace path
        let workspace_path = self.workspace_root.join(format!("agents/{}", &agent_id));
        tokio::fs::create_dir_all(&workspace_path)
            .await
            .context("Failed to create agent workspace")?;

        // Create agent identity
        let identity = AgentIdentity {
            agent_id: agent_id.clone(),
            specialization: role,
            workspace_path,
            env_vars: Self::create_env_vars(&agent_id),
            session_id: Uuid::new_v4().to_string(),
            parent_process_id: std::process::id().to_string(),
            initialized_at: Utc::now(),
        };

        // Create persistent agent
        let agent = PersistentClaudeAgent::new(identity, claude_config).await?;
        let agent = Arc::new(Mutex::new(agent));

        // Create session info
        let session_info = PersistentSessionInfo {
            session_id: Uuid::new_v4().to_string(),
            agent_id: agent_id.clone(),
            created_at: Utc::now(),
            last_activity: Utc::now(),
            status: PersistentSessionStatus::Creating,
            tasks_completed: 0,
            workspace_path: agent.lock().await.identity.workspace_path.clone(),
        };

        // Store session
        {
            let mut sessions = self.sessions.write().await;
            sessions.insert(agent_id.clone(), Arc::clone(&agent));
        }

        {
            let mut info_map = self.session_info.write().await;
            info_map.insert(agent_id, session_info);
        }

        tracing::info!("Persistent session created successfully");
        Ok(agent)
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:cleanup_sessions (lines 380-419) ---[0m
    async fn cleanup_sessions(
        sessions: &SessionStorage,
        session_info: &Arc<RwLock<HashMap<String, PersistentSessionInfo>>>,
        idle_timeout: Duration,
        max_lifetime: Duration,
    ) -> Result<()> {
        let now = Utc::now();
        let mut to_remove = Vec::new();

        {
            let info_map = session_info.read().await;
            for (agent_id, info) in info_map.iter() {
                let idle_duration = now.signed_duration_since(info.last_activity);
                let lifetime_duration = now.signed_duration_since(info.created_at);

                let should_cleanup = match info.status {
                    PersistentSessionStatus::Idle => {
                        idle_duration.to_std().unwrap_or(Duration::ZERO) > idle_timeout
                    }
                    PersistentSessionStatus::Terminated => true,
                    _ => lifetime_duration.to_std().unwrap_or(Duration::ZERO) > max_lifetime,
                };

                if should_cleanup {
                    to_remove.push(agent_id.clone());
                }
            }
        }

        // Remove expired sessions
        for agent_id in to_remove {
            if let Err(e) = Self::remove_session(sessions, session_info, &agent_id).await {
                tracing::error!("Failed to remove session {}: {}", agent_id, e);
            } else {
                tracing::info!("Cleaned up expired session: {}", agent_id);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/persistent_session.rs:252-283 method execute_task <-> ./crates/ccswarm/src/session/persistent_session.rs:380-419 method cleanup_sessions
  Similarity: 88.02%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:execute_task (lines 252-283) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let session = self.get_or_create_session(role, claude_config).await?;

        // Update session status to active
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, PersistentSessionStatus::Active)
            .await;

        // Execute task
        let result = {
            let mut agent = session.lock().await;
            agent.execute_task(task).await?
        };

        // Update session info
        self.update_session_activity(&agent_id).await;

        // Mark as idle after task completion
        self.update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:cleanup_sessions (lines 380-419) ---[0m
    async fn cleanup_sessions(
        sessions: &SessionStorage,
        session_info: &Arc<RwLock<HashMap<String, PersistentSessionInfo>>>,
        idle_timeout: Duration,
        max_lifetime: Duration,
    ) -> Result<()> {
        let now = Utc::now();
        let mut to_remove = Vec::new();

        {
            let info_map = session_info.read().await;
            for (agent_id, info) in info_map.iter() {
                let idle_duration = now.signed_duration_since(info.last_activity);
                let lifetime_duration = now.signed_duration_since(info.created_at);

                let should_cleanup = match info.status {
                    PersistentSessionStatus::Idle => {
                        idle_duration.to_std().unwrap_or(Duration::ZERO) > idle_timeout
                    }
                    PersistentSessionStatus::Terminated => true,
                    _ => lifetime_duration.to_std().unwrap_or(Duration::ZERO) > max_lifetime,
                };

                if should_cleanup {
                    to_remove.push(agent_id.clone());
                }
            }
        }

        // Remove expired sessions
        for agent_id in to_remove {
            if let Err(e) = Self::remove_session(sessions, session_info, &agent_id).await {
                tracing::error!("Failed to remove session {}: {}", agent_id, e);
            } else {
                tracing::info!("Cleaned up expired session: {}", agent_id);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/persistent_session.rs:380-419 method cleanup_sessions <-> ./crates/ccswarm/src/session/persistent_session.rs:422-448 method remove_session
  Similarity: 92.57%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:cleanup_sessions (lines 380-419) ---[0m
    async fn cleanup_sessions(
        sessions: &SessionStorage,
        session_info: &Arc<RwLock<HashMap<String, PersistentSessionInfo>>>,
        idle_timeout: Duration,
        max_lifetime: Duration,
    ) -> Result<()> {
        let now = Utc::now();
        let mut to_remove = Vec::new();

        {
            let info_map = session_info.read().await;
            for (agent_id, info) in info_map.iter() {
                let idle_duration = now.signed_duration_since(info.last_activity);
                let lifetime_duration = now.signed_duration_since(info.created_at);

                let should_cleanup = match info.status {
                    PersistentSessionStatus::Idle => {
                        idle_duration.to_std().unwrap_or(Duration::ZERO) > idle_timeout
                    }
                    PersistentSessionStatus::Terminated => true,
                    _ => lifetime_duration.to_std().unwrap_or(Duration::ZERO) > max_lifetime,
                };

                if should_cleanup {
                    to_remove.push(agent_id.clone());
                }
            }
        }

        // Remove expired sessions
        for agent_id in to_remove {
            if let Err(e) = Self::remove_session(sessions, session_info, &agent_id).await {
                tracing::error!("Failed to remove session {}: {}", agent_id, e);
            } else {
                tracing::info!("Cleaned up expired session: {}", agent_id);
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:remove_session (lines 422-448) ---[0m
    async fn remove_session(
        sessions: &SessionStorage,
        session_info: &Arc<RwLock<HashMap<String, PersistentSessionInfo>>>,
        agent_id: &str,
    ) -> Result<()> {
        // Shutdown the agent
        {
            let sessions_guard = sessions.read().await;
            if let Some(session) = sessions_guard.get(agent_id) {
                let mut agent = session.lock().await;
                agent.shutdown().await?;
            }
        }

        // Remove from maps
        {
            let mut sessions_guard = sessions.write().await;
            sessions_guard.remove(agent_id);
        }

        {
            let mut info_guard = session_info.write().await;
            info_guard.remove(agent_id);
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/persistent_session.rs:286-326 method execute_task_batch <-> ./crates/ccswarm/src/session/persistent_session.rs:490-525 method get_efficiency_stats
  Similarity: 80.23%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:execute_task_batch (lines 286-326) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks for role: {}",
            tasks.len(),
            role.name()
        );

        let session = self.get_or_create_session(role, claude_config).await?;

        // Update session status to active
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, PersistentSessionStatus::Active)
            .await;

        // Execute batch - this amortizes the identity establishment cost
        let results = {
            let mut agent = session.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Update session info
        self.update_session_activity(&agent_id).await;
        self.update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        tracing::info!("Batch execution completed successfully");
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_efficiency_stats (lines 490-525) ---[0m
    pub async fn get_efficiency_stats(&self) -> EfficiencyStats {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        let total_sessions = sessions.len();
        let active_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Active)
            .count();
        let idle_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Idle)
            .count();
        let total_tasks = session_info.values().map(|info| info.tasks_completed).sum();

        // Estimate token savings based on task reuse
        let estimated_token_savings = if total_tasks > total_sessions {
            // Each reused session saves ~3400 tokens
            (total_tasks - total_sessions) * 3400
        } else {
            0
        };

        EfficiencyStats {
            total_sessions,
            active_sessions,
            idle_sessions,
            total_tasks_completed: total_tasks,
            estimated_token_savings,
            session_reuse_rate: if total_tasks > 0 {
                (total_tasks - total_sessions) as f64 / total_tasks as f64
            } else {
                0.0
            },
        }
    }

  ./crates/ccswarm/src/session/persistent_session.rs:252-283 method execute_task <-> ./crates/ccswarm/src/session/persistent_session.rs:286-326 method execute_task_batch
  Similarity: 84.47%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:execute_task (lines 252-283) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let session = self.get_or_create_session(role, claude_config).await?;

        // Update session status to active
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, PersistentSessionStatus::Active)
            .await;

        // Execute task
        let result = {
            let mut agent = session.lock().await;
            agent.execute_task(task).await?
        };

        // Update session info
        self.update_session_activity(&agent_id).await;

        // Mark as idle after task completion
        self.update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:execute_task_batch (lines 286-326) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks for role: {}",
            tasks.len(),
            role.name()
        );

        let session = self.get_or_create_session(role, claude_config).await?;

        // Update session status to active
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, PersistentSessionStatus::Active)
            .await;

        // Execute batch - this amortizes the identity establishment cost
        let results = {
            let mut agent = session.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Update session info
        self.update_session_activity(&agent_id).await;
        self.update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        tracing::info!("Batch execution completed successfully");
        Ok(results)
    }

  ./crates/ccswarm/src/session/persistent_session.rs:286-326 method execute_task_batch <-> ./crates/ccswarm/src/session/persistent_session.rs:584-620 function test_session_reuse
  Similarity: 77.28%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:execute_task_batch (lines 286-326) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks for role: {}",
            tasks.len(),
            role.name()
        );

        let session = self.get_or_create_session(role, claude_config).await?;

        // Update session status to active
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, PersistentSessionStatus::Active)
            .await;

        // Execute batch - this amortizes the identity establishment cost
        let results = {
            let mut agent = session.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Update session info
        self.update_session_activity(&agent_id).await;
        self.update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        tracing::info!("Batch execution completed successfully");
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_session_reuse (lines 584-620) ---[0m
    async fn test_session_reuse() {
        let temp_dir = TempDir::new().unwrap();
        let mut config = test_config();
        config.enable_session_reuse = true;

        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        let claude_config = ClaudeConfig::default();

        // Create first session
        let session1 = manager
            .get_or_create_session(default_frontend_role(), claude_config.clone())
            .await
            .unwrap();

        // Mark as idle - extract agent_id first then update
        let agent_id = {
            let agent = session1.lock().await;
            agent.identity.agent_id.clone()
        };

        manager
            .update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        // Request another session of same type
        let session2 = manager
            .get_or_create_session(default_frontend_role(), claude_config)
            .await
            .unwrap();

        // Should still have only 1 session (reused)
        assert_eq!(manager.sessions.read().await.len(), 1);

        // Sessions should be the same - check by Arc pointer equality
        assert!(Arc::ptr_eq(&session1, &session2));
    }

  ./crates/ccswarm/src/session/persistent_session.rs:141-167 method get_or_create_session <-> ./crates/ccswarm/src/session/persistent_session.rs:380-419 method cleanup_sessions
  Similarity: 89.17%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_or_create_session (lines 141-167) ---[0m
    pub async fn get_or_create_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());

        // Check if we can reuse an existing session
        if self.config.enable_session_reuse {
            if let Some(existing) = self.find_reusable_session(&role).await {
                tracing::info!("Reusing existing session for role: {}", role.name());
                return Ok(existing);
            }
        }

        // Check session limits
        let current_count = self.sessions.read().await.len();
        if current_count >= self.config.max_concurrent_sessions {
            return Err(anyhow::anyhow!(
                "Maximum concurrent sessions reached: {}",
                self.config.max_concurrent_sessions
            ));
        }

        // Create new session
        self.create_new_session(agent_id, role, claude_config).await
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:cleanup_sessions (lines 380-419) ---[0m
    async fn cleanup_sessions(
        sessions: &SessionStorage,
        session_info: &Arc<RwLock<HashMap<String, PersistentSessionInfo>>>,
        idle_timeout: Duration,
        max_lifetime: Duration,
    ) -> Result<()> {
        let now = Utc::now();
        let mut to_remove = Vec::new();

        {
            let info_map = session_info.read().await;
            for (agent_id, info) in info_map.iter() {
                let idle_duration = now.signed_duration_since(info.last_activity);
                let lifetime_duration = now.signed_duration_since(info.created_at);

                let should_cleanup = match info.status {
                    PersistentSessionStatus::Idle => {
                        idle_duration.to_std().unwrap_or(Duration::ZERO) > idle_timeout
                    }
                    PersistentSessionStatus::Terminated => true,
                    _ => lifetime_duration.to_std().unwrap_or(Duration::ZERO) > max_lifetime,
                };

                if should_cleanup {
                    to_remove.push(agent_id.clone());
                }
            }
        }

        // Remove expired sessions
        for agent_id in to_remove {
            if let Err(e) = Self::remove_session(sessions, session_info, &agent_id).await {
                tracing::error!("Failed to remove session {}: {}", agent_id, e);
            } else {
                tracing::info!("Cleaned up expired session: {}", agent_id);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/persistent_session.rs:252-283 method execute_task <-> ./crates/ccswarm/src/session/persistent_session.rs:490-525 method get_efficiency_stats
  Similarity: 86.02%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:execute_task (lines 252-283) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let session = self.get_or_create_session(role, claude_config).await?;

        // Update session status to active
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, PersistentSessionStatus::Active)
            .await;

        // Execute task
        let result = {
            let mut agent = session.lock().await;
            agent.execute_task(task).await?
        };

        // Update session info
        self.update_session_activity(&agent_id).await;

        // Mark as idle after task completion
        self.update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_efficiency_stats (lines 490-525) ---[0m
    pub async fn get_efficiency_stats(&self) -> EfficiencyStats {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        let total_sessions = sessions.len();
        let active_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Active)
            .count();
        let idle_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Idle)
            .count();
        let total_tasks = session_info.values().map(|info| info.tasks_completed).sum();

        // Estimate token savings based on task reuse
        let estimated_token_savings = if total_tasks > total_sessions {
            // Each reused session saves ~3400 tokens
            (total_tasks - total_sessions) * 3400
        } else {
            0
        };

        EfficiencyStats {
            total_sessions,
            active_sessions,
            idle_sessions,
            total_tasks_completed: total_tasks,
            estimated_token_savings,
            session_reuse_rate: if total_tasks > 0 {
                (total_tasks - total_sessions) as f64 / total_tasks as f64
            } else {
                0.0
            },
        }
    }

  ./crates/ccswarm/src/session/persistent_session.rs:170-193 method find_reusable_session <-> ./crates/ccswarm/src/session/persistent_session.rs:380-419 method cleanup_sessions
  Similarity: 89.93%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:find_reusable_session (lines 170-193) ---[0m
    async fn find_reusable_session(
        &self,
        role: &AgentRole,
    ) -> Option<Arc<Mutex<PersistentClaudeAgent>>> {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        for (agent_id, session) in sessions.iter() {
            if let Some(info) = session_info.get(agent_id) {
                if info.status == PersistentSessionStatus::Idle {
                    // Try to lock the agent, but don't block indefinitely
                    if let Ok(agent) = session.try_lock() {
                        if agent.identity.specialization.name() == role.name() {
                            // Found a reusable session
                            return Some(Arc::clone(session));
                        }
                    }
                    // If we can't lock, skip this session for now
                }
            }
        }

        None
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:cleanup_sessions (lines 380-419) ---[0m
    async fn cleanup_sessions(
        sessions: &SessionStorage,
        session_info: &Arc<RwLock<HashMap<String, PersistentSessionInfo>>>,
        idle_timeout: Duration,
        max_lifetime: Duration,
    ) -> Result<()> {
        let now = Utc::now();
        let mut to_remove = Vec::new();

        {
            let info_map = session_info.read().await;
            for (agent_id, info) in info_map.iter() {
                let idle_duration = now.signed_duration_since(info.last_activity);
                let lifetime_duration = now.signed_duration_since(info.created_at);

                let should_cleanup = match info.status {
                    PersistentSessionStatus::Idle => {
                        idle_duration.to_std().unwrap_or(Duration::ZERO) > idle_timeout
                    }
                    PersistentSessionStatus::Terminated => true,
                    _ => lifetime_duration.to_std().unwrap_or(Duration::ZERO) > max_lifetime,
                };

                if should_cleanup {
                    to_remove.push(agent_id.clone());
                }
            }
        }

        // Remove expired sessions
        for agent_id in to_remove {
            if let Err(e) = Self::remove_session(sessions, session_info, &agent_id).await {
                tracing::error!("Failed to remove session {}: {}", agent_id, e);
            } else {
                tracing::info!("Cleaned up expired session: {}", agent_id);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/persistent_session.rs:380-419 method cleanup_sessions <-> ./crates/ccswarm/src/session/persistent_session.rs:490-525 method get_efficiency_stats
  Similarity: 75.31%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:cleanup_sessions (lines 380-419) ---[0m
    async fn cleanup_sessions(
        sessions: &SessionStorage,
        session_info: &Arc<RwLock<HashMap<String, PersistentSessionInfo>>>,
        idle_timeout: Duration,
        max_lifetime: Duration,
    ) -> Result<()> {
        let now = Utc::now();
        let mut to_remove = Vec::new();

        {
            let info_map = session_info.read().await;
            for (agent_id, info) in info_map.iter() {
                let idle_duration = now.signed_duration_since(info.last_activity);
                let lifetime_duration = now.signed_duration_since(info.created_at);

                let should_cleanup = match info.status {
                    PersistentSessionStatus::Idle => {
                        idle_duration.to_std().unwrap_or(Duration::ZERO) > idle_timeout
                    }
                    PersistentSessionStatus::Terminated => true,
                    _ => lifetime_duration.to_std().unwrap_or(Duration::ZERO) > max_lifetime,
                };

                if should_cleanup {
                    to_remove.push(agent_id.clone());
                }
            }
        }

        // Remove expired sessions
        for agent_id in to_remove {
            if let Err(e) = Self::remove_session(sessions, session_info, &agent_id).await {
                tracing::error!("Failed to remove session {}: {}", agent_id, e);
            } else {
                tracing::info!("Cleaned up expired session: {}", agent_id);
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_efficiency_stats (lines 490-525) ---[0m
    pub async fn get_efficiency_stats(&self) -> EfficiencyStats {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        let total_sessions = sessions.len();
        let active_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Active)
            .count();
        let idle_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Idle)
            .count();
        let total_tasks = session_info.values().map(|info| info.tasks_completed).sum();

        // Estimate token savings based on task reuse
        let estimated_token_savings = if total_tasks > total_sessions {
            // Each reused session saves ~3400 tokens
            (total_tasks - total_sessions) * 3400
        } else {
            0
        };

        EfficiencyStats {
            total_sessions,
            active_sessions,
            idle_sessions,
            total_tasks_completed: total_tasks,
            estimated_token_savings,
            session_reuse_rate: if total_tasks > 0 {
                (total_tasks - total_sessions) as f64 / total_tasks as f64
            } else {
                0.0
            },
        }
    }

  ./crates/ccswarm/src/session/persistent_session.rs:252-283 method execute_task <-> ./crates/ccswarm/src/session/persistent_session.rs:584-620 function test_session_reuse
  Similarity: 82.75%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:execute_task (lines 252-283) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let session = self.get_or_create_session(role, claude_config).await?;

        // Update session status to active
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, PersistentSessionStatus::Active)
            .await;

        // Execute task
        let result = {
            let mut agent = session.lock().await;
            agent.execute_task(task).await?
        };

        // Update session info
        self.update_session_activity(&agent_id).await;

        // Mark as idle after task completion
        self.update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_session_reuse (lines 584-620) ---[0m
    async fn test_session_reuse() {
        let temp_dir = TempDir::new().unwrap();
        let mut config = test_config();
        config.enable_session_reuse = true;

        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        let claude_config = ClaudeConfig::default();

        // Create first session
        let session1 = manager
            .get_or_create_session(default_frontend_role(), claude_config.clone())
            .await
            .unwrap();

        // Mark as idle - extract agent_id first then update
        let agent_id = {
            let agent = session1.lock().await;
            agent.identity.agent_id.clone()
        };

        manager
            .update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        // Request another session of same type
        let session2 = manager
            .get_or_create_session(default_frontend_role(), claude_config)
            .await
            .unwrap();

        // Should still have only 1 session (reused)
        assert_eq!(manager.sessions.read().await.len(), 1);

        // Sessions should be the same - check by Arc pointer equality
        assert!(Arc::ptr_eq(&session1, &session2));
    }

  ./crates/ccswarm/src/session/persistent_session.rs:380-419 method cleanup_sessions <-> ./crates/ccswarm/src/session/persistent_session.rs:451-475 method shutdown
  Similarity: 87.46%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:cleanup_sessions (lines 380-419) ---[0m
    async fn cleanup_sessions(
        sessions: &SessionStorage,
        session_info: &Arc<RwLock<HashMap<String, PersistentSessionInfo>>>,
        idle_timeout: Duration,
        max_lifetime: Duration,
    ) -> Result<()> {
        let now = Utc::now();
        let mut to_remove = Vec::new();

        {
            let info_map = session_info.read().await;
            for (agent_id, info) in info_map.iter() {
                let idle_duration = now.signed_duration_since(info.last_activity);
                let lifetime_duration = now.signed_duration_since(info.created_at);

                let should_cleanup = match info.status {
                    PersistentSessionStatus::Idle => {
                        idle_duration.to_std().unwrap_or(Duration::ZERO) > idle_timeout
                    }
                    PersistentSessionStatus::Terminated => true,
                    _ => lifetime_duration.to_std().unwrap_or(Duration::ZERO) > max_lifetime,
                };

                if should_cleanup {
                    to_remove.push(agent_id.clone());
                }
            }
        }

        // Remove expired sessions
        for agent_id in to_remove {
            if let Err(e) = Self::remove_session(sessions, session_info, &agent_id).await {
                tracing::error!("Failed to remove session {}: {}", agent_id, e);
            } else {
                tracing::info!("Cleaned up expired session: {}", agent_id);
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:shutdown (lines 451-475) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down persistent session manager");

        // Cancel cleanup task
        if let Some(handle) = self.cleanup_handle.take() {
            handle.abort();
        }

        // Shutdown all sessions
        let agent_ids: Vec<String> = {
            let session_info = self.session_info.read().await;
            session_info.keys().cloned().collect()
        };

        for agent_id in agent_ids {
            if let Err(e) =
                Self::remove_session(&self.sessions, &self.session_info, &agent_id).await
            {
                tracing::error!("Failed to shutdown session {}: {}", agent_id, e);
            }
        }

        tracing::info!("Persistent session manager shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/persistent_session.rs:490-525 method get_efficiency_stats <-> ./crates/ccswarm/src/session/persistent_session.rs:584-620 function test_session_reuse
  Similarity: 75.62%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_efficiency_stats (lines 490-525) ---[0m
    pub async fn get_efficiency_stats(&self) -> EfficiencyStats {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        let total_sessions = sessions.len();
        let active_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Active)
            .count();
        let idle_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Idle)
            .count();
        let total_tasks = session_info.values().map(|info| info.tasks_completed).sum();

        // Estimate token savings based on task reuse
        let estimated_token_savings = if total_tasks > total_sessions {
            // Each reused session saves ~3400 tokens
            (total_tasks - total_sessions) * 3400
        } else {
            0
        };

        EfficiencyStats {
            total_sessions,
            active_sessions,
            idle_sessions,
            total_tasks_completed: total_tasks,
            estimated_token_savings,
            session_reuse_rate: if total_tasks > 0 {
                (total_tasks - total_sessions) as f64 / total_tasks as f64
            } else {
                0.0
            },
        }
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_session_reuse (lines 584-620) ---[0m
    async fn test_session_reuse() {
        let temp_dir = TempDir::new().unwrap();
        let mut config = test_config();
        config.enable_session_reuse = true;

        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        let claude_config = ClaudeConfig::default();

        // Create first session
        let session1 = manager
            .get_or_create_session(default_frontend_role(), claude_config.clone())
            .await
            .unwrap();

        // Mark as idle - extract agent_id first then update
        let agent_id = {
            let agent = session1.lock().await;
            agent.identity.agent_id.clone()
        };

        manager
            .update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        // Request another session of same type
        let session2 = manager
            .get_or_create_session(default_frontend_role(), claude_config)
            .await
            .unwrap();

        // Should still have only 1 session (reused)
        assert_eq!(manager.sessions.read().await.len(), 1);

        // Sessions should be the same - check by Arc pointer equality
        assert!(Arc::ptr_eq(&session1, &session2));
    }

  ./crates/ccswarm/src/session/persistent_session.rs:112-138 method start <-> ./crates/ccswarm/src/session/persistent_session.rs:490-525 method get_efficiency_stats
  Similarity: 85.17%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:start (lines 112-138) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting persistent session manager");

        let sessions = Arc::clone(&self.sessions);
        let session_info = Arc::clone(&self.session_info);
        let cleanup_interval = self.config.cleanup_interval;
        let idle_timeout = self.config.idle_timeout;
        let max_lifetime = self.config.max_session_lifetime;

        let cleanup_handle = tokio::spawn(async move {
            let mut interval = interval(cleanup_interval);

            loop {
                interval.tick().await;

                if let Err(e) =
                    Self::cleanup_sessions(&sessions, &session_info, idle_timeout, max_lifetime)
                        .await
                {
                    tracing::error!("Session cleanup error: {}", e);
                }
            }
        });

        self.cleanup_handle = Some(cleanup_handle);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_efficiency_stats (lines 490-525) ---[0m
    pub async fn get_efficiency_stats(&self) -> EfficiencyStats {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        let total_sessions = sessions.len();
        let active_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Active)
            .count();
        let idle_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Idle)
            .count();
        let total_tasks = session_info.values().map(|info| info.tasks_completed).sum();

        // Estimate token savings based on task reuse
        let estimated_token_savings = if total_tasks > total_sessions {
            // Each reused session saves ~3400 tokens
            (total_tasks - total_sessions) * 3400
        } else {
            0
        };

        EfficiencyStats {
            total_sessions,
            active_sessions,
            idle_sessions,
            total_tasks_completed: total_tasks,
            estimated_token_savings,
            session_reuse_rate: if total_tasks > 0 {
                (total_tasks - total_sessions) as f64 / total_tasks as f64
            } else {
                0.0
            },
        }
    }

  ./crates/ccswarm/src/session/persistent_session.rs:141-167 method get_or_create_session <-> ./crates/ccswarm/src/session/persistent_session.rs:490-525 method get_efficiency_stats
  Similarity: 85.13%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_or_create_session (lines 141-167) ---[0m
    pub async fn get_or_create_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());

        // Check if we can reuse an existing session
        if self.config.enable_session_reuse {
            if let Some(existing) = self.find_reusable_session(&role).await {
                tracing::info!("Reusing existing session for role: {}", role.name());
                return Ok(existing);
            }
        }

        // Check session limits
        let current_count = self.sessions.read().await.len();
        if current_count >= self.config.max_concurrent_sessions {
            return Err(anyhow::anyhow!(
                "Maximum concurrent sessions reached: {}",
                self.config.max_concurrent_sessions
            ));
        }

        // Create new session
        self.create_new_session(agent_id, role, claude_config).await
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_efficiency_stats (lines 490-525) ---[0m
    pub async fn get_efficiency_stats(&self) -> EfficiencyStats {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        let total_sessions = sessions.len();
        let active_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Active)
            .count();
        let idle_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Idle)
            .count();
        let total_tasks = session_info.values().map(|info| info.tasks_completed).sum();

        // Estimate token savings based on task reuse
        let estimated_token_savings = if total_tasks > total_sessions {
            // Each reused session saves ~3400 tokens
            (total_tasks - total_sessions) * 3400
        } else {
            0
        };

        EfficiencyStats {
            total_sessions,
            active_sessions,
            idle_sessions,
            total_tasks_completed: total_tasks,
            estimated_token_savings,
            session_reuse_rate: if total_tasks > 0 {
                (total_tasks - total_sessions) as f64 / total_tasks as f64
            } else {
                0.0
            },
        }
    }

  ./crates/ccswarm/src/session/persistent_session.rs:112-138 method start <-> ./crates/ccswarm/src/session/persistent_session.rs:286-326 method execute_task_batch
  Similarity: 77.87%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:start (lines 112-138) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting persistent session manager");

        let sessions = Arc::clone(&self.sessions);
        let session_info = Arc::clone(&self.session_info);
        let cleanup_interval = self.config.cleanup_interval;
        let idle_timeout = self.config.idle_timeout;
        let max_lifetime = self.config.max_session_lifetime;

        let cleanup_handle = tokio::spawn(async move {
            let mut interval = interval(cleanup_interval);

            loop {
                interval.tick().await;

                if let Err(e) =
                    Self::cleanup_sessions(&sessions, &session_info, idle_timeout, max_lifetime)
                        .await
                {
                    tracing::error!("Session cleanup error: {}", e);
                }
            }
        });

        self.cleanup_handle = Some(cleanup_handle);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:execute_task_batch (lines 286-326) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks for role: {}",
            tasks.len(),
            role.name()
        );

        let session = self.get_or_create_session(role, claude_config).await?;

        // Update session status to active
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, PersistentSessionStatus::Active)
            .await;

        // Execute batch - this amortizes the identity establishment cost
        let results = {
            let mut agent = session.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Update session info
        self.update_session_activity(&agent_id).await;
        self.update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        tracing::info!("Batch execution completed successfully");
        Ok(results)
    }

  ./crates/ccswarm/src/session/persistent_session.rs:112-138 method start <-> ./crates/ccswarm/src/session/persistent_session.rs:380-419 method cleanup_sessions
  Similarity: 78.88%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:start (lines 112-138) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting persistent session manager");

        let sessions = Arc::clone(&self.sessions);
        let session_info = Arc::clone(&self.session_info);
        let cleanup_interval = self.config.cleanup_interval;
        let idle_timeout = self.config.idle_timeout;
        let max_lifetime = self.config.max_session_lifetime;

        let cleanup_handle = tokio::spawn(async move {
            let mut interval = interval(cleanup_interval);

            loop {
                interval.tick().await;

                if let Err(e) =
                    Self::cleanup_sessions(&sessions, &session_info, idle_timeout, max_lifetime)
                        .await
                {
                    tracing::error!("Session cleanup error: {}", e);
                }
            }
        });

        self.cleanup_handle = Some(cleanup_handle);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:cleanup_sessions (lines 380-419) ---[0m
    async fn cleanup_sessions(
        sessions: &SessionStorage,
        session_info: &Arc<RwLock<HashMap<String, PersistentSessionInfo>>>,
        idle_timeout: Duration,
        max_lifetime: Duration,
    ) -> Result<()> {
        let now = Utc::now();
        let mut to_remove = Vec::new();

        {
            let info_map = session_info.read().await;
            for (agent_id, info) in info_map.iter() {
                let idle_duration = now.signed_duration_since(info.last_activity);
                let lifetime_duration = now.signed_duration_since(info.created_at);

                let should_cleanup = match info.status {
                    PersistentSessionStatus::Idle => {
                        idle_duration.to_std().unwrap_or(Duration::ZERO) > idle_timeout
                    }
                    PersistentSessionStatus::Terminated => true,
                    _ => lifetime_duration.to_std().unwrap_or(Duration::ZERO) > max_lifetime,
                };

                if should_cleanup {
                    to_remove.push(agent_id.clone());
                }
            }
        }

        // Remove expired sessions
        for agent_id in to_remove {
            if let Err(e) = Self::remove_session(sessions, session_info, &agent_id).await {
                tracing::error!("Failed to remove session {}: {}", agent_id, e);
            } else {
                tracing::info!("Cleaned up expired session: {}", agent_id);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/persistent_session.rs:141-167 method get_or_create_session <-> ./crates/ccswarm/src/session/persistent_session.rs:252-283 method execute_task
  Similarity: 85.25%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_or_create_session (lines 141-167) ---[0m
    pub async fn get_or_create_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());

        // Check if we can reuse an existing session
        if self.config.enable_session_reuse {
            if let Some(existing) = self.find_reusable_session(&role).await {
                tracing::info!("Reusing existing session for role: {}", role.name());
                return Ok(existing);
            }
        }

        // Check session limits
        let current_count = self.sessions.read().await.len();
        if current_count >= self.config.max_concurrent_sessions {
            return Err(anyhow::anyhow!(
                "Maximum concurrent sessions reached: {}",
                self.config.max_concurrent_sessions
            ));
        }

        // Create new session
        self.create_new_session(agent_id, role, claude_config).await
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:execute_task (lines 252-283) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let session = self.get_or_create_session(role, claude_config).await?;

        // Update session status to active
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, PersistentSessionStatus::Active)
            .await;

        // Execute task
        let result = {
            let mut agent = session.lock().await;
            agent.execute_task(task).await?
        };

        // Update session info
        self.update_session_activity(&agent_id).await;

        // Mark as idle after task completion
        self.update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        Ok(result)
    }

  ./crates/ccswarm/src/session/persistent_session.rs:363-377 method get_sessions_by_role <-> ./crates/ccswarm/src/session/persistent_session.rs:380-419 method cleanup_sessions
  Similarity: 90.03%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_sessions_by_role (lines 363-377) ---[0m
    pub async fn get_sessions_by_role(&self, role: &AgentRole) -> Vec<PersistentSessionInfo> {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        let mut result = Vec::new();
        for (agent_id, session) in sessions.iter() {
            if let Some(info) = session_info.get(agent_id) {
                let agent = session.lock().await;
                if agent.identity.specialization.name() == role.name() {
                    result.push(info.clone());
                }
            }
        }
        result
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:cleanup_sessions (lines 380-419) ---[0m
    async fn cleanup_sessions(
        sessions: &SessionStorage,
        session_info: &Arc<RwLock<HashMap<String, PersistentSessionInfo>>>,
        idle_timeout: Duration,
        max_lifetime: Duration,
    ) -> Result<()> {
        let now = Utc::now();
        let mut to_remove = Vec::new();

        {
            let info_map = session_info.read().await;
            for (agent_id, info) in info_map.iter() {
                let idle_duration = now.signed_duration_since(info.last_activity);
                let lifetime_duration = now.signed_duration_since(info.created_at);

                let should_cleanup = match info.status {
                    PersistentSessionStatus::Idle => {
                        idle_duration.to_std().unwrap_or(Duration::ZERO) > idle_timeout
                    }
                    PersistentSessionStatus::Terminated => true,
                    _ => lifetime_duration.to_std().unwrap_or(Duration::ZERO) > max_lifetime,
                };

                if should_cleanup {
                    to_remove.push(agent_id.clone());
                }
            }
        }

        // Remove expired sessions
        for agent_id in to_remove {
            if let Err(e) = Self::remove_session(sessions, session_info, &agent_id).await {
                tracing::error!("Failed to remove session {}: {}", agent_id, e);
            } else {
                tracing::info!("Cleaned up expired session: {}", agent_id);
            }
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/persistent_session.rs:451-475 method shutdown <-> ./crates/ccswarm/src/session/persistent_session.rs:490-525 method get_efficiency_stats
  Similarity: 80.27%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:shutdown (lines 451-475) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down persistent session manager");

        // Cancel cleanup task
        if let Some(handle) = self.cleanup_handle.take() {
            handle.abort();
        }

        // Shutdown all sessions
        let agent_ids: Vec<String> = {
            let session_info = self.session_info.read().await;
            session_info.keys().cloned().collect()
        };

        for agent_id in agent_ids {
            if let Err(e) =
                Self::remove_session(&self.sessions, &self.session_info, &agent_id).await
            {
                tracing::error!("Failed to shutdown session {}: {}", agent_id, e);
            }
        }

        tracing::info!("Persistent session manager shutdown complete");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_efficiency_stats (lines 490-525) ---[0m
    pub async fn get_efficiency_stats(&self) -> EfficiencyStats {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        let total_sessions = sessions.len();
        let active_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Active)
            .count();
        let idle_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Idle)
            .count();
        let total_tasks = session_info.values().map(|info| info.tasks_completed).sum();

        // Estimate token savings based on task reuse
        let estimated_token_savings = if total_tasks > total_sessions {
            // Each reused session saves ~3400 tokens
            (total_tasks - total_sessions) * 3400
        } else {
            0
        };

        EfficiencyStats {
            total_sessions,
            active_sessions,
            idle_sessions,
            total_tasks_completed: total_tasks,
            estimated_token_savings,
            session_reuse_rate: if total_tasks > 0 {
                (total_tasks - total_sessions) as f64 / total_tasks as f64
            } else {
                0.0
            },
        }
    }

  ./crates/ccswarm/src/session/persistent_session.rs:112-138 method start <-> ./crates/ccswarm/src/session/persistent_session.rs:252-283 method execute_task
  Similarity: 82.14%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:start (lines 112-138) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting persistent session manager");

        let sessions = Arc::clone(&self.sessions);
        let session_info = Arc::clone(&self.session_info);
        let cleanup_interval = self.config.cleanup_interval;
        let idle_timeout = self.config.idle_timeout;
        let max_lifetime = self.config.max_session_lifetime;

        let cleanup_handle = tokio::spawn(async move {
            let mut interval = interval(cleanup_interval);

            loop {
                interval.tick().await;

                if let Err(e) =
                    Self::cleanup_sessions(&sessions, &session_info, idle_timeout, max_lifetime)
                        .await
                {
                    tracing::error!("Session cleanup error: {}", e);
                }
            }
        });

        self.cleanup_handle = Some(cleanup_handle);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:execute_task (lines 252-283) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let session = self.get_or_create_session(role, claude_config).await?;

        // Update session status to active
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, PersistentSessionStatus::Active)
            .await;

        // Execute task
        let result = {
            let mut agent = session.lock().await;
            agent.execute_task(task).await?
        };

        // Update session info
        self.update_session_activity(&agent_id).await;

        // Mark as idle after task completion
        self.update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        Ok(result)
    }

  ./crates/ccswarm/src/session/persistent_session.rs:252-283 method execute_task <-> ./crates/ccswarm/src/session/persistent_session.rs:451-475 method shutdown
  Similarity: 83.77%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:execute_task (lines 252-283) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let session = self.get_or_create_session(role, claude_config).await?;

        // Update session status to active
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, PersistentSessionStatus::Active)
            .await;

        // Execute task
        let result = {
            let mut agent = session.lock().await;
            agent.execute_task(task).await?
        };

        // Update session info
        self.update_session_activity(&agent_id).await;

        // Mark as idle after task completion
        self.update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:shutdown (lines 451-475) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down persistent session manager");

        // Cancel cleanup task
        if let Some(handle) = self.cleanup_handle.take() {
            handle.abort();
        }

        // Shutdown all sessions
        let agent_ids: Vec<String> = {
            let session_info = self.session_info.read().await;
            session_info.keys().cloned().collect()
        };

        for agent_id in agent_ids {
            if let Err(e) =
                Self::remove_session(&self.sessions, &self.session_info, &agent_id).await
            {
                tracing::error!("Failed to shutdown session {}: {}", agent_id, e);
            }
        }

        tracing::info!("Persistent session manager shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/persistent_session.rs:112-138 method start <-> ./crates/ccswarm/src/session/persistent_session.rs:584-620 function test_session_reuse
  Similarity: 73.17%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:start (lines 112-138) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting persistent session manager");

        let sessions = Arc::clone(&self.sessions);
        let session_info = Arc::clone(&self.session_info);
        let cleanup_interval = self.config.cleanup_interval;
        let idle_timeout = self.config.idle_timeout;
        let max_lifetime = self.config.max_session_lifetime;

        let cleanup_handle = tokio::spawn(async move {
            let mut interval = interval(cleanup_interval);

            loop {
                interval.tick().await;

                if let Err(e) =
                    Self::cleanup_sessions(&sessions, &session_info, idle_timeout, max_lifetime)
                        .await
                {
                    tracing::error!("Session cleanup error: {}", e);
                }
            }
        });

        self.cleanup_handle = Some(cleanup_handle);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_session_reuse (lines 584-620) ---[0m
    async fn test_session_reuse() {
        let temp_dir = TempDir::new().unwrap();
        let mut config = test_config();
        config.enable_session_reuse = true;

        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        let claude_config = ClaudeConfig::default();

        // Create first session
        let session1 = manager
            .get_or_create_session(default_frontend_role(), claude_config.clone())
            .await
            .unwrap();

        // Mark as idle - extract agent_id first then update
        let agent_id = {
            let agent = session1.lock().await;
            agent.identity.agent_id.clone()
        };

        manager
            .update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        // Request another session of same type
        let session2 = manager
            .get_or_create_session(default_frontend_role(), claude_config)
            .await
            .unwrap();

        // Should still have only 1 session (reused)
        assert_eq!(manager.sessions.read().await.len(), 1);

        // Sessions should be the same - check by Arc pointer equality
        assert!(Arc::ptr_eq(&session1, &session2));
    }

  ./crates/ccswarm/src/session/persistent_session.rs:141-167 method get_or_create_session <-> ./crates/ccswarm/src/session/persistent_session.rs:422-448 method remove_session
  Similarity: 86.30%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_or_create_session (lines 141-167) ---[0m
    pub async fn get_or_create_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());

        // Check if we can reuse an existing session
        if self.config.enable_session_reuse {
            if let Some(existing) = self.find_reusable_session(&role).await {
                tracing::info!("Reusing existing session for role: {}", role.name());
                return Ok(existing);
            }
        }

        // Check session limits
        let current_count = self.sessions.read().await.len();
        if current_count >= self.config.max_concurrent_sessions {
            return Err(anyhow::anyhow!(
                "Maximum concurrent sessions reached: {}",
                self.config.max_concurrent_sessions
            ));
        }

        // Create new session
        self.create_new_session(agent_id, role, claude_config).await
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:remove_session (lines 422-448) ---[0m
    async fn remove_session(
        sessions: &SessionStorage,
        session_info: &Arc<RwLock<HashMap<String, PersistentSessionInfo>>>,
        agent_id: &str,
    ) -> Result<()> {
        // Shutdown the agent
        {
            let sessions_guard = sessions.read().await;
            if let Some(session) = sessions_guard.get(agent_id) {
                let mut agent = session.lock().await;
                agent.shutdown().await?;
            }
        }

        // Remove from maps
        {
            let mut sessions_guard = sessions.write().await;
            sessions_guard.remove(agent_id);
        }

        {
            let mut info_guard = session_info.write().await;
            info_guard.remove(agent_id);
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/persistent_session.rs:286-326 method execute_task_batch <-> ./crates/ccswarm/src/session/persistent_session.rs:451-475 method shutdown
  Similarity: 70.16%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:execute_task_batch (lines 286-326) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks for role: {}",
            tasks.len(),
            role.name()
        );

        let session = self.get_or_create_session(role, claude_config).await?;

        // Update session status to active
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, PersistentSessionStatus::Active)
            .await;

        // Execute batch - this amortizes the identity establishment cost
        let results = {
            let mut agent = session.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Update session info
        self.update_session_activity(&agent_id).await;
        self.update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        tracing::info!("Batch execution completed successfully");
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:shutdown (lines 451-475) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down persistent session manager");

        // Cancel cleanup task
        if let Some(handle) = self.cleanup_handle.take() {
            handle.abort();
        }

        // Shutdown all sessions
        let agent_ids: Vec<String> = {
            let session_info = self.session_info.read().await;
            session_info.keys().cloned().collect()
        };

        for agent_id in agent_ids {
            if let Err(e) =
                Self::remove_session(&self.sessions, &self.session_info, &agent_id).await
            {
                tracing::error!("Failed to shutdown session {}: {}", agent_id, e);
            }
        }

        tracing::info!("Persistent session manager shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/persistent_session.rs:380-419 method cleanup_sessions <-> ./crates/ccswarm/src/session/persistent_session.rs:566-581 function test_persistent_session_creation
  Similarity: 81.68%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:cleanup_sessions (lines 380-419) ---[0m
    async fn cleanup_sessions(
        sessions: &SessionStorage,
        session_info: &Arc<RwLock<HashMap<String, PersistentSessionInfo>>>,
        idle_timeout: Duration,
        max_lifetime: Duration,
    ) -> Result<()> {
        let now = Utc::now();
        let mut to_remove = Vec::new();

        {
            let info_map = session_info.read().await;
            for (agent_id, info) in info_map.iter() {
                let idle_duration = now.signed_duration_since(info.last_activity);
                let lifetime_duration = now.signed_duration_since(info.created_at);

                let should_cleanup = match info.status {
                    PersistentSessionStatus::Idle => {
                        idle_duration.to_std().unwrap_or(Duration::ZERO) > idle_timeout
                    }
                    PersistentSessionStatus::Terminated => true,
                    _ => lifetime_duration.to_std().unwrap_or(Duration::ZERO) > max_lifetime,
                };

                if should_cleanup {
                    to_remove.push(agent_id.clone());
                }
            }
        }

        // Remove expired sessions
        for agent_id in to_remove {
            if let Err(e) = Self::remove_session(sessions, session_info, &agent_id).await {
                tracing::error!("Failed to remove session {}: {}", agent_id, e);
            } else {
                tracing::info!("Cleaned up expired session: {}", agent_id);
            }
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_persistent_session_creation (lines 566-581) ---[0m
    async fn test_persistent_session_creation() {
        let temp_dir = TempDir::new().unwrap();
        let config = test_config();
        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        let claude_config = ClaudeConfig::default();
        let session = manager
            .get_or_create_session(default_frontend_role(), claude_config)
            .await
            .unwrap();

        assert_eq!(manager.sessions.read().await.len(), 1);

        let agent = session.lock().await;
        assert!(agent.identity.agent_id.contains("frontend"));
    }

  ./crates/ccswarm/src/session/persistent_session.rs:490-525 method get_efficiency_stats <-> ./crates/ccswarm/src/session/persistent_session.rs:566-581 function test_persistent_session_creation
  Similarity: 87.51%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_efficiency_stats (lines 490-525) ---[0m
    pub async fn get_efficiency_stats(&self) -> EfficiencyStats {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        let total_sessions = sessions.len();
        let active_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Active)
            .count();
        let idle_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Idle)
            .count();
        let total_tasks = session_info.values().map(|info| info.tasks_completed).sum();

        // Estimate token savings based on task reuse
        let estimated_token_savings = if total_tasks > total_sessions {
            // Each reused session saves ~3400 tokens
            (total_tasks - total_sessions) * 3400
        } else {
            0
        };

        EfficiencyStats {
            total_sessions,
            active_sessions,
            idle_sessions,
            total_tasks_completed: total_tasks,
            estimated_token_savings,
            session_reuse_rate: if total_tasks > 0 {
                (total_tasks - total_sessions) as f64 / total_tasks as f64
            } else {
                0.0
            },
        }
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_persistent_session_creation (lines 566-581) ---[0m
    async fn test_persistent_session_creation() {
        let temp_dir = TempDir::new().unwrap();
        let config = test_config();
        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        let claude_config = ClaudeConfig::default();
        let session = manager
            .get_or_create_session(default_frontend_role(), claude_config)
            .await
            .unwrap();

        assert_eq!(manager.sessions.read().await.len(), 1);

        let agent = session.lock().await;
        assert!(agent.identity.agent_id.contains("frontend"));
    }

  ./crates/ccswarm/src/session/persistent_session.rs:141-167 method get_or_create_session <-> ./crates/ccswarm/src/session/persistent_session.rs:451-475 method shutdown
  Similarity: 85.25%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_or_create_session (lines 141-167) ---[0m
    pub async fn get_or_create_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());

        // Check if we can reuse an existing session
        if self.config.enable_session_reuse {
            if let Some(existing) = self.find_reusable_session(&role).await {
                tracing::info!("Reusing existing session for role: {}", role.name());
                return Ok(existing);
            }
        }

        // Check session limits
        let current_count = self.sessions.read().await.len();
        if current_count >= self.config.max_concurrent_sessions {
            return Err(anyhow::anyhow!(
                "Maximum concurrent sessions reached: {}",
                self.config.max_concurrent_sessions
            ));
        }

        // Create new session
        self.create_new_session(agent_id, role, claude_config).await
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:shutdown (lines 451-475) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down persistent session manager");

        // Cancel cleanup task
        if let Some(handle) = self.cleanup_handle.take() {
            handle.abort();
        }

        // Shutdown all sessions
        let agent_ids: Vec<String> = {
            let session_info = self.session_info.read().await;
            session_info.keys().cloned().collect()
        };

        for agent_id in agent_ids {
            if let Err(e) =
                Self::remove_session(&self.sessions, &self.session_info, &agent_id).await
            {
                tracing::error!("Failed to shutdown session {}: {}", agent_id, e);
            }
        }

        tracing::info!("Persistent session manager shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/persistent_session.rs:422-448 method remove_session <-> ./crates/ccswarm/src/session/persistent_session.rs:451-475 method shutdown
  Similarity: 83.43%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:remove_session (lines 422-448) ---[0m
    async fn remove_session(
        sessions: &SessionStorage,
        session_info: &Arc<RwLock<HashMap<String, PersistentSessionInfo>>>,
        agent_id: &str,
    ) -> Result<()> {
        // Shutdown the agent
        {
            let sessions_guard = sessions.read().await;
            if let Some(session) = sessions_guard.get(agent_id) {
                let mut agent = session.lock().await;
                agent.shutdown().await?;
            }
        }

        // Remove from maps
        {
            let mut sessions_guard = sessions.write().await;
            sessions_guard.remove(agent_id);
        }

        {
            let mut info_guard = session_info.write().await;
            info_guard.remove(agent_id);
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:shutdown (lines 451-475) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down persistent session manager");

        // Cancel cleanup task
        if let Some(handle) = self.cleanup_handle.take() {
            handle.abort();
        }

        // Shutdown all sessions
        let agent_ids: Vec<String> = {
            let session_info = self.session_info.read().await;
            session_info.keys().cloned().collect()
        };

        for agent_id in agent_ids {
            if let Err(e) =
                Self::remove_session(&self.sessions, &self.session_info, &agent_id).await
            {
                tracing::error!("Failed to shutdown session {}: {}", agent_id, e);
            }
        }

        tracing::info!("Persistent session manager shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/persistent_session.rs:252-283 method execute_task <-> ./crates/ccswarm/src/session/persistent_session.rs:422-448 method remove_session
  Similarity: 71.46%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:execute_task (lines 252-283) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let session = self.get_or_create_session(role, claude_config).await?;

        // Update session status to active
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, PersistentSessionStatus::Active)
            .await;

        // Execute task
        let result = {
            let mut agent = session.lock().await;
            agent.execute_task(task).await?
        };

        // Update session info
        self.update_session_activity(&agent_id).await;

        // Mark as idle after task completion
        self.update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:remove_session (lines 422-448) ---[0m
    async fn remove_session(
        sessions: &SessionStorage,
        session_info: &Arc<RwLock<HashMap<String, PersistentSessionInfo>>>,
        agent_id: &str,
    ) -> Result<()> {
        // Shutdown the agent
        {
            let sessions_guard = sessions.read().await;
            if let Some(session) = sessions_guard.get(agent_id) {
                let mut agent = session.lock().await;
                agent.shutdown().await?;
            }
        }

        // Remove from maps
        {
            let mut sessions_guard = sessions.write().await;
            sessions_guard.remove(agent_id);
        }

        {
            let mut info_guard = session_info.write().await;
            info_guard.remove(agent_id);
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/persistent_session.rs:170-193 method find_reusable_session <-> ./crates/ccswarm/src/session/persistent_session.rs:422-448 method remove_session
  Similarity: 82.06%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:find_reusable_session (lines 170-193) ---[0m
    async fn find_reusable_session(
        &self,
        role: &AgentRole,
    ) -> Option<Arc<Mutex<PersistentClaudeAgent>>> {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        for (agent_id, session) in sessions.iter() {
            if let Some(info) = session_info.get(agent_id) {
                if info.status == PersistentSessionStatus::Idle {
                    // Try to lock the agent, but don't block indefinitely
                    if let Ok(agent) = session.try_lock() {
                        if agent.identity.specialization.name() == role.name() {
                            // Found a reusable session
                            return Some(Arc::clone(session));
                        }
                    }
                    // If we can't lock, skip this session for now
                }
            }
        }

        None
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:remove_session (lines 422-448) ---[0m
    async fn remove_session(
        sessions: &SessionStorage,
        session_info: &Arc<RwLock<HashMap<String, PersistentSessionInfo>>>,
        agent_id: &str,
    ) -> Result<()> {
        // Shutdown the agent
        {
            let sessions_guard = sessions.read().await;
            if let Some(session) = sessions_guard.get(agent_id) {
                let mut agent = session.lock().await;
                agent.shutdown().await?;
            }
        }

        // Remove from maps
        {
            let mut sessions_guard = sessions.write().await;
            sessions_guard.remove(agent_id);
        }

        {
            let mut info_guard = session_info.write().await;
            info_guard.remove(agent_id);
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/persistent_session.rs:112-138 method start <-> ./crates/ccswarm/src/session/persistent_session.rs:141-167 method get_or_create_session
  Similarity: 75.91%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:start (lines 112-138) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting persistent session manager");

        let sessions = Arc::clone(&self.sessions);
        let session_info = Arc::clone(&self.session_info);
        let cleanup_interval = self.config.cleanup_interval;
        let idle_timeout = self.config.idle_timeout;
        let max_lifetime = self.config.max_session_lifetime;

        let cleanup_handle = tokio::spawn(async move {
            let mut interval = interval(cleanup_interval);

            loop {
                interval.tick().await;

                if let Err(e) =
                    Self::cleanup_sessions(&sessions, &session_info, idle_timeout, max_lifetime)
                        .await
                {
                    tracing::error!("Session cleanup error: {}", e);
                }
            }
        });

        self.cleanup_handle = Some(cleanup_handle);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_or_create_session (lines 141-167) ---[0m
    pub async fn get_or_create_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());

        // Check if we can reuse an existing session
        if self.config.enable_session_reuse {
            if let Some(existing) = self.find_reusable_session(&role).await {
                tracing::info!("Reusing existing session for role: {}", role.name());
                return Ok(existing);
            }
        }

        // Check session limits
        let current_count = self.sessions.read().await.len();
        if current_count >= self.config.max_concurrent_sessions {
            return Err(anyhow::anyhow!(
                "Maximum concurrent sessions reached: {}",
                self.config.max_concurrent_sessions
            ));
        }

        // Create new session
        self.create_new_session(agent_id, role, claude_config).await
    }

  ./crates/ccswarm/src/session/persistent_session.rs:112-138 method start <-> ./crates/ccswarm/src/session/persistent_session.rs:451-475 method shutdown
  Similarity: 78.37%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:start (lines 112-138) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting persistent session manager");

        let sessions = Arc::clone(&self.sessions);
        let session_info = Arc::clone(&self.session_info);
        let cleanup_interval = self.config.cleanup_interval;
        let idle_timeout = self.config.idle_timeout;
        let max_lifetime = self.config.max_session_lifetime;

        let cleanup_handle = tokio::spawn(async move {
            let mut interval = interval(cleanup_interval);

            loop {
                interval.tick().await;

                if let Err(e) =
                    Self::cleanup_sessions(&sessions, &session_info, idle_timeout, max_lifetime)
                        .await
                {
                    tracing::error!("Session cleanup error: {}", e);
                }
            }
        });

        self.cleanup_handle = Some(cleanup_handle);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:shutdown (lines 451-475) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down persistent session manager");

        // Cancel cleanup task
        if let Some(handle) = self.cleanup_handle.take() {
            handle.abort();
        }

        // Shutdown all sessions
        let agent_ids: Vec<String> = {
            let session_info = self.session_info.read().await;
            session_info.keys().cloned().collect()
        };

        for agent_id in agent_ids {
            if let Err(e) =
                Self::remove_session(&self.sessions, &self.session_info, &agent_id).await
            {
                tracing::error!("Failed to shutdown session {}: {}", agent_id, e);
            }
        }

        tracing::info!("Persistent session manager shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/persistent_session.rs:286-326 method execute_task_batch <-> ./crates/ccswarm/src/session/persistent_session.rs:566-581 function test_persistent_session_creation
  Similarity: 70.33%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:execute_task_batch (lines 286-326) ---[0m
    pub async fn execute_task_batch(
        &self,
        role: AgentRole,
        tasks: Vec<Task>,
        claude_config: ClaudeConfig,
    ) -> Result<Vec<TaskResult>> {
        if tasks.is_empty() {
            return Ok(Vec::new());
        }

        tracing::info!(
            "Executing batch of {} tasks for role: {}",
            tasks.len(),
            role.name()
        );

        let session = self.get_or_create_session(role, claude_config).await?;

        // Update session status to active
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, PersistentSessionStatus::Active)
            .await;

        // Execute batch - this amortizes the identity establishment cost
        let results = {
            let mut agent = session.lock().await;
            agent.execute_task_batch(tasks).await?
        };

        // Update session info
        self.update_session_activity(&agent_id).await;
        self.update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        tracing::info!("Batch execution completed successfully");
        Ok(results)
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_persistent_session_creation (lines 566-581) ---[0m
    async fn test_persistent_session_creation() {
        let temp_dir = TempDir::new().unwrap();
        let config = test_config();
        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        let claude_config = ClaudeConfig::default();
        let session = manager
            .get_or_create_session(default_frontend_role(), claude_config)
            .await
            .unwrap();

        assert_eq!(manager.sessions.read().await.len(), 1);

        let agent = session.lock().await;
        assert!(agent.identity.agent_id.contains("frontend"));
    }

  ./crates/ccswarm/src/session/persistent_session.rs:566-581 function test_persistent_session_creation <-> ./crates/ccswarm/src/session/persistent_session.rs:584-620 function test_session_reuse
  Similarity: 71.58%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_persistent_session_creation (lines 566-581) ---[0m
    async fn test_persistent_session_creation() {
        let temp_dir = TempDir::new().unwrap();
        let config = test_config();
        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        let claude_config = ClaudeConfig::default();
        let session = manager
            .get_or_create_session(default_frontend_role(), claude_config)
            .await
            .unwrap();

        assert_eq!(manager.sessions.read().await.len(), 1);

        let agent = session.lock().await;
        assert!(agent.identity.agent_id.contains("frontend"));
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_session_reuse (lines 584-620) ---[0m
    async fn test_session_reuse() {
        let temp_dir = TempDir::new().unwrap();
        let mut config = test_config();
        config.enable_session_reuse = true;

        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        let claude_config = ClaudeConfig::default();

        // Create first session
        let session1 = manager
            .get_or_create_session(default_frontend_role(), claude_config.clone())
            .await
            .unwrap();

        // Mark as idle - extract agent_id first then update
        let agent_id = {
            let agent = session1.lock().await;
            agent.identity.agent_id.clone()
        };

        manager
            .update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        // Request another session of same type
        let session2 = manager
            .get_or_create_session(default_frontend_role(), claude_config)
            .await
            .unwrap();

        // Should still have only 1 session (reused)
        assert_eq!(manager.sessions.read().await.len(), 1);

        // Sessions should be the same - check by Arc pointer equality
        assert!(Arc::ptr_eq(&session1, &session2));
    }

  ./crates/ccswarm/src/session/persistent_session.rs:252-283 method execute_task <-> ./crates/ccswarm/src/session/persistent_session.rs:566-581 function test_persistent_session_creation
  Similarity: 78.71%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:execute_task (lines 252-283) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let session = self.get_or_create_session(role, claude_config).await?;

        // Update session status to active
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, PersistentSessionStatus::Active)
            .await;

        // Execute task
        let result = {
            let mut agent = session.lock().await;
            agent.execute_task(task).await?
        };

        // Update session info
        self.update_session_activity(&agent_id).await;

        // Mark as idle after task completion
        self.update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_persistent_session_creation (lines 566-581) ---[0m
    async fn test_persistent_session_creation() {
        let temp_dir = TempDir::new().unwrap();
        let config = test_config();
        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        let claude_config = ClaudeConfig::default();
        let session = manager
            .get_or_create_session(default_frontend_role(), claude_config)
            .await
            .unwrap();

        assert_eq!(manager.sessions.read().await.len(), 1);

        let agent = session.lock().await;
        assert!(agent.identity.agent_id.contains("frontend"));
    }

  ./crates/ccswarm/src/session/persistent_session.rs:170-193 method find_reusable_session <-> ./crates/ccswarm/src/session/persistent_session.rs:451-475 method shutdown
  Similarity: 77.01%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:find_reusable_session (lines 170-193) ---[0m
    async fn find_reusable_session(
        &self,
        role: &AgentRole,
    ) -> Option<Arc<Mutex<PersistentClaudeAgent>>> {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        for (agent_id, session) in sessions.iter() {
            if let Some(info) = session_info.get(agent_id) {
                if info.status == PersistentSessionStatus::Idle {
                    // Try to lock the agent, but don't block indefinitely
                    if let Ok(agent) = session.try_lock() {
                        if agent.identity.specialization.name() == role.name() {
                            // Found a reusable session
                            return Some(Arc::clone(session));
                        }
                    }
                    // If we can't lock, skip this session for now
                }
            }
        }

        None
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:shutdown (lines 451-475) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down persistent session manager");

        // Cancel cleanup task
        if let Some(handle) = self.cleanup_handle.take() {
            handle.abort();
        }

        // Shutdown all sessions
        let agent_ids: Vec<String> = {
            let session_info = self.session_info.read().await;
            session_info.keys().cloned().collect()
        };

        for agent_id in agent_ids {
            if let Err(e) =
                Self::remove_session(&self.sessions, &self.session_info, &agent_id).await
            {
                tracing::error!("Failed to shutdown session {}: {}", agent_id, e);
            }
        }

        tracing::info!("Persistent session manager shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/persistent_session.rs:141-167 method get_or_create_session <-> ./crates/ccswarm/src/session/persistent_session.rs:170-193 method find_reusable_session
  Similarity: 73.70%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_or_create_session (lines 141-167) ---[0m
    pub async fn get_or_create_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());

        // Check if we can reuse an existing session
        if self.config.enable_session_reuse {
            if let Some(existing) = self.find_reusable_session(&role).await {
                tracing::info!("Reusing existing session for role: {}", role.name());
                return Ok(existing);
            }
        }

        // Check session limits
        let current_count = self.sessions.read().await.len();
        if current_count >= self.config.max_concurrent_sessions {
            return Err(anyhow::anyhow!(
                "Maximum concurrent sessions reached: {}",
                self.config.max_concurrent_sessions
            ));
        }

        // Create new session
        self.create_new_session(agent_id, role, claude_config).await
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:find_reusable_session (lines 170-193) ---[0m
    async fn find_reusable_session(
        &self,
        role: &AgentRole,
    ) -> Option<Arc<Mutex<PersistentClaudeAgent>>> {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        for (agent_id, session) in sessions.iter() {
            if let Some(info) = session_info.get(agent_id) {
                if info.status == PersistentSessionStatus::Idle {
                    // Try to lock the agent, but don't block indefinitely
                    if let Ok(agent) = session.try_lock() {
                        if agent.identity.specialization.name() == role.name() {
                            // Found a reusable session
                            return Some(Arc::clone(session));
                        }
                    }
                    // If we can't lock, skip this session for now
                }
            }
        }

        None
    }

  ./crates/ccswarm/src/session/persistent_session.rs:170-193 method find_reusable_session <-> ./crates/ccswarm/src/session/persistent_session.rs:363-377 method get_sessions_by_role
  Similarity: 91.00%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:find_reusable_session (lines 170-193) ---[0m
    async fn find_reusable_session(
        &self,
        role: &AgentRole,
    ) -> Option<Arc<Mutex<PersistentClaudeAgent>>> {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        for (agent_id, session) in sessions.iter() {
            if let Some(info) = session_info.get(agent_id) {
                if info.status == PersistentSessionStatus::Idle {
                    // Try to lock the agent, but don't block indefinitely
                    if let Ok(agent) = session.try_lock() {
                        if agent.identity.specialization.name() == role.name() {
                            // Found a reusable session
                            return Some(Arc::clone(session));
                        }
                    }
                    // If we can't lock, skip this session for now
                }
            }
        }

        None
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_sessions_by_role (lines 363-377) ---[0m
    pub async fn get_sessions_by_role(&self, role: &AgentRole) -> Vec<PersistentSessionInfo> {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        let mut result = Vec::new();
        for (agent_id, session) in sessions.iter() {
            if let Some(info) = session_info.get(agent_id) {
                let agent = session.lock().await;
                if agent.identity.specialization.name() == role.name() {
                    result.push(info.clone());
                }
            }
        }
        result
    }

  ./crates/ccswarm/src/session/persistent_session.rs:252-283 method execute_task <-> ./crates/ccswarm/src/session/persistent_session.rs:623-633 function test_efficiency_stats
  Similarity: 82.22%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:execute_task (lines 252-283) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let session = self.get_or_create_session(role, claude_config).await?;

        // Update session status to active
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, PersistentSessionStatus::Active)
            .await;

        // Execute task
        let result = {
            let mut agent = session.lock().await;
            agent.execute_task(task).await?
        };

        // Update session info
        self.update_session_activity(&agent_id).await;

        // Mark as idle after task completion
        self.update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_efficiency_stats (lines 623-633) ---[0m
    async fn test_efficiency_stats() {
        let temp_dir = TempDir::new().unwrap();
        let config = test_config();
        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        // Initially no sessions
        let stats = manager.get_efficiency_stats().await;
        assert_eq!(stats.total_sessions, 0);
        assert_eq!(stats.total_tasks_completed, 0);
        assert_eq!(stats.estimated_token_savings, 0);
    }

  ./crates/ccswarm/src/session/persistent_session.rs:141-167 method get_or_create_session <-> ./crates/ccswarm/src/session/persistent_session.rs:363-377 method get_sessions_by_role
  Similarity: 83.55%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_or_create_session (lines 141-167) ---[0m
    pub async fn get_or_create_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());

        // Check if we can reuse an existing session
        if self.config.enable_session_reuse {
            if let Some(existing) = self.find_reusable_session(&role).await {
                tracing::info!("Reusing existing session for role: {}", role.name());
                return Ok(existing);
            }
        }

        // Check session limits
        let current_count = self.sessions.read().await.len();
        if current_count >= self.config.max_concurrent_sessions {
            return Err(anyhow::anyhow!(
                "Maximum concurrent sessions reached: {}",
                self.config.max_concurrent_sessions
            ));
        }

        // Create new session
        self.create_new_session(agent_id, role, claude_config).await
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_sessions_by_role (lines 363-377) ---[0m
    pub async fn get_sessions_by_role(&self, role: &AgentRole) -> Vec<PersistentSessionInfo> {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        let mut result = Vec::new();
        for (agent_id, session) in sessions.iter() {
            if let Some(info) = session_info.get(agent_id) {
                let agent = session.lock().await;
                if agent.identity.specialization.name() == role.name() {
                    result.push(info.clone());
                }
            }
        }
        result
    }

  ./crates/ccswarm/src/session/persistent_session.rs:363-377 method get_sessions_by_role <-> ./crates/ccswarm/src/session/persistent_session.rs:422-448 method remove_session
  Similarity: 81.17%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_sessions_by_role (lines 363-377) ---[0m
    pub async fn get_sessions_by_role(&self, role: &AgentRole) -> Vec<PersistentSessionInfo> {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        let mut result = Vec::new();
        for (agent_id, session) in sessions.iter() {
            if let Some(info) = session_info.get(agent_id) {
                let agent = session.lock().await;
                if agent.identity.specialization.name() == role.name() {
                    result.push(info.clone());
                }
            }
        }
        result
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:remove_session (lines 422-448) ---[0m
    async fn remove_session(
        sessions: &SessionStorage,
        session_info: &Arc<RwLock<HashMap<String, PersistentSessionInfo>>>,
        agent_id: &str,
    ) -> Result<()> {
        // Shutdown the agent
        {
            let sessions_guard = sessions.read().await;
            if let Some(session) = sessions_guard.get(agent_id) {
                let mut agent = session.lock().await;
                agent.shutdown().await?;
            }
        }

        // Remove from maps
        {
            let mut sessions_guard = sessions.write().await;
            sessions_guard.remove(agent_id);
        }

        {
            let mut info_guard = session_info.write().await;
            info_guard.remove(agent_id);
        }

        Ok(())
    }

  ./crates/ccswarm/src/session/persistent_session.rs:112-138 method start <-> ./crates/ccswarm/src/session/persistent_session.rs:566-581 function test_persistent_session_creation
  Similarity: 78.84%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:start (lines 112-138) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting persistent session manager");

        let sessions = Arc::clone(&self.sessions);
        let session_info = Arc::clone(&self.session_info);
        let cleanup_interval = self.config.cleanup_interval;
        let idle_timeout = self.config.idle_timeout;
        let max_lifetime = self.config.max_session_lifetime;

        let cleanup_handle = tokio::spawn(async move {
            let mut interval = interval(cleanup_interval);

            loop {
                interval.tick().await;

                if let Err(e) =
                    Self::cleanup_sessions(&sessions, &session_info, idle_timeout, max_lifetime)
                        .await
                {
                    tracing::error!("Session cleanup error: {}", e);
                }
            }
        });

        self.cleanup_handle = Some(cleanup_handle);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_persistent_session_creation (lines 566-581) ---[0m
    async fn test_persistent_session_creation() {
        let temp_dir = TempDir::new().unwrap();
        let config = test_config();
        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        let claude_config = ClaudeConfig::default();
        let session = manager
            .get_or_create_session(default_frontend_role(), claude_config)
            .await
            .unwrap();

        assert_eq!(manager.sessions.read().await.len(), 1);

        let agent = session.lock().await;
        assert!(agent.identity.agent_id.contains("frontend"));
    }

  ./crates/ccswarm/src/session/persistent_session.rs:478-487 method create_env_vars <-> ./crates/ccswarm/src/session/persistent_session.rs:490-525 method get_efficiency_stats
  Similarity: 71.84%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:create_env_vars (lines 478-487) ---[0m
    fn create_env_vars(agent_id: &str) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), Uuid::new_v4().to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_efficiency_stats (lines 490-525) ---[0m
    pub async fn get_efficiency_stats(&self) -> EfficiencyStats {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        let total_sessions = sessions.len();
        let active_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Active)
            .count();
        let idle_sessions = session_info
            .values()
            .filter(|info| info.status == PersistentSessionStatus::Idle)
            .count();
        let total_tasks = session_info.values().map(|info| info.tasks_completed).sum();

        // Estimate token savings based on task reuse
        let estimated_token_savings = if total_tasks > total_sessions {
            // Each reused session saves ~3400 tokens
            (total_tasks - total_sessions) * 3400
        } else {
            0
        };

        EfficiencyStats {
            total_sessions,
            active_sessions,
            idle_sessions,
            total_tasks_completed: total_tasks,
            estimated_token_savings,
            session_reuse_rate: if total_tasks > 0 {
                (total_tasks - total_sessions) as f64 / total_tasks as f64
            } else {
                0.0
            },
        }
    }

  ./crates/ccswarm/src/session/persistent_session.rs:363-377 method get_sessions_by_role <-> ./crates/ccswarm/src/session/persistent_session.rs:451-475 method shutdown
  Similarity: 82.11%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_sessions_by_role (lines 363-377) ---[0m
    pub async fn get_sessions_by_role(&self, role: &AgentRole) -> Vec<PersistentSessionInfo> {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        let mut result = Vec::new();
        for (agent_id, session) in sessions.iter() {
            if let Some(info) = session_info.get(agent_id) {
                let agent = session.lock().await;
                if agent.identity.specialization.name() == role.name() {
                    result.push(info.clone());
                }
            }
        }
        result
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:shutdown (lines 451-475) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down persistent session manager");

        // Cancel cleanup task
        if let Some(handle) = self.cleanup_handle.take() {
            handle.abort();
        }

        // Shutdown all sessions
        let agent_ids: Vec<String> = {
            let session_info = self.session_info.read().await;
            session_info.keys().cloned().collect()
        };

        for agent_id in agent_ids {
            if let Err(e) =
                Self::remove_session(&self.sessions, &self.session_info, &agent_id).await
            {
                tracing::error!("Failed to shutdown session {}: {}", agent_id, e);
            }
        }

        tracing::info!("Persistent session manager shutdown complete");
        Ok(())
    }

  ./crates/ccswarm/src/session/persistent_session.rs:141-167 method get_or_create_session <-> ./crates/ccswarm/src/session/persistent_session.rs:566-581 function test_persistent_session_creation
  Similarity: 73.65%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_or_create_session (lines 141-167) ---[0m
    pub async fn get_or_create_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());

        // Check if we can reuse an existing session
        if self.config.enable_session_reuse {
            if let Some(existing) = self.find_reusable_session(&role).await {
                tracing::info!("Reusing existing session for role: {}", role.name());
                return Ok(existing);
            }
        }

        // Check session limits
        let current_count = self.sessions.read().await.len();
        if current_count >= self.config.max_concurrent_sessions {
            return Err(anyhow::anyhow!(
                "Maximum concurrent sessions reached: {}",
                self.config.max_concurrent_sessions
            ));
        }

        // Create new session
        self.create_new_session(agent_id, role, claude_config).await
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_persistent_session_creation (lines 566-581) ---[0m
    async fn test_persistent_session_creation() {
        let temp_dir = TempDir::new().unwrap();
        let config = test_config();
        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        let claude_config = ClaudeConfig::default();
        let session = manager
            .get_or_create_session(default_frontend_role(), claude_config)
            .await
            .unwrap();

        assert_eq!(manager.sessions.read().await.len(), 1);

        let agent = session.lock().await;
        assert!(agent.identity.agent_id.contains("frontend"));
    }

  ./crates/ccswarm/src/session/persistent_session.rs:141-167 method get_or_create_session <-> ./crates/ccswarm/src/session/persistent_session.rs:623-633 function test_efficiency_stats
  Similarity: 80.70%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_or_create_session (lines 141-167) ---[0m
    pub async fn get_or_create_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());

        // Check if we can reuse an existing session
        if self.config.enable_session_reuse {
            if let Some(existing) = self.find_reusable_session(&role).await {
                tracing::info!("Reusing existing session for role: {}", role.name());
                return Ok(existing);
            }
        }

        // Check session limits
        let current_count = self.sessions.read().await.len();
        if current_count >= self.config.max_concurrent_sessions {
            return Err(anyhow::anyhow!(
                "Maximum concurrent sessions reached: {}",
                self.config.max_concurrent_sessions
            ));
        }

        // Create new session
        self.create_new_session(agent_id, role, claude_config).await
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_efficiency_stats (lines 623-633) ---[0m
    async fn test_efficiency_stats() {
        let temp_dir = TempDir::new().unwrap();
        let config = test_config();
        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        // Initially no sessions
        let stats = manager.get_efficiency_stats().await;
        assert_eq!(stats.total_sessions, 0);
        assert_eq!(stats.total_tasks_completed, 0);
        assert_eq!(stats.estimated_token_savings, 0);
    }

  ./crates/ccswarm/src/session/persistent_session.rs:141-167 method get_or_create_session <-> ./crates/ccswarm/src/session/persistent_session.rs:478-487 method create_env_vars
  Similarity: 82.35%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_or_create_session (lines 141-167) ---[0m
    pub async fn get_or_create_session(
        &self,
        role: AgentRole,
        claude_config: ClaudeConfig,
    ) -> Result<Arc<Mutex<PersistentClaudeAgent>>> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());

        // Check if we can reuse an existing session
        if self.config.enable_session_reuse {
            if let Some(existing) = self.find_reusable_session(&role).await {
                tracing::info!("Reusing existing session for role: {}", role.name());
                return Ok(existing);
            }
        }

        // Check session limits
        let current_count = self.sessions.read().await.len();
        if current_count >= self.config.max_concurrent_sessions {
            return Err(anyhow::anyhow!(
                "Maximum concurrent sessions reached: {}",
                self.config.max_concurrent_sessions
            ));
        }

        // Create new session
        self.create_new_session(agent_id, role, claude_config).await
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:create_env_vars (lines 478-487) ---[0m
    fn create_env_vars(agent_id: &str) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), Uuid::new_v4().to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

  ./crates/ccswarm/src/session/persistent_session.rs:112-138 method start <-> ./crates/ccswarm/src/session/persistent_session.rs:623-633 function test_efficiency_stats
  Similarity: 78.93%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:start (lines 112-138) ---[0m
    pub async fn start(&mut self) -> Result<()> {
        tracing::info!("Starting persistent session manager");

        let sessions = Arc::clone(&self.sessions);
        let session_info = Arc::clone(&self.session_info);
        let cleanup_interval = self.config.cleanup_interval;
        let idle_timeout = self.config.idle_timeout;
        let max_lifetime = self.config.max_session_lifetime;

        let cleanup_handle = tokio::spawn(async move {
            let mut interval = interval(cleanup_interval);

            loop {
                interval.tick().await;

                if let Err(e) =
                    Self::cleanup_sessions(&sessions, &session_info, idle_timeout, max_lifetime)
                        .await
                {
                    tracing::error!("Session cleanup error: {}", e);
                }
            }
        });

        self.cleanup_handle = Some(cleanup_handle);
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_efficiency_stats (lines 623-633) ---[0m
    async fn test_efficiency_stats() {
        let temp_dir = TempDir::new().unwrap();
        let config = test_config();
        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        // Initially no sessions
        let stats = manager.get_efficiency_stats().await;
        assert_eq!(stats.total_sessions, 0);
        assert_eq!(stats.total_tasks_completed, 0);
        assert_eq!(stats.estimated_token_savings, 0);
    }

  ./crates/ccswarm/src/session/persistent_session.rs:451-475 method shutdown <-> ./crates/ccswarm/src/session/persistent_session.rs:566-581 function test_persistent_session_creation
  Similarity: 72.94%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:shutdown (lines 451-475) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down persistent session manager");

        // Cancel cleanup task
        if let Some(handle) = self.cleanup_handle.take() {
            handle.abort();
        }

        // Shutdown all sessions
        let agent_ids: Vec<String> = {
            let session_info = self.session_info.read().await;
            session_info.keys().cloned().collect()
        };

        for agent_id in agent_ids {
            if let Err(e) =
                Self::remove_session(&self.sessions, &self.session_info, &agent_id).await
            {
                tracing::error!("Failed to shutdown session {}: {}", agent_id, e);
            }
        }

        tracing::info!("Persistent session manager shutdown complete");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_persistent_session_creation (lines 566-581) ---[0m
    async fn test_persistent_session_creation() {
        let temp_dir = TempDir::new().unwrap();
        let config = test_config();
        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        let claude_config = ClaudeConfig::default();
        let session = manager
            .get_or_create_session(default_frontend_role(), claude_config)
            .await
            .unwrap();

        assert_eq!(manager.sessions.read().await.len(), 1);

        let agent = session.lock().await;
        assert!(agent.identity.agent_id.contains("frontend"));
    }

  ./crates/ccswarm/src/session/persistent_session.rs:252-283 method execute_task <-> ./crates/ccswarm/src/session/persistent_session.rs:478-487 method create_env_vars
  Similarity: 70.29%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:execute_task (lines 252-283) ---[0m
    pub async fn execute_task(
        &self,
        role: AgentRole,
        task: Task,
        claude_config: ClaudeConfig,
    ) -> Result<TaskResult> {
        let session = self.get_or_create_session(role, claude_config).await?;

        // Update session status to active
        let agent_id = {
            let agent = session.lock().await;
            agent.identity.agent_id.clone()
        };

        self.update_session_status(&agent_id, PersistentSessionStatus::Active)
            .await;

        // Execute task
        let result = {
            let mut agent = session.lock().await;
            agent.execute_task(task).await?
        };

        // Update session info
        self.update_session_activity(&agent_id).await;

        // Mark as idle after task completion
        self.update_session_status(&agent_id, PersistentSessionStatus::Idle)
            .await;

        Ok(result)
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:create_env_vars (lines 478-487) ---[0m
    fn create_env_vars(agent_id: &str) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), Uuid::new_v4().to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

  ./crates/ccswarm/src/session/persistent_session.rs:451-475 method shutdown <-> ./crates/ccswarm/src/session/persistent_session.rs:623-633 function test_efficiency_stats
  Similarity: 78.48%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:shutdown (lines 451-475) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down persistent session manager");

        // Cancel cleanup task
        if let Some(handle) = self.cleanup_handle.take() {
            handle.abort();
        }

        // Shutdown all sessions
        let agent_ids: Vec<String> = {
            let session_info = self.session_info.read().await;
            session_info.keys().cloned().collect()
        };

        for agent_id in agent_ids {
            if let Err(e) =
                Self::remove_session(&self.sessions, &self.session_info, &agent_id).await
            {
                tracing::error!("Failed to shutdown session {}: {}", agent_id, e);
            }
        }

        tracing::info!("Persistent session manager shutdown complete");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_efficiency_stats (lines 623-633) ---[0m
    async fn test_efficiency_stats() {
        let temp_dir = TempDir::new().unwrap();
        let config = test_config();
        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        // Initially no sessions
        let stats = manager.get_efficiency_stats().await;
        assert_eq!(stats.total_sessions, 0);
        assert_eq!(stats.total_tasks_completed, 0);
        assert_eq!(stats.estimated_token_savings, 0);
    }

  ./crates/ccswarm/src/session/persistent_session.rs:170-193 method find_reusable_session <-> ./crates/ccswarm/src/session/persistent_session.rs:478-487 method create_env_vars
  Similarity: 83.06%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:find_reusable_session (lines 170-193) ---[0m
    async fn find_reusable_session(
        &self,
        role: &AgentRole,
    ) -> Option<Arc<Mutex<PersistentClaudeAgent>>> {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        for (agent_id, session) in sessions.iter() {
            if let Some(info) = session_info.get(agent_id) {
                if info.status == PersistentSessionStatus::Idle {
                    // Try to lock the agent, but don't block indefinitely
                    if let Ok(agent) = session.try_lock() {
                        if agent.identity.specialization.name() == role.name() {
                            // Found a reusable session
                            return Some(Arc::clone(session));
                        }
                    }
                    // If we can't lock, skip this session for now
                }
            }
        }

        None
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:create_env_vars (lines 478-487) ---[0m
    fn create_env_vars(agent_id: &str) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), Uuid::new_v4().to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

  ./crates/ccswarm/src/session/persistent_session.rs:422-448 method remove_session <-> ./crates/ccswarm/src/session/persistent_session.rs:623-633 function test_efficiency_stats
  Similarity: 70.92%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:remove_session (lines 422-448) ---[0m
    async fn remove_session(
        sessions: &SessionStorage,
        session_info: &Arc<RwLock<HashMap<String, PersistentSessionInfo>>>,
        agent_id: &str,
    ) -> Result<()> {
        // Shutdown the agent
        {
            let sessions_guard = sessions.read().await;
            if let Some(session) = sessions_guard.get(agent_id) {
                let mut agent = session.lock().await;
                agent.shutdown().await?;
            }
        }

        // Remove from maps
        {
            let mut sessions_guard = sessions.write().await;
            sessions_guard.remove(agent_id);
        }

        {
            let mut info_guard = session_info.write().await;
            info_guard.remove(agent_id);
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_efficiency_stats (lines 623-633) ---[0m
    async fn test_efficiency_stats() {
        let temp_dir = TempDir::new().unwrap();
        let config = test_config();
        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        // Initially no sessions
        let stats = manager.get_efficiency_stats().await;
        assert_eq!(stats.total_sessions, 0);
        assert_eq!(stats.total_tasks_completed, 0);
        assert_eq!(stats.estimated_token_savings, 0);
    }

  ./crates/ccswarm/src/session/persistent_session.rs:451-475 method shutdown <-> ./crates/ccswarm/src/session/persistent_session.rs:478-487 method create_env_vars
  Similarity: 73.68%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:shutdown (lines 451-475) ---[0m
    pub async fn shutdown(&mut self) -> Result<()> {
        tracing::info!("Shutting down persistent session manager");

        // Cancel cleanup task
        if let Some(handle) = self.cleanup_handle.take() {
            handle.abort();
        }

        // Shutdown all sessions
        let agent_ids: Vec<String> = {
            let session_info = self.session_info.read().await;
            session_info.keys().cloned().collect()
        };

        for agent_id in agent_ids {
            if let Err(e) =
                Self::remove_session(&self.sessions, &self.session_info, &agent_id).await
            {
                tracing::error!("Failed to shutdown session {}: {}", agent_id, e);
            }
        }

        tracing::info!("Persistent session manager shutdown complete");
        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:create_env_vars (lines 478-487) ---[0m
    fn create_env_vars(agent_id: &str) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), Uuid::new_v4().to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

  ./crates/ccswarm/src/session/persistent_session.rs:170-193 method find_reusable_session <-> ./crates/ccswarm/src/session/persistent_session.rs:557-563 function test_persistent_session_manager_creation
  Similarity: 82.71%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:find_reusable_session (lines 170-193) ---[0m
    async fn find_reusable_session(
        &self,
        role: &AgentRole,
    ) -> Option<Arc<Mutex<PersistentClaudeAgent>>> {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        for (agent_id, session) in sessions.iter() {
            if let Some(info) = session_info.get(agent_id) {
                if info.status == PersistentSessionStatus::Idle {
                    // Try to lock the agent, but don't block indefinitely
                    if let Ok(agent) = session.try_lock() {
                        if agent.identity.specialization.name() == role.name() {
                            // Found a reusable session
                            return Some(Arc::clone(session));
                        }
                    }
                    // If we can't lock, skip this session for now
                }
            }
        }

        None
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_persistent_session_manager_creation (lines 557-563) ---[0m
    async fn test_persistent_session_manager_creation() {
        let temp_dir = TempDir::new().unwrap();
        let config = test_config();
        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        assert_eq!(manager.sessions.read().await.len(), 0);
    }

  ./crates/ccswarm/src/session/persistent_session.rs:422-448 method remove_session <-> ./crates/ccswarm/src/session/persistent_session.rs:557-563 function test_persistent_session_manager_creation
  Similarity: 72.04%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:remove_session (lines 422-448) ---[0m
    async fn remove_session(
        sessions: &SessionStorage,
        session_info: &Arc<RwLock<HashMap<String, PersistentSessionInfo>>>,
        agent_id: &str,
    ) -> Result<()> {
        // Shutdown the agent
        {
            let sessions_guard = sessions.read().await;
            if let Some(session) = sessions_guard.get(agent_id) {
                let mut agent = session.lock().await;
                agent.shutdown().await?;
            }
        }

        // Remove from maps
        {
            let mut sessions_guard = sessions.write().await;
            sessions_guard.remove(agent_id);
        }

        {
            let mut info_guard = session_info.write().await;
            info_guard.remove(agent_id);
        }

        Ok(())
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_persistent_session_manager_creation (lines 557-563) ---[0m
    async fn test_persistent_session_manager_creation() {
        let temp_dir = TempDir::new().unwrap();
        let config = test_config();
        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        assert_eq!(manager.sessions.read().await.len(), 0);
    }

  ./crates/ccswarm/src/session/persistent_session.rs:566-581 function test_persistent_session_creation <-> ./crates/ccswarm/src/session/persistent_session.rs:623-633 function test_efficiency_stats
  Similarity: 85.15%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_persistent_session_creation (lines 566-581) ---[0m
    async fn test_persistent_session_creation() {
        let temp_dir = TempDir::new().unwrap();
        let config = test_config();
        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        let claude_config = ClaudeConfig::default();
        let session = manager
            .get_or_create_session(default_frontend_role(), claude_config)
            .await
            .unwrap();

        assert_eq!(manager.sessions.read().await.len(), 1);

        let agent = session.lock().await;
        assert!(agent.identity.agent_id.contains("frontend"));
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_efficiency_stats (lines 623-633) ---[0m
    async fn test_efficiency_stats() {
        let temp_dir = TempDir::new().unwrap();
        let config = test_config();
        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        // Initially no sessions
        let stats = manager.get_efficiency_stats().await;
        assert_eq!(stats.total_sessions, 0);
        assert_eq!(stats.total_tasks_completed, 0);
        assert_eq!(stats.estimated_token_savings, 0);
    }

  ./crates/ccswarm/src/session/persistent_session.rs:363-377 method get_sessions_by_role <-> ./crates/ccswarm/src/session/persistent_session.rs:478-487 method create_env_vars
  Similarity: 78.77%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_sessions_by_role (lines 363-377) ---[0m
    pub async fn get_sessions_by_role(&self, role: &AgentRole) -> Vec<PersistentSessionInfo> {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        let mut result = Vec::new();
        for (agent_id, session) in sessions.iter() {
            if let Some(info) = session_info.get(agent_id) {
                let agent = session.lock().await;
                if agent.identity.specialization.name() == role.name() {
                    result.push(info.clone());
                }
            }
        }
        result
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:create_env_vars (lines 478-487) ---[0m
    fn create_env_vars(agent_id: &str) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), Uuid::new_v4().to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

  ./crates/ccswarm/src/session/persistent_session.rs:363-377 method get_sessions_by_role <-> ./crates/ccswarm/src/session/persistent_session.rs:557-563 function test_persistent_session_manager_creation
  Similarity: 79.57%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_sessions_by_role (lines 363-377) ---[0m
    pub async fn get_sessions_by_role(&self, role: &AgentRole) -> Vec<PersistentSessionInfo> {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        let mut result = Vec::new();
        for (agent_id, session) in sessions.iter() {
            if let Some(info) = session_info.get(agent_id) {
                let agent = session.lock().await;
                if agent.identity.specialization.name() == role.name() {
                    result.push(info.clone());
                }
            }
        }
        result
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_persistent_session_manager_creation (lines 557-563) ---[0m
    async fn test_persistent_session_manager_creation() {
        let temp_dir = TempDir::new().unwrap();
        let config = test_config();
        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        assert_eq!(manager.sessions.read().await.len(), 0);
    }

  ./crates/ccswarm/src/session/persistent_session.rs:69-77 method default <-> ./crates/ccswarm/src/session/persistent_session.rs:546-554 function test_config
  Similarity: 96.25%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:default (lines 69-77) ---[0m
    fn default() -> Self {
        Self {
            max_concurrent_sessions: 10,
            idle_timeout: Duration::from_secs(300), // 5 minutes
            cleanup_interval: Duration::from_secs(60), // 1 minute
            max_session_lifetime: Duration::from_secs(3600), // 1 hour
            enable_session_reuse: true,
        }
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_config (lines 546-554) ---[0m
    fn test_config() -> PersistentSessionManagerConfig {
        PersistentSessionManagerConfig {
            max_concurrent_sessions: 10,
            idle_timeout: Duration::from_millis(100), // 100ms for tests
            cleanup_interval: Duration::from_millis(50), // 50ms for tests
            max_session_lifetime: Duration::from_secs(10), // 10s for tests
            enable_session_reuse: false,
        }
    }

  ./crates/ccswarm/src/session/persistent_session.rs:346-354 method get_session_stats <-> ./crates/ccswarm/src/session/persistent_session.rs:363-377 method get_sessions_by_role
  Similarity: 70.12%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_session_stats (lines 346-354) ---[0m
    pub async fn get_session_stats(&self, agent_id: &str) -> Option<SessionStats> {
        let sessions = self.sessions.read().await;
        if let Some(session) = sessions.get(agent_id) {
            let agent = session.lock().await;
            Some(agent.get_session_stats().await)
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_sessions_by_role (lines 363-377) ---[0m
    pub async fn get_sessions_by_role(&self, role: &AgentRole) -> Vec<PersistentSessionInfo> {
        let sessions = self.sessions.read().await;
        let session_info = self.session_info.read().await;

        let mut result = Vec::new();
        for (agent_id, session) in sessions.iter() {
            if let Some(info) = session_info.get(agent_id) {
                let agent = session.lock().await;
                if agent.identity.specialization.name() == role.name() {
                    result.push(info.clone());
                }
            }
        }
        result
    }

  ./crates/ccswarm/src/session/persistent_session.rs:69-77 method default <-> ./crates/ccswarm/src/session/persistent_session.rs:101-109 method new
  Similarity: 88.36%
  Classes: Default <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:default (lines 69-77) ---[0m
    fn default() -> Self {
        Self {
            max_concurrent_sessions: 10,
            idle_timeout: Duration::from_secs(300), // 5 minutes
            cleanup_interval: Duration::from_secs(60), // 1 minute
            max_session_lifetime: Duration::from_secs(3600), // 1 hour
            enable_session_reuse: true,
        }
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:new (lines 101-109) ---[0m
    pub fn new(workspace_root: PathBuf, config: PersistentSessionManagerConfig) -> Self {
        Self {
            sessions: Arc::new(RwLock::new(HashMap::new())),
            session_info: Arc::new(RwLock::new(HashMap::new())),
            config,
            workspace_root,
            cleanup_handle: None,
        }
    }

  ./crates/ccswarm/src/session/persistent_session.rs:101-109 method new <-> ./crates/ccswarm/src/session/persistent_session.rs:546-554 function test_config
  Similarity: 87.95%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:new (lines 101-109) ---[0m
    pub fn new(workspace_root: PathBuf, config: PersistentSessionManagerConfig) -> Self {
        Self {
            sessions: Arc::new(RwLock::new(HashMap::new())),
            session_info: Arc::new(RwLock::new(HashMap::new())),
            config,
            workspace_root,
            cleanup_handle: None,
        }
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_config (lines 546-554) ---[0m
    fn test_config() -> PersistentSessionManagerConfig {
        PersistentSessionManagerConfig {
            max_concurrent_sessions: 10,
            idle_timeout: Duration::from_millis(100), // 100ms for tests
            cleanup_interval: Duration::from_millis(50), // 50ms for tests
            max_session_lifetime: Duration::from_secs(10), // 10s for tests
            enable_session_reuse: false,
        }
    }

  ./crates/ccswarm/src/session/persistent_session.rs:478-487 method create_env_vars <-> ./crates/ccswarm/src/session/persistent_session.rs:557-563 function test_persistent_session_manager_creation
  Similarity: 78.90%

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:create_env_vars (lines 478-487) ---[0m
    fn create_env_vars(agent_id: &str) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), Uuid::new_v4().to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:test_persistent_session_manager_creation (lines 557-563) ---[0m
    async fn test_persistent_session_manager_creation() {
        let temp_dir = TempDir::new().unwrap();
        let config = test_config();
        let manager = PersistentSessionManager::new(temp_dir.path().to_path_buf(), config);

        assert_eq!(manager.sessions.read().await.len(), 0);
    }

  ./crates/ccswarm/src/session/persistent_session.rs:329-335 method update_session_activity <-> ./crates/ccswarm/src/session/persistent_session.rs:346-354 method get_session_stats
  Similarity: 81.36%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:update_session_activity (lines 329-335) ---[0m
    async fn update_session_activity(&self, agent_id: &str) {
        let mut session_info = self.session_info.write().await;
        if let Some(info) = session_info.get_mut(agent_id) {
            info.last_activity = Utc::now();
            info.tasks_completed += 1;
        }
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_session_stats (lines 346-354) ---[0m
    pub async fn get_session_stats(&self, agent_id: &str) -> Option<SessionStats> {
        let sessions = self.sessions.read().await;
        if let Some(session) = sessions.get(agent_id) {
            let agent = session.lock().await;
            Some(agent.get_session_stats().await)
        } else {
            None
        }
    }

  ./crates/ccswarm/src/session/persistent_session.rs:338-343 method update_session_status <-> ./crates/ccswarm/src/session/persistent_session.rs:346-354 method get_session_stats
  Similarity: 83.95%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:update_session_status (lines 338-343) ---[0m
    async fn update_session_status(&self, agent_id: &str, status: PersistentSessionStatus) {
        let mut session_info = self.session_info.write().await;
        if let Some(info) = session_info.get_mut(agent_id) {
            info.status = status;
        }
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_session_stats (lines 346-354) ---[0m
    pub async fn get_session_stats(&self, agent_id: &str) -> Option<SessionStats> {
        let sessions = self.sessions.read().await;
        if let Some(session) = sessions.get(agent_id) {
            let agent = session.lock().await;
            Some(agent.get_session_stats().await)
        } else {
            None
        }
    }

  ./crates/ccswarm/src/session/persistent_session.rs:329-335 method update_session_activity <-> ./crates/ccswarm/src/session/persistent_session.rs:338-343 method update_session_status
  Similarity: 95.36%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:update_session_activity (lines 329-335) ---[0m
    async fn update_session_activity(&self, agent_id: &str) {
        let mut session_info = self.session_info.write().await;
        if let Some(info) = session_info.get_mut(agent_id) {
            info.last_activity = Utc::now();
            info.tasks_completed += 1;
        }
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:update_session_status (lines 338-343) ---[0m
    async fn update_session_status(&self, agent_id: &str, status: PersistentSessionStatus) {
        let mut session_info = self.session_info.write().await;
        if let Some(info) = session_info.get_mut(agent_id) {
            info.status = status;
        }
    }

  ./crates/ccswarm/src/session/persistent_session.rs:346-354 method get_session_stats <-> ./crates/ccswarm/src/session/persistent_session.rs:357-360 method list_sessions
  Similarity: 83.70%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:get_session_stats (lines 346-354) ---[0m
    pub async fn get_session_stats(&self, agent_id: &str) -> Option<SessionStats> {
        let sessions = self.sessions.read().await;
        if let Some(session) = sessions.get(agent_id) {
            let agent = session.lock().await;
            Some(agent.get_session_stats().await)
        } else {
            None
        }
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:list_sessions (lines 357-360) ---[0m
    pub async fn list_sessions(&self) -> Vec<PersistentSessionInfo> {
        let session_info = self.session_info.read().await;
        session_info.values().cloned().collect()
    }

  ./crates/ccswarm/src/session/persistent_session.rs:329-335 method update_session_activity <-> ./crates/ccswarm/src/session/persistent_session.rs:357-360 method list_sessions
  Similarity: 88.55%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:update_session_activity (lines 329-335) ---[0m
    async fn update_session_activity(&self, agent_id: &str) {
        let mut session_info = self.session_info.write().await;
        if let Some(info) = session_info.get_mut(agent_id) {
            info.last_activity = Utc::now();
            info.tasks_completed += 1;
        }
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:list_sessions (lines 357-360) ---[0m
    pub async fn list_sessions(&self) -> Vec<PersistentSessionInfo> {
        let session_info = self.session_info.read().await;
        session_info.values().cloned().collect()
    }

  ./crates/ccswarm/src/session/persistent_session.rs:338-343 method update_session_status <-> ./crates/ccswarm/src/session/persistent_session.rs:357-360 method list_sessions
  Similarity: 85.09%
  Classes: PersistentSessionManager <-> PersistentSessionManager

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:update_session_status (lines 338-343) ---[0m
    async fn update_session_status(&self, agent_id: &str, status: PersistentSessionStatus) {
        let mut session_info = self.session_info.write().await;
        if let Some(info) = session_info.get_mut(agent_id) {
            info.status = status;
        }
    }

[36m--- ./crates/ccswarm/src/session/persistent_session.rs:list_sessions (lines 357-360) ---[0m
    pub async fn list_sessions(&self) -> Vec<PersistentSessionInfo> {
        let session_info = self.session_info.read().await;
        session_info.values().cloned().collect()
    }


Duplicates in ./crates/ccswarm/src/agent/task.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/agent/task.rs:48-64 method from_str <-> ./crates/ccswarm/src/agent/task.rs:68-83 method fmt
  Similarity: 75.43%
  Classes: FromStr <-> TaskType

[36m--- ./crates/ccswarm/src/agent/task.rs:from_str (lines 48-64) ---[0m
    fn from_str(s: &str) -> Result<Self, Self::Err> {
        match s.to_lowercase().as_str() {
            "development" | "dev" => Ok(TaskType::Development),
            "testing" | "test" => Ok(TaskType::Testing),
            "documentation" | "docs" => Ok(TaskType::Documentation),
            "infrastructure" | "infra" => Ok(TaskType::Infrastructure),
            "coordination" | "coord" => Ok(TaskType::Coordination),
            "review" => Ok(TaskType::Review),
            "bugfix" => Ok(TaskType::Bugfix),
            "bug" => Ok(TaskType::Bug),
            "feature" | "feat" => Ok(TaskType::Feature),
            "remediation" | "fix" => Ok(TaskType::Remediation),
            "assistance" | "assist" | "help" => Ok(TaskType::Assistance),
            "research" | "investigate" => Ok(TaskType::Research),
            _ => Err(anyhow::anyhow!("Unknown task type: {}", s)),
        }
    }

[36m--- ./crates/ccswarm/src/agent/task.rs:fmt (lines 68-83) ---[0m
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            TaskType::Development => write!(f, "Development"),
            TaskType::Testing => write!(f, "Testing"),
            TaskType::Documentation => write!(f, "Documentation"),
            TaskType::Infrastructure => write!(f, "Infrastructure"),
            TaskType::Coordination => write!(f, "Coordination"),
            TaskType::Review => write!(f, "Review"),
            TaskType::Bugfix => write!(f, "Bugfix"),
            TaskType::Bug => write!(f, "Bug"),
            TaskType::Feature => write!(f, "Feature"),
            TaskType::Remediation => write!(f, "Remediation"),
            TaskType::Assistance => write!(f, "Assistance"),
            TaskType::Research => write!(f, "Research"),
        }
    }

  ./crates/ccswarm/src/agent/task.rs:211-222 function test_task_creation <-> ./crates/ccswarm/src/agent/task.rs:240-254 function test_task_result
  Similarity: 71.39%

[36m--- ./crates/ccswarm/src/agent/task.rs:test_task_creation (lines 211-222) ---[0m
    fn test_task_creation() {
        let task = Task::new(
            "test-1".to_string(),
            "Create user component".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        assert_eq!(task.id, "test-1");
        assert_eq!(task.priority, Priority::Medium);
        assert!(task.details.is_none());
    }

[36m--- ./crates/ccswarm/src/agent/task.rs:test_task_result (lines 240-254) ---[0m
    fn test_task_result() {
        let success_result = TaskResult::success(
            serde_json::json!({"status": "completed"}),
            Duration::from_secs(120),
        );

        assert!(success_result.success);
        assert!(success_result.error.is_none());

        let failure_result =
            TaskResult::failure("Connection timeout".to_string(), Duration::from_secs(30));

        assert!(!failure_result.success);
        assert_eq!(failure_result.error, Some("Connection timeout".to_string()));
    }

  ./crates/ccswarm/src/agent/task.rs:211-222 function test_task_creation <-> ./crates/ccswarm/src/agent/task.rs:225-237 function test_task_with_details
  Similarity: 76.64%

[36m--- ./crates/ccswarm/src/agent/task.rs:test_task_creation (lines 211-222) ---[0m
    fn test_task_creation() {
        let task = Task::new(
            "test-1".to_string(),
            "Create user component".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        assert_eq!(task.id, "test-1");
        assert_eq!(task.priority, Priority::Medium);
        assert!(task.details.is_none());
    }

[36m--- ./crates/ccswarm/src/agent/task.rs:test_task_with_details (lines 225-237) ---[0m
    fn test_task_with_details() {
        let task = Task::new(
            "test-2".to_string(),
            "Fix API bug".to_string(),
            Priority::High,
            TaskType::Bugfix,
        )
        .with_details("Users getting 500 errors on login".to_string())
        .with_duration(3600);

        assert!(task.details.is_some());
        assert_eq!(task.estimated_duration, Some(3600));
    }


Duplicates in ./crates/ccswarm/src/utils/command.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/utils/command.rs:14-42 method run <-> ./crates/ccswarm/src/utils/command.rs:57-92 method run_with_output
  Similarity: 78.27%
  Classes: CommandExecutor <-> CommandExecutor

[36m--- ./crates/ccswarm/src/utils/command.rs:run (lines 14-42) ---[0m
    pub async fn run(cmd: &str, args: &[&str], dir: Option<&Path>) -> Result<String> {
        let mut command = Command::new(cmd);
        command.args(args);

        if let Some(dir) = dir {
            command.current_dir(dir);
        }

        command.stdout(Stdio::piped());
        command.stderr(Stdio::piped());

        let output = command.output().await.context(format!(
            "Failed to execute command: {} {}",
            cmd,
            args.join(" ")
        ))?;

        if !output.status.success() {
            let stderr = String::from_utf8_lossy(&output.stderr);
            anyhow::bail!(
                "Command failed: {} {}\nStderr: {}",
                cmd,
                args.join(" "),
                stderr
            );
        }

        Ok(String::from_utf8_lossy(&output.stdout).to_string())
    }

[36m--- ./crates/ccswarm/src/utils/command.rs:run_with_output (lines 57-92) ---[0m
    pub async fn run_with_output(
        cmd: &str,
        args: &[&str],
        dir: Option<&Path>,
    ) -> Result<(String, String)> {
        let mut command = Command::new(cmd);
        command.args(args);

        if let Some(dir) = dir {
            command.current_dir(dir);
        }

        command.stdout(Stdio::piped());
        command.stderr(Stdio::piped());

        let output = command.output().await.context(format!(
            "Failed to execute command: {} {}",
            cmd,
            args.join(" ")
        ))?;

        let stdout = String::from_utf8_lossy(&output.stdout).to_string();
        let stderr = String::from_utf8_lossy(&output.stderr).to_string();

        if !output.status.success() {
            anyhow::bail!(
                "Command failed: {} {}\nStdout: {}\nStderr: {}",
                cmd,
                args.join(" "),
                stdout,
                stderr
            );
        }

        Ok((stdout, stderr))
    }

  ./crates/ccswarm/src/utils/command.rs:45-54 method run_with_timeout <-> ./crates/ccswarm/src/utils/command.rs:95-102 method command_exists
  Similarity: 79.37%
  Classes: CommandExecutor <-> CommandExecutor

[36m--- ./crates/ccswarm/src/utils/command.rs:run_with_timeout (lines 45-54) ---[0m
    pub async fn run_with_timeout(
        cmd: &str,
        args: &[&str],
        dir: Option<&Path>,
        timeout: Duration,
    ) -> Result<String> {
        tokio::time::timeout(timeout, Self::run(cmd, args, dir))
            .await
            .context("Command timed out")?
    }

[36m--- ./crates/ccswarm/src/utils/command.rs:command_exists (lines 95-102) ---[0m
    pub async fn command_exists(cmd: &str) -> bool {
        Command::new("which")
            .arg(cmd)
            .output()
            .await
            .map(|output| output.status.success())
            .unwrap_or(false)
    }


Duplicates in ./crates/ccswarm/examples/orchestrator_demo.rs:
------------------------------------------------------------
  ./crates/ccswarm/examples/orchestrator_demo.rs:62-94 function demo_frontend_orchestration <-> ./crates/ccswarm/examples/orchestrator_demo.rs:117-152 function demo_quality_check_orchestration
  Similarity: 84.38%

[36m--- ./crates/ccswarm/examples/orchestrator_demo.rs:demo_frontend_orchestration (lines 62-94) ---[0m
async fn demo_frontend_orchestration(agent: &SimpleClaudeAgent) -> Result<()> {
    let task = TaskBuilder::new(
        "Create a new dashboard component with real-time data updates".to_string(),
    )
    .priority(Priority::High)
    .build();

    println!("Task: {}", task.description);
    println!("Analyzing and planning orchestration...\n");

    // Analyze task to create plan
    let plan = agent.analyze_task(&task).await?;

    println!("📊 Orchestration Plan:");
    for (i, step) in plan.steps.iter().enumerate() {
        println!("  Step {}: {} ({:?})", i + 1, step.name, step.step_type);
        if !step.parallel_tasks.is_empty() {
            println!("    Parallel tasks:");
            for task in &step.parallel_tasks {
                println!(
                    "      - {} {}",
                    if task.critical { "⚠️" } else { "📌" },
                    task.name
                );
            }
        }
    }

    // In a real implementation, this would execute the plan
    println!("\n✅ Plan created successfully!");

    Ok(())
}

[36m--- ./crates/ccswarm/examples/orchestrator_demo.rs:demo_quality_check_orchestration (lines 117-152) ---[0m
async fn demo_quality_check_orchestration(agent: &SimpleClaudeAgent) -> Result<()> {
    let task =
        TaskBuilder::new("Run comprehensive quality checks: test, lint, and format".to_string())
            .build();

    println!("Task: {}", task.description);
    println!("Setting up quality check orchestration...\n");

    let plan = agent.analyze_task(&task).await?;

    println!("📊 Quality Check Plan:");
    for step in &plan.steps {
        match step.step_type {
            StepType::Analysis => {
                println!("1️⃣ {} - Understand project structure", step.name);
            }
            StepType::Validation => {
                println!("2️⃣ {} - Run all checks in parallel", step.name);
                for task in &step.parallel_tasks {
                    println!("   ⚡ {}", task.name);
                }
            }
            StepType::Execution => {
                println!("3️⃣ {} - Fix any issues found", step.name);
            }
            _ => {}
        }
    }

    println!("\n💡 Benefits of orchestration:");
    println!("  • Parallel execution saves time");
    println!("  • Adaptive planning handles failures");
    println!("  • Clear progress tracking");

    Ok(())
}

  ./crates/ccswarm/examples/orchestrator_demo.rs:96-115 function demo_backend_orchestration <-> ./crates/ccswarm/examples/orchestrator_demo.rs:117-152 function demo_quality_check_orchestration
  Similarity: 94.72%

[36m--- ./crates/ccswarm/examples/orchestrator_demo.rs:demo_backend_orchestration (lines 96-115) ---[0m
async fn demo_backend_orchestration(agent: &SimpleClaudeAgent) -> Result<()> {
    let task =
        TaskBuilder::new("Implement user authentication API with JWT tokens".to_string()).build();

    println!("Task: {}", task.description);
    println!("Creating orchestration plan...\n");

    let plan = agent.analyze_task(&task).await?;

    // Show the adaptive nature of planning
    println!("📊 Initial Plan:");
    print_plan_summary(&plan);

    // Simulate finding that we need database migrations
    println!("\n🔄 Adapting plan based on analysis...");
    println!("  → Discovered need for database migrations");
    println!("  → Adding migration step before implementation");

    Ok(())
}

[36m--- ./crates/ccswarm/examples/orchestrator_demo.rs:demo_quality_check_orchestration (lines 117-152) ---[0m
async fn demo_quality_check_orchestration(agent: &SimpleClaudeAgent) -> Result<()> {
    let task =
        TaskBuilder::new("Run comprehensive quality checks: test, lint, and format".to_string())
            .build();

    println!("Task: {}", task.description);
    println!("Setting up quality check orchestration...\n");

    let plan = agent.analyze_task(&task).await?;

    println!("📊 Quality Check Plan:");
    for step in &plan.steps {
        match step.step_type {
            StepType::Analysis => {
                println!("1️⃣ {} - Understand project structure", step.name);
            }
            StepType::Validation => {
                println!("2️⃣ {} - Run all checks in parallel", step.name);
                for task in &step.parallel_tasks {
                    println!("   ⚡ {}", task.name);
                }
            }
            StepType::Execution => {
                println!("3️⃣ {} - Fix any issues found", step.name);
            }
            _ => {}
        }
    }

    println!("\n💡 Benefits of orchestration:");
    println!("  • Parallel execution saves time");
    println!("  • Adaptive planning handles failures");
    println!("  • Clear progress tracking");

    Ok(())
}

  ./crates/ccswarm/examples/orchestrator_demo.rs:62-94 function demo_frontend_orchestration <-> ./crates/ccswarm/examples/orchestrator_demo.rs:96-115 function demo_backend_orchestration
  Similarity: 87.90%

[36m--- ./crates/ccswarm/examples/orchestrator_demo.rs:demo_frontend_orchestration (lines 62-94) ---[0m
async fn demo_frontend_orchestration(agent: &SimpleClaudeAgent) -> Result<()> {
    let task = TaskBuilder::new(
        "Create a new dashboard component with real-time data updates".to_string(),
    )
    .priority(Priority::High)
    .build();

    println!("Task: {}", task.description);
    println!("Analyzing and planning orchestration...\n");

    // Analyze task to create plan
    let plan = agent.analyze_task(&task).await?;

    println!("📊 Orchestration Plan:");
    for (i, step) in plan.steps.iter().enumerate() {
        println!("  Step {}: {} ({:?})", i + 1, step.name, step.step_type);
        if !step.parallel_tasks.is_empty() {
            println!("    Parallel tasks:");
            for task in &step.parallel_tasks {
                println!(
                    "      - {} {}",
                    if task.critical { "⚠️" } else { "📌" },
                    task.name
                );
            }
        }
    }

    // In a real implementation, this would execute the plan
    println!("\n✅ Plan created successfully!");

    Ok(())
}

[36m--- ./crates/ccswarm/examples/orchestrator_demo.rs:demo_backend_orchestration (lines 96-115) ---[0m
async fn demo_backend_orchestration(agent: &SimpleClaudeAgent) -> Result<()> {
    let task =
        TaskBuilder::new("Implement user authentication API with JWT tokens".to_string()).build();

    println!("Task: {}", task.description);
    println!("Creating orchestration plan...\n");

    let plan = agent.analyze_task(&task).await?;

    // Show the adaptive nature of planning
    println!("📊 Initial Plan:");
    print_plan_summary(&plan);

    // Simulate finding that we need database migrations
    println!("\n🔄 Adapting plan based on analysis...");
    println!("  → Discovered need for database migrations");
    println!("  → Adding migration step before implementation");

    Ok(())
}


Duplicates in ./crates/ccswarm/src/agent/simple.rs:
------------------------------------------------------------
  ./crates/ccswarm/src/agent/simple.rs:43-73 method new <-> ./crates/ccswarm/src/agent/simple.rs:157-189 method simulate_task_execution
  Similarity: 90.13%
  Classes: SimpleClaudeAgent <-> SimpleClaudeAgent

[36m--- ./crates/ccswarm/src/agent/simple.rs:new (lines 43-73) ---[0m
    pub async fn new(
        role: AgentRole,
        workspace_root: &std::path::Path,
        claude_config: ClaudeConfig,
    ) -> Result<Self> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());
        let session_id = Uuid::new_v4().to_string();
        let workspace_path = workspace_root.join("agents").join(&agent_id);

        let identity = AgentIdentity {
            agent_id: agent_id.clone(),
            specialization: role,
            workspace_path: workspace_path.clone(),
            env_vars: Self::create_env_vars(&agent_id, &session_id),
            session_id,
            parent_process_id: std::process::id().to_string(),
            initialized_at: Utc::now(),
        };

        let agent = Self {
            identity,
            workspace_path,
            claude_config,
            status: AgentStatus::Initializing,
            current_task: None,
            task_history: Vec::new(),
            last_activity: Utc::now(),
        };

        Ok(agent)
    }

[36m--- ./crates/ccswarm/src/agent/simple.rs:simulate_task_execution (lines 157-189) ---[0m
    async fn simulate_task_execution(&self, task: &Task) -> Result<SimulatedResult> {
        // エージェントの専門性に基づいてタスクを評価
        let can_handle = self.can_handle_task(task);

        if !can_handle {
            return Ok(SimulatedResult {
                is_success: false,
                error_message: format!(
                    "Task '{}' is outside my specialization as a {} agent",
                    task.description,
                    self.identity.specialization.name()
                ),
                output: String::new(),
                notes: vec![
                    "Task delegation recommended".to_string(),
                    format!("Should be handled by appropriate specialist agent"),
                ],
            });
        }

        // 成功をシミュレート
        tokio::time::sleep(std::time::Duration::from_millis(100)).await;

        Ok(SimulatedResult {
            is_success: true,
            error_message: String::new(),
            output: format!("Successfully completed: {}", task.description),
            notes: vec![
                format!("Processed by {} agent", self.identity.specialization.name()),
                "Task completed within agent boundaries".to_string(),
            ],
        })
    }

  ./crates/ccswarm/src/agent/simple.rs:91-113 method initialize <-> ./crates/ccswarm/src/agent/simple.rs:116-154 method execute_task
  Similarity: 75.95%
  Classes: SimpleClaudeAgent <-> SimpleClaudeAgent

[36m--- ./crates/ccswarm/src/agent/simple.rs:initialize (lines 91-113) ---[0m
    pub async fn initialize(&mut self, workspace_manager: &SimpleWorkspaceManager) -> Result<()> {
        tracing::info!("Initializing simple agent: {}", self.identity.agent_id);

        // ワークスペースを作成
        workspace_manager
            .create_workspace(&self.identity.agent_id)
            .await?;

        // CLAUDE.mdを生成
        let claude_md_content = crate::agent::claude::generate_claude_md_content(&self.identity);
        workspace_manager
            .setup_claude_config(&self.identity.agent_id, &claude_md_content)
            .await?;

        self.status = AgentStatus::Available;
        self.last_activity = Utc::now();

        tracing::info!(
            "Simple agent {} initialized successfully",
            self.identity.agent_id
        );
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/simple.rs:execute_task (lines 116-154) ---[0m
    pub async fn execute_task(&mut self, task: Task) -> Result<TaskResult> {
        tracing::info!(
            "Agent {} executing task: {}",
            self.identity.agent_id,
            task.id
        );

        self.status = AgentStatus::Working;
        self.current_task = Some(task.clone());
        self.last_activity = Utc::now();

        let start_time = std::time::Instant::now();

        // シミュレートされたタスク実行
        let result = self.simulate_task_execution(&task).await?;

        let duration = start_time.elapsed();
        let task_result = if result.is_success {
            TaskResult::success(
                serde_json::json!({
                    "agent_id": self.identity.agent_id,
                    "task_id": task.id,
                    "result": result.output,
                    "notes": result.notes
                }),
                duration,
            )
        } else {
            TaskResult::failure(result.error_message, duration)
        };

        // タスク履歴に追加
        self.task_history.push((task, task_result.clone()));
        self.current_task = None;
        self.status = AgentStatus::Available;
        self.last_activity = Utc::now();

        Ok(task_result)
    }

  ./crates/ccswarm/src/agent/simple.rs:116-154 method execute_task <-> ./crates/ccswarm/src/agent/simple.rs:275-297 function test_task_handling
  Similarity: 72.01%

[36m--- ./crates/ccswarm/src/agent/simple.rs:execute_task (lines 116-154) ---[0m
    pub async fn execute_task(&mut self, task: Task) -> Result<TaskResult> {
        tracing::info!(
            "Agent {} executing task: {}",
            self.identity.agent_id,
            task.id
        );

        self.status = AgentStatus::Working;
        self.current_task = Some(task.clone());
        self.last_activity = Utc::now();

        let start_time = std::time::Instant::now();

        // シミュレートされたタスク実行
        let result = self.simulate_task_execution(&task).await?;

        let duration = start_time.elapsed();
        let task_result = if result.is_success {
            TaskResult::success(
                serde_json::json!({
                    "agent_id": self.identity.agent_id,
                    "task_id": task.id,
                    "result": result.output,
                    "notes": result.notes
                }),
                duration,
            )
        } else {
            TaskResult::failure(result.error_message, duration)
        };

        // タスク履歴に追加
        self.task_history.push((task, task_result.clone()));
        self.current_task = None;
        self.status = AgentStatus::Available;
        self.last_activity = Utc::now();

        Ok(task_result)
    }

[36m--- ./crates/ccswarm/src/agent/simple.rs:test_task_handling (lines 275-297) ---[0m
    async fn test_task_handling() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_manager = SimpleWorkspaceManager::new(temp_dir.path().to_path_buf());
        workspace_manager.init_if_needed().await.unwrap();

        let config = ClaudeConfig::for_agent("frontend");
        let mut agent = SimpleClaudeAgent::new(default_frontend_role(), temp_dir.path(), config)
            .await
            .unwrap();

        agent.initialize(&workspace_manager).await.unwrap();

        let task = Task::new(
            "test-1".to_string(),
            "Create React component".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let result = agent.execute_task(task).await.unwrap();
        assert!(result.success);
        assert_eq!(agent.task_history.len(), 1);
    }

  ./crates/ccswarm/src/agent/simple.rs:91-113 method initialize <-> ./crates/ccswarm/src/agent/simple.rs:157-189 method simulate_task_execution
  Similarity: 78.10%
  Classes: SimpleClaudeAgent <-> SimpleClaudeAgent

[36m--- ./crates/ccswarm/src/agent/simple.rs:initialize (lines 91-113) ---[0m
    pub async fn initialize(&mut self, workspace_manager: &SimpleWorkspaceManager) -> Result<()> {
        tracing::info!("Initializing simple agent: {}", self.identity.agent_id);

        // ワークスペースを作成
        workspace_manager
            .create_workspace(&self.identity.agent_id)
            .await?;

        // CLAUDE.mdを生成
        let claude_md_content = crate::agent::claude::generate_claude_md_content(&self.identity);
        workspace_manager
            .setup_claude_config(&self.identity.agent_id, &claude_md_content)
            .await?;

        self.status = AgentStatus::Available;
        self.last_activity = Utc::now();

        tracing::info!(
            "Simple agent {} initialized successfully",
            self.identity.agent_id
        );
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/simple.rs:simulate_task_execution (lines 157-189) ---[0m
    async fn simulate_task_execution(&self, task: &Task) -> Result<SimulatedResult> {
        // エージェントの専門性に基づいてタスクを評価
        let can_handle = self.can_handle_task(task);

        if !can_handle {
            return Ok(SimulatedResult {
                is_success: false,
                error_message: format!(
                    "Task '{}' is outside my specialization as a {} agent",
                    task.description,
                    self.identity.specialization.name()
                ),
                output: String::new(),
                notes: vec![
                    "Task delegation recommended".to_string(),
                    format!("Should be handled by appropriate specialist agent"),
                ],
            });
        }

        // 成功をシミュレート
        tokio::time::sleep(std::time::Duration::from_millis(100)).await;

        Ok(SimulatedResult {
            is_success: true,
            error_message: String::new(),
            output: format!("Successfully completed: {}", task.description),
            notes: vec![
                format!("Processed by {} agent", self.identity.specialization.name()),
                "Task completed within agent boundaries".to_string(),
            ],
        })
    }

  ./crates/ccswarm/src/agent/simple.rs:43-73 method new <-> ./crates/ccswarm/src/agent/simple.rs:91-113 method initialize
  Similarity: 78.33%
  Classes: SimpleClaudeAgent <-> SimpleClaudeAgent

[36m--- ./crates/ccswarm/src/agent/simple.rs:new (lines 43-73) ---[0m
    pub async fn new(
        role: AgentRole,
        workspace_root: &std::path::Path,
        claude_config: ClaudeConfig,
    ) -> Result<Self> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());
        let session_id = Uuid::new_v4().to_string();
        let workspace_path = workspace_root.join("agents").join(&agent_id);

        let identity = AgentIdentity {
            agent_id: agent_id.clone(),
            specialization: role,
            workspace_path: workspace_path.clone(),
            env_vars: Self::create_env_vars(&agent_id, &session_id),
            session_id,
            parent_process_id: std::process::id().to_string(),
            initialized_at: Utc::now(),
        };

        let agent = Self {
            identity,
            workspace_path,
            claude_config,
            status: AgentStatus::Initializing,
            current_task: None,
            task_history: Vec::new(),
            last_activity: Utc::now(),
        };

        Ok(agent)
    }

[36m--- ./crates/ccswarm/src/agent/simple.rs:initialize (lines 91-113) ---[0m
    pub async fn initialize(&mut self, workspace_manager: &SimpleWorkspaceManager) -> Result<()> {
        tracing::info!("Initializing simple agent: {}", self.identity.agent_id);

        // ワークスペースを作成
        workspace_manager
            .create_workspace(&self.identity.agent_id)
            .await?;

        // CLAUDE.mdを生成
        let claude_md_content = crate::agent::claude::generate_claude_md_content(&self.identity);
        workspace_manager
            .setup_claude_config(&self.identity.agent_id, &claude_md_content)
            .await?;

        self.status = AgentStatus::Available;
        self.last_activity = Utc::now();

        tracing::info!(
            "Simple agent {} initialized successfully",
            self.identity.agent_id
        );
        Ok(())
    }

  ./crates/ccswarm/src/agent/simple.rs:76-88 method create_env_vars <-> ./crates/ccswarm/src/agent/simple.rs:157-189 method simulate_task_execution
  Similarity: 90.00%
  Classes: SimpleClaudeAgent <-> SimpleClaudeAgent

[36m--- ./crates/ccswarm/src/agent/simple.rs:create_env_vars (lines 76-88) ---[0m
    fn create_env_vars(
        agent_id: &str,
        session_id: &str,
    ) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

[36m--- ./crates/ccswarm/src/agent/simple.rs:simulate_task_execution (lines 157-189) ---[0m
    async fn simulate_task_execution(&self, task: &Task) -> Result<SimulatedResult> {
        // エージェントの専門性に基づいてタスクを評価
        let can_handle = self.can_handle_task(task);

        if !can_handle {
            return Ok(SimulatedResult {
                is_success: false,
                error_message: format!(
                    "Task '{}' is outside my specialization as a {} agent",
                    task.description,
                    self.identity.specialization.name()
                ),
                output: String::new(),
                notes: vec![
                    "Task delegation recommended".to_string(),
                    format!("Should be handled by appropriate specialist agent"),
                ],
            });
        }

        // 成功をシミュレート
        tokio::time::sleep(std::time::Duration::from_millis(100)).await;

        Ok(SimulatedResult {
            is_success: true,
            error_message: String::new(),
            output: format!("Successfully completed: {}", task.description),
            notes: vec![
                format!("Processed by {} agent", self.identity.specialization.name()),
                "Task completed within agent boundaries".to_string(),
            ],
        })
    }

  ./crates/ccswarm/src/agent/simple.rs:43-73 method new <-> ./crates/ccswarm/src/agent/simple.rs:76-88 method create_env_vars
  Similarity: 88.91%
  Classes: SimpleClaudeAgent <-> SimpleClaudeAgent

[36m--- ./crates/ccswarm/src/agent/simple.rs:new (lines 43-73) ---[0m
    pub async fn new(
        role: AgentRole,
        workspace_root: &std::path::Path,
        claude_config: ClaudeConfig,
    ) -> Result<Self> {
        let agent_id = format!("{}-agent-{}", role.name().to_lowercase(), Uuid::new_v4());
        let session_id = Uuid::new_v4().to_string();
        let workspace_path = workspace_root.join("agents").join(&agent_id);

        let identity = AgentIdentity {
            agent_id: agent_id.clone(),
            specialization: role,
            workspace_path: workspace_path.clone(),
            env_vars: Self::create_env_vars(&agent_id, &session_id),
            session_id,
            parent_process_id: std::process::id().to_string(),
            initialized_at: Utc::now(),
        };

        let agent = Self {
            identity,
            workspace_path,
            claude_config,
            status: AgentStatus::Initializing,
            current_task: None,
            task_history: Vec::new(),
            last_activity: Utc::now(),
        };

        Ok(agent)
    }

[36m--- ./crates/ccswarm/src/agent/simple.rs:create_env_vars (lines 76-88) ---[0m
    fn create_env_vars(
        agent_id: &str,
        session_id: &str,
    ) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

  ./crates/ccswarm/src/agent/simple.rs:91-113 method initialize <-> ./crates/ccswarm/src/agent/simple.rs:275-297 function test_task_handling
  Similarity: 81.08%

[36m--- ./crates/ccswarm/src/agent/simple.rs:initialize (lines 91-113) ---[0m
    pub async fn initialize(&mut self, workspace_manager: &SimpleWorkspaceManager) -> Result<()> {
        tracing::info!("Initializing simple agent: {}", self.identity.agent_id);

        // ワークスペースを作成
        workspace_manager
            .create_workspace(&self.identity.agent_id)
            .await?;

        // CLAUDE.mdを生成
        let claude_md_content = crate::agent::claude::generate_claude_md_content(&self.identity);
        workspace_manager
            .setup_claude_config(&self.identity.agent_id, &claude_md_content)
            .await?;

        self.status = AgentStatus::Available;
        self.last_activity = Utc::now();

        tracing::info!(
            "Simple agent {} initialized successfully",
            self.identity.agent_id
        );
        Ok(())
    }

[36m--- ./crates/ccswarm/src/agent/simple.rs:test_task_handling (lines 275-297) ---[0m
    async fn test_task_handling() {
        let temp_dir = TempDir::new().unwrap();
        let workspace_manager = SimpleWorkspaceManager::new(temp_dir.path().to_path_buf());
        workspace_manager.init_if_needed().await.unwrap();

        let config = ClaudeConfig::for_agent("frontend");
        let mut agent = SimpleClaudeAgent::new(default_frontend_role(), temp_dir.path(), config)
            .await
            .unwrap();

        agent.initialize(&workspace_manager).await.unwrap();

        let task = Task::new(
            "test-1".to_string(),
            "Create React component".to_string(),
            Priority::Medium,
            TaskType::Development,
        );

        let result = agent.execute_task(task).await.unwrap();
        assert!(result.success);
        assert_eq!(agent.task_history.len(), 1);
    }

  ./crates/ccswarm/src/agent/simple.rs:76-88 method create_env_vars <-> ./crates/ccswarm/src/agent/simple.rs:262-272 function test_simple_agent_creation
  Similarity: 81.94%

[36m--- ./crates/ccswarm/src/agent/simple.rs:create_env_vars (lines 76-88) ---[0m
    fn create_env_vars(
        agent_id: &str,
        session_id: &str,
    ) -> std::collections::HashMap<String, String> {
        let mut env_vars = std::collections::HashMap::new();
        env_vars.insert("CCSWARM_AGENT_ID".to_string(), agent_id.to_string());
        env_vars.insert("CCSWARM_SESSION_ID".to_string(), session_id.to_string());
        env_vars.insert(
            "CCSWARM_ROLE".to_string(),
            agent_id.split('-').next().unwrap_or("unknown").to_string(),
        );
        env_vars
    }

[36m--- ./crates/ccswarm/src/agent/simple.rs:test_simple_agent_creation (lines 262-272) ---[0m
    async fn test_simple_agent_creation() {
        let temp_dir = TempDir::new().unwrap();
        let config = ClaudeConfig::for_agent("frontend");

        let agent = SimpleClaudeAgent::new(default_frontend_role(), temp_dir.path(), config)
            .await
            .unwrap();

        assert!(agent.identity.agent_id.contains("frontend"));
        assert_eq!(agent.status, AgentStatus::Initializing);
    }


Total duplicate pairs found: 4075
